# CLAUDE.md — Multi-LLM Agent Harness Workspace

*Workspace-level guidance for Claude Code at Phase 7 execution. Loaded at every Claude Code session startup. Canonical pointer to design-phase artifacts.*

---

## 1. Project framing

### 1.1 What this workspace builds

This workspace implements **H_T** (the target harness) — a multi-LLM agent harness — under the **H_E** execution surface (Claude Code CLI). H_T is specified by the design-phase artifact corpus enumerated at §2. H_T implementation traverses the per-axis atomic-unit plans at §2.4 in topological-sort order per per-axis-plan dependency graphs. H_E provides the development environment, IDE substrate, and bounded substitutions for not-yet-built H_T primitives (see §4).

The harness has four design axes plus a cross-axis composition surface:

| Axis | Scope |
|---|---|
| **IS** — Information Substrate | State ledger (6-field hash-chained entries per C-IS-05 §5), content-addressed index, semantic cache, filesystem-path classification |
| **AS** — Action Surface | Tool contracts (typed I/O schemas), MCP integration (FastMCP host + client), sandbox (4-tier blast radius), skills filesystem |
| **CP** — Control Plane | Routing (capability-aware multi-LLM), retry / breaker / idempotency, workflow lifecycle, topology (6-class enum), HITL placement |
| **OD** — Operational Discipline | HITL primitives (4-response palette), audit ledger schema, cost attribution (5-step chain), observability (12-namespace OTel schema) |
| **CXA** — Cross-Axis Composition | 99 canonical cross-axis relationships across 7 composition buckets per `Cross_Axis_Composition_Document_v2_6.md` §2.3 (29 genuine typed seams; 46 convention-level; 24 phase-2-runtime); §2.3.7 CP→OD bucket extended 2→7 typed seams at v2.6 (Phase A.2 composer-arc absorption: ValidatorFramework + PauseResumeProtocol + PerServerTrustEvaluator + HITL webhook delivery + HITL operator-burden — all 5 sharing the `cp_audit_to_od_audit` converter with distinct F2 action_id prefixes); v2.9 amendment owed for cost-attribution audit-write seam (handoff §6, paired with U-CP-72 implementation; v2.7 path-β citation absorption + v2.8 path-γ citation absorption preceded without resolving cost-attribution) |

### 1.2 What this workspace is NOT

| NOT | Why |
|---|---|
| A H_T design extension surface | H_T design is canonical at design-phase artifacts (§2); design extensions route to design-phase back-flow (§4.3) |
| A re-litigation surface for ADR / spec / plan decisions | Decisions are P3-CK / P5-CK / P6-CK cleared; revision requires explicit back-flow trigger |
| A Claude Code customization workspace | This workspace is H_T build context; Claude Code user-level config goes at `~/.claude/` |
| A documentation workspace | Documentation generated by H_T at execution-time per per-axis-plan unit acceptance criteria |

### 1.3 Canonical authority chain

ADR (foundational F1–F5 + derivative D1–D6) → ADD v1.3 → PRD v1.1 → per-axis spec v1.x → per-axis plan v2.x + CXA v2.1 → Phase 7 implementation (this workspace).

Earlier artifacts in the chain are canonical for later artifacts. Conflicts route to design-phase back-flow per §4.3.

---

## 2. Canonical artifact pointers

All canonical artifacts reside at the **design-phase workspace** (separate Claude.ai project; not co-resident with this workspace). When this `CLAUDE.md` cites a canonical artifact, consult the design-phase workspace copy. Citation byte-exact discipline per `Project_Workflow_v1_8.md` §7.4.2.

### 2.1 Governance + execution discipline

| Artifact | Role |
|---|---|
| `Project_Workflow_v1_8.md` | Canonical workflow governance; §2.6.5 Phase 6.5 arc; §2.7 Phase 7 execution discipline; §2.7.6 back-flow routing; §7.4 fidelity-grammar |
| `Phase_7_Meta_Architecture_v1.md` | H_T ↔ H_E substitution mapping (§5 — 49 entries); anti-leakage rules (§7 — 18 axis + 3 cross-cutting); self-hosting milestone gradient (§6) |
| `Phase_7_Kickoff_Prompt.md` | Phase 7 entry framing; back-flow routing reference at §6 |
| `Target_Stack_Commitment_v1.md` | Stack commitment (see §3) |
| `Plan_Executability_Audit_v1.md` | Plan executability validation; framework-pull discipline reference |
| `Canonical_Substrate_Inventory.md` | KB navigation anchor; canonical-vs-superseded disambiguation |

### 2.2 ADR + ADD + PRD

| Artifact | Version | Role |
|---|---|---|
| ADR-F1 | v1.2 | Multi-LLM commitment |
| ADR-F2 | v1.2 | State ledger primitive |
| ADR-F3 | v1.1 | Index primitive |
| ADR-F4 | v1.1 | Workflow lifecycle primitive |
| ADR-F5 | v1.1 | Observability substrate primitive |
| ADR-D1 | v1.2 | HITL primitive |
| ADR-D2 | v1.2 | Sandbox tier |
| ADR-D3 | v1.2 | Validation contract |
| ADR-D4 | v1.1 | Cost attribution |
| ADR-D5 | v1.4 | Topology pattern — §1.4 storage-form reconciliation (SQLite → JSONL via IS composition) + §1.4.1 `entry_hash` recipe tightening per U-RT-59 Fork 2 Path B-revised-a (2026-05-20) |
| ADR-D6 | v1.2 | OTel schema (12 namespaces) |
| `Architectural_Design_Document_v1_3.md` | v1.3 | ADR consolidation; coherent architectural overview |
| `PRD_v1_1.md` | v1.1 | Product requirements; observable behavior |

### 2.3 Per-axis specs (Phase 5 canonical — contract authority)

| Axis | Spec |
|---|---|
| IS | `Spec_Information_Substrate_v1.md` (v1.2 per ADD v1.3 attestation) |
| AS | `Spec_Action_Surface_v1.md` (v1.5 — Class 1 fork H_T-CP-16+17 §16 ratified absorption: NEW §14.7 producer-site reference footer note repointing `memory.*` 6-attr namespace emission to runtime spec v1.17 §14.5.1 + §14.12 C-RT-22 callback-invocation sites; documents structural-pattern divergence from §14.3 mcp.* (per-callback emission vs per-dispatch per ADR-D3 §1.1 #11 client-side classification); §14.6 `files.*` footer NOT authored (Files arc deferred per §14.C); 2026-05-23. v1.4 — Phase A.2 ratified-drafts apply pass: §14.3 + §15 producer-site reference footer notes documenting v1.13 runtime contracts consume v1.4 namespaces (2026-05-21). v1.3 — C-AS-02/§11.1 `sandbox_tier_floor` reconciliation per S2 + C-AS-05 §5.1 3-param `fetch_secret` per S3, 2026-05-15) |
| CP | `Spec_Control_Plane_v1_11.md` (v1.11 — Path γ enum identifier rename absorption: §26.2 `PauseReason` → `WorkflowPauseReason` 5-class workflow-layer pause taxonomy disambiguation from OLD §22.1 `PauseReason` 4-class engine-layer per Class 1 fork resolution at `.harness/class_1_fork_u_cp_63_pause_reason_collision.md`; NEW §26 NOTE making §22 ↔ §26 engine-layer vs workflow-layer coexistence explicit (2026-05-21); v1.10 NEW §25/§26/§27/§17.4 substantive content preserved verbatim outside the §26.2 single-identifier rename) |
| OD | `Spec_Operational_Discipline_v1_9.md` (v1.9 — Path γ enum citation absorption: §C-OD-30.1 `pause.reason` attribute type cite `PauseReason per CP spec v1.10 §26.2` → `WorkflowPauseReason per CP spec v1.11 §26.2` (2026-05-21); v1.8 NEW §C-OD-25..33 substantive content preserved verbatim outside the single attribute-type cite cell) |
| Runtime | `Spec_Harness_Runtime_v1.md` (v1.17 — Class 1 fork H_T-CP-16+17 §16 ratified absorption per `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` (2026-05-23, same session as §11 PROVISIONAL filing + §13 systems-architect Mode 3 recommendation + §14 routing): NEW §14.12 C-RT-22 `MemoryToolRegistry` + `MemoryToolStorageBackendProtocol` contract (5 CRUD callbacks per ADR-D3 §1.1 #11 — `view`/`create`/`delete`/`str_replace`/`insert` on `/memories` paths; 3 NEW fail classes; stage-5 `materialize_memory_tool_registry_stage` factory); NEW §14.5.1 sub-section at C-RT-15 documenting Memory tool storage-backend callback binding (inner-loop mechanism deferred to implementation discretion per FM-2); NEW optional field `memory_tool_backend_config` at §3 C-RT-02 RuntimeConfig; NEW field `memory_tool_registry` at §4 C-RT-04 HarnessContext; operator-opt-in RETIRE-READY pattern preserved per §16 §6.D (structural criterion-B MET at landing arc; full RETIRED gated on operator-bound config + local-filesystem-backend e2e per §16 §6.C v2 C.vii local-fs scope; S3 deferred). NO Files API contract authoring at v1.17 — Files arc deferred indefinitely per §14.C. ZERO cross-axis cascade per §13.6 architect recommendation — CXA v2.8 unchanged; co-published with AS spec v1.5 §14.7 footer note. v1.16 — Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 post-cluster-4-OD-E partial close `128ab4f`): §3 C-RT-02 `sandbox_decision_policy` carrier-home cite re-pointed from `(AS spec v1.3 §15 carrier)` → `(harness-core carrier)` per Q1=C-i ratification — empirical phantom-cite verification: AS spec v1.3 §15/C-AS-15 is `secret.fetch` span schema not a sandbox policy class, ZERO `SandboxDecisionPolicy` hits across all axis source trees at filing time; single-token-edit citation-correction patch under FM-2 no-extension discipline; ZERO cross-axis cascade; §14.9.1 step 5 dangling forward-cite + filing-footer staleness + v1.15 historical cite preservation surfaced as adjacent findings (not patched). v1.15 substantive content preserved verbatim — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW §14.11 C-RT-21 `RetryBreakerToolDispatcher` sibling carrier (retry-only MVP per Q1=B + Q1a=(i); no breaker; no fallback chain); NEW factory contracts at §14.9.3 (stage-3a `materialize_mcp_client_host_stage` + stage-5 `materialize_runtime_tool_dispatcher_stage` 5-step composition body); NEW optional fields at §3 C-RT-02 RuntimeConfig (`trust_policy` + `sandbox_decision_policy`); NEW fields at §4 C-RT-04 HarnessContext (`mcp_client_host` stage 3a + `tool_dispatcher` stage 5 (bound to wrapper) + `per_server_trust_evaluator` + `mcp_namespace_emitter`); amended §14.9.2 inv 4 / §14.9.3 stage-5 prose / §14.9.6 inv 6 prose; new fail class `RT-FAIL-TOOL-RETRY-EXHAUSTED` per Q2=B2 bootstrap-chain decomposition (2026-05-22); v1.14 path-β citation preserved verbatim; v1.13 NEW §14.9 + §14.10 contracts preserved verbatim) |

### 2.4 Per-axis plans (Phase 6 canonical — execution authority)

| Axis | Plan | Unit count |
|---|---|---|
| core | `Implementation_Plan_Harness_Core_v1_2.md` | 2 units (U-CORE-01, U-CORE-02); v1.1 carrier-thin Class 1 fork resolution preserved verbatim (WorkflowEvent payload model struck 2026-05-15); v1.2 — NEW U-CORE-02 `SandboxDecisionPolicy` empty-marker carrier added per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` Q1=C-i ratification 2026-05-22 (re-home from phantom "AS spec v1.3 §15 carrier" cite to harness-core package; empty-marker shape per X-AL-3 no-spec-extension discipline — frozen Pydantic v2 BaseModel with NO fields + `.default()` factory; consumed by `harness-runtime` U-RT-71 at runtime plan v2.13); co-published with runtime spec v1.16 + runtime plan v2.13 this arc |
| IS | `Implementation_Plan_Information_Substrate_v2_3.md` | 17 units (U-IS-01 – U-IS-17) |
| AS | `Implementation_Plan_Action_Surface_v1_2.md` | 33 units (U-AS-01 – U-AS-33) |
| CP | `Implementation_Plan_Control_Plane_v2_17.md` | 73 units (U-CP-00, U-CP-00b, U-CP-00c, U-CP-01 – U-CP-72); v2.16 prior absorptions preserved; v2.17 — Path γ citation absorption: U-CP-62 carrier `PauseReason` → `WorkflowPauseReason` rename + U-CP-63 AC #4 (line-121 NotImplementedError closure) STRUCK + U-CP-63/64 Files-column note amended per CP spec v1.11 §26 NEW NOTE coexistence (line 121/143 belong to OLD §22 / U-CP-49 surface, NOT NEW §26 class-method surface), per Class 1 fork resolution at `.harness/class_1_fork_u_cp_63_pause_reason_collision.md` (2026-05-21); DAG + coverage preserved verbatim |
| OD | `Implementation_Plan_Operational_Discipline_v2_15.md` | 55 units (U-OD-00, U-OD-01 – U-OD-54); v2.14 prior absorptions preserved; v2.15 — Path γ citation absorption: U-OD-51 plan-body Implements line + AC #3 spec-version cite bumps (OD spec v1.8 → v1.9; CP spec v1.10 §26.4 → v1.11 §26.4) absorbing WorkflowPauseReason rename byte-exact (2026-05-21); attribute count + Pattern-P1 alignment + cross-axis edges + DAG preserved verbatim |
| Runtime | `Implementation_Plan_Harness_Runtime_v2_13.md` | 76 units (U-RT-00..U-RT-70 + U-RT-71..U-RT-75); v2.12 prior + v2.13 — `SandboxDecisionPolicy` Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 Q1=C-i): U-RT-71 + U-RT-75 cite-edited (runtime spec v1.15 → v1.16 absorption); U-RT-71 gains NEW within-axis-cross-package dependency edge on U-CORE-02 (harness-core plan v1.2 — co-published); no AC body change; no unit re-decomposition; no contract addition; single-token-class cite-edit absorption patch under FM-2 no-extension discipline; ZERO cross-axis cascade per fork doc §5. v2.12 prior — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW L9-septies cluster (5 units U-RT-71..U-RT-75) decomposing Q2=B2 bootstrap-wiring chain — RuntimeConfig schema extension + HarnessContext schema extension + stage-3a `materialize_mcp_client_host_stage` factory + C-RT-21 `RetryBreakerToolDispatcher` class body + stage-5 `materialize_runtime_tool_dispatcher_stage` factory; U-RT-68 REWRITTEN at v2.12 per Q1=B + Q1a=(i) ratification (stage-5 callsite consuming new factory; original v2.11 AC #2 with literal `RetryBreakerFallbackDispatcher` reuse STRUCK); ZERO cross-axis cascade per fork doc §5; L9-sexies preserved verbatim except U-RT-68 (2026-05-22) |
| CXA | `Cross_Axis_Composition_Document_v2_8.md` | canonical cross-axis relationships per v2.8 (path-γ citation reconciliation 2026-05-21); v2.6 cardinality preserved (99 aggregate / 29 genuine / 46 convention / 24 phase-2-runtime); v2.8 amendment scope: §2.3.7 row 6 (C-CP-26 §26 producer) spec-version cite bumps CP v1.10 → v1.11 + OD v1.8 → v1.9; §2.3.7 row 8 cost-attribution audit-write seam **STILL OWED** at future v2.9 amendment (paired with U-CP-72 implementation per handoff §6 — v2.8 path-γ-only publication advances counter without resolving cost-attribution) |

### 2.5 Per-axis subdirectory `CLAUDE.md` pointers

| Subdirectory | CLAUDE.md role |
|---|---|
| `harness-is/CLAUDE.md` | IS-axis scope; plan unit entry-points; IS substitution surface (9 entries); IS anti-leakage rules |
| `harness-as/CLAUDE.md` | AS-axis scope; plan unit entry-points; AS substitution surface (6 entries); AS anti-leakage rules |
| `harness-cp/CLAUDE.md` | CP-axis scope; plan unit entry-points; CP substitution surface (21 entries); CP anti-leakage rules |
| `harness-od/CLAUDE.md` | OD-axis scope; plan unit entry-points; OD substitution surface (8 entries); OD anti-leakage rules |

`harness-core/` hosts shared types + cross-axis utilities. `harness-cxa/` hosts CXA seam instantiation (5 CXA-axis substitution entries; CXA-AL-1 anti-leakage); cross-axis edges per CXA v2.1 §2.3.

---

## 3. Stack discipline

### 3.1 Committed stack

Per `Target_Stack_Commitment_v1.md` §5.1 + §5.2:

| Decision area | Commitment |
|---|---|
| Programming language | Python 3.12+ |
| Type contracts | Pydantic v2 |
| Concurrency primitive | `asyncio` |
| Package manager | `uv` (workspace-aware) |
| Type checker | `pyright` (strict mode) |
| Linter / formatter | `ruff` (single tool) |
| Test runner | `pytest` + `pytest-asyncio` |
| Repo structure | Monorepo with axis-subdirectory uv workspace (`harness-{is,as,cp,od,cxa}/` + shared `harness-core/`) |
| Git posture | Conventional commits; commit-per-unit-cluster; PR-per-axis-cluster; feature branches off `main` |
| CI substrate | Deferred to post-bootstrap milestone |
| Multi-LLM SDK stance | Per-provider official SDKs (`anthropic`, `openai`, `ollama`) under capability-aware abstraction per ADR-F1 v1.2; **NOT LiteLLM** |
| OTel libraries | `opentelemetry-api` + `opentelemetry-sdk` + `opentelemetry-exporter-otlp` + `opentelemetry-instrumentation-genai` (selective contrib adoption) |
| MCP host + client | `modelcontextprotocol/python-sdk` (FastMCP) |
| Secrets | `python-keyring` |
| Reliability primitives | Hand-rolled retry / breaker / idempotency (NO framework) per `Plan_Executability_Audit_v1.md` framework-pull discipline |

### 3.2 Framework-pull discipline

Per `Plan_Executability_Audit_v1.md`:

| Discipline | Statement |
|---|---|
| Bounded adoption | Framework adoption is bounded. New frameworks pulled in only if no existing-stack solution meets the unit's acceptance criteria |
| Reliability primitives | Hand-rolled. Do NOT pull `tenacity`, `pybreaker`, `circuitbreaker`, or equivalent framework libraries |
| Workflow orchestration | Hand-rolled per CP-axis plan units. Do NOT pull `prefect`, `temporal`, `langgraph`, `crewai`, `langchain`, `llamaindex`, or equivalent |
| Validation | Pydantic v2 + hand-rolled deterministic gates per AS-axis + CP-axis plan units. Do NOT pull `pydantic-validators-plus` or equivalent |
| Multi-LLM abstraction | Hand-rolled capability-aware abstraction per ADR-F1 v1.2. Do NOT pull LiteLLM |

### 3.3 Repo layout

```
<workspace_root>/
├── CLAUDE.md                                # This file
├── Sub_Agent_Boundary_Specification_v1.md   # §5 sub-agent boundary
├── .claude/
│   └── skills/                              # Phase 7-specific skills (§6)
│       ├── phase-7-implementation/SKILL.md
│       ├── phase-7-cross-axis-composition/SKILL.md
│       ├── phase-7-substitution-retirement/SKILL.md
│       └── phase-7-back-flow-routing/SKILL.md
├── pyproject.toml                           # uv workspace root
├── uv.lock
├── harness-core/                            # Shared types + cross-axis utilities
│   ├── pyproject.toml
│   └── ...
├── harness-is/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-as/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-cp/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-od/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
└── harness-cxa/                             # CXA seam instantiation
    ├── pyproject.toml
    └── ...
```

---

## 4. Substitution + back-flow discipline

### 4.1 H_T ↔ H_E substitution discipline

Per `Phase_7_Meta_Architecture_v1.md` §5 (49-row substitution mapping table) + §6 (self-hosting milestone gradient) + §7 (18 axis + 3 cross-cutting anti-leakage rules):

H_E provides bounded substitutions for not-yet-built H_T primitives across 6 substitution-mechanism categories:

| Mechanism | Count | Examples |
|---|---|---|
| H_E-direct | 11 | H_E filesystem ops; `Edit` / `Read` / `Write` tools |
| MCP-server | 12 | Substitution routed through MCP server boundary |
| Convention | 9 | Operator-authored `CLAUDE.md` / prompts |
| Shell-out | 8 | `Bash` invocations of `git`, `sha256sum`, `python -c` |
| Manual | 5 | Operator-driven gates (review, approval) |
| Authoring-only | 4 | Substitutions retired at authoring close |

Per-axis substitution count: IS=9 / AS=6 / CP=21 / OD=8 / CXA=5.

### 4.2 Substitution retirement discipline

Per X-AL-2 (Meta-Architecture §7.7):

> Retirement = (cited unit IDs landed) ∧ (substituted H_E surface no longer invoked at substitution site). Both conditions required.

Partial retirement is non-retirement. The `phase-7-substitution-retirement` skill at `.claude/skills/` governs retirement event discipline. Substitution retirement triggers throughout 7b–7d sub-phases, not only at 7d.

### 4.3 Back-flow routing

Phase 7 execution-time forks route to design-phase channels per `Project_Workflow_v1_8.md` §2.7.6:

| Fork class | Trigger | Routing |
|---|---|---|
| Class 1 (halt-execution) | Architectural defect; design-phase artifact requires revision | Halt Phase 7 sub-phase execution; surface to operator; route to design-phase workspace (Phase 6 plan / Phase 5 spec / Phase 3a/3b ADR / Phase 3d ADD / Phase 4 PRD / Workflow revision); re-load at re-clearance |
| Class 2 (in-execution operator decision) | In-session decision-point requiring operator selection between substantive alternatives | Surface to operator in this workspace; record decision at sub-phase log |
| Class 3 (informational) | Observation requiring documentation; non-blocking | Log at Phase 7 execution log + `Canonical_Substrate_Inventory.md` update at design-phase workspace |

The `phase-7-back-flow-routing` skill at `.claude/skills/` governs fork detection discipline. **Silent absorption of design-phase defects is the worst failure mode** — defect absorption at Phase 7 execution contaminates downstream implementation against an invalid spec and propagates to every dependent atomic unit.

### 4.4 NO silent H_T design extension at Phase 7

Per X-AL-3 (Meta-Architecture §7.7): no silent H_T design extension at Phase 7 execution. New H_T primitives surfaced at execution-time route to design-phase back-flow (Class 1) before implementation proceeds.

---

## 5. Sub-agent boundary

**CRITICAL.** Claude Code sub-agents (H_E orchestration primitive) are NOT the H_T Control Plane axis topology. Per `Phase_7_Meta_Architecture_v1.md` §7.4 anti-leakage rule **CP-AL-1**:

> H_E sub-agent topology (orchestrator-workers via `Agent` tool) ≠ H_T TopologyPattern 6-class enum (single-threaded-linear / orchestrator-workers / decentralized-handoff / hierarchical-delegation / evaluator-optimizer / parallelization)
>
> *Anti-pattern foreclosed:* Concluding "we already have orchestrator-workers" implies H_T-CP-10 is met

### 5.1 Operative discipline

H_E sub-agents are an execution-time parallelism primitive within this workspace. They:

- **DO** assist with Phase 7 implementation parallelism (per-axis-stream execution at sub-phase 7b)
- **DO** instantiate H_E orchestrator-workers for axis-stream coordination
- **DO NOT** instantiate H_T's CP-axis primitives
- **DO NOT** substitute for H_T's `WorkflowManifestEntry` schema, per-step override evaluator, audit-ledger emission contract, or any of the 6 H_T TopologyPattern values
- **DO NOT** collapse the H_E ↔ H_T substrate boundary (X-AL-1: boundary lives at the MCP server process; enforced by process isolation, not convention)

### 5.2 Sub-agent topology specification

See `Sub_Agent_Boundary_Specification_v1.md` at workspace root for sub-agent count, per-sub-agent responsibility, per-sub-agent anti-leakage citation, and per-sub-agent scope boundaries against the 4 axes.

---

## 6. Skill activation

This workspace ships with 4 Phase 7-specific skills at `<workspace_root>/.claude/skills/`. Skills activate per `tool_search`-driven trigger evaluation against YAML frontmatter description. Project-level skills (this workspace) take priority over user-level skills (`~/.claude/skills/`) on name collision per Anthropic Claude Code skill-directory precedence.

| Skill | Path | Activation surface |
|---|---|---|
| `phase-7-implementation` | `.claude/skills/phase-7-implementation/SKILL.md` | Per-axis-stream unit consumption (7b sub-phase); acceptance-criteria-driven implementation; cross-unit dependency-graph traversal |
| `phase-7-cross-axis-composition` | `.claude/skills/phase-7-cross-axis-composition/SKILL.md` | Cross-axis composition seam instantiation (7c sub-phase); CXA v2.1 §2.3 byte-exact alignment verification |
| `phase-7-substitution-retirement` | `.claude/skills/phase-7-substitution-retirement/SKILL.md` | Substitution retirement events (all sub-phases); per-primitive retirement criterion verification per Meta-Architecture §6 |
| `phase-7-back-flow-routing` | `.claude/skills/phase-7-back-flow-routing/SKILL.md` | Fork detection (all sub-phases); design-phase routing per §4.3 |

---

## 7. Phase 7 sub-phase enumeration

Per `Phase_7_Meta_Architecture_v1.md` § Phase 7 internal workflow + `Project_Workflow_v1_8.md` §2.7:

| Sub-phase | Scope | Primary skill | Event-driven skills |
|---|---|---|---|
| 7a | Bootstrap — Claude Code workspace initialization; first H_T primitive landings; substitution scaffolding under single-LLM-during-7a runtime substitution | None specific (consult Meta-Architecture §5 + §6) | `phase-7-back-flow-routing` |
| 7b | Per-axis-stream implementation — IS / AS / CP / OD axis-stream parallel execution per atomic-unit plans | `phase-7-implementation` | `phase-7-substitution-retirement`; `phase-7-back-flow-routing` |
| 7c | Cross-axis composition — CXA v2.1 seam instantiation; cross-axis edge wiring across 6 composition buckets | `phase-7-cross-axis-composition` | `phase-7-substitution-retirement`; `phase-7-back-flow-routing` |
| 7d | Substitution retirement — H_E substitution gradient retirement per self-hosting milestone gradient; closure when all H_E substitutions retired OR bounded-residual carried with documented rationale per X-AL-2 | `phase-7-substitution-retirement` | `phase-7-back-flow-routing` |

---

## 8. Execution invariants

The following invariants hold across all Phase 7 sessions. Violation surfaces to operator immediately.

| Invariant | Statement | Source |
|---|---|---|
| I-1 | Canonical artifact citations resolve byte-exact | `Project_Workflow_v1_8.md` §7.4.2 |
| I-2 | No H_T design extension at Phase 7 execution-time | X-AL-3 (Meta-Architecture §7.7) |
| I-3 | Substitution retirement is event-driven; retirement = (units landed) ∧ (H_E surface no longer invoked) | X-AL-2 (Meta-Architecture §7.7) |
| I-4 | H_E ↔ H_T substrate boundary at MCP server process; process isolation, not convention | X-AL-1 (Meta-Architecture §7.7) |
| I-5 | Class 1 forks halt sub-phase execution; route to design-phase back-flow | §4.3; `Project_Workflow_v1_8.md` §2.7.6 |
| I-6 | Framework-pull discipline holds — no `tenacity` / `pybreaker` / `langgraph` / `crewai` / `langchain` / `llamaindex` / `temporal` / `prefect` / LiteLLM | `Plan_Executability_Audit_v1.md` |
| I-7 | Multi-LLM commitment unchanged — per-provider SDKs under capability-aware abstraction; NOT single-LLM permanent commitment | ADR-F1 v1.2; CP-AL-4 (Meta-Architecture §7.4) |

---

## 9. Workspace bootstrap state

This workspace was bootstrapped at Phase 6.5 Session 6 (ε) per `Phase_6_5_Session_6_Kickoff.md`. Bootstrap substrate authored at design-phase workspace; pushed to this workspace at Session 6 close (operator action).

### 9.1 Filing footer

| Field | Value |
|---|---|
| Artifact | `CLAUDE.md` (workspace root) |
| Authored at | Phase 6.5 Session 6 (ε), 2026-05-15 |
| Authoring authority | `Phase_6_5_Session_6_Kickoff.md` §2.1.1 |
| Predecessor authoring | Design-phase workspace (separate Claude.ai project) |
| Successor consumption | Phase 7 Session 1 onward (this workspace) |
| Revision policy | This file is canonical for this workspace; revisions route to design-phase back-flow (§4.3) prior to in-workspace edit |

---

*End of root `CLAUDE.md`. Per-axis subdirectory `CLAUDE.md` files at `harness-{is,as,cp,od}/`. Skills at `.claude/skills/`. Sub-agent boundary at `Sub_Agent_Boundary_Specification_v1.md`.*
