# CLAUDE.md — Multi-LLM Agent Harness Workspace

*Workspace-level guidance for Claude Code at Phase 7 execution. Loaded at every Claude Code session startup. Canonical pointer to design-phase artifacts.*

---

## 1. Project framing

### 1.1 What this workspace builds

This workspace implements **H_T** (the target harness) — a multi-LLM agent harness — under the **H_E** execution surface (Claude Code CLI). H_T is specified by the design-phase artifact corpus enumerated at §2. H_T implementation traverses the per-axis atomic-unit plans at §2.4 in topological-sort order per per-axis-plan dependency graphs. H_E provides the development environment, IDE substrate, and bounded substitutions for not-yet-built H_T primitives (see §4).

The harness has four design axes plus a cross-axis composition surface:

| Axis | Scope |
|---|---|
| **IS** — Information Substrate | State ledger (6-field hash-chained entries per C-IS-05 §5), content-addressed index, semantic cache, filesystem-path classification |
| **AS** — Action Surface | Tool contracts (typed I/O schemas), MCP integration (FastMCP host + client), sandbox (4-tier blast radius), skills filesystem |
| **CP** — Control Plane | Routing (capability-aware multi-LLM), retry / breaker / idempotency, workflow lifecycle, topology (6-class enum), HITL placement |
| **OD** — Operational Discipline | HITL primitives (4-response palette), audit ledger schema, cost attribution (5-step chain), observability (12-namespace OTel schema) |
| **CXA** — Cross-Axis Composition | **100 canonical cross-axis relationships** across 7 composition buckets per `Cross_Axis_Composition_Document_v2_9.md` §2.3 (**30 genuine typed seams**; 46 convention-level; 24 phase-2-runtime); §2.3.7 CP→OD bucket extended 2→7 typed seams at v2.6 (Phase A.2 composer-arc absorption: ValidatorFramework + PauseResumeProtocol + PerServerTrustEvaluator + HITL webhook delivery + HITL operator-burden — all 5 sharing the `cp_audit_to_od_audit` converter with distinct F2 action_id prefixes); **§2.3.7 row 8 cost-attribution audit-write seam LANDED at v2.9** (handoff §6 + Phase D iteration-2 F2-02 absorption; carried across batches 13/14/15/16/17 + v2.7/v2.8 narrow-scope publications; bucket grows 7→8; aggregate 99→100; genuine 29→30; U-OD-41 SpanCostRecord composer → `cp_audit_to_od_audit` converter via `cost:` action_id prefix → U-OD-00 audit-ledger ingestion; per-axis attribution = OD outbound 26→27 per established v2.6 namespace-ownership convention; OD-side sub-arc B sequel still outstanding — OD spec v1.9→v1.10 NEW §C-OD-NN CostRecordAuditPayload + U-CP-72 `cost:` branch un-STRIKE + U-OD-41 plan revision per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]`) |

### 1.2 What this workspace is NOT

| NOT | Why |
|---|---|
| A H_T design extension surface | H_T design is canonical at design-phase artifacts (§2); design extensions route to design-phase back-flow (§4.3) |
| A re-litigation surface for ADR / spec / plan decisions | Decisions are P3-CK / P5-CK / P6-CK cleared; revision requires explicit back-flow trigger |
| A Claude Code customization workspace | This workspace is H_T build context; Claude Code user-level config goes at `~/.claude/` |
| A documentation workspace | Documentation generated by H_T at execution-time per per-axis-plan unit acceptance criteria |

### 1.3 Canonical authority chain

ADR (foundational F1–F5 + derivative D1–D6) → ADD v1.3 → PRD v1.1 → per-axis spec v1.x → per-axis plan v2.x + CXA v2.1 → Phase 7 implementation (this workspace).

Earlier artifacts in the chain are canonical for later artifacts. Conflicts route to design-phase back-flow per §4.3.

---

## 2. Canonical artifact pointers

All canonical artifacts reside at the **design-phase workspace** (separate Claude.ai project; not co-resident with this workspace). When this `CLAUDE.md` cites a canonical artifact, consult the design-phase workspace copy. Citation byte-exact discipline per `Project_Workflow_v1_8.md` §7.4.2.

### 2.1 Governance + execution discipline

| Artifact | Role |
|---|---|
| `Project_Workflow_v1_8.md` | Canonical workflow governance; §2.6.5 Phase 6.5 arc; §2.7 Phase 7 execution discipline; §2.7.6 back-flow routing; §7.4 fidelity-grammar |
| `Phase_7_Meta_Architecture_v1.md` | H_T ↔ H_E substitution mapping (§5 — 49 entries); anti-leakage rules (§7 — 18 axis + 3 cross-cutting); self-hosting milestone gradient (§6) |
| `Phase_7_Kickoff_Prompt.md` | Phase 7 entry framing; back-flow routing reference at §6 |
| `Target_Stack_Commitment_v1.md` | Stack commitment (see §3) |
| `Plan_Executability_Audit_v1.md` | Plan executability validation; framework-pull discipline reference |
| `Canonical_Substrate_Inventory.md` | KB navigation anchor; canonical-vs-superseded disambiguation |

### 2.2 ADR + ADD + PRD

| Artifact | Version | Role |
|---|---|---|
| ADR-F1 | v1.2 | Multi-LLM commitment |
| ADR-F2 | v1.2 | State ledger primitive |
| ADR-F3 | v1.1 | Index primitive |
| ADR-F4 | v1.1 | Workflow lifecycle primitive |
| ADR-F5 | v1.1 | Observability substrate primitive |
| ADR-D1 | v1.2 | HITL primitive |
| ADR-D2 | v1.2 | Sandbox tier |
| ADR-D3 | v1.2 | Validation contract |
| ADR-D4 | v1.1 | Cost attribution |
| ADR-D5 | v1.4 | Topology pattern — §1.4 storage-form reconciliation (SQLite → JSONL via IS composition) + §1.4.1 `entry_hash` recipe tightening per U-RT-59 Fork 2 Path B-revised-a (2026-05-20) |
| ADR-D6 | v1.2 | OTel schema (12 namespaces) |
| `Architectural_Design_Document_v1_3.md` | v1.3 | ADR consolidation; coherent architectural overview |
| `PRD_v1_1.md` | v1.1 | Product requirements; observable behavior |

### 2.3 Per-axis specs (Phase 5 canonical — contract authority)

| Axis | Spec |
|---|---|
| IS | `Spec_Information_Substrate_v1.md` (v1.2 per ADD v1.3 attestation) |
| AS | `Spec_Action_Surface_v1.md` (v1.5 — Class 1 fork H_T-CP-16+17 §16 ratified absorption: NEW §14.7 producer-site reference footer note repointing `memory.*` 6-attr namespace emission to runtime spec v1.17 §14.5.1 + §14.12 C-RT-22 callback-invocation sites; documents structural-pattern divergence from §14.3 mcp.* (per-callback emission vs per-dispatch per ADR-D3 §1.1 #11 client-side classification); §14.6 `files.*` footer NOT authored (Files arc deferred per §14.C); 2026-05-23. v1.4 — Phase A.2 ratified-drafts apply pass: §14.3 + §15 producer-site reference footer notes documenting v1.13 runtime contracts consume v1.4 namespaces (2026-05-21). v1.3 — C-AS-02/§11.1 `sandbox_tier_floor` reconciliation per S2 + C-AS-05 §5.1 3-param `fetch_secret` per S3, 2026-05-15) |
| CP | `Spec_Control_Plane_v1_15.md` (v1.15 — Narrow-scope spec clarification 2026-05-24: NEW §19.1.1 sub-section authoring canonical 4-axis statement explicitly enumerating `{per_tool_gate_level, blast_radius_floor, per_mcp_server_trust_floor, persona_tier_floor}` per ADR-D5 v1.3 §1.3.2 + AS spec C-AS-03 + C-AS-10 §10.3 + C-AS-12. Explicit non-axis statement: `deployment_surface` is NOT a §19.1 D5-layer axis (belongs at §19.3 D2-layer sandbox composition only). Resolves FM-2 item (a) from Reading B close + CP plan v2.4 §0.8 row 1 carry. ZERO contract / signature / AC / behavior change at spec-side (canonical formula was always 4-axis with per_tool_gate_level; v1.15 makes per-axis cite explicit). Co-published with CP plan v2.19 → v2.20 (U-CP-43 GateLevelInput field-set conform: add per_tool_gate_level, drop deployment_surface; §0.8 row 1 → RESOLVED-as-plan-conformed-to-spec; row 2 MCP_TRUST mapping → PARTIAL-ADVANCE) + harness-cp impl + harness-runtime impl per (B2) plan-follows-spec operator disposition. 1715 tests pass + 3 skipped. v1.14 prior — Narrow-scope citation-correction patch 2026-05-24: amends canonical reading of v1.2-lineage §19.1 + §21.3 4 cite sites where "C10 five-tier framework" narrative → "C10 four-tier framework per AS spec C-AS-10 §10.3 (Level 0/1/2/3 4-level enumeration)". Resolves CP plan v2.8 §0.6 Class 3 informational doc-hygiene drift logged 2026-05-21+. AS §10.3 canonical 4-level enumeration unchanged; CP plan v2.8 §2.0c U-CP-00c 4-value `MCPTrustTier` carrier unchanged. ZERO contract / signature / field-set / AC / behavior change; ZERO cross-axis cascade. FM-2 item (b) from Reading B close. Bundled fix at site (iv) v1.2 line 1888 §21.3 row also corrects §10.1 → §10.3 cite-shape drift. v1.13 prior — Class 1 fork resolution Reading A apply pass per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §7 ratified 2026-05-24: v1.10 NEW §25 / C-CP-25 ValidatorFramework renamed to §28 / C-CP-28 per first-author primacy + v1.10's preservation-claim self-contradiction + F-derivative anchor proximity tiebreaker; v1.6-lineage §25 / C-CP-25 WorkflowDriver retains canonical-ID primacy; fidelity-pure citation-correction patch — ZERO contract change, ZERO signature change, ZERO acceptance-criterion change, ZERO behavior change, ZERO cross-axis cascade at semantics layer; cite-cascade APPLIED 2026-05-24 across runtime spec v1.18 → v1.19 (17 sites) + CP plan v2.18 → v2.19 (canonical-reading amendment for cluster 10-CP-A) + runtime plan v2.17 → v2.18 (canonical-reading amendment for L9-decies, 23 sites) + adjacent fork doc cite retag at lines 127 + 152; harness-cp/CLAUDE.md empirical-grep ZERO §25/C-CP-25 spec-section cite hits; CXA v2.9 ZERO §25/C-CP-25/ValidatorFramework cite hits. v1.12 prior — Class 1 fork resolution Path A absorption per `.harness/class_1_fork_step_execution_context_workflow_id_field_absence.md` ratified 2026-05-24: §25.2.1 `StepExecutionContext` Pydantic BaseModel gains 9th field `workflow_id: str` (was 8 at v1.6 baseline preserved verbatim through v1.11) sourced from `manifest_entry.workflow_id` at driver §25.3.3.4 composition site; resolves OD spec v1.10 §C-OD-26.6.1 step 2 cite `step_context.workflow_id` (implicit forward-cite at Sub-arc B publication arc `0919a9b`); ZERO contract removal; ZERO behavior change at existing consumers; ZERO cross-axis edge. v1.11 prior — Path γ enum identifier rename absorption: §26.2 `PauseReason` → `WorkflowPauseReason` 5-class workflow-layer pause taxonomy disambiguation per `.harness/class_1_fork_u_cp_63_pause_reason_collision.md`; NEW §26 NOTE making §22 ↔ §26 engine-layer vs workflow-layer coexistence explicit (2026-05-21); v1.10 NEW §25/§26/§27/§17.4 substantive content preserved verbatim) |
| OD | `Spec_Operational_Discipline_v1_11.md` (v1.11 — Pause/resume back-flow arc (narrow scope per operator AskUserQuestion 2026-05-24 — "Narrow — OD-side helper only") per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §9 residual routing target. NEW §C-OD-30.4 — `PauseResumeAuditPayload` canonical production-invocation contract authoring: two helper signatures (`_project_pause_event_to_audit_payload` + `_project_resume_outcome_to_audit_payload`) + canonical invocation pattern routing through `cp_audit_to_od_audit` converter (already operational at `cp_audit_conversion.py:289-299` per Sub-arc A) + per-payload composition discipline (action_id pattern `pause:<workflow_id>:<step_index>` / `resume:<workflow_id>:<step_index>`; audit_cp_response constant selection per ResumeOutcomeKind 4-case switch; path-disjoint field nullification) + invariants + §C-OD-30.4.5 deferred discretion (production callsite construction explicitly DEFERRED — helper lands as DEAD CODE until CP composer authoring arc, which gates H_T-CP-22 PARTIAL → RETIRE-READY per `harness-cp/CLAUDE.md` §4.1). Structural asymmetry from cost-axis Sub-arc B: cost-axis widened EXISTING production callsite (`cost_attribution_llm_dispatch.py:198-202`); pause/resume lands helper WITHOUT a callsite to migrate (`capture_pause_snapshot` + `attempt_resume` are NotImplementedError stubs; workflow_driver.py does not invoke `PauseResumeProtocol`). NO new fail class; NO new span site; NO sampling change; NO CXA amendment (`pause:` + `resume:` discriminators at CXA v2.9 §0.3 from v2.6 composer-arc absorption). Reverses fork doc §9 doc-only deferral. Co-published with OD plan v2.18 (U-OD-51 AC re-decomposition 5→10 ACs absorbing v1.11 contract; AC #9 PARTIAL-LANDED per `[[halt-route-split-AC-pattern]]`) + impl arc helper landings at `pause_resume_namespace.py` + 16 new unit tests (16/16 helper + 27/27 Sub-arc A preserved + 16/16 converter preserved = 59 PASS; pyright strict 0 errors). 2026-05-24. v1.10 prior — Sub-arc B sequel landing per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD plan v2.16 §0(d) operator preference (i) revise-signature: NEW §C-OD-26.6 `CostRecordAuditPayload` typed Pydantic v2 carrier (4 audit_cp_* common fields + 5 cost-specific projected from SpanCostRecord per CXA v2.9 §0.3 row 8) + §C-OD-26.1 canonical invocation signature AMENDED to route through `cp_audit_to_od_audit` converter via typed CostRecordAuditPayload (v1.8 helper-only path preserved as production state at `cost_attribution_llm_dispatch.py:198-202`; migration to typed path deferred per FM-2 + `[[halt-route-split-AC-pattern]]` to follow-on arc requiring signature widening of `attribute_llm_dispatch_cost`). Co-published with OD plan v2.17 (U-OD-41 absorption) + CXA v2.9 forward-cite resolution (`OD spec v1.10 §C-OD-NN` → resolved to `§C-OD-26.6`). 2026-05-24. v1.9 prior — Path γ enum citation absorption: §C-OD-30.1 `pause.reason` attribute type cite `PauseReason per CP spec v1.10 §26.2` → `WorkflowPauseReason per CP spec v1.11 §26.2` (2026-05-21); v1.8 NEW §C-OD-25..33 substantive content preserved verbatim outside the single attribute-type cite cell) |
| Runtime | `Spec_Harness_Runtime_v1.md` (v1.23 — Narrow-scope bookkeeping refresh 2026-05-24: §14.15.2 step 7 self-flag flipped (was "CXA v2.9 §0.3 action-id prefix enumeration MAY need refresh ... NOT patched per FM-2" → now "patched at CXA v2.10 §0.3 per v1.22 → v1.23 paired co-publication arc"); clarifies CXA-side canonicalization at v2.6 §2.3.7 ValidatorFramework row vs runtime-side production-consumer at v1.22 §14.15.2 step 7. Co-published with CXA v2.9 → v2.10 §0.3 `validator:` discriminator production-consumer cite refresh. ZERO contract / signature / AC / behavior change; ZERO cross-axis cascade. FM-2 item (c) from Reading B close. v1.22 prior — Narrow-scope CP composer authoring arc absorption per operator-ratified AskUserQuestion 2026-05-24 ("driver-invocation-only" scope). NEW §14.14 C-RT-24 `materialize_pause_resume_protocol_stage` stage-5 LOOP_INIT factory + `PauseResumeProtocolConfig` empty-marker sub-model (per ValidatorFrameworkConfig precedent); NEW optional field `pause_resume_protocol_config: PauseResumeProtocolConfig | None` at §3 C-RT-02 RuntimeConfig; NEW fields `pause_resume_protocol: PauseResumeProtocol | None` + `pause_requested_flag: asyncio.Event` (sibling-pattern to `drained_flag`) at §4 C-RT-04 HarnessContext; NEW fail class `RT-FAIL-PAUSE-RESUME-STAGE-MATERIALIZE`. C-CP-26 §26 PauseResumeProtocol class body landed at U-CP-62 cluster 10-CP-B `49617e7` consumed via new workflow_driver per-step pre-entry pause-trigger detection (sibling check to `drained_flag.is_set()` at line 549) + entry-point resume detection firing `protocol.attempt_resume(snapshot, material_diff_policy=STRICT)`. Mirrors v1.18 §14.13 validator-framework precedent (Reading A scope discipline). NO CP spec / OD spec amendment owed — per OD spec v1.11 §C-OD-30.4.5 + CP spec v1.13 §26.7 workflow_driver invocation + per-step pause-trigger source are implementer-discretion; v1.21 executes those deferrals at the runtime spec layer. ZERO cross-axis cascade per spec §14.14.6 (CXA v2.9 unchanged; CP spec v1.13 unchanged; OD spec v1.11 unchanged). Co-published with runtime plan v2.19 → v2.20 NEW L9-undecies cluster (U-RT-87/88/89) + impl arc 4 commits (U-RT-87 + U-RT-88 + U-RT-89 driver + U-RT-89 e2e) + batch-18 retirement event H_T-CP-22 PARTIAL → RETIRED (joint single-batch transit; FOURTH RETIRE-READY → RETIRED close in ledger history; workspace 27/49 = 55.1% RETIRED; CP-axis 14/22 = 63.6%). 2026-05-24. v1.20 prior — AdvReview 07 Class 1 F1-01 inline doc-drift fix per `Adversarial_Review_07_Runtime_v1_18_+_v2_17.md`. §14.13.3 stage-4 ordering paragraph strict-total-order enumeration (`tracer_provider → audit_writer → cost_chain → collector_daemon → materialize_validator_framework_stage`) REPLACED with partial-order phrasing — validator framework runs AFTER all 4 sibling stage-4 OD-bucket bindings; intermediate sibling ordering unconstrained. Resolves self-contradiction with §"Adjacent defects surfaced" (v) at v1.18 + harmonizes with plan U-RT-84 AC #4 partial-order phrasing + matches empirical bootstrap order at HEAD (`stage_4_od.py`: `tracer_provider → collector_daemon → cost_chain → audit_writer`). ZERO contract / signature / AC / behavior change. 2026-05-24. v1.19 prior — Cite-cascade absorption pass per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24). 17 ValidatorFramework-context cite sites retagged §25.x → §28.x / C-CP-25 → C-CP-28 across §3 + §4 + §14.13 + §14.13.1 + §14.13.2 + §14.13.3 + §14.13.6 + 2 historical change-notes (v1.14→v1.15 path β fork + v1.16→v1.17 §(b)) per uniform-retag discipline. v1.6-lineage WorkflowDriver cites (§25.2.1 StepExecutionContext, §25.3.3.4 step-dispatch try/except boundary, §25.3.3 step-body-opaque) preserved verbatim. Reading B full validator-composer arc remains OPEN at fork doc §3.2. ZERO contract / signature / field-set / AC / behavior change — fidelity-pure citation-correction patch under FM-2. 2026-05-24. v1.18 prior — Class 1 fork validator-composer arc stage-4 absence Reading A ratified absorption per `.harness/class_1_fork_validator_composer_arc_stage_4_absence.md` §3.1 (operator-ratified 2026-05-24 post-batch-16 close at `1c55138`): NEW §14.13 C-RT-23 `materialize_validator_framework_stage` stage-4 OD-bucket factory + `ValidatorFrameworkConfig` empty-marker sub-model (per SandboxDecisionPolicy precedent); NEW optional field `validator_framework_config: ValidatorFrameworkConfig | None` at §3 C-RT-02 RuntimeConfig; NEW field `validator_framework: ValidatorFramework | None` at §4 C-RT-04 HarnessContext (type narrowed from v1.17-era `object | None` to CP spec v1.11 §25 Protocol surface `harness_cp.validator_framework_types.ValidatorFramework`; concrete body at `harness-cp/src/harness_cp/validator_framework.py:130`); NEW fail class `RT-FAIL-VALIDATOR-STAGE-MATERIALIZE`; consumed by existing C-CP-25 §25.3.3.4 driver hook at `workflow_driver.py:668` (preserved verbatim — no driver code change at v1.18); operator-opt-in RETIRE-READY pattern per spec §14.13.6 (mirrors CP-16/CP-18/AS-2 RETIRE-READY shape); §14.8.2 deferrals (VALIDATOR_ESCALATION foreclosure at step 3 + 4-axis `_hitl_required` composition at step 4c + cross-trust-boundary palette restriction at step 4d) PRESERVED VERBATIM per Reading A scope discipline (verified byte-exact via `git diff 1c55138..1707867`; 198 insertions / 1 deletion total). Reading B full validator-composer arc remains OPEN at fork doc §3.2 for future operator-discretion routing. ZERO cross-axis cascade per fork doc §5 — CXA v2.8 unchanged; CP spec v1.11 unchanged; OD spec v1.9 unchanged. Co-published with runtime plan v2.17 NEW L9-decies cluster (U-RT-83/84/85). Adversarial Review 07 CLEARED at 0 Class 3 / 0 Class 2 / 3 Class 1 documentation-drift findings. Batch-17 retirement-event filed restoring H_T-CP-21 from batch-15 DOWN-classification to RETIRED via Reading A resolution path (FIRST corrective close in ledger history; THIRD RETIRE-READY → RETIRED close). v1.17 prior — Class 1 fork H_T-CP-16+17 §16 ratified absorption per `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` (2026-05-23, same session as §11 PROVISIONAL filing + §13 systems-architect Mode 3 recommendation + §14 routing): NEW §14.12 C-RT-22 `MemoryToolRegistry` + `MemoryToolStorageBackendProtocol` contract (5 CRUD callbacks per ADR-D3 §1.1 #11 — `view`/`create`/`delete`/`str_replace`/`insert` on `/memories` paths; 3 NEW fail classes; stage-5 `materialize_memory_tool_registry_stage` factory); NEW §14.5.1 sub-section at C-RT-15 documenting Memory tool storage-backend callback binding (inner-loop mechanism deferred to implementation discretion per FM-2); NEW optional field `memory_tool_backend_config` at §3 C-RT-02 RuntimeConfig; NEW field `memory_tool_registry` at §4 C-RT-04 HarnessContext; operator-opt-in RETIRE-READY pattern preserved per §16 §6.D (structural criterion-B MET at landing arc; full RETIRED gated on operator-bound config + local-filesystem-backend e2e per §16 §6.C v2 C.vii local-fs scope; S3 deferred). NO Files API contract authoring at v1.17 — Files arc deferred indefinitely per §14.C. ZERO cross-axis cascade per §13.6 architect recommendation — CXA v2.8 unchanged; co-published with AS spec v1.5 §14.7 footer note. v1.16 — Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 post-cluster-4-OD-E partial close `128ab4f`): §3 C-RT-02 `sandbox_decision_policy` carrier-home cite re-pointed from `(AS spec v1.3 §15 carrier)` → `(harness-core carrier)` per Q1=C-i ratification — empirical phantom-cite verification: AS spec v1.3 §15/C-AS-15 is `secret.fetch` span schema not a sandbox policy class, ZERO `SandboxDecisionPolicy` hits across all axis source trees at filing time; single-token-edit citation-correction patch under FM-2 no-extension discipline; ZERO cross-axis cascade; §14.9.1 step 5 dangling forward-cite + filing-footer staleness + v1.15 historical cite preservation surfaced as adjacent findings (not patched). v1.15 substantive content preserved verbatim — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW §14.11 C-RT-21 `RetryBreakerToolDispatcher` sibling carrier (retry-only MVP per Q1=B + Q1a=(i); no breaker; no fallback chain); NEW factory contracts at §14.9.3 (stage-3a `materialize_mcp_client_host_stage` + stage-5 `materialize_runtime_tool_dispatcher_stage` 5-step composition body); NEW optional fields at §3 C-RT-02 RuntimeConfig (`trust_policy` + `sandbox_decision_policy`); NEW fields at §4 C-RT-04 HarnessContext (`mcp_client_host` stage 3a + `tool_dispatcher` stage 5 (bound to wrapper) + `per_server_trust_evaluator` + `mcp_namespace_emitter`); amended §14.9.2 inv 4 / §14.9.3 stage-5 prose / §14.9.6 inv 6 prose; new fail class `RT-FAIL-TOOL-RETRY-EXHAUSTED` per Q2=B2 bootstrap-chain decomposition (2026-05-22); v1.14 path-β citation preserved verbatim; v1.13 NEW §14.9 + §14.10 contracts preserved verbatim) |

### 2.4 Per-axis plans (Phase 6 canonical — execution authority)

| Axis | Plan | Unit count |
|---|---|---|
| core | `Implementation_Plan_Harness_Core_v1_2.md` | 2 units (U-CORE-01, U-CORE-02); v1.1 carrier-thin Class 1 fork resolution preserved verbatim (WorkflowEvent payload model struck 2026-05-15); v1.2 — NEW U-CORE-02 `SandboxDecisionPolicy` empty-marker carrier added per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` Q1=C-i ratification 2026-05-22 (re-home from phantom "AS spec v1.3 §15 carrier" cite to harness-core package; empty-marker shape per X-AL-3 no-spec-extension discipline — frozen Pydantic v2 BaseModel with NO fields + `.default()` factory; consumed by `harness-runtime` U-RT-71 at runtime plan v2.13); co-published with runtime spec v1.16 + runtime plan v2.13 this arc |
| IS | `Implementation_Plan_Information_Substrate_v2_3.md` | 17 units (U-IS-01 – U-IS-17) |
| AS | `Implementation_Plan_Action_Surface_v1_2.md` | 33 units (U-AS-01 – U-AS-33) |
| CP | `Implementation_Plan_Control_Plane_v2_20.md` | 73 units (U-CP-00, U-CP-00b, U-CP-00c, U-CP-01 – U-CP-72); v2.20 — Plan-follows-spec absorption pass 2026-05-24 per CP spec v1.14 → v1.15 NEW §19.1.1 canonical 4-axis statement publication (this session) + operator (B2) disposition. U-CP-43 `GateLevelInput` field-set canonical-reading amendment at delta-only-plan-chain layer: ADD `per_tool_gate_level: GateLevel` field per CP spec v1.15 §19.1.1 (i) degenerate axis; DROP `deployment_surface: DeploymentSurface` field per §19.1.1 (v) non-axis statement; RETIRE `DEPLOYMENT_SURFACE_GATE_LEVEL_FLOOR` constant; `MCP_TRUST_GATE_LEVEL_FLOOR` preserved verbatim with §0.8 row 2 PARTIAL-ADVANCE pointer (mapping spec-silent at CP §19.1 + AS §10.3; owed at separate follow-on spec-extension arc). §0.8 row 1 (U-CP-43 4-axis input-set divergence) → RESOLVED-as-plan-conformed-to-spec at v2.20. acc #6/#9 STRUCK + REPLACED with v2.20-NEW per_tool_gate_level + field-set conformance acceptance criteria. Tests: 4 new gate_level + composer tests authored; DEPLOYMENT_SURFACE tests retired; MCP_TRUST mapping tests preserved deferred. ZERO new units; ZERO DAG topology change; ZERO coverage matrix structural change. Co-published with harness-cp impl (`gate_level_rule.py` + `five_axis_composition.py`) + harness-runtime impl (`hitl_gate_composer.py`). 1715 tests pass + 3 skipped. FM-2 item (a) from Reading B close. 2026-05-24. v2.19 prior — Cite-cascade absorption per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24): §0(i) adjacent-defect note refreshed from "non-blocking; routing to future arc" to RESOLVED at CP spec v1.13; NEW §1 cluster 10-CP-A canonical-reading amendment table for U-CP-58/59/60/61 (cite-cascade absorbed without per-unit-body edit per delta-only convention; v2.15/v2.16 plan files preserved byte-exact). U-CP-56 (StepExecutionContext schema; v1.6-lineage WorkflowDriver at §25.2.1) preserved verbatim — the v2.18 9th-field amendment cite at §25.2.1 is the v1.6-lineage authoring site and retains canonical-ID primacy. ZERO contract / signature / AC / DAG change. 2026-05-24. v2.18 prior — Companion plan revision to CP spec v1.11 → v1.12 NEW §25.2.1 9th-field `workflow_id` per Class 1 fork resolution Path A absorption at `.harness/class_1_fork_step_execution_context_workflow_id_field_absence.md` ratified 2026-05-24. Single-unit-body amendment at U-CP-56 (StepExecutionContext: 8 → 9 fields; AC #1 + AC #6 text updates; Signatures line "8-field" → "9-field"; Files line + Depends-on edges preserved verbatim). ZERO unit re-decomposition; ZERO new units; ZERO DAG topology change; ZERO coverage matrix change. v2.17 prior — Path γ citation absorption: U-CP-62 carrier `PauseReason` → `WorkflowPauseReason` rename + U-CP-63 AC #4 STRUCK + U-CP-63/64 Files-column note amended per CP spec v1.11 §26 NEW NOTE coexistence per `.harness/class_1_fork_u_cp_63_pause_reason_collision.md` (2026-05-21) |
| OD | `Implementation_Plan_Operational_Discipline_v2_18.md` | 55 units (U-OD-00, U-OD-01 – U-OD-54); v2.18 — Pause/resume back-flow arc (narrow scope per operator AskUserQuestion 2026-05-24) U-OD-51 AC re-decomposition per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §9 + OD spec v1.10 → v1.11 NEW §C-OD-30.4 production-invocation contract. Single-unit-body amendment at U-OD-51 (5 → 10 ACs; Implements + Files + Signatures revised to absorb v1.11 production-helper contract). ACs #1-#5 preserved verbatim from Sub-arc A v2.14 authoring; NEW ACs #6-#10: pause helper landing + resume helper landing + path-disjoint nullification + production callsite construction DEFERRED per `[[halt-route-split-AC-pattern]]` (AC #9 PARTIAL-LANDED — gates H_T-CP-22 PARTIAL → RETIRE-READY at CP composer arc) + unit tests covering helper construction shape. Co-published this session: OD spec v1.11 + impl arc (pause_resume_namespace.py EXTEND with `_project_pause_event_to_audit_payload` + `_project_resume_outcome_to_audit_payload` module-level helpers; __all__ export + test-fixture carve-out per §C-OD-30.4.1 step 1) + 16 new unit tests verifying ACs #6-#10 + pyright strict clean. NO new units; NO DAG topology change; NO cluster reorganization; NO cross-axis edge addition. 2026-05-24. v2.17 prior — Sub-arc B sequel U-OD-41 absorption per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD spec v1.10 NEW §C-OD-26.6 CostRecordAuditPayload + amended §C-OD-26.1 + CXA v2.9 §2.3.7 row 8. Single-unit-body amendment at U-OD-41 (5 → 9 ACs; Implements + Files + Signatures revised to absorb v1.10 typed-carrier contract; cost_namespace.py NEW + cost_record_audit_writer.py NEW + cp_audit_conversion.py EXTEND with `cost:` branch restoration + COST_AUDIT_NAMESPACE_PREFIX). AC #8 PARTIAL-LANDED per `[[halt-route-split-AC-pattern]]` (production callsite migration at `cost_attribution_llm_dispatch.py:198-202` deferred per FM-2 to follow-on arc widening `attribute_llm_dispatch_cost` signature). Co-published this session: OD spec v1.10 + impl arc (cost_namespace.py + cost_record_audit_writer.py + cp_audit_conversion.py extension + 4 new cost: branch tests). 2026-05-24. v2.16 prior — Cross-axis-block lift at U-OD-51 (U-CP-62 landed `49617e7`); v2.15 — Path γ citation absorption; v2.14 + earlier preserved verbatim |
| Runtime | `Implementation_Plan_Harness_Runtime_v2_22.md` | 90 units (U-RT-00..U-RT-70 + U-RT-71..U-RT-75 L9-septies + U-RT-76..U-RT-82 L9-octies + U-RT-86 L9-novies + U-RT-83..U-RT-85 L9-decies + U-RT-87..U-RT-89 L9-undecies); v2.22 — Narrow-scope status-advance bookkeeping 2026-05-24: v2.21 §"Adjacent observation (a)" + line 213 carry status-advance → RESOLVED-via-CP-axis-conformance per CP spec v1.15 §19.1.1 + CP plan v2.20 U-CP-43 GateLevelInput conform (prior commit `44fc925`). Reading B pragmatic-path Class 3 informational divergence framing RETIRED — CP-axis GateLevelInput now exposes spec-canonical 4-axis; runtime thin-wrap consumes spec-canonical surface; divergence dissolved by conformance. ZERO contract / signature / AC / behavior change at runtime layer. FM-2 item (a) Reading B follow-on. v2.21 prior — Reading B L9-duodecies cluster authoring; v2.20 prior — Spec-revision-driven plan revision absorbing runtime spec v1.20 → v1.21 §14.14 C-RT-24 NEW contract per narrow-scope CP composer authoring arc (operator-ratified AskUserQuestion 2026-05-24). NEW L9-undecies 3-unit linear-chain cluster (U-RT-87 RuntimeConfig + HarnessContext fields + PauseResumeProtocolConfig empty-marker; U-RT-88 materialize_pause_resume_protocol_stage factory + stage-5 LOOP_INIT wiring + pause_context_reader composition + RT-FAIL-PAUSE-RESUME-STAGE-MATERIALIZE fail-class; U-RT-89 workflow_driver per-step pre-entry pause-trigger detection + entry-point resume detection + RunStatus.PAUSED enum extension + RunResult.pause_snapshot field + real-bootstrap e2e pause-resume cycle — mechanism α default). 3 within-cluster edges + 8 cluster-boundary edges to already-landed cluster 10-CP-B carriers (U-CP-62/63/64/65 at closure commits `49617e7`); ZERO cross-axis cascade per spec §14.14.6 (CXA v2.9 unchanged); NO CP spec / OD spec amendment triggered. Mechanism α/β/γ enumerated for e2e test-substrate selection per FM-2 implementer-discretion (recommended default α in-process pause-resume cycle). L9-undecies impl landed at `a783673` (U-RT-87 fields + sub-model) + `9e8a938` (U-RT-88 factory + stage-5 wiring + 18/18 unit tests) + `de4ae66` (U-RT-89 driver-side; 677/677 harness-cp tests preserved) + `671f195` (U-RT-89 e2e; 6/6 tests verifying binding chain + protocol method invocation + clean-resume cycle + corruption path through real `run_bootstrap`). U-RT-89 e2e satisfies H_T-CP-22 operational-criterion-B per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline enabling batch-18 H_T-CP-22 PARTIAL → RETIRED joint single-batch transit (FOURTH RETIRE-READY → RETIRED close in ledger history). 2026-05-24. v2.19 prior — AdvReview 07 Class 1 F1-02 + F1-03 inline doc-drift absorption per `Adversarial_Review_07_Runtime_v1_18_+_v2_17.md`. NEW §1 U-RT-83 AC #4 reword (F1-02): mis-attribution of Pydantic v2 frozen-model validation to `@dataclass(frozen=True)` `ValidatorFrameworkConfig` disambiguated — outer `RuntimeConfig` carries Pydantic discipline; inner sub-model is dataclass. NEW §2 U-RT-83 Files section + change-note (d) retag (F1-03): recommended path updated from top-level `validator_framework_config.py` to empirically-landed `lifecycle/validator_framework_types.py` (matches §14.12 `MemoryToolBackendConfig` precedent at `lifecycle/memory_tool_types.py`; implementer-discretion clause at AC #3 absorbed the resolution at impl arc). Delta-only plan-file preservation: v2.17 file U-RT-83 unit body preserved byte-exact; canonical reading at v2.19 retags per §1 + §2 tables. ZERO contract / signature / AC-semantic / DAG change. 2026-05-24. v2.18 prior — Cite-cascade absorption per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24) + runtime spec v1.18 → v1.19 cite-cascade absorption (this session). L9-decies cluster body (U-RT-83/84/85) at v2.17 cites the ValidatorFramework Protocol surface across 23 sites — all retag §25.x → §28.x / C-CP-25 → C-CP-28 per CP spec v1.13 §1. NEW §1 per-line retag enumeration table (23-site canonical reading). Delta-only plan-file preservation: v2.17 plan file containing L9-decies cluster body NOT edited — preserved byte-exact; canonical reading at v2.18 retags per §1. ZERO contract / signature / AC / DAG change; cluster-boundary edges to U-CP-58/59/60/61 unchanged at edge-declaration level. 2026-05-24. v2.17 prior — Spec-revision-driven plan revision absorbing runtime spec v1.17 → v1.18 §14.13 C-RT-23 NEW contract per `.harness/class_1_fork_validator_composer_arc_stage_4_absence.md` §3.1 Reading A (operator-ratified 2026-05-24). NEW L9-decies 3-unit linear-chain cluster (U-RT-83 RuntimeConfig field + ValidatorFrameworkConfig empty-marker; U-RT-84 materialize_validator_framework_stage factory + stage-4 wiring + HarnessContext type narrowing + RT-FAIL-VALIDATOR-STAGE-MATERIALIZE fail-class; U-RT-85 real-bootstrap e2e against operator-supplied ValidatorFramework instance — mechanism α-lite default). 3 within-cluster edges + 7 cluster-boundary edges to already-landed cluster 10-CP-A carriers (U-CP-58/59/60/61 at closure commits `16cf6d7`/`cdf83b1`/`5ca86aa`/`9b009d3`); ZERO cross-axis cascade per fork doc §5 (CXA v2.8 unchanged); NO spec amendment triggered beyond v1.17 → v1.18 already absorbed in spec row. U-RT-84 AC #3 implementer-discretion (i) NotImplementedError opt-in branch OR (ii) minimal construction body per `[[halt-route-split-AC-pattern]]` precedent. U-RT-85 implementer-discretion mechanism α (recommended default — in-process deterministic PASS fixture, no LLM in loop) / β (PASS + PERMANENT_FAIL coverage) / γ (env-var-gated fixture path). L9-decies impl landed at `3005643`/`d55fbd7`/`37e9d67` in worktree this session; U-RT-85 e2e 4/4 PASS confirms binding-chain operational-MET enabling batch-17 H_T-CP-21 corrective close. Adversarial Review 07 CLEARED at P5-CK + P6-CK joint review (0 Class 3 / 0 Class 2 / 3 Class 1 documentation-drift findings; Reading A scope discipline empirically preserved). v2.16 prior — Retirement-batch-driven plan revision adding NEW L9-novies singleton cluster (1 unit: U-RT-86 e2e integration test for MCP-client external server + real TOOL_STEP dispatch + `mcp.*` namespace verification + AS-2 ToolContract enforcement). Enables joint H_T-CP-18 + H_T-AS-2 RETIRE-READY → RETIRED close at follow-on batch-16 per shared MCP-client substrate framing at `.harness/phase-7d-retirement-events-batch-12.md` §1.2 + U-RT-82 close-pattern precedent at `.harness/phase-7d-retirement-events-batch-14.md` §6(a). Cluster-boundary deps to already-landed L9-septies (U-RT-71..U-RT-75); ZERO within-cluster edges (singleton); ZERO cross-axis cascade (CXA v2.8 unchanged); NO spec amendment triggered. Mechanism α/β/γ enumerated for test-substrate selection per FM-2 implementer-discretion (recommended default α in-process FastMCP). Retroactively corrects asymmetry between L9-octies (which pre-included U-RT-82 close-evidence unit) and L9-septies (which landed before the close-pattern discipline was catalogued at batch-14 §6(a)). v2.15 prior + v2.14 prior preserved verbatim. v2.15 — P6-CK adversarial-review absorption pass per `Adversarial_Review_06_Runtime_v2_14.md` (4 Class 2 + 3 Class 1 findings; F2-04 downgraded to Class 1 after grep-resolution confirmed CP §25.3.3.4 resolves at CP spec v1.7 line 13 preserved verbatim through v1.11 per delta-only-spec-file preservation chain — L9-octies cluster-opening gate CLEARED): 7 surgical edits across change-note + 4 unit bodies (U-RT-76 Implements cite-shape fix re ADR-D3 §1.1 #11 vs runtime §14.12.1 5-callback origin per F1-01; U-RT-77 NEW AC #9 covering §14.12.2 invariant 4 "no retry inside callback" per F1-02; U-RT-81 Depends on direct U-RT-76 edge added per F2-02 + AS §14.7→§14.8 sampling-row cite-replace per F2-03 + cite-discoverability note added re CP §25.3.3.4 delta-only-spec preservation chain per F2-04; coverage matrix §14.12.5 invariant 6 spec-deferred rationale per F1-03; adjacent-defects restructured into "Patched at plan-body (A)" + "NOT patched per FM-2 (B)" sub-sections per F2-01); NO DAG topology change (1 new direct edge U-RT-76→U-RT-81 declared per F2-02 — no new transitive path); NO contract addition; NO upstream-phase artifact revision triggered; 2 spec-side adjacent-defects (iv) AS §14.7 footer-note self-claim drift + (v) CP delta-only-spec-file cite-discoverability surfaced for future spec/workflow revision passes; ZERO cross-axis cascade. v2.14 prior — Class 1 fork H_T-CP-16+17 executable-consumer-absence resolution absorption pass per `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` §16 RATIFIED-AMENDED (operator-ratified 2026-05-23, Memory-only scope per §14.C; Files arc deferred indefinitely): NEW L9-octies cluster (7 units U-RT-76..U-RT-82) decomposing runtime spec v1.16 → v1.17 NEW §14.5.1 sub-section at C-RT-15 + NEW §14.12 C-RT-22 contract (`MemoryToolRegistry` + `MemoryToolStorageBackendProtocol` — 5 CRUD callbacks on `/memories` paths per ADR-D3 §1.1 #11) — U-RT-76 (Protocol + sub-model + typed-exception carriers) + U-RT-77 (LocalFilesystemMemoryToolBackend impl per §14.D local-fs-only scope) + U-RT-78 (MemoryToolRegistry class) + U-RT-79 (RuntimeConfig.memory_tool_backend_config + HarnessContext.memory_tool_registry field landings) + U-RT-80 (materialize_memory_tool_registry_stage factory + stage-5 wiring + Protocol-conformance enforcement) + U-RT-81 (C-RT-15 §14.5.1 callback-injection composer-step at llm_dispatch.py — α/β/γ inner-loop mechanism discretion per FM-2; callback→kind enum mapping locked at plan-body per advisor coherence-pass) + U-RT-82 (e2e local-fs backend + real Anthropic API messages.create with tools=[memory_tool] + deterministic-prompt fixture forcing write-path); 3 adjacent defects surfaced (§14.12.3 stage-5 ordering self-contradiction; §14.5.1 enum non-bijective with 5-callback Protocol; §14.5.1 driver-line-cite drift :380-389 → :618-635 — NOT patched per FM-2); ZERO cross-axis cascade per fork doc §5 + architect §13.6.D (CXA v2.8 unchanged); operator-opt-in RETIRE-READY pattern owed at retirement-batch-12 follow-on arc per §16 §6.D. v2.13 prior — `SandboxDecisionPolicy` Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 Q1=C-i): U-RT-71 + U-RT-75 cite-edited (runtime spec v1.15 → v1.16 absorption); U-RT-71 gains NEW within-axis-cross-package dependency edge on U-CORE-02 (harness-core plan v1.2 — co-published); no AC body change; no unit re-decomposition; no contract addition; single-token-class cite-edit absorption patch under FM-2 no-extension discipline; ZERO cross-axis cascade per fork doc §5. v2.12 prior — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW L9-septies cluster (5 units U-RT-71..U-RT-75) decomposing Q2=B2 bootstrap-wiring chain — RuntimeConfig schema extension + HarnessContext schema extension + stage-3a `materialize_mcp_client_host_stage` factory + C-RT-21 `RetryBreakerToolDispatcher` class body + stage-5 `materialize_runtime_tool_dispatcher_stage` factory; U-RT-68 REWRITTEN at v2.12 per Q1=B + Q1a=(i) ratification (stage-5 callsite consuming new factory; original v2.11 AC #2 with literal `RetryBreakerFallbackDispatcher` reuse STRUCK); ZERO cross-axis cascade per fork doc §5; L9-sexies preserved verbatim except U-RT-68 (2026-05-22) |
| CXA | `Cross_Axis_Composition_Document_v2_10.md` | canonical cross-axis relationships per v2.10 (narrow-scope bookkeeping refresh 2026-05-24 — `validator:` discriminator production-consumer cite to runtime spec v1.22 §14.15.2 step 7 per Reading B validator-composer arc landing; co-published with runtime spec v1.22 → v1.23 §14.15.2 step 7 self-flag flip; ZERO matrix change; ZERO new edge; CXA-side `validator:` canonicalization preserved verbatim at v2.6 §2.3.7 ValidatorFramework row; §0.7 surfaces 2 not-patched-per-FM-2 adjacent findings — v2.6 §2.3.7 bucket-note prose enumeration drift "7 names vs 8 listed" pre-existing carry; full 9-discriminator §0.3 enumeration refresh deferred); v2.10 cardinality **100 aggregate / 30 genuine / 46 convention / 24 phase-2-runtime** (unchanged from v2.9). v2.9 prior — cost-attribution audit-write seam row 8 publication 2026-05-24 per `.harness/Remaining_Work_Closure_Arc_Handoff_v1.md` §6 + Phase D iteration-2 F2-02 absorption; v2.9 amendment scope: NEW §2.3.7 row 8 cost-attribution audit-write seam (U-OD-41 SpanCostRecord composer → `cp_audit_to_od_audit` converter via `cost:` action_id prefix → U-OD-00; CP→OD bucket 7→8; aggregate 99→100; genuine 29→30; per-axis attribution = OD outbound 26→27 per established v2.6 namespace-ownership convention preserving §2.1-vs-§2.4 divergence as natural consequence); forward-cite OD spec v1.10 §C-OD-NN per v2.6 §0.5 forward-cite hygiene (CostRecordAuditPayload class authoring owed at OD-side sub-arc B sequel — OD spec v1.9 → v1.10 + U-CP-72 `cost:` branch un-STRIKE + U-OD-41 plan revision per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]`); v2.8 prior — path-γ citation reconciliation 2026-05-21 (§2.3.7 row 6 C-CP-26 §26 producer spec-version cite bumps CP v1.10 → v1.11 + OD v1.8 → v1.9); v2.6 cardinality preserved verbatim outside the additive row 8 |

### 2.5 Per-axis subdirectory `CLAUDE.md` pointers

| Subdirectory | CLAUDE.md role |
|---|---|
| `harness-is/CLAUDE.md` | IS-axis scope; plan unit entry-points; IS substitution surface (9 entries); IS anti-leakage rules |
| `harness-as/CLAUDE.md` | AS-axis scope; plan unit entry-points; AS substitution surface (6 entries); AS anti-leakage rules |
| `harness-cp/CLAUDE.md` | CP-axis scope; plan unit entry-points; CP substitution surface (21 entries); CP anti-leakage rules |
| `harness-od/CLAUDE.md` | OD-axis scope; plan unit entry-points; OD substitution surface (8 entries); OD anti-leakage rules |

`harness-core/` hosts shared types + cross-axis utilities. `harness-cxa/` hosts CXA seam instantiation (5 CXA-axis substitution entries; CXA-AL-1 anti-leakage); cross-axis edges per CXA v2.1 §2.3.

---

## 3. Stack discipline

### 3.1 Committed stack

Per `Target_Stack_Commitment_v1.md` §5.1 + §5.2:

| Decision area | Commitment |
|---|---|
| Programming language | Python 3.12+ |
| Type contracts | Pydantic v2 |
| Concurrency primitive | `asyncio` |
| Package manager | `uv` (workspace-aware) |
| Type checker | `pyright` (strict mode) |
| Linter / formatter | `ruff` (single tool) |
| Test runner | `pytest` + `pytest-asyncio` |
| Repo structure | Monorepo with axis-subdirectory uv workspace (`harness-{is,as,cp,od,cxa}/` + shared `harness-core/`) |
| Git posture | Conventional commits; commit-per-unit-cluster; PR-per-axis-cluster; feature branches off `main` |
| CI substrate | Deferred to post-bootstrap milestone |
| Multi-LLM SDK stance | Per-provider official SDKs (`anthropic`, `openai`, `ollama`) under capability-aware abstraction per ADR-F1 v1.2; **NOT LiteLLM** |
| OTel libraries | `opentelemetry-api` + `opentelemetry-sdk` + `opentelemetry-exporter-otlp` + `opentelemetry-instrumentation-genai` (selective contrib adoption) |
| MCP host + client | `modelcontextprotocol/python-sdk` (FastMCP) |
| Secrets | `python-keyring` |
| Reliability primitives | Hand-rolled retry / breaker / idempotency (NO framework) per `Plan_Executability_Audit_v1.md` framework-pull discipline |

### 3.2 Framework-pull discipline

Per `Plan_Executability_Audit_v1.md`:

| Discipline | Statement |
|---|---|
| Bounded adoption | Framework adoption is bounded. New frameworks pulled in only if no existing-stack solution meets the unit's acceptance criteria |
| Reliability primitives | Hand-rolled. Do NOT pull `tenacity`, `pybreaker`, `circuitbreaker`, or equivalent framework libraries |
| Workflow orchestration | Hand-rolled per CP-axis plan units. Do NOT pull `prefect`, `temporal`, `langgraph`, `crewai`, `langchain`, `llamaindex`, or equivalent |
| Validation | Pydantic v2 + hand-rolled deterministic gates per AS-axis + CP-axis plan units. Do NOT pull `pydantic-validators-plus` or equivalent |
| Multi-LLM abstraction | Hand-rolled capability-aware abstraction per ADR-F1 v1.2. Do NOT pull LiteLLM |

### 3.3 Repo layout

```
<workspace_root>/
├── CLAUDE.md                                # This file
├── Sub_Agent_Boundary_Specification_v1.md   # §5 sub-agent boundary
├── .claude/
│   └── skills/                              # Phase 7-specific skills (§6)
│       ├── phase-7-implementation/SKILL.md
│       ├── phase-7-cross-axis-composition/SKILL.md
│       ├── phase-7-substitution-retirement/SKILL.md
│       └── phase-7-back-flow-routing/SKILL.md
├── pyproject.toml                           # uv workspace root
├── uv.lock
├── harness-core/                            # Shared types + cross-axis utilities
│   ├── pyproject.toml
│   └── ...
├── harness-is/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-as/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-cp/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
├── harness-od/
│   ├── CLAUDE.md
│   ├── pyproject.toml
│   └── ...
└── harness-cxa/                             # CXA seam instantiation
    ├── pyproject.toml
    └── ...
```

---

## 4. Substitution + back-flow discipline

### 4.1 H_T ↔ H_E substitution discipline

Per `Phase_7_Meta_Architecture_v1.md` §5 (49-row substitution mapping table) + §6 (self-hosting milestone gradient) + §7 (18 axis + 3 cross-cutting anti-leakage rules):

H_E provides bounded substitutions for not-yet-built H_T primitives across 6 substitution-mechanism categories:

| Mechanism | Count | Examples |
|---|---|---|
| H_E-direct | 11 | H_E filesystem ops; `Edit` / `Read` / `Write` tools |
| MCP-server | 12 | Substitution routed through MCP server boundary |
| Convention | 9 | Operator-authored `CLAUDE.md` / prompts |
| Shell-out | 8 | `Bash` invocations of `git`, `sha256sum`, `python -c` |
| Manual | 5 | Operator-driven gates (review, approval) |
| Authoring-only | 4 | Substitutions retired at authoring close |

Per-axis substitution count: IS=9 / AS=6 / CP=21 / OD=8 / CXA=5.

### 4.2 Substitution retirement discipline

Per X-AL-2 (Meta-Architecture §7.7):

> Retirement = (cited unit IDs landed) ∧ (substituted H_E surface no longer invoked at substitution site). Both conditions required.

Partial retirement is non-retirement. The `phase-7-substitution-retirement` skill at `.claude/skills/` governs retirement event discipline. Substitution retirement triggers throughout 7b–7d sub-phases, not only at 7d.

### 4.3 Back-flow routing

Phase 7 execution-time forks route to design-phase channels per `Project_Workflow_v1_8.md` §2.7.6:

| Fork class | Trigger | Routing |
|---|---|---|
| Class 1 (halt-execution) | Architectural defect; design-phase artifact requires revision | Halt Phase 7 sub-phase execution; surface to operator; route to design-phase workspace (Phase 6 plan / Phase 5 spec / Phase 3a/3b ADR / Phase 3d ADD / Phase 4 PRD / Workflow revision); re-load at re-clearance |
| Class 2 (in-execution operator decision) | In-session decision-point requiring operator selection between substantive alternatives | Surface to operator in this workspace; record decision at sub-phase log |
| Class 3 (informational) | Observation requiring documentation; non-blocking | Log at Phase 7 execution log + `Canonical_Substrate_Inventory.md` update at design-phase workspace |

The `phase-7-back-flow-routing` skill at `.claude/skills/` governs fork detection discipline. **Silent absorption of design-phase defects is the worst failure mode** — defect absorption at Phase 7 execution contaminates downstream implementation against an invalid spec and propagates to every dependent atomic unit.

### 4.4 NO silent H_T design extension at Phase 7

Per X-AL-3 (Meta-Architecture §7.7): no silent H_T design extension at Phase 7 execution. New H_T primitives surfaced at execution-time route to design-phase back-flow (Class 1) before implementation proceeds.

---

## 5. Sub-agent boundary

**CRITICAL.** Claude Code sub-agents (H_E orchestration primitive) are NOT the H_T Control Plane axis topology. Per `Phase_7_Meta_Architecture_v1.md` §7.4 anti-leakage rule **CP-AL-1**:

> H_E sub-agent topology (orchestrator-workers via `Agent` tool) ≠ H_T TopologyPattern 6-class enum (single-threaded-linear / orchestrator-workers / decentralized-handoff / hierarchical-delegation / evaluator-optimizer / parallelization)
>
> *Anti-pattern foreclosed:* Concluding "we already have orchestrator-workers" implies H_T-CP-10 is met

### 5.1 Operative discipline

H_E sub-agents are an execution-time parallelism primitive within this workspace. They:

- **DO** assist with Phase 7 implementation parallelism (per-axis-stream execution at sub-phase 7b)
- **DO** instantiate H_E orchestrator-workers for axis-stream coordination
- **DO NOT** instantiate H_T's CP-axis primitives
- **DO NOT** substitute for H_T's `WorkflowManifestEntry` schema, per-step override evaluator, audit-ledger emission contract, or any of the 6 H_T TopologyPattern values
- **DO NOT** collapse the H_E ↔ H_T substrate boundary (X-AL-1: boundary lives at the MCP server process; enforced by process isolation, not convention)

### 5.2 Sub-agent topology specification

See `Sub_Agent_Boundary_Specification_v1.md` at workspace root for sub-agent count, per-sub-agent responsibility, per-sub-agent anti-leakage citation, and per-sub-agent scope boundaries against the 4 axes.

---

## 6. Skill activation

This workspace ships with 4 Phase 7-specific skills at `<workspace_root>/.claude/skills/`. Skills activate per `tool_search`-driven trigger evaluation against YAML frontmatter description. Project-level skills (this workspace) take priority over user-level skills (`~/.claude/skills/`) on name collision per Anthropic Claude Code skill-directory precedence.

| Skill | Path | Activation surface |
|---|---|---|
| `phase-7-implementation` | `.claude/skills/phase-7-implementation/SKILL.md` | Per-axis-stream unit consumption (7b sub-phase); acceptance-criteria-driven implementation; cross-unit dependency-graph traversal |
| `phase-7-cross-axis-composition` | `.claude/skills/phase-7-cross-axis-composition/SKILL.md` | Cross-axis composition seam instantiation (7c sub-phase); CXA v2.1 §2.3 byte-exact alignment verification |
| `phase-7-substitution-retirement` | `.claude/skills/phase-7-substitution-retirement/SKILL.md` | Substitution retirement events (all sub-phases); per-primitive retirement criterion verification per Meta-Architecture §6 |
| `phase-7-back-flow-routing` | `.claude/skills/phase-7-back-flow-routing/SKILL.md` | Fork detection (all sub-phases); design-phase routing per §4.3 |

---

## 7. Phase 7 sub-phase enumeration

Per `Phase_7_Meta_Architecture_v1.md` § Phase 7 internal workflow + `Project_Workflow_v1_8.md` §2.7:

| Sub-phase | Scope | Primary skill | Event-driven skills |
|---|---|---|---|
| 7a | Bootstrap — Claude Code workspace initialization; first H_T primitive landings; substitution scaffolding under single-LLM-during-7a runtime substitution | None specific (consult Meta-Architecture §5 + §6) | `phase-7-back-flow-routing` |
| 7b | Per-axis-stream implementation — IS / AS / CP / OD axis-stream parallel execution per atomic-unit plans | `phase-7-implementation` | `phase-7-substitution-retirement`; `phase-7-back-flow-routing` |
| 7c | Cross-axis composition — CXA v2.1 seam instantiation; cross-axis edge wiring across 6 composition buckets | `phase-7-cross-axis-composition` | `phase-7-substitution-retirement`; `phase-7-back-flow-routing` |
| 7d | Substitution retirement — H_E substitution gradient retirement per self-hosting milestone gradient; closure when all H_E substitutions retired OR bounded-residual carried with documented rationale per X-AL-2 | `phase-7-substitution-retirement` | `phase-7-back-flow-routing` |

---

## 8. Execution invariants

The following invariants hold across all Phase 7 sessions. Violation surfaces to operator immediately.

| Invariant | Statement | Source |
|---|---|---|
| I-1 | Canonical artifact citations resolve byte-exact | `Project_Workflow_v1_8.md` §7.4.2 |
| I-2 | No H_T design extension at Phase 7 execution-time | X-AL-3 (Meta-Architecture §7.7) |
| I-3 | Substitution retirement is event-driven; retirement = (units landed) ∧ (H_E surface no longer invoked) | X-AL-2 (Meta-Architecture §7.7) |
| I-4 | H_E ↔ H_T substrate boundary at MCP server process; process isolation, not convention | X-AL-1 (Meta-Architecture §7.7) |
| I-5 | Class 1 forks halt sub-phase execution; route to design-phase back-flow | §4.3; `Project_Workflow_v1_8.md` §2.7.6 |
| I-6 | Framework-pull discipline holds — no `tenacity` / `pybreaker` / `langgraph` / `crewai` / `langchain` / `llamaindex` / `temporal` / `prefect` / LiteLLM | `Plan_Executability_Audit_v1.md` |
| I-7 | Multi-LLM commitment unchanged — per-provider SDKs under capability-aware abstraction; NOT single-LLM permanent commitment | ADR-F1 v1.2; CP-AL-4 (Meta-Architecture §7.4) |

---

## 9. Workspace bootstrap state

This workspace was bootstrapped at Phase 6.5 Session 6 (ε) per `Phase_6_5_Session_6_Kickoff.md`. Bootstrap substrate authored at design-phase workspace; pushed to this workspace at Session 6 close (operator action).

### 9.1 Filing footer

| Field | Value |
|---|---|
| Artifact | `CLAUDE.md` (workspace root) |
| Authored at | Phase 6.5 Session 6 (ε), 2026-05-15 |
| Authoring authority | `Phase_6_5_Session_6_Kickoff.md` §2.1.1 |
| Predecessor authoring | Design-phase workspace (separate Claude.ai project) |
| Successor consumption | Phase 7 Session 1 onward (this workspace) |
| Revision policy | This file is canonical for this workspace; revisions route to design-phase back-flow (§4.3) prior to in-workspace edit |

---

*End of root `CLAUDE.md`. Per-axis subdirectory `CLAUDE.md` files at `harness-{is,as,cp,od}/`. Skills at `.claude/skills/`. Sub-agent boundary at `Sub_Agent_Boundary_Specification_v1.md`.*
