---
name: frontend-design
description: Create distinctive production-grade frontend interfaces without generic AI aesthetics. Use for web pages, dashboards, components, HTML/CSS, or visual frontend implementation.
---

# Frontend Design — Codex bridge

From any linked worktree, run
`git rev-parse --path-format=absolute --git-common-dir`, take the parent of the
returned `.git` directory, and adopt the complete canonical skill at
`.claude/skills/frontend-design/SKILL.md` under that shared operator root.
Read that file completely before acting, resolve its relative assets from its
own directory, and follow every applicable instruction. The operator-installed
Claude directory remains the source of truth; this bridge does not shorten or
reinterpret its design discipline.

Translate only runner names and tool mechanics. "Claude" in capability prose
means the executing Codex design agent. Use Codex's available browser, image,
and editing surfaces where the canonical skill calls for the corresponding
Claude tool. If the canonical source is missing, report that concrete missing
path instead of silently substituting a generic design workflow.
