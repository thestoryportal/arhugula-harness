---
name: taste-skill
description: Apply high-end visual taste and anti-generic design discipline to frontend products, pages, applications, and interface implementation.
---

# Taste Skill — Codex bridge

From any linked worktree, run
`git rev-parse --path-format=absolute --git-common-dir`, take the parent of the
returned `.git` directory, and adopt the complete canonical skill at
`.claude/skills/taste-skill/SKILL.md` under that shared operator root.
Read it completely before acting and resolve every referenced asset or script
from that directory. The operator-installed Claude directory remains the source
of truth; this bridge does not summarize, narrow, or override its visual system.

Translate only runner names and tool mechanics. Apply its full design process
with Codex's available browser, image, and editing surfaces. If the canonical
path is absent, report the missing installation instead of silently replacing
it with generic frontend advice.
