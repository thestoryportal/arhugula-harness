---
name: impeccable
description: Design, critique, polish, harden, or optimize frontend interfaces across UX, accessibility, typography, layout, color, motion, responsive behavior, and production quality.
---

# Impeccable — Codex bridge

From any linked worktree, run
`git rev-parse --path-format=absolute --git-common-dir`, take the parent of the
returned `.git` directory, and adopt the complete canonical skill at
`.claude/skills/impeccable/SKILL.md` under that shared operator root.
Read it completely before acting. Run its required context script, read every
routed command/register reference, and resolve all relative files from the
canonical skill directory. The operator-installed Claude directory remains the
source of truth; this bridge does not shorten its commands, setup, registers,
absolute bans, or verification obligations.

Translate only runner/tool mechanics. Use the matching Codex browser and editing
capabilities; preserve the canonical skill's stop conditions and required live
checks. If the source path is unavailable, report that exact gap rather than
falling back to a generic UI review.
