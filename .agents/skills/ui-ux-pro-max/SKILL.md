---
name: ui-ux-pro-max
description: UI and UX design intelligence for web and mobile across accessibility, interaction, layout, typography, color, motion, charts, responsive behavior, and multiple implementation stacks.
---

# UI/UX Pro Max — Codex bridge

From any linked worktree, run
`git rev-parse --path-format=absolute --git-common-dir`, take the parent of the
returned `.git` directory, and adopt the complete canonical skill at
`.claude/skills/ui-ux-pro-max/SKILL.md` under that shared operator root.
Read it completely before acting, execute its database/search scripts when
routed, and resolve relative resources from the canonical skill directory. The
operator-installed Claude directory remains the source of truth; this bridge
does not reduce its priorities, search workflow, stack guidance, or checklists.

Translate only runner and tool mechanics. Use Codex's corresponding browser,
component-search, image, and editing surfaces while preserving all substantive
requirements. If the source path is missing, report that exact installation
gap instead of inventing a replacement workflow.
