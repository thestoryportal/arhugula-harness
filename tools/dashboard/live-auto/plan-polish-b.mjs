// Polish pass for candidate B (Almanac noir) via the skill-chaining loop.
// Contained move: the activation/deployment meter card. Real outerHTML so the
// producer polishes the actual element within B's dark instrument identity.
export default [
  {
    classes: 'panel,meter,cold',
    tag: 'div',
    text: 'Activation / deployment closure',
    action: 'polish',
    count: 3,
    acceptVariant: 1,
    pageUrl: '/',
    outerHTML:
      '<div class="panel meter cold">' +
      '<div class="k">Activation / deployment closure</div>' +
      '<div class="big">0<span class="u">% exercised</span></div>' +
      '<div class="sub"><strong>17 of 20</strong> forward items open. This is <strong>not remaining build work</strong>: it is operator-gated (credentials + infrastructure that cannot run in this workspace) and bounded-residual by design.</div>' +
      '<div class="quote">"The harness is built; this axis switches on at a real deployment."</div>' +
      '</div>',
  },
];
