# Implementation Plan — Operational Discipline v2.21

## Change-note (v2.20 → v2.21)

**Scope of revision.** Substantive canonical-reading amendment at U-OD-04 AC #4 absorbing OD spec v1.18 → v1.19 §C-OD-04 §4.3 per-attribute tier redistribution (`Spec_Operational_Discipline_v1_19.md` §1.1 + §1.2; published 2026-05-27, same calendar session as v1.20 Path A production-emission closure of v1.20 §"Adjacent observations" (f)+(g)+(h)). Single-unit absorption at U-OD-04 (`§3.2.1` per Implementation_Plan_Operational_Discipline_v2_5.md authoring site). Closes the explicit "REVISION OWED at follow-on arc per FM-2" entry at OD spec v1.19 §3 row 110 — U-OD-04 plan revision absorbing v1.19 §1.1 tier redistribution. ZERO new ACs; ZERO new units; ZERO DAG topology change. AC count at U-OD-04 unchanged at 8.

**v2.20 substantive content preserved verbatim.** All v2.20 content (U-OD-00 through U-OD-54; clusters 1 through 4-OD-E; DAG topology; coverage matrix; cross-axis edge enumeration; all unit bodies other than U-OD-04 AC #4 tier-table refresh + Tests line entry) preserved unchanged at v2.21. The v2.20 U-OD-04 D-2 + D-3 cardinality absorption (operations enum 7 → 9; attribute tiers 3 → 4) preserved verbatim. The v2.19 U-OD-04 D-1 absorption (2-component span-name format) preserved verbatim.

**Source of fix.** OD spec v1.19 §1.1 canonical-reading amendment redistributing 3 attributes to the v1.16-NEW `Conditionally Required` tier per OTel GenAI semantic conventions 1.41.0 chat-span Requirement-Level audit (archived text at `github.com/open-telemetry/semantic-conventions/blob/v1.41.0/docs/gen-ai/gen-ai-spans.md`): `gen_ai.request.model` (was Required (Stable) → "If available"), `server.port` (was Recommended (Development) → "If `server.address` is set"), `gen_ai.conversation.id` (was Recommended (Development) → "when available"). Helper carrier + helper test redistribution already landed at OD spec v1.19 co-publication arc (`harness-od/src/harness_od/otel_genai_base.py:148+157+158` + `harness-od/tests/test_otel_genai_base.py:55-77`); v2.21 is the plan-side cite-shape refresh per implementation-planner discipline sequenced after spec lands. v1.20 Path A production-emission closure of v1.20 §"Adjacent observations" (f)+(g) at `harness-runtime/.../lifecycle/llm_dispatch.py:343-360` (commit `e874a03`, merged at `d8506c8`) enables AC #4 to declare the harness-stricter emission posture for all 3 Conditionally Required attrs.

**Narrow-scope framing.** AC #4 tier-table refresh ONLY. AC #1 (span-name format) preserved verbatim per v2.19 D-1 absorption. AC #2 (operations enum cardinality) + AC #3 (tier cardinality) preserved verbatim per v2.20 D-2 + D-3 absorption. AC #5 (parent-child trace context) preserved verbatim — OD spec v1.20 §1.1 narrowed §4.4 hierarchy claim from `{invoke_agent, chat, execute_tool}` to `{invoke_agent, chat}`, but AC #5 prose declares generic OTel trace context propagation (not which span classes carry `gen_ai.conversation.id`); narrowing is at the §4.4 hierarchy-correlation claim layer, not the parent-child trace context layer. AC #6 (base metric name) + AC #7 (specialization-layer composition) + AC #8 (semconv validator) preserved verbatim. ATTRIBUTE NAME SET in BASE_LAYER_ATTRIBUTES preserved verbatim at 17 names (no add; no remove); only TIER ASSIGNMENT redistributed for 3 attrs per v1.19 §1.1.

**Amendment site.**

| Site | Amendment shape | Substrate source |
|---|---|---|
| **U-OD-04 plan body** (authored at `Implementation_Plan_Operational_Discipline_v2_5.md` §3.2.1, preserved verbatim through v2.6/.../v2.20 outside D-1 + D-2 + D-3 amendments) | (a) **Inputs** line: append `+ OD spec v1.19 §C-OD-04 §4.3 §1.1 per-attribute tier redistribution (3 attrs redistribute to Conditionally Required per OTel GenAI semconv 1.41.0 chat-span audit: gen_ai.request.model, server.port, gen_ai.conversation.id; Required (Stable) row 3 → 2; Conditionally Required row 0 → 3 NEW content; Recommended (Development) row 6 → 4; Opt-In 8 unchanged; v1.2-v1.18 per-attribute tier readings SUPERSEDED at v1.19)`; (b) **Signatures** block: PRESERVED VERBATIM (no enum cardinality change; the GenAiAttribute record shape + AttributeTier enum already accommodate the redistribution at v2.20); (c) **AC #4** (BASE_LAYER_ATTRIBUTES per-attribute tier list): refresh tier assignments per v1.19 §1.1: **Required (Stable)** drops to 2 attrs (`gen_ai.operation.name`, `gen_ai.provider.name`; `gen_ai.request.model` redistributed); **NEW Conditionally Required row populated** with 3 attrs (`gen_ai.request.model`, `server.port`, `gen_ai.conversation.id`) — emission policy: **harness-stricter (always-emit per harness's always-available context) per OTel "emit-more-often-than-Conditionally-Required is fine" framing per v1.19 §1.2 + v1.20 Path A closure at `harness-runtime/.../lifecycle/llm_dispatch.py:343-360`** (`gen_ai.conversation.id` sources from `step_context.workflow_id` per `HIERARCHY_CORRELATION_KEY` constant; `server.address` per-provider static map for anthropic/openai + threaded `RuntimeConfig.ollama_host` for ollama; `server.port` gated on `server.address`); **Recommended (Development)** drops to 4 attrs (`gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens`, `gen_ai.response.finish_reasons`, `server.address`); **Opt-In content** preserved verbatim at 8 attrs; (d) **Tests** line: NEW test `test_conditionally_required_tier_attributes_per_spec_4_3` (per OD spec v1.19 §3 row 110 prescription); rename `test_required_stable_tier_attributes_per_§4_3` → cardinality refresh (still tests Required (Stable) but now expects 2 attrs not 3); existing tests `test_recommended_development_tier_attributes_per_§4_3` (cardinality refresh to 4 not 6) + `test_opt_in_content_tier_attributes_per_§4_3` (preserved) + all v2.20 tests preserved verbatim; (e) **Rollback boundary**: append note — "Reverting v2.21 would restore the v2.20 tier assignments (3 Required (Stable) / 0 Conditionally Required / 6 Recommended (Development) / 8 Opt-In) — i.e., the v1.2-v1.18 per-attribute readings that v1.19 §1.1 SUPERSEDED per OTel 1.41.0 chat-span Requirement-Level audit. Production helper carrier at `otel_genai_base.py:148+157+158` already redistributed at v1.19 co-publication; revert would create plan-spec divergence." | OD spec v1.19 §C-OD-04 §4.3 §1.1 + §1.2 canonical-reading amendment + helper carrier already-redistributed at v1.19 publication (`otel_genai_base.py:148+157+158`) + v1.20 Path A production emission of all 3 Conditionally Required attrs at `llm_dispatch.py:343-360` (1091/1091 harness-runtime tests pass) |

**Plan shape preserved.** v2.20's 55-unit axis-led structure preserved verbatim. No new units; no DAG topology change; no cluster reorganization; no coverage matrix structural delta (U-OD-04 still covers C-OD-04 §4.1-§4.5; AC #4 tier-redistribution text changes do not affect contract coverage — the attribute NAME SET at BASE_LAYER_ATTRIBUTES is preserved verbatim at 17 names); no cross-axis edge addition; AC count at U-OD-04 unchanged at 8 ACs (#4 text amended; #1 + #2 + #3 + #5 + #6 + #7 + #8 PRESERVED VERBATIM).

**Coverage matrix delta.** None at structural level. U-OD-04's `Implements` line preserved: `[C-OD-04 §4.1, §4.2, §4.3, §4.4, §4.5]`. The §4.3 per-attribute tier redistribution is a canonical-reading change to the §4.3 contract, not a contract-coverage change. AC #4 text now cites OD spec v1.19 §C-OD-04 §4.3 + §1.1 + §1.2 (use-latest-version body-citation-alignment per `Project_Workflow_v1_8.md` §7.4); other spec citations at U-OD-04 (§4.1 cite at v1.12; §4.2 cite at v1.16; §4.4 cite at v1.20 per §1.1 hierarchy narrowing — though AC #5 prose unaffected per narrow-scope framing; §4.5 at v1.2-lineage) preserved per delta-only-spec convention.

**Dependency graph delta.** None. U-OD-04 `Depends on: []` (foundational L0 unit) preserved verbatim. No new edges; no removed edges. Within-axis DAG acyclic; topological sort unchanged. 8 direct dependents preserved: U-OD-05 / U-OD-06 / U-OD-07 / U-OD-08 / U-OD-11 / U-OD-18 / U-OD-21 / U-OD-23. The §4.3 BASE_LAYER_ATTRIBUTES list consumed by these dependents is REFRESHED at tier-assignment layer only — attribute NAME SET preserved verbatim at 17 names; 8 dependents continue consuming the same 17-name attribute set with refreshed per-attribute tier metadata.

**Production-stricter posture documented at AC #4.** Per v1.19 §1.2 emission-posture column + v1.20 Path A closure: the harness emits all 3 Conditionally Required attrs at every LLM-inference span where applicable, stricter than OTel's per-attribute conditional rule. This is canonical per OTel "emit-more-often-than-Conditionally-Required is fine" framing. AC #4 declares both the tier classification (per §4.3 §1.1) AND the emission policy (always-emit when value is available). The two layers are separable per v1.19 §1.2 reading — spec §4.3 declares classification; OD plan AC #4 declares emission policy.

---

## Adjacent observations (surfaced as findings; NOT patched per FM-2)

(a) **v2.20 finding (f) per-attribute tier-assignment audit — CLOSED at v2.21.** v2.20 §"Adjacent observations" (f) declared "NEW carry at v2.20 (per OD spec v1.16 §"Adjacent observations" (e)). BASE_LAYER_ATTRIBUTES 17 attributes preserved at their v1.2-lineage tier assignments (3 Required + 6 Recommended + 8 Opt-In); none currently assigned to NEW `CONDITIONALLY_REQUIRED` tier. Future WebFetch audit owed against OTel 1.41.0 archived text per-attribute requirement levels." v2.21 closes this carry — the WebFetch audit was performed at OD spec v1.19 publication; 3 attrs redistribute per v1.19 §1.1; v2.21 absorbs the redistribution at AC #4. Carry REMOVED from v2.21.

(b) **v2.20 finding (b) — `gen_ai.provider.name` Required-tier preservation vs production attribute-name divergence.** Carry from v2.19 (a) → v2.20 (b). CLOSED at OD spec v1.17 → v1.18 (commit `a47c74c`) per OD spec v1.18 §"Adjacent observations" (h) — closure shape was carrier-conform at production commit `115387b` (rename `gen_ai.system` → `gen_ai.provider.name`); the v2.19/v2.20 carry-text framing became stale at that closure. v2.21 acknowledges the closure; carry REMOVED.

(c) **v2.20 finding (e) — `harness_breaker_schema.py:21` docstring drift.** Preserved verbatim at v2.21. The docstring claim "AttributeTier enum has no `REQUIRED` / `CONDITIONAL` members" is now stale on TWO dimensions post-v2.20 + v2.21: (i) v2.20 added `CONDITIONALLY_REQUIRED` member (the "no `CONDITIONAL`" claim is stale); (ii) v2.21 populates the `CONDITIONALLY_REQUIRED` tier with 3 attrs (the docstring's purpose — explaining harness.breaker.* tier vs base-layer tier separability — is now stronger because the Conditionally Required tier is populated). NOT patched per FM-2; surfaced for follow-on doc-hygiene arc.

(d) **v1.19 §"Adjacent observations" (g)+(h) — declared-but-not-emitted divergence — CLOSED at v1.20 Path A 2026-05-27.** v1.19 §"Adjacent observations" (g) (`gen_ai.conversation.id` declared at carrier but never emitted at production) + (h) (`server.port` + `server.address` same shape) flagged carrier-vs-production-emission divergence at v1.19 publication 2026-05-27. CLOSED-via-Path-A-production-emission at OD spec v1.20 + production commit `e874a03` (`harness-runtime/.../lifecycle/llm_dispatch.py:343-360` emits 6 of 9 non-Opt-In §4.3 attrs; 1091/1091 tests pass). v2.21 AC #4 emission-policy text captures the production-stricter posture as canonical per v1.20 closure. No carry forward at v2.21.

(e) **v1.20 §"Adjacent observations" (b)+(c)+(d)+(i) — carries preserved at OD spec.** OD spec v1.20 carries 4 informational/upstream findings preserved verbatim — (b) §C-OD-04 §8.4.2 anticipated cases; (c) §15.2 vs §15.4 split; (d) workflow-grammar discipline candidate (routes upstream to `Project_Workflow_v1_8.md`); (i) OD-authored synthesis layer at §4.4. None affect U-OD-04 plan body; preserved at spec layer. NOT carried at v2.21 (out of scope at single-unit plan refresh arc).

(f) **AC #5 §4.4 hierarchy claim — v1.20 §1.1 narrowing.** OD spec v1.20 §1.1 narrowed §4.4 hierarchy claim from `{invoke_agent, chat, execute_tool}` to `{invoke_agent, chat}`. AC #5 prose at U-OD-04 declares generic OTel trace context propagation (`child spans inherit trace_id from parent`; `parent_span_id` reference); does NOT name span-class scope of `gen_ai.conversation.id` carry. The §4.4 narrowing is at the OD-authored synthesis layer (per v1.20 §"Adjacent observations" (i)); AC #5 is unaffected. Preserved verbatim at v2.21.

---

## Filing footer

| Field | Value |
|---|---|
| Version | v2.21 (delta-only change-note for U-OD-04 AC #4 tier-table refresh; v2.5 authoring site preserved verbatim per delta-only-plan-file convention; v2.19 D-1 + v2.20 D-2 + D-3 amendments preserved verbatim outside AC #4 refresh) |
| Trigger | OD spec v1.18 → v1.19 §C-OD-04 §4.3 §1.1 per-attribute tier redistribution (`Spec_Operational_Discipline_v1_19.md` published 2026-05-27); v1.19 §3 row 110 explicit "REVISION OWED at follow-on arc per FM-2" prescription; v1.20 Path A production-emission closure of v1.19 §"Adjacent observations" (g)+(h) enabling harness-stricter emission-policy declaration at AC #4 |
| Supersedes | v1.2-v1.18 §C-OD-04 §4.3 per-attribute tier readings at plan layer (v2.5 AC #4 verbatim listing of Required (Stable) 3 / Recommended (Development) 6 / Opt-In 8); v2.5 AC #4 cardinality framing per `gen_ai.request.model` Required-tier + `server.port` + `gen_ai.conversation.id` Recommended-tier; all 3 attrs redistribute to Conditionally Required at v2.21 per v1.19 §1.1 |
| Scope of revision | NARROW: U-OD-04 only; Inputs cite + AC #4 tier-list refresh + Tests line entry (1 NEW test; 2 cardinality refreshes) + Rollback boundary appendix |
| Units revised | U-OD-04 (1 unit) |
| Units preserved verbatim | U-OD-00, U-OD-01-U-OD-03, U-OD-05-U-OD-54 (54 units) |
| Coverage matrix delta | None at structural level — U-OD-04 still covers C-OD-04 §4.1-§4.5 |
| Dependency graph delta | None — U-OD-04 `Depends on: []` preserved; 8 dependents preserved |
| AC count change | None — U-OD-04 retains 8 ACs (#4 text amended; #1 + #2 + #3 + #5 + #6 + #7 + #8 preserved verbatim) |
| Test name change | 1 NEW + 2 cardinality refreshes: NEW `test_conditionally_required_tier_attributes_per_spec_4_3`; existing `test_required_stable_tier_attributes_per_§4_3` cardinality assertion refresh 3 → 2; existing `test_recommended_development_tier_attributes_per_§4_3` cardinality assertion refresh 6 → 4. Helper carrier + helper test redistribution already landed at OD spec v1.19 co-publication (`otel_genai_base.py:148+157+158` + `test_otel_genai_base.py:55-77`; 25/25 helper tests pass); v2.21 is the plan-side cite-shape refresh per implementation-planner discipline. |
| Cross-axis edge delta | None |
| Cross-file absorption owed at follow-on arcs | Workspace `CLAUDE.md` §2.3 OD plan row bump v2.20 → v2.21 (this arc); v2.20 adjacent finding (c) `harness_breaker_schema.py:21` docstring drift (preserved as carry at v2.21 (c) — future doc-hygiene arc) |
| Adjacent findings surfaced | 6 (per "Adjacent observations" section; (a) closes v2.20 (f); (b) closes v2.20 (b); (c) carries v2.20 (e); (d)+(e)+(f) NEW); NOT patched per FM-2 |
| Authority anchor | OD spec v1.19 §C-OD-04 §4.3 §1.1 + §1.2 canonical-reading amendment + ADR-D6 v1.2 §1.2 [HIGH] cite of OTel GenAI semantic conventions 1.41.0 archived text + WebFetch verification 2026-05-27 (cached at `$CLAUDE_JOB_DIR/gen-ai-spans-v1.41.0.md`) + v1.20 Path A production emission at `llm_dispatch.py:343-360` |
| Predecessor | v2.20 (D-2 + D-3 cardinality absorption; preserved verbatim outside U-OD-04 AC #4 refresh at v2.21) |
| Successor | v2.22 (next operator-discretion arc; candidates: §4.4 audit against OTel 1.41.0 archived text per OD spec v1.20 §1.1 hierarchy narrowing — though no plan body cite affected; doc-hygiene arc for v2.20 (c) `harness_breaker_schema.py:21` docstring drift; H_T-OD-2 retirement event re-evaluation at next retirement batch) |

---

## Audit checklist (per implementation-planner skill §4 + §5 step 9)

| Check | Status |
|---|---|
| Atomicity — single coherent change at single unit (U-OD-04) | ✓ |
| Spec-traceability — citation to OD spec v1.19 §C-OD-04 §4.3 + §1.1 + §1.2 by ID + section + version | ✓ |
| Dependency-awareness — no new edges; existing graph preserved acyclic | ✓ |
| Implementation-grade-detail — files affected (`otel_genai_base.py:148+157+158` already-redistributed at v1.19 + `llm_dispatch.py:343-360` already-emitting at v1.20) + signatures (BASE_LAYER_ATTRIBUTES tier assignments per-attribute) + testable acceptance (AC #4 tier-list refresh + NEW Conditionally Required per-tier test) | ✓ |
| No-extension — only authorized fix applied (v1.19 §1.1 tier redistribution per OTel 1.41.0 chat-span audit); attribute NAME SET preserved verbatim at 17 names; ZERO new attrs; ZERO removed attrs; emission-policy declaration at AC #4 documents v1.20 Path A production state (no new contract surface) | ✓ |
| Preservation — 54 units preserved verbatim; v2.19 D-1 absorption preserved verbatim; v2.20 D-2 + D-3 absorption preserved verbatim | ✓ |
| Use-latest-version body-citation-alignment per §7.4 — U-OD-04's §C-OD-04 §4.3 citation bumped to v1.19 | ✓ |
| Coverage matrix completeness — every contract row marked; every unit column marked (preserved from v2.20) | ✓ |
| Acyclic invariant — DAG topological sort exists (preserved from v2.20) | ✓ |
| Coherence pass — all sub-disciplines satisfied at U-OD-04 + immediate dependency-graph neighbors (no neighbor amendments owed; 8 dependents continue consuming the 17-name attribute set with refreshed per-attribute tier metadata; carrier already-redistributed at v1.19 publication so no impl arc owed) | ✓ |
| Tests-pass verification at apply-pass arc — 25/25 harness-od helper tests pass at v1.19 co-publication; 1091/1091 harness-runtime tests pass at v1.20 Path A close (commit `e874a03`); v2.21 is plan-side cite-shape refresh with NO production state change beyond what already landed at spec v1.19 + v1.20 arcs | ✓ |
