# Implementation Plan — Operational Discipline v2.17

## Change-note (v2.16 → v2.17)

**Scope of revision.** Sub-arc B sequel U-OD-41 absorption per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD plan v2.16 §0(d) "operator preference (i) revise-signature noted at session checkpoint" + OD spec v1.10 NEW §C-OD-26.6 CostRecordAuditPayload typed carrier + amended §C-OD-26.1 canonical invocation prose (both at `Spec_Operational_Discipline_v1_10.md` co-published this session) + CXA v2.9 `Cross_Axis_Composition_Document_v2_9.md` §2.3.7 row 8 cost-attribution audit-write seam (at HEAD `39e4f1c` 2026-05-24). U-OD-41 plan body amendment scope: **Implements** line + **Signatures** line + **Files** line + **ACs** revisions absorbing the v1.10 spec contract.

**v2.16 substantive content preserved verbatim.** All v2.16 content (U-OD-00 through U-OD-54; clusters 1 through 4-OD-E; DAG topology; coverage matrix; cross-axis edge enumeration; all unit bodies other than U-OD-41) preserved unchanged at v2.17. The U-OD-51 PauseResumeAuditPayload absorption at v2.16 + Sub-arc A cross-axis-block lift preserved verbatim. The v2.15 + v2.14 + ... + v2 chain all preserved.

**Source of fix.** Sub-arc B of the 3-arc cascade per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6. Companion artifacts at this session: OD spec v1.10 (`Spec_Operational_Discipline_v1_10.md` — NEW §C-OD-26.6 + §C-OD-26.1 amendment); CXA v2.9 (already at HEAD `39e4f1c` 2026-05-24). Companion impl arc this same session: cost_namespace.py NEW module at harness-od + cp_audit_conversion.py extension (cost: branch restoration) + U-OD-41 helper landing + production wiring at cost_attribution_llm_dispatch.py.

**Single amendment site.**

| Site | Amendment shape | Substrate source |
|---|---|---|
| **U-OD-41 plan body** | (a) **Implements** line: cite bump `OD spec v1.8 §C-OD-26.3` → `OD spec v1.10 §C-OD-26.1 (amended) + §C-OD-26.3 (preserved) + §C-OD-26.6 (NEW)`; (b) **Files** line: add `harness-od/src/harness_od/cost_namespace.py` (NEW for CostRecordAuditPayload class) alongside existing `harness-od/src/harness_od/cost_record_audit_writer.py` (NEW for the helper); (c) **Signatures** line: helper rename + signature widening per §C-OD-26.1 v1.10 amendment — `_project_cost_record_to_audit_entry(attached: SpanCostRecord) -> CPAuditLedgerEntry` → `_project_cost_record_to_audit_payload(attached: SpanCostRecord, *, workflow_id: str, parent_action_id: str, actor: str, prior_event_hash: str \| None) -> CostRecordAuditPayload`; also add converter-integration signature `cp_audit_to_od_audit` extension at harness-cxa to accept `CostRecordAuditPayload` per §C-OD-26.6.2; (d) **ACs**: 8 ACs total (was 5) — AC #1 action_id pattern bumped to `cost:<workflow_id>:<step_action_id>` per CXA v2.9 §0.3 row 8 (was `cost:{span_id}` per OD spec v1.8 §26.3 prose, preserved at v1.10 but operationalized to the more-specific CXA pattern at §C-OD-26.6.1 step 2); AC #3 reviewer-discretion note RESOLVED at CXA v2.9 row 8 + U-CP-72 minor revision this session (the 8th-pattern bucket extension is now canonical); NEW ACs #6 (CostRecordAuditPayload class + cost_namespace.py module landing), #7 (cp_audit_to_od_audit cost: branch restoration), #8 (production wiring at cost_attribution_llm_dispatch.py callsite per §C-OD-26.1 v1.10 invocation pattern). Existing AC #2 + #4 + #5 preserved verbatim. | OD spec v1.10 §C-OD-26.1 + §C-OD-26.6 + CXA v2.9 §0.3 row 8 + cp_audit_conversion.py extension pattern (mirrors PauseResumeAuditPayload at Sub-arc A landing) |

**Plan shape preserved.** v2.16's 55-unit axis-led structure preserved verbatim. No new units; no DAG topology change at any cluster boundary; no cluster reorganization; no coverage matrix delta beyond U-OD-41's spec-cite bump; no cross-axis edge addition (the CXA v2.9 row 8 was added at the CXA spec, not at this plan); no AC count change at any unit other than U-OD-41 (5 → 8 ACs at U-OD-41 only).

**Status posture.** Proposed (v2.16) → **Proposed (v2.17)**. v2.17 is a spec-revision-driven plan revision — single-unit-body amendment at U-OD-41 absorbing the v1.10 spec contract.

**Adjacent defects surfaced (not patched per FM-2 no-extension discipline).**

(i) **AC #3 reviewer-discretion note resolution.** The v2.14-authored AC #3 reviewer-discretion note ("reviewer to confirm bucket sizing or extend U-CP-72") is RESOLVED at this v2.17 absorption: CXA v2.9 row 8 IS the bucket extension (CP→OD 7→8); U-CP-72 minor revision at this session restores the cost: branch. AC #3 prose preserved verbatim outside the reviewer-discretion bracket-clause; the clause itself updates to reflect resolution.

(ii) **`audit.cp.response = "cost_attributed"` field set RESOLVED at OD spec v1.10 §C-OD-26.6.** Per OD spec v1.8 §26.3 prose preserved at v1.10: `audit.cp.response = "cost_attributed"` is the audit-ledger entry's response field. v1.10 §C-OD-26.6 RESOLVED the structural-placement question by declaring `audit_cp_response: str` as the 2nd of 4 `audit_cp_*` common fields on CostRecordAuditPayload (mirrors empirical PauseResumeAuditPayload field-set at `harness-od/src/harness_od/pause_resume_namespace.py:220-221`). Helper constructs CostRecordAuditPayload with `audit_cp_response="cost_attributed"` inlined as a constant per §C-OD-26.6.1 step 4 (preserves sibling-subclass uniformity).

(iii) **Per-axis CLAUDE.md bucket-count refresh.** CXA v2.9 §0.7(iv) flagged harness-cp/CLAUDE.md §2.3 + harness-od/CLAUDE.md §2.2 cross-axis-bucket count refresh as owed. This v2.17 plan revision does NOT touch per-axis CLAUDE.md files (scope is OD plan body amendment); the per-axis refresh remains a separate workspace-governance follow-on per CXA v2.9 §0.8(e) downstream-owed list.

(iv) **U-OD-41 implementation status at HEAD.** Per pre-arc verification (HEAD `39e4f1c`): `_project_cost_record_to_audit_entry` helper does NOT exist in production code; `harness-od/src/harness_od/cost_record_audit_writer.py` does NOT exist; the SpanCostRecord type exists at `harness-od/src/harness_od/idempotency_join_dedup.py:134` and is consumed by cross_family_rollup but not yet wired to audit-ledger write per existing §C-OD-26.1 invocation. U-OD-41 implementation is GREEN-FIELD at this arc; the impl arc lands the helper + the cost_namespace.py module + the cp_audit_conversion.py extension + the production wiring all together. The Implements/Signatures/Files lines at v2.17 reflect the v1.10 spec contract; impl arc following this plan revision lands all 4 changes per the 8 ACs.

(v) **Workspace CLAUDE.md §2.4 OD plan row version bump (v2.16 → v2.17).** APPLIED at this session per OD spec v1.10 change-note §(a) downstream-owed item.

(vi) **Production callsite migration deferred per FM-2 + `[[halt-route-split-AC-pattern]]`.** Per AC #8 rewrite (above): existing production code at `cost_attribution_llm_dispatch.py:198-202` uses the CPAuditLedgerEntry path with `cost:{span_id}` action_id pattern (operational since U-OD-38 landing per documented placeholder shape). The v1.10 typed CostRecordAuditPayload path + NEW `cost:{workflow_id}:{step_action_id}` pattern is now AVAILABLE post this arc. Migration is operational refinement, NOT spec-correctness gate (both paths produce valid audit entries per v1.10 §C-OD-26.3 prose preserved). The follow-on arc that migrates production code requires (a) widening `attribute_llm_dispatch_cost` signature to accept `workflow_id` + `parent_action_id`, (b) updating caller at `llm_dispatch.py:701` to pass these from runtime context, (c) replacing `_project_and_convert_audit_entry()` helper at `cost_attribution_llm_dispatch.py:209-244` with the canonical `_project_cost_record_to_audit_payload()` import. Operator-discretion timing.

**Downstream absorption owed (post-v2.17).**

(a) Workspace `CLAUDE.md` §2.4 OD plan row version bump (v2.16 → v2.17) + description amendment to enumerate the U-OD-41 absorption shape. APPLIED at this session.

(b) Impl arc this same session: 4 code landings per the 8 ACs — cost_namespace.py + cost_record_audit_writer.py + cp_audit_conversion.py extension + production wiring at cost_attribution_llm_dispatch.py callsite.

(c) Memory entry `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` description advance per OD spec v1.10 change-note §(c).

(d) Future retirement-event batch may file H_T-OD-5 (cost-attribution chain) status advance per CXA v2.9 §0.8(d) + OD spec v1.10 §(d) downstream-owed-list framing (the chain now writes audit entries via the canonical converter path post-impl-arc-close; criterion-B operational-MET may shift).

(e) Per-axis CLAUDE.md bucket-count refresh per CXA v2.9 §0.7(iv) + this change-note (iii). Separate workspace-governance follow-on; operator-discretion timing.

---

## §1 — U-OD-41 plan-body amendment (v2.17)

The v2.14-authored U-OD-41 plan body (preserved verbatim through v2.15 + v2.16) is amended at v2.17 absorbing OD spec v1.10 NEW §C-OD-26.6 + amended §C-OD-26.1. Pre-v2.17 plan body preserved verbatim outside the 4 amendment cells (Implements + Files + Signatures + ACs).

### U-OD-41 — Cost-record audit-ledger write composition (REVISED v2.17)

- **Implements:** OD spec v1.10 §C-OD-26.1 (amended canonical invocation signature per v1.10 §2 — replaces helper-only path with canonical `cp_audit_to_od_audit` converter consuming CostRecordAuditPayload typed carrier) + §C-OD-26.3 (preserved audit-ledger write per cost-record prose — `audit.cp.action_id = f"cost:{span_id}"` + `audit.cp.response = "cost_attributed"` via the canonical converter at `harness-cxa/src/harness_cxa/cp_audit_conversion.py`) + §C-OD-26.6 (NEW at v1.10 — CostRecordAuditPayload typed carrier declaration with 4 audit_cp_* common fields + 5 cost-specific fields per CXA v2.9 §0.3 row 8 enumeration projected from SpanCostRecord). Cross-axis carrier: CXA v2.9 §2.3.7 row 8 (cost-attribution audit-write seam — bucket CP→OD 8th typed seam; `cost:` action_id prefix discriminator) + cp_audit_to_od_audit converter U-CP-72 minor revision restoring `cost:` branch at this session.

- **Files:**
  - `harness-od/src/harness_od/cost_namespace.py` (NEW — `CostRecordAuditPayload` Pydantic v2 BaseModel class per OD spec v1.10 §C-OD-26.6 field-set; module parallel to existing `harness-od/src/harness_od/pause_resume_namespace.py` + `hitl_webhook_namespace.py` + `validator_namespace.py` + `mcp_trust_namespace.py` + `hitl_operator_burden_namespace.py` AuditPayload-subclass-module pattern).
  - `harness-od/src/harness_od/cost_record_audit_writer.py` (NEW — `_project_cost_record_to_audit_payload(attached: SpanCostRecord, *, workflow_id: str, parent_action_id: str, actor: str, prior_event_hash: str | None) -> CostRecordAuditPayload` helper per OD spec v1.10 §C-OD-26.1 v1.10 invocation pattern + §C-OD-26.6.1 helper-composition discipline).
  - `harness-cxa/src/harness_cxa/cp_audit_conversion.py` (EXTEND — import `CostRecordAuditPayload` from `harness_od.cost_namespace`; add `COST_AUDIT_NAMESPACE_PREFIX = "audit.cost"` constant; extend `CpAuditCarrier` union to include `CostRecordAuditPayload`; add `elif isinstance(cp_entry, CostRecordAuditPayload):` branch parallel to 5 existing producer-specific branches; remove the cost-specific STRUCK-fallback prose from the TypeError message at line 293-299 per OD spec v1.10 §C-OD-26.6.2 step 5).
  - `harness-runtime/src/harness_runtime/lifecycle/cost_attribution_llm_dispatch.py` (or equivalent production billable-span exit site) — wire the v1.10 §C-OD-26.1 invocation pattern: call `_project_cost_record_to_audit_payload(...)` + `cp_audit_to_od_audit(cost_payload, ...)` + `ctx.audit_writer.append(tenant_id, audit_entry)`. Production callsite per existing §C-OD-26.1 invocation contract (billable-span exit hook).

- **Signatures:**
  - `class CostRecordAuditPayload(BaseModel)` at `harness-od/src/harness_od/cost_namespace.py` with `model_config = ConfigDict(extra="forbid", frozen=True)`; 9 fields per OD spec v1.10 §C-OD-26.6 (4 audit_cp_* common per empirical sibling-subclass pattern at PauseResumeAuditPayload `pause_resume_namespace.py:216-227`: `audit_cp_action_id: str` + `audit_cp_response: str` + `audit_cp_timestamp: str` + `audit_cp_prior_event_hash: str`; 5 cost-specific: `span_id: str` + `idempotency_key: str` + `provider: str` + `model_id: str` + `usage_total_cost_usd: float`); `usage_input_tokens`/`usage_output_tokens`/`cumulative_cost_usd` per §C-OD-26.6.5 deferred-to-implementation-discretion enumeration MAY be added as Optional fields at impl per FM-2.
  - `_project_cost_record_to_audit_payload(attached: SpanCostRecord, *, workflow_id: str, parent_action_id: str, prior_event_hash: str, timestamp: str = "") -> CostRecordAuditPayload` at `harness-od/src/harness_od/cost_record_audit_writer.py` per OD spec v1.10 §C-OD-26.1 v1.10 invocation signature + §C-OD-26.6.1 helper-composition discipline (action_id pattern enforcement; `audit_cp_response="cost_attributed"` hard-coded per §C-OD-26.6.1 step 4; `prior_event_hash` defaults to `"0"*64` sentinel at first-cost-record-in-workflow per §C-OD-26.6.1 step 3; `timestamp` defaults to `""` MVP sentinel per §24.4 NOTE 8a-iii).
  - `cp_audit_to_od_audit(cp_entry: CpAuditCarrier, *, key_id: str, algo: SignatureAlgorithm = SignatureAlgorithm.ED25519, entry_core: StateLedgerEntryRef | None = None) -> AuditLedgerEntry` — existing signature at `harness-cxa/src/harness_cxa/cp_audit_conversion.py` preserved verbatim; the `CpAuditCarrier` union TYPE widens to include `CostRecordAuditPayload` (per OD spec v1.10 §C-OD-26.6.2 step 3); the function body gains a NEW `elif isinstance(cp_entry, CostRecordAuditPayload):` branch (per §C-OD-26.6.2 step 4) parallel to the existing 5 producer-specific branches. Signature itself unchanged.

- **Depends on:** (within-cluster) [U-OD-38, U-OD-39, U-OD-40 — existing cross-axis-substrate predecessors preserved verbatim from v2.14]; (cluster-boundary, CP-axis) [U-CP-72 (cross-axis: CP) — preserved verbatim; the minor revision restoring the `cost:` branch at this session is a same-session co-publication, not a new DAG-predecessor]; (within-axis-cross-package) (none).

- **Rollback boundary:** Single commit revertible — the cost_namespace.py + cost_record_audit_writer.py + cp_audit_conversion.py extension + production wiring are bound together by the binding-chain contract per OD spec v1.10 §C-OD-26.6.2 + §C-OD-26.1 v1.10 invocation pattern; reverting the commit returns the cost: branch to STRUCK fallback per pre-v2.17 state at HEAD `39e4f1c`.

- **ACs:**
  1. **Cost-record projects to typed audit payload with `audit.cp.action_id` pattern.** Per OD spec v1.10 §C-OD-26.6.1 step 2: `audit_cp_action_id = f"cost:{workflow_id}:{step_action_id}"` (NOT `f"cost:{span_id}"` — the v1.10 §C-OD-26.6.1 step 2 supersedes the v1.8 §26.3 prose `cost:{span_id}` shape with the CXA v2.9 §0.3 row 8 more-specific pattern per change-note (ii)). Helper-composed only per §C-OD-26.6.1 step 1.
  2. **`audit.cp.response = "cost_attributed"`** at the AuditLedgerEntry (set by the converter post-conversion OR by the helper at construction per change-note (ii) implementer-discretion). Per OD spec v1.8 §C-OD-26.3 prose preserved verbatim at v1.10.
  3. **Routes via `cp_audit_to_od_audit` converter with `cost:` branch RESTORED.** The cost: branch at the converter handles CostRecordAuditPayload per OD spec v1.10 §C-OD-26.6.2 step 4 (added `elif isinstance(cp_entry, CostRecordAuditPayload):` parallel to the 5 existing producer-specific branches). CXA v2.9 row 8 (bucket 7→8) + U-CP-72 minor revision this session land the converter extension. (v2.14 reviewer-discretion note RESOLVED at this AC text.)
  4. **Idempotency-key joins to parent IS state-ledger entry** per existing C-OD-24.4 invariant. The CostRecordAuditPayload `idempotency_key` field carries the SpanCostRecord's idempotency_key (the §14.4 join key from C-IS-05); preserves the existing audit-trail join discipline.
  5. **Integration test: cost-record write + audit ledger query.** Per existing v2.14 AC preserved verbatim. Test: invoke the full §C-OD-26.1 v1.10 invocation pattern at a billable-span exit; verify the AuditLedgerEntry lands at audit_writer.append; verify the audit-ledger query returns the entry with `action_id` starting with `cost:` + `response = "cost_attributed"` + the projected cost-specific fields.
  6. **`CostRecordAuditPayload` class authored at `harness-od/src/harness_od/cost_namespace.py`** per OD spec v1.10 §C-OD-26.6 field-set: Pydantic v2 BaseModel with `model_config = ConfigDict(extra="forbid", frozen=True)`; 9 fields (4 audit_cp_* common + 5 cost-specific per §C-OD-26.6); module parallels the existing 5 sibling AuditPayload-subclass modules. Importable: `from harness_od.cost_namespace import CostRecordAuditPayload` resolves. (NEW at v2.17.)
  7. **`cp_audit_to_od_audit` extended with `cost:` branch + COST_AUDIT_NAMESPACE_PREFIX constant.** Per OD spec v1.10 §C-OD-26.6.2 steps 1-5: import `CostRecordAuditPayload`; add `COST_AUDIT_NAMESPACE_PREFIX = "audit.cost"`; widen `CpAuditCarrier` union to include `CostRecordAuditPayload`; add isinstance branch parallel to 5 existing branches; remove cost-specific STRUCK fallback from TypeError message at line 293-299. Existing 6 carrier branches (CPAuditLedgerEntry + WebhookDeliveryAuditPayload + OperatorBurdenAuditPayload + ValidatorEscalationAuditPayload + TrustEvaluationAuditPayload + PauseResumeAuditPayload) preserved verbatim. (NEW at v2.17.)
  8. **Production migration to typed CostRecordAuditPayload path DEFERRED per FM-2 + `[[halt-route-split-AC-pattern]]` precedent.** Current production code at `harness-runtime/src/harness_runtime/lifecycle/cost_attribution_llm_dispatch.py:198-202` calls `audit_writer.append` via the CPAuditLedgerEntry path with `cost:{span_id}` action_id pattern (documented placeholder per the function's docstring at line 222 — "U-CP-72 will canonicalize this projection at the converter producer-side rewrite"). The v1.10 typed CostRecordAuditPayload path + NEW action_id pattern `cost:{workflow_id}:{step_action_id}` per §C-OD-26.6.1 step 2 is now AVAILABLE post this arc (carrier + helper + converter branch all land). Migration of the production callsite from CPAuditLedgerEntry path → typed CostRecordAuditPayload path requires widening `attribute_llm_dispatch_cost` function signature to accept `workflow_id: str` + `parent_action_id: str` kwargs + updating the caller at `llm_dispatch.py:701` to pass these from runtime context. Migration is DEFERRED at this arc per FM-2 no-extension discipline (Sub-arc B scope is carrier + helper + converter branch landing; production callsite migration is a separate follow-on arc). The CPAuditLedgerEntry path remains spec-supported per the v1.8 §C-OD-26.3 prose (preserved verbatim through v1.10); both paths produce valid audit entries; migration is operational refinement rather than spec-correctness gate. **AC #8 success condition**: this deferral discipline documented at U-OD-41 plan body + change-note adjacent finding (vi); existing production code unchanged at this arc. (PARTIAL-LANDED at v2.17.)
  9. **Importable; pyright strict mode passes.** `from harness_od.cost_namespace import CostRecordAuditPayload` + `from harness_od.cost_record_audit_writer import _project_cost_record_to_audit_payload` + the `cp_audit_to_od_audit` extension (COST_AUDIT_NAMESPACE_PREFIX + CpAuditCarrier union widening + isinstance branch) all resolve without error; existing converter tests pass (24/24 in test_cp_audit_conversion.py + test_u_cp_72_converter_6_prefix_extension.py); NEW cost: branch tests added at test_u_cp_72_converter_6_prefix_extension.py (4 tests covering projection + signature + prior_hash + 6-branch distinct prefixes); integration test suite (broader workspace) remains green at U-OD-41 landing arc.

---

## §2 — Preservation guarantees

| Section | Preservation |
|---|---|
| All v2.16 units other than U-OD-41 (54 units) | Preserved verbatim |
| v2.16 U-OD-51 cross-axis-block lift + PauseResumeAuditPayload absorption | Preserved verbatim |
| v2.15 U-OD-51 enum-citation absorption + all v2.14 + v2.13 + ... + v2 content | Preserved verbatim |
| DAG topology + coverage matrix + cross-axis edge enumeration | Preserved verbatim (no delta at v2.17 — single-unit-body amendment) |
| U-OD-41 plan body | **AMENDED at v2.17** per §1 above (Implements + Files + Signatures + ACs all revised; preserved AC #2 + #4 + #5 verbatim; new ACs #6-#9; new files cost_namespace.py + cp_audit_conversion.py extension cited; new helper signature per v1.10 §C-OD-26.1) |
| U-OD-41 Depends-on cite | Preserved verbatim (U-OD-38, U-OD-39, U-OD-40, U-CP-72 cross-axis: CP — same predecessor set; U-CP-72 minor revision at this session is a same-session co-publication, not a new DAG-predecessor) |

---

## Filing footer

| Field | Value |
|---|---|
| Artifact | `Implementation_Plan_Operational_Discipline_v2_17.md` |
| Version | v2.17 |
| Filing event | Sub-arc B sequel U-OD-41 absorption per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD spec v1.10 NEW §C-OD-26.6 + amended §C-OD-26.1 + CXA v2.9 §2.3.7 row 8. 2026-05-24 |
| Predecessor | `Implementation_Plan_Operational_Discipline_v2_16.md` (v2.16 substantive content preserved verbatim outside the single U-OD-41 plan-body amendment) |
| New units | 0 (single-unit-body amendment at U-OD-41) |
| Revised units | 1 — U-OD-41 (Implements + Files + Signatures + ACs absorbing v1.10 spec contract; 5 → 8 ACs at U-OD-41 only) |
| Cluster | No cluster change — U-OD-41 preserved at its existing cluster placement |
| Co-published with | `Spec_Operational_Discipline_v1_10.md` (NEW §C-OD-26.6 + §C-OD-26.1 amendment); `Cross_Axis_Composition_Document_v2_9.md` (already at HEAD `39e4f1c` 2026-05-24); impl arc this same session (cost_namespace.py + cost_record_audit_writer.py + cp_audit_conversion.py extension + production wiring) |
| Cross-axis dependencies | Unchanged at v2.17. U-OD-41's [U-CP-72 (cross-axis: CP)] dependency preserved verbatim; U-CP-72 minor revision at this session is a same-session co-publication (cost: branch un-STRUKE), not a new DAG-predecessor. CXA v2.8 → v2.9 already published at HEAD `39e4f1c` (CP→OD bucket 7→8 — the cross-axis composition surface). |
| DAG verification | Preserved verbatim from v2.16 — no topology change; single-unit-body amendment leaves all edges intact. |
| Coverage verification | U-OD-41 spec-cite bumps from `OD spec v1.8 §C-OD-26.3` (1 cite at v2.16) → `OD spec v1.10 §C-OD-26.1 + §C-OD-26.3 + §C-OD-26.6` (3 cites at v2.17) + CXA v2.9 §2.3.7 row 8 cite + impl-side citations to cost_namespace.py + cp_audit_conversion.py extension. All cites verified against `design-substrate/` + `harness-od/src/` + `harness-cxa/src/` at HEAD; no `Phase_7_Class_N_Tension` filing required. |
| Retirement-batch absorption owed | Future retirement-event batch may file H_T-OD-5 (cost-attribution chain) status advance per CXA v2.9 §0.8(d) + OD spec v1.10 §(d) post-impl-arc-close (criterion-B operational-MET may shift when the chain writes audit entries via canonical converter path). Not blocked on this v2.17 publication alone; gates on impl arc close. |
| Date | 2026-05-24 |
