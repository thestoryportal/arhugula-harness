# Implementation Plan — Control Plane v2.19

## Change-note (v2.18 → v2.19)

**Scope of revision.** Cite-cascade absorption pass per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24 at HEAD `01d11c8`). The v1.10 NEW §25 / C-CP-25 ValidatorFramework was renamed to §28 / C-CP-28 at CP spec v1.13 per first-author primacy + v1.10 preservation-claim self-contradiction + F-derivative anchor proximity tiebreaker (operator-ratified Reading A). This v2.19 records the absorption of the rename across the CP plan's ValidatorFramework-context cite surface and updates v2.18 §0(i) adjacent-defect note from "non-blocking; routing to a future arc" to **RESOLVED at v1.13 apply pass**.

**v2.18 substantive content preserved verbatim.** All v2.18 content (the U-CP-56 9th-field amendment + 73-unit axis-led structure + DAG topology + coverage matrix + cluster table) preserved unchanged outside the §0(i) adjacent-defect note refresh enumerated below + the canonical-reading amendment for cluster 10-CP-A enumerated at §1. The v2.17 + v2.16 + v2.15 + ... + v2 chain all preserved.

**Source of fix.** `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply-pass footer (RATIFIED 2026-05-24). Cite-cascade owed per fork §8 routing target table: "Implementation_Plan_Control_Plane_v2_18.md → v2.19 via implementation-planner revision-pass — cluster 10-CP-A units (U-CP-58/59/60/61) Implements/Files/Signatures cite retag C-CP-25 → C-CP-28; U-CP-56 (WorkflowDriver-lineage) preserved verbatim."

**Discrimination rule.** v1.13 §1 enumerates the rename mapping: v1.10 NEW §25 / C-CP-25 ValidatorFramework + sub-sections §25.1–§25.9 → §28 / C-CP-28 + §28.1–§28.9 byte-exact. v1.6-lineage §25 / C-CP-25 WorkflowDriver (including §25.2.1 `StepExecutionContext` + §25.3.3.4 step-dispatch contract) retains canonical-ID primacy and is preserved verbatim. U-CP-56 (StepExecutionContext authoring per CP spec v1.6 / v1.12 §25.2.1 → WorkflowDriver lineage) is PRESERVED verbatim — the U-CP-56 9th-field amendment landed at v2.18 cites `§25.2.1` which is the v1.6-lineage WorkflowDriver authoring site, not the renamed ValidatorFramework section. U-CP-58/59/60/61 (cluster 10-CP-A — `ValidatorFailClass` enum + `Validator` + `ValidatorFramework` Protocol envelopes + `ConcreteValidatorFramework` body + driver-hook landing) are ValidatorFramework lineage and absorb the rename at the canonical-reading layer per §1 below.

**Amendments.**

| Site | Amendment shape | Substrate source |
|---|---|---|
| **§0(i) adjacent-defect note (v2.18 line 35)** | "Surfaced; routing to a future CP spec hygiene revision arc; non-blocking at v1.12 + v2.18 publication" → **"RESOLVED at CP spec v1.13 per Reading A apply pass operator-ratified 2026-05-24 (HEAD `01d11c8`). The v1.10 NEW §25 / C-CP-25 ValidatorFramework is renamed to §28 / C-CP-28 at CP spec v1.13; the v1.6-lineage §25 / C-CP-25 WorkflowDriver retains canonical-ID primacy. The v1.12 amendment site at v2.18 cites `§25.2.1` which is the v1.6-lineage WorkflowDriver authoring site (StepExecutionContext at §25.2.1) — that cite is unchanged at v1.13 and preserved verbatim. U-CP-58/59/60/61 cluster 10-CP-A cite-cascade absorption at canonical-reading layer per v2.19 §1."** | Fork doc §8 Reading A apply pass |
| **§1 (NEW) — U-CP-58/59/60/61 cluster 10-CP-A canonical-reading amendment (v2.19)** | NEW section authored at v2.19 recording the cite-cascade absorption for the cluster 10-CP-A units. Per delta-only spec-file preservation convention, the v2.15 + v2.16 plan files containing U-CP-58/59/60/61 unit bodies are NOT edited at v2.19 — the canonical reading at v2.19 incorporates the §25 → §28 rename per CP spec v1.13 §1 sub-section mapping. Consumers reading the plan chain interpret all U-CP-58/59/60/61 Implements / Files / Signatures cite-shapes referencing "CP spec §25" / "C-CP-25 §25.x" in ValidatorFramework context AS canonically retagged to "CP spec §28" / "C-CP-28 §28.x" at v2.19. | Fork doc §8 Reading A apply pass + delta-only plan-file preservation convention (parallel to CP spec v1.13 §"v1.10 spec file itself NOT edited") |

**Plan shape preserved.** v2.18's 73-unit axis-led structure preserved verbatim. No new units; no DAG topology change; no coverage matrix change. ZERO contract change; ZERO signature change; ZERO acceptance-criterion change; ZERO field-set change; ZERO behavior change — fidelity-pure citation-correction patch under FM-2 no-extension discipline.

**Status posture.** Proposed (v2.18) → **Proposed (v2.19)**. v2.19 is a citation-correction patch absorbing the upstream CP spec v1.13 Reading A rename; no new unit, no new contract, no new AC, no DAG change.

**Downstream absorption owed (post-v2.19).**
(a) Workspace `CLAUDE.md` §2.4 CP plan row version bump (v2.18 → v2.19); co-published this cite-cascade arc.
(b) `Implementation_Plan_Harness_Runtime_v2_17.md` → v2.18 via `implementation-planner` cite-cascade revision-pass (L9-decies cluster ~13 ValidatorFramework-context cite sites + cluster-boundary edges to U-CP-58/59/60/61 retag). Separate skill invocation arc per fork §8 routing.
(c) `harness-cp/CLAUDE.md` — empirical grep at this arc confirms ZERO §25/C-CP-25 spec-section cite-shape hits (only `ValidatorFramework` code-symbol references at retirement-status rows, which are not spec section cites and do not need retag).
(d) `.harness/class_1_fork_validator_composer_arc_stage_4_absence.md` cite retag at lines 127 + 152. In-place edit at next-touch arc.

**Adjacent defects surfaced (not patched per FM-2 no-extension discipline).**

(i) **§28 non-sequential numbering at CP spec v1.13.** v1.13 §"Adjacent defects surfaced" (i) records the §28 placement as non-sequentially adjacent to §27 — readable but a numbering oddity preserved at fidelity-pure rename. Surfaced for awareness; NOT addressed at CP plan v2.19.

(ii) **Delta-only canonical-reading amendment pattern.** v2.19 mirrors CP spec v1.13's approach of recording the rename as an additive amendment at the new version while preserving the predecessor file byte-exact. This pattern is valid under delta-only convention but creates an interpretive layer: consumers reading the U-CP-58/59/60/61 bodies at v2.15/v2.16 see literal `§25.x` / `C-CP-25 §25.x` cites that must be canonically retagged per v2.19 §1. Surfaced; an alternative future revision MAY in-place edit the U-CP-58/59/60/61 bodies if operator policy shifts to non-delta-only.

---

## §1 — U-CP-58/59/60/61 cluster 10-CP-A canonical-reading amendment (v2.19)

The cluster 10-CP-A units (U-CP-58 `ValidatorFailClass` 5-class enum + U-CP-59 `Validator` + `ValidatorFramework` Protocol envelopes + U-CP-60 `ConcreteValidatorFramework` body + U-CP-61 driver post-dispatch hook) were authored at the canonical CP plan v2.15 + v2.16 spec-revision-driven decomposition of CP spec v1.10 NEW §25 / C-CP-25 ValidatorFramework. Per CP spec v1.13 §1 Reading A apply pass (operator-ratified 2026-05-24), the v1.10 NEW §25 / C-CP-25 ValidatorFramework is renamed to **§28 / C-CP-28** to resolve the section-number + contract-ID collision with the v1.6 NEW §25 / C-CP-25 WorkflowDriver.

**Cluster 10-CP-A canonical-reading amendment table.**

| Cluster 10-CP-A unit | Pre-v2.19 cite (in U-CP-58/59/60/61 bodies at v2.15/v2.16) | Post-v2.19 canonical reading |
|---|---|---|
| U-CP-58 (`ValidatorFailClass` 5-class enum at C-CP-25 §25.2) | `C-CP-25 §25.2` / `§25.2 ValidatorFailClass` | `C-CP-28 §28.2` / `§28.2 ValidatorFailClass` |
| U-CP-59 (`Validator` + `ValidatorFramework` Protocol envelopes at C-CP-25 §25.1) | `C-CP-25 §25.1` / `§25.1 Validator` / `§25.1 ValidatorFramework` | `C-CP-28 §28.1` / `§28.1 Validator` / `§28.1 ValidatorFramework` |
| U-CP-60 (`ConcreteValidatorFramework` body + `evaluate()` async method at C-CP-25 §25.3) | `C-CP-25 §25.3` / `§25.3 ConcreteValidatorFramework` / `§25.3 evaluate()` | `C-CP-28 §28.3` / `§28.3 ConcreteValidatorFramework` / `§28.3 evaluate()` |
| U-CP-61 (driver post-dispatch hook at C-CP-25 §25.3.3.4 — the validator-hook, NOT the v1.6-lineage step-dispatch §25.3.3.4) | `C-CP-25 §25.3.3.4` (validator-hook context) | `C-CP-28 §28.3.3.4` |

**Discrimination note.** U-CP-61's `workflow_driver.py:668` hook cite at `C-CP-25 §25.3.3.4` is the validator-hook (5-class outcome routing) authored at CP spec v1.10 NEW §25 ValidatorFramework body — distinct from the v1.6-lineage §25.3.3.4 step-dispatch contract (step-body opaque to driver / `try/except` boundary mapping). The validator-hook is renamed to §28.3.3.4 per Reading A; the v1.6-lineage step-dispatch §25.3.3.4 is preserved verbatim.

**Delta-only plan-file preservation.** The v2.15 + v2.16 spec-revision-driven plan files containing U-CP-58/59/60/61 unit bodies are NOT edited at v2.19 — preserved byte-exact per the delta-only convention. Consumers reading the plan chain interpret all U-CP-58/59/60/61 cite-shapes per the table above. This mirrors CP spec v1.13's approach (v1.10 spec file itself NOT edited; consumers interpret the rename at v1.13).

**U-CP-56 preserved verbatim.** U-CP-56 (StepExecutionContext schema + driver core) at v2.18 cites `CP spec v1.12 §25.2.1` which is the v1.6-lineage WorkflowDriver `StepExecutionContext` authoring site — that cite is unchanged at v1.13 (the v1.6-lineage §25 / C-CP-25 retains canonical-ID primacy) and is preserved verbatim at v2.19. No retag at U-CP-56.

**ZERO DAG impact.** U-CP-58/59/60/61 cluster-boundary edges (within-axis to U-CP-13/14/15/etc + cross-axis edges declared per CXA v2.x) preserved verbatim. The cite-cascade absorption is a citation-correction patch only; no DAG topology change.

**ZERO cross-axis cascade.** The renamed §28 / C-CP-28 ValidatorFramework Protocol surface is the same Protocol body (Mapping[str, Validator] + `evaluate(...)` async method + 5-class `ValidatorFailClass` enum + `ValidatorEvaluation` envelope + 4-event span emission set) preserved verbatim at the renamed location. Cross-axis consumers (runtime spec v1.19 §14.13 C-RT-23 stage-4 factory + OD spec v1.9 §C-OD-29.1 `validator.*` span schema) consume the same Protocol body at the renamed `§28` / `C-CP-28` cite. CXA v2.9 unchanged per Reading A apply pass.

---

## §2 — Preservation guarantees

| Element | Disposition |
|---|---|
| All v2.18 units (U-CP-00 through U-CP-72) | Preserved verbatim outside §0(i) adjacent-defect note refresh |
| v2.18 U-CP-56 plan-body amendment (9th-field `workflow_id` addition) | Preserved verbatim (cites `§25.2.1` per v1.6-lineage WorkflowDriver — NOT renamed) |
| v2.17 Path γ absorptions at U-CP-62/63/64 (`WorkflowPauseReason` rename + AC #4 strikes + Files-column note amendments) | Preserved verbatim |
| v2.16 path-β `ValidatorFailClass` → `ValidatorRetryExitClass` rename at U-CP-47 + U-CP-48 | Preserved verbatim |
| v2.15 + v2.14 + v2.13 + ... + v2 chain | Preserved verbatim |
| U-CP-58/59/60/61 cluster 10-CP-A unit body cite-shapes (literal `§25.x` / `C-CP-25 §25.x` in v2.15/v2.16 files) | Preserved byte-exact at predecessor files; canonical reading at v2.19 retags per §1 table |
| DAG topology | Preserved verbatim (zero new edges; zero edge removals) |
| Coverage matrix | Preserved verbatim |
| Cluster table (9 clusters; 55 in-cluster units + 3 pre-cluster L0) | Preserved verbatim |

---

## §3 — Filing footer

| Field | Value |
|---|---|
| Artifact | `Implementation_Plan_Control_Plane_v2_19.md` |
| Authored at | Cite-cascade absorption arc 2026-05-24 (this session) |
| Authoring authority | `implementation-planner` skill cite-cascade revision-pass per fork doc §8 routing target table |
| Predecessor authoring | `Implementation_Plan_Control_Plane_v2_18.md` (v2.18 substantive content preserved verbatim outside §0(i) adjacent-defect note refresh) |
| Successor consumption | Runtime plan v2.17 → v2.18 cite-cascade absorption arc (next-arc routing per fork §8) |
| Source fork | `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply-pass footer |
| Co-published this session | CP spec v1.13 (commit `01d11c8`); runtime spec v1.18 → v1.19 (this cite-cascade arc) |

---

*End of CP plan v2.19. Next cite-cascade artifact: runtime plan v2.17 → v2.18 (L9-decies cluster body + cluster-boundary edge cites). Adjacent fork doc cite retag + workspace `CLAUDE.md` row bumps + fork doc §8 closure follow.*
