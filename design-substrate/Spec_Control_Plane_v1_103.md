# Spec: Control Plane — v1.103 (delta over v1.102)

*Delta-only file. The v1.102 body + the entire C-CP-01 … C-CP-29 contract body are PRESERVED VERBATIM (delta-only-spec-file convention). This delta is the CP-owned rider of the ratified B-65 post-effect signing-carrier cascade-disposition arc (fork §3 + §3b, Option A): it carries exactly ONE section — the AMENDED §25.15 (C-CP-25) branch-terminality rider making a branch that fails with the Runtime-owned post-effect audit-signing carrier BRANCH-TERMINAL-WITH-RESULT under EVERY `cascade_policy`. The protected result store the carrier's `result_ref` resolves through — its keying, encryption envelope, tenant-bound lookup, write-once/retention/GC contract — is Runtime-owned, DEFINED at the same-arc Runtime v1.102 → v1.103 rider (NEW §14.8.11), and is CROSS-REFERENCED here, never restated. The v1.101 §2 post-effect catch-ordering contract (the per-attempt classifier fences) is COMPOSED WITH, never restated — this rider specifies the branch-vs-workflow terminality layer v1.101's own change-note left unspecified.*

**Filed:** 2026-07-22
**Authoring authority:** Class 2 fork `.harness/class_2_fork_b65_post_effect_signing_carrier_cascade_disposition.md` (**RATIFIED 2026-07-21 — the operator selected OPTION A AS RECOMMENDED**: §3 terminal-with-result rider + §3b protected result store — outage-independent envelope, tenant-bound lookup, full-strength composite keys, idempotent retrieval with ack-gated deletion + TTL GC fallback, fail-closed writes; the fork's §2(a-d) witnesses bind the apply arc), applied per workspace `CLAUDE.md` §4.3 back-flow + §4.5 clearance discipline.
**Predecessor:** `Spec_Control_Plane_v1_102.md` (v1.102 — the B-48 apply arc's CP rider; filed 2026-07-19)
**Revision shape:** Delta-only spec file per the CP delta-only convention. v1.102 + all earlier file bodies PRESERVED VERBATIM. v1.103 carries this change-note + exactly ONE section: the AMENDED §25.15 (C-CP-25) post-effect signing-carrier branch-terminality rider. The §25.15.1 `cascade_policy` table and the eight §25.15.2 cascade-cancel obligations are PRESERVED VERBATIM — this rider adds a carrier-specific branch disposition among their inputs; it amends no existing row and introduces no new `RunStatus` value, control-transfer mode, or cascade row.

---

## Change-note (v1.102 → v1.103)

**The CP rider of the ratified B-65 apply arc** (fork §4 option A — one bundle with the same-arc Runtime v1.102 → v1.103 rider and the CP + Runtime plan deltas). Division of authority: where this file names the protected result store, its composite key, its encryption envelope, or its retention/GC contract, **Runtime v1.103 §14.8.11 is the definition site** and this file only cross-references and binds CP semantics to it; where Runtime v1.103 names the branch-terminality semantics under each `cascade_policy`, THIS file is the definition site.

1. **§1 (fork §3 — the terminal-with-result rider).** A branch failing with the post-effect audit-signing carrier (the Runtime-owned `PostEffectAuditSigningError`, identified at the CP driver via the name-match discipline — `harness-cp` cannot import runtime types) is **BRANCH-TERMINAL-WITH-RESULT under EVERY `cascade_policy`**: `pause` MUST NOT mint a resume path that re-dispatches it (the branch enters the terminal disposition set, mirroring the v1.73/v1.74 scoped-abort precedent); `proceed`/`cascade-cancel` folds CARRY the carrier's `result_ref` into the PARTIAL/FAILED report; the run-level status still follows the policy for the REMAINING branches. Rationale (fork §3): the carrier is definitionally "effect landed, audit incomplete" — at-most-once (the C-RT effect discipline) dominates resumability; the audit gap is an operator-repair item (the migrate/inspect surfaces), never a re-dispatch trigger.
2. **Composition with the v1.101 §2 catch-ordering contract (cross-referenced, not amended).** v1.101 §2 governs the PER-ATTEMPT layer — the carrier's typed family caught AHEAD of every generic per-attempt classifier catch (*"A signing failure raised AFTER a completed effect MUST NEVER: be classified `TRANSIENT_RETRY` (or any staircase class), advance candidates, or record breaker failure"*) — but, as the fork §1 registers, it does NOT specify branch-vs-workflow terminality under each `cascade_policy`. THIS rider closes exactly that layer: the CASCADE-handling disposition of a branch the v1.101 fences have already re-raised. The v1.101 §2 text is PRESERVED VERBATIM.
3. **Result recovery (Runtime-owned, cross-referenced).** The `result_ref` the folds carry RESOLVES to the preserved payload through the DEDICATED protected result store — keyed full-strength tenant-composite, write-once, encrypted via a signing-KMS-independent envelope, tenant-bound lookup with typed cross-tenant refusal, idempotent retrieval with ack-gated deletion + TTL GC fallback, fail-closed write disposition — DEFINED at the same-arc Runtime v1.103 §14.8.11, cross-referenced at §1 row 6, never restated here.

**Grounding (verified at apply time, 2026-07-22).** Spec: §25.15's last substantive definition at `Spec_Control_Plane_v1_32.md` — the §25.15.1 table (header column *"On a branch failure"*; `pause` row *"Halt the fan-out at a HITL/pause boundary (composes with C-CP-26 PauseResumeProtocol + C-RT-35 `api.resume`)"*; run-level PARTIAL/PAUSED/FAILED) and §25.15.2 obligation 7 (*"reads each branch's persisted `terminal_status` and MUST NOT re-dispatch a branch that is `cancelled` / `completed` / `timed_out`"*); the scoped-abort precedent at `Spec_Control_Plane_v1_74.md` (preamble: *"at-most-once is PRESERVED (the scoped-abort branch is never re-dispatched on either the in-resume or the crash-resume path)"* — the replay-store-side distinct-disposition mechanism, no IS-hash-bearing ledger change); the v1.101 §2 catch-ordering block at `Spec_Control_Plane_v1_101.md` (the MUST-NEVER fragment quoted at item 2 above); OD policy at `Spec_Operational_Discipline_v1_34.md` §21.2.3 row 7 (*"MUST surface directly and result-preservingly"*). Code: the carrier at `harness-runtime/src/harness_runtime/lifecycle/audit_signing_errors.py:65` (`class PostEffectAuditSigningError(AuditSigningFailedError)`) with `self.result_ref = f"post-effect-{uuid.uuid4().hex[:12]}"` at `:91` (the 48-bit truncated ref the same-arc Runtime rider WIDENS), `effect_class` at `:95`, preserved `result` at `:98`. Name-match precedent: `Spec_Harness_Runtime_v1.md` §14.22.8 (*"The workflow driver name-matches `type(exc).__name__ == \"EffectFenceAmbiguousUncommittedError\"` at the linear/TOOL_STEP step-dispatch boundary (harness-cp cannot import harness-runtime — the `HITLPauseRequestedSignal` precedent)"*); the fork §2 prescribes the same idiom for this carrier (`type(exc).__name__ == "PostEffectAuditSigningError"` — the `StepDispatchTimeoutError` precedent). Plan home: CP plan v2.39 §0.2 (*"§25.15 `cascade_policy` consumption is U-CP-85"*). (Delta-baseline §-cite convention: each §-cite names the version of last substantive definition, per workspace `CLAUDE.md` §2.)

**Downstream absorption owed (same arc; flagged per spec-writer discipline, NOT edited here).** The CP plan delta (`Implementation_Plan_Control_Plane_v2_40.md` — the U-CP-85 fence + fold acceptance criteria; the v2.39 head carries none for this carrier) + the Runtime plan delta (`Implementation_Plan_Harness_Runtime_v2_51.md` — the store + carrier-widening + write-once-wiring unit) enumerated at Runtime v1.103's change-note. OD spec UNCHANGED (§21.2.3 row 7's policy text already covers this carrier; this rider composes with it). IS / AS specs UNCHANGED. CXA UNCHANGED (the fence is the import-free name-match idiom and the store is an intra-Runtime mechanism per Runtime v1.103's disposition; no new cross-axis seam).

**Verification obligations for the impl arc — the fork §2 witnesses (each mutation-probed per Workflow v1.18 PD-8).** A fan-out run under `audit_signing_fail_closed=ON` whose branch raises the carrier asserts: **(a)** NO resumable PAUSED snapshot is minted for the carrier branch (under `cascade_policy=pause` the branch is terminal; the REMAINING branches pause per policy); **(b)** the surfaced failure carries the carrier's `result_ref`; **(c)** NO re-dispatch path can re-fire the effect (in-resume and crash-resume alike, per the scoped-abort precedent); **(d)** the surfaced failure's `result_ref` RESOLVES to the preserved payload through the protected store under the OWNING tenant, and a cross-tenant read is refused typed (the store half of (d) is Runtime-owned — witness home at the Runtime plan delta; the CP half asserts the folds carry the ref the store resolves).

---

## §1 — AMENDED §25.15 (C-CP-25) — post-effect audit-signing carrier: BRANCH-TERMINAL-WITH-RESULT under every `cascade_policy` (fork §3)

*Amends §25.15 (`Spec_Control_Plane_v1_32.md`) by ADDING a carrier-specific branch disposition among its inputs. The §25.15.1 table, the eight §25.15.2 obligations, and the v1.102 §1 row-5 admission-rejection composition are PRESERVED VERBATIM.*

| # | Property | Contract |
|---|---|---|
| 1 | **The rider (branch-terminal-with-result)** | A branch failing with the post-effect audit-signing carrier — the Runtime-owned `PostEffectAuditSigningError` (`audit_signing_errors.py:65`; raised only under `audit_signing_fail_closed=ON` at the Runtime post-effect fences: the child's PAID effect COMPLETED, only the post-effect audit signing failed, per OD v1.34 §21.2.3 row 7's result-preserving bypass) — is **TERMINAL-with-result for THAT branch under EVERY `cascade_policy`**. The disposition is carrier-specific: every other branch-failure class keeps its existing §25.15.1 handling byte-verbatim. |
| 2 | **Identification (name-match fence, import-free)** | The CP driver identifies the carrier via the established name-match discipline — `type(exc).__name__ == "PostEffectAuditSigningError"` — because `harness-cp` cannot import the runtime type (the `StepDispatchTimeoutError` idiom per the fork §2; the same import-free mechanism as the §14.22.8 `EffectFenceAmbiguousUncommittedError` driver routing). The fence sits AHEAD of the branch-failure → PAUSED/PARTIAL conversions in `workflow_driver.py`, so the condition stays TERMINAL and RESULT-REFERENCED before any generic conversion runs. |
| 3 | **`pause` MUST NOT mint a resume path** | Under `cascade_policy=pause`, the carrier branch MUST NOT be given a resumable PAUSED path that re-dispatches it: the branch enters the terminal disposition set — recorded under a DISTINCT terminal disposition on the resume-visible record (value naming implementation-discretion, mirroring the v1.74 scoped-abort replay-store-side precedent: no IS-hash-bearing ledger change; §25.15.2 obligation 7's resume-terminality then applies — a recovered terminal is never re-dispatched, in-resume and crash-resume alike). The REMAINING branches follow `pause` exactly as at v1.102 (finish in-flight, then pause; run-level `PAUSED` per the policy for the remaining branches). |
| 4 | **`proceed` / `cascade-cancel` folds CARRY `result_ref`** | Under `proceed` and `cascade-cancel`, the fold that produces the PARTIAL/FAILED report CARRIES the carrier's `result_ref` into that report — the completed effect's result reference is never dropped from the fold (the fork §1's PROCEED-tier defect: reduction to PARTIAL WITHOUT the `result_ref` discards the preserved effect). The sibling-branch and run-level semantics of both policies are otherwise PRESERVED VERBATIM (`proceed` → partial result set / `PARTIAL`; `cascade-cancel` → cancel not-yet-dispatched siblings / `FAILED`). |
| 5 | **Run-level status follows the policy for the REMAINING branches** | The rider changes the CARRIER BRANCH's disposition only. Run-level `RunStatus` still follows the §25.15.1 policy mapping computed over the remaining branches — NO new `RunStatus` value, NO new control-transfer mode, NO new cascade row (the same closed-composition discipline as the v1.102 §1 row-5 admission-rejection input). |
| 6 | **Result recovery (Runtime-owned store, cross-referenced)** | The carried `result_ref` RESOLVES to the preserved payload through the DEDICATED protected result store DEFINED at the same-arc Runtime v1.103 §14.8.11 (full-strength tenant-composite key over the WIDENED ref; write-once at the carrier's raise site; signing-KMS-independent encryption envelope; tenant-bound lookup with typed cross-tenant refusal; idempotent retrieval, ack-gated deletion, TTL GC fallback; fail-closed write disposition) — cross-referenced, never restated here. CP carries the ref as an OPAQUE string; CP neither reads nor writes the store. |
| 7 | **Rationale (fork §3)** | The carrier is definitionally "effect landed, audit incomplete" — at-most-once (the C-RT effect discipline) DOMINATES resumability; re-dispatching the branch re-fires an already-completed PAID effect (the live at-most-once violation the fork §1 registers under `pause`). Audit repair is an OPERATOR flow (the migrate/inspect surfaces), never a re-dispatch trigger. |

---

## §2 — Status

One CP-owned section, ratified 2026-07-21 at the fork gate (the operator decision this arc surfaced is ALREADY TAKEN — no further gate on this delta): the §25.15 post-effect signing-carrier branch-terminality rider (terminal-with-result under every `cascade_policy`; name-match fence ahead of the PAUSED/PARTIAL conversions; folds carry `result_ref`; run-level status follows the policy for the remaining branches).

v1.102 + earlier PRESERVED VERBATIM per delta-only-spec-file convention — the entire C-CP-01 … C-CP-29 body except the §25.15 rider above. §25.15.1 table + §25.15.2 obligations PRESERVED VERBATIM. Runtime spec AMENDED at the SAME-ARC v1.102 → v1.103 rider (separate file — the definition site for the protected result store + the `result_ref` widening this rider cross-references). OD spec UNCHANGED. IS spec UNCHANGED. AS spec UNCHANGED. CXA UNCHANGED (import-free name-match fence + intra-Runtime store; no new cross-axis seam). ADR-F1..F5 / D1–D6 UNCHANGED. ADD v1.3 + PRD v1.1/v1.2 UNCHANGED. CP + Runtime plan deltas owed in the same arc per the change-note.

Clearance marker owed at `.harness/clearance/spec-control-plane-v1-103-cleared-2026-07-22.md` in the landing PR per workspace `CLAUDE.md` §4.5.

2026-07-22.
