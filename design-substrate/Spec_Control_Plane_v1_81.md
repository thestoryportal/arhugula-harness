# Spec: Control Plane — v1.81 (delta over v1.80)

*Delta-only file. The v1.80 body + the entire C-CP-01 … C-CP-29 contract body are PRESERVED VERBATIM (delta-only-spec-file convention). This delta records the CP half of the R-FS-1 standalone arc **`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD`** — the **maybe-ran SUB_AGENT_DISPATCH re-dispatch recoverability** predicate (C-CP-25 §25.15) is extended to admit a **`SAVE_POINT_CHECKPOINT`** `SINGLE_THREADED_LINEAR` leaf child, alongside the existing `{EVENT_SOURCED_REPLAY, WAL_SEGMENT}`. A maybe-ran fan-out / orchestrator `SUB_AGENT_DISPATCH` whose child is a LINEAR SAVE_POINT leaf now RECOVERS by re-dispatch (the child auto-resumes from its durable store under the deterministic child run_id, result-faithfully) instead of failing the run closed. Paired with **runtime spec change-note v1.86 → v1.87** (the typed mirror predicate `subagent_child_recoverable` engine-class set). It is a SECOND-mechanism scope extension under the existing §25.15 maybe-ran classifier; it changes NO contract removal, NO committed-invariant sacrifice (it CLOSES a fail-closed gap, additively); at-most-once is PRESERVED. **The arc is close-1-register-1 (net-zero)** — closing the SAVE_POINT slice and registering the **distinct** RECONCILER slice (`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD`, the F-1 CAS-claim at-most-once window) → `standalone_registered` is **4 → 4**.*

## Change-note (v1.80 → v1.81)

**Scope of revision.** One mechanism under **C-CP-25 §25.15** (the strict-tier fan-out maybe-ran crash-resume classifier `_fence_unrecoverable_maybe_ran_indices` + its `_subagent_child_recoverable` child-recoverability predicate) — the CP half of `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD`. v1.77 built the SUB_AGENT re-dispatch recovery disjunct for a `{ESR,WAL}` ∧ LINEAR ∧ leaf child; v1.78 extended it to the orchestrator analogue; this delta extends the child-engine-class conjunct to admit **`SAVE_POINT_CHECKPOINT`**. The §25.15 §1 conjunct-1 set reads **`{EVENT_SOURCED_REPLAY, WAL_SEGMENT, SAVE_POINT_CHECKPOINT}`** (the impl `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES`, mirrored CP + runtime). `RECONCILER_LOOP` stays OUT (the registered follow-on).

**The grounding that resolved the open at-most-once probe.** The arc was registered (v1.80 decompose-at-open) anticipating that the SAVE_POINT/RECONCILER child re-dispatch recoverability is a *distinct* mechanism from the final_state reconstruction the RECONCILER close (v1.80) landed — *"final_state now reconstructs for SAVE_POINT/RECONCILER children, but re-dispatch recoverability stays fail-closed pending grounding of the RECONCILER F-1 CAS-claim at-most-once window; a SAVE_POINT leaf has no CAS lease → likely the cleaner decomposed slice."* Empirical grounding at open (2026-06-26) **resolved the probe for SAVE_POINT** and confirmed the decomposition:

> **A re-dispatched maybe-ran SAVE_POINT leaf child auto-resumes, at-most-once-safe.** The CP driver computes `resume_at` for a `SAVE_POINT_CHECKPOINT` run via `_determine_resume_at` — the **engine-class-agnostic F2-prefix join** (the reference impl that ESR/WAL/RECONCILER all delegate to; `workflow_driver.py` resume dispatch). So a maybe-ran SAVE_POINT child re-dispatched under the deterministic child run_id computes `resume_at > 0` from its durable F2 prefix → the committed prefix `[0, resume_at)` is **auto-resumed, NOT re-dispatched** (at-most-once for committed effects) → and its `final_state` reconstructs (the CP v1.79 class-agnostic `EngineOutputStore` seed). Crucially, a SAVE_POINT resume fires **NEITHER** the ESR/WAL-only inter-step cached-output rehydrate **NOR** the RECONCILER engine-layer recovery loop (U-CP-97 `attempt_resume`) — so a SAVE_POINT leaf carries **no CAS lease and no F-1 ABORT window**: the cleanest possible auto-resume.

> **Why RECONCILER stays the follow-on (the decomposition cut holds).** A maybe-ran `RECONCILER_LOOP` child re-dispatch DOES fire the U-CP-97 engine-layer reconverge (`attempt_resume`), whose documented **F-1 limit** (`reconciler_pause_resume_substrate.py`) ABORTs a retry of an already-won CAS revision claim → C-CP-22 §22.1 HITL. So re-dispatching a maybe-ran RECONCILER child whose first attempt WON the claim could ABORT rather than auto-resume — a distinct at-most-once question this fail-closed gate must NOT widen over without its own grounding. Registered as `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD`.

**The §25.15 mechanism (extended).** The maybe-ran classifier's SUB_AGENT recovery disjunct (v1.77 §1) is UNCHANGED in shape — the [P1-b] dual gate (recoverable BOTH at dispatch via the `subagent_recoverable_indexes` marker AND in the resumed manifest), the same-kind requirement, the changed-step_id guard, the per-branch-unique deterministic child run_id — only the `_subagent_child_recoverable` engine-class conjunct widens to admit SAVE_POINT. Both the WORKER (fan-out) and ORCHESTRATOR dispatch use the SAME predicate (`_subagent_child_recoverable` at the worker `_resumed_subagent_recoverable_by_ordinal` + the orchestrator `steps[0]` re-check), so this single conjunct change covers both surfaces. The set is **DEDICATED** (`_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES`), distinct from `_FANOUT_REPLAY_ENGINE_CLASSES` (which the separate B-FANOUT-OUTPUT-REPLAY `_fanout_replay_store` branch-capture gate consumes and must NOT admit SAVE_POINT — carrier segregation, not a shared-constant widen).

**At-most-once invariants preserved.** The SAVE_POINT recovery rides the existing strict-tier classifier guards (range, changed-step_id, same-kind dual gate). A re-dispatched SAVE_POINT child auto-resumes from its durable prefix (committed effects not re-fired); a child edited recoverable→non-recoverable between dispatch + resume (e.g. SAVE_POINT→RECONCILER, or a nested sub-agent added) has durable dispatch records but fails the resumed-manifest conjunct → fail closed. No production path re-dispatches a non-recoverable maybe-ran SUB_AGENT worker.

**Spec-vs-fork.** Bundled-absorption amendment, NOT X-AL-3. §25.15 v1.77 §3 explicitly framed `{ESR,WAL}` as the *witnessed* slice and named "the SAVE_POINT/RECONCILER child case is already covered by the registered follow-on" — a fail-closed gap pending the blocker, NOT a committed closed set. v1.77 §3 set the precedent: *"Additive recovery for a previously-fail-closed surface — it CLOSES a capability gap, sacrifices no committed decision → no operator gate."* The blocker (suffix-only final_state for SAVE_POINT children) was retired at v1.79. NO §5.2 IS-hash change (the recoverability marker is non-attested, not in the §6 chain). NO new contract / ADR / enum / fail-class.

## §1 — the SUB_AGENT_DISPATCH child-recoverability engine-class set (C-CP-25 §25.15)

> **`_subagent_child_recoverable` conjunct-1 widens to `{ESR, WAL, SAVE_POINT}`.** A maybe-ran fan-out / orchestrator branch ordinal whose `SUB_AGENT_DISPATCH` child manifest is a `SINGLE_THREADED_LINEAR` leaf with engine class ∈ `{EVENT_SOURCED_REPLAY, WAL_SEGMENT, SAVE_POINT_CHECKPOINT}` is RE-DISPATCH-RECOVERABLE (the marker records `child_recoverable=True`; the resumed re-check admits it). `RECONCILER_LOOP` returns `False` (the registered `…-RECONCILER-CHILD` follow-on). The dedicated `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES` set is the CP-side mirror of the runtime typed `subagent_child_recoverable`; the by-execution agreement witness enforces parity (a CP-True / runtime-False drift would admit re-dispatch with no deterministic seed → double-fire).

## §2 — R-FS-1 closure semantics (FULL-SPEC)

> **CLOSE + REGISTER (net-zero).** This delta CLOSES `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD` — the SAVE_POINT slice (a maybe-ran SUB_AGENT worker OR orchestrator whose child is a `SINGLE_THREADED_LINEAR` SAVE_POINT_CHECKPOINT leaf now recovers). It REGISTERS the genuinely-distinct **`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD`** — the RECONCILER slice, whose maybe-ran child re-dispatch fires the U-CP-97 engine-layer reconverge carrying the F-1 won-CAS-claim-retry-ABORTs-to-HITL at-most-once window (needs its own grounding before widening the gate). The non-leaf-child + fan-out-child residuals stay the separately-registered `…-NONLEAF-CHILD` follow-ons. `closure_gate.py` G1.1 (`standalone_registered + standalone_gated == 0`) was **4 + 0**; after this close-1-register-1 it is **4 + 0** (net-zero) → R-FS-1 stays ACTIVE.

> **Why no operator gate.** Additive recovery for a previously-fail-closed surface — it CLOSES a capability gap, sacrifices no committed decision (`[[feedback-gate-only-on-meaningful-architecture-change]]`), at-most-once preserved. The user-facing change (a maybe-ran sub-agent fan-out worker / orchestrator with a LINEAR SAVE_POINT leaf child now recovers instead of failing the run closed) is reported in the PR/handoff for operator visibility, NOT gated — FULL-SPEC + pre-authorized back-flow. `operator_gated: false`. Co-published with one `.harness/clearance/` marker (CP v1.81) per workspace `CLAUDE.md` §11.4.

> **Design-vetted.** advisor full-transcript BEFORE substantive work (the GO + five sharpenings: the EFFECT-level crux witness that can't pass vacuously [confirm `resume_at>0`, mind the no-cached-output-replay difference]; carrier-segregation grep before touching `_FANOUT_REPLAY_ENGINE_CLASSES`; the RECONCILER→False contrasting baseline + agreement-test extension; the §25.15-body fail-closed-default confirmation; don't-over-excavate-RECONCILER). The crux probe resolved by code-read — SAVE_POINT computes `resume_at` via the engine-class-agnostic F2-prefix join (`_determine_resume_at`, its reference impl) firing no recovery loop / CAS-claim. Witnessed: the predicate verdict flip (runtime SAVE_POINT→True, RED-without-fix; + the new RECONCILER→False control), the extended CP↔runtime agreement witness (SAVE_POINT True / RECONCILER False, both predicates agree), the EFFECT-level recursive-child SAVE_POINT crash-resume reconstruct (full final_state + only step-2 re-dispatched → committed prefix not re-fired), and the CP full-chain orchestrator maybe-ran SAVE_POINT child RECOVERS (RED-without-fix) + the repurposed RECONCILER negative-control STAYS fail-closed. + out-of-family Codex on the diff. No council — single-axis CP+runtime, the A-vs-B readings resolved by grounding + the empirical witnesses, no nameable unresolved cross-domain tension.
