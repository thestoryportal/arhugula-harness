# Implementation Plan: Operational Discipline — v2.34 (delta over v2.33)

*v2.34 is the OD plan leg of the **`B-123`** repair — the §10.2 tail-keep classification-trigger predicate `is_classification_trigger` matches the two event-shaped triggers (`sandbox.violation` / `breaker.tripped`) by SPAN NAME while the cleared §C-OD-10 §10.2 contract table has always said EVENT (`design-substrate/Spec_Operational_Discipline_v1_2.md:583-584`, PRESERVED VERBATIM through the current head), and the real `breaker.tripped` producer (`harness-od/src/harness_od/harness_breaker_schema.py:282`) emits exactly that — an event on a carrier span (`harness.runtime.retry_breaker_fallback`) that is itself neither a §9.2 nor a §10.2 member. `Spec_Operational_Discipline_v1_39.md` §1 flips §C-OD-09 §9.2.1 **term 3** in place — replacing its explicit decline to widen the predicate with a requirement that it resolve both the span-name and span-event shapes of the same two trigger names — and states plainly that this is a **conformance repair to already-cleared contract text**, not a design extension (v1.39 §0.2). This delta authors **ONE NEW atomic unit, U-OD-60**, carrying that term's realization at the shipped predicate and its witness set. All sections except the §0 change note and the NEW U-OD-60 body + coverage delta below are PRESERVED VERBATIM from v2.33 (delta-only-plan-chain convention).*

**Status:** Proposed

---

## §0 Change-note (v2.33 → v2.34)

### §0.1 Predecessor

`Implementation_Plan_Operational_Discipline_v2_33.md` (v2.33 — the `B-133` realization leg; NEW U-OD-59).

### §0.2 Why this delta exists

U-OD-59 (v2.33) closed the §9.2 half of the span-name-vs-span-event mismatch — three §9.2 members ride as span events, and the tail consumer now resolves both shapes through the same `is_always_sampled` SSOT (OD spec v1.38 §9.2.1 terms 1–2). Term 3 of that same subsection deliberately left the *other* half untouched: *"The §10.2 trigger predicate itself is **NOT** widened to events at this delta … That half … is register row `B-123`'s scope and is left to it deliberately."* U-OD-59's own witness `test_w7_event_carried_breaker_tripped_does_not_set_the_keep_flag` (`harness-od/tests/test_b133_event_aware_tail_floor.py`) was written to PIN that boundary — an event-carried `breaker.tripped` forwards its own carrier (the §9.2 floor) but does **not** flag its trace for §10.2 sibling preservation — precisely so that widening it later would be a deliberate, test-visible act rather than silent drift.

`B-123`'s close-out has since run (forward-register, re-grounded 2026-08-08/09). Its positive control — the same real exhausted dispatch `B-133` used — observed `is_classification_trigger(span)` returning `False` on the carrying span, confirming the trigger was inert on the dispatch path. Its step (3) widened the probe to the `sandbox.violation` sibling arm and found it **already conformant**: `sandbox.violation` is emitted as a real span (`harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py:725`, `tracer.start_as_current_span("sandbox.violation")`), never as an event, at every live producer (an exhaustive `add_event(` sweep over all seven `harness-*/src` trees returns exactly six sites, none named `sandbox.violation` — verified independently this session). Its step (2) settled the repair as **disposition (a)**: widen the predicate to also scan span events, conforming the realization to what §10.2 already contracts — not "emit a span so named," which would mint a new emission primitive and repeat the venue-(b) route `B-133` already refused. **This unit realizes that settled repair; the fork was already closed at the register, so nothing routes to the operator here.**

### §0.3 Sections revised

§0 (this change note, including the NEW §0.5 falsified-premise record); §1 (the NEW U-OD-60 body); §2 (coverage delta). All other sections — every existing `U-OD-NN` body including U-OD-58 and U-OD-59, all dependency graphs, cross-cutting units, open items — PRESERVED VERBATIM from v2.33.

### §0.4 Scope discipline

ADDITIVE — ONE NEW atomic unit (U-OD-60), the next free OD unit ID after v2.33's U-OD-59 (verified: no `U-OD-60` occurrence anywhere in `design-substrate/` or `.harness/` before this filing that names an already-authored unit; the only prior occurrences are forward-pointers at `.harness/roadmap_status.md` and the sibling `Spec_Operational_Discipline_v1_39.md` §1.3, both of which cite this plan file and unit ID as forward-owed, not already-landed). **ZERO amended units** — neither U-OD-11 (which owns `tail_keep_classification.py` per the v2.26 canonical-reading amendment) nor U-OD-59 (which owns `tail_keep_span_processor.py` and the `_carries_always_sampled_event` pattern this unit's predicate mirrors) has its acceptance criteria rewritten, per the `B-97`(a) → U-RT-149 / `B-116-t3` → U-OD-58 / `B-133` → U-OD-59 precedent that a landed unit is not retroactively re-scoped by a later contract amendment. **ZERO new contract IDs** — C-OD-10 §10.2 is UNCHANGED (its table already specifies the event carrier; OD spec v1.39 §0.2 states this is the whole reason no contestable spec direction exists), and the amended term (§C-OD-09 §9.2.1 term 3) is a subsection of an existing contract, not a new number (a spec leg cannot mint a `C-*` number per the workspace's spec-leg discipline; OD spec v1.39 filing footer confirms zero minted). **ZERO roster change** (§9.2 stays at nineteen rows — this unit touches the DIFFERENT, disjoint §10.2 trigger-name set, not the §9.2 roster; OD spec v1.39 §1 second paragraph states the two rosters answer different questions and MUST NOT be collapsed). **ZERO new namespace** (C-OD-05 §5.1 roster unchanged at 15). **ZERO new cluster.** **ONE new DAG node + TWO new intra-axis edges**: U-OD-11 → U-OD-60 (the module U-OD-11 owns is amended) and U-OD-59 → U-OD-60 (the pattern + the witness this unit inverts both live in U-OD-59's surface). **ZERO cross-axis edges; ZERO CXA rows** — verified, not assumed: `is_classification_trigger` has exactly THREE production call sites, all three inside `harness-od/src/harness_od/tail_keep_span_processor.py` (already OD-internal, U-OD-59's surface) — the §9.2 name arm, the `B-133` event-aware arm's trigger-flag mirror, and the buffer path — and a repo-wide grep for `is_classification_trigger` outside `tail_keep_classification.py` / `tail_keep_span_processor.py` returns only test-side imports at `harness-runtime/tests/` (expected — runtime tests already depend on every axis; not a new production dependency). OD spec v1.39 §1.2 independently confirms CXA UNCHANGED, aggregate frozen at 111.

### §0.5 A brief-supplied premise that grounding FALSIFIED — recorded, not carried

`[HIGH]` This unit's authoring brief instructed that a Class-3 informational rider ride this delta at zero marginal cost: that §10.2 row 1's cite (*"classification == `permanent-fail` per ADR-D2 §1.8 fail-class taxonomy"*, `Spec_Operational_Discipline_v1_2.md:582`) does not resolve, because ADR-D2 §1.8 allegedly "contains NO `permanent-fail` value." **That claim is FALSE, verified independently by direct read of `design-substrate/ADR-D2.md` this session.** §1.8 is at line 301 (*"Sandbox-violation fail-class taxonomy"*), and its table at lines 303–311 has **three** columns — the second headed *"C5 fail-class (per `c5-validation-contract` SKILL.md)"* — whose values include **`permanent-fail`** at line 305 (`escape_attempt`), line 306 (`egress_denied` — *"permanent-fail (deterministic policy hit)"*) and line 309 (`signal` — *"permanent-fail (operator-induced)"*). The premise's own source was derived from the `sandbox.fail.class` value enum at `ADR-D2.md:285` alone and missed the C5-fail-class column of the very table it cites. **The §10.2 row-1 cite RESOLVES.**

The rider is therefore **NOT carried by this delta.** The sibling spec leg independently re-grounded and recorded the identical correction at `Spec_Operational_Discipline_v1_39.md` §0.6 (*"A premise the grounding pass FALSIFIED — recorded rather than inherited"*), which this section cross-references rather than restates. Both corrections were reached independently by direct read, not by one copying the other.

---

## §1 U-OD-60 — the §10.2 event-name classification-trigger predicate widening

**Implements:** C-OD-10 §10.2 (existing, OD spec v1.2 — the two event-shaped trigger rows this predicate becomes conformant to) + C-OD-09 §9.2.1 term 3 (AMENDED at OD spec v1.39 §1 — the explicit decline to widen is retracted and replaced with a requirement to resolve both shapes).

**Depends on:** [**U-OD-11**, **U-OD-59**]. U-OD-11 owns `harness-od/src/harness_od/tail_keep_classification.py` (established at the v2.26 canonical-reading amendment, §1.1 table row 1: `is_classification_trigger(span)` per-span §10.2 trigger predicate) — this unit amends that module's function body and constant set. U-OD-59 owns `tail_keep_span_processor.py` and introduced the `_carries_always_sampled_event` scan-events-once-tolerant-of-absence pattern this unit's event arm mirrors (against a DIFFERENT, disjoint name set — the two §10.2 trigger names, not the nineteen-member §9.2 roster); U-OD-59 also authored the witness this unit inverts in place (`test_w7_event_carried_breaker_tripped_does_not_set_the_keep_flag`) and the comment block at `tail_keep_span_processor.py` (the B-123-scope note immediately following the event-arm's trigger-flag mirror) this unit rewrites.

**Consumed by (cross-axis):** **NONE.** `is_classification_trigger` is OD-owned; all THREE of its production call sites are inside `tail_keep_span_processor.py`, already within OD. This unit widens the predicate's internal resolution — it adds only an OPTIONAL keyword-only `events` parameter (defaulting to `None`, so every existing call is byte-identical — see `Files affected`, taken at out-of-family review to keep `span.events` read ONCE per `on_end`), no new exported symbol beyond the derived constant, and no import outside `harness_od.*` — so no new cross-package consumption is introduced and no CXA row is owed. Confirmed by grep, not assumed: OD spec v1.39 §1.2 records the identical CXA-UNCHANGED disposition at the sibling spec leg.

**Files affected (logical):**

- `harness-od/src/harness_od/tail_keep_classification.py` — a derived constant (the two existing §10.2 name constants, `SANDBOX_VIOLATION_SPAN_NAME` + `BREAKER_TRIPPED_SPAN_NAME`, composed into a frozenset rather than re-literalled) + an event-scan arm appended to `is_classification_trigger`, reached only after the existing name and attribute checks have failed + module-docstring and function-docstring updates recording the mechanism and the empirical grounding. The pre-existing `if attrs is None: return False` early exit is necessarily restructured (to `if attrs is not None: …`, falling through) so the event arm still runs when `attrs` is absent — the same tolerant-fall-through shape U-OD-59 already established for the analogous `attrs is None` / zero-events cases.
- `harness-od/src/harness_od/tail_keep_span_processor.py` — a **minimal** logic change, scoped to the SINGLE-READ discipline and nothing else: `on_end` reads `span.events` **once** into a local and threads that snapshot into both `_carries_always_sampled_event` and `is_classification_trigger` (each gaining an optional keyword-only `events` parameter that defaults to `None`, meaning *read it yourself* — so every existing caller's behaviour is byte-identical). **`on_end`'s control flow, its arms and their ordering are UNCHANGED**; only the events SOURCE moves at two of the three `is_classification_trigger` call sites. The third (inside the §9.2 name arm, which returns before the local exists) is deliberately left reading for itself — still at most ONE read on that path. Plus the `B-123`-scope comment after the event arm's trigger-flag mirror, rewritten from *"NOT widened at this leg, confirmed inert"* to record the widened behaviour subject to the `B-137` head-admission bound. **This bullet was originally filed as COMMENT-ONLY; out-of-family Codex round 1 raised a [P2] showing the widened predicate re-read `span.events` a second time per ordinary buffered span — a regression this arc introduced, since the predicate previously never touched `.events` at all. The finding was adjudicated CORRECT and taken, and this bullet is corrected rather than left standing.** OD spec v1.39 §1.3 delegates *"the once-only read of the span's event collection"* to implementation discretion, so the repair needs no contract change; the normative terms it must preserve — the predicate stays TOTAL and side-effect-free, and the event scan is reached only after the name and attribute resolutions fail — are preserved and witnessed (AC6, AC7, AC11).
- `harness-od/tests/test_b133_event_aware_tail_floor.py` — **W7 inverted in place**: rename `test_w7_event_carried_breaker_tripped_does_not_set_the_keep_flag` → `test_w7_event_carried_breaker_tripped_now_sets_the_keep_flag`, docstring rewritten to record the boundary as CLOSED rather than pinned, assertion flipped from `"sibling.work" not in names` to `names == ["ordinary.root", "sibling.work"]` (order asserted, per the U-OD-59 §0.7 precedent that membership alone under-discriminates). The module's witness table (W7 row) and the W6/W7 section comment move with it. No other witness in that module changes.
- `harness-od/tests/test_b123_event_aware_classification_trigger.py` — **NEW**, the predicate-level witnesses for AC1/AC2/AC5/AC6/AC7/AC8 (six test functions). Deliberately a NEW file rather than an extension of `test_b133_event_aware_tail_floor.py` (which pins the DIFFERENT `_carries_always_sampled_event` function against the §9.2 roster) or `test_tail_keep_span_processor.py` (whose four existing `is_classification_trigger` name/attribute-arm witnesses are untouched by this widening). `harness_runtime`-FREE by rule (the `axis-isolation — harness-od` CI leg requirement U-OD-59 §0.10 established) — verified by import inspection: its import block names only `harness_od.sampling_mode`, `harness_od.tail_keep_classification`, `harness_od.tail_keep_span_processor` and `opentelemetry`.
- `harness-runtime/tests/test_b133_event_aware_tail_floor_real_dispatch.py` — **TWO NEW test functions**, `test_ac4_buffered_sibling_span_survives_a_real_breaker_trip` (carrier nested under an ambient root, so a true §10.2 *sibling* exists) and `test_ac4b_carrier_as_root_preserves_buffered_children_on_a_real_breaker_trip` (carrier IS the trace root, the term-5 production shape, with the dispatcher's own `harness.runtime.retry_attempt` child as the buffered span). Both reuse the module's existing `_tail_provider` / `_dispatch_exhausted` fixtures and neither calls `force_flush`. The second was added because PD-8 probe (v) proved the first does not reach the event arm's own root-close call — see AC #4. Cross-axis-homed deliberately, per the U-OD-59 §0.10 split (runtime is what composes the tail processor with a real dispatcher in production; runtime tests already depend on every axis).
- `harness-od/src/harness_od/sampling_mode.py` — **NOT EDITED.** `ALWAYS_SAMPLED_EVENT_CLASSES` and `is_always_sampled` are untouched; this unit's derived §10.2 set is a disjoint, OD-local constant, not a member of the §9.2 SSOT.
- `harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py` + `harness-od/src/harness_od/harness_breaker_schema.py` + `harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py` — **NOT EDITED.** The three emission/call sites this repair is grounded against (`retry_breaker.py:641` → `emit_breaker_trip_span_event` → `harness_breaker_schema.py:282`'s `parent_span_ref.add_event(name="breaker.tripped", …)`; `runtime_tool_dispatcher.py:725`'s `tracer.start_as_current_span("sandbox.violation")`) are byte-unchanged — verified this session by direct read. AC #8 asserts this by absence.

**Scale:** roughly four functional `src` lines in `tail_keep_classification.py` (the derived constant + the event-scan return), per the register row's own pricing, reusing the U-OD-59 `_carries_always_sampled_event` shape rather than inventing a new one; plus the single-read threading at `tail_keep_span_processor.py` (one new local, an optional parameter on each of the two helpers, and two call-site arguments) taken at out-of-family review.

**Thread / async posture.** `is_classification_trigger` is a pure function with no `Lock`, no `async def` and no shared mutable state — unchanged by this widening. The new arm reads `span.events` exactly once into a local before iterating (mirroring `_carries_always_sampled_event`'s own cost discipline at `tail_keep_span_processor.py`, which documents `span.events` as an OTel property that takes a lock and copies a deque on every access), so the added cost is paid at most once per call and only when the cheaper name/attribute checks have already failed.

### §1.1 Acceptance criteria — by EXECUTION

1. **A span carrying a `breaker.tripped` EVENT on a non-trigger-named carrier is a trigger.** This is the exact production shape: `harness_breaker_schema.py:282` emits the event on the dispatch wrapper span (`harness.runtime.retry_breaker_fallback`), never on a span literally named `breaker.tripped`. Before this unit, `is_classification_trigger` returned `False` on that carrier (the `B-123` grounded defect); the event arm closes it. Witness: `test_ac1_breaker_tripped_event_on_a_non_trigger_named_span_is_a_trigger` (`harness-od/tests/test_b123_event_aware_classification_trigger.py`).
2. **The `sandbox.violation` sibling arm is covered too, and the witness exists because the CONTRACT names both — not because a producer does.** §10.2's table (`Spec_Operational_Discipline_v1_2.md:583`) preserves both event-shaped rows symmetrically; a repair scoped to the one name with a live event producer today would re-create the exact defect class this unit closes the moment a producer's shape changed. Grounded, not speculative: the exhaustive `add_event(` sweep (§0.2) confirms `sandbox.violation` has zero event-shaped producers at HEAD, so this witness's coverage is deliberately ahead of production population. Witness: `test_ac2_sandbox_violation_event_on_a_non_trigger_named_span_is_a_trigger`.
3. **The U-OD-59 boundary pin INVERTS in place, and nothing is duplicated.** `test_w7_event_carried_breaker_tripped_does_not_set_the_keep_flag` — deliberately authored by U-OD-59 to pin the pre-repair boundary so widening it would be test-visible rather than silent — is renamed, its docstring rewritten to record the boundary as CLOSED, and its assertion flipped to require the buffered sibling now survive at root close (asserted by export ORDER, per the U-OD-59 §0.7 precedent that membership alone under-discriminates: the trigger-carrying root forwards immediately via the event arm, its sibling resolves second via the now-set keep flag). No stale duplicate is left in the module; the rename IS the record.
4. **REAL-DISPATCH end-to-end: a real charging-fault dispatch drives a real breaker trip, and a buffered SIBLING span in the SAME trace — not merely the trip's own carrier — is exported, WITHOUT `force_flush`.** Grep proves presence; only the run proves behaviour. The trace arrangement is stated rather than assumed: the dispatch carrier is opened as a CHILD of an ambient root span (so the carrier itself is not the trace's root close), a plain sibling span buffers under the same root before the dispatch runs, and the ambient root's own close is what materializes the trace decision — asserted via `carrier.parent is not None` so the witness cannot pass by accident via the carrier being its own root. Witness: `test_ac4_buffered_sibling_span_survives_a_real_breaker_trip` (`harness-runtime/tests/test_b133_event_aware_tail_floor_real_dispatch.py`), reusing the `_tail_provider` / `_dispatch_exhausted` fixtures the `B-133` witnesses already established.

   **AC #4 is discharged by TWO witnesses, and the second exists because PD-8 exposed a real gap rather than a wrong prediction.** The ambient-root arrangement above means AC4's trace materializes through the ORDINARY buffer-path root close — so it never reaches the event arm's OWN root-close call (`if span.parent is None: self._materialize_trace_decision(ctx.trace_id)`), and probe (v) left it GREEN. That call governs precisely the shape OD spec v1.38 §9.2.1 term 5 says actually ships (*"the dispatch carrier is routinely the root close"*), which would then have had no real-dispatch coverage under the widened flag. Second witness: `test_ac4b_carrier_as_root_preserves_buffered_children_on_a_real_breaker_trip` — the SAME real charging-fault dispatch with **no ambient wrapper**, so the carrier is genuinely the trace root (asserted via `carrier.parent is None`, the mirror of AC4's own assertion). **No span is injected to create the buffered child:** the dispatcher opens `harness.runtime.retry_attempt` as a real child of the carrier on every attempt (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:1226`) and closes it before the carrier closes, so the buffered child is production's own. Also without `force_flush`. The two witnesses are complementary, not redundant: §10.2's "Parent + sibling" language structurally requires a parent, which a root span lacks, so the sibling case genuinely needs the ambient arrangement while the term-5 case genuinely needs the root one.
5. **The §10.2 event-name set is NOT the §9.2 roster, and a span carrying only a §9.2-but-not-§10.2 event must NOT be flagged.** `fallback.exhausted` is a live discriminator: it is a §9.2 always-sampled member (its own carrier forwards via the pre-existing U-OD-59 arm) but is absent from the two-member §10.2 trigger-name set, so a carrier whose only event is `fallback.exhausted` must forward itself but leave a buffered sibling DROPPED. This directly kills the mutation that would collapse the two rosters (replacing the two-name §10.2 set with the nineteen-member `ALWAYS_SAMPLED_EVENT_CLASSES` import) — such a mutation makes `fallback.exhausted` a false-positive §10.2 trigger and this witness catches it. Witness: `test_ac5_section_9_2_member_event_is_not_a_section_10_2_trigger`.
6. **Tolerance is preserved: a span with zero events must not raise and must return the SAME verdict the predicate returned before this arm existed**, mirroring the pre-existing `attrs is None` tolerance. Witness: `test_ac6_zero_events_does_not_raise_and_preserves_pre_existing_verdict`.
7. **Cheapest-first ordering is preserved, asserted structurally rather than by timing.** A span NAMED `breaker.tripped` must return `True` via the existing name arm and must NEVER read `span.events` at all — asserted with a `ReadableSpan`-shaped double whose `.events` property raises if accessed, so a passing call proves the short-circuit rather than merely being consistent with it. Witness: `test_ac7_name_matching_span_never_touches_events_cheapest_first`.
8. **The §10.2 event-name set is DERIVED from the two existing name constants, not re-literalled**, so a future rename of either constant reaches the event arm by construction rather than drifting silently apart from the name arm it mirrors. Structural on purpose: this pins only that the derived constant equals the two source constants today; AC1/AC2/AC5 are the behavioural proof that the arm is actually reached and actually discriminates. Witness: `test_ac8_event_trigger_names_are_derived_not_re_literalled`.
9. **The head-admission bound is STATED, not overclaimed.** This repair realizes §10.2 sibling preservation for carriers the head sampler ADMITTED; it does nothing for carriers the head never recorded. Per `B-137` (measured at U-OD-59), a `TAIL_BASED_PROD` cell at base-rate 0.1 admits only ~10% of event carriers to the tail consumer at all (10.4% over 4,000 carriers), ~20% at base-rate 0.2 (20.9%) — this unit changes what happens to the sibling tree for the carriers that DO arrive, not how many arrive. No acceptance criterion here asserts a full floor; the bound is recorded in the AC1/AC4 witness docstrings rather than as a separate assertion, since no additional behaviour needs pinning beyond what U-OD-59's own W13/W14 already measured.
10. **The full pre-existing OD suite, this unit's new witnesses, and the runtime real-dispatch suite pass together — AND the OD-local witnesses pass with `harness_runtime` UNIMPORTABLE**, per the `axis-isolation — harness-od` CI contract U-OD-59 §0.10 established. `harness-od/tests/test_b123_event_aware_classification_trigger.py`'s import set is `{harness_core, harness_od, opentelemetry, pytest, typing}`-shaped (no `harness_runtime`), so the axis-isolation lane requires no new carve-out. **Zero edits to any pre-existing test module's assertions** other than the single in-place W7 inversion (AC #3) — every other pre-existing witness in `test_b133_event_aware_tail_floor.py`, `test_tail_keep_span_processor.py` and the runtime `test_b133_...` module is untouched, since this widening only ever ADDS a `True` answer the predicate did not previously give.
11. **`span.events` is read EXACTLY ONCE per `on_end`, and the witness is structural.** The widened predicate and the pre-existing `_carries_always_sampled_event` would otherwise each read the OTel `.events` PROPERTY — a lock acquisition, a deque copy and a tuple allocation apiece — for every ordinary buffered span, including zero-event spans. Asserted with a `ReadableSpan`-shaped double whose `.events` property INCREMENTS A COUNTER on access (the counting sibling of AC7's raising double), parametrized over the three paths that reach the event checks: zero events, a non-matching event on the ordinary buffer path, and an always-sampled event carrier. **The assertion is `== 1`, deliberately, not `<= 2`** — a bounded-above assertion would pass against the very code this criterion exists to correct. A paired witness re-confirms AC7 on the same instrument: a NAME-matching span must touch `.events` **zero** times. Witnesses: `test_ac11_span_events_is_read_exactly_once_per_on_end`, `test_ac7_confirmed_by_counting_double_zero_events_touches`. *(Added at out-of-family Codex round 1 [P2]; see the `Files affected` note.)*
12. **The event scan reads the WHOLE event list, not just the first event — and this criterion exists because the merge gate proved its absence was exploitable.** A mutation reducing the event-scan `any(...)` to a first-event-only check slipped through the ENTIRE pre-AC12 witness set: every single-function witness (AC1/AC2/AC5/AC6/AC7/AC11) uses a span with ≤ 1 event, where `any()` and *first-event-only* are indistinguishable; the one multi-event witness reaching this line (`test_w15_...` at `member_index=2`) asserts only carrier export and `buffered_trace_count == 0`, both driven by the SEPARATE `_carries_always_sampled_event` §9.2 arm with no buffered sibling in that trace for the §10.2 keep flag to gate; and AC4/AC4b's real dispatch happens to emit `breaker.tripped` FIRST, so a first-event-only scan finds it there too. **This is the same defect class `W15`/`W16` were authored to guard for the sibling function — left unguarded on the function this unit actually modifies.** The witness therefore asserts on the buffered SIBLING's survival (what the keep flag gates), never on carrier export (what the unmutated sibling function gates), with a real non-member (`retry.skipped` — what the dispatcher emits when a candidate's breaker is pre-open) placed ahead of the member, parametrized over member position **first / middle / LAST**; the last-position case is the strongest discriminator. Witness: `test_ac12_keep_flag_scans_the_whole_event_list_not_just_the_first`. *(Added at the 3-lens merge gate's test-witness BLOCK; see probe (vii).)*

**PD-8 — the arm is load-bearing, demonstrated by mutation and MEASURED.** Seven probes. Each was applied to a `cp` backup of the target file, run against the full witness set, and restored with `cp` and a `diff -q` byte-identity check — never `git checkout`, since the working tree carried uncommitted arc content a checkout would have destroyed. The table records what actually went RED, not what was predicted; where a prediction and the measurement disagreed, the measurement is recorded and the disagreement is explained.

| # | Mutation | Measured RED |
|---|---|---|
| i | Delete the event-scan arm from `is_classification_trigger` entirely | **5 RED** — AC1, AC2, AC3 (the inverted W7), AC4, AC4b. *(Re-measured after AC4b was added; the first run, before that witness existed, returned the same set minus AC4b.)* |
| ii | Replace the derived two-member §10.2 event-name set with the nineteen-member `ALWAYS_SAMPLED_EVENT_CLASSES` import | **AC5 RED**, plus expected collateral at W1 / W8 / W10 — the nineteen-member superset over-matches, so §9.2-only members become false-positive §10.2 triggers. The collateral is the mutation working as designed, not a witness defect |
| iii | Move the event scan to the TOP of the predicate, ahead of the name/attribute checks | **BOTH AC7 witnesses RED (the raising double and AC11's counting sibling), and NOTHING else** — AC11's own `== 1` count stays GREEN, correctly: a single call reads at most once wherever the scan sits, so reordering breaks the name-arm short-circuit and not the single-read discipline. The two criteria are genuinely independent — zero behavioural witnesses moved. This is the CORRECT result, honestly recorded: ordering here is a cost property, not a behavioural one, and AC7 exists precisely to make it falsifiable (a span double whose `.events` property raises if touched). The prediction of GREEN-except-AC7, made by analogy with U-OD-59 §0.8's identical finding, held |
| iv | Remove `SANDBOX_VIOLATION_SPAN_NAME` from the derived event-name set | **AC2 RED**, plus **AC8 RED** as a direct consequence — AC8 pins the derived set against its two source constants, so removing a member breaks the structural identity too |
| v | Delete the event arm's root-close `_materialize_trace_decision` call at `tail_keep_span_processor.py` | **7 RED** — AC5, W6, AC3 (W7), W10, W2, W4, and **AC4b**. **AC4 stays GREEN, and that is the finding, not a miss:** its ambient-root arrangement materializes through the ordinary buffer path and never reaches this line. The prediction that AC4 would go RED was WRONG; rather than restate the probe to fit, AC4b was ADDED to cover the term-5 production shape the gap exposed, and probe (v) was re-run against it |
| vi | Hard-code the derived set as a literal and rename one of the two source constants | **AC8 RED**, plus AC1 collateral |
| vii | Reduce the event scan's `any(...)` to a FIRST-EVENT-ONLY check | **AC12 `member-middle` + `member-last` RED, `member-first` correctly GREEN, and ZERO collateral** — 2 failed / 83 passed. The zero-collateral result is the finding: it confirms by measurement that every other witness in the set genuinely cannot see this defect, which is why AC12 had to exist |

Probes (v) and (vii) each changed the deliverable. Probe (v): it did not merely fail to fire, it revealed that the shipped production shape — carrier as trace root, per OD spec v1.38 §9.2.1 term 5 — had no real-dispatch witness under the widened flag. The unit's witness set is larger because the probe was believed over the plan. **Probe (vii) is the second such case, and it came from the 3-lens merge gate rather than from this plan's own probe set** — the test-witness lens reasoned out the first-event-only reduction, showed it survived every existing witness, and BLOCKED. AC12 and this probe row are the remedy; the probe was then run and killed the mutation at two of its three positions with zero collateral. **Recorded rather than smoothed over, because the same defect class had already been caught once at the `B-133` leg (closed there by `W15`/`W16` on the sibling function) — a repeat catch means the class is systemic to this event-scanning pattern, not incidental.**

### §1.2 What this unit does NOT own

- **The HEAD half of the floor.** Declared bound per OD spec v1.38 §9.2.1 term 4 (inherited unamended by v1.39), asserted at AC #9, **not repaired here**. This unit delivers the §10.2 sibling tree only for carriers the head sampler admitted; `B-137`'s architecture-level route (an unconditionally admitting head in `TAIL_BASED_PROD` mode) is not decided here.
- **§10.2 row 1** (`validator.fail.permanence`). An attribute carrier, outside the name-versus-event family entirely — the name-versus-event distinction this unit closes does not apply to it. Its inertness (no production path sets that attribute) is register row **`B-124`**'s scope, split into a closeable §10.2 half and a forward-owed §9.2 half per the register's own grounding; neither half is touched here.
- **The `B-136` name-arm root-close buffer leak.** Pre-existing (surfaced at the U-OD-59 leg), registered, not repaired — this unit's own arm inherits the SAME materialize-on-root-close call U-OD-59 already built and does not extend the leak, but does not close it either.
- **The `B-137` head-starves-the-tail architecture question.** Cross-referenced at AC #9; not decided here.
- **Emitting a span named `sandbox.violation` or `breaker.tripped`.** The register row's SECOND candidate remedy (`B-123` close-out step (2)) — explicitly rejected in favor of the predicate-widening candidate taken here, because minting a new emission primitive is an X-AL-3 design extension requiring back-flow, and the same venue-(b) route `B-133` already refused.

---

## §2 Coverage matrix delta (v2.33 → v2.34)

| Contract surface | Units covering (delta) |
|---|---|
| C-OD-10 §10.2 (existing, OD spec v1.2 — the two event-shaped trigger rows, now realized conformant) + C-OD-09 §9.2.1 term 3 (AMENDED at OD spec v1.39 — the `B-123` predicate widening) | **U-OD-60 (NEW)** |

DAG: U-OD-60 added as a new node; in-degree per its `Depends on` (**U-OD-11**, **U-OD-59**) — **two new intra-axis edges, U-OD-11 → U-OD-60 and U-OD-59 → U-OD-60**; no existing edge removed or rewired; **no cross-axis edge**.

---

## §3 Filing footer

| Field | Value |
|---|---|
| Artifact | `Implementation_Plan_Operational_Discipline_v2_34.md` (delta over v2.33) |
| Authored at | Phase 7 — the `B-123` repair leg (2026-08-09) |
| Authoring authority | `Spec_Operational_Discipline_v1_39.md` §1 (the term this unit realizes) + forward-register row `B-123` close-out steps (2)+(3) (the repair candidate + scope selection) |
| Predecessor | `Implementation_Plan_Operational_Discipline_v2_33.md` (v2.33 — the `B-133` realization leg) |
| Siblings (same arc) | `Spec_Operational_Discipline_v1_39.md` — filed in the SAME PR, together with the implementation, per the `B-116-t3` / `B-133` precedent |
| Unit-count change | **+1** (NEW U-OD-60) |
| Cluster-count change | None |
| DAG topology change | One new node (U-OD-60); two new intra-axis edges (U-OD-11 → U-OD-60; U-OD-59 → U-OD-60); **zero cross-axis edges** |
| Cross-axis cascade | **NONE** — verified, not assumed: `is_classification_trigger`'s THREE production call sites are all OD-internal (`tail_keep_span_processor.py`), it gains no import outside `harness_od.*`, and no cross-package consumer exists. CXA aggregate stays frozen at 111 |
| Empirical grounding | The `B-123` positive control (re-grounded at the register, 2026-08-08/09): `is_classification_trigger` observed returning `False` on the real `breaker.tripped` event carrier before this unit; the exhaustive `add_event(` production sweep confirming `sandbox.violation` has zero event-shaped producers at HEAD (re-verified independently this session); the ADR-D2 §1.8 cite-resolution correction (§0.5), re-verified independently of the sibling spec leg |
| Register consequence | **`B-123` CLOSES at this leg's implementation.** Its Class-3 informational rider is FALSIFIED and corrected at the register per §0.5, not carried into contract or plan text |
| Revision policy | Delta-only per workspace `CLAUDE.md` §2.4; revisions route to design-phase back-flow |
