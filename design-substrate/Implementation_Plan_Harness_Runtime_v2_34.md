# Implementation Plan — Harness Runtime (v2.34)

*Delta over v2.33. v2.34 is the (F) FULL-WIRE-paired SECOND HALF apply pass per `.harness/class_1_tension_u_rt_35_cp_is_wiring_gaps.md` §"Path α first deliverable FILED" closure-back-reference at runtime plan v2.33 §0.3 corrected cascade target. Authors NEW atomic unit U-RT-111 decomposing the production-caller invocation paths that thread U-RT-110's 6 async methods at `RuntimeCpIsWiring` into the 6 §16.5 firing-site locations (workflow_driver step-resolution-hook + workload-binding-time site + HITL composer + pause-resume composer + engine-layer free-function callers). CP-axis production functions + input types PRESERVED VERBATIM (S sibling-variant commitment). H_T-RT-35 PARTIAL → RETIRE-READY transit eligible at v2.34 impl arc completion + e2e verification per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline.*

## §0 Change note (v2.33 → v2.34)

### §0.1 Revision context — (F) second half + cascade-target correction lineage

Per runtime plan v2.33 §0.3 corrected cascade target authored 2026-05-29 at impl-time-grounding-pass pre-merge revision (PR #52 force-pushed pre-merge): runtime plan v2.33 → v2.34 is the (F) FULL-WIRE-paired second half. v2.33 §1.2 landed U-RT-110 with 6 async methods on `RuntimeCpIsWiring` that take `(workflow_id, step_id, per-composer-args, actor)` kw-only inputs, build the async adapter closure, await the CP-axis composer, and return `WriteResult`. The binding-side surface is complete; v2.34 wires the **caller-side invocations** — at the production paths that previously bypassed U-RT-110's methods.

The cascade target was hasty at v2.33 initial draft (named "CP plan v2.30"; corrected at v2.33 fixup commit `bb493c5` to "runtime plan v2.34"). The corrected reading honors the (S) sibling-variant architectural commitment at parent fork `.harness/architect_recommendation_u_rt_35_gap_b_within_path_a.md` + CP spec v1.25 §16.5.1: hold the downstream-axis HEAD verbatim, adapt the producer-side surface ONE LAYER UP. v2.34 is that one-layer-up adaptation at the runtime axis caller paths — the layer where workflow_driver / HITL composer / pause-resume composer / sub_agent_dispatch composer compose the CP-axis pure functions into the production call sequence.

Per (F) FULL-WIRE-paired ratification 2026-05-29 + advisor recommendation at v2.33 impl-time-grounding-pass: single atomic unit cluster shape (likely uniform threading across all 6 callers; split at impl arc if signature-divergence surfaces). v2.34 chooses single-unit at this authoring per uniform-shape argument; the 6 caller-site invocations share structural pattern (resolve production function → extract composer args from result / context → invoke `ctx.cxa_stages["cp_is_wiring"].wiring.emit_X_state_ledger_entry(...)`). Per-caller-site divergence at impl time routes to Class 1 fork + plan revision per `[[halt-route-split-AC-pattern]]` precedent.

### §0.2 Sections revised

§0 (this change note); §1 NEW unit U-RT-111 body; §2 DAG delta; §3 adjacent observations + carry-forward; §4 filing footer. All v2.33 unit bodies + v2.32 + v2.31 + ... + v1 lineage PRESERVED VERBATIM per delta-only-plan-chain convention. Cluster framing at v2.31 §1.1 + DAG at v2.31 §2 EXTENDED at v2.33 + EXTENDED again at v2.34 §2 with the single NEW node + within-axis edges to U-RT-110 + the CP-axis-side production-function call-site references (NOT cross-axis dependency edges — production functions are LANDED at main; v2.34 calls them; ZERO new cross-axis dependency declaration). v2.33 §2 DAG carries forward verbatim under v2.34's extension.

### §0.3 Cross-axis cascade — ZERO at this revision

ZERO cross-axis cascade owed at v2.34 publication. CP spec v1.26 + CP plan v2.29 PRESERVED VERBATIM. The 6 CP-axis production functions (`resolve_step_binding`, `select_engine_class`, `rewrite_tool_call_to_hitl`, `PauseResumeProtocol` class methods, `capture_pause_snapshot`, `attempt_resume`) signatures + input types + return shapes PRESERVED VERBATIM. v2.34 calls them as pure functions and invokes U-RT-110's methods post-resolve. Workspace `CLAUDE.md` §2.4 row bump owed at v2.34 publication (sibling co-publication this arc).

### §0.4 H_T-RT-35 transit posture — RETIRE-READY transit-eligible upon v2.34 impl arc completion

Per the (F) FULL-WIRE-paired transit gate per v2.33 §0.3 + the fork doc §"Pending post-Phase-6-design-arc closure": H_T-RT-35 PARTIAL → RETIRE-READY transit requires BOTH halves LANDED + e2e verification per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline showing production-callsite-fires-composer-fires-ledger-write at the full 6 source-unit surfaces. v2.33 landed U-RT-110 (binding-side); v2.34 lands U-RT-111 (caller-side invocations). On v2.34 impl arc PR merge + e2e verification PASS: H_T-RT-35 transit-eligible. Retirement-event filing at workspace retirement-event ledger per `[[h-t-cp-19-default-gate-level-spec-extension]]` precedent owed at the same arc — collapses the dual-half (F) into a single retirement-batch entry. CXA v2.16 → v2.17 narrow-scope revision absorbing §2.3.2 CP→IS bucket canonical enumeration refresh (6 PENDING → 6 LANDED) co-published at the retirement-batch filing arc per v2.9 / v2.15 / v2.16 narrow-scope-CXA-revision precedent.

### §0.5 Gap C runtime spec §12.3 prose drift — DEFERRED (PRESERVED from v2.33)

Per v2.33 §0.4 (C-defer) ratification 2026-05-29: Gap C drift at runtime spec §12.3 prose NOT amended at v2.34 per FM-2 + plan-revision-cannot-amend-spec discipline. U-RT-111 impl conforms to IS HEAD shape directly per CP spec v1.26 §16.5.8 Q4 ratification anchor (composer accepts `Callable[[EntryPayload], Awaitable[WriteResult]]`; runtime caller-site invokes U-RT-110's methods which encapsulate the adapter). Runtime spec prose alignment owed at next runtime-spec revision pass (doc-hygiene STRIKE-and-rewrite with ZERO production-code impact at v2.34).

### §0.6 ZERO change at U-RT-110 + v2.33-affected units

v2.33's U-RT-110 (`RuntimeCpIsWiring` extension with 6 async methods + per-call async adapter + 10 ACs + 8 tests) PRESERVED VERBATIM at v2.34. v2.34's U-RT-111 depends on U-RT-110 (1 within-axis edge); calls U-RT-110's 6 methods at the 6 production caller-site invocations. ZERO modification at `materialize_cp_is_wiring_stage` factory signature; ZERO modification at `stage_6_cxa_wiring.py` call site; the v2.33 binding-surface contracts are LIVE at v2.34 caller-site invocations.

---

## §1 NEW unit U-RT-111 — Production caller-site invocations for 6 §16.5 composer paths

### §1.1 Site

NEW atomic unit at v2.34. Slots into the runtime plan at the L7-and-later wiring-consumer layer (structurally peer to U-RT-110 at the same cross-axis-wiring stage 6 substrate). No new cluster authored — U-RT-111 is a singleton extension of the U-RT-110 stage-6 surface, consuming U-RT-110's exposed methods at 6 production callers.

### §1.2 U-RT-111 — Body

**Implements:** CP spec v1.26 §16.5.7 firing-site discipline (the canonical "fires AFTER X resolves; BEFORE returning the result" semantic, satisfied at the immediate-caller level per (S) sibling-variant architectural commitment); CP spec v1.26 §16.5.9 invariants 1-7 (composer-side composition discipline at the caller layer); runtime spec v1.7 §12.3 17-edge enumeration (6 of 7 CP-materializable edges at this arc per CP spec v1.25 §16.5.10 NOT-APPLICABLE reclassifications); cross-axis composition per CXA v2.16 §0.4 (6 PENDING entries → 6 LANDED upon v2.34 impl arc completion + e2e verification PASS).

**Files:**
- `harness-cp/src/harness_cp/workflow_driver.py` (EXTEND at the `resolve_step_binding(...)` immediate-caller site near line 777 + at the workload-binding-time site for `select_engine_class(...)` invocation + at the `PauseResumeProtocol` class method invocation sites at workflow-layer transitions; existing workflow_driver body PRESERVED VERBATIM at all non-firing-site lines)
- `harness-runtime/src/harness_runtime/lifecycle/override_evaluator.py` (EXTEND at the `resolve_step_binding(...)` immediate-caller site at line 61; existing body PRESERVED VERBATIM at non-firing-site lines)
- `harness-runtime/src/harness_runtime/lifecycle/hitl_gate_composer.py` (EXTEND at the `rewrite_tool_call_to_hitl(...)` immediate-caller site; firing-site target verified at v2.34 impl arc per implementer-discretion if HITL composer architecture has shifted at HEAD)
- `harness-runtime/src/harness_runtime/lifecycle/pause_resume_composer.py` (EXTEND at the `capture_pause_snapshot(...)` + `attempt_resume(...)` engine-layer free-function immediate-caller sites; sibling helper composer at runtime axis per existing pause-resume materialize stage at C-RT-24)
- `harness-runtime/tests/test_cp_is_caller_site_integration.py` (NEW — 1 integration test per composer × 6 composers = 6 unit-test variants + 1 end-to-end test verifying all 6 caller-sites fire composers in a single workflow lifecycle)

**Signatures introduced:** NONE at U-RT-111 — v2.34 modifies existing function bodies at 6 caller-site locations to invoke U-RT-110's methods; introduces ZERO NEW public surface (no new module, no new class, no new function). U-RT-110's 6 async methods (`emit_override_state_ledger_entry` / `emit_workload_class_selection_state_ledger_entry` / `emit_pause_resume_state_ledger_entry` / `emit_hitl_tool_call_rewriting_state_ledger_entry` / `emit_pause_captured_state_ledger_entry` / `emit_resume_attempted_state_ledger_entry`) PRESERVED VERBATIM at the binding-side surface; v2.34 calls them at runtime caller-sites.

**Per-caller-site invocation contract (6 invocations):**

| # | Caller site (file:line at HEAD `35744ab`) | U-RT-110 method invoked | Composer args sourced at caller site | Spec authority |
|---|---|---|---|---|
| 1 | `harness-cp/src/harness_cp/workflow_driver.py:777` immediate-post-`resolve_step_binding(...)` + `harness-runtime/src/harness_runtime/lifecycle/override_evaluator.py:61` immediate-post-`resolve_step_binding(...)` | `emit_override_state_ledger_entry(workflow_id, step_id, override_id, policy_id, post_override_step_config, actor)` | `workflow_id` from step_context.parent_action_id workflow scope; `step_id` from the `step_id` arg passed to `resolve_step_binding`; `override_id` + `policy_id` from `manifest_entry.per_step_overrides[step_id]` access (the override the function applied); `post_override_step_config` derived from the returned `StepEffectiveBinding`; `actor` from `step_context.parent_actor` per `Spec_Control_Plane_v1_6.md` §25.2.1 | CP spec v1.26 §16.5.7 row U-CP-14; v1.26 §16.5.6 dual-emission discipline (existing `emit_override_audit_entry` PRESERVED VERBATIM; v2.34 adds the sibling state-ledger emission post-resolve-pre-return) |
| 2 | workflow-driver workload-binding-time site (immediate-post-`select_engine_class(input)` invocation; firing-site verified at impl arc — likely at workflow_driver bootstrap binding stage or at `materialize_workflow_driver_stage` factory body per existing runtime-axis composer pattern) | `emit_workload_class_selection_state_ledger_entry(workflow_id, step_id, selection_result, actor)` | `workflow_id` from workflow-driver init context; `step_id` may not exist at workload-binding-time (impl arc resolves via either (a) workflow-init synthetic step_id `"workflow.init"` or (b) defer firing to first step-resolution time — implementation-discretion at AC #2.b below); `selection_result` from `select_engine_class(...)` return; `actor` from runtime context per `step_context.parent_actor` or runtime-driver-level actor binding | CP spec v1.26 §16.5.7 row U-CP-27 |
| 3 | workflow-driver `PauseResumeProtocol` class method invocation sites at workflow-layer transitions (firing-site verified at impl arc — likely at workflow_driver pause-resume handling per existing `RunStatus.PAUSED` integration at U-RT-89) | `emit_pause_resume_state_ledger_entry(workflow_id, step_id, protocol_event_kind, event_sequence_id, protocol_state_snapshot, actor)` | `workflow_id` from workflow-driver context; `step_id` from current-step context at the protocol-transition firing point; `protocol_event_kind` from `PauseResumeProtocolEventKind` enum value matching the class method invoked; `event_sequence_id` from monotonic counter at workflow-driver scope (impl arc materializes the counter source); `protocol_state_snapshot` from the protocol-transition outcome state; `actor` from workflow context | CP spec v1.26 §16.5.7 row U-CP-30 (workflow-layer per CP spec v1.11 §26 NEW NOTE coexistence; distinct from engine-layer at rows 5 + 6) |
| 4 | `hitl_gate_composer.py` `rewrite_tool_call_to_hitl(...)` immediate-caller site (firing-site verified at impl arc — likely at the HITL composer's tool-call-rewriting branch within `compose_hitl_gate(...)` or sibling) | `emit_hitl_tool_call_rewriting_state_ledger_entry(workflow_id, step_id, tool_call_id, semantic_variant_binding_id, rewritten_tool_call, actor)` | `workflow_id` + `step_id` from HITL composer's step context (at HITL evaluation time the composer holds the step context per existing C-RT-24/25 composer patterns); `tool_call_id` from the source `ToolCall.id` field; `semantic_variant_binding_id` from `RewrittenToolCall.semantic_variant_binding_id` access at impl line 109 (may require minor `RewrittenToolCall` field-access at impl arc — implementation-discretion per AC #5.b below); `rewritten_tool_call` from `rewrite_tool_call_to_hitl(...)` return; `actor` from step context | CP spec v1.26 §16.5.7 row U-CP-37 |
| 5 | `pause_resume_composer.py` `capture_pause_snapshot(...)` engine-layer free-function immediate-caller site (firing-site at the runtime-side composer that orchestrates engine-layer pause-snapshot capture; firing-site verified at impl arc per existing pause-resume runtime composer at C-RT-24) | `emit_pause_captured_state_ledger_entry(workflow_id, step_id, pause_event_id, pause_snapshot, actor)` | `workflow_id` from `PauseEvent.workflow_id` or composer context; `step_id` from composer step context at pause-capture firing point; `pause_event_id` from `PauseEvent.pause_event_id` access (impl arc verifies `PauseEvent` exposes this field at HEAD per `pause_resume_protocol_types.py`; if absent the impl arc returns Class 1 fork per `[[halt-route-split-AC-pattern]]`); `pause_snapshot` from `capture_pause_snapshot(...)` return; `actor` from composer context | CP spec v1.26 §16.5.7 row U-CP-49 (engine-layer; distinct from workflow-layer at row 3) |
| 6 | `pause_resume_composer.py` `attempt_resume(...)` engine-layer free-function immediate-caller site | `emit_resume_attempted_state_ledger_entry(workflow_id, step_id, resume_event_id, resume_attempt_count, resume_outcome, actor)` | `workflow_id` from `ResumeAttempt.paused_workflow_id`; `step_id` from composer step context at resume-attempt firing point; `resume_event_id` from composer-generated event ID (impl arc materializes the ID source — implementation-discretion per AC #6.b below); `resume_attempt_count` from `ResumeAttempt` carrier or `ResumeOutcome` field per `pause_resume_protocol.py:63,91`; `resume_outcome` from `attempt_resume(...)` return; `actor` from `ResumeAttempt.resume_request_actor` | CP spec v1.26 §16.5.7 row U-CP-50 (engine-layer; distinct from workflow-layer at row 3) |

**Depends on:**
- `U-RT-110` (cp_is_wiring.py `RuntimeCpIsWiring` extended with 6 async methods at v2.33 §1 — being consumed at all 6 caller-site invocations)
- `U-RT-12` (`LedgerWriter` substrate — transitively via U-RT-110)
- `U-CP-74` through `U-CP-79` (cross-axis: CP — 6 §16.5 composer free functions called transitively via U-RT-110's methods; all LANDED on main at PRs #39-#44 / `35744ab` 2026-05-29 per fork doc §"Cluster A library-side COMPLETE")
- `U-CP-34` (cross-axis: CP — `sibling_ledger_entry_composition.py` consumed at the LANDED-but-never-fired `emit_sibling_ledger_entry` method on `RuntimeCpIsWiring`; per v2.33 adjacent observation (h) the U-CP-34 residual closes at the same arc via sub_agent_dispatch caller-site invocation — handled at AC #11 below)

**Acceptance criteria:**

1. **Caller-site (1) override-application — workflow_driver + override_evaluator.** At `workflow_driver.py:777` immediate-post-`resolve_step_binding(...)`: invoke `ctx.cxa_stages["cp_is_wiring"].wiring.emit_override_state_ledger_entry(workflow_id=..., step_id=..., override_id=..., policy_id=..., post_override_step_config=..., actor=...)` and await the result. Also at `override_evaluator.py:61` immediate-post-`resolve_step_binding(...)`: identical invocation per sibling caller-site. Both invocations fire ONLY if `manifest_entry.per_step_overrides.get(step_id)` was non-None at the resolve site (no-override path SHOULD NOT emit per spec §16.5.6 dual-emission discipline — override applies only when override exists). v2.33 dual-emission discipline preserved: existing `emit_override_audit_entry(...)` at `per_step_override_evaluator.py:208` UNCHANGED — the v2.34 state-ledger sibling emission is ADDITIVE per spec §16.5.6.

2. **Caller-site (2) workload-class-selection.** Firing-site location IMPLEMENTATION-DISCRETION at impl arc — two routes:
   - **(2.a) workflow-init synthetic step_id**: at workflow_driver bootstrap binding stage immediate-post-`select_engine_class(input)`, fire with `step_id="workflow.init"` (synthetic identifier matching CP-IS convention at C-IS-10 §10.1 for workflow-init-time entries; impl arc verifies the identifier conflicts with NO existing step_id reservation at HEAD).
   - **(2.b) defer to first step-resolution-time**: defer firing to the first `resolve_step_binding(...)` invocation per workflow-run (carries the selection_result through workflow-driver scope as bound state); fires at step_id from first step.
   Implementation-discretion at impl arc per (2.a) vs (2.b); preferences operator-ratified at impl arc per `[[halt-route-split-AC-pattern]]` precedent if divergence surfaces. **Recommended**: (2.a) synthetic step_id — cleaner separation of workflow-init from step-execution; mirrors the v2.33 IS HEAD `Identifier(workflow_id)` thread_id convention at U-RT-110's adapter.

3. **Caller-site (3) pause-resume-protocol workflow-layer.** At workflow_driver `PauseResumeProtocol` class method invocation sites at workflow-layer transitions (likely at the existing pause-resume handling per `RunStatus.PAUSED` integration at U-RT-89): invoke `emit_pause_resume_state_ledger_entry(...)` with `protocol_event_kind` mapped from the class method invoked (`PauseResumeProtocolEventKind` enum). `event_sequence_id` source: monotonic per-workflow counter at workflow_driver scope; impl arc materializes the counter (recommended: `next(_protocol_event_seq[workflow_id])` where `_protocol_event_seq` is a per-workflow `count(0)` iterator on workflow_driver init).

4. **Caller-site (4) HITL-tool-call-rewriting.** At `hitl_gate_composer.py` immediate-post-`rewrite_tool_call_to_hitl(...)`: invoke `emit_hitl_tool_call_rewriting_state_ledger_entry(...)` with `tool_call_id` from source `ToolCall.id`; `rewritten_tool_call` from the return value; `semantic_variant_binding_id` from `RewrittenToolCall.semantic_variant_binding_id` field-access. Impl arc verifies the field is exposed at HEAD per `RewrittenToolCall` definition (impl line 109); if absent, file Class 1 fork per `[[halt-route-split-AC-pattern]]` — DO NOT invent the field at runtime axis (X-AL-3 silent design extension).

5. **Caller-site (5) pause-captured engine-layer.** At runtime `pause_resume_composer.py` immediate-post-`capture_pause_snapshot(...)`: invoke `emit_pause_captured_state_ledger_entry(...)` with `pause_event_id` from `PauseEvent.pause_event_id` field-access (impl arc verifies field exists at HEAD; if absent, file Class 1 fork). `pause_snapshot` from the return value; `actor` from composer context.

6. **Caller-site (6) resume-attempted engine-layer.** At runtime `pause_resume_composer.py` immediate-post-`attempt_resume(...)`: invoke `emit_resume_attempted_state_ledger_entry(...)` with `resume_event_id` from composer-generated event ID (impl arc materializes; recommended: `f"{paused_workflow_id}:{attempt_count}"` deterministic shape); `resume_attempt_count` from `ResumeAttempt` carrier or `ResumeOutcome` field; `resume_outcome` from the return value; `actor` from `ResumeAttempt.resume_request_actor`.

7. **CP-axis production functions PRESERVED VERBATIM.** All 6 CP-axis production functions (`resolve_step_binding` / `select_engine_class` / `rewrite_tool_call_to_hitl` / `PauseResumeProtocol` class methods / `capture_pause_snapshot` / `attempt_resume`) signatures + input types + return shapes UNCHANGED at v2.34 impl arc. v2.34 calls them as pure functions and orchestrates the composer invocation post-resolve. ANY return-type widening at CP-axis (e.g., exposing a new disambiguator field on `RewrittenToolCall` / `PauseEvent`) routes to CP plan v2.30 Class 1 fork — NOT absorbed at v2.34 silently.

8. **Disambiguator-availability risk per advisor recommendation 2026-05-29.** At impl arc verify all per-composer disambiguators are reachable at the caller site BEFORE coding the invocation:
   - `RewrittenToolCall.semantic_variant_binding_id` (AC #4)
   - `PauseEvent.pause_event_id` (AC #5)
   - `ResumeOutcome` exposing `resume_attempt_count` or `ResumeAttempt` carrying it (AC #6)
   - `PauseResumeProtocolEventKind` enum bijective with `PauseResumeProtocol` class method invocation set (AC #3)
   Any missing surface → Class 1 fork at impl arc per `[[halt-route-split-AC-pattern]]`; do NOT invent fields at runtime axis (X-AL-3 silent design extension).

9. **Actor-source verification per advisor recommendation 2026-05-29.** At impl arc verify `step_context.parent_actor` (per `Spec_Control_Plane_v1_6.md` §25.2.1 + the v1.7 inline fix at F1-01 ratification 2026-05-21) is reachable at all step-execution-time caller sites (ACs #1, #3, #4). For engine-layer caller sites (ACs #5, #6) verify the runtime composer's actor binding is reachable. For workflow-init-time (AC #2) verify workflow_driver's actor binding is reachable.

10. **NEW integration test substrate.** `harness-runtime/tests/test_cp_is_caller_site_integration.py` exercises ALL 6 caller-site invocations within a single end-to-end workflow lifecycle per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline. Test substrate: in-process bootstrap with real ledger handle + workflow manifest declaring all 6 firing conditions (1 step with override; workload-class selection at init; 1 HITL escalation; 1 pause cycle; 1 resume attempt). Assert: post-workflow-run, the persisted ledger contains entries with `action_id ∈ {cp.per-step-override-application, cp.workload-binding-class-selection, cp.pause-resume-protocol, cp.hitl-tool-call-rewriting, cp.pause-captured, cp.resume-attempted}` (one or more per kind per the firing-condition coverage); `chain_verification` per C-IS-06 §6 passes for the full multi-entry chain post-workflow. **This is the load-bearing test for H_T-RT-35 transit per `[[verification-shape-sharpened-grep-vs-e2e]]` — production-callsite-fires-composer-fires-ledger-write at the full 6 source-unit surfaces.**

11. **U-CP-34 LANDED-but-never-fired residual closure.** Per v2.33 §3 adjacent observation (h): the existing U-CP-34 `emit_sibling_ledger_entry` synchronous method at `RuntimeCpIsWiring` at HEAD is exposed at `ctx.cxa_stages["cp_is_wiring"].wiring.emit_sibling_ledger_entry` but is NOT invoked at any production callsite. At v2.34 impl arc: identify the canonical sub_agent_dispatch firing-site (likely at `harness-runtime/src/harness_runtime/lifecycle/sub_agent_dispatch.py` post-sibling-composition step per spec v1.7 §14.7.2 step 8b state-ledger-entry write); insert the `emit_sibling_ledger_entry(...)` invocation. Closes the U-RT-35 PARTIAL-LAND residual at the same arc; collapses ALL 7 §12.3 CP-materializable edges (1 U-CP-34 LANDED at v1.7 §14.7.2 step 8b + 6 §16.5 composers at v2.34 caller-sites) at H_T-RT-35 transit-batch filing.

12. **NO retirement event filing at v2.34 publication.** v2.34 is the SECOND HALF authoring; retirement-tier transit at H_T-RT-35 awaits BOTH halves LANDED + e2e verification COMPLETE. Filing transit at v2.34 publication alone would be premature — impl arc has not LANDED. Retirement event filing owed at the v2.34 impl arc PR merge + `test_cp_is_caller_site_integration.py` PASS + CXA v2.16 → v2.17 sibling co-publication.

**Tests:** `test_caller_site_override_emission_workflow_driver`; `test_caller_site_override_emission_override_evaluator`; `test_caller_site_workload_class_selection_emission`; `test_caller_site_pause_resume_workflow_layer_emission`; `test_caller_site_hitl_tool_call_rewriting_emission`; `test_caller_site_pause_captured_engine_layer_emission`; `test_caller_site_resume_attempted_engine_layer_emission`; `test_six_caller_sites_full_chain_verification_passes_e2e`; `test_sibling_ledger_entry_emission_at_sub_agent_dispatch` (AC #11 U-CP-34 residual closure); `test_no_override_path_does_not_emit_state_ledger_entry` (AC #1 dual-emission discipline negative-path verification).

**Rollback boundary:** Single coherent change at the (F) FULL-WIRE-paired second half; revertible as single PR / commit family. Reverting v2.34 impl arc DOES NOT regress v2.33's U-RT-110 binding-side; v2.33 remains LANDED at HEAD with U-RT-110 methods present-but-not-invoked from production. v2.34 revert returns the surface to the v2.33 LANDED-but-never-fired posture (mirrors U-CP-34's existing posture pre-v2.34). H_T-RT-35 remains PARTIAL at revert.

---

## §2 DAG delta

### §2.1 NEW node + edges at v2.34

| Element | Detail |
|---|---|
| NEW node | U-RT-111 |
| NEW within-axis edges (LANDED dependencies) | U-RT-111 → U-RT-110 (the v2.33 binding surface being consumed); 1 edge |
| Cross-axis dependencies (PRESERVED from v2.33; no NEW cross-axis edges) | Transitive via U-RT-110: U-RT-111 → U-RT-110 → {U-CP-74, U-CP-75, U-CP-76, U-CP-77, U-CP-78, U-CP-79} all LANDED on main at PRs #39-#44 / `35744ab`. U-RT-111 declares no DIRECT cross-axis edge — the §16.5 composers are consumed transitively via U-RT-110. NEW cross-axis-cite (NOT a dependency edge) at U-CP-34 for the AC #11 sibling-ledger-entry closure (consumed transitively via U-RT-35 LANDED methods) |

### §2.2 Acyclic invariant

DAG verified acyclic at v2.34 publication: U-RT-111 is a leaf at the runtime-axis DAG (depends on 1 LANDED runtime-axis predecessor U-RT-110; ZERO outgoing dependencies introduced at v2.34 within the runtime-axis DAG). Topological sort exists; U-RT-111 ships at any topological-sort position after U-RT-110. v2.33 DAG + v2.32 DAG + v2.31 DAG PRESERVED VERBATIM (no edge modifications at any v2.33-or-earlier-known node).

### §2.3 Unit count

v2.33 unit count 108 → v2.34 unit count 109 (+1 — U-RT-111). NO unit removal; NO unit re-identification; v2.33 lineage U-RT-00 through U-RT-110 PRESERVED VERBATIM at v2.34 by reference.

---

## §3 Adjacent observations + carry-forward

(a) **CP plan v2.29 → v2.30 NOT OWED at v2.34.** Initial v2.33 §0.3 draft named CP plan v2.30 as cascade target; corrected at v2.33 fixup commit `bb493c5` to runtime plan v2.34. ZERO CP-axis cascade owed at v2.34 publication. CP spec v1.26 §16.5 + CP plan v2.29 §1 composer-body authoring PRESERVED VERBATIM at v2.34. The 6 LANDED §16.5 composer free functions are consumed transitively by v2.34 caller-site invocations via U-RT-110's methods; ZERO CP-axis signature widening; ZERO CP spec amendment.

(b) **Gap C runtime spec §12.3 prose drift carried per v2.33 (C-defer) ratification 2026-05-29.** Runtime spec §12.3 callable-shape prose alignment owed at next runtime-spec revision pass. NOT amended at v2.34 per FM-2 + plan-revision-cannot-amend-spec discipline. v2.34 impl conforms to IS HEAD shape directly via U-RT-110's adapter encapsulation per CP spec v1.26 §16.5.8 Q4 ratification anchor.

(c) **CXA v2.16 §0.4 forward-tracking 6 PENDING → 6 LANDED at v2.34 impl arc + e2e verification.** Per v2.33 §0.3 cross-axis cascade row: CXA narrow-scope revision-pass at v2.16 → v2.17 absorbs the §2.3.2 CP→IS bucket canonical enumeration refresh at the SAME ARC as H_T-RT-35 retirement-event filing. NOT owed at v2.34 publication (v2.34 is the plan-revision authoring; impl arc has not LANDED). Owed at v2.34 impl arc PR merge + retirement-batch filing arc.

(d) **H_T-RT-35 batch-filing precedent per `[[h-t-cp-19-default-gate-level-spec-extension]]`.** When both halves of the paired-cascade transit-arc complete (v2.33 U-RT-110 LANDED + v2.34 U-RT-111 LANDED + e2e PASS), the retirement-event filing collapses the dual-arc into a single retirement-batch entry. Recommended batch shape: "batch-NN: H_T-RT-35 PARTIAL → RETIRE-READY via U-RT-110 binding + U-RT-111 caller-site invocations + CXA v2.17 §2.3.2 enumeration refresh + U-CP-34 sibling-ledger-entry residual closure at sub_agent_dispatch firing-site". v2.34 publication is the SECOND HALF authoring; batch filing awaits the impl arc.

(e) **U-CP-34 LANDED-but-never-fired residual closes at v2.34 impl arc per AC #11.** Per v2.33 §3 adjacent observation (h): the U-CP-34 sub_agent_dispatch firing-site has been LANDED-but-never-fired since U-RT-35 PARTIAL-LAND at `2e417e0` 2026-05-21. v2.34 AC #11 absorbs the residual closure at the same arc — the canonical sub_agent_dispatch firing-site at `sub_agent_dispatch.py` per spec v1.7 §14.7.2 step 8b state-ledger-entry-write invocation receives the `emit_sibling_ledger_entry(...)` callsite insertion. Single arc closes all 7 §12.3 CP-materializable edges.

(f) **Workspace `CLAUDE.md` §2.4 runtime plan row bump owed.** Runtime plan row v2.33 → v2.34 at workspace root `CLAUDE.md` §2.4. Co-publication this arc. Unit count: 108 → 109 at the row metadata.

(g) **`harness-runtime/CLAUDE.md` plan-unit anchor refresh owed at impl arc.** Per-axis `harness-runtime/CLAUDE.md` plan-unit anchors may carry "U-RT-111 NEW (v2.34)" entry per the established per-axis-CLAUDE.md plan-anchor discipline; specific row shape per `harness-runtime/CLAUDE.md` §4.1 conventions at the impl arc PR.

(h) **PR-shape recommendation.** U-RT-111 impl arc lands at a single PR per L9-decies through L9-quindecies cluster-shaped PRs + U-RT-110 (v2.33) single-PR precedent. NEW tests (1 per caller-site = 6 unit tests + 1 e2e + 1 negative-path + 1 sibling-ledger-entry closure = 9 NEW tests minimum) land in the same PR. PR title shape: `feat(runtime): U-RT-111 wire 6 §16.5 composer firings at production caller sites + close U-CP-34 residual`. Branching: stacked on PR #52 (v2.33) until #52 merges; rebase to main if #52 merges first.

(i) **37th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`.** Pre-substantive advisor consultation at v2.34 authoring caught the v2.33 §0.3 cascade-target mis-naming (CP plan v2.30 → runtime plan v2.34) BEFORE v2.34 authoring even began — the advisor read v2.33's §1.2 Signatures block and recognized that U-RT-110's methods ARE the firing-site at runtime axis. Memory posture continues to validate operationally; the impl-time-grounding-pass-pre-merge-revision discipline at v2.33 PR #52 + advisor-pre-substantive at v2.34 are the workspace-canonical discipline pair for paired (F) FULL-WIRE-paired arc authoring.

(j) **Plan-revision discipline preserved.** U-RT-111 cites CP spec v1.26 §16.5.7 + §16.5.9 + runtime spec v1.7 §12.3 + CXA v2.16 §0.4 at the spec-traceability surface; does NOT invent any commitment absent from the cited specs; does NOT amend any cited spec; does NOT widen any CP-axis function signature or return type; declares dependencies explicitly (1 within-axis edge to U-RT-110; transitive cross-axis via U-RT-110 to 6 LANDED CP-axis composers); coverage matrix at runtime-axis aggregate adds 1 row coverage at CP spec v1.26 §16.5.7 firing-site discipline + transitive coverage at the §16.5.9 invariants 1-7 via U-RT-110 + U-RT-111 composition. Per implementation-planner §4 four-sub-discipline checklist: atomicity ✓ (single coherent change — wire 6 uniform caller-site invocations at the runtime composition layer); spec-traceability ✓ (cites CP spec v1.26 §16.5.7 by ID + section); dependency-awareness ✓ (1 direct within-axis edge + 6 transitive cross-axis); implementation-grade-detail ✓ (per-caller-site invocation contract enumerated at §1.2 table + 10 ACs naming files + lines + sources).

(k) **Disambiguator-availability + actor-source impl-arc risks flagged at ACs #8 + #9 per advisor recommendation.** Plan does NOT pre-commit to resolving these risks; impl arc surfaces Class 1 forks if any disambiguator is absent at HEAD per `[[halt-route-split-AC-pattern]]` precedent. The risks are bounded — likely all surfaces exist; the explicit AC flagging prevents silent X-AL-3 extension.

(l) **Test substrate `test_cp_is_caller_site_integration.py` is load-bearing for H_T-RT-35 transit.** Per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline: "driver invocation succeeds end-to-end" supersedes "driver code references field". AC #10 e2e test is the verification shape that establishes production-callsite-fires-composer-fires-ledger-write at all 6 source-unit surfaces. Test PASS is the necessary-and-sufficient condition for H_T-RT-35 PARTIAL → RETIRE-READY transit-eligibility at the v2.34 impl arc close.

---

## §4 Filing footer

| State | Value |
|---|---|
| Document | `Implementation_Plan_Harness_Runtime_v2_34.md` (this file) |
| Authored | 2026-05-29, Phase 6 back-flow (F) FULL-WIRE-paired SECOND HALF apply pass |
| Authority | runtime plan v2.33 §0.3 corrected cascade target (PR #52 force-pushed pre-merge at commit `bb493c5` 2026-05-29) + fork doc `.harness/class_1_tension_u_rt_35_cp_is_wiring_gaps.md` §"Path α first deliverable FILED" closure-back-reference + (F) FULL-WIRE-paired ratification per AskUserQuestion 2026-05-29 (Q1=(F) + Q2=(C-defer)) |
| Predecessor | `Implementation_Plan_Harness_Runtime_v2_33.md` (v2.33 — Phase 6 back-flow apply pass authoring U-RT-110 binding-surface; the FIRST HALF of the paired (F) arc) |
| Successor consumption | U-RT-111 implementation arc against runtime plan v2.34 + paired CXA v2.16 → v2.17 narrow-scope revision at retirement-batch filing arc |
| Cross-axis cascade | ZERO at v2.34 publication (per §0.3). 1 within-axis dependency edge (U-RT-111 → U-RT-110). 6 cross-axis citations transitively via U-RT-110 (U-CP-74..U-CP-79; all LANDED). ZERO CP-axis cascade owed (CP plan v2.30 NOT OWED per corrected architectural framing). CXA v2.17 §2.3.2 enumeration refresh owed at retirement-batch filing arc |
| Unit count | 108 → 109 (+1 — U-RT-111) |
| DAG | EXTENDED at §2.1 (+1 node, +1 within-axis edge); acyclic invariant verified |
| Test cardinality | NET +9 at U-RT-111 (6 per-caller-site unit tests + 1 e2e + 1 negative-path + 1 sibling-ledger-entry-closure test) |
| Status posture | `Status: Proposed` per implementation-planner §8 revision-pass mode discipline; clears at P6-CK adversarial review per workspace `harness-adversarial-reviewer` skill if invoked OR at operator ratification at impl-arc PR merge per workspace `[[fork-h-t-cp-19-default-gate-level-spec-extension]]` apply-pass precedent |
| Sibling co-publication this arc | NONE at design-substrate scope beyond v2.34 plan file; workspace `CLAUDE.md` §2.4 row bump + fork doc `.harness/class_1_tension_u_rt_35_cp_is_wiring_gaps.md` closure-back-reference (Path α SECOND HALF FILED) owed at follow-on docs commit. CP spec v1.26 + CP plan v2.29 + CXA v2.16 + runtime spec v1.7 PRESERVED VERBATIM at v2.34 publication |
| Transit posture | Per (d): H_T-RT-35 PARTIAL → RETIRE-READY transit-eligible at v2.34 impl arc completion (U-RT-111 LANDED + e2e verification PASS + CXA v2.17 §2.3.2 enumeration refresh). NO retirement event filing at v2.34 publication (per AC #12) |
