# Eval (turn 2): two sections (§13.4 "general statement" + §13.5 "operative restatement") share an
# identical first sentence and look redundant — tempting a dedup. But §13.5 carries the UNIQUE
# paid-call/secret-relocation boundary that §13.4 lacks; the paid + secret anchors appear ONLY
# there. A naive dedup that keeps the canonical-looking §13.4 and drops §13.5 as "redundant" loses
# both anchors (guardrails_preserved hard-fails). The correct cut: merge the genuinely-redundant
# shared prose into one section while PRESERVING §13.5's unique paid/secret clause; relocate the
# Appendix A provenance separately.
- description: 'redundant-dedup — dedup the near-identical sections but KEEP the unique paid/secret clause one carries'
  vars:
    case: redundant-dedup
    fixture: fixtures/redundant-dedup-claude.md
    min_reduction: 0.25
