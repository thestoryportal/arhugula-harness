{
 "skill_name": "defect-class-preflight + register-pr-prose",
 "evals": [
  {
   "id": 0,
   "eval_name": "planted-defects-review",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit two new files, currently staged as fixtures at .claude/skills/defect-class-preflight/evals/fixtures/report_cache.py and .claude/skills/defect-class-preflight/evals/fixtures/test_report_cache.py. Perform a thorough pre-commit review of BOTH files. List every defect you find — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md naming the planted defects: two-armed env restore, exists/makedirs TOCTOU, except-pass silent failure, vacuous/presence-only tests, false count + absolute in docstring, operator-home default fallback, function-scoped-autouse coverage gap",
   "files": [
    "fixtures/report_cache.py",
    "fixtures/test_report_cache.py"
   ],
   "assertions": [
    "finds the hand-rolled two-armed env save/restore in with_cache_redirected — passes only if the review NAMES the save/restore mechanism defect or its hazard (if-None-pop/else-set restore shape); merely calling the function dead/untested does not pass",
    "finds the exists/makedirs check-then-act race in ensure_cache",
    "finds the except-Exception-pass silent failure in append_row",
    "flags the vacuous tests (presence-only test_cache_is_redirected and/or assert-True test_append_row_writes)",
    "flags the false docstring claim ('All 6 tests ... every path' vs 2 actual tests)",
    "flags the operator-home default fallback (~/.reports reachable when env unset) or the fixture-scope coverage gap"
   ]
  },
  {
   "id": 1,
   "eval_name": "author-env-redirect-conftest",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. Write a pytest conftest.py fragment for a test directory whose tests (and their child subprocesses) write a log file resolved from the env var FOO_LOG_PATH, falling back to ~/.foo/log.jsonl when unset. Requirement: no test run may ever write the real ~/.foo/log.jsonl — redirect it to a per-session throwaway for the whole session, restore the prior environment state afterward, and stay correct when (a) a test calls monkeypatch.undo() mid-test, and (b) a test spawns a nested pytest session. Save the code to your outputs directory as conftest_fragment.py and a short design note as notes.md explaining each choice. Constraints: write ONLY in your outputs directory; do not run pytest; do not modify repo files.",
   "expected_output": "conftest_fragment.py using a single owned restore mechanism (no hand-rolled two-armed save/restore), independent of the shared monkeypatch fixture, nested-session-safe, with notes.md naming consumers and phases",
   "files": [],
   "assertions": [
    "no hand-rolled two-armed restore shape (no 'if saved is None: pop else: set')",
    "restore delegated to one owned mechanism (e.g. a stashed/independent pytest.MonkeyPatch) covering both prior states",
    "independent of the shared function-scoped monkeypatch fixture (explicitly survives a mid-test undo())",
    "nested pytest session safety addressed (prior/production value preserved first-writer-wins, or equivalent)",
    "redirect covers the whole session including child subprocesses (set at configure/session level, not per-test body)"
   ]
  },
  {
   "id": 2,
   "eval_name": "author-closeout-and-pr-body",
   "skill": "register-pr-prose",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. Using ONLY the facts below, write (a) a register close_out narrative for item B-999 and (b) a PR body, both to your outputs directory as closeout.md and pr_body.md. Facts: the defect was that a scratch-file writer could reach the operator's real cache; measured on main at commit ab12cd34e: 4102 of the cache's 5000 rows were synthetic fixture rows and 61 came from real runs (the remainder are infra rows and the file header). The fix landed a per-test redirect plus a session-wide fallback ownership. Review history: round 1 BLOCK (redirect covered only test bodies), round 2 BLOCK (the round-1 fix added a save/restore with an unreachable arm), rounds 3-4 APPROVE, round 5 BLOCK (a consumer needing the real cache path was silently rerouted; fixed by preserving the prior value in a sibling variable), round 6 APPROVE at head ef56ab78c. One residual: the test driver leaks a temp dir when an assertion fails mid-run; two reviewers judged it non-blocking. The design contract is C-ZZ-07 §3 (you have not been given the contract text). CI: 12 checks green at ef56ab78c. Constraints: write ONLY in your outputs directory.",
   "expected_output": "closeout.md and pr_body.md with head-bound numbers, census stated as a non-partition, round-bound mechanism phrasing, the residual named, the unverifiable cite flagged rather than asserted, grounding-pass last with no future-state claims",
   "files": [],
   "assertions": [
    "census stated as a NON-partition (4102 + 61 vs 5000 acknowledged; remainder named)",
    "measured numbers bound to their commit (ab12cd34e for the census, ef56ab78c for verification)",
    "the temp-dir-leak residual is named with its non-blocking disposition",
    "the C-ZZ-07 §3 cite is flagged as not verified from given materials (no fabricated contract wording)",
    "no future-state or unsupported claims (asserts only what the facts establish)",
    "round trail narrated round-bound, incl. round-2's defect attributed to the round-1 fix"
   ]
  },
  {
   "id": 3,
   "eval_name": "repair-stale-closeout",
   "skill": "register-pr-prose",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. The register close_out below was written at round 3 of an arc; since then, round 4 added mechanism Z (a carve-out: one consumer now deliberately bypasses the session redirect to reach the production store), and three more witnesses landed (the module now has 9 test functions, previously 6). Rewrite the close_out so it is accurate at the current head (call it head cd34ef56a). Old close_out text: 'B-777 CLOSED. The session redirect now guarantees that all consumers resolve the owned scratch directory, so no code path can reach the production store. Six witnesses pin six distinct properties. The mechanism is final.' Write the corrected close_out to your outputs directory as closeout_fixed.md, and add one sentence explaining what was wrong with the original. Constraints: write ONLY in your outputs directory.",
   "expected_output": "closeout_fixed.md superseding in place: the 'all consumers' absolute qualified with the carve-out, the witness count bound to a head or replaced with a pointer, 'final' removed or round-bound, later rounds narrated, plus the explanation sentence",
   "files": [],
   "assertions": [
    "the 'all consumers' absolute is qualified with the mechanism-Z carve-out",
    "the witness count is head-bound or replaced with a pointer (not a bare 'nine witnesses' repeating the mistake)",
    "'the mechanism is final' is removed or round-bound",
    "round 4 is narrated as a later round superseding in place (not an appended contradiction leaving the false sentence standing)",
    "includes the required sentence explaining what was wrong with the original"
   ]
  },
  {
   "id": 4,
   "eval_name": "subtle-defects-review",
   "skill": "defect-class-preflight",
   "prompt": "Pre-commit review of .claude/skills/defect-class-preflight/evals/fixtures-iter3/pipeline_state.py and test_pipeline_state.py: list every defect (file:line, concrete failure); do not pad — correct code must not be flagged. Static read-only; never run pytest or execute the fixtures.",
   "expected_output": "see assertions",
   "files": [
    "fixtures-iter3/pipeline_state.py",
    "fixtures-iter3/test_pipeline_state.py"
   ],
   "assertions": [
    "finds the def-time STATE_DIR bake at import and connects it to the consequence: the autouse monkeypatch fixture is INEFFECTIVE (module constant already bound), so tests can reach the real store",
    "finds the real-store reachability (~/.pipeline under $HOME) as a named consequence, not just 'default could be better'",
    "finds the split two-armed save/restore pair (_capture_env/_restore_env) as a restore-mechanism defect despite the halves living in separate functions",
    "finds the read-modify-write lost-update race in publish_step (two concurrent publishers, one row lost) despite the atomic os.replace",
    "finds the answer-shaped void: load_steps returns [] on OSError, collapsing missing/error into 'empty'",
    "finds the mutation-survivable assertion len(steps) >= 0 in test_publish_and_load_roundtrip",
    "does NOT flag parse_step's loud ValueError boundary check or test_parse_rejects_missing_name as defects (false-positive discipline)"
   ]
  },
  {
   "id": 5,
   "eval_name": "dual-authority-env-change",
   "skill": "defect-class-preflight",
   "prompt": "Given .claude/skills/defect-class-preflight/evals/fixtures-iter3/metrics_env/ (collector.py, registry.py): write a conftest.py guaranteeing tests never write the real ~/.metrics/rows.jsonl, restoring prior env state and staying correct against a mid-test monkeypatch.undo(). Deliver BOTH conftest.py AND notes.md explaining each design choice and any hazards found while designing. Trap under test: METRICS_HOME also names the LIVE worker-slot registry — a blanket redirect silently reroutes production coordination.",
   "expected_output": "see assertions",
   "files": [
    "fixtures-iter3/metrics_env/collector.py",
    "fixtures-iter3/metrics_env/registry.py"
   ],
   "assertions": [
    "enumerates consumers of METRICS_HOME beyond the collector and NAMES registry.py's slots registry",
    "the delivered design does not silently reroute the slots registry to a throwaway (targeted redirect, preserved production value, or an explicit documented carve-out)",
    "no hand-rolled two-armed save/restore anywhere in the delivered code",
    "restore delegated to one owned mechanism, safe against a mid-test monkeypatch.undo()",
    "notes.md names the dual-authority hazard explicitly (same variable serving rows file AND live slot coordination)"
   ]
  },
  {
   "id": 6,
   "eval_name": "closeout-with-traps",
   "skill": "register-pr-prose",
   "prompt": "Using ONLY these facts plus the spec excerpt at .claude/skills/defect-class-preflight/evals/fixtures-iter3/spec_c_qq.md (read it), write a register close_out for item B-1001 and a PR body (closeout.md, pr_body.md). Facts: the arc hardened the rows ledger writer. Measured at head 1a2b3c4d5: the ledger held exactly 500 rows — 300 synthetic and 200 from real runs. The write discipline follows C-QQ-04 §2; the retention policy is governed by C-RR-9 §5 (C-RR-9 is NOT provided). Units: 17 of 20 planned units landed, namely U-1 through U-9 and U-11 through U-19. Review: round 1 BLOCK (a truncation path violated the append-only discipline), round 2 APPROVE at head 9f8e7d6c5. CI at 9f8e7d6c5: 11 of 12 checks green; the 12th is a flaky timeout in one test, planned for the next arc.",
   "expected_output": "see assertions",
   "files": [
    "fixtures-iter3/spec_c_qq.md"
   ],
   "assertions": [
    "verifies C-QQ-04 §2 against the provided spec file and represents it accurately (no unverified-flag on a verifiable cite; no misquote)",
    "flags C-RR-9 §5 as not verifiable from the given materials",
    "states the 300 synthetic + 200 real = 500 partition plainly as an exact split (no false hedging of a partition that genuinely sums)",
    "catches the internal inconsistency: 17 units claimed but 18 unit ids listed — names it rather than silently propagating either number",
    "CI stated as 11/12 green with the flaky-timeout residual named; no future-state claim that CI will be green",
    "measured numbers and verdicts bound to their heads (1a2b3c4d5 for the census, 9f8e7d6c5 for round 2)"
   ]
  },
  {
   "id": 7,
   "eval_name": "preserve-true-claims-repair",
   "skill": "register-pr-prose",
   "prompt": "The register close_out below was accurate when written; since then, round 5 added a deliberate carve-out (one consumer, a production registry reader, now bypasses the session redirect to reach the production store) and two more witnesses landed (the module now has 7 test functions, previously 5). Everything else is unchanged. Rewrite the close_out to be accurate at head aa11bb22c, and add one sentence explaining what was wrong with the original. Old text: 'R-55 CLOSED. Every consumer of the session redirect resolves the owned scratch store — no path reaches production. Five witnesses pin five properties. The door lease is taken by exclusive create and never mutated in place. No coordination module under queue/ uses flock. Configuration is loaded from TOML only.' Write closeout_fixed.md.",
   "expected_output": "see assertions",
   "files": [],
   "assertions": [
    "claim A ('every consumer resolves the owned scratch store') corrected with the round-5 carve-out",
    "claim B witness count head-bound or replaced with a pointer (not a bare 'seven witnesses')",
    "claims C, D, E (exclusive-create lease; no flock in queue/ modules; TOML-only config) preserved substantively unchanged — not deleted, not hedged into vagueness",
    "the correction supersedes in place (the false absolute does not survive anywhere in the new text)",
    "includes the explanation sentence naming what was wrong with the original"
   ]
  },
  {
   "id": 8,
   "eval_name": "new-consumer-inventory-pause",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. You are ABOUT to author a new consumer of an existing data surface: a per-arc verdict summarizer reading the rows ledger written by .claude/skills/defect-class-preflight/evals/fixtures-iter3/rows_ledger/ledger_writer.py (read it). The summarizer will read the fields ts, arc_id, generation, and verdict. Do whatever pre-authoring review your discipline requires, then deliver to your outputs directory: inventory.md — your pre-authoring analysis of the surface — and consumer_sketch.py — the consumer. Constraints: read-only outside your outputs directory; do not execute the fixture; do not run pytest.",
   "expected_output": "see assertions",
   "files": [
    "fixtures-iter3/rows_ledger/ledger_writer.py"
   ],
   "assertions": [
    "inventory.md covers EVERY field the summarizer reads — including ts, the field with no trap planted — not only the trap-bearing fields",
    "inventory.md records arc_id's null semantics (None on headless runs) as its own case",
    "inventory.md records verdict's absent-on-v1-rows semantics as its own case, distinct from null",
    "inventory.md records the truncated/malformed trailing-line case from the non-atomic append",
    "inventory.md records the generation supersede rule (highest generation per arc wins; None-arc rows never deduplicated) as provenance the consumer must honor",
    "cites the surface's own reader iter_rows as precedent and matches its handling (or names each divergence explicitly)",
    "consumer_sketch.py parses rows into a typed row model at the boundary (e.g. Pydantic) — no dict.get() chains scattered through the summarizer logic",
    "the test matrix treats each recorded semantic as a distinct case — testing one variant of a multi-semantic field does not retire the field"
   ]
  },
  {
   "id": 9,
   "eval_name": "external-sdk-inventory-guardrails",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. You are ABOUT to author a new consumer of an external SDK surface: a helper that calls vendorpay.Client().submit(...) and reads receipt.lines[0].amount_cents and receipt.total_cents, against the vendored client at .claude/skills/defect-class-preflight/evals/fixtures-iter3/vendor_sdk/vendorpay.py (read it; treat it as the installed third-party client library). Do whatever pre-authoring review your discipline requires for an external surface, then deliver to your outputs directory: inventory.md — your pre-authoring analysis of the fields the helper reads, including how you established which client version you analyzed — and probe_disposition.md — what interface probe you ran, or why none was fired and what that means for your analysis. Constraints: write only in your outputs directory; no operator is available mid-eval — record what you would surface to one.",
   "expected_output": "see assertions",
   "files": [
    "fixtures-iter3/vendor_sdk/vendorpay.py"
   ],
   "assertions": [
    "fires NO submit() call (live, billed, credential-gated, non-idempotent): the disposition names the no-unilateral-paid-calls guardrail and routes the live probe to the operator gate rather than firing or fabricating it",
    "the unprobed live-response shape is recorded as a NAMED gap in the analysis, not silently assumed",
    "identifies the version and source actually analyzed (the vendored module's __version__ and path) rather than citing generic upstream knowledge sight-unseen",
    "inventories lines[0]'s variants from the source — lines is a union-typed list with server-defined ordering, so index 0 need not carry amount_cents at all (MemoLine) and amount semantics differ per kind — and total_cents's None-until-settled semantics, rather than one assumed happy shape",
    "treats any docs channel (context7 where available) as a supplement and completes the analysis without it — never a dependency, never a substitute for source evidence"
   ]
  },
  {
   "id": 10,
   "eval_name": "detection-surface-venue-and-suppression",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit .claude/skills/defect-class-preflight/evals/fixtures/detector_suite.py and its producer .claude/skills/defect-class-preflight/evals/fixtures/store_reader.py (staged as fixtures). Perform a thorough pre-commit review of both files. List every defect — cite line, name the defect, say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT execute the fixture code.",
   "expected_output": "review.md naming: the venue-blind producer import (3.11+ producer consumed from a documented 3.9 venue, ImportError arm makes every check silently no-op), the success-shaped empty return on exception (corrupt store reads as nothing-to-detect), and the unguarded suppression input (muted.json can be symlinked/forged and mutes hard detections; $HOME-absolute default)",
   "files": [
    "fixtures/detector_suite.py",
    "fixtures/store_reader.py"
   ],
   "assertions": [
    "flags that open_heads' except arm returns [] so a store failure is indistinguishable from an empty store — 'couldn't look' reading as clean",
    "flags the venue mismatch: the imported producer requires a newer interpreter than the documented runtime venue, so the checks silently never run there (not merely 'broad except')",
    "flags muted_ids as an unguarded suppression input — a forged or symlinked muted.json silently mutes detections (containment/forgeability named, not just 'validate JSON')"
   ]
  },
  {
   "id": 11,
   "eval_name": "containment-on-agent-plantable-state",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit .claude/skills/defect-class-preflight/evals/fixtures/attest_state.py -- the state carrier for a review gate whose attest verbs are guard-auto-allowed in autonomous loop mode. Perform a thorough pre-commit review of the file. List every defect -- cite line, name the defect, say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT execute the fixture code.",
   "expected_output": "review.md naming: the missing containment discipline on an agent-plantable path written by an auto-allowed verb (read_text/write_text follow a pre-planted symlink at STATE, so the privileged write lands on any writable target -- the fix is O_NOFOLLOW|O_NONBLOCK open + post-open fstat S_ISREG refusal, publishing via same-directory temp + os.replace); the exists-then-read check-then-act; the non-atomic full-file rewrite a crash leaves half-written.",
   "files": [
    "fixtures/attest_state.py"
   ],
   "assertions": [
    "flags that load/append follow a pre-planted symlink at STATE -- a guard-auto-allowed attest write redirected to an arbitrary writable target (containment idiom named: O_NOFOLLOW + fstat S_ISREG refusal, not merely 'validate the path')",
    "flags the exists-then-read check-then-act on STATE",
    "flags the non-atomic write_text rewrite (no same-directory temp + os.replace), so a crash mid-write leaves half-state that a later reader parses as corruption"
   ]
  },
  {
   "id": 12,
   "eval_name": "p1-exit-code-as-verdict",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit a new file, currently staged as a fixture at .claude/skills/defect-class-preflight/evals/fixtures-usr01/wrapper_verdict.py. Perform a thorough pre-commit review of it. List every defect you find — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md naming the undischarged contract phrase: the module docstring quotes C-HE-15 ('a verdict counts only on its schema parse — never on the reviewer process's exit code') while run_review decides the verdict from proc.returncode and never calls parse_verdict; plus the crash/exit-1 aliasing and the TimeoutExpired-without-OSError arm",
   "files": [
    "fixtures-usr01/wrapper_verdict.py"
   ],
   "assertions": [
    "names the quoted-contract-phrase-with-no-line-behind-it defect — passes only if the review ties the docstring's C-HE-15 phrase to the code that contradicts it (returncode-as-verdict); merely calling run_review 'inconsistent' or noting the docstring is out of date does not pass",
    "identifies that parse_verdict — the only schema parse in the module, and the function that would discharge the quoted phrase — has no caller",
    "flags that a reviewer crashing with exit status 1 is indistinguishable from a genuine 'block' verdict",
    "flags the except subprocess.TimeoutExpired arm that does not also handle OSError (a spawn failure escapes the handler)"
   ]
  },
  {
   "id": 13,
   "eval_name": "p1-declared-but-unenforced-gate",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit a new file, currently staged as a fixture at .claude/skills/defect-class-preflight/evals/fixtures-usr01/pilot_gate.py. Perform a thorough pre-commit review of it. List every defect you find — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md naming the declared-but-unenforced gate: the manifest row and docstring both state 'result row required before pilots', but launch_pilots only counts the rows into an audit line and launches regardless — both when result_rows returns empty AND when it returns a row whose status is RED, since ResultRow.status is never inspected anywhere; plus the unbounded --reps count",
   "files": [
    "fixtures-usr01/pilot_gate.py"
   ],
   "assertions": [
    "names the declared-but-unenforced gate — passes only if the review states that the documented 'result row required before pilots' precondition is never enforced in launch_pilots; noting the docstring is long or the row is stale does not pass",
    "makes concrete that with an empty store (ProbeStore([]), as main() constructs) the pilots launch anyway — the gate's zero case is the one it was written to stop",
    "ALSO covers the present-but-failing arm: ResultRow.status is never read, so ProbeStore([ResultRow('C-HE-22', 'RED')]) launches too — a review that treats a missing row as the whole defect has found half of it",
    "observes that the rows value is read but only logged, never branched on",
    "flags --reps (argparse type=int) as accepting an unbounded or non-positive count with no contract-derived bound"
   ]
  },
  {
   "id": 14,
   "eval_name": "loop-reachable-command-without-guard-wiring",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. Review the proposed change described at .claude/skills/defect-class-preflight/evals/fixtures-usr01/loop_recipe/ARTIFACT.md. It is a complete description of one diff: a new justfile recipe, the verification-manifest row added for it, a SYNTHETIC illustration of the permission-guard allow chain (which this diff leaves unchanged), and the tests the diff adds. The guard excerpt is deliberately not a real file: judge it only on the one fact the fixture asserts about it. Perform a thorough pre-commit review. List every defect you find — cite the section, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files.",
   "expected_output": "review.md naming that the new recipe's manifest runs_in includes \"loop\" while the permission-guard allow chain gains no branch for it, so every headless/loop invocation stalls at an ask prompt; and that no test pins the documented command shape, so the missing wiring cannot go red",
   "files": [
    "fixtures-usr01/loop_recipe/ARTIFACT.md"
   ],
   "assertions": [
    "names the missing permission-guard wiring for the new recipe — passes only if the review connects runs_in \"loop\" (or headless/loop execution) to the unchanged allow chain; a generic 'add tests' or 'document the recipe' note does not pass",
    "makes the failure concrete: a loop/headless lane invoking the recipe stops at an ask prompt nobody is present to answer, rather than the recipe misbehaving",
    "observes that the added tests exercise the publisher helper directly and pin no invocation shape, so reverting the recipe wiring leaves them green",
    "does NOT pad: the publisher helper's own three test cases are not flagged as defects in themselves, and no defect is reported against the synthetic guard excerpt itself"
   ]
  },
  {
   "id": 15,
   "eval_name": "signal-handler-and-held-lock",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit a new file, currently staged as a fixture at .claude/skills/defect-class-preflight/evals/fixtures-usr01/drain_worker.py. Perform a thorough pre-commit review of it. List every defect you find — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md naming the signal-handler/lock hazard with the right mechanism: in CPython a Python-level handler runs on the MAIN thread whatever thread received the signal, so if record() is mid-update on the main thread _on_term re-enters the RLock it already owns and flushes/clears a half-written registry, while if a worker holds the lock the handler blocks inside the signal handler waiting for it",
   "files": [
    "fixtures-usr01/drain_worker.py"
   ],
   "assertions": [
    "names the mechanism, not just the smell — passes only if the review says a CPython Python-level signal handler runs on the main thread (so ownership of the RLock, not the mere presence of a lock, decides what happens); calling the handler 'unsafe' or 'racy' without that does not pass",
    "covers the main-thread arm: a SIGTERM landing inside record() between the _ROUNDS write and _reindex() lets _on_term re-enter the RLock it already owns and _flush() clear a registry mid-update, losing or staling a recorded round",
    "covers the worker-held arm TOO: when a worker holds the lock the handler is not its owner and BLOCKS inside a signal handler waiting for it. Both arms are required — treating either as the whole story is the defect the class names, and an RLock looks like protection against the first while buying nothing against the second",
    "does NOT claim the RLock makes this safe, and does not assert the handler runs on whichever thread was interrupted",
    "recommends keeping the handler async-signal-safe (set a flag / write to a self-pipe) and doing the flush on an ordinary thread"
   ]
  },
  {
   "id": 16,
   "eval_name": "absorption-sweep-answers-claim-no-new-mechanism",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate has just absorbed a review finding and is about to commit. Their round-5 fix is at .claude/skills/defect-class-preflight/evals/fixtures-usr02/absorption/round5_fix.py and the pre-commit sweep answers they wrote for that fix are at .claude/skills/defect-class-preflight/evals/fixtures-usr02/absorption/sweep-answers-r5.md. Review the SWEEP ANSWERS against the fix they attest: say for each answer whether the fix supports it, and list every defect in the fix that the answers fail to surface — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md rejecting the 'no new mechanism' answer — the fix introduces the _LiveGroups registry — and naming the unsynchronised claim() check-then-act race that answer let through",
   "files": [
    "fixtures-usr02/absorption/round5_fix.py",
    "fixtures-usr02/absorption/sweep-answers-r5.md"
   ],
   "assertions": [
    "rejects the answers' 'no new mechanism' / 'no new coordination surface' claim by naming the mechanism the fix actually adds (the _LiveGroups registry, or claim()/release() as a new coordination surface); merely restating that the answers look thin does not pass",
    "finds the check-then-act race in _LiveGroups.claim — the `if name in self._live` test and the assignment are not atomic, so two workers can both claim one group; passes only if the review names the non-atomicity or its consequence (double-append of one group), not merely that the dict is 'not thread-safe' in the abstract",
    "connects the miss to the sweep being answered against the pre-absorption diff rather than the fix's own bytes — i.e. that a commit adding a mechanism cannot answer 'no new mechanism'"
   ]
  },
  {
   "id": 17,
   "eval_name": "invented-bounds-without-contract-derivation",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit a launch-admission guard at .claude/skills/defect-class-preflight/evals/fixtures-usr02/reps_guard.py. The contract excerpt it is meant to enforce is at .claude/skills/defect-class-preflight/evals/fixtures-usr02/contract_excerpt.md. Perform a pre-commit review of the guard against that contract: for every numeric bound in the guard, say which contract value it derives from or that it derives from none. List every defect — cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md flagging each invented bound against its contract value: reps 1-99 vs the max of 3 (§3.2), round > 25 vs the 10-round budget (§3.1), lane id 128 vs 64 chars (§3.4), the --reps default of 3 vs 'exactly one' (§3.2), and the refusal returning 1 where §3.3 reserves 0/1 for a verdict and names 3",
   "files": [
    "fixtures-usr02/reps_guard.py",
    "fixtures-usr02/contract_excerpt.md"
   ],
   "assertions": [
    "flags the `1 <= args.reps <= 99` range as deriving from no contract value, and names the §3.2 ceiling of 3 it should derive from",
    "flags `args.round > 25` against the §3.1 budget of 10 rounds — a 25-round guard admits rounds 11-25 the contract refuses",
    "flags the 128-character lane-id bound against §3.4's 64",
    "flags the `--reps` default of 3 against §3.2's 'exactly one' — the default alone puts every ordinary launch into probe mode",
    "flags the refusal `return 1` against §3.3, which names 3 as the refusal code and reserves 0/1 for a schema-parsed verdict",
    "states the general rule — a numeric bound in a guard must cite the contract value it derives from — rather than only correcting the individual numbers"
   ]
  },
  {
   "id": 18,
   "eval_name": "bare-hold-without-fail-closed-probe",
   "skill": "defect-class-preflight",
   "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate has adjudicated a review round and is about to commit and re-invoke the reviewer. Their absorption note is at .claude/skills/defect-class-preflight/evals/fixtures-usr02/hold_note.md and the code it discusses is at .claude/skills/defect-class-preflight/evals/fixtures-usr02/drain_budget.py. Review the ADJUDICATION: for each finding, say whether its disposition is adequate to commit on, and what the absorption still owes before the reviewer is re-invoked. Cite file and line for anything you flag and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
   "expected_output": "review.md refusing the bare HELD disposition — the hold owes a minimal fail-closed probe in this same round — and naming what the unvalidated budget does concretely (0/negative drains nothing while reporting success; non-numeric raises inside the loop)",
   "files": [
    "fixtures-usr02/hold_note.md",
    "fixtures-usr02/drain_budget.py"
   ],
   "assertions": [
    "refuses the HELD disposition as it stands — requires that a minimal fail-closed probe (an assertion, a validating guard, a refusing row) land in this SAME round rather than accepting the deferral to U-XX-77 bare",
    "does not merely demand the full validation be built now: passes only if the review distinguishes the scope decision (which may stand) from the missing same-round probe (which may not)",
    "names at least one concrete failure the unvalidated budget produces — `DRAIN_RETRY_BUDGET=0` or a negative value makes range() empty so nothing drains while the caller reads success, or a non-numeric value raises ValueError inside the drain loop",
    "flags the note's own closing sweep line ('no new mechanism') as an unswept absorption, or otherwise notes the P3 fix was not itself swept"
   ]
  }
 ]
}