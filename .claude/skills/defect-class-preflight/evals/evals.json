{
  "skill_name": "defect-class-preflight + register-pr-prose",
  "evals": [
    {
      "id": 0,
      "eval_name": "planted-defects-review",
      "skill": "defect-class-preflight",
      "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. A teammate is about to commit two new files, currently staged as fixtures at .claude/skills/defect-class-preflight/evals/fixtures/report_cache.py and .claude/skills/defect-class-preflight/evals/fixtures/test_report_cache.py. Perform a thorough pre-commit review of BOTH files. List every defect you find \u2014 cite file and line, name the defect, and say concretely how it fails (inputs/state -> wrong outcome). Write your review to the outputs directory given to you as review.md. Constraints: read-only outside your outputs directory; do NOT modify repo files, do NOT run pytest, do NOT execute the fixture code.",
      "expected_output": "review.md naming the planted defects: two-armed env restore, exists/makedirs TOCTOU, except-pass silent failure, vacuous/presence-only tests, false count + absolute in docstring, operator-home default fallback, function-scoped-autouse coverage gap",
      "files": [
        "fixtures/report_cache.py",
        "fixtures/test_report_cache.py"
      ],
      "assertions": [
        "finds the hand-rolled two-armed env save/restore in with_cache_redirected \u2014 passes only if the review NAMES the save/restore mechanism defect or its hazard (if-None-pop/else-set restore shape); merely calling the function dead/untested does not pass",
        "finds the exists/makedirs check-then-act race in ensure_cache",
        "finds the except-Exception-pass silent failure in append_row",
        "flags the vacuous tests (presence-only test_cache_is_redirected and/or assert-True test_append_row_writes)",
        "flags the false docstring claim ('All 6 tests ... every path' vs 2 actual tests)",
        "flags the operator-home default fallback (~/.reports reachable when env unset) or the fixture-scope coverage gap"
      ]
    },
    {
      "id": 1,
      "eval_name": "author-env-redirect-conftest",
      "skill": "defect-class-preflight",
      "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. Write a pytest conftest.py fragment for a test directory whose tests (and their child subprocesses) write a log file resolved from the env var FOO_LOG_PATH, falling back to ~/.foo/log.jsonl when unset. Requirement: no test run may ever write the real ~/.foo/log.jsonl \u2014 redirect it to a per-session throwaway for the whole session, restore the prior environment state afterward, and stay correct when (a) a test calls monkeypatch.undo() mid-test, and (b) a test spawns a nested pytest session. Save the code to your outputs directory as conftest_fragment.py and a short design note as notes.md explaining each choice. Constraints: write ONLY in your outputs directory; do not run pytest; do not modify repo files.",
      "expected_output": "conftest_fragment.py using a single owned restore mechanism (no hand-rolled two-armed save/restore), independent of the shared monkeypatch fixture, nested-session-safe, with notes.md naming consumers and phases",
      "files": [],
      "assertions": [
        "no hand-rolled two-armed restore shape (no 'if saved is None: pop else: set')",
        "restore delegated to one owned mechanism (e.g. a stashed/independent pytest.MonkeyPatch) covering both prior states",
        "independent of the shared function-scoped monkeypatch fixture (explicitly survives a mid-test undo())",
        "nested pytest session safety addressed (prior/production value preserved first-writer-wins, or equivalent)",
        "redirect covers the whole session including child subprocesses (set at configure/session level, not per-test body)"
      ]
    },
    {
      "id": 2,
      "eval_name": "author-closeout-and-pr-body",
      "skill": "register-pr-prose",
      "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. Using ONLY the facts below, write (a) a register close_out narrative for item B-999 and (b) a PR body, both to your outputs directory as closeout.md and pr_body.md. Facts: the defect was that a scratch-file writer could reach the operator's real cache; measured on main at commit ab12cd34e: 4102 of the cache's 5000 rows were synthetic fixture rows and 61 came from real runs (the remainder are infra rows and the file header). The fix landed a per-test redirect plus a session-wide fallback ownership. Review history: round 1 BLOCK (redirect covered only test bodies), round 2 BLOCK (the round-1 fix added a save/restore with an unreachable arm), rounds 3-4 APPROVE, round 5 BLOCK (a consumer needing the real cache path was silently rerouted; fixed by preserving the prior value in a sibling variable), round 6 APPROVE at head ef56ab78c. One residual: the test driver leaks a temp dir when an assertion fails mid-run; two reviewers judged it non-blocking. The design contract is C-ZZ-07 \u00a73 (you have not been given the contract text). CI: 12 checks green at ef56ab78c. Constraints: write ONLY in your outputs directory.",
      "expected_output": "closeout.md and pr_body.md with head-bound numbers, census stated as a non-partition, round-bound mechanism phrasing, the residual named, the unverifiable cite flagged rather than asserted, grounding-pass last with no future-state claims",
      "files": [],
      "assertions": [
        "census stated as a NON-partition (4102 + 61 vs 5000 acknowledged; remainder named)",
        "measured numbers bound to their commit (ab12cd34e for the census, ef56ab78c for verification)",
        "the temp-dir-leak residual is named with its non-blocking disposition",
        "the C-ZZ-07 \u00a73 cite is flagged as not verified from given materials (no fabricated contract wording)",
        "no future-state or unsupported claims (asserts only what the facts establish)",
        "round trail narrated round-bound, incl. round-2's defect attributed to the round-1 fix"
      ]
    },
    {
      "id": 3,
      "eval_name": "repair-stale-closeout",
      "skill": "register-pr-prose",
      "prompt": "You are in /Users/robertrhu/Projects/arhugula-v2. The register close_out below was written at round 3 of an arc; since then, round 4 added mechanism Z (a carve-out: one consumer now deliberately bypasses the session redirect to reach the production store), and three more witnesses landed (the module now has 9 test functions, previously 6). Rewrite the close_out so it is accurate at the current head (call it head cd34ef56a). Old close_out text: 'B-777 CLOSED. The session redirect now guarantees that all consumers resolve the owned scratch directory, so no code path can reach the production store. Six witnesses pin six distinct properties. The mechanism is final.' Write the corrected close_out to your outputs directory as closeout_fixed.md, and add one sentence explaining what was wrong with the original. Constraints: write ONLY in your outputs directory.",
      "expected_output": "closeout_fixed.md superseding in place: the 'all consumers' absolute qualified with the carve-out, the witness count bound to a head or replaced with a pointer, 'final' removed or round-bound, later rounds narrated, plus the explanation sentence",
      "files": [],
      "assertions": [
        "the 'all consumers' absolute is qualified with the mechanism-Z carve-out",
        "the witness count is head-bound or replaced with a pointer (not a bare 'nine witnesses' repeating the mistake)",
        "'the mechanism is final' is removed or round-bound",
        "round 4 is narrated as a later round superseding in place (not an appended contradiction leaving the false sentence standing)",
        "includes the required sentence explaining what was wrong with the original"
      ]
    },
    {
      "id": 4,
      "eval_name": "subtle-defects-review",
      "skill": "defect-class-preflight",
      "prompt": "Pre-commit review of .claude/skills/defect-class-preflight/evals/fixtures-iter3/pipeline_state.py and test_pipeline_state.py: list every defect (file:line, concrete failure); do not pad \u2014 correct code must not be flagged. Static read-only; never run pytest or execute the fixtures.",
      "expected_output": "see assertions",
      "files": [
        "fixtures-iter3/pipeline_state.py",
        "fixtures-iter3/test_pipeline_state.py"
      ],
      "assertions": [
        "finds the def-time STATE_DIR bake at import and connects it to the consequence: the autouse monkeypatch fixture is INEFFECTIVE (module constant already bound), so tests can reach the real store",
        "finds the real-store reachability (~/.pipeline under $HOME) as a named consequence, not just 'default could be better'",
        "finds the split two-armed save/restore pair (_capture_env/_restore_env) as a restore-mechanism defect despite the halves living in separate functions",
        "finds the read-modify-write lost-update race in publish_step (two concurrent publishers, one row lost) despite the atomic os.replace",
        "finds the answer-shaped void: load_steps returns [] on OSError, collapsing missing/error into 'empty'",
        "finds the mutation-survivable assertion len(steps) >= 0 in test_publish_and_load_roundtrip",
        "does NOT flag parse_step's loud ValueError boundary check or test_parse_rejects_missing_name as defects (false-positive discipline)"
      ]
    },
    {
      "id": 5,
      "eval_name": "dual-authority-env-change",
      "skill": "defect-class-preflight",
      "prompt": "Given .claude/skills/defect-class-preflight/evals/fixtures-iter3/metrics_env/ (collector.py, registry.py): write a conftest.py guaranteeing tests never write the real ~/.metrics/rows.jsonl, restore prior env, survive mid-test monkeypatch.undo(). Trap: METRICS_HOME also names the LIVE worker-slot registry \u2014 a blanket redirect silently reroutes production coordination.",
      "expected_output": "see assertions",
      "files": [
        "fixtures-iter3/metrics_env/collector.py",
        "fixtures-iter3/metrics_env/registry.py"
      ],
      "assertions": [
        "enumerates consumers of METRICS_HOME beyond the collector and NAMES registry.py's slots registry",
        "the delivered design does not silently reroute the slots registry to a throwaway (targeted redirect, preserved production value, or an explicit documented carve-out)",
        "no hand-rolled two-armed save/restore anywhere in the delivered code",
        "restore delegated to one owned mechanism, safe against a mid-test monkeypatch.undo()",
        "notes.md names the dual-authority hazard explicitly (same variable serving rows file AND live slot coordination)"
      ]
    },
    {
      "id": 6,
      "eval_name": "closeout-with-traps",
      "skill": "register-pr-prose",
      "prompt": "Write closeout.md + pr_body.md from a facts sheet with traps: a verifiable cite (spec at fixtures-iter3/spec_c_qq.md) beside an unverifiable one; a partition that genuinely sums (must not be hedged); an internally inconsistent unit count (17 claimed, 18 listed); CI 11/12 with a flaky residual (no future-green claims); two distinct heads to bind facts to.",
      "expected_output": "see assertions",
      "files": [
        "fixtures-iter3/spec_c_qq.md"
      ],
      "assertions": [
        "verifies C-QQ-04 \u00a72 against the provided spec file and represents it accurately (no unverified-flag on a verifiable cite; no misquote)",
        "flags C-RR-9 \u00a75 as not verifiable from the given materials",
        "states the 300 synthetic + 200 real = 500 partition plainly as an exact split (no false hedging of a partition that genuinely sums)",
        "catches the internal inconsistency: 17 units claimed but 18 unit ids listed \u2014 names it rather than silently propagating either number",
        "CI stated as 11/12 green with the flaky-timeout residual named; no future-state claim that CI will be green",
        "measured numbers and verdicts bound to their heads (1a2b3c4d5 for the census, 9f8e7d6c5 for round 2)"
      ]
    },
    {
      "id": 7,
      "eval_name": "preserve-true-claims-repair",
      "skill": "register-pr-prose",
      "prompt": "Repair a stale close_out where two claims were falsified (an absolute needing a carve-out; a bare witness count) and three claims remain true and must survive substantively unchanged; supersede in place with an explanation sentence.",
      "expected_output": "see assertions",
      "files": [],
      "assertions": [
        "claim A ('every consumer resolves the owned scratch store') corrected with the round-5 carve-out",
        "claim B witness count head-bound or replaced with a pointer (not a bare 'seven witnesses')",
        "claims C, D, E (exclusive-create lease; no flock in queue/ modules; TOML-only config) preserved substantively unchanged \u2014 not deleted, not hedged into vagueness",
        "the correction supersedes in place (the false absolute does not survive anywhere in the new text)",
        "includes the explanation sentence naming what was wrong with the original"
      ]
    }
  ]
}