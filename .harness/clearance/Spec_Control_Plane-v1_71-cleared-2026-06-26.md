---
artifact: design-substrate/Spec_Control_Plane_v1_71.md
version: v1.71
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_70.md §2 (registered this arc: "B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN … Resolving it needs the per-kind effect-surface refinement (re-fire-safe / fence-recoverable absent branches) COMPOSED with this delta's obl-5-respecting re-pause mode. operator_gated: false (provisional — decided at open)")
  - design-substrate/Spec_Control_Plane_v1_62.md §1 (the re-fire-safe maybe-ran kinds DECLARATIVE_STEP/INFERENCE_STEP — `_refire_unsafe_branch_indices`, the classifier this delta reuses) + design-substrate/Spec_Control_Plane_v1_69.md (the orchestrator same-step_id guard precedent — the fence-key includes step_id)
  - design-substrate/Spec_Control_Plane_v1_32.md §25.15.1 (the PAUSE row "Allowed to finish in-flight, then pause") + §25.15.2 obl. 5 (high-blast-radius PRE-dispatch gating — the operator's resume decision, NOT crash-resume) + obl. 7 (resume MUST NOT re-dispatch a terminal branch)
  - harness-cp/src/harness_cp/workflow_driver.py (the `PAUSE ∧ _crash_pause_trigger ∧ ¬_crash_complete` leg gate WIDENED from `¬_pr_maybe_ran` to `¬_pr_maybe_ran_unsafe` via `_refire_unsafe_branch_indices` — re-fire-safe ONLY; the fence-recoverable kinds fall into the unsafe set → fail-closed; the three fan-out strategies + the `_crash_pause_reconstruct_no_dispatch` thread UNCHANGED)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN registered → closed as the re-fire-safe slice; registers B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID; snapshot standalone_closed 51→52, standalone_registered 5→5 net-zero)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, BEFORE substantive work + a RECONCILE call after Codex):
      (1) CONFIRMED the design + the reframe: v1.70's stricter gate was a SCOPE boundary, not a foreclosure (§2 pre-specifies this lift); the gate-only widening reusing the §25.15 per-kind predicate; non-operator-gated; no council.
      (2) BLOCKING — terminal_status storage consistency: RESOLVED by direct read — the resume re-dispatch decision is `branch_index in _recovered_terminal` (snapshot membership at workflow_driver.py:5752), NOT a divergent terminal_status store read (proven by the fresh-store witness). No silent-drop.
      (3) BLOCKING — the second-pause-trigger soundness: directed a full-chain failed-live→re-dispatch-fails-again witness. Built + green (test_crash_resume_pause_maybe_ran_resume_redispatch_fails_again_handled).
      (4) RECONCILE after out-of-family Codex [P1]: CONFIRMED the fence-recoverable lift is UNSOUND for the deferred-omitted resume. Option B (a classification-time same-step_id guard) does NOT fix it — it checks the CRASH-RESUME manifest (Run 2), but the double-fire happens at api.resume (Run 3) with a manifest invisible at classification, and the snapshot OMITS the ordinal (no carrier to compare against). The real fix needs a snapshot step_id carrier (mirroring the orchestrator's OrchestratorEffectFencePausedResumeState) → a distinct follow-on. Directed: narrow to RE-FIRE-SAFE only (no external effect → no double-fire under ANY resume manifest), register the FENCE-STEP-ID follow-on, decompose accounting (close 1 + register 1 → net-zero). Also flagged the same lower-exposure hole in #742's crash-time predicate → folded into the follow-on's scope, not fixed here.
  - out-of-family Codex — diff review (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): [P1] caught the fence-recoverable changed-step_id double-fire (the omitted ordinal re-dispatched at api.resume under a different fence key while the maybe-fired effect already fired; the orchestrator path has a same-step_id guard for this exact case). The decorrelated catch that drove the narrow-to-re-fire-safe + decompose. [P2] (untracked dashboard-design/.agents artifacts ~100MB) — N/A to the commit: only the 7 arc files are staged. Re-review of the reworked diff owed pre-merge.
  - direct grounding: the orchestrator same-step_id guard at workflow_driver.py:5487-5495 (`_orch_marker_step_id == _resumed_orch_step_id`) confirms the codebase treats a fence-recoverable changed-step_id as a double-fire to guard, NOT accepted parity; the worker store has no per-branch dispatch-step_id reader (only dispatched_branch_kinds), so a CP-side worker guard is not implementable without a runtime reader + a snapshot carrier. The resume re-dispatch loop (`if branch_index in _recovered_terminal: continue`, :5746-5752) is the snapshot-membership re-dispatch decision.
  - by-execution witnesses (harness-cp test_workflow_driver_fanout_output_replay_full_chain.py): a re-fire-safe DECLARATIVE maybe-ran reconstructs PAUSED omitting the ordinal (re-dispatch nothing at crash-time); the full-chain api.resume → re-dispatch EXACTLY the omitted ordinal → PARTIAL EQUAL to the no-crash baseline; the BLOCKING second-pause-trigger fail-again is handled not dropped; a FENCE-RECOVERABLE TOOL_STEP maybe-ran STAYS fail-closed (Codex [P1]); a SUB_AGENT_DISPATCH maybe-ran stays fail-closed; a mixed re-fire-safe+unrecoverable set fails closed; an un-kinded marker fails closed; a re-fire-safe maybe-ran with no protocol bound fails `…-pause-resume-protocol-not-bound`; the ORCHESTRATOR_WORKERS analogue reconstructs PAUSED omitting the re-fire-safe worker. Full suites: harness-cp 1362 passed / 1 xfailed; harness-runtime + harness-cxa 2157 passed / 23 skipped (credential-gated live-e2e) / 1 xfailed; pyright 0/0/0; ruff clean; overlay-check 31/31.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.71`

v1.71 is a **bundled-absorption** delta over v1.70 (co-published with `harness-cp` impl + tests) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN` as its **RE-FIRE-SAFE slice** (a **decompose**, not a whole close). CP-ONLY (no runtime delta — reuses the §26.2 PauseResumeProtocol + the v1.44 fan-out pause-capture machinery + the v1.60 dispatch-instrumentation store + the `_refire_unsafe_branch_indices` per-kind classifier verbatim).

- **§1 — the re-fire-safe-maybe-ran re-pause build.** The `PAUSE ∧ _crash_pause_trigger ∧ ¬_crash_complete` crash-resume leg now recovers a RE-FIRE-SAFE (DECLARATIVE_STEP/INFERENCE_STEP) maybe-ran ordinal: `_instrumented ∧ ¬_refire_unsafe(maybe_ran)` → set `_crash_pause_reconstruct_no_dispatch` + fall through to the v1.70 re-pause mode (SKIP + re-establish PAUSED OMITTING); `api.resume` re-dispatches it under obl-5 (no external effect → at-most-once-external under ANY resume manifest). A non-re-fire-safe maybe-ran / un-instrumented journal STAYS fail-closed.
- **§2 — residual + scope (decompose).** Closes the re-fire-safe slice + registers `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID` (the fence-recoverable TOOL_STEP/MANAGED_AGENTS case needs a snapshot step_id carrier — the deferred-omitted resume could double-fire under a changed step_id [Codex [P1]]; also covers the lower-exposure #742 crash-time hole). The SUB_AGENT residual stays fail-closed, subsumed by `…-MAYBE-RAN-SUBAGENT`. `standalone_registered` 5 → 5 (net-zero).
- **§3 — R-FS-1 closure semantics.** G1.1 = 5 + 0 → R-FS-1 stays ACTIVE (5 follow-ons remain).

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change / new fail-class. At-most-once is PRESERVED — a re-fire-safe maybe-ran has NO external effect to double-fire under ANY resume manifest. §25.15.1 + §25.15.2 obl-5/obl-7 are IMPLEMENTED for the re-fire-safe-maybe-ran crash-resume path, not changed (the v1.68/v1.70 impl-to-cleared-spec posture). The §26.2 PauseSnapshot envelope + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **CP-ONLY, no runtime delta** — the gate widening reuses the existing per-kind classifier; there is no paired runtime spec change. The three fan-out strategies are UNCHANGED. #742's `_fence_unrecoverable_maybe_ran_indices` cleared semantics are UNTOUCHED.
- **Why fence-recoverable is split off (out-of-family Codex [P1]).** The §25.15 incomplete leg re-dispatches a fence-recoverable maybe-ran at CRASH-TIME with the SAME manifest → the fence key (idempotency_key, step_id) matches the held claim → at-most-once. This reconstruct OMITS the ordinal + DEFERS the re-dispatch to the operator-mediated `api.resume`, and the snapshot carries NO step_id, so a same-kind CHANGED-step_id resume would re-dispatch under a DIFFERENT fence key and DOUBLE-FIRE. The sound fix needs a snapshot step_id carrier → the registered FENCE-STEP-ID follow-on. The re-fire-safe limb has no such hazard (no external effect).
- **obl-5 honored** — the recovery is the RE-PAUSE mode (omit + defer to `api.resume`), NEVER the §25.15 crash-time re-dispatch. **Fidelity is invariant-3 re-ask, NOT byte-match** — a deterministic re-fire-safe branch folds identically; a non-deterministic one can change across re-execution. `operator_gated: false` decided at open.
