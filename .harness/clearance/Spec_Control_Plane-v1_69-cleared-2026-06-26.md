---
artifact: design-substrate/Spec_Control_Plane_v1_69.md
version: v1.69
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_64.md §1/§3 (registered B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING as the orchestrator analogue of the worker effect-bearing maybe-ran split; the re-fire-safe orchestrator relaxation this delta extends to the fence-recoverable kinds)
  - design-substrate/Spec_Control_Plane_v1_65.md §1/§3 (the worker B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING two-half shape — the live effect-fence ambiguous-pause composed through the barrier + the fence-recoverable maybe-ran relaxation + the same-kind/changed-kind guard — mirrored here at the orchestrator dispatch site)
  - design-substrate/Spec_Control_Plane_v1_67.md §1 (the worker MANAGED_AGENTS fence-recoverable kind + the §14.20 vendor-session fence the #748 build added — reused verbatim for the MANAGED_AGENTS orchestrator)
  - harness-cp/src/harness_cp/workflow_driver.py (the `_determine_fanout_resume` fence-recoverable orchestrator leg; the `_execute_orchestrator_workers` orchestrator dispatch §26.2 pause compose + the resume→orchestrator directive threading + the holder-peek gate extension + the `_RESUME_CARRIER_STRATEGIES` registration)
  - harness-cp/src/harness_cp/pause_resume_protocol_types.py (the new `OrchestratorEffectFencePausedResumeState` carrier + the 6th `PauseSnapshot.orchestrator_effect_fence_resume` field) + pause_resume_protocol.py (the `_compute_snapshot_hash` conditional include + the recursive `_strip_default_fanout_resume_fields` nested-strip)
  - harness-runtime/src/harness_runtime/lifecycle/durable_pause_resume_protocol.py (the mechanical override-signature sync forwarding the additive kwarg — impl-to-cleared-spec, NOT a runtime SPEC change)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING registered → closed + registers 1 follow-on [ORCHESTRATOR-MAYBE-RAN-SUBAGENT]; snapshot standalone_closed 49→50, standalone_registered 6→6 net-zero)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, BEFORE substantive work — the load-bearing scoping calls):
      (1) The 3-part design (classification relaxation + orchestrator §26.2 pause compose + the NEW resume→orchestrator application site) CONFIRMED as the right track (mirrors the proven worker #742 shape; the pristine-window "return None → let the fence three-way-split" reasoning sound; CP-only plausible).
      (2) SKIP_AS_FIRED — flagged NOT as a green light. Discriminator traced ON EVIDENCE: `worker_steps` is static `steps[1:]` (NOT built from `orchestrator_output`), so SKIP does not cause zero-workers; but `orchestrator_output` flows into `_aggregate_orchestrator_workers` → an empty orchestrator contribution silently structures a DEGENERATE aggregate (the no-silent-failure class). Determination: REJECT SKIP_AS_FIRED at the orchestrator resume site → loud FAILED (palette = RE_FIRE/ABORT). Witnessed.
      (3) MANAGED_AGENTS bundled with TOOL_STEP — the worker family SPLIT them (#742 TOOL_STEP CP-only / #748 MANAGED_AGENTS needed a runtime fence EXTENSION). Verified the #748 runtime work is ALREADY LANDED: `managed_agents_dispatch.py:297-300` consumes `step_context.effect_fence_resolution`, the fence gate opens on `run_engine_class ∈ _DURABLE_AUTO_FENCE_ENGINE_CLASSES`, and the key is `(parent_idempotency_key=orchestrator_idempotency_key, step_id)` (deterministic across crash via `_compute_step_idempotency_key(run_idempotency_key, 0)`). So MANAGED_AGENTS-orchestrator IS CP-only-safe (the advisor's conditional "if both hold → bundle" satisfied) — bundled + witnessed by a real crash-resume MANAGED_AGENTS recovery.
      (4) Nested HIERARCHICAL hash drop MUST be recursive (the #728 [P1] was a top-level-only strip). The new carrier is a PauseSnapshot-level field, so it nests inside `paused_child_branches[].child_snapshot`; extended the already-recursive `_strip_default_fanout_resume_fields` to drop it when None (safe for a brand-new field — can only RESTORE byte-compat). Witnessed (nested-strip + top-level absent-carrier hash-equivalence).
  - direct grounding (presence-not-correctness) — the orchestrator-workers + effect-fence code surfaces re-grounded by direct read: `_execute_orchestrator_workers` (the orchestrator dispatch + its except + the `_finish` PAUSED shape); the worker `_OrchestratorFenceAmbiguousDispatcher` real-fence test pattern (name-matched test-local `EffectFenceAmbiguousUncommittedError`); the `_ef_resume_ctx` peek-gate (gated on `_recovered_effect_fence_paused` — found the orchestrator-resume hole by direct read: an orchestrator pause carries no worker fence branches → the gate left the orchestrator resolution unread → extended the gate). The 3 initially-failing resume witnesses (re-pause instead of resolve) surfaced exactly this hole — a real bug the witnesses caught, fixed + re-verified.
  - Explore sub-agent — mapped the orchestrator dispatch path, the maybe-ran resolution, the effect-fence ambiguous-pause machinery, the worker analogue carriers, and the orchestrator step-kind reachability; ALL load-bearing claims re-grounded by direct read (the #746 lesson).
  - out-of-family Codex (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`) — caught THREE genuine bugs the advisor + I missed, all FIXED + witnessed: **[P1]** a renamed-same-kind orchestrator re-dispatched into a DIFFERENT fence key (the key includes step_id) → double-fire (FIX: a same-step_id guard in the fence-recoverable leg + the `orchestrator_dispatched_step_id` store reader); **[P2-a]** a synthesis-bearing orchestrator fence pause was rejected by `_synthesis_resume_material_diff` (captured identity `None` vs a present resumed synthesis → false "removed" diff) → unresumable (FIX: skip the diff when `orchestrator_effect_fence_resume` is set — nothing ran, everything re-dispatches fresh; SAFE because the carrier is never co-set with `fan_out_resume`); **[P2-b, codex-vs-main]** an orchestrator fence resume with the body changed to `[]` hit the empty-steps SUCCESS fast path BEFORE the changed-orchestrator guard → silently abandoned the ambiguous effect + the operator resolution (FIX: an empty-body fail-closed guard). Each fix has a by-execution witness. The advisor pre-done also flagged that round-1's `codex-review-uncommitted` reviewed an empty diff post-commit → re-run as `codex-review` (branch-vs-main), which surfaced [P2-b].
  - by-execution witnesses (harness-cp): real-fence orchestrator-self-fence pause → the `orchestrator_effect_fence_resume` carrier captured (key + step_id + step_kind) + hash-valid + round-trip + no `fan_out_resume` (nothing ran); resume RE_FIRE → orchestrator re-dispatches WITH the key-bound directive → SUCCESS; resume ABORT → terminal FAILED (not a re-pause); resume SKIP_AS_FIRED → rejected FAILED, orchestrator NOT re-dispatched; changed-orchestrator (step_kind) → fail closed; no-protocol → fall-through FAILED; crash-resume TOOL_STEP orchestrator maybe-ran → recovers (re-dispatches into the fence); crash-resume MANAGED_AGENTS orchestrator maybe-ran → recovers; cross-kind swap (TOOL_STEP marker, MANAGED_AGENTS resumed) → fail closed; nested HIERARCHICAL byte-compat strip; top-level absent-carrier hash-equivalence. Full suite: harness-cp 1346 passed / 1 xfailed; harness-runtime 2129 passed / 23 skipped / 1 xfailed; harness-cxa 28 passed; pyright 0/0/0; ruff clean.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.69`

v1.69 is a **bundled-absorption** delta over v1.68 (co-published with `harness-cp` impl + tests + a mechanical `harness-runtime` durable-subclass kwarg forward) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING`, as the **direct-fence (TOOL_STEP + MANAGED_AGENTS) slice**. CP-ONLY at the spec level (NO runtime SPEC delta — the §14.22 tool fence + the §14.20 managed-agents fence + the `EffectFenceResolution` consumption already exist; the orchestrator threads the existing `StepExecutionContext.effect_fence_resolution`).

- **§1 — the fence-recoverable orchestrator-maybe-ran relaxation (crash-resume half).** `_determine_fanout_resume` no longer fails closed a maybe-ran orchestrator of a DIRECT-fence kind (TOOL_STEP / MANAGED_AGENTS) — it returns `None` (re-run fresh) so the orchestrator re-dispatches into the auto-active fence (at-most-once at the sink). The same-kind guard (marker kind == resumed kind, both recoverable) closes the changed-kind silent-abandonment hole. SUB_AGENT_DISPATCH / un-kinded / orphaned stay fail-closed.
- **§2 — the §26.2 pause + the NEW resume→orchestrator application site (ambiguous half).** The orchestrator dispatch composes a §26.2 `EFFECT_FENCE_AMBIGUOUS` pause carrying the new `OrchestratorEffectFencePausedResumeState` (a 6th top-level resume carrier) when protocol-bound + pause-resumable + strict-tier; else FAILED fall-through. On resume the orchestrator re-dispatches WITH the operator's key-bound `EffectFenceResolution` threaded onto its context (RE_FIRE → re-fire; ABORT → FAILED; SKIP_AS_FIRED → rejected loud, the orchestrator output structures the aggregate).
- **§3 — decompose-at-open.** Closes the direct-fence slice + registers `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT` (the recursively-fenced SUB_AGENT_DISPATCH orchestrator — fenced at the child's tool sinks, the worker-SUBAGENT mechanism shape at the orchestrator dispatch site) → `standalone_registered` 6 → 6 (net-zero).

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change. At-most-once is PRESERVED — the fence makes every re-dispatch at-most-once at the sink; the same-kind + changed-orchestrator guards close the silent-abandonment hole; SKIP_AS_FIRED is rejected for an orchestrator. The §26.2 PauseSnapshot envelope + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY (the carrier is an additive 6th optional resume field). No §5.2 IS-hash / §16.5 key change.
- **NO runtime SPEC delta.** The only `harness-runtime` edit is the durable `PauseResumeProtocol` subclass override signature, mechanically synced to forward the additive `orchestrator_effect_fence_resume` kwarg (else a durable-config capture drops the carrier / raises `TypeError`) — impl-to-cleared-spec plumbing, not a C-RT contract change.
- **Two new free-text fail-classes** (`fan-out-orchestrator-effect-fence-resume-changed-orchestrator` + `…-skip-as-fired-unsupported`) on `RunResult.fail_class` — NOT the closed `ValidatorRetryExitClass` taxonomy.
- **The direct-fence cut.** TOOL_STEP + MANAGED_AGENTS orchestrators are LEAF effects (full result-fidelity by construction — the #746 recursive-child trap does not transfer). A SUB_AGENT_DISPATCH orchestrator (fenced only at its child sub-workflow's tool sinks) STAYS fail-closed in this slice → the registered `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT` follow-on.
- **Verification provenance (read-verified parity, advisor pre-done).** The orchestrator introduces NO new fence logic — it reaches the SAME runtime tool / managed-agents fence the worker path uses, via the SAME dispatcher (`run_engine_class` set, key = `orchestrator_idempotency_key`, deterministic — verified by read). The fence's at-most-once BEHAVIOR is proven by the runtime `test_effect_fence.py` / `test_managed_agents_dispatch_effect_fence.py` + the worker real-fence witnesses; the CP "real-fence" witnesses here exercise the REAL `_execute_orchestrator_workers` driver path with the fence error name-matched (the established worker convention), NOT the real runtime fence reaching the orchestrator. A real-runtime-fence orchestrator crash-resume e2e is the read-verified-parity residual (no new fence behavior to witness — only the structural same-dispatcher reach).
