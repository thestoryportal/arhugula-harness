---
artifact: design-substrate/Spec_Control_Plane_v1_83.md
version: v1.83
cleared_at: 2026-06-27T14:30:00-06:00
clearance_type: phase-7-bundled-absorption
back_reference:
  - .harness/arc-ledger.yaml
merge_commit: (pending — co-landed in this arc's PR)
reviewer_chain:
  - advisor full-transcript (BEFORE substantive work — the arc pick + decomposition; the LOAD-BEARING reversal of the "witness-and-relax, correct at all depths by construction" framing after the by-execution witness surfaced that the recursive predicate ALONE is a double-fire regression [a LINEAR child's sequential grandchild dispatch got no seed → fresh uuid → re-fire]; the completeness ARGUMENT for the at-most-once identity tuple [convergence ≠ completeness — the tuple is complete by construction over (a) run_id-deriving fields + (b) the recovery-mechanism-selecting field, NOT by Codex-exhaustion]; the honest orchestrator-composition narrative [the orchestrator close rests on #777 outer seed + the parent-agnostic depth-2 witness, NOT a dedicated orchestrator e2e])
  - by-execution witnesses (the recursive classifier verdict flip [nested-recoverable→True RED-without-fix + fan-out/PURE_PATTERN/MANAGED grandchild → False controls + depth-3]; the CP↔runtime agreement incl. partially-malformed-nested parity; the depth-2 crash-resume `test_maybe_ran_nonleaf_child_grandchild_auto_resumes_at_most_once` [real compose_child_workflow_runner → execute_workflow × RuntimeSubAgentDispatcher; grandchild auto-resumes committed prefix, only in-flight step re-dispatched — RED→GREEN on the seed, the double-fire fix]; resumed-side stale-verdict + full-identity edit-detection [engine/step_id/ordinal/workflow_id] + linear-seed step_id+engine binding)
  - out-of-family Codex on the diff (`just codex-review` --base, 6 genuine at-most-once findings across rounds 1-6, all fixed, CONVERGED round 7: recursive engine signature; CP↔runtime parity → one source of truth; step_id/ordinal/workflow_id signature dimensions; JSON-unambiguous encoding; linear-seed step_id+engine binding)
supersedes:
superseded_by:
---

# Clearance — `Spec_Control_Plane v1.83`

v1.83 records the CP half of the R-FS-1 standalone arcs `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` (worker) + `…-ORCHESTRATOR-…-NONLEAF-CHILD` — the **case-(b) [nested SUB_AGENT grandchild]** slice (C-CP-25 §25.15). The `_subagent_child_recoverable` predicate's conjunct 3 (the leaf condition) is RELAXED to a RECURSIVE one: a nested `SUB_AGENT_DISPATCH` child step is admitted IFF it is ITSELF recoverable (the same predicate on the grandchild payload, bottoming out at a LINEAR leaf); `MANAGED_AGENTS` stays hard-excluded. Paired with runtime spec change-note v1.88 → v1.89.

What was reviewed: the predicate ALONE is a double-fire regression (the surfaced finding). The recursive predicate makes the parent CLASSIFY a child-with-recoverable-grandchild as recoverable → it re-dispatches the child. But the prior `child_run_id_seed` was gated only to fan-out workers + orchestrators; a LINEAR child's SEQUENTIAL nested grandchild dispatch (at the SINGLE_THREADED_LINEAR inline loop) got no seed → a fresh uuid → on re-dispatch the grandchild re-runs fresh → its committed effects double-fire. The COMPANION fix is a third seed surface, gated on a NEW hash-inert `StepExecutionContext.is_linear_sequential_dispatch` flag (the LINEAR inline loop is once-per-run; `resume_at` advances forward; the EVALUATOR_OPTIMIZER step-index-reuse hazard lives in `_execute_evaluator_optimizer`, which never reaches the linear loop). The predicate + the seed co-land; the predicate without the seed is incorrect.

The recursive at-most-once identity guard — COMPLETE BY CONSTRUCTION (the load-bearing argument). The at-most-once-relevant identity is exactly (a) the fields the grandchild run_id derives from — `compose_child_run_id_seed`'s `parent_idempotency_key` IS the child-step-index key (→ ordinal), `child_workflow_id`, and the disambiguator `branch_path = "linear-step:" + json([step_id, engine_class])` → {ordinal, step_id, child_workflow_id, engine_class} — and (b) the field that selects the recovery mechanism — {engine_class}. The CP marker (`_subagent_child_engine_class` → the store's `child_engine_class` field, compared at the dual gate) is made a RECURSIVE signature capturing exactly `[ordinal, step_id, child_workflow_id, recursive_sig]` per nested grandchild (JSON-encoded — no delimiter collision). Every grandchild-payload field NOT in the tuple (`workload_class`, `persona_tier`, `layer_budgets`, `fallback_chain`, `per_step_overrides`, `hitl_placements`, `brief`) neither derives the run_id nor selects the recovery mechanism → editing it changes how REMAINING steps run, committed prefix auto-resumed not re-fired (normal resume-after-edit). `topology_pattern` is verdict-enforced LINEAR. So the tuple is complete over the at-most-once-relevant identity — any identity-changing edit changes BOTH the seed AND the marker → dual gate fails closed → no double-fire. This argument converts "Codex went quiet at round 7" into "provably at-most-once."

ONE SOURCE OF TRUTH (CP↔runtime parity, out-of-family Codex [P1]): the recursive decision lives in the single shared `payload_child_recoverable`; the CP entry + the runtime composer's nested recursion both delegate to it. A runtime-only `model_validate` of a nested payload would reject what the CP defensive read (which cannot model_validate — a forbidden harness_cp→harness_runtime import) admits → CP-True/runtime-False → marker admits re-dispatch with no seed → double-fire. The shared predicate eliminates the drift class; the agreement witness (incl. partially-malformed nested) enforces it.

Case-(a) [the child is itself FAN-OUT] stays fail-closed → REGISTERED, not folded (`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-FANOUT-CHILD`): a fan-out child's final_state aggregate reconstruction is a separate LINEAR-excluded mechanism (the `reconstruct_final_state` seed site is SINGLE_THREADED_LINEAR-only; the B-FANOUT-OUTPUT-REPLAY family reconstructs a fan-out aggregate through the parent barrier — unwitnessed). Empirically probed at open → genuinely unwired.

Witness composition (honest): the case-(b) close is parent-agnostic (the new code is entirely at the child→grandchild seam; the worker/orchestrator difference lives only in the unchanged outer seam #774/#777). The depth-2 witness drives the real runner through NEITHER outer dispatch → both arcs sit at the same witness level → closing both is honest. The orchestrator close rests on COMPOSITION (#777 outer seed + the parent-agnostic depth-2 witness), NOT a dedicated orchestrator→child→grandchild e2e — each link independently witnessed (not a half-proof).

Spec-vs-fork: bundled-absorption amendment, NOT X-AL-3. §25.15 v1.77 §3 framed the leaf condition as a witnessed slice and named the deeper recursion as a registered follow-on (a fail-closed gap, NOT a committed closed predicate). The at-most-once invariant is committed; this delta completes the mechanism honoring it (impl-to-cleared-invariant). NO §5.2 IS-hash change (`is_linear_sequential_dispatch` hash-inert; the recursive `child_engine_class` marker non-attested); NO new contract / ADR / enum / fail-class / Protocol widening / CXA edge. `closure_gate.py` G1.1 was `3 + 0`; after close 2 (worker + orchestrator case-b, shared mechanism) + register 1 (`…-FANOUT-CHILD`), `standalone_registered` is `3 → 2` → R-FS-1 stays ACTIVE.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
