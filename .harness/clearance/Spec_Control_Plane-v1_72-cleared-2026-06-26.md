---
artifact: design-substrate/Spec_Control_Plane_v1_72.md
version: v1.72
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_71.md §2 (registered this arc: "B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID … the sound mechanism needs the PauseSnapshot to CARRY the maybe-fired effect's fence key (idempotency_key, step_id) per omitted fence-recoverable ordinal (a new resume carrier, mirroring the orchestrator's OrchestratorEffectFencePausedResumeState) … operator_gated: false") — this delta REFINES that anticipation: the WORKER carrier EffectFencePausedBranchResumeState ALREADY exists and IS the mirror (no new carrier).
  - design-substrate/Spec_Control_Plane_v1_65.md (the v1.65 B-FANOUT-EFFECT-FENCE-BRANCH-PAUSE carrier EffectFencePausedBranchResumeState + the PARALLELIZATION/ORCHESTRATOR_WORKERS resume material-diff guard this delta REUSES verbatim) + design-substrate/Spec_Control_Plane_v1_69.md (the orchestrator OrchestratorEffectFencePausedResumeState — the analogue whose docstring names the worker carrier as the original)
  - design-substrate/Spec_Harness_Runtime_v1.md v1.83 (the paired runtime delta — the `dispatched_branch_step_ids` read accessor over the already-recorded marker step_id) + v1.80 (the `dispatched_branch_kinds` sibling reader this mirrors)
  - design-substrate/Spec_Control_Plane_v1_32.md §25.15.1/§25.15.2 (the PAUSE finish-in-flight + obl-5/obl-7 re-dispatch semantics)
  - harness-cp/src/harness_cp/workflow_driver.py (the reconstruct-gate three-way split via `_fence_unrecoverable_maybe_ran_indices` + the `_crash_pause_reconstruct_fence_paused` carrier build [idempotency_key=""] + the strategy thread + the THREE reconstruct-snapshot `effect_fence_paused_branches` appends [peer PeerFanOutResumeState ~6732 + orchestrator-workers EARLY `_reestablish_fan_out_resume` ~8553 (the ACTUAL reconstruct path — branch_plan is empty → the `not branch_plan` short-circuit, the Codex [P1] fix site) + the worker-failed pause-site FanOutResumeState ~9230 (dead for reconstruct, `()` — kept for symmetry)] + the #742 step_id conjunct INSIDE `_fence_unrecoverable_maybe_ran_indices` + the `_resumed_branch_step_ids_by_ordinal` helper)
  - harness-runtime/src/harness_runtime/lifecycle/engine_output_store.py (the `dispatched_branch_step_ids` reader)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID registered → closed as a WHOLE close; registers nothing; snapshot standalone_closed 52→53, standalone_registered 5→4)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, BEFORE substantive work + a RECONCILE call after the derivability finding):
      (1) BLOCKING reuse insight — verify the WORKER carrier EffectFencePausedBranchResumeState already exists before authoring a new one (the orchestrator needed a new top-level carrier ONLY because steps[0] runs before any branch exists; that reason does not apply to a worker). CONFIRMED at pause_resume_protocol_types.py:743 — the worker carrier holds (branch_index, step_id, step_kind, idempotency_key) + a working resume path. Overturns v1.71 §2's "new carrier" anticipation.
      (2) BLOCKING co-existence gate — one reconstructed snapshot must carry BOTH the no-dispatch omit (re-fire-safe + not-yet-dispatched) AND effect_fence_paused_branches (fence-recoverable). CONFIRMED: disjoint, additive, bounded by branch_count; the reconstruct snapshot construction already builds effect_fence_paused_branches (appended the threaded carriers).
      (3) BLOCKING peer-path guard — the reconstruct gate feeds BOTH _execute_parallelization AND _execute_orchestrator_workers; the changed-step_id guard must be present on BOTH resume paths or PARALLELIZATION fence-recoverable ordinals re-dispatch unprotected. CONFIRMED at workflow_driver.py:5711/5724 (peer) + 7738/7744 (orchestrator-workers).
      (4) RECONCILE after the derivability finding — CP cannot derive the runtime fence key (it composes the opaque tool_id, runtime §14.9.7/§14.22; X-AL axis isolation). So idempotency_key="" — but this is NOT a degradation (the operator is never asked at reconstruct time → no resolution to key-bind on the first resume; the first resume re-dispatches FRESH into the auto-active fence under the runtime-computed key, re-pauses with the REAL key if ambiguous). At-most-once rides on the durable fsynced reserve + the step_id/kind guard (derivable from the marker), never the carried key. The GUARD is not vacuous; the key-bind is eventually-correct.
      (5) The changed-step_id witness MUST change the step_id — a same-step_id witness hid this bug in #752 AND #756. Directed the changed-step_id / changed-kind / TOOL⇄MANAGED-swap fail-closed witnesses + a same-step_id POSITIVE CONTROL + MANAGED_AGENTS coverage. Directed #742 step_id conjunct INSIDE the SoT classifier (passes trivially at reconstruct; bites crash-time edited-manifest).
  - out-of-family Codex — diff review (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): **[P1] caught** the ORCHESTRATOR_WORKERS / HIERARCHICAL gap — in reconstruct-no-dispatch mode EVERY worker is skipped → empty branch_plan → `_execute_orchestrator_workers` returns from the EARLY `not branch_plan` re-establish block (the `_reestablish_fan_out_resume` FanOutResumeState at workflow_driver.py:8553), BEFORE the worker-failed pause site (9230) where the carrier was first appended → the fence-recoverable worker was DROPPED from `effect_fence_paused_branches` for orchestrator-workers, re-opening the changed-step_id double-fire for that topology. The PARALLELIZATION-only witnesses passed while the orchestrator-workers path had the hole (the decorrelation catch). FIXED: thread `crash_pause_reconstruct_fence_paused` into the EARLY re-establish FanOutResumeState too + add the ORCHESTRATOR_WORKERS by-execution witness (`test_crash_resume_pause_orchestrator_maybe_ran_fence_recoverable_reconstructs_paused` — asserts `fr.effect_fence_paused_branches` carries worker-2 + the changed-step_id resume → FAILED). Re-review of the reworked diff owed pre-merge.
  - direct grounding: the worker carrier EffectFencePausedBranchResumeState (pause_resume_protocol_types.py:743) + both resume material-diff guards (peer 5702-5730, orchestrator-workers 7726-7744) + the runtime fence-key composer (effect_fence.py:208 — composed with the opaque tool_id) + the marker already records step_id (engine_output_store.py:379, "for parity").
  - by-execution witnesses (harness-cp test_workflow_driver_fanout_output_replay_full_chain.py): a fence-recoverable TOOL_STEP maybe-ran reconstructs PAUSED CARRYING it in effect_fence_paused_branches (step_id/kind, idempotency_key=""); MANAGED_AGENTS reconstructs identically; the same-step_id POSITIVE CONTROL re-dispatches EXACTLY the fence-paused ordinal → PARTIAL; the CHANGED-step_id resume → FAILED effect-fence-paused-identity-mismatch, no re-dispatch (the Codex [P1] witness); the changed-kind + TOOL⇄MANAGED-swap resume → FAILED effect-fence-paused-kind-changed; the #742 crash-time changed-step_id → FAILED, no double-fire; 3 new classifier units (test_workflow_driver_managed_agents_fence_classifier.py); the runtime dispatched_branch_step_ids round-trip + torn-marker None (test_engine_output_store.py). Full suites: harness-cp 1371 passed / 1 xfailed; harness-runtime + harness-cxa 2159 passed / 23 skipped (credential-gated live-e2e) / 1 xfailed; pyright 0/0/0; ruff clean; overlay-check 31/31.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.72`

v1.72 is a **bundled-absorption** delta over v1.71 (co-published with `harness-cp` + `harness-runtime` impl + tests) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID` as a **WHOLE close**. Paired runtime delta v1.82 → v1.83 (a thin `dispatched_branch_step_ids` read accessor over the already-recorded marker step_id).

- **§1 — the fence-recoverable PAUSE-reconstruct lift.** The `PAUSE ∧ _crash_pause_trigger ∧ ¬_crash_complete` crash-resume leg now ALSO recovers the FENCE-RECOVERABLE (TOOL_STEP/MANAGED_AGENTS) maybe-ran ordinals: `_instrumented ∧ ¬_fence_unrecoverable` → set `_crash_pause_reconstruct_no_dispatch` (re-fire-safe + not-yet-dispatched OMITTED) + build `_crash_pause_reconstruct_fence_paused` (the fence-recoverable ordinals CARRIED as `EffectFencePausedBranchResumeState(step_id, step_kind, idempotency_key="")` appended to the reconstructed snapshot's `effect_fence_paused_branches`). `api.resume` re-dispatches the carried ordinals THROUGH the EXISTING v1.65 resume material-diff guard (changed-step_id / changed-kind → fail closed) + the auto-active fence. NO new model, NO new snapshot field, NO new resume guard — the v1.65 worker carrier IS the mirror v1.71 §2 anticipated.
- **§2 — the #742 crash-time fold-in.** `_fence_unrecoverable_maybe_ran_indices` (the SoT classifier) gains a marker-step_id == resumed-step_id conjunct (fed by the new runtime reader + `_resumed_branch_step_ids_by_ordinal`). Passes trivially at reconstruct (same manifest); bites only the crash-time edited-manifest case.
- **§3 — R-FS-1 closure semantics.** WHOLE close, registers nothing → G1.1 = 4 + 0 → R-FS-1 stays ACTIVE (4 follow-ons remain: MAYBE-RAN-SUBAGENT, EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT [operator-gated], ORCHESTRATOR-MAYBE-RAN-SUBAGENT, ORCHESTRATOR-PROCEED-RESIDUAL [operator-gated]).

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change / new fail-class / new field. At-most-once is PRESERVED — the durable fsynced fence reserve + the resume-side step_id/kind guard make every re-dispatch at-most-once at the tool sink; the carried `idempotency_key=""` never auto-fires. The §26.2 PauseSnapshot envelope + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **The carrier reuse (the grounding correction to v1.71 §2).** v1.71 §2 anticipated a NEW resume carrier mirroring the orchestrator's. Grounding showed the WORKER carrier `EffectFencePausedBranchResumeState` already IS that mirror (the orchestrator carrier is its analogue, needed only because steps[0] runs before any branch exists). So the fix REUSES the v1.65 machinery — no new model/field/guard.
- **idempotency_key="" is eventually-correct, not a degradation.** CP cannot derive the runtime fence key (opaque tool_id, X-AL isolation). The operator is never asked at reconstruct time, so there is no resolution to key-bind on the first resume; the first resume re-dispatches FRESH into the auto-active fence (runtime-computed key), which re-pauses with the REAL key (captured off the runtime error) if still ambiguous → a subsequent resume key-binds. The GUARD (on step_id/kind, derivable from the marker) is what forecloses the double-fire.
- **Paired runtime delta v1.83** — a read accessor for an already-recorded field (the marker `step_id` has been persisted since v1.55). No new field; change-note-level.
