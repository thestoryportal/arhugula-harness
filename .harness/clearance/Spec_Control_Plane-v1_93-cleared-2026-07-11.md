---
artifact: design-substrate/Spec_Control_Plane_v1_93.md
version: v1.93
cleared_at: 2026-07-11T00:00:00-07:00
clearance_type: Phase-7-absorbed-via-fork-doc
back_reference:
  - .harness/b18-fence-ledger-fidelity-ow-design-decision-record.md (pre-build DDR, review-folded)
  - Spec_Control_Plane_v1_92.md item 7 (the registered charter this arc discharges)
  - B-18-FENCE-LEDGER-FIDELITY-OW (arc-ledger)
merge_commit: pending (marker rides the bundled-absorption PR)
reviewer_chain:
  - Fable-5 adversarial pre-build DESIGN review (fresh agent; advisor unavailable + Codex TLS-blocked in bg session — standing fallback ladder per [[fable5-fallback-reviewer]]) — VERDICT AMEND, 0 blocking / 4 concern / 3 cosmetic; the E1–E7 exit enumeration independently re-derived line-exact and confirmed complete for the worker-fan-out region; every capture decision verified at the crash-gate code (a store `completed` capture flips marker-present/no-terminal → recovered-terminal, so capture-less is load-bearing for the aborted/recovered-fence/paused-child arms); aborted-∈-recovered grounded BY CONSTRUCTION (the ABORT directive is built from the recovered dict); findings folded pre-build — (1) E3 reachability corrected to {1,4,6} + witness OW10 + the pre-existing PROCEED paused-child × deadline PARTIAL composition named; (2) the E5 "structurally empty" race pin re-worded to the committed v1.91 item-3 race-reachable characterization; (3) the reconstruct-no-dispatch × protocol-unbound zero-footprint residual (SHARED with the parallelization sibling, whose scan call is vacuous over the empty plan there) NAMED + REGISTERED (`B-18-FENCE-LEDGER-RECONSTRUCT-RESIDUAL`) rather than silently absorbed or charter-widened; (4) invariant-3 re-scoped to honest capture-arm reachability (E1/E7 live, E5 race, E6 direct-call); (5–7) witness mechanics named / cite fix / E1 tier wording. Reviewer CONCURRED with the E6-include judgment (uniform terminal-exit discipline; defence-in-depth labeled).
  - Build-time empirical corrections (folded into DDR + spec item 6 + witness file): a sibling's raise never deterministically cancels later task bodies (TaskGroup abort via done-callback — the poisoned ordinal ITSELF is the deterministic zero-footprint branch); `execute_workflow` ignores `pause_snapshot_input` without a bound protocol, so the recovered arms at the pause-guard exits are direct-call-only (witnesses honestly labeled).
  - impl witnesses (9 new OW tests — OW1/OW4/OW5/OW6a/OW6b/OW8a/OW8b/OW9/OW10; harness-cp full suite 1518 green (+9); runtime non-e2e 2360 green; axes 1590 green; changed-file pyright 0/0/0; ruff clean; determinism 3/3 consecutive runs on the OW selection)
  - Fable-5 post-build decorrelated DIFF review (fresh agent, main...HEAD; independent suite re-run: witness file 9/9 + harness-cp 1518 + runtime non-e2e 2360 reproduced green; pyright 0/0/0; ruff clean) — VERDICT **0 BLOCKING / 0 CONCERN / 2 COSMETIC, recommendation MERGE**. All 8 attack points CLEAN: DDR/spec fidelity (all seven call sites verified immediately before their unchanged returns; six arms in committed order; scan-free boundaries verified by absence; the spec item-6 `execute_workflow` gate claim verified at workflow_driver.py:2541; arc-ledger registration TRUE of the diff with `arc_ledger.py --check` OK); E2 regression surface byte-compared against `git show main:` (only deltas = the intended, spec-named union arm + the dead aborted arm at E2); no double-record path (one scan per run; disposition gate; scoped-abort structurally invisible); no step inflation; **non-vacuity verified BY EXECUTION** — the witness file run against main's workflow_driver.py: 7 failed / 2 passed, the 2 passes being the byte-preservation negative controls DESIGNED to pass on main; no typing/closure hazards; spec-delta truth item-by-item; no stale-carry in touched regions (one pre-existing out-of-diff comment at 9011–9020 noted as a future hygiene candidate, not charged to this arc). COSMETIC 1 (three witnesses asserted last-wins dicts rather than exact multisets for synthesized ordinals) fixed in-arc → `_branch_terminal_statuses` exact asserts added at OW6a/OW8a/OW9. COSMETIC 2 (forward-named PR number) resolved by verification — the PR IS #930.
---

# Clearance — `Spec_Control_Plane_v1_93.md`

This delta closes **B-18-FENCE-LEDGER-FIDELITY-OW** — the ORCHESTRATOR_WORKERS counterpart registered at v1.92 item 7. The obligation-4 scan (v1.91 M2 + v1.92 fence arms, PARALLELIZATION-only until now) is factored into an O-W-local closure and runs at **seven** O-W terminal exits — the fence-ABORT bypass fixed, the never-landed deadline/pause-tier exits covered, the CASCADE_CANCEL inline loop preserved byte-equivalent — with **six arms honoring O-W's three disposition classes**: aborted-first (`completed` terminal-ONLY capture-less), fence this-round (`completed` + capture), fence recovered (`completed` capture-less), paused-child this-round AND recovered (`completed` capture-less — the third-class union half; a capture would flip the crash gate's child-recoverable classification and drop the child snapshot's resumability), else `cancelled`.

**The load-bearing clearance facts.** (1) The union arm extends the v1.92 snapshot-carried asymmetry to O-W's third class: a recovered paused-child withheld on a resume round records `completed` (its attempt-1 dispatched — its child paused mid-flight) capture-less, never the marker-contradicting `cancelled`. (2) The crash-visible additions are named, not silent: the fence-this-round capture at the fence-ABORT exit (OW1) and the protocol-not-bound fresh-run exit (OW6b) — the committed v1.65 §1(c) trade per the v1.92 item-4 convention; byte-identical-crash-resume stays scoped to the capture-less arms. (3) The out-of-region residual surfaced by the pre-build review is **registered, not silently absorbed** (`B-18-FENCE-LEDGER-RECONSTRUCT-RESIDUAL`, two-engine scope — the parallelization sibling shares it). (4) HD inherits every site per level with zero HD-specific code.

## Notes

- NO §5.2 IS-hash change (ledger terminal Literal unchanged); NO new contract/enum/fail-class/CXA edge/snapshot schema; runtime spec UNCHANGED; `workflow.step_count` unaffected (terminal-only synthesis; OW1 pins zero STEP_BOUNDARYs on the synthesis round).
- Baseline byte-preserved: the pre-arc O-W suites green unmodified; OW8a pins the CASCADE_CANCEL all-`cancelled` cell byte-exact; PAUSED boundary stays scan-free (OW8b); PARALLELIZATION engine untouched.
- Remaining open B-18 follow-ons: `B-18-EPOCH-PARTITION` (dedicated session) + `B-18-FENCE-LEDGER-RECONSTRUCT-RESIDUAL` (registered this arc).
