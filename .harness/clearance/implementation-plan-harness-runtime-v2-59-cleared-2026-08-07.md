---
artifact: design-substrate/Implementation_Plan_Harness_Runtime_v2_59.md
version: v2.59
cleared_at: 2026-08-07T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-register-disposition-ratification (B-111 disposition (a); implementation-planner plan-text-correction pass per CLAUDE.md §4.3 + §4.5)
back_reference:
  - .harness/forward-register.yaml row `B-111` (RATIFIED 2026-08-07 — disposition (a): narrow AC #15's no-removal clause, then build the prefix-scoped cleanup)
  - .harness/post-phase-8-forward-register.md `### B-111 · …` (prose home, including the 2026-08-07 window-correction grounding pass that recommended (a) on plan-text-correctness grounds)
  - PR #1237 (the U-RT-150 impl leg this defect was surfaced against, DECLINED with a grounded pin and registered rather than absorbed)
  - design-substrate/Implementation_Plan_Harness_Runtime_v2_58.md (predecessor — the unit whose AC #15 is qualified here)
merge_commit: pending (pre-merge at filing time; B-111 plan leg PR)
reviewer_chain:
  - operator ratification (2026-08-07) — disposition (a)
  - implementation-planner plan-text-correction pass (2026-08-07) — every code anchor re-resolved BY CONTENT at HEAD 65a56e39; Runtime spec v1.111 §14.8.11.1 carrier paragraph re-read at session time to determine that NO spec delta is owed; the existing AC #15 witness read in full to determine exactly which assertion group the disposition flips
  - out-of-family review to convergence on the landing PR (recorded at the PR thread)
supersedes: implementation-plan-harness-runtime-v2-58-cleared-2026-08-05.md
---

# Clearance — Implementation_Plan_Harness_Runtime v2.59 (`B-111` disposition (a))

v2.58 → v2.59 applies **operator-ratified `B-111` disposition (a)** (2026-08-07) as **ONE amendment site plus ONE new unit**. **U-RT-150's AC #15** gains an **appended narrow qualifier** scoping its no-removal clause to the **SWEEP's payload-orphan accounting path**, so the record's **own publication path MAY** reclaim **stale leftovers of its own publication-temp prefix**, prefix-scoped by construction and under the locks the publication already holds. **NEW unit U-RT-151** carries the obligation the qualifier makes legal: the ~5-line cleanup mirroring `_publish_atomic`'s `finally: os.unlink(tmp_name)` shape (`protected_result_store.py:857`–`:858`), its witnesses, and **one declared re-pin** of an existing witness. **The Runtime SPEC is UNTOUCHED — v1.111 stands and NO spec delta is owed**; no `C-RT-*` number is minted, no term is added to §14.8.11.1's twelve, and the does-NOT-acquire list is unchanged. **ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact, ZERO cluster, ZERO cross-axis edge, ZERO CXA rows** (aggregate frozen at 111). U-RT-145 / U-RT-148 / U-RT-149 and **every U-RT-150 criterion other than #15** are **PRESERVED VERBATIM**, and AC #15's own body is preserved verbatim with the qualifier **appended** — **no word retracted, reworded or reordered**.

**The deciding argument, and it is plan-text correctness rather than severity.** AC #15's mutation probe states that a post-crash sweep *"must not enumerate, report or remove it"*, while the reason the same AC gives is entirely about **payload classification** — *"count and age accounting, being reported as a candidate, and being unlinked as though it were ciphertext."* The clause is therefore **stronger than its own stated reason**: read literally it also forbids the store's own publication path from reclaiming its own leftovers, and combined with the **disjointness AC #15's first half requires**, that leaves a killed publication's `gc-observations.publishing-*` file reclaimable by **no path at all**. AC #15 as written is thus not merely failing to close `B-111` — **it is the sole reason `B-111` is open**, which is why the impl leg correctly declined and registered rather than absorbing a plan-level contradiction (X-AL-3).

**What was reviewed, and what it determined rather than assumed.** Every code anchor was re-resolved **by content** at HEAD `65a56e39`: `_publish_observation_record` (`:1282`–`:1340`), the `mkstemp` (`:1327`), the **`fsync`-INCLUSIVE** kill window (`:1329`–`:1332`), the `os.replace` (`:1333`), the prefix constant (`:142`), the two sweep globs (`:150`–`:151`) and `_publish_atomic`'s finally-unlink (`:857`–`:858`). Runtime spec **v1.111 §14.8.11.1 *"The observation record's carrier"*** was read in full and **determines the no-spec-delta finding**: it commits the publication mechanism and the lock scope, **explicitly acknowledges the orphan** (*"a failed write leaves only an orphan temporary file"*), states **no disposition** for it, and leaves the record's file name to implementation discretion — so the prohibition `B-111` runs into exists **only in this plan**. The **CXA determination is likewise made rather than assumed**: this delta introduces **no cross-package consumption at all**, so nothing is owed to classify; and **OD is unchanged by reading U-RT-150 AC #12's emission surface** and confirming the cleanup adds nothing to it (`C-OD-05` §5.1 roster 15 → 15). The register row's own **two 2026-08-07 corrections are carried, not smoothed**: reachability is **HIGHER** than registered (the window is `fsync`-inclusive and **correlated** with supervisor kills on slow mounts) and the harm is **NARROWER** than registered (**accounting invisibility**, not capacity — stated against interest).

**Caveats for Phase 7 consumers — read these three before building.** **(1) PLAN LEG ONLY.** No code lands here; **`B-111` stays OPEN** and flips to `closed` only when **U-RT-151** merges. **(2) The qualifier is a PERMISSION, not an obligation, and that is deliberate.** U-RT-150 is **LANDED and CLOSED** (PR #1237); a **MUST** written into AC #15 would make its conjunctive closure criterion **retroactively false** — the exact defect v2.58 §0.3 names. The permission cannot be violated by code that does not exercise it, so U-RT-150 stays closed and conforming, and the **MUST** lives at U-RT-151 per the **`B-97`(a) → U-RT-149** precedent v2.58 itself cites. **(3) ONE witness flip is DECLARED rather than left for discovery.** Exercising the permission makes `test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan`'s `exists()` assertion false; U-RT-151 **AC #2** owes that re-pin explicitly — flipped to a **positive** *removed-by-a-LATER-PUBLICATION* witness that **attributes** the removal to the publication path (absence alone would also pass a widened sweep glob, the defect AC #15 exists to close), while its **two other assertion groups — prefix-disjointness and not-reported — are PRESERVED VERBATIM**. **The qualifier does not cause the flip; exercising it does.** Nothing here weakens the payload-orphan semantics: **no glob widens**, the prefix constant is unchanged, and U-RT-151 **AC #3** asserts the `.tmp-*` class's unchanged classification as a first-class criterion rather than assuming it. **`B-110` is neither closed nor widened** (§1.2) — the publication temporary is never a candidate of either class and never appears in the record's closed two-member content set.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
