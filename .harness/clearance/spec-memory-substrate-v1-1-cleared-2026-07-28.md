---
artifact: design-substrate/Spec_Memory_Substrate_v1.md
version: v1.1
cleared_at: 2026-07-28T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-Class-1-fork-ratification (B-86 spec leg; spec-writer apply pass per CLAUDE.md §4.3 + §4.5)
back_reference:
  - .harness/class_1_fork_b86_memory_scope_provider_family_keying.md (RATIFIED Class 1 fork, filed 2026-07-28 — §5 recommendations Q1 + Q2, §6 drafting targets)
  - .harness/forward-register.yaml — row B-86 (design_substrate_gated at the fork filing; transited to open at this same PR's spec-leg apply, since this leg is what clears the design gate — see the register for live status), with forward rows B-89 + B-90 (both likewise transited to open in this PR)
merge_commit: pending (pre-merge at filing time; B-86 spec-leg apply PR)
reviewer_chain:
  - Opus grounding agent (2026-07-28) — direct code + spec read with file:line evidence; confirmed the defect at HEAD f79dbe85 and the C-MEM-03 / C-MEM-11 / C-MEM-13 spec silence
  - advisor() transcript-aware pass (2026-07-28) — GO plus 4 framing adjustments, including deliberating on the post-B-89 counterfactual rather than today's broken state
  - genuinely-convened council (2026-07-28) — C10 (action-safety / blast-radius) + C3 (state / memory / persistence) co-primary, C6 (model routing) consultant; the Q1 C10↔C3 tension surfaced and probe-resolved in favour of chain-primary keying, with C10's requirement satisfied at the dispatch-side predicate
  - spec-writer apply pass (2026-07-28) — this delta; no decision taken at the apply pass
  - just codex-review round 1 (2026-07-28) — 2 P1 + 1 P2, all fixed same round and all grounded by direct code read before editing: [P1-a] the null-wildcard over-claim (memory_policy._scope_not_broader rejects a null-REQUEST against a non-null record, and runs on the retrieval path, so only the stored-record side is a wildcard) — corrected to the asymmetric reading with the C-MEM-09 anchor; [P1-b] the "ledgered outcome" over-claim inherited from the fork's own §6 wording — reworded to "recorded outcome" with the C-MEM-19 telemetry surface named, plus a new C-MEM-13 recording-surface paragraph splitting the durable-ledger half honestly (dischargeable via the existing `inject` kind where the withholding is a no_memory_access transition; span-only otherwise, a pre-existing C-MEM-08 closed-enum limitation not created by v1.1); [P2-c] closeout staleness — absorbed at the plan side as U-MEM-26 acceptance + verification
  - just codex-review round 2 (2026-07-28) — [P1] interim-window false certification: honest-annotation half applied to the U-MEM-25 evidence packet (three rows scoped to v1 with a `PENDING — U-MEM-26` marker, plus a version-scoping paragraph), red-gate alternative DECLINED on record with CI-discipline rationale at the plan v1.1 change-note; [P2] register transit — B-86 and B-89 flipped `design_substrate_gated` → `open` since this PR clears the design gate
  - just codex-review rounds 3-7 (2026-07-28) — all findings landed at the plan and the evidence packet rather than this spec (R3 R-MEM version scoping + CLAUDE.md §2.4 pointer; R4-R6 the U-MEM-26 write-boundary, dependency, and surface-inventory widenings; R7 the B-90 register transit and marker-body refresh). The spec text itself was unchanged from round 1 through round 8.
  - just codex-review round 9 (2026-07-28) — [P2-a] landed at the plan (redaction/tombstone transition added as write-boundary surface (f) under an explicit LEGACY-REDACTION preserve-verbatim rule, so a central write validator cannot make a legacy raw-key record un-redactable); [P2-b] landed here and at the spec change-note — the `B-86` register status recorded at both sites is now labelled historical-at-filing, since the row transited `design_substrate_gated` → `open` inside this same PR and this marker is the version-binding source later sessions consume
  - just codex-review round 10 (2026-07-28) — two spec wording corrections: [P2-a] the asymmetry rationale had the direction inverted (an unnamed requested partition is a request for BROADER reach, not narrower — which is why the policy predicate refuses it), fixed at the C-MEM-03 subsection and change-note (b); [P2-b] `B-90` "is closed incidentally" relaxed to future/conditional across the change-note, both markers, and the register prose — the repair closes it when U-MEM-26 lands, not at this spec leg
  - just codex-review round 12 (2026-07-28) — TWO CONTRACT-LEVEL findings, which correctly REOPENED the leg after round 11's premature terminal label: [P2-a] the value domain was one-sided (it bound authored record scopes but not retrieval-request / derived-index-query scopes, and since the scope predicates match raw strings a crafted raw-key request reached a legacy raw-key record, collapsing the unretrievability guarantee and the permanent-residual claim) — request-boundary paragraph added to C-MEM-03 mirroring the authoring side's registered-canonicalize / out-of-domain-reject split, with plan acceptance + both-read-layer negative witnesses; [P2-b] the derivation rule over-claimed the composed run scope as authority for EVERY record a run writes — narrowed to automatically-captured records, with promotion / compaction / native-adapter writers authoring their own scopes subject to the value domain, aligning the spec with the write-boundary criterion the plan already assumed
supersedes: Spec_Memory_Substrate_v1-cleared-2026-07-09.md
---

# Clearance — Spec_Memory_Substrate_v1 v1.1 (B-86 spec leg)

v1 → v1.1 amends three C-MEM contracts and leaves the Memory threat model untouched. **C-MEM-03** gains a new subsection fixing `MemoryScope.provider_family`'s value domain (a `ProviderFamily` value — `anthropic`, `openai`, `google`, `local_open_weight` — never a provider key, model identifier, or CLI-profile identifier; non-value identifiers are unretrievable under family-scoped requests; normalization forward-only), the previously-undocumented but load-bearing `null` semantics (a `null` **stored** value is the unpartitioned wildcard and **not** unknown-deny; a `null` **requested** family does not widen access past a family-scoped record — the asymmetry the enforcement predicates in aggregate actually produce, and this field's instance of C-MEM-09's "Injection cannot be broader than the record's scope."), and the run-level derivation rule (composed once at run-scope composition from the fallback chain's primary family binding, never re-derived per dispatch) with its paired writer-side obligation (capture writes under the run's composed record scope and constructs no independent scope). **C-MEM-13** gains a cross-family withholding invariant: on a `standard_memory_tools` dispatch whose candidate family differs from `MemoryScope.provider_family`, the tool schemas and the scope reference must not be exposed, the dispatch proceeds without model-facing memory access, and the withholding is recorded with a named denial reason — harness-authored capture unaffected. **C-MEM-14**'s exposure obligation is qualified against that invariant: a withheld exposure is a **recorded** outcome — carried on the C-MEM-19 memory telemetry surface, with a durable `inject`-kind row owed only where the withholding is realized as a `no_memory_access` transition — not a contract violation.

The amendment is **conformance repair, not design extension**. The threat-model invariant "Retrieval and injection enforce project, workflow, tenant, provider-family, CLI-profile, and visibility scope before ranking." (line `:481` at fork-filing HEAD `f79dbe85`) already mandated the boundary at v1; it never said what the boundary is keyed to, leaving the mandate unfalsifiable at the contract level. v1.1 supplies the value domain, derivation rule, and dispatch-boundary condition that make the cleared invariant checkable, and that invariant itself stands byte-unchanged — the X-AL-3 posture this leg rests on.

Scope discipline: zero new record type, zero new field, zero new enum member, zero change to any ledger, packet, or telemetry shape. C-MEM-01, C-MEM-02, C-MEM-04 through C-MEM-12, the Memory threat model, and C-MEM-15 through C-MEM-20 are preserved verbatim; the C-MEM-03 / C-MEM-13 / C-MEM-14 field shapes, vocabularies, tables, and pre-existing invariants are byte-unchanged, with subsections and invariants appended only.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- The impl leg — the C-MEM-13 withholding guard (fork §5 Q2), the `B-89` writer-side repair, and the incidental `B-90` fold-in — is a separate, not-yet-opened arc, decomposed at `Implementation_Plan_Memory_Substrate_v1.md` v1.1 as U-MEM-26.
- Two items are carried, not discharged, at v1.1: C6's stated limit (family equality is necessary but not sufficient; the within-family local-terminal posture is addressed outside this contract) and C3's open question on promotion eligibility of records captured during a cross-family fallback leg (C-MEM-10 policy territory).
- Closeout-coverage caveat for consumers: the U-MEM-25 evidence packet certifies this family at **v1**. Its C-MEM-03 / C-MEM-13 / C-MEM-14 rows **and** its R-MEM-01 / R-MEM-09 / R-MEM-12 rows now carry an explicit `PENDING — U-MEM-26` annotation for the v1.1 obligations, and the packet's status line is scoped to spec v1 plus plan v1 (U-MEM-01..25). `just memory-closeout-check` stays green because it tests heading-derived id coverage, which v1.1 does not change; making it red for the interim window was considered and DECLINED on main-always-green CI grounds (rationale at the plan v1.1 change-note).
- The predecessor marker `Spec_Memory_Substrate_v1-cleared-2026-07-09.md` recorded the retroactive Phase-7 in-flight absorption of v1; this marker supersedes it for version-currency purposes and does not invalidate it.
- See `.harness/clearance/README.md` for marker discipline.
