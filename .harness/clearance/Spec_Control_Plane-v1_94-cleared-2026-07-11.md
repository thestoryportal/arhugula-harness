---
artifact: design-substrate/Spec_Control_Plane_v1_94.md
version: v1.94
cleared_at: 2026-07-11T00:00:00-07:00
clearance_type: Phase-7-absorbed-via-fork-doc
back_reference:
  - .harness/b18-fence-ledger-reconstruct-residual-design-decision-record.md (pre-build DDR, review-folded, §8 build-notes addendum)
  - Spec_Control_Plane_v1_93.md item 9 (the registered charter this arc discharges)
  - B-18-FENCE-LEDGER-RECONSTRUCT-RESIDUAL (arc-ledger)
merge_commit: pending (marker rides the bundled-absorption PR)
reviewer_chain:
  - Fable-5 adversarial pre-build DESIGN review (fresh agent; advisor unavailable in bg session — standing fallback ladder per [[fable5-fallback-reviewer]]) — VERDICT AMEND, 0 blocking / 5 concern / 2 cosmetic, ALL folded pre-build. Top finding RR-1 was load-bearing: the DDR's class-(c) derivation had to be structurally GATED on `crash_pause_reconstruct_no_dispatch=True` (an ungated domain-minus-recovered arm would synthesize a phantom `cancelled` per live branch at every one of the 12 pre-existing terminal-exit call sites on fresh runs — an audit-only arc becoming a ledger-corrupting one). Also folded: RR-2 per-engine ordinal domains stated explicitly (parallelization `range(len(steps))` / O-W `range(len(worker_steps))`, `step_index=bi+1`); RR-3 two explicit v1.93 supersessions owed in the delta (item 1 seven→EIGHT O-W call sites; item 4's `not branch_plan` scan-free row re-scoped); RR-4 witness crash-construction mechanics section (the v1.70/v1.71 store levers named pre-build); RR-5 RR6-observables pin shape + the NEW RR7 obligation-7 leg (api.resume re-dispatches despite marker-bearing synthesized-`completed`). Reviewer verified clean: arm-(b) reachability at the driver code, the 3-site+HD threading completeness, the closure-def move safety (name-by-name enumeration), protocol-unbound as the ONLY zero-footprint reconstruct exit (the PROCEED gate doubly unreachable; the all-abort return shadowed), and every DDR line-cite at HEAD. All three DDR open questions answered concurring (arm-(b) `completed` capture-less per the v1.91 item-2(d) commitment; ledger-SILENT would recreate the item-9 defect in worse form).
  - impl witnesses (6 new RR tests in the full-chain suite — RR1/RR2/RR3/RR4/RR5/RR6+7 merged; fails-on-main verified BY EXECUTION at a throwaway main worktree: RR1 KeyError on the class-(b) ordinal + RR3 the O-W mirror = 2/6 fail exactly at the defect, 4/6 designed byte-preservation/invariant controls pass); harness-cp full suite 1524 green (+6); runtime non-e2e 2360 green; axes 1590 green; changed-file pyright 0/0/0; ruff clean; overlay 357 nodes 31/31 seams
  - Fable-5 post-build decorrelated DIFF review (fresh agent, main...HEAD; independent re-verification BY EXECUTION: full-chain 137 green, harness-cp 1524 green, pyright 0/0/0, ruff clean, AND the fails-on-main claim re-run at its own throwaway main worktree — RR1/RR3 fail, controls pass, matching the spec's on-main column) — VERDICT **0 blocking / 1 concern / 1 cosmetic, recommendation MERGE**. All 8 attack points CLEAN: gate correctness (in-mode the non-firing exits verified unreachable in code, not comments — PROCEED policy pinned by the identical `d4_tunable` derivation, warm-up gate len<2-guarded, O-W `not branch_plan` every-leg-returns); class math (domains match store ordinal spaces; `terminal_dispositions` = exactly the recovered seed at these exits; carried ∩ refire-safe = ∅ by construction); driver threading (populated only inside the gate branch; immutable defaults protect ~190 direct callers); drain + step accounting (single drain per path; terminal-only writers contribute 0); store inertness (zero store writes, grep-verified + deep-equality-pinned); witnesses non-vacuous; spec-delta truth item-by-item incl. the two supersessions verified honest; comment hygiene (O-W closure body byte-preserves the six pre-existing arms verbatim vs `git show main:`; the obsolete "REGISTERED, not absorbed here" sentence replaced; no stale seven-exits text). CONCERN RR-D1 (the owed in-PR companions — this marker + the arc-ledger row close + dashboard regen) discharged in-PR before merge. COSMETIC RR-D2 (RR5's "gate pin" framing overstated — the gate is actually pinned by the 12 pre-existing scan-site suites) folded: docstring + spec witness-table wording softened to "containment control on the gate-off path".
---

# Clearance — `Spec_Control_Plane_v1_94.md`

This delta closes **B-18-FENCE-LEDGER-RECONSTRUCT-RESIDUAL** — the shared two-engine residual registered at v1.93 item 9. The §25.15.2 obligation-4 scan gains its **reconstruct arms** at the crash-reconstruct × protocol-unbound terminal exit in BOTH fan-out engines: the PARALLELIZATION site's formerly-vacuous scan call now synthesizes over the reconstruct-skipped + carried ordinals, and the O-W `not branch_plan` exit becomes the **eighth** O-W call site (the closure def moved above the block; the six pre-existing arms byte-preserved).

**The load-bearing clearance facts.** (1) **Three classes, not the registered two** — the decompose-at-open discovery: v1.71 admits re-fire-safe maybe-ran ordinals (marker, no capture, DECLARATIVE/INFERENCE) into reconstruct mode with NO carrier; obligation-4's marker-presence discriminator forecloses `cancelled` for them → a NEW threaded `crash_pause_reconstruct_refire_safe` frozenset carrier, `completed` capture-less — alongside carried fence-recoverable (`completed` capture-less, the v1.92 union-arm vocabulary) and not-yet-dispatched (`cancelled`, the M2 baseline). (2) **The arms are structurally gated on reconstruct mode** (the pre-build review's top finding): off-mode they are skipped by the gate, not incidentally empty — the difference between an audit-only arc and a ledger-corrupting one. (3) **The whole synthesis is store-inert** (zero store writes — stronger than v1.92/v1.93, which each named capture additions): a later re-run reconstructs byte-identically; binding the protocol on a later attempt re-establishes PAUSED exactly as before; there are NO named crash-visible additions. (4) The driver's ONE store read stays the sole classification authority — the classification is threaded, never re-derived engine-side.

## Notes

- NO §5.2 IS-hash change (ledger terminal Literal unchanged); NO new contract/enum/fail-class/CXA edge/snapshot schema; runtime spec UNCHANGED; `workflow.step_count`/`steps_executed` byte-unchanged (terminal-only synthesis; the step-entry drain predicate).
- Boundaries stay scan-free: the protocol-BOUND reconstruct leg (RR2/RR4 — snapshot omission IS the re-dispatchable contract), the complete-recovery legs (RR5), the resume-rejection guards, the worker-failure → PAUSED boundary.
- Obligation-7 extended to the new shape by execution: RR7 pins a genuine `api.resume` re-dispatch of the omitted + carried ordinals despite a prior attempt's synthesized terminals.
- Remaining open B-18 follow-on: `B-18-EPOCH-PARTITION` (dedicated session) — now the SOLE registered arc.
