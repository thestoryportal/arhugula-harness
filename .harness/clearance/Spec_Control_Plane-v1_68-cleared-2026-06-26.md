---
artifact: design-substrate/Spec_Control_Plane_v1_68.md
version: v1.68
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_57.md §2 (registered this arc as "a CORRECT reconstruct that honors 'finish in-flight, then pause' — a genuine design problem, not a deferral"; §3 footer line 42: "§25.15.1 + §25.15.2 obl. 7 are IMPLEMENTED for the crash-resume path, not changed" — the impl-to-cleared-spec posture this delta continues)
  - design-substrate/Spec_Control_Plane_v1_32.md §25.15.1 (the PAUSE row "Allowed to finish in-flight, then pause" → PAUSED) + §25.15.2 obl. 5 (high-blast-radius PRE-dispatch gating) + obl. 7 (resume MUST NOT re-dispatch a terminal branch) — the cleared semantics this delta implements for the complete-recovery crash-resume
  - harness-cp/src/harness_cp/workflow_driver.py (the `PAUSE ∧ _crash_degraded` crash-resume leg gated on `not _crash_complete`; the `_crash_pause_reestablish` genuine-failure gate at `_execute_parallelization`; the re-establish in `_execute_orchestrator_workers` `not branch_plan` block; the `_determine_fanout_resume` true-`terminal_status` correction)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT registered → closed + registers 2 follow-ons [NOT-YET-DISPATCHED + MAYBE-RAN]; snapshot standalone_closed 48→49, standalone_registered 5→6)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, ×2 — the load-bearing scoping calls):
      (1) The A-vs-B pick: PAUSE-RECONSTRUCT over ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING (the latter flagged "genuinely larger" — needs a non-existent resume→orchestrator application site). Then Reading A vs B (reconstruct the PAUSED state vs silently finalize PARTIAL): CONFIRMED Reading A — a crash-resume is a FRESH re-execution (`resume_snapshot is None`), NOT the operator's resume; the run was SUPPOSED to be PAUSED, so restoring it preserves the operator's `pause` HITL election (finalizing PARTIAL directly would silently consume it).
      (2) The slice-boundary decomposition: register NOT-YET-DISPATCHED SEPARATELY from MAYBE-RAN — folding the provably-not-run case via the complete-recovery strategy fall-through is UNSOUND, because the live strategy re-dispatches the not-yet-dispatched branches IMMEDIATELY (before the operator's pause-resolution), bypassing the §25.15.2 obl-5 PRE-dispatch blast-radius gate. Registering the tractable sub-case as if it were the hard one is the over-reach trap (`[[grounding-reveals-claude-closeable-slice-close-honestly]]`).
      (3) The fidelity assertion: assert on the snapshot RESUME-CARRIER (`pause_snapshot.<carrier>.branches`), not just `final_state` — the #746-class gap hides in the carrier (`[[verification-shape-sharpened-grep-vs-e2e]]`). Verified the errored trigger branch is represented IDENTICALLY (`terminal_status="completed"`, `output=None`) in both the live pause and the crash reconstruct → the reconstructed snapshot byte-matches.
      (4) The `operator_gated: false` spec-forbids read: confirmed v1.57 §3 says §25.15.1 + obl.7 are IMPLEMENTED not changed → impl-discretion → no operator gate (`[[disposition-label-is-a-claim-verify-against-spec]]`).
  - direct grounding (presence-not-correctness, re-grounded the §25.15.1 spec text after the v1.67 grep came back empty — the delta-only-chain `[[wrong-version-read-of-a-delta-only-baseline]]`): §25.15 is born at CP v1.32, last substantively re-tabled by the cascade-policy arc at v1.57. The dispatch model (branches dispatch concurrently in one TaskGroup) → complete-recovery is the snapshot-write-window EDGE, not the primary pause shape — the spec delta states this truthfully. The live errored branch records `_capture_branch_terminal(..., "completed", output=None)` + `terminal_dispositions[bi]="completed"`, NOT in `collected` (workflow_driver.py:6231-6263) → the crash reconstruct's representation matches.
  - Explore sub-agent — mapped the live fan-out PAUSE→resume path (the RunResult fields, the PeerFanOutResumeState/FanOutResumeState carriers, _determine_fanout_resume, the resume re-entry); ALL load-bearing claims re-grounded by direct read (the #746 lesson — subagent maps need verification; the orchestrator `not branch_plan` short-circuit + the hierarchical→orchestrator delegation were found by direct read, not the map).
  - out-of-family Codex — diff review (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): **[P2] (FIXED, the genuine correctness catch)** — under `fanout_timeout_disposition=RECOVER_AS_TERMINAL`, `_determine_fanout_resume` collapsed a `timed_out` branch into an `output is None` recovered terminal, so the `_crash_degraded`/`output is None` gate would re-establish PAUSED for a recovered TIMEOUT that never lost a pause snapshot (a live timeout is `deadline_struck` → FAILED, never a pause). FIX: `_determine_fanout_resume` preserves the true `terminal_status` (it had hardcoded `"completed"`; every other consumer keys on `output is None` → the change is inert except this discriminator), and BOTH strategy re-establish gates key on `terminal_status == "completed"` + no output (a genuine ran-and-errored FAILURE = the pause trigger), excluding recovered timeouts → a recovered-timeout complete recovery finalizes PARTIAL. Regression-guarded by a by-execution witness. The two [P1]s (missing clearance marker + stale dashboard snapshot) were the expected bundled-absorption governance artifacts, supplied here + by the dashboard regen.
  - by-execution witnesses (harness-cp test_workflow_driver_fanout_output_replay_full_chain.py): complete-recovery reconstructs PAUSED; the RESULT-FIDELITY witness (the reconstruct snapshot's resume-carrier `branches` BYTE-EQUAL a non-crashed live pause's + resuming either yields the identical PARTIAL state — the no-timeout case); incomplete recovery under PAUSE stays fail-closed `pause-trigger-ambiguous`; protocol-not-bound fails honestly; non-degraded complete → SUCCESS; ORCHESTRATOR_WORKERS complete reconstructs PAUSED; re-crash-before-resume is idempotent. **The three timeout permutations (Codex [P2] ×2 + advisor):** (a) a recovered-timeout-only complete recovery finalizes PARTIAL not PAUSED (Codex r1); (b) a timeout + re-fire-safe absent ordinal incomplete recovery RECOVERS (re-dispatch the safe branch + PARTIAL), NOT fail-closed-ambiguous (Codex r2 — the block-level genuine-failure trigger); (c) a genuine failure + recovered timeout complete recovery re-establishes PAUSED with the `timed_out` branch faithfully carried (advisor — the RECOVER_AS_TERMINAL-intended disposition, NOT a byte-match of a live pause, which FAILs on a deadline-cut). Full suite: harness-cp 1335 passed / 1 xfailed; pyright 0/0/0; ruff clean; overlay-check OK 31/31 seams; harness-runtime + harness-cxa green (the shared `_determine_fanout_resume` `terminal_status` edit).
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.68`

v1.68 is a **bundled-absorption** delta over v1.67 (co-published with `harness-cp` impl + tests) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT`, as the **complete-recovery slice**. CP-ONLY (no runtime delta — reuses the §26.2 PauseResumeProtocol + the v1.44 fan-out pause-capture machinery + the §25.12 store verbatim).

- **§1 — the complete-recovery PAUSE reconstruct.** The `PAUSE ∧ _crash_degraded` crash-resume leg (the v1.57 §2 `pause-trigger-ambiguous` fail-closed) splits on `_crash_complete`: complete falls through to the strategy, which RE-ESTABLISHES the lost PAUSED state via the `_crash_pause_reestablish` gate (reusing the live `capture_pause_snapshot` construction → the reconstructed snapshot's `branches` byte-match a real pause); incomplete stays fail-closed. **Reading A**: a crash-resume is a fresh re-execution, NOT the operator's resume → restore the PAUSED the run was supposed to be in, not a silent PARTIAL. The gate keys on a genuine branch FAILURE (`terminal_status == "completed"` + no output), NOT a RECOVER_AS_TERMINAL recovered timeout (Codex [P2]).
- **§2 — the incomplete-recovery residual decomposition.** The incomplete case (an absent ordinal) stays fail-closed and splits into TWO follow-ons: `…-NOT-YET-DISPATCHED` (provably-not-run absent — tractable, but needs an obl-5-respecting re-pause-without-dispatch mode, distinct from the complete slice's fall-through) + `…-MAYBE-RAN` (interrupted-in-flight — the hard ambiguity the v1.57 foreclosure names).
- **§3 — decompose-at-open.** Closes the complete-recovery slice + registers 2 → `standalone_registered` 5 → 6 (net increase — honest FULL-SPEC accounting).

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change / new fail-class. At-most-once is PRESERVED — the complete-recovery slice re-dispatches NOTHING (every branch recovered-terminal + skipped per §25.15.2 obl. 7). §25.15.1 + §25.15.2 obl. 7 are IMPLEMENTED for the complete-recovery crash-resume path, not changed (the v1.57 §3 posture). The §26.2 PauseSnapshot envelope + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **CP-ONLY, no runtime delta** — the reconstruct reuses the existing pause-capture + resume machinery; there is no paired runtime spec change.
- **`_determine_fanout_resume` now preserves the true `terminal_status`** in the recovered carrier (it had collapsed `timed_out` → `"completed"`). Every other consumer keys on `output is None`, so the change is inert except for the PAUSE-reconstruct genuine-failure discriminator (verified: full suite green after the change, zero ripple).
- **The complete-recovery cut is the snapshot-write-window** (branches dispatch concurrently → the crash landed after all siblings finished but before the `PauseSnapshot` write). The common crash shape (interrupted-in-flight branches) is the registered `…-MAYBE-RAN` follow-on; the provably-not-run-absent case is `…-NOT-YET-DISPATCHED`. Both stay fail-closed in this slice.
