---
artifact: design-substrate/Spec_Harness_Runtime_v1.md
version: v1.113
cleared_at: 2026-08-08T00:00:00-06:00
clearance_type: conditional-row-discharge-apply-pass
back_reference:
  - .harness/forward-register.yaml row `B-115` (the MEASURED `B-84` W-5 cell-(3) residual; disposition (b′) recommended at the #1265 grounding, commit `05abb419`, which FALSIFIED reading (b)-as-registered — status `registered_finding` → `closed` at this PR)
  - design-substrate/Spec_Harness_Runtime_v1.md v1.112 §14.6.3 row 6 (the PROSPECTIVE-CONDITIONAL row this leg discharges — *"joins the waiver tuple only after `B-115`'s build confirms determinism; if racy, this row is VOID"*)
  - .harness/council/b116-breaker-semantics/DELIVERABLE.md (the ratified Reading (II) package whose recovery-model normative test governs this row's adjudication) + 02-adversarial/REVIEW-e2.md F-05 (which flagged the ratify-then-verify circularity this leg resolves)
  - design-substrate/Implementation_Plan_Harness_Runtime_v2_61.md U-RT-153 (the plan leg, landing in the SAME commit — see the bundling rationale below)
  - .harness/forward-register.yaml row `B-132` (NARROWED, not closed, at this leg) + row `B-134` (MINTED at this leg — the dedicated C-MEM-19 `ledger_conflict` class question)
  - PR '#pending' (this arc)
merge_commit: pending
reviewer_chain:
  - conditional-row discharge pass — it applies the disposition v1.112 §14.6.3 row 6 ALREADY contracted, subject to a condition, and EXTENDS NOTHING beyond it. The row's own text fixes both branches (waived on confirmed determinism; VOID + C5 reroute if racy); this leg establishes which branch obtains and records the evidence. No new disposition is chosen and no other row moves.
  - empirical grounding pass at this leg — the spec head verified at **v1.112** before editing and the plan head at **v2.60**; the whole `B-115` mechanism re-grounded BY CONTENT at HEAD `1bce5d15` rather than from the row's own snapshot (which predates PR #1271 and understated the classifier's membership): the memory_id derivation re-read (`_memory_id_for`; `capture_tool_event`'s content dict carries `summary_model` and NOT `provider`), the record-append path re-read (`_JSONL_BY_KIND` includes `TOOL_EVENT`; `_append_jsonl` is unconditional), the ledger's single conflict raise site and its 18-field equivalence projections re-read (`provider` is field 11 of 18; `timestamp` is EXCLUDED), and `_classify_provider_exception` re-read as **FIVE** named types + the 401/403 duck-typed arm (the row's snapshot said two memory types; `LLMDispatchPayloadShapeInternalError` was added by PR #1271 after the row was written). The B-84 build-leg record (PR #1243) and the landed W-5 cell-(3) witness were both re-read for the executed fixture shape.
  - determinism confirmation — the load-bearing review item, and the one the council record left UNDEFINED. `"Confirms determinism"` is defined nowhere in §14.6.3 or the DELIVERABLE; this leg supplies a four-part conjunctive definition by analogy to the one witness shape the council DID price, states it at contract altitude so it can be overturned rather than discovered, and discharges it: repeat-invariance (8/8 identical outcomes), candidate-independence (third and fourth candidates refuse identically), state-driven-not-stochastic (the origin candidate's payload resolves as `IDEMPOTENT_NOOP` — the positive control), structural clock-exclusion (the 18-field set pinned by name; source-level assertion that neither projection reads a clock). Witnessed at `harness-is/tests/test_b115_ledger_conflict_determinism.py`, deliberately at the LEDGER so the evidence survives future re-typing of the executor surface. The HONEST BOUNDARY is stated in-contract rather than in a commit message: the refusal is deterministic over the SAME inputs, not permanent under all inputs.
  - scope-widening pass, recorded rather than absorbed — the register row named ONE executor surface; grounding found THREE, and the two it missed escaped RAW onto the retry staircase. Both were proven reachable **BY EXECUTION** before scope was fixed, per the row's own close_out requirement that a code-read inference is not sufficient. The widening stays inside the ratified (b′) direction (same mechanism, more sites) and is flagged in the change-note.
  - build-time correction, recorded rather than absorbed — the impl leg's first discriminator shape (re-raising the ledger exception out of `EpisodicMemoryCapture._capture`, on the `_repair_capture_operation` precedent) was tried and FALSIFIED by the existing suite: `_capture` has SIX public entry points and `FAILED`-on-conflict is a CONTRACTED outcome for at least two of them (U-MEM-26 / Codex R6+R8), each pinned by a landed witness whose docstring states that widening the catch would silently mask the divergence. The precedent does not transfer — `_repair_capture_operation` has ONE caller and no contracted FAILED semantics. The discriminator is carried as a VALUE instead (a closed `MemoryCaptureFailureKind` enum), so every caller's control flow is byte-unchanged and the closed two-value status enum is not widened. The two U-MEM-26 witnesses pass UNMODIFIED, which is AC #9.
  - out-of-family Codex review, round 1 — ONE finding, [P2], REAL against this leg's own AC and FIXED rather than argued: the value-discriminator realization left the capture surface raising an UNCHAINED `MemoryToolExecutionLedgerConflictError` while the two direct-append surfaces chained, so the same substrate event lost its traceback origin depending on which surface caught it. The capture result now retains the ledger exception as `failure_cause` (typed as the concrete conflict type; present IFF `failure_kind is LEDGER_CONFLICT`, enforced both ways by a model validator because the missing-cause direction would degrade the wrapper to `raise ... from None`, which SUPPRESSES the context rather than omitting it) and the wrapper chains from it. `model_config` gains `arbitrary_types_allowed=True` — a field-type permission that weakens neither `extra="forbid"` nor `frozen=True`; the model is never serialized, verified by direct search. The plan AC was STRENGTHENED (chaining on every surface + a joint symmetry witness, because the observed failure mode was two-of-three chaining, which a per-surface witness alone does not catch), not narrowed to fit the implementation.
  - PD-8 mutation probes — SEVEN, all confirmed RED then restored under cp-backup discipline: narrow the tuple by the new member (3 named witnesses fail); broaden to the memory family base (2 fail); de-list from the classifier while keeping the waiver (6 fail, incl. the classifier-consistency assertion this leg added); collapse the capture-boundary discriminator so every FAILED reports STORE_IO (5 fail); re-parent the new type under the store subtype (5 fail, incl. the C-MEM-19 residual witness and both classification-table rows); drop the `from` chain at the capture wrapper (2 fail — the capture-surface witness AND the symmetry witness); drop the `failure_cause` retention (5 fail, loudly at the result model's own validator rather than silently producing an unchained raise).
supersedes: spec-harness-runtime-v1-112-cleared-2026-08-08.md
---

# Clearance — `Spec_Harness_Runtime_v1.md v1.113`

v1.113 discharges the ONE conditional row v1.112 left open. **THREE amendment sites, ALL inside §14.6.3:** (A) row 6 of the per-member disposition table flips *(prospective)* / **WAIVED — CONDITIONAL** to **WAIVED (condition DISCHARGED)**, names the realized type `MemoryToolExecutionLedgerConflictError`, and cites the determinism witness — the table stays SEVEN rows, its split moving 2 count / 4 waived / 1 prospective → **2 count / 5 waived / 0 prospective**; (B) the guard paragraph goes **four-type → five-type**, its brace set gains the member, and it gains the sibling-not-subclass constraint (a subclass of the store subtype would carry `io_failure` back onto this population by MRO lookup and void the C-MEM-19 half of the split) — the BY-NAME rule, the classifier-consistency requirement and the *"changes the CHARGE, never the control flow"* clause are PRESERVED VERBATIM; (C) the declared-residual paragraph gains ONE appended clause recording that **`B-132` is NARROWED, not closed**.

**Why spec + plan + impl land TOGETHER**, departing from the `B-116` / `B-96` / `B-107` split-leg precedent: the bundling is FORCED. Row 6 conditions the contract text on a fact only the build can establish, and C9's binding form is *"never speculatively"*. A spec-first leg would assert the confirmed disposition before the confirming evidence existed — precisely the circularity `REVIEW-e2.md` F-05 flagged. The operative precedent is the bundled `B-116-t3` leg (PR #1272).

**Two v1.112 statements checked and found NOT to fire**, recorded so the absences are decisions: the trip-threshold revisit-trigger binds arcs adding a **provider-attested** fail-fast member, and this member is harness-internal and waived — no threshold revisit is owed; the dead-half-open-latch residual is INHERITED UNCHANGED (`attempt_half_open` still has zero production call sites, so the normative test remains design intent contracted against C-OD-07 §7.1 until `B-118`), restated so the fifth waiver is not read as resting on a latch that does not run.

What was reviewed during clearance: the whole cell-(3) mechanism re-grounded by content at HEAD and re-confirmed by executing the landed W-5 witnesses; the determinism definition authored, stated in-contract, and discharged by executed witness; both raw-escape surfaces proven reachable by execution before scope was fixed; a survey of every non-test `append_memory_operation` caller run so the partition witness is honestly scoped (one further site — `memory_promotion`'s decision append — carries the same shape, is outside this leg's ratified scope, and is recorded in the witness docstring rather than omitted). **ZERO new `C-RT-*` or `C-MEM-*` numbers, ZERO Memory-spec leg (the new type declares no class and inherits the base residual, which C-MEM-19 v1.3 states is conforming — the U-MEM-28 precedent), ZERO CXA rows (aggregate frozen at 111), ZERO OD delta, ZERO CP §3.5 delta, ZERO `snapshot_hash` impact.**

Caveat for Phase 7 consumers: unlike v1.112, this version's waiver IS built — spec, plan and implementation land in the same commit, so §14.6.3's five-member tuple describes shipped behaviour rather than a contracted-but-pending one. The duplicate-RECORD half of `B-84` W-5 cell (3) remains OPEN: a single conflicting attempt still appends a second physical JSONL line, and only the RETRY amplification is closed.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
