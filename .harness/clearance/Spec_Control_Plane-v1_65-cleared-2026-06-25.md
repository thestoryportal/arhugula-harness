---
artifact: design-substrate/Spec_Control_Plane_v1_65.md
version: v1.65
cleared_at: 2026-06-25T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_62.md §1/§3 (the WORKER re-fire-safe maybe-ran split this delta extends — `_refire_unsafe_branch_indices` + the dispatch-time-kind marker + the changed-manifest at-most-once guard the v1.65 changed-KIND guard mirrors)
  - design-substrate/Spec_Control_Plane_v1_51.md §26.2 + v1_52 §26.8 (the LINEAR effect-fence HITL-route + pause-resolution this delta is the fan-out analogue of — the `WorkflowPauseReason.EFFECT_FENCE_AMBIGUOUS` value + `EffectFenceResolution` / `EffectFenceResolutionDirective` / `ResumeContext.effect_fence_resolution` reused verbatim, NO runtime delta)
  - design-substrate/Spec_Harness_Runtime_v1.md §14.22 C-RT-31 (the effect fence whose `EffectFenceAmbiguousUncommittedError` the branch catch name-matches + whose per-(run, step, tool) idempotency_key the tool-swap parity note reasons about) + §14.22.7 v1.67 B-EFFECT-FENCE-DURABLE-AUTO (the auto-activation making the re-dispatched TOOL_STEP re-reach the fence with no opt-in)
  - harness-cp/src/harness_cp/workflow_driver.py (the `_execute_parallelization` + `_execute_orchestrator_workers` effect-fence catch + carrier population + per-branch resume directive threading; `_fence_unrecoverable_maybe_ran_indices` resumed-kind conjunct + `_resumed_branch_kinds_by_ordinal` at both classification sites)
  - harness-cp/src/harness_cp/pause_resume_protocol_types.py (`EffectFencePausedBranchResumeState` + the `effect_fence_paused_branches` field on `FanOutResumeState`/`PeerFanOutResumeState`) + pause_resume_protocol.py (`_strip_default_fanout_resume_fields` drop-when-empty hash)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING registered → closed as the TOOL_STEP slice + the THREE NEW registered follow-ons MAYBE-RAN-SUBAGENT + MAYBE-RAN-UNFENCED-EXTERNAL + EFFECT-FENCE-PER-BRANCH-RESOLUTION; snapshot standalone_closed 45→46, standalone_registered 4→6)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, on the in-flight WIP review — the load-bearing scoping + correctness catch, not just sign-off):
      (1) confirmed the orientation (drift correctly dismissed as benign branch + one-commit lag; the fence auto-active reachability verified; the 5-stale-vs-1-genuine-regression split), and CONFIRMED the central call — the changed-manifest test failure is a CODE bug, not a test to update (keep it asserting FAILED; "just update the assertion" would silently re-absorb the exact v1.62 regression out-of-family Codex caught).
      (2) sharpened the tool-swap question: kind-match closes TOOL→DECLARATIVE but the provably-safe condition is "the re-dispatch reaches the SAME held reserve" = the same fence idempotency_key; the §14.22 key is per-(run, step, tool) → kind-match does not guarantee key-collision under a tool-swap. Decision rule given: cite §14.22 if it explicitly clears tool-swap-under-same-step_id, else match-on-the-key OR register the gap explicitly.
      (3) directed front-loading the real-fence witness (the single load-bearing proof, the riskiest item — whether the name-matched catch fires through the real TaskGroup+shield vs ExceptionGroup-wrapping/shield-swallow), proving BOTH halves (PAUSE + key-bound resume).
  - the fence-reachability grep (verify-by-execution): `_FANOUT_REPLAY_ENGINE_CLASSES = {EVENT_SOURCED_REPLAY, WAL_SEGMENT}` (`workflow_driver.py:696`) ⊂ `_DURABLE_AUTO_FENCE_ENGINE_CLASSES = {SAVE_POINT_CHECKPOINT, EVENT_SOURCED_REPLAY, WAL_SEGMENT, RECONCILER_LOOP}` (`runtime_tool_dispatcher.py:112`) — a fan-out crash-resume runs ONLY on engines where the fence AUTO-ACTIVATES at the tool sink, so a re-dispatched maybe-ran TOOL_STEP DOES re-reach the fence → the relaxation's at-most-once claim is sound.
  - the §14.22 idempotency-key composition read (`runtime_tool_dispatcher.py:886` `_compose_idempotency_key(parent, step.step_id, tool_id)` — the key includes tool_id) + the opaque-payload boundary (`WorkflowStep.step_payload` is opaque to the driver, workflow_driver_types.py:110): this is the decisive architectural finding that PIVOTED the fix from "record tool_id CP-side" (Option A — would couple the driver to the AS/runtime payload schema, breaking axis isolation) to the CP-only resumed-`step_kind` conjunct (Option B — `step_kind` is a typed CP-visible `WorkflowStep` field). §14.22 does NOT explicitly clear tool-swap-under-same-step_id, but the LINEAR effect-fence path structurally has the IDENTICAL behavior (re-dispatch the resumed manifest's step into the fence keyed on tool_id) → tool-swap is documented as ACCEPTED PARITY-with-linear (per-key at-most-once preserved), not a registered gap (per advisor's "cite OR register" rule — it resolves to "cite the cleared linear behavior").
  - the changed-manifest correctness finding (the genuine regression the in-flight WIP introduced): keying fence-recovery on the dispatch-MARKER kind ALONE re-opened the v1.62 changed-manifest hole — a maybe-ran TOOL_STEP re-supplied at the same ordinal as a DECLARATIVE on resume re-dispatched a step reaching NO tool fence → the original tool effect's ambiguity silently abandoned, recovered to SUCCESS. The existing `test_crash_resume_maybe_ran_changed_manifest_kind_fails_closed` was RED for this reason. FIX: fence-recovery requires BOTH the marker kind AND the RESUMED `step_kind` be TOOL_STEP (the `_resumed_branch_kinds_by_ordinal` + `_fence_unrecoverable_maybe_ran_indices` resumed-kind conjunct, at both classification sites). Witnessed by the now-passing changed-manifest test + a NEW cardinality-only changed-kind fail-closed test (the second classification site).
  - the real-fence witnesses (the front-loaded load-bearing proof, BOTH live-composition sites, BOTH halves): `test_peer_branch_effect_fence_ambiguous_composes_through_barrier_to_pause` + `..._resume_threads_key_bound_resolution` (PARALLELIZATION) + `test_orchestrator_worker_effect_fence_ambiguous_composes_through_barrier_to_pause` + `..._resume_threads_key_bound_resolution` (ORCHESTRATOR_WORKERS) — a name-matched `EffectFenceAmbiguousUncommittedError` raised through the REAL `_execute_parallelization` / `_execute_orchestrator_workers` TaskGroup+shield PAUSES with `effect_fence_paused_branches` populated (NOT ExceptionGroup-swallowed), and resume re-enters ONLY the fence-paused branch with the key-bound directive (terminal siblings skipped). Proves the catch composes through the concurrency machinery.
  - out-of-family Codex — diff review at pre-PR (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): <filled after the pre-PR Codex round>.
  - by-execution witnesses: harness-cp 1300 passed / 1 xfailed (incl. the 4 real-fence witnesses + the positive TOOL_STEP-recovers tests at both classification sites + the changed-kind fail-closed guards + the 5 stale tests reworked to MANAGED_AGENTS); harness-runtime 2115 passed / 23 skipped / 1 xfailed; harness-cxa 28 passed; pyright 0/0/0; ruff clean.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.65`

v1.65 is a **bundled-absorption** delta over v1.64 (co-published with `harness-cp` impl + tests; **CP-only, NO runtime delta** — the #738 shape) — the build of `B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING` as the **TOOL_STEP slice**.

- **§1 — the live effect-fence ambiguous-pause composed through the fan-out barrier.** A fan-out branch (PARALLELIZATION peer OR ORCHESTRATOR_WORKERS / HIERARCHICAL worker) whose OWN dispatch raises `EffectFenceAmbiguousUncommittedError` (C-RT-31 §14.22) is captured as a NON-terminal effect-fence-paused disposition (not a `completed` cascade branch) and composes through the worker barrier to a genuine §26.2 `EFFECT_FENCE_AMBIGUOUS` PAUSE carrying the new `effect_fence_paused_branches` carrier; resume re-enters the branch with the operator's key-bound `EffectFenceResolution` (reusing the LINEAR v1.51/v1.52 + runtime v1.72/v1.73 machinery verbatim).
- **§2 — the fence-recoverable TOOL_STEP maybe-ran crash-resume relaxation + the changed-kind guard.** A maybe-ran TOOL_STEP on a strict-tier crash-resume now RECOVERS (re-dispatch re-reaches the auto-active fence → at-most-once at the tool sink) instead of the v1.62 unconditional effect-bearing fail-closed — PROVIDED the RESUMED branch's `step_kind` is ALSO TOOL_STEP (the changed-kind guard, at both the incomplete-recovery + cardinality-only classification sites). SUB_AGENT_DISPATCH / MANAGED_AGENTS stay fail-closed.
- **§3 — honest split accounting.** Grounding the arc at open revealed the effect-bearing kinds split by their fence surface → closes as the TOOL_STEP slice + registers THREE narrower follow-ons (`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` + `B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL` + `B-FANOUT-EFFECT-FENCE-PER-BRANCH-RESOLUTION`) → `standalone_registered` 4 → 6 (a net increase; a split per `[[spec-leg-split-on-ratification-boundary]]`, NOT a defer). G1.1 = 6 + 0 → R-FS-1 stays ACTIVE.

## Caveats for Phase 7 consumers

- Bundled-absorption: additive. NO new contract / closed-enum / committed-invariant change. At-most-once is PRESERVED — a maybe-ran TOOL_STEP re-dispatches ONLY into the auto-active fence (at-most-once at the tool sink) AND only when the resumed branch is STILL a TOOL_STEP (the changed-kind guard); SUB_AGENT / MANAGED stay fail-closed (no fail-open path). §25.12 D1/D1.b + §25.15.1 cascade contract + the v1.55 §1 disposition keystone + the v1.63 timeout-disposition policy + the v1.64 orchestrator re-fire-safety + the C-CP-26 §26.2 PauseSnapshot envelope PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **CP-only — NO runtime spec delta.** The live PAUSE composition reuses the existing LINEAR effect-fence resume contract; the crash-resume fence-recovery reuses the v1.80 `dispatched_branch_kinds` marker; the changed-kind guard reads the CP-visible `WorkflowStep.step_kind`, never the opaque `step_payload`.
- The new `effect_fence_paused_branches` carrier (C-CP-26 §26.2) is additive, default-empty, DROPPED-from-hash-when-empty → every pre-existing snapshot hashes byte-identically.
- **Tool-swap is ACCEPTED PARITY-with-linear, NOT a registered gap.** A tool-swap under the same step_id (resumed STILL TOOL_STEP, different tool_id) composes a different fence key → fires the new tool fresh; per-key at-most-once preserved — the SAME structural consequence as the cleared LINEAR effect-fence path. The CP driver cannot distinguish it (tool_id lives in the opaque step_payload); this is correct parity, not a missing capability.
- **The maybe-ran SUB_AGENT / MANAGED residuals are NOT resolved here** — they STAY fail-closed and are the registered follow-ons (SUB_AGENT = recursive child crash-resume; MANAGED = needs the fence extended to the vendor-session sink). The per-branch-distinct fence resolution (one resolution TYPE applies across all paused branches today) is the registered `B-FANOUT-EFFECT-FENCE-PER-BRANCH-RESOLUTION` (a key→resolution map on `ResumeContext`).
