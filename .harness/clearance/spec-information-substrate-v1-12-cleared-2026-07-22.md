---
artifact: design-substrate/Spec_Information_Substrate_v1.md
version: v1.12
cleared_at: 2026-07-22T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-Class-1-fork-ratification (B-33-A spec leg; spec-writer apply pass per CLAUDE.md §4.3 + §4.5)
back_reference:
  - .harness/class_1_fork_b33_rotation_correlation_carrier.md (RATIFIED 2026-07-21 — option A AS RECOMMENDED)
merge_commit: pending (pre-merge at filing time; B-33-A spec-leg apply PR)
reviewer_chain:
  - operator ratification (2026-07-21) — option A as recommended
  - advisor() review of the carrier-shape reconciliation (open-dict framing vs the IS typed-sidecar idiom) prior to authoring
  - just codex-review round 1 (2026-07-22, PR #1082) — 2 P1 + 4 P2 findings, all fixed same round: strengthened the Option-A/B hash-coverage reconciliation to directly rebut the fork's "hash-covered by the chain itself" Option-B parenthetical; added §7.7 invariant (c) non-emptiness guard (presence/uniqueness both pass vacuously on an empty window) + matching plan AC + witness; added construction-time canonical-UUID rejection to §5.6 + plan AC + witness; corrected "Closes B-33" → "Closes the IS-side half of B-33" (B-33 itself stays open) at 2 sites; refreshed harness-is/CLAUDE.md + .harness/claude-artifact-pointers.md pointers; refreshed the B-33 prose block at .harness/post-phase-8-forward-register.md to match the YAML transition
  - just codex-review round 2 (2026-07-22, PR #1082) — 2 P1 + 2 P2 findings, all fixed same round: added §7.7's "window vs. full genesis-anchored chain" clarification (a claimed rotation window is a caller-derived sub-sequence, distinct from the full-chain input `verify_chain` requires — the two checks compose, neither substitutes the other); corrected the §5.6 "Producer-supplied" claim — `execute_key_rotation` has NO ledger-write call site at HEAD (5 of 6 narrated steps are simulated), so the impl leg must build write capability from scratch, not merely extend an existing call, mirrored at the plan's Note (b); tightened the canonical-UUID constraint from bare `uuid.UUID`-parseability to a canonical-round-trip check (`str(uuid.UUID(value)) == value`) rejecting parseable-but-noncanonical spellings (unhyphenated 32-char hex, brace-wrapped), with a matching plan AC + witness; updated the sibling plan clearance marker's reviewer_chain (was a placeholder)
  - just codex-review round 3 (2026-07-22, PR #1082) — 1 P1 + 3 P2 findings, all fixed same round: closed a REAL logical flaw — the round-2 fix's illustrative window-derivation example ("filter by non-`None` id") was self-validating (presence/uniqueness would trivially pass because the derivation used the very field being checked); §7.7 now REQUIRES the boundary-derivation evidence be independent of `rotation_correlation_id` (positional bounds / a dedicated marker / OD-pair-named evidence), pinned as a requirement even though the concrete mechanism stays impl-deferred; corrected the plan's hash-coverage witness AC #3 to match `verify_chain`'s real detection mechanism (successor-link mismatch at N+1, not "at that entry" — a tampered last entry isn't caught by this path at all); disambiguated "the `B-33-A` impl leg" into two distinct arcs (the U-IS-20 code arc, IS-only, satisfies AC #8 literally; a later separate CP-axis arc for the verifier extension) at both the spec's §5.6 scope-discipline paragraph and the plan's AC #8; fixed the leading `v1.11 → v1.12` canonical-HEAD-label contradiction at `.harness/claude-artifact-pointers.md`
  - just codex-review round 4 (2026-07-22, PR #1082) — 1 P1 (open-dict carrier shape, a 2nd re-raise of round-1's finding #1) + 2 P1 (chain-head tamper coverage; window-bounds trust) + 2 P2 (plan-header two-arc propagation; stale B-33 prose), triaged via `advisor()` before acting: the repeated carrier-shape P1 is a labeling non-fork (the artifact is identical under either label; IS has no open-dict primitive to literally implement Option A, and building one would be MORE unratified design, not less) — NOT re-litigated further per `advisor()`'s explicit convergence read; the two new P1s are real but impl-leg-owned, so §7.7 REDUCED its claims rather than growing more defensive prose — `rotation_correlation_id` is now framed explicitly as a join key, not a trust anchor (presence/uniqueness/non-emptiness are computable in isolation but not meaningful without OD-anchored window boundaries; chain-head tamper coverage is a pre-existing `verify_chain` limitation shared by every C-IS-05 sidecar, remedied only by an independent OD-signature check, CP-owned) — both constraints REGISTERED at the plan's U-IS-20 Note (d) and the B-33 forward-register entry, not solved at this leg; propagated the two-arc split to the plan header; refreshed the stale B-33 prose (B-36 + B-47 are both CLOSED — no external dependency remains; the "wait for real caller" framing does not gate the build per the fork's own §3)
  - just codex-review round 5 (2026-07-22, PR #1082) — TERMINAL ROUND (pre-committed convergence rule), 2 P1 findings, both fixed same round, both narrower than round 4's: (1) the early §7.7 "contiguous run of entries" phrasing was stale against the round-4 OD-anchored/sparse-set reframe — a concurrent writer's unrelated entry CAN interleave positionally between rotation-pair siblings (`append_ledger_entry`'s lock serializes one append at a time, never a multi-entry atomic rotation-window write), so a genuinely OD-anchored window is a sparse REFERENCE SET, not necessarily a contiguous range; fixed at (a)'s definition + the boundary-derivation paragraph (positional bounds alone downgraded from "admissible" to "independent-but-insufficient"). (2) the plan's three separately-callable helpers (non-emptiness / presence / uniqueness) let a careless caller bypass the guard by invoking presence/uniqueness directly — restructured AC #5-#8 to require ONE public composed validator (non-goal fence renumbered #8→#9 accordingly). Both rounds 4 and 5 findings converge to the SAME conclusion: no further findings expected to change the carrier's own shape or the plan's own acceptance criteria — remaining risk is entirely impl-leg-owned and already registered at U-IS-20 Note (d) + the B-33 forward-register entry
---

# Clearance — Spec_Information_Substrate_v1 v1.12 (B-33-A spec leg)

v1.11→v1.12: NEW §5.6 D-derivative sidecar field `rotation_correlation_id: str | None` on `StateLedgerEntry`/`EntryPayload` (C-IS-05), following the established typed-sidecar idiom at §5.1 (`procedural_tier_snapshot_ref`) and §5.4 (`branch_metadata`) — omit-when-`None` canonicalization, ZERO breaking change to the existing chain. NEW §7.7 (C-IS-07) authors the two read-side invariants checkable against the IS chain alone: presence (every entry in a claimed rotation window carries the id) and uniqueness (a window carries at most one distinct id). This closes the IS-side half of `B-33` (registered at `.harness/post-phase-8-forward-register.md`; `verify_rotation_6_steps` at `harness_cp.five_axis_composition` proved chain continuity, not rotation, because the IS entries it walked carried no rotation-event identity).

The fork's own text described the ratified shape as "an open-dict additive IS carrier." Grounding against the actual `StateLedgerEntry`/`EntryPayload` schema showed IS has no open-dict/namespace-attrs mechanism; the change-note reconciles this explicitly — IS's own additive-carrier idiom is the typed D-derivative sidecar, which is (and must be) hash-covered exactly as OD §24.7 (the cited precedent) already is. The `verify_rotation_6_steps` 3-step extension, the composition-root-injected OD-join evidence DTO, and the B-36 `AwsKmsSigningBackend` key-identity boundary attestation are explicitly OUT of scope here (CP-owned; IS has zero outbound cross-axis edges) — cross-referenced at §7.7, decomposed at the separate CP-axis impl leg (mirrors the `B-59-A` spec-leg → impl-leg precedent, PRs #1080 → #1081). ZERO change to the §5 six-field shape / §5.1–§5.5 / §6 hash-chain construction / §7.1–§7.6 / §10 seam exports.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- The CP-axis impl leg (carrier code + `verify_rotation_6_steps` extension + OD-join) is a separate, not-yet-opened arc.
- See `.harness/clearance/README.md` for marker discipline.
