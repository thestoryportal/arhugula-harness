---
artifact: design-substrate/Spec_Control_Plane_v1_33.md
version: v1.33
cleared_at: 2026-06-15T00:00:00-06:00
clearance_type: spec-writer-apply-pass (E-spec-3 — reconciler-loop substrate-deferral reconciliation at C-CP-07 §7.4; operator-ratified cleared-contract touch; design-substrate-only, impl deferred to E-impl-3)
back_reference:
  - .harness/architect_recommendation_e_engine_fork_vs_impl.md (systems-architect §4A — the E-1/E-2-impl vs E-3-narrow-fork verdict; §4/§6/§7 derive this reconciliation)
  - .harness/class_1_fork_e3_reconciler_loop_substrate_deferral.md (the Class-1 fork — pins the exact §7.4 amendment text + grounding facts)
  - design-substrate/Spec_Control_Plane_v1_2.md §7.1/§7.4 (canonical body — the splitting fact: §7.4 omits reconciler-loop from the deferral clause)
  - Project_Roadmap_v1.md §5.0 (FULL-SPEC standing directive) + R-FS-1 E sub-program
  - PR (pending — this arc)
merge_commit: (pending)
reviewer_chain:
  - architect recommendation (systems-architect §4A dedicated-agent, grounded at HEAD 0500aa4) — rendered the canonical reading: E-1/E-2 = impl-against-cleared-spec (no fork), E-3 RECONCILER_LOOP = the one Class-1 narrow-fork (§7.4 omits reconciler-loop from the impl-discretion clause while §7.1/§7.4 name its substrate as vendored "K8s controller / etcd + CRD" ⊥ I-6). Out-of-family Codex concurred on the substance at the correction PR (#560); advisor re-consulted this session and concurred (gate-not-resolve, gate minimally + concretely).
  - operator AskUserQuestion ratification 2026-06-15 — the §7.4 amendment text ratified VERBATIM ("Ratify as written"). The single genuine operator fork in the E sub-program (a P5-CK cleared-contract touch reconciling two committed surfaces: spec-named "K8s controller / etcd" vs I-6 "no vendored K8s").
  - spec-writer apply pass (HEAD eb8b90b) — verified at session-time before authoring: true CP spec head = v1_32 (not the rec's parenthetical "v1_32" head-cite ambiguity nor CLAUDE.md §2.3's stale v1_30); §7.4 last-substantive-definition = v1_2, preserved verbatim through v1_32 (v1_30/31/32 touch §16.5/§29/§25-28 — none re-table §7.4); _IN_SCOPE_ENGINE_CLASSES = {PURE_PATTERN, SAVE_POINT, EVENT_SOURCED_REPLAY, WAL_SEGMENT} at workflow_driver.py:187-214, only RECONCILER_LOOP raises at :1398; engine_class.py:48 RECONCILER_LOOP docstring has no "deferred per §7.4" qualifier (corroborates the split). Amendment lands as v1_33; §7.1/§7.2/§7.4-floor-table preserved verbatim, only the §7.4 deferral paragraph gains the reconciler-loop member.
  - harness-adversarial-reviewer Phase-7 pre-merge review (dedicated-agent adopting `.claude/skills/harness-adversarial-reviewer/SKILL.md`, 23 tool-uses, grounded at HEAD eb8b90b) — **CLEARED to merge: 0 blocking, 1 fix-in-arc (F-1), 2 informational**. Verified BYTE-EXACT: (a) §7.4 deferral base = v1_2, NOT re-tabled by v1_30/31/32 (v1_31's "Deferred to implementation discretion" is §29.7 prompts; v1_32's is §25.18 topology — neither the §7.4 floor); (b) v1_33 floor table (i)–(iv) byte-identical to v1_2; (c) inserted reconciler clause byte-identical to the fork §3 ratified text; (d) EngineClass + ResumptionKind both closed-at-5 with reconciler-loop already a member; (e) no new contract ID / ADR / enum / hash-chain change (X-AL-3-clean); (f) all three pointer refreshes consistent; cross-spec: runtime/OD specs have ZERO reconciler refs, CP plan v2_34 + RT plan v2_45 already name E-3 as the E-spec-3 fork (forward-consistent, opposite of drift). **F-1 (fix-in-arc, APPLIED):** the reconciliation note was silent on §7.2 deployment-admissibility — the hand-rolled reading undercuts the §7.2 `local-development` exclusion ("requires K8s control plane"); fixed by adding a deployment-admissibility-deferred-to-E-impl-3 paragraph to the §7.4 note + widening the change-note downstream-gate scope, WITHOUT editing §7.2 text (an edit there = over-reach + an upstream ADR-D1 §1.2 touch). F-2 (engine_class_candidate.py:70 exclusion_reason stale) + F-3 (code-cite line-tails approximate but all resolve) = informational, flagged for E-impl-3.
  - out-of-family Codex review (just codex-review-uncommitted, $0 ChatGPT subscription, decorrelated) — **2 [P2] governance-hygiene findings, BOTH APPLIED** (decorrelated from the adversarial agent — Codex caught artifact-STATE inconsistency the content-focused agent did not): [P2-1] clearance reviewer rows were `[TO BE FILLED]` → filled with these outcomes; [P2-2] the fork doc still asked the operator to ratify though the spec + marker said ratified → fork doc updated to ✅ RATIFIED-AND-APPLIED disposition + §6 RESOLVED. Codex did NOT challenge the substantive spec content. Decorrelation payoff: agent caught the spec-content under-reach (F-1), Codex caught the governance-state drift — two catchers by finding-type.
supersedes:
superseded_by:
---

# Clearance — `Spec_Control_Plane v1.33`

v1.33 is the **E-spec-3** leg of the R-FS-1 E durable-execution-engine sub-program — a single **substrate-deferral reconciliation** at C-CP-07 §7.4. The §7.4 "Deferred to implementation discretion" clause (v1.2, preserved verbatim through v1.32) named event-sourced-replay, save-point-checkpoint, and WAL-segment as substrate-deferred but **omitted `reconciler-loop`**, whose substrate §7.1 row-4 + §7.4 floor-(i)/(iii) named concretely and non-deferred as "K8s controller / etcd + CRD events / etcd compare-and-swap." That named-vendored-K8s substrate conflicts with **I-6** (committed: hand-rolled reliability primitives, no vendored K8s). v1.33 adds `reconciler-loop` to the deferral clause, re-reading the §7.1/§7.4 "K8s controller / etcd" naming as the capability-floor **reference** implementation (parallel to "Temporal / DBOS / Restate" at event-sourced-replay), not a vendored-K8s mandate, and blessing the harness-hosted hand-rolled etcd-style reconciliation control-loop (per I-6) as the candidate satisfying the same F3 floor (i)–(iv).

This is the **only spec touch the E arc owes** — E-1 EVENT_SOURCED_REPLAY (#564) and E-2 WAL_SEGMENT (#566) were impl-against-cleared-spec with no spec bump (the U-CP-56 precedent). The reconciliation is narrow: one within-paragraph member added; §7.1/§7.2/§7.3 + the §7.4 floor table (i)–(iv) + §8 (closed-at-5 ResumptionKind; `reconciler_converge` already mapped) preserved verbatim. No new contract ID, no new ADR, no EngineClass/ResumptionKind enum change, no six-field/hash-chain/ADR-F2 change. X-AL-3-clean — v1.33 reconciles a cleared contract with a committed invariant; it introduces no new primitive.

**Caveat for Phase 7 consumers.** v1.33 blesses the hand-rolled substrate; the build is **E-impl-3** (decompose at E-plan → materialize RECONCILER_LOOP into `_IN_SCOPE_ENGINE_CLASSES` + dispatch branch + a hand-rolled etcd-style reconciliation substrate + non-live in-memory/filesystem proof). The **live-K8s e2e is a separate downstream deployment-surface gate** at E-impl-3 (`engine_class_candidate.py:70` excludes reconciler-loop at local-development; candidate at self-hosted-server + managed-cloud) — not cleared here. CLAUDE.md §2.3's CP-spec pointer was stale (v1_30) before this PR; refreshed to v1_33 here.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
