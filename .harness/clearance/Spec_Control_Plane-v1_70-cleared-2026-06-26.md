---
artifact: design-substrate/Spec_Control_Plane_v1_70.md
version: v1.70
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_68.md §2 item 1 (registered this arc: "B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-NOT-YET-DISPATCHED (tractable — distinct mechanism) … a real pause snapshot LEGITIMATELY omits not-yet-dispatched branches … needs a NEW re-pause-without-dispatching-the-absent strategy mode; operator_gated: false")
  - design-substrate/Spec_Control_Plane_v1_32.md §25.15.1 (the PAUSE row "Allowed to finish in-flight, then pause" → PAUSED, not-yet-dispatched left re-dispatchable) + §25.15.2 obl. 5 (high-blast-radius PRE-dispatch gating — the operator's resume decision, NOT crash-resume) + obl. 7 (resume MUST NOT re-dispatch a terminal branch) — the cleared semantics this delta implements for the provably-not-yet-dispatched crash-resume
  - harness-cp/src/harness_cp/workflow_driver.py (the `PAUSE ∧ _crash_pause_trigger ∧ ¬_crash_complete` leg split on `dispatch_instrumented ∧ present_dispatched_indexes − recovered == ∅`; the `_crash_pause_reconstruct_no_dispatch` thread through the three fan-out strategy calls; the branch-plan skip in `_execute_parallelization` + `_execute_orchestrator_workers` [hierarchical via delegation]; the §25.15 incomplete leg guarded `and not _crash_pause_reconstruct_no_dispatch`)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-NOT-YET-DISPATCHED registered → closed, WHOLE close; snapshot standalone_closed 50→51, standalone_registered 6→5)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, BEFORE substantive work — the load-bearing calls):
      (1) CONFIRMED the approach: the v1.68 §2 design is pre-specified; "make branch_plan empty → ride #750's reconstruct path" is a sound core; non-operator-gated; no council (single-axis, no nameable cross-domain tension).
      (2) BLOCKING — the RESUME half: the reconstruct producing a valid-looking snapshot proves nothing about resume. The "re-dispatch not-yet-dispatched on resume" path was DEAD-until-now for PARALLELIZATION (a live parallelization pause has all branches). Directed a full-chain reconstruct→resume witness: resume re-dispatches EXACTLY the omitted ordinals, the seeded failed branch keeps it PARTIAL without spurious re-pause / dropped failure, and the final state EQUALS the no-crash baseline. Built + green (test_crash_resume_pause_not_yet_dispatched_resume_redispatches_omitted).
      (3) Read the orchestrator-workers `not branch_plan` block (8263) BEFORE assuming generalization — confirmed by direct read it has its own re-establish block that the empty-branch_plan trick rides cleanly; scope DECIDED explicitly = all three topologies (matching #750), not discovered mid-impl.
      (4) Gate-strictness PROTECTED: `_instrumented ∧ ¬_maybe_ran` (the FULL maybe-ran set empty), NOT `¬_maybe_ran_fail_closed` — ANY interrupted-in-flight ordinal is the MAYBE-RAN follow-on's territory, fail-closed here (even a re-fire-safe DECLARATIVE_STEP).
      (5) Fidelity-is-not-byte-match CORRECTION: a live parallelization pause has all branches; this reconstruct deliberately omits the not-yet-dispatched. Stated in the spec delta as "resume re-dispatches the omitted ordinals + honors obl-5 by re-pausing-not-auto-firing + final-state-equivalent after resume" — NOT v1.68's byte-match language.
  - direct grounding (presence-not-correctness, delta-only chain `[[wrong-version-read-of-a-delta-only-baseline]]`): §25.15 born at CP v1.32, last re-tabled at v1.57; v1.68 §2 item 1 PRE-SPECIFIES this slice's design verbatim. The two seed loops (parallelization 5917, orchestrator-workers 8222) populate `terminal_dispositions` from `_recovered_terminal` BEFORE the re-establish gate → the recovered errored trigger is detected with an empty branch_plan. The existing maybe-ran test (forget_branch keeps the marker) STILL fails closed under the new gate (`_maybe_ran` non-empty → else-branch) — verified by re-run.
  - Explore sub-agent — mapped the live fan-out crash-resume + pause path (`_crash_pause_reestablish`, `_crash_complete`/`_crash_degraded`, the three strategies' branch_plan, the dispatch-marker store reads); ALL load-bearing claims re-grounded by direct read (the #746 lesson — the gate's `not branch_plan` conjunct + the orchestrator-workers separate re-establish block were confirmed by direct read, not the map).
  - out-of-family Codex — diff review (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): converged across TWO rounds. R1 [P1] DASHBOARD_SNAPSHOT_STALE (the mechanical §12.2 dashboard-source regen owed when arc-ledger.yaml changes — FIXED by regenerating tools/dashboard/roadmap.html via generate.py); R2 "No actionable correctness issues were found in the diff" — NO crash-resume logic finding (the high-blast-radius surface is codex-clean). The closeout-mode ROOT_CHECKOUT_EDIT HARD is a worktree-expectation false-positive for this in-place-configured session (the codex-closeout helper, not ci.yml); OPEN_PRS_UNAVAILABLE + ROADMAP_DASHBOARD_BRANCH_DIVERGED are expected feature-branch warnings.
  - by-execution witnesses (harness-cp test_workflow_driver_fanout_output_replay_full_chain.py): not-yet-dispatched reconstructs PAUSED omitting the absent ordinal, re-dispatch nothing; the full-chain `api.resume` → re-dispatch EXACTLY the omitted ordinal → PARTIAL EQUAL to the no-crash baseline (the blocking RESUME-half trace); a maybe-ran absent ordinal of a re-fire-safe DECLARATIVE_STEP STAYS fail-closed (gate-strictness); a pre-arc un-instrumented journal fails closed (the `_instrumented` guard); the ORCHESTRATOR_WORKERS analogue reconstructs PAUSED omitting the not-yet-dispatched worker. Full suites: harness-cp 1355 passed / 1 xfailed; harness-runtime 2129 passed / 23 skipped (credential-gated live-e2e) / 1 xfailed + harness-cxa 28 = 2157 cross-axis green (the FAILED→PAUSED behavior change at the shared driver breaks no cross-axis e2e); pyright 0/0/0; ruff clean; overlay-check 31/31.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.70`

v1.70 is a **bundled-absorption** delta over v1.69 (co-published with `harness-cp` impl + tests) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-NOT-YET-DISPATCHED`, as a **WHOLE close**. CP-ONLY (no runtime delta — reuses the §26.2 PauseResumeProtocol + the v1.44 fan-out pause-capture machinery + the v1.60 dispatch-instrumentation store reads verbatim).

- **§1 — the not-yet-dispatched re-pause-without-dispatch build.** The `PAUSE ∧ _crash_pause_trigger ∧ ¬_crash_complete` crash-resume leg (the v1.68 §2 `pause-trigger-ambiguous` fail-closed) now classifies the absent set: every absent ordinal PROVABLY-NOT-RUN (`dispatch_instrumented ∧ present_dispatched_indexes − recovered == ∅`) → set `_crash_pause_reconstruct_no_dispatch` + fall through; the strategy SKIPS the not-yet-dispatched ordinals → an EMPTY branch_plan → the v1.68 `_crash_pause_reestablish` gate / `not branch_plan` re-establish block re-establishes PAUSED OMITTING them; `api.resume` re-dispatches them under obl-5. ANY maybe-ran ordinal / un-instrumented journal STAYS fail-closed.
- **§2 — residual + scope.** WHOLE close (no decompose) — the tractable not-yet-dispatched case is built uniformly across all three fan-out topologies. The hard `…-MAYBE-RAN` residual (interrupted-in-flight absent ordinal) stays registered. `standalone_registered` 6 → 5 (net −1).
- **§3 — R-FS-1 closure semantics.** G1.1 = 5 + 0 → R-FS-1 stays ACTIVE (5 follow-ons remain).

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change / new fail-class. At-most-once is PRESERVED — crash-resume re-dispatches NOTHING (the not-yet-dispatched ordinals never fired; the dispatch marker is the at-most-once proof; their re-dispatch on `api.resume` is first-and-only). §25.15.1 + §25.15.2 obl-5/obl-7 are IMPLEMENTED for the provably-not-yet-dispatched crash-resume path, not changed (the v1.68 impl-to-cleared-spec posture). The §26.2 PauseSnapshot envelope + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **CP-ONLY, no runtime delta** — the reconstruct + the re-pause-without-dispatch mode reuse existing machinery; there is no paired runtime spec change.
- **The gate is STRICTER than the §25.15 incomplete leg.** It keys on the FULL `_maybe_ran` set being EMPTY (`_instrumented ∧ ¬_maybe_ran`), NOT the narrowed `_maybe_ran_fail_closed`. A lost-pause reconstruct cannot honor "finish in-flight" when an absent ordinal maybe-ran (doubly-ambiguous), so even a re-fire-safe DECLARATIVE_STEP maybe-ran ordinal fails closed → the registered `…-MAYBE-RAN` follow-on.
- **Fidelity is NOT a byte-match of a live pause** (unlike the v1.68 complete-recovery slice). A live PARALLELIZATION/ORCHESTRATOR_WORKERS pause holds every ordinal (fan-out is one TaskGroup, pause is evaluated post-barrier); this reconstruct deliberately OMITS the not-yet-dispatched ordinals. The fidelity claim is: `api.resume` re-dispatches EXACTLY the omitted ordinals (the declared `branch_count`/`worker_count` bounds the resume), and the post-resume PARTIAL state EQUALS the no-crash baseline (witnessed). The omitted ordinal fires on RESUME under the operator's resolution, not on crash-resume — strictly more conservative than the live run, which is the obl-5 blast-radius gate working as intended.
