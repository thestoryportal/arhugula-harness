---
artifact: design-substrate/Spec_Harness_Runtime_v1.md
version: v1.49
cleared_at: 2026-06-14T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-fork-doc (design-substrate amendment — materializes the CP §19.5 operator-policy override authoring schema as a NEW RuntimeConfig field; R-FS-1 arc B3 / B3-spec-1 / F-B3-1)
back_reference:
  - .harness/class_1_fork_b3_1_hitl_auto_approve_policy_field.md (F-B3-1 — the Class-1 fork doc this amendment applies; operator-ratified at the B3-spec-1 front-gate 2026-06-14 via AskUserQuestion, preceded by a genuine dyadic C10⊥C11 council: Q1 default-value = READ_ONLY auto-ON; Q2 materialization = declared field + narrow runtime-spec fork)
  - .harness/r-fs-1-b3-smart-hitl-design-v1.md §1.3 (keystone — all-ASK persona floor ⇒ gate always fires; the §19.5 override is the only skip path) + §3.3 (D-cond.2 Reading C tunable-floor; Reading D rejected) + §8.1 (F-B3-1 fork row + the F1-02 silent-absorption guard) + §8.3 (sequence); cleared #549
  - design-substrate/Spec_Control_Plane_v1_2.md §19.1 (floor enumeration; line 1634 blast local-mutation "configurable to auto at solo-developer" + line 1639 persona solo "operator may override to auto for non-irreversible") + §19.4 (`_hitl_required` truth table) + §19.5 (override surface + per-tier permissions; line 1696 "any max() floor" / line 1698 solo "operator IS the policy authority" + "each override emits audit-ledger entry per C-CP-20 §20.1" / line 1700 multi "structurally prohibited" / line 1702 the deferred authoring schema) — §19 defined at v1.2, carried verbatim to head v1.32, NOT re-tabled (delta-baseline §-cite convention)
  - design-substrate/Spec_Harness_Runtime_v1.md §14.8.2 step-4c (the v1.22 4-axis `_hitl_required` evaluation site; line 1937) — the consumption site this amendment canonical-reading-extends with the in-`max()` override
  - .harness/beyond-mvp-capability-boundary-ledger.md (R-FS-1 full-spec build program spine; Bucket B row B3) + Project_Roadmap_v1.md R-FS-1 (the active umbrella)
  - .harness/adversarial-review-r-fs-1-b3-spec-1.md (the filed pre-merge adversarial review report)
  - PR (pending — this arc)
merge_commit: (pending)
reviewer_chain:
  - harness-adversarial-reviewer Phase-7 pre-merge review (dedicated-agent invocation per [[feedback-genuine-skill-invocation-dedicated-agent]], 17 tool-uses; full report at `.harness/adversarial-review-r-fs-1-b3-spec-1.md`) — VERDICT **APPROVE-WITH-CLASS-3** (0 Class-1 blocking / 0 Class-2 / 1 optional Class-1 doc-hygiene nicety). All EIGHT load-bearing claims verified TRUE by direct read of code + cleared specs: (1) **C-RT-03-only** — BODY-read of `hitl_gate_composer.py`: `RuntimeHITLGateComposer` is a stage-5-constructed dataclass; `dispatch(self, binding, step, *, step_context)` has NO `ctx` param + reads `binding`/instance-state at step-4c (the `validator_framework`/`pause_resume`/`webhook`/`prompt_manifest` precedents carry C-RT-04 precisely because they ARE `ctx.<field>`-consumed at dispatch) → no missing amendment; (2) **shape forecloses Reading-D + EXTERNAL_*** — two-bool named-cell + `extra="forbid"`, no field maps to EXTERNAL_* or a post-`max()` bypass → structurally not representable; (3) **default `max()` arithmetic** re-derived independently from `gate_level_rule.py` (READ_ONLY→AUTO/skip because the READ_ONLY blast floor is already AUTO so lowering only persona makes all three axes AUTO; LOCAL_MUTATION→ASK/gate; EXTERNAL_*→ASK/gate); (4) **§19.1↔§19.5 asymmetry registered-not-resolved (X-AL-3)** — `git diff` confirms ZERO CP-spec edit; reconciliation registered (fork §2.4/§6) not patched — the only X-AL-3-clean disposition; (5) **FULL-SPEC honored** — team override + external-reversible + CP-reconciliation registered as owed arcs (fork §6); multi satisfied by construction (solo-scoped); solo-scope matches cleared design §3.3 Reading C; (6) **spec-only/inert-until-impl** explicit (precedented by `prompt_manifest` v1.42 / `validator_framework_config` v1.18); no `harness-*/src` edit; (7) **byte-exact cites** — all 10 CP §19 line cites + the §14.8.2 step-4c line-1937 anchor + the C-RT-03/04 precedent rows resolve; (8) **delta-only preservation** — diff +59/-1 (the -1 = version header), v1.48 + prior lineage untouched, net "+1 field, +1 sub-model, +0 C-RT-04, +0 contract, +0 fail class" matches the actual diff; cross-spec drift probe clean. The 1 optional Class-1 (**F1-01**, non-blocking, did not gate clearance): the §3.8 READ_ONLY arithmetic row could note the skip requires both the persona knob ON AND the already-AUTO READ_ONLY blast floor → **APPLIED** (a clarifying parenthetical added after the §3.8 table).
  - out-of-family Codex review (`just codex-review-uncommitted`, decorrelated, on the uncommitted diff; ran `just overlay-check` → clean 312 nodes / 31-31 seams) — 2 [P2], BOTH governance-tracking (NO spec-content defect found): **(1)** file the claimed v1.49 clearance marker (the fork-doc Status line referenced a not-yet-filed marker) → **ADDRESSED by this marker**; **(2)** the `.harness/roadmap_status.md` next_action still selects "author F-B3-1" → owed at the post-merge §12.2 refresh (moves to B3-spec-2) → **HONORED at the terminating refresh**. **Decorrelation payoff (CLAUDE.md §13.1):** Codex caught the tracking-artifact inconsistency (the v1.48 "don't land with stale tracking" defect class) that the adversarial agent does not probe; the adversarial agent did the deep spec-content correctness verification (C-RT-03-only / arithmetic / register-don't-resolve) that Codex did not. Their division of labor — not disagreement — held; neither flagged a spec defect.
  - advisor() — pre-substantive decision-fork was consulted earlier this session (it established the C-RT-03-only trace + the register-don't-resolve fork boundary + the GateLevelInput→B3-plan forward); the pre-done advisor() call TIMED OUT and was compensated by the adversarial agent's independent code-level arithmetic re-derivation + a repo-wide drift grep + the out-of-family Codex pass (the two diff-reviewers + the transcript-grounded session reasoning cover the pre-done check).
  - empirical code-grounding (verify-by-execution): `gate_level_rule.py` (`PERSONA_TIER_GATE_LEVEL_FLOOR` all-ASK; `BLAST_RADIUS_GATE_LEVEL_FLOOR` READ_ONLY→AUTO/rest→ASK, no DENY; `gate_level()` = rank-`max()`; `GateLevelInput` frozen `extra="forbid"`) + `hitl_gate_composer.py` (`RuntimeHITLGateComposer` stage-5 dataclass; `_evaluate_hitl_required_tolerant` reads `binding`/instance-state, not `ctx`, at the gate eval) — confirms C-RT-03-only + the arithmetic; no code lands in this PR (impl is B3-impl-1)
  - design-phase bundled-absorption posture (workspace CLAUDE.md §11.4; X-AL-3 guard satisfied by the F-B3-1 fork doc + this clearance marker as the `.harness/` back-flow companions to the `design-substrate/**` edit)
supersedes: design-substrate/Spec_Harness_Runtime_v1.md v1.48
superseded_by:
---

# Clearance — `Spec_Harness_Runtime v1.49`

v1.49 is the **B3-spec-1 leg of R-FS-1 arc B3 (smart-HITL)** — it materializes the **CP §19.5 operator-policy override authoring schema** (the schema CP v1.2 §19.5 line 1702 deferred to implementation discretion) as a NEW operator-supply `RuntimeConfig` field. Per `.harness/class_1_fork_b3_1_hitl_auto_approve_policy_field.md` (F-B3-1; operator-ratified front-gate 2026-06-14, preceded by a genuine dyadic C10⊥C11 council). This is a **design-substrate-only** amendment; **no code lands** here (impl is B3-impl-1; the field is inert in production until then).

**What changed.** Three additive sites (plus the title bump + the v1.48→v1.49 Change-note):

1. **§3 C-RT-03 `RuntimeConfig` field table — NEW row `hitl_auto_approve_policy`.** Type `HITLAutoApprovePolicy` (the new §3.8 sub-model; frozen, `extra="forbid"`). Default `HITLAutoApprovePolicy()` = `{solo_persona_floor_auto: True, solo_local_mutation_floor_auto: False}` ⇒ **READ_ONLY auto-ON / LOCAL_MUTATION opt-in / EXTERNAL_* hard-stop** at solo-developer (the operator-ratified posture). Consumed at §14.8.2 step-4c by `RuntimeHITLGateComposer` (read at stage-5 construction, held as composer instance state — **no C-RT-04 field**, because the composer does not read `ctx.<field>` at dispatch; the distinguishing trace vs the validator/pause/webhook precedents).

2. **NEW §3.8 `HITLAutoApprovePolicy` sub-model + the CP §19.5 in-`max()` floor-override consumption.** The two-bool **named-cell** tunable-floor sub-model (Reading C — overrides ONLY the two §19.1-annotated cells: `persona_tier_floor[SOLO_DEVELOPER]→AUTO` line 1639 + `blast_radius_floor[LOCAL_MUTATION]→AUTO` line 1634; applied **in-`max()`**, NOT a post-`max()` bypass — Reading D rejected) + the `max()` arithmetic table + the solo-scoping (multi structurally foreclosed; team a registered follow-on) + the two C10 acceptance criteria carried to B3-impl-1 (AC-1 audit-wiring-not-vacuous; AC-2 EXTERNAL_REVERSIBLE-not-representable) + the `gate_level()` carrier-shape forward to B3-plan (U-CP-43).

3. **§14.8.2 step-4c consumption CANONICAL-READING AMENDMENT.** The v1.22 step-4c `_hitl_required(...)` evaluation is canonically re-read as composing the §19.5 floor override **in-`max()`** (lower the §19.1-annotated cell per `config.hitl_auto_approve_policy` BEFORE `gate_level()` composes the max). The v1.22 step-4c body text is NOT edited; the carrier-shape touch is a B3-plan concern (U-CP-43), NOT a C-CP-19 spec change.

**Why "extend, not mint."** The field extends C-RT-03 §3; the consumption is a canonical reading of the v1.22 §14.8.2 step-4c site. No new C-RT-NN, no new fail class, no new HarnessContext field, no new RunStatus value. Net: +1 RuntimeConfig field, +1 sub-model.

**Why this is a faithful materialization, NOT a silent design extension (X-AL-3).** The amendment materializes the §19.5 *deferred authoring schema*; it does **not** change CP §19.1/§19.5 (ZERO CP-spec edit, `git diff`-verified). The genuine **§19.1↔§19.5 internal inconsistency on the external-reversible cell** (§19.1 line 1635/1640 forbid solo/team external-reversible override ⊥ §19.5 line 1696/1699 permit it) is **registered** (F-B3-1 §2.4/§6) as owing a CP-spec reconciliation arc — NOT resolved in the permissive direction (which WOULD be the X-AL-3 violation). The field's expressible domain is the conservative intersection where §19.1 and §19.5 agree (persona[SOLO], blast[LOCAL_MUTATION]); EXTERNAL_* override is structurally not representable.

**Why FULL-SPEC is honored (nothing sacrificed/deferred).** The §19.5 cells F-B3-1 does not build are **registered** (F-B3-1 §6), not dropped: (1) team-binding override (§19.5 line 1699) — owed follow-on; (2) multi-tenant — no build owed (structurally foreclosed by solo-scoping); (3) the CP-spec §19.1↔§19.5 reconciliation — owed Class-1-fork-against-CP-spec, prerequisite for any external-reversible override. The operator's 2026-06-14 EXTERNAL_* hard-stop is the standing answer until that arc opens.

**Carve-outs for Phase 7 / downstream consumers.**
- **All implementation lands at B3-impl-1** — the composer stage-5 ingestion of `config.hitl_auto_approve_policy`, the in-`max()` floor-lowering, and the §20.1 per-skip audit emission are code, NOT this arc. This arc's spec **describes** them. The field is **inert in production until B3-impl-1**.
- **C-RT-04 PRESERVED VERBATIM** — no HarnessContext field (the composer reads `config` at stage-5 construction).
- **CP spec PRESERVED — ZERO edit** — §19.5 already specs the override surface + tier-permissions; the §19.1↔§19.5 asymmetry is registered, not patched.
- **C-RT-02 / C-RT-04 / C-RT-15 contract bodies, §14.x, and all prior lineage PRESERVED VERBATIM** except the three enumerated additive sites.
- **The `gate_level()` carrier-shape** (how the lowered floor threads into `harness_cp.gate_level_rule.gate_level()`) is a B3-plan concern (U-CP-43 plan-carrier), NOT a CP-spec change.

## Notes
- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- The coordinated next arcs are **B3-spec-2** (F-B3-2 timeout-degradation-disposition, Class-1) → **B3-plan** (atomic-unit decomposition incl. the U-CP-43 `gate_level()` carrier-shape touch) → **B3-impl-1** (the G1-skip override consumption + the two C10 ACs) per design §8.3.
- See `.harness/clearance/README.md` for marker discipline.
