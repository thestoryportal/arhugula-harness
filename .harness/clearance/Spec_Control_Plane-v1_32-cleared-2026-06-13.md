---
artifact: design-substrate/Spec_Control_Plane_v1_32.md
version: v1.32
cleared_at: 2026-06-13T00:00:00-06:00
clearance_type: spec-writer-apply-pass (design-first B1-spec-1 — in-place C-CP-25 extension absorbing the cleared B1 design + resolving 3 forks; design-substrate-only, impl deferred to B1-impl-N)
back_reference:
  - .harness/r-fs-1-b1-topology-orchestration-design-v1.md (cleared B1 design, R-FS-1 arc #2 — the orchestration semantics this spec encodes)
  - .harness/r-fs-1-arc-1-scoping-v1.md (R-FS-1 arc #1 — child-arc B1 in the frozen ordered list)
  - .harness/class_2_fork_b1_spec_1_contract_shape.md (fork (a) — in-place §25.10+ extension, RESOLVED)
  - .harness/class_1_fork_b1_branch_causality_route_x_vs_y.md (fork (b) — Route Y, RESOLVED)
  - .harness/class_1_fork_b1_effectful_cancellation_c10.md (fork (c) — Fork A, council-RESOLVED)
  - .harness/council/r-fs-1-b1-cascade-cancel/DELIVERABLE.md (genuine dyadic C1⊥C10 council resolution)
  - .harness/class_1_fork_cp_spec_section_25_contract_id_collision.md (v1.13 Reading A — the §25/C-CP-25=WorkflowDriver identity v1.32 corrects forward)
  - Project_Roadmap_v1.md §5.0 (FULL-SPEC standing directive) + R-FS-1
  - PR (pending — this arc)
merge_commit: (pending)
reviewer_chain:
  - advisor() pre-substantive decision-fork (4 flags applied — (1) map the C-CP ID space from contract BODIES not the drifted change-note tables before minting any ID; (2) the FULL-SPEC directive makes the C10 fork RESOLVE not carry-open, with the composes-committed-primitives discriminator; (3) author so the adversarial reviewer survives — address the §25/§28 mislabel head-on + mark the Route-Y IS sidecar as a forward-coordination reference not a resolvable cite; (4) scope = CP spec PR only). Also produced the cascade-cancel reframe (rollback-of-already-sent-effects is not coherent → out of the primitive's domain, a complete semantic, not a defer).
  - authoring grounding pass (HEAD): C-CP ID space code-grounded (highest top-level = C-CP-29 → next free C-CP-30, UNUSED by the in-place §25.10+ extension; `.b` sub-IDs are not an established convention; the deferral-table "C-CP-26" guess is occupied by PauseResumeProtocol); §25/C-CP-25 = WorkflowDriver + §28/C-CP-28 = ValidatorFramework verified against the §25 body header (`Spec_Control_Plane_v1_6.md`) + the §28 ValidatorFramework body + code (`workflow_driver.py` vs `validator_framework_types.py`) + the v1.13 Reading A fork; `RunStatus.PARTIAL` reserved at §25.2 (`Spec_Control_Plane_v1_6.md:238` + `workflow_driver_types.py`) so `proceed→PARTIAL` uses an EXISTING value (no silent enum extension); `_CP_TO_RT_STATUS` has no PARTIAL entry → runtime projection correctly deferred to B1-spec-2; `_IN_SCOPE_TOPOLOGY` gate at `workflow_driver.py:83`; `CascadePolicy` declared-but-unconsumed (`topology_pattern.py:55-67`); C-CP-10 §10.1/§10.2/§10.3 byte-exact (`Spec_Control_Plane_v1_2.md`); C-AS-02 sandbox four-tier + C-CP-19 §19.1 + C-CP-16 palette gating chain verified.
  - genuine dyadic C1⊥C10 council (`.claude/skills/council/c1-orchestration` + `c10-action-safety-blast-radius`, dedicated-agent invocations per `[[feedback-genuine-skill-invocation-dedicated-agent]]`) — both voices independently reached Fork A + the reframe; 3-way decorrelated convergence (advisor + C1 + C10); resolution composes committed primitives (DRAIN §25.4, C-AS-02/ADR-F4, C-CP-19 §19.1, C-CP-16, Route-Y sidecar, branch-scoped idempotency) → not a meaningful-architecture gate (§13.4 discriminator) → resolved, no operator gate.
  - harness-adversarial-reviewer Phase-7 pre-merge review (dedicated-agent invocation, 33 tool-uses) — VERDICT CLEAR (0 blocks-merge / 0 fix-in-arc / 2 informational). Empirically verified EVERY load-bearing cite byte-exact at HEAD (§25.1 deferral, RunStatus.PARTIAL reserved, _IN_SCOPE_TOPOLOGY, CascadePolicy, ADR-F2 six-field + single-threaded-write boundary, C-IS-07 §7.5 dedup, ADR-F4 four-tier, C-CP-10/12/16/19, C-AS-02) — no phantom/mis-numbered cite; §25.13 Route-Y correctly marked forward-coordination (not the phantom-cite trap); §25/§28 identity correction correct + history-preserving; X-AL-3 clean (proceed→PARTIAL uses the reserved value; runtime projection deferred to a build arc; zero ADR-F2/six-field/hash-chain change); cascade-cancel complete-not-deferred (the "no compensation primitive" prong (b) grounded in `Spec_Action_Surface_v1.md:452` external-reversible-has-rollback-API). The 2 informational: F-INFO-01 footer referenced the not-yet-filed clearance marker (this file resolves it); F-INFO-02 §25.14 `llm_dispatch.py` path qualifier implicit (APPLIED — qualified to `harness-runtime/.../lifecycle/llm_dispatch.py`).
  - out-of-family Codex review (decorrelated, `just codex-review-uncommitted`, $0 ChatGPT subscription) — 2 findings, BOTH provenance (converged with the adversarial F-INFO-01, decorrelation value): [P2] footer claimed co-published clearance marker + roadmap refresh not present → APPLIED (clearance marker filed = this file; footer softened to mark the §12.2.1 roadmap refresh as post-merge-owed); [P3] charter referenced a `01-council/cross-read.md` not present → APPLIED (charter ledger updated — the B-stage cross-read is folded into DELIVERABLE.md for a 2-voice dyadic convening). Codex did NOT challenge the substantive spec content.
supersedes:
superseded_by:
---

# Clearance — `Spec_Control_Plane v1.32`

v1.32 is the **B1-spec-1** amendment of the R-FS-1 full-spec build program: an **in-place additive extension of C-CP-25 WorkflowDriver** (NEW §25.10–§25.18) that materializes the 5 non-`SINGLE_THREADED_LINEAR` topology patterns (`PARALLELIZATION`, `ORCHESTRATOR_WORKERS`, `DECENTRALIZED_HANDOFF`, `HIERARCHICAL_DELEGATION`, `EVALUATOR_OPTIMIZER`), lifting the §25.1 deferral under the FULL-SPEC directive. It authors the driver-strategy dispatch surface (lifting the `_IN_SCOPE_TOPOLOGY` gate), the 5 per-pattern orchestration contracts, the buffered/deterministic-append branch path (D1.b; ADR-F2's own single-threaded-write boundary → zero six-field/hash-chain/ADR-F2-§Decision change), the Route-Y branch-metadata sidecar seam (D1.a; forward-coordination reference to the coordinated B1-spec-1b IS amendment), the B1↔B4 role seam (D2), `cascade_policy` consumption + the cascade-cancel reach (D3 / Fork A — council-resolved, 8 obligations), branch-scoped idempotency-key composition, the failure-mode taxonomy extension, and the recorded-forks list.

Three forks were resolved at this amendment: (a) contract shape → in-place §25.10+ extension of C-CP-25 (not a new ID; the §25.9 v1.5 precedent); (b) branch-causality → Route Y (bounded IS D-derivative sidecar, owed at B1-spec-1b); (c) effectful-cancellation C10 → Fork A (dispatch-boundary-bounded, no compensation/saga primitive — genuine dyadic C1⊥C10 council). v1.32 also **corrects forward** the §25/C-CP-25 ("WorkflowDriver", not "ValidatorFramework") + §28/C-CP-28 ("ValidatorFramework") identity mislabel that drifted into the v1.29–v1.31 change-note tables — without rewriting the historical notes (delta-only preservation); the contract bodies were never affected.

**Caveats for Phase 7 consumers.** This is a **design-substrate-only** amendment (the CP spec). Implementation is deferred to the coordinated B1 cascade: **B1-spec-1b** (the IS `branch_metadata` sidecar), **B1-spec-2** (runtime driver-strategy materialization + the `RunStatus.PARTIAL` runtime projection `_CP_TO_RT_STATUS` + Literal widen, mirroring v1.45's PAUSED treatment), **B1-plan**, **B1-impl-N** (the 5 strategies, simplest→hardest). Until those land, the driver still gates the 5 patterns to `TopologyPatternNotYetMaterializedError` at runtime — v1.32 specifies the contract, not the materialization. Adjacent Q1 doc-hygiene residuals flagged in §-adjacent observations (the "closed at cardinality 4" stale docstring; the §25.2 PAUSED CP-spec-body gap; the back-catalog of the §25/§28 mislabel) are NOT patched here (FM-2 narrow-scope).

## Notes

- Phase 7 consumers may rely on v1.32 as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
