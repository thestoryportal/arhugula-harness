---
artifact: design-substrate/Spec_Control_Plane_v1_73.md
version: v1.73
cleared_at: 2026-06-26T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_66.md §2 (registered this arc: "B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT … abort just ONE fence-paused branch … while the SIBLING branches … resolve (SKIP_AS_FIRED / RE_FIRE) and the run folds survivors per cascade_policy … likely a new EffectFenceResolution value … whether it sacrifices a committed semantic (ABORT-is-run-terminal) is decided at open") — this delta makes the decided-at-open determination (NOT gated; ABORT stays byte-identically run-terminal) and builds the additive ABORT_BRANCH value.
  - design-substrate/Spec_Control_Plane_v1_65.md §1(b) (the committed ABORT-is-RUN-terminal semantic the gate determination turns on — preserved byte-for-byte) + §1(d) (the PROCEED degrade-and-proceed precedent that probe-resolves the C10 concern inline → no council).
  - harness-cp/src/harness_cp/pause_resume_protocol_types.py (EffectFenceResolution.ABORT_BRANCH — the closed enum 3→4; the per-branch-resolution map docstring refreshed; the run-level-ABORT-vs-scoped-ABORT_BRANCH distinction).
  - harness-cp/src/harness_cp/workflow_driver.py (the scoped-abort interception + dedicated terminal-recording loop + `_scoped_abort_ordinals` all-abort guard + provenance fail_class at BOTH `_execute_parallelization` + `_execute_orchestrator_workers`, incl. the orchestrator empty-`branch_plan` short-circuit at the `not branch_plan` block).
  - .harness/arc-ledger.yaml (B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT registered → closed + registers 1 [B-FANOUT-EFFECT-FENCE-SCOPED-ABORT-CRASH-DURABLE], net-zero; snapshot standalone_closed 53→54, standalone_registered 4→4).
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, BEFORE substantive work + a RECONCILE call after the IS-hash finding):
      (1) BLOCKING gate determination — the v1.66 §2 "decided-at-open" question must be answered from the CLEARED SPEC, not the code comments (the `[[disposition-label-is-a-claim-verify-against-spec]]` trap; the v1.38 per-step-ROLE override is the "looks-additive-but-gated" precedent). GROUNDED: v1.65 §1(b) frames ABORT-is-run-terminal as ABORT's SEMANTIC (the LINEAR analogue), not an independent invariant forbidding failure-isolation; the v1.66 §1 sibling-suppression guard keys on `is ABORT` and PROTECTS that semantic — a distinct ABORT_BRANCH value never matches it; v1.66 §2 EXPLICITLY anticipates "a new EffectFenceResolution value." → NOT operator-gated; matches arc-ledger operator_gated: false.
      (2) the mechanism (enum value, not flag) is illegal-states-unrepresentable (§4); the SubAgentResultStatus 3→4 (PAUSED) precedent is the additive-closed-enum-extension model — own the now-false "no enum change" claim + add a cardinality test.
      (3) the C10⊥C11 nameable-tension check resolves to NO unresolved tension (operator vouches per sibling via the existing key-bind; at-most-once intact; the C10 "proceed-despite-ambiguity" concern is foreclosed by the v1.65 §1(d) PROCEED degrade-and-proceed precedent) → name it inline, DO NOT convene (the §10.9 primary-collapse failure mode).
      (4) RECONCILE after the IS-hash finding — watchpoint #2 ("add a new terminal_status") assumed a CP-local field; `terminal_status` is a `Literal` in the IS schema (state_ledger_entry_schema.py:92) feeding entry_hash.py (§5.2 hash). A new value = an IS-spec + hash + cross-axis arc, breaking the family's CP-only/no-§5.2-IS-hash invariant. RESOLVED: reuse `completed`/no-output (control-flow separation is by-construction — excluded-from-dispatch branches never raise → never set `branch_failed`); carry provenance via the CP-side `_scoped_abort_ordinals` set + a run-level fail_class.
      (5) build-time watchpoints — contrasting-baseline-first (`{ABORT_BRANCH, RE_FIRE}` → sibling FIRES, the exact inverse of the shipped `{ABORT, RE_FIRE}` → suppressed); all-abort → FAILED not vacuous PARTIAL; durable capture for the excluded branch (`[[durable-recovery-presence-validity-scope]]`); ABORT-dominates precedence; iterative re-pause; BOTH fan-out sites through the real TaskGroup.
  - out-of-family Codex — diff review (gpt-5.5, `[[hooks-codex-pilots-decorrelation-validated]]`): **2× [P1] caught (decorrelated from advisor)** — under CASCADE_CANCEL (MULTI_TENANT_COMPLIANCE) tier the cascade-cancel block returns SUCCESS/FAILED BEFORE the §25.15.1 scoped-abort fold, so a scoped-abort resumed on that tier was HIDDEN as SUCCESS (both fan-out sites). FIXED: a scoped-abort guard in BOTH cascade-cancel blocks (parallelization + orchestrator-workers) → FAILED with the provenance fail_class, before the SUCCESS return; directed witnesses `test_*_scoped_abort_under_cascade_cancel_fails` at both sites. (The advisor's decorrelated [P1] — the {ABORT, ABORT_BRANCH} mixed-map ordering — was caught separately: interception-BEFORE-suppression so the scoped-abort branch is deterministically recorded terminal; directed witnesses `test_*_mixed_run_abort_and_scoped_abort_deterministic`.) Re-review of the reworked diff CONVERGED to a single [P2] (the 2× [P1] fixes held): the iterative-re-pause provenance loss (a partial-ABORT_BRANCH PAUSE → later-resolve → final PARTIAL with `fail_class=None`). RESOLVED by the FAILED-only-provenance decision below (not by carrying a marker): the codebase reads `fail_class` gated on `status is FAILED` and EVERY existing PARTIAL return uses `fail_class=None`, so a PARTIAL+fail_class broke convention AND was the source of both the iterative-loss + the advisor's misread-risk #4 → the provenance fail_class is now set ONLY on the FAILED returns (all-abort + cascade-cancel); a PARTIAL with survivors carries `fail_class=None` like every degraded PARTIAL (the aborted branch is a degraded terminal non-contributor recorded per-branch in the ledger; run-result operator-abort-vs-errored provenance on a PARTIAL is the pre-existing degraded-disposition observability limit, out of scope). advisor (#4: acceptable-if-noted) + Codex ([P2]: fix) reconciled on this convention-consistent resolution. A THIRD Codex re-review found a further [P2] (decorrelated): the scoped-abort DURABLE recording loop ran BEFORE the pre-existing strict-tier guard that rejects a PROCEED-tier effect-fence resume → an ABORT_BRANCH resume under PROCEED persisted a `completed` terminal/capture for a resume then rejected → corrupt later strict/crash recovery. FIXED: the recording loop is PROCEED-guarded at both sites (no durable write under PROCEED — fail-closed precedes durable writes, `[[durable-recovery-presence-validity-scope]]`) + an early strict-tier rejection at the orchestrator `not branch_plan` short-circuit (which returns before the existing guard) so an all-scoped-abort PROCEED resume reports `requires-strict-tier`. Directed witnesses `test_*_scoped_abort_under_proceed_*` (mixed + all-abort) at both sites. The FOURTH re-review found a FIFTH [P1] (decorrelated): the reuse-`completed` durable record is INDISTINGUISHABLE from a ran-and-errored branch in the crash-resume store → an all-scoped-abort resume that CRASHES after the captures reconstructs PAUSED→PARTIAL not the all-abort FAILED. advisor reframed rounds 3/4/5 as ONE root cause (reuse-`completed` cannot carry the scoped-abort semantic across the DURABLE boundary) → the discriminator: a distinguishable-durable-record fix splits the disposition vocabulary (replay-store 4 / IS-ledger 3) → not self-contained → round-6 risk. DECISION (FULL-SPEC, advisor-led): REGISTER a named BUILD-arc `B-FANOUT-EFFECT-FENCE-SCOPED-ABORT-CRASH-DURABLE` (NOT fix-now, NOT accept-and-document). Execution-reproduced: the divergence is REAL (the crash-resume hits `_crash_pause_reestablish`, never the all-abort FAILED) + STATUS-FIDELITY ONLY — NO double-fire (`re-dispatched=[]`). standalone_registered 4 → 4 net-zero (close 1 + register 1, the v1.66 shape).
  - direct grounding: the runtime §14.22 fence gate (runtime_tool_dispatcher.py:936-1001) switches on RE_FIRE/SKIP_AS_FIRED/ABORT with a default no-resolution fall-through → ABORT_BRANCH is handled ENTIRELY CP-side (never threaded to the runtime → no runtime delta); both fan-out sites' branch-plan loops + post-barrier folds (parallelization ~5955/6601/6835; orchestrator ~8386/9157 + the `not branch_plan` short-circuit ~8640).
  - by-execution witnesses (harness-cp): test_workflow_driver_parallelization_pause.py + test_workflow_driver_fanout_pause.py — the CRUX contrasting baseline `{ABORT_BRANCH, RE_FIRE}` → sibling FIRES + run PARTIAL (vs the shipped `{ABORT, RE_FIRE}` → suppressed + FAILED); all-abort → FAILED not vacuous PARTIAL; iterative re-pause (the scoped-abort branch becomes a terminal `branches` entry, the unresolved sibling re-pauses in `effect_fence_paused_branches`); at BOTH PARALLELIZATION + ORCHESTRATOR_WORKERS through the REAL `_execute_*` TaskGroup; the {ABORT, ABORT_BRANCH} mixed-map deterministic-exclusion (advisor [P1]) + the scoped-abort-under-CASCADE_CANCEL → FAILED (Codex [P1]) at both sites. the scoped-abort-under-PROCEED → requires-strict-tier rejection (mixed + all-abort) at both sites (Codex [P2] durable-corruption fix). Cardinality test `test_effect_fence_resolution_cardinality_four`. Full suites: harness-cp 1385 passed / 1 xfailed; harness-runtime fence tests 33 passed; pyright 0/0/0; ruff clean.
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.73`

v1.73 builds the R-FS-1 standalone arc **`B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT`** (registered at v1.66 §2) as a WHOLE close: a fourth `EffectFenceResolution` value `ABORT_BRANCH` that fails JUST one fence-paused fan-out branch (record it terminal, never re-dispatched) while the SIBLINGS the operator vouched for (SKIP_AS_FIRED / RE_FIRE) fire and the run folds survivors per `cascade_policy` (PARTIAL with survivors; FAILED if none survive). It is the strictly-narrower disposition beside the run-level `ABORT` (which suppresses ALL siblings + fails the run — v1.65 §1(b), preserved byte-for-byte; the `_any_fence_abort` guard keys on `is ABORT`, so `ABORT_BRANCH` never triggers it).

The decided-at-open gate determination (the v1.66 §2 open question) is **NOT operator-gated**, grounded in the cleared spec: the committed semantic the arc could sacrifice is ABORT-is-RUN-terminal, and the additive `ABORT_BRANCH` value leaves it byte-identical → no committed-invariant sacrifice (the v1.38 per-step-ROLE-override disanalogy: there an explicit §14.5.3 invariant 2 forbade the new field; here v1.66 §2 explicitly anticipated a new value). The C10 (action-safety) concern is probe-resolved inline by the v1.65 §1(d) PROCEED degrade-and-proceed precedent → no council. CP-only — `ABORT_BRANCH` is consumed CP-side at the two fan-out resume sites and NEVER reaches the runtime §14.22 fence gate (no runtime delta); the scoped-abort branch reuses the IS-hash-bearing `completed` `terminal_status` (no §5.2 IS-hash change, no cross-axis cascade). At-most-once is PRESERVED (the scoped-abort branch is never re-dispatched → its ambiguous effect is never re-fired).

WHOLE close — registers no follow-on. `closure_gate.py` G1.1 (`standalone_registered + standalone_gated == 0`) moves 4+0 → 3+0; R-FS-1 stays ACTIVE (the 3 remaining: the two recursive-child MAYBE-RAN-SUBAGENT legs + the operator-gated ORCHESTRATOR-PROCEED-RESIDUAL).

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
