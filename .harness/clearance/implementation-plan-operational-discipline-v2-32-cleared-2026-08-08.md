---
artifact: design-substrate/Implementation_Plan_Operational_Discipline_v2_32.md
version: v2.32
cleared_at: 2026-08-08T00:00:00-06:00
clearance_type: ratified-fork-apply-pass
back_reference:
  - design-substrate/Spec_Operational_Discipline_v1_37.md §1 — the C-OD-09 §9.2 row 19 this unit realizes; cleared at `.harness/clearance/spec-operational-discipline-v1-37-cleared-2026-08-08.md` (SAME PR)
  - design-substrate/Spec_Harness_Runtime_v1.md v1.112 §14.6.3 term **t3′** — the NAMED closure gate `B-116-t3`
  - .harness/council/b116-breaker-semantics/01-council/contributions/C-c7-reconcile.md — the "t3-leg registration" block's pricing (`sampling_mode.py` 18 → 19; four fixture sites / five edit points; disjointness verified NOT to move), CORRECTED at this plan's §0.5 and EXTENDED at §0.6
  - .harness/forward-register.yaml row `B-116` (status `open` → `closed` at this leg)
  - PR '#pending' (this arc)
merge_commit: pending
reviewer_chain:
  - implementation-planner apply pass — ONE new atomic unit (**U-OD-58**), ZERO amended units, ZERO new contract IDs, ZERO new cluster, ONE new intra-axis DAG edge, ZERO cross-axis edges, ZERO CXA rows. **U-OD-11 is NOT amended**, per the `B-97`(a) → U-RT-149 and `B-96` → U-RT-150 precedent that a landed unit's acceptance criteria are not retroactively re-scoped by a later contract amendment.
  - "empirical grounding pass — **the unit ID was verified free before minting**: no `U-OD-58` occurrence anywhere in `design-substrate/` or `.harness/`; the max prior OD unit is `U-OD-57` (v2.31). **The dependency was verified from the FILE rather than assumed**: `harness-od/src/harness_od/sampling_mode.py` names its owner in its own docstring line 1 (`\"…always-sampled set — U-OD-11\"`) and its Authority block cites `Implementation_Plan_Operational_Discipline_v2_5.md §3.4.1 U-OD-11`, so the declared edge is **U-OD-11 → U-OD-58**."
  - pricing corrected, not carried — §0.5 records that FOUR of the council's five named edit points carry a literal that must move and the FIFTH (`test_sampling_mode.py:98`, the set-equality assertion) does NOT, because both operands move together; only its docstring count claim moves. §0.6 records TWO shipped `src/` count claims the pricing never covered, found by sweep (`alignment_floor_drift_detection.py` "18 rows" ×2; `substrate_seam_exports_aggregate_manifest.py:203` `export_name`), both reconciled under this unit. `bridging_arc_table.py:34` was checked and carries NO count — correctly untouched.
  - "PD-8 executed at authoring, and one probe FALSIFIED an assumption — probe (iii) (*revert one fixture literal only*) came back **GREEN, 96 passed / 0 failed**, because `_LITERAL_ALWAYS_SAMPLED` is a hand-maintained `parametrize` argument with no completeness assertion behind it: dropping a member merely generates fewer cases. §0.7 records the gap and CLOSES it in-unit with `test_literal_fixture_is_complete_against_the_canonical_literal_arm` (one assertion, inside this unit's own file set), after which probe (iii) FAILS as required. Probes (i) and (ii) were load-bearing as designed — (i) 7 failures incl. both `len == 19` witnesses, the set-equality witness and both new parametrized cases; (ii) failed the AC #4 disjointness witness, the arm that proves the event entered the RIGHT regime rather than merely a regime. All restorations by pre-probe file copy, never `git checkout`."
  - "out-of-family Codex round 1 (against commit `9af64d37`) — **[P1] adjudicated TRUE-but-PRE-EXISTING → NEW row `B-133` minted** (both §9.2 consumers classify span NAMES — `composite_sampler.py:110`, `tail_keep_span_processor.py:259` with zero `.events` inspection — while three members are span EVENTS, two of them so since original ingestion; the reviewer's redesign remedies rejected here as an unpriced X-AL-3 extension / an over-sampling of every dispatch wrapper). **[P2] accepted → the spec's rider-(a) paragraph reworded** to its structural core with the population claim retracted in-text. **U-OD-58's ACs are UNCHANGED by both** — roster, fixtures, disjointness and PD-8 are decision- and conformance-level and none asserted an end-to-end outcome — but §1.2's scope statements were **sharpened** so the unit cannot be read as delivering the realized floor: AC #3 now says explicitly that it asserts the sampling DECISION and not survival to a backend; the `B-116`-gate bullet now says **discharges the membership half** (the content §14.6.3 t3′ and the council's pricing named) rather than \"closes the observability floor\"; and a new bullet names end-to-end realization as **NOT owned**, at `B-133`. Recorded at plan §0.6a."
  - "gate result at the final commit — `just check`: `ruff check` clean, `ruff format --check` clean (1000 files), `pyright` **0 errors / 0 warnings**, `docs_completeness --check` ready, `memory_closeout_check --check` ready, `closure_certification --check` ready, `forward_register.py --check` green (132 items: **0 open** / 29 registered_finding / 1 held / 102 closed), `arc_ledger.py --check` green (**zero arc-ledger rows owed** — the `B-109` register-closure precedent holds), `just roadmap-status-check` OK. Test lane **7588 passed / 10 skipped**, with the TWO documented pre-existing `test_pause_journal_enumeration_b97a.py` failures caused by a gitignored local `harness.toml`. **Causality was EXCLUDED empirically, not by inspection** — `harness_runtime` genuinely imports two of the modules this arc edits (`config/otel_config.py` ← `sampling_mode`; `bootstrap/stage_6_cxa_wiring.py` + `lifecycle/od_cp_wiring.py` ← `substrate_seam_exports_aggregate_manifest`), so a revert-probe was run: all five OD files restored to HEAD, the two tests **still fail identically**, changes restored, suite re-verified green."
  - "collected-test delta measured programmatically (the `B-117` duplicate-basename hazard) — **7595 → 7600 non-e2e collected (+5), 7631 → 7636 total**, fully accounted: three new named witnesses plus two parametrized cases the new `_LITERAL_ALWAYS_SAMPLED` member generates (`test_is_always_sampled_matches_literal_entries[fallback.exhausted]`, `test_always_sampled_literal_at_base_rate_zero_still_samples[fallback.exhausted]`). No witness was displaced."
  - "scope verification by execution — AC #4 is satisfied by RUNNING `test_base_rate_set_and_envelope.py` **unmodified** (the council predicted it would not move; this leg verified it rather than carrying the prediction). AC #8 is asserted by absence: the single `fallback.exhausted` emission site (`retry_breaker_fallback.py:947`) and every `harness-cp/` file are untouched in the arc diff — *this unit moves a sampling disposition; if the diff reaches an emission site, the scope claim is false*."
supersedes: implementation-plan-operational-discipline-v2-31-cleared-2026-07-31.md
---

# Clearance — `Implementation_Plan_Operational_Discipline_v2_32.md v2.32`

v2.32 is the OD plan leg of the **`B-116-t3`** arc, carrying **ONE NEW atomic unit, U-OD-58** — the realization of OD spec v1.37 §9.2 row 19 (`fallback.exhausted`) at the shipped substrate, with eight acceptance criteria: the roster addition and its byte-exact conformance to the v1.37 table (both directions); the five fixture edit points enumerated exhaustively; a member-specific witness **at the discriminating multi-tenant cell** (`base_rate=0.2` — the only cell where §9.2 membership changes the answer); the disjointness witness verified UNMOVED by execution; the literal/prefix decomposition at 17 + 2 with the fixture asserted complete against it; the two swept `src/` count claims; PD-8 in three arms; and the zero-emission-site / zero-CP / zero-CXA scope claim asserted by absence.

**Nothing is deferred to a later leg** — the contrast with v2.31 is deliberate and stated in §0.2. v2.31's spec predecessor held the carrier's *shape* to impl discretion, so the plan leg had to wait for the choice. Here the shape is fully determined by the §9.2 table itself (one unconditional literal member, no new attribute, no new call site), and the two artifacts are coupled by a live CI assertion — `ALWAYS_SAMPLED_EVENT_CLASSES` is asserted byte-exact against the table — so spec, plan and code land in one commit or not at all.

**Two things this leg found rather than inherited**, both recorded in the plan body so a reviewer can overturn them: the council's five-edit-point pricing is off by one in kind (§0.5), and a witness gap sits behind the composite-sampler fixture that made a PD-8 arm unsatisfiable until it was closed (§0.7). The second is the more consequential — it means any future §9.2 amendment could have left the new member's SDK-boundary resolution entirely unexercised.

Note for Phase 7 consumers: U-OD-58's `Consumed by (cross-axis)` is **NONE**. The amended set is OD-owned and OD-consumed, which is the determined (not assumed) ground for the zero-CXA disposition.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
