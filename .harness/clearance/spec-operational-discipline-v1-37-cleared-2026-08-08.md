---
artifact: design-substrate/Spec_Operational_Discipline_v1_37.md
version: v1.37
cleared_at: 2026-08-08T00:00:00-06:00
clearance_type: ratified-fork-apply-pass
back_reference:
  - design-substrate/Spec_Harness_Runtime_v1.md v1.112 §14.6.3 term **t3′** — the NAMED closure gate `B-116-t3` this delta discharges ("the full floor guarantee is the separate B-116-t3 leg (OD §9.2 roster 18 → 19 plus its sampling fixtures, priced at the council record C-c7-reconcile.md)"); cleared at `.harness/clearance/spec-harness-runtime-v1-112-cleared-2026-08-08.md`, merged PR #1269
  - .harness/council/b116-breaker-semantics/01-council/contributions/C-c7-reconcile.md — §F-03 (the direction ruling on the ingestion chain + the §9.3 "inviolable" reconciliation), §F-08 (the rider-(a) cardinality-budget check registered as owed at this leg), and the closing "t3-leg registration" block (the pricing)
  - operator ratification 2026-08-07 — Reading (II), over the council-unanimous DELIVERABLE at `.harness/council/b116-breaker-semantics/`
  - .harness/forward-register.yaml row `B-116` (status `open` → `closed` at this leg — §14.6.3 t3′ required BOTH the impl leg U-RT-152, merged PR #1271 at commit 3ba68285, AND this t3 leg)
  - PR '#pending' (this arc)
merge_commit: pending
reviewer_chain:
  - ratified-fork apply pass — applies the gate `Spec_Harness_Runtime_v1.md` v1.112 §14.6.3 t3′ REGISTERED, at the direction the council record grounded, and EXTENDS NOTHING beyond it. The amendment's content is fixed by the §9.2 table's own shape (one unconditional literal row, ingesting a disposition CP already declares); the one open item the record left — rider (a) — was EXECUTED at this leg rather than deferred, and its finding is recorded at contract altitude so a reviewer can overturn rather than discover it.
  - empirical grounding pass at this leg — every cite re-read directly at HEAD `c9ce5c8c` before it was written. **The §9.2 owning contract was verified from the FILE, not from the prompt or the council record**: the enclosing heading at `Spec_Operational_Discipline_v1_2.md:490` reads `## §9 C-OD-09 — Sampling discipline: head-based-dev / tail-based-prod + always-sampled set`, so **C-OD-09 owns §9.2** — the council record's `C-OD-09` attribution is CONFIRMED, no discrepancy. The table was counted row-by-row at `:515-532` = **EXACTLY 18** (header at `:513-514`). The CP source cite was verified byte-exact at `Spec_Control_Plane_v1_2.md:410` (`| `fallback.exhausted` | **Always-sampled (head=1.0)** — chain exhaustion is reliability-critical |`), with its two absorbed siblings at CP `:409`/`:411` → OD `:521`/`:522` and the declaring-contract preamble at CP `:397`. The §9.3 "inviolable" text was re-read at `:538`. The whole OD delta chain v1.3…v1.36 was swept for a §9.2 re-table — **none exists**; the only later mention is `v1_27.md:19`, a timestamped "Status at HEAD pre-v1.27" carrier-landscape row, which is PRESERVED VERBATIM and flagged in the change-note as historical rather than stale.
  - rider (a) EXECUTED, not deferred — the C7 reconcile's F-08 registered the multi-tenant per-cell cardinality-budget check as owed BEFORE this leg lands. Run at this filing against the real carriers: the single emission site (`retry_breaker_fallback.py:947`, outer span, once per fully-exhausted dispatch — re-grounded at HEAD; the council's `:895-898` predates PR #1271) and the budget (`per_cell_cardinality_budget.py`: `cell_rate_limit=10_000.0` at every ACTIVE cell, `tenant_rate_limit=1_000.0` at the two multi-tenant cells via `_tenant_rate_limit_for`, enforced at COLLECTOR_BOUNDARY / BACKEND_INGESTION). **PASSES** on the structural ground that enforcement sits downstream of and independent of the sampling decision; the F-08 population question is left explicitly UNMEASURED, consistent with C7's own FM-K refusal, and no term depends on it.
  - riders (b)/(c)/(d) cross-referenced to the already-minted rows `B-123` / `B-124` / `B-125` and NOT re-minted — each verified present in `.harness/forward-register.yaml` by direct read before being cited. Rider (c)'s subject (the inert `validator.fail.permanence` §9.2 row `:530`) is stated in-text precisely because it sits inside the amended table and would otherwise read as an unexamined inconsistency.
  - out-of-family Codex round 1 (against commit `9af64d37`) — **TWO findings, both adjudicated empirically and both absorbed; neither changes the §9.2 row.** **[P1] the always-sampled floor is unrealized for `fallback.exhausted` because both consumers classify span NAMES while the event is a span EVENT — VERIFIED TRUE** (`composite_sampler.py:110` `is_always_sampled(name, attributes)`; `tail_keep_span_processor.py:259` `is_always_sampled(span.name, span.attributes)`, module contains ZERO `.events` occurrences) **but ALSO verified PRE-EXISTING**: the two CP-declared siblings `fallback.triggered` (`retry_breaker_fallback.py:688-689`) and `breaker.tripped` (`harness_breaker_schema.py:282`) are span EVENTS too and have been since the ORIGINAL OD ingestion of §9.2 — so this is a property of the floor MECHANISM, not exposure created by this delta, and the membership contract plus the ingestion-chain direction are unaffected either way. The reviewer's remedies were **REJECTED at this leg**: a dedicated span mints a new emission primitive no cleared spec authorizes (an X-AL-3 extension routing through §4.3 back-flow, not an impl fix), and always-sampling the wrapper would over-sample every dispatch and silently rewrite the §10.3 envelope. **Disposition: REGISTERED as NEW forward row `B-133`** (same family as `B-123` / `B-124`, whose root cause it shares) **plus a new "Honest scope" change-note section stating the bound at contract altitude** — registration IS the non-silent disposition. **[P2] the rider-(a) paragraph's *"bounded far below it"* is an unmeasured population claim — ACCEPTED.** The wording overstated: at a sufficiently high per-tenant exhaustion rate the marginal keep-volume could approach the 1,000 spans/sec budget, and asserting otherwise is exactly the population claim C7's own FM-K refusal forbids. The paragraph is **REWORDED to the defensible structural core** (the budget enforces at COLLECTOR_BOUNDARY / BACKEND_INGESTION independently of any sampling decision, so membership cannot ADMIT throughput past the caps — sampling governs which spans are KEPT within the admitted stream, not the admitted rate; and the event is once-per-exhausted-dispatch, so membership adds no multiplicity), with the retraction stated in-text rather than quietly softened, and the population question carried at `B-133` close-out step (3) — sequenced deliberately AFTER realization, since measuring keep-volume before the floor is realized measures the wrong thing.
  - scope verification by execution — ZERO new contract numbers (C-OD-09 pre-exists; this amends its own table at the venue where the set is defined); C-OD-05 §5.1 namespace roster UNCHANGED at 15; C-OD-10 §10.1 base-rate set UNCHANGED at 13 and §10.2 triggers at 3; C-OD-11 §11.1 budget UNCHANGED; ZERO CP edit; ZERO Runtime text moved; ZERO CXA rows (aggregate frozen at 111 — determined, not assumed: the amended set is OD-owned and OD-consumed, so no cross-package consumption is introduced and nothing is owed); ZERO hash impact; ZERO emission-site change (asserted by absence in the arc diff).
supersedes: spec-operational-discipline-v1-36-cleared-2026-07-30.md
---

# Clearance — `Spec_Operational_Discipline_v1_37.md v1.37`

v1.37 is the **`B-116-t3`** OD leg — the NAMED closure gate Runtime v1.112 §14.6.3 term **t3′** registered against the ratified `B-116` package. It carries **exactly ONE amendment**: the **C-OD-09 §9.2 always-sampled exception set gains ONE row, 18 → 19**, adding `fallback.exhausted` with source declaration `C-CP-03 §3.5` and the rationale *reliability-critical — terminal once-per-exhausted-chain event whose CP-declared always-sampled disposition was dropped at original OD ingestion*. §9.1, §9.3, the §9 header block and the §9 deferral footer are **UNTOUCHED**; §9.2's other **eighteen** rows are **PRESERVED VERBATIM**.

**The gap this closes is spec-vs-spec, not code drift** — a distinction the change-note makes explicitly, because the "drift correction" framing would price the leg as conformance work owing no amendment, and no amount of conformance work can put a row into a table the table does not have. `fallback.exhausted` was in **neither** OD regime: not among §9.2's eighteen, not among C-OD-10 §10.1's thirteen. The repair direction (OD 18 → 19, not a CP demotion) rests on three stated grounds — the CP-declares/OD-ingests authority chain at CP `:397`, the demote direction costing two tables anyway, and head=1.0 being the only guarantee that does not depend on the possibly-inert tail-keep trigger (`B-123`) and that also binds at head-based local-development.

**§9.3's "inviolable" was reconciled rather than treated as a bar.** It governs **operator tunability at the deployment-binding layer**, not contract revision at the venue where the set is defined — the C7 reconcile's F-03 ruling, adopted here. What it does establish is why **membership** is the only durable repair: a member cannot be tuned down, so an out-of-table mitigation could not deliver the guarantee t3′ is owed.

What was reviewed during clearance: the §9.2 table counted row-by-row from the file; the owning contract number read from the enclosing heading rather than taken from the prompt or the record; the CP source cite verified byte-exact; the full OD delta chain swept for a later §9.2 re-table; rider (a) executed to a recorded finding; riders (b)/(c)/(d) verified present as register rows before citation. Deferrals: none introduced — the §9 deferral footer is preserved unchanged and this delta adds no deferral of its own.

Note for Phase 7 consumers: unlike v1.36, **nothing here is held for a later impl leg**. The row's realization is fully determined by the table, so `Implementation_Plan_Operational_Discipline_v2_32.md` (U-OD-58) and the substrate change land in the SAME PR. `ALWAYS_SAMPLED_EVENT_CLASSES` is asserted byte-exact against this table by a live CI witness, so spec and substrate cannot be landed apart.

## Notes

- Phase 7 consumers may rely on this version as canonical until a successor marker is filed.
- See `.harness/clearance/README.md` for marker discipline.
