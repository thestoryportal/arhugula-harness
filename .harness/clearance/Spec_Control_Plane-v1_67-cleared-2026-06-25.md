---
artifact: design-substrate/Spec_Control_Plane_v1_67.md
version: v1.67
cleared_at: 2026-06-25T00:00:00-06:00
clearance_type: Phase-7-absorbed-via-bundled-absorption
back_reference:
  - design-substrate/Spec_Control_Plane_v1_65.md §3 (registered this arc as "the maybe-ran MANAGED_AGENTS worker; UNFENCED vendor-session sink → needs the C-RT-31 fence EXTENDED to the managed-agents sink" — this delta builds exactly that, the CP consumer half)
  - design-substrate/Spec_Harness_Runtime_v1.md §14.22 (the C-RT-31 effect fence) + §14.20 (the C-RT-28 managed-agents dispatcher) — the PRIMARY paired delta v1.81 → v1.82 extends the fence to the managed-agents sink
  - design-substrate/Spec_Control_Plane_v1_65.md §1 (the v1.62 B-FANOUT-EFFECT-FENCE-BRANCH-PAUSE TOOL_STEP fence-recovery classifier this delta extends to MANAGED_AGENTS + the same-kind guard)
  - harness-cp/src/harness_cp/workflow_driver.py (_FANOUT_MAYBE_RAN_FENCE_RECOVERABLE_KIND_VALUES gains MANAGED_AGENTS + the same-kind-equality conjunct in _fence_unrecoverable_maybe_ran_indices)
  - .harness/arc-ledger.yaml (B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL registered → closed, NO new registration; snapshot standalone_closed 47→48, standalone_registered 6→5)
  - .harness/clearance/Spec_Harness_Runtime-v1_82-cleared-2026-06-25.md (the paired primary runtime delta)
merge_commit: <filled at merge>
reviewer_chain:
  - advisor (full-transcript, before substantive work — the load-bearing scoping call):
      (1) CONFIRMED the pick of this arc over a fresh attempt at the reverted #746 SUBAGENT slice (which needs a non-existent non-mocked recursive-child harness — the sunk-cost / loop-wants-progress trap the #746 finding names).
      (2) Established the SOUNDNESS argument: the recursive-RESULT-fidelity [P1] that reverted #746 does NOT transfer — MANAGED_AGENTS is a LEAF (one opaque vendor effect, no harness-side reconstruction), so a fence suppress folds the captured outcome verbatim → result-fidelity holds by construction.
      (3) FLAGGED the real risk surface: NOT create+send, but the disposition matrix — non-success terminal / poll-exhausted raise WITHOUT capturing → the claim is held + no output → ambiguous → §26.2 PAUSE → the existing per-branch resolution; and "what does the parent fold for a SKIP_AS_FIRED branch with no captured output" (the empty-but-keyed outcome cell). Verified by-execution, not assumed.
      (4) Named the two build constraints that became the load-bearing correctness conjuncts: the CP same-kind guard (MANAGED_AGENTS in BOTH conjuncts + marker==resumed) + the payload-independent fence key (agent-swap suppressed).
  - direct grounding (presence-not-correctness): the fan-out worker dispatch is kind-generic (dispatcher.dispatch routes MANAGED_AGENTS to ManagedAgentsStepDispatcher) → a fan-out MANAGED_AGENTS worker is reachable, the arc is NOT hollow; compose_branch_child_context preserves run_engine_class + parent_idempotency_key via model_copy → a fan-out worker's step_context carries the fence inputs.
  - out-of-family Codex — diff review (gpt-5.5, 3-round convergence, `[[hooks-codex-pilots-decorrelation-validated]]`): **R1 [P1] (FIXED)** a pyright reportRedeclaration in the classifier test helper (a local dict reused the `resumed` parameter name → the typecheck gate would block CI even though pytest passed); renamed to `resumed_map`. **R2 [P2] (FIXED)** the fence-key collision — the first-draft key `H(parent : step : managed_agents)` was byte-identical to a TOOL_STEP whose `tool_id == "managed_agents"` at the same (parent, step_id), and both dispatchers share `.harness/effect-fence` → cross-sink fence contamination. FIX: lead the digest with the `managed_agents` domain tag (`H(managed_agents : parent : step)`) — disjoint from every tool key by construction; added a regression guard asserting the disjointness. **R3 — clean** ("No discrete correctness issues were found in the reviewed diff").
  - by-execution witnesses: harness-runtime 14 real-fence dispatcher witnesses through a counting vendor port (suppress / ambiguous / fresh-fire / SKIP-empty / ABORT / RE_FIRE / non-success-leaves-ambiguous / poll-exhausted-leaves-ambiguous / gate-closed-fires-every / explicit-opt-in / durable-across-fresh-instance / agent-swap-suppressed / tool-key-disjoint / drift-guard); harness-cp 8 classifier units (incl. cross-kind fail-closed) + 2 full-chain witnesses (MANAGED_AGENTS recovers + cross-kind-swap fails closed) + the 5 prior fail-closed full-chain tests switched MANAGED_AGENTS → SUB_AGENT_DISPATCH (the still-unfenced effect-bearing kind); regression sweep harness-runtime 238 passed / 3 e2e-skipped + harness-cp 511 passed / 1 xfailed; pyright 0/0/0; ruff clean.
  - process-gate note: the `codex-closeout` ROOT_CHECKOUT_EDIT + DESIGN_IMPL_MIX HARDs are artifacts of this session working in-place in the main checkout (per its config — NOT a worktree) + pre-existing untracked `dashboard-design/**` + `.agents/**` clutter (DIFFERENT efforts, excluded from this PR); the actual Codex model review surfaced normally. The roadmap_status.md + roadmap.html hash refresh is the §12.2 post-merge terminating-refresh PR (the expected pre-merge divergence).
supersedes: <none>
superseded_by: <none>
---

# Clearance — `Spec_Control_Plane v1.67`

v1.67 is a **bundled-absorption** delta over v1.66 (co-published with `harness-cp` + `harness-runtime` impl + tests + the paired runtime delta v1.82) — the build of the registered R-FS-1 arc `B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL`.

- **§1 — MANAGED_AGENTS fence-recoverable classification + the same-kind guard.** `_FANOUT_MAYBE_RAN_FENCE_RECOVERABLE_KIND_VALUES` grows from `{TOOL_STEP}` to `{TOOL_STEP, MANAGED_AGENTS}`; `_fence_unrecoverable_maybe_ran_indices` gains a same-kind-equality conjunct (marker kind == resumed kind) that closes the cross-kind hole a >1-kind set opens. A maybe-ran MANAGED_AGENTS branch whose resumed manifest is still MANAGED_AGENTS recovers (re-dispatches into the §14.22 vendor-session fence); a cross-kind TOOL_STEP ⇄ MANAGED_AGENTS swap fails closed.
- **§2 — the runtime-paired fence extension (the primary half).** The §14.20 ManagedAgentsStepDispatcher gains the §14.22 fence (reserve-before-create_session, capture-after-success, the lost-reserve directive/captured-output split) keyed on the payload-independent leading-domain-tag `H(managed_agents : parent : step)`. The disposition matrix (non-success / poll-exhausted / cancel-on-giveup) routes to ambiguous-PAUSE + the existing per-branch resolution.
- **§3 — WHOLE close, no new registration.** MANAGED_AGENTS is a LEAF (result-fidelity by construction, unlike the reverted #746 SUBAGENT recursive-child slice) → no residual → `standalone_registered` 6 → 5 (the second net decrement of the family after #738). G1.1 = 5 + 0 → R-FS-1 stays ACTIVE.

## Caveats for Phase 7 consumers

- Bundled-absorption: ADDITIVE. NO contract removal / closed-enum / committed-invariant change / new fail-class. MANAGED_AGENTS is an existing `StepKind` (no enum change). At-most-once is PRESERVED + STRENGTHENED (MANAGED_AGENTS goes from fail-closed to fence-recoverable; the same-kind guard prevents cross-sink mis-recovery; the payload-independent key suppresses a resumed agent-swap). The §26.2 PauseSnapshot envelope + the v1.65/v1.66 per-branch resolution machinery + the entire C-CP-01 … C-CP-29 body PRESERVED LITERALLY. No §5.2 IS-hash / §16.5 key change.
- **Paired runtime delta v1.82 is the PRIMARY half** — the fence extension itself lives runtime-side; this CP delta is the consumer classifier. The two land together (the classifier would be a `[[wired-handler-unreachable]]` half-mechanism without the fence).
- **The barrier-catch seam is GENERIC over the exception (verified by read, advisor-directed `[[full-chain-witness-not-half-proofs]]` check).** A managed-agents dispatch raising `EffectFenceAmbiguousUncommittedError` in a real fan-out rides the #742-witnessed TOOL_STEP path UNCHANGED: the in-flight + own-dispatch barrier catches (`workflow_driver.py:6136` / `:3370`) name-match the exception type and capture `getattr(exc, "idempotency_key", None)` GENERICALLY — keyed on the exception, NOT on `StepKind.TOOL_STEP` — so the managed-agents fence key flows into `effect_fence_paused_dispositions` → the §26.2 pause → resume → the per-branch directive key-matches the managed key → SKIP/RE_FIRE applied, identically to a tool. The dispatcher-level real-fence witnesses (suppress/ambiguous/SKIP/RE_FIRE/ABORT through the REAL `RuntimeEffectFence` + counting vendor port) + the full-chain `_CountingDispatcher` recovery/cross-kind witnesses therefore cover the two halves of one generic seam; a separate real-fence managed-agents full-chain witness would re-prove #742's already-witnessed generic path.
