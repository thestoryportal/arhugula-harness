# Adversarial Review — U-RT-60 pre-implementation (Phase-7 mode)

## Summary

- **Mode:** Phase-7 pre-implementation review per `harness-adversarial-reviewer` skill §"Activation discipline"
- **Artifact reviewed:** Runtime plan v2.7 L9-quater section (U-RT-60 body; 14 ACs) at `.harness/phase-2-session-3-track-a-atomic-decomposition.md`; reviewed against spec contract C-RT-18 at `design-substrate/Spec_Harness_Runtime_v1.md` §14.8 (v1.9) + `design-substrate/Cross_Axis_Composition_Document_v2_5.md` §2.3.7
- **Reviewed at:** 2026-05-21 (worktree branch `worktree-u-rt-52-close`; plan HEAD `fdaf1d7`; spec HEAD `2685774`)
- **Finding count by class (§4.1 review-severity scale):** Class 3: 1 · Class 2: 3 · Class 1: 3
- **Highest-severity finding:** F3-01 (`AskUserQuestionSurface` H_E binding mechanism under-specified)
- **Disposition recommendation:** **Fork to spec revision (§4.1.3 Class 3 path) — proposing.** F3-01 absorbs cleanly into a single Class-2 spec amendment if operator picks one of the three candidate H_E transport mechanisms surfaced in the finding; if operator declines, the implementation arc opens with a known load-bearing gap that will surface as a §2.7.6 Class 1 execution-time fork at the bootstrap-stage-5 wiring step. F2-01/02/03 + F1-01..03 absorb as a single plan v2.7 → v2.8 minor revision in either branch.

---

## Class 3 findings (severe — phase re-opening)

### F3-01 — `AskUserQuestionSurface` H_E binding mechanism under-specified

- **Location:** Spec `Spec_Harness_Runtime_v1.md` §14.8.3 (line 1504); §14.8.1 item 2 (line 1455–1459); spec "Deferred to implementation discretion" list (line 1605). Plan v2.7 AC #2 (L9-quater body).
- **Defect:** Both spec §14.8.3 and plan AC #2 commit to "an H_E-backed implementation that wraps Claude Code's `AskUserQuestion` mechanism" but neither pins the **transport mechanism** by which Python runtime code (`harness-runtime/lifecycle/hitl_gate_composer.py` invoking `await ctx.ask_user_question_surface.ask(...)`) reaches the Claude Code agent loop and back. The H_E-direct vs MCP-server vs convention substitution-category choice (per `Phase_7_Meta_Architecture_v1.md` §5 6-category catalogue) is load-bearing for: (a) the bootstrap stage-5 binding code shape, (b) the AskUserQuestionResult await-semantics (single-process callback? stdout-marker + agent-fill-in? MCP request-response?), (c) the H_T-CP-20 retirement-criterion B reading at §14.8.3 ("H_E surface IS still invoked at the H_E binding"). Spec defers WHEN (stage 5 vs earlier) at the "Deferred to implementation discretion" list but does NOT defer HOW.
- **Discriminator that classifies as Class 3:** (b) requires upstream-phase artifact revision. The mechanism choice is a substitution-category architectural decision per `Phase_7_Meta_Architecture_v1.md` §5; the §14.8.3 narrative needs a single sentence pinning the category before the implementation arc opens. Resolution at the implementation arc without spec amendment = silent absorption of a §2.7.6 Class 1 architectural surface per workspace `CLAUDE.md` §4.3 + X-AL-3 (no silent H_T design extension at Phase 7).
- **Evidence:** Spec line 1458 reads "Bound at bootstrap stage 5 to an H_E-backed implementation that wraps Claude Code's `AskUserQuestion` mechanism" — no further pin. Spec line 1605 deferred-list entry reads "Whether `AskUserQuestionSurface` is constructed at bootstrap stage 5 or earlier (suggest stage 5 for symmetry with other dispatcher bindings; **H_E surface availability assumed throughout bootstrap**)" — "H_E surface availability assumed" is the gap surface. Plan AC #2 inherits the spec deferral verbatim ("bootstrap stage 5 binds `ctx.ask_user_question_surface` to an H_E-backed implementation wrapping Claude Code's `AskUserQuestion` mechanism per spec §14.8.3").
- **Anti-fabrication attack engaged:** A2 (silent scope narrowing — spec deferred-list scope omits HOW; A8-adjacent for H_E↔H_T substrate-boundary architectural choice per X-AL-1).
- **Axis-domain attack engaged:** CP (H_T-CP-20 substitution boundary; X-AL-2 retirement-criterion B reading) + Meta-Architecture §5 substitution-mechanism category.
- **Resolution path:** Spec §14.8.3 amendment to name the substitution-mechanism category (one of the 6 at Meta-Architecture §5). Operator decision required to pick among at least three candidate mechanisms: (i) MCP-server-backed (runtime emits a tool call through an MCP host that an H_E-side handler intercepts and forwards to AskUserQuestion); (ii) H_E-direct via stdout-marker protocol (runtime emits a structured `ASK_USER_QUESTION:` JSON payload to stdout that Claude Code interprets and re-injects via a stdin-fed response); (iii) single-process callback (runtime invoked INSIDE the Claude Code agent loop; AskUserQuestion bound to a Python callable that the agent loop services synchronously). The choice impacts §14.8.3 substitution-retirement reading, the bootstrap stage-5 binding signature, and the test fixture mock shape. **§2.7.6 Phase-7 fork class if surfaced at implementation arc instead of resolved at spec:** Class 1 (halt-execution; architectural defect; design-phase artifact revision required). Proposing: this could be downgraded to Class 2 (§4.1) if operator accepts that picking the mechanism IS implementation discretion and the spec deferred-list grows by one entry; that reading materially weakens the X-AL-3 anti-silent-design-extension discipline at exactly the substitution boundary the H_T-CP-20 retirement criterion B depends on, but is the operator's call.

---

## Class 2 findings (moderate — current-phase revision)

### F2-01 — H_T-CP-20 retirement-criterion enumeration under-counts: omits U-CP-13

- **Location:** Spec `Spec_Harness_Runtime_v1.md` §14.8.3 line 1506 ("Condition A: cited unit IDs landed — U-CP-37 + U-CP-38 + U-CP-39 + U-CP-40 + U-CP-41 + U-CP-46 + U-RT-60"); plan v2.7 AC #14 (L9-quater body — "condition A satisfied: U-CP-37 + U-CP-38 + U-CP-39 + U-CP-40 + U-CP-41 + U-CP-46 + U-RT-25 + U-RT-60 all landed"); plan v2.7 §6 v2.7 addendum row 1.
- **Defect:** Spec §14.8.2 step 1 reads `placements = step.hitl_placements (Sequence[HITLPlacement] per C-CP-17 §17.3; **populated at workflow-binding time per U-CP-13 + U-CP-38**)` — i.e., U-CP-13 IS the workflow-binding production site that populates `step.hitl_placements` for real workflows. Without U-CP-13 landed, no real workflow's steps carry `hitl_placements`; the composer's step-1 read always returns `[]` and the composer always short-circuits to inner-dispatcher delegation (per AC #3). The H_E `AskUserQuestion` substitution-site invocations would still occur via the `CLAUDE.md`-substituted manual channel (per Meta-Architecture §5 H_T-CP-20 substitution row) because no real-workflow trigger ever exercises the production composer. Condition B ("H_E surface no longer invoked at substitution site") cannot be evaluated at U-RT-60 landing — U-CP-13 is the load-bearing prerequisite that closes the workflow-binding loop. Plan AC #14 correctly adds U-RT-25 that spec line 1506 omits, but inherits the spec's omission of U-CP-13.
- **Discriminator that classifies as Class 2:** (a) affects substantive content of current-phase artifact (plan AC #14 enumeration). Bordering Class 3 — resolution touches spec §14.8.3 line 1506 + plan AC #14 + plan §6 v2.7 addendum row simultaneously. Treating as Class 2 because the spec change is a single-line enumeration extension (NOTE-form absorption shape — does not change C-RT-18 contract surface).
- **Evidence:** Spec line 1477 (composer body step 1) ↔ spec line 1506 (X-AL-2 retirement criterion) — the same spec section cites U-CP-13 + U-CP-38 as the workflow-binding pair but the retirement-criterion enumeration lists only U-CP-38. Plan AC #14 line "U-CP-37 + U-CP-38 + U-CP-39 + U-CP-40 + U-CP-41 + U-CP-46 + U-RT-25 + U-RT-60" omits U-CP-13.
- **Anti-fabrication attack engaged:** A2 (silent scope narrowing within the spec's own §14.8.3 retirement enumeration); cross-reference internal-consistency failure.
- **Axis-domain attack engaged:** CP (X-AL-2 retirement-criterion completeness; condition A unit-ID enumeration).
- **Resolution path:** Spec §14.8.3 line 1506 amendment to extend condition A enumeration with U-CP-13; plan AC #14 + §6 v2.7 addendum row mirror the spec extension. Plan v2.7 → v2.8 minor revision absorbs both layers. Co-publication discipline per workspace `CLAUDE.md` §1.3 authority chain.

### F2-02 — AC #11 multi-placement test design tests an impossible-at-v1.9 case

- **Location:** Plan v2.7 AC #11 (L9-quater body — "Test asserts at minimum the 2-placement case (PRE_ACTION + SUB_AGENT_BOUNDARY) does not collide on action_id and both audit entries reach the writer").
- **Defect:** Plan AC #11 test design specifies a 2-placement case with PRE_ACTION + SUB_AGENT_BOUNDARY on a single step. This case **cannot occur at v1.9 MVP** because of the step-kind routing discipline: a single step has a single `step.kind`; an `INFERENCE_STEP`-kind step routes through `ctx.step_dispatchers.lookup(INFERENCE_STEP)` to the composer with `applicable_placements={PRE_ACTION}`, and a SUB_AGENT_BOUNDARY placement declared on that step would be filtered out at composer body step 2 (per spec §14.8.2 step 2 — placement `not in self.applicable_placements`); a `SUB_AGENT_DISPATCH`-kind step routes through the composer with `applicable_placements={SUB_AGENT_BOUNDARY}` and would filter PRE_ACTION the same way. The exercise-able NOTE 6-i case at v1.9 MVP is **multiple placements of the same `position` value** on a single step (e.g., two PRE_ACTION placements on a single INFERENCE_STEP step — one for "review the prompt", one for "approve model selection"), which both match a single composer's single-element `applicable_placements` set and both fire in the §14.8.2 step 4 per-placement loop. The AC #11 test design exercises an impossible case and would never trigger the action_id collision-risk that NOTE 6-i actually anticipates.
- **Discriminator that classifies as Class 2:** (a) affects substantive content (AC test specification). Test as authored would pass trivially (the case cannot occur) without exercising the NOTE 6-i deferral surface — the NOTE-mirror discipline (cite the deferral; exercise the case) fails the second half.
- **Evidence:** Spec §14.8.2 step 2 line 1478 (`matching = [p for p in placements if p.position in self.applicable_placements]`); spec §14.8.1 item 1 line 1453 ("The composer is **single-instance-per-step_kind** at v1.9 MVP — one `INFERENCE_STEP`-targeted instance with `applicable_placements={PRE_ACTION}` + one `SUB_AGENT_DISPATCH`-targeted instance with `applicable_placements={SUB_AGENT_BOUNDARY}`"); spec §14.8.7 NOTE 6-i line 1558 ("e.g., one step declares both `PRE_ACTION` for the action AND `SUB_AGENT_BOUNDARY` for a child sub-agent") — the spec NOTE's example illustrates a future arc where the routing-layer permits cross-position dispatch, not the v1.9 MVP shape.
- **Anti-fabrication attack engaged:** None — this is an axis-domain precision finding.
- **Axis-domain attack engaged:** CP (HITL placement evaluation; single-position-per-composer routing constraint at v1.9 MVP).
- **Resolution path:** Plan AC #11 test specification revision to use two same-position placements on a single step (the v1.9 MVP-exercisable case). The NOTE 6-i deferral comment in composer source still cites §14.8.7 NOTE 6-i verbatim per the AC's existing prescription. Plan v2.7 → v2.8 minor revision.

### F2-03 — AC #9 reproduces spec literal `entry_core=StateLedgerEntryRef(<step-8b-HITL entry_hash>)` but does not surface the U-RT-59 carry-forward drift

- **Location:** Plan v2.7 AC #9 substep 8c-HITL: `od_entry = cp_audit_to_od_audit(cp_entry, key_id=ctx.audit_signing_key_id, algo=ctx.audit_signing_algorithm, entry_core=StateLedgerEntryRef(<step-8b-HITL entry_hash>))`. Mirrors spec §14.8.2 substep 8c-HITL line 1491. Inherited from spec §14.7.2 step 8c (U-RT-59 sub-agent dispatch) via the Q3-ratified shared-converter discipline.
- **Defect:** Spec line 1287 (§14.7.2 step 8b for sub-agent dispatch) says the F2 entry's `entry_hash` is "returned in `WriteResult` or read post-append via `ledger_writer.last_appended_entry_hash` API per `Spec_Control_Plane_v1_6.md` §25.2.1 v1.6 step_context note." The U-RT-59 implementation-arc memory (`[[fork-u-rt-59-cp-to-od-audit-write-gap]]`) records that the actual landed code uses `StateLedgerEntryRef(str(dispatch_action_id))` — the action_id-as-opaque-str discipline per `Spec_Operational_Discipline_v1_5.md` C-OD-24.4 — because `LedgerWriter.append` does not expose the forward chain hash. This is recorded as a Class 3 prose-drift carry-forward at `.harness/class_3_tension_u_rt_59_spec_prose_drift.md`. AC #9 reproduces the spec literal that does NOT match the U-RT-59 production callsite shape; the implementation arc will hit the identical gap and either (a) repeat the same drift (silent absorption of inherited gap) or (b) surface a fresh Class 1 fork. The NOTE-mirror discipline that handled U-RT-59's other path-(ii) deferrals at v1.8 NOTE 8a-i–iv pre-emption is not exercised here; the inherited drift is not surfaced.
- **Discriminator that classifies as Class 2:** (a) affects substantive content (AC #9 carry-forward acknowledgment). Could be Class 1 if the drift is purely prose (impl already uses action_id-as-marker via shared converter; HITL composer would inherit the same convention transparently) — but the inheritance is implicit, not explicit, and the implementation arc operator will burn an orientation pass tracing it.
- **Evidence:** Plan AC #9 substep 8c-HITL literal vs `.harness/class_3_tension_u_rt_59_spec_prose_drift.md` carry-forward item 5 ("action_id-as-StateLedgerEntryRef prose drift carry-forward") per memory `[[fork-u-rt-59-cp-to-od-audit-write-gap]]` items-deferred list. Spec §14.7.2 step 8b line 1287 (`ledger_writer.last_appended_entry_hash` API mention) is the un-revised spec prose; the U-RT-59 implementation used the opaque-str fallback.
- **Anti-fabrication attack engaged:** A2 (silent scope narrowing — known drift not surfaced at AC); FM-G (smoothing — "shared converter handles it transparently" is assumed).
- **Axis-domain attack engaged:** CP↔OD cross-axis audit-write convention; OD C-OD-24.4 opaque-str discipline.
- **Resolution path:** Plan AC #9 substep 8c-HITL revision to acknowledge the inherited action_id-as-opaque-str pattern from U-RT-59 (cite the U-RT-59 implementation arc memory + the Class 3 prose-drift record) so the implementation arc operator does not re-derive. Optional spec §14.8.2 substep 8c-HITL line 1491 amendment to bring the spec literal in line with the U-RT-59 carry-forward (would simultaneously close Class 3 prose-drift item 5 at the HITL composer landing — would arguably be a Class 2 spec amendment but worth surfacing for operator consideration). Plan v2.7 → v2.8 minor revision in either case.

---

## Class 1 findings (minor — documentation drift)

### F1-01 — Plan ACs silent on spec §14.8 "Deferred to implementation discretion" list

- **Location:** Plan v2.7 L9-quater body (no AC cites the deferred-list). Spec §14.8 "Deferred to implementation discretion" line 1600–1607.
- **Defect:** Spec §14.8 lists 6 implementation-discretion items (`compose_gate_prompt` prompt shape; `compose_hitl_action_id` construction shape suggestion; `MockAskUserQuestionSurface` fixture suggestion; `AskUserQuestionSurface` construction timing; `_hitl_required` predicate at step 4c bounded-vs-full reading). Plan ACs reference some inline (AC #9 cites the `hitl:` action_id prefix; AC #5 cites the bounded `_hitl_required` reading at step 4c) but do not surface the discretionary nature — the implementation arc operator must read the spec deferred-list to understand where to author rather than translate. The U-RT-59 plan body did the same and produced a clean implementation arc, so this is not load-bearing — but it is documentation drift versus the NOTE-mirror discipline applied to spec §14.8.7 path-(ii) NOTEs.
- **Resolution path:** Plan AC #1 or AC #2 extension to add a single sentence enumerating the spec §14.8 deferred-list items inherited by the implementation arc. Inline fix.

### F1-02 — U-RT-51 plan body unchanged in v2.7 despite §6 addendum extending its assertion count 22 → 23

- **Location:** Plan v2.7 §6 v2.7 addendum closing paragraph ("U-RT-51 Pattern P1 completeness assertion seam-count extends from 22 → 23 at U-RT-60 landing"). Plan v2.7 L11 U-RT-51 body unchanged from v2.5 ("test asserts the 22 genuine typed CXA seams (per CXA v2.3 §3) realize Pattern P1 byte-exact").
- **Defect:** §6 v2.7 addendum declares the U-RT-51 assertion-count update but the U-RT-51 plan body AC + scope text still cite 22 seams per CXA v2.3 §3. The CXA v2.5 §2.3.7 second seam (U-CP-46 → U-OD-00) lands at U-RT-60 implementation but U-RT-51's test set does not extend automatically — the implementation arc would need to either (a) extend U-RT-51's test_cxa_pattern_p1.py to assert the 23rd seam, or (b) the U-RT-51 plan body needs update to cite CXA v2.5 §3 + 23 seams. Plan v2.7 declares the downstream effect at §6 without absorbing it at U-RT-51 in the same revision.
- **Resolution path:** Either (a) extend U-RT-51 plan body in plan v2.7 → v2.8 to cite 23 seams + CXA v2.5 §3, or (b) add a "downstream absorption owed at U-RT-60 implementation landing" note at the v2.7 change-note. Operator decision on which path. Inline fix.

### F1-03 — Fail class `RT-FAIL-HITL-PLACEMENT-FORECLOSED-AT-V19` bakes version into name (naming-convention outlier)

- **Location:** Plan v2.7 AC #4; spec §14.8 failure-mode taxonomy "Additional fail class added at v1.9 for placement-foreclosure" table.
- **Defect:** Other runtime fail classes follow a version-neutral naming convention: `RT-FAIL-PROVIDER-UNREACHABLE`, `RT-FAIL-FALLBACK-EXHAUSTED`, `RT-FAIL-HITL-GATE-REJECTED`, `RT-FAIL-HITL-GATE-TIMEOUT`, `RT-FAIL-HITL-GATE-AUDIT-COMPOSE`, `RT-FAIL-STEP-KIND-DISPATCHER-NOT-BOUND`, `RT-FAIL-SUB-AGENT-AUDIT-COMPOSE`. `RT-FAIL-HITL-PLACEMENT-FORECLOSED-AT-V19` is the only fail-class identifier that bakes a spec version into the name. The spec acknowledges this is "Documented expected behavior at MVP; resolved at the validator-composer arc landing (future C-RT-NN)" — but the version-baking creates a future churn point: at the validator-composer arc, either the class is renamed (breaking error-class identity downstream) or kept with confusing name. Not load-bearing for U-RT-60 implementation, but worth surfacing at retirement-event time.
- **Resolution path:** Either (a) leave as-is and document the future-churn expectation at the spec §14.8 fail-class table (one-line note); or (b) rename to a version-neutral form at spec amendment (e.g., `RT-FAIL-HITL-PLACEMENT-FORECLOSED-PRE-VALIDATOR`). Inline fix at spec; plan inherits.

---

## Findings considered and rejected (transparency)

Substantive attack vectors applied that did NOT surface findings:

1. **A4 (fabricated citations).** Spot-checked all spec sub-section citations in the plan body — §14.8.1 / §14.8.2 / §14.8.3 / §14.8.4 / §14.8.5 / §14.8.6 / §14.8.7 / §14.7.2 step 8 / CXA v2.5 §2.3.7 / CP §13.5.1 NOTE 5 — all resolve. CP cross-axis cites (C-CP-13 §13.1, C-CP-14 §14.2, C-CP-16 §16.1–§16.4, C-CP-17 §17.1–§17.3, C-CP-18 §18.1–§18.5, C-CP-19 §19.1, C-CP-20 §20.4/§20.5/§20.6, C-CP-25 §25.2/§25.3.3.4) spot-checked. No fabrication. Plan §9 traceability pointer correctly bumped v1.6 → v1.9 to cite the newly authored U-RT-60 spec §15 row.

2. **Topological sort acyclicity.** U-RT-60 dependencies (U-RT-12 + U-RT-25 + U-RT-26 + U-RT-27 + U-RT-32 + U-RT-42 + U-RT-58 + U-RT-59) all upstream — no cycle introduced. §3 topology graph L9-quater block explicitly enumerates. Acyclic invariant preserved.

3. **Spec coverage of §14.8.1–§14.8.7.** All 7 sub-sections cited by ≥1 AC per coherence-pass verification (§14.8.1 = AC #13; §14.8.2 = AC #3/#4/#5/#6/#7/#8/#9/#10; §14.8.3 = AC #2; §14.8.4 = AC #4; §14.8.5 = AC #7; §14.8.6 = AC #9; §14.8.7 = AC #6/#10/#11/#12). No sub-section orphaned.

4. **Fail-class taxonomy completeness.** All 4 fail classes at spec §14.8 pinned at surfacing ACs (REJECTED → AC #10; TIMEOUT → AC #8; AUDIT-COMPOSE → AC #9 [suppressed-on-REJECT discipline preserved]; PLACEMENT-FORECLOSED-AT-V19 → AC #4). Coverage complete.

5. **NOTE-mirror discipline.** All 4 path-(ii) NOTEs at spec §14.8.7 mirrored as ACs with verbatim section citation (NOTE 6-i = AC #11; NOTE 6-ii = AC #10; NOTE 6-iii = AC #12; NOTE 6-iv = AC #6). The deferral comment-in-source obligation is named at each AC. (Test-design quality issue noted at F2-02 for NOTE 6-i; the mirror citation itself is sound.)

6. **Q3 shared-converter ratification fidelity.** AC #9 substep 8c-HITL explicitly states "same `cp_audit_to_od_audit` converter at `harness-cxa/src/harness_cxa/cp_audit_conversion.py` shared with U-RT-59 sub-agent dispatch per Q3 ratification". CXA v2.5 §2.3.7 single-converter discipline preserved at the plan layer. Pattern P1 byte-exact alignment obligation for the new typed seam (U-CP-46 → U-OD-00) surfaced at AC #9 ("test verifies the second seam realizes Pattern P1 byte-exact alignment") — modulo the U-RT-51 absorption-owed observation at F1-02.

7. **Wrap-asymmetry table fidelity.** AC #13 cites spec §14.8.1 wrap-asymmetry table without restating it (FM-C author-mode-drift avoided) and asserts the post-bootstrap `ctx.llm_dispatcher` + `ctx.sub_agent_dispatcher` shapes match the table's two rows. Q2 retry-outer-of-HITL discipline preserved at the bootstrap-stage-5 binding.

8. **Producer-side carrier import discipline (AS-AL-3 + per-axis ownership).** AC #13 forbids hand-coded attribute strings and pins the import to `harness_cp.audit_hitl_span_namespace.{AUDIT_NAMESPACE_SCHEMA,HITL_SPAN_NAMESPACE_SCHEMA}` per U-CP-46. Considered the candidate finding "OD owns OTel schema → CP-side attribute carrier imported by runtime composer crosses an axis-domain ownership line per OD-AL-1." Rejected because spec §14.8.5 explicitly cites C-CP-20 §20.4/§20.5/§20.6 as the carrier-source-of-truth — the `audit.*` + `hitl.*` namespace ownership lives at CP per the spec architecture; OD-AL-1 owns the OTel-schema *framework* not the *namespace content* declarations.

9. **CXA v2.5 §2.3.7 byte-exact alignment at the new seam.** Considered whether AC #9's "test verifies the second seam realizes Pattern P1 byte-exact alignment" is sufficient given U-RT-51's separate verification scope. Resolved as adequate for U-RT-60 landing — U-RT-51's seam-count extension is a downstream absorption issue (surfaced at F1-02), not a U-RT-60 AC gap.

10. **FM-K disposition-without-evidence (self-check).** Disposition recommendation = "fork to spec revision" matches finding inventory (1 Class 3 + 3 Class 2 + 3 Class 1). Resolution lattice: F3-01 needs operator decision; F2-01 needs spec amendment + plan AC + §6 mirror; F2-02/F2-03 + F1-01/F1-02/F1-03 all absorb as plan v2.7 → v2.8 minor revision. No Class 3 was discriminator-(c) framing contamination — all findings are domain-precision or scope-completeness.

---

## Disposition

**Recommendation: Fork to spec revision (§4.1.3 Class 3 path) — proposing.**

The single Class 3 finding (F3-01) is *proposing* per §"Decision-claim vocabulary" because the discriminator depends on operator judgment about whether the H_E binding mechanism is genuinely under-specified (Class 3 — spec §14.8.3 amendment required before implementation arc opens) or is acceptable implementation discretion (Class 2 — spec deferred-list entry extension; plan absorbs by AC #2 acknowledging the discretion).

**If operator picks the Class 3 reading:** spec `Spec_Harness_Runtime_v1.md` v1.9 → v1.10 minor revision at §14.8.3 to pin the substitution-mechanism category per `Phase_7_Meta_Architecture_v1.md` §5; co-publish plan v2.7 → v2.8 absorbing the spec choice into AC #2 + AC #14 (retirement-criterion B reading); absorb F2-01/F2-02/F2-03 + F1-01/F1-02/F1-03 into the same plan v2.8 revision. The pattern matches the v1.6 → v1.7 + v2.5 → v2.6 U-RT-59 absorption arc.

**If operator picks the Class 2 reading:** spec deferred-list extension only (no §14.8.3 narrative amendment); plan v2.7 → v2.8 minor revision absorbing AC #2 acknowledgment + the F2-01/02/03 + F1-01/02/03 findings. Carries forward the H_E binding-mechanism choice as a §2.7.6 Class 1 fork hazard at implementation arc landing (acknowledged at filing; impl arc operator empowered to halt and re-spec if the chosen mechanism breaks the X-AL-2 retirement-criterion B reading).

**Pattern observation (single-arc, not yet a systemic pattern per §6):** The U-RT-58 + U-RT-59 + U-RT-60 sequence of runtime composer arcs all surfaced a substitution-boundary architectural question at adversarial review (U-RT-58 = retry-attribute drift at C-RT-16; U-RT-59 = CP→OD audit-write gap at C-RT-17; U-RT-60 = AskUserQuestionSurface H_E binding mechanism at C-RT-18). At 3 occurrences this crosses the §6 systemic-pattern threshold: **the spec-authoring pass for runtime composer arcs reliably under-specifies the substitution-boundary surface at the architectural-decision granularity that matters for retirement-criterion B end-to-end verification.** Worth considering whether the `phase-7-implementation` skill body or the `spec-writer` skill body needs an explicit "substitution-boundary surface pinning" check at the spec-authoring phase. Routed to Class 1 (§4.1 minor — pattern-observation) at the workflow level; defer the systemic-pattern resolution to operator judgment.
