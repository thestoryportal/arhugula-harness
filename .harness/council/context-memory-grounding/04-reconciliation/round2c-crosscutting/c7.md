My empirical checks confirm the structure-not-content property. The negative check returned only `permission-guard` test files and the `resolve_lib.sh` line that *unsets* `OPENAI_API_KEY` (`env -u OPENAI_API_KEY`) — i.e., secret-name strings appear only in deny-list enforcement / tests, never as emitted content in any hook surface (`session-start.sh` emits `[ROADMAP] hash=ok next=… in_flight=N forks=N`; StatusLine emits `ctx N%`; MEMORY.md cap emits `${SZ}B > 24400B cap`). Every WS-5 surface my health states would extend emits a byte count / a count / a structural flag — never content, never a secret. AR-8 confirmed empirically.

`prompt-context.sh` returned empty (the mid-session injector lives under a different filename on this branch — the C7 Stage-1 contribution cited it at the lifecycle role; the emit shape is the same `[roadmap] next=` structural string per the deliberation, and the `[ROADMAP]` emit family above confirms the structure-not-content discipline regardless of exact filename). I have everything needed. Writing the deliverable.

---

## C7 cross-cutting response

### Lane dispositions

| AR-ID | Disposition | PLAN delta |
|---|---|---|
| **AR-5** (WS-5 proportionality — rank the 5 health states + mid-session surface + dashboard health vs solo scale per cluster-2 §1.11; name WS-5 MVP vs defer) | **ACCEPT** | DESIGN.md WS-5 gets an explicit **MVP instrument + proportionality ladder**, citing cluster-2 §1.11, applying the filter the council named-but-didn't-apply. **MVP (build now — exactly ONE instrument): the mid-session byte-budget surface** — a `UserPromptSubmit` injector (or StatusLine field) that emits the CLAUDE.md + MEMORY.md byte-count-vs-cap mid-session, NOT only at boundaries (my F3). It is load-bearing because it is the **runtime sensor that makes G1's CI gate actionable in-loop** (per the C5↔C9 surface-split the siblings resolved): G1 fails the PR at build-time; the mid-session surface lets the agent *see the overage and self-correct before the PR*, the observe-half of "observability is logically prior to a cap value" (my F2). It is near-zero-cost: it reuses the **exact emit shape that already exists** (`[ROADMAP]`/`ctx N%`/MEMORY.md-cap-flag — verified structural-only this session; <50 tokens; correctly placed in the dynamic suffix per C-IS-07 §7.2). **DEFER-eligible (full 5-state dashboard surface): the other 4 health states** — `.harness/` ledger active-vs-archived count, per-store checkpoint count vs ceiling, MEMORY.md write-collision detector, cache-efficiency-via-git-proxy. Each is a *legibility requirement I keep in the design as backlog*, but **five new health instruments + a dashboard surface is precisely the "instrument everything" pattern cluster-2 §1.11 cautions against at solo scale.** Promotion triggers, per-state: (a) ledger-count surface → promote when `.harness/` archival (C3 WS-3 defer-item) is actually built (the count is the sensor *for* that policy — co-sequenced, not standalone); (b) checkpoint-count surface → promote with the same trigger as C3's checkpoint-asymmetry item (self-deferred per workspace §12.5.3 "optional low-value hygiene"); (c) MEMORY.md write-collision detector → **promote on C3's AR-4 detection-step** (it IS C3's "detect-before-serialize" forward-detection sensor — my legibility-half of C3's gated step 1, NOT a separate instrument); (d) cache-efficiency → the **git-derivable §2-edit-cadence proxy** (my F4), which is *already observable for free via `git log -- CLAUDE.md`* and needs no new hook — so it is not a deferred *instrument* at all, just a query the PLAN names. **Net WS-5 MVP = {mid-session byte-budget surface}. Deferred = {4 dashboard health states, each trigger-gated; cache-proxy = zero-build git query}.** Full 5-state design preserved as backlog — additive, nothing dropped. |
| **AR-8** (confirm WS-5 health hooks / version-state injector carry NO secret material — structure-not-content) | **ACCEPT (empirically grounded — confirmed this session)** | DESIGN.md re-affirms the secrets pre-check to **explicitly name the WS-5 surfaces** (jointly with C2's WS-2 version-state injector re-affirmation): *"No WS-5 health hook nor any mid-session byte-budget / dashboard-health injector places secret material in the static prefix or dynamic suffix — every WS-5 emit is structural (a byte count, a doc count, a cap flag, a context-fill %), never content."* **Empirical grounding (verified at HEAD this session):** the existing hook emit surfaces my WS-5 states extend are structure-only — `session-start.sh` emits `[ROADMAP] hash=ok next=… in_flight=N forks=N` (`:80`); StatusLine emits `ctx N%` from `context_window.used_percentage` (`context-recovery.sh:23,:56`); the MEMORY.md cap emits `${SZ}B > 24400B cap` (`loop-gc.sh:56` / `session-end-cleanup.sh:53`). A negative grep for secret-shaped tokens (`api_key|secret|token=|password|bearer`) across `tools/hooks/` + `tools/roadmap-audit/` + `tools/statusline/` returned **only deny-list-enforcement + test files** (`permission-guard`, `test_*`) and the one `env -u OPENAI_API_KEY` line in `resolve_lib.sh` that *unsets* a secret — **zero secret material in any emitted-content position.** This is the structure-not-content discipline (my skill §4.5) the hooks already follow, inherited by construction by any new WS-5 injector of the same shape. C10 *enforces* via `permission-guard.sh`; C7 only *asserts* the WS-5 injection surfaces are structural-by-construction. **Owner note for the orchestrator:** dashboard *rendering* of the deferred 4 health states is C11 co-primary (my skill §7.10) — I own the legibility-coverage requirement (which health state must be surfaceable); C11 owns the UX/rendering/deployment (Almanac Noir, launchd, regen-per-GET); the secrets-free property holds at the data layer I own regardless of render. |

### Cross-cutting coherence checks

**Seam 1 — WS-5 MVP ⟷ C2's unified-slice ranking (the central cross-read).** C2's MVP = **WS-1 + G1 + WS-6/D14 + X**, with WS-2 split into *load-bearing SSOT-pointer/INDEX* vs *deferrable orientation-docs*. **COHERE.** C2's slice does not list WS-5 as a top-line MVP member — and my own proportionality verdict agrees: the *full* WS-5 5-state dashboard surface is deferrable and should NOT be in the load-bearing core. The unified resolution from my vantage: **the one WS-5 piece that is load-bearing-WITH-the-core is the mid-session byte-budget surface, and it is load-bearing precisely as the runtime *companion* of G1** (which C2 *does* place in MVP). G1 (C5's CI gate) catches an over-budget CLAUDE.md at PR-time; the WS-5 mid-session surface is the in-loop sensor that lets the agent avoid producing the over-budget diff in the first place — they are the build-time and runtime halves of the *same* byte-budget discipline (the identical C5↔C9 surface-split the siblings resolved, applied to the observe-side). So the unified slice is: **WS-1 + G1 + (WS-5 mid-session byte-budget surface) + WS-6/D14 + X + WS-2's SSOT-pointer/INDEX half** — with the WS-5 *dashboard* 4-state surface, WS-2's orientation-docs, and WS-4 G2–G4 all in the deferred tail. The mid-session surface is the natural sensor for C2's own G1 placement; no conflict — it *completes* C2's slice rather than competing with it.

**Seam 2 — WS-5 MVP ⟷ C5's unified-slice ranking (`{G1}`, defer G2–G4).** **COHERE — exactly.** C5's WS-4 verdict (MVP = {G1}; defer G2 promote-on-trigger, G3 fold-into-G2, G4 resolved-no-teeth) and my WS-5 verdict (MVP = {mid-session byte-budget surface}; defer the 4 dashboard states trigger-gated) are the **same proportionality shape applied to adjacent lanes**: one load-bearing piece each, the rest preserved as trigger-gated backlog, citing cluster-2 §1.11. They are *complementary*: C5's G1 is the gate; my mid-session surface is the sensor that feeds the same byte-budget. And the deferred tails interlock cleanly — C5's G2 (clearance-marker schema gate) and my deferred ledger/checkpoint dashboard states are both "defer-until-the-corpus-bites" decisions on the same solo-scale logic. The unified resolution: **MVP gate+sensor pair = {G1 (C5, CI/PR-time) + mid-session byte-budget surface (C7, runtime)}**; everything else in WS-4 and WS-5 is the proportionate-but-deferrable tail. No conflict.

**Seam 3 — WS-5 cache-efficiency ⟷ C2's cache-detonation cost claim + AR-6.** **COHERE.** C2 demoted the cache *multipliers* to [MODERATE] (AR-6) but kept the load-bearing claim [HIGH] and self-grounded on the **edit-cadence** (60/60 commits touch CLAUDE.md). My F4 supplies the matching observability verdict: cache-hit-rate has *no dev-loop observability home* (it lives in H_T product OTel, not Claude Code hook JSON), so the cache-efficiency story must be measured by the **git-derivable §2-edit-cadence proxy**, not a dev-loop cache-hit-rate. This is the same edit-cadence C2 leans on — my lane confirms it is the *only reachable* cache-cost signal, and it is **free** (`git log -- CLAUDE.md`, no new instrument). Unified resolution: the PLAN's cache-efficiency story is the git-edit-cadence proxy (mine, zero-build) measuring the mechanism C2 owns (the cost model); I make no population-level cache-hit-rate claim (FM-A/FM-K held). No conflict — my F4 and C2's AR-6 disposition point the same direction (measure the verifiable cadence, demote the unverifiable rate/multipliers).

**Seam 4 — WS-5 MEMORY.md write-collision detector ⟷ C3's AR-4 detection-step + C9's AR-4 rollback-boundary verdict.** **COHERE (and it sharpens my own MVP framing).** C3's AR-4 disposition gates the FM-H serialization remedy behind a **cheap forward-detection step** (a write-collision counter / mtime-race probe) *before* building flock/journal machinery, and explicitly defers the rollback-primitive remedy to C9/T5. C9's AR-4 confirmed the rollback boundary **negative** (the out-of-worktree memory store is not a git repo — no rollback primitive exists). My WS-5 "MEMORY.md write-collision detector" is **exactly C3's detection sensor**, viewed through my legibility lane — it *is* the observability-half of C3's gated step 1, NOT a separate WS-5 instrument I own end-to-end. Unified resolution: the write-collision detector belongs to **C3's "X"-workstream step-1 detection** (C3 owns the detector *mechanism*; I own only the requirement that the collision be *legible* — silent-by-construction is the failure, per my coverage map). I fold it OUT of WS-5's standalone instrument count and INTO C3's detect-before-serialize step, which is why my WS-5 MVP is *one* instrument (the mid-session byte-budget surface), not two. This is the cleaner placement and it removes a latent double-count between WS-5 and the "X" workstream. No conflict — I defer the detector's mechanism + the serialization + the rollback-boundary remedy to C3/C9 and keep only the legibility requirement.

**Seam 5 — WS-5 dashboard-health 4 states ⟷ C11 co-primary (handled-by-reference) + AR-8 secrets.** **COHERE.** My skill §7.10 names dashboard UX as C7↔C11 co-primary; the deliberation handled C11 by-reference. I own the *legibility-coverage* requirement (which health states must be surfaceable); C11 owns rendering/deployment. Since the 4 dashboard health states are all in my *deferred* tail (not MVP), the C11 co-primary surface is also deferred — there is no MVP-blocking C7↔C11 convening owed. AR-8's secrets-free property holds at the data layer regardless of render (verified structure-only). Unified resolution: deferred dashboard states carry a "convene C7+C11 at the execution arc that builds them" note; nothing in the MVP slice requires C11 resolution now. No conflict.

### Residual cross-cutting conflict?

**None — coheres.** My WS-5 MVP ranking (MVP = the single mid-session byte-budget surface; full 5-state dashboard surface deferred + trigger-gated) consolidates with C2's unified slice (WS-1 + G1 + WS-6/D14 + X, WS-2 split) and C5's (`{G1}`, defer G2–G4) into **one consistent MVP slice**: the load-bearing core is **WS-1 + {G1 + mid-session byte-budget surface} + WS-2-SSOT/INDEX + WS-6/D14 + X**, with the full WS-5 dashboard 4-state surface, WS-2 orientation-docs, and WS-4 G2–G4 as the proportionate-but-deferrable tail — every voice applying the same cluster-2 §1.11 filter, one load-bearing piece per lane, full design preserved as trigger-gated backlog (additive, nothing dropped). The byte-budget discipline is internally coherent across lanes via the C5↔C9 surface-split the siblings already resolved: **G1 is the CI/PR-time gate (C5); the mid-session surface is the runtime sensor (C7); C9's `context_budget_exceeded` is the runtime degradation mode** — three complementary surfaces of one discipline, none halting the loop (never-halt preserved). Two latent double-counts are *removed* by my dispositions, not created: (1) the WS-5 write-collision detector folds into C3's "X" step-1 detection (mechanism = C3; legibility = C7), and (2) the cache-efficiency "instrument" collapses to a free git query (no new hook). All locked posture preserved — never-halt / defer-and-continue / prefer-free-ollama / paid-secret-destructive deny-list untouched; AR-8 confirms WS-5 emits structure-not-content with zero secret material. No seam where my disposition and a sibling's genuinely conflict.