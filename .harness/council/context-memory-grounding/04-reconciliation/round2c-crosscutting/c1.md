Both empirical checks confirm cleanly:

1. **No WS-2 top-level doc names exist** at the worktree root — `AGENTS.md` / `ARCHITECTURE.md` / `WORKFLOW.md` / `HOOKS.md` / `INDEX.md` are all absent. `CLAUDE.md` is the sole governance doc among the top-level `.md` files. This confirms C2's "none currently auto-loaded" finding AND my F1 "no artifact maps these boundaries" — `HOOKS.md` is genuinely a NEW additive file, not an edit.
2. **Project `CLAUDE.md` has zero `@import` lines** — no WS-2 anchor is `@import`-ed into the prefix. C2's rescope guard ("forbid `@import`-ing WS-2 anchors into CLAUDE.md") is currently satisfied by construction; it's a forward-looking constraint, not a fix to an existing violation.

I have everything. Writing the deliverable.

## C1 cross-cutting response

### Lane dispositions

| AR-ID | Disposition (accept/reconcile/rebut) | PLAN delta |
|---|---|---|
| **AR-2** (home-of-record — co-decide with C9) | **ACCEPT — co-decision confirmed, refined with a lifecycle-TOPOLOGY-contract carve-out** | C9's split is correct and I co-sign it: **HARDENING_PLAN (`.harness/hardening-workflow/HARDENING_PLAN.md`, lines 277/365/375) = home-of-record for the `tools/hooks/` EXECUTION units** (U-HK-30 D14 Piece A + U-HK-40 D14 Piece B — their bodies, `test_*.sh` fixtures, StopFailure-trigger detail); **council PLAN WS-6 = a DEPENDENCY-CITE on D14** owning only the governance-layer sequencing clause ("P1 prefix-slim MUST land WITH D14, not before — recovery-completeness is a co-requisite per R1"). The two compose; neither re-authors the other. **C1 refinement for the lifecycle-TOPOLOGY contract (HOOKS.md):** the split needs a *third* term that neither C9's WS-6 nor the HARDENING_PLAN currently names. There are three distinct things on this `tools/hooks/` surface, owned by three different artifacts: **(a) the EXECUTION units** that BUILD/modify a hook body (U-HK-30/40 → HARDENING_PLAN); **(b) the RECOVERY-SEQUENCING clause** that orders P1-vs-D14 (→ council PLAN WS-6, C9); **(c) the lifecycle-TOPOLOGY CONTRACT** — the read-only synthesized doc (`HOOKS.md`, my F1) that maps the 3 context-IN + 4 state-OUT control-transfer boundaries to the §7.1/§7.2 read/write events they fire, states the loop termination contract once (my F2), and disambiguates the three halt-semantics (my F3). PLAN delta: **DESIGN.md WS-2 owns the `HOOKS.md` lifecycle-TOPOLOGY contract as a NEW additive descriptive artifact (C1×C2 co-owned); it DESCRIBES the boundaries — it does NOT own, build, or modify any hook body.** When a U-HK execution unit changes a hook's firing behavior, the HARDENING_PLAN is the home-of-record for that change and `HOOKS.md` is a downstream consumer that re-derives its boundary map from the (changed) `settings.json` wiring — never the reverse. HOOKS.md is documentation-of-topology, not a build spec; this keeps it from forking against the HARDENING_PLAN's execution authority. |
| **AR-7** (WS-6 credit/build split — touches my lifecycle-topology doc) | **ACCEPT — co-sign C9's 6a-credit / 6b-build split; add the topology-doc placement of each half** | C9's empirical verification confirms my F1/F4 framing exactly: `postcompact-reinject.sh` reads `precompact-latest.md` back into the dynamic suffix (§7.2-conformant) and `context-recovery.sh` does the 60/75/85% pre-save — **BUILT, no work owed (WS-6a CREDIT)**; while `session-start.sh` reads NEITHER the StopFailure breadcrumb NOR the checkpoint, and `stop-loop.sh` emits `decision:block` with no `hook_write_checkpoint` call — **unbuilt = D14 U-HK-30/40 (WS-6b BUILD)**. This sharpens my F4 ("two handoff topologies with sound per-topology contracts") into a precise as-built credit: **the PreCompact→PostCompact handoff topology IS built and conforms; the Stop-failure→SessionStart recovery handoff topology is the owed half.** PLAN delta: in the `HOOKS.md` lifecycle-topology contract, the state-OUT boundary table must mark each handoff-topology row with its as-built status — **PreCompact/PostCompact handoff = CREDIT (conforms to §7.2/§7.1, verified)**; **Stop-failure-recovery handoff = BUILD-OWED (D14, cite U-HK-30/40 — no body here)**. The `context_budget_exceeded` degradation MODE shape stays the council PLAN's contribution (its in-iteration recovery is the loop-shape-recovery affordance I own per T-perm-3; the per-attempt mechanics defer to C9). This removes the AR-7 reader-hazard (credited behavior mistaken for owed work) at the topology-doc layer too, not just in WS-6. |

### Cross-cutting coherence checks

**Seam 1 — WS-2 / AGENTS.md rescope (sibling: C2). COHERE.**
C2 verified empirically that Claude Code auto-loads only `CLAUDE.md` + its `@import` chain (the session preamble's `claudeMd` block = global CLAUDE.md + RTK.md-via-`@RTK.md` + project CLAUDE.md + MEMORY.md; no `AGENTS.md`), and that no top-level orientation doc currently exists. I independently reproduced both legs: the five WS-2 doc names are all absent at the worktree root, and project `CLAUDE.md` has zero `@import` lines. **C2's rescope COHERES with my HOOKS.md-as-lifecycle-topology-contract, and in fact strengthens it.** My F1 recommends `HOOKS.md` as the navigation anchor a fresh context lands on to read the control spine — that is *exactly* a JIT-navigable orientation target, the safe class in C2's per-doc auto-load disposition table (`ARCHITECTURE.md` / `WORKFLOW.md` / `HOOKS.md` / `INDEX.md` → JIT-navigable, NOT auto-loaded). My whole F1 premise — that the topology contract must be *discoverable on demand by a fresh context*, NOT pre-loaded into the prefix — is the same cache-discipline principle C2's rescope enforces:
- A lifecycle-topology contract that fired into the static prefix on every turn would re-detonate the exact WS-1 cost the arc removes (and my own FM-G cost finding: the loop re-attends the prefix up to 25× per run — `HOOKS.md` in the prefix would be paid 25× too).
- C2's guard "WS-2 anchors MUST NOT be `@import`-ed into CLAUDE.md (the only prefix-forcing path)" is **directly load-bearing for my lane**: `HOOKS.md` is correct *only* as a JIT-read navigation target. If a future execution arc `@import`-ed it into `CLAUDE.md`, it would convert my topology contract from a discoverable anchor into re-attended prefix bloat — reintroducing the disease. **I adopt C2's `@import`-forbid as a hard constraint on the HOOKS.md I co-own.** Verified currently satisfied (zero `@import` lines), so it is a forward guard, not a present fix.

**Unified resolution (C1×C2):** `HOOKS.md` is a NEW additive JIT-navigable lifecycle-topology contract under WS-2; **C2 owns the loading/prefix-placement disposition** (JIT-navigable, never `@import`-ed, never auto-loaded); **C1 owns the topology CONTENT** (the 3+4 boundary map, the single-sited termination contract, the three-halt-semantics disambiguation, the two handoff-topology rows with credit/build status); **C7 owns the observability-anchor overlay** (per C2's own cross-lane note that WS-2 is C2×C1×C7). No conflict — three orthogonal lanes on one additive file.

**Seam 2 — WS-6 home-of-record + `tools/hooks/` execution (sibling: C9). COHERE.**
C9's AR-2 disposition and mine are the same decision, reached independently, and they nest cleanly: C9 draws the EXECUTION-vs-SEQUENCING line (HARDENING_PLAN owns U-HK-30/40 build; WS-6 owns the P1-WITH-D14 ordering clause); I add the third term (the descriptive `HOOKS.md` topology contract is neither — it's a read-only consumer of the wiring). C9 explicitly deferred this to me: *"C1 co-owns the WS-2 lifecycle-topology-contract (`HOOKS.md`) seam against the same `tools/hooks/` surface — I defer the HOOKS.md authoring boundary to C1 … and only assert that WS-6's recovery sequencing cites the D14 units rather than re-specifying the hook lifecycle."* **That deference is precisely the boundary I draw above** — WS-6 cites D14 by unit-ID+path and copies no implementation detail; `HOOKS.md` describes boundaries and modifies no hook body. **Unified resolution:** three artifacts, three non-overlapping authorities on the `tools/hooks/` surface — HARDENING_PLAN = build-of-record (a), council WS-6 = sequencing (b, C9), `HOOKS.md` = topology-description (c, C1×C2). The direction-of-truth rule prevents the fork: when a hook's firing behavior changes via a U-HK unit, HARDENING_PLAN is canonical and `HOOKS.md` re-derives downstream; the topology doc never drives a build.

**Seam 3 — AR-7 credit/build split vs my F4 handoff-topology framing (sibling: C9). COHERE.**
C9's 6a/6b empirical split (PreCompact/PostCompact built-and-conforming; Stop-recovery owed-as-D14) is the same partition as my F4 ("two handoff topologies cross the context boundary"), now resolved by C9's code-grep into per-topology as-built status. No conflict — C9 supplied the empirical verdict my F4 deferred ("the per-topology contracts are sound" → now "the PreCompact one is built+conforming, the Stop-recovery one is owed"). The only thing I add is *where this lands in the artifact*: the status annotations belong in the `HOOKS.md` state-OUT boundary table (my lane), mirroring C9's WS-6 6a/6b split (C9's lane). Same facts, two complementary homes.

**Cross-cutting note on the C5↔C9 reconciliation (not my seam, but it touches my halt-semantics F3) — COHERE.**
C9's gate-at-build-time (C5) vs degrade-at-runtime (never-halt-bound) discriminator is consistent with — and is a fourth instance of — the disambiguation discipline in my F3. My F3 named three control events sharing the word "halt" (drift→HALT integrity gate / `.loop-halt` work-exhaustion stand-down / never-halt progress posture); C9's resolution adds the surface-and-time axis (CI/PR-time gate may fail a PR; loop-runtime mode never halts the turn; never-halt binds *only* the runtime surface). These compose — the WS-4 G1 byte-budget `--check` gate is a build-time event, distinct from all three of my runtime "halt" semantics. **I flag for DESIGN.md only that the `HOOKS.md` halt-semantics disambiguation (F3) should be extended to name C5's build-time `--check` gate as a *fourth distinct control event* so the four are not conflated** — that is additive to my F3 and aligned with C9's reconciliation; not a conflict. (The never-halt/defer-and-continue posture is operator-locked and not relitigated by either of us — preserved.)

### Residual cross-cutting conflict?

**None — coheres.** Both sibling dispositions I was asked to cross-read (C2's WS-2/AGENTS.md rescope, C9's AR-2 home-of-record + AR-7 credit/build split) nest cleanly with my lifecycle-topology lane, and both empirical legs I verified (no top-level doc names exist; zero `@import` lines in project CLAUDE.md) reproduce. The three artifacts on the `tools/hooks/` surface (HARDENING_PLAN execution / WS-6 sequencing / `HOOKS.md` topology-description) have non-overlapping authority with an explicit direction-of-truth rule, so they compose rather than fork. C2's `@import`-forbid is adopted as a hard guard on the `HOOKS.md` I co-own. The one item I hand the orchestrator is **additive, not a conflict**: extend the F3 halt-semantics disambiguation in `HOOKS.md` to include C5's build-time `--check` gate as a fourth named control event (aligned with C9's C5↔C9 reconciliation). All never-halt / defer-and-continue / prefer-free-ollama / paid-secret-destructive deny-list postures preserved; additive-only, PLAN-not-execution, no design-substrate/src/roadmap edits.