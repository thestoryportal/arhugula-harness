Verified. The never-halt / defer-and-continue / prefer-free-ollama / locked deny-list lock is at `00-CHARTER.md:33-34` ("This arc does not relitigate them") — a session-runtime posture, exactly as I asserted in Stage-2b. Both surface anchors of the C5↔C9 composition now re-confirm independently. I have everything needed. Writing my cross-cutting response.

## C5 cross-cutting response

### Lane dispositions (re-run carry-forward, unchanged from Stage-2b)

| AR-ID | Disposition | PLAN delta |
|---|---|---|
| **AR-5** (WS-4 proportionality — rank G1–G4 against solo scale) | **ACCEPT** | DESIGN.md WS-4 gets an explicit MVP gate set + proportionality ladder (cluster-2 §1.11). **MVP (build now) = {G1}** — `--check`-tier byte-budget gate over CLAUDE.md + MEMORY.md, modeled byte-for-byte on `tools/substitution_ledger.py --check` (CI-wired `ci.yml:253`, `exit 1` at :206, contract test :257). **Deferred:** G2 (clearance-marker schema gate) = promote-on-trigger (real gap — 19 markers, typed `clearance_type` enum in `TEMPLATE.md`, zero `tools/` validator — but governance-overhead under-paid at 19 hand-authored markers); G3 (ledger-shape gate) = fold-into-G2 (shares `validate()→violation-list→exit-1` machinery); G4 (freshness teeth) = resolved-as-no-new-gate (the §12 check exists; teeth-decision = leave as model-prose drift-reconciliation, no blocking gate, per the C5↔C9 line below). |
| **MISSED TENSION C5↔C9** (gate-vs-never-halt) | **RECONCILE — COMPOSE (surface-disjoint)** | G1 `--check` fires at **CI/PR time** (build-machine check on a proposed diff, identical surface to `substitution_ledger --check` at `ci.yml:253`); never-halt / defer-and-continue (CHARTER §2, `00-CHARTER.md:33-34`, verbatim) governs **session-runtime in-loop** behavior. G1 is scoped to fire ONLY at CI/PR-time and is **forbidden from firing as a session-runtime in-loop blocker**; in-session budget overage routes to C9's `context_budget_exceeded` degradation MODE (observe → degrade → continue, never halt). |

### Cross-cutting coherence checks

**Seam 1 — C5↔C9 composition (THE seam I must confirm).**
Sibling: **C9**. Verdict: **COHERE — C9's words describe the SAME composition I do, with the same surface-discriminator and the same co-ownership split.**

C9's Stage-2b half (c9.md:19, plus rationale at :29 and residual at :34) states it verbatim:
- *"C5's `--check` byte-budget GATE is a **build-time / PR-time / CI-time** check (`tools/substitution_ledger.py --check`-tier; `exit 1` on over-budget) — it fails *a PR before merge*, where halting is the correct and intended behavior."* — Identical to my "G1 is a pre-merge CI gate (fails the build on a proposed over-budget change)."
- *"C9's `context_budget_exceeded` is a **session-runtime graceful-degradation MODE** … it fires *inside a live loop turn* where the locked never-halt / defer-and-continue posture governs, so it MUST observe-and-recover … never halt the turn."* — Identical to my "in-session budget overage routes to C9's `context_budget_exceeded` degradation mode (never-halt)."
- C9's discriminator: *"gate-at-build-time (C5/G1, may fail a PR) + degrade-gracefully-at-runtime (C9 mode, never halts the loop) are distinct surfaces; the never-halt posture binds only the runtime surface, never the CI gate."* — This is byte-for-byte the boundary statement I authored. We use the same word ("surface"), the same time-discriminator (build/PR/CI-time vs session-runtime), and the same never-halt scoping (binds runtime only, has no jurisdiction over the CI gate).
- Co-ownership split is identical and three-way consistent: C9 says *"C5 owns the GATE tier; C9 owns the runtime MODE shape; C3 owns the compaction POLICY the mode exits via."* My Stage-2b assigned exactly the same: C5 owns the CI/PR-time gate contract, C9 owns the session-runtime degradation mode, C3 owns the cap value / compaction policy (cross-lane defer). The third leg (C3 = compaction policy) cross-confirms against C3's own c3.md:11 ("C3 owns the cap value + compaction policy … the gate is C5's — consistent with the T2 resolution") — so the C5/C9/C3 triad is mutually consistent on this seam, not just the C5↔C9 dyad.

**Single agreed C5↔C9 resolution:** The G1 `--check` byte-budget gate (C5-owned, CI/PR-time, `exit 1` on an over-budget CLAUDE.md/MEMORY.md diff, modeled on `substitution_ledger --check` at `ci.yml:253`) and C9's `context_budget_exceeded` graceful-degradation MODE (C9-owned, session-runtime, observe→degrade→continue) govern **disjoint surfaces** — PR-time artifact-correctness vs in-loop resilience — and therefore **compose without contention**. The never-halt / defer-and-continue lock (`00-CHARTER.md:33-34`) binds only the runtime surface; the CI gate is explicitly forbidden from acting as an in-loop blocker. C3 owns the compaction policy the runtime mode exits via. No never-halt conflict survives. This matches the adversarial reviewer's own pre-judgment (REVIEW §6 missed-tension #1: the `--check` gate is CI/PR-time, "different surface from never-halt-in-the-loop"), so the council now closes structurally what the reviewer only suspected.

**Seam 2 — Unified MVP slice (C5 WS-4 ⟷ C2 + C7's WS-5 ranking).**
Siblings: **C2** (read), **C7** (c7.md present but empty — not yet produced this stage; reasoned from C2's MVP per task instruction). Verdict: **COHERE.**

- **G1 is the shared load-bearing gate — confirmed.** My WS-4 MVP = {G1}; C2's MVP (c2.md:11, AR-5 disposition) names *"WS-1 (the cost win) + WS-4 G1 (the one gate that holds the split) + WS-6/D14 (recovery co-requisite per T4) + X (FM-H fix)."* C2 explicitly imports **WS-4 G1** as the gate leg of its unified MVP and uses my exact load-bearing rationale — *"the one gate that holds the split."* Our two MVPs are not just compatible, they reference the same artifact for the same reason: G1 is the single gate that converts the C2/C3 static/dynamic split from prose (which "doesn't fail a build" — the mechanism that let §2 reach the re-measured 277,298 B = 81%) into durable teeth. No conflict.
- **The MVPs compose, they don't compete.** C2's MVP is the *full unified slice* (cost-win WS-1 + my gate G1 + recovery WS-6/D14 + FM-H fix X); my WS-4 MVP is the *gate leg* of that slice. C2 ranks WS-1 load-bearing #1, splits WS-2 (SSOT-pointer/INDEX.md = load-bearing; orientation-doc tail = deferrable), and pulls in {G1} as the gate — which is precisely my deferral verdict (build {G1}, defer {G2/G3/G4}). There is no double-counting and no over-build: C2 takes exactly one gate (G1) into the MVP and leaves the rest of WS-4 deferred, matching my ladder.
- **C7's WS-5 health hooks (reasoned from C2's vantage, pending C7's own ranking).** C2's MVP does NOT include WS-5 — consistent with WS-5 (observability/health-hook self-monitoring) being a proportionate-but-deferrable tail at solo scale per cluster-2 §1.11, the same filter I applied to G2–G4. My lane only touches WS-5 at the secrets boundary (any WS-5 health hook injector must be secret-free by construction — C2 re-affirmed this at c2.md:13 AR-8, and C10 *enforces* via `permission-guard.sh`). I name **C7 as owner of the WS-5 ranking**; from C2's vantage the unified MVP is gate-complete with {G1} alone and does not require WS-5, so no conflict is visible from my lane. If C7's eventual WS-5 ranking proposes a *health-gate* that fires in-loop, that would route to the same C5↔C9 surface-discriminator (CI/PR-time gate vs never-halt runtime) — but that is a C7×C9 question, deferred, not a C5↔C2 conflict.

### Residual cross-cutting conflict?

**None — coheres.**

- The C5↔C9 seam resolves to a single agreed composition (surface-disjoint CI-gate vs runtime-degradation-mode), with C9's Stage-2b words matching mine clause-for-clause and the C3 third leg (compaction policy) cross-confirming the triad. Never-halt is preserved, not relitigated.
- The unified-MVP seam coheres: G1 is the shared load-bearing gate, imported into C2's full MVP slice with my exact rationale; my G2–G4 deferral matches C2 taking exactly one gate. WS-5 (C7-owned) is deferrable from C2's vantage and only touches my lane at the secrets-free-injector boundary (already affirmed by C2/AR-8, enforced by C10).
- **Cross-lane defers named for the orchestrator (not conflicts):** cap VALUE + at-rest compaction policy = **C3** (WS-3, confirmed at c3.md:11); cut-list/altitude the budget covers = **C2** (WS-1); G4's drift-reconciliation HITL primitive = **C11**; gate placement in the hook/CI topology = lifecycle owner (**C1** for HOOKS.md-as-topology-contract); WS-5 health-hook ranking = **C7** (its c7.md is pending this stage — orchestrator should confirm C7's MVP omits WS-5 / keeps health hooks deferrable once it lands, to close that leg cleanly). I specify only the G1 gate-contract and its CI/PR-vs-runtime surface boundary.