I have everything I need. The two decorrelated evaluators converged hard on my exact lane (CA-1), and the advisor explicitly named `[[use-the-product-probe-pattern]]` as the fix — which I must operationalize. Let me ground the proportionality discipline I'll cite, then write.

The C8 skill gives me the canonical apparatus: **error-analysis-first Husain loop** (Stage 1 = manual review of 20-50 outputs → categorize into a labeled failure taxonomy → only then automate), **counterfactual baseline mandatory** (FM-H = single-arm claim is a quality failure), **binary/measurable** (FM-H counterfactual-free; eval-set-construction five fields), and **the human floor** (the probe's grader is Robert, not a model-judge — avoids the meta-eval recursion and stays solo-scale). CA-7 maps to my decision-claim vocabulary directly: the success criterion is a **regression-pass criterion against a counterfactual baseline (v(N-1) = current CLAUDE.md)**, not "reconciled-to-zero."

I have my answer. Writing the deliverable now.

## C8 Stage-3b reconciliation

*Voice C8 (Eval Engineer) — the genuinely-missing eval voice. Lane: drift metric + before/after validation (CA-1 load-bearing half) + validation-≠-agreement (CA-7). Grounded in the C8 skill's error-analysis-first Husain loop, mandatory counterfactual baseline (FM-H), binary measurability, and the human-floor stop (FM-M) — kept proportionate per cluster-2 §1.9/§1.11 + the CA-2 anti-bloat filter.*

### Disposition table

| CA-ID | Disposition | PLAN-v3 delta |
|---|---|---|
| **CA-1** (drift metric + before/after probe) | **ACCEPT** | Add **WS-0 "Drift definition + use-the-product probe"** as a first-class workstream and **make it the PLAN's success gate**. It carries: (a) a 6-class **binary-checkable drift-incident taxonomy**; (b) a **lightweight error-analysis probe** (Husain Stage-1 manual review, ~15-25 sessions, current-CLAUDE.md vs slimmed-CLAUDE.md, **counterfactual baseline = current `CLAUDE.md` @ HEAD = v(N-1)**, graded by **Robert = the human floor**, binary per-incident tally); (c) the **regression-pass criterion**: slimmed MUST NOT increase any drift-incident class vs baseline, and SHOULD reduce ≥1 class. WS-1's byte-cap is **demoted to a leading-indicator proxy**, not the success criterion. |
| **CA-7** (validation ≠ council agreement) | **ACCEPT** | In `DESIGN.md`, add one **falsifiability clause**: *"This PLAN's success is confirmed or falsified by the WS-0 probe (measured drift-incident counts, slimmed vs current), NOT by reconciliation-to-zero. Reconciliation removes contradictions; it does not validate the outcome."* State plainly: **byte≤cap is an acceptable leading-indicator proxy, NOT the acceptance gate** — every MVP item still carries a `cost / drift / governance` tag (per advisor #1), but **only WS-0 can declare the PLAN succeeded**. |

### Rationale

**CA-1 — ACCEPT.** Both decorrelated evaluators independently reached my lane's conclusion: Codex DRIFT-CONNECTION "*this is a context slimming plan, not yet a demonstrated drift-reduction plan*"; advisor #2 "*nothing validates that this reduces drift... 'byte-budget ≤ cap' is a proxy, not an outcome.*" That convergence is the strongest signal of the arc, and it is a textbook **FM-H (counterfactual-free claim)** + the catalog's **end-state-evaluation** gap: the PLAN asserts "better" (less drift) with no baseline and no measured outcome. Here is the concrete definition + protocol:

**(a) Drift = a binary-checkable incident set (the taxonomy).** Drift is not a continuous "rot" feeling — it is a *countable per-session incident* against the loaded context. Six classes, each a yes/no a human can tally from a session transcript:

| # | Drift class | Binary check (1 if observed in the session) |
|---|---|---|
| D1 | **Stale-rule use** | Agent applied a CLAUDE.md/MEMORY.md rule that is no longer current (superseded/wrong-version). |
| D2 | **Wrong-canonical-artifact** | Agent cited/used a non-canonical or wrong-version design-substrate artifact when a canonical one existed. |
| D3 | **Forgotten task-constraint** | Agent dropped a constraint the operator stated *this session* (mid-window attention loss — the "40KB still rots" failure). |
| D4 | **Memory-pollution / context-poisoning** | Agent acted on a stale/false memory entry, or a bad write entered the store. |
| D5 | **Bad resumption** | On resume, agent mis-derived next-action vs the roadmap/checkpoint truth. |
| D6 | **Instruction-conflict** | Agent surfaced or silently resolved a contradiction between two loaded instructions. |

These map 1:1 to the candidate set the SYNTHESIS handed me, and each is **deterministically labelable by Robert from the transcript** — no model-judge, so the meta-eval recursion (FM-M) never opens and the C5↔C8 boundary (FM-A: per-call gate) is not crossed: I am defining a *population-level incident rate over a window of sessions*, not a per-call gate.

**(b) The before/after use-the-product probe (Husain Stage-1, solo-scale).** Per the advisor's named `[[use-the-product-probe-pattern]]` + the C8 skill's **error-analysis-first** discipline + eval-set-construction five-field contract — deliberately the *lightweight* version (manual review, NOT an automated eval harness — that would be the CA-2 governance-bloat irony recurring in my own lane):

- **Task set:** ~15-25 real coding-loop sessions across the workspace's representative workflow classes (roadmap-continue, spec/plan amendment, a phase-7 impl arc, a resumption). Re-use *existing* `[[use-the-product-probe-pattern]]` sessions — do **not** build a synthetic corpus.
- **Two arms (counterfactual baseline = mandatory):** Arm A = **current `CLAUDE.md` @ HEAD (v(N-1))**; Arm B = **slimmed CLAUDE.md (WS-1 output)**. Single-arm "the slim version feels better" is FM-H and is rejected.
- **Measured how:** Robert (the **human floor**) tallies D1-D6 per session from the transcript → a binary incidence matrix → per-class incidence rate per arm. *Optional* cheap secondary: cache-write-rate proxy (the [MODERATE]-tagged cost signal) logged but **not** the gate.
- **Pass condition (the regression-pass criterion):** **slimmed (Arm B) MUST NOT show a higher incidence rate in ANY drift class than baseline (Arm A), AND SHOULD show a reduction in ≥1 class** (D1/D2 are the most directly attackable by altitude-extraction + the SSOT index). A *zero-incident floor* is not required (proportionate for a solo dev — drift is being *reduced*, not eliminated). If Arm B regresses any class, WS-1 evicted load-bearing content → that is exactly the **CA-3 verify-before-evict** failure, caught empirically.

This is leak-free per the C8 eval-set contract: no train/test contamination (sessions are observed, not authored-to-pass), no oracle-knowledge (the labeler doesn't know which arm "should" win if arms are de-identified), and the baseline is frozen (current HEAD). **One workstream, one matrix, Robert's eyes — no new tooling.** That is the entire validation apparatus the PLAN was missing.

**CA-7 — ACCEPT.** The advisor's own calibration is decisive: "*concurrence = weak evidence (correlated blind spots)... if it merely concurs, log that as 'no new signal,' not 'validated.'*" Reconciliation-to-zero is a *coherence* property (no internal contradictions); it is not a *correctness* property (the plan reduces the operator's pain). Conflating them is the meta-failure Codex #4 named. The fix is one sentence in DESIGN.md plus the demotion of byte≤cap: byte-count is a fine **leading indicator** (cheap, CI-enforceable, correlated with cache-cost) — keep it as the WS-4 G1 *guardrail* — but it is **not the acceptance gate**. Acceptance lives at WS-0's measured drift-reduction. This directly answers the operator's question I was handed: *byte≤cap is an acceptable proxy/leading-indicator; it is NOT required that every WS carry a drift-linked measurable* — only that **the PLAN as a whole** is falsified/confirmed by the one WS-0 drift probe. Per-WS, the `cost/drift/governance` tag suffices.

### Cross-coherence

- **C2 (CA-1 keep/position WS + CA-3 verify-before-evict):** **COHERE — complementary, no conflict.** C2 owns *what to keep and how to attention-position it* (WS-0 "retained-content" in the SYNTHESIS sense). My WS-0 is the *measurement* that tells C2 whether its keep/position call was right — C2's retained-content hypothesis is the thing Arm B *is*, and my probe is its test. One naming note to avoid a collision: the SYNTHESIS uses "WS-0" for C2's keep/position WS; I am attaching my probe as the **success-gate of that same WS-0** (or label mine **WS-0-validation** if the orchestrator wants distinct IDs). They are the two halves of one workstream — design hypothesis (C2) + falsification test (C8) — which is exactly the C2↔C8 clean seam in my skill (C2 owns prompt/context structure; C8 owns the eval set built on it). My CA-3 touch is *empirical only*: a WS-1 over-eviction surfaces as an Arm-B regression in my matrix — I do **not** author C2's dependency-scan/live-invariant-contract (that is C2's lane); I cite it as the mechanism my probe would catch.
- **C3 (CA-4 minimal-recoverability) + advisor #3 pressure-test on "X":** **COHERE, with my lane's input recorded.** My D4 (memory-pollution) is the drift class that *would make the X workstream load-bearing if observed*. This gives the operator a proportionality lever the SYNTHESIS lacked: **if the WS-0 probe shows D4 incidence ≈ 0 across the window, "X" minimal-recoverability is empirically deprioritizable** (advisor #3's instinct), but if D4 fires even once, Codex's "build minimal recoverability now" is empirically justified. I do **not** override the CA-4 disposition (C3's lane; reconciled toward Codex) — I *connect it to a measurable* so it stops being a taste dispute. No conflict.
- **C5 (CA-5 G1 guardrail-not-religion):** **COHERE — mutually reinforcing.** My CA-7 demotion of byte≤cap from acceptance-gate to leading-indicator *is* the eval-side of C5's "guardrail-not-religion." We agree from two directions: C5 says the gate must warn-then-hard-fail and never force unreadable compression; I say the gate is a proxy, not the outcome — both keep G1 in its place. No contention.

### Residual?

**none — reconciled.** Both assigned findings ACCEPT; the deltas are additive (one new/attached workstream WS-0 success-gate + one DESIGN.md falsifiability clause + a proxy-demotion), introduce **zero new tooling/ledgers/hooks** (CA-2-clean — manual error-analysis, Robert's eyes, re-used probe sessions, frozen-HEAD baseline), stay strictly in the C8 eval-methodology lane (cross-lane work named and deferred to C2/C3/C5 owners), and preserve the standing locks (never-halt/defer-and-continue/prefer-free-ollama/paid-secret-destructive deny-list are untouched — the probe reads transcripts, fires no paid call). The single most important Stage-3b shift is delivered: **the PLAN's success criterion moves from byte-count → measured drift-incident reduction against the current-CLAUDE.md counterfactual, graded at the human floor.**