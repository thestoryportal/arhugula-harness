The advisor sharpened three things I'll fold in: (1) name the minimal CA-4 mechanism as `git init` + commit-on-write (fixing a documented FALSE claim, not adding a feature — that's the anti-bloat justification AND the drift-connection), (2) CA-6 is C3-owns-policy not a second gate — compose with C5's G1, and (3) actively keep my own pet findings F3/F4/F6 deferred. Let me write the deliverable.

## C3 Stage-3b reconciliation

### Disposition table

| CA-ID | Disposition | PLAN-v3 delta |
|---|---|---|
| **CA-4** (memory rollback is load-bearing) | **ACCEPT (toward Codex)** | Reshape "X" into a **two-part PLAN spec**, tagged **drift**: **MVP = minimal recoverability via `git init` of the out-of-worktree memory store + commit-on-write at SessionEnd** (establishes the missing Tier-2 rollback boundary; restore = `git checkout`). **Deferred (trigger-gated) = serialization/locking**, fired only on an *observed* concurrent-write race. Detect-only is **rejected** (cannot restore a lost entry). The plan-line justification is foregrounded: this **fixes a documented FALSE claim** — CLAUDE.md §12.5.1 asserts "Provenance lives in git history at the global memory store," which is empirically false at HEAD (re-confirmed this session: `fatal: not a git repository`). So this is *closing a lie*, not adding a feature. *(Plan-only per AR-1: this arc specifies the rollback-boundary contract; a downstream execution arc runs `git init` and binds the hook.)* |
| **CA-6** (MEMORY.md retention closer to MVP) | **ACCEPT-promote** | Promote **enforced MEMORY.md compaction into the MVP slice**, tagged **drift**, scoped as a **C3-owned pruning *policy*, NOT a second gate**. Policy: axis = byte-budget (≤ 24,400 B cap); compaction action = shorten index descriptions / drop superseded entries / **preserve full text at `memory/<slug>.md`** (per C3 skill §"Pruning and GC"); operator-tunable as a `memory_index_max_bytes`-analog. Bind the **existing** `memory_audit` round artifacts + the **existing** advisory over-cap flag (already in `session-end-cleanup.sh:53` + `loop-gc.sh:56`) to fire the compaction action — **warn → compact, never block** (preserves never-halt). Eval signal: `MEMORY.md bytes ≤ cap`, machine-checked at SessionEnd. |

### Rationale

**CA-4 — Codex is right; recoverability is load-bearing.** The advisor's "regenerable-hints-so-skip" read holds *only* if a lost entry is cheaply reconstructable — but the §C finding (re-verified: the store is not a git repo up the tree) means a bad write or delete is **permanently unrecoverable today**, independent of any race. That is a real consequence-HIGH gap, not a speculative-incidence one. The proportionate remedy is the *minimal* one: `git init` + commit-on-write gives full history and trivial restore using C3's own Tier-2 substrate — and crucially **avoids re-committing the governance-native-bloat sin (CA-2)** that a bespoke snapshot/`.bak` regime would (backup files pile up = my own ledger-bloat). Note the lock can stay deferred *precisely because* git-init makes a concurrent write recoverable (both versions land in history); locking only optimizes which one wins, which is exactly what licenses trigger-gating it.

**CA-6 — promote, because the drift-connection is live in this very session.** MEMORY.md is session-loaded every session; over-cap → truncation-at-load (the system reminder this session literally states "Only part of it was loaded") → index entries silently never enter context. That is two **named CA-1 drift categories direct, not proxy**: `forgotten-constraint` + `memory-pollution`. The minimal form is **not a compaction engine** — it's binding an existing audit primitive + an existing advisory flag to an enforced action. This is the cheapest possible promotion and it connects straight to the drift metric, which is why it earns MVP placement under the CA-2 anti-bloat filter.

### Cross-coherence

**One flag — CA-6 ↔ C5's G1 (resolve by composition, do NOT duplicate).** WS-4/CA-5 scopes G1 to "CLAUDE.md + MEMORY.md byte-budget `--check`." My CA-6 delta and C5's G1 must NOT read as two gates over the same surface. Composition: **G1 = the detector/gate (C5, CI/PR-time, `exit 1` over cap); CA-6 = the compaction *remedy* (C3, SessionEnd-runtime, warn→compact) that the over-cap signal triggers.** Disjoint surfaces (gate detects; remedy acts), so they compose without contention — and both honor the C5↔C9 never-halt resolution (CI-gate is not an in-loop blocker; runtime remedy degrades-and-continues). C5 should cite this composition in the G1 delta; I cite it here.

**No conflict on CA-4** — pure C3 Tier-2 lane, no sibling surface engaged. **Deliberately holding my Round-2 findings F3 (checkpoint-store asymmetry), F4 (`.harness/` ledger retention), F6 (version-chain copies) in the deferred tail** — Codex's MVP-slice explicitly defers "archive asymmetry," and the advisor flagged these as the invested-in durability findings most prone to my voice's proportionality lapse. The valued disposition here is to let my own pet findings stay deferred and ship only the two **drift**-connected minimal remedies. (C2 owns the within-turn cut-list / attention-positioning half of CLAUDE.md per T-perm-2; C8 owns the drift metric + before/after probe — both deferred to their owners, not absorbed.)

### Residual?

**none — reconciled.** Both findings ACCEPT with concrete minimal deltas; the single cross-coherence risk (CA-6↔G1 reading as a duplicate gate) is resolved-by-composition and stated explicitly above so it does not surface as an open residual at Round 3.