The advisor confirms the shape and sharpens the framing precisely: my two findings are the front half of one **specify-keep → verify → evict** sequence, both land as reframes/preconditions (not new artifacts), and the tripwire is the 6-row governance sub-table. I'll write the deliverable now in one pass.

## C2 Stage-3b reconciliation

### Disposition table

| CA-ID | Disposition | PLAN-v3 delta |
|---|---|---|
| **CA-1** (keep/position) | **ACCEPT** | Promote **WS-0 "retained-content + attention-positioning"** to first-class, *ahead* of WS-1. WS-0's load-bearing content is **attention ORDERING** (not the keep-list — P1 already holds the KEEP set: §1, §3, §4, §5, §10, §11, §12, §13, §14 + locked rules). Commitment: the most drift-critical tokens — locked operating rules (never-halt / defer-and-continue / prefer-free-ollama / paid-secret-destructive deny-list) + §12 loop discipline — sit at an **attention-favorable position (start and/or end), explicitly OUT of the lost-in-the-middle zone** (research §2.2.2 U-shaped). Reframe the PLAN success criterion from "byte ≤ cap" → **"the retained ~40KB demonstrably reduces drift"** — C2 owns positioning + the eval-*surface*; **C8 owns the drift metric + before/after probe** (named, not absorbed). |
| **CA-3** (verify-before-evict) | **ACCEPT** | WS-1 gains a **3-item precondition checklist** (a verification gate, NOT a new workstream), and WS-0→CA-3→WS-1 is framed as **one "specify-keep → verify → evict" ordering**: (a) confirm live pointers (artifact→canonical-version) are lifted to the WS-2 SSOT index *before* provenance leaves — no new doc; (b) **dependency-scan = a grep** of hooks/scripts/tools/skills for any consumer that reads `CLAUDE.md §2` or resolves canonical-version *out of* those cells → repoint to the SSOT index first; (c) the archive target must be **verified-already-git-versioned** in `design-substrate/` spec files (delta-only-preserve) — **verify, don't assume** (diff/grep to confirm content recoverable). Explicitly NOT the memory store (that's the not-git-versioned one; CA-4/C3's lane). |

### Rationale

**CA-1 — ACCEPT.** My Round-1 contribution was rich on what to EVICT (the §2 cache-detonation cost mechanism, F1/F2) and thin on what stays + WHERE — exactly the decorrelated catch. P1 already names the KEEP set, so WS-0 would be pure ceremony if it restated that; its *only* new content is the positioning claim, and that positioning is **the mechanism by which retained context reduces drift** (not "fewer bytes"). **C2 sets the attention-positioning discipline:** the drift-critical operating rules + loop discipline occupy attention-favorable positions (start/end), out of the U-shaped lost-in-the-middle zone (research §2.2.2). The reframe of success from byte-count → drift-reduction is jointly mine + C8's — I commit the eval-surface (which observable: e.g. "does a coding session use a stale rule / wrong-canonical-artifact after the slim?"); **I defer the metric definition + the use-the-product before/after probe to C8 by name** (FM-A discipline — I do not define drift).

**CA-3 — ACCEPT, and the actual protocol:** WS-1 eviction is gated by a precondition checklist run in this order. **(a) Live-invariant lift:** the "compact contract" is *not* a third doc — it is WS-0's retained prefix + WS-2's SSOT `design-substrate/INDEX.md` (artifact→canonical-version); confirm those pointers are populated before any §2 cell body leaves the prefix. **(b) Dependency-scan = grep, not build:** `grep -rn 'CLAUDE.md §2\|CLAUDE.md.*Canonical artifact'` across `tools/ .githooks/ .claude/` (hooks, scripts, skills) — any consumer that resolves a canonical version *from* the §2 cells (rather than from the SSOT index) is repointed to the index *first*; this prevents a NEW drift class (a broken active dependency on evicted provenance). **(c) Archive-target verification:** the §2 cell bodies are delta-version change-note summaries; the source provenance very likely already lives git-versioned in the `design-substrate/*.md` spec files per the delta-only-preserve-verbatim convention — but **verify before evicting** (the cells are summaries, not byte-identical), via a spot diff/grep that the change-note content is recoverable from a git-versioned location. This is the out-of-family catch's sharpest edge ("don't rely on 'git history somewhere'") and the verification is cheap.

**Anti-bloat self-check (CA-2):** every delta above is a **reframe / precondition / verification of something already in the plan** — zero new files, hooks, ledgers, or sub-tables. WS-0 adds an ordering claim to an existing keep-list; CA-3 adds a grep + two confirmations to WS-1. The proportionate framing *is* "specify-keep → verify → evict" as one coherent sequence (you cannot know what to evict until you've said what stays — verify-before-evict comes for free). No 6-row governance sub-table under WS-0.

### Cross-coherence

- **vs C8 (CA-1 metric):** No conflict — complementary by construction. I own positioning + name the eval-surface; C8 owns the drift metric + the before/after use-the-product probe. The reframe ("success = drift-reduction") is the *shared* deliverable; the split (I name the observable / C8 defines the metric) is the lane boundary, consistent with the synthesis CA-1 owner-split.
- **vs C3 (CA-3(c) archive + CA-4 memory rollback):** No conflict, and a sharp **anti-conflation guard**: CA-3(c)'s "archive must be git-versioned" applies to the **§2 provenance → `design-substrate/` spec files**, which IS git-versioned (verify, don't assume). It is **distinct from** the memory store at `~/.claude/projects/.../memory/`, which is NOT git-versioned (round2 §C) and is **C3's CA-4 lane** (minimal-recoverability). I explicitly do not touch the memory-store rollback; I only verify the *design-substrate* archive. The §2-provenance retention *policy* (where it lands at rest, delta-only-preserve convention) remains C3's at-rest surface — I own only that it leaves the prefix and is verified-recoverable before eviction.
- **vs C5 (CA-5 G1 gate):** No conflict — my WS-0 byte target (~40KB) feeds G1's `--check` cap, but the gate posture (warn-then-hard-fail, override/waiver, effective-context) is C5's; I name the budget, C5 holds it.

### Residual?

**None — reconciled.** Both findings ACCEPT, both land as reframes/preconditions (zero new ceremony), the WS-0→CA-3→WS-1 sequence is the proportionate framing, and the FM-A lane boundaries to C8 (metric) and C3 (memory-store rollback + at-rest retention) are held explicitly under the reframe pressure.