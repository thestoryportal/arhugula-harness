I have everything I need. My finding is CA-5 (the G1 gate contract refinement) — and notably I am the C5 voice that authored these gate contracts in Round 1, so I have direct grounding. Let me write the reconciliation.

## C5 Stage-3b reconciliation

### Disposition table

| CA-ID | Disposition | PLAN-v3 delta |
|---|---|---|
| **CA-5** | **ACCEPT (with one sharpening that REBUTs the "effective-context" framing as MVP)** | Refine the G1 gate contract to the 5-clause form below: (1) **measures the loaded-prefix budget**, defined as a deterministic sum of bytes-actually-auto-loaded (`CLAUDE.md` + its `@import` chain + `MEMORY.md`) — **not** "raw repo bytes" and **not** a token-estimator; (2) **explicit override/waiver** via a `# g1-budget-waiver: <reason>` line that the gate echoes, never silently honors; (3) **warn-then-hard-fail**: `--check` runs in `warn` mode (exit 0 + STDERR notice) until WS-1's eviction lands a clean baseline file `.harness/g1-baseline.json`, then flips to `enforce` (exit 1 over cap); (4) **never forces unreadable compression** — the gate asserts a byte ceiling ONLY, emits no compression directive, and the cap is set at WS-1's post-eviction measured value + headroom (not an aspirational floor); (5) **CI/PR-tier only** — the gate is forbidden from firing in-loop (PreToolUse/Stop), composing with the C5↔C9 never-halt lock. **G2–G4 stay deferred per AR-5** (do not re-expand). |

### Rationale

**CA-5 is correct and I accept it — it sharpens my own Round-1 G1 contract toward the drift metric, which is exactly the anti-bloat direction this stage demands.** My Round-1 G1 (`F4`/`F2`) said "bytes ≤ declared cap, CI tier, exit 1, modeled on `substitution_ledger --check`." That was right on *tier* and *teeth* but Codex's out-of-family catch is load-bearing: a raw-byte cap that flips to hard-fail on day one, with no baseline and no override, **reintroduces the exact prose-budget failure it replaces** — it just fails the build instead of being ignored, and an operator under a red CI gate will compress §1 into unreadable shorthand to pass it (the "never force unreadable compression" clause forbids precisely this). The four Codex refinements map cleanly:

1. **Effective loaded context, not raw bytes.** The actual definition: G1's input is `bytes(CLAUDE.md) + bytes(each @import target) + bytes(MEMORY.md)` — the set Claude Code *auto-loads into the prefix every turn* (per AR's verified finding that only `CLAUDE.md` + its `@import` chain auto-loads; WS-2 anchors are JIT). This is deterministic, requires no token estimator (which would break the C5↔C8 line — a token *model* is a population claim, byte-count is per-artifact deterministic), and is drift-connected because the auto-loaded prefix IS what rots mid-window. **REBUT of one over-reach:** I do *not* accept "effective loaded context" if it means modeling attention/token-position decay — that is C8's holdout territory and C2's attention-budget lane; G1 stays a byte sum. I read Codex's "effective" as "the bytes that are actually loaded" (the `@import`-closure), not "the bytes weighted by attention," and I'm pinning that reading.

2. **Override/waiver path.** Definition: a single `# g1-budget-waiver: <one-line-reason>` comment line in the doc; when present, the gate exits 0 but **echoes the waiver + the overage to STDERR and to the gate-event** so the override is visible in CI logs and the C7 span — a waiver is never silent. This is the proportionality release-valve: a legitimately-large session (a big bundled-absorption arc) can ship without the gate becoming a religion.

3. **Warn-then-hard-fail against a clean baseline.** Definition: G1 reads a mode from `.harness/g1-baseline.json` (which WS-1's *execution* arc writes after eviction, recording the post-cut measured prefix bytes). Pre-baseline (today, prefix is ~334KB+) the gate runs `warn` (exit 0, loud STDERR) so it never red-lights a repo that hasn't been slimmed yet; post-baseline it runs `enforce` (exit 1). This is the ONLY sequencing that lets the gate land in the same PLAN as WS-1 without bricking CI before the cut happens.

4. **Never force unreadable compression.** Definition: the gate's only assertion is `sum ≤ cap`; it emits no "compact this" directive (unlike the current `session-end-cleanup.sh:53` advisory which prints "compact"), and the cap is `baseline + headroom` (a ceiling above the *achieved readable* state), not a squeeze target. The drift-connection: the goal is "the prefix stays at the slimmed-but-readable size," not "minimize bytes" — over-compression is itself a drift source (forgotten constraints).

**Composition with C5↔C9 never-halt (confirmed COHERE).** Round-2 §B.2 already byte-aligned this: G1 `--check` lives at CI/PR-time (`exit 1`), C9's `context_budget_exceeded` mode lives at session-runtime (observe→degrade→continue). They govern **disjoint surfaces** — clause (5) makes the disjointness explicit in the gate contract itself: G1 is *forbidden* from firing at PreToolUse/Stop. A hard-fail CI gate therefore *cannot* contradict never-halt/defer-and-continue, because never-halt binds only the runtime surface and G1 never touches it. The hard-fail is a pre-merge guardrail on the artifact-at-rest; the runtime always defers-and-continues. No contention.

**Anti-bloat self-discipline (CA-2):** my delta adds **zero new artifacts** — it *refines the contract of the one gate already in the MVP slice* and adds exactly one tiny state file (`g1-baseline.json`, written by the downstream execution arc, not this plan). G2/G3/G4 stay deferred. The waiver clause is anti-ceremony by construction (one comment line, no approval workflow). This is the *minimal* change that connects G1 to drift-reduction rather than byte-count.

### Cross-coherence

- **vs C8 (CA-1 drift metric + before/after probe):** COHERE and mutually reinforcing. G1 measures the deterministic *input* lever (auto-loaded prefix bytes); C8 owns whether that cap *value* reduces measured drift on the use-the-product probe. My clause (4) (cap = baseline+headroom, not a squeeze) is the right shape precisely *because* C8's probe — not G1 — is the success criterion. I make zero population claims (FM-A held); G1 surfaces bytes-vs-cap, C8 judges the threshold. No conflict.
- **vs C2 (CA-1 keep/position WS + CA-3 verify-before-evict):** COHERE. C2 owns the cut-list + altitude + attention-positioning; G1 only *enforces the byte ceiling C2's cut produces*. My REBUT of "effective = attention-weighted" explicitly *cedes* attention-positioning to C2 — G1 doesn't model it. Clause (3) warn-then-enforce is sequenced *behind* WS-1's eviction so it composes with C2's verify-before-evict precondition (the baseline is written only after the dependency-scanned eviction lands).
- **vs C3 (CA-4 minimal-recoverability + CA-6 MEMORY.md retention to MVP):** COHERE. C3 owns the cap *value* + at-rest compaction/retention policy + memory-store recoverability; C5 owns only the *gate that fails when the cap is exceeded*. CA-6 promoting MEMORY.md compaction to MVP strengthens G1 (MEMORY.md is in G1's loaded-prefix sum), and C3's compaction policy is what *makes the cap satisfiable* — the gate and the policy are co-primary at the seam, both in MVP, no contention. Clause (1)'s prefix-set explicitly includes MEMORY.md, consistent with CA-6.
- **vs orchestrator (CA-7 reconciled-to-zero ≠ correctness):** COHERE — my delta is *falsifiable by C8's probe*, not validated by council agreement, exactly as CA-7 demands. G1's cap value is provisional until the probe confirms it reduces drift.

No conflict with any sibling. The one place I sharpen against the literal CA-5 text ("effective loaded context") is a *boundary-hold* (byte-sum-not-token-model), which protects the C5↔C8 line my siblings rely on — it strengthens cross-coherence rather than breaking it.

### Residual?

**none — reconciled.** G1 stays the single load-bearing gate (G2–G4 deferred per AR-5, not re-expanded); the 5-clause refinement is drift-connected, proportionate, and adds no ceremony; never-halt composition confirmed disjoint-surface; all four sibling seams COHERE.