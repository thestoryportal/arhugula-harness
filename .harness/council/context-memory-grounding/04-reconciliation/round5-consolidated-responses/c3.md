# C3 — consolidated reconcile (Round 5, wave2) — verbatim

> Genuine agent invocation · workflow `wf_be40ac24-ebe` · responds to the merged adversarial + Codex + advisor findings · 2026-06-04.

---

The cites confirm the proportionality math. Both patterns are genuinely unwritten (dangling `[[ref]]` only, no note file), each at 5 inbound refs — both well past the `≥4-ref` trigger. The store has 171 note files (the EVID "169" + drift). This is the empirical ground for my reconcile. Let me run the co-primary scan and write.

**Co-primary scan (SKILL §"Activation discipline"):** ITEM 3 (degree-tiering as compaction key) is Tier-1/Tier-4 retention + pruning policy — squarely C3, with a C2-read seam I do NOT author. ITEM 4 (promote-2-patterns) is the semantic-tier *write* — C3-owned per my (b) confirm-back, with C1 owning the firing trigger (already settled in cross-read; the trigger is *already met*, so no firing-site question is open). The C9-react is store-side recoverability — FM-H/X-min, mine. No CONFLICT surfaces; the seam-routed reconcile is the venue. Holding FM-A (no compaction/firing-site absorption), FM-G (pruning axis named), FM-H (concurrent-write), FM-L (Tier-1 frontmatter ≠ Tier-5 hash-chain).

## C3 — consolidated reconcile (Round 5)

I adopt my voice. Engaging by name: **Codex** (cold, out-of-family) earned its two P2s on my lane precisely by catching what I *conceded and then under-folded* — the same move the C8 wave-1 ruling models (cold-Codex finishing a finding the voice half-stated). The **adversarial (E2)** did not flag either item; the **advisor** did not press them. So this is Codex-unique, and the divergence is mine to resolve. I hold the spine: a fold earns MVP only if it changes what the WS-0 grader tallies in D1–D6 *and* costs near-zero machinery. I verified both cites at HEAD before ruling.

### Per-item disposition

**ITEM 3 — carry degree-keyed tiering into WS-3a as the compaction selection key. → RECONCILE-with-refinement (FOLD the degree RULE as the selection key; HOLD the dir-split set-aside; REBUT any in-degree *computation tool*).**

Codex is right that I left the load-bearing thing out. My A1 set aside the episodic/semantic *directory* split correctly (repo-empirical: `fork-*` 25/28 load-bearing, `pr-*` ~50/50 — a flat-by-prefix dir split mis-tiers) — but Codex's sharpening is that **refusing the dir-split is not the same as refusing the degree-rule**, and the degree-rule is the boundary the EVID doc *already counted* (the 39 zero-inbound; the ~20 hub notes). WS-3a's compaction step at HEAD says only "shorten descriptions; move detail to bodies; drop superseded" — it names **no selection key**. So a downstream executor compacting MEMORY.md has no rule for *which* entries to compact first, and would re-make the prefix/category mistake the corpus warns against. That is a real gap that connects to measured drift (D4 memory-pollution: an un-keyed compaction can evict a hub note and keep noise).

- **What changes in DESIGN.md v2, §5 WS-3a:** the compaction step gains an explicit **selection key** clause: *"compact by wiki-link in-degree, not by prefix/category — **KEEP-HOT** the ≈20 hub notes (in-degree ≥5: descriptions stay full); **KEEP-LINKED** the 1–4-inbound notes (compaction-eligible: shorten description, detail to body); **ARCHIVE/JIT** the 39 zero-inbound notes (move body to `memory/archive/`, drop the index line — recoverable per X-min). Thresholds (≥5 / 1–4 / 0) are operator-tunable; the *rule* is degree-keyed."* This **supersedes the bare "drop superseded"** clause's silence on ordering — and dovetails with my A1 supersede-mark fold (the supersede-marked entry is a special case of ARCHIVE: marked + body-archived, never silent-dropped).
- **Drift-class cut:** **D4 (memory-pollution)** primary — degree-keying stops compaction from evicting a load-bearing hub while keeping a zero-inbound singleton, the exact pollution-by-bad-eviction vector; **D1 (stale-rule)** secondary — the 39 zero-inbound includes stale singleton recipes that ARCHIVE removes from the "currently-cited" surface.
- **REBUT the over-reach (which Codex did NOT ask for, but I foreclose so the fold stays cheap):** the rule does **not** pull an in-degree *computation tool* into MVP. In-degree is a `grep -ro '\[\[name\]\]' | sort | uniq -c` one-liner Robert runs by eye at compaction time (I just ran the equivalent — 5 refs each, two commands). Folding the *key* is zero new machinery; folding a *standing degree-recompute daemon* or a graph-index would be the CA-2 bloat my A1 named and the arc refuses. **Report the degree; don't build a degree engine.**
- **Proportionality:** earns its place — it is one clause naming a selection *rule* (not a tool) over a step already in MVP, keying on a count Robert already reads. Codex correctly caught that I under-folded; it did **not** over-reach (it asked for the rule, not the engine), so I ACCEPT the rule and pre-emptively bound it to "by eye." **Boundary (FM-A):** I own the at-rest selection key + the ARCHIVE retention move; I do **not** own the C2 JIT-retrieval-read seam (the "JIT" in ARCHIVE/JIT means C2 may pull an archived body back into a prompt on demand — C2's lane, not mine). I name the key; C2 decides per-turn what to read back.

**ITEM 4 — promote the 2 already-triggered patterns now as MVP one-time hygiene. → ACCEPT-finding (I own what-to-promote + the semantic-tier write; C1 owns the already-met firing trigger).**

Codex is correct and I verified it: both `[[plan-revision-against-not-yet-built-substrate]]` (5 refs, no note file) and `[[strike-revision-on-refined-second-tier-reason]]` (5 refs, no note file) are dangling at HEAD — each is *literally* "a consolidation that should already have fired." My A1 called the 2-pattern promotion "one-time hygiene, not a standing WS" and routed it to *defer* — but Codex's sharpening is decisive: **the defer-trigger (`≥4-ref-unwritten > 0`) is ALREADY MET (it's at 5).** Deferring the whole pass either makes the trigger fire on day one of the execution arc anyway, or leaves a known D4 surface (two dangling hub-refs reading as "a note exists" when none does) sitting open. The honest disposition is exactly C8's ITEM-1 shape: do the one-time thing now; defer only the *recurring* mechanism.

- **What changes in DESIGN.md v2, §5:** add a **one-time MVP hygiene line** (a discrete deliverable-of-this-arc, NOT a new standing workstream): *"Write the 2 currently-dangling ≥4-ref pattern bodies — `plan-revision-against-not-yet-built-substrate.md` + `strike-revision-on-refined-second-tier-reason.md` (each at 5 inbound refs, no note file at HEAD) — into the semantic tier (`memory/<slug>.md` body + MEMORY.md index line). One-time consolidation hygiene; clears the two known dangling hub-refs."* The **recurring consolidation mechanism stays DEFERRED** under §6 with its trigger unchanged (`observed-D4 = ≥4-ref-unwritten count > 0` at SessionEnd) — but now it lands in the execution arc *already at zero* for these two, so its first fire is a genuine new pattern, not a backlog flush.
- **Drift-class cut:** **D4 (memory-pollution)** — a dangling `[[name]]` ref reads as "a current note exists" (the exact pollution vector my supersede-mark fold also closes from the other side); writing the body makes the ref resolve. Secondary **D1** — the patterns themselves (`plan-revision-against-not-yet-built-substrate`) are *load-bearing rules* that should be applied, and an unwritten rule can't be consulted.
- **Proportionality:** earns its place — writing 2 markdown bodies is **bounded, finite, one-time, zero-machinery** (it is precisely the "3 bucket-C breaks = one-time hygiene" shape C5 ratified, mirror-applied to bucket-D). The over-reach Codex correctly did *not* request — and I refuse — is promoting the *recurring consolidation pass* to MVP (that drags C9 cadence + a standing surface = CA-2 bloat, my FM-J). **Boundary (FM-A/FM-J held):** I own *what-to-promote* (the 2 named slugs) + the *semantic-tier write semantics* (Tier-4/Tier-1 body + index, inherits Tier-4 age-decay/relevance pruning, operator-tunable, FM-G named); **C1 owns the firing-site/trigger-as-control-decision** — and here the trigger is observably already true, so there is no open firing-site question, just the one-time write. **FM-L note for the reviewer:** these are Tier-1 frontmatter/body writes, **not** Tier-5 ledger writes — no hash-chain obligation; the store's recoverability of these bodies is X-min's snapshot.

### REACT to C9 wave-1 on ITEM 2 (store-side): does my supersede-mark `superseded_by` schema need C9's standing-pointer resolver, or does X-min recoverability + the `[ -f ]` guard suffice?

**X-min recoverability + the `[ -f ]` guard SUFFICE for my supersede-mark; I do NOT need C9's resolver — and I CONFIRM C9's wave-1 ruling holds the spine on my surface.** C9 ruled dormant-G-LINK + the absence-guard sufficient at bare MVP, rebutting Codex's MVP-now resolver as mis-targeted, with the decisive merit that the adversarial's repo-grep proved the *one* standing machine-managed pointer (the reinject pointer) **resolves at HEAD** while the live cross-store drift Codex named is a *memory-note* break on C3/C5's lane. I confirm the store-side of that exactly:

- **My `superseded_by: [[slug]]` is a Tier-1 frontmatter breadcrumb, not a standing machine-emitted pointer.** At first landing, WS-3a's compaction authors a *handful* of supersede-marks by hand-hygiene — episodic, identical in shape to the "3 bucket-C breaks = one-time hygiene" C5 ratified. A dangling `superseded_by` is **recoverable via the X-min snapshot** (the archived superseded body is on disk) even if not *validated* — so recoverability is the floor, exactly as C9 ruled for the reinject pointer. **No resolver workstream for my surface.**
- **The asymmetry C9 flagged is correct and it cuts in my favor:** my supersede-mark is *episodic hand-hygiene* (dormant-OK); C9's reinject pointer is a *standing* machine-managed ref. C9 ruled the reinject pointer resolves today and routed its cross-store-drift dimension to **C5's WS-4 G1 link-`--check`** (not a new resolver). I confirm my supersede-mark binds to the **same** G-LINK `--check` when it promotes — **MVP-WITHOUT-but-named-as-a-trigger** (my A1's (a) confirm-back stands verbatim): the supersede-mark ships as write-discipline now; the validating `--check` is MVP-on-trigger, with my `superseded_by` surface named as one of G-LINK's triggers (the instant it becomes machine-emitted, or a dangling-supersede D4 is *observed* at WS-0).
- **Drift-class cut (my surface):** **D4** — a `superseded_by` breadcrumb pointing nowhere reads as "a current entry exists"; X-min recoverability + the deferred-but-named G-LINK `--check` close it without over-gating a hand-authored handful.
- **Proportionality:** C9 got it right and I refuse the resolver on my surface for the same reason — pulling a standing resolver/CI gate over a hand-authored handful of breadcrumbs is the governance-bloat irony the arc exists to refuse. **I REBUT Codex's MVP-now resolver as applied to my supersede surface**, and (echoing C9) note Codex's worry is correctly-*shaped* (absence-guard ≠ resolution-validation is a true distinction) but mis-*targeted* on my lane: my breadcrumbs are episodic, the reinject pointer is C9's, and both are covered by recoverability-floor + the deferred-named G-LINK.

### Residual for E4 (adversarial #2) — if any

- **R1 (carried from my A1 confirm-back + C9's wave-1 residual, now jointly sharpened):** the **one seam I cannot close unilaterally** is whether the reinject pointer's *input set* is in C5's WS-4 G1 `--check`. C9's wave-1 ruling rests (its words) on "C5 confirming the reinject pointer is in WS-4 G1's input set." I confirm the *store-side* corollary: **my supersede-mark schema and C9's reinject pointer should share the SAME G-LINK `--check` input set** when it promotes — if WS-4 G1 covers only the *memory-note* link-set and not the *reinject-pointer / superseded_by* set, there is a one-line MVP scoping note on an existing gate (add those surfaces to G1's input set), **not** a new workstream. E4 should rule that the §6 G-LINK trigger names **both** my `superseded_by` surface AND the reinject pointer as triggers, so neither dangling-ref class is unobservable. **(No CONFLICT with C9 — I confirm C9's verdict and add the store-side input-set corollary.)**
- **R2 (anti-bloat audit on my two new folds, per F2-02's gate-accretion vigilance):** E4 should verify the ITEM-3 degree-key clause and the ITEM-4 one-time-hygiene line do **not** silently accrete into standing machinery — the degree-key must stay a *rule Robert reads by eye* (not a degree-recompute daemon), and the 2-pattern promotion must stay a *finite one-time write* (not a promotion of the recurring consolidation pass). Both are the F2-02-adjacent "does the anti-bloat fold itself bloat?" trap on my lane; I bounded both pre-emptively, but the watch is E4's.

### One-line reconcile verdict

**C3 round-5 reconcile: I ACCEPT both of Codex's cold P2s on my lane as finishing what I conceded-then-under-folded (the C8 ITEM-1 shape) — ITEM 3 FOLDS the degree-keyed *selection rule* (KEEP-HOT ≥5 / KEEP-LINKED 1–4 / ARCHIVE-JIT the 39 zero-inbound) into WS-3a's compaction step as the missing selection key while HOLDING my dir-split set-aside and REBUTTING any in-degree *computation engine* (it's a grep-by-eye, I verified — report the degree, don't build a degree engine; D4+D1 cut, zero machinery, FM-A holds the C2 read-back seam off my lane); ITEM 4 promotes the 2 already-dangling ≥4-ref patterns (`plan-revision-against-not-yet-built-substrate` + `strike-revision-on-refined-second-tier-reason`, both at 5 inbound refs / no note file at HEAD — I verified) as a finite one-time MVP hygiene write (I own what-to-promote + the semantic-tier write; C1's firing-trigger is observably ALREADY MET at 5; the *recurring* consolidation pass stays DEFERRED, FM-J held; D4+D1 cut); and on the C9 wave-1 react I CONFIRM C9's dormant-G-LINK ruling holds my store surface — my `superseded_by` is an episodic Tier-1 breadcrumb (not Tier-5, no hash-chain per FM-L) recoverable via X-min, so X-min-floor + the deferred-but-NAMED G-LINK `--check` suffice and I need NO standing resolver (REBUT Codex's MVP-now resolver as mis-targeted on my lane, exactly as C9 ruled for the reinject pointer) — my sole residual to E4 is that my `superseded_by` surface and C9's reinject pointer must share the SAME WS-4 G1 `--check` input set when G-LINK promotes (a one-line scoping note on an existing gate, not a workstream), plus the F2-02 watch that neither of my two folds silently accretes into machinery.**