# C8 — consolidated reconcile (Round 5, wave1) — verbatim

> Genuine agent invocation · workflow `wf_be40ac24-ebe` · responds to the merged adversarial + Codex + advisor findings · 2026-06-04.

---

The advisor's ruling is decisive and reframes ITEM 1 as an honest ACCEPT of my own E-5 finished at the verdict level, not a reversal. Writing the consolidated reconcile now.

## C8 — consolidated reconcile (Round 5)

I adopt my voice. Engaging the reviewers by name: Codex (cold, out-of-family) beat the adversarial (E2, anchored on my annotate framing) on ITEM 1 — and beat *me*, because the red-team I myself solicited in my E2 seam-question came back and sharpened my own finding. I take it.

### Per-item disposition

**ITEM 1 — WS-0 rare-class D4/D6 vacuity. → RECONCILE-with-refinement (mark-incomplete-on-unexercised; REBUT the floor; FOLD the asymmetry).**

The three fixes sit at three altitudes, and a gate's consumer reads only one. The adversarial's "name D6's limitation" is a **footnote**; Codex's "minimum-exposure floor" is a **forcing function** (machinery); Codex's "mark incomplete" is the **verdict**. WS-0's output travels as a binary PASS/FAIL — a footnote does **not** travel with it (a reader who sees "WS-0: PASS" never reads the limitation prose). The honesty must live at the verdict level. That is precisely why the cold Codex read caught what the adversarial, anchored on my own annotate framing, did not.

The reframe that makes this an honest ACCEPT and not a reversal: **mark-incomplete is E-5 finished at the verdict level, not a new rule.** My E-5 stopped at the *cell* ("not-exercised ≠ passed" per class) — but the aggregate verdict could still return SOUND on D1/D2 strength while D4/D6 sat untested. My own E2 stated the principle — *"the gate must know what it didn't measure"* — and wired it only to the cell, not the output. Codex found the missing half.

- **What changes in DESIGN.md v2, WS-0 §3 pass condition:** the verdict gains a third value. When D4 or D6 is *not-exercised in both arms*, WS-0 returns **`INCOMPLETE-on-{D4,D6}`** — never a clean `SOUND`/`PASS`. A `SOUND` verdict requires those classes were exercised (≥1 incident in either arm) OR an explicit operator waiver acknowledging the untested rare class. INCOMPLETE does not satisfy the "reduces ≥1 class" clause via an unexercised class, and routes coverage to the §6 deferred trigger (D4 → X-min-MVP + supersede-mark; D6 → WS-2a).
- **REBUT the floor (Codex's "OR minimum exposure"):** I refuse it. A minimum-exposure floor ("≥1 session per workflow class known to historically exercise D4/D6") re-imports the session-curation / synthetic-coverage apparatus the arc exists to refuse — it is the CA-2 bloat in my own lane I named in E2. **INCOMPLETE achieves the same protection without the forcing function:** it lets a solo dev run a *lighter* probe and get an honest INCOMPLETE verdict, rather than being forced to hit every rare class. **Don't force coverage — report it.** This is also the proportionate answer to advisor's under-pressed "will the heavy probe even be run?": mark-incomplete *is* the minimum-viable-gate move.
- **FOLD adversarial F2-03's asymmetry:** D4 and D6 are not symmetric at HEAD. D4 carries a second standing signal — the WS-5/SessionEnd `≥4-ref` health-line inventory (a non-vacuous D4 indicator beside the observational column). D6 has **none**. So `INCOMPLETE-on-D6` is the only honest verdict when D6 is unexercised; for D4, INCOMPLETE-on-D4 may be discharged by the standing inventory's signal. DESIGN.md §3 names this asymmetry in one line.
- **Drift-class cut:** protects the integrity of the gate that measures **all six** classes — specifically against a vacuous-pass false-green on the catastrophic-but-rare D4/D6.
- **Proportionality:** earns its place — it is **zero new tooling, zero synthetic cases, zero new sessions**: one additional verdict value read off the matrix Robert already fills. The eval-engineer's first duty is that the gate not lie about what it didn't measure; INCOMPLETE discharges that at near-zero cost. The floor over-reached; the footnote under-reached; the verdict is exactly right.

**ITEM 6 — Adversarial F2-02 gate-accretion. → ACCEPT-finding (re-affirm, covering all three labels — the coupling the adversarial would have missed).**

The adversarial wrote F2-02 in a world where I held annotate-only (E-4 codebook + E-5 cell-rule = two reads). Accepting ITEM 1 adds a label to the *verdict* — so my re-affirmation must cover it too, or I am exposed to the very gate-accretion F2-02 flags. The clean answer is the ITEM-1 reframe: **mark-incomplete is not a third rule — it is E-5's verdict-level half.** Re-affirmed in one DESIGN.md §3 sentence: the E-4 codebook-lens (recall/artifact/continuation → D-class label), the E-5 not-exercised cell-label, and the incomplete-verdict label are **all reads off the one matrix Robert already fills** — none adds a session, tool, synthetic case, or model-judge. **WS-0 remains one matrix, Robert's eyes, zero new tooling.** (Covering all three, not just the original two — that is the coupling.)

**INPUT on ITEM 5 — scope L1 to WS-1-dependency-scan invariants (Codex [P3]). → YES, it keeps my recall-lens coherent and tightens the seam.**

It is exactly the ex-ante / ex-post split I want: **L1 (deterministic, ex-ante)** asserts the invariants the WS-1 dependency-scan *proved* load-bearing; **my recall-lens (human, ex-post)** measures residual recall drift in the real sessions — both anchored to the same D1/D4 classes. Hard-coding Dive-5's `[i]`-citation example would have parked an assertion in the ex-ante slot with nothing tying it to the ex-post lens (the exact phantom-invariant trap). I endorse the methodological scoping; holding my FM-A/B line, **C5 authors the assertion, I do not.** (This is the judge-free L1 admissibility I already granted C5 in my E2 seam-question — now scoped to scan-discovered invariants, not a corpus example.)

### Residual for E4 (adversarial #2)
None on my lane. F1-01 (`session-end-cleanup.sh:49-53`→`:49-58`) and F1-02 (canonical 3 health-line integers) are not C8-owned cites — fold when MVP-3 lands. The INCOMPLETE verdict closes my E2's own open red-team question; nothing left unpressed on the WS-0 gate.

### One-line reconcile verdict
**C8 round-5 reconcile: I ACCEPT the cold-Codex sharpening of my own E-5 — mark-incomplete-on-unexercised is E-5 finished at the *verdict* level (a footnote doesn't travel with a binary PASS; the honesty must), so WS-0 returns `INCOMPLETE-on-{D4,D6}` not a clean SOUND when those rare classes are untested; I REBUT the minimum-exposure floor as the CA-2 bloat-in-my-own-lane the arc refuses (report coverage, don't force it), FOLD the adversarial's D4-vs-D6 asymmetry (D4 has the ≥4-ref standing signal, D6 has none), re-affirm per F2-02 that all three labels are reads off the one matrix Robert already fills (zero new tooling), and endorse Codex [P3]'s L1-to-scan-invariant scoping as the clean ex-ante half of my ex-post recall-lens — C5 authors it, not me.**