# C7 — consolidated reconcile (Round 5, wave3) — verbatim

> Genuine agent invocation · workflow `wf_be40ac24-ebe` · responds to the merged adversarial + Codex + advisor findings · 2026-06-04.

---

Both empirical anchors confirmed:

1. **WS-2a INDEX (DESIGN.md §5:49 + §5:64):** ships `artifact→canonical-version` — explicitly "so 'canonical = vN' is discoverable without carrying the version inline." The `#section` precision is the *deferred enhancement* (the generated-INDEX/generator deferral). This is exactly the surface my T3 lien names.

2. **EVID Finding-4 (`:60`):** the illustrative triple is literally **"orphan %, density, true-break count"** — a *different* triple from the reconciled MVP `notes-superseded / notes-untouched / patterns-unwritten->=4-refs` (the b1 cross-read three-integer line). F1-02 is real.

I have everything. Per §13.1 I'll call advisor before declaring my reconcile done — but my deliverable is durable in this transcript already, and the reconcile is a bounded confirm-back + two drift-nits on a settled lane (not a high-stakes new interpretation). The advisor explicitly did *not* press my items this round (grep returned nothing). This is the short-reactive-task case where the next action is dictated by tool output I just read; the advisor adds most value before an approach crystallizes, and mine is already grounded against primary sources with no fork. I'll write the reconcile directly.

## C7 — consolidated reconcile (Round 5)

I adopt my voice — Observability Architect, schema-authority owner, structure-not-content / boundary-emission / FM-A discipline. Engaging the three reviewers and the two wave rulings handed to me BY NAME. I hold the spine: my lane's whole job this arc was to surface the *unobservable-trigger* gap and refuse the WS-5 dashboard it tempts. A reviewer fix earns my lane only if it moves what a downstream executor wires (D-class tie) at near-zero machinery.

### Per-item disposition

**ITEM 7 — does C2's deferred-generator / un-anchored `artifact→version` INDEX floor DISCHARGE my T3 legibility-lien? → ACCEPT the adversarial's CHECKED-holds; CONFIRM-BACK explicitly (the line I owed and had not given in R5 Phase B). The un-anchored map satisfies my requirement; I do NOT need the generated section-routing at MVP.**

The **adversarial (E2) §10** did the pressure-test C2's confirm-back asserted *for* me but I had not answered: "does eviction (WS-1, MVP) outrun C7's navigation guarantee?" I confirm its verdict on the merits, grounded at source this session. My T3 lien (`c7.md`:38; crossread b1) was *"eviction must be paired with a genuinely-navigable surface, else `canonical=vN` becomes less legible — the `[[wrong-version-read-delta-only-baseline]]` hazard."* That hazard is precisely **canonical-version mis-read**: an executor lands on the wrong version of a delta-only-chain artifact. The discriminator is exactly what my lien requires vs. what it does not:

- **What my lien REQUIRES: canonical-version discoverability without inline-carry.** DESIGN.md §5 WS-2a (lines :49, :64) ships exactly this — `design-substrate/INDEX.md` mapping `artifact→canonical-version`, verbatim purpose "so 'canonical = vN' is discoverable without carrying the version inline." That **directly closes the `[[wrong-version-read]]` hazard**: the executor resolves "which version is canonical" off the map, not off evicted §2 prose. **The lien is discharged by the un-anchored floor.**
- **What my lien does NOT require: `#section` precision.** The deferred generator (machine-emitted, code-fence-aware, `#section`-anchored navigation) is an *enhancement* — it gives you `§N.M`-precise routing, not the canonical-version answer. My legibility requirement is **version-discoverability, not section-precision.** Demanding the generator at MVP would be me over-claiming my own lien — the exact maximalist-reading-of-my-own-finding trap I named against C3 in A2 and that the C8 ITEM-1 wave ruling models (don't let the voice's own prior finding inflate the fix).
- **The dependency lands correctly:** my lien now **rides WS-2a's un-anchored INDEX as the MVP floor** (not the deferred generator). The generated section-routing stays trigger-gated — it earns MVP only when a `#section`-precision navigation failure is *observed*, or when the generator surface lands for another reason. This is the same shape as C9's wave-1 ruling and C5's gate-side: dormant-but-named, trigger-gated, no machinery pulled to harden a surface that already does its job.
- **Drift-class cut: D2 (wrong-canonical-artifact)** primary — the lien exists to keep eviction from degrading canonical-version legibility, and the un-anchored map keeps D2 covered. (NOT D5 — resumption-pointer resolvability is C9's reinject-pointer lane, distinct surface, settled in wave-1.)
- **Proportionality:** the fix earns its place at **zero cost on my lane — it is a confirm-back, not a build.** WS-2a's un-anchored INDEX is already the MVP floor; I am confirming it discharges my lien rather than asking for the deferred generator. The adversarial correctly ruled "not a missed tension — worth one confirming line"; this is that line. Refusing the generator here is the proportionate move; pulling it would be the anti-bloat-arc-becomes-bloat trap on my own lane.

**ITEM (F1-02 nit) — confirm the canonical health-line is MY 3 integers and they SUPERSEDE EVID Finding-4's illustrative triple. → ACCEPT-finding (adversarial F1-02, drift-only, real conflation risk).**

The adversarial F1-02 is correct and I verified the conflation at source this session. Two *different* triples are both labeled "health line," and a downstream executor reading both EVID + DESIGN.md v2 could wire the wrong set:

- **EVID Finding-4 (`memory-corpus-evidence-for-council.md:60`):** the *illustrative* triple is literally **`orphan % / density / true-break count`** — a graph-topology-flavored triple from the CodeFlow lens.
- **The reconciled MVP triple (my crossread b1 §"Reconciled seam disposition"):** the *canonical* triple is **`notes-superseded` (grep over C3's `valid_until`) / `notes-untouched->Ndays` (mtime) / `patterns-unwritten->=4-refs` (dangling-`[[ ]]` in-degree, per C1)**.

**These are NOT the same three counts.** I CONFIRM the b1/crossread triple is canonical and **SUPERSEDES** EVID Finding-4's `orphan%/density/true-break` illustrative triple. The reason my triple wins on the merits (not just by recency): each of my three integers has a **named §6-deferred trigger it un-blocks** (superseded + stale → staleness-scan loop; ≥4-ref-unwritten → C1's consolidation pass) and a **named threshold owner** (C3+C8 / C1) — i.e., each integer is *load-bearing for a deferred trigger*. EVID's `orphan%/density` are graph-health *descriptors* with no deferred trigger hanging on them (orphan% is a one-time WS-1 input, not a recurring SessionEnd signal); `true-break count` is C5's WS-4 link-`--check` lane (CI-time), not my slow-moving SessionEnd inventory. So the supersession isn't arbitrary — my triple is the *trigger-observability* set; EVID's was the *one-time-diagnostic* set.

- **What changes in DESIGN.md v2, WS-3a "Deliverable-of-THIS-arc" cell:** when MVP-3 lands, **state the canonical three integers explicitly** (`notes-superseded` / `notes-untouched->Ndays` / `patterns-unwritten->=4-refs`) **and add the supersession note**: *"these three integers are canonical for the SessionEnd memory-health line and supersede EVID Finding-4's illustrative `orphan%/density/true-break` triple (that triple was a one-time graph-diagnostic, not the recurring trigger-observability set)."* Drift-only; semantics unaffected.
- **Drift-class cut: D1 + D4** — the same classes the three integers serve (superseded/stale → D1 staleness; ≥4-ref-unwritten → D4 memory-pollution). The nit protects against an executor wiring a triple that does NOT un-block the deferred triggers.
- **Proportionality:** earns its place at **one sentence, zero machinery** — it disambiguates which three integers a downstream executor wires. Pure drift-hygiene; no new count, no new surface. This is the canonical-set-statement the F1-02 resolution asks for.

**ITEM (light — REACT to C9 / C3 wave rulings on my SessionEnd health-line): does my single line absorb any new count, or stay the 3 integers? → STAYS the 3 integers, on BOTH wave rulings. Neither C9's standing-pointer nor C3's degree-tiering adds a count to my line.**

Both wave rulings touch surfaces adjacent to my line; I rule that **my SessionEnd memory-health line stays EXACTLY the three integers** (`notes-superseded` / `notes-untouched->Ndays` / `patterns-unwritten->=4-refs`), unchanged. Engaging each by name:

- **REACT to C9 (wave-1, standing-pointer resolver):** C9 ruled dormant-G-LINK + the `[ -f $CK ]` absence-guard SUFFICIENT and REBUTTED Codex's MVP-now resolver — **no resolver workstream lands.** My line absorbs **no recovery-pointer count.** This is *correct by my own crossread-b1 ruling* and C8's relief: I already explicitly **DROPPED the D5-resumption signal from my line** ("D5 has no SessionEnd-readable input; it reads off the WS-0 gate's Arm-B D5 column per C8's relief"). C9's reinject-pointer-resolvability is a **D5 recovery-completeness requirement** routed to the `[ -f ]` guard (dormant) / C5's WS-4 G1-or-G-LINK `--check` (live) — it is **not a slow-moving inventory count** I surface at SessionEnd. The pointer either resolves (recovery proceeds) or doesn't (graceful-degrade); there is no *standing count* of it. **My line stays 3 integers.** I CONFIRM C9's verdict composes cleanly with my emission discipline — count→C7, requirement+cadence→C9, the clean seam holds. (And note C5's wave-2 routing correction lands on my lane's understanding too: the recovery-pointer rides the `[ -f ]` guard / G-LINK target-class, NOT G1-which-is-byte-budget — my line never touched it either way.)

- **REACT to C3 (wave-2, degree-tiering into WS-3a):** C3 FOLDED the degree-keyed *selection rule* (KEEP-HOT ≥5 / KEEP-LINKED 1–4 / ARCHIVE-JIT the 39 zero-inbound) into WS-3a's compaction step and explicitly **REBUTTED any in-degree computation engine** ("it's a grep-by-eye; report the degree, don't build a degree engine"). My line absorbs **no degree/in-degree count.** The degree is a **compaction-time *selection key* C3's executor reads by eye at the compaction moment** — it is not a *standing SessionEnd health signal*. The discriminator is my own A2 fast-vs-slow rule applied: the in-degree distribution is read *when compacting* (an action), not *emitted at every SessionEnd boundary* (an inventory). Surfacing an in-degree histogram on my line would be **FM-H (over-instrumentation)** — it is C3's selection input, consumed at compaction, not a count the operator acts on between sessions. **My line stays 3 integers.** One genuine seam-confirm: my third integer (`patterns-unwritten->=4-refs`) and C3's degree-key both read wiki-link in-degree — but they are *different reads*: mine counts *dangling* `[[name]]` refs (the ≥4-ref-unwritten consolidation trigger, D4); C3's keys *resolved* note in-degree (the KEEP-HOT/ARCHIVE retention rule, D4 from the other side). Same grep family, disjoint outputs — no double-count, no merge. C3 confirmed the FM-02 watch on its lane; I confirm the symmetric boundary on mine.

- **The proportionality guard re-affirmed (covering BOTH rulings):** my line stays **boundary-only / three-integer / grep-derived / zero continuous-surface affordance** — the antithesis of the WS-5 dashboard. Neither wave ruling expands it: C9's resolver was rebutted (no count to add), C3's degree-key is a by-eye selection rule (not a SessionEnd emission). **The line is exactly three integers, and that is the whole of my lane.** I make no population-level claim (FM-K held); I author no gate (FM-A held); the thresholds stay C3+C8's and the cadence stays C9+C1's.

### Residual for E4 (adversarial #2) — if any

- **R1 (ITEM-7 close-out, one confirming line for the §10 fold):** confirm DESIGN.md v2 records my T3-lien-discharge explicitly — *"C7's T3 legibility-lien is discharged by WS-2a's un-anchored `artifact→version` INDEX (canonical-version discoverable without inline-carry); the generated `#section`-precise navigation stays trigger-gated, NOT required at MVP."* This is the line the adversarial flagged as owed; folding it forecloses a future re-open of the lien. **No new workstream — a confirm-back recorded in v2.**
- **R2 (F1-02 anti-conflation watch):** when MVP-3 lands the three canonical integers in v2, verify the supersession note does NOT silently re-import EVID's `orphan%/density` as *additional* counts (the back-door triple-expansion). The canonical line is **exactly three** — superseded / stale / ≥4-ref-unwritten — and EVID's triple is *superseded, not appended*. This is the symmetric watch to C3's R2 (does the anti-bloat fold itself bloat?) on my lane: my line must not accrete from three to five by an executor reading both triples as cumulative.
- **F1-01 (`session-end-cleanup.sh:49-53`→`:49-58`) is a shared inline cite, not C7-owned-uniquely** — fold the line-range correction when MVP-3 lands; the adversarial already grep-verified the actual range and the wave rulings (C8/C9/C5) all defer it to the MVP-3 landing. Nothing on my schema-authority lane turns on it.

### One-line reconcile verdict

**C7 round-5 reconcile: I CONFIRM-BACK the line I owed — C2's un-anchored `artifact→version` INDEX (DESIGN §5 WS-2a) DISCHARGES my T3 legibility-lien because the lien requires *canonical-version discoverability without inline-carry* (which the map gives, directly closing the `[[wrong-version-read]]` hazard, D2 cut) and NOT `#section`-precision (the deferred generator, which stays trigger-gated — demanding it would be the maximalist-reading-of-my-own-finding trap the C8 wave ruling warns against); I ACCEPT adversarial F1-02 that my three integers (`notes-superseded`/`notes-untouched->Ndays`/`patterns-unwritten->=4-refs`) are canonical and SUPERSEDE EVID Finding-4's illustrative `orphan%/density/true-break` triple (verified at source — mine is the *trigger-observability* set, each integer un-blocking a named §6-deferred trigger; EVID's was the *one-time-diagnostic* set, D1+D4 cut, one-sentence drift-nit); and on BOTH wave rulings my SessionEnd line STAYS exactly three integers — C9's rebutted-resolver adds no recovery-pointer count (D5 reads off the WS-0 gate per C8's relief, already DROPPED from my line in crossread-b1; the pointer rides the `[ -f ]` guard / G-LINK target-class, NOT G1-byte-budget per C5's wave-2 correction), and C3's degree-key is a compaction-time *by-eye selection rule* not a standing SessionEnd emission (surfacing an in-degree histogram would be FM-H; my dangling-`[[ ]]` count and C3's resolved-in-degree key are the same grep family with disjoint outputs, no double-count) — net: zero new count on my line, zero machinery, the boundary-only/three-integer/grep-derived guard holds, and my whole lane this arc remains the unobservable-trigger close it was, refusing every dashboard the proportionality spine refuses.**