# C9 — consolidated reconcile (Round 5, wave1) — verbatim

> Genuine agent invocation · workflow `wf_be40ac24-ebe` · responds to the merged adversarial + Codex + advisor findings · 2026-06-04.

---

I now have everything I need. Let me reason through the spine-call on my lane.

The three reviewers all touch my item (the standing recovery/supersede pointer):
- **Adversarial F2-01** RULED: dormant-G-LINK fine; name the existing `[ -f $CK ]` self-check at `postcompact-reinject.sh:28`. Verified the reinject targets resolve at HEAD; the live cross-store drift is a *memory-note* break, NOT the reinject pointer.
- **Codex [P2]** DIVERGES: `[ -f ]` handles *absence* not *cross-store drift*; a minimal resolver should be MVP-now or D5 silently fails.
- **advisor** did not press my item.

The crux of the divergence: absence-guard vs resolution-validation. The adversarial verified empirically that the ONE standing machine-managed pointer at bare MVP — the reinject pointer — has targets that **resolve at HEAD** (`roadmap_status.md` exists, 130KB; checkpoint pointer `[ -f ]`-guarded). Codex never had the repo grep (cold read, research primer only); its "live at HEAD" cross-store-drift claim is true for *memory notes* (EVID Finding-4 bucket C/D) but NOT for the reinject pointer set. So Codex's worry attaches to a different surface than the one it names as MVP-now.

My own R5 contribution already landed the recovery requirement ("the reinject pointer must resolve, or recovery is silent-fail") and routed the gate to C5, store to C3. That's the disciplined home. Let me write the reconcile.

## C9 — consolidated reconcile (Round 5)

### Per-item disposition

**ITEM 2 — standing recovery/supersede pointer · RULE: is a minimal standing-pointer resolver MVP-now, or is dormant-G-LINK + the `[ -f ]` absence-guard sufficient?**

**REBUT Codex [P2] on its MVP-now framing; RECONCILE-with-refinement on the underlying requirement.** Dormant-G-LINK + the absence-guard is sufficient at bare MVP — but DESIGN.md v2 must *name* the recovery-pointer-resolvability requirement and route its enforcement to C5's gate (the adversarial's F2-01 naming, plus the one dependency-line my own R5 already drew). Here is the merits adjudication, because the reviewers genuinely diverge and this is my lane to call:

- **The absence-guard and resolution-validation ARE distinct** — Codex is right on the *distinction*. `[ -f "$CK" ]` (`postcompact-reinject.sh:28`) catches a *missing* file (graceful-degrade: skip the reinject hop). It does NOT catch a *present-but-wrong* target nor a *cross-store-drifted* ref (kebab-case `memory/` ↔ underscore `.harness/` — EVID Finding-4). On recovery-completeness — my owned concern — a dead/drifted pointer is a **silent-fail** (recovery lands on nothing or the wrong target → D5 bad-resumption / D2 wrong-canonical-artifact). Codex named a real failure *shape*.

- **But Codex's "MVP-now resolver" over-reaches on the *surface it actually attaches to*.** The adversarial did the grep Codex (cold, research-primer-only) could not: at **bare MVP there is exactly ONE standing machine-managed cross-ref — the reinject pointer — and its targets RESOLVE at HEAD** (`roadmap_status.md` exists @ 130KB; checkpoint `[ -f ]`-guarded). The supersede-links (`superseded_by`) and the INDEX generator are **deferred**, so there is **no machine-emitted supersede surface in MVP** for a resolver to validate. The live cross-store drift Codex cites is real — but it is a **memory-NOTE break** (bucket C/D), which is C3's store-hygiene + C5's WS-4 G1 link-`--check` lane, **NOT the reinject pointer**. Codex's worry is correctly-shaped but mis-targeted: it would have me build a resolver workstream for a drift class that lives on someone else's lane and a supersede surface that doesn't exist yet. That is the **CA-2 bloat trap the arc exists to refuse** — pulling machinery to harden a pointer that already degrades gracefully and already resolves.

- **The reconciled cut (whose lane).** Recovery-pointer-resolvability is a **recovery-completeness REQUIREMENT** (mine) — but it is **not a new C9 workstream** and the *mechanism* is not mine to author. The canonical C9↔C5↔C3 split applies verbatim (skill §"Co-primary scan"; FM-A discipline — own the requirement, author neither gate nor store): the **integrity check** rides C5's WS-4 G1 `--check` (or a sibling link-integrity check), the **store-versioning/navigability** is C3's X-min (versioned-AND-navigable: versioning recovers content; a resolving link recovers the *path to* it), and I keep ONLY the requirement statement. This is exactly what my R5 contribution already routed.

**Reconciled MVP shape (what changes in DESIGN.md v2):** Adopt the **adversarial F2-01 disposition** — resolve residual (b) toward **dormant-G-LINK-is-fine**, and add **one naming deliverable** at §6 (WS-6 6a row, currently credits `postcompact-reinject.sh` as §7.2-conformant but does not name pointer-resolvability as a *guaranteed property*): name the **reinject-pointer self-check as an explicit MVP recovery deliverable** — the absence-guard at `postcompact-reinject.sh:28` IS the MVP floor, and **recovery-pointer-resolvability is a stated requirement routed to C5's WS-4 G1 link-integrity `--check`** for the *cross-store-drift* dimension Codex correctly flagged (that check is owned by C5, already in MVP, already covers the memory-note breaks; I scope the recovery-pointer onto it, I do not duplicate it). **No G-LINK pulled to MVP. No new C9 resolver workstream.** When the deferred surfaces land (INDEX generator emitting machine-managed refs; `superseded_by` machine-emitted supersede-marks), the full code-fence-aware G-LINK gate is the trigger-gated answer per §6 — *that* is when a resolver earns its place, on an *observed* machine-emitted-pointer surface, not before.

**Drift-class cut:** **D5 (bad-resumption)** primary + **D2 (wrong-canonical-artifact)** secondary — a dead/drifted reinject pointer mis-derives next-action on resume, or lands recovery on a non-canonical/wrong target. (NOT D4 — the *memory-note* drift Codex conflated is the D4 surface, and it is C3/C5's, already covered by the WS-4 link-`--check` + the X-min store-versioning.)

**Proportionality justification:** The fix earns its place because it closes a *real* silent-fail shape Codex correctly named (recovery-via-pointer rots on a dead link) — but at the **cost of one sentence**, not a workstream: the absence-guard already exists (grep-verified by the adversarial at `:28`), the integrity-`--check` already exists in MVP (C5's WS-4 G1), the store-versioning already exists in MVP (C3's X-min). There is **nothing to build** — only to *name the requirement and route it*. Codex over-reached by recommending standing resolver *machinery* for (a) a supersede surface deferred-out-of-MVP and (b) a drift class on C3/C5's lane that the existing MVP gates already cover; building it would be the anti-bloat-arc-becomes-bloat trap (the exact trap advisor confirmed the council otherwise avoided, and F2-01's "name-don't-gate" disposition holds the spine). The decisive merit: the adversarial's repo grep beats Codex's cold inference on *which surface the pointer-drift actually lives on* — and on that surface, the reinject pointer, it resolves today.

### Residual for E4 (adversarial #2) — if any

- **→ C5 (the dependency I land on your gate, confirm-back):** my recovery-pointer-resolvability requirement routes to your **WS-4 G1 link-integrity `--check`** — confirm the `--check` covers that the reinject pointer's targets (`roadmap_status.md`; the `precompact-latest.md`→checkpoint pointer) **resolve to the intended target**, not merely that they are non-empty, AND that the cross-store kebab/underscore drift (EVID Finding-4) is in its scope. If WS-4 G1 covers only the *memory-note* link-set and not the *reinject-pointer* set, there is a one-line MVP scoping gap (the check exists; its *input set* must include the reinject pointer) — that is a scope note on an existing gate, still **not** a new workstream. This is the one seam where my REBUT-of-MVP-now-resolver rests on C5's gate actually covering the resolution dimension; if it does not, the dormant-is-fine verdict needs the reinject pointer added to G1's input set (cheap), not a G-LINK pull.
- **→ E4 audit ask:** verify the §6 naming-deliverable does not silently re-introduce a coverage-floor obligation (the F2-03-adjacent trap) — "name the property" must stay a *stated requirement + a route to an existing check*, NOT an assertion that MVP *guarantees* resolution for surfaces that don't exist yet (the deferred supersede-links). Watch that the one sentence doesn't accrete into "MVP must prove every standing pointer resolves" — that would over-claim for the deferred surfaces and re-create the resolver workstream by the back door.

### One-line reconcile verdict

**C9 RULES dormant-G-LINK + the `[ -f $CK ]` absence-guard SUFFICIENT at bare MVP and REBUTS Codex's MVP-now resolver as mis-targeted CA-2 bloat — the adversarial's repo grep (which Codex, cold, lacked) proves the ONE standing machine-managed pointer at MVP, the reinject pointer, RESOLVES at HEAD, while the cross-store drift Codex correctly named lives on the *memory-note* surface (C3/C5's lane, already covered by X-min versioning + WS-4 G1 link-`--check`) and the *supersede-link* surface is deferred-out-of-MVP with no resolver to author; I ADOPT F2-01's name-don't-gate disposition (one §6 deliverable: name reinject-pointer-resolvability as a stated recovery requirement routed to C5's WS-4 G1, D5+D2 cut) — Codex got the absence-vs-resolution *distinction* right, the *surface* and the *MVP-now* wrong; full code-fence-aware G-LINK stays trigger-gated on an observed machine-emitted-pointer surface, and my only residual is C5 confirming the reinject pointer is in WS-4 G1's input set.**