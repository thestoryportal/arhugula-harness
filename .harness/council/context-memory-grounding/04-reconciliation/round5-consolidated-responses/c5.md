# C5 — consolidated reconcile (Round 5, wave2) — verbatim

> Genuine agent invocation · workflow `wf_be40ac24-ebe` · responds to the merged adversarial + Codex + advisor findings · 2026-06-04.

---

I have everything I need. Let me reason through the spine-call on my two items before writing the reconcile.

**ITEM 5 (Codex [P3] — derive L1 from live invariants).** Codex says: scope L1 to invariants the WS-1 dependency-scan discovers, not the hard-coded `[i]` example. The adversarial's checklist item 7 already PASSES my L1 framing ("the L1 deterministic assertion is correctly scoped as the *ex-ante* half with the human probe as the *ex-post* half"). Advisor noted "L1 fine." So this is a refinement to L1 *content*, not its *home*. In B2 I ruled L1 lives at C2's WS-1 verify-before-evict precondition (not my gate). Codex [P3] does NOT touch that home — it touches what the assertion *asserts*. C8 (wave-1) already endorsed this exact scoping and routed authorship to C5. This is clean ACCEPT, and it actively *prevents* the phantom-invariant trap (hard-coding a corpus example as a work-item = an L1 assertion with no live D-class tie = exactly FM-A-adjacent over-claim). The decisive merit: Codex caught that promoting an *illustration* to a *work item* is the same shape as a forward-looking phantom cite (checklist item 3).

**ITEM 2 gate-side (react to C9 wave-1).** C9 RULED dormant-is-fine and REBUTTED Codex's MVP-now resolver. C9's residual to me: confirm the reinject pointer is in WS-4 G1's *input set*. This is exactly the residual I myself routed to the adversarial in B2 ("dormant-G-LINK reinject-pointer coverage gap"). The adversarial's F2-01 ruled it: dormant-is-fine, the `[ -f ]` self-check is *already true at HEAD*, name-don't-gate. So I have three signals — C9's ruling, the adversarial's F2-01, and my own B2 lean ("dormant-is-fine") — all converging, with only Codex [P2] dissenting (cold, mis-targeted per C9's grep). My job: confirm G-LINK stays DEFER-on-trigger, and resolve C9's one open seam (is the reinject pointer in G1's input set, or does it need a home when G-LINK is dormant?).

Now I write.

## C5 — consolidated reconcile (Round 5)

I adopt my voice — Validation Contract Architect, gate-as-contract owner, FM-A/FM-C boundary discipline. Engaging the reviewers and C9's wave-1 ruling by name. I hold the spine: a *gate* must earn MVP exactly as a workstream must — recommending machinery to harden an anti-bloat arc is the trap the adversarial named (R-9) and the council otherwise refused everywhere.

### Per-item disposition

**ITEM 5 — Codex [P3] derive L1 from live invariants. → ACCEPT-finding (refines L1 *content*; does NOT move the home I ruled in B2).**

Codex (cold, out-of-family) caught a real shape that the adversarial's checklist item 7 *passed over*: my B2 folded L1 into WS-1's verify-before-evict precondition but left its *content* as Dive-5's `[i]`-citation **illustration**. Promoting an illustration to a work-item is the same defect as a forward-looking phantom cite (checklist item 3) — an L1 assertion hard-coded from a corpus example has **no live D-class tie**, so its catch-rate-of-1.0 is over a thing that may not be a harness invariant at all. That is the phantom-invariant trap, and it sits squarely on my admissibility lane.

- **What changes in DESIGN.md v2 — WS-1 (NOT a new workstream, NOT my gate):** the L1 deterministic half of WS-1's verify-before-evict precondition asserts **the invariants the WS-1 dependency-scan step (a) actually discovered as load-bearing** — e.g., "the live invariants extracted pre-evict are still present post-evict; the citation/recall format the scan proved load-bearing still emits." The Dive-5 `[i]`-citation example is **demoted from a work-item back to an illustration** of the *form*; the scan supplies the *content*. This is the clean ex-ante (deterministic, scan-discovered invariants) / ex-post (C8/WS-0 human recall-lens over real sessions) split.
- **Does Codex [P3] change the home I ruled in B2? No — only the content.** In B2 I ruled L1 lives at **C2's WS-1 verify-before-evict precondition, not my gate** (it is a one-shot eviction-precondition fired at slim-time, not a standing CI gate). C8 (wave-1) endorsed this exact scoping — "C5 authors the assertion, I do not" — and held FM-A clean (no judge ⇒ κ/TPR/TNR govern nothing). Codex [P3] sharpens *what L1 asserts*; it leaves the home untouched. **C5 authors the assertion; it rides WS-1.**
- **Gate-admissibility ruling (the question I was asked):** L1 stays **judge-free and MVP-admissible** when scoped to scan-discovered invariants — *more* so than under the hard-coded example, because a scan-discovered invariant has catch-rate 1.0 for **a thing demonstrably present at HEAD**, whereas a corpus-example assertion has catch-rate 1.0 for a thing of unverified liveness. The κ/TPR/TNR drop holds verbatim (L2-judge metrics, no judge at MVP).
- **Drift-class cut:** D1 (lost-decision/instruction) + D4 (memory-pollution) — the scan-discovered invariants are precisely the recall/continuation surfaces those classes degrade; the assertion is a deterministic floor under them at the evict moment.
- **Proportionality:** earns its place at **zero new cost** — it is a content-refinement of an assertion already riding an already-MVP workstream (WS-1). It is *net anti-bloat*: it removes a phantom work-item (the hard-coded `[i]` example) and replaces it with a tie to a scan the plan already runs. Codex did not over-reach — it tightened the seam exactly where C8 and I had left a soft edge.

**ITEM 2 gate-side — react to C9's wave-1 dormant-is-fine ruling. → REBUT Codex [P2] (mis-targeted); CONFIRM G-LINK stays fully DEFER-on-trigger; RECONCILE my B2 disposition to C9's ruling.**

C9 RULED dormant-G-LINK + the `[ -f $CK ]` absence-guard SUFFICIENT at bare MVP and REBUTTED Codex's MVP-now resolver as mis-targeted CA-2 bloat. The adversarial's F2-01 independently ruled the same — the self-check is **already true at HEAD** (`postcompact-reinject.sh:28` conditional guard; `roadmap_status.md` resolves @ 130KB), and the live cross-store drift Codex cites is a **memory-NOTE break** (bucket C/D), *not* the reinject pointer. My own B2 lean was "dormant-is-fine." Three converging signals against one cold dissent that grepped a different surface than the one it named MVP-now. I take the convergence.

- **G-LINK stays fully DEFER-on-trigger.** My B2 disposition is RECONCILED to C9's ruling **unchanged** — G-LINK is a §6 deferred-tail sibling to G2/G3/G4, **promoted to MVP iff** {C2-E2 generated section-routing ships} OR {C3 supersede-marking becomes machine-emitted} OR {a D4-via-dangling-ref incident is observed at WS-0}. Absent a trigger, the 3 bucket-C breaks are one-time hand-hygiene, not a standing gate. **No minimal G-LINK pulled to MVP-now.** Codex over-reached: it recommended standing resolver machinery for (a) a supersede surface deferred out of MVP and (b) a drift class on C3/C5's *memory-note* lane, while naming the reinject pointer as the surface — which, per the adversarial's grep C9 cited, **resolves today**.
- **Is the reinject-pointer check a minimal `--check` I own (scoped subset of G-LINK)? — Yes when G-LINK is live; no at bare MVP.** When G-LINK promotes, the reinject-pointer target is **one of its validated target-classes** — I own that conformance behavior. But at bare MVP (G-LINK dormant), the protection is the **absence-guard already at `postcompact-reinject.sh:28`** (C9's lane — a recovery-completeness self-check), **not my gate**. This is the asymmetry C9 and I agree on: I own *G-LINK's conformance over the pointer when G-LINK is live*; C9 owns *the bare-MVP self-check*. There is no orphan: the reinject pointer is covered at every MVP state — by the existing `[ -f ]` guard when dormant, by G-LINK's target-class when live.
- **Does it compose with never-halt? — Yes, and this is the surface-disjoint resolution.** G-LINK (when live) is a **CI/PR-time guardrail that exits 1, never an in-loop session-runtime blocker** (the C5↔C9 surface-disjoint cut, DESIGN.md §7). The bare-MVP `[ -f ]` self-check is *also* never-halt by construction — it **graceful-degrades** (skip the reinject hop on absence), it does not halt the recovery loop. So both states (dormant guard + live `--check`) compose with never-halt; the two surfaces never contend. Fail-class on a dangling machine-managed ref stays `permanent-fail-exit` / `cause_attribution: contract_violation` (structural break, not transient — per the locked five-class taxonomy + FM-J).
- **C9's open seam — is the reinject pointer in WS-4 G1's input set?** C9 routed the requirement to "WS-4 G1 link-integrity `--check`." I refine the routing: **G1 is the byte-budget `--check`; it is NOT a link-integrity check.** Link-integrity is G-LINK (deferred). So the recovery-pointer-resolvability requirement does **not** ride G1 — at bare MVP it rides the **`postcompact-reinject.sh:28` absence-guard** (C9's recovery primitive), and on G-LINK promotion it rides **G-LINK's target-class**. I confirm C9's *requirement* lands; I correct the *home* (G1 byte-budget ≠ link-integrity). The reinject pointer's **memory-note** cross-store drift (kebab/underscore, EVID Finding-4) is in G-LINK's scope when live, NOT in MVP-now scope — matching C9's "that drift lives on the memory-note surface, already covered by X-min versioning."
- **Drift-class cut:** D5 (bad-resumption) primary + D2 (wrong-canonical-artifact) secondary — a dangling reinject pointer mis-derives next-action on resume. (NOT D4 — the memory-note drift is C3/C5's lane, covered by X-min + G-LINK-when-live.)
- **Proportionality:** the fix earns its place at **one sentence, zero build** — the absence-guard exists (grep-verified `:28`), G-LINK is already a deferred §6 row, X-min store-versioning is already MVP. Codex correctly named the absence-vs-resolution *distinction* but got the *surface* (memory-note, not reinject pointer) and the *timing* (MVP-now, not on-trigger) wrong; building a standing resolver would be the anti-bloat-arc-becomes-bloat trap. The adversarial's repo grep beats Codex's cold inference on which surface the pointer-drift lives on — and on the reinject pointer, it resolves today.

### Residual for E4 (adversarial #2) — if any

- **One precision on C9's seam, for the §6 G-LINK row wording:** confirm the row does NOT silently re-route recovery-pointer-resolvability onto **G1** (byte-budget) — G1 is not a link-integrity check; the requirement rides the `[ -f ]` guard (dormant) / G-LINK target-class (live). If the §6 wording conflates "WS-4 G1 `--check`" with link-integrity, that is a one-line home-correction (the check exists; its *identity* must read G-LINK-not-G1), still not a new workstream. This composes with C9's residual-(b) audit ask — watch that the §6 naming-deliverable stays a *stated requirement + route to an existing guard/deferred-gate*, NOT an assertion that MVP *guarantees* resolution for surfaces that don't exist yet (the deferred supersede-links). That over-claim is how the resolver workstream sneaks in the back door (the F2-03-adjacent trap).
- **F1-01 / F1-02 (`session-end-cleanup.sh:49-53`→`:49-58`; canonical 3 health-line integers) are not C5-owned cites** — fold when MVP-3 lands; nothing on my gate lane.

### One-line reconcile verdict

**C5 round-5 reconcile: I ACCEPT Codex [P3]'s L1-from-live-invariants sharpening — it refines what L1 *asserts* (scan-discovered invariants, not the hard-coded `[i]` illustration, which demotes back to a form-example) WITHOUT moving the home I ruled in B2 (L1 rides C2's WS-1 verify-before-evict precondition, C5 authors it, judge-free and *more* MVP-admissible than the corpus-example version because a scan-discovered invariant has catch-rate 1.0 over a thing live at HEAD), cutting D1+D4 at zero new cost as net anti-bloat; and on the gate side I REBUT Codex [P2]'s MVP-now resolver as mis-targeted CA-2 bloat and CONFIRM G-LINK stays fully DEFER→MVP-on-trigger in lockstep with C9's dormant-is-fine ruling and the adversarial's F2-01 (the reinject-pointer is covered at every MVP state — by the existing `postcompact-reinject.sh:28` absence-guard when G-LINK is dormant, by G-LINK's validated target-class when live, both composing with never-halt as a CI/PR-time guardrail that graceful-degrades and never blocks the in-loop runtime), correcting only C9's routing (the requirement rides the `[ -f ]` guard / G-LINK target-class, NOT G1 which is byte-budget-not-link-integrity), D5+D2 cut, one sentence and zero build — Codex got the absence-vs-resolution distinction right, the surface and the MVP-now timing wrong.**