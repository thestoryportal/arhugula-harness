All cites resolve. I now have the full empirical picture. Key findings:

1. **AR-7 (credit half) CONFIRMED:** `postcompact-reinject.sh` exists and injects a bounded pointer (`precompact-latest.md` + roadmap-next + re-audit nudge) into the dynamic suffix — conforms to §7.2. `context-recovery.sh` does the 60/75/85% threshold pre-save via `hook_write_checkpoint ... skip_gh`. Both BUILT + conforming = no work owed.
2. **AR-7 (build half) CONFIRMED:** `session-start.sh` reads NEITHER the StopFailure breadcrumb NOR the checkpoint (grep empty). `stop-loop.sh` continue path emits `decision:block` JSON but calls NO `hook_write_checkpoint`. Both unbuilt = D14 Piece A (U-HK-30) + Piece B (U-HK-40) owed.
3. **AR-2 CONFIRMED:** D14 / U-HK-30 / U-HK-40 all resolve at `.harness/hardening-workflow/HARDENING_PLAN.md` (lines 277, 365, 375) — but the real path is `.harness/hardening-workflow/`, NOT the repo root the AR REVIEW.md implied. The home-of-record split holds.
4. **AR-4 / R5 — LOAD-BEARING EMPIRICAL FINDING:** the out-of-worktree memory store at `~/.claude/projects/.../memory/` is **NOT a git repo** (no `.git` anywhere up the tree). The CLAUDE.md §12.5.1 claim "Provenance lives in git history at the global memory store" is **empirically false at HEAD** — there is NO git rollback boundary. This is exactly the R5 "not confirmed this session" check, now confirmed *negative*. This sharpens AR-4 materially.

Now I write the reconciliation response.

## C9 reconciliation response

### Disposition table

| AR-ID | Disposition | PLAN delta (specific) |
|---|---|---|
| **AR-2** (home-of-record, co-decide w/ C1) | **ACCEPT** | DESIGN.md WS-6 encodes the split as a **dependency-cite, not a re-authoring**: HARDENING_PLAN (`.harness/hardening-workflow/HARDENING_PLAN.md`) is the **home-of-record for `tools/hooks/` EXECUTION** — owns D14 / U-HK-30 (Piece A) / U-HK-40 (Piece B) build units, their `test_*.sh` fixtures, and the StopFailure-trigger details. Council PLAN WS-6 owns **only the governance-layer sequencing**: the single normative clause "P1 prefix-slim MUST land WITH D14 (U-HK-30 + U-HK-40), not before — recovery-completeness is a co-requisite of the slim-down (R1)." WS-6 cites D14 by unit-ID + path; it copies no Piece-A/B implementation detail. **Path correction folded in:** the cite is `.harness/hardening-workflow/HARDENING_PLAN.md` (lines 277/365/375), NOT a repo-root `HARDENING_PLAN.md` (which does not exist). The two compose; neither re-authors the other. C1 co-owns the WS-2 lifecycle-topology-contract (`HOOKS.md`) seam against the same `tools/hooks/` surface — I defer the HOOKS.md authoring boundary to C1 and only assert that WS-6's recovery sequencing cites the D14 units rather than re-specifying the hook lifecycle. |
| **AR-7** (Class-1 split) | **ACCEPT** | DESIGN.md splits WS-6 into two explicitly-labelled halves so the credited behavior is not mistaken for owed work: **WS-6a "CREDIT — no work owed"** = `postcompact-reinject.sh` (re-injects bounded `precompact-latest.md` pointer + roadmap-next + re-audit nudge into the dynamic suffix — **conforms to §7.2, verified**) + `context-recovery.sh` (60/75/85% threshold pre-save via `hook_write_checkpoint … skip_gh`, **BUILT, verified**); these are R3/R4-conformance evidence, marked DONE. **WS-6b "BUILD — owed"** = D14 Piece A (U-HK-30: `stop-loop.sh` continue path calls `hook_write_checkpoint … skip_gh`, stdout-silent) + D14 Piece B (U-HK-40: `session-start.sh`+`prompt-context.sh` read the `"event":"StopFailure"` breadcrumb with freshness window) + the `context_budget_exceeded` degradation MODE (R4). The build half is owned-for-execution by HARDENING_PLAN per AR-2 for the two U-HK pieces; the degradation MODE shape is the council PLAN's contribution. |
| **AR-4** (FM-H recovery-primitive, my half) | **ACCEPT (sharpened by negative empirical finding)** | DESIGN.md sequences the FM-H "X" workstream in **three gated steps**, recovery-primitive half owned by C9: **(1) DETECTION FIRST** — before any serialization machinery, add a cheap concurrent-write conflict detector (C3 owns the detector mechanism; gates whether the race is observed vs latent). **(2) CONFIRM ROLLBACK BOUNDARY** — C9 recovery-primitive existence check: **EMPIRICALLY RESOLVED NEGATIVE this session** — the out-of-worktree store at `~/.claude/projects/-Users-robertrhu-Projects-arhugula-v2/memory/` is **NOT under git** (no `.git` anywhere up the tree); the CLAUDE.md §12.5.1 claim "Provenance lives in git history at the global memory store" is **false at HEAD**. So a corrupted/lost-update MEMORY.md is currently **unrecoverable** — there is no rollback primitive. DESIGN.md records this as a confirmed C9 finding and routes the *remedy* (establish a rollback boundary for the store — e.g., the store becomes a tracked path, or a periodic snapshot under the worktree's `.harness/`) to **C3 as store-owner**, with C9 contributing only the existence-check verdict. **(3) ONLY THEN BUILD SERIALIZATION** — flock/lease machinery (C3-owned) is gated behind step 1's detection result; re-rate FM-H as **HIGH-structural / incidence-unconfirmed** per the AR. The serialization (prevent the race) is C3's; the rollback boundary (recover from it) is now a confirmed C9-surfaced gap that C3 owns the remedy for. |
| **MISSED TENSION C5↔C9** (my half) | **RECONCILE (compose, not contradict)** | They compose without contradicting never-halt because they are **different surfaces at different times**. C5's `--check` byte-budget GATE is a **build-time / PR-time / CI-time** check (`tools/substitution_ledger.py --check`-tier; `exit 1` on over-budget) — it fails *a PR before merge*, where halting is the correct and intended behavior (you do not merge an over-budget CLAUDE.md). C9's `context_budget_exceeded` is a **session-runtime graceful-degradation MODE** (Pillar-6 shape: trigger / scope / exit-condition / surfaced signal) — it fires *inside a live loop turn* where the locked **never-halt / defer-and-continue** posture governs, so it MUST observe-and-recover (surface the rot, force re-grounding, continue), never halt the turn. DESIGN.md encodes the discriminator explicitly: **gate-at-build-time (C5/G1, may fail a PR) + degrade-gracefully-at-runtime (C9 mode, never halts the loop) are distinct surfaces; the never-halt posture binds only the runtime surface, never the CI gate.** No contradiction; the two are complementary halves of one budget discipline. C5 owns the GATE tier; C9 owns the runtime MODE shape; C3 owns the compaction POLICY the mode exits via. |

### Rationale

**AR-2.** ACCEPT — the AR is correct that the council flagged the home-of-record *need* but deferred the *decision*, and that an unresolved owner is the workspace's signature cross-plan drift. My empirical pass made the split sharper than the AR stated: the D14 units resolve at `.harness/hardening-workflow/HARDENING_PLAN.md` (lines 277/365/375), **not** a repo-root file (which does not exist) — my Stage-1 c9.md said "MAIN checkout" loosely and the AR REVIEW.md cited a bare `HARDENING_PLAN.md:277`; DESIGN.md must use the real path. The dependency-cite framing (HARDENING_PLAN owns execution; WS-6 owns sequencing) is exactly the compose-don't-fork shape, and I defer the `HOOKS.md` lifecycle-topology authoring to C1 as co-owner of that same `tools/hooks/` surface.

**AR-7.** ACCEPT — verified both halves empirically. CREDIT half: `postcompact-reinject.sh` injects a bounded pointer into the dynamic suffix (§7.2-conformant, head-of-file self-documents the design) and `context-recovery.sh` does the threshold pre-save — both BUILT, no work owed. BUILD half: `session-start.sh` grep for `StopFailure|checkpoint|precompact|capture-failure` returned **empty** (reads neither), and `stop-loop.sh` emits `decision:block` JSON but has **no** `hook_write_checkpoint` call — both unbuilt, exactly D14 U-HK-30/40. Splitting WS-6 into 6a-credit / 6b-build removes the reader-hazard the AR named.

**AR-4 (VERIFY task — reported).** ACCEPT, and the verification flipped a "not confirmed this session" into a **confirmed negative**: `git -C ~/.claude/projects/.../memory rev-parse` → *fatal: not a git repository*, and walking the full parent tree found **no `.git`**. The store has **no git rollback boundary**, contradicting CLAUDE.md §12.5.1's "Provenance lives in git history at the global memory store." This is the strongest single empirical result of my pass: the recovery primitive C3's FM-H framing assumed is **absent**, so an FM-H lost-update is currently unrecoverable. DESIGN.md sequences detection → (now-resolved) rollback-boundary-confirm → serialization, and routes the rollback-boundary *remedy* to C3 as store-owner (C9 contributes only the existence verdict, per my FM-A line).

**MISSED TENSION C5↔C9.** RECONCILE — the AR correctly suspected the tension is reconcilable, and the resolution is a clean surface-discriminator: a CI/PR-time `--check` gate (C5) and a session-runtime degradation mode (C9) operate at different times on different surfaces. never-halt is a *loop-runtime* posture (CHARTER §2) and binds only C9's runtime mode; it has no jurisdiction over a PR-time CI gate, which is *supposed* to fail an over-budget PR before merge. They are the build-time and runtime halves of one byte-budget discipline — complementary, not contradictory. DESIGN.md states the discriminator so Stage 3 doesn't re-open it.

### Residual-open?

**None — reconciled.** All four assigned findings are ACCEPT or RECONCILE with empirical grounding the adversarial reviewer would concur with:
- AR-2 / AR-7 / the C5↔C9 reconciliation align with the AR's stated recommendations (the AR explicitly recommended the dependency-cite split for AR-2, the credit/build split for AR-7, and named the C5↔C9 surface-distinction as "likely reconcilable").
- AR-4 is *strengthened beyond* the AR's ask: the AR wanted the serialization gated behind a detection step and the rollback boundary confirmed; I confirmed it **negative** (no git boundary exists) — which the AR would accept as resolving its R5 "not confirmed this session" flag, not dispute it. The one item DESIGN.md must carry forward is a **cross-lane routing note** (not a dispute): the rollback-boundary *remedy* belongs to **C3** as store-owner — I surface the gap, C3 owns the fix. That is the standard C3↔C9 partial-failure-recovery co-primary, already the agreed shape in T5, so it is not an open dispute between adversarial and council.