All five findings are now empirically grounded. The §2 = 277,298B = 81% figure reproduces exactly (this also incidentally closes AR-6's sibling concern that the 81% was only [MODERATE] — it's now independently re-measured and exact, though AR-6 is specifically about the cache *multipliers*, which I handle separately). Existing injectors carry only roadmap/version strings, zero secret material. I have everything. Writing my reconciliation response now.

## C2 reconciliation response

### Disposition table

| AR-ID | Disposition | PLAN delta (specific) |
|---|---|---|
| **AR-1** (plan/execute boundary) | **ACCEPT** | DESIGN.md gets a one-line **plan/execute boundary banner at the head of the WS table** + a per-WS `Deliverable-of-THIS-arc` column. For WS-1: deliverable = *the static/dynamic cut-list spec + the byte-budget value* — NOT the CLAUDE.md eviction itself. For WS-2: deliverable = *the navigation-infra spec (which doc names, what each anchors, JIT-vs-prefix disposition per doc)* — NOT the authored `ARCHITECTURE.md`/`AGENTS.md`/`HOOKS.md`/`INDEX.md` files. Restate verbatim: "This arc produces a PLAN; a downstream execution arc authors the files and performs the eviction. No file under `/` is created and no CLAUDE.md content is evicted in this arc." |
| **§7 MISS #1** (AGENTS.md auto-load — VERIFIED) | **ACCEPT + RESCOPE** | WS-2 gains a **per-doc-name auto-load disposition table** keyed to the actual H_E (Claude Code). Verified result drives it (see Rationale). Concretely: (a) `ARCHITECTURE.md` / `WORKFLOW.md` / `HOOKS.md` / `INDEX.md` → **JIT-navigable, NOT auto-loaded** (safe as nav targets — confirmed Claude Code does not auto-read these). (b) `AGENTS.md` → **DROP the name OR budget it**: it is NOT auto-loaded by Claude Code today, but it IS a recognized auto-load convention for *other* runtimes (Codex/OpenAI) and a future H_T self-host. WS-2 default: **prefer `ARCHITECTURE.md` as the canonical orientation anchor; if `AGENTS.md` is retained for cross-runtime convention, it MUST be a thin (≤ nav-anchor budget) pointer-to-`ARCHITECTURE.md`, never a content doc** — so that any runtime which auto-loads it pays only a pointer, not a re-bloat. Add a standing note: the ONE prefix-forcing mechanism in Claude Code is `CLAUDE.md` `@import`; WS-2 anchors MUST NOT be `@import`-ed into CLAUDE.md (that would convert a nav target back into prefix content, re-creating the WS-1 disease). |
| **AR-5** (proportionality, my part: WS-1 + G1 = MVP) | **ACCEPT** | DESIGN.md names the **load-bearing MVP slice explicitly: WS-1 (the cost win) + WS-4 G1 (the one gate that holds the split) + WS-6/D14 (recovery co-requisite per T4) + X (FM-H fix)**. WS-1 is ranked **load-bearing #1**; WS-2 is split: **SSOT pointer + `design-substrate/INDEX.md` = load-bearing** (it is the *cause* of the §2 bloat per my F3 — without it the agent must carry the version inline), while the **top-level orientation docs (`ARCHITECTURE.md`/`HOOKS.md`/etc.) = proportionate-but-deferrable** (nice-to-have orientation anchors, not the cost mechanism). Cite cluster-2 §1.11 on the deferrable tail. |
| **AR-6** (cache multipliers [MODERATE]) | **ACCEPT** | In DESIGN.md (and the F2 carry), tag the "**1.25× write / 0.10× read**" multipliers **[MODERATE — Anthropic prompt-caching pricing, research §2.3; not re-verified at exact line this session]**. The **load-bearing claim stays [HIGH] and self-grounded**: "frequently-edited content must not sit in the re-attended prefix" + "the §2 cells are edited on 60/60 recent commits" (probe #2, reproduced). The qualitative cache-detonation argument survives without the specific numbers; the numbers are directional color, demoted from the load-bearing line. |
| **AR-8** (secrets pre-check covers NEW WS-2/WS-5 injectors) | **ACCEPT** | Re-state the C2 secrets pre-check in DESIGN.md as: "**No P1–P6 commitment, NOR any new WS-2 version-state injector, NOR any WS-5 health hook, places secret material in the static prefix or dynamic suffix.**" Empirically grounded: the proposed injectors carry only version-state / roadmap / health-metric strings (verified the existing sibling injectors carry zero secret material); the locked paid/secret deny-list remains *operating-rule* prefix content, not secret material. Owner note: C10 *enforces* via `permission-guard.sh`; C2 only *asserts* the injection surfaces are secret-free by construction. |

### Rationale

- **AR-1** — Correct and proportionate. My Stage-1 contribution already carried the lane discipline ("do NOT delete the versioned copies"), but the *WS table phrasing* reads as a build mandate. The fix is purely framing: a per-WS deliverable column that makes "this arc emits a spec, not the artifact" unmissable. No substantive change to my P1–P6 commitments — they remain the *content* of the plan. Folding in.

- **§7 MISS #1 (the VERIFY task) — empirical finding reported:** I verified three things at HEAD. (1) **No `AGENTS.md` / `ARCHITECTURE.md` / `WORKFLOW.md` / `HOOKS.md` / `INDEX.md` exists at repo root** (worktree or main); CLAUDE.md (341,611B) is the sole top-level doc — so none of these is *currently* auto-loaded because none exists. (2) **Claude Code's auto-load surface for this session is CLAUDE.md + its `@import` chain only** — the session preamble's `claudeMd` block contains global CLAUDE.md, RTK.md (pulled via the single `@RTK.md` import at `~/.claude/CLAUDE.md:1`), project CLAUDE.md, and MEMORY.md; **no AGENTS.md**. (3) The one `AGENTS.md` in the repo is `dashboard-design/impeccable/AGENTS.md`, a vendored frontend-skill file scoped to that subdir, **not** loaded here. **Conclusion:** the adversarial worry is *valid as a general convention* (AGENTS.md is an auto-load name for Codex/other runtimes) but **does NOT manifest in this harness's actual H_E** — Claude Code auto-loads CLAUDE.md, not AGENTS.md. The rescope therefore is: keep the non-auto-loaded names as JIT nav targets; for AGENTS.md specifically, prefer `ARCHITECTURE.md` as the canonical anchor and if AGENTS.md is kept (cross-runtime hedge / future self-host), constrain it to a thin pointer + forbid `@import`-ing any WS-2 anchor into CLAUDE.md (the only prefix-forcing path). This closes the disease-reintroduction risk at its actual mechanism. This is the sharpest finding and it sharpens WS-2 materially — ACCEPT.

- **AR-5** — The proportionality filter applies and my part ranks cleanly. I re-measured §2 = **277,298B = 81%** of CLAUDE.md (exact reproduction) — confirming WS-1 + G1 are the load-bearing cost win, and that the **SSOT pointer / INDEX.md half of WS-2 is also load-bearing** (it is *causally upstream* of the bloat per my F3: absent a "canonical = vN" pointer, the version got carried inline). The orientation-doc tail of WS-2 is the deferrable part. I rank accordingly and cite cluster-2 §1.11 on the tail.

- **AR-6** — Fair Class-1 nit. The multipliers are Anthropic pricing I cited from research §2.3 without isolating the exact line this session. The *mechanism* claim (cache detonation from per-PR prefix edits) is [HIGH] and self-grounded from CLAUDE.md's own edit cadence (60/60 commits, probe #2). I demote the numbers to [MODERATE] color and keep the qualitative claim load-bearing. ACCEPT.

- **AR-8** — Correct that my Stage-1 "no secret in prefix/suffix" claim predated WS-2's version-state injector and WS-5's health hooks as *new* injection surfaces. I verified the existing sibling injectors (`prompt-context.sh`, `session-start.sh`) carry only roadmap/version strings, zero secret material — so the new injectors of the same shape inherit the property. I re-affirm the pre-check to explicitly name the WS-2/WS-5 surfaces. ACCEPT.

### Residual-open?

**None — reconciled.** All five assigned findings are ACCEPT (one with an empirically-grounded RESCOPE on WS-2/AGENTS.md). No REBUT, no negotiated-middle the adversarial reviewer would still dispute. Cross-lane note for the orchestrator: the AGENTS.md rescope touches WS-2 which is co-owned **C2 × C1 (HOOKS.md-as-topology-contract) × C7 (observability anchor)** — my disposition constrains only the *loading/prefix-placement* of the proposed doc names (my lane); the *content* of `HOOKS.md`/`ARCHITECTURE.md` and whether they're authored at all remains C1/C7's call at execution time. AR-5's deferral of the orientation-doc tail and AR-2's home-of-record decision (WS-6, owned by C9) are adjacent but not mine to resolve.