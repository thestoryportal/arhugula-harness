Excellent advisor guidance. The AR-4 conflict-marker rationale was self-undermining (markers only arise from git merges; a non-git last-writer-wins overwrite is silent regardless) — I'll replace it with the two grounds that actually hold and turn the silent-by-construction property into the *justification* for detect-before-serialize. I'll make AR-4 two-dimensional, defer the rollback-primitive remedy to C9 (FM-J trap avoidance), add the inbound-cite gate to AR-3, and rank AR-5 with the §12.5.3 self-citation. All three remain ACCEPT, residual zero. Writing my reconciliation now.

## C3 reconciliation response

### Disposition table

| AR-ID | Disposition | PLAN delta (specific) |
|---|---|---|
| **AR-4** (FM-H "X" workstream severity) | **ACCEPT** | In DESIGN.md, re-label the **"X" workstream** from `HIGH — affects this very arc` to **two-dimensional: `consequence-severity HIGH (elevated) / incidence UNCONFIRMED`**. Consequence stays HIGH-and-arguably-elevated because I verified the durable store at `~/.claude/projects/.../memory/` is **not a git repository** → a lost-update is unrecoverable (my F2 stakes). Incidence is UNCONFIRMED on two grounds: (a) single-session reality (`git worktree list` = 2 worktrees, main + council-context-memory); (b) **the FM-H symptom is silent-by-construction (last-writer-wins on a non-git flat file leaves no trace), so incidence is retrospectively unobservable** — which is precisely the argument *for* gating the serialization remedy behind a cheap **forward-detection step** (a write-collision counter / mtime-race probe across loop multi-worktree usage) BEFORE building flock/journal machinery. C3 owns the serialization discipline + detection-signal; the **store-rollback-primitive remedy (version the memory store) is C9/T5's lane — defer it, do not author it** (FM-J avoidance). Eval signal unchanged: `concurrent-MEMORY.md-write conflicts per N sessions = 0`, now reframed as the detection-gate's pass condition. |
| **AR-3** (version-chain X-AL-3 execution-posture) | **ACCEPT** | In DESIGN.md, scope the **WS-3 version-chain remedy** (my F6) to **NON-canonical / superseded copies ONLY** (canonical-now file per artifact stays in place). Add an **inbound-path-cite verification gate** as the execution arc's precondition: grep for inbound `design-substrate/<artifact>_vN.md` path-cites to each superseded copy first — **zero hits → `git mv` to `design-substrate/archive/` is content-safe** (the dir already exists, 26 files; this *extends* an existing convention, not creates one); **non-zero hits → leave-in-place + SSOT-pointer** (relocation would break delta-only-preserve resolvable-anchor cites = scope creep). Name explicitly that **executing the relocation (later arc) touches `design-substrate/**` git-tracked paths** (verified: 19 CXA + 7 Workflow copies are `git ls-files`-tracked) → requires the **X-AL-3 escape-hatch per CLAUDE.md §4.4 (clearance marker OR `design-phase-direct` label) AND design-phase posture, NOT mode-agnostic**. As a *plan*, F6 is additive-safe; the execution-time posture + cite-gate must be named. |
| **AR-5** (WS-3 proportionality ranking) | **ACCEPT** | In DESIGN.md, rank the three WS-3 retention items against solo-scale (cluster-2 §1.11). **MVP (load-bearing): MEMORY.md compaction** — over-cap *now* (verified 27,051 B vs 24,400 cap this session), observed pain, machine-checkable; this is the one item that survives the §1.11 filter. **Defer-eligible: (i) `.harness/` Tier-5 archival** (F4 — 306 files, named-but-not-biting at solo scale) + **(ii) checkpoint-store asymmetry reconcile** (F3 — cite the workspace's *own* CLAUDE.md §12.5.3 calling gstack archival "optional low-value hygiene"; self-citation makes the defer un-arguable). Within the MVP item, reconcile my F1 "enforced compaction trigger": proportionate enforcement is **`--check`-tier (C5/WS-4 gate mechanism)**, NOT a C3-owned auto-rewrite of a durable store. **C3 owns the cap value + compaction policy** (shorten descriptions / drop superseded entries, full text preserved at topic files); **the gate is C5's** — consistent with the T2 resolution (cap value = C3, gate = C5). |

### Rationale

**AR-4** — VERIFY done: I checked for an actual concurrent-write event and found **0 conflict markers** in MEMORY.md and **no lock/lease/journal artifacts** — but per advisor, that probe has zero discriminating power, because conflict markers only arise from a git *merge*, and a non-git store overwritten last-writer-wins is silent whether or not the race ever fired. The honest, defensible grounds for incidence-unconfirmed are (a) the single-worktree session reality I verified, and (b) the silent-by-construction property — which I now turn into the *justification* for detect-before-serialize rather than a weakness. I keep consequence HIGH (the store is not git-versioned → unrecoverable lost-update), so the re-rate is two-dimensional, not a flat downgrade. REBUT was never on the table: I have no observed event, and unqualified-HIGH-on-an-unobserved-race is exactly the over-prioritization the reviewer flags.

**AR-3** — VERIFY done: the version copies ARE `git ls-files`-tracked `design-substrate/**` files (19 CXA + 7 Workflow), so the reviewer is correct that even a `git mv` touches protected paths. I also found `design-substrate/archive/` **already exists** (26 files — handoffs/logs), which *strengthens* the ACCEPT: the remedy extends an existing convention rather than inventing one. The boundary is thinner than the reviewer stated — relocation is text-safe but breaks inbound path-cites under the delta-only-preserve-verbatim resolvable-anchor convention — so I add an inbound-cite grep gate and decline to declare git-log-recovery the "safer default" (leaving files in place doesn't fix the F6 folder-pollution I diagnosed; it under-delivers). Scope-to-superseded + §4.4 escape-hatch + design-phase posture closes the gap.

**AR-5** — VERIFY done: MEMORY.md is **27,051 B vs the 24,400 cap** this session — observed, recurring pain (the system-reminder partial-load is live evidence), so compaction is unambiguously the MVP item. The other two (`.harness/` archival, checkpoint asymmetry) are named-but-not-biting at solo scale; for the checkpoint asymmetry, the workspace's own §12.5.3 already calls gstack archival "optional low-value hygiene," so deferring it is self-consistent, not a punt. The enforcement-shape sharpening (C5 `--check` gate, not a C3 auto-rewrite) keeps the lane split clean and stays consistent with the already-resolved T2.

### Residual-open?

**None — reconciled.** All three findings are ACCEPT; the dispositions *strengthen* the adversarial findings rather than reopening any dispute. The AR-4 rationale is corrected to grounds that survive Stage-3 scrutiny (the conflict-marker probe is discarded as non-discriminating; incidence-unconfirmed rests on single-session reality + silent-by-construction). The store-not-git-versioned finding is reported for severity but its remedy is explicitly deferred to C9/T5 (no FM-J lane leak). AR-3's inbound-cite gate + §4.4-posture and AR-5's MVP/defer ranking + C5-gate enforcement are all in-lane, additive-only, and consistent with the already-resolved T2. Housekeeping: the `Exit code 1` / session-learning reminder is the benign `grep -c` returning 1 on a zero count (it printed `0` correctly) — not a real failure, no action owed.