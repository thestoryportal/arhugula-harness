My verification is complete. Every empirical claim resolves:

1. **CHARTER §2 never-halt lock** — confirmed verbatim (lines 33-34): "never-halt, defer-and-continue, prefer-free-ollama, the locked deny-list... This arc does not relitigate them." This is the constraint the C5↔C9 tension must respect.
2. **`substitution_ledger.py --check` is the real model gate** — `return 1` at :206, `--check` "exit 1 on any [violation]" at :193, AND **CI-wired** (`.github/workflows/ci.yml:253` runs `--check`, :257 runs the contract test). The G1 precedent is real and CI-tier.
3. **MEMORY.md cap is advisory** — `session-end-cleanup.sh:53` detects `>24400B` then `exit 0` at :59; `loop-gc.sh:56` sets a `MEMFLAG` string, routes to advisory only. Routes-on-nothing confirmed. G1 target verified.
4. **Clearance markers: schema authored, zero validator** — `TEMPLATE.md` defines a typed `clearance_type` enum (9 values) + required `artifact`/`version`/`merge_commit`/`reviewer_chain`; **`grep` for any `tools/` validator returned empty.** **19 markers exist** — a real, growing corpus, contract-without-a-gate confirmed.

Now I can write my reconciliation with grounding.

## C5 reconciliation response

### Disposition table

| AR-ID | Disposition | PLAN delta (specific) |
|---|---|---|
| **AR-5** (WS-4 proportionality — rank G1-G4 against solo scale) | **ACCEPT** | DESIGN.md WS-4 gets an explicit **MVP gate set + a proportionality ladder**, citing cluster-2 §1.11. **MVP (build now): G1 only** — `--check`-tier byte-budget gate over CLAUDE.md + MEMORY.md, modeled byte-for-byte on `tools/substitution_ledger.py --check` (CI-wired, `exit 1`, contract test). G1 is the one gate that *holds the WS-1 split* — it is the load-bearing 80%. **G2 (clearance-marker schema gate): DEFER — promote-on-trigger.** It is a *real* contract-without-a-gate (verified: 19 markers, typed `clearance_type` enum in `TEMPLATE.md`, zero `tools/` validator), but at 19 markers hand-authored from a template, a new CI gate is governance-overhead "rarely paid back at solo scale." Mark it **optional/deferred**, with a named promotion trigger: *build G2 when the marker corpus crosses a threshold OR a malformed marker is first observed in the wild.* **G3 (ledger-shape gate): DEFER — likely fold into G2's machinery, not a standalone gate.** Same proportionality verdict; it shares `validate()→violation-list→exit-1` shape with G2, so if/when G2 is built, G3 rides the same harness at near-zero marginal cost. Standalone now = a second under-paid gate. **G4 (freshness-gate-teeth): NOT a new gate at all — DEFER as a posture *decision*, default to NO new teeth.** The §12 freshness CHECK already exists and is well-engineered; G4 was only ever a *decision* about whether to upgrade the §12.3 prose-halt to a blocking gate. Per the C5↔C9 resolution below, the answer is **leave it as model-prose drift-reconciliation (HITL-recoverable), do NOT add gate-teeth** — adding a blocking freshness gate would directly collide with never-halt. So G4 produces **no new CI gate**; it is resolved-as-no-change. **Net MVP gate set = {G1}. Deferred = {G2 promote-on-trigger, G3 fold-into-G2, G4 resolved-no-teeth}.** |
| **MISSED TENSION C5↔C9** (gate-vs-never-halt: surface + resolve my half) | **RECONCILE — they COMPOSE (no real conflict); resolved by surface-disjointness** | DESIGN.md must **name the C5↔C9 tension and record its resolution**: the G1 `--check` byte-budget gate fires at **CI/PR time** (a build-machine check on a proposed diff, identical surface to `substitution_ledger --check` at `ci.yml:253`); the never-halt / defer-and-continue posture (CHARTER §2, verified verbatim) governs **session-runtime in-loop behavior** (the agent observes a budget overage mid-arc and *continues*, never halting the loop). **These are different surfaces, so they compose without contention.** My C5 half: the G1 gate is **scoped to fire ONLY at CI/PR-time, and is forbidden from firing as a session-runtime in-loop blocker.** At session runtime the byte-budget is observe-and-continue (C9's `context_budget_exceeded` graceful-degradation MODE owns that surface — emit, degrade, recover, never halt). The boundary statement DESIGN.md encodes: *"G1 is a pre-merge CI gate (fails the build on a proposed over-budget CLAUDE.md/MEMORY.md change). It is NOT a session-runtime halt. In-session budget overage routes to C9's `context_budget_exceeded` degradation mode (never-halt). The `--check` gate and the never-halt posture govern disjoint surfaces — PR-time correctness vs in-loop resilience — and do not contradict."* |

### Rationale

**AR-5 — ACCEPT.** The reviewer is correct and the finding sharpens my own contribution: my Stage-1 contribution authored G1-G4 flat without ranking them against solo scale, which is exactly the "named the risk, didn't apply it" lapse AR-5 names. Applying the cluster-2 §1.11 filter honestly: **G1 is load-bearing** — it is the only gate that converts the C2/C3 split from prose (which "doesn't fail a build," the mechanism that let §2 reach 277KB) into something durable, and it mirrors a CI-tier gate that *already exists and is proven* (`substitution_ledger --check`, verified CI-wired at `ci.yml:253`). **G2/G3/G4 are each defensible but not load-bearing at 19 hand-authored markers** — building three more CI gates re-introduces the governance overhead the arc is removing. My empirical check makes this concrete: G2 targets a real gap (schema authored in `TEMPLATE.md`, zero validator), but proportionality says *defer-with-a-named-trigger*, not *build-now*; G3 shares G2's machinery so it should ride G2 rather than stand alone; G4 was never a new gate (the check exists) and its teeth-decision resolves to "no teeth" per the C5↔C9 line below. **MVP = {G1}.** This keeps my lane's contribution proportionate while preserving the full G2-G4 design as a documented, trigger-gated backlog — additive, nothing lost.

**MISSED TENSION C5↔C9 — RECONCILE (compose).** The reviewer caught a genuine unsurfaced tension and asked me to state my half. I verified the CHARTER §2 lock verbatim (never-halt / defer-and-continue, "does not relitigate"). The resolution is **surface-disjointness, not compromise**: a `--check` gate is a *build-time* artifact-correctness check on a proposed diff (the `substitution_ledger --check` precedent runs in CI on the PR, not in the agent's session loop); never-halt is a *session-runtime in-loop* posture. They never touch the same surface. My Stage-1 contribution already implicitly held this — F2/G1 routed G1 to "the CI tally-gate tier, NOT advisory hook" and "PR-open," never to an in-loop halt — but I did not *name* the C9 boundary explicitly, which is the sharpening owed. The clean three-way: **C5 owns the CI/PR-time gate contract** (fail the build on an over-budget proposed change), **C9 owns the session-runtime `context_budget_exceeded` degradation mode** (observe + degrade + continue, never halt), and the two are explicitly scoped to disjoint surfaces. This preserves never-halt intact (the gate is forbidden from being a runtime blocker) while still giving the C2/C3 split the `--check`-tier teeth it needs to hold. No conflict survives.

### Residual-open?

**None — reconciled.** Both findings in my lane resolve to ACCEPT/RECONCILE with no surviving dispute:
- AR-5 is a straight ACCEPT (proportionality filter applied; MVP={G1}, G2-G4 deferred-with-triggers) — the reviewer asked for exactly this ranking and the deferral is additive (full G2-G4 design preserved as backlog, nothing deleted).
- The C5↔C9 tension resolves to **compose** via surface-disjointness, which the reviewer itself pre-judged "likely reconcilable" (REVIEW §6 missed-tension #1: "the `--check` gate is a CI/PR-time check, not a session-runtime halt — different surface from never-halt-in-the-loop"). My resolution matches that prediction and adds the explicit boundary statement DESIGN.md should encode, plus the C9 co-owner assignment for the runtime surface. The never-halt / defer-and-continue lock is preserved, not relitigated.

**Cross-lane defers (not mine to resolve, named for the orchestrator):** the *cap VALUE* and at-rest compaction policy behind G1 stay **C3**'s (WS-3); the *cut-list/altitude* that determines what the budget covers stays **C2**'s (WS-1); G4's drift-reconciliation HITL primitive stays **C11**'s; gate *placement* in the hook/CI topology stays the **lifecycle owner**'s. I specify only the G1 gate-contract and its CI/PR-vs-runtime surface boundary.