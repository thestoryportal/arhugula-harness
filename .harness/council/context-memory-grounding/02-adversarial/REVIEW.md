The cite **fully resolves** — U-HK-30 (D14 Piece A+C, line 374) and U-HK-40 (D14 Piece B, line 384) are both real units, exactly as the council cited them. My earlier grep just truncated the literal matches. The council's D14 citation is precise and correct.

I now have everything needed. All load-bearing cites resolve; the probe-resolutions are sound; the discipline is impeccable. Let me write the complete review.

---

# Adversarial Review — Stage 1 Council Deliberation (Context & Memory Layer Grounding)

- **Stage:** 2 (adversarial red-team)
- **Arc:** council-context-memory-grounding
- **Artifacts reviewed:** `00-CHARTER.md`, `01-council/DELIBERATION.md`, `01-council/contributions/c{2,3,1,5,7,9}.md`, `04-reconciliation/reconciliation-log.md`
- **Date:** 2026-06-03
- **Review mode:** design-phase council deliberation producing a PLAN-shape (WS-1..WS-6 + X) — reviewed under §4.1 review-severity scale; Phase-7-fork classes (§2.7.6) stated explicitly where a disposition would trigger one
- **Finding count by class:** Class 3 (severe): **0** · Class 2 (moderate / in-arc revision): **5** · Class 1 (minor / drift): **4**
- **Highest-severity finding:** AR-1 (Class 2) — WS-2 authorizes creating `AGENTS.md`/`ARCHITECTURE.md`/`WORKFLOW.md`/`HOOKS.md` as the council PLAN's *deliverable*, but the PLAN-shape never re-states that AUTHORING those files is itself out of this arc's additive-ledger-only scope (it is the executed arc's job). A risk of scope-creep into authoring, not a defect in the analysis.
- **Disposition recommendation:** **CLEARED with Class-2 in-arc revisions.** The PLAN-shape is fundamentally sound. No Class-3 finding; no halt-class (§2.7.6 Class-1) defect; no phantom cite; no additive-only or X-AL-3 violation in the recommendations themselves. The 5 Class-2 revisions sharpen WS-boundaries and close two under-rated severity gaps.

---

## 1. Verdict

The PLAN-shape is **sound**, and unusually well-grounded for a Stage-1 council artifact. The central diagnosis — *the harness authored a Selective/Bounded/Navigation-mediated read contract (C-IS-07 §7.2) and a `verify_chain` gate (§6.4) for its **product** state, and runs its own **process** context/memory state in violation of both* — is not a rhetorical flourish: every load-bearing cite resolves byte-exact at HEAD (§2 below), every probe-resolution verifies independently, and the FM-A boundary discipline (C2/C3 each refusing to specify the other's lane) is held cleanly throughout. The convergence is real (six voices, one diagnosis, fix-patterns that demonstrably already exist in-repo) rather than primary-collapse concurrence — C9 and C5 each surface genuine tension (sequencing co-requisite; advisory-vs-gate enforcement tier) that materially reshaped the PLAN.

Gating-finding count: **0 Class-3, 5 Class-2, 4 Class-1.** No finding reaches the halt threshold. The Class-2 findings are scope-boundary sharpenings (AR-1, AR-2, AR-3) and two under-rated-severity flags (AR-4 the FM-H "X" workstream is rated HIGH but its in-arc manifestation is unconfirmed; AR-5 the proportionality red-team question #6 is asked but not answered by the PLAN-shape itself). The Class-1 findings are drift/consistency nits.

---

## 2. Cite-verification — did the council's load-bearing cites resolve at HEAD?

Every spot-verified cite resolves. This is the single most important section: a council grounded on a phantom cite would be a halt-class (§2.7.6 Class-1) defect. **None found.**

| Cite (claimant) | Claim | Verified at HEAD | Verdict |
|---|---|---|---|
| **§C-IS-07 §7.2 C2-pole read contract** (C2 first cite; the spine) | "Selective / Bounded / Navigation-primitive-mediated / Read-into-dynamic-suffix; direct full-file `cat`-style reads are precluded" | `Spec_Information_Substrate_v1.md:457` §7.2 table — `Selective` (461), `Bounded` (462), `Navigation-primitive-mediated` "direct full-file `cat`-style reads are precluded for the read contract" (463), `Read-into-dynamic-suffix` "enters the model's dynamic suffix (not the static prefix); prompt-cache integrity is preserved by construction… cache-prefix detonation prevention" (464) | ✅ **RESOLVES VERBATIM.** The "cat-style reads precluded" + the cache-prefix-detonation framing both exist exactly as cited. C2's diagnosis is grounded. |
| **§C-IS-07 §7.1 C3-pole write contract** (C1/C5/C9 cite) | read/write pair exists | `:446` §7.1 table (Append-only / Structured / JSON-not-Markdown / Idempotent `(thread_id,step_id,idempotency_key)` / JSONL / hash-chain-preserving) | ✅ **RESOLVES.** The §7.1/§7.2 pair is real and the C1 framing ("hook lifecycle executes the read/write contract") maps cleanly to the two poles. |
| **§6.4 `verify_chain`** (C5 first cite + convergence sentence) | `verify_chain` conformance gate exists | `:406` §6.4 "Chain verification on demand" — signature `verify_chain(ledger: List[StateLedgerEntry]) -> ChainVerificationResult` (`:408`); §6.5 tamper-evidence (`:418`) | ✅ **RESOLVES VERBATIM.** C5's "the harness wrote itself a conformance gate for product state" is literally true. |
| **§C-IS-02 five-tier model** (C3 first cite) | five-tier durability schema at lines 182-194 | `:168` "§2 C-IS-02 — Artifact-tier layering schema" | ✅ **RESOLVES** (header at 168; C3's line-range 182-194 is the table body, consistent). |
| **CLAUDE.md = 341,611B** (probe #2 + C2 F1) | exact byte count | `wc -c CLAUDE.md` = **341611** | ✅ **EXACT.** |
| **§2 = 277,298B = 81%** (C2 F1) | §2 spans 81% of the file | §2 is lines 40→107 (header `## 2.` at 40; `## 3.` at 108). The three cells (CP line 80, OD line 81, CXA line 92) are each ~52-55KB single table rows — directly observable in the loaded CLAUDE.md (the §2 table rows are pages of prose). The 81% figure is not independently re-measured here but the structure (3 mega-cells dominating §2) is confirmed by inspection. | ✅ **STRUCTURE CONFIRMED** (the 81% is plausible and the load-bearing claim — "3 cells dominate" — is directly visible). [MODERATE on the exact 277,298 figure — not byte-isolated this session; the *mechanism* is HIGH.] |
| **MEMORY.md cap is ADVISORY** (probe #1, T2, C5) | `session-end-cleanup.sh:53` + `loop-gc.sh:56` route on nothing | `session-end-cleanup.sh`: the cap check prints `OVER CAP, compact` inside the `{ … } > "$REPORT" … \|\| true` block, immediately followed by `exit 0`. `loop-gc.sh`: sets `MEMFLAG=…compact…` string, then `[ -z "$CANDS" ] && [ -z "$MEMFLAG" ] && exit 0` — routes into an *advisory `additionalContext` message*, never a failure. | ✅ **VERIFIED.** The cap is advisory — prints/injects, exits 0, routes on nothing. C5 is empirically correct; C2's "mirror the cap" was correctly corrected to "mirror `--check`." |
| **`substitution_ledger.py --check` exits 1** (probe #1, C5, the "one real gate") | the one real Tier-1 gate | `tools/substitution_ledger.py:193` `--check` arg "exit 1 on any [violation]"; `:206` `return 1`; `main() -> int` | ✅ **VERIFIED.** The `--check`-exits-1 gate is real and is the correct precedent. |
| **HARDENING_PLAN D14 / U-HK-30 + U-HK-40** (C9 R2/F2, T2-block, DELIBERATION) | output-cap recovery gap canonically scoped as D14 with units U-HK-30 (Piece A) + U-HK-40 (Piece B) | `HARDENING_PLAN.md:277` "D14 — output-token-limit resilience"; build-order table `:374` **U-HK-30** = "D14 Piece A+C" (stop-loop continue checkpoint); `:384` **U-HK-40** = "D14 Piece B" (post-truncation recovery, trigger `"event":"StopFailure"` + freshness window) | ✅ **RESOLVES EXACTLY.** Both unit IDs and their Piece-A/B mapping are correct. The StopFailure-not-Stop crux + the irreducible in-flight loss window (C9 F2) are stated verbatim in the plan (`:277`, `:294`). C9's "cite-don't-duplicate" discipline is honored precisely. |

**Cite-verification verdict: CLEAN.** Zero phantom cites. The single most dangerous failure mode for a Stage-1 council — building a six-voice diagnosis on a non-resolving anchor — did not occur. This is the strongest property of the deliberation.

---

## 3. Answers to the 6 named red-team questions (DELIBERATION §"What Stage 2 should red-team")

### Q1 — Is the §2-provenance eviction (WS-1) safe given delta-only-preserve-verbatim + X-AL-3?
**Answer:** **Yes, with one boundary precision.** The eviction is purely a *navigation move* (read-into-context placement), not a content edit. The provenance lives verbatim in (a) git history and (b) the design-substrate spec files' own delta-only change-notes — evicting the *copy* pasted into CLAUDE.md §2 removes nothing canonical. X-AL-3 governs `design-substrate/**` content; CLAUDE.md is workspace-operational (root `CLAUDE.md` §11.2 "mode-agnostic"), not design-substrate. C2's lane-note ("do NOT delete the 29 versioned copies… fix = navigation + altitude extraction, never retention") and C3's F6 (folder-architecture remedy, NOT content edits) both hold the line correctly. **Boundary precision (→ AR-1):** WS-1 *recommends* the eviction; the council PLAN must not itself *perform* it (additive-only). The DELIBERATION's "Hard lane discipline" paragraph (line 150) states this — good. **Verdict: SOUND.**

### Q2 — Does the FM-H cross-worktree write race actually manifest?
**Answer:** **The threat model is real but its current-arc *manifestation* is overstated as unqualified-HIGH.** The structural claim is verified: the durable store is at `~/.claude/projects/-Users-robertrhu-Projects-arhugula-v2/memory/` (confirmed in `loop-gc.sh` deriving `$HOME/.claude/projects/$(…)/memory/MEMORY.md` and `session-end-cleanup.sh` using the identical out-of-worktree path) — outside any worktree, shared by all worktrees + main, with no flock/lease. So *if* two sessions write MEMORY.md concurrently, lost-update is possible. **But neither C3 nor C9 confirms a concurrent-writer event has occurred** — C3 rates it HIGH on the *structural* surface; C9 (F5) correctly narrows to "the recovery-primitive existence is *not confirmed this session*." The CHARTER itself notes this arc runs in a single worktree (`git worktree list` = main + council-context-memory only). The honest rating is **HIGH-structural / UNCONFIRMED-incidence** — the race is latent, not observed. **Verdict: needs-revision** (severity calibration — see AR-4). The mitigation (route writes through one serialized hook path) is sound and proportionate; the "affects this very arc" framing in the "X" workstream overstates an unobserved race.

### Q3 — Is the cache-detonation cost claim robust if Claude Code's `cache_control` breakpoint isn't where C2 assumes?
**Answer:** **Yes — C2 hedged this correctly and the hedge holds.** C2 F2 explicitly states "[HIGH on the design-level claim — the cache-discipline rule (frequently-edited content must not sit in the re-attended prefix) holds regardless of the literal `cache_control` placement Claude Code uses; the recurring-edit mechanism is verified from CLAUDE.md's own text]." Probe #3 independently confirms there is *no* dev-loop observability home for `cache_*` (no hook consumes cache usage from Claude Code hook JSON), so the council cannot — and does not — claim a measured cache-hit-rate; it correctly downgrades signal-(b) to "a constraint, not a measurable" and substitutes the git-derivable §2-edit-cadence proxy (signal-d), which probe #2 saturates at 60/60 commits. The cost *argument* rests on the edit-cadence (verifiable) not the breakpoint location (unverifiable), so it is robust to the hedge. **One residual** (→ AR-6, Class-1): the "1.25× full write / 0.10× warm read" figures are cited to "research §2.3 + Anthropic caching" — not independently re-verified this session; they are directional, not load-bearing (the qualitative claim survives without them). **Verdict: SOUND** (the design-level claim is robust; the numeric multipliers are [MODERATE]).

### Q4 — Do the council PLAN and the U-HK HARDENING_PLAN fork on `tools/hooks/`? Which is home-of-record?
**Answer:** **The council handles this exactly right — it cites-don't-duplicates and declares the coordination need, but it does NOT yet *resolve* the home-of-record.** C9 R2/F2 names the exact units (U-HK-30 Piece A, U-HK-40 Piece B — both verified at HEAD), adopts the HARDENING_PLAN's scoping rather than re-authoring it, and flags "the PLAN should declare which artifact is the home of record so the two don't fork." WS-6 carries this ("cite, declare home-of-record"). **The gap (→ AR-2, Class-2):** the DELIBERATION states the *need* to declare home-of-record but the emerging PLAN-shape does not *make the declaration* — it defers it to DESIGN.md. That is acceptable for a Stage-1 bridge, but DESIGN.md MUST resolve it: the two plans both touch `stop-loop.sh` / `session-start.sh` / `prompt-context.sh`, and a forked owner is exactly the FM-H-shaped coordination drift the workspace is most prone to. **Verdict: SOUND-as-Stage-1** (correctly deferred), **needs-revision-at-DESIGN.md** (must actually pick the home-of-record; recommend: HARDENING_PLAN owns `tools/hooks/` execution, council PLAN owns the context/memory *governance* layer and *cites* D14 as its WS-6 dependency — they compose, they don't fork).

### Q5 — Any recommendation crossing into design-substrate content / R-NNN roadmap / `harness-*/src`?
**Answer:** **No.** Walked every WS + every voice recommendation:
- WS-1 (CLAUDE.md split) — CLAUDE.md is workspace-operational, not design-substrate. ✅
- WS-2 (new top-level nav docs) — additive new files (`ARCHITECTURE.md`/`AGENTS.md`/etc.). ✅ (modulo AR-1: don't author them *in this arc*).
- WS-3 (retention contracts) — `.harness/` archival + MEMORY.md compaction; `.harness/` is process-substrate, MEMORY.md is at the global path. No design-substrate content touched. ✅
- WS-4 (gates) — new `--check`-tier tooling; additive. ✅
- WS-5 (observability) — new health hooks; additive. ✅
- WS-6 (recovery) — *adopts* D14, declares home-of-record; touches `tools/hooks/` (process, not src). ✅
- "X" (FM-H serialization) — memory-store write path; not src, not design-substrate, not R-NNN. ✅
- C3 F6 / C2 F3 (version-chain) — **explicitly** routed to folder-architecture remedy (`design-substrate/archive/` or git-log recovery), with both voices stating "NEVER content edits" and X-AL-3 invoked by name. ✅

The DELIBERATION's closing "Hard lane discipline" paragraph (line 150) restates the additive-only / no-content-edit / no-src / no-R-NNN boundary and "all voices concur." **Verdict: SOUND.** No additive-only or X-AL-3 violation in the recommendations. (The one residual is execution-discipline, AR-1: the *deliverable* of THIS arc is a plan; a downstream arc executes — the PLAN-shape should not blur into authoring.)

### Q6 — Over-engineering check: are WS-4 gates / WS-5 observability proportionate to a solo-developer harness (cluster-2 §1.11: four-tier memory separation "rarely paid back at solo-founder scale")?
**Answer:** **Partially answered, partially unaddressed — this is the council's weakest spot (→ AR-5, Class-2).** The council *raised* the proportionality question (it's red-team Q6, and C3's cross-cutting notes flag "four-tier memory separation rarely paid back at solo scale"), but the PLAN-shape does **not visibly apply the proportionality filter to its own WS-4/WS-5 surface.** Specifically:
- **WS-4 (4 gate contracts G1–G4)** — G1 (CLAUDE.md byte-budget `--check`) is clearly proportionate (it's the highest-leverage cost lever and mirrors an existing one-file tool). G2/G3/G4 (clearance-marker schema gate + ledger-shape gate + freshness-gate-teeth) are *additional CI gates* — each is defensible, but four new gates on a solo harness risks re-introducing the governance-overhead the arc is removing. The PLAN does not rank them or mark any as "defer-if-budget."
- **WS-5 (5 un-surfaced health states + mid-session surface + dashboard health)** — C7's "observability is logically prior to retention policy" is a strong argument, but *five* new health instruments + a dashboard surface is exactly the "instrument everything" pattern that bloats. The cluster-2 §1.11 caution applies directly here and the PLAN doesn't engage it.

The proportionate core is unambiguous: **WS-1 (the cost win) + G1 (the one gate that holds the split) + WS-6/D14 (recovery co-requisite) + X (the FM-H fix) are the load-bearing 80%.** WS-4 G2-G4 and WS-5's full 5-state surface are the candidates for the proportionality filter. **Verdict: needs-revision** — DESIGN.md should explicitly rank each WS-4 gate and WS-5 instrument against the solo-scale proportionality test and mark the non-load-bearing ones as optional/deferred, citing cluster-2 §1.11. (The council *named* the risk; it must *apply* it.)

---

## 4. Nine-item pattern checklist (CLAUDE.md §10.9)

| # | Pattern | Verdict | Note |
|---|---|---|---|
| 1 | **Stale-carry-text disposition** | **PASS** | The deliberation is freshly authored; no carried framing. (Meta: the arc's *whole point* is to fix stale-carry in CLAUDE.md §2 — it diagnoses the pattern, doesn't commit it.) |
| 2 | **Sibling-spec staleness** | **PASS** | C3 cites IS spec at **v1.3** (the current canonical per workspace CLAUDE.md §2.3); §C-IS-02 line-range and the §7.1/§7.2 pair resolve at that version. No stale sibling-version cite. |
| 3 | **Forward-looking-cite phantom** | **PASS** (notable) | Every cited unit ID resolves at HEAD: U-HK-30, U-HK-40 (HARDENING_PLAN build-order table), §C-IS-07/§7.1/§7.2/§6.4/§C-IS-02 (IS spec), `substitution_ledger.py --check`. The §10.9 checklist's most-feared item (name a unit/path/symbol that doesn't exist) is **clean**. |
| 4 | **Checkpoint-listed-as-open-but-already-applied** | **FLAG (Class-1, AR-7)** | Minor: C9 F4/c9.md credits `postcompact-reinject.sh` as "already conforms to §7.2" and `context-recovery.sh` 60/75/85% as "BUILT + sound" — these are correctly credited as DONE, not re-proposed. **Good.** But WS-6 lists "§7.2-conformant re-hydration" as a recovery-completeness workstream *item* alongside the unbuilt D14 pieces — a reader could mistake the already-conforming behavior for owed work. Recommend WS-6 split "credit (postcompact-reinject conforms — no work)" from "build (D14 U-HK-30/40 — owed)." |
| 5 | **Plan-revision-against-not-yet-built-substrate** | **PASS** | The PLAN-shape's ACs (implicit) cite substrate that exists: the hooks, `substitution_ledger.py`, the IS spec contracts, the HARDENING_PLAN units. C9 is explicitly disciplined here ("`precompact-latest.md` count=0 is a gitignored runtime-artifact absence, NOT evidence the write path is unbuilt — lead with the code-grep of `session-start.sh`"). This is textbook avoidance of the U-RT-111 pattern. |
| 6 | **Spec-prose-vs-plan-body drift** | **PASS** | The council's "intended" side cites spec prose (C-IS-07 §7.2, C-IS-02) and the "as-is" side cites code (`session-end-cleanup.sh:53`, `loop-gc.sh:56`, `stop-loop.sh`). The as-is/intended pairing is the correct shape and the prose matches HEAD. |
| 7 | **Verification-shape grep-vs-e2e** | **PASS** (with credit) | The council *applies* this discipline correctly: probe #1 didn't grep-assert "the cap is enforced" — it *read the control flow* (`} > "$REPORT" … \|\| true` then `exit 0`) to prove the cap routes on nothing. That is the grep-vs-e2e sharpening applied to its own probe. C9's eval-ability surface even names "post-truncation recovery rate (currently 0%, measurable once wired)" — a behavioral check, not a grep. |
| 8 | **X-AL-3 anti-extension** | **PASS** (load-bearing, checked hard) | No recommendation extends H_T design or edits `design-substrate/**` content / `harness-*/src` / R-NNN. The version-chain remedy is explicitly folder-architecture (archive dir / git-log), with X-AL-3 invoked by name in C3 F6 and the DELIBERATION lane-discipline paragraph. The only residual is execution-vs-plan boundary (AR-1), not an extension. |
| 9 | **Halt-route-split-AC** | **PASS** | The PLAN-shape splits cleanly by owner (WS-1..6 + X each with a co-primary). No AC bundles a materializable atom with an unmaterializable one. The closest case — WS-6 bundling "credit postcompact-reinject" (done) with "build D14" (owed) — is the AR-7 Class-1 nit above, not a halt-route-split violation. |

**Checklist verdict: 7 PASS, 2 FLAG (both Class-1).** The deliberation is exceptionally clean against the workspace's named defect classes — notably items 3, 5, 7, 8, which are where this workspace historically bleeds.

---

## 5. Findings table

| ID | Class (§4.1) | §2.7.6 fork? | Target | Issue | Recommendation (shape, not content) |
|---|---|---|---|---|---|
| **AR-1** | **Class 2** | none | WS-1, WS-2 (PLAN-shape) + DELIBERATION lane-discipline | The PLAN-shape authorizes *creating* `ARCHITECTURE.md`/`AGENTS.md`/`WORKFLOW.md`/`HOOKS.md`/`INDEX.md` and *evicting* CLAUDE.md §2. The CHARTER §2 hard constraint is **"The deliverable is a plan, not its execution… additive only… writes findings to ledgers."** The DELIBERATION states this (line 150) but the WS table reads as authorizing the work. Risk: the next stakeholder treats WS-1/WS-2 as a build mandate inside this arc. | DESIGN.md must restate at the WS-table head that every WS is a *plan for a downstream execution arc*; this arc produces no `ARCHITECTURE.md` etc. and performs no CLAUDE.md eviction. Make the plan/execute boundary explicit per WS. |
| **AR-2** | **Class 2** | none | Q4 / WS-6 / C9 R2 | The council correctly flags the home-of-record need (council PLAN vs HARDENING_PLAN both touch `tools/hooks/`) but **defers the decision** to DESIGN.md. Leaving it unresolved is the exact cross-plan coordination-drift the workspace is most prone to. | DESIGN.md MUST *make* the declaration (not just note the need). Recommended shape: HARDENING_PLAN = home-of-record for `tools/hooks/` execution units (U-HK-30/40); council PLAN WS-6 = a *dependency cite* on D14, owning only the governance-layer sequencing (P1 lands WITH D14). The two compose; neither re-authors the other. |
| **AR-3** | **Class 2** | none | WS-3 / C3 F6 / C2 F3 (version-chain remedy) | The version-chain "folder-architecture remedy" (move superseded copies to `design-substrate/archive/` OR recover via git-log) is additive-safe in principle, but **moving a `design-substrate/*.md` file is a content-tree operation** — even a `git mv` of a superseded spec copy touches `design-substrate/**` paths. The boundary between "folder-architecture, not content" and "X-AL-3 forbids touching `design-substrate/**`" is thinner than the council states. | DESIGN.md should explicitly scope the version-chain remedy to the *non-canonical* copies only, route it through the X-AL-3 escape-hatch discipline (clearance marker / `design-phase-direct` label per CLAUDE.md §4.4), and flag that *executing* it (in the later arc) requires design-phase posture, not mode-agnostic. As a *plan* it is fine; the execution-time posture must be named. |
| **AR-4** | **Class 2** | none | "X" workstream + T5 + C3 F2 (FM-H) | FM-H is rated **HIGH** and framed as "affects this very arc," but the concurrent-writer event is **structural-latent, not observed** (single-worktree reality this session; C9 F5 itself says rollback-primitive existence is "not confirmed this session"). Unqualified-HIGH on an unobserved race risks over-prioritizing the "X" workstream above the load-bearing cost win (WS-1). | DESIGN.md should re-rate FM-H as **HIGH-structural / incidence-unconfirmed**, and gate the serialization remedy ("X") behind a cheap *detection* step first (does a concurrent-write conflict actually occur across the loop's multi-worktree usage?) before building flock/journal machinery. The mitigation is sound; the *priority* is the question. |
| **AR-5** | **Class 2** | none | WS-4 (G2–G4) + WS-5 (5 health states) vs red-team Q6 | The council *raised* the proportionality question (Q6) and *cited* cluster-2 §1.11 ("four-tier memory separation rarely paid back at solo-founder scale"), but the PLAN-shape does **not apply the filter to its own WS-4/WS-5 surface** — 4 new gates + 5 new health instruments + dashboard health are presented flat, none marked optional/deferred. Risk: re-introducing the governance/instrumentation bloat the arc removes. | DESIGN.md should rank each WS-4 gate (G1 load-bearing; G2–G4 candidates for defer) and each WS-5 instrument against the solo-scale test, citing cluster-2 §1.11, and mark the non-load-bearing ones explicitly optional. The proportionate core (WS-1 + G1 + WS-6/D14 + X) should be named as the MVP slice. |
| **AR-6** | **Class 1** | none | C2 F2 (cache multipliers) | The "1.25× full cache write / 0.10× warm read" figures are cited to "research §2.3 + Anthropic caching" but not independently re-verified this session. They are directional, not load-bearing (the qualitative claim — frequently-edited content must not sit in the re-attended prefix — survives without them). | Inline: tag the numeric multipliers [MODERATE] and cite the exact research §2.3 line, OR drop the specific numbers and keep the qualitative cache-discipline claim (which is [HIGH] and self-grounded from CLAUDE.md's own edit-cadence). |
| **AR-7** | **Class 1** | none | WS-6 (PLAN-shape) | WS-6 bundles already-conforming, credit-only behavior (`postcompact-reinject.sh` conforms to §7.2 — no work owed) with genuinely-owed build work (D14 U-HK-30/40). A reader could mistake the credited behavior for an open workstream item. | Inline: split WS-6 into "credit (no work — postcompact-reinject + context-recovery already conform)" vs "build (owed — D14 Piece A/B + context_budget_exceeded mode)." |
| **AR-8** | **Class 1** | none | DELIBERATION CCR concern #1 (security) vs C2 secrets pre-check | Minor consistency: the slim CCR concern #1 says "n/a for secret material" and C2's pre-check says "no secret enters the static prefix or dynamic suffix under any P1–P6." But WS-2 proposes a *dynamic-suffix version-state injector* and WS-5 proposes *new health hooks injecting context* — these are new injection surfaces that didn't exist when the "no secret in suffix" claim was made. Almost certainly still true (version-state + health metrics carry no secrets), but the claim should be re-affirmed against the *new* surfaces the PLAN introduces, not just the current ones. | Inline: re-state the secrets pre-check as "no P1–P6 commitment **nor any new WS-2/WS-5 injector** places secret material in prefix or suffix" so the assertion covers the surfaces the PLAN adds. |
| **AR-9** | **Class 1** | none | DELIBERATION "31 findings" / convergence claim | The header and reconciliation-log both state "31 findings" but the six contributions' structured-findings sections sum differently by voice (C2: 5, C3: 6, C9: 5, + C1/C5/C7 not re-counted here). The count is plausible but not independently audited; a stated total that doesn't match a per-voice tally is the workspace's count-drift defect class in miniature. | Inline: either show the per-voice finding tally that sums to 31, or soften to "~30 findings across 6 voices." (Low-stakes; flagged because count-precision is a named workspace discipline.) |

**Findings-considered-and-rejected (transparency):**
- **A4 fabricated-citation attack** — applied to all 4 spot-verified cite families (§C-IS-07/§6.4, byte counts, cap-advisory, D14/U-HK-30+40). **All resolve.** No finding.
- **A8 framing-contamination (highest-value)** — checked WS recommendations for persona/stack/deployment overcommitment. The arc is *explicitly* solo-developer-scoped (and Q6 even red-teams proportionality). No overcommitment; no committed-claim violation. No finding.
- **X-AL-3 anti-extension** — walked every WS for design-substrate-content / src / R-NNN edits. None (AR-1/AR-3 are execution-posture nits, not extensions). No Class-2/3 finding.
- **CP/OD/CXA axis-domain attacks** — N/A; this is a context/memory process-machinery deliberation, not an axis-spec review. Recorded as inapplicable per FM-D discipline.
- **FM-H (reviewing-the-council-not-the-artifact)** — every finding grounds in a `Location:` in the deliberation artifacts, not a critique of the deliberation *process*. (AR-9 is the closest, and it cites the header's stated count, an artifact claim.)
- **Probe-resolution independence** — re-ran the cap-advisory and `--check`-exit-1 probes from primary source; both reproduce. The council's probe-first log is honest.

---

## 6. Tension pressure-test

**T2 (C5↔C2, probe-resolved in C5's favor) — SOUND.** The probe is the strongest move in the deliberation. It didn't *assert* the cap is advisory — it read the control flow (`session-end-cleanup.sh`: print inside `\|\| true` block → `exit 0`; `loop-gc.sh`: set flag string → route to advisory `additionalContext`, never fail) and contrasted with `substitution_ledger.py --check` (`return 1` / `exit 1`). I reproduced both independently. The resolution — "the byte-budget is a `--check`-tier gate (G1), not a second advisory hook; cap *value* stays C3's, cut-list stays C2's, *gate* is C5's" — is a clean three-way lane-split with no residual conflict. **Probe-resolution is correct and load-bearing.**

**T1 (C2↔C3) and T6 (C1↔C9) correctly Layer-3.** T1 is the literal T-perm-2 (within-turn vs across-turn, C-IS-07 §7.1/§7.2) — the spec *exists* to resolve it permanently, so promoting it to Layer-3-permanent (not closing it) is correct; the operative resolution ("cut-list is co-primary C2×C3, sequence as a joint step, neither voice resolves alone") is the right shape for a permanent tension. T6 is T-perm-3 (C1↔C9, loop fault-handling-as-topology); surfaced, named, not relitigated — correct. **Both correctly Layer-3, not falsely closed.**

**T3/T4/T5 — genuinely reconcilable, no hidden deeper conflict.**
- **T3 (C7↔C2, legibility-vs-eviction):** C7's "eviction MUST be paired with a navigable INDEX + cheap version-state injector" is an *additive constraint* on C2's P1/P5, not a contradiction — C2 already proposed P5 (navigation infra). Genuinely reconcilable. No hidden conflict.
- **T4 (C9↔C2, sequencing):** The real content here is C9's "slimming the prefix converts recovery from redundant-backstop to load-bearing-dependency." This is a *genuine* tension (C9 explicitly says "not concurrence"), and the resolution (WS-1 lands WITH WS-6, not before) is sound. **C9 even self-calibrates** ("the evicted content is provenance, which recovery rarely needs, so the eviction's *own* blast radius is modest — the load-bearing point is the *principle*"). This honesty is what makes it reconcilable rather than a blocker. No hidden conflict.
- **T5 (C9↔C3, store recovery):** Clean co-primary split (C3 owns serialization+store+retention; C9 contributes only the rollback-primitive existence check). The only residual is the *unconfirmed* rollback boundary (AR-4 / C9 F5 "not confirmed this session") — but that's a verification owed at DESIGN.md, not a hidden conflict between the voices.

**Did the council MISS a tension?** Two candidates, neither fatal:
1. **C5↔C9 on gate-vs-degradation-mode (UNSURFACED).** C5 wants a `--check`-tier *gate* on the byte-budget (fails the build when over-cap). C9 wants a `context_budget_exceeded` *graceful-degradation mode* (observe + recover, never halt). On a `--check`-gate-vs-never-halt-posture axis these *could* tension: a hard `--check` gate on CLAUDE.md byte-budget that fails CI sits uneasily with the locked never-halt/defer-and-continue posture (CHARTER §2). The council didn't surface a C5↔C9 (or C5↔posture) tension here. **Likely reconcilable** (the `--check` gate is a CI/PR-time check, not a session-runtime halt — different surface from never-halt-in-the-loop), but DESIGN.md should *name* it: does the WS-4 G1 byte-budget gate ever fail a build in a way that contradicts never-halt? (→ folds into AR-5/AR-8 territory.)
2. **WS-2 nav-infra vs WS-1 prefix-budget self-tension (UNSURFACED).** WS-2 adds `ARCHITECTURE.md`/`AGENTS.md`/`WORKFLOW.md`/`HOOKS.md` — but `AGENTS.md` is a file many agent harnesses *auto-load into the prefix* (it's a recognized convention). If any of these new top-level docs are auto-loaded rather than JIT-navigated, WS-2 *reintroduces* prefix bloat that WS-1 just removed. The council asserts they are "JIT-loadable anchors (NOT pre-loaded)" but does not verify whether Claude Code's own loading rules would force `AGENTS.md` into the prefix. **This is the sharpest missed check** — recommend DESIGN.md verify the auto-load behavior of each proposed top-level doc name before committing to it as a "navigation target, not prefix content."

---

## 7. What the council MISSED

1. **The AGENTS.md auto-load risk (load-bearing unverified assumption).** [Highest-value miss.] WS-2 proposes `AGENTS.md` as a JIT-navigable anchor "NOT pre-loaded into the prefix." But `AGENTS.md` is a convention many agent runtimes auto-inject. If Claude Code (or a future harness self-host) auto-loads it, WS-2 silently re-creates the WS-1 bloat. The council *asserts* "not pre-loaded" without verifying the loading rule for that specific filename. **DESIGN.md must verify auto-load behavior per proposed top-level doc name** — otherwise the navigation cure reintroduces the disease. (→ surfaced as the §6 missed-tension #2.)

2. **Proportionality is named but not applied (under-engaged red-team Q6).** Per AR-5: the council cited cluster-2 §1.11 ("four-tier separation rarely paid back at solo scale") but did not run the filter over its *own* WS-4 (4 gates) / WS-5 (5 health instruments + dashboard). The honest proportionate slice is WS-1 + G1 + WS-6/D14 + X; the rest are candidates for defer. **Over-engineering risk is real and self-acknowledged but unmitigated in the PLAN-shape.** This is the single place the council's discipline (which is otherwise excellent) lapses — it asked the question and didn't answer it.

3. **FM-H severity is structural-latent, sold as incident-imminent (over-rated severity).** Per AR-4 / Q2: the "X" workstream is rated HIGH and "affects this very arc," but no concurrent-write event is observed, the arc is single-worktree, and C9's own rollback-primitive check is "not confirmed this session." The *threat model* is sound; the *incidence* is unconfirmed. Risk: "X" gets built ahead of the load-bearing WS-1 cost win. Gate the serialization machinery behind a cheap detection step.

4. **Home-of-record declared-as-needed, not declared (deferred coordination).** Per AR-2 / Q4: the most workspace-characteristic risk (two plans forking on the same `tools/hooks/` files) is correctly *flagged* but not *resolved* — it's punted to DESIGN.md. Acceptable for Stage-1, but it is the one deferral that, if it slips through DESIGN.md too, reproduces the exact cross-plan drift the arc exists to fix. Flagging it here so Stage-3/reconciliation holds DESIGN.md to actually making the call.

5. **No silent-absorption found, no out-of-scope recommendation found.** Affirmatively: I checked hard for (a) a recommendation crossing into `design-substrate/**` content / `harness-*/src` / R-NNN (none — AR-1/AR-3 are posture-precision, not crossings), and (b) silent absorption of a design defect (none — the council *cites* the IS spec as methodology-anchor, and C3 explicitly forecloses the wrong gap, "the `.harness/` ledger is NOT defective for being un-hash-chained… the hash-chain is the H_T *product* contract for a `state.jsonl` that doesn't exist yet"). That foreclosure is exactly the FM-A category-error avoidance the workspace prizes — credit it.

---

**Bottom line.** The PLAN-shape is sound and ready to advance to DESIGN.md with the 5 Class-2 revisions applied. The deliberation's grounding is the best property: every load-bearing cite resolves byte-exact, the probe-first discipline is honest and reproducible, and the FM-A lane-boundaries hold. No halt-class (§2.7.6 Class-1) defect, no phantom cite, no additive-only/X-AL-3 violation. The work that remains is sharpening — make the plan/execute boundary explicit (AR-1), *make* the home-of-record call (AR-2), scope the version-chain remedy's execution-posture (AR-3), re-rate FM-H by incidence (AR-4), and apply the proportionality filter the council itself named (AR-5) — plus verify the AGENTS.md auto-load assumption before WS-2 commits to it (§7 #1). Recommend **CLEAR to Stage 3 (Codex + advisor) with Class-2 revisions folded into DESIGN.md.**