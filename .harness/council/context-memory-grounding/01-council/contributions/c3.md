# C3 — co-primary
**First cite (canonical, grounded first):** Spec_Information_Substrate_v1.md (v1.3) §C-IS-02 five-tier artifact-layering schema (lines 182-194) + §C-IS-07 read/write contract pair (T-perm-2 F2-layer resolution)
## C3 — State, Memory & Persistence: grounding the harness's *own* durable-state machinery against the five-tier methodology

**Framing (what I own here, and the one thing I am NOT claiming).** My anchor is the five-tier durability model at `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-02 (working / episodic / semantic / procedural / durable), the C-IS-07 read/write contract pair (the T-perm-2 F2-layer resolution), and the research the spec is built on — `research/agent-harness-eng-research-cluster-2-context-prompts-memory.md` §2.3 (State and Memory Consistency), which names the **State Ledger pattern** and **"ledger bloat … requires a real policy"** as a field failure mode. The mandate is to align the *context/memory PROCESS machinery* (CLAUDE.md/MEMORY.md/checkpoints/`.harness/` ledgers/git-as-state/hooks) to that methodology.

I want to foreclose one tempting-but-wrong central gap up front, because it is the FM-A category-error my own skill warns against. **The `.harness/*.md` process ledger is NOT defective for being "un-hash-chained."** C-IS-06's hash-chain is an **H_T product contract** for the runtime `state.jsonl` — and grep confirms no `state.jsonl` exists anywhere in this worktree; the hash-chain primitive is the thing being *built*, not the thing the dev process runs on. The process ledger is git-tracked (`git ls-files .harness/` returns the corpus), and **git commits already ARE a parent-SHA chain** — research §2.3 explicitly sanctions git-as-state as the durable ledger and names **rebase/squash**, not absence-of-hash-chain, as the integrity failure mode. So the "intended" side of my findings is C3's **five-tier methodology** (append-only discipline + *mandatory* pruning-axis-per-tier + concurrent-write contract), with the IS spec as **methodology anchor, not conformance target**. Every gap below is an **FM-G (pruning silent)** or **FM-H (concurrent-write silent)** instance — which is exactly the class the repo data hands me.

### The five durability tiers mapped onto this repo (the as-is map)

| C3 tier | This-repo substrate (AS-IS) | Pruning policy AS-IS | FM verdict |
|---|---|---|---|
| **Tier 1 — filesystem working state** | `CLAUDE.md` (761 lines / ~333KB, re-read every turn); `MEMORY.md` index (27,051 B); `.harness/roadmap_status.md` dashboard (`workspace_state_hash` = Tier-1-as-progress-file) | **manual only**; MEMORY.md cap *advisory-surfaced* in `session-end-cleanup.sh`, **no enforced compaction** | **FM-G** (MEMORY.md over cap) |
| **Tier 2 — git history** | the whole repo; `.harness/**` is git-tracked; design-substrate delta-version chains | indefinite (correct per methodology); **but** forward-only-ledger discipline is *unenforced convention* | partial — rebase/squash risk (research §2.3) |
| **Tier 3 — serialized snapshots** | `.harness/.checkpoints/` (PreCompact, **keep-10 enforced**) + `~/.gstack/.../checkpoints/` (97 files, **`/context-save`, NO policy**) | **asymmetric**: keep-10 here, unbounded there | **FM-G** + retention-contract split |
| **Tier 4 — vector / semantic store** | **absent** — no vector store; semantic memory lives as *flat-file* `memory/*.md` (169 files) retrieved by index scan, not embedding | n/a (no Tier-4 substrate) | scoped finding (below) |
| **Tier 5 — append-only ledger** | `.harness/` 306 `.md` (118 forks + 50 retirement batches + 21 clearance + 2 architect-rec + ~115 other); `substitutions.yaml` (schema-backed, R-600) | **archive = 3 files**; no `ledger_retention_days`-analog | **FM-G** (ledger bloat, research §2.3 named) |

The methodology's own framing — "most state classes touch at least two tiers" — holds: a single arc lands in Tier-1 (roadmap_status), Tier-2 (commit), Tier-3 (checkpoint), Tier-5 (`.harness/` fork+retirement doc), and a memory entry. The defect is not the *spread*; it's that **three of the five tiers have no enforced retention contract**, which is precisely the FM-G my skill exists to prevent.

### The C2↔C3 seam (T-perm-2) — surfaced, NOT silently split

CLAUDE.md is where the Layer-3 permanent tension fires, and I engage it directly per the mandate. **`C3 specifies` the at-rest half**: CLAUDE.md is Tier-1 filesystem working state; its *write/prune discipline* (who edits it, version-binding, drift against canonical HEAD, byte-budget hygiene) is mine. **The within-turn half is C2's read seam** and I defer it: the 333KB-into-every-prompt cost, attention budget, and *what to cut for context-budget reasons* are C2's call, not mine — specifying that would be FM-A boundary leakage. So my contribution to operator-hunch #1 is bounded: I flag that CLAUDE.md-at-rest has **no pruning axis and no single-writer contract** (a Tier-1 FM-G + FM-H), and that the `optimize-claude-md` skill already exists as the *write-discipline* primitive but is **cadence-unbound** (no enforced trigger). **The actual cut-list is co-primary territory (C3 storage-discipline + C2 attention-budget); route the "what to trim" decision to council, not to me alone.** I name T-perm-2 as `surfaced`.

### FM-H — the concurrent-write surface I am NOT underweighting

This is squarely mine and the repo data exposes it cleanly. The durable memory store lives at `~/.claude/projects/-Users-robertrhu-Projects-arhugula-v2/memory/` — **outside the worktree**. Therefore every worktree (`council-context-memory` and siblings) **plus** main share **one** `MEMORY.md` + `memory/*.md` set with **no serialization contract**. Two concurrent council/loop sessions in different worktrees both appending an index line to the 27KB MEMORY.md is the research §2.9 single-writer-per-thread violation; symptom is **lost-update / last-writer-wins on the index** (the cap-compaction race makes it worse — a near-cap file edited concurrently silently drops entries). My own §9.2 concurrent-read invariant is satisfied by the read-only nature of most accesses, but the *write* path has no flock/lease/single-writer discipline. This is FM-H and it is real today.

### Tier-5 ledger bloat — the named failure mode, observed

`.harness/` is the process Tier-5. 306 `.md`, forward-only (retirement batches 1→51, monotonic — good append-only discipline), but the retention axis is **absent**: `.harness/archive/` holds **3 files**. Research §2.3 names "ledger bloat requires a real policy" and the methodology gives the remedy shape: `ledger_retention_days` with archival-to-cold-storage. The *good* news worth crediting: `substitutions.yaml` + `tools/substitution_ledger.py` is the **one place** the methodology is correctly applied — a schema-backed single-source-of-truth with a CI tally gate, killing the count-drift defect class (R-600). That is the model the *rest* of the Tier-5 surface should follow (derive-don't-hand-maintain), and it argues the remedy is tractable.

### Version-chain bloat (operator #3) — a tier-confusion finding, in my lane

7 `Project_Workflow_v1_*.md` + 19 `Cross_Axis_Composition_Document_v2_*.md` physical copies in `design-substrate/` (240 `.md` total). **Content is out of scope** (design-substrate is canonical; X-AL-3 forbids my touching it). But there is a clean C3 finding: **git (Tier-2) already holds version history**; materializing N physical copies **duplicates Tier-2's job at Tier-1** and pollutes the folder-architecture-as-interpretable-context. The remedy is a *folder-architecture* convention (single canonical file per artifact + git history for prior versions, with superseded versions moved to a `design-substrate/archive/` or recovered via `git log`), **not** content edits. This is the `[[wrong-version-read-delta-only-baseline]]` hazard's structural root: when 19 copies coexist, "canonical" is ambiguous at the filesystem layer.

### Closing cross-cutting pre-checks (mandatory per my skill §8)

- **Reliability/failure-containment (C9 seam):** the keep-10 Tier-3 prune races nothing today (single-process), but the *gstack 97-checkpoint* unbounded growth + the cross-worktree MEMORY.md write are the recovery-integrity risks. Rollback primitive is sound (git commit = Tier-2 boundary); *when* to prune is the open contract.
- **Eval-ability (C8 seam):** the measurable claims a PLAN should carry — `MEMORY.md bytes ≤ cap` (enforced, not advisory); `.harness/ active-doc count` vs archived; `checkpoint count per store ≤ retention-N`; `concurrent-MEMORY.md-write conflicts per N sessions = 0`. "We prune" is not measurable; these are.
- **Cost (storage):** Tier-5 `.harness/` and Tier-3 gstack are the unbounded-growth cost surfaces; Tier-1 CLAUDE.md is a *per-turn token* cost (C2-owned), not a storage cost.

---

## Key findings (structured)

### F1 [high] MEMORY.md durable-memory index is OVER its byte cap with no enforced pruning (FM-G ledger-bloat at Tier-1/semantic-index)
- **As-is:** /Users/robertrhu/.claude/projects/-Users-robertrhu-Projects-arhugula-v2/memory/MEMORY.md = 27,051 bytes vs the 24,400-byte cap (system reminder confirms only partial load); 169 topic files; cap is surfaced ADVISORY-only in tools/hooks/session-end-cleanup.sh (prints a report, auto-deletes nothing).
- **Intended:** C3 five-tier methodology (Spec_Information_Substrate_v1.md v1.3 §C-IS-02 + skill §'Pruning and GC') + research §2.3 'ledger bloat … requires a real policy': every tier states a retention axis + default; near-cap stores must compact (shorten descriptions / drop superseded entries, full text preserved at topic files) BEFORE adding.
- **Recommendation:** PLAN should specify an ENFORCED MEMORY.md compaction trigger (not advisory). C3 specifies pruning policy for the semantic-index Tier-1 surface, axis byte-budget, default ≤ cap, operator-tunable as a memory_index_max_bytes-analog; the 3 existing memory_audit rounds at .harness/memory_audit_2026-05-31{,_round2,_round3}.md are the audit primitive — bind them to a cadence. Eval signal: MEMORY.md bytes ≤ cap, machine-checked at SessionEnd.

### F2 [high] Cross-worktree shared memory store has NO concurrent-write contract (FM-H single-writer violation)
- **As-is:** The durable store lives at ~/.claude/projects/-Users-robertrhu-Projects-arhugula-v2/memory/ — OUTSIDE the worktree. All worktrees (council-context-memory + siblings) AND main write the same MEMORY.md + memory/*.md with no flock/lease/single-writer serialization. Concurrent loop/council sessions appending index lines = lost-update; worse during near-cap compaction.
- **Intended:** C3 skill §'Concurrent-write contract' (research §2.9): single writer per logical state lineage; symptom-on-violation must be specified. Methodology requires the write contract to make serialization explicit.
- **Recommendation:** PLAN should specify a concurrent-write discipline for the shared memory store. C3 specifies concurrent-write semantics for the durable memory store: single-writer-per-store via advisory flock OR an append-only journal merged at a serialization point; failure symptom on violation is silent index lost-update. Cheapest viable: route all memory writes through one hook-mediated serialized path rather than direct multi-session edits.

### F3 [medium] Tier-3 checkpoint pruning is asymmetric across two stores (one enforced, one unbounded)
- **As-is:** .harness/.checkpoints/ (PreCompact snapshots) has an ENFORCED keep-10 prune in session-end-cleanup.sh; the gstack /context-save store at ~/.gstack/projects/thestoryportal-arhugula-harness/checkpoints/ has 97 files and NO retention policy. Same C3 tier, two different retention contracts.
- **Intended:** C3 skill Tier-3: 'retention by count (last N) or by age (last T); default leans keep-last-N to bound storage.' One tier should carry one coherent retention contract (or an explicit rationale for the split).
- **Recommendation:** PLAN should reconcile the two Tier-3 retention contracts. CLAUDE.md §12.5.3 already declares the gstack store 'append-only history by default; archiving … optional low-value hygiene' — make that rationale-for-asymmetry EXPLICIT and bounded (e.g. keep-last-N-per-arc archived), so the 97-and-growing store has a stated ceiling. The keep-10 PreCompact store is correctly the thin-temporal lifecycle; preserve it.

### F4 [medium] Process Tier-5 ledger (.harness/) has no retention/archival policy — the named ledger-bloat failure mode
- **As-is:** .harness/ holds 306 .md (118 class_*_fork + 50 retirement batches + 21 clearance + 2 architect-rec + ~115 audit/plan/handoff). Forward-only discipline is observed (retirement batches monotonic 1→51 — good), but the archival surface .harness/archive/ holds only 3 files. No ledger_retention_days-analog.
- **Intended:** C3 skill Tier-5: 'named failure mode ledger bloat requires a real policy; default retention by age with archival to cold storage; operator-tunable as ledger_retention_days.' Research §2.3 names this explicitly.
- **Recommendation:** PLAN should specify a Tier-5 archival policy for .harness/. C3 specifies pruning policy for the process Tier-5 ledger, axis age + lifecycle-state (closed/applied fork docs, superseded batches), default archive-not-delete (append-only integrity preserved), operator-tunable. Model it on the ONE place done right: substitutions.yaml + tools/substitution_ledger.py (schema-backed single-source-of-truth, derive-don't-hand-maintain, CI tally gate). Never auto-delete (forward-only); archive to .harness/archive/.

### F5 [medium] CLAUDE.md-at-rest lacks a pruning axis and single-writer discipline (the C3 half of operator-hunch #1; the C2 half is the within-turn cost)
- **As-is:** CLAUDE.md = 761 lines / ~333KB / ~60 headers, re-read every turn. The optimize-claude-md skill exists as a WRITE-discipline primitive but is cadence-unbound (no enforced trigger). No byte-budget hygiene contract at the Tier-1 storage layer.
- **Intended:** C3 skill Tier-1: CLAUDE.md pruning is 'manual; operator-tunable' — but the methodology still requires a STATED discipline. T-perm-2 (C2↔C3) fires: at-rest storage/prune-discipline is C3; into-prompt attention-budget + what-to-cut is C2 (FM-A boundary — I do NOT specify the cut-list).
- **Recommendation:** PLAN should (a) bind optimize-claude-md to a cadence (~every N PRs / SessionEnd) so the write-discipline primitive actually fires — C3-ownable; (b) route the WHAT-to-trim decision to council as co-primary C3-storage + C2-attention-budget, NOT to either voice alone. C3 specifies a stated Tier-1 hygiene discipline; defers within-turn-cost trimming to C2. Surface a single canonical governance-doc set (the AGENTS.md/ARCHITECTURE.md/WORKFLOW.md absence operator-hunch #4 is C2/folder-architecture-owned; I flag only that whatever lands must carry an at-rest write+prune contract).

### F6 [low] Design-substrate version-chain materializes N physical copies — duplicates Tier-2's job at Tier-1 / Tier-5 (folder-architecture-as-interpretable-context pollution)
- **As-is:** 7 Project_Workflow_v1_*.md + 19 Cross_Axis_Composition_Document_v2_*.md physical files in design-substrate/ (240 .md total). 'Canonical' version is ambiguous at the filesystem layer — the structural root of the [[wrong-version-read-delta-only-baseline]] hazard.
- **Intended:** C3 five-tier model: git (Tier-2) IS the version-history substrate. Materializing every delta version as a separate file at the filesystem layer duplicates Tier-2 and pollutes the folder architecture (interpretable context). Methodology: version history lives in git; the working tree holds the canonical-now.
- **Recommendation:** PLAN should recommend a FOLDER-ARCHITECTURE remedy (NOT content edits — design-substrate content is canonical + X-AL-3-protected): single canonical file per artifact name with prior versions recovered via git log OR moved to design-substrate/archive/. This is a tier-confusion fix in C3's lane; the spec content itself is untouched. Co-check with the voice owning interpretable-context/folder-architecture before sequencing (likely C2).

## Tensions raised

- **with C2** — CLAUDE.md (333KB re-read every turn) — at-rest storage/prune-discipline (C3) vs into-every-prompt attention-budget + what-to-cut (C2). This is T-perm-2 firing on the harness's own Tier-1 governance doc.
  - **C3 position:** C3 owns the at-rest write+prune discipline and flags that CLAUDE.md has no enforced pruning cadence + no single-writer contract; C3 explicitly does NOT specify what to cut for context-budget reasons (that is C2's read-seam call, and specifying it would be FM-A). The WHAT-to-trim decision is co-primary C3-storage + C2-attention-budget and must route to council, not be resolved unilaterally by either voice. Surfaced as T-perm-2, not silently split.

## Cross-cutting notes

Reliability/failure-containment (C9 seam): rollback primitive is sound (git commit = Tier-2 boundary); the open contracts are WHEN to prune the unbounded gstack Tier-3 store + the cross-worktree MEMORY.md write race — both recovery-integrity risks. Eval-ability (C8 seam): a PLAN must carry measurable claims — MEMORY.md bytes ≤ cap (enforced); .harness/ active-vs-archived doc count; checkpoint-count-per-store ≤ retention-N; concurrent-MEMORY.md-write conflicts per N sessions = 0. 'We prune' is not measurable; these are. Cost: Tier-5 .harness/ + Tier-3 gstack are the unbounded storage-growth surfaces; CLAUDE.md is a per-turn TOKEN cost (C2-owned), not a storage cost — do not conflate. HITL/local-first (C11 seam): all stores are local filesystem; the durable memory store sitting OUTSIDE the worktree is the local-first surface that breaks worktree isolation (FM-H). Security/blast-radius: n/a to this contribution (the locked paid/secret/destructive deny-list + never-halt/defer-and-continue operator rules are preserved and not relitigated).
