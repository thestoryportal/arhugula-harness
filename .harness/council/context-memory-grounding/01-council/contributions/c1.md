# C1 — consultant
**First cite (canonical, grounded first):** Spec_Information_Substrate_v1.md (v1.3) §C-IS-07 §7.1 C3-pole write contract + §7.2 C2-pole read contract (the T-perm-2 read/write pair — the seam the session/loop lifecycle executes)
## C1 — Orchestration & Control: the session/loop lifecycle IS the runtime execution of the C2↔C3 read/write contract

### The frame: the hooks are not metadata about the harness — they are the control-flow that fires C-IS-07's reads and writes

My first cite is the C2↔C3 spine itself, because my lane sits exactly on it. **C-IS-07 §7.2 (the C2-pole read contract)** specifies *what* enters context and *that* it enters via navigation primitives into the dynamic suffix; **§7.1 (the C3-pole write contract)** specifies *what* is durably appended. Neither contract says *when* on the control-flow timeline those events fire, or *what control-transfer boundary* triggers them. That is C1's surface. The connective claim no other voice will make: **the hook lifecycle wired at `.claude/settings.json` is the runtime topology that executes the C-IS-07 read/write pair.** The read events (context IN) sit at three control-transfer boundaries — `SessionStart` (session boundary; the §12.1 audit, `tools/roadmap-audit/session-start.sh`), `UserPromptSubmit` (turn boundary; `prompt-context.sh` + `skill-activation-check.sh` + `prompt-lint.sh`), and `PostCompact` (compaction boundary; `postcompact-reinject.sh`). The write events (state OUT) sit at `PreCompact` (`precompact-checkpoint.sh`), `SessionEnd` (`session-end-cleanup.sh`), `Stop` (`stop-loop.sh`), and `PostToolUse` (`post-merge-refresh.sh`). Per my skill's C1↔C2 seam, **C1 owns where these read/write events sit on the control-flow timeline; C2 owns what enters at them; C3 owns what is durably written.** That is my grounded connection to T-perm-2 — and it reframes the operator's hunch #5 ("no synthesized hook-system doc") as a *topology* gap, not an inventory gap. [HIGH — settings.json wiring + hook bodies read this session; C-IS-07 §7.1/§7.2 verbatim]

I am convened as **consultant** on the C2/C3 read/write seam: I contribute the lifecycle-topology layer and name owners; I do not recuse (recusal discipline is for single-voice invocations).

### Reaction to C2 — concur on placement, but the *why* is topological, and the loop AMPLIFIES C2's cost finding

**Concur-with-rationale (non-formulaic):** C2's claim that the UserPromptSubmit injectors are "correctly placed in the dynamic suffix" is right, and the *reason* is mine to supply: each injector fires at a **control-transfer boundary** (turn / session / compaction), which is precisely where a Read-into-dynamic-suffix event (§7.2) belongs — it cannot warm the static prefix because it is re-derived per boundary by construction. C2 diagnosed *what* enters and *where in the prompt*; I confirm *where on the timeline* and that the timing is correct. The harness's best-disciplined context-engineering asset is correctly placed because the control-flow boundaries are the right firing sites.

**Refine C2's HOOKS.md recommendation:** C2 framed the gap as a navigation-anchor / interpretability gap and recommended a `HOOKS.md`. I refine the *content*: the missing artifact is not a hook **inventory** — it is a **lifecycle-topology contract** documenting (a) the control-transfer boundaries, (b) the read/write event at each, (c) the termination contract, and (d) the handoff contracts (below). An inventory lists 14 hooks; a topology doc names the 3 context-IN boundaries + 4 state-OUT boundaries and the contracts at each. That is the discoverable navigation anchor a fresh context lands on to understand the control spine.

**Additive cost amplification (FM-G cost pre-check — my obligation):** C2's largest finding is the ~334KB prefix cache-detonating per spec PR. The loop topology **multiplies** that cost. `stop-loop.sh` auto-continues up to `HARNESS_LOOP_MAX=25` turns per run without an operator round-trip; every one of those 25 turns re-attends the full static prefix. So under loop mode, C2's per-turn prefix cost is paid up to 25× per run before any operator boundary. **C2's prefix-warming fix (P1/P3) therefore has outsized value specifically under autonomous loop mode** — the loop is the cost multiplier on the exact prefix C2 wants to stabilize. I surface this as the topology-level cost implication; the optimization is joint C2 (attention budget) — I do not specify the cut-list.

### Reaction to C3 — concur on the at-rest/within-turn split; the checkpoint asymmetry is a handoff-LIFECYCLE question

**Concur-with-rationale:** C3's T-perm-2 split — at-rest write/prune discipline is C3's, the within-turn cut-list is C2's — is correct, and I add the third leg: the *firing-site placement* of the write events is mine. C3 owns *what* `precompact-checkpoint.sh` durably writes and its retention; I own *that* it fires synchronously at the compaction boundary and *that* its output is the guaranteed-surviving handoff payload.

**Refine C3's checkpoint-asymmetry finding into a handoff-topology observation.** C3 correctly flagged two Tier-3 stores with asymmetric retention (`.harness/.checkpoints/` keep-10 enforced; gstack 95 files unbounded — I measured 95, confirming C3's "97"-class observation). The retention is C3's lane and I defer it. My distinct angle: these are **two handoff topologies across a context boundary**, and the *handoff contract* (per topology) is well-specified — `precompact-checkpoint.sh` writes `precompact-latest.md`; `postcompact-reinject.sh` reads exactly that pointer back; the payload that is *guaranteed to survive compaction* is contract-specified. This is the research §2.2.3 compaction-with-durable-handoff pattern, correctly instantiated. **The gap is composition, not contract:** the relationship between the two handoff topologies (PreCompact/PostCompact thin-temporal vs `/context-save`÷`/context-restore` rich gstack) lives in CLAUDE.md §12.5.3 prose but is not surfaced as a single handoff-lifecycle contract. (Note: `.harness/.checkpoints/`=0 files this session is **not a defect** — it is expected per-worktree gitignored state with no compaction having fired here.)

### C1's unique, non-overlapping finding — the "continue" loop termination contract is SOUND but DISTRIBUTED, and three halt/continue semantics share one word

This is the finding neither primary owns. Per my FM-E discipline ("loop until success" is not a valid commitment — a loop needs explicit exit conditions), I examined `stop-loop.sh` empirically. **The termination contract is sound, not missing.** The exit set is exactly three conditions:

| # | Exit condition | Mechanism (stop-loop.sh) | C1 verdict |
|---|---|---|---|
| 1 | Loop mode off | `loop_mode_active \|\| exit 0` (inert by default) | clean |
| 2 | Genuine-gate stand-down | `.harness/.loop-halt` marker present → stop (raised ONLY when *every* forward item is deferred) | clean |
| 3 | Iteration cap | `HARNESS_LOOP_MAX` (default 25) → hard bound; raises `.loop-halt` so the cap is a real boundary | **guarantees termination** |

The iteration cap is a genuine hard bound (a counter, not `stop_hook_active` alone) — it **guarantees the loop terminates regardless of state**, which satisfies FM-E. (Correction to a tempting misread: the run-scoped *skip-set* injection is the **continuation** path — item 4, "increment + inject next-action + skip-set" — NOT an exit condition. The skip-set advances the loop *past* already-deferred items so it never re-attempts one; it is anti-livelock machinery, not termination.) **C1 terminates on `{loop-mode-off ∨ halt-marker ∨ iteration-cap=25}`** with skip-set-mediated forward advancement between turns.

The finding, exactly parallel to how C2 framed the hooks: **the termination contract is the harness's, but it is distributed across five surfaces** — `stop-loop.sh`'s header comment + CLAUDE.md §12.4.1 (no-parking directive) + the marker files (`.loop-active` / `.loop-halt` / `.loop-iter`) + the `roadmap-continue` skill + the `loop-stop` skill. A fresh context cannot read the loop's exit conditions from one place. The lifecycle-topology contract (the refined HOOKS.md) should state the termination contract once, canonically.

**The sharp sub-finding: three distinct halt/continue semantics share the vocabulary "halt"** — and a fresh context can conflate them:

| Semantic | Trigger | Meaning | Owner posture |
|---|---|---|---|
| **drift→HALT** (§12.1) | `workspace_state_hash` mismatch at session-start | **data-integrity reconcile gate** (state consistency) | C1 placement; halts work until reconciled |
| **`.loop-halt`** (stop-loop.sh) | every forward item deferred | **work-exhaustion stand-down** (forward menu empty) | C1 placement |
| **never-halt / defer-and-continue** | operator-locked posture | **anti-premature-halt posture** (advance around gates) | operator-locked — NOT relitigated here |

These read identically as "halt" but are three different control events on the timeline. **C1 recommends the lifecycle-topology contract disambiguate them by name** — a data-integrity gate, a work-exhaustion stand-down, and a progress posture are not the same control primitive. I surface this and recommend disambiguation only; I do **not** relitigate the never-halt / defer-and-continue lock (operator-locked per the arc constraints).

**Bonus hand-off-mechanics observation (clean C1 surface):** the loop creates a **context-IN event that interactive mode lacks** — at `Stop`, `stop-loop.sh` injects the next-action + skip-set, *replacing the operator's turn-boundary input*. And `loop_status.md` DEFERRED-HIL rows are a **cross-session / cross-headless-child handoff artifact**: stop-loop writes them → `session-start.sh` reads them back via `loop_pending_hil_summary` → surfaces to the operator at next engagement. That is a typed hand-off contract (write-site → read-site → operator-surface) the lifecycle-topology doc should name.

### The §3 operator hunches, tested through my lens (TEST/refine, not my frame)

- **Hunch #5 (no synthesized hook-system doc): CONFIRMED + reframed.** No `tools/hooks/README*` exists; only `wave2/wave3/wave4-hooks-status.md` (per-wave build status, not a synthesized lifecycle). The HARDENING_PLAN item (c′) independently confirms the loop skills (`loop-start`/`roadmap-continue`/`ship-pr`) carry an active durability/handoff concern. The gap is real — but it is a **lifecycle-TOPOLOGY** gap (control-transfer boundaries + termination + handoff contracts), not merely a hook list.
- **Hunch #1 (CLAUDE.md bloated): in C1's lane only via cost-amplification** — the loop re-attends the prefix up to 25× (above). The cut-list is C2's; retention is C3's. I add only the topology multiplier.
- **Hunches #2/#3/#4 (stale .md / no SSOT / no AGENTS.md): NOT my lane** — folder-architecture-as-navigation is C2's; version-chain-as-tier-confusion is C3's. I defer both. My only adjacent note: the loop's `roadmap-continue` skill *correctly* does not restate the §12 protocol (it executes it live, citing sections) — that is the right anti-stale-carry topology and should be the template, not a finding against it.

---

## Key findings (structured)

### F1 [low] The hook lifecycle is the runtime topology that EXECUTES the C-IS-07 read/write contract pair: context-IN read events (§7.2) fire at 3 control-transfer boundaries (SessionStart / UserPromptSubmit / PostCompact); state-OUT write events (§7.1) fire at 4 (PreCompact / SessionEnd / Stop / PostToolUse). C1 owns WHERE these events sit on the control-flow timeline; C2 owns what enters; C3 owns what is durably written.
- **As-is:** .claude/settings.json wires SessionStart(session-start.sh audit + loop-gc) / UserPromptSubmit(prompt-context + skill-activation-check + prompt-lint) / PostCompact(postcompact-reinject) as context-IN; PreCompact(precompact-checkpoint) / SessionEnd(session-end-cleanup) / Stop(stop-loop + stop-gate + git-arc-guard) / PostToolUse(post-merge-refresh) as state-OUT. No artifact maps these boundaries to the §7.1/§7.2 read/write events they execute.
- **Intended:** C-IS-07 §7.2 read contract (Read-into-dynamic-suffix, navigation-primitive-mediated) + §7.1 write contract (append-only, idempotent, hash-chain-preserving). Research §2.2.3 names the session startup/work-loop ritual (read claude-progress.txt → work → git commit → append progress) as the canonical control-flow that drives state in/out at session boundaries.
- **Recommendation:** PLAN should specify a lifecycle-TOPOLOGY contract (the refined HOOKS.md per C2's anchor): enumerate the 3 context-IN + 4 state-OUT control-transfer boundaries and the read/write event at each. This is the navigation anchor a fresh context lands on to read the control spine. C1 owns the boundary placement; defer what-enters to C2 and what-persists to C3.

### F2 [medium] The 'continue' loop termination contract is SOUND (the iteration cap HARNESS_LOOP_MAX=25 GUARANTEES termination, satisfying FM-E) but DISTRIBUTED across 5 surfaces; a fresh context cannot read the loop's exit conditions from one place. The exact exit set is {loop-mode-off, .loop-halt marker, iteration-cap}; the run-scoped skip-set is the CONTINUATION path, not an exit condition.
- **As-is:** stop-loop.sh exit conditions: (1) loop_mode_active||exit0; (2) .harness/.loop-halt present→stop (raised only when every forward item deferred); (3) HARNESS_LOOP_MAX(default 25) hard counter cap. Same semantics scattered across stop-loop.sh header + CLAUDE.md §12.4.1 + marker files (.loop-active/.loop-halt/.loop-iter) + roadmap-continue skill + loop-stop skill. No single canonical termination-contract statement.
- **Intended:** C1 FM-E discipline: every iterative topology has explicit max-iteration count + exit conditions stated once. Research Pattern Reference Catalog §3.priority-1 (DeerFlow task() tool with hardcoded 3-concurrent/15-min termination conditions) + §3.priority-2 (pi-mono agent loop, explicit 'no max_steps' design choice) show termination contracts are a named, single-sited topology primitive in production harnesses.
- **Recommendation:** PLAN should state the loop termination contract ONCE in the lifecycle-topology doc: C1 terminates on {loop-mode-off OR halt-marker OR iteration-cap=25} with skip-set-mediated forward advancement between turns. The contract is correct today; the fix is discoverability/synthesis (parallel to C2's 'best-disciplined asset, undocumented as a whole'), not correctness. Loop-shape/exit-conditions = C1; retry-inside-an-iteration = C9 (T-perm-3).

### F3 [medium] Three distinct halt/continue control semantics share the vocabulary 'halt' and a fresh context can conflate them: (a) §12.1 drift→HALT = data-integrity reconcile gate; (b) .loop-halt = work-exhaustion stand-down (every forward item deferred); (c) never-halt/defer-and-continue = the anti-premature-halt posture.
- **As-is:** §12.1 'Mismatch → HALT' (state-consistency reconcile) vs stop-loop.sh '.loop-halt raised ONLY when EVERY forward item is deferred' (work exhaustion) vs the operator-locked never-halt/defer-and-continue posture (§12.4.1 no-parking). All three read as 'halt' but are different control events on the timeline.
- **Intended:** C1 termination-clarity discipline — distinct control primitives must be distinctly named so placement is unambiguous. A data-integrity gate, a work-exhaustion stand-down, and a progress posture are three different topology events.
- **Recommendation:** PLAN should recommend the lifecycle-topology contract DISAMBIGUATE the three by name (integrity-reconcile gate / work-exhaustion stand-down / progress posture). SURFACE-only: do NOT relitigate never-halt or defer-and-continue (operator-locked). Verify against stop-loop.sh + §12.3 before authoring (done this session).

### F4 [low] Two handoff topologies cross the context boundary with well-specified per-topology contracts but no single surfaced handoff-lifecycle contract: PreCompact/PostCompact (thin-temporal, precompact-latest.md pointer guaranteed to survive, reinjected by postcompact-reinject.sh) and /context-save÷/context-restore (rich gstack store). Their composition lives in CLAUDE.md §12.5.3 prose, not as a contract.
- **As-is:** precompact-checkpoint.sh writes .harness/.checkpoints/precompact-latest.md SYNCHRONOUSLY; postcompact-reinject.sh reads exactly that pointer back. gstack store measured at 95 files (unbounded). .harness/.checkpoints/=0 files this session (expected: per-worktree, gitignored, no compaction fired). §12.5.3 describes the two-store asymmetry in prose.
- **Intended:** Research §2.2.3 compaction-with-durable-handoff pattern (compressed summary + 5 most-recently-accessed files form the new context). C1 owns the handoff CONTRACT (that a guaranteed-surviving payload exists + its slot); C3 owns the artifact's retention.
- **Recommendation:** PLAN should surface the two handoff topologies' COMPOSITION as a single handoff-lifecycle contract in the lifecycle-topology doc (what payload survives each boundary, who reads it back). The per-topology contracts are sound; .checkpoints/=0 is NOT a defect. Retention/ceiling of the gstack store = C3 (defer); the contract-that-handoff-exists = C1.

### F5 [low] The loop creates a context-IN event interactive mode lacks (next-action+skip-set injection at Stop REPLACES the operator's turn-boundary input) and a cross-session/cross-headless-child handoff artifact (loop_status.md DEFERRED-HIL rows: written by stop-loop → read back by session-start.sh via loop_pending_hil_summary → surfaced to operator at next engagement).
- **As-is:** stop-loop.sh item 4 injects next-action + run-scoped skip-set as decision:block (substituting the operator turn boundary); loop_lib.sh maintains .harness/loop_status.md append-only ledger; session-start.sh appends loop_pending_hil_summary to every emit so a fresh operator session sees what the last unattended run deferred. A typed write-site→read-site→operator-surface handoff with no named contract.
- **Intended:** C1 hand-off-mechanics — every cross-boundary control transfer has a typed payload + control-transfer mode. The DEFERRED-HIL ledger is a fire-and-persist handoff across session/process boundaries; its contract should be named.
- **Recommendation:** PLAN should name the loop's two non-interactive control transfers in the lifecycle-topology doc: (1) the Stop-time next-action+skip-set injection (operator-turn-boundary substitute), (2) the loop_status.md DEFERRED-HIL cross-session handoff (write→read-back→operator-surface). The genuine-gate where the loop stops to ask = HITL PLACEMENT (C1 owns where it sits); what the operator sees/can do at it = C11 (defer the primitive).

## Tensions raised

- **with C9** — The 'continue' loop's recovery/fault-handling posture — defer-and-advance on a gated item, the self-heal recovery loop, breaker-trip-routes-to-fallback — is fault-handling-as-topology (tunable parameter topology_fault_handling). This is C1's OWN permanent Layer-3 tension (T-perm-3, C1↔C9), parallel to the C2↔C3 spine, not a restatement of it.
  - **C1 position:** C1 owns the loop SHAPE: where the recovery transition sits, the exit set {loop-mode-off ∨ halt-marker ∨ iteration-cap=25}, and that a defer-and-advance branch exists. C9 owns what happens INSIDE a failed iteration — retry mechanics, backoff, breaker thresholds, idempotency. The loop's 'on a gated item: build the non-gated slice, log DEFERRED-HIL, advance' is the topology-level recovery affordance (C1); the per-attempt retry posture is C9. When the lifecycle-topology contract specifies the recovery branch, it must name C9 as owner of the in-iteration retry posture and stop at the boundary (FM-B discipline). The seam is permanent — surfaced as known-permanent, not relitigated.

- **with C2** — Engaging the C2↔C3 spine (T-perm-2) at the seam C1's lens touches: what defines a turn / session / compaction boundary. C2 owns what enters the context window; C1 owns the control-transfer boundary at which the read event fires.
  - **C1 position:** CONCUR-with-refinement, not tension on substance: C2's 'injectors correctly placed in the dynamic suffix' is right BECAUSE each fires at a control-transfer boundary (turn/session/compaction) — the correct firing site for a Read-into-dynamic-suffix event per §7.2. The one place I sharpen C2: the HOOKS.md gap is a lifecycle-TOPOLOGY contract (control-transfer boundaries + termination + handoff contracts), not a hook inventory. And additively (no absorption): the loop re-attends C2's ~334KB prefix up to 25× per run, so C2's prefix-warming fix has outsized value under loop mode. C2 owns the cut-list; I own only the topology multiplier.

## Cross-cutting notes

Reliability/failure-containment (concern 4, owner C9): the loop's recovery posture is fault-handling-as-topology (T-perm-3) — defer-and-advance affordance + self-heal recovery loop are C1's loop-shape; in-iteration retry/backoff/breaker = C9. The iteration cap (25) is the topology-level failure bound that guarantees termination; rollback primitive is git-commit (Tier-2, C3). Cost/token economy (concern 3, joint C2/C4/C6): the loop AMPLIFIES C2's prefix-cache cost — up to 25 auto-continued turns each re-attend the ~334KB static prefix, so C2's prefix-warming fix has outsized value specifically under autonomous loop mode; cut-list optimization is C2's, I surface only the topology multiplier. Observability (concern 2, owner C7): every control-transfer boundary (SessionStart/UserPromptSubmit/PostCompact/PreCompact/SessionEnd/Stop/PostToolUse) is an instrumentation point + the loop's turn-counter / DEFERRED-HIL ledger are trace-worthy events — span emission is C7's; I name the boundaries so C7 need not reverse-engineer them from prose. HITL placement (concern owner C11): the loop's genuine-gate (where it stops to ask the operator) is a HITL PLACEMENT point — C1 owns WHERE it sits on the control timeline; C11 owns what the operator sees/can do (the approve/edit/reject/respond primitive) — defer the primitive. Eval-ability (concern 8, owner C8): measurable lifecycle signals — auto-continued-turns-per-run ≤ cap; loop-termination-reason distribution (off/halt/cap); DEFERRED-HIL surfaced-at-next-session = yes/no. Security/blast-radius: n/a to lifecycle topology directly (the locked paid/secret/destructive deny-list is enforced at permission-guard.sh PreToolUse, a gate-slot C1 places + C10 enforces; not relitigated). Local-first: the entire lifecycle is filesystem+git+hook-local with no external dependency — consistent with the operator-locked posture.
