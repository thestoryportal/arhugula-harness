# C9 — consultant
**First cite (canonical, grounded first):** Spec_Information_Substrate_v1.md (v1.3) §C-IS-07 §7.2 C2-pole read contract (lines 457-464) — the discontinuity-triggered direction of which is the recovery seam I own
## C9 — Reliability & Recovery (CONSULTANT): recovery is the discontinuity-triggered direction of the C2↔C3 read seam, and slimming the prefix makes it load-bearing

### My anchor, and the one thing I am NOT claiming

My lens is the **mechanics of survival** across crash / compaction / handoff — the old D14 layer. The failure *surface* (bloat, retention, the store schema) belongs to C2/C3; I own the *how-we-get-through-it*: the rollback-snapshot primitive's trigger and timing, the graceful-degradation mode, and the re-hydration discipline. I anchor in **§C-IS-07 §7.2 (the C2-pole read contract — Selective / Bounded / Navigation-primitive-mediated / Read-into-dynamic-suffix)** because **recovery is exactly that read contract fired by discontinuity** (compaction/crash forcing C3-durable → C2-working) rather than by normal turn-flow. C3 named §7.2 the spine anchor; I claim the discontinuity-triggered half of it.

Per my C3↔C9 boundary (skill §"Co-primary scan", s12 §7.3): **C3 owns the rollback primitive, the durability tier, and the store; C9 *invokes* the primitive, emits the trip/recovery events, and decides *when*.** I defer to C3 by name: gstack keep-N retention (Tier-3 — C3 claimed it), MEMORY.md compaction policy (C3 claimed it), cross-worktree write serialization (C3's FM-H finding — claimed). I do not touch any of those; specifying them would be FM-A boundary leakage.

### Headline — a genuine sequencing tension with C2 (not concurrence)

C2's slim-down is correct and I concur on the *direction* (334KB→~40KB prefix, evict provenance, JIT-load). But C2 costed it **only for cache/attention**. From my lens there is a reliability consequence C2 did not price: **today everything lives in the re-attended-every-turn prefix — pathological for cost, but it means a recovered or compacted context is fat-by-default.** Slim the prefix and you **convert the recovery/reinject machinery from a redundant backstop into a load-bearing dependency.** After P1, "what the agent knows on turn N+1 post-compaction" is no longer guaranteed by the prefix — it is guaranteed only if PostCompact reinject + SessionStart audit + the checkpoint pointer actually fire and actually re-hydrate the right thing.

Calibrated honestly (FM-A discipline, no alarm): the *specific* content C2 evicts is **version-provenance**, which recovery rarely needs — a recovering agent needs "where am I in the arc" (= `roadmap_status.md` + the checkpoint), **not** spec change-notes. So the blast radius of the provenance-eviction *itself* is modest. The load-bearing point is the **principle**: the more you JIT, the more the recovery layer must be complete before the slim-down is safe. **C9 position: recovery-completeness (the D14 gap, below) is a CO-REQUISITE for the aggressive P1 slim-down, sequenced WITH it — not a follow-on.** This is the C9↔C2 sequencing flag that earns my seat.

### The recovery substrate — a precise three-way build/unbuilt map (lead with code, not artifact-absence)

The biggest mistake a reviewer could puncture is flattening this to "recovery wiring is owed." It is three discontinuity types at three maturities. I verified each at HEAD:

| Discontinuity | Built? | Evidence (mechanism lens) |
|---|---|---|
| **Compaction recovery** | **BUILT + sound — credit it** | `precompact-checkpoint.sh` writes the snapshot **synchronously** (self-documents the race-avoidance: "the write must finish before compaction completes or the snapshot races its own reader"); `tools/statusline/context-recovery.sh` proactively pre-saves at **60/75/85%** context-fill (the only place `context_window.used_percentage` is exposed); `postcompact-reinject.sh` re-injects the `precompact-latest.md` pointer AND tells the agent to re-run the §12.1 audit. This is a sound rollback-snapshot primitive correctly placed in the **dynamic suffix** (conforms to §7.2). |
| **Output-cap truncation recovery** | **UNBUILT — the specific gap** | `capture-failure.sh` LOGS `StopFailure` (max_output_tokens) to `session-issues.jsonl`, but **`tools/roadmap-audit/session-start.sh` reads neither the breadcrumb nor the checkpoint** (grep-confirmed: reads NEITHER) and `stop-loop.sh` continue path calls **no** `hook_write_checkpoint`. The CRUX (correct, from my lens): an output-cap hit ends the turn via **StopFailure, whose output Claude Code IGNORES** — recovery cannot come *from* StopFailure output, and no event exposes a running output-token count, so the trigger is genuinely unhookable in-flight. |
| **Crash / handoff** | **SPLIT** | roadmap-state re-derivation is BUILT (SessionStart §12.1 audit + `hook_write_checkpoint` HEAD/branch/next/dirty snapshot); rich-context restore is MANUAL (gstack `/context-restore`). |

**Evidence discipline:** `precompact-latest.md` count=0 in this worktree is a *gitignored runtime-artifact* absence, NOT evidence the write path is unbuilt — `hook_write_checkpoint` IS wired (`lib.sh:110-138`). The load-bearing evidence for the truncation-recovery gap is the **code-grep of `session-start.sh` (reads neither)**, and I lead with that.

### CITE-DON'T-DUPLICATE — the D14 gap is already canonically scoped

The output-cap recovery gap I just confirmed is **already diagnosed** in `HARDENING_PLAN.md` (MAIN checkout) as **D14**, with named units: **U-HK-30** (Piece A — `stop-loop.sh` calls `hook_write_checkpoint ... skip_gh` on the continue path, stdout-silent so it cannot corrupt the `decision:block` JSON) and **U-HK-40** (Piece B — `session-start.sh` + `prompt-context.sh` read the StopFailure breadcrumb, trigger on the DOCUMENTED `"event":"StopFailure"` not the unverified `error_type`, with a freshness window). It even names the **irreducible residual** correctly: *the in-flight-turn loss window — no hook intercepts an in-flight truncation; the only further lever is the SKILL discipline (§14.5 chunked writes).*

I do **not** re-derive "build StopFailure recovery" as a novel C9 recommendation — that would be the duplication failure mode. **C9 move: cite HARDENING_PLAN D14 / U-HK-30+40 as the canonical scoping, confirm it from the recovery-mechanism lens (the StopFailure-not-Stop crux + the irreducible loss window are correct), and the council PLAN ADOPTS that scoping and SEQUENCES it as the P1 co-requisite — it does not re-author it.** This is also a *scope-coordination* finding the council PLAN must own: the U-HK plan and this council PLAN target the **same** `tools/hooks/` machinery; the PLAN should declare which artifact is the home of record so the two don't fork.

### The under-served-brief finding — graceful degradation as context ROTS (the C9-shaped gap neither primary asked)

My brief explicitly includes recovery from a **poisoned/bloated** context, and here is the question neither C2 nor C3 posed: **does the context-budget surface degrade DETECTABLY with a recovery PATH, or silently?** The MEMORY.md "only partially loaded" state is a *silent* drop today — the loader truncates independently of any hook (proven by the current over-cap state). The cap-detection hook (`session-end-cleanup.sh:53`) + the HARDENING_PLAN's planned imperative-escalation (line 237: `>~26KB` → imperative first-action) are a degradation **trigger**. The C9-shaped gap is that there is **no defined degradation MODE** — no "context-budget-exceeded" analog to my Pillar-6 modes (`mode × trigger × scope × exit-condition × trace-event`). 

**C9 commitment:** the PLAN should define a `context_budget_exceeded` degradation mode — trigger (MEMORY.md over-cap OR CLAUDE.md over its new P4 budget), scope (the offending Tier-1 artifact), exit condition (compaction-or-`/optimize-claude-md` brings it under cap), and a surfaced signal so the rot is observable, not silent. **I own the mode/trigger/recovery-path SHAPE; I defer the retention/compaction POLICY (what to keep, when to delete) to C3.** That keeps the FM-A line clean and adds something genuinely C9 to the layer.

### The C2↔C3 seam — hold recovery to the §7.2 contract (my sharpest seam engagement)

T-perm-2 fires here as the **discontinuity-triggered** C3-durable→C2-working direction. The good news worth crediting: `postcompact-reinject.sh` **already conforms to §7.2** — it re-injects a **bounded, navigation-mediated POINTER into the dynamic suffix** (`precompact-latest.md` + roadmap next + a nudge to revalidate), NOT a fat dump into the prefix. **C9 commitment: recovery re-hydration MUST hold to the same §7.2 contract — recover by reinjecting a pointer + forcing re-grounding, NEVER by re-bloating the prefix.** This is the reliability mirror of C2's read-seam concern, and it is the discipline that prevents a "recovery" that silently re-creates the bloat C2 just removed. The auto-recovery layer being **thin-by-design and forcing re-validation** is correct (PostCompact tells the agent to re-run the §12.1 audit; the `[[feedback-checkpoint-remaining-work-is-advisory-not-authoritative]]` memory entry codifies that recovered checkpoint state is advisory, re-verify at HEAD) — credit it; do not "enrich" the auto-path into a fat restore.

### Live C3↔C9 co-primary face (partial-failure recovery)

C3 raised FM-H: the memory store lives **outside the worktree**, shared across all worktrees + main, with no write serialization → lost-update. That is C3's store + serialization to own (C3 claimed it). My **recovery-primitive** contribution to that surface: **does the out-of-worktree memory store even have a git rollback boundary?** The in-worktree durable tiers recover via git (Tier-2 = commit boundary); the `~/.claude/projects/.../memory/` store's rollback boundary is its **own** git history at the global memory path, which is a *different* repo than the worktree — so a corrupted/lost-update MEMORY.md is recoverable only if that path is itself versioned. **C9 flag for the PLAN: confirm the memory store's rollback boundary exists and name it** (recovery-primitive availability); the serialization that prevents the corruption in the first place is C3's. This is the C3↔C9 partial-failure-recovery co-primary; route the joint piece (store rollback + write serialization) to C3 as owner with C9 contributing only the recovery-primitive existence check.

### C9 PLAN commitments (decision-claim vocabulary)

| # | C9 commitment | Parameter / sequencing |
|---|---|---|
| R1 | **Recovery-completeness is a co-requisite for C2's P1 slim-down**, sequenced together — not a follow-on. The slimmer the prefix, the more graceful degradation depends on the reinject machinery firing. | gate: P1 prefix-slim lands WITH D14 Piece-A+B recovery wiring, not before it |
| R2 | **Adopt (don't re-author) HARDENING_PLAN D14 / U-HK-30 + U-HK-40** as the canonical scoping of the output-cap recovery gap; the council PLAN declares the home-of-record so the two plans don't fork on `tools/hooks/`. | cite D14; PLAN states scope-coordination with the U-HK plan |
| R3 | **Recovery re-hydration holds to §C-IS-07 §7.2** — pointer-into-dynamic-suffix + forced revalidation; NEVER re-bloat the prefix. Recovered checkpoint state is advisory (re-verify at HEAD). | discipline, not a new artifact; conforms to existing PostCompact behavior |
| R4 | **Define a `context_budget_exceeded` graceful-degradation mode** (trigger / scope / exit-condition / surfaced signal) so context-rot is observable, not silent. C9 owns the mode shape; C3 owns the compaction policy it exits via. | mode added to the PLAN's degradation surface; trigger = MEMORY.md over-cap OR CLAUDE.md over P4 budget |
| R5 | **Confirm the rollback boundary of the out-of-worktree memory store** (recovery-primitive existence check). C3 owns the write serialization that prevents the corruption; C9 confirms the recovery primitive exists. | flag for C3 co-primary; PLAN names the memory-store git boundary |

### Cross-cutting pre-checks (mandatory per skill §8)

- **#2 Observability (C7 owns schema; C9 emission declared):** the recovery layer already emits the right *signals* — PreCompact snapshot write, PostCompact reinject, capture-failure breadcrumb, statusline threshold-save markers. The `context_budget_exceeded` mode (R4) should emit a `degradation_mode_entered`-analog so the rot is on the trace, not just a hook side-effect. Schema → C7.
- **#5 Eval-ability (C8 owns discipline; C9 surface declared):** measurable recovery surfaces the PLAN can carry — *recovery-pointer-present-after-compaction rate* (target ~100%, currently BUILT), *post-truncation recovery rate* (currently 0% — the U-HK-40 gap, measurable once wired), *MEMORY.md-bytes-≤-cap* (currently failing; the degradation-mode exit signal). "We recover" is not measurable; these are.
- **#6 HITL / local-first (C11 owns specifics; C9 composition surface declared):** the entire recovery layer is local-first by construction (checkpoints to local git, no network on the hot path per `context-recovery.sh`). The gstack `/context-restore` manual handoff is the C11-owned operator-UI surface; C9 owns only the discipline that a handoff re-grounds rather than trusts stale state.
- **#1 Security / blast-radius:** n/a to recovery content; no secrets enter the checkpoint snapshot (it carries HEAD/branch/next/dirty-file-list only — verified in `hook_write_checkpoint`). The locked paid/secret/destructive deny-list is preserved; nothing in R1–R5 touches it.
- **#3 Cost:** recovery is cheap (the one slow `gh` call is hook-bounded 5s at PreCompact only; the hot-path statusline save uses `skip_gh`). The dominant cost lever is C2's, not mine; my R1 only ensures C2's saving doesn't trade away recoverability.

---

## Key findings (structured)

### F1 [high] Recovery-completeness is a CO-REQUISITE for C2's prefix slim-down, not a follow-on: slimming the always-present prefix converts the recovery/reinject machinery from a redundant backstop into a load-bearing dependency. C2 costed the slim-down only for cache/attention; the reliability consequence (graceful degradation now depends on reinject actually firing) was unpriced.
- **As-is:** Everything lives in the re-attended-every-turn ~334KB prefix → a recovered/compacted context is fat-by-default (pathological for cost, but recoverability is free). Compaction recovery IS built and sound (precompact-checkpoint.sh synchronous write + context-recovery.sh 60/75/85% pre-save + postcompact-reinject.sh pointer + re-audit nudge).
- **Intended:** C9 graceful-degradation discipline (skill Pillar 6): every degradation mode has a defined recovery path. Spec_Information_Substrate_v1.md §C-IS-07 §7.2 Read-into-dynamic-suffix — durable state re-enters context bounded + navigation-mediated, not as a prefix dump.
- **Recommendation:** R1: sequence the P1 prefix-slim WITH D14 recovery-wiring completion (Piece A+B), not before it. Calibrated: the evicted content is version-provenance, which recovery rarely needs (recovery needs roadmap_status + checkpoint, not spec change-notes) so blast radius of the eviction itself is modest — the load-bearing point is the principle (more JIT ⇒ recovery must be complete first), surfaced as a C9↔C2 sequencing flag.

### F2 [high] The output-cap truncation recovery gap is ALREADY canonically scoped in HARDENING_PLAN.md as D14 / U-HK-30 (Piece A) + U-HK-40 (Piece B), including the correct StopFailure-not-Stop crux and the irreducible in-flight-turn loss window. C9 confirms the gap from the recovery-mechanism lens; the council PLAN must ADOPT this scoping, not re-author it — and must coordinate scope because both plans target the same tools/hooks/ machinery.
- **As-is:** capture-failure.sh LOGS StopFailure(max_output_tokens) to session-issues.jsonl, but session-start.sh reads NEITHER the breadcrumb NOR the checkpoint (grep-confirmed) and stop-loop.sh continue path calls no hook_write_checkpoint. The recovery substrate (hook_write_checkpoint at lib.sh:110-138) IS wired; the recovery CONSUMPTION on the truncation path is not. HARDENING_PLAN.md lives in MAIN checkout, U-HK-scoped to tools/hooks/.
- **Intended:** HARDENING_PLAN.md §D14 + U-HK-30 + U-HK-40 (Piece A stdout-silent stop-loop checkpoint; Piece B StopFailure-triggered + freshness-windowed reinject). C9 skill: own recovery trigger/timing; the irreducible loss window routes to the §14.5 chunked-writes SKILL discipline.
- **Recommendation:** R2: cite D14/U-HK-30+40 as canonical scoping; council PLAN adopts + sequences it as the P1 co-requisite (R1) and declares the home-of-record so the U-HK plan and this council PLAN do not fork on tools/hooks/. Lead the gap evidence with the session-start.sh code-grep, NOT the gitignored precompact-latest.md count=0 (runtime-artifact absence, not unbuilt write-path).

### F3 [medium] No defined graceful-degradation MODE for context-rot — the C9-shaped question neither primary asked: does the context-budget surface degrade DETECTABLY with a recovery path, or silently? Today MEMORY.md over-cap is a SILENT loader truncation (proven by the current over-cap 'only partially loaded' state). The cap-detection hook is a degradation TRIGGER with no MODE.
- **As-is:** session-end-cleanup.sh flags MEMORY.md >24400B (advisory report); HARDENING_PLAN line 237 plans imperative escalation at >~26KB. But there is no 'context_budget_exceeded' mode (analog to C9 Pillar-6 mode×trigger×scope×exit×trace-event); the loader truncates independently of any hook (SessionStart cannot block).
- **Intended:** C9 skill Pillar 6: four canonical degradation modes, each emitting degradation_mode_entered/exited with mode/trigger/scope. A rotting context is a degradation surface that should degrade observably with a defined recovery path, not silently drop content.
- **Recommendation:** R4: PLAN defines a context_budget_exceeded degradation mode — trigger (MEMORY.md over-cap OR CLAUDE.md over its new C2-P4 budget), scope (the offending Tier-1 artifact), exit (compaction or /optimize-claude-md brings under cap), surfaced signal so rot is observable. C9 owns the mode SHAPE; the compaction/retention POLICY it exits via is C3's (defer by name).

### F4 [low] Recovery re-hydration must be held to the same §C-IS-07 §7.2 read contract as the C2↔C3 seam — pointer-into-dynamic-suffix + forced revalidation, never re-bloat the prefix. This is the reliability discipline that prevents a 'recovery' from silently re-creating the bloat C2's slim-down removed.
- **As-is:** postcompact-reinject.sh ALREADY conforms — it re-injects a bounded navigation POINTER (precompact-latest.md + roadmap next + a re-audit nudge) into the dynamic suffix, not a fat restore. The [[feedback-checkpoint-remaining-work-is-advisory-not-authoritative]] memory entry codifies that recovered state is advisory (re-verify at HEAD). The auto-recovery layer is correctly thin-by-design and forces re-grounding.
- **Intended:** Spec_Information_Substrate_v1.md §C-IS-07 §7.2: Selective / Bounded / Navigation-primitive-mediated / Read-into-dynamic-suffix; 'direct full-file cat-style reads are precluded.' Recovery is the discontinuity-triggered direction of this same seam (C3-durable → C2-working, forced by compaction/crash).
- **Recommendation:** R3: codify as a PLAN discipline — recovery recovers by reinjecting a pointer + forcing re-grounding, never by enriching the auto-path into a fat prefix restore. Credit the existing conforming behavior; do NOT 'enrich' it. Ties the recovery lens to the spine spec and to C2's read-seam concern rather than restating C9's domain.

### F5 [medium] C3↔C9 partial-failure-recovery co-primary: the out-of-worktree memory store (~/.claude/projects/.../memory/) has its rollback boundary in a DIFFERENT git repo than any worktree, so a corrupted/lost-update MEMORY.md is recoverable only if that path is itself versioned. C3 raised the FM-H write-serialization gap (lost-update across concurrent worktrees); C9 adds the recovery-primitive-existence question.
- **As-is:** In-worktree durable tiers recover via the worktree's git (Tier-2 commit boundary). The shared memory store is outside the worktree and written by all worktrees + main with no serialization (C3's FM-H). Its recovery primitive = its own git history at the global memory path, which is a distinct repo — existence/versioning not confirmed this session.
- **Intended:** C9 skill §C3↔C9 (s12 §7.3): C3 owns the rollback primitive + durability tier + store; C9 invokes the primitive and confirms its availability. Co-primary common on partial-failure-recovery (corrupted checkpoint / partial cross-worktree write).
- **Recommendation:** R5: PLAN confirms + names the memory-store rollback boundary (recovery-primitive existence check). C9 contributes ONLY the recovery-primitive existence; the write serialization that PREVENTS the corruption is C3's (claimed). Route the joint store-rollback + serialization piece to C3 as owner with C9 as recovery-primitive consultant.

## Tensions raised

- **with C2** — Sequencing of the CLAUDE.md prefix slim-down (P1: 334KB→~40KB, evict provenance, JIT-load) relative to the recovery-completeness gap. C2 costed the slim-down for cache/attention only.
  - **C9 position:** Concur with the slim-down DIRECTION, but it carries an unpriced reliability consequence: slimming the always-present prefix converts the recovery/reinject machinery from a redundant backstop into a LOAD-BEARING dependency (today a recovered/compacted context is fat-by-default precisely because everything is in the prefix). Therefore recovery-completeness — the HARDENING_PLAN D14 / U-HK-30+40 wiring — is a CO-REQUISITE for the aggressive P1 slim-down, sequenced WITH it, not a follow-on. Calibrated, not alarmist: the evicted content is version-provenance which recovery rarely needs, so the eviction's own blast radius is modest; the load-bearing point is the principle (more JIT ⇒ recovery must be complete first). This is a sequencing flag, not a disagreement on the slim-down itself.

- **with C3** — Recovery of the cross-worktree shared memory store after a lost-update/corruption (C3's FM-H). Boundary between the recovery PRIMITIVE (C9) and the write SERIALIZATION + store (C3).
  - **C9 position:** C3 correctly owns the store, the retention policy, and the write serialization that PREVENTS the lost-update — I defer all three by name and do NOT re-specify them. C9 adds only the partial-failure-RECOVERY question C3's framing didn't close: does the out-of-worktree store even have a git rollback boundary (it lives in a different repo than any worktree, so its recoverability is not the worktree's Tier-2 commit boundary)? Route the joint store-rollback + serialization piece to C3 as owner; C9 contributes the recovery-primitive existence check only. This is the standard C3↔C9 partial-failure-recovery co-primary (s12 §7.3), surfaced cleanly, not a boundary grab.

## Cross-cutting notes

#2 Observability: recovery layer already emits the right signals (PreCompact snapshot write, PostCompact reinject, capture-failure breadcrumb, statusline threshold-save markers); the R4 context_budget_exceeded mode should emit a degradation_mode_entered-analog so context-rot is on the trace not just a hook side-effect — schema owned by C7. #5 Eval-ability: measurable recovery surfaces for the PLAN — recovery-pointer-present-after-compaction rate (BUILT, ~100% target), post-truncation recovery rate (currently 0%, the U-HK-40 gap, measurable once wired), MEMORY.md-bytes-≤-cap (currently failing; the degradation-mode exit signal); methodology owned by C8. #6 HITL/local-first: recovery layer is local-first by construction (checkpoints to local git, no network on the hot path; statusline uses skip_gh); gstack /context-restore manual handoff is C11-owned operator-UI; C9 owns only the discipline that a handoff re-grounds rather than trusts stale state. #1 Security/blast-radius: n/a to recovery content — checkpoint snapshot carries HEAD/branch/next/dirty-file-list only (verified in hook_write_checkpoint), no secrets; locked paid/secret/destructive deny-list untouched by R1-R5. #3 Cost: recovery is cheap (slow gh call hook-bounded 5s at PreCompact only; hot-path statusline save uses skip_gh); the dominant cost lever is C2's prefix-slim, and R1 only ensures that saving does not trade away recoverability. HITL/local-first + never-halt/defer-and-continue/prefer-free-ollama operator-locked rules preserved; not relitigated.
