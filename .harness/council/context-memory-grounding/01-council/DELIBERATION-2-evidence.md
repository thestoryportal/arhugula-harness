# Stage E1 — Council Deliberation #2: Evidence-Enrichment of DESIGN.md (Round 5)

**Arc:** council-context-memory-grounding · **Round:** 5 (evidence-enrichment) · **Stage:** E1 (council convening) · **Date:** 2026-06-04
**Mechanism:** genuine 7-voice invocation in two sub-phases — **A1 primaries** C2/C3 deliberated independently (blind to each other) on the fresh `03-evidence/` research (`wf_07c07c77-82b`); **A2 consultants** C1/C5/C7/C8/C9 were then introduced to **react to the primaries' real output** (read both contributions, engaged them by name, answered directed seam-questions; charter §4 *formulaic-concurrence-rejected* binding) (`wf_db74601a-68d`). Each voice ran as a dedicated agent adopting its own `cN` council SKILL, read-only; orchestrator composes this envelope + writes the ledger. **Verbatim contributions:** `contributions-r5/c{2,3,1,5,7,8,9}.md`.
**This is the isolated-pass synthesis.** The seam-by-seam cross-read DEBATE (Phase B) closes the cross-cutting tensions enumerated at the end; the integration plan is *emerging*, not yet reconciled.

---

## Convening Block

- **Question type:** evidence-enrichment of a committed plan — does the fresh external/empirical research (ICM arXiv anchor; CoALA/Letta tiers; cache mechanics; temporal frontmatter; eval-gate cascade; the empirical 169-note graph) move, sharpen, or merely anchor `DESIGN.md` v1, through each domain lens?
- **Voices convened (7):** C2 (primary), C3 (co-primary), C1/C5/C7/C9 (charter consultants), **C8** (eval — promoted to first-class this round: the fresh Deep-dive 5 eval-gates + WS-0/the success gate are squarely C8's). C4/C10/C11 handled-by-reference (anti-bloat per CA-2). Roster deviation logged at `00-CHARTER.md` process-log 2026-06-04.
- **Convening spine-tension (the round's center):** **proportionality-vs-canon** — the research pushes hard toward more machinery; the arc's own discipline (CA-2 governance-bloat irony; cluster-2 §1.11) pushes back. Every enrichment must earn its place against (a) the solo-developer filter AND (b) the WS-0 drift metric (D1–D6).
- **Triage discipline (proportionality enforcement):** every fresh item classified DECISION-MOVING / CITATION-ENRICHING / CONFIRMATORY; only decision-moving items get a full proposal.

---

## Spine verdict — the convergent finding of the round

**All 7 voices resolved the spine toward PROPORTIONALITY, independently.** This is the round's strongest signal: a 7-voice council handed a research corpus that overwhelmingly recommends *more structure* returned **a near-total triage of citation-enriching / confirmatory, ~9 small MVP enrichments (all sharpens/riders of existing workstreams — ZERO new MVP workstreams beyond a single trigger-gated gate), and a long list of explicit canon-refusals on drift+solo grounds.** Each voice refused the bloat its *own* lane was most tempted by (C8 refused the eval cascade; C7 refused the rot-score dashboard; C3 refused the tier-directory split; C1 refused the consolidation daemon; C2 refused the ICM numbered-folder architecture). The research **enriches** v1 (named anchors, one new mechanism, two gate sharpenings, a base-rate correction to the gate) without inflating it — the de-bloat effort did not become governance-bloat.

---

## Per-voice one-line dispositions

| Voice | Role | Disposition (one line) |
|---|---|---|
| **C2** | primary | Proportionality wins outright; 2 WS-2a sharpens (Dive-6 do-not-invent navigation guardrail; ICM section-routing admitted MVP-*only-if-generated*), self-corrects own prior over-listing; owns demoting the cache-headline gate→leading-indicator; sets aside ICM numbered-folder architecture as CA-2 bloat. |
| **C3** | co-primary | Overwhelmingly citation/confirmatory; **one earner** = bi-temporal supersede-mark (WS-3a "drop superseded"→"supersede-mark, never silent-drop", dual-serves X-min, cuts D1+D4); **sets aside** the episodic/semantic *directory* split because its own repo-empirical degree-tiering refutes a flat category split. |
| **C5** | consultant | The link-integrity gate both primaries assumed is **not in DESIGN.md** — right call until it earns a trigger; **ONE** code-fence-aware `--check` (G-LINK) answers C2's generated-anchor + C3's cross-store + holds C3's otherwise-unvalidated `superseded_by` schema, ship-gated on whichever machine-cross-ref surface lands first; ranks G1 MVP-as-waiver'd-guardrail, G2/G3/G4 defer; cuts Dive-5 cascade to **L1 deterministic assertions only**, drops κ/TPR/TNR as judge-calibration with no judge. |
| **C7** | consultant | Honors C3's silent-never-fires gap but **refuses the WS-5 promotion** it tempts — the stale-note count rides WS-3a's *existing* SessionEnd report as a 2-integer grep rider (boundary-only: counts are slow-moving); confirms C2's edit-cadence proxy but refines the cheaper "did-it-take" signal = WS-4 G1's byte-delta; **sets aside** embedding-drift/MMD/cosine rot-scores as *contrary to* WS-0's deliberately binary/human-graded/no-judge gate (the dashboard the arc rejected). |
| **C8** | consultant | Collapses Dive-5's entire threshold tier under **one defeater** ("presupposes an automated judge WS-0 deletes by design" — TPR/TNR *undefined* not relaxed, κ moot, calibration vacuous); adopts only the judge-free residue (recall/artifact/continuation as a grading **codebook lens**, never synthetic tests); surfaces a **seat-earning original finding** — WS-0 passes *vacuously* on rare classes D4/D6 (0≤0) → "not-exercised ≠ passed"; retracts own round-3 claim (zero-observed-D4 = no coverage, not no risk). |
| **C9** | consultant | **Zero new workstreams** (correct landing); **holds the deferral** (DESIGN §6's reconciled D5-gated WS-6-6b *supersedes* the older "lands WITH" phrasing; deeper slim doesn't un-defer — reinject is §7.2-pointer-mediated, orthogonal to prefix depth); earns seat with two refinements — the T5 split is clean *iff* the defer-trigger is C7-observable, AND deeper slim opens one new dependency (**the reinject pointer must resolve** or recovery silently fails). |
| **C1** | consultant | **Zero new workstreams**; confirms HOOKS.md 3-lane split + adds C1 **do-not-author-WORKFLOWS.md** guardrail (procedures already live as JIT loop-skills — parallel to C2's do-not-invent); reframes C3's consolidation "daemon" → **synchronous SessionEnd write-event (no daemon, no C9), deferred-with-trigger on observed-D4 (≥4-ref-unwritten count, NOT episode cadence — a solo-scale category error)**; verdicts ICM Reality-Checker ALREADY-COVERED; one sharpen — **FM-H is a decentralized-handoff topology with no lead to serialize at → the fix must live at C3's store-level OCC, not the topology**. |

---

## Emerging integration plan (what folds into DESIGN.md v2 — pending Phase B reconciliation)

### A. MVP enrichments (decision-moving, earn their place against drift + solo)

| # | Enrichment | Owner | DESIGN.md target | Drift cut |
|---|---|---|---|---|
| E-1 | Navigation-set guardrail: closed list of real anchors (`ARCHITECTURE.md`✅; WORKFLOWS/HOOKS→WS-2b) + **do-NOT-invent** GC.md/ROUTING.md/CATALOG.md + (C1) **do-NOT-author** WORKFLOWS.md (procedures = JIT skills); self-corrects C2's prior AGENTS.md/WORKFLOW.md over-listing | C2 ×C1 | WS-2a | D2 |
| E-2 | ICM section-routing in the INDEX, **generated-only** (hand-authored anchors forbidden = D4 surface); MVP floor = un-anchored `artifact→version` INDEX; section-routing MVP-iff-generated else defer WS-2b | C2 | WS-2a | D2 (gate prevents D4) |
| E-3 | **Bi-temporal supersede-mark**: WS-3a "drop superseded" → "supersede-mark (`valid_until`/`superseded_by` frontmatter, archive body) — never silent-drop"; dual-serves X-min recoverability (the mark = the rollback breadcrumb) | C3 | WS-3a × X-min | D1 + D4 |
| E-4 | WS-0 grading **codebook lens**: recall/artifact/continuation probe *types* map onto the D1–D6 tally (recall→D1/D4; artifact→D2/D5; continuation→D3/D5) for inter-rater reproducibility — **NOT synthetic tests** | C8 | WS-0 §3 | hardens the instrument |
| E-5 | WS-0 **base-rate honesty rule**: a class with zero incidents in *both* arms = "not-exercised" ≠ "passed"; does not satisfy "reduces ≥1 class"; routes coverage to the named §6 deferred trigger | C8 | WS-0 §3 | protects gate integrity (D4/D6) |
| E-6 | **Stale-note count rider**: WS-3a's existing SessionEnd report (`session-end-cleanup.sh:49-53`) gains 2 grep integers (`notes-with-valid_until-set`, `notes-untouched->Ndays`) so the §6 deferred staleness-scan trigger is observable — **NOT a WS-5 promotion** | C7 ×C3 | WS-3a | D1 (trigger observability) |
| E-7 | **FM-H topology-class sharpen**: FM-H is decentralized-handoff (no lead to serialize at) → serialization MUST live at the store (C3 OCC), not the topology; forecloses a tempting wrong-fix | C1 ×C3 | X-min §5/§7 | D4 |
| E-8 | **Cache-headline demotion** (ledger/rationale only): §2 eviction measured by git-§2-edit-cadence proxy + WS-4 G1 byte-delta, **not** `cache_read` (no dev-loop home); no resurrection of `cache_read` as a signal | C2 ×C7 | WS-1 rationale | (corrects a measurement claim) |

### B. Trigger-gated / DEFER (named triggers — the proportionality discipline)

| Item | Owner | Trigger to promote |
|---|---|---|
| **G-LINK** link-integrity `--check` (code-fence-aware; `[[wiki]]` + `[md](slug)` + cross-store kebab/underscore + generated `file#header`) | C5 | **iff** C2-E2 generated section-routing OR C3 supersede-mark ships (either introduces machine-managed cross-refs the gate must hold). Absent both, the 3 bucket-C breaks are one-time hygiene. |
| **L1 deterministic assertion** (the `[i]` citation format still emits post-evict; a recall-regression assertion) folded into WS-1 verify-before-evict as its deterministic half; κ/TPR/TNR/L2-judge/L3 dropped/deferred | C5 ×C8 ×C2 | MVP-admissible *because* judge-free; lands at the gate or the WS-1 precondition (Phase-B seam) |
| **Consolidation pass** = synchronous SessionEnd write-event (no daemon, no C9) | C1 ×C3 | observed-D4: the count of ≥4-ref-unwritten patterns > 0 (NOT episode cadence) |
| **WS-6-6b recovery build** (unchanged) | C9 ×C3 | D5 (bad-resumption) drift after WS-1 — deferral HELD; deeper slim doesn't un-defer |
| **Reinject-pointer resolvability** dependency | C9 | rides G-LINK / G1 (the pointer target must resolve, or recovery silent-fails) |
| **G2 / G3 / G4** | C5 | G2 (clearance-schema) HARD-when-built / no observed D2-via-marker; G3 (ledger-shape) clearest defer; G4 (freshness-teeth) highest promotion-likelihood (D5, HITL-recoverable, co-triggers WS-6-6b) |

### C. Set asides (canon refused on drift + solo grounds — the anti-bloat record)

- Episodic/semantic **directory** split (C3: repo-empirical degree-tiering refutes a flat category split; cluster-2 §1.11).
- ICM **numbered-folder / CONTEXT.md architecture** (C2/C3: CA-2 bloat-irony; only the CONTEXT.md Inputs/Process/Outputs *shape* is taken for the deferred HOOKS.md format).
- Dive-5 **threshold tier** TPR/TNR/κ/L2-LLM-judge/100-trace-calibration (C8: presupposes a judge WS-0 deletes by design).
- BROAD-B **embedding-drift / MMD / cosine rot-scores** (C7: contrary to WS-0's binary/human-graded/no-judge gate — the dashboard the arc rejected).
- ICM **Reality-Checker meta-pass** (C1: already-covered by §12.1 SessionStart integrity gate + §10.9 pre-merge adversarial pass).
- Consolidation **as a daemon** + the **50–200-episode cadence** (C1: solo-scale category error → reframed to synchronous-at-SessionEnd, observed-D4 trigger).
- `cache_read > 0.60` as a **dev-loop measurable** (C2/C7: no dev-loop home — H_T product OTel only).
- **WS-5 promotion** (C7: the stale-note count rides the existing SessionEnd report; mid-session surface + dashboard stay deferred).

### D. Citation-enriching (fold as one-line cites; no workstream)

ICM = **arXiv:2603.16021v2** (the CHARTER's named methodology, now citable) · CoALA working/episodic/semantic/procedural + Letta Main/Recall/Archival (anchor KEEP-HOT/LINKED/ARCHIVE) · OCC / Cursor lock→OCC pivot / **git-as-state** (anchors X-min's deferral direction + names the rollback boundary) · Dive-6 size norms + format rules (MD+XML; volatile-below-breakpoint) · OpenAI 1-min-build-gate (anchors advisory-caps-fail → G1-hard) · Devin SWE-grep 60%-first-turn-JIT (validates evict→navigate) · Dive-2 cache mechanics (confirms the §2 cache-detonation).

---

## Cross-cutting seams for Phase B (the cross-read DEBATE must close these)

1. **The unobservable-trigger seam — CONVERGENT (C3, C7, C9, C1 independently).** Every §6 deferred trigger (staleness-scan D1; consolidation D4; D5-resumption) is silent-never-fires unless a health surface carries its count. C7 proposes a *single periodic memory-health line on the existing SessionEnd report* (stale-note count + ≥4-ref-unwritten count + orphan% + true-break count); C8 partially relieves (the D1-scan trigger is already readable off Arm B's D1 column at the next WS-0 run). **DEBATE:** does C7's single SessionEnd line + C8's periodic-WS-0-read fully close the gap at MVP without promoting WS-5? Ownership: C7 surfaces / C3+C8 thresholds / C9+C1 cadence. *(Lead: C7; reactors C3, C1, C9, C8.)*
2. **The unified gate seam (G-LINK + L1 + reinject-pointer).** C5's ONE link-integrity `--check` is proposed as the gate over C2's generated anchors + C3's `superseded_by` schema + the bucket-C breaks; C8's L1 deterministic recall-regression assertion is judge-free-admissible; C9's reinject-pointer-resolvability lands on the same gate. **DEBATE:** is G-LINK MVP-on-trigger (ship-coupled to the first machine-cross-ref surface)? Does C2 accept the generator↔gate ship-coupling or prefer the un-anchored INDEX floor? Does C3 bind supersede-mark to G-LINK (MVP-with) or accept the unvalidated-schema risk (MVP-without)? Does the L1 assertion live at the gate (C5) or the WS-1 verify-before-evict precondition (C2)? *(Lead: C5; reactors C2, C3, C8, C9.)*
3. **Primary confirm-back.** Do C2 + C3 accept the consultant refinements landing in their lanes — the ship-gate coupling, the supersede-mark↔G-LINK binding, the consolidation firing-site (SessionEnd, C1-fires/C3-fills), the FM-H store-level-OCC sharpen — or flag a conflict? *(Lead: C2 + C3; reactors C5, C1.)*

## Residuals routed to the adversarial reviewer (E2)

- **C8's base-rate gap (the soft spot C8 itself flagged):** is "annotate not-exercised ≠ passed" sufficient, or does the WS-0 gate need a **minimum-exposure floor** (≥1 session per workflow class known to historically exercise D4/D6)? C8 chose annotate-not-floor to avoid bloat; red-team whether that under-protects the catastrophic-rare classes.
- **Anti-bloat self-application:** does the v2 integration (8 MVP sharpens + 1 trigger-gated gate) itself stay proportionate, or does the aggregate re-introduce governance-bloat (CA-2)?
- **The single-health-line proposal:** does loading one more periodic line onto the SessionEnd report risk re-creating the WS-5 the arc deferred?

---

---

## Phase B — RECONCILED (seam-routed cross-read closure)

The seam-routed cross-read (`wf_8fb2e418-cfe`) closed E1 to **internal-zero**: 2 seam closures (B1 C7 unobservable-trigger; B2 C5 unified-gate) + 2 primary confirm-backs (C2, C3) — **every confirm-back ACCEPT or ACCEPT-WITH-REFINEMENT; zero CONFLICT.** The cross-read *tightened* the plan (the isolated "8 MVP + 1 gate" refined to a leaner MVP + a deferred trigger-gated gate). Verbatim: `crossread-r5/{b1-c7-unobservable-trigger,b2-c5-unified-gate,confirm-c2,confirm-c3}.md`.

### Finalized integration plan (DESIGN.md v2 — reconciled; pending E2 adversarial)

**MVP (folds into DESIGN.md §4–§5):**
1. **WS-2a navigation guardrail** (C2-E1 ×C1): closed anchor list (`ARCHITECTURE.md`✅; WORKFLOWS/HOOKS→WS-2b) + **do-NOT-invent** GC/ROUTING/CATALOG + **do-NOT-author** WORKFLOWS.md (procedures = JIT skills; a static one would be eager `@import`-able prefix); self-corrects C2's prior over-listing. **WS-2a MVP floor = un-anchored `artifact→version` INDEX** (C2 confirm: defers the generator + G-LINK). Cuts D2.
2. **WS-3a supersede-mark** (C3): "drop superseded" → "supersede-mark (`valid_until`/`superseded_by` frontmatter, archive body) — never silent-drop." Ships **MVP-without G-LINK but NAMES G-LINK as a trigger**. Tier-1 frontmatter (no hash-chain). Dual-serves X-min. Cuts D1+D4.
3. **WS-3a 3-integer health-line rider** (C7 ×C3 ×C1): the *existing* SessionEnd report (`session-end-cleanup.sh:49-53`) gains `notes-superseded` + `notes-untouched->Ndays` + `patterns-unwritten->=4-refs`. Boundary-only, grep-derived. Un-blocks the §6 deferred staleness-scan + consolidation triggers. **NOT a WS-5 promotion.** Cuts D1+D4 observability.
4. **WS-0 codebook-lens** (C8-E1): recall/artifact/continuation as a grading lens over real sessions (NOT synthetic tests), mapped onto D1–D6. Hardens inter-rater reproducibility.
5. **WS-0 base-rate honesty rule** (C8-E2): zero-incidents-in-both-arms = "not-exercised" ≠ "passed"; routes coverage to the deferred trigger. Protects gate integrity (D4/D6).
6. **WS-1 L1 deterministic assertion** (C5×C8×C2): the `[i]` citation format still emits post-evict + WS-1-step-(a) invariants still present — folded into WS-1 verify-before-evict as its deterministic ex-ante half (C8's recall-lens = the ex-post human half). Judge-free. MVP.
7. **X-min FM-H sharpen** (C1×C3): FM-H is **decentralized-handoff** (no lead to serialize at) → serialization at **store-level OCC + git-as-state** rollback boundary; corrects the false `CLAUDE.md §12.5.1` git-history claim. Cuts D4.
8. **WS-1/WS-4 cache-headline demotion + G1 byte-delta legibility** (C2-E3 ×C7): §2 eviction measured by git-§2-edit-cadence proxy (ongoing) + G1 byte-delta (did-it-take), **not** `cache_read`; byte-count stays a leading indicator, WS-0 stays the gate.

**Trigger-gated / DEFER (the proportionality discipline):**
- **G-LINK** link-integrity `--check` → §6, **DEFER→MVP-on-trigger** ({generated section-routing ships} OR {supersede machine-emitted} OR {observed D4-via-dangling-ref}); validates `[[wiki]]` + `[md](slug)` + cross-store kebab/underscore + generated `file#header` + reinject-pointer target; composes with never-halt (CI/PR-time only).
- **Consolidation pass** → §6, **synchronous SessionEnd write-event** (C1 fires / C3 fills the Tier-4 semantic write), trigger = observed-D4 (≥4-ref-unwritten count), NOT episode cadence. No daemon, no C9.
- **WS-6-6b recovery** → §6 unchanged, D5-gated (deferral HELD; deeper slim doesn't un-defer; reinject is §7.2-pointer-mediated, orthogonal to prefix depth).
- **G2/G3/G4 + WS-5** → DEFER (named triggers per E1).

**Set-asides (canon refused on drift+solo grounds):** episodic/semantic dir-split; ICM numbered-folder/CONTEXT.md architecture; Dive-5 threshold tier (TPR/TNR/κ/L2-judge/L3); embedding-drift/MMD/cosine rot-scores; ICM Reality-Checker (already-covered §12.1+§10.9); consolidation-as-daemon + 50–200-episode cadence; `cache_read` measurable; WS-5 promotion.

**Citation-enriching (one-line cites; no WS):** ICM=arXiv:2603.16021v2 · CoALA/Letta tiers · OCC/Cursor/git-as-state · Dive-6 size/format norms · OpenAI 1-min-gate · Devin SWE-grep · Dive-2 cache mechanics.

### Residuals routed to E2 (adversarial #1) — genuinely open
1. **C8 base-rate floor (sharpest):** is "annotate not-exercised ≠ passed" sufficient, or does WS-0 need a **minimum-exposure floor** for the rare classes D4/D6? (C8's own flag; C2's slim is the exposed surface.)
2. **Dormant-G-LINK reinject-pointer gap (C5×C3×C9):** at bare MVP G-LINK is dormant, but C9's reinject pointer + C3's `superseded_by` links can dangle (the cross-store kebab/underscore drift is *live at HEAD*). Is "dormant + `postcompact-reinject.sh` self-check" sufficient, or does the **standing** reinject pointer (unlike episodic hand-authored marks) warrant pulling a minimal G-LINK to MVP-now?
3. **Anti-bloat self-application:** does the v2 aggregate (8 MVP sharpens + the health-line rider) stay proportionate, or re-introduce CA-2 governance-bloat? Does one more SessionEnd line risk re-creating the WS-5 the arc deferred?
4. **C7↔C11 sequencing (low-stakes):** when the deferred WS-5 dashboard eventually lands, the SessionEnd report stays the counts' permanent home (the dashboard *adds* a render, does not replace boundary emission).

---

*Stage E1 COMPLETE and reconciled-to-internal-zero (A1 primaries → A2 consultants → Phase B cross-read + confirm-back). The 4 residuals above are the genuine red-team surface. **Next: E2 — adversarial #1** (`harness-adversarial-reviewer`, genuinely invoked) red-teams this reconciled deliberation + integration plan → `02-adversarial/`.*
