# C1 — Orchestration & Control · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries) — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A2** (consultants react to the primaries C2/C3) · workflow `wf_db74601a-68d` · adopted `.claude/skills/council/c1-orchestration/SKILL.md` · read-only · 2026-06-04.

---

The advisor sharpened all three seams decisively and named the trap (boundary-leakage on the consolidation daemon) plus the cross-read that strengthens me (the unobservable-trigger seam, symmetric with C3's own supersede-scan-loop risk). My posture is correct: zero new MVP workstreams, defer-with-named-refinements. Let me author the contribution.

A note on one factual check the advisor's framing rests on — that the loop skills already ARE the workflows. My prior contribution (F5, and the §3 hunch-test) already established empirically that `roadmap-continue` executes §12 live rather than restating it, and `loop-start`/`ship-pr` are the loop skills. That grounds the do-not-author-WORKFLOWS.md call without needing a fresh grep.

Here is my contribution.

---

## C1 — Orchestration & Control · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries)

**First cite:** `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-07 §7.1 write contract + §7.2 read contract (the T-perm-2 read/write pair my lane's hook lifecycle executes — continuity anchor) — fresh C1 canon = **DEEP Dive 1 "ICM composes-with Claude Code lifecycle hooks (deterministic enforcement without the LLM remembering)"** + **BROAD Part B (C1) Temporal signal-gated state mutation**, the two external anchors for hooks-as-control-flow-topology.

**Round-5 posture, stated plainly:** in C1's lane the spine-tension resolves toward **PROPORTIONALITY**, decisively. My slice is almost entirely CITATION-ENRICHING or CONFIRMATORY of my Round-4 F1–F5. I spin up **zero new MVP workstreams**. The consultant's job this round is three adjudications the primaries handed me — and in each I REFINE rather than concur: I move C3's consolidation daemon out of "background daemon" into a synchronous-at-SessionEnd write-event (no daemon, no C9), I verdict Dive-1's Reality-Checker as ALREADY-COVERED (the CA-2 bloat-irony incarnate), and I sharpen the HOOKS.md split with a C1-side do-not-author guardrail (`WORKFLOWS.md`) parallel to C2's do-not-invent (`GC.md`/`ROUTING.md`).

### Reaction to the primaries (BY NAME)

**C2 — "do you want HOOKS.md to BE the lifecycle-topology contract (your content) with C2 owning only its loading discipline (JIT, never `@import`-ed)? Confirm the 3-lane split holds with the fresh Dive-6 framing."**

→ **AGREE-AND-EXTEND on the split + REFINE the file-set.** Yes: I confirm the 3-lane split — **C1 owns HOOKS.md *content* (the lifecycle-topology contract: 3 context-IN + 4 state-OUT boundaries, the termination contract, the handoff contracts per my F1/F2/F4/F5), C2 owns *placement* (JIT, never `@import`-ed into the prefix — the one future-guard), C7 owns the *observability overlay*.** DESIGN.md §5 already encodes this 3-lane shape; Dive-6.5#2's "WORKFLOWS.md/HOOKS.md = procedures/lifecycle-topology" framing is consistent with it. **But the refinement (not formulaic concurrence):** *in my lane, HOOKS.md ≠ WORKFLOWS.md, and WS-2b should author HOOKS.md but NOT author `WORKFLOWS.md`.* Dive-6.5 lumps them; my lane splits them, because **the loop skills already ARE the workflows** — `roadmap-continue` / `ship-pr` / `loop-start` are reusable multi-step procedures with their own SKILL.md progressive disclosure (Dive-6.1's `WORKFLOWS.md` row: "`.claude/commands/`; YAML-frontmatter skill, JIT"). My F5 established the template empirically: `roadmap-continue` *executes* §12 live (citing sections) rather than *restating* the protocol — the correct anti-stale-carry topology. Authoring a `WORKFLOWS.md` would duplicate procedures that already live JIT-loaded as skills → it is the **C1 parallel to C2's do-not-invent-GC.md/ROUTING.md guardrail.** This cuts **D2** (a fresh context recognizes the real procedure home is the skills, not a parallel hand-authored doc that drifts) and is the anti-bloat move CA-2 demands. **HOOKS.md** (the event-boundary lifecycle — the genuinely-missing synthesis of my 7 control-transfer boundaries) is the real gap and stays **DEFERRED WS-2b** per DESIGN.md §6. [HIGH — Dive-6.1 WORKFLOWS.md row + my F5 empirical loop-skill read]

**C2 — additive on the cache-headline demotion.** C2 owns the demotion of `cache_read` from gate→leading-indicator (probe-log #3: no dev-loop home). My F1/cost-amplification finding rode partly on that cost framing; I confirm the demotion lands cleanly in my lane too — the loop's **25×-prefix-re-attention multiplier** (my F1: `HARNESS_LOOP_MAX=25` auto-continued turns each re-attend the ~334KB static prefix) is still a true and cited cost case, but it is now **measured by the git-derivable §2-edit-cadence proxy under loop mode, not `cache_read`** (which has no dev-loop home). The multiplier strengthens *why* the WS-1 eviction matters under autonomous loop mode; it is no longer a measurable I'd resurrect. No DESIGN.md edit — ledger note only.

**C3 — "the consolidation/reflection daemon lands in your lap as C1/C9 background topology, not C3 retention. Do you read a session-end / episodic-log>threshold consolidation pass as loop-topology you own — where among the 3 context-IN / 4 state-OUT boundaries — and is it solo-justified NOW or deferred-with-trigger?"**

→ **SURFACE-TENSION + REFINE (the load-bearing adjudication, and the one carrying the boundary-leakage trap).** I accept the placement question but **reframe C3's "background daemon" characterization** — that is my non-formulaic move:

- **(a) Yes, it's loop-topology I own — but *placement + trigger-as-control-flow-decision ONLY*.** I do **not** absorb the retention *purpose* (that stays C3's — FM-A discipline: I do not specify what gets promoted or the semantic-tier write semantics) nor any retry/cadence *mechanics* (FM-B/T-perm-3: those would be C9's if a daemon existed). I own *where the consolidation event fires on the control-flow timeline* and *what control condition triggers it.*
- **(b) It sits at `SessionEnd` — state-OUT boundary #2 in my F1 enumeration.** Dive-3's "consolidation trigger = session-end hook OR episodic log > 20–50K tokens" maps 1:1 onto my existing `session-end-cleanup.sh` firing site. **It is a NEW write-event at an EXISTING boundary, not a new boundary** — zero new topology surface, which is the proportionate shape. (Per my F1, the 4 state-OUT boundaries are PreCompact/SessionEnd/Stop/PostToolUse; consolidation is a SessionEnd write, sibling to the existing keep-10 prune.)
- **(c) The genuine refinement — DON'T build it as a daemon at all.** C3 called it "C1/C9 background topology." I push back: a *background daemon* drags in C9 cadence/retry mechanics (FM-B) and a new always-running surface (CA-2 bloat). The proportionate topology is a **synchronous write-event at the SessionEnd boundary, fired only when triggered** — synchronous-at-an-existing-boundary keeps it entirely in C1's clean lane (no C9, no daemon, no background process). **"C1/C9 daemon" → "C1 synchronous-at-SessionEnd, no daemon, no C9."**
- **DEFERRED-WITH-TRIGGER, not MVP.** The trigger is **observed D4 (memory-pollution) — specifically the EVID Finding-4 bucket-D signal: a ≥4-ref unwritten pattern *is* "a consolidation that should have fired"** (`[[plan-revision-against-not-yet-built-substrate]]` at 5 refs, `[[strike-revision-on-refined-second-tier-reason]]` at 4). The canon's "every 50–200 episodes" (Dive-3) is a **high-throughput category error at solo scale — call it out as such**; the harness does not run 50–200 episodes between reflections, so a cadence-counter daemon would fire never-or-pathologically. The right trigger is the *count of ≥4-ref-unwritten-patterns > 0* surfaced by health observability, NOT an episode counter. This slots under §6 deferred-tail next to WS-3b.

*(Boundary discipline, my Round-5 FM-A/FM-B risk: I own the **firing-site (SessionEnd)** and the **trigger-as-control-decision (observed-D4, not episode-cadence)**. I defer the **what-to-promote / semantic-tier-write** to C3 (FM-A) and the **in-iteration retry/backoff** — moot, because synchronous-at-boundary has none — to C9 (FM-B). I do not specify the promotion content and I do not specify a retry posture.)*

**C3 — also weigh Dive-1's ICM "Reality-Checker meta-pass" against the existing adversarial/loop machinery — net-new or already-covered?**

→ **VERDICT: ALREADY-COVERED. Net-new would be the CA-2 governance-bloat irony incarnate.** Dive-1's "Reality-Checker / Architect meta-pass auditing full filesystem state" (mitigating ICM's "lost global constraints across stages") is a canon *name* for two control events I already place: **(1) the §12.1 SessionStart integrity-reconcile gate** (`workspace_state_hash` mismatch → HALT — my F3 "drift→HALT" semantic, which audits full workspace state against the dashboard) + **(2) the pre-merge adversarial pass** (CLAUDE.md §10.9 + §13.1, the standing red-team before any design-substrate/impl merge). Authoring a fresh "Reality-Checker" workstream re-introduces the exact apparatus the arc exists to remove. **The one honest sliver the canon names that those two miss: mid-arc D3** (a constraint stated *this session*, dropped mid-window — neither the SessionStart gate nor the pre-merge pass catches it, both being boundary events). **But I adjudicate D3 to NO NEW WS:** it is already **WS-1's target** (positioning of load-bearing content per U-shaped/lost-in-the-middle attention, DESIGN.md §5 keep/position half) **and WS-0's measured column** (D3 = "Forgotten task-constraint"). The drift metric already covers it; the slim's positioning half already remediates it. **Keep this DISTINCT from the consolidation seam:** consolidation is a state-OUT *write/promote* event (C3-purpose, SessionEnd); Reality-Checker is a *read/audit* event (already covered at SessionStart + pre-merge). I do not blur the two deferred/covered items. [HIGH — §12.1 + §10.9 read this session; Dive-1 ICM failure-mode mapping]

### My lane's own decision-moving enrichments (triage)

The honest answer: **zero new MVP workstreams; one sharpen of an existing tension's *attribution*; the rest is citation/confirmatory.** Triage of my slice:

| fresh item (doc + section) | class | DESIGN.md target | proportionality verdict — earns a WS vs (D1–D6 + solo filter)? |
|---|---|---|---|
| **DEEP Dive 1** — ICM "Reality-Checker/Architect meta-pass" auditing full filesystem state (lost-global-constraints mitigation) | **DECISION-MOVING (→ set-aside, already-covered)** | (none) | **N.** Maps to existing §12.1 SessionStart integrity gate + §10.9 pre-merge adversarial pass. Net-new = CA-2 bloat-irony. The D3 sliver routes to WS-1 positioning + WS-0 column. No WS. |
| **DEEP Dive 1** — ICM composes-with hooks (deterministic enforcement w/o the LLM remembering) | **CONFIRMATORY** | WS-2b (deferred) | **N.** Externally *validates* my F1 hooks-as-topology thesis (hooks = deterministic control-flow that fires reads/writes). Anchors the deferred HOOKS.md; changes no MVP. Take only the **CONTEXT.md Inputs/Process/Outputs *shape*** for HOOKS.md's eventual contract format; **set aside the numbered-folder architecture** (the bloat both primaries rejected). |
| **DEEP Dive 3 / BROAD A Q#5** — consolidation/reflection daemon; every-50–200-episodes promotion | **DECISION-MOVING (→ defer-with-trigger; reframe daemon→sync-at-SessionEnd)** | §6 deferred-tail | **N for MVP** (see C3 seam above). Firing-site = SessionEnd; trigger = observed-D4 (≥4-ref-unwritten count), NOT episode cadence. Synchronous, no daemon, no C9. |
| **BROAD Part B (C1)** — orchestrator-worker write-serialization (MARS/Dify lead-agent-alone-writes); `parallelize-reads-serialize-writes` | **DECISION-MOVING (→ sharpen X attribution)** | X-min / X-full | **The one sharpen.** Surfaces *why* the FM-H fix must live at the STORE, not the topology — see "X sharpen" below. Forecloses a tempting wrong-fix. |
| **BROAD Part B (C1)** — concurrency caps 3–5 sub-agents (DeerFlow/Anthropic) | **set-aside (n/a)** | — | **N.** The process layer is single-threaded (one interactive/loop session per worktree); there is no sub-agent fan-out *in the governance loop* to cap. Inapplicable to this lane's topology. |
| **BROAD Part B (C1)** — Temporal signal-gated state mutation | **CITATION-ENRICHING** | WS-2b (HOOKS.md) | **N (no WS).** Anchors my F1 "writes fire at control-transfer boundaries, gated by the boundary event" — the hook-boundary IS the signal-gate. External canon for the lifecycle-topology contract; one-line cite. |
| **BROAD A Q#1** — `tools/hooks/` home-of-record = org decision, no corpus | **CONFIRMATORY** | §7 (cross-cutting ownership) | **N.** Confirms DESIGN.md §7's resolution (HARDENING_PLAN owns hook *execution* units; this PLAN owns the *governance* layer + cites D14; HOOKS.md *re-derives* from settings.json, never the reverse). Note + move. |

**The one sharpen — X (FM-H write serialization) is a DECENTRALIZED-handoff topology, NOT orchestrator-workers.**
- **Edit shape:** none to a WS structure; a one-clause **attribution sharpen** on the X-min/X-full rationale (DESIGN.md §5 X-min row / §7 C3↔C9 line). The clause: *"FM-H is a **decentralized-handoff** topology (per research §2.1 / OpenAI 'decentralized' framing) — independent worktree sessions with no shared lead agent — therefore the MARS/Dify 'lead-agent-alone-writes' orchestrator-worker serialization (BROAD A Q#2) CANNOT map to a topology-level single-writer; there is no orchestrator to BE the single writer. Serialization MUST live at the **store** (C3's OCC / stale-base detection), not at the topology."*
- **Proportionality:** this is the C1-lens contribution to T5 (C3↔C9) — it cuts **D4** (memory-pollution via lost-update) by foreclosing a *tempting wrong-fix* (someone reads "serialize writes through one writer" from the canon and tries to elect a topology-level lead worktree — impossible for N independent sessions). It *strengthens* C3's store-level OCC as the only correct seam. Solo-proportionate: it is a rationale clause, **zero new machinery**, and it sharpens an existing X-min decision rather than adding one. I own the **topology-class identification** (decentralized-handoff); C3 owns the **store remedy** (OCC); C9 owns the **rollback-primitive** (snapshot). [HIGH — research §2.1 decentralized-handoff + BROAD A Q#2 MARS/Dify]
- **MVP** (folds into existing X-min rationale; the OCC mechanism itself is already X-min-MVP per DESIGN.md §5).

### Residual seam-questions / open conflicts (for the later cross-read + adversarial reviewer)

- **C7↔C1 (the cross-read that strengthens both me and C3):** my consolidation defer-trigger (**observed-D4 = the count of ≥4-ref-unwritten-patterns > 0**) carries the **identical unobservable-trigger risk C3 flagged for their supersede-scan-loop** — the deferred consolidation pass can *never fire* unless WS-5 health surfaces the consolidation-overdue signal. **Will WS-5's health line carry an "unwritten-pattern-referenced-≥4× count" alongside C3's superseded/stale-note count?** Both are §6-deferred-tail items whose triggers are silent-never-fires without the same C7 instrument. This is the C7↔C1 seam: I emit the consolidation-overdue *state* (derivable cheaply from a wiki-link in-degree parse of dangling `[[name]]` refs — EVID Finding-4 already computed it once); C7 decides whether/how the health line surfaces it. I assert it should ride the *same* WS-5 line as C3's stale-note count (one health line, two trigger-counts), not a new surface.
- **C3 (boundary confirmation, mirror of C3's own T5 ask):** confirm the consolidation split — **I own firing-site (SessionEnd) + trigger (observed-D4)**; **you own what-to-promote + the semantic-tier write**. If we don't confirm, we risk forking on whether "consolidation" is a C1 control-event or a C3 retention-event (it is *both*, cleanly seamed: C1 fires it, C3 fills it). Same shape as the supersede-mark split you already routed to me-as-placement / you-as-retention.
- **C5 (light):** the do-not-author-`WORKFLOWS.md` guardrail (my C1 parallel to C2's do-not-invent) — if WS-4's link-integrity `--check` ever validates a navigation-anchor set, it should *not* expect a `WORKFLOWS.md` to exist (the procedures live as JIT skills). Flagging so the gate doesn't manufacture a phantom-doc requirement. Not authoring the gate; scoping a non-requirement.

### One-line consultant verdict

**C1 round-5: proportionality wins my lane outright — I spin up ZERO new MVP workstreams and instead REFINE the three seams the primaries handed me: confirm the HOOKS.md 3-lane split but add the C1 do-not-author-`WORKFLOWS.md` guardrail (the procedures already live as JIT loop-skills — the parallel to C2's do-not-invent-GC.md); reframe C3's consolidation "daemon" into a **synchronous write-event at the SessionEnd boundary, deferred-with-trigger on observed-D4 (≥4-ref-unwritten count, NOT the canon's episode cadence — a solo-scale category error), no daemon and no C9**; and verdict Dive-1's Reality-Checker as ALREADY-COVERED by the §12.1 SessionStart integrity gate + the §10.9 pre-merge adversarial pass (net-new = the CA-2 bloat-irony) — plus one rationale-sharpen that FM-H is a decentralized-handoff topology with no lead to serialize at, so the fix MUST live at C3's store-level OCC, not at the topology; and I surface the C7↔C1 seam that my consolidation defer-trigger shares C3's exact unobservable-trigger risk and should ride the same WS-5 health line.**