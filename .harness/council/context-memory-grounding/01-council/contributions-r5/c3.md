# C3 — State, Memory & Persistence (CO-PRIMARY) · Round 5 evidence-enrichment — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A1** (primaries, independent / blind to each other) · workflow `wf_07c07c77-82b` · adopted `.claude/skills/council/c3-state-memory-persistence/SKILL.md` · read-only · 2026-06-04.

---

The advisor confirms my shape and sharpens the calls. I diverge on one point worth noting: the advisor lands episodic/semantic split as SET-ASIDE on EVID Finding 3 grounds — I agree, and I'll add my own repo-empirical reinforcement (my prior c3.md already mapped Tier-4 as absent + flat-file semantic store). The single decision-moving item is bi-temporal supersede-mark folded into WS-3a, dual-serving with X-min. Everything else is citation/confirmatory. Let me write.

## C3 — State, Memory & Persistence (CO-PRIMARY) · Round 5 evidence-enrichment (PRIMARY)

**First cite:** `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-02 five-tier artifact-layering + §C-IS-07 read/write contract pair (T-perm-2 resolution); C3 skill §"Pruning and GC" (FM-G) + §"Concurrent-write contract" (FM-H); methodology anchor `research/agent-harness-eng-research-cluster-2-context-prompts-memory.md` §2.3 (State Ledger pattern; "ledger bloat … requires a real policy").

I hold the spine-tension explicitly: the DEEP dives package ICM + Letta-tiers + bi-temporal + consolidation-daemon + eval-cascade as ONE "composed picture." That bundle framing **is** the CA-2 governance-native-bloat gravity my prior contribution already named. I adjudicate each piece standalone, do **not** inherit the composition, and run every item through one discriminator: *would it change what the WS-0 human grader tallies in the D1–D6 matrix?* If it only renames, only anchors, or only validates an existing call, it is not decision-moving and earns no workstream.

### Triage (every fresh item touching my lane — WS-3, X-min)

| fresh item (doc + section) | class | DESIGN.md target | proportionality verdict — earns place vs (drift metric + solo filter)? |
|---|---|---|---|
| CoALA working/episodic/semantic/procedural vocab (BROAD A Q#5 + C#1) | CITATION-ENRICHING | WS-3a | **N** — vocab is a one-line cite; it renames, tallies no D-class. My SKILL §4.2 already maps CoALA tiers; this anchors that mapping, nothing more. |
| Letta/MemGPT Main/Recall/Archival tier names (DEEP 3; BROAD A Q#5) | CITATION-ENRICHING | WS-3a | **N** — names for KEEP-HOT/KEEP-LINKED/ARCHIVE the EVID doc already counted. Cite; no structure. |
| **Episodic/semantic *directory* split (DEEP 3 missing-piece #1; BROAD A Q#5)** | DECISION-MOVING (→ **set-aside**) | WS-3a | **N — sharpest call.** EVID Finding 3 ("tier by DEGREE, not prefix") is my *own repo's* empirical data **contradicting** the canon's category-split: `fork-*` is 25/28 load-bearing, `pr-*` splits ~50/50 — a flat-by-prefix dir split would mis-tier. Repo-empirical beats external-canon at the drift gate. Cluster-2 §1.11 dispositive (four-tier separation "rarely paid back at solo-founder scale"). |
| **Bi-temporal `valid_until`/`superseded_by` + Mark-and-New supersede (DEEP 4)** | **DECISION-MOVING (→ sharpen)** | **WS-3a × X-min** | **Y (narrow slice only)** — the *write*-side supersede-mark is NOT in DESIGN.md (the existing "N-days-old verify" reminder is the *read* side). Cuts D1 (stale-rule) + D4 (memory-pollution). MVP slice = schema + mark-not-drop; full loop DEFERs. |
| Consolidation/reflection daemon; every-N-episodes promotion (DEEP 3 #2; BROAD A Q#5, C#8) | DECISION-MOVING (→ **defer**) | (none / WS-3b-adjacent) | **N for MVP** — no *observed* drift from missing consolidation; a daemon is C1/C9 background topology, not C3 retention (FM-A/J risk). The "promote 2 high-ref unwritten patterns" (EVID Finding 4, bucket D) is **one-time hygiene**, not a standing WS. |
| OCC / git-as-state / shadow-git store recovery (BROAD A Q#2, C#2/C#3; DEEP 4) | CONFIRMATORY | X-min | **N** — X-min already specifies snapshot/version + atomic-writes + **stale-base detection** *(= OCC, exactly)*, and already defers X-full locking. Cursor's lock→OCC pivot validates the *deferral direction*. One line; re-propose nothing. |
| MEMORY.md over-cap = predicted advisory-cap failure (BROAD A Q#3; my F1) | CONFIRMATORY | WS-3a | **N** — validates WS-3a's existence + WS-4 G1 hard-gate. OpenAI 1-min-build precedent is the external anchor for "advisory drifts." Note and move. |
| Cline `memory-bank/` 6-file bounded split (DEEP 6.1) | CITATION-ENRICHING | WS-3a | **N** — proves *bounded-file* discipline, which MEMORY.md-compaction-to-bodies already does. Anchor, not mechanism. |
| MD-vs-JSON format rule for machine-state ledger (DEEP 6.2/6.4 #4) | CITATION-ENRICHING | WS-3b (deferred) / X-min | **N for MVP** — `substitutions.yaml` already IS the schema-backed machine-state instance (R-600). If WS-3b authors a retention ledger, JSON/YAML per `feature_list.json` rule; not an MVP move. |
| link-integrity / index-coverage `--check`; 3 bucket-C breaks + kebab/underscore cross-store drift (EVID Finding 4) | CITATION-ENRICHING (→ **C5 seam**, not mine) | WS-4 | **N for C3** — this is C5's gate lane; I surface it as a seam-question, do not absorb (FM boundary). |

### Decision-moving enrichments (full — only the one item that survives the discriminator)

**[1] Bi-temporal supersede-mark — SHARPEN WS-3a, dual-served with X-min (the one earner).**

- **DESIGN.md WS-N → action:** **sharpen WS-3a + bind to X-min.**
- **Exact edit shape:** in §5 WS-3a ("MEMORY.md compaction"), the current rule reads *"shorten descriptions; move detail to `memory/<slug>.md` bodies; **drop superseded**."* Change the third clause: **`drop superseded` → `supersede-mark (set `valid_until` + `superseded_by` frontmatter on the body, archive the body) — never silent-drop`.** Add to the WS-3a "Deliverable-of-THIS-arc" cell: *"the consolidated-note frontmatter schema (`last_updated`, `valid_until` (null = current), `superseded_by: [[slug]]`) per Zep/Graphiti bi-temporal model (DEEP Dive 4)."* In §4 X-min row, append one clause to the recoverability contract: *"a supersede-mark is a reversible breadcrumb — the archived superseded body is recoverable via the X-min snapshot, so compaction is non-lossy by construction."*
- **Proportionality justification (which D-class it cuts; why solo-proportionate):** cuts **D1 (stale-rule use)** — a memory whose `valid_until` is set is excluded from the "currently true" set, so the grader stops tallying stale-rule applications sourced from compacted-but-still-present entries; and **D4 (memory-pollution)** — a superseded entry that is *marked* (not deleted, not silently-edited-in-place) cannot be silently re-read as current, which is the exact pollution vector. **Why this is NOT the full Mark-and-New protocol:** I take only the *schema + the mark-not-drop write discipline*. This is solo-proportionate because it is a **frontmatter field + a one-word change to an already-MVP compaction step** — zero new daemon, zero new file, zero new tool. The dual-serve is what earns it: it makes WS-3a's compaction *and* X-min's recoverability stronger at the same edit (a marked supersede is the rollback breadcrumb X-min needs; lossy "drop" would have undercut X-min).
- **MVP or DEFER:** **MVP** for the schema + mark-not-drop. **DEFER** the staleness-maintenance *scan loop* (DEEP 4's "scan notes untouched > N days; inject 'still accurate?'") — that is a C9 cadence + C1 filesystem-scan, not C3 retention; **named trigger:** the WS-0 probe showing residual **D1** drift *after* the slim (i.e. supersede-marking-on-write proves insufficient and active re-validation is needed). This slots cleanly under §6 deferred-tail next to WS-6 6b's D5-gated trigger.

*(Boundary discipline, my Round-5 FM-A/J risk: I own the frontmatter **schema** and the **mark-not-drop write** discipline; I defer **when-to-scan** to C9/C1 and the **read-time temporal filter** — "exclude `valid_until != null` at retrieval" — to whoever owns the read seam, almost certainly C2's JIT lane. I do not specify the scan cadence and I do not specify the retrieval filter. Specifying either would be FM-J / FM-A.)*

### Open seam-questions for the consultants

- **C7 (observability):** if supersede-mark lands in MVP but the staleness *scan loop* defers-until-observed-D1, the defer-trigger can never fire unless WS-5 health surfaces a **superseded/stale-note count** (notes with `valid_until` set, and notes untouched > N days). **Will WS-5's health line carry that count?** Without it the §6 trigger for the deferred loop is unobservable — a silent-never-fires gap. (This is the C7↔C3 seam: I emit the supersede-mark state as a durable signal; you decide whether/how it surfaces.)
- **C5 (validation):** EVID Finding 4 counts **3 true bucket-C breaks** (`[[test-bypass-as-runtime-truth]]` → `…-pattern`, etc.) + a systematic **kebab-case (memory) ↔ underscore (`.harness/`) cross-store naming drift** that makes valid cross-store refs read as dead. **Should the WS-4 link-integrity `--check` cover (a) the 3 true-breaks and (b) a first-class cross-store link form?** I flag this as squarely your gate lane, not mine — but X-min's recoverability is weaker if integrity breaks accumulate silently (the C5↔C3 compose the EVID doc already named). I am *not* authoring the gate; I am asking you to scope it.
- **C9 (recovery):** is the **supersede-breadcrumb** (the archived superseded body, recoverable via the X-min snapshot) in-scope with X-min's rollback boundary, or do you read the staleness scan loop itself as recovery-cadence? My read: breadcrumb = X-min (C3-owned, MVP); scan-loop = C9 cadence (deferred). Confirm the split so we don't fork on the boundary (the T5 C3↔C9 co-primary the DELIBERATION already routed to me-as-owner + you-as-recovery-primitive).

### One-line lane verdict

The fresh research is overwhelmingly **citation/confirmatory** for my lane — CoALA/Letta name what the EVID doc already counted, OCC confirms X-min's deferral, the over-cap is the predicted failure — and its two *structural* temptations (episodic/semantic dirs, a consolidation daemon) both fail the drift discriminator (my own repo's degree-tiering refutes the dir split; no observed drift justifies the daemon); the **single earner is bi-temporal supersede-mark**, folded as a one-word sharpen of WS-3a's "drop superseded" → "supersede-mark, never silent-drop," dual-serving X-min's recoverability and cutting D1+D4 at zero new machinery.
