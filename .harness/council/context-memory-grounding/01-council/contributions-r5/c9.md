# C9 — Reliability & Recovery · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries) — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A2** (consultants react to the primaries C2/C3) · workflow `wf_db74601a-68d` · adopted `.claude/skills/council/c9-reliability-recovery/SKILL.md` · read-only · 2026-06-04.

---

The advisor sharpened the spine-call decisively and confirmed my landing. The key adjudication: **DESIGN.md §6 (deferred, D5-gated) supersedes the DELIBERATION T4 "lands WITH" phrasing** — the later reconciled artifact wins, and C2's relayed question presupposes deferred is the committed state. My job is to HOLD the deferral, not re-escalate my own R4 co-requisite framing. The two non-formulaic moves are refinements (observability of the defer-trigger; navigation-integrity dependency on the reinject pointer), zero new workstreams. I'm cleared. Writing my contribution now.

## C9 — Reliability & Recovery · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries)

**First cite:** `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-07 §7.2 (the C2-pole read contract — Selective / Bounded / Navigation-primitive-mediated / Read-into-dynamic-suffix) — recovery is the discontinuity-triggered direction of this same seam; fresh-canon recovery anchors = DEEP Dive 2 (Claude Code FullCompact thin-reinject), DEEP Dive 4 (Zep/Graphiti Mark-and-New), DEEP Dive 5 (continuation probe), BROAD A Q#2 (Cursor lock→OCC pivot / git-as-state).

**Round-5 posture, stated plainly:** my slice is **CONFIRMATORY / CITATION-ENRICHING throughout — zero new C9 workstreams**, and that is the *correct* landing, not a consultant failing. My Round-4 R1–R5 are all already integrated into DESIGN.md (R1→softened-to-deferred §6; R2→adopted §7 home-of-record; R3→adopted WS-6 6a credited; R4→folded into WS-6 6b deferred; R5→absorbed into X-min). The fresh research validates each with external canon and **does not move my lane** — it *confirms the deferral*. I earn the seat with two refinements (both DISCIPLINE-not-surface, FM-A clean) and one corrective to my own Round-4 headline, surfaced below. I hold the spine-tension toward PROPORTIONALITY: the canon's recovery-side machinery (consolidation daemon, 3-tier eval cascade, FullCompact 13K-buffer mechanics) is mostly CA-2 bloat-irony or another voice's lane at the drift gate.

### Reaction to the primaries (BY NAME)

**C3 — answering the directed T5 seam-question ("is the supersede-breadcrumb in-scope with X-min's rollback boundary, or do you read the staleness scan-loop as recovery-cadence? My read: breadcrumb = X-min (C3-owned, MVP); scan-loop = C9 cadence (deferred)").**

**AGREE-AND-EXTEND — your split is exactly right, and here is WHY it survives my lens (not formulaic concurrence):** the cut is the canonical C3↔C9 boundary verbatim (skill §"Co-primary scan", s12 §7.3): **C3 owns the rollback PRIMITIVE + the durability tier + the store; C9 INVOKES the primitive and decides WHEN.** A supersede-mark with an archived body recoverable via the X-min snapshot is a *store-primitive + recoverability* surface — that is C3's (the breadcrumb is the thing the rollback rolls back to; you own the thing). The staleness scan-loop ("scan notes untouched > N days; inject 'still accurate?'") is a *cadence* — WHEN to fire a re-validation — which is C9 (mirror of my Pillar-6 mode-trigger shape). **Confirmed: breadcrumb = X-min, MVP, C3-owned; scan-loop = C9 cadence, deferred.** We do NOT fork on T5. DEEP Dive 4's Mark-and-New supersede protocol is the external-canon model *behind* your breadcrumb — cite it; it confirms "mark-not-drop" is the production temporal substrate, which is exactly the reversible-breadcrumb property X-min's recoverability needs (a lossy `drop` would have undercut X-min, which is why your one-word `drop superseded → supersede-mark` sharpen dual-serves recovery — I credit that as the right call from my lane too).

**SURFACE-TENSION (the extension that earns my seat) — your deferred scan-loop trigger is UNOBSERVABLE, which makes it a silent-never-fires recovery gap.** You directed exactly this worry at C7 ("the §6 trigger can never fire unless WS-5 surfaces a superseded/stale-note count — without it the §6 trigger is a silent-never-fires gap"). I pull it into MY lane because it is precisely the failure my Round-4 R4 `context_budget_exceeded` mode exists to prevent: **a recovery/re-validation cadence whose defer-trigger can never fire is the same silent-degradation shape as a context that rots without a surfaced signal.** So I sharpen the boundary: **the C3↔C9 T5 split is clean IFF the defer-trigger is observable.** D-class cut: **D1 (stale-rule use)** — if the scan-loop's "residual-D1-after-slim" trigger is invisible, residual stale-rule drift accumulates with no path to fire active re-validation. Proportionality: this is a *seam-confirmation*, not a new WS — the count-schema is C7's (WS-5 health line), the cadence-shape is mine (deferred), and neither of us authors the other's surface. **MVP: none** (both halves already deferred); I only assert the **dependency** that the deferred-scan-loop's trigger MUST be one of WS-5's surfaced counts, else §6's WS-6-6b-adjacent staleness trigger is dead-on-arrival.

**C2 — answering the relayed deeper-slim question ("if ICM externalizes provenance OUT of the prefix entirely, deeper than v1's §2 eviction, does that DEEPEN the T4 co-requisite — must recovery-completeness precede WS-1? Is WS-6-6b's D5-gated trigger still right, or does deeper externalization move recovery from co-requisite-DEFERRED to co-requisite-now?").**

**REFINE + SURFACE-TENSION — no, deeper externalization does NOT un-defer recovery; I HOLD the deferral, and I correct my own Round-4 headline to do so.** This is the call that governs my whole contribution. My Round-4 R1 said "recovery-completeness is a CO-REQUISITE sequenced WITH P1." **DESIGN.md §6 already adjudicated that and softened it** — WS-6 6b is now *deferred, D5-gated* ("gated on the WS-0 probe showing D5 (bad-resumption) drift after WS-1"), explicitly reconciling "the C9-co-requisite ↔ Codex-defer tension." Two decorrelated reviewers moved it; the **later reconciled artifact (DESIGN.md §6) supersedes the DELIBERATION T4 "lands WITH" phrasing** — they disagree, and the reconciled one wins. C2's question itself presupposes "deferred" is the committed state ("move from co-requisite-*deferred* to..."). So the disciplined answer is: **hold the defer.** The airtight discriminator is my own F4/R3: **recovery re-hydrates via the §7.2-conformant reinject POINTER (`postcompact-reinject.sh` — grep-verified at HEAD), NOT via the static prefix.** Depth of *static-prefix* externalization is **orthogonal** to recovery-completeness, as long as the reinject channel carries the recovery-relevant orientation (`roadmap_status.md` + checkpoint pointer = "where am I in the arc"), which it does. The blast-radius argument was always about the *content* (provenance, which recovery rarely needs), never the depth. **Bonus that makes the defer doubly-correct: C2's OWN R5 set aside the deeper ICM numbered-folder/CONTEXT.md externalization as CA-2 bloat-irony — so the "deeper externalization" premise isn't even in v1.** I do not answer a hypothetical as if committed. D-class: **D5 (bad-resumption)** stays the gate — the trigger shape (D5-drift-after-slim → build WS-6-6b) is exactly right and survives the deeper-externalization framing unchanged. **Proportionality: HOLD; zero new WS.**

**REFINE (the second move that earns my seat) — deeper externalization DOES open one new recovery DEPENDENCY: the reinject pointer's target must resolve.** Here is the one thing C2's "externalize OUT of the prefix entirely" framing actually changes for my lane. Recovery-via-pointer (R3/F4) is safe *only if the pointer resolves*. If reinject re-hydrates by JIT-loading from the externalized store, then **a dead pointer = an unrecoverable reinject** — recovery silently fails at the navigation hop. This ties my recovery lane directly to **EVID Finding 4** (3 true bucket-C link-breaks + the kebab-case-memory ↔ underscore-`.harness/` cross-store naming drift that makes valid cross-store refs read as dead) and to **X-min** (the store must be both *versioned* AND *navigable* — versioning recovers content; a resolving link recovers the *path to* the content). D-class cut: **D5 (bad-resumption)** + **D2 (wrong-canonical-artifact)** — a reinject pointer that resolves to the wrong/dead target mis-derives next-action OR lands on a non-canonical artifact. **Proportionality: this is a recovery-completeness DEPENDENCY, not a new WS** — I route the integrity gate to C5 (WS-4 link-integrity `--check`), the store-versioning to C3/X-min, and keep ONLY the recovery-primitive requirement ("the reinject pointer must resolve, or recovery is silent-fail") in my lane. FM-A clean: I own the *requirement*; I author neither the gate nor the store.

### My lane's own decision-moving enrichments (triage my slice)

**Verdict: ZERO decision-moving items → ZERO new C9 workstreams.** Per the mandatory triage, every fresh item in my lane is CONFIRMATORY or CITATION-ENRICHING. I do not manufacture machinery to "earn the seat" — the two refinements above + the clean T5 answer are the contribution.

| fresh item (doc + section) | class | DESIGN.md target | proportionality verdict — earns a WS vs (drift metric + solo filter)? |
|---|---|---|---|
| **DEEP Dive 2** — FullCompact reboots fresh session + **re-injects intent + key decisions + 5-most-recent-files (≤5K tok) + active plans/TODOs**; reset to 50K budget | **CONFIRMATORY** | WS-6 6a (credited) | **N.** External product-canon *confirming* my R3/F4: the production-validated recovery pattern is **thin-reinject of pointers+intent, NOT a fat prefix restore** — exactly `postcompact-reinject.sh`'s §7.2-conformant behavior. **Caveat (citation-grounding, workspace #1 discipline):** Dive 2's internals (13K buffer, 5-file reinject) are *NotebookLM external canon*, NOT repo-verified — my R4 verified the *actual* HEAD hooks. Cite Dive 2 as the external anchor for "thin-reinject is canon"; do NOT conflate with the grep-verified hook. |
| **DEEP Dive 2** — AutoCompact **circuit-breaker after 3 consecutive compaction failures** | **CONFIRMATORY (set-aside)** | — | **N.** This is Claude Code's *own* breaker (my Pillar-4 shape, in the product I run under) — not a harness-governance lever. Validates that the recovery substrate I depend on already has a breaker; I author nothing. FM-A: this is C9-mechanism-in-the-host-product, not a DESIGN.md WS. |
| **DEEP Dive 2** — `feature_list.json` anti-"victory-declaration" (re-read each session; JSON resists hallucinate-overwrite) | **CITATION-ENRICHING** | (X-min adjacent / WS-3b deferred) | **N.** Anchors the "are-you-really-done" recovery-completeness check (the continuation/artifact-survival probe). The harness's analog is `roadmap_status.md` re-derivation (SessionStart §12.1 audit) — already BUILT. Cite as the external model for "machine-state the agent must not corrupt"; no MVP move (C3's `substitutions.yaml` already IS the schema-backed instance per R-600). |
| **DEEP Dive 4** — Mark-and-New supersede as a recovery model (`valid_until`/`superseded_by`; close-window-never-overwrite) | **CONFIRMATORY** | WS-3a × X-min (C3's earner) | **N for C9.** Confirms the **breadcrumb** model behind C3's T5 split — a marked (not dropped) supersede IS the reversible rollback-breadcrumb X-min's recoverability needs. I credit C3's one-word sharpen from my lane; the *write-discipline* is C3's, the *recovery-primitive* (breadcrumb recoverable via snapshot) is the C3-owned half I confirmed. No C9 WS. |
| **DEEP Dive 5** — continuation probe (recall / artifact / **continuation** = resume-a-multi-step-task) | **CONFIRMATORY (→ C8 seam)** | WS-0 (C8's gate) | **N.** The **continuation probe IS the operationalization of my D5 (bad-resumption) measurable** — "does the agent resume the right next-action post-reset." This confirms D5 is the right recovery gate. **Seam to C8, NOT mine to author:** I surface that D5 is measurable via a continuation probe; C8 owns whether it needs threshold machinery. I *reinforce* C2's read against the 3-tier-cascade CANON pull: **κ≥0.70 is moot at MVP** — Cohen-κ governs human↔judge agreement, the WS-0 grader is a single human floor with NO judge, so the cascade's TPR/TNR/κ thresholds are population-claims for a judge that doesn't exist. Proportionality: the binary human-labeled D5 check is sufficient; the cascade is C8-lane bloat-irony at the drift gate. |
| **BROAD A Q#2** — Cursor pivoted locks→**OCC** (detection-first); git-as-state / shadow-git for the unrecoverable store | **CONFIRMATORY** | X-min | **N.** Confirms X-min's *deferral direction* exactly (X-min specifies snapshot/version + atomic-writes + **stale-base detection = OCC**, and already defers X-full locking). Cursor's lock→OCC pivot validates "detect-first, lock only on observed race." **Names my R5 rollback boundary concretely: git-as-state.** The out-of-worktree memory store's recovery primitive = *its own git history* — so X-min's "version/snapshot the store" IS the git-as-state remedy. One line; C3 owns the store, I confirmed the primitive (git boundary) exists-or-must-be-created. No new WS. |

### Residual seam-questions / open conflicts (for the cross-read + adversarial reviewer)

- **→ C7 (the one I most need closed):** my T5-split-extension asserts the C3↔C9 split is clean **iff the deferred scan-loop's defer-trigger is observable.** C3 directed the same worry at you (stale-note count). **Confirm WS-5's health line carries (a) a superseded/stale-note count AND (b) the D5-resumption-drift signal** — because §6's WS-6-6b trigger ("D5 drift after WS-1") and the deferred scan-loop trigger ("residual D1 after slim") are BOTH unobservable without WS-5. Two deferred recovery cadences hang on your health surface existing; if WS-5 stays fully deferred, both triggers are silent-never-fires. Is a *single periodic memory-health line* (orphan% + true-break count + stale-note count, per EVID Finding 4's recommendation) the MVP floor that un-blocks both triggers cheaply?
- **→ C5:** my "reinject pointer must resolve, else recovery is silent-fail" dependency lands the recovery lane on your gate. **Does WS-4 G1's `--check` (or a sibling link-integrity check) cover that the reinject pointer's target (`precompact-latest.md` → checkpoint, `roadmap_status.md`) resolves?** EVID Finding 4's 3 true breaks + cross-store kebab/underscore drift are the live failure surface. I want the recovery-pointer-resolvability owned at the gate, not assumed — but I am NOT authoring the gate (FM-A); I am scoping the recovery requirement onto it.
- **→ C8:** the continuation probe (Dive 5) operationalizes my D5 measurable. **Does WS-0's binary human-labeled D5 check need any of the 3-tier cascade's threshold machinery, or is the single-human-floor grader sufficient?** My read (reinforcing C2): sufficient — κ is moot (no judge). Your population claim to defend WS-0's no-harness simplicity against the CANON pull.
- **→ C3 (confirm-back, no fork):** I confirmed breadcrumb=X-min / scan-loop=C9-deferred. One residual: the **`[[checkpoint-…]]` memory→transient-tier ref** (EVID Finding 4 bucket D) is a memory→checkpoint leak — a recovered pointer that crosses the durable↔transient boundary. I read that as your store-hygiene to seal (the leak is a *write* into the durable index of a transient-tier ref), with my only flag being that recovery must not depend on a transient-tier target (checkpoints are advisory per `[[feedback-checkpoint-remaining-work-is-advisory-not-authoritative]]`). Confirm that's your seal, not mine.

### One-line consultant verdict

**C9 round-5: proportionality wins my lane outright and I HOLD the deferral — DESIGN.md §6's reconciled deferred-D5-gated WS-6-6b SUPERSEDES the DELIBERATION's "lands WITH" phrasing, so deeper static-prefix externalization does NOT un-defer recovery (the reinject channel is §7.2-pointer-mediated, orthogonal to prefix depth; the evicted content is provenance recovery rarely needs); I confirm C3's T5 split (breadcrumb=X-min MVP / scan-loop=C9-deferred) and earn my seat with exactly two refinements, NEITHER a new WS — (1) the split is clean *iff* the defer-trigger is C7-observable, else it is the silent-never-fires recovery gap my `context_budget_exceeded` mode exists to prevent; (2) deeper externalization opens one new recovery dependency, *the reinject pointer must resolve* (recovery-via-pointer silent-fails on a dead link, tying recovery to EVID Finding 4 + X-min's versioned-AND-navigable store) — routing count→C7, gate→C5, store→C3, keeping only the recovery-completeness requirement mine; my whole fresh slice (FullCompact thin-reinject, Mark-and-New breadcrumb, continuation-probe-as-D5, OCC/git-as-state) is CONFIRMATORY/CITATION-ENRICHING with zero new C9 machinery, and I reject the 3-tier eval cascade's κ≥0.70 as moot-at-MVP CANON-pull (no judge exists for the single-human-floor grader).**