# C2 — Context Engineering (PRIMARY) · Round 5 evidence-enrichment — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A1** (primaries, independent / blind to each other) · workflow `wf_07c07c77-82b` · adopted `.claude/skills/council/c2-context-engineering/SKILL.md` · read-only · 2026-06-04.

---

Confirmed: all four Dive-6 established nav anchors (ARCHITECTURE.md / WORKFLOWS.md / HOOKS.md / AGENTS.md) and design-substrate/INDEX.md are absent — consistent with my prior F3/F5 and the DESIGN.md WS-2a/WS-2b split. (Note: DESIGN.md WS-2a prefers `ARCHITECTURE.md`; my prior P5 over-listed `AGENTS.md`/`WORKFLOW.md` — the Dive-6 guardrail tightens this.) I have everything I need. Writing my contribution now.

## C2 — Context Engineering (PRIMARY) · Round 5 evidence-enrichment (PRIMARY)

**First cite:** `Spec_Information_Substrate_v1.md` §C-IS-07 §7.2 (C2-pole read contract — Selective / Bounded / Navigation-primitive-mediated / Read-into-dynamic-suffix) — continuity anchor; fresh-canon enrichment = ICM, **Van Clief & McDermott (2026), arXiv:2603.16021v2** (the methodology the CHARTER named).

**Round-5 posture, stated plainly:** in C2's lane the spine-tension resolves decisively toward **PROPORTIONALITY**. My slice is almost entirely CITATION-ENRICHING or CONFIRMATORY. The one decorrelated reviewer that mattered most this round is the arc's own reframe-to-drift (§2 of DESIGN.md): it **demotes my prior headline** ("the load-bearing claim no other voice makes is the cache mechanism") to CONFIRMATORY. I own that demotion — it is the correct consequence of moving the gate from cost→drift, not a loss. The deliverable this round is a long fold-as-cite table + a *short* decision-moving section with exactly **two sharpens, both of WS-2a, neither a new WS.**

### Triage (every fresh item touching my lane)

| fresh item (doc + section) | class | DESIGN.md target | proportionality verdict — earns a WS vs (drift metric + solo filter)? |
|---|---|---|---|
| **DEEP Dive 6.5 #1** — do-NOT-invent `GC.md`/`ROUTING.md`/`CATALOG.md`; established set = `ARCHITECTURE.md`✅ / `WORKFLOWS.md`◑ / `HOOKS.md` / MEMORY-ToC | **DECISION-MOVING** | WS-2a (+WS-2b) | **Y (negatively).** Constrains the WS-2a file set — reduces invention, cuts **D2** (a fresh context recognizes canonical anchors). Anti-bloat by construction. Sharpens, adds nothing. |
| **DEEP Dive 1** — ICM Selective Section Routing (`file#header` in Inputs table) | **DECISION-MOVING (conditional)** | WS-2a (INDEX shape) | **Y *iff* the INDEX is generated; N if hand-authored.** Generated anchors can't go stale → cuts **D2**. Hand-authored section-anchors become a NEW dead-link/**D4** surface (EVID Finding 4 shows naming-drift breaks are already live). The generated/hand discriminator is the whole adjudication. |
| **DEEP Dive 2** — Claude Code §2 cache-detonation mechanics (`tools→system→messages` hash-of-prefix; any CLAUDE.md edit = full miss; 60/60 commits touch §2) | **CONFIRMATORY** | WS-1 | Mechanically *confirms* my prior F2; changes no WS. Kept measurable = git-derivable **§2-edit-cadence proxy** (probe-log signal d), **not** `cache_read`. |
| **DEEP Dive 2** — "measure success by `cache_read` > 0.60" | **CONFIRMATORY → disqualified-as-measurable** | WS-1 / (WS-5) | **N.** Probe-log #3 settled it: no dev-loop home (cache attribution lives in H_T *product* OTel, not the dev loop). Plus the reframe moved the gate to drift. Doubly disqualified as a gate/signal. |
| **DEEP Dive 2** — intra-file breakpoint placement ("breakpoint after immutable section; volatile at bottom") | **set-aside (un-actionable here)** | — | **N.** Assumes a harness building its own prompt. `CLAUDE.md` is *auto-loaded*; the harness does not control `cache_control` *inside* it (red-team #3). The only actionable lever is eviction = already WS-1. |
| **BROAD Part C #5** — re-derive budget as %-utilization (60–80% target; Dumb-Zone 40–60%) | **CITATION-ENRICHING** | **WS-5 (deferred)**, NOT WS-1 | **N for WS-1.** Category error to apply a *whole-window runtime* property to WS-1's *static-prefix byte-cap*. Folds as a cite that lands on WS-5-when-built. |
| **DEEP Dive 6.3** — size norms (`AGENTS.md` <60 ln; SKILL/WORKFLOWS body <500 ln/5K tok; repomap 1024-tok; tool-defs ~55K/50 tools; 35-min wall @ 80–150K) | **CITATION-ENRICHING** | WS-1 / WS-2 | **N (no WS).** External size-norm anchors for the WS-1 keep-half + the WS-2 anchor bodies. One-line cites. |
| **DEEP Dive 6.2/6.4** — format rules (instructions = MD+XML tags; volatile *below* a breakpoint; load order `tools→system→messages`) | **CITATION-ENRICHING** | WS-1 | **N (no WS).** Anchors the WS-1 keep/position-half "where it sits for attention" deliverable. |
| **EVID Finding 1** — load-always hub core (in-degree: `advisor-…`=65, `halt-route-…`=27, `verification-shape-…`=26; top-3 = 26% of edges; ~20 notes ≥5) | **CONFIRMATORY (mine) → hands key to C3** | WS-3a (C3) / WS-2a loading | **N for a C2 WS.** *Confirms* my P6 one-line-index + JIT-bodies discipline; the **retention cut-list key (in-degree) is C3's at-rest surface** — credit + route, do not absorb (FM-A). |
| **BROAD C #1 / Part B (C2)** — CoALA tiers; ICM = CHARTER methodology; Devin SWE-grep "60% first turn JIT-retrieving" | **CITATION-ENRICHING** | WS-3 (C3) / WS-1 | **N.** Vocabulary + the JIT-over-eager production default (validates WS-1's evict→navigate direction). Cites only. |
| **DEEP Dive 1 / DEEP Dive 3 / Dive 4 / Dive 5** — full ICM numbered-folder + CONTEXT.md contracts; episodic/semantic split; consolidation/reflection daemon; bi-temporal frontmatter; 3-tier eval cascade (TPR≥0.90/TNR≥0.85/κ≥0.70) | **set-aside (C3/C5/C8 lanes) / CA-2 bloat-irony** | — | **N.** The numbered-folder/CONTEXT.md architecture is the governance-bloat irony incarnate (cluster-2 §1.11 "four-tier rarely paid back at solo scale"); the tier/daemon/temporal pieces are C3's; the eval cascade is C5/C8's. None is mine to move. |

### Decision-moving enrichments (full — ONLY decision-moving items)

**E1 — Dive-6.5 navigation-set guardrail → SHARPEN WS-2a (and tighten my own prior P5).**
- **Edit shape:** WS-2a §5's anchor set becomes an *explicit closed list of established conventions* + an explicit do-not-invent clause. Concretely: WS-2a names `ARCHITECTURE.md`✅ (DESIGN.md already prefers it) and (deferred to WS-2b) `WORKFLOWS.md`/`HOOKS.md` as the *only* sanctioned new anchors; add a one-line rule **"do NOT author `GC.md` / `ROUTING.md` / `CATALOG.md` — none is a real convention; context-GC is a runtime compaction mechanism, routing is CP code-config the harness already has (`modelPointers`/`fallbackChains`), a tool/skill catalog if ever needed is a *generated* index."** This also **corrects my own Round-4 P5 over-listing** (`AGENTS.md`/`WORKFLOW.md` as new anchors): per BROAD Q#4, Claude Code does NOT auto-load `AGENTS.md` (safe-as-not-loaded), but the deliberate-naming caveat is real → WS-2a should *prefer `ARCHITECTURE.md`*, name `AGENTS.md` only with the cross-runtime-portability note, and drop the bare `WORKFLOW.md` (the convention is `WORKFLOWS.md`).
- **Proportionality:** cuts **D2** (wrong-canonical-artifact — a fresh context lands on recognized anchor names; inventing non-conventions degrades that signal). Solo-proportionate because it *reduces* surface (a do-not-invent rule is the anti-bloat move CA-2 demands). [HIGH — arXiv:2603.16021v2 + Dive-6 evidence-of-absence on GC/ROUTING.md]
- **MVP** (folds into existing WS-2a; zero new machinery).

**E2 — ICM Selective Section Routing → SHARPEN the WS-2a INDEX *shape*, gated on generated-not-hand-authored.**
- **Edit shape:** WS-2a's `design-substrate/INDEX.md` deliverable gains a *generated-only* qualifier and a forward-option: the index maps `artifact → canonical-version` (already in DESIGN.md), and **MAY** additionally expose `file#header` section anchors **only if those anchors are mechanically derived** (a generator walks the markdown headers; the `--check` re-derives and fails on drift). Add the explicit discriminator sentence: *"hand-authored section anchors are forbidden — they become a stale-link/D4 surface (cf. EVID Finding 4's live naming-drift breaks); section routing is admitted to MVP only as generated output."* If a generator is not built this arc, section-routing **DEFERS** to WS-2b; the plain `artifact→version` INDEX (un-anchored) is the MVP floor.
- **Proportionality:** generated section-routing cuts **D2** (precise navigation to "canonical vN §NN" without carrying it inline — the exact §2-inflation cause). The gate prevents it from *creating* **D4**. Solo-proportionate because the generator is a thin walk-the-headers script, not an apparatus — and it is *refused* if it can't be made self-maintaining. [HIGH on the hazard; MODERATE on whether a generator lands MVP — operator/execution-arc discretion]
- **MVP floor = un-anchored `artifact→version` INDEX; section-routing = MVP-iff-generated, else DEFER to WS-2b** (trigger: a generator exists OR an observed wrong-§-citation D2 incident).

**E3 (demotion, not enrichment) — own the cache-headline demotion at WS-1.**
- **Edit shape:** *none to DESIGN.md.* This is a self-correction for the ledger: WS-1's *rationale* annotation should read "§2 eviction confirmed as a cache-detonation cure (Dive 2 mechanics + probe-log #2's 60/60), **measured by the git-derivable §2-edit-cadence proxy, not `cache_read`** (no dev-loop home — probe-log #3)." The cost case stays *true and cited*; it is simply no longer the gate, and `cache_read>0.60` is **not** adopted as a signal. WS-1's success remains the WS-0 drift probe. I flag this so no downstream reader resurrects `cache_read` as a measurable from my Round-4 P-table.

### Open seam-questions for the consultants

- **C7 (observability):** with `cache_read` confirmed to have **no dev-loop home** (probe-log #3), is the git-derivable **§2-edit-cadence proxy** the *sole* cheap legibility instrument for "did the WS-1 eviction actually take," or does Dive 2's **"silent zero-cache"** framing surface any *cheaper-than-product-OTel* signal you'd wire into the WS-5 health line? I'm asserting the proxy is sufficient at MVP — push back if there's a leaner one.
- **C5 (validation):** if WS-2a admits **generated** section-routing (E2), does your WS-4 link-integrity `--check` **extend to validating that `file#header` anchors resolve** (re-derive headers, fail on a dangling anchor)? That extension is what lets E2 be MVP-safe rather than a D4 source — I want it owned at the gate, not assumed.
- **C8 (eval-ability):** does the WS-0 **binary, human-labeled** drift probe need *any* of Dive 5's threshold machinery (TPR≥0.90 / TNR≥0.85 / **Cohen-κ≥0.70**), or does the single-human-floor grader make judge-alignment **moot at MVP**? My read: moot — κ governs human↔judge agreement and there is no judge — but it's your population claim to defend WS-0's no-harness simplicity against the 3-tier-cascade pull.
- **C1 (orchestration):** Dive-6 names `HOOKS.md`/`WORKFLOWS.md` as the lifecycle/procedure anchors (your topology-content lane). For WS-2b: do you want `HOOKS.md` to *be* the lifecycle-topology contract (your content) with C2 owning only its loading discipline (JIT, **never `@import`-ed** into the prefix — the one future-guard)? Confirming the 3-lane split (C1 content / C2 placement / C7 overlay) holds with the fresh Dive-6 framing.

### One-line lane verdict

**C2 round-5: proportionality wins my lane outright — the fresh canon CONFIRMS the §2 cache-detonation (and rightly demotes it from gate to leading-indicator) and ENRICHES every existing decision with named anchors (ICM arXiv:2603.16021v2, Dive-6 taxonomy), while moving exactly two things — both SHARPEN of WS-2a (the Dive-6 do-not-invent navigation guardrail + ICM section-routing admitted MVP-only-if-generated) — and I set aside the ICM numbered-folder/CONTEXT.md architecture, the episodic/semantic split, the consolidation daemon, bi-temporal frontmatter, and the 3-tier eval cascade as either C3/C5/C8 lanes or the CA-2 governance-bloat irony the arc exists to avoid.**
