# C7 — Observability · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries) — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A2** (consultants react to the primaries C2/C3) · workflow `wf_db74601a-68d` · adopted `.claude/skills/council/c7-observability/SKILL.md` · read-only · 2026-06-04.

---

Both surfaces confirmed exactly as the advisor and primaries claimed. `session-end-cleanup.sh:49-53` writes a `## MEMORY.md cap` section to `session-end-report.md` — that report is the natural home for C3's stale-note count rider (one grep, no new workstream). And `loop-gc.sh:56` is the SessionStart surface. The C3 gap is real (no `valid_until` count surfaces today) but it rides this existing SessionEnd report. My adjudication holds. I have everything grounded. Writing my contribution.

## C7 — Observability . Round 5 evidence-enrichment (CONSULTANT — reaction to primaries)

**First cite:** `s10-c7-observability-spec.md` §4.5 (structure-not-content discipline — structural attributes always-on, content default-off) + §4.6 (sampling: stage-keyed posture, keep-criteria) — my schema-authority anchor; canonical workspace read-seam is `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-07 §7.2 (read-into-dynamic-suffix). Fresh-canon this round: **DEEP Dive 2** (silent-zero-cache diagnostic) + **BROAD Part B (C7)** (context-rot / embedding-drift) + **DEEP Dive 6.3** (size budgets). Net: my lane moves **exactly one** decision-moving item — a *one-line rider* on WS-3a, not a new workstream — and surfaces **one clean tension with the fresh canon itself**.

### Reaction to the primaries (BY NAME)

**C3's seam-question — AGREE the gap is REAL, but REFUSE the apparatus it tempts (surface-tension with the maximalist reading of my OWN prior finding).**

C3 asks, precisely: *"if supersede-mark lands in MVP but the staleness scan-loop defers-until-observed-D1, the defer-trigger can never fire unless WS-5 health surfaces a superseded/stale-note count … Will WS-5's health line carry that count? Else the §6 trigger is a silent-never-fires gap."*

C3 is **empirically right about the gap.** A deferred-until-observed trigger that has no observable surface is a silent-never-fires gap — and that is exactly the failure my own Round-1 F2 named ("an observability requirement is logically prior to any retention policy"). I concede this plainly; it is a genuine catch, not a formality.

But the seam-question is also a **setup that tempts me to promote WS-5 from deferred** — and my own F2 makes "yes, promote WS-5" feel inevitable. I **SURFACE-TENSION against that reading**, including against the maximalist pull of my own prior finding. The discriminator: *does the superseded/stale-note count REQUIRE WS-5's infrastructure (mid-session injector + dashboard), or can it ride a check ALREADY in MVP?* **It rides one.** C3's supersede-mark frontmatter (`valid_until` null=current; `superseded_by`) makes the count a **grep**: `count(notes with valid_until set)` + `count(notes untouched > N days)`. And the home already exists and is already in WS-3a's scope — verified at source this session: `session-end-cleanup.sh:49-53` already writes a `## MEMORY.md cap` section to `session-end-report.md` at SessionEnd; `loop-gc.sh:56` is the SessionStart surface. **Appending two grep-derived integers to that existing SessionEnd report is a one-line rider on WS-3a's enforced-compaction boundary check — not a workstream.**

- **Adjudication:** honor the gap, refuse the apparatus. The count is added to WS-3a's already-MVP SessionEnd report. **WS-5's mid-session surface + dashboard STAY deferred.** Drift-class cut: **D1** (makes the D1-remediation trigger observable, so the §6 deferred scan-loop can actually fire). Proportionality: **MVP** as a rider — one grep-derived integer pair on a check that already runs; zero new hook, zero dashboard, zero new file. This is the answer that holds the convening spine: PROPORTIONALITY wins, but the named gap is not ignored.
- **The non-formulaic refinement that proves I'm not reflexively maximalist:** the stale-note count is **SLOW-moving** — notes do not go stale *within* a session. So unlike CLAUDE.md/MEMORY.md byte-budget (which my Round-1 F3 argued *needs* a mid-session surface because an agent can bloat the prefix mid-loop), the stale-note count is correctly **boundary-only emission.** Drawing the fast-vs-slow distinction is the in-lane judgment: F3's mid-session argument does **not** generalize to this signal. Emitting it mid-session would be FM-H (over-instrumentation) for a quantity that cannot change between two UserPromptSubmit turns.
- **FM-A / FM-K boundary held:** C7 owns only that the count is **SURFACED at the SessionEnd boundary**. The **threshold** (what value of N fires the deferred loop) is C3's retention policy / C8's threshold-validity — not mine. The **scan cadence** (when the staleness loop runs) is C9/C1 — not mine. I make **no population-level claim** about staleness rate. All three routed out of lane explicitly.

**C2's seam-question — AGREE the §2-edit-cadence proxy is sufficient for what it measures, but REFINE: it is not the SOLE nor the cheapest instrument for "did the eviction take."**

C2 asks: *"with `cache_read` confirmed to have no dev-loop home (probe-log #3), is the git-derivable §2-edit-cadence proxy the SOLE cheap legibility instrument for 'did the WS-1 eviction actually take,' or does Dive-2's silent-zero-cache framing surface a cheaper-than-product-OTel signal you'd wire into the WS-5 health line? (C2 asserts the proxy is sufficient at MVP — push back if leaner.)"*

On the **silent-zero-cache axis: AGREE-AND-CONFIRM, no pushback.** Dive-2's diagnostic (*"if `cache_read` is consistently 0 despite breakpoints, a dynamic block sits before your breakpoint"*) is gated on `cache_read`, which has **no dev-loop home** — probe-log #3 settled it, and it *confirms* my Round-1 F4 exactly (the same dead-end signal). Dive-2 surfaces nothing leaner; that axis is **CONFIRMATORY**, and I do **not** wire silent-zero-cache into any WS-5 health line (it has no readable input in the dev loop). This is concurrence that survives my lane's scrutiny *because I checked the substrate and it is genuinely unreachable* — not formulaic.

But I **REFINE** the "sole instrument" framing, because **"did the eviction take" and "does the prefix stay slim" are two different questions:**
- The **one-shot "did it take"** is answered most cheaply *not* by parsing git-log cadence, but by the **WS-4 G1 byte-delta** — `bytes(CLAUDE.md)` dropping ~277KB → ~40KB. That number is **already G1's deterministic `--check` input** (DESIGN §5 WS-4 G1: `bytes(CLAUDE.md) + bytes(@import-closure) + bytes(MEMORY.md)`). It is free, deterministic, and present at the exact moment of the eviction. I read it as a **legibility signal** (did the evict land?); the **gate mechanics are C5's** (FM-A boundary — I do not author the gate).
- The **ongoing "is provenance creeping back"** is exactly where the §2-edit-cadence proxy (signal d, git-derivable; `git log -- CLAUDE.md` cadence) is the right instrument, and C2 is correct that it is sufficient for *that* question.
- **Net pushback:** the proxy is sufficient for the *ongoing* question but is **not the SOLE** instrument — the cheaper "did it take" signal (G1 byte-delta) is already in MVP. Drift-class: D2 indirectly (provenance creeping back into the prefix is the §2-inflation cause; the cadence proxy watches it). Proportionality: **both signals are already-MVP** (G1 byte-delta is in WS-4 G1; edit-cadence is git-derivable) — **zero new machinery.** No WS-5 promotion.

**C2's E1/E2 (WS-2a navigation guardrail + generated-only INDEX) — AGREE, with the legibility-preservation lien I already filed at T3.** C2's Dive-6.5 do-not-invent rule (`GC.md`/`ROUTING.md`/`CATALOG.md` are non-conventions) and the generated-not-hand-authored INDEX discriminator both *strengthen* my Round-1 T3 tension (eviction must be paired with a genuinely-navigable surface, else `canonical=vN` becomes *less* legible — the `[[wrong-version-read-delta-only-baseline]]` hazard). The generated-only qualifier is precisely what makes the navigation surface trustworthy for my legibility requirement. **Concur; my T3 lien rides on E2 landing as generated.** (No new C7 move — this is C2's lane closing my open constraint.)

### My lane's own decision-moving enrichments

**[1] (the one earner) — Stale-note count rider → SHARPEN WS-3a (NOT a WS-5 promotion).**
- **WS-N → action:** sharpen WS-3a (rider on its already-MVP SessionEnd boundary check).
- **Exact edit shape:** In §5 WS-3a, the enforced-compaction contract's SessionEnd report (`session-end-cleanup.sh` `## MEMORY.md cap` section, verified at `:49-53`) gains **two grep-derived integers**: `notes-with-valid_until-set: N` and `notes-untouched->Ndays: M`. One sentence added to WS-3a's "Deliverable-of-THIS-arc" cell: *"the SessionEnd compaction report additionally surfaces the superseded/stale-note count (grep over C3's `valid_until` frontmatter) so the §6 deferred staleness-scan trigger is observable rather than silent-never-fires."* **No edit to WS-5; WS-5 stays deferred per §6.**
- **Drift-class cut:** **D1** (the D1-remediation trigger becomes observable). **MVP** (rider). **Trigger to escalate to a richer surface:** identical to the existing §6 WS-5 trigger — *only* if G1 (CI-time) + the SessionEnd count together prove insufficient and mid-session blindness causes *observed* drift. The count rides the boundary check that already exists; it does not pre-build WS-5.
- **Boundary:** C7 owns *the count is surfaced at SessionEnd*. C3 owns the `valid_until` schema + the threshold. C8 owns threshold-validity. C9/C1 own the scan cadence. [HIGH — both surfaces read at source this session: `session-end-cleanup.sh:49-53`, `loop-gc.sh:56`.]

**[2] G1 byte-delta as a legibility signal → CONFIRM/sharpen WS-4 G1 reading (no new machinery).** WS-4 G1's deterministic byte-sum is *also* the cheapest "did the eviction take" instrument. No edit to G1's contract; this is a one-line note in WS-1's success rationale that the *did-it-take* check reads G1's byte-delta (C5's output), while the *staying-slim* check reads the §2-edit-cadence proxy (C2's signal d). **MVP** (both inputs already exist). Boundary: gate mechanics → C5 (FM-A).

### Triage of the rest of my slice (only the above are decision-moving)

| fresh item | class | verdict |
|---|---|---|
| **DEEP Dive 2 — silent-zero-cache diagnostic** (`cache_read`=0 despite breakpoints) | **CONFIRMATORY** | Confirms my F4; same dead-end (`cache_read` has no dev-loop home, probe-log #3). No WS. Not wired into any health line — no readable input. |
| **DEEP Dive 2 — Cache-Hit-Rate > 0.60 target** | **CONFIRMATORY → disqualified-as-measurable** | Lives in H_T *product* OTel, not the dev loop (F4 + probe-log #3). Doubly disqualified (no home + gate-is-drift-not-cost). Not a WS-5 signal. |
| **DEEP Dive 6.3 — size/token budgets** (AGENTS.md <60ln; SKILL/WORKFLOWS <500ln/5K tok; repomap 1024-tok; 35-min wall @ 80–150K; lost-in-mid 40–60%) | **CITATION-ENRICHING** | External size-norm anchors C2 already took for WS-1 keep-half / WS-2 anchor bodies. One-line cites; no C7 WS. |
| **BROAD Part B (C7) — lost-in-the-middle 15–20% / Dumb-Zone 40–60%** | **CITATION-ENRICHING** | Anchors WS-1's keep/**position** half (DESIGN §5 already cites research §2.2.2 U-shaped). Reinforces "a 40KB prefix still rots in the middle; positioning is first-class." No new WS. |
| **BROAD Part B (C7) — context-rot via embedding drift (MMD / cosine)** | **SET-ASIDE — surface-tension WITH THE CANON** | See below — this is my cleanest in-lane refusal of canon-overreach. |

### The tension I surface WITH THE FRESH CANON (in lane, the convening spine made explicit)

**BROAD Part B (C7) offers embedding-drift / MMD / cosine context-rot scores. I SET THESE ASIDE — and the reason is the cleanest PROPORTIONALITY-vs-CANON adjudication in my lane.** WS-0 (DESIGN §3, the gate) *deliberately* chose **binary, human-labelable, no-model-judge, no-continuous-"rot-feeling"** drift incidents (D1–D6). Embedding-drift / MMD / cosine are **continuous, model-computed** signals. They are **not complementary to WS-0 — they are *contrary to its design.*** Adopting an MMD/cosine rot-detector would (a) re-introduce a model-in-the-loop the gate explicitly excluded, (b) make D1–D6 un-blindable (the grader is the human floor; a continuous score is not a per-session binary tally), and (c) be the **CA-2 governance-native-bloat apparatus the arc exists to refuse** — a dashboard of drift-scores is precisely "the dashboard the arc rejected." This is squarely C7 territory (it is *runtime-introspection signal design*), and my schema-authority verdict is: **the right instrument for measured drift here is the human-graded D1–D6 matrix, not an embedding-drift detector.** I make **no population-level rot claim** (FM-K). The canon names a richer signal; my lane *refuses* it on the gate's own design grounds. [HIGH — DESIGN §3 explicitly rejects continuous rot-feeling + model-judge; BROAD-B embedding-drift is the contrary apparatus.]

### Residual seam-questions / open conflicts (for the cross-read + adversarial reviewer)

- **C3 / C8 (threshold ownership):** who owns the **threshold** at which the stale-note count (my SessionEnd rider) fires the §6 deferred staleness-scan loop? My read: C3 (retention policy) sets it; C8 validates it; C7 only surfaces the integer. Confirm so we don't fork on the boundary.
- **C11 (permanent home of the count):** when the deferred WS-5 dashboard *does* land (on its §6 trigger), does it **absorb** the stale-note count, or does the SessionEnd report remain its permanent home? My read: SessionEnd report is the MVP home; dashboard *adds* a render later (C11 UX), it does not replace the boundary emission. C7↔C11 co-primary if the dashboard sequences.
- **C5 (G1 byte-delta queryability):** confirm G1's deterministic byte-sum is **readable as a "did-the-eviction-take" signal** (not only as a pass/fail gate exit code). If G1 only emits exit-0/1, my [2] needs the byte number exposed in the report line, not just the verdict. I own reading it as legibility; C5 owns whether it is queryable.

### One-line consultant verdict

**C7 round-5: proportionality wins my lane — I AGREE C3's silent-never-fires gap is real but REFUSE the WS-5 promotion it tempts (the superseded/stale-note count rides WS-3a's already-MVP SessionEnd report as a two-integer grep rider, boundary-only because the count is slow-moving — the fast-vs-slow distinction my own F3 does NOT generalize to), I CONFIRM C2's edit-cadence proxy as sufficient-for-ongoing but REFINE that the cheaper "did-it-take" signal is WS-4 G1's already-present byte-delta, and my cleanest in-lane move is to SET ASIDE BROAD-B's embedding-drift/MMD/cosine rot-scores as *contrary to* WS-0's deliberately binary/human-graded/no-model-judge gate — the CA-2 dashboard-the-arc-rejected apparatus — making this a zero-new-machinery, exactly-one-rider round that honors the named gaps without building the governance bloat the arc exists to remove.**