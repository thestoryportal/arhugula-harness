# C5 — Validation Contract · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries) — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A2** (consultants react to the primaries C2/C3) · workflow `wf_db74601a-68d` · adopted `.claude/skills/council/c5-validation-contract/SKILL.md` · read-only · 2026-06-04.

---

The advisor confirms the spine sharply and adds three things I had only partially seen: (1) the link-integrity gate is genuinely NOT in DESIGN.md — so both seam-questions presuppose a missing workstream (this IS my proportionality enforcement); (2) the synthesis that ONE gate answers both primaries' questions, with a ship-trigger binding it to whichever cross-ref surface lands first; (3) C3's supersede schema is itself unvalidated — my prior-F3 "contract-without-a-gate" reborn. And the L1-is-threshold-free insight for the C8 cut. I'm ready to write.

## C5 — Validation Contract . Round 5 evidence-enrichment (CONSULTANT — reaction to primaries)

**First cite:** `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-07 §7.1 (C3-pole write) + §7.2 (C2-pole read) + **§6.4 `verify_chain` — the harness's spec'd deterministic conformance gate over its own durable state** (my prior-round anchor; the model the process layer must copy). Fresh canon = **NotebookLM Deep Dive 5** (tiered eval cascade) + **Broad A Q#3** (advisory caps fail; structural⇒HARD / heuristic⇒ADVISORY) + **EVID Finding 4** (link-integrity gate candidate).

### The catch both primaries set up — I name it before answering (this IS my proportionality enforcement)

C2 and C3 both directed seam-questions at "**your WS-4 link-integrity `--check`**." **That gate is not in committed DESIGN.md.** §5 says verbatim "G1 is the *only* load-bearing gate; G2–G4 are deferred"; §6 defers G2/G3/G4; **link-integrity appears nowhere in G1–G4** — it is a fresh candidate from EVID Finding 4's recommendations, not a planned workstream. Both seam-questions presuppose a workstream the plan's proportionality discipline omitted. My consultant job is **not** to answer "yes, the `--check` extends to anchors" as if the gate existed — that would be silently absorbing a new MVP gate, the exact governance-bloat-via-canon-pull this arc exists to prevent. My job is to rule on whether it earns MVP. Verdict below: **it earns MVP only as a ship-gate bound to whichever machine-managed-cross-reference surface lands first; absent that, the 3 true breaks are one-time hand-hygiene, not a standing gate.**

### Reaction to the primaries (BY NAME)

**C2's E2 seam-question — "if WS-2a admits generated `file#header` routing, does WS-4 link-integrity `--check` extend to validating those anchors resolve?"** — **AGREE-AND-EXTEND, with the synthesis only my lane sees.** C2's instinct is exactly right and C2 already half-built my answer: C2 itself gates section-routing on "MVP-iff-generated, else it becomes a stale-link/**D4** surface." That conditional is *unenforceable as prose* — it is the same failure as my prior-round clearance-marker finding (a contract authored, no gate to hold it). So: **yes, the gate extends — but the gate is the thing that makes C2-E2 admissible at all.** Generated routing *without* the validating `--check` IS the D4 hazard C2 named. Therefore the `--check` is a **ship-gate on the generator: they ship together or neither does.** [HIGH on the hazard; the gate is the proportionality control C2's "iff-generated" needs.] Fail-class on a dangling anchor: `permanent-fail-exit` / cause_attribution `contract_violation` (a generated anchor that doesn't resolve is a structural break, not a transient fault — per FM-J).

**C3's seam-question — "should WS-4 link-integrity `--check` cover (a) the 3 true bucket-C breaks and (b) a first-class cross-store link form?"** — **AGREE on (a)-as-byproduct, REFINE on (b).** (a): the 3 bucket-C breaks (`[[test-bypass-as-runtime-truth]]`→`…-pattern`, etc., EVID Finding 4) fall out of the same `--check` *for free* — but on their own they are **one-time hand-hygiene, not a standing gate** (3 known breaks ≠ a recurring drift surface). They do not justify the workstream alone; they ride it. (b) **REFINE, holding my boundary:** I do **not** author the cross-store link *form* — that is C3/C2's convention surface (FM-C if I design it). The fix EVID Finding 4 actually points at is **not new syntax**: it is a **code-fence-aware parser that knows both kebab-case (memory) and underscore (`.harness/`) paths** (closes bucket B parser-pollution AND the bucket-A cross-store kebab/underscore drift that makes valid refs read as dead). I own the `--check`'s conformance behavior over whatever form C3/C2 pick; I do not pick the form.

**The synthesis that unifies BOTH seam-questions (the load-bearing C5 move):** C2's generated `file#header` anchors and C3's `superseded_by: [[slug]]` frontmatter are **the same surface — machine-managed cross-references.** ONE link-integrity `--check` is the conformance gate over both, *and* catches the 3 bucket-C breaks. Its MVP-trigger = **"whichever of {C2-E2 generated section-routing, C3 bi-temporal supersede-mark} ships first"** — either one introduces machine-generated cross-refs that need validation; absent both, no standing gate.

**The tension C3 missed (surfacing it — the charter rewards this):** C3's supersede-mark is good and drift-connected (cuts D1+D4 at near-zero machinery — I concur it survives my scrutiny *as a write discipline*). **But C3's `valid_until`/`superseded_by` frontmatter schema is itself validated by nothing.** It is my prior-round F3 "contract-authored-but-unvalidated" reborn: a dangling `superseded_by: [[slug]]` is a *fresh* **D4** pollution vector — a supersede breadcrumb that points nowhere reads as "current entry exists" when it doesn't. **Refinement:** the mark earns MVP, but its schema needs the same conformance gate as my deferred G2 — and since `superseded_by` is a wiki-link, **that gate *is* the link-integrity `--check` (the synthesis above).** So C3's supersede-mark is a *third* trigger that pulls the `--check` into MVP. C3 correctly deferred the scan-loop to C9 and the read-time filter to C2's JIT lane; I add only: the *write* schema you land needs the `--check` to hold it, or it decays exactly as the §2 prose budget did.

### My lane's own decision-moving enrichments (triage applied; only these get full proposals)

**[G-LINK] Link-integrity `--check` — ADD as a *single, trigger-gated* gate (NOT standalone MVP).** This is the only new gate my slice surfaces, and it earns its place by *answering both primaries at once* rather than as a fresh workstream.
- **Edit shape:** Add to §6 deferred-tail (G2/G3/G4 row) a new sibling: **"G-LINK — link-integrity `--check` (code-fence-aware parser; validates `[[wiki]]` + `[md](slug)` + cross-store kebab/underscore + generated `file#header` anchors all resolve; mirrors `substitution_ledger --check`, exits 1). **Promoted to MVP iff** C2-E2 generated section-routing OR C3 bi-temporal supersede-mark ships — either introduces machine-generated cross-refs the gate must hold. Absent either, the 3 bucket-C breaks are one-time hygiene."**
- **Drift-class cut:** **D4** (memory-pollution — a dangling generated/supersede ref read as live) + **D2** (wrong-canonical — a generated INDEX anchor pointing at a moved/renamed section). Fail-class `permanent-fail-exit` / `contract_violation`.
- **Proportionality:** solo-proportionate *because* it is refused until a machine-generated-cross-ref surface exists — it cannot become a standing apparatus over a hand-authored store (where breaks are episodic hygiene). The trigger-gating IS the anti-bloat discipline. **DEFER → MVP-on-trigger** (trigger named above).

**[RANK] G1–G4 MVP-vs-defer via structural⇒HARD / heuristic⇒ADVISORY (Broad A Q#3) — applied to my OWN machinery, post-drift-reframe.** The drift reframe (DESIGN.md §2) post-dates my prior round; it demotes byte-count to a leading indicator. I do **not** re-run "hard-gate everything":
- **G1 (byte-budget):** structural in FORM, *heuristic in PURPOSE* (a proxy for drift, not drift itself). → **HARD-tier `--check` WITH waiver + warning-mode ramp** (matches DESIGN.md §5 verbatim). My prior-round "exits 1, not advisory" **SURVIVES** — DESIGN.md made it a `--check` — but is **REFINED**: religiously hard-failing a *proxy* is over-gating; the waiver/warning-ramp is the proportionate cut the drift-reframe demands. **MVP** (already in plan; I confirm + refine the religion-vs-guardrail framing).
- **G4 (freshness-teeth):** strongest drift link (**D5** bad-resumption); the §12.3 halt is currently model-prose (my prior-F5). Stays **`HITL-recoverable`** even with teeth (operator reconciles per §12.3 — never auto-resolve; the one place a human-validator is *correct*). **DEFER, highest promotion-likelihood** — co-triggers with WS-6 6b on an observed D5 incident.
- **G2 (clearance-schema):** structural-HARD-*when-built*, but **no observed D2-via-marker** at HEAD. **DEFER.** "HARD-when-built ≠ build-now."
- **G3 (ledger-shape):** near-zero D-class connection — the governance-bloat-irony candidate; may never earn MVP. **Clearest DEFER.**
- *(Showing that structural-HARD-**eligible** gates G2/G3 still DEFER on no-observed-drift is me applying proportionality to my own lane — the gates I'd most want to build are the ones I'm ruling out.)*

**[L1-CUT] Dive-5 3-tier eval cascade → L1-code-assertions ONLY is the solo-proportionate cut (C8-indirect).** Engaging C8's lane only at the boundary I own:
- **Edit shape:** Fold L1 into **WS-1's verify-before-evict precondition as its *deterministic half*** — NOT a new workstream. Concretely: a code assertion that **the `[i]`/citation format still emits after §2 provenance eviction** + a **recall/continuation deterministic assertion** ("the live invariants extracted in WS-1 step (a) are still present post-evict"). This is a gate *over C2's eviction*, exactly C9's "recovery-completeness is co-requisite" satisfied deterministically.
- **Threshold insight (the load-bearing C8-indirect cut):** **L1 is threshold-free by nature** — TPR≥0.90 / TNR≥0.85 / **Cohen-κ≥0.70** are L2-*judge*-calibration metrics; a deterministic assertion needs no calibration (its catch-rate is 1.0 for the invariant it encodes). So I **explicitly SPLIT Dive-5's bundled "`--check` fails on κ<0.70 OR a recall-probe regression"**: keep the deterministic **recall-regression** assertion (L1, MVP); **drop the κ threshold** (L2-judge, DEFER). That bundling is the canon-pushing-machinery move; un-bundling it is the proportionality call. `cache-hit>0.60` is **doubly-disqualified** (C2's probe-log #3: no dev-loop home + the drift-reframe moved the gate off cost). **L2-judge = DEFER** (WS-0's single-human floor covers judge-alignment at MVP); **L3 = IS WS-0 already** (Robert's eyes on the D1–D6 matrix). **Fold the κ-mootness as a *gate-side* surfacing** (I hold FM-A: I do not make the population claim — I note from the gate side that there is no judge, so κ governs nothing at MVP; C8 defends WS-0's no-judge simplicity). I do **not** duplicate C2's already-asked C8 seam-question.

*Everything else in my slice is CITATION-ENRICHING / CONFIRMATORY (one line each, no workstream):* Dive-1 ICM stage audits = "C5's gate model at the folder layer" — **confirms** the `substitution_ledger --check` pattern is canonical; its "audits-by-convention, no deterministic runtime" failure mode is **exactly why** automated `--check` gates matter (anchors G1/G-LINK, adds nothing). Broad A Q#3 OpenAI 1-min-build-gate = external anchor for "advisory caps drift" → **confirms** G1-as-hard (CONFIRMATORY of DESIGN.md §5). Broad A "structural⇒HARD / heuristic⇒ADVISORY" = the ranking *criterion* I applied above (CITATION-ENRICHING the [RANK]). EVID bucket-B "skip code-fenced content" = a parser requirement folded into G-LINK, not its own item.

### Residual seam-questions / open conflicts (for cross-read + adversarial reviewer)

- **To C2 (re E2):** I've made G-LINK a **ship-gate on your generator** (generated routing + validating `--check` ship together or neither). Does your WS-2a INDEX-generator arc *accept* that coupling — i.e. the generator's own acceptance criterion includes "G-LINK passes on its output"? If you'd rather ship the un-anchored `artifact→version` INDEX as MVP-floor (your E2 fallback) and defer both generator + G-LINK together, that is cleaner and I prefer it — confirm which.
- **To C3 (re supersede-schema):** your `valid_until`/`superseded_by` write-schema needs G-LINK to hold its `superseded_by` wiki-link, or it is a fresh D4 vector (my surfaced tension). Do you read the supersede-mark as MVP *with* G-LINK as its co-requisite (they bind), or MVP *without* (accepting the unvalidated-schema risk until an observed D4-via-dangling-supersede incident)? I lean: bind them — but it's your call which write-discipline ships, and I gate only what you land.
- **To C7:** G-LINK emits a gate-event (`gate-failed-with-classification-and-cause`, `link.dangling.count`); C3 also asked you for a superseded/stale-note count on the WS-5 health line. Same emission surface — one `link.integrity` + `note.temporal` health line covers both. I surface the event; you design the span. (Not duplicating C2's `cache_read`-has-no-home question — that's C2's.)
- **To C8 (boundary-only, not duplicating C2):** I assert from the *gate side* that κ/TPR/TNR are moot at MVP (no judge in WS-0). You own whether that holds as a population claim against the 3-tier-cascade pull. I've kept the deterministic recall-regression assertion (L1) and dropped only the judge-calibration thresholds — confirm the split survives your lane.

### One-line consultant verdict

**The link-integrity `--check` both primaries assumed is NOT in the plan — and that is the right call until it earns its trigger: ONE code-fence-aware gate answers C2's generated-anchor question, C3's cross-store-ref question, AND holds C3's otherwise-unvalidated `superseded_by` schema, ship-gated on whichever machine-managed-cross-ref surface lands first; G1 stays MVP as a *waiver'd* guardrail (proxy, not religion), G2/G3/G4 stay DEFER even though G2/G3 are structural-HARD-*eligible* (no observed drift = no MVP), and Dive-5's cascade cuts to L1-deterministic-assertions-only folded into WS-1's verify-before-evict — κ/TPR/TNR explicitly dropped as judge-calibration metrics that govern nothing when WS-0's only judge is a human.**