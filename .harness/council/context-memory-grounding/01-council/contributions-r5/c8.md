# C8 — Eval Engineer · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries) — verbatim contribution

> Genuine agent invocation · sub-phase **E1-A2** (consultants react to the primaries C2/C3) · workflow `wf_db74601a-68d` · adopted `.claude/skills/council/c8-eval-engineer/SKILL.md` · read-only · 2026-06-04.

---

The advisor sharpened the spine. The single defeater — "does it presuppose an automated judge?" — collapses the entire threshold tier at once and is itself the extension past C2 (C2 only addressed κ; I show κ is the tip). The base-rate / statistical-power gap is my seat-earning original finding the framing didn't hand me. And I must tighten my own round-3 claim against C3. I have everything I need. Writing the deliverable now.

## C8 — Eval Engineer · Round 5 evidence-enrichment (CONSULTANT — reaction to primaries)

**First cite:** C8 skill §"Judge calibration discipline" (alignment metric / alignment floor / judge-base-model collision) + §"Eval-set construction discipline" (the five-field leak-prevention contract) + FM-M (recursion bottoms at the human floor) — the canon that governs whether Dive 5's threshold machinery applies. Fresh-canon anchor I adjudicate against: **DEEP Dive 5 (error-analysis-first eval, NotebookLM, 28 cites)** + **BROAD A Q#3 (advisory-caps-demonstrably-fail; OpenAI 1-min-build precedent)**.

**Round-5 posture, stated plainly:** in C8's lane the spine resolves toward **PROPORTIONALITY — but the resolution is load-bearing, not a citation.** Dive 5 is the single hardest CANON pull in the entire evidence set (it is *my* lane's full apparatus: 3-tier cascade, TPR≥0.90 / TNR≥0.85 / κ≥0.70, L2 LLM-judge, ≥100-trace calibration, CI-block-the-PR). My job is to hold WS-0's human-floor simplicity against it. I ADOPT exactly two judge-*independent* residues (the 3 probe TYPES as a categorization lens; the pairwise/blinding structure — already in DESIGN.md §3) and REFUSE the rest with **one** proportionality reason. And I surface one original finding the framing did not hand me: a **statistical-power / base-rate gap** in the WS-0 pass condition that an eval engineer must name or the gate passes vacuously on the rare drift classes.

---

### Reaction to the primaries (BY NAME)

**C2 directed the gate question straight at me:** *"does the WS-0 binary, human-labeled drift probe need ANY of Dive-5's threshold machinery (TPR≥0.90 / TNR≥0.85 / Cohen-κ≥0.70), or does the single-human-floor grader make judge-alignment moot at MVP? My read: moot — κ governs human↔judge agreement and there is no judge."*

**AGREE on the verdict, EXTEND the reasoning — and the extension is the whole point (else this is formulaic concurrence).** C2 is right that κ is moot, but C2 named only κ. **κ is the tip of a single defeater that takes down the entire threshold tier at once.** TPR≥0.90 / TNR≥0.85 / κ≥0.70 / the L2 LLM-judge / the ≥100-trace calibration are **not five independent knobs — they are one package**, and the package exists to do exactly one thing: *stand up an automated judge and validate it against humans.* The discriminating test for every Dive-5 item is therefore **one question: "does this presuppose an automated judge-under-test?"**

- **κ** — moot, as C2 said: κ scores human↔judge agreement; **no judge ⇒ nothing to correlate.**
- **TPR / TNR** — stronger than moot: they are **undefined**, not relaxed. TPR/TNR score a *classifier* against ground truth. WS-0's human floor **collapses classifier-and-ground-truth into one person** (Robert is both the labeler and the truth). There is no classifier-under-test to compute a true-positive *rate* over — the quantity does not exist by construction.
- **L2 LLM-judge + ≥100-trace calibration** — these are the machinery whose *sole purpose* is to make an automated judge trustworthy. WS-0 deletes the judge (DESIGN.md §3: "no model-judge, no eval harness"); the calibration apparatus has nothing left to calibrate.

So the refusal needs exactly **one** reason, not five: **every threshold/judge item in Dive 5 presupposes an automated judge WS-0 does not have by design; adopting any of it re-opens FM-M (a judge-of-the-judge whose human floor must then be re-named) and is the CA-2 governance-bloat irony recurring inside my own lane** — the eval engineer building the eval harness the arc explicitly rejected (DESIGN.md §1 reframe; cluster-2 §1.11). [HIGH — C8 skill §"Judge calibration discipline" applies *iff* a model-based judge is used; §"meta-eval recursion bottoms at the human floor"; DESIGN.md §3 human-floor mandate]

**What survives the "presupposes-a-judge?" test is the judge-independent residue — and THAT I adopt:** Dive 5's **3 probe TYPES (recall / artifact / continuation)** and its **pairwise before/after Control-vs-Treatment + blinding** structure. Neither presupposes a judge; both sharpen WS-0. (Adopt-shape is constrained below — the over-adoption trap is real.)

**C3 directed the supersede-mark earner + episodic/semantic set-aside at me by implication (the drift-discriminator we share).** Three reactions:

1. **CONFIRM C3's bi-temporal supersede-mark from my lane — and note the dual-serve is observable at *my* gate.** C3 claims the supersede-mark "cuts D1 + D4." **The same WS-0 matrix is the instrument that confirms-or-refutes that claim** — D1 (stale-rule use) and D4 (memory-pollution) are two columns Robert already tallies per arm. So C3's earner is not a taste assertion; it is falsifiable by the gate I own. **REFINE one seam C3 punted:** C3's §6 deferred-trigger for the staleness *scan loop* is "WS-0 probe showing residual D1 *after* slim," and C3 routed the observability of that trigger to C7's WS-5 health line as a "silent-never-fires gap." **Partial answer from my lane: the trigger is readable directly off Arm B's D1 column — the gate output itself carries it; no standing health-surface is strictly required for the *trigger to fire*.** (WS-5 is still the right home for *continuous* mid-session visibility; but the deferred-scan-loop trigger specifically is not blind — it surfaces at the next WS-0 run.) This narrows C3's open seam-question to C7 without absorbing C3's retention lane.

2. **SURFACE-TENSION against my OWN round-3 claim — C3's repo-empirical degree-tiering forces me to tighten it.** In round 3 (c8.md cross-coherence) I wrote: *"if the WS-0 probe shows D4 incidence ≈ 0 across the window, 'X' minimal-recoverability is empirically deprioritizable."* **That is now too strong, and the base-rate finding below is why.** Zero observed D4 over ~15–25 real sessions may mean **no coverage, not no risk** (D4 is a rare class; see §"original finding"). I am **retracting the deprioritization lever** — it would hand the operator a false signal. (Practically moot: X-min is already MVP per DESIGN.md §4, reconciled toward Codex; but I will not leave a live claim that a near-zero base rate justifies deprioritizing recoverability. The correct read is the inverse: rare-but-catastrophic classes are *exactly* where observational base rates under-detect, so X-min's MVP status survives my lane's scrutiny on the merits, not by taste.) [HIGH]

3. **CONFIRM C3's episodic/semantic dir-split SET-ASIDE — one line, my lane.** From the drift metric: the dir-split moves **no D-class** that the supersede-mark + enforced compaction don't already cover (D1/D4 covered by the mark; navigation/D2 by WS-2a). An anchor-only/structure-only enrichment earns no WS per the triage rule. C3's own repo-empirical degree-tiering (EVID Finding 3) refutes the prefix-split; I do not absorb it — it is C3's lane and it is correctly set aside.

---

### My lane's own decision-moving enrichments (triage of my slice)

| fresh item | class | target | proportionality verdict (drift class + solo filter) |
|---|---|---|---|
| **Dive 5** — 3 probe TYPES (recall / artifact / continuation) | **DECISION-MOVING** | WS-0 §3 | **Y — adopt as a *categorization lens*, not synthetic tests.** Sharpens the D1–D6 codebook (below). |
| **Dive 5** — pairwise Control/Treatment + blinding (position/verbosity/self-enhancement) | **CONFIRMATORY** | WS-0 §3 | **N (no new WS).** DESIGN.md §3 *already* mandates 2 arms + arm-label blinding. Self-enhancement bias is judge-only → n/a (no judge). Note + move. |
| **Dive 5** — TPR≥0.90 / TNR≥0.85 / κ≥0.70 / L2 LLM-judge / ≥100-trace calibration | **set-aside (CA-2 bloat in my lane)** | — | **N.** Single defeater: presupposes an automated judge WS-0 has none of; TPR/TNR *undefined*, κ moot, calibration vacuous. Refuse the package. |
| **Dive 5** — CI `--check` blocks PR on recall-probe regression / κ<0.70 | **set-aside (→ C5 seam)** | WS-4 | **N for C8 (FM-A/B).** The gate-wiring is C5's lane; I surface it as a seam-question, do not author it. |
| **BROAD A Q#3** — advisory caps demonstrably fail (OpenAI 1-min-build; MEMORY.md 27KB>24.4KB breached-and-ignored) | **CONFIRMATORY** | WS-3a / WS-4 G1 | **N (no new WS).** Validates the *existence* of WS-4 G1 hard-gate + WS-3a enforced compaction (C5/C3 lanes). From my lane: it is the external anchor for *why byte≤cap stays a leading-indicator, not the gate* (an advisory proxy drifts; my CA-7 demotion already encodes this). Note + move. |
| **Dive 5** — regression sets from REAL traces not synthetic; Train/Dev/Test split | **CONFIRMATORY** | WS-0 §3 | **N.** DESIGN.md §3 already: re-use existing `[[use-the-product-probe-pattern]]` sessions, no synthetic corpus. The Train/Dev/Test split is judge-calibration infrastructure → n/a at the human floor (no learned classifier to hold out from). Confirm + move. |

**E1 — Dive-5 probe TYPES → SHARPEN WS-0 §3 (the categorization lens; the only decision-moving adopt).**
- **Edit shape:** WS-0 §3's grading protocol gains a **codebook qualifier on the D1–D6 tally**: when Robert grades a real session, the recall / artifact / continuation triad is the **Husain "categorize"-stage lens** that maps an observed failure onto the binary D-class — *recall* failure (did a fact/rule survive the slim?) → typically **D1/D4**; *artifact* failure (does the agent know which files/version it touched?) → typically **D2/D5**; *continuation* failure (did a multi-step task resume correctly?) → typically **D3/D5**. This makes the human label *reproducible* (a second grader, or future-Robert, classifies the same way) without adding a single test case.
- **CRITICAL adopt-constraint (over-adoption trap):** the probe types enter **as a categorization lens over the observational tally**, **NOT as constructed synthetic test cases.** DESIGN.md §3 forbids a synthetic corpus and mandates re-using real `[[use-the-product-probe-pattern]]` sessions. Pulling recall/artifact/continuation in as *authored* tests would rebuild the eval harness I am refusing (CA-2 in my lane). A downstream reader must not read E1 as "author 3 probe sets" — it is "ask 3 questions while grading the sessions that already happened."
- **Proportionality:** improves **inter-rater reproducibility of the D1–D6 grading** (the gate's own measurement validity — an eval engineer's first duty); cuts no *new* D-class but hardens the instrument that measures all six. Solo-proportionate: zero new tooling, zero synthetic corpus, a 3-word codebook annotation on an existing manual step. [HIGH — Dive 5 probe-type taxonomy; C8 skill §"Husain loop" categorize-stage]
- **MVP** (folds into existing WS-0 §3 grading protocol; no new machinery).

**E2 — Statistical-power / base-rate gap in the WS-0 pass condition → SHARPEN WS-0 §3 (my seat-earning original finding; the framing did NOT hand me this).**
- **The finding:** WS-0 is *observational* over ~15–25 real sessions (DESIGN.md §3). The rare drift classes — **D4 (memory-pollution)** and **D6 (instruction-conflict)** — likely have **near-zero base rate** in that window. The pass condition "Arm B ≤ Arm A on **every** class AND < on ≥1 class" then **passes D4/D6 vacuously (0 ≤ 0)** while the gate is **blind to whether the slim helped or hurt them.** A vacuous pass on a catastrophic-but-rare class is a false green — the exact failure an eval engineer exists to catch (FM-H-adjacent: a claim of "no regression" with no power to detect one).
- **Edit shape (proportionate — no bloat, no synthetic cases):** WS-0 §3 pass condition gains one annotation rule: **any drift class with zero incidents in *both* arms is recorded as "not exercised" ≠ "passed."** A "not-exercised" class does **not** satisfy the "reduces ≥1 class" clause and does **not** count as a validated no-regression; its coverage **routes to the named §6 deferred trigger** (D4→X-min-already-MVP + the supersede-mark; D6→WS-2a instruction-conflict surface). Do **NOT** manufacture synthetic D4/D6 cases to force the base rate up — that re-imports the rejected harness.
- **Proportionality:** protects the **integrity of the gate itself** against a vacuous-pass false-green on D4/D6 — the rare classes where observational base rates structurally under-detect. Solo-proportionate: a one-clause honesty annotation on the pass condition, zero new sessions, zero synthetic corpus. This is the eval-engineer's seat: the gate must *know what it didn't measure.* [HIGH — base-rate / statistical-power is foundational eval methodology; C8 skill FM-H counterfactual-power lineage]
- **MVP** (folds into WS-0 §3 pass condition as a recording rule).

---

### Residual seam-questions / open conflicts (for the cross-read + adversarial reviewer)

- **→ C5 (validation; answering C2's directed E2-gate seam AND C3's Finding-4 seam):** C2 asked you whether WS-4's link-integrity `--check` extends to validating `file#header` anchors resolve. **From my lane I add the parallel question I must NOT author (FM-A/B):** if WS-0's grading adopts the recall/artifact/continuation lens (E1), **should WS-4's `--check` carry a deterministic *recall-probe regression assertion*** (Dive 5's L1 code-assertion tier — e.g. "the `[i]` citation format still emits after §2 eviction")? Dive 5 bundles this CI-block-PR wiring *with* my eval design; it is **your gate lane, not mine.** I surface it; I do not scope it. My only constraint on you: an L1 code-assertion is judge-free (it survives my "presupposes-a-judge?" refusal) — so it is *admissible* at MVP in a way the L2/L3 tiers are not, **if** you judge it proportionate.
- **→ C7 (observability; narrowing C3's punted seam):** C3 asked you whether WS-5's health line must carry a superseded/stale-note count so the deferred scan-loop trigger can fire. **Partial relief from my lane:** the scan-loop's *fire-trigger* ("residual D1 after slim") is **already readable off Arm B's D1 column at the next WS-0 run** — so the trigger is not silent-never-fires even without WS-5. The open question for you narrows to: do you want a *continuous mid-session* stale-note count (WS-5), or is the *periodic* WS-0-gate read sufficient for the deferred-trigger specifically? I assert periodic-gate-read is sufficient for the *trigger*; push back if continuous visibility buys drift-reduction the periodic read misses.
- **→ adversarial reviewer (the one I most want pressure-tested):** **E2's base-rate gap is the soft spot in the entire WS-0 gate.** If D4/D6 never fire in *either* arm across the whole window, the gate's "no-regression" claim on those classes is **unfalsified, not validated** — and my "not-exercised ≠ passed" annotation is honest but does not *solve* the power problem (it documents it). Is the proportionate fix genuinely "annotate + route to deferred trigger," or does the gate need a **minimum-exposure floor** (e.g. "≥1 session per workflow class known to historically exercise D4/D6")? I chose annotate-not-floor to avoid bloat; red-team whether that under-protects the catastrophic-rare classes.

### One-line consultant verdict

**C8 round-5: proportionality wins my lane on a load-bearing argument, not a citation — Dive 5's entire threshold tier (TPR/TNR/κ/L2-judge/100-trace calibration) collapses under ONE defeater ("it all presupposes an automated judge WS-0 deletes by design; κ is merely the tip C2 named"), so I REFUSE the apparatus as CA-2 bloat in my own lane and ADOPT only the two judge-independent residues (the recall/artifact/continuation probe types as a grading *codebook lens* — never synthetic tests; the pairwise/blinding structure DESIGN.md §3 already has) — while surfacing the one finding the framing did not hand me: the WS-0 pass condition **passes vacuously on the rare classes D4/D6** (0≤0 on a near-zero base rate), fixed proportionately by "not-exercised ≠ passed" + route-to-deferred-trigger, NOT by manufacturing synthetic cases; and I tighten my own round-3 claim against C3's repo-empirical degree-tiering (zero observed D4 = no *coverage*, not no *risk* → X-min's MVP status survives on merit, the deprioritization lever is retracted).**