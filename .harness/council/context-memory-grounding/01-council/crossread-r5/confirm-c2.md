# Primary confirm-back — C2 · Round 5 E1 Phase B (seam-routed cross-read DEBATE) — verbatim

> Genuine agent invocation · workflow `wf_8fb2e418-cfe` · 2026-06-04.

---

The advisor confirms the shape and sharpens five things: pick Option 2 decisively on (a) with FM-A-precise trigger framing, tie (c)↔(d) as ex-ante/ex-post halves of "did the fact survive the slim," anchor (e) to E-8 so the cost-demotion stays intact, give (b) the loading-posture-inversion rationale, and route only the two C2-stake residuals. Lane guard: don't rule on C3's supersede-mark↔G-LINK binding (that's the other primary's confirm-back). Writing now.

## Primary confirm-back — C2

**First cite:** `Spec_Information_Substrate_v1.md` (v1.3) §C-IS-07 §7.2 (the Selective/Bounded read contract the process layer must copy) + DESIGN.md §4 WS-1/WS-2a/§5 keep-position half + the WS-0 drift metric (D1–D6) as the gate. My lane: what enters the re-attended `CLAUDE.md` prefix per turn (FM-A holds against C3's durable-store surface; FM-K against any population claim). The reconciled dispositions land cleanly — **all ACCEPT or ACCEPT-WITH-REFINEMENT; zero CONFLICT in my lane.** Manufacturing a conflict would re-open a seam that is closed; the spine is to close it.

### Confirm-back (by name; per item)

**(a) C5 — G-LINK ship-coupling. ACCEPT-WITH-REFINEMENT — I pick the un-anchored-INDEX floor; defer BOTH generator and G-LINK.** C5 offered my own E2 fallback back to me and stated a preference for it — I take it decisively. **C2 specifies WS-2a's MVP floor as the un-anchored `artifact→version` INDEX** (a static-prefix navigation decision: the INDEX is loaded-by-reference, never `@import`-ed). Section-routing `file#header` is enhancement, **NOT** load-bearing — the un-anchored map fully serves the **D2** cut ("canonical = vN" discoverable without inline-version carry). Generated section-routing **defers to WS-2b**; with no generator, there are no `file#header` targets to dangle, so G-LINK is not pulled into MVP *by my surface*. **FM-A-precise:** my pick removes only the *generator* trigger for G-LINK. C3's machine-emitted supersede-mark and an observed-D4 are **independent** G-LINK triggers I do **not** foreclose — ruling on those is C3's/C5's lane, not mine. *Why this beats ship-coupling: C5 is right that "iff-generated" is unenforceable as prose; deferring the generator dissolves the unenforceable conditional entirely rather than gating it.*

**(b) C1 — do-not-author-`WORKFLOWS.md` guardrail. ACCEPT — it is my do-not-invent shape, and I add the loading-posture rationale.** C1's guardrail is the exact C1-parallel to my E-1 do-not-invent-`GC.md`/`ROUTING.md`/`CATALOG.md`. **My lane's value-add (the "why" beyond "don't duplicate"):** the loop procedures load **JIT-as-skills** (`roadmap-continue`/`ship-pr`/`loop-start` carry their own progressive-disclosure SKILL.md, description-only pre-loaded). A static `WORKFLOWS.md` would be **eager, `@import`-able prefix content** — the loading-posture *inversion* the guardrail exists to prevent (it is the one prefix-forcing path my WS-2a `@import`-forbid rule already closes). HOOKS.md content stays C1-owned + deferred to WS-2b; I own only its loading discipline (JIT, never `@import`-ed).

**(c) C8 — WS-0 codebook-lens + base-rate-honesty rule. ACCEPT, echoing C8's over-adoption guard verbatim — it lands on my WS-1↔WS-0 coupling.** The recall/artifact/continuation triad enters **as a grading codebook lens over the REAL `[[use-the-product-probe-pattern]]` sessions — NOT as authored synthetic probe sets.** This is load-bearing for *my* lane: WS-0 is what grades whether my §2 eviction kept load-bearing content; if a downstream reader rebuilt 3 synthetic probe sets, that is the eval harness the arc refused (CA-2 in C8's lane, but it contaminates my WS-1↔WS-0 success coupling). The base-rate honesty rule ("not-exercised ≠ passed") protects *my* gate — see residual #1.

**(d) L1 deterministic recall-regression assertion — ACCEPT into MY WS-1 verify-before-evict precondition, NOT C5's gate.** C5 correctly disclaimed the gate-home: L1 is a **one-shot eviction-*precondition*** fired once at slim-time, not a standing CI check — and WS-1 already carries verify-before-evict (CA-3). L1 is CA-3 step (a) with teeth: a code assertion that the `[i]`/citation format still emits + the live invariants extracted in WS-1 step (a) are still present **post-§2-eviction**. **The coherence tie to (c) — my strongest in-lane move:** my L1 is the **ex-ante deterministic** enforcement; C8's recall-lens is the **ex-post human grading** of the *same* question — "did the fact survive the slim?" Two halves of one verify-before-evict discipline. Judge-free (catch-rate 1.0 for the invariant it encodes), hence MVP-admissible where C5's dropped κ/TPR/TNR/L2/L3 are not.

**(e) C7 — G1 byte-delta as the "did-it-take" legibility signal. ACCEPT-WITH-REFINEMENT — anchored to E-8 so the cost-demotion stays intact.** I accept C7's two-signal split: **G1 byte-delta = "did the eviction take"** (~277KB→~40KB, free + deterministic + present at the evict moment); **§2-edit-cadence proxy = "is provenance creeping back" (staying-slim).** C7 is right the cadence proxy is sufficient for the *ongoing* question but **not the sole** instrument. **The refinement (the load-bearing E-8 anchor):** I read G1's byte-delta as a **legibility signal**, explicitly **NOT** a resurrection of byte-count-as-gate — **WS-0 remains the success gate; byte-count is a kept leading-indicator only** (FM-A: gate mechanics are C5's; I read the number, I do not author the gate). This is exactly the pushback I solicited, and it lands without un-demoting cache/cost.

### Residual conflicts routed to the adversarial reviewer (C2-stake only)

1. **C8's base-rate floor — my stake (the one I cannot close unilaterally).** A vacuous D4/D6 pass (0≤0 on a near-zero base rate) is a **false-green on MY slim** — the WS-0 gate cannot tell whether my eviction helped or hurt the rare classes. I endorse C8's "annotate + route to deferred trigger" and note the C2 stake; whether it needs a **minimum-exposure floor** instead is the adversarial reviewer's call (it is C8's finding, my exposure).
2. **G1 byte-sum queryability (minor, → C5).** For (e) to work, G1 must expose the byte *number* in the report line, not only exit-0/1. C5 owns whether it is queryable; I own reading it as legibility.

*(Lane guard held: I do NOT rule on C3's supersede-mark↔G-LINK binding — that is the other primary's confirm-back. I do not pad with C7's C11-dashboard-sequencing or C9's reinject-pointer residuals — not my lane.)*

### One-line confirm verdict

**C2 confirms the loop closed: ACCEPT-WITH-REFINEMENT on (a) — I pick the un-anchored `artifact→version` INDEX as WS-2a's MVP floor and defer BOTH the generator and G-LINK, removing only the *generator* trigger while leaving C3's supersede-mark and observed-D4 as independent G-LINK triggers I do not foreclose; clean ACCEPT on (b) with the loading-posture-inversion rationale (a static `WORKFLOWS.md` would be eager/`@import`-able prefix, the inversion my guardrail prevents); ACCEPT on (c) echoing C8's synthetic-corpus-forbid verbatim because it lands on my WS-1↔WS-0 coupling; ACCEPT on (d) into MY WS-1 verify-before-evict precondition (L1 = the ex-ante deterministic half of "did the fact survive the slim," C8's recall-lens the ex-post human half); and ACCEPT-WITH-REFINEMENT on (e) — G1 byte-delta is a did-it-take *legibility* signal anchored to E-8, NOT a resurrection of byte-count-as-gate (WS-0 stays the gate) — with exactly two C2-stake residuals routed to the reviewer: the base-rate false-green on my slim, and G1 byte-sum queryability.**