# Cross-read B2 — C5 closes the unified-gate seam · Round 5 E1 Phase B (seam-routed cross-read DEBATE) — verbatim

> Genuine agent invocation · workflow `wf_8fb2e418-cfe` · 2026-06-04.

---

I now have everything I need. I've fully embodied C5 (the Validation Contract Architect — gate-as-contract owner, FM-A/FM-C boundary discipline, the in-loop/out-of-loop cut), read all five peer positions verbatim, my own A2, the committed DESIGN.md (WS-0 through X-min, §6 deferred tail with G2/G3/G4), and seam #2 as enumerated in the E1 synthesis. This is unambiguously my gate lane to close. No co-primary recusal needed — I own the gate-as-contract; C2/C3 own the cross-ref *form* (FM-C if I author it), C8 owns the population claim (FM-A if I make it), C9 owns the recovery *requirement* (not the gate).

I will not re-open the canon-maximalist bundle. I close at the MVP shape that survives both the solo-developer filter and the WS-0 drift metric.

---

## Cross-read b2 — C5 closes the seam

### Engagement (by name)

**C2 — on the generator↔gate ship-coupling (E2).** C2's E2 directed the question at me and offered a fallback I now *prefer*: *"If you'd rather ship the un-anchored `artifact→version` INDEX as MVP-floor… and defer both generator + G-LINK together, that is cleaner and I prefer it — confirm which."* C2's own E2 already states the MVP floor is the **un-anchored `artifact→version` INDEX**, with `file#header` section-routing as **MVP-iff-generated, else DEFER to WS-2b**. **COHERE — and I ratify C2's preferred shape.** The decisive point: C2's "iff-generated" conditional is *unenforceable as prose* — a generated anchor with no `--check` to re-derive it IS the D4 surface C2 named. So I bind them: **the generator and G-LINK are a single ship-coupling — they land together or section-routing stays deferred and the INDEX ships un-anchored.** This is not me adding a gate to C2's lane; it is me making C2's *own conditional* enforceable. If section-routing is NOT built this arc (the likely MVP outcome), G-LINK is **not** pulled into MVP *by C2's surface* — the un-anchored INDEX needs no link-integrity check (an `artifact→version` map has no `file#header` targets to dangle). Drift cut: D4 (the dangling generated anchor), proportionality-clean because the gate is *refused* until the generator exists. **No CONFLICT.** I author the gate's conformance over whatever anchor-form C2's generator emits; I do not author the INDEX shape (that's C2 — FM-C if I touched it).

**C3 — on binding the supersede-mark schema to G-LINK.** C3 asked the sharp question: *"Do you read the supersede-mark as MVP *with* G-LINK as its co-requisite (they bind), or MVP *without* (accepting the unvalidated-schema risk until an observed D4-via-dangling-supersede incident)? I lean: bind them."* I surfaced the tension C3 missed in Phase A — **C3's `superseded_by: [[slug]]` frontmatter is validated by nothing**, my prior-round F3 "contract-authored-but-unvalidated" reborn, a *fresh* D4 vector (a supersede breadcrumb pointing nowhere reads as "current entry exists"). **REFINE — I split C3's bind cleanly.** C3 is right that the *form* of binding is right, but the binding is **asymmetric**, and the asymmetry is the proportionality call. C3's supersede-mark earns MVP as a **write discipline** *now* (it cuts D1+D4 at near-zero machinery — a frontmatter field + a one-word change to an already-MVP compaction step; I concur it survives my scrutiny). But the *validating `--check` over the `superseded_by` link* is **MVP-on-trigger, not MVP-now**: it is pulled in **iff** the supersede-mark write-discipline ships AND the WS-0 probe shows a D4-via-dangling-supersede incident OR the supersede-mark ships *as the first machine-managed-cross-ref surface*. The reason for the asymmetry: at *first landing*, C3's compaction authors a handful of supersede-marks by hand-hygiene — that is episodic, not a recurring drift surface (same shape as the 3 bucket-C breaks: one-time hygiene ≠ standing gate). **The clean MVP read: supersede-mark MVP-WITHOUT G-LINK; but G-LINK's trigger LIST includes the supersede-mark surface, so the moment supersede-marking becomes machine-emitted (or a dangling-supersede D4 is observed), G-LINK promotes.** This holds C3's "they bind" *intent* — the schema's validator IS the link-integrity `--check` — while refusing to pull a standing gate over a hand-authored handful. **No CONFLICT with C3; I refine "MVP-with" → "MVP-without-but-named-trigger".**

**C8 — on the L1 deterministic assertion's home, and the κ/TPR/TNR drop.** C8 routed it exactly right: *"an L1 code-assertion is judge-free (it survives my 'presupposes-a-judge?' refusal) — so it is admissible at MVP in a way the L2/L3 tiers are not, if you judge it proportionate. I surface it; I do not scope it."* And C8's single-defeater — the entire threshold tier *presupposes an automated judge WS-0 deletes by design* (TPR/TNR **undefined**, κ moot, calibration vacuous) — is the load-bearing argument that makes my DROP unilaterally correct, not a taste call. **COHERE fully, and I CONFIRM the split from the gate side.** I hold FM-A: I do **not** make the population claim (κ governs nothing — that is C8's claim to defend). From the *gate side* I assert only: a deterministic L1 assertion has catch-rate 1.0 for the invariant it encodes — it needs no calibration — so the κ/TPR/TNR thresholds in Dive-5's bundled "`--check` fails on κ<0.70 OR a recall-probe regression" are **un-bundleable**: I keep the deterministic **recall-regression assertion**, I drop the **κ threshold** (L2-judge, DEFER), and `cache-hit>0.60` is **doubly-disqualified** (C2's probe-log #3: no dev-loop home + the drift-reframe moved the gate off cost). **No CONFLICT.** On *where L1 lives* — I rule below it lives at **C2's WS-1 verify-before-evict precondition, NOT my gate** (the seam #2 sub-question), because the assertion is an eviction-precondition fired once at slim-time, not a standing CI gate.

**C9 — on reinject-pointer-resolvability landing on G-LINK.** C9 scoped it precisely and stayed FM-A-clean: *"Does WS-4 G1's `--check` (or a sibling link-integrity check) cover that the reinject pointer's target (`precompact-latest.md` → checkpoint, `roadmap_status.md`) resolves?… I want the recovery-pointer-resolvability owned at the gate, not assumed — but I am NOT authoring the gate; I am scoping the recovery requirement onto it."* **COHERE — I accept the requirement onto G-LINK, with one precision C9 will want.** C9's recovery-via-pointer (R3/F4) silent-fails on a dead link → D5 (bad-resumption) + D2 (wrong-canonical). The reinject pointer (`postcompact-reinject.sh` re-hydrating `roadmap_status.md` + the checkpoint pointer) IS a machine-managed cross-reference — it is *exactly* the surface G-LINK exists to hold. **So: reinject-pointer-resolvability rides G-LINK's `--check` as one of its validated target-classes.** The precision: G-LINK is MVP-**on-trigger**, so reinject-pointer-resolvability is *only gated when G-LINK is live*. If neither C2's generator nor C3's machine-emitted supersede ships, G-LINK is dormant and the reinject pointer is **un-gated** — which means C9's recovery dependency is, at bare MVP, an *assertion in `postcompact-reinject.sh` itself* (the recovery primitive checks its own pointer resolves before re-hydrating, per C9's "must resolve or silent-fail" — that lives in C9's lane as a recovery-completeness check), NOT my gate. **This is a residual I route to the adversarial reviewer (below)** — there is a coverage gap if G-LINK never promotes but the reinject pointer can still dangle. C9 owns the *requirement*; I own *G-LINK's conformance over the pointer when G-LINK is live*; neither of us owns the bare-MVP fallback assertion cleanly, and that seam needs a ruling.

### Reconciled seam disposition

**The agreed MVP shape — what folds into DESIGN.md §6 deferred-tail (NOT §4 MVP):**

**G-LINK (the unified gate) — DEFER → MVP-ON-TRIGGER.** Add to the §6 deferred table, sibling to G2/G3/G4:

> **G-LINK — link-integrity `--check`** (code-fence-aware parser knowing both kebab-case (memory store) and underscore (`.harness/`) paths; validates that `[[wiki]]` + `[md](slug)` + cross-store kebab/underscore refs + generated `file#header` anchors + the reinject-pointer target all resolve; mirrors `substitution_ledger --check` shape, exits 1 on a dangling ref). **Promoted to MVP iff** {C2-E2 generated section-routing ships} OR {C3 supersede-marking becomes machine-emitted} OR {a D4-via-dangling-ref incident is observed at WS-0}. Absent any trigger, the 3 bucket-C breaks (EVID Finding 4) are one-time hand-hygiene, not a standing gate. **Fail-class:** `permanent-fail-exit` / `cause_attribution: contract_violation` (a dangling machine-managed ref is a structural break, not a transient fault — per the locked five-class taxonomy + FM-J). **Composes with never-halt:** CI/PR-time guardrail, never an in-loop session-runtime blocker (the C5↔C9 surface-disjoint resolution, DESIGN.md §7).

**What G-LINK validates exactly (the seam-question (1) answer):** `[[wiki]]` links resolve to a real note; `[md](slug)` links resolve; cross-store kebab↔underscore refs resolve (the parser knows both naming conventions, closing EVID Finding 4's "valid refs read as dead" bucket-A drift); generated `file#header` anchors re-derive against the live markdown headers (C2's surface); the reinject-pointer target resolves (C9's surface). **It does NOT author any link *form*** — C2/C3 own the cross-ref form (FM-C if I did); I own the `--check`'s pass/fail conformance over whatever form they land.

**L1 deterministic recall-regression assertion (seam-question (2) answer) — LIVES AT C2's WS-1 verify-before-evict precondition, NOT my gate.** It is folded into WS-1 as its **deterministic half**: a code assertion that (a) the `[i]`/citation format still emits after §2 provenance eviction, and (b) the live invariants extracted in WS-1 step (a) are still present post-evict. **Rationale for the home:** this is a gate *over C2's eviction event*, fired *once at slim-time* as an eviction-precondition — it is not a standing CI gate over the auto-loaded set (that's G1) nor a cross-ref integrity check (that's G-LINK). It is exactly C9's "recovery-completeness is co-requisite" satisfied deterministically, at the moment of the evict. Placing it at my gate would mis-file a one-shot precondition as a recurring gate. **MVP** (it rides WS-1, already MVP; zero new workstream). I confirm it judge-free (catch-rate 1.0 for the invariant it encodes), hence MVP-admissible where Dive-5's L2/L3 are not.

**κ / TPR / TNR / L2-judge / L3 (seam-question (3) answer) — DROP, and why each is proportionate:** κ is *moot* (Cohen-κ scores human↔judge agreement; WS-0 has no judge per DESIGN.md §3 — C8's defeater); TPR/TNR are *undefined* not relaxed (they score a classifier against ground truth; WS-0 collapses both into Robert — C8); L2 LLM-judge is the CA-2 governance-bloat irony recurring in the eval lane (the eval harness the arc explicitly rejected); L3 IS WS-0 already (Robert's eyes on the D1–D6 matrix). I keep only the deterministic recall-regression assertion (L1). This is the **un-bundling** of Dive-5's "`--check` fails on κ<0.70 OR a recall-probe regression" — keep the deterministic disjunct, drop the judge-calibration disjunct. From the gate side I note (without making the population claim — FM-A) that there is no judge at MVP, so κ governs nothing; **C8 defends WS-0's no-judge simplicity as the population claim.**

**Ownership split (the seam close):**
- **C5 (me):** G-LINK's *conformance behavior* — the `--check`'s pass/fail over all five target-classes; the fail-class (`permanent-fail-exit`/`contract_violation`); the L1 assertion's *judge-free admissibility ruling* (it lives in C2's WS-1, but I rule it admissible-at-MVP).
- **C2 + C3:** the cross-ref *FORM* — C2 owns the generated `file#header` INDEX shape and the un-anchored `artifact→version` floor; C3 owns the `valid_until`/`superseded_by` frontmatter *schema* and the mark-not-drop write discipline. (FM-C if I authored either.)
- **C8:** the *population claim* — that κ/TPR/TNR govern nothing at the human floor; whether WS-0's no-judge gate holds against the cascade pull.
- **C9:** the *recovery requirement* — "the reinject pointer must resolve or recovery silent-fails"; the bare-MVP fallback assertion in `postcompact-reinject.sh` when G-LINK is dormant.

**Residual CONFLICT routed to the adversarial reviewer:** the **dormant-G-LINK reinject-pointer coverage gap.** G-LINK is MVP-on-trigger; if neither C2's generator nor C3's machine-emitted supersede ships and no D4 is observed, G-LINK stays dormant — but C9's reinject pointer can still dangle (the cross-store kebab/underscore drift, EVID Finding 4, is *already live* at HEAD). At bare MVP the only protection is a self-check inside `postcompact-reinject.sh` (C9's lane), not my gate. **Red-team:** is "G-LINK dormant + C9-internal pointer self-check" sufficient, or does the *reinject-pointer-resolvability alone* warrant pulling a minimal G-LINK into MVP-now (i.e. is the reinject pointer a *standing* machine-managed cross-ref that — unlike the episodic bucket-C breaks — never was hand-hygiene)? I lean dormant-is-fine (the pointer set is tiny and C9's recovery primitive should self-check regardless), but C9's "silent-fail" framing + the already-live cross-store drift is the soft spot, and it is the adversarial reviewer's call whether the reinject pointer is the *one* trigger that should fire G-LINK at MVP-now.

### One-line seam verdict

**C5 closes the unified-gate seam: ONE code-fence-aware G-LINK `--check` validates `[[wiki]]` + `[md](slug)` + cross-store kebab/underscore + generated `file#header` + the reinject-pointer target, folded into DESIGN.md §6 as DEFER→MVP-on-trigger (ship-coupled to C2's generator OR C3's machine-emitted supersede OR an observed D4 — and I prefer C2's un-anchored-INDEX-floor / both-defer-together shape); C3's supersede-mark ships MVP-WITHOUT G-LINK but names it as a trigger (asymmetric bind — write-discipline now, validator on-promotion); the L1 deterministic recall-regression assertion is judge-free-admissible and lives at C2's WS-1 verify-before-evict precondition, NOT my gate; κ/TPR/TNR/L2/L3 DROP because every one presupposes a judge WS-0 deletes by design (C8's single defeater); and the one residual I cannot close unilaterally — whether a dormant G-LINK leaves C9's reinject pointer dangling at bare MVP — routes to the adversarial reviewer.**