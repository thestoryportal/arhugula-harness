# Cross-read B1 — C7 closes the unobservable-trigger seam · Round 5 E1 Phase B (seam-routed cross-read DEBATE) — verbatim

> Genuine agent invocation · workflow `wf_8fb2e418-cfe` · 2026-06-04.

---

I have fully embodied the C7 voice and read all peer positions. I have everything grounded — my own A2 position, the convergent peers (C3, C1, C9), and C8's partial-relief. The seam is mine to close: I own the legibility surface. Let me close it.

## Cross-read b1 — C7 closes the seam

### Engagement (by name)

**C3 — "Will WS-5's health line carry the superseded/stale-note count? Else the §6 deferred staleness-scan trigger is a silent-never-fires gap."** → **COHERE on the gap, REFINE the home.** C3 is empirically right and I conceded this in A2: a deferred-until-observed trigger with no observable surface is silent-never-fires (it is exactly my own Round-1 F2 — "an observability requirement is logically prior to any retention policy"). But the count does **not** need WS-5's infrastructure. C3's own supersede-mark frontmatter (`valid_until` null=current, `superseded_by`) makes it a *grep*: `count(notes with valid_until set)`. The home already exists — `session-end-cleanup.sh:49-53` writes a `## MEMORY.md cap` section to `session-end-report.md` *today*. **Drift-class D1; MVP-as-rider.** Honor the gap, refuse the apparatus (CA-2 governance-native-bloat is precisely the WS-5 dashboard the arc deferred). C3 owns the `valid_until` schema + threshold; I own only that the integer is surfaced at the SessionEnd boundary.

**C1 — "my consolidation defer-trigger (count of ≥4-ref-unwritten patterns > 0) carries the IDENTICAL unobservable-trigger risk; it should ride the SAME WS-5 health line as C3's stale-note count — one health line, two trigger-counts."** → **COHERE, and I REFINE the line's home to the SAME existing SessionEnd report (not a new WS-5 surface).** C1 is right that the shapes are structurally identical — both are §6-deferred-tail triggers, both silent-never-fires without a count, both cheaply derivable (C1's is a wiki-link in-degree parse of dangling `[[name]]` refs; EVID Finding-4 computed it once). C1 asserts "one health line, two counts" — I **strengthen** this: it is one line, but the line lives on the *already-MVP SessionEnd report*, not on a promoted WS-5 dashboard. The ≥4-ref-unwritten count folds in as a third grep-derived integer beside C3's two. **Drift-class D4 (memory-pollution: an unwritten ≥4-ref pattern *is* a consolidation that should have fired); MVP-as-rider.** C1 owns firing-site (SessionEnd) + trigger-as-control-decision; I own the count's surfacing. **Zero CONFLICT** — C1's "synchronous-at-SessionEnd, no daemon, no C9" reframe makes the count's home *coincide* with my report's home, which is the cleanest possible composition.

**C9 — "the C3↔C9 T5 split is clean IFF the defer-trigger is observable; BOTH the staleness-scan trigger AND the WS-6-6b D5-resumption trigger hang on a health surface existing. Is a SINGLE periodic memory-health line (orphan% + true-break count + stale-note count) the MVP floor that un-blocks both triggers cheaply?"** → **COHERE on the single-line MVP floor; REFINE which signals are mine to surface vs which read off the gate.** C9 names the load-bearing structural claim: two deferred *recovery* cadences (scan-loop "residual-D1-after-slim" + WS-6-6b "D5-after-WS-1") both die silent without a surfaced count. I **AGREE the single periodic line is the floor** — and it is the right C7 answer because surfacing slow-moving counts at a boundary is squarely my structure-not-content / boundary-emission discipline (§4.5/§4.6 stage-keyed posture). But I **REFINE the D5 half** below per C8's relief: the D5-resumption signal is *different in kind* from the stale-note/orphan counts and may not need the line at all. C9 owns the recovery-completeness *requirement* (the pointer must resolve, the cadence-shape); I own only the count's emission. **No CONFLICT** — C9 explicitly routes count→C7, cadence→C9, which is the clean seam.

**C8 — "PARTIAL RELIEF: the scan-loop's fire-trigger ('residual D1 after slim') is already readable off Arm B's D1 column at the next WS-0 run — so the trigger is not silent-never-fires even without WS-5. Periodic-gate-read is sufficient for the *trigger*; do you want continuous mid-session visibility, or is the periodic WS-0-gate read sufficient?"** → **COHERE, and this is the relief that lets me DROP one signal from my line.** C8 is correct and this is the sharpest narrowing in the seam: the D1-remediation *trigger* (does residual stale-rule drift persist after the slim?) is **already measured** — it is Arm B's D1 column in the WS-0 human-graded matrix, which fires at the next gate run. C8's distinction between *trigger-fire* (periodic, gate-read) and *continuous mid-session visibility* (WS-5) is exactly the fast-vs-slow discipline I drew in A2. **So I adopt C8's relief to keep the line minimal:** the WS-0 D1 column carries the *fire-trigger*; my SessionEnd line carries the *standing inventory* (how many stale notes exist *right now*, so the operator can act before the next full WS-0 run). These are complementary, not redundant — but the relief means **the D5-resumption signal does NOT belong on my SessionEnd line at all.** D5 (bad-resumption) is measured by C9's continuation probe → it is an Arm-B WS-0 column (C8/C9's gate lane), read at the gate run, not a slow-moving inventory count I surface at SessionEnd. **REFINE against C9 here, gently:** C9 asked whether the single line carries "the D5-resumption signal" — my ruling is **no, it reads off the WS-0 gate per C8's relief**, so my line stays a three-count *inventory* line and does not absorb a resumption-drift signal that has no SessionEnd-readable input.

### Reconciled seam disposition

**The agreed MVP shape — exactly what folds into DESIGN.md:**

**Sharpen WS-3a (rider on its already-MVP SessionEnd boundary check). NOT a WS-5 promotion. NOT a new workstream.** The enforced-compaction SessionEnd report (`session-end-cleanup.sh:49-53`, the `## MEMORY.md cap` section) gains **a single memory-health line carrying three grep-derived integers**:

| count | source-grep | drift-class | owner of threshold | trigger it un-blocks |
|---|---|---|---|---|
| `notes-superseded` (`valid_until` set) | C3's frontmatter | **D1** | C3 (retention) / C8 (validity) | §6 deferred staleness-scan loop (*standing inventory* half) |
| `notes-untouched->Ndays` | mtime over `memory/` | **D1** | C3 / C8 | §6 deferred staleness-scan loop (*standing inventory* half) |
| `patterns-unwritten->=4-refs` | wiki-link in-degree parse of dangling `[[name]]` (EVID Finding-4) | **D4** | C1 (consolidation control) | §6 deferred consolidation pass (C1's defer-trigger) |

**One sentence added to WS-3a's "Deliverable-of-THIS-arc" cell:** *"the SessionEnd compaction report additionally surfaces a three-integer memory-health line — superseded-note count + stale-note count (grep over C3's `valid_until` frontmatter / mtime) + ≥4-ref-unwritten-pattern count (dangling-`[[ ]]` in-degree, per C1) — so the §6 deferred staleness-scan and consolidation triggers are observable rather than silent-never-fires. WS-5's continuous mid-session surface + dashboard STAY deferred per §6."*

**MVP-or-DEFER + named trigger:**
- **MVP:** the three-count SessionEnd line (one grep each; zero new hook, zero dashboard, zero new file — rides a check that already runs at SessionEnd).
- **DROPPED-by-C8-relief (read off the WS-0 gate, NOT on my line):**
  - the **D1 scan-loop *fire-trigger*** ("residual D1 after slim") — Arm B's D1 column at the next WS-0 run. My line carries the standing *inventory*; the gate carries the *fire decision*.
  - the **D5-resumption signal** (WS-6-6b's trigger) — C9's continuation probe → Arm B's D5 column at the next WS-0 run. It has no SessionEnd-readable input; it does **not** ride my line.
- **DEFERRED to WS-5:** continuous *mid-session* visibility of any of these counts. **Named escalation trigger (identical to the existing §6 WS-5 trigger):** *only* if the SessionEnd periodic read + the WS-0 gate read together prove insufficient and mid-session blindness causes *observed* drift (D1 or D4) that a boundary-only emission missed. The counts are slow-moving (notes do not go stale, and patterns do not cross the ≥4-ref line, *within* a session) — so boundary-only is correct, and emitting mid-session would be **FM-H (over-instrumentation)** for quantities that cannot change between two UserPromptSubmit turns.

**Ownership split (the seam's load-bearing cut):**
- **C7 surfaces** — the three integers are *emitted on the SessionEnd line*. That is the whole of my lane. I make **no population-level claim** about staleness rate or consolidation rate (FM-K held); I do not author the gate (FM-A held).
- **C3 + C8 own thresholds** — C3 sets the retention threshold (what value of N fires the staleness loop); C8 validates threshold-validity against the WS-0 matrix. C8's relief established that D1/D4 are falsifiable *columns* in Arm B, so the thresholds are gate-checkable, not taste.
- **C9 + C1 own cadence** — C1 owns the consolidation firing-site (SessionEnd) + control-trigger (observed-D4 ≥4-ref count); C9 owns the staleness-scan cadence (WHEN re-validation fires) + the recovery-completeness requirement (the reinject pointer must resolve). Neither cadence is mine.

**The proportionality guard (this line does NOT become the WS-5 dashboard the arc deferred):** the line is **boundary-only** (SessionEnd, where `session-end-cleanup.sh` already writes), **three integers** (not a render, not a stream, not a TUI), **grep-derived** (no model-in-the-loop, no rot-score), and carries **zero continuous-surface affordance**. It is the antithesis of the WS-5 dashboard: WS-5 is a *mid-session continuous render*; this is a *post-session static tally on a report that already exists*. The discriminator C3, C1, C9, and I all converged on holds — does the count *require* WS-5's mid-session injector + dashboard, or can it ride a check already in MVP? **It rides one.** And — consistent with my A2 SET-ASIDE — I explicitly **refuse the embedding-drift/MMD/cosine rot-scores** that BROAD-B offers as a richer health signal: they are *contrary to* WS-0's deliberately binary/human-graded/no-model-judge gate, and a dashboard of continuous rot-scores is exactly the CA-2 governance-native-bloat the arc exists to remove.

**Residual CONFLICT routed to the adversarial reviewer:**
- **One residual, low-stakes (C7↔C11, sequencing-only):** when the deferred WS-5 dashboard *does* land on its §6 trigger, does it **absorb** these three counts or does the SessionEnd report remain their permanent home? My ruling: **SessionEnd report is the MVP permanent home; the dashboard *adds a render* later (C11 UX), it does not replace the boundary emission.** This is C7↔C11 co-primary *if and only if* the dashboard sequences — not an MVP conflict, flagged for the reviewer to confirm the boundary-emission survives the eventual dashboard rather than being superseded by it.
- **One C8-surfaced soft-spot I endorse routing (not mine to solve):** C8's E2 base-rate gap — D4/D6 may pass *vacuously* (0≤0) at the WS-0 gate on a near-zero base rate. My three-count line *partially mitigates D4* (a standing ≥4-ref-unwritten inventory > 0 is a non-vacuous D4 signal even when the gate's D4 column shows 0 incidents), but it does **not** solve the statistical-power problem — it only gives the operator a *second, standing* D4 indicator beside the gate's observational column. I route C8's "annotate-vs-minimum-exposure-floor" question to the adversarial reviewer unchanged; my line is a mitigation, not the fix.

### One-line seam verdict

**C7 closes the unobservable-trigger seam at PROPORTIONALITY: a single three-integer memory-health line (superseded-count + stale-count + ≥4-ref-unwritten-count) rides WS-3a's ALREADY-MVP SessionEnd report (`session-end-cleanup.sh:49-53`) as boundary-only grep riders — un-blocking C3's staleness-scan trigger and C1's consolidation trigger at zero new machinery; C8's relief lets me DROP the D1 *fire-trigger* and the D5-resumption signal from the line entirely (both read off the WS-0 gate's Arm-B columns at the next run, not my slow-moving inventory); thresholds are C3+C8's, cadence is C9+C1's, I own only the emission — and the boundary-only / three-integer / grep-derived / no-rot-score shape is precisely the guard that keeps this from becoming the WS-5 dashboard the arc deferred.**