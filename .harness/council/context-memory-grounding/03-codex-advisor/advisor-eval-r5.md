# Advisor evaluation — Round 5 (evidence-enrichment) · Stage E3 (transcript-aware, in-family half)

> The `advisor()` (stronger transcript-aware Claude reviewer) — the in-family half of the E3 decorrelated pair · 2026-06-04. Advisor also authored the **research primer fed to Codex** (the out-of-family half) — that primer is descriptive-only by design (decorrelation discipline: Codex must not be handed Claude-family conclusions); it lives in the Codex prompt + `codex-eval-r5.md`.

---

## Wiring discipline advisor enforced (decisive)

Codex's value is **decorrelation** (CLAUDE.md §13.1). So: (1) Codex gets the **research primer only** — never advisor's eval, never the council's conclusions; (2) the **adversarial review is withheld from Codex too** (it is also Claude-family — handing Codex its F2-01/02/03 would anchor it). Codex reviews the plan + raw research **cold**; the orchestrator then **compares** Codex's independent findings against the adversarial's — convergence = corroboration, divergence = the real find.

## Advisor's E3 evaluation (Part 2 — kept OUT of Codex's prompt)

**Concur with CLEAR.** The proportionality is **genuine, not soft** — and demonstrated on the artifact, not asserted: 5 of 8 MVP items are riders of existing workstreams, the one new mechanism (G-LINK) is deferred, and the adversarial's own meta-attack ("is '8 sharpens' disguised over-adoption?") was tested and rejected against the artifact. **F2-01/02/03 are dispositioned correctly** (all "name-this," no machinery). No objection to proceeding.

**The probe both the council AND the adversarial under-pressed (the highest-value open question):** *Is WS-0 itself going to be run?* The plan's entire falsifiability rests on a ~15–25-session, two-arm, blinded, human-graded drift probe. The council designed it and the adversarial checked its *internal* soundness (D6 base-rate blindness; gate-accretion) — but **neither asked whether a solo developer will actually execute a probe that heavy**, or whether the slim just ships on faith with a decorative gate. Partially mitigated (Arm A re-uses existing sessions), but Arm B + blinded 6-class grading is real labor. A right-sized **"minimum viable gate" floor** may be the proportionate move the arc's own discipline implies but **did not apply to its own gate.** This is exactly the kind of framing blind-spot a cold Codex read can catch — so it was deliberately NOT pre-empted in Codex's prompt.

**Two lighter notes:** (a) the cost axis (cache-detonation × ~25 under loop mode) was correctly demoted from *gate* to *leading-indicator*, but it is the more *reliably measurable* benefit (git-derivable) — keep it a **reported** win, not an invisible one. (b) Most external-canon claims are unverified, but the plan deliberately doesn't lean on them (it set most aside) — so if Codex flags "these aren't verified," the correct answer is "contained by the refusals," not a defect.

**The signal to weight:** where Codex **disagrees** with advisor or with the adversarial. Three-way convergence on CLEAR → ship to the consolidated council reconcile with confidence. Any place Codex lands somewhere the two Claude-family reviewers didn't → that is the real find; dig there before reconciling.
