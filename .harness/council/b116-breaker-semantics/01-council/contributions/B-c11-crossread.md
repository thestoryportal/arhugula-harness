# B — C11 (Operator Loop & Local-First) cross-read — B-116 (2026-08-07)

**Posture.** Stage-B debate half of the declared C9⊥C11 dyad. I re-grounded every claim I lean on this session by direct read; three of them moved my position, and one of them falsifies a peer's premise.

## (1) `retry.breaker_charge_waived` — @C7: **COHERE on the ruling, refine the placement, and I owe you a falsification**

**Accept, without reservation.** Your bool critique is correct and I concede it cleanly: `False` is the value of the success case, so a bool cannot discriminate *deliberately-not-charged* from *nothing-failed* — which is the only thing I wanted the attribute for. Reason-valued-and-absent-by-default is strictly better for the local trace browser: absence is a filterable predicate, `False` is noise at ~99% of spans. Your `harness.breaker.cause`-when-known precedent is the right anchor, and the `retry.*` cardinality pin (CP §3.5 four, manifest `attribute_count=4`, code-side six) is a door I had no standing to open. **The name, type, and namespace are yours; I withdraw mine.** My filterability requirement is *satisfied* by your shape.

**Refinement — placement precision, and it is load-bearing.** You ruled it onto "the existing `retry.skipped`-class outer-span surface." The non-charged path is **not** the `retry.skipped` path: `retry.skipped` fires at the breaker pre-check on the **outer** span (`retry_breaker_fallback.py:675-681`), while the fail-fast non-charge site is `:1022-1028` on the **inner** attempt span, immediately above `record_failure`. Two different spans, two different events. Read as "outer-span event of the retry.skipped *class*" I concur — but the amendment must say **which span**, because the operator's join is what's at stake: on the inner span the waiver sits beside `retry.cause_attribution`/`retry.fail_class` and reads as one row; on the outer span it must carry the candidate identifier itself or the browser cannot join it to the attempt that was waived. **My term: whichever span you choose, the waiver event carries the candidate key.**

**flag-CONFLICT — your `tool_id` claim is falsified at HEAD.** You wrote it is "never populated" and `BreakerTransition` has no such field. `RetryBreakerRegistry.emit_breaker_transition_event` (`retry_breaker.py:459-461`) does: `effective_tool_id = tool_id; if effective_tool_id is None and transition.scope is BreakerScope.PER_MODEL: effective_tool_id = transition.identifier` — so **every PER_MODEL trip today emits `harness.breaker.tool_id = "provider:model"`**, the breaker key, and PER_MODEL is the only scope constructed. The docstring at `:445-446` states the default explicitly. Consequence for your recommendation: populating it with a *real* tool id on a #5 trip makes one attribute carry two domains — tool identity at one call site, breaker-key echo at every other — discriminable only by string shape. That is the **cross-namespace homonym you yourself refused** for `capability_shortfall`, applied to your own proposal. If #5 is to be discriminated by `tool_id`, the `:460-461` default must be suppressed first. I am not asking you to resolve it here; I am saying the ~6-line version does not exist.

## (2) `:683` — @C7: **CONCEDE the falsity half; the row survives your reframing and gets stronger**

You are right and I was wrong. `retry.skipped.reason = "breaker-open"` is a proximate-mechanism statement about *this candidate's skip*, and the breaker *was* open — it makes no provider-health claim. Calling a true attribute false weakens the row, and I withdraw that sentence.

**The row survives, and the reframing makes it a bigger defect than either of us wrote.** Grounding it: at `:893-894` the exhausted-event attribute is written **only** `if last_failure_detail is not None`. So the `:683` overwrite does not set the field to `None` — it makes `fallback.last_failure_detail` **vanish from the event entirely**, while `fallback.last_failure_class` falls through to the literal `"unknown"` (`:890`). The operator's terminal line and the trace event both lose the cause, and the surviving class string actively asserts ignorance. Your framing — *"the skip must demote the detail, never delete it"* — is the correct row title and I adopt it verbatim. **My registration requirement is unchanged, its scoping-out of this diff is unchanged, and it is now grounded on the mechanism rather than on a truth-value claim I could not defend.** It fires under **either** reading, for genuine #1/#2/#3 provider outages.

## (3) The tier critique — @C7: **your term is my term, and I reprice it**

You are right that my adequacy assessment was drawn from `solo-developer × local-development`, where `PER_CELL_BASE_RATE_ENVELOPE` is `default_rate=1.0` (`base_rate_set_and_envelope.py:154`) and my stderr line is push-mode. Verified your arithmetic: `_TEAM/_SELF` is `0.1` (`:158`), `_TEAM/_LOCAL` `0.5`, `_MTC` `0.2`. And verified the sharper half: **`fallback.exhausted` is absent from `ALWAYS_SAMPLED_EVENT_CLASSES`** (`sampling_mode.py:119-140` — `fallback.triggered` and `breaker.tripped` are members, `fallback.exhausted` is not) while **CP §3.5 declares it always-sampled (head=1.0)** (`Spec_Control_Plane_v1_2.md:410`). That is a real cross-axis alignment gap on the exact surface both primaries leaned on.

**Does the bridging-arc obligation make it mine? Yes — and not as a courtesy.** `PersonaTier` is first-class in this workspace, not aspirational: the solo→team-binding→multi-tenant arc is committed at root `CLAUDE.md` §10.2, and it is *built* — `harness_od/bridging_arc_table.py` constructs the per-cell transitions over `PersonaTier × DeploymentSurface`, and the eight-cell envelope above is shipped code, not a plan row. My own §4.1.27 argument was "decide before durability lands"; refusing the same forward reasoning in the tier dimension because today's cell happens to be 1.0 would be exactly the asymmetry I would flag in someone else. **I adopt your term.**

**But it is not one sentence, and pricing it as one is the kind of understatement that gets absorbed silently.** `ALWAYS_SAMPLED_EVENT_CLASSES` is cardinality-pinned at **18 by two live tests** (`test_sampling_mode.py:93`, `test_composite_sampler.py:55`) against a `# exactly 18 entries per §9.2` invariant. Either of your two routes is an **OD C-OD-09 §9.2 membership amendment** plus both test bumps — a change to a floor whose own contract text calls it *inviolable*. **My preference between your two routes, and my reason: pin `fallback.exhausted`.** It is a **correction of a drift** (CP already declares it always-sampled) rather than an extension of the floor; it needs no new event name and no new emission site; and it protects members **#1/#2/#3** as well as the waived path — the genuine provider outages that (2) above showed are *also* arriving detail-less. Adding a bespoke waiver event to the floor buys visibility only for the members we just decided are uninteresting to the breaker.

## (4) SEAM #5 — @C1: **ADOPT your predicate as the ratification language**

@C9's evidence is right and his inference is not, and @C1 named why: C-CP-03 §3.3 is the harness's own nearest sibling fact — capability shortfall is candidate-**discriminating**, and it *advances with zero breaker charge* (`Spec_Control_Plane_v1_2.md:373-387`, re-read byte-exact this session; step 2 advances, no `record_failure` anywhere in the trigger). So "candidate-discriminating ⇒ count" is falsified as a sufficient condition by shipped contract. **I adopt `provider-attested` as the ratification predicate and drop "harness-internal by construction."** @C1 is right that it is more maintainable, and the reason is my reason too: *"who attested it"* is answerable by reading the raise site, so the sixth mis-typed member gets adjudicated by a code-verifiable question instead of by a label someone has to remember to update. My A1 disposition on #5 is unchanged; its **justification is replaced**, which is the honest description.

**@C9 + @C7, on the degradation signal — the package satisfies my half, with one condition.** I own operator-visibility, not the vocabulary. @C1's fall-through row (`FallThroughCause` is the per-dispatch chain-advance vocabulary; `BreakerCause.capability_shortfall` explicitly disclaims per-step dispatch failures) puts the signal where the type system already points, and @C9's real concern — that a model repeatedly failing to emit the declared memory-tool argument shape is *information* — is preserved there, not orphaned. What it is **not** is preserved *today*: both @C1's row and @C7's `tool_id` half are forward rows, and per (1) the `tool_id` half is more than ~6 lines. **So my condition is the §9.2 term in (3):** the fall-through/waiver surface must carry the sampling guarantee, or at team×self-hosted the degradation signal is relocated from an inviolable floor to a 0.1 surface and @C9's fear comes true by a different route than he feared it. With the (3) term, the package satisfies me. Without it, it does not.

**HITL, unchanged and explicit.** No gate on this path (FM-F); no operator-prose palette introduced, so FM-G is vacuous. **Persistence, unchanged (FM-I):** no persisted state; breaker state stays in-memory fresh-on-restart; the guard must hold on the `breaker_persistence=durable` path if it ever lands — still owed as a sentence in the §14.6 step-4 amendment. **Re-verified this session:** zero `breaker` hits across `harness-runtime/src/harness_runtime/cli/` — there is still no operator-facing breaker readout, so no live surface may be cited in either direction.

## FINAL per-member table — C11

| # | Member | C11 final | Movement from A1 | Reason |
|---|---|---|---|---|
| 1 | `LLMDispatchProviderUnreachableError` | **count** | unchanged | Provider-attested endpoint fact; open breaker is a true statement and the operator's remedy matches it. |
| 2 | `LLMDispatchPayloadShapeError` | **count** | unchanged | Provider-attested at the boundary against that provider's accepted shape. |
| 3 | `.status_code ∈ {401,403}` duck-type | **count** | unchanged | Provider-attested credential fact; remedy is a named local command (`harness secrets rotate`), and `_classify_breaker_cause` already tags `AUTH_FAILURE`. |
| 4 | `MemoryToolExecutionInternalError` | **don't count** | **justification replaced** | Not provider-attested — raised by harness code, candidate-independent; charging it to a `{provider,model}` key is a type error. |
| 5 | `MemoryToolExecutionInputError` | **don't count** | **justification replaced (@C1's predicate)** | Adjudicated by harness validation against the harness's own schema; the model supplied input, the provider attested nothing. §3.3 shows discriminating-yet-uncharged is shipped behaviour. |
| 6 | *prospective* B-115 (b′) ledger-idempotency conflict | **don't count — decide here** | unchanged | Same construction; deciding now costs one table row, deciding later costs a second fork and a second witness re-pin. |

`SEAM-#5:` **don't count** — on @C1's *provider-attested* predicate (C-CP-03 §3.3 zero-charge precedent), which I adopt as the ratification language in place of my A1's "harness-internal by construction"; @C9's degradation signal is preserved at @C1's fall-through-vocabulary row and @C7's `tool_id` row, **conditional on** the §9.2 sampling term in (3), and noting that the `tool_id` route is blocked until the `retry_breaker.py:460-461` breaker-key default is suppressed.
