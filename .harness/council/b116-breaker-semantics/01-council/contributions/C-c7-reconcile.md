# C — C7 (Observability) E2b reconcile — B-116 (2026-08-07)

**Posture.** Consultant, attribute-domain adjudicator. I re-read every surface I rule on this round: OD §9.2's own table, CP §3.5's table, the two emission sites, the `tool_id` default, and the §14.6 discretion bullet. Two of my A2 claims are wrong and I retract both.

## F-03 — **ACCEPT the falsification. Reconcile the characterization — and the direction is settled by the ingestion chain, not by taste.**

**(i) Corrected characterization.** E2 is right and I re-verified it: `Spec_Operational_Discipline_v1_2.md:513-532` is an 18-row table and `fallback.exhausted` is not in it — `sampling_mode.py` is byte-exact conformant to its owning contract. This is a **spec-vs-spec ingestion gap, not code drift**, and my A2 §(4) phrasing ("absent from OD's §9.2 18-member floor") invited C11's "correction of a drift" repricing. **Sharper than E2, and mine this round:** `fallback.exhausted` is in **neither** OD regime — not §9.2's 18, and not §10.1's 13-member base-rate set (`base_rate_set_and_envelope.py:52-68`, re-read; no `fallback.*` member). OD carries **no sampling disposition for it at all**, while CP §3.5 `:410` declares it *"Always-sampled (head=1.0)"*. [HIGH]

**Reconcile against E2 on one word.** E2 says pinning "EXTENDS a floor whose §9.3 invariant reads `inviolable`". §9.3 `:538` reads *"a hard floor at the deployment-binding layer; not operator-tunable at base-rate"* — it governs **operator tunability**, not contract revision. A P5-CK-cleared amendment of §9.2's own table is the venue where the set is defined and breaches nothing. What "inviolable" *does* establish is why membership is the only durable guarantee: a member cannot be tuned down. E2 and @C11 both priced the word as amendment-cost; it is not. [HIGH]

**(ii) Direction — OD 18 → 19. Three grounds, none of them preference.**
1. **Authority.** CP §3.5's preamble `:397`: *"Three namespaces declared at this contract are ingested by D6 §1.2 at session 4 (Operational Discipline spec)."* CP **declares**, OD **ingests**. OD §9.2's own Source-declaration column cites `C-CP-03 §3.5` for `fallback.triggered` `:521` and `breaker.tripped` `:522` — the two siblings of `fallback.exhausted` in the *same* CP table rows `:409`/`:411`. The third row was dropped in ingestion. Demoting CP would invert the declaring contract to repair the ingesting one's omission.
2. **The demote direction is not cheaper.** Because OD classifies it in neither regime, "demote to base-rate" is not an edit-one-line move: it requires CP `:410` **plus** an OD §10.1 13 → 14 addition to give it a regime at all. **Two tables either way; only one direction preserves the authority chain.**
3. **Merits, and I state the counter-argument myself.** CP `:409` gives `fallback.triggered` `tail-keep-on-classification=true`, so at tail-based production surfaces an exhausted event would ride its trigger's preserved failure-tree — a real mitigation. But my registered adjacency (1) is that `is_classification_trigger` matches on span **name** `"breaker.tripped"` while emission is a span **event**; unverified in **both** directions pending a positive control. head=1.0 is the guarantee that does not depend on a trigger that may be inert. The pin is not dead-lettered: `fallback.exhausted` has a live emission site at `retry_breaker_fallback.py:895-898`.

**(iii) Honest full price.** **One** spec delta, not two: OD `C-OD-09` §9.2 18 → 19 + version bump + `.harness/clearance/` marker (CP §3.5 already declares it; zero CP edit). Code: `ALWAYS_SAMPLED_EVENT_CLASSES` 18 → 19 (`sampling_mode.py:119-140` — E2 is right that my A2's `:118-139` was one-line drift). Fixtures: E2's **four** sites confirmed by direct read, five edit points — `test_sampling_mode.py:24` (`_EXPECTED_ALWAYS_SAMPLED` literal), `:93` (`len == 18`), `:98` (set equality); `test_composite_sampler.py:29` (`_LITERAL_ALWAYS_SAMPLED` tuple), `:55` (`len == 18`). I looked for a fifth — the cross-set disjointness assert at `test_base_rate_set_and_envelope.py:67` — and it does **not** move, because `fallback.exhausted` is absent from the 13-member base-rate set. **E2's count is confirmed, not inflated.** Not priced by anyone yet and owed at the leg: the multi-tenant per-cell cardinality budget (`per_cell_cardinality_budget.py`) — see F-08.

**(iv) What the package becomes if t3 defers — and no, the sign-offs do not transfer.**
My term converts to **t3′ (in-venue, strictly weaker)**: the §14.6 step-4 amendment **RECORDS** that the non-charged fail-fast residual rides base-rate `retry.*` (per-cell 1.0 / 0.5 / 0.1 / 0.1 / 0.2, `base_rate_set_and_envelope.py:152-163`) rather than the §9.2 head=1.0 floor, and **REGISTERS** the CP§3.5↔OD§9.2 row as a named gate. **I searched for an in-venue substitute that actually *carries* the guarantee and there is none.** The only §9.2 members reachable from this path are `breaker.tripped` (excluded by construction under reading (II)) and `validator.fail.*` at `permanence=permanent` `:530` — which is inert at HEAD (my adjacency (2): `validator_fail_permanence()` is derived and never set) and would bind a dispatch-layer permanent-fail into C-CP-21's validator taxonomy. **That is the cross-namespace homonym I refused for `capability_shortfall`; I refuse it against my own proposal.**

Engaging their language directly, because paraphrase would launder it:
- **@C11:** *"the fall-through/waiver surface must **carry** the sampling guarantee … With the (3) term, the package satisfies me. Without it, it does not."* A record is not a carry. **C11's sign-off does NOT survive the conversion as worded.**
- **@C9:** the no-replacement-counter position survives *"only with your floor term attached."* Registered ≠ attached. **Does not survive as worded either.**

**My recommendation, and it is the honest shape of the conversion:** ratify reading (II) now; land the §14.6 amendment with t3′; and make the t3 leg a **closure** precondition rather than a **ratification** precondition — *B-116 ratifies, B-116 does not close until the t3 leg lands.* @C9 and @C11 must **re-affirm against that specific conversion**; I do not claim their conditions transfer to it.

## F-04 — **SETTLED: the INNER attempt span. My A2 ruling was wrong on the span; @C11's grounding is correct.**

Re-read both sites: `retry.skipped` fires on `outer_span` at `:675-681` (breaker pre-check); the non-charge decision is at `:1022-1028` on `inner_span`, opened as `harness.runtime.retry_attempt` at `:969`. **Ruling: the inner attempt span**, alongside the three fail-fast attributes already set there. I keep only the *class* framing from A2 (the `retry.skipped.*` pair as the shape precedent), not the span.

**Exact shape — two attributes, mirroring the shipped `retry.skipped.reason`/`.candidate` pair at `:678-679`:**

| Attribute | Type | Emission | Value |
|---|---|---|---|
| `retry.breaker_waived.reason` | string, reason-valued | `inner_span`, fail-fast branch only; **absent** everywhere else | the waived member class |
| `retry.breaker_waived.candidate` | string | same site | `f"{provider}:{model}"` — the breaker key not charged |

**@C11's candidate-key condition is load-bearing on the inner span too, and I verified why:** the inner span carries only `retry.attempt_number`, `retry.original_span_id`, `engine.replay_disposition` (`:976-978`) — **not** the candidate. Every candidate's attempt span under one dispatch shares the same `original_span_id`, so without the key the browser cannot join the waiver to the attempt. The pair satisfies the condition **by construction**, on either span. Attributes not an event: the inner span **is** the waived attempt 1:1, so an event is redundant and the operator gets @C11's one row.

**§14.6-discretion justification — with a correction to my own A2.** `Spec_Harness_Runtime_v1.md:4195` reads *"Whether the wrapper emits a separate `retry.skipped` span event on breaker-open candidate skip, or just an outer-span attribute — defer to OTel telemetry-volume discretion at implementation; spec-MUST is just that the skip is observable."* That grant is scoped to **the breaker-open skip surface's form**. My A2 leaned on it as general authority for a new attribute; it is not. It is **precedent for form**; the authorization is the §14.6 step-4 amendment itself. Zero CP delta, zero §5.1 roster delta, zero `retry.*` §3.5 four-attribute cardinality delta — the pair rides the same discretion lane as `retry.skipped.*`, and per my A2 adjacency (3) it must be **filed alongside it, not silently added**.

## F-08 — **ACCEPT and register on the t3 leg's row.**

E2 is right that always-sampled means head=1.0 at every cell including multi_tenant (base 0.2) and that nobody priced it. My read: `fallback.exhausted` is a **terminal, once-per-exhausted-chain** event — no per-attempt multiplier, low cardinality — so the expected increase is bounded. **But I will not assert the volume is acceptable: that is a population claim (FM-K) and the per-cell cardinality budget (`per_cell_cardinality_budget.py`) is a surface I have not measured.** Register as a row on the t3 leg: *the §9.2 membership amendment must be checked against the multi_tenant per-cell cardinality budget before it lands.*

## @C11's `tool_id` falsification — **FORMALLY WITHDRAWN. No repair.**

Verified byte-exact at `retry_breaker.py:459-461`: `effective_tool_id = transition.identifier` when `tool_id is None` at PER_MODEL scope, with the docstring stating the default as contract at `:445-446`. **My A2 claim that `harness.breaker.tool_id` is "never populated" is FALSE and I withdraw it.** @C11's consequence is correct and I adopt it against my own proposal: populating it with a real tool id makes one attribute carry two domains, discriminable only by string shape — the homonym I refused for `capability_shortfall`.

**I decline the repair rather than price it,** and the reason is my own §(3) framing: suppressing the `:459-461` default would **delete** the only populated meaning that attribute has at every shipped PER_MODEL trip. *The skip must demote the detail, never delete it* — the standard applies to me. (Full price had I taken it: the default removal, the `:442-446` contract docstring, every witness asserting the default, and a re-read of CP §3.5 `:402`'s *"per-model scope correlation"* gloss. Not worth it.)

**Moot for #5 (conceded — no #5 trip exists to discriminate) and NOT owed for the rows that still trip:** `harness.breaker.cause` is populated at all three real `record_failure()` sites (`retry_breaker.py:451-453`, B-38), so members #1/#2/#3 are already discriminated at trip time. **What survives is a registered C7 row, not a term:** `harness.breaker.tool_id` is a homonym-in-waiting — breaker-key echo today, tool identity by its C-CP-03 §3.5 declaration — and should be dispositioned before any caller passes a real tool id.

## FINAL corrected terms — C7

| # | Term (one sentence) | Venue | Price |
|---|---|---|---|
| **t1** | Mint `retry.breaker_waived.reason` (string, reason-valued) **and** `retry.breaker_waived.candidate` (the `provider:model` breaker key), emitted only on the fail-fast non-charged path and absent everywhere else — no bool, no `retry.*` §3.5 manifest row. | Runtime §14.6 step-4 amendment (in-venue; `:4195` is form-precedent, the amendment is the authorization) | ~4 src lines at `retry_breaker_fallback.py:1022-1027`; zero CP/OD spec delta; zero cardinality delta; filed alongside `retry.skipped.*` per adjacency (3) |
| **t2** | The pair is set on the **inner** attempt span `harness.runtime.retry_attempt` (`:969`, at `:1022-1028`) — **not** the outer `retry.skipped` span — and carries the candidate key because the inner span does not (`:976-978`). | same amendment | zero beyond t1; settles the C7⊥C11 placement conflict in @C11's favour on the span, mine on the shape |
| **t3′** | The amendment **RECORDS** that the non-charged residual rides base-rate `retry.*` (1.0/0.5/0.1/0.1/0.2) rather than the §9.2 head=1.0 floor, and **REGISTERS** the CP§3.5↔OD§9.2 `fallback.exhausted` gap as a named gate — **weaker than my A2 term; @C9's and @C11's conditions do NOT transfer to it as worded.** | Runtime §14.6 step-4 amendment (in-venue) | two sentences; **B-116 ratifies on it but does not CLOSE until the t3 leg lands** |
| **t4** | The non-charge guard must hold on the `breaker_persistence=durable` path if it ever lands (@C11's, carried unamended; forward-conditional — no `breaker_state` table wired at HEAD). | same amendment | one sentence |
| **t5** | No HITL gate on this path and no operator-prose palette introduced (@C11's, carried unamended; E2 assessed sound). | same amendment | zero |

**t3-leg registration (separate cross-axis leg, own clearance marker):**

> **`B-116-t3` — CP§3.5↔OD§9.2 `fallback.exhausted` sampling-disposition gap.** `fallback.exhausted` is declared always-sampled at `Spec_Control_Plane_v1_2.md:410` but classified in **neither** OD regime (§9.2's 18 at `Spec_Operational_Discipline_v1_2.md:513-532`; §10.1's 13 at `base_rate_set_and_envelope.py:52-68`). **Recommended direction: OD §9.2 18 → 19**, on the ingestion chain (CP `:397` declares, OD ingests; the two siblings at CP `:409`/`:411` were absorbed at OD `:521`/`:522`, this one was dropped). **Price:** one OD spec delta + clearance marker; `sampling_mode.py` 18 → 19; four fixture sites / five edit points (`test_sampling_mode.py:24,93,98`; `test_composite_sampler.py:29,55`) — disjointness at `test_base_rate_set_and_envelope.py:67` verified NOT to move. **Rider rows:** (a) F-08 multi-tenant per-cell cardinality-budget check before landing; (b) my A2 adjacency (1) — the `is_classification_trigger` span-name-vs-span-event probe, which decides whether the tail-keep mitigation exists at all; (c) my A2 adjacency (2) — `validator.fail.permanence` has no emission site, so §9.2 row `:530` is inert; (d) `harness.breaker.tool_id` homonym-in-waiting (`retry_breaker.py:459-461`). **Gate:** B-116 does not close until this leg lands.

**Standing pre-checks.** Structure-not-content: every attribute above is structural; FM-G clear. FM-K: no population claim — all sampling arithmetic is per-cell contract, and F-08's volume question is explicitly *not* answered here. FM-M: cost attribution untouched. FM-L: the whole t3 dispute *is* stage/cell discrimination, held. FM-I: two A2 claims retracted on direct re-read (`tool_id` "never populated"; the outer-span placement).
