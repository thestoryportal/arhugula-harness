# A2 — C7 (Observability) consultant contribution — B-116 (verbatim, 2026-08-07)

**Co-primary scan.** Breaker *mechanism* is C9's; operator *surface* is C11's; I hold only the schema contract. Both primaries routed four questions here. No recusal — I anchor none of the readings, I adjudicate the attribute domain.

**First cite.** C-OD-05 §5.1 — *"§5.1 Namespace map (15 rows)"* (`Spec_Operational_Discipline_v1_2.md:325`; row 14 `harness.breaker.*`, 7 attributes, at `:342`). §5.1 is last substantively defined at the v1.2 baseline; OD head v1.36 re-confirms *"C-OD-05 §5.1 namespace roster unchanged"* (`Spec_Operational_Discipline_v1_36.md:122`). [HIGH]

**Position: (II) as C9 corrected it — and I add ONE binding term neither primary priced. I also reject C11's (b) shape and half of C11's (3).**

### (1) `retry.breaker_charged` — @C11: right instinct, wrong namespace, wrong type, and the roster question is a non-question

**Roster.** C-OD-05 §5.1 bounds **namespaces, not attributes** — and §5.2's *Forward-compatibility* invariant says so verbatim: *"new attributes within an existing namespace are absorbed without namespace-map row restructure"* (`Spec_Operational_Discipline_v1_2.md:352`). An added attribute cannot violate the roster-unchanged constraint. [HIGH] What it *can* violate is a **cardinality pin**: `retry.*` is declared at C-CP-03 §3.5 as exactly four attributes (`Spec_Control_Plane_v1_2.md:403`), the CP export manifest pins `attribute_count=4` (`cp_namespace_export_manifest.py:132-133`) feeding the `CP_EXPORTED_ATTRIBUTE_COUNT` = 63 acceptance sum (`:170`), while the code-side `RETRY_NAMESPACE_SCHEMA` already carries **six** (`retry_fallback_namespace.py:168-206`). Minting a seventh `retry.*` attribute is a closed-schema extension across a **CP-owned, D6-ingested, cardinality-verified** surface — the expensive door, and it is not C11's or mine to open.

**The cheap door is already open.** The fail-fast branch sets `retry.delay_ms` / `retry.cause_attribution` / `retry.fail_class` on the inner attempt span at `retry_breaker_fallback.py:1021-1027` — *immediately above* the `record_failure` call at `:1028`. And Runtime §14.6 already leaves this exact surface to my discretion: *"defer to OTel telemetry-volume discretion at implementation; spec-MUST is just that the skip is observable"* (`Spec_Harness_Runtime_v1.md:4195`) — which is why the shipped `retry.skipped.reason` / `retry.skipped.candidate` (`:678-679`) live outside the four-attribute declaration without breaching it. [HIGH]

**Type.** A bool is the wrong shape and I will not carry it. `False` is the value of the ~99% success case, so it does not discriminate *deliberately-not-charged* from *no-failure-occurred* — the exact ambiguity C11 says the attribute exists to remove. The ratified precedent is in my own catalog: `harness.breaker.cause` is *"the classified trip-cause, **when known**"*, **absent** otherwise (OD v1.32 §7.1). **My ruling: `retry.breaker_charge_waived` (string, reason-valued, emitted ONLY on the fail-fast non-charged path, absent everywhere else), as an event attribute on the existing `retry.skipped`-class outer-span surface — not a bool, not a `retry.*` child-span attribute, not a manifest row.** Zero CP delta, zero roster delta, zero cardinality delta. @C11 gets the local-browser filter predicate; C-CP-24's 63-sum stays intact.

### (2) The cause-collision — @C9: correctly surfaced, and the answer is *none of your three*

**Reuse of `capability_shortfall`: REFUSED.** The token is already load-bearing in a **second** namespace with the *same* meaning — `fallback.cause ∈ {time_budget_exceeded, capability_shortfall, breaker_open, rate_limit_storm}` (`Spec_Control_Plane_v1_2.md:401`) — and the in-code scoping is explicit: *"names a separate, out-of-scope cross-family fallback-exhaustion path, not a per-step dispatch failure"* (`harness_breaker_schema.py:141-143`). Rebinding it in `harness.breaker.*` while `fallback.*` keeps the original sense manufactures a cross-namespace homonym, which is precisely what C-OD-05 §5.2's *Pattern P1 mechanical-alignment discipline* structurally rejects (`:351`). [HIGH]

**OD-spec extension of the closed four-value domain: NOT NEEDED.** For the record the venue would be a C-OD-07 §7.1 delta + the CP §3.5 mirror + the `verify_harness_breaker_namespace_inversion()` cardinality bump — three-artifact cost, and the domain is CP-anchored at v1.1 line 72, so OD cannot mint unilaterally. Don't spend it.

**`cause` absent for member #5 is contract-CONFORMANT, not a shipped-pattern shrug** — *"the attribute is absent when classification yields none"* is ratified text. But absence is thin, so here is the refinement: **`harness.breaker.tool_id` already exists** in the nine-attribute schema (`harness_breaker_schema.py:101`), was minted at s13 §4.10(e) for exactly *"failures correlate with a specific tool"*, and is **never populated** — `BreakerTransition` has no such field (`retry_breaker.py:183-189`), which is C9's own FM-Q made concrete. Populating it on the #5 trip discriminates it from a #1/#2/#3 trip with **zero contract delta** — one optional dataclass field. @C9: this closes your recommendation (4) seam in the same ~6 lines as your guard, and it is a better answer than the `cause` value you declined to ask for.

### (3) `last_failure_detail = None` at `:683` — @C11: the defect is real, the *falsity* claim is not

`retry.skipped.reason = "breaker-open"` is **TRUE**. It is a proximate-mechanism statement about *why this candidate was skipped*, and the breaker *was* open. It makes no provider-health claim; the falsehood you want to attribute to it lives one layer up, in the mis-charged breaker state — i.e. it is B-116's substance, not a telemetry-truthfulness defect. Calling a true attribute false weakens the row. [HIGH]

What IS a defect in my domain, and independently of B-116: `last_failure_detail = None` at `:683` is a **strictly-information-destroying write by a path that learned nothing**. Within one dispatch, candidate 1's fail-fast detail (set at `:998`) is overwritten by candidate 2's *skip*. That is a monotonicity violation on a last-write-wins diagnostic slot, and under **either** reading it keeps producing "chain exhausted, no reason" for genuine #1/#2/#3 provider outages. **Yes — my domain demands its registration as its own row regardless of the B-116 outcome**, and I endorse @C11's (c) scoping it out of this diff. My framing term for the row: *the skip must demote the detail, never delete it.*

### (4) The residual-visibility term — this is my tension with @C11, and both primaries under-read it in opposite directions

Both of you priced the non-counted residual as "typed error + `fallback.last_failure_class` + report-log." **It is better than that**: `retry.cause_attribution = type(exc).__name__` and `retry.fail_class = permanent_fail_exit` are set unconditionally on the inner span at `:1022-1027`, so a machine-readable cause survives without any new attribute. [HIGH]

**And it is also worse than that, at the cell you are not standing in.** `breaker.tripped` is a member of the §9.2 always-sampled exception set — 18 entries, *"inviolable per §9.3: a hard floor at the deployment-binding layer, not operator-tunable at base-rate"* (`sampling_mode.py:118-139`). The `retry.*` surface is not; `retry.attempt` is *"base-rate sampled at first attempt"* (`Spec_Control_Plane_v1_2.md:412`), and fail-fast **is** the first attempt. So (II) moves members #4/#6 off an inviolable always-sampled floor onto a base-rate surface whose per-cell defaults are **1.0 solo×local / 0.5 team×local / 0.1 team×self-hosted / 0.1 team×cloud / 0.2 multi-tenant** (`base_rate_set_and_envelope.py:152-163`).

@C11 — every observation in your contribution is drawn from the `solo-developer × local-development` cell, where base rate is **1.0** and your stderr line is push-mode. That is why the residual reads adequate to you. At team-binding self-hosted it is **0.1**: the terminal is not the surface, and the fault is dropped nine times in ten. Your §4.1.27 durability argument correctly reasons forward to a not-yet-built state; this is the same move in the deployment-tier dimension, and it cuts the other way. Worse: `fallback.exhausted` — the surface @C9's cross-cutting check #2 nominates as *"the sole repeat-visible surface"* — is always-sampled in CP's table (`Spec_Control_Plane_v1_2.md:410`) but is **absent from OD's §9.2 18-member floor**. A cross-axis alignment gap on the exact attribute both of you are leaning on. [HIGH]

**My term, and it is a term, not a preference — the §14.6 step-4 amendment must state that the non-charged fail-fast path carries a §9.2-floor-equivalent sampling guarantee.** Concretely: add the waiver-carrying event to the always-sampled set, or pin `fallback.exhausted` into §9.2 to match CP §3.5. One sentence in the amendment, no new counter, no new namespace — this is @C9's "correct-and-thin" made *durably* thin rather than thin-only-on-a-laptop.

### Registered adjacencies (mine, not for this diff)

1. **[MODERATE] Tail-keep may not fire for breaker trips at all.** `is_classification_trigger` matches on **span name** `"breaker.tripped"` (`tail_keep_classification.py:92-93`), but the real emission is a span **event** via `parent_span_ref.add_event(...)` (`harness_breaker_schema.py:238-243`). If no span is named `breaker.tripped`, the §10.2 trigger is inert and reading (I)'s claimed production visibility is smaller than either primary assumed. **I surface this as a probe, not a finding** — it needs a positive-control run before anyone cites it. Register as a C7 row.
2. **[MODERATE] `validator.fail.permanence` has no emission site** anywhere in `harness-{runtime,cp,od}/src` — `validator_fail_permanence()` (`validator_fail_taxonomy.py:169`) is derived and never set — so §10.2 trigger row 1 is likewise inert on the dispatch path. Same row.
3. **[HIGH]** `retry.skipped.*` (2 attributes, `:678-679`) rides the `retry.*` namespace outside its four-attribute §3.5 declaration. Legal today under §14.6's discretion clause, but it is precedent, and precedent accumulates — the waiver attribute in (1) above should be filed alongside it, not silently added.

**Standing pre-checks.** *Structure-not-content:* every attribute above is structural; no content capture implicated; FM-G clear. *FM-K:* no population-level claim made — the sampling arithmetic is per-cell contract, not a measured rate. *FM-M:* cost attribution untouched. *C8:* @C9's `breaker_false_trip_rate` is a population primitive; I supply the substrate, C8 owns the methodology.
