# E1 A1 — C7 observability (primary, independent, blind)

## Position on spine tensions

**T3 — 8-field core (+envelope) vs R-25.** ACCEPT, with a provenance caveat. Re-verified: `Finding` (`codex_context_guard.py:114-117`) is `severity, code, message`; `finding_id` zero matches. C-HE-24 §2's tiebreaker holds: every `Finding.code` consumer (`test_codex_context_guard.py:110-308`) is exact-equality; `:894` is a string render — nothing parses positionally, so a namespaced `code` is additive. Caveat: `[R: L0.2′]` is not one of the ten named operator decisions in §0.5; it is inferred from the BUILD-PLAN's Arc-2 text plus the authority-chain rule — sound, but should be stated as an inference.

**T9 — kill threshold "< 2 unique catches in 15".** RECONCILE. Threshold fine, but the denominator is not reconstructable from rows alone and adjudicator neutrality is unrecorded (F3/F4). Reconciled wording for C-HE-29 §3: "Each scored round MUST emit at least one row with `producer=<second_reviewer_identity>` regardless of finding count (a `finding_type=no_finding` marker row when the lens catches nothing), and the appended disposition row MUST carry the adjudicating party's identity in a new envelope field distinct from `producer`."

**T10 — sequencing.** ACCEPT the shape; instrument-before-claim is right. But S2's "GREEN" is satisfied by field presence, not by join paths resolving — F1/F2/F5/F6 are S2-owned gaps a schema test would not catch. RECONCILE: add to S2's gate: "S2 is GREEN only once the arc_type-open join, the phase-span durable-storage location, and the concurrent_lanes cohort-key shape are each concretely resolved, not merely field-named."

**T2 — loop_status.** ACCEPT the resolution (reducer keys on `$3`/`$4`, verified `loop_lib.sh:150-153`). Correction: `loop_status_path()` already exists (`loop_lib.sh:24`) and is dual-sourced; only its body needs to change. Substantive (F7): §3's "key on `lane_id`" has no row-schema change specified, unlike C-HE-10's explicit fifth column. RECONCILE: fold a `lane_id` column addition into C-HE-09 §3.

## Findings

| id | class | contract | quote (verbatim) | defect | concrete fix text |
|---|---|---|---|---|---|
| C7-F1 | 1 | C-HE-03 §3/§4, C-HE-26 | "(none)→pending at arc open — the instant a lane selects the roadmap unit and creates its queue entry and `arc_type` (C-HE-26)" | The reservation payload has no `arc_type` field, so no durable home for the open-time declaration. | Add `arc_type: <inventing|applying|null>` to the C-HE-03 §3 payload; state it is the open-time capture point joining into the ledger row via `arc_id`. |
| C7-F2 | 1 | C-HE-26 §1, Verification | "Unit: queue entry without `arc_type` → rejected at open" | At HEAD "queue entry" = `arc_metrics.py queue` (help: "record capture inputs out-of-repo (arc closure)"), fires at CLOSE with `--arc-type` already required. Reusing the term for the open-time artifact invites wiring the requirement onto the close-time verb. | Reserve "queue entry" for the existing `arc_metrics.py queue` artifact; name the open-time artifact "reservation" throughout; C-HE-26 §1 points at C-HE-03's `pending` creation. |
| C7-F3 | 1 | C-HE-29 §2/§3 | "Each scored round emits finding rows with `producer=<second_reviewer_identity>` and a `unique_catch: bool` disposition" | A round with zero findings produces zero rows; "ran, caught nothing" is indistinguishable from "never invoked"; the 15-round denominator is not reconstructable from rows alone. | Require a marker row every scored round (`finding_type=no_finding`, `producer=<second_reviewer_identity>`) so `COUNT(DISTINCT round_n WHERE producer=…)` is the scored-round count. |
| C7-F4 | 1 | C-HE-29 §2, C-HE-24 envelope | "each round's `unique_catch` disposition set by an adjudicator of **neither model family**" | No field records the actor appending the disposition; `producer` is the finding's own producer; the "differ only by timestamp/disposition" invariant forecloses repurposing it. Adjudicator neutrality is unauditable. | Add `adjudicator_identity` (nullable) to the row envelope, populated only on disposition-appending rows. |
| C7-F5 | 1 | C-HE-03 §7, C-HE-28 §1 | "`concurrent_lanes` … preferably as `{min, max}` …" / "using the existing exact-lever-set cohort split (`arc_metrics.py:812-832`)" | The reused cohort split keys on a hashable string (`" + ".join(sorted(levers))`); `{min,max}` has no canonicalization; neither D7 nor M8 mentions a window form. | Commit `concurrent_lanes` to a plain derived scalar (matching D7/M8) and move the window refinement to §11, or add an explicit key canonicalization (`f"{min}-{max}"`) to C-HE-28 §1. |
| C7-F6 | 1 | C-HE-27 §3 | "spans are durable — recorded in the ledger row's `phases` map (the row exists anyway, so durability is free)" | The ledger row is built at/after arc close (`queue`'s help; no "open" event exists today). Early-phase timestamps must be held elsewhere first; that intermediate durable location is unnamed. | Accrete `phases` onto the reservation payload via the payload-only-update CAS path (C-HE-03 §1), folding into the ledger row at drain. |
| C7-F7 | 2 | C-HE-09 §3 | "The reducer MUST scope `ACTIVATE` resets to the emitting lane (key on `lane_id`)" | `loop_log()` writes exactly 3 fields; no lane column exists; C-HE-10 adds its column explicitly, C-HE-09 does not. | Add: "`lane_id` is added as a new column on every row a lane emits (starting with `ACTIVATE`), mirroring C-HE-10 §1." |
| C7-F8 | 2 | C-HE-24 §1 | "This shape is **load-bearing** (without it N6 'problems prevented per hour' is uncomputable …)" | N6 is never defined (which dispositions count as "prevented"; wall-clock denominator). | Add an explicit N6 formula (numerator: reduced rows with `disposition=accepted`; denominator: named wall-clock source) plus a §8.1 row, or replace "load-bearing" with a forward-register pointer. |
| C7-F9 | 3 | C-HE-23 §2 | "Consistency check: … every markdown row has a JSONL row with the same `(pr, head_sha, verdict)`" | One-directional; a crash between the two writes yields an orphaned JSONL row with no `warn` finding, indistinguishable from the legitimate case. | Flag a JSONL row with neither a markdown sibling nor a matching `warn` finding as its own class, or reconcile pending markdown writes on the next drain. |
| C7-F10 | 3 | C-HE-09 §2 | "resolved by one function `loop_status_path()` used by hooks and raw shells alike" | Reads as new; it exists at `loop_lib.sh:24`; only its body changes. | Note in the plan. |
| C7-F11 | 1 | C-HE-15 §3, C-HE-24 §1, C-HE-25 | "Every verdict MUST be bound to immutable code state: `{head_sha, base_sha, diff_digest, reviewer_identity, prompt_version, config_hash}`" | Only `head_sha` (envelope) and `reviewer_identity`/`prompt_version`/`config_hash` (C-HE-25) land; `base_sha`/`diff_digest` have no field anywhere. Ledger today has only `merge_sha`. | Add `base_sha` and `diff_digest` to the row envelope (C-HE-24 §1) or the ledger extension (C-HE-25), and state which. |

## Verified at HEAD

`codex_context_guard.py:114-117`, `:774-781`, `:894` — held · `test_codex_context_guard.py:110-308` exact-equality — held · `arc_metrics.py:812-832` string cohort key — held (grounds F5) · `.harness/arc-metrics.jsonl` 18 rows, no `lane_id`/`concurrent_lanes`/`reviewer_identity`/`prompt_version`/`config_hash`/`phases`/`head_sha`/`base_sha` · `.harness/merge-gate-log.md` 440 lines prose · `loop_lib.sh:150-153` reducer keys `$3` — held · `loop_lib.sh:77-85` 3 fields, no lane column (F7) · `loop_lib.sh:24` `loop_status_path` exists; `:209` `loop_cap_list`, `:225` `loop_pending_hil_summary` (spec's `:165, 191-225` close; exact starts 209/225) · `arc_metrics.py` `lane_id|concurrent_lanes|reservation` → 0 · `queue` subcommand help "record capture inputs out-of-repo (arc closure)", `--arc-type` required (F1/F2) · ADR-HE-1 D7 / HE-3 M8 plain derived count, no window (F5) · ADR-HE-3 §5 `producer` = finding's producer (F4) · `diff_digest` once / `N6` undefined in spec (F8/F11).

## Sound — no finding

C-HE-24 projection round-trip additive · C-HE-25 field names consistent with C-HE-15 §3 for three of six · C-HE-12 "1 of 19" baseline correct · C-HE-19 matches `arc_metrics.py:270-277` · C-HE-23 §1 premise (two records) · C-HE-30 no double-counted authority.

## Voice self-check

C7 is worst placed to judge whether the content of each fix is architecturally sound (C9/C10/C1); it can only say whether the resulting facts are recorded somewhere durable, joinable, and unambiguous.
