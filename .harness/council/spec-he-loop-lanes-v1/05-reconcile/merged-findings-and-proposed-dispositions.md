# Consolidated reconcile — merged finding-set + orchestrator's proposed dispositions

Sources: E1 primaries C9/C10/C7 · E1 consultants C5/C1/C11/C8 · E2 adversarial (LOOP-BACK: 3/3/1) · E3 Codex cold (7×C1, 5×C2, 3×C3). Deduplicated into fold groups G1–G22. Each group names its sources, the proposed disposition (ACCEPT = fold as proposed; RECONCILE = fold with the wording below; REBUT = not folded, reason), and the owning voice asked to confirm. Convergence marks: **⟂** = independently raised by an out-of-family (Codex) or decorrelated (adversarial) reviewer AND a council voice.

| G | Contract(s) | Sources | Proposed disposition (fold text summary) | Owner to confirm |
|---|---|---|---|---|
| **G1 Reservation record = generation-versioned immutable files, no rename, no payload-replace** | C-HE-03 §1–4 | Codex C1-01 (C1), C3-03; adversary falsifier audit | RECONCILE. Replace the state-encoded-filename design: `QUEUE_DIR/reservations/<arc_id>/<gen>.json`, each version an immutable file created by exclusive create (`publish_exclusive`) carrying the full payload incl. `state`, `generation`, `prev_generation`; current record = highest `gen`; EVERY mutation (state or payload) = read current gen n → build payload → exclusive-create `<n+1>.json`; `FileExistsError` = lost the CAS → re-read, bounded retry (≤ 8) then RAISE. No `os.rename`/`os.replace` on reservations at all. Terminal history retained by construction; GC prunes gens older than terminal+30d. Cycle rule: readers detect a repeated `arc_id` in the `superseded_by` walk immediately and RAISE (cycles representable but invalid); depth cap 5 kept as a second guard. | C9 |
| **G2 No TTL reclaim at any tier ⟂** | C-HE-03 §5, Invariants; C-HE-20 §2; §12 O3 | Codex C1-02, ADV-F1 | ACCEPT. Delete the `pending`-aged silent reclaim. Aged `pending` (> 24 h) → `NOTIFY` row (G13) + HITL escalation, state unchanged; `pending→abandoned` only by explicit operator resolution recorded as a `RESOLVED-HIL` row or by ground truth (a superseding arc's reservation naming it in `superseded_by`). Invariant "no path reclaims a reservation on TTL expiry" now holds for all tiers; §12 O3 row corrected. | C9 |
| **G3 Lease token + transition marker (ABA-safe release/reclaim)** | C-HE-06 §2/§3/§6 | Codex C1-03; C9-F9 (terminology) | RECONCILE. Lease payload gains `lease_token` (128-bit random). Every release-or-reclaim of a lease MUST first win the exclusive create of `QUEUE_DIR/merge-door/transition.<lease_token>` (one transition per token, ever); only the marker winner may `os.rename(LEASE, released.<token>)` (holder release) or `os.rename(LEASE, reclaimed.<token>)` + create a fresh LEASE (reclaimer). A holder that loses the marker MUST stop driving the merge and reconcile via ground truth. No path-only unlink. Retained markers are GC'd with the lease history. | C9, C10 |
| **G4 Lease spans post-merge CI + terminating refresh; main runs not cancellable ⟂** | C-HE-06 §4(vi); C-HE-19 §2; `.github/workflows/ci.yml:43-45` | Codex C1-04 | ACCEPT. Release only after (a) the merge SHA's own `main` run = `success` (bounded wait 45 min; failure/timeout → durable `blocked` lease state + HITL, lease held) and (b) the terminating refresh PR merged and its run `success`. Plus: `ci.yml` concurrency group for pushes to `main` MUST key by SHA (`github.ref == 'refs/heads/main' && github.sha || github.ref`) so lane B's landing cannot cancel lane A's post-merge run. Files ref: `.github/workflows/ci.yml:43-45`. | C9, C10 |
| **G5 Carrier scoping: Codex-exec lanes OUT of v1; §5 AGENTS.md row removed ⟂** | C-HE-01 §1; §5 C-HE-17 row; §10; §11 | C9-F1 (C1), C1 ruling, C1-F1/F3, ADV-F3 | ACCEPT C1's ruling verbatim: carrier-scoping sentence in C-HE-01 §1; new §11 row "cross-carrier merge-door fencing" (joint Claude/Codex arc; rewire `.agents/skills/ship-pr/SKILL.md:96` to `safe-merge.sh` under Codex posture); N=4 dial and `AGENTS.md:32` cap are disjoint pools (note in C-HE-01 §2 + C-HE-11); drop `AGENTS.md` from the C-HE-17 files row and route the #3 restatement via `.claude/skills/*` docs + the same forward row. **Operator-facing flag:** this is a scope narrowing relative to "4 lanes" if the operator intended mixed carriers; surfaced in the final report as reversible. | C1, C10 |
| **G6 Drain: flip-before-append, holder-only append, teardown guard, 6th interleaving** | C-HE-03 §4; C-HE-04 §2/§3/Verification; C-HE-02 §6 | ADV-F6 (C2), C9-F2, C9-F9, C9-F6, C9-F7 | RECONCILE. (a) Transition order at drain: reservation `pending→open` (holder = this `lane_id`) BEFORE `append()`; `append()` MUST refuse unless this lane holds the `open` reservation — so a lane killed after append cannot be silently superseded by a second appender (B sees `open` held by A → not re-appendable; A's `open` + no PR + dead lane → HITL, never auto). (b) Teardown contract: `tools/hooks/safe-worktree-remove.sh` MUST refuse to dispose a worktree whose `.harness/arc-metrics.jsonl` has uncommitted changes (the existing mutex-backed wrapper is the enforcement site). (c) Sixth AC#2(a) interleaving: kill A between `append()` and the restore line (`MERGE_DOOR_TEST_KILL_AFTER`-style env hook in `arc_metrics.py`: `ARC_METRICS_TEST_KILL_AFTER=<step>` → `os._exit(137)`). (d) Fault isolation wraps `_claim_arc` too; a systemic `OSError` (queue-dir permission/I/O) aborts the loop once with one message. (e) C-HE-02 §6 renamed "liveness-predicate compare (pid+host via `_process_is_alive`)"; pid-reuse false-liveness recorded as an accepted residual (fails toward stall); the mutation-probe target is the read-liveness-then-unlink-by-path window `:624-626` (revert → E9 loss reproduces). (f) Barrier poll bounded 30 s → explicit rendezvous-timeout failure. | C9 |
| **G7 Merge-door caller/primitive split: yield-point + primitive rate limit + gate rows** | C-HE-06 §8/§9 | C9-T4/F3, C1-T4/F4, C5 reactions, C5-F2, C5-F3 | RECONCILE. §8 split: sentence 1 (yield): "acquire-fail MUST yield control to the caller's next natural gate-pass event — a control-flow return, not a sleep-and-retry inside the acquire call"; sentence 2 (numbers, unchanged). Primitive-side bound: `merge_door.py` refuses > 3 acquire attempts per `lane_id` per 60 s → `cause_attribution: lease_acquire_rate_exceeded`. §9 gains rows: `merge-door-lease-acquire` (`lease_contended`), rate refusal (`lease_acquire_rate_exceeded`, `transient-retry`), budget exhaustion (`HITL-recoverable`, `lease_acquire_budget_exhausted` — explicit taxonomy exception), `merge-door-reconcile` (hybrid; exhaustion `permanent-fail-exit`, `merge_reissue_exhausted`). | C9, C5 |
| **G8 T6 in-flight double-merge claim** | C-HE-06 §5 | C9-F8/T6, C10 T6 ACCEPT, adversary rejected | RECONCILE. Keep C10's/adversary's reading (safe under `--match-head-commit` + ground truth) but drop the uncited server-behavior sentence and add the mocked-concurrent-request test C9 asked for (`test_merge_door.py::test_inflight_first_attempt_then_reissue` — mock models a delayed first landing; assert exactly one MERGED and no error path proceeds past step (v)). | C9 |
| **G9 Wrapper exact arity + explicit push deny + guard audit** | C-HE-07 §1; C-HE-08 §1 | C10-F1, C10-F2 | ACCEPT verbatim. | C10 |
| **G10 X9 recipes + protection-verify phase0 row + strict:true tiebreaker ⟂** | C-HE-08 §2–4; §8.1 | C11-F1, C10-T8, Codex C1-07, ADV-F5 | ACCEPT. `just main-protection-{apply,show,rollback,tiebreaker,verify}`; `verify` is a read-only phase0 manifest row (exact-compare every required setting + context; 404/unprotected/mismatch → RED); `tiebreaker` runs the stale-refresh-branch fast-forward check under `strict:true`; operator answers one AskUserQuestion. §8.1 row retagged. | C10, C11 |
| **G11 Finding record: `record_kind` union, envelope fields, projection wording** | C-HE-23 §2; C-HE-24 §1/§2/§4; C-HE-25; C-HE-12 §2; C-HE-26 §2 | Codex C2-01, C2-02, C1-06, C2-04, C3-02; C7-F11, C7-F4; C5-F1, C5-F4, C5-F6, C10-F3 (mechanism per C5); C7-F9; ADV-F7 | RECONCILE. (a) Every row carries `record_kind`; `.harness/arc-metrics.jsonl` carries **only** `record_kind=arc` (one row per `arc_id`; per-round outcomes nested; `arc_type_open` + `arc_type_close` fields — no second arc row on relabel; `SPLIT_BRAIN_LEDGER` applies to arc rows only). `.harness/merge-gate-log.jsonl` carries `record_kind ∈ {finding, finding_adjudication, no_finding, equivalence_proof, gate_demotion, reviewer_unavailable}` from every producer. (b) Envelope = `{record_kind, ts, arc_id, lane_id, head_sha, base_sha, diff_digest, round_n, cause_attribution, disposition, disposition_actor, unique_catch}`; `disposition_actor` populated only on adjudication rows and MUST differ from the `producer` substring of `finding_id`; C-HE-29's adjudicator identity = `disposition_actor`. (c) C-HE-15 §4 schema requires all six binding fields; the wrapper computes expected values and byte-compares; `severity ∈ {P1,P2,P3}`, `additionalProperties:false`; `REVIEWER_UNAVAILABLE(permanent|transient)` → `fail_class permanent-fail-exit|transient-retry`. (d) C-HE-24 §2 tiebreaker wording: "no consumer splits or positionally parses `code` on `:`" (a `startswith` prefix consumer exists at `test_codex_context_guard.py:634`); existing codes byte-identical. (e) Consistency reducer flags a JSONL row with neither a markdown sibling nor a `warn` finding as its own class. (f) `[R: L0.2′]` → `[R: BUILD-PLAN L0.2′ (D-A × D-B reconciliation)]` + §12 row. | C7, C5 |
| **G12 `concurrent_lanes_at_open` integer key; joint stratification; correlational** | C-HE-03 §7; C-HE-25; C-HE-28 §1/§3; §6 S2/S6 | C7-F5, Codex C2-03, C8-F6, C8 reactions, C1-F2/C7-T10 | ACCEPT. `concurrent_lanes_at_open: int` is the cohort key; optional `concurrent_lanes_min/max` ints, never primary key; cohort split joint on `(concurrent_lanes_at_open, arc_type)` once labels are uncontaminated; §3 names the selection mechanism and states AC#10 results are correlational unless a forward-register item adds randomized assignment; §6 gains the S2 hand-off row (schema-present only; consumers own semantic resolution; S6 additionally requires joint stratification before any AC#10 claim). | C7, C8 |
| **G13 `loop_status`: option (b) + `lane_id` column + rendered token + pointer sweep + `NOTIFY` kind + pull-based coalescing** | C-HE-09 §2/§3; C-HE-10; C-HE-06 §10; C-HE-31 §4 | C7-F7, C7-F10, ADV-F4, C11-F2/F3, C11 reactions, Codex C2-05 | RECONCILE. ACTIVATE stays per-lane (control) and MUST NOT reset globally-visible HIL rows (option b — the `lane_id`-keyed reset disjunct is struck); skip-set = all pending HIL rows across lanes (over-skipping is safe). Every row a lane emits gains a `lane_id` column; `loop_pending_hil_summary()`/`loop_cap_list()` render `[<lane_id>]` per item. `loop_status_path()` (`loop_lib.sh:24`, exists) body changes AND the 7 literal `.harness/loop_status.md` pointers are swept (acceptance: zero hits outside tests). New `NOTIFY` row kind: append-only, rendered beside (never merged into) the HIL summary, excluded from `loop_skip_set()`; used by C-HE-06 §10 tiering, gate demotion, aged-`pending`, RAM shortfall. Coalescing (C-HE-10) is pull-based: lanes only append; the SessionStart/merge-lane path emits a group only after `first_seen + window`, records a `delivered_generation`, suppresses re-delivery of rows already included. | C7, C11 |
| **G14 Phase spans accrete on the reservation record; N6 defined** | C-HE-27 §3; C-HE-24 §1 | C7-F6, C7-F8, C8-F5 | ACCEPT. Early-phase spans accrete on the reservation record via G1's generation CAS and fold into the arc row at drain. N6 = COUNT(DISTINCT `finding_id` whose last row has `disposition=accepted`) ÷ Σ(`phases.verify`+`phases.edit`) hours from the durable map, never an inter-row delta; §8.1 row added. | C7 |
| **G15 Shadow trial: marker row, `unique_catch` definition, OC table + n=30/<3 default, HITL delivery, rejected-fixture** | C-HE-29 §2/§3/Verification | C7-F3, C8-F1, C8-F2, C8 reactions, C11-F4, C5/C7-F4 | RECONCILE. Per-round `no_finding` marker row (denominator = COUNT DISTINCT round_n with `producer=<lens>`). `unique_catch=true` iff (a) `location`+`finding_type` absent from blocking-reviewer rows for the same `head_sha` AND (b) last-row `disposition=accepted`; a later `rejected` MUST NOT count. Kill rule restated with its OC table (P(kill|p) at p ∈ {0,.05,.10,.15,.20,.25} for the chosen n) — v1 default **n=30, kill if < 3 unique catches** (P(false-kill|p=0.10) ≈ 0.18), the operator may amend per BUILD-PLAN; the round-n decision fires as an escalation-kind HITL request (dispositions + threshold shown; responses ∈ {approve-kill, reject-keep, amend-threshold}); adjudicator identity = `disposition_actor`, named as 'operator' or a specific third-party reviewer identity — never a placeholder. Verification adds the `unique_catch=true`-then-`rejected` fixture. | C8, C7, C11 |
| **G16 Mechanized checks: window semantics, hysteresis, OC, audit row + NOTIFY, state-machine test** | C-HE-31 §4; §8.1 | C10-T7, C8-F3, C5-F5, C11 reaction, C8 reaction to C5-F5 | RECONCILE. Promotion: advisory→blocking after 0 FP across a fixed 20-arc replay (state OC: P(pass|p=0.05)≈0.36 — the bar excludes p ≳ 0.15, not less; recorded honestly). Demotion: rolling window evaluated at `just lanes-verify`/CI, **two-strikes** hysteresis (≥2 rejected in each of two independent consecutive 20-arc windows); every demotion emits a `gate_demotion` row AND a `NOTIFY`. §8.1 row `test_promotion_demotion_state_machine` (mechanism-only; not calibration evidence). | C10, C8, C5 |
| **G17 K7 gates: BUILD (>3/18) vs EVALUATE (≥20 labeled arcs)** | C-HE-26 §3 | C8-F4 | ACCEPT. | C8 |
| **G18 Reviewer-concurrency probe: reps + pass rule** | C-HE-22 Verification | C8-F7 | ACCEPT. ≥5 reps at {1,2,4}; GREEN iff median per-call wall-clock at N ≤ 2× the N=1 median AND zero validity failures. | C8 |
| **G19 Review-wrapper retry table + session-artifact discovery** | C-HE-16; C-HE-18 | C9-F4, C5 (falsified reuse) | RECONCILE. `{per_attempt_timeout: 600 s, max_attempts: 2, backoff_base_ms: 0, backoff_cap_ms: 0, total_budget_s: 1260}` — `total_budget_s` reuses `agy_review.py`'s `TOTAL_REVIEW_TIMEOUT_SECONDS`; the loop is new; exhaustion → `HITL-recoverable`. | C5, C9 |
| **G20 Env isolation: port blocks, RAM probe, ref-lock numbers ⟂** | C-HE-11 §1/§3 | Codex C1-05, C11-F6, C9-F5, C1-F3 | ACCEPT. Host ports for lane k ≥ 1 = `30000 + 100·k + {0,1,2,3}` (grafana, tempo, otel-grpc, otel-http); lane 0 keeps today's; validate k < 350; `k` from `HARNESS_LANE_INDEX` allocated at lane-init by exclusive create of `QUEUE_DIR/lanes/<k>`. RAM/Docker-VM headroom probe before a stack at k ≥ 2 on a machine below an operator-configured floor (default 32 GB) → `NOTIFY` naming the constraint; such failures never carry a `merge-door-`/`reservation-` cause_signature. Ref-lock retry `{base 100 ms, factor 2, cap 5 s, max 8}`; exhaustion fails the git op and emits `NOTIFY`. Codex-exec legs have no `HARNESS_LANE_INDEX` — same forward row as G5. | C11, C9 |
| **G21 Verification infrastructure: C-HE-30 block + manifest row; `mutation-probe` column + coverage check; pilot report; recurring def; flock scope; C-HE-19 claim** | C-HE-30; §8.1; C-HE-13 §3; C-HE-02 Invariants; C-HE-19 | ADV-F2, C8-F9, C11-F5, C8-F8, Codex C3-01, adversary [V] audit gap | ACCEPT. C-HE-30 gains Invariants + Verification (static: audit doc exists, exactly 8 stores, one authority each) + phase0 row. §8.1 gains a `mutation-probe` column and `just mutation-probe-coverage-check`. `just lanes-pilot-report <run-id>` computes the pilot iff-clause; "recurring" = a `cause_signature` in ≥2 of the ≥3 pilots OR one operator-rated-severe occurrence. C-HE-02's grep invariant scoped to `tools/arc_metrics.py`, `tools/merge_door.py`, and the reservation module (7 pre-existing `tools/hooks/**` lock users allowlisted by name). C-HE-19's `CANCELLED` zero-occurrence claim re-verified by the orchestrator before fold. | C9, C8, C11 |
| **G22 Change-note + version** | Status; Change-note | all | Fold lands as an in-place amendment with a dated "Change-note (clearance fold, 2026-08-18)" enumerating G1–G21 by finding id (permitted: the record has not been consumed). Version stays v1; the clearance marker cites the folded byte state. | orchestrator |

**Not folded (REBUT / already resolved):** C-HE-06 §5 T6 safety reading stands (C10 ACCEPT; adversary rejected the candidate; C9's citation gap handled by G8's test). C10 T1 ACCEPTS the R-19 supersession — P1 stands normative. C7 T3 caveat handled by G11(f). C8's SPRT alternative recorded in §11 as an option the plan may take instead of n=30. Nothing in the merged set re-litigates D-A..D-D or D5–D8.
