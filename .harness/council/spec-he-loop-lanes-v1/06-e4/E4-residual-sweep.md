# E4 — adversarial #2 residual sweep over the folded spec

**Verdict: CLEAR-WITH-FOLD.** 22/22 fold groups landed, including every second-order C9/C10/C7 reconcile correction (G1 re-validate-on-loss, G3 marker provenance + idempotent completion + self-resume-as-reclaim, G4 continuation + 45-min bounds + `blocked` + `merge-door-unblock` + tradeoff statement, G5 branch-protection sentence + cross-carrier `NOTIFY`, G6 holder transfer + `@{u}` check + local-row reconciliation, G7 K=5 not counted, G10 "Runs in: local" + `gh-auth-absent`, G11 `:`-free identifiers, G13 structured column + `COALESCE-DELIVERED`, G14 N6 exclusion, G15 corrected OC (p=.10 → 0.184), G16 runtime state file, G19 550 s / dynamic). Zero architectural findings; zero re-litigation.

**Residuals (4, all mechanical, all folded by the orchestrator 2026-08-18):**

| id | line | defect | fold |
|---|---|---|---|
| F-01 | C-HE-30 store row; §8 AC#10 witness | bare `concurrent_lanes` after G12 rename | → `concurrent_lanes_at_open` (+ `arc_type`, `phases` in the store row) |
| F-02 | C-HE-23 §2; C-HE-35 K6 row; §9 row 6 | C-HE-24 §-refs shifted by the new §2 envelope | §4→§5, §5→§6 |
| F-03 | C-HE-09 §5 | kind-vocabulary cite `loop_lib.sh:2-9` | → `:73-74` (also §13) |
| F-04 | §13 | `codex_context_guard.py:634` should be `test_codex_context_guard.py:634` | fixed |

**`[V]` re-verification (different sample from E2):** 14 held; 2 did-not-hold (F-03, F-04 — citation transcription, not substance). Confirmed: `_process_is_alive` `:541-548`; `drain()` `:718-756` only `append` isolated; `read_ledger` bare `json.loads`; `fmt_span` ~5×; `loop_status_path` `:24`; rejoin loop `:175-186`; `hook_project_dir` `:18-22`; `hook_worktree_local_state` no `@{u}` check; `agy_review.py:22,461-463`; `ci.yml:39-40,47-48`; `AGENTS.md:32,56-57`; `.agents/skills/ship-pr/SKILL.md:96`; 7 literal pointers; 7 `flock|fcntl` files under `tools/hooks/`; ledger `arc_type` 6/12/0.

**Rejected candidates:** C-HE-06 step numbering (all (i)–(ix)); "fifth column" remnants (none); `[R: L0.2′]` bare tag (none); §8 vs §8.1 (consistent); C-HE-14/§9 stale-design rows (none); C-HE-30 "eight" (8 rows); §12 vs contracts (consistent); `reservations.py`/`merge_door.py` absence (NEW, expected); C-HE-02's "and their tests" phrasing (loose; the 7-count holds).
