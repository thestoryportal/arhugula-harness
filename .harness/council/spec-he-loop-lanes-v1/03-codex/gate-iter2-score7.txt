SCORE: 7
AMBIGUITIES: Spec is not executable until council clearance, yet conditional contracts and downstream dependencies are not fully resolved; C-HE-06 says a PR merge is never invoked twice but permits one OPEN-state reissue, while its timeout test requires no second call; C-HE-27 declares durable spans decided, but §§10–11 call the choice open and plan-owned; phase-0 handling of Docker skips is contradictory; reservation rename/CAS behavior does not specify destination-exists handling or atomic payload/state updates; dual markdown/JSONL gate writes lack failure and consistency semantics; several checks lack exact algorithms or pass thresholds, especially semantic conflict detection, mechanized checks, pilot success, and the 4/19 detection target; many line-number and external-corpus references are commit-sensitive or outside the repo, and the specification’s own target file path is absent.
