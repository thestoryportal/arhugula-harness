SCORE: 6
AMBIGUITIES: Repository-relative paths are missing for BUILD-PLAN, design v1/v2, STAGE3/5/7, ERROR-LEDGER, and PR/session evidence; several requirements remain undecided or council-dependent (C-HE-07, phase-span storage, classifier location, branch-protection/operator gates), so the implementation target is not cleared or stable; “all Phase-0 verifications GREEN,” “enumerate interleavings,” “same atomic operation,” “minutes-scale 5–15 min,” “distinguishable from zero,” “clean cycles,” “first N production merges,” “actual-write based,” and “well under N×” lack exact algorithms, thresholds, fixtures, or commands; `lane_id`, `reservation_id`, file naming/encoding, sequence-counter allocation, state-transition schemas, timestamps/dispositions beyond the nominal 8 fields, shared venue paths, retry/backoff bounds, staleness ages, “pending + aged” reclaim threshold, `cause_signature`, port-remapping formula, gate-schema format, session-artifact discovery, and known vendor-error tables are unspecified; C-HE-03 says transitions use atomic rename but does not define CAS/precondition enforcement, while its “at most one reservation not abandoned” invariant conflicts with supersession/terminal-history semantics; C-HE-04’s invariant omits locally appended-but-unmerged rows despite requiring their queue retention; C-HE-06 does not define how the attested merge-tree digest is stored/retrieved or how OPEN restart avoids a second merge request after an indeterminate first call; C-HE-08 requires mutable GitHub settings without naming exact rules/check identifiers or rollback evidence; C-HE-23 says no new ledger while mandating a new structured sibling; C-HE-24 calls the record “8-field” while additionally requiring lane_id, arc_id, timestamp, disposition, and unique_catch; S6 says pilots do not gate N while C-HE-13 mechanically refuses pilots until Phase 0, making AC1’s four-lane safety gate unclear; many acceptance witnesses are documentation, grep, mocked, Docker/operator/provider-dependent, or future-pilot measurements with no unified executable command, skip policy, required artifacts, or pass/fail manifest; and no contract-to-file/change inventory tells an unfamiliar implementer which exact files own each requirement.
