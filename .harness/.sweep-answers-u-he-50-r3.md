# Round-3 sweep answers — u-he-50

- codex_review_wrapper:42a4f93bbc26f4c034a20ddc244c5f0dee0bc43a:2d8d09ad4dab:1 (P2,
  Codex-native carrier lacks the emission surface) — ABSORBED: `.agents/skills/
  roadmap-continue/SKILL.md` now documents the prefixed logged-wrapper invocation
  (automatic verify edges) and the interim absorb/edit/verify_unavailable block, with
  the same B-218 deletion trigger as the Claude copy; cross-carrier witness
  `test_roadmap_continue_carriers_document_span_emission_parity` pins both carriers
  (wrapper name, command shapes, verify_unavailable, B-218). Disposed accepted.

Class-sibling sweep (carrier-parity class): `.agents/skills/ship-pr/SKILL.md` and
`codex-autonomous-loop/SKILL.md` carry no span text — deliberate: the spec (C-HE-27 §5
X6a) designates roadmap-continue as THE interim carrier ("the skill actually in context
during review rounds"), and the span DEFINITION stays single-homed in the Claude
ship-pr block (one-source-of-truth: a third definition copy would be a drift surface).
The Codex bridge's new block points at that definition by name.

## Grep-hit labels, answered verbatim (fix diff)

- [save/restore pair: two-armed restore] — none added (doc + static test only).
- [check-then-act on paths] — none added; the new test reads committed carriers.
- [bare counts/absolutes in prose] — "never emit `verify` by hand" is the enforced
  no-op-replay contract (wrapper-emitted, first-write-wins); "deleted in the same PR"
  is the spec'd B-218 trigger, not a count.
- [new retry/timeout constants] — zero.

No findings deferred.
