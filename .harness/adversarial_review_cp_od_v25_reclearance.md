# Adversarial Review — CP v2.5 + OD v2.5 §4A-conformance pre-implementation re-clearance

## Summary

- **Mode:** Phase-7 pre-implementation review (per `harness-adversarial-reviewer` SKILL.md §"Phase-7 pre-implementation review mode"). Deferred re-clearance pass of the §4A verbatim-divergence conformance delta plans, per `.harness/review-pipeline.md` §7 pilot.
- **Corpus reviewed:**
  - `design-substrate/Implementation_Plan_Control_Plane_v2_5.md` (delta — U-CP-00 + U-CP-22 amendment) + `_v2_4.md` (the §4A conformance bodies — U-CP-01/10/12/19/22/23/43/46/47/48) + `_v2_3.md`/`_v2_2.md`/`_v2_1.md` (resolved verbatim pointers).
  - `design-substrate/Implementation_Plan_Operational_Discipline_v2_5.md` (the §4A conformance bodies — U-OD-02/04/09/11/12/14/30/32/33) + `_v2_4.md`/`_v2_1.md` (resolved verbatim pointers).
  - Cited specs: `Spec_Control_Plane_v1_2.md` §1.4/§5.1/§8.1/§10.1/§10.2/§10.3/§11.1/§16.2/§19.1/§19.2/§19.4/§20.4/§20.5/§20.6/§21.1/§21.2/§21.3/§21.5; `Spec_Control_Plane_v1_3.md`; `Spec_Operational_Discipline_v1_2.md` §2.1/§4.1–§4.5/§7.1/§9.2/§10.1/§11.2/§11.3/§21.2/§22.1/§22.2; `Spec_Operational_Discipline_v1_3.md`.
  - ADR: `ADR-D2.md` §1.2 + §1.7.1 (read for the carried U-OD-29 item).
  - Prior review context (read, not re-derived): `.harness/verbatim_audit_cp_plan.md`, `.harness/verbatim_audit_od_plan.md`, `.harness/adversarial_review_phase7_cp_od_preimpl.md`.
- **Date:** 2026-05-15
- **Finding count by §4.1 review-severity class:** Class 3: 0 · Class 2: 0 · Class 1: 1
- **Highest-severity finding:** F1-01 — U-CP-22 v2.5 acc #3 admissibility prose silently renders the §10.3 workload-class identifiers in SCREAMING_SNAKE_CASE where the v2.4-conformed body used lowercase-hyphen; undeclared in the v2.5 §0.3 revised-sections table. Documentation-scope hygiene drift; stem match preserved, no conformance regression.
- **Disposition recommendation:** **CLEARED with one inline documentation fix.** The §4A conformance revision-pass (CP v2.4 / OD v2.5) faithfully conformed every revised unit's signature to its cited spec section — all conformed enums, namespace schemas, member sets, span-name maps, and value tokens were verified byte-exact against the cited `Spec_Control_Plane` / `Spec_Operational_Discipline` sections. No verbatim-claim divergence of the Tension 002 / 004 / F3-01 shape was re-introduced or left in any revised unit. Per §4.1: only-Class-1 findings → clearance with inline fix. The 4 carried §4A items + 5 originally-flagged items are dispositioned in the dedicated section below — they are pre-existing, plan-self-declared, operator-decision-pending forks, not defects introduced by the conformance pass.

### Class-taxonomy disambiguation (per SKILL.md title-section)

This report's per-finding severity is the **§4.1 review-severity** scale (Class 1 minor / Class 2 moderate / Class 3 severe). The carried/flagged items each carry a **§2.7.6 Phase-7 execution fork** class, stated explicitly per item. A §4.1 Class-3 review finding ≠ a §2.7.6 Class-3 (informational) fork. There are no §4.1 Class-3 review findings in this pass; the BLOCKED units below are blocked by §2.7.6 Class-1 forks the plans themselves already declared.

### Calibration note — what this pass attacked

The §4A conformance pass's authoring invariant (CP v2.4 §0.1 / OD v2.5 §0.8) was a mechanical copy-then-reverify discipline: open the cited spec section, copy canonical value names directly, paste, re-verify post-authoring. This review independently re-ran that verification — it did not trust the §0.9 / §0.10 coherence-pass self-attestations. Every "verbatim" / "exactly N" / "matches §X" acceptance criterion in the revised units was cross-checked against the cited `design-substrate/` spec section directly. The result: the conformance pass holds. This is a finding-free outcome on the primary attack vector (the verbatim-divergence systemic pattern), recorded as such per FM-E.

---

## Class 3 findings (severe — phase re-opening)

None. No revised unit re-introduces or retains a verbatim-claim divergence; no revised unit's signature fails to transcribe its cited spec contract; no revised unit triggers discriminator (b) or (c).

---

## Class 2 findings (moderate — current-phase revision)

None.

---

## Class 1 findings (minor — documentation drift)

### F1-01 — U-CP-22 v2.5 acc #3 admissibility prose case-renders the §10.3 workload identifiers without declaring the edit in §0.3

- **Location:** `design-substrate/Implementation_Plan_Control_Plane_v2_5.md` §2.4 U-CP-22 acceptance #3 (lines 188–192) vs `Implementation_Plan_Control_Plane_v2_4.md` §2.4 U-CP-22 acceptance #3 (lines 375–379); v2.5 §0.3 revised-sections table (lines 38–44).
- **Defect:** The v2.4-conformed U-CP-22 acc #3 renders the §10.3 cross-pattern admissibility annotations with the workload-class identifiers in their spec-canonical lowercase-hyphen form — `software-engineering`, `research`, `pipeline-automation`, `content-creation` — matching `Spec_Control_Plane_v1_2.md` §10.3 byte-exact. The v2.5 body re-renders the same acceptance criterion with those identifiers in SCREAMING_SNAKE_CASE (`SOFTWARE_ENGINEERING`, `RESEARCH`, `PIPELINE_AUTOMATION`, `CONTENT_CREATION`). v2.5 §0.3 declares only two U-CP-22 amendments — `Depends on:` and `Inputs:` — and §0.2 states the "v2.4 §10.1/§10.2 conformance body preserved verbatim". The acc #3 case change is therefore an **undeclared edit**: the body was not preserved verbatim, and the change is not enumerated in the revised-sections table.
- **Discriminator that classifies as Class 1 (§4.1):** (a/b/c) all miss. Per the CP-plan audit's own casing-convention carve-out (`.harness/verbatim_audit_cp_plan.md` Findings-rejected item 6 — SCREAMING_SNAKE_CASE rendering of spec lowercase-hyphen identifiers is a Python-stack naming convention, not a divergence, when token stems match 1:1), the case change does not alter semantics: the stems match the §10.3 identifiers exactly, and U-CP-22 already consumes `WorkloadClass` whose member names are SCREAMING_SNAKE_CASE per U-CP-00. The defect is a change-note completeness gap, not a content gap. Documentation drift.
- **Evidence:** v2.4 line 376 `HIERARCHICAL_DELEGATION — admissible at software-engineering and research workloads`; v2.5 line 189 `HIERARCHICAL_DELEGATION — admissible at SOFTWARE_ENGINEERING and RESEARCH workloads`. v2.5 §0.3 table rows 42–44 list only `Depends on:` / `Inputs:`.
- **Anti-fabrication attack engaged:** none specifically; surfaced by the cross-version verbatim-preservation check (v2.5 claims "preserved verbatim" against a body that was edited).
- **Decision-vocabulary label:** *decided* — the text supports a single reading (the case change is real and undeclared; the carve-out makes it non-substantive).
- **Resolution:** Inline fix in the affected document — either restore the acc #3 lowercase-hyphen rendering to honour the "preserved verbatim" claim, or add the acc #3 case-rendering to the v2.5 §0.3 revised-sections table so the declared scope matches the actual edit. The reviewer does not pick which; both close the change-note completeness gap. Non-blocking for U-CP-22 landing.

---

## Findings considered and rejected (transparency)

Substantive attack vectors applied to the CP v2.5 / v2.4 + OD v2.5 corpus that did **not** surface a finding:

1. **Byte-exact verification of every cited spec section (the primary calibration attack — Tension 002 shape).** All ~15 CP spec sections and ~13 OD spec sections cited by the revised units were opened and the conformed signatures cross-checked: CP §1.4 (4 `routing.*` attrs), §5.1 (8 event classes + 8 span names), §8.1 (5 `resumption.kind` values + 1:1 engine-class map), §10.1 (6-pattern taxonomy), §10.2 (`cascade_policy` 3-literal field domain), §10.3 (admissibility annotations), §11.1 (per-workload-class primary patterns), §19.1/§19.4 (`{auto, ask, deny}` gate level + `_hitl_required`), §20.4 (7 `audit.*` attrs), §20.5 (per-tier emission), §20.6 (4-span HITL schema), §21.1 (5-class retry-exit taxonomy), §21.5 (3 `validator.fail.*` attrs); OD §2.1 (8 cells / 7 classes), §4.1–§4.5 (3-component span name / 7 operations / 3 tiers / `gen_ai.client.operation.duration`), §7.1 (`BreakerScope` 2 values), §9.2 (18-row always-sampled set), §10.1 (13-row base-rate set), §11.2/§11.3 (13 / 6 cardinality attribute sets), §21.2 (`{ed25519, ecdsa-p256, rsa-pss-2048}`), §22.1 (8 transitions, 5 within-column + 3 diagonal), §22.2 (5 preservation dimensions). **Every conformed signature transcribes its cited contract byte-exact.** The systemic verbatim-divergence pattern is not present in any revised unit — the conformance pass removed it.

2. **Casing-convention mechanical-application self-check (FM-D).** SCREAMING_SNAKE_CASE rendering of spec lowercase-hyphen identifiers (`SINGLE_THREADED_LINEAR`↔`single-threaded-linear`, `ENGINE_REPLAY`↔`engine_replay`, `AUTO`↔`auto`, `RSA_PSS_2048`↔`rsa-pss-2048`) was treated as a Python-stack naming convention, not a divergence, where token stems match 1:1 — consistent with the CP/OD audits' own carve-out. Mechanical "any case difference is a finding" application was avoided; the one case-related finding (F1-01) is a *change-note completeness* defect, not a casing defect.

3. **A4 (fabricated citations) on revised-unit `Implements:` fields.** Every `Implements:` field on the revised units (U-CP-00 `C-CP-07 §7.3`; U-CP-22 `C-CP-10 §10.1/§10.2/§10.3`; U-OD-04 `C-OD-04 §4.1–§4.5`; U-OD-30 `C-OD-21 §21.1–§21.3`; etc.) resolves to a real section in the cited spec. The v2.4 conformance also corrected three prior mis-citations (U-CP-22 acc #2 §10.3→§10.2; U-CP-46 acc #2 §20.5→§20.6, acc #4 §20.4→§20.5; U-CP-48 acc #5 §21.4→§21.3) — each verified correct against the spec. No citation fabrication.

4. **A8 (framing contamination) on the revised units.** U-CP-00 (`WorkloadClass` enum), U-OD-04 (OTel base layer), U-OD-30 (`SignatureAlgorithm`), and the other revised units are stack-neutral enum/schema declarations. None embeds a persona, deployment-surface, or stack overcommitment contrary to `CLAUDE.md` framing. U-OD-04 explicitly defers OTel SDK binding to implementation discretion per §4.5. No discriminator (c) finding.

5. **Cross-unit propagation completeness.** CP v2.4 §0.1 scoped each `TopologyPattern`/`LifecycleEventClass`/`ResumptionKind`/`ValidatorFailClass` consumer for literal-value-token enumeration vs symbolic type reference: U-CP-23/U-CP-12/U-CP-48 (literal enumerators) were full-revised and verified; U-CP-24/U-CP-25/U-CP-31/U-CP-13/U-CP-35/U-CP-53/U-CP-27/U-CP-39/U-CP-45/U-CP-20 (symbolic reference only) were confirmed to carry no value-token enumeration in acceptance text — pointer-preservation is correct for them. OD v2.5 §0.5 scoped U-OD-14/U-OD-22/U-OD-28 against the `BreakerScope`/`BackendClass` renames and confirmed none literally enumerates a renamed member. The propagation scoping is sound; no consumer was missed.

6. **U-CP-48 token-set propagation against U-CP-47.** U-CP-48 acc #2 `TRANSIENT_STAIRCASE_TRANSITIONS` `on_cause` enumeration was checked against U-CP-47's v2.4-conformed `ValidatorFailClass` — it keys on `TRANSIENT_RETRY`/`REFLEXION_RECOVERABLE`/`PERMANENT_FAIL_EXIT`/`TERMINAL_FAIL_EXIT`, the §21.1 retry-exit taxonomy, not the divergent v2.1 fail-cause tokens. The conformance note correctly observes that fail-*cause* discrimination rides on `validator.fail.cause_attribution` (U-CP-47), not `validator.fail.class`. Cross-unit composition survives the rename.

7. **OD §11.2/§11.3 compound-spec-row rendering judgment call.** U-OD-14 acc #1 claims cardinality 13 for `CARDINALITY_SAFE_ATTRIBUTES` against an OD spec §11.2 table that has 12 rows — the `harness.breaker.from_state` / `to_state` row covers two attribute names. The plan's acc #1 prose explicitly discloses this ("12 rows; `harness.breaker.from_state` / `to_state` is one row covering two attribute names"); §11.2 was read directly and confirmed to enumerate 13 distinct attribute names across 12 rows. The plan's rendering of the compound row as two set members is a faithful expansion of the cited contract, with the row-vs-name distinction surfaced honestly in the acceptance text. Not a divergence — this is the correct disposition of the OD compound-spec-row judgment call (one of the 4 carried §4A items; see disposition below).

8. **U-OD-20 v2.2 cascade closure intact.** U-OD-20 (the only OD unit revised pre-v2.5, at v2.2 cascade Step 6b + v2.4 Form A) was confirmed on the OD audit clean-list and is preserved-verbatim at v2.5; the §14.5 OD-spec-v1.3 absorption it carries is not touched by the v2.5 verbatim-conformance pass. No regression.

9. **U-CP-00 residence + closure discipline.** U-CP-00 declares `WorkloadClass` 4-value enum, `Files affected: harness-core`, matching the operator's Tension 003 resolution (declare in `harness-core` as a cross-axis shared type). acc #2 correctly documents — does not materialize — the §7.3 `extension-class` open-extension option; the enum stays closed at cardinality 4. No spec extension; the values are spec-committed at C-CP-07 §7.3 (verified). U-CP-00 is a clean foundational unit.

10. **A5 (missing uncertainty signals).** Implementation-plan units are not confidence-tagged artifacts; absence of `[SPECULATIVE]` tags in the revised units is expected for the artifact type. Not a finding.

11. **CP/OD coverage + dependency-graph deltas.** CP v2.4 declares null coverage/graph deltas (vocabulary conformance only); CP v2.5 declares one coverage-cell add (C-CP-07 §7.3 → U-CP-00) and one graph node add (U-CP-00 at L0, U-CP-22 L0→L1). OD v2.5 declares null coverage/graph deltas. Each declared delta was checked against the change-note rationale and is internally consistent; the U-CP-00 node is a pure source node (in-degree 0) so the acyclic invariant is preserved by construction. No hidden coupling surfaced.

12. **Deferred-materialization note at CP v2.5 §0.5.** The ~10 other `WorkloadClass`-consuming CP units gain a `[U-CP-00]` edge recorded but not yet written into their pointer-preserved bodies. CP v2.5 §0.8 tracks this as a plan-internal completeness item, explicitly not a fork, and notes none of the ~10 lands before its next revision-pass. This is a correctly-classified Class 3 (§2.7.6 informational) observation by the plan itself — not a blocking defect, recorded here as checked.

---

## Disposition of carried + flagged items (classify only — operator decides)

Per the task brief and SKILL.md §"Decision-claim vocabulary" / FM-C: each item below receives a §4.1 review-severity class, a §2.7.6 fork class, and the *shape* of resolution. No replacement text or spec-extension wording is supplied — that is operator-decision and `spec-writer` / `implementation-planner` territory.

### A. The 4 findings carried from the §4A conformance (CP v2.4 §0.8 / OD v2.5 §0.6)

| # | Item | §4.1 class | §2.7.6 fork | Resolution shape |
|---|---|---|---|---|
| **A-1** | **U-CP-43 4-axis input-set divergence + `MCP_TRUST` under-spec.** CP spec §19.1's `gate_level` `max()` is over `{per_tool_gate_level, blast_radius_floor, per_mcp_server_trust_floor, persona_tier_floor}` (verified directly at §19.1 lines 1622–1629); the plan's `GateLevelInput` is `{persona_tier, blast_radius_tier, deployment_surface, mcp_trust_tier}`. Two facets: (a) `per_tool_gate_level` — a §19.1-required C4 axis — is absent from the plan input set; (b) `deployment_surface` is plan-added and §19.1 does not carry it (§19.3's 5-axis composition does, via `sandbox_tier_floor`). `MCP_TRUST_GATE_LEVEL_FLOOR`: §19.1 names a "C10 five-tier framework" but enumerates no tier values or per-tier→gate-level mapping inside §19.1. | Class 3 | **Class 1 (halt-execution)** | Spec-silence shape — the cited section does not commit the structure the plan declares, and the plan omits a required axis. **Confirmed correctly classified by CP v2.4 §0.8.** Resolution is operator-decision: either `spec-writer` extends §19.1 to reconcile `per_tool_gate_level` and the trust-tier mapping (and to add or reject `deployment_surface`), or the operator sanctions the plan's input-set with recorded rationale. Not resolvable inside a verbatim-conformance pass. The `GateLevel` value-name conformance (`{AUTO, ASK, DENY}`) and the two spec-verbatim floor tables (`BLAST_RADIUS`, `PERSONA_TIER`) ARE conformed and verified — the divergence is isolated to the input-set + `MCP_TRUST`/`DEPLOYMENT_SURFACE` floors. |
| **A-2** | **U-CP-46 coverage shrink — dissolved plan-invented `audit.gate.*` namespace orphans `composition_winner`.** Conforming `AUDIT_NAMESPACE_SCHEMA` to CP spec §20.4 (7 signature-focused `audit.*` attrs — verified byte-exact) dissolved the v2.1/v2.3 plan-invented `audit.gate.*` / `audit.policy.*` attributes (and acc #12/#15/#16/#17 describing them). U-CP-43 acc #10's `composition_winner` field cited `audit.gate.composition_winner_axis` as its audit sink; that attribute is no longer in the cited contract. | Class 2 (for U-CP-46 itself: a documented, operator-ratified content removal — the §20.4 conformance is clean); the orphan rides on **U-CP-43** | **Class 3 (informational)** for the coverage-shrink record; the orphan is folded into **U-CP-43's Class 1 fork** | **U-CP-46 conforms cleanly to §20.4/§20.5/§20.6** — the coverage shrink is operator-ratified per §4A.4 and correctly documented at the v2.4 U-CP-46 coverage-shrink note. The *orphan* (`composition_winner` with no §20.4 audit sink) is a U-CP-43 surface, not a U-CP-46 surface — U-CP-43 v2.4 acc #10 already flags it. Resolution shape: if gate/policy audit attribution is required H_T behavior, it is a `spec-writer` extension of §20.4 — flagged, not invented; rides with the U-CP-43 operator decision (A-1). U-CP-46 itself is not blocked by this. |
| **A-3** | **U-CP-23 `default_pattern` single-vs-dual structural mismatch.** `PerWorkloadClassTopologyCommitment.default_pattern` is single-valued; CP spec §11.1 row 1 (`software-engineering`) declares **two** primary patterns — `evaluator-optimizer` (writes) and `orchestrator-workers` (reads/review/eval). Verified at §11.1 line 907. | Class 2 | **Class 2 (in-execution operator decision)** | Pre-existing v2.1 structural defect; not a verbatim divergence and not introduced by the conformance pass — the byte-aligned conformance made it visible. **Confirmed correctly classified by CP v2.4 §0.8.** The §11.1 row-1 dual-pattern commitment cannot be represented in a single-valued field. Resolution shape: operator decides whether a follow-on plan revision restructures `default_pattern` to multi-valued. U-CP-23's value-token conformance (the `TopologyPattern` rename) IS applied and verified; the structural-mismatch note is recorded at U-CP-23 acc #2. This is non-halting — U-CP-23 can land for the 3 single-pattern workload classes; the field shape is the open choice point. |
| **A-4** | **OD compound-spec-row rendering judgment call (U-OD-14 §11.2 / §11.3).** OD spec §11.2 table has 12 rows but enumerates 13 distinct attribute names (`harness.breaker.from_state` / `to_state` is one row, two names); U-OD-14 acc #1 claims cardinality 13. §11.3 has 6 rows; two rows are compound ("Session IDs, user IDs, tenant IDs"; "`audit.signature.sha256` / `audit.signature.prior_hash`"). | Class 1 (at most) — **not a finding** | **No fork** | The plan renders the compound rows as set members and **explicitly discloses the row-vs-name distinction in the acceptance-criterion prose** (acc #1: "12 rows; `harness.breaker.from_state` / `to_state` is one row covering two attribute names"). §11.2/§11.3 were read directly: the 13 / 6 cardinalities are a faithful expansion of the cited tables. This is a correctly-handled judgment call, not a divergence. **Disposition: cleared.** The honest in-text disclosure is the discipline the verbatim-conformance pass is supposed to apply; it applied it. Recorded here because the task brief named it as a carried item — the classification is "no fork; faithful rendering". |

### B. The 5 originally-flagged items

| # | Item | §4.1 class | §2.7.6 fork | Resolution shape |
|---|---|---|---|---|
| **B-1** | **U-CP-08 `FallThroughCause` design gap.** CP spec §3.2 (`on_layer_exceed_budget`) declares no enum — it is a procedure. The plan invents a 4-value `FallThroughCause` enum. There is nothing to conform to; the chain is genuinely silent. | Class 3 | **Class 1 (halt-execution)** of a *distinct shape* (design extension, not conformance tension) | **Confirmed correctly classified by CP v2.4 §0.8 + verbatim-audit Findings-rejected item 3.** Per `CLAUDE.md` I-2 / X-AL-3, a plan unit declaring an H_T structure the spec does not commit is a design extension Phase 7 may not silently absorb. Resolution shape: operator decides between (a) `spec-writer` extends CP spec §3.2 to commit the enum, or (b) operator sanctions the plan-extension with recorded rationale. Not a verbatim-conformance item; correctly NOT touched by the §4A pass. **U-CP-08 is not in the revised-unit set for this re-clearance** — it carries unchanged. |
| **B-2** | **U-CP-11 `LEASE_NAMESPACE_SCHEMA` naming call.** acc #1 claims "5 attributes per §5.3 verbatim: `lease.id, lease.holder, lease.acquired_at, lease.duration_ms, lease.event_kind`"; CP spec §5.3 declares `lease.key, lease.holder, lease.ttl_ms, lease.mechanism, lease.release_cause`. Cardinality 5 holds; only `lease.holder` matches by name. | Class 3 (on the strict verbatim calibration) | **Class 2 (in-execution operator decision)** | **Confirmed correctly classified by CP v2.4 §0.8 + verbatim-audit Findings-rejected item 1.** On the strict calibration this is the same divergence shape as the seven §4A cluster units; whether `lease.id ≈ lease.key` / `lease.duration_ms ≈ lease.ttl_ms` is a naming-refinement or a divergence is a judgment the reviewer does not own. Resolution shape: operator applies the strict reading (→ U-CP-11 joins a follow-on conformance pass as an 8th unit) or the refinement reading (→ no action). U-CP-11 is not in the revised-unit set; carries unchanged. |
| **B-3** | **U-OD-09 acc #2 tier-split design gap.** acc #2 asserts "4 Required / 3 Conditional tier classification per §7.1". OD spec §7.1 seven-attribute schema table (verified directly, lines 423–433) declares **no tier classification at all**. | Class 3 | **Class 1 (halt-execution)** of a *distinct shape* (design gap, not conformance tension) | **Confirmed correctly classified by OD v2.5 §0.6 FF-1.** The `BreakerScope` 4→2 conformance (acc #3) IS applied and verified byte-exact against §7.1. acc #2's tier split has no spec basis — a plan-introduced H_T structure. Resolution shape: operator decides between `spec-writer` extending §7.1 to commit a tier classification, or sanctioning the plan-introduced split with recorded rationale. OD v2.5 correctly preserved acc #2, the `tier:` annotations in `HARNESS_BREAKER_ATTRIBUTES`, and the `test_required_tier_*`/`test_conditional_tier_*` tests verbatim and did NOT conform them — the right call per X-AL-3. **U-OD-09 acc #2 blocks U-OD-09** (the unit's acceptance criteria remain internally contradictory until the operator dispositions acc #2); see the per-unit verdict. |
| **B-4** | **U-OD-28 `CollectorPlacement` "exactly 7 values per §20.1 verbatim".** §20.1 is a per-cell placement matrix, not a 7-value enum; the OD spec §1.2 collector-placement enum is 6-valued. The plan's 7-value enum resolves to no 7-element verbatim surface in §20.1. | Class 3 | **Class 2 (in-execution operator decision)** — *proposing* | **Confirmed correctly classified by OD v2.5 §0.6 FF-2 + verbatim-audit U-OD-28 row.** Decision-vocabulary label: *proposing* — both readings of §20.1 (verbatim enum the plan summarized; matrix the plan summarized) make the "verbatim" claim unsupportable, but the operator must confirm which §20.1 surface the claim targeted before the conformance target is fixed. OD v2.5 correctly carried U-OD-28 unconformed (target undetermined) — guessing the target would be FM-C author-mode drift. **U-OD-28 is not in the revised-unit set for this re-clearance** — it carries unchanged; conformance target pending operator confirmation. |
| **B-5** | **U-OD-29 `SandboxTier` `TIER_0..TIER_3` "per D2 v1.1 §1.2".** The prior OD verbatim-audit left this *open* pending an ADR-D2 §1.2 read. | Class 3 | **Class 1 (halt-execution)** | **NOW DECIDED — the prior *open* is resolved by a direct ADR-D2 read.** `ADR-D2.md` §1.7.1 (line 240) declares the canonical `sandbox.tier` enum as `{tier-1-process / tier-2-container / tier-3-microvm / tier-4-full-vm}` — i.e., **Tier-1..Tier-4**, four tiers, per the F4 four-tier graduated-isolation tier-set. The plan's `TIER_0..TIER_3` diverges from the ADR canonical (and from OD spec §20.3, which uses `Tier-1..Tier-4`). Additionally the plan's citation is wrong: ADR-D2 §1.2 is the sandbox **provider-class enumeration** (verified at ADR-D2 line 21 + line 48), not a tier-label section — the tier labels live at §1.7.1. So U-OD-29 carries both a value-divergence and a mis-citation. Resolution shape: this is a verbatim-divergence of the §4A cluster shape, but against an ADR rather than a spec-§; the conformance direction (Tier-1..4, citation corrected to §1.7.1) is authority-chain-determinate. Resolution is an `implementation-planner` pass conforming `SandboxTier` to the ADR-D2 §1.7.1 four-tier set + citation correction; operator ratification of the conform-to-ADR direction. **U-OD-29 is not in the revised-unit set for this re-clearance** — it carries unchanged; OD v2.5 §0.6 FF-3 correctly carried it pending the ADR read this pass performed. |

---

## Pipeline clearance verdict

Per `.harness/review-pipeline.md` §3–§4: a unit is **CLEARED** when the reviewer surfaced no blocking (§2.7.6 Class 1) fork against it; **BLOCKED** when a §2.7.6 Class 1 fork must be operator-resolved + applied before it can land. The verdict list covers the **revised units only** (CP v2.5 + the CP v2.4 conformance bodies it carries; OD v2.5). The carried/flagged non-revised units (U-CP-08, U-CP-11, U-OD-28, U-OD-29) are dispositioned above and do not receive a per-unit verdict line.

### Control Plane

| Unit | Verdict | Note |
|---|---|---|
| U-CP-00 | **CLEARED** | New foundational unit; `WorkloadClass` 4-value enum byte-exact vs C-CP-07 §7.3; `harness-core` residence matches operator Tension 003 resolution; closed-enum discipline clean. |
| U-CP-01 | **CLEARED** | `ROUTING_NAMESPACE_SCHEMA` 4-attr set conformed byte-exact to CP §1.4. |
| U-CP-10 | **CLEARED** | `LifecycleEventClass` 8-value enum + 8 span names conformed byte-exact to CP §5.1. |
| U-CP-12 | **CLEARED** | acc #4 `LifecycleEventClass` token enumeration conformed to U-CP-10; sampling-rate prose aligned to §5.4. |
| U-CP-19 | **CLEARED** | `ResumptionKind` 5-value enum + 1:1 engine-class bindings conformed byte-exact to CP §8.1. |
| U-CP-22 | **CLEARED** | `TopologyPattern`/`CascadePolicy`/admissibility conformed byte-exact to CP §10.1/§10.2/§10.3; `WorkloadClass` dependency on U-CP-00 resolved (Tension 003). One Class-1 documentation drift (F1-01) — non-blocking inline fix. |
| U-CP-23 | **CLEARED** | `PER_WORKLOAD_CLASS_TOPOLOGY` default-pattern values conformed to U-CP-22. Carries a §2.7.6 Class-2 (non-halting) operator decision on the single-vs-dual `default_pattern` field shape (A-3) — does not block landing for the 3 single-pattern workload classes. |
| U-CP-43 | **BLOCKED** | §2.7.6 Class-1 fork (A-1): the 4-axis `GateLevelInput` diverges from CP spec §19.1 (`per_tool_gate_level` absent; `deployment_surface` plan-added; `MCP_TRUST`/`DEPLOYMENT_SURFACE` floors spec-silent). Operator must disposition spec-extension vs sanctioned-plan-extension. The `GateLevel` enum + `BLAST_RADIUS`/`PERSONA_TIER` floors conformed cleanly; the block is isolated to the input-set + two carried floors. The `composition_winner` orphan (A-2) rides here. |
| U-CP-46 | **CLEARED** | `AUDIT_NAMESPACE_SCHEMA`/`HITL_SPAN_NAMESPACE_SCHEMA`/`PERSONA_TIER_AUDIT_EMISSION` conformed byte-exact to CP §20.4/§20.6/§20.5; section mis-citations corrected. Informational note: the operator-ratified coverage shrink dissolves the plan-invented `audit.gate.*`/`audit.policy.*` namespace (A-2) — non-blocking for U-CP-46 itself. |
| U-CP-47 | **CLEARED** | `ValidatorFailClass` 5-value enum + `VALIDATOR_FAIL_METADATA` + `VALIDATOR_FAIL_NAMESPACE_SCHEMA` conformed byte-exact to CP §21.1/§21.5. |
| U-CP-48 | **CLEARED** | `TRANSIENT_STAIRCASE_TRANSITIONS` `on_cause` enumeration conformed to U-CP-47; §21.4→§21.3 citation corrected; cross-unit composition verified. |

### Operational Discipline

| Unit | Verdict | Note |
|---|---|---|
| U-OD-02 | **CLEARED** | `BackendClass` 3→7 values + per-cell rows conformed byte-exact to OD §2.1. |
| U-OD-04 | **CLEARED** | 3-component span name / 7-operation enum / 3-tier `AttributeTier` / `gen_ai.client.operation.duration` base metric / acc #4 tier table conformed byte-exact to OD §4.1–§4.5. Absorbs prior Tension 004 per §4A.6. |
| U-OD-09 | **BLOCKED** | §2.7.6 Class-1 fork (B-3): acc #2's "4 Required / 3 Conditional tier classification per §7.1" has no spec basis — OD §7.1 declares no tier classification. The `BreakerScope` 4→2 conformance (acc #3) is clean and verified, but acc #2 keeps the unit's acceptance criteria internally contradictory. Operator must disposition spec-extension vs sanctioned-plan-extension before U-OD-09 lands. |
| U-OD-11 | **CLEARED** | `ALWAYS_SAMPLED_EVENT_CLASSES` 18-member set conformed byte-exact to OD §9.2. |
| U-OD-12 | **CLEARED** | `BASE_RATE_SAMPLED_EVENT_CLASSES` 13-member set conformed byte-exact to OD §10.1. |
| U-OD-14 | **CLEARED** | `CARDINALITY_SAFE_ATTRIBUTES` (13) + `CARDINALITY_PROHIBITED_ATTRIBUTES` (6) member sets conformed byte-exact to OD §11.2/§11.3; compound-row rendering faithful and disclosed in-text (A-4). |
| U-OD-30 | **CLEARED** | `SignatureAlgorithm` 3rd value `HMAC_SHA256`→`rsa-pss-2048` conformed byte-exact to OD §21.2 (chain-unanimous with ADR-D5 §1.4.1). |
| U-OD-32 | **CLEARED** | `BRIDGING_ARC_TRANSITIONS` 8-transition set (5 within-column + 3 diagonal) conformed byte-exact to OD §22.1. |
| U-OD-33 | **CLEARED** | `PreservationDimension` 5-member set conformed byte-exact to OD §22.2. |

**Summary:** 19 of 21 revised units CLEARED; 2 BLOCKED (U-CP-43, U-OD-09). Both blocks are pre-existing spec-silence design gaps the plans themselves already declared and carried — they are NOT defects introduced by the §4A conformance pass; the conformance pass correctly conformed everything authority-chain-determinate and correctly refused to silently absorb the spec-silence items (X-AL-3 honoured). U-CP-23 clears with a non-blocking §2.7.6 Class-2 operator decision noted. The cleared units flow to `pipeline-cleared-queue.md`; U-CP-43 + U-OD-09 flow to `pipeline-fork-queue.md` with the operator decisions named at A-1 / B-3. The non-revised carried units (U-CP-08, U-CP-11, U-OD-28, U-OD-29) remain operator-decision-pending per the disposition section — U-OD-29's prior *open* is now *decided* (conform to ADR-D2 §1.7.1 four-tier set + citation correction).

---

*Phase-7 pre-implementation re-clearance review. Read-only with respect to all `design-substrate/` artifacts, `CLAUDE.md`, and all plan/spec files — no canonical artifact modified by this pass. `ADR-D2.md` was read for the carried U-OD-29 disposition; not modified. Authored 2026-05-15 per `harness-adversarial-reviewer` SKILL.md Phase-7 pre-implementation review mode.*
