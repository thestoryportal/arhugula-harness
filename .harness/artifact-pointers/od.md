# OD — artifact pointer lineage

*Split byte-preservingly from `.harness/claude-artifact-pointers.md` at the U-CTX-03/04/05/06 R-CTX-1 context-optimization arc (2026-08-10, `B-17`/R-ICM-1 lineage). Old path is now a resolving stub — see `.harness/claude-artifact-pointers.md`. Loaded on demand only; never part of the default session-start read path.*

---

**Current head is DERIVED, not carried here.** This file is a lineage ARCHIVE; the authoritative per-family head version is derived from the `.harness/clearance/` marker corpus into `.harness/artifact-heads.md` by `tools/artifact_heads.py` (R-CTX-1 / U-CTX-11), and two CI gates keep that table current. Head labels inline below are reconciled at each arc but the generated table wins on any disagreement.

## §2.3 spec pointer

| Axis | Spec |
|---|---|
| OD | `Spec_Operational_Discipline_v1_42.md` (**v1.42 — canonical HEAD**, 2026-08-16 the `B-137` step-(3) candidate **C1** leg, operator-ratified 2026-08-16 — C-OD-09 §9.2 always-sampled exception set gains ONE row, **19 → 20**: `workflow.envelope`. An INGESTION REPAIR, not a new declaration: C-OD-25 §25.1 (`Spec_Operational_Discipline_v1_8.md:90`) has declared *"the envelope ALWAYS persists"* at head=1.0 since v1.8 and §9.2 never absorbed it — same shape v1.37 fixed for `fallback.exhausted`, differing only in that the declaring contract is intra-axis. Root membership is also the ONLY mechanism delivering the §9.2 floor to in-envelope member spans, since `ParentBased` consults the inner sampler only for roots. §9.1 / §9.3 / the §9 header block and all nineteen prior rows PRESERVED VERBATIM; literal/prefix decomposition moves 17+2 → 18+2. The ratification's carried-forward *"data loss, not delay"* cost was RE-MEASURED at this filing and does NOT hold (B-136's PR #1331 name-arm repair): 0 buffered / 0 evicted sequentially even at a buffer cap of 3, and the arc's follow-on claim that no configuration loses a span the status quo would have kept was WITHDRAWN at out-of-family review round 2 -- it compared cardinality, not identity: above `max_buffered_traces` C1's drop-oldest FIFO DISPLACES traces the base-rate draw would have preserved, while at the shipped 4096 default the displacement set is empty and C1 strictly dominates. Row 20 is written ROOT-CONDITIONAL in the contract (§0.2.1 + §0.2.3), joining `subagent.span (root)`, so the spec does not assert a floor the inheritance mechanism cannot deliver beneath a foreign unsampled parent (`B-186`); §9.3 and rows 1-19 are UNTOUCHED and this is NOT candidate C. The real residual — concurrent buffer occupancy × 1/base_rate — is split to its own register row `B-185`. Extends `B-160`'s head=1.0 divergence class 4 → 5 (its sweep was scoped to C-OD-3x and could not see C-OD-25). ZERO new contract numbers / namespace / attribute / emission-site change / CP delta / Runtime delta / CXA rows / hash impact. Back-flow record `b137-c1-admit-the-envelope-root-2026-08-16.md`; clearance `spec-operational-discipline-v1-42-cleared-2026-08-16.md`. Prior v1.41, 2026-08-12 the `B-141` cascade OD leg — C-OD-14 §14.5.3's escalation-on-mismatch table's `validator.fail.class` row `terminal-fail-exit` → `semantic_inconsistency` (`ValidatorFailClass` domain member per CP spec v1.116 / `B-138` operator-ratified disposition (a)); the invariance check, escalation prose, catalog-extension paragraph, and the table's other three rows incl. `permanence = permanent` + its C-OD-09 §9.2 always-sampled arm PRESERVED VERBATIM; the retry-exit ROUTING classification demoted from the wire name, not deleted (ADR-D5 v1.6 §1.10); same-PR code cascade `ReplaySemanticDivergenceError` default+witness; sibling legs ADR-D6 v1.3 + CP plan v2.52; zero production emission sites at HEAD (declared-shape-only); back-flow record `b-141-validator-fail-class-cascade-2026-08-12.md`; clearance `spec-operational-discipline-v1-41-cleared-2026-08-12.md`. Prior v1.40, 2026-08-11 the `B-144` OD leg — C-OD-05 §5.1 row 9 (`engine.*`) `Attribute count` 3→4, carrying the C-CP-09 §9.1 v1.3 supersession (+`engine.replay_disposition`, ADR-D1 v1.2 §1.1.1) the row's own `Ingest verbatim` posture + citation already point at (surfaced by out-of-family rounds 4-5 at PR #1311; the as-built `namespace_map.py` engine row tracked the stale 3 while citing §9.1 itself); all other §5.1 rows incl. row 6's hitl.* 11-across-4-span-names cell (`B-153` scope) PRESERVED VERBATIM; same-PR code cascade map-row+test; ZERO roster/namespace/attribute-mint/Runtime/CXA/hash impact; fork doc `class_1_fork_b144_cp_24_1b_stale_retable.md`; clearance `spec-operational-discipline-v1-40-cleared-2026-08-11.md`. Prior v1.39, 2026-08-09 — the `B-123` §10.2 conformance spec leg: **ONE amendment site**, §C-OD-09 **§9.2.1 term 3 REPLACED IN FULL**, retracting its explicit decline to widen the §10.2 classification-trigger predicate to span EVENTS. A **CONFORMANCE REPAIR, not a spec direction** — C-OD-10 §10.2's two event-shaped rows have specified the carrier as an *event* since the contract was written (`Spec_Operational_Discipline_v1_2.md:583-584`), so **§10.2 is NOT amended and its non-amendment is the point**. The term makes normative that the two-member §10.2 trigger set and the nineteen-member §9.2 roster are DISJOINT and must not be collapsed, that both name sets derive from a SINGLE declaration per trigger name, and that the predicate stays TOTAL and side-effect-free. Scope narrowed by an exhaustive `add_event(` sweep (six sites, none named `sandbox.violation` — already conformant as a real SPAN). **ADMISSION-BOUNDED:** term 4 inherited UNAMENDED, so per `B-137` the sibling tree is preserved only for head-ADMITTED carriers (~10% / ~20%) — no full-floor claim. A queued Class-3 rider was RE-GROUNDED and **FALSIFIED** (ADR-D2 §1.8 DOES carry `permanent-fail` at `:305/:306/:309`) and dropped. ZERO contract numbers, ZERO §10.2 edit, ZERO roster change (19), ZERO namespace delta (15), ZERO CXA rows (111), ZERO hash impact. Plan leg v2.34 U-OD-60; `B-123` CLOSED. Clearance `spec-operational-discipline-v1-39-cleared-2026-08-09.md`. Prior `Spec_Operational_Discipline_v1_38.md` — v1.38, 2026-08-08 the `B-133` realization spec leg — NEW **§C-OD-09 §9.2.1**, FIVE normative terms grounded on a positive control that exported **ZERO** spans at BOTH consumers for ALL THREE event-shaped §9.2 members: the TAIL consumer now resolves span EVENT names against the same `is_always_sampled` SSOT and forwards the carrier immediately; the §10.2 trigger predicate is deliberately NOT widened (**`B-123`**); the HEAD consumer is a **declared bound**, non-vacuous at `team-binding × local-development` (`HEAD_BASED_DEV`, §10.3 default 0.5, no tail consumer — which falsifies `B-133`'s own "samples at 1.0 anyway" hypothesis); an event-matching ROOT close must still materialize its trace decision. ZERO contract numbers / roster change / namespace delta / emission-site change / CP / Runtime / CXA rows / hash impact. Clearance `spec-operational-discipline-v1-38-cleared-2026-08-08.md`. **`B-133` closes** with its F-08 keep-volume step carried as a named OPEN residual; **`B-136` + `B-137`** newly registered — `B-137` being the out-of-family Codex round-1 finding adjudicated by measurement and CONFIRMED (the head applies the §10.3 ratio in both §9.1 modes, so the production tail cells admit only ~10%/~20% of event carriers; term 4's coverage claim retracted, the arm real but partial, and the starvation **non-uniform** since root-name-shaped members are admitted at 100%). Prior `Spec_Operational_Discipline_v1_37.md` (v1.37, 2026-08-08 the `B-116-t3` spec leg — the NAMED closure gate Runtime v1.112 §14.6.3 term **t3′** registered. **ONE amendment site**: the **C-OD-09 §9.2 always-sampled exception set 18 → 19**, adding `fallback.exhausted` (source declaration `C-CP-03 §3.5`) on the **CP-declares/OD-ingests** chain — CP `Spec_Control_Plane_v1_2.md:410` declares it always-sampled while OD dropped the row at original ingestion even as its two CP-table siblings at `:409`/`:411` were absorbed at OD `:521`/`:522`. A **spec-vs-spec ingestion gap, NOT code drift** — the shipped roster was byte-exact conformant to its owning contract, and no amount of conformance work can put a row into a table the table does not have. **§9.3's "inviolable" reconciled rather than treated as a bar**: it governs OPERATOR TUNABILITY at the deployment-binding layer, not contract revision at the venue where the set is defined (C7's F-03 ruling) — which is also why MEMBERSHIP is the only repair that survives operator tuning. **Rider (a) EXECUTED at this leg and PASSES**: the C-OD-11 §11.1 per-cell budget (`cell_rate_limit=10_000.0`; `tenant_rate_limit=1_000.0` at the two multi-tenant cells) enforces at COLLECTOR_BOUNDARY / BACKEND_INGESTION, **downstream of and independent of the sampling decision**, while the event stays terminal once-per-exhausted-chain with ONE emission site (`retry_breaker_fallback.py:947`) and no per-attempt multiplier — the F-08 population question stays **explicitly UNMEASURED** per C7's own FM-K refusal. Riders (b)/(c)/(d) cross-referenced to `B-123`/`B-124`/`B-125`, **NOT re-minted**; none is a precondition of the row. **HONEST SCOPE** (new change-note §, added at out-of-family Codex round 1): §9.2 membership is the CONTRACT, and its realization is BOUNDED at HEAD — both consumers classify span NAMES (`composite_sampler.py:110`; `tail_keep_span_processor.py:259`, which never inspects `span.events`) while THREE members are span EVENTS (`fallback.triggered`, `breaker.tripped`, `fallback.exhausted`), **PRE-EXISTING for the two siblings since ORIGINAL ingestion** — so the membership contract and the ingestion-chain direction are UNAFFECTED. Debt REGISTERED at NEW row **`B-133`** (same family as `B-123`/`B-124`); the reviewer's redesign remedies REJECTED at this leg as an unpriced X-AL-3 extension (a new emission primitive) / an over-sampling of every dispatch wrapper. **§9.1/§9.3 and §9.2's other eighteen rows PRESERVED VERBATIM; ZERO contract numbers, ZERO new namespace (C-OD-05 §5.1 roster unchanged at 15), ZERO CP delta, ZERO Runtime text moved, ZERO CXA rows (aggregate frozen at 111), ZERO emission-site change, ZERO hash impact.** Plan leg v2.32 (U-OD-58) bundled in the SAME PR; **`B-116` CLOSES at this leg** (impl leg U-RT-152 merged #1271). Clearance `spec-operational-discipline-v1-37-cleared-2026-08-08.md`. Prior `Spec_Operational_Discipline_v1_36.md` (**v1.36 — canonical HEAD**, 2026-07-30 the RATIFIED `B-69` spec leg — NEW §C-OD-30.5, the emission home for the accessor read + the pre-bootstrap staleness refusal. Both ride the EXISTING C-OD-30 family (no new namespace, C-OD-05 §5.1 roster UNCHANGED) but **neither is representable by the existing payload composition unchanged**, so §30.5.2 AUTHORIZES an additive carrier; the SAME token is emitted at both so a refusal is reconstructable as one causal pair; content splits by outcome (success → token + per-variant counts; failure → cause attribution, no token, no count, but it MUST still emit); sampling DECLARED, not inherited; **an OD plan delta is OWED AT THE IMPL LEG under either carrier option.** Clearance `spec-operational-discipline-v1-36-cleared-2026-07-30.md`. Prior:) `Spec_Operational_Discipline_v1_35.md` (v1.35 — 2026-07-23 B-33 spec leg (ii) — NEW §24.8 per-correlation-id rotation-pair evidence accessor (structural checks only, explicitly NOT a substitute for per-entry cryptographic signature verification — that remains a registered, not-yet-built residual); clearance `spec-operational-discipline-v1-35-cleared-2026-07-23.md`. Prior `Spec_Operational_Discipline_v1_34.md` (v1.34, 2026-07-18 RATIFIED B-51/B-52/B-54(+B-53) apply arc; clearance at `.harness/clearance/spec-operational-discipline-v1-34-cleared-2026-07-18.md` — AMENDED §21.2.1 tenant fifth segment + NEW §21.2.2 verification API + NEW §21.2.3 fail-closed policy. Prior v1.33, cleared 2026-07-16; v1.32 = B-19-BREAKER-AMBIENT-ATTRS §7.1 attrs (its vacuous-today prose corrected in-place 2026-07-18 per `B-43` + clearance marker), v1.33 = the C-OD-21 §21.2.1 SigningBackend seam; 2026-07-18 pointer refresh at the B-43 PR — prior head text preserved verbatim: v1.31 — canonical HEAD**, 2026-07-12 pointer catch-up per R-600 cadence-5 adjacent finding: this row was stale at v1.28 (root `CLAUDE.md` §2.3 was itself two versions ahead at v1.30 before this same catch-up). v1.31 ports `audit.rotation_correlation_id` into the `audit_namespace_attrs` open-dict convention (new §24.7) for B-AUDIT-KEY-ROTATION-RUNTIME (PR #938), zero Pydantic schema change; v1.28→v1.30 detail not reconstructed here (per-delta detail at each version's own change-note; see PR history). v1.28 chain below preserved verbatim as historical — v1.28 — R-CL-P4 OD tail-keep bounded-buffer closure: closes the v1.27 §2(a) bounded-buffer carve-out via operator-tunable `CollectorConfig` ceilings `tail_keep_max_buffered_traces` + `tail_keep_max_spans_per_trace` — drop-oldest trace eviction + per-trace overflow-span drop, both counted; a root-close-only new trace does not evict — eviction rationale + alternative at the carrier docstring; the bound scopes the `_buffer` (not the small per-trace `_keep` flag dict); ZERO new C-OD-NN contract (ceilings are runtime-impl `CollectorConfig` fields). Clearance marker `Spec_Operational_Discipline-v1_28-cleared-2026-06-10.md`. 2026-06-10. v1.27 prior — Fidelity-pure canonical-reading amendment closing OD spec v1.2 §C-OD-09 §9.3 deferred-to-implementation-discretion clause on (a) tail-based sampling decision algorithm + (b) tail-keep-on-classification filter implementation per OTel SDK + (c) always-sampled-event detection at SDK boundary. v1.27 declares the canonical reading at the deferred rows: algorithm + binding MATERIALIZE at the runtime span-processor materializer via NEW `TailKeepSpanProcessor` (homed at `harness-od/src/harness_od/tail_keep_span_processor.py`) wrapping `BatchSpanProcessor` at production-time deployment surfaces (self-hosted-server + managed-cloud); bypassed at local-development per §9.1 head-based mandate. NEW `is_classification_trigger(span)` per-span §10.2 trigger predicate at `harness-od/src/harness_od/tail_keep_classification.py` honoring 3 §10.2 triggers exhaustively (`validator.fail.permanence=permanent` attribute + `sandbox.violation` span name + `breaker.tripped` span name). NEW `SpanProcessorStage.tail_keep_processor: TailKeepSpanProcessor \| None` field at runtime materializer (None at LOCAL_DEVELOPMENT; non-None at production surfaces). Algorithm choice per §9.3 implementer-discretion: per-trace-completion-replay (buffer non-always-sampled spans by `trace_id`; root-close detected by `span.parent is None`; flush-or-drop materialization at root close). Wrap-BSP pattern (`TailKeepSpanProcessor` wraps BSP; registered on TracerProvider instead of BSP directly). Always-sampled spans (per §9.2) bypass buffer entirely (forward immediately at on_end); their tree-siblings buffered separately benefit from per-trace keep flag when classification trigger fires. §9.3 element (d) cross-SDK conformance test PRESERVED VERBATIM as deferred-indefinitely per single-SDK MVP scope (Target Stack Commitment v1 §5.2). **H_T-OD-3 PARTIAL → RETIRE-READY transit** at v1.27 publication: gate (b) §10.3 base_rate envelope CLOSED at PR #25 (v1.26); gate (a) §9.1 tail-keep-on-classification CLOSED at v1.27. Both OD-3 gates closed → tier transit per X-AL-2 retirement criterion (structural-criterion-B MET via materializer wrap + downstream BSP forwarding). Retirement event filing at `.harness/phase-7d-retirement-events-batch-36.md`. ZERO contract change; ZERO new field; ZERO new namespace; ZERO breaking change at any §C-OD-09 / §C-OD-10 surface; ZERO cross-axis cascade (intra-OD-axis canonical-reading amendment closing implementer-discretion clause without spec extension per X-AL-3). v1.26 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. Co-published: OD plan v2.25 → v2.26 single-unit canonical-reading amendment at U-OD-11 + NEW 21 tests (16 at `test_tail_keep_span_processor.py` + 5 at `test_lifecycle_span_processor.py`) + 3388/3388 tests pass + 10 skipped. **30th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** — pre-substantive advisor caught X-AL-2 risk (gate-text "all 3 triggers" vs "single trigger" scope choice; tier-transit vs within-PARTIAL refinement); operator AskUserQuestion ratified all-3-triggers (tier transit shape). NEW species candidate `single-row-multi-arc-tier-laddering` at workflow v1.12 §7.4.7.2 catalogued at batch-36 §3 (a) — OD-3 traversed STILL-BOUNDED → PARTIAL → PARTIAL (refined) → RETIRE-READY across 3 arcs in 1 calendar day (batches 34 + 35-via-PR-#25 + 36). 2026-05-28. v1.26 prior — Canonical-reading amendment clarifying §C-OD-10 §10.3 + §C-OD-13 §13.1 per-deployment persona_tier classification reading per `.harness/class_1_fork_od_3_od_4_retire_ready_persona_tier_plumbing.md` Reading (α) operator-ratified 2026-05-28 (Q5=α canonical-reading scope discipline). Surfaces a workspace-internal latent semantic-axis disambiguation defect: persona_tier is consumed at TWO semantic axes with distinct scope — CP-axis per-step / per-workflow via `StepEffectiveBinding.persona_tier` (CP spec v1.17 §6.5) drives gate-level / engine-class / HITL-matrix; OD-axis per-deployment via NEW `RuntimeConfig.persona_tier` (runtime spec v1.37 NEW field) drives sampler base_rate (§10.3 8-row table) + redaction discipline (§13.1 toggleability gradient). v1.26 §1.1 canonicalizes the OD-axis reading; §1.2 names the §10.3 materialization composition site (`materialize_tracer_provider_stage` via `PER_CELL_BASE_RATE_ENVELOPE[CellID(...)].default_rate`); ZERO body text amendment at §13.1 row text or §10.3 table values. §13.1 row 3 multi-tenant-compliance non-toggleability NEW-enforced at construction-time via `MultiTenantOverrideRefusedError` typed exception at `harness-od/.../redaction_span_processor.py` (empty `redacted_attributes` frozenset raises at multi-tenant). Solo-developer's §13.1 "OPERATOR_SELF_REDACT" mechanism column conformance scope-locked at MVP per fork doc §2.4 (α) recommendation (operator-runtime override mechanism deferred — per-session toggle requires session-control-substrate arc beyond deployment-binding-time scope). §10.3 "Base-rate envelope" column (operator-tunable ranges per cell) deferred to follow-on `RuntimeConfig.otel` sub-config extension arc. Refines H_T-OD-3 + H_T-OD-4 PARTIAL (within-PARTIAL refinement per X-AL-2 partial-retirement-is-non-retirement). OD-3 gate (b) §10.3 base_rate envelope CLOSED at v1.26 apply; OD-3 gate (a) §9.1 still deferred. OD-4 gate (a) §13.1 PARTIALLY CLOSED at v1.26 apply (deployment-level persona_tier + multi-tenant non-toggleability); per-session toggle + gate (b) §13.2 tokenization still deferred. Both rows remain PARTIAL (refined); no tier transit at this arc. ZERO cross-axis cascade per Q5=(α) — CP spec / AS spec / CXA / ADR / ADD / PRD unchanged. Co-published this arc: runtime spec v1.36 → v1.37 NEW `RuntimeConfig.persona_tier` field at §3 C-RT-03; OD plan v2.24 → v2.25 canonical-reading amendment at U-OD-04 + U-OD-12 + U-OD-16; production binding + 16 NEW tests; 3367/3367 tests pass + 10 skipped. v1.25 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-28. v1.25 prior — Fidelity-pure citation-correction patch closing 3 sibling phantom-`U-RT-30`-cite sites at §C-OD-27 spec body (line 10 §"What ships" enumeration + line 169 §C-OD-27 chapeau + line 199 §27.2 row 1) — all forward-looking unit-ID cites authored at OD spec v1.8 publication 2026-05-22 that never materialized as a runtime-axis plan unit. Closure path canonicalized at OD plan v2.14 §3.5..§3.8 `4-OD-B SqliteWritePath` cluster U-OD-42..U-OD-45 (LANDED PR #18 2026-05-28 commit `406fbf5`); real production carriers live at `harness-runtime/src/harness_runtime/lifecycle/ring_buffer.py` U-RT-29 landing + `harness-od/src/harness_od/sqlite_span_store.py` U-OD-42 landing. NEW §1 canonical-reading amendment table (3 sites refreshed) per delta-only-spec-file convention; v1.8 file body PRESERVED VERBATIM. Sub-species **3.forward-looking-code-comment-becomes-phantom-ledger-cite** catalogued at workflow v1.12 §7.4.7.2 — sibling closure-event-class to PR #17 OD-6 retirement-row phantom-cite + plan U-OD-43 carrier-path phantom (three instances of this sub-species in 8 days). Species-3 sub-species cardinality grows TEN → ELEVEN. H_T-OD-6 transit PARTIAL → RETIRE-READY-on-merge via PR #18 4-OD-B production binding chain (structural-criterion-B MET; full RETIRED gates on operator deployment per X-AL-2). ZERO contract / signature / acceptance-criterion / behavior change; ZERO cross-axis cascade. v1.24 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-28. v1.24 prior — Fidelity-pure citation-correction patch closing v1.23 §"Adjacent observations" finding (f) — `harness-od/` enum-rename arc deferred — via DERIVATIVE-naming retirement at production code layer. v1.23 §1.2 + §1.3 separated the requirement-level + stability dimensions at the canonical-reading layer but preserved enum member identifiers (`REQUIRED_STABLE` / `RECOMMENDED_DEVELOPMENT` / `OPT_IN_CONTENT`) + string values at `harness-od/src/harness_od/otel_genai_base.py:113-116` as DERIVATIVE naming per v1.16 §1.2 precedent. v1.24 retires the DERIVATIVE naming: enum identifiers + values renamed to canonical OTel vocabulary (`REQUIRED` / `RECOMMENDED` / `OPT_IN`; `CONDITIONALLY_REQUIRED` already canonical at v1.16). v1.23 (f) carry existed for ~70 minutes within the same session — closure landed immediately post-v1.23 push to `origin/main` (`8316383`). Sub-species **3.same-session-immediate-sequel** catalogued at v1.24 §"Change-note" Distinctive lineage finding — distinct from prior 4 species 3 sub-species (3.code-resolution / 3.fork-doc-closure / 3.workflow-grammar / 3.empirical-verification-of-external-authority). Sub-species set at species 3 now FIVE; workflow v1.9 §7.4.7.2 "Sub-species" column extension candidate strengthens at v1.24 (g) (5 sub-species in 3 consecutive arcs is empirical evidence the column extension is warranted). ZERO behavior change at runtime — tier IDENTITY unchanged; emission gating operates on tier identity, not identifier-string lookup. Production rename across 3 `harness-od/` files (otel_genai_base.py + tests/test_otel_genai_base.py + harness_breaker_schema.py); test value-set assertion at lines 152-155 refreshed in lockstep; 128/128 AttributeTier-touching tests pass post-rename (test_otel_genai_base.py 26/26 + test_attribute_class_enforcement.py + test_cross_family_rollup.py + test_idempotency_join_dedup.py). ZERO cross-axis cascade verified via grep — no non-`harness-od/` Python consumer of the renamed identifiers; design-substrate/.md cites (Phase_7_Class_1_Tension_004 + Impl_Plan_OD_v2_5 + v2_20 + v2_22 + Spec_OD_v1_19 + v1_23 + class_1_tension_u_od_09) all PRESERVED VERBATIM per delta-only-spec-file + file-resident-text-immutability conventions. v1.23 + earlier PRESERVED VERBATIM. 2026-05-27. v1.23 prior — Fidelity-pure citation-correction patch closing v1.22 §"Adjacent observations" finding (e) — `gen_ai.provider.name` stability tier divergence — via canonical-reading dimensional split at §C-OD-04 §4.3 tier-label vocabulary + NEW §4.3.1 stability classification table. Pre-v1.23 §4.3 tier labels (`Required (Stable)`, `Recommended (Development)`, `Conditionally Required`, `Opt-In content`) conflated two orthogonal OTel 1.41.0 dimensions — Requirement-Level + Stability; v1.16 §1.2 split 3 → 4 tiers on the requirement-level axis but inherited the v1.2-lineage conflation in 3 of 4 labels. v1.23 separates the dimensions at the canonical-reading layer: tier labels become PURE requirement-level (`Required` / `Conditionally Required` / `Recommended` / `Opt-In`); stability is declared per-attribute at NEW §4.3.1 table (all §4.3 base-layer attributes ship at OTel stability=Development per OTel 1.41.0 archived text WebFetch-verified 2026-05-27 — `gen_ai.operation.name` + `gen_ai.provider.name` Required+Development; `gen_ai.request.model` Conditionally Required+Development). v1.22 (e) carried verbatim across 7 versions (v1.16 → v1.22) awaiting operator-discretion stance routing; routed 2026-05-27 (this session, stance: separate the dimensions per operator AskUserQuestion). Sub-species 3.empirical-verification-of-external-authority catalogued at v1.23 §"Change-note" Distinctive lineage finding (sibling to v1.22's 3.workflow-grammar; both refinements of species 3 resolved-but-carry-stale-inherited at workflow v1.9 §7.4.7.2). ZERO behavior change at production — stability tier does NOT gate emission per v1.23 §1.3 emission-gating invariant; only Requirement-Level + cardinality-safe-attribute discipline (C-OD-11) gate emission. Co-published: production code comment + docstring refresh at `harness-od/src/harness_od/otel_genai_base.py` (AttributeTier docstring lines 82–115 + BASE_LAYER_ATTRIBUTES per-tier comments lines 159–195 + SPAN_NAME_FORMAT comment lines 128–132) for byte-exact alignment; enum identifiers (`REQUIRED_STABLE` / `RECOMMENDED_DEVELOPMENT` / `OPT_IN_CONTENT`) PRESERVED VERBATIM as DERIVATIVE naming per v1.16 §1.2 + v1.23 §1.2 precedent (enum-rename pass deferred as v1.23 §"Adjacent observations" (f) successor candidate — ~3-file ~49-site arc owed at future operator-discretion routing). ZERO cross-axis cascade verified via grep at design-substrate/ (CXA v2.13 + AS spec v1.7 + CP spec v1.19 + plan files do NOT cite the v1.2-lineage tier labels at canonical-reading sites). v1.22 + earlier PRESERVED VERBATIM. 26/26 harness-od/tests/test_otel_genai_base.py pass post-refresh (comment/docstring change only; enum identifiers + values preserved verbatim). NEW §"Adjacent observations" (g) catalogue candidate: workflow v1.9 §7.4.7.2 sub-species column extension — 2 sibling sub-species of species 3 catalogued in 2 consecutive arcs (3.workflow-grammar at v1.22; 3.empirical-verification-of-external-authority at v1.23) + 3.code-resolution + 3.fork-doc-closure form a 4-element set; future workflow-doc revision MAY add "Sub-species" column to §7.4.7.2. Pattern catalogued: external-authority WebFetch-verification at every spec-amendment arc catches v1.2-lineage drift that 7 prior delta arcs preserved without surfacing — strengthens workflow v1.9 §7.4.7.3 audit. 2026-05-27. v1.22 prior — Fidelity-pure citation-correction patch closing v1.21 §"Adjacent observations" finding (d) workflow-grammar discipline candidate + finding (i) FIFTH species post-authoring stale carry — both as **CLOSED-by-workflow-v1.9-§7.4.7** 2026-05-27. Workflow v1.9 publication (`8baddbd`) canonicalized §7.4.7.3 strengthened pre-substantive empirical-verification audit + §7.4.7.2 row 5 catalogued FIFTH species post-authoring stale carry — both v1.21 carries become stale at workflow v1.9 merge. FIRST PRODUCTION APPLICATION of workflow v1.9 §7.4.7.3 across workspace carry-set sweep 2026-05-27 (operator-routed "run the sweep"). Sweep enumerated 42 carries across 11 design-substrate/ artifacts; 7 STALE carries surfaced; 4 closure deltas owed (OD spec v1.22 + CXA v2.13 + OD plan v2.22 + CP plan v2.24). Sub-species 3.workflow-grammar catalogued at v1.22 §"Adjacent observations" (g) (resolved-but-carry-stale-inherited at upstream workflow-grammar canonicalization; distinct from species 3.code-resolution + species 3.fork-doc-closure). ZERO contract / signature / AC / behavior change. v1.21 + earlier PRESERVED VERBATIM. Co-published with workspace CLAUDE.md OD spec row bump + sibling closure deltas. 2026-05-27. v1.21 prior — Fidelity-pure citation-correction patch closing v1.20 §"Adjacent observations" finding (f) — `gen_ai.conversation.id` declared-but-not-emitted divergence — AND finding (g) — `server.port` + `server.address` declared-but-not-emitted divergence — both as **CLOSED-via-Path-A-production-emission-at-e874a03** 2026-05-27. The v1.20 carry-text framings for (f) + (g) were stale at v1.20 authoring commit `c326c03` (2026-05-26 23:14 -0600) BEFORE the Path A landing at `e874a03` (2026-05-27 16:43 -0600, +17.5 hours later). v1.21 corrects at the canonical-reading layer. NEW §1 finding-closure-disposition refresh (two-carry closure for v1.20 (f) + (g) with production emission sites at `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py:359` for `gen_ai.conversation.id` via `HIERARCHY_CORRELATION_KEY` + `:371` for `server.address` + `:373` for `server.port` gated; carrier at `harness-od/src/harness_od/otel_genai_base.py:145` with value `"gen_ai.conversation.id"`); NEW §2 cross-artifact cite-cascade disposition (7 sites; 1 co-published at workspace CLAUDE.md; 6 NO-change including helper constant + helper test constant-value-only + plan v2.21 line 37 closure already complete + ZERO references at runtime spec / CXA / CP / AS / ADR / ADD / PRD); NEW §3 sections-preserved-verbatim. ZERO contract change; ZERO signature change; ZERO acceptance-criterion change at C-OD-04; ZERO behavior change at production emission (Path A landed at `e874a03`; v1.21 is documentation-canonicalization for the closure disposition). ZERO cross-axis cascade verified via grep this session at `design-substrate/Spec_Harness_Runtime_v1.md` + `design-substrate/Cross_Axis_Composition_Document_v2_12.md`. v1.20 file body PRESERVED VERBATIM per delta-only-spec-file convention. **FIFTH species of stale-carry-text disposition catalogued — post-authoring stale carry** — SELF-AUTHORED carries stale BECAUSE downstream code landed AFTER the spec file body was authored, between authoring-commit-timestamp and next-substantive-amendment-opportunity; distinct from v1.18 species 4 (authoring-time stale carry) which operates on CONCURRENT unverified resolutions. Discipline candidate strengthens v1.18 §5: "re-verify inherited carries against production state AT THE TIME OF EACH NEW AMENDMENT (not only at the time of the originating arc)." THIRD PROSPECTIVE APPLICATION of v1.18 §5 strengthened discipline (FIRST at v1.19; SECOND at v1.20; THIRD at v1.21 — surfaced TWO stale carries per single arc). 21st application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture continues — pre-substantive empirical verification at worktree HEAD discriminated all 3 production emission sites + plan-side closure at v2.21 line 37 + ZERO cross-axis cascade; advisor pass not invoked at this arc per workspace precedent for well-rehearsed fidelity-pure citation-correction patches (v1.17 + v1.18 + v1.20 patches set the precedent). Carry-set at v1.21: (b)+(c)+(d)+(e)+(h)+(i) reflecting only the genuinely-open carries — (f)+(g) removed at v1.21 closure. NO fork doc filed per workspace precedent for fidelity-pure post-authoring stale-carry closures — the upstream Path A fork doc `class_2_fork_od_spec_declared_but_not_emitted_attributes.md` IS canonical authority for the Path A closure of v1.19 (g) + (h); v1.21 is the spec-side carry-text refresh per the same anchor. Co-published with workspace CLAUDE.md OD spec row bump. 2026-05-27. v1.20 prior — v1.20 §"Adjacent observations" (f)+(g) **CLOSED-via-Path-A-production-emission 2026-05-27** per `.harness/class_2_fork_od_spec_declared_but_not_emitted_attributes.md` RATIFIED-AND-APPLIED. Production at `harness-runtime/.../lifecycle/llm_dispatch.py:343-360` now emits 6 of the 9 non-Opt-In §C-OD-04 §4.3 attributes (was 5 at HEAD `0c4a99d`; added `gen_ai.conversation.id` + `server.address` + `server.port`); `gen_ai.conversation.id` sources from `step_context.workflow_id` via `HIERARCHY_CORRELATION_KEY` constant per CP spec v1.12 §25.2.1 9th-field absorption; `server.address` per-provider static map for anthropic (`api.anthropic.com`) / openai (`api.openai.com`) + threaded `RuntimeConfig.ollama_host` for ollama with new `_parse_ollama_host` URL parser (advisor 2026-05-27 correction to fork §3 static-map framing — OTel Conditionally Required "If `server.address` is set" gating prevents the static-map-lies-about-remote-daemon failure mode; static `localhost` for ollama would be a factual lie in telemetry when operator binds a remote daemon); `server.port` gated on `server.address` per OTel canonical condition; NEW `RuntimeLLMDispatcher.ollama_host: str | None = None` field threaded through `materialize_llm_dispatcher_stage` + bootstrap stage 5 wire-up at `stage_5_loop_init.py:138`; 7 NEW dispatch tests (1091/1091 harness-runtime pass + 4 skipped; was 1084+4 at HEAD `0c4a99d`; +7 net); pyright clean at `llm_dispatch.py` (0 errors). ZERO spec change — v1.20 §4.3 declares the tiers; production caught up to spec. ZERO cross-axis cascade — `step_context.workflow_id` already in scope at dispatch site per CP spec v1.12 9th-field absorption. **Posture precedent established: declared at canonical OTel tiers ⇒ emitted at production** (symmetric with v1.16 Tension 004 D-2 + D-3 re-litigation framing where harness emits ≥ OTel Conditionally Required floor; harness-stricter posture is conformant per "emit-more-often-than-Conditionally-Required is fine" framing). 19th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` resolved at fork doc filing 2026-05-27; 20th application this arc — advisor approved precedent-setting framing + flagged ollama-specific correction (thread config.ollama_host, don't static-lie) + flagged the `ollama_host=None` no-emission test as failure-mode coverage. v1.19 finding (g)+(h) carrier-vs-production-emission divergence pattern CLOSED at Path A application. v1.20 carries (b)+(c)+(d)+(h)+(i) preserved verbatim — non-closable in OD spec arcs (monitors / upstream / informational). **3rd Class 2 fork in workspace history closed**. v1.20 prior — Fidelity-pure canonical-reading amendment closing v1.19 §"Adjacent observations" finding (d) — §C-OD-04 §4.4 against OTel 1.41.0 archived text — as **CLOSED-via-narrowing-hierarchy-claim** 2026-05-27. §4.4 audit performed this arc against OTel GenAI semantic conventions 1.41.0 archived text at `github.com/open-telemetry/semantic-conventions/blob/v1.41.0/docs/gen-ai/gen-ai-agent-spans.md` (raw-fetched + cached at `$CLAUDE_JOB_DIR/gen-ai-agent-spans-v1.41.0.md`) + cross-referenced against gen-ai-spans.md execute_tool span attribute table (lines 612-621 cached at `$CLAUDE_JOB_DIR/gen-ai-spans-v1.41.0.md`). **Audit verdict**: OD v1.2 §4.4 (preserved through v1.19) claims `gen_ai.conversation.id` is correlation key for `invoke_agent` / `chat` / `execute_tool` hierarchy; ✓ chat-span carries `gen_ai.conversation.id` (gen-ai-spans.md line 76); ✓ invoke_agent client + internal spans carry it (agent-spans.md lines 217 + 478); ✗ **execute_tool span does NOT carry `gen_ai.conversation.id`** at OTel 1.41.0 (gen-ai-spans.md lines 612-621 declare 8 attributes — `gen_ai.operation.name`, `gen_ai.tool.name`, `error.type`, `gen_ai.tool.call.id`, `gen_ai.tool.description`, `gen_ai.tool.type`, `gen_ai.tool.call.arguments`, `gen_ai.tool.call.result`; none is conversation.id); cross-span linkage from execute_tool to its parent invoke_agent or chat span uses OTel-canonical trace context (trace_id + parent_span_id), not conversation.id. **Additional finding**: OTel does NOT canonicalize "hierarchy correlation key" framing for conversation.id; the OTel-derived framing is per-span attribute on chat + invoke_agent operations only — the "hierarchy correlation key" framing at OD §4.4 is OD-authored synthesis layered atop the OTel per-span attribute declarations (surfaced as NEW v1.20 §"Adjacent observations" (i)). v1.20 §1.1 narrows the §4.4 hierarchy claim from `{invoke_agent, chat, execute_tool}` to `{invoke_agent, chat}` per mirror-OTel stance (continuity with v1.19); preserves OD-authored synthesis layer for the 2 retained operations where OTel canonically supports the attribute. Cardinality-safe-attribute restriction at §4.4 PRESERVED VERBATIM. ZERO production-behavior change — production grep at HEAD `4333bc7` confirms ZERO `span.set_attribute("gen_ai.conversation.id", ...)` callsites at any production span (v1.19 finding (g) carried forward). **Pre-substantive advisor pass** redirected from premature stance-question framing to two verification steps (§4.4 sole-site grep + OTel-derived-vs-OD-authored framing check); both checks discriminated cleanly — §4.4 line 301 is sole OD spec body site of execute_tool inclusion in conversation.id hierarchy (doc-hygiene scope, not amendment-scope-widening); OTel does NOT canonicalize cross-span hierarchy mechanism (OD-authored synthesis layer); mirror-OTel narrowing is unambiguous. Advisor declined further calls — "use the v1.18 §5 discipline on yourself" — discipline-self-application succeeded. Co-published this session: helper docstring at `harness-od/src/harness_od/otel_genai_base.py:136-139` narrowed from execute_tool inclusion → `invoke_agent` / `chat` + trace-context cross-reference note for execute_tool linkage; helper test `test_hierarchy_correlation_key_is_conversation_id` UNCHANGED (asserts constant value only, no hierarchy-scope assertion); workspace CLAUDE.md OD spec row bump. **SECOND PROSPECTIVE APPLICATION** of v1.18 §5 strengthened discipline at substantive-amendment arc (FIRST was at v1.19) — pre-substantive empirical-verification audit identified ZERO stale-carries at v1.19 → v1.20 transition; discipline continues to validate. 18th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`. 1 NEW adjacent finding at v1.20: (i) OD-authored synthesis layer at §4.4 — distinct from per-attribute audit findings; documentable axis-discipline finding (Class 3 informational; future doc-hygiene footer routing). v1.19 findings (b)/(c)/(e)/(f)/(g)/(h)/(i) carried verbatim with audit-pass verification this arc. **No fork doc filed** per workspace precedent for fidelity-pure canonical-reading amendments (v1.15/v1.17/v1.18/v1.19 precedent). NO plan revision owed — U-OD-04 AC #5 covers constant-value test which is unaffected by the §4.4 doc narrowing. ZERO cross-axis cascade — no downstream artifact cites §4.4 hierarchy scope (verified via grep). 2026-05-27. v1.19 prior — Substantive canonical-reading amendment closing v1.18 §"Adjacent observations" finding (e) — §C-OD-04 §4.3 per-attribute tier-assignment audit against 4-tier table — as **CLOSED-via-tier-redistribution-to-OTel-1.41.0-chat-span** 2026-05-27. Per-attribute Requirement-Level audit performed this arc against OTel GenAI semantic conventions 1.41.0 archived text at `github.com/open-telemetry/semantic-conventions/blob/v1.41.0/docs/gen-ai/gen-ai-spans.md` chat-span table (raw-fetched via `gh api` + cached at `$CLAUDE_JOB_DIR/gen-ai-spans-v1.41.0.md`). Audit verdict: **3 chat-span attributes diverge** from OTel 1.41.0 Requirement Level — `gen_ai.request.model` (OD Required (Stable) → OTel Conditionally Required "If available"), `server.port` (OD Recommended (Development) → OTel Conditionally Required "If `server.address` is set"), `gen_ai.conversation.id` (OD Recommended (Development) → OTel Conditionally Required "when available"). v1.19 §1.1 redistributes these 3 attributes to the v1.16-NEW `Conditionally Required` tier per OTel canonical reading. **Updated tier-cardinality**: Required (Stable) 3 → 2; Conditionally Required 0 → 3 (NEW row content; v1.16 added the tier without populating it); Recommended (Development) 6 → 4; Opt-In content 8 (unchanged); total 17 attributes (attribute SET unchanged — only tier-assignment is redistributed). v1.19 §1.2 populates the emission-posture column for the Conditionally Required tier. ZERO production-behavior change — harness emits all 3 attributes at all spans where applicable (stricter posture than OTel's conditional rule; conformant per "emit-more-often-is-fine" framing). **Audit scope chat-span-only** per §4.1 alias-term "the LLM inference span" anchoring; OTel 1.41.0 declares 4 separate span tables (chat/embeddings/retrieval/execute_tool) — OD §4.3 lists non-chat-span attributes at Opt-In tier as content-policy (default-off PII discipline per §12.1), NOT as tier-binding from those spans' tables. **Tension 004 D-3b carry CLOSED-as-verified-MATCH at v1.19** (D-3b is specifically about `gen_ai.usage.input_tokens` + `gen_ai.usage.output_tokens` placement at Recommended (Development) — verified MATCH to OTel 1.41.0 chat-span Recommended per archived text lines 96-97; D-3b's attrs are NOT in the v1.19 redistributed set, which contains only `gen_ai.request.model` / `server.port` / `gen_ai.conversation.id`); full Tension 004 lineage now closed across all elements with split closure shapes — D-1 R2 / D-2 / D-3 amended at v1.12 / v1.16 / v1.16; D-3b / D-4 verified MATCH at v1.19 / v1.16. **Stance ratification**: operator deferred to assistant 2026-05-27; assistant chose mirror-OTel-tiers stance over document-harness-stricter-posture-explicitly stance for continuity with v1.16 spec→OTel-conformance lineage + cleaner amendment shape + harness-stricter emission correctly characterized as implementation-policy concern (OD plan declares emission policy at AC #4; the spec §4.3 declares tier classification; the two layers are separable). Co-published this session: helper carrier update at `harness-od/src/harness_od/otel_genai_base.py:148+157+158` (3 GenAiAttribute entries reassigned to `CONDITIONALLY_REQUIRED`); helper test redistribution at `harness-od/tests/test_otel_genai_base.py:55-77` (`_SPEC_REQUIRED_STABLE` 3 → 2; `_SPEC_RECOMMENDED_DEVELOPMENT` 6 → 4; NEW `_SPEC_CONDITIONALLY_REQUIRED` cardinality 3; NEW per-tier test); 3046/3046 tests pass + 4 skipped (was 3045 at v1.18; +1 NEW per-tier test). **FIRST PROSPECTIVE APPLICATION** of v1.18 §5 strengthened discipline at a substantive-amendment arc — pre-substantive empirical-verification audit at HEAD `a47c74c` identified ZERO stale-carries at v1.18 → v1.19 transition (all of v1.18 (c)–(h) carries verified still-genuine); discipline operationally validated. 17th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — pre-substantive advisor pass identified 3 discriminators materially reframing audit scope: chat-only vs cross-span; Requirement-Level vs Stability column (don't conflate with v1.18 (h)); production-emission as conformance reframe (harness-stricter is conformant; only candidate non-trivial divergence is `gen_ai.conversation.id` declared-but-not-emitted at production — now surfaced as NEW (g) carrier-vs-production-emission divergence). 3 NEW adjacent findings at v1.19: (g) `gen_ai.conversation.id` declared-but-not-emitted (Class 2 routing to runtime spec / impl); (h) `server.port` + `server.address` declared-but-not-emitted (sibling-of-(g)); (i) discipline-validation observation (informational, Class 3). v1.18 findings (c)/(d)/(f)/(g)/(h) carried verbatim with audit-pass verification this arc. 2 NEW divergence-pattern catalogue entries: carrier-vs-OTel-stability layer (v1.18 (h) lineage) + carrier-vs-production-emission layer (NEW at v1.19) are distinct divergence patterns at OD §4.3. **No fork doc filed** per workspace precedent (v1.16 Tension 004 D-2 + D-3 absorption precedent) — `class_1_fork_tension_004_d2_d3_otel_141_relitigation.md` IS canonical authority for the per-attribute audit it explicitly deferred at §4 step 3. NO plan revision at this arc — OD plan v2.20 U-OD-04 AC #4 refresh sequenced at follow-on arc per implementation-planner discipline. ZERO cross-axis cascade — no downstream artifact cites per-attribute tier rows of §4.3 (verified via grep). 2026-05-27. v1.18 prior — Fidelity-pure citation-correction patch closing v1.17 §"Adjacent observations" finding (g) — `_PROVIDER_OPERATIONS` value-space non-conformance to §4.2 operation enum — as **CLOSED-as-resolved-at-production-commit-ca5674b** 2026-05-26 + finding (h) — `provider_name` value-space partial-conformance to OTel 1.41.0 known-values enum — as **CLOSED-NOT-A-DEFECT-per-fork-doc-§10** 2026-05-26. **Distinctive lineage finding: both (g) and (h) were authored FRESH at v1.17 (this session, 2026-05-26 — same session as v1.17 publication) WITHOUT empirical verification at authoring time.** The defects had been resolved at commits `ca5674b` (g — 2026-05-26 19:45 -0600) + fork doc `class_1_fork_genai_span_name_four_way_drift.md` §10 (h) BEFORE the v1.17 amendment text was written; production self-documents (g) closure at inline comment `harness-runtime/.../llm_dispatch.py:502-504`. **FOURTH species of stale-carry-text disposition catalogued at v1.18 §5 — authoring-time stale carry** — distinct from prior 3 species which operate on INHERITED carries (v1.15 phantom-as-described + v1.16 different-shape + v1.17 resolved-but-carry-stale-inherited); the v1.18 pattern operates on SELF-AUTHORED carries (carry-text stale at the moment of writing because underlying production state / fork doc closure / cross-artifact resolution not empirically verified at authoring time). Discipline strengthening upgrades "at fork-arc opening empirically verify EVERY claim" (v1.16/v1.17 candidate) to "at EVERY §Adjacent observations entry authoring (inherited OR new) empirically verify the entry against production state / fork doc closures / cross-artifact resolutions at the moment of writing." Co-published this session: runtime spec v1.29 NEW change-note refreshing v1.27 top adjacent-observation (g) carry-text disposition + v1.28 top change-note paragraph reiteration via canonical-reading amendment table (both sites stale per the same lineage event); workspace CLAUDE.md OD spec + runtime spec row bumps. ZERO contract change; ZERO signature change; ZERO acceptance-criterion change; ZERO behavior change at any consumer. 1 NEW adjacent finding surfaced — (h) `gen_ai.provider.name` stability tier divergence (OTel declares `stability: development` at attribute + member layer; OD spec C-OD-04 §4.3 classifies as Required (Stable) tier — divergence is at spec-narrative layer, not wire-protocol layer; per fork doc §10 sub-finding (i)) — NOT patched per FM-2 single-focus arc scope. 5 inherited carries audited empirically this session + verified still-genuine (b–f at v1.17 numbering → (c–g) at v1.18 numbering; (b) §8.4.2 anticipated cases, (c) §15.2 vs §15.4 split, (d) tier-assignment audit, (e) §4.4 audit, (f→g) workflow-grammar discipline candidate STRENGTHENED). 16th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — advisor flagged audit-discipline broadening (audit ALL of v1.17 §"Adjacent observations" (b–h) before authoring v1.18; one corrective delta, not multiple sequential closures); empirical-verification audit this session confirmed (b)–(f) carries still genuine; (g)+(h) stale-as-described. NO fork doc filed — the upstream `class_1_fork_genai_span_name_four_way_drift.md` IS canonical authority for both closures (§9 for g; §10 for h); re-using it as authority anchor without modifying it is correct routing. 2026-05-26. v1.17 prior — Fidelity-pure citation-correction patch closing v1.13/v1.14/v1.15/v1.16 §"Adjacent observations" finding (d)/(c)/(c)/(b) — `gen_ai.system` vs `gen_ai.provider.name` divergence — as **CLOSED-as-resolved-at-production-commit-115387b** 2026-05-26. The divergence flagged at v1.12 §"Adjacent observations" (f) was resolved at producer-side commit `115387b` (2026-05-26 19:40 -0600) via rename `gen_ai.system` → `gen_ai.provider.name` at `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py:341` + NEW emission of `gen_ai.operation.name` at line 340 + preserved `gen_ai.request.model` at line 342 — all 3 §C-OD-04 §4.3 Required (Stable) tier attributes now emit on every GenAI span. The v1.13/v1.14/v1.15/v1.16 carry-text was never refreshed when the resolution landed; v1.17 corrects the carry-text disposition + closes the finding at the canonical-reading layer. NEW §1 canonical-reading amendment table (7 carry-text sites refreshed) + §2 finding closure + §3 cross-artifact cite-cascade disposition (8 cite sites enumerated; 2 refreshed at this arc — runtime spec body lines 1963+1993 via NEW v1.28 delta + harness-od/CLAUDE.md in-place; 6 carry per FM-2 with downstream readers applying v1.17 §1.1 mapping in-context — CXA v2.11/v2.12, AS spec v1.7, OD plan v2.19/v2.20, runtime spec v1.27 top adjacent-observation) + §5 pattern catalogue (THIRD species of stale-carry-text disposition catalogued — sibling to v1.15 phantom-as-described + v1.16 stale-carry-with-real-but-different-shape; common ancestor "stale carry-text disposition"; discipline candidate "at fork-arc opening empirically verify EVERY claim BEFORE treating the carry as actionable"). Co-published this session: runtime spec v1.28 (NEW change-note + canonical-reading amendment table refreshing §14.5 body cites at lines 1963 + 1993); harness-od/CLAUDE.md H_T-OD-2 retirement entry in-place edit; workspace CLAUDE.md OD spec + runtime spec row bumps. ZERO contract change; ZERO signature change; ZERO acceptance-criterion change; ZERO behavior change at any consumer. 2 NEW adjacent findings surfaced — (g) `_PROVIDER_OPERATIONS` value-space non-conformance to §4.2 operation enum + (h) `provider_name` value-space partial-conformance to OTel 1.41.0 `gen_ai.provider.name` known-values enum (`ollama` not in 1.41.0 enum) — NOT patched per FM-2 single-focus arc scope. 15th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — pre-substantive advisor pass confirmed resolved-but-carry-stale diagnosis (distinct from v1.15 phantom + v1.16 different-shape lineage patterns) + sharpened scope selection (A/B/C ladder; operator ratified C = wider) + flagged downstream sites my initial sketch undercounted (runtime spec body + harness-od CLAUDE.md not in OD-only sketch). NO fork doc filed per workspace fidelity-pure citation-correction precedent (CP spec v1.14 §1.2; runtime spec v1.16; OD spec v1.15). **Pattern catalogued: resolved-but-carry-stale** — a defect flagged at vN with disposition "NOT patched per FM-2; separate apply-pass arc owed" gets resolved at downstream code (production / impl / co-publication) at commit C BEFORE the carry-text disposition is refreshed at vN+1, vN+2, etc.; carry-text becomes stale on day of resolution but propagates verbatim across delta files. Discovery requires empirical-grep verification; corrective re-litigation closes as **CLOSED-as-resolved-at-{commit}** rather than **CLOSED-as-resolved-at-{spec-version}**. Common ancestor with v1.15 phantom-as-described + v1.16 stale-carry-with-real-but-different-shape: "stale carry-text disposition." 2026-05-26. v1.16 prior — Tension 004 D-2 + D-3 re-litigation Class 1 fork resolution apply pass per `.harness/class_1_fork_tension_004_d2_d3_otel_141_relitigation.md` §4.1 operator-ratified 2026-05-26 option (A) full conformance + §4.2 option (γ) single-focus. NEW §1 canonical-reading amendment table for §C-OD-04 §4.2 + §4.3: §1.1 operations enum 7 → 9 values (add `invoke_workflow` + `retrieval` per OTel GenAI semconv 1.41.0 archived text at `github.com/open-telemetry/semantic-conventions/blob/v1.41.0/docs/gen-ai/gen-ai-spans.md`); §1.2 attribute tiers 3 → 4 tiers (add `Conditionally Required` per same archived text); §1.3 D-3b tier-assignment PRESERVED VERBATIM (per-attribute audit owed at future arc); §1.4 §4.5 base metric verified MATCH byte-exact (no amendment owed; gen-ai-metrics.md WebFetch-confirmed `gen_ai.client.operation.duration` Histogram). NEW §2 finding-(b) closure (v1.13/v1.14/v1.15 Tension 004 D-2/D-3/D-4 carry — CLOSED-as-resolved-via-re-litigation; D-2 + D-3 amended at v1.16; D-4 MATCH). NEW §3 Tension 004 §7 reconciliation supersession documentation (the 2026-05-26 §7 reconciliation closed D-2/D-3 as "RESOLVED at plan v2.5 plan-conforms-to-spec" without re-performing the §4 step 3 tiebreaker check; v1.16 performs the check + supersedes per the doc's own framing — **SECOND tension re-litigation in workspace history** after D-1 R2 this morning; D-3 specifically REVERSES the v2.5 plan-strike of `CONDITIONAL` — the original v2.1-v2.4 plan was conformant to OTel 1.41.0, the spec was wrong, plan-conforms-to-spec was wrong-direction). v1.2-v1.15 PRESERVED VERBATIM per delta-only-spec-file convention; downstream readers MUST apply v1.16 §1 substitutions when interpreting §C-OD-04 §4.2 + §4.3 from prior versions. ZERO contract removal; additive on enum (+2 values) + tiers (+1 tier); backward-compatible at consumer layer per Pydantic v2 StrEnum semantics. ZERO cross-axis cascade — verified at production grep: `harness-runtime/.../llm_dispatch.py:505-508` `_PROVIDER_OPERATIONS` dict maps all 3 providers to `GenAiOperation.CHAT`; no exhaustive match on enum cardinality; 27/27 runtime LLM dispatch tests pass post-amendment. Co-published this session: OD plan v2.19 → v2.20 (U-OD-04 single-unit absorption: AC #2 cardinality 6 → 9 + AC #3 cardinality 3 → 4 + 2 test renames `_seven` → `_nine` / `_three` → `_four` + 3 NEW test names `_includes_invoke_workflow` / `_includes_retrieval` / `_conditionally_required_present`); helper update at `harness-od/src/harness_od/otel_genai_base.py:58-86` (GenAiOperation StrEnum 7 → 9 members alphabetic-ordered + AttributeTier StrEnum 3 → 4 members + docstring updates citing v1.16 §1.1 + §1.2); 25/25 helper tests pass (3 NEW); Tension 004 doc NEW §7.6 supersession block; workspace `CLAUDE.md` §2.3 OD spec + OD plan rows bumped. **14th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** — advisor confirmed phantom-vs-real diagnosis (v1.15 carry-text framing implied "spec amendment owed at §4.2/§4.3/§4.5"; actual shape was "WebFetch verification owed; +2/+1 cardinality refresh + D-4 MATCH"); sharpened arc shape (verification + outcome-branched closure); strict stop-point on divergence (STOP + AskUserQuestion routing before fork doc authoring). 6 adjacent findings preserved at v1.16 §"Adjacent observations" — NOT patched per FM-2 (a/b/c/d carries from v1.15; (e) NEW §C-OD-04 §4.3 per-attribute tier-assignment audit; (f) NEW §C-OD-04 §4.4 archived-text audit; (g) NEW workflow-grammar reconciliation-pass tiebreaker-check re-performance discipline candidate). **Pattern catalogued: reconciliation-pass tiebreaker-check re-performance discipline** — reconciliation passes that close divergences against internal authority without re-performing named external-authority tiebreaker checks CAN silently absorb the same defect twice (Tension 004 §7 closed D-2/D-3 without the check; v1.16 is the corrective re-litigation; same lineage as D-1 R2 morning arc). **Pattern catalogued: stale-carry-with-real-but-different-shape** — v1.13/v1.14/v1.15 carry forecast wrong defect shape ("amend spec sections") but real defect existed at different shape ("perform deferred WebFetch tiebreaker check"); mirrors v1.15 finding (a) phantom closure pattern at one level of generality up. 2026-05-26. v1.15 prior — Fidelity-pure citation-correction patch closing v1.13/v1.14 §"Adjacent observations" finding (a) as **phantom-as-described** 2026-05-26. Empirical verification at OD spec v1.2 line 333 (preserved verbatim through v1.14) confirms §C-OD-05 §5.1 **row 5** is the canonical `sandbox.*` namespace declaration with cite to C-AS-15 **§15.2** (7-attribute table); row 10 in v1.2 is `audit.*` (line 338); AS spec v1 §15.2 declared 7 sandbox attributes from v1 baseline with no 6→7 cardinality event in lineage. The v1.13 finding (a) framing ("6-attribute via AS §15.4 reference") is incorrect on three claims: row reference (10→5), section cite (§15.4 always-sampled events ≠ §15.2 attribute declaration), cardinality event (no 6→7 extension exists). The actual defect is row-number + section-cite drift introduced at v1.13 sub-note authoring (AS-4 dual-attribute arc `ff67bc7..0c22efc`) and propagated through v1.14 §C-OD-08 §8.4 (mcp.* cross-emission arc `c646577..65692bc`). v1.15 amendment scope: NEW §1 canonical-reading amendment table mapping 25 drift sites across v1.13 + v1.14 (§1.1 row-number drift sandbox.* "row 10"→row 5 + mcp.* "row 4"→row 2 at 22 sites including v1.13 finding (b) line 70 mcp.* parallel-drift site; §1.2 section-cite drift sandbox.* §15.4→§15.2 + mcp.* §14.8→§14.3 at attribute-declaration cite sites preserving §15.4 + §14.8 as supplementary cites for sampling/audit-floor-posture contexts; §1.3 NOT-drift verification — hitl.* row 6 + anthropic.* row 1 + AS §15.2 cite at v1.14 line 81 §8.4.4 all already canonical); NEW §2 finding-(a) closure (CLOSED-as-phantom on empirical verification — three-claim breakdown enumerated); NEW §3 origin-of-drift documentation (lineage-level, not blame framing per workspace `CLAUDE.md` §8 I-1 byte-exact + §4.3 Class-3 informational routing). Per delta-only-spec-file convention: v1.13 + v1.14 files PRESERVED VERBATIM; downstream readers apply §1 substitutions when interpreting those files. ZERO contract change; ZERO signature change; ZERO acceptance-criterion change; ZERO behavior change; ZERO cross-axis cascade. NO fork doc filed — fidelity-pure citation correction with conclusive empirical posture follows workspace precedent (CP spec v1.14 §1.2 cite-shape correction 2026-05-24; runtime spec v1.16 phantom-cite resolution 2026-05-22). v1.13/v1.14 finding (a) removed from carry; carries (b)+(c)+(d) preserved. CXA v2.12 §0.4 cite-refresh deferred per FM-2 (downstream readers apply v1.15 §1.1 mapping in-context). NO plan revision owed (atomic units cite AS spec §15.2 + §15.9 directly, not via OD row numbers). 13th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — advisor caught the phantom-as-described diagnosis before any spec edit; orientation grep on AS spec attribute-count lineage + OD v1.2 row table revealed the cardinality claim was forecast-of-defect-that-never-landed; routing changed from "cardinality refresh arc" to "inline cite-correction patch + phantom closure". 2026-05-26. v1.14 prior — `mcp.*` namespace cross-emission on non-`mcp.tool.call` spans Class 2 fork resolution apply pass per `.harness/class_1_fork_mcp_namespace_cross_emission_ingestion_ownership.md` §6 operator-ratified 2026-05-26 (Q0 = Class 2 in-execution decision; Q1 = Reading A parent-span-class rule; Q2 = (α) single NEW §C-OD-08 §8.4 sub-section; Q3 = (a) ratify-apply-now paired publication; Q4 = mixed (ii) empirical-state footer; Q1 + Q4 deferred to assistant recommendation per session continuation). NEW §C-OD-08 §8.4 canonicalizing the **parent-span-class ingestion routing rule** for cross-namespace attribute carriers on a single span: when an attribute uses namespace-prefix-A but is emitted on a span-class-B, OD audit-ledger ingestion is owned by §C-OD-05 row-B (parent-span-class primacy), NOT row-A (attribute-prefix nominal). §8.4.1 worked example: `mcp.fail.class` on `sandbox.violation` ingests via row 10 (`sandbox.*`) — generalizes the v1.13 §C-OD-05 §5.1 row 10 sub-note from one-off precedent to canonical rule. §8.4.2 anticipated cases (3 illustrative not-yet-emitted specialization-namespace co-emission cases — `topology.*` on `sandbox.exit`, `audit.*` on `hitl.invocation.responded`, `validator.*` on `mcp.tool.call`); OTel canonical attributes at §C-OD-04 (`gen_ai.*`) explicitly out-of-scope per §8.4 scope statement (governed independently at §C-OD-04 + §8.1 no-override invariant); §8.4.3 empirical-state footer per Q4 mixed (ii) ratification (production at HEAD `0c22efc` emits the §8.4.1 case but NO consumer routes on attribute-prefix at OD ingestion — `namespace_map.py` is structural reference, `*_namespace_verification.py` are set-membership verifiers; §8.4 is doc-canonicalization for future-proofing, NOT behavioral-routing); §8.4.4 row-attribute-set composition (cross-namespace ingestion does NOT increment row attribute counts; carriers are additive at span layer, not new row-attribute-set members). §C-OD-08 §8.1 (no-override invariant) + §8.2 (canonical anthropic.*/gen_ai.* example) + §8.3 (cross-namespace cardinality discipline) PRESERVED VERBATIM from v1.2 lineage. ZERO new namespace; ZERO new ingestion row; ZERO row-attribute-count increment; ZERO cross-axis cascade (CXA v2.12 §0.4 already handles spec ↔ spec citation discipline; no edge change; no cardinality refresh; ADR-D6 §1.2 reference frame UNCHANGED per X-AL-3 + Q2 narrow-scope). Closes OD spec v1.13 adjacent finding (b) (`mcp.*` cross-emission namespace ownership ambiguity). 4 adjacent findings preserved at v1.14 §"Adjacent observations" — NOT patched per FM-2 (v1.13 finding (a) cardinality drift carry + (c) Tension 004 carries + (d) gen_ai.system divergence + v1.14 finding (d) anticipated-cases empirical-verification). 12th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — advisor caught Class 1 over-classification risk before any spec edit; fork doc filed FIRST with Q0 classification ambiguity, operator ratified Class 2. Workspace-correct discipline: file fork → ratify §6 → apply. 2026-05-26. v1.13 prior — AS-4 dual-attribute `sandbox.violation` OD/CXA ingestion cascade Class 1 fork resolution apply pass per `.harness/class_1_fork_as_4_od_cxa_dual_attribute_cascade.md` §6 operator-ratified 2026-05-26 Option A (paired single-arc: OD §C-OD-05 sub-note + CXA §0.4-shape convention seam). NEW §C-OD-05 §5.1 row 10 sub-note acknowledging AS spec v1.6 §15.9 dual-attribute co-emission discipline: `mcp.fail.class` co-emitted with `sandbox.fail.class` on the `sandbox.violation` child span when an MCP-protocol-layer failure surfaces a sandbox violation. Substrate-layer namespace ingestion footnote, NOT a lifecycle-event classification — §C-OD-06 §6.1 PRESERVED VERBATIM per Option A ratification (Option D §6.1 lifecycle event row was explicitly NOT-SELECTED at fork §6 Q1). Ingestion-posture invariants at v1.13: (1) single-attribute case (process-execution failure) — `sandbox.fail.class` only; (2) dual-attribute case (MCP-protocol-layer failure) — both `sandbox.fail.class` (best-effort projection per AS §15.10) AND `mcp.fail.class` (raw per AS §15.8) co-emitted; (3) best-effort projection acknowledgement — OD audit-ledger does NOT re-validate the projection; raw `mcp.fail.class` is canonical MCP-layer fail-class declaration. ZERO new lifecycle event class; ZERO new namespace; ZERO new ingestion row — additive sub-note ONLY. Closes AS plan v1.4 §3 (b) FM-2 deferral. 4 adjacent findings surfaced per v1.13 §"Adjacent observations" — NOT patched per FM-2. Co-published this session with CXA v2.11 → v2.12 §0.4-shape convention seam declaration (sibling commit). 2026-05-26. v1.12 prior — GenAI span-name format Class 1 fork resolution apply pass per `.harness/class_1_fork_genai_span_name_four_way_drift.md` §7.4.1 (R2) operator-ratified 2026-05-26 option (A) full acceptance. NEW §C-OD-04 §4.1 canonical-reading amendment: 3-token form (`{operation} {provider} {model}` v1.2-v1.11) SUPERSEDED → 2-token form (`{operation} {model}`) byte-exact to OTel GenAI semantic conventions 1.41.0 archived text at `github.com/open-telemetry/semantic-conventions/blob/v1.41.0/docs/gen-ai/gen-ai-spans.md` ("Span name SHOULD be `{gen_ai.operation.name} {gen_ai.request.model}`"). `gen_ai.provider.name` REMOVED from span-name format; PRESERVED at §4.3 Required (Stable) tier attribute per narrow-scope framing. Re-litigates `Phase_7_Class_1_Tension_004_OD04_Span_Schema_Divergence.md` (2026-05-15) D-1 ratification — that ratification's own §4 step 3 named the tiebreaker check against actual 1.41.0 text that was deferred-not-performed; this arc performed the check (WebFetch 2026-05-26); the check failed; Tension 004 D-1 ratification SUPERSEDED per its own framing. Narrow scope per FM-2: §4.2 operations enum 6-vs-7 divergence (Tension 004 D-2) + §4.3 attribute tiers 4-vs-3 divergence (D-3) + §4.5 base metric divergence (D-4) all PRESERVED VERBATIM — owed at separate apply-pass arcs. Co-published this session: helper at `harness-od/src/harness_od/otel_genai_base.py:104` SPAN_NAME_FORMAT + `span_name()` signature drops `provider` parameter; test at `harness-od/tests/test_otel_genai_base.py:79-92` 2-token assertion (renamed `test_span_name_format_byte_exact_three_component` → `_two_component`). R4 STRIKE concurrent at `.harness/class_3_drift_c_od_04_gen_ai_binding_site_silence.md` line 34 false claim. Cross-file absorption owed (R1 production rename at `llm_dispatch.py:324` + runtime spec line 2033 deferral suggestion removal — separate follow-on arc; R3 AS spec alias-term abstraction at §14.1 row 1129 + 11 source-file docstring sites — separate follow-on arc per fork §7.4.3 option (b)); implementation-planner follow-up owed for OD plan v2.18 → v2.19 U-OD-04 absorption (sequenced after spec lands). 6 adjacent findings surfaced per v1.12 §"Adjacent observations" — NOT patched per FM-2. 2026-05-26. v1.11 prior — Pause/resume back-flow arc (narrow scope per operator AskUserQuestion 2026-05-24 — "Narrow — OD-side helper only") per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §9 residual routing target. NEW §C-OD-30.4 — `PauseResumeAuditPayload` canonical production-invocation contract authoring: two helper signatures (`_project_pause_event_to_audit_payload` + `_project_resume_outcome_to_audit_payload`) + canonical invocation pattern routing through `cp_audit_to_od_audit` converter (already operational at `cp_audit_conversion.py:289-299` per Sub-arc A) + per-payload composition discipline (action_id pattern `pause:<workflow_id>:<step_index>` / `resume:<workflow_id>:<step_index>`; audit_cp_response constant selection per ResumeOutcomeKind 4-case switch; path-disjoint field nullification) + invariants + §C-OD-30.4.5 deferred discretion (production callsite construction explicitly DEFERRED — helper lands as DEAD CODE until CP composer authoring arc, which gates H_T-CP-22 PARTIAL → RETIRE-READY per `harness-cp/CLAUDE.md` §4.1). Structural asymmetry from cost-axis Sub-arc B: cost-axis widened EXISTING production callsite (`cost_attribution_llm_dispatch.py:198-202`); pause/resume lands helper WITHOUT a callsite to migrate (`capture_pause_snapshot` + `attempt_resume` are NotImplementedError stubs; workflow_driver.py does not invoke `PauseResumeProtocol`). NO new fail class; NO new span site; NO sampling change; NO CXA amendment (`pause:` + `resume:` discriminators at CXA v2.9 §0.3 from v2.6 composer-arc absorption). Reverses fork doc §9 doc-only deferral. Co-published with OD plan v2.18 (U-OD-51 AC re-decomposition 5→10 ACs absorbing v1.11 contract; AC #9 PARTIAL-LANDED per `[[halt-route-split-AC-pattern]]`) + impl arc helper landings at `pause_resume_namespace.py` + 16 new unit tests (16/16 helper + 27/27 Sub-arc A preserved + 16/16 converter preserved = 59 PASS; pyright strict 0 errors). 2026-05-24. v1.10 prior — Sub-arc B sequel landing per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD plan v2.16 §0(d) operator preference (i) revise-signature: NEW §C-OD-26.6 `CostRecordAuditPayload` typed Pydantic v2 carrier (4 audit_cp_* common fields + 5 cost-specific projected from SpanCostRecord per CXA v2.9 §0.3 row 8) + §C-OD-26.1 canonical invocation signature AMENDED to route through `cp_audit_to_od_audit` converter via typed CostRecordAuditPayload (v1.8 helper-only path preserved as production state at `cost_attribution_llm_dispatch.py:198-202`; migration to typed path deferred per FM-2 + `[[halt-route-split-AC-pattern]]` to follow-on arc requiring signature widening of `attribute_llm_dispatch_cost`). Co-published with OD plan v2.17 (U-OD-41 absorption) + CXA v2.9 forward-cite resolution (`OD spec v1.10 §C-OD-NN` → resolved to `§C-OD-26.6`). 2026-05-24. v1.9 prior — Path γ enum citation absorption: §C-OD-30.1 `pause.reason` attribute type cite `PauseReason per CP spec v1.10 §26.2` → `WorkflowPauseReason per CP spec v1.11 §26.2` (2026-05-21); v1.8 NEW §C-OD-25..33 substantive content preserved verbatim outside the single attribute-type cite cell) |

## §2.4 plan pointer

| Axis | Plan | Unit count |
|---|---|---|
| OD | `Implementation_Plan_Operational_Discipline_v2_36.md` (**v2.36 — canonical HEAD**, 2026-08-16 the `B-137` step-(3) candidate **C1** plan leg — ONE NEW atomic unit **U-OD-61**, the C-OD-09 §9.2 **row 20** (`workflow.envelope`) membership + count-contract reconciliation that OD spec v1.42 §0.2 lands. Mirrors the v2.32 / U-OD-58 treatment of row 19: a spec row landing without a plan unit would leave the implementation with no execution-authority carrier and no acceptance criteria. Six acceptance criteria — byte-exact 20-member set, five-carrier count reconciliation in one commit, literal-arm (not prefix-arm) resolution, end-to-end floor delivery at the real `api.run` venue WITH a pre-v1.42 counterfactual arm, the ratification's owed ordinary-child measurement, and a mutation probe. ZERO existing unit amended; ZERO new contract number; ZERO signature change; ZERO CXA rows. Known bound carried not hidden: the floor holds only while the envelope is the trace ROOT — `B-186`. Prior v2.35, 2026-08-12 — the `B-141` as-value supersession note: v2.2-era U-OD-20 "ESCALATION to terminal-fail-exit" superseded AS VALUE by `semantic_inconsistency` (`ValidatorFailClass` per CP spec v1.116 / B-138 (a); OD spec v1.41); NOTE-ONLY, zero unit-body edits; clearance `implementation-plan-operational-discipline-v2-35-cleared-2026-08-12.md`. Prior v2.34, 2026-08-09 — the `B-123` repair plan leg: **ONE new unit U-OD-60, ZERO amended units** — the §10.2 event-name trigger widening (~4 functional src lines: a DERIVED two-member event-name constant composed from the existing name constants, plus an event-scan arm reached only after the name/attribute checks) with a **minimal** single-read change at `tail_keep_span_processor.py` (`on_end` reads `span.events` ONCE and threads it into both helpers via an optional `events` parameter defaulting to `None`; control flow, arms and ordering UNCHANGED). **TWELVE ACs by execution.** The load-bearing witness is an **INVERSION by prior design** — U-OD-59 wrote W7 specifically to pin this boundary so widening it would be test-visible rather than silent drift, and it inverts in place. AC4 (ambient root, a true sibling) and AC4b (carrier IS the trace root, the term-5 production shape, buffering the dispatcher's OWN `harness.runtime.retry_attempt` child) are both REAL charging-fault dispatches, neither using `force_flush`. AC11 asserts `span.events` is read `== 1` times per `on_end` (deliberately not `<= 2`, which would pass against the code it corrects). SIX PD-8 probes, all MEASURED — **probe (v) CHANGED THE DELIVERABLE**, leaving AC4 green and exposing that the shipped production shape had no real-dispatch witness, so AC4b was ADDED and the probe re-run RED. §0.5 records a brief-supplied premise **independently FALSIFIED** at this leg and the spec leg. Depends on U-OD-11 + U-OD-59; TWO new intra-axis edges; ZERO cross-axis edges; ZERO CXA rows. Clearance `implementation-plan-operational-discipline-v2-34-cleared-2026-08-09.md`. Prior `Implementation_Plan_Operational_Discipline_v2_33.md` — v2.33, 2026-08-08 — the `B-133` realization plan leg: NEW **U-OD-59**, the §9.2.1 event-aware arm (~14 functional src lines) with TEN ACs by execution, SIXTEEN witnesses W1–W16 / 40 cases (split across the axis boundary per §0.10) and SEVEN PD-8 probes, two of which changed the deliverable. Depends on U-OD-11 + U-OD-58; two new intra-axis edges; zero cross-axis edges; zero CXA rows. Clearance `implementation-plan-operational-discipline-v2-33-cleared-2026-08-08.md`. Prior `Implementation_Plan_Operational_Discipline_v2_32.md` (v2.32, 2026-08-08 — the `B-116-t3` plan leg: **ONE new unit, ZERO amended units** — **U-OD-58**, the §9.2 row-19 `fallback.exhausted` roster addition (~1 functional src line + five fixture edit points). EIGHT ACs: byte-exact roster conformance **in both directions**; the member witness **at the discriminating multi-tenant cell** (`base_rate=0.2` — the only cell where §9.2 membership changes the answer); the base-rate disjointness witness verified UNMOVED **by running it unmodified** rather than on the council's prediction; the literal/prefix decomposition at **17 + 2** with the composite-sampler fixture asserted COMPLETE against the literal arm; PD-8 in three arms; and the zero-emission-site / zero-CP / zero-CXA scope claim asserted BY ABSENCE. **U-OD-11 is NOT amended**, per the `B-97`(a)→U-RT-149 / `B-96`→U-RT-150 precedent that a landed unit's ACs are not retroactively re-scoped by a later contract amendment. THREE re-grounding findings recorded rather than absorbed: **§0.5** — the council's five-edit-point pricing is off by one *in kind* (the set-equality **assertion** is byte-unchanged; only its docstring count moves); **§0.6** — TWO shipped `src/` count claims the pricing never covered (`alignment_floor_drift_detection.py` ×2; `substrate_seam_exports_aggregate_manifest.py:203`), with `bridging_arc_table.py:34` checked and correctly untouched (no count); **§0.7** — PD-8 probe (iii) came back **GREEN**, because `_LITERAL_ALWAYS_SAMPLED` is a hand-maintained `parametrize` argument with **no completeness assertion** behind it, a drift hole any future §9.2 amendment could have fallen into — **closed in-unit** with one assertion, after which the probe fails as required. ONE new intra-axis DAG edge (U-OD-11 → U-OD-58); ZERO cross-axis edges; ZERO CXA rows. Clearance `implementation-plan-operational-discipline-v2-32-cleared-2026-08-08.md`) | **58 units (U-OD-00, U-OD-01 – U-OD-58)**; prior `Implementation_Plan_Operational_Discipline_v2_31.md` (v2.31 — 2026-07-31, the `B-69` **impl** leg's OD plan delta, the one piece OD spec v1.36 §30.5.2 deliberately held for that leg: NEW **U-OD-57**, the §C-OD-30.5 pre-bootstrap pause-state audit carrier; carrier option **(b) sibling payload type** selected on byte-compat grounding; clearance `implementation-plan-operational-discipline-v2-31-cleared-2026-07-31.md`). *(This row was **stale at v2.30** — v2.31 landed 2026-07-31 without a pointer refresh; the lineage is reconciled here.)* Prior `Implementation_Plan_Operational_Discipline_v2_30.md` (2026-07-23 B-33 spec+plan leg — NEW U-OD-56, absorbing OD spec v1.35 §24.8 AND retroactively backfilling §24.7's own zero prior plan coverage; clearance `implementation-plan-operational-discipline-v2-30-cleared-2026-07-23.md`; the axis carried 57 units at that revision — U-OD-00, U-OD-01 – U-OD-56). **v2.29 — 2026-07-18 B-51/B-52/B-54 arc plan delta (U-OD-30 amended + NEW U-OD-55 §21.2.2 verification API; clearance `implementation-plan-operational-discipline-v2-29-cleared-2026-07-18.md`); prior v2.28 = the U-OD-21 §15.1 reconciliation.** Prior v2.27 — NEW §4.6 "OD-internal cross-cluster dependency" carve-out per halt-doc Item 12 (OD-INTERNAL-FORMALIZATION) operator AskUserQuestion 2026-05-31 Option (B); formalizes OD-axis-internal cross-cluster composition (within-axis; NOT a cross-axis edge — those stay at §4.5) with C3-15 examples (the 2 deleted U-OD-27 rows at v2.4 §0.4.2: sqlite-substrate residence + ring-buffer eviction). Sibling to PR #110 (CXA v2.18) closing halt-doc Item 11. ZERO new contract; ZERO new atomic unit; ZERO AC change; ZERO DAG/coverage delta; ZERO cross-axis cascade. Documentation-formalization scope; cleared 2026-05-31. v2.26 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-31. v2.26 — Single-unit-body canonical-reading amendment at U-OD-11 (`SamplingMode` + `PER_DEPLOYMENT_SURFACE_SAMPLING` + always-sampled set + tail-keep algorithm-lift) absorbing OD spec v1.26 → v1.27 §1.1 + §1.2 + §1.3 + §1.4 canonical-reading at the §C-OD-09 §9.3 implementer-discretion clause closure (tail-based sampling decision algorithm + tail-keep-on-classification filter implementation per OTel SDK). U-OD-11 covers C-OD-09 §9.1 + §9.2 + §9.3 (per-deployment-surface sampling mode + always-sampled exception set + sampling-discipline invariants surface). v2.26 amends U-OD-11 at the canonical-reading layer absorbing the **tail-keep-on-classification consumer-site lift** at the runtime span-processor materializer: NEW `is_classification_trigger(span)` per-span §10.2 trigger predicate at `harness-od/.../tail_keep_classification.py`; NEW `TailKeepSpanProcessor(SpanProcessor)` wrap-BSP processor at `harness-od/.../tail_keep_span_processor.py`; NEW per-deployment-surface gate at `materialize_span_processor_stage` (wraps BSP iff `config.deployment_surface != LOCAL_DEVELOPMENT`); NEW `SpanProcessorStage.tail_keep_processor: TailKeepSpanProcessor \| None` field type extension. +13 NEW acceptance criteria at U-OD-11 (additive carrier + consumer-site lift; AC #N.1 through #N.13 covering 3 §10.2 trigger predicates + per-trace buffering + always-sampled passthrough + force_flush keep-all + per-deployment-surface gating + end-to-end via InMemoryExporter). ZERO new unit; ZERO new cluster; ZERO new acceptance criterion at the spec layer (§9.3 implementer-discretion clause closure is intra-spec; plan AC #N where N already covers `TAIL_KEEP_RULES` substrate at U-OD-12 is unchanged at v2.26); ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade per OD spec v1.27 §3 (intra-OD-axis only). **Closes H_T-OD-3 PARTIAL → RETIRE-READY gate (a)** §9.1 tail-keep-on-classification at OTLP collector boundary per OD spec v1.27 §1.1 (a)+(b)+(c) closure. Gate (b) §10.3 base_rate envelope CLOSED at PR #25 (v1.26 / v2.25). Both OD-3 gates closed at v2.26 publication → tier transit per X-AL-2. NEW carriers consumed at U-OD-12 conceptually (`TAIL_KEEP_RULES` substrate retained declarative role; `is_classification_trigger` consumer translates declarative rule-strings into concrete OTel carriers per OD spec v1.27 §1.3); ZERO U-OD-12 amendment owed (per v2.26 §2 (a)). Co-published with OD spec v1.27 + retirement batch-36 + workspace `CLAUDE.md` row bumps; 3388/3388 tests pass + 10 skipped (+21 NEW). v2.25 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-28. v2.25 prior — Canonical-reading amendment at U-OD-04 (telemetry primitive — sampler binding) + U-OD-12 (base-rate set + envelope substrate) + U-OD-16 (per-persona-tier redaction gradient substrate) absorbing OD-3 + OD-4 RETIRE-READY persona_tier plumbing arc per `.harness/class_1_fork_od_3_od_4_retire_ready_persona_tier_plumbing.md` Reading (α) operator-ratified 2026-05-28 (Q1=A + Q2=A + Q3=a + Q4=i + Q5=α). Runtime spec v1.37 NEW `RuntimeConfig.persona_tier: PersonaTier = PersonaTier.SOLO_DEVELOPER` field at §3 C-RT-03 + OD spec v1.26 canonical-reading amendments at §C-OD-10 §10.3 + §C-OD-13 §13.1 co-published this arc. Substrate at HEAD already canonical (`PER_CELL_BASE_RATE_ENVELOPE` U-OD-12 + `PER_PERSONA_TIER_REDACTION` U-OD-16 + `CellID` U-OD-01 + `reject_excluded_cell`); v2.25 lifts the consumer site at the runtime materializers (`materialize_tracer_provider_stage` reads §10.3 8-row table via `PER_CELL_BASE_RATE_ENVELOPE[CellID(persona_tier=..., deployment_surface=...)].default_rate`; `materialize_span_processor_stage` threads `config.persona_tier` to `RedactionSpanProcessor`). NEW `MultiTenantOverrideRefusedError` typed exception at `harness-od/src/harness_od/redaction_span_processor.py` enforces §13.1 row 3 non-toggleability at construction. Pre-v2.25 module-level `_DEFAULT_SAMPLER` constant RETIRED at runtime-impl layer. ZERO new unit; ZERO new cluster; ZERO new acceptance criterion; ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade per Q5=(α) — CP spec v1.17 §6.5 `StepEffectiveBinding.persona_tier` (per-step / per-workflow reading) PRESERVED VERBATIM and DISTINCT from per-deployment OD-axis reading; CXA v2.15 + ADR-D5 v1.3 + ADR-D6 v1.2 + ADD v1.3 + PRD v1.1 + IS spec PRESERVED VERBATIM. Refines H_T-OD-3 + H_T-OD-4 PARTIAL (within-PARTIAL refinement; not tier transit per X-AL-2). OD-3 gate (b) §10.3 base_rate envelope closed at v2.25 apply; OD-3 gate (a) §9.1 still deferred. OD-4 gate (a) §13.1 partially closed (deployment-level persona_tier + multi-tenant non-toggleability); per-session toggle + gate (b) §13.2 still deferred. Pipeline-advanced count 6/8 = 75.0% UNCHANGED. 16 NEW tests at workspace `tests/test_persona_tier_plumbing.py` (RuntimeConfig field landing + sampler per-persona base_rate at all 4 enumerated cells + redactor per-persona toggle + multi-tenant non-toggleability typed-error + excluded-cell typed-error); 3367/3367 tests pass + 10 skipped. NEW species candidate `substrate-pre-landed-consumer-deferred-multi-arc-lift` at workflow v1.12 §7.4.7.2 catalogued. v2.24 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-28. v2.24 prior — Bundled fidelity-pure canonical-reading amendment closing 4 drift carries opened at the **`4-OD-B SqliteWritePath` cluster** impl arc landing 2026-05-28 (PR #18 single bundled cluster traversal U-OD-42 + U-OD-43 + U-OD-44 + U-OD-45 commit `406fbf5`). All 4 units **LANDED**; H_T-OD-6 transit **PARTIAL → RETIRE-READY-on-merge** (structural-criterion-B MET via production binding chain `sqlite_span_store.py` schema + INSERT helper + retention helper + reader module + `RuntimeRingBuffer.flush_to_sqlite` composition; full RETIRED gates on operator deployment per X-AL-2). Drift carries closed: (1) U-OD-42 AC #1 column count "12" → "14 per §C-OD-27.1" via `PRAGMA table_info` introspection at 11 NEW tests; (2) U-OD-42 AC #3 pragma enumeration extended to include `PRAGMA foreign_keys=OFF` per spec §27.2 row 2 (advisor 27th application caught at U-OD-42 authoring); (3) U-OD-43 phantom carrier-path `harness-od/.../ring_buffer.py` resolved via **Option (B) axis-split per operator routing 2026-05-28**: schema-pure helper at OD axis (`insert_spans` + `SpanInsertRow` Pydantic carrier) + lifecycle composition at runtime axis (`RuntimeRingBuffer.flush_to_sqlite` + `_project_span_row` placeholder-`SpanRow`-to-14-column-`SpanInsertRow` projection via `asyncio.to_thread`); phantom unit-ID cite `U-RT-30 PARTIAL-LAND` refreshed to real authoring site `U-RT-29 collector_daemon.py + lifecycle/ring_buffer.py LANDED`; (4) U-OD-44 AC #3 retention horizon binding cite refreshed to NEW field `CollectorConfig.sqlite_retention_days: int = 7` at `harness-runtime/src/harness_runtime/types.py:401` with positive-validator gate, threaded through `materialize_ring_buffer_stage` → `RuntimeRingBuffer.__init__(retention_days=)` → `flush_to_sqlite` retention-cleanup-after-INSERT call + 1 NEW config-threading test. Sub-species 7c sibling pattern catalogued: **forward-looking-cite-at-authoring-time-becomes-phantom-at-landing-time** emerges across 3 consecutive arcs in 8 days (OD-6 retirement row + plan U-OD-43 file-path + spec §C-OD-27 §27.2 row 1); sub-species catalogue expansion candidate at next workflow-doc revision pass. Co-published with OD spec v1.24 → v1.25 sibling delta (separate delta-only-spec-file per workspace convention). NEW §1 canonical-reading amendment table (4 unit sites refreshed); v2.14 unit bodies PRESERVED VERBATIM per delta-only-plan-chain convention. ZERO new units; ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade. v2.23 + earlier PRESERVED VERBATIM. 2026-05-28. v2.23 prior — Substantive single-unit-body amendment at U-OD-40 (cost-attribution invocation at `validator.evaluate` + `hitl.webhook.deliver` sites) absorbing the production-binding lifecycle PENDING → LANDED 2026-05-28 per the U-OD-40 bundled atomic-unit arc landing. All 5 ACs from `Implementation_Plan_Operational_Discipline_v2_14.md` §3.4 transit PENDING → LANDED at canonical-reading layer (validator CPU-meter / webhook flat-per-attempt + optional egress / cost-record attached / audit-ledger entry written / 1 validator + 1 webhook → 2 cost-records integration test). ZERO new units; ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade per `.harness/class_1_fork_u_od_40_validator_post_evaluate_hook.md` Reading (B) Q5=β ratification. Co-published with CP spec v1.24 + CP plan v2.27 NEW U-CP-73 + runtime plan v2.29 U-RT-84 +AC #6 + harness-cp impl (13 NEW tests) + harness-runtime impl (2 NEW cost-helper modules + factory binding + composer inline-wrap + 18 NEW unit tests + integration test) + batch-28 retirement event H_T-OD-5 PARTIAL → RETIRE-READY transit (surface coverage 2/4 → 4/4; bucket-membership 1 → 2 with OD-5 NEW alongside AS-8d). v2.22 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-28. v2.22 prior — Fidelity-pure citation-correction patch closing v2.21 §"Adjacent observations" finding (c) `harness_breaker_schema.py:21` docstring drift — as **CLOSED-at-commit-`0709b33`** 2026-05-27. Production doc-hygiene commit `0709b33` updated docstring at `harness-od/src/harness_od/harness_breaker_schema.py` lines 18-29 to correctly cite all 4 `AttributeTier` enum members; v2.21 carry framing became stale at the production commit. SECOND production application of workflow v1.9 §7.4.7.3 across workspace carry-set sweep 2026-05-27 (sweep cohort: 3 of 4 closure deltas; siblings OD spec v1.22 + CXA v2.13 + CP plan v2.24). Species 5 (post-authoring stale carry) at workflow v1.9 §7.4.7.2. ZERO contract / signature / AC / DAG change. v2.21 + earlier PRESERVED VERBATIM. 2026-05-27. v2.21 prior — Substantive canonical-reading amendment at U-OD-04 AC #4 absorbing OD spec v1.18 → v1.19 §C-OD-04 §4.3 §1.1 per-attribute tier redistribution (3 attrs redistribute to Conditionally Required per OTel GenAI semconv 1.41.0 chat-span audit: `gen_ai.request.model` was Required (Stable) → Conditionally Required; `server.port` + `gen_ai.conversation.id` were Recommended (Development) → Conditionally Required). Single-unit absorption at U-OD-04 (authoring site preserved at `Implementation_Plan_Operational_Discipline_v2_5.md` §3.2.1; v2.21 is delta-only change-note). Amendments: Inputs cite append v1.19 §C-OD-04 §4.3 §1.1 + §1.2; Signatures block PRESERVED VERBATIM (the v2.20 AttributeTier 4-member StrEnum already accommodates the redistribution); AC #4 tier-list refresh — Required (Stable) 3 → 2 attrs (`gen_ai.operation.name`, `gen_ai.provider.name`); NEW Conditionally Required row populated with 3 attrs (`gen_ai.request.model`, `server.port`, `gen_ai.conversation.id`) + emission policy "harness-stricter always-emit per OTel emit-more-often-than-CR-is-fine framing" per v1.19 §1.2 + v1.20 Path A closure at `harness-runtime/.../lifecycle/llm_dispatch.py:343-360`; Recommended (Development) 6 → 4 attrs; Opt-In 8 unchanged; attribute NAME SET preserved verbatim at 17 names; Tests-line: NEW `test_conditionally_required_tier_attributes_per_spec_4_3` (per OD spec v1.19 §3 row 110 prescription) + cardinality refreshes at existing Required (Stable) test 3 → 2 + existing Recommended (Development) test 6 → 4; Rollback boundary appendix appended. AC count unchanged at 8 (#4 text amended; #1 + #2 + #3 + #5 + #6 + #7 + #8 PRESERVED VERBATIM). Closes OD spec v1.19 §3 row 110 explicit "REVISION OWED at follow-on arc per FM-2" prescription + v2.20 §"Adjacent observations" (f) per-attribute tier-assignment audit carry. NO new units; NO DAG topology change; NO coverage matrix structural delta; NO cross-axis edge addition; NO dependency edge change (8 direct dependents preserved consuming the same 17-name attribute set with refreshed per-attribute tier metadata). Helper carrier already-redistributed at OD spec v1.19 co-publication arc (`otel_genai_base.py:148+157+158`; 25/25 helper tests pass); production already emits all 3 Conditionally Required attrs at v1.20 Path A close (commit `e874a03`; 1091/1091 harness-runtime tests pass). 6 adjacent findings surfaced — (a) closes v2.20 (f); (b) closes v2.20 (b); (c) carries v2.20 (e) `harness_breaker_schema.py:21` docstring drift; (d) NEW notes v1.19 (g)+(h) CLOSED-via-Path-A at v1.20; (e) NEW notes v1.20 (b)+(c)+(d)+(i) preserved at OD spec layer; (f) NEW AC #5 §4.4 hierarchy claim unaffected by v1.20 §1.1 narrowing. NOT patched per FM-2. 2026-05-27. v2.20 prior — Tension 004 D-2 + D-3 re-litigation Class 1 fork resolution absorption per `.harness/class_1_fork_tension_004_d2_d3_otel_141_relitigation.md` §4.1 (A) full conformance + §4.2 (γ) single-focus operator-ratified 2026-05-26 + OD spec v1.15 → v1.16 §C-OD-04 §4.2 + §4.3 canonical-reading amendment. Single-unit absorption at U-OD-04 (authoring site preserved at `Implementation_Plan_Operational_Discipline_v2_5.md` §3.2.1; v2.20 is delta-only change-note). Amendments at U-OD-04: Inputs cite append v1.16 §C-OD-04 §4.2 + §4.3 + §1.1 + §1.2; Signatures GenAiOperation 6/7 values → 9 values (add INVOKE_WORKFLOW + RETRIEVAL); AttributeTier 3 values → 4 values (add CONDITIONALLY_REQUIRED — reverses v2.5 plan-strike of original v2.1-v2.4 CONDITIONAL tier); AC #2 cardinality 6 → 9 (NEW sub-criterion: includes invoke_workflow + retrieval); AC #3 cardinality 3 → 4 (NEW sub-criterion: includes Conditionally Required); AC #4 BASE_LAYER_ATTRIBUTES tier assignments PRESERVED VERBATIM per v1.16 §1.3 D-3b preservation framing (per-attribute audit deferred); Tests-line: 2 renames (`_cardinality_seven` → `_nine`; `_cardinality_three` → `_four`) + 3 NEW tests (`_includes_invoke_workflow`, `_includes_retrieval`, `_conditionally_required_present`); Rollback boundary v2.5 revert appendix STRUCK in full at v2.20 (the v2.5 strike-of-CONDITIONAL was wrong-direction; the v2.20 4-tier shape matches the original v2.1-v2.4 plan + OTel 1.41.0 archived text). AC count unchanged at 8 (#2 + #3 text amended; #1 + #4 + #5 + #6 + #7 + #8 PRESERVED VERBATIM). Re-litigates Tension 004 D-2 + D-3 ratifications (deferred-not-performed §4 step 3 tiebreaker check now performed via WebFetch this session; the check failed for D-2 + D-3 — MATCH for D-4; SUPERSEDED per the doc's own framing). v2.19 D-1 absorption (2-component span-name format) preserved verbatim. NO new units; NO DAG topology change; NO cluster reorganization; NO coverage matrix structural delta (U-OD-04 still covers C-OD-04 §4.1-§4.5); NO cross-axis edge addition; NO dependency edge change (U-OD-04 `Depends on: []` preserved; 8 direct dependents preserved). 6 adjacent findings surfaced per v2.20 §"Adjacent observations" — (a) updates v2.19 (b) D-2/D-3 carries to RESOLVED-via-re-litigation; (b) carries v2.19 (a) `gen_ai.provider.name` divergence preserved; (c)+(d) NEW carrier-conform verifications; (e) NEW `harness_breaker_schema.py:21` docstring drift; (f) NEW per-attribute tier-assignment audit against 4-tier table. NOT patched per FM-2. Co-published this session: OD spec v1.16 (NEW §1 canonical-reading amendment); helper update at `otel_genai_base.py:58-86` + 25/25 helper tests pass + 27/27 runtime LLM dispatch tests pass; Tension 004 doc §7.6 NEW supersession block. 2026-05-26. v2.19 prior — GenAI span-name format Class 1 fork resolution absorption per `.harness/class_1_fork_genai_span_name_four_way_drift.md` §7.4.1 (R2) operator-ratified 2026-05-26 + OD spec v1.11 → v1.12 §C-OD-04 §4.1 canonical-reading amendment. Single-unit absorption at U-OD-04 (authoring site preserved at `Implementation_Plan_Operational_Discipline_v2_5.md` §3.2.1; v2.19 is delta-only change-note). Amendments: Inputs cite v1.2 → v1.12; Signatures `SPAN_NAME_FORMAT` 3-token → 2-token (`{gen_ai.operation.name} {gen_ai.request.model}` per actual OTel 1.41.0 archived text); AC #1 text 3-component → 2-component (cite to v1.12 §4.1; explicit note that `gen_ai.provider.name` is REMOVED from span-name format but PRESERVED at §4.3 Required (Stable) tier via AC #4); AC #4 Required (Stable) tier list PRESERVED VERBATIM (`gen_ai.provider.name` remains at §4.3 — 3 attributes); Tests-line rename `test_span_name_format_byte_exact_three_component` → `_two_component` (carrier-conform to impl arc `5221264`); Rollback boundary v2.5 revert appendix STRUCK in part (2-component span-name no longer divergent state; D-2/D-3/D-4 still carry as Tension 004 unresolved D2/D3/D4 elements — but D-2/D-3/D-4 plan-layer resolutions remain operative per v2.5 plan-conforms-to-spec). AC count unchanged at 8 (#1 + #4-line preserved-vs-amended split documented; #2/#3/#5/#6/#7/#8 PRESERVED VERBATIM). Re-litigates `Phase_7_Class_1_Tension_004_OD04_Span_Schema_Divergence.md` D-1 ratification (deferred-not-performed §4 tiebreaker check now performed; check failed; D-1 ratification SUPERSEDED). NO new units; NO DAG topology change; NO cluster reorganization; NO coverage matrix structural delta (U-OD-04 still covers C-OD-04 §4.1-§4.5); NO cross-axis edge addition; NO dependency edge change (U-OD-04 `Depends on: []` preserved; 8 direct dependents preserved). 4 adjacent findings surfaced per v2.19 §"Adjacent observations" — NOT patched per FM-2. Co-published this session: OD spec v1.12 (`43f6199`); R2 follow-on helper + test + R4 STRIKE + workspace CLAUDE.md (`5221264`). 2026-05-26. v2.18 prior — Pause/resume back-flow arc (narrow scope per operator AskUserQuestion 2026-05-24) U-OD-51 AC re-decomposition per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §9 + OD spec v1.10 → v1.11 NEW §C-OD-30.4 production-invocation contract. Single-unit-body amendment at U-OD-51 (5 → 10 ACs; Implements + Files + Signatures revised to absorb v1.11 production-helper contract). ACs #1-#5 preserved verbatim from Sub-arc A v2.14 authoring; NEW ACs #6-#10: pause helper landing + resume helper landing + path-disjoint nullification + production callsite construction DEFERRED per `[[halt-route-split-AC-pattern]]` (AC #9 PARTIAL-LANDED — gates H_T-CP-22 PARTIAL → RETIRE-READY at CP composer arc) + unit tests covering helper construction shape. Co-published this session: OD spec v1.11 + impl arc (pause_resume_namespace.py EXTEND with `_project_pause_event_to_audit_payload` + `_project_resume_outcome_to_audit_payload` module-level helpers; __all__ export + test-fixture carve-out per §C-OD-30.4.1 step 1) + 16 new unit tests verifying ACs #6-#10 + pyright strict clean. NO new units; NO DAG topology change; NO cluster reorganization; NO cross-axis edge addition. 2026-05-24. v2.17 prior — Sub-arc B sequel U-OD-41 absorption per `[[fork-u-cp-72-cost-and-pause-resume-prefix-gap]]` §6 + OD spec v1.10 NEW §C-OD-26.6 CostRecordAuditPayload + amended §C-OD-26.1 + CXA v2.9 §2.3.7 row 8. Single-unit-body amendment at U-OD-41 (5 → 9 ACs; Implements + Files + Signatures revised to absorb v1.10 typed-carrier contract; cost_namespace.py NEW + cost_record_audit_writer.py NEW + cp_audit_conversion.py EXTEND with `cost:` branch restoration + COST_AUDIT_NAMESPACE_PREFIX). AC #8 PARTIAL-LANDED per `[[halt-route-split-AC-pattern]]` (production callsite migration at `cost_attribution_llm_dispatch.py:198-202` deferred per FM-2 to follow-on arc widening `attribute_llm_dispatch_cost` signature). Co-published this session: OD spec v1.10 + impl arc (cost_namespace.py + cost_record_audit_writer.py + cp_audit_conversion.py extension + 4 new cost: branch tests). 2026-05-24. v2.16 prior — Cross-axis-block lift at U-OD-51 (U-CP-62 landed `49617e7`); v2.15 — Path γ citation absorption; v2.14 + earlier preserved verbatim |

## §1.2 axis CLAUDE.md archive (relocated 2026-08-10)

*Byte-preserved `harness-od/CLAUDE.md` §1.2 "Spec + plan authority" table Version-column cells, relocated here by the U-CTX-20 axis-file slimming pass (R-CTX-1 Arc 6). Root table row structure (Artifact / Role columns) is unchanged in place; only the per-version lineage narrative moved. Verbatim — no paraphrase, no trimming.*

### `Spec_Operational_Discipline_v1_42.md` (spec) — original §1.2 Version cell

**v1.42 — canonical HEAD** — 2026-08-16, the `B-137` step-(3) candidate **C1** leg, operator-ratified 2026-08-16 — C-OD-09 §9.2 always-sampled exception set gains ONE row, **19 → 20**: `workflow.envelope`. An INGESTION REPAIR, not a new declaration — C-OD-25 §25.1 (`Spec_Operational_Discipline_v1_8.md:90`) has declared *“the envelope ALWAYS persists”* at head=1.0 since v1.8, preserved verbatim through the whole v1.9 → v1.41 chain, and §9.2 simply never absorbed it (same shape v1.37 repaired for `fallback.exhausted`, differing only in that the declaring contract is intra-axis). Root membership is additionally the ONLY mechanism that delivers the §9.2 floor to in-envelope member SPANS, because the shipped `ParentBased(root=HarnessCompositeSampler)` consults the inner sampler only for roots. §9.1 / §9.3 / the §9 header block and all nineteen prior rows PRESERVED VERBATIM; the SDK-boundary literal/prefix decomposition moves 17+2 → 18+2. The ratification's carried-forward *“data loss, not delay”* cost was RE-MEASURED at this filing and does NOT hold — `B-136`'s name-arm repair (PR #1331) makes an always-sampled root run the root-close decision before forwarding itself, so C1 costs 0 buffered / 0 evicted sequentially even at a buffer cap of 3, and the arc's follow-on claim that no configuration loses a span the status quo would have kept was WITHDRAWN at out-of-family review round 2 -- it compared cardinality, not identity: above `max_buffered_traces` C1's drop-oldest FIFO DISPLACES traces the base-rate draw would have preserved, while at the shipped 4096 default the displacement set is empty and C1 strictly dominates. Row 20 is written ROOT-CONDITIONAL in the contract (§0.2.1 + §0.2.3), joining `subagent.span (root)`, so the spec does not assert a floor the inheritance mechanism cannot deliver beneath a foreign unsampled parent (`B-186`); §9.3 and rows 1-19 are UNTOUCHED and this is NOT candidate C. The residual that IS real — concurrent in-flight buffer occupancy × `1/base_rate` — is split to its own register row `B-185`. Extends `B-160`'s head=1.0 divergence class 4 → 5 (its close-out scoped the sweep to C-OD-3x, so it was structurally unable to see the C-OD-25 instance). ZERO new contract numbers; ZERO new namespace (C-OD-05 §5.1 roster unchanged at 15); ZERO new attribute; ZERO emission-site change; ZERO CP delta; ZERO Runtime delta; ZERO CXA rows (aggregate frozen at 111); ZERO hash impact. Back-flow record `b137-c1-admit-the-envelope-root-2026-08-16.md`; clearance `spec-operational-discipline-v1-42-cleared-2026-08-16.md`.

### `Spec_Operational_Discipline_v1_41.md` (spec) — original §1.2 Version cell

**v1.41 — canonical HEAD** — 2026-08-12, the `B-141` cascade OD leg — C-OD-14 §14.5.3's escalation-on-mismatch table's `validator.fail.class` row `terminal-fail-exit` → `semantic_inconsistency` (`ValidatorFailClass` domain member per CP spec v1.116 / `B-138` operator-ratified disposition (a)); the invariance check, escalation prose, catalog-extension paragraph and the table's other three rows incl. `permanence = permanent` + its C-OD-09 §9.2 always-sampled arm PRESERVED VERBATIM; the retry-exit ROUTING classification demoted from the wire name, not deleted (ADR-D5 v1.6 §1.10); same-PR code cascade `ReplaySemanticDivergenceError` Literal-pinned fixed attributes + witness; sibling legs ADR-D6 v1.3 + CP plan v2.52 + OD plan v2.35; ZERO production emission sites at HEAD (declared-shape-only); ZERO roster/namespace/attribute-mint/sampling/Runtime/CXA/hash impact; back-flow record `b-141-validator-fail-class-cascade-2026-08-12.md`; clearance `spec-operational-discipline-v1-41-cleared-2026-08-12.md`. Prior-head cell below preserved verbatim.

### `Spec_Operational_Discipline_v1_40.md` (spec) — original §1.2 Version cell

**v1.40 — canonical HEAD** — 2026-08-11, the `B-144` OD leg — C-OD-05 §5.1 row 9 (`engine.*`) `Attribute count` 3→4, carrying the C-CP-09 §9.1 v1.3 supersession (+`engine.replay_disposition`, ADR-D1 v1.2 §1.1.1) the row's own `Ingest verbatim` posture + citation already point at (surfaced by out-of-family rounds 4-5 at PR #1311; the as-built `namespace_map.py` engine row tracked the stale 3 while citing §9.1 itself); all other §5.1 rows incl. row 6's hitl.* 11-across-4-span-names cell (`B-153` scope) PRESERVED VERBATIM; same-PR code cascade map-row+test; ZERO roster/namespace/attribute-mint/Runtime/CXA/hash impact; fork doc `class_1_fork_b144_cp_24_1b_stale_retable.md`; clearance `spec-operational-discipline-v1-40-cleared-2026-08-11.md`. Prior-head cell below preserved verbatim.

### `Spec_Operational_Discipline_v1_39.md` (spec) — original §1.2 Version cell

**v1.39 — canonical HEAD** — 2026-08-09, the **`B-123` §10.2 conformance leg**: **ONE amendment site**, **§C-OD-09 §9.2.1 term 3 REPLACED IN FULL** — its explicit decline to widen the §10.2 classification-trigger predicate to span events is RETRACTED, and the predicate MUST now resolve `sandbox.violation` / `breaker.tripped` against the span **name** and, when the name does not match, against the names of the span's **events**. **This is a CONFORMANCE REPAIR, not a spec direction** — C-OD-10 §10.2's two event-shaped rows have read *"any `sandbox.violation` **event**"* / *"any `breaker.tripped` **event**"* since the contract was written (`Spec_Operational_Discipline_v1_2.md:583-584`, read verbatim) while the realization matched by span NAME, so the code was non-conformant to cleared contract text and there is no fork to route. **§10.2 is therefore NOT amended and its non-amendment is the point.** The term additionally makes NORMATIVE that the two-member §10.2 trigger set and the nineteen-member §9.2 roster are DISJOINT and MUST NOT be collapsed (`fallback.exhausted` is the live discriminator — a §9.2 member that is not a §10.2 trigger), that both name sets derive from a SINGLE declaration of each trigger name so span arm and event arm cannot drift, and that the predicate stays TOTAL and side-effect-free. **v1.38's mirror is unchanged and was VACUOUS at that revision** — it conditioned the keep flag on the span being a trigger while the predicate matched by name and the carrier is named `harness.runtime.retry_breaker_fallback`; this delta supplies the live case. **Scope narrowed by an exhaustive sweep, not assumed:** `sandbox.violation` is emitted as a REAL SPAN (`runtime_tool_dispatcher.py:725`) and an exhaustive `add_event(` scan over all seven `harness-*/src` trees returns SIX sites, none so named — the sibling arm is already conformant, and the amendment covers both names on contract-conformance grounds anyway. **§10.2 row 1 is outside this family** (an attribute carrier; its inertness stays `B-124`'s). **ADMISSION-BOUNDED, stated not glossed:** §9.2.1 term 4 is inherited UNAMENDED, so per `B-137` the sibling tree is preserved only for head-ADMITTED carriers (~10% at base-rate 0.1, ~20% at 0.2) — **no full-floor claim is made**. **A Class-3 rider the register queued for this leg was RE-GROUNDED and FALSIFIED** (ADR-D2 §1.8's table DOES carry `permanent-fail` in its C5-fail-class column at `ADR-D2.md:305/306/309`, so the §10.2 row-1 cite RESOLVES); the rider is DROPPED and the register corrected, recorded at §0.6. **ZERO contract numbers, ZERO §10.2 edit, ZERO roster change (§9.2 stays 19), ZERO namespace delta (C-OD-05 §5.1 roster 15), ZERO emission-site change, ZERO head-sampler edit, ZERO CP/Runtime delta, ZERO CXA rows (111), ZERO hash impact**; terms 1/2/4/5 + §9.1 + §9.3 PRESERVED VERBATIM. Plan leg v2.34 U-OD-60. **`B-123` CLOSES at the impl leg.** Clearance `spec-operational-discipline-v1-39-cleared-2026-08-09.md`. Prior `Spec_Operational_Discipline_v1_38.md` — v1.38 — 2026-08-08, the **`B-133` realization leg**: NEW **§C-OD-09 §9.2.1** *"Realization of the §9.2 floor at the SDK boundary"* (FIVE normative terms), discharging v1.37's honest-scope residual at the **TAIL** consumer and NARROWING it to a **declared bound** at the **HEAD** consumer. **The positive control ran FIRST and its RESULT — not an expectation — grounds every term**: a REAL exhausted dispatch through the REAL `HarnessCompositeSampler` + the REAL `TailKeepSpanProcessor`, seven configurations, all three event-shaped members (`fallback.triggered` / `breaker.tripped` / `fallback.exhausted`) — **ZERO spans exported at BOTH consumers for ALL THREE**; the discriminator arm shows a span NAMED `fallback.exhausted` surviving `base_rate=0.0` while a span CARRYING it as an event is dropped. Terms: (1) the tail resolves span **name** AND **event** names through the SAME `is_always_sampled` SSOT with event attributes passed (conservative-absent preserved); (2) a match forwards the CARRIER span immediately, the coarseness stated not glossed (OTel has no sub-span export unit); (3) the §10.2 keep flag is MIRRORED but the trigger predicate is **NOT** widened to events — that half is **`B-123`**, deliberately left to it; (4) the HEAD consumer is a **DECLARED BOUND**, non-vacuous because `team-binding × local-development` is `HEAD_BASED_DEV` at §10.3 default **0.5** with no tail consumer (this **falsifies** `B-133` close-out step (2)'s "samples at 1.0 anyway" hypothesis, corrected rather than inherited); (5) an event-matching ROOT close must still materialize its trace decision. **ZERO new contract numbers, ZERO roster change (§9.2 stays at 19), ZERO namespace delta (roster 15), ZERO emission-site change, ZERO head-sampler edit, ZERO CP/Runtime delta, ZERO CXA rows (111), ZERO hash impact**; §9.1/§9.2's nineteen rows/§9.3 PRESERVED VERBATIM. Impl at plan v2.33 U-OD-59. **`B-133` CLOSES** (steps 1+2 executed; step 3, the F-08 keep-volume measurement, carried as a named **OPEN** residual per the `B-104` precedent); **`B-136` + `B-137` newly registered** — `B-136` the pre-existing name-arm root-close buffer leak; **`B-137`** the head-starves-the-tail architecture finding (out-of-family Codex round 1 against this arc's commit `ff63c725`, **adjudicated by measurement and CONFIRMED**: ~10% / ~20% event-carrier admission at the two production tail cells, term 4's coverage claim RETRACTED, the arm real but PARTIAL; starvation **non-uniform** — root-name-shaped members admitted at 100%; **YES** council, C7 ⊥ C11; **merges** with F-08). Clearance `spec-operational-discipline-v1-38-cleared-2026-08-08.md`. Prior `Spec_Operational_Discipline_v1_37.md` — v1.37 — 2026-08-08, the **`B-116-t3` OD leg** (PR #1272; `Spec_Harness_Runtime_v1.md` v1.112 §14.6.3 t3′'s named closure gate, priced at the council record `C-c7-reconcile.md`): **C-OD-09 §9.2 always-sampled table 18 → 19**, appending `fallback.exhausted` (source declaration `C-CP-03 §3.5` at `Spec_Control_Plane_v1_2.md:410`) on the ingestion-chain direction — CP declares, OD ingests; the row's two CP-table siblings were absorbed at original ingestion and this one was dropped. Change-note carries the rider-(a) structural-compatibility result (STRUCTURAL check only — the population question stays OPEN, carried at register row `B-133` close-out step 3), the §9.3 "inviolable"-governs-tunability-not-revision ruling, and an **honest-scope paragraph**: both sampling consumers classify span NAMES while three §9.2 members (`fallback.triggered` / `breaker.tripped` / `fallback.exhausted`) are span EVENTS, so their floor realization is bounded pending **`B-133`** (registered at the leg — surfaced by out-of-family codex; NOT silently absorbed). §9.2's other 18 rows + §9.1/§9.3 PRESERVED VERBATIM; impl at plan v2.32 U-OD-58. **`B-116` CLOSED at this leg** (both legs merged: #1271 impl + #1272 t3). Clearance `spec-operational-discipline-v1-37-cleared-2026-08-08.md`. Prior `Spec_Operational_Discipline_v1_36.md` — v1.36 — 2026-07-30, the OD-owned leg of the RATIFIED **`B-69` durable-pause-state read accessor arc** (council record `.harness/council-b69-pause-state-accessor-2026-07-30.md`; **operator ratified OPTION A′ 2026-07-30**): NEW **§C-OD-30.5** — the observability home for the two events the council ruled REQUIRED, (i) the Runtime §14.14.9 accessor read and (ii) a `resume()` refused on the §30 staleness precondition. **Determination (the council delegated the namespace call to OD, and grounding returned it SPLIT):** both events emit within the **EXISTING** C-OD-30 `pause.*` / `resume.*` family — **NO new top-level namespace, C-OD-05 §5.1 roster UNCHANGED** — but **neither is representable by the existing payload composition unchanged.** The read is a **third event class** (neither a `PauseEvent` nor a `(ResumeAttempt, ResumeOutcome)` pair). The refusal is raised **PRE-BOOTSTRAP**, before any `(ResumeAttempt, ResumeOutcome)` exists, so it composes through neither existing §C-OD-30.4 helper nor the CP→OD converter's `resume:` branch — and `PauseResumeAuditPayload` is `frozen` + `extra="forbid"` with no staleness-token field. **§30.5.2 therefore AUTHORIZES an additive carrier** (an additive field, or a sibling payload type — impl discretion), with the existing field set and BOTH existing helpers **otherwise PRESERVED VERBATIM**. *(An earlier draft claimed "no new payload type and no new converter branch are owed"; out-of-family review round 2 [P1] found both halves false, corrected in place.)* **Load-bearing requirement:** the SAME staleness token is emitted at **BOTH** the successful read and the refusing resume, so a stale-read refusal is reconstructable as **ONE causal pair from telemetry alone**. Content is **split by outcome** — successful read carries the token + **PER-VARIANT location counts** (four counts, one per §14.14.9.2 classification; a single total cannot express classification, round 3 [P2]); failed read carries `workflow_id` + the cause attribution and **no token, no count**, but MUST still emit. **NEVER on either:** the locations' payload, or the never-keyable pre-dispatch / depth-0-root identity. **PLAN: no OD plan delta at this leg, and an OD plan delta is OWED AT THE IMPL LEG under EITHER carrier option** (both mutate or extend an OD-owned schema; a Runtime unit cannot own that — round 3 [P1]). Clearance at `.harness/clearance/spec-operational-discipline-v1-36-cleared-2026-07-30.md`. Prior `Spec_Operational_Discipline_v1_35.md` — v1.35 — 2026-07-23, the RATIFIED `B-33` rotation-correlation carrier arc's spec leg (Option A, ratified 2026-07-21): NEW §24.8 (C-OD-24) — a per-correlation-id rotation-pair evidence accessor (`find_rotation_pair_evidence`), sibling to the UNCHANGED §24.7 whole-ledger `verify_rotation_pairs` walk, reusing its crypto/structural checks via an extracted shared helper. Absence of a matching pair (EXACTLY 0 entries — a lone matching entry is NOT absence, it raises `RotationPairIntegrityBreach` as a torn-write/deleted-sibling signal) is explicitly distinguished from `RotationPairIntegrityBreach` (3+ entries, exactly 1 entry, or 2 entries failing the checks) — absence is reported as evidence, never raised as tamper; the evidence DTO also carries `signatures_verified: bool`, always `False` in this delta (no rotation-period-aware cryptographic verifier exists yet) — structural evidence is necessary but not sufficient. CP-owned (NEW §20.3.2 Protocol) + Runtime-owned (NEW §13.6 composition-root inputs) contract text at the same-arc `Spec_Control_Plane_v1_105.md` / Runtime spec v1.105 — cross-referenced, not restated. Clearance at `.harness/clearance/spec-operational-discipline-v1-35-cleared-2026-07-23.md`. Prior `Spec_Operational_Discipline_v1_34.md` — v1.34 — 2026-07-18, the RATIFIED `B-51`/`B-52`/`B-54` OD audit-signing amendment arc (all ten fork gate items ratified AS RECOMMENDED; three all-CONFIRM council dyads at the apply leg): AMENDED §21.2.1 — FIFTH canonical-message segment carrying the writer-normalized tenant tag (OD-homed normalizer, drop-when-`None` byte-compat), tenant-bearing `sign_audit_entry`, MTC tenant bootstrap invariant with the `migrate-audit-sidecar` retag upgrade story, `sign_rotation_pair` PROHIBITED at MTC until `B-33`; NEW §21.2.2 — backend-aware signature verification API (per-row resolver, message-format cutover, authenticated content-bound exemption triples, typed taxonomy `AuditSignatureInvalid` ≠ `HashChainBreach`, non-blocking default, UNVERIFIED-nonzero at MTC); NEW §21.2.3 — `audit_signing_fail_closed` policy (per-persona defaults, explicit-`false`-INVALID-at-MTC, single typed boundary, zeroth-site backend-required invariant, redaction path unconditionally fail-closed with mandatory typed propagation, post-effect result-preserving bypass). Clearance at `.harness/clearance/spec-operational-discipline-v1-34-cleared-2026-07-18.md`. Prior delta v1.33 — 2026-07-16, post-Phase-8 forward-register arc `B-47` (PR A): NEW C-OD-21 §21.2.1 `SigningBackend` composition-root injection seam — OPTIONAL `backend` keyword on the U-OD-30 `sign_audit_entry` (mirror of the cleared C-CP-20 §20.2.1/`B-22` seam; absent → placeholder attrs PRESERVED VERBATIM, zero caller regression; present → real signature over a metadata-bound canonical message, base64 value, per-algorithm length enforcement per C-CP-20 §20.4 widths); clearance at `.harness/clearance/spec-operational-discipline-v1-33-cleared-2026-07-16.md`. Prior delta v1.32 — 2026-07-13, R-FS-2 Wave 4 `B-19-BREAKER-AMBIENT-ATTRS`: C-OD-07 §7.1 ADDITIVE amendment, re-introduces `harness.breaker.cause` + `harness.breaker.cooldown_ms` (CP v1.1's dropped ambient 4-attribute set, re-landed as event attributes; 7→9 attributes). `cause` was a vacuous-by-honest-design slot at v1.32's authoring; the follow-on classifier landed at `B-38` (PR #1020) and it now populates `rate_limit`/`auth_failure`/`5xx_streak` at all three real `record_failure()` sites (`capability_shortfall` remains a forward slot) — v1.32 §7.1 corrected in-place 2026-07-18 per `B-43`; `cooldown_ms` is real and always populated at a trip. Prior delta v1.31 (2026-07-12 pointer catch-up, R-600 cadence-5 adjacent finding): ports `audit.rotation_correlation_id` into the existing `audit_namespace_attrs` open-dict convention (new §24.7, mirroring the §24.6 `audit.cp.*` precedent) for the two-row dual-signature audit-key rotation runtime path (B-AUDIT-KEY-ROTATION-RUNTIME, PR #938); zero Pydantic schema change. v1.30 chain below preserved verbatim as historical (delta-only chain; this row's prior `v1.4` was the Phase-6.5 authoring-era pin, pre-dating the Phase-7 v1.5→v1.30 deltas — latest delta = R-FS-1 `B-COST-DISCRIMINATOR-TAXONOMY` C-OD-15 §15.1 ADDITIVE `PER_DISPATCH_KIND` rollup axis + `DispatchKind` vocabulary + `provider_discriminator` per-dispatch-optional correction; prior delta v1.29 = R-PM-1 cascade PR #4 NEW C-OD-34 per-persona-tier prompt-governance posture. Per delta-only convention each version is canonical-at-authoring for its scope; the chain head is the current contract authority)

### `Implementation_Plan_Operational_Discipline_v2_36.md` (plan) — original §1.2 Version cell

**v2.36 — canonical HEAD** — 2026-08-16, the `B-137` step-(3) candidate **C1** plan leg — ONE NEW atomic unit **U-OD-61**, carrying the C-OD-09 §9.2 **row 20** (`workflow.envelope`) membership + count-contract reconciliation that OD spec v1.42 §0.2 lands. Applies the v2.32 / U-OD-58 treatment of row 19 to row 20, so the shipped implementation has an execution-authority carrier with acceptance criteria rather than a spec row with no plan side. Six acceptance criteria: byte-exact twenty-member set (both directions); every live count claim moved in the same commit across five carriers, with point-in-time carrier landscapes explicitly NOT amended per the v1.37 precedent; row 20 resolving through the LITERAL arm and not the prefix arm (both directions asserted); the floor reaching in-envelope member spans end-to-end at the real `api.run` venue, with a pre-v1.42 counterfactual arm required so the as-built assertion cannot be vacuous; the ordinary-child measurement the `B-137` ratification owed, with any residual split to its own register row; and a mutation probe. ZERO existing unit amended; ZERO new contract number; ZERO signature change to any landed unit; ZERO CXA rows. Does NOT carry the C11 volume pricing (`B-182` / `B-183`), candidate A (tail half unbuilt), or a repair for `B-186` (the floor holds only while the envelope is the trace ROOT — an unsampled ambient OTel parent defeats it; stated as a bound at v1.42 §0.3.1 and registered, not absorbed).

### `Implementation_Plan_Operational_Discipline_v2_35.md` (plan) — original §1.2 Version cell

**v2.35 — canonical HEAD** (2026-08-12, the `B-141` as-value supersession note: the v2.2-era U-OD-20 criteria pinning the §14.5.3 invariance-check ESCALATION to `terminal-fail-exit` (`Implementation_Plan_Operational_Discipline_v2_2.md:55/:66/:167/:368`, standing authority at v2.34 — no later delta re-states them) superseded AS VALUE by `semantic_inconsistency` (`ValidatorFailClass` domain per CP spec v1.116 / B-138 (a); OD spec v1.41) as ONE NOTE — NOTE-ONLY, **ZERO new units, ZERO amended unit bodies, ZERO contract IDs, ZERO roster/namespace/DAG/CXA change**; the escalation mechanism, `cause_attribution`, `permanence`, and the always-sampled arm (keying on `permanence`, unaffected) unchanged; landed unit stands as HISTORY per the B-97(a)/B-118 precedent; as-built witness `test_replay_semantic_divergence_event_attributes` asserts `semantic_inconsistency` + the Literal-pin override rejection. Clearance `implementation-plan-operational-discipline-v2-35-cleared-2026-08-12.md`. Prior-head cell below preserved verbatim.)

### `Implementation_Plan_Operational_Discipline_v2_34.md` (plan) — original §1.2 Version cell

**v2.34 — canonical HEAD** (2026-08-09, the `B-123` repair leg's OD plan delta: **ONE new unit, ZERO amended units** — **U-OD-60**, the §10.2 event-name trigger widening (~4 functional src lines: a DERIVED two-member event-name constant composed from the existing `SANDBOX_VIOLATION_SPAN_NAME` / `BREAKER_TRIPPED_SPAN_NAME` rather than re-literalled, plus an event-scan arm appended AFTER the existing name/attribute checks so a name-matching span never pays for it), plus a **minimal** single-read change at `tail_keep_span_processor.py` — `on_end` reads `span.events` ONCE and threads that snapshot into both helpers (each gaining an optional `events` parameter defaulting to `None`, so every existing caller stays byte-identical); control flow, arms and ordering UNCHANGED. Its THREE call sites (recounted programmatically after the draft claimed two; the all-OD-internal disposition is unaffected) receive a wider answer. **That bullet was filed COMMENT-ONLY and is CORRECTED**: out-of-family Codex round 1 raised a [P2] showing the widened predicate re-read `span.events` a second time per ordinary buffered span — a regression this arc introduced, since the predicate previously never touched `.events`; adjudicated CORRECT and taken under v1.39 §1.3's once-only-read discretion, with **NEW AC #11** witnessing it structurally via a counting double asserting `== 1` (deliberately not `<= 2`, which would pass against the code being fixed) and a paired `== 0` re-confirmation of AC7. **TWELVE ACs by execution.** The load-bearing witness is an **INVERSION, by prior design**: U-OD-59 authored `test_w7_event_carried_breaker_tripped_does_not_set_the_keep_flag` precisely to PIN the pre-repair boundary so widening it later would be test-visible rather than silent drift — AC #3 inverts it IN PLACE (rename + docstring + module witness-table row + section comment moving together, assertion flipped to an ORDER-asserting equality per U-OD-59 §0.7, no stale duplicate). AC #4 is a REAL charging-fault dispatch driving a REAL breaker trip with a buffered **sibling** span — not merely the trip's own carrier — proven to export **WITHOUT `force_flush`**, with the trace arrangement asserted (`carrier.parent is not None`) so it cannot pass by accident. AC #5 keeps the rosters apart (`fallback.exhausted` forwards its carrier but must NOT flag its trace, killing the collapse-into-`ALWAYS_SAMPLED_EVENT_CLASSES` mutation); AC #7 proves the cheapest-first short-circuit **structurally** via a span double whose `.events` raises if touched, never by timing; AC #9 states the `B-137` head-admission bound rather than claiming a floor. SIX PD-8 probes recorded as mutation→required-RED correspondence with an explicit preamble on why no figures are asserted at authoring time (the impl landed concurrently in the same PR — predictions are NOT presented as results); probe (iii) predicted GREEN by analogy with U-OD-59 §0.8. §0.5 records a brief-supplied premise **independently FALSIFIED** at this leg and at the spec leg by separate direct reads (the ADR-D2 §1.8 rider). Depends on U-OD-11 + U-OD-59; **TWO** new intra-axis edges; **ZERO** cross-axis edges and **ZERO** CXA rows, determined by grep rather than assumed. Clearance `implementation-plan-operational-discipline-v2-34-cleared-2026-08-09.md`. Prior `Implementation_Plan_Operational_Discipline_v2_33.md` — v2.33 (2026-08-08, the `B-133` realization leg's OD plan delta: **NEW U-OD-59** — the §9.2.1 event-aware arm at `TailKeepSpanProcessor.on_end` (~14 functional src lines: an `_carries_always_sampled_event` helper + the arm) with **TEN ACs by execution** and **SIXTEEN witnesses W1–W16** (40 cases) — split across the axis boundary per plan §0.10 (the five real-dispatch witnesses are runtime-homed at `test_b133_event_aware_tail_floor_real_dispatch.py`; the nine processor-contract witnesses stay OD-local and `harness_runtime`-FREE, which the `axis-isolation — harness-od` CI leg requires) — (the counterfactual; all three members surviving via REAL dispatch shapes **without `force_flush`**; the HEAD bound asserted and NAMED as a bound; the trigger-flag mirror; the `B-123` boundary pinned; conservative-absent three ways; buffer/eviction bookkeeping unchanged; SSOT completeness parametrized over `ALWAYS_SAMPLED_EVENT_CLASSES` itself, closing the parametrize-literal drift gap **by construction**) + **SEVEN PD-8 probes**, three of which changed the deliverable — probe (ii) caught a witness PASSING under the mutation it existed to catch (membership where the discriminator is export ORDER; sharpened) and probe (iii) returned GREEN as the **correct** result (ordering is a cost property, so no assertion was written). §0.5 records a FALSIFIED close-out hypothesis; §0.6 records the pre-existing name-arm buffer leak registered as **`B-136`**. Depends on U-OD-11 + U-OD-58; **two** new intra-axis edges, **zero** cross-axis edges, **zero** CXA rows (determined by import + constructor-signature inspection, not assumed). Clearance `implementation-plan-operational-discipline-v2-33-cleared-2026-08-08.md`). Prior `Implementation_Plan_Operational_Discipline_v2_32.md` — v2.32 (2026-08-08, the `B-116-t3` leg's OD plan delta, PR #1272: **NEW U-OD-58** — the §9.2 row-19 roster change in `sampling_mode.py` (one src line) + five fixture edit points + a completeness witness closing the parametrize-literal drift gap PD-8 probe (iii) exposed (§0.7) + the base-rate disjointness witness asserted UNMOVED by execution; 8 ACs incl. the AC #3 sampling-DECISION-not-backend-survival scope pin (end-to-end realization explicitly NOT owned — `B-133`); depends on U-OD-11, one intra-axis edge, zero cross-axis edges. Clearance `implementation-plan-operational-discipline-v2-32-cleared-2026-08-08.md`). Prior `Implementation_Plan_Operational_Discipline_v2_31.md` — v2.31 (2026-07-31, the `B-69` **impl** leg's OD plan delta — the one piece OD spec v1.36 §30.5.2 deliberately held for the impl leg (*"whichever option the impl leg selects, an OD plan delta is OWED AT THAT LEG"*). **NEW U-OD-57** — the §C-OD-30.5 pre-bootstrap pause-state audit carrier. **Carrier option (b) — a SIBLING payload type — selected**, on byte-compat grounding: option (a) would put a never-populated field on every already-shipped `PauseResumeAuditPayload` row AND make the union of two DISJOINT field sets optional on both, the illegal-state-representable shape the closed-schema posture exists to prevent. `PauseResumeAuditPayload` + both §C-OD-30.4 helpers + the CP→OD converter branch are **PRESERVED VERBATIM**. This unit is the PENDING, not-yet-numbered OD dependency `Implementation_Plan_Harness_Runtime_v2_55.md` records for U-RT-148 — numbered here and co-landed. Clearance `implementation-plan-operational-discipline-v2-31-cleared-2026-07-31.md`). Prior `Implementation_Plan_Operational_Discipline_v2_30.md` — v2.30 (2026-07-23 B-33 spec+plan leg — NEW U-OD-56, absorbing OD spec v1.35 §24.8 AND retroactively backfilling the already-landed §24.7 `sign_rotation_pair`/`verify_rotation_pairs` (PR #938), which had zero prior `U-OD-NN` coverage; clearance `implementation-plan-operational-discipline-v2-30-cleared-2026-07-23.md`). Prior `Implementation_Plan_Operational_Discipline_v2_29.md` — v2.29 — (delta-only chain; 2026-07-18 B-51/B-52/B-54 arc plan delta — U-OD-30 amended for OD v1.34 §21.2.1/§21.2.3 + NEW U-OD-55 for the §21.2.2 verification API; clearance at `.harness/clearance/implementation-plan-operational-discipline-v2-29-cleared-2026-07-18.md`; prior delta v2.28 = the U-OD-21 §15.1 reconciliation)
