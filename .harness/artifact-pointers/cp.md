# CP — artifact pointer lineage

*Split byte-preservingly from `.harness/claude-artifact-pointers.md` at the U-CTX-03/04/05/06 R-CTX-1 context-optimization arc (2026-08-10, `B-17`/R-ICM-1 lineage). Old path is now a resolving stub — see `.harness/claude-artifact-pointers.md`. Loaded on demand only; never part of the default session-start read path.*

---

**Current head is DERIVED, not carried here.** This file is a lineage ARCHIVE; the authoritative per-family head version is derived from the `.harness/clearance/` marker corpus into `.harness/artifact-heads.md` by `tools/artifact_heads.py` (R-CTX-1 / U-CTX-11), and two CI gates keep that table current. Head labels inline below are reconciled at each arc but the generated table wins on any disagreement.

## §2.3 spec pointer

| Axis | Spec |
|---|---|
| CP | `Spec_Control_Plane_v1_119.md` (**v1.119 — canonical HEAD**, 2026-08-13 the `B-71` spec leg — see the §1.2 archive cell below for the full note) |
| CP | `Spec_Control_Plane_v1_118.md` (**v1.118**, 2026-08-11 the `B-153` Attribute-count column ratification — the §24.1 column DEFINED as the namespace's declared live export claim in DISTINCT attribute keys (span names are not attributes; intra-row cross-event repeats count once; the v1.117 §0.3 cross-namespace sum caveat stands); hitl.* cell enumerated 4→11 from C-CP-20 §20.6 (matching OD C-OD-05 §5.1 row 6 and harness_od/namespace_map.py); audit.* audited CONFORMING (§20.4 seven distinct keys); §24.1.A subtotal 35→42, declared sum 68→75; same-PR cascade: manifest hitl 4→11 + test_total_attribute_count_seventy_five + NEW cross-package map↔manifest witness (closes the #1311 residual); companion plan v2.51; NO OD delta owed (already correct); clearance `spec-control-plane-v1-118-cleared-2026-08-11.md`; B-153 CLOSES) |
| CP | `Spec_Control_Plane_v1_117.md` (**v1.117**, 2026-08-11 the `B-144` **venue-A** §24.1.B re-table — the single-venue (v1_2:2152-2166) F3-lifecycle-export table's superseded `Attribute count` cells (and the §24.1.A `engine.*` row, 3→4 per C-CP-09 §9.1 v1.3, +`engine.replay_disposition`; out-of-family round-2 catch) carried to the live ratified figures: `retry.*` 4→6 (C-CP-03 §3.5 v1.3 wholesale replacement; the four v1.2 names are retired) and `harness.breaker.*` 7→9 (OD C-OD-07 §7.1 as amended at OD v1.32 — venue mislabel 'CP v1.32' corrected in code comments); declared §24.1.B subtotal 25→29, §24.1.A 34→35, declared export sum 65→68; `fallback.*`/`lease.*` rows, §24.1.C and the composition-path summary byte-preserved; same-PR cascade: manifest retry 4→6 + engine 3→4, terminal-manifest invariant derived from `CP_EXPORTED_ATTRIBUTE_COUNT`, acceptance-#6 test 65→68 (bundled absorption §11.4); the hitl.*/Attribute-count column-semantics question explicitly out-of-scope → minted `B-153`; /resolve codex=A advisor=A (hitl scope split → safer follow-up); fork doc `class_1_fork_b144_cp_24_1b_stale_retable.md`; clearance `spec-control-plane-v1-117-cleared-2026-08-11.md`; `B-144` CLOSES. Prior v1.116, 2026-08-09 the operator-ratified `B-138` **disposition (a)** spec leg — THREE amendment sites, all within C-CP-21 §21: §21.5 row-1's value-set gloss corrected to the C-CP-25→C-CP-28 §25.2 `ValidatorFailClass` domain (the wire attribute `validator.fail.class` carries `schema_violation`…`external_rejection`); a binding declaration demoting the §21.1 retry-exit taxonomy (`ValidatorRetryExitClass`) from the wire name (routing/staircase semantics preserved verbatim; taxonomy demoted, never deleted; no wire attribute minted for it); §21.5 row-3's `validator.fail.permanence` derivation clause demoted (outcome projection lossy — `ESCALATE` conflates `permanent-fail-exit` with `HITL-recoverable`; sub-decision routed to `B-124`, unblocked on the taxonomy question); §1.1 emission condition corrected (conditional on populated `fail_class`; composer `unspecified` sentinel a non-member). BUNDLED with ADR-D5 v1.5→v1.6 §1.10.1 (authority-chain reconciliation at the source). CP-3/OD-4 set divergence dispositioned as disjoint-except-`class` subsets of one namespace; §21.5's declared count stays THREE (count gates untouched). ZERO contract numbers / code / Runtime / OD / CXA / hash impact. `B-138` CLOSES; `B-139` + `B-140` + `B-141` minted (`cause_attribution` zero producers; the `validator.fail` span-site realization gap + composer sentinel; the C-OD-14 §14.5.3/ADR-D6 retry-exit cascade). Clearances `spec-control-plane-v1-116-cleared-2026-08-09.md` + `ADR-D5-v1-6-cleared-2026-08-09.md`. Prior v1.115, 2026-08-03 the ratified `B-107` **Reading A-hybrid** spec leg — THREE contract amendment sites (v1.107 §1.1 scalar membership: the uniform-fallback unaddressed/eligible set holds only NON-EMPTY captured keys; `ResumeContext.effect_fence_resolutions`: ordinary construction refuses empty keys through a validated immutable **copy**, inherited by every subclass; resolver boundary: an empty `idempotency_key` is unresolvable BEFORE map-hit or eligibility, making validation-bypassed content inert) + ONE publication reconciliation (v1.112 §2.1); the b80 `False → True` consequence ratified; B-101(a) NOT jointly discharged; 4 variants / 10 source shapes / 7 carriers UNCHANGED; no Runtime §30 delta, new C-CP number, hash impact, or CXA §2.3 row; U-CP-64 implementation separately owed; clearance `spec-control-plane-v1-115-cleared-2026-08-03.md`; predecessor v1.114 — 2026-08-01 the `B-101` spec leg, disposition **(b)-PLUS** — ONE amendment site: `Spec_Control_Plane_v1_112.md` §1.2 constraint 3's stated residual (the paragraph headed *"The residual, stated precisely rather than left for a reader to discover"*) is **APPENDED TO** with ONE paragraph — **NO text retracted or reworded**, though **§1.3(c)'s reach is made EXPLICIT where v1.112 left it implicit** (it binds the CP-OWNED affordances its own detect-then-refuse witness exercises, NOT generic base-schema serialization); **under the broadest reading of §1.3(c) that IS a narrowing — the counter-reading is recorded in full at §0.5 finding (vi) with the one-step escalation route (disposition (a)) named**. The paragraph (i) declares the **ORDINARY-SERIALIZATION boundary formally OUT of the staleness fence's scope** (Pydantic v2 serializes a subclass through a base-typed field using the BASE's schema, so an accessor-derived context round-tripped that way loses its provenance carrier and the §30 fence does not fire); (ii) **NAMES `StepExecutionContext.resume_context`** (`harness-cp/src/harness_cp/workflow_driver_types.py:460`) as the **ONE live base-typed carrier** — of nineteen `ResumeContext`-typed annotations in `src/**`, exactly one is a model field, the rest being sixteen function parameters and two local bindings; (iii) **RECORDS three grounded facts, re-verified at this leg rather than carried from the row's dated note** — ZERO `model_dump`/`model_dump_json`/`TypeAdapter` of that field OR of its enclosing `StepExecutionContext` across `src/` AND `tests/` (and `StepExecutionContext` is never a field of another model, so never transitively serialized), `compose_branch_child_context`'s `model_copy` is shallow and non-revalidating, and `revalidate_instances` is unset repository-wide so Pydantic's `'never'` default holds; and (iv) states a **normative PROMOTION TRIGGER, keyed on MECHANISM not on the named carrier** — any future arc introducing ANY base-schema serialization boundary over a `ResumeContext` value on a resume path (a `ResumeContext`-typed model field, a base `TypeAdapter`/`RootModel`, a base-annotated parameter/return crossing a schema-generating boundary, or any equivalent) MUST promote `B-101` to **disposition (a)** (the closed variant discriminator on `ResumeContext`) and land it **BEFORE or SIMULTANEOUSLY WITH** that arc, never after. **Disposition (a) deliberately NOT applied** — it breaks the field-set-preserved-verbatim guarantee v1.112 §0.4 makes, and the row conditions it on a serialization boundary appearing on a real resume path, which does not exist at HEAD. **`ResumeContext`'s field set PRESERVED VERBATIM; §2.1 unchanged at FOUR variants / TEN shapes / SEVEN carriers; ZERO contract numbers, ZERO carriers retyped, ZERO hash impact, ZERO impl units owed, ZERO plan deltas owed, ZERO test changes owed** (verified: no plan cites `B-101`). **FIVE findings surfaced not absorbed** — the pin test `test_provenance_is_lost_through_base_typed_serialization_registered_b101` (`harness-runtime/tests/test_paused_workflow_state_accessor_b69.py:924`) **STAYS as the boundary marker** and is the executable half of the trigger; no impl/plan/test delta is owed; the row's (a)-vs-(b) condition is now mechanized; **trigger enforcement is a PRE-MERGE REVIEW OBLIGATION, not a validator rule** — the pin test breaks on a BEHAVIOR change but does NOT fire on the bare addition of a `model_dump` call; and `B-107`'s disposition (d) hits the SAME field-set obstacle, an adjacency recorded with the rows deliberately NOT merged. Register row `B-101` → **closed**; clearance marker `.harness/clearance/spec-control-plane-v1-114-cleared-2026-08-01.md`. Predecessor v1.113, 2026-07-31 the `B-100` spec leg — ONE amendment site: §2.1's empty-`idempotency_key` key-ABSENT treatment generalized from a PROVENANCE scope (*"the crash-reconstruction carrier"*) to a **STATE** scope (*any effect-fence source shape whose captured key is EMPTY*), adding a KEY-ABSENT sibling SOURCE SHAPE for the LINEAR `EffectFenceResumeState` and for `OrchestratorEffectFencePausedResumeState`. **VARIANT set UNCHANGED at FOUR; `effect-fence-addressable` 4 → 6 source shapes; union 8 → 10; distinct source CARRIERS UNCHANGED at SEVEN.** ZERO contract numbers minted, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact — read-side only; disposition (b) (capture-side refusal) deliberately NOT applied. The §2.1 per-source-shape table and the sub-discriminator parenthetical counts are refreshed (a PRE-EXISTING shape-vs-CARRIER conflation). **SIX findings surfaced not absorbed, the first being a FALSIFIED PREMISE in the authorizing register row `B-100`** — the orchestrator capture site *does* guard on a truthy key (`workflow_driver.py:12376`–`:12381`), so that shape is applied on a corrected TYPE-TOTALITY ground and is UNREACHABLE in production; **the sixth is the SCALAR uniform-fallback channel this delta cannot close — a projection cannot constrain a scalar the caller sets independently — REGISTERED as `B-107`** (a NEW id — `B-105` was minted and withdrawn at PR #1172, and `B-106` was minted concurrently on `main` at `d9f68b02` while this leg was in flight). **IMPL delta OWED, not this leg** (U-CP-64 + U-RT-148). Clearance `spec-control-plane-v1-113-cleared-2026-07-31.md`. Prior `Spec_Control_Plane_v1_112.md` (v1.112 — 2026-07-30 the RATIFIED `B-69` spec leg — §1 the REQUIRED non-downgradable `ResumeContext` response-provenance carrier; §2 the NEW public projection-returning surface publishing the FOUR-variant location classification with a required source-shape sub-discriminator over **EIGHT** source shapes *(this row read "SEVEN-source-shape" through the v1.112 entry; the v1.113 change-note §0.4 finding (iv) established that as a shape-vs-CARRIER conflation — v1.112 already spanned eight shapes across seven carriers, since the key-absent branch shape has no carrier of its own; refreshed here rather than left to re-seed the same error)*; §3 two conditional scope-limit lifts. Clearance `spec-control-plane-v1-112-cleared-2026-07-30.md`. **Pointer-lag note (surfaced by out-of-family review at this arc, NOT introduced by it): this row was stale at v1.108 — v1.109 / v1.110 / v1.111 each landed without refreshing it. Per-delta detail for those three lives at each version's own change-note + `harness-cp/CLAUDE.md` §1.2; this refresh is version-label-only for them, no content re-derivation.** Prior chain:) `Spec_Control_Plane_v1_108.md` (v1.108 — 2026-07-25 `B-72` item (1) spec leg — Class 1 tension `.harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md`, operator ratified "open the CP spec-leg now, co-designed with `B-71`" via `AskUserQuestion` — NEW additive §1.2 property 6: a pre-dispatch gate-owning branch MUST be counted in property 4's unaddressed-gate-owning-set test via a tree-wide-unique internal identity, and MUST NEVER be resolvable via the `child_run_id`-keyed `hitl_responses` map; properties 1-5 preserved verbatim; clearance `spec-control-plane-v1-108-cleared-2026-07-25.md`. Prior `Spec_Control_Plane_v1_107.md` (v1.107 — canonical HEAD at authoring, 2026-07-24 `B-70` spec leg — Class 2 fork `.harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md`, operator ratified "open the spec-leg now" via `AskUserQuestion` — NEW §1 multi-branch effect-fence-resolution fallback-safety invariant on the already-shipped `effect_fence_resolution_for`/`effect_fence_resolutions` mechanism, mirroring v1.106 §1.2 property 4's safety+liveness shape but simpler (no property-5 equivalent), across FIVE same-day out-of-family review rounds (round 1 added the omitted ORCHESTRATOR carrier; round 2 found the rule was a no-op for the LINEAR consume site; round 3 found round 2's own fix would permanently livelock 2+-simultaneous-LINEAR-pause resumes, corrected to genuine map-addressability via the LINEAR carrier's already-existing `idempotency_key`; round 4 added a `resume_handle` scope-limit note mirroring `B-69`); NO new field, NO enum change; clearance `spec-control-plane-v1-107-cleared-2026-07-24.md`. Prior `Spec_Control_Plane_v1_105.md` (v1.105, 2026-07-23 B-33 spec leg (ii) — AMENDED §20.3.1 row 7 (retires the "remains B-33's scope" deferral: `verify_rotation_6_steps` gains real IS-anchored presence/uniqueness + OD-anchored structural evidence checks, correlation id DERIVED from the validated window, never caller-supplied independently) + NEW §20.3.2 (`RotationPairEvidenceProvider` Protocol + typed DTO + REQUIRED physical-key-distinctness comparator — absence is an explicit incomplete, never a silent skip); clearance `spec-control-plane-v1-105-cleared-2026-07-23.md`. Prior `Spec_Control_Plane_v1_104.md` (v1.104, 2026-07-22 B-59-A spec leg — AMENDED §25.11 admission-guarantee row 2, extending the shared budget to span sequential bootstraps within one process (cross-references Runtime v1.104 §14.8.10.6 for the mechanism); clearance `spec-control-plane-v1-104-cleared-2026-07-22.md`. Prior `Spec_Control_Plane_v1_103.md` (v1.103, AMENDED §25.15 post-effect signing-carrier BRANCH-TERMINAL-WITH-RESULT rider (name-match fence; folds carry result_ref; pause mints no re-dispatching resume path); clearance `spec-control-plane-v1-103-cleared-2026-07-22.md`. Prior `Spec_Control_Plane_v1_102.md` (v1.102, 2026-07-19 B-48 apply — AMENDED §25.11 capacity gating + formal delegation + B-39 interim constraint; clearance `spec-control-plane-v1-102-cleared-2026-07-19.md`. Prior v1.101, 2026-07-18 RATIFIED B-51/B-52/B-54(+B-53) apply arc; clearance at `.harness/clearance/spec-control-plane-v1-101-cleared-2026-07-18.md` — §13.5.1 tenant converter / §28.10.4 carve-out + catch-ordering / §20.3.1 backend-aware walk / NEW §20.1.1. Prior v1.100, cleared 2026-07-15; the v1.97 B-21 / v1.98 B-22 / v1.99 B-31 / v1.100 deltas carry their own change-notes + clearance markers; 2026-07-18 pointer refresh at the B-43 PR — prior head text preserved verbatim below as historical: v1.96 — canonical HEAD, 2026-07-12 pointer catch-up per R-600 cadence-5 adjacent finding: this row was stale at v1.38 through the B-18-3C-PREWARM family — PREWARM v1.87 / COHORTKEY v1.88 / DEFAULT-ON v1.89 / CASCADE v1.90 / TIMEOUT-LEDGER v1.91 / FENCE-LEDGER-FIDELITY v1.92 / -OW v1.93 / RECONSTRUCT-RESIDUAL v1.94 / EPOCH-PARTITION v1.95 / PREWARM-OW v1.96; per-delta detail at each version's own change-note + memory `b18-epoch-partition-design-decision-record.md` + `b-18-prewarm-ow-design-decision-record.md`. v1.38 chain below preserved verbatim as historical — v1.38 — HEAD; **B4-Slice-4** C-CP-06 §6.1/§6.2/§6.6 NEW `StepOverride.agent_role` per-step ROLE override [**paired runtime v1.51→v1.52 §14.5.3 invariant-2/3 RELAXATION**, the committed-invariant sacrifice Slice 3 foreclosed; mechanism = **Option B** composition-time fold (CP driver folds the override onto the single `StepExecutionContext.agent_role` source at all 6 topology sites, precedence per-step > fan-out-derived > default; **runtime dispatch read byte-unchanged**, zero `harness-runtime` impl edit — the `default_gate_level → parent_gate_level` v1.20 precedent); **OPERATOR-GATED** committed-invariant relaxation (advisor reversed a probe-resolves attempt to dissolve the gate: `default_gate_level` had no invariant forbidding it, role has the explicit §14.5.3 invariant 2); provenance at the WIRED per-step override state-ledger entry (`binding.model_dump`), NOT the §5.2 hash → no IS-spec change; reuses the committed `AgentRole` shared type, no new contract/ADR/enum/fail-class; bundled-absorption CP v1.38 + runtime v1.52 + `harness-cp` impl + by-execution tests co-land] 2026-06-17. Prior v1.37 — **B4-Slice-3** C-CP-06 §6.1/§6.2/§6.6 NEW `StepOverride.prompt_version_sha` per-step PROMPT override [surface=`StepOverride` not run-level `PromptSelectionManifest` (`step_id` is a workflow-scoped instance, not a run-level category — Route B is a Track-B reuse hazard); provenance at the WIRED per-step override state-ledger entry (`binding.model_dump`), NOT the run-level §5.2 hash → no IS-spec change; per-step ROLE OUT of scope = Slice-4 gate per §14.5.3 single-role-source; v1.27 §2(d) explicit-extension discipline, mirror v1.20/v1.22/v1.34; no new contract/ADR/enum/fail-class; bundled-absorption IMPL co-lands] 2026-06-17. Prior v1.36 — **R-spec-1** C-CP-02 NEW §2.5 Layer-3 LLM_AS_ROUTER resolution surface — resolve the async router-model call at the already-async `infer()` [**Reading B**; Reading A foreclosed by ADD §5.3.3 + §2.2 "Probabilistic"]; injected **OPTIONAL** `RouterResolutionFn`, absent ⇒ preserved `RoutingCandidateUnresolvedError` raise [DECLARATIVE-only + pre-bind byte-unbroken]; **no-regress** direct-dispatch never re-enters `route()`/`infer()`; rationale via C-CP-01 §1.4 `routing.binding_rationale` span attr, frozen 4-field `RoutingDecisionTrace` un-widened; 200 ms `LayerBudget`, L3-budget-exhausted ⇒ preserved raise; SPEC-ONLY impl@R-impl-1 mock/R-impl-2 vendor-gate, 2026-06-16. Prior v1.35 — **B2-spec-2** C-CP-19 NEW §19.1.2 `MCP_TRUST_GATE_LEVEL_FLOOR` per-server trust HITL *gate*-floor materialization (R-FS-1 multi-server MCP; F2-02; resolves the v1.15 §19.1.1.1 row-3 spec-silence — the 4th, last unmaterialized §19.1 axis; **floor-only/monotone** probe-resolved [the gate is `max()`-over-escalation-rank so a floor can only RAISE it + landed U-CP-91 `mcp_trust`-never-overridden → loosening would reverse on-main + break the locked T-perm-1 composition] + operator-ratified Table A: L0→DENY/L1→ASK/L2→ASK/L3→AUTO; SPEC-ONLY, impl at B2-impl) 2026-06-16. Prior v1.34 — **B2-spec-1** C-CP-27 NEW §27.8 identity-by-ordinal `MCPServerTrustLevel → MCPTrustTier` trust *telemetry* projection (R-FS-1 multi-server MCP reshape; adopt-and-note, C10⊥C11-council-converged; retires the constant-collapse stub; the per-MCP-server-trust *gate*-axis materialization is the now-landed B2-spec-2 leg) 2026-06-16. Prior v1.33 — **E-spec-3** C-CP-07 §7.4 reconciler-loop substrate-deferral reconciliation, operator-ratified 2026-06-15 — adds `reconciler-loop` to the §7.4 impl-discretion clause [hand-rolled etcd-style per I-6, reconciling §7.1 named "K8s controller / etcd" reference substrate with the no-vendored-K8s invariant; §7.1/§7.2/§7.4-floor-table + all else PRESERVED VERBATIM]. Intervening deltas: v1.31 NEW §29/C-CP-29 `PromptSelectionManifest`; v1.32 NEW §25.10–§25.21 C-CP-25 non-linear topology driver-strategy extension. **Prior v1.30** — Canonical-reading amendment at §16.5.12.2 + §16.5.12.3 collapsing the workflow-vs-engine-layer composer signature split into a single uniform pattern across all 6 §16.5.2 composers per `.harness/class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` Reading (C) operator-ratified 2026-05-31 (Q1=C uniform resolver-closure / Q2=v1.30 canonical-reading amendment / Q3=ZERO cross-axis cascade / Q4=PR-2+PR-3 collapse single PR). Probe-first discipline at PR-2 impl-arc opening empirically discriminated v1.29 §16.5.12.2 workflow-layer recipe column (`procedural_tier_snapshot_ref = resolve_procedural_tier_snapshot(harness_context)`) against composer signatures at HEAD: NONE of the 4 workflow-layer composer signatures accept `harness_context`. Three Readings enumerated at fork doc §3 (A ctx-passthrough with HarnessContext re-home + cross-axis cascade / B caller-resolves + passes Identifier + HALT-posture shift composer-site → caller-site / C uniform resolver-closure mirroring §16.5.12.3 engine-layer pattern + ZERO cross-axis cascade). Reading C ratified per fork doc §5. NEW §1.1 uniform composer signature shape (`procedural_tier_snapshot_resolver: Callable[[], Identifier]` kw-only-required across all 6) + §1.2 per-composer recipe table SUPERSEDING v1.29 §16.5.12.2 6-row table + §1.3 §16.5.12.3 signature-extension widening from 3 engine-layer to all 6 composers + §1.4 runtime wiring uniform across all 6 (cp_is_wiring layer gains `procedural_tier_snapshot_resolver` field; `materialize_cp_is_wiring_stage` gains resolver param; bootstrap stage 6 builds resolver via `make_procedural_tier_snapshot_resolver(ctx)` at stage-6 entry where `ctx.skills` stage-2 + `ctx.routing_manifest` stage-3b populated) + §1.5 §16.5.12.5 HALT-on-resolver-failure posture uniform across all 6 + §1.6 §16.5.12.7 invariants preservation. v1.29 §16.5.12.2 table + §16.5.12.3 + §16.5.12.4 + §16.5.12.5 + §16.5.12.7 file text PRESERVED VERBATIM per delta-only-spec-file convention; v1.30 §1 canonical reading operative for downstream consumers. ZERO contract removal at any other contract; ZERO new field at IS HEAD (EntryPayload sidecar already at IS spec v1.3); ZERO breaking change at C-CP-16 §16.2 / C-CP-20 §20.4 signed audit-ledger surfaces (PRESERVED VERBATIM); ZERO cross-axis cascade verified at design-substrate/ grep (AS / OD / IS / runtime spec / ADR / ADD / PRD / CXA v2.17 all unchanged). NEW sub-species candidate at workflow v1.13 §7.4.7.2: `cascade-split-collapse-to-uniform-via-empirical-probe` (cardinality 1; awaits second instance before workflow-doc promotion). Co-published: CP plan v2.31 single-arc canonical-reading amendment at U-CP-74..U-CP-79 + impl arc at 6 composer signatures + 6 EntryPayload sidecar populations + cp_is_wiring runtime layer + bootstrap stage 6 + 11 NEW v1.30 sanity tests at `harness-cp/tests/test_procedural_tier_resolver_v1_30_apply.py` + 8 sibling test-module adaptations + 2 stale-carry refreshes (harness-as secret-fetch-audit + harness-cp sibling-ledger composition from PR #89 IS spec v1.3 absorption) + clearance markers at `.harness/clearance/Spec_Control_Plane-v1_30-cleared-2026-05-31.md` + `.harness/clearance/Implementation_Plan_Control_Plane-v2_31-cleared-2026-05-31.md` + fork-doc Status PROPOSING → ✅ APPLIED-AS-READING-C. 3533/3533 tests pass + 10 skipped (was 3522/10 pre-arc; +11 NEW v1.30 sanity tests). 2026-05-31. v1.29 prior — Narrow-scope recipe-completion amendment authoring NEW §16.5.12 — per-composer `procedural_tier_snapshot_ref` sidecar population discipline at the 6 §16.5.2 composers — and canonical-reading amendment at §16.5.3 chapeau acknowledging IS HEAD `EntryPayload` field-set extension from 4 fields to 5 fields per IS spec v1.3 §5.1 absorption at PR #89 commit `ec42d22` 2026-05-30. PR-1 of a 3-PR stack closing the H_T-IS-2 producer-site cascade (PR-2 lifts 9 ctx-access composer sites; PR-3 lifts 3 engine-layer composer sites with §16.5.12.3 kw-only-callable signature extension). Council orchestrator pilot at session-open 2026-05-31 resolved T1 (cascade-grouping shape: 1+11 vs. 9+3) in favor of C1 (co-primary) per-pattern 9+3 PR shape via empirical probe of IS spec v1.3 §5.2 amendment 2 — engine-layer kw-only-callable threading pattern explicitly pre-authorized at IS-axis authority. §16.5.7 enumeration of `ledger_writer` is a floor (composer MUST accept this) not a ceiling (composer MUST accept ONLY this); Python signature semantics permit additional kw-only parameters without contradicting spec text. NEW §16.5.12.1 field scope + workflow-context-vs-outside-workflow-context discriminator + §16.5.12.2 uniform per-composer population recipe (6 composers split: 4 workflow-layer use direct `resolve_procedural_tier_snapshot(harness_context)`, 3 engine-layer use injected `procedural_tier_snapshot_resolver()` closure) + §16.5.12.3 composer signature extension at engine-layer composers (sibling kw-only `procedural_tier_snapshot_resolver: Callable[[], Identifier]` per IS spec v1.3 §5.2 amendment 2 pre-authorization) + §16.5.12.4 runtime wiring discipline binding per §16.5.8 to `make_procedural_tier_snapshot_resolver(harness_context)` U-RT-112 factory closure + §16.5.12.5 composer-site HALT-on-resolver-failure posture (workflow-context with `None` sidecar = producer-site bug per §16.5.12.1; resolver failures are programming errors at composition time) + §16.5.12.6 caching scope (per-emission re-resolve vs. per-composer-construction factory closure — both admissible per U-RT-112 AC #8 trailing implementer-discretion clause) + §16.5.12.7 7 invariants. Canonical-reading amendment at §2 (the 4-field → 5-field framing refresh at v1.26 §16.5.3 chapeau + §16.5.9 invariant 2; v1.26 file body PRESERVED VERBATIM; downstream readers apply substitution when interpreting v1.26 §16.5.3 / §16.5.9 invariant 2 field-set claims). NEW sub-species 3 candidate `cross-axis-stale-since-sibling-spec-version-bump` catalogued at v1.29 §3 (e) — v1.26 §16.5.3 "4-field" framing went stale at IS spec v1.3 absorption 2026-05-30 without CP spec authoring an acknowledgement amendment; carry persisted across v1.27 + v1.28 + ~24 hours. Cardinality 1; awaits second instance before workflow-doc promotion. First design-substrate amendment authored downstream of a council deliberation arc (v1.29 §3 (f)) rather than direct architect-recommendation or operator AskUserQuestion arc; council orchestrator pilot artifact captured for cadence calibration. ZERO impl + ZERO test at v1.29 (recipe-completion doc patch only). ZERO breaking change at C-CP-16 §16.2 + C-CP-20 §20.4 (audit-half + signing PRESERVED VERBATIM). ZERO new field at producer composer surface (3 engine-layer composers gain Python-level sibling kw arg per IS spec v1.3 pre-authorization; not spec-level new field). ZERO cross-axis cascade (IS / OD / AS / runtime / CXA / ADR / ADD / PRD / Workflow all unchanged at this arc). H_T-IS-2 transit posture UNCHANGED at PARTIAL; PARTIAL → RETIRED gated on PR-2 + PR-3 producer-site cascade completion per X-AL-2 second conjunct. Co-published with workspace `CLAUDE.md` row bump + harness-cp/CLAUDE.md row bump + clearance marker `.harness/clearance/Spec_Control_Plane-v1_29-cleared-2026-05-31.md`. 2026-05-31. v1.28 prior — Surgical amendment at v1.27 §16.5.6 audit-half stub annotation absorbing operator-ratified Reading (D) hybrid disposition of `.harness/class_2_fork_audit_stub_timestamp_universal_fix_plus_per_tier_annotation.md` (filed at this PR 2026-05-29 session 2). Operator AskUserQuestion Q-set: Q1=(D) hybrid — fix `timestamp` at all 3 sibling composer sites (universal bug per C-CP-16 §16.2 + ADR-D5 §1.4 — `timestamp` is non-tier-conditional); annotate `prior_event_hash` + signing as canonical end-state at solo-developer tier per ADR-D5 §1.4 row 1 ("no hash chain required by default ... no signing key required"); team-binding+ tier wiring stays PARTIAL with explicit deferral anchor. **Trigger reframing**: PR #66 Q2=(iii) framed remediation singularly at `emit_override_audit_entry`; empirical orientation at this arc surfaced **3 composer construction sites** with structurally-identical `timestamp = ""` pattern (`per_step_override_evaluator.py:225-231`, `sub_agent_gate_level_descent.py:199-206`, `hitl_gate_composer.py:713 + :753-762`). Universal-fix scope at v1.28: `timestamp` populated via composer-site clock `datetime.now(UTC).isoformat()` at all 3 sites — ZERO breaking change at signature surfaces; ZERO new types; ZERO field-set extension; ZERO X-AL-3 silent-extension concern. Sentinel `prior_event_hash` (`"0"*64` at sites 1+2; `sha256(b"")` at site 3) preserved at solo-developer tier per ADR-D5 §1.4 row 1 spec-canonical authority. Team-binding+ tier closure (CP-audit chain state holder + threading + signing wiring) deferred to operator-deployment-time opt-in arc (mirror precedent: AS-8d batch-31 + OD-5 batch-32 + OD-6 batch-33 sub-species 7.deployment-time-opt-in-gate). NEW §16.5.6.X per-tier-conditional stub field disposition supersedes v1.27 §16.5.6 single-site framing. v1.27 substantive content preserved verbatim except for the scoped §16.5.6 amendment. ZERO cross-axis cascade (intra-CP-axis + read-only citation of ADR-D5 §1.4 anchor). Co-published this arc: harness-cp impl (`per_step_override_evaluator.py` + `sub_agent_gate_level_descent.py` composer-site clock) + harness-runtime impl (`lifecycle/hitl_gate_composer.py` composer-site clock + drops redundant local datetime import) + harness-cp tests (+3 NEW) + harness-runtime tests (+1 NEW via OD projection) + 2 regression-flip refreshes (`test_override_state_ledger_emission.py` + `test_lifecycle_handoff.py`) + workspace `CLAUDE.md` CP spec row bump + harness-cp/CLAUDE.md row bump + fork doc Status PROPOSING → ✅ APPLIED-AS-(D). 2109/2109 tests pass + 10 skipped (was 2090 at v1.27; +19 net). 51st application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` — advisor caught multi-site-pattern + per-tier-carve-out framing before fork doc was authored; AskUserQuestion ratified Reading (D) hybrid over (A) full Class 1 / (B) refine-deferral-only / (C) full sub-species 10 reclassification. NEW workspace pattern candidate `multi-site-pattern-not-surfaced-at-original-deferral-arc` at workflow v1.13 §7.4.7.2 — cardinality 1; awaits second instance. 4th consecutive stale-pick caught at orientation from `20260529-204731` checkpoint per `[[feedback-checkpoint-remaining-work-is-advisory-not-authoritative]]`. 2026-05-29 session 2. v1.27 prior — Surgical amendment at v1.26 §16.5.4 row U-CP-14 idempotency-key formula + §16.5.6 dual-emission discipline annotation absorbing operator-ratified Reading A resolution of `.harness/class_1_tension_u_cp_14_dual_emission_stubs_and_disambiguator_semantics_gap.md` (filed at PR #65 `d8d091e` 2026-05-29). Operator AskUserQuestion Q-set: Q1=(A) drop `override_id` + `policy_id` placeholder segments + collapse formula to `workflow_id || step_id || sha256(outcome_canonical_bytes).hex()`; Q2=(iii) audit-half stub remediation IN-SCOPE-BUT-MARK-DEFERRED — annotate `emit_override_audit_entry` stub functional gap at §16.5.6 + per-axis CLAUDE.md + plan body; Q3=(i) accept v1.25 + v1.26 formula re-ratification reversing prior naming decision; Q5=(i) bundled apply-pass per CLAUDE.md §11.4; Q6=(α) intra-CP-axis only. Decisive structural constraint: `per_step_overrides: dict[StepID, StepOverride]` field at `workflow_manifest_entry.py:109` enforces per-WorkflowManifestEntry step-id uniqueness on override identity at v1.6 MVP scope; `override_id` + `policy_id` named-but-undefined placeholders had no semantic source at any LANDED type, composer body, or spec definition site. Reading A collapses to type-shape invariants empirically present at HEAD; ZERO new types; ZERO field-set extension; ZERO X-AL-3 silent-extension concern. NEW §16.5.6 v1.27 annotation acknowledges `emit_override_audit_entry` as a functional stub at HEAD `d8d091e` (ignores override + actor inputs; hardcodes placeholder fields); structural dual-emission discipline holds; functional closure deferred per Q2(iii). v1.26 §16.5.3 + §16.5.5 + §16.5.7 + §16.5.8 + §16.5.9 + §16.5.10 + §16.5.11 PRESERVED VERBATIM. ZERO breaking change at signed-payload surfaces (C-CP-16 §16.2 + C-CP-20 §20.4 preserved verbatim; `emit_override_audit_entry` signature preserved). ZERO cross-axis cascade per Q6(α). Co-published this arc: CP plan v2.29 → v2.30 single-unit-body amendment at U-CP-74 (AC #2 formula 5-tuple → 3-tuple + Signatures drop override_id + policy_id kwargs) + harness-cp impl (`per_step_override_evaluator.py` composer + `_override_idempotency_key` helper signature trim) + harness-runtime impl (`lifecycle/cp_is_wiring.py` wiring-layer signature trim) + harness-cp tests + harness-runtime tests (2090 / 10 skipped passing; 0 pyright errors at touched files) + fork doc Status PROPOSING → ✅ APPLIED-AS-READING-A + clearance marker `.harness/clearance/Spec_Control_Plane-v1_27-cleared-2026-05-29.md`. Workspace pattern at v1.27 closure: `[[strike-revision-on-refined-second-tier-reason]]` at spec layer — v1.25 + v1.26 formula naming STRIKE preserved on refined second-tier reason (no semantic source) rather than un-STRUCK at original framing (multi-version policy roadmap); mirror plan-layer precedent at runtime plan v2.39 AC #4 STRIKE refinement per Workflow v1.13 §7.4.7.2 species 2 sub-species catalogue (workspace v1.13 publication 2026-05-29 commit `9ddb9ba`). H_T-RT-35 transit posture UNCHANGED at PARTIAL — this arc closes 1 of 5 upstream blockers at the FORK-DOC-FILED → APPLIED transit; 4 remaining upstream arcs (engine-layer impl + bootstrap-emission-substrate + HITL `rewrite_tool_call` firing-site + sibling-ledger `emit_sibling_ledger_entry` firing-site) gate RETIRE-READY transit. 2026-05-29. v1.26 prior — Surgical amendment at v1.25 NEW §16.5 sub-section absorbing β.i resolution of nested fork `class_1_tension_u_cp_74_entrypayload_field_set_drift.md` operator-ratified 2026-05-29 Q-set (A + β.i + Q-β.i-1(a) + Q-β.i-3(b)). REWRITES §16.5.3 EntryPayload field set to match IS HEAD `(action_id, idempotency_key, actor, timestamp)` at `harness-is/src/harness_is/state_ledger_write.py:62-75`; relocates outcome-bytes semantic to idempotency_key derivation suffix per §16.5.4 (appends `|| sha256(outcome_canonical_bytes).hex()` per row preserving v1.25 disambiguators verbatim); reframes §16.5.5 chapeau as outcome-bytes scheme consumed by idempotency_key per Q-β.i-3(b); updates §16.5.8 Q4 attribution clause + §16.5.9 invariant 2. §16.5.1 / §16.5.2 / §16.5.6 / §16.5.7 / §16.5.10 / §16.5.11 + §16.5.5 per-composer outcome-bytes recipe TABLE + §16.5.4 per-composer disambiguator notes PRESERVED VERBATIM. ZERO IS-axis cascade (β.i is structural mirror of parent fork (S) sibling-variant). ZERO cross-axis cascade (IS / OD / AS / runtime spec / CXA all unchanged). Co-published with CP plan v2.28 → v2.29 cascade (U-CP-74..79 ACs + signatures re-author against corrected EntryPayload + idempotency-key suffix) + nested fork doc + parent fork doc closure-back-references owed post-merge. `[[impl-time-grounding-pass-pre-merge-revision]]` workspace pattern SHARPENED: grounding MUST verify type field-sets when design substrate enumerates per-field semantics for externally-defined types (v1.25 grounding caught module/symbol existence + 3 naming mismatches but missed EntryPayload field-set drift). PR #38 routing doc + this v1.26 amendment compose the closure arc. v1.25 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-29. v1.25 prior — Substantive amendment authoring NEW §16.5 sub-section under C-CP-16 absorbing the CP→IS state-ledger emission contract for **6 CP source units** (U-CP-14 sibling-variant + 5 greenfield composers at U-CP-27/30/37/49/50) per `.harness/architect_recommendation_u_rt_35_gap_b_within_path_a.md` operator-ratified 2026-05-28 Q-set ((W/S)=S sibling-variant; Q1=Q1(b) override-specific idempotency; Q3=Q3(a) composer-site clock; Q4=Q4(b) IS-computes-hash-chain; Q5=Q5(a) outcome-bytes; Q6=Q6(a) `emit_override_state_ledger_entry` naming). 11 sub-sections authored: §16.5.1 scope-and-authority (two-typed-ledger distinction CP-audit C-CP-16 §16.2 vs IS-anchored C-IS-10 §10.1) + §16.5.2 6-composer enumeration grounded against `harness-cp/src/harness_cp/` HEAD function refs (`select_engine_class` / `rewrite_tool_call_to_hitl` / `capture_pause_snapshot` / `attempt_resume` / `PauseResumeProtocol` class methods) + §16.5.3 canonical EntryPayload composition (4-field producer-side; IS computes timestamp + prior_event_hash per C-IS-13 §13.5) + §16.5.4 idempotency-key formulas (per-composer override-specific) + §16.5.5 response-hash recipes (Q5(a) outcome-bytes; canonical JSON via shared `_canonicalize_outcome_bytes` helper) + §16.5.6 U-CP-14 dual-emission discipline + §16.5.7 greenfield firing-site discipline (post-resolve-pre-return per composer; 2-layer pause/resume split per CP spec v1.11 §26 NEW NOTE coexistence) + §16.5.8 runtime wiring discipline (binds to `ctx.ledger_writer.append_ledger_entry`) + §16.5.9 7 invariants + **NEW §16.5.10 NOT-APPLICABLE reclassifications** (U-CP-12 declarative-only per C-CP-05 §5.2 static `PER_CLASS_ATTRIBUTE_SETS` tuple; U-CP-52 runtime-axis-composed per canonical §17.4 `hitl_gate(...)` signature raising `NotImplementedError` + runtime-side `RuntimeHITLGateComposer` per C-RT-18 §14.8 production routing) + §16.5.11 status posture. Impl-time grounding pass against `harness-cp/src/harness_cp/` HEAD identified 2 architectural reclassifications + 3 naming-mismatches corrected (PR #37 force-pushed pre-merge revision avoiding 5+ sub-species-3 stale-row carry events). Decisive structural constraint for (S) sibling-variant: CP spec v1.7 §13.5.1 NOTE 3 hash-bytes-immutability discipline at C-CP-20 §20.4 signing contract — widening C-CP-16 §16.2 would break CPSignedAuditLedgerEntry signed bytes OR defeat widening. ZERO change to §16.2 + §20.4 + v1.7 §13.5.1 cp_audit_to_od_audit converter; ZERO breaking change at OD axis. Co-published with CP plan v2.28 NEW units U-CP-74..79 + CXA v2.16 §0.4 tracking-marker (6 PENDING + 2 NOT-APPLICABLE rows) + fork-doc closure-back-reference annotation + workspace CLAUDE.md row bumps. v1.24 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-28. v1.24 prior — v1.24 prior — v1.24 — Substantive amendment authoring NEW §28.10 sub-section under C-CP-28 ValidatorFramework absorbing the `ValidatorPostEvaluateHook` Protocol surface per `.harness/class_1_fork_u_od_40_validator_post_evaluate_hook.md` Reading (B) operator-ratified 2026-05-28 Q-set (Q1=B Hook Protocol; Q2=harness-cp Protocol home; Q3=best-effort swallow; Q4=optional ctor param None default; Q5=β NO new CXA edge). 7 sub-sections authored: §28.10.1 Protocol declaration (async on_post_evaluate kw-only) + §28.10.2 ctor extension + §28.10.3 firing site post-construction pre-return + §28.10.4 6 invariants + §28.10.5 deferred-to-discretion + §28.10.6 producer-side reference + §28.10.7 status posture. Trigger: U-OD-40 cost-attribution at validator.evaluate site requires callback into harness-runtime cost-attribution helpers; harness-cp dep-graph excludes harness-od import; X-AL-3 spec extension required. Mirror precedent: SkillActivationHook (U-RT-101 / runtime spec v1.32 §14.17). ZERO change to §28.1-§28.9; ZERO breaking change at 6 existing ConcreteValidatorFramework(...) sites (None default); ZERO change to SyncValidatorFrameworkFacade; ZERO cross-axis cascade per Q5=β. Co-published with CP plan v2.27 NEW U-CP-73 singleton-extension unit + OD plan v2.23 U-OD-40 LANDED + runtime plan v2.29 U-RT-84 +AC #6 + harness-cp impl (13 NEW tests) + harness-runtime impl (2 NEW cost-helper modules + factory binding + composer inline-wrap + 18 NEW unit tests + integration test) + batch-28 retirement event H_T-OD-5 PARTIAL → RETIRE-READY (surface coverage 2/4 → 4/4; bucket-membership 1 → 2 with OD-5 NEW alongside AS-8d). 2188/2188 tests pass + 4 skipped. v1.23 + earlier PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-28. v1.23 prior — Fidelity-pure citation-correction patch closing the long-carried `resumption.kind` ↔ `engine.replay_disposition` attribute-carrier divergence between §5.2 + §8.1 + §8.3 v1.2 lineage and §9.1 v1.3 4-attribute amendment — **CLOSED-as-canonical-reading-harmonization-with-production-de-facto** 2026-05-27. Arc opened as H_T-CP-9 ResumptionKind PARTIAL → RETIRE-READY investigation; empirical orientation at `workflow_driver.py:649-663` + `per_class_attribute_composition.py:140-145` surfaced TWO divergences. (1) Emission-scope cite-correction: production skips `WorkflowEventClass.RESUMPTION` emission for `EngineClass.PURE_PATTERN_NO_ENGINE` per §25.5 v1.4 scope carve-out ("At v1.4 scope: emit on re-entry if engine_class == save-point-checkpoint") — pre-v1.23 skip-comment cited §8.2 row 3 (JOIN/dedup, wrong cite); v1.23 co-publication refreshes the comment to cite §25.5 v1.4 scope carve-out + §8.1/§8.3 contract-space note. Reading B canonical (§8.1/§8.3 declare full contract space; §25.5 carves out v1.4 implementation scope; spec internally coherent). (2) Attribute-carrier harmonization at §5.2 + §8.1 + §8.3 (6 enumerated sites): the §9.1 v1.3 4-attribute amendment (F2-12 sub-scope (i) closure 2026-05-14) introduced `engine.replay_disposition` as the canonical 4th `engine.*` attribute superseding the v1 §5.2/§8.1/§8.3 `resumption.kind` carrier name at the at-emission layer, but the original declaration sites carried stale carrier-name text from v1.3 through v1.22 (20+ delta versions, 13+ days). Grep across `harness-cp/src/` + `harness-runtime/src/` + `harness-od/src/` confirms ZERO literal `"resumption.kind"` set sites; production carries `"engine.replay_disposition"` exclusively per `engine_namespace.py:85`. Plan U-CP-20 acc #2 + U-CP-21 4-attribute namespace conformed to §9.1 v1.3. v1.23 canonical-reading amendment harmonizes the 6 declaration sites with §9.1 v1.3 production-de-facto. Conceptual enum identity preserved (`ResumptionKind` 5-class semantic taxonomy at `resumption_kind.py` + `ReplayDisposition` 5-value enum at `engine_namespace.py` are TWO ENUMS with distinct member string values mapped 1:1 to `EngineClass` per ADR-D1 v1.2 §1.1.1; the carrier-name harmonization at v1.23 does NOT collapse them — it canonicalizes which attribute name is set on the `workflow.resumption` span). Sub-species **3.intra-spec-sibling-supersession** catalogued at v1.23 §"Adjacent observations" (d) — EIGHTH sub-species refinement of species 3 (resolved-but-carry-stale-inherited) at workflow v1.9 §7.4.7.2 in 6 consecutive arcs. Distinct closure-event-class: a sibling sub-section amendment within the same spec file lineage supersedes an attribute/contract surface, but the original declaration site is not amended at the same arc, leaving stale carry-text in the original section across all subsequent delta-only versions. Sub-species set at species 3 now EIGHT in 6 consecutive arcs; workflow v1.9 §7.4.7.2 "Sub-species" column extension increasingly warranted (empirical cardinality of 8 in 6 consecutive arcs is strong evidence). 22nd application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture — advisor caught Reading-B-not-Class-1-fork framing during empirical orientation BEFORE fork doc was authored; operator AskUserQuestion ratified Reading-B + dual fidelity-pure cite-correction arc shape (Option A) over Reading-A widen-production (B) OR Class 1 fork doc + full ratification (C). Co-publication: workflow_driver.py:662-669 comment + per_class_attribute_composition.py:140-150 comment + workspace CLAUDE.md + harness-cp/CLAUDE.md row bumps. ZERO C-CP-NN contract change; ZERO Workflow §4.1.2 Class-2 amendment; ZERO fork doc filed; ZERO test addition or modification; ZERO production behavior change; ZERO cross-axis cascade verified via grep. NO CP plan delta (canonical-reading harmonization is intra-spec scope; plan already cites §9.1 v1.3 at U-CP-20/U-CP-21 directly). NEW adjacent finding (f) catalogues `empirical-orientation-discovers-pre-existing-divergence` pattern + strengthening candidate for workflow v1.9 §7.4.7.3 audit-template: "at any §N.M amendment that introduces a new attribute / carrier / enum, audit ALL declaration sites in sibling sub-sections for stale-carry-text against the amendment." v1.22 + earlier PRESERVED VERBATIM. 2026-05-27. v1.22 prior — Fidelity-pure citation-correction patch partially closing v1.21 §"Adjacent observations" (c) at the **tenant_id** sub-axis only — **CLOSED-as-binding-fix-not-schema-extension** 2026-05-27. Arc-shape divergence from CP-19 precedent: tenant_id is per-deployment scoping sourced from `RuntimeConfig.tenant_id` via `HarnessContext.tenant_id` (already plumbed end-to-end pre-v1.22 except at `workflow_driver.py:764` hardcode), NOT per-workflow operator-surfaced like `default_gate_level` at v1.20 §6.1.Y. The v1.7+ deferral comment at `workflow_driver_types.py:189-192` explicitly named `HarnessContext.tenant_id` / `RuntimeConfig.tenant_id` as canonical source — binding-fix-shape forecloses CP-19-precedent fork-doc + Workflow §4.1.2 Class-2 amendment ceremony. Production lift: `workflow_driver.py:764` reads `ctx.tenant_id`; DriverContext Protocol declares `tenant_id: str | None`; HarnessContext exposes via `@computed_field` reading `self.config.tenant_id`. 1799/1799 harness-cp + harness-runtime tests pass + 4 skipped (4 NEW tenant_id propagation tests + 4 `_FakeCtx` fixture updates across test_workflow_driver / _drain / _envelope / _validator_hook). ZERO C-CP-NN contract change; ZERO Workflow §4.1.2 Class-2 amendment; ZERO fork doc filed; ZERO behavior change at `RuntimeConfig.tenant_id=None` (v1.6 single-tenant default preserved at audit-writer via `_SINGLE_TENANT_TAG`); ZERO cross-axis cascade verified via grep. NO CP plan delta (binding fix is intra-driver-composition; not an atomic unit boundary). Sub-species **3.binding-fix-not-schema-extension** catalogued at v1.22 §"Adjacent observations" (e) — SEVENTH sub-species refinement of species 3 (resolved-but-carry-stale-inherited) at workflow v1.9 §7.4.7.2 in 5 consecutive arcs; distinct closure-event-class: arc-shape-reframe at advisor pre-substantive consultation. 21st application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture — advisor caught framing error in original AskUserQuestion that presented all 3 v1.7+ fields as CP-19-precedent shape; empirical orientation discriminated tenant_id surface. `parent_sandbox_tier` + `parent_entry_hash` axes preserve verbatim per FM-2 (CP-19-precedent shape remains canonical for those). v1.21 + earlier PRESERVED VERBATIM. 2026-05-27. v1.21 prior — Fidelity-pure citation-correction patch closing v1.20 §0.8 (h) batch-21 filing carry + filing footer "H_T-CP-19 status" line final clause — both as **CLOSED-as-filed-at-batch-21** 2026-05-27. Batch-21 retirement event filed at commit `c5582b6` (2026-05-27 21:14) merged to main at `79865d6` (2026-05-27 21:15); v1.20 §0.8 (h) framing "(separate retirement-event filing arc; operator-discretion timing)" + filing footer "PARTIAL → RETIRE-READY transit owed at batch-21" both became stale ~10 min after v1.20 publication. NEW §1 finding-closure-disposition refresh + §2 cross-artifact cite-cascade + §3 sections-preserved-verbatim. Sub-species **3.retirement-event-filing-arc** catalogued at v1.21 §"Adjacent observations" (e) — sixth sub-species refinement of species 3 (resolved-but-carry-stale-inherited) at workflow v1.9 §7.4.7.2; distinct from prior 5 species 3 sub-species. Sub-species set at species 3 now SIX in 4 consecutive arcs. ZERO contract / signature / AC / behavior change; ZERO cross-axis cascade verified via grep. Co-published with sibling CP plan v2.26 closure delta absorbing same closure event at v2.25 §0.8 (c) + filing footer. v1.20 + earlier PRESERVED VERBATIM. 2026-05-27. v1.20 prior — Workflow §4.1.2 Class-2 amendment landing the long-deferred `WorkflowManifestEntry.default_gate_level: GateLevel | None = None` field at NEW §6.1.Y per Reading A absorption of `.harness/class_1_fork_h_t_cp_19_default_gate_level_spec_extension.md` (operator-ratified 2026-05-27 Q1=A + Q2=apply-now + Q3=defer-layer-3-e2e). Closes the X-AL-3 silent-absorption gap at `harness-cp/src/harness_cp/workflow_driver.py:738` hardcoded `parent_gate_level=GateLevel.AUTO` documented at `workflow_driver_types.py:163-168` as "v1.7+ extension". The v1.6 §6 line 333 anti-extension invariant explicitly self-declared this exact amendment shape as the ratified channel ("v1.7+ extension to surface them via operator-authored `WorkflowManifestEntry` extension fields is a Workflow §4.1.2 Class-2 amendment to this contract") — v1.20 IS that amendment. Reading A optional shape preserves backward compatibility per Pydantic v2 Optional discipline across the 100+ existing test fixtures + manifest construction sites (ZERO downstream-consumer disruption per fork §2.1). Workflow_driver composition site reads `manifest_entry.default_gate_level if not None else GateLevel.AUTO` — None preserves v1.6 MVP; operator-supplied values flow through unchanged. Anti-extension invariant scope-narrowed: 4 deferred fields → 3 (parent_gate_level lifted; sandbox_tier + entry_hash + tenant_id preserved). Co-published with CP plan v2.24 → v2.25 U-CP-13 single-unit-body absorption + harness-cp impl (workflow_manifest_entry.py 11→12 fields + workflow_driver.py:738 composition site read + workflow_driver_types.py:163-168 docstring lift) + 3 NEW tests at test_workflow_manifest_entry.py + 1 test rename (`_eleven_fields` → `_twelve_fields`) + workspace `CLAUDE.md` row bumps + fork doc Status PROPOSING → ✅ APPLIED. 692/692 harness-cp tests pass + 1091/1091 harness-runtime tests pass + 4 skipped. ZERO contract change at any other contract; ZERO cross-axis cascade (intra-CP-axis only — no CXA/OD/AS/IS/ADR touch). H_T-CP-19 layer 1 (spec extension) + layer 2 (production binding) **APPLIED**; layer 3 (multi-deployment e2e) **DEFERRED** per Q3 ratification. PARTIAL → RETIRE-READY transit owed at batch-21 retirement event filing. v1.19 prior — `HITLEscalationBrief.fail_detail_hash` Optional widening 2026-05-25: NEW §25.2.Y canonical-reading amendment widening `HITLEscalationBrief.fail_detail_hash` from `str` (non-Optional) to `str \| None = None` (Optional, default None). Resolves CP spec v1.18 change-note adjacent defect (i) per operator routing decision option (α) parallel widening. Full structural symmetry achieved with `ValidatorResult.fail_detail_hash: str \| None` (already Optional at v1.10); completes the symmetry that v1.18 began for `fail_class`. Production composer at `hitl_gate_composer.py:985` drops sentinel `"0" * 64` and constructs `fail_detail_hash=None` directly. ZERO production reads of `brief.fail_detail_hash` verified empirically pre-amendment. ZERO cross-axis cascade — `HITLEscalationBrief` intra-CP-axis; `validator_namespace.py:220` declares its own field not a reader. Co-published with CP plan v2.23 + runtime plan v2.26 + harness-cp impl + harness-runtime impl + workspace `CLAUDE.md` row bumps + test docstring update. 3008 tests pass + 4 skipped. v1.18 prior — Reading A path 1 absorption of fork `class_1_fork_u_rt_94_webhook_delivery_composer_binding_chain_absence.md` 2026-05-24-25: NEW §25.2.X canonical-reading amendment widening `HITLEscalationBrief.fail_class` from `ValidatorFailClass` to `ValidatorFailClass | None = None` (Optional, default None). Parallel posture preservation with v1.10 §25.2 `ValidatorResult.fail_class: ValidatorFailClass | None`. Resolves 3rd spec/code divergence at impl(U-RT-93) HALT-on-discovery; `fail_detail_hash` parallel widening foreclosed at FM-2 per change-note adjacent defect (i) — RESOLVED at v1.19. ZERO cross-axis cascade. Co-published with runtime spec v1.26 + CP plan v2.22 + runtime plan v2.25 + harness-cp impl + harness-runtime impl. 1754 tests pass + 4 skipped. v1.17 prior — Reading A path 1 absorption 2026-05-24-25: NEW §6.5 sub-section authoring `StepEffectiveBinding.persona_tier: PersonaTier` field-set extension (7th field, position-end, required no-default) + `resolve_step_binding(manifest_entry, step_id, *, persona_tier: PersonaTier)` signature widening (NEW required kw-only param; backward-INcompatible per AUTHORIZED scope at adjacent defect (iv)). Closes fork doc §2 helper-unreachability finding. ZERO cross-axis cascade. Co-published with runtime spec v1.26 + CP spec v1.18 + CP plan v2.22 + runtime plan v2.25. v1.15 prior — Narrow-scope spec clarification 2026-05-24: NEW §19.1.1 sub-section authoring canonical 4-axis statement explicitly enumerating `{per_tool_gate_level, blast_radius_floor, per_mcp_server_trust_floor, persona_tier_floor}` per ADR-D5 v1.3 §1.3.2 + AS spec C-AS-03 + C-AS-10 §10.3 + C-AS-12. Explicit non-axis statement: `deployment_surface` is NOT a §19.1 D5-layer axis (belongs at §19.3 D2-layer sandbox composition only). Resolves FM-2 item (a) from Reading B close + CP plan v2.4 §0.8 row 1 carry. ZERO contract / signature / AC / behavior change at spec-side (canonical formula was always 4-axis with per_tool_gate_level; v1.15 makes per-axis cite explicit). Co-published with CP plan v2.19 → v2.20 (U-CP-43 GateLevelInput field-set conform: add per_tool_gate_level, drop deployment_surface; §0.8 row 1 → RESOLVED-as-plan-conformed-to-spec; row 2 MCP_TRUST mapping → PARTIAL-ADVANCE) + harness-cp impl + harness-runtime impl per (B2) plan-follows-spec operator disposition. 1715 tests pass + 3 skipped. v1.14 prior — Narrow-scope citation-correction patch 2026-05-24: amends canonical reading of v1.2-lineage §19.1 + §21.3 4 cite sites where "C10 five-tier framework" narrative → "C10 four-tier framework per AS spec C-AS-10 §10.3 (Level 0/1/2/3 4-level enumeration)". Resolves CP plan v2.8 §0.6 Class 3 informational doc-hygiene drift logged 2026-05-21+. AS §10.3 canonical 4-level enumeration unchanged; CP plan v2.8 §2.0c U-CP-00c 4-value `MCPTrustTier` carrier unchanged. ZERO contract / signature / field-set / AC / behavior change; ZERO cross-axis cascade. FM-2 item (b) from Reading B close. Bundled fix at site (iv) v1.2 line 1888 §21.3 row also corrects §10.1 → §10.3 cite-shape drift. v1.13 prior — Class 1 fork resolution Reading A apply pass per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §7 ratified 2026-05-24: v1.10 NEW §25 / C-CP-25 ValidatorFramework renamed to §28 / C-CP-28 per first-author primacy + v1.10's preservation-claim self-contradiction + F-derivative anchor proximity tiebreaker; v1.6-lineage §25 / C-CP-25 WorkflowDriver retains canonical-ID primacy; fidelity-pure citation-correction patch — ZERO contract change, ZERO signature change, ZERO acceptance-criterion change, ZERO behavior change, ZERO cross-axis cascade at semantics layer; cite-cascade APPLIED 2026-05-24 across runtime spec v1.18 → v1.19 (17 sites) + CP plan v2.18 → v2.19 (canonical-reading amendment for cluster 10-CP-A) + runtime plan v2.17 → v2.18 (canonical-reading amendment for L9-decies, 23 sites) + adjacent fork doc cite retag at lines 127 + 152; harness-cp/CLAUDE.md empirical-grep ZERO §25/C-CP-25 spec-section cite hits; CXA v2.9 ZERO §25/C-CP-25/ValidatorFramework cite hits. v1.12 prior — Class 1 fork resolution Path A absorption per `.harness/class_1_fork_step_execution_context_workflow_id_field_absence.md` ratified 2026-05-24: §25.2.1 `StepExecutionContext` Pydantic BaseModel gains 9th field `workflow_id: str` (was 8 at v1.6 baseline preserved verbatim through v1.11) sourced from `manifest_entry.workflow_id` at driver §25.3.3.4 composition site; resolves OD spec v1.10 §C-OD-26.6.1 step 2 cite `step_context.workflow_id` (implicit forward-cite at Sub-arc B publication arc `0919a9b`); ZERO contract removal; ZERO behavior change at existing consumers; ZERO cross-axis edge. v1.11 prior — Path γ enum identifier rename absorption: §26.2 `PauseReason` → `WorkflowPauseReason` 5-class workflow-layer pause taxonomy disambiguation per `.harness/class_1_fork_u_cp_63_pause_reason_collision.md`; NEW §26 NOTE making §22 ↔ §26 engine-layer vs workflow-layer coexistence explicit (2026-05-21); v1.10 NEW §25/§26/§27/§17.4 substantive content preserved verbatim)) |

## §2.4 plan pointer

| Axis | Plan | Unit count |
|---|---|---|
| CP | `Implementation_Plan_Control_Plane_v2_53.md` (**v2.53 — canonical HEAD**, 2026-08-13 the `B-71` CP-side execution authority — see the §1.2 archive cell below) |
| CP | `Implementation_Plan_Control_Plane_v2_52.md` (**v2.52**, 2026-08-12 the `B-141` plan-authority re-pin: two as-domain supersession notes (U-CP-41 #5 + U-CP-47-era `VALIDATOR_FAIL_NAMESPACE_SCHEMA`), zero unit-body edits, superseded AS DOMAIN by `ValidatorFailClass` per CP spec v1.116 (B-138 (a)); test-roster supersession `test_verifier_fail_class_in_cp_21_5_set` → `test_verifier_fail_class_in_validator_fail_class_domain`; clearance `implementation-plan-control-plane-v2-52-cleared-2026-08-12.md`. Prior v2.51, 2026-08-11 the `B-153` U-CP-54 acceptance re-pin: criterion #2 hitl.* 4→11 attrs per C-CP-20 §20.6 distinct declared keys; criterion #6 total 68→75 (42+29+4); test roster → test_total_attribute_count_seventy_five; exact sibling of the v2.50 B-144 re-pin; clearance `implementation-plan-control-plane-v2-51-cleared-2026-08-11.md`) |
| CP | `Implementation_Plan_Control_Plane_v2_50.md` (**v2.50**, 2026-08-11 the `B-144` plan-authority re-pin absorbing CP spec v1.117: U-CP-54 criterion #3 `retry.*` 4→6 (C-CP-03 §3.5 v1.3) + `harness.breaker.*` 7→9 (OD C-OD-07 §7.1 at OD v1.32), criterion #2 `engine.*` 3→4 (C-CP-09 §9.1 v1.3), criterion #6 total 63→68 (35+29+4), test roster `test_total_attribute_count_sixty_three` → `test_total_attribute_count_sixty_eight`; everything else U-CP-54 and all other units PRESERVED VERBATIM; hitl.* row deliberately untouched (`B-153`); surfaced by out-of-family round 1 at PR #1311; clearance `implementation-plan-control-plane-v2-50-cleared-2026-08-11.md`. Prior v2.49, 2026-08-03 the ratified `B-107` A-hybrid plan leg — U-CP-64 amended with ONE ADDED AC **#A12**: the eight-cell witness grid (map + scalar channels × LINEAR / PARALLELIZATION branch / ORCHESTRATOR_WORKERS worker / ORCHESTRATOR-own; keyed siblings in BOTH carrier orders; orchestrator-own cells constructed snapshots), the scalar-removal witnesses (b80 flip + direct empty scalar-parameter no-directive case), the PD-8 validate-copy-mutation probes, `model_construct` forged-context inertness, and the binding-rule reader inventory (seven resolver call sites + two non-test terminal key-match consumers at this revision); #A1–#A11 + the dependency graph PRESERVED; clearance `implementation-plan-control-plane-v2-49-cleared-2026-08-03.md`; predecessor v2.48 — 2026-07-31 the `B-100` spec-leg plan absorption — **U-CP-64 amended** with ONE ADDED acceptance criterion (**#A11**) requiring the KEY-ABSENT sibling source shapes for all three effect-fence carriers, witnessed by detect-then-refuse in BOTH directions (a key-absent shape cannot be constructed carrying a key; a key-bearing one cannot be constructed without one). **#A1–#A10 PRESERVED VERBATIM** — #A4's crash-reconstruction clause stays true as written and becomes a special case of the general rule. ZERO new units / clusters / DAG edges (the U-RT-148 → U-CP-64 edge already exists); CXA v2.23 UNCHANGED, aggregate frozen at 111. #A11 carries TWO explicit witness-shape constraints: the orchestrator case MUST be a constructed-snapshot test (no production path exists), the LINEAR case IS reachable and must not be downgraded to it. Clearance `implementation-plan-control-plane-v2-48-cleared-2026-07-31.md`. Prior `Implementation_Plan_Control_Plane_v2_47.md` (v2.47 — 2026-07-30 the `B-69` spec-leg plan absorption — **U-CP-64 amended** for both CP-owned surfaces with TEN added ACs (#A1–#A10); ZERO new units; ZERO within-CP DAG change, though the arc adds cross-axis edges INTO U-CP-64 from Runtime's U-RT-148; a deferred coverage-matrix row is deliberately NOT the instrument here because **this surface has an owner**. Clearance `implementation-plan-control-plane-v2-47-cleared-2026-07-30.md`. **Pointer-lag note: this row was stale at v2.44 — v2.45 / v2.46 landed without refreshing it.** Prior:) `Implementation_Plan_Control_Plane_v2_44.md` (v2.44 — 2026-07-25 `B-72` item (1) spec-leg plan absorption — ONE deferred coverage-matrix row for CP spec v1.108 §1's property 6, explicitly naming the new-carrier/counting side, the tree-wide-uniqueness + collision-witness requirement, AND the delivery-cell-construction side at the fan-out dispatch sites (out-of-family `just codex-review` caught two [P1]s across both rounds — a first draft omitted delivery-cell construction, a second draft's identity scheme lacked tree-wide uniqueness — both fixed before merge); ZERO unit amendment; clearance `implementation-plan-control-plane-v2-44-cleared-2026-07-25.md`. Prior `Implementation_Plan_Control_Plane_v2_43.md` (2026-07-24 `B-70` spec-leg plan absorption — ONE deferred coverage-matrix row for CP spec v1.107 §1's multi-branch effect-fence-resolution fallback-safety invariant; ZERO unit amendment (the resolver that will enforce it does not yet exist, mirroring how v2.42 §5 deferred the parallel `hitl_responses` property 4/5 invariants); ZERO new units, 102 units; clearance `implementation-plan-control-plane-v2-43-cleared-2026-07-24.md`; prior `Implementation_Plan_Control_Plane_v2_42.md` (2026-07-23 B-39 spec-leg plan absorption — U-CP-64 amended for `ResumeContext.hitl_responses`/`hitl_response_for` + the CONTRACT-only `DriverContext.resume_context_holder` retirement; clearance `implementation-plan-control-plane-v2-42-cleared-2026-07-23.md`); prior `Implementation_Plan_Control_Plane_v2_41.md` (2026-07-23 B-33 spec+plan leg — U-CP-45 amended (`verify_rotation_6_steps` extension) + U-CP-44 amended (physical-key-distinctness comparator, REQUIRED not skippable); clearance `implementation-plan-control-plane-v2-41-cleared-2026-07-23.md`; prior `Implementation_Plan_Control_Plane_v2_40.md` (2026-07-22 B-65-A apply — U-CP-85 amended; clearance `implementation-plan-control-plane-v2-40-cleared-2026-07-22.md`; prior v2.39 preserved)) | 102 units (U-CP-00, U-CP-00b, U-CP-00c, U-CP-01 – U-CP-101); **v2.39 — 2026-07-19 B-48 apply plan delta (U-CP-82/85/86/88/89 amended + NEW U-CP-101 capacity-authority declaration home; clearance `implementation-plan-control-plane-v2-39-cleared-2026-07-19.md`). Prior v2.38 — 2026-07-18 B-51/B-52/B-54 arc plan delta (U-CP-72/73/44/45/42 amended for the CP v1.101 rider; ZERO new units; clearance `implementation-plan-control-plane-v2-38-cleared-2026-07-18.md`). Prior v2.37 — R-FS-1 R-plan-1 2026-06-16: 2 NEW units — U-CP-99 (R-routing contract types — `RouterResolutionFn` + `RouterResolution` + the `ProviderDispatchFn` Protocol-ization with the additive optional `binding_rationale` kwarg) + U-CP-100 (the `infer()` Layer-3 router-resolution branch — optional `router` param + sentinel→router branch + L3-budget timeout wrap + 4-field trace rebuild + the 4 no-regress preserved-raise paths); impl-against-cleared-spec post-CP-v1.36 §2.5 (Reading B); the `ProviderDispatchFn` Protocol-ization is a cross-package TYPE-RIPPLE CO-LAND with runtime U-RT-132 (verified by a pyright spike) → all 4 R units land in one R-impl-1 arc; ZERO new contract ID, X-AL-3-clean. Co-published runtime plan v2.48 (U-RT-132/133). Clearance marker filed.** Prior v2.36 — R-FS-1 B2-plan 2026-06-16: 1 NEW unit — U-CP-98 (`gate_level()` 4th-axis composition: `MCP_TRUST_GATE_LEVEL_FLOOR` Table A + `Axis.MCP_TRUST` into `per_axis_floors`, 3-of-4 → 4-of-4 §19.1 axes; impl-against-cleared-spec post-CP-v1.35 §19.1.2; HARMFUL-if-alone → co-land pin with runtime U-RT-131 at B2-impl-3; ZERO new contract ID, X-AL-3-clean). Co-published runtime plan v2.47 (U-RT-125..131). Clearance marker filed.** Prior v2.35 — R-FS-1 E-plan-3 2026-06-15: 2 NEW units — U-CP-96 (RECONCILER_LOOP engine materialization, impl-against-cleared-spec post-v1_33 §7.4 deferral) + U-CP-97 (RECONCILER_LOOP engine-layer recovery-loop firing branch → C-CP-49/50). v2.34 — R-FS-1 E-plan 2026-06-15: 3 NEW units — U-CP-93 (EVENT_SOURCED_REPLAY engine materialization, impl-against-cleared-spec) + U-CP-94 (WAL_SEGMENT materialization, CP/IS-only — the Path-(i) `test_u_rt_95` durable e2e is U-RT-122) + U-CP-95 (WAL_SEGMENT engine-layer recovery-loop firing branch → C-CP-49/50 → R-CXA-2 go-live) + open-item O-CP-4 (R-CXA-2 owned by E-2, recorded-not-gated). Co-published runtime plan v2.45 (U-RT-121/122). ZERO spec amendment; ZERO new contract ID; X-AL-3-clean. Clearance marker filed. v2.33 — R-FS-1 arc #20 (B3-plan) 2026-06-14: 2 NEW units — U-CP-91 (`GateLevelInput` floor-override carrier-shape, F-B3-1 plan-carrier; explicit override field/arg, NOT the foreclosed enum-substitution path) + U-CP-92 (`TimeoutDegradationKind` vocab-B→vocab-A reconciliation + fail-open config-guard, F-B3-2) + open-item O-CP-3 (G2c `per_tool_gate_level` producer — REGISTERED, owes a skipped AS-leg gate). Co-published runtime plan v2.44 (U-RT-115..120). ZERO spec amendment; ZERO new contract ID. Clearance marker filed. v2.32 — R-FS-1 arc #6 (B1-plan) 2026-06-13: 11 NEW units (U-CP-80..U-CP-90) decomposing CP spec v1.32 §25.10–§25.21 (the C-CP-25 WorkflowDriver non-linear-topology extension) — U-CP-80 driver-strategy dispatch table, U-CP-81 branch `StepExecutionContext` composition (causality + `AgentRole` field), U-CP-82 buffered/deferred-append drain, U-CP-83 branch-scoped idempotency-key, U-CP-84 `branch_metadata.terminal_status` write-cadence (tri-spec cross-cutting), U-CP-85 `cascade_policy` + cascade-cancel (8 obligations), U-CP-86..90 the 5 strategies (PARALLELIZATION/EVALUATOR_OPTIMIZER/ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION/DECENTRALIZED_HANDOFF). This delta is the B1-arc **aggregate cross-axis dependency-graph home** (14 nodes: 11 CP + U-RT-113/114 + U-IS-19; acyclic; cross-axis edges U-CP-84→U-IS-19 + U-RT-114→U-CP-81 downstream; IS 0-outbound cycle guard). ZERO spec amendment; ZERO new contract ID. Co-published with runtime plan v2.43 + IS plan v2.6. Clearance marker filed. v2.31 prior — Single-arc canonical-reading amendment at U-CP-74..U-CP-79 absorbing CP spec v1.29 → v1.30 §1.1–§1.6 canonical-reading collapse of the workflow/engine composer signature split per `.harness/class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` Reading (C) operator-ratified 2026-05-31 (Q1=C / Q2=v1.30 amendment / Q3=ZERO cross-axis cascade / Q4=PR-2+PR-3 collapse single PR). Each composer atomic unit gains a uniform kw-only-required `procedural_tier_snapshot_resolver: Callable[[], Identifier]` param at canonical-reading layer + 2 NEW acceptance criteria (AC v2.31-NEW-α resolver invocation populating `EntryPayload.procedural_tier_snapshot_ref` + AC v2.31-NEW-β HALT-on-resolver-failure per CP spec v1.30 §1.5). U-CP-78 + U-CP-79 (engine-layer) preserve the v1.29 §16.5.12.3 signature shape verbatim; U-CP-74 + U-CP-75 + U-CP-76 + U-CP-77 (4 workflow-layer + 1 class method) catch up to the same shape at v2.31. v2.30 unit bodies + DAG + coverage matrix PRESERVED VERBATIM per delta-only-plan-chain convention; v2.31 §1.1 + §1.2 canonical-reading layer applies signature extension + AC additions additively. ZERO new atomic units; ZERO removed units; ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade. PR-stack collapse per fork doc §5 Q4 — all 6 composers ship at the uniform shape in a single PR. Co-published with CP spec v1.30 + impl arc at 6 composer signatures + 6 EntryPayload sidecar populations + `RuntimeCpIsWiring.procedural_tier_snapshot_resolver` field + 6 wiring methods threading resolver + `materialize_cp_is_wiring_stage(...)` factory + bootstrap stage 6 `make_procedural_tier_snapshot_resolver(ctx)` binding + 11 NEW v1.30 sanity tests at `harness-cp/tests/test_procedural_tier_resolver_v1_30_apply.py` + 8 sibling test-module adaptations + 2 stale-carry refreshes (harness-as secret-fetch-audit + harness-cp sibling-ledger composition from PR #89 IS spec v1.3 absorption) + clearance markers at `.harness/clearance/Spec_Control_Plane-v1_30-cleared-2026-05-31.md` + `.harness/clearance/Implementation_Plan_Control_Plane-v2_31-cleared-2026-05-31.md` + fork-doc Status PROPOSING → ✅ APPLIED-AS-READING-C + workspace `CLAUDE.md` row bumps. 3533/3533 tests pass + 10 skipped (was 3522/10 pre-arc; +11 NEW v1.30 sanity tests). v2.30 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-31. v2.30 prior — Surgical single-unit-body amendment at U-CP-74 absorbing CP spec v1.26 → v1.27 §16.5.4 row U-CP-14 Reading A resolution per `.harness/class_1_tension_u_cp_14_dual_emission_stubs_and_disambiguator_semantics_gap.md` operator-ratified 2026-05-29 Q-set (Q1=A drop override_id + policy_id + collapse formula to 3-tuple; Q2=iii audit-stub IN-SCOPE-BUT-MARK-DEFERRED; Q3=i accept v1.25 + v1.26 formula re-ratification; Q5=i bundled apply-pass; Q6=α intra-CP only). U-CP-74 AC #2 formula 5-tuple `(workflow_id, step_id, override_id, policy_id, outcome_hash)` → 3-tuple `(workflow_id, step_id, outcome_hash)` per Reading A apply. U-CP-74 Signatures: `emit_override_state_ledger_entry` + `_override_idempotency_key` drop `override_id: str` + `policy_id: str` parameters; `emit_override_audit_entry` signature PRESERVED VERBATIM per Q2=iii (audit-stub structural surface unchanged; functional remediation deferred). NEW test name `test_emit_override_audit_entry_is_known_functional_stub_per_v1_27_annotation` documenting Q2=iii audit-half stub annotation context at the plan body. AC #1 + #3 + #4 + #5 + #6 + #7 + #8 + #9 PRESERVED VERBATIM from v2.29. U-CP-75 / U-CP-76 / U-CP-77 / U-CP-78 / U-CP-79 unit bodies PRESERVED VERBATIM at v2.30 (v1.27 amendment intra-row-U-CP-14 only). ZERO new units; ZERO DAG topology change; ZERO coverage matrix change; ZERO cross-axis cascade per Q6=α. Unit count 80 unchanged. Co-published with CP spec v1.27 (NEW delta file) + harness-cp impl (`per_step_override_evaluator.py` composer + helper signature trim) + harness-runtime impl (`lifecycle/cp_is_wiring.py` wiring-layer signature trim) + harness-cp tests + harness-runtime tests (2090 / 10 skipped passing; 0 pyright errors at touched files) + workspace `CLAUDE.md` row bumps + fork doc Status PROPOSING → ✅ APPLIED-AS-READING-A + clearance marker. Workspace pattern at v2.30: `[[strike-revision-on-refined-second-tier-reason]]` at plan layer — v2.28 + v2.29 AC #2 5-tuple formula STRIKE preserved on refined second-tier reason (no semantic source for override_id + policy_id) rather than un-STRUCK at original framing (multi-version policy roadmap); mirror plan-layer precedent at runtime plan v2.39 AC #4 STRIKE refinement per Workflow v1.13 §7.4.7.2 species 2 sub-species catalogue. H_T-RT-35 transit posture UNCHANGED at PARTIAL — this arc closes 1 of 5 upstream blockers at the FORK-DOC-FILED → APPLIED transit. v2.29 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-29. v2.29 prior — Surgical cascade at v2.28 §2 U-CP-74..79 unit bodies absorbing CP spec v1.26 β.i amendment (commit `ec4a2f7`) per nested fork `class_1_tension_u_cp_74_entrypayload_field_set_drift.md` operator-ratified 2026-05-29 Q-set (A + β.i + Q-β.i-1(a) + Q-β.i-3(b)). Per-unit AC #2 (idempotency_key formula) appends `|| sha256(outcome_canonical_bytes).hex()` suffix per row preserving v2.28 disambiguator segments verbatim. Per-unit AC #3 (was 'response_hash recipe' at U-CP-74; AC #4 at U-CP-75..79 for analogous 'response_hash over X canonical bytes') reframes: `response_hash` is IS-internal per C-IS-06 §6.2; composer does NOT control it; outcome-bytes consumed by AC #2 idempotency_key derivation per CP spec v1.26 §16.5.4 NOT by `response_hash`. Per-unit Implements citation refreshed to spec v1.26. Per-unit Tests-list rename at the response_hash test entry (splits into `idempotency_key_includes_outcome_hash_suffix` + `response_hash_is_is_computed_not_composer_controlled` per unit). All other unit-body fields PRESERVED VERBATIM (Files + Signatures outer surface + Depends on + untouched ACs including U-CP-74 AC #8 `_canonicalize_outcome_bytes` helper acceptance + untouched test names). ZERO DAG topology change. ZERO coverage matrix change. ZERO cross-axis cascade (IS / OD / AS / runtime plan / CXA all unchanged). ZERO new units / ZERO removed units / ZERO outer signature shape change. Unit count 80 unchanged. Co-published with CP spec v1.26 (commit `ec4a2f7`) + nested fork doc + parent fork doc closure-back-references + PR #38 routing-doc. v2.28 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-29. v2.28 prior — 6 NEW atomic units U-CP-74 through U-CP-79 authoring per CP spec v1.25 NEW §16.5 CP→IS state-ledger emission contract per `.harness/architect_recommendation_u_rt_35_gap_b_within_path_a.md` operator-ratified 2026-05-28 Q-set + impl-time grounding pass ratification (PR #37 in-flight revision). Unit-count 74 → 80 (+6); +6 NEW composer units at L4-within-axis singleton-extensions; ZERO new cluster; DAG verified Kahn-acyclic. U-CP-74 (`emit_override_state_ledger_entry` sibling to existing `emit_override_audit_entry` at `per_step_override_evaluator.py:200` per U-CP-14 + co-publishes NEW shared `_canonicalize_outcome_bytes` helper at `state_ledger_canonicalization.py`); U-CP-75 (`emit_workload_class_selection_state_ledger_entry` against `select_engine_class(...)` at `workload_binding_engine_class_selection.py:142` per U-CP-27); U-CP-76 (`emit_pause_resume_state_ledger_entry` against `PauseResumeProtocol` class methods at `pause_resume_protocol.py:214+` per U-CP-30; workflow-layer per CP spec v1.11 §26 NEW NOTE 2-layer coexistence); U-CP-77 (`emit_hitl_tool_call_rewriting_state_ledger_entry` against `rewrite_tool_call_to_hitl(...)` at `hitl_as_tool_call_rewriting.py:149` per U-CP-37); U-CP-78 (`emit_pause_captured_state_ledger_entry` against engine-layer free function `capture_pause_snapshot(...)` at `pause_resume_protocol.py:106` per U-CP-49); U-CP-79 (`emit_resume_attempted_state_ledger_entry` against engine-layer free function `attempt_resume(...)` at `pause_resume_protocol.py:128` per U-CP-50). **2 NOT-APPLICABLE reclassifications per CP spec v1.25 §16.5.10:** U-CP-12 (declarative-only `per_class_attribute_composition.py` — static `PER_CLASS_ATTRIBUTE_SETS` tuple per C-CP-05 §5.2; ZERO atomic unit owed); U-CP-52 (runtime-axis-composed `hitl_placement.py` — canonical §17.4 `hitl_gate(...)` signature raises NotImplementedError; production gate body at runtime composer per C-RT-18 §14.8; future runtime-plan revision authors `emit_hitl_gate_state_ledger_entry` at runtime composer site). Each unit: full body template per CP plan v2.15 §2 pattern (Implements + Files + Signatures + Depends on + ACs + Tests); 5-10 ACs per unit; ~5-10 tests per unit. ZERO cross-axis cascade at this arc. Mirror precedent: U-CP-34 `sibling_ledger_entry_composition` Cluster 7 closure 2026-05-21 (single LANDED §12.3 edge at U-RT-35 AC #1). Co-published with CP spec v1.25 NEW §16.5 + CXA v2.16 §0.4 forward-tracking marker (6 PENDING + 2 NOT-APPLICABLE rows) + fork-doc closure-back-reference annotation. H_T-RT-35 PARTIAL → RETIRE-READY transit GATED on full CP-materializable 6-edge wired (1 LANDED + 6 PENDING per-CP-unit landings + runtime-plan revision) + Gap C canonical-vs-materialized differential resolution at runtime spec revision pass. 2026-05-28. v2.27 prior — v2.27 prior — v2.27 — NEW singleton-extension unit **U-CP-73** at L4-within-Cluster-10 ValidatorFramework substrate decomposing CP spec v1.24 NEW §28.10 `ValidatorPostEvaluateHook` Protocol surface per `.harness/class_1_fork_u_od_40_validator_post_evaluate_hook.md` Reading (B) operator-ratified 2026-05-28 Q-set. Unit count 73 → 74 (+1 NEW); +9 NEW ACs covering Protocol declaration + ctor extension + firing site + best-effort swallow + elapsed-time measurement + 6 invariants enforcement + 12 test names. Cluster placement: singleton-extension under operator-opt-in hook Protocol pattern (mirror runtime plan v2.20 L9-undecies + v2.28 L9-quindecies singleton-extension precedent). ZERO cross-axis cascade per Q5=β ratification (intra-axis CP Protocol; intra-axis runtime composition; NO new CXA edge; convention-seam at CXA §0.4 NOT amended per FM-2). ZERO change to existing C-CP-28 §28.1-§28.9 (preserved verbatim); ZERO breaking change at 6 existing ConcreteValidatorFramework(...) sites; ZERO change to SyncValidatorFrameworkFacade. DAG verified Kahn-acyclic at insertion. Co-published with CP spec v1.24 + harness-cp impl (13 NEW unit tests; 721/721 harness-cp tests pass) + harness-runtime impl (2 NEW cost-helper modules + factory binding + composer inline-wrap + 18 NEW unit tests + integration test) + runtime plan v2.29 U-RT-84 single-unit-body amendment +AC #6 + OD plan v2.23 U-OD-40 LANDED + batch-28 retirement event filing H_T-OD-5 PARTIAL → RETIRE-READY (surface coverage 2/4 → 4/4 = 100% wired). v2.26 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-28. v2.26 prior — Fidelity-pure citation-correction patch closing v2.25 §0.8 (c) batch-21 filing carry + filing footer "H_T-CP-19 status" line final clause — both as **CLOSED-as-filed-at-batch-21** 2026-05-27. Sibling closure delta to CP spec v1.20 → v1.21 absorbing the same closure event (batch-21 retirement event filing at commit `c5582b6` merged at `79865d6`). NEW §1 + §2 + §3; sub-species 3.retirement-event-filing-arc catalogued at sibling CP spec v1.21. ZERO contract / signature / AC / DAG / coverage matrix change. Unit count 73 unchanged. v2.25 + earlier PRESERVED VERBATIM. 2026-05-27. v2.25 prior — Single-unit-body amendment at U-CP-13 absorbing CP spec v1.19 → v1.20 NEW §6.1.Y `WorkflowManifestEntry.default_gate_level: GateLevel | None = None` field per Reading A absorption of `.harness/class_1_fork_h_t_cp_19_default_gate_level_spec_extension.md` (operator-ratified 2026-05-27 Q1=A + Q2=apply-now + Q3=defer-layer-3-e2e). Canonical-reading amendment at delta-only-plan-chain layer: U-CP-13 field-set 11 → 12 (NEW `default_gate_level` at position-end); AC #1 11-field assertion → 12-field assertion; Tests-line rename `_eleven_fields` → `_twelve_fields` + 3 NEW test names; Signatures line adds `default_gate_level: GateLevel | None = None` at position-end; NEW intra-axis import `from harness_cp.gate_level_rule import GateLevel` at `workflow_manifest_entry.py`. Net AC count +0 (AC #1 text amended; +3 tests are coverage extensions, not new ACs). Unit count 73→73; ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO coverage matrix structural delta; ZERO cross-axis cascade (intra-CP-axis only). Co-published with CP spec v1.20 + harness-cp impl + 1091/1091 harness-runtime tests pass + workspace `CLAUDE.md` row bumps + fork doc Status PROPOSING → ✅ APPLIED. H_T-CP-19 plan-side absorption **APPLIED at v2.25**; production binding **APPLIED at v2.25 co-publication**; PARTIAL → RETIRE-READY transit owed at batch-21 retirement event filing. 2026-05-27. v2.24 prior — Fidelity-pure citation-correction patch closing v2.23 §"Adjacent observations" finding (i) `HITLEscalationBrief.fail_detail_hash` field-type follow-on consideration — as **CLOSED-at-self-version-v2.23** (per CP spec v1.19 §25.2.Y absorption at v2.23 U-CP-59 amendment) — AND finding (v) workspace CLAUDE.md §2.4 CP plan row stale — as **CLOSED-at-workspace-CLAUDE.md-current-state** (per v2.23 row bump). Both v2.23 carries became stale at v2.23 own publication: (i) inherited from v2.22 with "if (α) chosen at follow-on arc" framing but v2.23 IS the arc that executed (α); (v) inherited with "row cites v2.20" framing but workspace CLAUDE.md was already bumped at v2.23 publication. THIRD production application of workflow v1.9 §7.4.7.3 across workspace carry-set sweep 2026-05-27 (sweep cohort: 4 of 4 closure deltas; SWEEP BATCH COMPLETE; siblings OD spec v1.22 + CXA v2.13 + OD plan v2.22). Species 4 (authoring-time stale carry) at workflow v1.9 §7.4.7.2. ZERO contract / signature / AC / DAG change. v2.23 + earlier PRESERVED VERBATIM. 2026-05-27. v2.23 prior — Single-unit-body amendment at U-CP-59 absorbing CP spec v1.18 → v1.19 NEW §25.2.Y `HITLEscalationBrief.fail_detail_hash` Optional widening (option (α) parallel widening per operator routing decision 2026-05-25). Net AC count +1; unit count 73→73; ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO cross-axis cascade. Co-published with CP spec v1.19 + runtime plan v2.26 + harness-cp impl + harness-runtime impl + workspace `CLAUDE.md` row bumps. 3008 tests pass + 4 skipped. v2.22 prior — Reading A path 1 absorption 2026-05-24-25 per fork `class_1_fork_u_rt_94_webhook_delivery_composer_binding_chain_absence.md`: two single-unit-body amendments — U-CP-14 absorbing CP spec v1.17 §6.5 (StepEffectiveBinding +persona_tier field + resolve_step_binding signature widening + 2 NEW ACs + 3 NEW test names) + U-CP-59 absorbing CP spec v1.18 §25.2.X (HITLEscalationBrief.fail_class Optional widening + 2 NEW ACs + 3 NEW test names). Empirical-verification at canonical plan body resolved over-broad spec hedges to exact amendment sites (U-CP-14 NOT U-CP-13; U-CP-59 NOT U-CP-58/59/60/61) per FM-2. Net AC count +4; unit count 73→73; ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO cross-axis cascade. Co-published with CP spec v1.17 + v1.18 + runtime spec v1.26 + runtime plan v2.25 + harness-cp impl + harness-runtime impl. v2.21 prior — U-CP-64 plan-body amendment (ResumeContext carrier + attempt_resume signature widening) per CP spec v1.16 §26.8. v2.20 prior — Plan-follows-spec absorption pass 2026-05-24 per CP spec v1.14 → v1.15 NEW §19.1.1 canonical 4-axis statement publication (this session) + operator (B2) disposition. U-CP-43 `GateLevelInput` field-set canonical-reading amendment at delta-only-plan-chain layer: ADD `per_tool_gate_level: GateLevel` field per CP spec v1.15 §19.1.1 (i) degenerate axis; DROP `deployment_surface: DeploymentSurface` field per §19.1.1 (v) non-axis statement; RETIRE `DEPLOYMENT_SURFACE_GATE_LEVEL_FLOOR` constant; `MCP_TRUST_GATE_LEVEL_FLOOR` preserved verbatim with §0.8 row 2 PARTIAL-ADVANCE pointer (mapping spec-silent at CP §19.1 + AS §10.3; owed at separate follow-on spec-extension arc). §0.8 row 1 (U-CP-43 4-axis input-set divergence) → RESOLVED-as-plan-conformed-to-spec at v2.20. acc #6/#9 STRUCK + REPLACED with v2.20-NEW per_tool_gate_level + field-set conformance acceptance criteria. Tests: 4 new gate_level + composer tests authored; DEPLOYMENT_SURFACE tests retired; MCP_TRUST mapping tests preserved deferred. ZERO new units; ZERO DAG topology change; ZERO coverage matrix structural change. Co-published with harness-cp impl (`gate_level_rule.py` + `five_axis_composition.py`) + harness-runtime impl (`hitl_gate_composer.py`). 1715 tests pass + 3 skipped. FM-2 item (a) from Reading B close. 2026-05-24. v2.19 prior — Cite-cascade absorption per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24): §0(i) adjacent-defect note refreshed from "non-blocking; routing to future arc" to RESOLVED at CP spec v1.13; NEW §1 cluster 10-CP-A canonical-reading amendment table for U-CP-58/59/60/61 (cite-cascade absorbed without per-unit-body edit per delta-only convention; v2.15/v2.16 plan files preserved byte-exact). U-CP-56 (StepExecutionContext schema; v1.6-lineage WorkflowDriver at §25.2.1) preserved verbatim — the v2.18 9th-field amendment cite at §25.2.1 is the v1.6-lineage authoring site and retains canonical-ID primacy. ZERO contract / signature / AC / DAG change. 2026-05-24. v2.18 prior — Companion plan revision to CP spec v1.11 → v1.12 NEW §25.2.1 9th-field `workflow_id` per Class 1 fork resolution Path A absorption at `.harness/class_1_fork_step_execution_context_workflow_id_field_absence.md` ratified 2026-05-24. Single-unit-body amendment at U-CP-56 (StepExecutionContext: 8 → 9 fields; AC #1 + AC #6 text updates; Signatures line "8-field" → "9-field"; Files line + Depends-on edges preserved verbatim). ZERO unit re-decomposition; ZERO new units; ZERO DAG topology change; ZERO coverage matrix change. v2.17 prior — Path γ citation absorption: U-CP-62 carrier `PauseReason` → `WorkflowPauseReason` rename + U-CP-63 AC #4 STRUCK + U-CP-63/64 Files-column note amended per CP spec v1.11 §26 NEW NOTE coexistence per `.harness/class_1_fork_u_cp_63_pause_reason_collision.md` (2026-05-21)) |

## §2.3 relocated dated change-note block, C3 (2026-07-13 .. 2026-07-25)

*Routed here — CP-plurality (23 `CP`-shaped mentions vs Runtime's 20, OD's 7, IS's 2, CXA's 1 in this block) — a Class 3 routing judgment call, not a byte-preservation claim of exclusivity; the block is genuinely multi-axis, relocated whole (never split mid-block) to avoid narrative fragmentation.*

**C3 dated change-note narrative block (2026-07-13 .. 2026-07-25)** — relocated verbatim from root `CLAUDE.md` §2.3:

 *(2026-07-13: CP v1.96→v1.97 — `B-21` — `PeerFanOutResumeState.paused_child_branches` (NEW additive field, reuses v1.45's `PausedChildBranchResumeState` carrier verbatim) extends the v1.45 cross-recursion-boundary child-pause mechanism to `PARALLELIZATION`; NO new carrier/exception/runtime change; byte-compat scoped per-carrier-kind so v1.45's already-shipped `FanOutResumeState.paused_child_branches` nested hash behavior is unchanged; clearance at `.harness/clearance/spec-control-plane-v1-97-cleared-2026-07-13.md`. CP v1.97→v1.98 — `B-22` — NEW §20.2.1 `SigningBackend` Protocol + optional `backend` keyword param on `sign_audit_entry`/`verify_audit_entry_signature` (composition-root injection seam; absent → the existing fail-loud raise PRESERVED VERBATIM; concrete prod-tech backend selection remains DEFERRED per ADR-F5); clearance at `.harness/clearance/spec-control-plane-v1-98-cleared-2026-07-14.md`. CP v1.98→v1.99 — `B-31` — `PausedChildBranchResumeState.child_workflow_id` (NEW additive `str | None` field) + a matching resume-guard comparison at both cross-recursion-boundary closures, closing the identity gap v1.97's own change-note registered as forward work (a same-`step_id`/same-`step_kind` edit swapping the target child workflow previously passed undetected); NO new carrier/exception/runtime change; byte-compat scoped drop-when-`None`, mirroring v1.97's own scoping; clearance at `.harness/clearance/spec-control-plane-v1-99-cleared-2026-07-15.md`. OD v1.31→v1.32 — C-OD-07 §7.1 ADDITIVE `harness.breaker.cause` + `harness.breaker.cooldown_ms` [7→9 attributes; R-FS-2 Wave 4 B-19-BREAKER-AMBIENT-ATTRS; `cause` populated since `B-38`/PR #1020 for rate_limit/auth_failure/5xx_streak (capability_shortfall remains a forward slot); v1.32 §7.1 prose corrected in-place 2026-07-18 per `B-43` — see `.harness/b19-breaker-ambient-attrs-redundancy-analysis.md` for the authoring-era grounding]. Version refresh 2026-07-09: CP v1.38→v1.85, OD v1.28→v1.30, Runtime v1.52→v1.92 caught up after the R-FS-1 + memory-substrate arcs, then Runtime v1.92→v1.93 = F-B cite-hygiene (phantom `C-IS-11` correction), v1.93→v1.94 = U-1 slice-1 tools-block `cache_control` breakpoint, v1.94→v1.95 = U-1 slice-2 full §1.5 `[tools+system]` breakpoint (OQ-1 resolved; B-18); **2026-07-10: v1.95→v1.96 = U-1 slice-3a child-scoped downgraded `frozen_tool_superset` (ADR-D4 §1.5 REMOVE half; closes the F1 latent C10 condition-2 gap) + paired CP v1.85→v1.86 = the `StepExecutionContext.sub_agent_descent` descent carrier**; **2026-07-12 pointer-staleness catch-up (R-600 cadence-5 adjacent finding, not itself a spec amendment): AS v1.10→v1.13 (v1.11 per-tool sandbox-tier discriminators; v1.12 `idempotent` effect-fence exemption; v1.13 §13.7 `search_tools` capability-discovery), CP v1.86→v1.96 (the B-18-3C-PREWARM family: PREWARM v1.87 / COHORTKEY v1.88 / DEFAULT-ON v1.89 / CASCADE v1.90 / TIMEOUT-LEDGER v1.91 / FENCE-LEDGER-FIDELITY v1.92 / -OW v1.93 / RECONSTRUCT-RESIDUAL v1.94 / EPOCH-PARTITION v1.95 / PREWARM-OW v1.96), OD v1.30→v1.31 (§24.7 `audit.rotation_correlation_id` port, B-AUDIT-KEY-ROTATION-RUNTIME), Runtime v1.96→v1.99 (v1.97 cacheable-epoch ttl-selection / v1.98 cache-ttl observability fix / v1.99 B-18-KEEPALIVE boot pre-warm + daemon keep-alive) — none of these were caught at their own landing PRs; this catch-up is version-label-only, no content re-derivation**; the delta-only chains mean a `§`-cite may name an older last-substantive-definition version — verify byte-exact per §10.4. **2026-07-18: the RATIFIED B-51/B-52/B-54(+B-53) apply arc** — OD v1.33→v1.34 (AMENDED §21.2.1 tenant fifth segment + NEW §21.2.2 verification API + NEW §21.2.3 fail-closed policy) + CP v1.100→v1.101 (all-four-sections rider) + Runtime v1.100→v1.101 (flag carrier, prewarm/keepalive disable, §13.5 verifier inputs, B-53 §13.4 row), one bundle per the fork's gate item 10; clearance markers at `.harness/clearance/*-2026-07-18.md`. **2026-07-19: the RATIFIED B-48 apply arc** — Runtime v1.101→v1.102 (C-RT-03 cap field + NEW §14.8.10 executor contract) + CP v1.101→v1.102 (§25.11 capacity gating + formal delegation + B-39 interim constraint) + IS v1.10→v1.11 (C-IS-07 §7.6 writer-owned drain-path timestamps); markers at `.harness/clearance/*-2026-07-19.md`. **2026-07-22: the RATIFIED B-65 apply arc** (fork ratified 2026-07-21, option A) — CP v1.102→v1.103 (AMENDED §25.15 — post-effect signing-carrier BRANCH-TERMINAL-WITH-RESULT under every `cascade_policy`; folds carry `result_ref`) + Runtime v1.102→v1.103 (NEW §14.8.11 protected post-effect result store + `result_ref` full-strength tenant-composite widening) + plan deltas CP v2.40 (U-CP-85 amended) / Runtime v2.51 (NEW U-RT-145); markers at `.harness/clearance/*-2026-07-22.md`. **2026-07-22 (spec leg): the RATIFIED B-59 apply arc** (fork ratified 2026-07-21, option A) — CP v1.103→v1.104 (AMENDED §25.11 admission-guarantee row 2 — the shared budget spans sequential bootstraps within one process) + Runtime v1.103→v1.104 (NEW §14.8.10.6 cross-bootstrap capacity-authority continuity + NEW §14.8.10.5 `RT-FAIL-CAPACITY-AUTHORITY-BUDGET-SHRINK` taxonomy row) + Runtime plan v2.52 (NEW U-RT-146); no CP plan unit owed (cross-reference only); markers at `.harness/clearance/*-2026-07-22.md`. Impl leg follows per the fork's §4 witnesses. **2026-07-22 (spec leg): the RATIFIED B-33-A apply arc** (fork `.harness/class_1_fork_b33_rotation_correlation_carrier.md`, ratified 2026-07-21, option A) — IS v1.11→v1.12 (NEW §5.6 D-derivative sidecar `rotation_correlation_id` on `StateLedgerEntry`/`EntryPayload`, C-IS-05; NEW §7.7 read-side presence/uniqueness invariants, C-IS-07) + IS plan v2.8 (NEW U-IS-20); no CP spec/plan unit owed at this leg (the `verify_rotation_6_steps` extension + OD-join are CP-owned, decomposed at the separate impl leg); marker at `.harness/clearance/*-2026-07-22.md`. **2026-07-23 (spec leg (ii)): the RATIFIED B-33-A arc's CP+OD+Runtime spec surface** — CP v1.104→v1.105 (AMENDED §20.3.1 row 7 — retires the "remains B-33's scope" deferral by extending `verify_rotation_6_steps` with real IS-anchored presence/uniqueness + OD-anchored evidence checks; NEW §20.3.2 `RotationPairEvidenceProvider` Protocol + typed DTO + physical-key-distinctness comparator) + OD v1.34→v1.35 (NEW §24.8 per-correlation-id rotation-pair evidence accessor, sibling to the unchanged §24.7 whole-ledger walk) + Runtime v1.104→v1.105 (NEW §13.6 composition-root inputs for a second, independently-wired injected verifier) + plan deltas CP v2.41 (U-CP-44/45 amended) / OD v2.30 (NEW U-OD-56, also backfilling §24.7's own zero prior plan coverage) / Runtime v2.53 (NEW U-RT-147) / CXA v2.22 (NEW §2.3.10, `R-planned`); markers at `.harness/clearance/*-2026-07-23.md`. Impl leg (iii) follows per the fork's §3 sequencing. **2026-07-23 (spec leg, CONTRACT-altitude correction pass same-day): the RATIFIED B-39 nested-paused-child-HITL-response-routing arc's CP+Runtime spec surface** (fork `.harness/class_1_fork_b39_nested_hitl_response_threading.md`, ratified 2026-07-23; Q2 carrier shape revised at this spec leg's grounding via a second operator AskUserQuestion to a keyed-field-inside-ResumeContext reading, not the fork's originally-ratified Mapping-at-api.resume()-boundary reading) — CP v1.105→v1.106 (`ResumeContext.hitl_responses`/`hitl_response_for` keyed field, keyed by the paused child's own `run_id` (round-2-corrected — `PausedChildBranchResumeState.branch_path` was tried at round 1, found to collide on repeated same-child_workflow_id dispatch, and removed; `child_snapshot.run_id`, an EXISTING field, already suffices); `DriverContext.resume_context_holder` Protocol field RETIRED as a ctx-level binding — the replacement delivery mechanism is stated as a CONTRACT (per-branch-distinct delivery; one-shot preserved under retry; no new global sharing), NOT prescribed) + Runtime v1.105→v1.106 (RETIRE §14.8.8.9 `ResumeContextHolder` as a ctx-level binding; NEW §14.8.8.10 CONTRACT-only consumption requirements; C-RT-04 `resume_context_holder` row RETIRED) + plan deltas CP v2.42 (U-CP-64 amended only — U-CP-86/88/89 unamended, post-correction) / Runtime v2.54 (U-RT-94/95 amended); markers at `.harness/clearance/*-2026-07-23.md`. **Correction context:** a first same-day draft prescribed exact wiring (an `execute_workflow` two-parameter signature; `attempt_resume` as the depth-0 entry point; direct intra-CP recursion for nested children); `just codex-review-uncommitted` + an `Explore` grounding pass found 3 of those wiring claims false against the real call graph (the real depth-0 path is `harness_runtime/api.py`'s `resume()` + `lifecycle/mcp_server.py`; nested-child re-dispatch crosses into `harness-runtime`'s `RuntimeSubAgentDispatcher`/`child_workflow_runner.py`) plus a retry-replay gap (a bare parameter would replay across `RetryBreakerFallbackDispatcher` retries within one resume cycle); advisor() recommended pulling back to CONTRACT altitude rather than a 4th unverified mechanism attempt. **Round-2 correction (second same-day pass):** a second `just codex-review-uncommitted` pass found the round-1-corrected `hitl_responses` key shape (`branch_path`) collides when two peer branches dispatch the SAME `child_workflow_id` (`branch_path` derives from a workflow_id-scoped `action_id` with no run-instance component); re-keyed to the paused child's own `run_id` (`PausedChildBranchResumeState.child_snapshot.run_id`, an EXISTING field — grep-verified via `compose_child_run_id_seed`'s `run_idempotency_key` folding to genuinely propagate run-instance distinctness through arbitrary recursion depth); the round-1-added `PausedChildBranchResumeState.branch_path` field was removed as unnecessary. Impl leg (code + tests, plus a scope-discovery pass determining which units carry the propagation-mechanism wiring across both axes) follows per the B-33/B-59 precedent. **2026-07-24 (spec leg): the RATIFIED `B-70` arc** (Class 2 fork `.harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md`, filed 2026-07-24; operator ratified "open the spec-leg now" via `AskUserQuestion` the same day) — CP v1.106→v1.107 (NEW §1 multi-branch effect-fence-resolution fallback-safety invariant, mirroring v1.106 §1.2 property 4's safety+liveness shape but simpler — no property-5 equivalent needed, per grounding that every `effect_fence_resolution_for` carrier entry is inherently gate-owning by construction) + plan delta CP v2.43 (NEW deferred coverage-matrix row, zero unit amendment); markers at `.harness/clearance/*-2026-07-24.md`. Impl leg not yet opened. **2026-07-25 (spec leg): `B-72` item (1) arc** (Class 1 tension `.harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md`, `systems-architect` tension-resolution mode — `advisor()` unavailable at authoring time, recommendation grounded directly against verbatim CP spec v1.106 §1.2 properties 1/4/5 + verbatim `sub_agent_dispatch.py` code; operator ratified "open the CP spec-leg now, co-designed with `B-71`" via `AskUserQuestion` 2026-07-25) — CP v1.107→v1.108 (NEW additive §1 property 6: a pre-dispatch gate-owning branch — one whose own `SUB_AGENT_BOUNDARY` gate fired before any child run was dispatched — MUST be counted in property 4's Safety-clause unaddressed-gate-owning-set test via an implementation-discretion internal identity, and MUST NEVER be resolvable via the `child_run_id`-keyed `hitl_responses` map; properties 1-5 PRESERVED VERBATIM) + plan delta CP v2.44 (NEW deferred coverage-matrix row, zero unit amendment; out-of-family `just codex-review` [P1] caught a first draft's row omitting the delivery-cell-construction side, fixed before merge — counting alone does not consume the resolved response). Resolves the fork doc's (a)/(b)/(c) framing: neither widening property 1's key nor a separate delivery path is needed; `B-71` confirmed NOT a co-requisite for this specific fix (round 7's own finding — `B-71` only exposes an already-dispatched child's `run_id`, a narrower sequential follow-on). Closes `B-72` net-position item (1) only; items (2)/(3)/(4) remain open. Markers at `.harness/clearance/*-2026-07-25.md`. Impl leg not yet opened.)*


## §1.2 axis CLAUDE.md archive (relocated 2026-08-10)

*Byte-preserved `harness-cp/CLAUDE.md` §1.2 "Spec + plan authority" table Version-column cells, relocated here by the U-CTX-20 axis-file slimming pass (R-CTX-1 Arc 6). Root table row structure (Artifact / Role columns) is unchanged in place; only the per-version lineage narrative moved. Verbatim — no paraphrase, no trimming.*

### `Spec_Control_Plane_v1_119.md` (spec) — original §1.2 Version cell

**v1.119 — canonical HEAD** — 2026-08-13, the `B-71` spec leg: `HITLEscalationBrief` (C-CP-28 §25.2) gains an optional, opaque, one-way, ≥128-bit `escalation_instance_id` so two peer fan-out branches sharing a `child_workflow_id` stop producing byte-identical operator-facing escalations — which also closes the per-peer HITL audit-entry loss under the IS writer's key-only dedup (C-IS-07 §7.5) as an ABSORBED half, not a follow-on. **FIVE carrier amendments**, all additive / `None`-defaulted / byte-identical when absent: §25.2.Z the brief field + four `payload_body` keys (including the BARE token, without which the operator cannot equality-match the webhook against the pause-view projection) (§0.6); §25.20 the pre-hash basis on `StepExecutionContext`; §26.9 the post-hash echo on the per-branch pre-dispatch gate-owning resume state (with a named WRITER and a drop-when-`None` serialization contract, so already-durable snapshots are not rejected as corrupt on upgrade); §25.21 the echo READ carrier on `StepExecutionContext`; §2.2.A the public correlation projection on `PreDispatchUniformFallbackOnlyLocation` — the design record's own precondition, since without it the correlation loop terminates in a struct no operator reads. §0.4(2-bis) also pins the token-PRESENT composed key (a `":"`-separated suffix, never re-hashed), because pinning only the absent case would let two compliant implementations emit different keys for one escalation after a deployment change. **The echo READ carrier is what makes the leg landable**: without it §0.4.2's normative use-the-echo-do-not-recompute order names a value the composer cannot reach, so every resume would take the crash-fallback arm — the persist-once contract was unsatisfiable, not merely unimplemented. The digest formula at §0.4(2) is a PINNED contract surface (domain separator, identity shape, enum string VALUE, sha256 lowercase hex, never truncated), because an idempotency-key family cannot tolerate two compliant implementations disagreeing. §0.13 names the owed **Runtime co-requisite** (spec v1.121) — an earlier draft left Runtime unmentioned, which reads as a zero-change claim by omission and is false: §14.8.8.1 step 2 specifies the two-argument `compose_hitl_action_id`. ZERO new contract number, fail class, enum extension, or field retype; `resolvability` reuses the CLOSED `PauseLocationVariant` vocabulary so the webhook and the pause view cannot become two authorities over one concept; the palette-suppression binding is WITHDRAWN (§0.6.1) and the residual explicitly left open. Predecessor PR #1325 carried this text with three amendments and was deliberately NOT landed (14 findings across four out-of-family rounds; true shape recorded on main at #1326) — landing an under-specified canonical contract is worse than landing nothing. Clearance `spec-control-plane-v1-119-cleared-2026-08-13.md`. Prior-head cell below preserved verbatim.

### `Spec_Control_Plane_v1_118.md` (spec) — original §1.2 Version cell

**v1.118 — canonical HEAD** — 2026-08-11, the `B-153` Attribute-count column ratification: the §24.1 column DEFINED as the namespace's declared live export claim in DISTINCT attribute keys (span names are not attributes; intra-row cross-event repeats count once; the v1.117 §0.3 cross-namespace sum caveat stands; parent-span reads are not row exports); the §24.1.A `hitl.*` cell enumerated 4→11 from C-CP-20 §20.6 — matching OD C-OD-05 §5.1 row 6's standing 11; `audit.*` audited CONFORMING (§20.4 seven distinct keys); §24.1.A subtotal 35→42, declared sum 68→75 (42+29+4). Same-PR cascade: manifest hitl 4→11, `test_total_attribute_count_seventy_five`, NEW cross-package map↔manifest witness (closes the #1311 residual); companion plan v2.51; NO OD delta owed. Clearance `spec-control-plane-v1-118-cleared-2026-08-11.md`. `B-153` CLOSES. Prior-head cell below preserved verbatim.

### `Spec_Control_Plane_v1_117.md` (spec) — original §1.2 Version cell

**v1.117 — canonical HEAD** — 2026-08-11, the `B-144` **venue-A** §24.1.B re-table: the single-venue (v1_2:2152-2166) F3-lifecycle-export table's superseded `Attribute count` cells (and the §24.1.A `engine.*` row, 3→4 per C-CP-09 §9.1 v1.3, +`engine.replay_disposition`; out-of-family round-2 catch) carried to the live ratified figures — `retry.*` 4→6 (C-CP-03 §3.5 v1.3 wholesale replacement; the four v1.2 names retired) and `harness.breaker.*` 7→9 (OD C-OD-07 §7.1 as amended at OD v1.32; the bare "v1.32" code-comment lineage mislabel corrected). Declared §24.1.B subtotal 25→29, §24.1.A 34→35, declared export sum 65→68; `fallback.*`/`lease.*` rows, §24.1.C and the composition-path summary byte-preserved. Same-PR cascade: manifest retry 4→6 + engine 3→4, terminal-manifest invariant derived from `CP_EXPORTED_ATTRIBUTE_COUNT`, acceptance-#6 test 65→68 (bundled absorption §11.4). The hitl.*/Attribute-count column-semantics question explicitly out-of-scope → minted `B-153` (spec §0.5 withholds ratification of span-names-as-attributes). /resolve codex=A advisor=A (hitl scope split → safer follow-up). Fork doc `class_1_fork_b144_cp_24_1b_stale_retable.md`; clearance `spec-control-plane-v1-117-cleared-2026-08-11.md`. `B-144` CLOSES. Prior-head cell below preserved verbatim.

### `Spec_Control_Plane_v1_116.md` (spec) — original §1.2 Version cell

**v1.116 — canonical HEAD** — 2026-08-09, the operator-ratified **`B-138` disposition (a)** spec leg: THREE amendment sites, all within C-CP-21 §21 — §21.5 row-1's value-set gloss corrected to the C-CP-25→C-CP-28 §25.2 `ValidatorFailClass` domain (the wire attribute `validator.fail.class` carries `schema_violation`…`external_rejection`); the §21.1 retry-exit taxonomy (`ValidatorRetryExitClass`) demoted from the wire name (routing/staircase semantics preserved verbatim, taxonomy never deleted, no wire attribute minted for it); `validator.fail.permanence` derivation clause demoted (the outcome projection is lossy — sub-decision routed to `B-124`, which is unblocked on the taxonomy question); §1.1 emission condition corrected (conditional on populated `fail_class`; the composer `unspecified` sentinel a non-member). BUNDLED with ADR-D5 v1.5→v1.6 §1.10.1. §21.5's declared attribute count stays THREE (count gates untouched). ZERO contract numbers / code / Runtime / OD / CXA / hash impact. `B-138` CLOSES; `B-139` + `B-140` + `B-141` minted. Clearances `spec-control-plane-v1-116-cleared-2026-08-09.md` + `ADR-D5-v1-6-cleared-2026-08-09.md`. Prior v1.115 — 2026-08-03, the ratified **`B-107` Reading A-hybrid** spec leg: THREE contract amendment sites — v1.107 §1.1 membership (the uniform-fallback unaddressed/eligible set holds only NON-EMPTY captured keys, so the scalar fallback cannot nominate `""`), `ResumeContext.effect_fence_resolutions` (ordinary construction REFUSES empty keys; the field is a validated immutable **copy**, inherited by every subclass), and the resolver boundary (an empty `idempotency_key` is unresolvable BEFORE map-hit or eligibility, making validation-bypassed content inert) — plus ONE publication reconciliation at v1.112 §2.1. The b80 `False → True` consequence is ratified; **`B-101`(a) NOT jointly discharged** (same compatibility-cost category, no variant discriminator; B-101 stays separately closed under (b)-PLUS, promotion trigger unchanged). **Union UNCHANGED at 4 variants / 10 source shapes / 7 carriers; ZERO contract numbers, ZERO Runtime §30 delta, ZERO CXA §2.3 rows, ZERO hash impact; U-CP-64 implementation separately owed — `B-107` stays OPEN.** Clearance `spec-control-plane-v1-115-cleared-2026-08-03.md`. Prior `Spec_Control_Plane_v1_114.md` (v1.114 — 2026-08-01, the CP-owned leg of register row **`B-101`** (disposition **(b)-PLUS**, SPEC-SIDE, doc-only). **ONE amendment site:** `Spec_Control_Plane_v1_112.md` §1.2 constraint 3's stated residual (the paragraph headed *"The residual, stated precisely rather than left for a reader to discover"*) is **APPENDED TO** with ONE paragraph — **NO text retracted or reworded**, though **§1.3(c)'s reach is made EXPLICIT where v1.112 left it implicit** (it binds the CP-OWNED affordances its own detect-then-refuse witness exercises — `test_pause_state_projection_b69.py:149` AC #A2 — and NOT generic base-schema serialization). **Under the broadest reading of §1.3(c) that IS a narrowing; the counter-reading is recorded in full at the delta's §0.5 finding (vi), with the one-step escalation route (disposition (a)) named.** The paragraph **declares the ORDINARY-SERIALIZATION boundary formally OUT of the staleness fence's scope** (Pydantic v2 serializes a subclass through a base-typed field using the BASE's schema, so an `AccessorDerivedResumeContext` round-tripped that way loses `pause_state` and the Runtime v1.107 §30 fence does not fire), **NAMES `StepExecutionContext.resume_context`** (`harness-cp/src/harness_cp/workflow_driver_types.py:460`) as the **ONE live base-typed carrier** (of nineteen `ResumeContext`-typed annotations in `src/**`, exactly one is a model field; the rest are sixteen function parameters + two local bindings, none serialized), **RECORDS three grounded facts re-verified at this leg** — ZERO `model_dump`/`model_dump_json`/`TypeAdapter` of that field OR of its enclosing `StepExecutionContext` across `src/` AND `tests/` (and `StepExecutionContext` is never a field of another model, so never transitively serialized); `compose_branch_child_context`'s `model_copy` is **shallow and non-revalidating**; `revalidate_instances` is **unset repository-wide** so Pydantic's `'never'` default holds — and states a **normative PROMOTION TRIGGER, keyed on MECHANISM not on the named carrier: any future arc introducing ANY base-schema serialization boundary over a `ResumeContext` value on a resume path — a `ResumeContext`-typed model field, a base `TypeAdapter`/`RootModel`, a base-annotated parameter/return crossing a schema-generating boundary, or any equivalent — MUST promote `B-101` to disposition (a) — the closed variant discriminator on `ResumeContext` — and land it BEFORE or SIMULTANEOUSLY WITH that arc, never after.** Disposition (a) is **NOT applied here** (it breaks the field-set-preserved-verbatim guarantee v1.112 §0.4 makes, and the row conditions it on a serialization boundary appearing on a real resume path, which does not exist at HEAD). **`ResumeContext`'s field set PRESERVED VERBATIM; §2.1 UNCHANGED at FOUR variants / TEN shapes / SEVEN carriers; ZERO contract numbers, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact, ZERO impl units owed, ZERO plan deltas owed, ZERO test changes owed** — verified: no implementation plan cites `B-101`, and `Implementation_Plan_Control_Plane_v2_48.md` is UNCHANGED at this leg. **DO NOT DELETE the pin test** `test_provenance_is_lost_through_base_typed_serialization_registered_b101` (`harness-runtime/tests/test_paused_workflow_state_accessor_b69.py:924`) — it asserts the CURRENT lossy behavior deliberately and is the **executable half of the promotion trigger**. **Honest limit:** trigger enforcement is a PRE-MERGE REVIEW OBLIGATION, not a validator rule, and the pin test breaks on a BEHAVIOR change but does **NOT** fire on the bare addition of a `model_dump` call. Clearance `spec-control-plane-v1-114-cleared-2026-08-01.md`). Prior `Spec_Control_Plane_v1_113.md` (v1.113 — 2026-07-31, the CP-owned leg of register row **`B-100`** (disposition **(a)**, SPEC-SIDE). **ONE amendment site:** §2.1's empty-`idempotency_key` key-ABSENT treatment is generalized from a **PROVENANCE** scope (*"the crash-reconstruction carrier"*) to a **STATE** scope — ***any effect-fence source shape whose captured key is EMPTY carries NO key field*** — adding a **KEY-ABSENT sibling SOURCE SHAPE** for the LINEAR `EffectFenceResumeState` and for `OrchestratorEffectFencePausedResumeState`. **VARIANT set UNCHANGED at FOUR; `effect-fence-addressable` 4 → 6 source shapes; union 8 → 10; distinct source CARRIERS UNCHANGED at SEVEN** (three now yield two shapes each). The §2.1 per-source-shape table and the sub-discriminator parenthetical counts are refreshed (a pre-existing shape-vs-carrier conflation). **ZERO contract numbers minted, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact — read-side only**; disposition (b) (capture-side refusal) deliberately NOT applied. **SIX findings surfaced not absorbed**, the first a **FALSIFIED PREMISE in the authorizing row** — the orchestrator capture site *does* guard on a truthy key (`workflow_driver.py:12376`–`:12381`), so that shape rests on **TYPE TOTALITY**, is **UNREACHABLE in production**, and its witness MUST be a constructed-snapshot test; the sixth is the **SCALAR uniform-fallback channel this delta cannot close, registered as `B-107`**. **IMPL DELTA OWED at U-CP-64 — build to CP plan v2.48 AC #A11, not to v2.47 alone.** Clearance `spec-control-plane-v1-113-cleared-2026-07-31.md`). Prior `Spec_Control_Plane_v1_112.md` (v1.112 — 2026-07-30, the CP-owned leg of the RATIFIED **`B-69` durable-pause-state read accessor arc** (council record `.harness/council-b69-pause-state-accessor-2026-07-30.md`, C10 + C11 + C9, converged; **operator ratified OPTION A′ — CO-REQUISITE, SEQUENCED — 2026-07-30**). THREE sections. **§1** — the REQUIRED `ResumeContext` **response-provenance carrier**: a closed two-variant discrimination (accessor-derived, token **non-optional**; legacy, token-field-absent and semantically byte-identical to today), the legacy variant **NOT constructible from** an accessor-derived one AND the accessor-derived variant constructible ONLY from a §14.14.9 projection — *without those the carrier IS the escape value the Runtime-side precondition forbids*. An OPTIONAL token would have made **omission itself** the prohibited escape (`resume()` cannot distinguish "read then omitted" from "never read"), leaving every accessor user's default path unfenced — *safe-by-diligence is a species of luck*. **Residual stated, not hidden:** a caller who reads and then independently constructs the legacy variant is unfenced; the type system cannot track a prior side effect, and closing that would require deprecating the legacy path. **§2** — the NEW **public projection-returning surface** over the two private resume-tree walks, publishing the authoritative FOUR-variant location classification (HITL-addressable / effect-fence-addressable / uniform-fallback-only / transitively-paused) plus tree position; **bare identifier sets FORBIDDEN** as the return shape; the never-keyable pre-dispatch **and depth-0-root** identities **ABSENT, not opaque** (they are `run_id`-shaped, so a keyed response is **silently DROPPED, not refused — livelock with no diagnostic**); enumeration **TOTAL** over gate-owning locations. Two out-of-family review rounds widened SOURCE columns without changing the variant count: `uniform-fallback-only` gained the **depth-0 root gate-owning pause** (round 1 [P1] — the most common HITL shape, which had no truthful variant), and `effect-fence-addressable` gained the **LINEAR `EffectFenceResumeState`** (round 2 [P1] — whose omission would have defeated §3.2's own lift; it declares exactly one field, `idempotency_key`). Checkable one-authority AC: ***"Runtime's accessor contains no recursion over `PauseSnapshot` and reads no nested resume-carrier field."*** **§3** — two scope-limit **LIFTS**: `Spec_Control_Plane_v1_106.md` §3 follow-on **(a)** (follow-on **(b)** untouched — separately `B-70`), and `Spec_Control_Plane_v1_107.md` §1.1's **round-4** `resume_handle` note (its **round-5** sibling is a different correction and is NOT lifted) — each lifted **conditionally**: only for a caller who takes the read, only in conjunction with the Runtime-side precondition, and only for **ADDRESSING** (§1.1(a)'s 2+-member INERT rule PRESERVED VERBATIM). Properties 1–8 PRESERVED VERBATIM; `attempt_resume` unchanged since v1.16 §26.8.5; ADDITIVE only. Clearance at `.harness/clearance/spec-control-plane-v1-112-cleared-2026-07-30.md`. Prior `Spec_Control_Plane_v1_111.md` — v1.111 — 2026-07-27, a PROSE-ONLY correction (self-caught via `advisor()`-directed fresh full re-read of the just-merged v1.110 body, before opening the B-79/B-80 impl leg) to §1.1(d)'s miscounted "ALL FOUR of LINEAR/EO/DH" → "ALL THREE of LINEAR/EO/DH" (the property always bound 3 sequential sites there plus the fan-out site = 4 total; the sequential-subset count itself was miscounted as 4; the very next sentence already said "all three", confirming a pure miscount, not a scope disagreement). NO contract, carrier, or enum change. Clearance at `.harness/clearance/spec-control-plane-v1-111-cleared-2026-07-27.md`. Prior `Spec_Control_Plane_v1_110.md` — v1.110 — 2026-07-27, the co-designed `B-79`/`B-80` spec leg (two dyadic `council-orchestrator` convenings, `roadmap-continue`, each run under the orchestrator's mandatory probe-first discipline before deliberation, plus an independent re-verification of every load-bearing call-graph claim at spec-authoring time — per this same session's own `B-71` arc lesson about a spec delta built on an unverified premise). NEW additive §1.2 property 7 (closes `B-79`): a resumed branch's HITL-relevant gate configuration (its applicable `HITLPlacement` set + `removed_placements` override) MUST be validated unchanged between pause and resume at the fan-out closure's pre-dispatch-gate-owning delivery site, the LINEAR `resume_at` site, the `EVALUATOR_OPTIMIZER` site, and the `DECENTRALIZED_HANDOFF` site (all 3 sequential sites have NO per-step identity check of any kind today, round-2 codex out-of-family review confirming EO/DH — landed later by the `B-78` impl leg — mirror LINEAR's mechanism EXACTLY and share the identical gap), and — impl discretion, not required — `RuntimeHITLGateComposer`'s Step 0; a mismatch MUST reject the resume as a material diff. The `B-79` convening's probe-first check FALSIFIED the row's own registered premise — Runtime-owned placement-resolution logic is NOT opaque to `harness-cp`; `step_context.hitl_placements`/`removed_placements` are both CP-native, so NO new cross-axis seam is needed. §1.3a authorizes one new additive field on `PreDispatchGateOwningBranchResumeState` ONLY (drop-when-empty/`None` hash-strip scoped, mirroring the `v1.108` precedent) plus a net-new carrier at the LINEAR site — `PausedChildBranchResumeState` gains NO field (out-of-family codex [P1], caught before merge: that carrier only forwards `child_resume_snapshot` to a recursively-dispatched child, it never constructs a `HITLDeliveryCell`, so a hash there would neither close a gap nor be neutral — it would reject valid resumes on an unrelated parent-placement change). NEW additive §2 property 8 (closes `B-80`): the effect-fence-abort HITL-delivery suppression guard MUST be computed against the true root of the resume tree, mirroring the ALREADY-SHIPPED `compute_hitl_uniform_fallback_eligible_run_id`/`compute_effect_fence_uniform_fallback_eligible_key` root-computed, thread-down-unchanged discipline — a PURE COMPUTED VALUE, no durable-carrier authorization needed (confirmed: `compute_effect_fence_uniform_fallback_eligible_key` derives fresh from the already-persisted `root_snapshot`, never itself stored). Scope is deliberately blanket, not `cascade_policy`-precise (`PauseSnapshot` carries no `cascade_policy` field at any level). Property 8's signal, once true, TAKES PRECEDENCE over property 6's §1.1(b) mandatory-delivery outcome (§2.3) — property 6's TEXT is unamended, only the circumstances under which its outcome fires are now conditioned. The new parameter threads through the Runtime-owned `ChildWorkflowRunner` Protocol (out-of-family codex [P2], caught before merge: an initial draft's "zero cross-axis cascade" claim was false — the touch is real, though it reuses the SAME crossing point its two sibling parameters, `hitl_uniform_fallback_eligible_run_id`/`effect_fence_uniform_fallback_eligible_key`, already use, so no NEW seam is introduced). A third out-of-family codex [P2] caught §1.1(c)'s initial draft falsely claiming a C-CP-20 audit-ledger emission on rejection — corrected to match `_resume_body_mismatch`'s actual existing behavior (`RunResult.fail_class` only, no audit-ledger write). Properties 1-6 PRESERVED VERBATIM. Clearance at `.harness/clearance/spec-control-plane-v1-110-cleared-2026-07-27.md`. Prior `Spec_Control_Plane_v1_109.md` — v1.109 — 2026-07-26, a PROSE-ONLY stale-carry-text correction caught by the `merge-gate` skill's spec-conformance lens reviewing PR #1117 (the `B-78` impl leg): §1.2's `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` exclusion paragraph asserted the gap was "ungrounded... not yet even reproduced" while the PR under review had already reproduced and closed it (mirroring LINEAR's delivery-cell mechanism, NO new CP-spec carrier field). NO contract, carrier, or enum change; property 6's text otherwise PRESERVED VERBATIM. Clearance at `.harness/clearance/spec-control-plane-v1-109-cleared-2026-07-26.md`. Prior `Spec_Control_Plane_v1_108.md` — v1.108 — 2026-07-25, the `B-72` item (1) spec leg (Class 1 tension `.harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md`, `systems-architect` tension-resolution mode — `advisor()` unavailable at authoring time, recommendation grounded directly against verbatim CP spec v1.106 §1.2 properties 1/4/5 + verbatim `sub_agent_dispatch.py` code; operator ratified "open the CP spec-leg now, co-designed with `B-71`" via `AskUserQuestion` 2026-07-25): NEW additive §1.2 property 6 — a pre-dispatch gate-owning branch (per property 5's own gate-owning test: it dispatched into its own HITL gate, but no child run exists yet) MUST be counted in property 4's Safety-clause "unaddressed gate-owning set" via a tree-wide-unique (not merely locally-unique) implementation-discretion internal identity, MUST NEVER be resolvable via the `child_run_id`-keyed `hitl_responses` map, and — round-3 codex correction, since counting + never-keying alone would still livelock — MUST be DELIVERED the operator's uniform `hitl_response` when it is the sole unaddressed member (a required contract OUTCOME, not merely a plan-level note). §1.3a additionally pre-authorizes ONE new additive, drop-when-empty/`None` hash-strip-scoped field on `PeerFanOutResumeState`/`FanOutResumeState` to carry this identity (existence + byte-compat treatment fixed here; exact name/type remains impl discretion) — a durable-carrier gap codex's round-4 review caught, since these types participate in the persisted `snapshot_hash` a bare "impl discretion" grant would have left unauthorized. Properties 1-5 PRESERVED VERBATIM; NO method signature or enum change. Resolves the fork doc's (a)/(b)/(c) framing (`.harness/class_1_fork_b72_gate_ownership_missing_carrier.md`): neither widening property 1's `run_id`-shaped key nor a separate delivery path is needed; `B-71` confirmed NOT a co-requisite for this specific fix (round 7's own finding — `B-71` only exposes an already-dispatched child's `run_id`, a narrower sequential follow-on). Closes `B-72`'s net-position item (1) — the round-3 reproduction's SOLE pre-dispatch gate-owning branch case — ONLY; items (2) (keyed multi-peer addressing, `B-71`-gated), (3) (property 4's general resolver set-membership mechanism), and (4) (`B-72`'s own round-1 hybrid case) remain open. Impl leg LANDED 2026-07-25 (PR #1116) — closes net-position item (1) end-to-end: NEW `PreDispatchGateOwningBranchResumeState(branch_index, step_id)` carrier on `PeerFanOutResumeState`/`FanOutResumeState` per §1.3a; `_collect_gate_owning_run_ids` + a new `_collect_pre_dispatch_gate_owning_identities` never-keyable discriminator; `HITLDeliveryCell` construction wired at `_execute_parallelization` and `_execute_orchestrator_workers` (the latter covers `HIERARCHICAL_DELEGATION` via its recursive reuse). 6 out-of-family `just codex-review` rounds to convergence — round 1's 2 real [P1]s (ordinal-only carrier lacking step_id identity; a hitl_responses map-collision addressability gap) both fixed; round 3's finding (a resumed branch's HITL placement/gate-configuration is not itself material-diff-checked) confirmed PRE-EXISTING at the already-shipped LINEAR `resume_at` delivery-cell site this diff mirrors, registered separately as `B-79`; round 4's 2 real [P1]s (a same-step_id workflow-id/kind swap material-diff gap; a same-level effect-fence-abort HITL-delivery suppression gap) both fixed; round 5 found round 4's own kind-changed guard regressed an UNCHANGED `PRE_ACTION`-gated `INFERENCE_STEP` resume (hardcoded a `SUB_AGENT_DISPATCH`-only constant instead of a captured-and-compared value) — fixed; round 6 found the fence-abort suppression guard is scoped to the current fan-out level only (confirmed PRE-EXISTING, inherited from the B-70 impl leg's own fan-out-local computation — registered separately as `B-80`) and that a repeated resume silently drops a recovered gate-owning peer withheld by §25.19 warm-up cohort scheduling (genuinely FIXED — carried forward at both construction sites, mutation-probe-verified regression test). Clearance at `.harness/clearance/spec-control-plane-v1-108-cleared-2026-07-25.md`. Prior `Spec_Control_Plane_v1_107.md` — v1.107 — 2026-07-24, the RATIFIED `B-70` Class 2 fork's spec leg (`.harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md`, operator ratified "open the spec-leg now" via `AskUserQuestion` the same day): NEW §1 multi-branch effect-fence-resolution fallback-safety invariant on the ALREADY-SHIPPED `ResumeContext.effect_fence_resolutions`/`effect_fence_resolution_for` mechanism (C-CP-26 §26.8.1, built at `Spec_Control_Plane_v1_66.md`) — mirrors CP spec v1.106 §1.2 property 4's safety+liveness shape but SIMPLER (no property-5 gate-owning/container split needed — grounding confirmed every `effect_fence_resolution_for` carrier entry is inherently gate-owning by construction, per `EffectFencePausedBranchResumeState`'s own "always tool-step in production" docstring + v1.106 §2 REMOVED's own text that the effect-fence mechanism "does not cross a recursion boundary the way HITL child-pause delivery does"). NO field, method signature, or enum change. Closes `B-70` (register flipped `design_substrate_gated` → `open` → `closed` at the impl leg, PR #1101, 2026-07-25). Impl leg LANDED 2026-07-25 (PR #1101) — the resolver's tree-walk enumeration wired at the LINEAR + ORCHESTRATOR + both fan-out consume sites, plus the LINEAR site's round-3 map-addressability amendment; 2 merge-gate rounds to all-approve. Clearance at `.harness/clearance/spec-control-plane-v1-107-cleared-2026-07-24.md`. Prior `Spec_Control_Plane_v1_106.md` — v1.106 — 2026-07-23, the RATIFIED `B-39` nested-paused-child-HITL-response-routing arc's spec leg (Q1=(A) retire `ResumeContextHolder`; Q2 revised at spec-grounding time via a second operator AskUserQuestion to a keyed-field-inside-`ResumeContext` reading, not the fork's originally-ratified Mapping-at-api.resume()-boundary reading, ratified 2026-07-23), **FOUR same-day out-of-family correction passes — round 3 (branch-vs-main `just codex-review` against open PR #1092) found 3 P1s of an addressability/safety class: a multi-child uniform-fallback misapplication risk, an unsafely-sequenced standalone field-removal AC (withdrawn, folded into the deferred delivery-mechanism bucket), and a `resume_handle`-path addressability gap (narrowed to a registered follow-on, B-69); round 4 (branch-vs-main against the round-3 fix commit) found round 3's OWN §1.2 property 4 fix was itself defective — a prescribed resolver mechanism that strands transitively-paused container branches and is unassignable to any unit — CONVERTED to a black-box safety+liveness invariant, deferred to the impl leg alongside the delivery mechanism (NOT a resolver-level gate any unit implements today), plus a NEW property 5 formally distinguishing gate-owning branches from transitively-paused container branches — see the spec's own round-3/round-4 correction notes**: `ResumeContext.hitl_responses: dict[str, HITLResult]` + `hitl_response_for(child_run_id)` — keyed by the paused child's own `run_id` (round-2-corrected; a round-1 `branch_path` keying + a companion `PausedChildBranchResumeState.branch_path` field were found at round 2 to collide when two peer branches dispatch the SAME `child_workflow_id`, since `branch_path` derives from a workflow_id-scoped identifier with no run-instance component — the round-1 field was removed, `child_snapshot.run_id`, an EXISTING field, already suffices); `DriverContext.resume_context_holder: object | None` Protocol field RETIRED as a ctx-level, run-tree-wide-shared binding — the replacement delivery mechanism is a CONTRACT (per-branch-distinct delivery; one-shot preserved under `RetryBreakerFallbackDispatcher` retry within one resume cycle; no new global sharing), deliberately NOT prescribed (a first draft asserted a specific `execute_workflow` two-parameter signature + `attempt_resume` as the depth-0 entry point + direct intra-CP recursion for nested children; out-of-family review + `Explore` grounding found these false against the real call graph — the real depth-0 path is `harness_runtime/api.py`'s `resume()` + `lifecycle/mcp_server.py`; nested-child re-dispatch crosses into `harness-runtime`'s `RuntimeSubAgentDispatcher`/`child_workflow_runner.py`). Runtime-owned contract text (the `ResumeContextHolder` sidecar retirement, same CONTRACT altitude) at the same-arc Runtime spec v1.106 §14.8.8.10 — cross-referenced, never restated. Spec leg only; impl leg (code + tests, plus a scope-discovery pass across both axes) is a separate follow-on arc per the B-33/B-59 precedent. Clearance at `.harness/clearance/spec-control-plane-v1-106-cleared-2026-07-23.md`. Prior `Spec_Control_Plane_v1_105.md` — v1.105 — 2026-07-23, the RATIFIED `B-33` rotation-correlation carrier arc's spec leg (Option A, ratified 2026-07-21): AMENDED §20.3.1 (C-CP-20) row 7 — retires the "remains B-33's scope" deferral by extending `verify_rotation_6_steps` with a real IS-anchored presence/uniqueness check (`WRITE_DUAL_VERIFY_ENTRY`, consuming IS spec v1.12 §7.7's `verify_rotation_window`) and a real OD-anchored evidence check (`PROBE_VERIFY_AT_READ`, via an injected `RotationPairEvidenceProvider`); NEW §20.3.2 declares that Protocol + its typed `RotationPairEvidence` DTO + two exception types + a REQUIRED (not optional — absence is an explicit incomplete disposition, never a silent skip) physical-key-distinctness boundary attestation against a signing backend's key-identity mapping (e.g. B-36's `AwsKmsSigningBackend.key_arns`); `verify_rotation_6_steps` also gains a SEPARATE `rotation_window_entries` parameter distinct from the pre-existing full-genesis-chain `audit_ledger_entries`, and `PROBE_VERIFY_AT_READ` additionally requires OD's `signatures_verified=True` (structural evidence alone is necessary but not sufficient) plus an evidence correlation-id echo check. Deliberately not byte-compatible for absent-parameter callers (zero production callers exist today). OD-owned (NEW §24.8 accessor) + Runtime-owned (NEW §13.6 composition-root inputs) contract text at the same-arc `Spec_Operational_Discipline_v1_35.md` / Runtime spec v1.105 — cross-referenced, not restated. Clearance at `.harness/clearance/spec-control-plane-v1-105-cleared-2026-07-23.md`. Prior `Spec_Control_Plane_v1_104.md` — v1.104 — 2026-07-22, the RATIFIED `B-59` apply arc's CP rider (option A — process-scoped capacity-authority singleton; ratified 2026-07-21): AMENDED §25.11 admission-guarantee row 2 — the shared budget (`occupied + N + S ≤ cap`) is extended to span sequential `api.run()` bootstraps within one process, not only concurrent activity within a single bootstrap; no new admission formula, no new `RunStatus` value. The process-lifetime mechanism itself (the frame-ledger adoption, adopt-new-budget-carry-occupied reconciliation, budget-shrink typed refusal) is Runtime-owned at the same-arc `Spec_Harness_Runtime_v1.md` v1.104 §14.8.10.6 — cross-referenced, never restated here. Prior `Spec_Control_Plane_v1_103.md` — **v1.103** — 2026-07-22, the RATIFIED `B-65` apply arc's CP rider (option A — the fork §3 terminal-with-result rider + §3b protected result store; ratified 2026-07-21): AMENDED §25.15 — a branch failing with the Runtime-owned post-effect audit-signing carrier (`PostEffectAuditSigningError`, identified via the import-free name-match fence AHEAD of the branch-failure → PAUSED/PARTIAL conversions in `workflow_driver.py`) is BRANCH-TERMINAL-WITH-RESULT under EVERY `cascade_policy` (`pause` MUST NOT mint a resume path re-dispatching it — the branch enters the terminal disposition set per the v1.74 scoped-abort precedent; `proceed`/`cascade-cancel` folds CARRY the carrier's `result_ref` into the PARTIAL/FAILED report; run-level status follows the policy for the REMAINING branches); result recovery is Runtime-owned at the same-arc Runtime v1.103 §14.8.11 protected result store (cross-referenced, never restated). Clearance at `.harness/clearance/spec-control-plane-v1-103-cleared-2026-07-22.md`. Prior delta v1.102 — 2026-07-19, the RATIFIED `B-48` apply arc's CP rider (option B; C1⊥C9 dyad 16/16 CONFIRM): AMENDED §25.11 — fan-out capacity gating under the single Runtime §14.8.10 executor authority (occupied+N+S shared budget, atomic per-branch reservation, typed step-attributable fail-fast never queue; initial admission gated at ALL FOUR fan-out sites (proceed AND cancel-policy execution); only frame-releasing teardown of already-admitted branches exempt; admission-rejection composes as a §25.15 `cascade_policy` branch failure), the unmaterialized delegation-depth phrase RETIRED via formal delegation (width-3 per C-CP-10 §10.3 PRESERVED), + the B-39 interim constraint (durable-HITL-gated concurrent siblings SEQUENCED until B-39's own back-flow). Clearance at `.harness/clearance/spec-control-plane-v1-102-cleared-2026-07-19.md`. Prior delta v1.101 — 2026-07-18, the RATIFIED `B-51`/`B-52`/`B-54` OD audit-signing amendment arc's CP rider (all ten fork gate items ratified AS RECOMMENDED; three all-CONFIRM council dyads at the apply leg): §1 AMENDED §13.5.1 `cp_audit_to_od_audit` gains `tenant_id: str \| None = None` (raw forwarding to `sign_audit_entry`; drop-when-`None` byte-compat); §2 AMENDED §28.10.4 invariant 2 — NARROW carve-out for the typed `AUDIT_SIGNING_HARD_FAILURES` family under `audit_signing_fail_closed=ON` (all other hook exceptions still swallowed) + the post-effect catch-ordering contract; §3 AMENDED §20.3.1 — backend-aware blocking-walk reconciliation with the OD v1.34 §21.2.2 API via a cycle-safe INJECTED verifier (CP-owned result boundary; `harness-cp` never imports `harness-od`); §4 NEW §20.1.1 — narrow cutover-scoped historical exception to §20.1 row 3 (membership decided exclusively by the authenticated cutover record's signed content-bound triples; forward posture unrelaxed). Clearance at `.harness/clearance/spec-control-plane-v1-101-cleared-2026-07-18.md`. Prior delta v1.100 — prose-only correction to v1.97's Change-note (v1.96→v1.97) registered-forward-work paragraph (unheaded prose, not a numbered section): gaps (b) [per-child HITL response routing, still open at `B-39`] and (c) [nested pause_reason propagation, closed at `B-32`] are no longer accurately described as "coupled ... travel together" — grounded via direct code read (pause_reason has exactly one non-telemetry reader, and the composer's resume path never branches on it, so (c) alone is not a false affordance); no contract/carrier/exception change. Clearance at `.harness/clearance/spec-control-plane-v1-100-cleared-2026-07-15.md`. Prior delta v1.99 = `B-31`: `PausedChildBranchResumeState.child_workflow_id` (NEW additive `str \| None = None` field) + a matching resume-guard comparison at both the `PARALLELIZATION` and `ORCHESTRATOR_WORKERS`/`HIERARCHICAL_DELEGATION` `_resume_body_mismatch` closures — closes the identity gap v1.97's own change-note registered as forward work (a same-`step_id`/same-`step_kind` edit that also swapped the re-supplied `SUB_AGENT_DISPATCH` branch's target `child_workflow_id` previously passed the resume guard undetected). Read via the existing `_opaque_field` opaque-mapping-key convention (no new cross-axis Protocol required); byte-compat via drop-when-`None` hash-strip scoping mirroring v1.97's own `paused_child_branches` precedent; no runtime-side change (the identifier already reached `harness-cp` via the pre-existing `SubAgentChildPausedError`, merely uncarried forward). Clearance at `.harness/clearance/spec-control-plane-v1-99-cleared-2026-07-15.md`. Prior delta v1.98 = `B-22` seam half: NEW §20.2.1 `SigningBackend` Protocol + optional `backend` keyword param on `sign_audit_entry` / `verify_audit_entry_signature` (composition-root injection seam; absent → the existing `AuditSigningBackendUnavailableError` fail-loud raise PRESERVED VERBATIM, zero regression for the zero production callers; present → real SHA-256-then-sign / recompute-then-verify per §20.3.1). Concrete prod-tech backend selection (which vault/KMS) remains DEFERRED per ADR-F5 §Deferred D-ADRs + ADR-D5 v1.5 §1.4 row 3 — operator chose "build the seam now" over "hold fully" via AskUserQuestion 2026-07-14; clearance at `.harness/clearance/spec-control-plane-v1-98-cleared-2026-07-14.md`. Prior delta v1.97 = `B-21`: `PeerFanOutResumeState.paused_child_branches` (NEW additive field, reuses v1.45's `PausedChildBranchResumeState` carrier verbatim) extends the v1.45 cross-recursion-boundary child-pause mechanism to `PARALLELIZATION`; byte-compat scoped per-carrier-kind (v1.45's `FanOutResumeState.paused_child_branches` nested hash behavior UNCHANGED); no new carrier/exception/runtime change; clearance at `.harness/clearance/spec-control-plane-v1-97-cleared-2026-07-13.md`. Prior delta v1.96 — 2026-07-12 pointer catch-up (R-600 cadence-5 adjacent finding): this row was stale at v1.38 through the B-18-3C-PREWARM family (PREWARM v1.87 / COHORTKEY v1.88 / DEFAULT-ON v1.89 / CASCADE v1.90 / TIMEOUT-LEDGER v1.91 / FENCE-LEDGER-FIDELITY v1.92 / -OW v1.93 / RECONSTRUCT-RESIDUAL v1.94 / EPOCH-PARTITION v1.95 / PREWARM-OW v1.96); per-delta detail at each version's own change-note + memory `b18-epoch-partition-design-decision-record.md`. v1.38 chain below preserved verbatim as historical (delta-only chain; this row's prior `v1.3` was the Phase-6.5 authoring-era pin, pre-dating the Phase-7 v1.4→v1.38 deltas — latest delta = **C-CP-06 §6.1/§6.2/§6.6 per-step ROLE override** [R-FS-1 arc B4 Slice 4; additive `StepOverride.agent_role` + `StepEffectiveBinding` propagation + §6.6 provenance extension; **paired with runtime spec v1.51→v1.52 §14.5.3 invariant-2/3 RELAXATION** — the committed-invariant sacrifice Slice 3 foreclosed; mechanism = **Option B** composition-time fold (CP driver folds the override onto the single `StepExecutionContext.agent_role` source, precedence per-step > fan-out-derived > default; **runtime dispatch read byte-unchanged**, zero `harness-runtime` impl edit — the `default_gate_level → parent_gate_level` v1.20 precedent); **OPERATOR-GATED** (committed-invariant relaxation; advisor reversed a probe-resolves attempt to dissolve the gate — the asymmetry: `default_gate_level` had no invariant forbidding it, role has the explicit §14.5.3 invariant 2); provenance at the WIRED per-step override state-ledger entry (`binding.model_dump`), NOT the §5.2 hash → no IS-spec change; bundled-absorption co-lands CP v1.38 + runtime v1.52 + `harness-cp` impl + by-execution tests]. Prior delta v1.37 = **C-CP-06 §6.1/§6.2/§6.6 per-step PROMPT override** [R-FS-1 arc B4 Slice 3; additive `StepOverride.prompt_version_sha` + `StepEffectiveBinding` propagation + NEW §6.6 provenance-scope — per-step prompt coherence at the WIRED override state-ledger entry (`binding.model_dump` at `workflow_driver.py:1913-1930`), NOT the run-level §5.2 hash (no IS-spec change; HEAD re-ground overturned the stale v1.27 "no production caller" note); surface=`StepOverride` not `PromptSelectionManifest` (`step_id` is a workflow-scoped instance, not a run-level category — Route B is a Track-B reuse hazard); per-step ROLE OUT of scope = Slice-4 gate per the §14.5.3 single-role-source invariant; v1.27 §2(d) explicit-extension discipline (mirror precedents v1.20/v1.22/v1.34); no new contract/ADR/enum/fail-class; no operator gate; bundled-absorption IMPL co-lands]. Prior delta v1.36 = **C-CP-02 §2.5 Layer-3 LLM_AS_ROUTER resolution surface** [R-FS-1 arc R / R-spec-1; resolve the async router-model call at the already-async `infer()` — **Reading B**, Reading A [widen `route()` async] foreclosed by ADD §5.3.3 + §2.2 "Probabilistic"; injected **OPTIONAL** `RouterResolutionFn` — absent ⇒ the existing `RoutingCandidateUnresolvedError` raise PRESERVED VERBATIM so DECLARATIVE-only + pre-bind deployments byte-unbroken; **no-regress** — router dispatches DIRECT against a pre-bound model, never re-enters `route()`/`infer()`; rationale via C-CP-01 §1.4 `routing.binding_rationale` span attr, frozen 4-field `RoutingDecisionTrace` NOT widened; 200 ms `LayerBudget` bounds it, L3-budget-exhausted ⇒ preserved raise [L3 terminal]; SPEC-ONLY, impl at R-impl-1 mock / R-impl-2 vendor-gate]. Prior delta v1.35 = **C-CP-19 §19.1.2 `MCP_TRUST_GATE_LEVEL_FLOOR` per-server trust HITL *gate*-floor materialization** (R-FS-1 B2-spec-2 / F2-02; resolves the v1.15 §19.1.1.1 row-3 spec-silence — the 4th, last unmaterialized §19.1 axis; **floor-only/monotone** probe-resolved [the gate is `max()`-over-escalation-rank so a floor can only RAISE it, and U-CP-91 `gate_level_rule.py:131-132` is a landed "`mcp_trust` NEVER overridden" commitment → loosening would reverse on-main + break the locked T-perm-1 composition] + operator-ratified Table A: L0→DENY / L1→ASK / L2→ASK / L3→AUTO; SPEC-ONLY, impl at B2-impl). Prior delta v1.34 = **C-CP-27 §27.8 identity-by-ordinal `MCPServerTrustLevel → MCPTrustTier` trust *telemetry* projection** (R-FS-1 B2-spec-1; adopt-and-note, C10⊥C11-council-converged; retires the constant-collapse stub; the per-MCP-server-trust *gate*-axis materialization is the separate B2-spec-2 leg [now landed at v1.35]). Prior delta v1.33 = the **C-CP-07 §7.4 reconciler-loop substrate-deferral reconciliation** (R-FS-1 E-spec-3): adds `reconciler-loop` to the §7.4 impl-discretion clause (hand-rolled etcd-style per I-6, reconciling §7.1's named "K8s controller / etcd" *reference* substrate with the no-vendored-K8s invariant; operator-ratified 2026-06-15); §7.1/§7.2/§7.4-floor-table + all other sections PRESERVED VERBATIM. Prior delta v1.32 = NEW §25.10–§25.21, an **in-place additive extension of C-CP-25 WorkflowDriver** materializing the 5 non-`SINGLE_THREADED_LINEAR` topology patterns (R-FS-1 arc #3 / B1-spec-1): driver-strategy dispatch + per-pattern contracts + buffered/deterministic-append path (D1.b; ADR-F2 single-threaded-write boundary, ZERO six-field/hash-chain change) + Route-Y branch-metadata sidecar seam (forward-ref to coordinated B1-spec-1b IS amendment) + B1↔B4 role seam (D2) + cascade_policy/cascade-cancel reach (D3 Fork A, council-resolved) + branch-scoped idempotency keys. §25.10+ additive; §1/§16.5.x/§25.1–§25.9/§26–§29 PRESERVED VERBATIM. Prior delta v1.31 = NEW §29 / C-CP-29 `PromptSelectionManifest`. Per delta-only convention each version is canonical-at-authoring for its scope; the chain head is the current contract authority. **Identity-label corrected at v1.32:** the v1.29–31 change-note tables mislabeled §25/C-CP-25 (it is WorkflowDriver, not ValidatorFramework) and §28/C-CP-28 (it is ValidatorFramework) per the v1.13 Reading A collision-resolution)

### `Implementation_Plan_Control_Plane_v2_53.md` (plan) — original §1.2 Version cell

**v2.53 — canonical HEAD** — 2026-08-13, the `B-71` CP-side execution authority: **ONE new unit, U-CP-102**, owning all five spec v1.119 carrier amendments plus the four `payload_body` keys (including the BARE token, without which the operator cannot equality-match the webhook against the pause-view projection), with FIFTEEN acceptance criteria and eight `# mutation-probe:` obligations (invert the read order; perturb one byte of the digest domain separator; project the un-hashed identity onto `branch_context`; emit the field on the linear path; drop the minter→snapshot write; emit the field when `None` instead of dropping it; honour a validator-supplied token; emit the public projection field as `null` instead of omitting it). A NEW unit rather than criteria on the landed C-CP-25 / C-CP-26 / C-CP-28 carriers — amending those in place would rewrite history verified against a different contract (the failure v2.52 §0.2 avoided by riding supersession as a note) and scatter ONE mechanism across three units whose landings cannot be sequenced against each other. **Declared reach** into C-CP-25/C-CP-26 carriers is stated rather than implied, and adds NO new dependency edge into any landed unit (every amendment is additive and `None`-defaulted, so no landed acceptance is invalidated). **The U-RT-155 co-requisite is bidirectional** and recorded as a plan-level fact in BOTH plan deltas: carriers with no minter stay `None` forever, a minter with no carriers has nothing to write, so a future session cannot schedule one alone and read a green suite as evidence the mechanism works. ZERO contract number minted, ZERO landed unit body amended, ZERO OD / IS / AS / CXA / ADR / ADD / PRD change; the SEVEN follow-ons registered at spec §0.9 — including register row `B-165`, the duplicate same-position placement collision, which needs a design-record revisit of the ratified identity basis rather than a spec leg — are explicitly out of scope and each owes its own leg. Clearance `implementation-plan-control-plane-v2-53-cleared-2026-08-13.md`. Prior-head cell below preserved verbatim.

### `Implementation_Plan_Control_Plane_v2_52.md` (plan) — original §1.2 Version cell

**v2.52 — canonical HEAD** (2026-08-12, the `B-141` plan-authority re-pin: TWO as-domain supersession NOTES, **ZERO new fields, ZERO unit-body rewrites** — U-CP-41 acceptance criterion #5's "C-CP-21 §21.5 5-value set" framing and the U-CP-47-era `VALIDATOR_FAIL_NAMESPACE_SCHEMA` retry-exit enumeration (`Implementation_Plan_Control_Plane_v2_4.md:631-638`) superseded AS DOMAIN by `ValidatorFailClass` per CP spec v1.116 (B-138 (a)); the ONE authorized code-shape change is the `VerifierResult.validator_fail_class` field-type supersession `Optional<str>` → `Optional<ValidatorFailClass>` (construction-enforced domain; out-of-family r2 at the B-141 PR); test-roster supersession `test_verifier_fail_class_in_cp_21_5_set` → `test_verifier_fail_class_in_validator_fail_class_domain`; landed units stand as HISTORY (B-97(a)/B-118 precedent). Clearance `implementation-plan-control-plane-v2-52-cleared-2026-08-12.md`. Prior-head cell below preserved verbatim.)

### `Implementation_Plan_Control_Plane_v2_51.md` (plan) — original §1.2 Version cell

**v2.51 — canonical HEAD** — 2026-08-11, the `B-153` plan-authority re-pin absorbing CP spec v1.118: U-CP-54 criterion #2 `hitl.*` 4→11 attrs (C-CP-20 §20.6 distinct declared keys), criterion #6 total 68→75 (42+29+4), test roster → `test_total_attribute_count_seventy_five`; exact sibling of the v2.50 B-144 re-pin; audit.* untouched (spec v1.118 §0.4 CONFORMING). Clearance `implementation-plan-control-plane-v2-51-cleared-2026-08-11.md`. Prior-head cell below preserved verbatim.

### `Implementation_Plan_Control_Plane_v2_50.md` (plan) — original §1.2 Version cell

**v2.50 — canonical HEAD** — 2026-08-11, the `B-144` plan-authority re-pin absorbing CP spec v1.117: U-CP-54 criterion #3 `retry.*` 4→6 (C-CP-03 §3.5 v1.3) + `harness.breaker.*` 7→9 (OD C-OD-07 §7.1 at OD v1.32), criterion #2 `engine.*` 3→4 (C-CP-09 §9.1 v1.3), criterion #6 total 63→68 (35+29+4), test roster `test_total_attribute_count_sixty_three` → `test_total_attribute_count_sixty_eight`; everything else U-CP-54 and all other units PRESERVED VERBATIM; hitl.* row deliberately untouched (`B-153`); surfaced by out-of-family round 1 at PR #1311; clearance `implementation-plan-control-plane-v2-50-cleared-2026-08-11.md`. Prior-head cell below preserved verbatim.

### `Implementation_Plan_Control_Plane_v2_49.md` (plan) — original §1.2 Version cell

**v2.49 — canonical HEAD** (2026-08-03, the ratified `B-107` A-hybrid plan leg — **U-CP-64 amended with ONE ADDED AC #A12**: the eight-cell witness grid (map + scalar channels × LINEAR / PARALLELIZATION branch / ORCHESTRATOR_WORKERS worker / ORCHESTRATOR-own; keyed siblings in BOTH carrier orders; ORCHESTRATOR-own cells constructed snapshots — they witness construction/classification semantics only, §1.3 resolver-guard discrimination binds at the LINEAR + branch-scan cells whose consults are ungated), the scalar-removal witnesses (the b80 flip + the direct caller-supplied `effect_fence_uniform_fallback_eligible_key=""` no-directive case), the PD-8 validate-copy-mutation probes, `model_construct` forged-context inertness, and the binding-rule reader inventory (seven resolver call sites + two non-test terminal key-match consumers at this revision); **#A1–#A11 PRESERVED VERBATIM; zero new units/clusters/DAG edges/CXA rows.** Clearance `implementation-plan-control-plane-v2-49-cleared-2026-08-03.md`. Prior `Implementation_Plan_Control_Plane_v2_48.md` (v2.48 — 2026-07-31, the `B-100` spec-leg plan absorption — **U-CP-64 amended** with **ONE ADDED acceptance criterion, #A11**: the KEY-ABSENT sibling source shapes for **all three** effect-fence carriers, witnessed by **detect-then-refuse in BOTH directions** (a key-absent shape cannot be constructed carrying a key; a key-bearing one cannot be constructed without one, nor carrying an EMPTY one). **#A1–#A10 PRESERVED VERBATIM.** ZERO new units / clusters / DAG edges; CXA v2.23 UNCHANGED, aggregate frozen at 111. **#A11 carries TWO explicit witness-shape constraints** — the orchestrator case MUST be a constructed-snapshot test (no production path exists), the LINEAR case IS reachable and must not be downgraded to it. **AC #A11 does NOT close the SCALAR uniform-fallback channel (`B-107`) — do not read it green as evidence that channel is closed.** Clearance `implementation-plan-control-plane-v2-48-cleared-2026-07-31.md`). Prior `Implementation_Plan_Control_Plane_v2_47.md` (v2.47 — 2026-07-30, the `B-69` spec-leg plan absorption — **U-CP-64 amended** (the `ResumeContext` carrier-owning unit, per its v2.21 authoring and its v2.42 `hitl_responses` amendment) for BOTH CP-owned surfaces, with **ten added ACs (#A1–#A10)**; **ZERO new units, ZERO new cluster, ZERO DAG topology change WITHIN CP** — the arc adds ONE cross-axis edge, U-RT-148 → U-CP-64, stated rather than folded into a blanket "zero". **A deferred coverage-matrix row is deliberately NOT the instrument here**: v2.43/v2.44/v2.46 used it correctly for properties constraining resolvers that do not yet exist, whereas **this surface has an owner**. **No topology unit (U-CP-86/88/89) is amended, stated explicitly** because v2.42's own round-1 draft amended exactly those three on an empirically false call-graph claim. Cross-axis cascade **named, not assumed away** — no NEW crossing point (the same boundary three sibling public CP computations already use), **but the payload widens materially, three scalars → a structured sequence**; CXA v2.23 determined **no new row, aggregate frozen at 111**. Clearance `implementation-plan-control-plane-v2-47-cleared-2026-07-30.md`). Prior `Implementation_Plan_Control_Plane_v2_46.md` — v2.46 (2026-07-27, the co-designed `B-79`/`B-80` spec-leg plan absorption — TWO new deferred coverage-matrix rows for CP spec v1.110 §1 property 7 and §2 property 8, mirroring the v2.42/v2.43/v2.44 precedent of deferring a not-yet-existing resolver rather than assigning it to U-CP-64; ZERO unit amendment, ZERO new units, 102 units. Property 7's carrier fields are entirely CP-native, zero cascade. Property 8's threaded parameter DOES touch the Runtime-owned `ChildWorkflowRunner` Protocol (out-of-family codex [P2] x2, caught before merge — an initial draft's "zero cross-axis cascade" claim was wrong; corrected: no NEW crossing point is introduced, same established boundary its two sibling parameters already use, but the touch is real). Clearance `implementation-plan-control-plane-v2-46-cleared-2026-07-27.md`). Prior `Implementation_Plan_Control_Plane_v2_45.md` — v2.45 — canonical HEAD (2026-07-26, PROSE-ONLY sibling correction to v2.44 §5's coverage-matrix row, same trigger as the spec-side v1.109 correction above — the row's "ungrounded by any reproduction... a future arc" language for `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` is corrected to reflect the `B-78` impl leg's closure (PR #1117); ZERO unit amendment, ZERO new unit; clearance `implementation-plan-control-plane-v2-45-cleared-2026-07-26.md`). Prior `Implementation_Plan_Control_Plane_v2_44.md` — v2.44 — canonical HEAD (2026-07-25 `B-72` item (1) spec-leg plan absorption — ONE deferred coverage-matrix row for CP spec v1.108 §1's property 6, explicitly naming both the new-carrier/counting side AND the delivery-cell-construction side at the fan-out dispatch sites (out-of-family `just codex-review` [P1] caught a first draft's row omitting the latter); ZERO unit amendment, the resolver that will enforce it does not yet exist, mirroring how v2.42 §5 + v2.43 §5 deferred the parallel `hitl_responses` invariants rather than assigning them to U-CP-64; ZERO new units, 102 units; clearance `implementation-plan-control-plane-v2-44-cleared-2026-07-25.md`). Prior `Implementation_Plan_Control_Plane_v2_43.md` — v2.43 — canonical HEAD (2026-07-24 `B-70` spec-leg plan absorption — ONE deferred coverage-matrix row for CP spec v1.107 §1's multi-branch effect-fence-resolution fallback-safety invariant; ZERO unit amendment, the resolver that will enforce it does not yet exist, mirroring how v2.42 §5 deferred the parallel `hitl_responses` property 4/5 invariants rather than assigning them to U-CP-64; ZERO new units, 102 units; clearance `implementation-plan-control-plane-v2-43-cleared-2026-07-24.md`). Prior `Implementation_Plan_Control_Plane_v2_42.md` — v2.42 — 2026-07-23 B-39 spec-leg plan absorption, FOUR same-day out-of-family correction passes — round 3 found AC #8's standalone field removal unsafely broke 3 currently-working effect-fence readers, withdrawn and folded into the deferred bucket; round 4 found round 3's own new AC #7 multi-child mechanism unassignable to U-CP-64 and unsafe (strands container branches), REVERTED to carrier-level-only tests with the invariant moved to the deferred bucket — U-CP-64 amended for `ResumeContext.hitl_responses`/`hitl_response_for` (keyed by the paused child's own `run_id`, round-2-corrected off a colliding `branch_path` key + a since-removed `PausedChildBranchResumeState.branch_path` field) + the CONTRACT-only `DriverContext.resume_context_holder` retirement; U-CP-86/U-CP-88/U-CP-89 UNAMENDED — a first draft amended them against a call graph out-of-family review + grounding found false (these functions don't recursively call `execute_workflow`; re-dispatch crosses into `harness-runtime`), so the propagation-mechanism wiring is deferred to impl-leg scope-discovery instead, ZERO new units, 102 units; clearance `implementation-plan-control-plane-v2-42-cleared-2026-07-23.md`). Prior v2.41 — canonical HEAD (2026-07-23 B-33 spec+plan leg — U-CP-45 amended for the `verify_rotation_6_steps` extension + U-CP-44 amended for the physical-key-distinctness comparator, ZERO new units, 102 units; explicit scope fence — no real production caller wired at this leg; clearance `implementation-plan-control-plane-v2-41-cleared-2026-07-23.md`). Prior v2.40 (2026-07-22 B-65 apply plan delta — U-CP-85 amended for the CP v1.103 §25.15 branch-terminality rider (name-match fence ahead of the PAUSED/PARTIAL conversions; pause-tier terminal disposition, no resumable PAUSED for the carrier branch; `proceed`/`cascade-cancel` fold `result_ref` carriage), ZERO new units, 102 units; witness (d) co-land pin at Runtime plan v2.51 U-RT-145; clearance `implementation-plan-control-plane-v2-40-cleared-2026-07-22.md`). Prior v2.39 (2026-07-19 B-48 apply plan delta — U-CP-82/85/86/88/89 amended + NEW U-CP-101 (the capacity-authority Protocol + default bounded authority declaration home; admission gated at all four fan-out sites, teardown-only exemption), 102 units; clearance `implementation-plan-control-plane-v2-39-cleared-2026-07-19.md`). Prior v2.38 (delta-only chain; 2026-07-18 B-51/B-52/B-54 arc plan delta — U-CP-72/73/44/45/42 amended for the CP v1.101 rider, zero new units; clearance at `.harness/clearance/implementation-plan-control-plane-v2-38-cleared-2026-07-18.md`. NOTE: this row's prior `v2.31` pin was itself stale against the then-head v2.37 — pointer refreshed here; per-delta lineage at the chain files)
