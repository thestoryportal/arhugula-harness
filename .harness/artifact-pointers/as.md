# AS — artifact pointer lineage

*Split byte-preservingly from `.harness/claude-artifact-pointers.md` at the U-CTX-03/04/05/06 R-CTX-1 context-optimization arc (2026-08-10, `B-17`/R-ICM-1 lineage). Old path is now a resolving stub — see `.harness/claude-artifact-pointers.md`. Loaded on demand only; never part of the default session-start read path.*

---

**Current head is DERIVED, not carried here.** This file is a lineage ARCHIVE; the authoritative per-family head version is derived from the `.harness/clearance/` marker corpus into `.harness/artifact-heads.md` by `tools/artifact_heads.py` (R-CTX-1 / U-CTX-11), and two CI gates keep that table current. Head labels inline below are reconciled at each arc but the generated table wins on any disagreement.

## §2.3 spec pointer

| Axis | Spec |
|---|---|
| AS | `Spec_Action_Surface_v1.md` (**v1.14 — canonical HEAD**, cleared 2026-07-15 — the spec-layer absorption of the ADR-D2 v1.3 `B-25` resolution (Reading A): C-AS-02 §2.3 row 7's condition text corrected to "Read-only, any" (matching rows 8–10's shape); the row→argument-keying paragraph gains a trailing sentence stating explicitly that `is_deterministic_inhouse` is not currently a keying input for ANY row — reserved, not removed; plus the §3.1 `ToolContract.is_deterministic_inhouse` field-docstring correction and the `ToolMetadata`-discriminators prose correction (only rows 1–2 are forcing). ZERO `sandbox_tier_floor` signature change, ZERO `ToolContract`/`ToolMetadata`/`MCPClientConfig` field added or removed, ZERO `max()` composition change. Clearance `spec-action-surface-v1-14-cleared-2026-07-15.md`, whose Notes recorded the root `CLAUDE.md` §2.3 pointer bump as OWED — discharged at the R-CTX-1 U-CTX-12 reconciliation. Predecessor v1.13 — 2026-07-12 pointer catch-up per R-600 cadence-5 adjacent finding: this row was stale at v1.10; v1.13 = R-FS-2 Wave 2 `B-TOOL-SEARCH-RUNTIME` NEW §13.7 `search_tools` capability-discovery contract; v1.12 = R-FS-1 `B-EFFECT-FENCE-PER-TOOL` AS leg, `ToolContract.idempotent` optional field; v1.11 = R-FS-1 B6 Slice 2 `B-PER-TOOL-SANDBOX-TIER` AS leg, three `ToolMetadata` forcing discriminators on `ToolContract`; each additive/byte-identical-by-default, per-delta detail in the spec file's own change-notes. v1.10 chain below preserved verbatim as historical — v1.10 — R-FS-1 B2-spec-1 2026-06-16: C-AS-10 §10.3 reciprocal Class-3 cross-ref — AS `MCPServerTrustLevel` projects identity-by-ordinal to CP `MCPTrustTier` per CP §27.8 (telemetry; per-transport floor + trust framework UNCHANGED; the per-MCP-server-trust *gate*-axis materialization = separate B2-spec-2 leg). Intervening: v1.9 R-CL-P6 spec-prose↔impl hygiene (§14.5/§14.6 producer-site footers); v1.8 footer-only consolidation. Prior v1.7 — GenAI span-name format Class 1 fork resolution R3 follow-on per `.harness/class_1_fork_genai_span_name_four_way_drift.md` §7.4.3 (R3) operator-ratified 2026-05-26 option (A) + option (b) alias-term abstraction. NEW alias-term convention introduced at §14.1: literal string `llm.inference` (phantom anchor — never emitted at production per fork §2.2) replaced with conceptual alias term **"the LLM inference span"** with cross-reference to OD spec v1.12 §C-OD-04 §4.1 for actual runtime span-name format. Amendment sites: §14.1 NEW sub-note + row 1129 `anthropic.*` parent-span column; §14.2 section header; §14.7 `memory.context_editing_active` semantic cite; §14.8 sampling-policy table row 1. Architectural rationale: option (a) literal rename is structurally impossible because the post-R2 actual emitted span name is template-instantiated per-call (`{operation} {model}`); option (b) alias-term decouples spec parent-anchor citations from runtime span-name resolution — future OTel semconv version bumps ripple through OD §4.1 + production rename only; AS spec parent-anchor cites are immune. Narrow-scope: other §14.1 parent-span literals (`mcp.tool.call` / `skill.activation` / `managed_agents.runtime` / `files.operation` / `memory.operation`) PRESERVED VERBATIM — those names ARE actual emitted span names at production. Co-published this session: production refactor at 5 carrier sites + 4 docstring sites (`anthropic_attribute_namespaces.py` `_ANTHROPIC_SPAN` constant; `anthropic_primitive_sampling.py` mapping key; `routing_namespace.py` `_LLM_INFERENCE_PARENT` text + class docstring; `multi_agent_span_hierarchy.py:84` literal; `cp_namespace_export_manifest.py:48` docstring; `workflow_driver.py:178` docstring; `cp_source_namespace_verification.py:14` docstring; `llm_dispatch.py:285` docstring; `memory_tool_dispatch.py:108,112` docstring) + test alias-aware assertion at `test_routing_namespace.py:37`. 3042 tests PASS / 4 skipped. ZERO contract change; ZERO new spec extension; alias-term IS the abstraction layer per architect §7.4.3 option (b). Co-resolved this session: R2 spec v1.12 (`43f6199`); R2 follow-on helper+test+R4+CLAUDE.md OD spec row (`5221264`); OD plan v2.19 (`65922eb`); R1 production rename + runtime spec v1.27 line 2033 STRIKE (`8fbf0cd`). Cross-axis cascade: CXA v2.10 → v2.11 seam declaration owed at §2.3.x convention bucket (AS §14.1 alias-term ↔ OD §4.1 format owner) — separate commit this session. 2026-05-26. v1.6 prior — Class 1 fork resolution Reading B apply pass per `.harness/class_1_fork_as_4_f4_enum_taxonomy_mismatch_and_production_bug.md` operator-ratified 2026-05-25. Additive substantive amendment at C-AS-15 §15: NEW §15.8 `MCPInvocationFailClass` 4-value StrEnum (`transport` / `protocol_error` / `schema_violation` / `timeout`) authored at AS-axis as canonical declaration site for MCP-protocol-layer fail-class taxonomy sibling to F4 process-execution-layer taxonomy at C-AS-04 §4.1; NEW §15.9 `mcp.fail.class` attribute on `sandbox.violation` child span (dual-attribute emission discipline with `sandbox.fail.class` per §15.2 row 3); NEW §15.10 best-effort projection table MCP-shape → F4-shape for cross-layer audit-ledger continuity. F4 enum at §4.1 PRESERVED VERBATIM. §15.1..§15.7 PRESERVED VERBATIM. Producer-side mutation discipline at runtime spec §14.9 owns dispatcher-side mapping per v1.4 §14.3 footer producer-vs-canonical-schema separation pattern. Resolves AS-4 retirement gate at `sandbox.violation` emission absence + production bug at `harness-runtime/.../runtime_tool_dispatcher.py:400 + :407` invented strings (`"transport"` / `"schema-violation"` not in F4 enum). Co-publication owed (post-v1.6): AS plan v1.2 → v1.3 + harness-as `MCPInvocationFailClass` carrier + harness-runtime dispatcher impl fix + retirement batch-20 H_T-AS-4 PARTIAL → RETIRED. 5 adjacent defects surfaced (CXA/OD cite cascade owed at follow-on, ADR-D2 reference unchanged, production bug fix owed at impl arc, `SandboxTierFloorViolationError` span-during-exception discriminator owed at impl arc, §15.10 projection-table best-effort acknowledgement of MCP-vs-F4 semantic stretch). v1.5 prior — Class 1 fork H_T-CP-16+17 §16 ratified absorption: NEW §14.7 producer-site reference footer note repointing `memory.*` 6-attr namespace emission to runtime spec v1.17 §14.5.1 + §14.12 C-RT-22 callback-invocation sites; documents structural-pattern divergence from §14.3 mcp.* (per-callback emission vs per-dispatch per ADR-D3 §1.1 #11 client-side classification); §14.6 `files.*` footer NOT authored (Files arc deferred per §14.C); 2026-05-23. v1.4 — Phase A.2 ratified-drafts apply pass: §14.3 + §15 producer-site reference footer notes documenting v1.13 runtime contracts consume v1.4 namespaces (2026-05-21). v1.3 — C-AS-02/§11.1 `sandbox_tier_floor` reconciliation per S2 + C-AS-05 §5.1 3-param `fetch_secret` per S3, 2026-05-15) |

## §2.4 plan pointer

| Axis | Plan | Unit count |
|---|---|---|
| AS | `Implementation_Plan_Action_Surface_v1_6.md` | 33 units (U-AS-01 – U-AS-33); **v1.6 — canonical HEAD**, cleared 2026-07-15 — the plan-layer half of the same `B-25` resolution, caught by out-of-family `just codex-review` round 1 on PR #1030: U-AS-06's Inputs line, Signatures block comment and AC #7 (preserved verbatim through v1.5) still named `is_deterministic_inhouse` as a §2.3 row-1/2/7 discriminator, so the PLAN — the canonical execution authority for the unit — carried the superseded reading after the spec had already moved. All four sites corrected to reserved-and-non-gating. ZERO code change, ZERO test change, ZERO AC-count change. Clearance `implementation-plan-action-surface-v1-6-cleared-2026-07-15.md`. Predecessor v1.5; predecessor v1.4 — Class 1 fork resolution Reading B arc 2 absorption per `.harness/class_1_fork_as_4_f4_enum_taxonomy_mismatch_and_production_bug.md` operator-ratified 2026-05-25, arc 2 landed 2026-05-26. Two single-unit-body amendments at delta-only-plan-chain layer: U-AS-03 carrier-extension adding `MCPInvocationFailClass` 4-value StrEnum + `project_mcp_to_sandbox_fail_class` projection function per AS spec v1.6 §15.8/§15.10 (3 NEW ACs + 7 NEW test names); U-AS-17 AC #3 text-replace (`sandbox.violation` carries 1+ → 2+ attributes per §15.9 dual-attribute emission) + 2 NEW ACs (#9 + #10) absorbing `SANDBOX_VIOLATION_ATTRIBUTES` extension + `MCP_INVOCATION_ATTRIBUTE_SCHEMA` sibling registration. Net AC count +3 across two units; unit count 33→33; ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade at v1.4 semantics layer. 3 adjacent OD/CXA/ADR-D2 cascades surfaced at §3 NOT patched per FM-2. Co-published with harness-as carrier impl + harness-runtime dispatcher bug fix at `runtime_tool_dispatcher.py:395-412` (replaced invented-string dead-code assignments with isinstance dispatch; NEW `_emit_sandbox_violation` helper opens `sandbox.violation` child span on exception path with dual fail-class attrs per §15.9) + 17 new harness-as unit tests + 5 new harness-runtime e2e tests against real fastmcp + retirement batch-19 H_T-AS-4 PARTIAL → RETIRED (28/49 = 57.1%; AS-axis 4/6 = 66.7%). 317/317 harness-as tests pass + 1069/1069 harness-runtime tests pass; pyright src-side neutral. **+ retirement batch-23 (2026-05-28) H_T-AS-5 STILL-BOUNDED → RETIRED via AS spec v1.6 §15.6 row 1 idempotency-key attribute attachment at `sandbox.violation` production span emission per `phase-7d-retirement-events-batch-23.md`** (30/49 = 61.2%; AS-axis 5/6 = 83.3%; AS-axis STILL-BOUNDED bucket NOW EMPTY; only AS-8 PARTIAL remains pre-final). ZERO AS plan delta — production caught up to AS spec §15.6 row 1 contract at runtime dispatcher; `_emit_sandbox_violation` signature widened to take `idempotency_key: str` 3rd param; 4 callsites (TIMEOUT/PROTOCOL_ERROR/TRANSPORT/SCHEMA_VIOLATION) updated; +1 NEW dedicated join-contract test + 4 existing dual-attrs tests extended. Gate-text reframe per `[[batch-22 sub-species 7]]` precedent — gate-text "invoke `sandbox_event_idempotency` composition function" was structurally stale against production OTel-span architecture (harness-as helpers operate on Pydantic `SandboxSpanEvent` model; production never constructs the Pydantic shape); the satisfiable reading per C-AS-15 §15.6 row 1 is OTel span attribute presence. Operator-ratified Option A AskUserQuestion 2026-05-28. 1092/1092 harness-runtime tests pass + 4 skipped; pyright clean. **+ retirement batch-24 (2026-05-28) H_T-AS-8 monolithic → 6 sub-rows ledger-v2-layer decomposition + 3 immediate sub-RETIRED transits (AS-8a anthropic.* + AS-8b mcp.* + AS-8c memory.*) per `phase-7d-retirement-events-batch-24.md`** (raw ledger cardinality 49 → 54; cumulative 30/49 → 33/54 = 61.1%; AS-axis ledger-decomposed view 7/10 = 70.0%; active-substitution view excluding AS-8e files.* INDEFINITELY-DEFERRED 8/10 = 80.0%; Meta-Arch §2.2 design-declaration view PRESERVED VERBATIM at 6 AS rows per X-AL-3). ZERO AS plan delta + ZERO production code change + ZERO cross-axis cascade — retirement-ID-scoping correction at ledger layer only. harness-as/CLAUDE.md §4.1 H_T-AS-8 monolithic row REPLACED with 6 sub-rows (AS-8a/b/c RETIRED at decomposition event; AS-8d STILL-BOUNDED for skill.*; AS-8e STILL-BOUNDED-INDEFINITELY for files.* per runtime spec v1.17 §14.C Memory-only scope; AS-8f STILL-BOUNDED for managed_agents.*). Sub-species 7c "retirement-ID-scoping-too-coarse" catalogued at batch-24 §2 — distinct from prior 7a (CP-19 operator-explicit-deferred-close-gate batch-22) + 7b (AS-5 gate-text-stale-vs-production-architecture batch-23); FIRST instance of retirement-ID decomposition in ledger history. Operator-ratified Option A AskUserQuestion 2026-05-28 + advisor Scope B pre-substantive consultation. Ledger v2 §11 NEW supersession entry documents the decomposition per §0.5 forward-only ledger discipline. v1.3 prior — Phase C atomic-unit decomposition pass thin revision absorbing AS spec v1.4 annotation-only producer-site reference notes (2026-05-21); ZERO new atomic units; v1.2 substantive content preserved verbatim. v1.2 baseline preserved verbatim through v1.4 delta-only-plan-chain convention |

## §1.2 axis CLAUDE.md archive (relocated 2026-08-10)

*Byte-preserved `harness-as/CLAUDE.md` §1.2 "Spec + plan authority" table Version-column cells, relocated here by the U-CTX-20 axis-file slimming pass (R-CTX-1 Arc 6). Root table row structure (Artifact / Role columns) is unchanged in place; only the per-version lineage narrative moved. Verbatim — no paraphrase, no trimming.*

### `Spec_Action_Surface_v1.md` (spec) — original §1.2 Version cell

**v1.14 — canonical HEAD** (B-25 resolution 2026-07-15 — `.harness/class_1_fork_sandbox_tier_floor_deterministic_inhouse_false_undefined.md`; dyadic C10⊥C4 council + operator `AskUserQuestion` selected Reading A; §2.3 row 7 condition text + "Row→argument keying" paragraph reconciled (the field is non-gating, reserved); stale `ToolContract`/`ToolMetadata`-discriminators docstring prose corrected; NO code behavior change, NO signature change; clearance at `.harness/clearance/spec-action-surface-v1-14-cleared-2026-07-15.md`. v1.13 — R-FS-2 Wave 2 `B-TOOL-SEARCH-RUNTIME`: NEW §13.7 `search_tools` capability-discovery contract, additive `defer_names` param on `compute_frozen_tool_superset`. v1.12 — R-FS-1 `B-EFFECT-FENCE-PER-TOOL` AS leg: `ToolContract` gains optional `idempotent: bool` (default `false`, byte-identical) for the runtime effect-fence per-tool exemption. v1.11 — R-FS-1 B6 Slice 2 `B-PER-TOOL-SANDBOX-TIER` AS leg: `ToolContract` gains the three `ToolMetadata` forcing discriminators (`forces_computer_use`/`forces_code_execution`/`is_deterministic_inhouse`), all optional/non-forcing defaults. v1.10 prior — R-FS-1 B2-spec-1 2026-06-16 — C-AS-10 §10.3 reciprocal Class-3 cross-ref: the AS-owned `MCPServerTrustLevel` value-set projects identity-by-ordinal to CP `MCPTrustTier` per CP §27.8 [telemetry; per-transport floor + trust framework UNCHANGED]; the per-MCP-server-trust *gate*-axis materialization is the separate B2-spec-2 leg. Prior v1.9 — R-CL-P6 spec-prose↔impl hygiene: §14.5/§14.6 producer-site footers. v1.8 — footer-only consolidation pass 2026-05-28 — (i) §14.4 `skill.*` producer-site footer reference per AS-8d Q1=B + co-published with runtime spec v1.32 C-RT-27; (ii) §14.5 `managed_agents.*` production-only exclusion footer per AS-8f Q1=C DEFER INDEFINITELY mirror of AS-8e. v1.7 → v1.8 revision row added 2026-05-28; v1.6 → v1.7 GenAI span-name fork resolution row catch-up authored 2026-05-26)

### `Implementation_Plan_Action_Surface_v1_6.md` (plan) — original §1.2 Version cell

**v1.6 — canonical HEAD** (B-25 resolution 2026-07-15 — `.harness/class_1_fork_sandbox_tier_floor_deterministic_inhouse_false_undefined.md`; U-AS-06 prose-only correction: Inputs line + Signatures comment + AC #1 row-7 label + AC #7 all corrected to state `is_deterministic_inhouse` is carried but non-gating/reserved, not a row-7 discriminator; net AC delta 0 (text corrected in place); ZERO code/test change; clearance at `.harness/clearance/implementation-plan-action-surface-v1-6-cleared-2026-07-15.md`). Prior v1.5 (Class 1 fork resolution 2026-07-15 — `.harness/class_1_fork_secret_negative_observation_missing_enum_member.md` / B-24; U-AS-21 signature extension: `NegativeObservationSurface` gains a 5th member `WORKFLOW_MANIFEST_ENTRY` + real `secret_arrival_site` dispatch replacing a hardcoded return; net AC +1; ZERO new units; ZERO DAG change; ZERO cross-axis cascade; clearance at `.harness/clearance/implementation-plan-action-surface-v1-5-cleared-2026-07-15.md`). Prior v1.4 (Class 1 fork resolution Reading B arc 2 absorption 2026-05-26 — U-AS-03 carrier-extension + U-AS-17 AC #3 text-replace + ACs #9/#10; net AC +3; ZERO new units; ZERO DAG change; ZERO cross-axis cascade)
