# Runtime — artifact pointer lineage

*Split byte-preservingly from `.harness/claude-artifact-pointers.md` at the U-CTX-03/04/05/06 R-CTX-1 context-optimization arc (2026-08-10, `B-17`/R-ICM-1 lineage). Old path is now a resolving stub — see `.harness/claude-artifact-pointers.md`. Loaded on demand only; never part of the default session-start read path.*

---

**Current head is DERIVED, not carried here.** This file is a lineage ARCHIVE; the authoritative per-family head version is derived from the `.harness/clearance/` marker corpus into `.harness/artifact-heads.md` by `tools/artifact_heads.py` (R-CTX-1 / U-CTX-11), and two CI gates keep that table current. Head labels inline below are reconciled at each arc but the generated table wins on any disagreement.

## §2.3 spec pointer

| Axis | Spec |
|---|---|
| Runtime | `Spec_Harness_Runtime_v1.md` (**v1.114 — canonical HEAD**, 2026-08-08 the `B-118` spec leg — **WIRES THE HALF-OPEN LATCH** and **DISCHARGES** the dead-half-open-latch residual v1.112 registered and v1.113 restated unchanged. Grounded at HEAD `fb3dda44`: `attempt_half_open` had **ZERO production call sites**, `should_attempt()` was a clock-free state read, `get_breaker` caches identity-stable for the process — so `open` was **ABSORBING for the process lifetime** and `record_failure`'s own `half_open → open` arm was unreachable. **FOUR amendment sites, all within §14.6**: step 4's pre-check becomes **THREE-WAY** (`closed` proceeds / `open` invokes a **CONJUNCTIVELY**-gated `attempt_half_open(now)` and on admission runs the SINGLE trial **capped at ONE attempt** / `half_open` = sibling holds the permit → skipped with the new `half-open-trial-in-flight` reason) plus a **release-on-every-exit MUST**; the §14.6.3 residual **DISCHARGED IN PLACE** with that section's seven dispositions explicitly unchanged; NEW **§14.6.4** carrying the **nine-cell trial-outcome × waiver matrix** as contract; and a §14.6 invariants strict append pinning the clock as composer-injected, monotonic, **IN-MEMORY** — §14.6.3 **t4**'s boundary held POSITIVELY. **CONTRACT-versus-CODE gap, not a design extension** — step 4 already read *"state is OPEN and cooldown unexpired"*, which `should_attempt()` cannot express, and C-OD-07 §7.1 already contracts BOTH recovery transitions by name; the latch was specified at two venues and built at neither, so the row's registered falsifier (*"absorbing-OPEN is intentional"*) is NOT taken. **THE ONE OPERATOR GATE**: §14.6.4 cell 3 (a WAIVED fault MID-TRIAL) is **not answered by Reading (II)**, which governs whether a fault CHARGES and was ratified while the half-open path was unreachable — unhandled, it **STRANDS the machine in `half_open`**, a **NEW absorbing state strictly worse** than the `OPEN` this row removes, as do four further cells (audit-signing hard failure, terminal HITL, `DispatchFenceTrippedSignal`, `CancelledError`). INCONCLUSIVE → re-arm is derivable from the attributability clause; the FRESH-vs-PRESERVED cooldown choice is not. **Operator-ratified 2026-08-08 (AskUserQuestion): FRESH cooldown**, `trigger_count = 0` (a zero-schema-cost wire discriminator against a genuine re-trip), `fail_count` UNCHANGED. **The clock is SPLIT** — composer holds the SOURCE (injectable `monotonic`, the `sleep_fn` posture; NOT a `RuntimeConfig` scalar), machine holds only the caller-recorded `opened_at` DEADLINE and still never reads a clock; template `BreakerGuardedSigningBackend`. **ZERO new `C-RT-*`, ZERO OD delta of ANY kind, ZERO CP §3.5 delta, ZERO CXA rows (111), ZERO hash impact, ZERO Protocol widening, ZERO persisted state** — so the registered council condition (durable cooldown clock → C9 ⊥ C11) did **NOT** fire and no council was convened. Plan leg v2.62 U-RT-154; `B-118` → `closed`, `B-119` cross-noted; clearance `spec-harness-runtime-v1-114-cleared-2026-08-08.md`. Predecessor v1.113, 2026-08-08 the `B-115` **(b′)** spec leg — DISCHARGES the one CONDITIONAL row v1.112 left open; **THREE amendment sites, ALL inside §14.6.3**: (A) row 6 flips *(prospective)* / **WAIVED — CONDITIONAL** to **WAIVED (condition DISCHARGED)**, naming `MemoryToolExecutionLedgerConflictError` (table stays SEVEN rows; split 2 count / 4 waived / 1 prospective → **2 count / 5 waived / 0 prospective**); (B) the guard paragraph **four-type → five-type** plus the **sibling-not-subclass** constraint (a subclass of the store subtype would carry `io_failure` back by MRO and void the C-MEM-19 half of the split) — the BY-NAME rule, the classifier-consistency requirement and the *"changes the CHARGE, never the control flow"* clause PRESERVED VERBATIM; (C) the declared-residual paragraph gains ONE appended clause — **`B-132` NARROWED, NOT closed**, the store subtype's own disposition (`io_failure`, `TRANSIENT_RETRY`, still charging) UNCHANGED. **Spec + plan + impl land TOGETHER and the bundling is FORCED**: row 6 conditions the contract text on a fact only the build can establish and C9's binding form is *"never speculatively"*, so a spec-first leg would assert the confirmed disposition before the confirming evidence existed (`REVIEW-e2.md` F-05's circularity) — the `B-116-t3`/#1272 precedent. **"Confirms determinism" was UNDEFINED by both §14.6.3 and the council DELIVERABLE**; this leg supplies a four-part conjunctive definition (repeat-invariance; candidate-independence; state-driven-not-stochastic; structural clock-exclusion) at contract altitude and discharges it at the **LEDGER**, so the evidence survives future re-typing of the executor surface. Trip-threshold revisit-trigger does **NOT** fire (harness-internal, not provider-attested); dead half-open latch inherited unchanged (`B-118`). ZERO new `C-RT-*` / `C-MEM-*`, **ZERO Memory-spec leg** (the type declares no class and inherits the base residual — U-MEM-28 precedent; a dedicated `ledger_conflict` class is operator-gated Class 2, registered as **`B-134`**), ZERO CXA rows (111), ZERO OD delta, ZERO CP §3.5 delta, ZERO hash impact. Duplicate-RECORD half stays OPEN (W-5 cell (3)'s `== 2` assertion PRESERVED VERBATIM); only the RETRY amplification closes. Plan leg v2.61 U-RT-153; `B-115` → `closed`; clearance `spec-harness-runtime-v1-113-cleared-2026-08-08.md`; predecessor v1.112 — 2026-08-08 the RATIFIED `B-116` **Reading (II)** spec leg — THREE amendment sites, all inside §14.6: a NEW **§14.6.3** carrying the ratified recovery-model normative test (*a fault charges the provider-model breaker only if a half-open trial could return a different result than the trip did, attributably to the `{provider, model}` the breaker is keyed to*), the 7-row per-member disposition table (2 count / 4 waived / 1 prospective-conditional — 401/403 and the 25 response-parsing payload-shape sites STILL CHARGE; the four harness-internal types are WAIVED BY NAME, the 3 pre-flight payload-shape sites via re-type to NEW `LLMDispatchPayloadShapeInternalError`), binding terms **t1–t5** (waived-charge span-attribute pair on the INNER attempt span; the in-venue floor record with the **`B-116-t3`** OD leg registered as a NAMED closure gate; the durable-persistence pin; no HITL), C9's trip-threshold revisit-trigger and the dead-half-open-latch residual declared (zero `attempt_half_open` production call sites; forward row `B-118` owns the debt) — plus TWO strict appends (step 4's fail-fast bullet; the `retry.*` invariant bullet admitting t1+t2 outside the CP-canonical six). **ZERO new `C-RT-*`, ZERO CXA rows (111), ZERO OD roster delta at this leg** (OD §9.2 18 → 19 is the separate t3 leg). Plan leg: v2.60 U-RT-152. `B-116` `operator_gated` → `open`; closes only when BOTH the impl leg AND the t3 leg merge. Clearance `spec-harness-runtime-v1-112-cleared-2026-08-08.md`. Prior: **v1.111**, 2026-08-05 the RATIFIED `B-96` **Reading C / ceiling form C-2** spec leg — THREE amendment sites, all inside §14.8.11: a NEW **§14.8.11.1** carrying the `.harness/council-b96-grace-ceiling-2026-08-01.md` §7.1 **TWELVE** conditions as contract terms 1–12 (reclaim becomes CONJUNCTIVE and publication-bounded — past-TTL filesystem age **AND** past-TTL elapsed time since a DURABLY RECORDED first observation, **no third path**, **NO absolute `k × ttl_seconds` ceiling**), plus two strict APPENDS. Reading A NOT selected; Reading B RETIRED at term 10. Retention typically `2×TTL` + up to two sweep-trigger intervals and **CONDITIONAL, never unconditional**. ZERO new `C-RT-*`, ZERO CXA rows (111), ZERO OD delta (`C-OD-05` §5.1 roster 15 → 15). Plan leg: v2.58 U-RT-150. *(Predecessor v1.110, 2026-08-01, the `B-104` Component 1 declaration leg — **this row was itself one version stale at v1.109 before the `B-96` leg refreshed it; the staleness is pre-existing, recorded not silently absorbed**.)* Prior: v1.109, 2026-07-31 the `B-100` Runtime leg — ONE amendment site entirely inside §14.14.9.2: the key-ABSENT treatment generalized PROVENANCE → **STATE**, adding the LINEAR + orchestrator KEY-ABSENT sibling source shapes; `EffectFenceAddressable` 4 → 6 shapes, union 8 → 10, carriers UNCHANGED at SEVEN, **VARIANT set UNCHANGED at FOUR**; `step_kind` recounted (present on FIVE, absent from FIVE) and the sub-discriminator parenthetical corrected 3 → 6. **NO new `C-RT-*` number, NO carrier retyped, NO field added, NO `snapshot_hash` impact.** §14.14.9.1, §14.14.9.3–§14.14.9.6, §30, §14.14.8 and §13.7 PRESERVED VERBATIM; §14.14.9.5's per-variant counts stay at FOUR, so the OD-owned §C-OD-30.5 schema is untouched and no OD delta is owed. Clearance `spec-harness-runtime-v1-109-cleared-2026-07-31.md`. Prior v1.108 — 2026-07-31 the RATIFIED `B-97` half (a) spec leg — §14.14.8 keying AMENDED from `workflow_id` to the TENANT-COMPOSITE `(normalized_tenant_scope, workflow_id)` realized as **per-tenant PATH SEGREGATION** under a **TOTAL injective CONSTANT** encoding closing all THREE hazards (branch / delimiter / field-boundary), delegating to the **Runtime-local** `normalize_tenant_scope` and explicitly NOT to OD's; **record shape UNCHANGED**, so `snapshot_hash` is untouched and the five-member `PauseJournalReadCause` vocabulary stays **CLOSED at five** — the cross-tenant refusal is **DISSOLVED, not discharged**; (3a) abandon-by-default with the failure-mode asymmetry as spec text + a MANDATORY no-mixed-version cutover stated alongside the NEGATIVE FACT that no directory-scoped liveness predicate exists + the (3b) adoption's **FIVE** mechanical terms (the fifth being the filename↔wrapper binding) + the named silence + the two-halved orphan statement; §14.14.9.1 parallel keying amendment (both-surfaces-or-neither; caller input triple BYTE-UNCHANGED); §30's `absent` row tenant-scoped in MEANING and REPAIR across THREE states + the `B-102` reachability qualification folded into `absent`/`corrupt-latest`; NEW **§13.7** `harness-inspect` pause-journal ENUMERATION surface, a **PRECONDITION** of (3a); §13.4 inventory rows; SIX stale-as-described fold-ins, one of them a **code** touch (`shutdown_cli`'s STRUCK-drain self-description, strings only). CP / OD / IS / AS and CXA all UNCHANGED, each DETERMINED not assumed. Clearance `spec-harness-runtime-v1-108-cleared-2026-07-31.md`; prior **v1.107**, 2026-07-30 the RATIFIED `B-69` spec leg — NEW §14.14.9 durable-pause-state read accessor (closed four-variant / seven-source-shape projection DTO; five-member cause attribution with **decode failure routed PERMANENT**; fail-closed on inability to mint; no-gate / no-HITL-trigger / no-internal-retry); NEW §30 refusal-only staleness precondition (token stated as a PROPERTY, never a composition) + the cause-attribution refinement on the EXISTING `RT-FAIL-RESUME-HANDLE-UNKNOWN` whose retry disposition is now CAUSE-DEPENDENT; the `:3274` scope-limit sentence DISCHARGED; NEW §14.14.8 append-only/never-truncated substrate invariant. Clearance `spec-harness-runtime-v1-107-cleared-2026-07-30.md`. **Pointer-lag note (surfaced at this arc, not introduced by it): this row was stale at v1.105 — v1.106 (the B-39 arc) landed without refreshing it; detail at that version's own change-note.** Prior:) `Spec_Harness_Runtime_v1.md` (v1.105 — 2026-07-23 B-33 spec leg (ii) — NEW §13.6 rotation-pair-evidence composition-root inputs (signing-key identity mapping REQUIRED, not optional-skip; factory + adapter only — no live production injection site exists yet, mirrors §13.5's shape for a second independently-wired injected verifier); clearance `spec-harness-runtime-v1-105-cleared-2026-07-23.md`. Prior `Spec_Harness_Runtime_v1.md` (v1.104, 2026-07-22 B-59-A spec leg — NEW §14.8.10.6 cross-bootstrap capacity-authority continuity (process-lifetime frame ledger adopted across sequential `api.run()` invocations; the SubAgentDispatchExecutor object itself is rebuilt fresh every bootstrap around the adopted ledger, never reused — drain() permanently flips _draining) + NEW §14.8.10.5 RT-FAIL-CAPACITY-AUTHORITY-BUDGET-SHRINK taxonomy row; clearance `spec-harness-runtime-v1-104-cleared-2026-07-22.md`. Prior `Spec_Harness_Runtime_v1.md` (v1.103, 2026-07-22 B-65-A apply — NEW §14.8.11 protected post-effect result store + result_ref widening; clearance `spec-harness-runtime-v1-103-cleared-2026-07-22.md`. Prior v1.102, 2026-07-19 B-48 apply — C-RT-03 cap field + NEW §14.8.10 executor contract; clearance `spec-harness-runtime-v1-102-cleared-2026-07-19.md`. Prior v1.101, 2026-07-18 RATIFIED B-51/B-52/B-54(+B-53) apply arc; clearance at `.harness/clearance/spec-harness-runtime-v1-101-cleared-2026-07-18.md` — flag carrier + resolved-flag prewarm/keepalive disable + NEW §13.5 verifier inputs + B-53 §13.4 row. Prior v1.100, cleared 2026-07-15 per `.harness/clearance/spec-harness-runtime-v1-100-cleared-2026-07-15.md`; 2026-07-18 pointer refresh at the B-43 PR — prior head text preserved verbatim: v1.99 — canonical HEAD**, 2026-07-12 pointer catch-up per R-600 cadence-5 adjacent finding: this row was stale at v1.52 (root `CLAUDE.md` §2.3 was itself at v1.96 before this same catch-up). v1.97 = cacheable-epoch ttl-selection (5m/1h, U-1 slice 3b); v1.98 = cache-ttl observability fix (extractor now scans translated wire kwargs, not the frozen payload); v1.99 = B-18-KEEPALIVE (ADR-D3 §1.5 boot pre-warm + daemon keep-alive, opt-in-default-off). v1.52→v1.96 detail not reconstructed here (per-delta detail at each version's own change-note in the file itself + root `CLAUDE.md` §2.3's own partial parenthetical). v1.52 chain below preserved verbatim as historical — v1.52 — R-FS-1 **B4-Slice-4** 2026-06-17: **§14.5.3 invariant-2/3 RELAXATION** for the per-step ROLE override + linear-path role indexing — invariant 2 "single role source" relaxed to "single *dispatch-read* source" (a `StepOverride.agent_role` carrier admitted as a **composition-time** input via **Option B** — the CP driver folds it onto the one `step_context.agent_role` source; **runtime dispatch read byte-unchanged**, zero `harness-runtime` impl edit) + invariant 3 "linear path untouched" → conditional; invariant 1 non-breaking-default PRESERVED. **OPERATOR-GATED** committed-invariant relaxation (paired CP v1.38; the `default_gate_level → parent_gate_level` v1.20 precedent — but role has the explicit §14.5.3 invariant, so the operator ratifies). No new C-RT-NN / fail class / IS change / ADR change. Clearance `Spec_Harness_Runtime-v1_52-cleared-2026-06-17.md`. Prior v1.51 — R-FS-1 B2-spec-1 2026-06-16: NEW §14.9.10 multi-server host-materialization + tool→server routing reshape (C-RT-04 singular→`dict[ServerName, MCPClientHost]`; routing index + fail-loud collision `RT-FAIL-MCP-TOOL-NAME-COLLISION` [10th fail class]; per-host sandbox; identity-by-ordinal trust *telemetry* projection consuming CP §27.8; the *gate*-axis materialization = separate B2-spec-2 leg). Intervening v1.43–v1.50: sandbox-driver selection / non-linear topology B1 / smart-HITL B3 / durable-execution engine-classes E. Prior v1.42 — R-CL-P4 **prompts-management binding** 2026-06-11 (co-published with IS spec v1.5): NEW §4 C-RT-04 field row `prompt_manifest: PromptManifest` (`harness_is.prompt_manifest`) — operator-supplied empty-defaultable carrier read by `resolve_procedural_tier_snapshot` (U-RT-112) as the third procedural-tier hash component `active_prompt_version`; ambient substrate (stage-0, NOT stage-enriched) mirroring `routing_manifest`. Runtime-binding-extension arc (IS §5.2 precondition 1); ZERO new C-RT-NN. Clearance marker `Spec_Harness_Runtime-v1_42-cleared-2026-06-11.md`. Fork `class_1_fork_prompts_management_surface_active_prompt_version.md` OPEN → APPLIED. 2026-06-11. v1.41 prior — AC#2-closing arc: NEW **§14.9.8** sandbox-decision-resolver contract per `.harness/class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` operator-ratified **Reading B** 2026-06-01 (per-server sandbox-mechanism fields). Authors the missing spec anchor for the `SandboxDispatchDecision` 5-field carrier + `SandboxDecisionResolver` callable the runtime carried since v1.13 with **phantom §14.9.7 cites** (an execution-backed 4-agent investigation found the AC#2 blocker set is FIVE gaps {D,B,C,E,F}, not three, AND verified §14.9.7 defers only emitter/idempotency/health-check — never the resolver). The stage-5 factory builds a per-server default-policy resolver from `MCPClientConfig.{default_sandbox_tier, default_sandbox_tech, default_sandbox_provider}` (impl-declared +3 fields, faithful-minimal); under Reading B the §14.9.4 tier-floor is LIVE (resolved tier independent of `minimum_tier`; declare both consistently). Per-server-uniform (per-tool granularity future). Co-published impl wires all 5 gaps: Gap B stage-3a `host.start()`, Gap C resolver + fields, Gap D config-around, Gap E emitter `info_lookup` from host, Gap F host `shutdown()` in shutdown.py; phantom §14.9.7 docstring cites at `runtime_tool_dispatcher.py:85,:98` corrected; 18 CI-green tests (resolver/info_lookup/fields/stage-3a-start/shutdown-drain) + skipif-gated echo-MCP-via-`api.run` TOOL_STEP e2e. 1355/1355 harness-runtime non-e2e tests pass; pyright strict + ruff clean. Clearance marker `.harness/clearance/Spec_Harness_Runtime-v1_41-cleared-2026-06-01.md`. **R-100 AC #2 closes on the operator's live e2e run** (Gap D needs a live provider — ollama OR `ANTHROPIC_API_KEY`; skipif-gated; live-green is operator's run; NOT auto-closed). §14.9.7 PRESERVED VERBATIM (correct; only the code's mis-cite was phantom). Reading B chosen over the recommended identity resolver (Reading A) for a meaningful floor. 2026-06-01. v1.40 prior — Class 1 fork resolution **Reading B** apply pass per `.harness/class_1_fork_tool_step_no_operator_supplied_converter.md` operator-ratified 2026-06-01 (PR #169). §14.9.3 stage-3a `materialize_mcp_client_host_stage` factory contract extended: for a configured MCP server the factory MUST build a default-policy `MCPToolContractConverter` from two NEW operator-declared `MCPClientConfig` fields (`default_minimum_tier: SandboxTier` + `default_blast_radius: BlastRadiusTier`, conservative defaults `TIER_2_CONTAINER` / `READ_ONLY`) and pass it to the `MCPClientHost` constructor; the converter maps an advertised MCP `Tool` → AS `ToolContract` stamping the per-server defaults. Closes the converter half of the operator `api.run` TOOL_STEP path surfaced by the R-100-mvp-real-workflow-execution use-the-product probe (previously the bootstrap host carried only the raise-on-call default converter with no operator config surface). Spec body PRESERVED VERBATIM except the §14.9.3 stage-3a paragraph (Reading B clause appended); §14.9.3 stage-5 (`materialize_runtime_tool_dispatcher_stage`) PRESERVED VERBATIM. Co-published: `MCPClientConfig` +2 fields; `mcp_client_host_factory._build_default_policy_converter`; 5 converter unit tests; clearance marker `.harness/clearance/Spec_Harness_Runtime-v1_40-cleared-2026-06-01.md`. 1344/1344 harness-runtime non-e2e tests pass + 1 xfail AC#2 marker; pyright strict + ruff clean on touched files. **One necessary piece — NOT a sufficient set; R-100 AC #2 NOT closed:** a pre-merge completeness critic (PR #171) found the "converter + resolver = sufficient" framing undercounted. Known open gaps, list NOT asserted complete: (B) the stage-3a bootstrap body never calls `host.start()` (`stage_3a_cp_clients.py:48`) → registry empty → the v1.40 converter is currently *unreachable* through the bootstrap (impl/spec-conformance bug); (C) the bootstrap `RuntimeToolDispatcher` wires no `sandbox_decision_resolver` (defaults-to-raise; design decision) — filed `.harness/class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` (PROPOSING) §4-§5; (D?) bootstrap provider construction for a tool-only workflow unresolved. AC #2 closes only when the full bootstrap TOOL_STEP path is wired AND demonstrated end-to-end (echo MCP via `api.run`), proven by execution not unit tests. Deterministic xfail marker for Gap C at `test_u_rt_75_runtime_tool_dispatcher_factory.py::test_ac2_bootstrap_dispatcher_resolves_sandbox_decision`. Class 3 finding: pre-existing unconsumed `MCPClientConfig.blast_radius` overlaps `default_blast_radius`; consolidation owed at a future hygiene arc per X-AL-3. Field count +2 as ratified per fork §3 (NOT re-decided to +1). 2026-06-01. v1.39 prior — Class 1 fork resolution Reading (A) apply pass per `.harness/class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` operator-ratified 2026-05-29 (Q1=A pyyaml StrictSafeLoader + Q2=α pipeline-automation fixture + Q3→routes-to-PR-#80 + Q4=b §14.19.4 invariant 8 becomes empirically true). Re-litigation of Phase 2a G2 Q-H=b ratification: strictyaml chosen for strictness; use-the-product probe finding #16/#17 surfaced that `strictyaml.dirty_load` stringifies every YAML scalar — LLM SDKs reject string-typed `max_tokens`; operators cannot run any `inference-step` workflow via YAML manifest at HEAD. v1.39 replaces `strictyaml.dirty_load` with `pyyaml.safe_load` via NEW 78-line `StrictSafeLoader` subclass at `harness-runtime/.../lifecycle/strict_safe_loader.py` preserving all four LOAD-BEARING strictness features (duplicate-key + non-empty-flow-style ban + anchor/alias ban + native scalar typing). Empirical shim verification at advisor 56th application (78 LOC; 10/10 strictness tests pass at scratch BEFORE spec authoring). `_coerce_int_fields` helper RETIRED at U-RT-104 (native typing redundant). `_check_version` simplified to reject non-int `version` directly. dep `strictyaml>=1.7` → `pyyaml>=6.0` at `harness-runtime/pyproject.toml`. minimal.yaml + minimal.toml fixtures updated for Q2=α (pipeline-automation + single-threaded-linear + native int `max_tokens: 8`). Closes use-the-product probe finding #16/#17 + §14.19.4 invariant 8 (YAML↔TOML round-trip becomes empirically true at typed-scalar layer). NEW pattern catalogued at fork doc §4(e) — `g2-en-bloc-ratification-probe-surface` (Q-H=b + Q-(loader-admissibility) BOTH ratified en-bloc at G2; BOTH surfaced as Class 1 forks at probe; BOTH required Reading A apply within 24h). 57th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture. ZERO contract removal at any other contract; ZERO signature change at §14.19.1; ZERO cross-axis cascade (Target_Stack_Commitment_v1 §5.1 PRESERVED VERBATIM at framework-pull-discipline layer). 1305/1305 harness-runtime + 794/794 harness-cp tests pass. v1.38 + earlier lineage PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-29. v1.38 prior — Class 1 fork resolution Reading (A) apply pass per `.harness/class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` operator-ratified 2026-05-29 (Q1=A defer-to-runtime + Q2=α pipeline-automation fixture + Q3=i sole-enforcement-at-#585 + Q4=c runtime-spec-amendment + Q5=β runtime-spec-cascade). Canonical-reading amendment at §14.19.4 invariant 2 + §14.19.2 row 7 deferring topology_pattern admissibility from load-time at `WorkflowManifestLoader.load()` to runtime sub-agent-dispatch site (`sub_agent_dispatch.py:585`, `topology_dispatcher.is_topology_permitted`). Pattern-consistent extension of v1.36 Reading (β) which deferred engine_class admissibility loader → U-RT-106. Production retirement at `workflow_manifest_loader.py`: drop import of `is_topology_permitted_for_workload`; drop call at `:212`; retire `_check_topology_admissibility` method. Tests flipped from rejection-assertion to acceptance-assertion + sibling test added covering `(RESEARCH, SINGLE_THREADED_LINEAR)`. Closes use-the-product probe finding #14 (PR #79 §4(a) catalogue) + MVP-scope SE-workload runnability gap (probe findings #5 + #6). NEW pattern catalogued at fork doc §4(e) — `test-bypass-as-runtime-truth`. 56th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture. ZERO contract removal at any other contract; ZERO signature change at §14.19.1; ZERO cross-axis cascade per Q5=β (CP spec C-CP-22 §11.1 matrix design-intent PRESERVED VERBATIM). 1301/1301 harness-runtime + 794/794 harness-cp tests pass. v1.37 + earlier lineage PRESERVED VERBATIM per delta-only-spec-file convention. 2026-05-29. v1.37 prior — Substantive amendment authoring NEW `persona_tier: PersonaTier = PersonaTier.SOLO_DEVELOPER` field at §3 C-RT-03 `RuntimeConfig` schema per `.harness/class_1_fork_od_3_od_4_retire_ready_persona_tier_plumbing.md` Reading (α) operator-ratified 2026-05-28 (Q1=A + Q2=A + Q3=a + Q4=i + Q5=α). Single bundled binding-lift arc per `tenant_id` v1.22 + `default_gate_level` v1.20 + `step_dispatch_timeout_seconds` v1.31 + `skill_activation_hook_config` v1.32 precedents. Drives (a) `HarnessCompositeSampler` base_rate at `materialize_tracer_provider_stage` per OD spec §10.3 8-row table via `PER_CELL_BASE_RATE_ENVELOPE` (U-OD-12) consumer + `reject_excluded_cell` (U-OD-01); (b) `RedactionSpanProcessor` per-persona override toggle at `materialize_span_processor_stage` per OD spec §13.1 via NEW `MultiTenantOverrideRefusedError` typed surface at `harness-od/src/harness_od/redaction_span_processor.py`. Pre-v1.37 module-level `_DEFAULT_SAMPLER: Final[Sampler] = build_default_sampler()` RETIRED (forced base_rate=1.0 at every cell — was production gap blocking OD-3 PARTIAL → RETIRE-READY). Field-set cardinality 28 → 29; §3.7 RuntimeConfigSource 3-source precedence operates on the NEW field at ZERO new wire-up cost (env `HARNESS_PERSONA_TIER` / `harness.toml` `persona_tier` / CLI flag `--persona-tier`). ZERO breaking change at existing call sites (backward-compat default `SOLO_DEVELOPER` matches MVP pre-arc base_rate=1.0 behavior at all solo cells). ZERO cross-axis cascade per Q5=(α) operator-ratified scope discipline — CP spec v1.17 §6.5 `StepEffectiveBinding.persona_tier` (per-step / per-workflow gate-level / engine-class / HITL-matrix reading) PRESERVED VERBATIM and DISTINCT from per-deployment OD-axis reading; semantic-axis disambiguation surfaced at OD spec v1.26 §1.1 canonical-reading amendment. Co-published this arc: OD spec v1.25 → v1.26 canonical-reading amendments at §C-OD-10 §10.3 + §C-OD-13 §13.1; OD plan v2.24 → v2.25 canonical-reading amendment at U-OD-04 + U-OD-12 + U-OD-16; harness-runtime impl (`types.py` field + `tracer_provider.py` per-cell base_rate composition retiring `_DEFAULT_SAMPLER` + `span_processor.py` per-persona thread-through) + harness-od impl (`redaction_span_processor.py` ctor `persona_tier` kwarg + `MultiTenantOverrideRefusedError` typed-error + `persona_tier` property); 16 NEW tests at `tests/test_persona_tier_plumbing.py`; 3367/3367 tests pass + 10 skipped. **Refines H_T-OD-3 + H_T-OD-4 PARTIAL** (within-PARTIAL refinement per X-AL-2 partial-retirement-is-non-retirement at tier-up direction; per workflow v1.12 §7.4.7.3.C audit-template applied at `harness-od/CLAUDE.md` §4.1). **OD-3 gate (b)** persona-tier-aware base_rate envelope per §10.3 **CLOSED at apply arc**; OD-3 gate (a) §9.1 tail-keep-on-classification at OTLP collector boundary still deferred per §9.3 implementer-discretion. **OD-4 gate (a)** per-persona-tier override toggle per §13.1 acceptance #3 **PARTIALLY CLOSED at apply arc** (deployment-level persona_tier plumbed at materializer + multi-tenant non-toggleability NEW-enforced via `MultiTenantOverrideRefusedError`); solo-developer per-session toggle mechanism still deferred (out-of-scope at deployment-binding-time per fork doc §2.4 (α) recommendation; requires session-control-substrate arc). OD-4 gate (b) §13.2 opaque-token tokenization mode still deferred (strip-not-tokenize MVP scope-lock per advisor 29th application). Both rows remain PARTIAL (refined); no tier transit at this arc. Pipeline-advanced count 6/8 = 75.0% UNCHANGED. 26th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture — advisor caught (1) §10.3 byte-exact-read need pre-substantive (8-row matrix FIXES base_rate; NOT deferred discretion); (2) `_DEFAULT_SAMPLER` module-level refactor safety (no external callers); (3) (a) vs (b) redactor-scope routing recommendation (a) field-plumbing-no-behavior-change. Duplicate substrate-authoring caught mid-arc at empirical grep (NEW `base_rate_envelope.py` module + `base_rate_for(persona_tier, deployment_surface)` helper authored then deleted; replaced with canonical `PER_CELL_BASE_RATE_ENVELOPE` + `CellID` + `reject_excluded_cell` substrate at runtime materializer). Substrate at HEAD already canonical: `PER_CELL_BASE_RATE_ENVELOPE` (U-OD-12) + `PER_PERSONA_TIER_REDACTION` (U-OD-16) + `CellID` (U-OD-01) + `reject_excluded_cell` — v1.37 lifts consumer site; ZERO new OD substrate authored. NEW species candidate `substrate-pre-landed-consumer-deferred-multi-arc-lift` at workflow v1.12 §7.4.7.2 — mirror-shape to OD-3 batch-34 + OD-4 batch-35 precedents (substrate canonical at one arc; consumer lifts at later arc when cross-axis prerequisite — RuntimeConfig.persona_tier field — is in place). Cross-axis semantic-axis disambiguation NEW species candidate `cross-axis-semantic-axis-disambiguation-deferred-then-surfaced` — workspace-internal latent defect (persona_tier consumed at CP per-step + OD per-deployment without canonical disambiguation at spec layer) persisted across multiple spec arcs; surfaced at v1.26 §1.1 canonical-reading amendment co-published this arc. 2026-05-28. v1.36 prior — Class 1 fork resolution Reading (β) + Q2=(i) apply pass per `.harness/class_1_fork_u_rt_104_admissibility_keying_and_carrier_defaults.md` operator-ratified 2026-05-28 (Q1=β defer-to-runtime + Q2=i SF-1 doc-only reframe). Canonical-reading amendment at v1.35 §14.19.2 row 7 (`ManifestAdmissibilityError` trigger reframed — `(workload_class, engine_class)` U-CP-16 cite was wrong shape; actual mapping is `(deployment_surface, engine_class)` keyed; `deployment_surface` lives at `RuntimeConfig` not at manifest; engine_class admissibility DEFERS to U-RT-106 dispatch site per signature-without-config discipline) + §14.19.4 invariant 2 (eager-validation carve-out documenting engine_class admissibility deferral) + §14.18.4 (`RT-FAIL-CLI-MANIFEST-ADMISSIBILITY` emission site relocation U-RT-104 → U-RT-106). Architecturally-symmetric with existing `is_topology_permitted_for_workload(topology, workload)` precedent at `harness-cp/src/harness_cp/per_workload_class_topology.py:174` (both inputs manifest-internal). v1.35 body PRESERVED VERBATIM per delta-only-spec-file convention; canonical reading at v1.36 is the operative interpretation for downstream consumers. ZERO body text amendment; ZERO contract removal; ZERO cross-axis cascade verified at §14.19.6 (intra-runtime-axis). 25th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture caught the structurally-blocking defect at U-RT-104 empirical orientation BEFORE any loader code authored. Co-published this arc: plan v2.31 → v2.32 (U-RT-104 AC #11 reframe + U-RT-106 NEW AC #4 absorbing admissibility check at dispatch site); SF-1 fork doc canonical-reading addendum at §3.3 row 5 + §4.2 row 7 (Finding A cite shape) + §3.1 + §3.3 OPTIONAL/Default columns drift note + phantom `FallbackChain.default()` cite strike (Finding B per Q2=i); fork doc `.harness/class_1_fork_u_rt_104_admissibility_keying_and_carrier_defaults.md` Status PROPOSING → ✅ APPLIED-AS-READING-β + Q2=APPLIED-AS-(i); workspace `CLAUDE.md` §2.3 + §2.4 row bumps. U-RT-104 implementation resumes at worktree branch `worktree-phase-2b-u-rt-102-cli-scaffolding` post-publication. ZERO retirement event filing at this arc. NEW species-3 sub-species candidate at workflow v1.12 §7.4.7.2: `3.pre-substantive-empirical-orientation-surfaces-cross-artifact-divergence-against-production-code` — SF-1 + spec v1.35 + plan v2.31 carry same defect at THREE SIBLING ARTIFACTS in same session (2026-05-28) without empirical verification at HEAD against `engine_class_candidate.py`; detected at empirical orientation BEFORE substantive code. Same-session triple-carry pattern: candidate "session-coherence drift" sub-species at species-4 (authoring-time stale carry); workflow doc revision owed at separate arc. 2026-05-28. v1.35 prior — Phase-7 Phase-2a Gate G4 substantive amendment authoring 4 NEW H_T design surfaces per Phase 2a design substrate G1-RATIFIED (all 17 sub-questions Q-A..Q-N en-bloc operator AskUserQuestion 2026-05-28) + SF-1 G2-RATIFIED (all 6 Q-S sub-decisions en-bloc 2026-05-28). NEW §14.18 C-RT-29 `HarnessRunCLI` operator-facing CLI contract: subcommand dispatcher (run / run --daemon / daemon / inspect / shutdown per Q-J=a flat structure); Typer dispatcher per Q-I=c; one-shot mode (invokes api.run() synchronously) + daemon-client mode (MCP client to FastMCP server via Unix-socket transport per Q-K=c); SIGINT/SIGTERM propagates drain via existing drained_flag per §11; 5-level exit-code mapping (0 SUCCESS / 1 workflow-fail / 2 manifest-error / 3 config-error / 4 runtime-bootstrap-error per Q-G strict); CLI flag inventory `--config` + `--daemon` + `--output={text,json}` per Q-F=a + `--provider` + `--model` + `--tenant-id`; `--watch`/`--reload` deferred per Q-Q=b. NEW §14.19 C-RT-30 `WorkflowManifestLoader` contract: classmethod `load(path: Path) → WorkflowObject` with file-extension dispatch (`.yaml`/`.yml` → strictyaml per Q-H=b, `.toml` → tomllib stdlib per Q-B2=c); 7 typed exceptions inheriting `WorkflowManifestLoadError` (UnsupportedManifestFormat / UnsupportedManifestVersion / ManifestParse / ManifestSchema / ManifestEnumValue / ManifestStepIDCollision / ManifestAdmissibility); 17-row field-by-field projection contract to WorkflowObject + WorkflowManifestEntry + WorkflowStep per SF-1 §3.3; 9 invariants — closed schema per Q-B4=a / eager validation per Q-N=a / enum-value strictness / step-ID uniqueness / path-class neutrality / Pydantic-default discipline / idempotency / YAML↔TOML round-trip equivalence per Q-S3 / step_payload JSON-serializability per Q-S6; step_payload OPAQUE per Q-S1 + C-CP-25 §25.3.3 step-body-opaque invariant. NEW §13.4 operator-facing CLI subcommand extension: 5-subcommand registration table; Track A `harness-inspect` + `harness-shutdown` PRESERVED VERBATIM as standalone hyphenated binaries; NEW Track B `harness` parent dispatcher entrypoint `harness = "harness_runtime.cli:main"`; flat structure per Q-J=a (no nested subcommands at v1). NEW §3.7 RuntimeConfigSource 3-source layered precedence per Q-C=β: env vars (HARNESS_* prefix) → harness.toml at workspace root → CLI flags (lowest → highest priority); pydantic-settings>=2.0 NEW dep declaration owed at Phase 2b `harness-runtime/pyproject.toml`; existing 28-field RuntimeConfig field-set PRESERVED VERBATIM — config-source precedence operates on same field-set; secrets EXCLUDED from config-file per Q-L=b ADR-F5 tier-aware python-keyring with env-var fallback at LOCAL_DEV tier (plaintext config-file storage REJECTED per X-AL-3 security discipline). NEW 10 fail-class rows extending §14 taxonomy: RT-FAIL-CLI-ARG-INVALID / RT-FAIL-CLI-CONFIG-LOAD / RT-FAIL-CLI-DAEMON-CONNECTION + 7 manifest-loader fail classes mapping to §14.19.2 typed exception taxonomy. Concurrency invariant resolution Q-M: F2-05 `ConcurrentRunNotSupported` applies to same-process same-instance re-entry; per-MCP-session ctx isolation means concurrent run_workflow from distinct clients are INDEPENDENT runs NOT F2-05 triggers (daemon-mode FastMCP server MUST support concurrent independent run_workflow invocations from distinct MCP client sessions). Daemon-mode = existing FastMCP server (U-RT-62) per Q-A=α; ZERO new IPC contract (SF-2 FORECLOSED at G1). C-RT-08 §8 `api.run()` signature PRESERVED VERBATIM — CLI delegates. §14.8.3 v1.10 Q1=α CC-initiates topology PRESERVED VERBATIM — `harness run --daemon` is ADDITIONAL MCP client (NOT replacement for Claude Code). All v1.34 + v1.33 + ... + v1 lineage PRESERVED VERBATIM per delta-only-spec-file convention. ZERO cross-axis cascade verified at design substrate §9 + SF-1 §8 (CP/AS/OD/IS/CXA/ADR/ADD/PRD unchanged). Closes `class_1_tension_runtime_entrypoint_design_gap.md` Track B partition at Phase 2b implementation completion (post-G6). Phase 2a complete; G5 plan v2.30 → v2.31 + G6 Phase 2b U-RT-102..U-RT-109 8-unit L-cluster impl arc remain open. Spec growth 4192 → 4401 lines (+210 net). 2026-05-28. v1.34 prior — Class 1 fork resolution Reading H apply pass per `.harness/class_1_fork_webhook_composer_per_workflow_context_threading.md` operator-ratified 2026-05-28. Closes long-carried carrier-consumer signature divergence at §14.10.1: spec text declared 2-arg `deliver_webhook(brief, idempotency_key)` against `HITLEscalationBrief`; production carrier declared 3-arg `deliver_webhook(webhook_config, payload, idempotency_key)` against `WebhookConfig` + `WebhookPayload`. Caller at `hitl_gate_composer.py:1002` matched spec, not production — dead code (TypeError if reached). Original fork Q-set was Reading A (per-call params); pre-substantive empirical orientation surfaced the divergence + retracted Reading A at `e378c9b`; operator re-ratified NEW Reading H (hybrid-with-adapter) at AskUserQuestion 2026-05-28. Reading H ADDITIVE: NEW public method `deliver_webhook_for_brief(brief: HITLEscalationBrief, idempotency_key: str) -> WebhookDeliveryResult` projects brief → `WebhookPayload` via NEW adapter module `harness-runtime/src/harness_runtime/lifecycle/webhook_brief_adapter.py` + dispatches via existing raw 3-arg `deliver_webhook(...)` surface using ctor-supplied `webhook_config`. NEW optional ctor kw-only param `webhook_config: WebhookConfig | None = None`. Caller at `:1002` refreshes to `deliver_webhook_for_brief(durable_brief, idempotency_key)`. Existing raw 3-arg surface PRESERVED VERBATIM; existing 5 webhook composer tests + 3 U-OD-40 integration tests unchanged. Cost-attribution per-workflow concern REMAINS ORTHOGONAL (Reading H closes signature-divergence; OD-5 RETIRE-READY deployment-time gate per AS-8d precedent unchanged). 5 NEW adapter tests + 3 NEW composer brief-surface tests; 1158/1158 harness-runtime tests pass + 4 skipped. ZERO cross-axis cascade. Co-published this arc: runtime plan v2.29 → v2.30 single-unit-body amendment at U-RT-94 (additive surface; ZERO U-RT-97 amendment; ZERO new unit). Audit-trail lineage at git history: `5c16dfb` fork filing → `c249ee3` Reading-A draft → `e378c9b` reframe-retract → this commit Reading-H apply. NO retirement event filing — H_T-OD-5 RETIRE-READY preserved. v1.33 substantive content preserved verbatim. 2026-05-28. v1.33 prior — change-note-only AS-8f Q1=(C) DEFER INDEFINITELY ratification record per `.harness/class_1_fork_as_8f_managed_agents_namespace_production_only_exclusion.md` 2026-05-28 mirror of v1.17 §14.C Files-arc-indefinite-defer precedent; H_T-AS-8f STILL-BOUNDED → STILL-BOUNDED-INDEFINITELY; NO managed_agents executable consumer contract authored at v1.33; ZERO contract-body amendment; v1.32 substantive content preserved verbatim. v1.32 prior — Substantive amendment authoring NEW §14.17 C-RT-27 `SkillActivationSpanEmitter` + `SkillActivationHook` contract surface + 3 hook binding sites at production (per-LLM-dispatch at `lifecycle/llm_dispatch.py` → `activation_mode = tool_search`; per-workflow-init at `harness-cp/.../workflow_driver.py:execute_workflow` → `activation_mode = frontmatter_only`; operator-explicit `HarnessContext.activate_skill(...)` → `activation_mode = filesystem_read`) + NEW `RuntimeConfig.skill_activation_hook_config` field at §3 + NEW `HarnessContext.skill_activation_emitter` field at §4 + NEW `RT-FAIL-SKILL-ACTIVATION-STAGE-MATERIALIZE` fail class at §11. Closes H_T-AS-8d `skill.*` namespace producer-site absence per `.harness/class_1_fork_as_8d_skill_activation_surface_absence.md` Reading B + Q-set ratification 2026-05-28 (Q1=B IN-SCOPE-MVP / Q2=d hybrid all 3 hooks / Q3=i preserve Claude Code taxonomy / Q4=q NEW `lifecycle/skill_activation.py` module per Memory-tool precedent / Q5=β NO new CXA edge). Mirrors CP-18 / CP-21 / CP-22 / RT-94 operator-opt-in RETIRE-READY precedent — `None` default at `skill_activation_hook_config` = no emission (production-default preserved); non-None = operator-supplied `SkillActivationHook` Protocol implementation queried at 3 hook firing sites. H_T-AS-8d transit STILL-BOUNDED → RETIRE-READY at apply arc close (structural-criterion-B MET via factory wiring + 3 hook surfaces at production). Co-published with AS spec v1.7 §14.4 footer note + runtime plan v2.27 → v2.28 NEW L9-quindecies cluster (U-RT-99 carrier substrate + U-RT-100 emitter+factory+stage-5-wiring + U-RT-101 3 hook binding sites). harness-runtime impl: NEW `lifecycle/skill_activation.py` (SkillActivationMode StrEnum + SkillActivationHook Protocol + SkillActivationHookConfig dataclass + SkillActivationSpanEmitter class + UnknownSkillError + SkillActivationEmitterStageMaterializeError) + EXTEND `lifecycle/skills.py` (SkillManifest +2 fields version_sha + body_tokens; `compute_git_blob_sha` byte-exact-identical-to-`git hash-object` recipe; load-time computation) + EXTEND `types.py` (RuntimeConfig + HarnessContext field + activate_skill method) + EXTEND `bootstrap/mutable_context.py` + EXTEND `bootstrap/stage_5_loop_init.py` (factory binding pre-bare_dispatcher to enable hook-2 threading) + NEW `bootstrap/factories/skill_activation_emitter_factory.py` + EXTEND `lifecycle/llm_dispatch.py` (RuntimeLLMDispatcher +2 fields + hook-2 firing block + materialize_llm_dispatcher_stage signature widened). harness-cp impl: EXTEND `workflow_driver.py` DriverContext Protocol +2 fields + hook-1 firing block in execute_workflow post-drain-check + str-literal `"frontmatter_only"` cross-boundary preserving workspace dep-graph discipline (harness-cp does NOT depend on harness-runtime per `harness-cp/pyproject.toml`). 16 NEW tests at `harness-runtime/tests/test_lifecycle_skill_activation.py` covering enum/Protocol/dataclass/SkillManifest extension/compute_git_blob_sha-vs-subprocess/load-with-computed-fields/emitter-emit-6-attrs/str-literal-cross-boundary/factory-opt-out-vs-opt-in/UnknownSkillError/silent-skip/dispatcher-hook-2-emit. test_types.py `test_harness_context_declares_all_c_rt_04_fields` field-set refreshed +1. 1110/1110 harness-runtime + 708/708 harness-cp + 317/317 harness-as tests pass + 4 skipped. ZERO cross-axis cascade per Q5=β ratification (CXA v2.15 unchanged; CP / OD / ADR / ADD / PRD unchanged). v1.31 substantive content preserved verbatim. 2026-05-28. v1.31 prior — Substantive amendment authoring NEW `step_dispatch_timeout_seconds: float = 30.0` field at §3 C-RT-03 `RuntimeConfig` schema + NEW `RT-FAIL-STEP-DISPATCH-TIMEOUT` fail class at §11 failure-mode taxonomy. Closes the v1.30 §7 carve-out documented at "Adjacent observation — fork doc §7 (step_dispatch_timeout_seconds) CARVED OUT, NOT absorbed at v1.30" via Class 1 fork `.harness/class_1_fork_step_dispatch_timeout_seconds_field_extension.md` filed 2026-05-28 (commit `d591263`) + operator-ratified Reading A (Q1=A required-with-default 30.0s; Q2=30.0s default; Q3=β in-place at U-RT-02 per primitive-scalar field-set-extension precedent; Q4=p `RT-FAIL-STEP-DISPATCH-TIMEOUT` per existing taxonomy) 2026-05-28. **8-OF-8 CLOSED** at `class_3_tension_u_rt_59_spec_prose_drift.md` carry-set; carry-window total 22 + 1 = 23 delta versions × 8 days. Production binding co-published this arc: `harness-runtime/src/harness_runtime/types.py` NEW field landing (sibling to existing `drain_timeout_seconds: float = 60.0`); `harness-runtime/src/harness_runtime/lifecycle/sync_dispatcher_facade.py` NEW `StepDispatchTimeoutError` typed exception class (discriminates per-step timeout from generic `TimeoutError` for CP-side name-match handler); 3 callsite updates at `harness-runtime/src/harness_runtime/bootstrap/stage_5_loop_init.py:332/336/356` reading `config.step_dispatch_timeout_seconds` (replacing v1.30-era `config.drain_timeout_seconds` conflation per fork doc §1.1); drift-item-7 comment-marker removal at `stage_5_loop_init.py:325-331` (closure-and-cleanup); `harness-cp/src/harness_cp/workflow_driver.py` name-match handler at the `except BaseException` block mapping to `RT-FAIL-STEP-DISPATCH-TIMEOUT` per the HITLPauseRequestedSignal name-match pattern (workspace dependency-graph discipline — harness-cp cannot import from harness-runtime). 4 NEW tests (RuntimeConfig field-default + drain-independence + `StepDispatchTimeoutError` raise verification on facade + bootstrap stage-5 binding refresh) + 1 in-place test refresh at `test_d4_result_timeout_fires` (TimeoutError → StepDispatchTimeoutError) + 1 in-place bootstrap test refresh (`config.drain_timeout_seconds` → `config.step_dispatch_timeout_seconds`). 1802/1802 harness-runtime + harness-cp tests pass + 4 skipped. ZERO cross-axis cascade verified via grep at `design-substrate/` — NO CP spec / OD spec / AS spec / CXA / ADR / ADD / PRD amendment owed; intra-runtime-axis only. Pattern catalogued — **carve-out → fork-doc → ratify → apply lineage** — distinct from v1.30 bundled-absorption sub-species; operates on the single-item residual closure dimension after a bundled-absorption arc when X-AL-3 silent-absorption discipline carved one item out for proper back-flow routing. Mirrors `class_1_fork_h_t_cp_19_default_gate_level_spec_extension.md` filed → ratified → applied single-day 3-arc precedent (2026-05-27). 24th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture (pre-substantive advisor call 2026-05-28 at v1.30 bundled arc caught the X-AL-3 risk + sharpened scope 8 → 7 + 1 carve-out; the §7 routing closed at v1.31 this arc validates the discipline end-to-end). Co-published this session: runtime plan v2.26 → v2.27 U-RT-02 in-place AC amendment + harness-runtime impl + harness-cp impl + fork doc Status PROPOSING → ✅ FULLY-APPLIED + class_3_tension_u_rt_59_spec_prose_drift Status refresh 7-OF-8 → 8-OF-8 CLOSED + workspace `CLAUDE.md` row bump. 2026-05-28. v1.30 prior — Fidelity-pure citation-correction patch absorbing 7 of 8 long-carried spec-prose-vs-production-code drift items from `.harness/class_3_tension_u_rt_59_spec_prose_drift.md` (filed 2026-05-20 at U-RT-59 landing arc; carried verbatim across runtime spec v1.7..v1.29 — 22 delta versions, 8 days). NEW §1 amendment-site table at v1.30 absorbing items §§1-residual + 2 + 3 + 4 + 5 + 6 + 8 as canonical-reading amendments per delta-only-spec-file convention: (1) §14.7.6 `ctx.audit_ledger_writer` residual → `ctx.audit_writer` per C-RT-04; (2) §14.7.5 producer-side attribute carrier `harness_cp.handoff_context` → `harness_cp.topology_subagent_namespace` (real `SUBAGENT_NAMESPACE_SCHEMA` + `TOPOLOGY_NAMESPACE_SCHEMA` carrier module); (3) §14.7.3 v1.6 MVP composition table `ProposedAction(text=...)` → `ProposedAction(action_kind=ActionKind.SUB_AGENT_DISPATCH, payload={"objective": ...}, brief=payload.brief)` per real 3-field `ProposedAction` schema; (4) §14.7.4 `ChildWorkflowRunner` Protocol 5-kwarg async → 6-kwarg sync (additive `default_model_binding: ModelBinding` per C-CP-13 §13.3 brief-authoring inheritance MVP); (5) §14.7.1 + §14.7.2 step 6 + §14.7.4 async surfaces → sync surfaces (operator-ratified 2026-05-20 + un-reversed); (6) §14.7.7 INFERENCE_STEP binding via `ctx.llm_dispatcher` direct → via `SyncDispatcherFacade(ctx.llm_dispatcher)` per `stage_5_loop_init.py:148-167` (loop-capture + cancellation invariants documented); (7) §14.7.2 step 4 + 5 sibling sites `is_admissible` → `is_topology_permitted` per Path A landing at commit `e52c2da`. Item §7 (step_dispatch_timeout_seconds NEW C-RT-03 field) CARVED OUT per X-AL-3 silent-absorption discipline — ZERO production binding verified at all axis src/ trees; only comment marker at `stage_5_loop_init.py:331`; absorbing §7 as spec-only contract addition would be silent H_T design extension at Phase 7 per workspace `CLAUDE.md` §4.4; §7 routes separately as (a) Class 1 fork + production binding arc OR (b) explicit deferred-to-substantive-arc disposition at fork doc per operator-discretion timing; advisor pre-substantive consultation this session caught the X-AL-3 risk + sharpened scope from 8 → 7 items + 1 carve-out. Body text PRESERVED VERBATIM at all 7 absorbed sites; canonical-reading amendment IS canonical interpretation going forward; downstream readers MUST apply v1.30 §1 substitutions when interpreting §14.7 narrative. v1.29 substantive content preserved verbatim (`_PROVIDER_OPERATIONS` value-space canonical-reading refresh); v1.28 + v1.27 + v1.26..v1 lineage preserved verbatim. ZERO new field at production layer; ZERO new contract; ZERO signature change (production already at absorbed surfaces; spec catches up); ZERO behavior change; ZERO cross-axis cascade (verified via grep at `design-substrate/` this session — NO CP spec / OD spec / ADR / CXA / ADD / PRD cite-cascade owed). NO test addition / modification (absorption is doc-only; existing tests verify production surfaces v1.30 canonicalizes). Pattern catalogued — bundled-absorption of multi-item Class 3 spec-prose-drift fork docs filed at landing-arc time with explicit "owed at next spec revision pass" disposition; 7-item absorption in single arc well-bounded under canonical-reading amendment convention + production-already-conforms posture + cite-correction shape. Sub-species candidate — bundled-absorption of multi-item spec-prose drift at delta-only-spec-file lineage; distinct from species 3 (resolved-but-carry-stale-inherited) because the carry was always-genuine at the absorbed-text layer (production diverged from spec prose at LANDING time, not later); operates on carry-window-duration dimension (22 versions × 8 days) rather than staleness-event dimension. Co-published with fork doc Status refresh (OPEN → 7-OF-8 CLOSED-via-v1.30-bundled-absorption; §7 carved out with routing target). 2026-05-28. v1.29 prior — Fidelity-pure citation-correction patch refreshing canonical-reading at v1.27 top change-note "Adjacent observation" framing — `_PROVIDER_OPERATIONS` value-space non-conformance to §4.2 operation enum (finding (g) sibling-defect) — as **CLOSED-as-resolved-at-production-commit-ca5674b** 2026-05-26 per OD spec v1.18 §3 cross-artifact cite-cascade disposition (co-publication this arc). The v1.27 top adjacent-observation flagged the carry; the v1.28 top change-note paragraph reiterated the carry framing. Both v1.27 + v1.28 carry-text framings became stale at commit `ca5674b` (2026-05-26 19:45 -0600, 5 minutes after sibling commit `115387b` finding (f) closure) which producer-side-conformed `_PROVIDER_OPERATIONS` from `dict[str, str]` (API method-name string values) → `dict[str, GenAiOperation]` (canonical §4.2 enum members; all 3 providers map to `GenAiOperation.CHAT`). Post-`ca5674b` line-number reference is `:497-509` (was `:484-488` at v1.27 carry-text); post-`ca5674b` span name is e.g. `chat claude-opus-4-7` (was `messages.create claude-opus-4-7`); `gen_ai.operation.name` attribute emits `"chat"` (was API method name). v1.28 substantive content preserved verbatim (§14.5 body cite refresh `gen_ai.system` → `gen_ai.provider.name`); v1.27 + earlier lineage preserved verbatim. Body text PRESERVED VERBATIM; canonical-reading amendment is canonical interpretation going forward. ZERO contract change; ZERO signature change; ZERO behavior change. Finding (h) `provider_name` value-space partial-conformance closure at OD spec v1.18 §1.2 + fork doc §10 (`ollama` is OTel-conformant under open-known-values discipline); no runtime-spec amendment owed for (h). Co-published with OD spec v1.18 (NEW delta closing (g)+(h) + §5 FOURTH species of stale-carry-text disposition catalogued — authoring-time stale carry) + workspace CLAUDE.md row bumps. 2026-05-26. v1.28 prior — Fidelity-pure citation-correction patch refreshing canonical-reading at §14.5 body cites for the `gen_ai.system` → `gen_ai.provider.name` attribute-name rename per OD spec v1.17 §3 cross-artifact cite-cascade disposition (co-publication this arc). The v1 baseline file body at §14.5 C-RT-15 cites `gen_ai.system` at two sites (composer step 3 narrative line 1963 + TracerProvider integration narrative line 1993) as canonical-text — these cites were correct at v1 authoring (pre-OD spec v1.12 §"Adjacent observations" (f) divergence-flagging) but became stale at OD spec v1.12 amendment + producer-side conform at commit `115387b` (2026-05-26, same day) which renamed the producer attribute `gen_ai.system` → `gen_ai.provider.name` to match the OD spec §C-OD-04 §4.3 canonical declaration. The runtime spec body cites lagged the rename; v1.28 refreshes the canonical reading via NEW amendment table per delta-only-spec-file convention. Body text PRESERVED VERBATIM at lines 1963 + 1993; the canonical-reading amendment is the canonical interpretation going forward. v1.27 substantive content preserved verbatim (§14.5 C-RT-15 "Deferred to implementation discretion" sub-section span-name convention amendment + all v1.26..v1 lineage). ZERO contract change; ZERO signature change; ZERO behavior change. v1.27 top change-note "Adjacent observation" on `_PROVIDER_OPERATIONS` value-space non-conformance NOT closed at v1.28 (carried forward per FM-2 single-focus arc scope; sibling-defect surfaced as OD spec v1.17 finding (g) for future operator-routed arc). Co-published with OD spec v1.17 (NEW delta closing finding (b) as CLOSED-as-resolved-at-115387b) + harness-od/CLAUDE.md H_T-OD-2 retirement entry in-place edit + workspace CLAUDE.md row bumps. 2026-05-26. v1.27 prior — GenAI span-name format Class 1 fork resolution R1 follow-on per `.harness/class_1_fork_genai_span_name_four_way_drift.md` §7.4.2 + §7.5a operator-ratified 2026-05-26. Single-line STRIKE at §14.5 C-RT-15 "Deferred to implementation discretion" bullet on span-name convention. The v1.26-and-earlier informal deferral suggestion `gen_ai.{provider}.{model_or_method}` REMOVED — the suggestion was the lineage of production divergence from OD spec C-OD-04 §4.1 per fork §7.5a S6 finding (production at `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py:324` silently followed the runtime-spec informal suggestion rather than the OD-spec authoritative declaration). Bullet replacement: cross-reference to OD spec v1.12 §C-OD-04 §4.1 with no inline format suggestion + axis-ownership statement (OD owns span-name format; runtime spec does NOT). Adjacent finding (g) surfaced: `_PROVIDER_OPERATIONS` dict values are API method names (`messages.create` / `chat.completions` / `chat`), NOT §4.2 operation enum values — post-R1 span names like `messages.create claude-opus-4-7` conform to v1.12 §4.1 2-token shape but the `operation` token is non-conformant to §4.2 enum; sibling to OD spec v1.12 finding (f); NOT patched per FM-2. Co-published this session: OD spec v1.12 (`43f6199`); R2 follow-on (`5221264`); OD plan v2.19 (`65922eb`); R1 production rename + test (sibling commit). ZERO contract / signature change at runtime spec; the runtime spec amendment is purely a doc-hygiene STRIKE removing a misleading informal suggestion. 2026-05-26. v1.26 prior — Reading A path 1 absorption of fork `class_1_fork_u_rt_94_webhook_delivery_composer_binding_chain_absence.md` 2026-05-24-25: NEW §14.16 C-RT-26 `materialize_webhook_delivery_composer_stage` contract surface (sibling to §14.13 C-RT-23 + §14.14 C-RT-24 + §14.15 C-RT-25) + `WebhookDeliveryComposerConfig` empty-marker sub-model + §3 C-RT-02 NEW field `webhook_delivery_composer_config: WebhookDeliveryComposerConfig | None` + §4 C-RT-04 NEW field `webhook_delivery_composer: WebhookDeliveryComposer | None` + NEW fail class `RT-FAIL-WEBHOOK-COMPOSER-STAGE-MATERIALIZE` + §14.8.8.1 step 0 precondition CANONICAL-READING AMENDMENT extending v1.25 single-binding to OR-form joint-binding (`pause_resume_protocol is None OR webhook_delivery_composer is None` → sync fall-through) + §14.8.8.6 composition claim CANONICAL-READING AMENDMENT acknowledging binding-chain authoring. Closes the empirical-verification gap at §14.8.8.1 step 3 (`ctx.webhook_delivery_composer.deliver_webhook(...)` cite that was unreachable pre-v1.26 because the binding chain was never authored). ZERO cross-axis cascade. Co-published with CP spec v1.17 + v1.18 + runtime plan v2.25 + L9-quaterdecies (U-RT-96/97/98) impl + U-RT-94 composer body + U-RT-95 driver catch + e2e matrix. 1754 tests pass + 4 skipped. v1.25 prior — D9 step 0 precondition (pause-resume-only binding check) + D10 NEW §14.8.8.9 ResumeContextHolder sidecar carrier per advisor-caught pre-impl gaps + operator AskUserQuestion 2026-05-24. v1.24 prior — NEW §14.8.8 durable-async cell HITL composition (6-step composer body + HITLPauseRequestedSignal class + _evaluate_cell_synchrony_tolerant helper + driver-side signal handling + resume-side one-shot delivery + 6 invariants) per `.harness/scope_hitl_pause_trigger.md` Reading A scope. v1.23 — Narrow-scope bookkeeping refresh 2026-05-24: §14.15.2 step 7 self-flag flipped (was "CXA v2.9 §0.3 action-id prefix enumeration MAY need refresh ... NOT patched per FM-2" → now "patched at CXA v2.10 §0.3 per v1.22 → v1.23 paired co-publication arc"); clarifies CXA-side canonicalization at v2.6 §2.3.7 ValidatorFramework row vs runtime-side production-consumer at v1.22 §14.15.2 step 7. Co-published with CXA v2.9 → v2.10 §0.3 `validator:` discriminator production-consumer cite refresh. ZERO contract / signature / AC / behavior change; ZERO cross-axis cascade. FM-2 item (c) from Reading B close. v1.22 prior — Reading B full validator-composer arc absorption per `.harness/reading_b_validator_composer_arc_scoping.md` Mode-3 systems-architect recommendation + operator AskUserQuestion ratifications 2026-05-24 ((D2) = Open Reading B NOW; (D1) = Q2(c-i) absorb cite-completeness fix into amendment). NEW §14.15 C-RT-25 `ValidatorEscalationGateComposer` mid-step re-entrant HITL composition contract surface (sibling to §14.13 C-RT-23 + §14.14 C-RT-24); 4 amendment sites at §14.8.2: step 3 UN-FORECLOSED (VALIDATOR_ESCALATION placement-trigger evaluator returns match; `HITLPlacementForeclosedAtV19Error` raise REMOVED); step 4c REPLACED v1.9 MVP `placement.requires_hitl` shortcut → full 4-axis `_hitl_required(persona_tier, blast_radius_tier, mcp_server_trust_tier, per_tool_gate_level)` per C-CP-19 §19.1 multiplicative max() composition; step 4d REPLACED `DEFAULT_FULL_PALETTE` → `compute_effective_palette(gate_level, cross_trust_state, validator_escalation_brief)` UNION-intersection per C-CP-19 §19.4 deny-row + C-CP-21 §21.3 cross-trust-boundary (Q2(c-i) cite-completeness fix); §14.8 failure-mode taxonomy: `RT-FAIL-HITL-PLACEMENT-FORECLOSED-AT-V19` REMOVED + NEW `RT-FAIL-VALIDATOR-ESCALATION-GATE-COMPOSE` permanent fail class; §0.6 Q5 ratification REVERSED. 5 adjacent harmonization sites updated (§14.8.4 placement-trigger evaluator row; §14.8.6 invariants; §14.8 deferral pointer; §14.13.5 invariant 4 OPEN→LANDED-AT-V22; §14.13.7 deferrals updated). Authority anchors: C-CP-28 §25.3 sync post-dispatch lifecycle + §25.4 invariant 2 "after dispatch, before ledger append" + §25.4/§25.7 invariant 4 "ESCALATE always emits HITL gate". ZERO cross-axis cascade per scoping doc §3 + §5 (CXA v2.9 / CP spec v1.13 / OD spec v1.11 / ADR-D1 v1.2 / D5 v1.4 / D6 v1.2 all unchanged). Co-published with runtime plan v2.20 → v2.21 NEW L9-duodecies cluster (U-RT-90/91/92) + impl arc 4 commits (`acc33bb` U-RT-90 helpers + `1c10f89` U-RT-91 composer + `145b8e6` / `e394074` U-RT-92 e2e). 29/29 Reading-B tests PASS at HEAD. Reading B fork doc §3.2 STATUS: LANDED-AT-V22. 2026-05-24. v1.21 prior — Narrow-scope CP composer authoring arc absorption per operator-ratified AskUserQuestion 2026-05-24 ("driver-invocation-only" scope). NEW §14.14 C-RT-24 `materialize_pause_resume_protocol_stage` stage-5 LOOP_INIT factory + `PauseResumeProtocolConfig` empty-marker sub-model (per ValidatorFrameworkConfig precedent); NEW optional field `pause_resume_protocol_config: PauseResumeProtocolConfig | None` at §3 C-RT-02 RuntimeConfig; NEW fields `pause_resume_protocol: PauseResumeProtocol | None` + `pause_requested_flag: asyncio.Event` (sibling-pattern to `drained_flag`) at §4 C-RT-04 HarnessContext; NEW fail class `RT-FAIL-PAUSE-RESUME-STAGE-MATERIALIZE`. C-CP-26 §26 PauseResumeProtocol class body landed at U-CP-62 cluster 10-CP-B `49617e7` consumed via new workflow_driver per-step pre-entry pause-trigger detection (sibling check to `drained_flag.is_set()` at line 549) + entry-point resume detection firing `protocol.attempt_resume(snapshot, material_diff_policy=STRICT)`. Mirrors v1.18 §14.13 validator-framework precedent (Reading A scope discipline). NO CP spec / OD spec amendment owed — per OD spec v1.11 §C-OD-30.4.5 + CP spec v1.13 §26.7 workflow_driver invocation + per-step pause-trigger source are implementer-discretion; v1.21 executes those deferrals at the runtime spec layer. ZERO cross-axis cascade per spec §14.14.6 (CXA v2.9 unchanged; CP spec v1.13 unchanged; OD spec v1.11 unchanged). Co-published with runtime plan v2.19 → v2.20 NEW L9-undecies cluster (U-RT-87/88/89) + impl arc 4 commits (U-RT-87 + U-RT-88 + U-RT-89 driver + U-RT-89 e2e) + batch-18 retirement event H_T-CP-22 PARTIAL → RETIRED (joint single-batch transit; FOURTH RETIRE-READY → RETIRED close in ledger history; workspace 27/49 = 55.1% RETIRED; CP-axis 14/22 = 63.6%). 2026-05-24. v1.20 prior — AdvReview 07 Class 1 F1-01 inline doc-drift fix per `Adversarial_Review_07_Runtime_v1_18_+_v2_17.md`. §14.13.3 stage-4 ordering paragraph strict-total-order enumeration (`tracer_provider → audit_writer → cost_chain → collector_daemon → materialize_validator_framework_stage`) REPLACED with partial-order phrasing — validator framework runs AFTER all 4 sibling stage-4 OD-bucket bindings; intermediate sibling ordering unconstrained. Resolves self-contradiction with §"Adjacent defects surfaced" (v) at v1.18 + harmonizes with plan U-RT-84 AC #4 partial-order phrasing + matches empirical bootstrap order at HEAD (`stage_4_od.py`: `tracer_provider → collector_daemon → cost_chain → audit_writer`). ZERO contract / signature / AC / behavior change. 2026-05-24. v1.19 prior — Cite-cascade absorption pass per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24). 17 ValidatorFramework-context cite sites retagged §25.x → §28.x / C-CP-25 → C-CP-28 across §3 + §4 + §14.13 + §14.13.1 + §14.13.2 + §14.13.3 + §14.13.6 + 2 historical change-notes (v1.14→v1.15 path β fork + v1.16→v1.17 §(b)) per uniform-retag discipline. v1.6-lineage WorkflowDriver cites (§25.2.1 StepExecutionContext, §25.3.3.4 step-dispatch try/except boundary, §25.3.3 step-body-opaque) preserved verbatim. Reading B full validator-composer arc remains OPEN at fork doc §3.2. ZERO contract / signature / field-set / AC / behavior change — fidelity-pure citation-correction patch under FM-2. 2026-05-24. v1.18 prior — Class 1 fork validator-composer arc stage-4 absence Reading A ratified absorption per `.harness/class_1_fork_validator_composer_arc_stage_4_absence.md` §3.1 (operator-ratified 2026-05-24 post-batch-16 close at `1c55138`): NEW §14.13 C-RT-23 `materialize_validator_framework_stage` stage-4 OD-bucket factory + `ValidatorFrameworkConfig` empty-marker sub-model (per SandboxDecisionPolicy precedent); NEW optional field `validator_framework_config: ValidatorFrameworkConfig | None` at §3 C-RT-02 RuntimeConfig; NEW field `validator_framework: ValidatorFramework | None` at §4 C-RT-04 HarnessContext (type narrowed from v1.17-era `object | None` to CP spec v1.11 §25 Protocol surface `harness_cp.validator_framework_types.ValidatorFramework`; concrete body at `harness-cp/src/harness_cp/validator_framework.py:130`); NEW fail class `RT-FAIL-VALIDATOR-STAGE-MATERIALIZE`; consumed by existing C-CP-25 §25.3.3.4 driver hook at `workflow_driver.py:668` (preserved verbatim — no driver code change at v1.18); operator-opt-in RETIRE-READY pattern per spec §14.13.6 (mirrors CP-16/CP-18/AS-2 RETIRE-READY shape); §14.8.2 deferrals (VALIDATOR_ESCALATION foreclosure at step 3 + 4-axis `_hitl_required` composition at step 4c + cross-trust-boundary palette restriction at step 4d) PRESERVED VERBATIM per Reading A scope discipline (verified byte-exact via `git diff 1c55138..1707867`; 198 insertions / 1 deletion total). Reading B full validator-composer arc remains OPEN at fork doc §3.2 for future operator-discretion routing. ZERO cross-axis cascade per fork doc §5 — CXA v2.8 unchanged; CP spec v1.11 unchanged; OD spec v1.9 unchanged. Co-published with runtime plan v2.17 NEW L9-decies cluster (U-RT-83/84/85). Adversarial Review 07 CLEARED at 0 Class 3 / 0 Class 2 / 3 Class 1 documentation-drift findings. Batch-17 retirement-event filed restoring H_T-CP-21 from batch-15 DOWN-classification to RETIRED via Reading A resolution path (FIRST corrective close in ledger history; THIRD RETIRE-READY → RETIRED close). v1.17 prior — Class 1 fork H_T-CP-16+17 §16 ratified absorption per `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` (2026-05-23, same session as §11 PROVISIONAL filing + §13 systems-architect Mode 3 recommendation + §14 routing): NEW §14.12 C-RT-22 `MemoryToolRegistry` + `MemoryToolStorageBackendProtocol` contract (5 CRUD callbacks per ADR-D3 §1.1 #11 — `view`/`create`/`delete`/`str_replace`/`insert` on `/memories` paths; 3 NEW fail classes; stage-5 `materialize_memory_tool_registry_stage` factory); NEW §14.5.1 sub-section at C-RT-15 documenting Memory tool storage-backend callback binding (inner-loop mechanism deferred to implementation discretion per FM-2); NEW optional field `memory_tool_backend_config` at §3 C-RT-02 RuntimeConfig; NEW field `memory_tool_registry` at §4 C-RT-04 HarnessContext; operator-opt-in RETIRE-READY pattern preserved per §16 §6.D (structural criterion-B MET at landing arc; full RETIRED gated on operator-bound config + local-filesystem-backend e2e per §16 §6.C v2 C.vii local-fs scope; S3 deferred). NO Files API contract authoring at v1.17 — Files arc deferred indefinitely per §14.C. ZERO cross-axis cascade per §13.6 architect recommendation — CXA v2.8 unchanged; co-published with AS spec v1.5 §14.7 footer note. v1.16 — Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 post-cluster-4-OD-E partial close `128ab4f`): §3 C-RT-02 `sandbox_decision_policy` carrier-home cite re-pointed from `(AS spec v1.3 §15 carrier)` → `(harness-core carrier)` per Q1=C-i ratification — empirical phantom-cite verification: AS spec v1.3 §15/C-AS-15 is `secret.fetch` span schema not a sandbox policy class, ZERO `SandboxDecisionPolicy` hits across all axis source trees at filing time; single-token-edit citation-correction patch under FM-2 no-extension discipline; ZERO cross-axis cascade; §14.9.1 step 5 dangling forward-cite + filing-footer staleness + v1.15 historical cite preservation surfaced as adjacent findings (not patched). v1.15 substantive content preserved verbatim — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW §14.11 C-RT-21 `RetryBreakerToolDispatcher` sibling carrier (retry-only MVP per Q1=B + Q1a=(i); no breaker; no fallback chain); NEW factory contracts at §14.9.3 (stage-3a `materialize_mcp_client_host_stage` + stage-5 `materialize_runtime_tool_dispatcher_stage` 5-step composition body); NEW optional fields at §3 C-RT-02 RuntimeConfig (`trust_policy` + `sandbox_decision_policy`); NEW fields at §4 C-RT-04 HarnessContext (`mcp_client_host` stage 3a + `tool_dispatcher` stage 5 (bound to wrapper) + `per_server_trust_evaluator` + `mcp_namespace_emitter`); amended §14.9.2 inv 4 / §14.9.3 stage-5 prose / §14.9.6 inv 6 prose; new fail class `RT-FAIL-TOOL-RETRY-EXHAUSTED` per Q2=B2 bootstrap-chain decomposition (2026-05-22); v1.14 path-β citation preserved verbatim; v1.13 NEW §14.9 + §14.10 contracts preserved verbatim) |

## §2.4 plan pointer

| Axis | Plan | Unit count |
|---|---|---|
| Runtime | `Implementation_Plan_Harness_Runtime_v2_62.md` (**v2.62 — canonical HEAD**, 2026-08-08 — the `B-118` plan leg: **ONE new unit, ZERO amended units** — **U-RT-154**, the half-open latch wiring in **FIFTEEN ACs / SEVENTEEN PD-8 probes** (ten and ten at first filing; AC #11 — epoch-guarded trial ownership — plus P11–P14 at review round 1; AC #12 — the TRIAL PROTECTION BOUNDARY, the admission emission having sat outside the round-1 guard where a raising exporter recreated the absorbing latch — plus P15 at round 3; and AC #13 — a failing release emitter must never REPLACE the propagating exception — plus P16 at round 4; and AC #14 — the exclusivity SCOPE pinned by witness, after round 6 found the §14.6.4 text overclaimed against pre-existing per-candidate pre-check semantics — at round 6; and AC #5's cell-5 charge-vs-re-arm discrimination requirement + AC #15 + P17 at the MERGE GATE, whose lens 3 measured a token-drop mutation surviving the entire suite because `state is OPEN` + a fresh `opened_at` are equally true of an inconclusive re-arm): the conjunctive cooldown admission with `now` **REQUIRED and undefaulted** (an `open` breaker with no open-instant can never recover, so a `None` default would make that unrecoverable state the SILENT fallback) and a refusal for the `opened_at is None` illegal state; the **one-line-but-load-bearing** `should_attempt()` narrowing to `state is closed`, which **IS** the single-trial permit, plus NEW `re_arm_half_open_trial(now)` (a no-op unless still `half_open` — the *recorded-neither-outcome* test as a state read rather than a second flag); the ONE-attempt trial cap; **BOTH directions witnessed end-to-end through the REAL composer**, since *a green state-machine unit test is NOT the witness — the machine was already green and the gap was REACHABILITY*; a three-branch concurrency witness whose interleaving is **GUARANTEED** by an `asyncio.Event`; one witness per inconclusive cell (3/6/7/8/9), cell 8 load-bearing on the release sitting on a `BaseException` arm and cell 9 asserting an emission **ABSENCE**; the trial-spacing witness discriminating the ratified FRESH cooldown from the unratified alternative; and **AC #8, the deliberate `B-116` witness-strengthening roster** C9's A1 rec 2 had forbidden while the path was unreachable — seven rows strengthened (the Probe-C **clock-advance** form discriminating *never-opened* from *opened-and-reset*; the 401 / response-parsing **recovery-completion positive controls**; the exhaustion-identity admissibility loop), three named UNCHANGED, `B-115`'s IS-side W-D witnesses UNTOUCHED. **TEN PD-8 probes**, each naming the witness that must be seen RED. **No lock, and the absence is a DECISION** (one event loop → the hazard is interleaving at the `await`, not a data race; contrast the signing breaker, which DOES lock for span-end worker threads), carried in a source comment. U-RT-152/153 LANDED/CLOSED and NOT amended per the `B-97`(a)→U-RT-149 / `B-111`→U-RT-151 / `B-115`→U-RT-153 precedent. ONE new DAG edge (U-RT-153 → U-RT-154); ZERO cross-axis edges; ZERO CXA rows; ZERO clusters; ZERO contract numbers; clearance `implementation-plan-harness-runtime-v2-62-cleared-2026-08-08.md`. Predecessor v2.61, 2026-08-08 — the `B-115` **(b′)** plan leg: **ONE new unit, ZERO amended units** — **U-RT-153**, the deterministic ledger-conflict split in four implement items: the NEW memory-family **SIBLING** type with its three shape constraints (not a subclass of the store subtype; not the family base; declares NO C-MEM-19 class, inheriting the residual); the **capture-boundary VALUE discriminator** that makes the refusal distinguishable at all (a closed `MemoryCaptureFailureKind` enum + an optional `failure_kind` field; the closed two-value `MemoryCaptureStatus` enum is NOT widened — **propagating was tried and FALSIFIED at the build**, since `FAILED`-on-conflict is a CONTRACTED outcome for two of `_capture`'s six entry points per U-MEM-26 / Codex R6+R8, so every existing caller's control flow stays byte-unchanged); **THREE** executor re-type surfaces through ONE shared helper (the register row named ONE; grounding found three and the two it missed escaped **RAW** onto the staircase — both proven reachable **BY EXECUTION** before scope was fixed, per the row's own no-code-read-inference requirement); and the paired classifier-admission (arms 6 → 7) / waiver-tuple (**4 → 5**) move, which MUST happen together because §14.6.3's classifier-consistency rule makes a one-sided change silently wrong. **TEN ACs** incl. the four-part determinism set at the LEDGER, a **structural** single-direct-append assertion (a fourth append site would leak the raw ledger type silently — a shape no behavioural sweep can cover), the type-level partition witness in **FOUR** directions, and the `B-84` W-5 `== 2` assertion PRESERVED VERBATIM. **U-RT-152 is LANDED/CLOSED and NOT amended** — a NEW unit per the `B-97`(a)→U-RT-149 / `B-111`→U-RT-151 precedent. ONE new DAG edge (U-RT-152 → U-RT-153); ZERO cross-axis edges; ZERO CXA rows; ZERO Memory-plan delta; clearance `implementation-plan-harness-runtime-v2-61-cleared-2026-08-08.md`; predecessor v2.60 — 2026-08-08 — the `B-116` plan leg: **ONE new unit, ZERO amended units**: **U-RT-152** — the §14.6.3 breaker-waiver guard (≈30–35 src lines across `retry_breaker_fallback.py` + `llm_dispatch.py`, incl. the row-2a re-type to NEW `LLMDispatchPayloadShapeInternalError`), the t1+t2 waived-charge emission, and the witness set — Probes B/C as ASSERTIONS (chain-length amplifier foreclosed; null-topology foreclosed), positive controls incl. the store-subtype BY-NAME control, the **3/25 raise-site partition witness** (type drift fails a test), PD-8 throughout; exhaustion-identity witnessed (waiver changes the CHARGE, never control flow); t4/t5 carried as declared obligations (no durable-persistence path at HEAD). ONE new DAG edge (U-RT-58 → U-RT-152); ZERO cross-axis edges; ZERO CXA rows. Also discharges the standing v2.60 probe-text rider at its §2 (the `B-111` closure-bullet trio + the #1263 ENOENT-narrowing note; ONE item carried forward by name — its detail was never durably recorded, the #1256/#1263 gate-log rows having never been committed). Prior: **v2.59**, 2026-08-07 — the `B-111` plan leg, operator-ratified **disposition (a)**: **ONE amendment site + ONE new unit**. **U-RT-150 AC #15** gains an **APPENDED narrow qualifier** (its existing body PRESERVED VERBATIM — no word retracted, reworded or reordered) scoping the no-removal clause to the **SWEEP's PAYLOAD-orphan accounting path**: the sweep half — `_ENTRY_GLOB` / `_ORPHAN_GLOB` enumeration and everything downstream (count/age accounting, the AC #8 gauge at **both** surfaces, candidate reporting, reclaim-as-ciphertext) — is **PRESERVED UNQUALIFIED**, **no glob widens**, the prefix constant is unchanged, and the **`.tmp-*` PAYLOAD-orphan semantics are BYTE-UNCHANGED**; while the record's **OWN publication path MAY** remove stale leftovers of its **OWN** `_GC_OBSERVATION_RECORD_TEMP_PREFIX`, **prefix-scoped by construction** and **under the locks the publication already holds**. Deciding argument is **PLAN-TEXT CORRECTNESS**: AC #15's clause is stronger than its own stated reason (which is entirely about **payload classification**), and combined with the disjointness its first half **requires** it left a killed publication's temporary reclaimable by **no path at all** — AC #15 as written was the sole reason `B-111` was open. **The Runtime SPEC is UNTOUCHED — v1.111 stands and NO spec delta is owed**, determined by direct read: §14.8.11.1 *"The observation record's carrier"* commits the publication mechanism + lock scope and **explicitly acknowledges the orphan** while stating **no disposition** for it. **The qualifier is a PERMISSION, not an obligation — deliberately**: U-RT-150 is **LANDED and CLOSED** (PR #1237) and a MUST there would make its conjunctive closure criterion **retroactively false**, so the MUST lives at **NEW unit U-RT-151** per the **`B-97`(a) → U-RT-149** precedent. U-RT-151 = FOUR ACs / ~15 lines across TWO functional sites plus a collected-list parameter threaded between them (four signature/call touch points: `_observe_candidates` `:1224`–`:1230` / `:1130`, `_publish_observation_record` `:1282` / `:1269`): the prefix-scoped cleanup with an **exact-set** scope refusal, whose **ENUMERATION MUST RUN OFF THE CROSS-PROCESS LOCK** (`_publish_observation_record` is reached at `:1130` **inside** the lock opened at `:1111`, so a root scan there is O(store size) — the cost `B-75` moved off-lock at `:1020`–`:1036` — and would falsify the method's own docstring pin at `:1317`–`:1319`; the enumeration joins the sweep's already-unlocked pass at `:1068`–`:1104`, leaving only a bounded unlink loop under the lock and excluding the path's own in-flight temp **by construction**); **ONE DECLARED re-pin** of `test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan` (the `exists()` assertion flips to a **POSITIVE** removed-by-a-LATER-PUBLICATION witness that **ATTRIBUTES** the removal to the publication path — absence alone would also pass a widened sweep glob — while the prefix-disjointness and not-reported assertion groups are **PRESERVED VERBATIM**); the `.tmp-*`-classified-exactly-as-before assertion; and the success-path no-new-surface absences (failure-path unlink `OSError` reported on the EXISTING report-log carrier — round-4 absorb). **ZERO contract numbers, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact, ZERO cluster, ZERO cross-axis edge, ZERO CXA rows** (frozen at 111); `B-110` neither closed nor widened by the delta — but the U-RT-151 impl arc **fires its standing phase-split trigger** and takes its priced fix as a companion *(round-4 absorb, which also **past-TTL age-gates** the cleanup's candidates and **reverses AC #4's failure-path silence** onto the EXISTING report-log carrier)*; `B-111` `registered_finding` → `open`, closing only at U-RT-151. v2.58 prior — 2026-08-05 the `B-96` **Reading C / form C-2** spec-leg plan absorption — **ONE NEW unit `U-RT-150`** with **FIFTEEN** acceptance criteria carrying the whole grace: the conjunctive reclaim rule, the durable candidate-filename + timestamp observation record over BOTH sweep classes, the two falsifiability surfaces (report-line field + a read-only `harness-inspect` row extension reporting the record's state THREE-WAY), the one-shot multi-invocation witness that discriminates the ratified form from the retired Reading B, and the re-grounding pass over every inverted grace-dependent witness including the `B-74` pin. **A NEW unit deliberately, NOT an amendment of the landed U-RT-145** — its AC #7 remains true and unchanged under v1.111 (the `B-97`(a) → U-RT-149 precedent). **ZERO existing units amended; ZERO new cluster; ONE new DAG edge (U-RT-145 → U-RT-150); ZERO cross-axis edge. Unit ids now run to U-RT-150.** Predecessor v2.57, 2026-07-31 the `B-100` spec-leg plan absorption — **U-RT-148 amended at THREE sites**: the §1 scope statement's source-shape enumeration and counts (TEN shapes across SEVEN carriers; `EffectFenceAddressable` spanning SIX), **AC #15(b)** and **AC #16(a)**, both widened from the single crash-reconstruction shape to all three effect-fence carriers' key-absent shapes. **SIXTEEN ACs stay SIXTEEN** — two widened, none added/removed/renumbered; #1–#14, #15(a), #16(b) PRESERVED VERBATIM. **Load-bearing rather than bookkeeping: #15(b)'s symmetric clause would ACTIVELY REFUSE the two new shapes if left unamended**, so an impl leg building to the unamended unit would satisfy every criterion and still violate v1.109 §14.14.9.2. ZERO new units / clusters / DAG edges; CXA v2.23 + OD / IS / AS UNCHANGED, determined not assumed. Clearance `implementation-plan-harness-runtime-v2-57-cleared-2026-07-31.md`. Prior `Implementation_Plan_Harness_Runtime_v2_56.md` (v2.56 — 2026-07-31 the `B-97` half (a) spec-leg plan absorption — **NEW U-RT-149** carrying the keying change, the (3a)/(3b) migration machinery, the §13.7 enumeration surface and the two §13.4 admin actions as ONE unit, because the enumeration surface is a **PRECONDITION** of the ratified (3a) disposition rather than a companion to it; **TWELVE ACs, every one by EXECUTION**, with the PD-8 mutation probe applied TWICE (the tenant component of the path composition, and the length-prefixed encoding vs. a naive delimiter concatenation) and AC #1 required to run BOTH ways — two processes AND two sequential `run()` calls in ONE process; **ZERO new cross-axis edges**, the one candidate crossing being a deliberate non-crossing; the v2.55 accounting gap (`JournalWorkflowPauseStore` is LANDED with NO owning plan unit at any Runtime plan version) **restated, not silently closed**. Clearance `implementation-plan-harness-runtime-v2-56-cleared-2026-07-31.md`; prior **v2.55**, 2026-07-30 the `B-69` spec-leg plan absorption — **NEW U-RT-148** carrying BOTH ratified surfaces under the arc's conjunctive closure criterion, with **SIXTEEN ACs (#1–#16), every one by EXECUTION** — AC #16 covering the empty-`idempotency_key` re-probe state and the SCALAR-field staleness binding — the ordering constraint restated as unit-internal AC #10, and **TWO cross-axis dependency edges: U-CP-64 (concrete) plus a PENDING, not-yet-numbered OD unit** for the §30.5.2 carrier (AC #13(c) cannot execute without it). **Unit count 147 → 148.** Clearance `implementation-plan-harness-runtime-v2-55-cleared-2026-07-30.md`. **Pointer-lag note: this row was stale at v2.53 — v2.54 (the B-39 arc) landed without refreshing it.** Prior:) `Implementation_Plan_Harness_Runtime_v2_53.md` (2026-07-23 B-33 spec+plan leg — NEW U-RT-147 rotation-pair-evidence composition-root adapter (factory + integration witness only — no live production call site exists for `verify_rotation_6_steps` yet), unit count 146→147; clearance `implementation-plan-harness-runtime-v2-53-cleared-2026-07-23.md`. Prior `Implementation_Plan_Harness_Runtime_v2_52.md` (2026-07-22 B-59-A spec leg — NEW U-RT-146 cross-bootstrap capacity-authority continuity, unit count 145→146; clearance `implementation-plan-harness-runtime-v2-52-cleared-2026-07-22.md`. Prior `Implementation_Plan_Harness_Runtime_v2_51.md` (2026-07-22 B-65-A apply — NEW U-RT-145; clearance `implementation-plan-harness-runtime-v2-51-cleared-2026-07-22.md`; prior v2.50 preserved) | 151 units (U-RT-00 lineage + U-RT-01 – U-RT-151 — U-RT-150 minted at v2.58, U-RT-151 at v2.59); **v2.50 — 2026-07-19 B-48 apply plan delta (NEW U-RT-140..144: cap field, grow-on-demand executor + capacity-authority adapter, mode selection, cancellation/fence, pause/contextvar riders; clearance `implementation-plan-harness-runtime-v2-50-cleared-2026-07-19.md`). Prior v2.49 — 2026-07-18 B-51/B-52/B-54 arc plan delta (NEW U-RT-134..139 + U-RT-102 amended dispatch-only; clearance `implementation-plan-harness-runtime-v2-49-cleared-2026-07-18.md`). Prior v2.48 — R-FS-1 R-plan-1 2026-06-16: 2 NEW units — U-RT-132 (rationale-consume + span emission: `_provider_dispatch`/`_invoke_provider` accept the additive `binding_rationale` carrier [the Protocol-match]; the `llm.inference` span emitter sets `routing.binding_rationale` from the threaded rationale when present, else the existing `f"{layer}:{candidate}"` derivation) + U-RT-133 (router-injection binding: thread an injected `RouterResolutionFn` into `infer(router=…)`; production `router=None` — literal inertness, the mock router is a TEST fixture, R-impl-2 binds the real one); impl-against-cleared-spec post-CP-v1.36 §2.5 (Reading B); cross-package TYPE-RIPPLE CO-LAND with CP U-CP-99/100 (the `ProviderDispatchFn` Protocol-ization, verified by a pyright spike) → all 4 R units land in one R-impl-1 arc; ZERO spec amendment, X-AL-3-clean. Co-published CP plan v2.37 (U-CP-99/100). Clearance marker filed.** Prior v2.47 — R-FS-1 B2-plan 2026-06-16: 7 NEW units — U-RT-125 (`ServerName` + `mcp_client_hosts` dict reshape, D1) + U-RT-126 (stage-3a all-hosts factory, D1) + U-RT-127 (routing index + `RT-FAIL-MCP-TOOL-NAME-COLLISION`, D2) + U-RT-128 (dispatch tool→server resolution + ~10 consumer reshapes, D2) + U-RT-129 (identity-by-ordinal `MCPServerTrustLevel → MCPTrustTier` trust telemetry projection + telemetry-only docstring fix, D3) + U-RT-130 (per-host sandbox resolver/driver, D4) + U-RT-131 (gate-axis composer no-floor default — replaces the harmful `hitl_gate_composer.py:462` L0 constant with the L3 AUTO-mapping default at the host-less gate sites; leaf; co-land pin with CP U-CP-98 at B2-impl-3; the real per-server resolved-host feed is the `B-TOOL-GATE` forward arc); impl-against-cleared-spec post runtime-v1.51/CP-v1.34/CP-v1.35; ZERO new contract ID beyond the cleared `RT-FAIL-MCP-TOOL-NAME-COLLISION`; 0 cross-axis DAG edges. Co-published CP plan v2.36 (U-CP-98). Clearance marker filed.).** Prior v2.46 — R-FS-1 E-plan-3 2026-06-15: 2 NEW units U-RT-123 (hand-rolled etcd-style reconciliation `EnginePauseResumeSubstrate`, CAS lease, parallel to U-RT-121) + U-RT-124 (R-CXA-2 engine-layer activation, RECONCILER_LOOP: engine-class-aware factory bind + NEW non-live reconciler e2e). v2.45 — R-FS-1 E-plan 2026-06-15: 2 NEW units U-RT-121 (hand-rolled WAL segment-log `EnginePauseResumeSubstrate`, extends #475) + U-RT-122 (R-CXA-2 engine-layer activation — durable factory bind + go-live e2e **= the Path-(i) `test_u_rt_95` durable e2e + un-skip + fork flip, relocated from U-CP-94**; gives `RuntimeEngineRecoveryLoop` its first production driver). Co-published CP plan v2.34 (U-CP-93/94/95). ZERO spec amendment; X-AL-3-clean. Clearance marker filed. v2.44 R-FS-1 arc #20 (B3-plan) 2026-06-14: 6 NEW units U-RT-115..120 (smart-HITL gate-site, runtime-homed: blast resolver / `HITLAutoApprovePolicy` in-`max()` consumption / gate_level-once-thread / degradation-attr / timeout dispatch-on-mode / EDIT replace-not-merge) decomposing runtime spec v1.49 §3.8 + v1.50 §14.8.9 + design §8.2 gaps; co-published CP plan v2.33. ZERO spec amendment. Clearance marker filed. v2.43 — R-FS-1 arc #6 (B1-plan) 2026-06-13 adds 2 NEW units: **U-RT-113** `RunStatus.PARTIAL` runtime projection (`_CP_TO_RT_STATUS[PARTIAL]→'partial'` + C-RT-09 §9 `Literal` widen) + **U-RT-114** branch `AgentRole` dispatch-read (model-binding only; per-role prompt→B4) per runtime spec v1.48 §9/§14.5.3; co-published CP plan v2.32 + IS plan v2.6; runtime §2.2a materialization-site = no-new-binding → §6 Open-item O-RT-1, not a unit; clearance marker filed. Full prior set: U-RT-00..U-RT-95 + L9-quaterdecies U-RT-96/97/98 + L9-quindecies U-RT-99/100/101 + L9-sedecies U-RT-102/103/104/105/106/107/108/109 + L7-stage6-extension U-RT-110 + L7-stage6-caller-extension U-RT-111 + L4-procedural-tier-resolver U-RT-112); v2.42 — Phase 7 H_T-IS-2 substitution-retirement apply-pass impl-half 2026-05-30 per `.harness/architect_recommendation_h_t_is_2_artifact_tier_registry_wiring.md` §11.4 Q-γ operator-ratified (γ-2) 2026-05-30. NEW unit U-RT-112 authors `resolve_procedural_tier_snapshot` resolver primitive at `harness-runtime/src/harness_runtime/lifecycle/procedural_tier_snapshot.py` per IS spec v1.3 §C-IS-05 §5.2 resolver contract; residence pinned to runtime axis per Q-γ=(γ-2) (mirror precedent U-CORE-02 harness-core-impl of AS-axis contract + U-RT-99 Skill type at runtime axis). Supersedes IS plan v2.4 U-IS-18 residence-deferred placeholder (retired at IS plan v2.5 sibling delta). Recipe: `sha256(canonical_join(active_skills_versions ‖ routing_manifest_sha))` per spec v1.3 §5.2 — 2-component scope at v1.3 (prompts component deferred per spec §5.2 Deferral footer); skills sourced from `HarnessContext.skills` via `SkillManifest.version_sha`; routing manifest sha derived via `sha256(model_dump_json(by_alias=False).encode("utf-8")).hexdigest()` per spec implementer-discretion footer (RoutingManifest exposes no .sha method at HEAD). Factory `make_procedural_tier_snapshot_resolver(ctx) -> Callable[[], Identifier]` for engine-layer composers without HarnessContext access at firing time per CP spec v1.25 §16.5.7 + §16.5.8 kw-only-callable precedent (consumer-site lift at producer composers DEFERRED per Q2=narrow). 14 ACs + 14 NEW tests at `test_procedural_tier_snapshot.py`. Unit count 109 → 110 (+1). DAG: +1 node + 4 NEW edges (2 cross-package runtime→IS: U-RT-112→U-IS-07 `Identifier` + U-RT-112→U-IS-11 sidecar; 2 within-runtime: U-RT-112→U-RT-01 HarnessContext + U-RT-112→U-RT-99 SkillManifest.version_sha); acyclicity preserved (cross-package edges match existing `harness-runtime`→`harness-is` dep direction). ZERO cross-axis cascade at v2.42 per Q2=narrow ratification — producer-site lifts at ~13 sites across CP / runtime / AS composers deferred per `[[substrate-pre-landed-consumer-deferred-multi-arc-lift]]` precedent. ZERO spec amendment owed (runtime spec PRESERVED VERBATIM at v1.39; U-RT-112 implements an IS-spec contract via runtime-residence per Q-γ ratification). ZERO new dep edge at `harness-runtime/pyproject.toml` (harness-is + harness-cp + harness-core already declared). Co-published with IS plan v2.5 U-IS-18 retirement + production binding at harness-runtime resolver module + EntryPayload sidecar lift at `state_ledger_write.py` + StateLedgerEntry D-derivative field at `state_ledger_entry_schema.py` + canonicalize sidecar contribution at `entry_hash.py` + 8 NEW tests at `test_state_ledger_write_sidecar.py` + 1 schema completeness test update. H_T-IS-2 STILL-BOUNDED → PARTIAL transit per `.harness/phase-7d-retirement-events-batch-49.md`; PARTIAL → RETIRED gated on full producer-site lift per X-AL-2 second conjunct. Clearance markers at `.harness/clearance/Implementation_Plan_Harness_Runtime-v2_42-cleared-2026-05-30.md` + `Implementation_Plan_Information_Substrate-v2_5-cleared-2026-05-30.md`. 56th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture caught (1) SkillID residence checkpoint claim was stale (at harness-core not harness-runtime); (2) RoutingManifest sha derivation surface absent at HEAD (resolved at impl-discretion); (3) scope discipline at Q2=narrow holds. 1458/1458 tests pass + 10 skipped workspace-wide. NEW species candidate `[[is-spec-contract-runtime-axis-impl-cross-package-pattern]]` catalogued at workflow v1.13 §7.4.7.2 — third instance (after U-CORE-02 + U-RT-99); workspace-convention candidate. NEW species candidate `[[checkpoint-recall-vs-empirical-HEAD]]` catalogued — cardinality 1 (SkillID residence drift). 2026-05-30. v2.41 prior — Class 1 fork resolution Reading (A) apply pass per `.harness/class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` operator-ratified 2026-05-29 absorbing runtime spec v1.38 → v1.39 §14.19 Q-H=b re-litigation + `_coerce_int_fields` retirement. Single-unit-body amendment at U-RT-104: dep `strictyaml>=1.7` → `pyyaml>=6.0`; `_coerce_int_fields` helper RETIRED (native typing makes redundant); `_check_version` simplified (rejects non-int directly); NEW `lifecycle/strict_safe_loader.py` module (78 lines preserving 4 strictness features); 4 NEW tests covering native scalar typing + 3 strictness bans. ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade. Unit count 109 UNCHANGED. NEW pattern catalogued — `g2-en-bloc-ratification-probe-surface` (sub-species candidate at workflow §7.4.7.2 — cardinality 2 from Q-H=b + Q-(loader-admissibility); await third instance). v2.40 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. Co-published with runtime spec v1.39 + production code + tests + fork doc Status PROPOSING → ✅ APPLIED-AS-READING-A + clearance marker. 2026-05-29. v2.40 prior — Class 1 fork resolution Reading (A) apply pass per `.harness/class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` operator-ratified 2026-05-29 absorbing runtime spec v1.37 → v1.38 §14.19.4 invariant 2 canonical-reading amendment + production retirement of `_check_topology_admissibility` at the loader. Single-unit-body amendment at U-RT-104 retiring AC #12 (load-time topology admissibility check). U-RT-104 AC count 14 → 13. Runtime sub-agent-dispatch site (`sub_agent_dispatch.py:585`) becomes sole enforcement authority per spec v1.38 Reading A. Pattern-consistent extension of spec v1.36 → plan v2.32 Reading β precedent at U-RT-104 AC #11. NEW pattern catalogued — `landed-ac-retired-via-spec-amendment` — first explicit workspace instance of a LANDED atomic-unit AC retired via downstream spec amendment (distinct from species 2 `strike-revision-on-refined-second-tier-reason` at workflow v1.13 §7.4.7.2). Sub-species candidate at workflow §7.4.7.2 — `4.ac-retired-via-spec-deferral-to-existing-runtime-authority`; cardinality 1; await second instance. ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade per Q5=β. Unit count 109 UNCHANGED. v2.39 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. Co-published with runtime spec v1.38 + production code + tests + fork doc Status PROPOSING → ✅ APPLIED-AS-READING-A + clearance marker. 2026-05-29. v2.39 prior — Plan-doc-only delta absorbing AC #4 disambiguator-derivation closure per Reading B + firing-site-absence carry-on per `.harness/class_1_tension_u_rt_111_engine_layer_substrate_absence.md` §12 NEW (2026-05-29 post-PR-#61-merge session at main `8012777`). Operator AskUserQuestion ratification 2026-05-29 (mid-arc reframe option 1 "Document Reading B + keep AC #4 STRUCK"). v2.35 STRUCK AC #4 framing the gap as "RewrittenToolCall.semantic_variant_binding_id NOT a field on the class at HEAD"; v2.39 empirical orientation confirms Reading B derivation rule (`semantic_variant_binding_id = rewritten_call.variant.value`; the existing `HITLSemanticVariant` enum at `RewrittenToolCall.variant` IS the discriminator; StrEnum `.value` IS the opaque string composer-kwarg; ZERO field-extension at any type). Mid-arc empirical grep at HEAD `8012777` surfaced SECOND-tier finding the v2.35 STRIKE framing missed: `RuntimeHITLPlacementRegistry.rewrite_tool_call` at `harness-runtime/.../lifecycle/hitl_placement.py:187` has 6 test callers + ZERO production callers — same firing-site-absence shape as U-CP-34 STRUCK at v2.37 AC #11. v2.39 CAPTURES Reading B derivation rule in plan body §1.2 for future-applicability + PRESERVES AC #4 STRIKE on refined second-tier reason (firing-site-absence). **Cumulative ACs STRUCK at U-RT-111 = 7 of 12 UNCHANGED**; RETAINED = 5 of 12 UNCHANGED. FIRST plan-revision at U-RT-111 that does NOT change the STRUCK count (v2.35→v2.38 each added a STRIKE; v2.39 refines an existing STRIKE narrative). Upstream-blocker count UNCHANGED at 5 arcs (composition refined: HITL disambiguator-field-extension arc CLOSES at v2.39 plan-doc per Reading B captured + HITL firing-site-absence arc opens as sibling of U-CP-34 firing-site arc). H_T-RT-35 transit posture UNCHANGED at PARTIAL. ZERO impl at v2.39; ZERO unit count change (109 UNCHANGED); ZERO DAG topology change; ZERO cross-axis cascade; ZERO spec change. NEW pattern catalogues at v2.39: `[[strike-revision-on-refined-second-tier-reason]]` (distinct closure-event-class from prior `[[plan-revision-against-not-yet-built-substrate]]` sub-species — STRIKE preserved on different gap than originally cited) + `[[plan-revision-explicit-derivation-rule-under-spec-composer-kwarg-silence]]` cardinality 1 → 2 (extends v2.38 AC #3 single-instance precedent to v2.39 AC #4 plan-doc capture; 2 instances across 2 atomic units in 2-day window). 43rd application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` enabled the second-tier gap discovery BEFORE plan-revision authoring committed to "un-STRIKE AC #4" framing that would have been silent X-AL-3 extension on the firing-site axis. Standalone plan-revision PR (NOT bundled with v2.38 impl arc which merged at PR #61 main `8012777`). 2026-05-29. v2.38 prior — Phase 7 → design-phase Class 1 FOURTH same-calendar-day sequel-rescope per `.harness/class_1_tension_u_rt_111_engine_layer_substrate_absence.md` §11 NEW operator-ratified 2026-05-29. STRIKES AC #2 (workload-class-selection emission @ engine_selector.py:145 inside `materialize_engine_selector(config)`) per **substrate-lifecycle-mismatch** finding: bootstrap stage 3b CP_ROUTING (firing site) precedes stage 6 CXA_WIRING (where `cp_is_wiring` binding is built) per `harness-runtime/src/harness_runtime/bootstrap/__init__.py:101-110` `_STAGE_MODULES`; at firing site `ctx.cp_is_wiring` is unset; `materialize_engine_selector(config)` does not take a `ctx` parameter at all; actor source also unanchored. Three architectural branches (bootstrap reorder / inline-adapter signature widening / `bootstrap_emission_buffer` carrier-extension) all require spec amendment (runtime spec §1 BootstrapStage enum OR CP spec §16.5.8 clarification OR CP spec §16.5 extension). Synthesis at runtime axis = X-AL-3 silent design extension. FOURTH structural shape STRUCK at U-RT-111 in single calendar day (v2.35 missing-carrier-fields + v2.36 missing-field-set-on-caller + v2.37 primitive-scope-mismatch + v2.38 substrate-lifecycle-mismatch). REFRAMES AC #10 e2e from v2.37's 2 sites to v2.38's 1 site (pause-resume workflow-layer only); narrows AC #9 effective scope. REVERSES v2.37 §3 (e) U-CP-75 LANDED-at-v2.37-impl-arc claim — U-CP-75 carries to upstream bootstrap-emission-substrate arc. Cumulative ACs STRUCK at U-RT-111 = **7 of 12**; RETAINED at v2.38 = 5 of 12. ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade. Unit count 109 UNCHANGED. CXA v2.16 §0.4 partial transit at v2.38 impl arc: **1 PENDING → 1 LANDED** (U-CP-76 only); remaining 5 PENDING carry; **6-arc convergence** now required for H_T-RT-35 RETIRE-READY (engine-layer impl + HITL disambiguator + override disambiguator + sibling-ledger firing-site + bootstrap-emission-substrate + v2.38 impl arc). 41st application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture — advisor flagged bootstrap stage ordering as load-bearing pre-substantive ("at AC #2 firing site, `ctx.cp_is_wiring` is None — the binding chain isn't built yet"); recommended grep `_STAGE_MODULES` verification + STRIKE-via-AskUserQuestion if confirmed; empirical verification confirmed 3b → 6 ordering; operator AskUserQuestion ratified Reading (A) STRIKE. FOURTH consecutive same-calendar-day sequel-rescope at U-RT-111 ratifies sub-species `plan-revision-against-not-yet-built-substrate` cardinality 3 → 4 at workflow doc §7.4.7.2 — distinct closure-event-class at v2.38 (substrate-lifecycle-mismatch shape) from v2.35/v2.36 (downstream-substrate-absence) + v2.37 (semantic-scope-conflation). Co-published: fork doc §11 NEW + workspace CLAUDE.md §2.4 row v2.37 → v2.38 + PR #61 same-branch additional commits (plan + fork doc + AC #3 impl + AC #10 e2e + negative path test) per CLAUDE.md §11.4 mixed-posture bundled-absorption. H_T-RT-35 PRESERVED at PARTIAL post-v2.38; 5 upstream-arc blockers now (was 4 at v2.37, 3 at v2.36, 2 at v2.35). 2026-05-29. v2.37 prior — Phase 7 → design-phase Class 1 THIRD same-calendar-day sequel-rescope per `.harness/class_1_tension_u_rt_111_engine_layer_substrate_absence.md` §10 NEW operator-ratified 2026-05-29. STRIKES AC #11 (sibling-ledger emission @ sub_agent_dispatch.py:716 post-step-8 success path) per primitive-scope mismatch finding: CP spec v1.2 §15.1 (preserved through v1.26) binds `emit_sibling_ledger_entry` to **per-sibling tool-call events inside child agent execution** (`response_hash = sha256(canonicalize(tool_output))` — tool_output does NOT exist at parent dispatch moment); test fixture `harness-runtime/tests/test_lifecycle_cp_is_wiring.py:106-123` exercises §15.1 canonically (`tool="Bash"`, `canonical_args='{"cmd":"echo hi"}'`); at the parent dispatch site, `tool` + `canonical_args` slots have no spec-anchored value (brief's hash IS the F2 `response_hash` at step 8b, not §15.1's canonical_args; re-purposing conflates two hash-roles). Zero non-test callers across `harness-*/src/` (empirical grep at HEAD `9cca6d6`). The v2.34 plan row 11 conflated CP spec §15.1 (per-sibling tool-call ledger entry) with runtime spec §14.7.2 step 8b (F2 dispatch-entry write) at adjacent spec sections. CP spec v1.26 §16.5 does NOT include U-CP-34 (§16.5.7 enumerates U-CP-27/30/37/49/50/14 only; U-CP-34 predates §16.5 anchored at C-CP-15 §15.1). Synthesis at runtime axis = X-AL-3 silent design extension. THIRD structural shape STRUCK at U-RT-111 (v2.35 missing-carrier-fields + v2.36 missing-field-set-on-caller + v2.37 primitive-scope-mismatch). REFRAMES AC #10 e2e from v2.36's 3 sites to v2.37's 2 sites (workload-class-selection + pause-resume workflow-layer; sibling-ledger dropped). REVERSES v2.36 §3 (e) U-CP-34 LANDED-but-never-fired residual closure claim — residual carries to upstream sibling-ledger firing-site arc. Cumulative ACs STRUCK at U-RT-111 = **6 of 12**; RETAINED at v2.37 = 6 of 12. ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade. Unit count 109 UNCHANGED. CXA v2.16 §0.4 partial transit at v2.37 impl arc UNCHANGED from v2.36: **2 PENDING → 2 LANDED** (U-CP-75 + U-CP-76); remaining 4 PENDING carry; **5-arc convergence** now required for H_T-RT-35 RETIRE-READY (engine-layer impl + HITL disambiguator + override disambiguator + NEW sibling-ledger firing-site + v2.37 impl arc). 40th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture — advisor flagged `canonical_args` source-derivation as load-bearing pre-substantive ("brief_hash is convenient not principled — that's the response_hash for F2 dispatch event, NOT §15.1 canonical_args"); recommended single operator AskUserQuestion if zero callers exist; grep returned zero; operator AskUserQuestion ratified Reading (A) STRIKE. THIRD consecutive same-calendar-day sequel-rescope at U-RT-111 ratifies sub-species `plan-revision-against-not-yet-built-substrate` cardinality 2 → 3 at workflow doc §7.4.7.2 — distinct closure-event-class at v2.37 from v2.35/v2.36 (semantic-scope-conflation shape vs downstream-substrate-absence shape). Co-published: fork doc §10 NEW + workspace CLAUDE.md §2.4 row v2.36 → v2.37 + PR #61 same-branch additional commit per CLAUDE.md §11.4 mixed-posture bundled-absorption. H_T-RT-35 PRESERVED at PARTIAL post-v2.37; 4 upstream-arc blockers now (was 3 at v2.36, was 2 at v2.35). 2026-05-29. v2.36 prior — Phase 7 → design-phase Class 1 SEQUEL-rescope per `.harness/class_1_tension_u_rt_111_engine_layer_substrate_absence.md` §9 NEW operator-ratified 2026-05-29 same-calendar-day. STRIKES AC #1 (override caller-site invocation @ workflow_driver:777 + override_evaluator:61) per second-instance disambiguator gap finding: U-CP-14 composer `emit_override_state_ledger_entry` at `harness-cp/.../per_step_override_evaluator.py:282-315` requires `override_id` + `policy_id` as REQUIRED kw inputs per CP spec v1.26 §16.5.4 row U-CP-14 idempotency-key formula; `StepOverride` model at `harness-cp/.../workflow_manifest_entry.py:51-65` has empirical field set `{step_id, model_binding, engine_class, hitl_placement}` — NEITHER field present; CP spec v1.26 §16.5.4 has NO per-composer disambiguator note for U-CP-14 (rows U-CP-27/30/37/49/50 do); spec SILENT on derivation. Synthesis at runtime axis = X-AL-3 silent design extension per `Phase_7_Meta_Architecture_v1.md` §7.7. Same structural shape as v2.35's AC #4 STRIKE (HITL `semantic_variant_binding_id` missing from `RewrittenToolCall`). REFRAMES AC #10 e2e from "3-or-4 sites" to "3 sites" (workload-class-selection + pause-resume workflow-layer + sibling-ledger). VERIFIES AC #3 CLEAN at v2.36 empirical orientation (`PauseResumeProtocol` class methods at `pause_resume_protocol.py:263+296` IMPLEMENTED, NOT stubs; 3 firing sites at `workflow_driver.py:546+756+881` reachable). Cumulative ACs STRUCK at U-RT-111 = 5 of 12 (v2.34 original 12 ACs); RETAINED at v2.36 = 7 of 12. ZERO new units; ZERO DAG topology change; ZERO cross-axis cascade. Unit count 109 UNCHANGED (canonical-reading sequel-rescope, NOT a new unit). CXA v2.16 §0.4 partial transit at v2.36 impl arc: **2 PENDING → 2 LANDED** (U-CP-75 workload-class-selection + U-CP-76 pause-resume workflow-layer); remaining 4 PENDING carry to upstream arcs (U-CP-74 override + U-CP-77 HITL + U-CP-78 + U-CP-79 engine-layer). H_T-RT-35 PRESERVED at PARTIAL post-v2.36; now **3 upstream-arc blockers** (engine-layer impl + HITL disambiguator + override disambiguator) instead of v2.35's 2. Co-published: fork doc §9 NEW sequel finding (same PR per CLAUDE.md §11.4 bundled-absorption); workspace CLAUDE.md §2.4 row v2.35 → v2.36; runtime impl arc bundled at same PR (3 caller-site invocations: AC #2 workload-class-selection + AC #3 pause-resume 3 sites + AC #11 sub_agent_dispatch sibling-ledger). **39th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** — advisor caught AC #1 disambiguator gap upon first U-CP-14 composer signature check; sharpened "synthesize impl-discretion" framing to "halt + route per X-AL-3 — same shape as AC #4 STRIKE precedent". **Sub-species `[[plan-revision-against-not-yet-built-substrate]]` cardinality 1 → 2 at workflow doc §7.4.7.2** — SECOND instance at SAME atomic-unit (U-RT-111) in 19-hour window (v2.35 published 2026-05-29 commit `f7d6442` merged at `a35c716`; v2.36 sequel authored same calendar day); workflow-doc revision candidate strengthens empirically. 2026-05-29. v2.35 prior — Phase 7 → design-phase Class 1 back-flow apply pass per `.harness/class_1_tension_u_rt_111_engine_layer_substrate_absence.md` Reading (A) operator-ratified 2026-05-29. Narrow-scope canonical-reading amendment at U-RT-111 (v2.34 NEW unit) absorbing the pre-substantive empirical orientation findings at the impl-arc HALT triggered by ACs #8 + #9 disambiguator-availability + actor-source verification halt-conditions. **7 gaps surfaced** at HEAD `a2a0fc2` post-PR #59 U-RT-110 merge: (a) `RewrittenToolCall.semantic_variant_binding_id` NOT a field on class; (b) `PauseEvent.pause_event_id` NOT a field; (c) `resume_attempt_count` NOT on `ResumeAttempt` or `ResumeOutcome`; (d) `pause_resume_composer.py` does NOT exist at runtime path; (e) `capture_pause_snapshot(...)` is NotImplementedError stub at `harness-cp/.../pause_resume_protocol.py:106`; (f) `attempt_resume(...)` is NotImplementedError stub at line 128; (g) PauseEvent vs PauseSnapshot type-mismatch at the U-CP-78 composer signature. **5 clean:** workflow_driver.py:777 site, override_evaluator.py:61 site, step_context.parent_actor reachable, PauseResumeProtocolEventKind 2-value bijective, sub_agent_dispatch.py exists. **Decisive X-AL-2 second-conjunct finding:** the engine-layer NotImplementedError stubs ARE the H_E substitute being invoked at the substitution site; wiring caller-sites #5 + #6 to never-fired paths cannot satisfy X-AL-2 retirement criterion. The (F) FULL-WIRE-paired transit claim at v2.33 §0.3 + v2.34 §0.3 implicitly assumed engine-layer substrate was landed — empirically false at HEAD. **v2.35 STRIKES ACs #4/#5/#6/#10** as structurally-blocked; **RETAINS ACs #1/#2/#3/#7/#9/#11**; **REFRAMES AC #2 firing-site** from workflow_driver bootstrap to `harness-runtime/.../lifecycle/engine_selector.py:145` per empirical orientation; **REFRAMES AC #10 e2e** to cover only the 3-or-4 implementable caller-sites; **REFRAMES AC #12 transit posture** — H_T-RT-35 STAYS PARTIAL post-v2.35 impl arc; RETIRE-READY transit gated on upstream engine-layer impl arc (separate Class 1 back-flow owed at CP-axis design-phase routing — NOT authored at v2.35 per FM-2). ZERO unit removal; ZERO DAG edge change; ZERO cross-axis cascade. Unit count 109 UNCHANGED (canonical-reading rescope, NOT a new unit). CXA v2.16 §0.4 partial transit at impl arc: 2-or-3 PENDING → 2-or-3 LANDED (U-CP-74 + U-CP-75 + possibly U-CP-76 if AC #3 verifies clean); remaining 3-or-4 PENDING carry to upstream engine-layer impl arc + CP-axis disambiguator-extension arc. **38th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** caught the X-AL-2 unreachability framing — sharper than the disambiguator-gap framing initially drafted. **Sub-species candidate at workflow doc §7.4.7.2: plan-revision-against-not-yet-built-substrate** — v2.33 + v2.34 conflated "§16.5 composer free functions LANDED" with "engine-layer substrate LANDED" at the same `pause_resume_protocol.py` module; distinct closure events; catalogue candidate at workflow doc revision pass. NO retirement event filing at v2.35 publication. PR-shape: design-phase posture per CLAUDE.md §11; X-AL-3 CI guard at PR #48 satisfied via back-flow doc inclusion at same PR. 2026-05-29. v2.34 prior — (F) FULL-WIRE-paired SECOND HALF apply pass per runtime plan v2.33 §0.3 corrected cascade target. Authors NEW atomic unit **U-RT-111** decomposing the production-caller invocation paths that thread U-RT-110's 6 async methods at 6 firing-site locations (workflow_driver post-`resolve_step_binding` hook at `workflow_driver.py:777` + `override_evaluator.py:61` + workload-binding-time site + workflow-driver `PauseResumeProtocol` workflow-layer transitions + HITL composer post-`rewrite_tool_call_to_hitl` + runtime pause-resume composer post-`capture_pause_snapshot` + post-`attempt_resume`). CP-axis production functions + input types PRESERVED VERBATIM (S sibling-variant commitment held one layer up). 12 ACs covering 6 per-caller-site invocation contracts + dual-emission discipline preservation at AC #1 + U-CP-34 LANDED-but-never-fired residual closure at AC #11 via sub_agent_dispatch firing-site insertion per runtime spec v1.7 §14.7.2 step 8b. NEW integration test substrate `test_cp_is_caller_site_integration.py` exercising all 6 caller-sites within single e2e workflow lifecycle (load-bearing per `[[verification-shape-sharpened-grep-vs-e2e]]` for H_T-RT-35 transit). Unit count 108 → 109 (+1). DAG: +1 node + 1 within-axis edge (U-RT-111 → U-RT-110 LANDED-at-v2.33); ZERO new cross-axis dependency edges (6 §16.5 composers consumed transitively via U-RT-110); acyclic verified. **2 impl-arc risks flagged at ACs #8 + #9 per advisor:** (8) disambiguator-availability at `RewrittenToolCall.semantic_variant_binding_id` / `PauseEvent.pause_event_id` / `ResumeOutcome` + `PauseResumeProtocolEventKind` bijectivity (route Class 1 fork if absent — NO silent X-AL-3 extension); (9) actor-source verification at `step_context.parent_actor` reachability per `Spec_Control_Plane_v1_6.md` §25.2.1 v1.7 F1-01 inline-fix anchor across all 6 caller sites + engine-layer caller-context actor binding. **H_T-RT-35 PARTIAL → RETIRE-READY transit-eligible** at v2.34 impl arc completion (U-RT-111 LANDED + e2e test PASS) + CXA v2.16 → v2.17 §2.3.2 enumeration refresh sibling co-publication at retirement-batch filing arc per `[[h-t-cp-19-default-gate-level-spec-extension]]` precedent. U-CP-34 LANDED-but-never-fired residual closes at SAME ARC via AC #11 sub_agent_dispatch firing-site insertion (collapses all 7 §12.3 CP-materializable edges). Gap C PRESERVED per (C-defer); ZERO runtime-spec amendment owed; impl conforms to IS HEAD directly per CP spec v1.26 §16.5.8 Q4. CP plan v2.30 NOT OWED. NO retirement event filing at v2.34 publication (per AC #12); owed at impl arc PR merge + e2e PASS. **PR-shape:** stacked on PR #52 (v2.33) until #52 merges; rebase to main if #52 merges first. **37th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** caught the v2.33 §0.3 cascade-target mis-naming BEFORE v2.34 authoring (advisor read v2.33 §1.2 Signatures and recognized U-RT-110 methods ARE the firing-site at runtime axis). 2026-05-29. v2.33 prior — Phase 6 back-flow apply pass per `.harness/class_1_tension_u_rt_35_cp_is_wiring_gaps.md` §"Path α AUTHORIZED" operator-ratified 2026-05-29 + AskUserQuestion Q1=(F) FULL-WIRE-paired + Q2=(C-defer) Gap C deferred. NEW atomic unit **U-RT-110** decomposing runtime-side wiring of 6 CP spec v1.26 §16.5 composers (U-CP-74..U-CP-79 LANDED on main `35744ab` 2026-05-29) onto `ctx.ledger_writer.append`. Extends existing `RuntimeCpIsWiring` frozen dataclass at `harness-runtime/src/harness_runtime/lifecycle/cp_is_wiring.py` with 6 NEW async methods (`emit_override_state_ledger_entry` + `emit_workload_class_selection_state_ledger_entry` + `emit_pause_resume_state_ledger_entry` + `emit_hitl_tool_call_rewriting_state_ledger_entry` + `emit_pause_captured_state_ledger_entry` + `emit_resume_attempted_state_ledger_entry`); each builds `async _adapter(payload) -> WriteResult` closure over `(workflow_id, step_id)` constructing `WriteKey(thread_id=Identifier(workflow_id), step_id=Identifier(step_id), idempotency_key=payload.idempotency_key)` + delegating sync to `self.ledger_writer.append(payload, write_key)`; method awaits CP-axis composer with `ledger_writer=_adapter`. 10 ACs + 6 per-method unit tests + 1 integration test + 1 idempotent-on-replay test. `materialize_cp_is_wiring_stage` factory PRESERVED VERBATIM (additive at dataclass body; bootstrap-stage-6 at `stage_6_cxa_wiring.py:65` UNCHANGED). Unit count 107 → 108 (+1). DAG: +1 node + 2 within-axis edges (U-RT-110 → U-RT-12 + U-RT-35; LANDED) + 6 cross-axis edges (U-RT-110 → U-CP-74..U-CP-79; LANDED at PRs #39-#44); acyclic verified. **(F) FULL-WIRE-paired cascade declared §0.3 (corrected per impl-time-grounding-pass authoring correction):** runtime plan v2.33 → v2.34 owes 1-3 NEW caller-site invocation units threading U-RT-110's 6 async methods into production paths (workflow_driver post-resolve hook at `workflow_driver.py:777` / workload-binding-time site / HITL composer / pause-resume composer / engine-layer free-function callers). CP-axis production functions + input types PRESERVED VERBATIM. Initial v2.33 draft named "CP plan v2.30" as cascade target; impl-time grounding pass surfaced that U-RT-110's methods ARE the firing-site orchestration surface at runtime axis (mirrors U-CP-34 LANDED `emit_sibling_ledger_entry` precedent + (S) sibling-variant commitment to hold downstream axis pure). PR #52 force-pushed pre-merge to absorb the correction (mirrors PR #37/#38 in-flight-revision-pre-merge precedent). CP plan v2.30 NOT OWED. **H_T-RT-35 PARTIAL → RETIRE-READY transit GATED** on BOTH halves LANDED (U-RT-110 binding + runtime v2.34 caller-site units) + e2e per `[[verification-shape-sharpened-grep-vs-e2e]]`; filing at v2.33 alone = X-AL-2 second-conjunct violation. **Gap C deferred (C-defer):** runtime spec §12.3 prose drift NOT amended per FM-2; owed at next runtime-spec revision pass; impl conforms to IS HEAD per CP spec v1.26 §16.5.8 Q4. **CXA v2.16 §0.4 6 PENDING → 6 LANDED** at paired-cascade-completion; v2.16 → v2.17 narrow-scope revision owed at retirement-batch filing per v2.9 / v2.15 / v2.16 precedent. **U-CP-34 LANDED-but-never-fired residual** closes same arc via runtime plan v2.34 sub_agent_dispatch caller-site invocation. **PR-shape:** U-RT-110 impl single PR per L9-decies through L9-quindecies precedent. NO retirement event filing at v2.33 publication. **36th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]`** caught (P)-vs-(F) scope ambiguity at fork doc internal-inconsistency pre-AskUserQuestion. NO co-publication at design-substrate scope (CP spec v1.26 + runtime spec v1.7 + CXA v2.16 PRESERVED VERBATIM; plan-revision-pass scope only); `harness-runtime/CLAUDE.md` plan-unit anchor refresh owed at follow-on. ZERO interaction with U-RT-104 / U-RT-106 v2.32 amendments; L9-sedecies cluster PRESERVED VERBATIM. 2026-05-29. v2.32 prior — Class 1 fork resolution Reading (β) + Q2=(i) apply pass absorbing runtime spec v1.36 §14.19.2 row 7 canonical-reading amendment (`(workload_class, engine_class)` U-CP-16 cite was wrong shape; actual mapping is `(deployment_surface, engine_class)` keyed at `harness-cp/src/harness_cp/engine_class_candidate.py:57`; deployment_surface lives at RuntimeConfig not at manifest). Two single-unit-body amendments: U-RT-104 §1.4 AC #11 REFRAMED to enum-validity only (covered by AC #9 enum-strictness); deployment-surface-keyed admissibility check DEFERS to U-RT-106 §1.6 NEW AC #4 (slots after manifest-load AC, before dispatch — verifies `workflow.manifest_entry.engine_class in ENGINE_CLASS_CANDIDATES[config.deployment_surface].candidate_set` post-config-bind + maps to `RT-FAIL-CLI-MANIFEST-ADMISSIBILITY` → exit code 2 at CLI layer). v2.31 §1.6 ACs #4-#7 renumber to #5-#8 at v2.32 canonical reading. v2.31 unit bodies PRESERVED VERBATIM at U-RT-102 (§1.2) + U-RT-103 (§1.3) + U-RT-105 (§1.5) + U-RT-107 (§1.7) + U-RT-108 (§1.8) + U-RT-109 (§1.9) + cluster framing (§1.1) + DAG (§2). Architecturally-symmetric with existing `is_topology_permitted_for_workload(topology, workload)` precedent at `harness-cp/src/harness_cp/per_workload_class_topology.py:174` (manifest-internal predicates take manifest-internal inputs only; config-dependent checks at config-bound caller). ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO cross-axis cascade per spec v1.36 §14.19.6. Test cardinality net: +0 at U-RT-104 (rename `test_admissibility_check_workload_class_engine_class_not_in_u_cp_16` → `test_engine_class_invalid_enum_value_raises_manifest_enum_value_error` — coverage-equivalent to AC #9); +1 NEW test at U-RT-106 (`test_engine_class_not_admissible_for_deployment_surface_raises_admissibility_error`). U-RT-102 + U-RT-103 already LANDED at worktree branch `worktree-phase-2b-u-rt-102-cli-scaffolding` commits `176d262` + `f129390`; v2.32 unit-body preservation honors landed state. Co-published this arc: runtime spec v1.36 + SF-1 fork doc `.harness/class_1_fork_harness_run_yaml_manifest_schema.md` canonical-reading addendum at §3.3 row 5 + §4.2 row 7 (Finding A cite shape) + §3.1 + §3.3 OPTIONAL/Default columns drift note + phantom `FallbackChain.default()` cite strike (Finding B per Q2=(i)) + fork doc `.harness/class_1_fork_u_rt_104_admissibility_keying_and_carrier_defaults.md` Status PROPOSING → ✅ APPLIED-AS-READING-β + Q2=APPLIED-AS-(i) + workspace `CLAUDE.md` §2.3 + §2.4 row bumps. 25th application of `[[advisor-before-substantive-work-for-cross-axis-blockers]]` posture caught the structurally-blocking defect at U-RT-104 empirical orientation BEFORE any loader code authored. Worktree branch durable; U-RT-104 implementation resumes against reframed AC #11 + U-RT-106 absorbs NEW AC #4 at its impl arc. ZERO retirement event filing. v2.31 prior — Phase-7 Phase-2a Gate G5 substantive amendment authoring NEW L9-sedecies 8-unit linear-chain cluster decomposing runtime spec v1.34 → v1.35 (G4 LANDED at `f51874a`) NEW §14.18 C-RT-29 HarnessRunCLI + NEW §14.19 C-RT-30 WorkflowManifestLoader + NEW §13.4 operator-facing CLI subcommand extension + NEW §3.7 RuntimeConfigSource 3-source layered precedence per Phase 2a design substrate G1-RATIFIED + SF-1 G2-RATIFIED en-bloc operator AskUserQuestion ratifications 2026-05-28. U-RT-102 CLI scaffolding (Typer parent app + 5 subcommand stubs + `[project.scripts]` `harness` entrypoint + 7 acceptance criteria + 6 tests); U-RT-103 RuntimeConfigSource (pydantic-settings 3-source precedence env→harness.toml→CLI per Q-C=β + secrets exclusion per Q-L=b ADR-F5 keyring + 9 acceptance criteria + 9 tests); U-RT-104 WorkflowManifestLoader (strictyaml per Q-H=b + tomllib stdlib per Q-B2=c + 7 typed exceptions inheriting WorkflowManifestLoadError + 14 acceptance criteria + 14 + 5 fixture-driven tests); U-RT-105 LoadedWorkflow projection (manifest → WorkflowObject Protocol-conformant value via frozen Pydantic BaseModel + 6 acceptance criteria covering YAML↔TOML round-trip + step_payload JSON-serializability + 6 tests); U-RT-106 one-shot mode (synchronous asyncio.run wrapping api.run + SIGINT/SIGTERM drain via existing drained_flag + 5-level exit-code mapping 0/1/2/3/4 per Q-G strict + 11 acceptance criteria + 11 subprocess tests); U-RT-107 daemon entrypoint (Unix-socket FastMCP server reusing U-RT-62 per Q-A=α + Q-K=c + per-MCP-session ctx isolation invariant + 8 acceptance criteria + 8 integration tests); U-RT-108 daemon-client mode (MCP client over Unix-socket + RT-FAIL-CLI-DAEMON-CONNECTION handling + 7 acceptance criteria + 7 integration tests); U-RT-109 e2e integration (fixture YAML + TOML manifests + real Anthropic provider + multi-step workflow + SIGINT mid-workflow drain + AS-8d skill activation advancement + OD-5 webhook delivery advancement + 10 acceptance criteria + 10 integration tests). 8 within-cluster edges (U-RT-102→103→104→105→106→107→108→109 linear + sibling U-RT-103→U-RT-106 dependency); 1 cluster-boundary edge to existing U-RT-62 FastMCP server; ZERO cross-axis edges. DAG verified Kahn-acyclic. Unit count 99 → 107 (+8). 3 NEW dep declarations: typer>=0.12 (U-RT-102) + pydantic-settings>=2.0 (U-RT-103) + strictyaml>=1.7 (U-RT-104) — all 3 absent at design substrate §3.4 + spec §3.7 dep-set; framework-pull-discipline carve-out per CLAUDE.md §3.2 (general-purpose config-loading + arg-parsing + YAML-parsing have canonical solutions; framework-pull discipline scope is retry/breaker/workflow-orchestration). 12 adjacent observations carried forward — AS-8d + OD-5 RETIRE-READY advancement gated at U-RT-109 AC #7+#8 via operator-binds-production-substitution-surface (X-AL-2 second conjunct); harness-inspect + harness-shutdown standalone-binary preservation; iteration-2 deferrals per Q-S2 multi-document / Q-S4 JSON Schema / Q-S5 env-var interpolation; PR-per-cluster recommended shape mirroring L9-decies / L9-undecies / L9-duodecies / L9-terdecies / L9-quaterdecies / L9-quindecies precedents; adversarial review owed at G6 close per workflow §4.1. ZERO cross-axis cascade verified at design substrate §9 + SF-1 §8 + spec v1.35 §14.18.5 + §14.19.6 + plan §0.3 + §2.3. Phase 2a Gate G5 closed. G6 Phase 2b implementation arc opens. 2026-05-28. v2.30 prior — Single-unit-body canonical-reading amendment at U-RT-94 absorbing runtime spec v1.34 Reading H signature-divergence resolution per `.harness/class_1_fork_webhook_composer_per_workflow_context_threading.md` operator-ratified 2026-05-28. NEW ctor kw-only param `webhook_config: WebhookConfig | None = None` (additive; 13-param ctor preserved). NEW public method `deliver_webhook_for_brief(brief: HITLEscalationBrief, idempotency_key: str) -> WebhookDeliveryResult` projecting brief → `WebhookPayload` via NEW adapter module `webhook_brief_adapter.py` + dispatching via existing raw 3-arg `deliver_webhook(...)` surface; raises `RuntimeError` when ctor `webhook_config is None`. Caller at `hitl_gate_composer.py:1002` refreshes to brief surface (pre-v1.34 dead code now LIVE). Existing raw 3-arg surface PRESERVED VERBATIM. ZERO U-RT-97 amendment (factory does NOT thread cost-attribution substrates at this arc — orthogonal to signature-divergence per fork doc §0.2). ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO cross-axis cascade. Unit count 99 → 99. Co-published with runtime spec v1.34 + harness-runtime impl (webhook_brief_adapter.py NEW + composer ctor + brief surface + caller update) + 5 NEW adapter tests + 3 NEW composer brief-surface tests; 1158/1158 harness-runtime tests pass + 4 skipped. NO retirement event filing — H_T-OD-5 RETIRE-READY preserved at batch-28 disposition (deployment-time gate per AS-8d precedent unchanged). Audit-trail at git history: `5c16dfb` fork filing → `c249ee3` initial Reading-A draft → `e378c9b` reframe-retract → this commit Reading-H apply. v2.29 + earlier PRESERVED VERBATIM per delta-only-plan-chain convention. 2026-05-28. v2.29 prior — Single-unit-body canonical-reading amendment at U-RT-84 (the `materialize_validator_framework_stage` factory unit) absorbing the v1.24-widened signature per CP spec v1.24 §28.10.5 mechanism (a) cost-attribution hook construction. NEW kw-only params `rate_table` + `cost_chain` + `audit_writer`; when all 3 bound, factory constructs `CostAttributingValidatorHook(...)` from `harness_runtime.lifecycle.cost_attribution_validator_dispatch` + injects via ConcreteValidatorFramework optional `post_evaluate_hook` ctor param. `bootstrap/stage_4_od.py:89` updated to thread `RATE_TABLE_V1 + ctx.cost_chain + ctx.audit_writer` through (stage 4 ordering steps 6+7 binds cost_chain + audit_writer BEFORE step 8 validator framework). NEW AC #6 at U-RT-84 covering hook construction; verified via integration test `test_u_od_40_validator_webhook_integration.py`. ZERO new units; ZERO new cluster; ZERO DAG topology change; ZERO cross-axis cascade per Class 1 fork Q5=β ratification. Unit count 99 → 99. Co-published with CP spec v1.24 + CP plan v2.27 + harness-cp + harness-runtime impl + OD plan v2.23 + batch-28 retirement event H_T-OD-5 PARTIAL → RETIRE-READY. v2.28 + earlier PRESERVED VERBATIM. 2026-05-28. v2.28 prior — NEW L9-quindecies 3-unit linear-chain cluster (U-RT-99 / U-RT-100 / U-RT-101) decomposing runtime spec v1.31 → v1.32 NEW §14.17 C-RT-27 `SkillActivationSpanEmitter` + `SkillActivationHook` contract surface per `.harness/class_1_fork_as_8d_skill_activation_surface_absence.md` Reading B + Q-set ratification 2026-05-28 (Q1=B + Q2=d hybrid + Q3=i preserve + Q4=q + Q5=β). U-RT-99 = carrier substrate (SkillActivationMode StrEnum + SkillActivationHook Protocol + SkillActivationHookConfig dataclass + SkillManifest +2-field extension version_sha + body_tokens). U-RT-100 = emitter + materialize factory + stage-5 wiring + RuntimeConfig field + HarnessContext field + RT-FAIL-SKILL-ACTIVATION-STAGE-MATERIALIZE fail class. U-RT-101 = 3 hook binding sites at production (per-LLM-dispatch + per-workflow-init + operator-explicit) + AS-8d retirement gate refresh. Unit count 96 → 99 (+3). 2 within-cluster edges (U-RT-99 → U-RT-100 → U-RT-101); ZERO cross-axis cascade per Q5=β ratification (CXA v2.15 unchanged). H_T-AS-8d retirement gate transit STILL-BOUNDED → RETIRE-READY at apply-arc close (structural-criterion-B MET via factory wiring + 3 hook surfaces at production); full RETIRED gates on operator-bound `RuntimeConfig.skill_activation_hook_config` non-None + e2e exercise per X-AL-2. Co-published with runtime spec v1.32 + AS spec v1.7 §14.4 footer + harness-runtime + harness-cp impl. 2026-05-28. v2.27 prior — Narrow-scope substantive amendment at U-RT-02 in-place absorbing runtime spec v1.30 → v1.31 NEW §3 C-RT-03 `step_dispatch_timeout_seconds: float = 30.0` field + NEW §11 `RT-FAIL-STEP-DISPATCH-TIMEOUT` fail-class. Single-unit-body amendment per Q3(β) operator ratification 2026-05-28 (primitive-scalar field-set extension shape per CP plan v2.25 U-CP-13 + runtime plan v2.26 U-RT-94 precedent; binding-chain L9-N cluster shape reserved for sub-model + factory bindings). NO new units; NO new cluster; NO DAG topology change; ZERO cross-axis cascade. Closes the v1.30 §7 carve-out at the plan-side per the `class_1_fork_step_dispatch_timeout_seconds_field_extension.md` Reading A apply pass. Co-published with runtime spec v1.31 + harness-runtime impl (`types.py` field landing + `sync_dispatcher_facade.py` NEW `StepDispatchTimeoutError` typed exception class + 3 `stage_5_loop_init.py:332/336/356` callsite updates + drift-item-7 comment-marker removal at lines 325-331 + fail-class registration via name-match handler at `workflow_driver.py`) + 4 NEW tests + 1 in-place test refresh + 1 in-place bootstrap test refresh + class_3_tension_u_rt_59_spec_prose_drift Status closure (OPEN → 8-OF-8 CLOSED at §7) + workspace `CLAUDE.md` row bump + fork doc Status PROPOSING → ✅ FULLY-APPLIED. 1802/1802 harness-runtime + harness-cp tests pass + 4 skipped. Unit count 96 → 96. 2026-05-28. v2.26 prior — Narrow-scope in-place AC text amendment at U-RT-94 AC #13 absorbing CP spec v1.18 → v1.19 NEW §25.2.Y `HITLEscalationBrief.fail_detail_hash` Optional widening. v2.25 §7.2 AC #13 text "no parallel widening at v1.18" became stale at v1.19 publication; amended in-place to reflect both `fail_class=None` AND `fail_detail_hash=None` direct construction (sentinel `"0"*64` dropped). NO new units; NO new cluster; NO DAG topology change. Co-published with CP spec v1.19 + CP plan v2.23 + harness-cp impl + harness-runtime impl + test docstring update. 3008 tests pass + 4 skipped. v2.25 prior — Reading A path 1 absorption 2026-05-24-25 per fork `class_1_fork_u_rt_94_webhook_delivery_composer_binding_chain_absence.md`. NEW L9-quaterdecies 3-unit linear-chain cluster (U-RT-96 = RuntimeConfig + HarnessContext + _MutableHarnessContext + freeze() field landings + WebhookDeliveryComposerConfig empty-marker; U-RT-97 = materialize_webhook_delivery_composer_stage factory body + stage-5 LOOP_INIT wiring + RT-FAIL-WEBHOOK-COMPOSER-STAGE-MATERIALIZE fail-class; U-RT-98 = real-bootstrap e2e per L9-decies U-RT-85 verification-shape sharpening) decomposing runtime spec v1.26 §14.16 C-RT-26 binding chain. Cluster shape mirrors L9-decies (no driver-consumer at L2; consumer is U-RT-94 composer body, not driver mutation). L9-terdecies amendments: U-RT-93 +ACs #5/#6/#7 (helper drops getattr-tolerance + sentinel pattern + pyright suppression per CP v1.17 + v1.18); U-RT-94 +ACs #11/#12/#13/#14 (OR-form precondition supersedes v2.24 AC #1 single-binding reading + composer constructor extension with 4 NEW fields {pause_resume_protocol, pause_requested_flag, webhook_delivery_composer, resume_context_holder} + fail_class=None direct usage + cluster-boundary dep on U-RT-97); U-RT-95 +ACs #8/#9 (path-vi pause-resume-only-fall-through + path-vii bare-StepEffectiveBinding regression gate). Unit-count 93→96; AC-count +24; +1 within-axis cross-cluster edge + 2 within-axis-cross-package PENDING edges (U-RT-93→U-CP-14; U-RT-94→U-CP-59) CLEARED at Phase 3 step 6 CP impl arc. DAG verified acyclic at topological sort. ZERO cross-axis cascade. v2.24 prior — Single-cluster L9-terdecies amendment absorbing runtime spec v1.25 D9 step 0 precondition + D10 ResumeContextHolder sidecar (U-RT-94 +ACs #9 #10; U-RT-95 +AC #7 path-v). v2.23 prior — NEW L9-terdecies 3-unit linear-chain cluster (U-RT-93/94/95) decomposing runtime spec v1.24 NEW §14.8.8 durable-async cell HITL composition. v2.22 — Narrow-scope status-advance bookkeeping 2026-05-24: v2.21 §"Adjacent observation (a)" + line 213 carry status-advance → RESOLVED-via-CP-axis-conformance per CP spec v1.15 §19.1.1 + CP plan v2.20 U-CP-43 GateLevelInput conform (prior commit `44fc925`). Reading B pragmatic-path Class 3 informational divergence framing RETIRED — CP-axis GateLevelInput now exposes spec-canonical 4-axis; runtime thin-wrap consumes spec-canonical surface; divergence dissolved by conformance. ZERO contract / signature / AC / behavior change at runtime layer. FM-2 item (a) Reading B follow-on. v2.21 prior — Reading B L9-duodecies cluster authoring; v2.20 prior — Spec-revision-driven plan revision absorbing runtime spec v1.20 → v1.21 §14.14 C-RT-24 NEW contract per narrow-scope CP composer authoring arc (operator-ratified AskUserQuestion 2026-05-24). NEW L9-undecies 3-unit linear-chain cluster (U-RT-87 RuntimeConfig + HarnessContext fields + PauseResumeProtocolConfig empty-marker; U-RT-88 materialize_pause_resume_protocol_stage factory + stage-5 LOOP_INIT wiring + pause_context_reader composition + RT-FAIL-PAUSE-RESUME-STAGE-MATERIALIZE fail-class; U-RT-89 workflow_driver per-step pre-entry pause-trigger detection + entry-point resume detection + RunStatus.PAUSED enum extension + RunResult.pause_snapshot field + real-bootstrap e2e pause-resume cycle — mechanism α default). 3 within-cluster edges + 8 cluster-boundary edges to already-landed cluster 10-CP-B carriers (U-CP-62/63/64/65 at closure commits `49617e7`); ZERO cross-axis cascade per spec §14.14.6 (CXA v2.9 unchanged); NO CP spec / OD spec amendment triggered. Mechanism α/β/γ enumerated for e2e test-substrate selection per FM-2 implementer-discretion (recommended default α in-process pause-resume cycle). L9-undecies impl landed at `a783673` (U-RT-87 fields + sub-model) + `9e8a938` (U-RT-88 factory + stage-5 wiring + 18/18 unit tests) + `de4ae66` (U-RT-89 driver-side; 677/677 harness-cp tests preserved) + `671f195` (U-RT-89 e2e; 6/6 tests verifying binding chain + protocol method invocation + clean-resume cycle + corruption path through real `run_bootstrap`). U-RT-89 e2e satisfies H_T-CP-22 operational-criterion-B per `[[verification-shape-sharpened-grep-vs-e2e]]` discipline enabling batch-18 H_T-CP-22 PARTIAL → RETIRED joint single-batch transit (FOURTH RETIRE-READY → RETIRED close in ledger history). 2026-05-24. v2.19 prior — AdvReview 07 Class 1 F1-02 + F1-03 inline doc-drift absorption per `Adversarial_Review_07_Runtime_v1_18_+_v2_17.md`. NEW §1 U-RT-83 AC #4 reword (F1-02): mis-attribution of Pydantic v2 frozen-model validation to `@dataclass(frozen=True)` `ValidatorFrameworkConfig` disambiguated — outer `RuntimeConfig` carries Pydantic discipline; inner sub-model is dataclass. NEW §2 U-RT-83 Files section + change-note (d) retag (F1-03): recommended path updated from top-level `validator_framework_config.py` to empirically-landed `lifecycle/validator_framework_types.py` (matches §14.12 `MemoryToolBackendConfig` precedent at `lifecycle/memory_tool_types.py`; implementer-discretion clause at AC #3 absorbed the resolution at impl arc). Delta-only plan-file preservation: v2.17 file U-RT-83 unit body preserved byte-exact; canonical reading at v2.19 retags per §1 + §2 tables. ZERO contract / signature / AC-semantic / DAG change. 2026-05-24. v2.18 prior — Cite-cascade absorption per `.harness/class_1_fork_cp_spec_section_25_contract_id_collision.md` §8 Reading A apply pass (CP spec v1.13 ratified 2026-05-24) + runtime spec v1.18 → v1.19 cite-cascade absorption (this session). L9-decies cluster body (U-RT-83/84/85) at v2.17 cites the ValidatorFramework Protocol surface across 23 sites — all retag §25.x → §28.x / C-CP-25 → C-CP-28 per CP spec v1.13 §1. NEW §1 per-line retag enumeration table (23-site canonical reading). Delta-only plan-file preservation: v2.17 plan file containing L9-decies cluster body NOT edited — preserved byte-exact; canonical reading at v2.18 retags per §1. ZERO contract / signature / AC / DAG change; cluster-boundary edges to U-CP-58/59/60/61 unchanged at edge-declaration level. 2026-05-24. v2.17 prior — Spec-revision-driven plan revision absorbing runtime spec v1.17 → v1.18 §14.13 C-RT-23 NEW contract per `.harness/class_1_fork_validator_composer_arc_stage_4_absence.md` §3.1 Reading A (operator-ratified 2026-05-24). NEW L9-decies 3-unit linear-chain cluster (U-RT-83 RuntimeConfig field + ValidatorFrameworkConfig empty-marker; U-RT-84 materialize_validator_framework_stage factory + stage-4 wiring + HarnessContext type narrowing + RT-FAIL-VALIDATOR-STAGE-MATERIALIZE fail-class; U-RT-85 real-bootstrap e2e against operator-supplied ValidatorFramework instance — mechanism α-lite default). 3 within-cluster edges + 7 cluster-boundary edges to already-landed cluster 10-CP-A carriers (U-CP-58/59/60/61 at closure commits `16cf6d7`/`cdf83b1`/`5ca86aa`/`9b009d3`); ZERO cross-axis cascade per fork doc §5 (CXA v2.8 unchanged); NO spec amendment triggered beyond v1.17 → v1.18 already absorbed in spec row. U-RT-84 AC #3 implementer-discretion (i) NotImplementedError opt-in branch OR (ii) minimal construction body per `[[halt-route-split-AC-pattern]]` precedent. U-RT-85 implementer-discretion mechanism α (recommended default — in-process deterministic PASS fixture, no LLM in loop) / β (PASS + PERMANENT_FAIL coverage) / γ (env-var-gated fixture path). L9-decies impl landed at `3005643`/`d55fbd7`/`37e9d67` in worktree this session; U-RT-85 e2e 4/4 PASS confirms binding-chain operational-MET enabling batch-17 H_T-CP-21 corrective close. Adversarial Review 07 CLEARED at P5-CK + P6-CK joint review (0 Class 3 / 0 Class 2 / 3 Class 1 documentation-drift findings; Reading A scope discipline empirically preserved). v2.16 prior — Retirement-batch-driven plan revision adding NEW L9-novies singleton cluster (1 unit: U-RT-86 e2e integration test for MCP-client external server + real TOOL_STEP dispatch + `mcp.*` namespace verification + AS-2 ToolContract enforcement). Enables joint H_T-CP-18 + H_T-AS-2 RETIRE-READY → RETIRED close at follow-on batch-16 per shared MCP-client substrate framing at `.harness/phase-7d-retirement-events-batch-12.md` §1.2 + U-RT-82 close-pattern precedent at `.harness/phase-7d-retirement-events-batch-14.md` §6(a). Cluster-boundary deps to already-landed L9-septies (U-RT-71..U-RT-75); ZERO within-cluster edges (singleton); ZERO cross-axis cascade (CXA v2.8 unchanged); NO spec amendment triggered. Mechanism α/β/γ enumerated for test-substrate selection per FM-2 implementer-discretion (recommended default α in-process FastMCP). Retroactively corrects asymmetry between L9-octies (which pre-included U-RT-82 close-evidence unit) and L9-septies (which landed before the close-pattern discipline was catalogued at batch-14 §6(a)). v2.15 prior + v2.14 prior preserved verbatim. v2.15 — P6-CK adversarial-review absorption pass per `Adversarial_Review_06_Runtime_v2_14.md` (4 Class 2 + 3 Class 1 findings; F2-04 downgraded to Class 1 after grep-resolution confirmed CP §25.3.3.4 resolves at CP spec v1.7 line 13 preserved verbatim through v1.11 per delta-only-spec-file preservation chain — L9-octies cluster-opening gate CLEARED): 7 surgical edits across change-note + 4 unit bodies (U-RT-76 Implements cite-shape fix re ADR-D3 §1.1 #11 vs runtime §14.12.1 5-callback origin per F1-01; U-RT-77 NEW AC #9 covering §14.12.2 invariant 4 "no retry inside callback" per F1-02; U-RT-81 Depends on direct U-RT-76 edge added per F2-02 + AS §14.7→§14.8 sampling-row cite-replace per F2-03 + cite-discoverability note added re CP §25.3.3.4 delta-only-spec preservation chain per F2-04; coverage matrix §14.12.5 invariant 6 spec-deferred rationale per F1-03; adjacent-defects restructured into "Patched at plan-body (A)" + "NOT patched per FM-2 (B)" sub-sections per F2-01); NO DAG topology change (1 new direct edge U-RT-76→U-RT-81 declared per F2-02 — no new transitive path); NO contract addition; NO upstream-phase artifact revision triggered; 2 spec-side adjacent-defects (iv) AS §14.7 footer-note self-claim drift + (v) CP delta-only-spec-file cite-discoverability surfaced for future spec/workflow revision passes; ZERO cross-axis cascade. v2.14 prior — Class 1 fork H_T-CP-16+17 executable-consumer-absence resolution absorption pass per `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` §16 RATIFIED-AMENDED (operator-ratified 2026-05-23, Memory-only scope per §14.C; Files arc deferred indefinitely): NEW L9-octies cluster (7 units U-RT-76..U-RT-82) decomposing runtime spec v1.16 → v1.17 NEW §14.5.1 sub-section at C-RT-15 + NEW §14.12 C-RT-22 contract (`MemoryToolRegistry` + `MemoryToolStorageBackendProtocol` — 5 CRUD callbacks on `/memories` paths per ADR-D3 §1.1 #11) — U-RT-76 (Protocol + sub-model + typed-exception carriers) + U-RT-77 (LocalFilesystemMemoryToolBackend impl per §14.D local-fs-only scope) + U-RT-78 (MemoryToolRegistry class) + U-RT-79 (RuntimeConfig.memory_tool_backend_config + HarnessContext.memory_tool_registry field landings) + U-RT-80 (materialize_memory_tool_registry_stage factory + stage-5 wiring + Protocol-conformance enforcement) + U-RT-81 (C-RT-15 §14.5.1 callback-injection composer-step at llm_dispatch.py — α/β/γ inner-loop mechanism discretion per FM-2; callback→kind enum mapping locked at plan-body per advisor coherence-pass) + U-RT-82 (e2e local-fs backend + real Anthropic API messages.create with tools=[memory_tool] + deterministic-prompt fixture forcing write-path); 3 adjacent defects surfaced (§14.12.3 stage-5 ordering self-contradiction; §14.5.1 enum non-bijective with 5-callback Protocol; §14.5.1 driver-line-cite drift :380-389 → :618-635 — NOT patched per FM-2); ZERO cross-axis cascade per fork doc §5 + architect §13.6.D (CXA v2.8 unchanged); operator-opt-in RETIRE-READY pattern owed at retirement-batch-12 follow-on arc per §16 §6.D. v2.13 prior — `SandboxDecisionPolicy` Class 1 fork resolution absorption per `.harness/class_1_fork_sandbox_decision_policy_phantom_cite.md` (RATIFIED 2026-05-22 Q1=C-i): U-RT-71 + U-RT-75 cite-edited (runtime spec v1.15 → v1.16 absorption); U-RT-71 gains NEW within-axis-cross-package dependency edge on U-CORE-02 (harness-core plan v1.2 — co-published); no AC body change; no unit re-decomposition; no contract addition; single-token-class cite-edit absorption patch under FM-2 no-extension discipline; ZERO cross-axis cascade per fork doc §5. v2.12 prior — U-RT-68 Class 1 fork ratification absorption per `.harness/class_1_fork_u_rt_68_retry_wrap_shape_gap.md` (RATIFIED 2026-05-22): NEW L9-septies cluster (5 units U-RT-71..U-RT-75) decomposing Q2=B2 bootstrap-wiring chain — RuntimeConfig schema extension + HarnessContext schema extension + stage-3a `materialize_mcp_client_host_stage` factory + C-RT-21 `RetryBreakerToolDispatcher` class body + stage-5 `materialize_runtime_tool_dispatcher_stage` factory; U-RT-68 REWRITTEN at v2.12 per Q1=B + Q1a=(i) ratification (stage-5 callsite consuming new factory; original v2.11 AC #2 with literal `RetryBreakerFallbackDispatcher` reuse STRUCK); ZERO cross-axis cascade per fork doc §5; L9-sexies preserved verbatim except U-RT-68 (2026-05-22) |

## Dated change-note lineage (relocated verbatim from root `CLAUDE.md`)

### §2.3 / §2.4 change-note lineage — the RATIFIED `B-69` durable-pause-state read accessor spec leg (2026-07-30)

*Appended at the `B-69` spec leg per the post-#1161 B-17 governance split: root `CLAUDE.md` §2.3/§2.4 carry ONE sentence per bumped head; the change-note lineage lives here.*

**Trigger.** Council record `.harness/council-b69-pause-state-accessor-2026-07-30.md` — a genuine three-voice convening (**C10** action safety / blast radius, promoted primary; **C11** operator loop / local deployment, primary; **C9** reliability / recovery, the justified dyadic-default expansion), each a dedicated agent invocation adopting its own `cN/SKILL.md`. Four seams surfaced, **four resolved** (three probe-resolved, one by symmetric empirical defeat), **zero surfaced-unresolved**; **20 positions withdrawn, replaced or corrected** across two reconcile rounds; in-family adversarial pass 0 Class 3 / 7 Class 2 / 4 Class 1; out-of-family `just codex-review-uncommitted` (GPT-5.6) 4 × [P1] + 1 × [P2] with **near-zero overlap** against the in-family pass — three of those five changed what the spec leg wrote. E4 verdict **CLEAR-TO-COMMIT**. **Operator ratified OPTION A′ — CO-REQUISITE, SEQUENCED — 2026-07-30** (record §14 addendum).

**Closure criterion, X-AL-2 conjunctive form (recorded VERBATIM at the `B-69` register row's `close_out`, so it travels with the row — but enforcement is a PRE-MERGE REVIEW OBLIGATION on the closing PR, since `tools/forward_register.py --check` validates `close_out` presence and a closed row's PR cite without parsing the conjunction; structured per-conjunct fields + validator support are a `tools/` change outside a doc-only spec leg's scope, registered rather than assumed).** ***B-69 closes iff (accessor landed) ∧ (staleness precondition landed ∧ exercised by the W3 mutation-probe); partial is non-closure.*** Spec surfaces and impl units MAY land in separate merges within the arc, subject to one ordering constraint: **the staleness precondition lands FIRST OR SIMULTANEOUSLY — never after.**

**`Spec_Harness_Runtime_v1.md` v1.106 → v1.107.** Three surfaces plus one co-requisite invariant. **(A)** NEW **§14.14.9** — a public async read on the `harness_runtime` package root taking the IDENTICAL `(workflow, resume_handle, config)` triple `resume()` requires; a CLOSED discriminated union of **FOUR** location variants spanning **SEVEN-PLUS source shapes** (a draft added a fifth for the empty-`idempotency_key` crash-reconstruction carrier; out-of-family review round 11 [P1] showed that would break the one-authority invariant, since the empty-key distinction is not derivable from CP properties 1-8 — it is modelled as an `EffectFenceAddressable` SOURCE SHAPE with the key field ABSENT instead) (`HitlAddressable` keyed by the paused child's own `run_id` / `EffectFenceAddressable` keyed by `idempotency_key`, **three source shapes incl. the LINEAR `EffectFenceResumeState`** / `UniformFallbackOnly` with the key field **ABSENT**, **two source shapes incl. the depth-0 ROOT gate-owning pause** / `TransitivelyPaused` with the key field **ABSENT**), with fields declared **PER SOURCE SHAPE** and a **SOURCE-SHAPE SUB-DISCRIMINATOR required** on the two multi-shape variants so an absent field is *unrepresentable* rather than optional; `workflow_id`-only keying; fail-closed on inability to mint the staleness token (**no-token-returned MUST mean no-projection-returned**); a five-member cause-attribution vocabulary; declared **no-gate / no-HITL-trigger / no-internal-retry** postures; and a stated naming constraint (**do not name it after the journal** — the operator's question is *"what is this workflow waiting on?"*). **Contract-number choice** (the council left it to the spec-writer): a NEW `C-RT-*` at a NEW top-level §14.14.9 rather than a §14.14.9 sibling, because §14.14.x is C-RT-24 internal-composition territory whereas this is a caller-facing package-root API — the second member of C-RT-35 §30's class; the file's own `:680` "no new operation" framing is the discriminator. **(B)** the `:3274` scope-limit sentence **DISCHARGED** to a cross-reference rather than left as stale-carry. **(C)** NEW §30 **refusal-only staleness precondition** — the token stated as a **PROPERTY, never a composition** (*it MUST differ between any two durably-journaled pause records of the same workflow a caller could successively observe*), refusal-only with **no escape value in the domain**, binding **REQUIRED not optional** (an optional token makes omission itself the prohibited escape, leaving every accessor user's default path unfenced — *safe-by-diligence is a species of luck*), ONE new fail class `RT-FAIL-RESUME-PAUSE-STATE-STALE`, plus a **five-member cause-attribution refinement on the EXISTING `RT-FAIL-RESUME-HANDLE-UNKNOWN`** (`absent` / `empty-journal` / `read-error` **[transient — I/O only]** / `corrupt-latest` **[permanent — now covering DECODE failure as well as parse failure]** / `workflow-mismatch`; `empty-journal` MUST NOT fold into `absent` despite shared routing; **cause class only** — never exception text, never a resolved filesystem path). **One deliberate divergence from the council record's literal §6.4 mapping, stated rather than absorbed** (round 4 [P2], verified against the store's read path): the record grouped `OSError`/`UnicodeDecodeError` together as transient `read-error`, but the journal is JSON-serialized, so an undecodable byte is **persistent on-disk corruption no re-invocation can repair** — routing it transient would send the operator loop into unbounded retry, which is the misrouting this refinement exists to prevent, one branch further in. **The five-member vocabulary is UNCHANGED**; `corrupt-latest`'s definition widens to "the stored bytes could not be decoded, OR the latest record failed to parse." **(C-co-requisite)** NEW **§14.14.8 substrate invariant** — the per-workflow pause journal is **append-only and NEVER TRUNCATED**, stated because §30's absolute property was otherwise resting on an unstated implementation fact (*building a stated absolute property on an unstated substrate invariant is the §14.8.8.10 error one layer down*). **The council's probe P4 is why no composition is named:** for a non-fan-out pause, `run_id` is REUSED across resume, `created_at` is not a hash parameter, and `hitl_gate_config_hash` derives deterministically from the workflow body — so two genuinely distinct successive LINEAR pauses at the same `step_index` produce an **IDENTICAL `snapshot_hash`**, and a fence built on it fails **OPEN**. Clearance `.harness/clearance/spec-harness-runtime-v1-107-cleared-2026-07-30.md`.

**`Spec_Control_Plane_v1_111.md` → `Spec_Control_Plane_v1_112.md`.** **§1** the REQUIRED `ResumeContext` **response-provenance carrier** — a closed two-variant discrimination (accessor-derived, token non-optional; legacy, token-field-absent and semantically byte-identical to today), with the legacy variant **NOT constructible from** an accessor-derived one (no downgrade, no `.without_token()`, no field mutation; frozen carriers) — *without that constraint the carrier IS the escape value the precondition forbids; with it, it is not.* **CP-homed rather than Runtime-only** (the council's `[MODERATE]` §8.2 sizing residual, resolved with the grounding stated): `hitl_responses` and `effect_fence_resolutions` are CP-owned `ResumeContext` fields, so a Runtime-side parallel carrier would leave *"these responses came from **this** read"* untyped and create a second authority. **§2** the NEW **public projection-returning surface** over the two private resume-tree walks — bare identifier sets **FORBIDDEN** as the return shape (they cannot populate position/step/reason/addressability without the caller re-walking, and worse, they would carry the ONE value that must not cross); the never-keyable pre-dispatch internal identity **ABSENT, not opaque** (it is `run_id`-shaped, so an operator who keys it hits the resolver's collision defence and the response is **silently DROPPED, not refused — livelock with no diagnostic**); enumeration **TOTAL** over gate-owning locations (*omit pre-dispatch locations and the operator's safety judgment **inverts***); with the checkable one-authority acceptance criterion ***"Runtime's accessor contains no recursion over `PauseSnapshot` and reads no nested resume-carrier field."*** **§3** two registered scope-limit **LIFTS** — `Spec_Control_Plane_v1_106.md` §3's follow-on **(a)** (follow-on **(b)** untouched — it was separately `B-70`), and `Spec_Control_Plane_v1_107.md` §1.1's **round-4** `resume_handle` note (its **round-5** sibling is a DIFFERENT correction and is NOT lifted) — each lifted **conditionally**: only for a caller who **takes the read**, only **in conjunction with** the precondition, and only for **ADDRESSING** (§1.1(a)'s 2+-member INERT re-pause rule is PRESERVED VERBATIM). **Both lifted notes live at v1.106/v1.107, and the delta says so, or the lifts would not resolve byte-exact.** Properties 1–8 PRESERVED VERBATIM. Clearance `.harness/clearance/spec-control-plane-v1-112-cleared-2026-07-30.md`.

**`Spec_Operational_Discipline_v1_35.md` → `Spec_Operational_Discipline_v1_36.md`.** NEW **§C-OD-30.5**. The namespace determination the council delegated to OD came back **SPLIT**, which is why a bump was filed rather than a bare rides-existing sentence — and the split is **not** where a first draft put it. **Ruling: both events emit within the EXISTING C-OD-30 `pause.*` / `resume.*` family; NO new top-level namespace; the C-OD-05 §5.1 roster UNCHANGED; the read declared as a NEW event kind — but NEITHER event is representable by the existing payload composition unchanged.** The **read** is neither a `PauseEvent` nor a `(ResumeAttempt, ResumeOutcome)` pair but a **third event class**. The **staleness refusal** is raised **PRE-BOOTSTRAP**, before any `(ResumeAttempt, ResumeOutcome)` exists, so it composes through neither existing §C-OD-30.4 helper nor the CP→OD converter's `resume:` branch — and `PauseResumeAuditPayload` is `frozen` + `extra="forbid"` with **no staleness-token field**. **§30.5.2 therefore AUTHORIZES an additive carrier** (an additive field, or a sibling payload type — impl discretion), with the existing field set and BOTH existing helpers **otherwise PRESERVED VERBATIM**. *(An earlier draft of this delta AND of this lineage paragraph asserted the refusal needed "no new payload type and no new converter branch"; out-of-family review round 2 [P1] found both halves false — on timing and on schema — and round 5 [P1] caught this paragraph still carrying the superseded claim. Corrected in both places rather than quietly repaired.)* **Load-bearing requirement:** the SAME staleness token at **BOTH** the successful read and the refusing resume, so a stale-read refusal is reconstructable as **one causal pair from telemetry alone**. **Content splits by outcome** (round 3 [P2] + round 2 [P2]): successful read → token + **PER-VARIANT counts** (four, one per §14.14.9.2 classification; a single total cannot express classification while the disclosure limit demands classification without identity); failed read → `workflow_id` + cause attribution, **no token, no count**, but it MUST still emit — *silence is not the failure disposition.* **NEVER on either:** the locations' payload, or the never-keyable pre-dispatch / depth-0-root identity; **cause class only**, never exception text or a resolved path. **PLAN: no OD plan delta at THIS leg, but one is OWED AT THE IMPL LEG under EITHER carrier option** — both mutate or extend an OD-owned schema, and a Runtime unit cannot own that (round 3 [P1] / round 5 [P1]). Runtime plan v2.55 records the resulting **pending, not-yet-numbered OD dependency** of U-RT-148. Clearance `.harness/clearance/spec-operational-discipline-v1-36-cleared-2026-07-30.md`.

**`Cross_Axis_Composition_Document_v2_22.md` → `v2_23.md` — CLASSIFICATION-ONLY.** **NO new §2.3 row; §2.1 aggregate FROZEN; total remains 111.** Three findings: (1) the consumption is **not a member of this document's enumerated relationship space** — §2.1 is a 4×4 matrix over IS/AS/CP/OD, and every forward-capability row since v2.20 is an axis→axis edge in which `harness-runtime` appears strictly as **MEDIATOR**, never endpoint; (2) it introduces **no new crossing point** — same `harness-runtime` → `harness-cp` boundary three sibling public CP computations already use; (3) **but the PAYLOAD widens materially, three scalars → one structured sequence**, recorded because *"no new crossing point" must not carry weight it cannot.* Two riders: the ruling is about **enumeration membership, not significance**, and it is **conditional on the direction holding** — if the impl leg cannot compose the accessor without `harness-cp` importing `harness-od` or without a genuine new axis→axis seam, that is a fresh Class 1/2 fork for the impl leg to raise. `harness-cp` MUST NOT import `harness-od` (§2.3.3) preserved unchanged. Clearance `.harness/clearance/cross-axis-composition-v2-23-cleared-2026-07-30.md`.

**`Implementation_Plan_Harness_Runtime_v2_54.md` → `v2_55.md`.** **NEW U-RT-148** — ONE unit carrying **both** ratified surfaces, chosen over a paired sibling (the council authorized either) because the closure criterion is conjunctive, because the primary witness **W2 is inherently joint**, and because splitting would create precisely the merge interval A′ forecloses. The ordering constraint is restated as a **unit-internal obligation (AC #10)**: no commit may make the accessor reachable through the public surface before the precondition is enforced. **SIXTEEN** ACs (recounted programmatically at the final review round), **every one by EXECUTION** — AC #1 (**W2 PRIMARY**, the 1→1 sole-member misattribution, asserted refused *under* the fence and reproducing *without* it, with C9's falsifier on the record); AC #2 (W1, the 2+-member INERT clause); AC #3 (**W2′**, the 1→1 shape with a `snapshot_hash` fence installed **still misattributes** — converting P4 from reasoned to executed and foreclosing a later re-nomination of the conceded carrier); AC #4 (**W3**, the PD-8 mutation-probe against a **REAL** second `capture()`, **one half of the conjunctive closure criterion**); AC #5 (W4 characterization, with the **mandatory in-test annotation** that its correct disposition on placeholder replacement is **DELETION or INVERSION, never repair**); AC #6–#12 + #14 (non-downgradability by detect-then-refuse; fail-closed on inability to mint; the five causes distinctly, with **decode failure routed PERMANENT**; the checkable one-authority criterion; the ordering obligation; the exclusions; total enumeration across BOTH `UniformFallbackOnly` source shapes; legacy byte-compatibility); **AC #13** the emission with success and failure asserted SEPARATELY (successful read → token + **per-variant counts**; failed read → `workflow_id` + cause attribution, no token, no count, but it MUST still emit; plus the causal pair); and **AC #15** the **source-shape sub-discriminator by detect-then-refuse** — the four variants span **seven source shapes** with different field sets, so a per-variant optional `step_kind` would readmit an illegal state. Clearance `.harness/clearance/implementation-plan-harness-runtime-v2-55-cleared-2026-07-30.md`.

**`Implementation_Plan_Control_Plane_v2_46.md` → `v2_47.md`.** **U-CP-64 amended** (the `ResumeContext` carrier-owning unit per its v2.21 authoring and its v2.42 `hitl_responses` amendment) — **zero new units.** A deferred coverage-matrix row, the instrument v2.43/v2.44/v2.46 used correctly for properties constraining resolvers that do not yet exist, is the **WRONG** instrument here: **this surface has an owner**, and §2's projection publishes exactly the four resolution channels §1's carrier is keyed by. Ten added ACs (#A1–#A10), the load-bearing ones being **#A2** non-downgradability by **detect-then-refuse** (not absence-of-helper), **#A4** the ABSENT key fields on the two unkeyable variants, **#A5** total enumeration, **#A6** bare identifier sets refused, **#A7** the one-authority criterion carried **verbatim** and duplicated at Runtime plan v2.55 AC #9 so neither side drifts unilaterally, and **#A8** a drift witness asserting the projection and the resolvers agree on gate-ownership for the same tree. **No topology unit (U-CP-86/88/89) is amended, stated explicitly** because v2.42's own round-1 draft amended exactly those three on an empirically false call-graph claim. Cross-axis cascade **named, not assumed away** — no new crossing point, but the payload widens; this avoids repeating the blanket zero-cascade claim v2.46 itself had corrected pre-merge. Clearance `.harness/clearance/implementation-plan-control-plane-v2-47-cleared-2026-07-30.md`.

**Register effect.** `B-69` `registered_finding` → **`open`** (mirroring the `B-92` spec-leg shape at PR #1158) with a LIVE-NEXT-STEP summary head, the X-AL-2 conjunctive criterion recorded **verbatim** in `close_out`, and `council:` → *"ran 2026-07-30, converged, record at `.harness/council-b69-pause-state-accessor-2026-07-30.md`"*. Three findings **routed OUT, not absorbed** — **`B-97`** (pause-journal tenant binding + the unserialized `_append`, urgency-raised), **`B-98`** (gate description absent from every durable pause carrier), **`B-99`** (the `summary_text` reopening condition). Folded **INTO** the impl arc rather than filed: the Class 3 cite-hygiene fix for **three** stale docstrings in `pause_resume_protocol_types.py` (an "8-field" claim on a **15**-field type; a "5-class" claim on a **six**-member enum; a **four**-element hash description) — not cosmetic, since a projection arc enumerating from them undercounts `EFFECT_FENCE_AMBIGUOUS`, and the stale text caused two real miscounts inside the convening itself.

---

### §2.3 / §2.4 change-note lineage — the RATIFIED `B-97` half (a) pause-journal tenant-binding spec leg (2026-07-31)

*Appended at the `B-97`(a) spec leg per the post-#1161 B-17 governance split: root `CLAUDE.md` §2.3/§2.4 carry ONE sentence per bumped head; the change-note lineage lives here.*

**Provenance.** `.harness/class_2_fork_b97a_pause_journal_tenant_binding.md` (filed PR #1168, merged `59ec1b32`; 11 out-of-family review rounds, SOUNDNESS EXIT declared after round 11 — first declared after round 9, **REOPENED** by round 10's [P1] against the recommended reading, re-declared and held). **Operator ratification, TWO gates, both 2026-07-31, recorded verbatim at the fork's `§11 RATIFICATION` addendum: gate 1 = READING A** (per-tenant path segregation; injective constant encoding; five-cause vocabulary stays at five; mandatory quiesced cutover; `(workflow_id, run_id)` rider NOT bundled); **gate 2 = (3a) abandon-by-default CONFIRMED + §5.3.1 as (i)+(iii) with (iii) a PRECONDITION of (3a)**, both as the intervening C3 ⊥ C11 dyadic recommended (`.harness/council-b97a-tenant-path-segregation-2026-07-31.md`, PR #1169 — **CONFIRM-WITH-CONDITIONS**, 17 conditions `K-1`…`K-17`, 15 load-bearing).

**`Spec_Harness_Runtime_v1.md` v1.107 → v1.108.** Four surfaces plus six fold-ins.

**(A) §14.14.8.** `Keying = workflow_id` (`:5826`) → the **tenant-composite `(normalized_tenant_scope, workflow_id)` realized as per-tenant PATH SEGREGATION**, amended in place rather than added beside, because two keying statements over one durable address space would be two authorities. The **record shape is UNCHANGED** — no wrapper `tenant_id` key — which is what keeps `PauseSnapshot` (CP-owned) untouched, `snapshot_hash` inert, and the five-member cause vocabulary closed. **Why the address space and not the record:** the read is latest-record-only and MUST NOT walk backward, so whether two scopes share a FILE decides whether each keeps a resumable stream at all — a shared file converts wrong-tenant *execution* into wrong-tenant *mutual resume denial*. **The cross-tenant refusal is therefore DISSOLVED, not discharged, and that is stated as a DECISION**: a wrong-tenant lookup resolves the caller's **own** path and returns the ordinary `absent`, which under segregation is the truth. Contract terms carried: a **TOTAL injective** encoding closing **all three** hazards (branch / delimiter / field-boundary) on the in-house **length-prefixed canonical-segment** shape **with its byte grammar PINNED** — UTF-8-with-`surrogatepass` segments (the error handler is part of the grammar: plain `str` accepts lone surrogates a strict encoder refuses, which would make the encoding partial over its own declared domain), decimal-ASCII **byte**-length prefix + `:`, `|`-joined, arity discriminating the untenanted branch — because the codebase ships **two incompatible** length-prefix precedents, one character-counting and one byte-counting; the derivation as a **CONSTANT with NO configuration surface** (a knob would be a second authority that silently re-addresses every journal); the **Runtime-local** `normalize_tenant_scope` delegate, explicitly **not** OD's `sidecar_tag` / `_tenant_tag`; the **tenant-subdirectory layout FORECLOSED** on path-safety grounds; **(3a) abandon-by-default** with the failure-mode asymmetry as the stated reason; a **MANDATORY no-mixed-version cutover** stated with the **negative fact** that no directory-scoped liveness predicate exists (pidfile `repository_root`-keyed vs. journal from `path_bindings`), so quiescence is **never a discharged precondition**; **(3b)** adoption's **FIVE** mechanical terms — legacy-**source** exclusion on the contended inode (a directory-tree lock explicitly NOT sufficient), read-back verification that **detects and narrows but does NOT close**, the **write-once crash-atomic no-replace publication as the empty-target enforcement** with **no second target lock**, and per-journal idempotent re-runnable sequencing discriminated on **content equality** — plus **refuse-where-the-primitive-degrades**; the **named silence** (a violated adoption passes every shipped guard over a store with **no hash chain**); and the **orphan statement in both halves**. **Honest scope limit stated in-spec:** this is isolation and mis-addressing prevention between mutually-trusting deployments, **NOT a security boundary** — the journal is plaintext, so §14.8.11's writable-disk argument is inert here.

**(B) §14.14.9.1.** `Keying is workflow_id-only` (`:3384`) amended in parallel, **by that paragraph's own both-surfaces-or-neither rule**; the caller-facing input triple is **byte-unchanged** (the tenant scope always lived inside `config`). §14.14.9.2–§14.14.9.5 **PRESERVED VERBATIM**.

**(C) §30.** The `absent` row (`:3318`) tenant-scoped in **meaning AND repair text** across **THREE** states (never-journaled / **wrong-tenant** / **abandoned-legacy**) — a **definition amendment**, like `corrupt-latest`'s at `:3324`, with **no new cause member**. Folded in: **`B-102`**'s disposition (a) — `absent` and `corrupt-latest` each gain the *"INDETERMINATE across processes / MUST NOT present as decisive loss where a second writer is reachable"* qualification `empty-journal` already carried, so **all three** permanent-absence causes are qualified rather than one of three — with `corrupt-latest`'s qualification **restricted to the PARSE-failure branch**, because a DECODE failure is persistent on-disk corruption every re-read reproduces and stays unconditionally permanent; the `RT-FAIL-RESUME-HANDLE-UNKNOWN` taxonomy row and §14.14.9.4's routing clause were refreshed in the same edit so **no surface flattens the table**. `B-102`'s **impl** half (the shipped read-side attribution flip) rides U-RT-149 AC #4(c); its row is **NARROWED, not closed**.

**(D) §13.4 + NEW §13.7.** §13.7 declares the §5.3.1(iii) enumeration surface as a **§13.5-shaped extension of the EXISTING `harness-inspect` binary** under seven binding terms: **enumerate-never-probe** (a single-path probe on an admin binary is the withdrawn read-path oracle in a hat), **pre-flight-capable**, reports `record_count` + `latest_record_digest` (zero new persistence), **never parses / returns / resumes** a snapshot, **engages only when a journal directory exists** (byte-unchanged output otherwise), the **read-only invariant verbatim** (which **forecloses disposal from that surface**), and **admin-tier enumeration permitted while the tenant-facing runtime read path MUST NOT gain it**. §13.4 gains rows for the **(3b) adoption tool** and the **OPTIONAL disposal action** (dry-run default; refuses while adoption remains possible), the subcommand-count invariant moving 6 → 8. **A bookkeeping divergence is recorded rather than smoothed:** the governing condition asked for a §13.4 row for all three surfaces, but a third *subcommand* row for the enumeration would declare a binary §13.7 forbids — so two take rows and the third takes an **amendment of the `harness-inspect` row's scope**.

**Six stale-as-described fold-ins.** `:3313` term 5 and `:3319`'s `empty-journal` cell (both false as described since PR #1167 closed half (b); **asserted properties unchanged**); the v1.107 change-note's `:4636`/`:4639` §14.8.11 cites (stale → the v1.108-head `:4873`/`:4876`); `:5886`'s `directory-fsync-on-new-file` (both dir fsyncs unconditional since #1167 — the stale text is the **mechanism description itself**); the **`_single`-sentinel mechanism claim** (the spec MUST NOT, and does not, say this store's untenanted scope normalizes to `_single`; the fork §7 parenthetical is struck in place, **conclusion standing**); and — **a CODE touch, not spec** — `harness-runtime/src/harness_runtime/admin/shutdown_cli.py`'s docstring, `--wait` help and wait-timeout runtime message, which framed the workflow-loop drain as **STRUCK** against a fork `harness_runtime/drain.py` records as **CLOSED** (C-RT-11 full-land 2026-05-20). Strings only, zero logic change, scoped so the timeout behaviour and its true cause (*the handler sets a flag; it does not call `shutdown()`*) are preserved. It is folded in here rather than registered separately because it is the **second leg** of why *"drain pauses before upgrading"* read as unavailable — the recourse §13.7 exists to repair.

**`Implementation_Plan_Harness_Runtime_v2_55.md` → `v2_56.md`.** **NEW U-RT-149** — ONE unit, chosen over a three-way split because the enumeration surface is a **PRECONDITION** of the ratified disposition (landing the keying change without it ships the state the ratification refused), the (3b) machinery is unimplementable over an unlistable directory, and the witnesses interlock. **Conjunctive closure criterion:** *keying landed ∧ injectivity witnessed across all three hazards* ∧ *§13.7 landed* ∧ *(3a) + orphan statement witnessed* ∧ ***the (3b) adoption tool landed to its FULL FIVE-TERM set*** (the fifth being the filename↔wrapper binding, via §13.7's three-way classification); **partial is non-closure**, with only the **OPTIONAL disposal action** outside the conjunction (all-or-nothing if built). *(A draft placed (3b) outside it too; out-of-family review round 1 [P1] showed that misreads the ratified contract — the spec says (3b) is AVAILABLE and it is the only recovery path for a deployment that upgraded with pauses outstanding.)* Twelve ACs, all by execution; the PD-8 mutation probe applied twice; **two witness shapes explicitly FORBIDDEN** because prior fork drafts wrote them and they are unsatisfiable or wrong (a legacy-abandonment witness demanding the refusal "name the abandonment rule"; a mis-filing witness written as a whole-directory restore). **ZERO new cross-axis edges.** The v2.55 accounting gap — `JournalWorkflowPauseStore` is LANDED with **no owning plan unit at any Runtime plan version** — is **restated, not silently closed**: U-RT-149 owns the change *to* the store, which is not retro-assigning ownership *of* it.

**CP / OD / IS / AS specs and plans UNCHANGED; CXA UNCHANGED — no new §2.3 row, no CXA document delta, aggregate remains 111.** Each determined at the leg, not carried forward: the record shape is unchanged so no CP type is touched; no `K-*` names an OD-owned schema and the council routed observability out (span design for admin surfaces is a follow-on); and the arc introduces **no cross-package consumption at all** — a stronger ground than B-69's, which had one and therefore owed the classification-only v2.23 delta. Governing precedent: Runtime v1.104's *"intra-Runtime composition-root mechanism; no new cross-axis seam"*, with CXA v2.23 §0.2 finding (1) supplying the standing ground that `harness-runtime` is not one of the four axes §2.1's matrix enumerates.

**One narrowing applied at this leg's out-of-family review, recorded rather than reproduced.** `K-10(c)` asks §13.7 to report *"how much would be LOST"*; the store keeps a journal after its pause resolves and writes **no pause-resolved marker**, so a resolved journal is byte-indistinguishable from an outstanding one. §13.7 term **3-bis** states the limit as a contract term — the enumeration **bounds the at-risk set from ABOVE** and MUST NOT be cited as proof a cutover is safe. `K-10`'s mechanism is applied unchanged; only its claim is scoped to what the substrate supports, which is `K-1`'s own discipline one condition over. The discriminator is **capture-side** and is **REGISTERED as `B-104`**, not patched.

**Register.** `B-97` → **`open`** (design question answered; impl leg is the only thing between it and closure), with the ratification, the four answered decisions and the conjunctive closure criterion recorded at its `close_out`. `B-102` → **`open`, NARROWED** to its impl remainder. **NEW `B-103`** — the blocking-`flock`-on-the-event-loop follow-up, **split out** per the fork's own leg-2 obligation so it does not vanish from `--open` when `B-97` closes. **NEW `B-104`** — the absent pause-resolved marker. Snapshot pin recomputed via `tools/forward_register.py`'s own functions: **105 items — 2 open / 21 registered_finding / 1 held / 81 closed**.

---

### §2.3 change-note lineage — the RATIFIED `B-104` Reading D leg (Component 1 only) + the RATIFIED `B-98` Reading C deferral (2026-08-01)

*Appended at the combined `B-98` + `B-104` ratified-defer legs per the post-#1161 B-17 governance split: root `CLAUDE.md` §2.3 carries ONE sentence per bumped head; the change-note lineage lives here. `B-98` bumps NO head — it owes no spec text — and is recorded here because the two ratifications landed as one act.*

**Provenance.** Two Class 2 fork docs, both filed and both ratified by operator `AskUserQuestion` on 2026-08-01, each recorded verbatim-in-substance at its own `## §11 RATIFICATION` addendum: `.harness/class_2_fork_b98_gate_description_durable_carrier.md` (filed PR #1180, merged `8d035105`) → **READING C, DEFER, NO carrier change**; `.harness/class_2_fork_b104_pause_resolved_marker.md` (filed PR #1181, merged `4e4b702f`) → **READING D, DECLARE + DEFER**. Both rows stay `status: registered_finding` — a ratified deferral is an answer, not a parking label.

**`Spec_Harness_Runtime_v1.md` v1.109 → v1.110 — `B-104` Component 1, and NOTHING else.** **TWO amendment sites, both pure DECLARATIONS of an EXISTING limit.** **(A) §14.14.9.1** gains a closing paragraph: the accessor reports the **latest durable record**, which is **NOT a liveness claim** — §14.14.8's store is append-only, never truncated, and writes **no pause-resolved marker**, so a resolved journal is **byte-indistinguishable** from an outstanding one; the projection is authority for *what the latest durable pause was waiting on* and is the correct basis for composing a `resume_context`, but **never** for *the workflow is paused right now*, and MUST NOT be presented to an operator or consumed by a policy as an outstanding-pause assertion. **(B) §30** gains a NEW invariant bullet immediately after `Durable-handle read (v1.46)` stating the same substance **in full rather than by pointer** — because the fork's §3(vii) ground is a **DISCLOSURE asymmetry**, which a pointer would reproduce — plus the sharpening §14.14.9.1 does not need: `resume()`'s shipped guards (`snapshot_hash`, workflow-match, step-index-range) validate **integrity and applicability, never outstandingness**, and the §30 staleness precondition fences a composed claim against a **CHANGED** record, so **a re-resume of an UNCHANGED, already-resolved record presents no mismatch and IS ADMITTED**. Both are **symmetric with §13.7 term 3-bis**, which states the same fact for the operator enumeration, and both scope the limit to the **steady state for a current-format journal** — **not** merely the pre-v1.108 legacy set the row was filed against.

**Why the declaration was MANDATORY rather than optional.** The fork's round-2 amendment made it so: `read_paused_workflow_state` (`api.py:925`) and `resume(resume_handle=...)` (`:1190`, reading at `:1360` — **post-edit anchors**; this leg's own docstrings shift `api.py`'s lower half, so the fork's pre-leg `:1170` / `:1321` no longer resolve in the landed tree) both route through `_read_durable_pause_snapshot` (`:775`), so the limit was **contract text at §13.7 term 3-bis and UNDECLARED on both tenant-facing surfaces that share it**. A pure deferral would have left the same asymmetry this arc had already rejected once for the enumeration. **The limit is DECLARED, not FIXED — the disclosure half closes; the blindness does not**, and the spec text says so in its own words.

**Docstring companions, MANDATORY per Component 1 and therefore in the same PR.** `harness-runtime/src/harness_runtime/api.py` — `read_paused_workflow_state` (`:925`) and `resume()` (**`:1190` post-edit**) on **BOTH** its durable-handle prose **and** its separately-consulted `resume_handle` parameter entry (**`:1260` post-edit**), per the fork's round-3 [P2] correction that disclosing on the accessor alone warns the weaker surface and leaves the one that **executes** unwarned. **ZERO logic changed.**

**What the delta does NOT do, stated so each absence is a DECISION.** No resolved-sentinel record (Reading **A** — needs a **SIXTH** `PauseJournalReadCause` member, re-opening a `B-97`(a)-ratified five-member closure **and forcing a cross-axis OD spec + plan leg**, because `harness-od` re-declares the vocabulary independently at `pause_resume_namespace.py:482`–`:498` and `api.py:1183` converts **by value** (post-edit anchor)); no out-of-band marker file (Reading **B**); no third `PauseStateEventKind` in the existing §C-OD-30.5 sink (Reading **C**). **ZERO** `C-RT-*` numbers minted — a doc-only declaration leg cannot mint one — **ZERO** carriers retyped, **ZERO** fields added, **ZERO** `snapshot_hash` impact, the five-member cause vocabulary stays **CLOSED at five**, **ZERO** plan units owed at the Runtime / OD / CP plans, and **ZERO** CXA rows (aggregate frozen at **111**; this leg introduces no cross-package consumption at all). **§13.7 / §13.7.1, §14.14.8, §14.14.9.2 – §14.14.9.6, §30's cause table and the entire §30 failure-mode taxonomy are PRESERVED VERBATIM.** Counts recounted programmatically: §30 invariant bullets **13 → 14** (one ADDED, none reworded, none removed); §30 taxonomy rows **12 → 12**; §14.14.9.2 variants **4 → 4** and source shapes **10 → 10**; §13.7 term rows **9 → 9**. Clearance `.harness/clearance/spec-harness-runtime-v1-110-cleared-2026-08-01.md`.

**`B-98` — Reading C, and the spec corpus is UNTOUCHED by it.** Fork §9's `C` column owes *"none owed — no design extension"*, discharged by making none: CP spec §26.2 untouched, Runtime §14.14.9.2's exclusion row untouched, no plan unit opened / amended / cited. **Verified, not assumed, for BOTH rows:** `B-98` and `B-104` are each cited at **no** implementation plan and at **no** acceptance criterion, so **no plan cite contradicts either deferral**. Reading C's own Class 3 cite-hygiene item (`Spec_Control_Plane_v1_99.md:40`, *"v1.94's `synthesis_step_id`"* — falsified, first occurrence v1.58) is **recorded on the row's cross-ref, NOT patched**: a design-substrate edit does not ride a doc-only ratification.

**Register effect.** Both rows stay **`registered_finding`** — status enum unchanged, so the `snapshot:` `identity_digest` is unchanged and no pin bump is owed (`tools/forward_register.py --check` green at 107 items: 0 open / 1 design_substrate_gated / 18 registered_finding / 0 operator_gated / 1 held / 87 closed). Each row's `close_out` now carries the ratification, the absorbed grounding, and its **falsifiable four-disjunct demand test** — `B-98`: **D-0** (dominant) the FM-2 webhook-config arc making the HITL-pause class production-reachable, D-1 in its round-2 tightened two-disjunct form, D-2 the channel opening anyway, D-3 an observed misdescription; `B-104`: **D-0** (dominant) a retention/pruning-policy arc, since a pruner is **unbuildable** without the discriminator, D-1 in its round-2 re-scoped form (a surface *branching* on liveness, or an observed stale re-resume in a fresh process), D-2 a second keying change, D-3 the resumer ceasing to be the party performing the cutover. Each `council` field is **narrowed** to its fork's §7 per-reading form — `B-98`: none now, **additionally owed at D-0 or D-1 even while C stands** (D-0 makes the pause class real; D-1 is when free text the operator does not already hold starts to exist), and owed before the spec leg only for A-2 or B; `B-104`: none now, **one owed at D-0**, with per-reading routing for A / B / C. *(The `B-98` D-0/D-1 clause was omitted from this leg's first draft of the fork addendum and the row and was restored at out-of-family review round 1 [P2] — an omission that would have let a reopening skip a convening §7 requires.)* **Both prose bullets at `.harness/post-phase-8-forward-register.md` were replaced, not merely appended to** — the pre-ratification `B-98` unconditional *"yes if opened"* and the pre-ratification `B-104` *"only if shape (a)"* would otherwise have contradicted the ratified rows through the `--detail` / heading back-pointer path *(same review round, second [P2])*.

