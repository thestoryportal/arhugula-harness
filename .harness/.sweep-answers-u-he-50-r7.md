# Round-7 sweep answers — u-he-50

- codex_review_wrapper:c38c8091ae2fe8d1c3cd134ddf153eac0b4f372d:f426492a0ce0:1 (P2,
  immutable start before in-process GATE_REFUSED) — REJECTED, standing disposition
  (fourth raise of the r5-registered residual; B-218-homed, named in the recipe
  comment; register-and-hold). Disposed rejected.
- codex_review_wrapper:c38c8091ae2fe8d1c3cd134ddf153eac0b4f372d:d25434102177:1 (P2,
  contradictory repair instruction) — ABSORBED: the repair is now edge-specific in
  BOTH carriers — only a failed final-round verify END is re-run; a failed
  final-round START is explicitly NOT repairable (a post-verdict start would
  fabricate a late window). Disposed accepted.
- codex_review_wrapper:c38c8091ae2fe8d1c3cd134ddf153eac0b4f372d:e15fafd8cf67:1 (P2,
  verify_unavailable close omits the held-arc terminal) — ABSORBED: both carriers
  regain the canonical "or the arc is held" terminal from the ship-pr definition, so
  a held arc's outage window closes rather than silently vanishing. Disposed
  accepted.

Class-sibling sweep (definition-drift class): both carrier copies re-diffed line by
line against the ship-pr "Phase-span edges" block after these fixes — the remaining
deltas are the intended ones only (wrapper-emitted verify on the logged path; inline
gemini commands on the Codex side; B-218 deletion trigger). No further drift.

## Grep-hit labels, answered verbatim (fix diff)

- [save/restore pair: two-armed restore] — none (prose only).
- [check-then-act on paths] — none.
- [bare counts/absolutes in prose] — "NOT repairable" is the enforced disposition
  (post-verdict start fabrication), matching the r4 test's repair witness which
  repairs END only.
- [new retry/timeout constants] — zero.

No findings deferred.
