# Round-2 sweep answers — u-he-50

- codex_review_wrapper:63e8fcf8350c...:57fcaeaf2b4e:1 (P2, SKILL.md:141 interim
  carrier) — REJECTED with witnessed grounds: C-HE-27 §5 (spec v1.6 X6a) itself
  prescribes the staging — "Skill prose is not an emission site — until the wrapper
  emits, the interim carrier is the emission block in `roadmap-continue/SKILL.md`, the
  skill actually in context during review rounds" — and plan §8.3 U-HE-50 bullet 2
  stages the copy with its deletion trigger ("deleted in the same PR that completes
  wrapper emission"; the wrapper-internal absorb/edit emitter is registered at B-218).
  The absorb/edit edges key off events only the session observes (first fix edit,
  absorption commit); the phase CLI stamps now() only, so a wrapper-side emission at
  its own boundaries would record the WRONG events. Disposed rejected.
- codex_review_wrapper:63e8fcf8350c...:5e0255d79dfd:1 (P2, end-write failure leaves
  an open window) — ABSORBED: pinned as the SAME named bound as the crash window
  (fail-end injection test; start survives, next invocation closes, upper bound may
  overlap edit); recipe comment names it; no durable same-attempt signal exists that
  would not mint a second authority. Disposed accepted.
- codex_review_wrapper:63e8fcf8350c...:86420637c247:1 (P3, half-set ids silent skip)
  — ABSORBED: half-set ids now WARN loudly and skip ("must both be set"); test added
  asserting the warn and zero store writes. Disposed accepted.

Class-sibling sweep: the partial-pair injection matrix is now exhaustive over the pair
(fail-start / fail-end / crash) — no further arms exist for a two-edge protocol; the
env-discriminator class has one carrier (emit_verify's guard), no siblings read the
HARNESS_* pair elsewhere in this diff.

## Grep-hit labels, answered verbatim (fix diff)

- [save/restore pair: two-armed restore] — none added; the new guard arms return 0
  without saving state.
- [check-then-act on paths] — none added; store writes still funnel through
  record_phase's CAS.
- [bare counts/absolutes in prose] — "must both be set" is the enforced invocation
  contract (tested); "no durable same-attempt signal ... second authority" is the
  single-enforcer argument, not a count.
- [new retry/timeout constants] — zero new bounds.

No findings deferred.
