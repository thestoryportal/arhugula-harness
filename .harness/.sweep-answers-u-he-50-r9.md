# Round-9 sweep answers — u-he-50

- codex_review_wrapper:3e7fcf93f0099d831e2d3f70c4d9ef5be74c7265:522818e0bce3:1 (P2,
  refused attempt's open start closed by the next round) — REJECTED, standing
  disposition: sixth raise of the r5-registered residual (rounds 5–9). Session layer
  cannot observe admit(); structural fix homed at B-218; named in the recipe comment.
  Disposed rejected.
- codex_review_wrapper:3e7fcf93f0099d831e2d3f70c4d9ef5be74c7265:f0fc2204171f:1 (P2,
  retry's pair fills the round-1 slot after a failed round-1 start) — REJECTED with
  witnessed grounds: this restates the r2-ACCEPTED disposition
  (`test_recipe_start_failure_records_no_partial_pair` pins the fresh coherent
  retry pair deliberately). Under the CLEARED one-pair-per-phase design (C-HE-27 §3
  v1: one durable pair, no attempt identity) the alternatives are (a) a reversed
  corrupt pair — fixed at r1, (b) nothing ever recorded for the arc — strictly less
  information, or (c) per-attempt identity — a spec-design change routing to
  design-phase back-flow. The recorded pair is a coherent review window whose
  round-attribution under partial failure is a named representation bound of the
  cleared design, not a diff defect. Disposed rejected.

Class-sibling sweep: no code changed this round; rounds 8–9 raised zero new shapes
(all four findings restate rounds 1–5 dispositions). Budget: 9 of 10 rounds spent;
if round 10 restates again, the arc proceeds to the decorrelated 3-lens merge-gate
per register-and-hold, which reads these adjudication rows.

## Grep-hit labels, answered verbatim (no fix diff this round)

- [save/restore pair: two-armed restore] — no diff.
- [check-then-act on paths] — no diff.
- [bare counts/absolutes in prose] — round counts bound to the arc's ledger rows
  (grep `u-he-50` in .harness/merge-gate-log.jsonl).
- [new retry/timeout constants] — no diff.

No findings deferred.
