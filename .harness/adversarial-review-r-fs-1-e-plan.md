# Adversarial Review — R-FS-1 E-plan (durable-execution engine classes E-1 + E-2)

## Summary

- **Mode:** Phase-6 pre-merge plan-amendment review (harness-adversarial-reviewer skill).
- **Artifacts reviewed:** `design-substrate/Implementation_Plan_Control_Plane_v2_34.md` (delta over v2_33; +3 units U-CP-93/94/95) · `design-substrate/Implementation_Plan_Harness_Runtime_v2_45.md` (delta over v2_44; +2 units U-RT-121/122) · `.harness/r-fs-1-e-plan-decomposition.md` (companion) · the two clearance markers (`…CP-v2_34…` + `…Runtime-v2_45…`).
- **Branch / HEAD:** `r-fs-1-e-plan-decomposition` @ `f2468f5` (delta base `d7102a6`).
- **Date:** 2026-06-15.
- **Reviewer:** harness-adversarial-reviewer (dedicated agent) + advisor() decorrelated pass.

> **Severity scale used in this report (read this first — taxonomy disambiguation per SKILL.md §title).** This task requested the **§2.7.6 Phase-7 execution-fork scale: Class 1 = blocking / Class 2 = substantive / Class 3 = doc-hygiene.** That is the **inverse** of the skill's default §4.1 review-severity template (where Class 3 = severe). **Every "Class N" label below is the §2.7.6 scale** (1 blocking, 2 substantive, 3 doc-hygiene). I do NOT use the §4.1 template headers, to avoid the inversion the skill's own preamble flags.

- **Finding count:** Class 1 (blocking): **0** · Class 2 (substantive): **1** · Class 3 (doc-hygiene): **2**.
- **Highest-severity finding:** F2-01 — O-CP-4 silently diverges from its **own controlling authority** (architect rec §5 endorses E-1; O-CP-4 relocates to E-2 but attributes the correction only to the lower design doc).
- **Verdict:** **APPROVE-WITH-CHANGES** (1 Class 2 + 2 Class 3). No blocking finding; the decomposition is sound and may proceed once F2-01 is acknowledged. NOT a clean APPROVE.

---

## What was verified by direct read (the load-bearing claims)

Every claim below was re-grounded from the actual file at HEAD — not from the plan's self-description.

| # | Claim | Verdict | Evidence (re-grounded) |
|---|---|---|---|
| V1 | **Delta-only preservation (BLOCKING).** U-CP-01..92 + U-RT-01..120 prior unit bodies preserved verbatim. | **HOLDS** | Full `diff v2_33→v2_34`: 34 deletion lines, **all** in §0 change-note (header + §0.1–§0.5) + §7 footer — ZERO inside any unit body / §1 prior rows / §3 prior graphs / §4 prior coverage / §5 / §6 prior open-items. `diff v2_44→v2_45`: 35 deletions, same shape. Targeted extract-and-diff of the U-CP-80..92 + U-RT-113..120 bodies = byte-identical (only a trailing `---` section-boundary artifact). §1 spec-inventory diff = additive E-rows only. |
| V2 | **X-AL-3 anti-extension.** `EngineClass` closed at 5; `ResumptionKind` closed at 5; ZERO spec amendment; ZERO new contract ID. | **HOLDS** | `engine_class.py:31-53` = exactly 5 members; `resumption_kind.py:27-` = exactly 5. Units consume the cleared closed enums (`engine_replay`/`segment_replay` are §8.1-committed members). No `design-substrate/Spec_*.md` edit in the diff set; both plan footers + both markers state ZERO spec amendment / new contract ID. |
| V3 | **O-CP-4: R-CXA-2 engine-layer activation homes at E-2 (WAL_SEGMENT), not E-1** — technical premise. | **TECHNICALLY SOUND** (but see F2-01) | (a) The engine-layer loop IS a snapshot surface: `pause_resume_protocol.py:123-135` Protocol = `capture_pause_snapshot` / `attempt_resume`; the Protocol docstring confirms it is "distinct from the C-CP-26 workflow-layer `PauseResumeProtocol` class." (b) `test_u_rt_95:129-131` maps **only** RECONCILER_LOOP / WAL_SEGMENT to DURABLE_ASYNC cells — EVENT_SOURCED_REPLAY is NOT one. (c) WAL_SEGMENT's append-then-resume maps onto #475's append-JSONL / read-latest shape. The finding's three corroborating facts all check out. |
| V4 | **Codex [P2] fix is correct + complete.** The `test_u_rt_95` un-skip body is vacuous; un-skip alone = false green; U-CP-94 AC now requires authoring a real e2e before flip. | **HOLDS** | `test_u_rt_95...py:347` is `@pytest.mark.skip`; the body `:368-375` is genuinely vacuous (`_ = patched_runtime, tmp_path`, no `execute_workflow`, no paused/resume assertions; docstring marks it "documentation of the intended e2e shape"). U-CP-94 AC (CP v2_34 :337) now reads: body is vacuous → "merely removing `@pytest.mark.skip` proves NOTHING" → requires authoring a real `execute_workflow` paused→resume e2e + a contrasting "fails-if-not-materialized" check; **"ONLY after this non-vacuous test passes are the `@pytest.mark.skip` removed AND the Path-(i) fork status flipped."** The fork-flip is correctly gated on the non-vacuous test. |
| V5 | **Zero production callers of the engine-layer loop** (`RuntimeEngineRecoveryLoop.capture_pause` / `.attempt_resume`). | **HOLDS** | `grep -rn ... harness-*/src` for `.capture_pause(` / `.attempt_resume(` on `engine_recovery_loop`: ZERO in any src tree. The only src `.attempt_resume(` calls are on the **workflow-layer** `PauseResumeProtocol` (`pause_resume_protocol.py:291`, `workflow_driver.py:1203`) — a distinct C-CP-26 surface. Six test files DO call them → genuinely test-only / dormant. |
| V6 | **DAG acyclic; cross-axis edge U-RT-122 → U-CP-95 is RT→CP downstream; no CP→RT import.** | **HOLDS** | §3.5 (CP) + §3.1b (RT) acyclicity proofs check out. The sole cross-axis edge is RT→CP (matching the `harness-runtime`→`harness-cp` package direction). U-CP-95 consumes `ctx.engine_recovery_loop` duck-typed, mirroring the real precedent `ctx.pause_resume_protocol` at `workflow_driver.py:1201` (verified). No CP unit depends on any U-RT-*. U-CP-94/93 resume_at reads F2 IS (CP→IS downstream), not the runtime substrate. |
| V7 | **Cited line numbers resolve at HEAD.** | **HOLD (1 region-cite tolerance)** | `_IN_SCOPE_ENGINE_CLASSES`=`{PURE_PATTERN_NO_ENGINE, SAVE_POINT_CHECKPOINT}` at :184; gate raise at :1352 (plan cites `:1351-1352` — the `if` is :1351, the `raise` :1352, region-correct); save-point fork at :1445-1446; `_determine_resume_at` at :1974; workflow-layer pause fire at :1201; composers at :856/:951; factory binds `DeterministicEnginePauseResumeSubstrate` at :210 (within :208-214); Protocol :123-135; Deterministic :171; #475 Journal :123. All resolve. |
| V8 | **Spec authority — §7.4 deferral clause names ESR+save-point+WAL, OMITS reconciler-loop.** | **HOLDS** | `Spec_Control_Plane_v1_2.md:704`: "Temporal / DBOS / Restate at event-sourced-replay; LangGraph… at save-point-checkpoint; **specific WAL implementation at WAL-segment class**" — reconciler-loop absent; its substrate is "etcd + CRD events" / "etcd compare-and-swap" in the §7 floors table (:699-701), non-deferred. `engine_class_candidate.py` corroborates: RECONCILER_LOOP excluded at LOCAL_DEVELOPMENT ("requires K8s control plane"); ESR+WAL are candidates. The E-3-only-fork split is grounded. |
| V9 | **Determinism AC traces to C-CP-08 §8.1 "no re-execution".** | **HOLDS** | `Spec_Control_Plane_v1_2.md:728` `engine_replay` row: "activity outputs cached and replayed; **no re-execution of activities**." U-CP-93's determinism AC (CP v2_34 :317) is a faithful by-execution restatement (side-effect counter unchanged across replay). §8.4/:770 indeed defers span-re-emission to impl, correctly noted as the reason the determinism AC is now plan-owned (E-spec-1 dropped). |
| V10 | **CP §16.5.9 invariant 5 (ZERO `CPAuditLedgerEntry` greenfield) — U-RT-122 / U-CP-95 cite.** | **HOLDS** | `Spec_Control_Plane_v1_25.md:164` invariant 5: "ZERO CP-audit emission at 5 greenfield composers… MUST NOT emit `CPAuditLedgerEntry`; their canonical role is IS state-ledger" — U-CP-49/U-CP-50 named in that set (:136). U-RT-122 / U-CP-95 ACs restate it correctly. |
| V11 | **Clearance markers honest; reviewer_chain matches reality.** | **HOLDS** | Both markers correctly record the Codex [P2] as APPLIED, the advisor probe, and list the adversarial review as "pending this PR's review pass" (= this pass). `merge_commit: (pending)`, PR "(pending)" — no over-claim. The CP marker's "delta-only-preservation verified (U-CP-01..92 headers byte-identical)" matches my V1 finding. |
| V12 | **Coverage completeness — every E-1/E-2 cleared subsection + design §6 cross-cutting → unit or disposition.** | **HOLDS (1 partial — see F3-02)** | §4.3 (CP) + §4.1b (RT) + companion §3 map every C-CP-07 §7.1/§7.4 row, §8.1/§8.2/§8.3, the 4 F3 floors, PathClass, consumer updates, the SINGLE_THREADED_LINEAR regression guard, and OQ-2 (ESR-not-DURABLE_ASYNC, resolved NO). E-3 explicitly OUT. The one gap: §8.3's second observable `resumption.is_replay` (F3-02). |

---

## Class 2 findings (substantive — current-phase plan revision)

### F2-01 — O-CP-4 silently diverges from its own controlling authority (architect rec §5), not just the design doc

- **Location:** `Implementation_Plan_Control_Plane_v2_34.md` §6 O-CP-4 (:545) + companion `.harness/r-fs-1-e-plan-decomposition.md` §5 (:91-100) + both clearance markers. The contradicted authority: `.harness/architect_recommendation_e_engine_fork_vs_impl.md` §5 "Operational discipline" bullet (:89).
- **Defect:** O-CP-4 (and companion §5) frames the R-CXA-2→E-2 relocation as **"correcting the design doc's E-1 attribution"** — citing only `r-fs-1-e-engine-classes-design-v1.md` §0/§2/§3.3. But the plan's **cited controlling authority for the entire fork-vs-impl decision** — the architect recommendation — *independently endorses E-1*. Architect rec §5 (:89, the sole line where R-CXA-2 appears in that doc) reads verbatim: *"The R-CXA-2 CP→IS engine-layer seam goes live at the first real producer **(E-1)**. **No fork — this leg of the design doc is correct.**"* So O-CP-4 overrides the architect rec, not merely the lower design doc, while attributing the correction only to the design doc. The architect rec is NOT internally reconciled to E-2 (it has exactly one R-CXA-2 mention, and it says E-1).
- **Discriminator (§2.7.6 → §4.1 mapping):** discriminator **(a)** — affects substantive content of a current-phase artifact; resolution is self-contained to the plan + a one-line annotation to the architect rec, NOT a teardown. The *technical substance is correct* (V3: ESR genuinely cannot fire the snapshot loop; WAL_SEGMENT is the DURABLE_ASYNC class), so this is NOT a halt — it is a framing/acknowledgment gap. **§2.7.6 Class 2 (substantive).**
- **Pattern-checklist hit:** item #1 **stale-carry-text disposition.** After O-CP-4 lands, architect rec §5 still reads "E-1 / this leg of the design doc is correct" while the plan + both clearance markers + companion say E-2. A future session that trusts the architect rec (the higher authority) over the plan gets the wrong slice for R-CXA-2 go-live. The design-doc carry was caught; the *authority's* identical now-stale claim was not.
- **Decision-claim:** *decided.* The text supports a single reading (architect rec §5 says E-1; O-CP-4 says E-2; O-CP-4 names only the design doc as corrected).
- **Resolution path (shape only — no replacement text supplied):** O-CP-4 + companion §5 should explicitly acknowledge that the relocation corrects **architect rec §5** (the controlling authority), not only the design doc, with the three-fact rationale already present. The architect rec owes a one-line reconciling annotation (a `.harness/` back-flow doc edit — mode-agnostic, X-AL-3-clean) noting §5's E-1 endorsement is superseded by O-CP-4's E-2 re-grounding. This converts the silent authority-divergence into a documented one. Whether to gate the plan merge on the architect-rec annotation, or land it as a paired follow-on, is an operator call — but the *plan's framing* ("corrects the design doc") must be widened to "corrects architect rec §5 + the design doc" before merge.

---

## Class 3 findings (doc-hygiene — inline fix)

### F3-01 — "R-CXA-2 (CXA §2.3.2)" pairs a roadmap identifier with a CXA-doc section as if both were CXA-doc labels

- **Location:** spec-cites throughout both plans, e.g. CP v2_34 U-CP-95 spec-linkage (:349 "CXA §2.3.2 R-CXA-2 (CP→IS engine-layer producer seam)"); RT v2_45 U-RT-122 (:67, :252); companion §5; §4.3/§4.1b coverage rows.
- **Defect:** The string **"R-CXA-2" appears in ZERO CXA-document versions** (v2_1..v2_20, count=0 in every version). It is a **roadmap / forward-register identifier** (canonical home: `.harness/roadmap_status.md` (78×), `.harness/post-phase-8-forward-register.md` where it is "CXA-2", and the `.harness/r-cxa-2-*` brief/spec/fork docs). The cite pairs it with "**CXA §2.3.2**" as though "R-CXA-2" were a CXA-doc edge label. **The §2.3.2 half resolves correctly** — it is the CP→IS bucket, and in the frozen-baseline CXA (v2.16 §0.4 / the v2.16-era §2.3.2 enumeration) it carries exactly `U-CP-49 → emit_pause_captured_state_ledger_entry (engine-layer free fn)` + `U-CP-50 → emit_resume_attempted_state_ledger_entry (engine-layer free fn)`. CXA v2.20 is a **delta-only doc** (133 lines, change-note v2.19→v2.20, only the NEW §2.3.8 R-PM-1 family present); §2.3.2's body lives in the frozen full-baseline per the documented `[[wrong-version-read-delta-only-baseline]]` convention (CLAUDE.md §1.1 "this plan-derived 7c baseline is FROZEN"). **So this is NOT a fabricated cite (Attack A4 does not fire) — both halves resolve, to two different but consistently cross-referenced artifacts**, and the pairing is established workspace usage (it appears identically in the forward-register).
- **Discriminator:** drift only — the cite resolves; it just dresses a roadmap ID as a CXA-doc label without disambiguation. **§2.7.6 Class 3 (doc-hygiene).** (Recorded here, NOT in rejected-findings, to avoid FM-G smoothing — the conflation is real even though it resolves.)
- **Decision-claim:** *decided.*
- **Resolution path (shape only):** disambiguate the pairing once (e.g. note "R-CXA-2" is the roadmap/forward-register seam-disposition ID and "CXA §2.3.2" the frozen-baseline CP→IS bucket the engine-layer composers enumerate). Low priority; consistent with prior workspace artifacts.

### F3-02 — §8.3's second observable `resumption.is_replay` is not asserted by any E-unit AC (partial coverage of a cited element)

- **Location:** U-CP-93 + U-CP-94 cite "C-CP-08 §8.3" wholesale (CP v2_34 :309, :329; §4.3 coverage :500-501). The spec element: `Spec_Control_Plane_v1_2.md` §8.3 (:756) commits **two** resumption observables — (1) `workflow.resumption` span with `resumption.kind` AND (2) "Subsequent step.boundary spans carry attribute `resumption.is_replay : bool`."
- **Defect:** Both E-unit ACs assert `resumption.kind` (`engine_replay` / `segment_replay`) but **no AC asserts `resumption.is_replay`** on step.boundary spans (grep for `is_replay` across CP v2_34 = empty). The units cite §8.3 wholesale yet exercise only its first observable. This is partial coverage of a cited spec element.
- **Discriminator:** drift / partial-coverage of a cited element; the durable-execution *semantics* are covered, only a span-attribute observable is unasserted. **§2.7.6 Class 3 (doc-hygiene).** (Borderline Class 2 if `resumption.is_replay` were a hard contract obligation, but §8.3 also flags it under the F2-12 carry-forward [CF-1] for span-re-emission, so its impl-time status is itself partly deferred — keeps it doc-hygiene, not substantive.)
- **Decision-claim:** *decided.*
- **Resolution path (shape only):** add a sub-AC to U-CP-93/94 asserting `resumption.is_replay` on the post-resumption step.boundary spans, OR narrow the §8.3 cite to "§8.3 (resumption.kind half; is_replay per F2-12 [CF-1])." Either closes the partial-coverage gap.

---

## Findings considered and rejected (transparency — what was attacked and did NOT surface a defect)

1. **Delta-only mutation of a prior unit (BLOCKING attack).** Full-file diff + targeted body extract-diff: all changes additive; deletions confined to §0/§7 version-metadata (legitimately superseded per delta-only-plan-chain convention). **No mutation of any prior unit body.** PASSES.
2. **X-AL-3 silent extension (the most load-bearing rule).** EngineClass closed at 5, ResumptionKind closed at 5 (direct enum read); ZERO spec edit in diff set; no new contract ID. Units *consume* cleared closed enums. **No extension.** (The architect-rec-cleared E-1/E-2-as-impl reading is operator-cleared and out of scope to re-litigate, per the task — not flagged.)
3. **O-CP-4 technical premise (snapshot-surface vs pure-event-replay).** Verified the recovery loop is a snapshot Protocol distinct from the workflow-layer; ESR has no DURABLE_ASYNC cell; WAL_SEGMENT does. **Premise sound** (the divergence-from-authority framing is the real issue → F2-01, not the technical call).
4. **Codex [P2] false-green fix.** Read the vacuous body myself (:368-375); confirmed U-CP-94's AC now requires a non-vacuous e2e + gates the fork-flip on it. **Fix correct + complete.**
5. **Cross-axis cycle / CP→RT import.** Verified duck-typed `ctx.engine_recovery_loop` consumption mirrors the real `ctx.pause_resume_protocol:1201` precedent; sole edge RT→CP downstream; no CP unit depends on a U-RT-*. **No cycle, no back-import.**
6. **By-execution AC shape (verification-shape grep-vs-e2e).** U-RT-122 / U-CP-95 / U-CP-94 ACs all demand "by execution, NOT grep / NOT bound" — entries LAND with correct shape against the durable substrate, distinct engine-layer `action_id`s, restart-simulation. **Verification shape is correct** (`[[verification-shape-sharpened-grep-vs-e2e]]` honored).
7. **Unit A / Unit B separation.** U-CP-94 (resumption semantics) vs U-CP-95 (firing branch) are genuinely separate units with U-CP-95 → [U-CP-94]; the built-but-vacuous trap is explicitly foreclosed. **Clean separation.**
8. **Phantom forward-cite (units/symbols not at HEAD).** All cited symbols (`RuntimeEngineRecoveryLoop`, the C-CP-49/50 composers, the Protocol, #475 Journal, the factory, `_determine_resume_at`) pre-exist at HEAD (direct read). No edge to a not-yet-existing unit. **No phantom.**
9. **Cross-spec drift (sibling specs citing stale new-unit shapes).** No `design-substrate/Spec_*.md` references U-CP-93/94/95 or U-RT-121/122 (expected for NEW units). No stale sibling-spec cite-shape. **No drift.**
10. **Clearance-marker honesty / reviewer-chain over-claim.** Markers record Codex-applied, advisor, "adversarial review pending this pass," merge_commit pending. **No over-claim.**
11. **CXA cite fabrication (Attack A4).** Initially suspected — but §2.3.2 resolves byte-exact in the frozen-baseline CP→IS bucket (U-CP-49/50 engine-layer composers). NOT fabricated; downgraded to doc-hygiene (F3-01) for the roadmap-ID-vs-CXA-label conflation only.
12. **Determinism AC over-reach (plan owning a spec-deferred behavior).** The "no re-execution" semantics ARE §8.1-committed (:728); only span-re-emission is §8.4-deferred. The plan correctly scopes the determinism AC to the committed semantics. **No over-reach / no X-AL-3.**

---

## Disposition

**APPROVE-WITH-CHANGES.** The E-plan decomposition is methodologically sound and empirically grounded: the BLOCKING delta-only-preservation check passes cleanly, X-AL-3 holds (closed-enum consumption, zero spec amendment), the Codex [P2] false-green fix is correct and complete, the DAG is acyclic with the correct RT→CP cross-axis direction and no CP→RT import, and every cited code line + spec section resolves at HEAD (one region-cite within tolerance). The O-CP-4 technical premise (R-CXA-2 → E-2) is correct on the merits.

The one substantive finding (**F2-01, §2.7.6 Class 2**) is a framing/acknowledgment gap, not a teardown: O-CP-4 silently overrides its own controlling authority (architect rec §5 endorses E-1) while attributing the correction only to the lower design doc. **Recommended pre-merge:** widen O-CP-4 + companion §5 framing to acknowledge the architect-rec §5 divergence, and file a one-line reconciling annotation to the architect rec (mode-agnostic `.harness/` edit). The two Class-3 items (roadmap-ID-vs-CXA-label disambiguation; the `resumption.is_replay` partial-coverage sub-AC) are inline doc-hygiene fixes that may land with F2-01 or as a follow-on.

**No §2.7.6 Class-1 (halt-execution) fork results.** Execution (E-impl-1) may proceed once F2-01's framing is corrected.

---

*Filed by harness-adversarial-reviewer (dedicated agent) per CLAUDE.md §10.9 pre-merge gate posture. Decorrelated second opinion: advisor() (transcript-aware) — surfaced F2-01 (the architect-rec-§5 divergence) and the §8.3 partial-coverage probe; both re-grounded by direct read before pinning. Severity scale: §2.7.6 (1=blocking / 2=substantive / 3=doc-hygiene), stated explicitly to avoid the §4.1-inverse taxonomy trap.*
