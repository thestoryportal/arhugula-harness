# Post-Phase-8 Forward Work Register

> **Status: Claude-authored register for operator planning.** Comprehensive enumeration of the integrations, implementations, and operator-led actions that remain **after** Phase-8 substitution-closure — each grounded in the design-substrate, with current production state and concrete close-out steps. **Posture: mode-agnostic** (process-substrate; reads `design-substrate/` + `harness-*/src/` + `.harness/`; authors only this `.harness/` file). Sibling to `.harness/R-700-phase-8-closure-accounting-draft.md` (the substitution accounting this register builds forward from). Compiled at HEAD `01cd0fa`, 2026-06-01.

---

## 0. Thesis — what "post-Phase-8" means for this harness

**Phase 8 closed the *substitution accounting*, not the *capability activation*.** Per the roadmap §1 + X-AL-2 (Meta-Architecture §7.7), a substitution RETIRES when (A) its cited units land **and** (B) the H_E scaffold surface is no longer invoked at the substitution site. **X-AL-2 never required the underlying capability to be exercised in production** — only that the hand-built H_T substrate displaced the Claude-Code scaffold. The ratified Phase-8 accounting was **46/54 RETIRED (85.2%) + 49/54 pipeline-advanced (90.7%)**. Batch-52 later back-flowed the live R-810/R-820 evidence for AS-8e, AS-8f, and CP-17; batch-53 then back-flowed OD-4 and CXA-4; batch-54 lands the CP→AS runtime composer; batch-55 records CXA-2 as a counted bounded residual; batch-56 retires CXA-1 after the AS→IS producer and edge-scope audit close. The current live ledger is **54/54 RETIRED (100.0%) + 54/54 pipeline-advanced (100.0%)**.

What remains is a **different axis of work**: *activation, deployment, scale, and external integration* of capabilities that are, in many cases, **library-complete / structurally present but not yet exercised at runtime** (multi-LLM routing, real sandbox isolation, non-LOCAL deployment surfaces, multi-tenant redaction, MANAGED_CLOUD secrets, external MCP/Files/managed_agents integrations). This is **not "fixing incomplete retirements"** — it is building the production frontier the MVP deliberately stopped short of.

The register has **two tiers**, kept distinct:

- **Tier A — Phase-8 closure residuals.** These have now been dispositioned: the R-700 review declared Phase 8 closed, bounded-residual sign-offs were ratified, the invisible-row `R-NNN` coverage was authored, and the config-discovery fork was ratified. Historical detail remains below for provenance.
- **Tier B — Post-Phase-8 forward activation.** The genuinely-forward surfaces (Multi-LLM IV / Multi-deployment V / Multi-tenant VI / External-integrations IX / Research X / Operator-tooling XI) + the CXA composition seams.

**This register also discharges a roadmap obligation.** Roadmap §1 marks Surfaces IV / VI / VIII–X as `decomposition-owed` (§9). This document **is** that decomposition for IV / VI / IX / X — its items can seed `R-300` / `R-500` / `R-800` / `R-900` entries, which also closes the **no-`R-NNN`-entry gap** R-700 surfaced for `CXA-1/2/3/4` + `CP-17`.

**Standing directive (operator 2026-07-14) — ground every row before build-or-block.** `Project_Roadmap_v1.md` §5.0b + `[[full-register-ground-every-arc-before-blocking]]` extends the R-FS-1 "nothing deferred" discipline to this register: `registered_finding` / `operator_gated` / `design_substrate_gated`, and the empty `B-16` speculative bucket, are not stop conditions. Every row gets an active grounding pass against the design-substrate before it is built or left open with a label — a label describes current state, not permission to skip investigation.

**Council note (answering the standing question).** Compiling *this register* is descriptive synthesis grounded in substrate — **not** a design decision with a nameable cross-domain tension, so per CLAUDE.md §13.2 + §10.9 it routes to solo + `advisor()`, not the council. But several **forward arcs below carry nameable two-voice tensions that ARE council-eligible the moment the operator opens them to execute** — each is flagged `⚖️ council-eligible` with the named voices. (The roadmap already encodes this on R-410/411/412 via `council_required: conditional:nameable-tension`.)

---

## 1. Front summary table

*The Tier B table below stops at `B-17` (it predates `B-18` onward, added incrementally over several later sessions and never backfilled here). It is not the authoritative status source for anything past `B-17` — run `just forward-register --open` (`.harness/forward-register.yaml` + `tools/forward_register.py`) for the current, CI-validated status of every item; this table + the per-item sections below remain the prose detail each row's `heading` field back-points into.*

### Tier A — Phase-8 closure residuals (declare Phase 8 done)

*Status legend: ~~strike~~ = definitively closed · **[open]** = actionable now · **[blocked]** / **[deferred]** / **[proposed]** = not-closed-not-open (word = why). Mirrors the live dashboard's R-NNN board + Post-Phase-8 register sections.*

| # | Status | Item | Owner action | Spec/source | Council? |
|---|---|---|---|---|---|
| A-1 | ~~closed~~ | ~~R-700 Phase-8 substitution review + final integer ratification~~ | Phase 8 declared closed at 46/54 retired + 49/54 pipeline-advanced; batch-56 live ledger now reports 54/54 + 54/54 | `R-700-phase-8-substitution-accounting` RESOLVED; batch-56 | no (AUQ) |
| A-2 | ~~closed~~ | ~~Bounded-residual sign-offs (AS-8e, AS-8f, OD-6)~~ | Operator ratified terminal bounded-residual dispositions | X-AL-2 §5.3; Surface VIII | no (AUQ) |
| A-3 | ~~closed~~ | ~~`R-NNN` coverage for the 5 invisible open rows (CXA-1/2/3/4 + CP-17)~~ | CXA-1/2/3/4 and CP-17 now have roadmap entries | R-700 draft §C item 2 | no |
| A-4 | ~~closed~~ | ~~`harness.toml` auto-discovery fork ratification~~ | Reading A ratified 2026-06-06; implementation had already shipped at PR #279 | `R-100-mvp-config-discovery` RESOLVED | no |

### Tier B — Post-Phase-8 forward activation

| # | Status | Surface | Item | Current state | Close-out class | Council? |
|---|---|---|---|---|---|---|
| B-1 | ~~closed~~ | IV Multi-LLM | ~~Layered capability-aware routing activation~~ | **routing activation RESOLVED (R-300-multi-llm-routing-activation, PR #213)** — declarative layer live; the multi-provider *exercise* is B-2 | impl + operator creds | ⚖️ yes |
| B-2 | ~~closed~~ | IV Multi-LLM | ~~Multi-provider credentials + mixed-provider exercise~~ | **R-300 second-provider exercise RESOLVED** — deterministic fallback + live Anthropic→OpenAI (#281) + live Ollama (#283) | PR #281 + PR #283 | no |
| B-3 | ~~closed~~ | V Deployment | ~~Real TIER_2 container sandbox execution (R-410)~~ | **R-410 RESOLVED by local Docker execution driver + live TIER_2 container e2e** | this PR | ⚖️ yes |
| B-4 | ~~closed~~ | V Deployment | ~~TIER_3 microVM + TIER_4 full-VM execution (R-411/R-412)~~ | **R-411 RESOLVED** by Docker + gVisor/runsc ToolExecutionDriver on the operator-provisioned Lima Linux VM; **R-412 RESOLVED** by the managed E2B full-VM ToolExecutionDriver + live Tier-4 dispatcher e2e | impl + infra | ⚖️ yes |
| B-5 | ~~closed~~ | V Deployment | ~~SELF_HOSTED_SERVER + MANAGED_CLOUD e2e (R-420/R-421)~~ | **R-420 RESOLVED by local single-node self-hosted daemon + collector + keyring live e2e; R-421 RESOLVED by approved E2B + GCP Secret Manager + authenticated Cloud Run collector live e2e** | this PR | ⚖️ closed |
| B-6 | ~~closed~~ | V Deployment | ~~OTLP tail-keep collector-side validation (R-430)~~ | **R-430 RESOLVED** by the R-420 local real-collector tail-keep live proof | PR #326 | no |
| B-7 | ~~closed~~ | V Deployment | ~~Tier-level self-hosted secrets backend selector (R-440)~~ | **SELF_HOSTED keyring-only selector landed; cloud bootstrap-token remains R-421** | this PR | no |
| B-8 | ~~closed~~ | VI Multi-tenant | ~~Non-default `tenant_id` / non-SOLO `persona_tier` deployment~~ | **R-500 RESOLVED** by the local self-hosted multi-tenant live proof | PR #328 | ⚖️ closed |
| B-9 | ~~closed~~ | VI Multi-tenant | ~~OD-4: per-session redaction toggle (§13.1) + opaque-token tokenization (§13.2)~~ | **R-008 + batch-53 RESOLVED** — runtime code residual closed; live ledger back-flow moves OD-4 to RETIRED | accounting/back-flow | ⚖️ closed |
| B-10 | ~~closed~~ | IX External | ~~Real external MCP server connection~~ | **✅ R-800 RESOLVED (2026-06-01)** — `start()`/`shutdown()` wired at PR #172 (spec v1.41 §14.9.8 Gaps B/F); real external stdio e2e green & unconditional at `test_u_rt_86`. Full `api.run` path = Gap D (R-100 AC#2, operator-gated) | impl | no |
| B-11 | ~~closed~~ | IX External | ~~Files API integration (AS-8e / CP-17)~~ | **R-810 RESOLVED by real Anthropic Files upload/reference/delete plus managed-cloud `files.operation` Cloud Trace proof** | this PR | no |
| B-12 | ~~closed~~ | IX External | ~~managed_agents integration (AS-8f)~~ | **R-820 RESOLVED** by the real Anthropic Managed Agents SDK/session integration plus managed-cloud `managed_agents.*` proof | PR #380 | no |
| B-13 | ~~closed~~ | IX External | ~~Memory-tool production backend (CP-16)~~ | **RESOLVED (R-830)** — filesystem/SQLite/S3 backends landed; managed-DB (`ManagedSqlMemoryToolBackend`) live-proven against Neon PostgreSQL, gate closure recorded `closed_live_e2e_passed` at `.harness/codex_credential_gates.jsonl` (2026-06-08) | impl (done) | no |
| B-14 | ~~closed~~ | CXA | ~~Cross-axis seam completion (CXA-1)~~ | **CXA-1 is closed by batch-56** after the workflow-time scoped secret-fetch producer, resolver-bound AS→IS write, and edge-scope audit landed. **CXA-2 is closed by batch-55** as a counted bounded residual after provider-turn HITL continuation landed and durable recovery was recorded as post-MVP hardening; **CXA-3 is closed by batch-54** after the CP→AS runtime composer landed; **CXA-4 is closed by batch-53** after 0-wireable grounding. | impl (mechanical) | no |
| B-15 | ~~closed~~ | XI Tooling | ~~Dashboard iteration-2 (R-XI-02/R-XI-03)~~ | **Both RESOLVED 2026-06-02** (PR #235 dep-graph+sparklines; PR #237 live-poll) — stale-open correction, found 2026-07-14; Surface XI `next_pointer` is null, no open levers | impl (done) | no |
| B-16 | **[proposed]** | X Research | Open architectural / speculative arcs | not decomposed | research | no |
| B-17 | ~~closed~~ | XII Methodology *(NEW)* | ~~ICM workspace-methodology adoption~~ | **RATIFIED 2026-07-30** — governance-layer-only scope; ICM `<80`/`<200`-line caps for `CONTEXT.md` / router files ONLY (`design-substrate/` specs exempt); CONSERVATIVE L0 split. `R-ICM-1` + `R-ICM-2` delivered in one PR — see the per-item section below | design + ops (delivered) | ⚖️ resolved by ratification |

---

# TIER A — Phase-8 closure residuals

## A-1 · R-700 Phase-8 substitution review + final-integer ratification
- **What it is.** The operator-owned Phase-8 review (`R-700-phase-8-substitution-accounting`, BLOCKED) that consumes the R-700 draft and declares all 54 substitutions RETIRED or RETIRED-AS-BOUNDED-RESIDUAL with rationale (Surface VIII closure criterion, roadmap §1).
- **Current state.** CLOSED. The operator lifted the hold and Phase 8 was declared closed on 2026-06-02 at 46/54 RETIRED and 49/54 pipeline-advanced. Batch-52, batch-53, batch-54, batch-55, and batch-56 are forward live-ledger supersessions, not rewrites of that declaration: R-810/R-820 evidence moves AS-8e, AS-8f, and CP-17 to RETIRED; the OD-4/CXA-4 accounting back-flow moves two more rows to RETIRED; the CP→AS runtime composer moves CXA-3 to RETIRED; the CP→IS seam moves CXA-2 to counted bounded residual; the AS→IS seam moves CXA-1 to SUBSTANTIVE_RETIRED. The live ledger now reports 54/54 RETIRED and 54/54 pipeline-advanced.
- **Close-out steps.** None. **Council: no** — this was a ratification (advisor + AskUserQuestion), not a multi-voice design tension.

## A-2 · Bounded-residual sign-offs (AS-8e, AS-8f, OD-6)
- **What it is.** Three substitutions closed as deferred-by-design / dormant-at-MVP that need an explicit operator Phase-8 sign-off per X-AL-2 §5.3: **AS-8e** (`files.*` namespace, Files-arc Memory-only MVP scope, runtime spec v1.17 §14.C); **AS-8f** (`managed_agents.*`, production-only exclusion, `class_1_fork_as_8f_...` Q1=C); **OD-6** (local-first OTLP sqlite ingestion — `flush_to_sqlite` dormant at MVP, FIRST bounded-residual close in the ledger, batch-51).
- **Close-out steps.** None for Phase-8 accounting. The operator ratified the terminal bounded-residual dispositions. Later implementation proofs for Files and managed_agents are tracked by R-810/R-820; batch-52 is the separate accounting/back-flow action that moves AS-8e, AS-8f, and CP-17 in the live ledger. **Council: no** (sign-off).

## A-3 · `R-NNN` coverage for the 5 invisible open rows
- **What it is.** R-700 surfaced that `CXA-1/2/3/4` + `CP-17` have **no roadmap `R-NNN` entries** — the R-002 Surface-I decomposition surveyed only the per-axis `§4.1`, which excludes the CXA axis (no `§4.1` file) and didn't frame CP-17's batch-44 SB-INDEF reclassification as open.
- **Close-out steps.** None. CP-17 is tracked by R-010/R-810, and CXA-1/2/3/4 are tracked by R-CXA-1..4. **Council: no.**

## A-4 · `harness.toml` auto-discovery fork ratification
- **Status.** CLOSED. The operator ratified Reading **(A)** CWD discovery on 2026-06-06; empirical grounding found PR #279 (`a394032`) had already implemented that reading.
- **Closure.** `RuntimeConfigSource.load(config_file=None)` now discovers CWD-local `harness.toml`, preserves env+CLI-only behavior when absent, and lets explicit `--config` take precedence over discovery. `R-100-mvp-config-discovery` is RESOLVED; no spec amendment is owed. **Council: no** (AskUserQuestion was the only gate).

---

# TIER B — Post-Phase-8 forward activation

## Surface IV — Multi-LLM maturity (R-300..R-399, decomposition-owed)

> **The commitment (ADR-F1 v1.2 + `Target_Stack_Commitment_v1` §5.1–5.2).** Per-provider official SDKs (`anthropic` + `openai` + `ollama`) under a **capability-aware abstraction** — NOT LiteLLM — with a layered cheapest-deterministic-first routing strategy (declarative manifest → embedding classifier → LLM-as-router) and feature-preservation (Anthropic prompt-caching / extended-thinking / batch / MCP host must remain reachable, no LCD flattening). Spec surface at CP spec `C-CP-01..C-CP-04`. These correspond to substitution rows `H_T-CP-1..CP-5` (all RETIRED-as-substitution — legitimately; the library displaced the scaffold).

### B-1 · Layered capability-aware routing activation `⚖️ council-eligible`
- **Current state (empirically verified this session).** Provider construction is **live** — all 3 adapters (`AnthropicAdapter` / `OpenAIAdapter` / `OllamaAdapter`) built at bootstrap stage-3a (`providers.py`). The retry/breaker/**fallback-chain dispatch** (`C-RT-16`, `lifecycle/retry_breaker_fallback.py`) **is wired** at the stage-5 `StepDispatcher` and iterates fallback candidates (primary → same-family → cross-family → terminal) with `advance_or_raise` on per-candidate exhaustion. **But the layered routing-*selection* path is a stub:** `routing_core_surface.infer()` raises `NotImplementedError` (`routing_core_surface.py:83`/`:97`), and `layered_routing_strategy.route()` has **zero non-test callers** (verified). So *which provider to use* is taken statically from the manifest `model_binding`, not from capability-aware layered routing.
- **What this means for the harness.** Provider plumbing + failure-time fallback exist; **capability-aware provider *selection* is not active.** Combined with B-2, the multi-provider machinery is present but **unexercised at MVP**.
- **Close-out steps.** (1) Implement the `infer()` composition seam to invoke `layered_routing_strategy.route()` (declarative-layer first) before provider dispatch (CP spec C-CP-02); (2) bind per-layer time-budgets (C-CP-03 `LayerBudget`); (3) implement capability-shortfall fallback (C-CP-04 + ADR-F1 §Consequences(c)) — fall to a capable provider *before* the step fails when the primary lacks a required capability (e.g. extended-thinking).
- **`⚖️ Council (C5 ⊥ C9 ⊥ capability-preservation).`** Designing the routing-selection policy is a genuine tension: **cost** (cheapest-deterministic-first) vs **reliability/recovery** (when to fall back / breaker thresholds) vs **capability-preservation** (don't route a thinking-required step to a non-thinking model to save cost). Convene a dyad (cost-voice + reliability-voice) when this arc opens.

### B-2 · Multi-provider credentials + mixed-provider exercise
- **Status.** CLOSED 2026-06-03. The R-100 gap was closed by PR #281 (`2dc25e6`) and PR #283 (`e436252`).
- **Closure.** PR #281 added the deterministic production-path cross-family fallback fixture and the live Anthropic invalid-model → OpenAI `gpt-4o-mini` exercise (`just mvp-r300-cross-family`, live PASS 4.55s). PR #283 added the free local Ollama fallback exercise (invalid model → `llama3.2:3b`, live PASS 4.17s through `api.run`). Deterministic CI coverage remains for non-credentialed runs; the live provider halves are recorded in `.harness/roadmap_status.md`. **Council: no**.

---

## Surface V — Multi-deployment surfaces (R-400..R-499)

> Per ADR-D2 (per-deployment-surface sandbox provider) + ADR-F4 (4-tier blast-radius) + ADR-F5 (tier-aware secrets) + `C-AS-15 §15` + `C-RT-29 §14.18` (daemon). The **12-cell `deployment_matrix.py`** maps `(DeploymentSurface, BlastRadiusTier) → (SandboxTier, SandboxProviderClass)`: LOCAL/SELF_HOSTED use process/container/microVM/full-VM provider classes with **LOCAL keyring** secrets; **MANAGED_CLOUD** reserves FULL_VM at TIER_4 and **defers** its secrets backend to prod-tech. `multi-tenant-compliance × local-development` is a **closed cell** (raises `CellBindingViolation`). **R-410 now makes the first tier executable via a local Docker `ToolExecutionDriver`; higher tiers and deployment surfaces remain the honest frontier.**

### B-3 · Real TIER_2_CONTAINER sandbox execution (R-410) `⚖️ council-eligible`
- **What it is.** Make a tool call resolved to `TIER_2_CONTAINER` actually execute inside a container boundary (verifiable FS/network isolation), not in-process. **The honest heart of Surface V.**
- **Current state.** RESOLVED by this PR: `RuntimeToolDispatcher` now delegates execution through a `ToolExecutionDriver`, the default driver preserves prior MCP-host behavior, and `DockerToolRunnerExecutionDriver` runs TIER_2 calls inside a local-only Docker container by resolved immutable image id.
- **Close-out evidence.** The live e2e drives a TOOL_STEP resolved to `TIER_2_CONTAINER` through Docker, asserts outbound network is blocked, and asserts the host worktree path is not visible. Existing dispatcher tests cover under-tier rejection and sandbox.enter/exit provider/tech attribution. No provider credentials or paid calls required.
- **`⚖️ Council (C10 ⊥ C11) — roadmap already flags `conditional:nameable-tension`.`** Action-safety/blast-radius (C10) wants real isolation; operator-loop/local-deployment (C11) wants minimal provisioning burden. This slice resolves the tension narrowly with a local-only Docker driver that preserves the default in-process path unless explicitly injected.

### B-4 · TIER_3 microVM + TIER_4 full-VM execution (R-411 / R-412) `⚖️ council-eligible`
- **What it is.** Extend executable isolation up the ladder: TIER_3 (gVisor/Kata, EXTERNAL_REVERSIBLE) then TIER_4 (firecracker/full-VM, EXTERNAL_IRREVERSIBLE, **MANAGED_CLOUD-only** per the matrix).
- **Current state.** R-411 is closed for the selected no-taxonomy-change path. `GVisorRunscToolRunnerExecutionDriver` extends the R-410 Docker runner substrate, requires `SandboxTier.TIER_3_MICROVM`, and invokes Docker with `--runtime=runsc`. The approved live e2e targets the operator-provisioned Lima Linux VM through `R411_GVISOR_DOCKER_COMMAND`; host readiness passed, the TOOL_STEP executed under `runsc`, network egress was blocked, and the host repo path was not visible. R-412 is closed by the operator-approved managed E2B path: `E2BManagedFullVMToolRunnerExecutionDriver` requires `SandboxTier.TIER_4_FULL_VM`, creates an E2B managed sandbox with outbound internet disabled by default, and exchanges the same JSON runner shape as the local Docker/gVisor drivers. The approved live e2e (`just r412-e2b-full-vm-live-e2e`) passed on 2026-06-08 with provider `e2b-managed` and tech `e2b-firecracker`.
- **Close-out steps.** None for R-411/R-412. Local Firecracker and QEMU `microvm` remain valid future self-managed variants, but the roadmap Tier-4 managed-cloud lane is closed by E2B. **`⚖️ Council (C10 ⊥ C11)`** inherited from B-3.

### B-5 · SELF_HOSTED_SERVER + MANAGED_CLOUD deployment e2e (R-420 / R-421)
- **What it is.** First real non-LOCAL surfaces. **R-420 (SELF_HOSTED_SERVER):** harness daemon (`C-RT-29 §14.18`, FastMCP Unix-socket) against a **real OTLP collector** + tier-level secrets; tail-keep wrapping active (non-LOCAL); per-cell sampler `base_rate` = the SELF_HOSTED cell. **R-421 (MANAGED_CLOUD):** cloud env + cloud secrets + FULL_VM + managed collector; in-sandbox encrypted-fs secrets per ADR-F5; MANAGED_CLOUD per-cell sampler + redaction posture (`C-OD-13 §13.1`).
- **Current state.** R-420 is **RESOLVED** by the local single-node SELF_HOSTED_SERVER stack at `deploy/self-hosted-local/`: host-run harness daemon + Docker Compose OTel Collector Contrib, Tempo, and Grafana; `harness.selfhosted.local.example.toml` selects `SELF_HOSTED_BACKEND_COLLECTOR` and `self-hosted-keyring`; `just r420-self-hosted-stack-*` controls the backend. Closure evidence 2026-06-07: Docker stack running, `just r420-self-hosted-readiness harness.selfhosted.local.toml` passed, and `just r420-self-hosted-live-e2e harness.selfhosted.local.toml` passed with workflow `r420-self-hosted-tool-echo`, daemon status `success`, cost `0`, hosted-provider-calls `0`. The live workflow uses local Ollama plus non-secret keyring sentinel `r420_probe_key`; no hosted-provider inference is performed. R-421 is **RESOLVED** by the approved E2B + GCP Secret Manager + authenticated Cloud Run collector live e2e: `just r421-managed-cloud-readiness /private/tmp/r421-managed-cloud.live.toml --hosted-sandbox-provider e2b` passed, the hosted E2B sandbox ran the deterministic command, authenticated OTLP export reached Cloud Run, and Cloud Trace observed trace `d848a4da6622f42407a5e58c507513c5` with spans `r421.managed_cloud.root` and `sandbox.violation`.
- **Close-out steps.** None for R-420/R-421. R-420 **unblocks R-430** and removes the self-hosted prerequisite for R-421; R-421 supplied the managed-cloud prerequisite that R-412 consumed for the selected E2B full-VM provider path. **`⚖️ Council (R-421 only)`** — closed by the selected E2B + GCP path.

### B-6 · OTLP tail-keep preservation validation (R-430)
- **What it is.** Verify the `§10.2` classification-trigger preservation semantic against a **real** OTLP collector (the drop/keep decision is collector-side).
- **Status.** CLOSED by this PR. `TailKeepSpanProcessor` buffer logic remains in-process + bypassed at LOCAL by design (§9.1 head-based mandate), but the preservation semantic is now exercised against the R-420 local real-collector substrate.
- **Closure evidence.** `just r430-tail-keep-live-e2e harness.selfhosted.local.toml` passed against the local OTel Collector/Tempo stack: trigger trace `4972258a693b5d34c32c89ecd30749bc` exposed `r430.trigger.root` + `sandbox.violation` in Tempo, plain trace `364f9516e5f95cae58f4b44219981626` stayed absent through the negative window, and the command reported `trigger-trace-preserved=true`, `non-trigger-trace-exported=false`, `cost=0`, `hosted-provider-calls=0`.
- **Residual.** None for R-430. Managed-cloud collector posture remains R-421 scope; multi-tenant/non-SOLO telemetry posture remains R-500 scope.

### B-7 · Tier-level self-hosted secrets backend selector (R-440)
- **Status.** CLOSED by this PR. `ProviderSecretsConfig` now carries a `ProviderSecretBackend` selector. The default LOCAL path remains keyring + env fallback; `self-hosted-keyring` resolves through keyring only and disables ambient env fallback.
- **Closure evidence.** Focused tests prove `self-hosted-keyring` raises `SECRET_UNKNOWN` when only `ANTHROPIC_API_KEY` is set, resolves when the keyring entry exists, preserves LOCAL env fallback, and makes `just self-hosted-readiness` pass its static R-440 gate from TOML config.
- **Residual.** Live server/collector/keyring-entry exercise remains R-420. MANAGED_CLOUD bootstrap-token / in-sandbox HTTP fetch remains R-421 managed-cloud scope.

---

## Surface VI — Multi-tenant (R-500..R-599, decomposition-owed)

> Per `C-OD-13 §13.1` (redaction toggleability gradient) + `C-OD-10 §10.3` (per-`(persona_tier, deployment_surface)` sampler base-rate, 8-row table) + ADR-D5/D6. Three persona tiers (SOLO_DEVELOPER / TEAM_BINDING / MULTI_TENANT_COMPLIANCE) × three surfaces.

### B-8 · Non-default `tenant_id` / non-SOLO `persona_tier` deployment `⚖️ council-eligible`
- **Closure.** R-500 closes the self-hosted multi-tenant exercise on the R-420 local collector stack. `RuntimeConfig.tenant_id` now materializes as authoritative OTel `tenant.id` resource attr; `just r500-multitenant-live-e2e harness.selfhosted.local.toml` overlays two non-default tenants with `multi-tenant-compliance`, verifies `base_rate=0.2`, proves non-toggleable redaction strips default-off content before Tempo while preserving structure attributes, and exercises tenant-scoped audit-ledger reads. Closure evidence 2026-06-07: tenant A trace `5c0e5916bf84933296323f2038c6680b`, tenant B trace `a847370bbc76adc41b85e3328d3279aa`, `tenant-resource-separated=true`, `content-redacted=true`, `audit-ledger-separated=true`, `cost=0`, `hosted-provider-calls=0`.
- **`⚖️ Council (C7 + C8 ⊥ C11).`** Observability/privacy (C7) + security/compliance (C8) vs operator-burden (C11): how much redaction/audit ceremony is mandatory at TEAM vs MULTI_TENANT. Convene when the multi-tenant arc opens.

### B-9 · OD-4: per-session redaction toggle (§13.1) + opaque-token tokenization (§13.2) `⚖️ council-eligible`
- **What it is.** The OD-4 substitution row was `PARTIAL` / `RETIRED-AS-CROSS-AXIS-DEFERRED` in the Phase-8 accounting, but the previously open runtime gates have now been implemented and the live ledger was back-flowed at batch-53.
- **Current state.** The §13.1 solo-developer per-session toggle is closed. The 2026-06-07 R-008 slices added an OD-owned opaque-token substrate, durable audit-ledger token-map persistence, provider-free category labeling, and the runtime `EvalGradeSemanticRedactionClassifier` path for GenAI prompt/response, tool argument/result, retrieval, file, memory, skill, PII, and secret surfaces. Stage 4 now wires `MULTI_TENANT_COMPLIANCE` redaction through `OpaqueRedactionTokenizer` + `AuditLedgerRedactionTokenMap` when an audit sink is available; without an audit sink, it preserves fail-closed strip mode.
- **Close-out steps.** None. Batch-53 performs the separate accounting/back-flow decision and moves OD-4 from `PARTIAL` to `SUBSTANTIVE_RETIRED`; the `RETIRED-AS-CROSS-AXIS-DEFERRED` label remains historical Phase-8 provenance only.
- **`⚖️ Council (C7 ⊥ eval-utility/C11).`** Privacy-default-off (C7) vs eval-grade content-shape-reconstruction utility: tokenize vs strip is a real two-voice call. Convene when R-008 opens.

---

## Surface IX — External integrations (R-800..R-899, decomposition-owed)

### B-10 · Real external MCP server connection
- **What it is.** Connect the FastMCP client host to a **real external MCP server** for operator `api.run` TOOL_STEP execution (the TOOL_STEP path the R-100 use-the-product probe exercised).
- **✅ RESOLVED 2026-06-01 (R-800).** This "Current state" framing was **stale** — it was authored at PR #209 but describes pre-PR-#172 state. Verified empirically: the bootstrap **does** call `await ctx.mcp_client_host.start()` (`stage_3a_cp_clients.py:59`) and `host.shutdown()` is wired at teardown (`shutdown.py:484-489`) — both landed at PR #172 (spec v1.41 §14.9.8 Gaps B/F). The converter (spec v1.40, Reading B) + sandbox-decision-resolver (spec v1.41 §14.9.8, Reading B) also landed. A **real external stdio MCP server** is exercised end-to-end at `test_u_rt_86` (production factory `materialize_mcp_client_host_stage` + real subprocess + handshake + list_tools + TOOL_STEP dispatch + 7-attr `mcp.*` span) — **unconditional, no LLM, 9/9 green** (with `test_ac2_bootstrap_path_wiring`).
- **Original close-out steps (all met).** (1) `host.start()` at stage-3a — ✅ `stage_3a_cp_clients.py:59`; (2) `host.shutdown()` at teardown — ✅ `shutdown.py:484-489`; (3) operator `MCPClientConfig.connection_url` + per-server defaults — ✅ surface landed (spec v1.40/v1.41); (4) live e2e against a real external server — ✅ `test_u_rt_86` (real subprocess). **Council: no** (mechanical; confirmed not needed).
- **Residual (out of R-800 scope) — ✅ RESOLVED 2026-06-12 (this bullet was stale-carry-text, corrected 2026-07-12 at `B-GAPD-TOOLONLY-BOOTSTRAP` grounding).** The **full `api.run` bootstrap TOOL_STEP path** (must_pass[3] strict reading), **Gap D / R-100 AC#2**, is CLOSED. The C9⊥C11 tension (fail-fast reliability ⊥ tool-only-workflow ergonomics) was probed, dyadic-council-scoped, and operator-ratified (Reading B, AskUserQuestion 2026-06-12) at PR #515 (`feat(R-CC-1): inference-conditional provider bootstrap`) — see `.harness/class_1_fork_api_run_unconditional_provider_ping_for_tool_only_workflows.md` (Status: RESOLVED). Stage 3a now skips provider construction entirely for workflows where `_workflow_requires_inference()` (`api.py:524`) is False; `test_r100_ac2_tool_step_e2e.py` carries no `skipif` marker and passes green with zero provider credentials. Full re-verification at `.harness/b-gapd-toolonly-bootstrap-closure-record.md`.
- **Residual (found at `B-MCP-OAUTH-RS-ENFORCE` grounding, 2026-07-12, `.harness/b-mcp-oauth-rs-enforce-closure-record.md`) — split out to `B-37` 2026-07-14, corrected there (round-3 out-of-family Codex): this bullet's original function-naming was backwards.** Since R-800, a real remote **streamable-HTTP** client path was also added and is exercised through the production factory (`B-MCP-HOST-REMOTE-TRANSPORT`, `test_r_cl_p3_live_multi_tier_e2e.py`, L1-signed-pinned) — this stale "stdio only" framing above should be read as historical. Two genuine, independent gaps surfaced verifying that path; see `B-37` below for the corrected diagnosis (`streamable_http_client`, the one already imported, is CURRENT; `streamablehttp_client` is deprecated — the opposite of what this bullet originally said) and close-out direction. Neither blocks any current close. **Council: no** (mechanical fix once opened).

### B-11 · Files API integration (AS-8e / CP-17)
- **What it is.** Anthropic Files API (`/v1/files` upload/list/metadata/delete; `file_id` reference in message content) — ADR-D3 primitive #10; the `files.*` observability namespace (AS-8e) + the CP-16/17 Files-primitive consumption.
- **✅ RESOLVED 2026-06-08 (R-810).** Operator opened the previously deferred Files arc. The runtime design shape is the already-ratified FilesAPIClient/adapter + file_id-reference-composition path from `.harness/class_1_fork_h_t_cp_16_17_executable_consumer_absence.md` §13.6.C, landed by the R-810 runtime port and real Anthropic Files adapter.
- **Close-out steps (all met).** (1) **Design-phase:** Files arc opened at operator-discretion timing with the §13.6.C runtime unit shape; (2) **impl:** managed-cloud Anthropic Files client adapter + document file-reference and Batch request composition landed; (3) **e2e:** live Anthropic upload + reference-by-id + delete path passed, and `files.operation` exported through the authenticated managed collector was observed in Cloud Trace `bfd28fa8fc8ecc3ba973d1e405cdb865` with `files.*` attrs. The uploaded live file is no longer present (cleanup retry returned Anthropic 404). Temporary GCP TokenCreator IAM used for Cloud Run ID-token minting was removed.

### B-12 · managed_agents integration (AS-8f)
- **What it is.** Anthropic managed_agents primitive + the `managed_agents.*` 3-attribute namespace.
- **Current state.** **RESOLVED for the R-820 runtime/integration gate** — PR #380 added the real Anthropic Managed Agents SDK/session adapter and live managed-cloud e2e proof. Evidence: session `sesn_019aMgaF8sAW2cXhhpMTYij4` reached `session.status_idle`; `managed_agents.runtime` exported to the managed collector; Cloud Trace trace `009d7716b19c75e4ad7edb93e78f8d2b` carried `managed_agents.*` attributes. Temporary Cloud Run Token Creator IAM used for the proof was removed and verified absent.
- **Close-out steps.** R-820 is closed. Batch-52 performs the separate substitution-tally back-flow for AS-8f, moving it from the Phase-8 accepted-indefinite-defer disposition to `SUBSTANTIVE_RETIRED` in the live ledger. **Council: no.**

### B-13 · Memory-tool production backend (CP-16)
- **What it is.** A production storage backend for the Memory tool beyond local-filesystem. ADR-D3 #11 (Memory tool is client-side; harness implements the backend). `C-RT-22` `MemoryToolRegistry` + `MemoryToolStorageBackendProtocol`.
- **Current state.** **RESOLVED.** Registry + **FILESYSTEM backend landed** (`LocalFilesystemMemoryToolBackend`; `CP-16` closed RETIRED-AS-BOUNDED-RESIDUAL batch-44), **SELF_HOSTED_SERVER `DATABASE` backend landed** (`SqliteMemoryToolBackend`, stdlib sqlite3), **MANAGED_CLOUD S3 cloud-vault backend live-proven**, and **MANAGED_CLOUD PostgreSQL managed-DB backend live-proven**. The approved live S3 e2e bound `MemoryToolStorageBackend.S3` on `DeploymentSurface.MANAGED_CLOUD`, used AWS CLI profile credentials, and exercised create/view/str_replace/insert/delete through the Memory tool dispatch seam. The managed-DB backend (`ManagedSqlMemoryToolBackend`) handles PostgreSQL-compatible `postgres://` / `postgresql://` `DATABASE` bindings, the factory lazily constructs `psycopg` only for live use, and the operator-approved Neon e2e passed against the `Arhugula` project.
- **Close-out steps.** All R-830 production backend slices are closed. The managed-DB proof used `just r830-managed-db-live-e2e`, created the `memory_entries` table if needed, wrote a unique `/memories/live/...` row, exercised create/view/str_replace/insert/delete through `_invoke_protocol_callback`, and cleaned up. Gate closure is visible in `.harness/codex_credential_gates.jsonl`. **Council: no** (impl).

---

## CXA — Cross-axis composition seam completion (B-14)

> **Status spine = the live substitution ledger after batch-56, with R-700 dispositions preserved as historical provenance.** CXA edge counts per `Cross_Axis_Composition_Document_v2_18.md` §2.3.x. These are mostly **mechanical wiring** (runtime composer landings + production caller sites), not cross-domain design tensions → **Council: no** for all.

| Seam | Live disposition | Current state | Close-out |
|---|---|---|---|
| **CXA-1** (AS→IS, 13-edge legacy prose narrowed by audit) | **RETIRED** (batch-56) | `RuntimeToolDispatcher` emits scoped `SecretFetchEvent` records from active `TOOL_STEP` dispatch with non-hollow metadata; `RuntimeAsIsWiring` writes the AS→IS ledger entry with the bound R-003 procedural-tier sidecar. The edge-scope audit narrows current direct AS→IS obligations to U-AS-19/U-AS-28 read-only IS carrier imports plus the U-AS-26/U-AS-27 secret-fetch producer family. | **Closed by batch-56.** Bootstrap-value secret fetch remains excluded under Reading-D; do not invent callback surfaces for read-only carrier imports. |
| **CXA-2** (CP→IS, 36–43 edges) | **RETIRED** (batch-57; was RETIRED-AS-BOUNDED-RESIDUAL batch-55) | Workflow-layer pause/resume, override, and workload-selection producers fire from production caller sites; PR #449 added the HITL/recovery producer primitives; PR #452 bound them at stage 5 and proved direct CP→IS emissions; PR #454 wired Anthropic provider-turn HITL continuation through `ctx.hitl_tool_loop`. **The bounded residual (dormant engine recovery loop) is DISCHARGED at R-FS-1 E-impl-2 (batch-57):** the hand-rolled WAL segment-log substrate (U-RT-121) + WAL_SEGMENT materialization (U-CP-94) + engine-layer recovery-loop firing branch (U-CP-95) + durable factory bind & by-execution go-live e2e (U-RT-122) make `RuntimeEngineRecoveryLoop` fire `cp.pause-captured`/`cp.resume-attempted` against a crash-survivable store from a real WAL_SEGMENT driver — the R-CXA-2 engine-layer seam is LIVE in production. | **Closed by batch-55; residual discharged at batch-57.** The re-open trigger ("a real … WAL-segment … recovery loop lands") fired and was built — NOT a fake `workflow_driver.py` engine loop, but the genuine durable U-RT-121 substrate fired by the U-CP-95 WAL_SEGMENT driver branch. **RECONCILER_LOOP recovery (the other re-open class) is now CLOSED at R-FS-1 E-impl-3 (5/5 engine classes):** E-impl-3a #572 (U-CP-96 resumption) + E-impl-3b #574 (U-RT-123 etcd-style CAS-lease substrate) + E-impl-3c #576 (U-CP-97 engine-layer firing + U-RT-124 engine-class-aware go-live) bring R-CXA-2 LIVE for RECONCILER_LOOP against a DISTINCT durable reconciler store (no cross-contamination by construction). Non-gating residual: F1-01 WAL completed-run-retry exactly-once duplicate-emit (`.harness/r-fs-1-e-impl-3c-f1-01-wal-exactly-once.md`; symmetric fix-shape ready). |
| **CXA-3** (CP→AS, 24 edges) | **RETIRED** (batch-54) | `RuntimeCpAsWiring` materializes the CP-consumed AS terminal seam export registry at bootstrap stage 6, fail-closes on AS manifest coverage drift, and is exposed as `HarnessContext.cp_as_wiring`. | **Closed by batch-54.** The operator rejected scope narrowing and directed the runtime-composer path; focused lifecycle/bootstrap coverage proves the composer shape and context exposure. |
| **CXA-4** (OD→IS/AS/CP, 26 edges) | **RETIRED** (batch-53) | **Grounding sweep 2026-06-01 (R-CXA-4 grounding-first) corrects the prior "~5 of 26 / ~21 remaining" framing — a stale-carry mis-framing (CLAUDE.md §10.5).** Per CXA §2.5 the 26 canonical OD-outbound edges split **1 genuine typed seam + 19 convention-level + 6 phase-2-runtime**. The lone genuine data-flow edge (U-OD-30→U-IS-11 `audit_writer.append`) is already wired with production producers; the 6 phase-2 runtime edges are already materialized at bootstrap stage 6; the 19 convention edges are namespace/manifest/monotonicity alignment satisfied by stage-6 checks and manifest resolution. | **Closed by batch-53.** There is no wiring task and no cleanup task: grounding found 0 wireable edges, and placeholders were already resolved at CXA v2.3 + OD plan v2.11. Batch-53 consumes the bookkeeping/accounting residual and moves CXA-4 from `PARTIAL` to `SUBSTANTIVE_RETIRED`. |
| **CXA-5** (OD→CP inversion, 1 edge) | **RETIRED** (batch-3) | Production `harness.breaker.*` emission landed (U-RT-58); inversion seam fires end-to-end. | — (closed). |

---

## Surface XI — Operator tooling (R-XI-NN)

### B-15 · Dashboard iteration-2 (R-XI-02 / R-XI-03) — **✅ CLOSED (stale-open correction, 2026-07-14)**
- **What it was.** **R-XI-02:** dependency-graph viz + sparklines; **R-XI-03:** live-update mode (webhook or short-poll). Both depend on R-XI-01 (the MVP, RESOLVED + LIVE at `thestoryportal.github.io/arhugula-harness/`).
- **✅ Both already RESOLVED — this entry was stale-open, found during a 2026-07-14 register accuracy audit.** `R-XI-02` RESOLVED 2026-06-02 (PR #235): `tools/dashboard/generate.py` has a working `build_depgraph()` (Mermaid flowchart + per-node click-to-inspect schema panel) plus PR-cadence sparklines + RETIRED-count trend, runtime-verified in headless Chrome. `R-XI-03` RESOLVED 2026-06-02 (PR #237): a client-side 45s short-poll (`POLL_MS`) detects a generation-time git-HEAD change via `<meta dashboard-live-head>` and soft-reloads; runtime-verified. Per `Project_Roadmap_v1.md`, Surface XI's `next_pointer` is `null` — **Surface XI is COMPLETE**, no open levers. This entry's own "Iteration-2 features PROPOSED" framing had gone stale for over a month without being corrected.
- **Close-out steps.** None remaining. **Council: no.**

---

## Surface X — Existential / research (R-900..R-999, decomposition-owed)

### B-16 · Open architectural / speculative arcs
- **What it is.** Genuinely speculative, not-yet-decomposed forward research: open architectural questions, research-corpus extensions (`research/agentic-engineeriing-sdlc.md` Phase-8→Phase-9 graduation criteria; cross-cutting-concern application per the canonical SDLC), and any arc not fitting Surfaces I–IX/XI.
- **Current state.** Named with a decomposition-owed marker; **no concrete items committed** — this surface is deliberately not padded.
- **Close-out steps.** Operator-seeded when a specific research question crystallizes (e.g., the NotebookLM 28-URL research corpus). *(The workflow-doc v1.14+ Phase-8→Phase-9 retirement-criteria evolution — previously cited here as an example — was itself crystallized and closed: `R-901-phase-9-retirement-criteria` is `status: RESOLVED` 2026-06-08 per `Project_Roadmap_v1.md`, research-only, no design-substrate back-flow owed. Removed as a stale example, found during the 2026-07-14 register accuracy audit; this does not affect B-16 as a bucket, which remains deliberately unpadded.)* **Council: conditional** — case-by-case, only if a named cross-domain tension surfaces.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT; disposition VERIFIED-DORMANT.** The bucket terms are unchanged at HEAD and nothing has matured into the bucket since the `R-901` worked example was struck at the 2026-07-14 accuracy audit. The agreed empty-bucket disposition still stands; wakes only on an operator-seeded research question.

### B-17 · ICM workspace-methodology adoption *(appended 2026-06-04 at HEAD `ca2bc34`)*
- **HOLD REAFFIRMED 2026-07-21 (operator AUQ)** — keep holding; no adoption driver. A fresh operator answer, not a parking label: do not re-surface until a concrete driver appears.
- **SUPERSEDED 2026-07-30 — RATIFIED, hold lifted (operator AUQ).** The concrete driver appeared and all 3 audit-§8 gates were answered: (1) **scope** — governance-layer-only; (2) **file-size caps** — ICM `<80`/`<200`-line caps for `CONTEXT.md` / router files ONLY, `design-substrate/` specs exempt as a documented divergence; (3) **L0-refactor appetite** — CONSERVATIVE (relocate historical change-note lineage out of root `CLAUDE.md`, keep all live governance inline). Build arc DELIVERED in the same PR as this transition: `R-ICM-1` (§2.3/§2.4 lineage relocated byte-exact to `.harness/claude-artifact-pointers.md`) + `R-ICM-2` (workspace `CONTEXT.md` task-router, wired into both startup surfaces and into `optimize-claude-md`'s L1 budget tooling) — the row closes here, not at a later patch. **Forward question preserved, not closed:** ICM integration into the final deployed harness package/repo is the better long-term question — it belongs to the deployment-surface arc, not to this repo's governance layer.
- **What it is.** A potential forward effort to adopt elements of the **Interpreted Context Methodology** (ICM — "folder structure as agent architecture"; `RinDig/Interpreted-Context-Methdology` + arXiv `2603.16021`) into the workspace **governance layer**. Foundation landed this arc: `ICM_Alignment_Audit_v1.md` (read-only 4-agent audit, committed `ca2bc34`) with a tiered reconciliation roadmap at §8 (`R-ICM-1..7` + non-goals `N-ICM-1/2`). Introduces NEW surface **XII Methodology** to this register.
- ~~**Current state.** Audit only — **no adoption decided**.~~ **Superseded 2026-07-30 — see the RATIFIED bullet above; the three gates are answered.** *(Corrected 2026-07-30, `B-77` arc: this bullet previously read "and the build arc is in flight" — that arc is no longer in flight, it MERGED at PR `#1161` (`R-ICM-1` + `R-ICM-2`), and the row is `closed`.)* Repo scored ~90% on ICM *values* (one-way refs, canonical sources, plain-text, single-job decomposition, edit-surfaces, stage-audits) / ~50% on ICM *structure* (token budgets, numbered-stage `CONTEXT.md` layering, file-size caps). Headline debt: root `CLAUDE.md` ≈ 85.7K tok (~107x ICM's ~800-tok L0 target; 2.4–4.0x ICM's own 30–50K monolithic anti-pattern). Two in-repo ICM beachheads already exist (`.harness/spec-code-overlay/` = full canonical ICM `stages/NN/CONTEXT.md`; `.harness/council/context-memory-grounding/` = numbered stages). ICM reference corpus vendored at **gitignored** `ai-docs/Interpreted-Context-Methdology/`. Memory: `icm-audit-and-adoption-foundation`.
- ~~**Close-out steps.** Operator decides 3 gates from audit §8 **before** any spec.~~ **Answered 2026-07-30 — see the RATIFIED bullet above.** Retained for provenance, the gates were: (1) **scope** — governance-layer-only (recommended) vs governance + selected workflows; (2) **file-size caps** — adopt ICM `<80`/`<200`-line caps where, exempt what (`design-substrate/` specs?); (3) **L0-refactor appetite** — how aggressive on `R-ICM-1` (split the ~85.7K-tok root `CLAUDE.md`), the highest-leverage but most-invasive change to a load-bearing governance doc. If pursued: seeds a formal **`R-IF-113`** arc; start from audit §8; reuse the `.harness/spec-code-overlay/` canonical-ICM template; frame as **reconciliation, not conversion** (`N-ICM-1/2`: the product H_T stays a coordination framework; ICM is sequential / anti-framework — adopt VALUES in the governance layer, not wholesale). **Council: ⚖️ conditional** — nameable two-voice tension (context-minimalism / token-budget [C2-style] ⊥ governance-completeness; the L0-split risk).

---

## Surface XIII — Upstream decomposition-audit build arcs (2026-07-09)

*Source: `.harness/audit/Upstream_Decomposition_Audit_2026-07-09.md` — council-engaged intent→spec audit (0 silent gaps; 12 documented deferrals). Under the operator FULL-SPEC directive ("no MVP scoping; all beyond-MVP spec'd + built") the deferrals are build targets. The 9 already-tracked ones live in their existing homes (Arc R / B4 / B-TAIL / B-FALLBACK / C-AS-10 §10.3 / B3 / F-B3-2 / FM-2 / per-persona crypto — several BUILT-but-gated on a 2nd provider). The 2 net-new UNREGISTERED findings are registered here.*

***CORRECTED 2026-07-27** — the "BUILT-but-gated on a 2nd provider" clause above is stale on both of its referents. **(a) Per-role B4 is BUILT and ungated:** per-role MODEL dispatch resolves at `retry_breaker_fallback.py:743-749` (`_effective_chain` branch 3, unconditional — no activation flag) and per-role PROMPT dispatch at `llm_dispatch.py:1268/1297-1298` off `resolve_per_role_system_prompts` (`prompt_selection.py:256`); `step_context.agent_role` is a real threaded dimension (`workflow_driver_types.py:299`). **(b) `R-300-multi-llm-second-provider` itself RESOLVED 2026-06-03** (PR #281 + #283; `Project_Roadmap_v1.md` §5.10: "No further R-300 second-provider gate remains") — so nothing can still be gated on it. **(c) Arc R (L2 EMBEDDING / L3 LLM_AS_ROUTER) IS still production-inert by default**, but the live gate is the `routing_activation` opt-in flag (`llm_dispatch.py:1020`), not a second provider — tracked at `B-L2-EMBEDDING-ACTIVATION` in `.harness/beyond-mvp-capability-boundary-ledger.md`. Same `[[stale-carry-text-disposition]]` defect class corrected at the audit's own §3b correction note and the ledger's `B4` row.*

### B-18 · U-1 — `cache_control` breakpoint emission *(NET-NEW, registered 2026-07-09)* — **✅ BUILT 2026-07-10 (stale-open correction, found 2026-07-14 at forward-register-schema grounding, out-of-family Codex)**
- **What it is.** The executable half of the **ADR-D3 §1.5 "prompt-cache breakpoint placement contract"** (per-cell breakpoint placement at parent-agent + per-sub-agent levels, composing D4 §1.8 concurrent-cache warm-up). The static-prefix/dynamic-suffix discipline + the observation attrs are built; the harness never AUTHORS a breakpoint — the runtime dispatch composer emits a **string-only** system prompt, so it cannot place `cache_control` markers.
- ~~**Current state.** COMMITTED at ADR-D3 §1.5; deferred only as runtime spec §14.5.2:3620-3621 OQ-1 (string-only) + OQ-2 (perf follow-on); **in no ledger/register** (confirmed by acknowledgment-grep). Load-bearing C2 cost lever (cache-miss = 4–10× input tokens per ADR-F2 §Rationale(b)(ii)).~~ **Superseded — see the ✅ BUILT bullet below; OQ-1 is un-deferred and the breakpoints are emitted.**
- ~~**Close-out steps.** Un-defer runtime spec OQ-1 (structured/multi-block `system`-array content) + emit `cache_control` breakpoints per ADR-D3 §1.5 placement contract at the dispatch composer; tests (cache-prefix stability, breakpoint placement, ≤4-breakpoint Anthropic limit); clearance marker. Bundled design+impl. Free acceptance witness: the observation half already exists (`llm_dispatch.py:331-358` reads `anthropic.cache_breakpoint_id`) → emit→observe round-trip.~~ **Done — see the ✅ BUILT bullet below.**
- **Probe findings (2026-07-09, advisor-guided scoping):** the §1.5 parent breakpoint sits at end-of-[system_prompt + frozen_tool_superset + template]. Two constraints gate a *sound, non-vacuous* build: (1) the stable prefix must be ≥4096 tok (Anthropic min cacheable prefix); (2) `tools[]` must be a static **frozen superset per epoch** (§1.5 line 203). **At MVP neither holds:** `active_system_prompt: str \| None = None` by default (`llm_dispatch.py:533` — no prefix to cache), and tools flow **per-step** (`payload.tools` from `step_payload.tools`, applied at `_payload_to_anthropic_kwargs:1670`), NOT frozen-per-epoch. So a naive breakpoint is **built-but-vacuous**. **Operator decision (2026-07-09): FROZEN-TOOLS PREREQUISITE FIRST** (build the §1.5-line-203 static-frozen-superset-per-epoch tool model → then the full §1.5 parent breakpoint is sound in one arc), rejecting the narrower system-prompt-only-conditional slice.
- **Refined disposition:** the frozen-tools prerequisite is a **substantial architectural change to the tool-dispatch model** (per-step tools → frozen-superset-per-(workload-class × prompt-version)-epoch), NOT a quick prerequisite — council-eligible (C4 tools ⊥ C2 caching ⊥ C6 routing/epoch). **Council: ⚖️ conditional** (frozen-tools-superset architecture + epoch model).
- **✅ BUILT 2026-07-10 — this entry's own "ACTIVE, scoped" framing above went stale the moment the arc it names was actually built, and was never corrected.** All 5 slices landed: PR #919 (frozen-tool-superset `cache_control` breakpoint, ADR-D3 §1.5), PR #920 (full §1.5 `[tools+system]` structured-content breakpoint, un-defers runtime spec OQ-1), PR #921 (child-scoped downgraded `frozen_tool_superset`, closes the F1 latent C10 condition-2 gap), PR #922 (workload-class-aware Anthropic prompt-cache TTL), PR #923 (`cache_ttl_seconds`/`breakpoint_id` tools/system-breakpoint observability). Runtime spec v1.93→v1.98 tracks these slices; see the 2026-07-10 change-note lineage relocated from root `CLAUDE.md` §2.3 to `.harness/claude-artifact-pointers.md` (B-17 / R-ICM-1, 2026-07-30). The related `B-18-3C-PREWARM` follow-on family (concurrent-cache warm-up prewarm/keepalive, PRs #927–#933) is a separate, already-complete arc tracked at `.harness/u1-3c-prewarm-design-decision-record.md` — not a residual of this entry.

### B-19 · U-2 — breaker `breaker.cause` + `breaker.cooldown_ms` attributes *(NET-NEW, registered 2026-07-09; operator-discretionary)* — **✅ BUILT 2026-07-13**
- **What it is.** The two CP-side ambient breaker-state attributes (`breaker.cause` trip-cause enum + `breaker.cooldown_ms`) dropped when the CP `breaker.*` set was replaced by the OD 7-attribute *event* schema.
- **✅ BUILT 2026-07-13 (R-FS-2 Wave 4, `B-19-BREAKER-AMBIENT-ATTRS`).** Operator ratified BUILD against the grounded skip-and-close recommendation (redundancy analysis found zero real consumers). Landed as `breaker.tripped` **event** attributes (not ambient state) at OD spec v1.32 (C-OD-07 §7.1, 7→9 attrs) + `harness-od`/`harness-cp`/`harness-runtime` code + tests, bundled-absorption. `cooldown_ms` is real (`cooldown_seconds * 1000`, populated at every trip). `cause` is a typed 4-value enum slot, **vacuous today by honest design** — no runtime call site can non-speculatively populate any of the 4 values (capability-shortfall pre-empts before the breaker is consulted; the fail-fast exception types don't map to `auth_failure`; the transient bucket is undifferentiated; the runtime's only real auth-vs-transient discriminator is a bootstrap-ping-only classifier). Full record at `.harness/b19-breaker-ambient-attrs-redundancy-analysis.md`; clearance marker `Spec_Operational_Discipline-v1_32-cleared-2026-07-13.md`.
- **Follow-on registered (NOT built).** A fine-grained provider-exception classifier (distinguishing `rate_limit` / `auth_failure` / `5xx_streak` at a real per-step dispatch failure, not just the current bootstrap-ping-only auth split) would make `harness.breaker.cause` non-vacuous. Building it now would be speculative infrastructure ahead of any consumer — open this only if a real ambient/cause-reading consumer is ever built. **Council: no** (mechanical extension of `_classify_provider_exception` once opened).

### B-20 · CP-side `harness.breaker.*` composition-mirror attribute-name drift *(NET-NEW, registered 2026-07-13 at Wave-5 hygiene grounding)* — **✅ BUILT 2026-07-14**
- **What it was.** `harness_cp.retry_fallback_namespace.HARNESS_BREAKER_NAMESPACE_SCHEMA`'s 7 pre-v1.32 entries (`harness.breaker.id` / `state` / `scope` / `trip_count` / `trip_window_seconds` / `fail_count_in_window` / `fail_threshold`) carried names + a concept-set that did NOT match OD's canonical §7.1 emission names (`scope` / `from_state` / `to_state` / `trigger_count` / `permanent_fail_repeats` / `tool_id` / `model_version`) — only `scope` overlapped directly; the rest were a different concept set entirely (a single `state` field vs OD's separate `from_state`/`to_state`; no OD equivalent for `id`/`trip_window_seconds`/`fail_count_in_window`/`fail_threshold`). Flagged Class-3 (not fixed) at B-19-BREAKER-AMBIENT-ATTRS grounding (2026-07-13) — this module's `verify_harness_breaker_namespace_inversion()` cardinality check only compares COUNT, not names, so the drift was invisible to CI.
- **✅ BUILT 2026-07-14.** Grounded via a background research agent (verified the OD canonical §7.1 schema + the CP module + the runtime cardinality-only check + confirmed the drift was self-contained to this module + its own test, no other file referencing the 7 old names). Renamed/split/dropped/added the 7 entries to byte-match OD's canonical 9-attribute set: `scope` kept (retyped `ENUM_REF`); `state` split into `from_state`/`to_state`; `trip_count` renamed `trigger_count`; `id`/`trip_window_seconds`/`fail_count_in_window`/`fail_threshold` dropped (no OD equivalent); `tool_id`/`permanent_fail_repeats`/`model_version` newly added (OD attributes CP had no entry for at all). The 2 already-correct v1.32 entries (`cause`/`cooldown_ms`) unchanged. Updated `test_retry_fallback_namespace.py`'s `_SPEC_HARNESS_BREAKER` expected set + the module's stale docstring/comment. 18/18 targeted tests + `harness-runtime/tests/test_lifecycle_od_cp_wiring.py` (14/14, cardinality-only check unaffected as expected) pass; `just check` 5788 passed/10 skip/33 deselected/1 xfail; `just overlay-check` 365/33; `just codex-review` converged clean, no findings. PR #994.

### R-1 confirmed-DEFERRED · Managed-cloud deployment-surface dispatch
- **Operator decision (2026-07-09):** the prior fork AS-8f **DEFER-INDEFINITELY (2026-05-28)** **STANDS** — the FULL-SPEC directive does NOT override this held decision. Managed-cloud dispatch (Persona §9/§10.2 + ADR-D2 §1.1 3-tier surface × blast-radius) remains deferred until a managed-cloud deployment. Recorded so the hold is honored, not silently reversed.

---

## Surface XV — 2026-07-12 deployment-preflight code-review forward work (registered 2026-07-13)

*Source: `.harness/harness-preflight-code-review-2026-07-12.md` (47-agent Workflow, 21 critical/high src findings) → PR #964 (merged, `40fe0c35`), whose own body names a "Forward work (not in this PR)" section. That section lived only in the PR body prose — `python3 tools/arc_ledger.py --check` and this register both showed zero matches for it, the same "0 forward ≠ no work, just nothing registered" gap the R-600 discipline exists to catch. These items are **standalone**, NOT parented under `R-FS-2` (that umbrella is G2-closed; the dashboard already severs this code-review arc from the R-600/R-FS lineage) — do not add these as `arc-ledger.yaml` rows under `R-FS-2` or `rfs1_status` will incorrectly flip back to `active`.*

### B-21 · PARALLELIZATION nested-child-pause capability gap *(preflight HIGH #6, `workflow_driver.py:7787`)* — **✅ BUILT 2026-07-13**
- **What it was.** `PeerFanOutResumeState` (PARALLELIZATION's resume carrier) had no `paused_child_branches`-equivalent field, unlike `FanOutResumeState` (ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION, v1.45) — a PARALLELIZATION peer branch whose own dispatch was a `SUB_AGENT_DISPATCH` step recursing into a child that itself PAUSED had nowhere to record it (the runtime `SUB_AGENT_DISPATCH` dispatcher is topology-agnostic, so the same `SubAgentChildPausedError` v1.45 already handles for ORCHESTRATOR_WORKERS was raised here too — but fell into the generic branch-failure handler, silently dropping the child's snapshot behind a bogus terminal `completed`).
- **✅ BUILT 2026-07-13.** Witnessed empirically before any fix (a failing test proved the drop). `PeerFanOutResumeState.paused_child_branches` (additive, reuses v1.45's `PausedChildBranchResumeState` carrier verbatim — no new type) mirrors v1.45's `_execute_orchestrator_workers` capture/resume shape peer-shaped in `_execute_parallelization`: `SubAgentChildPausedError` caught in both PROCEED and PAUSE/CASCADE_CANCEL paths (incl. the in-flight cancellation-race capture) + a resume material-diff guard (bounds/identity/no-overlap **plus a step-kind-changed check v1.45's own equivalent guard lacks** — closes a hole, no compat risk). Bundled-absorption: CP spec v1.96 → v1.97 + clearance `spec-control-plane-v1-97-cleared-2026-07-13.md`. Full disposition + the byte-compat-scoping correctness argument (out-of-family Codex round 2 [P1] — a naive uniform strip would have retroactively broken v1.45's already-shipped nested-snapshot hash compat; fixed via a per-carrier-kind-scoped `strip_paused_child_branches` parameter) recorded at `.harness/beyond-mvp-capability-boundary-ledger.md`'s `B-21` entry. `test_workflow_driver_parallelization_pause.py` grew from 42 to 46 tests. `just check` 5734 passed/10 skip/33 deselected/1 xfailed; `just overlay-check` 365/31; `just codex-review` 3 rounds (round 1: 2 real findings fixed; round 2: the byte-compat scoping fix + 1 registered follow-on; round 3, two independent invocations: one clean, one surfaced 2 more registered follow-ons — see `B-31`/`B-32` below, both verified symmetric with the pre-existing v1.45 mechanism by direct read, not merely asserted).

### B-22 · F5 real audit-signature backend seam *(preflight CRITICAL #1 follow-on)* — **✅ CLOSED 2026-07-14 (seam scope only — see `B-36` for the split-off concrete-backend follow-on)**
- **What it was.** `sign_audit_entry`/`verify_audit_entry_signature` were making a false security attestation (unkeyed SHA-256 treated as `VERIFIED` — anyone who can write a ledger entry could forge one). Operator chose **fail loud**: both now raise `AuditSigningBackendUnavailableError` instead of fabricating a result.
- **✅ CLOSED 2026-07-14.** Grounding first: `ADR-F5 §Deferred D-ADRs` + `ADR-D5 v1.3 §1.4`'s signing-key-residence table (row 3) confirm the concrete prod-tech backend (AWS Secrets Manager / HashiCorp Vault / Azure Key Vault / GCP Secret Manager / Doppler / 1Password Connect) is **already explicitly deferred to a future D-ADR** by the design substrate itself — it is not merely under-specified, and needs real cloud credentials + an operator/D-ADR decision, genuinely out of Phase-7-execution scope (X-AL-3). This module is MULTI_TENANT_COMPLIANCE-exclusive and the workspace's operative persona tier is `solo-developer` (bridging-arc default) — zero production callers, inactive tier. Surfaced to the operator as a genuine fork (build the seam now vs. hold B-22 fully) via `advisor()`-corrected AskUserQuestion; **operator chose build-the-seam-now**. Landed: `Spec_Control_Plane_v1_98.md` (NEW §20.2.1 additive `SigningBackend` Protocol + optional `backend` keyword param on both functions; absent → identical fail-loud raise preserved byte-for-byte for the zero existing callers; present → real SHA-256-then-sign / recompute-then-verify per §20.3.1) + clearance `spec-control-plane-v1-98-cleared-2026-07-14.md` + `harness-cp` impl + a TEST-ONLY in-memory Ed25519 `SigningBackend` double proving the round-trip (no cloud creds, no backend-selection claim) — mutation-probed (forcing unconditional `VERIFIED` was caught by 2 of 10 tests). **`just codex-review` (4 rounds) caught 8 real findings, all fixed**: round 1 — [P1] a valid signature could be relabeled onto different `audit_signature_key_id`/`algorithm`/`key_period` metadata and still verify (the signed message covered only the content hash, not its own metadata) — fixed by binding all four values into a length-prefixed canonical message (mirrors the B-23 segment-injectivity fix); [P2] `sign_audit_entry` accepted a backend declaring an algorithm outside the §20.2 closed 3-value enum — fixed with an explicit membership check; [P2] `sign_audit_entry` accepted a negative `key_period` — fixed with an explicit non-negative check. Round 2 — [P1] the verifier still returned `VERIFIED` when the caller's `key.key_id` or the backend's declared `algorithm` disagreed with the entry's own stored metadata (a backend ignoring its `key_id` selector could attest the wrong key) — fixed with explicit equality checks before ever consulting the backend; [P2] the round-1 `key_period` check ran before the backend-presence check, changing the absent-backend exception type for negative periods — a real regression against this delta's own "byte-for-byte preserved" claim — fixed by reordering. Round 3 — [P2] `key_period=True`/`1.0` passes Python's `int` type hint by subtyping/duck-typing but signs a different textual message than what Pydantic coerces the field to, self-invalidating the entry — fixed with an exact `type(key_period) is not int` check; [P2] the read path never validated a `signed` entry's OWN stored algorithm/period against §20.2 before consulting the backend, so an externally-constructed entry with out-of-contract metadata could verify if a rogue backend agreed with the same invalid value — fixed by repeating the write-path checks at read time. Round 4 — finding-count trend (3→2→2→4) drifted from seam-contract into concrete-signature-representation territory, the `advisor()`-confirmed convergence signal; [P1] `SigningBackend.sign`/`verify` took only `key_id`, making a rotation-aware backend structurally unexpressible (a single stable `key_id` cannot distinguish keys across a rotation boundary per §20.3.1) — fixed by widening both methods to also take `key_period`, proven with a `_RotationAwareBackend` test double keying off period; 1 finding empirically probed and confirmed a no-op (Pydantic `model_validate` coercion of `key_period=True`/`1.0` produces a canonically-identical valid record, not a distinct forgery); 2 remaining findings (ECDSA-P256 DER-vs-64-byte signature encoding; `CPSignedAuditLedgerEntry.audit_signature_value` not JSON-serializable for real signature bytes — pre-existing on the shared type) are concrete-backend-representation concerns per §20.4's own "deferred to implementation discretion" framing — split off to `B-34`, not fixed inline (no backend exists yet to hold to a wire format). All 8 landed fixes mutation-probed individually. `test_f5_signing_key_resolution.py` grew 6 → 23 tests. `just check` CP suite 1576 passed/1 skip/1 xfail; `just overlay-check` 365/33 (no new cross-axis seam — CP-internal only); pyright + ruff clean. PR #976.
- **Why CLOSED, not held open.** This entry's scope was always the **seam** (the composition-root injection point + fail-loud contract), which is now fully built, reviewed to convergence, and merged. The remaining work — picking and provisioning a real prod-tech signing backend — is a *categorically different* kind of task (D-ADR + real cloud credentials, genuinely not Claude-closeable) that was always going to outlive this entry regardless of how it's labeled. Per operator direction 2026-07-14, that follow-on is split into its own entry, `B-36`, so this entry can accurately reflect "the seam is done" without either (a) mislabeling undone credential-gated work as closed, or (b) leaving the done seam work perpetually flagged as incomplete. See `B-36` for the concrete-backend tracking.

### B-23 · Cost-attribution composers fabricate their audit entry's IS-anchor *(preflight §4 "most urgent", `cp_audit_conversion.py:161`)* — **✅ BUILT 2026-07-13**
- **What it was.** `harness_cxa.cp_audit_conversion._entry_core_or_default` synthesized an opaque `cp-audit:<action_id>` marker whenever its caller omitted `entry_core`. All **4** production cost-attribution composers — `attribute_tool_dispatch_cost`, `attribute_llm_dispatch_cost`, `attribute_webhook_dispatch_cost`, `attribute_validator_dispatch_cost` — called `cp_audit_to_od_audit(...)` with no `entry_core`, unlike `sub_agent_dispatch.py`'s 8a/8b/8c pattern.
- **✅ BUILT 2026-07-13.** A shared `compose_cost_f2_entry_core` helper (`harness_runtime.lifecycle.cost_attribution_f2_write`) F2-writes the dispatch fact and returns its `StateLedgerEntryRef` as the real `entry_core`; `ledger_writer` + the R-003 `procedural_tier_snapshot_resolver` threaded into all 4 composer signatures + their 4 bootstrap construction sites (`runtime_tool_dispatcher_factory.py`, `stage_5_loop_init.py`'s `materialize_llm_dispatcher_stage` call, `webhook_delivery_composer_factory.py`, `stage_4_od.py`'s `materialize_validator_framework_stage` call) — each is the SOLE production construction site for its composer (verified by grep), so the `ledger_writer=None` fallback (preserved for unit-test ergonomics, matching the existing `cost_record_sink`/`rate_table` optional-substrate convention) is unreachable in production. Retry/REVALIDATE collisions guarded by a per-attempt disambiguator: a real per-attempt OTel `span_id` for tool/LLM/webhook (fresh per retry); for validator, a composed `(burden_count, outcome, branch_index)` string — `burden_count` alone collides on a REVALIDATE→PASS pair (PASS doesn't increment it per CP spec §25.4 invariant 5), caught by out-of-family Codex review round 1; `branch_index` guards the sibling-fan-out-branches-share-one-`parent_action_id` collision class (`sub_agent_dispatch.py`'s `child_index` precedent), added proactively per Codex's same finding. Codex round 2 flagged the webhook factory's ledger wiring landing only on the dormant (no-cost-substrate) construction path — closed by adding an integration test (`test_u_od_40_validator_webhook_integration.py`) on the pre-existing real cost-substrate-bound composition proving both surfaces produce a real `entry_core`; Codex's further "hard-require `ledger_writer`" suggestion was declined (would contradict the deliberately-tested None-fallback and force every existing cost-substrate-bound unit test to build a real `LedgerWriter` for zero production gain — production correctness is already structural: sole construction site + both params always threaded together). Codex round 3 flagged 3 findings against the shared `LedgerWriter` being cross-tenant/cross-run: **fixed both real ones** — (a) `tenant_id` folded into the F2 identity (mirroring `RuntimeAuditLedgerWriter._tenant_tag`'s convention), since the validator composer's disambiguator is deterministic (`burden_count`-derived) and two tenants could plausibly share `(workflow_id, parent_action_id, dispatch_disambiguator)`; (b) `parent_idempotency_key` folded into the F2 identity for run-scoping — `WorkflowManifestEntry.workflow_id` is operator-authored and STABLE across every invocation of "that workflow" (CP's own `run_idempotency_key = sha256(run_id, workflow_id, entry_version)` composition treats `run_id` and `workflow_id` as distinct components), so re-running the same workflow definition with an identical deterministic validator outcome would otherwise collapse cross-run via `IDEMPOTENT_NOOP`; `parent_idempotency_key` is already run-scoped (CP composes step-level keys from the run's own idempotency key) and already a parameter on all 4 composers. **Declined 1 as pre-existing/dormant, not novel to this fix** (registered separately at `B-29`, both parts still applicable): shared-clock threading with `RuntimeAuditLedgerWriter.time_source` — no production site injects a non-default clock today (confirmed by grep) and no existing test combines a custom `time_source` with any cost-attribution composer, so the flagged `NonMonotonicTimestampError` failure mode has no live trigger; the `branch_metadata` sidecar finding from round 3's first pass is likewise pre-existing (the `sub_agent_dispatch.py` precedent this fix mirrors ALSO omits it). Codex round 4 found 2 more real bugs against the round-3 tenant fix itself: (a) the tool-dispatch call site (`_attribute_tool_cost_best_effort`) hardcoded `tenant_id=None` (pre-existing since before this arc) instead of the in-scope `step_context.tenant_id` — silently defeating BOTH the new F2 tenant fix AND the pre-existing `audit_writer.append(tenant_id, ...)` OD tenant scoping for tool dispatch specifically (LLM/webhook/validator were already correct); **fixed** by passing the real value. (b) the `tenant_id or "_single"` sentinel encoding aliased `tenant_id=None` with a genuine `tenant_id="_single"` (`RuntimeConfig` doesn't forbid that literal tenant name); **fixed** with a length-prefixed injective encoding (`f"tenant:{len}:{value}"` vs a distinct `"no-tenant"` sentinel that can't collide with any wrapped value). Codex round 5 found 2 more real bugs: (a) `attribute_webhook_dispatch_cost`'s `span_id` (unlike tool/LLM) is a *synthesized* `f"webhook-deliver-{idempotency_key}"` string, not a real per-call OTel span — Codex's own probe showed 2 `deliver_webhook()` calls sharing one `idempotency_key` (a legitimate caller-side outer-retry pattern; the composer fires its cost wrapper unconditionally on every call, no idempotent skip) produced 2 audit entries but only 1 F2 entry; **fixed** with a new `dispatch_disambiguator` parameter (falls back to `span_id` for existing callers), wired at `WebhookDeliveryComposer` with a fresh `uuid.uuid4()` per invocation. (b) the length-prefixed tenant encoding still used truthiness (`if tenant_id else`), aliasing the legal-but-falsy `tenant_id=""` with `tenant_id=None`; **fixed** with `is not None`. Full-chain-witness tests (F2 entry lands in the real IS ledger AND the audit entry's `entry_core` references it, not separate seam tests) added to all 4 composer test files + the validator+webhook integration test + cross-tenant + cross-run + tool-dispatcher-real-tenant-propagation + sentinel-aliasing + webhook-repeat-idempotency-key + empty-tenant regression tests. **advisor() (round 6, after round-5 fix committed) diagnosed rounds 1-5 as one recurring defect class, not five unrelated bugs**: every round closed ONE aliasing case on the raw colon-joined F2 identity (`f"cost:{tenant_tag}:{workflow_id}:{parent_action_id}:{parent_idempotency_key}:{disambiguator}"`) without fixing the join itself — `parent_action_id` is production-shaped as `f"workflow:{workflow_id}:step:{n}"` (contains `:`), so e.g. `(workflow_id="a", parent_action_id="b:c")` and `(workflow_id="a:b", parent_action_id="c")` concatenate identically; the round-4/5 tenant fix only made the tenant segment injective, leaving the same class open on every other segment. **Fixed at the root**: `compose_cost_f2_entry_core` now length-prefixes every segment (`_length_prefixed`, `f"{len(s)}:{s}"`) including a dedicated tenant presence flag (`"1"`/`"0"`) ahead of the tenant value, replacing the bespoke `tenant_tag`/`_NO_TENANT_TAG` sentinel — each segment is self-delimiting, so the full join has exactly one parse and two distinct segment tuples always produce distinct strings (closes the whole class in one place, not per-case). New dedicated `tests/test_lifecycle_cost_attribution_f2_write.py` (4 tests) exercises the helper directly: colon-shifted `(workflow_id, parent_action_id)`, colon-shifted `(parent_idempotency_key, dispatch_disambiguator)`, `tenant_id=None` vs `tenant_id=""`, and a colon-containing tenant value shifting across the `tenant:workflow` boundary — all previously-passing composer/integration tests still pass unchanged (the encoding change is internal to the opaque `action_id` string; no caller inspects its structure). `just check` 5727 passed/10 skip/1 xfail (+4 vs round 5); `just overlay-check` 365/31.

advisor() also flagged a second-order question genuinely out of B-23's scope: the 4 composers now use 3 *different* disambiguator replay strategies (validator's is deterministic — dedupes a re-fired cost event on resume; tool/LLM/webhook's are per-attempt-random — always create a fresh F2 entry on resume) with no decided policy on whether a step re-firing on resume should be the SAME billable event or a NEW one. Registered as `B-30` rather than answered inline — it's a CP replay/fencing-semantics design decision, not a B-23 mechanical fix.

### B-24 · `secret_negative_observation.py:127` wrong `NegativeObservationSurface` label — needs a new enum member *(design-substrate)* — **✅ BUILT 2026-07-15**
- **What it was.** `verify_sole_resolution_path` hardcoded the wrong `NegativeObservationSurface` label; no existing enum value mapped to "workflow manifest" arrival. Zero production callers (latent).
- **Ratification.** Fork doc (`class_1_fork_secret_negative_observation_missing_enum_member.md`) grounded 2026-07-15. Filer's recommended readings (Q1=A `WORKFLOW_MANIFEST_ENTRY`, Q2=a bundled dispatch fix, Q3=a fork-doc-as-audit-trail) ratified as-is — mechanical, reversible, zero production callers, convention-following (member name mirrors the CP `WorkflowManifestEntry` carrier per the existing per-member naming pattern).
- **Process correction (out-of-family `just codex-review` round 2, same PR).** The first close-out pass claimed "no design-substrate edit owed," reasoning `Spec_Action_Surface_v1.md` never declares this enum — true, but incomplete: `Implementation_Plan_Action_Surface_v1.md` §1073-1078 (the PLAN, a distinct design-substrate artifact) DOES declare `NegativeObservationSurface`'s canonical 4-member signature at U-AS-21, preserved verbatim through v1.2/v1.3/v1.4. Extending it to 5 members without a plan delta was a silent design-extension under X-AL-3 even though the spec itself was silent. Corrected same-PR: `Implementation_Plan_Action_Surface_v1_5.md` + clearance marker `.harness/clearance/implementation-plan-action-surface-v1-5-cleared-2026-07-15.md`.
- **✅ BUILT 2026-07-15.** Added `NegativeObservationSurface.WORKFLOW_MANIFEST_ENTRY`; replaced the hardcoded `surface=STATIC_PROMPT_CACHE_PREFIX` with a real `_ARRIVAL_SITE_SURFACES` dispatch dict, defaulting unrecognized sites to `WORKFLOW_MANIFEST_ENTRY` (the one arrival path with no dedicated detector of its own). **Out-of-family `just codex-review` round 1** caught a real gap: the initial dict only keyed the longer-form aliases (`prompt_cache_prefix`, `log_records`, ...), not §5.3's own short-form vocabulary the docstring itself advertises ("manifest, prompt, log, ledger") — calling with `"prompt"` silently fell through to the `WORKFLOW_MANIFEST_ENTRY` default instead of `STATIC_PROMPT_CACHE_PREFIX`. Fixed by adding the 4 short-form keys alongside the existing long-form ones. Test updated to assert `.surface` (previously untested) + coverage for both the long-form and short-form dispatch branches. 9/9 tests pass. **Council: no.**

### B-25 · `sandbox_tier_floor.py:54` — `is_deterministic_inhouse=False` branch has no defined floor *(design-substrate)* — **✅ RESOLVED 2026-07-15 (Reading A via council + operator decision)**
- **What it was.** `is_deterministic_inhouse` is threaded through the types but never read; both the AS spec's §2.3 row 7 and its own upstream authority, `ADR-D2 v1.2` §1.5.1, self-contradicted — the `where:`-block's row-7 condition tuple named the field as a match input, while the adjacent "Row→argument keying" paragraph claimed row 7 keys purely on `blast_radius_tier` with no carve-out. Full grounding history (the ADR-level self-contradiction discovery) preserved at the fork doc's §1.5 addendum.
- **Resolution process, 2026-07-15 follow-on session.** Per the fork's own §2 Q3 recommendation, a dyadic council convening (C10 action-safety/blast-radius + C4 tool-contract-semantics) ran before any operator decision. Three empirical probes preceded deliberation; the decisive one — `is_deterministic_inhouse` carries **zero verification mechanism today** (a plain self-declared boolean; no code-signing, static check, or attestation) — meant a floor bump keyed on `False` would be trivially spoofable by exactly the actor it targets (a careless/dishonest author just sets the flag `True`), while its only real cost falls on honest authors who simply never opted into a newer field. Both voices converged on **Reading A** (do not branch on the field; state its non-gating status explicitly), a reversal of the filer's original Reading-B lean under the new evidence. The council synthesized 4 options for the operator; **the operator selected Option 1 — "Reading A + reserved annotation"** via `AskUserQuestion`.
- **✅ RESOLVED 2026-07-15.** `ADR-D2.md` v1.2 → v1.3 (`where:`-block row 7 corrected to the 2-element `(read-only, *)` form matching rows 8-10; explicit reserved-non-gating sentence added) + `Spec_Action_Surface_v1.md` v1.13 → v1.14 (same row-7 correction + keying-paragraph note + stale `ToolContract`/`ToolMetadata`-discriminators docstring prose corrected) — bundled-absorption arc, both carrying clearance markers (`.harness/clearance/adr-d2-v1-3-cleared-2026-07-15.md`, `.harness/clearance/spec-action-surface-v1-14-cleared-2026-07-15.md`). Stale docstrings also fixed at `harness-as/src/harness_as/tool_contract.py` (`ToolContract.is_deterministic_inhouse`), `harness-runtime/src/harness_runtime/types.py` (`MCPClientConfig.default_is_deterministic_inhouse`), and `harness-as/src/harness_as/sandbox_tier_floor.py` (`ToolMetadata` class docstring). **No code behavior change** — production already implemented Reading A; this closes the documentation self-contradiction and stale-docstring drift. A new mutation-probed witness test (`test_sandbox_tier_floor_read_only_ignores_is_deterministic_inhouse`) pins `True`/`False` producing identical tiers for a read-only tool, documenting this as intentional. 18/18 `test_sandbox_tier_floor.py` tests pass; 341/341 `harness-as` suite passes. **Council: ✅ convened — see above.**
- **Process correction (out-of-family `just codex-review` round 1, same PR).** The initial ADR/spec fix left the canonical *plan* (`Implementation_Plan_Action_Surface_v1_2.md` §5.2 U-AS-06, preserved verbatim through v1.5) still describing `is_deterministic_inhouse` as a "§2.3 row-1/2/7 discriminator" at its Inputs line, Signatures comment, and AC #7 — the same category of gap caught one session earlier at the B-24 arc. Corrected same-PR: `Implementation_Plan_Action_Surface_v1_6.md` (prose-only, zero code/test/AC-count change) + clearance marker `.harness/clearance/implementation-plan-action-surface-v1-6-cleared-2026-07-15.md`.
- **Downstream note.** A separate exploratory memo (`.harness/architect_recommendation_tool_determinism_attestation.md`, reviewed by `advisor()` + out-of-family Codex) explored whether `is_deterministic_inhouse` should ever become a *verified* rather than self-declared claim — that memo's own analysis was explicitly conditional on this B-25 resolution, and its Candidate 0 (deprecate the field entirely) is the road not taken here: the operator chose to keep the field reserved-but-non-gating (Option 1) rather than remove it (Option 2). No further action owed unless the operator later wants to pursue a verification mechanism per that memo's Candidates A/C.

### B-26 · `five_axis_composition.py:320` — `verify_rotation_6_steps` hardcoded-`True` stub — **✅ BUILT 2026-07-13**
- **What it was.** Real verification needs IS hash-chain re-wiring; the function always returned `True`. Dormant — zero production callers.
- **✅ BUILT 2026-07-13.** `verify_rotation_6_steps` / `execute_key_rotation` gain an optional `audit_ledger_entries: Sequence[StateLedgerEntry]` param; `VERIFY_HASH_CHAIN_LINK` now calls the real `harness_is.chain_verification.verify_chain` against it instead of hardcoding `succeeded=True`. Registered 2 NEW genuine CP→IS Pattern-P1 seams (`U-CP-45→U-IS-07` `StateLedgerEntry`, `U-CP-45→U-IS-10` `verify_chain`) code-resident at `PATTERN_P1_SEAMS` + `tools/semantic_overlay/` (not a delta-only CXA markdown edit — confirmed via `advisor()` + direct read that the overlay tool explicitly reads this test file, not the markdown; 31→33 genuine typed seams). 5 rounds of out-of-family Codex review, converging: round 1 caught (a) no rotation-boundary witness and (b) downstream steps (`ROTATE_SIGNING_TO_NEW`/`RETIRE_OLD_KEY`) independently claiming success after a failed hash-chain gate — (b) fixed (protocol-sequential: a failed step now blocks every step after it), (a) registered forward (see `B-33`, needs new design surface). Round 2 caught the CP→IS seam registration gap (fixed above) + a genesis-slice contract ambiguity (fixed via docstring contract note: caller must supply the genesis-anchored chain, not a filtered/tenant-scoped slice — `verify_chain`'s own contract, no code change needed, zero callers today). Round 3 caught the residual fake-success path: the no-args default (`audit_ledger_entries=()`) still reported `rotation_complete=True` from zero evidence — fixed (empty ledger now fails the hash-chain step: "zero entries is an absence of evidence, not proof of chain integrity"), mutation-probed. Round 4 caught a TOCTOU between the `list(audit_ledger_entries)` snapshot verified and a separate re-read for the emptiness check — fixed (single snapshot reused for both). Round 5 restated round-1(a) (rotation-boundary proof) with a concrete example — confirmed via `advisor()` as the SAME residual, not a new finding; registered as `B-33` rather than chased further (needs a rotation-correlation carrier on `StateLedgerEntry`, a separate IS-spec change, PLUS the `B-22` signing backend — neither exists; over-investing further on a dormant zero-caller function past the point where rounds stop producing new finding classes is the anti-pattern to avoid). New tests: `test_hash_chain_step_valid_multi_entry_ledger`, `test_hash_chain_step_broken_chain_fails_rotation` (mutation-probed), `test_empty_ledger_does_not_fake_success` (mutation-probed). `just check` 5739 passed/10 skip/33 deselect/1 xfail; `just overlay-check` 365/33; pyright 0/0/0. **Does NOT claim rotation-boundary verification is complete** — generic hash-chain continuity + fail-closed on empty/broken is what's real; see `B-33` for the residual.

### B-27 · `cli/app.py:347` — `paused` (HITL) workflow status shares `failed`'s exit code *(design-substrate)* — **✅ BUILT 2026-07-15**
- **What it was.** The runtime spec's §14.18.2 exit-code table never assigned `paused` a disposition (the `partial` status got an explicit invariant note when it was added; `paused` didn't), so the CLI mapped it to the same exit code as `failed`.
- **Ratification.** Fork doc (`class_1_fork_cli_exit_code_paused_status_undefined.md`) ratified 2026-07-15 per filer recommendation: Q1=A (new dedicated exit code `5`), Q2=ii (spec + CLI kept in sync, mirroring how `PARTIAL`'s addition was handled), Q3=b (documentation note only — no `resume` subcommand built). Reversible additive change (new exit code, no existing code touched); `RunResult.status`'s `Literal` enum unchanged.
- **✅ BUILT 2026-07-15.** Runtime spec `v1.99 → v1.100` — NEW §14.18.2 row (`5` / `PAUSED` / `RunResult.status == PAUSED`). `cli/app.py` gains `EXIT_PAUSED = 5`; both the daemon-mode `_CP_STATUS_TO_EXIT_CODE` dict and the one-shot `if/elif` chain now dispatch `paused` to it explicitly instead of falling through to the generic-failure branch. **Out-of-family `just codex-review` round 1** caught a real gap: the initial fix named the constant `EXIT_PAUSED_RESUMABLE` / row `PAUSED_RESUMABLE`, implying every `paused` run carries a `PauseSnapshot` resumable via `resume()` — but `workflow_driver.py:4224-4248`/`:4259-4282` (`WAL_SEGMENT`/`RECONCILER_LOOP` engine-native recovery-loop pauses) intentionally return `RunStatus.PAUSED` with `pause_snapshot=None` (durable state lives in the engine's own segment log / reconciler store, not the workflow-layer snapshot), so `api.resume(snapshot)` cannot resume them — a misleading name for automation that would attempt an impossible resume call. Fixed by renaming to the non-overclaiming `EXIT_PAUSED`/`PAUSED`. **Round 3 caught a follow-on, more serious problem (P1):** the round-1 fix's own replacement text asserted engine-native pauses ARE resumable via "a plain re-invocation of `harness run`" — false, and potentially unsafe. `mcp_server.py:357` generates a FRESH `run_id` (`uuid.uuid4().hex`) on every invocation lacking a snapshot, and the engine recovery loop's `capture_pause` records key on `(workflow_id, run_id)` (`workflow_driver.py:4234,4270`) — a fresh run_id cannot locate the prior pause record, so re-invocation silently starts an unrelated new run, risking re-execution of already-committed side effects rather than resuming anything. §14.18.2's row 5 is corrected to state plainly that engine-native (`WAL_SEGMENT`/`RECONCILER_LOOP`) pauses have NO safe resume path at v1.100 — a future `resume` subcommand must solve run_id recovery for the snapshotless case first; plain re-invocation MUST NOT be documented or implemented as a resume mechanism until then. New tests: `test_b27_workflow_paused_status_exits_five` (one-shot) + `("paused", EXIT_PAUSED)` parametrize case (daemon-mode mapping test) — unaffected by the prose correction (exit code behavior itself was always correct; only the spec's resumability claim was wrong). 28/28 tests pass; ruff clean. Clearance marker: `.harness/clearance/spec-harness-runtime-v1-100-cleared-2026-07-15.md`. **Council: no.**

### B-28 · Test-quality findings untriaged *(preflight report §6 — 19 findings, 3 high / 10 medium / 6 low)* — **✅ TRIAGED 2026-07-14**
- **What it is.** The 2026-07-12 preflight review's §6 (test-quality findings) was explicitly out of scope for PR #964 ("Test-quality findings (report §6) remain out of scope for this PR" / "Not yet triaged" in the PR's own forward-work list). Unlike the src findings (all 42 triaged across criticals/highs/mediums/lows), none of these 19 have been read against the actual test code yet.
- **✅ TRIAGED 2026-07-14 — all 19 read against the raw workflow output (`.harness/harness-preflight-code-review-2026-07-12-raw.json`) and the actual test file (not the one-line summary).** Disposition: **17 fixed** (real gaps, closed with a genuine negative-path/completeness test, each mutation-probed — guard/assertion reverted in isolation, confirmed the new test fails, then restored), **1 stale** (already independently fixed by another arc between 2026-07-12 and now), **1 discovered pre-existing + registered forward** (not one of the original 19 — a live gap surfaced while triaging #17, registered as `B-35` rather than silently fixed or dropped).
  - **IS (5 fixed):** #2 `MemoryOperationProjectionMismatchError`/`RedactionEventMismatchError` (`test_memory_operation_ledger.py`, +5 tests); #3 `MemoryRetrievalResult._hashes_match_packet` + retrieval canonicalization TypeErrors + retrieve-before-rebuild (`test_memory_retrieval.py`, +5 tests); #4 `DerivedRetrievalIndexMissingError` (`test_memory_retrieval_index.py`, +1 test); #5 `MemoryStoreContentHashMismatchError`/`UnsupportedKindError`/`RecordNotFoundError` (`test_memory_store.py`, +3 tests); #6 `cli_profile.py` partial-provider-binding branch (`test_cli_profile.py`, +1 test).
  - **CP/CXA (2 fixed):** #1 tautological `SignatureAlgorithm.ED25519 in {SignatureAlgorithm.ED25519,}` self-check, now asserts the real value across all **7** producer-specific branches (out-of-family Codex round 1 caught the initial fix's list omitting the distinct `CostRecordAuditPayload` branch — added; `harness-cxa/tests/test_u_cp_72_converter_6_prefix_extension.py`); #7 `WorkloadBindingError` never actually triggered (only `issubclass` tautology) — now genuinely raised via 2 independent tests: a monkeypatched pure-pattern-only candidate set (tier-admissibility guard) **and** an empty `ENGINE_CLASS_CANDIDATES` (Codex round 1 caught that `_candidate_set_for`'s separate "no §7.2 candidate set" raise site was still unreached by the first test alone) (`test_workload_binding_engine_class_selection.py`, +2 tests).
  - **OD (1 stale + 4 fixed):** #8 `MultiTenantOverrideRefusedError` — **STALE**, already exercised by `test_multi_tenant_compliance_refuses_empty_override`/`_non_empty_insufficient_override` (landed by another arc after the 2026-07-12 preflight); #9-#12 the 5 vacuous `hash(x) == hash(x)` self-comparisons across `test_deferral_envelope.py` (×2), `test_bridging_arc_table.py`, `test_cost_attribution_dashboard_binding.py`, `test_redaction_gradient.py` — all now compare against a separately-constructed equal instance.
  - **Runtime (3 fixed + 1 structure-only):** #13 the "silently skips" claim asserted nothing beyond "no exception" — now asserts `COST_ATTRIBUTED_DECIMAL_ATTR` is genuinely absent from the span (`test_lifecycle_cost_attribution_llm_dispatch.py`); #14 **structure-only, NOT a functional fix (Codex-corrected disposition)** — the 2 unconditional `pytest.skip()`s preceded by unreachable dead-code PATH checks were collapsed to honest `@pytest.mark.skip`, but Codex correctly noted this only makes the inertness explicit — the underlying route/auth regression these tests were meant to catch remains just as undetectable as before (no non-secret Antigravity/legacy-Gemini auth-status probe exists to build one; a real probe is out of this triage's scope, matching the standing condition already documented at root `CLAUDE.md` §2.2); #15 zero validation-branch coverage on `tools/external_cli_provider_config.py` — +7 tests covering all 5 `ValueError` branches + the CLI's `except ValueError` exit-2 path; #16 the 4 stage-5 precondition guards in `materialize_r_cxa_2_producer_loop_stage` never raised — +4 tests, one per guard (`test_r_cxa_2_producer_loop_factory.py`).
  - **Tools (2 fixed + 1 new discovery):** #17 `violations()` only ever called against the live clean matrix — +1 test with a synthetic dirty matrix; #18 vacuous filesystem round-trip — replaced with a real per-row completeness assertion (`test_qa_evidence_matrix.py`); #19 `test_closure_gate.py`'s `_green_delegates` mocked 4 delegates but no test asserted the 5 automatable Tier-1 or 6 Tier-2 predicates — +1 test asserting all 11. **Discovered while triaging #17:** `test_q3_evidence_matrix_is_complete` fails against live data (12 contracts — `C-IS-14` + 11 `C-MEM-*` — cite no test file anywhere in the repo) and neither this file nor `test_closure_gate.py` is wired into any CI job — pre-existing, confirmed via `git diff` against `origin/main` showing zero prior changes to that test's body. Not part of the original 19; the test was rewritten to pin the exact known gap count (not xfail — see `B-35`'s own entry for why a blanket xfail was rejected on round-2 out-of-family review) and registered separately as **`B-35`** rather than silently fixed or left as an unexplained red test.
  - **IS (2 additional fixes, out-of-family Codex rounds 1 + 3):** #2's initial fix only constructed `MemoryOperationPayload`; `MemoryOperationEntry` carries its own copy of `_projection_matches_kind` (`_deserialize_entry` constructs it directly, bypassing `Payload` entirely) — round 1 added `test_projection_mismatch_rejected_on_entry`; round 3 found the SAME gap on the 4 redaction-validation tests (`_validate_redaction_event` is also called from `MemoryOperationEntry`'s own validator, independently of `Payload`'s) — added 4 entry-side mirrors (`test_memory_operation_ledger.py`, +5 tests total across both rounds).
- **Verification.** All fixes mutation-probed individually (including the round-3 entry-side redaction guards: temporarily removed the `_validate_redaction_event` call from `MemoryOperationEntry._projection_matches_kind`, confirmed exactly the 4 new entry-side tests failed while the payload-side + projection-mismatch tests stayed green, then restored). `just codex-review` (3 rounds) caught 8 real problems: round 1 — cost-carrier branch omitted from #1; `MemoryOperationEntry`'s own validator copy unexercised for #2; `_candidate_set_for`'s second raise site unreached for #7; #14's disposition overclaimed as "fixed" when it was structure-only. Round 2 — the `B-35` fix's own `xfail` reason string spelled out a literal contract id, causing the evidence-matrix scanner to falsely count this test file as that contract's proof (empirically confirmed: gap count silently dropped 12→11); a blanket `xfail(strict=True)` on the whole assertion would have masked a genuinely new gap behind the same expected-failure outcome — replaced with a count-pinning assertion that never spells out any of the 12 ids. Round 3 — the 4 redaction-validation tests still only exercised `MemoryOperationPayload`, not `MemoryOperationEntry`'s independent validator call (fixed, this entry); the round-2 count-pinning fix couldn't detect a same-count identity substitution (one known gap closing while a different, previously-covered contract loses its citation nets the same count) — replaced the count-only assertion with a SHA-256 fingerprint of the sorted missing-contract-id set (still never spells out a literal id in the file's own text), keeping the count assertion alongside it for a friendlier first failure message. All 8 corrected across 3 rounds. Round 4 — **converged**: "The changes add meaningful test coverage without altering production behavior. All 179 targeted tests passed with 3 expected skips, and the repository closeout guard succeeded." — no new finding classes. `just check` full workspace: 5788 passed / 10 skipped / 33 deselected / 1 xfailed (pre-existing baseline, unrelated; +4 vs the round-2 baseline of 5784, matching the 4 new entry-side redaction tests); `just overlay-check` 365 nodes/33 seams wired; `tools/arc_ledger.py --check` 94 closed/0 gated/5 resolved/0 forward; `tools/substitution_ledger.py --check` 54/54 retired; pyright 0/0/0; ruff clean.
- **Close-out steps.** ~~A dedicated triage pass mirroring the src-finding discipline this arc used: read each finding against the actual test file (not the one-line summary), disposition as fixed/stale/registered-forward, with a fail-before/pass-after test for anything real.~~ Done above. **Council: no.**
- **Downstream discovery + workspace hygiene (2026-07-14, triggered by independently re-verifying this entry's closure end-to-end — not just trusting the register's own claim).** Re-ran the actual test suite at `origin/main` HEAD (matched the claimed 5788 passed) and checked `main`'s post-merge CI, not just the merged PR's own pre-merge CI. Found `main`'s CI had been HARD-failing on every push for the entire day (20/20 recent runs, spanning the whole day's merges — confirmed unrelated to this arc's own content). Root cause: `tools/codex_context_guard.py`'s `_normalize_dashboard_snapshot` blanked the volatile `cadence` array (daily-commit sparkline) before its byte-comparison but missed the structurally identical `pr_cadence` array (PR-merge-cadence sparkline), whose "today" bucket counts any PR-merge-shaped commit at HEAD — so a refresh PR's own merge commit incremented it the instant it landed, HARD-failing `DASHBOARD_SNAPSHOT_STALE` on the very next push; invisible because the standing merge protocol only checks a PR's own pre-merge CI, never `main`'s post-merge state. Fixed in **PR #982** (mirrors the existing `cadence` normalization; new `test_dashboard_snapshot_normalization_ignores_pr_cadence_alone`, mutation-probed), refresh landed at **PR #983** — watched that exact commit's real post-merge CI run go `success`, breaking the day-long red streak. A broader sample across the full repo history (PR #1 → #983) found no other unaddressed defect — only (a) early advisory-gate failures from before `R-200-ci-lint-typecheck-blocking` flipped them blocking, and (b) the standard, always-expected `ROADMAP_DASHBOARD_DRIFT` transient every content-PR merge legitimately hits until its own follow-on refresh PR lands. Separately, the operator flagged 484 stale remote branches (visible in the GitHub branch list with historical CI-failure badges on 16 spot-checked PR numbers — all already `MERGED`, confirming those badges were frozen historical artifacts, not live problems). Cross-referenced all 484 against `gh pr list --state merged` head-refs (the squash-merge-safe discriminator — squash commits aren't ancestors of the source branch, so `--is-ancestor` alone would misclassify these as unmerged): 471 matched merged-PR head-refs and were deleted directly; the remaining 12 were individually inspected (diff-stat + `git log` vs `main`, closed-PR comment history, `git merge-base --is-ancestor`) before any deletion — 7 were pure ancestors of `main` with zero unique diff, 3 had explicit "superseded by PR #NN, unsafe to merge" closing comments (with clearance markers confirmed present in `main` for 2 of them), 1 (`r-xi-06-impeccable-polish`) was confirmed superseded by timeline reconstruction (a duplicate of the immediately-merged PR #232, itself fully replaced by PR #234's redesign the same day), and all 11 were deleted. The 12th (`add-claude-github-actions-*`, the standard Anthropic GitHub-App auto-generated `@claude`-mention-trigger workflow branch) was a genuine, still-undecided operator choice, not superseded work — surfaced via `AskUserQuestion`; **operator declined it for now** (can be re-added later via the GitHub App setup flow if wanted). `main` is now the only remote branch. No code changed by the cleanup itself (remote-ref deletions only); `main`'s HEAD (`d1cd5b3c`, PR #983) and its CI status were unaffected and reconfirmed `success` afterward.

### B-29 · Pre-existing F2-write gaps surfaced at B-23 (out-of-family Codex, not novel to B-23) *(registered 2026-07-13)*
- **What it is.** Two Codex round-3 findings against B-23's new `compose_cost_f2_entry_core` helper that, on inspection, describe a PRE-EXISTING gap shared by `sub_agent_dispatch.py`'s own established 8a/8b/8c F2-write precedent — not something B-23 introduced or made worse: (1) **`branch_metadata` sidecar never populated** at these two producer sites; (2) **no shared clock with `RuntimeAuditLedgerWriter.time_source`**.
- **Close-out, part (1) — `branch_metadata`.** CLOSED 2026-07-15. This row's own "council-eligible, conditional on a proposed reading" framing was stale by the time this row was groundtruthed: the carrier-ownership question was already resolved as a Class 1 fork on 2026-06-13 (`.harness/class_1_fork_b1_branch_causality_route_x_vs_y.md`, Route Y) — CP spec v1.32 §25.13 states verbatim "The CP driver is the **producer** (composes branch metadata at branch-spawn + cancel); the IS sidecar is the **persisted carrier**," and IS spec v1.8 §5.4 confirms `terminal_status` is "producer-supplied by the CP `WorkflowDriver`." `WorkflowDriver.append_branch_step_ledger_entry` / `append_branch_terminal_ledger_entry` (`harness-cp/src/harness_cp/workflow_driver.py:5844-5953`) already compose and populate `branch_metadata` at ~19 real production call sites — the carrier is live and spec-conformant, not globally dormant as originally framed. `sub_agent_dispatch.py`'s dispatch-fact entry and the 4 cost-attribution composers are NOT branch-spawn/terminal entries per §25.13's scope, so their `branch_metadata=None` default is correct behavior, not a gap. No council was needed — the spec already resolved it. Added a one-line clarifying comment at both producer sites (`sub_agent_dispatch.py:698-701`, `cost_attribution_f2_write.py:149-152`) citing CP spec v1.32 §25.13 / IS spec v1.8 §5.4 so a future reader doesn't re-flag the omission as a gap. No behavior change; ruff clean; 155 sub_agent_dispatch/cost_attribution tests still pass.
- **Close-out, part (2) — shared clock, split to `B-44`.** The `time_source` half genuinely has no live consumer (confirmed: `stage_4_od.py:62` always defaults to `datetime.now(UTC)`, and `stage_5_loop_init.py:596`'s dispatcher-side `time_source` is likewise always hardcoded at bootstrap) — split out as its own row, `B-44`, since it has a distinct, still-open disposition rather than remaining bundled under a now-closed finding.

### B-44 · No production bootstrap site injects a non-default `time_source` clock *(split from B-29, 2026-07-15)*
- **What it is.** `RuntimeAuditLedgerWriter` and `RuntimeSubAgentDispatcher` both support test-clock injection via a `time_source` constructor field, but no production bootstrap site (`stage_4_od.py:62`, `stage_5_loop_init.py:596`) ever injects a non-default one — both always resolve to `lambda: datetime.now(UTC)`. The shared `compose_cost_f2_entry_core` helper also accepts a `time_source` parameter, but none of the 4 `attribute_*_dispatch_cost` composer functions (tool/llm/webhook/validator) expose one of their own — they call the helper without it, so it always defaults the same way. A flagged `NonMonotonicTimestampError` failure mode (from clock skew across concurrent writers) has no live trigger today because nothing ever supplies a different clock to compare against.
- **Current state.** Registered, not fixed — genuinely dormant, zero live production or test consumer exercises this. Mirrors the workspace's established B-19/B-42 discipline against building speculative infrastructure ahead of a real consumer.
- **Close-out steps — OPERATIVE (the originally-registered plan, RESTORED at out-of-family Codex round 3, 2026-07-27).** If a real deterministic-clock consumer ever surfaces (e.g. a multi-writer clock-skew test or a production need for injectable time), thread an optional `time_source` parameter through the 4 `attribute_*_dispatch_cost` composer signatures (tool/llm/webhook/validator, which currently have none) down to the already-`time_source`-accepting `compose_cost_f2_entry_core` helper, plus the bootstrap sites (`stage_4_od.py:185`, `stage_5_loop_init.py:649`, `migrate_audit_sidecar.py:151`). This is the **only** mechanism that satisfies the deterministic/injectable-clock use case, and it stays within IS spec C-IS-07 §7.6's preserved caller-supplied timestamp semantics for DIRECT surfaces — **no spec change needed**. **CONSIDERED AND REJECTED — `WRITER_OWNED_TIMESTAMP` sentinel adoption at the composer F2 write** (briefly recorded as the likely fix by the 2026-07-27 grounding pass; corrected the same day at out-of-family Codex round 3), on two spec-verified grounds: **(a)** IS spec C-IS-07 §7.6's "Surfaces that do NOT change" paragraph classifies *"runtime cost writes"* as DIRECT appends that keep *"**caller-supplied** timestamp semantics verbatim"*, and its "Registered residual" paragraph states that *"[e]xtending writer-owned authority to direct surfaces would change caller-supplied semantics for every existing direct producer and requires its own back-flow"* — an extension explicitly assigned to `B-57` (*"`B-57` covers the remaining direct writers"*), **not** to `B-44`; adopting the sentinel here at impl time would be a silent spec violation. **(b)** The sentinel cannot serve `B-44`'s own stated trigger at all — `state_ledger_write.py:316-320` resolves `WRITER_OWNED_TIMESTAMP` through a hardcoded `datetime.now(UTC)` inside the write lock, so a deterministic/injectable-clock consumer gets **no** determinism from it; only an injectable `time_source` can. The `sub_agent_dispatch.py:801` / `workflow_driver.py:2028-2030` cites describe only what B-48 did on the DRAIN/offloaded surfaces — they are not a precedent for the DIRECT composer F2 write. **Council: no** — mechanical once a consumer exists; not urgent.
- **UPDATE 2026-07-27 (grounding pass, re-verified at HEAD `25b44127`) — STILL DORMANT, and MORE dormant than registered.** (a) `RuntimeSubAgentDispatcher.time_source` is now **vestigial**: still accepted as a required ctor kwarg (`sub_agent_dispatch.py:607`, docstring `:587-592` retaining it for API reasons) but **never called** — the sole remaining `self.time_source` occurrence in that module is inside a comment (`:794`), because the 8b F2 dispatch entry now stamps `timestamp=WRITER_OWNED_TIMESTAMP` (`:801`) per IS spec C-IS-07 §7.6 and lets the IS writer sample inside its own lock. (b) A **third production construction site** this row never enumerated: `harness-runtime/src/harness_runtime/admin/migrate_audit_sidecar.py:139-152` builds a `RuntimeAuditLedgerWriter` with `time_source=lambda: datetime.now(UTC)` (`:151`) — the default clock, so the scope EXTENDS without the premise being falsified. (c) **Line cites drifted, refreshed here:** `stage_4_od.py:62` → the audit-writer materialization now sits at `:185` (`materialize_audit_writer_stage(...)`), which passes NO `time_source` at all and falls through to the `audit_writer.py:1639` default; `stage_5_loop_init.py:596` → `:649`. (d) All four `attribute_*_dispatch_cost` composers still expose **zero** `time_source` parameters (occurrence count 0 in each of the tool/llm/webhook/validator modules), and `compose_cost_f2_entry_core` still stamps `timestamp=time_source()` caller-side at `cost_attribution_f2_write.py:157`.
- **Close-out guidance REFRESHED, then CORRECTED BACK — the full arc (this bullet records WHEN the guidance changed; the operative instruction is the "Close-out steps — OPERATIVE" bullet above, and the committed posture is unchanged throughout).** The **2026-07-27 grounding pass** briefly flipped this row's guidance to make **`WRITER_OWNED_TIMESTAMP` sentinel adoption at the composer F2 write** the PRIMARY instruction and marked the originally-registered clock-threading plan "SUPERSEDED — do NOT follow," reasoning by analogy from `sub_agent_dispatch.py:801` / `workflow_driver.py:2028-2030`. **Out-of-family Codex round 3, the same day (2026-07-27)**, found that flip wrong on two spec-verified grounds, and it was **corrected back**: (a) C-IS-07 §7.6 preserves caller-supplied timestamp semantics for DIRECT surfaces — runtime cost writes are named as such — and its "Registered residual" paragraph requires a separate spec back-flow (`B-57`'s registered scope, not `B-44`'s) before writer-owned authority may extend to any direct surface, so following the flipped guidance at impl time would have been a silent spec violation; and (b) the sentinel resolves through a hardcoded `datetime.now(UTC)` (`state_ledger_write.py:316-320`) and therefore cannot serve a deterministic-clock consumer at all — the very trigger this row is registered against. **Clock-threading is the operative close-out again.** **Committed posture remains detect-then-refuse / register-don't-build** (B-19/B-42 discipline). **Council: no.**
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT; disposition VERIFIED-DORMANT.** All three cites the 2026-07-27 pass refreshed still resolve exactly: `stage_5_loop_init.py:649` and `migrate_audit_sidecar.py:151` both still inject a lambda returning `datetime.now(UTC)`, and `stage_4_od.py` contains **zero** `time_source` occurrences (falling through to the `audit_writer` default), exactly as claim (c) states. All four `attribute_*_dispatch_cost` composer modules still expose **zero** `time_source` parameters. `RuntimeSubAgentDispatcher.time_source` remains vestigial — required ctor kwarg at `sub_agent_dispatch.py:607`, retained-for-API docstring at `:587-592`, sole remaining `self.time_source` occurrence inside a comment at `:794`. Dormancy evidence unchanged, NARROWED to the production paths (codex 2026-08-05): zero PRODUCTION bootstrap/composer sites inject a non-default `time_source` clock; TEST consumers do exist (`test_lifecycle_audit_writer.py:391-399` ticking clock, `:1903-1906` scripted non-monotonic clock) and exercise the seam without converting the production gap. Wakes on a multi-writer clock-skew test or a production injectable-time need.

### B-30 · Cost-attribution disambiguator replay-semantics inconsistency *(surfaced at B-23 round 6, advisor())*
- **What it is.** The 4 cost-attribution composers now use 3 different `dispatch_disambiguator` replay strategies: validator dispatch composes a *deterministic* `(burden_count, outcome, branch_index)` string, so a step that re-fires cost attribution on resume recomputes the SAME F2 key and `compose_cost_f2_entry_core`'s underlying `ledger_writer.append` call `IDEMPOTENT_NOOP`s at the F2 anchor only (see below — this does NOT by itself mean the billed cost event dedupes). Tool/LLM dispatch use a real per-attempt OTel `span_id`, and webhook dispatch uses a fresh `uuid.uuid4()` per call — both create a genuinely NEW F2 entry on any re-invocation, resumed or not.
- **Correction, 2026-07-15 — this is NOT an undecided policy question, but is NOT fully diagnosed either.** The original framing ("no decided policy," council-conditional) was stale. `ADR-D6_v1_2.md` §1.5 + OD spec C-OD-14 §14.5.1 already specify a complete, ratified replay-dedup algorithm (materialized at `harness_od.idempotency_join_dedup.dedupe_on_replay`) — **no production ingestion-layer caller of it exists anywhere in the codebase today** (confirmed by grep across all 5 axis packages). This is a missing consumer, not merely an unwired composer — **and (round 4 correction) it's not JUST a missing consumer either**: all 4 `attribute_*_dispatch_cost` composers hard-code `SpanCostRecord.engine_replay_disposition=_DEFAULT_REPLAY_DISPOSITION` where `_DEFAULT_REPLAY_DISPOSITION = ReplayDisposition.NO_REPLAY` (verified at `cost_attribution_llm_dispatch.py:88,239`, `cost_attribution_tool_dispatch.py:90,295`, `cost_attribution_webhook_dispatch.py:65,204`, `cost_attribution_validator_dispatch.py:86,243` — none expose a parameter to override it). So even once an `ingest_span`-shaped consumer exists, it could never see `CHECKPOINT_RESUME`/`RECONCILER_ITERATION`/`WAL_CONSUME` from any of the 4 composers today — a producer-side replay-metadata wiring gap, separate from and in addition to the missing ingestion-layer consumer.
- **Why this is still NOT buildable right now (personally verified, not just a grounding agent's recommendation).** ADR-D6 §1.5's algorithm is specified as `ingest_span(span)` — a **trace-ingestion-time** function operating on spans read from the observability pipeline, not something a per-dispatch composer calls inline while producing its own F2 write. Wiring `dedupe_on_replay` directly into the 4 `attribute_*_dispatch_cost` composers (as an initial grounding pass recommended) would be a category error: the correct call site is an ingestion-layer consumer that doesn't exist in production yet, not merely an unimplemented wiring step inside the composers. Separately, the prior `B-COST-REPLAY-DEDUP-WITNESS` closure record (`.harness/b-cost-replay-dedup-witness-closure-record.md`, CLOSED GREEN no-fix) already verified the IS-layer content-addressed ledger write independently provides the real replay-dedup guarantee for the `JOURNAL_RESUME`/`PURE_PATTERN_NO_ENGINE` engine class, and its own scope note explicitly pre-commits any adjacent replay-dedup finding to register-don't-build, not a broadening of that arc. Webhook's fresh-`uuid4`-per-attempt billing is confirmed deliberate — the actual rationale lives at `webhook_delivery_composer.py:363-371`'s inline comment ("this method fires unconditionally on every invocation — no idempotent skip at this layer. A fresh UUID per call guarantees each invocation gets its own F2 anchor"), an intentional out-of-family-Codex-round-5 fix, not an oversight to align away.
- **Out-of-family Codex flagged three further nuances in this correction itself (2026-07-15, three rounds, all addressed same-PR).** (1) The paragraph above must not be read as settling whether validator's F2-key collision actually dedupes the *billable cost event* — it only describes the F2 anchor identity write. Verified directly: `compose_cost_f2_entry_core` (`cost_attribution_f2_write.py:153-166`) discards `ledger_writer.append`'s return value entirely and always returns a `StateLedgerEntryRef`, whether the underlying append was `APPENDED` or `IDEMPOTENT_NOOP`. The caller (e.g. `attribute_validator_dispatch_cost`, `cost_attribution_validator_dispatch.py:274-292`) then unconditionally builds `audit_entry` from that `entry_core` and calls `audit_writer.append(tenant_id, audit_entry)` — a SEPARATE, content-hash-addressed ledger write whose own dedup depends on `AuditPayload` byte-identity, not on whether the F2 cost-write above it deduped. **However** (round 3 correction) — this audit-ledger layer IS already independently verified to dedupe for ONE specific combination: `test_journal_resume_completed_run_retry_does_not_double_emit_cost` (`test_b_cost_replay_dedup_witness.py`) drives the real `attribute_llm_dispatch_cost` twice with the same `run_id` under `JOURNAL_RESUME` and proves the audit-ledger entry count stays at 1 — so "unverified for any composer" was too strong; the gap is specifically the OTHER 3 composers (tool/webhook/validator) and the OTHER engine classes, not this one already-proven case. (2) A third mechanism exists: ALL 4 production cost-wrapper call sites (`llm_dispatch.py:3031-3035`, `runtime_tool_dispatcher.py:625-628`, `webhook_delivery_composer.py:373-376`, and validator's equivalent lines 400-405) unconditionally append the returned `SpanCostRecord` to a run-scoped accumulator (`cost_record_sink`), which `api.py:1123-1132`'s `_build_run_result` rolls into the user-visible `RunResult.cost_attribution` via `_rollup_cost_attribution`. **However** (round 3 correction) — each public `run()`/`resume()` invocation installs a FRESH `CostRecordAccumulator` (`api.py:900`, `COST_ACCUM_VAR.set(CostRecordAccumulator())`; also `resume()` at `api.py:901-949` does the same), so a crash-then-separate-resume-call (the actual replay scenario this finding is about) does NOT carry the prior invocation's records into the new one's `RunResult` — the accumulator is NOT a confirmed cross-invocation dedup failure. The unconditional-append behavior only risks double-counting for repeated dispatches WITHIN one invocation (e.g. an in-flight retry loop calling the composer twice inside a single `execute_workflow`/`run()` call) or from external aggregation of multiple separate `RunResult`s by a caller — neither of which is established as happening in production today. **Net effect of both corrections**: the audit-ledger layer is proven-dedupe for LLM+`JOURNAL_RESUME` specifically and unverified elsewhere; the accumulator is NOT a general dedup-failure layer, only a narrower within-invocation-retry or external-aggregation concern.
- **Close-out steps.** (1) Resolve where `ingest_span`'s production call site actually lives (or should be built) before touching any composer. (2) Verify whether double-billing is an ACTUAL exercised risk for the durable engine classes (`SAVE_POINT_CHECKPOINT`/`EVENT_SOURCED_REPLAY`/`RECONCILER_LOOP`/`WAL_SEGMENT`) whose `resume_at` skip logic may already prevent re-dispatch of completed steps, mirroring the `JOURNAL_RESUME` finding. (3) Extend the existing `JOURNAL_RESUME`+LLM audit-ledger witness pattern to the other 3 composers (tool/webhook/validator) and, separately, verify whether a within-invocation retry (not a cross-invocation resume) can actually double-append to the run-scoped accumulator in production. (4) Thread the real `engine_replay_disposition` into all 4 composers (currently hard-coded to `NO_REPLAY`) so a future ingestion consumer can actually discriminate replay dispositions — a producer-side prerequisite, not optional polish. Only after (1)-(4) decide whether composer-level wiring is even the right fix, and at which layer. **Council: no** — a specified policy already exists (ADR-D6 v1.2 §1.5); the open question is call-site + live-risk + per-composer/per-engine-class verification + producer-side metadata wiring, not a design fork. Mirrors the B-19/B-42 discipline against building speculative infrastructure ahead of a real, demonstrated consumer.
- **✅ Step (4) BUILT + steps (1)/(2) grounded 2026-07-25 (`roadmap-continue`, advisor()-checked before build).** Re-verified at HEAD before building: `engine_replay_disposition` still had zero readers anywhere outside its own write-sites + the (uncalled) `dedupe_on_replay`/`attach_idempotency_key_to_cost_record` functions (confirmed by grepping the field itself, not just `dedupe_on_replay()` callers — advisor()'s check 1). But `StepExecutionContext.run_engine_class` (sourced from `manifest_entry.engine_class`) is **non-vacuous** — it already drives production durable-auto-fence logic at `runtime_tool_dispatcher.py` + `managed_agents_dispatch.py` (advisor()'s check 2), so a hard-coded `NO_REPLAY` was a genuine persisted-audit-ledger correctness defect for any workflow declaring a durable engine class, not a no-op default. That satisfied advisor()'s build-vs-defer decision rule (no reader, but a non-vacuous persisted-audit correctness value → build). Threaded `run_engine_class` through all 3 **live, step-scoped** composers — `attribute_llm_dispatch_cost` (`llm_dispatch.py`'s `dispatch()`, `step_context` already in scope at the existing cost-attribution call site), `attribute_tool_dispatch_cost` (`runtime_tool_dispatcher.py`'s `_attribute_tool_cost_best_effort`, same pattern), `attribute_validator_dispatch_cost` (`cost_attribution_validator_dispatch.py`'s `on_post_evaluate`, same pattern) — each now resolves `engine_replay_disposition` via the existing closed-total `REPLAY_DISPOSITION_MAPPING[EngineClass]` (the same table `retry_breaker_fallback.py`/`retry_breaker_tool.py` already use at a different surface) instead of the hard-coded default; `None` (engine class not surfaced) still falls back to `NO_REPLAY`, byte-preserving prior behavior for any caller that doesn't thread it. 15 new parametrized test cases (`EngineClass` is a 5-member enum × 3 composers) prove the mapping round-trips; a dedicated default-preserved test confirms the `None` fallback. `pyright` (strict) 0 errors; `ruff` clean; full `llm_dispatch`/`runtime_tool_dispatcher`/cost-attribution suites green (211 tests).
- **Webhook (4th composer) deliberately NOT threaded — deferred to the FM-2 webhook-config arc, not an oversight.** `WebhookDeliveryComposer` is a bootstrap-**stage-5 process-singleton** (constructed once at `webhook_delivery_composer_factory.py`), not per-step like the other 3 — its `workflow_id`/`parent_action_id`/`parent_idempotency_key` are **ctor** params, not per-call, and the v1.26 empty-marker factory doesn't even bind `rate_table`/`cost_chain`/`audit_writer` (confirmed by reading the factory body: those 3 are simply absent from the `WebhookDeliveryComposer(...)` call), so `_attribute_webhook_cost_best_effort`'s guard (`if self._rate_table is None: return`) always fires in production today — webhook cost-attribution is **fully dormant at HEAD**, a pre-existing, already-documented gap (this same finding's own text: "Webhook's fresh-uuid4-per-attempt billing is confirmed deliberate"). Threading a per-run `engine_class` into a per-process singleton is a real interface change (a new per-call param on `deliver_webhook`/`deliver_webhook_for_brief`, sourced from `hitl_gate_composer.py`'s `step_context` at its `deliver_webhook_for_brief` call site) that only matters once FM-2 binds the cost substrates — building it now would be dead code exercised only by a synthetic unit test. Registered here rather than silently dropped.
- **Step (1) (`ingest_span` consumer) — re-confirmed absent; register-don't-build stands (B-19/B-42 discipline).** Grepped `ingest_span` across the full repo (incl. all worktrees) — the only near-match is `collector_daemon.py`'s unrelated `ingest_span_row` (ring-buffer intake for `trace_browser.py`'s admin CLI), itself independently documented as having "no production caller." No `ADR-D6 §1.5`-shaped `ingest_span(span)` ingestion-time consumer exists anywhere. Building the ingestion-layer consumer itself remains out of scope — no real driver exists yet (mirrors the B-19/B-42 precedent this finding already cites).
- **Step (2) (double-billing risk) — verified NOT a live bug.** Read `workflow_driver.py:4619-4620` directly: the `SINGLE_THREADED_LINEAR` step-dispatch loop "Begin[s] at `resume_at` to skip already-materialized steps from a prior crashed/drained run" — confirming the register's own suspicion. Steps `0..resume_at-1` (the durably-committed prefix, for `SAVE_POINT_CHECKPOINT`/`EVENT_SOURCED_REPLAY`/`RECONCILER_LOOP`/`WAL_SEGMENT` alike) are never re-dispatched on resume, so there is no live double-cost-attribution bug for completed steps — only the metadata-correctness gap step (4) closes. An in-flight (dispatched-but-not-committed) step at crash time DOES re-dispatch on resume, but that re-dispatch is the step's *sole* attribution event (the original in-flight attempt never completed/never got audited) — correctly informational as `CHECKPOINT_RESUME`/etc. post step-(4), not a double-bill.
- **UPDATE 2026-07-27 (roadmap-continue, `advisor()`-checked before disposition) — step (3) VERIFIED, confirmed deliberate, NOT a defect.** An `Explore` agent traced the call graph and confirmed the double-append IS reachable, for the tool composer only: `retry_breaker_tool.py:123-129`'s two retried exception classes (`ToolInvocationTimeoutError`, `MCPHostUnreachableError`) each hit `runtime_tool_dispatcher.py`'s failure-path `attribute_tool_dispatch_cost` call (~1177/~1203) before re-raising, and the eventual success-path call (~1276) appends again to the SAME run-scoped `cost_record_sink` — confirmed NOT deduped downstream by direct read of `api.py:988-990`'s `_rollup_cost_attribution` (an unconditional sum by axis, no dedup logic of any kind). NOT reachable for the LLM composer (`llm_dispatch.py:1785-1824`: attribution fires only after a successful provider response, immediately before return — no retryable exception exists past that point). NOT applicable for the validator composer (its multiple `on_post_evaluate` firings under REVALIDATE are legitimately-separate billable `evaluate()` calls by design, per CP spec §28.10.4 invariant 1 — a `dispatch_disambiguator` built specifically to keep them from idempotency-key-colliding, not an accident). **The agent's own framing ("LIVE bug") was a presence finding, not a correctness finding, and was NOT adopted** — `advisor()` caught this before any fix was drafted: the tool composer's failure-path attribution is symmetric across all 3 exception classes including `ToolInvocationProtocolError` (`runtime_tool_dispatcher.py:1186-1198`), which is NOT even retried by `retry_breaker_tool.py` and still gets attributed — the tell that the design axis is "every physical MCP attempt," not "every retried attempt." The inline comment at `runtime_tool_dispatcher.py:1174` cites `U-OD-39 AC #1` directly ("cost-attribution invoked on every dispatch, success + failure paths") and reasons explicitly about the failure-path's degraded byte-count — not an oversight. This matches this same row's already-confirmed-deliberate webhook fresh-`uuid4`-per-attempt precedent exactly (same shape, different composer); applying a different disposition to tool than to webhook absent a spec change would itself be the inconsistency `[[forward-register-consistent-dismissal-criterion]]` warns against. **No fix landed, none warranted** — suppressing failure-path attribution would contradict a cited AC and diverge tool from webhook unilaterally. **One genuinely new residual this pass surfaced:** `U-OD-39`'s own AC #1 ("every tool dispatch, success + failure") and AC #5 ("1 tool call → 1 cost-record") use different granularity vocabulary, and neither the OD spec nor plan disambiguates whether a `flat_per_invocation` rate should bill per PHYSICAL MCP attempt or per LOGICAL dispatch when a retry eventually succeeds — an OD-spec-owned semantic question, not Phase-7 impl discretion. Registered, not answered unilaterally.
- **Remaining open surface (not closed by this pass):** the AC #1/AC #5 granularity-vocabulary gap named above, and webhook's per-call threading (deferred to FM-2, see above) stay open. Not re-registered as a fresh `B-*` — tracked here as this finding's own residual.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT on every limb; disposition VERIFIED-DORMANT on the replay-dedup half, FORK-SHAPED on the AC-granularity residual.** Step (4)'s threading is live for the three step-scoped composers (`engine_replay_disposition=_resolve_replay_disposition(run_engine_class)` at `cost_attribution_llm_dispatch.py:267`, `cost_attribution_tool_dispatch.py:317`, `cost_attribution_validator_dispatch.py:263`) while webhook remains hard-coded to `_DEFAULT_REPLAY_DISPOSITION` at `cost_attribution_webhook_dispatch.py:206` — the deliberate FM-2 deferral, not drift. Step (1) re-confirmed **absent** repo-wide: no ADR-D6 §1.5 `ingest_span(span)` consumer exists; the only near-match is `collector_daemon`'s unrelated `ingest_span_row`, itself test-only. The AC #1/AC #5 granularity gap is unresolved and stays **OD-spec-owned** (design-substrate back-flow, not Phase-7 impl discretion). *Not re-traced this pass: the `api.py` `RunResult` accumulator/rollup lines this row cites — adjudicated across three prior rounds, no carrier file in that path changed since.*

### B-31 · Paused-child resume guard does not validate `child_workflow_id` identity *(surfaced at B-21, out-of-family Codex round 2)*
- **What it is.** The `paused_child_branches` resume material-diff guard (both `_execute_parallelization`'s NEW guard and `_execute_orchestrator_workers`'s pre-existing v1.45 guard) validates a re-supplied `SUB_AGENT_DISPATCH` branch's bounds, `step_id` identity, and (PARALLELIZATION only, B-21) step-kind — but not that its `step_payload` still targets the SAME `child_workflow_id` the snapshot was captured against. A same-`step_id`/same-kind edit that swaps the target child workflow would pass the guard; the production child runner (`sub_agent_dispatch.py`) would raise a workflow-ID mismatch internally, but the generic branch-failure catch records the branch terminal and re-pauses the parent with `paused_child_branches` empty — silently discarding the original child's suspended state rather than surfacing the swap.
- **Current state.** Confirmed real by direct read (`attempt_resume` validates `snapshot_hash` + the pause-context anchor material diff, never `workflow_id`). PRE-EXISTING on the v1.45 mechanism (ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION) — not introduced by or unique to B-21's PARALLELIZATION port. Not fixed at B-21; CP cannot cheaply check this today because `step_payload` is an opaque runtime-owned `Mapping[str, Any]` (axis-boundary discipline — CP does not officially know the `child_workflow_id` payload-key convention).
- **Close-out steps.** Either (a) add a typed `child_workflow_id: str` field to `PausedChildBranchResumeState` (shared carrier — fixes both PARALLELIZATION and ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION in one pass) validated at the CP resume guard, or (b) add a runtime-side identity check inside the `SUB_AGENT_DISPATCH` dispatcher before it threads `child_resume_snapshot`. **Council: no** (mechanical carrier-completeness extension of an already-cleared mechanism — no nameable cross-domain tension). Scope spans BOTH carriers — not PARALLELIZATION-specific.

### B-32 · Nested paused-child pause-reason propagation *(surfaced at B-21, out-of-family Codex round 3; scope narrowed after a same-session Codex review of the first attempt)*
- **What it is.** The parent's `_pause_reason` derivation (both PARALLELIZATION and ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION — verified symmetric by direct read, not merely asserted) examined only `effect_fence_paused_dispositions`, never a nested child's OWN pause reason (e.g. `HITL_PENDING`) — an operator surface keying off the top-level `pause_reason` to decide what resume action is owed would see the generic `EXPLICIT_OPERATOR` instead.
- **Current state — CLOSED (pause_reason half only).** A first attempt bundled this with per-child response ROUTING (originally coupled here) and used a naive "any paused child present → HITL_PENDING" label. Out-of-family Codex review of that attempt caught it mislabeling a nested child that paused for an ordinary `cascade_policy=pause` branch failure (itself `EXPLICIT_OPERATOR`, not a HITL gate) as `HITL_PENDING` — a real bug, confirmed by executing the fixture. Corrected to check `child_snapshot.pause_reason` directly: a nested `HITL_PENDING` pause relabels the parent; any other nested reason does not. `EFFECT_FENCE_AMBIGUOUS` keeps its existing top precedence. Mutation-probed at BOTH the PARALLELIZATION site (a contrasting-baseline test + a positive test, in `test_workflow_driver_parallelization_pause.py`) AND the ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION site (a contrasting-baseline assertion added to the existing cancellation-race test + a new positive test, in `test_workflow_driver_fanout_pause.py`) — all four confirmed load-bearing by reverting/inverting the fix and observing the corresponding test fail, then restoring. The merge-gate test-witness lens caught that the second site was originally UNWITNESSED (an earlier close_out draft over-claimed "both sites" before those tests existed) — closed as part of this same arc. impl-to-cleared-spec: the existing `HITL_PENDING` semantic ("HITL gate opened; workflow paused awaiting operator response") already covers a nested gate without a new carrier/exception/enum value; a PROSE-ONLY CP spec delta (v1.99→v1.100) was authored to correct v1.97's Change-note registered-forward-work paragraph's stale "coupled" framing (see below) — no contract amendment.
- **Response ROUTING is NOT closed here.** The original "coupled" framing (this item + response routing must land together, or the propagated reason is a false affordance) is now moot as framed: the CORRECTED reason-propagation is honest on its own (it tells the operator a HITL response is owed somewhere in this round; it doesn't over-promise routing). Per-child response delivery is tracked separately at **B-39**, split off after Codex found the first routing attempt had 3 real defects. A SECOND out-of-family Codex round re-raised the false-affordance concern against the corrected label itself; grounded via direct grep before shipping — `pause_reason` is read in exactly one non-telemetry place (this derivation) and the composer's actual resume path (`consume_and_clear`) never branches on `pause_reason` at all, so nothing auto-dispatches a response off the label. Shipped with an explicit code comment at both sites cross-referencing B-39 so the limitation is documented, not silently assumed away. The merge-gate spec-conformance lens then caught that v1.97's Change-note registered-forward-work paragraph still asserted (b)+(c) "travel together" — contradicted by shipping (c) alone without a matching delta (the exact `B-31`/v1.99 precedent this workspace already established for closing a gap registered in that same Change-note paragraph). Closed via `design-substrate/Spec_Control_Plane_v1_100.md` (prose-only correction; clearance at `.harness/clearance/spec-control-plane-v1-100-cleared-2026-07-15.md`).

### B-39 · Nested paused-child HITL-response ROUTING (per-child operator response delivery) *(split from B-32 after a same-session Codex review found the first routing attempt had 3 real defects; not shipped — CLOSED 2026-07-24, spec leg PR #1092 + impl leg Slice A PR #1094 + Slice B PR #1096/#1097)*
- **CLOSED 2026-07-24 — spec leg + both impl-leg slices landed.** Spec leg (PR #1092, CP+Runtime spec v1.106 + plan deltas, 7 same-day correction rounds). Impl leg Slice A (PR #1094): `ResumeContext.hitl_responses`/`hitl_response_for(child_run_id)` carrier, byte-exact to CP spec v1.106 §0, 34 tests, 3 codex rounds, merge-gate 2 rounds to all-approve. Impl leg Slice B (PR #1096 + terminating refresh PR #1097): `HITLDeliveryCell` per-branch one-shot delivery (replaces the retired ctx-level `ResumeContextHolder`), `compute_hitl_uniform_fallback_eligible_run_id` implementing CP spec v1.106 §1.2 properties 4/5, and `_collect_gate_owning_run_ids` correctly distinguishing B-32-relabeled containers from true gate-owning leaves — 7 `just codex-review` rounds to convergence (rounds 2-5 each a genuine, mutation-probe-verified fix), merge-gate 3-lens all-approve (test-witness surfaced + fixed 2 real witness gaps before merge). One recurring finding (an ancestor `SUB_AGENT_BOUNDARY` gate re-firing on resume-forward, discarding a deeper-paused child's cursor) was grounded as reachable-but-pre-existing/orthogonal to this arc and registered separately as `B-72` rather than patched blind. CP suite 1753 passed/4 skipped, Runtime suite 3048 passed/40 skipped, ruff+pyright clean. The bullets below (What it is / Current state / Close-out steps) describe the FIRST, REVERTED attempt and are retained verbatim for provenance — read the CLOSED bullet above for the actual shipped design, not the bullets below.**

- **What it is.** Two or more concurrently-paused peer/nested children (PARALLELIZATION `asyncio.TaskGroup`, or nested ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION recursion) share ONE run-level `ctx.resume_context_holder`. Delivering each paused child its OWN correct operator HITL response — without one child silently starving another, and without a later unrelated resume accidentally reusing a stale response — is unresolved.
- **Current state.** A first attempt built `ResumeContext.hitl_responses: dict[str, HITLResult]` keyed by the paused child's `workflow_id` (mirroring the existing `effect_fence_resolutions` per-branch pattern), consumed via non-destructive `peek()` + keyed lookup at the runtime HITL gate composer's resume Step-0. It was mutation-probed (reverting broke the composer-level routing test) and passed the full workspace suite — but out-of-family Codex review caught 3 real defects before it shipped, all confirmed correct on direct inspection:
  1. **Never one-shot-consumes.** `peek()` is non-destructive by design (that's what avoids the original race), but nothing ever clears a KEYED entry once delivered — a LATER, unrelated HITL-composed step sharing the same `workflow_id` (e.g. the same child dispatched again, or coincidentally reused) would silently reuse the stale response instead of requiring a fresh operator gate.
  2. **`workflow_id` is not branch-unique.** Fan-out siblings can legitimately dispatch the SAME `child_workflow_id` — the runtime already uses `branch_path` (§25.16 idempotency-key composition, `compose_branch_path`) specifically to disambiguate this case. A map keyed only by `workflow_id` collapses two such paused siblings onto one entry, so both would receive the SAME operator decision.
  3. **The delivery mechanism itself is unresolved, not just the map shape.** `execute_workflow` / `child_workflow_runner` currently accept NO `resume_context` parameter at all (only `pause_snapshot_input`) — there is no existing plumbing that threads a resolved per-child value into a recursive child's OWN internal re-entry. `effect_fence_resolution`'s per-branch pattern (the thing this attempt mirrored) resolves PARENT-side and threads a single resolved value onto the CHILD's `StepExecutionContext` — but that field is consumed by TOOL-step dispatch code within the SAME step, not by a recursive child's OWN composer at a deeper recursion level. So the "mirror the effect-fence shape" premise this attempt started from does not actually carry through to the recursive-child case; the threading path needs its own design, not just a differently-keyed map.
  The attempt was fully reverted (not shipped) rather than landed with known defects.
- **Close-out steps.** Design constraints for the next attempt, per the 3 findings above: (a) key by something branch-unique — `branch_path` or an equivalent derived child-run identity, not `workflow_id` alone; (b) delivery must be genuinely one-shot per key without a destructive whole-holder clear racing concurrent siblings — `peek()` alone is insufficient once true one-shot semantics are required per key; (c) work out the actual threading mechanism first (how does a resolved per-child value reach that child's OWN recursive re-entry, given `execute_workflow` has no `resume_context` parameter today) — the map shape is the easy part. **Council:** not yet re-evaluated — the original C7/C8-HITL-fidelity-vs-C1/C5-orchestration-correctness framing may not even be the right lens once a concrete delivery mechanism is proposed (per this register's own `[[probe-resolves-fork-prescribed-council]]` discipline: ground the mechanism first, then decide if a real value-tension remains). Not urgent — open when a real multi-child concurrent-HITL-pause scenario is confirmed reachable in a live deployment (today's fixtures/tests do not exercise it in production; the B-32 fixtures that DO exercise nested pauses use plain branch-failure pauses, not HITL gates).

### B-33 · `verify_rotation_6_steps`'s hash-chain check cannot prove a rotation boundary occurred *(surfaced at B-26, out-of-family Codex rounds 1 + 5, `advisor()`-confirmed; CLOSED 2026-07-23)*
- **CLOSED 2026-07-23 — all three ratified-fork (Option A) legs landed.** (i) IS carrier: IS spec v1.11→v1.12 (NEW §5.6 D-derivative sidecar `rotation_correlation_id` on `StateLedgerEntry`/`EntryPayload`, C-IS-05; NEW §7.7 join-key read-side invariants, C-IS-07) + IS plan v2.8 (U-IS-20), code at PR #1083. (ii) CP+OD+Runtime spec+plan rider: CP spec v1.104→v1.105 (§20.3.1 row 7 amended + NEW §20.3.2), OD spec v1.34→v1.35 (NEW §24.8), Runtime spec v1.104→v1.105 (NEW §13.6), landed at PR #1084 (4 codex rounds, 24 findings fixed). (iii) the real CODE: OD `find_rotation_pair_evidence` + CP `RotationPairEvidenceProvider`/`KeyIdentityResolver` Protocols + `verify_rotation_6_steps` extension (`rotation_window_entries`/`evidence_provider`/`key_identity_resolver`, subset-membership authentication, dual `pair_present`+`signatures_verified` gate, required physical-key-distinctness attestation) + Runtime composition-root adapter, landed at PR #1087 (2 codex rounds to clean convergence + 3-lens merge-gate all-approve).
- **FORK FILED 2026-07-21, RATIFIED 2026-07-21 (option A)** — `.harness/class_1_fork_b33_rotation_correlation_carrier.md`.
- **What it was.** `VERIFY_HASH_CHAIN_LINK` (landed at `B-26`) verified **generic** `prior_event_hash` continuity via the real `harness_is.chain_verification.verify_chain` — it did not and could not confirm that a genuine §20.3.1 rotation boundary (a dual-signed sibling pair sharing a rotation-correlation id, under two consecutive key periods) was present among the supplied entries. Any intact non-rotation ledger (even a single ordinary `StateLedgerEntry`) passed the step, so `execute_key_rotation` could report `rotation_complete=True`/`ROW_2_RETIRE_OLD` without rotation-specific evidence. This is now closed: `verify_rotation_6_steps` cannot pass on absent, tampered, or structural-only-unverified evidence.
- **Two residuals REGISTERED as separate forward dependencies, NOT part of B-33's own ratified scope** (the fork's own §3 explicitly defers both): (a) `execute_key_rotation`'s real ledger-write path + a real `rotation_window_entries` producer — `verify_rotation_6_steps` still has ZERO production callers; (b) the OD §21.2.2 rotation-period-aware cryptographic signature verifier — `find_rotation_pair_evidence` always returns `signatures_verified=False` today, so `PROBE_VERIFY_AT_READ` cannot reach a genuine pass through any real caller until this lands. Neither blocks B-33's own closure; both are candidate new B-* filings for a future arc when a real rotation-execution production surface is prioritized. **Council: no** for any future such arc unless a genuine cross-domain tension surfaces at that time.

### B-34 · Concrete signature-representation gaps in the C-CP-20 §20.2.1 seam *(surfaced at B-22 round 4, out-of-family Codex, `advisor()`-confirmed non-blocking)*
- **What it is.** Out-of-family Codex round 4 on `B-22`'s `SigningBackend` seam found 2 genuine representation-layer gaps (a 3rd candidate finding was empirically probed and confirmed a no-op — see below) — deliberately NOT fixed inline, per `advisor()`'s discriminator: **seam-contract findings** (injection, absent-path-preserved, metadata-bound-into-the-signature) get fixed in the seam itself; **concrete-signature-representation findings** (encoding, serialization, strict parse-typing) are exactly what C-CP-20 §20.4 defers to "implementation discretion / the concrete backend" and get registered against the still-deferred backend arc (`B-36`), since inline-hardening a backend that doesn't exist yet is scope creep.
  1. **Algorithm-specific signature encoding not enforced.** A real `cryptography`-library ECDSA-P256 backend's `sign()` returns a DER-encoded ~70-72-byte signature, but C-CP-20 §20.4 requires a canonical 64-byte raw r‖s encoding for `ecdsa-p256`. Neither `sign_audit_entry` nor `verify_audit_entry_signature` validates the returned/stored signature's byte length against the algorithm's expected length.
  2. **`CPSignedAuditLedgerEntry.audit_signature_value: bytes` is not JSON-serializable for real (non-UTF-8) signature bytes.** Pydantic's default `bytes` field serialization assumes UTF-8-decodable content; a genuine Ed25519/ECDSA/RSA-PSS signature's raw bytes are not, so `model_dump_json()` raises `PydanticSerializationError` once a signed entry needs to cross a JSON/JSONL persistence boundary. **Pre-existing on the shared `CPSignedAuditLedgerEntry` type** (U-CP-14, `per_step_override_evaluator.py`) — `B-22` did not introduce this field; fixing it (e.g., a base64 field-serializer) touches every other consumer of that type, out of this seam's blast radius.
  3. **(Probed, confirmed NOT a bug — recorded for completeness.)** Codex flagged that Pydantic coerces `key_period=True`/`1.0` to canonical `int(1)` when a `CPSignedAuditLedgerEntry` is built via `model_validate`, potentially defeating the round-3 strict-typing check. Empirically probed: `model_validate` coercion produces a value byte-identical to the honestly-signed record (no distinct forgery — `str(1) == str(1)`), and a genuinely type-distinct relabel via `model_copy(update=...)` (which bypasses validation) is already caught by the round-1 message-binding fix (mismatched `str()` representation → `SIGNATURE_MISMATCH`, verified directly). No code change needed.
- **Current state.** Non-blocking — the module still has zero production callers, `multi-tenant-compliance` is not this workspace's active persona tier, and no real backend (ECDSA-P256 or otherwise) exists yet to trigger either gap. Round-4 finding-count trend (3→2→2→4, drifting from seam-contract into representation-layer territory) was itself the convergence signal per `advisor()` — round 1's `SigningBackend.sign`/`verify` widened to also pass `key_period` (closing round-4's genuinely in-scope P1 finding: a rotation-aware backend was structurally unexpressible without it, since a single stable `key_id` cannot distinguish keys across a rotation boundary) is the one round-4 fix that landed inline; these 2 remain registered.
- **Close-out.** CLOSED 2026-07-16 (PR #1032). Both gaps fixed per this block's own prescribed steps, now that B-36 (ADR-D8, PR #1031) supplies a real backend: (1) `_SIGNATURE_LENGTH_BY_ALGORITHM` (`{ed25519: 64, ecdsa-p256: 64 raw r‖s, rsa-pss-2048: 256}`) enforced on the write path (fail-loud `ValueError` immediately after `backend.sign`) and the read path (`SIGNATURE_MISMATCH` before ever consulting the backend) — the `ecdsa-p256` row forecloses the DER-vs-raw ambiguity by contract (fixed-width raw only; a future DER-emitting backend fails loud at sign time); (2) reversible base64 field-serializer/validator on `CPSignedAuditLedgerEntry.audit_signature_value` — consumer sweep first confirmed zero serialization consumers of the shared type outside the two edited harness-cp modules; JSON round-trip is lossless and a persisted-then-reloaded signed entry still cryptographically VERIFIES (e2e witness). 5 new witnesses, each PD-8 mutation-probed. Original close-out steps (preserved): address both when a real backend (of any concrete kind) is actually wired: (1) validate `len(signature)` against `{ed25519: 64, ecdsa-p256: 64, rsa-pss-2048: 256}` for the declared algorithm, on both the write path (immediately after `backend.sign`) and the read path (before calling `backend.verify`); (2) add a reversible byte-encoding (base64) field-serializer to `CPSignedAuditLedgerEntry.audit_signature_value` — check ALL existing consumers of that shared type first (not just this module) before changing its serialization behavior. **Council: no** (mechanical once a real backend exists to test against).

### B-35 · `tools/test_qa_evidence_matrix.py` / `test_closure_gate.py` are orphaned from CI + 12 contracts lack test-file citation *(surfaced during B-28 triage, 2026-07-14; closed same session as B-32)*
- **What it is.** Two independent gaps discovered while triaging `B-28` finding #17/#18: (1) `qa_evidence_matrix.py --check` failed against live data, reporting 12 contracts with no test-file citing their contract id anywhere in the repo (`C-IS-14`, `C-MEM-01`, `C-MEM-04/05/06/11/12/13/14/15/17/19`); (2) neither `tools/test_qa_evidence_matrix.py` nor `tools/test_closure_gate.py` was wired into any CI job, so gap 1 sat silently red with zero CI signal.
- **Current state — CLOSED.** Grounded each of the 12 individually before touching anything:
  - **`C-IS-14` — tooling false-positive.** `Spec_Harness_Runtime_v1.md`'s v1.93 change-note confirms the IS spec defines only `C-IS-01..10` and explicitly names `C-IS-11/14/15` as confirmed-phantom ids from a prior spec version. `closure_gate.py` already had a local `DOCUMENTED_NON_CONTRACTS = {"C-IS-11"}` exclusion for exactly this reason but it only listed 1 of the 3 documented phantoms, and `qa_evidence_matrix.py` had no such exclusion at all. Moved the constant to a shared home (`tools/semantic_overlay/overlay.py::DOCUMENTED_NON_CONTRACTS = {"C-IS-11", "C-IS-14", "C-IS-15"}`, citing the same spec text) so both consumers read one source of truth instead of drifting independently.
  - **10 of 11 `C-MEM-*` contracts — citation-only.** Each already had a real, already-passing test exercising its behavior (several tests already embedded the contract id in their own function name in snake_case, e.g. `test_..._c_mem_17_...`, just never in the hyphenated `C-MEM-17` form the citation-scan regex needs). Added a one-line docstring citation to the nearest existing test for each: `C-MEM-04` (`test_memory_capture.py`), `C-MEM-05`/`C-MEM-06` (`test_memory_promotion_review.py`), `C-MEM-11`/`C-MEM-12` (`test_memory_retrieval.py`), `C-MEM-13` (`test_memory_access_mode.py`), `C-MEM-14` (`test_memory_tool_contracts.py`), `C-MEM-15` (`test_native_memory_adapter.py`), `C-MEM-17` (`test_memory_engine_durability.py`), `C-MEM-19` (`test_memory_capture.py`).
  - **`C-MEM-01` — genuine gap, now closed.** The memory-plane-boundary architectural contract (no dedicated source module by design — its own text: "atomic implementation sequencing is allowed; product completion requires the full contract family"). Added `harness-runtime/tests/test_memory_plane_boundary.py` with two new tests, both mutation-probed against real code: (1) native provider memory, standard tools, and prompt-packet fallback all write to the SAME physical canonical-store ledger — proven by instantiating each component independently at the same `MemoryRootBinding` and reading all three operation kinds back from a fifth, independently-constructed store (mutation-probe: pointing one component's store at a different root correctly drops its operation kind from the read-back set); (2) promotion and injection are independently-settable policy decisions — proven by promoting a record to `ACTIVE` while its `injection_policy` is simultaneously `NEVER` (mutation-probe: forcing production code to ignore the caller's `injection_policy` and always use a fixed value correctly fails the assertion). Also added a one-line `C-MEM-01` citation to `CanonicalMemoryStore`'s own docstring (the class that physically embodies the boundary invariant), which incidentally closed `closure_gate.py`'s separate G1.3 source-code-orphan check too (2 real → 0 real; all 5 automatable Tier-1 predicates now PASS, reconciling the tool with an already-RESOLVED R-CL-C1 certification, not a new governance decision).
  - **CI wiring.** New job `q3-evidence-and-closure-gate` in `.github/workflows/ci.yml`, mirroring the existing arc-ledger/substitution-ledger/semantic-overlay job pattern: runs both test modules' pytest suites + `qa_evidence_matrix.py --check` + `closure_gate.py --check`, all blocking. `test_qa_evidence_matrix.py`'s import was normalized from a `from tools import X` package-relative style (which only worked from repo root) to the same self-contained `sys.path.insert` convention every sibling `tools/test_*.py` file already uses, matching the `working-directory: tools` job shape.
  - The pinned fingerprint test (`test_q3_evidence_matrix_known_gaps_are_tracked_not_growing`) is simplified back to a direct `missing_ids == []` assertion now that the gap is empty — the elaborate SHA-256 machinery existed only to track a known-nonzero gap without letting it silently drift; with 0 missing, the honest assertion serves the same regression-catching purpose without carrying forward dead scaffolding.
- **Verification.** `just check` clean throughout (5796 passed, +2 for the new tests); `qa_evidence_matrix.py --check` → `q3 evidence OK contracts=141/141 cxa=33/33`; `closure_gate.py --check` → all automatable predicates PASS. **Council: no** (mechanical once each contract was grounded; no cross-domain tension).

### B-36 · Concrete F5 audit-signature backend selection *(split off `B-22` 2026-07-14, per operator direction — `B-22`'s seam is CLOSED, this is what remains; CLOSED 2026-07-16 via ADR-D8)*
- **What it is.** `B-22` built and closed the `SigningBackend` composition-root seam (C-CP-20 §20.2.1, PR #976) — the CP-side call surface can accept a real backend, and a TEST-ONLY in-memory Ed25519 double proves the round-trip. This row is what remained: no real backend existed.
- **Resolution (ADR-D8; real AWS KMS key provisioned, ARN not published — see `B36_KMS_KEY_ARN` in the operator's local `.env`).** The operator authorized a real AWS account for this decision. Two facts were verified before deciding: (1) AWS KMS added native Ed25519 support (`ECC_NIST_EDWARDS25519` key spec, `ED25519_SHA_512` algorithm) in November 2025 — this dissolved the assumed algorithm-vs-exposure tradeoff, since KMS can now produce ADR-D5 §1.4's own default algorithm while never exposing key material; (2) the account's ambient credentials resolved to IAM root, so a dedicated least-privilege IAM user (`arhugula-harness-cp-signing`) scoped to `kms:Sign`/`kms:Verify`/`kms:GetPublicKey`/`kms:DescribeKey` on the one provisioned key ARN was created first. A dyadic council (C10 blast-radius ⊥ C11 operator-loop) then decided the remaining live tension — whether the signing key should ever resolve into harness process memory at all, independent of algorithm. C10's grounding: `resolve_signing_key` already resolves its `SecretRef` at `SandboxTier.TIER_4_FULL_VM` (in-sandbox HTTP per AS spec §5.4, not a literal-string env-var read) and is gated exclusively to `MULTI_TENANT_COMPLIANCE` — the landed code already signals a no-exposure design intent, and the signing key's compromise blast radius (silent, retroactive invalidation of the *entire* ledger's tamper-evidence) is categorically worse than a typical secret's. C11 conceded the exposure argument but flagged that KMS moves AWS availability onto the per-signature write-time hot path — a real, separate cost for the eventual composition-root wiring arc to route through the existing C9 breaker discipline, not a reason to prefer local signing. Council ranked KMS-delegated signing over Secrets-Manager-plus-local-signing.
- **What shipped.** New `harness_cp.aws_kms_signing_backend.AwsKmsSigningBackend` implements the `SigningBackend` Protocol via `boto3` KMS `Sign`/`Verify` (`ED25519_SHA_512`, `MessageType=RAW`), constructed with an explicit `key_id → KMS key ARN` mapping supplied by the deployment-time composition root (an unmapped `key_id` fails loud, never silently signs under a default key). Zero change to `resolve_signing_key`/`sign_audit_entry`/`verify_audit_entry_signature` — the `§20.2.1` seam already accommodated this backend shape; `resolve_signing_key`'s `fetch_secret(...)` call still executes harmlessly (it is a pure `SecretRef` constructor per AS spec §5.1 that never resolves literal secret bytes) and remains unread by the KMS-backed signing path, exactly as it is today for the no-backend path. Unit-tested against a mocked KMS client (real `cryptography` Ed25519 crypto behind the mock, proving the request/response plumbing, not just call-recording); mutation-probed (the `_resolve_key_arn` fail-loud check reverted to a silent default-key fallback, confirmed the corresponding tests fail, restored). Live-e2e-proven twice against the real provisioned key (`harness-cp/tests/integration/test_b36_kms_signing_live_e2e.py`, `just b36-kms-signing-live-e2e`): real sign/verify round-trip, tamper rejection, wrong-message rejection, unmapped-`key_id` fail-loud, and confirmation the scoped IAM identity is denied S3 (`s3:ListAllMyBuckets`), IAM (`iam:ListUsers`), and any KMS action beyond the 4 granted even on its own key (`kms:GetKeyPolicy`).
- **Out-of-family Codex round-2 findings, all fixed same session.** (1) The initial landing's `key_arns` mapping accepted KMS aliases, which are mutable — a repointed alias would silently swap key material under an unchanged logical `key_id`; fixed by rejecting aliases at construction (`MutableKeyAliasRejectedError`). (2) The initial live-e2e least-privilege test called `iam.create_user(...)` directly — if the identity were ever misconfigured with broader permissions, that call would succeed and leave a persistent IAM user before the assertion could run; replaced with three read-only, not-granted-action calls (S3 list, IAM list, and `kms:GetKeyPolicy` on the identity's OWN key — the last proving action-level KMS scoping beyond the 4 granted actions, since the account has only the one KMS key and IAM policy simulation is itself not a granted permission on this identity). (3) `boto3>=1.34` didn't guarantee the botocore KMS service model included Ed25519 support; bisected empirically (`botocore==1.40.0` lacks `ED25519_SHA_512`, `1.41.0` has it) and bumped the floor to `boto3>=1.41`. (4) B-36's own `summary` field still read "no real signing backend exists yet" directly beneath `status: closed` — fixed. (5) Composition-root wiring was closed-over without a tracked follow-on — split out as `B-47`.
- **Scope carried forward.** Single-physical-key-per-logical-`key_id` proof only — multi-key fleet routing across distinct `WORKFLOW_BOUND`/`TENANT_BOUND`/`FLEET_BOUND` scopes is out of scope. `B-33` (rotation-boundary proof) now has a real backend to build the rotation-correlation carrier against — extended, not closed. `B-34` narrows: KMS Ed25519 signatures are always a fixed 64 raw bytes with no DER-wrapping ambiguity, so the DER-encoding half of that finding only remains live if a future ECDSA-P256 fallback backend is added alongside this one; the JSON-serializability half is unaffected by backend choice and still open at B-36's close (both B-34 halves subsequently CLOSED at PR 1032). `B-47` (NEW) carries the composition-root wiring gap Codex flagged. **Council: DONE** — C10 blast-radius ⊥ C11 operator-loop/deployment-surface, dyadic council 2026-07-16, ranked AWS KMS delegated signing first.

### B-47 · `AwsKmsSigningBackend` has no production composition-root call site *(surfaced at `B-36`'s own PR, out-of-family Codex review, 2026-07-16)*
- **What it is.** `B-36` (ADR-D8) selected + implemented `AwsKmsSigningBackend` and live-e2e-proved it against a real KMS key, but zero production code path constructs it and passes it as `sign_audit_entry(backend=...)` / `verify_audit_entry_signature(backend=...)`. It is reachable only from tests. A `MULTI_TENANT_COMPLIANCE` deployment cannot get real signed audit entries until a `harness_runtime` bootstrap composition-root arc wires this backend in.
- **Close-out steps.** Author a `harness_runtime` composition-root unit (mirrors the `ProviderSecretResolver`/`GcpSecretManagerResolver` precedent at R-421) that: (a) resolves a `key_id → KMS key ARN` mapping from `RuntimeConfig` at `MULTI_TENANT_COMPLIANCE` deployment-time; (b) constructs `AwsKmsSigningBackend` with a `boto3` KMS client sourced from the runtime's own credential-resolution chain; (c) threads it through to the real `sign_audit_entry`/`verify_audit_entry_signature` call sites once those exist in production code (currently zero callers — this unit may need to land together with whatever CP-axis arc first introduces a real audit-signing call site); (d) per `ADR-D8`'s own C11 concession, wires the C9 per-`{secret_backend, scope}` breaker onto the signing call itself, not just key resolution, since KMS availability now sits on the write-time hot path. **Council: conditional** — mechanical composition-root wiring once a real `sign_audit_entry` call site exists; revisit if the call-site design itself surfaces a cross-axis tension.
- **Progress (PR A, 2026-07-16).** Grounding found the REAL production audit-signing path is the OD `sign_audit_entry` (hash-only placeholder) invoked via `cp_audit_to_od_audit` at every runtime audit write — the CP-side B-22 seam has zero production callers, so a composition root had nothing to inject into. PR A landed the missing seam end-to-end short of the composition root itself — OD spec v1.33 NEW §21.2.1 (clearance filed) + `sign_audit_entry(..., backend=...)` (absent path preserved byte-identically, present path real signature over a metadata-bound canonical message, base64 value, per-algorithm length enforcement) + `cp_audit_to_od_audit(..., backend=...)` passthrough, all mutation-probed. **Remaining (PR B, the actual close):** the harness_runtime composition root per close-out items (a)-(d) — RuntimeConfig audit-signing surface, boto3-guarded factory (R-421 shape), threading `ctx` backend into the runtime converter call sites, and the C9 per-{secret_backend, scope} breaker on the signing call, **plus (e)** (out-of-family Codex round 3 on PR A, verified directly): `RuntimeAuditLedgerWriter.append` persists only the `action_id` wrap (`entry_hash`) — neither `payload` nor `signature_attrs` reach durable storage, so a real KMS signature would be produced then dropped at persistence (pre-existing shape; placeholder signatures were equally discarded). PR B must persist the signed entry or an equivalent durable sidecar before real signatures can be claimed durable — **landed at PR B1 (2026-07-16)** together with items (a)+(b) (AuditSigningConfig + boto3-guarded factory) and the token-map half of (c) (stage-4 construction + span-processor threading; full-chain stage→real-signature→sidecar witness). Remaining: rest of (c) composer threading, (d) breaker, (f). **And (f)** (Codex round 5 on PR A): the canonical signed message does not bind tenant identity (it enters only at the writer wrap), so under a DEPLOYMENT-scoped signing key a signed entry could be re-presented under another tenant; tenant-scoped key_ids already bind the tenant. Resolve jointly with (e) at PR B — payload-attr binding, a fifth message segment (spec delta), or tenant-scoped key_ids required at MTC. No live exposure at PR A (zero backends wired). **And (g)** (Codex round 24 on PR B1): the in-memory sidecar membership index grows with complete audit history; a bounded LRU cannot prove absence, so the fix is a disk-backed index — plain performance engineering within §21.2's committed Tier-5 durability CLASS (no storage engine is spec-committed; corrected 2026-07-17 per the disposition doc — the earlier SQLite-commitment phrasing was an over-reading). **And (h)** (Codex round 30): a tenant's sidecar entries interleave independent OD chain families (CXA §0.3 action-id prefixes discriminate); the PR-B2 verifier must verify per family — global re-sequencing at sign time would touch spec-committed converter prior-hash semantics (design-phase question). Reader docstring states the non-single-chain contract. **And (i)** (Codex round 32): redaction-chain tail-read + compose + append is not cross-process atomic — two processes can sign different entries against one predecessor (the sidecar lock serializes persistence only); in-process is serialized; the clean fix is widening the existing B-40 lock scope across tail-read+compose+append (no new primitive; no SQLite redesign is spec-forced — corrected 2026-07-17). In-code residual since round 13, formally registered. **PR B2a (2026-07-17):** rest of (c) + (d) landed — signing backend threaded into every remaining production audit composer (3× HITL composer, sub-agent dispatcher, 4 cost builders, `for_single_host`) with a real-bootstrap identity witness; C9 `BreakerGuardedSigningBackend` (epoch-guarded, fail-loud) wrapped at the factory; bootstrap key-id validation; dedicated daemon audit-offload executor with per-dispatch completion ack (no post-timeout audit commits); typed + loudly-surfaced signing failures (fail-closed policy = item (m)); append serialization with in-lock timestamps; item-(k) witnesses. Remaining cluster DISPOSITIONED 2026-07-17 at `.harness/b-47-pr-b2-design-disposition-v1.md`: (g)/(i) impl-discretion engineering, (h) the next buildable impl unit (per-family chain verifier), (f)+(m) the two genuine spec-delta forks (recommendations recorded), (j) rides the next runtime-spec delta; the earlier §21.2-SQLite-commitment phrasing was corrected (Tier-5 is the F2 state-ledger durability CLASS per ADR-D1 v1.2 — no engine committed). B-48 tracks the sync-inner-on-loop fork revision.

### B-49 · Per-family audit chain verifier *(split from B-47 disposition, 2026-07-17)*
- **What it is.** B-47 disposition item (h): a tenant's sidecar entries interleave independent OD entry families, so `verify_hash_chain_integrity` over the raw per-tenant sequence fails by construction — and most families do not chain at all (cost/HITL/dispatch deliberately emit genesis predecessors per entry); only the redaction-token family carries real chain positions, discriminated by its `audit.redaction_token.*` namespace keys (caller-supplied `entry_core` rows have no reliable prefix). Buildable now against cleared contracts.
- **Close-out steps.** Build `verify_per_family_chains(entries)` with a PER-PRODUCER verification policy — chain-verified = ONLY the redaction-token family, discriminated by `audit.redaction_token.*` namespace keys (prefix matching misses caller-supplied `entry_core` rows); per-entry CONTENT-HASH checks for cost (`cost_record_audit_writer.py:50`), HITL (`hitl_gate_composer.py:1235`), and sub-agent dispatch (`sub_agent_gate_level_descent.py:215`). Backend SIGNATURE verification is out of scope (§21.2.1 leaves it out; `verify_hash_chain_integrity` never touches `signature_attrs`) — a backend-aware verification API is spec surface for the fork legs. Witnessed with interleaved multi-family fixtures + mutation probes. **Council: no.**

### B-50 · Sidecar scale engineering *(split from B-47 disposition, 2026-07-17; CLOSED 2026-07-17 — item (i) PR #1042, item (g) PR #1044)*
- **What it is.** B-47 disposition items (g)+(i): the in-memory membership index refolds full history at restart (O(history) cold start); the redaction-chain tail-read+compose+append is not cross-process atomic. Both impl-discretion within Tier-5's committed properties — no spec fork (grounding corrected at `.harness/b-47-pr-b2-design-disposition-v1.md` §0).
- **Close-out steps.** Disk-backed index (stdlib `sqlite3` on the 3.12 baseline, or an offset-checkpointed snapshot); make the token map's tail-read+compose+append transactional — NOT a plain outer-lock hold (POSIX `flock` is non-reentrant across separately-opened descriptors; the inner reader/append reacquiring the sidecar lock would self-deadlock): unlocked inner variants under an outer transaction, or a lock-ownership API on the writer. POSIX-only (`_flock` is a win32 no-op; Windows closure rides B-45). **Council: no.**

### B-51 · Tenant-scope binding under deployment-scoped keys *(split from B-47 disposition, 2026-07-17)*
- **CLOSED 2026-07-21** — impl arc COMPLETE: U-OD-30 (#1061) + U-RT-137⊕U-CP-72 (#1066); five-segment tenant-bound signing live end-to-end.
- **RATIFIED 2026-07-18** — all ten gate items as recommended. **Spec-apply COMPLETE 2026-07-18** (OD v1.34 + CP v1.101 + Runtime v1.101 + plans v2.29/v2.38/v2.49 + clearance markers; three all-CONFIRM dyads at `.harness/council-dyads-b51-b52-b54-apply-2026-07-18.md`); row OPEN — impl arc next.
- **Fork doc FILED 2026-07-17** at `.harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md` — RATIFIED 2026-07-18; spec-apply COMPLETE (PR 1056); impl arc live.
- **What it is.** B-47 disposition item (f): the §21.2.1 canonical message binds entry-hash/key_id/algo/period but NOT tenant identity — under a deployment-scoped signing key a signed entry could be re-presented under another tenant.
- **Close-out steps.** Class 1 fork doc RATIFIED + spec/plan deltas APPLIED 2026-07-18 (PR 1056) — next step is the IMPLEMENTATION arc, not design work (context preserved from codex round-3: the fifth canonical-message segment is the PRIMARY fix — `key_arns` has no tenant association and composers use fixed global key_ids, so a key-scope presence check would be a false guarantee; NO effective interim mitigation exists — composers select fixed global key_ids, so per-tenant mapping entries would never be used; cross-tenant replay stays unmitigated until the segment lands — and the segment itself needs a tenant-bearing signing API, since `sign_audit_entry`/`cp_audit_to_od_audit` receive no tenant today and the writer sees `tenant_id` only post-signing); spec-apply COMPLETE — the impl arc (U-OD-30 + U-CP-72 + U-RT-137) is live. **Council: yes at apply leg** — C7 compliance vs C2 schema-minimalism dyad, REQUIRED by the ratified filing's final section (dyad 1).

### B-52 · Audit-signing fail-closed policy at MTC *(split from B-47 disposition, 2026-07-17)*
- **CLOSED 2026-07-21** — impl arc COMPLETE: U-RT-134 + U-RT-135 (#1064) + U-RT-136⊕U-CP-73 (#1065); fail-closed policy resolved-ON at MTC across all ten catch sites + CP carve-out.
- **RATIFIED 2026-07-18** — two-spec shape; explicit `false` invalid at MTC; redaction path unconditionally fail-closed; prewarm disabled at MTC under fail-closed. **Spec-apply COMPLETE 2026-07-18** (OD v1.34 + CP v1.101 + Runtime v1.101 + plans v2.29/v2.38/v2.49 + clearance markers; three all-CONFIRM dyads at `.harness/council-dyads-b51-b52-b54-apply-2026-07-18.md`); row OPEN — impl arc next.
- **Fork doc FILED 2026-07-17** at `.harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md` — RATIFIED 2026-07-18; spec-apply COMPLETE (PR 1056); impl arc live.
- **What it is.** B-47 disposition item (m): signing failures are typed + loudly surfaced (PR B2a) but dispatch stays fail-open — CP §28.10.4 invariant 2 is spec-committed, in tension with §21.2's signed-audit commitment at MULTI_TENANT_COMPLIANCE.
- **Close-out steps.** Class 1 fork doc RATIFIED + spec/plan deltas APPLIED 2026-07-18 (PR 1056) — next step is the IMPLEMENTATION arc (applied obligations: mirror OD v1.8 §C-OD-28.2's operator-configurable default-fail-closed pattern — `audit_signing_fail_closed` flag, default ON at MTC). The delta is TWO-SPEC (RATIFIED gate item 2; the weaker single-spec fail-open-hook alternative was REJECTED): §28.10.4 invariant 2 is CP-owned, so the validator-hook carve-out lands as a CP amendment alongside the OD addendum; the redaction path is UNCONDITIONALLY fail-closed and prewarm/keepalive is DISABLED at MTC under fail-closed (gate item 8); spec-apply COMPLETE — the impl arc (U-OD-30 §21.2.3 criteria + U-CP-73 + U-RT-134/135/136) is live. **Council: yes at apply leg** — C7 compliance vs C1/C9 reliability dyad, REQUIRED by the ratified filing's final section (dyad 2).

### B-53 · Migration-CLI scripts-inventory promotion *(split from B-47 disposition, 2026-07-17)*
- **CLOSED 2026-07-21** — impl arc COMPLETE: U-RT-139 record modes (#1069) + the U-RT-102 amendment (#1070, flat-namespace subcommand with §14.18.4 arg-parse remap).
- **RATIFIED 2026-07-18** — `harness migrate-audit-sidecar` subcommand rides the Runtime v1.101 rider (§13.4 row landed; count 5→6). **Spec-apply COMPLETE 2026-07-18** (OD v1.34 + CP v1.101 + Runtime v1.101 + plans v2.29/v2.38/v2.49 + clearance markers; three all-CONFIRM dyads at `.harness/council-dyads-b51-b52-b54-apply-2026-07-18.md`); row OPEN — impl arc next.
- **Fork FILED 2026-07-17** via `.harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md` (gate item 7) — rides the arc's Runtime v1.100 → v1.101 rider; ratified with the same gate (item 7) 2026-07-18 — the §13.4 row LANDED (PR 1056); impl arc (U-RT-102 dispatch + U-RT-139 record modes) live.
- **What it is.** B-47 disposition item (j): `python -m harness_runtime.admin.migrate_audit_sidecar` works today; promotion = a `harness migrate-audit-sidecar` subcommand under the existing flat `harness` console-script namespace (no new `[project.scripts]` entry — dispatch rides `harness_runtime.cli:main`), recorded as a runtime spec §13.4 inventory row. LOW urgency.
- **Close-out steps.** The §13.4 inventory row + clearance marker LANDED at Runtime v1.101 (PR 1056) — remaining work is the U-RT-102 (flat-namespace dispatch) + U-RT-139 (record modes: authoring, authentication, all-or-nothing retag, completeness refusal) IMPLEMENTATION (no pyproject change). **Council: no.**

### B-54 · Backend-aware audit signature verification API *(split from B-49 at the disposition leg, 2026-07-17)*
- **CLOSED 2026-07-21** — impl arc COMPLETE: U-OD-55 verifier + U-CP-44/45/42⊕U-RT-138 (#1067) + U-RT-139 live alias wiring (#1069); CXA §2.3.9 seam R-live (#1068).
- **RATIFIED 2026-07-18** — §21.2.2 API with all contract requirements (resolver, tenant input, cutover, disposition triples, taxonomy, UNVERIFIED-at-MTC), non-blocking default. **Spec-apply COMPLETE 2026-07-18** (OD v1.34 + CP v1.101 + Runtime v1.101 + plans v2.29/v2.38/v2.49 + clearance markers; three all-CONFIRM dyads at `.harness/council-dyads-b51-b52-b54-apply-2026-07-18.md`); row OPEN — impl arc next.
- **Fork doc FILED 2026-07-17** at `.harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md` — RATIFIED 2026-07-18; spec-apply COMPLETE (PR 1056); impl arc live.
- **What it is.** `verify_hash_chain_integrity` checks only unkeyed content hashes + links — recomputable by a ledger-rewriting attacker. The production `SigningBackend.verify` path (real KMS verification of persisted `signature_attrs`) has NO consumer, and §21.2.1 explicitly leaves verification out of scope, so adding the API is spec surface. Without it, MTC tamper-evidence stops at hash-chain level.
- **Close-out steps.** Verification contract SPECIFIED at OD v1.34 §21.2.2 + CP v1.101 §3 (spec-apply COMPLETE, PR 1056) — next step is the IMPLEMENTATION arc: implement U-OD-55 + wire into B-49's per-family unit and `harness-inspect` per U-RT-138. **Council: yes at apply leg** — C7 compliance vs C9 availability dyad; the blocking condition is MET by ratified gate item 5 (§20.3.1 reconciled into the CP rider).

### B-55 · Lower-tier fail-closed prewarm/keepalive posture *(registered at the B-51/B-52/B-54 apply arc, 2026-07-18)*
- **CLOSED 2026-07-21 (operator ratification via batched AUQ)** — MTC-only scoping ratified AS-IS: the gate-item-8 letter (`mtc_audit_prewarm_disabled` = MTC ∧ resolved-fail-closed) is the permanent contract; a lower-tier explicit `fail_closed=true` keeps the v1.99 prewarm/keepalive posture. No code change.
- **What it is.** Gate item 8 ratified the prewarm/keepalive disable at MTC ONLY; an EXPLICIT `audit_signing_fail_closed=true` at solo-developer/team-binding would leave the prewarm/keepalive swallow boundary active under a resolved-ON policy (a post-paid-call signing failure logged as `FAILED`, not raised). Extending the disable was not ratified; propagation breaks the committed v1.99 bootstrap-isolation contract. **INTERIM (Runtime v1.101):** lower-tier explicit `true` is ACCEPTED and honored at every catch-site surface (the ratified non-MTC opt-in); ONLY the prewarm/keepalive swallow boundary keeps its v1.99 posture there until this row dispositions.
- **Close-out steps.** Operator selects: (a) extend the disable to any tier with the resolved flag ON (one-line Runtime delta + U-RT-135 widening — RECOMMENDED: the same integrity-over-latency tradeoff already ratified at MTC, triggered only by the operator's own explicit opt-in), (b) define typed propagation through the prewarm boundary (amends the v1.99 bootstrap-isolation contract), or (c) ratify the interim rejection as permanent (spec note only). **Council: no** — a bounded three-option selection; C7/C9 positions recorded at apply-arc dyad 2.

### B-56 · Corrected-relabel repair for already-tagged audit history *(registered at the B-51/B-52/B-54 apply arc, 2026-07-18)*
- **What it is.** An already-tagged sidecar row whose mutable `source_tag` differs from the operator-attested `tenant_scope` — the relabel scenario the OD v1.34 §21.2.2 row-5 trusted-source rule detects. The ratified arc authorizes retagging ONLY `source_tag="_single"` rows (codex rounds 34/42), so the cross-tenant repair is NOT buildable without its own spec delta. **Current posture:** record validation REJECTS the mismatch typed and surfaces it for operator investigation — never silently repaired, never silently exempted.
- **Close-out steps.** If operator demand materializes: a narrow OD delta authorizing the corrected-relabel retag (record-attested mismatches only; generalized alias; the same atomicity/completeness discipline) + U-RT-139 widening. Until then the typed rejection is the complete posture. **Council: no.**
- **UPDATE 2026-07-27 (grounding pass, re-verified at HEAD `25b44127`) — STILL DORMANT; all four load-bearing claims hold.** (1) OD v1.34 §21.2.2 row-5 text unchanged. (2) The retag path still copies already-tagged rows through with their **tag, nested entry content, and `entry_hash` unchanged** (`record_migration.py:516-519` — `if tag != _SINGLE_TAG:` → count, `new_lines.append(json.dumps(row))`, `continue`). Not *byte*-unchanged, precisely (out-of-family codex [P2] at this pass): the row is re-serialized by a default-separator `json.dumps`, whereas original sidecar lines are written compact (`audit_writer.py:754-761`, `separators=(",", ":")`), so raw line whitespace may differ on a rewrite — semantically inert for every identity this row depends on. (3) The typed rejections are intact at `audit_cutover_record.py:201-214` — whose message names **`B-56` verbatim** at `:212` as the carrier of "the future explicit-relabel mechanism" — and at `record_migration.py:733-739` + `:762-767` ("a relabeled wrapper; refusing"). (4) The generalized `(source_tag, entry_hash)` alias remains **absent** at `per_family_audit_verification.py:287-312`: resolution still matches only `source_tag == normalized_scope` (`:296`) or the `"_single"` tier (`:310`). No commit since 2026-07-25 touches any OD/admin surface.
- **NEW this pass — a direct negative-path test witness (incidental test-hygiene gap, NOT a defect).** Every pre-existing row-level test in `harness-od/tests/test_audit_cutover_record.py` constructs `source_tag == tenant_scope` or the `"_single"` migration default, so the cross-tenant-mismatch branch at `audit_cutover_record.py:201` — the raise that names this row — had **no direct witness**, even though its runtime-layer siblings in `record_migration` are witnessed via `match="relabeled wrapper"`. Added `test_record_rejects_cross_tenant_relabel_of_already_tagged_row`, asserting `match="cross-tenant relabel"`. NOTE: the surfaced exception type is Pydantic's `ValidationError`, **not** `CutoverRecordValidationError` directly — that raise happens inside a `@model_validator(mode="after")`, which wraps it (the same wrapping every other construction-rejection test in that file already relies on; empirically confirmed at HEAD, not assumed). Mutation-probe verified: neutralizing the `:201` guard makes exactly this test fail ("DID NOT RAISE") and leaves the other 16 green. **Posture unchanged — the typed rejection remains the complete posture. Council: no.**
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT and every cited line still resolves EXACTLY (no drift); disposition VERIFIED-DORMANT.** The retag passthrough still short-circuits non-`_single` tags at `record_migration.py:516`; the typed cross-tenant rejection stands at `audit_cutover_record.py:208` and names **B-56 verbatim** at `:212`; and the generalized `(source_tag, entry_hash)` alias remains **absent** at `per_family_audit_verification.py:296`, which still matches only `row.source_tag == normalized_scope`, with the `"_single"` tier at `:310`. Wakes only on operator demand for the corrected-relabel retag.

### B-57 · Direct-writer caller-supplied timestamp capture-order inversion *(registered at the B-48 apply arc, 2026-07-19)*
- **What it is.** The C-IS-07 §7.6 writer-owned fix covers the buffered/branch-DRAIN path only (the ratified narrow scope). DIRECT appends keep caller-supplied timestamps with detect-then-refuse semantics — two uncoordinated concurrent direct writers can still capture outside the lock in inverted order and hit `NonMonotonicTimestampError`. The superseded defect record §8.3 phrased the fix this broadly; the filing did not ratify it (and, per the rounds-28/29 carve-out, the OFFLOADED sub-agent audit appends belong to B-48's writer-owned scope — NOT this residual).
- **Close-out steps.** If the direct-path inversion materializes in production: a narrow IS delta extending §7.6's writer-owned sampling to an opt-in direct-append mode + U-IS-11 widening. Until then detect-then-refuse is the committed posture. **Council: no.**
- **UPDATE 2026-07-27 (grounding pass, re-verified at HEAD `25b44127`) — STILL DORMANT; premise fully intact.** `_CLOCK_SKEW_TOLERANCE = timedelta(0)` (`state_ledger_write.py:69`) is unchanged; the refusal still fires at `:321-324`; writer-owned sampling remains strictly **opt-in via the `WRITER_OWNED_TIMESTAMP` sentinel** (`:316-320` — "Every other timestamp value is caller-supplied verbatim"), so the DIRECT paths keep caller-supplied semantics exactly as this row describes. The named production direct-writer sites all re-verified present and still capturing caller-side, outside the lock: `shadow_git_rollback.py:141`, `pause_resume_protocol.py:1103`/`:1190`/`:1294`, `workflow_driver.py:6038`/`:6261`, `hitl_gate_composer.py:1566`, `cost_attribution_f2_write.py:157`, `as_is_wiring.py:129`, `audit_writer.py:701`. (A `workflow_driver.py:10550` cite was dropped from this enumeration at this pass — out-of-family codex [P2]: `_append_buffered_sequential_entry` takes a `BufferingLedgerWriter` at every one of its three call sites (`:10815`, `:12214`, `:12244`), and `drain_branch_buffers` re-stamps those buffered payloads with `WRITER_OWNED_TIMESTAMP`, so the site belongs to B-48's closed DRAIN half, not this DIRECT residual. The two retained `workflow_driver` cites are genuine — both append straight to `ctx.ledger_writer` at `:6046`/`:6269`.) (No exact total is pinned: a strict count is sensitive to what qualifies as a state-ledger direct append versus any `timestamp=<clock>` construction — a broad sweep of `harness-*/src` returns ~21 such sites.) **No new direct writer was introduced by the B-72/B-78/B-79/B-80 arcs.**
- **Nuance worth recording — asymmetric caller-side recovery already exists on ONE half of this row's own named interleave.** `audit_writer.py:727-752` carries a **bounded 5-attempt resample-retry** (`attempts_left = 5`) recovering from exactly this refusal on the **AUDIT** half of the audit/cost interleave: on `NonMonotonicTimestampError` it resamples a fresh timestamp and retries the IS append, exhausting loudly rather than silently (its own comment explains why that is safe — the OD entry and sidecar identity are timestamp-independent and the idempotency key is unchanged). That is caller-side recovery layered ATOP detect-then-refuse, fully consistent with the committed posture rather than a departure from it. The **COST** half (`cost_attribution_f2_write.py`) and every other direct writer have **no** such recovery, so the row's failure mode stays live-but-unexercised for them. **Posture unchanged. Council: no.**
- **Adjacent, and explicitly NOT evidence about this row's premise.** The stale-carry comment block at `workflow_driver.py` ~`:6752-6784` still described the drain path's timestamp as captured OUTSIDE `_WRITE_LOCK`, called the resulting inversion "a known gap ... the clean fix is timestamp-authority INSIDE `_WRITE_LOCK`", and labelled `test_concurrent_sibling_drains_invert_timestamp` "(xfail, strict)" — all three false at HEAD since the B-48 apply arc. That is CP **documentation decay**, corrected in this same PR (comment-only, zero behavior change). B-48 closed the **DRAIN** path; B-57 is the **DIRECT-writer** residual — the two must not be conflated.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT; disposition VERIFIED-DORMANT.** `state_ledger_write.py` is untouched since the last re-check and the load-bearing lines were read directly: `_CLOCK_SKEW_TOLERANCE = timedelta(0)` at `:69` (exact cite holds), the `NonMonotonicTimestampError` refusal at `:321-322`, and writer-owned sampling strictly **opt-in via the sentinel** at `:318` (`if entry_payload.timestamp == WRITER_OWNED_TIMESTAMP`), with `WRITER_OWNED_TIMESTAMP` defined at `:83`. DIRECT appends therefore keep caller-supplied semantics exactly as registered; detect-then-refuse remains the committed posture. *Not re-walked this pass: the ~10 enumerated direct-writer call sites individually — the refusal mechanism and the opt-in gate are the load-bearing claims and both hold.*

- **2026-08-06 — disposition moves from VERIFIED-DORMANT to PARTIALLY MET** *(evidence from the `B-93` build leg — `B-112`, PR #1241)*. This row's close-out is conditioned on the direct-path inversion *"materializ[ing] in practice"*. `B-112` records a **mechanism that raises its probability**, not an observed production failure: the `B-93` liveness deadline replaced a blocking `flock` with a **polling** retry, which does not preserve the kernel's handoff fairness, so a direct writer whose timestamp was sampled *before* it queued can now be overtaken and refused — reproduced **12 of 20** runs by out-of-family review. **The deferral still holds and the prescribed route is unchanged** (narrow IS spec delta extending §7.6's writer-owned sampling to an opt-in direct-append mode, or per-writer serialization guidance, + U-IS-11 widening); what changed is the **probability**, not the remedy. `B-112` is the cause-and-evidence record and **defers the adoption question here** — this row remains the **remedy owner**. One data point for pricing that route: `test_shadow_git_rollback.py`'s concurrent-append worker now opts into the sentinel on a **direct** append surface — adjudicated at the `B-93` leg as permitted (§7.6 constrains the **contract** for every existing direct producer; a single explicit opt-in changes no producer's semantics and sets no default) — the first direct-surface use of the sentinel in the tree.

- **2026-08-07 — grounding input for the pending `B-57`/`B-112` fork filing** *(recorded here, **not** acted on — the fork is **not** filed at this PR)*. **The 2026-07-27 enumeration on this row under-counts the direct writers by four.** Four production sites construct a state-ledger entry with a **caller-sampled** timestamp and appear in **no** prior enumeration here, each re-resolved **by content** at HEAD: `workload_binding_engine_class_selection.py:337` (`timestamp=datetime.now(UTC)`); `hitl_as_tool_call_rewriting.py:290` (same); `per_step_override_evaluator.py:489` (a `timestamp` **parameter**, sampled by its caller at `:523` as `datetime.now(UTC)`); `sibling_ledger_entry_composition.py:163` (a `timestamp` parameter on the sibling-composition path) — all four under `harness-cp/src/harness_cp/`. **Two existing cites have drifted** and are re-stamped: the two genuine `workflow_driver` direct appends now sit at `:6074` and `:6297` (this row previously cited `:6038` / `:6261`); the **substance** is unchanged — both are still `timestamp=datetime.now(UTC)` on a `ctx.ledger_writer` append. **This does not change the row's disposition** — still PARTIALLY MET, still deferred, remedy unchanged — it changes the **size of the surface** the prescribed route must price, which is grounding the fork filing owes.
- **2026-08-07 — CLASS 2 FORK FILED** at `.harness/class_2_fork_b57_direct_append_writer_owned_opt_in.md` *(doc-only; this row's status is **UNCHANGED** at `registered_finding` and its `pr:` pointer is **NOT** set here — both ride the ratification leg, per the `B-88` precedent)*. This row is the **remedy owner**; the filing prices the route this close-out already prescribes. **Readings: A (recommended)** — a narrow IS delta authorizing **per-call-site** opt-in to `WRITER_OWNED_TIMESTAMP` on direct surfaces, default unchanged for every non-electing producer, extending the EXISTING C-IS-07 §7.6 (**zero** contract numbers, hash impact, CXA rows, new plan units) + a U-IS-11 AC widening + a per-site classification table; **B** — HOLD until an observed production refusal. **The single operator gate:** whether authorizing a per-call-site opt-in to an **already-unconditional** writer-side mechanism (the sentinel check at `state_ledger_write.py:316-320` fires for ANY caller that stamps it — no surface discriminator exists, so the direct-path restriction is held by **prose alone**) is a spec act or needs ratification first. **Council: NOT owed — probe-resolved** (`B-112`'s step-(3) `_CLOCK_SKEW_TOLERANCE` trigger is not reached: the classification leaves no exposed caller stranded, so the C3 ⊥ C11 tension does not arise). **Three grounding corrections recorded rather than absorbed:** (i) `sibling_ledger_entry_composition.py:163` is **production-UNREACHABLE** — `emit_sibling_ledger_entry` (`harness-runtime/src/harness_runtime/lifecycle/cp_is_wiring.py:124`) has **zero non-test callers**, so the exposed direct-writer surface is **11 of 12 sites**, not 12; (ii) `audit_writer.py:701`/`:751` and `cost_attribution_f2_write.py:157` sample an **injected** `time_source`, not `datetime.now(UTC)` at the site, so electing there **overrides the test-determinism injection seam** — a real per-site cost the spec leg must resolve explicitly; (iii) `_CLOCK_SKEW_TOLERANCE`'s own comment (`state_ledger_write.py:67-68`) calls it *"configuration-supplied"* while it is a bare module constant with no configuration path at HEAD — a pre-existing shipped-comment defect. **Also recorded:** the 2026-07-27 `audit_writer` resample-retry nuance on this row is **attributed, not re-discovered**; the filing disagrees only about its **weight** (this row reads it as consistent with the committed posture; the filing reads it as circumstantial materialization evidence, because the mitigation samples outside the lock and the interleave's cost half has none) and marks that ground **CONTESTED**.

- **2026-08-07 — RATIFIED (Reading A) and APPLIED; status `registered_finding` → `open`.** The operator selected **Reading A** — per-call-site opt-in to `WRITER_OWNED_TIMESTAMP` on DIRECT append surfaces, default unchanged for every non-electing producer (Reading B, HOLD, declined); stamped at the filing's new §11. **The route this close-out has prescribed since 2026-07-19 is now applied, not merely prescribed:** IS spec **v1.12 → v1.13** extends the EXISTING C-IS-07 §7.6 with a NEW **§7.6.1** (the election; the **eligibility rule** — elect only where the timestamp means *when the entry was appended*, never where it means *when the event happened*; a **negatively-demonstrable** default-preservation property; an explicit does-NOT-authorize list), plus ONE residual-status paragraph recording the §7.6 residual as **DISCHARGED** (the `"Surfaces that do NOT change"` and `Registered residual` paragraphs themselves **PRESERVED VERBATIM**), plus ONE **cross-reference-only** clause at the §7.1 row-7 cell. IS plan **v2.8 → v2.9** amends **U-IS-11** with ACs **#14–#20** (plus **#14-bis**) and carries the **per-site classification table** the contract routes to the plan. **ZERO** contract numbers / hash / canonicalization / migration / CXA rows / new plan units, nodes, edges or auxiliary types; **ZERO code** at this leg. **Why `open` and not `closed`:** the decision is taken and the design substrate has landed, so this is no longer a mere surfaced finding — but the implementation is owed, so it is not closed (the same enum reading `B-96` / `B-88` applied when a ratified finding's spec-and-plan legs land ahead of its impl leg). **B-57 closes at the impl leg.**
- **What the impl leg owes** (from IS plan v2.9 U-IS-11 ACs #14–#20 + #14-bis): the **10 ELECT** conversions; the **sentinel-docstring refresh** at `state_ledger_write.py:78`–`:80` (the OTHER prose carrier of the restriction §7.6.1 replaces); the **2 injection-caveat** sites (`audit_writer`, `cost_attribution_f2_write`) resolved **explicitly per site with rationale recorded**; the `audit_writer` **resample-retry disposition** (the loop becomes unreachable at that site under ELECT — take the simplification deliberately, or record the retention); the `shadow_git_rollback` `restored_at` **decoupling pin**; a **two-process contention witness** under a PD-8 mutation probe; a **NON-ELECTING negative witness** proving the default did not move; an `as_is_wiring` **event-time control**; and a **per-site table conformance** check. **Council: still NOT owed — probe-resolved and RE-DETERMINED at the spec leg** (`B-112` step (3) is not reached: the one RETAIN site, `as_is_wiring.py:129`, carries genuine event time and is not an exposed caller that *cannot* adopt). **A LIVE TRIGGER SURVIVES:** a **RETAIN** verdict on either injection-caveat site is an exposed caller of exactly the shape step (3) tests for, and the C3 ⊥ C11 determination MUST be re-run before any `_CLOCK_SKEW_TOLERANCE` change is authored (recorded at IS plan v2.9 §0.6).
- **Two of the filing's own COUNT claims corrected at the spec leg, not carried** (`[[count-claims-drift-every-review-round]]`; all fourteen classification rows re-resolved by direct read at HEAD `acfc1afa`, and the `emit_sibling_ledger_entry` zero-non-test-caller sweep re-run independently — 19 hits, exactly 3 non-test). (i) *"Fourteen rows, twelve SITES"* does not reconcile with a table whose two folded companions (`audit_writer:751`, `per_step_override_evaluator:523`) are **already** folded into rows 12 and 9 — by content the table is **14 rows**. (ii) *"~8 conversions"* under-counts its own ELECT cell, which enumerates **TEN** payload-construction rows (**10 ELECT + 2 injection-caveat + 1 RETAIN + 1 DEFER = 14**). Neither changes a disposition, a reading, or any zero-cost line — both change the **size of the surface** the impl leg must convert. The three earlier grounding corrections (the UNREACHABLE sibling site, the injected `time_source` seam, the stale `_CLOCK_SKEW_TOLERANCE` comment) all re-verified **INTACT** at `acfc1afa`.

- **CLOSED at the U-IS-11 impl leg, PR #1256** *(this closure bullet was OWED since that merge — the yaml row was stamped `closed`/`pr: #1256` at the #1263 rider bundle while this prose surface still ended at "What the impl leg owes"; gap named at that bundle's close and discharged here)*. The impl leg landed the full ACs #14–#20 + #14-bis surface: the **12 C-IS-07 §7.6.1 elections** (the 10 ELECT conversions plus BOTH injection-caveat sites resolved ELECT with per-site rationale recorded — so the §0.6 LIVE TRIGGER, a RETAIN verdict on an injection-caveat site, did NOT fire and no `_CLOCK_SKEW_TOLERANCE` re-determination was owed), the sentinel-docstring refresh at `state_ledger_write.py`, the `audit_writer` resample-retry loop removal (unreachable under ELECT — the simplification taken deliberately), the `shadow_git_rollback` `restored_at` decoupling pin, and the two-process contention witness. **`B-112` closed at the same leg** on its own prescribed two-process-witness evidence.

### B-58 · Pre-existing harness-cp function-level import of runtime escalation error types *(registered at the B-48 apply arc, 2026-07-19)*
- **What it is.** `workflow_driver.py:4737` imports three `ValidatorEscalation*` exception classes from `harness_runtime.lifecycle.validator_escalation_composer` at function level — a pre-existing cross-package import (the carrier-home-defect class). Present at the merge base; the B-48 capacity-seam package-graph witness EXCLUDES it explicitly.
- **Close-out steps.** If a hygiene arc opens: re-home the three classes to `harness-core` (the U-CORE-03 precedent) and drop the import. Nothing blocks on it. **Council: no.**

- **CLOSED 2026-07-21 (PR #1074)** — the three `ValidatorEscalationGate*Error` carriers re-homed to `harness_core.validator_escalation_errors` (docstrings + RT-FAIL mappings verbatim; spec lens byte-checked §14.15.5/§14.8); the composer re-exports the SAME objects (`__all__` unchanged, identity-witnessed + PD-8 probed); the driver's lazy cross-package import narrowed to the composer FUNCTION only (source-hygiene witnessed + probed); drain-test timeout-symbol guard allowlists the deadline carrier with rationale. Codex clean round 1; merge-gate 3-lens all-APPROVE (X-AL-3 judged implementation discretion — pre-existing types, home-only move, close_out pre-authorized). Follow-on noted, not built: a REJECT-path raise-through-driver e2e (pre-existing coverage gap, test-witness lens). Process note: the final docstring-nit commit merged while its own PR checks were pending — main CI at the merge commit verified green immediately after (no consequence; logged for honesty).

### B-59 · Capacity authority not preserved across sequential bootstrap invocations *(registered at the B-48 apply arc, 2026-07-19; CLOSED 2026-07-22)*
- **FORK FILED 2026-07-21** — `.harness/class_2_fork_b59_capacity_authority_across_bootstraps.md` (Class 2: process-scoped singleton vs drain barrier vs accept-residual; rec A with the every-admission-surface rider). **RATIFIED 2026-07-21 (option A)** — apply arc is forward work.
- **What it is.** Registered at codex round-4 [P1] "preserve one capacity authority across bootstrap runs": if `api.run()`'s shutdown times out while a daemon dispatch worker is still draining (the documented abandoned-worker-keeps-its-lease-until-fence-ack behavior), the process releases its lock and permits another `api.run()` invocation — but the NEXT bootstrap constructs a FRESH `SubAgentDispatchExecutor` with the full configured frame budget. The old, still-draining worker's occupied frame is invisible to the new executor's accounting, so the process can transiently exceed the single-shared-cap promise across the boundary. The B-48 fork's own §4 verification obligations scope entirely to a SINGLE dispatch/workflow-run's offload mechanics — multiple SEQUENTIAL bootstrap invocations sharing one OS process is a genuinely new, unspecified surface the fork never anticipated.
- **Close-out steps (historical — resolved below).** A real fix needs one of two architectural choices the fork never ratified: (a) a process-scoped capacity-authority singleton that survives across `api.run()` invocations (surviving drain state, not just the frame count), or (b) block a NEW bootstrap from starting until the PRIOR executor has fully drained (a stronger cross-invocation barrier than today's per-run shutdown timeout). Route through Class 2 fork back-flow before building — this is an architectural decision on multi-bootstrap-per-process semantics, not a bounded bug fix. **Council: yes if opened** — C1 orchestration (process lifecycle) vs C9 reliability (capacity invariant integrity) is a nameable tension.
- **CLOSED 2026-07-22 (PR #1080 spec + PR #1081 impl)** — ratified option A (process-scoped capacity-authority singleton). Spec leg: CP v1.103→v1.104 (§25.11 admission-guarantee span clarification) + Runtime v1.103→v1.104 (NEW §14.8.10.6 cross-bootstrap capacity-authority continuity + `RT-FAIL-CAPACITY-AUTHORITY-BUDGET-SHRINK` taxonomy row) + Runtime plan v2.52 (NEW U-RT-146). Impl leg: `FrameLedger` split out of `SubAgentDispatchExecutor`, adopted process-lifetime via `adopt_or_create_process_capacity_ledger()` at `stage_5_loop_init.py`; typed `CapacityAuthorityBudgetShrinkError`; 14 PD-8 mutation-probed tests. Merge-gate test-witness BLOCK r1 (atomicity backstop was a substring count, not a nesting check) fixed via AST-walk assertion + 2 new ctor guard tests, re-verified APPROVE r2.

### B-60 · Cancellation fence not checked at nonlinear-topology child entry points *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*
- **What it is.** Registered at codex round-10 [P1] "check cancellation before nonlinear topology dispatch": when an offloaded child workflow uses PARALLELIZATION, ORCHESTRATOR_WORKERS, or HIERARCHICAL_DELEGATION, `_execute_workflow_body` returns into the topology-strategy handler before ever reaching the linear step loop's `_dispatch_cancel_token.check()` (`workflow_driver.py:4357-4359`). If an ancestor timeout trips the token between fan-out phases (e.g. between warm-up Phase 1 and Phase 2, or between a HIERARCHICAL_DELEGATION recursion level), the child can continue scheduling later work past the fence grace at those branch/level boundaries — the linear-path check this arc added does not extend to the nonlinear dispatch entry points. Not a regression introduced by round 9/10's admission-deferral or future-publication fixes; a pre-existing coverage gap in the round 4-8 cancellation-fence work, surfaced for the first time at round 10.
- **Close-out steps.** Needs equivalent `_dispatch_cancel_token.check()` calls at each nonlinear-topology strategy's entry point (before `_execute_parallelization` / `_execute_orchestrator_workers` / the HIERARCHICAL_DELEGATION recursion re-entry begins dispatching) and likely at each fan-out phase boundary (post warm-up Phase 1, pre Phase 2) — not just once at function entry, since the fence can trip mid-fan-out. Ground against CP spec v1.102 / Runtime spec v1.102 §14.8.10.3's fence-ack contract before building: determine whether "check once at strategy entry" or "check at every phase boundary" is the spec-intended granularity. **Council: yes if opened** — C1 orchestration (fan-out phase structure) vs C9 reliability (fence completeness) is a nameable tension.

- **CLOSED 2026-07-21 (PR #1075)** — fence consults landed at EVERY nonlinear boundary per the §14.8.10.3 grounding (strategy entry; warm-up Phase1→Phase2 at both tiers × both strategies with admission-release-on-raise; orchestrator marker→dispatch + orchestrator→worker boundaries; per-branch pre/post-marker in the cancel coroutines; guarded pre-admission consults in the proceed coroutines; EO per-dispatch; decentralized per-stage; synthesis entry + dispatch boundary) PLUS the no-fold guarantees (gather-result scans incl. group-wrapped, BaseExceptionGroup unwraps at the cascade arms and Phase-0 loops — a fence trip can no longer mint a resumable PAUSED or fold to PARTIAL). 10 codex rounds (terminal rule pre-committed at r9, honored at r10) + a 2-round merge-gate: round-1 test-witness BLOCK empirically overturned the per-site PD-8 claim (10/16 sites shadowed by defense-in-depth) and its minimal closure set was implemented + mutation-verified (depth-2 group unwrap, OW worker intra-phase, OW pre-tripped entry, all-rejected joint-pin with an honesty note); round-2 all-APPROVE with the lens re-running every mutation itself incl. the non-vacuity proof of the joint pinning. Residual registered as B-66. 23 b60 witnesses; CP suite 1704 green.

### B-61 · Buffered fan-out ledger drain writes are not fenced *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*
- **What it is.** Registered at codex round-10 [P1] "fence each buffered ledger append": `drain_branch_buffers`'s real `writer.append(...)` calls (`workflow_driver.py:1922-1924`) run without `effect_entry()` or `check()`. When a timed-out offloaded child is inside a nested fan-out, its cancel token can trip while branch tasks are still finishing; `_finish()` still reaches these physical ledger appends afterward, uncounted by `_effects_in_flight` and unblocked by the fence — so `wait_ack()` can classify the timeout as `ACKED_CLEAN` even though F2 ledger entries actually begin writing after the trip. Not a regression introduced by round 9/10's admission-deferral or future-publication fixes; a pre-existing coverage gap in the round 4-8 cancellation-fence work (the `effect_entry()` guard was applied to the direct dispatch write paths, not the buffered-writer drain path), surfaced for the first time at round 10.
- **Close-out steps.** Needs the same `effect_entry()`/`check()` guard `drain_branch_buffers`'s sibling direct writes already carry (per the round 6/7 fixes), applied at each real `writer.append()` inside the drain loop. Ground against whether drain time is single-threaded-post-barrier (making a coarser single guard around the whole drain loop sufficient) or whether per-append granularity is required — resolves the discriminator B-62 below shares. **Council: no** — narrow, single-site guard-application fix once the drain-loop threading model is confirmed.

- **CLOSED 2026-07-21 (PR #1072)** — every physical `real_writer.append` in `drain_branch_buffers` now enters its OWN `effect_entry()` from the ambient token (per-append granularity, resolved per the shared discriminator: the fence contract is per-effect, `append_ledger_entry` dedups by idempotency_key so a partial drain is resume-safe, and drain time is single-threaded post-barrier). Witnesses: mid-drain trip → 1 append + AMBIGUOUS ack + signal; no-token byte-unchanged; pre-tripped → zero appends (all PD-8 probed). Codex clean round 1; merge-gate 3-lens all-APPROVE (concurrency lens swept all 11 drain call sites — no stranded state/leases). Residual noted, not built: an e2e drain-trip transit witness through a real fan-out topology (test-witness lens G2, scope-arguable — the propagation machinery is pre-existing B-48 job-boundary behavior).

### B-62 · A single effect_entry() guard spans multiple independent sequential writes *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*
- **What it is.** Registered at codex round-10 [P1] "split the fence guard between sequential writes": at `sub_agent_dispatch.py:744-750` (and the durable-output/ledger pair in `_execute_workflow_body`), one `effect_entry()` guard wraps a SEQUENCE of independent effects (ledger append, then signing, then audit append). If the token trips while the FIRST write is executing, the guard remains entered for the whole block — it permits the already-running operation to finish (correct), but ALSO permits the subsequent signing + audit append to begin without a fresh consultation of the token, since the guard was entered before the trip and never re-checked mid-block. Not a regression introduced by round 9/10's admission-deferral or future-publication fixes; a pre-existing granularity gap in the round 4-8 cancellation-fence work, surfaced for the first time at round 10.
- **Close-out steps.** Needs each independent effect (ledger append / signing / audit append) wrapped in its OWN `effect_entry()` call rather than one guard spanning all three, so a trip between operations is caught before the next one begins. Ground against whether signing is itself effect-bearing (network call to the KMS backend per ADR-D8) or purely computational before deciding scope — shares the drain-loop threading-model discriminator with B-61. **Council: no** — narrow, per-site guard-splitting fix once the signing effect-classification is confirmed.

- **CLOSED 2026-07-21 (PR #1072)** — both multi-effect guard sites split per-effect: `_compose_and_persist_audit` 8b/8c/8d (signing grounded effect-bearing — `cp_audit_to_od_audit` → `sign_audit_entry` → `backend.sign()` KMS network call at MTC) and `_execute_workflow_body`'s durable-output journal + step ledger append. A trip during one effect now refuses the NEXT at its own fresh consult. Witnesses: 8b-trip → 8c/8d never begin; 8c-trip → 8d never begins (added per the merge-gate test-witness lens G1 note); journal-trip → ledger append never begins (real `execute_workflow`); all PD-8 probed incl. the coarse-guard and per-site nullcontext mutations.

### B-63 · SigningBackend.verify() infra failures are not typed-availability at the concrete backend *(registered at the U-OD-55 impl arc, codex round-10, 2026-07-20)*
- **What it is.** Registered at the U-OD-55 impl arc (codex round-10 [P2] "translate verify outages into the OD availability type"): OD spec v1.34 §21.2.2 row 7(b) requires backend availability errors during verification to surface through a dedicated typed availability error. The `AwsKmsSigningBackend.verify()` concrete backend (harness-cp, B-36/ADR-D8) catches only `KMSInvalidSignatureException` and returns `False` for it; any OTHER boto3/KMS failure (credential, network, throttling, service exception) propagates as a raw vendor exception, unclassified. Confirmed architecturally cross-axis at grounding: the `SigningBackend` Protocol (`harness_cp.f5_signing_key_resolution`) documents no availability-exception contract at all (checked directly, not inferred) — and even if it did, the OD-owned `AuditVerificationBackendUnavailableError` cannot be raised by a CP-axis backend without CP importing FROM OD, which the axis-import direction (OD consumes CP; not the reverse) forbids. A blanket `except Exception` at the OD-axis verifier call site is not a substitute — it would misclassify genuine backend defects as availability, which the same row 7(b) text forbids ("raises that are NOT the typed availability error and NOT `AuditSignatureInvalid` are DEFECTS and propagate unwrapped"). U-OD-55's own `per_family_audit_verification.py` docstring previously claimed "a well-behaved backend implementation is expected to raise THIS type itself" — corrected in-place at the same PR to state the gap accurately (no such contract exists yet) rather than assert an architecturally-impossible design. **Concrete instance (codex round-12, same PR):** when a resolver returns `AwsKmsSigningBackend` for a key_id absent from that instance's OWN internal `_key_arns` mapping, `verify()` raises `UnknownSigningKeyIdError` (a `KeyError` subclass) BEFORE reaching KMS — semantically identical to "the composition root failed to supply the key" (the exact category `AuditVerificationBackendUnavailableError`'s docstring already describes), but unreachable to translate at the OD-axis verifier without importing a name from ONE SPECIFIC backend's implementation module (`harness_cp.aws_kms_signing_backend`, not the generic `SigningBackend` Protocol module) — the same wrong-direction coupling this row already forecloses, confirmed by grep: `UnknownSigningKeyIdError` is defined and used ONLY in that one backend module, not on the Protocol. Same registered gap, not a second independent defect.
- **Close-out steps.** Needs a shared/CP-side (or harness-core) typed availability-exception contract that `SigningBackend` implementations (starting with `AwsKmsSigningBackend`) raise for infra failures distinct from a genuine signature mismatch, PLUS a translation catch at the OD-axis verifier call sites (`_verify_entry_signature` / `verify_cutover_record_signature`) that maps the CP/core type to `AuditVerificationBackendUnavailableError`. Cross-axis scope (touches harness-cp production code already shipped under B-36/PR #1031) — out of U-OD-55's OD-axis-only reach; does not block U-OD-55's merge (the resolver-path availability typing — unknown `key_id` — is already correct and covers the row 7(b) example explicitly named in spec text; this residual is the real-backend infra-failure sub-case only). **Council: no** — narrow, single cross-axis contract addition once a concrete infra-failure taxonomy is confirmed against the deployed backend(s).

- **CLOSED 2026-07-21 (PR #1073)** — the shared typed availability contract landed at `harness_core.SigningBackendUnavailableError` (the close-out's harness-core branch; carrier-home discipline): `AwsKmsSigningBackend.verify()` classifies `(BotoCoreError, ClientError)` infra failures as the shared type (mismatch still `False`, caught FIRST — order pinned by a real-ClientError-subclassing fake per the merge-gate test-witness lens; `ParamValidationError` carved out as a DEFECT per codex r3), `UnknownSigningKeyIdError` gains the shared base (the row's concrete instance), and BOTH named OD verifier sites translate narrowly to `AuditVerificationBackendUnavailableError` per OD v1.34 §21.2.2 row 7(b) — defect raises still propagate unwrapped, row-9 breaker asymmetry untouched. Codex r2 extended the taxonomy to the CLI boundaries (harness-inspect → UNVERIFIED exit 3 retryable wording; migrate-audit-sidecar → typed exit-1, zero tags changed) and the runtime-local name collision was renamed (`SigningBackendSdkUnavailableError`). 4 codex rounds converged; merge-gate 2 rounds (r1 two BLOCKs fixed per prescription) to 3-lens all-APPROVE; 15 witnesses, 7 PD-8 probes all pinning. Residual noted, not built: a CP §20.2.1 Protocol-docstring cross-reference to the availability contract (spec-lens follow-on nicety); bootstrap greenfield round-trip availability reads as config-invalid (pre-existing posture, test-witness lens note).

### B-64 · Greenfield cutover-record freshness probe and publication are not atomic cross-process *(registered at the U-RT-134 impl arc, codex round-9, 2026-07-20)*

- **What it is.** `initialize_mtc_audit_signing_record`'s ledger-freshness probes (sidecar rows + IS `audit:` refs, each under its own short-lived `cross_process_read_lock`) release their locks BEFORE `_greenfield_sign_empty_record` publishes the record — a concurrent audit append landing in the probe→publish window could in principle let an empty cutover record be minted over just-written history (codex round-9 [P1], the arc's pre-committed terminal review round).
- **Why registered, not fixed.** The practical attack paths are already closed: at MTC an appender only runs POST-bootstrap, and its own bootstrap either verified an existing record or minted one (the U-RT-134 invariant itself), so a racing appender implies the record exists and the greenfield branch is never taken; simultaneous dual-greenfield bootstraps converge via the round-7 no-clobber `os.link` publication (the loser verifies the winner). The residual hardening — holding the sidecar/ledger locks across probe + publication — interacts with `harness_is.cross_process_ledger_lock` same-process nesting semantics and needs its own deadlock-safety analysis, not a terminal-round patch.
- **Close-out.** Hold `cross_process_write_lock(sidecar_path)` (or an equivalent single lock scope) across ALL THREE probes (the persisted-row key scan shares the same probe→publish window — merge-gate concurrency lens, PR #1063) and the no-clobber publication, after confirming the IS lock implementation's same-process nesting semantics (replace inner read-lock acquisitions with lock-free reads under the held write lock if nesting is unsafe). Witness: an interleaving test that starts an append inside the probe→publish window and asserts the greenfield mint is refused. Does not block U-RT-134's merge (terminal-round registration per the B-48 r10 precedent).

- **CLOSED 2026-07-21 (PR #1071)** — `initialize_mtc_audit_signing_record` now holds ONE `cross_process_write_lock(sidecar_path)` across the persisted-row key scan, both freshness probes, and the no-clobber `os.link` publication, exactly per the close-out; same-path inner reads run lock-free under the hold (`_sidecar_has_rows_locked` + the `_reject_record_key_used_by_persisted_rows` wrapper/`_locked` split — `record_migration.py` callers unchanged), and the nested `ledger_has_audit_refs` probe's state-ledger read lock was deadlock-safety-grounded against the dir lock's per-thread reentrant face (the B-50 composition). The existing-record verify branch releases early (codex r3 — no publication there; availability preserved). Witness: two-thread interleaving test proving an appender holding the writer's own lock cannot land inside the window at EITHER the probe or publication half (merge-gate test-witness lens strengthened the publication half pre-merge); 4 codex rounds to convergence (2 typed-error translation fixes + the verify-branch release), merge-gate 3-lens all-APPROVE, 5 PD-8 mutation probes all pinning, full runtime suite 2926 passed.

### B-65 · Post-effect signing carrier disposition at CP topology cascade handlers is unspecified *(registered at the U-RT-136/U-CP-73 impl arc, codex round-2, 2026-07-20)*

- **CLOSED 2026-07-22 (PR #1078)** — the RATIFIED option-A apply arc landed in full: CP spec v1.103 §25.15 terminal-with-result rider + Runtime spec v1.103 §14.8.11 protected result store (spec leg PR #1077; clearance markers filed), plus the CP/Runtime impl (`workflow_driver.py` name-match fence ahead of every branch-failure → PAUSED/PARTIAL conversion across all 6 topology handlers; `protected_result_store.py` encrypted/tenant-bound/ack-gated-retention store; both dispatch-side `result_ref` threading through llm_dispatch/runtime_tool_dispatcher/sub_agent_dispatch/webhook_delivery_composer). 11 out-of-family Codex rounds to terminal (round 7 introduced then round 8 self-corrected a tenant-sentinel-encoding regression; round 11's two findings closed the arc — one fixed via a collision-free tenant-scope encoding, one registered as `B-68`, a narrow TTL-dependent GC/publish race). Merge-gate 3-lens round-1 BLOCKed on missing e2e coverage for 3 of 4 post-effect raise-site families — fixed + PD-8 mutation-probed per site, APPROVE at round 2. Both packages full-green throughout (harness-runtime 3028 passed/29 skipped; harness-cp 1717 passed/4 skipped). Second facet below (payload recoverability) is DELIVERED by the same protected result store — the store IS the resolution.
- **FORK FILED 2026-07-21** — `.harness/class_2_fork_b65_post_effect_signing_carrier_cascade_disposition.md` (Class 2: §25.15 terminal-with-result rider + §3b protected result store with encrypted/tenant-bound/ack-gated-retention contract and full-strength composite keying; rec A). **RATIFIED 2026-07-21 (option A)** — apply arc is forward work.
Under `audit_signing_fail_closed=ON`, a `PostEffectAuditSigningError` (the U-RT-136 result-preserving
post-effect carrier) raised at a fan-out branch reaches the CP workflow driver's topology cascade
handling as a generic branch failure: under `cascade_policy=pause` it converts to a resumable PAUSED
result whose resume RE-DISPATCHES the same step — re-firing the already-completed PAID effect the
carrier exists to protect — and PROCEED-tier handling reduces it to PARTIAL without the `result_ref`.
The v1.101 CP rider's catch-ordering contract enumerates the per-attempt classifiers (all fenced at
this arc) but does NOT specify branch-vs-workflow terminality of the carrier per `cascade_policy`;
that disposition (terminal-for-workflow vs terminal-for-branch-with-siblings-preserved vs a
non-resumable pause variant) is a design decision over C-CP-25 §25.10+/§25.15 cascade semantics, and
`harness-cp` cannot import the runtime carrier (any driver-side fence uses the established
`type(exc).__name__` match — the `StepDispatchTimeoutError` precedent). Registered per the terminal
register discipline (B-64/B-60..62 precedents); blast radius today is bounded to flag-ON fan-out
topologies. Close-out: spec-leg the disposition, wire the name-match fence ahead of the
branch-failure → PAUSED/PARTIAL conversions, witness a flag-ON fan-out run asserting no resumable
snapshot + result_ref surfaced + no re-dispatch on resume. Second facet (codex round-3 P1, same
design surface): payload RECOVERABILITY beyond the in-memory carrier — after the driver's
string conversion the caller holds an opaque `result_ref` with no persisted lookup (the report
log is digest-only per the round-2 redaction fix); the ratified acc-1b letter requires only
report + caller-carried reference (delivered), so a protected result store keyed by
`result_ref` (or carrying the payload through the CP/runtime result model) resolves in the
same spec leg.

### B-48 · Sync sub-agent dispatch executes on the event loop *(surfaced at B-47 PR B2a, out-of-family Codex round-2, 2026-07-17)*
- **CLOSED 2026-07-19 (PR #1060)** — spec+plan deltas APPLIED (Runtime v1.102 §14.8.10 + CP v1.102 §25.11 + IS v1.11 §7.6 + plans v2.50/v2.39/v2.7 + Core v1.3 (U-CORE-03) + clearance markers; C1⊥C9 dyad 16/16 CONFIRM at `.harness/council-dyad-b48-apply-2026-07-18.md`); executor impl arc (U-CORE-03 + U-RT-140..144 + U-CP-101 + the B-39 interim constraint) LANDED — 10 successive rounds of decorrelated pre-merge review (merge-gate 3-lens + out-of-family Codex, each re-run to convergence), 35 findings fixed with PD-8 mutation-probed witnesses. Round 10 (the operator-approved final gate) converged clean on 9's fixes; codex round-10 surfaced 3 further findings against pre-existing cancellation-fence coverage from rounds 4-8 (not a round-9/10 regression), escalated to the operator per the pre-committed terminal rule, and registered separately as B-60/B-61/B-62 rather than blind-fixed (operator selected "register + merge now").
- **Fork doc FILED 2026-07-18** at `.harness/class_2_fork_b48_sync_subagent_dispatch_offload.md` — recommendation (grounded at filing rounds 1/15: the only materialized bound is HIERARCHICAL_DELEGATION's per-parent fan-out cap 3; the §25.11 depth phrase is unmaterialized, generic recursion and aggregate active frames unbounded): option B = custom grow-on-demand executor + configurable hard cap + fail-fast; C1/C9 council at apply; cancellation semantics defined first. Selection RATIFIED 2026-07-18 (option B); spec-apply COMPLETE 2026-07-19 — impl arc live.
- **What it is.** `RuntimeHITLGateComposer._dispatch_inner` invokes the sync C-RT-17 sub-agent dispatcher DIRECTLY on the event loop — the U-RT-60 wrap-asymmetry fork's Q3-ratified direct-call reading. The entire sub-agent dispatch therefore runs on the loop: child workflow execution (seconds-long), its F2/ledger writes, and (since B-47 PR B2a threaded the signing backend) KMS network signing. A slow child workflow or KMS call blocks every concurrent workflow and HITL interaction. Pre-existing at the fork ratification — B2a only ADDS KMS latency to an already-blocking ratified surface, which is why this is registered as its own arc rather than patched inside B2a.
- **Close-out steps.** Class 2 fork revision of the U-RT-60 direct-call reading: offload the sync inner dispatch in `_dispatch_inner` off the loop. The offload design must handle recursion (child workflows re-enter the sync driver whose `SyncDispatcherFacade` bridges back onto the loop; a bounded pool deadlocks under recursive `SUB_AGENT_DISPATCH` depth — NOT the 4-worker audit-offload pool, NOT the loop default pool), and re-validate pause/resume flags, OTel span context, and B-21 fan-out under worker-thread execution; re-run the r100 e2e. The B2a-landed mitigations already cover every OTHER production signing path (async composers/builders offload via the dedicated audit executor with join-on-cancel). **Council: yes at apply leg** (condition MET at filing rounds 1/15/17 — only the fan-out cap 3 is materialized; the depth phrase names no value/carrier; generic recursion + aggregate active frames unbounded; the C1/C9 dyad CONFIRMS the filing-settled RECOMMENDATION — fail-fast + typed error + RuntimeConfig ownership + default 256 shared-budget frames (N upstream + one per sync branch) — RATIFIED 2026-07-18 (option B as recommended); it does not re-derive the default).

### B-37 · Remote MCP streamable-HTTP transport residuals *(surfaced at `B-10`'s own `B-MCP-OAUTH-RS-ENFORCE` grounding, split out 2026-07-14 at forward-register-schema grounding; corrected 2026-07-14 round-3 out-of-family Codex — the original finding had the deprecated/current function names backwards)*
- **What it is.** `B-10`'s own "Residual" bullet (2026-07-12 grounding) named two independent gaps, left unregistered as their own item. **(1), corrected:** `mcp_client_host.py:495`'s `_http_connection_context` already imports the CURRENT, non-deprecated `mcp.client.streamable_http.streamable_http_client` (verified against the pinned `mcp==1.27.1`: `streamablehttp_client`, no underscore, carries `@deprecated("Use streamable_http_client instead.")` — the ORIGINAL B-10 bullet had the two names' deprecation status backwards). The real, empirically-confirmed gap: line 507 calls `streamable_http_client(url, **kwargs)` where `kwargs` is built (lines 498-506) in the OLD deprecated function's shape (`headers`/`timeout`/`sse_read_timeout`) — but the current function's actual signature is `(url, *, http_client: httpx.AsyncClient | None = None, terminate_on_close=True)`; it does not accept those kwargs at all. Harmless today only because `_build_transport_config` (`mcp_client_host_factory.py:97`) never populates `headers`/`timeout`/`sse_read_timeout` on the `transport_config` dict (confirmed by direct read — only `url` is ever set), but a latent `TypeError` the moment anything does. The docstrings at lines 203/483/544 are also stale, still naming the deprecated `streamablehttp_client` as what's used. **(2), unchanged:** `MCPClientConfig` (`types.py:618`) carries **no field at all** for auth credentials despite its own docstring's "auth-secret reference are operator-supplied" claim — there is currently no way, even manually, to authenticate a remote MCP connection.
- **Current state.** Both dormant — no live remote-OAuth deployment exists yet; neither blocks any current close. Registered here (rather than left buried inside a closed `B-10` row where a `--open`-filtered query would miss them) per the register's own "register-then-triage, not drop" discipline.
- **Close-out steps.** (1) **Keep** the current `streamable_http_client` import (do NOT migrate to the deprecated `streamablehttp_client`); build a configured `httpx.AsyncClient` from `headers`/`timeout`/`sse_read_timeout`/`auth` and pass it via `http_client=` instead of forwarding those as direct kwargs; fix the 3 stale docstrings naming the deprecated function. (2) add an auth-credential field to `MCPClientConfig` (distinct from `B-MCP-PRIMITIVE-SIG-GATE`'s tool-description-hash mechanism) and wire it into the connection context. **Council: no** — mechanical once a real remote-MCP credential-supply build opens; not urgent.

### B-38 · Fine-grained provider-exception classifier for `breaker.cause` *(surfaced at `B-19`'s own close, split out 2026-07-14 at forward-register-schema grounding, out-of-family Codex)*
- **What it is.** `B-19`'s own "Follow-on registered (NOT built)" bullet named a fine-grained provider-exception classifier (distinguishing `rate_limit` / `auth_failure` / `5xx_streak` at a real per-step dispatch failure) that would make `harness.breaker.cause` non-vacuous — every breaker-trip call site previously passed `cause=None` (the runtime's only real auth-vs-transient discriminator was a bootstrap-ping-only classifier that never reached per-step dispatch).
- **Close-out.** Added `_classify_breaker_cause(exc: BaseException) -> BreakerCause | None` (`retry_breaker_fallback.py`) — duck-typed on `.status_code` (`429`→`RATE_LIMIT`, `401`/`403`→`AUTH_FAILURE`, `500<=code<600`→`FIVE_XX_STREAK`, else `None`) rather than importing each provider SDK's exception hierarchy, matching the existing let-SDK-exceptions-propagate discipline at `llm_dispatch._dispatch_anthropic`/`_dispatch_openai`/`_dispatch_ollama`. Computed once per exception (as `breaker_cause`, distinct from the pre-existing `cause: ValidatorRetryExitClass` local that drives retry/fail-fast control flow) and threaded into all 3 `breaker.record_failure()` call sites. Verified via an isolated status-code table test AND an e2e test driving the real `RetryBreakerFallbackDispatcher.dispatch()` catch site with SDK-shaped fake exceptions, asserting the classified cause lands on the emitted `BreakerTransition` — mutation-probed against real production-code reversions (`record_failure(cause=breaker_cause)` → `record_failure()`; the `500<=code<600` bound reverted to `>=500`) confirming the tests genuinely fail without the fix, then restored.
- **Second-round Codex catch (real, not registered-away).** `emit_breaker_trip_span_event` (`harness_breaker_schema.py`) built an `EventEmission` bookkeeping record but never called the real span's `.add_event(...)` — so `harness.breaker.cause` would have stayed a Python object field, never reaching an actual OTel span, defeating this row's own purpose. Confirmed by comparison against the sibling `emit_drift_event` (`alignment_floor_drift_detection.py`), which *does* call `.add_event` — proving this was a local, comparison-provable omission, not the systemic H_T-OD-2 "OTel SDK injection absent" gap it initially looked like (`SpanRef` is a real `opentelemetry.trace.Span`, not a stub). Fixed to match the established pattern rather than registered as a follow-on, since leaving it unfixed would mean this row's stated goal ("make `harness.breaker.cause` a real signal") was not actually achieved end-to-end.
- **Split out, not folded in.** Two adjacent findings surfaced during this close were registered as their own rows rather than absorbed here: `B-41` (the pre-existing retry-routing classifier still misroutes `AuthenticationError` to `TRANSIENT_RETRY`, a real behavior-change decision with its own blast radius) and `B-42` (`FIVE_XX_STREAK` is tagged from the terminal exception only, not a verified N-consecutive streak — an acknowledged MVP simplification, caught independently by both this row's own grounding agent and out-of-family Codex reviewing the PR). Shipped at PR #1020.

### B-40 · Memory operation ledger has no cross-process write serialization *(surfaced at B-35's merge-gate concurrency lens, 2026-07-15; CLOSED at PR #1026 after a 4-round register-correction arc at PR #1025)*
- **What it is.** `append_memory_operation` (`harness-is/src/harness_is/memory_operation_ledger.py:478-497`) reads the full ledger to find the last entry, computes `prior_event_hash` from it, then appends — serialized only by a process-local `threading.Lock` (`memory_operation_ledger.py:36`). Two independent OS processes writing to the SAME canonical store (e.g. two concurrent `harness run` invocations against the same repository — see round 4 below) can both read the same last entry before either appends, producing a `prior_event_hash` mismatch that `verify_memory_operation_ledger` would later flag as `CHAIN_LINK_MISMATCH`. A second, related gap: `read_memory_operations()` (`memory_store.py:226-229`) does an unlocked `path.read_text()` with no coordination against an in-flight writer, risking a torn read of a partially-flushed line under true concurrency. Both gaps are real and unchanged across all four correction rounds below — only the disposition framing was wrong, three times, in three different directions.
- **Round 1 self-correction (2026-07-15) — WRONG.** Claimed this was "speculative, already-decided, not fork-eligible," conflating ADR-F2 §Rationale(c)(ii)'s `[SPECULATIVE]` tag on whole-system multi-process/cross-host *workload-mix posture* with C-MEM-08's specific, unconditional ledger invariant. Out-of-family Codex reviewing PR #1025 correctly flagged this as a P2.
- **Round 2 correction (2026-07-15) — ALSO WRONG.** Reclassified `design_substrate_gated`, treating whether C-MEM-08's "concurrent writers" covers cross-process/cross-worktree writers as an open interpretive question needing a Class 1 fork ruling. Out-of-family Codex flagged this too, citing `Spec_Information_Substrate_v1.md` C-IS-07 §7.3 and C-IS-09 §9.3 directly.
- **Round 3 correction (2026-07-15, Codex + advisor()) — the settled reading, still incomplete.** Verified directly against the cited sections: C-IS-07 §7.3 states "the C3-pole append-only write contract serializes writers" (unconditional, no in-process/cross-process qualifier). C-IS-09 §9.3 "Cross-worktree writer serialization" explicitly REQUIRES "concurrent writes across sibling worktrees against the same `.git` storage backend are serialized" — this is NOT open. §9.4 defers only the *mechanism* to implementation discretion, the same pattern `Spec_Information_Substrate_v1.md` §7.4 uses for the memory ledger specifically. Round 2 conflated this settled requirement with §9.4's separate, still-open "Multi-writer scaling BEYOND worktree-isolation requires a downstream D-ADR" clause (a different, larger, true-distributed-scale question that does not gate this row). Round 3 correctly reclassified `registered_finding`, buildable now, no fork — but still claimed "no live trigger, dormant."
- **Round 4 correction (2026-07-15, Codex round 3) — "dormant" was ALSO wrong.** Grepping for internal `multiprocessing`/`subprocess.Popen`/`ProcessPoolExecutor`/`os.fork(` only rules out the harness spawning its *own* child processes — it says nothing about two independently-launched, fully-supported CLI invocations. `RuntimeConfig.memory.enabled` defaults `True` and `memory.root_path` defaults to `<repository_root>/.harness/memory` (`types.py:1504-1508`, bound at `automatic_memory.py:114`) — so two ordinary concurrent `harness run` invocations against the SAME repository (two terminals, two CI runners — no exotic multi-process code required) already resolve to the identical canonical memory ledger **today**. This is a **live, directly reachable gap under ordinary usage**, not one gated on a future architecture.
- **Corrected disposition: `registered_finding`, buildable now, live — no fork, no council.** Requirement settled (C-IS-09 §9.3 + C-MEM-08); mechanism is implementation discretion (§9.4); trigger is live (two concurrent `harness run` invocations, no internal multiprocessing needed). This is impl-to-cleared-spec, not X-AL-3. Both real gaps are in scope for the eventual fix: (1) `append_memory_operation`'s `threading.Lock` does not serialize across OS processes — needs a cross-process mechanism per §9.4's own discretion list; (2) `read_memory_operation_ledger`'s unlocked `path.read_text()` can observe a torn/partial line during a concurrent append — needs reader coordination or tolerant parsing. Axis-wide: `memory_operation_ledger.py` and `state_ledger_write.py` share the identical writer pattern (`state_ledger_write.py:26-28,61-73`), so any fix must cover both, not memory-ledger-only.
- **CLOSED at PR #1026 (2026-07-15).** Added `harness_is.cross_process_ledger_lock` — a same-host `fcntl.flock` helper mirroring the proven pattern already shipped at `harness_runtime.lifecycle.reconciler_pause_resume_substrate._workflow_lock` (POSIX advisory, same-host only; true cross-host distributed writers remain out of design horizon per ADR-F2 §Rationale(c)(ii), unaffected). Wired into **4 real call sites** found during grounding — 2 more than originally named: `append_ledger_entry`, `append_memory_operation`, `shadow_git_rollback`'s ledger-preserve-across-checkout window, and `jsonl_event_ledger_lifecycle.initialize_jsonl_event_ledger`/`validate_jsonl_event_ledger_format` (the last caught by the merge-gate concurrency lens — reachable on every `harness run` bootstrap/resume via `harness_runtime.lifecycle.state_ledger.materialize_state_ledger`; its pre-existing unlocked read could undercount `entry_count` and silently skip the tamper-check gate). `read_ledger`/`read_memory_operation_ledger` take a shared lock; internal call sites use new `_unlocked` variants to avoid self-deadlock (`flock` is per-open-file-description, not reentrant). Added genuine multi-process test coverage (`multiprocessing`, `fork` context) for all 3 append-side call sites — the merge-gate test-witness lens caught that the first pass covered only 2 of 3 wired sites (missing `shadow_git_rollback`), fixed before merge. Every new test mutation-probed: reverted the fix, confirmed `CHAIN_LINK_MISMATCH` failure across 3 runs, restored. Full `harness-is` suite green; `ruff`/`pyright`/`overlay-check` clean. **Council: no** — requirement was settled (C-IS-09 §9.3 + C-MEM-08), mechanism was implementation discretion (§9.4); impl-to-cleared-spec.
- **Post-merge-gate Codex round on Windows + gitignore.** Out-of-family Codex round 1 on PR #1026 caught two further issues, both fixed before merge: (1) the unconditional top-level `import fcntl` broke `import harness_is` on Windows (eagerly imported via `memory_operation_ledger`) — C-STK-10 commits to macOS/Linux/Windows host-OS support "without per-OS port forking"; fixed by gating the import behind `sys.platform == "win32"` and degrading the lock context managers to a no-op on Windows (exact pre-B-40 parity — the in-process `threading.Lock` half is unaffected and cross-platform; only the new cross-process dimension is unavailable there). (2) the new `.lock` sidecar files were not gitignored; added `.harness/**/*.lock`, verified via `git check-ignore` against both actual lock paths. The genuine Windows locking *mechanism* remains unbuilt by design (advisor()-reviewed scope call, not an oversight) — registered separately as `B-45`.
- **Round 2 out-of-family Codex catch — `cross_process_read_lock`'s own P1 fix genuinely violated read-only-CLI, fixed.** Reviewing the FULL PR diff against `main` (an earlier attempt had been invoked against an invalid git ref and produced no usable verdict), Codex found the round-1 read-path fallback (opening the sidecar with `O_CREAT` when it didn't exist yet) still `mkdir`'d the parent directory even on the "no sidecar" branch — a read against a ledger with no lock file yet on a read-only parent would raise `PermissionError`, breaking `harness-inspect`'s no-writes contract exactly the way round 1 was supposed to prevent. Fixed: when the sidecar doesn't exist, `cross_process_read_lock` now skips locking entirely (no `mkdir`, no `O_CREAT`, no side effect) rather than trying to provision it — verified with a direct probe (a read-only-parent directory, no sidecar) confirming the read succeeds and creates nothing.
- **Round 2 also found 2 non-regression residuals, registered as `B-46` rather than fixed here.** (1) The same no-sidecar-yet fallback above reintroduces a narrow TOCTOU: a reader can pass the "sidecar doesn't exist" check moments before a racing first-writer creates it, then read unguarded while that writer is mid-append. (2) The identical shape exists in `jsonl_event_ledger_lifecycle.py`'s initialization probe. advisor()-confirmed both are pre-existing (every read was unguarded, always, before B-40 existed — this is a residual of B-40's own round-1 P1 fix, not a new hole) and narrow (once any writer has ever created the sidecar, all subsequent reads/writes correctly serialize). The durable fix — lock the canonical ledger file directly instead of a lazily-created sidecar — removes the provisioning race entirely but is a larger, `harness-inspect`-compatibility-affecting redesign properly scoped to its own arc, not bolted onto this PR's third round. Softened `cross_process_read_lock`'s docstring to state the guarantee precisely (closes the race once a ledger has been written at least once under the lock, not before).
- **Also fixed (round 2), unrelated to the code review: a self-inflicted register regression.** The B-40/B-45 register update in this PR used a full-file `yaml.safe_dump` reserialization, which silently stripped `.harness/forward-register.yaml`'s entire header comment block (status enum, standing grounding directive, forward-only update rules) even though only 2 rows changed semantically. Restored the header verbatim from `main`; all future register edits in this arc use a header-preserving splice, not a full-file dump.
- **Convergence called here (advisor()-confirmed).** Round 2's two P2s (findings 1+2 above) are non-regression residuals, not new holes this PR introduces — the round-3 read-only fix causing the round-4 TOCTOU is the same underlying tension (lazy sidecar provisioning) that only a canonical-file-lock redesign resolves, which is exactly why that redesign is its own scoped follow-on (`B-46`) rather than a fourth round on this PR. No further out-of-family review round was run on this disposition-only commit — registering + documenting a known, narrow, non-regression residual is the resolution, not a new substantive change needing re-review.

### B-46 · Cross-process ledger lock has a first-write TOCTOU on lazy sidecar provisioning *(surfaced at B-40's own PR, out-of-family Codex round 4 + advisor(), 2026-07-15; CLOSED 2026-07-18 — PR #1052)*
- **CLOSED 2026-07-18 (PR #1052; bullet refreshed at merge-gate round-1 to shipped HEAD)** — canonical-file lock redesign hardened through 9 codex rounds: writers dir(EXCLUSIVE, per-thread-reentrant)→file-EX non-blocking-probe handoff; lock never creates the canonical (round-36 guard preserved); readers dir-first, never create/mkdir; inode-stability verify-retry; `cross_process_replace_lock` for rollback's inode swap; ONE global lock order (cross-process before `_WRITE_LOCK`); transitional mixed-version coexistence (writers PROVISION + lock the legacy sidecar, O_NOFOLLOW + alias checks, loud non-regular refusal). Residuals per the module docstring: absent parent dir at read; new-reader vs fresh-old-writer first sidecar creation (torn read, loud). 20+ witnesses incl. two-process fork-based dir-flock coverage; all mutation classes probe-killed.
- **What it is.** `harness_is.cross_process_ledger_lock`'s lock sidecar (B-40) is provisioned lazily by whichever writer touches a ledger first. A reader racing that very first writer can pass `cross_process_read_lock`'s "sidecar doesn't exist yet" existence check before the writer creates it, then read the ledger unguarded while that writer is mid-append — defeating the torn-read protection for exactly the first write/read race against a brand-new ledger. The identical gap exists in `jsonl_event_ledger_lifecycle.py`'s initialization probe (a fresh ledger can take the missing-file branch and return `entry_count=0` without waiting, even while another process already holds the writer lock but hasn't yet created/populated the file).
- **Current state.** Registered, not built — advisor()-confirmed non-regression (every read was unguarded, always, before B-40 existed; this is a residual, not a new hole) and narrow in practice (once any writer has ever created the sidecar for a given ledger, all subsequent reads/writes correctly serialize — the window is exactly the first concurrent write+read pair against a not-yet-touched ledger, which degrades to a loud parse error in an already-degenerate scenario, not silent corruption). This finding and B-40's round-3 P1 fix (readers must never create the sidecar or `mkdir`, to preserve `harness-inspect`'s read-only-CLI contract) are in direct tension under a lazily-created sidecar — no fix satisfies both without changing the lock target.
- **Close-out steps.** Lock the CANONICAL ledger file directly (a separate fd opened purely for `flock`, independent of any fd used for actual I/O — POSIX `flock` is per open-file-description, so this is safe) instead of a lazily-created sidecar; when the canonical file doesn't exist, there is genuinely nothing to read yet (no race is possible on a file that isn't there); when it does exist, lock it read-only (`O_RDONLY`, no `O_CREAT` needed since it already exists) or read-write (`O_CREAT|O_RDWR`, needed once, for the writer that creates it). This removes the provisioning race entirely but touches the write path adjacent to hash-chain integrity and the `harness-inspect` read-only contract — its own scoped arc, not bolted onto B-40's PR. **Council: no** — mechanical redesign once scoped, same implementation-discretion category as `B-45`; not an open spec question.

### B-45 · Cross-process ledger lock has no genuine Windows serialization backend *(surfaced at B-40's own PR, out-of-family Codex + advisor(), 2026-07-15)* *(**RATIFIED-DEFERRED 2026-08-05** — the Windows backend is NOT built; D-1 / D-2 are the re-open triggers)*
- **Enumeration updated 2026-08-06 — #1241 (the `B-93` build leg), in place, because this row's own count is what changed.** The **§3(ii) import repair landed**: `reconciler_pause_resume_substrate` now declares `_IS_WINDOWS` and imports `fcntl` **function-locally** like its three siblings, so `_workflow_lock` degrades to a documented no-op on Windows instead of the module failing to **import** there (the bootstrap path `bootstrap/factories/r_cxa_2_producer_loop_factory.py` raised `ModuleNotFoundError`). **Consequence for this row's numbers — superseding the 2026-08-05 re-verification below rather than sitting beside it:** Windows no-op **lock** surfaces are now **seven, not six**, across four modules; `_IS_WINDOWS` flag declarations are now **four, not three**; and the clause *"`reconciler_pause_resume_substrate` still takes an unconditional `fcntl` lock with no Windows guard at all (the import-time-crash case, not a no-op)"* is **false as of this leg** — it is now a no-op case exactly like the other six. **Close-out step (3) is NOT discharged, not even in part** *(corrected at the merge-gate — "half-discharged" overstated it)*: step (3) demands genuine **Windows backend parity** across all three same-host lock primitives, and what landed is **flag** parity at **one** of them — a *precondition* for that work, not a portion of it. Windows still excludes nothing anywhere. **The row's substance is unchanged** — a repair that makes a Windows process *start* is not a serialization claim; Windows still **excludes nothing** at any of the seven surfaces, `journal_exclusion_is_degraded()` still returns `_IS_WINDOWS`, and the refuse-by-default consult at `admin/pause_journal_adoption.py` stays **armed**. **Status unchanged** at `registered_finding`; **D-1 / D-2 unchanged**; the deferral's deciding ground (witnessability, not the backend pick) unchanged. The `B-93` half is now **closed**, so the fork's **ordering** constraint is satisfied in advance: if decision 1 ever flips, the deadline leg has already landed and the Windows leg no longer needs to re-traverse the nine sites.
- **RATIFIED-DEFERRED 2026-08-05** (the combined `B-93`+`B-45` fork, Reading B): decision 1 — build a Windows serialization backend now — is **NO**, and this row's **status is unchanged at `registered_finding`** with its demand tests written in. **The deciding ground is WITNESSABILITY, not the backend pick:** with no Windows CI, landing a backend flips `journal_exclusion_is_degraded()` (`journal_workflow_pause_store.py:414`) to `False` and **disarms** the refuse-by-default consult at `admin/pause_journal_adoption.py:412`, replacing a **declared safe degradation** with an **unverified exclusion claim** — strictly worse than today. **Re-open triggers (either alone fires):** **D-1** — Windows CI exists, and it must be an **executing behavioural** witness (a lint-only `windows-latest` job does **not** satisfy it, codex R8); **D-2** — an operator or deployment **asks for** a Windows host, **or** any packaging surface **declares** one (an `Operating System ::` classifier or a `sys_platform` marker in a harness `pyproject.toml` — **today: zero**). *Kept in the fork §4's observable form and deliberately **not** abbreviated to "a declared Windows target": §3(i) already establishes Windows as a design-time **committed** host-OS surface, so the abbreviated form would be satisfied at filing time and this deferral would reopen immediately (out-of-family review round 1 [P2])*. **No council owed** unless decision 1 later flips. If it flips, the `B-93` deadline leg **must land first**, and the close is a **multi-surface doc sweep** across the 6+ sites citing this row as the workspace's Windows posture.
- **What it is.** `harness_is.cross_process_ledger_lock` (B-40) degrades to a no-op on Windows (`sys.platform == "win32"`) rather than providing real cross-process serialization there — Windows sits at exact pre-B-40 parity (same as before the module existed), not at feature parity with POSIX. The identical gap already exists, unregistered until now, at `harness_runtime.lifecycle.reconciler_pause_resume_substrate`'s own unconditional `fcntl` import (that module isn't eagerly imported at package `__init__`, so it doesn't break basic import on Windows, but its lock is equally POSIX-only).
- **Current state (rewritten in place 2026-08-05 at the B-93+B-45 combined fork filing).** Registered, not built — a deliberate scope call (advisor()-reviewed during B-40's own PR), not an oversight. **The fix is a real decision, and the decision is WHICH VEHICLE, with three arms — not whether the shared face must be degraded**: `msvcrt` exposes the non-blocking `LK_NBLCK` alongside `LK_LOCK`, and `filelock`'s `ReadWriteLock` (3.21.0+) provides the shared-reader/exclusive-writer split PORTABLY (SQLite-backed, referencing neither platform's lock face) — a vehicle `Plan_Executability_Audit_v1.md:84` recommended at [HIGH] for U-IS-11, with the binding decision routed to Session 6 at `:230` and NEVER RECORDED. Any `filelock` adoption is a lock-IDENTITY migration at every converted site, not a drop-in (see the filing §3(v)). *(This row's original registration reasoned only from "`msvcrt.locking` is byte-range and exclusive-only; it does not cleanly provide the `LOCK_SH`/`LOCK_EX` split" — true about msvcrt, but insufficient as the reason the fix is hard.)* See `.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md` §3(iv)–(v). A correct cross-platform abstraction preserving those semantics (e.g. a small shared/exclusive lock facade backed by `fcntl` on POSIX and a genuine Windows-native primitive, or a portable third-party lock library) is its own scoped arc, not a hasty `sys.platform` branch bolted onto a PR already touching hash-chain integrity.
- **Close-out steps.** (1) Pick a Windows backend (`msvcrt`-based exclusive-only lock with a documented downgrade of the shared-read guarantee, or a vetted portable library — a real design choice, council-eligible if the tradeoff is non-obvious); (2) wire it in behind the `_IS_WINDOWS` no-op branches of the canonical-file/directory lock context managers and `_DirLock` (the pre-B-46 `_flock` symbol was deleted at PR #1052), also covering the transitional legacy-sidecar protocol retirement once no pre-B-46 writers remain; (3) apply the same fix to `reconciler_pause_resume_substrate`'s `_workflow_lock` so both same-host lock primitives in this codebase reach genuine Windows parity together, not one at a time. **Windows is a design-substrate-contracted host OS with no CI witness and no packaging declaration** (rewritten in place 2026-08-05 at the combined filing): `Target_Stack_Commitment_v1.md:42` (C-STK-10, `[HIGH]`, "macOS, Linux, Windows … without per-OS port forking") + `ADR-F4.md:21`/`:48`, which the `B-40` block at `:536` above already quotes verbatim — a standing divergence from a committed constraint, currently unobservable, NOT an uncommitted surface. **The real gate on this row's half is WITNESSABILITY (no Windows CI exists), not the backend choice** — low urgency, not blocking. *(An earlier phrasing here read "until Windows becomes an active target", which dropped the C-STK-10 commitment when B-40's dismissal propagated to this row.)* **Council:** no for now — narrow implementation-discretion follow-on per C-IS-09 §9.4's own deferred-mechanism framing; council-eligible AT BUILD TIME only if the msvcrt-exclusive-only-lock tradeoff (weaker-than-POSIX shared-read guarantee on Windows) turns out non-obvious once actually scoped. ***ADJUDICATED 2026-08-05*** at the B-93+B-45 combined fork filing — the tradeoff was scoped and **probe-resolved as ORTHOGONAL** to the deadline semantics, so **no convening is owed for readings B, C or D**. The one genuine nameable tension, and the one exception: a **Reading-A** selection (build the Windows backend BEFORE a Windows CI witness exists) owes a dyadic **C3** (information-substrate integrity — an unverified exclusion claim is worse than a declared no-op, because it disarms the refuse-by-default consult at `admin/pause_journal_adoption.py:412`) **⊥ C11** (operator-loop / local-deployment — a committed cross-platform target that never works is a broken commitment) convening before any Windows code is authored.
- **UPDATE 2026-07-27 (grounding pass, re-verified at HEAD `25b44127`) — STILL DORMANT; premise intact, scope WIDER than registered.** Premise re-verified verbatim: `cross_process_ledger_lock.py:80` *(at `:90` at 2026-08-05)* still defines `_IS_WINDOWS = sys.platform == "win32"`; the three lock context managers still open with a bare `yield`/`return` no-op on Windows (`:289-292`, `:409-412`, `:491-494`); no `msvcrt` reference exists anywhere in the repo (0 hits across all non-worktree `.py` files); no Windows CI exists.
- **(a) A THIRD POSIX-only same-host lock primitive landed since this row was written.** `B-73` (PR #1120, 2026-07-26) added `protected_result_store.py`'s `_cross_process_lock()` (no-op guard at `:339-342` — *the `_IS_WINDOWS` branch is at `:363` at 2026-08-05, docstring `:358`*), whose docstring (`:334-337`) explicitly adopts this carve-out — by module name ("matching `harness_is.cross_process_ledger_lock`'s own Windows carve-out"), not by the `B-45` id. It is **exclusive-only** (`fcntl.flock(fd, fcntl.LOCK_EX)` at `:347`), so it maps CLEANLY onto `msvcrt.locking` and does **NOT** add to this row's hard part (the `LOCK_SH`/`LOCK_EX` shared-reader/exclusive-writer split). Close-out step (3)'s "**both** same-host lock primitives" is therefore now understated — widen it to the **three** lock sites: `harness_is.cross_process_ledger_lock`, `harness_runtime.lifecycle.reconciler_pause_resume_substrate._workflow_lock` (`:271`, lock body `:290-300`, with its unguarded top-level `import fcntl` at `:106`), and `harness_runtime.lifecycle.protected_result_store._cross_process_lock`.
- **(b) B-45 has become the codebase's NAMED Windows-platform posture**, cited from 6+ production/test surfaces rather than remaining one module's local carve-out: `audit_writer.py:783` (no `O_NOFOLLOW`/uid validation on win32), `audit_writer.py:1074` (parent-dir fsync skipped), `audit_writer.py:1527` (`tenant_transaction`'s cross-process half a win32 no-op), `redaction_token_audit_map.py:112`, `shutdown.py:117`, `harness-is/tests/test_cross_process_ledger_lock.py:24`, plus three skip-reasons at `harness-runtime/tests/test_u_rt_134_audit_signing_fail_closed.py:1106/1205/1263`. Retiring this row is correspondingly a multi-surface doc sweep, not a one-module change.
- **(c) Blast-radius nuance.** `reconciler_pause_resume_substrate` is imported **module-level** by `bootstrap/factories/r_cxa_2_producer_loop_factory.py:36`, so on Windows that one bootstrap path is an **import-time `ModuleNotFoundError: fcntl` crash**, not merely an unavailable lock. (The package `__init__` remains unaffected, so this row's original "doesn't break basic import" statement is still true as written.) **Council: unchanged — no for now.** *(Superseded 2026-08-05 — adjudicated at the close-out bullet above: probe-resolved, no convening, EXCEPT a Reading-A selection which owes a dyadic C3 ⊥ C11 convening.)*
- **UPDATE 2026-07-30 (post-#1159 gate-log LOW batch) — TWO corrections, neither restructuring the row above.**
- **(d) A FOURTH Windows no-op surface, inside an already-counted module.** Recounted by direct read at HEAD: **three** modules carry same-host POSIX `flock` locks — unchanged, and correctly recorded at (a) above — but they expose **five** distinct `_IS_WINDOWS` no-op branches, because the count above is of *modules* while what a Windows backend must cover is *surfaces*: `cross_process_write_lock` (`harness-is/src/harness_is/cross_process_ledger_lock.py:358`), `cross_process_replace_lock` (`:478`), `cross_process_read_lock` (`:560`), **`cross_process_scope_lock` (`:205`)**, and `protected_result_store._cross_process_lock` (`harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:339` — *at `:363` at 2026-08-05*). `cross_process_scope_lock` — added at **U-MEM-27** (PR #1159) for the memory store's per-root guarded-write hold — is the one this row has never named: it is neither a canonical-file lock nor a directory lock (it locks a *provisioned lock file* under a scope root), so the close-out's "canonical-file/directory lock context managers and `_DirLock`" phrasing does not reach it. It is **exclusive-only** (it builds on `_DirLock`'s `LOCK_EX` face), so it maps cleanly onto `msvcrt.locking` and does **not** add to the `LOCK_SH`/`LOCK_EX` hard part. `_DirLock` itself has no guard of its own (callers gate); `reconciler_pause_resume_substrate` still has none either — its unconditional `fcntl` import is an *import-time* failure, not a no-op, already recorded at (c).
- **(e) The "parity-only" framing is no longer whole — U-MEM-27 made the no-op withhold a CORRECTNESS guarantee.** "Windows sits at exact pre-B-40 parity" holds while the no-op only withholds an *improvement*. It no longer does. `CanonicalMemoryStore.write_record_guarded` (`harness-is/src/harness_is/memory_store.py`) documents its compare-and-commit binding as **cross-process** in its own SCOPE paragraph, and that claim is witnessed against a genuine second OS process (`harness-is/tests/test_memory_store_cross_process_guard.py`) — the binding rests entirely on `cross_process_scope_lock`. On Windows that lock yields immediately, so the binding **silently degrades to in-process-only**: a second same-host process (the documented `harness run` alongside `harness daemon` case) can append superseding provenance between the precondition read and the write it authorizes, and a **promotion activation commits against provenance already superseded**. That is a wrong durable write, not a missing optimization. Still **not blocking** — no Windows CI, no Windows deployment *(the phrase "no active Windows target" was CORRECTED IN PLACE 2026-08-05 per the close-out bullet above: Windows is design-substrate-committed at C-STK-10, merely unobservable)* — but the failure mode to state at build time is now **data correctness**, not parity, and it raises the floor on how carefully the Windows backend's guarantees must be documented.
- **UPDATE 2026-07-31 (at the `B-103` arc) — enumeration refreshed again: FOUR modules / SIX surfaces; status unchanged.** The `B-97`(a) leg promoted `cross_process_journal_lock` to module scope in `harness-runtime/src/harness_runtime/lifecycle/journal_workflow_pause_store.py`; its Windows branch (`:477`) is a **sixth** no-op surface in a **fourth** module — same carve-out, same posture, newly named.
- **Novel posture datum — the first predicate over this row's degradation.** That same module added **`journal_exclusion_is_degraded()`** (`:402`), the first *named predicate* anywhere over B-45's carve-out: callers ask *"can this primitive exclude a straggler appender?"* rather than re-testing the platform, and the **(3b) pause-journal adoption tool consults it to REFUSE BY DEFAULT** where the lock degrades (`admin/pause_journal_adoption.py:412`). This is the first place the codebase **acts** on the carve-out rather than merely documenting it.
- **O-1 (lens-1, `B-97`(b) arc, 2026-07-31) — lock-identity residual of the same primitive, ACCEPTED.** Pause-journal **disposal unlinks the lock file and takes no lock**, so a holder of the *old* inode does not exclude a re-creator (which gets a fresh `O_CREAT` inode). Gated behind `--acknowledge-discarding-recoverable-state`; **accepted residual**. Recorded here because it was recoverable from no durable surface.
- **Cross-ref (2026-07-30).** Sibling lock-substrate row **B-93** (no deadline on any of the blocking-`flock` acquisition sites) registers against the *same* primitive set and is B-45's natural co-rider — see B-93 for the grounded enumeration (**nine** sites / **four** modules as of the 2026-07-31 recount).
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT, the 2026-07-31 enumeration is exact, and the drifted cites are corrected above; disposition VERIFIED-DORMANT with the fix FORK-SHAPED.** Six Windows no-op surfaces re-verified individually across four modules: `cross_process_ledger_lock.py:205` (scope), `:358` (write), `:478` (replace), `:560` (read); `protected_result_store.py:363`; `journal_workflow_pause_store.py:477` — with `_IS_WINDOWS` itself now defined at `cross_process_ledger_lock.py:90`. `reconciler_pause_resume_substrate.py` still carries an unconditional `fcntl` lock at `:294` with **no** Windows guard at all (the import-time-crash case, not a no-op). `journal_exclusion_is_degraded()` (`journal_workflow_pause_store.py:402`, returning `_IS_WINDOWS` at `:414`) and its refuse-by-default consult at `admin/pause_journal_adoption.py:412` both resolve exactly; **zero** `msvcrt` references repo-wide; no Windows CI. The (e) correctness-degradation sharpening also holds — `DerivedIndexInvalidation.commit_seq`'s own docstring now states the Windows uniqueness bound in code, citing this row (`harness-is/src/harness_is/memory_store.py:154-163`). The fix stays a real decision — *though the stated ground was corrected 2026-08-05 (see the close-out bullet): `msvcrt.locking` cannot express the `LOCK_SH` face at `cross_process_ledger_lock.py:330`/`:605`, but that is no longer the reason the fix is hard, because `filelock.ReadWriteLock` expresses it portably. The decision is now which VEHICLE.* *Not re-verified this pass: the full downstream doc-cite inventory a retirement sweep would need — only the six no-op surfaces themselves were re-read.*
- **FILED 2026-08-05 — combined Class 2 fork with `B-93` at `.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md`** (grounding HEAD `966bc658`). ***RATIFIED-DEFERRED the same day** — the "awaiting operator ratification" clause below is **superseded**; **status unchanged (`registered_finding`)** remains true, and is now true **by decision** rather than by pendency. Everything from here on is the pre-ratification record.* Four readings — A full combined fix / **B deadline-now, Windows deferred (RECOMMENDED)** / C Windows-only (dominated) / D defer both under demand tests **D-0** (a second `write_record_guarded` precondition) **D-1** (Windows CI exists) **D-2** (a declared Windows target) **D-3** (an observed stall) **D-4** (a third row against this substrate); runner-up **D**. **The decisive finding for THIS row: its half is gated on WITNESSABILITY, not on the backend pick** — with no Windows CI, landing a backend flips `journal_exclusion_is_degraded()` (`journal_workflow_pause_store.py:414`) to `False` and disarms the refuse-by-default consult at `admin/pause_journal_adoption.py:412`, replacing a declared safe degradation with an unverified exclusion claim. **Council: the `B-93` "only if taken WITH B-45" condition IS met by the combined filing and is PROBE-RESOLVED — the backend choice and the deadline semantics are ORTHOGONAL** (`msvcrt.LK_NBLCK` gives both platforms the same non-blocking-probe shape), so no dyad convenes; **a Reading-A selection, and only that, owes a dyadic C3 ⊥ C11 convening first.** **ZERO spec/plan delta under every reading** — verified, not assumed: C-IS-09 §9.4 (`Spec_Information_Substrate_v1.md:900`) and C-IS-07 §7.4 (`:756`) both defer the mechanism, and the IS spec contains ZERO occurrences of `timeout` / `deadline` / `platform` / `cross-process`.

### B-41 · `_classify_provider_exception` misroutes AuthenticationError to TRANSIENT_RETRY *(surfaced at B-38 grounding, 2026-07-15, advisor()-flagged)*
- **What it is.** `_classify_provider_exception` (`retry_breaker_fallback.py:265-286`) returned `ValidatorRetryExitClass.TRANSIENT_RETRY` for any exception other than `LLMDispatchProviderUnreachableError`/`LLMDispatchPayloadShapeError` — including an SDK `AuthenticationError` (`anthropic`/`openai` `.status_code` 401/403) — running the full retry staircase before eventually exhausting, contradicting the function's own docstring's claim that "AUTH ... are fail-fast."
- **Close-out.** Verified against `Spec_Harness_Runtime_v1.md` v1.99 (canonical HEAD) directly, not from memory: §14.5's C-RT-15 fail-class table states `RT-FAIL-PROVIDER-AUTH` (the raw SDK auth error, 401/403 — NOT a harness wrapper type) is "Raise unmodified"; §14.6 D2's fork-resolution text and its own narrative both independently say AUTH is fail-fast. Three independent spec statements plus the function's own docstring all agreed — this was a straightforward conformance fix, not an open design decision, so no fork was needed. Added a `status_code in (401, 403)` duck-typed guard to `_classify_provider_exception`, placed after the wrapper-type check and before the `TRANSIENT_RETRY` catch-all — deliberately NOT reusing `_classify_breaker_cause` so the two classifiers' control-flow contracts stay independent. This also closes `B-38`'s noted divergence as a side effect (both classifiers now agree on AUTH).
- **Test-witness gap closed.** Added a fail-fast witness test — the naive assertion "exactly 2 total dispatch calls" does NOT discriminate fail-fast (candidate 0 abandoned after 1 call, candidate 1 gets the 2nd) from a buggy retry-then-succeed on candidate 0 (also totals 2 calls); the real witness checks WHICH candidate the 2nd call actually reached. Added a regression-guard test proving 429/5xx still retry (guards against an overly-broad `status_code is not None` mutation, which would wrongly fail-fast rate-limits and server errors). Both mutation-probed against real production-code reversions — the fail-fast test genuinely fails when the guard is reverted, and the regression-guard test genuinely fails when the guard is broadened past 401/403.

### B-42 · `breaker.cause`'s `FIVE_XX_STREAK` is tagged by the terminal exception only, not a verified streak *(surfaced at B-38's own PR, out-of-family Codex round 1, 2026-07-15)*
- **What it is.** `_classify_breaker_cause` tags `FIVE_XX_STREAK` from a single last-attempt exception's `.status_code >= 500` — but `BreakerStateMachine` (`retry_breaker.py`) tracks only an aggregate `fail_count`, not a per-cause failure history. A breaker with `fail_threshold > 1` that trips after a mixed sequence (e.g. a rate-limit failure, then an auth failure, then a terminal 5xx) still reports `FIVE_XX_STREAK` — the name implies N *consecutive* 5xx responses, but only the terminal exception is ever inspected.
- **Close-out.** CLOSED 2026-07-15. Grounded directly against `Spec_Operational_Discipline_v1_32.md` §7.1 (only requires "the classified trip-cause, when known" — no streak-verification mandate) and `BreakerStateMachine`'s own source (only an aggregate `fail_count`, no per-cause history); confirmed no live consumer reads `harness.breaker.cause` today. Chose option (b) from this row's own close-out options: a documentation-only clarification, not new per-cause tracking infrastructure (which would be speculative ahead of any consumer — the same discipline the B-19 redundancy analysis already established). Added a clarifying paragraph to `BreakerCause`'s docstring in `harness_breaker_schema.py` stating `FIVE_XX_STREAK` is tagged from a single terminal 5xx exception, not a verified N-consecutive streak — mirroring the equivalent clarification `_classify_breaker_cause`'s own docstring in `retry_breaker_fallback.py` already carried since `B-38`'s original landing. The spec-committed enum value (`5xx_streak`) is UNCHANGED; only the docstring's description of its derivation is now precise. No behavior/test change; ruff clean; OD breaker test suite (30 tests) still green.
- **Adjacent finding split out, not folded in.** OD spec v1.32 §7.1's own prose still describes `harness.breaker.cause` as "vacuous-today ... absent from every emitted event," which is now stale since `B-38` populated it for 3 of 4 values. Fixing spec prose is a design-substrate edit requiring design-phase posture, not a Phase-7 code fix — registered separately as `B-43` rather than bundled into this row's code-only PR.

### B-43 · OD spec v1.32 §7.1 prose still describes `harness.breaker.cause` as vacuous-today, stale since B-38 *(surfaced at B-42 grounding, 2026-07-15, advisor()-flagged)*
- **CLOSED 2026-07-18 (PR #1050)** — in-place correction of the v1.32 §7.1 `cause` row + change-note historical annotation + `harness-od/CLAUDE.md` mirror, clearance marker `spec-operational-discipline-v1-32-b43-correction-cleared-2026-07-18.md` (ADR-D7 in-place precedent; no contract change).
- **What it is.** `Spec_Operational_Discipline_v1_32.md` §7.1's `harness.breaker.cause` row states: "Vacuous-today, honestly documented ... the attribute is present in the schema (forward-compatible typed slot) and absent from every emitted event until a follow-on classifier arc supplies real signal." `B-38` (PR #1020) landed exactly that follow-on classifier — `_classify_breaker_cause` now populates `RATE_LIMIT`/`AUTH_FAILURE`/`FIVE_XX_STREAK` at all 3 real `record_failure()` call sites and the attribute reaches the real span. The spec prose was never updated to reflect this and is now factually wrong about current runtime behavior for 3 of the 4 enum values (`CAPABILITY_SHORTFALL` remains genuinely vacuous, per `B-38`'s own grounding).
- **Current state.** FIXED at PR #1050 (see the CLOSED line above): the design-phase posture + clearance-marker requirements this row anticipated were satisfied by the correction PR itself; the historical constraint text below is preserved as the registration-era record.
- **Close-out steps (historical — superseded by the in-place correction; the v1.33-delta route was NOT taken because v1.33 was already consumed by the SigningBackend seam and v1.34 is reserved by the pending B-51/B-52/B-54 amendment).** The original plan: a future design-phase session should author a narrow ADDITIVE prose-only delta correcting the §7.1 `cause` row's "vacuous-today" language to reflect `B-38`'s landed classifier, then file the matching clearance marker. The same stale-vacuous framing likely also appears at `harness-od/CLAUDE.md` §1.2 (spec-pointer summary) and §4.1 (graduation-note context) — both mode-agnostic, not design-substrate, so those ARE Phase-7-editable, but were deliberately left untouched here to keep this a pure registration pass; the design-phase pass on the spec itself should sweep all copies at once rather than fixing them piecemeal across postures. **Council: no** — this is a narrow prose-accuracy delta over an already-cleared spec version (v1.32 stays canonical), not a contract change.

---

## 2. Provenance + method

- **Two tiers per advisor guidance:** Phase-8 *closure residuals* (Tier A — declare Phase 8 done) kept distinct from *post*-Phase-8 forward activation (Tier B); the retirements are **legitimate per X-AL-2** (the H_E scaffold was displaced; production exercise was never an X-AL-2 condition) — this register is the *next axis* of work, not a correction.
- **CXA status spine = the R-700 dispositions** verified against the ledger + merged at PR #207 (CXA-1 PARTIAL / CXA-2 STILL-BOUNDED / CXA-3 STILL-BOUNDED / CXA-4 PARTIAL / CXA-5 RETIRED). Subagent reports supplied close-out *mechanism* detail only — three subagent CXA *status* claims were rejected (CXA-3 "N/A" was a misread; CXA-4 "fully-wired" contradicted R-700 PARTIAL; an "RT-35 PR #52 awaiting merge" claim was stale).
- **Surface IV centerpiece empirically verified this session:** `routing_core_surface.infer()` raises `NotImplementedError` (`:83`/`:97`); `layered_routing_strategy.route()` has zero non-test callers; **but** `retry_breaker_fallback.py` (C-RT-16) *does* implement fallback-chain advancement (the subagent's "fallback not implemented / re-raises" claim was corrected). Accurate framing: provider construction + failure-time fallback wired; capability-aware routing-*selection* stubbed; multi-provider operation **unexercised at MVP**.
- **Council discrimination per CLAUDE.md §13.2 + §10.9:** register-compilation is descriptive synthesis (solo + advisor, not council); 7 forward arcs flagged `⚖️ council-eligible` with named two-voice tensions for when the operator opens them (the roadmap already encodes this on R-410/411/412).
- **Decomposition obligation:** this register discharges the roadmap §9 decomposition owed for Surfaces IV/VI/IX/X and supplies the substance for the `R-NNN` coverage gap (A-3 / B-14) R-700 surfaced.
- Grounded in: ADR-F1/F4/F5 + ADR-D2/D3/D5/D6; CP spec v1.30 `C-CP-01..04`; OD spec v1.27 `C-OD-10/13`; AS spec `C-AS-05/13/15`; runtime spec v1.41 `C-RT-16/22/29` + §14.9.8 + §14.C/D; CXA v2.18 §2.3.x; production source at `harness-{cp,runtime,od,as}/src/`.

### B-66 · B-60 fence-consult order at admission discrimination has no single-site witness *(registered at the B-60 close, merge-gate test-witness lens, 2026-07-21)*

- **What it is.** The round-10 consult-before-admission-discrimination order and the `_cancel_worker` pre-marker consult have no single-site behavioral discriminators — outcomes are pinned jointly with the B-61 drain fence and the post-marker sibling respectively (lens-verified non-vacuous: disabling BOTH layers fails the witness).
- **Posture.** Strictly-earlier semantics; in-code order comments at all four sites are the guard; an implementation-order spy would be a change-detector, not a witness — build only if a regression occurs. Accepted at merge-gate round-2 APPROVE.
- **UPDATE 2026-07-27 (grounding pass, re-verified at HEAD `25b44127`) — STILL DORMANT; every load-bearing claim holds.** The round-10 consult-before-admission-discrimination order is intact at BOTH proceed sites: `_consult_dispatch_fence()` at `workflow_driver.py:9078` *(at `:9114` at 2026-08-05)* precedes the `isinstance(_admission, SubAgentDispatchCapacityError)` discrimination at `:9083` *(at `:9119`)* in `_proceed_branch`, and `:13540` *(at `:13576`)* precedes `:13545` *(at `:13581`)* in `_proceed_worker`. The B-61 per-append drain fence is live (`:2003-2027` — each physical append guarded by its own `token.effect_entry()`). All **23** `test_b60_*` witnesses are unchanged and green across their 7 carrier test modules.
- **Explicitly recorded to prevent a future false-positive.** B-80's `compute_effect_fence_tree_wide_abort_present` (`workflow_driver.py:3009-3058`, landed PR #1131) is a **RESUME-time `EffectFenceResolution.ABORT`** mechanism, unrelated to B-60/B-66's **dispatch-time cooperative cancel token** — two mechanisms sharing the word "fence", with no interaction. Verified structurally rather than by reading alone: the combined B-79/B-80 source diff (`f4696aa0~1..25b44127` over `harness-*/src`; 548 added / 6 removed lines) contains **ZERO** hits on `B-60`, `B-66`, `B-61`, `_consult_dispatch_fence`, `_DISPATCH_CANCEL_TOKEN_VAR`, `effect_entry`, `_cancel_branch`, `_cancel_worker`, `wait_ack`, or `drain_branch_buffers`.
- **Minor row imprecision corrected in passing.** Claim (2) names only `_cancel_worker`'s pre-marker consult, but the "four sites" the posture cites also include `_cancel_branch`'s structurally-identical pre-marker consult at `:9532` (its post-marker sibling is `:9547`; `_cancel_worker`'s pair is `:13945`/`:13960`). The four order-bearing sites are therefore `_proceed_branch:9078`, `_proceed_worker:13540`, `_cancel_branch:9532`, `_cancel_worker:13945`. **Posture unchanged — accepted residual. Council: no.** *(All eight cites in this bullet drifted +36 and are superseded by the 2026-08-05 re-check below.)*
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT; disposition VERIFIED-DORMANT; ALL LINE CITES IN THIS ROW REFRESHED.** All four order-bearing sites verified present with the correct enclosing functions (checked programmatically, not by eye) in `harness-cp/src/harness_cp/workflow_driver.py`: `_proceed_branch` — `_consult_dispatch_fence()` at `:9114` precedes `isinstance(_admission, SubAgentDispatchCapacityError)` at `:9119`; `_proceed_worker` — `:13576` precedes `:13581`; `_cancel_branch` pre/post-marker pair at `:9568`/`:9583`; `_cancel_worker` pair at `:13981`/`:13996`. Order intact everywhere; posture unchanged (build an implementation-order spy only if a regression actually occurs). The uniform **+36** drift comes from the unrelated **B-107/U-CP-64** arc growing the file, not from any change to these sites. *Not re-run this pass: the 23 `test_b60_*` witnesses — this was a read-only sweep, so their green status is unconfirmed here.*

### B-67 · Protected result store's TTL GC sweep has no shutdown-time hook — bootstrap half only *(registered at the B-65-A impl arc, out-of-family Codex round 1 [P1], 2026-07-22; CLOSED at round 4 correction, same date)*

- **What it is.** CORRECTED at out-of-family Codex round 4: this row's own premise ("this runtime has no graceful-teardown chain to hook") was FALSE. `shutdown(ctx)` at `harness_runtime/shutdown.py` IS that chain — it is invoked unconditionally in `run()`'s/`resume()`'s `finally` block regardless of workflow outcome (verified by direct read of `api.py` lines ~671/941). The row's earlier reasoning conflated this with the SEPARATE signal-handler drain path (`admin/shutdown_cli.py`, which only sets `drained_flag`) — that path is real but immaterial here: `shutdown()`'s own step 1 sets `drained_flag` too, so whether a signal pre-emptively flips it or not, `shutdown()` still runs once `run()`/`resume()` unwinds.
- **Fix landed.** A new step 5b in `shutdown()`: a bounded, best-effort `ctx.protected_result_store.gc_sweep()` off the default executor via `asyncio.to_thread` — safe here specifically because `shutdown()` only runs AFTER `execute_workflow` has already returned, so no sync driver can be occupying the default pool concurrently (unlike the request-path raise sites this same round also fixed to avoid that exact deadlock class). The `stage_4_od.py` comment carrying the false premise was corrected in the same commit. Both halves are now wired: bootstrap reaps a PRIOR process's abandoned entries; shutdown reaps THIS process's own before exit. Residual: only a hard kill (SIGKILL/segfault) that bypasses Python's `finally` entirely still relies on the next process's bootstrap sweep — the same case the bootstrap half was always designed for.
- **Council: no** — corrective wiring fix once the false premise was caught by review, not a design fork.

### B-68 · Protected result store's opportunistic GC can race a concurrent in-flight publication under a short TTL *(registered at the B-65-A impl arc, out-of-family Codex round 11 [P2], 2026-07-22; CLOSED 2026-07-25 -- in-process synchronization scope; cross-process residual split to B-73)*

- **What it is.** `write_once()` captures `_StoredEnvelope.written_at` via `time.time()` BEFORE serialization/encryption/`_publish_atomic`'s fsync+`os.link` commit — a real, non-instantaneous gap. A CONCURRENT sweep (a different in-process opportunistic call from another request, or the bootstrap/shutdown-half sweep from a co-resident process sharing the same store directory) that runs during that gap decrypts the envelope, computes `current_time - written_at`, and — if the deployment-configured `ttl_seconds` (operator-settable, `gt=0.0`, no floor) is shorter than the write's own encrypt+fsync latency — can judge the just-published entry already expired and unlink it before `write_once()` even returns. The caller then holds a "live-looking" `composite_key` whose first `read()` raises an unhandled `FileNotFoundError` instead of the typed `ProtectedStoreCrossTenantError`/`ProtectedStoreTamperError` refusals the store otherwise guarantees.
- **Fix landed (2026-07-25, branch `b68-protected-result-store-gc-race-mtime-authority`, 5 rounds of out-of-family Codex review).** (1) `gc_sweep()`'s age authority switched from the pre-publish `_StoredEnvelope.written_at` field to the committed entry file's own filesystem mtime, explicitly refreshed via `os.utime` + its own fsync AFTER the durable `os.link` commit — closing the original timing gap. (2) A process-wide `threading.Lock` registry keyed by the store root's RESOLVED path (`_root_locks`, module-level, not per-instance) now serializes `_publish_atomic`'s entire publish critical section against `gc_sweep`'s entire scan — closing same-process races, including a co-resident concurrent-INSTANCE race (e.g. across bootstrap/resume cycles) the original registration did not anticipate; the lock key uses the root's filesystem identity (`(st_dev, st_ino)`, falling back to the resolved path only before the directory exists) so filesystem-equivalent path aliases (e.g. a `..`-containing spelling, or a case-folded spelling on a case-insensitive volume) share the same lock, and the temp-file's full lifetime (creation through the post-commit mtime-refresh fsync) is inside the lock so a sweep cannot reclaim an in-flight temp file mid-publish. (3) `read()` degrades a missing entry to a typed `ProtectedStoreEntryNotFoundError(FileNotFoundError)` — the optional interim hardening this row's close_out named — instead of a raw `FileNotFoundError`. Each fix is witnessed by a deterministic (non-flaky, mechanism-level) mutation-probed test: reverting the fix makes the corresponding test fail; restoring it passes. 10 new tests added across all rounds; full `harness-runtime` + `harness-cp` suites green.
- **Frequency correction (merge-gate round-1 concurrency lens, PR #1078, 2026-07-22).** Independently confirmed the race is real and reachable via genuine multi-threading, and sharpened this writeup's own framing: `shutdown()` step 5b fires `gc_sweep()` UNCONDITIONALLY once per `run()`/`resume()` call, with no interval throttle — unlike the in-write opportunistic trigger's `_OPPORTUNISTIC_GC_INTERVAL_SECONDS = 300.0` gate. On a daemon serving many concurrent short-lived workflow runs against the SAME shared store instance, concurrent-sweep frequency is materially higher than a reading based only on the 300s opportunistic throttle would suggest — the fix above closes the race regardless of frequency for the same-process case.
- **Residual split out (round 5 convergence check).** Out-of-family Codex round 5 confirmed the in-process fix is sound but restated the SAME cross-PROCESS gap this row's fix comments always disclosed ("a genuinely separate OS process sharing the store directory remains an open residual, registered, not silently assumed closed") — two co-resident daemon PROCESSES each hold their own independent `_root_locks` registry, so a `threading.Lock` alone cannot exclude them. Not a defect introduced by this fix; split to **B-73** as its own registered row rather than expanding this arc's scope, per the terminal register discipline (B-64/B-67 precedents).
- **Council: no** — narrow TTL-dependent race with a documented reachability precondition; the in-process fix was a known primitive (a resolved-path-keyed lock), not a judgment call.

### B-69 · `resume_handle` crash-recovery resume path cannot address individual paused children for multi-child HITL delivery *(registered at the B-39 spec-leg PR #1092, out-of-family Codex round 3 (branch-vs-main), 2026-07-23; council convened + spec leg applied 2026-07-30)*

- **STATUS 2026-07-31 — Status flipped `open` → `closed`. IMPL LEG LANDED (PR #1166).** U-RT-148 (Runtime plan v2.55) + the U-CP-64 amendment (CP plan v2.47) + NEW **U-OD-57** (OD plan **v2.31** — the delta OD spec v1.36 §30.5.2 held for the impl leg; carrier **option (b), a sibling payload type**, selected). All **16** U-RT-148 ACs and all **10** U-CP-64 ACs met **by EXECUTION**; the A′ ordering honored at **commit** granularity (the fence lands before the accessor is reachable at any commit); W1 / W2 / W2′ / W3 / W4 all landed, with **W3 mutation-probed green**. **Both conjuncts of the closure criterion below are therefore satisfied.** Residuals surfaced and **REGISTERED rather than absorbed**: `B-100` (the empty-key carve-out does not reach the LINEAR/orchestrator fence carriers) and `B-101` (accessor-derived provenance is lost through base-typed Pydantic serialization). `pr: '#1166'` — reconciled.
- **STATUS 2026-07-30 — `registered_finding` → `open`. SPEC LEG APPLIED.** Council record `.harness/council-b69-pause-state-accessor-2026-07-30.md` (C10 + C11 + C9, converged, `CLEAR-TO-COMMIT`); **operator ratified OPTION A′ — CO-REQUISITE, SEQUENCED — 2026-07-30** (record §14). Landed: Runtime spec v1.106→**v1.107** (NEW §14.14.9 accessor + closed four-variant projection DTO + five-member cause attribution + declared postures; NEW §30 refusal-only staleness precondition + cause attribution on the EXISTING `RT-FAIL-RESUME-HANDLE-UNKNOWN`, **no new peer class**; the `:3274` scope-limit sentence **discharged** to a cross-reference; NEW §14.14.8 **append-only/never-truncated** substrate invariant); CP spec v1.111→**v1.112** (REQUIRED non-downgradable `ResumeContext` response-provenance carrier; NEW public projection-returning surface over the two private resume-tree walks; two registered scope-limit lifts); OD spec v1.35→**v1.36** (NEW §C-OD-30.5 emission home); CXA v2.22→**v2.23** (classification-only: **NO new row**, aggregate FROZEN at 111, payload widening recorded); plans Runtime **v2.55** (NEW U-RT-148) + CP **v2.47** (U-CP-64 amended).
- **Closure criterion — X-AL-2 conjunctive form, verbatim.** ***B-69 closes iff (accessor landed) ∧ (precondition landed ∧ exercised by the W3 mutation-probe); partial is non-closure.*** Spec surfaces and impl units MAY land in separate merges within the arc, subject to one ordering constraint: **the staleness precondition lands first or simultaneously — never after.** No interval may exist in which an unfenced accessor is reachable on `main`.
- **Why the accessor may not ship alone.** `resume()` re-derives the **snapshot** from the journal but never re-derives the operator's **response map** — two different objects, only one is re-derived — and CP spec v1.106 §1.2 property 4's sole-member carve-out **permits** a uniform `hitl_response` to resolve the single unaddressed gate-owning branch of the live cycle. A superseded read can therefore land a response on a branch the operator never saw, reachable through the **1→1 single-location happy path**.
- **Routed OUT, not absorbed:** `B-97` (pause-journal tenant binding + unserialized `_append`), `B-98` (gate description absent from durable state), `B-99` (the `summary_text` reopening condition). Folded INTO the impl arc: the Class 3 cite-hygiene fix for **three** stale docstrings in `pause_resume_protocol_types.py`.
- **What it is.** `Spec_Control_Plane_v1_106.md` §0's `ResumeContext.hitl_responses` (keyed by a paused child's own `run_id`, read off `PausedChildBranchResumeState.child_snapshot.run_id`) requires the caller to already possess a prior `RunResult.pause_snapshot` object — true on `api.resume()`'s `pause_snapshot`-supplied resume mode. `api.resume()`'s OTHER supported mode, `resume_handle` (crash-recovery: the caller supplies only a `workflow_id`; the harness reads the latest durably-journaled snapshot itself from its own `JournalWorkflowPauseStore`, `Spec_Harness_Runtime_v1.md` §14.14.8), gives the caller NO prior snapshot to read a paused child's `run_id` from BEFORE `resume_context` must be constructed and passed to the SAME `resume()` call — there is no intermediate step where the caller can inspect which children are paused and by what `run_id` before committing to a `resume_context` value.
- **⚠️ HISTORICAL — the three bullets below (`Current state`, `Close-out steps`, `Council: TBD`) describe the PRE-2026-07-30 state and are SUPERSEDED by the STATUS bullet at the top of this section. They are preserved verbatim as provenance, NOT as operative instructions.** The accessor has been **designed and specified**; the council **has run and converged**; the exposure tradeoff they anticipate as the likely operator question **was found not to be one** (the accessor grants a strict subset of the authority `resume()` already grants the identical caller). **Do not re-open the design or re-convene the council on the strength of these bullets** — the live next step is the impl arc (U-RT-148 + the U-CP-64 amendment).
- **Current state** *(historical, pre-2026-07-30)*. Registered, not built (narrowed out of the B-39 spec leg's scope per advisor()-recommended narrowing, round 3 correction — solving this would require designing a new read-accessor, a materially bigger surface than the spec leg's addressing-scheme fix). `Spec_Control_Plane_v1_106.md` §0 states this as an explicit scope limit: `hitl_responses` multi-child addressing works only on the `pause_snapshot`-supplied resume path; a `resume_handle` caller is limited to the single-paused-child case (the uniform `hitl_response` field only, valid per CP spec v1.106 §1.2 property 4 when exactly one child is paused this cycle). `Spec_Harness_Runtime_v1.md`'s `resume()` invariants (the "Resume-context one-shot delivery" bullet) carry the matching Runtime-owned cross-reference. **GENERALIZED 2026-07-24** (out-of-family Codex, `B-70` spec-leg round 5): `Spec_Control_Plane_v1_107.md` §1's `resume_handle` scope-limit note for effect-fence pauses cites this row as already covering the gap, but this row as previously written named only HITL child `run_id`s, not effect-fence `idempotency_key`s — the accessor's design scope is widened here (durably-journaled `run_id`s AND `idempotency_key`s) so that citation is accurate rather than merely asserted.
- **Close-out steps** *(historical, pre-2026-07-30 — DISCHARGED by the spec leg; the live close-out is the X-AL-2 conjunctive criterion in the STATUS bullet above)*. Design a durable-pause-state read accessor (Runtime-owned, since it reads `JournalWorkflowPauseStore`, §14.14.8) that a `resume_handle` caller can invoke BEFORE constructing `resume_context`, to learn the currently-paused children's `run_id`s (and, ideally, enough context per child — e.g. the gate description/workflow position — to compose a sensible operator response) for the SAME `workflow_id` the caller intends to resume. This is a genuine new API surface, not a mechanical patch — likely council-eligible if the tradeoff between exposing partial durable state pre-resume vs. keeping `resume()` a single atomic call is non-obvious once scoped. **Council: TBD at build time** — narrow implementation-discretion follow-on in scope shape, but the read-accessor's own API design (a new function? an optional `resume()` pre-flight mode? a field on `RunResult`?) is a real design choice worth a dyadic council pass if it turns out non-obvious.

### B-70 · `effect_fence_resolution_for`'s uniform-fallback semantics share B-39's cross-branch-misapplication hazard, unfixed in the already-shipped mechanism *(registered at the B-39 spec-leg PR #1092, out-of-family Codex round 3 (branch-vs-main), 2026-07-23; GROUNDED + fork filed 2026-07-24; SPEC LEG LANDED 2026-07-24 — CP spec v1.107 + plan v2.43; IMPL LEG LANDED 2026-07-25 — PR #1101, CLOSED)*

- **What it is.** `Spec_Control_Plane_v1_106.md` §1.2 property 4 (round-3 correction) closes a safety gap in the NEW `ResumeContext.hitl_responses`/`hitl_response_for` mechanism: when 2+ children are concurrently paused, an operator supplying only the uniform `hitl_response` field (no keyed map) must NOT have that response silently applied to every unaddressed child — the resolver must instead leave unaddressed children paused/INERT. The SHIPPED, ALREADY-CLEARED sibling mechanism this pattern was modeled on, `ResumeContext.effect_fence_resolutions`/`effect_fence_resolution_for` (§26.8.1, unamended by the B-39 arc), has the IDENTICAL uniform-fallback shape and the IDENTICAL gap: with 2+ children concurrently effect-fence-paused, a uniform `effect_fence_resolution` (no keyed map) resolves EVERY unaddressed child to the SAME SKIP_AS_FIRED/RE_FIRE/ABORT judgment, which can be wrong for some of them (e.g. one child's non-idempotent effect genuinely fired and needs SKIP_AS_FIRED while a sibling's did not and needs RE_FIRE).
- **Current state.** GROUNDED 2026-07-24 (autonomous-loop grounding pass, `.harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md`) — direct read of `workflow_driver.py` confirms the mechanism (the singular `resume_context.effect_fence_resolution` is consulted independently at every `execute_workflow` recursion depth whose own `resume_snapshot.effect_fence_resume` is set, with no cross-depth uniqueness check) and resolves the two open grounding questions the register originally deferred: (1) NO container/gate-owning split is needed (unlike HITL) — `EffectFencePausedBranchResumeState`'s own docstring + `Spec_Control_Plane_v1_106.md` §2 (REMOVED) both confirm effect-fence carriers never cross a recursion boundary the way HITL child-pause delivery does (a nested descendant's own effect-fence pause propagates via the generic `PausedChildBranchResumeState`/`SubAgentChildPausedError` path, never via `EffectFencePausedBranchResumeState`, so every captured fence-pause entry is inherently gate-owning and already `idempotency_key`-unique across depth); (2) the fix DOES need its own CP spec property (a §26.8.1 companion mirroring §1.2 property 4's safety+liveness shape, simpler — no property-5 equivalent) before code, since restricting the auto-fallback is an observable CONTRACT change to a cleared mechanism, not pure impl-to-spec. **SPEC LEG LANDED 2026-07-24** (operator ratified "open the spec-leg now" via `AskUserQuestion`) — `Spec_Control_Plane_v1_107.md` NEW §1 (the safety+liveness invariant, sharpened across 5 out-of-family review rounds from the fork's §3 draft — round 1 added the previously-omitted ORCHESTRATOR carrier; round 2 fixed a gap where the LINEAR consume site never calls `effect_fence_resolution_for` at all, but its own fix over-corrected; round 3 found round 2's fix would permanently livelock any 2+-simultaneous-LINEAR-pause resume, corrected to genuine map-addressability via the LINEAR carrier's already-existing `idempotency_key` field; round 4 added a `resume_handle` scope-limit note mirroring `B-69`; no property-5 equivalent still holds per the grounding above) + `Implementation_Plan_Control_Plane_v2_43.md` (ONE deferred coverage-matrix row, zero unit amendment — mirrors how v2.42 §5 deferred the parallel `hitl_responses` property 4/5 invariants) + 2 clearance markers. Status flipped `registered_finding` → `design_substrate_gated` → `open` (spec-leg gate cleared; impl leg — the resolver's tree-walk enumeration of the unaddressed effect-fence-pause set, wired at the LINEAR + ORCHESTRATOR + two fan-out consume sites — is the next buildable slice, NOT yet opened, per the B-33/B-39/B-59 precedent). `council: no` unchanged (mechanical parity, no cross-domain tension). **IMPL LEG LANDED 2026-07-25** (PR #1101) — `compute_effect_fence_uniform_fallback_eligible_key`/`_collect_effect_fence_idempotency_keys` (a tree-walk across all three carriers) + `_resolve_effect_fence_gated` (a genuine caller of the existing `effect_fence_resolution_for`, per a merge-gate spec-conformance BLOCK caught pre-merge — an earlier draft reimplemented its logic inline) wired at the LINEAR site (round-3-corrected to become map-addressable), the ORCHESTRATOR's own dispatch, and both fan-out consume sites; `effect_fence_uniform_fallback_eligible_key` threaded end-to-end `mcp_server.py` → `execute_workflow` → `StepExecutionContext` → `sub_agent_dispatch.py` → `child_workflow_runner.py`, mirroring the existing `hitl_uniform_fallback_eligible_run_id` wiring. 2 merge-gate rounds (round 1 BLOCK on spec-conformance + test-witness, both fixed; round 2 all-approve); 1767 harness-cp + 3046 harness-runtime tests green. **Status flipped `open` → `closed`.**
- **Close-out steps (round-7-corrected — do NOT copy the round-3-vintage phrasing below verbatim; see the correction note).** Extend `effect_fence_resolution_for`'s CONSUMPTION contract (not necessarily the field/method signature itself) with the SAME KIND of multi-branch fallback-safety invariant B-39's `hitl_response_for` now carries at its FINAL, round-7-corrected shape (`Spec_Control_Plane_v1_106.md` §1.2 property 4) — a deterministic SAFETY rule (uniform fallback resolves a branch only when it is the sole unaddressed member of the relevant set this cycle) stated as an invariant on the OUTCOME, not a prescribed counting mechanism. **Correction note:** an earlier draft of this row said "the resolver must count concurrently-effect-fence-paused children first and apply the uniform fallback only when exactly one is paused" without B-39's own gate-owning-vs-transitively-paused-container distinction (property 5) — B-39 itself REJECTED an unscoped version of that exact count-based framing at its own round 4 (it strands container branches). Before implementing this row's fix: FIRST determine whether effect-fence pauses have an analogous transitively-paused-container concept under `HIERARCHICAL_DELEGATION` recursion (unresolved as of this registration — `Spec_Control_Plane_v1_106.md` §2's REMOVED-section text claims "the effect-fence mechanism does not cross a recursion boundary the way HITL child-pause delivery does," but this claim was never re-verified against the ACTUAL `EffectFencePausedBranchResumeState`/`effect_fence_paused_branches` carrier code — grep `harness-cp/src/harness_cp/pause_resume_protocol_types.py` before assuming either way). If containers exist for effect-fence too, mirror B-39's gate-owning/property-5 split exactly; if not, the plain count-based rule (no container exclusion needed) is safe as originally drafted. Requires a small design-substrate amendment to CP spec §26.8.1 (a Class 2/3 fork, likely — mechanical parity fix once the container question is resolved) plus updating the callers of `effect_fence_resolution_for` (the SAME `workflow_driver.py` peek-reader sites B-39's own `resume_context_holder` retirement touches — see the B-39 impl leg's own scope-discovery pass) to enforce the gate. **Council: no** — parity fix mirroring an already-ratified pattern (B-39's own property 4), not a fresh open design question.

### B-71 · Operator-facing HITL escalation requests carry no run-instance-distinct correlation identifier, making `hitl_responses` addressing impossible for peer branches sharing a `child_workflow_id` *(registered at the B-39 spec-leg PR #1092, out-of-family Codex round 6 (branch-vs-main), 2026-07-23)*

- **✅ CLOSED 2026-08-14 — IMPL LEG LANDED; READ THIS FIRST. Every bullet below, INCLUDING the "SPEC LEG LANDED" one, is SUPERSEDED HISTORY retained for its findings and NOT a live instruction.** `U-CP-102` + `U-RT-155` co-landed in one arc per their mutual co-land pin (CP plan v2.53 §0.4 / Runtime plan v2.63 §0.4). CP: the brief field (§25.2.Z), the two `StepExecutionContext` carriers (§25.20/§25.21) with the basis derived **UNCONDITIONALLY** at both fan-out composition sites — inside the pre-existing resume-only guard the whole mechanism is inert on exactly the first-escalation path this row exists to fix — the §26.9 persisted echo written from the pause signal's own brief at all four disposition catch sites **and preserved verbatim on the §25.19 warm-up carried-forward path** (a reset there would rotate the key of a gate the operator is still holding), the byte-compat strip so a pre-upgrade durable snapshot re-hashes to its ORIGINAL value, the §2.2.A public projection (omitted when `None`, never `null`), the validator trust seam **overwriting** rather than ignoring a supplied token, and a loud one-way ingress diagnostic. Runtime: the §0.4.3 three-arm read order (the persisted echo wins verbatim and is **never** compared against a recompute), the token folded INSIDE `compose_hitl_action_id` as a `:`-separated suffix reaching **all three** live invocations, and the four §0.6 `payload_body` keys projected through the adapter including the BARE token. **44 witnesses / 24 mutation-probe annotations, verified by 24 distinct mutations in an executed driver, all PINNED**; `just check` 7922 passed / 0 failed. Post-round-7 recount — SEVEN out-of-family rounds plus a 3-lens merge gate were absorbed after the first green build, and rounds 6-7 found defects this arc's own round-5 fix had introduced. Residuals named, not hidden: several probes redden collateral witnesses as well as their own — unavoidable where one mint feeds four consumers, so the plans' "and no other" clause holds only for the surgical probes; and **`B-165`** (the duplicate same-position placement question) is UNCHANGED and still open — this leg does not touch the ratified identity basis.
- **SPEC LEG LANDED 2026-08-13 — READ THIS FIRST; every "Current state" / "Close-out steps" bullet below is SUPERSEDED HISTORY, retained for its findings and NOT a live instruction.** Do **NOT** open a new spec leg. All six artifacts are authored and merged: CP spec **v1.119** (FIVE carrier amendments — brief field + FOUR `payload_body` keys incl. the BARE token; pre-hash basis; persisted echo; echo READ carrier; public correlation projection), Runtime spec **v1.121** (THREE sites — the step-1 minter, the `compose_hitl_action_id` fold at ALL THREE live invocations `hitl_gate_composer.py:1302`/`:1543`/`:2238`, and the step-3 `project_brief_to_payload` projection), CP plan **v2.53** (`U-CP-102`, 16 ACs / 9 probes), Runtime plan **v2.63** (`U-RT-155`, 11 ACs / 6 probes), and clearance markers for all four. **NINE out-of-family review rounds absorbed.** **THE REMAINING WORK ON THIS ROW IS THE IMPL LEG** (`U-CP-102` + `U-RT-155`, co-land pinned — neither is independently observable), and that is what closes it: a spec leg authorizes a repair, it does not close a defect. One residual was registered rather than repaired — **`B-165`**, the duplicate same-position placement question, whose premise is explicitly UNVERIFIED (see that row).

- **What it is.** `Spec_Control_Plane_v1_106.md` §0's `ResumeContext.hitl_responses` (keyed by a paused child's own `run_id`) lets a caller who already KNOWS a paused child's `run_id` construct a correctly-keyed response — but it does not solve how the caller LEARNS which `run_id` a given HITL escalation request corresponds to. The existing operator-facing request types this arc does not touch — `HITLEscalationBrief` (C-CP-28 §25.2, `harness_cp/validator_framework_types.py`: `parent_step_id`, `parent_action_id`, `fail_class`, `fail_detail_hash`, `escalation_reason`, `proposed_response_palette` — no `run_id` field) and the webhook delivery payload the HITL gate composer sends — carry `parent_action_id` and the `hitl:`-prefixed `compose_hitl_action_id(parent_action_id, placement_position)` (`harness-runtime/lifecycle/hitl_gate_composer.py:425-437` — *at `:424-436` at 2026-08-05*: `f"hitl:{parent_action_id}:{placement_position.value}"`), both derived from the SAME workflow_id-scoped, non-run-instance identifier this arc's own `branch_path` keying-defect note already found collides across repeated same-`child_workflow_id` dispatch. Empirically confirmed (direct read of both files, 2026-07-23): neither type carries a `run_id`. Concretely — when two peer branches dispatch the SAME `child_workflow_id` and BOTH hit the identical internal HITL gate, their two escalation requests/webhook payloads are byte-identical on every field an operator or webhook consumer can observe, so there is structurally no way to tell them apart — a human or automated consumer cannot know which `child_run_id` to key a `hitl_responses` entry under for either request.
- **Current state.** *(SUPERSEDED 2026-08-12 — see the DESIGN RECORD bullet at the end of this entry: the council ran, and the identity basis is resolved.)* Registered, not built — narrowed out of the B-39 spec leg's scope (advisor()-pattern narrowing, matching `B-69`'s precedent): `HITLEscalationBrief`/the webhook payload are an EXISTING C-CP-28 contract this delta does not touch, and fixing them is a genuinely separate carrier/design surface from this delta's `ResumeContext` addressing fix. `Spec_Control_Plane_v1_106.md` §0 states this as an explicit scope limit.
- **Close-out steps.** *(SUPERSEDED 2026-08-12 — the `run_id`-threading close-out described here was falsified on 2026-07-27 and again by the v2 witness; the resolved basis is (B), not a `run_id` field-add. Retained for the audit trail, NOT as instructions.)* Amend `HITLEscalationBrief` (and the equivalent webhook payload type, if distinct) to carry a run-instance-distinct correlation identifier — most naturally the paused child's own `run_id` (the SAME value `hitl_responses` already keys by), threaded through at HITL-gate composition time (the composer already has access to the executing step's `run_id` at dispatch). This is a C-CP-28 contract amendment (NEW field, additive/byte-compat if optional) — likely mechanical once scoped, but touches an operator-facing/webhook-external payload shape, so any downstream webhook consumer contract implications should be checked before landing. **Council: TBD at build time** — likely narrow (adding a field mirroring an established pattern), but the operator-facing/webhook-external surface warrants a quick check before assuming so.
- **UPDATE 2026-07-27 (roadmap-continue, `advisor()`-directed premise falsification — DOC-ONLY, no spec/plan change landed, prior draft reverted pre-merge):** the "likely mechanical" framing above is wrong. A `run_id`-threading fix was authored across both CP and Runtime axes (`StepExecutionContext`/`HITLEscalationBrief` gain `run_id`; the webhook idempotency key and payload incorporate it) and taken through 5 rounds of out-of-family `just codex-review-uncommitted`, each catching a genuine defect (Optional-vs-required breakage against a 6th out-of-driver `StepExecutionContext(...)` constructor at `r_cxa_2_producer_loop_factory.py`; a missing `None`-guard that would silently recreate the exact collision for any context lacking `run_id`; a new exception not classified terminal by the driver's `RT-FAIL-HITL-GATE-` prefix nor added to `retry_breaker_fallback.py`'s allowlist; wrong unit ownership, U-RT-58 vs U-RT-94). **Round 5 found the fix's premise itself is false:** `RuntimeHITLGateComposer` fires `_escalate_to_secondary_channel` INSIDE the PARENT's dispatch of a `SUB_AGENT_DISPATCH` step, BEFORE `RuntimeSubAgentDispatcher` mints any child `run_id` — `execute_workflow`'s `run_id` parameter is per-PARENT-RUN, not per-branch, so every peer branch of one fan-out shares the identical value. A `...:{run_id}` idempotency-key/payload suffix therefore does NOT disambiguate B-71's actual collision — it only disambiguates across distinct parent runs, not peers within one. `advisor()` confirmed this against the full transcript and directed a full revert rather than a 6th patch: 5 consecutive rounds, each fixing a different layer of the same wrong approach, is the non-convergence signal (mirrors `[[reviewer-oscillation-register-and-hold]]`), not an invitation to keep iterating. **This is the SAME pre-dispatch-identity problem `B-72` CP spec v1.108 property 6 already owns** — a `branch_index`-derived tree-wide-unique INTERNAL identity, deliberately NOT `hitl_responses`/`run_id`-addressable, per v1.108 §1.1(b): "no `child_run_id` exists to key by." The real fix needs a branch-distinct identity co-designed with B-72's mechanism, not a mechanical field-add — and it touches an operator-facing/webhook-EXTERNAL payload, which is itself a design decision, not impl discretion. **Third falsified premise on this row** (registration-time "run_id already accessible at the composer"; round-1 draft's "required field is safe"; round-5's "run_id disambiguates peers") — the pattern across all three is the same: assuming a value threaded at ONE call-graph layer is available/meaningful at a DIFFERENT layer without tracing the actual dispatch path first. **Council flips from "TBD, likely narrow" to genuinely OPEN** — needs a real convening (C1 orchestration + C9/C10 blast-radius/action-safety) co-designed with B-72 property 6's mechanism before the next build attempt. Register status stays `registered_finding` (not `open` — no Claude-closeable slice was actually landed this pass; all drafted artifacts reverted, `git status` confirmed clean of this arc's files before this update was written). *(SUPERSEDED 2026-08-12 — this bullet's closing instruction, "Council flips … to genuinely OPEN … needs a real convening … before the next build attempt", is discharged: the convening happened. See the DESIGN RECORD bullet below.)*
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT and re-confirmed by direct read; disposition FORK-SHAPED.** `HITLEscalationBrief` (`harness-cp/src/harness_cp/validator_framework_types.py:134-151`) carries exactly `parent_step_id`, `parent_action_id`, `fail_class`, `fail_detail_hash`, `escalation_reason`, `proposed_response_palette` — **no `run_id`**; `compose_hitl_action_id` (`harness-runtime/src/harness_runtime/lifecycle/hitl_gate_composer.py:424-436`) still derives `f"hitl:{parent_action_id}:{placement_position.value}"` from the same workflow_id-scoped identifier. Two peer branches sharing a `child_workflow_id` therefore remain byte-identical on every observable field. Both genuine decisions stand: what branch-distinct identity to use (co-designed with B-72 CP spec v1.108 property 6's `branch_index`-derived, deliberately non-`run_id`-addressable internal identity), and whether to change an operator-facing/webhook-**external** payload shape at all. **Do not attempt a build before that convening** — three consecutively falsified premises and an `advisor()`-directed full revert after 5 codex rounds say so. *(SUPERSEDED 2026-08-12 — this bullet's closing instruction, "Do not attempt a build before that convening", is discharged: the convening happened and the basis is resolved. The remaining gate is preconditions 3 + 5 on the spec leg, NOT another council.)*
- **2026-08-12 — DESIGN RECORD v1 LANDED (PR #1319) then v2 (this arc): the basis fork is RESOLVED, and the spec leg is STILL NOT authorizable.** The council convening the two bullets above demanded happened: five voices reconciled to internal zero, three decorrelated reviewers then falsified the load-bearing uniqueness claim, and the record landed at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md` with five hard preconditions rather than spec text. **v2 discharges preconditions 1 + 2 on executed evidence and bounds 4; ~~3 + 5 remain OPEN~~, so no spec leg is authorized.** *(That status line is SUPERSEDED 2026-08-12 by the v3 bullet below: 3 is discharged. **At v4 exactly ONE gate remains: precondition 4 (BOUNDED-not-closed).** 3 closed at v3, 5 closed at v4.)* The witness is `harness-runtime/tests/test_b71_escalation_identity_basis_collision_witness.py` (13 tests, green, mutation-probed four ways across the (A) and (B) paths); every basis it tests is read off a `StepExecutionContext` the real `compose_branch_child_context` produced, over the realizable collision tree — two peer workers dispatching the SAME `child_workflow_id`, the shape `compose_child_run_id_seed`'s own docstring names as live (`sub_agent_dispatch.py:371-380`). **RESULT: candidate (A) `(parent_action_id, branch_index, placement)` is FALSIFIED** — `_parallelization_fanout_action_id` (`workflow_driver.py:7168-7177`) takes `workflow_id` and nothing else, so two sibling child runs compose the identical triple and the collision propagates through the real `compose_hitl_action_id` into the webhook `Idempotency-Key`, the CP audit `action_id` and the F2 ledger key at once (`orchestrator_action_id` is run-blind the same way at `:12137`, so this is not PARALLELIZATION-local). **The v1 escalation is CORRECTED, downward:** verbatim inheritance of `parent_idempotency_key` through `compose_branch_child_context` is real but *within-run*, where `branch_index` still separates the peers — nested fan-out is *cross-run*. The council's own `(parent_idempotency_key, branch_index, placement)` triple therefore survives uniqueness; it loses on precondition 4: `entry_version` is folded into `run_idempotency_key` (`:3312-3316`), so INSIDE the mint→persist recompute window — where the paused child's `run_id` is reused verbatim (`child_workflow_runner.py:230-234`) — a bump rotates (C) while (B) reproduces its token. (On the ordinary resume path the persisted echo is read and nothing is recomputed, so no basis rotates there; an earlier v2 draft claimed otherwise and codex round 3 corrected it. The fork's resolution does not rest on this margin — (A) is falsified outright on uniqueness.) **BASIS RESOLVED = (B)**, the run-scoped internal identity + `placement`; its "not in composer scope" objection stands as stated (`StepExecutionContext` carries no `run_id`) but is not a blocker — the driver already composes this identity at `workflow_driver.py:8346`/`:12670`, and threading it is one additive `None`-defaulted context field, the shape the context already carries six times. Recorded as owed work on the spec leg, not as free. **Fourth falsified premise on this row — and the second authored by the falsifier**, which is why preconditions 1 + 2 were written as *execute* and *resolve on that evidence*. **Scope honesty:** the witness covers PARALLELIZATION fan-out; HIERARCHICAL_DELEGATION recursion and the DECENTRALIZED_HANDOFF stage chain (`workflow_driver.py:15375-15441`) are unexamined, and the resume-reuses-`snapshot.run_id` half is CITED, not witnessed. Status stays `registered_finding`.
- **2026-08-12 (v3) — precondition 3 ALSO CLOSED; status is now 1+2+3 CLOSED, 4 BOUNDED, 5 OPEN.** Witness `harness-cp/tests/test_b71_resolvability_cannot_be_a_static_stamp.py` (6 tests; mutation-probed — making the resolver ignore `hitl_responses` turns 3 of the 6 RED). **`resolvability` carries the resolution CHANNEL** — drawn from the EXISTING closed `PauseLocationVariant` (CP spec v1.112 §2.1), so the webhook and the pause view cannot become two authorities over one concept — and never the outcome. The `held-for-sole-resolution` stamp is retired on a **witnessed flip**: hold one pre-dispatch branch and its tree fixed, vary only the `ResumeContext`, and it goes from not-eligible (2 unaddressed gate-owners) to eligible (sole, `workflow_driver.py:2895-2897`) — the claim is ELIGIBILITY (nomination), not delivery: the driver additionally needs a uniform `hitl_response` (a **cited** code read at `workflow_driver.py:8350`/`:12674`, not executed by the witness), a SECOND input unknowable at mint time, which strengthens rather than weakens the case — so a mint-time stamp is a false negative exactly when the operator most needs the truth. **v1's own recommendation is falsified in passing:** the pause view cannot report live status — `project_pause_locations` takes the snapshot alone (witnessed), and `read_paused_workflow_state` is ratified as NOT a liveness claim (**cited, not executed** — a Runtime round-trip is owed) (Runtime v1.110 §14.14.9.1 / `B-104` Reading D: the journal writes no pause-resolved marker, so a resolved pause is byte-indistinguishable from an outstanding one). `resolvability_note` therefore states the sole-member RULE and promises no readout. The surfaced sub-fork (live per-location eligibility in the view) is filed to **`B-155`**, which already owns this population. **D-2's binding is WITHDRAWN (out-of-family rounds 8 + 14 — the round-8 answer was REVERSED on evidence).** A channel-only field is time-*invariant* and D-2's harm is time-*varying* (the palette over-promises only in the 2+ state; it is honest when sole), so `resolvability` **cannot** disarm D-2. The round-8 keyed-vs-uniform split fails because the webhook carries **no ingress keys at all** — the palette there is the admissible response set, not a keyed reply affordance, and a sole pre-dispatch owner genuinely **consumes** the uniform response (`workflow_driver.py:8346-8350`; witnessed by `test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py:576-590` — 1 POST, no re-escalation). Suppressing in both states would hide a VALID action when sole, the same liveness failure precondition 3 closes. **Disposition:** keep the palette; the disarm is **informational** (`resolvability_note` carries the uniform routing + sole-member condition); **D-2 is NOT fully absorbed** and keeps a recorded residual — in the 2+ state the palette still implies an action the resolver refuses, closable only by a time-varying signal this design forbids, the same dependency `B-155` carries. **The closure was CHALLENGED (out-of-family round 16) and HELD.** The rebuttal: a partial resume would re-enter the composer and mint a FRESH escalation describing the sole-owner state, making the stamp's error transient. It fails, and the decisive case is witnessed on the REAL fan-out stack — `test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py` is exactly this population (PARALLELIZATION branch with its own `SUB_AGENT_BOUNDARY` gate, real dispatcher registry + real `RuntimeHITLGateComposer`), and at `:576-590` the branch **consumes** a supplied response: 1 POST, no re-escalation. **NARROWED at round 17**, which supplied a third path the first draft missed: the operator may obey the stamp AND answer the addressable peer via `hitl_responses`, in which case a resume DOES occur and the now-eligible pre-dispatch branch gets `HITLDeliveryCell(None)` and may re-escalate — **that path is UNRUN**, so the stamp is not provably a *permanent* false negative. What survives and still retires it: the stamp is a false negative **at the moment the operator reads it** (witnessed — eligibility flips; a mint-time value cannot track it), and a later correction would mitigate rather than remove that, arriving only after the operator was already told not to act. Precondition 3's ask is that the field **cannot assert** a false negative — a static stamp does; the channel does not. **Owed:** the mixed keyed-peer / no-uniform-response fan-out round-trip (the same one `B-155` carries). *(SUPERSEDED at v4 — precondition 5 is now CLOSED; see the v4 bullet below.)* *(SUPERSEDED at v5 — preconditions 1+2+3+5 were then CLOSED; **precondition 4 closed at v6** — see the v6 bullet below. See the v5 bullet below.)* ~~**TWO gates remain:** (1) **precondition 5**, OPEN; and (2) **precondition 4**, **BOUNDED-not-closed** — it still owes an explicit scope statement AND a live-resume witness for the *cited* `snapshot.run_id` reuse. Named residuals on top (owed, not gates): a Runtime round-trip for the `B-104` liveness limit, a driver round-trip for the keyed-only re-pause behaviour, and the unexamined HIERARCHICAL_DELEGATION / DECENTRALIZED_HANDOFF shapes. Scope: the effect-fence sibling resolver has the same sole-member shape and is unexamined. Status stays `registered_finding`.
- **2026-08-12 (v4) — precondition 5 ALSO CLOSED; `4` was then the sole remaining gate** *(CLOSED at v6 — see the v6 bullet below)*. Witness `harness-runtime/tests/test_b71_escalation_token_rides_the_tracing_export.py` (8 tests, mutation-probed). **The charter's premise is FALSE in effect:** it declined to convene C7 because *"the identity is not a span attribute"*, but §3 lands the widening inside `compose_hitl_action_id` — whose output rides the tracing export as `webhook.idempotency_key` on the outer `hitl.webhook.deliver` span (OD-canonical at C-OD-32), with **no dedicated attribute**. **CORRECTED at round 1: ONE reachable carrier, not two** — `hitl.invocation.audit_ledger_entry_id` is DEFAULT-ON but **unreachable** here, since `_escalate_to_secondary_channel` is `NoReturn` and the `hitl.invocation.opened` span opens only on the fall-through (`hitl_gate_composer.py:2033-2044`). One reachable carrier is enough — and the export half is now **witnessed end-to-end** (**producer→exporter**: the real `_escalate_to_secondary_channel` composes its own `idempotency_key` and threads it to `deliver_webhook_for_brief`, read back off the exported span with nothing in the test supplying it; deleting the production `_set`, or making the helper forward a different value, each turns it RED). The audit attribute is unreachable in **both** venues; note the *timeout* venue DOES open `hitl.invocation.opened`/`timed_out` before escalating, so the "homeless" rationale for declining C1's attribute is scoped to durable-async. **Consequence:** C10's leak bar extends to tracing as a *description of what ships*, and it binds the token's form — basis (B)'s raw identity contains the run_id **verbatim** (witnessed), so hashing must occur **before** the value enters the composer, after which it is already on the exported carrier. **C1's `hitl.escalation.instance_id` is DECLINED** (adds no information; a SECOND authority over one concept, and homeless on the durable-async venue; needs its own C-OD-32/§12.2 amendment; nothing `hitl.escalation.*` exists today, so declining is cheap to reverse). The span-export round-trip is **DONE** (executed, both links mutation-pinned). **Egress — a round-5 draft called it CONDITIONAL from the implementation side; round 6 CORRECTED that against the canonical contract.** OD spec v1.8 §C-OD-32.3 says webhook spans are head=1.0 **always-sampled**, while `hitl.webhook.deliver` is absent from the 19-entry `ALWAYS_SAMPLED_EVENT_CLASSES` that §9.2 defines as exactly that set — a **contract-vs-impl drift on an inviolable §9.3 floor**, now registered as **`B-160`** (not B-71's to fix). The contract governs here per the authority chain, so the egress is **unconditional** — the stronger reading a leak bar must assume; the hashing requirement is unaffected either way. Surviving asymmetry: the always-sampled `hitl.invocation.opened`/`timed_out` are precisely the spans that never carry the token. The witnesses prove **emission**, not **survival** — exercising `materialize_span_processor_stage` is **owed**, and also settles `B-160`. **Still open:** whether an OD `redact_span` surface already filters these is unexamined. Status stays `registered_finding`.
- **2026-08-12 — SPEC LEG SCOPED, NOT LANDED (current state).** A full draft was authored and taken through **four** out-of-family review rounds (14 findings, all absorbed) on branch `b71-spec-leg-v1-119` / **draft PR #1325**. Deliberately not merged: each round kept opening new **structural** surface rather than converging, and round 3 caught a **regression the draft would have landed** — it composed over v1.18 while **v1.19** had already widened `fail_detail_hash`, plus a `§25.2.Y` label collision and the wrong carrier shape (`dataclass` vs Pydantic `BaseModel`). Landing an under-specified canonical contract is worse than landing nothing.
- **The leg's true shape — SIX artifacts, not one** (the durable output of that arc): **(1)** CP spec delta with **four** carrier amendments — `escalation_instance_id`; the pre-hash basis carrier on `StepExecutionContext` (the mint site has **no** run identity without it); the persisted echo on the per-branch pre-dispatch resume state; **and a read path carrying the post-hash echo back to the composer** (without it the persisted-value-wins rule is unsatisfiable). **(2)** A **Runtime spec delta** — `Spec_Harness_Runtime_v1.md` §14.8.8.1 step 2 still specifies the **two-argument** `compose_hitl_action_id` and says the driver does not extract pause state from the signal; both contradict the design. **(3)** CP implementation-plan delta. **(4)** Runtime implementation-plan delta. **(5)** Clearance markers per amended spec. **(6)** Then the impl leg.
- **Also settled by the draft — carry forward, do not re-derive.** The digest must be **pinned** (an idempotency-key formula is a contract surface): sha256 over a domain-separated canonical string, lowercase hex, placement's enum VALUE. The leak bar needs **one scoped carve-out** for the ordinal in prose on `branch_context`, or it contradicts the `branch_context` requirement outright. The ingress **diagnosis must be advisory** until its typed carrier lands (`ResumeResult`/`RunResult` are closed schemas). The palette-suppression binding stays **withdrawn**.
- **2026-08-12 (v6) — precondition 4 CLOSED; ALL FIVE are closed and the spec leg is AUTHORIZABLE.** (A v5 draft claimed this prematurely and was retracted; v6 earns it.) **It also corrects a citation the record had wrong since v2.** Basis (B)'s token is composed from `pre_dispatch_gate_owning_branch_identity(run_id, branch_index)` at `workflow_driver.py:8346`, where `run_id` is **the run executing the fan-out** — for a top-level fan-out, not a child at all. The load-bearing continuity is therefore the **top-level** resume path, `mcp_server.py:371-372` (C-RT-35), **not** `child_workflow_runner.py:230-234`, which applies only when the fan-out is nested inside a child run. Both are now witnessed. **Live-resume witness** delivered on the real stack for the exact population — `test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py::test_b71_resume_preserves_the_run_id_the_pre_dispatch_identity_is_composed_from`: `api.run` → PAUSED → `api.resume` (empty `ResumeContext`) → re-pause, asserting the run_id is identical across both snapshots; mutation-probed (dropping C-RT-35 continuity turns it RED, with the module's own core assertion). **The `durable=False` decision:** the window is **two-mode**, differing in *who* closes it — at `durable=True` the harness journals the snapshot; at the shipped `durable=False` default the **caller** persists `RunResult.pause_snapshot`, a **shipped, tested** capability (`test_api_resume_restart_proof_round_trip` round-trips a real snapshot and resumes to SUCCESS under `PauseResumeProtocolConfig.default()`). A v6 draft called the default *vacuous* because `resume_handle` requires durable; **codex falsified that** — `resume_handle` is not the only post-restart path. At the default the guarantee is conditional on caller behaviour the harness cannot enforce. The residual inside it is not a gate (needs a durability change against §3's ordering — its own arc). **Still cited:** that the durable wrapper journals synchronously at capture. Status stays `registered_finding` — the ROW closes when the spec leg lands.

### B-72 · A `SUB_AGENT_DISPATCH` step's own `SUB_AGENT_BOUNDARY` HITL gate can re-fire on every resume-forward of an already-dispatched, deeper-paused child, discarding the child's `paused_child_branches` cursor *(registered at the B-39 impl-leg Slice B branch `b39-impl-leg-slice-b-hitl-delivery-mechanism`, out-of-family Codex round 4 (branch-vs-main), 2026-07-24)*

- **What it is.** `RuntimeHITLGateComposer.dispatch()`'s Step 0 one-shot short-circuit (`hitl_gate_composer.py` ~1652-1730) only bypasses the gate when `step_context.hitl_delivery_holder.consume_and_clear()` is non-`None`. This session's B-39 Slice B delivery mechanism populates `hitl_delivery_holder` ONLY at the linear `resume_at` reconstruction site in `workflow_driver.py` — it is never threaded to fan-out branch composition (`_execute_parallelization` / `_execute_orchestrator_workers`, which build each branch's `StepExecutionContext` via `compose_branch_child_context` and only pass `resume_context`/`child_resume_snapshot` through, never `hitl_delivery_holder`). So when a fan-out branch's own `step_kind` is `SUB_AGENT_DISPATCH` AND that step carries its own `HITLPlacement(position=SUB_AGENT_BOUNDARY)` declaration, EVERY re-dispatch of that branch (including a pure resume-forward whose sole purpose is carrying `child_resume_snapshot` down to an already-paused descendant) runs Step 0 → finds `hitl_delivery_holder is None` → falls through to full gate re-evaluation. `HITLPlacementTrigger` for `SUB_AGENT_BOUNDARY` (`hitl_placement.py:128-138`) states applicability as "All cells; sub-agent interrupt stranding mitigated via cascade-timeout per C-CP-21 §21.3" — confirming `DURABLE_ASYNC` synchrony is spec-reachable for this placement, and that the spec's own anticipated mitigation for this exact stranding class is a cascade-timeout, not cursor-preservation. If the ancestor's OWN gate re-raises `HITLPauseRequestedSignal` on this re-evaluation (never reaching `inner.dispatch()`, so `child_resume_snapshot` is never forwarded to `RuntimeSubAgentDispatcher`), the fan-out branch-failure capture logic builds a FRESH `PausedChildBranchResumeState` for this cycle representing "paused at its own gate" — with no evident mechanism to nest or preserve the PRIOR round's already-known deeper `child_snapshot` (the descendant that was already dispatched and is already paused further down), because a fresh signal carries no reference to the previous cycle's branch disposition. This is PRE-EXISTING and orthogonal to B-39's own delivery-cell mechanism, not introduced by it: under the OLD, now-retired ctx-level global `ResumeContextHolder`, the SAME ancestor gate re-fire would ALSO occur on every resume-forward (the composer's Step 0 had no first-dispatch-vs-resume-forward distinction then either) — but the OLD holder was tree-wide-shared, so ANY non-`None` operator-supplied value (even one addressed to a different, deeper branch) would satisfy the ancestor's Step 0 and let it proceed to `inner.dispatch()`, accidentally forwarding the child cursor correctly (while risking the OLD mechanism's own already-documented misapplication hazard). B-39's move to per-branch, non-globally-shared delivery removes that accidental masking, making the orphaning failure mode newly *visible*, but the root defect — the composer's dispatch entrypoint cannot distinguish "first dispatch of this branch" from "forwarding an already-dispatched, already-paused-deeper branch" — predates this arc.
- **Current state.** Registered, not built. Reachability confirmed via direct spec-text read (not merely reasoned) — `SUB_AGENT_BOUNDARY`'s "all cells" applicability plus the composer's `applicable_placements={SUB_AGENT_BOUNDARY}` single-instance-per-step_kind binding (`hitl_gate_composer.py:834-849`). NOT built into the B-39 Slice B delivery-mechanism PR — genuinely bigger scope than this arc's delivery-cell mechanism (touches the composer's own dispatch entrypoint and the fan-out branch-failure capture path, not the `ResumeContext`/`HITLDeliveryCell` surface this arc owns), and per advisor()'s framing this is a pre-existing structural gap this arc's B-39 changes make more visible rather than introduce — so it does not block this arc's PR.
- **UPDATE 2026-07-25 (roadmap-continue, fresh empirical grounding pass on the fan-out branch-failure capture logic, no code change owed):** the close-out steps' own open question — "confirm whether a fresh `HITLPauseRequestedSignal` genuinely drops the prior cycle's `child_snapshot` cursor" — is now answered via direct trace, not reasoning: (1) `HITLPauseRequestedSignal` inherits `BaseException` directly, not `Exception` (`workflow_driver.py:~5079-5081`'s own comment on the LINEAR path); (2) the fan-out branch-dispatch try/except chain (`ORCHESTRATOR_WORKERS` ~8680-8800, `PARALLELIZATION` ~12620+) catches only `CancelledError`, `SubAgentChildPausedError`, `SubAgentDispatchCapacityError`, and a bare `except Exception as _branch_exc:` — none of which match a `BaseException`-only signal, so a re-fired ancestor-gate pause is NOT caught by any handler in that chain and propagates out of the branch coroutine; (3) `asyncio.gather(*branches, return_exceptions=True)` DOES capture `BaseException` subclasses as ordinary result-list entries — confirmed by this same file's own B-60 comment at `workflow_driver.py:1891-1898` ("`return_exceptions=True` captures BaseExceptions — including `DispatchFenceTrippedSignal` — as ordinary results"), which is the PRECEDENT this workspace already built a fix for; (4) the only post-gather rescue, `_reraise_captured_fence_signal()` (`workflow_driver.py:1891`, called at ~8906/8928/8963/12858/12880/12911), scans EXCLUSIVELY for `_DispatchFenceTrippedSignal` (+ its `BaseExceptionGroup`-wrapped form) — it has no `HITLPauseRequestedSignal` case, so a re-fired ancestor-gate signal passes through un-re-raised and lands as a plain object in `results_by_ordinal[plan[0]]`; (5) `any_failed = any(isinstance(r, BaseException) for r in results)` (`workflow_driver.py:9011`/`12953`) DOES correctly classify it as a failure (this is `BaseException`-checked, not `Exception`-checked, so it is not silently read as success) — but since no `SubAgentChildPausedError` was raised this cycle (the ancestor's OWN gate fired, not the descendant's), `paused_child_dispositions` gets no entry for this branch, and under `proceed` the run finishes `_finish(RunStatus.PARTIAL, fail_class=None, salvage=True)` with the deeper descendant's `child_snapshot` cursor nowhere captured. **This confirms the register's suspected mechanism is structurally sound** (not merely plausible) for the PROCEED-tier fan-out path; the CASCADE_CANCEL/PAUSE-tier TaskGroup path (which the B-60 comment also flags for `BaseExceptionGroup`-wrapped delivery) was traced only for the `_DispatchFenceTrippedSignal` case, not re-verified here for `HITLPauseRequestedSignal` specifically — that remains open for whoever builds the reproduction. **Reproduction is still NOT built** — this pass was static-trace-only, deliberately stopping short of the ~700-line real-stack scaffold `test_u_rt_95_hitl_resume_consume_cycle_e2e.py` implies, per this session's own multi-arc length this wake (B-30 ship + a 9-row grounding sweep already landed) and the standing discipline against designing a fix on unverified footing. Next session should build the reproduction directly — this grounding pass narrows exactly what it needs to assert: a fan-out branch whose `SUB_AGENT_DISPATCH` step has its own `SUB_AGENT_BOUNDARY` gate, a resume-forward carrying a real `child_resume_snapshot`, and an assertion that the run ends `PARTIAL`/cursor-lost rather than correctly re-entering the deeper paused child.
- **UPDATE 2026-07-25 (round 2, roadmap-continue, structural reframe — verified, not just reasoned):** `hitl_response_for`'s own docstring (`pause_resume_protocol_types.py:~1069-1098`) states directly that "HOW the resolved per-branch answer physically reaches the resumed step's gate composer (which parameter, which call site, at which recursion level) is deliberately UNSPECIFIED here" and that a container/ancestor branch "is simply re-entered/recursed into, unconditionally, by whatever mechanism the impl leg lands (**deferred, §1.3**)." Cross-checked against the code, not just the docstring's claim: `grep -n 'HITLDeliveryCell(' harness-cp/src/harness_cp/workflow_driver.py` returns exactly ONE construction site, `workflow_driver.py:4777`, inside `_execute_workflow_body` (`workflow_driver.py:3126`, single call site at line 3072 — the `SINGLE_THREADED_LINEAR`-only per-step loop). No equivalent construction exists in `_execute_parallelization`/`_execute_orchestrator_workers`/`_execute_hierarchical_delegation`. **This reframes B-72's disposition:** it is not a surprising, accidentally-discovered re-fire bug — it is the CP spec's OWN acknowledged, explicitly-deferred fan-out delivery-mechanism gap (§1.3, "impl discretion"). B-39 Slice B built the CARRIER (`ResumeContext.hitl_responses`/`hitl_response_for`) but explicitly scoped OUT the WIRING that would call `hitl_response_for(child_run_id)` at the right fan-out dispatch site and construct a `HITLDeliveryCell`-equivalent for a branch's `StepExecutionContext` — B-72 IS that deferred wiring, not a separate correctness bug found alongside it. **Retiring the "skip-a-gate vs. strand-a-descendant" council framing** — the spec's mechanism ROUTES a resolved answer to the correct branch via `hitl_response_for(child_run_id)`; it does not skip or bypass gate evaluation. A branch with no resolved answer yet SHOULD re-fire (correct behavior, not a bug); the actual gap is that even a branch WITH a resolved answer waiting has no delivery path, since no `HITLDeliveryCell`-equivalent construction exists for any fan-out topology. **The design-risk flag relocates, it does not disappear:** the same docstring spends a full paragraph on property 4 (multi-child fallback-safety, a black-box invariant the deferred §1.3 resolver must independently satisfy) and explicitly states a PRIOR DRAFT's counting/INERT mechanism was found by out-of-family review to strand a transitively-paused container branch — this is genuine, already-once-falsified design subtlety, not a solved problem. **What remains genuinely unverified (NOT resolved by this citation, still reproduction-owed):** the OBSERVABLE runtime failure shape — whether a realistic persona/blast-radius config actually reaches gate re-evaluation rather than auto-approving it away first (`test_u_rt_95`'s own SOLO-persona lesson: `solo_persona_floor_auto` silently auto-approves before synchrony is even evaluated), whether the PROCEED-tier `PARTIAL`/cursor-lost outcome traced statically in the round-1 update above is what actually happens at runtime, and what the CASCADE_CANCEL/PAUSE-tier TaskGroup path does (untraced for this signal type). Keep these as predicted-not-confirmed until the reproduction runs.
- **UPDATE 2026-07-25 (round 3, roadmap-continue, EMPIRICALLY CONFIRMED — reproduction built and run):** `harness-runtime/tests/integration/test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py` (2 tests, both PASS — pin the confirmed broken state directly, not via `xfail`). Deliberately simpler than the original "forwarding an already-paused descendant's cursor" framing — no nested child pause needed to observe the gap, since round 2's grounding found the delivery-mechanism absence is unconditional for ANY fan-out branch resume, not specific to cursor-forwarding. Test 1 (precondition) confirms a `PARALLELIZATION` workflow's single branch, `step_kind=SUB_AGENT_DISPATCH` carrying `HITLPlacement(position=SUB_AGENT_BOUNDARY)`, correctly PAUSES on first dispatch via the REAL `RuntimeHITLGateComposer` (confirms `fold_step_hitl_placements` reaches fan-out branches — the placement producer is NOT the gap). Test 2 pins the core gap with 4 independent oracles: `api.resume(..., resume_context=ResumeContext(hitl_response=APPROVE))` does NOT reach `completed` — `resumed.status=='paused'`, `failure_cause is None`, 2 webhook POSTs (re-escalated, didn't consume), and a DIRECT spy on `RuntimeSubAgentDispatcher.dispatch` (`sub_agent_dispatcher_calls == []`) proving the real dispatcher was never reached, not just that the aggregate status looks wrong. **Refines the observable failure shape** vs. round 1's PROCEED-tier `PARTIAL`/cursor-lost static prediction: this reproduction used `TEAM_BINDING` (→ `cascade_policy=pause`), and the observed shape is cleaner than "lost" — the run correctly reports `paused` again (no state corruption), it simply NEVER converges no matter how many times a resolved answer is delivered, because the delivered value is never consumed. This is effectively an infinite re-pause for any fan-out branch carrying its own HITL gate. The exact terminal status a genuine fix produces is deliberately NOT documented as `completed` — codex round 2 empirically bypassed `RuntimeHITLGateComposer.dispatch` entirely and found the run STILL ends `paused` (`pause_reason='explicit_operator'`) for this test's specific child shape (a separate, not-yet-understood quirk unrelated to the HITL gap, confirmed both by codex's bypass and an independent bypass I ran myself), even though the real dispatcher WAS reached and the branch's own terminal disposition came back `completed`. The dispatcher-spy oracle is therefore the load-bearing, confirmed-safe assertion — not the run's eventual terminal status. The PROCEED-tier `PARTIAL` shape from round 1 remains untested by this reproduction (this test uses `pause` cascade policy only) — a `proceed`-tier variant is a natural follow-on but not required to confirm the core gap. Full harness-runtime suite re-run after adding this test: 3069 passed, 40 skipped, 0 xfailed, 0 unexpected failures — no regressions (`just check`, full monorepo: 6682 passed, 10 skipped, 36 deselected, 0 xfailed). **4 rounds of codex out-of-family review on this PR, all real, all fixed:** (1) a blanket `@pytest.mark.xfail` would have swallowed ANY failure, not just the confirmed one — replaced with direct oracles; (2) asserting only aggregate `status`, and separately proxying "was the child dispatched" via the Anthropic client's call count, were both too weak — codex empirically bypassed the gate and showed the Anthropic-client proxy doesn't discriminate for this child's engine class (`PURE_PATTERN_NO_ENGINE`); replaced with a direct spy on `RuntimeSubAgentDispatcher.dispatch`, independently mutation-probed (0 calls before resume, jumps to 1 when the gate is bypassed — confirmed non-vacuous); (3) this register text itself went stale after fix (2), still describing the retired Anthropic-client oracle — corrected; (4) the test never asserted `resumed.pause_snapshot is not None` — a re-fire that silently dropped the resumable snapshot (leaving the caller stuck, `status='paused'` with `pause_snapshot=None`) would have passed every prior assertion, contradicting the documented "clean, indefinitely resumable" shape; added that assertion plus a workflow-identity check. Final oracle count: 6 (`status`, `failure_cause`, `pause_snapshot is not None`, `workflow_id` preserved, webhook count, dispatcher-spy). **A separate, still-open finding narrows what this reproduction scopes for the multi-branch case (see close-out steps below).**
- **Close-out steps.** Build the deferred fan-out HITL delivery-mechanism wiring: at the fan-out branch dispatch site (or an equivalent seam in `_execute_parallelization`/`_execute_orchestrator_workers`/`_execute_hierarchical_delegation`), call `resume_context.hitl_response_for(child_run_id)` for a gate-owning branch and construct a `HITLDeliveryCell`-equivalent on that branch's `StepExecutionContext` (mirroring `workflow_driver.py:4777`'s LINEAR-path construction) so the composer's Step 0 can consume it — while independently satisfying property 4's multi-child fallback-safety invariant per the deferred §1.3 resolver contract (a real design task, not mechanical — a prior draft's counting approach already failed review here). **Codex's third round-1 finding (NOT yet addressed — genuinely open):** in this reproduction the gate fires BEFORE `_dispatch_inner` ever creates a child run, so the resulting `PauseSnapshot` carries no child run ID or branch-specific identity — `hitl_response_for(child_run_id)` cannot by itself distinguish two PEER gate-owning branches in this exact pre-dispatch-gate shape (neither would have a child run yet either). This single-branch reproduction confirms the LIVENESS symptom (gate never consumes, infinite re-pause) but does NOT by itself fully scope the branch-addressing mechanism for 2+ concurrently-paused gate-owning branches — either a multi-peer-branch reproduction, or grounding on how a branch's OWN pre-dispatch identity would be exposed for addressing (distinct from a recursively-dispatched child's `run_id`), is additional groundwork owed before the fix is genuinely scoped. Do not treat "the fix is scoped" as settled — treat it as "the single-branch liveness gap is scoped; the multi-branch addressing question is still open." **Council:** the property-4-safe resolver design (not a gate-skip tradeoff) is worth a dyadic pass before implementing the fix — likely C10 (blast-radius/gate-integrity, given the prior falsified draft) paired with whichever voice owns liveness/fallback-safety composition; the multi-branch addressing gap above should be part of that same council pass, not resolved unilaterally.
- **UPDATE 2026-07-25 (round 4, roadmap-continue, `advisor()`-checked twice — a scoping trap caught before being written up as a finding, not a new build):** re-confirmed by fresh direct read (not re-derivation) that `hitl_delivery_holder=`/`hitl_delivery_cell`/`HITLDeliveryCell(` appear in `workflow_driver.py` EXACTLY where round 2 already found them — all three strictly inside `_execute_workflow_body` (def at line 3126); `_execute_parallelization` (def at line 7399) and `_execute_orchestrator_workers` (def at line 10759) reference none of them. This is a reconfirmation of round 2's finding, not a new one. **The genuinely new, still-unresolved observation this pass surfaces: the register's own two framings of "what kind of bug B-72 is" don't reconcile, and nobody has named that yet.** Round 1's "What it is" text frames this as a **dispatch-idempotency** defect — "the composer's dispatch entrypoint cannot distinguish 'first dispatch of this branch' from 'forwarding an already-dispatched, already-paused-deeper branch'" (a fix needs NO addressing key at all — just a first-dispatch-vs-resume-forward check before the gate re-evaluates). Round 2's reframe describes an **addressing/delivery** defect — "a branch WITH a resolved answer waiting has no delivery path" (a fix needs `hitl_response_for` wired to a per-branch key, exactly the property-4 resolver problem). These are different fixes with different shapes and different council scope. **This session almost committed the mistake this note exists to prevent:** mid-grounding, `branch_path` (`compose_branch_path`, `{parent_action_id}:{branch_index}`) was floated as a candidate addressing key for category-(a), reasoning that its documented collision (`pause_resume_protocol_types.py`'s keying-defect note, ~line 1031) only applies to NESTED grandchildren under two different peer children, not to peer branches themselves. `advisor()` correctly rejected this before it was written up: (1) the (a)-vs-(b) bug category is unsettled, so blessing an addressing *key* presupposes an answer to a question that hasn't been answered; (2) this single-branch reproduction (round 3) exercises ZERO multi-branch addressing, so "collision only hits grandchildren, not peers" is an untested hypothesis, not a finding — and it is *specifically* the reading the code's own keying-defect note says was "recorded so the `branch_path` reading is not reinvented." **`branch_path`-as-addressing-key is explicitly NOT a finding of this pass — flagged here only so a future session doesn't independently re-derive and trust it without the same warning.** **First question the next session (or the council pass already recommended above) must resolve, before any design or code: is B-72 a dispatch-idempotency defect (b) or an addressing/delivery defect (a)?** The answer determines whether a key is needed at all, and if so, whether `branch_path`, `run_id`, or something new is safe to use for it — none of that is decided by this pass.
- **UPDATE 2026-07-25 (round 5, roadmap-continue, `systems-architect` tension-resolution mode, `advisor()`-corrected before write-up — ROUND-3 REPRODUCTION'S CATEGORY RESOLVED; B-72'S ORIGINAL REGISTERED SCENARIO STILL UNCLASSIFIED, addressing key still open):** **Scope correction up front (out-of-family Codex [P2], caught before merge):** an earlier draft of this update's heading and early conclusion overstated this pass as resolving "B-72" wholesale. It does not. What is resolved below is the category of the round-3 REPRODUCTION's narrower single-dispatch scenario only. B-72's own ORIGINAL "What it is" text (lines 601-603, round 1) describes a DIFFERENT, hybrid scenario (a branch that already dispatched into its own gate, got approved, and is now resume-forwarded toward its self-dispatched, deeper-paused descendant) that this same pass's own later correction (below) admits neither property 5 nor the round-3 repro settles. Do not read this update as closing B-72's category question in general — only the repro's narrower shape is closed; B-72's original scenario remains open, and the council/reproduction work below must not be narrowed on the false premise that it is settled. With that scope correction stated: round 4's own framing of the (a)-vs-(b) split was the wrong axis. Direct read of the canonical spec text, `Spec_Control_Plane_v1_106.md` §1.2 properties 4-5 (the highest artifact in the authority chain that speaks to this, both cited verbatim, not paraphrased) — **not two competing bug categories for the SAME branch, but two DIFFERENT branch roles the spec already distinguishes and already prescribes different treatment for**: property 5 states "Transitively-paused container/ancestor branches are not gate-owning... it never itself dispatched into a HITL gate, never itself needs a `hitl_responses` key, and MUST always be traversable (re-entered/recursed into) regardless of what `hitl_responses` does or does not contain"; property 4 states its safety/liveness invariant "applies ONLY among gate-owning branches — never to a container/ancestor branch's own re-entry." So: a **gate-owning branch** (its own `SUB_AGENT_BOUNDARY` placement is what paused it) needs a *delivered* answer — a delivery-CELL construction, mirroring LINEAR's, that the composer's Step 0 can consume — category (a); a **transitively-paused container/ancestor branch** (paused only because a descendant is paused, forwarding `child_resume_snapshot` down) is re-entered *unconditionally*, no answer, no delivery — category (b), and the spec already mandates this treatment, it is not an open question. Round 1's "What it is" text describes the container case ("forwarding an already-dispatched, already-paused-deeper branch"); round 3's reproduction pins the gate-owning case — confirmed directly from the test file (`test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py:17-19,240,249`), a single `PARALLELIZATION` branch whose own `SUB_AGENT_DISPATCH` step carries its own `HITLPlacement(position=SUB_AGENT_BOUNDARY)` — no `HIERARCHICAL_DELEGATION`, no nested/recursive child pause, i.e. genuinely gate-owning, not a container. **B-72 as empirically pinned by the round-3 reproduction is therefore category (a) — but "category (a)" is DELIVERY-mechanism-construction, not necessarily KEYED addressing, and conflating the two is itself a correction owed here (out-of-family Codex [P2], caught before merge, same pass):** an earlier draft of this update said a gate-owning branch "needs an *addressed* answer" — too strong. The round-3 test itself resumes via `resume_context = ResumeContext(hitl_response=...)` (`test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py:573`) — the UNIFORM field, not a keyed `hitl_responses` map — and property 4 explicitly permits the uniform fallback to resolve a gate-owning branch when it is the SOLE member of the unaddressed gate-owning set this cycle (exactly this repro's shape: one branch, no peers). So the round-3 repro's confirmed gap is purely the ABSENCE of any delivery-cell construction for fan-out branches (mirroring round 2's original framing, `workflow_driver.py:4777`'s LINEAR-only construction) — wiring that alone, with no keying/addressing logic at all, would already fix this specific reproduction via the uniform fallback. **Keyed multi-peer addressing (2+ concurrently unaddressed gate-owning branches) is a SEPARATE, harder, still fully open question the round-3 repro does not exercise even partially** — not "category (a) settled, addressing key still open" as an earlier draft implied, but "delivery-construction resolved for the single-branch case; keyed addressing for the multi-peer case is untouched by any evidence gathered so far." **Correction (same pass, out-of-family Codex [P2], caught before merge):** round 1's actual "What it is" scenario is NOT the clean property-5 container case, and an earlier draft of this update wrongly declared it settled by property 5 — property 5's text is explicit that it covers a branch that "never itself dispatched into a HITL gate" (`Spec_Control_Plane_v1_106.md` §1.2 property 5, verbatim), scoped to `HIERARCHICAL_DELEGATION` ancestors. Round 1's branch is a fan-out `SUB_AGENT_DISPATCH` step that DID dispatch into its OWN `SUB_AGENT_BOUNDARY` gate — that is how it got approved and recursed into the child that is now paused deeper — and is only later, on the resume-forward cycle, acting in a container-like role toward that self-dispatched descendant. Property 5 does not speak to this branch at all; it is neither the round-3 repro's clean single-dispatch gate-owning case nor property 5's pure never-gate-owning ancestor — it is a **third, hybrid case** (previously-gate-owning, now forwarding past its own already-once-satisfied gate) that this pass does NOT resolve. **This hybrid case stays OPEN, unresolved, and in scope** — it is exercised by zero of the round-3 repro (same epistemic status as the still-open multi-branch addressing gap at the close-out steps above) and needs its own reproduction/grounding before any claim about its correct treatment, likely as its own register row once scoped (mirroring how B-73–B-77 were split from B-68 at close) rather than staying conflated into B-72 as-is. Supporting evidence that a keyless idempotency-only fix could not be the answer FOR THE GATE-OWNING CASE regardless: `hitl_gate_composer.py:1684-1689`'s Step 0 has exactly one bypass path and it requires a delivered value (`consume_and_clear()` non-`None`); there is no "already-fired-this-branch" keyless bypass in the current contract, and inventing one would let the gate proceed with no operator answer — the exact fail-safe violation round 2's "a branch with no resolved answer yet SHOULD re-fire" and the B-EDIT-CARRIER response-routing history both rule out. **What this pass explicitly does NOT resolve, per `advisor()`'s caution against blessing a key as a side effect of settling the category (mirrors round 4's own rejection of `branch_path`):** `hitl_response_for(child_run_id)` cannot be restated as "the wiring" — the register's own already-standing finding (close-out steps above) that the gate fires BEFORE `_dispatch_inner` ever creates a child run stands unchanged, now cited against the correct call path (an earlier draft mis-cited this at `hitl_gate_composer.py:864`, which is unrelated code — corrected, out-of-family Codex [P3]): `_dispatch_inner` is defined at `hitl_gate_composer.py:1015` and calls `self.inner.dispatch(...)` at `hitl_gate_composer.py:1035` (i.e. only after Step 0's bypass-or-refire decision); the actual child run — and its `run_id` — does not exist until `RuntimeSubAgentDispatcher`'s own dispatch path constructs `_child_run_id_seed` and invokes `self.child_workflow_runner(...)` at `sub_agent_dispatch.py:1078-1108`, strictly downstream of the gate's own re-evaluation: a fan-out gate-owning branch is a STEP inside the parent run, not a run itself (contrast LINEAR's `run_id`, `workflow_driver.py:4716-4719`, "this call's own run identifier"), so `run_id` is identical across peer gate-owning branches and cannot address them individually — the addressing key for the gate-owning case remains genuinely open. **Tiebreaker for the next session (unresolved by this pass, and corrected same-pass, out-of-family Codex [P2], caught before merge — the tiebreaker itself was mis-scoped):** property 1 (`Spec_Control_Plane_v1_106.md` §1.2, verbatim) does NOT leave the `hitl_responses` KEY SHAPE to impl discretion — it normatively FIXES it: "keyed by the paused child's own `run_id`, not `branch_path`." So finding *any* pre-dispatch identity is not by itself sufficient for Class 3 — reusing a non-`run_id`-shaped identity (`branch_index`, `parent_action_id`, or similar) as the `hitl_responses` key would itself change property 1's fixed carrier semantics, which is a spec amendment, not impl discretion, regardless of whether the identity happens to already exist in code. The real tiebreaker: does any mechanism exist (or can one be added without changing PUBLIC carrier semantics) to make the child's `run_id` itself known/assignable BEFORE `_dispatch_inner` fires — e.g. reserving/minting the child run's `run_id` earlier in the dispatch sequence, still keyed the same way property 1 already fixes? **This tiebreaker itself needs a further correction (out-of-family Codex [P2], caught before merge, same pass): even a YES answer does not clear this to Class 3 on its own.** Internally minting the child's `run_id` earlier only solves half the problem — the OPERATOR (the external caller constructing `resume_context.hitl_responses`) has no public way to learn that minted `run_id` before responding. This is exactly `B-71`'s own already-registered, separate scope (`.harness/forward-register.yaml` id `B-71`, filed independently of this pass): `HITLEscalationBrief` and the HITL webhook payload derive their identifier from a workflow_id-scoped `compose_hitl_action_id`, NOT `run_id` — the public escalation surface exposes no run-instance-distinct identifier at all, and B-71's own close-out already calls the fix a `C-CP-28` contract amendment (additive, but still a public-carrier change, not silent impl discretion). So: **even if `run_id` can be minted early internally (YES branch), the gate-owning addressing fix is still GATED on B-71 landing (or being co-designed with it)** — a caller cannot address a keyed response to a branch it was never told the identifier of. Class 3 (pure impl discretion, no design-artifact revision) would require BOTH an internally-mintable-early `run_id` AND that identifier already being exposed on the public escalation payload without further change — the second condition is currently FALSE per B-71, so this tiebreaker's honest current answer leans toward Class 1 / B-71-dependent, not Class 3, until B-71 is scoped or landed. If NO (no `run_id`-compatible carrier is available pre-dispatch at all, only a differently-shaped identity), satisfying property 4 for the gate-owning case requires either changing property 1's fixed keying contract (a spec amendment) or a new carrier §1.3 does not define — a design gap, Class 1 per this skill's §4A.4 (inventing an ad hoc identity unilaterally would extend the H_T design, `CLAUDE.md` I-2/X-AL-3). This pass does not fully resolve either branch of that tiebreaker, and explicitly retracts the earlier, too-permissive framing that any pre-dispatch identity (or an early-mintable `run_id` alone) would qualify as Class 3 — B-71 is a load-bearing dependency for the keyed-addressing side of this fix that any council pass must account for. **Council recommendation at the close-out steps above stays alive and is NOT narrowed by this pass** — it now covers: (1) the delivery-cell-construction fix for the round-3-confirmed single-branch gate-owning case (no keying needed, per the correction above), (2) the keyed multi-peer addressing design, gated on `B-71` landing or being co-designed with it (per the tiebreaker correction above), (3) property 4's resolver set-membership mechanism, and (4) the round-1 hybrid case (previously-gate-owning, now forwarding past its own already-satisfied gate) this correction re-opened — property 5 does not settle it, so it is not safely retirable from scope. **Operator decides:** whether the round-1 hybrid case is worth a dedicated reproduction before the council convenes (it is currently untested by any existing repro), and whether the tiebreaker check is worth its own grounding pass first. **Close-out caveat (out-of-family Codex [P2], NOT independently verified this pass — flagged so whoever builds the fix checks it, not asserted as a finding):** `_collect_gate_owning_run_ids` (`workflow_driver.py:2665-2698`) treats any node carrying `fan_out_resume`/`peer_fan_out_resume` as an unconditional container regardless of its own `pause_reason` (the function's own docstring states this is "checked FIRST, regardless of its own pause_reason"), and `PeerFanOutResumeState` is constructed whenever any branch fails/pauses in a `PARALLELIZATION` run (`workflow_driver.py:9722`) — including, plausibly, a branch paused at its own gate with zero dispatched children. If so, a branch in exactly the round-3 repro's shape could compute to an EMPTY gate-owning set via `compute_hitl_uniform_fallback_eligible_run_id`, meaning delivery-cell construction alone may not suffice — gate-ownership detection may also need a fix for the pre-dispatch (own-gate, zero-children) case. This is unverified against the repro's actual captured snapshot fields this pass; verify before building on it.

**Net position after all corrections above (read this, not the correction trail, for the current disposition):** (1) the round-3 reproduction's branch is confirmed gate-owning; its fix needs delivery-cell construction (mirroring LINEAR's), not necessarily a keyed map — the uniform fallback should suffice once wired, MODULO the unverified gate-ownership-detection caveat directly above. (2) Whether KEYED multi-peer addressing is separately needed remains fully open, and any such design is gated on `B-71` (landing, or being co-designed with it) — B-71 is not optional groundwork, it is a hard dependency. (3) B-72's own original registered scenario (a branch already-gate-owning once, now resume-forwarded toward its self-dispatched deeper-paused descendant) is a third, hybrid case neither property 5 nor the round-3 repro resolves — it stays open and untested. (4) Council scope covers all four items above (delivery-cell fix, B-71-gated keyed addressing, property 4's resolver mechanism, the hybrid case) — none are retired from scope by this pass.

- **UPDATE 2026-07-25 (round 6, roadmap-continue, `advisor()`-directed empirical check BEFORE any code — NET POSITION ITEM (1) FALSIFIED):** the close-out caveat directly above (whether `_collect_gate_owning_run_ids` returns an empty gate-owning set for a branch paused at its own gate with zero dispatched children) is **CONFIRMED TRUE** by direct instrumentation of the real round-3 repro object (not reasoning): `compute_hitl_uniform_fallback_eligible_run_id` returns `None`; `PeerFanOutResumeState` captures `branch_count=1` but both `branches=()` and `paused_child_branches=()` are empty. A second instrumentation pass (dumping `terminal_dispositions` at `workflow_driver.py:9698`) confirms the mechanism directly: `terminal_dispositions={}` for this branch's ordinal even though `branch_failed=True` (derived independently from the `gather()` `BaseException` scan). **Delivery-cell construction alone cannot fix the round-3 repro** — the fix needs a NEW per-branch carrier on `PeerFanOutResumeState`/`FanOutResumeState`, which is a CP-spec-owned additive schema change (Class 1, not impl discretion). Also flags an unexplained datum: root `pause_reason=EXPLICIT_OPERATOR`, not `HITL_PENDING` — possibly related to round 3's own noted "separate, not-yet-understood quirk"; the new carrier's reason-labeling convention needs its own design decision, not an assumed compatibility with `_collect_gate_owning_run_ids`' existing `HITL_PENDING` leaf test. Filed as `.harness/class_1_fork_b72_gate_ownership_missing_carrier.md` (states the carrier as a REQUIREMENT — identity + `B-71`-compatible key shape + reason-labeling decision — not a prescribed field name/type, per round 5's own lesson about premature carrier-shape prescription). Register status flips `registered_finding` → `design_substrate_gated` (mirrors the `B-70` discriminator). No design-substrate edit, no code edit (both debug instrumentation passes reverted; `git diff` confirmed clean). Recommended: open the CP spec-leg now, co-designed with `B-71` — note this is now a genuine two-row co-design, wider in scope than the single-delta `B-70`/`B-33`/`B-39` precedent it otherwise mirrors. **OPERATOR RATIFIED 2026-07-25 (AskUserQuestion): open now, co-designed with `B-71`.**

- **UPDATE 2026-07-25 (round 7, pre-authoring grounding, before any spec text) — narrows the co-design's actual shape:** `advisor()` asked whether `B-71` genuinely shares one identity with this carrier gap before authoring. Direct read of `B-71`'s full `close_out` (not the round-5/6 paraphrase): `B-71` only proposes exposing an ALREADY-DISPATCHED child's own `run_id` on the operator-facing escalation payload — it does not mint an identity for a not-yet-dispatched branch. **The co-design is therefore SEQUENTIAL, not simultaneous** — this carrier's identity is designed first; `B-71` can echo it afterward. A second empirical check of `compose_child_run_id_seed` (`sub_agent_dispatch.py:363-395`, called at `:1078-1098`) found round 5's own tiebreaker option — mint the child's `run_id` deterministically before dispatch — is **gated on `subagent_child_recoverable`** (engine ∈ `{ESR, WAL, SAVE_POINT, RECONCILER_LOOP}`). For **`PURE_PATTERN_NO_ENGINE`** — the exact engine class the round-3 repro's child uses — the seed falls through to `None` and a fresh, non-deterministic `uuid` is minted only at actual dispatch time. **Deterministic pre-dispatch minting alone does not cover the repro's own shape**, which is not a corner case. Full reasoning (including an unresolved, NOT-yet-verified observation about whether the round-3 repro's SOLE-gate-owning-branch case could be fixed via the uniform fallback without any property-1-compliant external identity at all — flagged as exactly the kind of subtle reasoning that has already been gotten wrong multiple times this arc, not asserted as settled) at the fork doc's new §7.

- **SPEC LEG LANDED 2026-07-25 — CP spec v1.107 → v1.108 + CP plan v2.43 → v2.44:** operator ratified opening the spec-leg via `AskUserQuestion`; `systems-architect` tension-resolution mode (`advisor()` unavailable at authoring time, recommendation grounded directly against verbatim CP spec v1.106 §1.2 properties 1/4/5 + verbatim `sub_agent_dispatch.py` code — tension record at `.harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md`) resolved the round-7 observation cleanly: **property 4's Safety clause only needs an internal, non-externally-addressable identity** for counting a pre-dispatch gate-owning branch toward its "unaddressed gate-owning set" — this closes the fork doc's (a)/(b)/(c) framing without widening property 1's `run_id`-shaped key OR inventing a separate delivery path, and confirms `B-71` is NOT a co-requisite for this specific fix. CP spec v1.108 adds ONE additive property (property 6, §1.2 properties 1-5 preserved verbatim): a pre-dispatch gate-owning branch MUST be counted in property 4's unaddressed-set test via an implementation-discretion internal identity, and MUST NEVER be resolvable via `hitl_responses`. **Out-of-family `just codex-review` (round 1, base=main) caught 2 real [P1]s before merge:** (1) the plan delta's first-draft coverage-matrix row named only the counting/carrier side and omitted the delivery-cell construction at the fan-out dispatch sites (`_execute_parallelization`/`_execute_orchestrator_workers`) required to actually consume the resolved response — counting a branch as eligible does not by itself deliver the answer to it, mirroring `workflow_driver.py:4766-4777`'s LINEAR-only `HITLDeliveryCell` construction; fixed, the row now explicitly names both; (2) the canonicalization bundle (clearance markers, `CLAUDE.md` pointer bumps, this register update) was incomplete when the spec/plan text alone was reviewed — fixed by this update plus the co-landing PR. Register status flips `design_substrate_gated` → `open` (spec-leg gate cleared; the impl leg — code + tests wiring the new carrier + the now-corrected delivery-cell construction — is the next buildable slice, NOT yet opened, per the `B-33`/`B-39`/`B-59`/`B-70` spec-leg/impl-leg precedent). Closes ONLY net-position item (1); items (2)/(3)/(4) remain open exactly as rounds 5-7 scoped them. Clearance markers at `.harness/clearance/spec-control-plane-v1-108-cleared-2026-07-25.md` + `.harness/clearance/implementation-plan-control-plane-v2-44-cleared-2026-07-25.md`.

- **UPDATE 2026-07-25 (rounds 2-5, out-of-family `just codex-review` base=main, same branch `b72-cp-spec-leg-property-6`, pre-merge — 5 rounds total to convergence):** round 2 caught property 6's internal identity permitting only LOCAL uniqueness, colliding under nested fan-out and violating property 4's SOLE-member safety guarantee — fixed, §1.1(d) now requires resume-TREE-WIDE uniqueness plus a collision-witness test (plus a stale close-out recipe and stale `claude-artifact-pointers.md` rows elsewhere in the bundle). Round 3 caught property 6 permitting "counted but never keyed" WITHOUT requiring actual delivery to the sole branch — leaving the round-3 repro's livelock unclosed even after (a) and (d) were satisfied — fixed, §1.1(b) now REQUIRES delivery as an explicit contract outcome, not merely counting + never-keying (plus a stale tension-record Status field and an overbroad `B-71` dependency label on items (3)/(4)). Round 4 caught the new carrier's EXISTENCE and byte-compat treatment being left to unauthorized impl discretion despite `PeerFanOutResumeState`/`FanOutResumeState` participating in the persisted `snapshot_hash` — fixed, NEW §1.3a pre-authorizes ONE additive, drop-when-empty/`None` hash-strip-scoped field, mirroring the `v1.97`/`v1.99` precedent (plus stale `harness-cp/CLAUDE.md` and clearance-marker summaries). **Round 5 caught two real findings:** (a) this session's own `.harness/roadmap_status.md` dashboard still said the spec leg was "not yet started" despite the completed delta — fixed at the dashboard's own round-8 update, since `roadmap-continue`/session-start derive their next action from that file and would otherwise repeat the already-completed spec arc; (b) property 6's contract text read as topology-agnostic while ONLY `PARALLELIZATION`/`ORCHESTRATOR_WORKERS` are grounded by the round-3 reproduction — confirmed via direct grep (the repro test uses `TopologyPattern.PARALLELIZATION` exclusively) — so `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF`'s sequential `EvaluatorOptimizerResumeState`/`HandoffResumeState` carriers were silently left uncovered by the plan while the spec's own wording implied universal coverage; fixed, NEW §1.2b explicitly scopes property 6 to the two fan-out topologies and names the sequential-topology case as a DISTINCT, ungrounded, out-of-scope gap for a future arc's own reproduction-first grounding pass — NOT silently folded into this delta's build scope. All fixes verified against `tools/forward_register.py --check`, `yaml.safe_load`, and `git diff --stat` on `harness-*/src|tests` (empty at every step — doc-only throughout).

- **UPDATE 2026-07-25 (round 6, out-of-family `just codex-review` base=main, same branch, pre-merge — 6 rounds total to convergence):** caught that round 5's own topology-scope fix under-scoped — `HIERARCHICAL_DELEGATION` was omitted despite recursively reusing `_execute_orchestrator_workers` per U-CP-88/89, confirmed by direct code read of `workflow_driver.py:13745-13849` (`_execute_hierarchical_delegation` takes the identical `crash_fan_out_resume: FanOutResumeState | PeerFanOutResumeState | None` parameter and threads it through the same helper, NOT a separate implementation) and therefore sharing the exact carrier §1.2b was already scoping property 6 to — a supported topology left inconsistent with the runtime call graph the property itself constrains. Fixed: §1.1/§1.2b now include `HIERARCHICAL_DELEGATION` on carrier-identity grounds (not reproduction grounds — the round-3 repro still only exercises `PARALLELIZATION`); the plan's coverage row already amends `_execute_orchestrator_workers` directly, so this automatically covers every `HIERARCHICAL_DELEGATION` recursion level, no separate call site owed. `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` remain correctly excluded (genuinely distinct carrier family, no code reuse). All mirrors (tension record, both clearance markers, this file, `.harness/roadmap_status.md`, `.harness/forward-register.yaml`) synced in the same commit.

- **UPDATE 2026-07-25 (round 7, out-of-family `just codex-review` base=main, same branch, pre-merge — 7 rounds total to convergence, 2 real [P2]s):** (a) the delta's own header/revision-shape text (top of `Spec_Control_Plane_v1_108.md`) asserted "NO new field, NO carrier-shape change" while §1.3a explicitly authorizes one additive default-empty field on `PeerFanOutResumeState`/`FanOutResumeState` — a genuine internal contradiction in the canonical contract text that could mislead a downstream reviewer into rejecting or omitting the required schema work; fixed, reworded to acknowledge the field (exact name/type still deferred to the impl leg). (b) The `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` topology exclusion named at §1.2b was tracked only in this arc's own correction-history prose, with no independently-tracked register row or close-out recipe — fixed, split out as new **`B-78`** (`registered_finding`; see its own section below) with its own close-out recipe (a future arc's reproduction-first grounding pass against those carriers). All mirrors (tension record, both clearance markers, `.harness/roadmap_status.md`, this file, `.harness/forward-register.yaml`'s snapshot block) synced in the same commit.

- **IMPL LEG LANDED 2026-07-25 (PR #1116) — closes net-position item (1) end-to-end.** NEW additive `PreDispatchGateOwningBranchResumeState(branch_index, step_id)` carrier on `PeerFanOutResumeState`/`FanOutResumeState` per §1.3a's pre-authorization (drop-when-empty hash-strip scoped — pre-B-72 snapshots re-hash byte-identically). `_collect_gate_owning_run_ids` amended to walk it via a tree-wide-unique internal identity (`f"{snapshot_run_id}:pre-dispatch-gate:{branch_index}"`). NEW `_collect_pre_dispatch_gate_owning_identities` excludes these identities from the `hitl_responses`-keyed addressability check per §1.1(b)'s never-keyable requirement. `HITLDeliveryCell` construction wired at both `_execute_parallelization` and `_execute_orchestrator_workers` (the latter covers `HIERARCHICAL_DELEGATION` via its existing recursive reuse), gated on this branch's identity being the SOLE unaddressed member per property 4/6. The round-3 repro's own test renamed/re-asserted to confirm FIXED behavior (webhook count stays 1, `RuntimeSubAgentDispatcher` reached exactly once, second pause is the separate pre-existing `EngineClass.PURE_PATTERN_NO_ENGINE` child-shape quirk the original test already flagged out of scope) plus a NEW assertion that the first pause is labeled `HITL_PENDING` (the operator-facing signal the carrier is active). **No `_resumed_target_plan` extension needed for concurrency** — property 6(c) means when 2+ branches are unaddressed no delivery cell is built for ANY of them, so at most one branch ever holds a `HITLDeliveryCell` in a single resume cycle; the B-39 interim-sequencing hazard the existing plan extension guards against is structurally unreachable here. Also confirmed: the PROCEED cascade-policy path needs no equivalent wiring — `_proceed_branch`/`_proceed_worker` have no `except BaseException` handler, so a `HITLPauseRequestedSignal` propagates through `asyncio.gather(..., return_exceptions=True)` unchanged and is already caught by the existing generic `any_failed` `BaseException` scan, matching the established proceed-degrades-never-resumes precedent for every other unhandled pause/failure class on that path. **3 rounds of out-of-family `just codex-review` (base=main) to convergence:** round 1 caught 2 real [P1]s — (a) the carrier was ordinal-only, giving `_resume_body_mismatch` nothing to validate identity against — fixed by adding `step_id` to the carrier plus bounds/no-overlap/step_id-identity validation in both fan-out `_resume_body_mismatch` closures, empirically verified by editing a paused step's `step_id` between pause and resume and confirming the resume now fails closed; (b) `compute_hitl_uniform_fallback_eligible_run_id`'s generic `rid not in hitl_responses` filter would treat a COLLIDING map key as addressed for a pre-dispatch identity, violating §1.1(b) — fixed via the never-keyable exclusion above, regression-tested. Round 2 confirmed both fixes clean. Round 3 found a THIRD, genuinely distinct gap this diff does NOT introduce — confirmed pre-existing at the already-shipped LINEAR `resume_at` delivery-cell construction site (`workflow_driver.py` ~4748-4777), which this diff's fan-out wiring mirrors verbatim and therefore inherits, not causes — registered separately as **`B-79`** (see its own section below) rather than fixed here. Full harness-cp + harness-runtime targeted suites re-run clean (153 + 2 passed, 0 regressions); ruff + pyright strict clean on all touched files. Register status flips `open` → `closed`.

- **ROUND 4 CODEX FIX (2026-07-25, same PR #1116, pre-merge, base=main, out-of-family `just codex-review`) — 2 real [P1]s in the impl leg's OWN new code** (NOT the round-3 pre-existing gap registered as `B-79`): (a) the carrier validated `step_id` identity but not `step_kind` or (for `SUB_AGENT_DISPATCH`) the target `child_workflow_id` — a same-`step_id` edit swapping either previously passed the resume guard undetected — fixed by mirroring `PausedChildBranchResumeState` (B-31)'s identical guards exactly: `step_kind` is checked against the CONSTANT `StepKind.SUB_AGENT_DISPATCH` (a pre-dispatch gate-owning branch is always that kind by construction, so no field is captured for it), `child_workflow_id` is a NEW optional captured field read from the step's own payload at pause time (no dispatched child exists yet to source it from an exception, unlike B-31's carrier). (b) a `HITLDeliveryCell` could still be constructed for the sole unaddressed pre-dispatch gate-owning peer even when a SIBLING effect-fence-paused peer resolved this resume to a run-level ABORT — letting the HITL-approved peer dispatch concurrently before the ABORT fails the run — fixed by gating both fan-out delivery-cell construction sites on `not _any_fence_abort`, mirroring the existing effect-fence-directive suppression at the same two sites. 3 new regression tests witness both fixes in `test_workflow_driver_parallelization_pause.py`, including a non-vacuous check that the suppressed peer is still re-dispatched (re-pausing INERT) rather than silently skipped. Full harness-cp targeted suite re-run clean (156 passed, +3 from this round); ruff + pyright strict clean on all touched files. **4 rounds of out-of-family `just codex-review` total to convergence for the impl leg.**

- **ROUND 5 CODEX FIX (2026-07-25, same PR #1116, pre-merge, base=main, out-of-family `just codex-review`) — round 4's OWN kind-changed guard was itself broken.** It hardcoded a constant check against `StepKind.SUB_AGENT_DISPATCH`, wrongly assuming a pre-dispatch gate-owning branch can only ever be that kind. `HITLPlacementKind.PRE_ACTION` can ALSO gate `INFERENCE_STEP`/`TOOL_STEP` steps and raises the SAME name-matched `HITLPauseRequestedSignal` — an unchanged `PRE_ACTION`-gated resume would have been rejected as a false material diff, a genuine regression round 4 introduced. Fixed by restoring `step_kind` as a CAPTURED field (compared for equality on resume), not a constant check — the `PausedChildBranchResumeState` (B-31) precedent round 4 mirrored does NOT apply here, since that carrier IS uniquely `SUB_AGENT_DISPATCH` by construction while this one is genuinely multi-kind. `child_workflow_id` is unaffected (already defaulted to `None` via a try/except for non-`SUB_AGENT_DISPATCH` payloads). NEW regression test reproduces the exact scenario codex flagged: a `PRE_ACTION`-gated `INFERENCE_STEP` branch pre-dispatch-pauses, and resuming with the UNCHANGED workflow now succeeds instead of failing closed on a bogus kind-changed material diff. Full harness-cp + harness-runtime targeted suites re-run clean (157 + 2 passed, +1 from this round); ruff + pyright strict clean on all touched files. **5 rounds of out-of-family `just codex-review` total to convergence for the impl leg.**

### B-73 · Protected result store's GC/publish synchronization is in-process only -- two co-resident OS processes sharing one store root are not mutually excluded *(split from B-68 at its close, out-of-family Codex round 5, 2026-07-25; CLOSED 2026-07-26 -- PR #1120)*

- **What it is.** Split out of B-68 at its close (2026-07-25). B-68's fix serializes `gc_sweep()` against `_publish_atomic()` via a `threading.Lock` keyed by the store root's resolved path, held in a module-level `_root_locks` registry — this closes every same-process race, including multiple `ProtectedResultStore` instances constructed against the same root within one process (e.g. across bootstrap/resume cycles). Out-of-family Codex round 5 (the B-68 fix's convergence check) confirmed the registry does NOT extend across a process boundary: two separate daemon PROCESSES sharing one store directory each hold their own independent `_root_locks` dict, so one process's bootstrap/shutdown sweep can still unlink another process's just-published entry between its `os.link()` commit and mtime refresh, if the deployment-configured TTL is shorter than that window.
- **Current state.** Registered, not built. This is the SAME reachability precondition B-68's original registration already named (TTL shorter than single-digit-ms publish latency — roughly seven orders of magnitude below the factory 86400s default) and the same residual B-68's own code comments and commit history disclosed throughout its 5 review rounds ("a genuinely separate OS process sharing the store directory remains an open residual, registered, not silently assumed closed"). Not a defect introduced by B-68's fix — an inherent limitation of an in-process primitive.
- **Close-out steps.** Add an OS-level advisory lock (`fcntl.flock` on a dedicated lockfile under the store root, or equivalent) wrapping the same `_publish_atomic`/`gc_sweep` critical sections the in-process `threading.Lock` already wraps, acquired/released around the existing lock so both layers compose without a nested-lock ordering hazard. Witness with a genuine multi-PROCESS test (e.g. `multiprocessing` workers, not threads) pinning a TTL shorter than an injected publication delay in one process while a second process sweeps concurrently, asserting the entry survives.
- **Council: no** — narrow TTL-dependent cross-process race with a documented, unchanged reachability precondition; the fix is a known primitive (`fcntl.flock`), not a judgment call.
- **CLOSED 2026-07-26 (PR #1120).** `_cross_process_lock()` (an `fcntl.flock` on a dedicated `.cross_process.lock` file, disjoint from both `gc_sweep` glob patterns) is now composed INSIDE the existing in-process `self._publish_lock` at both `_publish_atomic` and `gc_sweep`'s candidate-selection phase — a single, fixed nesting order everywhere, no lock-ordering hazard. Guarded on Windows (`_IS_WINDOWS = sys.platform == "win32"`) per C-STK-10, mirroring the same carve-out already shipped at `harness_is.cross_process_ledger_lock`; `fcntl` is imported lazily inside the lock method, never at module scope. **4 rounds of out-of-family `just codex-review main` to clean convergence:** round 1 caught a [P1] (the unguarded top-level `import fcntl` would have broken `import harness_runtime` entirely on Windows, since `harness_runtime.types` imports `ProtectedResultStore` unconditionally) plus a [P2] (a fresh child store's opportunistic pre-write GC sweep already acquires the same lock, so the write-once regression test passed even with `_publish_atomic`'s own lock removed alone — fixed by suppressing the opportunistic sweep in the test's child process); round 2 caught 2 further [P2]s (the new tests would spuriously fail on Windows, where the lock intentionally no-ops — added `skipif`; the readiness marker was written before `flock` was ever attempted, leaving a scheduling window); round 3 found the same underlying concern persisted (writing the marker "right before" the contending call still is not proof of being AT the `flock` attempt, since real serialization + Fernet encryption happen first) — fixed by wrapping `fcntl.flock` itself in each child script so the marker fires at the actual syscall attempt; round 4 clean. Each fix mutation-probe-verified (revert, confirm the regression test fails; restore, confirm green) before landing. Witnessed with two genuine-subprocess regression tests (`subprocess`, not `multiprocessing` — avoids both the fork-after-lock-held deadlock hazard and spawn's pickle-by-module-name ambiguity in this monorepo's several same-named `tests` packages) asserting cross-process blocking via `store._cross_process_lock()` held directly in the parent — a deliberate, reasoned deviation from the originally-envisioned TTL-race-survival recipe (lock-identity blocking is the more deterministic, non-flaky witness shape for this claim). `merge-gate` 3-lens review: concurrency + test-witness APPROVE at round 1; spec-conformance BLOCKED once (this register row was not updated in the same PR, diverging from the B-68 precedent) — fixed by this same update.

### B-74 · Protected result store's mtime-based GC age authority can under-report publish time below a filesystem's timestamp granularity *(surfaced at the B-68 impl arc, out-of-family Codex round 7 [P2], 2026-07-25)* *(**CLOSED 2026-08-05 at the B-96 impl leg** -- residue DISSOLVED under ceiling form C-2, not narrowed)*

- **CLOSED 2026-08-05 at the `B-96` impl leg (`U-RT-150`).** The residue is **DISSOLVED, not narrowed**: reclaim no longer rests on the filesystem timestamp ALONE, so a granularity error can no longer reach the decision by itself, and the inter-sweep gap ceases to be a term in it at all. No TTL floor, no volume probe and no knowledge of the filesystem's real resolution were required — exactly this row's SECOND stated option, delivered structurally. The witness disposition this row prescribes was applied literally: `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight` is **re-pinned as a POSITIVE witness that the live entry SURVIVES BOTH SWEEPS**, kept rather than deleted.
- **2026-08-05 ratification-leg disposition (`B-96` Reading C / ceiling C-2): under the ratified C-2 form THIS ROW CLOSES**, and closes in the shape its own `close_out` asks for (`.harness/council-b96-grace-ceiling-2026-08-01.md` §8, reached independently by all three voices and re-derived by that record's adversarial pass against the live test). C-2 delivers this row's **second** stated option — *an age authority that cannot predate publication by more than the TTL* — **structurally**, with no volume probe, no floor and no knowledge of the filesystem granularity `G`, because the age authority stops being mtime-alone. **The residue as scoped is DISSOLVED, not narrowed:** the inter-sweep gap ceases to be a term in the reclaim decision at all. **Witness disposition at the `B-96` impl leg:** `:1982` flips `== [entry_path.stem]` → `== []` and is **re-pinned as a positive witness**, exactly as its own comment at `:1978`–`:1981` instructs; `:1975` and `:1976` are **untouched**. **Two corrections the impl leg must not inherit as fact:** (1) the Δ figure — neither *Δ ≈ 1.0s* nor *Δ ∈ [0.3, 0.9) guaranteed* is right; Δ is the fractional part of wall-clock at the patched `os.utime` call, phase-guarded only at loop exit, so `[0.3, 0.9)` is **overwhelmingly likely, not guaranteed**; (2) the stale §14.8.11 anchor lived in **this** row — **refreshed** at this leg by direct read at HEAD `4815ebef` to **`:4926` at Runtime v1.110** (heading `:4915`); the council record's own *`:4909` at v1.109* prescription was already stale. **Status unchanged (`registered_finding`)** — this row flips to `closed` only when the `B-96` impl leg merges. **No separate `B-74`-residue filing is owed**: the question it would have carried is answered by the `B-96` ratification.
- **What it is.** `_publish_atomic`'s `os.utime(entry_path, None)` refresh sets the entry's mtime to the current system time, but the underlying filesystem is what actually STORES that value, at whatever timestamp resolution it supports — on a coarse-granularity filesystem (e.g. legacy FAT32, or certain network filesystem configurations; NOT the modern POSIX filesystems — ext4/APFS/NTFS — this workspace's committed local-development/self-hosted-server/managed-cloud deployment surfaces default to, which offer sub-millisecond resolution), the stored mtime can round DOWN by up to the filesystem's own granularity (e.g. up to ~1 second on a 1-second-resolution filesystem). If the deployment-configured `ttl_seconds` is SHORTER than that rounding error, `gc_sweep()` can classify a just-published, genuinely-live entry as already expired and reclaim it — even though the B-68 mtime-authority fix (round 1) is working exactly as designed; the filesystem itself cannot represent the precision the fix relies on.
- **Current state.** NARROWED (not closed) 2026-07-26 (PR #1123): the "likely ONE finding" framing below was directionally right but not literal — `B-77` and `B-74` share raw mtime as an untrustworthy age authority, but `B-77`'s reorder fix only removes the DATA write+fsync duration from its OWN crash window, not the whole window (see `B-77`'s row — a residual metadata-fsync+commit tail remains, reproduced by out-of-family Codex round 9). This row's own concern (filesystem storage-resolution rounding) is UNTOUCHED by that reorder either way — it was never about ordering, only about what a coarse filesystem stores once a value is fsync'd, so it stays fully in scope, not reduced. Two same-arc attempts to dispose of this row were both wrong and reverted: PR #1123 round 1 tried "no realistic operator would configure a below-floor TTL" (rejected by Codex — `protected_result_store_ttl_seconds` enforces only `gt=0.0`, no floor, and the env loader + factory pass an override through unchanged, so this is a SUPPORTED configuration, not an unreachable one); a later round considered adding that same TTL floor as a mechanical fix and advisor() rejected it too — it is this row's own close_out option ("reject TTLs below a guaranteed timestamp resolution"), already flagged as a genuine design decision, and the field is a spec'd deployment-configurable surface (Runtime §14.8.11's bounded-retention bullet, `:4883` at v1.108 — **cite corrected 2026-07-31**: §14.8.11 carries no AC numbering, so the prior "AC 7" resolved to nothing) Phase-7 impl discretion should not narrow unilaterally. Remains open, not dormant, not built this arc.

  **NARROWED AGAIN 2026-07-30 (dated re-check against the landed `B-77` fix, PR `#1163`).** `B-77`'s first-observation grace covers this row's reachable loss case but does NOT subsume it. **Now covered:** a granularity error up to ONE sweep interval — the sweep that FIRST observes a coarse-stamped, genuinely-live entry can no longer reclaim it, which is the bootstrap-sweep-immediately-after-publication path (witnessed at `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight`, whose first assertion passes only because of the grace). **Surviving residue — this row's whole remaining scope:** a rounding error EXCEEDING the gap between two consecutive sweeps. That gap is not TTL-bound — a bootstrap sweep (`stage_4_od.py`) and an explicit or shutdown-step-5b sweep (`shutdown.py`) can land milliseconds apart, well inside a 1-second filesystem granularity — so a live entry can still be lost on such a volume. That exact residue is PINNED as an executable assertion in the same witness (its second `gc_sweep` reclaims the live entry, labelled REGISTERED RESIDUAL in the test body), so a future fix must come back and update it rather than silently diverge. Not closed as subsumed-by-`B-77`; the close-out below is unchanged and still the fix.

  **Cross-reference (advisor(), B-76 arc, 2026-07-26):** likely ONE finding with `B-77` — both concern raw mtime as an untrustworthy age authority (this row: filesystem timestamp-granularity rounding; `B-77`: crash-window non-atomicity). A fix that moves the age authority off raw mtime would likely close both; ground as a single "does the age authority stay mtime?" question before building either in isolation.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT on every limb; disposition FORK-SHAPED.** `_publish_atomic`'s `os.utime(entry_path, None)` refresh is at `protected_result_store.py:581`; the mtime-derived age comparisons at `:756`, `:777`, `:787`, `:794`; the B-77 first-observation grace at `:796` with the previous-sweep set at `:877-879`. Critically, **the TTL still has no floor**: `protected_result_store_ttl_seconds: float = Field(default=86400.0, gt=0.0, allow_inf_nan=False)` (`harness-runtime/src/harness_runtime/types.py:1879`), so a below-granularity TTL remains a **supported** configuration — the exact ground on which the "no realistic operator" dismissal was rejected. The narrowed residue is still pinned as an executable assertion labelled `REGISTERED RESIDUAL (B-74)` at `harness-runtime/tests/test_lifecycle_protected_result_store.py:1943`/`:1978`, inside `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight` (`:1924`). **Decision required:** pick a concrete resolution floor, or move the age authority off raw mtime entirely.
- **⚠️ Linkage correction (2026-08-05).** This row is **not** deferred pending `B-96`'s ratification, and no row surface has ever said it was. This row's own cross-reference names **`B-77`** (now closed), and its close-out is stated as *"unchanged and still the fix"* — not gated on `B-96`. The relation appears only from **`B-96`'s** side, which names this row as *"open — the filesystem-granularity dimension of the same mtime-as-age-authority question"* and adds *"All three re-check together."* The two are **siblings sharing one root question** (does the age authority stay mtime?), not a dependency. **B-74 was independently actionable at that pass — and the shared root question is now ANSWERED.** *(2026-08-05: `B-96` ratified as Reading C with ceiling **C-2**, so the age authority does **not** stay mtime. Per the council record §8 this row's residue is **dissolved, not narrowed**, and it closes at the `B-96` impl leg — see the ratification-leg bullet at the top of this block. The sibling-not-dependency reading above stays correct as history: the two rows were never in a formal blocking relation; they shared a question, and that question now has an answer.)*
- **Close-out steps.** Design a TTL-floor validation or a probe-based age-authority selection that accounts for the deployment filesystem's actual timestamp resolution — e.g. probe the store root's filesystem granularity at construction/first-write time (write a marker file, read back its mtime precision) and either refuse a TTL below that floor with a typed error, or fall back to a higher-resolution age signal (e.g. a companion sidecar file recording a monotonic sequence number or a separate high-resolution timestamp store) when the TTL is below the floor. Witness with a test that simulates a coarse-mtime filesystem (e.g. via a monkeypatched `os.stat`/`os.utime` rounding to whole seconds) and a TTL below that resolution, asserting the store either refuses the TTL or does not lose the live entry.
- **Council: no** — narrow, filesystem-dependent TTL-precondition gap; the fix requires picking a concrete resolution floor which is a design decision, not an immediate judgment call.

### B-75 · Protected result store's gc_sweep candidate-selection scan can itself stall inside the publish lock under a pathological backlog or a hanging filesystem *(surfaced at the B-68 impl arc, out-of-family Codex round 8 [P1], 2026-07-25; FIXED 2026-07-26 -- PR #1122, CLOSED)*

- **What it is.** Surfaced at round 8 — a finding directly about round 7's OWN fix, the third consecutive round to surface an issue in the immediately-prior round's change (round 6→7→8). Round 7 narrowed `gc_sweep`'s lock to a fast, `stat()`-only candidate-selection phase specifically to bound lock-hold time against a slow DECRYPT phase (see B-68's closed row). Codex round 8 observes that even this narrowed phase — `glob()` + `stat()` over every `*.entry`/`.tmp-*` file, still inside the lock — is itself unbounded in a MORE EXTREME scenario: a pathologically large backlog (far larger than what makes decrypt slow, since `stat()` is orders of magnitude cheaper per file), or a filesystem mount that hangs/stalls on `stat()` itself (e.g. a dead network mount). Under `run_off_loop_detach_on_cancel`'s detach semantics, a shutdown sweep stuck in THIS phase would ALSO get detached while still holding the lock, reopening the same class of liveness hazard round 7 closed for the decrypt phase specifically.
- **Current state.** CLOSED. Registered, not built — was the state through round 8's own registration — per `advisor()`'s explicit round-8 guidance: three consecutive rounds each surfacing a defect the PRIOR round's own fix introduced is the non-convergent-adversarial-hardening-arms-race signature (this workspace's own standing discipline) — stop patching, register, and ship rather than open a round 9. Reachability is genuinely narrower than round 7's finding (needs either an extreme file count or a hanging/failing filesystem mount, not merely a large-but-normal backlog), and a failing filesystem mount would likely be causing failures well beyond this store already. **Escalation (advisor(), B-76 arc, 2026-07-26):** `B-73`'s cross-process `flock` is now ALSO held across this candidate-selection phase, so a stalled `glob()`/`stat()` scan blocks other OS PROCESSES sharing the store root, not just other threads in this process — worse than this row's original P1 framing. Own arc, not a continuation of B-76's.
- **Close-out steps.** Bound the candidate-selection phase itself — e.g. batch the `glob()`/`stat()` scan with a periodic lock release/re-acquire (yielding between batches so a stalled scan cannot monopolize the lock indefinitely), or move the scan itself off-lock using a snapshot-then-verify pattern (enumerate + stat unlocked, then re-verify each candidate's mtime under a BRIEF lock hold immediately before acting on it — mirroring how phase 2's unlink already trusts a monotonic once-confirmed-expired fact, but applied one level earlier). Witness with a test that stalls `glob()`/`stat()` itself (not decrypt) and asserts a concurrent write still completes within a short bounded window.
- **Council: no** — narrow liveness-hardening follow-on to an already-shipped fix; the fix shape (batch/yield or snapshot-then-verify) is a known pattern, not an open design question.
- **CLOSED 2026-07-26 (PR #1122).** Selected via `advisor()` over the still-open `B-74`/`B-77` pair — its close_out already named a known fix shape (no open design question), it's the P1, and `B-73` escalated it to cross-process blast radius. Candidate ENUMERATION (`glob()`/`stat()` over `*.entry`/`.tmp-*`) now runs fully unlocked, producing PROVISIONAL candidates; each is re-verified in a single brief COMBINED lock acquisition before being trusted — never one acquisition per candidate (would turn `_cross_process_lock`'s per-call `fcntl.flock` into N kernel round-trips). Resolved advisor()'s flagged asymmetry (does the fix cover `.tmp-*`, or scope to `*.entry` only?) by hand: a `.tmp-*` file's mtime is not a trustworthy "definitely done" signal unlocked (it could be mid-write) the way an entry's is (monotonic expiry + fresh uuid4 keys) — but since `tempfile.mkstemp` itself only ever runs INSIDE the lock, a temp-file candidate that survives to be re-observed once the sweep holds the lock can only be a genuine crash orphan (its creator either already finished and already unlinked it, or crashed while holding a now-free lock). Same unlocked-scan-then-brief-relock-reverify pattern is correct and safe for both file types — not scoped to `*.entry` only. Two new regression tests: a structural witness recording whether `_publish_lock.locked()` is true at each `glob()` call (asserts unheld, not a wall-clock check per `[[verification-shape-sharpened-grep-vs-e2e]]`), and a stalled-enumeration concurrent-write test mirroring the existing stalled-decrypt pattern. Both mutation-probe-verified. Hand-traced the fix against the two most load-bearing pre-existing lock tests before writing code to confirm neither would regress — both pass unchanged. Full `harness-runtime` suite: 3087 passed, 10 skipped; ruff + pyright strict clean.

### B-76 · Protected result store's lock-key registry can fail open on a transient stat() failure during root-identity resolution *(surfaced at the B-68 impl arc, out-of-family Codex round 8 [P2], 2026-07-25; FIXED 2026-07-26 -- PR #1121, CLOSED)*

- **What it is.** `_lock_for_root` keys the process-wide lock registry on `(st_dev, st_ino)` when `resolved.stat()` succeeds, falling back to the resolved-path STRING when it raises `OSError` (a defensive branch added at round 6 for the not-yet-created-directory case, now effectively unreachable in normal operation since round 6's own fix made `_lock_for_root` unconditionally `mkdir` the directory before stat'ing). Codex observes the fallback is still live for a TRANSIENT `stat()` failure (not "doesn't exist yet", but a genuine intermittent I/O error) — if one concurrent caller's `stat()` transiently fails while another's (near-simultaneous) call succeeds, the two callers key DIFFERENT locks for the SAME logical root (one string-keyed, one inode-keyed), silently reopening the exact aliasing race the identity-based key exists to close.
- **Current state.** CLOSED. Registered, not built — was the state through the B-68 arc's round 8. Narrow: requires a genuinely intermittent filesystem error at the precise moment of a concurrent lock-key computation, not a sustained failure (a sustained failure would surface elsewhere — e.g. the `mkdir` immediately preceding this stat would itself already be failing loudly).
- **Close-out steps.** Do not silently fall back to a different key namespace on a `stat()` failure after a successful `mkdir` — propagate the `OSError` (or retry with a bounded backoff before propagating) so a transient failure surfaces as a typed refusal rather than a silent identity-namespace split. Witness with a test that monkeypatches `Path.stat` to fail once then succeed, asserting the lock registry does not admit two distinct lock objects for the same resolved root across that transient failure.
- **Council: no** — narrow transient-I/O-error handling gap; propagate-vs-fallback is a correctness fix, not an open design question.
- **CLOSED 2026-07-26 (PR #1121).** Selected via `advisor()` as the cleanest of the 4 findings surfaced at the same round (`B-74`/`B-75`/`B-76`/`B-77`, all in this file) — a genuinely mechanical propagate-vs-fallback fix with no interaction with `B-73`'s new cross-process flock and no reopening of the phase-1/phase-2 GC-sweep safety argument. Removed the `except OSError: key = str(resolved)` fallback; `resolved.stat()`'s `OSError` now propagates. Verified all 3 direct/indirect callers of `_publish_lock`/`_lock_for_root` already tolerate the raised error by direct grep (not just the docstring's own claim): `write_once` folds it to `UnresolvableResultRef`; the bootstrap (`stage_4_od.py`) and shutdown-step-5b (`shutdown.py`) `gc_sweep()` callers each wrap it in `except Exception`; `_maybe_opportunistic_gc_sweep` does too. Witnessed with a frame-targeted `Path.stat` monkeypatch (checks the immediate caller is `_lock_for_root` specifically) rather than a blind global intercept — `Path.resolve()`/`Path.mkdir()` both call `.stat()` internally in this pathlib version and each already swallows its own `OSError`, so a naive intercept gets absorbed there instead of ever reaching the line under test. Mutation-probe-verified: reverting the fix makes the new test fail (`DID NOT RAISE`); restoring it passes. `advisor()` additionally flagged that `B-74`/`B-77` are likely ONE finding (both concern raw mtime as an untrustworthy age authority — worth a combined grounding pass, not two separate builds) and that `B-73` made `B-75` WORSE (candidate selection now also holds the cross-process flock, so a stalled scan blocks other PROCESSES, not just threads) — both recorded at their own rows, not built here. Full `harness-runtime` suite: 3085 passed, 10 skipped; ruff + pyright strict clean.

### B-77 · Protected result store's mtime refresh is not durable atomically with the publish commit, leaving a crash window where a recovered entry looks pre-aged *(surfaced at the B-68 impl arc, out-of-family Codex round 8 [P2], 2026-07-25; CLOSED 2026-07-30 -- first-observation grace)*

- **THE `B-77` RESIDUAL IS NOW CLOSED (2026-08-05, `B-96` impl leg / `U-RT-150`).** The sweep-COUNT grace this row landed is replaced by a DURABLE ELAPSED-TIME one, so the residual practical-duration gap — a short or immediately-failing run whose own shutdown sweep fires milliseconds after its bootstrap sweep — no longer exists, and the persisted-sidecar reversal recorded below is now BUILT rather than merely ratified. **This row's own status was already `closed` and does not move.**
- **2026-08-05 — the *no persisted sidecar* judgment is REVERSED, and the reversal is recorded here so it is never later read as unratified drift.** The ratified `B-96` Reading C / ceiling **C-2** form publishes a **dedicated candidate-filename + timestamp sidecar** (carrier **(C-i)**, keyed over **both** sweep classes per council condition #5) by temp-write + `fsync` + `os.replace` + directory `fsync`. The reversal is **ratified** — operator `AskUserQuestion` 2026-08-05 on the `B-96` fork, with the C-1/C-2 sub-decision resolved by the convening at `.harness/council-b96-grace-ceiling-2026-08-01.md` (PR #1183). **The `B-77` residual flips to `closed` when the `B-96` impl leg merges**; this row's own status is already `closed` and does not move.
- **What it is.** `_publish_atomic` makes the entry durable in two separate steps: the `os.link` commit + its directory fsync (durability step 1), THEN a separate `os.utime` mtime refresh + its own fsync (durability step 2). If the process crashes BETWEEN these two steps (after the entry is durably linked and visible, but before its mtime is refreshed), the entry that survives the crash retains the TEMP FILE's original, pre-fsync-latency mtime — the exact stale timestamp the B-68 round-1 fix exists to move away from. The NEXT process's bootstrap sweep, reading this crash-recovered entry, would then see the OLD (potentially already-TTL-exceeded) mtime and could immediately reclaim an entry that in fact just barely finished publishing before the crash.
- **Current state.** NARROWED TWICE, not closed, 2026-07-26 (PR #1123). This PR went through THREE wrong claims before landing here, all caught by out-of-family Codex: round 1 tried to classify B-77 as dormant via a "sub-millisecond `os.link`-to-`os.utime` window" argument (wrong — the real pre-fix window was the FULL pre-link write+fsync duration, unbounded). Round 2 moved the SOLE mtime refresh from post-commit to pre-commit and claimed this "closes the window structurally" (wrong — Codex round 9 [P1] reproduced a DIFFERENT, normal-path regression against `main`: with the stamp now before the metadata-fsync+`os.link`+directory-fsync tail instead of after, a successful uninterrupted write could return a reference an IMMEDIATE sweep reclaims — `ttl_seconds=0.02` + a 100ms-delayed directory fsync — something that could never happen on `main`, where the stamp already ran after that tail). Round 3 (current): `_publish_atomic` now stamps TWICE — once pre-commit (on the temp file, for crash recovery) AND once post-commit (on the entry, restoring `main`'s exact original normal-path behavior) — rather than moving the single stamp. Strict improvement over `main` on both axes, no reopened ground. The residual crash window is now `os.link` → the post-commit refresh completing — the SAME shape B-77 originally named, starting from the fresher pre-commit baseline rather than write-time. Restoring the post-commit block also reintroduces a pre-existing, out-of-scope inconsistency the round-2 reorder had incidentally fixed: if the post-commit refresh's own fsync fails, `write_once` reports `UnresolvableResultRef` even though `entry_path` is already durably linked and readable — matches `main`'s original semantics, intentionally not fixed here. Mutation-probe-verified regression tests added for BOTH the crash-recovery path (pre-commit stamp) and the normal-path regression Codex round 9 found (post-commit stamp); the residual is registered, not tested — no fix for it has landed. `merge-gate`'s 3-lens review approved the round-2 diff; only the concurrency lens was re-run for round 3 (the durability-step change is inside the same lock its reasoning already covered) per advisor()-directed scope.
- **Close-out steps.** advisor()-directed: do NOT add a TTL floor — that is `B-74`'s own already-declined close_out option ("reject TTLs below a guaranteed timestamp resolution... a genuine design decision"), and the field is a spec'd deployment-configurable surface (Runtime §14.8.11's bounded-retention bullet, `:4883` at v1.108 — **cite corrected 2026-07-31**: §14.8.11 carries no AC numbering, so the prior "AC 7" resolved to nothing) Phase-7 impl discretion should not narrow. The actual fix is an age authority that cannot predate publication by construction, not a further reordering of a two-stamp pipeline: `gc_sweep` should not reclaim an entry it has not observed in a PRIOR sweep (a grace period) — bounded to one sweep interval, immune to arbitrary I/O latency in the publish path, implementable entirely inside `gc_sweep`/`_publish_atomic` without touching the config contract. Witness with a test that simulates a crash strictly BETWEEN `os.link` and the post-commit refresh completing (e.g. a monkeypatched post-commit `os.utime` that raises once) and asserts a subsequent bootstrap-style sweep does not reclaim the crash-recovered entry immediately.
- **Council: no** — the grace-period fix shape is a known pattern (first-observation gating), not an open design question; narrow implementation-discretion follow-on once picked up.
- **CLOSED 2026-07-30 (PR `#1163`, branch `b77-gc-first-observation-grace-20260730`).** The close_out above landed verbatim as a FIRST-OBSERVATION GRACE inside `gc_sweep` — no fourth reordering of the two-stamp pipeline, no TTL floor, no change to the `ttl_seconds` config contract. `gc_sweep` now records the filenames it verified past TTL under its own lock (`_observe_expired`) and reclaims only those a PRIOR sweep of the same root already recorded. **Persistence judgment (the central design call):** the record is module-wide, keyed by the root's filesystem identity (`_root_identity_key`, extracted from `_lock_for_root` so both key identically) — NOT per-instance. Grounded against the real call graph rather than the abstract pattern: the composition root builds a FRESH `ProtectedResultStore` per `run()`/`resume()` bootstrap (`bootstrap/factories/protected_result_store_factory.py:78` ← `bootstrap/stage_4_od.py:123`, which sweeps immediately), so a per-instance record would hand EVERY bootstrap sweep a fresh grace and defer reclaim to shutdown indefinitely, weakening the spec's own bounded-retention term. A fresh PROCESS legitimately starts with an empty registry — it cannot tell a genuine age from a crash-window artifact, so granting the grace is the safe direction, and that IS precisely the crash-recovery case this row names. Four mutation-probed witnesses: `test_crash_between_link_and_post_commit_refresh_survives_a_fresh_bootstrap_sweep` (the close_out's own recipe), `test_uninterrupted_write_survives_an_immediate_sweep_past_the_grace` (the PR #1123 round-2 normal-path regression class, swept twice so the grace cannot mask it), `test_genuinely_expired_entry_is_reclaimed_at_the_second_sweep_not_the_first` (liveness — one interval, not immortality), and `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight` (the `B-74` re-assessment). Pre-existing single-sweep witnesses were updated to sweep twice at one pinned `now` (`_sweep_past_grace`) so their original TTL-classification discrimination is preserved rather than masked. Spec check before building: Runtime v1.106 §14.8.11's bounded-retention term requires GC sweeps at bootstrap/shutdown plus a periodic-or-opportunistic runtime sweep and a typed expiry report line — it states no reclaim-on-first-observation timing, so the grace violates no AC. The out-of-scope inconsistency this row also recorded (a failing post-commit refresh fsync reporting `UnresolvableResultRef` for an already-durably-linked entry) is UNCHANGED and still not fixed.

### B-78 · A `SUB_AGENT_DISPATCH` step's own `SUB_AGENT_BOUNDARY` gate under `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` can exhibit the same pre-dispatch re-fire livelock `B-72` property 6 closes for the fan-out topologies, ungrounded by any reproduction *(split from B-72's spec leg, out-of-family Codex round 5+7, 2026-07-25; GROUNDED + IMPL LEG LANDED 2026-07-26 — PR #1117, CLOSED)*

- **What it is.** `B-72`'s CP spec v1.108 §1.2b scopes property 6 to ONLY the `PARALLELIZATION`/`ORCHESTRATOR_WORKERS`/`HIERARCHICAL_DELEGATION` topologies — the ones whose gate-owning-branch pause state is captured via the `PeerFanOutResumeState`/`FanOutResumeState` carrier family (`HIERARCHICAL_DELEGATION` included on carrier-identity grounds per round 6, since it recursively reuses `_execute_orchestrator_workers` per U-CP-88/89, not a separate implementation). A `SUB_AGENT_DISPATCH` step carrying its own `SUB_AGENT_BOUNDARY` placement under `EVALUATOR_OPTIMIZER` or `DECENTRALIZED_HANDOFF` is captured into a DIFFERENT carrier family instead (`EvaluatorOptimizerResumeState`/`HandoffResumeState`) and can plausibly exhibit the SAME re-fire-on-resume symptom `B-72`'s round-3 reproduction pins for the fan-out case.
- **Current state.** CLOSED. Registered, not built, not even reproduced — was the state through round 7. This has never been directly grounded against those carriers' own code paths — round 5 first flagged it as an explicit exclusion in `B-72`'s spec leg; round 7 (out-of-family Codex, [P2]) flagged that the exclusion needed its own durably-tracked register row, not just correction-history prose buried inside `B-72`.
- **Close-out steps.** A future arc's own reproduction-first grounding pass (mirroring how `B-72`'s round-3 repro preceded its own spec leg): construct a real `EVALUATOR_OPTIMIZER` or `DECENTRALIZED_HANDOFF` workflow whose `SUB_AGENT_DISPATCH` step carries `SUB_AGENT_BOUNDARY`, confirm (or falsify) that its own pre-dispatch gate re-fires identically on resume, and only then determine whether a property-6-shaped fix (or a materially different one, given the different carrier shape) is warranted — not yet designed, not yet specified.
- **Council: no** — narrow topology-specific livelock candidate with a clear grounding-first next step (reproduce before designing), mirroring `B-72`'s own precedent; not a judgment call yet.
- **CLOSED 2026-07-26 (PR #1117).** The reproduction-first grounding pass confirmed a real defect WORSE than the registered livelock hypothesis: a genuine `HITLPauseRequestedSignal` (a deliberate `BaseException`, not `Exception`) raised from the dispatch path inside `_execute_evaluator_optimizer`/`_execute_decentralized_handoff` was UNCAUGHT — crashing the run outright rather than pausing it — and separately, neither topology constructed a `HITLDeliveryCell` at its own resumed-step dispatch site, so even a caught pause could never deliver a resolved operator response on resume. Fixed by mirroring the already-cleared LINEAR delivery-cell mechanism (CP spec v1.108 §1.2, line 37) at both topologies' resumed-step dispatch sites — a name-matched `except BaseException` catch + `HITLDeliveryCell` construction, identical resolution shape to LINEAR/fan-out. NO new CP-spec carrier field needed (EO/DH have no branch-ordinal ambiguity property 6's field was built for) — plain Phase-7 impl work, not a Class 1 change. **6 rounds of out-of-family `just codex-review main` to convergence:** rounds 1-3 each fixed a genuine regression this arc's own prior fix introduced (REJECT-must-be-terminal; the terminal carve-out over-matched non-HITL `rt_fail_class`es; the narrowed carve-out was checked ahead of `cascade_policy`'s PROCEED branch in DH, wrongly overriding its PARTIAL-salvage disposition); round 4's finding (an unanswered `HITL_PENDING` resume proceeds to dispatch rather than staying inert) was confirmed, via direct grep, to be the identical pre-existing shape already at the shipped LINEAR site — folded into `B-79`'s scope (broadened, not duplicated) rather than fixed here; round 5 found nothing new; round 6 (a `merge-gate` spec-conformance-lens-driven pass) caught + fixed a stale-carry-text defect this same PR's own CP spec/plan correction left behind (see `Spec_Control_Plane_v1_109.md`/`Implementation_Plan_Control_Plane_v2_45.md`) plus a genuine [P2] audit-completeness gap — the new EO/DH pause handlers omitted the `PAUSE_CAPTURED` CP→IS state-ledger emission LINEAR's own HITL branch fires, fixed by mirroring it at both sites with mutation-probed regression tests. **`merge-gate` 3-lens review:** concurrency + test-witness APPROVE round 1 (unchanged across rounds); spec-conformance BLOCKED twice on stale-carry-text (round 1: CP spec/plan still asserting the gap "ungrounded"; round 2: the round-1 fix's own filing footer + this file's own B-78 section left stale) before all-approve.

### B-79 · A resumed branch's `HITLDeliveryCell` is constructed from `step_id` identity alone, so a HITL placement/gate-configuration change between pause and resume is not detected as a material diff *(surfaced at the B-72 impl leg, out-of-family Codex round 3 (branch-vs-main), 2026-07-25; SPEC LEG + IMPL LEG SLICE 1 LANDED 2026-07-27 — PR #1132; IMPL LEG SLICE 2 LANDED 2026-07-27 — PR #1133, CLOSED)*

- **What it is.** Out-of-family Codex round 3 (branch-vs-main, B-72 impl leg PR #1116, 2026-07-25). The B-72 impl leg's `_resume_body_mismatch` guard (mirroring the pre-existing `effect_fence_paused_branches`/`paused_child_branches` precedent) validates a pre-dispatch gate-owning branch's `step_id` identity before delivering the operator's resolved response, but does not validate whether the branch's HITL placement (e.g. `SUB_AGENT_BOUNDARY`) or `step_kind` still applies to the resumed step. If a workflow is edited between pause and resume while preserving the branch's `step_id` — removing its `SUB_AGENT_BOUNDARY` placement, for instance — the guard still accepts the resume and `RuntimeHITLGateComposer`'s Step 0 unconditionally consumes and applies the stored `HITLDeliveryCell` (e.g. a REJECT) to the now-ungated branch, before any current-placement evaluation runs. Confirmed PRE-EXISTING and NOT introduced by the B-72 impl leg — the identical unconditional construction (no placement-identity check at all) already exists at the already-shipped LINEAR `resume_at` delivery-cell site (`workflow_driver.py` ~4748-4777, landed at the B-39 impl leg), which B-72's fan-out wiring mirrors verbatim — confirmed by direct grep/read of that site before registering this row, per this workspace's own `[[codex-finding-scope-verify-sibling-pattern]]` discipline.
- **Current state.** Registered, not built.
- **Close-out steps.** Design a HITL placement/gate-configuration identity check (e.g. persisting the resolved set of applicable HITL placements, or a content hash of the step's HITL-relevant fields, alongside `step_id` at pause-capture time) and validate it at BOTH the LINEAR `resume_at` site and the fan-out `_resume_body_mismatch` closures (`_execute_parallelization`/`_execute_orchestrator_workers`) before a `HITLDeliveryCell` is constructed — reject the resume as a material diff if the check fails, mirroring the existing step_id/step_kind precedent. This is a genuine design task (what identity to persist, and whether the composer's own persona/engine-class placement-resolution logic — which `harness-cp` does not have visibility into, that logic being Runtime-owned — needs a new cross-axis seam to expose it), not a mechanical patch. Witness with a test that edits a paused step's HITL placement (or step_kind) between pause and resume while preserving `step_id`, and asserts the resume is rejected as a material diff rather than silently delivering the stale response.
- **Council: likely.** A dyadic pass (a design task touching a Runtime-owned cross-axis seam, not mechanical) — C10 (blast-radius/gate-integrity, mirroring the B-72 spec-leg's own prior falsified-draft caution) paired with whichever voice owns the CP↔Runtime HITL-placement-resolution boundary. Not urgent — the reachability precondition (an operator edits a workflow's HITL placements between an in-flight pause and its resume) is narrow, and this gap is symmetric with an already-shipped LINEAR mechanism that has not surfaced in production.
- **CONVENED 2026-07-27 (roadmap-continue, dyadic `council-orchestrator` pass, C10 + C1 co-primary; C11 considered and recused — the register's "HITL" phrasing invited it by association, but C1's own SKILL.md draws the boundary explicitly: "C1 owns *where* HITL plugs in; C11 owns the *primitive*," and this question is squarely placement, not primitive).** The convened agent ran the orchestrator's mandatory probe-first discipline BEFORE any deliberation, and the probe **falsified this row's own premise**: "the composer's own persona/engine-class placement-resolution logic... is Runtime-owned, and `harness-cp` does not have visibility into it" is false for the scenario in scope. Direct read confirmed `step_context.hitl_placements` (via `fold_step_hitl_placements`, computed at every dispatch/resume site e.g. `workflow_driver.py` lines 5107-5114, 7885-7890, 8051-8060, 11832-11840) and `StepEffectiveBinding.removed_placements` (`per_step_override_evaluator.py:259-362`) are BOTH CP-native, deterministic folds already available at pause-capture time — `hitl_gate_composer.py:1752-1762`'s own inline comment confirms the Runtime composer only *consumes* `step_context.hitl_placements`, which is "surfaced onto the per-step `StepExecutionContext` at workflow-binding time by the CP driver." Only the gate-*level* computation (persona_tier × blast_radius × mcp_trust_tier) is genuinely Runtime-resolved, and that's a function of deployment configuration, not the workflow-body edit this row's reachability precondition names — **so no new cross-axis seam is needed**, contrary to the close-out steps' own framing above. One TENSION surfaced and probe-resolved: C1 initially proposed hashing only `HITLPlacement.position` (narrower, cheaper); C10 wanted the full applicability surface covered (a `tool_filter`/`cascade_policy`/`timeout` edit on the same placement kind would otherwise slip through). Re-checking the evidence found `HITLPlacement` (`hitl_placement.py:176-197`) is itself a frozen 4-field Pydantic model already inside the SAME tuple C1 proposed hashing — hashing all 4 fields costs nothing extra over the narrow framing, so both voices converged on the broader hash with zero additional plumbing cost. **Recommendation:** persist a SHA-256 content hash over `(ordered HITLPlacement tuple, sorted removed_placements)` on the existing pause-capture carriers (`PausedChildBranchResumeState`/`PreDispatchGateOwningBranchResumeState`, drop-when-`None` byte-compat mirroring the B-31 `child_workflow_id`/B-72 `step_kind` precedent exactly) plus a new analogous carrier for the LINEAR `resume_at` site — the probe found that site has NO per-step identity check at all today, not even `step_id`, a slightly larger gap than the fan-out sites this row originally scoped; a companion fix is also owed at `hitl_gate_composer.py`'s Step 0 short-circuit (~1684-1750), which never gates delivery on placement drift today. **Fix size:** mechanical-with-a-thin-design-decision, not a genuine architectural task. **Classification: Class 1** — every prior material-diff-identity addition to these same carriers (`step_kind`; `child_workflow_id` at B-31/v1.99; the B-72 pre-dispatch-gate-owning identity itself at v1.107→v1.108) went through a CP spec-leg amendment before impl per `C-CP-26` PauseResumeProtocol authority; this is architecturally identical in kind, not a bug-fix-shaped impl-discretion call. **Next action:** open a CP spec-leg (fork doc, or a B-72-lineage follow-on) proposing the additive field(s) + a new §1.x invariant mirroring property 6's shape, before any impl. Not yet opened this pass — this update is doc-only.
- **SPEC LEG LANDED 2026-07-27 — `Spec_Control_Plane_v1_110.md` §1 (property 7) + `Implementation_Plan_Control_Plane_v2_46.md`, co-designed with `B-80`.** Before drafting, every load-bearing call-graph claim from the convening above was independently re-verified against current HEAD (per this same session's own `B-71` arc lesson about a spec delta built on an unverified premise): `PreDispatchGateOwningBranchResumeState`'s exact field shape (`branch_index`/`step_id`/`step_kind`/`child_workflow_id`, `pause_resume_protocol_types.py:208-252`); `HITLPlacement`'s exact 4-field, frozen, hashable shape (`hitl_placement.py`); `fold_step_hitl_placements`'s CP-native fold (`workflow_driver_types.py`); `removed_placements`' CP-native origin (`per_step_override_evaluator.py`, `hitl_placement.py`, `workflow_manifest_entry.py`); and the Runtime composer's Step 0 unconditional-delivery shape (`hitl_gate_composer.py:1684-1750`, confirming `resumed_placements`/`resumed_matching` are computed ONLY for the audit entry, never gating delivery) — all confirmed exactly as the convening found. Property 7 requires the gate-configuration identity be validated unchanged at the fan-out closure's pre-dispatch-gate-owning delivery site, the LINEAR `resume_at` site, the `EVALUATOR_OPTIMIZER` site, and the `DECENTRALIZED_HANDOFF` site (all 3 sequential sites carry no per-step identity check of any kind today; a net-new carrier, shared or per-site, is authorized); a Runtime-side defense-in-depth check at Step 0 is left to impl discretion, not required, since a rejected CP-side resume never constructs a `HITLDeliveryCell` for the paths this property covers. **4 out-of-family codex findings caught before merge across 2 rounds, all fixed:** (1) [P1] round 1: an initial draft ALSO authorized this field on `PausedChildBranchResumeState` — WRONG, confirmed by direct read (`workflow_driver.py:8016-8058`) that carrier only forwards `child_resume_snapshot` to a recursively-dispatched child's own, separate `execute_workflow()` call and never itself constructs a `HITLDeliveryCell` (that only happens for `branch_index in _recovered_pre_dispatch_gate_owning`, `workflow_driver.py:8036-8045`) — a hash there would neither close a real gap NOR be neutral, it would incorrectly reject a valid resume whenever the PARENT's own unrelated placement config changed; corrected to scope §1.3a's carrier authorization to `PreDispatchGateOwningBranchResumeState` only (the child's own gate identity is covered recursively at its own `execute_workflow()` invocation); (2) [P2] round 1: §1.1(c) falsely claimed the rejection emits a C-CP-20 audit-ledger entry — confirmed by direct read that `_resume_body_mismatch`'s actual existing behavior is `RunResult(status=FAILED, fail_class=...)` only, no audit-ledger write; corrected to match; (3) [P2] round 1: the plan's "entirely CP-native, zero cross-axis cascade" claim was wrong for property 8 (see B-80's own update below); (4) [P1] round 2: property 7's scope entirely OMITTED the `EVALUATOR_OPTIMIZER` (`workflow_driver.py:10789-10806`) and `DECENTRALIZED_HANDOFF` (`workflow_driver.py:14803-14820`) delivery-cell construction sites — both landed later by the `B-78` impl leg (PR #1117) and confirmed by their own inline comments to mirror the LINEAR mechanism EXACTLY, sharing the identical no-per-step-identity-check gap; corrected to cover all 3 sequential sites uniformly, one shared net-new carrier or 3 distinct ones at impl discretion. Clearance at `.harness/clearance/spec-control-plane-v1-110-cleared-2026-07-27.md` + `.harness/clearance/implementation-plan-control-plane-v2-46-cleared-2026-07-27.md`. Register status flips `registered_finding` → `open` (the design-extension blocker is cleared; the impl leg — the hashing scheme, the new fields, the comparison wiring, the witness tests at all 4 sites — is a separate follow-on arc, not yet built).

### B-80 · The `_any_fence_abort` HITL-delivery suppression guard is scoped to the current fan-out level, not the true root of a nested resume tree *(surfaced at the B-72 impl leg, out-of-family Codex round 6 (branch-vs-main), 2026-07-25)*

- **What it is.** Out-of-family Codex round 6 (branch-vs-main, B-72 impl leg PR #1116, 2026-07-25). The round-4 fix suppresses a pre-dispatch gate-owning peer's `HITLDeliveryCell` construction when `_any_fence_abort` is true, but that value is computed from `_recovered_effect_fence_paused`, which is scoped to the CURRENT fan-out level's own recovered branches only. When an effect-fence ABORT targets a DIFFERENT, nested pause subtree (e.g. a deeper `HIERARCHICAL_DELEGATION` level, or a sibling peer's recursively paused child), a shallower level's own `_any_fence_abort` stays false and its pre-dispatch gate-owning peer can still receive a delivery cell and dispatch before the run-level abort is honored — resumed branches are processed sequentially, so an earlier pre-dispatch branch can consume its HITL response and execute ahead of a later subtree processing the abort. Confirmed PRE-EXISTING and NOT introduced by the B-72 impl leg — `_any_fence_abort` itself was computed fan-out-locally by the B-70 impl leg (CP spec v1.107), and the pre-existing effect-fence-directive peer suppression already consumes that same narrow value; B-72's round-4 guard mirrors the established pattern verbatim. Affects BOTH the pre-existing effect-fence directive suppression and the new B-72 HITL delivery gate, at all four fan-out dispatch sites (`_execute_parallelization` and `_execute_orchestrator_workers`, including `HIERARCHICAL_DELEGATION`'s recursive reuse).
- **Current state.** Registered, not built.
- **Close-out steps.** Compute the abort guard from the true root of the resume tree (or thread a run-level abort signal down through the recursive `_execute_orchestrator_workers`/`HIERARCHICAL_DELEGATION` calls) rather than from the current level's own `_recovered_effect_fence_paused`, and consume it uniformly at both the pre-existing effect-fence-directive suppression site and the B-72 pre-dispatch HITL delivery-cell gate. This is a genuine design task (what "true root" means for a peer fan-out vs. a recursively nested `HIERARCHICAL_DELEGATION` tree, and how to thread it without a new cross-axis seam), not a mechanical patch. Witness with a test where a nested/deeper subtree's effect-fence ABORT races a shallower sibling subtree's pre-dispatch HITL delivery in the same resume round, and assert the shallower delivery is suppressed.
- **Council: likely.** A dyadic pass (a design task touching cross-recursion-boundary abort propagation, not mechanical) — C1 (orchestration/topology, owns `HIERARCHICAL_DELEGATION`'s recursive reuse) paired with C10 (blast-radius/gate-integrity, mirroring B-79's own reasoning). Not urgent — the reachability precondition (a nested subtree ABORT racing a shallower pre-dispatch HITL delivery in the SAME resume round) is narrow, and the suppression gap is symmetric with an already-shipped effect-fence-directive mechanism that has not surfaced in production.
- **CONVENED 2026-07-27 (roadmap-continue, dyadic `council-orchestrator` pass, C1 + C10 co-primary).** The convened agent's probe-first check confirmed `HIERARCHICAL_DELEGATION`'s "recursion" is NOT same-stack-frame Python recursion — each nesting level re-enters via a FRESH `execute_workflow()` call across the CP→Runtime→CP boundary (`RuntimeSubAgentDispatcher` → `ChildWorkflowRunner` → `execute_workflow`), and TWO existing sibling values — `hitl_uniform_fallback_eligible_run_id` (B-39) and `effect_fence_uniform_fallback_eligible_key` (B-70, `workflow_driver.py:2873-2908`) — are ALREADY computed once at the true depth-0 root and threaded VERBATIM through this exact crossing (`execute_workflow()`'s signature → `_execute_orchestrator_workers()`/`_execute_parallelization()` → `child_workflow_runner.py`'s `ChildWorkflowRunner` Protocol), which is precisely B-70's own already-solved instance of this problem shape. **Recommendation:** a THIRD sibling value, `effect_fence_tree_wide_abort_present: bool`, computed once at depth-0 via a ~5-line helper reusing the EXISTING `_collect_effect_fence_idempotency_keys` tree walk + `_resolve_effect_fence_gated` resolver, threaded through the IDENTICAL channel as the two siblings, **OR**ed (not replacing) into the level-local `_any_fence_abort` at both consume sites — no new cross-axis seam, no new carrier field, satisfies this row's own registered constraint exactly. **TENSION probe-resolved** (blanket tree-wide boolean vs. a precise `cascade_policy`-chain walk, in favor of the blanket reading): (a) the already-shipped local-level rule this arc extends is itself `cascade_policy`-blind ("tier-agnostic FAILED" per CP spec v1.107 §1.1) — a `cascade_policy`-aware tree-wide extension would be MORE discriminating than the shipped convention it extends, out of family; (b) `PauseSnapshot` carries ZERO `cascade_policy` field anywhere in its schema (verified directly) — the precise reading would need a NEW durable carrier field, a strictly bigger, separately-scoped task. C1's precision concern is registered as a named, deliberately-declined refinement, not silently dropped — revisit only if a real production case demonstrates non-negligible over-suppression cost under `cascade_policy=proceed` nesting. **A genuine new finding, not resolvable as pure impl discretion:** the fix surfaces an undocumented interaction between TWO existing CP-spec invariants — §1.1's abort-suppression rule and §1.2 property 6's MANDATORY delivery for the sole-unaddressed pre-dispatch gate-owning branch (v1.108) — since widening `_any_fence_abort` tree-wide widens the circumstances under which that mandatory delivery is skipped. Traced empirically: this does NOT reintroduce a livelock (suppression only delays a peer by one resume round; the run either terminates `FAILED` via the abort's own path regardless, or a later round recovers the peer if the run turns out not doomed) — but the spec text as written does not currently reconcile §1.1 against §1.2 property 6 for the tree-wide case. **Fix size:** mechanical (one new pure function reusing two existing utilities + a parameter threaded through an existing channel + a 2-line `OR` at each of 2 consume sites + the already-specified witness test). **Classification: Class 1, not Class 3** — mirrors the B-70/B-72 precedent exactly (both routed through a CP spec delta + clearance marker + plan coverage-row before impl), and additionally owes the §1.1/§1.2-property-6 reconciliation as new spec prose (likely a new §1.1(c) or similar, noting the tree-wide suppression rule takes precedence over property 6's mandatory-delivery obligation, with the delayed-not-lost liveness rationale this convening worked out). **Next action:** route to a CP spec leg before any impl, co-designable with B-79 (both touch the same `_resume_body_mismatch`/pre-dispatch-carrier family). Not yet opened this pass — this update is doc-only.
- **SPEC LEG LANDED 2026-07-27 — `Spec_Control_Plane_v1_110.md` §2 (property 8) + `Implementation_Plan_Control_Plane_v2_46.md`, co-designed with `B-79`.** Before drafting, every load-bearing call-graph claim from the convening above was independently re-verified against current HEAD (per this same session's own `B-71` arc lesson): `execute_workflow()`'s own signature threading `effect_fence_uniform_fallback_eligible_key` through `_execute_orchestrator_workers`/`_execute_parallelization` and `child_workflow_runner.py`'s `ChildWorkflowRunner` Protocol at lines 88-89/185-186/258-259; `_any_fence_abort`'s exact consume sites at `workflow_driver.py:7999`/`8037` and `12257`/`12281`; and — the single most load-bearing check — that `compute_effect_fence_uniform_fallback_eligible_key` (`workflow_driver.py:2873-2908`) is a PURE COMPUTED VALUE derived fresh from the already-persisted `root_snapshot` at each resume call, never itself stored in `PauseSnapshot` — confirming property 8 needs NO new durable carrier, unlike property 6/7. §2.3 states the genuine new spec-prose reconciliation: property 8's tree-wide signal, once true, TAKES PRECEDENCE over property 6's §1.1(b) mandatory-delivery outcome — property 6's TEXT is unamended, only the circumstances under which it fires are now conditioned. **Out-of-family codex [P2] caught before merge:** an initial draft's plan text claimed "zero cross-axis cascade" for this property — WRONG, the new parameter's threading DOES touch the Runtime-owned `ChildWorkflowRunner` Protocol (`child_workflow_runner.py`) exactly like its two sibling parameters already do; corrected to state accurately: no NEW crossing point is introduced (the SAME established CP→Runtime→CP boundary the two siblings already use), but the touch itself is real, not absent — confirmed at this correction that neither sibling parameter is documented anywhere in `Spec_Harness_Runtime_v1.md` today, a pre-existing Runtime-spec coverage gap this delta does not attempt to backfill. Clearance at `.harness/clearance/spec-control-plane-v1-110-cleared-2026-07-27.md` + `.harness/clearance/implementation-plan-control-plane-v2-46-cleared-2026-07-27.md`. Register status flips `registered_finding` → `open` (the design-extension blocker is cleared; the impl leg — the new function, the parameter thread, the two `OR`-sites, the §1.1(b) precedence wiring, the witness test — is a separate follow-on arc, not yet built).

### B-81 · OW/HIERARCHICAL_DELEGATION test parity gaps remain for the B-72 pre-dispatch gate-owning carrier's exclusion conjuncts and resume-side material-diff validation *(surfaced at the B-72 impl leg, merge-gate 3-lens review round 2, 2026-07-25; CLOSED 2026-07-26 -- PR #1118 + PR #1119)*

- **What it is.** merge-gate 3-lens review round 2 (test-witness lens, B-72 impl leg PR #1116, 2026-07-25) found two coverage gaps after round 1's OW `HITLDeliveryCell`-construction gap was closed by a dedicated regression test (`test_ow_worker_resume_delivers_and_excludes_resolved_pre_dispatch_gate_owner` in `test_workflow_driver_fanout_pause.py`): (1) the OW/`HIERARCHICAL_DELEGATION` mirror of `_resume_body_mismatch`'s pre-dispatch-gate-owning material-diff validation (`workflow_driver.py`, the `FanOutResumeState`-consuming closure near the PARALLELIZATION site's ~7772-7831) has zero test coverage — PARALLELIZATION got three dedicated witnesses (`test_peer_resume_rejects_pre_dispatch_gate_owning_kind_changed`, `test_peer_resume_rejects_pre_dispatch_gate_owning_workflow_id_swap`, plus the INFERENCE_STEP round-5 regression test) but the OW mirror has none; (2) the new exclusion tests (both the PARALLELIZATION and OW variants) only exercise the completed/`terminal_dispositions` arm of the carry-forward filter's three exclusion conjuncts — the `paused_child_dispositions` and `effect_fence_paused_dispositions` arms remain completely unwitnessed at BOTH construction sites (`workflow_driver.py` ~9958-9965 and ~14036-14043). Per `advisor()` consultation during the merge-gate loop: these are guard-hardening on symmetric, already-reviewed code (confirmed structurally identical to the tested PARALLELIZATION paths by the concurrency lens across 2 rounds), not the load-bearing delivery outcome round 1's finding was — registered as a follow-on rather than blocking this PR's merge a third time.
- **Current state.** CLOSED.
- **Close-out steps.** (1) Port `test_peer_resume_rejects_pre_dispatch_gate_owning_kind_changed` + `test_peer_resume_rejects_pre_dispatch_gate_owning_workflow_id_swap` + the INFERENCE_STEP round-5 regression test to `test_workflow_driver_fanout_pause.py` against `_execute_orchestrator_workers`'s own `_resume_body_mismatch` closure (`worker_steps`-based, not `steps`-based). (2) Add a paused-child-resolves-this-round exclusion test (a recovered pre-dispatch gate-owning branch's delivered cell triggers a REAL nested `execute_workflow` dispatch that itself pauses, landing the branch in `paused_child_dispositions` instead of `terminal_dispositions`) and an effect-fence-resolves-this-round exclusion test (the branch's delivered cell leads to an effect-fence-ambiguous dispatch), at BOTH the `PeerFanOutResumeState` and `FanOutResumeState` construction sites — 4 new tests total. Mutation-probe each by deleting the specific exclusion conjunct it targets, mirroring the discipline already applied to the completed-arm tests this PR shipped.
- **Council: no.** Mechanical test-coverage parity against an already-established pattern (mirror an existing PARALLELIZATION test to its OW twin; add the 2 remaining exclusion-conjunct arms using the same shape as the completed-arm test this PR already ships). Not a design decision.
- **CLOSED 2026-07-26 (PR #1118 + PR #1119).** Step 1/2 (PR #1118, all-approve merge-gate round 1, clean codex-review round 1): ported the 3 PARALLELIZATION material-diff tests to their OW mirror. Step 2/2 (PR #1119, all-approve merge-gate round 1, clean codex-review round 1): added the 4 remaining exclusion-conjunct tests at both construction sites, each driving a recovered pre-dispatch gate-owning branch's delivered cell into a real nested-child pause (`SubAgentChildPausedError` via a faithful `RuntimeSubAgentDispatcher` double) or an effect-fence-ambiguous outcome instead of a clean complete, proving exclusion from `pre_dispatch_gate_owning_branches`. Before starting step 2, grepped both construction sites and confirmed the exclusion-conjunct filter is byte-identical — no production asymmetry, safe as pure test-parity work. All 7 tests mutation-probe-verified (a naive single-marker `str.replace` correctly raised an ambiguity error when probing the workflow-id-swap guard, since PARALLELIZATION's and OW's guard text is byte-identical at two locations in the same file — disambiguated via a unique nearby anchor; sharpening added to `mutation-probe-load-bearing-witness` memory). merge-gate test-witness lens on PR #1119 found the paused-child dispatcher docstrings overclaimed which exception-catch path the tests exercise (own-dispatch vs. in-flight-cancellation-race) — both write the same disposition dict identically, so mutation-probe validity was unaffected; fixed inline, no re-gate needed.

### B-82 · Ollama dispatch branch never serializes the standard memory tools its selected access mode promises *(surfaced at the gate-(d) R-300 live re-confirmation, 2026-07-27; CLOSED 2026-07-27 — PR #1140)*

- **What it is.** `reflect_memory_provider_capabilities` reports `supports_standard_memory_tools=True` for `ollama` (verified empirically 2026-07-27), so `select_memory_access_mode` (`harness-cp/src/harness_cp/memory_access_mode.py:190-200`) returns `MemoryAccessMode.STANDARD_MEMORY_TOOLS` for an ollama binding under a permissive policy. But the ollama arm of `RuntimeLLMDispatcher.dispatch` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`, the `elif provider_name == "ollama":` branch) has no standard-memory-tools counterpart to the openai arm's `_dispatch_openai_with_standard_memory_tools` — it calls the bare `_dispatch_ollama` unconditionally. The five C-MEM-14 tools the selected access mode promises are therefore never serialized into the ollama request, and no continuation loop exists to execute a returned tool call: **the selected mode silently degrades to no memory access at all.**
- **Why it is coupled to the OpenAI wire-name fix.** This is NOT itself a wire-name defect (`ollama.chat` was never handed a dotted `function.name` because it was never handed a memory tool at all). But fixing it *independently* of the wire-name map would **extend the OpenAI HTTP-400 failure class** — a dotted `tools[].function.name` rejected by `^[a-zA-Z0-9_-]{1,64}$` — to any name-validating ollama build or downstream OpenAI-compatible shim. Any ollama injection must route through an adapter-owned wire encoding from the start.
- **Current state.** CLOSED. Discovered 2026-07-27 during the gate-(d) R-300 live re-confirmation, while diagnosing the OpenAI 400 that the wire-name map closes; deliberately left out of that fix's scope to keep the surgical diff surgical.
- **Class 3 doc-clarity observation (same discovery).** C-MEM-14's tool-name table (`Spec_Memory_Substrate_v1.md` lines 485-497) reads as **identity-normative**, and the per-provider **wire encoding** of those identities is **adapter-owned** — not a spec-level rename. The `MemoryToolName` enum stays dotted and provider-neutral (`memory.search` ...); only the OpenAI adapter boundary re-spells it (`memory_search` ...). Recorded here so a future reader does not mistake the wire map for a C-MEM-14 divergence.
- **Close-out steps.** (1) Decide the ollama shape: `ollama.chat` accepts an OpenAI-compatible `tools=` array, so the most likely fix is an `_dispatch_ollama_with_standard_memory_tools` sibling reusing the same tool-schema builder + continuation loop, with the wire-name map (or an ollama-specific equivalent) applied at serialization AND at inbound resolve. Ground the actual ollama tool-call response shape against a real local daemon before writing the loop — do not assume it is byte-identical to OpenAI's `choices[].message.tool_calls[]`. (2) Alternatively, if standard memory tools are deliberately out of scope for ollama, the FIX IS IN THE OTHER DIRECTION: `reflect_memory_provider_capabilities` must stop reporting `supports_standard_memory_tools=True` for ollama so the selection degrades explicitly to `PROMPT_EXTENSION_PACKET` rather than silently to nothing. Either way the current state — a selected mode the dispatch branch does not serve — is a fail-silent gap. (3) Witness with a deterministic test that captures the kwargs handed to a fake `client.chat` and asserts either the tools are present under provider-safe names, or that the selected access mode is not `STANDARD_MEMORY_TOOLS` for an ollama binding.
- **Council: mixed.** No for the mechanical half (mirroring an existing, already-reviewed openai dispatch arm). YES if grounding shows option (2) is the right answer — narrowing a declared provider capability changes which memory access mode an entire provider family receives, which is a C-MEM-13 selection-semantics decision, not impl discretion.
- **CLOSED 2026-07-27 (PR #1140) — direction (1) built; the wire-name coupling DISSOLVED by grounding, so the council gate this row raised never fired.** A live probe against the local ollama daemon (`llama3.2:3b`, client `ollama==0.6.2`) confirmed `/api/chat` accepts the DOTTED C-MEM-14 provider-neutral identities (`memory.search` ...) verbatim in `tools[].function.name` and echoes them back unchanged in `message.tool_calls[].function.name` — ollama imposes NO `^[a-zA-Z0-9_-]{1,64}$` name grammar, so **there is no wire-name map on this path**. The "coupled to the OpenAI wire-name fix" framing above is therefore superseded: adding an ollama wire-name map would encode a **false capability claim** about the provider, and would additionally require an inbound re-resolve for names ollama never mangles. Direction (2) — narrowing `reflect_memory_provider_capabilities` — was REJECTED on the same grounding: ollama genuinely supports tool calling, so declaring otherwise would amputate the WRITE half of C-MEM-14 (`memory.write_note` / `memory.propose_promotion` / `memory.request_redaction` are unreachable through a prompt-extension packet) for the entire local-open-weight family — a C-MEM-13 selection-semantics regression, not a fix. Because grounding settled direction (1) unambiguously, the row's own "YES if grounding shows option (2)" council condition was not met.
- **What shipped (PR #1140).** `_dispatch_ollama_with_standard_memory_tools` + 6 sibling helpers in `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`, gated by a branch guard byte-mirroring the OpenAI arm's predicate (`standard_memory_tool_executor is not None and memory_context is not None and memory_context.access_mode is STANDARD_MEMORY_TOOLS`). The loop mirrors the OpenAI arm's SHAPE (same `max_iterations=16` bound, same `_standard_memory_tool_context` scope_ref/policy_ref binding, same deepcopy-before-mutate schema discipline guarding the shared module-level `MEMORY_TOOL_CONTRACTS` against concurrent-dispatch aliasing, same `LLMDispatchPayloadShapeError` fail-loud convention) against ollama's DIFFERENT wire shapes: **no `choices[]` envelope** (the assistant turn is top-level `ChatResponse.message`, `ollama/_types.py:418`); **`arguments` arrives ALREADY DECODED** as a `Mapping[str, Any]` (`_types.py:346`), so the happy path does not `json.loads` (a JSON string is still tolerated defensively for hand-rolled stubs; junk and non-object payloads fail loud); and the typed client carries **no `tool_call_id` at all** — `Message`'s fields are `role`/`content`/`thinking`/`images`/`tool_name`/`tool_calls` (`_types.py:304-355`), and the daemon's tool-call id is dropped before it reaches Python — so the tool-result turn correlates BY NAME as `{"role": "tool", "tool_name": "<dotted identity>", "content": "<json>"}`; an extra `tool_call_id` key would be silently dropped by the pydantic model (default `extra='ignore'`), so it is deliberately not emitted. **Malformed-call semantics deliberately MIRROR OpenAI: fail-loud** — `_standard_memory_tool_context` raises `MemoryToolExecutionInputError` on a scope_ref mismatch and it propagates exactly as on the OpenAI path; a feed-the-error-back-as-a-tool-turn recovery mechanism was considered and rejected as a per-provider semantic divergence, i.e. a design extension. One adjacent rename: `_openai_function_tool_name` → `_function_tool_name` (the `{"type":"function","function":{"name"}}` envelope reader is genuinely provider-neutral; both arms now use it). **Witnesses:** 6 new deterministic tests in `harness-runtime/tests/test_lifecycle_llm_dispatch.py` — a serialization witness asserting all five DOTTED names plus a caller-supplied dotted duplicate deduped under a `len()` check (a set comparison alone cannot see a surviving duplicate) and the per-dispatch scope_ref/policy_ref binding leaving the shared contract schema unmutated; a loop witness asserting the executor receives the provider-neutral `MemoryToolName`, the tool-result turn's exact ollama shape, and usage read off the FINAL turn's `prompt_eval_count`/`eval_count` with no `gen_ai.response.id`; an unknown-name raise (the OpenAI wire spelling `memory_search` is NOT tolerated here — it is a name this path never advertises); a malformed-arguments raise; a scope_ref-mismatch fail-loud; and a bare-path over-fire guard proving a `PROMPT_EXTENSION_PACKET` context injects no tools. Both mutation probes confirmed: reverting the branch guard fails 3 of them, dropping the tool-result-turn append fails the loop witness. The Class 3 doc-clarity observation above stands and is now sharper: the wire encoding is adapter-owned **per adapter** — the OpenAI boundary re-spells, the ollama boundary does not.
- **Out-of-family Codex round 1 — two durable-double-write defects, BOTH pre-existing in the OpenAI sibling, fixed symmetrically in the same PR.** The mandatory sibling check found the OpenAI loop has shared both shapes since it shipped, so fixing only the new ollama arm would have left the higher-traffic provider defective *and* introduced asymmetric semantics between two loops that are meant to mirror. **[P1] batch pre-validation.** Both loops resolved-and-executed each tool call lazily inside a `messages.extend(generator)`, so with a multi-call response an earlier — possibly `memory.write_note` — call committed *before* a later call was validated (unknown name / malformed arguments / `scope_ref` mismatch). Both loops now resolve and fully validate the COMPLETE batch first, via `_openai_prepared_memory_tool_calls` / `_ollama_prepared_memory_tool_calls` returning a `tuple[_PreparedMemoryToolCall, ...]`, and only then execute in order through `_openai_memory_tool_result_messages` / `_ollama_memory_tool_result_messages`. Fail-loud semantics are unchanged — any invalid call still fails the dispatch, just before side effects instead of mid-batch. **[P2] iteration bound.** Both loops let the 16th iteration execute its batch and only *then* raised loop-exhausted, committing a terminal write for a dispatch that reports failure. Both now check the bound BEFORE executing: if a response carries tool calls and no iteration budget remains to send their results back, the exhaustion error raises with nothing executed. **Why these were P1/P2 rather than cosmetic:** both aborts classify `TRANSIENT_RETRY` — `_classify_provider_exception` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:299-304`) fail-fasts only `LLMDispatchProviderUnreachableError` / `LLMDispatchPayloadShapeError` / HTTP 401-403 and maps everything else, including bare `RuntimeError` and `MemoryToolExecutionInputError`, to the transient staircase — so `RetryBreakerFallbackDispatcher` re-ran the whole dispatch from the top and the already-committed call executed a second time. **The exhaustion error type is deliberately unchanged** (the same generic `RuntimeError` both loops always raised, now built by the shared `_memory_tool_loop_exhausted` helper so the two messages cannot drift): re-typing it would change how retry routes loop exhaustion, which is a separate decision from the execute-before-raise defect and was explicitly out of scope. **4 further witnesses, one pair per provider** (a two-call batch whose second call is invalid → the executor receives ZERO calls; an all-tool-calls script driven to the bound → exactly 15 executions, 16 provider calls, exhaustion raised). Both new mutation probes confirmed: making the prepare functions lazy generators again fails both batch witnesses (the failure diff shows the leaked `MemoryToolName.WRITE_NOTE` request — the double-write class, made visible), and moving the bound check back after execution fails both iteration-bound witnesses.
- **Out-of-family Codex round 2 — two more [P1]s, again BOTH pre-existing on the OpenAI sibling, again fixed symmetrically.** **[P1-a] three of the five advertised tools were unreachable.** The dispatch-side `_standard_memory_tool_context` required `arguments["scope_ref"]` for every call, but only `memory.search` and `memory.write_note` declare `scope_ref`; `memory.read` / `memory.propose_promotion` / `memory.request_redaction` declare none **and** set `additionalProperties: false` (`harness-as/src/harness_as/memory_tool_contracts.py` — READ at `:181-217`, PROPOSE_PROMOTION at `:250-283`, REQUEST_REDACTION at `:284-307`), so a schema-conformant model *cannot* send one. Those three therefore failed at the dispatch boundary before ever reaching the executor. **Grounding verdict on OpenAI history:** latently broken there since the memory loop shipped — pre-round-1 `_openai_memory_tool_result_message` called `_standard_memory_tool_context(arguments, ...)` unconditionally with no per-tool branch, and the existing tests only ever exercised `memory.search`, which is one of the two tools that *does* declare the field. The check is now scoped to the declaring tools via `_MEMORY_TOOLS_DECLARING_SCOPE_REF`, derived from the contracts at import — mirroring **both** `_bind_schema_fixed_value`'s properties-presence authority **and** the executor's own partition, since `StandardMemoryToolExecutor._require_scope_ref` is reached only from `_search` and `_write_note` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:195` / `:262`). Nothing is weakened: the authoritative `scope_ref` on `MemoryToolExecutionContext` was always `RuntimeMemoryContext.scope_ref`, with the argument serving only as a cross-check — which is exactly why it can be scoped to the tools that carry one. **[P1-b] automatic memory broke ordinary tool workflows.** With standard-memory mode active, a caller's legitimate non-memory tool survives the dedup filter and is advertised — but a model call to it hit the memory resolver and raised `LLMDispatchPayloadShapeError`. Both loops now classify the batch before touching it: if **any** call names something outside our advertised memory identities, the whole response is returned to the caller unprocessed with **zero** memory calls executed. **Grounding verdict on hallucinated names:** the bare paths (`_dispatch_openai` / `_dispatch_ollama`) return every `tool_call` to the caller untouched whatever it names — they never inspect names at all — so hallucinated names were always the caller's to handle. Returning them (rather than raising) is therefore bare-path parity, and raising would be a *new* failure mode that appears only once automatic memory is enabled — precisely the regression class this finding names. **Raises are now reserved for memory-named calls with malformed payloads**, i.e. a tool we advertised, called wrong. The choice also preserves round-1's batch atomicity: a mixed batch cannot half-commit memory before control returns. One adjacent simplification: the two round-2 accessors collapsed into the existing provider-neutral `_function_tool_name`, which reads the same `{"type":"function","function":{"name"}}` envelope for both the outbound `tools[]` array and the inbound `tool_calls[]` list. **6 further witnesses** (a `memory.read` executing end-to-end with no `scope_ref` argument; a mixed memory+caller batch returned whole with the executor untouched; a caller-only batch returned — one triple per provider), and one pre-existing witness was re-pointed: its "invalid second call" is now a *memory*-named call with malformed arguments, since a non-memory name is no longer an error. Both fixes mutation-probed — re-requiring `scope_ref` for every tool fails both `memory.read` witnesses, and dropping the caller-owned-batch return fails all four caller-tool witnesses with the exact `LLMDispatchPayloadShapeError` the finding reported.
- **Out-of-family Codex round 3 — two bounded [P2]s, again symmetric across both loops.** **[P2-a] the retryable exhaustion error masked a fail-fast one.** Round 2 placed the iteration-bound check before batch preparation, so a *malformed* memory call arriving in the **terminal** batch raised the generic exhaustion `RuntimeError` — which classifies `TRANSIENT_RETRY` — instead of `LLMDispatchPayloadShapeError`, which is fail-fast. A request that can never succeed would therefore replay up to 16 provider calls per attempt, plus whatever writes the preceding 15 valid turns committed. Both loops now order the three guards correctly: caller-owned return → **validate the whole batch** → bound check → execute. Validation is pure, so moving it earlier costs nothing and cannot itself commit. **[P2-b] four fixtures used the wrong required key.** `memory.write_note` requires `note` (`harness-as/src/harness_as/memory_tool_contracts.py:218-249`), not `text`; all four corrected. **Grounding — where required-key validation actually lives:** the **executor**, not the dispatch layer. `StandardMemoryToolExecutor._write_note` reads it via `_string_arg(args, "note")` at `harness-runtime/src/harness_runtime/memory_tool_executor.py:266`, and `_string_arg` raises `MemoryToolExecutionInputError` at `:549-553`. The dispatch-side `_standard_memory_tool_context` validates only `scope_ref`/`policy_ref` presence-and-match — so **no** dispatch-side required-key validation was added (that would have been new semantics this arc has no authority for). Instead one witness now runs through a **real** `StandardMemoryToolExecutor` over a `tmp_path` filesystem store, constructed exactly the way `tests/test_memory_tool_executor.py::_executor` constructs it and pinned to the same `policy:u-mem-16` / `scope:u-mem-16` refs the dispatch fixtures already use — the fake executor validates nothing, so only a real one can witness this class at all. **4 further witnesses** (a malformed *terminal* batch failing fast rather than as exhaustion, one per loop; a real-executor `memory.write_note` round trip whose tool-result turn carries a genuine `memory_ref`/`operation_ref`, proving the write committed; the same call with `text` failing loud). Both fixes mutation-probed: reverting the reorder reproduces the masking `RuntimeError: … exceeded 16 continuation turns` verbatim on both loops, and restoring the `text` key fails at `memory_tool_executor.py:552`. A first draft of the terminal-batch witnesses was **found inadequate by its own probe** — they used a repeating canned response, so they raised on turn 1 and never reached the bound; the probe passed, which is what exposed them. Rewritten to script 15 valid turns followed by one malformed turn exactly at the bound (asserting 16 provider calls, 15 executions, and that the message is not the exhaustion text), after which the probe correctly failed.
- **Out-of-family Codex round 4 — one [P2] fixed, one bounded residual registered as `B-84`.** **[P2] usage was bundled from the final response only.** Both loops returned `_provider_response_bundle(final_response)`, whose usage reads that one response — so every continuation turn's tokens vanished from turn capture, `gen_ai.usage.*`, and cost attribution. **Sibling verdict: the OpenAI arm shared the shape verbatim** — `_openai_response_bundle` read `getattr(response, "usage", None)` off whichever single response it was handed, exactly like `_ollama_response_bundle` read that response's `prompt_eval_count`/`eval_count`. Both now thread a running `_UsageAttrs` accumulator through the loop, folding each turn in immediately after the provider call and *before* any exit path, so the caller-owned-batch return and the no-tool-calls return both carry full totals. `None` is treated as **absent, not zero** (a turn the provider reported nothing for must neither invent a `0` total nor erase counts already accumulated), and `response_id` is deliberately **not** accumulated — it identifies one response, so the bundle keeps the final turn's, unchanged. Witnessed by extending both existing loop tests rather than adding parallel ones: their two calls already carry distinct counts (10/4 then 12/5 on OpenAI; 20/8 then 31/9 on ollama), so the asserted sums are discriminating against *both* a final-turn-only reading and a first-turn-only one. Mutation-probed — collapsing the accumulator back to the per-turn value fails both, with `assert 12 == (10 + 12)` and `assert 31 == (20 + 31)`. **The registered residual (`B-84`):** an executor-level argument failure — e.g. `memory.read` missing the contract-required `memory_ref` — still occurs *mid-batch* during sequential execution, so a valid earlier write commits before the later call raises, and the transient classification then re-runs the dispatch and duplicates the write. Deliberately **not** fixed here: the naive fix (full per-tool contract prevalidation at the dispatch layer) would mirror-copy the executor's required-key and policy semantics into a second location — the drift class this workspace treats as a defect in its own right, and directly against round 3's grounded boundary that required-key enforcement lives at the executor.
- **Out-of-family Codex round 5 — non-tool-template ollama models were regressed by the injection itself.** `reflect_provider_capabilities` returns `supports_tools=True` for **every** ollama model string (`harness-cp/src/harness_cp/provider_capabilities.py:127`, pre-existing), so `select_memory_access_mode` picks `STANDARD_MEMORY_TOOLS` even for a model with no tool template — e.g. `llava-llama3` — and the new branch then sends `tools=`, which the daemon rejects with a 400. A dispatch that worked as plain text before B-82 started failing. **Fixed narrowly, with the daemon as the authority rather than a capability table** (a per-model table is a real design question and deliberately out of scope): on the **first** chat call of the memory loop only, a tool-unsupported rejection re-dispatches through the bare `_dispatch_ollama` **without** tools, byte-restoring the pre-B-82 path for exactly those models. *(Superseded at B-83 / PR #1142: the retry remains tool-free — the daemon fix is intact — but now carries the C-MEM-12 packet folded into the single system source when the B-83 repair is authorized; the byte-restore property was itself B-83's defect on this path.)* First call only — a mid-loop rejection would mean the model changed identity between continuation turns, which stays fail-loud. **Both SDK facts were probed live rather than assumed:** `ollama.ResponseError` (`ollama/_types.py:632-651`) carries only `.error` (str) and `.status_code`, giving no machine-readable discriminator, and against a local daemon on ollama 0.6.2 the tool rejection is `.error = 'registry.ollama.ai/library/llava-llama3:latest does not support tools'` with `.status_code = 400`, while a model-not-found is the *same type* with `.error = "model 'x' not found"` and `.status_code = 404` — so a blanket `except ResponseError` would have swallowed unrelated faults, and the message substring really is the only available discriminator. **The brittleness is documented at the catch site**: matching an upstream string means an upstream rewording stops the match — but it then fails **closed** (a loud dispatch failure), never silently absorbing a different error. The predicate duck-types on the class name plus the `.error` substring rather than importing `ollama`, keeping this module free of provider-SDK imports like every other dispatch branch; the *tests* use the real `ollama.ResponseError`, so the shape stays faithful. **The OpenAI loop needs no equivalent and none was added** — every OpenAI chat model supports tools, so the failure mode does not exist there and a mirrored catch would be dead code. **3 witnesses, each mutation-probed against a distinct conjunct:** removing the fallback fails the fallback witness; widening the catch to any `ResponseError` fails the model-not-found propagation witness; dropping the first-call restriction fails the mid-loop fail-loud witness. A **fourth** issue Codex raised in the same round — the SDK silently dropping declared schema constraints — was verified, judged an aggravator of an existing registered class rather than a new defect, and **recorded on `B-84`** instead of fixed here. Full harness-runtime non-e2e suite green (**3123 passed / 10 skipped**).

### B-83 · No dispatch-side guard that the selected `STANDARD_MEMORY_TOOLS` access mode is actually served by the provider arm reached *(surfaced by the B-82 fix, 2026-07-27; CLOSED 2026-07-28 — PR #1142)*

- **What it is.** B-82 was one instance of a general shape: `select_memory_access_mode` (`harness-cp/src/harness_cp/memory_access_mode.py:158-245`) picks an access mode from *declared provider capabilities*, and `RuntimeLLMDispatcher.dispatch` then routes to a *provider arm* — but nothing checks that the arm reached actually serves the mode selected. With B-82 closed, both arms that `reflect_memory_provider_capabilities` can select `STANDARD_MEMORY_TOOLS` for (openai, ollama) now serve it, so the defect is not currently reachable through the DEFAULT capability reflection. It remains reachable two ways: **(a)** a caller supplying `MemoryAccessModeRequest.provider_capabilities` explicitly (the `request.provider_capabilities or reflect_...` short-circuit at `memory_access_mode.py:163-166`) can declare `supports_standard_memory_tools=True` for anthropic or an external-CLI binding, whose dispatch arms ignore memory tools entirely — the anthropic arm branches only on `step_has_memory_tool(payload.tools)`/`hitl_tool_loop`, never on access mode, and `_payload_to_external_cli_prompt` is text-only by contract; **(b)** more importantly, a **statically injected** `RuntimeLLMDispatcher.memory_context` (the no-`memory_runtime` path at `llm_dispatch.py:1302`) is composed ONCE and is NOT recomposed when `RetryBreakerFallbackDispatcher` rebinds the provider per attempt (`retry_breaker_fallback.py:188-191` constructs a fresh `StepEffectiveBinding` with a different provider) — so a context composed for openai under `STANDARD_MEMORY_TOOLS` can be dispatched against an anthropic or external-CLI fallback and degrade to zero memory access, silently. The `memory_runtime`-bound path is NOT affected (it recomposes per attempt via `compose_for_dispatch`).
- **Grounding note — what this row is NOT.** The B-82 grounding pass also checked whether C-MEM-13's `no_memory_access is a valid policy outcome and must be ledgered when memory was requested` (`Spec_Memory_Substrate_v1.md:463`) lacks a ledgering witness. It does NOT: `MemoryContextComposer.compose_run_start` ledgers the denial via `_write_injection_decision` and emits a `DENIAL` telemetry span with `POLICY_DENIAL` failure class (`harness-runtime/src/harness_runtime/memory_context.py:165-205`), and a direct witness already exists at `harness-runtime/tests/test_memory_context.py:364-388` (`test_no_memory_access_denial_is_explicit_and_ledgered_without_retrieval`). That candidate finding is recorded here as CHECKED-AND-CLOSED rather than registered, so a future reader does not re-open it.
- **Reachability (c) — DEFAULT-reachable, added 2026-07-27 correcting this row's own earlier claim.** This row originally asserted the defect was "not reachable through the default capability reflection". **That is false, and was falsified by the B-82 round-5 fallback filed the same day.** `reflect_provider_capabilities` returns `supports_tools=True` for every ollama model string (`harness-cp/src/harness_cp/provider_capabilities.py:127`), so a model with no tool template — `llava-llama3`, say — selects `STANDARD_MEMORY_TOOLS` through **plain default reflection**, and the round-5 fallback then deliberately re-dispatches it bare, with no memory access at all and **no ledger trace of the degradation**. That is precisely this row's shape: a selected access mode the reached provider path does not serve, degrading silently. The fallback is still the right call for its own problem (it byte-restores a previously-working text dispatch, and the daemon is the only authority on which models take tools) — it simply makes B-83 reachable on the default path rather than only via the two exotic routes below, which raises this row's priority. Recorded here rather than fixed because the honest fix is the same posture decision (1) already owes.
- **Adjacent, same family — the caller-owned mixed-batch return.** When a response mixes a memory call with a caller-advertised tool call, both loops return the whole response unprocessed (B-82 round 2, for batch atomicity + bare-path parity). The memory call the model asked for is therefore **not served, and nothing records that it wasn't** — the caller cannot execute it either, since it does not own the memory tools. Same fail-silent family as this row, and it is a *consequence of a deliberate tradeoff* rather than an independent defect, so it is recorded here under this row's scope rather than registered separately: whatever posture (1) settles on — fail-loud, or explicit ledgered degradation — should cover this exit path too.
- **Current state.** **CLOSED 2026-07-28 (PR #1142)** — dispatch-time serve-check + repair-to-packet, covering (a), (b) and (c) from one authority.
- **What was built.** `_standard_memory_tools_context` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) is now the single predicate answering *"will THIS dispatch inject the C-MEM-14 tools?"*, read by both provider arm guards **and** by the serve-check, so the branch taken and the check applied cannot disagree. Its provider-identity conjunct (`selection.selected_provider != provider_name`) **detects** **(b)** — `MemoryAccessModeSelection.selected_provider` is `request.model_binding.provider` verbatim (`harness-cp/src/harness_cp/memory_access_mode.py:372`), the same string that reaches the dispatch, and the `memory_runtime`-bound path recomposes per attempt so the conjunct is inert there. When `STANDARD_MEMORY_TOOLS` was selected and the arm will not serve it — anthropic, external-CLI, or an openai/ollama dispatch with no bound executor — the already-retrieved, already-policy-checked C-MEM-12 packet is rendered into the effective system prompt through a new `render_packet_for_degraded_dispatch` sibling in `memory_context.py` (identical body to `render_prompt_extension_packet` via a shared `_render_packet`; only the access-mode gate is dropped, so every existing caller is byte-identical) and appended through the shared `append_rendered_memory_packet` seam. Reachability **(c)** repairs at its own site inside the ollama loop and reports the repair back on a third return element, so one emitter covers all three.
- **Two dispositions — repair vs. report-only** *(split at out-of-family Codex round 1, [P1-a])*. **(a)** and **(c)** are **repaired**: the context was composed *for the dispatched provider*, so its packet is provider-matched by construction and discloses nothing the selection had not already cleared. **(b)** is **report-only** — neither the tools *nor the packet* reach the rebound provider. Retrieval and injection enforce **provider-family scope before ranking** (`Spec_Memory_Substrate_v1.md:481`) and the packet was assembled under provider X's `MemoryScope.provider_family`, so forwarding its *text* to a Y-family provider bypasses that scope boundary — a **disclosure** leak, which read-only framing does not fix and which the write-authority argument this row originally gave does not cover. Recomposition is impossible on that path by construction (it is definitionally the `memory_runtime`-**unbound** configuration) and fail-closed would convert a silent degradation into a run failure, so the degradation is made **visible** in telemetry rather than repaired.
- **Telemetry.** Exactly one `memory.operation` span per degraded dispatch, with every attribute reporting what *actually* happened so the two dispositions are distinguishable without parsing a reason string. **Repaired** — `operation_name=injection`, `operation_kind=inject`, `access_mode=prompt_extension_packet`, the *served* `packet_hash` + `record_count`, and **no** `failure_class` (a successful injection through a different seam, not a failure). **Report-only** — `operation_name=denial`, `operation_kind=inject`, `access_mode=no_memory_access`, `record_count=0`, and **no** `packet_hash` (claiming a served hash for a withheld packet would be a lie); this mirrors `compose_run_start`'s own DENIAL+INJECT shape for the no-memory outcome. `failure_class` is deliberately unset on both: no member of the closed `MemoryTelemetryFailureClass` describes a provider-scope mismatch, and reusing `POLICY_DENIAL` would assert a decision no resolver made. The B-83 detail (`memory.degraded_serve` + `.reason` — `arm_unservable` | `provider_scope_mismatch` — + `.selected_access_mode`) lands on the memory tracer's span only, never on the `gen_ai` span, per C-OD-04 §4.3 namespace discipline; the reasons are span-attribute **values**, never new enum members.
- **Posture chosen — repair, not fail-loud.** Close-out step (1) leaned toward the fail-loud raise. Grounding reversed it: a raise turns a currently-silent degradation into a **run failure** for any deployment on the injected-context + fallback shape, while C-MEM-13 (`Spec_Memory_Substrate_v1.md:460` — "Providers without usable tool support may receive prompt-extension packets") already sanctions serving the packet. C-MEM-14 (`:489`) states the exposure as a present-tense obligation, so serving *something* is the contract-faithful reading. Both cites verified byte-exact at the arc.
- **NOT built, deliberately — recorded so nobody silently absorbs it.** The literal *"explicit **ledgered** degradation"* half of close-out step (1) was **not** implemented. Writing a degradation *entry* (as opposed to the span above) is fork-shaped: the load-bearing blocker is the **absent store carrier on the dispatch path** (the memory-operation sink is reachable only via the composer/executor privates; a dispatcher-visible carrier is a new Protocol/field surface), i.e. an X-AL-3 design extension routed through back-flow, not impl discretion. *(Qualified at the merge-gate: an entry reusing the run-start DENIAL+INJECT shape needs no new enum members — `compose_run_start` already ledgers a no-memory outcome with existing members; new members on the three closed enums — `MemoryOperationKind`, `MemoryTelemetryOperationName`, `MemoryTelemetryFailureClass` — would be required only for a dispatch-time-DISTINCT operation kind, if one were wanted.)*
- **Behavior change, flagged.** The B-82 round-5 ollama fallback **no longer byte-restores the pre-B-82 bare dispatch**. The retry still drops the *tools* (which is what the daemon rejected, so the daemon fix is intact) but now carries the packet as read-only prompt text. The R5 comment block states this explicitly, and its witness was updated to the new contract and renamed `test_ollama_tool_unsupported_model_falls_back_to_the_tool_free_path` rather than deleted.
- **Out-of-family Codex round 7 (final fix round before merge-gate) — one [P2] at the intersection of the authorization and observability surfaces.** **[P2-e]** the span always reported `packet_policy_denied` whenever the packet fallback was ineligible, because the carrier was a **boolean**. A run denied by an **empty token budget**, or by an **absent provider capability**, was therefore reported as an operator **policy** decision — a false accusation about a configuration the operator never made, and one that points any investigation at the wrong file. Fixed by replacing `RuntimeMemoryContext.prompt_packet_fallback_eligible: bool` with `prompt_packet_fallback_denial: str | None` — `None` means authorized; otherwise the counterfactual selector's **own** `MemoryAccessModeDenialReason` value, carried verbatim to the span.
- **Grounded correction to the round-7 brief's own hypothesis.** The brief anticipated three distinguishable causes (policy / budget / capability). The selector yields only **two** reachable values: `token_budget_empty` for the budget gate, and `no_supported_mode` for **both** a denying `injection_access` *and* an absent `supports_prompt_extension_packet` — it does not separate those two once past its packet branch. Verified empirically across all five configurations before writing a line. The fix therefore removes the **false** distinction without inventing one the selector cannot observe, and a witness asserts that non-distinction explicitly so a later reader does not mistake it for a gap. The fail-safe default is a `not_derived` sentinel, asserted **disjoint** from every real `MemoryAccessModeDenialReason` value, so an underived context is never mistaken for a gate that actually closed.
- **Recorded residual of the observability surface → B-87.** The `finally` emission introduced at round 6 over-reports a **pre-wire** translation failure as a completed injection — packet_hash present, `operation.name=injection`, and **zero adapter calls made**. Registered rather than fixed: the imprecision **over**-reports (it claims a disclosure that never left the process, so an investigator over-responds rather than misses a leak), and this arc reached its fix-round cap at Codex R7. *(Superseded — B-87 CLOSED 2026-07-29 at PR #1149; see its own row.)*
- **Out-of-family Codex round 6 — one [P2], a fourth defect shape: the *observability* of the decision.** **[P2-d]** the degraded-serve span was emitted from a **post-dispatch** block, so whenever the provider call raised — timeout, adapter error, or the bare ollama retry itself failing — control left before emission and the only B-83-specific signal vanished. By then the packet had already been **sent** (the disclosure happened) or already been **withheld** (the denial happened): precisely the cases where the signal matters most. Fixed by moving emission into a `finally` around the whole arm-dispatch chain, with `memory.degraded_serve.dispatch_outcome` (`completed` | `provider_error` | `cancelled`) plus `memory.degraded_serve.provider_error_type` on failure. Cancellation is labelled separately rather than blamed on the provider. The enclosing `gen_ai` span already records the exception itself through OTel's `record_exception` / `set_status_on_exception` defaults — which this module relies on everywhere rather than calling explicitly — so the short degraded span carries the class as an attribute instead.
- **The round-2 return-bundle choice had to be reversed.** The R5 fallback decides its degradation *inside* the arm call and reported it as a tuple element — which travels **only on success**. An R5 retry that itself failed therefore lost the report entirely, and the arm site's own local was `None` on that path (the serve-check returns `None` for a servable arm). Replaced with a `_DegradedMemoryServeSink` single-slot carrier, written the moment the disposition is settled and **before** the retry it describes. Both halves — the `finally` and the publish-before-retry ordering — are separately load-bearing and separately probed.
- **Residual exit paths, stated plainly.** `asyncio.CancelledError` propagates *through* `finally`, so the span is still emitted — witnessed, not assumed — and the same holds for any `BaseException`. The only remaining losses are process-level: `SIGKILL`, a hard interpreter crash, or an OTel exporter dropping the span downstream. No in-process placement can address those, and all of them lose the enclosing `gen_ai` span too, so the degraded span is no weaker than the dispatch telemetry it accompanies.
- **Out-of-family Codex round 5 — one [P1], a third defect shape: the repair's own *authorization*.** **[P1-e]** the repair rendered the packet **without evaluating the prompt-packet mode's own gates**. `select_memory_access_mode` returns at the *first* mode whose gates pass, so a `STANDARD_MEMORY_TOOLS` selection proves **nothing** about `PROMPT_EXTENSION_PACKET` — that branch was never evaluated. A policy allowing `standard_tool_access` while `injection_access` denies prompt packets is legal and expressible (`DENY` is the field default, and the in-repo `test_external_cli_route_metadata_composes_with_standard_tool_context` already builds exactly that shape), and the packet branch additionally requires `token_budget > 0` which the tools branch does not. The degradation could therefore disclose memory **through a mode the operator switched off**, on an unbudgeted run.
- **Authority gap, stated plainly.** The needed inputs — `workflow_policy` / `step_policy` / `token_budget` / `provider_capabilities` — live on the **composition request** and are on *neither* `RuntimeMemoryContext` *nor* `MemoryAccessModeSelection`. The dispatcher genuinely cannot answer the question at dispatch time. Rather than mirror-copying policy predicates into harness-runtime (the drift class this workspace treats as a defect in its own right) or parsing `decision_trace` strings, the answer is settled at **composition**, where the inputs are in scope, by **re-running the real `select_memory_access_mode`** with native + standard-tools capabilities masked off — the exact counterfactual the repair faces — and carried forward on a new additive `RuntimeMemoryContext.prompt_packet_fallback_eligible`. The probe is pure (the selector is documented "without touching credentials") and composes the whole decision tree in canonical order with **zero duplicated logic**. The field **defaults False**: a context assembled anywhere but `compose_run_start` has not proven eligibility, and unproven reads as ineligible. `compose_run_start` is the *only* production producer (the static `RuntimeLLMDispatcher.memory_context` seam has no production wiring — `materialize_llm_dispatcher_stage` defaults it `None`), so production coverage is total while hand-built contexts fail safe.
- **Fixture honesty.** The B-83 dispatch fixtures **were** silently relying on the unchecked path: their implied `policy:u-mem-16` document leaves `injection_access` at its `DENY` default. The flag is now an **explicit** fixture parameter (default `True`, i.e. those witnesses model an `injection_access=PROMPT_PACKET` policy with a non-empty budget) instead of an unstated assumption.
- **The authorization set is exhaustive against the selector's own decision tree.** The repair requires **(1)** provider identity, **(2)** provider-family scope, **(3)** prompt-packet eligibility — and (3) composes *every* gate on the path to `PROMPT_EXTENSION_PACKET`: `policy.enabled`, `retrieval_access` DENY, external-CLI route/auth denial, `supports_prompt_extension_packet`, `injection_access` ≠ `PROMPT_PACKET`, and `token_budget <= 0`. Verified mechanically by driving each of those six gates negative one at a time: three (`enabled`, `retrieval_access`, external-CLI auth) collapse the selection to `NO_MEMORY_ACCESS`, where `_packet_matches_mode` forbids a packet at all and the repair is unreachable **by construction**; the other three leave `STANDARD_MEMORY_TOOLS` selected (repair reachable) and the eligibility flag correctly reads `False`. There is no seventh gate on that path.
- **Out-of-family Codex round 4 — one [P2], the anthropic member of the fold-shape family.** **[P2-c]** the fold asked *"is there an existing system source?"* by **truthiness**, while the conflict check asks it by **presence** (`"system" in kwargs`). Two *valid* shapes slipped through the gap — the SDK content-block **array** (the same `[{"type": "text", ...}]` form `_anthropic_system_cache_block` itself emits) and the **empty string** — landing in the `system`-variable branch, where the still-present `params["system"]` then collided and raised `PromptInjectionConflictError`: the repair converting a *valid* dispatch into a failure. Fixed by keying the fold on `"system" in payload.params`, so **the fold predicate and the conflict predicate agree on what "present" means**, and by routing the value through round 3's `_content_with_appended_packet` — whose sequence branch appends a `{"type": "text", ...}` block, a valid Anthropic `TextBlockParam` (`cache_control` is optional) — so no divergent serialization was introduced.
- **Completeness sweep — the fold family is closed.** Run through the *real* translators across **17 arm × shape combinations**. For each of the four arms the **fold predicate now mirrors that arm's own conflict predicate**: anthropic on `"system" in payload.params`; openai/ollama on a leading `role:"system"` in the **post-`params`-merge** `kwargs["messages"]` (so a `params["messages"]` override is covered, matching `_inject_leading_system_message`); external-CLI on a leading `role:"system"` in `payload.messages` (that renderer ignores `params` for messages, so there is no post-merge divergence to cover). Every value shape reaching a fold is handled totally — `_content_with_appended_packet` (str / `None` / sequence / other) for anthropic+openai+ollama, `_external_cli_message_content` (str / other) for the CLI. All 17 combinations kept the original text **and** carried the packet, with no raise and exactly one system source. Ollama's reachable set is narrower by construction: `Message.content` is `Optional[str]`.
- **One residual, characterized rather than fixed.** The "exactly one system source" property is **index-0-scoped** on openai/ollama — a `role:"system"` message at index > 0 is seen by neither the conflict check nor the fold, so the fold prepends a leading one and the wire carries two system messages. That is the **pre-existing** R-PM-1 conflict-check semantics, not a B-83 regression; it raises nothing, replaces nothing, and OpenAI accepts multiple system messages.
- **Out-of-family Codex round 3 — two bounded [P2]s, both inside the fold helpers round 2 introduced.** **[P2-a]** OpenAI accepts a system-message `content` as either a plain string *or* a content-part **array**, and the fold coerced it through `str()` before appending — putting a Python repr (`"[{'type': 'text', ...}]"`, reproduced verbatim at the probe) on the wire and destroying the parts. Fixed with `_content_with_appended_packet`, which folds each shape in its own terms: string/`None` → string append; a parts **sequence** → one **additional** `{"type": "text", "text": ...}` part (the same part shape this module already emits at the Anthropic system block and the external-CLI response) with every original part preserved in order; any other shape → JSON-serialized (`sort_keys` + `default=str`, matching `_inject_upstream_context_message`) then string-appended, so an unexpected shape is neither mangled nor raised on. **Grounded and skipped for ollama:** `ollama._types.Message.content` is `Optional[str]` (`ollama/_types.py:312`) — str-only, so a parts array cannot reach the daemon through the typed client; ollama shares the helper but can only ever take the string branch. **[P2-b]** the external-CLI fold passed `None` to the append helper for any non-string content, **replacing** the step's own system instructions with the packet alone — and since the external-CLI arm is text-only it *always* degrades `standard_memory_tools`, making that the **common** path there rather than an edge case. Fixed by extracting the renderer's own serialization into `_external_cli_message_content` and reusing it at **both** the render loop and the fold, so a repaired dispatch can never represent the same content differently from an unrepaired one.
- **Out-of-family Codex round 2 — the same two defect shapes, one layer deeper.** **[P1-c]** the round-1 scope conjunct was **identity**-level while the disclosure boundary is **family**-level. On the `memory_runtime` path a cross-family fallback *recomposes* the context for the current candidate — so `selection.selected_provider` matches and the identity conjunct passes — while `compose_for_dispatch` keeps deriving `record_scope.provider_family` from `fallback_chain.primary.family` (`automatic_memory.py:191-199`), and `MemoryRetriever._scope_mismatch` (`memory_retrieval.py:371-384`) filters records on exactly that field. A **primary-family** packet therefore reached the ollama R5 retry and was rendered. Fixed with a family conjunct keyed on the packet's *actual* `record_scope.provider_family` versus `provider_family_for_provider(provider)` (`cross_family_cost_tag.py:82` — the workspace's existing provider→family authority; a second mapping here would be the mirror-copy drift class), reason `provider_family_scope_mismatch`, with both conjuncts now living in one shared `_degraded_serve_disposition` read by the arm-selection check *and* the R5 site. An absent scope or absent family counts as **not** matching: `_scope_mismatch` treats a `None` request family as "no constraint", so the packet may hold records of any family — unverifiable is not safe. This also exposed a **fixture lie**: the test helper hardcoded `provider_family=OPENAI` for every context regardless of provider, so the anthropic/ollama witnesses were asserting a scope their provider did not have; the fixture now derives family from provider through the same authority. **[P1-d]** the *arm-selection* repair still appended the packet as a **second** system source — the exact class fixed at the R5 site in round 2 — so a payload carrying its own system prompt raised `PromptInjectionConflictError` across all four translators. Fixed by threading the packet as a `memory_packet` parameter so **each translator folds it into its own single effective system source** (Anthropic's `params["system"]`-or-kwarg, folded *before* the cache-breakpoint decision so the block covers the merged text; OpenAI/Ollama's leading `role:"system"` message post-`params`-merge; the external CLI's system section) rather than duplicating each translator's rule at the call site. A genuine **two-base** collision (active prompt *and* payload system) still fails loud — witnessed.
- **Grounding finding, registered not absorbed → B-86.** The *servable* tools path crosses the same family boundary with strictly greater reach: the injected schemas bind `scope_ref`/`policy_ref` as fixed values, so a cross-family candidate can `memory.search` across **and** `memory.write_note` into the primary family's scope — read disclosure *and* write attribution, where the packet leak was read-only. Pre-existing since U-MEM-16/B-82 and gated on a C-MEM-11/13 scope-semantics decision, so it is registered rather than absorbed here.
- **Out-of-family Codex round 1 — two real [P1]s, both fixed on this branch, both reproduced empirically before fixing.** **[P1-a]** the (b) repair forwarded provider X's packet to a Y-family provider — the scope leak above; (b) became report-only. **[P1-b]** the R5 retry supplied the packet as a **second** system source, so `_inject_leading_system_message` raised `PromptInjectionConflictError` for any payload already leading with a `{"role":"system"}` message — i.e. the previously-working tool-free fallback hard-failed for the idiomatic self-system-prompt step shape. Closed by folding the packet into the **one** effective system source (`_fold_memory_packet_into_system`, plus a defaulted `memory_packet` kwarg on `_dispatch_ollama` so every other caller stays byte-identical), operating on the **finished kwargs** post-`params`-merge for exactly the reason `_inject_leading_system_message` does — a `params["messages"]` override can hide the real leading system message, so a payload-level merge would reintroduce the collision.
- **Witnesses.** 27 deterministic tests (anthropic override; external-CLI arm; provider-rebound context report-only on a tool-capable openai arm; cross-family recomposed context report-only through the R5 path; ollama tools-unsupported retry; the R5 retry against a payload-carried system message; the arm-selection repair against a payload-carried system message; the arm-selection repair against Anthropic's `params["system"]`; a two-base collision still failing loud; a servable-path over-fire guard; an OpenAI content-part-array system message; an external-CLI mapping-content system message; an anthropic `params["system"]` block array; an empty-string `params["system"]`; a packet-policy-denied context at the arm site; the same at the R5 site; two composer-level eligibility-derivation tests against the real selector; a repair-carrying dispatch whose provider call raises; a report-only dispatch whose provider call raises; an R5 retry that itself fails; a cancelled dispatch; a success-path outcome assertion; an underived-eligibility context reporting `not_derived`; a sentinel-disjointness check; active-prompt composition ordering) and 16 mutation probes: disabling the serve-check fails 4; reverting the R5 packet injection fails only (c); dropping the provider-identity conjunct fails only (b); *repairing* the provider mismatch fails only the (b) report-only witness; reverting to the two-source retry shape reproduces `PromptInjectionConflictError` at the P1-b witness; dropping the family conjunct fails only the cross-family witness; reverting the arm-selection repair to append-as-second-source reproduces `PromptInjectionConflictError` at both P1-d witnesses; reverting the OpenAI content fold to `str()` fails the parts-array witness on a Python repr; reverting the external-CLI fold to the `None`-pass fails the mapping-content witness with the step's directive entirely absent from the prompt; reverting the anthropic fold to the truthiness key reproduces `PromptInjectionConflictError` at **both** the block-array and empty-string witnesses; dropping the eligibility conjunct fails both packet-policy-denied witnesses (arm site *and* R5 site); stubbing the composition-time derivation to always-eligible fails both composer-level derivation witnesses; moving the emission back post-dispatch fails all four exception-path witnesses; publishing the R5 sink *after* its retry instead of before fails the R5-retry-failure witness alone; collapsing the composer derivation back to one label fails both composer-level precise-reason witnesses; collapsing the span reason at the disposition fails all three dispatch-level precise-reason witnesses.
- **Scope split.** The mixed memory+caller batch case, previously recorded under this row's "adjacent, same family" note, is genuinely unrepairable at its site and moved to **B-85**.
- **Close-out steps.** (1) Decide the failure posture: a fail-loud typed error at the dispatch boundary when `memory_context.access_mode is STANDARD_MEMORY_TOOLS` and the resolved provider arm has no standard-memory-tools sibling, versus an explicit, ledgered degradation to `PROMPT_EXTENSION_PACKET`/`NO_MEMORY_ACCESS` (which C-MEM-13:463 would then require be ledgered — the check-and-closed note above confirms the ledgering mechanism already exists, only the dispatch-side re-composition trigger would be new). Fail-loud is the cheaper and more consistent reading given the surrounding `LLMDispatchPayloadShapeError`/`MemoryToolExecutionInputError` conventions. (2) Separately decide whether a statically injected `memory_context` should be re-composed (or invalidated) on a fallback provider rebinding, or whether that configuration should simply be refused at bind time when a `fallback_chain` is also bound — the latter is a much smaller surface. (3) Witness with two deterministic tests: an explicit-capability-override dispatch against the anthropic arm, and a rebinding test where a `STANDARD_MEMORY_TOOLS` context composed for openai is dispatched through a cross-family fallback to anthropic.
- **Council: likely for step (1), no for step (3).** Choosing fail-loud versus explicit-ledgered-degradation is a C-MEM-13 selection-semantics decision with a blast-radius dimension (a fail-loud raise turns a currently-silent degradation into a run failure for any deployment relying on the injected-context + fallback configuration) — C10 (blast radius) paired with whichever voice owns the memory access-mode selection semantics. Step (2)'s bind-time refusal is mechanical once (1) is settled.

### B-84 · Memory tool batch: an executor-level argument failure mid-batch can commit earlier writes, then retry-duplicate them *(surfaced by out-of-family Codex round 4 on the B-82 arc, 2026-07-27)* *(**RATIFIED 2026-08-05 as READING A** — the pure `validate()` pre-pass; the held element ratified **Class 2**; **BUILD LEG LANDED 2026-08-06 — CLOSED**)*

- **BUILD LEG LANDED 2026-08-06 (#1243) — READING A BUILT, ROW CLOSED.** The executor grew a side-effect-free `StandardMemoryToolExecutor.validate(request)` and **both** provider batch pre-passes call it for every call before any of them executes. **Shape** (recorded so a later leg need not re-derive it): parity is **unfalsifiable by construction** rather than pinned by an enumeration — each tool has a pure `_prepare_<tool>` returning a frozen per-tool prepared-arguments value, `_prepare` dispatches over `MemoryToolName` to exactly one of them, `validate()` calls `_prepare` and discards, and `_execute_authorized` calls the **same** `_prepare` and then narrows on the **result type**, with every tool body taking its prepared value as a **required** parameter. There is no second checking path to drift from, and a sixth prepared variant is a type error rather than a silently unvalidated tool — W-4's **preferred** discharge, so no site cap applies. **Witnesses (51)** at `harness-runtime/tests/test_b84_validate_prepass.py`: W-1 pre-fix baseline (the same batch through the loop against a non-validating executor **still** partial-commits — the counterfactual that keeps the closure witnesses non-vacuous), W-2 closure **parameterized over both provider arms** plus a wholly-valid-batch positive control, W-4 a 30-row (tool × argument × branch) parity matrix plus two **structural** witnesses (`_prepare` totality over the enum; every body requiring its prepared value), W-7 the retry-precedence change pinned in **both** directions, W-8 **inverted** with a positive control, and the `C-MEM-19` telemetry pair. **Mutation probes (PD-8), all confirmed RED then restored:** remove the OpenAI `validate()` call → W-2/W-7 `[openai]` fail while the ollama arms stay green; remove the ollama call → the mirror image; empty `validate()`'s body → 37 of 51 fail; revert `_search`'s prepare-first ordering → W-8 + the `search/limit` parity rows fail; drop `validate()`'s span emission → the telemetry witness fails; emit on the success path too → the no-double-count witness fails. **Telemetry obligation discharged** — `validate()` emits the same `memory.tool_call` span with the same `memory.failure_class` / `memory.policy.decision` `execute()`'s catch would have emitted, **on the failure path only** (a success-path span would double-count every standard tool call), witnessed by asserting **byte-equal attribute maps** between the two surfaces for the identical request over an input-error row and a policy-denial row. No `C-MEM-19` weakening, so no Class 1 back-flow was owed. **Single-call `memory.search` (§3(vii)): closed, as the ratified reading prescribes** — W-8's inverted form requires `limit=0` rejected **before** any retrieval event, and the shared-prepare structure delivers it on **both** surfaces (the dispatch pre-pass and a direct `execute()`), asserted separately so the direct-call ordering is pinned too. **W-5's four-cell matrix, EXECUTED** (all four were `[MODERATE]` code-read inferences before this leg): **(1)** re-sampled text → different `memory_id`, ordinary append, one JSONL line each — an **extra distinct note**, not a duplicate; **(2)** same text / different model → different `memory_id` (`summary_model` is hashed), same outcome; **(3)** same text / same model name / different provider → `memory_id` **collides**, the record JSONL line **is** appended a second time, and the ledger then **raises** on its 18-field equivalence payload (`provider` differs), surfacing as `MemoryToolExecutionStoreError` which classifies **`TRANSIENT_RETRY`** — **cell (3) is confirmed by execution, so the fork's D-2 falsifier is answered affirmatively: the landed fail-fast half IS re-enterable through the store-error class.** That is a pre-existing residual the fork already priced as **outside** Reading A's scope (A never claimed to close store errors), not a regression this leg introduces — recorded as a measured fact and left **open**; **(4)** fully identical → same `memory_id`, duplicate physical line, ledger `IDEMPOTENT_NOOP`, call reports CAPTURED. **Fifth dimension** also executed: a re-sampled `idempotency_key` gives a different `memory_id` and **collapses cell (3) into cell (1)**. **Residuals that stay open and documented, exactly as ratified and NOT built:** the policy-flip TOCTOU window, store errors including §3(vi)'s **intra-call** window, and the §3(iv) cross-candidate duplication measured above. Retry precedence **changed**, as ratified. **§9 items 3–7 cite repairs done**, re-grounded at this leg's final head; two **adjacent** stale cites carrying the same offsets, in files this leg touches (`llm_dispatch.py`'s `_memory_tools_declaring` docstring and `_real_memory_tool_executor`'s docstring at `tests/test_lifecycle_llm_dispatch.py`), were repaired in the same pass and are **recorded rather than smuggled**. **Zero design-substrate edits, zero contract numbers, zero CXA rows, zero spec/plan version bumps. Direction (2) was not re-priced or reopened.**
- **RATIFIED 2026-08-05 as READING A — the executor's pure `validate()` pre-pass** (one method + two dispatch call sites, zero contract text). Operator two-stage `AskUserQuestion` on the fork's §10.1 (`.harness/class_2_fork_b84_memory_batch_partial_commit.md`): stage 1 discriminator = **INPUT**, stage 2 = **A** (not **A+C**). **A+C, C, B and D were not selected and are not partially adopted** — D's `_PreparedMemoryToolCall` docstring-qualification-at-ratification obligation does **not** fire, and B's spec leg is **not** opened. **The held sub-decision is resolved: the Class-2-vs-Class-1 classification is RATIFIED AS CLASS 2** (in-execution operator decision, impl discretion) — the fork does **not** re-file as Class 1. That does **not** endorse the routing skill's Recording-row-vs-practice divergence (`.claude/skills/phase-7-back-flow-routing/SKILL.md` §2.2): it stays a real defect belonging to whoever next revises that skill, and the operative discriminator remains §2.2's **Trigger** row. **The build leg owes:** the `validate()` pre-pass plus witnesses **W-1, W-2, W-4, W-5, W-7** and **W-8 in its INVERTED form** (`limit=0` rejected **before** any retrieval event — not C's residual pin), the §8 mutation probes, **W-5's four-cell matrix executed with its answers recorded back on this row**, and the §9 items 3–7 shipped-source cite repairs in the touched files. **Cross-fork note** *(out-of-family review round 6 [P2])*: the `validate()` pre-pass raises malformed-model-argument failures on the **dispatch prepare path** (`_standard_memory_tool_context`, reached from `:4213` / `:4845`), which runs **before** `StandardMemoryToolExecutor.execute()` opens its telemetry span — and `llm_dispatch.py` contains **zero** `classify_memory_failure` calls, so those failures emit **no** `memory.failure_class`. This moves *more* of that class onto an unclassified path, interacting with `B-88`'s ratified seventh class — and the relocation **newly introduces** a telemetry loss for checks that are classified today, rather than inheriting a pre-existing one *(corrected at rounds 7 and 9)*. **PRESERVATION IS MANDATORY:** `C-MEM-19` (`Spec_Memory_Substrate_v1.md:835`–`:870`) covers *"Standard memory tool call"* and requires `memory.failure_class`; Reading A opened **no** spec leg and this row is `open` with no blocker, so an impl leg **cannot** drop the attribute on its own authority — that is the X-AL-3 silent-absorption failure mode. The build leg **must preserve** classification for every relocated check and **witness the preserved emission end-to-end**; **if preservation proves infeasible that is a CLASS 1 back-flow against `C-MEM-19`, filed and ratified before the code lands.** Only the `:4344`/`:4346` raises' position on the unclassified prepare path is pre-existing. **Accepted costs, so the build leg does not re-litigate them:** the policy-flip TOCTOU and intra-call windows remain, and retry precedence changes. **Status → `open`** — the fork's §10.2 ratification row prescribes the `pr:` stamp and the `close_out` outcome but is **silent on status**, and the enum decides it (a ratified build reading is actionable-now-no-blocker, with no design-substrate gate under A); the row closes when the build leg merges. **No council owed** (the C10 ⊥ memory-ledger-owner dyadic was conditional on Reading B).
- **What it is.** The B-82 arc's round-1 fix made both memory tool loops validate the COMPLETE batch before executing any of it, which closed the *dispatch-side* half of the partial-batch problem. A second half remains, one layer down: validation the DISPATCH layer legitimately does not perform still happens mid-execution, at the executor. Concretely — a batch of `[memory.write_note (valid), memory.read (missing the contract-required `memory_ref`)]` passes every dispatch-side check, so `_openai_memory_tool_result_messages` / `_ollama_memory_tool_result_messages` execute the calls in order: the write COMMITS a durable record, then the read raises `MemoryToolExecutionInputError` from `_string_arg` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:549-553` — *`_string_arg` is at `:678` at 2026-08-05; the whole helper inventory drifted, refreshed in the 2026-08-05 bullet below*). That error **was** retry-classified TRANSIENT — `_classify_provider_exception` fail-fasted only `LLMDispatchProviderUnreachableError` / `LLMDispatchPayloadShapeError` / HTTP 401-403 and mapped everything else to the staircase — so `RetryBreakerFallbackDispatcher` re-ran the whole dispatch from the top and the already-committed write executed a second time. Same double-write class as the round-1 [P1], reached through a different door. **PARTIAL MITIGATION LANDED (2026-07-28)** — `MemoryToolExecutionInputError` now joins the fail-fast branch of `_classify_provider_exception` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:326-334`), so the retry staircase no longer re-runs the dispatch for this class: on the default single-candidate chain a mid-batch executor input failure now consumes exactly ONE dispatch instead of `max_attempts`. **The row stays `registered_finding`** — the retry-duplication half is closed, the partial-commit half is not: the earlier write still commits before the later call raises, and a MULTI-candidate fallback chain CAN duplicate the committed prefix — contingent, not guaranteed (codex 2026-08-05): the failed inner dispatch's `prepared` batch is local and not forwarded; the next candidate re-samples its own tool calls, which may repeat, differ, or be absent — up to one extra copy per candidate when re-sampled alike, versus `max_attempts` × candidates before.
- **What IS already prevalidated (so the residual is genuinely narrow).** The batch pre-pass built at B-82 rounds 1/3 resolves and checks, for EVERY call, before anything executes: the tool_call wire shape (`function` is a mapping; OpenAI's `id` present and non-empty); the name resolving to a `MemoryToolName` (via the wire map on the OpenAI arm, dotted-only on the ollama arm); the `arguments` payload normalizing to a mapping (already-decoded on ollama, JSON-decoded on OpenAI, fail-loud otherwise); and the `RuntimeMemoryContext` checks — `record_scope`/`scope_ref` present, and the `scope_ref` argument matching the context for the two tools whose contracts declare it. What it does NOT check is per-tool required-argument keys (`note` for `write_note`, `memory_ref` for `read`/`propose_promotion`/`request_redaction`, `query` for `search`, …) or per-tool policy gates — because those are the EXECUTOR's semantics, not the dispatch layer's.
- **Why it was not fixed in the B-82 arc.** The obvious fix — add full per-tool contract prevalidation at the dispatch layer — would **mirror-copy the executor's own required-key and policy semantics into a second location**, which is precisely the drift class this workspace treats as a defect in its own right (see `deferred-mechanism-spec-leg-exit-on-soundness` / the one-source-of-truth discipline): the copy silently rots the first time a contract's required set changes, and the two copies then disagree about which calls are legal. Round 3 established the grounded boundary empirically — required-key enforcement lives at `memory_tool_executor.py:266` -> `:549-553`, and the dispatch layer validates only `scope_ref`/`policy_ref` — so widening the dispatch layer here would be inventing new dispatch semantics, not honoring existing ones. Registered rather than fixed unilaterally.
- **AGGRAVATOR — the ollama SDK silently drops the schema constraints the contracts declare** *(out-of-family Codex round 5; verified empirically at registration time against the locked `ollama==0.6.2`)*. The client **revalidates** caller-supplied tool mappings as its own `ollama.Tool` model before posting to `/api/chat` — `ChatRequest.tools: Optional[Sequence[Tool]]` (`ollama/_types.py:400`) — and that model is a **closed field subset** layered over a plain pydantic `BaseModel`: `SubscriptableBaseModel` (`_types.py:19`) sets no `model_config`, so pydantic's default `extra='ignore'` applies; `Tool.Function.Parameters` (`_types.py:365-371`) declares only `type` / `$defs` / `items` / `required` / `properties`, and `Parameters.Property` (`_types.py:372-379`) declares only `type` / `items` / `description` / `enum`. Every other JSON-Schema keyword is therefore discarded en route to the daemon, with no error and no warning. Measured directly against the real C-MEM-14 contracts by round-tripping them through `Tool.model_validate`: `memory.search` loses top-level `additionalProperties: false`, `limit.minimum: 1`, and `query.minLength: 1`; `memory.write_note` loses `note.minLength: 1`. The model consequently never sees the constraints the contracts declare, which **raises the odds it emits an argument payload the executor then rejects** — feeding precisely this row's mid-batch / transient-retry class. This is an aggravator only: it changes the likelihood, not the mechanism, and the mitigation menu below is unchanged. (Orthogonal to the separate, already-fixed B-82 round-5 issue of non-tool-template models 400-ing on `tools=` at all.)
- **Current state — `[SUPERSEDED 2026-08-05]`; the operative state is the RATIFIED bullet at the top of this block (Reading A, `status: open`, build leg owed). What follows is the pre-ratification record.** REGISTERED, partially mitigated (the fail-fast reclassification below landed 2026-07-28; the partial-commit half is what kept this row open). Reachability requires a multi-call batch in which an earlier call is a valid write AND a later call is dispatch-valid but executor-invalid. Narrower than the round-1 [P1] it descends from (which needed only a malformed later call of any kind), and unreachable for single-call batches, which is the common shape.
- **Close-out steps.** Two candidate directions, both real, neither obviously dominant: **(1) an executor-exposed pure `validate()` pre-pass** — the executor grows a side-effect-free method that runs exactly the argument/policy checks `_execute_authorized` would run, and the dispatch batch pre-pass calls it for every call before executing any. Keeps ONE source of truth (the executor still owns the semantics; dispatch only asks) at the cost of a new public surface on the executor and a discipline that every future check lands in the shared path rather than inline. **(2) transactional / journaled batch execution at the executor** — the executor accepts a batch and either commits all durable operations or none, via the existing memory-operation ledger as the journal. Strictly stronger (it also covers a mid-batch failure that no pre-pass could predict, e.g. a policy resolver flipping or an I/O error) but a materially larger change touching durable-write ordering, and it needs a decision about what "rollback" means for an append-only canonical store — likely compensating ledger entries rather than deletion. **The third, cheaper mitigation is LANDED (2026-07-28) and does not close this row:** executor-level input errors are now fail-FAST rather than transient. **Grounding verdict before building — IMPL-DISCRETION, not spec-enumerated.** Runtime spec §14.6 D2 (`design-substrate/Spec_Harness_Runtime_v1.md:3796`) states the predicate as *"Retry-eligible iff the provider exception matches CP §21.2 transient staircase (`REFLEXION` / `RETRY_WITH_BACKOFF` stages); fail-fast otherwise (AUTH, payload-shape, shutdown)"* — fail-fast is the RESIDUAL class and the parenthetical is illustrative, not a closed set. What IS closed is the §14.5 C-RT-15 fail-class table (`:3678-3685`) and CP C-CP-21 §21.1's five-value `ValidatorRetryExitClass` (`design-substrate/Spec_Control_Plane_v1_2.md:1843-1878`, cardinality-5 closed); neither enumerates which concrete Python exception types map to which class — that mapping is the classifier's, and its own docstring already declared itself *"MVP discrimination (conservative)"*. A contract-invalid C-MEM-14 memory tool call matches nothing in §21.2 (network / rate-limit / 5xx) and is the same permanent malformed-input class as `RT-FAIL-PAYLOAD-SHAPE` raised one layer deeper, so the reclassification moves the impl TOWARD the spec predicate rather than extending it (no X-AL-3 surface). **One correction to this row's own earlier rationale:** "the same arguments will fail identically" is imprecise — a retry re-dispatches to the provider and re-samples the tool-call arguments, so a retry *could* emit valid ones. The defensible rationale is the spec predicate + the `RT-FAIL-PAYLOAD-SHAPE` parity, and what fail-fast removes is the duplicate durable commit, not merely wasted attempts. **Scope — `MemoryToolExecutionDeniedError` deliberately EXCLUDED:** `_read_record_by_ref` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:446-449`) wraps ANY store exception, including genuinely transient I/O, into the denied class, and `_allowed_index_entry` (`:424-435`) raises it off mutable index/store state — fail-fasting the `MemoryToolExecutionError` base would kill retries for real flakes; and whether a policy DENIAL is permanently terminal is a C-MEM policy-semantics decision this narrow change does not take. Fail-fast also still permits the outer candidate advance, so a rebinding-recomposed context is not foreclosed either way. **Witnesses** (`harness-runtime/tests/test_lifecycle_retry_breaker_fallback.py`): a fail-fast candidate-advance witness mirroring the B-41 auth test's discriminating shape (which candidate got call 2) plus the `permanent-fail-exit` / `MemoryToolExecutionInputError` span attributes; a single-candidate exactly-one-dispatch witness; a denied-still-retries scope guard. Both probes confirmed: reverting the `isinstance` addition fails the first two (the second reports `assert 3 == 1` — the three-copy duplication made visible), and widening it to the `MemoryToolExecutionError` base fails the third. **Out-of-family Codex round 1 on this arc — one real [P1], fixed here.** `_allowed_kinds` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:574-598`; the bare conversion sat at pre-fix `:584`) raised a **bare** `ValueError` from `MemoryRecordKind(item)` on an unknown `allowed_kinds` string — and since `MemoryToolExecutionInputError` *subclasses* `ValueError` rather than the reverse, a bare `ValueError` misses the fail-fast isinstance check entirely, so a `memory.search` with a bad kind following a committed `memory.write_note` still replayed transient. Wrapped to raise the typed input error naming the offending value + the accepted kinds. **Mandatory sibling sweep — this was the only gap.** Every model-supplied argument in the executor reaches exactly five helpers, all already typed: `_string_arg` (`:549-553`), `_optional_string_arg` (`:556-562`), `_positive_int_arg` (`:565-571`), `_allowed_kinds` (`:574-598`, the fixed site), `_promotion_kind` (`:654-669`, `KeyError` already wrapped); `_kind_from_memory_ref` (`:635-642`) already wraps its enum conversion. `MemoryID` / `Identifier` are `NewType` (`harness-is/src/harness_is/memory_record_envelope.py:23`, `state_ledger_entry_schema.py:33`) — zero runtime validation, not raise sites. The pydantic constructions (`MemoryRetrievalRequest` / `PromotionCandidate` / `MemoryOperationPayload`) carry no constraint reachable from a model argument. `MemoryToolExecutionContext._non_empty_string` (`:113-118`) raises a bare `ValueError` but validates HARNESS-supplied context strings, never model input. Dispatch side: both `MemoryToolName(name)` conversions (`llm_dispatch.py:3562` OpenAI, `:4160` ollama) and every other wire-shape check already raise `LLMDispatchPayloadShapeError`, already fail-fast. Witness at `harness-runtime/tests/test_memory_tool_executor.py` — a real-executor `memory.search` with an unknown `allowed_kind`, asserting the typed error, its message content, and (cross-layer, on the REAL exception instance) `_classify_provider_exception(...) is None`. Mutation-probed: unwrapping the conversion fails the witness with `ValueError: 'not_a_real_kind' is not a valid MemoryRecordKind`, and that same exception then classifies `transient-retry` — the replay. **Adjacent row:** typing this site also *shifted* its emitted telemetry class — the pre-fix bare `ValueError` classified `provider_adapter_failure`, the typed error classifies `io_failure`; neither is correct, and the substring-keyed `classify_memory_failure` defect behind it is registered as **B-88**.
- **Council: NONE OWED — settled 2026-08-05.** Reading A is ratified and the fork's §7 is **probe-resolved for A** (the C10 ⊥ memory-ledger-owner dyadic was owed under **Reading B only**, and B was not selected). *The pre-ratification disposition that follows is **superseded**, retained only as the filing-time record:* **likely.** Direction (2) is a durable-write-semantics decision (all-or-nothing across a batch, and what compensation means for an append-only store) with a blast-radius dimension — C10 paired with whichever voice owns the IS-side memory operation ledger. Direction (1) and the fail-fast reclassification are closer to mechanical once the direction is chosen, but the choice between (1) and (2) is not: it is the difference between "prevent the predictable case" and "make the whole batch atomic," and picking (1) forecloses nothing while picking (2) subsumes it.
- **2026-07-30 re-price note (post-#1159/#1160, mirrors the YAML summary).** Direction (2)'s cost assessment predates U-MEM-27: the generic `write_record_guarded` compare-and-commit seam (`harness-is/src/harness_is/memory_store.py`) and the reentrant per-root `cross_process_scope_lock` now exist, so a batch could hold the root scope across multiple writes without self-deadlock — the (1)-vs-(2) tradeoff leans differently than the recorded lean. Defect premise unchanged. Re-price BOTH directions at build time rather than reading the recorded lean.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT; this was genuinely still-open work at that pass, NOT completed-and-unre-checked; disposition FORK-SHAPED — and the fork was RESOLVED the same day as **Reading A** (see the ratification bullet at the top of this block; everything below it is the pre-ratification record).** **Landed half confirmed:** `MemoryToolExecutionInputError` is documented in the fail-fast branch of `_classify_provider_exception` at `retry_breaker_fallback.py:293` and is the `isinstance` member at `:330`, with `MemoryToolExecutionDeniedError` still deliberately excluded at `:312`. **Both open directions confirmed unbuilt:** the executor exposes **no** public `validate()` pre-pass — its public execution surface is `StandardMemoryToolExecutor.execute()` at `memory_tool_executor.py:231` (which alone carries the access checks, telemetry, and ledger recording, and invokes the private `_execute_authorized` helper at `:240`/`:259`; calling the helper directly bypasses those) (direction 1 unbuilt) — and there is no transactional/journaled batch surface at the ACTUAL standard-memory batch owner — the sequential per-call loop `for call in prepared: standard_memory_tool_executor.execute(...)` in `lifecycle/llm_dispatch.py` (~`:4234` and its sibling ~`:4865`; re-grounded 2026-08-05 by direct read — `lifecycle/memory_tool_dispatch.py` is the Anthropic `memory_20250818` callback loop and never invokes `StandardMemoryToolExecutor`, so it is the WRONG surface for this check) (direction 2 unbuilt). So the partial-commit half survives; cross-candidate duplication is CONTINGENT on the next provider re-emitting like calls (the prior `prepared` batch is not forwarded). **Helper line cites refreshed** (the B-84 fix itself grew `memory_tool_executor.py`; every `:5xx`/`:6xx` helper cite above is stale by roughly +129 lines): at HEAD the five argument helpers are `_string_arg` `:678`, `_optional_string_arg` `:685`, `_positive_int_arg` `:694`, `_allowed_kinds` `:703`, `_kind_from_memory_ref` `:764`, `_promotion_kind` `:783`; the two denied-class sites are `_allowed_index_entry` `:516` and `_read_record_by_ref` `:545`. The 2026-07-30 re-price note is **`[SUPERSEDED 2026-08-05]` as an instruction** *(out-of-family review round 9 [P2])* and retained only as history: *"re-price **both** directions at build time rather than reading the recorded lean"* would **reopen the transactional direction (2) the operator REJECTED** by selecting Reading A. **Direction (2) is not to be re-priced or reopened by the build leg.** What survives is the factual observation that U-MEM-27's `write_record_guarded` (`memory_store.py:298`) and the reentrant `cross_process_scope_lock` both exist at HEAD — facts the ratified Reading A build may use, not a mandate to re-open the fork.


### B-85 · A mixed memory+caller tool batch returns the advertised memory call unserved and un-ledgered *(surfaced at the B-83 fix, 2026-07-28)*

- **What it is.** Both standard-memory-tool continuation loops, on seeing a response whose `tool_calls[]` mixes a C-MEM-14 memory call with a caller-advertised tool call, hand the **whole response back unprocessed** and execute nothing from it. That is a deliberate B-82 round-2 choice, for batch atomicity + bare-path parity: a mixed batch must not half-commit memory before control returns to the caller. The consequence is this row: the memory call the model explicitly asked for is **never served, and nothing records that it wasn't** — no span, no ledger entry, no error. The caller cannot serve it either, since it does not own the memory tools.
- **Reachability.** `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py:3389-3401` (openai arm) and `:3979-3987` (ollama arm) — line numbers at the B-83 arc's final HEAD (they drifted across the arc's later rounds; corrected at the merge-gate) — the two `if not _all_calls_are_memory_tools(...)` early returns. Both sites carry an inline `B-85` marker, which is the durable anchor if lines drift again.
- **Why it was not fixed at the B-83 arc.** B-83's repair is *prompt composition*, applied **before** the provider call. This exit happens **after** a response is already composed, and a prompt packet cannot retroactively answer a tool call the model has already emitted. There is no mechanical repair to apply — only a posture to choose. Note this is **not** a defect in the atomicity choice, which remains correct.
- **Current state — CLOSED 2026-07-28 (Round 37); posture (3) RECORD landed.** Split out of B-83 rather than absorbed by it, precisely because the B-83 repair provably cannot reach it.
- **Close-out steps.** Choose the posture; the mechanics follow trivially. **(1) FAIL** — raise at the mixed-batch detection so the unserved memory call is loud rather than silent; consistent with the surrounding `LLMDispatchPayloadShapeError` conventions, but it turns a currently-working caller-tool dispatch into a failure. **(2) SPLIT** — execute the memory calls and return the caller's calls unprocessed; restores service but reopens exactly the half-commit window B-82 round 2 closed (the memory writes commit, the caller's handling can then fail, and the whole dispatch retries under the TRANSIENT classification). **(3) REPAIR/RECORD** — keep the return-whole semantics and emit a degraded/unserved telemetry span at the exit; cheapest, preserves atomicity, but leaves the model's request genuinely unanswered. (3)'s span is emittable with the existing closed enums; a **ledgered** entry is not, under the same X-AL-3 constraint recorded at B-83's close-out.
- **Council: likely.** This is the same C-MEM-13/14 serve-semantics decision B-83 answered for the pre-call case, now for the post-response case, and each option trades a different harm: (1) blast radius on working dispatches, (2) reopens a durable double-write window, (3) accepts a silent-to-the-model outcome. C10 (blast radius) paired with whichever voice owns the memory access-mode selection semantics.
- **CLOSED 2026-07-28 (Round 37).** Landed in the same PR as the B-86 Class 1 fork filing.
  - **Posture (3) RECORD chosen, and grounding is what chose it — the spec is genuinely silent.** C-MEM-14 (`design-substrate/Spec_Memory_Substrate_v1.md:489`) obliges tool **exposure**, not service. C-MEM-13's must-ledger clause (`:463`) binds only `no_memory_access` transitions, which this exit is not. And C-MEM-08's **closed** 12-kind operation enum (`:247` / `:251`) has no way to express *requested-but-unserved* — which is precisely what makes the **ledger** half X-AL-3-blocked and the **span** half emittable, exactly as this row's own close-out predicted.
  - **Emission shape — a SEPARATE single-slot sink + emitter, deliberately not the degraded-serve one.** The degraded-serve emitter's `rendered=None` shape hardcodes `DENIAL` / `no_memory_access` / `degraded_serve=True`; all three are **false** at iteration k>0, where the loop has already served k rounds of memory calls. The new span carries `operation_name=standard_tool_call`, `access_mode=standard_memory_tools`, `record_count=0`, **no** packet_hash, **no** failure_class, `reason=mixed_batch_caller_owned`, plus a quantitative `memory.unserved_tool_call.count` — every one of them an attribute **value** per the B-83 precedent, never a new member of a closed enum. Return-whole semantics are byte-identical, and a zero-memory-call caller-only batch deliberately emits **nothing** (negative-asserted, so the row cannot drift into emitting on every mixed batch).
  - **Postures (1) and (2) rejected as Class-2-shaped, not as unworkable.** **(1) FAIL** inverts the witnessed bare-path-parity invariant — enabling memory must never turn a working caller-tool dispatch into a failure — and pays fail-fast candidate abandonment for what is an *observability* gap. **(2) SPLIT** reopens exactly the duplicate-durable-write window B-82 round 2 and B-84 / PR #1143 closed, on a path where the harness cannot serve the caller half anyway.
  - **A fourth option was enumerated and rejected.** Withhold the memory tools whenever the step advertises caller tools, making the mixed batch *unrepresentable*. Genuinely preventive rather than observational — but it disables memory on every tool-declaring step and changes **which** memory a run reaches, which is B-86's territory, not this row's.
  - **Stated honestly.** The span may fire 0..N times across retry attempts, because model-emitted batches are nondeterministic; that is the same property the B-83 span has, and the witnesses do not assert exactly-once.
  - **Not foreclosing.** Advisor and grounding both record that choosing (3) leaves (1) and (2) available later as a Class 2 fork, and that (3) is the option which preserves the committed B-82-round-2 atomicity decision.


### B-86 · Cross-family fallback serves the chain primary's memory scope to a different-family provider on the SERVABLE tools path *(surfaced while grounding B-83 [P1-c], 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*

- **What it is.** The B-83 fix stops the degraded-serve *packet* from crossing a provider-family boundary. The **servable tools path** crosses the same boundary and was deliberately left untouched. On the `memory_runtime` path a cross-family fallback *recomposes* the context for the current candidate, so `select_memory_access_mode` re-picks `STANDARD_MEMORY_TOOLS` from the **candidate's** capabilities and the tools are injected — but `compose_for_dispatch` derives `record_scope.provider_family` from `fallback_chain.primary.family` (`harness-runtime/src/harness_runtime/automatic_memory.py:191-199`), **not** from the candidate. The injected schemas bind `scope_ref` (a hash of that primary-family scope) and `policy_ref` as fixed enum values (`_openai_standard_memory_tools` / `_ollama_standard_memory_tools`), so the candidate can `memory.search` **across**, and `memory.write_note` **into**, a scope belonging to a different provider family — read disclosure *and* write attribution, where B-83's packet leak was read-only.
- **The field is a real boundary, not decoration.** `MemoryRetriever._scope_mismatch` (`harness-is/src/harness_is/memory_retrieval.py:371-384`) filters candidate records on `provider_family`, which is what makes the primary-derived scope load-bearing in the first place.
- **Residual, same family.** `provider_family_for_provider` (`harness-runtime/src/harness_runtime/lifecycle/cross_family_cost_tag.py:82`) falls back to `LOCAL_OPEN_WEIGHT` for an **unknown** provider key, so an unregistered provider compares equal to an ollama-scoped packet at B-83's new family conjunct — conservative for its original cost-attribution use, wrong for a scope-boundary check.
- **Current state — `closed` (2026-07-28, at the U-MEM-26 impl leg; `registered_finding` → `design_substrate_gated` at the fork filing, → `open` at the spec leg, → `closed` here — all three transits on the same day).** The **C-MEM-13 withhold guard landed** as the **sixth conjunct** of `_standard_memory_tools_context` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`), sharing `_packet_scope_matches_dispatch_family` **verbatim** with the predicate `_degraded_serve_disposition` already reads — deliberately *one* predicate, not a second cross-family test: a divergent test would let a cross-family dispatch fail the arm guard, fall through to `_degraded_serve_for_unservable_arm`, pass disposition conjunct 2, and land on the `arm_unservable` **repair** branch, serving the same cross-family packet as prompt text. Downstream is by construction — the guard returns `None`, the disposition reports `provider_family_scope_mismatch` with `rendered=None`, the plain arm dispatches with no schemas / no `scope_ref` / no packet, and the degraded span carries DENIAL / `no_memory_access` / `record_count=0` / no `packet_hash`. The dispatch still completes. **Two defect-pinning tests were inverted** with contract cites rather than deleted: the B-83-era R5 witness (which required the tools to be *armed* cross-family so the daemon could refuse them) is now `test_u_mem_26_cross_family_ollama_dispatch_withholds_tools_and_packet`, and the r300 `api.run` integration test — which asserted the five C-MEM-14 wire names **do** reach the cross-family openai leg — now asserts the opposite at `test_r300_deterministic_cross_family_fallback_through_production_path`, so the withholding is proven on the **canonical production path**, not only at the unit boundary. Net posture is the report-only **denial**, with **harness-authored capture unaffected** (C3's condition of concurrence), witnessed with the run's composed scope. **Pre-existing** — the tools path had behaved this way since U-MEM-16 / B-82. The close-out steps below are **history (pre-ratification)**.
- **Why it was scoped out of the B-83 arc.** Closing it means either disabling memory entirely on every cross-family fallback, or recomposing the scope per candidate. Both change *which memory a run can reach* — a C-MEM-11/13 scope-semantics decision, not the dispatch-side serve-check B-83 owns.
- **Close-out steps.** **(1)** Decide whether `record_scope.provider_family` should track the **dispatched candidate** rather than the chain primary. Ground it first: does a per-candidate scope *fragment* memory across a fallback chain — each family reading a disjoint record set, so a fallback silently loses the run's accumulated memory — and is that preferable to the current cross-family reach? This is the load-bearing question and it is not mechanical. **(2)** If the scope stays primary-derived, decide the dispatch-side posture for a cross-family *servable* dispatch: withhold the tools (memory off for the fallback leg, consistent with B-83's packet posture), or keep them and accept documented cross-family reach. **(3)** Independently and cheaply: make the unknown-provider fallback explicit at B-83's call site — an unknown key should not silently compare equal to `LOCAL_OPEN_WEIGHT` for a scope-boundary decision; a `None`/unknown sentinel forcing report-only is the conservative shape and does not disturb the cost-attribution caller.
- **Council: likely, for (1) and (2).** Whether memory scope follows the chain primary or the dispatched candidate decides which records a run can read and write across a fallback, trading a disclosure/attribution boundary against memory continuity on fallback — C10 (blast radius) paired with whichever voice owns C-MEM-11/13 retrieval scope semantics. (3) is mechanical once (1) is settled and could land ahead of it.
- **GROUNDED + COUNCIL-CONVENED 2026-07-28 (Round 37); flipped `registered_finding` → `design_substrate_gated`.** Filed as `.harness/class_1_fork_b86_memory_scope_provider_family_keying.md` per the **B-70 discriminator**: a real gap that needs spec-level CONTRACT text before code. No `design-substrate/**` file is edited by the filing; the spec-leg is a separate follow-on arc per the B-33 / B-59 / B-70 / B-72 precedent.
  - **The council (C10 + C3 co-primary, C6 consultant) probe-RESOLVED the Q1 tension.** The leak's carrier is the **unconditional `scope_ref` binding at the injection site**, not the scope's *key*. Probe E is the dissolving finding: the **writer is already candidate-derived today** (`memory_capture.py:585-591`) and the disclosure occurs anyway — so re-keying the scope would suppress it only as a side effect of shattering the partition. Neither voice conceded its axis; the mechanism was the conflation.
  - **Recommendations (operator holds decision authority).** **Q1** — key `record_scope.provider_family` to the **chain primary** (a run-level partition), stated in the spec so it stops being an accident. **Q2** — **withhold** the standard memory tools on a cross-family *servable* dispatch, reporting a named denial reason (the B-83 disposition shape), with C3's condition of concurrence that **harness-authored automatic capture is unaffected**.
  - **Stated limit (C6), recorded as a non-claim.** Family equality is *necessary but not sufficient*: `local_terminal` sits **within** `local_open_weight` while carrying the slate's most restrictive trust posture. Addressed outside this contract, and needs its own pass post-spec-leg.
  - **What the spec-leg owes** (drafting targets at fork doc §6): a **C-MEM-03 value domain** plus explicit `null` semantics (the wildcard-on-`None` behaviour is load-bearing and currently undocumented), the **derivation rule** (run-level, composed once, capture writes under the run's composed record scope), a **C-MEM-13 withhold invariant**, and a **C-MEM-14 exposure qualification**. Threat model `:481` needs no amendment — B-86 is *conformance repair to a cleared invariant*, which is what keeps the spec-leg X-AL-3-clean.
  - **Item (3) LANDED in the same PR as the filing.** `provider_family_for_scope_check` now fails **closed** (`None`) on unregistered provider keys at `_packet_scope_matches_dispatch_family`, so an unknown key no longer compares equal to a `local_open_weight`-scoped packet. Previously the packet got **repaired** — a live leak; it is now report-only, and mutation-probed.
  - **Forward items carried by the filing.** The direction consequence for the producer key-vs-value asymmetry is recorded at **B-89**; the adjacent tenant / `workload_class` omission is registered as **B-90**.
- **SPEC-LEG LANDED 2026-07-28.** `Spec_Memory_Substrate_v1.md` v1 → v1.1 (C-MEM-03 value domain + `null` semantics + derivation rule; C-MEM-13 cross-family withhold invariant; C-MEM-14 exposure qualification; the threat-model `:481` invariant unchanged and standing as the authority, which is what keeps the leg X-AL-3-clean) plus the paired plan delta `Implementation_Plan_Memory_Substrate_v1.md` v1 → v1.1 (**NEW U-MEM-26**). Clearance markers at `.harness/clearance/spec-memory-substrate-v1-1-cleared-2026-07-28.md` + `.harness/clearance/implementation-plan-memory-substrate-v1-1-cleared-2026-07-28.md`. The **impl leg** — the C-MEM-13 withhold guard, the `B-89` writer-side repair, and the incidental `B-90` fold-in, all decomposed at U-MEM-26 — is the remaining gate on this row.
- **IMPL-LEG LANDED 2026-07-28 — row CLOSED.** U-MEM-26 shipped in four slices: the `harness-is` read boundary (null asymmetry, the `B-90` predicate trio, the request-side value domain), the `B-89` writer repair plus write-boundary enforcement at all scope-authoring surfaces, the C-MEM-13 withhold guard with its production-wired witnesses, and the docs/register refresh. Wire-name / HTTP-400 coverage displaced by the r300 inversion is preserved on the same-family paths that still inject. Mutation probes: dropping the conjunct reddens all three withhold witnesses; swapping it for a raw provider-name `!=` reddens 18 same-family ollama tests — the shared-predicate discipline is load-bearing, not cosmetic. The C6 stated limit (family equality is *necessary but not sufficient*; `local_terminal` sits within `local_open_weight`) is explicitly **out of scope** for U-MEM-26 and still needs its own pass. Arc record at `.harness/u-mem-26-impl-leg-close-2026-07-28.md`.


### B-87 · Degraded-serve span over-reports pre-wire failures as completed injections *(surfaced by out-of-family Codex round 8 on the B-83 arc, 2026-07-28; CLOSED 2026-07-29 — PR #1149)*

- **What it is.** B-83 round 6 moved the degraded-serve span emission into a `finally` around the whole arm-dispatch chain, so a repair that was *sent* and then failed still reports. That correctly covers **post-wire** failures. It over-covers **pre-wire** ones: when the repair is authorized, `_DegradedMemoryServe.rendered` is populated at the serve-check — *before* any translation runs — and the per-arm translators can raise before the provider adapter is ever called. The `finally` then emits `operation_name=injection` + `access_mode=prompt_extension_packet` + the packet_hash + a non-zero record_count: the span asserts the packet **was injected**, when nothing left the process.
- **Verified empirically at registration time, not hypothesised.** Driving the residual two-base case — an `active_system_prompt` bound *and* a payload-owned `params["system"]`, which B-83 round 4 deliberately leaves as a fail-loud `PromptInjectionConflictError` — through the real dispatcher on a repair-eligible context produced **zero adapter calls** and yet a span carrying `memory.operation.name=injection`, `memory.access_mode=prompt_extension_packet`, `memory.packet_hash=cccc…`, `memory.record_count=1`, `dispatch_outcome=provider_error`, `provider_error_type=PromptInjectionConflictError`.
- **Reachability.** Any translation-layer raise on a **repaired** dispatch: the two-base `PromptInjectionConflictError` residual (reachable on all four arms), `LLMDispatchPayloadShapeError` from the external-CLI text-only tools rejection, and any future pre-adapter validation. **Not** reachable for report-only dispositions (`rendered is None` already emits `denial` / `no_memory_access` / no packet_hash), nor when the repair is unauthorized. Note that `test_b83_two_base_system_sources_still_fail_loud_through_the_repair` already exercises this exact path but asserts only the raise, not the span — the suite passes *over* the defect rather than covering it.
- **Candidate fix.** A reached-the-wire marker set at the provider-call boundary — the one place per arm where the adapter is actually invoked — with the `finally` emitting a **distinct** classification when the marker is unset: pre-wire failures reported as e.g. `denial` plus a `dispatch_outcome="translation_error"` value and **no** packet_hash, since no packet reached a provider. Mechanically small; the care is in threading the marker to every arm's true adapter-call site (including the ollama R5 bare retry) without reintroducing the round-6 loss on genuine post-wire failures.
- **Current state.** **CLOSED 2026-07-29 at PR #1149** (branch `b87-reached-the-wire-prewire-marker`) — all three close-out steps land.
  - **Mechanism.** A frame-local `_ProviderWireReachedSink` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) is constructed once per dispatch and threaded to every arm; `_emit_degraded_memory_serve_span` gained a `reached_wire` parameter and branches on it. Marker **set** → today's post-wire shape **verbatim** (`injection` + packet_hash + record_count, `completed` or `provider_error`). Marker **unset** → `dispatch_outcome="pre_wire_failure"` plus a new `memory.degraded_serve.pre_wire_error_type` attribute, with **no** packet_hash and no `injection` operation name (the existing closed-enum member `packet_assembly` is emitted instead).
  - **The candidate name `translation_error` above was rejected**, at out-of-family Codex R1 [P2], as over-claiming: *any* pre-wire phase can fail — payload-shape rejection, adapter selection, a repair-authorization raise — not only translation. The shipped value is phase-neutral by construction and names the **boundary**, not the phase.
  - **Placement, per arm.** Direct-SDK arms construct the awaitable, mark, *then* await — so a call-time **synchronous** `TypeError` from the SDK is correctly pre-wire (R7). The external-CLI arm fires **inside** the subprocess runner after `create_subprocess_exec` returns — the semantic anchor is that a child process now exists that can observe the payload, so a spawn failure stays pre-wire (R2/R3). The anthropic-memory arm fires once inside `execute_with_memory_callbacks` before its first SDK call (R5), since that helper owns a tool loop the dispatcher cannot see into.
  - **Two declines, on the record.** (a) **In-coroutine vendor-side validation** that raises after the awaitable is entered is **post-wire by design** — resolving it would need SDK-internal hooks that do not exist. (b) Consequently the SDK arms' boundary is **our last observable point**, deliberately; this row does not claim byte-on-the-wire fidelity.
  - **Compatibility tiers.** An adapter or runner that does not **declare** an explicit `on_wire` parameter (a bare `**kwargs` deliberately does *not* count, R4) keeps the pre-B-87 marker placement, discriminated by the single shared `accepts_explicit_on_wire` predicate; the public runner seam statically admits **both** shapes (R6).
  - **Robustness.** An `on_wire` callback that raises after spawn kills and reaps the child, and the orphaned coroutine is closed rather than leaked (R7).
  - **Witnesses.** Step (2) landed as specified — `test_b83_two_base_system_sources_still_fail_loud_through_the_repair` now asserts the **span** as well as the raise. Step (3) resolved as `dispatch_outcome`, an attribute **value**; no member added to any closed C-MEM-19 enum. 8 out-of-family Codex rounds (9 real findings), ~25 mutation-probed witnesses across 4 test files, `just check` 7014 passed.
- **Why registered rather than fixed now — direction rationale.** Two reasons, both deliberate. **(1) The imprecision over-reports, not under-reports.** It claims a disclosure that never occurred; it never hides one that did. An investigator reading this span over-responds — checks a provider that was never called — rather than missing a real leak. That is the fail-safe direction for a telemetry defect on a disclosure-adjacent surface, and it is what makes deferral tolerable at all. **(2) This arc reached its fix-round cap at Codex R7** per the workspace's non-convergent-adversarial-hardening discipline: eight review rounds on a single repair, each finding a narrower instance of an already-characterized surface, is the point at which continued in-arc fixing costs more than it returns. Registering keeps the finding live and specified rather than absorbed.
- **Close-out steps.** **(1)** Add the reached-the-wire marker and branch the `finally` emission on it — marker set → today's behavior verbatim; marker unset → the pre-wire classification with no packet_hash and no `injection` operation name, since claiming a packet_hash for a packet no provider ever saw *is* the defect. Ground the exact adapter-call sites first (there is more than one per arm: anthropic has three variants, ollama has the standard-tools loop *and* its R5 bare retry), and mind that the marker must be set **before** the awaited call, not after — otherwise a post-wire failure regresses to being reported as pre-wire, the mirror image of this row and the exact ordering trap B-83 round 6 [P2-d] already hit once with the R5 sink. **(2)** Extend the existing two-base witness to assert the **span** as well as the raise; the path is already covered, only the assertion is missing. **(3)** Decide whether `dispatch_outcome` gains a `translation_error` value or whether pre-wire failures ride the `reason` axis — either is an attribute **value**, never a new member of the closed C-MEM-19 enums.
- **Council: no.** A telemetry-accuracy fix inside an already-settled span contract, with the classification axis (attribute values, not enum members) established by B-83 rounds 5–7. No cross-domain tension to surface: nothing about *which* memory is served or *who* may see it changes — only whether the span truthfully reports that a disclosure occurred. The one judgement call (`dispatch_outcome` vs `reason`) is impl discretion once the marker exists.

### B-88 · Memory failure telemetry: substring-keyed `classify_memory_failure` collapses the typed input-error family — and two real policy denials — to `io_failure` *(surfaced by the merge-gate lens-2 sweep on the B-84 arc, 2026-07-28)* *(**RATIFIED 2026-08-05 as READING A, sub-decision A-ii = RE-TYPE**; SPEC LEG LANDED 2026-08-06 at PR #1245; IMPL LEG (U-MEM-28) LANDED 2026-08-07 -- CLOSED, with ONE known non-conformant type-boundary site carried at `B-114`)*

- **RATIFIED 2026-08-05 as READING A, with sub-decision A-ii = RE-TYPE.** Operator `AskUserQuestion` on the fork's §8 (`.harness/class_2_fork_b88_memory_failure_input_validation_class.md`). `C-MEM-19` gains **`input_validation_failure`** as a **seventh** failure class (the stated fallback name `input_error` is **not** taken), plus the **boundary invariant** separating it from provider-adapter and serialization failure. **A-ii is ratified in its RE-TYPE form:** the `llm_dispatch.py` sites raising `MemoryToolExecutionInputError` for **internal** `MEMORY_TOOL_CONTRACTS` schema faults contradict that invariant and are **re-typed** — not recorded as mis-typed-at-birth and left in place. **Scoped to SIX sites, not eight** *(out-of-family review round 4 [P2], after direct read)*: the internal set is `llm_dispatch.py:4334`/`:4338` (the harness's own `RuntimeMemoryContext` left unset) and `:4450`/`:4458`/`:4947`/`:4958` (`MEMORY_TOOL_CONTRACTS` schema-injection faults). **`:4344` and `:4346` are NOT re-typed** — they validate the **model-supplied** `scope_ref` argument (missing/non-string, then context-mismatched), which is exactly what the ratified `input_validation_failure` class exists for. **Coverage narrowed honestly** *(out-of-family review round 6 [P2], after direct grounding)*: `classify_memory_failure` has **four** production call sites (`memory_redaction.py:273`, `memory_capture.py:778`, `memory_tool_executor.py:255`, `native_memory_adapter.py:556`) and **zero in `llm_dispatch.py`**. The two retained raises sit in `_standard_memory_tool_context`, called from the **dispatch prepare path** (`:4213` / `:4845`) which runs **before** `StandardMemoryToolExecutor.execute()` opens its span — so they emit **no** `memory.failure_class` today, and the re-type does not change that. **A-ii establishes reachability for the classify-routed population, not for the prepare path**, and the spec leg must say so rather than promise emission it does not wire. **Cross-fork note, in TWO parts** *(separated at out-of-family review round 7 [P2]; an earlier revision called the whole residue pre-existing, which over-claimed)*: **(i) pre-existing** — the `:4344`/`:4346` raises already sit on the unclassified prepare path at HEAD; **(ii) newly introduced by `B-84`'s ratified pre-pass** — checks that today raise *inside* `_execute_authorized()` (`limit=0` among them) **are** classified today via `execute()`'s catch (`memory_tool_executor.py:255`), and relocating them to the prepare path **removes** that emission, which none of `B-84`'s ratified witnesses asserts. **PRESERVATION IS MANDATORY** — the *"or ratify the loss"* arm carried at round 7 is **withdrawn at round 9 [P2]**, because it would authorize a contract weakening with no design-substrate change. **Grounded:** C-MEM-19 (`Spec_Memory_Substrate_v1.md:835`–`:870`) lists **"Standard memory tool call"** among the covered operations and **`memory.failure_class`** among the **required** attributes; Reading A opened **no spec leg** and this row is `open` with no blocker, so an impl leg cannot drop that attribute on its own authority — that is the X-AL-3 silent-absorption failure mode. **The build leg MUST preserve classification for every relocated check and witness the preserved emission end-to-end. If preservation proves infeasible, that is a CLASS 1 back-flow against C-MEM-19, filed and ratified BEFORE the code lands** — not an impl-discretion call, and not closable by witnessing the absence. This constrains *how* Reading A is built, not *whether*. **Retry-exit-class consequence of the re-type** *(round 7 [P2], grounded by direct read + probe)*: `_classify_provider_exception` (`retry_breaker_fallback.py:269`) returns `None` (fail-fast) only for an isinstance match including `MemoryToolExecutionInputError` (`:325`–`:333`); everything else falls through to `TRANSIENT_RETRY` (`:337`). **Re-typing the six sites away from that class flips them fail-fast → retry unless the new type is admitted to the tuple**, so internal wiring/schema faults would begin retrying and advancing candidates. The **spec leg must record the intended exit classification** and the **impl leg owes an end-to-end dispatch/retry witness** — this is about RETRY behaviour and is *not* covered by the telemetry-coverage narrowing. **Leg ownership, made explicit** *(out-of-family review round 1 [P2])*: the **spec leg** records the boundary invariant and the re-type **decision** in contract text and establishes the dispatch population's classification **reachability**; the **impl leg** performs the **source** re-typing at the `llm_dispatch.py` sites and carries its reachability **witness** (the fork's *impl rider*). **Readings B and C were not selected and are not partially adopted** — in particular **C's docstring-rewrite-at-ratification obligation does NOT fire**: under A the `memory_tool_executor.py:149`–`:171` stopgap docstring is rewritten **at the impl leg** (fork §9), and no `harness-*/src` edit rides a doc-only ratification. **Status → `design_substrate_gated`** at the ratification, → **`open` at the 2026-08-06 SPEC LEG** *(the enum at `.harness/forward-register.yaml`'s head is explicit: only ratified + applied spec deltas plus their Phase-6 plan deltas open the row for code — both landed there)*. **Spec leg LANDED 2026-08-06 (PR #1245):** `Spec_Memory_Substrate_v1.md` v1.2 → v1.3 **and** `Implementation_Plan_Memory_Substrate_v1.md` v1.2 → v1.3, **both** clearance markers filed. Two departures from the text above are recorded there rather than absorbed, neither changing a disposition: **(i)** the plan delta extended U-MEM-22 `:829` to seven values *as prescribed* but **did** add a new unit — **U-MEM-28 at G8** — because the fork's *"no new unit"* line predates A-ii's ratified scope and the six-site re-typing + retry witness had no owning unit (flagged overturnable); **and (i-b) a SEVENTH re-typing was proposed and WITHDRAWN** — `_prepare`'s executor-side exhaustiveness fall-through (`memory_tool_executor.py:373`) is a harness-internal fault that spec v1.3's type-boundary rule forbids raising as the input-error type, at a site the fork never surveyed because it surveyed `llm_dispatch.py` only. The spec leg initially required it; **out-of-family round 8 objected that a spec leg APPLIES ratified fixes and may not widen a ratified six-site enumeration on its own reading, and the objection was accepted** — the obligation is removed and the finding carried, with the consequence stated: **at U-MEM-28's close, C-MEM-19's type-boundary invariant has ONE known non-conformant site, recorded and unrepaired**, **that finding is now owned by its own row, `B-114`**, so it survives this row's close at U-MEM-28 rather than disappearing with it *(out-of-family round 9 [P1])*. Everything in U-MEM-28 therefore stays within the ratification's own enumerated impl-leg list; **(ii)** the classify-routed population is **five** call sites at HEAD `3a1300eb`, not four — PR #1243's `B-84` validate pre-pass added `memory_tool_executor.py:355` and thereby **satisfies the mandatory C-MEM-19 preservation clause as built** — and every `llm_dispatch.py` offset in the six/two enumeration has shifted (internal: `:4359`/`:4363`/`:4475`/`:4483`/`:4977`/`:4988`; **retained** model-argument: `:4369`/`:4371`), re-resolved by content with the split itself unchanged. **What remains is the impl leg** (1 enum member + 1 declaration flip + 1 docstring rewrite + 1 fixture re-key; 8 witness rows flip + 2 added negative rows + the mutation probes; **plus the A-ii source re-typing** at the `llm_dispatch.py` internal-fault sites and its reachability witness). **Zero hash / CXA / contract-number impact. No council owed** (the convening was conditional on Reading B).
- **What it is.** `classify_memory_failure` (`harness-is/src/harness_is/memory_observability.py:150-163` *(the classifier sits at `:166`–`:200` at 2026-08-05; filing §10)*) classifies into the C-MEM-19 failure vocabulary by matching **substrings** of the exception message and of the lowercased type name. Its third rule is `isinstance(exc, (OSError, IOError)) or "io" in exc_name` — and `"io"` is a substring of **"execut·io·n"**. Every member of the `MemoryToolExecutionError` family therefore matches: `memorytoolexecutioninputerror`, `memorytoolexecutiondeniederror`, and the base `memorytoolexecutionerror` all contain `"io"`. The rules are ordered, so a member escapes only if an EARLIER message rule happens to fire first. That is the whole of what keeps policy denials out of the IO bucket today — an accident of wording, not a type distinction.
- **Empirical evidence (probed at registration time, not inferred).** Fed real exception instances:

  | exception | message | emitted `memory.failure_class` |
  |---|---|---|
  | `MemoryToolExecutionInputError` | `unsupported allowed_kinds entry …` | `io_failure` |
  | `MemoryToolExecutionInputError` | `memory tool argument 'note' must be a string` | `io_failure` |
  | `MemoryToolExecutionInputError` | `memory tool argument 'limit' must be >= 1` | `io_failure` |
  | `MemoryToolExecutionInputError` | `unsupported promotion target_kind …` | `io_failure` |
  | `MemoryToolExecutionError` (base) | `write_note capture failed` | `io_failure` |
  | **`MemoryToolExecutionDeniedError`** | `memory ref mem:x is **superseded**` (`memory_tool_executor.py:433`) | **`io_failure`** |
  | **`MemoryToolExecutionDeniedError`** | `memory ref mem:x is **not retrievable**` (`:435`) | **`io_failure`** |
  | `MemoryToolExecutionDeniedError` | `memory ref mem:x denied by policy` | `policy_denial` (correct) |
  | bare `ValueError` (the pre-B-84 `_allowed_kinds` shape) | `'x' is not a valid MemoryRecordKind` | `provider_adapter_failure` |

  Note the last row: the SAME logical failure changed telemetry class as a side effect of B-84 typing it (`provider_adapter_failure` → `io_failure`). Neither value is right — which is the point.
- **Why it is a defect and not a nit.** `Spec_Memory_Substrate_v1.md:641` *(at `:870` at 2026-08-05; filing §10)* (C-MEM-19 Invariants) requires: *"Failure telemetry must distinguish policy denial, path violation, IO failure, serialization failure, provider adapter failure, and retrieval empty-result."* Two genuine policy denials emitting `io_failure` violates that invariant **on an axis the vocabulary already has** — an operator triaging memory IO failures sees records that were merely superseded or unretrievable. **A second, spec-side half:** the six-value vocabulary has **no input-validation class**, so even a correct classifier has nowhere truthful to put a malformed-argument failure. Whether to add one is a C-MEM-19 amendment — design-substrate, not impl — and must route as back-flow, not be absorbed here.
- **Reachability.** Every standard-memory-tool failure path. `StandardMemoryToolExecutor.execute` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:167-175`) calls `classify_memory_failure(exc)` in its blanket `except`, so the attribute is emitted on every raise, including the now-fail-fast B-84 class. Pre-existing and latent — it predates B-84 and is orthogonal to it, but B-84 is what made it visible (and shifted one class).
- **Adjacent, same family — the fail-fast breaker-cause emission is unwitnessed for this class** *(merge-gate lens 3)*. On the fail-fast exit, `retry_breaker_fallback.py` calls `breaker.record_failure(cause=_classify_breaker_cause(exc))`, which returns `None` for an exception with no `.status_code` — correct, but nothing pins it: a mutation hardcoding `BreakerCause.AUTH_FAILURE` there **survives** the B-84 witnesses. Same telemetry-precision family (a failure emitting a confidently wrong classification), same fix shape (assert the emitted classification, not just the control flow), so it is recorded here rather than as a separate row.
- **Current state — SUPERSEDED 2026-08-05.** *(Current state is the RATIFIED bullet at the top of this block: **Reading A with sub-decision A-ii = re-type**, `status: open` (was `design_substrate_gated`), spec leg LANDED, impl leg owed. The clause below — "the SPEC half REMAINS, so this row stays `registered_finding`" — was true until the operator answered, and is kept as history.)* **IMPL HALF LANDED 2026-07-28; the SPEC half was then still open.** Classification is now **type-keyed**: `classify_memory_failure` first reads a `memory_failure_class` class attribute declared at the exception's own definition site (`MEMORY_FAILURE_CLASS_ATTRIBUTE`, `harness-is/src/harness_is/memory_observability.py`), and that declaration wins outright; only a real `MemoryTelemetryFailureClass` member is honoured, so a typo or a string cannot invent a class outside the closed vocabulary. `getattr` walks the MRO, so subclasses inherit for free. The message/type-name heuristics survive ONLY as the residual for types that declare nothing, and the `"io" in exc_name` clause is **deleted** — `isinstance(exc, OSError)` is the whole IO rule now (`IOError` is an alias of `OSError`, so the old tuple was redundant).
  - **Shape chosen — declaration-at-the-definition-site, not a name list in the classifier.** The alternative (a frozen exact-type-name set inside `harness-is`) would be a SECOND authority for a hierarchy `harness-is` cannot import (`harness-runtime` imports `harness-is`, never the reverse), i.e. exactly the mirror-copy drift class this workspace already treats as a defect in its own right (see B-82/B-84's own close-out reasoning). The declaration keeps ONE source of truth: the exception says what it is. In-repo precedent: `memory_tool_executor.py:170-172` already keys `memory.policy.decision` on `isinstance(exc, MemoryToolExecutionDeniedError)` — `failure_class` on the very next line was the only substring-keyed attribute on that span, and the two could disagree.
  - **Populations table (old → new).** Every change is deliberate:

    | population (raise sites) | before | after | why |
    |---|---|---|---|
    | `MemoryToolExecutionDeniedError`, all **14** raise sites *(15 at 2026-08-05 — one added by a later arc; filing §10)* | `policy_denial` OR `io_failure` by wording | **`policy_denial`** always | declared. FOUR sites actually changed, not the two this row named: "memory promotion review is forbidden", "memory ref X is superseded", "memory ref X is not retrievable", "memory tool scope_ref does not match context" (named by message, not by line offset, which this arc's own edits shift) |
    | `MemoryToolExecutionInputError`, all 10 executor sites *(18 repo-wide at 2026-08-05; filing §10)* | `io_failure` (and `policy_denial` for the `'policy_ref'`-named argument message) | **`provider_adapter_failure`** | LEAST-WRONG stopgap; see the choice rationale below. Also kills the `policy_ref` miss, where a malformed argument asserted a policy decision no resolver made |
    | `write_note` durable-write failure (capture `status=FAILED`) | `io_failure` | **`io_failure`** (via NEW `MemoryToolExecutionStoreError`) | **round 2, closing a regression round 1 introduced.** Round 1's unconditional base declaration reported a disk-full store write as `provider_adapter_failure`. Discriminated on the capture API's **closed two-value status enum** — never on `failure_reason` wording — because `FAILED` is produced at exactly ONE place, `EpisodicMemoryCapture._capture`'s `except` around `write_record` + `append_memory_operation` (`memory_capture.py:541-552`), which classifies its own span `io_failure`. The subtype ADOPTS that classification rather than inventing a third answer |
    | `MemoryToolExecutionError` base | `io_failure` | **`provider_adapter_failure`** | after round 2 the base's ONLY raise site is `_write_note`'s defensive `memory_id is None` branch, unreachable through the real capture API (a `CAPTURED` result always sets `memory_id`) — the type determines nothing, so it declares the residual. A future subclass that declares nothing inherits the residual, never a confidently wrong class |
    | undeclared harness types whose NAME contains "io" but whose failure is not IO — `MemoryOperationIdempotencyConflictError` / `…ProjectionMismatchError` / `…RedactionEventMismatchError` (reachable at the `memory_redaction.py:260` site), pydantic `ValidationError` ("validat·io·n") | `io_failure` | **`provider_adapter_failure`** | the collision class this row named; an idempotency conflict is not an IO failure |
    | `MemoryPathViolationError` | `path_violation` (every message contains "path") | `path_violation` (declared) | unchanged value, now by construction |
    | `MemoryCallbackIOError` non-policy sites (not-found / out-of-range / content_b64 decode) | `io_failure` (via the `"io"` substring) | `io_failure` (declared) | unchanged value. Without the declaration these EIGHT sites would have regressed to the residual once the substring rule died — the type *is* the C-RT-15 `RT-FAIL-MEMORY-CALLBACK-IO` storage-backend I/O failure |
    | native-adapter POLICY denials — `_require_native_access`, `_require_capture_allowed`, and both `_require_retrieval_allowed` branches (redaction-state + resolver DENY) | `policy_denial` by wording | `policy_denial` by construction | the C-MEM-15 callback surface offers exactly ONE failure carrier, so a denial must be raised as a `MemoryCallbackIOError`; a **module-private** `_NativeMemoryPolicyDeniedError(MemoryCallbackIOError)` now carries them *(6 raise sites at 2026-08-05, not the 4 listed — two added by later arcs, span-pinning unverified; filing §10)*. `isinstance`, every `except`, every `pytest.raises`, and the transient `RT-FAIL-MEMORY-CALLBACK-IO` mapping are all unchanged — it is a telemetry discriminator, NOT an addition to the §14.12.1 typed-exception surface |
    | everything else undeclared (`MemoryStoreRecordUnavailableError`, raw `OSError`, stdlib) | — | unchanged | residual rules preserved |

  - **The input-validation choice (`provider_adapter_failure`), stated for the spec half to overturn.** (i) The fault originates in a provider/model-emitted tool call arriving through the adapter, not in the memory substrate's own IO or record codecs. (ii) `retry_breaker_fallback._classify_provider_exception` already groups `MemoryToolExecutionInputError` with `LLMDispatchPayloadShapeError` — the provider-payload-shape family — so this reads the same failure the same way at both layers. (iii) It RESTORES the value this population emitted before B-84 typed `_allowed_kinds` (a bare `ValueError` fell to the same residual), so B-84's incidental `provider_adapter_failure` → `io_failure` shift is reverted rather than entrenched. (iv) `serialization_failure` is reserved for record-codec faults; using it here would poison that bucket for triage. The declaration is written explicitly on `MemoryToolExecutionInputError` (not inherited) precisely so it is the single flip site when the vocabulary gains an input class.
  - **Residual, stated honestly.** Undeclared types are still classified by message wording (`policy`/`denied`/`unavailable` → `policy_denial`; `path`/`traversal`/`prefix` → `path_violation`; `json`/`serial`/`decode` → `serialization_failure`). That is wording-luck for e.g. `MemoryStoreRecordUnavailableError`, and it is left alone: those types have no vocabulary-determined class, and inventing one for them is outside this row.
  - **Base-type raise-site sweep (round 2, mirroring the B-84 R1 sweep).** `raise MemoryToolExecutionError(` occurs at **exactly one** site repo-wide, so the round-1 mislabel class was confined to that site and is now closed. Store exceptions do NOT otherwise reach the base: `_read_record_by_ref` wraps them into `MemoryToolExecutionDeniedError`, and any un-wrapped store exception propagates raw through `execute`'s blanket `except` and is classified by the residual (`isinstance(exc, OSError)` → `io_failure`), which was already correct.
  - **Adjacent, untouched (recorded, not fixed).** `memory_capture.py:545` hardcodes `IO_FAILURE` for ANY exception from the `write_record` + `append_memory_operation` pair. Round 2 makes the executor **adopt** that classification instead of contradicting it, so the coarseness is now inherited by one more span: a non-IO exception escaping that pair is reported `io_failure` at both layers. That is the capture layer's pre-existing choice — changing it is a separate judgement about whether a capture-layer span may assert IO without evidence, and it cannot be made at the executor, which receives only a formatted reason STRING (`f"{type(exc).__name__}: {exc}"`) with no exception retained on `MemoryCaptureResult`.
  - **Merge-gate round 3 (test-witness lens BLOCK, closed).** The lens found the three *other* `_NativeMemoryPolicyDeniedError` raise sites unwitnessed: their covering tests asserted only `pytest.raises(MemoryCallbackIOError, ...)`, which the subtype satisfies identically, so reverting any one of them to the base type left the suite green while re-emitting `io_failure` on a genuine denial. All three are now span-pinned (`_require_capture_allowed`; `_require_retrieval_allowed`'s resolver-DENY branch; and its redaction-state branch, which previously had NO test at all), each probed individually: reverting each site fails exactly its own test with `assert 'io_failure' == 'policy_denial'`. The same round added the third consumer's failure-path pin (`memory_redaction`), wording-CONFLICTING rows so the base and path declarations are no longer deletable-green, and fixed two count claims (14 denial raise sites, not 12) plus an inaccurate test comment.
  - **Witnesses.** `harness-runtime/tests/test_memory_failure_classification.py` — THE population table, 37 parametrized rows *(39 at 2026-08-05; filing §10)* over real exception instances carrying their actual raise-site messages, plus a pin that `memory.policy.decision` and `failure_class` agree on the four previously-disagreeing denials, plus a pin that the private discriminator is still a `MemoryCallbackIOError`. `harness-is/tests/test_memory_observability.py` — the declaration mechanism (wins over conflicting wording; inherited; overridable; a non-member declaration is IGNORED) and the residual rules. `harness-runtime/tests/test_memory_tool_executor.py::test_write_note_store_failure_emits_io_failure_class` — round 2 END-TO-END through the real executor with `store.write_record` raising `OSError("disk full")`, asserting the emitted `memory.failure_class == "io_failure"` on the outer `memory.tool_call` span (grounded there: `_write_note` does NOT thread the executor's tracer into `EpisodicMemoryCapture`, so on this path the outer span is the ONLY telemetry record of the failure). `harness-runtime/tests/test_lifecycle_retry_breaker_fallback.py::test_memory_tool_input_error_fail_fast_trip_carries_no_breaker_cause` — the lens-3 half below. `harness-runtime/tests/test_native_memory_adapter.py` — all four native-adapter denial sites span-pinned (the fourth, `_require_native_access`, was already pinned). `harness-is/tests/test_memory_redaction.py::test_redaction_write_failure_emits_classified_failure_class` — the THIRD consumer, two rows with DIFFERENT expected classes (`OSError` → `io_failure`; `MemoryOperationIdempotencyConflictError` → `provider_adapter_failure`) so the assertion pins the classification rather than a constant.
  - **Mutation probes ((a)–(g), seven as enumerated — the original "all five" undercounted; filing §10; all fail post-fix, restored after).** (a) Restoring `or "io" in exc_name`: 2 rows fail (`MemoryOperationIdempotencyConflictError`, and the test's own `_BogusDeclarationError` — "declarat·io·n"). (b) Removing `MemoryToolExecutionDeniedError`'s declaration: 11 failures, every denial row plus the agreement pin. (c) Hardcoding `BreakerCause.AUTH_FAILURE` at the fail-fast `record_failure`: 1 failure — the NEW test only, confirming this row's claim that the B-84 witnesses survive it. (d) **Round 2 — restoring the regression** (store failure raises the base again): the end-to-end witness fails. (e) **Round 2 — removing `MemoryToolExecutionStoreError`'s `IO_FAILURE` declaration** (so it inherits the base residual): 4 failures — the end-to-end span assertion (`assert 'provider_adapter_failure' == 'io_failure'`) plus the 3 store-subtype table rows. (f) **Round 3 — reverting each of the three newly-pinned native-adapter denial sites to `MemoryCallbackIOError`, one at a time**: each run fails exactly ONE test, its own, with `AssertionError: assert 'io_failure' == 'policy_denial'`. (g) **Round 3 — hardcoding `failure_class="io_failure"` at `memory_redaction`'s emission site**: fails the non-IO row only (`assert 'io_failure' == 'provider_adapter_failure'`), which is the correct discriminating shape — the IO row still passes, proving the failure is about classification and not about the site being exercised.
- **Adjacent lens-3 half — CLOSED at the same arc.** The fail-fast breaker-cause emission is now witnessed end-to-end: `fail_threshold=1` so the single fail-fast failure trips a real breaker, a spying registry captures the emitted `BreakerTransition`, and the test asserts `transition.cause is None` after asserting the transition exists (positive control, so the assertion is never vacuous).
- **Close-out steps.** (1) **Type-keyed classification — LANDED** (2026-07-28) as described above. (2) **Decide the input-validation class — DECIDED 2026-08-05 as Reading A** (a seventh `C-MEM-19` member `input_validation_failure` + the boundary invariant, with A-ii = re-type); the *"REMAINS OPEN, spec-side"* framing below is the pre-ratification record, and what remains is the spec leg that writes the decision, not the decision. The impl half took the least-wrong existing value with the rationale recorded above; adding a member to the closed C-MEM-19 vocabulary is a **Class 1 back-flow to `Spec_Memory_Substrate_v1.md` C-MEM-19, not absorbable at Phase 7 per X-AL-3** *(the 2026-08-05 filing measured the contract text itself: the vocabulary is ONE prose must-distinguish sentence with NO closure statement — "closed" is this register's word, not the contract's; the back-flow routing stands either way)*. When it lands, the single flip site is `MemoryToolExecutionInputError.memory_failure_class`. (3) **Witnesses + mutation probes — LANDED**, including the breaker-cause pin.
- **Council: PROBE-RESOLVED at the 2026-08-05 filing — no convening owed unless Reading B is selected.** The original disposition here ("likely, for step (2) only — an observability-vocabulary blast radius, every consumer switching on the value") was a claim; the filing measured it at HEAD and FALSIFIED the premise: zero consumers switch exhaustively (no `match`, no enum iteration, no `__members__`; the only site enumerating the vocabulary is the enum definition itself), so a seventh member cannot break any call site. A dyadic C7/C8 ⊥ memory-contract-owner convening is owed ONLY if the operator selects Reading B (blessing the stopgap is a real observability-vs-contract tension). Steps (1)/(3) unchanged: mechanical once (2) is settled.
- **SPEC-HALF FORK FILED 2026-08-05** at `.harness/class_2_fork_b88_memory_failure_input_validation_class.md` (Class 2, doc-only; grounding HEAD `4f914159`). Three readings: **A — add a SEVENTH C-MEM-19 member `input_validation_failure`** plus a boundary invariant separating it from provider-adapter and serialization failure (**RECOMMENDED**); **B — bless the stopgap**, recording `provider_adapter_failure` as the deliberate home in contract text, vocabulary stays six; **C — defer** under a falsifiable `D-0..D-3` demand test (D-0, a real operator-facing consumer of `memory.failure_class`, dominant). **Recommendation A, runner-up C** (not B: B is the only option whose cost is a later *retraction*), with the discriminator stated — **does `memory.failure_class` name the fault's KIND or its LOCUS?** KIND → A; LOCUS → B; "no consumer needs it yet" → C. The filing's decisive ground is that `provider_adapter_failure` is simultaneously the classifier's **residual** (`memory_observability.py:200`), so a deliberate class and a failure-to-classify currently share one value. **The council field below is PROBE-RESOLVED and superseded:** its premise — *"every consumer switching on the value"* — was measured at HEAD and **falsified**. Zero consumers switch exhaustively (no `match`, no enum iteration, no `__members__`; the only site in the tree enumerating the vocabulary is the enum definition itself); the 20 non-test member references are 9 `ClassVar` declarations + 5 hardcoded emissions + 6 inside the classifier. Zero hash participation, zero CXA rows, zero cross-artifact restatements beyond `Spec_Memory_Substrate_v1.md:870` and `Implementation_Plan_Memory_Substrate_v1.md:829`. **No convening is owed unless the operator selects B.** Reading A's measured footprint: 1 enum member + the single declaration flip + 8 of 39 witness-table rows + 1 fixture re-key. Ratification joined the **2026-08-05** operator batch alongside `B-96` / `B-98` / `B-104` / `B-93`+`B-45` / `B-84` / `B-95`, and its outcome is the RATIFIED bullet at the top of this block. **Five count claims and one file:line cite in the bullets above had drifted since the impl leg** — recorded at the filing's §10 and **REPAIRED IN PLACE 2026-08-05 at the ratification leg** (none changes any disposition): the `memory_capture.py:215`/`:236` declaration cite is stale (actual `:286`–`:288` / `:307`–`:309`); `MemoryToolExecutionInputError` is 10 sites *in the executor* but 18 repo-wide; denial raise sites are 15, not 14; the classification table is 39 rows, not 37; the mutation-probe bullet says "all five" while enumerating **(a)–(g), seven**; and `_NativeMemoryPolicyDeniedError` now has 6 raise sites, not 4 (`_require_scope_family_in_domain:305` plus a third `_require_retrieval_allowed` branch at `:334`, both added by later arcs, whose span-pinning the filing did **not** verify and which is therefore recorded as UNVERIFIED — no later arc may inherit *"all four are pinned"* as covering six sites).
- **Cross-ref.** Surfaced by the merge-gate lens-2 sweep on the **B-84** arc; the `provider_adapter_failure` → `io_failure` shift is a direct consequence of B-84 typing `_allowed_kinds`. The bullet above folds in the lens-3 breaker-cause observation.


### B-89 · Memory-scope producer asymmetry: capture stamps raw provider KEYS while retrieval requests carry family VALUES *(surfaced by the B-86 grounding pass, 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*

- **What it is.** The two producers of a `MemoryScope` disagree about the *vocabulary* of `provider_family`. The capture path constructs its **own** scope with `provider_family=provider` — the raw per-turn dispatched provider **KEY** (`harness-runtime/src/harness_runtime/memory_capture.py:585-591`; key vocabulary `claude_code` / `codex` / `gemini` / `antigravity` / `ollama` / `anthropic` / `openai` / `google` at `harness-runtime/src/harness_runtime/lifecycle/cross_family_cost_tag.py:60-69`) — while retrieval requests carry the composed `record_scope`'s `ProviderFamily` **VALUE** (`harness-runtime/src/harness_runtime/automatic_memory.py:197`). All three enforcement sites compare raw strings with **no normalization**: `MemoryRetriever._scope_mismatch` (`harness-is/src/harness_is/memory_retrieval.py:371-384`), the index `_scope_matches` (`memory_retrieval_index.py:379-396`), and `memory_policy._scope_not_broader` (`memory_policy.py:336-359`).
- **Empirically confirmed at registration, not inferred.** An `ollama`-written note is invisible even to a `local_open_weight`-scoped request **of its own family**. Tool-written notes are therefore **write-only for 5 of the 8 registered provider keys**; only `anthropic` / `openai` / `google` coincide with a family value, and they do so by accident. The practical consequence: "memory continuity" under primary-derived keying is largely **notional** today — the continuity side of B-86's Q1 tension only becomes real once this row lands.
- **Existence is independent of B-86's Q1; direction is not.** Both Q1 readings leave the `ollama` note invisible — Q1 asks *primary-or-candidate*, this row asks *key-or-value*; orthogonal axes. But the **direction** is determined by the B-86 council record (fork doc §5-Q1): the **writer adopts the reader's key** — the capture path stops constructing an independent `MemoryScope` and consumes the run's composed `record_scope`. That is the larger of the two candidate repairs, correct on one-source-of-truth grounds, and it will incidentally close **B-90** when it lands.
- **Intrinsic evidence for the family reading.** The field is literally named `provider_family` (`harness-is/src/harness_is/memory_record_envelope.py:120`) while C-MEM-03 declares **no value domain** at all. That is evidence for the fork to weigh — **not** an impl licence to normalize ahead of the contract text.
- **Hash-inert; blast radius low.** `MemoryScope` is not folded into `memory_id` (content-hash + run_id only, `memory_capture.py:574-575`), so a writer-side normalization moves **no identities**.
- **Migration residual — forward-only and permanent.** Records already written with raw provider keys stay invisible under family-value requests. That is **not a regression** (they are invisible today), but the spec-leg must **state** it rather than leave it implied, alongside the C-MEM-03 value domain it belongs to.
- **Current state.** `closed` 2026-07-28 at the U-MEM-26 impl leg (`design_substrate_gated` → `open` at the B-86 spec leg → `closed` here). `EpisodicMemoryCapture` gained a **`record_scope` parameter** and writes records under the run's composed scope verbatim; **both construction sites pass it**. The `provider` argument is unchanged and still feeds the C-MEM-08 payload and the C-MEM-19 span **as the raw key it is** — only the *record* scope stops deriving from it; the residual self-construction remains for callers with no composed scope and is bound by the C-MEM-03 value domain instead. The **ollama divergence is the discriminating witness** throughout (its provider *key* is not value-equal to its `ProviderFamily`, so a test that passes under `anthropic` or `openai` proves nothing about canonicalization), and **round-trip retrievability is witnessed directly** — `test_captured_scope_round_trips_where_the_pre_repair_scope_does_not` in `harness-runtime/tests/test_u_mem_26_write_boundary.py`, carrying an explicit precondition anchor because *both sides of its comparison are derived from the writer* and it would otherwise stay green against a writer that repaired nothing (caught by the mutation probe, not by review). Mutation-probed: ignoring the composed `record_scope` reddens all four capture witnesses. The forward-only residual stands as **stated** at C-MEM-03 — pre-repair raw-key records are not rewritten and stay unreachable under family-scoped requests. Close-out steps below are history (pre-ratification).
- **Close-out steps.** **(1)** Do **not** land ahead of the B-86 spec-leg; the direction is the B-86 council record's, not this row's. **(2)** Once ratified, the repair is mechanical at `memory_capture.py:585-591` and its callers, witnessed by asserting that an `ollama`-written note **is** retrievable under a `local_open_weight`-scoped request of its own family (the currently-failing case), and mutation-probed by restoring the raw-key construction. **(3)** State the forward-only migration residual in the spec-leg.
- **Council: no.** The keying decision is the B-86 council record's; once the spec-leg ratifies, the repair is mechanical (the writer consumes the composed `record_scope`).
- **Cross-ref.** Surfaced by the B-86 grounding pass; direction fixed by the B-86 council record; **did** incidentally close **B-90** when the repair landed (both rows closed 2026-07-28). Arc record at `.harness/u-mem-26-impl-leg-close-2026-07-28.md`.


### B-90 · Capture-path `MemoryScope` omits `tenant` and `workload_class` — tool-captured records are tenant-unpartitioned *(surfaced at the B-86 council convening, 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*

- **What it is.** `memory_capture.py:585-591` constructs its `MemoryScope` with only `project` / `workflow` / `provider_family` / `cli_profile` / `visibility`. `tenant` and `workload_class` are **omitted entirely**, while the composed **request** scope populates them (`harness-runtime/src/harness_runtime/automatic_memory.py:194-199`).
- **Why the omission is not benign — wildcard-on-`None`.** A `None` **record** value passes all three enforcement sites: `MemoryRetriever._scope_mismatch` skips a `None` on either side (`harness-is/src/harness_is/memory_retrieval.py:377-383`); the index `_scope_matches` carries the same field list (`memory_retrieval_index.py:379-396`); and `memory_policy._scope_not_broader` guards only **non-`None`** record values (`if record_value is not None and requested_value != record_value`, `memory_policy.py:336-359`), so a `None` record tenant passes there too. **Net:** every tool-captured / episodic record is **tenant-unpartitioned** and reachable by **any** tenant's family-matching request — against the cleared threat-model mandate at `design-substrate/Spec_Memory_Substrate_v1.md:481`: *"Retrieval and injection enforce project, workflow, tenant, provider-family, CLI-profile, and visibility scope before ranking."*
- **Honest caveats.** Single-tenant local deployment is the current default posture, so **live exposure is bounded today**. And the wildcard-on-`None` semantics were, **at registration time**, load-bearing but undocumented — they are **documented as of spec v1.1** (C-MEM-03) — the B-86 spec-leg's C-MEM-03 null-semantics sentence is what documents them.
- **Current state.** `closed` 2026-07-28 at the U-MEM-26 impl leg (`registered_finding` → `open` at the B-86 spec leg → `closed` here), **incidentally by the B-89 writer repair exactly as route (1) predicted**: the run's composed `record_scope` carries `tenant` and `workload_class`, so tool-captured records are no longer tenant-unpartitioned. Witnessed per step (3) below by **three separate per-predicate assertions** in `harness-is/tests/test_memory_scope_value_domain.py` — `test_b90_tenant_denial_at_the_retriever_predicate` (`_scope_mismatch`), `test_b90_tenant_denial_at_the_index_predicate` (`_scope_matches`), and `test_b90_tenant_denial_at_the_policy_predicate` (`_scope_not_broader`) — **never one composite retrieval test**, which is precisely how the omission survived; a fourth witness, `test_b90_null_tenant_request_is_denied_at_every_predicate`, pins the wildcard-on-`None` arm. Each is mutation-probed. The cleared threat-model mandate at `design-substrate/Spec_Memory_Substrate_v1.md:481` is now conformant for the capture path. Close-out steps below are history (pre-ratification).
- **Close-out steps.** **(1) Preferred — fold into the B-89 writer-side repair:** consuming the run's composed `record_scope` brings `tenant` + `workload_class` along, so the B-89 repair will incidentally close this row when it lands. **(2)** An **independent narrow fix** (stamp the two fields into the constructed scope at `memory_capture.py:585-591`) is landable **without** the spec-leg, since it needs no contract decision — but it would be superseded by (1). Prefer (1) unless a multi-tenant deployment lands first, in which case take (2) immediately and let B-89 subsume it. **(3)** Witness either shape by asserting a tenant-A request does **not** retrieve a tenant-B tool-captured record at **all three** enforcement sites; a single-site witness passes over the other two, which is how this survived.
- **Council: no.** Conformance repair to a cleared threat-model invariant (`:481`); no cross-domain tension. Fold into the B-89 writer repair at the B-86 spec-leg's impl leg.
- **Cross-ref.** Surfaced at the B-86 council convening (Probe C's adjacent half); **incidentally closed by B-89's repair**, which landed at the U-MEM-26 impl leg 2026-07-28. Arc record at `.harness/u-mem-26-impl-leg-close-2026-07-28.md`.


### B-91 · Report-only degraded-serve `failure_class` / `policy.decision` semantics for the scope-mismatch and `not_derived` reason families *(surfaced by the merge-gate spec-conformance lens on the C-MEM-19 emitter-attrs arc, 2026-07-29; CLOSED 2026-07-29 — Q1 policy_denial applied (R2-converged: class follows the family relation, not the conjunct), Q2 keep-as-is)*

- **What it is.** The B-83 degraded-serve emitter's **report-only** branch (`degraded.rendered is None`, `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) fires on **three** reason families, not one, and they do not share a telemetry class: **(1)** the scope-mismatch conjuncts (`provider_scope_mismatch` / `provider_family_scope_mismatch`); **(2)** genuine `MemoryAccessModeDenialReason` values carried verbatim from `prompt_packet_fallback_denial` — `no_supported_mode` / `token_budget_empty` (`harness-runtime/src/harness_runtime/memory_context.py:134-138`); **(3)** the `not_derived` / `prompt_packet_denial_undetermined` sentinels (`memory_context.py:48-52`). A single rationale covering all three was written into the emitter docstring at PR #1151 and was **false for family (2)**, which is what the merge-gate spec-conformance lens caught.
- **Family (2) is CLOSED at PR #1151.** It now carries `failure_class=POLICY_DENIAL`, mirroring the **primary** DENIAL span, which sets exactly that class for exactly those values (`memory_context.py:210-216`) off a real `select_memory_access_mode` run (`memory_context.py:545-549`). One decision reported by two emitters must not carry two classes. The discrimination is carried as **data** on the `_DegradedMemoryServe` carrier (`policy_denial`, set from membership in the closed `MemoryAccessModeDenialReason` value domain at the site that knows the reason's provenance), never re-derived by string-matching in the emitter.
- **Q1 — should family (1) carry `POLICY_DENIAL` too? DECIDED 2026-07-29: YES.** The deciding text is the C-MEM-13 **recording-surface** paragraph at `design-substrate/Spec_Memory_Substrate_v1.md:511` — the withholding is recorded on the C-MEM-19 surface *"whose declared coverage already includes standard memory tool calls and policy denial"*. That sentence earns its "no new coverage item needed" by pointing at the **existing** list (`:674-683`), and of its two candidate items a withheld exposure is definitionally **not a tool CALL** — the schemas never reach the model — so *"Policy denial"* (`:683`) is the item being invoked. `memory.failure_class` is the only required attribute (`:687-698`) whose value domain carries that classification, and the failure-class invariant at `:703` uses the **identical phrase**; reading the coverage item and the vocabulary as unrelated would leave the coverage item with no carrier at all. That vocabulary is scoped by **outcome kind** (policy denial / path violation / IO failure / serialization failure / provider adapter failure / retrieval empty-result), never by *which component decided* — which settles the genuinely-open part: a **contract-mandated** withhold and a **resolver's** denial are one telemetry class. Corroborating: the withhold is a `MUST NOT` at `:509`, an invariant at `:523`, a C-MEM-14 exposure qualification at `:559`, and `no_memory_access` — what both branches report — is itself *"a valid policy outcome"* at `:522`.
- **Q1 SCOPE REFINED 2026-07-29 by out-of-family Codex R1 (two [P2]s, both upheld).** The first application set the class on **both** scope conjuncts unconditionally, which over-claimed on two counts — and the arc's own truthfulness principle (never report a determination that was not made) is what condemns it. **[P2-1] the IDENTITY conjunct carries no class at all *(SUPERSEDED by the R2 convergence below — at HEAD the relation IS resolved before the identity return and a confirmed cross-family rebind DOES carry the class)*.** It fires whenever the provider **key** moved, including an `openai`-composed context rebound to `codex` — one family, two keys — where C-MEM-13, which withholds only across **families**, mandates nothing whatsoever; the branch never resolves a family, so it cannot have confirmed the condition the `POLICY_DENIAL` claim rests on. It is a **stale-context safety guard**, and `memory.policy.decision` still names it verbatim. **[P2-2] the FAMILY conjunct carries the class only on a CONFIRMED mismatch.** `_packet_scope_matches_dispatch_family` returned `False` for an absent `record_scope`, an out-of-domain scope family, or an unregistered provider key — states where the relationship is **UNKNOWN**, not known-unequal — so the unconditional flag reported **fail-closed validation** as an operator policy decision. The Q1 reading above is unchanged and still governs the case it actually addressed: a **confirmed** cross-family withhold, both families resolved and unequal. **Discrimination mechanism:** `_packet_scope_family_relation` (new) returns a tri-state `_ScopeFamilyRelation` — `MATCH` / `CONFIRMED_MISMATCH` / `UNVERIFIABLE` — over the **same** existing `provider_family_for_scope_check` + `canonical_scope_family` authorities, no second policy path; `_packet_scope_matches_dispatch_family` is now a pure projection of it (`is MATCH`), so both withholding callers behave **identically** to before and only the telemetry class changed.
- **Q2 — does `not_derived` belong on `memory.policy.decision` at all? DECIDED 2026-07-29: KEEP AS IS** (verbatim; **no code change**). `memory.policy.decision` is a **required** attribute on every memory span (`:694`), so *omitting* it on the one branch where no gate closed would drop a required attribute exactly where a reader most needs to know the decision axis was empty. Mapping to the primary DENIAL span's generic `denied` fallback is the worse option: it asserts a denial **nobody issued** — the same false accusation the per-reason [P2-e] split exists to prevent. Verbatim is truthful on **both** halves of the B-87/B-91 principle: the sentinel names **no gate** (defined distinct from every `MemoryAccessModeDenialReason`, `memory_context.py:48-52`, so it can never be misread as one), while the span still reports `denial` / `no_memory_access` / `record_count=0`, so the degradation is never hidden.
- **No contract defect either way.** C-MEM-19 requires `memory.policy.decision` on every memory span and does **not** enumerate which spans owe a `failure_class`; every answer on the table was conformant, before and after the flip. This row was about telemetry **truthfulness**, not conformance — which is why it registered rather than halted, and why closing it needed a spec **reading** rather than a spec **amendment**.
- **Applied 2026-07-29 (close), as refined by Codex R1 and converged by Codex R2 the same day.** `_degraded_serve_disposition` sets `_DegradedMemoryServe.policy_denial` on exactly the report-only outcomes where a policy determination was **actually reached**: a selector-returned `MemoryAccessModeDenialReason`, and a **confirmed** cross-family withhold (`_ScopeFamilyRelation.CONFIRMED_MISMATCH`) — under **either** scope conjunct. R1 had corrected the first application's unconditional flag to an unconditional `False` on the **identity** branch; R2 found that under-reports by the mirror amount, since an `anthropic`-composed context rebound to `openai` resolves both families, finds them **unequal**, and is a confirmed C-MEM-13 withhold that the identity return exited before ever evaluating — unclassified purely by **test order**. **Converged rule:** `policy_denial` ⟺ `CONFIRMED_MISMATCH`, a function of the family **relation** alone, never of which conjunct fired; the relation is now resolved **once, before** the identity test (it reads only `record_scope` + `provider_name`, neither touched by that test, so no ordering hazard — two dict lookups on an already-degraded path). It stays **unset** on a **same-family** identity rebind, on every `UNVERIFIABLE` family state under either conjunct, and on the sentinels — three shapes of *nobody determined this*. The emitter itself is unchanged, since it already reads the carrier flag. Q2 changed no code. The `#1151`-era emitter docstring, which held the question open, now cites the decisions and the R1 refinement. **Witnesses stay discriminating** per close-out step (3), all four cells pinned: `test_u_mem_26_cross_family_ollama_dispatch_withholds_tools_and_packet` (the confirmed case — scope family `openai`, dispatch `ollama` → `local_open_weight`, **both resolve** and are unequal) asserts `failure_class` **present**; `test_b83_provider_rebound_context_is_reported_but_never_served` (`anthropic`-composed → `openai` — **cross**-family) asserts it **present** on the **identity** guard, while `test_b91_same_family_provider_rebind_is_not_a_policy_denial` (`codex`-composed → `openai`, **one** family) asserts it **absent** on that same guard — the R2 pair pinning the class to the relation rather than to the branch; `test_b83_absent_record_scope_fails_closed_to_report_only` and `test_b86_unregistered_provider_key_fails_closed_against_local_scope` assert it **absent** on the two `UNVERIFIABLE` shapes; `test_b83_undervied_eligibility_reports_not_derived_not_a_gate_denial` asserts it **absent** on the sentinel and additionally pins the Q2 verbatim value. **Mutation probes, all killing** (two re-run at the R2 convergence, on top of three at the R1 refinement and four at the first application): reverting the family construction to an unconditional `True` reddens **both** `UNVERIFIABLE` witnesses; dropping the `CONFIRMED_MISMATCH` `True` reddens the U-MEM-26 cross-family witness; R2 (a) forcing the identity construction back to a relation-blind `False` reddens the **cross-family** rebound witness (`KeyError: 'memory.failure_class'`); R2 (b) forcing it unconditionally `True` reddens the **same-family** rebind witness. **No spec amendment**: a telemetry-truthfulness reading of already-cleared text, decided inside the C-MEM-19 closed enum (attribute **values** only, no new members).
- **Council: no.** A bounded telemetry-semantics decision inside an already-cleared contract, with no cross-domain tension to surface. It does require a careful spec **reading** (C-MEM-13 `:511` / `:523` + C-MEM-14 `:559` against C-MEM-19), so it is not mechanical — but one grounded pass settles both questions.
- **Cross-ref.** Surfaced by the merge-gate spec-conformance lens on PR #1151 (the C-MEM-19 `cli_profile` + `policy.decision` emitter-attrs arc); family (2) closed in that same PR. Adjacent registered telemetry-vocabulary row: **B-88** (the closed C-MEM-19 `MemoryTelemetryFailureClass` enum has no input-validation member).
### B-92 · C-MEM-10 promotion eligibility of records captured during a cross-family fallback leg *(named open question carried forward at Spec_Memory_Substrate_v1.md v1.1 change-note "Named open question carried forward, not discharged." and U-MEM-26's "Out of scope for this unit:" list; Class 1 fork FILED 2026-07-29; CLOSED 2026-07-29 — U-MEM-27 impl leg)*

- **What it is.** The memory spec's **own** named-but-undischarged open question, now filed as a Class 1 fork at `.harness/class_1_fork_c_mem_10_cross_family_promotion_eligibility.md`. Since U-MEM-26 (PR #1148) capture writes under the run's **composed** scope, so a cross-family fallback leg lands an `anthropic`-family record while the dispatch that produced its content ran on `openai`. Whether such a record may be **promoted** — and thereafter injected back into future runs under the primary's family — is the C-MEM-10 question the change-note paragraph **"Named open question carried forward, not discharged."** of `design-substrate/Spec_Memory_Substrate_v1.md` explicitly carries forward: *"whether records captured during a cross-family fallback leg are promotion-eligible under C-MEM-10 … v1.1 neither resolves nor forecloses it"*. `design-substrate/Implementation_Plan_Memory_Substrate_v1.md` lists it out-of-scope for U-MEM-26 with the same carry, at that unit's **"Out of scope for this unit:"** subsection. *(Cite convention, Codex R3 [P2-2]: this row cites the memory spec by contract ID + section name + byte-verified quoted fragment, never by line, per that spec's own v1.1 instruction — "Cite this spec by contract ID and section name, not by line, in any text authored after v1.1." Code cites keep `file:line` and were verified at HEAD `6ab41d7f`, against which no code file differs from the `dd2a8c1a` grounding HEAD.)*
- **Witnessed, not inferred.** A shipped test asserts the behaviour: `test_u_mem_26_capture_is_unaffected_by_the_cross_family_withholding` (`harness-runtime/tests/test_automatic_memory_runtime.py:515`) drives an `anthropic`-primary / `openai`-candidate dispatch and asserts at `:579` that `captured.envelope.scope.provider_family == ProviderFamily.ANTHROPIC.value`.
- **Current behaviour: silent at both entry points — but ZERO live auto-promotion exposure at HEAD.** *(The first draft's exposure claims were corrected by an out-of-family Codex R1 pass, 2026-07-29 — one [P1] and one [P2], both upheld on direct re-read.)* **(1) The hint path — the candidate-side family is producer-defined, hence undefined today.** `_candidate_from_hint` (`harness-runtime/src/harness_runtime/memory_promotion.py:296`) builds risk flags from the *hint* with `source_scope=record.envelope.scope` (`:626-629`), but the **candidate** side that `_family_escapes_source` (`:715-743`) compares is `hint.suggested_scope` (`:682`) — a **required, caller-supplied** field (`:191`) with no default and no derivation from the record. A producer mirroring the composed run scope supplies `anthropic` (no flag); a producer supplying the **dispatched** family already trips `CROSS_SCOPE` (`:683`). *Which* convention a producer must supply is itself part of what C-MEM-10 owes, and cannot be read off code that has no producer. `_auto_promote_allowed` (`:776-790`) carries no family term and no risk-flag term. **(2) The model-facing tool path — not a live auto-promotion exposure, for two independent reasons.** `MemoryToolExecutor._propose_promotion` (`memory_tool_executor.py:399-435`) serves the C-MEM-14 `memory.propose_promotion` tool (C-MEM-14 Contract, tool table), hardcodes `confidence=HIGH` (`:423`), and computes `risk_flags=_promotion_risk_flags(source)` (`:425`), which inspects **only** `redaction_state` (`:882-885`) — that no-scope-comparison point stands. What does **not** stand is reachability for a *captured* record. **(a)** The record kind is denied before a candidate is constructed: `_read_retrievable_record_by_ref` (`:554-567`) routes the source through `resolve_retrieval`, and `_record_kind_allowed` (`harness-is/src/harness_is/memory_policy.py:327-333`) denies any kind outside `eligible_record_kinds`; the **sole** production policy `_policy_from_config` (`automatic_memory.py:532-557`) — the only *enabled* `MemoryPolicyDocument` built anywhere under `src/` — pins that set to `_retrievable_kinds()` (`:560-569`), the seven promoted/derived kinds, **excluding every kind capture writes** (`EPISODIC_RUN`/`EPISODIC_TURN`/`TOOL_EVENT`/`COMPACTION_EVENT`, `memory_tool_executor.py:769-775`) — covering both the witness's `EPISODIC_TURN` and `memory.write_note`'s own `TOOL_EVENT` output (`memory_capture.py:471-473`) — so the call raises `MemoryToolExecutionDeniedError` (`:565-566`) *before* candidate construction at `:417`. **(b)** An eligible record could not auto-promote either: `_policy_from_config:542` pins `promotion_decision=PROPOSE_SEMANTIC` while `_promotion_auto_allowed` (`:897-903`) returns True only for `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL`, so every candidate on this path takes the `propose_for_review` branch (`:456`) with `review_required=True` — **a branch that is durable but inactive, not ephemeral** *(the R2 correction of the first draft's "review, not durability")*: `propose_for_review` (`memory_promotion.py:328-352`) calls `_persist_decision` with `status=PROPOSED`, and `_persist_decision` (`:451-506`) writes **both** the semantic/procedural record (`write_record`, `:498`) **and** a durable C-MEM-08 operation entry (`append_memory_operation`, `:499-506`). What review withholds is **activation**, not persistence — a `PROPOSED` record is excluded from retrieval by construction (`_INACTIVE_STATUS_REASONS["proposed"]` → `RetrievalExclusionReason.PROPOSED`, `harness-is/src/harness_is/memory_retrieval.py:65` / `:71-77`) — and only `approve` / `edit_and_approve` (`:354-390` / `:416-449`) flip it to `ACTIVE`, neither consulting any family term.
- **What the question actually is, stated exactly.** Not "a live tool auto-promotes cross-family content today", but: **(i)** *future retrievable-kind records* — `eligible_record_kinds` is a C-MEM-09 **policy** field, and a **second-order** case needs no policy change at all, since a semantic/procedural record already promoted out of a cross-family leg is an eligible kind by construction and carries the same unrecorded provenance forward; **(ii)** *future policy configs* setting `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL` with `ReviewMode.AUTOMATIC`, which C-MEM-09 explicitly permits; **(iii)** the *first caller* to populate `promotion_candidates`. **None of the three is individually sufficient, and the row states the combinations rather than letting "a single policy field" imply otherwise** *(Codex R6 [P2-2], upheld by direct read at `1c99e208`; the earlier framing was true of candidate-**reachability**, not of active **promotion**)*: the gate functions are conjunctions and one conjunct is **already satisfied in production**. **(a)** admitting a capture-written kind into `eligible_record_kinds` alone buys **candidate-reachability only** — `_policy_from_config` still pins `PROPOSE_SEMANTIC` (`automatic_memory.py:542`), so `_promotion_auto_allowed` (`memory_tool_executor.py:897-903`) is False and the record lands as a durable, retrieval-excluded `PROPOSED`. **(b)** switching the decision to `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL` alone buys **active auto-promotion of already-eligible kinds only** — exactly the second-order case — since capture-written kinds are still denied before a candidate exists. **(c)** `ReviewMode.AUTOMATIC` buys **nothing: it is already set** (`automatic_memory.py:555`), so only the decision conjunct of `_promotion_auto_allowed` is unmet at HEAD (and the other two modes are live foreclosures a deployment could re-assert — `OPERATOR_REQUIRED` forces review at `:889-890`, `FORBIDDEN` denies the tool at `:406-407`). So on the **tool path** live auto-promotion of a *captured* record requires **(a) ∧ (b)**, both hard-coded side by side at `_policy_from_config` (`:542` and `:556`) and drawn from no `config.memory` field — today a code change, though C-MEM-09 **policy** fields by contract, which is why the gap is real regardless. On the **hint path** it requires **(iii) ∧ (b) ∧ non-`LOW` confidence** (`memory_promotion.py:786-787`; `PROMOTE_PROCEDURAL` specifically for a `PROCEDURAL_UPDATE` kind, `:788-790`) **∧ an actuator that does not exist**: the only `PromotionDecisionService.approve` call site anywhere under `src/` is the tool path's (`memory_tool_executor.py:449`), `extract_from_records` is reached solely from `CompactionSafetyHook.extract_candidates` (`memory_compaction_safety.py:153-159`) whose sibling `complete_compaction` (`:161-209`) persists **dispositions** and never promotes, and `CompactionSafetyHook` itself has no production caller at all (only its definition and its `harness_runtime/__init__.py` export). On that path `auto_promote_allowed=True` is computed, persisted, and **actuated by nothing**. The **contract** gap is unconditioned by all of it — C-MEM-10 has no provenance term at any policy setting. The §1 witness stands regardless: it pins the **capture-side** fact, which is what makes the question well-posed at all.
- **Prerequisite finding — on the composed-scope path (the U-MEM-26 production path), the pipeline cannot discriminate even if policy said to.** *(Scoped by Codex R2; the first draft asserted an exhaustive `provider` data-flow that holds only on that path.)* `EpisodicMemoryCapture` takes `record_scope: MemoryScope | None = None` (`memory_capture.py:253`) and `_scope_for_record` (`:1048-1086`) branches on it. **Composed path (scope supplied):** the envelope is the run's composed scope (`:1074`) through canonicalize-or-deny (`:1083-1086`), and the per-dispatch `provider` never reaches the envelope — this is **production**, since the automatic-memory runtime always supplies a composed scope keyed to `fallback_chain.primary.family.value` (`automatic_memory.py:211-221`, family at `:217`, threaded at `:504-512` / `:511`), and it is the path the witness exercises. **Residual path (no scope):** `_scope_for_record` builds the scope itself with `provider_family=provider` (`:1075-1082`, field at `:1079`), so such a record **does** retain its producing family and the exhaustive claim does not hold for it — but it retains that family by **partitioning the record under it**, the very per-turn re-derivation B-89 named as a defect and B-90 / U-MEM-26 repaired, so the cross-family question **does not arise there at all** (a record written under the producing family is not a foreign-family record inside a primary's partition) and there is nothing for C-MEM-10 to discriminate. **Force undiminished:** the path where the question is well-posed is exactly the path that cannot answer it, and it is the production path. On that path, `capture_turn_completion` writes `content` with **no** provider and **no** dispatched-model field (`:407-422`); `provider` / `model` reach only the **C-MEM-08 ledger row** (`:694-707`; `MemoryOperationPayload.provider` at `:1006`, `memory_refs=(memory_id,)` at `:1010`) and the **C-MEM-19 span** attributes (`:686-688`, `:714-716`) — and promotion reads neither (`_hints_from_record` reads `record.content` alone, `:545-560`). So readings B and C below are not merely unbuilt for composed-scope records (every record the production path writes), they are **unrepresentable**, and surfacing the discriminator is itself contract-altitude work, X-AL-3-blocked at Phase 7.
- **Second grounded correction — `risk_flags` gates nothing today.** A repo-wide grep finds it only at its definition, its two construction sites, and its persistence; `_review_required` (`memory_promotion.py:762-773`) never consults it and `_auto_promote_allowed` reads `review_required`, not flags. Any flag-based reading must therefore state the **gate** as well as the flag or it lands inert.
- **Net, corrected.** The permissiveness is **structural, not yet exercised**: live auto-promotion exposure at HEAD is **nil** on the composed-scope production path — the tool path is closed twice over (by record kind and by policy decision), and the hint path is **unfed** (the sole production caller passes `promotion_candidates=()`, `automatic_memory.py:273`; the extractor is otherwise reached only from compaction safety, `memory_compaction_safety.py:159`) with its candidate convention undefined. What is real *now* is the contract gap — which **no single named change converts into a live exposure** (Codex R6 [P2-2]; the combinations are **(a) ∧ (b)** on the tool path, with **(c)** `ReviewMode.AUTOMATIC` already satisfied in production, or the hint producer **∧ (b) ∧ an actuator that does not exist**). What a single change *does* buy is the **transition below it** — (a) makes captured records candidate-reachable and durably `PROPOSED`; (b) makes the second-order case actively auto-promoting — and each such transition happens **with no further review**, because no gate on any of these paths has a family term to consult. That is why this is a Class 1 **spec** question and not a Phase 7 bug, and why the **urgency** is low while the **necessity** is not: nothing is leaking today, the last step to leaking is a two-field policy change nothing forbids, and the contract has no position to fall back on when it happens.
- **Three readings (fork §4).** **(A)** promotion-eligible unchanged, status quo made **explicit** — cheapest (one C-MEM-10 paragraph, zero code, zero discriminator); grounded in C-MEM-03 §"`MemoryScope.provider_family` value domain and derivation" (the run's partition is a run-level identity the fallback chain preserves), the B-86/B-89 capture ratification (C-MEM-13 §"Cross-family withholding of standard memory tools", "a different authorship class"), and the fact that the content passed the same capture pipeline as any other record. **Codex R4 [P1] correction to that second bullet, upheld by direct read and stated rather than quietly repaired:** a first draft added *"promotion confers durability on content the harness holds, and grants the foreign candidate no new reach"*, and **both halves were wrong — promotion is a reach transition, not a durability one.** The captured record is **already durable**: `_capture` writes it to the canonical store (`self._store.write_record(record)`, `memory_capture.py:730`) *before* its C-MEM-08 row, deliberately (`:721-729`). What promotion adds is **injectability** — every capture-written kind sits outside the sole production policy's `eligible_record_kinds` (`_policy_from_config` pins it to `_retrievable_kinds()`, `automatic_memory.py:556`, defined `:560-569`), and a `PROPOSED` promotion output is retrieval-excluded by construction (`memory_retrieval.py:71-77`), so **only** an `ACTIVE` semantic/procedural record is retrievable and therefore injectable into a future run. Foreign-produced content **does** gain new reach at promotion — content that could not enter a future run's context becomes content that can, under the primary's family attribution — which is exactly the Memory threat model §Threats channel *"Cross-run prompt-injection persistence through promoted semantic/procedural memory."* and exactly the §Invariants transition *"Model-authored notes are episodic by default and cannot become injectable semantic memory without policy and evidence."* What gains nothing is the foreign **provider** (no capability, no tool, no boundary crossing it did not already have) — that, and only that, is the true residue of the C-MEM-13 carve-out. **A's honest form is therefore "the reach transition is *acceptable* because the composed scope is the run's declared identity", not "there is no reach transition"**: A loses the only rationale that claimed the question was *benign* rather than merely *acceptable*, and B's contract argument is correspondingly **strengthened**. **(B)** flag cross-family-produced candidates with a `CROSS_SCOPE`-equivalent risk flag **plus a stated gate** (review-required, never auto-promotable) — preserves eligibility, removes silent auto-promotion, requires the discriminator; grounded in the threat model's own provenance axis (its §Threats opener and its cross-run-prompt-injection-persistence bullet, and its model-authored-notes §Invariant). **(C)** refuse promotion outright — strongest isolation, still requires the discriminator, and forecloses the continuity value the B-86 council explicitly preserved.
- **Discriminator options (fork §5; needed by B and C).** **(i) A new content/envelope field at C-MEM-03 altitude** — **two variants with materially different blast radii, split at Codex R6 [P2-1] (upheld by direct read at `1c99e208`) because the R4 accounting charged the identity-movement consequence to the whole of (i) when it belongs to the content variant alone.** *Both* variants pay a closed-schema amendment (`MemoryRecordEnvelope` is `ConfigDict(extra="forbid", frozen=True)`, `harness-is/src/harness_is/memory_record_envelope.py:129`; and at contract altitude either reverses v1.1's own *"zero new field"* posture, change-note **"Sections preserved verbatim at v1.1."**, one delta later), *both* are derived at **one central site** (R9 below), and *both* are **forward-only** absent a back-fill (which would be option (ii) run offline, inheriting its whole unresolvable set). **Codex R9 [P2-1] correction, upheld by direct read at `26720e8e` and stated rather than quietly repaired:** the pre-R9 text charged both variants with *"touching every capture writer"* (fork §5: *"Six `capture_*` methods plus `_capture` / `_record` thread the new value"*) — **false on two counts.** There are **seven** `capture_*` methods on `EpisodicMemoryCapture` (`memory_capture.py:294`/`:337`/`:382`/`:439`/`:487`/`:534`/`:585`; class at `:241`), not six, and **none needs to thread anything**: each already takes `provider: str | None` and passes it to `_capture` (`:321`/`:366`/`:423`/`:471`/`:518`/`:569`/`:615`), which threads it into `_record` (`:649-658`). `_record` (`:1015-1046`) already holds **both** inputs the predicate needs — the call's raw `provider` (`:1025`) and the record's **resolved** scope from `_scope_for_record` (`:1038-1042`, defined `:1048-1086`) — so the derivation is a **single hoist inside `_record`**, with zero per-method threading and zero public signature change; and an optional field defaulted to the tri-state *unknown* leaves the other three `MemoryRecordEnvelope` construction sites untouched (`memory_promotion.py:844`, `native_memory_adapter.py:463`, `memory_compaction_safety.py:327`) and lets a pre-amendment envelope still validate under `extra="forbid"`. **The leveling is unmoved** — third cost struck from (i) in three rounds (R6 identity movement, R8 the family value domain, R9 write-side reach), but on an axis (ii) had already won at **zero**, leaving untouched the three that carry (ii)'s side: existing records, aggregate-run provenance, comparison-basis flexibility. What it corrects is the cost's **kind**: (i)'s write-side price is a **contract** obligation, never a seven-signature refactor. **Codex R8 [P2-2] correction, upheld by direct read at `d9907c24` and stated rather than quietly repaired — the remaining cost, a second family-valued field beside `MemoryScope.provider_family` plus the C-MEM-03 value-domain reconciliation, was charged to all of (i) and belongs to one PAYLOAD shape only.** Option (i) varies along **two** axes: **placement** (content vs envelope, the R6 split) and **payload** (a family *value* vs the boolean `captured_cross_family` the option's own opening sentence offers and nothing then costed). **(i-envelope-bool)** stores a **tri-state** predicate (true / false / unknown, the B-91 idiom) derived at capture by canonicalizing the call's own `provider` through the *existing* house authorities (`provider_family_for_scope_check`, `harness-runtime/src/harness_runtime/lifecycle/cross_family_cost_tag.py:101`; `canonical_scope_family` / `resolve_scope_family`, `harness-runtime/src/harness_runtime/memory_scope_family.py:50` / `:62`) and comparing it against the record's own composed `scope.provider_family` — **no** new value domain, **no** second family-valued field, hence no one-source-of-truth hazard (a predicate *over* that field is a dependent of it, not a rival authority). It still pays the closed-schema addition, the **one** central derivation in `_record` (R9 [P2-1]), and forward-only: **not a cheaper schema change — the same schema change with a narrower contract surface behind it.** It **must** be a tri-state, not a `bool`: `provider` is `str | None` at every `capture_*` signature (`memory_capture.py:302` / `:347` / `:396`), `provider_family_for_scope_check` returns `None` for an **unregistered** key and requires scope-boundary callers to fail closed on it, and a composed scope's `provider_family` may itself be `null` (the C-MEM-03 unpartitioned wildcard), against which "cross-family" is undefined — storing `false` there would record a determination the writer never made, the permissive-by-silence defect this fork exists to close. **What it cannot answer:** *which* family produced the content (so reading B's flag semantics are bounded to present/absent, and a later family-discriminating policy needs a second amendment at the family payload's price); aggregate-run provenance (unchanged); and it bakes the comparison basis in at write time. **Net, honestly: on reading B as this filing argues it — a risk *flag* plus a gate, i.e. a present/absent signal — (i-envelope-bool) is plausibly the cheapest *adequate* discriminator**, which is R8's honest discharge rather than a restored Q2 recommendation: it changes (i)'s internal shape only, leaving untouched the three axes on which (ii) wins (already-written records, zero write-side reach, aggregate-run provenance). **(i-content)** additionally **moves identities**: `compute_memory_content_hash(content)` digests the canonicalized **content mapping alone** (`memory_record_envelope.py:207-210` over `canonicalize_memory_content`, `:189-204`) and `derive_memory_id(tier, kind, content_hash)` (`:213-222`) composes the id from it — as C-MEM-03 §Invariants states ("`content_hash` is computed over canonical serialized content excluding derived indexes.") — so every newly written record of every kind **except** `EPISODIC_RUN` (whose id is `run_id`-derived, `memory_capture.py:1100-1109`) changes identity, needing a forward-only statement. **(i-envelope)** moves **none**: `_record` (`memory_capture.py:1015-1046`) computes `content_hash` at `:1027` and `memory_id` at `:1028` **before** building the envelope around them (`:1030-1044`), and the store re-derives the same way (`expected_hash = compute_memory_content_hash(self.content)`, `harness-is/src/harness_is/memory_store.py:95`). *(This also corrects a drifted cite carried since R4, which anchored `MemoryScope`'s hash-inertness at `memory_capture.py:574-575` — a `source_ref=` / `run_id=` argument pair inside `capture_failure_observation`; the property is real, the anchors are the three named here.)* **(i-envelope) is therefore the real contender and (i-content) is strictly dominated by it. Codex R7 [P2] correction, upheld by direct read at `d8df7647` and stated rather than quietly repaired — the R3/R6 multi-writer penalty charged to (i-envelope) is FALSE.** The mechanics R6 recited are correct: `capture_run_close` (`:337-380`) does not patch a stored record but goes through `_record`, which builds a **complete, fresh** envelope including a re-computed `scope=self._scope_for_record(...)` (`:1038-1042`), and `MemoryStore.write_record` writes the **whole** canonicalized record — envelope *and* content — to the single `run.json` (`memory_store.py:178-188`; `EPISODIC_RUN` takes the `_write_file_atomically` branch at `:186` against `_run_record_path`, `:273-274` / `:290-295`). **The inference from them is wrong.** Envelope and content being written *together, from one call*, is exactly why an envelope provenance field does **not** go stale: the stored `content["event_type"]` says `run_close`, and the field would carry **that same call's** `provider` (the argument `_capture` threads into `_record` at `:649-658`), so the overwrite **preserves** provenance for the version that is stored — the identical semantics (ii-a) reaches *indirectly* by selecting the row `event_type` names. "Records nothing about which writer set it" is not a defect when the field describes the version it ships inside; the reader promotes **that** version, not the run's history. And the two diverge in exactly one place — the R7 [P1] case below — where **(i-envelope) reads the stored version's own provider and (ii-a) reads a stale one**, so on the multi-writer kind the asymmetry **inverts** in (i-envelope)'s favour. **(ii) A C-MEM-08 ledger join at promotion time** — **read-side only, no schema change**: the join key already exists — though it is the capture **`idempotency_key`**, **not** `memory_refs` and **not** the bare `action_id` (see (ii-a)); `MemoryOperationEntry` (C-MEM-08 Contract) carries `provider: string | null` and `memory_refs: list<memory_id>`, and the raw-key→family normalization reuses the existing `provider_family_for_scope_check` / `canonical_scope_family` authorities. **Codex R3 [P2-1] correction, upheld by direct read and stated rather than quietly repaired:** the first draft's premise that *"capture writes exactly one row per record"* is **false for `EPISODIC_RUN`**. `_memory_id_for` (`harness-runtime/src/harness_runtime/memory_capture.py:1100-1109`) derives an `EPISODIC_RUN` `memory_id` from the **`run_id` alone** — content-independent — so `capture_run_start` (`:294-335`) and `capture_run_close` (`:337-380`) write **two** capture rows against **one** `memory_id`, and the module says so where it exports the remedy: `capture_operation_action_id` (`:68-77`), *"run-start and run-close share a single EPISODIC_RUN `memory_id`, so `memory_refs` cannot tell them apart"* (and again at the repair-row probe, `:857-861`). Each `capture_*` method takes its **own** per-call `provider` (`:302` / `:347`; the runtime supplies the run-start selection at `automatic_memory.py:336`), so post-fallback the two rows can carry **different provider keys** and a `memory_refs`-keyed join is **not a function** — it classifies provenance by whichever row the scan selected. *Honest scoping, same footing as the rest of this row:* `capture_run_close` has **no production caller at HEAD** (sole call site `harness-runtime/tests/test_memory_capture.py:151`), so today every stored `EPISODIC_RUN` record does carry exactly one row — the ambiguity is **structural, not yet exercised**, and contract text must not rest on the accident. **Resolution, two sub-options.** **(ii-a) event-qualified join [recommended]** — key the join on the capture **`idempotency_key`**, itself composed from the capture `action_id`, which is composed from the record's *own declared writer*: `Identifier(f"idempotent:{capture_operation_action_id(stored_capture_event_type(record.content), memory_id)}")`, i.e. `idempotent:capture:{event_kind}:{memory_id}`. Both inner halves are **already exported**, and their docstrings name this exact discrimination as the reason: `stored_capture_event_type` (`memory_capture.py:1149-1163`) is *"the record's own statement of WHICH event wrote it — the discriminator a reader needs when one `memory_id` is shared by several events"*. Because the record is overwritten by its later writer, its `content["event_type"]` names the event that wrote **the version being promoted**, which is precisely the row whose `provider` is that content's provenance. **Codex R4 [P2] correction, upheld by direct read at `ca0cc5a2` and stated rather than quietly repaired:** the R3 formulation ended *"the lookup is exact-match single-row and the ledger's `action_id` idempotency makes '>1 row' unreachable rather than merely unlikely"* — and **there is no `action_id` idempotency in the ledger.** `append_memory_operation` (`harness-is/src/harness_is/memory_operation_ledger.py:522-545`) dedups by **`idempotency_key` alone** (`:532-539`) — equal key + equal 18-field equivalence payload → `IDEMPOTENT_NOOP`; equal key + divergent payload → `MemoryOperationIdempotencyConflictError` — while `action_id` is a caller-supplied field with **no** ledger-enforced uniqueness, so two entries may share one `action_id` under different idempotency keys and an `action_id`-keyed join is **not** guaranteed single-row. What makes the join well-defined is that the capture writer derives the key **deterministically** from the id (`idempotency_key=Identifier(f"idempotent:{action_id}")`, `memory_capture.py:999`, the sole construction site for every `capture_*` method), so the key **is reconstructible at promotion time from the stored record alone**, and once reconstructed the ledger guarantees **at most one** occupying entry. Two consequences the contract must carry: **(1) state the join on the key, not on the id**; and **(2) qualify on `operation_kind == CAPTURE`** — writer namespacing (`capture:...` here vs promotion's `promotion:...` at `memory_promotion.py:523-528`, which shares the *identical* `idempotent:{action_id}` spelling) is a convention across producers, not a ledger invariant, so the matched row must be **confirmed** to be a capture row, never assumed to be one. **Codex R7 [P1] correction, upheld by direct read at `d8df7647` and stated rather than quietly repaired — a matched row is not, by itself, a row about *this version of* the record, so (ii-a) owes a third consequence: a MANDATORY record/ledger coherence check whose detection bound must be stated.** Exhibited by a **shipped test**, not hypothesized: `test_a_divergent_second_run_start_capture_is_not_read_as_completion` (`harness-runtime/tests/test_u_mem_26_write_boundary.py:1602-1662`) — a divergent second `capture_run_start` for one `run_id`, reachable because `EpisodicMemoryCapture` is a **public API** and `LocalAutomaticMemoryRuntime`'s presence guard is not in it, **overwrites `run.json`** (`memory_capture.py:730`) and only *then* meets the first call's row (`:732`). The capture correctly returns `FAILED`, but the durable residue is a record holding **call two's** content beside a single ledger row holding **call one's** `provider`; reconstructing the (ii-a) key against that record still resolves to exactly **one** `CAPTURE` row with a non-`None` provider, so every qualification above passes and the join returns **stale provenance as if determined**. None of cases (1)-(5) catches it. **What is checkable, exactly:** no field on the capture row binds it to a stored-content *version* — `MemoryOperationEntry` (`harness-is/src/harness_is/memory_operation_ledger.py:136-154`) carries no content hash; the `response_hash` inherited from `StateLedgerEntry` (`state_ledger_entry_schema.py:164-169`) is computed over **the ledger entry's own** canonical payload (`compute_memory_operation_response_hash`, `:335-338`, over `canonicalize_memory_operation`, `:324-332`); and `memory_refs` carries a `run_id`-derived, content-independent `memory_id`. So *"bind the row to the stored content hash"* **is not available at HEAD** — the field does not exist. What *is* available is the set of values `_capture` threads from **one call** into **both** surfaces: `entry.timestamp` ↔ `record.envelope.created_at` (to `_record` at `:649-658` and `_operation_payload` at `:694-707`), `entry.cli_profile` ↔ `content["cli_profile"]`, `entry.engine_class` ↔ `content["engine_class"]` (`_engine_class_value`, `:1122-1125`), `entry.step_id` ↔ `content["step_id"]` on the kinds that carry one, and `entry.run_id` ↔ `content["run_id"]` (vacuous — the key already derives from it). The `created_at` comparison must be scoped to **dispatch** rows (`repair_capture_operation` takes the repairer's own `datetime.now(UTC)`, `automatic_memory.py:442`), which costs nothing since a repair row is case (5) already. **Codex R8 [P2-1] correction, upheld by direct read at `d9907c24` and stated rather than quietly repaired — that comparable-field set is PER-KIND and the check is owed on `EPISODIC_RUN` ALONE.** Only `capture_run_start` (`memory_capture.py:309-320`) and `capture_run_close` (`:354-365`) write `cli_profile` and `engine_class` into **content**; `capture_turn_completion` (`:407-422`), `capture_tool_event` (`:459-470`), `capture_provider_route` (`:504-517`), `capture_failure_observation` (`:553-568`) and `capture_compaction_event` (`:604-614`) write neither — for those kinds both values reach the **ledger row only** (`_operation_payload`, `:1008-1009`) — while `step_id` is content-side on the turn / tool kinds and `None` on **both** surfaces for the run kinds (`:328` / `:377`) and compaction (`:622`). Stated kind-blind, the check false-`UNVERIFIABLE`s the entire non-run corpus against content fields those writers never store. **And the window it exists to close is `EPISODIC_RUN`-only anyway:** `_memory_id_for` (`:1100-1109`) derives every non-run kind's id from `content_hash` and `capture_operation_action_id(event_kind, memory_id)` (`:68-77`) composes the join key from that id — so there **the key IS the content binding**, precisely what R7 found missing for `EPISODIC_RUN`; a divergent re-capture is a *different* `memory_id` with its own row, and row and stored version cannot come apart. Every content-side comparator is therefore vacuous by construction there, leaving `created_at` — which **false-positives**: `timestamp` is not in the ledger's equivalence payload (`_equivalence_payload_from_payload`, `harness-is/src/harness_is/memory_operation_ledger.py:499-519`; eighteen keys, `provider` at `:511`, no `timestamp`), so a benign identical-content re-capture at a later instant returns `IDEMPOTENT_NOOP` while re-writing the record with a new `created_at`, and the three JSONL-backed episodic kinds read **last-match-wins** (`_read_jsonl_record`, `harness-is/src/harness_is/memory_store.py:445-463`; `_JSONL_BY_KIND` at `:143-147`). The genuinely divergent case never gets there — the equivalence payload *does* carry `provider`, so a same-content re-capture on a different provider raises `MemoryOperationIdempotencyConflictError` and, not being `RUN_START_EVENT_KIND`, re-raises into `FAILED` (`memory_capture.py:796-802`). **So the contract text is:** mandatory on `EPISODIC_RUN`, comprising `envelope.created_at` ↔ `entry.timestamp` (dispatch rows only), `content["cli_profile"]` ↔ `entry.cli_profile` and `content["engine_class"]` ↔ `entry.engine_class` (on the enum's `.value`, `_engine_class_value`, `:1122-1125`); **not owed on any other kind**. The residual disposition (α/β/γ) is likewise `EPISODIC_RUN`-scoped, which *sharpens* β — it is visibly the whole affected corpus, not a subset. **And the honest bound: this check does not close the case that produced it.** The witness's two calls differ in `workflow_id` / `provider` / `model` / `policy_ref` and agree on `cli_profile`, `engine_class` and `timestamp`; `provider` / `model` / `policy_ref` / `procedural_snapshot_ref` are **ledger-only** and `workflow_id` / `thread_id` / `provider_route` / `started_at` / `close_status` are **content-only**, so a divergence confined to either set is invisible to any comparison the two surfaces support. **The spec leg therefore owes an explicit disposition of the residual**, in one of three forms, none free: **(α)** add the record's `content_hash` (or an envelope-version stamp) to the capture row — a **C-MEM-08 closed-schema field addition**, i.e. exactly the schema cost (ii) was preferred for avoiding, and it repairs no already-written row; **(β)** classify every `EPISODIC_RUN` record `UNVERIFIABLE` by rule, which is (ii-b) applied to that kind and carries its stated foreclosure; or **(γ)** state the residual as **accepted**. **(i-envelope) has no counterpart residual** — its field ships *inside* the version it describes. **(ii-b) fail closed on ambiguity** — define no event-qualified key and map any multi-row resolution to `UNVERIFIABLE`. It invents no join semantics, but it is **not** the conservative default it looks like: it makes **every** `EPISODIC_RUN` record permanently unverifiable the moment `capture_run_close` acquires a production caller, i.e. it silently becomes reading **C** for run records while presenting itself as reading B, and must be taken as a stated foreclosure. The two are **not exclusive**: (ii-a) is the join, (ii-b) is its residue. Its real costs are therefore a **new cross-contract dependency** (C-MEM-10 on C-MEM-08 availability, plus an unindexed scan of `durable/memory_ops.jsonl` — no index over `memory_refs` *or* `action_id`) and an **unresolvable set each round makes larger — R3 took it from one case to three, R4 to five, R7 to six**: (1) no reachable capture row (promotion-, compaction-, native-adapter-authored, or unreadable ledger); (2) `stored_capture_event_type` returning `None` — deliberately strict, *"an unrecognizable record, never a guess at one"* — so the record cannot name its own writer; (3) the composed **key** matching no row (a torn capture whose repair never ran, `memory_capture.py:721-732`); **(4) [R4]** the matched row is **not** a `CAPTURE` row (`operation_kind != MemoryOperationKind.CAPTURE`) — the cross-producer `action_id` namespacing convention did not hold, and the ledger never guaranteed it would; **(5) [R4]** the matched row **is** a capture row but carries `provider = None`, which is the torn-capture **repair** row's shape *by construction* (`repair_capture_operation` passes `provider=None` / `model=None`, `memory_capture.py:938-939`, deliberately — *"a repair row attests that this capture is durable, not which dispatch performed it"*, `:916-921`), so a **repaired** capture yields *unknown* provenance and must not read as *same-family* merely because a row was found; **(6) [R7, scoped at R8]** the matched row **fails the record/ledger coherence check** (above) — row and stored record disagree on a value only one call could have written. **`EPISODIC_RUN` only** (`envelope.created_at` ↔ `entry.timestamp` on dispatch rows, `content["cli_profile"]`, `content["engine_class"]`; **not** `step_id`, `None` on both surfaces there — and the other kinds' join key is itself the content binding, so the failure mode cannot arise) — so the row is about a *different version* of this record and its `provider` is not this content's provenance; **detection-bounded, not exhaustive**, per the residual disposition above. Plus the ambiguity arm: **any** resolution returning more than one row is `UNVERIFIABLE` — **mandatory in the contract regardless**, vacuous only for the exact key-shaped (ii-a) join, since any join keyed on `action_id` or on `memory_refs` carries no single-row guarantee at all. All of them must be specified rather than defaulted — reuse the B-91 tri-state idiom verbatim (`MATCH` / `CONFIRMED_MISMATCH` / `UNVERIFIABLE`; `UNVERIFIABLE` → review-required, never auto-promote).
- **Recommendation: reading B [MODERATE] on Q1. On Q2 the recommendation is WITHDRAWN at R7 — the comparison has LEVELED and is presented as a two-option decision, not a pick, with a payload sub-choice under (i) added at R8.** *(R8 tested the leveling and left it standing: its two findings refine one column each — (ii)'s coherence check narrowed to `EPISODIC_RUN`, (i)'s boolean payload de-priced — and cancel on the balance. **R9 tested it again and it held again:** [P2-1] struck (i)'s write-side reach, but on the one axis (ii) had already won at zero, leaving the three axes that carry (ii)'s side untouched.)* Through R6 the pick was **(ii)** with the **(ii-a)** event-qualified join, held in place by **one** asymmetry: that (i-envelope) resolved the multi-writer kind by silent last-writer-wins accident where (ii-a) resolved it deliberately. **R7 [P2] found that asymmetry false** (envelope and content are co-written from one call, so the field describes the version it ships inside — (ii-a)'s semantics reached *directly*), and **R7 [P1] found the reverse asymmetry real** (nothing on a capture row binds it to a stored-content version, so a divergent second writer leaves (ii-a) reading stale provenance from a correctly-qualified single row — a case (i-envelope) cannot have). Having said at R6 that a struck-out cost *would* move the balance and that this row would say so, it says so. **The leveled ledger.** **(i-envelope)**: same-call provenance semantics that cannot go stale relative to the version they describe; no identity movement; no cross-contract dependency; a one-field read side — *costs* a closed-schema amendment at C-MEM-03 reversing v1.1's own zero-new-field posture one delta later, a second family-valued field beside `MemoryScope.provider_family`, and it is **forward-only** (no help for already-written records absent a back-fill, which is option (ii) offline) — the *"every capture writer touched"* cost previously listed here is **struck at R9 [P2-1]** as false. **(ii)/(ii-a)**: read-side only *as specified*, works on already-written records, and is the **only** option that can answer **aggregate-run** provenance (every capture row for the run is retained with its own `provider`; an envelope carries one writer's value only) — *costs* a new cross-contract read dependency plus an unindexed scan, **five** owed contract sentences (up from four), a mandatory coherence check whose detection bound must be stated, and an explicit disposition of the residual it cannot reach, of which **(α)** is itself a C-MEM-08 closed-schema addition — so "read-side only" is true of (ii-a) *as specified*, not of every honest closure of it. **What would decide it, and what this filing declines to invent:** the two columns answer *different questions* and the requirement has never been stated. **Stored-version provenance** ("which dispatch produced the content I am promoting") leaves them semantically equivalent on the coherent path with (i-envelope) strictly more reliable on the incoherent one; **aggregate-run provenance** ("did any leg of this run run cross-family") *requires* (ii), and adopting that reading is itself a decision this filing has not argued for. Q2 confidence stays **[MODERATE]** and is now confidence in the *framing* rather than in a pick: this comparison has been corrected at R3 (wrong field family), R4 (wrong uniqueness guarantee), R6 (variant-blind cost) and twice at R7, once on each side. What the filing carries forward is the mechanics **both** options must respect — state the key exactly and what enforces its uniqueness; state the coherence check and its detection bound; state the residual's disposition; and under (i) state hash-inertness and the forward-only consequence. The B-86 council record decides it in two directions at once. It **forecloses C**: C3's continuity argument (candidate-side partitioning *"makes the moment accumulated context matters most (the fallback leg) the moment it disappears — silent, permanent, inverted-in-timing data loss"*) and C6's *"a fallback chain is a **continuity** abstraction, not a substitution abstraction"* refinement apply *a fortiori* to a promotion refusal. It does **not license A's silence**: C10 classified `memory.write_note` `write-bounded-irreversible` precisely because it is *"durable, primary-attributed, promotion-eligible"* — naming promotion-eligibility itself as part of the danger — and declined to re-key the scope only because the *mechanism* was wrong (*"the defect is in the absent gate at the dispatch boundary, not in the scope's key"*). Reading B is that same resolution shape one contract downstream: keep the partition (C3/C6), put the gate where it belongs (C10). **The R4 correction strengthens exactly this half:** capture is durable but **not injectable**, and promotion to an `ACTIVE` semantic/procedural record is exactly and only the step that makes content injectable into a future run — so the threat model's *"Cross-run prompt-injection persistence through promoted semantic/procedural memory."* bullet is not *adjacent* to this question: **promotion is the whole of that channel**, and C-MEM-10 is the contract that governs it. The decisive asymmetry against A is that the pipeline today is permissive **by blindness**, not by policy, on the production composed-scope path (the residual path, which can see the producing family, does not pose the question), so "eligible, unchanged" would convert a structural gap into a contract commitment the policy layer never evaluated — the exact defect the v1.1 change-note named as B-86's own trigger (**"Why this is conformance repair, not design extension (X-AL-3)"**). **Confidence on Q1 stays MODERATE, not HIGH — the R1 exposure corrections *lowered* it, and R4 is the first round that pushed *upward* without moving it.** B costs a discriminator A does not, and the exposure that discriminator buys down is **nil at HEAD**, not merely bounded; B therefore rests entirely on the *contract* argument, which R1 leaves unweakened and R4 *strengthens* (it was never that content is leaking today). The tag was never set by doubt about that argument but by the cost/exposure asymmetry, which R4 leaves intact — so what moves is **A-as-permanent** (now weaker, having lost its "no new reach" rationale), while **A-as-interim is untouched**, resting as it does on the exposure fact rather than on the corrected rationale. **Consequence, stated rather than steered:** **A-as-interim is now materially more defensible** — with zero live exposure it costs nothing observable, while B's discriminator is real work against a path nothing exercises — *provided* two conditions are recorded with it: (a) an interim with **B named as the target**, not a discharge; and (b) the interim-ending triggers named **as the combinations they actually are** (the R6 [P2-2] sharpening — they remain **review-triggers**, any one of which re-opens the question, but each is now recorded with the transition it enables so no later reader infers that one alone turns on live auto-promotion): **T1** a policy admitting a capture-written kind into `eligible_record_kinds` → **candidate-reachability**, a durable retrieval-excluded `PROPOSED` corpus that a later family-term-free operator flip activates; **T2** the decision moving to `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL` → **active auto-promotion of already-eligible kinds**, live *immediately* since `ReviewMode.AUTOMATIC` is already the production setting (`automatic_memory.py:555`), i.e. the only trigger one change away from real promotions today; **T1 ∧ T2** → the full **first-order** exposure, at which point the interim's premise is false; **T3** the first caller to populate `promotion_candidates` → **hint-path candidate-reachability only** (it promotes nothing by itself: under `PROPOSE_SEMANTIC` `_auto_promote_allowed` is False, and no actuator reads it), but it forces the undefined candidate-family convention and would naturally arrive with the actuator that does not exist today. Absent (b) the interim silently becomes permanent exactly when it stops being safe — and the single-change framing would have let T1 or T2 pass later as "not the live one yet". B remains the recommendation on the merits; A-as-recorded-interim is now a close second rather than a concession. What must not happen is still a second round of silence.
- **Ratification ask.** **Q1** — A, B, or C (or "A as recorded interim, B as target", recorded with the **T1 / T2 / T1 ∧ T2 / T3** triggers above rather than with a two-item list, so each transition is named). **Q2, only if B or C — a LEVELED two-option decision, presented without a recommendation** *(changed at R7; the A/B/C reading recommendation is untouched — it never rested on Q2)*. **(i-envelope)** — a new hash-inert provenance field on `MemoryRecordEnvelope`: same-call semantics that cannot go stale relative to the stored version, no identity movement, no cross-contract dependency; costs a closed-schema C-MEM-03 amendment and is forward-only (the write-side reach previously listed here is struck at R9 [P2-1]: one central derivation in `_record`, no public signature change) — **with a payload sub-choice added at R8**: **(i-envelope-bool)** (a tri-state `captured_cross_family`) adds no value domain and no second family-valued field and is the cheapest shape, but answers present/absent only; **(i-envelope-family)** adds the family value, supports a future family-discriminating policy, and pays the value domain plus a second family-valued field beside `MemoryScope.provider_family`. *(Variant (i-content) is strictly dominated by it — same benefits plus identity movement.)* **(ii)/(ii-a)** — the event-qualified C-MEM-08 ledger join: the row is selected by the capture **`idempotency_key`** (`idempotent:capture:{event_kind}:{memory_id}`, composed from the record's own declared `event_type`), **not** by `memory_refs`, which cannot discriminate the run-start / run-close rows sharing one `EPISODIC_RUN` `memory_id`, and **not** by the bare `action_id`, which the ledger does not dedup on; the matched row is qualified on `operation_kind == CAPTURE`, and the **six**-case `UNVERIFIABLE` residue plus the multi-match arm fail closed — review-required, never auto-promote; works on already-written records and is the only option that answers aggregate-run provenance; costs a cross-contract read dependency plus an unindexed scan, five owed sentences, the mandatory coherence check with its detection bound, and the residual disposition (α/β/γ) of which α is itself a C-MEM-08 schema addition. Sub-choice **(ii-b)** — fail closed on *any* multi-row ambiguity — is available and needs no new join semantics, but is a de-facto reading **C** for `EPISODIC_RUN` records and must be taken as a stated foreclosure. **What would decide it:** state the requirement — **stored-version** provenance favours (i-envelope); **aggregate-run** provenance *requires* (ii). **A durable contract commitment** — once C-MEM-10 states a position, records accumulate against it, and promotion supersession does not delete (C-MEM-03 §Invariants, *"Supersession does not delete the prior record."*). **Codex R9 [P2-2] correction, upheld by direct read at `26720e8e`:** the pre-R9 text said *"irreversible"*, with reversal leaving the corpus *"with no way to re-adjudicate the already-promoted records"* — **too strong.** `_semantic_record_content` persists lineage (`"source_memory_refs"`, `memory_promotion.py:919-943`, field at `:932`) and capture rows retain their `provider` — exactly what option (ii) reads, and option (ii) works on already-written records — so a later migration **can** traverse lineage → row → provider and supersede or deny promotions made under a reversed position. **Reversal is costly and INCOMPLETE, not impossible**, bounded by this filing's own unresolvable set: **missing** rows (cases 1/3 — no reachable row, or a torn capture whose repair never ran), **repaired** rows (case 5 — `provider = None` by construction, `memory_capture.py:938-939`), **stale** rows (case 6, `EPISODIC_RUN`-scoped per R8 — a matched row describing a superseded version, detected only within the coherence check's own bound); **plus a fourth bound on the traversal**: `_procedural_record_content` (`:959-989`) writes `evidence` but **no** `source_memory_refs`, so promoted **procedural** records carry no lineage at all. **The gate stands on a narrower ground:** the cheapest moment to decide is *before* durable records accumulate — every promotion under a later-reversed position becomes a row in an unindexed, partly-blind, non-deleting migration that cannot recall content already injected into a completed run. **RATIFIED 2026-07-29 (operator `AskUserQuestion`, autonomous-loop session; root `CLAUDE.md` §14.2 — the interactive menu is this workspace's ratification primitive; §12.4.1 — the loop drives an item to its **genuine** gate and surfaces exactly that). Q1 = READING B, "flag + gate":** cross-family-produced candidates carry a risk flag **and** a review gate (`review_required`, never `auto_promote_allowed`) — **eligibility PRESERVED** (reading C's outright refusal foreclosed, and with it the harm the B-86 council named — C3's *"silent, permanent, inverted-in-timing data loss"*, C6's continuity abstraction) and **the silence REMOVED** (reading A not adopted as the permanent posture). Per fork §4's own cost line the ratified position **states the gate, not only the flag**. The **A-as-recorded-interim** variant was **not** selected, so its T1/T2/T3 interim-ending triggers are not owed. **Q2 (the discriminator mechanism) was deliberately NOT decided** — the selected option's own text routes it: *"spec leg picks the mechanism from the filing's leveled Q2 analysis"*, the same disposition fork §10's cap already made binding and which this ratification leaves unchanged; the option + payload sub-choice, the join key and its `operation_kind == CAPTURE` qualification, the six-case `UNVERIFIABLE` set + multi-match arm, the coherence check with its `EPISODIC_RUN` scope and detection bound, the α/β/γ residual, (i)'s placement / payload / derivation rule / tri-state / hash-inertness, and the missing **requirement** statement are **all the spec leg's**. §8's discriminator guidance stands as *input, not decision*: reading B's requirement as this filing argues it is stored-version provenance **present/absent**, answered most cheaply by **(i-envelope-bool)** — a recommendation the spec leg **re-derives** against its own HEAD, not a ratified choice. **Status STAYS `design_substrate_gated`** — the ratification answers §8, it does not run the spec leg (this register's own status enum: *"a filed doc does NOT flip the status — only ratified + applied spec deltas"*); the row flips to `open` when the C-MEM + plan deltas are APPLIED. **Next-step head: open the SPEC-WRITER APPLY LEG as its own arc/PR** — the C-MEM-10 amendment (risk-flag vocabulary sentence — `risk_flags` is an **open** `list<string>`, so a vocabulary addition and **not** a schema widening — plus the gate-bearing obligation that makes the flag non-inert) + the discriminator's owed contract text at C-MEM-03 (option (i)) or C-MEM-08 (option (ii)) + a **new U-MEM-27** memory-plan delta + clearance markers in the same PR + an out-of-family `just codex-review` decorator on the C-MEM diff; the **impl leg** follows as its own arc, per the **B-86 → PR #1147 (spec) → PR #1148 (impl)** precedent chain — spec first, impl second, never bundled. **Run the spec leg as a FRESH, FOCUSED session:** fork §10's cap *requires* it to re-ground every mechanics claim (all pinned to code at `26720e8e`) against its **own** HEAD before authoring contract text (the B-86 → U-MEM-26 precedent), and a session carrying this filing's nine review rounds forward inherits precisely the claims the cap requires be re-derived. Recorded in full at the fork doc's **new §11**.
- **Council: not required — the deciding council already sat.** The B-86 convening (C10 + C3 co-primary, C6 consultant, 2026-07-28) produced both the continuity argument that forecloses C and the write-bounded-irreversible argument that forecloses A's silence; the C10↔C3 tension here is the **same one already surfaced and probe-resolved**, not a new one. Re-convene only if the operator selects against the recommendation and wants it re-argued at C-MEM-10 altitude.
- **Routing.** Class 1 → **Phase 5 spec revision-pass** (C-MEM-10, plus C-MEM-08 or C-MEM-03 per Q2 — and per R7 the C-MEM-08 leg now owes **five** sentences including the coherence check — stated per R8 with its **record-kind scope** (`EPISODIC_RUN` only) as well as its detection bound — and the residual disposition, while the C-MEM-03 leg owes the forward-only consequence alongside hash-inertness and, per R8, differs by **payload**: (i-envelope-bool) owes a derivation rule and a tri-state unknown, (i-envelope-family) owes that plus a value domain and the reconciliation against `MemoryScope.provider_family`), with a memory-plan delta (a new **U-MEM-27**) owed under B/C and clearance markers in the same PR. Impl leg follows the spec leg per the B-33 / B-59 / B-70 / B-72 / B-86 precedent. Note `PromotionCandidate.risk_flags` is `list<string>` in the C-MEM-10 Contract `PromotionCandidate` block, an **open** list — reading B needs a vocabulary sentence, not a schema widening (only the impl-side `PromotionRiskFlag` enum, `memory_promotion.py:80-86`, is closed). **No Phase 7 execution is halted**: U-MEM-26 landed and already scoped this out at that plan's "Out of scope for this unit:" subsection; what is blocked is any implementation of a promotion-side family term.
- **Review status — SOUNDNESS EXIT declared at R9; the filing is CLOSED to further review rounds (fork §10).** Nine out-of-family rounds. **Stable across all nine:** the three A/B/C readings and their owed spec text, the **Q1 recommendation of B** at [MODERATE], the A-as-recorded-interim option with its T1/T2/T3 triggers, the §1 witness, the routing, and the **ratification ask as an ask**. **What moved:** only the Q2 mechanics comparison — **eight** corrections, **one** verdict change (R7's leveling) — which the filing now explicitly declines to decide, and at R9 the *precision* of the ask's justification. **Why R9 was admitted after R8 declared itself the last round the rule admits:** [P2-2] landed on the **ratification ask**, one of the three surfaces the exit condition names as live and the first finding since R4 to reach one; [P2-1] is a Q2-mechanics finding that would otherwise be **registered** against the spec leg, applied alongside it only under R8's own false-statement standard. **The exit is on soundness, not review-quiet.** All remaining sub-altitude mechanics are the **spec leg's by rule** — the Q2 option + payload choice, the join key and its qualification, the six-case `UNVERIFIABLE` set + multi-match arm, the coherence check with its `EPISODIC_RUN` scope and detection bound, the α/β/γ residual disposition, (i)'s placement/payload/derivation-rule/tri-state/hash-inertness text, and the missing **requirement** statement that would decide between the options; the spec leg re-grounds every one against its own HEAD (the B-86 → U-MEM-26 precedent). **Any further finding — including a factually correct one — is to be registered against `B-92` for the spec leg, not applied to the filing**; the sole exception is a finding against an A/B/C **reading**, the **Q1 recommendation**, or the **ask itself**, none of which nine rounds has touched. What remains owed is not review — it is the operator's answer.
- **Cross-ref.** Carried from **B-86** §7 (C3's fourth forward item) through the B-86 spec leg (`Spec_Memory_Substrate_v1.md`, change-note "Named open question carried forward, not discharged.") and U-MEM-26 (`Implementation_Plan_Memory_Substrate_v1.md`, "Out of scope for this unit:"). Adjacent open memory rows: **B-84** (batch atomicity), **B-88** (C-MEM-19 failure-class vocabulary).
- **CLOSED 2026-07-29 — U-MEM-27 impl leg.** Reading B is built. The envelope carries the tri-state `captured_cross_family` (hash-inert, defaulting to `unknown`, never back-filled); the write-side derivation is origin-aware exactly as the spec leg fixed it (`_capture` determines the content-origin disposition from `event_kind` and passes it into `_record`, which gates the canonicalized family comparison on it — `turn_completion` / `tool_event` dispatch-derived, the five other mapped kinds and every unmapped kind undetermined); the `cross_family_capture` flag plus its review-required / never-auto-promotable gate fire at **both** entry points under every policy configuration, with the illegal pair refused at every validating constructor and the reserved flag re-derived in both directions; and the activation boundary re-derives rather than trusts, on ONE frozen snapshot per service call with worst-value aggregation, per-branch fail-closed handling, choke-point normalization across all four `_persist_decision` callers, and the activation write bound to the snapshot through a guarded conditional write held inside the store's own write locks. **Eligibility is preserved, not removed** — the same candidate still reaches `ACTIVE` under explicit operator approval, which is the anti-reading-C witness. The two stated bounds stand: present/absent only (not by-family), and no aggregate-run provenance. Closeout window 2 closed in the same leg; arc record at `.harness/u-mem-27-impl-leg-close-2026-07-29.md`.

### B-93 · Whole-root memory-write serialization has no deadline — an unbounded guarded precondition stalls every memory writer on the host *(surfaced by the merge-gate concurrency lens on the U-MEM-27 impl leg, PR #1159, 2026-07-29; classified REGISTER at the post-#1159 gate-log grounding pass, 2026-07-30)* *(**RATIFIED 2026-08-05 as READING B** — deadline YES, Windows NO, vehicle HAND-ROLLED; **CLOSED 2026-08-06 — #1241**, the build leg)*

- **CLOSED 2026-08-06 — #1241 (the build leg).** Reading B built as ratified, **hand-rolled, no dependency added**. All **nine** acquisition sites bounded by a non-blocking probe + bounded retry against one typed **`CrossProcessLockTimeoutError` homed at `harness-core`** (`harness_core.cross_process_lock_deadline`, which also hosts the mechanism — `CrossProcessLockDeadline` + `flock_until_deadline`; zero workspace deps, and `fcntl` is imported lazily *inside* the helper so `import harness_core` stays Windows-safe). **Two design choices beyond the ratification, both flagged overturnable.** *First*, the deadline is **end-to-end per entry surface**, not per acquisition — trap 1 says one entry surface blocks at up to three sites in sequence *and* retries the whole acquisition on an inode swap, so a per-acquisition bound is exactly the partial-guarantee-that-reads-as-total variant **F** was dominated on; each of the seven entry surfaces mints one **absolute** deadline and threads it down, witnessed by *elapsed time across two sequential waits* rather than by a docstring. *Second*, the default budget is **300 s** — conservative because the registered harm is entirely prospective while a short bound would convert working systems into failing ones; every entry surface takes a per-call `deadline_seconds` override. **ABBA release-before-blocking preserved** at all three probe arms and separately witnessed (a sibling ledger in the same directory stays writable while a caller waits on the file lock). **Hold-time contract** documented at `write_record_guarded`. **§3(ii) reconciler import repair** landed — see `B-45`, whose no-op-surface enumeration this changes from six to seven. **58 new witnesses** (22 `harness-core` unit + 17 `harness-is` per-site + 19 across the `harness-runtime` suites), every per-site one driven by a **genuine second OS process** and each pinning *which* lock timed out via `lock_target`, so one bounded early acquisition cannot make the other eight pass. **The unwitnessed `_workflow_lock` carrier (codex R9) is now witnessed by execution** — a second OS process runs the *real* `_workflow_lock` and this process provably cannot enter until it leaves; deliberately **not** a deadline test, so exclusion and liveness stay separate claims. **43 PD-8 mutation probes, all RED:** reverting each of the nine sites to a bare blocking `flock` **hangs** its witness (the pre-B-93 harm reproduced); restoring the module-scope `import fcntl` fails the import-repair witness; deleting `_workflow_lock`'s flock fails the exclusion witness; re-minting the deadline at the second site fails the end-to-end witness *on elapsed time*; giving up after the first probe fails the acquire-on-release witness.
- **One behavioural consequence the filing did not anticipate — recorded, not absorbed.** Polling does **not** preserve the kernel's **handoff fairness**. A blocking `flock` parks the waiter in the kernel, which grants the lock on release before the ex-holder can realistically re-acquire it; a *polling* waiter is asleep at that instant, so a hot re-acquiring holder wins the next round. This changes **ordering, never exclusion** — mutual exclusion is the same kernel `flock` it always was — and starvation is bounded at both ends (the poll ceiling, and the deadline itself). It is **intrinsic to the ratified shape**, not to this implementation: `filelock` — the vehicle the fork evaluated and declined — polls at ten times this interval, and `msvcrt.LK_LOCK` retries at 1 s; no portable primitive offers a kernel-parked wait *with* a timeout. It surfaced as a real regression in **one shipped witness**, `test_rollback_cross_process_lock_prevents_lost_concurrent_append`, whose fixed child timestamp silently **pinned the old ordering** under a **zero** clock-skew tolerance — repaired **order-INDEPENDENTLY** — an order-pinned formulation proved flaky in *both* directions (`flock` specifies no acquisition ordering, so an early stamp needs the child to win and a late one needs the parent to), so the child stamps the pre-existing entry's own timestamp and, if the parent landed first, retries with C-IS-07 §7.6's `WRITER_OWNED_TIMESTAMP`. The position assertion an interim round added was **removed** — it asserted a property `flock` does not provide. The load-bearing survival assertion is unchanged, still discriminates under mutation, and the witness is 20/20 clean on repetition. A **second** shipped witness, `test_ac9d_never_holds_two_journal_locks_at_once`, keyed its flock spy on `op == LOCK_EX` and silently reported a peak of **zero** once the probe became `LOCK_EX | LOCK_NB` — a **pass-on-absence**, fixed by matching the *bit* and by counting only **successful** acquisitions.
- **RATIFIED 2026-08-05 as READING B** — decision 2 (liveness deadline) **YES**, decision 1 (Windows serialization backend) **NO**, decision 3 (vehicle) **HAND-ROLLED EXTENSION of the existing primitive, NOT `filelock`**. Operator `AskUserQuestion` on the fork's §8 (`.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md`). **The build leg owes:** a deadline at **all nine** acquisition sites; one typed **`CrossProcessLockTimeoutError` homed at `harness-core`** (settled by the `ValidatorEscalationGateTimeoutError` precedent — defining it in `harness-runtime` is **illegal**, inverting the IS 0-outbound invariant); the ABBA release-before-blocking handoff **preserved** at all three probe arms; the hold-time contract documented at `write_record_guarded`; the §3(ii) **reconciler import repair**; and ≥1 process-based deadline witness per site-class **including the `_workflow_lock` carrier, UNWITNESSED at HEAD** (codex R9 — the reconciler CAS test proves `os.link`, not `_workflow_lock`), each with a PD-8 mutation probe. **Vehicle rationale as ratified:** seven of the nine sites are exclusive-only and need no shared face; `filelock` is revisited only if decision 1 later goes YES. **Zero spec and plan delta under every reading** — so **no spec leg** and no X-AL-3 exposure. **Status → `open`** (fork §9: status flips for a YES half; `open` is the enum's actionable-now-no-blocker value and this half has no design-substrate gate); the row closes when the build leg merges. Variants **E** and **F** stay recorded as **dominated** so they are not re-proposed.
- **What it is.** Every `CanonicalMemoryStore` write takes `cross_process_scope_lock` over the whole memory **root** (`_write_scope`, `harness-is/src/harness_is/memory_store.py`) — deliberately, because the section `write_record_guarded` wraps re-reads a caller-chosen set of *other* records before writing this one, and a per-file lock cannot exclude a writer of a file the section has not named yet. That lock is a plain **blocking** `flock(LOCK_EX)` with no timeout (`_DirLock.acquire`, `harness-is/src/harness_is/cross_process_ledger_lock.py`). `write_record_guarded` additionally holds it **across a caller-supplied precondition callable**, so the hold time of a root-wide exclusive lock is set by code the store does not control and cannot bound.
- **No live hazard at HEAD.** The only precondition in the tree is `_provenance_unchanged` (`harness-runtime/src/harness_runtime/memory_promotion.py`), bounded at one `read_record` per cited source memory ref — a small N of local file reads, no network, no operator wait, no unbounded loop. This row is registered against a **future** precondition, not a present stall.
- **Why it is worth registering anyway.** The seam is **generic by design** — it "knows nothing about what the precondition tests" — which is exactly what makes the next precondition author unaware that they are holding a host-wide lock. An operator confirmation, a provider round-trip, or a retrieval sweep inside a precondition blocks **every same-host memory writer**: a `harness run` alongside a `harness daemon` is the shared-root case the seam's own SCOPE paragraph documents, and it would block with no deadline and no diagnostic.
- **No convention to copy — which is why this is a scoped arc, not a one-liner.** Counted by direct read at HEAD — **recounted 2026-07-31 at the `B-103` arc: nine, not eight, across four modules, not three** (`journal_workflow_pause_store.cross_process_journal_lock` was created at the `B-97`(a) leg, PR #1171, *after* this row's original count) — this codebase has **nine blocking-`flock` acquisition sites** across **four** modules, reachable through **seven** entry surfaces — and *not one of them* takes a timeout. In `harness-is/src/harness_is/cross_process_ledger_lock.py` (**six** sites): `_DirLock.acquire` `:133` (`LOCK_EX`), the shared engine behind **all four** of that module's entry surfaces (`_dir_lock_for` callers at `:213` scope / `:365` write / `:484` replace / `:572` read); `cross_process_write_lock` `:406` (`LOCK_EX`); `cross_process_replace_lock` `:507` (`LOCK_EX`); `cross_process_read_lock` `:605` (**`LOCK_SH`** — so "they are all `LOCK_EX`" is false); `_acquire_legacy_sidecar_for_writer` `:274` (`LOCK_EX`); `_acquire_legacy_sidecar_if_present` `:330` (`LOCK_EX`/`LOCK_SH`). In `harness-runtime` (**three** sites): `reconciler_pause_resume_substrate._workflow_lock` `:294` (`LOCK_EX`), `protected_result_store._cross_process_lock` `:371` **[superseded — see the post-edit anchor note on this row: `:371` was the pre-#1237 value]** (`LOCK_EX`) *(cite corrected from `:347` at the 2026-08-05 re-check)*, and — **new at the 2026-07-31 recount** — `journal_workflow_pause_store.cross_process_journal_lock` `:489` (`LOCK_EX`), the per-workflow pause-journal append lock promoted to module scope at `B-97`(a); it is a **leaf** (nothing inside its hold takes another cross-process lock), so it adds a site but no ordering hazard. **The `B-45` co-rider text now agrees** — B-45's own 2026-07-31 refresh names this same module as its fourth Windows no-op surface, so both rows enumerate the same four-module primitive set. A deadline means `LOCK_NB` + retry + a new typed exception at each, so putting one on a single site would itself be the inconsistent surface.
- **Close-out steps.** **(1)** Pick the deadline shape — `LOCK_NB` + bounded retry with backoff (portable, and composes with B-45's Windows backend) or a watchdog around the blocking call. **(2)** Choose the timeout's semantics and its exception: a **new** typed error distinct from `MemoryStoreGuardedWriteConflictError`, because a refused commit is a *precondition* outcome and a timeout is a *liveness* one — collapsing them would let a caller retry-loop against a stuck host. **(3)** Apply it consistently across **all nine acquisition sites** above — the acquisition site, not the entry surface, is where a deadline goes. **Two traps** a naive impl falls into. *First*, one entry surface can block at up to **three** distinct sites in sequence (dir lock → legacy sidecar → file lock), so a deadline bolted onto `cross_process_write_lock`'s own `flock` leaves the other two unbounded and the entry surface still has no end-to-end bound. *Second*, the three `LOCK_NB` calls (`:402`, `:498`, `:599`) are contention **probes**, not deadlines, and must not be counted as coverage — on contention each arm releases the directory lock and THEN blocks at `:406`/`:507`/`:605` (release-before-blocking, the ABBA-avoidance handoff to preserve). **(4)** Document the hold-time contract at `write_record_guarded` so a precondition author meets the constraint at the seam rather than in production.
- **Natural co-rider on B-45.** Both arcs traverse the same set of lock primitives — B-45 to give them a Windows backend, this row to give them a deadline — so doing them together costs one traversal instead of two.
- **Council: no.** An implementation-level liveness bound on an already-cleared lock mechanism (C-IS-09 §9.4 defers the mechanism to implementation discretion), with no cross-domain tension to name. Step (2) does carry a real design choice, so it is not mechanical — `advisor()` + out-of-family review at build time is the right weight. Council-eligible only if taken **with** B-45 and the Windows tradeoff makes the deadline semantics non-obvious. ***CONDITION MET AND ADJUDICATED 2026-08-05*** at the B-93+B-45 combined fork filing — the antecedent holds (the rows ARE taken together) and the consequent is **FALSE, probe-resolved**: `msvcrt.LK_NBLCK` gives Windows the same non-blocking-probe shape close-out step (1) prescribes and `LK_LOCK` already carries a fixed ~10 s bound, so the backend decides *exclusion granularity* while the deadline decides *liveness* and neither constrains the other. **No dyad convenes.** The one convening that IS owed: a **Reading-A** selection (Windows backend before a Windows CI witness) owes a dyadic **C3 ⊥ C11** convening — see `B-45`.
- **Post-edit anchor note** *(merge-gate lens 2; the `B-104` precedent — drifted anchors recorded durably on the row, not only in a PR body)*. Three cites in the combined fork filing drifted **before this build leg began**, at #1237's U-RT-150 landing. The anchors recorded here are the **pre-leg main baseline** (`eea1dec9`, the commit this branch forked from) — what the filing's cites *should* have said when written — **not** this branch's head, where the same three sites have moved again under the leg's own edits (`:59` / `:567` / `:617`). Naming the frame explicitly, because a bare line number without one is exactly the drift being corrected: `protected_result_store.py` acquisition site `:371` → `:592`, its `_cross_process_lock` entry `:340` → `:561`, and its `_IS_WINDOWS` flag `:48` → `:53`. The **site itself is exactly as the filing describes** — only the anchors moved — and every other cite in the filing resolved as written. The filing was **deliberately not edited** in this PR: a fork-filing edit riding a code PR is the `DESIGN_IMPL_MIX` condition the codex context guard refuses.

- **Sweep-scope correction** *(merge-gate lens 3, G2)*. The arc's AST sweep of timeout-leak boundaries enumerated **direct** callers of the seven lock entry surfaces — **not** the full exposure set. `admin/inspect.py` and `admin/migrate_audit_sidecar.py` reach locks **transitively** and both needed folds in this same PR, so neither appeared in that enumeration. The honest claim: the **direct-call** set is closed by enumeration, and the two known transitive operator-facing surfaces were fixed separately and witnessed. The full transitive set is **not** enumerated (a call-graph analysis in its own right) and is recorded as residual on `B-112`.

- **Trigger for re-check.** The **second** precondition. The row should be re-read at every new `write_record_guarded` caller, since the first unbounded one converts this from latent to live.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT, all NINE sites re-verified individually, ONE drifted cite corrected above; disposition VERIFIED-DORMANT with step (2) FORK-SHAPED.** Six in `cross_process_ledger_lock.py`: `_DirLock.acquire` `:133` (`LOCK_EX`), `_acquire_legacy_sidecar_for_writer` `:274`, `_acquire_legacy_sidecar_if_present` `:330` (`LOCK_EX`/`LOCK_SH`), `cross_process_write_lock` `:406`, `cross_process_replace_lock` `:507`, `cross_process_read_lock` `:605` (**`LOCK_SH`**). Three in `harness-runtime`: `reconciler_pause_resume_substrate.py:294`, `protected_result_store.py:371` (was `:347`; **`:371` has since been superseded by `:592` at #1237 — see the post-edit anchor note on this row**), `journal_workflow_pause_store.py:489`. **Not one takes a timeout.** The three `LOCK_NB` calls (`:402`, `:498`, `:599`) are still contention PROBES, not deadlines — trap 2 holds — but the fallback mechanics are: on contention each probe arm RELEASES the directory lock first, then blocks at `:406`/`:507`/`:605` respectively (release-before-blocking, the load-bearing ABBA-avoidance handoff a deadline implementation must preserve). The `_dir_lock_for` caller cites `:213`/`:365`/`:484`/`:572` all resolve exactly. **Trigger has NOT fired:** `write_record_guarded` (`memory_store.py:298`, precondition param `:302`) still has exactly **one** PRODUCTION caller in the tree, `_provenance_unchanged` at `memory_promotion.py:907` *(cite corrected from `:911` at the 2026-08-05 fork filing — `:911` is the `write_record_guarded(...)` CALL, `:907` the `def`; the enclosing method is `_commit_record` at `:840`)* (direct TEST callers exist at `test_memory_store.py:523/:539/:564/:618` + `test_memory_store_cross_process_guard.py:168` — and, *added 2026-08-05, the list was not exhaustive* — four protocol-double definitions plus three delegating calls at `test_u_mem_27_activation_boundary.py:273/:342/:391/:629` and `:348/:397/:638`; **none supplies a new precondition**, so they exercise the seam and do not convert the trigger), bounded at one `read_record` per cited source. Step (2) — a *new* typed timeout exception distinct from `MemoryStoreGuardedWriteConflictError`, because a refused commit and a stuck host are different outcomes — remains the one genuine decision; everything else is fully enumerated.
- **Cross-ref.** Surfaced by merge-gate lens 1 on PR #1159 (U-MEM-27 impl leg). Sibling lock-substrate row: **B-45** (Windows backend for the same primitives, whose enumeration this arc's `cross_process_scope_lock` extends to four).
- **FILED 2026-08-05 — combined Class 2 fork with `B-45` at `.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md`** (grounding HEAD `966bc658`). ***RATIFIED the same day as Reading B** — the "awaiting operator ratification; status unchanged (`registered_finding`)" clause below is **superseded**: this row is now **`open`**. Everything from here on is the pre-ratification record.* Four readings — A full combined fix / **B deadline-now + the minimal Windows import repair, serialization deferred (RECOMMENDED)** / C Windows-only (dominated) / D defer both under demand tests **D-0** (a second `write_record_guarded` precondition — this row's own trigger, unchanged) **D-1**..**D-4**; runner-up **D**, and the B↔D margin is genuinely narrow. **Two variants recorded as dominated so they are not re-proposed: F** — a deadline on `cross_process_scope_lock` alone, which trap 1 defeats (one entry surface blocks at up to three sites in sequence, verified at `cross_process_write_lock:363`–`:406`) — and **E**, adopting `filelock` without deciding either half. **COUNCIL — the "only if taken WITH B-45" condition IS met by this filing and is PROBE-RESOLVED: NO convening.** The probe (Python `msvcrt` docs + `filelock` 3.29.4, both accessed 2026-08-05) found the two decisions **ORTHOGONAL** — `msvcrt.LK_NBLCK` gives Windows the same non-blocking-probe shape close-out step (1) prescribes, and `LK_LOCK` already carries a fixed ~10 s bound, so the backend choice cannot make the deadline semantics less expressible; the backend decides EXCLUSION GRANULARITY, the deadline decides LIVENESS, neither constrains the other. One residual coupling named rather than hidden: under a `filelock` vehicle the library's own `Timeout` subordinates step (2)'s exception design — an implementation-vehicle coupling inside one arc, not a cross-domain tension. **Step (2)'s exception home is settled by precedent, not open: `harness-core`** (zero workspace deps, `harness-core/pyproject.toml:9`–`:11`; precedent `ValidatorEscalationGateTimeoutError` at `harness-core/src/harness_core/validator_escalation_errors.py:43`, re-homed for exactly this carrier-home reason at `:1`–`:13`), fallback `harness-is`; defining it in `harness-runtime` is illegal (inverts the IS 0-outbound invariant). **ZERO spec/plan delta under every reading** — C-IS-09 §9.4 (`Spec_Information_Substrate_v1.md:900`) and C-IS-07 §7.4 (`:756`) both defer the mechanism; the IS spec contains ZERO `timeout` / `deadline` / `platform` / `cross-process` occurrences.

### B-94 · Derived-index auto-refresh rebuild overlap race — two concurrent rebuilds can leave the OLDER snapshot last and reading fresh *(surfaced by out-of-family Codex R5 on the B-93 gate-log arc, 2026-07-30; classified PRE-EXISTING by git-show verification against fc40fb2b)*

- **What it is.** `DerivedRetrievalIndexStore.rebuild` computes its entry set from the filesystem and *then* appends it, holding **no lock across the two** (`_append_jsonl` is a bare `open("ab")` + write). Two overlapping rebuilds interleave as: X reads the record set → a commit lands → Y reads the **newer** set and appends it → X appends its **older** set **last**. `read_current` resolves an untokenized ledger by **position** (last rebuild wins), so it returns X's stale snapshot with `stale=False`: a committed record missing from an index reported as current.
- **Pre-existing, and NOT widened by the B-93 arc — verified by `git show fc40fb2b`.** Pre-branch `rebuild` (`memory_retrieval_index.py:177-200`) computed entries at `:180` and appended at `:199` with no lock; `_append_jsonl` (`:571-574`) took none either; `read_current` (`:210-227`) was purely positional last-wins; and `AutoRefreshingDerivedRetrievalIndexStore.read_current` (`automatic_memory.py:110-114`) rebuilt on missing/stale with no coordination. The same interleaving produces the same wrong outcome on the pre-branch code. An all-untokenized ledger takes the identical positional path before and after the arc.
- **What the B-93 arc covered, and why this sits outside it.** That arc added a commit-ordering protocol — `commit_seq` stamped inside the memory store's write hold, plus a `rebuilt_coverage` event binding a rebuild to the commit it answers — which orders **tokenized** rebuilds: the hook path, where the caller has an invalidation event to quote. The **production auto-refresh path is untokenized by design**: it rebuilds because a read found the index missing or stale, not in response to a specific write, so it has no token to pass and correctly falls to the legacy positional rule. The protocol does not reach this race.
- **Candidate fix shape** *(Codex R5's direction, recorded not built)*. Derive a coverage token on the auto-refresh path from **the ledger length captured at the read that decided to rebuild**. That read already walks the whole ledger, so its byte length at that moment is free — and it is exactly the monotonic quantity `_next_commit_seq` uses. It names *"the state I found stale"*, which is the honest under-claiming attribution the rebuild contract already requires. The subclass would capture it in its `except` branch and pass it to `rebuild(commit_seq=...)`; the existing ordering rules then reject the older overlapping snapshot with no change to `read_current`.
- **Open questions for build time.** **(1)** The capture must happen *inside* the failed read or it re-races — `read_current` returns nothing on the raise, so the token needs a carrier on the exception or a private read variant. **(2)** Whether the fix belongs at the `harness-runtime` subclass (where auto-refresh policy lives) or the IS-axis base (where token semantics live) — the IS axis has **zero outbound cross-axis edges**, so a base-class change must not import runtime concerns. **(3)** Whether a losing rebuild should be **pruned** rather than merely ignored, since the ledger still grows by a full snapshot per overlap.
- **Not urgent.** The outcome is a **stale-but-valid** index — a retrieval base that under-serves, never a wrong record — the path self-corrects at the next canonical write's stale marker, and single-process local development (the default posture) does not overlap rebuilds at all.
- **Council: no.** A bounded concurrency fix inside an already-cleared mechanism, with the ordering protocol it would extend already built and witnessed. No cross-domain tension: the question is *where the token is captured*, not whether the index should be ordered. Open question (2) is a real placement decision, so `advisor()` + out-of-family review at build time is the right weight.
- **Cross-ref.** Surfaced by Codex R5 on the B-93 gate-log arc. Sibling: **B-93** (no deadline on the same substrate's blocking locks) — both re-check on the same trigger, a second concurrent memory consumer becoming routine.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT and now documented in code; disposition FORK-SHAPED (low urgency).** The `memory_retrieval_index.py` module docstring names **both** B-95 and B-94 explicitly at `:19-23` (*"Rebuilds that pass no `commit_seq` — which includes the production auto-refresh path, untokenized by design — remain positional last-wins … See register row B-94."*). `_append_jsonl` still takes no lock; `read_current` still resolves untokenized ledgers positionally; and the production auto-refresh path is confirmed untokenized — `AutoRefreshingDerivedRetrievalIndexStore.read_current` calls `self.rebuild(indexed_at=datetime.now(UTC))` with **no `commit_seq`** (`harness-runtime/src/harness_runtime/automatic_memory.py:110-114`). All three build-time decisions stand: where the token is captured, whether the fix lands at the `harness-runtime` subclass or the IS-axis base (a real axis-placement call under IS's zero-outbound-edge invariant), and prune-vs-ignore for losing rebuilds.

### B-95 · Mixed-version ordering bound — a live PRE-B-93 **writer**'s untokenized stale markers cannot be ordered against a covered rebuild *(surfaced by out-of-family Codex R6 on the B-93 gate-log arc, 2026-07-30; REGISTER-AND-HOLD under the arc's own stop rule)* *(**CLOSED 2026-08-05** — a ratified accepted bound; the re-open trigger is preserved verbatim)*

- **CLOSED 2026-08-05 by operator ratification, as a RATIFIED ACCEPTED BOUND** — not built, not foreclosed by supersession, but **accepted with its limit documented in shipped source**. This resolves the row's own **flagged-not-flipped** close candidacy: disposition **(3)** (accept and document) is **fully executed at HEAD** — the bound is stated in the `memory_retrieval_index` module docstring at `:19`–`:20` and on `commit_seq` itself at `memory_store.py:154`–`:163`, both citing this row — and all three preconditions were re-verified **counterfactual** by direct read at the 2026-08-05 grounding pass. **The re-open trigger is preserved VERBATIM and survives the close:** *"Re-open when a genuine mixed-version deployment is planned, or when the first production `derived_index_hook` consumer is wired — whichever comes first, since either alone makes a precondition true."* Closure does **not** retire that trigger; it records that no work is owed while both preconditions stay counterfactual. **Trigger-carrier note** *(out-of-family review round 3 [P2], recorded rather than used to reverse the operator)*: the reviewer is right that `--open` no longer surfaces this row, and that the status enum names only two closure senses while this is a **third — a ratified accepted bound**. The operator's answer stands; the trigger is kept monitorable by naming its carriers: **(1) shipped source** — the bound and this row are cited at `memory_retrieval_index.py:19`–`:20` and `memory_store.py:154`–`:163`, so any arc on that surface meets the bound where it lives; **(2) two live sibling rows** over the same mixed-version surface stay in the open inventory and cross-reference this one — **`B-94`** (the untokenized auto-refresh rebuild path) and **`B-45`** (the Windows same-value `commit_seq` bound); **(3)** reopening is a one-field edit, and the information required has been kept deliberately intact. Dispositions **(1)** and **(2)** stay rejected on their recorded grounds, and the **hold rationale stands** — this was the third consecutive self-inflicted `read_current` finding and the arc's pre-declared arms-race stop rule fired exactly as written.
- **What it is.** During a mixed-version window a **new** hook captures snapshot A; a **pre-B-93 writer** commits B and appends an **untokenized** stale marker; the hook then appends A's rebuild pair last. In `read_current` the untokenized marker has no `commit_seq`, so it is excluded from `max_stale_seq`, and accepting the covered rebuild resets the positional `stale_after_current` flag — the A-only index reads **fresh** with B missing. Codex reproduced it deterministically.
- **Precondition triple — each verified by direct read at HEAD, each false today.** **(a)** A live pre-B-93 **writer**, not merely an old *reader* (whose parse compatibility the R3 fix already covers): at HEAD `commit_seq` is a **required** field on `DerivedIndexInvalidation`, so every B-93+ writer stamps every marker, and an untokenized marker can only originate from an older writer process. **(b)** A **wired** `derived_index_hook` — re-confirmed **zero production consumers** at HEAD; the only wirings are `harness-is/tests/test_memory_store.py` and `harness-is/tests/test_memory_redaction.py`. **(c)** An overlapping write + hook-rebuild interleaving.
- **Sharpening found while verifying.** Historical untokenized lines written *before* an upgrade are **benign** — a later rebuild reads the whole filesystem and genuinely covers them. Only a **concurrent** untokenized write, interleaved with an in-flight rebuild, is harmful. The bound is about the live window, not about legacy ledger content.
- **The information-theoretic limit, precisely.** A legacy stale marker carries **no commit token by definition**, so no read-side rule can retroactively order it against a covered rebuild — the information the comparison needs was never written. This is not a missing check; it is **absent data**. Hence only three choices exist:
  - **(1) Serialize notifications during a detected mixed window.** Reintroduces exactly the callback-deadlock surface the arc's first commit removed (a hook holding the notify turn while its inner thread's write waits behind it) — trading a latent ordering gap for a latent hang. Rejected unless the window can be *detected* and the serialization *scoped* to it.
  - **(2) Version-fence the ledger.** Have B-93+ writers refuse to share a ledger they can detect an older writer is also appending to (a writer-version marker line, or a fence file beside the ledger), converting a silent misorder into a **loud refusal** during the upgrade window. Costs a new failure mode operators must understand, plus a decision about what a fenced writer should *do*.
  - **(3) Accept the bound and document it.** **Taken here** — all three preconditions are counterfactual at HEAD.
- **HOLD rationale.** This is the **third consecutive self-inflicted `read_current` finding** (R4 caused by R3's carrier; R5's [P2] by R3/R4's two-pass read; this by R2's ordering protocol). That is the arms-race stop rule the arc pre-declared at round 4, firing exactly as written. **No further carrier change is made.**
- **Documented in code, not witnessed.** The bound is stated in the `memory_retrieval_index` module docstring and on `DerivedIndexInvalidation.commit_seq`, both citing this row. The R3 mixed-version witness was **re-scoped** to say it proves reader *parse* compatibility only and explicitly disclaims any ordering guarantee — deliberately no witness for the gap itself, since it is registered rather than fixed.
- **Re-open when** a genuine mixed-version deployment is planned, **or** the first production `derived_index_hook` consumer is wired — whichever comes first, since either alone makes a precondition true.
- **Council: not for the bound itself** — accepting a documented limit needs no deliberation. **If** direction (2) is ever taken it becomes council-eligible: refusing to write during an upgrade window is an operator-visible availability tradeoff (C11 operator-loop / local-deployment against C3 state-and-persistence integrity), which *is* a nameable cross-domain tension.
- **Cross-ref.** Sibling ordering-scope row **B-94** (the untokenized auto-refresh path, pre-existing); substrate sibling **B-93**. All three re-check on overlapping triggers around concurrent memory consumers.
- **2026-08-05 grounding re-check at HEAD `43d4dda5` — premise INTACT, all three preconditions re-verified COUNTERFACTUAL; disposition VERIFIED-DORMANT.** **(a)** `commit_seq: int` is a **required** field (no default) on `DerivedIndexInvalidation` at `harness-is/src/harness_is/memory_store.py:138`, so every B-93+ writer stamps every marker. **(b)** **Zero production `derived_index_hook` consumers** — the only wirings repo-wide are `harness-is/tests/test_memory_store.py:611` and `harness-is/tests/test_memory_redaction.py:68`; the production surface is only the ctor param (`memory_store.py:215`) and the notify site (`:284-285`). **(c)** A concurrent overlap is still required and absent.
- **📌 Reclassification NOTE (2026-08-05) — flagged, NOT flipped — `[SUPERSEDED 2026-08-05]`, the candidacy it flagged was ANSWERED the same day and this row is now `closed`; kept only as the record of the pass that deferred the call.** The chosen disposition **(3) accept and document** is **fully executed at HEAD**: the bound is stated in the `memory_retrieval_index` module docstring (`:19-20`) and on `commit_seq` itself (`memory_store.py:154-163`), both citing this row. This row is therefore not open work but a **ratified accepted bound with a documented re-open trigger**, and is a **CLOSE candidate at the next operator ratification batch**. The status is deliberately left unchanged here — the accepted bound plus its re-open trigger make closure an operator call, not a sweep's.

### B-96 · The B-77 first-observation grace is bounded by SWEEP COUNT, not elapsed time -- a short or failing run's own shutdown sweep can reclaim a crash-recovered entry milliseconds after its bootstrap sweep first observed it *(surfaced at the B-77 impl arc, out-of-family Codex round 2 [P2], 2026-07-30)* *(**RATIFIED 2026-08-05 as READING C, ceiling sub-decision C-2** — the grace term alone, carrier `(C-i)`; **SPEC LEG LANDED 2026-08-05 (Runtime v1.111 + plan v2.58 U-RT-150); IMPL LEG LANDED 2026-08-05 (U-RT-150) — CLOSED**)*

- **IMPL LEG LANDED 2026-08-05 — `U-RT-150` built; this row, the `B-77` residual and `B-74` all CLOSE here.** `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py`'s sweep-COUNT grace is REPLACED by the ratified CONJUNCTIVE, publication-bounded, DURABLE elapsed-time rule: reclaim requires past-TTL filesystem age **AND** past-TTL elapsed time since a durably recorded `first_observed_at`, with **no third path** and **no absolute ceiling**. The carrier is `(C-i)` as ratified — a dedicated `gc-observations.json` in the store root holding the CLOSED two-member set keyed on the candidate filename over BOTH sweep classes, published by temp-write + `fsync` + `os.replace` + directory `fsync` (never the write-once no-replace primitive, never unlink-then-recreate), glob-disjoint and **not dot-leading**, with a publication temporary that does not reuse the `.tmp-` payload prefix. Both falsifiability surfaces landed: the oldest-resident-candidate age as a FIELD of every sweep emission plus the which-term-fired-last discriminator, and a read-only, sweep-free `harness-inspect` store row reporting the record state THREE-WAY off the **config-derived** store root. All FIFTEEN ACs green with PD-8 probes, including AC #13's re-grounding pass (no witness weakened or deleted; the `B-74` pin RE-PINNED as a POSITIVE live-entry-survives witness) and AC #10 / #14(i) driven as REAL successive one-shot subprocess invocations. Registered row `B-110` disposition (c) was **not** taken — see that row.
- **SPEC LEG LANDED 2026-08-05 (PR #1235) — `Spec_Harness_Runtime_v1.md` **v1.110 → v1.111**, NEW **§14.8.11.1** + `Implementation_Plan_Harness_Runtime_v2_58.md` **NEW unit U-RT-150**.** §14.8.11.1 carries the council record's §7.1 **TWELVE** conditions as contract terms 1–12, plus the observation record's carrier discipline, an explicit **does-NOT-acquire** list and the §7.2 tier-conditional counter-anchor; §14.8.11's bounded-retention bullet and its deferred-to-discretion paragraph each gain a **strict APPEND** (three amendment sites total, **no existing sentence reworded, retracted or relocated**). The plan absorbs it as **ONE NEW unit** with **FIFTEEN** ACs — deliberately **NOT** an amendment of the landed **U-RT-145**, whose AC #7 remains true and unchanged under v1.111 (the `B-97`(a) → **U-RT-149** precedent). **The FIVE-of-twelve EXPANSION FLAG** — conditions **#5, #6, #8, #11, #12** did not exist in the fork's §8 ask and ride the ratification only as *conditions of the selected form* — is surfaced in the spec's clearance marker per council record §7.2, **with a Class 2 route named** if the operator judges they owe a fresh decision. **NO** new `C-RT-*` number; **NO** CXA delta (aggregate frozen at **111**, determined not assumed); **NO** OD delta (`C-OD-05` §5.1 roster **15 → 15**); **NO** `harness-*/src|tests` edit. **Status `design_substrate_gated` → `open`** — per this register's OWN status enum (`.harness/forward-register.yaml` lines 31–39: only **ratified + APPLIED** spec deltas *plus* the corresponding Phase-6 plan deltas open the row for code, and **both landed here**). This does **not** contradict the fork §11.5, which contrasts `design_substrate_gated` against **`closed`** at the *ratification* leg — `open` is not `closed`, and **`B-96`, the `B-77` residual and `B-74` still flip to `closed` only when the IMPL leg merges**. *(Corrected at out-of-family review round 4 [P2]; this leg's first draft held the row gated.)* **One bounded residual was surfaced against this leg's own text and REGISTERED rather than absorbed: `B-110`** — the record cannot distinguish generations of a **reused** temporary candidate name, because its ratified content set is closed at two members. The conjunctive rule still gates the recreated file by its **own timestamp**, but **term 2's PUBLICATION bound does not extend to a reused temporary name**; only the *additional* grace degrades, for crash orphans alone.
- **RATIFIED 2026-08-05 as READING C, ceiling sub-decision C-2 — the GRACE TERM ALONE, with NO absolute mtime-keyed reclaim ceiling.** Operator `AskUserQuestion` on the fork's §8 (`.harness/class_2_fork_b96_gc_grace_elapsed_time_bound.md`, FILED at PR #1179); the **held** C-1/C-2 sub-decision was resolved by the dyadic convening recorded at `.harness/council-b96-grace-ceiling-2026-08-01.md` §7 — **verdict C-2, unanimous** across C3 / C10 / C7 from non-overlapping grounds, merged at PR #1183. **Ratified form:** `first_observed_at` is **wall-clock**; carrier **(C-i)**, a dedicated **candidate-filename + timestamp** sidecar published by temp-write + `fsync` + `os.replace` + directory `fsync` — its content is council condition #5's **closed two-member set `{candidate filename, first_observed_at}`, keyed on the CANDIDATE FILENAME over BOTH sweep classes, `*.entry` AND `.tmp-*`** *(stated as candidate-filename and never "digest": a digest-only key leaves condition #1's second conjunct permanently unsatisfiable for `.tmp-*` crash orphans — corrected at out-of-family review round 3 [P2])* (explicitly **not** the write-once `O_EXCL`/`os.link` primitive); record-absence reads as **no observation**; retention **typically** up to **2×TTL** plus **up to two** sweep-trigger intervals — **conditional, never an unconditional bound** (council condition #7): the trigger interval is **unbounded in the one-shot process shape** (pre-existing at HEAD), and the figure holds only while the observation record is **present and readable**. **No unconditional "bounded by N × TTL" claim is available under either form**, and the spec leg must not encode one. **Reading B (per-process elapsed time) is RETIRED explicitly** — no impl leg may build it as written; **Reading A was not selected and is not partially adopted.** **Status: `open`** — was `design_substrate_gated`, and still **NOT `closed`** (fork §9, out-of-family round 3 [P1]). **SPEC LEG NOW LANDED** — the Runtime §14.8.11 amendment carrying the council record's **twelve** conditions at its §7.1, with §7.2's expansion flag surfaced in the clearance marker, shipped 2026-08-05 as **§14.8.11.1** at Runtime spec **v1.111** with plan **v2.58 / U-RT-150** (see the SPEC LEG LANDED bullet above). **Only the IMPL leg remains outstanding**, which is why the row is `open` (actionable, no blocker) rather than `closed` — `closed` would drop that live work out of the open-work inventory. **Spec and impl do NOT land together.** **CXA: no delta owed** — determined by the council record's adversarial pass, not assumed. `B-96`, the `B-77` residual and `B-74` all flip to `closed` **only when the impl leg merges**. **Anchor refreshed at this leg** by direct read at HEAD `4815ebef`: the bounded-retention bullet is **`:4926` at Runtime v1.110** (heading `:4915`) — the council record's own *`:4909` at v1.109* prescription was already stale, v1.110's `B-104` Component 1 insertions having shifted the file; the bullet **text** is unchanged across v1.106→v1.110.
- **What it is.** `gc_sweep`'s B-77 first-observation grace refuses to reclaim an entry the store has not already seen past TTL at a PRIOR sweep of the same root. That closes B-77's named defect — a crash-recovered entry carrying only the pre-commit mtime stamp is never reclaimed by the sweep that FIRST sees it, i.e. never by the recovering process's bootstrap sweep. The residue is the grace's **practical duration**: every `run()`/`resume()` sweeps **at least** twice by construction — once at `bootstrap/stage_4_od.py:123` and once, unconditionally, at `shutdown.py` step 5b — plus a **third trigger the original count missed** *(corrected 2026-07-31)*: `_maybe_opportunistic_gc_sweep` (`protected_result_store.py:882`) fires from inside `write_once` at most once per `_OPPORTUNISTIC_GC_INTERVAL_SECONDS` (300s, `:72`), so a long-lived process also sweeps on **write cadence**. "Twice by construction" was a **floor**, not the count — which *sharpens* the finding (a third trigger can only shorten the interval) rather than weakening it. So the gap between first and second observation is the run's own duration, milliseconds for a short or immediately-failing run. A genuinely-live, crash-recovered entry therefore survives only to that same run's shutdown, not for any wall-clock interval a repair flow can rely on.
- **Current state — SUPERSEDED 2026-08-05; the block below is the pre-ratification record.** *(Current state is the two bullets at the top of this block: **Reading C, ceiling C-2**, `status: open` (was `design_substrate_gated`), **spec leg LANDED 2026-08-05 (Runtime v1.111 §14.8.11.1 + plan v2.58 U-RT-150); IMPL leg owed**. What follows described the row while `B-96` was unanswered, and is kept as history.)* Registered, not built. The sweep-COUNT reading is exactly what `B-77`'s ratified close_out specifies (verbatim: "should not reclaim an entry it has not observed in a PRIOR sweep (a grace period) — bounded to one sweep interval"), and it landed as ratified. It was deliberately NOT redesigned mid-arc: `B-77`'s own row records three prior wrong mechanism claims on this same file, and a fourth unratified attempt is precisely the non-convergent-adversarial-hardening pattern this workspace stops on. The practical bound is stated honestly in `gc_sweep`'s docstring rather than overclaimed. Reachability is the same narrow floor `B-74`/`B-77` share: a deployment `ttl_seconds` short enough that the crash window alone tips a live entry past it (the factory default is 86400s, ~7 orders of magnitude above), AND a crash landing strictly between `os.link` and the post-commit refresh, AND a recovering run short enough that its shutdown sweep fires before any repair read.
- **Close-out steps** *(the pre-ratification recipe — RESOLVED 2026-08-05 as Reading C with ceiling C-2; the impl leg builds exactly this, under the ratified form tabled at the fork's §11.3)*. Replace the sweep-COUNT grace with an elapsed-TIME one, using the first observation's wall-clock as the age authority `B-77`'s close_out actually names ("an age authority that cannot predate publication by construction") — record `first_observed_at` alongside the filename in `_root_observed_expired` and reclaim only when BOTH the mtime-derived age AND `now - first_observed_at` exceed `ttl_seconds`. The tradeoff that makes this ratification-owned rather than mechanical: the conservative rule lengthens worst-case retention of a genuine crash orphan by up to one full TTL (a day at the factory default), against the spec's own bounded-retention term (Runtime v1.108 §14.8.11 — *version label refreshed 2026-07-31; bullet text unchanged across v1.106→v1.108* — "a signing outage must not grow an unbounded store of sensitive payloads"). Do not narrow `ttl_seconds` itself (`B-74`'s declined option). Witness by driving a full bootstrap-then-shutdown pair against a crash-window entry.
- **Council: ⚖️ conditional** — the mechanism is mechanical, but the retention bound it picks is a nameable cross-domain tension (C3 state-and-persistence integrity wanting the conservative age authority ⊥ C7/C8 wanting the bounded-retention floor on sensitive payloads); convene if the elapsed-time variant is taken.
- **Cross-ref.** `B-77` (closed — the grace itself); `B-74` (open — the filesystem-granularity dimension of the same mtime-as-age-authority question). All three re-check together.

### B-97 · The durable pause journal carries no tenant binding *(surfaced at the B-69 council convening as residual R-3, routed OUT of that arc, 2026-07-30; half (b), cross-process append serialization, CLOSED 2026-07-31; half (a), the tenant binding, RATIFIED + SPEC-APPLIED 2026-07-31 as Reading A — per-tenant path segregation — with the impl leg owed)*

- **Half (b) — CLOSED 2026-07-31 (the `B-97`(b) arc).** `JournalWorkflowPauseStore._append` now runs its **entire** body — the `is_new_file` / `needs_leading_newline` / append TOCTOU triple, the write, the `fsync`, and the torn-append self-heal — under `_cross_process_append_lock`: a hand-rolled `fcntl.flock` on a dedicated **per-workflow** `<journal>.lock` file, mirroring the primitive already shipped at `protected_result_store._cross_process_lock`. The **read path deliberately takes no lock**, which is what keeps Runtime spec v1.107 §30's `empty-journal` INDETERMINATE clause exact **as written** — and is why the arc owed **zero spec text**.
- **Granularity is the grounded part.** The house `harness_is.cross_process_ledger_lock.cross_process_scope_lock` (one lock per directory TREE) was implemented **first** and **rejected at out-of-family review round 2 [P2]**: a directory-wide lock makes one workflow's append wait behind **another** workflow's, which contradicts this store's own isolation property (per-workflow files exist so one workflow cannot block another) and — because reads are unlocked — widens the window in which a read of the *queued* workflow reports the `absent` cause that §30 attributes as **PERMANENT**. The per-workflow lock leaves that pre-existing capture-not-yet-landed race exactly the size it already was.
- **Two pre-existing crash windows closed on the way past.** Out-of-family review round 1 **[P1]**: a process that creates the journal directory and dies before writing leaves the dirent **present but not durable**, and a parent-fsync keyed on `dir_is_new` alone then skips it **forever** — a capture returns successfully while the whole `pause-journal` directory stays losable. Round 1 re-keyed it on `is_new_file or dir_is_new`; the **pre-merge 3-lens gate found that form narrows but does not close** the window — a writer whose `path.open("a")` creates the journal file and then dies before the fsync leaves **both** flags False for every successor, so *"a journal file exists ⇒ some earlier writer already fsynced"* is false for that interleaving.
- **Shipped form: both directory fsyncs are UNCONDITIONAL.** The parent (`STATE_LEDGER`) fsync runs after `mkdir` and **before the lock**, so it precedes any statement that could create a journal file; the journal-dir fsync runs after **every** append. True by construction, no interleaving exception — and it **deletes both flags** rather than adding a third. The journal-dir level carried the identical defect one level down (create file, write, die before fsync, every successor reads `is_new_file=False`); that one is **purely pre-existing**, byte-identical to the pre-`B-97` code, and is closed here rather than left as a known twin of the defect being fixed beside it. Cost: two best-effort directory fsyncs on a path that already `fsync`s the record, for a human-latency event.
- **Spec refresh owed at the half-(a) leg.** Two Runtime spec v1.107 §30 sentences go **stale-as-described** once half (b) lands: `:3313` term 5 (*"makes no claim about cross-process write-write serialization of the journal, which remains unguarded"*) and `:3319` (*"cross-process append serialization is explicitly unresolved (registered at `B-97`)"*). **Neither is a Class-1 halt** — both sentences' asserted *properties* still hold (term 5's supersession-window claim is untouched; `:3319`'s `empty-journal` indeterminacy never depended on append serialization); only their parenthetical descriptions of the store are now wrong. The half-(a) leg touches §30 anyway for the sixth cause member, so it carries this refresh.
- **Forward note — blocking `flock` on the event loop** *(out-of-family [P2], pre-merge gate)*. `capture()` is called synchronously from the async `capture_pause_snapshot`, so the `flock` blocks the loop. Precedented — the sibling protected result store grew `resolve_result_ref_off_loop` for exactly this — **deliberately not built in this arc**. **SPLIT to its own row `B-103` at the half-(a) spec leg, 2026-07-31**, per this row's own leg-2 obligation: parking it here would have lost it from `--open` the moment this row closes.
- **Sharpening recorded honestly.** The original finding framed (b) as *interleaved / torn records*. An empirical probe at the arc found that half **not reachable at the shipped record shape**: `_append` emits each record in exactly **one** `write()` syscall (measured on the real `FileIO` → `BufferedWriter` → `TextIOWrapper` stack at 60 KB, at 4 MB, and on the leading-newline self-heal path, whose 1-byte newline is coalesced into the same flush), and `O_APPEND` makes a single write atomic on a local POSIX filesystem — a 2×12 concurrent-append run at 12 KB / 60 KB / 500 KB / 4 MB produced **zero** malformed records with the lock **removed**. What the lock genuinely buys: cross-process mutual exclusion **at all**; independence from an **unspecified** platform guarantee (`O_APPEND` atomicity does not hold on NFS/SMB, and the resolved `STATE_LEDGER` dir is operator-configured); and **soundness** for the self-heal's documented single-writer-only reasoning.
- **Half (a) — CLOSED 2026-07-31 (PR #1171): RATIFIED, SPEC-APPLIED at Runtime v1.108, IMPL LANDED at U-RT-149.** *(The bullets below preserve the finding as filed; this bullet records how it was answered. The row's status moved `registered_finding` → `open` → `closed`; the conjunctive closure criterion was met in full, not in part.)* **Gate 1 (operator, 2026-07-31): READING A — per-tenant PATH SEGREGATION**, with a contractually injective **CONSTANT** encoding, the **record shape UNCHANGED**, the five-member `PauseJournalReadCause` vocabulary **staying at five**, a **MANDATORY quiesced cutover**, and the `(workflow_id, run_id)` rider **NOT bundled**. **Gate 2 (operator, 2026-07-31), after the C3 ⊥ C11 dyadic:** **(3a) abandon-by-default CONFIRMED**, and **§5.3.1 = (i)+(iii) with (iii) a PRECONDITION of (3a)** — both as the council recommended. **The four decisions enumerated below are answered as:** (1) the **filename**, under a TOTAL injective encoding closing **all three** hazards (branch / delimiter / field-boundary), realized on the in-house length-prefixed canonical-segment shape as a module constant with **no configuration surface**, delegating to the **Runtime-local** `normalize_tenant_scope` and explicitly **not** to OD's; (2) **DISSOLVED, not discharged** — a wrong-tenant read resolves the caller's **own** path and returns the ordinary `absent`, which under segregation is the *truth*, so **no sixth cause member is minted** and decision (2) as posed below is superseded; (3) **(3a) abandon by default** with the failure-mode asymmetry stated as spec text, plus **(3b)** operator-declared relocation under four mechanical terms; (4) **answered NO explicitly.** **Applied at** `Spec_Harness_Runtime_v1.md` **v1.108** (§14.14.8 keying + encoding + migration contract + orphan statement + named silence; §14.14.9.1 parallel keying; §30's `absent` row tenant-scoped across three states; §13.4 rows; **NEW §13.7** enumeration surface) and `Implementation_Plan_Harness_Runtime_v2_56.md` **NEW U-RT-149**. **CP / OD / IS / AS and CXA UNCHANGED**, each determined at the leg rather than assumed — the arc is wholly intra-`harness-runtime`.
- **Half (a) — the finding as filed.** `Spec_Harness_Runtime_v1.md` §14.8.11 (`:4804` heading, `:4809` tenant-composite key, `:4812` tenant-bound lookup — **cites refreshed at this arc**; the prior `:4636`/`:4639` pair was stale by ~168 lines after v1.107) commits a **full-strength tenant-composite key + a typed cross-tenant refusal** for comparable durable state. `JournalWorkflowPauseStore` (§14.14.8) is **`workflow_id`-keyed, one file per workflow** — and now carries recovered branch outputs. **PRE-EXISTING and explicitly NOT `B-69`'s**: `api.resume()` reads the same key today, and the `B-69` accessor adds **zero** tenant exposure.
- **Why (a) is fork-shaped, not an impl task.** It changes the store's **keying** — capture-side and hash-adjacent (the `B-73`/`B-80` split-out discipline that kept it out of `B-69` in the first place). The chain mirrors `B-65`'s: **Class 2 fork doc → operator ratification → spec leg → impl leg.** Four decisions the fork must put to the operator, none of which an impl leg may settle silently:
  1. **Key composition** — does the tenant scope enter the sha256 **filename** (opaque, but makes an existing journal unaddressable) or ride **in-record** alongside `workflow_id` (readable, but leaves the path tenant-blind)?
  2. **Refusal semantics** — a cross-tenant read must be **REFUSED TYPED** per §14.8.11 `:4812`, which needs a **sixth** cause in the deliberately **closed** five-member `PauseJournalReadCause` vocabulary (Runtime spec §30). Widening a closed vocabulary *is* the spec question.
  3. **Migration** — an existing untenanted journal must be readable or deliberately abandoned, against §14.14.8's **append-only / never-truncated** substrate invariant, which forbids rewriting in place.
  4. **The `(workflow_id, run_id)` rider** — whether the same arc should carry run-instance distinctness, since both are keying changes and a second keying arc would pay the migration cost twice.
- **Discriminator worth preserving.** The two failure modes are **distinct**, and only (b) was a concurrency question. *Sequential interleaving* — one writer at a time, both records well-formed — is the **supersession** problem `B-69`'s staleness precondition already fences, and it needs no concurrency at all: *"it is not writing at the same time; it is writing in between."*
- **Council: ⚖️ conditional** — (a) is C3's surface (state/persistence keying) with a nameable C3 ⊥ C11 tension if a tenant key adds local-deployment config burden. (b) was mechanical and is closed, so the prior *"convene only if (a) is taken without (b)"* condition is now **unconditionally met**: opening (a) means convening.
- **Cross-ref.** `B-69` (closed — the arc that surfaced it and deliberately excluded it); Runtime spec §14.8.11 (the shape to mirror); §14.14.8 (the store); **`B-102`** — (b)'s lock **shrinks but does not close** the window that row names.

### B-98 · Gate description / question text is absent from every durable pause carrier *(surfaced at the B-69 council convening, SEAM 3, routed OUT of that arc, 2026-07-30; Class 2 fork FILED 2026-08-01 at PR #1180; RATIFIED 2026-08-01 as READING C — DEFER with the four-disjunct falsifiable demand test D-0…D-3, D-0 dominant; NO carrier change, NO spec leg, NO impl leg)*

- **RE-AFFIRMED 2026-08-05 in the operator ratification batch: Reading C (DEFER) stands, unchanged, D-0 dominant.** No new work is owed — the substantive ratification ran on 2026-08-01 and its **ratification leg** landed at **PR #1182** (the fork's `§11 RATIFICATION` addendum). **PR #1182 carried no `B-98` spec-side component** — Reading C owes **no** spec leg and **no** impl leg; the Runtime v1.110 delta in that PR belongs to **`B-104`**, a different row and a different ratified reading — the two shared a PR, not a scope. *(Attribution corrected at out-of-family review round 9 [P2].)* The row's `pr:` pointer, which still read `#pending` for that leg, is **repaired** here to name #1182. Status stays `registered_finding` with the four-disjunct demand test as the falsifiable re-open trigger.
- **RATIFIED 2026-08-01 as READING C — DEFER, with NO CARRIER CHANGE.** Operator `AskUserQuestion`; recorded verbatim-in-substance at the fork's `## §11 RATIFICATION` addendum (`.harness/class_2_fork_b98_gate_description_durable_carrier.md`, FILED at PR #1180). **The row stays `registered_finding`.** No field is added to `PauseSnapshot`, to `PreDispatchGateOwningBranchResumeState`, or to any other durable pause carrier; the hash-disposition question is not reached; the read-side projection is untouched. **Runner-up A-1 was NOT selected and is NOT partially adopted** — landing "just the cheap half" of A-1 under cover of a deferral would be the silent-absorption failure mode. **No spec leg and no impl leg are owed** (fork §9, column `C`: *"none owed — no design extension"*), and **no plan cite contradicts the deferral** — `B-98` is cited at **no** implementation plan and at **no** acceptance criterion.
- **THE REOPENING TRIGGERS — a FALSIFIABLE four-disjunct demand test, any one disjunct fires.** This *replaces* the row's prior unfalsifiable gate (*"open only if the operator loop demands it"*). **D-0 (DOMINANT) — the HITL-pause class becomes production-reachable:** the **FM-2 webhook-config arc** lands (operator-supplied `WebhookConfig`, deferred at `webhook_delivery_composer_factory.py:17`–`:22`), so a durable `HITL_PENDING` pause can occur outside tests at all. Until D-0 this row is a finding about a **dormant class**, which is why D-0 dominates. **D-1 — a description exists that the webhook does NOT already deliver** to the resuming operator (round-2 tightened form): either **(a)** a description surface lands that is **not** projected into `WebhookPayload.payload_body` (`webhook_brief_adapter.py:76`–`:84`) — e.g. `HITLPlacement` (`hitl_placement.py:176`) gains an author-written text field — **or (b)** the resumer ceases to be the webhook recipient (the webhook stops being the delivery channel, or a supported flow resumes a pause through an operator who never received it, i.e. the workspace advances past `solo-developer` on the committed bridging arc, root `CLAUDE.md` §10.2). **Explicitly NOT a D-1 on its own:** `HITLEscalationBrief.escalation_reason` (`validator_framework_types.py:150`) acquiring a non-literal production value — it is projected into every webhook at `webhook_brief_adapter.py:77`, so it fires D-1 only in conjunction with (b). **D-2 — the channel opens anyway:** any arc threads content off `HITLPauseRequestedSignal` (`hitl_gate_composer.py:381`) across the `workflow_driver.py:5375` drop point, at which point the carrier-home cost `B-98` would pay is already paid and A-2/B become marginal. **D-3 — an observed misdescription:** an operator-loop report of a response composed against the wrong gate that `pause_reason` + position + `step_kind` did not prevent. *The harm class, not the convenience.*
- **Grounding carried by the ratification** (fork §3, absorbed into the `close_out`): the class is **not production-reachable in stock bootstrap** at HEAD; the description **already reaches the operator contractually** via the webhook, so the durable gap for the TEXT is **correlation** — which **narrows, not abolishes**, this row's `B-71`-distinctness claim; the **PLACEMENT** is by contrast **not** contractually available (`compose_hitl_action_id`'s shape is explicitly discretionary), so a typed placement field is net-new; **B and B′ stay DISTINCT** (B carries the already-existing `escalation_reason` and needs no authoring surface; B′ — the row's literal *"free-text description"* — needs one **minted first**); the undecided question is the **channel**, not the field; the carrier answer is **two** sites, forced by cardinality; the hash disposition is **fully pre-decided** by the five-instance drop-when-`None` precedent; and **additivity removes the decide-now pressure**.
- **Ordering recorded, NOT acted on.** `B-71` **dominates** `B-98` for the operator-loop win — the description already reaches the operator, and what is missing is the join `B-71` owns. This ratification does **not** re-open `B-71`.
- **Class 3 cite-hygiene item, recorded NOT patched.** `Spec_Control_Plane_v1_99.md:40` (*"v1.94's `synthesis_step_id`"*) is **falsified** — first occurrence is v1.58. A design-substrate edit does not ride a doc-only ratification; the CP spec is untouched by this leg.
- **What it is.** An operator inspecting durable pause state can learn **where** a workflow is paused and **through which resolution channel**, but not **what was asked**. Verified across every `PauseSnapshot` field at the `B-69` convening — and **symmetrically** for the closed-vocabulary counter-proposal: `HITLPlacementKind` exists as a `StrEnum`, but **no durable pause carrier declares a field of that type**; every `placement` occurrence in `pause_resume_protocol_types.py` is docstring prose. The same empirical fact defeats both a free-text description and a closed-vocabulary placement field: **both are capture-side scope.**
- **`hitl_gate_config_hash` is not a substitute.** Tested and rejected on its surviving ground: it is a **material-diff identity, not a description** — an opaque digest whose only operator-meaningful semantic is *"the gate config changed since capture."* It discriminates a config **edit**, never a **re-pause**.
- **Current state.** Registered, not built. Explicitly **declined for the `B-69` arc by the ergonomics voice itself** — the projection ships without it, with position + `pause_reason` judged sufficient for the map-composition task.
- **Close-out.** Capture-side, **hash-affecting**, X-AL-3-relevant — materially bigger than a read-only projection. Requires a capture-side carrier decision (which field, on which resume-state carriers, with what hash disposition per the drop-when-empty precedent) **before** any read-side projection field is added.
- **Distinct from `B-71`.** `B-71` adds a run-instance correlation id to `HITLEscalationBrief` / the webhook payload — it is about **disambiguating** two identical-looking escalations. This row is about **describing** one. Adjacent, not the same.
- **Council: ⚖️ CONDITIONAL as of the 2026-08-01 Reading C ratification** — **narrowed from the pre-ratification unconditional *"yes if opened"*** to the fork's §7 per-reading form, on a **probe** rather than a preference. **Under the selected C: NONE is owed now** — the probe found that the only author-independent "description" the product renders is a closed-vocabulary value (`hitl_gate_composer.py:2051`), so C10's *closed vocabulary* and C11's *description a human can act on* denote **the same value**, and the richer free text that does exist (`escalation_reason`) is already delivered contractually (`webhook_brief_adapter.py:77`). **BUT a convening is ADDITIONALLY OWED AT D-0 OR D-1 even while C remains the standing disposition** — D-0 because it makes the pause class real and so gives the tension a live surface; D-1 because it is precisely the condition under which free text the operator does *not* already hold starts to exist and the two positions separate. **If a build reading is later opened:** a dyadic **C10 ⊥ C11** convening is owed **before the spec leg** for **A-2 or B** (C7 added as consultant under B) — the payload-type question is exactly what it adjudicates, with the redaction posture as the concrete stake; **A-1 needs none** (no cross-axis channel, pre-decided hash disposition). The tension itself is unchanged and nameable: action-safety wanting a closed vocabulary and no unbounded free-text channel in durable state, against operator-loop wanting a description a human can act on — the `B-69` convening surfaced exactly that tension without resolving it, because the empirical absence closed the seam first.
- **Cross-ref.** `B-69` (open — the arc that surfaced it); `B-71` (registered — adjacent, distinct); `B-99` (the sibling payload-boundary reopening condition).

### B-99 · The B-69 projection's `summary_text` exclusion must be re-adjudicated when the `pause_context_reader` deferral is discharged *(named reopening condition registered at the B-69 spec leg, 2026-07-30)*

- **What it is.** A **named reopening condition** — not a defect, not a gate — filed so the `B-69` projection's own justification cannot go stale silently. `Spec_Harness_Runtime_v1.md` v1.107 §14.14.9.2 excludes `StateSummary.summary_text` from the caller-facing projection. That exclusion originally rested on **two** legs, and the convening's own adversarial pass **falsified one**.
- **The falsified leg.** *"The exclusion is free, since `summary_text` is empty everywhere."* True today — but only because the MVP binding is the injected **placeholder** `pause_context_reader` that §14.14.7 marks explicitly deferred and whose own docstring says its successor *"reads the current ledger head."* The emptiness is a property of a placeholder, not a structural property.
- **The surviving leg, now carrying the argument alone.** The field is an **unbounded forward channel** — a placeholder *for real ledger content*, with no redaction contract anywhere. An exclusion decided while the field is empty is free; the same exclusion after it carries ledger content is a **removal from a shipped caller-facing contract**, which nobody makes. Decided now, and stated as forward-load-bearing.
- **Honest counter, left unsmoothed.** When a real reader is composed, the **ergonomics case for inclusion genuinely reopens.**
- **Trigger.** The discharge of §14.14.7's `pause_context_reader` deferral — i.e. when a real reader is composed and `summary_text` begins carrying ledger content.
- **Obligation at that point.** Re-adjudicate the projection's field set **with a redaction contract as a PRECONDITION of any inclusion**, and re-adjudicate the paired `Spec_Operational_Discipline_v1_36.md` §30.5.4 redaction posture (currently **NOT ENGAGED** precisely because nothing redactable is emitted) **in the same arc**. Also re-examine `Implementation_Plan_Harness_Runtime_v2_55.md` U-RT-148 **AC #5** (the W4 characterization witness), which asserts a **placeholder property** and will fail *for a good reason* once a real reader lands — its correct disposition is then **DELETION or INVERSION, never repair**.
- **No gate is imposed now, deliberately.** The field is excluded, so there is nothing to redact, and gating an absent channel is textbook over-gating — the available over-gate was explicitly declined on the record.
- **Dated re-check 2026-07-31** *(post-`#1170`/`#1171`, at the `B-103` arc)*, **re-checked again 2026-08-05 at HEAD `43d4dda5`** with the Runtime spec now at **v1.110**, two versions past that pass. **The trigger has NOT fired** — on the spec side, and (newly covered at this pass) on the **code** side. **Spec side, cites refreshed 2026-08-05** — the `B-97`(a)/`B-100` legs grew the file, so the 2026-07-31 offsets `:5865`/`:5868`/`:5949` are superseded: the §14.14.7 *"Deferred to implementation discretion"* heading is at **`:5908`**, the `pause_context_reader` deferral bullet at **`:5911`**, and the MVP constant-sentinel statement (*"the MVP `pause_context_reader` returns the constant sentinel"*) at **`:5992`**, with §§14.14.9.2–.5 preserved verbatim through v1.110 — so neither the exclusion nor the paired redaction posture has moved. **Code side, added at this pass:** the production binding is still the placeholder — `_make_default_pause_context_reader` (`harness-runtime/src/harness_runtime/bootstrap/factories/pause_resume_protocol_factory.py:71-102`) returns a `StateSummary` with `summary_text=""` at `:102`, its docstring explicitly labelling the state-tracking semantics a placeholder, and every other production `StateSummary` on the pause path likewise constructs `summary_text=""`. **Nothing owed**; disposition VERIFIED-DORMANT; re-check when a real reader is composed.
- **Council: ❌ no** — the tension (C10 ⊥ C11 on the payload boundary) was already convened and adjudicated at the `B-69` record; this row records the condition under which that adjudication must be re-run, and the re-run inherits the same voice pair.
- **Cross-ref.** `B-69` (open — the arc whose exclusion this guards); Runtime spec §14.14.7 (the deferral that is the trigger) + v1.107 §14.14.9.2 (the exclusion); OD spec v1.36 §30.5.4 (the paired posture).

### B-100 · The empty-`idempotency_key` key-ABSENT carve-out does not reach the LINEAR and orchestrator fence carriers *(surfaced at the `B-69` impl leg, reported not absorbed, 2026-07-31)*

- **IMPL LEG LANDED 2026-08-01 — the ROW IS NOW CLOSED.** The remainder this row was narrowed to at the spec leg is built and witnessed. **ONE file:** `harness-cp/src/harness_cp/pause_state_projection.py`. Two new `PauseLocationSourceShape` members + two new projection types (`KeyAbsentLinearEffectFenceAddressableLocation`, `KeyAbsentOrchestratorEffectFenceAddressableLocation`) + two sibling routing helpers (`_effect_fence_linear_projection`, `_effect_fence_orchestrator_projection`) mirroring the shipped `_effect_fence_branch_projection` **exactly** — the sub-discriminator is the projection-stamped source shape, so **no new machinery** — plus `min_length=1` on the two key-BEARING shapes, which is what makes a `""` key *unrepresentable* rather than merely unrouted. Source shapes **8 → 10**; VARIANT set UNCHANGED at **FOUR**; carriers UNCHANGED at **SEVEN**. ZERO capture-side change, ZERO carrier retyped, ZERO `snapshot_hash` impact — disposition **(b)** still NOT applied. **All three closure conjuncts satisfied**, witnessed by NINE tests, **every one mutation-probed**: `a11a` / `a11b` / `a11c1` / `a11c2` / `a11c3` / `a11d` / the resolver-invariance guard (CP) and `ac16a(ii)` / `ac16a(iii)` (Runtime), with the widened `ac15b` covering all three carriers. Two probes are load-bearing beyond the obvious — narrowing `PauseTreeWalkEntry.effect_fence_key` to `None` **fails** the invariance guard (that field MUST keep surfacing `""`; suppressing it would silently change a CP **classification rule**, which is `B-107` scope, not this row's), and a key-bearing shape given a **default** still passes (c2) while failing (c3), which is precisely why (c3) is not redundant with (c2). Tests 1859 → **1866** (CP) and 3557 → **3559** (Runtime); pyright 0/0/0; ruff clean. **VERIFIED-AND-REPORTED, not fixed:** the four ordinary-fan-out `""`-coercion sites (`:9625` / `:9752` / `:14024` / `:14149`) **do** route correctly at this head — each writes `""` into `effect_fence_paused_dispositions`, which reaches `EffectFencePausedBranchResumeState(idempotency_key="")` **unfiltered** at `:10272` / `:14631` (no truthiness guard on either path) and thence to the branch keyless shape, exactly as the spec leg predicted at finding (iii). **`B-107` NOT touched** — this diff changes no resolution or refusal channel.
- **SPEC HALF APPLIED at the `B-100` spec leg, 2026-07-31 — at that leg the ROW STAYED OPEN, NARROWED to the IMPL remainder (since discharged, see the bullet above).** Disposition **(a)** applied across four artifacts: **CP spec v1.112 → v1.113** (ONE amendment site — §2.1's key-ABSENT treatment generalized from a **PROVENANCE** scope to a **STATE** scope, *any effect-fence source shape whose captured key is EMPTY*, adding a KEY-ABSENT sibling SOURCE SHAPE for the LINEAR and orchestrator carriers), **Runtime spec v1.108 → v1.109** (the identical amendment inside §14.14.9.2), **CP plan v2.47 → v2.48** (U-CP-64, ONE ADDED AC #A11) and **Runtime plan v2.56 → v2.57** (U-RT-148, AC #15(b) + #16(a) WIDENED; sixteen ACs stay sixteen). **VARIANT set UNCHANGED at FOUR**; `effect-fence-addressable` source shapes **4 → 6**, the union **8 → 10**, distinct source **CARRIERS UNCHANGED at SEVEN** (three of them now yield two shapes each). ZERO contract numbers minted, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact, ZERO new CXA rows (v2.23 unchanged, aggregate frozen at 111), ZERO new units / clusters / DAG edges. Disposition **(b)** NOT applied, per this row's own declination. **The row is NOT closed**: the status enum defines `closed` as *built + merged, or foreclosed/superseded*, and the projection types plus their emptiness routing are unbuilt — the `B-97`(a) precedent (open through the v1.108 spec leg at PR #1168, closed only at PR #1171 once U-RT-149 impl landed) and the `B-102` precedent (*narrowed to that remainder rather than closed*) both apply.
- **TWO OF THIS ROW'S ORIGINAL GROUNDS WERE FALSIFIED AT THE SPEC LEG and are corrected below in place. The DISPOSITION is unchanged; only its ground is.** The bullets that follow carry the corrected facts, not the filed ones — the filed wording is preserved in this file's git history and in the row's `summary` at `.harness/forward-register.yaml`, which carries the same correction.
- **What it is (CORRECTED).** `Spec_Control_Plane_v1_112.md` §2.1 and `Spec_Harness_Runtime_v1.md` v1.107/v1.108 §14.14.9.2 both scoped the empty-key / key-field-**ABSENT** source shape by **PROVENANCE** — to fan-out crash reconstruction, which constructs `idempotency_key=""` deliberately (`workflow_driver.py:4074`). The **LINEAR** capture site admits an empty key too: it constructs `EffectFenceResumeState(idempotency_key=_fence_key)` whenever `isinstance(_fence_key, str)` (`:5456`), which admits `""`. **The ORCHESTRATOR site does NOT** — *the filed claim that it "is unconstrained the same way" is FALSE.* `OrchestratorEffectFencePausedResumeState` has exactly **ONE** construction site (`:12392`) whose guard chain at `:12376`–`:12381` ends with `and _orch_fence_key`, a **truthiness** test excluding `""`. **A third fact the filing did not have:** the branch carrier already reaches `""` **without any crash reconstruction**, via the ordinary fan-out path's defensive coercion at `:9625` / `:9752` / `:14024` / `:14149` — so the provenance scoping was already narrower than the shape's reachable population.
- **Why it matters (CORRECTED MECHANISM).** *"Both real resume sites consult a fence resolution only when the key is truthy"* is **true of the fan-out per-branch dispatch sites** (`if (_branch_fence_key and _ef_resume_ctx is not None)`) **and of the ORCHESTRATOR consult** (`:12118` — `if (_orch_fence_resume.idempotency_key and _ef_resume_ctx is not None)`, truthiness-gated the same way), and **false of the LINEAR consult ALONE** (`:4925`–`:4936`), which calls `_resolve_effect_fence_gated` with the captured key unconditionally. The harm at the LINEAR site is real but arrives differently: the threaded directive is applied **only when the runtime's RECOMPUTED per-`(run, step, tool)` dispatch key equals it** (`runtime_tool_dispatcher.py:1042`, against the key composed at `:989`) — never `""` — so it is threaded, then discarded, and the dispatch re-pauses INERT. Independently, `compute_effect_fence_tree_wide_abort_present` filters *all* empty keys before resolving (`workflow_driver.py:3054`). **Every route ends in the same place: the operator's response is silently DROPPED, not refused** — the same livelock the branch-carrier carve-out exists to foreclose, arriving one door over.
- **Why the ORCHESTRATOR shape is specified anyway, on a DIFFERENT ground.** **TYPE TOTALITY.** All three effect-fence carriers declare `idempotency_key: str` with **no length constraint** (`extra="forbid"`, frozen), and the projection is a **total function over journaled records that outlive the capture-site code that wrote them** (§14.14.8's store is append-only and never truncated). A totality resting on a capture-site `if` is a convention, not the type invariant this contract's posture requires. **Consequence:** that shape is **UNREACHABLE in production**, so its acceptance witness MUST be a **constructed-snapshot** detect-then-refuse test, never an end-to-end run — recorded at CP plan v2.48 AC #A11 and Runtime plan v2.57 AC #16(a) so a later closeout does not demand an unbuildable witness. *(Flagged as a spec-writer determination so a reviewer can overturn it rather than discover it.)*
- **Why it was filed rather than fixed in place.** The `B-69` impl leg conformed to the cleared enumeration **exactly**. Adding keyless sibling source shapes would have extended the cleared eight-shape enumeration — an **X-AL-3 design extension an impl leg must not make silently**, and the spec leg reversed a *fifth variant* on exactly this kind of reasoning at its round 11.
- **Severity, stated honestly.** **Defensive-only** at the LINEAR site today (the key is read by name off a runtime error; no production path is known to yield `""`) and **unreachable** at the orchestrator site. A **soundness gap, not a live defect** — which is why it was registered rather than treated as a halt.
- **Close-out — the IMPL REMAINDER (DISCHARGED 2026-08-01; stated in the past tense, since the condition it describes no longer holds).** `walk_pause_tree` *had* constructed `LinearEffectFenceAddressableLocation` (`pause_state_projection.py:509`) and `OrchestratorEffectFenceAddressableLocation` (`:522`) **unconditionally** from the carrier's key, with no emptiness routing — so an empty key *was* surfaced as a `""` key *value*, the state v1.113 §1.1 forbids. **Both now route by emptiness through their own sibling helper.** The branch shape already has its routing helper (`_effect_fence_branch_projection`, `:621`–`:649`); the two new shapes need its sibling plus their projection types. **Owning units: U-CP-64** (CP plan v2.48 AC #A11) **and U-RT-148** (Runtime plan v2.57's widened AC #15(b) + #16(a)). **B-100 closes iff** (the LINEAR and orchestrator KEY-ABSENT projection types exist **AND** route by emptiness) **AND** (AC #A11 (a)–(d) witnessed green, with (b) as a constructed-snapshot test) **AND** (the widened AC #15(b) + #16(a) witnessed green). **Partial is non-closure; a spec leg alone does not close this row.** Disposition **(b)** (capture-side refusal) remains NOT applied — capture-side and therefore hash-adjacent.
- **Council.** No — the tension was already convened and adjudicated at the `B-69` record; this row only asked whether the *same* adjudicated rule reaches two more source carriers, which is a scoping question for a **spec-writer apply pass**. That routing is the row's own, and it is why the spec leg carried no fresh operator gate.

### B-101 · Accessor-derived provenance is lost through base-typed Pydantic serialization *(surfaced at the `B-69` impl leg, out-of-family review round 3, reported not absorbed, 2026-07-31; CLOSED 2026-08-01 — disposition (b)-PLUS applied at CP spec v1.114, with a normative PROMOTION TRIGGER to disposition (a) and the pin test retained as the boundary marker)*

- **STATUS 2026-08-01 — `registered_finding` → `closed`. SPEC LEG APPLIED, DISPOSITION (b)-PLUS.** CP spec v1.113→**v1.114**, **ONE amendment site**: §1.2 constraint 3's stated residual is **APPENDED TO** with ONE paragraph — **NO text retracted or reworded** (but see the counter-reading bullet below). The paragraph (i) declares the **ordinary-serialization boundary formally OUT of the staleness fence's scope**, (ii) **NAMES** `StepExecutionContext.resume_context` (`harness-cp/src/harness_cp/workflow_driver_types.py:460`) as the ONE live base-typed carrier, (iii) **records the three grounded facts** that keep it unreachable at HEAD, and (iv) states the normative **PROMOTION TRIGGER**. **ZERO contract numbers, ZERO carriers retyped, ZERO fields added, ZERO hash impact, ZERO impl units owed, ZERO plan deltas owed, ZERO test changes owed.** Clearance marker `.harness/clearance/spec-control-plane-v1-114-cleared-2026-08-01.md`; `pr: '#1175'` — reconciled 2026-08-01 at this register touch.
- **Why (b)-PLUS rather than plain (b).** Plain (b) would have been satisfied by one sentence of general prose. **This row's own NEAREST-APPROACH VECTOR note forecloses that reading** — the hazard is *one `model_dump` away on an existing carrier*, and *disposition (b) alone would NOT close that vector*. A residual saying *"serialization is out of scope"* without naming the live carrier would be **true and useless**: a later arc adding one `model_dump` to a debug path would land it without ever reading a sentence that applies to it. Naming the carrier is what makes the trigger checkable **at the site**.
- **The grounded facts, re-verified at the spec leg rather than carried from this row's 2026-07-31 note.** **ZERO** `model_dump` / `model_dump_json` / `TypeAdapter` of that field **or of its enclosing `StepExecutionContext`** across `src/` **AND** `tests/` of all seven packages — and `StepExecutionContext` is **never a field of another model**, so it is never transitively serialized either; `compose_branch_child_context`'s `model_copy` (`workflow_driver_types.py:638`) is **shallow and non-revalidating**; `revalidate_instances` is **unset repository-wide**, so Pydantic's `'never'` default holds. Of the **nineteen** `ResumeContext`-typed annotations in `src/**`, exactly **one** is a model field — the rest are sixteen function parameters and two local bindings, none of which is ever serialized.
- **PROMOTION TRIGGER (normative, binding on future arcs; keyed on MECHANISM, not on the named carrier).** Any arc that introduces **ANY base-schema serialization boundary over a `ResumeContext` value on a resume path** — a `ResumeContext`-typed model field (today only `StepExecutionContext.resume_context`), a base `TypeAdapter`/`RootModel` over `ResumeContext` (the route the pin test demonstrates directly, `:960`), a base-annotated parameter or return crossing a schema-generating boundary, or any equivalent — MUST promote this row to **disposition (a)** (the closed variant discriminator on `ResumeContext`) and land that amendment **BEFORE or SIMULTANEOUSLY WITH** the arc. **Never after.** The trigger fires on *introducing the serialization*, not on observing a failure — by then the fence has already silently not fired.
- **Honest limit, stated in the spec rather than papered over.** Trigger enforcement is a **pre-merge review obligation, not a validator rule**. The pin test breaks on a **behavior** change but does **NOT** fire on the bare addition of a `model_dump` call, so an arc could add one on a non-resume path, break nothing, and leave the carrier one step closer to reachable.
- **THE PIN TEST STAYS — do NOT delete it at this close.** `test_provenance_is_lost_through_base_typed_serialization_registered_b101` (`harness-runtime/tests/test_paused_workflow_state_accessor_b69.py:924`) is the **executable half** of the trigger: it asserts the CURRENT lossy behavior deliberately, so any future change to that behavior is reviewed rather than silent.
- **Why this row CLOSES where `B-100` / `B-97`(a) / `B-102` NARROWED.** Those close-outs left **unbuilt impl remainders**, so closing them would have presented unshipped work as done. Disposition (b) is **documentary by construction** — it declares a boundary rather than moving one — so its close-out is fully discharged by the delta. **Verified, not assumed:** `B-101` is cited at exactly ONE place in `design-substrate/**` (`Spec_Control_Plane_v1_113.md` §0.4 finding (vi), as the obstacle `B-107`'s disposition (d) would hit) and at **no implementation plan**.
- **The strongest counter-reading, recorded rather than answered away** *(out-of-family review round 2 `[P1]` at the spec leg)*. `Spec_Control_Plane_v1_112.md` §1.3(c) requires *"no construction path produces a legacy variant from an accessor-derived one"*. **Read at its broadest, a base-schema round-trip IS such a path**, so on that reading the shipped subclass realization does not satisfy (c) and declaring the boundary out of scope is a **NARROWING**, not a clarification. The delta applies the **narrower** reading on three grounds checked at HEAD: constraint 3's own enumeration is of **CP-owned affordances** (*"No downgrade, no `.without_token()`, no field mutation — frozen carriers"*); the detect-then-refuse witness §1.3 REQUIRES for (c) exercises exactly those three and no fourth (`harness-cp/tests/test_pause_state_projection_b69.py:149`, AC #A2), and the base-schema route slips past its third guard for a stated reason — the base schema never emits the provenance field for `extra="forbid"` to see; and constraint 3's own scoping sentence bounds the harm it claims to foreclose. **Escalation is ONE step, not a re-litigation:** a reviewer holding the broad reading routes this to **disposition (a)** — which is exactly what the promotion trigger already binds a future arc to do. If the operator holds the broad reading TODAY, apply (a) now; nothing in the delta obstructs that, and its trigger text becomes redundant rather than wrong. Recorded at `Spec_Control_Plane_v1_114.md` §0.5 finding (vi).
- **Disposition (a) is NOT applied, on this row's own ground.** It breaks the field-set-preserved-verbatim guarantee `Spec_Control_Plane_v1_112.md` §0.4 makes and would owe a byte-compatibility argument for every existing construction site; this row conditions it on *"a serialization boundary ever appearing on a real resume path"*, which **does not exist at HEAD**. `B-107`'s disposition (d) hits the **same** obstacle — the adjacency is recorded at `Spec_Control_Plane_v1_114.md` §0.5 finding (v); the rows are **NOT** merged.
- **What it is.** `AccessorDerivedResumeContext` is a **subclass** of the CP-owned `ResumeContext`, and Pydantic v2 serializes a subclass through a base-typed field or `TypeAdapter` using the **base's** schema. A round-trip through any `ResumeContext`-typed boundary therefore emits only the four legacy fields and silently **drops `pause_state`** — after which `api.resume()`'s variant check sees a legacy context and the §30 staleness fence does not fire.
- **Severity, stated honestly.** **Not reachable on any shipped path today**: `api.resume` takes the object directly, and the in-process MCP handoff stores it without serializing. A latent hazard, not a live defect.
- **Why it is filed rather than fixed.** It **cannot be closed from the subclass** — the behavior is controlled by the base type's schema, and CP spec v1.112 requires `ResumeContext`'s field set be **PRESERVED VERBATIM**, so adding a variant discriminator to it is a **spec change an impl leg must not make**.
- **Relation to the cleared residual.** CP spec v1.112 §1.2 already states *"a caller who takes an accessor read and then, independently, constructs a legacy `ResumeContext` is unfenced … the type system cannot track a side effect that happened before construction."* This is an instance of that — but it arrives through **ordinary serialization** rather than a deliberate act, which is the sharper form and the reason it is registered.
- **Pinned, not just described.** `test_provenance_is_lost_through_base_typed_serialization_registered_b101` asserts the **current** behavior, so the boundary is visible and any future change to it is deliberate and reviewed.
- **Nearest-approach vector** *(scoped re-gate at the `B-97`(b) arc, 2026-07-31)*. `ResumeContext` is **already** a base-typed field of `StepExecutionContext` (`workflow_driver_types.py:460`). It is **not dumped today** — only `HandoffContext` is `model_dump_json`'d, `model_copy` preserves object references rather than revalidating, and `revalidate_instances` defaults to `never` — so the not-reachable-today claim **stands**. But the hazard is **one `model_dump` away on an existing carrier**, not a hypothetical future boundary, and disposition **(b)** (documenting the boundary as out of scope) would **not** close that vector.
- **Close-out.** Both dispositions are spec-side: **(a)** amend CP spec §1 so `ResumeContext` carries a closed variant discriminator and the two variants become an explicit discriminated union — strictly stronger, but it breaks the field-set-preserved-verbatim guarantee §0.4 makes; **(b)** declare the serialization boundary formally out of the fence's scope, extending §1.2's stated residual to name it. **(b)** is the cheaper and more honest default; **(a)** is warranted only if a serialization boundary ever appears on a real resume path.
- **Council.** No — a scoping question about an already-adjudicated residual.

### B-102 · `corrupt-latest` is presented as decisive, but a torn in-flight append tail is cross-process reachable exactly as `empty-journal` is *(surfaced at the `B-97`(b) arc, lens-1 F1, 2026-07-31; spec half DISCHARGED at Runtime spec v1.108, 2026-07-31 — the read-side attribution flip rides the `B-97`(a) impl leg)*

- **SPEC HALF DISCHARGED at the `B-97`(a) spec leg, 2026-07-31.** Runtime spec **v1.108** amends §30's cause table so **`absent` (`:3318`) and `corrupt-latest` (`:3321`) each carry the SAME reachability qualification `empty-journal` (`:3319`) already carried** — permanent for the single-process deployment where it is decisive, **INDETERMINATE across processes**, and *an implementation MUST NOT present either as decisive loss where a second writer is reachable*. **All three permanent-absence causes are now qualified rather than one of three**, and the `RT-FAIL-RESUME-HANDLE-UNKNOWN` taxonomy row plus §14.14.9.4's routing clause were refreshed in the same edit so **no surface flattens the table**. This is the row's own disposition **(a)**, and it needed **no new cause member** — which is why it composed cleanly with `B-97` half (a) rather than competing with it. *(The bullets below preserve the finding as filed.)*
- **IMPL HALF OUTSTANDING — the whole remainder, and it is NOT independently scheduled.** Flip the **shipped** read-side attribution to the indeterminate disposition for `absent` and `corrupt-latest` where a second writer is reachable, matching what `empty-journal` already reports (`journal_workflow_pause_store.py` hard-codes `retryable=False, indeterminate=False` for both today). That flip **rides `Implementation_Plan_Harness_Runtime_v2_56.md` U-RT-149 AC #4(c)** at the `B-97`(a) impl leg — both touch the same attribution site, and two arcs editing one function is the duplication this register exists to prevent.
- **Why NARROWED rather than CLOSED.** Closing while half the row's own close-out is unshipped would present unshipped work as done, and would leave the contract and the code disagreeing with nothing tracking the gap. The stale cross-reference in `B-97`'s own text — that half (a) *"needs a sixth cause member"* — is corrected at that row: **Reading A mints none.**

- **What it is.** `Spec_Harness_Runtime_v1.md` v1.107 §30's cause table scopes its MUST-NOT-present-as-decisive clause to **one row of three**. `empty-journal` (`:3319`) carries *"INDETERMINATE across processes — a concurrent `capture()` may complete immediately after this read"* plus *"an implementation MUST NOT present it as decisive loss where a second writer is reachable."* The `corrupt-latest` row (`:3321`) says only **"permanent"**, and the shipped read hard-codes `retryable=False, indeterminate=False` for it (`journal_workflow_pause_store.py`, the non-empty branch of `read_latest_attributed`).
- **Why the same argument applies.** A torn trailing line is **exactly what an in-flight append looks like from a reader**. A read that lands mid-append attributes `corrupt-latest` to a record that is about to become well-formed — and where a second writer is reachable, that outcome is **indeterminate, not decisive loss**. The two rows differ only in *where* the concurrent writer is caught (before its first byte vs. between its bytes), which is not a difference the operator-repair column can act on.
- **The same omission reaches `absent`** *(found independently by out-of-family review round 2 at this arc)*. `absent` (`:3318`) is likewise flat **"permanent"**, yet a read of a workflow whose capture has been dispatched but has not yet created its journal file reports it with `indeterminate=False` — and that capture may complete moments later. So §30 qualifies **one** of the three permanent-absence causes and leaves the other two unqualified on the same reachability.
- **`B-97`(b) shrinks the window; it does not close it.** Concurrent **appends** are now serialized, so a torn tail can no longer be produced by two live writers racing. But a writer **killed mid-append** still leaves one, and a reader racing a **single** live writer is unaffected by a lock the reader deliberately does not take.
- **Severity, stated honestly.** Not a live defect on the shipped single-process deployment. Filed as a **soundness gap in the attribution contract**, exactly as `B-100` was.
- **Close-out — spec-touch owed.** An impl leg must not widen a cleared cause table on its own authority (X-AL-3). **(a)** Amend §30's cause table so `corrupt-latest` **and** `absent` carry the same reachability qualification `empty-journal` already carries, then flip the read-side attribution to `indeterminate=True` for those causes where a second writer is reachable. **(b)** The narrower alternative: leave the table alone and document why they *are* decisive despite the symmetry — viable only if someone can produce the asymmetry, which this row could not. **Prefer (a).** The fix is **attribution-only**: the fail-closed disposition is unchanged either way, and **no new cause member is needed** — which keeps it independent of `B-97` half (a), whose refusal semantics *do* need a sixth member.
- **Council.** No — this asks whether an already-adjudicated qualification reaches one more row of the same table; a scoping question for a spec-writer apply pass, not a fresh cross-domain tension.
- **Cross-ref.** `B-97` (half (a) touches the same closed vocabulary; half (b) is the reachability amplifier that motivated this row); `B-69` (closed — the arc that shipped the five-cause attribution).

### B-103 · The `B-97`(b) cross-process append `flock` is a BLOCKING syscall taken on the event loop *(surfaced at the `B-97`(b) pre-merge gate as an out-of-family [P2], 2026-07-31; split to its own row at the `B-97`(a) spec leg, 2026-07-31)*

- **What it is.** `JournalWorkflowPauseStore.capture()` is called **synchronously** from the async `capture_pause_snapshot`, so the per-workflow `fcntl.flock` `B-97`(b) introduced is a **blocking syscall taken on the event loop**. A contended acquisition stalls the whole loop — every other workflow, every pending timer — for the duration of another process's append.
- **Precedented, which is why it is registered rather than argued.** The sibling protected result store grew an off-loop resolution path (`resolve_result_ref_off_loop`) for exactly this shape.
- **Why it was split rather than folded into the keying arc.** It is a **mechanism / latency** question with **no keying content and no spec text**; folding it into `B-97` half (a) would have mixed two unrelated review surfaces. Parking it in `B-97`'s prose would have lost it from `--open` the moment that row closes — or, if the row were held open for it, would have falsely implied half (a) was unfinished. `[[spine-ledger-forward-arc-registration]]`.
- **Severity, stated honestly.** A durable pause is a **human-latency** event and the lock is held for one append, so the contention window is small and **no live stall has been observed**. This is a soundness / latency gap, **not a live defect**.
- **Close-out** *(as registered — SUPERSEDED, never delivered; see the CLOSED bullet below)*. Move the blocking acquisition off the event loop on the sibling's own precedent (an `asyncio.to_thread`-shaped offload of the locked append, or an equivalent off-loop resolution path), and **witness it by execution**: an unrelated concurrent workflow **and** a pending loop timer demonstrably advance while a second process holds the journal lock — with the **mutation probe** being the direct synchronous call, under which that witness must fail.
- **Two things this row MUST NOT do.** It must **not** change the lock's **granularity** — per-workflow was grounded and defended at the `B-97`(b) arc against a directory-tree alternative, and reopening it here relitigates a settled decision. It must **not** change the **read** path, which deliberately takes no lock — that no-lock property is what keeps Runtime spec §30's `empty-journal` **INDETERMINATE** clause exact as written.
- **Spec expectation.** **No spec text is expected to be owed** — the offload is an execution-venue mechanism and §14.8.1's wrap-asymmetry lineage already covers the class. **Confirm that before building, and file a fork if it turns out otherwise rather than absorbing it.**
- **Council: ❌ no** — a single-axis execution-venue question with no nameable cross-domain tension. The granularity decision that *would* have carried one is settled and explicitly out of scope; route to **C9** (reliability / recovery) directly, plus `advisor()` if the offload shape interacts with drain or shutdown ordering.
- **CLOSED 2026-07-31 at PR `#1172` — FALSE PREMISE, established at source at the merge gate (lens 1), not a delivered fix.** *(PR number stamped on the row 2026-08-05. Re-verified 2026-08-05 by direct read: `capture_pause_snapshot` still calls `self._store.capture(snapshot)` **inline** at `harness-runtime/src/harness_runtime/lifecycle/durable_pause_resume_protocol.py:124`, and the pause-capture path carries **zero** `run_audit_off_loop` references — that helper exists at `lifecycle/audit_offload.py:256`; the PAUSE-CAPTURE path has ZERO references to it — live consumers on OTHER paths at 2026-08-05: `hitl_gate_composer.py:1363`, `llm_dispatch.py:5107`, `cost_attribution_validator_dispatch.py:419`, `webhook_delivery_composer.py:390`, `runtime_tool_dispatcher.py:590`.)* **There is no event loop to starve at any shipped call site.** All **eleven** production `capture_pause_snapshot` invocations reach the protocol through `workflow_driver._run_protocol_method_sync` (`:2583-2603`), which runs the coroutine via **`asyncio.run` on a private, single-task loop** inside the driver's own `asyncio.to_thread` worker. That helper's own docstring states it verbatim: *"no current event loop is bound on the worker thread. `asyncio.run` constructs a new loop for this single coroutine."* The blocking `flock` therefore blocks a thread **already blocked awaiting that very coroutine**, with no co-resident coroutine or pending timer to starve.
- **The offload was BUILT, gated, and DECLINED** — preserved at this branch's pre-rework commits (head `3636762c`-era, unmerged): a dedicated executor + join-on-cancel helper + a 12-test execution witness. Measured against production it would have made things **worse**: a **second** blocked thread on top of the already-blocked driver worker, plus a cancellation window in which an append could land after `CancelledError` was observed (registered as `B-105`, now **withdrawn with the offload**) — for **zero** recovered concurrency. The witnesses were sound for the shape they tested and reproduced starvation exactly; that shape is one production never uses.
- **Reopening trigger — why this is closed, not deleted.** If a future arc replaces `_run_protocol_method_sync`'s `asyncio.run` with a **captured-loop bridge** — the evolution its own docstring anticipates at `:2598-2601`, conditioned on the protocol body gaining real async I/O — the capture becomes genuinely loop-co-resident, this premise goes **live**, and the preserved offload shape applies directly. Re-check at any change to that helper.
- **Cross-ref.** `B-97` (half (b) introduced the lock; half (a)'s spec leg split this row out); `B-102` (the adjacent attribution row from the same arc); `workflow_driver._run_protocol_method_sync` (the helper whose evolution is the reopening trigger).

### B-104 · The pause journal writes no pause-RESOLVED marker, so the §13.7 enumeration can only bound the at-risk set from ABOVE *(surfaced at the `B-97`(a) spec leg, out-of-family review round 1 [P1], reported not absorbed, 2026-07-31; Class 2 fork FILED 2026-08-01 at PR #1181; RATIFIED 2026-08-01 as READING D — DECLARE + DEFER: Component 1 DISCHARGED at Runtime spec v1.110, the discriminator DEFERRED with the four-disjunct demand test D-0…D-3, D-0 dominant)*

- **RE-AFFIRMED 2026-08-05 in the operator ratification batch: Reading D (DECLARE + DEFER) stands, unchanged, D-0 dominant.** No new work is owed — **Component 1**, the mandatory symmetric declaration, already landed at **Runtime spec v1.109 → v1.110 (PR #1182)** with clearance marker `.harness/clearance/spec-harness-runtime-v1-110-cleared-2026-08-01.md`, and **Component 2** stays deferred under D-0…D-3. The row's `pr:` pointer, which still read `#pending` for that leg, is **repaired** here to name #1182. Status stays `registered_finding`; the runner-up **A** remains registered-and-held and is **not** advanced.
- **RATIFIED 2026-08-01 as READING D — DECLARE + DEFER.** Operator `AskUserQuestion`; recorded verbatim-in-substance at the fork's `## §11 RATIFICATION` addendum (`.harness/class_2_fork_b104_pause_resolved_marker.md`, FILED at PR #1181). **The row stays `registered_finding`.** Readings **A, B and C were NOT selected and are NOT partially adopted**; runner-up **A is REGISTERED-AND-HELD** at the fork's §6 per `[[reviewer-oscillation-register-and-hold]]` and is **not** advanced here.
- **COMPONENT 1 — DISCHARGED at this leg's spec delta.** `Spec_Harness_Runtime_v1.md` **v1.109 → v1.110**, **TWO amendment sites, both pure DECLARATIONS of an EXISTING limit**, symmetric with §13.7 term **3-bis**: a new closing paragraph at **§14.14.9.1** (the accessor) and a **NEW invariant bullet in §30** immediately after `Durable-handle read (v1.46)`. Both state that the store is **append-only, never truncated, and writes NO pause-resolved marker**, so a resolved journal is **byte-indistinguishable** from an outstanding one and the read reports the **latest durable record**, which is **not a liveness claim** — and that the limit holds **in steady state for a current-format journal**, not merely for the pre-v1.108 legacy set (the fork's §3(vii) scope correction). The §30 bullet additionally states what §14.14.9.1 does not need: `resume()`'s shipped guards validate **integrity and applicability, never outstandingness**, and the §30 staleness precondition fences a composed claim against a **CHANGED** record — so a re-resume of an **UNCHANGED, already-resolved** record presents no mismatch and **is admitted**. The **mandatory docstring companions** land on both public surfaces (`harness-runtime/src/harness_runtime/api.py` — `read_paused_workflow_state` `:925`, and `resume()` at **`:1190` post-edit** on **both** its durable-handle prose and its separately-consulted `resume_handle` parameter entry at **`:1260` post-edit**), **zero logic changed**. *Anchors are stated **post-edit** deliberately — this leg's own docstrings shift `api.py`'s lower half, so the fork's pre-leg `:1170` / `:1197` / `:1223` / `:1321` cites do not resolve in the landed tree.* **The limit is DECLARED, not FIXED**: it closes the **disclosure** half of §3(vii); it does **not** close the blindness. **ZERO** contract numbers minted (a doc-only leg cannot), **ZERO** carriers retyped, **ZERO** fields added, **ZERO** `snapshot_hash` impact, the five-member `PauseJournalReadCause` vocabulary stays **CLOSED at five**, **ZERO** plan units owed at the Runtime / OD / CP plans, and **ZERO** CXA rows (aggregate frozen at 111 — no cross-package consumption at all). *Verified, not assumed:* `B-104` is cited at **no** implementation plan and at **no** acceptance criterion. Clearance: `.harness/clearance/spec-harness-runtime-v1-110-cleared-2026-08-01.md`.
- **COMPONENT 2 — the discriminator is DEFERRED; the reopening triggers are a FALSIFIABLE four-disjunct demand test, any one disjunct fires.** **D-0 (DOMINANT) — a retention / pruning-policy arc opens.** §14.14.8 declares a retention policy a **follow-on arc explicitly not owed** there (`pause_journal_disposal.py:14`–`:17`). **A pruner is UNBUILDABLE without this discriminator**: it cannot be permitted to reclaim an outstanding pause, and format-keying gives it nothing — post-cutover every journal is `CURRENT_FORMAT`. This is the one condition that turns the ceiling into a **hard blocker**, which is why it dominates. **D-1 — any surface BRANCHES on liveness, or a stale re-resume is OBSERVED** (round-2 re-scoped form; the first form — *"a second flow acts on the distinction for a current-format journal"* — was **already satisfied at HEAD** by §3(vii) and would have been vacuous the day it was written). Two disjuncts: **(a)** any code path makes a **control-flow** decision conditioned on whether a durable pause is outstanding — as opposed to merely reading the latest record and being blind to it, which is HEAD's shipped behaviour and is what Component 1 declares; **or (b)** an operator-loop report of a `resume(resume_handle=...)` re-entering an **already-resolved** snapshot in a fresh process. *The harm class, not the asymmetry — the asymmetry is discharged by Component 1.* **D-2 — a second KEYING change is opened.** §14.14.8 contemplates one (*"A future encoding change is a KEYING change and MUST take this arc's fork path"*); it re-runs the whole (3a)/(3b) cutover over an accumulated directory holding one journal per workflow that ever paused, where the upper bound approaches vacuity. **D-3 — the resumer ceases to be the party performing the cutover.** The workspace advances past `solo-developer` on the committed bridging arc (root `CLAUDE.md` §10.2), so the operator confirming a drain is not the party who ran the `resume()` calls. *Same shape as `B-98`'s D-1(b), and the same premise.* **Explicitly NOT a trigger on its own:** the §13.7 listing merely being loose — that is this row's already-disclosed, already-contract-stated condition (term 3-bis) and is what the `UPPER_BOUND_DISCLAIMER` exists to communicate.
- **Grounding carried by the ratification** (fork §3, absorbed into the `close_out`): the hypothesized **derive-from-existing** option is **FALSIFIED** (no resume-specific record carries a re-derivable identity; no identity-bearing record is tenant-qualified; the near-miss is Reading C); sub-decision **(a)-1 is FORCED, not open** — a `PauseSnapshot` sentinel is **resumable**, so only a **wrapper-level** record is non-defective; sub-decision **(a)-2 is confirmed genuine and its default is worse than unspecified** — `corrupt-latest` + `indeterminate=True`, empirically probed, and a **sixth** cause member is mandatory under A; the **second-authority objection to (b) does not hold as written**; the ambiguous set is **frozen, one-time and drainable**; the **NOT-IN-THE-ROW cross-axis cost** — the sixth cause member **forces an OD spec + plan leg**, because `harness-od` re-declares the vocabulary independently (`pause_resume_namespace.py:482`–`:498`) and `api.py:1183` converts **by value** (post-edit anchor); the **NOT-IN-THE-ROW occurrence-key requirement** common to all three build readings, pinned to the shipped §30 staleness token's `(record_count, latest_record_digest)`; and that any build reading **may** re-key disposal's acknowledgement gate — **an explicit decision each owes, not an automatic consequence and not an argument for deferral**.
- **PR #1170's scope, recorded correctly.** It was a **strings-only** self-description refresh (78/16, zero logic) that removed a *phantom* blocker on the drain recourse and left this one untouched — this row must **not** be read as having been narrowed by it.
- **Ordering recorded, NOT acted on.** The cheapest actionable operator-loop improvement is in **none** of the four readings: the drain recourse has **no `resume` ADMIN-CLI command** (the admin surface ships seven commands, none of them resume), so an operator driving pauses to resolution must call the **public package-root Python API `harness_runtime.resume()`** from their own code. **Precision, corrected at out-of-family review round 4 [P2]:** `resume()` **IS a SUPPORTED surface** — it is one of the **three** package-root async functions `B-106`'s own ruling enumerates as the operator-facing API (`run` / `resume` / `read_paused_workflow_state`). What `B-106` ruled **NOT SUPPORTED** is the distinct **embedder** path — direct `run_bootstrap()` / `ctx.pause_resume_protocol` access. The gap here is therefore an **absent CLI affordance, not an absent capability**, and an earlier draft of this bullet conflated the two. This ratification does **not** open that CLI surface.
- **What it is.** The pause journal is **append-only and never truncated** (Runtime spec §14.14.8's v1.107 substrate invariant), and **nothing is written when a resume succeeds** — no resolved-marker, no status field, no removal path anywhere in the store. A journal whose pause was successfully resumed is therefore **byte-indistinguishable** from one whose pause is still outstanding.
- **Why it surfaced now.** Runtime spec **v1.108 §13.7** declares the operator enumeration surface that makes **(3a) abandon-by-default** ratifiable, and council condition `K-10(c)` asks that surface to report *"how much would be LOST"*. **It cannot.** It can only bound the at-risk set **from above**.
- **How the spec leg handled it, and why that is the right disposition.** `K-10`'s **mechanism** was applied unchanged; only its **claim** was scoped, at §13.7 term **3-bis** — the enumeration is an **upper bound**, it cannot discriminate outstanding from resolved, and §13.7 **MUST NOT be cited as proof a cutover is safe**. That is the same discipline `K-1` established for quiescence (state the negative fact rather than a precondition you cannot discharge), applied one condition over.
- **Severity, stated honestly.** **Not a live defect** — nothing miscomputes, and nothing is lost that would otherwise survive. It is a **ceiling on what the operator recourse can tell a human**, and it makes *"drain pauses before upgrading"* a procedure the operator must verify by their own means rather than read off the harness.
- **Close-out — capture-side, therefore fork-shaped.** Any resolved-marker is a write the store does not make today, on a record shape whose hash disposition is load-bearing — the `B-73`/`B-80` split-out discipline that kept the tenant binding itself out of the read-only `B-69` arc. **Two candidate shapes, neither chosen:** **(a)** a **resolved sentinel record** appended at successful resume — cheapest against the append-only invariant, since an append rewrites and truncates nothing and the latest-record read would then report the journal as resolved; it needs a decision on whether that sentinel is a `PauseSnapshot` at all, and on what the **closed five-member cause vocabulary** reports for a resolved journal, which an impl leg must not settle. **(b)** an out-of-band per-journal **marker file** — avoids the record shape entirely but adds a **second authority** over one journal's state, the exact defect the no-fallback-read rule exists to prevent. **Prefer (a) if taken** — but ground it first: check whether an operator holding §13.7's upper bound **plus** their own drain procedure actually needs the discrimination, because a marker bought for a need nobody has is a capture-side change against a hash-adjacent record for nothing.
- **Do NOT bundle into the `B-97`(a) impl leg.** That leg is read-side and keying-side; this is capture-side.
- **Council: ⚖️ NARROWED at the 2026-08-01 Reading D ratification** — from the pre-ratification *"convene only if shape (a) is taken"* to the fork's §7 **per-reading** form, on a probe rather than a preference. **Under the selected D: NONE is owed now** (the record-shape half of C3's position is decided by shipped code — a `PauseSnapshot` sentinel is resumable — and at HEAD no post-cutover flow consumes the distinction), **and ONE IS OWED AT D-0**: a retention policy makes the tension live by turning the absence into a blocker. **If a build reading is later opened:** **A** → a dyadic **C3 ⊥ C11** convening **before the spec leg**, scoped to the sixth-cause-member question (name, semantics, `retryable` / `indeterminate` dispositions) **and to its cross-axis OD leg**, which raises rather than lowers C3's stake; **B** → **no** convening on the record shape (B touches neither it nor OD's mirror), but the §3(v) disposal-brake question still needs an answer at the spec leg — route to **C11** directly plus `advisor()`; **C** → a convening **with C7 or C8 added**, since C introduces an OD-owned schema surface plus a disclosure / tenant question the C3 ⊥ C11 pair does not cover. The tension itself is unchanged and nameable: state/persistence integrity wanting the record shape and the append-only invariant left alone, against operator-loop wanting an answerable *"is anything actually outstanding"* — the same pair the `B-97`(a) dyadic convened.
- **O-3 (lens-1, `B-97`(b) arc, 2026-07-31) — a second, arithmetic bound on the same enumeration.** **Line-count semantics differ across surfaces**: `str.splitlines()` splits on `\x0b`, `\x85` and `\u2028`; `bytes.splitlines()` does not — so two surfaces counting "records" over one journal can disagree. **Unreachable for store-written records** — and the load-bearing property is **`ensure_ascii=True`**, not "`json.dumps` escapes control characters": `_append` calls `json.dumps(record, sort_keys=True)`, leaving `ensure_ascii` at its default **True**, so every non-ASCII char (including `\x85` / `\u2028`) is emitted as an escaped `\uXXXX` sequence. Named precisely because it is an **editable default, not a law** — **seventeen** other modules in this tree pass `ensure_ascii=False` explicitly (counted programmatically across all seven axis packages at this arc — the merge-gate lens said "seven"; the empirical count at HEAD is 17), so the vaguer claim would silently falsify the moment someone added that argument here. Recorded as a bound on the enumeration's arithmetic under **hand-edited or foreign-written** journals, not as a live defect.
- **Cross-ref.** `B-97` half (a) (the arc whose §13.7 surface this bounds); `B-98` (the sibling capture-side pause-carrier gap — gate description absence — same class, same reason it is not absorbed).

### B-106 · Embedder-path event-loop exposure for `capture_pause_snapshot` *(surfaced by the out-of-family confirming round on the `B-103` rework, PR #1172 codex [P2], delivered post-merge, 2026-07-31)*

- **Structural-option preference RESOLVED 2026-08-05 by operator ratification: `run_bootstrap` STAYS EXPORTED in `harness_runtime.__all__`** (`__init__.py:266`, re-exported at `:44`–`:49`). The default stands — the operator-decidable follow-on this row **named but did not absorb** (dropping the package-root export to make the not-supported ruling **structural** at zero call-site cost) is **declined**, on the ground this row already recorded against it: it is a **public-surface narrowing**, major-bump-shaped by the corpus's own version-evolution grammar. The exclusion-defined ruling therefore remains **documentary**, which is what it already was and what the four grounding legs support. This resolves the standing preference; it does **not** reopen the shared `B-103`/`B-106` trigger, which is unchanged.
- **What codex found, after the merge.** The `B-103` premise-falsification is scoped to the SHIPPED call path (`workflow_driver._run_protocol_method_sync` → `asyncio.run` on a private single-task loop in the driver's `to_thread` worker). Codex's confirming round — whose output flushed only after the rework had merged — identified the one path outside that set: `run_bootstrap()` is a public export, `ctx.pause_resume_protocol` is a context field, and an **embedding application** that directly awaits `capture_pause_snapshot(...)` on its own live loop has no private-loop helper in between. Codex measured a 300 ms loop sleep delayed to ~2 s while a second process held the journal lock.
- **Why this is its own row rather than a B-103 reopen.** The `B-103` close_out's claim ("no event loop to starve at any shipped call site") stays literally true; what is genuinely open is whether the embedder surface is *supported* — a scope question, not a mechanism question. The mechanism sits on the shelf: the offload was built, witnessed, and gated at PR #1172's pre-rework commits and applies as-is if grounding rules the surface in. This row carries the decorrelated-reviewer disagreement (lens-1/orchestrator vs codex) on the record instead of silently adjudicating it — per the §13.1 disagreement-is-the-strongest-signal discipline.
- **Close-out.** Ground the embedding contract first; SUPPORTED → the preserved offload + a re-priced cancellation window (the withdrawn `B-105` analysis) against a now-real benefit; NOT SUPPORTED → one documentation sentence and close.
- **CLOSED 2026-08-01 — NOT SUPPORTED. The embedding contract is defined by EXCLUSION, not by silence** — so this is a ruling, not an absence of evidence. Four legs, each re-verified by direct read at head: **(1)** Runtime spec **§4 (`:3093`)** says it verbatim — *"`HarnessContext` is an internal type (consumers reach into specific fields, not the whole context) … The type is not part of any operator-facing surface in Track A."* `ctx.pause_resume_protocol` **is** one of those fields. **(2)** The operator-facing API **enumerates itself** and is exactly three package-root async functions — **§8 (`:3264`)** `run()`, **§30 (`:3363`)** `resume()`, **§14.14.9.1 (`:3456`)** `read_paused_workflow_state()`, introduced as *"exactly one"* / *"a second"* / *"a third async function exposed at the `harness_runtime` package root"*. `run_bootstrap` is not among them; a closed self-counting enumeration is the strongest exclusion this corpus has. **(3)** The contracted external-caller pause route is **designated, and it is not the protocol**: `pause_requested_flag` (**§4 `:3085`** — *"Caller-side pause-signaling primitive. Set by external caller (operator API, MCP tool, etc.)"*; **§14.14.7 `:5897`** leaves that flag-setting surface to implementation discretion). The caller **signals**; the **driver** captures. **Grounded sharpening (out-of-family round 1, [P2], accepted on the source rather than argued down):** no external setter is *exposed* today — the sole in-repo production setter is the internal HITL gate composer (`lifecycle/hitl_gate_composer.py:1329`); every other setter is a test reaching through `ctx`. That is not a hole in the ruling, it is the ruling's own evidence: the designated external route is *deliberately still at discretion*, which is exactly what "the embedding surface has not been minted yet" looks like from the inside. The embedder path is therefore a *second, uncontracted* route to something the contract routes elsewhere **and has not yet opened**. **(4)** No embedding surface exists to be in scope: a full scan of `design-substrate/**.md` finds **zero** library-embedding text (every `embed*` hit is the unrelated CP **EMBEDDING routing layer** or *"embedded credentials"*), and the committed deployment surfaces are the **ADR-D2 v1.2 three-tier** set — local-development / self-hosted-server / managed-cloud (`CLAUDE.md` §10.2) — with no library-embedding tier. **Precedent: `B-69`** is the controlling *how-surfaces-get-minted* rule — a surface is **minted by a ratified spec leg**, never inherited by an internal field that happens to be reachable. Minting one here would be an X-AL-3 silent design extension.
- **The mechanism half is real and is not explained away.** `DurablePauseResumeProtocol.capture_pause_snapshot` calls `self._store.capture(snapshot)` **synchronously** inside the async method (`lifecycle/durable_pause_resume_protocol.py:124`), and the `flock` inside would block a live loop — codex's 300 ms → ~2 s measurement stands. It is a measurement of a path **outside the contract**. The **daemon was checked** rather than assumed: its captures also route through `workflow_driver._run_protocol_method_sync`'s private single-task loop, so `B-103`'s falsification survives it and the embedder path is the **sole** live-loop route — which is why ruling it out-of-contract closes the whole question.
- **What shipped: documentation only, zero logic** (impl-discretion — it restates the cleared §4 invariant and mints nothing). An **AUDIENCE** paragraph on `run_bootstrap`'s docstring (`bootstrap/__init__.py`) naming the internal-type ruling, the three contracted functions, the caller's own-loop obligation and the `pause_requested_flag` route; and the concrete blocking-`flock` fact on the `pause_resume_protocol` field comment (`types.py`). **No spec text owed** — confirmed by `B-103`'s own recorded confirmation for the mechanism half (§14.8.1's *"The wrap chain above pins composition, not execution venue"*), and because this row adds no contract, config scalar, failure class or observable surface.
- **The `B-103` offload STAYS SHELVED** (preserved at PR #1172's pre-rework commits, `3636762c`-era head of `b103-off-loop-journal-lock`). `B-103`'s **reopening trigger is unchanged** — a future arc replacing `_run_protocol_method_sync`'s `asyncio.run` with a **captured-loop bridge** (`:2598-2601`). **Added here: re-check `B-106` alongside `B-103` at any change to that helper** — if the bridge lands, the **shipped** path joins the embedder path in being loop-co-resident, the harness acquires an **in-house** benefit from the offload, and the SUPPORTED branch reopens on *that* benefit rather than the embedder's. One trigger, two rows, evaluated together.
- **Named, not absorbed — the structural option, an OPERATOR-DECIDABLE follow-up.** `run_bootstrap` sits in `harness_runtime.__all__` (`__init__.py:266`, re-exported at `:44-49`) with **zero in-repo package-root consumers**: 215 occurrences across 40 files, and **every** import site is `from harness_runtime.bootstrap import run_bootstrap` (28 import lines; the two non-test callers, `api.py` and `cli/app.py`, are intra-package). Dropping the package-root export would make this ruling **structural at zero call-site cost** — an embedder would have to reach into a private submodule to obtain the context at all. It is **not absorbed here** because it is a **public-surface NARROWING**, major-bump-shaped by the corpus's own version-evolution grammar (**§9 `:3343`** — *"Removing a field is always a major bump"*; **§4 `:3093`** — *"field-removals or type-changes are major (consumers break)"*), and because the spec itself references `harness_runtime.run_bootstrap(...)` illustratively at `:6262` (inside an implementation-discretion note — not a contract, but not nothing). A major-bump-shaped narrowing of a public surface is an **operator** call, not an impl-discretion one. **Default = leave the export**; the docstrings carry the ruling without it.
- **Cross-ref.** `B-103` (shares the one reopening trigger; its offload is the shelved mechanism); `B-105` (withdrawn with that offload); `B-69` (the how-surfaces-get-minted precedent); `B-97`(b) (introduced the lock).

### B-107 · No channel REFUSES an operator resolution addressed to an EMPTY effect-fence key — the response map and the scalar uniform fallback DROP it on all three carriers, and the level-local abort scans MIS-APPLY it to a keyed sibling *(surfaced at the `B-100` spec leg, out-of-family review rounds 3 + 8, reported not absorbed, 2026-07-31; the over-application half established at the 2026-08-01 grounding pass)*

- **CLOSED 2026-08-04 — U-CP-64 AC #A12 implementation LANDED; both X-AL-2 conjuncts satisfied.** **(1) ACs landed.** Three amendment sites, all in `harness-cp`: **scalar (§1.1)** — `compute_effect_fence_uniform_fallback_eligible_key` filters empty captured keys out of the candidate set *before* the sole-unaddressed computation, so the uniform fallback can never nominate `""`; the filter now matches the one `compute_effect_fence_tree_wide_abort_present` already shipped, so the two agree **by construction** rather than the downstream sibling compensating. **map (§1.2)** — `ResumeContext.effect_fence_resolutions` refuses empty keys at ordinary construction and stores a validated immutable **copy** (`_ImmutableEffectFenceResolutions`: every mutation method refused, `__slots__`-based one-shot seal, explicit `__reduce__`), inherited by `AccessorDerivedResumeContext` through **both** construction entry points; the declared type stays `dict[str, EffectFenceResolution]` and dumps stay byte-identical in both modes (§0.3 preserved). **resolver (§1.3)** — `_resolve_effect_fence_gated` refuses an empty `idempotency_key` **before** the map-hit check and **before** the uniform-eligibility comparison, making a caller-supplied `effect_fence_uniform_fallback_eligible_key=""` powerless and `model_construct` content inert per §1.5. ZERO capture-side change, ZERO `snapshot_hash` impact, ZERO contract numbers, ZERO Runtime/CXA delta; `PauseTreeWalkEntry.effect_fence_key` still surfaces `""`; `B-101` untouched.
- **(2) Witnesses green.** New `harness-cp/tests/test_workflow_driver_effect_fence_empty_key_b107.py` (29 tests) covers all five AC #A12 clauses: the **eight-cell grid** (map + scalar × LINEAR / PARALLELIZATION branch / ORCHESTRATOR_WORKERS worker / ORCHESTRATOR-own, branch+worker cells in **both carrier orders** through real captured pauses; the two ORCHESTRATOR-own cells are constructed-snapshot classification tests, labelled **in-test** as *not* §1.3 closure evidence because the shipped orchestrator consult is truthiness-gated — §1.3 discrimination binds at the LINEAR site and the two **ungated** level-local `_any_fence_abort` branch scans); the scalar-removal witnesses plus the direct caller-supplied empty eligible-key case; **four PD-8 probes** (validation / copy / immutability / serialization-and-clone compatibility); `model_construct` inertness; and the **programmatic reader inventory** re-derived at run time — **seven** resolver call sites (`compute_effect_fence_tree_wide_abort_present` ×1, `_execute_workflow_body` ×1, `_execute_parallelization` ×2, `_execute_orchestrator_workers` ×3) and **two** non-test terminal key-match consumers, matching this row's BINDING RULE inventory exactly. The **b80 flip is ratified, not drift**: `test_map_addressed_keyed_abort_activates_alongside_an_unrelated_keyless_pause`'s final assert goes `False → True` (fork §11 item 1) while its sole-keyless and two-keyed siblings stay `False`. **Six mutation probes**, each reverted then restored, every one producing a NAMED failing test.
- **Three out-of-family codex rounds to convergence.** Round 1 [P2] caught a real *unauthorized* compatibility regression — a `MappingProxyType` field broke `deepcopy` / `model_copy(deep=True)` / `pickle` for **any valid map**, which §0.3 does not authorize — fixed by adopting the settled `_ImmutableKeyArns` dict-subclass precedent. Round 2 [P2]: inherited `dict.__init__` re-populated the sealed map in place → sealed. Round 3 [P2]: the seal itself was reopenable by plain attribute assignment → moved behind `__slots__` + a refusing `__setattr__`, with the **termination point** of that hardening line stated explicitly in the type's docstring (routes that *name a base class* are the same boundary §1.5 draws for `model_construct`).
- **One round-2 finding DECLINED with grounds, recorded not absorbed.** Extending the empty-key rule to the public `ResumeContext.effect_fence_resolution_for` lookup: fork §11 item 5 names `_resolve_effect_fence_gated` as *the* site; §1.3's wording (*yields no **directive***, *before a map-hit or **uniform-eligibility** comparison*) describes that function and not the pure lookup; and spec v1.115 §0.1/§3 enumerate exactly **three** amendment sites, so narrowing a method published at v1.66 would be a **fourth** — an X-AL-3 silent design extension at impl time. Safety does not rest on that reading: a pin test walks **every** `harness-*/src` tree and asserts the method has exactly **one** production caller (the now-guarded resolver), so a second caller — or a later ratification — breaks there instead of drifting.
- **Observed, NOT fixed (out of surgical scope, reported upward).** The sibling `_ImmutableKeyArns` at `harness-runtime/src/harness_runtime/types.py` carries the identical inherited-`dict.__init__` hole.
- **Verification.** `just check` exit 0 at the final tree — ruff clean, pyright strict 0/0/0, full suite green; `just overlay-check` OK — 397 nodes, 36/36 seams wired.
- **RATIFIED 2026-08-03 — current disposition, superseding the historical pre-ratification narrative below.** Reading A-hybrid is applied in CP spec v1.115 and plan v2.49: scalar eligibility excludes an empty captured key; map construction refuses an empty key and stores a validated immutable **copy**; the resolver treats an empty key as unresolvable before map-hit and eligibility checks. The b80 `False → True` consequence is ratified; the eight-cell grid, PD-8 mutation probes, validation-bypassed inertness, direct empty scalar-parameter witness, seven resolver sites, and two terminal consumers are the separately owed U-CP-64 implementation remainder. **B-101(a) is NOT jointly discharged** — this map-field change pays the same compatibility-cost category but does not add B-101(a)'s closed variant discriminator; B-101 stays separately closed under (b)-PLUS with its promotion trigger unchanged, and the rows remain separate. No Runtime §30 delta, no CXA §2.3 row, and no council are owed. The 4 variants / 10 source shapes / 7 carriers remain unchanged; B-107 is **OPEN, NOT CLOSED**. The 2026-08-01 transit note's "Class 1 design-substrate change, fork doc OWED" was the PRE-FILING FORECAST of this gate; the actual instrument is the CLASS 2 fork `.harness/class_2_fork_b107_empty_fence_key_resolution_refusal.md` (filed at PR #1178, operator selection between Readings A-hybrid/B/C/D per CLAUDE.md §4.3 Class 2 — the B-97(a)/B-96/B-98/B-104 filing-shape precedent), whose §11 ratification authorizes this apply pass; no Class 1 doc exists or is owed.
- **CROSS-REF 2026-08-01 — the `B-100` IMPL leg landed and DELIBERATELY did not touch this row's surfaces.** Verified against that diff, which is confined to `harness-cp/src/harness_cp/pause_state_projection.py`. **Both channels this row tracks are unchanged:** the MAP channel (`ResumeContext.effect_fence_resolutions` is still `dict[str, …]` with unconstrained keys) and the SCALAR channel (`compute_effect_fence_uniform_fallback_eligible_key` still does not filter empty keys). What `B-100` removed is the **ADVERTISEMENT** on all three carriers — an operator composing from the projection now has no empty key to paste on *any* of them, where before the LINEAR and orchestrator shapes surfaced one. That leg also **deliberately preserved** `PauseTreeWalkEntry.effect_fence_key == ""` for every key-absent shape (mutation-probed there), so **this row's site inventory is unchanged by it and no classification rule moved**. Read this row's own close-out, not that leg, before selecting a disposition — and honour its binding rule: **re-derive the site set programmatically** before choosing.
- **STATUS TRANSIT 2026-08-01 — `registered_finding` → `design_substrate_gated`** *(out-of-family review round 4 [P2])*. The enum at `.harness/forward-register.yaml`'s header defines `registered_finding` as a gap that **opens for BUILD once grounding confirms a genuinely buildable slice**; this grounding confirms there is **none** — no existing contract term supports a build-now refusal, and even the separable `_any_fence_abort` slice owes a v1.111 §2 reconciliation — so the row moves to the gate that actually blocks it: **a Class 1 design-substrate change, fork doc OWED** (not yet filed, not yet ratified).
- **GROUNDED 2026-08-01 (dedicated grounding pass; every cite below re-verified by direct read at head `c1afb225` — NO anchor moved). VERDICT RECORDED: SPEC-LEG-FIRST, and the spec leg is itself FORK-SHAPED.** **No existing contract term supports a build-now refusal.** **(i)** `Spec_Control_Plane_v1_107.md` §1.1's unaddressed-set membership **INCLUDES `""`** — membership is `not in resolutions` over an enumeration that keeps `""` **by design** (`workflow_driver.py:2965`–`:2967`; §1.1 words it *"whose own `idempotency_key` is NOT a key in `effect_fence_resolutions`"*), and `Spec_Control_Plane_v1_112.md` §2.1 states it outright — *"the authoritative effect-fence walk **includes** the empty key, the uniform-fallback computation **counts** it, and only the abort walk filters it."* **(ii)** The **"cannot address"** language (`Spec_Control_Plane_v1_113.md` §1.2, the amended `effect-fence-addressable` row of v1.112 §2.1's projection table) is **PUBLICATION**, and `Spec_Control_Plane_v1_112.md` §2.4 disqualifies it as a rule source in its own words — *"§2 publishes; it does not decide … MUST NOT become a second place where classification semantics are stated."* **(iii)** `Spec_Control_Plane_v1_114.md` holds this row and `B-101` **separate** (§0.5 finding (v)). **EMPIRICAL PIN:** disposition (a) **flips** `test_map_addressed_keyed_abort_activates_alongside_an_unrelated_keyless_pause` (`harness-cp/tests/test_workflow_driver_effect_fence_tree_wide_abort_b80.py:222`–`:257`, the `:254` `is False` assert) — filtering the keyless candidate leaves the **keyed** location solely eligible, so the uniform `ABORT` reaches it and that assert becomes `True`. **It is the executable pin of the rule (a) would move** — which is why (a) is a spec amendment and not an impl tweak.
- **CORRECTED SEVEN-SITE MAP 2026-08-01 — the round-11 `(c)` claim was FALSE for the BRANCH carrier and is corrected in place below.** The claim *"a refusal inside `_resolve_effect_fence_gated` MISSES the branch and orchestrator carriers entirely, because their consults SHORT-CIRCUIT on the truthiness guard"* is contradicted **by this row's own round-11 site finding**: sites `:7839` and `:11708` (the level-local `_any_fence_abort` scans) call the resolver with `""` **UNGUARDED**. There are **exactly seven** `_resolve_effect_fence_gated` call sites at head:
  1. `:3056` — **GUARDED** by the `if k` filter at `:3053` (`compute_effect_fence_tree_wide_abort_present`).
  2. `:4926` — **LINEAR consult, UNGUARDED** (`resume_snapshot.effect_fence_resume.idempotency_key` passed as-is).
  3. `:7839` — **fan-out level-local `_any_fence_abort` scan, UNGUARDED** (`:7841` iterates `_recovered_effect_fence_paused.values()` with no filter).
  4. `:8216` — **GUARDED** (`if (_branch_fence_key and _ef_resume_ctx is not None)`).
  5. `:11708` — **orchestrator-workers level-local `_any_fence_abort` scan, UNGUARDED** (`:11710`, no filter).
  6. `:12118` — **GUARDED**; the **ORCHESTRATOR's own** carrier short-circuits (`if (_orch_fence_resume.idempotency_key and …)`).
  7. `:12554` — **GUARDED** (`if (_branch_fence_key and …)`).
  **Consequence for `(c)`:** a single-function refusal **DOES** reach the **branch/worker** carrier's empty keys (sites 3 + 5) and **does NOT** reach the **orchestrator's own** carrier (site 6 is its only consult, and it is guarded). *"Misses both entirely"* was wrong on the branch half; *"misses the orchestrator entirely"* stands.
- **NEW FINDING (i) 2026-08-01 — the `any()` is POSITION-DEPENDENT, an independent reason the single-function refusal shape is unsound.** `_recovered_effect_fence_paused` is a dict built in carrier order off `effect_fence_paused_branches`, and that tuple is `tuple(sorted(keyed dispositions)) + crash_pause_reconstruct_fence_paused` (`:10280`; the orchestrator-workers twin at `:14639`) — **the crash-reconstruction `""` entries are appended LAST**. A keyed sibling resolving `ABORT` therefore **short-circuits the `any()` before the `""` entry is ever evaluated**, so a resolver-internal refusal would fire or not fire depending on **sibling resolution order** rather than on the presence of the empty key. **A refusal whose firing depends on iteration order is not a refusal.**
- **NEW FINDING (ii) 2026-08-01 — a FOURTH terminal consumption site this row had not named.** `managed_agents_dispatch.py:300` performs the **identical** key match to `runtime_tool_dispatcher.py:1042` (`step_context.effect_fence_resolution.idempotency_key == idempotency_key`). A programmatic sweep of the non-test tree finds **exactly these two** terminal key-match sites. This is the binding rule below doing its job, not an exception to it.
- **CLOSURE-CRITERION FLAG 2026-08-01 — the criterion as written UNDER-COVERS the worst case.** The `{"": ABORT}` suppression is **OVER-application, not a drop**: a resolution addressed to `""` reaches a **DIFFERENT** location's decision at `:8238` / `:12576` (the run-level ABORT guard suppresses a keyed sibling's valid directive) — **exactly the cross-location misattribution `Spec_Control_Plane_v1_107.md` §1.1(a) forbids** *("never a cross-location misattribution of a SKIP_AS_FIRED/RE_FIRE/ABORT/ABORT_BRANCH judgment intended for a different location")*. But the criterion below says the row closes iff no channel delivers a resolution *"without either applying it or REFUSING it"* — and this case **DOES apply it**, at the wrong location. **Literally read, the criterion is satisfied by the defect.** **The widening is APPLIED at this pass** rather than left to the opening arc *(out-of-family review round 1 [P1])* — see the amended **Closure criterion** below.
- **The separable `_any_fence_abort` slice is NOT free impl discretion** *(2026-08-01)*. The two-line `if _k` filter at `:7841` / `:11710` looks self-contained, but `Spec_Control_Plane_v1_111.md` §2.1(b) requires the level-local guard be combined with the tree-wide signal as *"a strict widening of when suppression fires, never a narrowing"* — and filtering empty keys **out** of the level-local scan is a **NARROWING**. **Even that slice owes a stated reconciliation, plausibly a v1.111 §2 amendment.** It **could** nonetheless ship as an **independent narrow leg before the (a)/(c)/(d) fork settles** — it is the one piece whose correctness does not depend on the fork's outcome.
- **THE FOUR FORK DECISIONS the eventual filing must put to the operator** *(2026-08-01)*.
  1. **(a)+(d) vs (c).** Two dispositions (one per channel) versus the one that converts DROP into REFUSE everywhere. Not a preference — they amend different things (a CP classification rule vs a resume path's failure mode vs a `ResumeContext` field's type).
  2. **Fail-loud vs INERT** — the **C9**-shaped tension this row's own council note already predicts. The bar is `Spec_Harness_Runtime_v1.md` §30 term 4: *"a refusal the operator cannot act on is a livelock with good manners."* A refusal on an empty key is worth raising only if the operator has something to re-read and recompose from.
  3. **Seam placement, RE-DECIDED against the corrected map.** The close-out's *"cheapest sound placement is an ADMISSION-TIME check"* was chosen against the **false** map. Admission via `api.py` (where `_enforce_pause_state_staleness_precondition` sits, `:823`) is **BYPASSABLE**: `execute_workflow` is a public export of `harness_cp.workflow_driver` (its `__all__`), so a caller can reach the consults without transiting `api.resume()`. **The non-bypassable chokepoint is `execute_workflow` itself** (`workflow_driver.py:3061`–`:3076`), which **already receives** `pause_snapshot_input`, `resume_context` **and all three root-computed signals** as parameters — every route (`api.resume` via `mcp_server`, a direct public caller, and the recursive child re-entry at `child_workflow_runner.py:261`) transits it. *(CORRECTED 2026-08-01 at out-of-family review round 1 [P1]: an earlier draft of this bullet claimed `mcp_server.py:378`–`:403` was "the one place both objects exist together" — that is **false**, and it would have left direct `execute_workflow` callers unfenced.)* **The real discriminant is TREE SCOPE, not reachability:** `execute_workflow` re-enters **per recursion level** with a **narrowed** `pause_snapshot_input`, so a check placed there sees only its own subtree, whereas `mcp_server.py:378`–`:403` is the **one** site holding the **un-narrowed depth-0 root** (precisely why the three tree-wide signals are computed there once and threaded down unchanged). **But the depth-0 site is not a safe home either** *(out-of-family review round 2 [P1])*: `mcp_server.py:378`–`:403` is merely where those signals **happen** to be computed today, and it is bypassed by a direct `execute_workflow` caller **exactly as `api.py` is**. The **outer** `execute_workflow` invocation itself receives the caller-supplied, **un-narrowed** `pause_snapshot_input` — narrowing happens only on **re-entry** (`child_workflow_runner.py:261` passes the child cursor snapshot) — so **both** a per-location **and** a tree-wide check can be homed at, or explicitly propagated from, the **driver entry**, which is the only non-bypassable one. Discriminating the outer entry from a recursive re-entry is itself part of the fork's design work (the driver already carries `sub_agent_descent` and receives the three root-computed signals as parameters; whether either is a sound discriminator must be **grounded, not assumed**). **What IS settled: any placement that leaves a direct `execute_workflow` caller unfenced is not a candidate.**
  4. **Orchestrator scope is TYPE-LEVEL ONLY.** That carrier is **production-unreachable** (its one construction site's guard chain at `:12376`–`:12381` ends in a truthiness test), so its witnesses MUST be **constructed-snapshot** detect-then-refuse tests per the `B-100` precedent (recorded at CP plan v2.48 AC #A11 / Runtime plan v2.57 AC #16(a)) — never an e2e run.
  - **WITNESS SHAPE — CORRECTED at out-of-family review round 2 [P2]** *(a plain 2 × 3 grid under-specifies it)*. The **branch carrier has TWO independent topology sites**, each with its own `_any_fence_abort` scan and its own suppression consumer — **PARALLELIZATION** at `:7839` / `:8238` and **ORCHESTRATOR_WORKERS** (HIERARCHICAL_DELEGATION reuses it per `Spec_Control_Plane_v1_111.md` §2.2) at `:11708` / `:12576` — so a matrix keyed on *carrier* alone can be satisfied by exercising one of them while the other retains the cross-location suppression. **The grid is 2 channels (map, scalar) × 4 carrier-SITES (LINEAR; PARALLELIZATION branch; ORCHESTRATOR_WORKERS worker; the orchestrator's own carrier) = EIGHT cells**, each branch cell exercising an empty-key location **alongside a keyed sibling**, and the two orchestrator cells constructed-snapshot.
- **The `(d)` / `B-101`(a) shared-obstacle cross-ref is UNCHANGED**, as recorded at `Spec_Control_Plane_v1_114.md` §0.5 finding (v): the same `ResumeContext` field-set-preserved-verbatim obstacle would discharge both, and **the rows stay SEPARATE** — no merge is proposed and none is performed.
- **ID note — TWO collisions avoided.** This row is **`B-107`**. It is **not `B-105`** — that id was minted and **withdrawn** at PR #1172 (the `B-103` premise-falsified rework; see `.harness/roadmap_status.md` and `.harness/merge-gate-log.md` row #1172), so reusing it would make register lookup and the historical record resolve to two different issues *(caught at out-of-family review round 5 [P1])*. And it is **not `B-106`** — that id was minted **concurrently on `main`** at commit `d9f68b02` (the embedder-path event-loop exposure) while this leg was in flight; renumbered at the merge rather than contested, since that row landed first.
- **What it is.** An **empty captured `idempotency_key`** is reachable on the durable effect-fence carriers, and **no channel REFUSES an operator resolution addressed to it**. **TWO failure modes, not one** — the second **established at the 2026-08-01 grounding pass** (below): most routes **DROP** it silently, and the level-local `_any_fence_abort` scans **MIS-APPLY** it, since a `{"": ABORT}` suppresses a **keyed sibling's** valid directive at `:8238` / `:12576` — the cross-location misattribution `Spec_Control_Plane_v1_107.md` §1.1(a) forbids. **Two channels, and `B-100` closes neither by type.**
- **(1) The MAP channel.** `B-100`'s projection change removes the **advertisement** — an operator composing from the projection has no key to paste — but it does **not** make the state unrepresentable: `ResumeContext.effect_fence_resolutions` is a `dict[str, EffectFenceResolution]` with **unconstrained keys**, so a caller acting outside the projection can hand-build `{"": ...}` (directly or through `AccessorDerivedResumeContext`), and `_resolve_effect_fence_gated` treats `""` as a mapped resolution. **A read-side projection contract cannot close this** — only a key constraint on the CP-owned map, or a refusal at the consult seam, can. *(Round 8 [P2].)*
- **(2) The SCALAR channel.** `compute_effect_fence_uniform_fallback_eligible_key` (`workflow_driver.py:2935`–`:2970`) does **not** filter empty keys — unlike its sibling `compute_effect_fence_tree_wide_abort_present`, whose `candidate_keys` comprehension at `:3054` does — so when a key-absent location is the **sole** unaddressed effect-fence location, that function returns `""` as the eligible key and `_resolve_effect_fence_gated` accepts the caller's **scalar** `effect_fence_resolution` for it. *(Round 3 [P2].)*
- **The terminal loss differs by carrier, and ALL THREE are in scope** *(scope corrected at round 8 [P2]; an earlier draft was LINEAR-only)*. At the **LINEAR** consult (`:4925`–`:4936`) the resolution IS looked up and threaded as `EffectFenceResolutionDirective(idempotency_key="")`, which the runtime dispatcher never matches (`runtime_tool_dispatcher.py:1042` vs the key composed at `:989`). At the **fan-out per-branch** and **orchestrator** consults the truthiness guard means it is never consulted at all. **Different mechanism, identical outcome — a silent INERT re-pause with no diagnostic.**
- **Reachability, per carrier.** The **BRANCH** carrier reaches `""` **deliberately** at crash reconstruction (`:4074`) *and* defensively through the ordinary fan-out coercion (`:9625` / `:9752` / `:14024` / `:14149`) — **a production shape, not a defensive-only one**. The **LINEAR** carrier reaches it defensively (`:5456`). The **ORCHESTRATOR** carrier does **not** reach it at all (truthiness-guarded at `:12376`–`:12381`), so its exposure is **type-level only**.
- **Why it is NOT a defect in the `B-100` leg.** Read-side projection scoping is the whole of what disposition (a) authorizes, and **a projection can constrain neither a scalar field nor a map the caller populates independently of it.** Surfaced rather than absorbed, per the same X-AL-3 discipline that produced `B-100` itself.
- **Close-out — FOUR candidate dispositions, none subsumed by `B-100`'s remainder, and TWO needed for full closure (one per channel) unless (c) is taken.**
  - **(a) Resolver-side — closes the SCALAR channel only.** Filter empty keys out of `compute_effect_fence_uniform_fallback_eligible_key`'s candidate set, exactly as its sibling already does at `:3054`, making an empty-key location *unaddressed by construction*. It changes a **CP classification rule**, so it needs a §1.1(a) spec amendment. **Liveness, grounded rather than assumed** *(an earlier draft claimed a liveness COST; round 4 [P2] falsified it)*: **neutral** in the lone case — the run already re-pauses INERT today — and **strictly more live** in the mixed case (one empty + one keyed), where today the 2-member unaddressed set blocks *both* while filtering leaves the keyed location solely eligible. **But it removes a false acceptance without producing a refusal, and does not touch the map channel.**
  - **(b) Capture-side.** `B-100`'s own declined disposition (b); still hash-adjacent. **It does NOT subsume this row** *(corrected at round 8 [P2])*: refusing at `:5456` leaves the **branch** carrier's empty key untouched, and that one is **deliberate** (crash reconstruction constructs it at `:4074` precisely because CP cannot derive the runtime fence key), so it would have to be **redesigned**, not merely refused.
  - **(c) Refuse-at-the-seam.** Raise rather than silently skip or thread when a resolution is addressed to an empty key. **The only option that converts DROP into REFUSE.** **But its seam is not one function** *(corrected at round 11 [P2]; **CORRECTED AGAIN 2026-08-01** — the round-11 sentence was itself FALSE for the BRANCH carrier)*: placing the refusal inside `_resolve_effect_fence_gated` alone **misses the orchestrator's own carrier entirely** (site 6, `:12118`, its only consult, truthiness-guarded) but **DOES reach the branch/worker carrier**, whose level-local `_any_fence_abort` scans at `:7839` / `:11708` call the resolver with `""` **UNGUARDED** — per the corrected seven-site map above. Full coverage needs **either** an earlier validation seam that sees the location before any consult is skipped — resume-context admission, or the collector — **or** a per-consult refusal at each of the three sites. The cheapest sound placement is an **admission-time** check, which also subsumes (d)'s map channel. It changes a resume path's failure mode either way. **RE-OPENED 2026-08-01:** that placement was chosen against the **false** map *and* against an `api.py` admission point that the public `harness_cp.workflow_driver.execute_workflow` export bypasses — see fork decision 3 above.
  - **(d) Constrain the map key.** Give `effect_fence_resolutions` a min-length-1 key type so `{"": ...}` is unrepresentable. **Closes the map channel by TYPE** — the strongest form — but it is a shape change to a `ResumeContext` field CP spec v1.112 §0.4 guarantees **preserved verbatim**, so it needs its own byte-compat argument (the same obstacle `B-101` records for that carrier).
- **Closure criterion — WIDENED 2026-08-01 across TWO review rounds.** *(Round 1 [P1] required the widening be applied here rather than deferred, since the prior wording was literally satisfied by the defect; round 5 [P2] found the first widening **still permitted an empty key to be HONOURED as an address locally** — which contradicts the **adjudicated rule** settled at the `B-69` / `B-100` records and published at `Spec_Control_Plane_v1_113.md` §1.2, that an empty fence key is **position-only** and the resume path **cannot address it**.)* `B-107` closes iff, **on every channel and every carrier**, an empty-key location is either **(i) removed BY CONSTRUCTION from the addressable/eligible set**, or **(ii) REFUSED** — and **never (a)** silently **dropped**, **nor (b) HONOURED as an address** (a resolution keyed to `""`, or a scalar uniform value threaded at an empty-key location, is a forbidden outcome *even when it affects nothing else*, since the directive it produces carries `idempotency_key=""` and no runtime fence can ever match it), **nor (c)** permitted to **alter another location's decision** (the `{"": ABORT}` over-application at `:8238` / `:12576`) — witnessed **per channel** (map, scalar) and **per carrier-SITE** (see the witness shape above). **Honouring an empty key locally, and mis-application at a different location, are both non-closure, exactly as silent drop is. Partial is non-closure. Ground (a)+(d) against (c) before opening any of them.**
- **Enumeration is bounded BY RULE, not by the list above** — an explicit **review-time-inventory** note, since four successive out-of-family rounds each found one more empty-key consumption site (the scalar eligibility computation and the LINEAR runtime key-match at round 3; the **level-local `_any_fence_abort` scans** at `:7838`–`:7842` / `:11707`–`:11711`, which do **not** filter empty keys and let `{"": ABORT}` suppress a keyed sibling's valid directive at `:8238` / `:12576`, at round 11; the truthiness short-circuits at round 11) and a fifth is not ruled out. **THE BINDING RULE: every site that reads a fence key off a resume carrier or off `resume_context`, on any path, is in this row's scope whether or not it is named here.** The sites listed are the inventory **as of 2026-08-01** — extended by that day's grounding pass with the corrected seven-site resolver map and the fourth terminal consumption site (`managed_agents_dispatch.py:300`), both above — not the definition. **The arc that opens `B-107` MUST re-derive the site set programmatically** from the carriers and from `resume_context`'s two fence surfaces before selecting a disposition; a disposition chosen against this list alone is unsound by construction.
- **Council — CONDITIONAL as of 2026-08-01, revised from "no"** *(out-of-family review round 4 [P2])*. The grounding pass **elevates** the disposition-(c) **fail-loud vs INERT** question from hypothetical (*"if a genuine tension appears"*) to a **named fork decision the filing must put to the operator**, which trips the workspace's nameable-tension discriminator. **A dyadic convening is therefore OWED at the arc that opens (c)** — **C9** (reliability: a silent INERT re-pause is an undiagnosable livelock) against the operator-actionability half `Spec_Harness_Runtime_v1.md` §30 term 4 states as the bar (*"a refusal the operator cannot act on is a livelock with good manners"*), for which **C11** (operator loop) is the natural second voice. **Probe-first still applies:** if a direct read of the §30 refusal battery settles whether an empty-key refusal is operator-actionable, record it as *probe-resolved* rather than convening. **The prior text, still accurate on its own terms and retained:** No, **and no reopening trigger is attached.** The adjudicated rule (*an empty fence key must never be surfaced or honoured as an address*) is settled at the `B-69` / `B-100` records; this row asks only **which seam enforces it**. *(An earlier draft attached a reopen-if-(a)-is-taken trigger on a claimed liveness cost; that cost was falsified above, so the trigger is removed rather than left to fire a council gate on a stale rationale — round 5 [P2].)* If a genuine tension appears it is the disposition-(c) **fail-loud vs INERT** call, a C9-shaped liveness-vs-diagnosability question belonging to whichever arc opens (c).
- **Cross-ref.** `B-100` (the leg that surfaced it; its impl remainder is the adjacent open work), `B-69` (the arc whose projection contract both rows sit under), `B-101` (the same `ResumeContext` preserved-verbatim obstacle disposition (d) would hit).

### B-108 · §14.8.11's typed report-log term has NO sink the HARNESS configures — absent host/operator logging configuration every report-log line reaches stderr and dies with the process *(surfaced by out-of-family Codex round 2 on the `B-96` council record, independently re-verified and CONCEDED by C7; scoped at round 3, 2026-08-01)*

- **What it is.** `Spec_Harness_Runtime_v1.md` §14.8.11 (`:4909`) requires that TTL expiry *"surfaces as a TYPED report-log line"*, and the protected result store ships four such emissions (`protected_result_store.py:830`, `:839`, `:849`, `:855`). **The harness configures no logging sink of its own — and that, precisely, is the claim; it is NOT that no sink can exist.** *(Scoped at round 3, out-of-family [P2]: the grep below proves only that the **harness** installs no handler. `harness.*` loggers set no `propagate = False` anywhere — verified: zero occurrences in production source, so the stdlib default `True` holds — which means a HOST embedding the harness, or an operator configuring logging around the CLI, CAN attach a handler to the root or to `harness.*` and capture these lines durably. The finding is therefore about the STOCK, unconfigured deployment, and it is exactly consistent with the `B-96` record's condition #12 term that carrier durability is deployment-configured. The original "any deployment shape" framing over-claimed and is withdrawn.)* A whole-tree scan of **all seven** production `src` trees (`harness-{runtime,core,is,as,cp,od,cxa}/src`) for `basicConfig` / `dictConfig` / `fileConfig` / `addHandler` / `logging.config` / `StreamHandler` / `FileHandler` / `configure_logging` / `setup_logging` / `handlers[` / `lastResort` returns **ZERO matches** — re-run programmatically at this filing, not carried from the reviewer. All **30** emission sites live in `harness-runtime/src` and sit on the `harness.*` logger hierarchy, so with no handler attached anywhere on that hierarchy or on root, every line falls through to Python's `logging.lastResort` handler: **WARNING+ to process stderr, and nowhere else.**
- **How it surfaced.** Out-of-family Codex round 2 [P1] against the `B-96` council record (`.harness/council-b96-grace-ceiling-2026-08-01.md` §11.2b, finding **C2-2**) falsified C7's `[MODERATE]`-tagged premise that *"the report log survives process exit"* — the step that made a **trend** diagnostic (compare successive grace-reset emissions) viable. **C7 conceded without reservation** in its round-2 response, re-verifying the grep independently and noting that the inference had been available to it in round 1 (it had already established there is no OTel log bridge) and was not drawn.
- **Scope — workspace-wide, not `B-96`-specific.** It is a property of **every** `logger.*` call in the harness. It applies **identically at HEAD, under `B-96`'s C-1 and under its C-2** — which is exactly the orthogonality discriminator that council used to route the write-driven-cadence gap out of its filing, applied consistently here.
- **Severity, stated honestly.** **Not a live data-loss defect** — nothing miscomputes, no payload is lost, and the store's own durability is untouched. What is absent is the **forensic surface the spec term names**: the report log is a **timely notification on stderr**, not a durable measurement. In the one-shot process shape nothing accumulates across runs, so any diagnostic requiring comparison of successive emissions is unavailable in the stock deployment.
- **What `B-96` already absorbed, so this row is not double-counted.** The **claim** half is closed by that council record: condition **#12** now states that carrier durability is **deployment-configured and MUST NOT be assumed**, condition **#6** demotes its own emission to timely notification, and condition **#8(b)** relocates the *authoritative* diagnostic onto an on-demand `harness-inspect` pull surface with **no configuration dependency** (C7's principle: *measure the fault on the substrate the fault preserves*). **This row carries only the GAP half — whether the harness owes a default sink at all.** That question is untouched by any `B-96` leg.
- **Close-out — ground first; three candidate shapes, none chosen.** **(1) Default handler at the CLI entry** — configure the `harness.*` hierarchy once at `harness-runtime/src/harness_runtime/cli/app.py`, level + destination operator-overridable. Cheapest, and it closes the one-shot case; but it is an **operator-facing behaviour change** (where lines go by default, at what level, with what rotation), and it does **not** cover the `api.py` library path, where configuring logging from inside a library is the anti-pattern. **(2) OTel log bridge** — route `harness.*` into the OTel pipeline that already exists for spans (`LoggerProvider` + `LoggingHandler`), making the report log a first-class signal alongside the 15-namespace OD schema. This **mints a new signal family** and is therefore **X-AL-3 spec-gated**: a Class 1 back-flow plus an OD/Runtime spec leg is owed **before** any code. Two independently verified absences it would close together: **no metrics pipeline** (no `get_meter` / `MeterProvider` anywhere) and **no log bridge** (no `LoggerProvider` / `LoggingHandler` anywhere). **(3) Document and close** — state normatively that carrier durability is deployment-configured and that no condition may depend on emission durability for its correctness; **half-taken already** by `B-96`'s conditions #12 and #6 (spec text owed at the `B-96` spec leg), which is precisely why (3) alone does not close this row.
- **Witness — DIFFERENT PER SHAPE, because one witness cannot cover all three** *(scoped at round 3, out-of-family [P2])*. **Shapes (1) and (2), which ADD a sink:** an **end-to-end run** (not a unit test) that drives a real TTL-expiry sweep and asserts the typed report line is retrievable from the configured sink **after the emitting process has exited**. A `caplog` assertion proves nothing for these — `caplog` installs the very handler whose absence **is** the finding. **Shape (3), which adds NO sink:** a post-exit witness is **impossible by construction** and demanding one would make the shape unclosable; the correct witness is instead **documentary and negative** — the spec text states the deployment-configured limit, and no condition anywhere in §14.8.11 depends on emission durability for its correctness (checkable by inspection against the twelve conditions, which the `B-96` record already satisfies at #6/#7(c)/#8(b)/#12).
- **Council: ⚖️ conditional.** Shape (3) alone needs no convening (stating a true negative is not a design decision). Shapes (1) and (2) carry a nameable **C7 ⊥ C11** tension — observability owns the signal, its carrier and its namespace and wants the store's sole forensic artifact to actually persist; operator-loop/local-first bears the configuration burden and the stderr noise on a local-development CLI and pays for whatever default the harness picks on the operator's behalf. Convene **dyadic only if (1) or (2) is taken**.
- **Cross-ref.** `B-96` (the arc that surfaced it; its conditions #6/#8(b)/#12 absorb the claim half and its council record cross-references this row); `B-74` / `B-77` (the same store's retention lineage).

### B-109 · Sibling immutable-map carrier `_ImmutableKeyArns` retains the inherited `dict.__init__` re-population hole *(surfaced at the B-107 impl leg, out-of-family review round 2, 2026-08-04; observed and deliberately not fixed there — harness-runtime was read-only for that arc)*

- **What it is.** The B-107 impl leg's codex round 2 found the B-107 map carrier's inherited `dict.__init__` re-population route (calling `__init__` on an already-constructed instance silently repopulated the sealed map in place) and sealed it — but the settled precedent it adopted, `_ImmutableKeyArns` at `harness-runtime/src/harness_runtime/types.py`, carries the identical hole.
- **Status.** `closed` (2026-08-04, PR #1216) — grounded and built in one arc; the CLOSED record below carries the evidence. Fix template applied: the B-107 carrier's sealed form (`__slots__` + refusing `__setattr__` + sealed `__init__`). Explicit-base-class routes stayed out of scope per the B-107 §6.1 / CP v1.115 §1.5 forged-instance boundary, extended to explicit base-class routes at the B-107 impl-leg docstring. *(Pre-close status was `registered_finding` with grounding owed — reachability at HEAD, site enumeration, sibling sweep; all three discharged below.)*
- **Closure — MET.** The sibling refuses re-initialization, witnessed by tests mirroring `test_pd8_probe_b/c` plus a performed revert-the-seal mutation probe (all four named witnesses fail unsealed).
- **CLOSED 2026-08-04 — BUILT (PR #1216).** Grounding confirmed all three owed facts at HEAD before build: `obj.__init__({...})` reproduced live on BOTH the raw carrier and a validated frozen `AuditSigningConfig.key_arns` field (an unvalidated `key_id -> ARN` entry injected post-validation); construction sites exactly two (`types.py` `__reduce__` + the field validator); the tree-wide sealed-mapping sweep found NO further siblings (the B-107 carrier and this one are the only two). The seal mirrors the B-107 template faithfully — sealed positional-only `__init__` defaulting to a module-level `MappingProxyType({})`, `__slots__ = ("_sealed",)` (the instance `__dict__` that made a naive flag reopenable is gone), refusing `__setattr__`/`__delattr__`, `__reduce__` byte-unchanged so deepcopy/pickle/`model_copy(deep=True)` still work and every copy ends sealed. Witnesses: 4 new tests in `test_config_audit_signing.py` (16 -> 20) mirroring `test_pd8_probe_b/c` — re-init refused + byte-unchanged on raw AND validated-field carriers; seal-reopen (`_sealed` assignment/`del`) refused; copies sealed; no-arg construction valid. PD-8 mutation probe PERFORMED: `__init__` override deleted -> all four named tests FAIL (`DID NOT RAISE TypeError`) -> restored -> green. Test totals, **CI-MEASURED (not derived)**: **7373 passed / 11 skipped / 36 deselected** at the PR branch head `dff5b25a`, blocking `pytest (all axis packages)` job, run `30972358886` — identical at the merge commit `ec438578` (run `30972622806`); the local run was 7372 passed + 2 known `harness.toml` env-artifact reds; pyright strict 0/0. Explicit-base-class routes stay out of scope per the B-107 §6.1 / CP v1.115 §1.5 forged-instance boundary, extended to explicit base-class routes at the B-107 impl-leg docstring, restated in the type's own docstring.

### B-110 · The GC observation record cannot distinguish generations of a REUSED temporary candidate name *(surfaced at the B-96 spec leg, out-of-family Codex rounds 3-4, 2026-08-05)*

- **What it is.** Runtime spec **v1.111 §14.8.11.1 term 5** keys the durable GC grace record on the **candidate filename** over both sweep classes, with a content set **RATIFIED CLOSED at two members** `{candidate filename, first_observed_at}`. The authorizing council record's condition #5 asserts a name is *"stable and never reused for a different file"* for **both** classes, grounding it on the absence of any rename/replace in the store. **That ground establishes the LIFETIME half only.** A temporary name is drawn by a mechanism that avoids only names **currently present**, so once an orphan is removed its name is redrawable and a surviving record row can match a **different** later file. The published-entry class is **not** exposed — those names are non-invertible digests of a `uuid4`-bearing composite key.
- **Why it could not be closed at the B-96 spec leg.** That leg's first response **mandated** that a stale row never be honoured. Out-of-family round 4 **[P1]** showed this is **unsatisfiable inside the ratified two-member set** — there is no field to carry a generation identity, and adding one would widen a **ratified closed set**, which is an operator decision rather than a spec-leg act. The guarantee was therefore **narrowed to what the ratified carrier can actually deliver**, and the remainder registered here.
- **What it costs, exactly.** **For the RECORD-MEDIATED reclaim decision the conjunctive rule still gates the recreated file by its own TIMESTAMP — which is the TIMESTAMP-DERIVED bound, NOT term 2, whose publication bound does not extend to a name that was never published and whose timestamp can predate its own creation:** term 1's conjunct (a) is keyed on the **new** file's own filesystem timestamp, so the *decision* to reclaim cannot precede that file's own timestamp + TTL. **This claim is scoped to the DECISION, and deliberately so — a second, DISTINCT and PRE-EXISTING mechanism can still reach a replacement file, and it is recorded below rather than folded into this bound.** What degrades is the **additional** grace: for a reused temporary name alone, the reclaim bound falls back to the pre-existing timestamp-derived one, reintroducing the under-report-width window **for that single candidate**. Reachability is remote — the name is drawn from a large random space and must be redrawn within the interval between two sweeps — and is confined to **crash orphans**, never to a paid effect's stored entry.
- **A SECOND, DISTINCT mechanism — PRE-EXISTING at HEAD, recorded not absorbed** *(surfaced at out-of-family review round 6 [P2]; verified by direct read)*. The sweep **selects** candidates under `_publish_lock` + the cross-process lock (`harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:781`–`:800`) but performs the **removals OUTSIDE** it (`:821`–`:822` for entries, `:845`–`:847` for temporary orphans). Two concurrent sweeps can therefore both select the same expired orphan; if the first removes it and a publisher is then handed the same freed name, the second sweep's **stale path** can unlink that **active replacement** — a **premature deletion**, not merely a lost grace. **This is a property of the shipped sweep's phase split, NOT of the observation record**: it is present at HEAD today under the sweep-COUNT grace, is reachable without any record at all, and is **not introduced by the `B-96` amendment**. **Disposition (c) — LOCK-BOUND IDENTITY REVALIDATION BEFORE REMOVAL**, i.e. re-verify under the lock that the path still names the same file the selection decided on. Cheap, and **`B-96`'s U-RT-150 is already working at that exact site, so that unit MAY take it** — but it is **NOT owed** by the `B-96` legs, whose ratified scope is the reclaim *rule*, and mandating it there would be scope creep on a pre-existing hazard. **Reachability is the same remote name-redraw window as the primary item, further narrowed by requiring two overlapping sweeps.**
- **Close-out — two grounded dispositions, neither owed by the `B-96` impl leg.** **(a) ACCEPT AND DOCUMENT** — the residual is already stated at §14.8.11.1 term 5 with its exact cost, and `B-96`'s **U-RT-150 AC #5** witnesses the **timestamp-derived bound** — explicitly **not** term 2, whose publication bound does not extend to a reused temporary name — rather than an unbuildable freshness guarantee; on this disposition the row closes as documented-and-accepted. **(b) WIDEN THE RATIFIED CONTENT SET** — a generation-stable identity as a third member, or a key that is not the bare basename. **That is a Class 2 operator decision**: the two-member closure is ratified at the `B-96` fork §11.3 table and at council condition #5, whose stated ground is that widening the candidate *classes* adds no field, no carrier and no namespace. **A fork filing is owed before (b) may be built.**
- **NOT owed by any leg.** Reopening the `B-96` ceiling question; any change to the **candidate-filename key over BOTH sweep classes**, which condition #5 requires for an independent reason (crash orphans would otherwise be permanently unreclaimable).
- **Council: not owed at registration.** Whether a ratified CLOSED content set should grow one member to close a remote, bounded, crash-orphan-only residual is a scoping question for an operator ratification, not a nameable cross-domain tension. **If disposition (b) opens, re-evaluate** — a third member touches the same C3 (state/persistence integrity) ⊥ C10/C7 (content restriction on a sensitive-payload store) axis condition #5 was authored on.
- **2026-08-07 — RE-SCOPED at a grounding pass; the row now carries mechanism (c) ALONE.**
  - **(i) The primary half is DISCHARGED IN SUBSTANCE.** Disposition **(a) accept-and-document** is **satisfied at HEAD**: the landed **U-RT-150** witness `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp` (`harness-runtime/tests/test_lifecycle_protected_result_store.py:2731-2795`) asserts exactly the **timestamp-derived** bound this row says survives a name reuse — the recreated file is **not** reclaimed before its own filesystem timestamp + TTL, and **is** reclaimed after (`:2794-2795`). The degradation is therefore witnessed at the strength the ratified two-member carrier can actually deliver, and no unbuildable freshness guarantee is claimed anywhere. **Disposition (b)** remains a **Class 2 operator decision** and is **not** opened by this pass; the row does not carry it as owed work.
  - **(ii) What the row retains: mechanism (c) alone.** **Offsets re-stamped at HEAD by content** — the bullet above cites `:781`–`:800` / `:821`–`:822` / `:845`–`:847` from the B-96-era file and those are **superseded**. At HEAD, selection of **both** candidate classes happens under `self._publish_lock` + `self._cross_process_lock()` at `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:1111-1135`; the **entry** unlink is **off-lock** at `:1173`; the **tmp-orphan** unlink is **off-lock** at `:1201`.
  - **(iii) Severity sharpened — (c) is a HOLE IN `B-68`'s OWN whole-lifetime-lock fix**, not merely an adjacent race. `_publish_atomic` holds **both** locks across the temporary's **entire lifetime** (`:810-858` — `mkstemp` at `:811` inside the `with` opened at `:810`, `os.unlink(tmp_name)` in the `finally` at `:857-858`) **precisely so that no sweep can unlink an in-flight temporary**. A **second** concurrent sweep's off-lock unlink at `:1201` walks a path captured **before** that hold began and therefore **bypasses** it. If it fires, the publisher's subsequent `os.link` raises `FileNotFoundError` — **a lost recovery record for a completed paid effect**, the exact loss this store exists to prevent.
  - **(iv) Reachability, quantified** *(why it stays registered rather than opening)*. Firing requires an **exact** `tempfile._RandomNameSequence` redraw of the just-freed name inside the window: 8 characters over a 37-symbol alphabet = **3.51e12** distinct names, so **~2.8e-13 per redraw** (ground-truthed against Python 3.12.13's `tempfile` on the dev machine), narrowed further by requiring two overlapping sweeps with a publish landing between them.
  - **(v) Trigger replaced.** The *"`B-96`'s U-RT-150 is already working at that exact site, so that unit MAY take it"* trigger is **expired** — U-RT-150 landed and did **not** take it. The standing trigger is now **opportunistic: the next arc that touches `gc_sweep`'s phase split takes it.** Nothing else opens the row. *(SCHEDULED 2026-08-07 at the B-111 plan leg's round-4 absorb: the U-RT-151 impl arc touches the phase split, so the trigger FIRES there — that arc takes the priced fix as a companion; see the B-111 row's APPLIED bullet.)*
  - **(vi) The fix is priced** so that arc need not re-derive it: move the `tmp_candidates` unlink loop (`:1199-1201`) back **under** the lock — `B-75`'s off-lock rationale does **not** bind it, since that loop performs no decrypt and no unbounded work — and add identity revalidation against the `(st_dev, st_ino, st_mtime)` triple captured at **selection** time, because a bare re-`stat()` is **insufficient** (POSIX offers no compare-inode-and-unlink primitive, so the comparison must be made under the same lock the unlink takes). **~25 lines plus one injected witness**: monkeypatch `tempfile.mkstemp` to hand the publisher the just-freed name, and assert the second sweep **refuses** to unlink it.
- **Cross-ref.** `B-96` (the arc that surfaced it — spec leg PR #1235); Runtime spec §14.8.11.1 terms 1/2/4/5/9; `B-108` (the sibling gap the same council record registered).
- **CLOSED at the same arc as `B-111` (the U-RT-151 impl leg, PR #1260) — the trigger fired as SCHEDULED.** U-RT-151 extends `gc_sweep`'s UNLOCKED enumeration phase and threads the collected list into the locked publication, which is exactly the phase-split condition bullet (v)'s re-armed opportunistic trigger names, so that arc TOOK the priced fix of bullet (vi): the `tmp_candidates` unlink loop is relocated back UNDER the lock hold that SELECTED the candidates (`B-75`'s off-lock rationale does not bind it — no decrypt, no unbounded work), and each unlink is now preceded by an identity revalidation against the `(st_dev, st_ino, st_mtime)` triple captured at SELECTION time, under that same hold; a mismatch REFUSES the unlink and reports it. The ENTRY unlink stays off-lock and is NOT in scope. **Witness:** `test_a_recycled_temporary_name_is_refused_by_lock_bound_identity_revalidation` — phase 1 asserts the removal runs with the selecting lock HELD; phase 2 INJECTS the collision deterministically (the other sweep's unlink, then a monkeypatched `tempfile.mkstemp` handing the publisher the just-freed name) and asserts the replacement SURVIVES. Both halves mutation-probed (relocating the loop off-lock kills phase 1; dropping the revalidation kills phase 2).

### B-111 · The GC observation record's publication temporary is unreclaimable after a mid-publication kill, because both sweep globs exclude it by construction *(surfaced at the B-96 impl leg, out-of-family Codex round 2 [P2], 2026-08-05)*

- **What it is.** Surfaced at the B-96 IMPL leg (U-RT-150) by out-of-family Codex round 2 [P2], against that leg's own code -- reported, not absorbed. `_publish_observation_record` publishes the durable grace record by `tempfile.mkstemp(prefix="gc-observations.publishing-")` -> write -> fsync -> `os.replace`, with an `except BaseException: unlink(tmp); raise` cleanup. A process KILLED strictly between `mkstemp` and `os.replace` runs NO Python cleanup, so the temporary survives. That prefix is deliberately disjoint from BOTH sweep globs (U-RT-150 AC #15's second half REQUIRES the disjointness: reusing the `.tmp-` payload prefix would make the next sweep classify the artifact as crash-orphaned ciphertext and unlink it as though it were a payload), and nothing else in the store enumerates it -- so no path ever removes it, and it is invisible to the term-8 gauge and to the `harness-inspect` counts as well. A repeated crash loop landing in that window could therefore accumulate files on the store volume with no bound and no operator-visible signal. Reachability is REMOTE and of the same order as B-110: the window is the sub-millisecond gap between `mkstemp` and `os.replace`, once per sweep, and it needs a KILL (not an exception -- an exception takes the cleanup path, which is witnessed by `test_record_publication_is_crash_atomic_and_never_no_replace_or_unlink_recreate`).

- **Current state (superseded — see RATIFICATION below).** DECLINED at the `B-96` impl leg with a grounded pin, registered rather than absorbed; the residual is stated in `_publish_observation_record`'s own docstring.

- **RATIFICATION — operator, 2026-08-07: DISPOSITION (a).** `status: registered_finding → open`. Narrow U-RT-150 **AC #15**'s no-removal clause to the **SWEEP's PAYLOAD-orphan accounting path**, then build the prefix-scoped cleanup. **The deciding argument is PLAN-TEXT CORRECTNESS, not severity:** AC #15's text encodes a constraint **stronger than its own stated reason** — the reason it gives is entirely about the artifact being *classified and handled as a PAYLOAD* (*"count and age accounting, being reported as a candidate, and being unlinked as though it were ciphertext"*), and nothing in it is about the artifact **surviving**. Read literally the clause also forbids the store's **own** publication path from reclaiming its **own** leftovers, which — combined with the disjointness AC #15's **first half requires** — is what leaves a killed publication's temporary reclaimable by **no path at all**. AC #15 as written is therefore **the sole reason this row is open**. Disposition **(b)** (accept and document) is **NOT** selected; the two dispositions recorded as *not owed by any leg* (re-prefixing toward `.tmp-`; widening a sweep glob) remain **not owed and are not taken**.

- **APPLIED — plan leg, `Implementation_Plan_Harness_Runtime_v2_59.md` (cleared 2026-08-07).** **ONE amendment site plus ONE new unit.** U-RT-150 **AC #15** gains an **APPENDED** qualifier (its existing body **PRESERVED VERBATIM** — no word retracted, reworded or reordered): the **SWEEP half is PRESERVED UNQUALIFIED** — the `_ENTRY_GLOB` / `_ORPHAN_GLOB` enumeration and everything downstream (count/age accounting, the AC #8 gauge at **both** surfaces, candidate reporting, reclaim-as-ciphertext) still must never enumerate, report, classify or remove the record or any intermediate file of its publication, **no glob widens**, and the **`.tmp-*` PAYLOAD-orphan semantics are BYTE-UNCHANGED** — while the record's **OWN publication path MAY** remove stale leftovers of its **OWN** `_GC_OBSERVATION_RECORD_TEMP_PREFIX`, **prefix-scoped by construction** and **under the locks the publication already holds** (§14.8.11.1's *"Published under the same locks the sweep already holds"* — no new lock, no new lock scope). **THE RUNTIME SPEC IS UNTOUCHED — v1.111 stands and NO spec delta is owed**, determined by direct read: §14.8.11.1 *"The observation record's carrier"* commits the publication mechanism and the lock scope and **explicitly acknowledges the orphan** (*"a failed write leaves only an orphan temporary file"*) while stating **no disposition** for it, and leaves the record's file name to implementation discretion — so the prohibition this row runs into exists **only in the plan**. **The qualifier is a PERMISSION, not an obligation — deliberately:** U-RT-150 is **LANDED and CLOSED** (PR #1237), and a **MUST** written into AC #15 would make its conjunctive closure criterion **retroactively false**, the exact defect the Runtime plan v2.58 §0.3 names; the **MUST** therefore lives at a new unit, per the **`B-97`(a) → U-RT-149** precedent. **ZERO contract numbers, ZERO carriers retyped, ZERO fields added, ZERO `snapshot_hash` impact, ZERO cluster, ZERO cross-axis edge, ZERO CXA rows** (aggregate frozen at 111). **`B-110` is neither closed nor widened** — the publication temporary is never a candidate of either class and never appears in the record's closed two-member content set. **The U-RT-151 impl ARC, however, fires `B-110`'s standing opportunistic trigger** *(out-of-family round 4 [P2])*: the unit works `gc_sweep`'s phase split — exactly the condition the re-armed trigger names — so that arc **takes `B-110`'s priced fix as a companion** and `B-110` closes there, not at this delta.

- **Impl leg owed — NEW unit `U-RT-151` (FOUR ACs).** Roughly **fifteen lines** of production code across **two functional sites, plus a collected-list parameter threaded between them** (**four** signature/call touch points: `_observe_candidates` `:1224`–`:1230` / `:1130` and `_publish_observation_record` `:1282` / `:1269`) — a prefix-scoped `glob` added to the sweep's **already-unlocked** enumeration phase (`protected_result_store.py:1068`–`:1104`) plus a **bounded** suppressed-`unlink` loop at the publication (the `contextlib.suppress(OSError)` shape the method already uses at `:1337`–`:1338`) — with **no new type, field, constant, configuration or migration, and no new emission SURFACE — the single failure emission AC #4 owes rides the EXISTING report-log carrier** *(round-4 absorb)*. **THE ENUMERATION MUST RUN OFF THE CROSS-PROCESS LOCK, and that is a closure condition, not a preference:** `_publish_observation_record` is reached from `_observe_candidates` at `:1130`, **inside** the combined lock opened at `:1111`, so a root `glob`/`iterdir` there is **O(store size)** — the cost **`B-75` moved off the lock** (`:1020`–`:1036`) — and would **falsify the method's own docstring justification** at `:1317`–`:1319`. Only a bounded unlink loop (zero-length in steady state) runs under the lock; the `B-76` snapshot-then-verify argument carries unchanged, and the path's **own** in-flight temporary — created by the `mkstemp` at `:1327`, **after** the pre-lock pass — is excluded **by construction**. **(POSIX; on Windows `_cross_process_lock` is a documented NO-OP at `:608`–`:610` and the exclusion degrades to the in-process lock — bounded by AC #1's **past-TTL age gate**, under which a LIVE co-resident publication's in-flight temporary is never selectable, so the residual exposure matches the **pre-existing `B-45`** shape the sweep's own past-TTL `.tmp-*` re-verify at `:1041`–`:1046` already carries — the identity claim is TRUE only WITH the gate, corrected at out-of-family round 4 [P2].)** **ORDERING IS PINNED:** the removal runs only **after** the `os.replace` at `:1333` returns and before the directory `fsync` at `:1340` — without that pin AC #2's failed-publication witness and AC #4's suppressed-`OSError` criterion are **mutually unsatisfiable**. *(Surfaced at adversarial review round 1 [P1]: the AC's first drafting mandated the under-lock scan and would have reopened a closed defect.)* **AC #1** the cleanup itself — candidates **past-TTL age-gated** on the sweep's own `ttl_seconds` — scope asserted as an **exact set** against a root that also holds a real `.tmp-*` orphan, an `*.entry`, the record, the lock file, an unrelated name **and a FRESH own-prefix leftover that must SURVIVE (the age-gate witness)**, with the path's **own in-flight temporary surviving its own cleanup** (otherwise the unlink-then-recreate window AC #14(iii) forbids reopens). **AC #2** — **ONE DECLARED witness re-pin**, stated so the impl leg cannot reach green by discovering it silently: in `test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan`, the `(root / name).exists()` assertion **flips** to a **POSITIVE** *removed-by-a-LATER-PUBLICATION* witness that **ATTRIBUTES** the removal to the publication path (absence alone would also pass a **widened sweep glob** — the very defect AC #15 exists to close), while the **`.tmp-` prefix-disjointness assertion and the not-reported log assertion are PRESERVED VERBATIM**. **The qualifier does not cause that flip — exercising it does**, which is why the flip is owed by U-RT-151's acceptance surface and not smuggled under U-RT-150's. **AC #3** the `.tmp-*` payload class asserted classified **exactly as before** (enumerated, counted, gauged, reported, reclaimed on the conjunction, and **not** on first observation) — load-bearing because the cheapest wrong fix is to let the record's temp fall into the payload class. **AC #4** the success-path no-new-surface absences plus best-effort removal that never fails the publication and never widens suppression over the `os.replace`; successful removals stay unlogged (the artifact is **not a candidate of either class** and appears in no count, gauge or report), but a **suppressed removal `OSError` MUST be reported on the sweep's EXISTING report-log carrier** (`:1172`–`:1188` / `:1199`–`:1208` shape; U-RT-150 AC #12 binds the emission) — *reversed from all-silent at out-of-family round 4 [P2]: persistent silent failure would recreate the exact accounting invisibility this row was ratified to close.* **AC #2 additionally names two non-assertion consequences** — the witness's teardown unlink loop (which would `FileNotFoundError` once the cleanup ships) and an **ADDED sweep-isolating witness** (leftover present, publication made to FAIL, full past-TTL sweep still neither enumerates, reports nor removes it) replacing the *"a sweep must not remove it"* coverage the flip would otherwise silently retire. **`B-111` flips to `closed` ONLY when U-RT-151 merges.**

- **Close-out steps *(HISTORICAL — pre-ratification record, kept for provenance; SUPERSEDED by the RATIFICATION + APPLIED bullets above: disposition (a) SELECTED 2026-08-07 and APPLIED at plan v2.59)*.** DECLINED AT THE B-96 IMPL LEG WITH A GROUNDED PIN, registered rather than absorbed. The natural fix -- having `_publish_observation_record` remove stale siblings of its OWN prefix at publication time, mirroring `_publish_atomic`'s `finally: os.unlink(tmp_name)` for payload temps -- is FORECLOSED by U-RT-150 AC #15 as written: its mutation probe states that a sweep after a simulated mid-publication crash "must not enumerate, report or remove it", and the AC's witness (`test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan`) asserts exactly that the leftover STILL EXISTS after a later sweep. Since `_publish_observation_record` is reached only FROM `gc_sweep`, any cleanup there IS a sweep removing it, so building the fix at this leg would mean absorbing a plan-level contradiction -- precisely the silent-absorption failure mode X-AL-3 forbids. TWO GROUNDED DISPOSITIONS, neither owed by U-RT-150. (a) NARROW AC #15 at a plan delta so its no-removal clause binds the PAYLOAD-orphan accounting path specifically (enumeration into count/age/report, and unlink AS ciphertext) rather than any removal whatsoever, then build the prefix-scoped cleanup under the lock the publication already holds -- cheap, and strictly in the bounded-retention direction the store exists to serve. This is a Phase-6 plan act, not an impl act. (b) ACCEPT AND DOCUMENT -- the residual is stated in `_publish_observation_record`'s own docstring with its exact cost and reachability, and the row closes as documented-and-accepted. NOT OWED BY ANY LEG: changing the temporary's prefix back toward `.tmp-` (AC #15 requires the disjointness for an independent and stronger reason), or widening the sweep globs (which would reintroduce exactly the unlink-as-ciphertext defect AC #15 exists to close).

- **Council: no** — NOT owed at registration. The question is whether an acceptance criterion's no-removal clause should be narrowed by one qualifier so a self-owned cleanup becomes legal -- a plan-scoping question, not a nameable cross-domain tension. If disposition (a) is opened, re-evaluate: it touches the same C3 (state/persistence integrity) vs C10 (blast radius on a sensitive-payload store) axis AC #15 was authored on. *(Disposition (a) SINCE OPENED — re-evaluation recorded at the plan leg: probe-resolved without convening; the C3↔C10 axis was carried by three adversarial rounds plus out-of-family round 4, whose past-TTL age-gate absorption IS the C10 blast-radius bound.)*

- **Window correction** *(2026-08-07 grounding pass, by direct read)*. The bullet above calls the window *"the sub-millisecond gap between `mkstemp` and `os.replace`"*. **That understates it.** The gap at `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:1329-1332` is **fsync-inclusive** — `os.fdopen` → `handle.write` → `handle.flush` → `os.fsync(handle.fileno())` all sit **inside** it, before the `os.replace` at `:1333` — so on a slow or contended volume the window is a **durability-bounded interval**, not a sub-millisecond one. Worse, it is **correlated** with exactly the condition that provokes an external kill: a supervisor SIGKILLing a process that appears hung on a slow mount lands **preferentially inside this window**. **Counter-correction, stated against interest:** the *"accumulate files on the store volume with no bound"* framing **overstates** the cost in the other direction — each leftover is **one record-sized file per killed sweep**, so exhausting a volume needs an implausible number of kills. **The real cost is accounting invisibility, not capacity**: the leftovers are invisible to the term-8 gauge and to the `harness-inspect` counts, so an operator's orphan accounting silently **under-reports**. **Net: reachability is HIGHER than registered and the harm is NARROWER than registered.** **Grounding recommends disposition (a)** — the AC #15 narrowing — on **plan-text-correctness** grounds rather than on severity: AC #15's no-removal clause, read literally, forbids the store from cleaning up its **own** prefix, which is not what the AC exists to protect (it exists to stop the record temporary being unlinked **as ciphertext**). **Decision RECORDED — the operator selected disposition (a) on 2026-08-07 (the RATIFICATION bullet above); this note is the pre-ratification grounding that recommended it.**

- **Cross-ref.** `B-96` (closed — the impl leg this surfaced against); `B-110` (the sibling registered residual of the same remote-reachability order, also surfaced against a `B-96` leg's own text).
- **CLOSED at the U-RT-151 impl leg, PR #1260.** The cleanup landed exactly as priced: a prefix-scoped, PAST-TTL-age-gated enumeration of `_GC_OBSERVATION_RECORD_TEMP_PREFIX` leftovers added to the sweep's ALREADY-UNLOCKED enumeration phase, threaded as an explicit parameter through `_observe_candidates` into `_publish_observation_record`, where a bounded suppressed-`unlink` loop runs ONLY AFTER the `os.replace` returns and BEFORE the directory `fsync`. No new type, field, constant, configuration, migration or emission SURFACE; the success path is silent and the single suppressed-`OSError` failure rides the sweep's EXISTING report-log carrier. **Witnesses (all mutation-probed):** `test_publication_removes_exactly_its_own_past_ttl_leftovers_and_nothing_else` (AC #1 — the EXACT-SET scope refusal with the FRESH own-prefix leftover surviving, the enumeration asserted OFF-LOCK by the lock's own state, and the path's own in-flight temporary surviving its own cleanup); the DECLARED re-pin of `test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan` (AC #2 — the `exists()` assertion flipped to a POSITIVE removed-by-a-LATER-PUBLICATION witness that ATTRIBUTES the removal by position INSIDE the publication window, with the `.tmp-` disjointness and not-reported assertions PRESERVED and the teardown loop dropped as redundant) plus the ADDED sweep-isolating `test_a_failed_publication_leaves_the_leftover_untouched_by_every_sweep`; `test_the_payload_orphan_class_is_byte_unchanged_by_the_record_temp_cleanup` (AC #3, both gauge surfaces); and `test_the_cleanup_is_silent_on_success_and_reports_a_suppressed_unlink_failure` (AC #4). No sweep glob, no prefix constant and no `.tmp-*` classification changed; the Runtime SPEC stayed UNTOUCHED at v1.111. **Three Class 3 impl-leg observations recorded, not absorbed** *(rider owed at the next Runtime plan delta — probe-text corrections only, no behaviour change)*: (i) plan ~L143 probe (i)'s broadened-suppression kill is unsatisfiable in AC #1's successful-publication witness and was homed at AC #4 phase 3, where it IS killed by the named leak consequence; (ii) AC #1's fourth probe (own-temp removal must fail AC #14(iii)) is satisfiable only against the pre-replace placement the plan's own ordering pin forbids — post-replace it no-ops into the suppression; the property is carried by AC #1's own in-flight-not-unlinked assertion (fourth instance of the probe-names-wrong-witness family the plan records at ~L143); (iii) AC #2's re-pin gained **TWO** ADDED assertions, not one — the count is corrected here: the never-enters-the-record assertion (without it the widened-glob mutation survived, since the cleanup pre-empts the sweep's reclaim so the preserved log assertion can never fire) **and**, the second addition, a fresh-leftover-survives assertion placed after the FIRST sweep, which pins the cleanup's PAST-TTL AGE GATE (without it a cleanup that removed leftovers regardless of age would pass); both preserved assertions byte-intact.

### B-112 · Deadline'd lock polling drops kernel handoff fairness, so a caller-sampled ledger timestamp can be overtaken and refused *(surfaced by out-of-family review round 1 on the B-93 build leg, 2026-08-06; reproduced 12/20 by the reviewer's own contention probe)*

- **What it is.** The `B-93` deadline replaces a bare blocking `fcntl.flock` with the ratified **non-blocking probe + bounded retry** shape. A blocking `flock` **parks** the waiter in the kernel, which grants the lock on release before the ex-holder can realistically re-acquire it; a **polling** waiter is asleep at that instant, so an ex-holder that releases and immediately re-acquires **wins the next round**. This changes **ordering, never exclusion** — mutual exclusion is the same kernel `flock` it always was.
- **Where it reaches a production contract — exactly one place.** `append_ledger_entry` (`harness-is/src/harness_is/state_ledger_write.py`) validates a **caller-sampled** timestamp against **physical append order** under a `_CLOCK_SKEW_TOLERANCE` of **zero**. A waiter whose timestamp was sampled *before* it queued can now be overtaken by a later-arriving writer and refused with `NonMonotonicTimestampError`. Out-of-family review reproduced the dropped waiter in **12 of 20** runs of a focused two-process contention probe.
- **Not newly possible, newly likely — stated against interest.** The same refusal was always reachable whenever the later writer genuinely acquired first, and POSIX `flock` specifies **no** acquisition ordering at all — so the pre-`B-93` code was relying on an **unspecified** kernel behaviour. `B-93` raises the probability; it does not create the class.
- **The lock layer cannot fix this and should not try.** No acquisition ordering makes a timestamp sampled arbitrarily long *before* acquisition correct, and **no portable primitive offers a kernel-parked wait *with* a timeout** — `F_SETLKW` has none, and a watchdog thread would orphan an acquisition in exactly the wedged-holder case the deadline targets, accumulating one stuck thread per attempt. Polling is intrinsic to the **ratified shape**, not to the implementation: `filelock` — the vehicle the fork evaluated and **declined** — polls ten times coarser (`poll_interval=0.05`), and `msvcrt.LK_LOCK` retries at 1 s.
- **The mechanism exists in the cleared spec, but §7.6 scopes it AWAY from this row's surface** *(corrected at the merge-gate — the first filing got this wrong in the row's own favour)*. C-IS-07 §7.6 added **`WRITER_OWNED_TIMESTAMP`**, which samples **inside** the write critical section so the C-IS-05 §5 constraint (verbatim) *"holds **by construction** on this surface — concurrent sibling drains cannot invert"*. **That surface is the buffered/branch-drain path only.** §7.6's *"Surfaces that do NOT change"* paragraph keeps every **direct** append on caller-supplied semantics verbatim, and its **registered residual** says extending writer-owned authority to direct surfaces *"would change caller-supplied semantics for every existing direct producer and **requires its own back-flow**"* — naming **`B-57`** as the owner of the remaining direct writers. The deferred-to-discretion footer likewise constrains the carrier to *"preserve the direct-path caller-supplied contract byte-verbatim"*.
- **The zero-delta claim is RETRACTED, and the adoption question defers to `B-57`.** Adopting the sentinel on direct producers **is** the extension §7.6 routes to back-flow. `B-57` already owns that residual and already prescribes the route — *a narrow IS spec delta extending §7.6's writer-owned sampling to an opt-in direct-append mode (or per-writer serialization guidance) + U-IS-11 widening*. **Why this stays a separate row rather than folding into `B-57`:** `B-57` registers the direct-writer inversion as a **latent** possibility whose close-out is conditioned on it *"materializ[ing] in practice"*; this row is the **evidence that its probability changed** — a specific, reproduced (12/20) mechanism, introduced by a specific arc, on a specific lock. Folding it in would bury a falsifiable trigger inside a dormancy note. The rows are **cross-linked**: `B-112` is the cause-and-evidence record, `B-57` is the remedy owner, and `B-57`'s materialization condition should be read as **partially met** by this evidence.
- **Test-side adjudication** *(by direct read of §7.6's scope, as the gate asked)*. `test_shadow_git_rollback.py`'s concurrent-append worker retries with `WRITER_OWNED_TIMESTAMP` on a **direct** append surface. §7.6's constraint is about changing the **contract** — *"caller-supplied semantics for every existing direct producer"* — and the sentinel is the ratified opt-in **carrier**, explicitly left to implementation discretion. A single test explicitly opting in changes no producer's semantics and sets no default, so the constraint does **not** forbid it and **the test is unchanged**. Recorded here as evidence: it is the first direct-surface use of the sentinel in the tree, and a data point `B-57` should weigh when it prices the opt-in mode.
- **Why registered rather than applied at the `B-93` leg.** Selecting writer-owned sampling changes **whose clock** an entry records — a semantic decision *per call site*, not a mechanical substitution (some entries deliberately carry an event time that is not the append time). Converting callers wholesale inside a lock-deadline arc is exactly the opportunistic scope creep the surgical-change discipline forbids. One **shipped witness** was repaired in place at the `B-93` leg for this reason (`test_rollback_cross_process_lock_prevents_lost_concurrent_append`, whose fixed child timestamp had silently **pinned** the old ordering). It is now **order-INDEPENDENT** rather than re-pinned — an order-pinned formulation proved flaky in *both* directions, so the child stamps the pre-existing entry's timestamp and retries with §7.6's `WRITER_OWNED_TIMESTAMP` if the parent landed first; its load-bearing survival assertion is unchanged and still discriminates under mutation.
- **Close-out steps.** **(1)** Sweep the production callers of `append_ledger_entry` and classify each: does it pass a caller-sampled timestamp, *and* can its ledger be appended to by a second OS process concurrently? Only the **intersection** is exposed — the `harness run` alongside `harness daemon` shared-root topology named in `write_record_guarded`'s own SCOPE paragraph is the reference shape. **(2)** For each exposed caller, decide between `WRITER_OWNED_TIMESTAMP` (correct wherever the timestamp means *"when this was appended"*) and keeping caller-supplied semantics (correct wherever it means *"when the event happened"*, where an out-of-order refusal is the honest outcome the caller should handle). **This is the actual decision, and it is per-caller.** **(3)** Only if some exposed caller must keep caller-supplied semantics: decide separately whether `_CLOCK_SKEW_TOLERANCE` — documented as *"configuration-supplied"* but hard-coded to `timedelta(0)` — should become genuinely configurable. Widening it weakens the C-IS-05 §5 monotonicity guarantee **for everyone**, so it is a contract question, not a tuning knob. **(4)** Witness with a two-OS-process probe: a waiter whose timestamp is sampled first must not be refused after a later writer overtakes it. **Do not attempt a fairness fix in the lock layer** — the substrate reasons above are why.
- **Council: no** (conditionally yes at step 3). Step (2) is a per-caller semantic classification against an already-cleared contract, with no cross-domain tension to name. Step (3) **would** be council-eligible if reached — widening the skew tolerance trades information-substrate integrity (**C3**) against operator-loop robustness (**C11**) across every ledger writer at once — but it is reached only if step (2) leaves an exposed caller that cannot adopt writer-owned sampling.
- **Adjacent infra gap, registered not built** *(merge-gate lens 3, G3)*. This arc hit a **silent test-loss** defect: two new test files shared a basename, and because every `tests/` directory is a package literally named `tests`, the module path **collided** — pytest imported one module for both paths and silently dropped **12 witnesses** (34 collected where 35 were written). Caught only by recounting programmatically; the suite was **green** throughout. **Nothing guards against a recurrence:** `__init__.py` is present in every tests dir and does **not** prevent this, so the invariant the tree actually relies on is **unique test basenames repo-wide** — currently true, currently unenforced, and violated *silently* rather than loudly. A CI guard (basename-uniqueness across `harness-*/tests/**`, or per-package rootdir isolation, or `--import-mode=importlib` with unique package names) is its own mini-arc with its own tradeoffs and is **not** built here. Recorded on this row because the same arc surfaced it; a future infra arc may lift it to its own row.

- **In-process widening** *(2026-08-07 grounding pass; **code-grounded, explicitly not reproduced**)*. The overtaking is **not** confined to two OS processes. `flock` contends between separate **file descriptors even within one process** — stated in the lock module's own text at `harness-is/src/harness_is/cross_process_ledger_lock.py:120` (*"`flock` contends between separate fds even within one process"*), which is precisely why the module carries a refcounted **in-process `_rlock` face** at all. A sibling **thread** in the **same** process that loses the in-process probe therefore enters the **same polling wait** (`:173-184`: `self._rlock.acquire(blocking=False)` fails → `resolved.note_contention()` → a `timeout`-bounded `acquire`), and the cross-process arm beneath it polls the same way (`:524`). The exposed population is therefore **broader** than the `harness run` alongside `harness daemon` two-process topology close-out step (1) names: it is **any two concurrent direct appenders on one ledger root, threads included**. Recorded as a code-grounded **widening of scope only** — the 12/20 figure remains a **two-process** measurement and **no in-process probe was run** at this pass.

- **Cross-ref.** Surfaced by out-of-family review round 1 on the **`B-93`** build leg. The fairness property itself is documented at `harness_core.cross_process_lock_deadline._MAX_POLL_SECONDS`, which names this row.
- **2026-08-07 — CLASS 2 FORK FILED** at `.harness/class_2_fork_b57_direct_append_writer_owned_opt_in.md` *(doc-only; this row's status is **UNCHANGED** at `registered_finding`)*. This row is the **cause-and-evidence record**, and the adoption question it defers to `B-57` is what that filing decides. **This row's `council` condition is resolved NEGATIVE by probe:** step (3) — `_CLOCK_SKEW_TOLERANCE` configurability, the C3 ⊥ C11 dyad — is reached *"only if step (2) leaves an exposed caller that cannot adopt writer-owned sampling"*, and the filing's twelve-site classification leaves exactly **one** site retaining caller-supplied semantics (`as_is_wiring.py:129`, `timestamp=composed.timestamp` — genuine **event time**, single-producer, for which an out-of-order refusal is the honest outcome by design). That is not a stranded exposed caller, so **step (3) is never reached and no convening is owed**. **Close-out step (1) is DISCHARGED** by the filing's §3(ii)/(iii): the production callers of `append_ledger_entry` are swept and classified **by content** at HEAD `73b0e87b`, and one of them (`sibling_ledger_entry_composition.py:163`) proved **production-unreachable** — its `cp_is_wiring.emit_sibling_ledger_entry` entry point has **zero non-test callers** — so the exposed surface is **11 sites, not 12**. Re-commissioning the sweep at a later leg is not owed; re-grounding it at the spec leg is. **The in-process widening** recorded on this row was **re-verified by direct read** at the filing's HEAD (`cross_process_ledger_lock.py:120` module text; `:173-184` in-process probe then bounded acquire; `:524` cross-process probe) and remains **code-grounded, not reproduced** — the 12/20 figure is still a two-process measurement only.

- **2026-08-07 — the deferred adoption question is ANSWERED; this row's status does NOT move.** This row deferred the adoption question to `B-57`; the `B-57` Class 2 fork was **operator-ratified 2026-08-07 as Reading A** (per-call-site opt-in to `WRITER_OWNED_TIMESTAMP` on DIRECT append surfaces, default unchanged for every non-electing producer) and applied at **IS spec v1.13** (NEW C-IS-07 **§7.6.1**, extending the EXISTING §7.6 — no contract number minted) + **IS plan v2.9** (U-IS-11 ACs #14–#20 + #14-bis + the per-site classification table). **This row stays `registered_finding`:** it is the **cause-and-evidence record**, not the remedy owner, and what it records — the polling wait dropping kernel handoff fairness — is unchanged and unrepaired by the ratification. What changed is that its named consequence now has an authorized remedy at the exposed call sites. **Its own remaining disposition resolves at the `B-57` impl leg, on evidence:** the two-process contention witness that leg owes is the direct analogue of this row's own 12/20 repro, and a green witness under a PD-8 mutation probe is what would let this row close. **Step (3) — widening `_CLOCK_SKEW_TOLERANCE`, this row's stated council-eligible trigger — is still NOT reached**, re-determined (not inherited) at the spec leg: the classification leaves exactly ONE site retaining caller-supplied semantics (`as_is_wiring.py:129`, genuine **event** time), for which an out-of-order refusal is honest **by design**. **LIVE TRIGGER:** if the `B-57` impl leg judges **either** injection-caveat site (`audit_writer`, `cost_attribution_f2_write`) **RETAIN**, step (3) IS reached and the C3 ⊥ C11 dyad is owed **before** any tolerance change is authored.

### B-113 · The three ABBA contention probes classify contention by EXCEPTION CLASS, so an emulated-`flock` platform would leave three of the nine acquisition sites unbounded *(surfaced by out-of-family review rounds 8 and 10 on the B-93 build leg, 2026-08-06; DECLINED AT THE LEG, registered)*

- **What it is.** The three ABBA arms in `cross_process_ledger_lock` run a non-blocking probe under the directory hold and key their contention arm on **`except BlockingIOError`** — on the exception **class**, which CPython raises for `EWOULDBLOCK`/`EAGAIN` only. `fcntl.flock`'s own docstring records that it is *"emulated using fcntl() on some systems"*, and the `lockf` docs spell out that a non-blocking failure arrives as **`EACCES` or `EAGAIN`**, with an explicit instruction to *"check for either value"*. On an emulated platform those probes raise `PermissionError` immediately instead of entering their release-and-wait arm — so `flock_until_deadline` is never reached and **three of the nine `B-93` sites would not be deadline-bounded there**, while the leg's own documentation says all nine are.
- **What `B-93` did fix.** The shared helper classifies contention by **errno** (`EWOULDBLOCK`/`EAGAIN`/`EACCES`), witnessed by an injected-`EACCES` test. Every acquisition the helper performs is correct on an emulated platform; only the three **pre-existing** probes that gate entry to it are not.
- **Why declined at the `B-93` leg** *(raised by out-of-family review at rounds 8 and 10; accepted as a finding, declined as a change)*. **(1)** The narrow catch is **shipped `B-46` behaviour**, not introduced by `B-93` — the helper mirrored it deliberately. **(2)** Widening it means **merging** each arm's `except BlockingIOError` with the `except OSError` beneath it that classifies `ENOTSUP` and yields **unguarded** — restructuring three hardened arms whose ABBA release-before-blocking invariant the fork forbids breaking. **(3)** `EACCES` from a **native** `flock` is not contention at all but a genuine permission failure, so the widening is unambiguously correct only on the emulated platform. **(4)** The exposure is **dormant on every platform this workspace can observe** — Linux and macOS both provide native `flock`; CI is `ubuntu-latest` only; the operator's machine is an x86 Mac.
- **Close-out steps.** **(1)** Establish **which** targets CPython emulates `flock` on (`HAVE_FLOCK` is a build-time `configure` probe, so this is a fact about the target, not a runtime condition). If no supported target lacks native `flock`, this row closes **unreachable**, not fixed. **(2)** If one does: merge each arm's `except BlockingIOError` into a single `except OSError` dispatching on errno — contention errnos take the release-and-wait path, `ENOTSUP` keeps its yield-unguarded arm, everything else re-raises. **(3)** Witness by **injection**, as the `B-93` helper's own `EACCES` witness does; a real emulated-`flock` platform is not available and pretending otherwise is the unwitnessable-claim failure `B-45` exists to avoid. **(4)** **Coupled to `B-45`:** if a Windows backend is ever built the same errno-vs-class question recurs there (`msvcrt.LK_NBLCK` raises `OSError`, not `BlockingIOError`), so read the two rows together at that point.
- **Council: no.** Implementation-level errno classification inside an already-cleared mechanism (C-IS-09 §9.4 defers it), no cross-domain tension to name; the only judgement is the platform-reachability fact at step (1).
- **✅ CLOSED 2026-08-07 — UNREACHABLE, NOT FIXED** *(the disposition this row's **own** step (1) provides for)*. **The finding is correct by inspection and is declined on REACHABILITY, not on correctness:** the three arms do key their contention path on the exception **class**, and on a platform where CPython emulates `flock` via `fcntl()` they would raise `PermissionError` and skip the ABBA release-and-wait arm exactly as described.
  - **What step (1) established.** **(i)** The discriminator is the **build-time `configure` probe `HAVE_FLOCK`**, not a runtime condition — a CPython built with `HAVE_FLOCK` defined calls native `flock(2)`, and only a build **without** it takes the `fcntl()` emulation whose non-blocking failure can arrive as `EACCES`. **(ii) Empirical positive control** on the operator's dev machine (Python **3.12.13**, `darwin`, x86_64): `sysconfig.get_config_var('HAVE_FLOCK')` is `1`, and a **real two-fd contention probe** (`LOCK_EX` held on one fd, `LOCK_EX|LOCK_NB` attempted on a second fd over the same file) raised **`BlockingIOError`** with **`errno 35` (`EAGAIN`)** — native, not emulated, so the shipped `except BlockingIOError` arm is correct there. **(iii) The committed deployment surface contains no emulated target.** The ADR-D2 decision matrix (`design-substrate/ADR-D2.md:96-98`) commits exactly three deployment surfaces — local-development, self-hosted-server, managed-cloud — and every one is a Linux/macOS/BSD-family target whose CPython builds provide native `flock`; CI is `ubuntu-latest` only; the RC runbook's Docker / Lima path is a Linux VM. The platform families where CPython actually emulates `flock` via `fcntl()` (the Solaris / HP-UX / z-OS class) appear **nowhere** in the committed surface — not in ADR-D2, not in CI, not in the RC runbook. Step (1)'s condition — *if no target the workspace supports lacks native `flock`, this row closes as UNREACHABLE rather than fixed* — is therefore **met**, and steps (2)–(3) are **not owed**.
  - **Two reopen triggers, both PRESERVED rather than discharged by this close.** **(A) `B-45`:** if a Windows serialization backend is ever built, the same errno-vs-class question recurs there — `msvcrt.LK_NBLCK` raises `OSError`, **not** `BlockingIOError` — so the two rows must be read **together** at that point, exactly as step (4) says. **(B) NEW, and independent of `B-45`:** **any new supported deployment target whose CPython build lacks `HAVE_FLOCK`** reopens this row directly, because the close rests on the composition of the committed deployment surface and on nothing else. Neither trigger is a defect in the finding; both are the conditions under which its dormancy ends.
- **Cross-ref.** Surfaced on the **`B-93`** build leg (PR #1241). Sibling: **`B-45`** (the Windows backend, deferred on witnessability) — same "correct by inspection, unwitnessable here" posture, same platform gate.

### B-114 · `_prepare`'s executor-side exhaustiveness fall-through raises the memory-tool INPUT-error type for a harness-internal fault, violating the C-MEM-19 type-boundary invariant v1.3 introduced *(surfaced at the B-88 spec leg, out-of-family Codex rounds 3/7/8, 2026-08-06; the re-typing was PROPOSED and WITHDRAWN there on authority grounds, registered)*

- **What it is.** `StandardMemoryToolExecutor._prepare` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:359-373`) dispatches over `MemoryToolName` to one `_prepare_<tool>` per member and ends with `raise MemoryToolExecutionInputError(f"unsupported memory tool {tool_name!s}")` at `:373`. `MemoryToolExecutionRequest.tool_name` is typed `MemoryToolName` (`:205-212`, the field at `:210`) on an `extra="forbid"` model, so a request that reaches `_prepare` **always** carries a valid enum member — the branch therefore fires only when the executor lacks a `_prepare_*` for one. That is a **harness-internal exhaustiveness fault**, not a caller- or model-supplied argument.
- **Why it is a defect once `U-MEM-28` lands.** `Spec_Memory_Substrate_v1.md` **v1.3** C-MEM-19 gained a `### Input validation failure` subsection and a type-boundary invariant: where an implementation determines the failure class from the raised exception type, the type carrying `input_validation_failure` is raised **only** for argument refusals at the memory tool boundary, and **a harness-internal fault is not raised as that type**. Today the site is conformant, because `MemoryToolExecutionInputError` still declares `provider_adapter_failure`. The moment U-MEM-28 flips that declaration, this site becomes a **known non-conformance with a cleared contract**.
- **Harm, stated against interest.** Latent, not active. The branch is defensive and unreachable while every `MemoryToolName` member has a `_prepare_*`; the cost is one triage entry under `input_validation_failure` that is really an internal wiring fault, and a contract invariant with one named exception.
- **Why this is a separate row rather than U-MEM-28 work — authority, not merit.** The `B-88` spec leg (PR #1245) initially **required** this seventh re-typing alongside the ratified six in `lifecycle/llm_dispatch.py`. Out-of-family review round 8 objected that the operator ratified an A-ii list of **exactly six**, that a spec leg *applies* operator-decided fixes rather than deciding them, and that **recording** a contradiction does not authorize it. The objection was **accepted** and the obligation removed (fork `§11.6` item 4; plan v1.3 change-note; U-MEM-28's Out-of-scope list). Round 9 then observed that a finding carried only on `B-88`'s close-out would **vanish from open work** when that row closes at U-MEM-28 — hence this row, which owns the question and outlives `B-88`.
- **Close-out steps.** **(1) Ground first** at the then-current HEAD: re-read `_prepare` and `MemoryToolExecutionRequest.tool_name`. If either has changed such that the branch is reachable from caller-supplied input, the site is **conformant as it stands**, and this row closes with that finding recorded and no code change owed. **(2) Decide the disposition**, and the discriminator is whether an operator gate is needed at all: **(a) plain conformance repair against a cleared contract** — the type-boundary rule is already ratified spec text and the executor site was **never before the operator** (the `B-88` fork surveyed `lifecycle/llm_dispatch.py` only), so nothing the operator decided is being extended and this is impl work under the cleared floor; or **(b) a genuine extension of the ratified A-ii enumeration**, needing a Class 2 fork and operator ratification. **Reading (a) is the stronger one** on the grounding above and is where a grounding pass should start — but the `B-88` leg deliberately did not take it on its own authority, and this row does not pre-decide it. **(3) If repaired:** re-type `:373` to a type outside the input-error family declaring the residual, under the **same receiving-type constraints** U-MEM-28 fixes for the six dispatch sites — **not** a subclass of `MemoryToolExecutionInputError` (the declaration is read through the MRO), and **not** the family base itself (`isinstance` is subclass-inclusive, so admitting the base to `_classify_provider_exception`'s fail-fast tuple would silently fail-fast the **denied** and **store** subtypes that deliberately stay retryable). Witness **both halves**: the emitted `memory.failure_class` **and** `_classify_provider_exception(exc) is None`, since telemetry alone cannot see a base-raise regression. The `test_memory_failure_classification.py` row carrying the *"unsupported memory tool"* message re-keys with it. **(4) Not actionable before U-MEM-28 lands** — until the declaration flips, this site is conformant.
- **CLOSED 2026-08-07 — step (2) resolved to READING (a), plain conformance repair, and the repair landed.** **The adjudication, on the invariant's own words.** C-MEM-19 v1.3 `### Invariants`: *"Where the failure class is determined from the raised exception type, the type carrying input validation failure is raised only for those argument refusals, and **a harness-internal fault is not raised as that type**; separating such a fault out does not change its retry disposition."* The prose paragraph it summarizes has the same scope, and closes *"The rule constrains the type-to-class relation only; it fixes neither the number nor the naming of an implementation's exception types."* That is **kind-of-fault** language quantified over the TYPE — not an enumerated-sites list; the em-dash clause (*"an unset runtime memory context, or a missing or malformed entry in the harness's own memory tool schema table"*) glosses what *harness-internal* means and is introduced as a definition, not as the exhaustive set of covered sites. The ratified "exactly six" enumerates what the **`B-88` arc undertook**; the invariant states what the **codebase must satisfy**. Reading (b) would require the cleared invariant not to bind this site — which the design substrate itself denies: fork `§11.6` item 4 and plan v1.3 U-MEM-28 Out-of-scope both call it a *"REAL non-conformance"* / *"ONE known non-conformant site"* against the cleared contract. Design-phase authority had therefore already adjudicated that the invariant **binds** here; what round 8 withheld was only the **spec leg's** authority to perform the repair in that leg. Treating six as a ceiling on conformance would make cleared spec text unenforceable wherever a filing did not happen to survey, inverting the `CLAUDE.md` §1.3 authority chain. This arc is an **impl leg**: zero `design-substrate/**` edits, no contract number minted, no re-clearance owed.
- **Premise check at HEAD — step (1)'s falsifier NOT met.** Offsets moved (`_prepare` at `:407-421`, the raise at `:421`; the `tool_name` field at `:258`), substance unchanged: still typed `MemoryToolName` on an `extra="forbid"` model, all five members still armed, so the branch is still reachable only from a harness-internal fault. Step (4)'s precondition is met — U-MEM-28 landed at `#1247`.
- **What landed** (offsets resolved **by content** — the docstring edits above the site shift every offset in this file, so the raise sits at `:421` pre-change and `:441` at the landed commit)**.** The fall-through raise re-typed to `MemoryToolExecutionInternalError` (which already satisfies the three receiving-type constraints and is already admitted BY NAME to `_classify_provider_exception`'s fail-fast tuple as of U-MEM-28, so the retry disposition is preserved by construction); the receiving type's and the fail-fast tuple's docstrings amended to name the seventh raise site; the `test_memory_failure_classification.py` row **re-keyed** onto the internal type expecting the inherited residual `provider_adapter_failure` (eight input rows → seven + one internal), exactly as U-MEM-28's own comment there said it would; and NEW `harness-runtime/tests/test_b114_prepare_fall_through_conformance.py` carrying the **both-halves** witnesses — raised type + *not* a subclass of the input type; emitted `memory.failure_class` on **both** classify-routed paths (`execute()`'s catch and the B-84 `validate()` pre-pass); and `_classify_provider_exception(exc) is None` — plus the reachability positive control (every shipped `MemoryToolName` member reaches its own arm), which doubles as the tripwire if a later arc adds a member without one. Unlike the six dispatch sites, this one **is** on a classify-routed path, so the repair **moves** the emission to the residual rather than withdrawing one: conforming per C-MEM-19 v1.3's residual paragraph (*"a report … is the ABSENCE of a claim"*) and consistent with its coverage paragraph, which forbids withdrawal.
- **PD-8 probes, both restored.** (a) Revert the fall-through raise to the input type → the three real-site witnesses go RED (the population-table row stays green, correctly — it pins a type's declaration, not a raise site). (b) Drop the internal type from the fail-fast tuple → **only** the retry-exit assertion goes RED while all three telemetry witnesses stay green, which is the discriminating shape the two independent obligations require. Zero existing witnesses weakened (full `harness-runtime` suite: 3692 passed / 39 skipped); ruff + pyright clean. `.harness/u-mem-25-memory-closeout-evidence.md`'s C-MEM-19 row and recorded-residual paragraph refreshed in place so the U-MEM-28 certification no longer carries a stale open residual.
- **Council: no** (assessed at registration, and the assessment held). No nameable cross-domain tension: the contract rule was already ratified, the blast radius is one defensive branch plus one witness row, and the only open judgement was the conformance-repair-vs-ratified-extension discriminator at step (2) — a governance reading resolved by grounding rather than a design fork between substantive alternatives. It would have escalated to a Class 2 fork only under reading (b), which did not obtain.
- **Cross-ref.** Surfaced on the **`B-88`** spec leg (PR #1245), rounds 3 / 7 / 8; registered at round 9. Sibling finding carried on `B-88`'s own close-out (not here, because it is inert until a dispatch-side emission is wired): the cross-entry-path `scope_ref`-mismatch divergence between `_require_scope_ref` (`memory_tool_executor.py:794-800`, a **denial**) and `lifecycle/llm_dispatch.py:4371` (an **input error**).

### B-115 · Memory capture same-text / same-model-NAME / different-PROVIDER re-entry: a duplicate record JSONL line, then a `TRANSIENT_RETRY` staircase via `MemoryToolExecutionStoreError` *(MEASURED at the `B-84` build leg, PR #1243, W-5 cell (3); ratified by the operator 2026-08-07; **CLOSED at the (b′) split — Runtime v1.113 §14.6.3 row 6 discharged + plan v2.61 U-RT-153, waiver tuple 4 → 5**)*

- **What it is — MEASURED, not inferred.** Executed at the **`B-84`** build leg (PR #1243) as **cell (3)** of that leg's W-5 four-cell matrix, and recorded there on the now-**closed** `B-84` row's `close_out`. A memory capture whose **text** and **model NAME** are unchanged but whose **PROVIDER** differs re-derives the **same `memory_id`** (`summary_model` is hashed into the id; the provider is not), so the record JSONL line **is appended a second time**, and the operation ledger then **raises** on its 18-field equivalence payload (because `provider` differs) — surfacing as `MemoryToolExecutionStoreError`.
- **Why that is the LIVE half.** `_classify_provider_exception` admits `MemoryToolExecutionInputError` and `MemoryToolExecutionInternalError` to its fail-fast branch **by name**, and deliberately **not** the family base, precisely so the *denied* and *store* subtypes are not swept in. The store error therefore is **not** fail-fast, classifies `TRANSIENT_RETRY`, and **re-enters the retry staircase**. **This answers `B-84`'s filing D-2 falsifier affirmatively:** the landed fail-fast half **is** re-enterable through the store-error class.
- **Stated against interest.** This is a **pre-existing** residual that `B-84`'s ratified Reading A explicitly priced as **out of scope** (A never claimed to close store errors) — **not** a regression the build leg introduced, and **not** a new defect. `B-84` left it **open and measured**.
- **Why this row exists.** `B-84` is **CLOSED**, so the residual currently lives only inside a closed row's `close_out` and is **not open-row-queryable**. This row makes the live residual queryable at its own id. **No disposition is committed here.** *Ratified by the operator 2026-08-07 on recommendation.*
- **Close-out steps.** **(1) Ground first** at the then-current HEAD, by content: the `memory_id` derivation (which dimensions are hashed, and specifically whether `provider` is among them), the record-append path, the ledger's 18-field equivalence payload, and that `_classify_provider_exception` still admits only the two memory types by name. If any has changed, the mechanism may no longer hold and this row closes with that finding. **(2) Decide the disposition** — candidate readings, none pre-selected: **(a)** make `provider` part of the capture identity (collapses cell (3) into cell (1) — an extra *distinct* note — but is an **identity-dimension change to a content-addressed id**, carrying the full new-surface hash audit and an already-persisted-address-space question); **(b)** leave identity alone and make the store-error class non-re-enterable (addresses the **staircase** half, not the duplicate line); **(c)** accept and document. **(3) Note the interaction** with `B-84`'s other two open residuals (policy-flip TOCTOU, intra-call window). **(4) Witness** with the same *executed*-matrix discipline plus a PD-8 mutation probe — a code-read inference is **not** sufficient here, since this row's whole value is that cell (3) was **executed**.
- **Council: no at registration; conditionally yes at step (2).** Framing a measured residual commits to no design. Reading **(a)** *would* be council-eligible — an identity-dimension change to a content-addressed `memory_id` trades capture fidelity against the stability of an already-persisted address space (C3 ⊥ whichever voice owns memory-capture semantics). Convene **only** if step (2) leans (a), and only after step (1)'s grounding. Readings (b)/(c) sit within a cleared contract and owe no convening.
- **Cross-ref.** Successor to **`B-84`** (closed; `pr:` #1228 + #1233 + #1243) — specifically its `close_out` W-5 cell (3) record and the filing's **D-2** falsifier.

- **CLOSED — 2026-08-08, disposition (b′) BUILT. All four registered steps executed.** **(1) Grounding** was done by content at HEAD `1bce5d15`, **not** from this row's own snapshot — which had drifted: the row says `_classify_provider_exception` admits only the input and internal memory types, but at HEAD it admits **five** named types plus the 401/403 duck-typed arm, `LLMDispatchPayloadShapeInternalError` having been added by the `B-116` impl leg (PR #1271) *after* this row was written. The store subtype was nonetheless re-confirmed **absent** from that tuple, so the mechanism held — and was re-confirmed **by execution**, the landed W-5 cell-(3) witness running green at HEAD. The `memory_id` derivation, the record-append path (`_JSONL_BY_KIND` includes `TOOL_EVENT`; `_append_jsonl` is unconditional) and the ledger's single conflict raise site with its two 18-field projections (`provider` is field 11 of 18; `timestamp` is **excluded**) were all re-read directly. **(2) Disposition (b′)**, per the #1265 grounding which falsified (b)-as-registered. Reading **(a)** is **not** built and remains the registered residual. **No council was owed** — the council row made convening conditional on step (2) leaning (a); it leans (b′), which sits within a cleared contract. **(3) `B-84`'s other two residuals are neither re-opened nor contradicted, structurally rather than by assertion:** the policy-flip TOCTOU window lives at the **policy resolver**, which this leg does not touch; the intra-call window is the record-before-ledger ordering, which this leg **preserves byte-for-byte** — AC #6 pins it, and W-5 cell (3)'s `== 2` duplicate-line assertion is **PRESERVED VERBATIM** so the leg cannot claim a closure it did not deliver. What (b′) closes is the **retry amplification** (one duplicate instead of `max_attempts` duplicates), not the duplicate line. **(4) Witnessed** with the executed-matrix discipline plus five PD-8 probes, all confirmed RED then restored. **Determinism — which §14.6.3 row 6 required but which neither the spec nor the council DELIVERABLE ever defined (`REVIEW-e2.md` F-05 flagged exactly that circularity)** — is discharged against a four-part definition this leg supplies and states at contract altitude: repeat-invariance (8/8), candidate-independence, state-driven-not-stochastic (the origin candidate's payload resolves as `IDEMPOTENT_NOOP` — the positive control that stops repeat-invariance passing vacuously), and structural clock-exclusion. Witnessed at the **ledger**, not the executor, so the evidence survives future re-typing of the executor surface. **Scope widened beyond this row's own framing, recorded rather than smuggled:** the row named ONE executor surface; grounding found **three**, and the two it missed escaped **raw** onto the staircase — both proven reachable **by execution** before scope was fixed, per this row's own requirement. **Honest boundary:** the refusal is deterministic over the *same* inputs, not permanent under all inputs. **Artifacts:** Runtime spec v1.113, plan v2.61 U-RT-153 (a NEW unit — U-RT-152 is landed and closed), both clearance markers. **`B-132` narrowed, not closed; `B-134` minted.**
- **GROUNDED — 2026-08-07 (grounding leg at main `35afeda1`): classification (a) BUILDABLE as reading (b′), SEQUENCED AFTER the B-116 ratification.** All six row cites verified EXACT at HEAD (zero drift since `73b0e87b`). Probes AMPLIFY the harm: the duplicate is not singular — the record JSONL grows **N+1 lines for N staircase re-entries** (measured 4 lines after 3 retries) while the operation ledger stays flat, the same harm-class B-84 Reading A closed for the argument-shape class. Reading (b) as registered is **FALSIFIED**: `_capture`'s broad catch (`memory_capture.py:892`) wraps genuine transient I/O too, so a blanket fail-fast for `MemoryToolExecutionStoreError` would kill retries for real flakes — the identical objection the code records for the denied class at `retry_breaker_fallback.py:343-349`. **Buildable shape (b′)**: split the DETERMINISTIC ledger idempotency-conflict out as a distinct non-retryable type keyed on a closed discriminator (`MemoryCaptureResult` closed-schema extension ~15 lines; a `MemoryToolExecutionConflictError` raise ~8; BY-NAME fail-fast admission ~6; ~90 witness lines incl. line-count-caps-at-2 and a transient-I/O-still-retries positive control), justified under Runtime §14.6 D2's iff-rule — no spec amendment needed. **COUPLED to `B-116`**: the (b′) fix routes a new deterministic class onto the exact fail-fast branch B-116 re-scopes — its breaker disposition is row (6) of the B-116 filing's table; build only after that ratification. Reading (a) (`provider` into capture identity) stays registered as the residual — (b′) caps the duplication at 2 lines, it does not eliminate it.

### B-116 · Should DETERMINISTIC harness-internal faults count toward a PROVIDER's circuit-breaker state? *(pre-existing; spans C-CP-03 §3.4+§3.5 + Runtime §14.6; RATIFIED Reading (II) 2026-08-07 — spec v1.112 + plan v2.60 landed; U-RT-152 impl leg merged #1271; **CLOSED at the `B-116-t3` OD leg — OD v1.37 §9.2 18 → 19 + plan v2.32 U-OD-58**)*

- **The question, which this row frames rather than answers.** When the retry/breaker/fallback dispatcher **fail-fasts** a candidate, it records **one breaker failure against that candidate's PROVIDER** before exiting. Should a **deterministic, harness-internal** fault do that?
- **Grounded by content at HEAD `73b0e87b`** *(the inbound note's `~:1025` is **corrected** — `:1025` is the `"retry.fail_class"` attribute-set line)*. `retry_breaker_fallback.py` computes `breaker_cause = _classify_breaker_cause(exc)` at **`:1006`**, and on the `cause is None` (fail-fast) branch calls `transition = breaker.record_failure(cause=breaker_cause)` at **`:1028`**. The **fail-fast set** (`_classify_provider_exception`, from `:272`) has **five** members: `LLMDispatchProviderUnreachableError`; `LLMDispatchPayloadShapeError`; a raw SDK exception with `.status_code in (401, 403)`; `MemoryToolExecutionInternalError`; `MemoryToolExecutionInputError`.
- **The sharpened framing — finer than "internal faults get tagged as provider faults".** `_classify_breaker_cause` is duck-typed on `.status_code` and returns `None` for anything lacking one (`:404-406`), so a `MemoryToolExecutionInternalError` yields `cause=None` — it is **not mis-tagged**. But `record_failure` (`retry_breaker.py:219`) **increments `fail_count` regardless of `cause`** — `cause` is telemetry-only (`:228-233`) — so the failure **still counts toward `fail_threshold`** and still moves the provider toward `OPEN`.
- **The tension.** `MemoryToolExecutionInternalError` is **by construction** a harness-internal wiring fault (an unset `RuntimeMemoryContext.record_scope`/`.scope_ref`; a `MEMORY_TOOL_CONTRACTS` entry whose input schema has no `properties` mapping) and is admitted to fail-fast on the express grounds that it is *"deterministic and candidate-independent"*. Such a fault is **not evidence about the provider's health** — yet it consumes provider breaker budget, and a repeated internal misconfiguration could **open a healthy provider's breaker**.
- **Pre-existing, not introduced by any recent arc.** Spans **C-CP-21** and **Runtime §14.6** (Runtime + CP). **No commitment to a change is made here.** *Ratified by the operator 2026-08-07.*
- **Close-out steps.** **(1) Ground first** by content: `_classify_provider_exception`'s membership, `_classify_breaker_cause`'s `None` return, the `record_failure` call on the cause-`None` branch, and `record_failure`'s counting semantics — **the whole framing rests on `record_failure` counting independently of `cause`, and that is the first thing to falsify.** **(2) Establish the contract position BEFORE deciding anything:** read **C-CP-21** and **Runtime §14.6** for what a breaker failure is *defined* to mean. If the contract already says, this is a **conformance** question, not a design fork; if it does not, that silence is itself the finding. **(3) Only then decide.** **(4) Note the scope asymmetry** when pricing: the question applies with **different force to each of the five members** — 401/403 is genuinely a provider-credential fact and unreachable is genuinely a provider fact, while payload-shape and the two memory types are harness-side. Any fix must be **per-member, not wholesale**, or it will silently stop counting real provider auth failures.
- **The discriminator.** Does the breaker count *"this **CANDIDATE** failed to produce a result"* or *"this **PROVIDER** is unhealthy"*? Under the first, an internal fault legitimately counts — the candidate did fail, and the chain is right to move on. Under the second it must **not** count, and the fix is to skip `record_failure` (or record on a non-provider scope) for the deterministic harness-internal members.
- **Council: no at registration; conditionally yes at step (3).** The tension is nameable in advance — changing what counts toward a breaker trades control-plane reliability semantics (C1/C9: a breaker that **under**-counts fails to protect a genuinely failing chain) against operator-loop robustness (C11: a breaker that **over**-counts opens a healthy provider on the operator's own misconfiguration). Convene **only** if step (2) finds the contract silent **and** step (3) leans toward changing the counting rule.
- **Cross-ref.** The `MemoryToolExecutionInternalError` fail-fast membership was established at the **`B-88`** spec leg (U-MEM-28) and extended to a seventh raise site by **`B-114`**; the `MemoryToolExecutionInputError` membership at **`B-84`**. Neither arc examined the **breaker-counting** consequence, which is why it is registered here.

- **GROUNDED + FILED — 2026-08-07 (grounding leg at main `35afeda1`).** Step (2) executed: the contract is SILENT everywhere (C-CP-03 §3.4 fixes scope only; C-OD-07 §7.1 never defines a failure; Runtime §14.6 D2 governs the retry split; C-MEM-19 disclaims) — **the silence is the finding**, and §14.6 step 4's prescribing bullet is under-enumerated (2 named vs 5 shipped discriminators). THREE row corrections carried at the filing, cite it forward, not this row's bullets above: the breaker is **PER_MODEL `provider:model`** (no PER_PROVIDER breaker is constructed anywhere), the method lives on **`BreakerStateMachine`** (`retry_breaker.py:219`; "BreakerRegistry" does not exist), and the breaker contract is **C-CP-03 §3.4+§3.5**, not C-CP-21. Executed probes show the harm BROADER than registered: the fault is candidate-independent, so one dispatch burns one failure on EVERY chain breaker across ALL providers (Probe B), and `fail_threshold` repeats open them all (Probe C); the landed witness at `test_lifecycle_retry_breaker_fallback.py:1089-1170` green-blesses the trip. **Class 2 fork FILED: `.harness/class_2_fork_b116_breaker_failure_semantics_harness_internal_faults.md`** — per-member disposition table (5 members + the prospective B-115 (b′) type), recommendation reading (II) don't-count for harness-internal members #4/#5, count for #1–#3. `status: registered_finding → operator_gated` — ratification is the one genuine gate. **COUNCIL RUN — 2026-08-07 (operator-convened via the ratification AUQ's 'council first' answer).** Full workflow at `.harness/council/b116-breaker-semantics/`: E1 (C9/C11 primaries independent → C1/C7 consultants → cross-read; seam #5 resolved by C9 concession on two self-found falsifiers), E2 adversarial (8 findings incl. one halt-class: member #1's three raise sites are harness registry lookups, never provider contact), E2b bounded reconcile (operator-authorized; #1 DEMOTED, #2 SPLIT 3-pre-flight/25-response-parsing via the A-ii re-type precedent, guard re-priced ~30-35 lines, recovery-model rationale adopted as the normative test). **Council-unanimous corrected package at `DELIVERABLE.md` — reading (II), 7-row table, terms t1-t5 with the OD §9.2 pin as a separate CLOSURE-gating leg. RATIFIED 2026-08-07 — see the RATIFIED + APPLIED bullet below.** Codex E3 leg skipped (quota floor; recorded deviation — may run cold against the DELIVERABLE post-reset if wanted).

- **RATIFIED (Reading II) 2026-08-07, APPLIED 2026-08-08; `status: operator_gated → open`.** The operator ratified the council-unanimous package after reviewing the full deliberation ledger. **Spec leg:** Runtime **v1.111 → v1.112** — NEW **§14.6.3** carrying the recovery-model normative test as contract (*a fault charges the provider-model breaker only if a half-open trial call could return a different result than the trip did, for a reason attributable to the `{provider, model}` the breaker is keyed to*), the 7-row per-member disposition table (2 count / 4 waived / 1 prospective-conditional), the four-type waiver tuple enumerated BY NAME, terms t1–t5, the persistence pin, C9's trip-threshold revisit-trigger and the dead-half-open-latch residual declared honestly — plus TWO strict appends (§14.6 step 4's fail-fast bullet; the §14.6 `retry.*` invariant bullet admitting t1+t2). **Plan leg:** Runtime plan **v2.59 → v2.60** — NEW **U-RT-152** (the guard ≈30–35 src lines across `retry_breaker_fallback.py` + `llm_dispatch.py` incl. the row-2a re-type to NEW `LLMDispatchPayloadShapeInternalError`; Probes B/C as assertions; positive controls incl. the store-subtype BY-NAME control; the 3/25 raise-site partition witness; PD-8 throughout) + the standing probe-text rider discharged at its §2. **ZERO contract numbers, ZERO CXA rows (aggregate frozen at 111), ZERO OD roster delta at this leg** — the OD §9.2 widening (18 → 19) is the separate **`B-116-t3`** leg, a NAMED closure gate per §14.6.3 t3′. **FIFTEEN forward rows minted at this PR** — `B-118`–`B-131` from the DELIVERABLE's forward-row enumeration plus `B-132`, the classifier catch-all residual surfaced at this leg's fresh-context review — none priced into this row. **Why `open` and not `closed`:** the ratified design substrate has landed, but the impl leg and the t3 leg are owed — `B-116` closes only when BOTH merge (the `B-96`/`B-57` enum reading). `B-115` (b′) proceeds after this leg and the impl leg; its type joins the waiver tuple only on confirmed determinism at its own build (racy ⇒ §14.6.3 row 6 VOID + C5 reroute). The Codex E3 out-of-family leg remained quota-floored at ratification; it MAY run against the spec-leg PR or the impl leg per §13.1 discipline.

- **CLOSED 2026-08-08 — BOTH legs landed; `status: open → closed`.** §14.6.3 t3′ made closure conjunctive, and both conjuncts are now satisfied. **(i) Impl leg — merged PR #1271** (merge commit `3ba68285`): U-RT-152's negative type check over the four-type waiver tuple at the fail-fast `record_failure` site, the row-2a re-type to `LLMDispatchPayloadShapeInternalError`, the t1+t2 waived-charge attribute pair on the inner attempt span, Probes B/C as assertions and the 3/25 raise-site partition witness. **(ii) `B-116-t3` OD leg — THIS PR:** `Spec_Operational_Discipline_v1_37.md` amends **C-OD-09 §9.2 18 → 19**, adding `fallback.exhausted` (source declaration `C-CP-03 §3.5`) on the CP-declares/OD-ingests chain — CP `:410` declared it always-sampled while OD dropped the row at original ingestion, even as its two CP-table siblings at `:409`/`:411` were absorbed at OD `:521`/`:522`. `Implementation_Plan_Operational_Discipline_v2_32.md` adds **U-OD-58**; the substrate roster, the five fixture edit points and two *unpriced* `src/` count claims found by sweep are reconciled in the same commit. **§9.3's "inviolable" was reconciled, not treated as a bar** — it governs operator tunability at the deployment-binding layer, not contract revision at the venue where the set is defined (C7's F-03 ruling); what it *does* establish is why membership is the only repair that survives operator tuning. **Rider (a) EXECUTED and PASSES** — the C-OD-11 §11.1 per-cell budget (`cell_rate_limit=10_000.0`; `tenant_rate_limit=1_000.0` at the two multi-tenant cells) enforces at COLLECTOR_BOUNDARY / BACKEND_INGESTION, **downstream of and independent of the sampling decision**, while `fallback.exhausted` remains a terminal once-per-exhausted-chain event with ONE emission site (`retry_breaker_fallback.py:947`) and no per-attempt multiplier; the F-08 population question stays **explicitly unmeasured** per C7's own FM-K refusal, and no term depends on it. **Riders (b)/(c)/(d) cross-referenced to `B-123`/`B-124`/`B-125`, NOT re-minted** — none is a precondition of the roster row. **ZERO contract numbers, ZERO new namespace (C-OD-05 §5.1 roster unchanged at 15), ZERO CP delta, ZERO Runtime text moved, ZERO CXA rows (aggregate frozen at 111), ZERO emission-site change, ZERO hash impact.** One finding surfaced at the leg's own PD-8 pass and was closed in-unit rather than registered: probe (iii) came back GREEN because `test_composite_sampler.py`'s `_LITERAL_ALWAYS_SAMPLED` tuple had **no completeness assertion** behind it — a hand-maintained `parametrize` argument that any future §9.2 amendment could have left silently short. Clearances: `spec-operational-discipline-v1-37-cleared-2026-08-08.md` + `implementation-plan-operational-discipline-v2-32-cleared-2026-08-08.md`.

### B-117 · Duplicate pytest test-file BASENAMES across workspace packages silently drop witnesses from collection, and nothing guards against it *(lifted to its own row 2026-08-07 from the `B-112` close-out's registered ADJACENT INFRA GAP, per that row's own "a future infra arc may lift it to its own row")*

- **CLOSED 2026-08-11 (#1315)** — the cheapest close-out shape built: `tools/module_path_guard.py` + root `conftest.py` raising `pytest.UsageError` at session start when ≥2 `harness-*/tests` files claim the same **relative module path** (the collision unit sharpened from bare basename during the build: `tests/test_x.py` vs `tests/integration/test_x.py` are distinct modules — the relpath rule reports exactly the drop shape with zero false positives; nested `integration/` subpackages exist at HEAD). Failure venue: collection-time hard failure everywhere (local + every CI axis job) — a CI-only step would leave local runs green while witnesses drop, the defect's own signature. Re-measure at HEAD (step 1): zero collisions across 517 files (both pytest discovery patterns, nested dirs included; recounted after codex r1/r2 broadened the scan). 15 witnesses — incl. a real-entry-point SUBPROCESS witness for the conftest half (a synthetic colliding workspace aborts an actual pytest session with the guard report) and probe-derived cases for both codex catches (*_test.py drop; outermost-contiguous-package anchoring with the broken-chain collision + non-package legality; r4 norecursedirs exclusion; r6 non-identifier package-boundary anchoring) — plus a LIVE e2e probe (a real duplicate aborted an unrelated package's run; removal restored green). The heavier candidates (per-package rootdir isolation; renaming the seven `tests` packages) priced and not taken — the hook covers the class. ONE residual class recorded, held with grounds (codex r8 vs its own r4, the opposite-direction pair): collisions under norecursedirs dirs reachable only via EXPLICIT pytest args are outside the guarded set — the gate protects recursive suite runs; including them re-opens r4's realistic false-positive block. CI roster: `test_module_path_guard.py` joins the tools pytest step.

- **What it is.** Every `harness-*/tests/` directory is a package literally named `tests` (all seven carry an `__init__.py`), so two test files that share a **basename** across two packages resolve to the SAME module path. pytest imports ONE module for BOTH paths and **silently drops** the loser's tests. This is not hypothetical: at the `B-93` build leg (PR #1241) `test_b93_cross_process_lock_deadline.py` existed in both `harness-is/tests/` and `harness-runtime/tests/`, and **12 written witnesses never ran** — 34 collected where 35 were written. The suite was **GREEN throughout**; it was caught only by recounting programmatically.
- **Why it is registered rather than absorbed.** The defect is silent in the worst direction — it removes coverage while every signal (exit code, pass count, CI green) says nothing is wrong. It is invisible to the `[[count-claims-drift-every-review-round]]` discipline unless someone happens to compare *written* against *collected* for that specific pair of files.
- **`--import-mode=importlib` is ALREADY SET and does NOT fix it — reproduced at HEAD `e711a857`, stated against the `B-112` close-out's own candidate list.** Root `pyproject.toml:380` carries `addopts = "--import-mode=importlib"` (with an explanatory comment at `:377-379` naming exactly this collision). A positive-control probe — one `test_zz_collision_probe.py` in `harness-is/tests/` (1 test) and another in `harness-runtime/tests/` (2 tests) — collected **2 of 3**, and the run reported `harness-runtime/tests/test_zz_collision_probe.py::test_is_side_probe`: the runtime path was resolved to the **IS** module's contents. The `__init__.py`-bearing `tests` packages defeat the import-mode fix. Probe files were removed after the measurement; nothing was left in the tree.
- **The invariant the tree actually relies on.** UNIQUE TEST BASENAMES REPO-WIDE — **currently true** (511 `harness-*/tests/**/test_*.py` files, zero duplicate basenames, measured at HEAD `e711a857`), **currently unenforced**, and violated **silently rather than loudly**.
- **Close-out steps.** **(1) Re-measure first** — re-run the basename-uniqueness count and the positive-control collision probe at the then-current HEAD; if a duplicate already exists, the guard's first run is also a repair. **(2) Price the guard against the three candidate shapes** the `B-112` close-out named, now that one of them is falsified: a **basename-uniqueness check** across `harness-*/tests/**` (the cheapest — a ~15-line `conftest.py` collection hook or a standalone CI step; the only shape the probe above leaves standing); **per-package pytest rootdir isolation** (heavier — changes how the whole suite is invoked); `--import-mode=importlib` **with unique package names** (the import-mode half is already in place, so this reduces to *renaming the seven `tests` packages* — a tree-wide rename, priced accordingly). **(3) Decide the failure venue** — a collection-time hard failure (loud, blocks the developer immediately, but fires inside every `pytest` invocation) versus a dedicated CI step (cheaper at inner-loop time, later feedback). **(4) Witness it under PD-8** — the guard must be shown to FAIL on a deliberately introduced duplicate basename and pass once it is removed; a guard that has never been seen red is not a guard.
- **Council: no.** A test-infrastructure hygiene guard with no cross-domain tension to name — the only tradeoff is inner-loop cost versus feedback latency (step 3), which is a single-domain judgment, not a nameable dyad. Route to a single voice plus `advisor()` if it is contested at all.
- **Trigger.** The next test-infrastructure arc, **or** any recurrence (a duplicate basename appearing, or a written-versus-collected count mismatch surfacing at any leg).
- **Cross-ref.** Surfaced on `B-112`'s close-out via merge-gate lens 3 (G3) at the `B-93` build leg; `B-112` itself is `closed` and its own subject matter (deadline'd lock polling and kernel handoff fairness) is unrelated to this row.

### B-118 · The half-open latch is dead code, so `OPEN` is an absorbing breaker state for the process lifetime *(surfaced at the B-116 council deliberation (C9), 2026-08-07)*

- **What it is.** `BreakerStateMachine.attempt_half_open()` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py:289`) has **ZERO production call sites** — the only three occurrences across every `harness-*/src` tree are two docstrings (`:51`, `:201`) and the definition itself (`:289`), re-verified at HEAD this session. `should_attempt()` (`:309-311`) is a pure state read that holds no clock, and `RuntimeRetryBreaker.get_breaker` (`:379-400`) caches breakers identity-stable for the process. Therefore **`OPEN` is an ABSORBING state for the process lifetime at HEAD** — the shipped breaker is a one-way latch. The contract already writes text for the transition the runtime never performs: OD spec v1.32 §7.1 names the *"caller-driven `open → half_open` cooldown-elapsed transition"* (`design-substrate/Spec_Operational_Discipline_v1_32.md:46`), and `record_failure`'s own `half_open → open` arm (`retry_breaker.py:239-251`) is unreachable for the same reason.
- **PRIORITY RAISED, and why.** This was C9's A1 recommendation 3 (register separately, do not fold into `B-116`). At the E2b reconcile it was **raised to the highest-priority forward row by finding F-06**: `B-116`'s ratified **normative test** is stated over exactly this path — *"a fault charges the provider-model breaker only if a half-open trial call could return a different result than the trip did, for a reason attributable to the `{provider, model}` the breaker is keyed to."* The test is not void (OD §7.1 contracts the transition, so it stands as design intent), but `B-116` now depends on a recovery path with no production caller. C9 stated the dependency direction plainly at cross-read: **ratifying a reading whose cost is a function of the latch ratifies the latch's consequences without ratifying the latch.**
- **Close-out steps.** **(1) Re-verify** zero production call sites and that `get_breaker` still returns identity-stable machines. **(2) Decide who holds the clock** — the state machine deliberately holds none (`retry_breaker.py:290-296` assigns it to the L8 orchestrator), so the caller must thread one; C9's Pillar-4 requirement is *half-open after the cooldown, one trial call, close on success, re-open on failure*, which `attempt_half_open` / `record_success` / `record_failure` already implement. **(3) Wire it** at or beside the breaker pre-check (`retry_breaker_fallback.py:674`). **(4) Witness both directions** — a real `open → half_open → closed` recovery AND an `open → half_open → open` re-trip; a green unit test on the state machine alone is not the witness, because the state machine is already green and the gap is reachability. **(5) Re-ground `B-116`'s witnesses** — C9's A1 recommendation 2 forbade asserting a half-open path while it is unreachable, so `B-116` asserts `fail_count == 0` and `state is CLOSED` instead; once this row lands those may be strengthened, and they should be, deliberately.
- **What would falsify it.** A production call site appearing (then the row is already closed), **or** a decision that absorbing-`OPEN` is intentional — in which case OD §7.1's transition text and `B-116`'s normative test both need amending, which is a Class 1 design-substrate route, not an impl fix.
- **Council:** **No at registration.** A single-domain C9 reliability-mechanism gap with no nameable cross-domain tension — the mechanism, the contract text, and the recovery model are all in one voice's lineage. **Conditionally yes** only if step (2) proposes a *durable* cooldown clock: that re-opens the persistence tension `B-116` term **t4** pinned (a durable breaker opened by a fault the operator cannot see survives the restart the operator reaches for first), which is a genuine C9 ⊥ C11 dyad.
- **CLOSED 2026-08-08 — PR #1275 (Runtime spec **v1.114** + plan **v2.62** U-RT-154 + impl, ONE bundled leg).** All five close-out steps executed. **(1)** Re-verified at HEAD `fb3dda44`: zero production call sites, `should_attempt()` clock-free, `get_breaker` identity-stable. **(2) The clock is SPLIT** — the **composer** holds the source (an injectable `monotonic` field defaulting to `time.monotonic`, mirroring the existing `sleep_fn` posture); the **state machine** holds only the caller-recorded `opened_at` deadline, because that state is per-`(scope, identifier)` and the composer has no per-breaker storage (a composer-side map would be a SECOND authority for breaker state). The machine still never reads a clock. NOT a `RuntimeConfig` scalar. The in-tree shape template is `BreakerGuardedSigningBackend` (`harness-runtime/src/harness_runtime/config/audit_signing.py`), which has carried an injected-clock + single-probe breaker since ADR-D8. **(3)** Wired at the pre-check, now **three-way**: `closed` proceeds / `open` attempts a conjunctively-gated trial **capped at ONE attempt** / `half_open` means a sibling holds the permit and is skipped with the new `half-open-trial-in-flight` reason. `should_attempt()` narrows to `state is closed`, which **is** the permit — `half_open` exists only while a trial is in flight, so the prior `state is not open` admitted every concurrent branch into a call contracted to be single. **No lock, and the absence is a decision**: all mutators run on one event loop, so the hazard is interleaving at the `await`, not a data race (contrast the signing breaker, which *does* lock, because span-end worker threads sign). **(4)** BOTH directions witnessed **end-to-end through the real composer** with an injected clock — `open → half_open → closed` recovery **and** `open → half_open → open` re-trip — plus a three-branch concurrency witness whose interleaving is **guaranteed** by an `asyncio.Event` rather than hoped for. **(5)** The `B-116` witnesses were strengthened **deliberately**: seven rows gained their now-available assertions (the Probe-C **clock-advance** form, which discriminates *never-opened* from *opened-and-reset*; the 401 and response-parsing **recovery-completion positive controls** that make the CHARGE mean something; the exhaustion-identity admissibility loop), three were left unchanged by name, and `B-115`'s IS-side determinism witnesses were untouched.
- **The one operator gate, and what was NOT derivable.** Grounding surfaced an interaction the ratified **Reading (II)** does *not* answer. With the `B-116` guard, a **waived** fail-fast during a trial charges nothing — so `record_failure` never runs and the machine is **stranded in `half_open`**, where `should_attempt()` refuses everyone and `attempt_half_open` returns `None`: **a NEW absorbing state strictly worse than the absorbing `OPEN` this row removes**, and one the wiring would itself have introduced. Four further cells (audit-signing hard failure, terminal HITL control flow, `DispatchFenceTrippedSignal`, `CancelledError`) strand identically. INCONCLUSIVE → re-arm is derivable from Reading (II)'s attributability clause; the **FRESH-vs-PRESERVED cooldown** choice is not, and was surfaced. **Operator-ratified 2026-08-08 (AskUserQuestion): FRESH cooldown**, `trigger_count = 0`, `fail_count` unchanged. All nine cells are contracted at **NEW Runtime spec v1.114 §14.6.4**. **The council condition did NOT fire** — the design is in-memory monotonic with nothing persisted, so the durable-cooldown-clock trigger for the C9 ⊥ C11 dyad was never reached and no council was convened.

- **Out-of-family review rounds 1 + 2.** Round 1 returned two REAL findings, both fixed: the **stale-outcome race** (the `_epoch` guard the in-tree `BreakerGuardedSigningBackend` template carries and the first build dropped — a stale SUCCESS resolving a trial it does not own closes an unhealthy breaker; fixed by a per-breaker admission epoch on all three `half_open` resolution paths, stale outcomes DROPPED without charging, `closed`-state accounting byte-unchanged) and **cell 4 unreachable** under the ONE-attempt cap (voided BY CONSTRUCTION, cap kept, not renumbered). Round 2 returned one mechanical count reconciliation (**11 ACs / 14 PD-8 probes**) and one **cross-transition** stale-outcome variant — a request admitted while `closed` that survives a trip AND a recovery, then resets `fail_count` on the tokenless `closed` arm. **Adjudicated PRE-EXISTING and REGISTERED, not absorbed**: the `closed`-arm success-reset is shipped consecutive-failure semantics reachable with **zero trips**, and guarding it would mean epoch-capturing every ordinary dispatch and rewriting contracted success-reset semantics — a redesign this leg does not price. Declared at Runtime spec v1.114 §14.6.4's honest-boundary paragraph and minted as forward row **`B-135`**. **Round 3** returned one further REAL finding — the round-1 release obligation guarded the DISPATCH but left the admission's own transition EMISSION outside it, so a raising span exporter stranded the shared breaker in `half_open` with no owner: **the absorbing latch this row removes, recreated by its own fix**, and strictly worse than absorbing `open` since the re-arm refuses a non-owner too. Fixed structurally (the protected region now begins at the **transition**, not the dispatch; the release there is state-only so the broken emitter is not re-entered and the original fault is not masked); the other four transition emissions were **swept** and verified to follow their state mutation. **Round 4** returned the MIRROR of round 3 at the inconclusive EXITS — a failing re-arm emitter raised BEFORE the bare `raise` and **replaced** the propagating `AuditSigningFailedError` / terminal HITL signal / `DispatchFenceTrippedSignal`, trading a contracted terminal mapping for a telemetry fault; fixed by a disposition flip keyed on whether an exception is in flight (suppressed **and attached** via `add_note`, never discarded), with the scope bound kept so exporter breakage still surfaces on the common path. **Round 6** found that §14.6.4's own text **overclaimed**: a `closed`-era dispatch keeps its multi-attempt policy and can resume attempt 2 across a trip, overlapping "the single trial" — but the non-re-consultation is **pre-existing** (the pre-check is per-candidate by contract) and the ratified intent bound THE TRIAL, never stale dispatches. Scoped §14.6.4 to **era-consistent admissions**, pinned the declared behaviour by witness, refused mid-staircase re-consultation as unpriced semantic surgery, and folded the attempt side into **`B-135`**. **The merge gate (#1275)** then MEASURED a witness gap the leg's own PD-8 discipline had missed: dropping `trial_token` from the max-attempts-exhaustion charge left the whole suite green while `fail_count` went 3→2 and `trigger_count` 3→0, because cell 5's witness asserted only `state is OPEN` + a fresh `opened_at` — **both equally true of an inconclusive re-arm**. Closed with the discriminating pair plus probe P17; the escalation site's token threading resolved by determining unreachability from content and pinning it; cells 7/8 given the `opened_at`-FRESH assertion; both `BaseException` asymmetries witnessed. Unit now **15 ACs / 17 probes**, closure criterion stated over the SETS rather than a count.
### B-119 · No per-provider breaker is constructed anywhere, so `BreakerScope.PER_PROVIDER` is a declared-but-unbuilt scope *(surfaced at the B-116 council deliberation (C9), 2026-08-07)*

- **What it is.** `BreakerScope` declares **two** values — `PER_MODEL` and `PER_PROVIDER` (`harness-od/src/harness_od/harness_breaker_schema.py:105-112`) — and both are contract-declared: CP §3.5 gives `harness.breaker.scope ∈ {per_model, per_provider}` (`design-substrate/Spec_Control_Plane_v1_2.md:402`, re-tabled at `Spec_Control_Plane_v1_3.md:75`) and OD §7.1 mirrors it (`Spec_Operational_Discipline_v1_32.md:38`). The **only** breaker constructed anywhere is `PER_MODEL`, at `retry_breaker_fallback.py:662-665`, keyed `f"{candidate.provider}:{candidate.model}"` (`:662`). C9 records this as his own **FM-O** (single-breaker-scope), live at HEAD.
- **Why it matters, in `B-116`'s own terms.** With no provider-scope aggregation, the **only** provider-wide health signal an operator can get today is *"every per-model breaker in the chain opened"* — which is precisely the signal a candidate-independent fault manufactures. `B-116`'s executed Probe B showed one dispatch burning one failure on **every** chain breaker across **both** providers, and Probe C showed `fail_threshold` repeats opening them all. So the absent scope is not merely unbuilt; its absence is what let a config-shaped fault imitate a provider outage.
- **Close-out steps.** **(1) Re-verify** that `PER_MODEL` is still the only constructed scope. **(2) Decide whether `PER_PROVIDER` should exist at all** — either build it (a second `get_breaker(BreakerScope.PER_PROVIDER, provider)` charge alongside the per-model one, with its own threshold and its own trip semantics) or record the enum member as deliberately forward-only in the same shape OD v1.32 used for `capability_shortfall` (`Spec_Operational_Discipline_v1_32.md:50`). Do not leave it declared-and-silent. **(3) If built, price the `B-116` interaction explicitly:** the ratified guard is stated on `record_failure` at `retry_breaker_fallback.py:1028`, and a *second* charge site must inherit the same negative predicate — otherwise the four demoted harness-internal members re-enter the breaker through the provider scope and `B-116` is silently undone. **(4) Witness scope discrimination** at the emission, which today can only ever report `per_model`. **(5) Note the coupling to `B-125`** — `emit_breaker_transition_event` defaults `tool_id` to the breaker key **only at `PER_MODEL` scope** (`retry_breaker.py:459-461`), so constructing a second scope changes which trips carry that default.
- **What would falsify it.** A `PER_PROVIDER` construction site appearing anywhere in `harness-*/src`.
- **Cross-note 2026-08-08 (`B-118` closed).** This row is **untouched** and not dependent on `B-118`, but its C11 half is materially changed. The registered council framing weighed detection breadth against *"one provider-scope `OPEN` disables every model of that provider at once, **and with `B-118` unfixed it does so irreversibly**"*. `B-118` is now closed — `OPEN` is escapable, bounded by a real cooldown with a single-trial half-open probe — so the irreversibility clause no longer holds and the C11 objection is **weaker** than when it was registered. The dyad is **not dissolved** (a provider-scope breaker still has a wider blast radius, and a recovery probe still costs one real call), but any future step-(2) deliberation MUST re-ground the C11 position against escapable-`OPEN` rather than quoting the registered text.
- **Council:** **No at registration** — nothing is decided here. **Conditionally yes at step (2):** building a second breaker scope trades detection breadth (C9 — a provider outage should be visible as one fact, not inferred from N per-model facts) against operator recoverability under a materially wider blast radius (C11 — one provider-scope `OPEN` disables every model of that provider at once, and with `B-118` unfixed it does so irreversibly). That is nameable in advance and dyadic.
- **CLOSED — 2026-08-08, PR #1277 (doc/register-only disposition leg). FORWARD-ONLY RECORD, not built** — the `capability_shortfall` shape (`Spec_Operational_Discipline_v1_32.md:50`). *This bullet states the CURRENT result; the bullets above are the row's registration text and are superseded where they conflict.* **(1) Re-verified at HEAD `49591934`.** `PER_MODEL` is still the **only** constructed scope: the sole production `get_breaker` call is `retry_breaker_fallback.py:774-776` (`breaker_identifier = f"{candidate.provider}:{candidate.model}"` at `:774`). The registration's `:662-665` is **line-drifted by `B-118`**, not falsified. `PER_PROVIDER` appears in `src` **only** at its enum declaration (`harness_breaker_schema.py:112`) plus one docstring mention (`config/audit_signing.py:81`); the only constructions anywhere are **tests** (`test_lifecycle_retry_breaker.py:187`, `:497`) through the scope-agnostic registry (`retry_breaker.py:546-568`). **The falsifier is not met.** **(3) Priced honestly, and the registration understates it.** The `B-116` waiver predicate is **free** to inherit — `_is_breaker_charge_waived(exc)` (`retry_breaker_fallback.py:456-467`) is purely exception-type-based over the tuple at `:420-427` and is **key-agnostic**, so the registration's fear that the demoted members "re-enter through the provider scope" is unfounded (and the tuple is **five**, not four, since `B-115` (b′)). What is **not** free is the `B-118` machinery: the pre-check at `retry_breaker_fallback.py:804-895` is a **single**-breaker state machine (one `half_open_trial` flag, one `trial_token = breaker.trial_epoch`, one one-attempt `effective_policy` cap, one `try/except BaseException → re_arm_half_open_trial` protection boundary). A second scope makes admission a **product of two independent permits** — per-model `CLOSED` × per-provider `HALF_OPEN` has **no defined cell**, and Runtime v1.114 §14.6.4's nine-cell matrix is written for **one** machine, so one dispatch outcome would resolve **two** trial tokens. That is a **Runtime spec delta**, not "a second `get_breaker` charge alongside". **Half-open coherence, answered:** a provider-scope trial *is* semantically coherent and §14.6.3's test is satisfiable at `{provider}` keying — the incoherence is **compositional**, not semantic. **(2)/(4) Demand is absent at HEAD, measured not assumed.** **Zero** consumers branch on `harness.breaker.scope`; the only production **read** of the value is `B-125`'s own default at `retry_breaker.py:627`. Everything else is declaration/manifest (`harness_breaker_schema.py:93`/`:267`, `retry_fallback_namespace.py:150`, `attribute_class_enforcement.py:53`, `content_structure_discipline.py:177`). **No config surface** — `fail_threshold`/`cooldown_seconds` are module constants, not `RuntimeConfig` fields (the only configurable breaker knobs under `config/` belong to the **signing** breaker, `audit_signing.py:60-61`). **No operator surface** — a `breaker` grep over `admin/inspect.py` and `cli/app.py` returns **zero** hits. Step (4) is therefore **void by construction** under this disposition. **(5) Discharged in the same leg:** because `PER_MODEL` remains the only scope, `:627`'s guard stays vacuously-true and `B-125` collapses to a declared bound (also closed at #1277, disposition (a)).
- **The four-disjunct demand trigger — falsifiable; any ONE re-opens this row.** **(i)** a `RuntimeConfig` scalar or `HARNESS_*` env knob selecting breaker scope; **(ii)** a consumer that **branches** on `harness.breaker.scope` (not merely declares or echoes it); **(iii)** an operator-facing surface (a `harness-inspect` row or CLI subcommand) reporting breaker state; **(iv)** a **second** `get_breaker(...)` call site anywhere in `harness-*/src`. *"We might want provider-level trips someday" is **not** a trigger* — this is the demand-trigger shape the workspace requires of a forward-only record, not a bare deferral.
- **Council, under this disposition: NOT convened.** The conditional C9 ⊥ C11 dyad fires **only if a future arc elects BUILD** at step (2). Any such deliberation MUST re-ground the C11 position against **escapable-`OPEN`** per the 2026-08-08 cross-note — `BreakerStateMachine` now carries `attempt_half_open` + `opened_at` + `trial_epoch` (`retry_breaker.py:236-268`), so the irreversibility clause the registered C11 text rests on is **falsified**.

### B-120 · The breaker-transition emission passes no `permanent_fail_repeats` and no `model_version`, so two declared attributes have no producer *(surfaced at the B-116 council deliberation (C9), 2026-08-07)*

- **What it is.** `RetryBreakerFallbackDispatcher._emit_breaker_transition` (`retry_breaker_fallback.py:1130-1136`) calls `emit_breaker_transition_event(transition, parent_span)` positionally and passes **none** of the three optional correlation keywords the registry accepts — `permanent_fail_repeats`, `tool_id`, `model_version` (`retry_breaker.py:430-437`). `BreakerTransition` itself carries no field for any of them (`retry_breaker.py:183-189`). Two of the three are therefore **dead at every shipped trip**: `harness.breaker.permanent_fail_repeats`, the declared C10 gating signal (*"Whether this trip is from repeated C5 permanent-fail-exits — the C10 gating signal"*, `Spec_Operational_Discipline_v1_32.md:42`; CP §3.5 `Spec_Control_Plane_v1_2.md:402`), and `harness.breaker.model_version` (`Spec_Operational_Discipline_v1_32.md:44`). C9 names this his own **FM-Q**, live.
- **The one-third correction, carried rather than dropped.** C9's A1 asserted all **three** are vacuous. That is true of the **call**, and false on the **wire** for `tool_id`: `emit_breaker_transition_event` sets `effective_tool_id = transition.identifier` whenever `tool_id is None` at `PER_MODEL` scope (`retry_breaker.py:459-461`), with the default stated as contract in the docstring (`:445-446`). C7 repeated the *"never populated"* claim at A2, C11 falsified it at cross-read, and C7 **formally withdrew** it at E2b. Since `PER_MODEL` is the only constructed scope (`B-119`), **every** shipped trip already emits `harness.breaker.tool_id = "provider:model"`. That half is `B-125`'s row, not this one.
- **Why it matters directionally.** C9's warning, not a slogan: the fail-fast branch maps to `PERMANENT_FAIL_EXIT` (`retry_breaker_fallback.py:1024-1027`), so **the moment anyone wires `permanent_fail_repeats`** — the natural follow-on — it begins gating on exactly the branch `B-116` re-scoped. Under the rejected reading (I) that would have driven C10's `gate_combination` to gate a *healthy* model+tool combination off a harness config bug. Reading (II)-as-ratified makes the signal more meaningful, not less, but the sequencing is load-bearing.
- **Close-out steps.** **(1) Re-verify** that the call site passes no keywords and that `BreakerTransition` still has no carrier fields. **(2) Decide per attribute whether a real producer exists** — `permanent_fail_repeats` needs per-cause failure history the state machine does not keep (the same gap `FIVE_XX_STREAK` already carries in-code at `harness_breaker_schema.py:145-149`), whereas `model_version` needs a binding-side value the dispatcher demonstrably has. These are not one task. **(3) For any attribute with no producer,** either wire it or record it as a forward-compatible slot in the shape OD v1.32 used for `capability_shortfall` (`Spec_Operational_Discipline_v1_32.md:50`) — declared-and-dead-and-undocumented is the state to leave. **(4) Sequence AFTER `B-116`'s guard lands,** per C9's own directional warning above. **(5) Witness** a trip carrying each newly-populated attribute, and a trip that legitimately omits it (the attributes are optional by contract, and `absent` is a meaningful value here).
- **What would falsify it.** A producer already existing for either attribute on some path this search missed.
- **Council:** **No.** Populating declared-but-unset attributes inside a cleared schema is adjudicated by the attribute owner (C7/OD); there is no cross-domain tradeoff to name. Route to a single voice plus `advisor()` if step (2) is contested.

### B-121 · `LLMDispatchProviderUnreachableError` means **unregistered** at all three raise sites, and the misleading name is a demonstrated defect vector *(surfaced at the B-116 council deliberation (C9 E2b reconcile, on adversarial finding F-01), 2026-08-07)*

- **CLOSED 2026-08-12 (#1317) — option (b′) via the U-HK-13 two-reviewer resolver (AGREEMENT: out-of-family codex, pick salvaged from its timed-out run's own loop-state record, + a neutral-brief Claude adjudicator with facts independently re-verified; RESOLVE row at `.harness/loop_status.md`), bundled Runtime spec v1.120 + implementation.** C-RT-14 now separates the genuine stage-3a network class `RT-FAIL-PROVIDER-UNREACHABLE` from the per-dispatch registry class `RT-FAIL-PROVIDER-UNREGISTERED`; the former row and C-RT-05 escalation remain byte-identical. All three registry-only raise sites now use `LLMDispatchProviderUnregisteredError`, and the C-RT-15/C-RT-16 failure narrative, waiver tuple, B-122 startup diagnostic, and active witnesses follow the accurate name. The v1.2 coherence row is qualified against the post-split C-RT-14 set; no Runtime plan unit, CXA row, cross-axis edge, configuration field, or behavior beyond diagnostic identity was added.
- **What it is.** The type is named *Unreachable*; **all three** of its raise sites are harness-side registry lookups that never contact a provider. Counted at HEAD: exactly **3** `raise LLMDispatchProviderUnreachableError` sites in `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py` — `:1525` (`provider_name not in self.providers`, an in-process dict), `:1549` (`_PROVIDER_OPERATIONS.get(...) is None`, whose own comment at `:1546-1548` reads *"Defensive — every key in `self.providers` is one of the three constructed at stage 3a per C-RT-05"*), and `:1865` (the terminal `else` of the `isinstance` adapter-arm chain). The class docstring is explicit: *"Raised when `binding.model_binding.provider` resolves to a provider absent from `ctx.providers`"* (`:184-193`). The semantics are **unregistered**, not **unreachable**.
- **Why it is a defect and not a nit.** The name is the **defect vector**, demonstrably. It misled the `B-116` fork filing's §5 table and **all four** council voices into keeping member #1 counting through A1 and the entire cross-read; the adversarial leg's halt-class finding F-01 demoted it on raise-site evidence, and C9 recorded the correction against himself: *"This is a defect I authored — my A1 row 1 came from the type's NAME, not its raise sites."* Under the ratified recovery-model test a half-open trial re-executes an in-process dict built once at bootstrap and re-fails identically for the process lifetime. Genuine provider-down never travels as this type at all: a real network fault raises the provider SDK's own exception, carries no `.status_code`, falls to the catch-all `TRANSIENT_RETRY` (`retry_breaker_fallback.py:371`) and charges the breaker at `:1072`/`:1091`, outside the branch `B-116` touches.
- **This is a Class 2 back-flow, and the row says so rather than walking it.** The type maps to the `RT-FAIL-PROVIDER-UNREACHABLE` fail-class token per **C-RT-14** (docstring `:189-190`), and that token is carried in cleared design-substrate — Runtime §C-RT-16's fail-class narrative propagates it through the wrapper (`design-substrate/Spec_Harness_Runtime_v1.md:4194`). Renaming the **Python type** is code-local; renaming or re-taxonomising the **token** is a `design-substrate/**` change and routes as a **Class 2 fork** per root `CLAUDE.md` §4.3, with the taxonomy value owned by C5/C7/OD — not by an impl leg. **Status stays `registered_finding`;** the fork route is named here, not taken.
- **Close-out steps.** **(1) Re-verify** the three raise sites and that none contacts a provider. **(2) Decide the scope:** **(a)** rename the Python type only, keeping `RT-FAIL-PROVIDER-UNREACHABLE` — cheapest, but leaves the operator-facing token still misleading, which is most of the harm; **(b)** mint a distinct `RT-FAIL-PROVIDER-UNREGISTERED` token and re-map, which is the design-substrate change. **(3) Route (b) as a Class 2 fork against the C-RT-14 taxonomy BEFORE any code lands** (X-AL-3 — no silent H_T design extension at Phase 7). **(4) Do not re-litigate `B-116`:** member #1's demotion stands on raise-site evidence regardless of what the type is called. **(5) Note the sibling** — `B-122` is the same raise sites' other half (should this fault be discoverable at bootstrap at all).
- **What would falsify it.** A fourth raise site that *does* contact a provider (then the type is genuinely heterogeneous and needs a split, not a rename — the `B-116` member-#2 shape).
- **Council:** **No.** The substantive question — does it count toward the breaker — was settled at `B-116` on evidence. What remains is a naming/taxonomy call inside a contract with a named owner, plus a routing decision this row already names.

### B-122 · Nothing validates at bootstrap that every fallback-chain candidate names a **registered** provider *(surfaced at the B-116 council deliberation (C9 E2b reconcile, on adversarial finding F-01), 2026-08-07)*

- **CLOSED 2026-08-12 (#1316)** — built as a stage-3b startup **DIAGNOSTIC**, deliberately not a gate: the close-out's venue counter-argument decided from the code's own commitments (skipped registration is a supported deployment shape per the `LLMDispatchProviderUnregisteredError` docstring's Ollama-degraded path; tool-only bootstraps run provider-free per v1.47 §2.1) — a hard refusal would be wrong, so `_warn_unregistered_chain_providers` logs once on `harness.runtime.fallback_chain` (the stage-5 prewarm best-effort venue precedent) naming the unregistered candidates + the dispatch-time consequence, and bootstrap continues. Partial-coverage limit stated in place (per-dispatch role-augmented chains, `retry_breaker_fallback.py`). Witnesses: partial-registration shape (openai registered / anthropic candidate not → warning + completed inference-bearing bootstrap) + all-registered silence; deletion mutation RED. The B-121 sibling closes in the bundled v1.120 naming/taxonomy split.

- **What it is.** The same three raise sites that ground `B-121` establish the shape: a fallback-chain candidate naming a provider absent from `ctx.providers` is discovered **at dispatch time, per dispatch**, as a runtime exception (`llm_dispatch.py:1525`) — and, after `B-116`, as one that charges no health counter at all. Nothing validates the chain against the registry earlier. The comment at `llm_dispatch.py:1546-1548` records that the provider set is fixed at **stage 3a per C-RT-05**, so the check is in principle decidable before any dispatch runs: both the chain and the registry are known. C9's framing — *"config faults fail at stage 3a, not at a runtime health counter"* — and he assigned the surface to C1/C11 rather than authoring it.
- **Stated honestly: this row is THINNER than its siblings.** C9 registered it in a single line at the E2b reconcile, with **no probe run, no venue chosen, and no shape priced**. Everything above beyond the raise-site facts is this row observing that the check is decidable — not a council finding, and not a designed proposal. It is registered so the observation is queryable, not because it arrived costed. Read it as a question, not a plan.
- **Close-out steps.** **(1) Ground the bootstrap sequence first** — where the fallback chain is composed relative to where `providers` is constructed, and whether a candidate can enter a chain **after** bootstrap. It can, partially: `_advance_or_exhaust` traverses a per-dispatch *effective* chain that may be role-augmented rather than the stage-bound one (`retry_breaker_fallback.py:878-883`), which bounds how total a bootstrap check can be. Answer this before pricing anything. **(2) Decide the failure venue, and answer the counter-argument first:** the class docstring names *"Ollama-degraded path skipped registration"* as an expected case (`llm_dispatch.py:186-187`) — if skipping registration is a supported deployment shape, a hard bootstrap refusal is **wrong**, and the correct answer is a startup diagnostic, not a gate. **(3) Price whatever survives (2).** **(4) Witness** with a configuration naming an unregistered provider, asserting the chosen venue fires and that a legitimately degraded deployment still boots.
- **What would falsify it.** Step (2) can kill the row outright, and that is a real outcome rather than a formality — the degraded-provider shape is sanctioned by the type's own docstring. Step (1) can also narrow it to near-nothing if augmented chains make the check substantially partial.
- **Council:** **No at registration** — the row is a question with no priced alternatives. **Conditionally yes at step (2)**, and only if step (1) finds the check can be made total: a bootstrap hard-fail trades fail-early configuration hygiene (C1/C9) against the degraded-provider local-deployment shape the docstring already sanctions (C11). Do not convene on the current thin state.

### B-123 · The tail-keep breaker trigger matches a **span name** while the breaker emits a span **event**, so the trigger may be inert *(surfaced at the B-116 council deliberation (C7 A2 consultant, registered adjacency 1), 2026-08-07)*

- **CLOSED 2026-08-09 at the Leg-1 impl (PR #1279) — read this bullet first; everything below it is the REGISTRATION HISTORY, preserved verbatim and superseded where it conflicts.** The settled step-(2) repair is BUILT: `is_classification_trigger` now resolves the two §10.2 trigger names against the span **name** and, when the name does not match, against the names of the span's **events**, via a DERIVED two-member frozenset composed from the existing name constants so the span arm and the event arm cannot drift apart under a rename. The owed doc obligation is discharged — OD spec v1.38 §9.2.1 term 3, which explicitly DECLINED to widen and named this row, is **replaced in full** at v1.39; plan leg v2.34 **U-OD-60** (twelve ACs). **§10.2 itself is NOT amended, and that is the point:** its table already specified the event carrier, so this was a conformance repair to cleared contract text, not a contestable spec direction, and nothing routed to the operator. **Admission-bounded** — per `B-137` the sibling tree is preserved only for head-ADMITTED carriers (~10% at base-rate 0.1, ~20% at 0.2); no full-floor claim is made. §10.2 row 1 stays `B-124`'s.
- **⚠ THE CLASS-3 RIDER BELOW IS FALSIFIED — do not carry it.** This row's own Class-3 informational bullet claims §10.2 row 1's cite (*"permanent-fail per ADR-D2 §1.8"*) does not resolve. **It does.** `design-substrate/ADR-D2.md` §1.8 is at line 301 and its table at lines 303–311 carries THREE columns — the second headed *"C5 fail-class"* — whose values include **`permanent-fail`** at `:305` (`escape_attempt`), `:306` (`egress_denied`) and `:309` (`signal`). The original claim was derived from the `sandbox.fail.class` value enum at `ADR-D2.md:285` alone and missed the fail-class column of the very table it cites. The rider was **NOT** carried into v1.39 or v2.34; the falsification is recorded at v1.39 §0.6 and plan §0.5, reached **independently at both legs** by separate direct reads.
- **A PD-8 probe changed the deliverable.** Deleting the event arm's own root-close materialize call left the first real-dispatch witness GREEN — exposing that the shape OD spec v1.38 §9.2.1 term 5 says actually ships (*the dispatch carrier IS routinely the root close*) had NO real-dispatch coverage under the widened flag. A **second** witness was ADDED rather than the probe restated, and the re-run turns it RED. A further out-of-family [P2] (a doubled `span.events` read per buffered span, a regression this arc introduced) was taken and is witnessed by AC #11's `== 1` counting double.

- **What it is.** `is_classification_trigger` matches the §10.2 breaker trigger on the **span name**: `if name == BREAKER_TRIPPED_SPAN_NAME` (`harness-od/src/harness_od/tail_keep_classification.py:94-95` — **re-grounded at HEAD**; C7 cited `:92-93`, which at HEAD is the sibling `SANDBOX_VIOLATION_SPAN_NAME` arm). The real breaker emission is a span **event on a parent span**: `parent_span_ref.add_event(name="breaker.tripped", attributes=attributes)` (`harness-od/src/harness_od/harness_breaker_schema.py:282`, with the shape committed in the function docstring at `:236-243`). If no span is ever *named* `breaker.tripped`, the §10.2 trigger is inert on the dispatch path — and every claim about breaker-trip visibility at tail-sampled production surfaces is smaller than either `B-116` primary assumed.
- **C7 filed this as a PROBE, not a finding — preserved verbatim here.** His own words: *"I surface this as a probe, not a finding — it needs a positive-control run before anyone cites it."* That framing is load-bearing and is not upgraded by registration. The row exists so the probe is queued, not so the conclusion can be cited.
- **Close-out steps.** **(1) RUN THE POSITIVE CONTROL FIRST.** Drive a real breaker trip through the tail-keep processor and observe whether the trace is preserved. Grep proves presence; only the run proves behaviour, and this row is entirely about behaviour. **(2) If inert, decide the repair** — match the event name as well as the span name, or emit a span so named. **(3) Widen the probe by one arm:** the identical span-name-only shape governs `sandbox.violation` (`tail_keep_classification.py:92-93`), so cover both rather than assuming the sibling is fine. **(4) Note the `B-116-t3` interaction:** C7's E2b argument for pinning `fallback.exhausted` to the §9.2 floor leaned on `head=1.0` **precisely because** the tail-keep mitigation might be inert. If this probe returns *the trigger fires*, t3's merits argument changes — it does not evaporate (a `head=1.0` guarantee does not depend on a trigger), but the row it rests on becomes weaker and should be re-stated rather than carried.
- **Council:** **No.** A probe with a binary answer and a named positive control. Convene nothing until it has run; if it returns *inert*, the repair is single-domain (C7 owns both the predicate and the emission shape).
- **Probe observation recorded at the `B-133` leg, 2026-08-08 — an OBSERVATION, not a closure; this row stays `registered_finding`.** The `B-133` positive control drove a REAL charging-fault dispatch to exhaustion through the REAL `TailKeepSpanProcessor` and observed the predicate directly on the carrying span: **`is_classification_trigger(span)` returned `False`** on the wrapper span `harness.runtime.retry_breaker_fallback` that carries the `breaker.tripped` **event**, and the trace's buffered siblings were consequently **dropped** at root close. **That answers this row's step (1) in the affirmative for the breaker arm: the §10.2 trigger is inert on the dispatch path.** It does **not** answer step (3) — the `sandbox.violation` sibling arm was not exercised — and it does not decide step (2)'s repair. OD spec v1.38 §9.2.1 **term 3 deliberately declined** to widen `is_classification_trigger` to event names while closing the §9.2 half beside it, and U-OD-59's witness **W7 pins the boundary** (an event-carried `breaker.tripped` forwards its own carrier under the §9.2 floor but sets no keep flag), so widening it later is a deliberate, test-visible act rather than silent drift. C7's *"I surface this as a probe, not a finding"* framing is preserved: the probe has now **run for one of its two arms**, and the result is recorded so the conclusion can be cited **for that arm** — with the repair decision still owed.
- **GROUNDED 2026-08-08, PR #1277 (doc/register-only leg) — steps (3) and (4) ANSWERED, step (2) SETTLED. Row stays `registered_finding`; it closes at the Leg-1 impl.** *This bullet states the CURRENT result; the bullets above are superseded where they conflict.* **(3) The `sandbox.violation` arm — RESOLVED NEGATIVE; the sibling IS fine.** It is emitted as a **real span**, not an event: `tracer.start_as_current_span("sandbox.violation")` at `harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py:725` (inside `_emit_sandbox_violation`, `:707-728`). An **exhaustive** `add_event(` sweep over all seven `harness-*/src` trees returns exactly **six** sites — `harness_breaker_schema.py:282`, `alignment_floor_drift_detection.py:242`, `retry_breaker_fallback.py:749`/`:879`/`:1151`, `retry_breaker_tool.py:314` — and **none** is named `sandbox.violation`. So `tail_keep_classification.py:92-93` matches a name that is genuinely produced, and this row **narrows to the breaker arm alone**. **What is still broken after `B-133`, sharpened by direct read.** The landed §9.2.1 arm is `tail_keep_span_processor.py:364-388`: it forwards the carrier, then at `:376-377` calls `is_classification_trigger(span)` — which at `tail_keep_classification.py:91-95` matches **span names only**. The carrier is named `harness.runtime.retry_breaker_fallback`, so the predicate returns `False`, the per-trace keep flag is **never set**, and `:386-387` then calls `_materialize_trace_decision`, which at `:445-452` **drops** the buffer when `keep` is False. **Net effect: the trip's own carrier span survives (`B-133`'s delivery), but its entire sibling TREE is dropped at root close.** `B-133` changed *signal* delivery, not *tree* preservation. The code says so deliberately at `:369-375` and names this row as owner; U-OD-59's **W7** pins the boundary, so the debt is test-visible, not silent.
- **(2) The repair — SETTLED, and NOT an operator gate.** The §10.2 contract table at `design-substrate/Spec_Operational_Discipline_v1_2.md:582-584` reads **verbatim** *"Parent + sibling spans of any `sandbox.violation` **event** preserved"* and *"…of any `breaker.tripped` **event** preserved"*. **The contract already says EVENT.** The name-only predicate is therefore **non-conformant to the contract as written**, and widening `is_classification_trigger` to scan `span.events` is a **conformance fix**, not a contestable direction — there is no genuine fork and nothing to route to the operator. Of the registration's two candidates, the **first** ("match the event name as well as the span name") is selected; the second mints a new emission primitive and is the X-AL-3 back-flow route `B-133` already refused as venue (b). **Shape + venue:** single-domain **C7/OD**; ~4 lines in `tail_keep_classification.py` reusing the `_carries_always_sampled_event` pattern (`tail_keep_span_processor.py:190-223`) but matched against the **two §10.2 names** rather than the §9.2 SSOT, plus the AC that the keep flag now sets for an event-carried trip, witnessed **by execution** through the real processor and a real exhausted dispatch. **One doc obligation rides it:** OD spec v1.38 §9.2.1 **term 3** explicitly declined to widen and named this row, so the impl leg owes a small OD spec realization delta flipping term 3 plus an OD plan unit in the U-OD-58/U-OD-59 shape. **Realism caveat the leg must state rather than overclaim:** per `B-137`, **non-root** spans are head-starved (~**9%** preservation measured at base-rate 0.1 for a non-root `sandbox.violation` trigger), so the repair delivers the tree only for head-**admitted** carriers. Correct to build; must not be sold as a full floor.
- **(4) The `B-116-t3` restatement — one line, and the direction is the OPPOSITE of the one anticipated.** `B-116` is **closed** (#1271 + #1272). C7's E2b argument leaned on `head=1.0` *precisely because* the tail-keep mitigation might be inert; the probe returned that the trigger **is** inert, so that argument is **CONFIRMED, not weakened**. t3's `head=1.0` guarantee never depended on a trigger either way, so the registration's conditional is **not triggered** and needs no carry.
- **~~Class-3 informational — the §10.2 row-1 cite does not resolve.~~ FALSIFIED 2026-08-09; preserved verbatim as registration history, NOT current (see the ⚠ bullet at the top of this row).** §10.2 row 1 cites *"classification == `permanent-fail` per ADR-D2 §1.8 fail-class taxonomy"* (`Spec_Operational_Discipline_v1_2.md:582`), but **ADR-D2 §1.8 is the SANDBOX-VIOLATION fail-class taxonomy** — `escape_attempt` / `egress_denied` / `timeout` / `oom` / `signal` / `exit_nonzero` / `policy_override` (`design-substrate/ADR-D2.md:285`, `:301`) — and contains **no `permanent-fail` value**. Non-blocking; the code's realization (`validator.fail.permanence == "permanent"` per C-CP-21 §21.6, `Spec_Control_Plane_v1_2.md:1914-1921`) is a downstream interpretation that happens to be sound. **Carry as a rider on the next OD §10.2 spec delta — which *is* the Leg-1 term-3 flip, so it costs nothing extra.**

### B-124 · `validator.fail.permanence` is never set on any span, so its OD §9.2 always-sampled row and its §10.2 tail-keep trigger are both inert *(surfaced at the B-116 council deliberation (C7 A2 consultant, registered adjacency 2), 2026-08-07)*

- **What it is.** Two live surfaces read this attribute. OD §9.2's always-sampled floor carries the row *"`validator.fail.*` at `validator.fail.permanence=permanent`"* (`design-substrate/Spec_Operational_Discipline_v1_2.md:530`), realized in code at `harness-od/src/harness_od/sampling_mode.py:136`. `is_classification_trigger`'s §10.2 row-1 arm reads the same attribute (`tail_keep_classification.py:58` declares the key, `:99` compares it). The value is **derived** by `validator_fail_permanence(fail_class)` (`harness-cp/src/harness_cp/validator_fail_taxonomy.py:169-176`) and declared in the CP schema at `:149`. But the function has **zero call sites** across `harness-{runtime,cp,od}/src`, and **nothing anywhere sets `"validator.fail.permanence"` as a span attribute** (searched at HEAD). Both rows are therefore inert on the dispatch path.
- **One refinement the council did not carry, added by direct read at HEAD.** There **is** exactly one structured carrier of the value: `ReplaySemanticDivergenceError.validator_fail_permanence: str = "permanent"` (`harness-od/src/harness_od/idempotency_join_dedup.py:361-368`, per §14.5.3). It is a frozen Pydantic record field, **not** an OTel span attribute — so neither the attribute-reading tail-keep predicate nor the always-sampled lookup can see it. The finding stands; its precise statement sharpens from *nothing carries the value* to **nothing carries it on a span**, which is what the two readers require.
- **Close-out steps.** **(1) Re-verify** zero span-attribute set sites and that the derivation function still has no callers. **(2) Determine whether a validator-fail span or event is emitted at all on any live path.** If none is, this row is about a whole missing emission rather than one unset attribute, and it must be **re-scoped upward before it is priced** — fixing the attribute on a span that never opens is motion, not progress. **(3) If an emission exists,** wire the derived attribute at it and witness that the §9.2 always-sampled lookup and the §10.2 trigger both fire, by execution rather than by grep. **(4) Carry it as rider (c) of the `B-116-t3` leg**, per C7's own registration — t3 is the arc that opens OD §9.2, and touching the table twice is the waste to avoid.
- **What would falsify it.** An emission site existing on a path this search missed — the search covered `harness-{runtime,cp,od}/src` only.
- **Council:** **No.** Wiring a declared-but-unset attribute inside a cleared schema, with a named owner (C7/OD). The only judgement is step (2)'s scope question, which grounding answers rather than deliberation.
- **GROUNDED 2026-08-08, PR #1277 (doc/register-only leg) — THE ROW SPLITS; it is not one finding. Row stays `registered_finding`.** *This bullet states the CURRENT result; the bullets above are superseded where they conflict.* Rider (c) of `B-116-t3` is **retired as a routing** (step (4)): `B-116` closed at #1271 + #1272 and the t3 leg is gone, so the two halves route independently. **(1) Re-verified at HEAD `49591934` — both halves hold.** `validator_fail_permanence()` (`harness-cp/src/harness_cp/validator_fail_taxonomy.py:169-176`) has **zero `src` callers** (only the definition, a docstring reference at `tail_keep_classification.py:60`, and three test assertions at `harness-cp/tests/test_validator_fail_taxonomy.py:76-83`); **no** span-attribute set site for `"validator.fail.permanence"` exists anywhere; the sole structured carrier remains `idempotency_join_dedup.py:366`. **(2) ANSWERED — an emission DOES exist, so the row does NOT re-scope upward; but the honest answer is sharper than the registration's binary.** A live `validator.evaluate` span is opened at `harness-cp/src/harness_cp/workflow_driver.py:5600`, attributes applied at `:5620-5622`, built at `harness-cp/src/harness_cp/validator_framework.py:320-352`, which on non-PASS sets **four** sibling `validator.fail.*` attributes at `:343-349` — `next_action`, `escalation_owed`, `class`, `detail_hash` — but **not** `permanence`. The registration's own falsifier ("an emission site existing on a path this search missed") **is met**: the search covered the derivation function and the attribute *name*, and missed the emission because the emission never names the attribute.
- **(3) The two readers do NOT share a fix — the load-bearing result.** **(A) The §10.2 half is CLOSEABLE.** `is_classification_trigger` reads the attribute off **any** span (`tail_keep_classification.py:96-100`), so setting `validator.fail.permanence` on the `validator.evaluate` span **would** make row 1 fire. **Owed at Leg 1, named — `pr: pending-leg-1`:** ~3 functional lines at `validator_framework.py:_build_span_attributes` deriving from `result.outcome` (`ValidatorOutcome.PERMANENT_FAIL → "permanent"`, else `"transient"`), witnessed **by execution** through the real `TailKeepSpanProcessor`, not by grep. **(B) The §9.2 half is NOT closeable by wiring the attribute and is RE-SCOPED FORWARD.** `is_always_sampled` resolves the `validator.fail.*` row against the span/event **NAME**, never against a carrier's attributes: `_conditional_always_sampled` gates on `event_name.startswith("validator.fail.")` (`harness-od/src/harness_od/sampling_mode.py:239-241`) and the literal / dot-anchored-prefix fallbacks (`:264-269`) also key on name. The live span is named `validator.evaluate`, so setting the attribute changes **nothing** there. Nor does `B-133`'s arm reach it — that arm resolves **event names** and this data is **attributes**. (A span named exactly `"validator.fail"`, the declared `SPAN_SITE_VALIDATOR_FAIL` at `harness-od/src/harness_od/validator_namespace.py:54`, would **also** miss: the derived prefix carries the dot anchor.) **This is a THIRD realization-mismatch shape — attribute-on-a-differently-named-span** — distinct from `B-133`'s (event-on-carrier) and `B-123`'s (event-vs-name at the §10.2 predicate). **The family is four rows, not three.** **Demand trigger for the §9.2 half:** any arc touching §9.2 **realization** (as opposed to membership) — the natural rider is `B-137`'s step-(3) posture decision, since candidates A/B/C all re-open how the floor resolves at the SDK boundary.
- **The taxonomy-collision caution — sequencing, not optional.** The naive wire (calling `validator_fail_permanence` at the emission) is **blocked by a type boundary**: that function consumes `ValidatorRetryExitClass` (C-CP-21 §21.1, `validator_fail_taxonomy.py:38-62`) while the live span's `validator.fail.class` carries `ValidatorFailClass` (C-CP-25 §25.2, `harness-cp/src/harness_cp/validator_framework_types.py:70-90`, whose own docstring at `:73-74` says it is *"Distinct from C-CP-21 §21.1 ValidatorRetryExitClass"*). That collision is registered separately as **`B-138`**, and **`B-138` MUST be dispositioned BEFORE any arc touches `_build_span_attributes`** — including Leg 1's (A) above — because deriving permanence from `result.outcome` mints a *second* derivation from a *third* taxonomy on a function whose authoritative domain is itself unsettled.
- **Cross-note 2026-08-09 (`B-138` CLOSED at #1278, disposition (a)).** The `B-138` sequencing gate on `_build_span_attributes` is **discharged** — the authoritative domain is settled (`ValidatorFailClass`, CP v1.116). **But the naive outcome wire this row planned (`permanent` iff `PERMANENT_FAIL` from `result.outcome`) is FORECLOSED as lossy** at CP v1.116 §1.3: `ESCALATE` conflates `permanent-fail-exit` with `HITL-recoverable`, so that wire would silently narrow §21.6's always-sample-permanent coverage to the abort path. This row now **owns the derivation sub-decision** (materialize the retry-exit classification at the emission site / ratify the narrowing WITH §21.6 re-specified / another carrier) and notes **`B-140`** (the `validator.fail` span-site gap) as ADJACENT, not gating: this row's §10.2 trigger reads the permanence ATTRIBUTE from any span, so the span-site gap gates the §9.2-style NAME-keying family (`B-123`/`B-133`/`B-137`), not this half (codex round-3 venue correction).
- **OPERATOR RATIFICATION 2026-08-16 — Reading A (materialize the retry-exit classification at the emission site).** Selected via `AskUserQuestion`; **discharges** the Class 2 fork filed at PR #1283 and makes the row **buildable**. **The fork recommended Reading D** (defer with a demand trigger) at `[MODERATE]`, resting on its §3(iv) — that the §9.2 half is the more valuable of the two and does not depend on the derivation landing now. **The operator selected A.** Recorded as provenance, not dissent: the recommendation was MODERATE and argued only that the derivation could *wait*, and Reading A is the direct repair that deferral was deferring. **Do not re-litigate it.** **What A commits:** derive the classification **at the emission site**, so `validator.fail.permanence` exists as something the sampling surfaces can read. The root problem it fixes is that the field is a **frozen Pydantic record field, not an OTel span attribute** — so neither the §10.2 attribute-reading tail-keep predicate nor the §9.2 always-sampled lookup can see it, which is why **both halves are inert**. A is the most direct of the four readings and puts work at **every emission path**; the implementing arc should expect that breadth rather than treat it as a single-site change.

### B-125 · `harness.breaker.tool_id` is a homonym-in-waiting: it echoes the breaker **key** at every shipped trip while its contract gloss promises a **tool identity** *(surfaced at the B-116 council deliberation (C7 A2 -> C11 cross-read falsification -> C7 E2b withdrawal), 2026-08-07)*

- **What it is.** The attribute is declared at OD §7.1 as *"Specific tool ID the failures correlate with (when scope is per-model and failures correlate with a specific tool)"* (`design-substrate/Spec_Operational_Discipline_v1_32.md:43`) and at CP §3.5 as *"per-model scope correlation"* (`design-substrate/Spec_Control_Plane_v1_2.md:402`); it sits in the nine-member `HARNESS_BREAKER_ATTRIBUTES` tuple at `harness-od/src/harness_od/harness_breaker_schema.py:98` (**re-grounded at HEAD** — C7 cited `:101`, which at HEAD is `harness.breaker.cooldown_ms`). What it actually carries is the **breaker key**: `RetryBreakerRegistry.emit_breaker_transition_event` sets `effective_tool_id = transition.identifier` whenever `tool_id is None` at `PER_MODEL` scope (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py:459-461`), with the default stated as contract in the docstring (`:445-446`). Since `PER_MODEL` is the only scope constructed (`B-119`), **every** shipped trip emits `harness.breaker.tool_id = "provider:model"`.
- **The correction chain, recorded because it is the row's whole substance.** C7 proposed at A2 populating the attribute with a real tool id to discriminate a memory-tool trip, on the claim it is *"never populated"* and that `BreakerTransition` has no such field. `BreakerTransition` indeed has no field (`retry_breaker.py:183-189`) — but the **registry supplies the default**, so the wire value is populated. C11 falsified the claim at cross-read by direct read of `:459-461`; C7 **formally withdrew** it at E2b: *"My A2 claim that `harness.breaker.tool_id` is 'never populated' is FALSE and I withdraw it."* The consequence C11 drew and C7 adopted **against his own proposal**: populating it with a real tool id makes one attribute carry two domains — tool identity at one call site, breaker-key echo at every other — discriminable only by string shape. That is exactly the cross-namespace homonym C7 had just **refused** for `capability_shortfall` (`harness_breaker_schema.py:141-143`).
- **Why it is registered rather than repaired.** C7 **declined the repair rather than pricing it**, on his own `B-131` standard: *the skip must demote the detail, never delete it* — and suppressing the `:459-461` default would **delete** the only populated meaning the attribute has at every shipped `PER_MODEL` trip. He also recorded the full price had he taken it: the default removal, the `:442-446` contract docstring, every witness asserting the default, and a re-read of CP §3.5's *"per-model scope correlation"* gloss. It is moot for `B-116`'s member #5 (conceded — there is no #5 trip left to discriminate) and **not owed** for the members that still trip, because `harness.breaker.cause` is populated at all three real `record_failure()` sites (`retry_breaker.py:451-453`, B-38), so those trips are already discriminated.
- **Close-out steps.** **(1) Re-verify** the default and that `PER_MODEL` is still the only constructed scope — this couples to `B-119`, since building `PER_PROVIDER` changes which trips receive the default at all. **(2) Decide the disposition BEFORE any caller passes a real `tool_id`:** **(a)** keep the breaker-key echo and forbid real tool ids on this attribute — in which case the OD §7.1 and CP §3.5 glosses are wrong as written and owe a correction; or **(b)** suppress the default and mint a separate attribute for the key echo, at C7's enumerated price above. **(3) The trigger is a caller passing a real tool id** — that is the moment the homonym goes live and the attribute becomes uninterpretable without a string-shape heuristic. This row must be dispositioned *before* that, not after.
- **Council:** **No.** An attribute-domain call owned by C7/OD, already argued to a conclusion in venue by two voices including a formal withdrawal. What remains is choosing (a) or (b) and paying for it.
- **CLOSED — 2026-08-08, PR #1277 (doc/register-only leg). DISPOSITION (a): keep the breaker-key echo, FORBID real tool ids, DECLARE the bound** — the `B-101` **(b)-PLUS** declared-bound shape ratified at Runtime v1.114. **No shipped behaviour changes.** *This bullet states the CURRENT result; the bullets above are superseded where they conflict.* **(1) Re-verified at HEAD `49591934`, with a line-drift correction.** `B-118` (#1275) **moved** the default: it is now `retry_breaker.py:626-628` (`effective_tool_id = tool_id` at `:626`; the `PER_MODEL` scope guard at `:627`; `= transition.identifier` at `:628`), and the contract docstring is now `:609-612`. The registration's `:459-461` / `:445-446` are **stale cites, not falsified findings** — the mechanism is byte-identical. The attribute still sits at `harness_breaker_schema.py:98`. `PER_MODEL` is still the only constructed scope (`B-119`, closed at this same leg as a forward-only record), so **every** shipped trip carries `harness.breaker.tool_id = "provider:model"`.
- **Two grounded facts that change the calculus.** **(i) The trigger is architecturally distant, not imminent.** Step (3) names it as *"a caller passing a real tool id"*. At HEAD there is exactly **one** production caller — `_emit_breaker_transition` (`retry_breaker_fallback.py:1477-1482`) — and it passes **no** `tool_id`. The composer that would supply one **does not participate at all**: `retry_breaker_tool.py:152-153` states *"per Q1a=(i) ratification, `get_breaker` / breaker transition events are NOT invoked at v1.15"*. A real tool id can arrive only **after** a tool-scope breaker lands — a separate, explicitly-ratified-as-absent surface. **The homonym cannot go live by accident.** **(ii) C7's enumerated price for option (b) is overstated at HEAD, falsified by count.** *"Every witness asserting the default"* is **one** test — `harness-runtime/tests/test_lifecycle_retry_breaker.py:583-604` — and it asserts only `emission.attribute_count == 8`, **never the value**. The only other occurrences in tests are attribute-**name** list assertions (`harness-od/tests/test_harness_breaker_schema.py:33`, `harness-cp/tests/test_retry_fallback_namespace.py:49`). (That witness constructs the breaker with identifier `"search-tool"` — a tool-shaped key at `PER_MODEL` scope, the homonym in miniature.) **The price correction does not flip the disposition:** C7's substantive objection — that suppressing the default **deletes** the only populated meaning the attribute has at every shipped trip, violating his own `B-131` *"demote the detail, never delete it"* standard — stands unchanged and is what decides it.
- **The promotion trigger (falsifiable, one disjunct).** The **first** caller passing a non-`None` `tool_id` to `emit_breaker_transition_event`. That is the instant the attribute carries two domains discriminable only by string shape, and it **re-opens this row to disposition (b)** — suppress the default, mint a separate attribute for the key echo — at the (now measured) price above. A second, weaker trigger is `B-119`'s: constructing `PER_PROVIDER` changes which trips receive the default at all. **`B-119`'s step (5) is therefore discharged here.**
- **The owed gloss corrections — carried as a NAMED RIDER rather than applied** (the #1263 stale-prose-carrier rider precedent). Under (a) the two contract glosses are **wrong as written**; this leg deliberately does **not** correct them because it edits no `design-substrate/` file. **(1)** OD §7.1 *"Specific tool ID the failures correlate with (when scope is per-model and failures correlate with a specific tool)"* (`design-substrate/Spec_Operational_Discipline_v1_32.md:43`) — owed correction: the shipped meaning is the **breaker key** (`"provider:model"`); a real tool id is forbidden until the promotion trigger fires. **(2)** CP §3.5 *"`harness.breaker.tool_id` (string; per-model scope correlation)"* (`design-substrate/Spec_Control_Plane_v1_2.md:402`) — same. **Rider owner:** the **next** OD or CP spec delta that opens §7.1 or §3.5 respectively MUST carry these; they are **not** a standalone arc. The code-side docstring at `retry_breaker.py:609-612` already states the default as contract and needs no change.

### B-126 · The `retry.*` namespace has three different cardinalities (manifest 4, code 6, wire 8 after `B-116`) and only the first is verified *(surfaced at the B-116 council deliberation (C7 A2 consultant, registered adjacency 3), 2026-08-07)*

- **CLOSED 2026-08-09 (#1289)** — built, and **this row's own framing was falsified in the process.** The three-cardinality structure holds and the corrected wire count of **TEN** is confirmed by two independent sweep shapes (a broad literal sweep and an AST emission-position sweep, both over `harness-*/src`, both returning exactly 10 distinct `retry.`-prefixed **attribute** keys = 5 declared at `C-CP-03 §3.5` + 5 undeclared). **But the discretion-lane premise is wrong**: of the 5 keys beyond §3.5, only `retry.skipped.candidate` rides pure telemetry-volume discretion — the other four are **mandated by name** at `Spec_Harness_Runtime_v1.md:4217` (§14.6 step 4), `:4364` (§14.6.3 t1+t2), and `:4227-4230` + `:5684-5687`. So `retry.*` is declared at **two contract venues** and the `C-CP-24 §24.1` manifest counts only one; it is not four declared keys plus a free-for-all. **Step (2) is answered as the re-grounding said** — the lane IS unbounded (`:4279`, `:5729` state no cap) — so what shipped is a **REGISTER, not a cap**: a new key is permitted, but not *invisibly*. Built: `RETRY_WIRE_REGISTER` (7 rows) + `RETRY_SPAN_AND_EVENT_NAMES` (3) at `harness-cp/src/harness_cp/retry_fallback_namespace.py`, count-language corrections at `cp_namespace_export_manifest.py` **and** the cross-axis-facing `cp_cross_axis_composition_manifest.py:118`, and **17 drift-catching tests / 29 assertions** at `harness-cp/tests/test_b126_retry_wire_register.py`, each load-bearing line shown RED under a mutation probe *(recounted programmatically at the arc close — an earlier "10" here and a contradictory "8" in the YAML were round-1/round-3 leftovers, caught by the merge gate)*. **ZERO** cardinality bump / §3.5 amendment / contract number / plan leg / design-substrate edit. **Two new findings registered rather than absorbed:** `B-144` (§24.1.B's count column was never re-tabled after §3.5's two amendments, and the code manifest resolves the divergence *inconsistently per row*) and `B-145` (`retry.backoff_ms` + `retry.cause_class` have zero producers; the mandated `retry.terminal = "escalate"` value is never emitted, collapsed into `max-attempts`). A **third** stale figure fixed in place: the manifest claimed a 63-attribute sum while the derived constant is **65**, misattributed to the wrong sub-table. **The close-out steps below are PRESERVED AS HISTORY and are `[SUPERSEDED 2026-08-09]` — do not action them.**

- **Re-grounded 2026-08-09 (#1285, batch pass) — SHARPENED, and this row's own count is wrong.** The three-cardinalities structure holds, but the wire figure UNDERCOUNTS: an exhaustive literal sweep of `harness-*/src` returns **five** undeclared keys, not four — the missed one is `retry.terminal` (`retry_breaker_fallback.py:1253`), which `git log -S` shows landed at **#1065**, *before* the council that filed this row. Corrected total: **ten** distinct wire keys, not eight. Step (2) is answered by the spec rather than open — Runtime §14.6's discretion bullet (`Spec_Harness_Runtime_v1.md:4279`) states no cap, so the lane is intentionally unbounded and this is manifest-conformance, not a fork. **Shape: BUILD-NOW.**

- **What it is.** CP §3.5 declares the `retry.*` namespace, and the CP export manifest pins it at `attribute_count=4` (`harness-cp/src/harness_cp/cp_namespace_export_manifest.py:131-138`), feeding the `CP_EXPORTED_ATTRIBUTE_COUNT` 63-sum acceptance (`:165-170`). The **code-side** `RETRY_NAMESPACE_SCHEMA` already carries **six** (`harness-cp/src/harness_cp/retry_fallback_namespace.py:168-207`, whose own docstring records *"v2.3 amendment from 4"*; the contract re-table is `design-substrate/Spec_Control_Plane_v1_3.md:76`, against the four-attribute v1.2 row at `Spec_Control_Plane_v1_2.md:403`). On top of that, the shipped `retry.skipped.reason` / `retry.skipped.candidate` pair (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:678-679`) rides the `retry.*` namespace **outside any §3.5 declaration**.
- **Why that is legal today, and why C7 still filed it [HIGH].** The pair is authorized by the Runtime §14.6 discretion bullet — *"Whether the wrapper emits a separate `retry.skipped` span event on breaker-open candidate skip, or just an outer-span attribute — defer to OTel telemetry-volume discretion at implementation; spec-MUST is just that the skip is observable"* (`design-substrate/Spec_Harness_Runtime_v1.md:4213`; **re-grounded at HEAD** — C7 cited `:4195`, which at HEAD is blank). C7's point is that this is **precedent, and precedent accumulates**: `B-116`'s ratified terms **t1+t2** add two more undeclared `retry.*` attributes (`retry.breaker_waived.reason` / `.candidate`), which he required be *"filed alongside"* the existing pair rather than silently added. The manifest's 4, the code's 6, and the wire's 8-after-`B-116` are three numbers for one namespace, and only the first is cardinality-verified.
- **Close-out steps.** **(1) Recount all three at the then-current HEAD** — these drift every arc, so a stale count is worse than none. **(2) Decide whether the discretion lane has a boundary.** If it does not, say so in the contract: an unbounded lane means the pinned `attribute_count=4` is not a cardinality guarantee about the *namespace*, only about the *declared subset*, and the 63-sum acceptance means less than it reads. That is the finding either way. **(3) If bounded,** the cheapest shape is a declared discretion-lane list the manifest acknowledges without ingesting — no §3.5 amendment, no cardinality bump. **(4) Do NOT use this row to block `B-116`'s t1/t2:** those were ratified in venue under exactly this discretion, and C7's requirement was *filing*, not refusal. Reading this row as a veto would invert its author's own position.
- **Council:** **No at registration** — nothing is decided until the recount. **Conditionally yes at step (2)** if it leans toward closing the lane, which trades emission-site observability freedom (C7) against the cardinality and cost guarantees the pinned manifest exists to give (C2/C6). Do not convene before the recount, per the workspace probe-first discipline.

### B-127 · The breaker-open advance passes no `exhaustion_cause`, so an all-breakers-open chain reports **retry** exhaustion when no attempt ran *(surfaced at the B-116 council deliberation (C1 A2 consultant, standing observability pre-check), 2026-08-07)*

- **CLOSED 2026-08-09 (#1287), paired with `B-131`.** The skip branch now passes `exhaustion_cause=skip_reason`, so an all-breakers-open chain reports the actual terminal cause. **Passed through, not hard-coded, and that is load-bearing:** `skip_reason` takes TWO values here — `breaker-open` and `half-open-trial-in-flight` (the second added by `B-118`) — so a hard-coded literal would mislabel every half-open skip. A dedicated witness exists for that exact mutation and probe **P2** turns it RED. Three witnesses (the all-open chain with **zero** inner dispatches; a discriminator keeping ordinary retry exhaustion at its default; and the half-open variant built on a **real** event-gated concurrent trial). `_advance_or_exhaust`'s signature and default are unchanged.

- **Re-grounded 2026-08-09 (#1285) — INTACT, confirmed BY EXECUTION.** A probe tripped both candidates' breakers OPEN with cooldown unexpired: **zero** inner provider calls, skip reasons `[breaker-open, breaker-open]`, and `fallback.exhaustion_cause = per-candidate-retry-exhaustion`. A chain where no attempt ever ran reports retry exhaustion. Step (2) settled empirically: `fallback.exhaustion_cause` is declared in **no** spec (zero hits) — a free `str` with its own hyphenated vocabulary, so the fix is a matching literal, NOT reuse of the underscored CP enum. **Shape: BUILD-NOW** — one keyword argument at `retry_breaker_fallback.py:888-895`. **Land together with `B-131`** (same call site, different attributes; neither fix resolves the other).

- **What it is.** `_advance_or_exhaust` takes `exhaustion_cause: str = "per-candidate-retry-exhaustion"` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:866`) and writes it onto the terminal `fallback.exhausted` event as `fallback.exhaustion_cause` (`:891`, in the attribute dict at `:888-897`). The capability-shortfall pre-check passes `exhaustion_cause="capability-shortfall"` **explicitly** (`:656`), and the method's own docstring states why: *"so an all-candidates-incapable chain is not mis-classified as retry exhaustion (no provider attempt ran in that case)"* (`:874-876`). The breaker-open pre-check, twenty-eight lines below the shortfall one, passes **no** `exhaustion_cause` (`:684-691`) and therefore takes the default. So an all-breakers-open chain exhaustion is reported as **retry** exhaustion — the identical mis-classification the sibling path was written to prevent, on the identical premise (no attempt ran).
- **Scope and ownership as the voice left it.** C1 surfaced this as an adjacency he *"did not see filed"*, one line away from C11's `:683` row (`B-131`), and routed the **value** to C7/OD while keeping only the naming: *"I own naming that the topology emits the wrong-shaped terminal event."* He explicitly registered it rather than folding it into `B-116`'s diff. It fires under **either** `B-116` reading, and after ratification it still fires for the members that keep counting.
- **Close-out steps.** **(1) Re-verify** the default, both call sites, and that `fallback.exhaustion_cause` is written **unconditionally** — unlike its sibling `fallback.last_failure_detail`, which is written only `if last_failure_detail is not None` (`:893-894`; that asymmetry is `B-131`). **(2) Select rather than mint, if possible:** `FallThroughCause.BREAKER_OPEN` already exists byte-exact in the closed CP §3.5 `fallback.cause` domain (`harness-cp/src/harness_cp/fall_through_procedure.py:67-68`; `design-substrate/Spec_Control_Plane_v1_2.md:401`). But `fallback.cause` and `fallback.exhaustion_cause` are **different attributes**, and the value domains must be checked before reuse — the same homonym discipline C7 applied when refusing `capability_shortfall`. **(3) Pass it** at `:684-691`. **(4) Witness both** — a breaker-open exhaustion carrying the new value, and a per-candidate retry exhaustion still carrying the default, so the fix is discriminating rather than blanket.
- **Price, stated because it is itself the argument.** One keyword argument at one call site, plus a value decision. The sibling path is the shipped precedent for both the fix and its rationale. A defect this cheap to close is one to close rather than carry.
- **Council:** **No.** A one-value fix inside a closed, declared domain with a shipped sibling precedent and a named value owner. Route to C7 plus `advisor()` if step (2)'s reuse question is contested.

### B-128 · The dispatcher borrows the validator-side `PERMANENT_FAIL_EXIT` label while performing none of the escalation that label routes to *(surfaced at the B-116 council deliberation (C1 A2 consultant, HITL pre-check), 2026-08-07)*

- **CLOSED 2026-08-09 (#1285) as a forward-only record — read this first.** The premise is intact, but this row's **own falsifier was run and does not fire**: close-out step (1) demanded an enumeration of `retry.fail_class` readers, and it returns **six** hits — five producers, and one consumer-shaped reference (`per_class_attribute_composition.py:209`) that is a **presence-only** tail-sampling trigger. Nothing branches control flow on the value. The row states its own disposition for exactly this result (*"the row is documentation-shaped, and that is the honest outcome to record rather than a reason to build"*), and is closed on that instruction. **Demand trigger:** any future reader that branches on the VALUE.

- **What it is.** The fail-fast branch tags the inner attempt span `retry.fail_class = ValidatorRetryExitClass.PERMANENT_FAIL_EXIT.value` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:1024-1027`). C-CP-21 §21.1's five-class fail taxonomy routes `permanent-fail-exit` as *"SKIP STAIRCASE; route directly to C11 HITL (validator-escalation placement per §17.1)"* (`design-substrate/Spec_Control_Plane_v1_2.md:1852`, in the §21.1 table at `:1843-1853`). But that taxonomy is **validator**-side: the dispatcher borrows the label for a span attribute while the exception propagates to a plain step failure — `workflow_driver.py:5564-5572` returns `RunStatus.FAILED` with a `terminal_step_index`, and no HITL invocation occurs. C1's statement: **the escalation the attribute names is not the escalation the topology performs.**
- **The operative warning, which is why it is a row and not a footnote.** C1 flagged it [MODERATE], declared it *"not this fork's business"*, and attached the reason it must still be recorded: the label *"must not be quietly used as evidence that an escalation path exists."* That is a stale-carry hazard aimed at future readers of the span attribute, not at `B-116`.
- **Close-out steps.** **(1) Enumerate producers and consumers first.** Every producer of `retry.fail_class = permanent-fail-exit` and — the load-bearing half — every consumer that reads the class as a **routing** signal rather than as telemetry. The divergence only bites where such a consumer exists. **If there is none, the row is documentation-shaped, and that is the honest outcome to record rather than a reason to build.** **(2) Then decide:** either the dispatcher stops borrowing the validator label (a new dispatch-side value in the `retry.fail_class` enum — a closed-domain extension against a C5/C7-owned vocabulary, expensive), or the contract records that `retry.fail_class` and `validator.fail.class` share a vocabulary but not a routing (cheap, and matches what the code does today). **(3) Do NOT resolve it by wiring the HITL escalation.** No voice proposed that, C1 and C11 concurred independently that no gate belongs on this path, and `B-116` ratified term **t5** (no HITL gate here). A fix that adds one would contradict a ratified term.
- **What would falsify it.** A consumer that **routes** on the dispatcher-set value would upgrade this from a labelling note to a live defect — that is the single check step (1) exists to run.
- **Council:** **No at registration.** **Conditionally yes at step (2)** only if the enum-extension branch is chosen, which touches a closed C5/C7-owned domain and trades vocabulary precision against domain closure — and only after step (1) shows a routing consumer exists.

### B-129 · The memory-tool argument-shape degradation signal has no vocabulary home after `B-116` demoted it off the breaker *(surfaced at the B-116 council deliberation (C1 A2 consultant; conceded by C9, conditioned by C11), 2026-08-07)*

- **Re-grounded 2026-08-09 (#1285) — INTACT, and one of its two stated obstacles is already gone.** The row worries (step 5) that its own preferred placement lands on a base-rate-only surface *"dropped nine times in ten"*, leaving C11's condition unmet. **That worry is stale:** the OD-side ingestion gap was closed by the landed `B-116-t3` leg, and `fallback.exhausted` is now row 19 of the §9.2 always-sampled roster (`Spec_Operational_Discipline_v1_38.md:78`) — so C11's condition is already satisfied and the placement is cheaper than the row assumes. **Shape: DEFER-WITH-TRIGGER** — step (2) remains a measurement question a read-only pass cannot answer.

- **What it is.** `B-116` ratified member #5 (`MemoryToolExecutionInputError`) as **don't-count** — but C9's substantive point survived his own concession: a model that repeatedly cannot emit the declared memory-tool argument shape **is information**, and reading (II) orphans it. The signal is real at the raise site: `llm_dispatch.py:4376-4383` validates the **model-supplied** `scope_ref`, and the in-file comment at `:4362-4367` explicitly distinguishes it from the two harness-internal sibling checks immediately above (`:4368-4375`), which the B-88 A-ii re-typing moved to `MemoryToolExecutionInternalError`. What is missing is a **home** for the signal.
- **Where the type system points, per C1.** The only `BreakerCause` that would fit is `capability_shortfall`, whose in-code scope **explicitly disclaims** exactly this use — *"names a separate, out-of-scope cross-family fallback-exhaustion path, not a per-step dispatch failure"* (`harness-od/src/harness_od/harness_breaker_schema.py:141-143`, mirroring OD §7.1's forward-slot note at `design-substrate/Spec_Operational_Discipline_v1_32.md:50`). Meanwhile `FallThroughCause.CAPABILITY_SHORTFALL` (`harness-cp/src/harness_cp/fall_through_procedure.py:64-65`) **is** the per-dispatch chain-advance vocabulary. C1: *"the type system is pointing at the fall-through path, not the counter"* — and he declined to author a value, because `FallThroughCause` is **closed at 4** and its own docstring names extension *"a Workflow §4.1.2 Class-2 D-revision"* (`:52-58`), with the domain CP-declared at `design-substrate/Spec_Control_Plane_v1_2.md:401`.
- **Stated against interest.** C9 accepted the placement while naming exactly what it costs him: *"be honest about what it gives my domain: a **registered home, not a live signal**. I accept that trade only because the alternative live signal is a corrupted counter."* C11 accepted it **conditional** on the `B-116` t3 sampling term, since a signal relocated from an inviolable §9.2 floor to a base-rate surface is dropped nine times in ten at `team-binding × self-hosted`.
- **Close-out steps.** **(1) Re-verify** both enums' scoping text and the closed-at-4 cardinality. **(2) Decide whether a fall-through value is warranted at all — and note this requires a POPULATION question, not a code read.** Does the shape actually recur in practice? That cannot be answered from the source, so the honest first step is **measurement**, not a mint. **(3) If warranted, route the extension as the Class-2 D-revision the enum's own docstring names** (C5/C7/OD own the value) — a spec leg, not an impl leg. **(4) Choose the producer site deliberately:** `B-116`'s guard means #5 no longer reaches `record_failure` at all, so the emission must be placed on the fall-through path itself, not inherited from the old one. **(5) Note the `B-116-t3` coupling** — C11's acceptance is conditioned on the sampling guarantee, so landing a value onto a base-rate-only surface satisfies C1's placement while failing C11's condition.
- **Council:** **No at registration.** **Conditionally yes at step (3):** extending a closed, CP-declared domain to carry a reliability signal is the C9 ⊥ C7 tension (signal fidelity versus vocabulary closure) the deliberation already surfaced once and resolved by refusal. Convene only **after** step (2) produces a measured population — convening on an unmeasured one would repeat the mistake the probe-first discipline exists to prevent.

### B-130 · All-candidates-permanently-ineligible is a reachable terminal state no canonical artifact declares *(surfaced at the B-116 council deliberation (C1 A2 consultant), 2026-08-07)*

- **CLOSED 2026-08-09 (#1285) as a forward-only record — read this first. The row is PARTIALLY FALSIFIED, and its own step (4) anticipated it.** Claim A (a zero-attempt exhaustion is reachable) is **still true** and was reproduced by execution — `inner.calls == 0`, three `retry.skipped`, `chain_length == 3`. Claim B (that this is **permanent/irreversible**) is **FALSE at HEAD**: `B-118` landed, and the same repro extended past the cooldown window **recovered** — `OPEN → half_open → closed` on a successful trial, dispatch returning normally. The state is **bounded and self-healing**, not an irreversible candidate-set deletion — a circuit breaker working as designed for a genuinely-down provider set. The negative claim was still enumerated and holds, but no new terminal-state enum is warranted; the one useful residual (making zero-attempt exhaustion distinguishable in telemetry) is already `B-127`'s. **Demand trigger:** any arc weakening cooldown-based half-open recovery.

- **What it is.** The breaker pre-check at `harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:674-683` is not telemetry — it is a **topology gate** that deletes a candidate slot from the chain. Combined with `B-118` (`OPEN` is absorbing for the process lifetime), a trip is an **irreversible candidate-set deletion**. C1's end state: a declared N-candidate fallback chain that has silently become a **null topology** — every subsequent dispatch walks N slots, emits N `retry.skipped` events (`:675-681`), does zero work, and exhausts. What the design **does** declare, both verified at HEAD: candidate exhaustion *within* a dispatch (`RetryBreakerFallbackExhaustedError`, `:899`) and first terminal step failure *within* a run (`harness-cp/src/harness_cp/workflow_driver.py:5564-5572`, `RunStatus.FAILED` plus `terminal_step_index`). *"All candidates permanently ineligible"* is neither.
- **The amplifier neither primary named.** `_advance_or_exhaust` is a **multiplier**: one candidate-independent fault is charged N times per dispatch, N = chain length (`B-116`'s executed Probe B: three breakers, one dispatch). So **lengthening the fallback chain to improve resilience makes the harm strictly worse and trips the whole chain sooner** — a topology decision inverting into a reliability regression. Scope after ratification, stated so the row is not read as larger than it is: `B-116`'s guard removes the four harness-internal members from the multiplier, so the shape narrows to the members that still count. It does not disappear.
- **Close-out steps.** **(1) Re-verify** the two declared terminations and that no third exists. **(2) Decide whether the state needs naming at all — and weigh C1's own three-level bound, which argues it may not:** `max_attempts=3` within a candidate (`retry_breaker_fallback.py:130-134`), chain length then `:899` within a dispatch, and first-failed-step termination within a run (`workflow_driver.py:5564-5572`), with the other topologies bounded too (evaluator-optimizer caps at `_DEFAULT_EVALUATOR_OPTIMIZER_MAX_ITERATIONS = 3`, `workflow_driver.py:10555`). For a candidate-independent fault the bound is **one step per run**, which is the honest argument for leaving the state undeclared. **(3) If it is declared, do not mint twice** — the natural carrier is the `fallback.exhaustion_cause` value `B-127` is already about. **(4) SEQUENCE `B-118` FIRST:** wiring the half-open latch stops `OPEN` being absorbing, which weakens this row's premise substantially and may retire it.
- **Council:** **No at registration.** A C1-domain termination-criterion question with C9's mechanism underneath (T-perm-3, C1 ⊥ C9, fires literally here — the breaker is a reliability primitive performing an irreversible topology edit, and C1 asked that it be recorded as such on the row). But C1 argued both halves in venue and the probe-first answer — the driver's own terminality — is grounded at HEAD. Re-convene only if step (2) leans toward declaring a new terminal state.

### B-131 · The breaker-open skip **deletes** the prior candidate's failure detail instead of demoting it, so a chain exhaustion reports no cause at all *(surfaced at the B-116 council deliberation (C11 A1 primary (c), reframed by C7 A2, re-grounded by C11 at cross-read), 2026-08-07)*

- **CLOSED 2026-08-09 (#1287), paired with `B-127`** — same call site, different attributes of the same event, neither fix resolving the other. A skip no longer clobbers a real candidate diagnostic: the unconditional overwrite is gated on a new `last_failure_is_real_attempt` flag. **The flag shape was chosen over a `None`-check deliberately:** a chain can take consecutive skips with *different* reasons, which must keep overwriting each other — a `None`-check would have frozen the FIRST skip reason instead of the last, silently breaking pre-existing behaviour. The row's own sub-claim was corrected at grounding: the class never fell through to `"unknown"`; it carried a **real-but-wrong** value, worse because it looks legitimate. Probes **P3** and **P4** turn the two witnesses RED respectively.

- **Re-grounded 2026-08-09 (#1285) — INTACT in mechanism, SHARPENED in one sub-claim, reproduced by execution.** The row's own step-(4) scenario was run: candidate 0 fails with a real diagnostic, candidate 1 is breaker-open-skipped, and `fallback.exhausted` carries `last_failure_class: breaker-open` with `fallback.last_failure_detail` **absent entirely**. **The sharpening:** the row says `last_failure_class` falls through to the literal `"unknown"`. It does not — it is SET to the skip reason, which is truthy, so the `or "unknown"` fallback never fires. The attribute is overwritten with a **real-but-wrong** value that masks the actual failure class, which is worse than `"unknown"` because it looks legitimate. Witness gap confirmed: no existing test exercises the multi-candidate erasure. **Shape: BUILD-NOW** — first-write-wins retention; **land with `B-127`.**

- **What it is.** **Before** the breakers open, the operator's terminal is correct and loud: `_failure_detail` composes `"<Type>: <message>"` bounded (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:178-187`), `RetryBreakerFallbackExhaustedError.__init__` appends it as *"; last failure: ..."* (`:169-175`), `_step_fail_class` interpolates `str(exc)` (`harness-cp/src/harness_cp/workflow_driver.py:1686-1687`), and the CLI echoes it to stderr (`harness-runtime/src/harness_runtime/cli/app.py:141-143` and `:243-244`). **After** a breaker opens, the pre-check sets `last_failure_class = "breaker-open"` and `last_failure_detail = None` (`:682-683`) — and the exhausted event writes the detail attribute **only** `if last_failure_detail is not None` (`:893-894`). So the overwrite does not set the field to `None`: it makes `fallback.last_failure_detail` **vanish from the event entirely**, while `fallback.last_failure_class` falls through to the literal `"unknown"` (`:890`). Within **one** dispatch, candidate 1's real fail-fast detail (set at `:998`) is destroyed by candidate 2's **skip**.
- **The mechanism, in C7's framing which C11 adopted verbatim.** *"A strictly-information-destroying write by a path that learned nothing"* — a monotonicity violation on a last-write-wins diagnostic slot. **The skip must demote the detail, never delete it.** C11 adopted that as the row's title at cross-read. It fires under **either** `B-116` reading, for the members that still count — i.e. genuine provider outages arrive at the operator as *"chain exhausted, no reason given"*. There is no compensating surface: **zero** `breaker` hits across `harness-runtime/src/harness_runtime/cli/` (three files, re-verified at HEAD), so no operator-facing breaker readout exists to cite in either direction.
- **The correction carried, not laundered.** C11's A1 additionally called `retry.skipped.reason = "breaker-open"` **false**. C7 refused that half — *"it is a proximate-mechanism statement about why THIS candidate was skipped, and the breaker WAS open; calling a true attribute false weakens the row"* — and C11 **conceded and withdrew the sentence** at cross-read. **This row therefore does not rest on any truth-value claim about `retry.skipped.reason`.** It rests on the erasure mechanism, which C11 then grounded more sharply than either voice had written it.
- **Close-out steps.** **(1) Re-verify the erasure chain** — `:682-683`, `:890`, `:893-894`, `:998`. **(2) Implement demotion rather than deletion:** preserve the prior candidate's detail (and class) across a skip instead of overwriting, or carry the skip as an **additional** fact alongside the retained detail. The open choice is the retention shape — first-write-wins, a list, or a two-field split — not whether to retain. **(3) Respect the `B-116` adjacency:** `B-116`'s binding term (C11's (a)) already requires the **non-counted** fault to keep carrying `type(exc).__name__` and the bounded message on `last_failure_detail` at `:1031-1035`. This row and that guard touch adjacent lines and must not fight; land them in a known order. **(4) Witness the multi-candidate case specifically** — candidate 1 fails with a real detail, candidate 2 is skipped breaker-open, and the `fallback.exhausted` event must still carry candidate 1's detail. A single-candidate test cannot see this defect. **(5) Note `B-127`** — the same terminal event's `exhaustion_cause` is mislabelled on the same path; the two are one event apart and should be priced together.
- **What would falsify it.** Nothing found. Two voices grounded the mechanism independently at HEAD, and the one contested claim was withdrawn by its author rather than carried.
- **Council:** **No.** C7 (attribute and schema owner) and C11 (operator surface) converged in venue on both the mechanism and the framing, including one concession in each direction. What remains is choosing the retention shape — a single-domain implementation judgement.

### B-132 · The classifier catch-all routes harness-side memory subtypes to the staircase, where they charge the breaker at escalation/exhaustion *(surfaced at the B-116 spec leg, fresh-context review round, 2026-08-08; **NARROWED — not closed — at the `B-115` (b′) leg, which removed the deterministic ledger-refusal population**)*

- **Re-grounded 2026-08-09 (#1285) — INTACT, corroborated by the shipped code's own comments.** Verified by direct classifier calls, not grep: `MemoryToolExecutionDeniedError` and `MemoryToolExecutionStoreError` both → `transient-retry`, **not waived**; `MemoryToolExecutionLedgerConflictError` → waived (the `B-115` split, confirmed live). **What precisely remains after that narrowing: exactly TWO members** — the denied subtype, and the store subtype minus the ledger-conflict slice (now exclusively disk-full / permission / other genuinely-transient I/O). Both still charge the **provider-model** breaker for a candidate-independent harness-local fault. **Shape: FORK-SHAPED** — either a finer split on the wrapped exception's own type (the `B-115` precedent), or a staircase-side waiver that **reopens the `B-116` C1/C9 ⊥ C11 dyad**. The hard part is untouched: discriminating transient store I/O from a persistent local fault without message-sniffing.

- **What it is.** `_classify_provider_exception`'s terminal arm (`retry_breaker_fallback.py:371`) maps EVERY unlisted `Exception` subclass to `TRANSIENT_RETRY` — deliberately, per its own docstring, treating unknown faults as transient until proven otherwise. `MemoryToolExecutionDeniedError` and `MemoryToolExecutionStoreError` — siblings of the two WAIVED memory types under the same family base — fall to that catch-all, travel the retry staircase, and charge the provider-model breaker at both staircase `record_failure` charge sites (`:1072`, `:1091` at HEAD). Under the `B-116` ratified normative test, a DETERMINISTIC policy denial or a local durable-store write failure charging the breaker is a residual — the same defect shape `B-116` closed at the fail-fast site, alive on the staircase path.
- **Why a residual, not a miss.** The council's ratified scope binds the fail-fast site only, and the classifier's exclusion of the denied class from fail-fast is itself deliberate and documented (its raise sites read mutable index/store state and it WRAPS genuinely transient store I/O — the docstring's own grounds at `:343-349`; wholesale fail-fasting it would kill retries for real flakes). The residual was stated as a FALSE universal (*"every other path passes the test by construction"*) in the first drafting of spec §14.6.3; the fresh-context review round falsified it by direct read, and the spec now declares the residual and cites this row.
- **Close-out steps.** **(1)** Re-verify the catch-all membership and the two staircase charge sites at the then-current HEAD. **(2)** Decide PER-SUBTYPE whether a finer classification is owed: a class that wraps genuinely transient I/O cannot be wholesale fail-fasted, so any fix is a finer split — discriminating persistent policy denial from wrapped transient I/O (e.g. on the wrapped exception's own type), or a staircase-side waiver at the exhaustion charge for instances that fail the recovery-model test. **(3)** Any change routes through the `B-116` normative test PER-MEMBER, per the §14.6.3 discipline. **What would falsify it:** the catch-all arm gaining member-specific handling for the denied/store subtypes, or an operator decision recording the staircase charge as accepted behaviour.
- **NARROWED at the `B-115` (b′) leg, 2026-08-08 — not closed, and the distinction is load-bearing.** That leg SPLIT the deterministic ledger divergent-replay refusal out of `MemoryToolExecutionStoreError` into a new sibling `MemoryToolExecutionLedgerConflictError`, which is now admitted to fail-fast BY NAME and waived as the **fifth** member of the §14.6.3 tuple. The population still riding the catch-all is therefore strictly smaller: the genuinely transient store I/O the store subtype thereafter exclusively carries, plus `MemoryToolExecutionDeniedError`. The store subtype's disposition is otherwise **UNCHANGED** — it keeps `io_failure`, keeps classifying `TRANSIENT_RETRY`, and still charges at both staircase sites. **What this means for the forward work:** the easiest candidate for a finer split has been taken, so what remains is the harder question the row was really about — whether a class that wraps transient I/O can be discriminated at all without message guessing. The (b′) leg is also the **worked precedent** for step (2)'s finer-split option: it discriminated on the wrapped exception's own TYPE by making that type survive the capture boundary, rather than by reading `failure_reason` wording.
- **Council: no at registration; conditionally yes at step (2)** if a staircase-side waiver is proposed — that re-opens the `B-116` C1/C9 ⊥ C11 dyad on a NEW path with its own retry-visibility tradeoffs.
- **Cross-ref.** `B-116` (the ratified fail-fast-site waiver whose scope boundary this row marks); the §14.6.3 declared-residual paragraph cites this row by id and records the narrowing; `B-115` (the (b′) leg that narrowed it).

### B-133 · The §9.2 always-sampled floor classifies **span names** at both consumers while three of its members are span **events**, so their head=1.0 floor is unrealized on the dispatch path *(surfaced by out-of-family Codex round 1 at the `B-116-t3` leg, 2026-08-08; same family as B-123 / B-124)*

- **CLOSED — 2026-08-08. Steps (1) and (2) EXECUTED; step (3) STILL OPEN**, carried as a named residual on this closed row (the `B-104` precedent) and **MERGED with `B-137`**. *This bullet states the CURRENT result; the bullets above it are the row's registration text and are superseded where they conflict.* **(1) The positive control was run FIRST, end-to-end, and before any repair was written** — a REAL `RetryBreakerFallbackDispatcher.dispatch` driven to fallback-chain exhaustion through a `TracerProvider` wired with the REAL `HarnessCompositeSampler` **and** the REAL `TailKeepSpanProcessor`, across **seven** configurations covering **all three** event-shaped members by their own real dispatch shapes (chain exhaustion → `fallback.exhausted`; capability-shortfall exhaustion → `fallback.triggered`; charging-fault exhaustion at `fail_threshold=1` → `breaker.tripped`, chosen deliberately because `LLMDispatchProviderUnreachableError` is a §14.6.3 **waived** type and does not trip). **Result — ZERO spans exported at BOTH consumers for ALL THREE members.** The `base_rate=1.0` control exported 3, proving carrier and event exist. **The decisive discriminator:** at `base_rate=0.0` a span **NAMED** `fallback.exhausted` survives while a span **carrying it as an event** is dropped — *event-shaped, not name-shaped* is the **measured** cause. **(2) Repair venue (a) BUILT**, as this row ranked it; venue **(b) REFUSED** as the X-AL-3 extension this row already classified it as. Contract at OD spec **v1.38** NEW **§C-OD-09 §9.2.1** (five terms); unit at OD plan **v2.33 U-OD-59**; **sixteen witnesses W1–W16** (40 cases, homed across the axis boundary — the five real-dispatch witnesses at `harness-runtime/tests/test_b133_event_aware_tail_floor_real_dispatch.py`, the nine processor-contract witnesses at `harness-od/tests/…`, which stays `harness_runtime`-free because the axis-isolation CI leg prunes every sibling of `harness-od`) plus **seven PD-8 probes**, every figure re-measured against the final roster after the decorrelated 3-lens merge gate on PR #1276 returned a **real BLOCK** — a first-event-only scan passed all 65 cases then in the suite while reinstating `B-133` on the production-reachable `retry.skipped`-first ordering, closed by **W15/W16** and pinned by probe (vii) — two changed the deliverable (probe (ii) caught the trigger-flag mirror witness **passing under the mutation it existed to catch**, sharpened membership→export ORDER, re-run 27 failed / 5 passed; probe (iii) returned GREEN as the **correct** result, ordering being a cost property), and probe (vi) **validates** rather than changes, inverting exactly the bound witnesses. **What the arm delivers, as measured scope rather than coverage:** it realizes the floor for carriers the head **ADMITTED**, and for those delivers **100%** (420/420, 837/837) — what bounds it is **admission, not classification**. `tracer_provider.py` binds the §10.3 ratio at the head **unconditionally in both §9.1 modes**, so admission measured **10.4%** (`team-binding × self-hosted-server`), **20.9%** (`multi-tenant-compliance × managed-cloud`), **100%** at the base-rate-1.0 cells. **The head bound therefore reaches FIVE of the EIGHT active cells** — every cell whose head base-rate is below 1.0 — **and it is SHAPE-specific**: at the same cell and rate a §9.2 member realized as a **root span name** is admitted at **100%** (4,000/4,000 for `sandbox.violation`), so only **event-carried** members and **non-root** spans are starved, the latter including non-root §10.2 triggers at ~9% preservation. Pinned by **W5 + W13 + W14**, named as a bound. **History, compressed rather than carried twice:** two successive framings of this bound were wrong and were corrected by **measurement, not argument** — first this row's own step-(2) hypothesis that head-based-dev cells *"sample at 1.0 anyway"*, then the leg's first drafting, which over-corrected to *"exactly one cell"* and claimed every `TAIL_BASED_PROD` cell was covered by terms 1–3. Both are **superseded** by the statement above; the full narrative is at OD spec v1.38 §0.2 and plan v2.33 §0.9. **Two closing routes remain, both outside this row:** the **shape** half is venue (b) (a new emission primitive, back-flow per X-AL-3); the **admission** half is **`B-137`**. **(3) F-08 per-tenant keep-volume — STILL OPEN**, and now **merged with `B-137`**: measure at a `TAIL_BASED_PROD` **multi-tenant** cell (head-based cells engage no tail consumer) and measure it **jointly**, because today's ~20% admission is the **ceiling** on this arm's added keep-volume and `B-137`'s candidate A removes it. **`B-123` NOT closed** — its step-(1) probe is answered as a side effect (an event-carried `breaker.tripped` sets no keep flag); term 3 declines to widen the predicate and **W7** pins the boundary. **`B-136` newly registered** (the pre-existing name-arm root-close buffer leak; this arm must not extend it — term 5 + AC #6c + PD-8 probe (iv)). **`B-137` newly registered** after out-of-family Codex round 1 against commit `ff63c725`, adjudicated empirically at the real production wiring and CONFIRMED: the arm is **real but partial**, and what was defective was the contract **TEXT**, not the arm.
- **What it is.** Both C-OD-09 §9.2 consumers resolve the always-sampled floor against a **span name** and never against an **event name**. **Head:** `HarnessCompositeSampler.should_sample` (`harness-od/src/harness_od/composite_sampler.py:100`) branches on `is_always_sampled(name, attributes)` at `:110`, where `name` is the span name supplied at span creation. **Tail:** `TailKeepSpanProcessor.on_end` (`harness-od/src/harness_od/tail_keep_span_processor.py:207`) branches on `is_always_sampled(span.name, span.attributes)` at `:259` and **never inspects `span.events`** — grounded at HEAD by direct read: the module contains **zero** occurrences of `.events`.
- **Three §9.2 members are span EVENTS on a wrapper span, not spans of their own.** `fallback.triggered` (`outer_span.add_event` at `retry_breaker_fallback.py:688-689`); `fallback.exhausted` (`outer_span.add_event` at `:946-947` — the row the `B-116-t3` leg added); `breaker.tripped` (`parent_span_ref.add_event(name="breaker.tripped", ...)` at `harness-od/src/harness_od/harness_breaker_schema.py:282`, the same emission shape `B-123` grounds). For all three the §9.2 head=1.0 guarantee is carried **only insofar as the WRAPPER span is kept** — at a base-rate-sampled cell the wrapper can be dropped and the always-sampled event goes with it.
- **PRE-EXISTING, not new exposure from the `B-116-t3` leg — and this is the load-bearing scope fact.** `fallback.triggered` and `breaker.tripped` have carried the identical shape since the **original OD ingestion** of §9.2, so the characteristic is a property of the **floor mechanism**, not of the row this leg added.
- **What is NOT in question.** The §9.2 **membership contract** is correct regardless, and so is the **ingestion-chain direction** that placed the row in OD rather than demoting CP — neither turns on how the floor is realized at the SDK boundary. **`B-116` CLOSED at its ratified gate**, whose stated content was the roster widening plus its sampling fixtures per the council record `C-c7-reconcile.md`'s own pricing; **this row carries the realization debt that pricing never covered.** *Registration is the non-silent disposition* — the finding is neither absorbed into a closed row nor repaired by redesign at a leg with no authority for it.
- **Why it is not fixed here.** The reviewer's two proposed remedies are both **REJECTED at the t3 leg**. Emitting a dedicated span mints a **new emission primitive no cleared spec authorizes** — an X-AL-3 design extension routing through back-flow, not an impl fix. Making the wrapper span always-sampled would always-sample **every dispatch wrapper**, over-sampling far beyond the three rows at issue and silently rewriting the §10.3 per-cell envelope.
- **Close-out steps.** **(1) Run the positive control first, end-to-end rather than at the predicate** — drive a real **exhausted** dispatch through the composite sampler **and** the tail-keep processor at a cell whose `base_rate` < 1 and observe whether the wrapper span carrying `fallback.exhausted` is dropped; cover the two siblings in the same run, since a repair scoped to one row leaves the family half-fixed. **(2) If dropped (expected), decide the repair venue — the two candidates differ in KIND:** **(a)** an event-aware arm at tail-keep `on_end` scanning `span.events` names against the §9.2 roster before the drop decision — single-domain C7, no new primitive, no spec amendment, and it **composes with `B-123`'s trigger arm** since both want the same event-name lookup; **(b)** an event→span projection at emission — a **new primitive routing through back-flow per X-AL-3**, which also changes the span topology every existing witness asserts against. Prefer (a) unless the control shows the tail arm cannot reach the decision; note (a) repairs the **tail** surface only — a head-based-dev cell samples at 1.0 anyway, so the head arm may need nothing (**confirm, do not assume**). **(3) Post-realization, and only then,** bound the multi-tenant keep-volume against the C-OD-11 §11.1 per-tenant **1,000 spans/sec** budget per C7's F-08 — the **population question the `B-116-t3` leg left explicitly OPEN**, that leg having established only the **structural** result (the budget enforces at COLLECTOR_BOUNDARY / BACKEND_INGESTION independently of any sampling decision, so membership cannot admit throughput past the enforced caps, and the event is once-per-exhausted-dispatch so membership adds no multiplicity). Measuring volume before the floor is actually realized would measure the wrong thing.
- **Council: no** — a probe-then-repair row in the `B-123` family; convene nothing until the positive control runs. **Conditionally yes** only if step (2) selects candidate (b): a new emission primitive re-opens the **C7 observability-floor ⊥ C11 telemetry-volume** tension on a surface wider than these three rows. Candidate (a) is single-domain and owes no convening.
- **Cross-ref.** `B-123` (the §10.2 tail-keep trigger arm of the *same* span-name-vs-span-event mismatch) and `B-124` (the inert attribute-qualified §9.2 row) — **the three are one family and should be dispositioned together**. `B-116` / the `B-116-t3` leg (`Spec_Operational_Discipline_v1_37.md`, whose change-note carries the matching honest-scope paragraph).
- **Out-of-family Codex round 1 against this leg's commit `ff63c725` — ONE P1, adjudicated EMPIRICALLY at the real production wiring and CONFIRMED, with the leg's own contract TEXT corrected where it overclaimed.** The reviewer held that at `TAIL_BASED_PROD` cells with `base_rate < 1` the head sampler drops the carrier at span **creation**, so no `ReadableSpan` reaches the new tail arm and §9.2.1 term 4's coverage claim is false. **Grounded first, not assumed either way:** `tracer_provider.py` resolves `build_default_sampler(base_rate=PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate)` **unconditionally**, and says in the same function that *"the current default sampler ignores the mode … Future units may wire mode-conditional samplers."* **Then measured** over 4,000 carriers per cell through the real composition: `team-binding × self-hosted-server` (base 0.1) admitted **10.4%**; `multi-tenant-compliance × managed-cloud` (base 0.2) admitted **20.9%**; `solo-developer × self-hosted-server` (base 1.0) admitted **100%**. **The repair this row built is therefore REAL but PARTIAL** — it realizes the floor for head-**admitted** carriers, and of those it delivers **100%** (420/420 and 837/837). **What was defective was the TEXT, not the arm:** term 4's *"every `TAIL_BASED_PROD` cell … is covered by terms 1–3"* is **RETRACTED** and replaced by a per-cell admission table plus the statement that the head bound reaches **five of the eight** ACTIVE cells, not one. **A second framing was ALSO falsified by the same run and is recorded so it is not carried forward:** the starvation is **not uniform**. A §9.2 member realized as a **root span name** is admitted at **100%** even at base-rate 0.1 (measured 4,000/4,000 for `sandbox.violation`), because the head resolves `is_always_sampled` against the span **name**. Only **event-carried** members and **non-root** spans are starved — the latter including non-root §10.2 triggers, measured at **~9%** preservation at base-rate 0.1. That shape-dependence is exactly what this row named. The architecture-level question — an unconditionally admitting head in `TAIL_BASED_PROD` mode with the §10.3 ratio moved into the tail consumer — is **registered as `B-137`**, not absorbed: it spans every class the tail preserves and multiplies admitted volume by `1/base_rate` at every production cell. Witnesses **W13** (parametrized over the two production cells) and **W14** (the root-name-vs-event asymmetry) pin the corrected bound on the **W5 pattern — named as a bound, not hidden** — and **PD-8 probe (vi)** shows they are load-bearing by inverting them under a `B-137` candidate-A sketch. **`B-137` MERGES with this row's F-08 residual** rather than sitting beside it: today's ~20% multi-tenant admission is the **ceiling** on the arm's added keep-volume, and candidate A removes that ceiling, so F-08 must be measured **jointly with `B-137`**.

### B-134 · Should `C-MEM-19` gain a dedicated failure class for deterministic ledger conflicts, rather than routing them to the residual? *(surfaced at the `B-115` (b′) leg, 2026-08-08 — the leg took the residual per the U-MEM-28 precedent and registered the vocabulary question rather than widening a ratified enumeration on its own authority)*

- **What it is, and it is a question the `B-115` leg deliberately did NOT answer on its own authority.** That leg introduced `MemoryToolExecutionLedgerConflictError` for the C-MEM-08 operation ledger's divergent-replay refusal. The type **declares no C-MEM-19 failure class** and inherits the family base's **residual** `provider_adapter_failure`.
- **Three options; none obviously dominant.** **(i) Keep `io_failure`** — REJECTED, the one clearly wrong answer: a ledger refusal is a pure comparison over durable state under a lock, not an I/O fault, so `io_failure` is a positively **wrong** claim. **(ii) Inherit the residual** — TAKEN. Per C-MEM-19 v1.3 a residual report is the **absence** of a claim rather than an assertion of adapter-hood, so this **withdraws a false positive** without inventing a replacement; it follows the **U-MEM-28 precedent**, which moved an emission `input_validation_failure` → residual on exactly these grounds; and it makes the two `classify_memory_failure` consumers **agree**, since `memory_redaction.py` already reports the underlying `MemoryOperationIdempotencyConflictError` as `provider_adapter_failure` under a landed two-row discriminating witness. **(iii) Mint a dedicated `ledger_conflict` class** — NOT taken, and the reason is **jurisdictional rather than technical**: it would widen C-MEM-19's ratified vocabulary, and `Spec_Memory_Substrate_v1.md` v1.3 records at its own change-note (the round-8 finding on the seventh re-typing) that widening a ratified enumeration on a leg's own reading of the rule is the **X-AL-3 shape even when the reading is sound**.
- **The honest cost of (ii), stated so the row is not read as cost-free.** The residual bucket now carries a well-typed, deliberately classified population alongside genuinely untriaged failures — the same triage-blindness C-MEM-19 v1.3's `input_validation_failure` section was written to forbid for a *different* population. The difference in scale is a reason to **defer**, not a reason the concern does not apply.
- **Close-out steps.** **(1) Re-ground** at the then-current HEAD: C-MEM-19's enumeration and invariants; that the residual is still `provider_adapter_failure`; that the conflict type still declares nothing (the `B-115` witness asserts the declaration-**absence** over `vars()`, so a silent later declaration would already have failed there). **(2) Measure the residual bucket rather than arguing about it** — enumerate every type and raise-site population currently reporting `provider_adapter_failure` and state how much is deliberately-classified-but-unnamed versus genuinely untriaged. **If the deliberate half dominates, the residual has stopped meaning "absence of a claim" and the vocabulary owes a member — that is the falsifiable trigger.** **(3) If a class is warranted**, it is a Class 2 fork against C-MEM-19 with a Memory spec leg (v1.3 → v1.4) plus a Memory plan unit, on the `B-88` / U-MEM-28 precedent — a full spec-leg arc, not an impl-discretion flip. The flip site is **one line** by construction (the type declares a `ClassVar` instead of inheriting), because the `B-115` leg wrote the declaration explicitly as an absence. **(4) Consider scope** — the same question applies to `MemoryToolExecutionInternalError`, which inherits the same residual for the same stated reason at U-MEM-28; a vocabulary arc should decide **both together** rather than minting one member and leaving its sibling in the residual. **What would falsify the deferral:** the bucket measured at step (2) turning out majority-deliberate, or an operator decision recording the residual routing as permanently accepted.
- **Council: no at registration** — the row frames a vocabulary question and commits to nothing. **Conditionally yes at step (3)** if a class is proposed: minting a C-MEM-19 member trades telemetry triage precision (**C3**, which owns record and ledger semantics) against vocabulary stability for every downstream consumer that switches on the enum (**C7**). The tension is nameable in advance, which is the convening discriminator. Convene **only** if step (2)'s measurement supports the widening.
- **Cross-ref.** `B-115` (the (b′) leg that surfaced it); `B-88` / U-MEM-28 (the precedent for both the residual routing and the fork shape a widening would need); `Spec_Memory_Substrate_v1.md` v1.3 C-MEM-19.

### B-135 · Stale-era breaker dispatches act under a superseded era on BOTH sides — outcomes reset the `closed`-arm `fail_count` un-epoch-guarded, and attempts continue a multi-attempt staircase across a trip that can overlap the single half-open trial *(outcome side surfaced by out-of-family codex review round 2 at the `B-118` leg, attempt side by round 6, both 2026-08-08 — each adjudicated PRE-EXISTING, so the leg declared the boundaries rather than absorbing an unpriced redesign)*

- **What it is.** The `B-118` trial-epoch guard (Runtime spec **v1.114 §14.6.4**) binds the **`half_open` resolution arms ONLY** — `record_success` / `record_failure` / `re_arm_half_open_trial` refuse to resolve a `half_open` machine unless the caller presents the current admission epoch. The **`closed` arms stay TOKENLESS by design.** Codex round 2 named a scenario that crosses the boundary: a request admitted while the breaker was `closed` survives, in flight, through a trip **and** a successful half-open recovery, and its stale success then arrives while the breaker is `closed` again. It takes the tokenless `closed` arm, resets `fail_count`, and erases whatever failure accumulation has built up **since** the recovery.
- **Why this is PRE-EXISTING and not a `B-118` regression — verified, not assumed.** `record_success` resetting `fail_count` in `closed` is the shipped **consecutive-failure** breaker semantics on main (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py`, the `record_success` CLOSED arm, pre-`B-118`), and the erasure is reachable with **ZERO trips and ZERO recoveries**: siblings fail twice, an older in-flight success lands, the count resets. That is the definition of a consecutive-failure counter and it long predates this leg. `B-118` adds **one more path** into the same arm (a request that outlived a full trip-and-recover cycle) but changes **nothing** about the arm's semantics.
- **Why the `B-118` guard deliberately stops at `half_open`.** The `half_open` race is categorically different — there a stale success does not merely reset a counter, it **resolves a trial it does not own** and closes a breaker the machine had already judged unhealthy, after which the real trial's failure lands under threshold and the provider-model is silently readmitted. Guarding `closed`-arm outcomes instead requires capturing an epoch on **every ordinary dispatch** and changes **contracted** success-reset semantics for every caller — an unpriced redesign, not a bug fix. That is why the leg **declared** the boundary at §14.6.4 rather than absorbing it.
- **The ATTEMPT side, added 2026-08-08 (out-of-family review round 6, same `B-118` leg).** The family has a second half. A dispatch admitted while the breaker was `closed` keeps its **multi-attempt** `RetryPolicy`. If it fails transiently, and siblings then trip the breaker while a later dispatch acquires the `half_open` permit, the `closed`-era dispatch resumes **attempt 2 with no breaker re-consultation** — a concurrent **paid** call overlapping what §14.6.4 calls "the single trial".
- **Why the attempt side is PRE-EXISTING too.** The breaker pre-check is **per-candidate** by contract (§14.6 step 4): it runs once when a candidate is selected and is never re-consulted between that candidate's own retry attempts. The pre-`v1.114` loop behaved identically, so `B-118` neither introduced nor priced this. What round 6 actually caught is that **`B-118`'s own §14.6.4 text overclaimed** — the ratified intent (C9's Pillar-4, the cell-3 operator ratification) binds THE TRIAL to one call and never deliberated stale `closed`-era dispatches. That text is now **scoped** at §14.6.4 to *era-consistent admissions*, with the boundary **declared and witnessed** rather than defended.
- **One family, two sides.** A dispatch admitted under an earlier breaker era continuing to act under that era's policy — whether it is reporting an **outcome** or making another **attempt**.
- **Close-out steps.** **(1) Decide whether `closed`-arm outcome ordering should be epoch-aware at all** — not obviously yes: a consecutive-failure counter that forgives on ANY success is a deliberate, contracted design, and making it order-aware changes what `fail_threshold` means for every caller. Ground against C-CP-03 §3.5 before pricing. **(2) If yes, price BOTH sides honestly** — epoch-awareness on one alone leaves the family half-open. **(2a) The outcome side** — the ordinary-dispatch epoch capture — it touches the **hot path**, not the rare recovery path the `B-118` guard touches, and needs its own witness set plus a decision about what a stale `closed`-arm success does *instead* of resetting (drop it? apply it to the epoch it belongs to? neither is free). **(2b) The attempt side** — mid-staircase re-consultation, the more expensive half and the reason this is registered rather than absorbed: re-checking the breaker *between* a candidate's own retry attempts means **abandoning a candidate partway through its attempt loop**, changing exhaustion accounting and `fallback.last_failure_class` attribution for a path that predates `B-118` entirely. Semantic surgery on the staircase, not a defect repair. Note the two sides can be decided **independently** — guarding outcomes without guarding attempts is coherent (a stale success can no longer close a breaker, even though a stale paid call may still land); the reverse is not, so if only one is built it should be the outcome side. **(3) Note the `B-116` t4 interaction** — any future breaker-persistence arc must replay epochs consistently, or a rehydrated breaker treats every in-flight outcome as stale (or every stale one as current), which is worse than the failure this row describes.
- **What would falsify it.** A demonstration that the `closed`-arm reset is NOT reachable without a trip (which the grounding above already contradicts), **or** a ratified decision that consecutive-failure-with-forgiveness is exactly the intended semantics — in which case this row closes as WORKING-AS-DESIGNED and §14.6.4's boundary paragraph becomes the permanent statement of it.
- **Council:** **No at registration.** A single-domain C9 breaker-semantics question — what `fail_threshold` counts, and whether a counter that forgives on any success is intended — with no nameable cross-domain tension. **Conditionally yes** only if step (2) proposes a **durable or persistent** epoch, which re-opens the persistence tension `B-116` term t4 pinned and is a genuine C9 ⊥ C11 dyad.

### B-136 · The tail-keep **name-matching** always-sampled arm returns before the root-close materialization, so an always-sampled **root** leaves its trace buffered until `force_flush` *(surfaced by direct probe at the `B-133` leg, 2026-08-08 — pre-existing; the `B-133` event arm deliberately does NOT share the shape)*

- **✅ CLOSED 2026-08-13 (PR #1331) — REPAIRED, and it matters more than the row priced.** **(1) Re-ground:** the two-trace probe still read **1 vs 0** at HEAD — the arm had not moved. **(2) Is it a defect — YES**, and the row's own *"nothing is lost, `force_flush` drains keep-all"* premise is **false under pressure**: at `max_buffered_traces=3` over 100 always-sampled-root traces, **97 are evicted** and their children never export. More decisively it contradicts **v1.28 §1.1's own containment model** — *"a new trace whose first observed span is already its root-close materializes + frees its slot … so it does NOT evict"* — an always-sampled-root trace root-closed yet behaved like a never-closing one, displacing live traces. **(3) Repair:** mirror §9.2.1 **term 5** onto the name arm — the same two lines the `B-133` **event** arm already carries, whose comment names B-136 as the case it must not extend. Consistency with the spec-normative arm is the justification. **(4) Both directions witnessed:** slot freed for every root shape; a **triggered** trace exports its siblings *earlier*; a **quiet** always-sampled root (e.g. `hitl.gate.evaluated`) now **drops** them — a real reduction, and exactly what an **ordinary** root already did. **(5) Venue:** not WORKING-AS-DESIGNED — §9.3's implementer discretion is exercised toward the model v1.28 already documents. **No existing witness asserted the stranding** (all 1178 OD tests passed unchanged), so the re-grounding concern was precautionary. **Witness:** `harness-od/tests/test_b136_name_arm_root_close_materialization.py` — 6 tests, mutation-probed (4/6 RED pre-repair).
- **Adjacent finding registered — `B-163`** *(out-of-family Codex round 2 on this PR)*. All three materialization sites — `:478` (original ordinary-root close), `:437` (`B-133` event arm) and `:391` (this repair) — pop the per-trace bucket with **no lock**, while a concurrent child `on_end` can `setdefault` an empty bucket and append to it *after* the pop, losing the span entirely. That window is **pre-existing and module-wide** (no `threading.Lock` anywhere), so it is **not introduced here** — this repair widens the exposed population by making every always-sampled root materialize. Registered **and fixed** as **B-163** in this same PR — one `threading.RLock` guards every buffer/keep/counter mutation, never held across `downstream.on_end`, with a coordinated deterministic witness that loses the span when the lock is neutered.
- **⚠️ COUPLING TO `B-137` STEP (3) — the most consequential part of this closure.** B-137's **candidate C1** was priced *one day earlier* as costing **data loss rather than delay**, because under C1 every workflow trace has an always-sampled root and therefore hit **this** arm (measured 3 buffered / 97 evicted at cap 3). **Post-repair the identical C1 counterfactual reads 0 buffered / 0 evicted**, and with a §10.2 trigger present **100/100** ordinary children export where **0** did before. So **C1's principal cost belonged to B-136, not to C1** — fixing B-136 makes C1 substantially **more attractive**, leaving it at its `1/base_rate` volume multiplier with ordinary trace semantics. **B-137's step-(3) council must be briefed:** the deferral filed earlier today cited C1's eviction cost, and that citation is **superseded**. Both B-137 C1 witnesses were re-priced in the same PR.
- **What it was** *(HISTORY — CLOSED at PR #1331; retained as the pre-repair description, no longer a live statement of behaviour)*.** `TailKeepSpanProcessor.on_end`'s §9.2 **name-matching** arm (`harness-od/src/harness_od/tail_keep_span_processor.py`, the `is_always_sampled(span.name, span.attributes)` branch) forwards the span downstream, optionally sets the §10.2 keep flag, and **returns — unconditionally**, before the `is_root_close → _materialize_trace_decision(trace_id)` step further down the method. When the always-sampled span is itself the trace's **root close**, the trace's buffered siblings are therefore never resolved: they sit in `self._buffer`, holding a `max_buffered_traces` slot, until `force_flush` drains them keep-all at shutdown. **Probed directly at the `B-133` leg, 2026-08-08:** a trace whose root is named `sandbox.violation` with one ordinary buffered child leaves `buffered_trace_count == 1` after the root closes; the control trace with an ordinary root returns `0`.
- **Why it mattered** *(HISTORY — the displacement described here is REMOVED by the repair)*.** The slot is not merely held, it is **charged**: under `max_buffered_traces` pressure `_evict_oldest_trace` sheds the oldest buffered trace FIFO, so a population of never-resolving always-sampled-root traces **displaces live traces** that would otherwise have taken a real keep decision. The module's own *"Eviction fidelity tradeoff"* paragraph reasons about eviction on the assumption that buffered traces resolve at their root close; this arm is the case where they do not.
- **Why it is PRE-EXISTING and not `B-133` exposure.** The name-matching arm and its unconditional return long predate the `B-133` leg; that leg only **asked** the question, because its own new event-aware arm sits beside the same return and the dispatch carrier span **is** routinely the root close, so an unconditional early return there would have put the common case into the leaking shape. **The `B-133` arm does NOT leak** — OD spec v1.38 §9.2.1 **term 5** makes the root-close materialization normative for the event arm, U-OD-59 AC #6c witnesses it, and PD-8 probe (iv) goes RED without it. The asymmetry between the two arms is **deliberate and recorded**, not an oversight: repairing the name arm changes shipped behaviour for every name-shaped always-sampled root, which the `B-133` arc had no authority to do.
- **Close-out steps** *(ALL DISCHARGED at PR #1331 — steps (1)–(5) are answered in the CLOSED bullets above; retained as history. DO NOT re-run: this row is closed.)*.** **(1) Re-ground, because the arm may have moved** — re-run the two-trace probe at the then-current HEAD (always-sampled root + one ordinary child, versus an ordinary root + one ordinary child) and confirm `buffered_trace_count` is still 1 versus 0. Grep proves the `return`; only the run proves the trace is still stranded. **(2) Decide whether it is a defect at all**, and this is a real question rather than a formality: `force_flush` drains keep-all, so **nothing is lost** — what is affected is *when* the siblings export (at drain rather than at root close) and how much `max_buffered_traces` headroom a long-running process retains. A deployment whose always-sampled roots are rare pays nothing. Ground against C-OD-09 §9.3's implementer-discretion clause, which is where the buffering algorithm's shape lives. **(3) If it is a defect, the repair is small but NOT free** — mirror OD spec v1.38 §9.2.1 term 5 onto the name arm (~2 lines). What is not free is the **behaviour change**: every existing witness asserting on export order or `buffered_trace_count` around an always-sampled root must be re-grounded, and a trace whose siblings previously survived to `force_flush` keep-all would, after the repair, be **DROPPED** at root close if no §10.2 trigger fired. That is a real reduction in exported spans for a real population, and it is why this was registered rather than folded into the `B-133` arm. **(4) Witness BOTH directions if built** — the root-close resolution (`buffered_trace_count → 0`) **and** the drop-when-no-trigger consequence, so the repair is not mistaken for a pure bookkeeping fix. **(5) Decide the venue** — if the answer to (2) is that `force_flush` keep-all is the **contracted** behaviour, this closes as WORKING-AS-DESIGNED and the module docstring's *"Drop semantics (false-but-bounded)"* paragraph should be extended to say so, since it currently describes only the never-root-closing case and not this one.
- **What would have falsified it** *(HISTORY — the re-ground at PR #1331 confirmed 1 vs 0, so neither falsifier held)*.** A re-run showing `buffered_trace_count == 0` for the always-sampled-root trace (the arm having changed since), or a ratified reading that deferring always-sampled-root traces to `force_flush` is intended.
- **Council: no.** A single-domain **C7** question about the tail-keep buffering algorithm's own bookkeeping, sitting squarely inside §9.3's implementer-discretion clause, with a named two-trace probe that answers it. The one thing that would change that is step (3) turning out to reduce exported spans for a population an operator depends on, putting the C7 observability floor against the C11 telemetry-volume posture — but that is a consequence to **measure** at step (4), not a tension nameable in advance, so it does not meet the convening discriminator at registration.
- **Cross-ref.** `B-133` (the leg whose probe surfaced it, and whose own arm deliberately does not share the shape); OD spec v1.38 §9.2.1 term 5; OD plan v2.33 U-OD-59 AC #6c + PD-8 probe (iv).

### B-137 · Head sampling starves the tail-keep mechanism for every preserved class that is **not a root span name** — the shipped head sampler applies the §10.3 ratio in **both** §9.1 modes *(surfaced by out-of-family Codex round 1 against the `B-133` leg's commit `ff63c725`, 2026-08-08; premise adjudicated empirically at the real production wiring and CONFIRMED, with the non-uniformity falsifying the first framing)*

- **What it is, measured at the real production composition.** C-OD-09 §9.1 says `TAIL_BASED_PROD` means the sampling decision is taken **at trace completion**. The shipped composition does not deliver that: `harness-runtime/src/harness_runtime/lifecycle/tracer_provider.py` resolves `build_default_sampler(base_rate=PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate)` **unconditionally, for every cell**, and states in the same function that *"the current default sampler ignores the mode … Future units may wire mode-conditional samplers."* So at a `TAIL_BASED_PROD` cell the §10.3 ratio is applied by `ParentBased(HarnessCompositeSampler(base_rate))` **at span creation**, and `TailKeepSpanProcessor` only ever sees the admitted fraction. **A span the head dropped never reaches `on_end` at all**, so no tail-side preservation rule — not §10.2 tail-keep-on-classification, not the §9.2 always-sampled forward, not the `B-133` event-aware arm — can act on it.
- **Measured** over 4,000 carriers per cell at HEAD: `team-binding × self-hosted-server` (base 0.1) admitted **10.4%**; `multi-tenant-compliance × managed-cloud` (base 0.2) admitted **20.9%**; `solo-developer × self-hosted-server` (base 1.0) admitted **100%**.
- **The starvation is NOT uniform, and the non-uniformity IS the finding** — an earlier framing of it as *"every preserved class is equally starved"* was **falsified by the same run**. A §9.2 member realized as a **root span name** is admitted at **100%** even at base-rate 0.1 (measured 4,000/4,000 for `sandbox.violation`), because the head sampler resolves `is_always_sampled` against the span **name** and returns `RECORD_AND_SAMPLE`. What **is** starved is everything the head cannot classify at creation, and there are exactly two such populations: **(i)** §9.2 members carried as span **events** — the `B-133` family, measured 10.4% at base-rate 0.1 — and **(ii)** **non-root** spans of any class, because `ParentBased` consults the inner sampler **only for roots** and children inherit an unsampled parent's decision. Population (ii) includes the §10.2 classification triggers when they are not roots: a trace with an ordinary root and a `sandbox.violation` **child** was preserved in **182 of 2,000** traces (~9%) at base-rate 0.1.
- **Why this is PRE-EXISTING and not `B-133` exposure.** Population (ii) is the bound `composite_sampler.py`'s own *"Enforcement boundary (honest scope)"* docstring **already declares**, and that the `B-TAIL-CONDITIONAL-SAMPLING` arc was opened for; it long predates `B-133`. Population (i) is `B-133`'s, whose **tail** half `B-133` closed for head-admitted carriers and whose **head** half OD spec v1.38 §9.2.1 term 4 declares as a bound with the per-cell table. **Neither row priced the architecture question this row carries.**
- **What makes it architecture rather than a defect repair.** Closing it means making the head unconditionally admitting in `TAIL_BASED_PROD` mode and moving the §10.3 ratio into the tail consumer — which multiplies admitted span volume by `1/base_rate` at every production cell (**×10** at the team cells, **×5** at multi-tenant), changes what `max_buffered_traces` and the C-OD-11 §11.1 per-cell budgets are sized against, and alters the posture of **every** class the tail preserves, not only the three `B-133` named.
- **RE-MEASURED AT THE REAL VENUE 2026-08-13 — steps (1) and (2) are DONE; step (3) is the only live step.** **(1) Re-grounding:** `tracer_provider.py:236-240` still binds `build_default_sampler(base_rate=PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate)` unconditionally, still discards the mode (`_ = resolve_sampling_mode(...)`), and still carries the *"the current default sampler ignores the mode … Future units may wire mode-conditional samplers"* note. **No mode-conditional sampler has landed; this row is not closed by other work.** **(2) Re-measurement, EXECUTED THROUGH `api.run`** — not a manufactured composition (an earlier draft of this arc was correctly rejected by out-of-family Codex for driving an uncalled helper). Running the B-72 fan-out workflow end to end with a real `TracerProvider` and the production sampler at `base_rate=0.0`, **nothing is exported at all** — including `hitl.gate.evaluated`, which **is** a §9.2 member carrying a declared `head=1.0` floor. With an always-on provider the identical run records exactly `workflow.envelope` (ROOT) and `hitl.gate.evaluated` nested under it. **All three of step (2)'s populations are now EXECUTED, not just the venue one (codex round 6).** The `api.run` witness covers the non-root population only; `test_all_three_starved_populations_at_one_composition` covers **root-name-matched** (survives), **non-root** (dropped) and **event-carried** (dropped) at one composition — the probe shape step (2) actually prescribes. **And a tripwire for the very fix this row awaits:** every other measurement supplies a deterministic sampler override, so a future mode-conditional sampler repairing only `TAIL_BASED_PROD` would leave them all green — `test_a_production_cell_still_binds_the_unconditional_ratio_sampler` composes a real production cell (`team-binding × self-hosted-server`, rate 0.1) through the shipped stage with **no override**, so it reddens the moment that sampler lands and forces re-measurement.
- **Why — the floor is ROOT-ONLY.** `workflow_driver.py:3305` opens `workflow.envelope` as the trace root; `workflow.envelope` is **not** a §9.2 member; and `ParentBased(root=HarnessCompositeSampler)` consults the composite sampler **only for roots**. The root loses its base-rate draw and every child inherits the drop, its own name never consulted. **The tail half is executed too** (the row's step (2) names `TailKeepSpanProcessor` explicitly): driving the REAL processor through the REAL run at `base_rate=0.0`, `on_end` receives **zero** arrivals — a head-dropped span is never recorded, so that processor's own §9.2 bypass-the-buffer arm can never act on it.
- **SCOPE — an earlier draft of this arc overclaimed, and the overclaim is WITHDRAWN.** It asserted that *all 19 §9.2 members, at their real emission sites, are children of the envelope.* **False.** Only **11 of the 19** have a span-open site in `src/` at all (the other 8 are event-carried or unimplemented — population (i)), and at least one span-backed member is a **root**: `skill.activation` is emitted from `workflow_driver.py:3206`, which **precedes** the envelope's open at `:3305`. The starvation is **scoped to the members emitted inside the envelope**, and any repricing of step (3) must be against that set rather than all 19. **Qualified 2026-08-13 (codex round 14).** That root status holds for a **top-level** run only. The emit sits inside `execute_workflow`, which is exactly the function the child runner re-enters under an active `subagent.span` — so under a nested recursive run the same pre-envelope emit is a **child** and is starved like the rest. The counterexample **scopes** the starved population rather than exempting `skill.activation`. **General lesson, now applied to every root/child claim on this row:** source-line ordering relative to the local envelope proves *ordering*, never *trace-root status* — root status depends on the call venue, and there are at least two (top-level `api.run`, nested sub-agent dispatch) for the same code.
- **Witness — `harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py` (18 tests).** Deterministic at `base_rate=0.0`, where the ratio arm never admits and the always-sampled arm always does, so each assertion is a decision rather than a sample; a positive control fails loudly if the membership patch stops reaching `is_always_sampled` (patching the public `ALWAYS_SAMPLED_EVENT_CLASSES` frozenset alone is a silent no-op — the literal/prefix structures are derived once at import at `sampling_mode.py:160-172`; there is no runtime mutation path in `src/`, so this is test mechanics, not a defect). **Mutation-probed** by replacing `ParentBased(root=X)` with the bare `X`: both load-bearing tests go RED.
- **Step (3) — repriced by two measured counterfactuals at the same venue. NOTE the naming: neither counterfactual is the row's original candidate A**, which is *mode-conditional* `ALWAYS_ON` in `TAIL_BASED_PROD` with the §10.3 ratio moved **into the tail consumer** — that mechanism was **not** measured here and its ×10-on-everything cost stands as the close-out states it. What was measured: **(A′) bare-composite head** — drop `ParentBased` so the composite sampler is consulted for *every* span. It admits `hitl.gate.evaluated` but **still drops `workflow.envelope`**, yielding **orphaned floor spans whose parent trace was discarded**, at `1/base_rate` applied to floor-member spans **only**. Cheap, and it breaks trace structure. **(C1) admit the root** — add `workflow.envelope` itself to §9.2. Delivers the floor to every in-workflow member **by inheritance**, in one line. ~~keeps traces whole~~ **CORRECTED 2026-08-13 (codex round 3) — it does NOT keep traces whole, and its real cost is worse than a volume multiplier.** Measured through the shipped `TailKeepSpanProcessor`: C1 forwards the envelope and every §9.2 member (both take the always-sampled bypass arm at `tail_keep_span_processor.py:361`), but an ordinary non-member child stays **buffered at root close** — and **a §10.2 classification trigger does not rescue it**, because making the ROOT always-sampled means the root itself takes the bypass arm and `return`s **before** the root-close flush-or-drop decision runs. So under C1 a workflow trace **that contains at least one ordinary non-member child** becomes a never-resolving buffered trace, drained only by `force_flush` — and **worse than that under production bounds** (measured 2026-08-13, codex round 12): `span_processor.py:373-374` always supplies `max_buffered_traces` / `max_spans_per_trace` from `CollectorConfig` (defaults **4096** each, `types.py:741,752`), and the buffer evicts **drop-oldest** — so beyond the ceiling the ordinary children are **evicted and lost**, and `force_flush` never sees them. Measured at a cap of 3 over 100 traces: 3 buffered, **97 evicted**. Under sustained load that is the steady state, since under C1 every trace with an ordinary child occupies a slot and none is released at root close. **C1's cost is data loss, not delay.** **SCOPED 2026-08-13 (codex round 4)** — an earlier wording said *every* trace, which is wrong: this arc's own B-72 venue emits only `workflow.envelope` + `hitl.gate.evaluated`, both of which bypass the buffer under C1, leaving its buffer at **zero**. The measured stranding used a manufactured `validator.evaluate` child. **How often that child population occurs in real workflows is UNMEASURED and is owed before step (3) chooses C1** — the driver does open ordinary spans (`validator.evaluate` at `workflow_driver.py:5600`, plus `tool.dispatch` / `sandbox.*` / `secret.fetch` in the tool dispatcher), so the population is non-empty, but its frequency is not established here. That is exactly the **B-136** pressure this row's own step (5) predicts, and **C1 maximises it** rather than avoiding it. So the live option set is A (unmeasured, literal reading of §9.1) / A′ (measured, cheap, orphaned spans) / **C1 (measured, full trace admitted at the HEAD but ordinary children stranded in the tail buffer, full cost — *not* “whole traces”, corrected 2026-08-13 codex round 5)** / B (reporting only, not a repair) / C (ratify a root-only floor, which must amend §9.2/§9.3's *head=1.0 across all cells* language). The dyadic **C7 + C11** council's stated precondition — step (2)'s re-measurement first — is now **met**. **A′ is a PARTIAL, name-only remedy (measured 2026-08-13, codex round 15).** It addresses only one of the two starved populations: removing `ParentBased` exposes every span's **name** to `HarnessCompositeSampler`, repairing the non-root **name-backed** members, but does **nothing** for the **event-carried** members (the `B-133` family, which B-137 explicitly includes) — those ride as span EVENTS on an ordinary carrier, and at span creation the head still sees only the carrier's name. Measured at `base_rate=0.0`: A′ exports `hitl.gate.evaluated` and **not** the event-carrying `ordinary.carrier`. **A′ must not be selected at step (3) as a complete repair.** Pinned by `test_candidate_a_prime_is_a_name_only_remedy`.
- **C1 MEASURED ON A′'S OWN FALSIFYING TEST 2026-08-15 — the step-(3) option set now compares on ONE axis, and the comparison moves decisively to C1.** The two measured candidates had never been run against the same question: A′ was demoted to a *partial, name-only remedy* by exactly one test (`test_candidate_a_prime_is_a_name_only_remedy`), and **that test was never run against C1**. It has been now, at the same composition, `base_rate=0.0`, every assertion a decision rather than a sample — `harness-runtime/tests/integration/test_b137_c1_discriminator.py` (8 tests, mutation-probed: neutering the C1 patch reds the positive control, the discriminator and the A-equivalence). **Three results.** **(1) C1 rescues BOTH starved populations where A′ rescues one.** Under C1 the ROOT takes the always-sampled arm, so `ParentBased` hands every child an inherited `RECORD_AND_SAMPLE` and never consults a child's name — which is exactly why the event-carried population (the `B-133` family) survives C1 and dies under A′, whose repair works by exposing the child's *name* and an event-carrier's name is ordinary. **A′'s name-only limitation is therefore not shared by C1.** **(2) C1's coverage bound is exactly the starved population this row names.** A carrier that is its own trace root still takes the base-rate draw under C1 (`test_c1_does_not_rescue_a_carrier_that_is_its_own_root`) — and this row's own scope statement, after it withdrew the *"all 19 members are children of the envelope"* overclaim, is that the starvation is *"scoped to the members emitted inside the envelope."* Coverage and scope coincide; C1 is not claimed to reach further. **(3) C1 and candidate A are NOT export-equivalent, and candidate A cannot be fully measured at all — a first draft of this arc claimed equivalence and it is WITHDRAWN (out-of-family Codex round 1, absorbed 2026-08-15).** Two measurement errors produced the withdrawn claim, and both are now pinned as tests rather than prose. **(i) Head admission is not export:** the draft composed only a `SimpleSpanProcessor`, so it measured what the head ADMITTED and reported it as what the pipeline EXPORTED. Every survival claim now runs through the real `TailKeepSpanProcessor`. **(ii) The event name chosen decides the answer:** the draft used `sandbox.violation`, which is a §9.2 member but ALSO a §10.2 classification trigger (`SANDBOX_VIOLATION_SPAN_NAME`), so it rescues its own whole trace at the tail and makes every candidate look equivalent. The representative `B-133` event-carried names (`fallback.triggered` / `fallback.exhausted`) are members and NOT triggers. **Measured properly** — real tail, `fallback.exhausted` — C1 exports `[workflow.envelope, hitl.gate.evaluated, ordinary.carrier]` and an unconditional head exports `[hitl.gate.evaluated, ordinary.carrier]`: **the envelope is dropped**, because under C1 it is a §9.2 member taking the bypass arm while under an unconditional head it is an ordinary span buffered and dropped at root close, the trace carrying no §10.2 trigger. The contrast is itself pinned at `test_the_trigger_event_hides_the_difference_that_the_representative_event_shows`. **And candidate A is not modellable here at all:** its defining half — the §10.3 ratio moved INTO the tail consumer — does not exist in code, so `ALWAYS_ON` is A's HEAD HALF against today's tail and no claim about A's full cost or behaviour follows from it. **What survives is the COST half only, and it is a head-admission fact asserted as one:** at `base_rate=0.0` an out-of-envelope ordinary root is admitted by an unconditional head and not by C1, so C1's `1/base_rate` multiplier is scoped to envelope-rooted traces while an unconditional head admits every root the process opens. **Net, stated at the strength the measurements support: C1 dominates A′ on coverage (both starved populations vs one, verified end-to-end); C1's head-side volume cost is scoped where an unconditional head's is not; and NO equivalence between C1 and candidate A is claimed in either direction.** **What this does NOT settle — the council's remaining question is narrowed, not answered.** The ×10 in-envelope multiplier at `team-binding × self-hosted-server` (rate grounded live at `test_the_production_cell_this_is_priced_against_still_carries_its_rate`) is **real and unpriced against the C-OD-11 §11.1 per-cell budgets** — that is C11's half of the declared C7 ⊥ C11 tension and no probe here touches it. The dyadic **C7 + C11** convening remains owed, and its agenda is **narrowed but not reduced to one question**. What this arc removes from the agenda: **A′ is out** as a complete repair (measured twice, both drafts agree), and **C1's coverage is settled** — it reaches both starved populations end-to-end. What remains genuinely open: **A vs C1 is NOT resolved**, because A's defining tail half is unbuilt and therefore unmeasurable here, so the council still weighs A, C1, B and C; and the **×10 in-envelope multiplier stays unpriced against the C-OD-11 §11.1 per-cell budgets**, which is C11's half of the tension and which no probe in this arc touches.
- **C11 HALF GROUNDED 2026-08-15 — facts filed at `B-182`; this bullet deliberately does NOT restate them.** Three successive rewrites of this bullet drifted out of sync with the row that owns the evidence (out-of-family Codex caught the contradiction twice, at rounds 4 and 6), so the structural fix is a **pointer, not a copy**: `B-182` is the single owner of the C11 grounding and is where the council reads it. Two things only, because they change what the council should ask. **(i) C11's numeric bound EXISTS — the open question is volume evidence, not a missing cap.** `Spec_Operational_Discipline_v1_37.md` Rider (a) states `cell_rate_limit=10_000.0` spans/sec at **every** ACTIVE cell, team × self-hosted included. What §11.1 leaves open at the team cells is the *throughput/population* to assess that cap against (deferred to `Persona_Document_v1.md` §11 open-item 4, still open). So the council must NOT be told there is no committed figure — it must be told the figure is committed and the **evidence to price against it is not**. **(ii) Process placement comes from C-OD-20 §20.1**, the canonical per-cell collector placement matrix — not from the implementation's logical `enforcement_layer` enum. Per §20.1 exactly ONE cell is unambiguously in-process (`solo × local-development`). Earlier drafts of this bullet asserted four, then three; both are **withdrawn**. **Neither `B-182` nor this bullet says whether C1 is affordable** — that call is untouched and still owed. **A discriminating question, registered not asserted:** if the effective cap is a rate limiter, raising head admission RELOCATES the drop rather than breaching the budget, and a rate limiter carries no §9.2 membership knowledge, so C1's floor could be shed one hop later. No artifact currently asks it.
- **C11's VOLUME EVIDENCE — attempted, NOT LANDED, and HELD after three successive wrong compositions (2026-08-16). The requirement is now precisely specified; the measurement is not made.** C11's budget factors as `spans/sec = spans-per-run × runs-per-sec`. `runs/sec` IS Persona §11 open-item 4. **`spans/run` is measurable and still unmeasured** — an arc tried and produced a different answer three times, each correction introducing a new error, so it was **withdrawn entirely rather than shipped at a fourth guess** (`[[reviewer-oscillation-register-and-hold]]`). **The three failures, recorded so the next attempt does not repeat them:** *(1)* reused a helper attaching only a `SimpleSpanProcessor` and reported **head admission as exports** — the exact error the preceding arc documented in a module the attempt cited by name; *(2)* hand-attached a `TailKeepSpanProcessor` to correct it, giving 2 spans/run — but **production omits that processor entirely at `local-development`** (`span_processor.py:368`: the wrap happens only when `deployment_surface != LOCAL_DEVELOPMENT`), so that chain exists at no shipped local run; *(3)* divided the `team-binding × self-hosted-server` cap by a count measured at `solo-developer × local-development`, which is what the B-72 harness actually binds — invalid, since the deployment surface **selects the processor chain** and the persona affects HITL behaviour. **THE REQUIREMENT, stated so it is executable:** build a `RuntimeConfig` bound to a **production surface** (`self-hosted-server` or `managed-cloud`) at the persona whose cap is being priced, drive `api.run`, and let the **stage-4 composer attach the processor chain** — never hand-assemble it, because the chain is cell-selected and that selection is the thing being measured. **AND DEFINE THE DENOMINATOR BEFORE DIVIDING ANYTHING (out-of-family Codex round 3).** Even at the right cell with the right chain, a single `api.run` measures **one workflow shape**; tool-heavy, retrying, or nested-agent workflows differ substantially in spans/run, so dividing the cap by one sample would reproduce this attempt's third failure in a new place. **And the units do not currently meet:** Persona §11 open-item 4 specifies *throughput per DAY*, generically — not workflow **runs/sec** — while the cap is **spans/sec**, so a division needs per-workload counts paired with throughput in matching units, or a representative workload mix. Establish the denominator first; only then divide. **Nothing about C1's affordability is established, and no spans/run figure from this attempt should be quoted.**
- **Cite drift — `workflow_driver.py:3305` → `:3458`.** This row cites the envelope open as `:3305`; at HEAD that line is skill-emitter code and the envelope really opens at **`workflow_driver.py:3458`** (`start_as_current_span("workflow.envelope")`, under the C-OD-25 §25.3 comment block). Mechanism unchanged — only the line moved. *(Kept as its own bullet deliberately: as a trailing sentence on a neighbouring bullet it was destroyed twice by region-rewrites of this row, at rounds 2 and 6, and both times only out-of-family review caught it.)*
- **⚠️ C1 RE-PRICED 2026-08-13 (PR #1331) — supersedes this row's C1 cost statement and the same-day deferral note.** C1's *"data loss, not delay"* cost was a consequence of **B-136**'s name-arm early return, not of C1 itself. With B-136 repaired the identical counterfactual reads **0 buffered / 0 evicted** (was 3 / 97 at cap 3), and with a §10.2 trigger present **100/100** ordinary children export where **0** did. C1 now costs its `1/base_rate` volume multiplier with **ordinary trace semantics** — no stranding, no eviction, no never-resolving population — and the B-136 interaction step (5) predicted is **discharged** rather than aggravated. **A′'s partial, name-only limitation is unchanged**, so the A′-vs-C1 comparison has moved materially toward **C1**.
- **Close-out steps.** *(SUPERSEDED 2026-08-13 for steps (1) and (2) — both are DONE; see the RE-MEASURED bullet above. Retained as history; step (3) onward remain live.)* ~~**(1) Re-ground the wiring first**~~, because it is explicitly marked provisional — confirm `tracer_provider.py` still binds the per-cell ratio in both modes and still carries the *"ignores the mode … future units may wire mode-conditional samplers"* note. If a mode-conditional sampler has since landed, this row may already be closed by that work and should be **re-measured rather than re-argued**. **(2) Re-measure, do not re-reason** — the probe is four lines of composition (`build_default_sampler` at the cell rate + the real `TailKeepSpanProcessor`, counting arrivals at `on_end`) across the three populations above, so the non-uniformity is re-established rather than assumed. **(3) Decide the posture — a genuine architectural fork, not a bug with a fix.** *(AMENDED 2026-08-13 — the option set below is EXTENDED, not replaced, by the measured A′ and C1 in the step-(3) bullet above; candidate A as defined here was NOT measured and its cost statement stands.)* **Candidate A: mode-conditional head** — `ALWAYS_ON` in `TAIL_BASED_PROD` mode with the §10.3 ratio applied inside the tail consumer at the root-close decision. This is what §9.1 literally says and what `tracer_provider.py` already names as an unbuilt surface, but it multiplies in-process span volume by `1/base_rate` and moves the ratio decision to a component with no ratio logic at all. **Candidate B: rejected-carrier recording** — leave the head as is, but record a counter or minimal marker for dropped spans that *would* have carried a preserved class, so the loss is **observable rather than silent**. Cheaper and honest, but it does not deliver the floor. **Candidate C: accept and document** — ratify that the §9.2 floor is a floor over the **admitted stream**, which would require amending §9.2/§9.3's *"head=1.0 across all cells"* language, since that language is what makes A the literal reading. **(4) Price the volume consequence before choosing A — and note it MERGES with `B-133`'s open F-08 residual** rather than sitting beside it: today's ~20% multi-tenant admission is the **ceiling** on the `B-133` arm's added keep-volume; candidate A removes that ceiling, so F-08 measured against today's admission **would not survive this repair**. The two must be measured together. **(5) Note the `B-136` interaction** — candidate A increases the buffered-trace population by `1/base_rate`, making `B-136`'s never-resolving always-sampled-root traces a proportionally larger share of `max_buffered_traces` pressure.
- **What would falsify it.** A re-measurement showing event-carried and non-root-trigger admission **at parity** with root-name-matched admission (which would mean the head composition changed), or a ratified reading under candidate C that the floor was always scoped to the admitted stream.
- **Council: YES** — and this is the row in the family that genuinely earns one, unlike `B-133`, whose repair was single-domain. Candidate A trades a real observability floor (**C7**: a preserved class dropped before any preservation rule can act on it is not preserved) against a 5×–10× in-process span-volume increase at every production cell (**C11**: telemetry volume, collector capacity, and the C-OD-11 §11.1 per-cell budgets those caps were sized against). The tension is **nameable in advance between two specific voices**, which is the convening discriminator, and the decision changes a committed per-cell envelope rather than an implementation detail. Convene **dyadic C7 + C11 at step (3), NOT before step (2)'s re-measurement** — the pilot lesson is that councils convened before the probe converge to a single voice plus cosmetic consultants.
- **OPERATOR RATIFICATION 2026-08-16 — step (3) is DECIDED: (C1) admit the root.** Selected via `AskUserQuestion` after the five-option set was put to the operator with each option's **measured** cost stated. This **discharges** the architectural gate the row has carried since 2026-08-13 (deferred at three separate loop runs) and makes it **buildable**. Option **A** was not offered as selectable — it remains unmeasurable, its tail half unbuilt — and the operator did not ask for it first. **What C1 commits:** the workflow root joins the always-sampled arm, so `ParentBased` hands every child an inherited `RECORD_AND_SAMPLE` and never consults a child's name — which is exactly why the **event-carried `B-133` family survives C1 and dies under A′**, whose repair works by exposing the child's *name*. **The ratified cost, carried forward rather than erased by the decision:** C1's cost is **data loss, not delay** — under production buffer bounds a trace containing any ordinary non-member child becomes never-resolving, drained only by `force_flush`, and C1 **maximises** the `B-136` pressure this row's own step (5) predicts. **The frequency of that ordinary-child population is STILL UNMEASURED.** The ratification settles the *posture*; it does not retire that measurement. The implementing arc MUST measure it before shipping C1 to a production-bounded cell, and MUST carry the `B-136` interaction explicitly. If the measurement shows C1's steady state is buffer exhaustion, that is a **new finding to route back** — not a licence to silently reopen a ratified choice.
- **Cross-ref.** `B-133` (the leg whose commit the finding was raised against; its arm is real but partial, and its F-08 residual **merges** with step (4) here); `B-136` (buffered-trace pressure, step (5)); `B-123` / `B-124` (the same §9.2/§10.2 family); `composite_sampler.py`'s own "Enforcement boundary (honest scope)" note and the `B-TAIL-CONDITIONAL-SAMPLING` arc (where population (ii) was already declared).

### B-138 · `validator.fail.class` carries **two taxonomies** at one attribute name — the shipped value is C-CP-25 `ValidatorFailClass` while C-CP-21 §21.5 declares the attribute as `ValidatorRetryExitClass` *(surfaced by direct read at the `B-124` grounding pass, 2026-08-08 — a LIVE homonym, same class as `B-125` but already shipped)*

- **⟦CLOSED at #1278, disposition (a) — READ THE CLOSED BULLET AT THE END OF THIS SECTION FIRST; it is the operative record. The registration bullets below are HISTORY: the domain is SETTLED (`ValidatorFailClass`), the fork is resolved, and `B-124`'s sequencing gate is DISCHARGED. They are superseded wherever they conflict.⟧**

- **What it was registered as (HISTORY — the "unresolved fork" story below is superseded; the domain is settled at #1278).** The span attribute `validator.fail.class` is declared by CP as an `ENUM_REF` at `Cardinality.LOW` with the schema docstring stating *"`validator.fail.class` is bounded(5)"* (`harness-cp/src/harness_cp/validator_fail_taxonomy.py:137-141`, docstring `:154-159`, per C-CP-21 §21.5 verbatim). The five values that declaration names are **`ValidatorRetryExitClass`** — `transient-retry` / `Reflexion-recoverable` / `HITL-recoverable` / `permanent-fail-exit` / `terminal-fail-exit` (`:38-62`, C-CP-21 §21.1). But the **only live producer** writes a **different enum**: `harness-cp/src/harness_cp/validator_framework.py:346` sets `attrs["validator.fail.class"] = result.fail_class.value` where `result.fail_class` is a **`ValidatorFailClass`** — the C-CP-25 §25.2 pre-emit categorization whose members are `schema_violation` / `semantic_inconsistency` / `safety_policy` / `resource_constraint` / `external_rejection` (`harness-cp/src/harness_cp/validator_framework_types.py:70-90`). That class's **own docstring names the collision without resolving it**: *"Distinct from C-CP-21 §21.1 ValidatorRetryExitClass (post-fail retry-exit classification) homed at `harness_cp.validator_fail_taxonomy`"* (`:73-74`). The attribute reaches the wire through the live `validator.evaluate` span at `harness-cp/src/harness_cp/workflow_driver.py:5600` with attributes applied at `:5620-5622`. **So one attribute name carries two closed five-value domains with disjoint member sets, discriminable only by string shape** — exactly the cross-namespace homonym class as `B-125`, except that `B-125`'s is a homonym-*in-waiting* (no caller can trigger it, `retry_breaker_tool.py:152-153`) while this one is **already shipped at every non-PASS validator evaluation**.
- **A second, smaller divergence on the same namespace, recorded because it is the same defect class.** CP declares **three** `validator.fail.*` attributes — `class`, `cause_attribution`, `permanence` (`validator_fail_taxonomy.py:137-153`, C-CP-21 §21.5). OD declares **four**, at span-site `"validator.fail"` — `class`, `detail_hash`, `next_action`, `escalation_owed` (`harness-od/src/harness_od/validator_namespace.py:112-136`, C-OD-29.1). The two sets **overlap on `class` only**. The live emission (`validator_framework.py:_build_span_attributes`, `:343-349`) produces OD's four **minus `permanence`** and **none** of CP's two remaining — so **neither declaration describes what ships**.
- **Close-out steps (HISTORY — ALL EXECUTED at #1278; nothing here is actionable).** **(1) Re-ground both halves at HEAD before pricing** — confirm `validator_framework.py:346` still writes a `ValidatorFailClass` value, that CP §21.5's declaration still names the `ValidatorRetryExitClass` domain, and that the CP-3 / OD-4 set divergence still stands. Direct read, not recall: the `B-124` pass found the registration cites of **three** sibling rows line-drifted by one intervening arc. **(2) Decide the authoritative taxonomy for `validator.fail.class` — the whole row, and a genuine fork rather than a wiring bug.** **(a) C-CP-25 `ValidatorFailClass` is authoritative** — the shipped value is correct and CP §21.5's *bounded(5)* gloss plus the §21.1 table are wrong as written and owe a design-substrate correction; consequence: `validator_fail_permanence(ValidatorRetryExitClass)` derives from a taxonomy the attribute does **not** carry, which is exactly why `B-124`'s naive wire is blocked. **(b) C-CP-21 `ValidatorRetryExitClass` is authoritative** — the shipped value is a **contract violation** and `validator_framework.py:346` must be re-typed or projected, changing a **live wire value** every downstream consumer sees. **(c) Two attributes** — keep `validator.fail.class` for one domain and mint a distinct name for the other (the `B-125` disposition-(b) shape, priced against the same *"demote the detail, never delete it"* standard). **Do not pick by preference**; ground it on which declaration the **downstream readers** were built against — start with `harness-od` `attribute_class_enforcement.py:56`, `namespace_map.py:185`, `cp_source_namespace_verification.py:64`, and the CP export-manifest cardinality sums, which is where a wrong choice breaks a count gate. **(3) Disposition the CP-3-vs-OD-4 set divergence in the same pass** — same defect class at set granularity; splitting it across two arcs guarantees one is forgotten. Note the emission produces neither set exactly, so *"make code match the spec"* is under-determined until (2) resolves. **(4) Sequencing, and it is BINDING rather than advisory:** this row MUST be dispositioned **before** any arc touches `harness-cp/src/harness_cp/validator_framework.py:_build_span_attributes` — specifically **before `B-124`'s §10.2 half is wired there** — because deriving `validator.fail.permanence` from `result.outcome` mints a **third** derivation on a function whose authoritative domain is unsettled, and a later (b) would then have to unpick two changes instead of one.
- **What would falsify it (HISTORY — the falsifier FIRED; see the CLOSED bullet).** A cleared spec surface stating that `validator.fail.class` carries the C-CP-25 domain (which would make this a stale-CP-gloss doc fix rather than a live homonym), **or** a projection function between the two taxonomies existing on a path this read missed — the search covered `harness-{cp,od,runtime}/src`.
- **Council:** **Not at registration** — nothing is decided here and step (1) is a re-grounding pass. **Conditionally yes at step (2)**, and the discriminator is which candidate survives: **(a)** and **(c)** are single-domain C5/CP attribute-domain calls of the kind `B-125` settled in venue, but **(b)** changes a **live wire value** that OD-side readers and cardinality gates are built against, which puts **C5** (validator semantics — the shipped categorization is the one operators actually see) against **C7** (observability contract — a declared `ENUM_REF` domain that no producer honours is not a contract). That is nameable in advance and dyadic, so if step (2) trends toward (b), convene **dyadic C5 + C7 AFTER** the step-(1) re-grounding and the downstream-reader inventory, never before.
- **Cross-ref.** `B-124` (the row whose grounding surfaced this, and whose §10.2 wire is blocked behind step (4)); `B-125` (the same homonym class, in-waiting rather than shipped); `B-123` / `B-133` / `B-136` / `B-137` (the §9.2/§10.2 realization family this sits beside).
- **CLOSED — 2026-08-09, PR #1278 (the B-138 spec-correction leg — design-substrate + register only, ZERO code). DISPOSITION (a), operator-ratified.** *This bullet states the CURRENT result; the bullets above are the row's registration text and are superseded where they conflict.* **Step (1) executed at HEAD:** the producer write re-verified (drifted `:346` → `:347`); a **second live producer** the registration missed was found — `harness-runtime/src/harness_runtime/lifecycle/validator_escalation_composer.py:151-153` writes `brief.fail_class.value` (`ValidatorFailClass | None`, `"unspecified"` on `None`). **Step (2): the row's own falsifier FIRED** — C-CP-28 §25.5 (`Spec_Control_Plane_v1_10.md:228-233`, with §25.2's `ValidatorFailClass`/`ValidatorResult` declarations) is a **cleared surface** declaring the `validator.fail` span emitting exactly the shipped four attributes, so this resolved as the stale-CP-gloss doc fix the falsifier predicted rather than a genuine live homonym. OD's C-OD-29.1 ingestion schema was built against that shape; the downstream reader inventory came back **all-agnostic** (bare-name membership sets + count gates only); **no** reader asserts a retry-exit value; two CP tests assert `"safety_policy"`; **no** projection function exists in `harness-{cp,od,runtime}/src`. The conditional C5⊥C7 dyad **never fired** — it was conditioned on (b) trending, and (b) was contradicted by every downstream fact. **Step (3) dispositioned in the same pass:** the CP-3/OD-4 divergence is two contracts declaring disjoint-except-`class` subsets of one namespace, not one contract violated; §21.5's declared count stays **three**, so the `namespace_map`/export-manifest count gates are untouched; `validator.fail.permanence`'s former derivation clause is **demoted** (the replacement derivation is deliberately NOT declared — see Step (4) below for why the outcome projection was rejected as lossy); `cause_attribution`'s gap is minted as `B-139`. **Spec leg:** `Spec_Control_Plane_v1_116.md` (three amendment sites, all within C-CP-21 §21) + clearance marker. **Step (4) discharged: `B-124` is UNBLOCKED on the TAXONOMY question, and the permanence-derivation SUB-DECISION is routed to it explicitly** (v1.116 §1.3 — the outcome projection is **lossy**: `ESCALATE` conflates `permanent-fail-exit` with `HITL-recoverable`, so a naive `PERMANENT_FAIL` wire would silently narrow §21.6's always-sample-permanent coverage to the abort path; `B-124` must materialize the retry-exit classification at the emission site, ratify the narrowing WITH §21.6 re-specified, or select another carrier). **Out-of-family review (codex, 3 rounds — round 1: 2 P1 + 2 P2; round 2: 1 P1 + 4 P2; round 3: 3 P1 + 1 P2 — all verified against source, one declined-then-accommodated):** the ADR-D5 §1.10.1 authority-chain conflict fixed by the **bundled ADR-D5 v1.5 → v1.6 amendment** at the canonical declaration site; the lossy outcome-derivation replaced by the routed sub-decision; the `fail_class=None` emission gap + the composer's out-of-domain `"unspecified"` sentinel corrected at §1.1 (conditional emission, sentinel declared non-member); `cause_attribution` declared-required-with-zero-producers **minted as `B-139`** rather than absorbed. Round 2: the span-site realization gap (no production `validator.fail` span; attributes ride `validator.evaluate`/`validator.escalation`; OD sampling keys on span names production never emits) **minted as `B-140`** with the sentinel disposition and §1.1 re-scoped to the attribute DOMAIN; `B-124` cross-noted (gate discharged, naive wire foreclosed); the ADR-D5 pointer row bumped; the replace-not-append register-prose demand **declined** per the `B-119` append-with-supersession-note precedent (its internal-contradiction flag was real and is fixed; at round 3 a SUPERSEDED-BY-CLOSURE marker was prepended to the yaml `close_out` so `--detail` output leads with the operative record). Round 3: **the grounding claim "no reader asserts a retry-exit value" was FALSIFIED** — the C-OD-14 §14.5.3 replay-divergence carrier (`ReplaySemanticDivergenceError`, declaration + test, zero production emission sites) fixes `terminal-fail-exit`, and ADR-D6 v1.2 declares the retry-exit domain — **minted as `B-141`** (the OD/D6 cascade), with the corrected claim now stated in v1.116 §0.1; ADR-D5 §1.10's own normative carrier references renamed in place (the interpretive-note-only approach rejected); `B-140`'s remedy sharpened (trailing-dot prefix precision) and its venue corrected §10.2 → §9.2-family. **Residuals:** the stale *"NEW at C-CP-25"* docstring at `validator_framework_types.py:71` (Class 3, rides the next arc touching that file); the second producer recorded for `B-124`'s witness scope.

### B-139 · `validator.fail.cause_attribution` is declared always-emitted at TWO canonical surfaces (ADR-D5 §1.10.1 + C-CP-21 §21.5) but has ZERO producers *(surfaced at the B-138 codex round, 2026-08-09)*

- **What it is.** Both canonical declaration surfaces state the attribute is always-emitted on every validator-failure event — ADR-D5 §1.10.1 bullet 2 (open-set enum, 10 base values + 5 F5 `secret_*` refinements, with the FM-J framing *"a fail-class without cause_attribution is FM-J per `c5-validation-contract` SKILL.md"*) and C-CP-21 §21.5 row 2 (`Spec_Control_Plane_v1_2.md` §21.5, medium/open-set cardinality). But **zero producers exist**: the framework producer (`validator_framework.py:_build_span_attributes`, `:320-352`) sets `class`/`detail_hash`/`next_action`/`escalation_owed` and never this key; the runtime escalation composer (`validator_escalation_composer.py:143-153`) likewise; an `rg` over `harness-*/src` finds no write site. Surfaced at the `B-138` spec-leg out-of-family codex round (P2), 2026-08-09 — the `B-138` leg corrected the `class` domain and demoted the permanence derivation clause, and without this row the third §21.5 attribute would be left declared-required with no owner.
- **GROUNDED 2026-08-15 — ROUTED TO BACK-FLOW AS A CLASS 1 FORK** (`.harness/class_1_fork_b139_validator_fail_cause_attribution_has_no_producer.md`, filed at #1359). **Step (1) CONFIRMED, row NOT falsified:** a FIXED-STRING search across **all seven** `harness-*/src` trees (`as`, `core`, `cp`, `cxa`, `is`, `od`, `runtime`) returns five hits and **none is a producer** — the declaration at `harness-cp/src/harness_cp/validator_fail_taxonomy.py:144`, a cardinality docstring at `:156`, an OD content-structure list entry, two OD comments. **A near-miss was ruled out rather than counted:** `harness-runtime/src/harness_runtime/api.py:1182` writes a bare `cause_attribution=` keyword but builds a `PauseStateCauseAttribution` on a `PauseStateAuditPayload`, and `retry.cause_attribution` is a different attribute that *does* have producers — so a loose search is the wrong instrument and the fixed-string one is sound. **Step (2)'s probe is decisive and prices (a) higher than this row assumed:** `ValidatorResult` (`harness-cp/src/harness_cp/validator_framework_types.py:171-188`) carries exactly `outcome` / `fail_class` / `revalidation_payload` / `escalation_brief` / `fail_detail_hash` — **no cause field, none derivable** — and `_build_span_attributes` (`harness-cp/src/harness_cp/validator_framework.py:237`, `:305`) receives only `step` / `result` / `next_action` / `burden_count`. The declaration and the declared producer's contract shape are **irreconcilable as written**, so this is Class 1, not a Phase-7 absorption. **This row's own claim that the cause values live in the staircase is FALSE at HEAD** (re-grounded at review round 3, after the fork first repeated it): `StaircaseTransition.on_cause` is typed `ValidatorRetryExitClass` (`harness-cp/src/harness_cp/validator_fail_transient_staircase.py:64`, docstring at `:66` — *"not a fail-cause token"*) and `_classify_provider_exception` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:281`) returns the same class. The staircase carries the **5-class retry-exit taxonomy**, not the 15-value attribution alphabet. **The cause-source inventory churned across rounds 3-5 and is now stated per surface, verified, and NOT asserted as complete:** the 5 F5 `secret_*` refinements have a typed carrier (`SecretFailClass`, `harness-as/src/harness_as/secret_fail_class.py:33-41`) and **partly** live producers (`SecretResolutionError` constructed **11x** by AST count — 8 in `harness-runtime/src/harness_runtime/config/provider_secrets.py`, 3 in `harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py`, zero in `types.py`; only 3 of the 5 values are referenced in production, `SECRET_EXPIRED` and `SECRET_REVOKED` have zero constructions and zero emissions, appearing only in the enum declaration and its metadata table); `replay_semantic_divergence` has a typed carrier (`harness-od/src/harness_od/idempotency_join_dedup.py:375-377`) with NO live constructor, and is ADR-D6's addition rather than a base member; the **10 base values have no general mapping from a validator failure at all**; and the staircase carries `ValidatorRetryExitClass`, not causes. So the precise defect is that **no general mapping exists**, which is what an always-emitted declaration requires — narrower than "nothing exists", and it re-prices (B) DOWN from the round-3 wording while leaving the gap real. A complete per-value inventory is **owed at the apply arc**. An **undischarged cross-ADR obligation** also constrains every disposition: `ADR-D6` §1.5.2 says ADR-D5 absorbs `replay_semantic_divergence` at the next D5 revision (`B-141` assigns that sync here), so demoting D5 while D6 stands would mint a fresh contradiction. the C5 sync scope is **every semantic occurrence, swept by SEARCH not by a hand list** (a `grep -c cause_attribution` over that file currently returns **33** lines; this fork's own enumeration grew 2 → 6 → 11+ before being retired).

- **Original close-out steps — REMOVED, not struck.** They are superseded by the grounding above, and their technical premise was DISPROVEN (they asserted the cause values live in §21.2's staircase branches, which is false at HEAD — see the inventory above), so retaining the text would leave `--detail B-139` returning a pre-ratification story that routes work from a false premise. Provenance: they required **(1)** re-verify zero producers, **(2)** decide between wiring and re-declaring, **(3)** disposition the FM-J framing — **step (1) is executed and answered**; **step (2) is PART-DONE — its probe and pricing are complete, but its DECISION (wire vs re-declare) is deliberately NOT made here and remains open for operator ratification (fork §8)**; **step (3) is ROUTED, not answered** — the FM-J disposition is an open question for the operator at ratification (fork §8). The original wording remains recoverable from this file's git history. **Council: see the CONDITIONALLY OWED bullet below** — a dyadic C5 + C7/C9 convening is owed before ratification.

- **What would falsify it.** *(tightened 2026-08-15 to match fork §6 — the previous single-producer condition was too loose.)* **Only** (a) **every** required validator-failure path emitting `validator.fail.cause_attribution`, or (b) the canonical requirement being demoted. A single new producer, a cause member added to `ValidatorResult`, or wiring the replay-divergence event **do NOT falsify it** — each only re-prices a disposition for one surface while every other required path stays nonconformant.
- **Council: CONDITIONALLY OWED before ratification — C5 ⊥ C7/C9** *(2026-08-15; this REVERSES an intermediate "not owed" reading taken while the fork was in review, which conflated the probe resolving the FACTS with the cross-domain TRADE being resolved — it is not).* The open A/B/C choice moves the C5 validation contract (the FM-J rule and the attribution obligation), C9 retry inputs (any new cause-classification source sits beside `ValidatorRetryExitClass`), and C7 observability semantics (a medium-cardinality open-set attribute on every validator-failure event). That is a nameable multi-domain tension, so per the orchestration matrix a dyadic **C5 + C7** (or **C5 + C9**) convening is owed BEFORE the operator ratifies. The registration-era text — "grounding first; conditionally at step (2) ... a probe-first pass may resolve it without convening" — is superseded only in its *grounding-first* half: grounding is done, and it showed the tension is live rather than dissolved.
- **Cross-ref.** `B-138` (the leg that minted this); `B-124` (the sibling §21.5 attribute gap — permanence — with its own routed sub-decision).

### B-140 · The `validator.fail.*` wire realization diverges from its declared span-site and value alphabet *(surfaced at the B-138 codex rounds, 2026-08-09)*

- **What it is — two gaps, one family.** Both are declared-vs-shipped divergences on the `validator.fail.*` namespace that survive `B-138`'s domain settlement. **(A) Span-site:** C-CP-28 §25.5 declares a `validator.fail` SPAN (`Spec_Control_Plane_v1_10.md:233`) and OD's C-OD-29.1 declares span-site `"validator.fail"` (`validator_namespace.py:54`, `:112-136`), but **no production path opens such a span** — the attributes ride the `validator.evaluate` span (`workflow_driver.py:5600`, §C-OD-29.1 envelope attach loop) and the runtime composer's `validator.escalation` span (`validator_escalation_composer.py:141`). OD sampling fixtures are built against `validator.fail.*`-shaped span NAMES production never emits (`test_sampling_mode.py:215` `"validator.fail.semantic_inconsistency"`; `test_composite_sampler.py:265-268`), so the declared §21.6/§25.5 always-sample keying cannot match a live span name — **load-bearing for the NAME/prefix-keyed always-sample family** (`B-123`/`B-133`/`B-137`'s §9.2-style keying — `is_always_sampled` matches the trailing-dot prefix `"validator.fail."` and would NOT match a span named exactly `validator.fail`, so the step-(2) open-the-span remedy MUST use a concrete prefixed name or change the sampler) — **not** for `B-124`'s §10.2 half, whose trigger reads the permanence ATTRIBUTE from any span (codex round-3 venue correction). **(B) Sentinel:** on `HITLEscalationBrief.fail_class=None` the composer writes `validator.fail.class="unspecified"` (`validator_escalation_composer.py:151-153`) — an out-of-domain value under the closed five-member declaration (CP v1.116 §1.1; ADR-D5 v1.6 §1.10.1), while the framework producer OMITS the attribute on `fail_class=None`. Surfaced at the `B-138` spec-leg codex rounds (round-2 P1 + round-1/2 P2), 2026-08-09; CP v1.116 §1.1 records both gaps and routes them here rather than absorbing them as conformance.
- **Close-out steps.** **(1) Re-ground both gaps at HEAD** (span-open sites; the OD sampling key shapes; the two `None`-path behaviours). **(2) Span-site decision:** open a `validator.fail.`-PREFIXED span (a concrete suffixed name — the exact name `validator.fail` alone leaves `sampling_mode.py:239`'s trailing-dot prefix match inert, so this option MUST pair a suffixed name or a sampler change, AND MUST amend the exact-span declarations it diverges from — C-CP-28 §25.5 + C-OD-29.1 — to declare the suffixed/prefix family, else the repair itself re-opens the contract violation) at the producer(s), OR re-declare the span-site at both contracts (C-CP-28 §25.5 + C-OD-29.1) to the shipped `validator.evaluate`/`validator.escalation` carriers, OR alias at the OD classification layer — grounded on what the OD samplers + `B-123`/`B-133` tail machinery actually key on, and coordinated with `B-124` as ADJACENCY, not a gate — its §10.2 trigger is attribute-keyed (`tail_keep_classification.py:96-100`) and is not blocked by span naming. **(3) Sentinel decision:** absent-on-`None` at the composer (matching the framework producer) OR widen the wire alphabet at both canonical surfaces; either way the two producers must agree. **(4) Coordinate:** `B-124` (sampling half), `B-123`/`B-133` family (tail keying), `B-139` (sibling §21.5 gap).
- **What would falsify it.** A production `start_as_current_span("validator.fail…")` site, or a cleared surface declaring `validator.evaluate` as the canonical carrier span for these attributes.
- **Council:** **No at registration** — grounding first. **Conditionally at step (2):** span-site repair trades contract fidelity (C7 — a declared span-site no sampler can match is dead contract) against emission-shape churn every OD consumer sees (C5/C8). Nameable and dyadic if the re-declare route trends; probe-first over the OD keying may resolve it.
- **Cross-ref.** `B-138` (the leg that minted this); `B-124` (ADJACENT — its §10.2 trigger is attribute-keyed, not gated by this row); `B-139` (sibling mint); `B-123`/`B-133`/`B-136`/`B-137` (tail-keying family).

### B-141 · The replay-semantic-divergence surfaces (C-OD-14 §14.5.3 + ADR-D6 v1.2) fix `validator.fail.class = terminal-fail-exit` -- out-of-domain under B-138 (a); cascade owed at the OD/D6 venue *(surfaced at the B-138 codex round 3, 2026-08-09)*

- **CLOSED 2026-08-12 — read this bullet first; the bullets below are the registration record.** All three close-out steps discharged; operative record at `.harness/b-141-validator-fail-class-cascade-2026-08-12.md`. **(1) Re-grounded at HEAD:** carrier at `idempotency_join_dedup.py:347` (drifted from `:345`), witness at `test_idempotency_join_dedup.py:307`, ADR-D6 rows `:120`/`:298`, production emission sites confirmed **zero** (`rg` over `harness-*/src` + `tests`). **(2) Cascade landed at all FIVE venues** (the fifth — the OD plan — surfaced by this arc's own cross-spec drift grep, not named at registration) under the carried value **`validator.fail.class = semantic_inconsistency`** — the row's own natural candidate, a `ValidatorFailClass` member (the replay contradicts the F2 ledger's prior recorded state); **no new wire attribute minted, so the council clause never fired**. ADR-D6 v1.2 → **v1.3 in place** (§1.5.2 escalation-table row + §1.2.2.1 `retry.fail_class` cross-ref clause reworded — `retry.fail_class` KEEPS the retry-exit domain; file name retained per the ADR-D5 v1.6 in-place mechanics). OD spec v1.40 → **v1.41** (C-OD-14 §14.5.3 escalation-table row; `permanence = permanent` + its C-OD-09 §9.2 always-sampled arm PRESERVED VERBATIM — the terminal/HITL intent survives structurally, carried by permanence + routing per ADR-D5 v1.6 §1.10, not by the wire class). CP plan v2.51 → **v2.52** (U-CP-41 acc #5 + the U-CP-47-era `VALIDATOR_FAIL_NAMESPACE_SCHEMA` enumeration superseded **as-domain as NOTES**; landed units NOT amended per the B-97(a)/B-118 precedent; test-roster supersession `test_verifier_fail_class_in_cp_21_5_set` → `test_verifier_fail_class_in_validator_fail_class_domain`). OD plan v2.34 → **v2.35** (the v2.2-era U-OD-20 "ESCALATION to terminal-fail-exit" criteria — standing authority at head v2.34 — superseded **as-value as a NOTE**; leaving them would have put OD spec v1.41 and the OD plan in direct in-axis disagreement). Carrier default + docstrings + both witness files updated. ADD v1.3's version-scoped "At v1.2" narrative recorded at the back-flow doc §4 (recorded-not-edited class), not touched. **(3) Honored:** declared-shape-only — no emission wiring. **Coordination notes honored, nothing widened:** `B-124` (permanence stays explicitly declared on the carrier; derivation NOT wired), `B-139` (cause_attribution producers untouched; the never-discharged D6-v1.2 forward flag that ADR-D5 would absorb `replay_semantic_divergence` is recorded at the back-flow doc §4 as B-139's family), `B-140` (span-site/keying family untouched), `B-126`/`B-146` (`retry.*` domain unchanged). `B-138` settled the wire attribute on the C-CP-25→C-CP-28 `ValidatorFailClass` domain (CP v1.116, operator-ratified (a)). One DECLARED surface family still binds the OLD retry-exit domain to the wire name: C-OD-14 §14.5.3's replay-semantic-divergence escalation carrier `ReplaySemanticDivergenceError` (`idempotency_join_dedup.py:345-367`) fixes `validator_fail_class = "terminal-fail-exit"` (plus `cause_attribution = "replay_semantic_divergence"`, `permanence = "permanent"`, `always_sampled = True`), enforced by `test_idempotency_join_dedup.py:304-306`; and ADR-D6 v1.2 declares the retry-exit five-value domain at its ingestion rows (`ADR-D6_v1_2.md:120`, `:298`). **Zero production emission sites construct the carrier at HEAD** (declaration + test only), so no live wire value violates the disposition — but the declared surfaces contradict it, and the `B-138` grounding claim (*"no reader asserts a retry-exit value"*) was falsified by this family at codex round 3. A SECOND deferred carrier is in the same family (codex round 5): `VerifierResult.validator_fail_class` (`both_by_tier_overlay.py:78-95`, the U-CP-41 two-agent-observer verifier output) is documented as drawn from the C-CP-21 §21.5 five-value set (`test_both_by_tier_overlay.py:121-126`; plan criteria `Implementation_Plan_Control_Plane_v2_9.md:621-630`/`:683-686`) — deferred, no live producer, but its audit contract routes to `validator.fail.*`, so it would violate the authoritative domain when the observer activates. The CP plan delta-chain history (`Implementation_Plan_Control_Plane_v2_4.md:604`/`:624`, U-CP-47-era criteria) also carries the five-value wire framing — history, not retroactively edited, recorded so no future reader mistakes it for current authority.
- **Close-out steps.** **(1) Re-ground at HEAD** (carrier defaults + test; the ADR-D6 rows; production emission sites still zero). **(2) Cascade at the owning venues:** an ADR-D6 amendment (in-place revision, the ADR-D5 v1.6 mechanics) + an OD spec leg for C-OD-14 §14.5.3 + the carrier-default/test updates + a CP plan-note delta superseding the landed U-CP-47 criteria as-domain (`Implementation_Plan_Control_Plane_v2_4.md:631-638`, preserved at v2.16 — an ACTIVE spec/plan contradiction for any future re-validation arc, per codex round 4; the landed unit is NOT amended, per the `B-97`(a)/`B-118` new-unit precedent) + the U-CP-41 family (the `VerifierResult` carrier docstrings + `test_both_by_tier_overlay.py:121-126` + the U-CP-41 plan-note, same as-domain supersession) — deciding WHAT the divergence escalation carries under the new domain: a `ValidatorFailClass` member (`semantic_inconsistency` is the natural candidate), a distinct attribute for the retry-exit classification (the `B-138` candidate-(c) shape), or a re-declaration of the §14.5.3 fixed attributes. The `permanence="permanent"` + always-sampled INTENT must survive whichever choice (coordinate with `B-124`'s derivation sub-decision and `B-140`'s keying family). **(3)** The choice changes DECLARED event shape only at HEAD (zero emission sites) — price accordingly; do not let the cascade silently widen into wiring the emission (its own arc).
- **What would falsify it.** A production construction site of `ReplaySemanticDivergenceError` appearing before the cascade lands (the out-of-domain value would become LIVE and upgrade urgency).
- **Council:** **No at registration** — the cascade direction is largely forced by the ratified `B-138` disposition; the step-(2) carried-value choice is a C5/C7 attribute-semantics call of the kind `B-125`/`B-138` settled in venue. Convene only if the distinct-attribute route trends AND it would mint a new wire attribute.
- **Cross-ref.** `B-138` (the disposition this cascades); `B-140` (keying family); `B-124` (derivation sub-decision); `B-139` (sibling §21.5 gap).

### B-142 · Two `harness-inspect` pause-journal witnesses assert *"no usable config exists"* without isolating an ambient auto-discovered `harness.toml`, so they pass in CI and fail on any machine with the documented local config *(surfaced at the `B-123` arc by a full `just check` run, 2026-08-09)*

- **Named OPEN residual on the closed row** (the `B-104`/`B-133` precedent), raised by the merge-gate test-witness lens at #1281 and recorded rather than absorbed: **the isolation is not structurally guaranteed.** A future edit dropping the helper call from ONE of the two tests reproduces this exact defect for that test, and **CI structurally cannot catch it** — CI never carries the ambient `harness.toml` that exposes the divergence. Step (2)'s option landscape weighed a *suite-wide* autouse fixture (rejected: masks genuinely config-dependent regressions) and an injectable discovery root (rejected: touches `src/`), but did **not** consider a **file-scoped** autouse fixture — precedented in this very tree at `harness-runtime/tests/test_config_source.py:45-56` — which would make the isolation structural for the module without the suite-wide risk. Not a correctness defect in what shipped (the lens verified the isolation load-bearing and non-vacuous, and returned APPROVE). Second caveat, same lens: the premise assertion could pass for the WRONG reason if a malformed ambient `HARNESS_*` var made `load()` raise via env-coercion *before* file discovery — narrow, absent in the verified environment, recorded for honesty.

- **CLOSED 2026-08-09 at PR #1281 — read this bullet first; everything below is the registration record.** All four close-out steps discharged. **(1) Re-grounded NON-DESTRUCTIVELY** — the discriminator is confirmed to be exactly the ambient file, proven WITHOUT touching the operator's real gitignored config: the same test file run with cwd = repo root gives *2 failed / 19 passed*, and with cwd = a clean temp dir (via `uv run --project` + absolute paths) gives **21 passed**. The mechanism is `RuntimeConfigSource._discover_default_config` resolving `Path.cwd() / harness.toml` — **process CWD only, no parent walk**. **(2) Candidate (a) selected**: both exposed witnesses now call a shared helper that `monkeypatch.chdir`s to an empty `tmp_path` **and asserts the premise** (`pytest.raises(RuntimeConfigLoadError)`), making the isolation a positive control rather than an assumption — if it ever silently stops working the test fails loudly at the premise, not mysteriously at the `scope_known` outcome. `monkeypatch.chdir` (never a bare `os.chdir`) so the CWD restores even on failure. **(b) and (c) rejected on grounds, not cost** — a suite-wide autouse fixture can mask a genuinely config-dependent regression elsewhere, and an injectable discovery root touches `src/` and could owe a contract question for no benefit. **ZERO src change**; the `_authoritative_tenant_scope` precedence-chain widening is contracted and correct and is preserved verbatim. **(3) Sibling sweep — completeness ARGUED, not assumed:** eleven candidate files checked and cleared with a stated reason each, and the decisive bound is that `grep -rn "Path.cwd()"` over all `harness-*/src` returns **exactly one** hit, so this auto-discovery class has exactly ONE production site and no further siblings can exist. **(4) Honored** — no assertion weakened, deleted or relaxed; the `scope_known` mutation-probe semantics are byte-preserved. **Load-bearing by mutation:** removing the isolation line reproduces the original RED from the repo root; restored from a `cp` backup, verified byte-identical. **Purpose verified by execution:** `just check` on the developer machine WITH the ambient `harness.toml` present now exits 0 at **7714 passed / 0 failed**, where it was *2 failed / 7705 passed*. The operator's `harness.toml` was never moved, renamed, deleted or modified.

- **What it is.** Two witnesses in `harness-runtime/tests/test_pause_journal_enumeration_b97a.py` assert `pause_journal_deployment_scope_known is False` on the stated premise *"where no usable config exists at all"*, but do not isolate the ambient config source. `_authoritative_tenant_scope` (`inspect.py:1035`) loads through the NORMAL `RuntimeConfigSource` precedence chain — a deliberate, correct out-of-family round-3 [P2] widening — so an auto-discovered `harness.toml` satisfies the load and `scope_known` returns `True`.
- **Grounded, not assumed.** Surfaced by a full `just check` at the `B-123` arc (2 failed / 7705 passed) on a branch whose diff touches no source either test imports. main's own CI is **green** at the same base commit `e496d97e`; `RuntimeConfigSource.load(config_file=None)` was executed directly and returns a config rather than raising, because a **gitignored `harness.toml`** sits at the repo root. A first hypothesis — the justfile's `dotenv-load` — was **tested and falsified** (they fail under plain `uv run pytest`, and `.env` has no `HARNESS_*` keys).
- **Close-out steps.** **(1) Re-ground** by running the pair with and without a root `harness.toml`. **(2) Decide the isolation mechanism** — (a) per-test absent-config path, (b) an autouse package fixture, (c) an injectable discovery root at `RuntimeConfigSource`, the only candidate touching `src/`. **(3) Sweep for siblings** asserting an absent ambient source. **(4) Do NOT weaken the assertions** — `scope_known` is load-bearing and the tests' own docstrings record it as a mutation probe.
- **What would falsify it.** The two tests failing in CI as well, or passing locally with the root `harness.toml` present — either would mean the discriminator is not the ambient file.
- **Council:** **No.** A test-isolation defect with a reproducible discriminator; convene only if step (2) selects candidate (c) and it owes a contract question.
- **Cross-ref.** `B-123` (the arc that surfaced it; not blocked by it — disclosed in PR #1279 rather than absorbed).

### B-143 · A wall-clock shutdown-bounding assertion has **zero headroom** — its threshold equals the simulated hang, so scheduler noise on a loaded CI runner flips it *(observed twice on doc-only PRs, 2026-08-09)*
- **CLOSED 2026-08-09 — test-only, with the whole measured class hardened rather than the first reported name.** The dispatch-executor witness and its three exact shutdown siblings (tracer, provider and MCP host) no longer compare real elapsed time to the same `0.3s` hang they simulate. Each fake now parks on an explicit release gate beneath an outer mutation bound that fails if the production `wait_for` wrapper is removed. The synchronous tracer/dispatch witnesses record the `to_thread` call attempt without requiring the worker body to win the same `0.02s` scheduling race and prove non-completion; only the dispatch stage separately proves its reachable timeout-before-worker-start venue. The async provider/MCP witnesses isolate already-tested predecessor stages and give the target stage a `0.5s` budget beneath a `2.0s` outer mutation bound before asserting start, cancellation, and non-completion. This applies #1226's principle with a structural gate because cancellation state, not TTL arithmetic, is the discriminator. The sibling sweep found **four** zero-headroom shutdown tests, and all four are hardened. The two cross-process lock witnesses also shared the cold-import deadline shape: both now use one `60s` cold-child readiness budget (above the measured `33.89s` maximum and consistent with existing process witnesses) plus immediate failure if the child exits before reaching `flock`. The ready-marker handshake remains intact and load-bearing. The aggregate gate measured two more process-start siblings: the Antigravity CLI descendant witness missed its arbitrary `5s` startup deadline and a Codex session-start hook exceeded the shared `30s` subprocess deadline, then both passed immediately in isolation (`6.49s` and `3.04s` complete-test durations). Their startup budgets are now `60s`; the reviewer witness also fails immediately if its controller exits before creating the child, while its independent `3s` descendant-reap discriminator and `10s` SIGTERM-completion bound remain unchanged. A later rerun exposed an observation-layer sibling: the pure PR-head mismatch unit test could be preempted by the production session-state shell probe returning UNKNOWN after its own `10s` timeout. The four pure merge-proof classifier tests now declare `SessionState.INACTIVE` as an explicit precondition; all session-state integration tests retain the real hook path, so merge-proof assertions remain discriminating without unrelated shell-probe timing. A third rerun found the original zero-headroom shape in the session-start advisory-hygiene witness (`5.77s` observed against `<5s` despite correct bounded return): it now captures the exact `1` argument at the real `hook_bounded` entry and asserts that the deliberately hung hygiene child received TERM, while a `15s` outer subprocess bound still kills a mutation that removes the hook's bound before the fake's `30s` sleep completes. The merge gate also exposed a distinct production defect before the tracer stage: `flush_observability()` can wait unbounded for saturated-default-executor scheduling. That defect is registered separately as B-147 rather than hidden or absorbed into this test-only arc. **ZERO `src` change, ZERO production-tool change, ZERO design-substrate change, ZERO load-bearing discriminator deletion; elapsed assertions were replaced by stronger state/call witnesses.**

- **HISTORICAL SIBLING GROUNDING 2026-08-09 (#1289; anchors resolve against that pre-closure tree) — step (5)'s sweep found a DIFFERENT MECHANISM in the same class.** `harness-runtime/tests/test_lifecycle_protected_result_store.py::test_gc_sweep_blocks_across_separate_processes_via_cross_process_lock` waited for a child process to signal readiness within a **10.0s** deadline (historical anchor `:1481`; current shared readiness helper at `:252`, child-script `gc_sweep()` call at `:238`, and readiness-helper invocation in the `gc_sweep` test at `:1533`). The child is a **cold interpreter** that must import the harness packages first, and that import cost is what the deadline raced. **Measured, not inferred:** three consecutive standalone runs of the same child script gave time-to-ready of **5.39s / 33.89s / 7.69s** — the deadline sat *inside* the observed spread, and one trial exceeded it by 3.4×. **Timing, not logic**, established by positive control: run standalone the child exited `rc 0` with empty stderr and *did* write its marker. Seen RED once during #1289's own gate run (competing with three review subagents plus an out-of-family reviewer) and again in isolation on the loaded machine, while passing **four** earlier full-gate runs over the same commit range. **The #1226 scripted-clock remedy did NOT transfer** — a fake clock cannot make a real cold import faster; the closure selected a measured `60s` readiness budget plus immediate child-exit failure and preserved the handshake. **Not introduced by #1289**, whose diff left `harness-runtime/` byte-unchanged.

- **Historical registration grounding (anchors resolve against the pre-closure PR #1285 tree).** `test_shutdown_bounded_by_timeout_when_dispatch_executor_drain_hangs` asserted `elapsed < 0.3` at `:894`, while the simulated hang was `drain_sleep_seconds=0.3` at `:886` and the bound under test was `timeout=0.02` at `:892`. The threshold **equaled the hang**, so the headroom between bounded (~0.02s) and not-bounded (0.3s) was entirely consumed by event-loop scheduling delay.
- **Observed twice in one session, both on DOC-ONLY PRs** where no code changed and the test could not have regressed: #1280 (`0.345 < 0.3`) and #1285 (`0.307 < 0.3`). The first was rerun green, confirming flakiness.
- **The discriminator could not simply be deleted.** The sibling report assertions did NOT discriminate the documented regression — waiting out the full 0.3s sleep still set `timed_out`. **Superseded at closure:** preserve `:894`'s discriminating intent, not its literal elapsed assertion; the closure replaces it with structural witnesses that fail on the same missing-`wait_for` mutation.
- **Close-out.** Re-ground the three historical figures; confirm timing-not-logic under load; apply **#1226's exact-discriminator principle** without widening a wall-clock bound; preserve the mutation discriminator while replacing the scheduler-sensitive elapsed byte; sweep for sibling zero-headroom wall-clock assertions across `harness-*/tests`.
- **Council:** **No.** Test hygiene, two measured occurrences, a named precedent for the fix shape.
- **Cross-ref.** Surfaced during the `B-126`..`B-132` grounding arc (#1285); unrelated to that diff.

### B-144 · C-CP-24 §24.1.B's `Attribute count` column was never re-tabled after C-CP-03 §3.5's two amendments, and the code manifest resolves the divergence inconsistently per row *(surfaced at the B-126 build arc, 2026-08-09)*

- **One declaration site, never re-tabled.** `§24.1.B` (the four F3-capability-floor lifecycle-event-attribute export rows) appears in exactly **one** file across all 115 `Spec_Control_Plane_v1_*.md` versions on disk — `Spec_Control_Plane_v1_2.md:2152-2159` (measured by scanning every version for the string `24.1.B`; only `:2152` the sub-heading and `:2167` a summary paragraph match). Its table pins `retry.*` at **4 attributes** (`:2157`) and `harness.breaker.*` at **7 attributes** (`:2159`).
- **Both figures were superseded at the other venue.** `C-CP-03 §3.5` extended `retry.*` **4 → 6** at v1.3 (`Spec_Control_Plane_v1_3.md:76`) — an outright **replacement** of the attribute set, since the v1.2 four (`retry.attempt` / `retry.cause` / `retry.backoff_ms` / `retry.policy`) do not survive it — and extended `harness.breaker.*` **7 → 9** at v1.32 (B-19-BREAKER-AMBIENT-ATTRS). Neither carried across to `§24.1.B`.
- **Why it is more than a stale count: the code resolves the divergence inconsistently, one row each way.** `cp_namespace_export_manifest.py` pins `retry.*` at **4** (tracking the stale `§24.1.B` row, not `§3.5`) and `harness.breaker.*` at **9** (tracking `§3.5`, not `§24.1.B`). The manifest therefore conforms to neither venue as a whole, and its own `test_per_namespace_attribute_count_match_spec` asserts the mixture.
- **Shape.** The **spec-vs-spec ingestion-gap** class `B-116-t3` closed for OD `§9.2` — *not* code drift: each individual code row is byte-exact to **some** cleared contract text.
- **CLOSED 2026-08-11 (#1311)** — **venue A ratified** via loop-mode `/resolve` (codex=A + advisor=A agreement; hitl scope split → safer follow-up, `B-153` minted): CP v1.117 re-tables `§24.1.B` (retry 4→6 per C-CP-03 §3.5 v1.3; breaker 7→9 per OD C-OD-07 §7.1 at **OD** v1.32 — the growth's venue is the OD chain, not "CP v1.32") **plus** the §24.1.A `engine.*` row 3→4 (C-CP-09 §9.1 v1.3; out-of-family round-2 catch), declared sum **65 → 68** (35+29+4); companion plan delta v2.50 re-pins U-CP-54; manifest+tests cascaded incl. the terminal manifest's frozen "65 attributes" invariant (now derived from `CP_EXPORTED_ATTRIBUTE_COUNT`) and OD's `namespace_map.py` engine row (was stale against its own cited authority; round-4 catch). Fork doc `class_1_fork_b144_cp_24_1b_stale_retable.md`; clearances `spec-control-plane-v1-117` + `implementation-plan-control-plane-v2-50`. The bullets below are the original filing, preserved verbatim.
- **Close-out.** Re-verify the single-venue claim at HEAD (a later re-table closes this row on its own falsifier); then **decide the venue** — amending `§24.1.B` to match `§3.5` bumps the declared export sum **65 → 67** and moves what CP claims to export for ADR-D6 `§1.2` ingestion, while leaving it as-is means the table is knowingly historical and should *say* so. Note the asymmetry when pricing: `retry.*`'s 4 names a **retired** attribute set, whereas `harness.breaker.*`'s 7 is a proper subset of the current 9. Do **not** edit any `attribute_count` as a drive-by — the sum feeds acceptance #6.
- **Council:** **Conditionally yes at the venue decision** — OD-facing export-count stability and the acceptance-#6 gate's meaning (C2/C6) against a contract table that knowingly misreports what CP exports (C7). Not before the re-verification step, per probe-first discipline.
- **Cross-ref.** Surfaced by direct read at the `B-126` build arc; `B-126` closed deliberately **without** touching these counts.
- **A third same-class case, added at the merge gate.** The divergence is **not** confined to §24.1.B. `Spec_Control_Plane_v1_2.md:2148` gives `hitl.*` **no attribute count at all** — *"per-event attributes across 4 span names"* — while `cp_namespace_export_manifest.py` pins `attribute_count=4` for it, i.e. **the code counts span names as attributes**, and that 4 feeds both the §24.1.A subtotal of 34 and the 65 sum. Pre-existing and untouched here. It makes nothing above false (this row claims nothing about §24.1.A), but the *"bumps the sum 65 → 67"* pricing covers only the two §24.1.B rows — a closer would not otherwise learn of a case whose repair is not a count bump at all, but a decision about what the column **means**.

### B-145 · `retry.backoff_ms` + `retry.cause_class` have **zero** producers, and the mandated `retry.terminal = "escalate"` value is never emitted — collapsed into `max-attempts` *(surfaced at the B-126 build arc, 2026-08-09; Gap 2 from out-of-family review round 1)*

- **CLOSED 2026-08-11 (#1309)** — all three legs landed: **GAP-1** (#1308, Runtime v1.115 — stale v1.2-era aliases aligned to the CP §3.5 names; two `emitted=False` register rows retired), **GAP-2b** (#1306, five `retry.terminal` values wired on the dispatch path), **GAP-2a** (#1309, Runtime v1.116 — the escalate clauses' phantom attempt-indexed `advance_staircase(policy, attempt_count, validator_fail_class)` signature corrected to the landed stage-keyed API consulted stage-fresh as a cause-class classifier; `retry.terminal = "escalate"` ratified a **defensive branch contract**, unreachable-by-construction at the v1.116 classifiers and witness-pinned on both composers; stage-threading rejected — operator `max_attempts` dead-letter, `CROSS_FAMILY_FALLBACK` void on the candidate-less tool path, escalation already structural — and declared a future design arc requiring its own back-flow; adjudicated via loop-mode `/resolve`, codex=B + advisor=B agreement; fork doc `class_1_fork_b145_gap2a_escalate_clause_phantom_api.md`; clearance `spec-harness-runtime-v1-116-cleared-2026-08-11.md`).

- **Two distinct gaps at one attribute family**, both against **normative step text** rather than a discretion bullet.
- **Gap 1 — two keys with zero producers.** `Spec_Harness_Runtime_v1.md:4229` (*annotate inner span with `retry.terminal = "retry"` + `retry.backoff_ms`*) and `:5685` for the tool path; `:4228` (*`retry.terminal = "fail-fast"` + `retry.cause_class = "{class_name}"`*) and `:5686` for the tool path. An **emission-position** sweep across all seven `harness-*/src` trees returns **zero** sites for either name; before this row's own register landed the bare string did not appear in `src` at all (f-string / concatenated construction checked too — none).
- **Gap 2 — a mandated value never emitted, plus a collapse that misattributes two dispositions.** Each path mandates **five** values, not four: `success` / `fail-fast` / `retry` / `max-attempts` / **`escalate`** — the fifth at `:4229` (*If staircase result escalates beyond `RETRY_WITH_BACKOFF` … annotate inner span with `retry.terminal = "escalate"`*) and the same clause at `:5685`.
- **The tool path carries four of five.** `retry_breaker_tool.py` emits `success` / `retry` / `fail-fast` / `max-attempts` (plus a non-mandated `audit-signing-fail-closed`) and **never** emits `escalate`: its `else` branch at `:275-286`, whose **own comment** reads *“Exhaustion: either last attempt or staircase escalation past STAGE_2”*, stamps `max-attempts` for **both** — so an escalation is indistinguishable on the wire from a retry-budget exhaustion. Same misattribution class `B-127` + `B-131` just closed at the sibling skip call site.
- **The dispatch path carries zero of five.** `retry_breaker_fallback.py` sets `retry.terminal` at exactly **one** site (`:1294`, value `audit-signing-fail-closed`, which `§14.6` does not name) — **despite** having the staircase branch those values belong to at `:1364-:1383`.
- **Carried as data where it can be.** Both Gap-1 keys sit in `RETRY_WIRE_REGISTER` with `emitted=False`, and `test_unemitted_keys_are_exactly_the_two_b145_registers` pins that set exactly, so wiring either **forces** the flag to flip. Gap 2 is value-level and not expressible at key-level granularity — recorded here only.
- **Close-out.** Re-ground the offsets at HEAD (delta chain — they move) and re-run the sweep — **the emission-position one, not a bare literal sweep**: since the register now declares both Gap-1 names as data, a literal sweep necessarily returns nonzero and would let a closer discharge this row **without wiring anything** (out-of-family round-1 catch against this row's own first filing). Separate the two gaps before pricing; for Gap 1 check first whether `retry.cause_class` duplicates the **emitted** `retry.fail_class` (if so the honest disposition may be a spec correction, not an emission); for Gap 2 treat `B-127`/`B-131`'s lesson as binding and note the dispatch path needs all five wired, not one added.
- **GROUNDED 2026-08-11 — the row SPLITS (full doc at `.harness/b145-grounding-split-2026-08-11.md`).** The close-out's step-3 check answered: **Gap 1's keys are stale pre-rename aliases**, not missing derivations — `retry.backoff_ms` is the v1.2-era name the CP §3.5 v1.3 amendment renamed to `retry.delay_ms` (`Spec_Control_Plane_v1_3.md:76` verbatim), the same Runtime sections bind the §3.5 names at `:4225`/`:5682`, and both producers already emit them (`retry_breaker_tool.py:263`/`:292`) — so Gap 1 routes as a **spec leg** (align the four bullet mentions + re-disposition the two `emitted=False` register rows + pinning test; X-AL-3), never duplicate wiring. **Gap 2a (tool-path `escalate`) is unreachable by construction** — the tool dispatcher hard-codes `STAGE_1` on every `advance_staircase` call (`retry_breaker_tool.py:252-256`) and `(STAGE_1, TRANSIENT_RETRY)` maps unconditionally to `STAGE_2` (`validator_fail_transient_staircase.py:114-119`, stage-keyed), so the collapsed `else` is genuinely last-attempt-only today; it routes as a **control-flow/spec leg** (thread the stage across attempts or ratify the carve-out), not a telemetry stamp. **Gap 2b (dispatch path zero-of-five)** stays the impl repair, with per-branch staircase reachability re-grounded before wiring.
- **Council:** **No.** Conformance to already-cleared normative step text with a named owner; the only judgement is answered by reading the two contracts.
- **Cross-ref.** Surfaced at the `B-126` build arc, 2026-08-09, alongside `B-144`. Gap 2's five-value inventory and the tool-path non-conformance came from **out-of-family review round 1**, which caught the first filing claiming four values and calling the tool path conformant — the author's spec read had been truncated at exactly the `escalate` clause.

### B-146 · The `retry.*` static drift catcher **cannot** guarantee that no undeclared key hides — the enumerated escape shapes, recorded rather than chased *(surfaced across out-of-family rounds 2/3/5/6/7 + merge-gate lens 3 at #1289, 2026-08-09)*

- **A retracted claim, falsified by execution at two independent reviewers.** The `B-126` arc's drift catcher originally asserted *"an undeclared key cannot hide"*. Out-of-family round 7 built a key as `NAMESPACE = "retry"` + `"."` + `suffix` — no fragment contains `retry.` — and the suite stayed green. Merge-gate lens 3 declared a key constant **in the declaration home** and emitted it from a producer, wiring an `emitted=False` key with everything green, because the literal then lives only in the module the liveness check excludes by design.
- **Why this is a row and not a fix.** Seven review rounds each closed one or two indirection shapes and each surfaced more: a regex crediting declarations, an alias arm crediting any call argument, a rebound alias crediting a stale literal, a dict write with no telemetry sink, split-literal composition, f-string interpolation, `join`/`format`/`%`. That is the **non-convergent adversarial-hardening signature**, and the workspace's own discipline is to re-scope rather than chase. The arc closed what was cheap and measured, and recorded the boundary here.
- **Enumerated residuals.** (1) a fragment held as a class attribute, imported, accumulated with `+=`, or routed via `functools.partial`; (2) **a key constant declared in the declaration home and imported by a producer** — escapes every arm, including liveness, *by construction*; (3) a helper reached as a method (`self._set(...)`), a helper→helper chain, or a key passed by keyword; (4) an inline `attributes={...}` at a **non-emitting** callee still counts as an emission (low reachability — the only two in-tree non-emitter `attributes=` sites pass a parameter, not an inline dict).
- **The boundary is measured, not guessed.** Widening the composition net to any string merely *containing* `retry` flags **twelve** sites at HEAD, all log and error messages; the bare-namespace-plus-dotted-prefix form flags **zero**. That ratio is why the net sits where it does.
- **Close-out.** Do **not** reflexively chase another shape — act only when a producer actually adopts one. Prefer **fail-closed** over resolution (a one-line net plus a witness, versus unbounded evaluation work). The one residual worth pre-emptive thought is the imported home-declared constant: it cannot be closed by widening a net, and the cheap enforceable rule is *the declaration home declares no emittable key constants* (true today). Re-measure before widening any net, and respect the shared-parse cost budget.
- **Council:** **No.** A recorded limit of test-side static analysis, no contract surface; the one judgement was answered by the arms-race discipline plus measurement.
- **Cross-ref.** `B-126` (closed, the arc that built the catcher), `B-145` (the `emitted=False` rows whose flags residual (2) could silently falsify).
- **Residual (5), added at the merge gate.** The rebind invalidation keys on `ast.Assign` only, so a name rebound by `+=`, an annotated assignment, a tuple-unpack, a `for` target, a walrus, or `with…as` is **not** invalidated and its old literal is still credited. Recorded for completeness, not as a live hole: every in-tree attribute-key constant is UPPERCASE, and lens 3 confirmed by running the repo's own pyright config that those rebind forms produce **three `reportConstantRedefinition` errors in a blocking CI lane**. If that convention relaxes, widen the invalidation to every binding form. (An `AnnAssign` constant is not registered as an alias at all — an under-count, i.e. the loud direction.)

### B-147 · `shutdown()` can hang before tracer close when `force_flush` waits in the saturated default-executor queue *(surfaced by PR #1290 merge-gate Lens 1, 2026-08-09)*

- **What it is.** `shutdown()` bounds dispatch drain before observability flush, but `flush_observability()` submits tracer `force_flush` through `asyncio.to_thread` without an outer `wait_for` covering default-executor queue delay. A timed-out workflow worker can still occupy that same loop default executor when `api.run()` enters shutdown. With a saturated one-worker executor, `force_flush` queues without starting, the top-level deadline expires without being observed, tracer close is never reached, and the caller receives no `ShutdownReport`.
- **Reproduced at the real venue.** PR #1290 merge-gate Lens 1 used a real saturated executor and observed `OUTER_TIMEOUT_BEFORE_REPORT`. The same review confirmed the dispatch queue-delay witness is sound because dispatch drain precedes flush and already bounds submission plus execution.
- **Why B-143 does not absorb it.** B-143 is a test-only timing-hygiene closure. Its tracer witness now tests the reachable hanging-`shutdown()` venue without claiming that a pre-existing saturated executor reaches that stage. This production defect is registered rather than hidden or broadened into the closing arc.
- **Close-out.** Reproduce through the shipped `api.run()`/MCP chain; bound `force_flush` submission plus execution by the remaining shutdown budget; preserve per-surface isolation; add a real-executor witness that does not stub an upstream stage and fails when the wrapper is removed; sweep the later flush fsync offloads for the same queue-delay venue.
- **What would falsify it.** A shipped call-chain trace proving the timed-out workflow worker cannot still occupy the same default executor when shutdown begins, or an existing outer deadline around the `force_flush` submission that the merge-gate probe bypassed.
- **Council:** **No.** Concrete bounded-shutdown conformance defect with a reproduced venue and a local mechanical repair; escalate only if the failure-tag contract needs a new public surface.
- **Cross-ref.** B-143 / PR #1290 (discovery and deliberate non-absorption).

### B-148 · R-CTX-1 context-optimization program — approved execution, Arcs 0–7 / Waves 0–5 *(operator-ratified 2026-08-10; 3-model reconciliation; Arc 8 parked at B-149)*

- **What it is.** The context-preload optimization program: baseline ~98–100k tokens preloaded per session, reconciled Floor-B target ~71k (−28%), acceptance gate ≤ 76k measured. The plan was authored via a 3-model adversarial pipeline (Codex gpt-5.6-sol × Claude Opus 5 independent reviews → 10-item deliberation → Fable 5 reconciliation); reconciled verdict BLOCK-as-written → APPROVE-WITH-CONDITIONS on a re-scoped plan of **9 arcs, of which 8 are executable** (Arcs 0–7; Arc 8 / Floor C is PARKED at sibling row B-149 and NOT authorized — this open row's scope is Arcs 0–7 only). The operator approved **execution of all waves** 2026-08-10.
- **Operator ratifications (2026-08-10, verbatim from the approved plan).** **R1** — "Arc 2's three rules (roadmap_status truncation + archive + query-not-Read for the .harness working set)". **R2** — "Floor B is TERMINAL. Arc 8 / Floor C parked in forward register with D-H probe preconditions. Do not run probes." **R3** — "12 machine-local bmad payloads: move ignore entries `.git/info/exclude` → `.gitignore` + documented reversible local removal." Earlier ratifications this program: D1 runner-neutral `docs/governance/`; D2 design-skills + bmad removal both runners; D3 per-repo skill disconnect preserving other repos + Codex superpowers/CodeRabbit intact.
- **Execution doc.** `.harness/r-ctx-1-implementation-plan-v1.md` (repo-committed at this row's own PR; the gstack checkpoint at `~/.gstack/projects/arhugula-v2/checkpoints/` is the advisory origin copy per AGENTS.md cross-runner-state discipline) — units U-CTX-00..22, wave/gate structure (Waves 0–5, Gate G1 on PR #1292), the token-efficiency routing law (batch-don't-scatter; one agent per arc), git discipline (serial merges + terminating refresh), and the sub-agent brief contract. Durable reconciliation record: the claude.ai artifact cited in that plan.
- **Close-out.** Program AC (U-CTX-21/22): measured cold-start preload ≤ 76k (gate; target ~71k) via the `just context-budget` first-turn metric, AND a post-compaction measurement via a TESTED compaction-generation selector in the tool (first assistant call after each compaction boundary) or a separate documented post-compaction acceptance procedure — the first-turn metric alone CANNOT satisfy the post-compaction half (a post-compaction request is not the session's first turn; plan errata E4); component-separated A/B for both; all new CI gates green on main; `git log --stat` audit shows ZERO `design-substrate/**` diffs across all program PRs; register flip to closed + memory entries + final terminating refresh at U-CTX-22.
- **Council:** **No.** The 3-model reconciliation pipeline already served as the decorrelated deliberation for this program, and the operator ratified R1–R3 + all-waves execution 2026-08-10 — no further multi-voice fork remains open.
- **Cross-ref.** B-149 (the parked Arc 8 / Floor C sibling — NOT part of this open scope).

### B-149 · R-CTX-1 Arc 8 / Floor C conditional-pack redesign — PARKED, Floor B terminal *(operator R2, 2026-08-10; D-H probe preconditions carried)*

- **Operator R2 ratification (2026-08-10, verbatim).** "Floor B is TERMINAL. Arc 8 / Floor C parked in forward register with D-H probe preconditions. Do not run probes."
- **D-H probe preconditions** *(carried verbatim from the reconciliation's Codex round-2 D-H position — the agreed probe sequence gating any future un-parking)*: "split and ship P6a hygiene first; build a shared pack router in observation-only mode; prove identical pack IDs for Claude/Codex payload fixtures across Bash, `apply_patch`, Edit/Write, MCP, multi-path, rename, and bypass cases; run harmless live PreToolUse probes on both installed hosts and inspect transcripts for context-before-side-effect plus deny-and-retry behavior; test missing/malformed packs, timeout, missing dependencies, and untrusted hooks; verify startup/resume/clear/manual-compact/auto-compact with `(session_id, compaction_generation)` reinjection; then shadow-log real sessions before moving one reference pack at a time. `SubagentStart` is not categorically unavailable, but editing the existing emitter would break its byte-exact fixture (tools/hooks/test_subagent_validate.sh:98); a separate handler still needs live aggregation and ordering proof. If PreToolUse transport, coverage, failure closure, parity, or compaction reinjection fails, Floor B is terminal and no load-bearing rule moves."
- **The safety kernel is never a candidate.** Parent delegation, posture/X-AL-3, paid/secret/destructive boundaries, recovery, and PR/CI fixed-point rules remain always-loaded regardless of any future probe outcome.
- **Status semantics.** `held` = ratified operator DEFER-INDEFINITELY. The D-H probes are **not** to be run; this row un-parks only on a fresh explicit operator decision, which would then follow the probe sequence above in order.
- **Cross-ref.** B-148 (the open program row this parking decision belongs to).

### B-150 · deferred-flush aftermath — collector-stop ordering + standalone-flush atexit arming *(surfaced by PR #1303 codex delta round post-cap, 2026-08-11)*

- **CLOSED 2026-08-11 (#1310; atexit half at #1307)** — both halves landed. Collector half: Runtime v1.117 spec leg — C-RT-10 step-3a gains the in-flight-conditional deferral mirroring 3b (deferred watcher executes 3a→3b in order after the last worker; a worker finishing between 3a and 3b completes the stop inline with the report tag withdrawn); the deferred-close discipline is ratified in contract text (ordered atexit backstop; loop-bound stop, closed loop ⇒ terminated; deferred closes excluded from the `timeout` bound; narrows-not-eliminates scope honesty); both step-6 invariants qualified deferred-path-only; forward-correctness framing (receiver-less scaffold at HEAD — the rejected carve-out would have ratified span loss against C-RT-10's own PRD enablement). Impl companion same-PR with 3 witnesses + 2 RED mutation probes (probe 2 caught a vacuous first-draft race witness) + the #1303 set re-run 84/84. `/resolve` advisor=A (3 corrections absorbed); codex vote unparseable ×3 (recorded, not counted — out-of-family still fires on the PR diff). Fork doc `class_1_fork_b150_collector_stop_ordering_deferral.md`; clearance `spec-harness-runtime-v1-117-cleared-2026-08-11.md`.

- **What it is.** Two aftermath surfaces of the B-147 bounded-flush machinery (PR #1303), surfaced by the codex delta round after the 10-round loop closed and deliberately registered rather than absorbed (the #1290 precedent). (1) **Collector ordering** (codex P1): C-RT-10 orders collector stop (step 3a) before the tracer close (3b), so a flush whose worker timed out can complete its export *after* `_close_collector_daemon()` ran — late spans land on a stopped collector. Inherent to the spec step order under any bounded shutdown (the pre-B-147 behavior on this venue was an unbounded hang), but the deferred-close machinery makes the window observable. (2) **Standalone-flush atexit** (codex P2): the ordered-backstop swap arms only on the shutdown path; a standalone `flush_observability()` timeout followed by process exit leaves OTel's `shutdown_on_exit` handler racing the live worker (subprocess probe: `atexit_shutdown_while_flush_active=True`). The shipped call graph has exactly one flush caller — `shutdown()` itself — so (2) is reachable only by a future external caller of the public primitive.
- **Close-out (superseded in place 2026-08-11 — the decide/check halves are ANSWERED by the grounding below; this is the single current disposition).** (1) **Collector half — confirmed spec-touching:** author the C-RT-10 spec leg FIRST (either an in-flight-conditional step-3a deferral mirroring 3b, or an explicit ratified-cost carve-out), with clearance marker, THEN the impl if the deferral reading is chosen. (2) **Atexit half — confirmed warranted:** arm the ordered atexit replacement on the flush-surface timeout path itself, witnessed with the subprocess-probe shape; impl-only, no spec touch. (3) Re-run the #1303 witness set green after either change.
- **GROUNDED 2026-08-11 — both original falsifiers checked; neither fires.** (1) C-RT-10 read in full (`Spec_Harness_Runtime_v1.md` §10, `:3718-3733`): the step order is unconditional — "Stop collector daemon" sits before `tracer_provider.shutdown()` with no flush-completion conditionality anywhere in the section, so any in-flight-conditional deferral of step 3a is a spec-behavior change. (2) Pinned OTel SDK **1.41.1** read at the installed source (`opentelemetry/sdk/_shared_internal/__init__.py:224-258`): `BatchProcessor.shutdown()` joins the worker bounded then calls `exporter.shutdown()` directly — not under `_export_lock` — and its own comment admits an in-flight export may still be running ("we will return instead of waiting"); `force_flush` checks `_shutdown` only at entry. The SDK does **not** internally serialize, so the atexit-arming half stays warranted for any future external caller.
- **Cross-ref.** B-147 (the closed parent repair this row post-dates).

### B-153 · C-CP-24 §24.1 Attribute-count column semantics undefined at hitl.* — span names counted as attributes *(split from B-144 at the CP v1.117 re-table, 2026-08-11)*

- **CLOSED 2026-08-11 (#1314)** — CP v1.117→v1.118 ratifies the `Attribute count` column as the namespace's declared live export claim in DISTINCT attribute keys (span names are not attributes; intra-row cross-event repeats count once; the v1.117 §0.3 cross-namespace sum caveat stands; parent-span reads are not row exports). The hitl.* cell enumerated 4→11 from C-CP-20 §20.6 (gate 3 + opened 3 + responded 3 + timed_out 2; `hitl.gate.level` once) — the OD counterexample (C-OD-05 §5.1 row 6's standing 11) becomes the ratified figure; `audit.*` audited **CONFORMING** (§20.4 = seven distinct keys; the qualifier describes §20.5 emission subsets, not a counting rule). §24.1.A 35→42; sum 68→75. Same-PR cascade: manifest hitl 4→11, `test_total_attribute_count_seventy_five`, NEW cross-package map↔manifest witness (closes the #1311 recorded residual — well-defined only under this ratification), plan v2.51 U-CP-54 re-pin; NO OD delta owed. Clearances `spec-control-plane-v1-118` + `implementation-plan-control-plane-v2-51`.

- **Registered 2026-08-11 (#1311, at the B-144 venue-A close).** The `hitl.*` row of §24.1.A (`Spec_Control_Plane_v1_2.md:2148`) declares no attribute count ("per-event attributes across 4 span names") while `cp_namespace_export_manifest.py` pins `attribute_count=4` — the code counts span names as attributes, and that 4 feeds the §24.1.A subtotal (35 since CP v1.117, which also carried engine.* 3→4 per C-CP-09 §9.1 v1.3) and the declared export sum (68). Not a stale-count case: no venue declares any hitl.* count, so the repair is a decision about what the `Attribute count` COLUMN MEANS, potentially rippling to other qualified §24.1.A rows (`audit.*` "7 attributes per persona-tier emission discipline"). CP v1.117 §0.5 explicitly withholds ratification of the span-names-as-attributes reading. Scope lineage: the B-144 /resolve split (codex in-delta / advisor follow-up) took the safer surgical option — this row is that follow-up. Close-out: enumerate C-CP-20 §20.6's real per-event inventory, decide the column semantics (B-144 venue-A precedent: declared live export claim), correct hitl.* and audit the audit.* qualifier under the same definition; any count edit is a contract-count change (spec delta first, manifest + acceptance-#6 cascade same-PR). Grounding lead (B-144 OD leg): OD already reads the count as attributes — C-OD-05 §5.1 row 6 commits hitl.* at "11 attributes across 4 span names" and OD's map/test ingest 11, a live cross-axis counterexample to the CP manifest's 4.

### B-152 · B-150 deferral-unavailable residuals -- stalled-loop late stop + watcher start-failure orphan *(surfaced at PR #1310 codex round 2, 2026-08-11)*

- **CLOSED 2026-08-11 (#1313)** — both residuals closed as **declared boundaries** via the Runtime v1.117→v1.118 spec leg (two one-clause qualifications, zero code change). Residual 1 grounded **vacuous in-repo**: every production path hosting `shutdown()` runs on an `asyncio.run`-managed loop (`cli/app.py:299/:342/:615`), closed on return — stopped-but-open is embedder-only, and the v1.117 paragraph already declares its treatment (harmless inversion direction); the C-RT-07 §7 invariant gains the deferred-path-only qualification, simultaneously discharging the owed #1310 lens-2 recorded residual (the ":3390 qualification"). Residual 2 boundary-declared at the §10 step-6 invariant (watcher-launch-failure carve-out: no chain exists — collector lives until loop/process death, both closes stay reported); the priced on-loop fallback **REJECTED with grounds** (second close-ownership mechanism; unbounded worker wait — the class B-147 removed; the fallback can itself fail to schedule). Falsifiers preserved in the v1.118 change-note: deferred-chain retirement or the live OTLP receiver arc re-opens both questions there. Clearance `spec-harness-runtime-v1-118-cleared-2026-08-11.md`.

- **Registered 2026-08-11 (#1310, at the B-150 collector-half close).** Two residuals of the C-RT-10 v1.117 step-3a deferral, surfaced by out-of-family codex round 2 and independently adjudicated benign-direction/bounded by merge-gate lens 1 on the same PR (the weight disagreement is the recorded signal). (1) A stopped-but-open harness loop defeats termination proof: the guard treats the collector terminated and the tracer closes; a later-resumed loop leaves the daemon task alive, and a >10s-stalled loop can run the queued stop after the tracer close — the HARMLESS inversion direction (collector-late cannot lose late-export spans; collector-early was the B-150 defect), declared best-effort by the v1.117 deferred-close paragraph; absorbing it needs proof-of-termination on a loop that cannot execute (the unbounded-wait class B-147 removed). (2) `Thread.start()` failure after the collector handoff orphans the collector for a long-lived process (backstop is tracer-only, exit-only) — one health-check task, reported in `ShutdownReport`; an on-loop fallback is a second ownership mechanism, priced at close.

### B-151 · standalone atexit backstop residual concurrency shapes *(surfaced by merge-gate lens 1 at PR #1307, 2026-08-11)*

**CLOSED 2026-08-11 (#1312)** — both shapes fixed impl-only in `shutdown.py`, no spec touch. Shape 1: `_spawn_deferred_tracer_close` commits a per-ctx *deferred-owns-forever* tombstone (`_deferred_owned`) before disarming the standalone backstop; the arm refuses while the tombstone is live — the lens's tombstone candidate chosen over deferred-snapshot event tracking (one dict commit under the existing lock). New flush workers stay legal in the gap; only close ownership is fenced. Shape 2: both closing-gate commit sites (`_standalone_ordered_backstop` + the pre-existing `_live_workers_or_gate` sibling, same commit per this row's close-out) skip the gate commit when the ctx weak probe is dead (id-only fallback keeps the commit); the close itself still proceeds. Three deterministic interleaving witnesses (spy-injected, the #1307 pattern), each mutation-probed RED with its guard reverted; re-ground condition resolved *both shapes still live* post-#1310 (collector half did not restructure close ownership). Severity honest-labeling upheld: benign-in-consequence hardening, not a defect-severity claim.

**What it is.** Two narrow residual concurrency shapes of the B-150 atexit half (PR #1307), registered rather than absorbed per the #1290/#1303 precedent (lens verdict APPROVE, register-don't-block). **(1) Re-arm after deferred takeover:** a later standalone `flush_observability` timing out AFTER the deferred-close machinery took ownership (but before its watcher sets the closing gate) arms a fresh standalone backstop alongside the live watcher — at process exit the two closers can run `provider.shutdown()` concurrently. Shutdown-vs-shutdown only: the gate commit fences any new `force_flush`, so the export-vs-teardown race B-150 closed is not reintroduced; OTel 1.41.1's `TracerProvider.shutdown` is idempotent-with-warning on this shape. **(2) Dead-probe gate overwrite under id reuse:** a gc'd armed ctx whose id is reused by a live ctx lets the exit-time backstop clobber the live ctx's `_deferred_closing` gate with a dead ref, which `_register_inflight_tracer_flush` then pops as stale — re-opening the round-10 registration door mid-close. The identical overwrite shape pre-exists in `_live_workers_or_gate`.

**Close-out.** Re-ground both at the then-current HEAD (the open B-150 collector-half spec leg may restructure close ownership and moot either); shape-1 candidate repairs: refuse arming while a deferred-snapshot worker event is outstanding, or a per-ctx deferred-owns-forever tombstone — either with a spy-injected deterministic interleaving witness (the PR #1307 pattern); shape-2 candidate repair: skip the gate commit when the ctx weak probe returns `None`, fixing the same shape in `_live_workers_or_gate` in the same commit or recording why not. Both are benign-in-consequence today — `wont-fix-with-rationale` is a legitimate disposition if re-grounding confirms consequence-free.

**Council.** No — bounded intra-file concurrency repairs with the lens's candidate fixes on file.

### B-154 · Uniform-response target selector keyed by the external correlation token *(registered at the B-71 council arc, 2026-08-12)*

- **What it is.** A properly-specified CP property-6 extension letting an operator target ONE of N parked pre-dispatch gate-owning branches, keyed by the B-71 external token on a NEW resume-context field -- NEVER hitl_responses (v1.108 section 1.1(b) forbids keying the pre-dispatch identity there, and B-71 deliberately makes the external token non-keyable). Surfaced at the B-71 council: a stable external identity is exactly the handle such an extension would key by, which is why B-71 registers it rather than designing it.
- **Close-out steps.** Gated on the B-71 spec leg landing (the token must exist and be stable before anything can target by it). Then: an observable contract change to a cleared mechanism -> its own Class-2 fork + CP spec leg per X-AL-3. Do NOT widen property 1's key shape.
- **Council:** yes at open -- it re-opens the property-4/6 sole-membership safety invariant that B-72 established, so C1 (orchestration) + C10 (action-safety) at minimum.
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.

### B-155 · Nothing signals the operator when a parked pre-dispatch branch becomes sole-addressable *(registered at the B-71 council arc, 2026-08-12; the original "no webhook re-fires" premise was RE-SCOPED 2026-08-12 as UNVERIFIED — see the PREMISE FLAGGED bullet)*

- **What it is** *(RE-SCOPED 2026-08-12 — B-71 v3 §4-ter.1b; the original unconditional "no webhook re-fires" claim is UNVERIFIED and may be false, and is retained below only as superseded history).* **What is certainly real:** no POSTURE SIGNAL reaches the operator — any request that does re-fire is byte-identical and cannot say the branch became sole (that is B-71's original defect). **What is unverified:** the absence of re-fire itself — a LINEAR depth-0 gate DOES re-escalate on re-pause (`test_hitl_resume_without_resolved_response_repauses`, 2 POSTs asserted); the FAN-OUT pre-dispatch path is UNRUN. Re-ground with a fan-out driver/composer round-trip at open. **SUPERSEDED ORIGINAL TEXT:** When N-1 peers resolve and the last pre-dispatch gate owner becomes the sole unaddressed member, the uniform fallback may deliver on the next resume (workflow_driver.py:2851-2898, esp. :2891-2897) -- but NO webhook re-fires to tell the operator that the request they were told to wait on is now actionable. Surfaced by C11 at the B-71 council (T4) and sharpened by out-of-family codex, which showed the same gap makes a STATIC held-for-sole-resolution stamp a liveness failure.
- **Close-out steps.** Coupled to the B-71 spec leg's resolvability re-derivation (precondition 3): if that precondition is met by routing the operator to the live pause view rather than asserting non-actionability, this row stays a UX-latency item; if it is met by a live posture field, this row becomes the mechanism that keeps the field honest. Re-ground both before opening. *(The fork in this bullet is SUPERSEDED 2026-08-12 and DECIDED — do not re-open it: B-71 DESIGN RECORD v3 §4-ter.3 chose (b), state-the-rule-promise-no-readout. This row IS the UX-latency item; there is no live posture field to keep honest yet, and the DEPENDENCY bullet below states what must clear before there can be.)*
- **Council:** no at registration -- C9 owns the redelivery knob and C11 the operator surface; both already deliberated at the B-71 arc. Convene only if a posture-transition webhook is proposed (a NEW outward event class, which is C10 territory).
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.
- **PREMISE FLAGGED 2026-08-12 (B-71 v3 §4-ter.1b, out-of-family round 11) — this row's own title premise is UNVERIFIED and may be FALSE.** `test_hitl_resume_without_resolved_response_repauses` (`harness-runtime/tests/integration/test_u_rt_95_hitl_resume_consume_cycle_e2e.py:649`) shows a resume with an empty `ResumeContext` falling through to Step 4-bis and **re-escalating via the webhook** (2 POSTs asserted) — for a **linear depth-0** gate. Whether the **fan-out pre-dispatch branch** re-escalates the same way is not shown by that test and was not run at B-71 v3. **Re-ground before opening: run a fan-out driver/composer round-trip.** If the fan-out path does re-fire, this is NOT an absence-of-redelivery item — what survives is the weaker claim that the re-fired request is byte-identical and carries no posture signal, which is B-71's original defect rather than a separate gap, and this row should be re-scoped or merged.
- **DEPENDENCY ADDED 2026-08-12 (B-71 DESIGN RECORD v3 §4-ter.3).** This row now also owns the **live-view sub-fork**: exposing per-location eligibility so an operator can see that a parked pre-dispatch branch has become sole. It is not merely unbuilt — it is blocked on two facts. (i) `project_pause_locations` takes `root_snapshot` **alone** (`pause_state_projection.py:816`) and `PausedWorkflowState` carries no `ResumeContext`, so eligibility cannot be computed from the published projection without amending a **cleared** contract (CP spec v1.112 §2.1) — its own spec leg under X-AL-3. (ii) More binding: `read_paused_workflow_state` is **ratified as NOT a liveness claim** (Runtime v1.110 §14.14.9.1 / `B-104` Reading D Component 1) — the journal writes no pause-resolved marker, so a resolved pause is byte-indistinguishable from an outstanding one and the read must not be presented to an operator as an outstanding-pause assertion. A per-location eligibility flag on a read with that limit would be the exact **false-positive twin** of the `held-for-sole-resolution` false negative that B-71 precondition 3 retired. **Clear the `B-104` staleness limit before authorizing any live posture field.**

### B-156 · Uniform-treatment extension of the correlation token to depth-0 root and already-dispatched-child escalations *(registered at the B-71 council arc, 2026-08-12)*

- **What it is.** B-71 populates the external correlation token for fan-out branch escalations ONLY (presence/absence is the population discriminator). The depth-0 root escalation and an already-dispatched child's own internal-gate escalation carry no token. Additive and non-breaking under B-71's placement-inclusive basis, but an observable population change.
- **Close-out steps.** Gated on the B-71 spec leg. The negative rule B-71 makes uniform NOW -- the webhook never carries ingress keys -- already holds for these populations, so this is a population-widening question, not a safety question.
- **Council:** no -- a scoped population widening under an already-adjudicated rule.
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.

### B-157 · The addressing half of B-71 -- pause-view-side capability for already-dispatched children *(registered at the B-71 council arc, 2026-08-12)*

- **What it is.** B-71's original narrower half: exposing an already-dispatched child's real run_id so the operator can build a hitl_responses key. The B-71 council answered NO for the WEBHOOK channel (a keyable run_id is an addressing CAPABILITY, not advisory context; putting it in third-party-visible transit is a spoof/misattribution primitive). What survives is the pause-view-side question: the operator-held local surface is where addressable run_ids already legitimately live, so the question is whether the projection should surface them more usably.
- **Close-out steps.** Re-ground against the public projection's current field set (pause_state_projection.py) before opening -- the capability may already be present and the row may reduce to a usability item. The webhook answer is SETTLED NO and must not be re-opened without a new fork.
- **Council:** conditional -- only if a proposal would move a capability handle onto an outward channel, which re-opens the settled C10 answer.
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.

### B-158 · entry_version is folded into every step idempotency key but is unguarded across the pause boundary *(surfaced at the B-71 council arc, 2026-08-12 -- pre-existing, strictly wider than B-71)*

- **What it is.** WHAT IT IS, and it is strictly wider than B-71. run_idempotency_key folds entry_version (workflow_driver.py:3312-3316 -> :646-665) and parent_idempotency_key inherits it (:668-694), so EVERY step idempotency key in a run depends on the manifest entry_version. But entry_version occurs ZERO times in harness-cp/src/harness_cp/pause_resume_protocol_types.py (verified, 1515 lines); snapshot_hash covers only (workflow_id + run_id + step_index + state_summary) at :929-930, and the resume material-diff guards cover step_id / step_kind / child_workflow_id / gate-config, not entry_version. A manifest version bump across a pause therefore SILENTLY ROTATES every step idempotency key on resume, with no guard firing. Surfaced at the B-71 council (C9 raised the sensitivity, C1 ground it to the zero-occurrence fact); out-of-family codex independently showed it leaves a B-71-specific crash window open.
- **Close-out steps.** (1) RE-GROUND at HEAD: re-verify the zero-occurrence claim and the snapshot_hash field set. (2) DECIDE whether the correct posture is to GUARD (reject a resume whose entry_version differs -- fail-closed, consistent with the other material-diff guards) or to EXCLUDE entry_version from the key derivation (changes every key, a migration question), or to DECLARE the rotation intentional (it may be: a manifest edit arguably SHOULD invalidate idempotency). This is a real fork, not a wiring bug -- do not pick by preference. (3) Note the B-71 interaction: whichever way this resolves may dissolve B-71 precondition 4 for free.
- **Council:** conditional -- if the disposition trends toward EXCLUDE (which changes every step key in every run) that is a C3 (state/ledger integrity) vs C9 (recovery semantics) tension worth a dyad. GUARD or DECLARE are single-domain calls.
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.

### B-159 · Typed ResumeKeyDisposition + a resume-outcome diagnostics carrier for non-honored hitl_responses keys *(registered as a named companion leg at the B-71 council arc, 2026-08-12)*

- **What it is.** Today a hitl_responses key that matches nothing addressable is SILENTLY excluded (workflow_driver.py:2824-2848, :2885-2894; pause_state_projection.py:338-341) and the operator observes only that the run paused again with no explanation. B-71 makes this materially worse if unmitigated, because it HANDS the operator an identity they may reasonably try to key with. C5 specified the shape: a typed per-key disposition (ADDRESSED / UNKNOWN_KEY / NEVER_KEYABLE_INTERNAL_MATCH / CORRELATION_TOKEN_MATCH), with the last two distinct because the operator errors differ (a probe/leak vs an invited mistake). C11 ranked the landing sites: the resume OUTCOME (primary, mandatory), the pause projection (secondary, mandatory), a report-log line (tertiary, never sufficient alone -- solo operators do not tail logs).
- **Close-out steps.** NAMED COMPANION LEG of the B-71 spec leg, NOT a deferral -- the B-71 design record commits to it, and C1 holds the sequencing condition: it ships WITH the B-71 spec leg, or C11's resolvability_note promise that the resume outcome reports a non-attaching response must be softened in the same commit. Impl-grounded open item: the exact resume-outcome result shape (marked [MEDIUM] by C11 -- not read at the council).
- **Council:** no -- the shape was specified by C5 and the landing sites ranked by C11 at the B-71 arc; what remains is carrier grounding.
- **Cross-ref.** Registered at the B-71 council arc; design record at `.harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md`.

### B-160 · `C-OD-32.3` declares webhook spans head=1.0 always-sampled, but `hitl.webhook.deliver` is absent from the 19-entry `ALWAYS_SAMPLED_EVENT_CLASSES` *(surfaced by the B-71 v4 leak analysis, 2026-08-12)*

- **What it is.** A **contract-vs-implementation drift on a sampling FLOOR**, surfaced by the B-71 v4 leak analysis (§4-quater.2b). OD spec v1.8 §C-OD-32.3 states *"Webhook spans head=1.0 (always-sampled — HITL delivery audit-critical)"*, carried verbatim in `harness_od/hitl_webhook_namespace.py`. §9.2 defines `ALWAYS_SAMPLED_EVENT_CLASSES` as exactly the head=1.0 set — and `hitl.webhook.deliver` is **not** among its 19 members. So on any `deployment_surface != LOCAL_DEVELOPMENT` the `TailKeepSpanProcessor` may drop a span the contract declares inviolable. Both halves are pinned by `harness-runtime/tests/test_b71_escalation_token_rides_the_tracing_export.py`, so the drift cannot be silently closed in either direction.
- **Why it matters beyond B-71.** §9.3 calls the always-sampled set **inviolable**, so this is a floor-conformance question rather than a preference — and HITL delivery is precisely the audit-critical case the floor exists for.
- **GROUNDED 2026-08-12 (PR #1328) — the close-out's own first step is DONE, and it RESCOPES this row: it is a CLASS, not an instance.** **Four** unconditional head=1.0 span-name declarations across **two** contracts are absent from the §9.2 floor set — `hitl.webhook.deliver` + `hitl.webhook.attempt` (C-OD-32.3) and `pause.captured` + `resume.attempted` (C-OD-30.3). Witness: `harness-od/tests/test_b160_head_one_declarations_vs_always_sampled_set.py` (3 tests, mutation-probed — adding the webhook names *alone* reds it, which is the point: a one-sided edit must force re-grounding). **Not the B-153 category mistake:** the set's own members are span/event names of the same shape (`hitl.gate.evaluated`, `hitl.invocation.opened`, `topology.fanout.opened`, `mcp.tool.call`) and **no wildcard covers the absentees** — the `hitl.invocation.*` members are enumerated individually and there is no `hitl.*` or `pause.*` wildcard, unlike `audit.*` / `validator.fail.*`. ~~**Conditional declarations are deliberately excluded** from the class — §9.2 handles those via its four conditional rows.~~ **STRUCK 2026-08-12 — that premise is FALSE.** §9.2's four conditional rows are `files.operation`, `memory.operation`, `validator.fail.*` and `subagent.span` (`sampling_mode.py:176-190`), and **neither** `mcp.trust.evaluate` (at `audit_required`) **nor** the operator-burden span (at `degrade`) is among them. Both are **part of the class**, which is therefore **six** span families in two shapes. **`pause.captured` is arguably the worst of the four**, since operator-explicit pause is exactly the audit-critical event a floor exists for. ~~**The `B-137` dependency applies to all four**~~ **RESTORED 2026-08-13 (codex round 9) — it applies to ALL FOUR for complete conformance.** This briefly read *"it applies to THREE"* earlier the same day. `resume.attempted` escapes the dependency **only for a top-level resume**, where its entry-point call precedes the envelope (`workflow_driver.py:3230` vs `:3305`) and it is a trace root; for a **nested child** resume it is emitted while `subagent.span` is current, so it is a non-root child and `ParentBased` never consults its name. Reducing the dependency to three would let B-160 close while nested resumes still miss the floor. The conformance-level dependency is **four**; top-level-only is partial credit, not an exemption: `ParentBased(root=…)` never consults a non-root child's name, so membership alone cannot deliver the floor for any of these child spans.
- **Close-out steps** *(remaining after the grounding above)*. Decide which side is canonical and conform the other: **(a)** add **all four** unconditional names to the §9.2 member set (19 → 23) — `hitl.webhook.deliver`, `hitl.webhook.attempt`, `pause.captured`, `resume.attempted` — **and** add the **two conditional** ones (`mcp.trust.evaluate` at `audit_required`; the operator-burden span at `degrade`) to the conditional resolver, which today covers only `files.operation` / `memory.operation` / `validator.fail.*` / `subagent.span`. A one-name edit does NOT close this row and would leave the witness red. This makes the implementation meet the floor — the §9.2 count is cited in several places, so a count-contract sweep rides along. **Membership alone is NOT sufficient** (round-7 sibling-overlap check): `hitl.webhook.deliver` is a **child** span, and `ParentBased(root=HarnessCompositeSampler)` never consults a non-root child's name — it inherits the parent workflow span's drop decision. **`B-137` already registers exactly this non-root starvation**, so (a) depends on or merges with B-137; closing it standalone would claim a floor the sampler still cannot deliver; or **(b)** amend §C-OD-32.3 if webhook spans are genuinely not meant to be always-sampled — a cleared-contract change under X-AL-3 that must explain how HITL delivery stays audit-critical without a floor. **Grounding: DONE 2026-08-12** — it IS a class (four unconditional + two conditional, across four contracts). Do not repeat it. The `materialize_span_processor_stage` end-to-end run owed on the B-71 spec leg is also the experiment that settles this empirically.
- **The B-137 dependency is MEASURED, and two of the four names are DOUBLY inert (2026-08-13).** The row carried the dependency as a reasoned claim (codex round 7); it is now executed at the real `api.run` venue. At `base_rate=0.0` through the shipped path, `hitl.gate.evaluated` — an **existing** §9.2 member with a declared floor — is not exported at all, because `ParentBased` consults the sampler only for the `workflow.envelope` root and the child inherits the drop. **So adding a name to the set does not deliver its floor** for any member emitted inside the envelope, which is exactly what disposition (a) assumes. **Sequencing:** land B-137 step (3)'s disposition FIRST; B-160's conformance sweep is then mechanical. **Separately** — `emit_pause_captured_span` and `emit_resume_attempted_span` have **no caller anywhere in `src/`** (registered as **B-162**), so `pause.captured` and `resume.attempted` are never emitted in production at all; adding a never-emitted name to the floor set changes nothing, so two of this row's four unconditional names are inert for a **second, independent** reason and (a) should not be written as though all four are symmetric. Executed at `harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py`. **SCOPED TO TOP-LEVEL RESUMES 2026-08-13 (codex round 7).** The root claim above generalized *local line ordering inside one driver function* into *trace-root status*, which is valid only for a **top-level** resume. For a **nested child** resume it is false: `sub_agent_dispatch.py` opens `subagent.span` and invokes `child_workflow_runner(..., pause_snapshot_input=...)` **inside** that block, and the child runner forwards the snapshot to `execute_workflow` — so the child driver's entry-point resume detection runs while `subagent.span` is current. `resume.attempted` would then be a **non-root child**, and it stays gated on **B-137** exactly like `pause.captured`. *"Membership alone is sufficient"* holds for **top-level resumes only**. Pinned by `test_a_nested_child_resume_runs_under_subagent_span`.
- **UPDATE 2026-08-13 (PR #1330).** `B-162` is **CLOSED by wiring** — `pause.captured` and `resume.attempted` are now genuinely emitted in production, so the *doubly inert* qualifier on those two names is **retired**. They are ordinary members of this row's conformance sweep again, still gated on **B-137** for the floor to actually reach them.
- **Council: no** at registration — a conformance reconciliation between two OD surfaces, not a design fork. It becomes council-eligible only under disposition (b), which would relax an inviolable §9.3 floor.

### B-161 · `test_shutdown.py::test_force_flush_returning_false_surfaces_timed_out` is order-dependent — a spurious `('ledger',)` failure under full-suite ordering *(surfaced on the B-71 precondition-4 PR #1324, 2026-08-12)*

- **What it is.** A **test-isolation defect**, surfaced 2026-08-12 on PR #1324 CI (run 31659849345) and once in a local `just check`. The test asserts `report.failures == ()` for a pure-timeout shutdown and instead observed `('ledger',)`.
- **Evidence it is a flake, not a regression.** The same commit passed on CI re-run (18/18); the test passes 6/6 in isolation; the full `harness-runtime` suite passed 3/3 locally (including once with `-p no:randomly`); and main was green on all 8 preceding runs. It reproduces only under **full-suite (all axis packages)** ordering — cross-module state leaking into the shutdown report, rather than a defect in the shutdown logic. **Not introduced by #1324**, which added one integration test plus doc/register edits; the pair (b72 integration module + `test_shutdown.py`) passes together 90/90.
- **✅ CLOSED 2026-08-12 (PR #1327) — root-caused and fixed, with the repro this row demanded.** **Not state leakage**, as first hypothesised: a **timing race on the shared flush budget**. `flush_observability` gives the ledger fsync only what is LEFT of the budget (`deadline - time.monotonic()`, `shutdown.py:943-947`), so with the test's tight 100 ms total a loaded runner's real fsync overran the remainder, raised `TimeoutError`, and appended `"ledger"` — turning `failures == ()` red for a reason unrelated to the tracer behaviour under test. **Deterministic repro:** a fast False tracer plus a 500 ms fsync reproduces the EXACT CI shape every time; slowing the *tracer* instead does **not**, because the tracer flush is bounded too and tags itself. **Fix:** widen that test's budget to 5 000 ms — the assertion stays EXACT (`failures == ()`), so a genuine tracer-path regression still fails; only the timing dependence is removed. **Not a retry marker**, per this row's own warning. Plus a new mechanism witness (`test_slow_ledger_fsync_is_tagged_and_times_out`) pinning that an overrunning fsync IS tagged — mutation-probed. **No production change:** the shared-budget behaviour is correct by design and already covered by `test_flush_surfaces_share_one_timeout_budget`.
- **Close-out steps** *(historical — discharged above)*. **Reproduce deterministically FIRST** — the value is the repro, not a retry. Run the all-axis suite with a fixed seed and bisect the module order to find which earlier test leaves a ledger-flush failure visible to the shutdown report; `-p no:randomly` did NOT reproduce locally, so the ordering that matters may be CI-specific (worker count / package interleave). Then isolate the shutdown report per-test, or reset whatever global the leaking module mutates.
- **Do NOT paper over it with a retry marker.** A shutdown-report assertion that intermittently sees a foreign failure is exactly the signal a real drain regression would produce; weakening it would blind a genuine defect.
- **Council: no** — a test-isolation defect, not a design fork.
### B-162 · `C-OD-30.3`'s two declared `head=1.0` spans (`pause.captured` / `resume.attempted`) have emitters that **nothing in `src/` calls**, so neither span is ever emitted in production *(surfaced while re-measuring B-137 at the real venue, 2026-08-13; the overclaim it corrects was caught by out-of-family Codex)*

- **✅ CLOSED 2026-08-13 (PR #1330) — WIRED, not amended.** Grounding settled the live-vs-amend question decisively: **U-CP-65's ACs require the spans to EMIT** (AC #1, #2) and require an **integration test verifying emission via an OTel collector** (AC #5) — so this was an **incomplete unit landing**, not a contract ambiguity, and no back-flow was needed. `workflow_driver.py` now calls `_emit_pause_captured` after every `protocol.capture_pause_snapshot` (**11 sites** across the 6 topology executors) and `_emit_resume_attempted` after the entry-point `protocol.attempt_resume` (1 site; the three `_engine_recovery_loop.attempt_resume` calls are a different, engine-layer function and are out of scope). The resume wrapper honours the **corruption carve-out** the emitter's own docstring prescribes. **Witness:** `harness-runtime/tests/integration/test_b162_pause_resume_span_emission_is_wired.py` — 6 tests driving the shipped `api.run` **and** `resume()` entry points, asserting the **exact** 4-attribute canonical sets plus per-site AST coverage so a partial wiring cannot pass; **mutation-probed** against the pre-wiring driver (all 6 RED). The B-137 witness's own no-caller tripwire fired on this change exactly as designed and is now **inverted** to guard the wiring. **Scope:** emitted *inside* `workflow.envelope`, so the §9.2 floor remains subject to **B-137**'s root-only starvation — AC conformance, **not** a sampling fix.
- **What it is.** `harness-cp/src/harness_cp/pause_resume_protocol.py:904` and `:925` define `emit_pause_captured_span` / `emit_resume_attempted_span`, each opening the span its C-OD-30.3 contract declares at `head=1.0`. **No call site exists anywhere under `harness-*/src/`** — the only matches are the definitions themselves and a docstring cross-reference at `per_server_trust_evaluator.py:294`. Both docstrings describe a *"caller-side invocation per the U-CP-61 pattern"* in which *"workflow driver invokes `protocol.capture_pause_snapshot(...)`, then this helper with the returned snapshot + driver-held tracer"* — the driver performs the first half and **not** the second.
- **Why it matters.** Two of the four unconditional names in **B-160**'s disposition (a) are these. Adding a never-emitted name to the §9.2 floor set changes nothing at all, so B-160's four names are **not symmetric** and its close-out should not treat them as one sweep. It is also a straightforward contract-vs-implementation gap on its own terms: a contract declares a span always-sampled and the span does not exist at runtime.
- **How it was found, and the discipline it illustrates.** An earlier draft of the B-137 arc used `emit_pause_captured_span` as its *"real production emitter"*, driving it directly. The function is real; the **path** is not. Out-of-family Codex rejected the witness on exactly that ground — a green test over a function no shipped code reaches is presence, not behaviour. `[[verification-shape-sharpened-grep-vs-e2e]]`
- **Witness.** `harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py::test_the_pause_span_emitters_have_no_caller_in_src` — enumerates `src/` for callers of both helpers and fails if either acquires one, so a later wiring cannot silently invalidate B-160's grounding.
- **The two spans are NOT symmetric — they land on OPPOSITE sides of the envelope (out-of-family Codex round 2).** A first draft of this row gated both on `B-137`. That holds for `pause.captured` (an envelope child) and **fails for `resume.attempted`**: the driver runs entry-point resume detection at `workflow_driver.py:3213-3225`, **before** `workflow.envelope` opens at `:3305`, and says so in-line — *"The resume detection runs BEFORE the workflow.envelope opens — a failed resume (corruption or diff-aborted) returns FAILED without opening a new envelope."* So a `resume.attempted` span at its prescribed call site would be a **root**, would consult the composite sampler directly, and **would** receive its §9.2 floor once added to the set. That makes `resume.attempted` **the one name among B-160's four for which membership alone is sufficient** — after wiring. Pinned by `…test_b137_ninety_two_floor_at_the_real_run_venue.py::test_the_two_uncalled_spans_land_on_opposite_sides_of_the_envelope`. **SCOPED TO TOP-LEVEL RESUMES 2026-08-13 (codex round 7).** The root claim above generalized *local line ordering inside one driver function* into *trace-root status*, which is valid only for a **top-level** resume. For a **nested child** resume it is false: `sub_agent_dispatch.py` opens `subagent.span` and invokes `child_workflow_runner(..., pause_snapshot_input=...)` **inside** that block, and the child runner forwards the snapshot to `execute_workflow` — so the child driver's entry-point resume detection runs while `subagent.span` is current. `resume.attempted` would then be a **non-root child**, and it stays gated on **B-137** exactly like `pause.captured`. *"Membership alone is sufficient"* holds for **top-level resumes only**. Pinned by `test_a_nested_child_resume_runs_under_subagent_span`.
- **Close-out steps.** **(1)** Decide whether C-OD-30.3 intends these spans to be live. If yes, wire the driver's pause and resume branches to call them with the driver-held tracer, per the pattern both docstrings already specify, and add emission witnesses. If no, the contract's `head=1.0` declaration is the thing to amend, via design-phase back-flow. **(2)** Either way, re-word **B-160**'s disposition (a) so its four names are no longer presented as a single symmetric sweep. **(3)** ~~Note that wiring them does not by itself deliver their floor — they would be emitted inside `workflow.envelope` and so land in B-137's root-only starvation.~~ **SUPERSEDED 2026-08-13 — true for `pause.captured` ONLY.** `pause.captured` would be an envelope child and so lands in B-137's root-only starvation; `resume.attempted` would be emitted **before** the envelope opens and so would be a **root**, receiving its §9.2 floor directly once added to the set. **Do not block the top-level resume slice on B-137** — but the **nested** child resume still needs it (the child driver's resume detection runs while `subagent.span` is current), so B-162's wiring can land independently while **complete** conformance for `resume.attempted` still depends on B-137.
- **Council: no.** A wiring gap plus a contract-intent question, single-domain; it becomes design-phase back-flow only if the answer to step (1) is that the contract should be amended.
- **Cross-ref.** `B-160` (two of its four names are these); `B-137` (the floor they would land under is root-only); `C-OD-30.3`; U-CP-65.
### B-163 · `TailKeepSpanProcessor` mutates `_buffer` / `_keep` / its counters with **no lock**, so a concurrent child-`on_end` and root-`on_end` can lose a span entirely — pre-existing at all three materialization sites *(surfaced by out-of-family Codex at the B-136 arc, 2026-08-13)*

- **✅ CLOSED 2026-08-13 (PR #1331) — FIXED in the same PR that surfaced it**, because that PR **widened the reachable surface**: the race pre-exists at `:478`/`:437`, but before B-136 the name arm never materialized, so B-136 made it newly reachable for name-matched roots — and widening a known data-loss path without guarding it is not acceptable scoping (out-of-family Codex escalated to **P1** on exactly that ground). **The fix:** one `threading.RLock` guarding every `_buffer` / `_keep` / counter mutation. **Scope discipline held:** the lock is **never** held across `downstream.on_end` — `_materialize_trace_decision` detaches the bucket under the lock and forwards outside it — so a slow exporter cannot serialize every span. The eviction test, the `setdefault` and the `append` are now **one** critical section. **Witness:** a *coordinated* interleaving (hooked `setdefault` blocks the child at the exact pause point while the root attempts its pop), not a stress loop; **mutation-probed** by swapping the RLock for `contextlib.nullcontext()` — the child span is lost every run. Close-out step (1) (reachability) is therefore **answered by construction**: the losing order is reproducible on demand, so this was a **live** defect, not a latent one.
- **Round-2 hardening + the residual, stated** *(codex round 4, P1)*. The first lock pass guarded each mutation separately, which was **not enough**: the keep write and the root materialization sat in **different** critical sections, so a thread could leave **torn state** — a `_keep` entry written against a trace whose buffer another thread had already popped, which nothing would ever pop. All three arms now go through **one indivisible transition**, `_publish_keep_and_maybe_detach`, which publishes the keep flag and detaches the trace under a single lock acquisition and returns the spans for the caller to forward **outside** the lock. The now-orphaned `_materialize_trace_decision` was removed. **RESIDUAL — not closed, and not claimed:** mutual exclusion fixes *torn state*; it cannot fix *ordering*. If a root's `on_end` runs to completion before a child's `on_end` begins, the trace materializes before that child is accounted for, and no lock discipline inside `on_end` can change it. Reaching that requires a **root to end before its own child**, violating the span-nesting contract OTel and this processor's root-close design assume — a malformed-trace shape, not a concurrency bug. An earlier draft of the witness asserted the ordering case and failed, which is what surfaced the distinction; the committed witness asserts what **is** guaranteed.
- **Round-3 hardening** *(codex round 5, P1)*. The state transition now runs **before** `downstream.on_end(span)` in both always-sampled arms. Forwarding never depended on the keep flag, and a **slow downstream** otherwise widened the publication window arbitrarily — long enough for a concurrent root close to detach the trace with `keep=False`, dropping the buffered siblings and leaving a stale `_keep` entry. **Crucially this window opened for a WELL-FORMED trace**, so unlike the ordering residual it is **closed**, not documented. The witness drives Codex's exact shape — a buffered ordinary child, a `sandbox.violation` trigger **blocked inside downstream**, a quiet always-sampled root closing concurrently — and is mutation-probed by moving the forward back ahead of the transition (the sibling is dropped every run).
- **Scope of this closure, final.** B-163 is closed for the three windows a lock **can** close, each with a deterministic mutation-probed witness. Codex rounds 4–6 then surfaced two further items that are **not lock-shaped** — `force_flush` returning before an in-flight detached batch reaches downstream, and a child paused before it registers — and those are registered as **B-164** rather than patched reactively here. That is **arc-sizing, not deferral**: the first needs in-flight batch accounting that changes shutdown semantics, and the second cannot be closed by any lock.
- **What it is.** OTel's `SpanProcessor.on_end` is called from whichever thread ends the span, so the SDK contract requires it to be thread-safe. `TailKeepSpanProcessor` holds `self._buffer` (dict of trace_id → list), `self._keep`, `self._dropped_trace_count` and `self._dropped_span_count` and guards **none** of them — there is no `threading.Lock` anywhere in the module and no docstring claim of single-threaded use.
- **The losing interleaving, as Codex stated it.** A child `on_end` executes `bucket = self._buffer.setdefault(trace_id, [])`, creating an empty list, and is descheduled before `bucket.append(span)`. A root `on_end` on another thread then runs `_materialize_trace_decision(trace_id)`, whose first act is `self._buffer.pop(trace_id, [])` — it pops the empty list and forwards nothing. The child resumes and appends to a now-detached list. The span reaches **neither** `downstream` **nor** `_buffer`, so even `force_flush` cannot recover it. When the root is a §10.2 trigger such as `sandbox.violation`, the lost span is context the keep flag was supposed to preserve.
- **PRE-EXISTING, and not introduced by `B-136`.** The identical shape sits at **all three** materialization sites: `:478` (the original ordinary-root close), `:437` (the `B-133` event arm) and `:391` (the `B-136` name arm). B-136 widened the exposed population — every always-sampled root now materializes — but the unguarded `pop`-vs-`setdefault` window is the module's from the start. Grounded at the B-136 arc: `grep` for `Lock|threading` across the module returns **nothing**.
- **Close-out steps.** **(1) Establish reachability before sizing the fix** — the harness runtime is predominantly single-event-loop, so the question is which real paths end spans off that thread (`BatchSpanProcessor`'s worker, shutdown/atexit drains, any thread-pool sub-agent dispatch). A race that no shipped path can reach is a latent contract violation rather than a live defect, and that distinction sets the priority. **(2) The fix is small and contained** — one `threading.Lock` (or `RLock`, since `_materialize_trace_decision` is called from inside `on_end`) guarding the buffer/keep mutations in `on_end`, `_materialize_trace_decision`, `_evict_oldest_trace` and `force_flush`. Hold it only around the dict work, never across `downstream.on_end`, or a slow exporter serialises every span. **(3) Witness with a coordinated interleaving**, not a stress loop: drive the exact setdefault-pause-pop-append order with an event or a monkeypatched `setdefault`, so the test is deterministic and actually fails without the lock.
- **What would falsify it.** A demonstration that `on_end` is only ever invoked from a single thread in every shipped composition (which would make it latent, not live), or a lock landing that makes the coordinated interleaving witness pass.
- **Council: no.** A single-domain **C7** thread-safety repair against an explicit OTel SDK contract, with no cross-axis surface and no committed envelope to renegotiate.
- **Cross-ref.** `B-136` (the arc that surfaced it and widened the exposed population); `B-133` (the event arm, second site); OD spec v1.28 §1.1 (the bounded-buffer materialization the sites implement).

### B-164 · `TailKeepSpanProcessor` concurrency — **(a) `force_flush` returning before an in-flight detached batch reaches downstream: CLOSED at PR #1332**; **(b) a child paused before it registers: OPEN — the PR #1334 unreachability basis was falsified at review** *(surfaced by out-of-family Codex rounds 4–6 at the B-136/B-163 arc, 2026-08-13)*

- **🔴 (b) REMAINS OPEN — the PR #1334 closure was WITHDRAWN at review.** Two bases were advanced for closing (b) and **both were falsified**. **Basis 1** (out-of-family Codex, P1): *production holds zero manual-lifetime spans* — false when written, and its test vacuous, because the scan filtered `ast.Assign` and so skipped `child: ChildSpanRef = tracer.start_span(...)`, an **annotated** assignment at `operator_burden_eval_primitives.py:252`. **Basis 2** (test-witness lens, P1): *exactly one `start_span` site exists and it has no caller, and every other site uses `with tracer.start_as_current_span(...)` whose lexical nesting forecloses a parent-ends-first ordering.* The **inventory** half is true and is kept as a maintained invariant (`test_b164b_the_only_manual_lifetime_span_is_uncalled`, matching at the **call** level and pinning both parts). What kills it as a **closure** is its unstated universal — *"the interleaving needs a span whose `end()` can be called out of order, **or one handed to another thread**; production has neither"* — because production has the second **by deliberate design**: `workflow_driver._run_fanout_to_completion` abandons its executor on ANY exception via `shutdown(wait=False)` and its own docstring says *"the orphaned thread runs to completion in the background"*; branch dispatch runs under `asyncio.to_thread`, which copies the `contextvars` context, so the OTel parent propagates into that thread and `llm_dispatch` opens a **real child span** there; the §25.11 barrier deadline then unwinds the `workflow.envelope` root **while that child is still open**. A sibling comment at the `to_thread` call site already records this orphaned-thread-outlives-cancellation shape as a prior codex [P1] + concurrency-lens finding, so the mechanism is independently attested in-tree. **CONFIRMED BY REPRO 2026-08-13** at `harness-runtime/tests/test_b164b_fanout_orphaned_child_span_repro.py`, which drives the REAL `_run_fanout_to_completion` (not a re-implementation) and is deterministic — 10/10 identical runs, because the failing branch waits on a child-span-open event rather than a sleep. The root ends first, the child is stranded in `_buffer`, and it never reaches downstream. **Measured cost, three distinct harms.** (1) The orphan misses the root-close materialization that exports its trace. (2) With no `max_buffered_traces` ceiling it is merely DELAYED to `force_flush`; under buffer pressure it is EVICTED (`dropped_trace_count=1`) and `force_flush` never recovers it — a permanent loss, and because the stranded bucket is the OLDEST entry it is the FIRST shed. (3) If the orphan is itself the §10.2 trigger, the keep decision is taken WITHOUT it — root and siblings DROP — and a `_keep` entry is left that no root close will ever pop. Note which span this is: the wedged branch's dispatch, i.e. exactly the one an operator wants when a branch hung badly enough to trip the barrier. **Next step is the FIX**, and the shape this row predicted stands — a post-materialization **late-arrival path**, not a wider lock. A fix must also decide what a LATE TRIGGER means (the keep decision is already published: honour it late, or accept the late trigger as lost) and must stop the `_keep` leak either way. The repro's three `xfail(strict=True)` witnesses flip to XPASS when the fix lands, which is the signal to close this row. **Precision correction + residual** (witness-adequacy lens, PR #1335, P2): the repro opens the child span DIRECTLY in the abandoned worker — the minimal faithful shape — but for every currently-wired dispatcher the span opens one hop further out. `SyncDispatcherFacade` (which opens no span itself) bridges back to a captured, persistent OUTER loop via `asyncio.run_coroutine_threadsafe`, so `RuntimeLLMDispatcher.dispatch`'s span runs on the outer loop's thread, not on the `to_thread` worker that is abandoned. The lens wired a REAL `SyncDispatcherFacade` and confirmed the same root-before-child ordering reproduces there, so reachability is unaffected — but the late-arrival window is gated by the outer loop's coroutine lifetime rather than literally by the abandoned executor, which a fix must account for; a facade-level witness is the natural next strengthening. The lens also ran the load-bearing **counterfactual**: swapping the abandon for joining semantics makes the ordering DISAPPEAR (child before root), proving `shutdown(wait=False)` — not the fixture — produces it. (a) is unaffected and stays CLOSED at #1332.
- **✅ (a) CLOSED 2026-08-13 (PR #1332) — `force_flush` now waits for in-flight batches.** **Step (1) grounded and answered: (a) IS in scope**, and the module's own contract settles it — `force_flush` is documented as existing to *"flush any still-buffered traces (treat them as keep-all to avoid silent loss on shutdown)"*, so returning before delivery defeats its stated purpose. The *"Drop semantics (false-but-bounded)"* posture does **not** cover it: that paragraph is about traces that never root-close, not spans already handed to the forwarder. **Step (2) built:** detached batches now go through `_forward_detached`, which registers them as in-flight (increment before, decrement in a `finally`, notify), so a concurrent `force_flush` waits for delivery instead of seeing an empty buffer. `downstream.on_end` is still called with **no** processor lock held — preserving the reason the forward was outside the lock at all. **The wait is bounded** by the caller's `timeout_millis`, with the remaining budget passed to the downstream flush; without that bound this would trade a silent-loss bug for a shutdown **hang**, which is strictly worse. **Witnesses (2, deterministic):** a slow downstream holding a detached sibling while another thread flushes (asserts it does *not* return), and a wedged exporter with a 300 ms budget (asserts it *does*). Mutation-probed: removing the wait reddens the first every run.
- **Round 2** *(codex on PR #1332)*. The **first** version registered the batch inside `_forward_detached` — *after* the detach had popped it and released the lock. A `force_flush` landing in that gap saw an **empty buffer AND zero in-flight**, delegated and returned, reproducing the very loss the fix targets. Registration now happens **inside the detaching critical section** (the condition shares `_state_lock`), and `force_flush` registers its own drained batch the same way. **On witness quality:** the original waits-witness could not tell the two versions apart — mutation-probing it against the gap **passed**, since it starts its flusher long before the window and only proves a *registered* batch is waited for. A second witness discriminates by construction: it suspends the producer at the entry to `_forward_detached` — exactly after detachment, before any post-hoc increment — and asserts the batch is **already** counted; that reddens deterministically. Both timing assumptions were also removed: the flush witness is now **order-based**, and the wedged-exporter witness waits on a producer **entry event** rather than a 100 ms sleep.
- **Round 3** *(codex on PR #1332)* — three more, all in code this arc introduced. **(i)** `force_flush` returned **True** on timeout expiry, telling the runtime shutdown may proceed while batches are demonstrably outstanding — a quieter form of this row's own bug; it now returns `downstream_ok and drained_in_time`, still delegating so a wedged forwarder cannot stop the downstream flushing what it has. **(ii)** The registration **leaked** if the current-span forward raised: the batch was already counted, so an exception from `downstream.on_end(span)` skipped the release and left the count positive **forever** — every later `force_flush` would burn its whole budget *and* report failure. Both arms now release in a `finally`. **(iii)** The flush witness was **still** scheduler-dependent — an event set just before calling `force_flush` does not prove the flusher reached the wait. It now hooks the condition's `wait()` and synchronizes on a point **inside** it; mutation-probed **3/3 red** with the wait removed, where the previous version passed. *Incidental:* the SDK **propagates** processor exceptions rather than swallowing them.
- **Round 4** *(codex on PR #1332)* — **a reporting failure of mine**, recorded as the lesson. Round 3 claimed the flush witness had been switched to synchronize **inside** the condition wait. It had not: the edit used a `str.replace` with **no assertion on the anchor**, the anchor did not match, the replace silently no-op'd, and the script still printed success — so the committed file kept the old `set-event` + `sleep(0.2)` shape while the commit message said otherwise. Codex caught it by **re-reading the committed file** instead of trusting the message. Now applied for real, verified present by grep, the edit assertion-guarded, and mutation-probed **3/3 red** with the wait removed. **Discipline:** never report an edit from a script's own success print — assert the anchor matched, then grep the committed file.
- **Round 5** *(codex on PR #1332)*. The batch counter alone cannot cover a span whose `on_end` has **started** but not yet taken `_state_lock` — at that moment **no batch exists to register**, so `force_flush` could observe zero, enter the downstream flush, and only then have the child buffer and its root detach a batch: registration landing **after** the only wait, and the flush returning `True` with a batch undelivered. `on_end` now counts its own **entry and exit** (`_active_on_end`, incremented at the top, decremented in a `finally`, notified) and `force_flush` waits for **both** counters; the body moved to `_on_end_inner` so the accounting wraps every path including early returns. Witness suspends the producer **inside** `on_end` before any buffering and asserts `force_flush` parks — mutation-probed **3/3 red** with the entry counter dropped from the condition.
- **Round 6** *(codex on PR #1332)* — **the single drain pass was itself unsound**, and this is the structural end of (a). The counters fix two windows but not a third: a non-root span that entered `on_end` **before** the drain and buffers **after** it leaves the span in `_buffer` with **both counters back at zero**, so the wait exits and `force_flush` returns `True` with the span buffered and never delivered. `force_flush` is now a **drain-until-stable** loop — drain, forward, wait for the counters to settle, re-check the buffer, repeat until a pass finds nothing new — still bounded by the caller's deadline and reporting failure if it exits with work outstanding. All three windows are named together in the code comment ((i) detached-not-forwarded, (ii) entered-not-registered, (iii) buffered-after-the-pass) so the next reader sees why one pass cannot work. Witness suspends the producer inside `on_end` so the first drain sees an **empty** buffer and releases it only once the flusher is parked — mutation-probed **3/3 red** at one pass.
- **Round 7** *(codex on PR #1332)* — **the entry counter needed its own lock.** Guarding it with `_state_lock` (which `_inflight_cv` shares) reintroduced the same hole one level down: a callback reaching `on_end` while `force_flush` held that lock blocked **before** being counted, so the flusher could release, immediately reacquire for its zero check, observe nothing and return — after which the queued callback buffered an already-ended span. `_active_on_end` now lives under a dedicated `_entry_lock`, held for two integer operations only and read via `_entrants_pending()`, which takes **only** that lock so no nesting can deadlock. Witness holds `_state_lock` in the test and asserts a span ending on another thread is **already counted** while provably unable to proceed (buffer still empty) — mutation-probed **3/3 red** with the entry counted under `_state_lock`.
- **Round 8** *(codex on PR #1332, P2 — severity dropping)*. Waiting on a bare *any-entrant-active* predicate is **over-conservative**: it includes callbacks that started **after** the flush began, so under sustained tracing the count never reaches zero, `force_flush` burns its whole budget and reports **failure** even though everything present at invocation drained — a spurious shutdown error rather than a loss. Entrants now carry a monotonic sequence number and each flush snapshots an `_entry_cutoff()`, waiting only for entrants at or below it. The bare counter became an `_active_entries` set; the dead `_active_on_end` field and its stale comment reference were removed. Witness takes a cutoff while idle, starts a **late** span inside `on_end`, and asserts it **is** registered globally but is **not** waited for by the earlier cutoff — mutation-probed **3/3 red** with the cutoff ignored.
- **Round 9** *(codex on PR #1332, P2)* — **the symmetric half of round 8.** The cutoff filtered entrants but the batch count stayed global, so a batch detached by a **post-cutoff** callback still made the flush wait: the same spurious-`False` shape one level down. Batches now carry the generation of the `on_end` that produced them (via a thread-local current-entry id, so no signature threading), and a flush waits only for batches at or below **its own** cutoff; `force_flush`'s own drained batch is tagged `0` — at or below every cutoff — so a flush always waits for itself. **Witness construction mattered:** a first draft started the late trace *before* calling `force_flush`, which makes it legitimately **pre**-cutoff work the flush *should* wait for — the test failed for the right reason and the code was fine. It now hooks `_entry_cutoff` and creates the late trace from inside it, and exercises `force_flush()` end-to-end as the reviewer asked rather than only the predicate. Mutation-probed **3/3 red**.
- **Remaining scope = (b) only.** *(The PR #1334 supersession was itself WITHDRAWN — (b) is open again; this statement is LIVE.)* A child descheduled **before** it reaches the buffering critical section cannot be registered by any lock, so a concurrent always-sampled root can materialize first and the child is stranded or evicted. Moving the lock ahead of classification **narrows** but cannot close it, and reaching it requires a **root to end before its own child** — violating the span-nesting contract the root-close design assumes. Step (3)'s question stands: is a malformed trace shape worth defending against at all, and if so the honest mechanism is a post-materialization late-arrival path, **not** a wider lock.
- **Why this is a separate row and not more of `B-163`.** B-163 closed the windows that a lock CAN close, each with a deterministic witness: the `pop`-vs-`setdefault` interleaving, the torn keep-write-vs-materialize transition, and the slow-downstream publication window. What remains is not another missing `with` statement — it is a **design** question about batch accounting and shutdown ordering, reached after four consecutive review rounds on a component whose defect predates the arc that surfaced it. Sizing it as its own arc is the point of this row.
- **(a) — HISTORY, CLOSED at PR #1332** *(the original statement, retained; see the CLOSED bullets above for what was built)*. ~~`force_flush` does not wait for in-flight detached batches.~~ `_publish_keep_and_maybe_detach` detaches a trace's spans under the lock and returns them so the caller can forward **outside** the lock — deliberately, because holding the lock across `downstream.on_end` would serialize a slow exporter behind every span. But a concurrent `force_flush()` then sees an **empty** buffer, delegates downstream and returns `True` while those already-ended spans are still being forwarded. Shutdown can close the downstream first and lose them. **Pre-existing:** the superseded `_materialize_trace_decision` had the same pop-then-forward shape, so `force_flush` could always miss an in-flight batch. **Shape of a fix:** an in-flight batch counter plus a condition variable that `force_flush` waits on, or an explicit hand-off queue — both change shutdown semantics and need their own witnesses.
- **(b) A child paused BEFORE registering cannot be registered.** If a child's `on_end` is descheduled before it reaches the buffering critical section, a concurrent always-sampled root can materialize the trace first; the child then re-creates the bucket after root close and is stranded until `force_flush`, or evicted under `max_buffered_traces` pressure. Moving the lock earlier (before classification) **narrows** the window but cannot close it — a span that has not entered the processor cannot be accounted for. This is the **ordering** half of the residual already recorded on B-163, restated here with its eviction consequence. Note it requires a root to end before one of its own children, which violates the span-nesting contract the root-close design assumes.
- **Close-out steps** *(step (1) ANSWERED and step (2) BUILT for (a) at PR #1332 — do not re-run them; step (3) ANSWERED for (b) at PR #1334)*. **(1) Decide whether (a) is in scope for a best-effort tail processor at all** — the module's own *"Drop semantics (false-but-bounded)"* posture and §9.3's implementer-discretion clause may already contemplate it; ground there first, exactly as B-136's step (5) did. **(2) If it is, implement in-flight accounting** and witness the shutdown race directly (a slow downstream plus a concurrent `force_flush`, asserting delivery before return). **(3) For (b), decide whether a malformed trace shape is worth defending against at all** — if yes, the honest mechanism is a post-materialization late-arrival path, not a wider lock.
- **What would falsify it.** A demonstration that `force_flush` is only ever called after all span-producing threads have quiesced (making (a) unreachable), or a ratified reading that best-effort delivery at shutdown is the contracted behaviour.
- **Council: no.** Single-domain **C7** concurrency-correctness work inside §9.3's implementer-discretion clause, with no cross-axis surface.
- **Cross-ref.** `B-163` (the windows a lock closed, and the ordering residual restated at (b)); `B-136` (the arc that surfaced the whole surface); OD spec v1.28 §1.1 + C-OD-09 §9.3.


### B-165 · GROUNDED — two same-position placements collide onto ONE audit identity, and Runtime spec §14.8.7 NOTE 6-i asserts the opposite *(registered UNVERIFIED at the `B-71` spec leg PR 2026-08-13; CONFIRMED BY EXECUTION at the grounding leg 2026-08-14 and routed to Class 1 back-flow — the fork filing, not code, is the deliverable the row predicted)*

- **What it is.** The premise was registered UNVERIFIED and is now CONFIRMED BY EXECUTION (grounding leg, 2026-08-14). Answers to the row's own (a)/(b)/(c): (a) on the durable-async webhook venue the collision is NOT reachable within one pass -- with TWO same-position placements declared, exactly ONE escalation is entered, because _escalate_to_secondary_channel is NoReturn; witnessed with a POSITIVE control (a counter reading exactly 1), since the durable-async path does not use the record's ledger writer and 'zero writes observed' could not otherwise be told apart from 'looked in the wrong place'. (b) YES -- the SYNC-BLOCKING loop mints for BOTH placements: each is gated independently and each reaches an audit write -- via s14.8.2 step 4h on an ASK-tier gate, and via the policy AUTO-APPROVAL branch (before the step-4j skip continue) on a READ_ONLY solo gate, which never reaches 4h at all. (c) the key is BYTE-IDENTICAL including the B-71 token, because both placements share parent, position and branch -- every input the basis hashes. The reachability chain is witnessed link by link: WorkflowManifestEntry has NO validator rejecting the pair (a full model_validate accepts it); fold_step_hitl_placements preserves both and disclaims de-duplication as 'a workflow-validation concern, out of scope here' (workflow_driver_types.py:753-756) -- an owner that does not exist anywhere. NOT a B-71 regression: the two-argument key already collided pre-arc, and the witness asserts BOTH halves (peer BRANCHES separate correctly; peer PLACEMENTS do not). THE DEFECT IS IN THE SPEC, not the code: Spec_Harness_Runtime_v1.md:4864 NOTE 6-i asserts 'Each placement's audit entry uses a distinct action_id' and that sibling-distinguishability 'is preserved', justifying it by 'action_id includes placement.position.value' -- which holds only when the positions DIFFER, as in its own PRE_ACTION + SUB_AGENT_BOUNDARY example. COST: the composed key is also the F2 idempotency_key, and append_ledger_entry dedups key-only (state_ledger_write.py:330), so the second placement's entry is an IDEMPOTENT_NOOP and is DROPPED -- a silent omission of oversight evidence, not a noisy duplicate.

- **Current state.** GROUNDED and CONFIRMED — the premise held, on the sync venue. What remains is not investigation but an operator decision among three named repairs on a ratified record. Filed as Class 1 back-flow at `.harness/class_1_fork_b165_same_position_placement_identity_collision.md`; witnessed at `harness-runtime/tests/test_b165_same_position_placement_identity_witness.py`, `harness-cp/tests/test_b165_production_feed_duplicate_placements.py`, and the unified round trip at `harness-runtime/tests/integration/test_b165_driver_through_composer_round_trip.py`. The Phase-7 side is closed and NO design-substrate edit was made — the fork filing is the deliverable, exactly as this row predicted it would be.

- **Close-out steps.** GROUNDING IS DONE; the row now awaits an operator decision on a committed surface. Filed as Class 1 back-flow at .harness/class_1_fork_b165_same_position_placement_identity_collision.md; witnessed by harness-runtime/tests/test_b165_same_position_placement_identity_witness.py (13 test functions / 16 parametrized nodes across three modules, 13/13 mutation-proven -- each annotation applied literally and restored from a file backup, since the comment-out driver cannot apply additive mutations). THREE candidate repairs, recommendation (A): (A) narrow NOTE 6-i so the distinctness claim is scoped to DISTINCT positions and the same-position collision is stated as a known bound -- zero blast radius, makes the spec true, leaves (B) available later; (B) widen the ratified CP v1.119 s0.4(2) basis with a per-declaration ordinal -- re-opens the s0.12 byte-identity criterion for the whole linear/validator population and should NOT be taken speculatively for a shape no in-tree workflow declares; (C) create the missing workflow-validation layer and refuse the shape -- a new committed invariant. RESIDUAL, STATED NOT HIDDEN: the ACROSS-PASSES half of (a) (re-fire / resume / retry recurring the same branch and position) was NOT executed this leg. It does not change the verdict -- (b) already confirms the collision on a live venue -- but it is unproven, and the durable-async resume path is known to dispatch the resumed step UNCHANGED, so anyone reopening this must execute that path rather than infer it. The Phase-7 side is CLOSED: the witness and the s2.3 comment correction landed; NO design-substrate edit was made, which is the outcome this row predicted.

- **Council.** No -- and the row's original reasoning held. Grounding was step 1 and it resolved the question without deliberation; what remains is an operator choice among three named repairs on a ratified record, which is a design-record revisit, not a new convening.


### B-166 · Wall-clock deadline witness `test_ow_proceed_deadline_stashed_child_completed_not_cancelled` — the near-boundary-race premise is REFUTED by a measured ~600x barrier headroom *(registered by the prevention arc preceding `U-CP-102`, 2026-08-13; premise refuted and row CLOSED at #1353, 2026-08-15)*

- **Disposition.** CLOSED at #1353. Step 1 was discharged BY EXECUTION and the row's own premise did not survive it. Per this row's falsifier clause the row is REWRITTEN, not patched; the still-real observation it was filed for is tracked separately at **`B-181`**.

- **What the measurement shows.** The premise was a near-boundary race: the test pins `_DEFAULT_FANOUT_BARRIER_DEADLINE_SECONDS` to `0.3` (`harness-cp/tests/test_workflow_driver_orchestrator_workers_fence_ledger.py:1103`), scripts worker-1 to block `1.2`s, and asserts `_branch_terminals(ledger) == {0: "completed", 1: "timed_out"}` — which requires worker-0's stash to land before the deadline, with nothing synchronizing the two. A deadline sweep driving the module's own helpers (5 runs per rung) deviates only at **deadline ≤ 0.0005 s**, against the pinned **0.3 s** — roughly **600x headroom**. The deviation SHAPE is exactly the predicted one (worker-0 flips to `timed_out`, giving `{0: 'timed_out', 1: 'timed_out'}`), so the mechanism's DIRECTION is confirmed while its quantitative premise is not: scheduler noise cannot cover a 600x gap.

- **Corroborating negatives.** **25 full-suite runs → 0 failures** — most of them 2-at-a-time through a second driver initially misdiagnosed as dead, which makes them a LOADED venue rather than the idle baseline first claimed. **20 isolated runs under 24 CPU spinners on 8 cores (3x oversubscription) → 0 failures.**

- **`[SUPERSEDED 2026-08-15]` the original filing's decisive claim.** It read: "against branch diffs containing ZERO Python files (PRs #1336 and #1337), so no arc under review can have caused it." That is FALSE for **#1336**, which changed `harness-od/src/harness_od/tail_keep_span_processor.py` and added `harness-runtime/tests/test_b164b_fanout_orphaned_child_span_repro.py`. Only #1337 is Python-free.

- **`[SUPERSEDED 2026-08-15]` the original mechanism + close-out steps.** The mechanism paragraph ("under full-suite interference worker-0 can be cut by the same deadline"), the "REGISTERED, not yet reproduced" state line, and close-out steps 1–3 are superseded by the measurement above. Step 2's prohibition still stands on its own terms and is why nothing was widened.

- **Why no repair.** Widening the threshold is forbidden by the original step 2. Adding synchronization machinery for a race that needs a 600x scheduler stall would re-arm precisely the flake class `B-177` just paid to remove (a host-calibrated wall-clock bound in the blocking suite). The masking risk the row was opened for has also passed: the `B-71` impl leg landed at `0af632f4` (2026-08-14), neither it nor `2c9094c6` touched the barrier/stash/deadline lines, and the test file is byte-unchanged since 2026-08-12.

- **Council.** No. The question was empirical and is now answered by measurement.

### B-167 · `just leg-selfcheck`'s count check compares CLAIMS against each other, never against GROUND TRUTH, so a mirror in an untouched file is invisible *(registered at the prevention arc that precedes `U-CP-102`, from out-of-family review round 1 [P1], 2026-08-13)*

- **What it is.** `tools/leg_selfcheck.py`'s count check detects **disagreement** between count claims; it does not **recount**. Out-of-family review raised this as a `[P1]` against the arc's own brief, which asked for cross-checking each claim against the artifact it describes *and* against every mirror (spec preamble, section heading, body, plan deltas, clearance markers, `artifact-pointers/*.md`, `forward-register.yaml`, the prose register).

- **What landed.** Added-line scanning **plus diff CONTEXT lines**, so a single edited mirror can still disagree with an untouched co-located one (a section preamble two lines above the body being edited). This is the cheapest sound widening: no extra file reads, no whole-file noise.

- **What did not.** A mirror in a file the arc never touched, or beyond the diff's context radius, is still invisible — and no claim is ever checked against a *derived* ground-truth count.

- **Why not — soundness, not cost.** Deriving a ground-truth count for an arbitrary noun in an arbitrary artifact is not mechanizable in general. The obvious cheap approximation — scanning the FULL TEXT of touched files — was considered and rejected: aggregate carriers (the 165-row prose register, `roadmap_status.md`) carry unrelated counts of the same nouns across unrelated rows, so it would produce constant false disagreements. **A gate that fires on everything is worse than no gate**, because it gets muted, and muting costs every check rather than one.

- **Current state.** REGISTERED as a known, stated bound. The tool's own docstring says plainly what it does and does not check, so no reader is misled into treating a clean count run as proof the mirrors agree.

- **Close-out steps.** **Step 1 — establish BY EXECUTION whether a format-scoped recount is even buildable.** The brief supplied two concrete recipes: `grep -c "^[0-9]\+\. "` for acceptance criteria in a plan unit block, and `grep -o "Criteria [0-9, and]*"` for mutation probes. Both presuppose the per-axis plan's structure (a numbered list under a `U-XXX-NN` heading). Run them against the current plan heads for a unit whose count is already known — if they do not return the right number, the recount is not buildable as specified and this row closes as a declared bound. **Step 2 — if they do,** scope the recount to exactly those two nouns in exactly the plan/spec artifacts, never as a general free-text facility, and make the ground-truth comparison HARD only where the derivation is exact. **Step 3 — measure false positives** against a sample of already-merged arcs before wiring it into the pre-push path; a gate is only as useful as its precision. **Falsifier:** a demonstration that the format-scoped recount cannot be made exact on the real artifacts — in which case claims-versus-claims plus diff context IS the sound ceiling, and the docstring already states it correctly.


- **STEP 3 ATTEMPTED 2026-08-16 (#1376) — NOT COMPLETE, and the row stays OPEN. What landed is an OBSTACLE CENSUS, not a precision figure.** Step 3 asks for a false-positive measurement *"before wiring it into the pre-push path; a gate is only as useful as its precision."* FOUR attempts produced **68%, then 40%, then 62%, then 61%**, each after out-of-family review found a different methodological flaw — and the third round showed the **classification behind the last figure was itself unsupported**. Reporting a fourth number would be guessing with extra steps, so the rate is **withdrawn** (`[[reviewer-oscillation-register-and-hold]]`). **What survives is the census of obstacles, each observed in this corpus and each pinned by a test at `tools/ac_claim_precision.py` (12 tests):** *(1)* **identifier digits read as counts** — `U-CP-56 acceptance criteria` scored as a claim of *56*; fixing it moved 68% → 40%. *(2)* **attribution by proximity** — on a line naming several units, nearest-left picks the cited dependency and manufactures a disagreement. *(3)* **self-reference** — the register paragraph recording a result quotes the corpus examples it cites, so the figure came to depend on having published it. *(4)* **REVISION COLLAPSE, UNSOLVED** — this corpus keeps every delta revision, so a unit-id key collapses them (`U-AS-06` derives `{6, 8}`), letting a **stale** claim score as agreement while `U-RT-106`'s v2.32 claims are compared against a v2.31 body; neither models what a gate would have seen at merge time. *(5)* **COUNT SEMANTICS, UNSOLVED** — `+1 AC`, `ZERO acceptance criterion change`, `STRUCK 4 ACs` and `AC #1` are deltas and references, not total-count claims, and the crude `>2 numbers` heuristic cannot tell them from a genuine live-versus-declared line. *(6)* **carrier population** — `check_counts` treats `.md`/`.yaml`/`.yml` as eligible; the scan reads `.md` only. *(7)* **BLOCK SEGMENTATION — the ground truth itself was wrong.** A `#` inside a fenced code block is a shell comment, not a heading; the corpus carries **twelve** such lines naming a unit, each creating a false block boundary that attributes one unit's criteria to another, and `U-CP-00b` was read as `U-CP-00`. **Fixing it moved the STEP-1 figure already published at #1368 — 386/285 → 388/287 — corrected here in every carrier.** **WHAT A SOUND STEP 3 REQUIRES, stated so the next attempt does not repeat this:** replay each merged arc against its **then-current plan head** (or preserve revision identity); **validate count semantics** before classifying any firing; and use the **gate's own carrier eligibility**. That is substantial work, and it is what step 3 actually costs. **NO refutation of the gate is claimed and no endorsement either — the honest state is *not yet measured*.** The recount remains a **derivation utility** and remains **UNWIRED**; `leg_selfcheck`'s claims-versus-claims check plus diff context remains the shipped behaviour, exactly as its docstring says.
- **Council.** No. A precision/soundness question with an executable step 1; the recall-vs-false-positive trade-off is settled by measurement, not deliberation.
- **STEP 1 EXECUTED 2026-08-16 — the falsifier is NOT met: a format-scoped recount IS buildable, and it is now built (unwired).** Step 1 asked whether the two supplied recipes return the right number on the real artifacts. **Measured across every `Implementation_Plan_*.md` in `design-substrate/` — 386 unit blocks carrying an acceptance-criteria section:** **287 are DERIVABLE** (unqualified header + a contiguous 1..N list, no top-level bullets) where the recount is exact; **101 are NOT** — amendment/additions blocks that renumber into a **parent unit's** space (one observed list starts at **`0`**) or carry no numbered list at all, so recounting them would produce a confident wrong answer. The discriminator is mechanical: a **parenthetical qualifier** on the header (`(v2.41 additions):`, `(v2.4 amendment):`) plus non-contiguous-from-1 numbering. **The supplied AC recipe is exact where it applies** — `U-CP-102`, the unit this row was registered against, recounts to **16**, corroborated inside its own block by the mutation-probe line's highest referenced index (16). **A caution the measurement itself produced:** in-block corroboration is nearly absent — only **8 of the derivable** blocks reference their own criteria numbers — and all 8 agree **once references to ANOTHER plan's unit are excluded**. The single apparent contradiction (`U-RT-147`) cites `CP plan v2.41 U-CP-45`'s criterion, not its own; a naive corroboration check would have mis-scored it. **So the recount is NOT self-checking** — its value is exactly what this row wanted: it derives a **ground-truth number an EXTERNAL claim can be checked against**. **BOTH supplied recipes are now implemented and tested** (out-of-family Codex round 1 [P2] correctly caught that a first pass executed only the acceptance-criteria half while the register claimed step 1 complete). **What landed:** `derive_acceptance_criteria_count` — a count only for a fresh enumeration, **`None` — never `0`** — otherwise; and `derive_mutation_probe_count`, which reads the obligations line's criteria set (`U-CP-102` → **9**: criteria 4, 6, 7, 10, 11, 12, 14, 15, 16), de-duplicated, and scoped to the obligations SECTION so unrelated prose cannot inflate it — the same cross-reference trap that mis-scored `U-RT-147`. **One more exactness bound, also from review:** a **MIXED** section — a numbered list PLUS top-level bullet criteria — returns `None`, because counting the numbered items alone is a confidently INCOMPLETE ground truth. The corpus really carries this shape (`U-RT-138` is 4 numbered + 1 bullet, `U-RT-141` is 6 + 2) and a first draft returned 4 and 6. 17 tests. **DELIBERATELY NOT WIRED into any gate:** step 3 owes a false-positive measurement against already-merged arcs first, and this row's own reasoning is that *a gate that fires on everything is worse than no gate*.
- **A dead test file found and revived in passing.** `tools/test_leg_selfcheck.py` was **doubly dead**: it lacked the `sys.path.insert` every wired sibling carries (so it raised `ModuleNotFoundError` on collection) **and** it was absent from `tools/codex-parity-check.sh`, so nothing ran it — the `[[tools-tests-need-explicit-ci-wiring]]` hazard, realised. Both are fixed and collection is **proven** (the parity set goes from **389 to 469** collected — exactly the **80** this file contributes). Its **63** pre-existing tests had never executed in CI; all pass, alongside **17** added here.

### B-168 · Archiving a superseded next-action round has no legal commit shape — three workspace constraints collide *(surfaced by out-of-family review rounds 11–13 of the prevention arc preceding `U-CP-102`, 2026-08-14)*

- **What it is.** A **trilemma**, each leg independently established and none of them introduced by this arc:
  - **(a) The archive is PRIOR-rounds-only by design.** Its own header states the current round lives only in the live head, because a one-file terminating refresh can never rotate the archive (codex round-6 resolution (b) at the U-CTX-03 arc). `test_actual_next_action_archive_exists_and_is_not_referenced_as_a_read_target` enforces it.
  - **(b) The terminating refresh may change EXACTLY `roadmap_status.md`** (CLAUDE.md §12.2.1), so the archive write cannot ride it.
  - **(c) `_owed_lag` tolerates exactly ONE non-refresh commit after a verified refresh** — it requires HEAD's *parent* to be that refresh — so an archive-only PR landing *after* the substantive content merge is a SECOND non-refresh commit and hard-fails `ROADMAP_STATUS_DRIFT` on `main`.

  Together: the archive write cannot ride the refresh (b), cannot follow the content merge (c), and cannot carry a still-current round (a) — which is exactly what riding the content PR would make it do.

- **Both live legs were verified, not argued.** (c) was reproduced against `codex_context_guard`; (a) was reproduced by actually running `--archive-current-next-action` inside the content PR and watching the enforcing test go RED.

- **Current state.** **RESOLVED 2026-08-14 by exit (iii)**, implemented at PR #1338 — archive round **N−1** (the most recent already-superseded round, read from the live head's git history) rather than round N. All three constraints hold **unchanged**, and **no governance amendment was needed**: exit (iii) is not a new convention, it is what the archive's own header has always specified. The defect was that the tool's first draft archived the *current* round. Shipped as `--archive-superseded`; the archive-before-replace precondition was removed because it silently encoded exit (i). Witnessed on the real repository: with post-#1337 live it selected post-#1336, wrote only the archive, left `roadmap_status.md` byte-identical, and the prior-only enforcing test stayed green. **Consequence, stated plainly:** the archive lags by exactly one arc by design; git history is the lossless record meanwhile.

- **Superseded framing (kept for provenance).** REGISTERED and **not** improvised around. What already exists and is correct *in isolation*: `--archive-current-next-action` writes only the archive and never touches `roadmap_status.md`; `--refresh --next-action` installs the pointer inside the single-file refresh and REFUSES if the outgoing round is not already archived. The unsettled part is purely **where** the archive step may legally run. Both `ship-pr` skills say so and tell the reader not to improvise it.

- **The record is NOT at risk.** The live head's own git history is lossless (`git log -p -- .harness/roadmap_status.md`), which makes this a choreography defect rather than a data-loss one.

- **Close-out steps.** **Step 1 — choose which leg to relax; this is an operator/architecture call, not a coding one,** because two of the three legs are governance: **(i)** relax (a), letting the archive hold the current round during the content-merge→refresh window — costs the prior-only invariant and needs its enforcing test rewritten *with a stated reason*; **(ii)** relax (c), widening `_owed_lag` to tolerate an archive-only commit specifically — a narrow, shape-verified exception in the style of the existing ones, **not** a general loosening; **(iii)** archive round *N−1* instead of *N*, reading the superseded text from the live head's git history exactly as the archive header already describes — preserves all three legs, needs a new `--archive-superseded --from-ref` mode, and means the newest round always lags by one arc. **Step 2 — amend the losing constraint AT ITS SOURCE** (the archive header, §12.2.1, or the guard), never at a call site. **Falsifier:** a demonstration that some fourth commit shape satisfies all three legs unchanged, in which case this closes as a documentation fix.

- **Council.** Possibly. Two legs are governance surfaces and the third is the drift guard, so this is a cross-domain trade rather than a coding preference. If the operator does not simply select a leg, a dyadic convening on process-invariant vs guard-precision is the right shape.

### B-169 · `test_loop_gc.sh` S16 STALE-boundary pin flakes on a loaded runner — a 29-minute fixture sits ONE minute from the 30-minute boundary *(observed on `main` at `be081e9d`, 2026-08-14; same commit passed on rerun)*

- **What it is.** A wall-clock boundary assertion with **one minute of headroom**. `tools/hooks/test_loop_gc.sh` case **S16** pins the unreconciled-subagent STALE boundary from both sides: it stamps a **29-minute** fixture (expected QUIET), asserts, then stamps a **31-minute** fixture and asserts exactly `1 unreconciled subagent`. Between those two assertions it runs real subprocess work (`sw_ctx` invokes the hook), so on a slow or loaded runner the 29-minute fixture crosses 30 minutes and the count becomes **2**.

- **Observed.** `main` CI at `be081e9d`: `FAIL: S16 31-min agent missed (STALE boundary drifted high): '[hygiene] 2 unreconciled subagent(s) (oldest: s16stale, 31m).'` Note the message blames the 31-minute agent, but the real deviation is that the **29-minute one was also counted**.

- **Evidence is same-sha differential** — the strongest available: a rerun of the **identical commit** passed 73/73 with zero code change.

- **Not caused by the arc that observed it.** PR #1338 touched **zero** files under `tools/hooks/`, the same job passed 18/18 on its head `6aeaeee3` minutes earlier, and the test last changed at #1296. Same family as the closed `B-143` and the open `B-166`.

- **Current state.** **CLOSED at #1350.** Mechanism REPRODUCED by positive control (phase :59 under load), and this row's own headroom figure corrected: the budget is a 0-60s SAWTOOTH, not a flat minute, because `touch -t` truncates to whole minutes. Repaired by exact `os.utime` stamping plus re-stamping the quiet side immediately before the assertion that re-evaluates it — both pins stay at 29/31 and both fixtures stay in ONE registry, because a first version that split them bought the same headroom while dropping the witness that a stale entry must not drag its fresh sibling into the count. Mutation probe: drift-low reds both assertions, drift-high reds the 31-min side. ORIGINAL FILING (retained verbatim below; every step is DISCHARGED and the 20-minute candidate it names was explicitly REJECTED — see the closure narrative above):

- **Why it is registered rather than waved off.** This arc shipped `B-166` specifically to stop a session dismissing a red as "the known flake". Observing a NEW same-sha-differential flake and not filing it would contradict that discipline in the same breath.

- **Close-out steps.** **Step 1 — confirm the mechanism BY EXECUTION**, not by reading: run S16 under artificial load (or a coarse clock) and capture whether the deviation is the 29-minute fixture being counted, as predicted. If it is something else, the mechanism above is FALSIFIED and this row is rewritten, not patched. **Step 2 — fix the HEADROOM, not the threshold.** Candidate repair: stamp the quiet-side fixture further from the boundary (say 20 minutes) while keeping a near-boundary case that does **not** straddle intervening subprocess work. Widening or deleting the boundary pin is explicitly wrong — S16 exists to stop the STALE constant drifting silently, and its own comment says so (*"Without this pair any STALE in (~2s, 2h) passed the suite"*). **Step 3 — keep it discriminating:** mutation-probe per PD-8, since a loosened pin is a green test that witnesses nothing. **Falsifier:** a demonstration that the two assertions cannot straddle enough wall-clock to cross the boundary, in which case the red has another cause and this row closes as misattributed.

- **Council.** No. Mechanism named, falsifier executable, repair is test hygiene with no contract surface. Same disposition as `B-166`.

### B-170 · Arc-metrics ledger — per-arc wall-clock capture, the GATE the other four units are measured against *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-178` in that plan)*


- **Current state.** ✅ **CLOSED 2026-08-14 (PR #1340).** `tools/arc_metrics.py`, `tools/test_arc_metrics.py` (**46 tests**, all green, each carrying a `# mutation-probe:` annotation — the filed plan said 10; nine codex rounds plus the merge gate added the other 32), `.harness/arc-metrics.jsonl` (16 baseline rows, 10 with round data) and the `justfile` `arc-metrics` recipe all landed. All four close-out steps discharged: the witness was re-run at HEAD (#1337 reproduces 1408 additions from `gh`, and 10 rounds with P1 arrivals [1,2,4,7,8,9] re-derive from the archived logs — not from any report), the mutation probe REDed its covering test with `DID NOT RAISE` before restore, and capture is wired into the `ship-pr` closure sequence. Two grounding corrections landed with it: the tool/test/justfile cited the provisional `B-178` (no such register row) and the prose claimed 11 rows with round data against a measured 10. **Out-of-family review (codex round 1) found 5 genuine defects in the instrument, all fixed before merge:** the summary reported the PR window instead of the real arc span (#1337 read 6.1m against 269.2m actual); `ci_metrics` scanned a 40-run window so 12 of 16 rows recorded unsearched CI history as a measured zero (now queried by full commit SHA — 16/16 carry real data); `count_p1` counted contextual `[P1]` prose as findings (4 of 7 re-verifiable rows carried false P1 rounds; the backfill was re-derived, and 3 rows whose log set is not uniquely reconstructible are now honestly `unmapped` rather than wrong); `extract` would append an unmerged row and burn the arc_id against the real capture; and the ship-pr wiring left the tracked ledger dirty inside a topic worktree, which strands the row and blocks the worktree disposal loop completion requires. Capture is now split `queue` (closure, writes outside the repo) -> `drain` (next arc's PR). 46 tests, each mutation-probe annotated.
- **The gate is open — B-171..B-174 are now evaluable against the `levers_active: []` baseline.**
- **What it is.** GATE UNIT — blocks B-171..B-174. One row per arc at `.harness/arc-metrics.jsonl`; the load-bearing field is levers_active, so efficacy is a cohort comparison rather than an assertion. Fields carry provenance (derived / declared / unmapped) and an absent measurement is never recorded as a measured zero. LANDED at PR #1340 with all four close-out steps discharged and the falsifier re-run at HEAD.

- **Close-out steps.** STEP 1 -- land the uncommitted work: tools/arc_metrics.py, tools/test_arc_metrics.py (10 tests, each carrying a mutation-probe annotation), .harness/arc-metrics.jsonl (16 baseline rows), and the justfile arc-metrics recipe. STEP 2 -- verify the witness still holds at HEAD: the tool must reproduce 10 rounds for #1337 and 1408 additions from git/gh, NOT from any report. STEP 3 -- confirm fail-closed by mutation probe: remove the zero-logs guard in round_metrics() and the test must RED with DID NOT RAISE, then restore. STEP 4 -- wire capture into ship-pr so every future arc appends a row. FALSIFIER: if the backfilled figures cannot be reproduced from git/gh at HEAD, the tool is wrong and no lever may be evaluated against it.

- **Council.** No. The instrument is built and its acceptance criteria are executable; nothing here changes a committed contract.

### B-171 · Round-timing instrumentation — the codex CLI emits NO timing data, so the loop's 78% is currently unpriceable *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-173`)*


- **Current state.** **NOT STARTED.** The `codex-review` recipe at `justfile:564` is unwrapped; no round-timing data exists anywhere for any arc. Blocks `B-173`'s kill condition, which needs per-reviewer round wall clock.
- **What it is.** The codex CLI prints no timing or token data -- verified directly against 1.6 MB transcripts. The split between codex thinking and agent absorbing inside each round is therefore unknowable, and every claim about the review loop is an estimate. One date +%s.%N either side of the codex review invocation fixes that on the very next round. PRECONDITION for B-173's kill condition, which compares per-reviewer round wall clock.

- **Close-out steps.** STEP 1 -- wrap the codex-review recipe (justfile:564) to record {pr, round, started_at, ended_at, codex_wall_s, base_sha} to a sidecar ledger. STEP 2 -- the wrapper must NOT change the review's exit status: a failed review still fails the recipe, and a failed write surfaces rather than being swallowed. STEP 3 -- flow codex_wall_s into B-170's per-arc row. WITNESS: one arc where sum(codex_wall_s) < sum(round_wall_s), the residual being absorption; a residual at or below zero means the wrapper measures the wrong span.

- **Council.** No. One recipe line, no contract surface.

### B-172 · Doc-only grounding sweep — ~20 register items whose honest next step is a grounding pass, not a code arc *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-170`)*


- **Current state.** **NOT STARTED.** The ~20 grounding-only items are identified by their own `close_out` text but none has been re-grounded against HEAD. Gated on `B-170` landing first so the arc is measured.
- **What it is.** Roughly 20 register items carry a close_out beginning "(1) Re-verify... (2) Decide..." -- their genuine next step is grounding, not code. Batched into ONE doc-only PR they pay the per-arc wrapper once instead of ~20 times. MEASURED saving approximately 8 h, from 19 avoided wrappers at the #1023 analogue of 25 min each; the estimate rests on n=1 well-matched analogue, which B-170 exists to improve. Five further items (B-16, B-45, B-66, B-98, B-104) carry an explicit no-work-owed disposition and close in the same sweep.

- **Close-out steps.** STEP 1 -- ground each item against HEAD, never recall: grep the cited symbol, re-read the cited section, write the verdict into close_out WITH the command that produced it. STEP 2 -- one commit per item, so the sweep is reviewable item-by-item rather than as one wall of diff. STEP 3 -- every verdict is one of: still-not-buildable (stays registered), build-now (spins a scoped arc), or no-work-owed (closes). STEP 4 -- ZERO harness-*/src or harness-*/tests files in the diff; one code file drags the whole sweep through the 3-lens gate and destroys the saving. STEP 5 -- run just leg-selfcheck --uncommitted before EVERY push: doc-only buys a merge-gate skip, never a grounding skip, and this sweep touches registers dense with cites and counts. ESCAPE HATCH: split into two sweeps if review or byte load proves unwieldy, at a named cost of one extra wrapper.

- **Council.** No. Grounding is per-item empirical work against HEAD; each verdict is individually verifiable.

### B-173 · Gemini AGY parallel decorrelated review — the only untried lever that touches the review loop's 78% *(operator-APPROVED 2026-08-14; provisionally numbered `B-176`)*


- **Current state.** **OPERATOR-APPROVED 2026-08-14, NOT STARTED.** `agy` v1.1.12 is on PATH and `tools/agy_review.py` exists, but **auth status is UNCONFIRMED** (the check needs a TTY) — confirming it is step 1, not an assumption. No live review has been fired.
- **What it is.** OPERATOR-APPROVED 2026-08-14. Runs just gemini-review alongside just codex-review against the identical diff, concurrently, on a code arc that is happening anyway -- no dedicated arc is spent. Infrastructure verified present: agy v1.1.12 on PATH and tools/agy_review.py, a 624-line fail-closed wrapper with diff-chunking, timeout and model verification, already emitting the same P1/P2/P3 shape as codex. AUTH STATUS UNCONFIRMED at filing -- the check needs a TTY -- so confirming it is step 1, not an assumption. UPSIDE at the measured 21 min/round median across 7 remaining code arcs: 2.5 h if it removes one round per arc, 9.9 h if four, ZERO if the kill condition fires.

- **Close-out steps.** STEP 1 -- confirm agy auth non-interactively before anything else. STEP 2 -- run both reviewers against the IDENTICAL diff, concurrently, for at least 3 rounds. STEP 3 -- classify findings both-raised / codex-unique / gemini-unique, and hand-verify each gemini-unique finding against the diff as real-and-fixable rather than hallucinated. STEP 4 -- record per-reviewer round wall clock via B-171. KILL CONDITION, PRE-COMMITTED: at most one gemini-unique P1 across three rounds, OR gemini's round wall clock exceeding codex's -- either kills the lever, because two parallel waits that do not overlap are not parallelism. ADOPTION BAR: a genuine P1 codex missed, at comparable or shorter wall clock, in at least 2 of 3 rounds. Record the false-positive rate: a second reviewer that adds fix-cycles for non-defects is net-negative and that must be visible. The verdict is declared against these thresholds, never chosen after seeing the data.

- **Council.** No. Operator has approved the trial; the thresholds are pre-committed and the outcome is a measurement, not a judgement call.

### B-174 · Mechanize the finding class, every arc — targeted at SELF-INFLICTED bookkeeping drift, which is ~29% of rounds post-mitigation *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-177`)*


- **Current state.** **NOT STARTED.** The targeting rule (prioritise self-inflicted bookkeeping classes) is derived but not written into `CLAUDE.md` §13.1, and no instance has shipped. Origin classification is not yet recorded in the ledger.
- **What it is.** When a review round surfaces a finding a deterministic check could have caught, the check is written IN THE SAME ARC. Precedent is measured: the trilemma class cost approximately 32 min of review across two rounds and now costs about 1 s in tools/test_roadmap_ritual_smoke.py. Unlike a round cap this removes a class WITHOUT narrowing what any review checks. TARGETING RULE, added after grounding: classify each finding original vs SELF-INFLICTED (introduced by this arc's own earlier rounds) and prioritise mechanising the self-inflicted ones. Reading #1337's ten absorption commits found roughly 6 of 10 rounds carrying a self-inflicted finding pre-mitigation -- two of them naming the agent explicitly (I OVER-TIGHTENED THE FILE SCOPE; I REUSED OCCUPIED SECTION LABELS) -- and these are overwhelmingly BOOKKEEPING (stale counts, occupied section labels, self-contradicting register rows), the class a deterministic check catches by construction. leg-selfcheck already cut this from about 60% to about 29% of rounds; the residual is roughly 85 min per arc at the measured 21 min/round median.

- **Close-out steps.** STEP 1 -- add a CLAUDE.md 13.1 always-on row stating the discipline. STEP 2 -- record {class, arc, rounds_consumed, test_path, runtime_s, origin} per mechanised class in B-170's ledger, where origin is original or self-inflicted. STEP 3 -- ship ONE real instance with the discipline, not the rule alone. NON-GOAL, stated explicitly: this must never become a reason to shorten review -- it removes classes from the loop, it does not license capping it. CAVEAT ON THE NUMBERS: the 6-of-10 origin split is a prose judgement over ONE arc and the 29% figure comes from a count on #1338 that could not be independently derived (no round logs survive for it). Both are exactly the kind of claim that moved twice under measurement during the audit, so record the classification going forward rather than trusting either.

- **Council.** No. The discipline is additive and the targeting rule is grounded in measured origin data; nothing narrows an existing gate.

### B-175 · Arc-metrics queue/drain cross-process durability residue — two parallel worktrees hold DIFFERENT ledgers, and a dead-claim takeover is not atomic *(surfaced by codex round 6 on PR #1340, 2026-08-14; registered rather than fixed at the arms-race point)*


- **Current state.** **REGISTERED, NOT STARTED.** Surfaced by out-of-family review round 6 on PR #1340 and deliberately registered rather than fixed: rounds 3-6 were all hardening the queue/drain machinery introduced in round 1, and these two are the arms-race tail — narrow cross-process interleavings, neither reachable in the single-lane flow `ship-pr` documents.
- **What it is.** Two residual cross-process hazards in the B-170 queue/drain protocol, both real and both requiring coordination beyond a single unit. (1) LEDGER is per-REPO while QUEUE_DIR is shared, so two parallel next-arc worktrees draining at once take disjoint arcs into disjoint local ledgers; each exits 0, and the rows land in two different PRs. Deferred acknowledgement (rows released only once present in COMMITTED history) removes the LOSS case, but not the split. (2) _claim_arc's dead-claim takeover reads liveness then unlinks; two drains can both judge the same claim dead and one can unlink the other's freshly created claim. Neither is reachable in the single-lane flow that ship-pr documents.

- **Close-out steps.** STEP 1 -- decide whether parallel-lane drain is a supported mode at all, or whether drain is declared single-lane by contract (the cheap resolution: ship-pr already serialises merges, so serialising drain costs nothing). STEP 2 -- if supported, give the takeover a real compare-and-swap (O_EXCL sentinel or flock) instead of liveness-read-then-unlink. STEP 3 -- if supported, make the ledger append itself the serialisation point rather than per-entry claiming. FALSIFIER: a two-process probe that drains the same queue concurrently must produce exactly one row per arc in exactly one ledger, with no queue entry released before its row is committed.

- **Council.** No. The mechanism is named and the falsifier is executable; this is a scope decision about whether parallel drain is supported, not a change to any committed contract.

### B-176 · Ring-buffer flush budget `test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5` asserts a bare 100 ms wall-clock budget with no isolation from load — the third member of the `B-166`/`B-169` flake class *(observed RED once on PR #1341 and GREEN on a same-sha rerun, 2026-08-14; filed by the `B-165` grounding leg, which the post-#1341 head had flagged as owing this registration)*

- **What it is.** A wall-clock BUDGET assertion, not a wall-clock ORDERING one -- which makes it a sibling of B-166 and B-169 rather than a duplicate. harness-runtime/tests/test_lifecycle_ring_buffer.py:525 seeds 100 span rows, calls ring.flush_to_sqlite(conn) against a real on-disk sqlite database under tmp_path, and asserts elapsed_ns < 100_000_000 (line 539). Nothing bounds what else is running: the measured interval spans real filesystem I/O, so on a loaded runner -- or one where another test in the same session is doing disk work -- the budget can be exceeded without the code under test having changed at all. EVIDENCE IS SAME-SHA DIFFERENTIAL, which is the only kind that proves it: it went RED once during PR #1341 and PASSED on a rerun of the SAME sha. MECHANISM IS GROUNDED BY CODE READ, NOT BY REPRODUCTION -- the read establishes that the assertion is load-sensitive by construction; it does NOT establish that load is what actually happened on the observed red.

- **Current state.** **CLOSED at #1347.** Mechanism REPRODUCED under load and CONFIRMED: the same unchanged flush measures median 2.51 ms / max 4.54 ms idle (n=40) and median 5.96 ms / max 139.92 ms with 3 of 60 samples breaching under 24 cpu + 8 io spinners. The median moves 2.4x while the tail moves 31x — a scheduler stall on the two `asyncio.to_thread` hops inside the timed window, not slow work. Repaired as best-of-5 with the 100 ms threshold untouched and the assertion retained. The arc's first mutation-probe annotation was falsified by its own probe and retracted; the residual it exposed is registered as B-177.

- **Close-out steps.** REGISTERED RATHER THAN WAVED OFF, for the same reason B-169 was: this workspace shipped B-166 specifically so a session would stop dismissing a red as 'the known flake', and observing a THIRD same-sha-differential red without filing it would contradict that discipline in the same breath. Filed here by the B-165 grounding leg because the post-#1341 next-action head named the omission explicitly. STEP 1 -- REPRODUCE UNDER LOAD: run the test with the machine deliberately busy (or with a constrained I/O path) and capture the actual elapsed_ns distribution. If the failures are NOT clustered near the boundary under load, the load mechanism is FALSIFIED and this row must be rewritten rather than patched -- the alternative hypothesis being a genuine flush-path regression, which is exactly what a mis-filed flake row would hide. STEP 2 -- FIX THE SHAPE, NOT THE NUMBER. Raising 100ms to some larger number buys silence, not information, and re-arms the same failure at the new threshold; the AC #5 budget is a real performance claim and should be measured in a way that survives an unquiet machine (e.g. best-of-N, or an explicitly perf-marked test excluded from the correctness suite). Do NOT simply delete the assertion: AC #5 is a stated contract and dropping its only witness would retire the claim silently. CROSS-REF: B-166 (unsynchronized barrier deadline) and B-169 (a 29-minute fixture one minute from a 30-minute boundary) -- three rows, one class, and the class is worth naming: an assertion whose truth depends on how busy the machine is.

- **Council.** No. Same disposition as B-166 and B-169: the mechanism is named, the falsifier is executable, and the repair is a test-hygiene change with no contract surface -- though STEP 2 should not silently weaken the AC #5 performance claim while making it stable.

### B-177 · The AC #5 100 ms flush budget has ~40x slack over real work, so it discriminates only catastrophic regressions — a per-row-commit regression runs 16x slower and still PASSES *(measured by the `B-176` mutation probe, 2026-08-14; the annotation it falsified was retracted rather than kept)*

- **What it is.** Surfaced by a mutation probe that FAILED TO KILL, during the B-176 arc. `test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5` asserts a 100 ms budget against a flush that actually takes ~2.5 ms idle — roughly 40x slack. MEASURED, NOT INFERRED: replacing the `conn.executemany` batch at `harness_od/sqlite_span_store.py:159` with a per-row `conn.execute` + `conn.commit` loop (100 separate commits) measures 33-57 ms across 15 samples and the test PASSES. Only a catastrophic mutation kills it — `PRAGMA fullfsync=ON` plus per-row commit, at ~2.0 s. CRITICALLY, THIS IS NOT A BEST-OF-N ARTIFACT: the slack is a property of AC #5's chosen number, and the single-sample assertion B-176 replaced had the identical blind spot, so the witness was never stronger than this.

- **Current state.** **CLOSED at #1349.** AC #5 is a TEST THRESHOLD WITH NO SPEC-LEVEL BASIS, and the number was NOT tightened. **Corrected in review:** the first draft claimed the spec corroborated a 1000 ms production cycle; the merge-gate spec lens BLOCKED and was right — `Spec_Operational_Discipline_v1_25.md` §1.3 supersedes that v1.8 line and states cadence is "operator-orchestrator-driven (NOT bound to `flush_interval_ms`)". What survives is firmer: the 100 ms figure appears nowhere in design-substrate except U-OD-43's own AC list (self-framed as an "Integration test:" criterion), and the spec states no flush latency contract at all — so tightening it would INVENT a contract the spec declined to state, and its being undetermined is precisely why a separate measurement-derived guard is needed. The missing coverage became a SEPARATE assertion on the call SHAPE, not the clock. A first implementation used a measurement-derived 15 ms min-of-5 bound; **codex raised a P1 and was right** — a host-calibrated wall-clock bound in the BLOCKING suite can fail unchanged code on slower storage, and this file already records a 139.92 ms stall on this path, so closing B-177 by adding a tighter member of the B-166/B-169/B-176/B-178 flake class was a poor trade. The guard now counts sqlite calls: one flush is `executemany`=1 / `execute`=1 / `commit`=2; a per-row loop reads 0/101/101. Verified in one run: under the regression the guard FAILS on those counts while the AC #5 ceiling PASSES — deterministically, with the host removed from the assertion. Bounded honestly: it does not catch slowdowns that preserve the call shape. ORIGINAL FILING:

- **Close-out steps.** REGISTERED RATHER THAN PATCHED, deliberately: tightening 100 ms toward the ~2.5 ms measurement would re-spec a STATED performance contract (AC #5), which is design-substrate territory, not test hygiene. STEP 1 — decide whether AC #5's 100 ms is a LATENCY CEILING (a bound the flush must never exceed, in which case the slack is intentional and this closes as by-design) or a REGRESSION GUARD (in which case it is ~40x too loose to be one). THE TWO CARRIERS FRAME IT DIFFERENTLY, which is why the question is live: the canonical AC text (U-OD-43 AC #5 at `design-substrate/Implementation_Plan_Operational_Discipline_v2_14.md:147`) says only "Integration test: 100-span batch flush in < 100ms", framing a test threshold, while the `flush_to_sqlite` docstring at `harness-runtime/src/harness_runtime/lifecycle/ring_buffer.py:333` calls the same number an "AC #5 latency target". A ceiling and a guard want different numbers, and one number is currently doing both jobs. STEP 2 — if a regression guard is wanted, it needs a SEPARATE assertion with a measurement-derived number (best-of-N makes a tight bound viable), not a re-pointing of the ceiling. STEP 3 — whatever lands must kill the per-row-commit mutation, the concrete regression this row proves escapes today. FALSIFIER: evidence that AC #5 was specified as a ceiling with the slack intended.

- **Council.** No. The measurement is unambiguous and the open question is a scoping one (ceiling vs guard) the spec text can settle. It does touch a stated contract, which is why it is registered rather than fixed in the arc that found it.

### B-178 · `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp` compares a SYNTHETIC `now` against a REAL-clock `first_observed_at`, so it holds only while under ~0.15 s of real time elapses — fourth of the `B-166`/`B-169`/`B-176` class, and a distinct sub-mechanism *(observed RED on CI at `58c9b90b` during PR #1347; mechanism REPRODUCED by positive control, 2026-08-14)*

- **What it is.** A wall-clock-sensitive assertion whose sensitivity comes from MIXING an injected clock with the real one, which makes it a distinct sub-mechanism from its three siblings rather than a duplicate. harness-runtime/tests/test_lifecycle_protected_result_store.py:2784 takes at = time.time() (:2819), backdates the orphan''s observation row by 10 s (:2825), recreates the file with a synthetic mtime recreated_at = at + 0.05 (:2831-2832), then sweeps at a SYNTHETIC now of recreated_at + ttl - 0.1 (:2836) and again at recreated_at + ttl + 0.1 (:2847), asserting the file is reclaimed. NEITHER of those two sweeps passes observed_at. MECHANISM, GROUNDED IN THE PRODUCTION CODE AND THEN REPRODUCED: _record_observations builds its updated map ONLY from past_ttl_names and keys each row record.observations.get(name, sample_time) (protected_result_store.py:1343-1346). At the intermediate sweep the recreated file''s age is 0.90 s against a 1.0 s TTL, so it is NOT past-TTL and its backdated row is DROPPED from the record entirely. At the final sweep it IS past-TTL, so the row is RE-CREATED using sample_time = observed_at if observed_at is not None else time.time() (:1342) -- i.e. the REAL clock, because the call omits observed_at. Term 3''s grace conjunct then compares the SYNTHETIC now (at + 1.15) against a REAL first_observed_at (at + delta), so the assertion holds only while delta -- the real elapsed time across two full sweeps, a JSON record read+write, an unlink, a write and a utime -- stays under ~0.15 s. EVIDENCE IS A POSITIVE CONTROL, not inference: injecting time.sleep(0.25) immediately after :2819 reproduces the CI failure exactly and deterministically (same AssertionError: assert not True at :2849), and the captured log emits ''no GC observation record was read -- a fresh grace begins for every past-TTL candidate (past_ttl_orphans=1)'', which is the dropped-then-recreated row stating itself. Removing the sleep restores green.

- **Current state.** **CLOSED at #1348.** Repaired by INJECTING the clock, not widening a margin: both remaining sweeps now pass `observed_at`, matching the idiom the test's own first sweep already used. The dropped-row fact is asserted rather than assumed (`_read_observation_record(store) == {}`). Verified by execution — the 0.25 s delay that reproduced the failure now passes, and so does 3.0 s (20x). The docstring's mutation probe was also corrected: conjunct (a) has TWO carriers and dropping only one is a false non-kill; with both dropped the test fails on its own assertion, and the pre-fix test fails identically, so witness strength is provably unchanged. ORIGINAL FILING: Observed RED on CI at `58c9b90b` (PR #1347, job 94955744496) while the identical tree passed `just check` locally at 7938 passed / EXIT=0. Reproduced deterministically by positive control.

- **Close-out steps.** STEP 1 IS ALREADY DISCHARGED -- unlike B-166/B-169/B-176 at filing, this row is REPRODUCED, so no further reproduction is owed and the mechanism above is not a hypothesis. STEP 2 -- DECIDE THE FIX, AND NOTE IT IS NOT A MARGIN WIDENING. The naive repair (pass observed_at at the two sweeps that omit it) does NOT work as written: at the final sweep a row created with observed_at = now has zero grace, so the grace conjunct becomes unsatisfiable and the test reds deterministically instead of flakily. The repair must first settle WHICH first_observed_at the scenario intends, and the candidates are (i) keep the backdated row alive by arranging the intermediate sweep to see the candidate as past-TTL too, (ii) pass an explicit, deliberately-old observed_at at the final sweep, or (iii) restructure so the outcome never depends on a re-created row at all. STEP 3 -- whatever lands must still kill the mutation the docstring already names (dropping conjunct (a) so a stale record row alone can reclaim must still make the recreated file disappear at the first post-recreation sweep). A NOTE THE REPAIR MUST NOT SKIP, because it is the real finding here: the docstring at :2843-2846 says the reclaim bound ''falls back to the pre-existing timestamp-derived one'', but the pre-existing row is in fact DROPPED before the final sweep, so today the test passes via a freshly-sampled real-clock observation that merely happens to precede the synthetic now by more than one TTL. The witness is therefore passing for a reason its own docstring does not describe -- which is why this is registered rather than patched inside the B-176 arc that found it: it touches what the witness proves about the ratified B-110 residual, and getting that wrong would weaken a ratified witness while making it stable. FALSIFIER: evidence that the real-clock sample is not what gates it -- e.g. the failure reproducing with the injected delay REMOVED, or the backdated row demonstrably surviving the intermediate sweep. CROSS-REF: B-166, B-169, B-176 -- four rows, one class (an assertion whose truth depends on how busy the machine is); B-110, whose residual this test exists to witness.

- **Council.** No. The mechanism is reproduced and the falsifier is executable. It is a test-hygiene repair with no contract surface, but it is NOT trivial -- it must preserve what the witness proves about the ratified B-110 residual, which is why it is registered for its own arc rather than absorbed.

### B-179 · Gate guidance on BOTH runners names `just check`, which never runs the hook shell suites — only the `.agents/` skill mirrors name `just codex-check`, so Codex's own guidance contradicts itself *(surfaced while grounding `B-169`'s CI venue; scope corrected by codex rounds 1 and 2, 2026-08-15)*

- **What it is.** The gate that the workflow docs name does not execute the tests it is trusted to cover, and the exposure spans BOTH runners rather than one. justfile:47 defines `check: ... test`, and `test` (justfile:23-24) is `uv run pytest -m 'not e2e'` -- pytest ONLY. The hook and statusline SHELL suites are run exclusively by tools/codex-parity-check.sh:20 (`for test_script in tools/hooks/test_*.sh tools/statusline/test_*.sh`), which sits in justfile:66 `codex-check` and in CI at .github/workflows/ci.yml:79 -- but NOT in `check`. WEAK CARRIERS (name `just check`): .claude/skills/roadmap-continue/SKILL.md:31 outright; .claude/skills/ship-pr/SKILL.md:14 says `just check` plus 'the relevant test suites', a discretionary hedge that never names the shell suites; and AGENTS.md:45 outright -- which is a CODEX carrier, not a Claude one, since AGENTS.md:1-3 declares itself 'the authoritative compact projection of the project rules' FOR CODEX. CORRECT CARRIERS (name `just codex-check`): .agents/skills/roadmap-continue/SKILL.md:37 and .agents/skills/ship-pr/SKILL.md:17. THE SHARPENED FINDING: Codex guidance is INTERNALLY INCONSISTENT -- its skill mirrors prescribe the strong gate while its own root projection prescribes the weak one -- so a Codex session is exposed depending on which carrier it follows, and the Claude side is uniformly weak. SCOPE CORRECTED TWICE BY OUT-OF-FAMILY REVIEW: round 1 showed the .agents/ skill mirrors are already right (the first filing had not named a tree at all); round 2 showed the resulting 'Claude-side-ONLY' framing was ALSO wrong, because AGENTS.md is Codex's authoritative projection. GROUNDED, NOT HYPOTHETICAL: in the B-169 arc `just check` went green (7939 passed) on a branch whose ONLY change was tools/hooks/test_loop_gc.sh; that file was exercised only because the arc separately ran the shell-suite glob by hand. Same family as the 'a gate cannot tell empty from unlooked' discipline. Exposure is LOCAL-ONLY -- CI does run the glob -- so this delays discovery to CI rather than losing it.

- **CLOSED AT #1351 via recommendation (c).** The weak carriers now name `just codex-check`. **The row undercounted its own carriers:** it named three, but `grep` at HEAD found **five** occurrences across those three files — `roadmap-continue:31` **and `:34`**, `ship-pr:14` **and `:18`** (the second of each pair is the grounding-pass restatement) — plus **two files it never named**, `two-lane:41` and `self-heal:19`. `self-heal` was a **partial** carrier: it named `just check` but separately globbed `tools/hooks/test_*.sh`, compensating while still missing `tools/statusline/test_*.sh`. Not a carrier: `harness-adversarial-reviewer/SKILL.md:258` is English prose ("just check whether…"), not the command. **STEP 2 DISCHARGED BY EXECUTION:** a deliberately reddened hook suite (backed up via `cp`, restored after) propagates a non-zero exit through the `codex-parity-check` glob naming the exact file, while the pytest lane collects **zero** `.sh` files. **REVIEW CORRECTED THE FIX ITSELF:** the first wording claimed `check` "runs pytest ONLY", which is **false** — `check` also runs sync, lint, fmt, typecheck and five docs/closure gates; the true distinction is that it omits `codex-parity-check`. Reworded across all five carriers. **Deliberately not adopted:** `tools/q1_review_gate.py:236-241` pins the literal string `just check`, but it gates ONE frozen artifact for the **closed** R-CL-Q1 phase and CI never runs `--check` against a live report, so no live contradiction exists. **Known cost, recorded:** in a root checkout carrying untracked edits, `codex-check` reds on a HARD `ROOT_CHECKOUT_EDIT` guard finding where `check` stays green — pre-existing, reproduces on clean `main`; fallback **(b)** (a narrow `hook-suites` recipe folded into `check`) remains available if that friction proves real. **The FALSIFIER was checked and not met:** no always-on git/session/pre-commit path runs the shell suites locally on either runner.

- **Close-out steps (original filing).** NOT fixed inside the B-169 arc: changing the primary gate's composition (or the guidance that selects it) is a scope expansion with its own blast radius -- `just check` is invoked by skills, hooks and operator habit, and codex-parity-check adds a ~3-minute pytest lane plus 23 shell suites to what is currently a ~4.5-minute run. STEP 1 -- decide the shape: (a) add `codex-parity-check` to `check`, paying the full cost on every invocation; (b) add a narrower `hook-suites` recipe running only the shell globs and put THAT in `check`, leaving the codex pytest lane where it is; or (c) align the three weak carriers (.claude/skills/roadmap-continue:31, .claude/skills/ship-pr:14, AGENTS.md:45) with the .agents/ skill mirrors that already prescribe `just codex-check`. (c) IS THE RECOMMENDATION: the .agents/ skill tree already runs exactly this fix, making (c) a proven docs-only change with no build-tooling risk -- and it additionally resolves the internal contradiction inside the Codex projection, which (a) and (b) would leave standing. (b) remains the fallback if the full codex-check cost proves too heavy, buying the shell-suite coverage alone at ~90s measured. STEP 2 -- whichever lands, verify BY EXECUTION that a deliberately reddened hook suite fails the gate the guidance names; a change that does not actually propagate the non-zero exit is the same did-not-look failure wearing a new name. FALSIFIER: evidence that some always-on path (a git hook, a session hook, a pre-commit) already runs the shell suites locally on either runner, in which case the exposure is only documentary and this row closes as over-stated.

- **Council.** No. The gap is grounded by file:line and the repair is a build-tooling composition choice with three enumerated options and a named recommendation. There is no cross-domain tension to convene over -- step 1 is an operator-visible cost/coverage trade, not a design fork.

### B-180 · S16's quiet-side assertion was ABSENCE-based, so a silent hook reported a PASS — now LIVENESS-ANCHORED, and the unanchored count greps are fixed as a class *(found by the merge-gate witness lens during `B-169`, 2026-08-15; closed at #1355)*

- **Disposition.** CLOSED at #1355. Both halves of the row were real, both are fixed, and both fixes are mutation-proven load-bearing.

- **The mechanism, confirmed by execution.** S16's quiet-side assertion was `grep -q "unreconciled" && bad ... || ok ...` — it passed whenever the substring was absent, FOR ANY REASON. `sw_ctx` pipes the hook through `jq -r ... 2>/dev/null` and command substitution discards the exit status. The row's FALSIFIER (that `jq` erroring might abort the case under `set -e`, making the silence path unreachable) is REFUTED: `tools/hooks/test_loop_gc.sh:10` is `set -uo pipefail` — **no `-e`** — and a direct probe confirms empty hook stdout leaves `jq` exiting 0 with `OUT=""`, so the assertion reported **ok** on silence. `loop-gc.sh:114-118` makes that silence ENGINEERED and reachable, not a remote crash scenario.

- **Why an envelope check was the WRONG shape.** The row's step-1 candidate was "assert the hook produced a well-formed envelope before asserting the clause is absent". That cannot work here: **S2 pins that a fully reconciled registry legitimately prints NOTHING and exits 0**, so empty is a CORRECT output and a JSON/envelope assertion would fail the legitimate case.

- **A separate positive control was ALSO insufficient — caught by codex round 1, with its own mutation.** The first fix added a liveness probe (a stale 2 h companion) in a SEPARATE hook invocation, then kept the absence check on a second one. Codex mutation-tested a sweep that goes silent only for the quiet one-row fixture: the probe stayed green, the absence assertion still reported `ok`, and **all 75 tests passed**. A control in a different invocation does not witness the invocation that carries the claim.

- **What actually landed.** Both sides are pinned through **ONE** hook invocation using **presence-based assertions only**, so there is no blind absence check left: a count of exactly `1` proves the 31-min key is flagged AND the 29-min key is not counted, and `oldest: s16stale` proves it is the RIGHT key — without that second assertion a low+high drift (counting `s16fresh`, missing `s16stale`) also reads `1`. Both 29/31 pins are untouched.

- **The hook was deliberately NOT touched.** `loop-gc.sh`'s failure-invisibility is load-bearing for production (SessionStart must never be blocked by a sweep bug). This is a test-observability fix, not a fail-loud conversion — the two are different surfaces and conflating them would re-litigate a ratified design choice, exactly as this row warned.

- **The CLASS was swept — and the first sweep MISSED members (codex round 3).** The initial enumeration searched for the `grep -q … && bad` shape and so missed the emptiness-based form `[ -z "$OUT" ]`. Codex mutation-tested **S7c** (`:358`): when the sweep branch for an empty-transcript row raises, the hook collapses it to empty stdout and S7c still reported `ok`. Reproduced exactly, then fixed. **S3** (`:288`) carried the identical shape and was fixed in the same pass. Both are now anchored the same way as S16 — a same-invocation stale companion, asserting exact count **and** `oldest:` key.

- **MY CARVE-OUT WAS HALF-WRONG, and the merge-gate witness lens BLOCKED on it.** I had lumped **S2** and **S10** together as "whole-hook silence contracts, unanchorable by construction". That is sound for **S10** — `loop-gc.sh:120` gates the python call at the bash level (`if [ -f "$REGISTRY" ]`), so with no registry python never runs and no failure state can masquerade as that case, and there is no registry to attach a companion to. It was **NOT** sound for **S2**: the lens mutation-proved that a sweep crashing on a registry of purely reconciled activity emits nothing and exits 0 — byte-identical to "correctly reconciled" — and found that **nothing else in the file witnessed reconciled-exclusion against a companion**. The separable half was anchorable all along.

- **Fixed by SPLITTING the claim rather than abandoning either half.** `S2` keeps its absence shape, because it pins the SILENCE CONTRACT itself (empty stdout + exit 0) and any companion that must be reported would destroy exactly that. New **`S2b`** carries the separable half as a PRESENCE-based claim: one invocation holding a reconciled pair AND an unambiguously stale key must report exactly `1` and name the companion — proving both that the sweep ran and that the reconciled key contributed 0.

- **Second half — the unanchored greps, fixed as a CLASS.** `grep -q "1 unreconciled subagent"` also matched `21 unreconciled subagent(s)`. **11 patterns** were anchored to `(^|[^0-9])N unreconciled subagent…` across S1/S5/S6/S7/S15/S16. Proven by predicate test: the old pattern matches a spoofed `21 unreconciled subagent(s)`, the anchored one rejects it and still matches a genuine `1`.

- **Mutation probes (PD-8) — six, against the FINAL shape.** (A) sweep silenced entirely → S3, S7c and S16 all RED (the paths that previously reported `ok`). (B) `STALE` low to 25 min → count reads `2`, S16 REDS. (C) `STALE` high to 35 min → empty, S16 REDS. (D) comparison inverted (`age > STALE` → `age < STALE`) → S16's count still reads `1` and PASSES while `oldest: s16fresh, 29m` REDS the second assertion, establishing the `oldest:` assertions as independently load-bearing. (E) codex's own scenario — sweep raises only on the empty-transcript branch → S7c REDS (it passed vacuously before). (F) predicate test for the anchoring. (G) the witness lens's own mutation — total sweep crash against a purely reconciled registry → `S2` still reports `ok` (correctly: that IS the silence contract) while **`S2b` REDS both assertions**, closing the vacuous pass the gate blocked on. (H) reconciliation-specific — `stop` events made a no-op so a reconciled key is wrongly counted → `S2b` reads `2` with the wrong `oldest:`, establishing that it witnesses EXCLUSION and not merely liveness. Hook restored byte-identical after every probe; all 23 shell suites green; 76 passed / 0 failed.

- **A self-inflicted defect, caught by codex round 2 and swept as a class.** The round-1 comment rewrite computed `t[t.index(A):t.index(B)]` where B precedes A, yielding an EMPTY slice — and `str.replace("", new, 1)` inserts at position 0, pushing `#!/usr/bin/env bash` to line 18. Every green check missed it because the parity runner invokes `bash <script>`, which needs no shebang; only an `execve` caller failed (`Errno 8 Exec format error`). Fixed, then **every** executable suite under `tools/hooks/` and `tools/statusline/` was execve-probed — zero ENOEXEC.

- **Council.** No. The one real tension (production failure-invisibility vs test observability) was nameable and resolved without changing the hook.

### B-181 · The 2026-08-13 `test_ow_proceed_deadline_stashed_child_completed_not_cancelled` reds are UNREPRODUCIBLE — the leading candidate is refuted and CI holds no record *(split out of `B-166` when its premise was refuted, 2026-08-15; closed unreproducible-and-stale at #1357)*

- **Disposition.** CLOSED at #1357 as **unreproducible-and-stale**, which is the disposition this row's own step 3 prescribes. Step 2 was executed in full and it REFUTED the leading candidate; no further hypothesis survives, and the row was written to bound this effort rather than license an open-ended hunt.

- **What was observed.** On 2026-08-13, `test_ow_proceed_deadline_stashed_child_completed_not_cancelled` reddened in **4 of ~12 full-suite runs**. Those observations were real. `B-166` filed them as a near-boundary barrier race; that explanation was refuted at #1353 by a deadline sweep showing ~**600x headroom** (deviation only at ≤ 0.0005 s against a pinned 0.3 s).

- **STEP 2 EXECUTED — the leading candidate is REFUTED on both axes.** The candidate was #1336's own Python changes, specifically `harness-runtime/tests/test_b164b_fanout_orphaned_child_span_repro.py`, which looked strong because its own docstring says it "abandons its `ThreadPoolExecutor` with `shutdown(wait=False)`" — orphaned threads are exactly the mechanism that could cut a later wall-clock-sensitive test in a shared process. **Mechanism axis:** both of its helpers in fact JOIN their threads (`child_done.wait(timeout=_TIMEOUT)` at `:205` and `:254`), and measurement confirms it — running the whole file leaves **0 threads leaked** and none alive. **Venue axis:** running the suspect immediately before OW10 in ONE process, 15 consecutive times, produced **15/15 clean with zero deviations**.

- **The observation layer was checked, not assumed.** CI holds **no record**: `#1336` and `#1337` both have clean check rollups, and grepping the `--log-failed` output of **all five** failed CI runs from 2026-08-13 returns **0 mentions** of this test. That is consistent with the original local-only framing, and it means no durable artifact of the reds exists to re-examine.

- **Run count, recorded as step 3 requires.** **60 runs across three shapes, all clean:** 25 full-suite (against an original scale of ~12; most of them 2-at-a-time, i.e. a LOADED venue), 20 isolated under 24 CPU spinners on 8 cores, and 15 targeted co-execution runs. Plus the ~600x headroom measurement and the thread-leak measurement.

- **What would re-open this.** A fresh observation of the same red, with the **captured terminal map** attached — that map is what distinguishes a genuine barrier cut (`{0: 'timed_out', 1: 'timed_out'}`) from whatever actually happened on 2026-08-13. Do NOT re-file this as a barrier-race row without one: that premise is refuted by measurement, not by absence of evidence.

- **Council.** No. This was an empirical attribution question, executed to its own stated stopping rule.


### B-182 · Both halves of the C-OD-11 §11.1 per-cell budget are declared but reach no production path — `cell_rate_limit` has no consumer at all and the `tenant_rate_limit` helper has no caller — and §11.1's team-binding row commits no figure *(surfaced grounding B-137 step (3)'s C11 half, 2026-08-15; reduced to facts after five out-of-family Codex rounds falsified every interpretation layered on them)*

- **Read this first — the arc that produced this row was wrong five times, and the shape of the error is the most transferable thing in it.** Out-of-family Codex reviewed it across **five rounds** and raised **19 valid findings**, four of them [P1]. Every one falsified an *interpretation* layered on top of the facts; **none falsified a fact.** The recurring mistake was identical each time: **inferring contract meaning from an implementation carrier instead of reading the canonical matrix that already answered the question.** Placement was "derived" by filtering the implementation's logical `COLLECTOR_BOUNDARY` mapping by persona name — twice, wrongly — while **C-OD-20 §20.1's per-cell collector placement matrix existed the whole time**. Provenance was "derived" from a number's absence in the v1.2 baseline, while `Spec_Operational_Discipline_v1_37.md` states it in canonical spec text. An enforcement asymmetry was asserted between two caps, neither of which is reached. A dependency cycle was asserted, then refuted, and the refutation was wrong too. **This row therefore records FACTS ONLY and leaves the interpretation to whoever dispositions it, with the canonical instruments named.**
- **FACT 1 — `cell_rate_limit` has no consumer.** `per_cell_cardinality_budget.py:118` commits a flat `cell_rate_limit=10_000.0` spans/sec at every ACTIVE cell. It has **zero readers** anywhere in `harness-*/src` outside its own declaration. Mutation-probed: introducing a reader reds the witness; breaking the scan root reds it on a never-report-unlooked-as-empty guard.
- **FACT 2 — the sibling's only consumer has no caller either, so NEITHER cap is reached by any IN-REPO wiring.** `assert_per_tenant_cardinality_isolation` implements a real comparison (it raises, witnessed behaviourally above and below the limit), but it has **no call site in `harness-*/src`** — only its definition, its `__all__` entry, two docstring mentions, and tests. Three drafts of this row claimed an enforced-vs-unenforced asymmetry between the two caps; **that is withdrawn.** What differs is only how much of the mechanism exists: one cap has semantics a caller *could* reach, the other has no consumer at all. **Scope, tightened at round 8 [P2]: this is a claim about THIS REPOSITORY, not about global enforcement.** At cells whose canonical placement is a sidecar, vendor pipeline, or self-hosted backend collector — `team × self-hosted` among them — an **external** collector or backend may enforce these caps. Nothing in this arc observes those venues, and the close-out below leaves that possibility open as the primary disposition question.
- **FACT 3 — §11.1's team-binding row commits no figure IN THAT TABLE, but the cap value is stated canonically elsewhere; what is open is the VOLUME EVIDENCE, not the cap.** `Spec_Operational_Discipline_v1_37.md` Rider (a) states `cell_rate_limit=10_000.0` spans/sec at **every** ACTIVE cell, team × self-hosted included, and uses it to declare a budget check passed. So C11 does have a numeric bound. What §11.1's team row leaves open is the throughput/population to assess that bound against: it defers verbatim to *"Persona §11.4 throughput rough order-of-magnitude open item; envelope refines downstream of §11.4 closure."* Resolved at the **effective** declaration across the whole 42-file OD delta chain (of every file carrying such a row, the highest-versioned one carries the deferral), not the v1.2 baseline alone — a synthetic later delta with a numeric row reds the witness.
- **FACT 4 — that deferral target is still open.** `Persona_Document_v1.md` **§11 Open items, row 4** — a table row, not a subsection *(a first pass searched for a `§11.4` heading, found nothing, and nearly filed the cite as dangling)*.
- **FACT 5 — the canonical placement instrument is C-OD-20 §20.1**, and per it exactly ONE cell is unambiguously in-process: `solo-developer × local-development` (*"In-process otelcol-contrib + BatchSpanProcessor"*). `solo × self-hosted-server` permits in-process only *"as alt-route"* with the backend collector **preferred**; `solo × managed-cloud` is *"Vendor-pipeline"*. **This row makes no claim about how many cells are in-process** — it names the instrument.
- **What this row does NOT establish.** Whether C1 is affordable; whether the §11.1 caps are correctly external; whether the throughput figure is recoverable from a starved stream (a *dependency cycle* was asserted and withdrawn, and its refutation via C-OD-16 §16.2 was withdrawn too — §16.2's flat `1/base_rate` rescale is unbiased only for a *uniformly* sampled stream, and this one is not). **The affordability call B-137's council owes is untouched by this row.**
- **Close-out steps.** **(1) Re-ground the five facts** — the witness module is the instrument and every assertion states what goes stale if it moves. **(2) Disposition FACT 1 + FACT 2 together, and derive placement from C-OD-20 §20.1, not from the enforcement-layer enum.** The question is whether a declared cap with no consumer is correct-by-design (external enforcement) or a gap, and it may answer differently per cell. **Do NOT wire an in-process enforcement site before that decision** — minting a cap the contract does not state would be an X-AL-3 design extension. **(3) Carry FACT 3 + FACT 4 into B-137's council as CONTEXT, not as a blocker.** **(4) NARROWED at #1380** (review round 4 [P2]), so this row and `B-183` stop giving opposite instructions: settle `B-183`'s quantity question first **only for a TENANT-cap consumer**. Authorising a consumer for `cell_rate_limit` **alone does not** require it — `cell_rate_limit` is declared a span rate at plan `:978`, nothing contradicts that, and `B-183`'s filed fork deliberately excludes cell-limit consumers from its halt target. The exception is fork disposition **(B)**, which would make the tenant cap a span rate too and re-unify them.
- **What would falsify it.** A `cell_rate_limit` reader appearing in `harness-*/src`; a production caller for `assert_per_tenant_cardinality_isolation`; §11.1 acquiring a team-cell figure anywhere in the delta chain; Persona §11 open-item 4 closing; or C-OD-20 §20.1's placement rows changing.
- **Council: no** at registration — these are facts plus a single-domain disposition with a named owner (C7/OD). They are inputs to `B-137`'s already-declared C7 + C11 convening, not a separate one.
- **Cross-ref.** `B-137` (the row whose C11 half this grounds); **`B-183`** (the latent quantity/unit mismatch in the tenant helper); `B-133` (whose F-08 rider produced the v1.37 independent-of-sampling ruling); `B-160` / `B-124` (the same §9.2 floor family, both gated on `B-137`).

### B-183 · The `C-OD-11` §11.1 per-tenant cap's QUANTITY is declared two ways INSIDE THE OD SPEC LINEAGE — the contract body says cardinality (and the plan and shipped code implement distinct series) while two later deltas' change-note prose reasons in spans/sec — and its TEMPORAL dimension is undeclared, `observation_window` being read nowhere *(surfaced by out-of-family Codex round 3 while grounding `B-182`, 2026-08-15; **heading neutralised at #1380** — the prior wording asserted a PER-SECOND limit whose threshold depends on the caller's window, which is one of the two readings the filed fork does **not** settle)*

- **What it is.** `tenant_rate_limit` is documented as a **per-second** rate — *"per-tenant rate limit (spans/sec)"* (`per_cell_cardinality_budget.py:58`, and again at `:50`) — and `CardinalityCounters` carries both an `observed_series` count **and** an `observation_window` string. But `assert_per_tenant_cardinality_isolation` compares `observed.observed_series > budget.tenant_rate_limit` **directly**, and never reads `observation_window`: the field name appears exactly **once** in that module, at its own declaration. The gate's effective threshold therefore depends entirely on whatever window the caller happened to pass. **1,001 series over `"1m"` raises exactly as 1,001 over `"1s"` does** — the window is invisible to the function. *(An earlier draft said the former is "~16.7/sec"; **withdrawn at round 5** — that arithmetic silently equates series with spans. See the quantity mismatch below, which is the deeper of the two problems.)*
- **Scope, corrected at round 4 [P1]: this is a defect in an UNCALLED helper.** `assert_per_tenant_cardinality_isolation` has **no call site in `harness-*/src`** — only its definition, its `__all__` entry, two docstring mentions, and tests. So no production path reaches this comparison today, and the row is **not** a live production defect. It is registered because the helper's semantics are what a future caller inherits. *(The clause that once continued **"and because the same 'spans/sec' documentation governs the sibling `cell_rate_limit`"** is **WITHDRAWN at #1380** — `cell_rate_limit` is declared a span rate at plan `:978` and nothing contradicts it, so the two fields plausibly carry different quantities by design. See the close-out's step (5) for the surviving narrow form.)*
- **TWO mismatches, and time is the lesser one (round 4 [P2] + round 5 [P2]).** *(a)* **Quantity:** `observed_series` counts **distinct attribute-value series**; `tenant_rate_limit` is documented in **spans**/sec. These are different things, and a series may contain any number of spans — so no time normalisation alone reconciles them, and no claim about the error's *direction* follows until the quantity is settled. *(b)* **Time:** the window is dropped entirely. Both must be resolved, quantity first.
- **Why it would matter once called.** A threshold that silently depends on a caller-supplied string, over a quantity that does not match its declared unit, sits on C-OD-21 §21.4 per-tenant cardinality isolation — a **compliance** surface at the multi-tenant cells. The direction of the error is **not stated here**, because stating it requires the quantity decision above.
- **How it surfaced, and why it was not fixed in place.** Out-of-family Codex round 3, reviewing `B-182`'s behavioural witness for the tenant half of the §11.1 budget. That witness's purpose was to show the helper really implements the comparison (it does — it raises). *(Round 4 then withdrew the surrounding "enforced" framing: the helper has no production caller, so nothing reaches it — see the scope bullet above.)* Codex's point is that the witness, by passing a `"1m"` window, **pinned a dimensionally incorrect comparison as if it were correct**. The witness now supplies `"1s"` so its own assertion is coherent; the underlying mismatch is registered rather than repaired, because the repair is a contract choice.
- **Close-out steps — REWRITTEN at #1378; the registration's menu is superseded.** **(1) Re-ground** the three authority surfaces (the witness module does this and fails closed if any goes missing). **(2) DISCHARGED at #1380 — the fork doc is FILED** at `.harness/class_1_fork_b183_tenant_limit_quantity_declared_two_ways.md`. It routes **both** decisions (quantity and temporal) to the operator and **recommends neither disposition**. A first draft did recommend **(A) CARDINALITY**; out-of-family review [P1] showed that rested on downgrading v1.37/v1.38 to descriptive text. They are **independent normative use** — v1.37`:40` poses a still-OPEN keep-volume-vs-budget question *in spans/sec*, re-stated at v1.38`:69` — **A second draft then over-corrected**, ranking the spec passage over the plan via the `CLAUDE.md` §1.3 chain; review round 2 [P2] falsified that too — §1.3 orders artifact *levels*, not passages within one artifact, and v1.37 declares *"exactly ONE amendment"* (to C-OD-09) while listing **C-OD-11 §11.1 UNCHANGED** (`:106`), as does v1.39 (`:47-58`). **The stable shape is an INTERNAL CONTRADICTION inside the OD spec lineage:** `C-OD-11`'s contract body frames the cap as cardinality (and the plan and code implement exactly that), while two later deltas' **change-note prose** reasons in spans/sec while disclaiming any change to `C-OD-11`. **No authority rule breaks the tie** — resolution needs `ADR-D6` §1.3 (non-resident here) or a new OD amendment. (A)'s true price is that it **invalidates v1.37's population question and `B-133`'s owed step (3)**, which must be re-expressed, not re-worded. **(3) The TEMPORAL interpretation is a SECOND decision the same fork must settle — it does NOT follow mechanically** (corrected at codex round 2). Even under *cardinality* the limit could mean **standing/concurrent** cardinality, **distinct series per window**, or a **cardinality rate**; the carrier already records an `observation_window`, so settling the quantity alone would leave this row's original time mismatch unresolved. **(4) The three repair shapes (normalise / re-declare / type the window) are retained only as what the decision picks among**, not as an agent-executable menu. **(5) The sibling is DECOUPLED — this row's earlier text is corrected** (fork §6(ii)). `cell_rate_limit`'s plan comment at `:978` declares it a **span rate** and nothing contradicts that, so the two fields plausibly carry **different quantities by design**; what they share is only that v1.37 rider (a) describes both in one parenthetical. `B-182`'s open question (external enforcement vs gap) is largely independent. **The surviving gate is narrow and tenant-side only** (tightened at review round 3 [P2]): authorising a consumer for the **tenant** cap requires `B-183` settled first; authorising one for `cell_rate_limit` **alone does not** — that is `B-182`'s own call, and the fork's halt target deliberately excludes it. The exception is a disposition that unifies them: under **(B)** the tenant cap becomes a span rate too, so a delta choosing (B) must say whether it governs `cell_rate_limit` as well. **(6) OWED, impl-only follow-up** — `test_the_plan_signature_declares_spans_per_second` reads `:978`, the **cell** limit, inside a module about the **tenant** limit; the assertion is true and stays green but its stated relevance is wrong. Fork docs match the codex guard's `DESIGN_RE`, so the doc-only filing could not carry the test edit.
- **What would falsify it.** `observation_window` acquiring a reader in the comparison; the *"spans/sec"* documentation changing to a per-window count; or a cleared surface declaring the window advisory-only.
- **Council: SUPERSEDED again at #1380 — and the fork's own first answer here was wrong too.** That draft said no council was owed because the evidence favoured (A). With v1.37/v1.38 restored to independent normative weight the evidence is **two-sided**, and no authority rule breaks the tie (the contradiction is internal to the OD spec lineage). **A dyadic C7 ⊥ C10 convening is the recommended shape** — what a compliance cap measures at the two multi-tenant cells, against what the observability substrate can count and afford — after the fork's probes (1) and (2) are run.
- **GROUNDED 2026-08-16 — the quantity question is LOCATED, is NOT agent-decidable, and the FORK IS FILED (#1380). This bullet supersedes the #1378 text, which overstated the plan's role.** Seven tests at `harness-od/tests/test_b183_tenant_limit_quantity_conflict.py` pin the surfaces. **What #1378 got wrong:** it said *"the OD plan's own field signature says SPANS/SEC."* It does not — that comment sits on **`cell_rate_limit`** (`Implementation_Plan_Operational_Discipline_v2.md:978`), and the tenant field at `:977` carries **no unit at all**. **The corrected balance.** *Cardinality* is declared by §11's heading and contract surface, `C-OD-21` §21.4's row title, the plan at `:991`, the plan's **acceptance criterion for this very function** at `:2186` (*"when per-tenant **cardinality** exceeds `tenant_rate_limit`"*), and the counter's declared shape at plan v2_6`:721` (*"observed distinct attribute-value series"*) — which the shipped code carries **verbatim**. So the implementation **conforms to its plan**. *Spans/sec* appears in the implementation's own docstring (`per_cell_cardinality_budget.py:49-50,58`) **and in two cleared deltas that USE it normatively** — v1.37`:38-40` rules that the caps bound **admitted span throughput** independently of sampling and then leaves an **OPEN** question on *"whether the realized keep-volume approaches the per-tenant 1,000 spans/sec budget"*, re-stated at v1.38`:69` as `B-133`'s owed step (3). **A distinct-series bound would make that open question malformed**, so this is not an inherited label. *(It is not an override either: v1.37 declares C-OD-11 §11.1 UNCHANGED at `:106` and carries one amendment, to C-OD-09; §1.3 orders artifact levels, not passages within one artifact. The contradiction is INTERNAL to the OD spec lineage.)* A third naming — §11.1's *"per-tenant **rate limits** at OTLP collector boundary"*, echoed at §11.4 and `C-OD-21` §21.4's body — reads as the **enforcement mechanism**, not the quantity. **WHY IT IS STILL ROUTED.** The Phase-7-shaped repair is one docstring line, but taking it would put the code in direct contradiction with **two cleared spec deltas**, which a Phase-7 edit cannot amend — silent absorption per `CLAUDE.md` §4.3. **Also not resident:** `ADR-D6` §1.3, the cited root authority, is a bracket placeholder at `ADR-D6_v1_2.md:227-229`; the design-phase workspace holds it and reading it there is the fork's cheapest first probe. **The temporal decision stays open in the same pass** — standing cardinality, distinct series per window, or a cardinality rate.
- **Cross-ref.** `B-182` (the arc that surfaced it, and whose (C) half concerns the unconsumed sibling `cell_rate_limit`); `B-137` (the row `B-182` grounds).

### B-184 · `tools/` test-execution audit — 15 of 43 test modules ran nowhere; **14 are now wired (+128 tests)** and only 1 is excluded, on runtime *(surfaced by the dead-file finding at `B-167` / #1368, audited as a class 2026-08-16)*

- **What it is.** `#1368` found `tools/test_leg_selfcheck.py` **doubly dead** — neither importable nor wired — and its own next-action said to treat that as a **class**. This is the audit.
- **How coverage was derived, and why it matters.** By **parsing the workflow YAML's `run:` blocks** plus `codex-parity-check.sh` and the `justfile` — *not* by regex over raw text. Three successive naive matchers each gave a different wrong answer: a path-prefixed pattern (`tools/test_*.py`) missed CI's **bare-filename** invocations under `working-directory: tools`, and a continuation-line pattern mis-reported an already-wired file as dead. **The audit method was the hard part, not the sweep.**
- **Result.** **43** test modules on disk; **28** executed somewhere; **15 executed by nothing** — and **14 of those 15 are now wired**. **5 were plain dead weight** (`arc_metrics`, `closure_certification`, `docs_completeness`, `memory_closeout_check`, `q4_packaging_gate`). **9 more were mis-classified by this arc as credential/infra-gated — from their FILENAMES, not from reading them** (out-of-family Codex [P2] caught it). `r420` / `r421`×2 / `r430` / `r810` / `r820` / `sandbox_host_readiness` / `self_hosted_readiness` / `managed_cloud_readiness` in fact hold **64 unmarked, provider-free unit and static tests** that pass in **5.5s with every credential stripped** — exactly the environment the parity gate runs. They are now wired too. **Only 1 module stays out**: `test_codex_loop.py`, on **runtime** — its **16** tests cost **82s**, a **~46%** increase on a ~180s gate. **All 128 newly-wired tests pass; none was hiding a red.** Collection **proven 469 → 533 → 597**, each delta reconciling exactly.
- **A VENUE TRAP worth carrying.** `tools/` hosts **two import conventions**: **bare** (`import leg_selfcheck`, needing a `sys.path` insert or `cwd=tools`) and **package** (`from tools.q4_packaging_gate import …`, needing **`cwd=ROOT`**). Running `test_q4_packaging_gate.py` from inside `tools/` reports **4 FAILED**; from the root it passes **4/4**. This arc did exactly that and briefly recorded a red that does not exist — **an audit run from the wrong cwd manufactures failures.**
- **Close-out steps.** **(1) Decide `test_codex_loop.py`** — 16 tests, 82s, a ~46% increase on a ~180s gate: accept the cost, move it to a non-blocking or nightly lane, or profile it. **Do not silently leave it unrun**, which is the state this row exists to end. **(2) Make the audit a standing invariant** — a cheap guard enumerating `tools/test_*.py`, subtracting the executed set, and requiring every remainder to carry an explicit excluded marker with a reason. This arc's own mis-classification is the argument for it: **a filename is not a gate**, and nothing today distinguishes *deliberately excluded* from *forgotten*. **(3) Normalise the two import conventions**, or document the required `cwd` per file, so the next audit cannot manufacture failures.
- **The mis-classification, kept in view because it is the session's recurring error.** This arc read nine filenames (`*_live_e2e`, `*_readiness`) and inferred a gate. Measuring took one command. **Every failure in this session's four arcs has this shape: a claim asserted from a label or a convenient artifact instead of the thing that actually determines it.**
- **What would falsify it.** A new execution mechanism this parse does not read (a workflow using a matrix variable or a shell indirection to name test files), which would make the executed set larger than measured.
- **Council: no.** A tooling-coverage question settled by execution; the only judgement is step 1's runtime tradeoff, a cost call with a named number.
- **CLOSE-OUT (1) AND (2) DONE 2026-08-16 (#1372) — the sweep is now an INVARIANT, and nothing in `tools/` is unrun.** **(2) The standing guard** is `tools/tools_test_coverage_guard.py`: every `tools/test_*.py` must either be **executed** by a workflow `run:` block, the parity script or the justfile, **or** carry an `EXCLUSIONS` entry **with a written reason**. It is **two-way** — a stale exclusion (for a module now executed, or since deleted) fails too, because a list that only grows becomes its own drift surface — and it derives coverage by **parsing** workflow YAML rather than scanning text, with a test pinning that a *comment* naming a module does not count as running it. 14 tests, both directions per rule, mutation-probed (unwiring one module reds the two real-tree assertions). **(1) The `test_codex_loop.py` decision: it is NOT excluded from execution at all.** 16 tests at ~82s is a reason to move a module to a cheaper lane, not to stop running it — so it now has its own CI job (`tools-test-coverage-and-codex-loop`) and stays out of the pre-push parity gate on latency alone. **`EXCLUSIONS` is consequently EMPTY, which is the healthy state**, and the guard reports **44 modules / 44 executed / 0 excluded**. Parity collection **597 → 611**. **Residual: only close-out (3)** — `tools/` still mixes bare imports (needing `cwd=tools` or a `sys.path` insert) with `tools.`-package imports (needing `cwd=ROOT`), which is what let this arc briefly record a 4-FAILED result that does not exist.
- **CLOSE-OUT (3) DONE 2026-08-16 (#1374) — and it was a LATENT DEFECT, not a style nit.** The row described *"two import conventions"*; measurement found something sharper. `tools/` is **not a package** and pytest runs under **`--import-mode=importlib`**, so nothing puts that directory on `sys.path`. **Ten modules nevertheless passed in the parity gate — because an unrelated sibling happened to `sys.path.insert` first, earlier in the same invocation.** Run alone from the repo root they failed at collection; witnessed directly: `pytest tools/test_arc_ledger.py` alone **errors**, while `pytest tools/test_leg_selfcheck.py tools/test_arc_ledger.py` gives **97 passed**. That is an **order-dependent green** — silent, and it evaporates the moment the inserting sibling is renamed or reordered. **Measured both directions across all 44 modules:** 11 failed running from the repo root, 2 from `tools/`. **Fixed:** the 10 order-dependent modules now insert their own directory; `test_q4_packaging_gate.py` additionally passed **`Path.cwd()` as the repo root**, so it failed from anywhere but the root — now anchored to `__file__`. Both cwds are green. **`from tools.X import ...` is left alone** — measured working from both cwds, so it is a second legitimate convention, not a violation. **Made an invariant:** the coverage guard now also fails any module that imports a **sibling** without making `tools/` importable itself, detecting siblings **by resolution** (`tools/<name>.py` exists) rather than by a stdlib blocklist — an earlier blocklist draft fired on `harness_core` / `harness_as`, installed workspace packages, which would have flagged **eleven non-defects** and got the gate muted. 30 tests; mutation-probed (stripping one insert reds it, exit 1).
- **Cross-ref.** `B-167` (whose #1368 arc surfaced the first instance).

### B-185 · C1 raises **concurrent** tail-buffer occupancy by `1/base_rate`, cutting `max_buffered_traces` headroom ~10× at the 0.1-rate production cells *(measured at the `B-137` C1 build arc, 2026-08-16; split off `B-137` at its close so the residual stays queryable — bounded, zero evictions at the shipped 4096 default, and dormant on the same throughput denominator as `B-182` / `B-183`)*

- **What it is.** `B-137` step (3) landed candidate **C1**: `workflow.envelope` joined §9.2 as row 20, so every envelope-rooted trace is now head-admitted rather than only the base-rate fraction. Sequentially that is free. Concurrently it is not.
- **Measured** at `team-binding × self-hosted-server` (base rate 0.1), 100 traces, through the real `TailKeepSpanProcessor`. **Sequential:** 0 buffered / 0 evicted, even at a buffer cap of 3 — `B-136`'s name-arm repair materializes each trace and frees its slot at root close. **Concurrent (100 open at once):** peak buffered occupancy goes **9 → 100**. Occupancy scales by `1/base_rate` because every envelope-rooted trace now reaches the buffer.
- **What it costs.** Against the shipped `max_buffered_traces` default of 4096 (`types.py:741,752`) the eviction threshold at a 0.1-rate cell moves from roughly **40,960 to roughly 4,096 concurrent** envelope-rooted workflows. At the default cap the measured eviction count at concurrency 100 is **zero**.
- **What it costs above the threshold — CORRECTED; this row's first draft understated it.** The draft called the eviction harmless because C1 and the status quo both exported **8** ordinary children at cap 8. Out-of-family Codex round 2 caught that this compares *cardinality*, not *identity*. Re-measured: the base-rate sampler preserves a hash-drawn spread across the population, while C1 admits every envelope and `_evict_oldest_trace` (drop-oldest FIFO) keeps exactly the newest `cap` — so at cap 8 over 100 concurrent traces the baseline's survivors are almost entirely **displaced**. Above the threshold C1 causes a real per-trace loss the status quo would not have. Equal counts concealed it.
- **Why it is still not a ship blocker.** Displacement requires concurrency **above** `max_buffered_traces`. At the shipped 4096 default, measured at concurrency 100, the displacement set is **empty** and C1 strictly dominates (100 ordinary children preserved against the baseline's 13). Both halves live in one witness — `test_c1_displaces_previously_preserved_traces_under_buffer_pressure` — so neither can drift without reddening: the deterministic half as an exact set (C1's survivors are precisely the newest `cap`, the policy being FIFO), the displacement as a shape rather than a particular hash draw.
- **Why it is DORMANT.** Going live requires a production cell whose steady-state *concurrent* envelope-rooted workflow count approaches `max_buffered_traces`. That is a throughput figure — the same unmeasured denominator `B-182` / `B-183` need (`Persona_Document_v1.md` §11 open-item 4, still open). Re-ground when that lands; do not probe it independently.
- **Repair candidates, if it does go live.** Raise per-cell `max_buffered_traces` (config-only, re-prices memory), or make the §9.2 root-bypass arm release its buffer slot eagerly rather than at root close (mechanism change, needs its own measurement).
- **What would falsify it.** A measurement showing sequential-equivalent occupancy under real concurrency, or a mode-conditional sampler landing (candidate A) that moves the ratio into the tail consumer — which would change the admitted population and invalidate the 9 → 100 figure entirely.
- **Council: no.** A measured number with a named repair set, not a cross-domain tension. Its parent earned a council because A traded C7 against C11 with no measurement; here the measurement exists and points one way. What is missing is a throughput figure, which a council cannot supply.
- **Cross-ref.** `B-137` (parent; closed by the C1 build arc), `B-136` (the name-arm repair that made the sequential case free), `B-182` / `B-183` (share the blocking throughput denominator).

### B-186 · C1's §9.2 floor evaporates under an **unsampled ambient OTel parent** — the envelope is then not a trace root, so `ParentBased` short-circuits to the parent's DROP and never consults the name *(surfaced by out-of-family Codex against the `B-137` C1 build leg #1390, 2026-08-16; premise CONFIRMED by direct measurement and by tracing the MCP `copy_context()` path before acceptance — an adjacent venue `B-137` never scoped, not a reopening of it)*

- **What it is.** C1 (OD spec v1.42 §9.2 row 20) delivers the floor by making the envelope always-sampled at the HEAD, and `ParentBased` consults the inner sampler **only for roots**. The floor therefore holds exactly while `workflow.envelope` IS the trace root. Under an unsampled ambient OTel span the envelope is a **child**: `ParentBased` short-circuits to the parent's DROP without ever reaching `is_always_sampled`, and the whole trace — envelope plus every §9.2 member under it — disappears exactly as it did before C1.
- **Measured, not argued.** At `base_rate=0.0` through the real `TailKeepSpanProcessor`: the root-envelope composition exports `[hitl.gate.evaluated, workflow.envelope]`; the identical composition under an unsampled ambient parent exports **nothing**. Pinned at `test_c1_does_not_survive_an_unsampled_ambient_parent`, with a root-envelope positive control so the emptiness cannot come from a broken fixture.
- **Production-reachable, verified rather than assumed.** `workflow_driver.py:3458` opens the envelope with no explicit root context, and the MCP daemon path hands the ambient context to its worker — `lifecycle/mcp_server.py` dispatches via `asyncio.to_thread`, which inherits the caller's `copy_context()`, and the OTel current span lives in a `ContextVar`.
- **Not a `B-137` reopening.** That row measured, and its step-(3) ratification priced, the venue where the envelope is the trace root — it reasons throughout from *"`workflow_driver.py` opens `workflow.envelope` as the trace root"*, and its own scope statement is that the starvation is *"scoped to the members emitted inside the envelope."* C1 closes that venue completely. This is an adjacent venue never in scope, split to its own row so it stays queryable.
- **Why it was not repaired in the same arc.** The obvious fix — open the envelope as a **forced** trace root — is an emission-site change that would detach every embedded/MCP run from its caller's distributed trace. That trades the observability floor against trace continuity: an architectural decision, not a mechanical repair, so absorbing it would be a silent X-AL-3 design extension. The C1 delta declares ZERO emission-site change and keeps that promise.
- **Close-out.** (1) Establish the real population first — determine whether any *shipped* deployment invokes `api.run` / the MCP daemon under an ambient span that can be unsampled; the harness's own bootstrap does not, so this may be reachable only for an embedding host. (2) If real, the option set is a genuine fork: **(a)** forced root (floor guaranteed, embedded runs lose caller-trace continuity), **(b)** attach the parent as an OTel **link** rather than a parent (keeps association without inheriting the sampling decision — needs verification the exporter chain preserves links), **(c)** ratify that the §9.2 floor is scoped to harness-rooted traces and amend §9.3, the C-shaped option `B-137` declined for its own venue.
- **What would falsify it.** A measurement showing no shipped entry point can begin under an unsampled ambient span (dormant-by-construction), or a mode-conditional sampler landing (`B-137` candidate A) that moves the ratio into the tail consumer — under which head inheritance stops deciding the outcome and the bound dissolves.
- **Council: deferred, not declined.** The discriminator is step (1). Dormant-by-construction → none owed; venue real → (a) vs (b) vs (c) is a real C7-versus-trace-interop tension over a committed contract's reach. Ground first, convene second.
- **Cross-ref.** `B-137` (parent venue, closed at #1390), `B-185` (the other residual split off that close), `B-160` (the head=1.0 divergence class this floor belongs to).

### B-187 · merge-door refresh continuation lacks LINEAGE-BINDING — the §12.2.1 shape gate (title prefix + roadmap-status-only file set) accepts an OLD terminating-refresh PR from a prior landing *(surfaced by out-of-family Codex r9/r10 against the U-HE-23 landing driver #1414, 2026-08-21; register-and-held at the 10-round cap; adjudicated to the 3-lens merge gate)*

- **What it is.** `tools/merge_door.py`'s refresh identity gate (the fresh-callback validation and the `record-refresh` recovery verb) proves the pair is a REAL PR at the claimed head AND carries the CLAUDE.md §12.2.1 terminating-refresh shape (`REFRESH_TITLE_PREFIX` + `REFRESH_ONLY_FILE`). It does NOT prove the refresh is *for this landing*: any terminating-refresh-shaped OPEN PR — including a stale one minted for an earlier landing — passes, letting the lease release with the CURRENT landing's owed refresh never applied.
- **Why it was not repaired in the arc.** Binding the refresh to its landing needs a CARRIER the shape does not currently have — e.g. the refresh PR body/title naming the landed PR number, or a recorded landing-id the gate can compare — which is a contract-surface design decision (which artifact carries the lineage, who mints it, what the gate does on legacy refreshes), not an incremental guard. Absorbing it silently would be an X-AL-3-shaped extension of the refresh contract.
- **Bounding.** The window requires a stale terminating-refresh PR to exist OPEN at recovery time on the same repo; the arc-serial door venue and the one-refresh-per-landing discipline make that a recovery-path anomaly, and the failure mode is an unapplied refresh (caught by the next session-start drift audit), not corruption.
- **Close-out.** (1) Choose the lineage carrier (refresh PR body naming `landed #<pr>` is the cheapest greppable candidate); (2) extend the identity gate (both sites) to require it, with an explicit posture for legacy refreshes; (3) witness: a stale shaped refresh PR is refused, the correctly-annotated one accepted; (4) file the plan rev item on U-HE-23's note.

### B-188 · C-HE-07 raw-merge deny is a FREE-TEXT scan — loop-mode commands whose quoted data mentions `gh pr merge` (commit messages, PR comment bodies, echoes) are hard-denied *(surfaced by out-of-family Codex r2/r4 against the U-HE-25 guard unit, 2026-08-21; spec-verbatim predicate held; adjudicated to the 3-lens merge gate)*

- **Current state.** Registered, unrepaired: the spec-verbatim free-text predicate landed with U-HE-25 and denies quoted mentions in loop mode today; the `git commit -F` / `--body-file` workaround is the operative posture until the spec-level decision below is taken.
- **What it is.** The C-HE-07 §2 deny predicate is folded VERBATIM from the cleared spec — `(^|[[:space:]])gh[[:space:]]+pr[[:space:]]+merge([[:space:]]|$)` over the whole command string — so loop-mode commands whose quoted DATA merely mentions the phrase are hard-denied rather than asked: `git commit -m "route gh pr merge through the wrapper"`, `gh pr comment N --body "…gh pr merge…"`, `echo gh pr merge`.
- **Why it was not repaired in the arc.** Narrowing to an argv-position parse (after the permitted env prefix) would make the deny WEAKER than the spec-verbatim predicate — a Spec_HE_Loop_Lanes change-note decision (§4.3), not an in-unit guard edit. It is also the same free-text class as the sibling denies in the same block (force-push, secret-relocation scans), which are longstanding and carry the same workaround.
- **Bounding.** Loop-mode only (the guard is inert otherwise); the memory-documented workaround holds — `git commit -F <file>` / `--body-file` keep the phrase out of the scanned command string; a deny is loud (audited `loop_log DENY`), never silent.
- **Close-out.** (1) Spec-level decision: prescribe argv-position matching or ratify the conservative free-text scan (change-note either way); (2) if narrowed, land the parse in a reviewed guard unit with allow+hardening witnesses (quoted-mention commit → allow; real merge argv → deny); (3) record the workaround at the U-HE-29 activation-gate go-live witness where loop-mode friction first materializes.

### B-189 · shell mutation-probe annotations collapse per (script, target-file) — two same-file range annotations reduce to one coverage requirement *(surfaced by out-of-family Codex r9 against the U-HE-26 guard unit, 2026-08-21; not exploitable at HEAD — one annotation per target; register-and-held)*

- **What it is.** `required_probes()` reduces a shell suite's `# mutation-probe:` annotations to `(script, target path)` pairs and the pin-log key (`_pinned_nodeids`) likewise drops the line range, so a shell suite carrying two annotations for DIFFERENT ranges of one source file would stay coverage-green after re-pinning only one of them.

- **Current state.** Not exploitable at HEAD: every shell suite carries at most one annotation per target file (the U-HE-26 suite carries exactly one). The `lines` field is already present in every probe-log entry, so the migration is key-widening, not data backfill.

- **Close-out steps.** (1) Decide whether the pin key becomes `(test, file, lines)` across `required_probes` / `_pinned_nodeids` / `mutation_probe.log_result` — a lockstep three-carrier schema change; (2) if taken, add the two-ranges-one-file witness codex r9 named; (3) until then the one-annotation-per-target convention holds by inspection at review time.

- **Council.** NO — single-tool coverage-gate schema refinement; no cross-axis consumer; the only decision is the three-carrier key migration.

### B-190 · C-HE-08 §1 client-side push parser residual classes premised on adversarial local git config *(surfaced by out-of-family Codex r10 against the U-HE-26 guard unit at the 10-round cap, 2026-08-21; register-and-held; bounded by the C-HE-08 §2 server fence at U-HE-27 per R-20)*

- **What it is.** Two r10 classes share one premise — an actor already holding arbitrary git-config write: (P1) a remote NAMED with an embedded quote (`o'igin`) defeats dequote-then-lookup so a main-targeting `remote.o'igin.push`/`mirror` is missed; (P2) the bare-push config checks sample mutable shared state with no lock/CAS binding it to the approved execution, so a concurrent writer can retarget `remote.origin.push` at main inside the window. Folded in at U-HE-27 (merge-gate witness lens P3 on PR #1417, same client-predicate residual family): the bare-push predicate's `branch.<b>.remote` resolution tier has no dedicated guard-suite witness — untested-tier misresolution is bounded by the same server fence.

- **Current state.** Config-write is code-write in this workspace (the same actor could edit the hook itself), so both classes sit outside the client predicate's threat model (accidental/haywire-agent commands). Rounds r1–r10 measured the class as non-convergent client-side.

- **Close-out steps.** (1) Land U-HE-27 (server-side branch protection, C-HE-08 §2) — the fence closes both classes for `main` regardless of client parsing (invariant R-20); (2) record in U-HE-27's evidence log that B-190's residuals (P1/P2 and the folded lens-P3 untested `branch.<b>.remote` tier) are bounded by the fence; (3) no client-side narrowing owed.

- **Council.** NO — the terminal is prescribed by the cleared spec itself (C-HE-08 §2 + R-20); the only open work is the already-scheduled next unit.

### B-191 · C-HE-08 §2 apply/rollback transaction residual classes premised on concurrent REMOTE writers *(surfaced by out-of-family Codex r10 against the U-HE-27 recipes unit at the 10-round cap, 2026-08-21; register-and-held)*

- **What it is.** Two r10 residuals with no clean upstream primitive: (P1) the rollback CAS is a non-atomic read-then-DELETE — GitHub's branch-protection endpoint documents no conditional-write (If-Match/ETag) support, so a remote administrator writing between the compare and the DELETE can have their policy erased; the window is milliseconds and the local lockfile cannot serialize remote writers. (P2) `main-protection-rollback` after an apply that replaced a NON-null prior policy restores only the unprotected 404 pre-state; the captured prior payload lives in the evidence-log BEFORE block and is surfaced by an explicit NOTE, but is not auto-restored (would require parsing the evidence log as a data store).

- **Current state.** Nine absorption rounds (r1–r9, 34 findings) hardened every client-side-fixable class: digest-bound approval (repo, BEFORE, AFTER), common-dir lockfile, CAS re-read before rollback, ambiguous-PUT/merge reconciliation, fence-liveness + enforcement-witness probe arms, ownership-tracked timeout-safe GC. The remaining window presumes a concurrent trusted administrator — outside the X9 threat model (accidental/haywire-agent pushes), which the fence itself closes.

- **Close-out steps.** (1) If GitHub ships conditional writes for the protection endpoint, upgrade the rollback to If-Match; (2) optionally teach rollback to offer the evidence-log BEFORE payload as the restore target; (3) no further client-side narrowing owed — the class is non-convergent by the same measurement pattern as B-190.

- **Council.** NO — single-tool hardening residual; no cross-axis consumer; the fence (C-HE-08 §2 + R-20) is the systemic guard.

### B-192 · C-HE-06 §4(viii) door-driven refresh pointer-freshness posture on a missing draft *(surfaced by out-of-family Codex r15 against U-HE-28, 2026-08-22; register-and-held; merge-gate spec lens is the scheduled arbiter)*

- **What it is.** A held policy fork: when the door-driven `--emit-refresh-pr-json` finds NO `.harness/.next-action-draft` and no explicit `--next-action`, it lands the terminating refresh with the pointer untouched — the documented self-healing posture (the pointer's standing "then <next unit>" tail + C-HE-03 terminal-head dedup keep next-action derivation correct). Codex r15 reads CLAUDE.md §12.2 strictly: the refresh must carry a re-derived pointer, so a forgotten draft should fail closed.

- **Current state.** Held; the self-healing posture is live at HEAD (documented at ship-pr + the U-HE-28 rev note); the merge-gate spec-conformance lens on the U-HE-28 PR is the scheduled arbiter.

- **Why held.** Fail-closed here fails the refresh AFTER the irreversible content merge, BLOCKING the shared global door on every forgotten-draft landing (operator unblock + refresh-branch cleanup) — a heavy failure mode traded against a silent-but-self-healing stale pointer. An UNREPRESENTED or malformed draft already fails loud (r14 refusal + sanitized warning); only the fully-absent-draft case is at issue, and both postures are defensible.

- **Close-out steps.** (1) If the U-HE-28 merge-gate spec-conformance lens ALSO blocks on this (decorrelated agreement), flip to fail-closed in a follow-up arc and document the blocked-door recovery; (2) otherwise hold the self-healing posture with the dissent recorded here; (3) derivation is unaffected either way (terminal-head dedup).

- **Council.** NO — two-option policy fork with a decorrelated arbiter already scheduled (the 3-lens merge gate).

### B-193 · C-HE-06 §4(viii) converging recovery for a BEHIND refresh + the mid-landing-correction intent chain *(surfaced by out-of-family Codex r18/r19 against U-HE-28, 2026-08-22; register-and-held after an absorb-revert cycle)*

- **What it is.** A registered recovery-design gap: (a) if main advances during the refresh checks wait (an out-of-band human merge while the door holds the global lease), the refresh PR is BEHIND under strict protection: merges are refused, the door blocks, and no verb converges the recovery — the refresh record survives unblock and pins the old pair, `record-refresh` refuses an existing record, and the r18 baseRefOid pin WEDGED (every valid re-mint carries the NEW tip; reverted r19). (b) The r17 mid-landing-correction refusal exits after PR creation with an unresolved intent; `record-refresh` can attach the stale PR and a resumed land performs no draft-representation check.

- **Current state.** Registered; at HEAD the BEHIND class blocks via the ordinary post-attempt adjudication (the r18 base-pin that wedged is reverted) and the mid-landing-correction refusal stands; no converging verb exists yet.

- **Close-out steps.** (1) Design a record-clearing/re-mint verb (operator-gated) so the BEHIND class converges without hand-editing door state; (2) decide whether record-refresh should consult the draft channel (the operator running the verb holds the correction context — possibly the right answer as-is); (3) fold into the B-192 posture decision.

- **Council.** NO — single-mechanism recovery design; the U-HE-28 merge-gate concurrency lens is the scheduled decorrelated check.

### B-194 · C-HE-09 §3 lossy lane-id encoding (collision half) *(surfaced by out-of-family Codex r6 against U-HE-29, 2026-08-22; misclassification half fixed in the landing PR, collision half register-and-held)*

- **What it is.** `_loop_structured_col` STRIPS ` `, tab, newline, CR, `|` and `;` out of a lane id before writing it, because each of those corrupts the row or the parse (`|` splits the row, whitespace breaks the item-token split, `;` terminates the lane, newlines split the row across lines). The strip is destructive rather than an encoding, so two distinct legal ids differing only in stripped characters (`lane;evil`, `laneevil`) render identically — one lane can read as another.

- **Current state.** The MISCLASSIFICATION half is fixed at HEAD: `arc_exit_report._sanitize_lane` applies the identical strip to its own `.lane-id` before comparing, so a lane bearing those characters no longer has its own obligations classified as foreign. The collision itself is inherent to a lossy mapping and remains. Not exploitable at HEAD — lane ids are minted from a host+repo+random-suffix template that produces none of the stripped characters, so a colliding pair needs a hand-set `HARNESS_LANE_ID` or a worktree name carrying a separator.

- **Close-out steps.** (1) Decide the shape: REJECT unsafe ids at mint time (`reservations.py mint-lane-id`, where identity is created) plus a fail-closed writer, OR one reversible encoding (percent-escape the unsafe set); (2) move all three carriers in ONE commit — `_loop_structured_col`, `rowparse`, `arc_exit_report._sanitize_lane` — since a writer and reader disagreeing about the encoding is the same defect in a new place; (3) pin a witness that two ids differing only in unsafe characters cannot render the same lane.

### B-195 · U-HE-29 cutover-window concurrency vs a live pre-U-HE-29 writer *(surfaced by out-of-family Codex r8 against U-HE-29, 2026-08-22; register-and-held — (a) fails in the C-HE-09 §4 safe direction, (b) is not fixable from this side)*

- **What it is.** Two residual races in `loop_status_migrate`, both confined to the cutover window in which a checkout still running PRE-U-HE-29 code is actively writing. **(a)** The staleness check and the import append are not atomic, so a `RESOLVED-HIL` landing between them lets an imported legacy `DEFERRED-HIL` become physically last and reopen a just-answered gate. **(b)** A pre-U-HE-29 process holding an fd on the legacy inode keeps appending after the claim rename; a deferral written after the reduce hits EOF lands only in the `.migrated-*` archive, which no later pass inspects. A third instance of the same root cause was re-found at r9: the orphan fold reads the live file with `cat` and replaces it with `mv`, so a row appended between EOF and the rename sits on an inode the replacement unlinks.

- **Current state.** Held deliberately. (a)'s consequence is the SAFE direction under C-HE-09 §4 — over-skipping is safe, under-skipping re-loops — so the item merely stays in the skip-set and resurfaces at the next SessionStart for the operator to re-resolve; no data loss, no wrongly-retried gate. (b) cannot be closed from this side at all: the old writer takes none of our locks and holds the inode regardless of its name. Both windows close on their own once no pre-U-HE-29 checkout remains.

- **Close-out steps.** (1) For (a), hold the existing `.loop-status.lock` (the fd-8 flock the TTL pass already uses) across the check AND the conditional append so the decision and the write are one critical section; confirm the contention cost, given that lock is otherwise taken only by the TTL pass. (2) For (b), have later passes also scan `.migrated-*` archives for rows appended after their archive timestamp and re-import under the same staleness rule — the only mitigation available without the old writer's cooperation. (3) Re-assess whether either is worth building once the window has closed.

### B-196 · per-arc `todo_for_human` scoping needs a per-item lane SET *(surfaced by out-of-family Codex r3/r5/r11 against U-HE-29, 2026-08-22; register-and-held after three flips — HEAD annotates and never drops)*

- **What it is.** U-WT-03 says `todo_for_human` is what THIS arc leaves for a human, but since U-HE-29 the ledger is shared and `loop_pending_hil_list` returns every lane's open gates. Filtering to this lane was tried twice and hid a real row both times: excluding every lane != mine dropped this arc's own UNATTRIBUTED rows (`defer.sh` writes no lane; migrated rows carry `-`), and excluding only demonstrably-foreign lanes still hid rows because the reducer collapses by ITEM TOKEN — when two lanes defer the same item, only the last deferrer's row survives, so another lane's rendering can be the sole trace of an item this lane also has open.

- **Current state.** The rendered lane is not evidence of sole ownership, so no exclusion built on it is sound. At HEAD the function ANNOTATES and never drops: it states the lane split in the notes and lists every row. Over-reporting a sibling's obligation is the safe direction; hiding this arc's own gate in the record that closes it — immediately before worktree disposition — is not.

- **Close-out steps.** (1) Decide whether C-HE-09 §3's rendered form should carry the SET of deferring lanes (`[L1,L2] item — detail`); that is a spec change and routes through design-phase back-flow, not an execution-time edit. (2) If the rendering stays singular, decide whether `arc_exit_report` should make a SECOND query for per-item lane membership rather than reading the rendering — weighed against the one-parse-one-authority rule. (3) Either way, keep annotate-never-drop until the chosen mechanism can prove sole ownership.

### B-197 · worktree GC can reap mid-drain (`.migrating-*` inside the `.harness/` ignore class) *(surfaced by the merge-gate concurrency lens on #1426, 2026-08-22; register-and-held — the raced outcome EQUALS the pre-U-HE-29 baseline, so this PR narrows exposure rather than creating loss)*

- **What it is.** `loop_status_migrate` claims a legacy ledger by renaming it to `.harness/loop_status.md.migrating-*`. `_HOOK_REGEN_IGNORED` (`tools/hooks/lib.sh:479`) matches `\.harness/`, so `hook_worktree_local_state` reports the worktree CLEAN — and a concurrent `loop_gc_worktrees reap`, which takes a removal mutex the migration never acquires, can delete the worktree mid-drain. The restore-on-failure `mv` then no-ops on the vanished path and the row reaches neither the shared venue nor disk. A third instance (merge-gate re-gate): the orphan-FOLD step runs BEFORE the claim-first rename with no serialization of its own, so two migrators seeing the same dead-owner orphan can each fold+republish+re-claim it — a duplicate `DEFERRED-HIL` and two archives for one orphan. Safe-direction under §4 (never a false `RESOLVED-HIL`), so log-noise, but it shows the claim-first guarantee does not span the whole function. A fourth instance: `loop_resolve`'s pre-write byte offset narrows the search but cannot EXCLUDE a concurrent append — if this process's write fails while another appends a byte-identical row in the same window, the grep succeeds and a false "resolved" is reported. A second, lower finding: `arc_exit_report.append_ledger_row` verifies its write by substring-in-tail rather than from a pre-write byte offset, so under the shared venue a concurrent duplicate EXIT-REPORT row for the same PR could make it report success when its own append failed.

- **Current state.** Held, with a severity correction the lens did not make: `origin/main` **already** gitignores `.harness/loop_status.md` (`.gitignore:149`), so the GC already reaps worktrees holding open `DEFERRED-HIL` rows. The raced outcome therefore **equals the pre-U-HE-29 baseline**, while every un-raced drain is strictly better (rows become durable in the shared venue). This PR narrows the exposure and adds one interrupted-drain window; it does not create the loss.

- **Close-out steps.** (1) The fix is a **decomposition** of the ignore class, not a reversal of either prior decision: `.migrated-*` is a permanent archive and must STAY ignored (codex r5 P2 — untracked residue wedges the worktree GC forever), while `.migrating-*` is a transient in-flight claim that should block reap while its owner is alive; `_loop_pid_alive` already exists for that test. This edits shared GC machinery (`_HOOK_REGEN_IGNORED` / `hook_worktree_local_state`) with its own suites, which is why it was not improvised at merge time. (2) For the growth check, scope to a pre-write byte offset **and** add a per-call nonce — byte-offset scoping alone cannot distinguish a byte-identical concurrent row. (3) Re-assess once no pre-U-HE-29 checkout remains: the migration becomes dead code then and the window closes with it.

### B-198 · pre-U-HE-29 legacy ledgers carry nothing under their own semantics — no drain built *(measured during the U-HE-29 PR, 2026-08-22; the migration built there was CUT on this measurement, and B-195/B-197 closed with it)*

- **What it is.** U-HE-29 moved the HIL ledger to a shared venue (C-HE-09 §2). A cutover migration was built during that PR and then **cut before merge**, on measurement.

- **The measurement, recorded so nobody re-derives it.** Reducing a legacy per-worktree ledger with the NEW no-ACTIVATE-reset rule (C-HE-09 §4) is **not evidence** about what it holds — those rows were *written* under the old rule, where `ACTIVATE` cleared prior-run state. On this workspace the root checkout's ledger (3204 lines, 18 `ACTIVATE` rows) reduces to `B-124 B-137` under the new rule and to **nothing** under the rule its rows were written with. The three other worktree ledgers hold 1 row each, same story. The live `--dry-run` confirmed **0 files to import**.

- **Why the machinery went with it.** ~318 lines of concurrency-sensitive code (atomic claim-by-rename, orphan recovery, pid liveness, NUL enumeration, timestamp staleness, a five-value exit taxonomy) existed to migrate nothing, and generated roughly 20 of that PR's 23 review rounds. `B-195` and `B-197` closed alongside it: every race they recorded was a property of the migration, and with the migration gone there is no code left to race.

- **Close-out steps.** (1) BEFORE building any drain, re-run the measurement on the actual checkouts — reduce each `<worktree>/.harness/loop_status.md` under LEGACY semantics and count what is genuinely open. Zero means nothing to build; say so and stop. (2) Only a checkout that genuinely holds an open row warrants a drain, and then scope it to *that* case — the general cross-worktree form is what carried the concurrency surface. (3) The old files are untouched on disk and readable directly, so nothing is lost by not draining. (4) If a drain is ever built, read `B-195`/`B-197` first: they record the failure modes it will have to answer.

### B-199 · `merge_door._notify` cannot durably record a DEFERRED-HIL when every write fails *(surfaced repeatedly by out-of-family Codex across the U-HE-29 PR, 2026-08-22; register-and-held — raising would wedge the global merge-door lease, so release-safety has to come first)*

- **What it is.** C-HE-20 §1 makes a blocking escalation MUST-durable in the HITL queue and forbids a *new* escalation store. `_notify` retries a `DEFERRED-HIL` write 3× — which converts the common transient failure into the durable row the contract wants — and, if every attempt fails, prints a loud `ROW LOST` line naming the contract and returns. It does **not** raise.

- **Why it does not raise, which is a trade rather than an oversight.** This module has **no `finally:`** releasing the lease around the 8 `DEFERRED-HIL` call sites, so an exception escaping `_notify` can leave the **global** merge-door lease held — wedging every lane. That swaps a recoverable *reporting* failure for an unrecoverable *coordination* one. The residual is real and precisely bounded: a persistent write failure leaves the escalation only in that stderr line, invisible at the next SessionStart.

- **Close-out steps.** (1) Make the call sites release-safe FIRST — wrap the lease-holding region in `try/finally` so an exception cannot strand the lease; only then is propagation affordable. (2) Then propagate on the MUST-durable kind (`DEFERRED-HIL`) while keeping tolerance for the informational `NOTIFY`, which §1 itself distinguishes. (3) Do **not** add a second escalation store as a fallback — §1 forbids it, and the lease sidecar is not a HITL queue. (4) Witness release-safety directly (force a raise mid-landing, assert the lease is released), not by inferring it from tests that never raise.

### B-200 · C-HE-11 §1 per-lane telemetry endpoints — the lane's stack is isolated, its consumers still dial lane 0 *(surfaced by out-of-family Codex r2 against U-HE-31, 2026-08-23; registered, not blocking the unit)*

- **Current state.** REGISTERED, not blocking. U-HE-31 landed the isolation half in full (per-lane project name + the four host ports, witnessed); no consumer has been rewired yet, and nothing is scheduled ahead of the remaining S4 units.

- **What it is.** U-HE-31 gives lane `k >= 1` its own Compose project and its own host-port block, and the `R420_PORT_*` exports carrying those numbers live only inside the `r420-self-hosted-stack-{up,down,status}` child shells. Nothing else in the workspace reads them: the self-hosted config surface and the R-420 live-E2E recipes still name `4317` / `3200` / `3000` literally. A `k >= 1` lane therefore brings up a correctly ISOLATED stack that its own harness run does not talk to — it exports to lane 0's collector, or, with no lane 0 up, skips or fails.

- **Why it is not a defect in the landed unit.** C-HE-11 §1 is a contract about bringing the stack up without colliding: a per-lane `-p` project and the full host-port set `{3000, 3200, 4317, 4318}`. That half is met and witnessed. The CONSUMER half is not in the contract's text and not in the unit's Files list, so wiring it inside U-HE-31 would have been a silent scope extension. It is named here instead, which is what the 3–4-lane pilots (C-HE-13 §3) will need the moment a lane wants to read its own traces.

- **Close-out steps.** (1) Choose the carrier — a lane-aware OTLP endpoint resolved at config load from `HARNESS_LANE_INDEX` / `R420_PORT_OTEL_GRPC`, versus per-lane config files. (2) Thread it through the R-420 live-E2E recipes and `docs/how-to-deploy.md`, whose port literals are the other half of the gap. (3) Witness with a two-lane run whose traces land in that lane's OWN Tempo, not lane 0's. Blocked on nothing; scheduled behind the remaining S4 units.

### B-201 · C-HE-11 §2 `gc.auto 0` — warn-and-continue vs refuse-to-open when the shared git config cannot be written *(raised three times by out-of-family Codex against U-HE-31, 2026-08-23; register-and-held on the documented reading; CLOSED at U-HE-32, 2026-08-24)*

- **Current state.** CLOSED. The hold named exactly one close-out condition — U-HE-32's bounded retry — and it has landed. `tools/hooks/lane-init.sh` now writes `gc.auto 0` through `hook_git_retry` (C-HE-11 §3: full jitter over base 100 ms, factor 2, cap 5 s, 8 attempts), so the ordinary way this write loses — a concurrent lane holding `.git/config.lock`, which clears in milliseconds — no longer reaches the warning at all. What still warns-and-opens is the residue after an exhausted budget: a config that is genuinely unwritable rather than momentarily contended, where refusing every lane in the workspace over a hygiene knob remains the worse trade. The reviewer's objection was to an unretried MUST; there is no longer one. The refuse-vs-warn question for that NON-transient residue is not dropped with this closure — it is split to **B-205**, which is where it is now tracked.

- **What it is.** C-HE-11 §2 says `gc.auto 0` MUST be set repo-wide, once, idempotently by whichever lane-init runs first. `tools/hooks/lane-init.sh` attempts it, and when the write fails (a locked or unwritable shared config) it prints a named warning to stderr and the lane still opens. The reviewer's position, raised in rounds 4, 5 and 6, is that a failed MUST should refuse to open the lane.

- **Why it is held rather than absorbed.** Two things separate this from the other MUSTs in the same file, both of which DO fail closed (an unpersistable lane identity, an unallocatable lane index). First, its failure mode is bounded and named: automatic GC during concurrent lane work causes local ref/index lock contention, which is exactly what C-HE-11 §3's bounded retry helper covers — `hook_git_retry`, landing at U-HE-32, the very next unit. Second, the blast radius of failing closed is worse than the hazard: an unwritable git config would make every lane in the workspace un-openable over a hygiene knob, converting a degraded-but-workable environment into an unusable one. The failure is loud (stderr, named, pre-action), so nothing is silently absorbed.

- **Close-out record (U-HE-32).** (1) DONE — `hook_git_retry` bounds the contention, and the witness is `tools/hooks/test_lib.sh`'s config-lock case, which exists because git reports this shape as `could not lock config file …: File exists` with no `.lock` substring anywhere: the plan's own suggested pattern, written from the index/ref wording, would have missed the very write this row is about. (2) is not a residual of this row and needs nothing built — it is a conditional re-open: if the 3–4-lane pilots ever record a `cause_signature` naming lock contention recurring across runs (C-HE-13 §3), that is an ordinary pilot finding and fail-closed gets revisited then. (3) no further narrowing owed.

### B-202 · C-HE-11 §1/§5 lane-teardown and headroom residuals that need a primitive this unit does not have — a claim↔cleanup lock, and available-memory INSIDE the Docker VM *(surfaced by out-of-family Codex across U-HE-31 rounds 9–11, 2026-08-23; registered at the 11-round cap)*

- **Current state.** REGISTERED at U-HE-31's 11-round review cap. Every cheap fix around both residuals has landed; what remains needs a primitive outside this unit, and neither residual has been observed — both are reasoned.

- **What it is.** Two residuals. (1) **Claim↔cleanup interleaving.** `hook_release_lane_index` re-reads the claim's owner immediately before AND after `docker compose down --volumes`, so a rebind by a worktree recreated at that path is detected and its claim left alone. What the re-reads cannot do is un-destroy containers: if the rebind lands inside the gap between the pre-check and the compose call, the old release destroys the new owner's stack. Closing it needs a lock shared by the release path and lane-init's claim path — a primitive neither has, and one that would put a lock acquisition inside the worktree-removal hook. (2) **Available memory inside the Docker VM.** C-HE-11 §5 asks for "available memory / Docker-VM headroom". Lane-init checks the VM's `MemTotal` ceiling AND host availability, both against the stack's need; what no Docker API exposes is the VM's *free* memory. A VM sized adequately but already consumed passes both checks. Measuring it means running a probe container (`docker run --rm … free`), which needs an image pull inside a sourced shell hook.

- **Why it is registered rather than fixed here.** Both wanted a mechanism outside this unit's Files list, and rounds 9–11 converged on them precisely because the cheap fixes around them had all landed. Their blast radius is bounded: (1) needs a worktree reaped and recreated at the same path inside one teardown window; (2) surfaces as containers failing to start, at the moment of use, which is loud even if it is not the pre-flight refusal §5 prefers.

- **Third residual, added at round 15 after a fix for it was built and then backed out.** A lane reaped between its worktree's deletion and its claim's release leaves a claim nothing retries — that index is consumed for good. U-HE-31 tried to close this by letting the allocator reclaim any claim naming a path that no longer exists. Out-of-family review then showed the cure was worse: the reclamation deletes by pathname after an UNLOCKED observation, so two initializers seeing one dead claim can have the second's stale `rm` delete the first's newly published live claim (P1), and "the directory is absent" is not proof of death at all — the removal path MOVES a live worktree to quarantine and can restore it, so the scan reclaims a lane that is coming back. Both need the same claim↔cleanup lock as residual (1), which is why the reclamation was reverted rather than patched: an unlocked half-fix loses a live claim, where the leak it addressed only wastes an index. Whichever lock closes (1) closes this too — one primitive, three symptoms.

- **Fourth symptom, and the one that names the shape of the whole row.** The post-verify duplicate-claim withdrawal in `lane-init.sh` cannot enforce "one worktree, one claim" without the same lock. Three review rounds each defeated a lock-free rule with a new interleaving: a symmetric "the peer's stands, mine goes" has both racers delete and leaves ZERO claims; a two-way tie-break on the index leaves two standing under a three-way race, because `lanes/*` sorts lexicographically and the middle claim never compares against the true minimum; comparing against the MINIMUM fixes that but still cannot help a source whose scan runs before any peer has published — it sees no duplicate, keeps its claim unconditionally, never re-checks, and a later lower-index source then correctly declines to withdraw, so both stand. Each rule is strictly better than the last and none of them is sufficient, because scan-and-decide without synchronisation is not a consensus primitive. The minimum rule is retained (it converges every case where the racers see each other, and can never leave zero) and the code now states that bound instead of claiming the invariant.

- **Close-out steps.** (1) Give the lane registry a per-index lock (the `mkdir` primitive lane-init already uses for corpse repair) and take it in BOTH `hook_release_lane_index` and lane-init's claim path — one lock, three callers, since the post-verify withdrawal needs it too; (2) for headroom, decide between a probe container behind an explicit opt-in and treating the VM ceiling as the contract's practical reading, and record the decision against C-HE-11 §5; (3) the 3–4-lane pilots (C-HE-13 §3) are the measurement that says whether either is worth the mechanism — neither has been observed, both are reasoned.

### B-203 · `hook_git_retry` does not reach the skill-side lane git operations — a shell function cannot survive an agent's Bash tool call, and the guard does not admit a `source … && hook_git_retry …` shape *(surfaced by out-of-family Codex r1 against U-HE-32, 2026-08-24; registered, not blocking the unit)*

- **Current state.** REGISTERED at U-HE-32 and HELD through four out-of-family raises (r1, r2, r3, r4 — it was the ONLY finding left standing at r4, every other having been absorbed). The hold is not a disagreement with the observation, which is correct: the hook-side consumers named by the unit's Scope are wired, the skill-side ones are not, and a registration is not an implementation. It is a statement about ownership — the close-out needs two surfaces this unit does not touch, named below.

- **What it is.** C-HE-11 §3 says local ref/index lock collisions MUST be retried. `hook_git_retry` delivers that for every git mutation that runs *inside a hook script* — `lane-init.sh`'s `gc.auto 0` write and the two worktree mutations `safe-worktree-remove.sh` drives. It does not cover the git commands an *agent* runs from a skill: `ship-pr` and `two-lane` issue raw `git add`, `git commit` and `git worktree add`, and those contend for `index.lock` exactly like the wrapped ones do.

- **Why it is registered rather than fixed here.** Two mechanisms are missing, and neither is this unit's surface. (1) A shell function does not survive across an agent's Bash tool calls — each call is a fresh shell — which is the same reason `roadmap-continue/SKILL.md` tells an agent to restate `HARNESS_ARC_ID`/`HARNESS_LANE_ID` inline on every command rather than exporting them once. So a skill cannot simply be told to "call `hook_git_retry`"; the guidance would be inert. (2) The only shape that would work is a chained `source tools/hooks/lib.sh && hook_git_retry …`, and the permission guard auto-allows bare git but not that chain, so in loop mode every wrapped call would stall at ask-then-deny. Closing this needs a guard-allowlisted invocation shape — a permission-guard change, which is where U-HE-25's surface lives, not U-HE-32's.

- **Close-out steps, and who owns them.** (1) Decide the invocation shape — a `just` recipe is the obvious candidate, because a recipe name is a single clean token the guard can admit where a `source … && …` chain is not; (2) admit it in `tools/hooks/permission-guard.sh` with its own assertions — that is the **U-HE-25** surface, the reviewed permission-guard unit; (3) convert the raw git mutations in `ship-pr`/`two-lane` to it — that is the **U-HE-39** surface, the skill-carrier doc sweep, which also owes the wiring-suite update any `SKILL.md` change requires. Doing (2) and (3) inside U-HE-32 would be a silent scope extension across two other units' Files lists, which is the thing the plan's Files discipline exists to prevent.

- **Blast radius while it is open.** Bounded and loud. An unretried collision fails the agent's git command visibly, and the agent re-runs it; nothing is silently corrupted and no ledger row is lost. That is materially different from the hook-side case this unit did close, where the same collision happened inside an unattended script with no one to re-run it.

### B-204 · `reservations.py update --set` coerces a valid abbreviated sha to a float, so `head_sha=<hex that reads as scientific notation>` is refused as `inf` *(surfaced in-lane while back-filling U-HE-32's reservation, 2026-08-24; registered)*

- **Current state.** REGISTERED at U-HE-32.

- **What it is.** `uv run python tools/reservations.py update --arc-id u-he-32 --set head_sha=65106e2179` — a valid 10-character abbreviated sha — is refused with `head_sha must be a 7-64 char lowercase-hex sha/oid or null (C-HE-03 §3), got inf`. The `--set` parser coerces numeric-looking values before the validator sees them, and `65106e2179` is legal scientific notation (`65106 × 10^2179`), which overflows to `inf`. Any abbreviated sha of the form `<digits>e<digits>` hits it; the full 40-character form usually does not, because it almost always carries a non-`e` hex letter.

- **Why it matters more than the cosmetics suggest.** The failure is loud here, but it is loud only because the validator happened to reject `inf`. The same coercion runs over every `--set` value, so a field whose validator *accepts* a float would take the coerced value silently. It is also a shape that fails intermittently by content rather than by code path — the reason it surfaced at all is that one particular commit's short sha happened to read as a float.

- **Close-out steps.** (1) Keep `--set` values as strings and let each field's own validator do any conversion it wants; (2) add a case pinning `head_sha=<digits>e<digits>`, which is the fixture that would have caught it.

### B-205 · C-HE-11 §2 `gc.auto 0` — refuse-to-open vs warn-and-continue once the write fails a NON-transient way, i.e. after U-HE-32's retry is exhausted *(split out of B-201 at its closure so the surviving obligation stays tracked; out-of-family Codex r2 against U-HE-32, 2026-08-24)*

- **Current state.** REGISTERED. This is the half of B-201 that U-HE-32 did NOT settle, split to its own row so closing B-201 does not bury it (out-of-family Codex r2 made exactly this objection to the closure, and it was right).

- **What it is.** C-HE-11 §2 makes `gc.auto 0` a MUST. B-201 held on warn-and-continue with one named close-out: U-HE-32's bounded retry, which has now landed — `lane-init.sh` writes through `hook_git_retry`, so transient `.git/config.lock` contention no longer surfaces at all. The question that survives is narrower and genuinely different: when the write fails *after* an exhausted budget — an unwritable or corrupt shared config, not a peer lane holding the lock for milliseconds — should lane-init still open the lane?

- **Why it is a real question and not a formality.** Both readings have force. The MUST is unqualified, and every other MUST in `lane-init.sh` fails closed (an unpersistable lane identity, an unallocatable lane index). Against that: the blast radius of refusing is that an unwritable git config makes EVERY lane in the workspace un-openable over a hygiene knob, converting a degraded-but-workable environment into an unusable one — and the hazard `gc.auto 0` removes is itself lock contention, which `hook_git_retry` now absorbs. What separates this from B-201 is that the transient case is gone, so the decision is no longer being made against a failure mode that mostly was not real.

- **Close-out steps.** (1) The 3–4-lane pilots (C-HE-13 §3) are the measurement: a `cause_signature` naming lock contention recurring across runs says warn-and-continue is too permissive; its absence says the retry settled it. (2) Decide refuse-vs-warn against that evidence rather than against the unqualified MUST alone, and record the decision at C-HE-11 §2. (3) Nothing is owed before the pilots — this row exists so the question is not lost, not because work is queued behind it.

### B-206 · `hook_git_retry` inside the worktree-removal mutex can add up to ~23 s to a critical section whose waiters time out at 30 s *(surfaced by the merge-gate concurrency lens against U-HE-32, 2026-08-24; registered — the measurement is the close-out, not a code change)*

- **Current state.** REGISTERED at U-HE-32, and CORROBORATED INDEPENDENTLY. The sibling half of the same finding — the signal-handler path racing a 2 s SIGKILL — WAS fixed in the unit (the trap now restores fail-fast at one attempt). This half was reached separately by two decorrelated reviewers in the same round — the merge-gate concurrency lens and out-of-family Codex r5 — arriving at the SAME arithmetic (≤ 22.6 s of added sleeps against a 30 s waiter timeout) from different starting points. Agreement between decorrelated reviewers is the strongest confidence signal available here, so this row is a high-priority registration rather than a speculative one; what it still lacks is a measurement, not credibility.

- **What it is.** `hook_safe_worktree_remove` holds the worktree mutex from `tools/hooks/lib.sh:1024` to `:1123`, and BOTH retried git calls sit inside it: the `worktree move` at `:1077` and the `worktree remove` at `:1099`. Each can now spend a full C-HE-11 §3 budget — seven jittered waits with ceilings 100/200/400/800/1600/3200/5000 ms, so **≤ 11.3 s worst case, ~5.6 s expected**. Out-of-family review r7 sharpened the count: the RESTORE path is a third retried call inside the same span, so the worst case is **three** budgets, **≤ 33.9 s**, not two. That crosses a threshold — 22.6 s merely ate most of a waiter's allowance, 33.9 s **exceeds it outright**. Concurrent waiters on the same mutex — SessionStart lease registration at `:488`, `:526`, `:561` — block for `HARNESS_WORKTREE_LOCK_TIMEOUT_SECONDS`, default **30 s** (`:428`). So a removal that hits sustained contention can push a concurrent lease registration past its acquire timeout, where before this unit it could not — and at three budgets it does so with room to spare rather than marginally.

- **Why it is registered rather than fixed here.** The retry only ENGAGES under contention — with no collision it adds nothing, and the worst case needs sustained lock contention across two separate git operations, which is a machine already in trouble. Both plausible fixes are design changes rather than tuning: shrink the mutex span so the retried calls sit outside it, or make the budget lock-aware (a caller holding a mutex passes a smaller `HOOK_GIT_RETRY_MAX_ATTEMPTS`, the seam the signal-handler fix already introduced). Picking between them without a measurement would be guessing, and *lowering* the budget inside the mutex would put a number below the C-HE-11 §3 MUST on a path the contract governs.

- **Relationship to B-202.** B-202 already owns the lane-registry lock work; this row is adjacent but distinct — B-202 is about a MISSING lock, this is about an EXISTING lock being held longer. If the B-202 lock work reshapes this critical section, close them together.

- **A third remedy, grounded while registering this row.** The 30 s bound is NOT contract-pinned — it appears nowhere in `Spec_HE_Loop_Lanes_v1.md`, only as a `${HARNESS_WORKTREE_LOCK_TIMEOUT_SECONDS:-30}` default at `tools/hooks/lib.sh:447` — so raising it is available without touching any MUST. It is NOT obviously right, which is why it is recorded rather than applied: a larger bound removes the spurious failure but makes a genuinely WEDGED holder that much slower to surface, and callers that deliberately pass 1 (`codex-session-end.sh:10`) show the value is already used as a policy knob rather than a constant. Which failure is worse is what the measurement below decides.

- **Close-out steps.** (1) During the 3–4-lane pilots (C-HE-13 §3), record whether any lease registration actually times out, and whether any `worktree move`/`remove` ever exhausts its budget at all — both are currently reasoned, neither observed; (2) if it fires, choose between shrinking the span and a lock-aware budget, and record the decision against C-HE-11 §3; (3) nothing owed before that measurement.

### B-207 · `hook_git_retry`'s stderr-capture temp file leaks when a signal trap `exit`s mid-retry *(surfaced and measured by the merge-gate concurrency lens against U-HE-32, 2026-08-24; registered — the lens itself judged it register-not-block)*

- **Current state.** REGISTERED at U-HE-32.

- **What it is.** `hook_git_retry` captures git's stderr to a `mktemp` file (`tools/hooks/lib.sh`, `errfile=$(mktemp)`) and removes it on the way out of the function. The removal is only reached by a normal `return`. `_hook_worktree_interrupted` terminates the process with `exit` straight from the HUP/INT/TERM trap, so a signal arriving while the retry is mid-backoff skips the cleanup and strands the file. Measured, not reasoned: the lens held `.git/index.lock` permanently, sent SIGTERM mid-backoff, and counted exactly one surviving suffix-less `tmp.*` entry under `$TMPDIR` (4353 → 4354).

- **Why it is registered rather than fixed.** One small file per INTERRUPTED teardown, in a directory the OS reaps; nothing about correctness, the retry contract, or the restore depends on it. The obvious fixes each cost more than the leak: an `EXIT` trap inside a library function would clobber whatever trap the caller installed (and `hook_safe_worktree_remove` installs three), and a deterministic per-PID path trades the leak for a predictable name in a shared temp directory. Note the leak became REACHABLE only because the trap now fires promptly — before the `sleep … & wait $!` fix the signal was deferred to the end of the sleep, so this is the small cost of a much larger correctness win.

- **Close-out steps.** (1) If a fix is wanted, the cheap shape is a per-invocation directory created under a caller-owned root and removed by the same code that owns the trap, rather than a trap inside the library; (2) otherwise close as WONTFIX once the pilots confirm no operator ever notices it. Nothing is owed before then.

### B-208 · the shared HITL loop ledger was reachable from a test process — 6288 of its 6589 rows were synthetic *(surfaced while clearing the U-HE-32 refresh gates, 2026-08-24; CLOSED at #1443 the same arc)*

- **Current state.** CLOSED at #1443, the arc that found it.

- **What it was.** `loop_status_path()` (`tools/hooks/loop_lib.sh:31`) resolves the loop ledger from the ambient environment and falls back to the operator's real `~/.gstack/projects/arhugula-v2/loop_status.md` whenever `HARNESS_LOOP_STATUS_PATH` is unset. Unset is the default state of every pytest process, so any test reaching a real emit path appended **real** `DEFERRED-HIL` rows to the venue the SessionStart hook reads. Measured on `main` at `e6913c112`: **6288** synthetic `lane=A` fixture rows and only **82** from real lanes, out of 6589 lines / 1.07 MB — deliberately *not* stated as a partition, since the balance is `lane=-` infra rows plus the file's own header (the first phrasing read as an exhaustive split and did not reconcile, 6288+82=6370; the round-5 lens caught it — the fourth claim in this arc to be checkable and wrong) — and a fabricated `pr-1 — pr #1: gh transport exploded` gate (pr #1, placeholder shas) nagging the operator at every session start. Because the ledger is append-only by contract (C-HE-09 §1, the single-file atomic-append guarantee; §2 is venue determinism), the debris is not removable after the fact; only a hand-written `RESOLVED-HIL` clears the nag.

- **How it was confirmed.** Reproduced, not inferred: running the single door test `test_raw_exception_after_attempt_blocks_lease` with `HARNESS_LOOP_STATUS_PATH` pointed at a scratch file emitted that exact nagging row into the scratch file — establishing both that the test really writes and that the environment variable is the lever.

- **Why the fix is not in `test_merge_door.py`.** Per-file isolation is what the workspace already tried and what drifted: `test_arc_exit_report.py` and `test_review_wrapper.py` remember to set the variable, `test_merge_door.py` does not, and a file written tomorrow will not either — correctness that depends on each author remembering an env var is ambient coupling, not a boundary. `test_merge_door.py` does monkeypatch `emit_loop_row` in most tests, so the hazard was seen; but several of its tests drive `merge_door.py` as a **real subprocess**, and monkeypatching a Python seam cannot cross a process boundary. Only the inherited environment reaches those, which is exactly why the enforcement belongs at the process boundary: one point for the whole directory, so no `tools/` test can reach the production ledger by omission. The first cut put it in the ROOT conftest and CI caught the over-reach — a root redirect leaks a `HARNESS_*` name into every axis suite, and `harness-runtime/tests/test_config_loader.py::test_env_defaults_to_os_environ_when_none` asserts that none is set, because RuntimeConfig's env loader consumes that namespace. Every loop-row producer lives under `tools/`, so that directory is the joint, and enforcing there keeps both invariants true instead of trading one for the other.

- **Witnesses.** `tools/test_loop_status_isolation.py`. One pins the redirect; the load-bearing one drives a real `emit_loop_row` (which shells out to `loop_log_structured`, the same writer the subprocess tests use) and asserts the row landed in the redirect. It is deliberately hermetic — it relocates the *computed fallback* to a tmp dir first, so reverting the fix sends the write into a file the test owns rather than reproducing the damage the fix prevents. Mutation-probed: commenting the redirect out reds both. The first cut read the env var by subscript and died with a `KeyError` before the emit ran, proving only that a variable was read; it was restructured to read defensively and assert the untouched-fallback claim first, so the probe now fails on the mechanism itself. Verified end to end: the full 119-test door suite left the operator's ledger byte-identical (1097455 bytes both sides).

- **Two out-of-family rounds, each finding a real defect in the fix rather than in the original code.** Round 1: the `mkdtemp` venue had no owner and accrued one temp tree per run — B-207's shape, reappearing in the thing written to fix a different leak. The re-home to a fixture gave it a teardown; measured 9 dirs before and 9 after, where it had grown by one. Round 2: the fixture was session-scoped, so `HARNESS_LOOP_STATUS_PATH` stayed set for everything running later *in the same process*, and a mixed `pytest tools/… harness-runtime/…` invocation failed the axis no-`HARNESS_*` invariant. That one was **confirmed by running it**, not conceded on argument — and it exposed a gap in my own verification, which had only ever exercised the two suites separately. The redirect is now per-test via `monkeypatch` over a session-scoped root, which makes the isolation a property rather than an ordering coincidence. Re-broadening it was, at that moment, structurally refused — a session-scoped fixture cannot request the function-scoped `monkeypatch` fixture, so pytest raised `ScopeMismatch`. That protection did NOT survive round 5: delegating to an independent `pytest.MonkeyPatch.context()` means the fixture no longer requests `monkeypatch` at all, so `scope="session"` became legal again and the third witness is now the only guard against it (re-verified by mutation at that point — the witness still reds). A claim that stops being true when a later round changes the mechanism is exactly the kind that rots, so it is corrected here rather than left standing. That third witness runs the mixed invocation in a subprocess, with `HARNESS_*` stripped from the child so it models a fresh run rather than inheriting the parent test's own redirect.

- **Round 3 — the fix walked into a trap this repo had already documented.** The per-test redirect rode on `monkeypatch`, which is ONE function-scoped instance shared between a test body and every fixture that requests it; `undo()` empties the whole stack, not just the caller's entries. Nine `tools/` tests call `monkeypatch.undo()` mid-test to drop a `run` stub *before* exercising the real subprocess runner, so the redirect would be lifted immediately before the emit that most needs it, and the row would resolve the operator's ledger. `test_arc_exit_report.py::shared_ledger` carries a comment saying exactly this and avoids it with an explicit save/restore — prior art the fix had not honoured. Found by out-of-family review and, independently, by CI, where that file's own pin was rolled back underneath the redirect and four tests failed with `FileNotFoundError`. Now an explicit `try/finally` around `os.environ`, with a fourth witness that performs the mid-test `undo()` and asserts the emit still lands in the redirect; mutation-probing it back to `monkeypatch` reds that witness *and* reproduces the four CI failures.

- **Round 4 — the merge-gate witness lens found half the round-3 fix untested.** Restoring an environment variable has two modes (it was set; it was not), and only one is reachable from a pytest process nobody pre-configured, so the hand-rolled `try/finally` shipped a restore branch no witness could reach. The lens confirmed it dead by mutation — replacing the teardown with an unconditional `pop` left both the new file (4/4) and `test_arc_exit_report.py` (92/92) green. Resolved by **deleting** the branch rather than contriving a witness for a path that only becomes observable after the process exits: an independent `pytest.MonkeyPatch.context()` owns both modes, and because it is a *separate* instance from the `monkeypatch` fixture, it stays immune to the mid-test `undo()` that round 3 was about. The same lens showed only the `[mine, axis]` order of witness 3 discriminates against a session-scope regression; that order is what catches it, the reverse order is kept because it alone catches a redirect established at import or collection time, and the docstring now says which is which instead of implying both do the same work.

- **Round 5 — the lens falsified a claim the previous round had just written.** Round 4 kept witness 3's reverse subprocess order on the grounds that it alone catches a redirect established at import or collection time. Probing that shape reds BOTH orders equally (1 failed, 1 passed each), because pytest imports every conftest before running any test — so the justification was simply wrong, and no regression shape was found that the reverse order uniquely catches. It is dropped rather than kept as ceremony whose stated reason does not hold. The same round surfaced a branch this arc *deadened elsewhere*: because the directory-level autouse redirect always sets up before module-level fixtures, `test_arc_exit_report.py::shared_ledger`'s own `prev is None` arm became unreachable (92/92 green with it dropped) — the exact untested-restore pattern round 4 had just removed, silently recreated in the file the arc keeps citing as prior art. That fixture now delegates to `MonkeyPatch.context()` as well, which removes the branch, keeps its documented immunity to a mid-test `undo()` (probed: 4 tests red without it), and stays correct if the conftest redirect ever goes away. Cleaning up an orphan this change created, not scope creep.

- **Round 6 — the fix for round 1 had gone three rounds without a witness.** The `shutil.rmtree` teardown that closes the `mkdtemp` accrual leak was never asserted by anything: deleting it left the new file and `test_arc_exit_report.py` entirely green. Session teardown cannot be observed from inside the session, so the fifth witness runs a child pytest with `TMPDIR` pointed at a directory it owns (`mkdtemp` honours it) and asserts nothing matching the venue prefix survives the child's exit. It is non-vacuous by construction: the child runs the redirect witness, which asserts the venue path contains the prefix, so its passing proves a directory *was* created under that `TMPDIR` — an empty survivor list therefore means removed, not never-made. Probed: deleting the teardown reds it, naming the surviving directory. The same round found witness 4's line citations stale by exactly two lines, shifted by this arc's **own** `shared_ledger` docstring edit the same day they were written. Correcting the numbers would only have reset the clock, so they were replaced by a count and a file list — which round 7 then found *also* false, and wrong on arrival rather than eventually (the real figures at that HEAD were fifteen sites across six files, including one the list never named). The paragraph now carries a bare `rg` invocation and no enumeration at all. Three representations rotted in this one arc — line numbers, a `ScopeMismatch` guarantee that a later round's own change invalidated, and a count — and each fix replaced one drifting map with another until the claim itself was removed. The durable rule the arc paid for: a fact checkable against HEAD does not belong in prose unless it is bound to a commit, because nothing redraws it.

- **Round 8 — the boundary was narrower than the claim.** The redirect was function-scoped, so it covered only test *bodies*: higher-scoped fixtures are set up before it and torn down after it, so anything emitting from a session- or module-scoped fixture still resolved the operator's ledger. Every witness through round 7 ran inside a test body, so none could see the gap. Grounding first showed it was not yet reachable — the only two session-scoped fixtures in `tools/` set an env var and scan for files — but the docstring claimed a boundary the mechanism did not deliver, and the honest options were to narrow the claim or widen the mechanism. Widened: a per-item `pytest_runtest_protocol` hookwrapper brackets setup, call and teardown together, so the whole lifecycle is covered while items outside `tools/` still never see the variable (round 2's property). **The path guard on that hookwrapper was measured, not assumed** — a subdirectory conftest's runtest hooks fire for items *anywhere* in the run, verified on a scratch tree, and an unguarded wrapper reintroduces the exact axis leak round 2 removed. Six witnesses then pinned six distinct properties (a round-8 figure — later rounds added more, and per this arc's own rule the current tally lives in the module, not in prose): dropping the guard reds only the axis-leak witness; reverting to a function-scoped fixture reds only the fixture-phase witness.

- **Round 9 — the cache guard was load-bearing and unwitnessed.** `_venue_root`'s `if _ROOT_KEY not in config.stash` is what makes one root serve the whole session. Deleting it left every witness then present green while stranding five directories per run — the exact B-207-shaped accrual round 1 exists to close. Witness 5 could not see it *by construction*: its child drove a single node id, and with one item "cached once" and "minted per item" are indistinguishable (both leave one directory, and `pytest_unconfigure` removes the one in the stash either way). The child now drives TWO items, chosen so neither spawns a child of its own — this test does, and naming the module would recurse. Verified: removing the cache reds it naming the survivor, and deleting the teardown still reds it too, so one witness now pins both halves of the accrual property.

- **Post-resume rounds (codex wrapper rounds 9–12, re-run at each new head).** Round 9: the module-phase witness called the real emitter without owning the computed fallback first, so in the exact regressed world it detects, its own setup would have polluted the operator ledger before the assertion fired — the observation is now captured first and the emit runs under an owned fallback (two-arm probe under a scratch HOME: old fixture reproduces the damage, fixed fixture leaves it absent, detection preserved). Round 10 demanded the phases outside the per-item bracket be witnessed; round 11 then showed per-scenario witnessing never converges (a session fixture in a module the canary child never imports slips every canary). Closed at the one joint every scenario shares instead: `pytest_configure` owns `ARC_METRICS_QUEUE_DIR` — the input `loop_status_path()` computes its fallback from, a documented override seam (C-HE-05) — for the whole session, so import time, collection, any-scope teardown and un-isolated child processes resolve an owned belt directory (every consumer but one — next bullet); a probe matrix measured each layer load-bearing (an inserted session-teardown emit and an inserted import-time emit each red their canary with the belt disabled and are absorbed with it on; module-scope teardown proved BRACKETED — pytest tears down to the next item's level inside the current item's teardown phase — so the reviewer's module-scope scenario was narrowed to session scope by measurement). Round 12: APPROVE. Full tools suite under the belt: 1719 passed, with the axis `HARNESS_*`-empty invariant green in a mixed run.

- **The 3-lens merge gate on the belt round.** The witness-adequacy lens caught two recurrences of this arc's own defect classes: the belt had only a presence witness (deleting its `setenv` red nothing behavioral), and its hand-rolled two-armed env restore reprised the exact round-4 shape whose dead arm was deleted rather than witnessed. Absorbed the same way as their ancestors: the restore now delegates to a stashed `MonkeyPatch` whose `undo()` at `pytest_unconfigure` owns both prior states, a behavioral witness lifts the bracket and asserts the real shell writer resolves the belt-derived fallback (reading the variable by subscript, so a belt-deletion probe reds before any emit can run), and a driver calls the configure/unconfigure pair directly to witness the restore in both prior states — the observation a live session structurally cannot make. All three probed to a kill. The spec-conformance lens caught this register itself stale at head — the belt rounds unnarrated and the round-8 witness count reading as current state — which this entry now corrects, plus the §2→§1 append-cite fix across carriers.

- **Rounds 13–15 — the belt carved, hardened, and converged.** Round 13 (P1): `ARC_METRICS_QUEUE_DIR` names TWO authorities — the ledger-fallback input and the LIVE lane registry — and the belt had demoted both, so the Docker lane test's destructive-teardown collision claims silently ran against a throwaway registry. The belt now preserves the demoted production value at `ARC_METRICS_QUEUE_DIR_PREBELT` and the lane test resolves through `production_queue_dir()` (PREBELT, else the home default — never the ambient variable, which under pytest IS the belt). Round 14 (P1): a nested pytest session — exactly what the canary children spawn — re-derived PREBELT from the parent's belt; publication is now first-writer-wins, the outermost capture being the authority, witnessed by the 7c driver's third arm. Round 15 (P2): the belt-implied fallback file and the bracket venue were two files, splitting a session's rows against C-HE-09 §2's one-venue reduction; the belt now sits directly under the venue root so both mechanisms name one ledger, pinned by an equality assert in 7a. Round 16: APPROVE. Every fix probe-killed both ways.

- **Gate round 2 (at the converged head).** Concurrency and witness-adequacy: APPROVE — the mid-arm-failure hazard in the 7c driver traces to a self-healing outcome through pytest's live-value `undo` semantics. Spec-conformance: BLOCK on exactly this register lagging rounds 13–15 (the round-12 belt sentence had become false once the round-13 carve-out existed) — corrected in this entry. One named residual is tracked rather than fixed-and-re-reviewed: the 7c driver leaks its own `mkdtemp` root when an arm fails mid-flight — bounded to already-failing runs, judged non-blocking by both code lenses, and left so the reviewed code stays byte-identical to its verdicts.

- **Deliberately not done.** The 6288 rows already in the ledger are not removed. It is append-only by contract and is operator state outside this repo, so truncating it is the operator's call, not this arc's; the pending synthetic gate was cleared the contract-sanctioned way, with a `RESOLVED-HIL` row.

### B-209 · the B-208 session belt shadows the production-default probe *(surfaced by codex round 3 on PR #1445, 2026-08-24; CLOSED at #1447)*

- **What it is.** `tools/conftest.py` installs `ARC_METRICS_QUEUE_DIR` at `pytest_configure`, before collection imports `arc_metrics`, so the module-level `QUEUE_DIR` always resolves the temporary belt. `tools/test_arc_metrics.py::test_queue_lives_outside_the_repo` therefore no longer exercises the UNSET production default: a regression moving the fallback to a repo-resident path stays green because the environment value wins.
- **Closure shape.** Test the default in an isolated import — a child process with the variable stripped, the same idiom the isolation module already uses for process-boundary claims.
- **Closed.** PR #1447 (2026-08-24): `test_queue_lives_outside_the_repo` rewritten exactly to that shape — child interpreter, both queue variables stripped, default asserted outside the repo — and probe-verified (a repo-resident default reds it).

### B-210 · production_queue_dir() lacks a callsite pin at the Docker lane test *(surfaced by codex round 3 on PR #1445, 2026-08-24; CLOSED at #1447)*

- **What it is.** Case 7d proves `production_queue_dir()` never returns the belt, but no witness proves `test_two_lanes_disjoint_names_and_ports` still resolves its `lanes_dir` THROUGH that helper. Reverting `test_compose_lanes.py` to the ambient `ARC_METRICS_QUEUE_DIR` lookup leaves 7d green while the Docker test's collision claims go back to the throwaway registry — and the docker test is skipif-gated, so CI cannot see the regression.
- **Closure shape.** Pin the callsite with a source-shape witness on the `lanes_dir` assignment, or factor claim-path selection into directly tested code.
- **Closed.** PR #1447 (2026-08-24): closed by the second shape — claim-path selection factored into `claim_production_lane_indices()`, hermetically witnessed without a daemon; the lens round hardened the taken-index witness at `562fd965f`.

### B-211 · Lever id: defect-class-preflight treated cohort *(minted 2026-08-24; OPEN)*

- **What it is.** Not a defect — a treated-cohort marker. An arc that ran the `defect-class-preflight` sweep on its diffs before each commit declares `B-211` in its arc-metrics `levers_active`, so cohort splits can price the skill against the untreated baseline on review rounds and P1 rounds (per-arc reviewer-finding counts share B-212's named residual — no committed pr-to-gate-log join key).
- **Current state.** OPEN as a measurement lever: one treated arc (pr-1447) is in the committed ledger; the n>=5 same-arc_type evaluation bar is not yet reached, so no keep/retire judgment exists. NAMED RESIDUAL (codex r3, corrected r9): the drain DOES join the reservation's open-time declaration (`_fold_head_onto`, tools/arc_metrics.py — the C-HE-26 §1 fold), but the join keys on the reservation arc_id, so a queue entry captured without `--arc-id` (as pr-1447 was) folds close-declared, and 12 committed rows are untyped legacy. Until rows exist that were queued with their reservation arc_id, typed cohorts render contaminated and the evaluation cannot use uncontaminated type discrimination. Practice: always `just arc-metrics queue --arc-id <reservation-arc-id> --pr N ...`.
- **First treated arc.** pr-1447: 2 codex rounds with 0 codex findings and 1 merge-gate lens finding (at `562fd965f`), beside the close-declared applying row pr-1434 (22 rounds) — a STRICTLY DESCRIPTIVE comparison, unusable for the lever decision: both rows are close-declared and that cohort is non-evaluable under C-HE-26. Mixed-type ranges are not quoted here: the evaluation contract is same-arc_type.
- **Evaluation.** `tools/arc_lever_report.py` (the `/arc-lever-report` skill) after ≥5 evaluable arcs of the same exact lever pattern (or a clean matched contrast) within one arc_type; retire the id if treated and baseline are indistinguishable.

### B-212 · Lever id: register-pr-prose treated cohort *(minted 2026-08-24; OPEN)*

- **What it is.** Treated-cohort marker for the `register-pr-prose` authoring discipline. Declared when the arc's PR body, register rows and close_outs were written under the skill's six binding rules; its target class is prose findings (145 of 1,084 at the 2026-08-24 distillation).
- **Current state.** OPEN as a measurement lever: one treated arc (pr-1447, zero prose-located findings); not separable from B-211 until a treated arc declares only one of the two ids.
- **First treated arc.** pr-1447: zero prose-located findings across 2 codex + 2 lens rounds.
- **Evaluation.** Same instrument and bar as B-211, on the shared review-round / P1 cohort medians. The prose-specific signal (prose-located reviewer findings per arc) is a NAMED RESIDUAL: the gate log keys findings by reservation arc_id, the metrics ledger by pr-N, and no committed join key exists — the instrument says so rather than fabricating the split (codex r1 on the report tool).

### B-213 · /arc-lever-report is guard-denied in loop mode *(surfaced by codex r11 on the lever-observability arc, 2026-08-25; REGISTERED)*

- **What it is.** The new read-only cohort report cannot run in autonomous (loop-mode) sessions: no allowlist case matches its invocation, so the guard's ask becomes a headless denial. Loud and fail-closed — nothing is misreported; interactive sessions are unaffected.
- **Current state.** OPEN. Registered instead of fix-looped at codex r11 because permission-guard edits are the workspace's highest-blast-radius shared-surface class and take their own reviewed arc (the U-HE-25 exact-shape precedent).
- **Closure shape.** An argument-safe allowlist case in `tools/hooks/permission-guard.sh` for the report's invocation plus a witness in the guard's shell suite.

### B-214 · BASE_TOCTOU CI re-check lacks a CI-visible verified-base authority *(surfaced by codex r2 on the u-he-33 arc, 2026-08-25; REGISTERED)*

- **What it is.** The U-HE-33 guard re-check joins main landings to the reservation store under the host-local queue dir; a fresh Actions runner has no such store, so on CI every landing is reported through the info `BASE_TOCTOU_UNATTRIBUTED` finding and no first-parent mismatch can become a hard CI finding. Loud and honest — unattributed is named, never silently skipped — and the operator-host main-audit venues run the real comparison.
- **Current state.** OPEN. Registered instead of built in-arc: a CI-visible carrier of the verified base (committed sidecar, PR annotation, or ledger row) is a new mechanism — a design decision (X-AL-3), not an impl slice of U-HE-33. Until then the CI backstop for hazard #7 is the door's own at-landing detection (U-HE-23) plus the operator-host re-check.
- **Closure shape.** A design-phase decision on where the verified-base attestation lives CI-visibly, then a small impl arc re-pointing the guard's join at that carrier.

### B-215 · deterministic post-round class-sibling sweep gate *(surfaced on the u-he-33 arc; CLOSED at #1456, 2026-08-25)*

- **What it is.** The u-he-33 arc measured that the between-rounds classify-then-sweep step is instruction-dependent and fails under load: the empty-on-exception class recurred in sibling helpers across multiple review rounds despite being written in defect-class-preflight class 3. Finding capture is already deterministic (per-round C-HE-24 rows; refresh-classes.py); the sweep is not.
- **Current state.** CLOSED at PR #1456 (b-215 arc): the admission gate `tools/review_loop_gate.py` wired into both loop channels — CURRENCY (attestation bound head+digest to the exact reviewable diff), OBLIGATIONS (every finding_id across both producers, token-exact answers), TERMINATION (10-round budget + ask-gated auditable extensions, refusal citing the u-he-29 counter-evidence). 10 codex rounds ending in the arc's own live BUDGET_EXHAUSTED refusal — the predicate demonstrated on its own construction. Residuals named in the register close_out; guard chaining class spun out as B-217.
- **Closure shape (as registered).** A deterministic post-round step in the review loop: extract the round's findings, grep the branch diff for class-sibling shapes, refuse the next review invocation until each hit carries a named answer. Review-wrapper surface — own reviewed arc (B-213 precedent).

### B-216 · u-he-33 review-loop tail refinements *(deferred at the operator stop, 2026-08-25; REGISTERED)*

- **What it is.** Three bounded guard refinements surfaced by codex r22 and deferred when the operator stopped the u-he-33 review loop at its measured negative-marginal-value point (the r22 absorption broke 20 tests while fixing 3 findings): the `ci` lane-id fallback ignoring the persisted `.harness/.lane-id`; detection rows carrying `head_sha=None` so adjudications recall identical recurrences at later heads (C-HE-24 §4); the continuation-lease check accepting a lease missing state/host.
- **Current state.** OPEN. None undermines the landed C-HE-12 contract (acceptance met at round 4). A draft absorption existed and was reverted for breaking the suite — re-derive from this row.
- **Closure shape.** A small follow-up arc with witnesses, alongside or after B-215's deterministic review-loop gate (this arc is B-215's grounding evidence for the termination-predicate requirement).




### B-217 · permission-guard just-recipe chaining class *(surfaced by codex r3 on the b-215 arc, 2026-08-25; REGISTERED)*

- **What it is.** `just` executes multiple recipes from one command line, so any prefix-anchored allowlist entry for an argument-bearing `just` recipe can front a chained SECOND recipe that was never allowlisted — measured shape: `just review-gate-check main review-attest-budget 2 ok` rode the gate-check allow and would have run the deliberately ask-gated budget verb.
- **Current state.** OPEN. The b-215 arc fixed its own three verbs with exact-anchored, arity-bounded shapes (no surplus token can be parsed as a recipe name) plus two chained-bypass witnesses in the guard suite; the CLASS remains for every other argument-taking recipe in the big `just` alternation.
- **Closure shape.** A guard-wide audit converting the alternation's argument-bearing entries to arity-bounded anchors (or one structural token-count check against the named recipe's parameter count), with per-shape chained-bypass witnesses (U-HE-25 exact-shape precedent; guard edits take their own reviewed arc per B-213).
- **Scope note (b-215 codex r7–r10).** The interim positive token grammar on just-first lines oscillated twice against documented free-form evidence shapes (quotes r8, parens r10): normalization-proof chain prevention WITHOUT arity knowledge degrades into an inner-charset ladder — which is this row's whole argument.

### B-218 · capture-phase span vs terminal-accretion refusal *(surfaced by codex r1 on the u-he-34 arc, 2026-08-25; REGISTERED)*

- **What it is.** C-HE-27 §1 lists `capture` among the phases recorded as explicit `{start,end}` pairs, but the arc-metrics capture (the ship-pr queue step) runs after the merge door has transitioned the reservation to `merged`, and `record_phase` refuses ALL terminal-state accretion (C-HE-03 §3) — a capture pair on the reservation is structurally unrecordable, not merely unwired.
- **Current state.** OPEN. The u-he-34 landing wires the queue/execute/absorb/edit/verify/verify_unavailable emitters and records NEITHER the capture pair NOR the `result_capture` split — both §1 obligations live on this row; ship-pr SKILL.md names the conflict at the queue step and the split's session-unobservability at the review loop, instead of instructing edges that necessarily fail or measure nothing.
- **Closure shape.** Either a spec leg narrowing C-HE-27 §1 (capture recorded on the ARC ROW at drain rather than accreted on the reservation — the drain is the one actor that already writes the row), or a drain-side fold stamping the capture pair from the queue-entry timestamps. A small spec/plan reconciliation precedes wiring — the phase is not implementable-as-specced.
- **Scope extension (u-he-34 codex r3).** The same reconciliation should decide per-round phase records — ONE `{start,end}` pair per phase cannot represent the alternating review/fix loop, so the v1 carve (verify = round-1 window, edit = fix window) leaves re-review rounds inside edit and the final confirming round outside both: a named systematic undercount of N6's denominator on every repaired arc. It should also cover a wrapper-internal `verify_unavailable` emitter — a successful failover's primary-channel downtime is unobservable at the session layer and stays inside verify until the wrapper records it (n6's nested-span subtraction already consumes it once recorded).
- **Scope extension (u-he-34 codex r4).** The §1 `result_capture` split (process-exit vs log-write) is also session-unrecordable: a synchronous in-recipe tee returns only after the log flushes, so two session timestamps around it measure one event, not the divergence. The split's recorder is wrapper-internal work on this same row; the interim `review-log-settle` polling recipe was removed for exactly this reason. Extended at r10: the single-pair limit also drops any SECOND outage interval on one arc (record_phase replay-no-ops a re-recorded edge), so multi-interval downtime needs the same per-round/multi-record reconciliation.

### B-219 · pilot-gate stale-GREEN over an incomplete rerun *(surfaced by codex r1 on PR #1462, 2026-08-26; REGISTERED)*

- **What it is.** The `just pilot-gate-check` reducer (`tools/lanes_verify.py:620` area, landed at #1460) selects the last historical probe-result row and ignores probe-sample rows entirely. After an existing GREEN, a rerun can record invalid samples and then crash or be terminated before emitting its terminal probe-result row — pilot-gate still returns the prior GREEN, so an incomplete run is indistinguishable from no run and C-HE-22's zero-validity-failures GREEN rule is unenforceable against the run actually in progress.
- **Current state.** OPEN. Surfaced by the LEAN codex round on the doc-only PR #1462; the code is pre-existing and untouched by that PR, so the finding is registered rather than absorbed (surgical-changes rule).
- **Closure shape.** A small follow-up arc on `tools/lanes_verify.py`: run-delimited probe-result reduction (run-id/start marker; the newest unterminated run reduces non-GREEN), with a witness that plants an unterminated rerun after a GREEN and asserts `just pilot-gate-check` refuses. Natural companion to U-HE-37 (pilot gate) or the plan §8 R0 instrument round.

### B-220 · C-HE-23 §2 pr-join wording vs reservation arc ids *(surfaced by codex r5/r7 on u-he-47, 2026-08-27; REGISTERED)*

- **What it is.** A contract-wording tension: since U-HE-34 the merge-gate carriers pass the reservation arc id on lens rows (the C-HE-24 §6 / N6 / phase joins key on it), while C-HE-23 §2 words the md↔JSONL consistency join as `(pr, head_sha, verdict)` — recoverable only from the legacy `pr-N` arc shape. The pre-U-HE-47 reducer therefore silently dropped every reservation-arc verdict (23 live MISSING-JSONL rows on main).
- **Current state.** U-HE-47 re-keyed the join (arc-grouped emissions × `(head12, lens, verdict)` md join, pr md-side only) — the live check is green. Residual: the wording deviation, and the theoretical cross-vouch of two concurrent PRs at one head with identical lens/verdict/ts/round/count (tamper-evidence is out of scope per §2's hash-chain drop). Closure = a C-HE-23 §2 change-note (or an envelope-level pr carrier) at design phase.

### B-221 · adjudicate actor not runner-bound in the shared guard *(surfaced by codex r6/r7 on u-he-47, 2026-08-27; REGISTERED)*

- **What it is.** A venue-signal gap: `tools/hooks/permission-guard.sh` serves BOTH runners (`.codex/hooks.json` registers the same script), so the merge-gate-adjudicate exact shape pins `--actor` to the absorber identity set but cannot bind WHICH runner invokes — either loop could claim the other's absorber in `disposition_actor` (false WHO; auditable append-only, actor ≠ producer still enforced).
- **Current state.** The two-absorber pin stands (the r6 claude-only pin was reverted at r7 as falsified-premise — it broke documented Codex absorption); append-only audit is the operative control. Closure = a runner-identity signal delivered to the guard (or per-runner entrypoints) + the pin keyed on it, witnessed on both runners.

### B-222 · holder-check-to-append window on adjudication *(surfaced by codex r6/r7 on u-he-47, 2026-08-27; REGISTERED)*

- **What it is.** A held bounded race: the holder authority check (inside `adjudicate()`, immediately before the append) and the gate-log append are not one atomic step — the reservation store and the gate log share no lock.
- **Current state.** HELD as bounded, stated in code: a §6 holder transfer is dead-claim recovery of a crashed/idle lane, and the landing row is exactly the one the then-authorized caller composed. Closure = a design-phase call: accept-as-bounded in C-HE-24 §5, or a common coordination point for reservation-authorized log writes.

### B-223 · Codex-runner cost-cohort gap *(surfaced by codex r2 on u-he-48, 2026-08-27; REGISTERED)*

- **What it is.** A named cost-cohort gap: `tools/arc_cost.py` (U-HE-48, C-HE-25 X6e) reads Claude session-transcript JSONL, but Codex sessions store date-partitioned rollout JSONL under `~/.codex/sessions` in a different shape, so the Codex ship-pr carrier deliberately OMITS `--transcript` (pinned by `test_codex_workflow_parity`) and Codex-shipped arcs record null cost fields — honest, but a systematic cost-baseline under-coverage for that runner.
- **Current state.** OPEN. Closure = a rollout-shaped usage extractor (or shared normalizer) feeding the same `cost_snapshot` fields, plus the carrier flipped to pass its transcript.
### B-225 · loop-venue root checkout permanently dirty — local stop-gate never green *(surfaced by u-he-49 preflight, 2026-08-27; REGISTERED)*

- **What it is.** A venue defect in the local gate: `test_stop_gate_emits_valid_stop_hook_json` runs the real `.codex/hooks/stop_gate.py` against the live checkout and requires exit 0, but `codex_context_guard` fires the HARD `ROOT_CHECKOUT_EDIT` on ANY git-status entry in a non-linked-worktree checkout — and the loop ritual itself keeps the root checkout permanently dirty (untracked `.harness/.preflight-answers-*` / `.sweep-answers-*` attestation files accumulate per arc), so `just codex-check` cannot go fully green locally on this venue.
- **Current state.** REGISTERED as a named environmental red; CI's clean checkout is where the test actually adjudicates (u-he-46..48 shipped under the identical local red; the guard shape landed at U-HE-33). Closure = a policy decision on the attestation answer files (gitignore, commit, or a guard exemption for the pattern) OR a hermetic venue for the stop-gate battery.

### B-224 · subagent-sidecar completeness has no verifying authority *(surfaced by codex r5–r8 on u-he-48, 2026-08-27; REGISTERED)*

- **What it is.** A held bounded measurement residual: `tools/arc_cost.py` cannot verify subagent-sidecar COMPLETENESS — no manifest enumerates the expected `agent-*.jsonl` set, so a whole stem-dir GC is indistinguishable from a session that spawned no subagents.
- **Current state.** HELD as bounded after four codex rounds re-flagged the class (r5–r8). Detectable cases refuse (sidechain records with zero files; an existing `subagents/` dir with zero agent files — 0 of 67 occur naturally; a zero-yield sidecar); the absent-dir case is a measured genuine zero on 59 of 126 project transcripts. Closure = a producer-side subagent manifest or retention guarantee the extractor can check against — an authority this consumer cannot mint.

### B-226 · unattended transcript-brief review debt lacks an executable gate *(surfaced by codex r10 on u-sr-06, 2026-09-01; REGISTERED)*

- **What it is.** A prose-only floor: the U-SR-06 rename conformed every governance carrier to the transcript-brief review, and the Codex-venue fallback (AGENTS.md, parity note, the 30 bridges) says an unattended arc owing a review HALTS at the decision-fork/pre-done point — but nothing in the loop state machine enforces that halt. The permission guard auto-allows only the merge-lens exec shape, and the loop recognizes only `decorrelated_review` and merge-gate review states, so an unattended Codex arc could proceed to merge with the owed review recorded nowhere a gate reads.
- **Current state.** REGISTERED. The prose halt rule is the fail-closed floor by instruction-following only. Closure = an executable review-debt mechanism: a recorded owed-review state the stop-gate/ship path refuses to pass, or a guard-allowlisted isolated transcript-brief exec shape for the Codex venue (a class-11 four-question surface: guard venue, authority binding, carrier parity, production-call witness).

### B-227 · rtk-shape-guard grammar corners beyond the modelled set *(surfaced by codex r1–r4 on u-sr-09, 2026-09-02; REGISTERED)*

- **What it is.** The rtk grep-rewrite shape guard (`tools/hooks/rtk-shape-guard.sh` + `tools/rtk_shape_guard.py`, U-SR-09 b4) has to model shell lexing and the grep/rg option + BRE grammar to say whether rtk's grep→rg rewrite will break a command. Codex r1–r4 on u-sr-09 each found a real new corner (quoted/escaped/newline separators, attached and clustered value options, `--`, backslash-run parity, bracket expressions and escaped `[`, every `-e`, redirections and globs in a re-joined re-issue) and each was absorbed — the re-join itself was deleted at r4, so no compound command is ever re-joined. The remaining class (intervals, `[[:class:]]`, word anchors, `-f FILE` patterns, locale collating, command substitution inside the command, …) is open-ended.
- **Current state.** OPEN. Cost is bounded by construction: a missed shape = the pre-arc baseline (the guard is fail-open; the mangled call runs as before), a false deny = one wasted call with an exact reason. Registered so the next grammar finding maps here rather than buying another review round.
- **Closure shape.** (a) rtk fixes its translation — `test_rtk_shape_guard.sh` section 4 reds "delete the guard" and the whole mechanism is deleted; or (b) a corner is witnessed on the real binary and added with one case in `tools/test_rtk_shape_guard.py`. No pre-emptive grammar work.

### B-228 · selection-time disjointness gate reads committed heads — two fresh lanes on the same main always pass it *(surfaced by codex r2/r6/r7 on u-he-36, 2026-09-02; REGISTERED)*

- **What it is.** C-HE-13 §5 (spec v1.7) has `tools/arc_disjoint_check.py check --candidate HEAD` merge-tree the candidate against every other lane's live head BEFORE the lane reserves. At selection the candidate is `main`, so two lanes branching from the same main that will later edit the same hunk both pass; the collision surfaces at landing via `BASE_TOCTOU` (C-HE-12), which §5 names as the complement. Codex raised this in r2, r6 and r7 of the U-HE-36 PR as "not actual-write enforcement"; each time it was refuted on the contract's own text and the limit was named in the tool docstring, both carriers, and `test_check_reads_committed_tips_only`.
- **Current state.** OPEN, held. The mitigation on the same PR: the identical gate runs a SECOND time at ship-pr pre-flight (both carriers), when the candidate carries the arc's commits — a real textual conflict with a live sibling surfaces before the PR opens rather than at the door. The selection-time call keeps the spec's semantics.
- **Closure shape.** A spec-level decision (v1.8 note): (a) ratify the two-site shape (selection + ship pre-flight + landing BASE_TOCTOU) as §5's meaning and close; or (b) define a declared-scope carrier the gate can merge-tree against at selection — a design extension (X-AL-3) needing its own unit. Further review rounds cannot close it.

### B-229 · O3 replay population is the 172 file-overlapping pairs — dir/file conflicts between path-disjoint branches fall outside it *(surfaced by codex r10 on u-he-36, 2026-09-03; REGISTERED)*

- **What it is.** C-HE-13 §4 fixes the O3 population as the 172 historical colliding pairs (P-R3: changed-file sets intersect after the four-file governance exclusion), and `tools/arc_disjoint_check.py historical` replays exactly that set. git can also conflict between disjoint path sets (`x` vs `x/y`, rename detection), so a pair with no shared file can still be a merge-tree conflict; 172/444 bounds file overlap, not every merge-tree conflict.
- **Current state.** OPEN. The replay measures the contract's population faithfully; the observation is about what the population omits. Registered on the U-HE-36 PR so it is not silently carried.
- **Closure shape.** Run the replay over all 444 window pairs (the 272 non-colliding pairs can conflict only through dir/file or rename collisions). Any conflicts → a named third term of the X7b interval via a v1.8 note; none → record the zero and close. A measurement task, not a review task.

### B-230 · Loop optimization program *(surfaced by the Archify loop diagrams, 2026-09-03; CLOSED at the b-230-task-8 arc, 2026-09-04)*

- **What it is.** The Archify diagrams at `docs/diagrams/code-loop/` (five HTML views of the continue → build → review → merge → refresh loop) surfaced six cost categories; grounding on 2026-09-03 sorted them into workspace-ops changes and spec-governed changes. The plan at `.harness/plan/loop-optimization-plan-2026-09-03.md` carries nine tasks ordered by measured value over cost, each with the witness that proves its saving against the §0 baseline (five CI runs per landed unit with the pytest job at 450 s, a merge-door lease held across two of them, 35 of 49 gate passes raised by a single lens — Task 0's head-bound count; the pre-registration read was 33 of 48 — a prompt hook printing `next=?`).
- **Current state.** CLOSED at the b-230-task-8 arc (2026-09-04). Tasks 9/0/1/3/4/5/2 and Task 6 Steps 1-5 merged at PRs #1499, #1501, #1503, #1505, #1507, #1509, #1511 and #1513 with their witnesses in the plan §0 (the refresh-merge `main` run fell from 455 s to about 55 s; the concurrency detector measured 74 fire / 5 would-skip over 79 lens rounds with no finding in a would-skip round). The operator answered the batched question on 2026-09-03: Task 6 Step 6 — keep three lenses always, no C-HE-34 spec leg, the detector stays a measurement instrument; Task 7 — no `--delete-branch` in the fixed merge string, deletion stays the once-per-session hand-approved batch; Task 1 Step 8 — held. Task 8 Steps 1-3 landed in the b-230-task-8 arc; its trigger lives on at B-232.
- **Closure shape.** Tasks 0-5 merged with their witnesses recorded in the plan §0, Tasks 6 and 7 answered by the operator, and Task 8 Steps 1-3 all landed (yield row emitted, trigger row registered, counter wired into the baseline) so the trigger is both durable and evaluated. No fixed merge string, lease invariant, or CI-at-final-head rule changes without its own spec leg.

### B-231 · gate-log rows carry no failover-child marker — a gemini round cannot be told from a standalone review *(surfaced by codex r6/r7 on b-230-task-0, 2026-09-03; REGISTERED)*

- **What it is.** `review_wrapper_common.round_n_for` forces the C-HE-17 D-C failover child (a gemini run after codex reported unavailable) to the primary's round number, but the child's rows carry no marker: `cause_attribution` is null and `lineage_claim` is `fresh`, the same as a standalone `just gemini-review`. A reducer over the gate log therefore cannot tell one from the other when they share an `(arc, head_sha, round_n)` key.
- **Current state.** REGISTERED. `tools/loop_cost_baseline.py` (B-230 Task 0) counts every gemini row as its own producer round and reports `failover_ambiguous_rounds` — the keys carrying both a codex `reviewer_unavailable` row and a gemini row — as the overcount's upper bound (4 at the Task 0 head). Inferring lineage from key coincidence was tried at codex r6 and reversed at r7.
- **Closure shape.** The failover child writes an explicit marker on its rows (a `cause_attribution` or `lineage_claim` value the wrapper sets under `HARNESS_FAILOVER_CHILD`) with a witness; the baseline consumes it in place of the ambiguity count. A wrapper-side change with its own review; not part of B-230.

### B-232 · merge-door lease released after content merge; refresh covers N merges *(trigger-gated by B-230 Task 8, 2026-09-04; HELD until the trigger fires)*

- **What it is.** Releasing the merge-door lease after the content merge and letting one terminating refresh cover N merges would end the per-unit serialization the loop pays today, but it needs three coupled changes: the C-HE-06 invariants (`.harness/spec/Spec_HE_Loop_Lanes_v1.md:317-321` — the lease is never released while the merge SHA's own `main` run or the terminating refresh is unconfirmed), the §12.2.1 one-commit fixed point in root `CLAUDE.md`, and `_owed_lag` in `tools/codex_context_guard.py:443`. Until this arc the data could not justify it in either direction: the only door row on record was the budget-exhausted one (`lease_acquire_budget_exhausted`, after twelve backoffs of up to ten minutes), so "2 rows in 76 arcs" counted hour-long stalls, not contention.
- **Current state.** HELD, trigger-gated — `held` because every other register status is schedulable and the loop would otherwise select this row before its trigger (codex r1 on b-230-task-8); the hold is grounded, not parked: the data cannot justify the change in either direction until the counter moves. Since the b-230-task-8 arc `merge_door.wait_for_door` appends ONE `NOTIFY` row per contention event — at the first `held` observation, cause `merge-door-lease-acquire:lease_held_yield`, detail `holder=<arc id> backoff=0` — to the shared `loop_status.md` (never the tracked gate log, whose mid-wait rows could not reach merged history), and `uv run python tools/loop_cost_baseline.py --loop-status <loop_status.md>` reports `lease_held_yields` (lifetime) and `lease_held_yields_30d_max` (the most rows in any half-open 30-day window), and `tools/roadmap-audit/session-start.sh` evaluates the trigger EVERY session through `tools/lease_yield_trigger.py`, printing `[b-232] lease_held_yields_30d_max=<n>/5` in the banner and `TRIGGER FIRED` past 5 — the mechanical carrier of "re-evaluated at each refresh". Both counts read 0 at registration.
- **Closure shape.** TRIGGER: `lease_held_yields_30d_max > 5`, printed by the SessionStart banner every session. When it fires, flip this row to `design_substrate_gated` and open the spec leg as a Class 2 decision with a proposed text at the next unallocated `Spec_HE_Loop_Lanes` version carrying the three cites above; until then nothing in acquire, release, the C-HE-06 invariants or the §12.2.1 fixed point changes.

### B-233 · the generic `just` verb alternation in the permission guard is not end-anchored — a trailing token is parsed by `just` as a chained second recipe *(surfaced by codex r1 on u-he-37, 2026-09-04; REGISTERED)*

- **What it is.** The loop-mode allowlist branch in `tools/hooks/permission-guard.sh` matches `just[[:space:]]+(check|lint|…)([[:space:]]|$)` — with no end anchor. `just` runs every trailing token as a further recipe, so `just lint main-protection-rollback` is auto-approved and executes an otherwise ask-gated outward-facing branch-protection mutation after `lint` finishes. Confirmed empirically rather than argued: `just lanes-pilot-report aaa zzz-nonexistent-recipe` errors with "justfile does not contain recipe", which is only reachable if the trailing token was parsed as a recipe.
- **Current state.** REGISTERED, pre-existing and general. Surfaced by codex r1 on u-he-37 against that arc's two new recipes; those two now carry their own end-anchored, arity-bounded matcher with seven negative guard cases, and `arc-close` was already immune because its variadic `*QUEUE_ARGS` swallows trailing tokens. Neither fix generalizes to the rest of the alternation.
- **Closure shape.** Every recipe in the generic alternation is end-anchored with its own arity bound, variadic, or moved to an exact-shape branch, with per-recipe negative chain cases in `tools/hooks/test_permission_guard.sh`. This needs a per-recipe arity audit of the justfile — a blanket anchor would break the parameterized entries that rely on the loose match — so it is an audit, not a one-line fix.

### B-234 · pilot membership is inferred, and arcless loop-ledger rows cannot be attributed to a pilot *(surfaced by codex r3 on u-he-37, 2026-09-04; REGISTERED)*

- **What it is.** Four bounds the C-HE-13 §3 pilot report cannot close from the consumer side — three of which, (a), (c) and (d), block a trustworthy PASS. Codex r3 surfaced the two below; r4 added the two door-completion bounds under *Also carries*, and the closure shape covers all four. Membership is inferred from the reservations that carry `pilot_run_id`, because nothing persists an expected roster — a lane that created its reservation and died before recording the id is invisible, so a four-lane pilot that lost one lane can still present three lanes and satisfy the 3–4 check. And the friction window closes at the pilot's last arc-attributed row, while three `merge_door._notify` details carry no leading arc id (lease acquired, lease released, main-CI queue depth), so the door's own release attestation falls outside every defensible close.
- **Current state.** REGISTERED, and **BLOCKING the Scope-level completeness of U-HE-37**. Until bounds (a), (c) and (d) land, `lanes-pilot-report` does NOT compute the C-HE-13 §3 iff-clause completely: three paths produce a false PASS — a lane that died before recording `pilot_run_id` is invisible and a short run presents as a valid clean one (a); an already externally-MERGED PR receives a `merge_sha` from `merge_door.land()` and satisfies clause (a) of the iff-clause (c); and a malformed live `LEASE` reads as absent so the report can PASS before the door's post-merge checks finish (d). **A PASS from this report must not be relied on for any decision that assumes every intended lane landed through the door and finished.** Bound (b) is reporting-only and does not gate a PASS. Three review rounds on u-he-37 landed on the friction-window heuristic, which is the register-and-hold point: the root cause is producer-side, not another consumer bound. (This disposition is mirrored here deliberately: `forward_register.py --detail` renders from THIS prose file, not the YAML, and the structural drift check compares headings only — so a blocker recorded solely in `close_out` would be invisible to the lookup future routing uses.) Interim floor landed with the unit — the report prints `arcs`, `lanes` and a `membership: inferred …` field so a PASS cannot be read as "every intended lane landed", and the module docstring states bounds (a) and (b) — the two known when it was written — but not (c) or (d), so the hazard set a reader of the code sees is two of four until the next `lanes_pilot.py` touch adds them (which a doc-only PR cannot do: `tools/` is IMPL and this plan is DESIGN).
- **Also carries, from codex r4 on the same arc.** Two door-completion bounds of the same shape: a non-empty `merge_sha` does not by itself prove a *door* landing, because `merge_door.land` accepts a PR already externally MERGED, reconciles the cycle and still writes that SHA; and `read_lease()` maps a present-but-malformed lease file to None, so a corrupted live lease lets the report proceed while post-merge checks are unfinished. Four review rounds converged on this family rather than on the code, which is the signal that the report sits outside the door and cannot verify door completion from the stores alone.
- **Closure shape.** (a) The pilot runner persists an expected participant roster at start and the report compares it against the reservations that recorded the run id; (b) every `merge_door._notify` detail leads with its arc id, so ledger rows are attributable without a time window at all. (b) alone retires the friction bound and is the smaller change; (a) needs a roster surface C-HE-13 does not define today, so it may route to a spec leg. (c) The door publishes an explicit completion marker the report can read — a released-lease record or a terminal door row — so "landed through the door and finished" stops being inferred from reservation payload; (d) `read_lease()` distinguishes absent from unreadable, as every other store reader in this family already does.

### B-235 · the register keeps one claim on two hand-maintained surfaces and the drift check compares only headings *(surfaced by codex r8 on u-he-37-plan-record, 2026-09-04; REGISTERED)*

- **What it is.** One claim, two hand-maintained bodies, and no check that compares them. `close_out` in `.harness/forward-register.yaml` is the canonical field, but `forward_register.py --detail` renders the **prose** carrier, and `check_prose_drift()` compares **headings only** — never bodies. So a safety-critical disposition must be typed twice, and reverting either copy leaves `--check` green while the canonical field and the surface routing actually reads disagree about which bounds block a PASS.
- **Current state.** REGISTERED. It does **not** block any unit's completeness — no claim is currently wrong on any surface: the three register-side enumerations for `B-234` are identical and in the same order, verified by reading each at head `27f3066ca`. What is open is the *mechanism*: nothing prevents them diverging again, and `--check` would stay green if they did. Until one of the closure shapes below lands, any edit to a `close_out` must be applied by hand to the prose carrier in the same commit, and the docstring carrier checked separately.
- **Why it is registered rather than argued.** It is the root cause of four consecutive review rounds on `u-he-37-plan-record`: r5 found the blocker recorded only in YAML and therefore invisible to `--detail`; r6 found the canonical YAML still saying two paths after the other two surfaces said three; r7 found a fourth carrier — the `tools/lanes_pilot.py` module docstring — asserted to agree while carrying two of four bounds; r8 found the plan's own paragraph saying "two" four lines above a sentence saying "all three". Each was a human re-typing a claim that has one authority.
- **Closure shape.** Either **derive** the rendered detail body from the canonical YAML, so the prose stops being a second authority — the stronger fix, since it deletes the class instead of detecting it, but it changes what `--detail` prints and needs the prose bodies that carry more than `close_out` reconciled first; **or** extend `check_prose_drift()` to compare the bodies it already pairs by heading — weaker, a second clock that at least alarms, and unable to express that a module docstring is a third carrier.
- **Why not here.** `tools/forward_register.py` matches `codex_context_guard.IMPL_RE` and this plan is a DESIGN surface, so the fix cannot ride the doc-only PR that found it. Interim state, verified by reading each surface at head `27f3066ca`: the three register-side enumerations are currently identical and in the same order.
