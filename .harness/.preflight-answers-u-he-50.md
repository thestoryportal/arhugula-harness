# defect-class-preflight answers — u-he-50 (C-HE-27 §5 X6a wrapper verify-span emission)

Diff: justfile (`review-with-failover-logged` emits verify start/end),
tools/test_review_loop_gate.py (+6 tests, fixture extension, helpers),
.claude/skills/roadmap-continue/SKILL.md (interim absorb/edit carrier block),
.claude/skills/ship-pr/SKILL.md (wrapper-emission notes on the definition block).

1. **Race/TOCTOU**: no new coordination idiom — emission funnels through the existing
   `reservations.py record_phase` exclusive-create CAS (holder-fenced, first-write-wins,
   replay-idempotent). Concurrent same-edge emission resolves in the CAS; a crash between
   start and end leaves a half pair that yields no span (reads null, never a fabricated
   duration).
2. **Prose drift**: cites verified at HEAD this session — C-HE-27 §5 X6a (spec read),
   B-218 (present in justfile comment + ship-pr block), "first-write-wins /
   replay-idempotent" (reservations.py:530-545). The ship-pr block is cited by its
   "Phase-span edges" heading, not line numbers.
3. **Silent failure**: emit_verify's `|| echo WARN >&2` is loud (the CLI's own `ABORT:`
   stderr line also surfaces) and never rewrites the verdict exit; the unreserved skip
   (`return 0`) is the spec'd C-HE-27 §3 disposition — absent span reads null downstream,
   never a measured zero. stdout `>/dev/null` discards only the success JSON echo.
4. **Vacuous witness**: every new test asserts through the REAL store (child-process
   `show` read-back). Mutations reasoned: drop `emit_verify start` → pair test red; drop
   `end` → set-equality red; move start above launch → refused-launch test red; drop the
   env-guard → unreserved test red (set -u error arm → WARN asserted absent); drop the
   `|| echo` arm → emission-failure test red.
5. **Timeout/budget**: none added.
6. **Unreachable branch**: both emit_verify arms reached by tests (skip arm: unreserved
   test; WARN arm: emission-failure test).
7. **Env-var**: production code only READS HARNESS_ARC_ID/HARNESS_LANE_ID (same meaning
   as the existing review_wrapper_common consumer — one producer, the session prefix).
   Tests pop both from inherited env (hermetic against a live session's exports) and
   pass ARC_METRICS_QUEUE_DIR per-call; no os.environ writes.
8. **Subprocess boundary**: the shim execs the real reservations.py as a child with the
   run()-built env — env lands at spawn; no monkeypatch-across-process assumptions.
9. **Path/default**: ARC_METRICS_QUEUE_DIR unset in production → the real per-project
   queue (intended: production emission on the operator store). The fixture pins it to
   tmp for ALL modes so even a defective guard can never touch the operator's real store.
10. **Fixture scope**: function-scoped recipe_env, subprocess-isolated; no conftest hooks.
11. **New authority surface**: none — no new verb/recipe; the recipe calls the existing
    guard-allowlisted `phase` verb whose authority half (holder fence) pre-exists.
    Carrier parity: roadmap-continue + ship-pr both updated; static composition pin test
    pins the recipe shape.

New-consumer pause: the recipe becomes a new WRITER through the existing CLI boundary
(record_phase validates phase/edge/holder — parse-don't-validate lives there); the tests'
only new read is `show` JSON post-seed with returncode asserted. No unprobed external SDK.

## Grep-hit labels, answered verbatim

- [save/restore pair: two-armed restore] — no save/restore pair in the diff: the
  fixture's `env.pop(...)` operates on a fresh `dict(os.environ)` COPY per run (nothing
  mutated, nothing to restore), and the recipe mutates no state it must restore.
- [check-then-act on paths] — the only new path predicates are read-only TEST
  assertions (`.exists()` / `.read_text()` post-run); the recipe adds no path
  check-then-act — store writes funnel through record_phase's exclusive-create CAS.
- [bare counts/absolutes in prose] — "the only span writer" is the single-enforcer
  claim, witnessed by the store's one write funnel (record_phase) and the CLI carrier;
  "ONE pair per phase" restates the C-HE-27 §3 durable-map contract; "0 of 60 expected
  edges" is bound to the recorded U-HE-35 baseline ([B] §3 item 4), not a live count.
- [new retry/timeout constants] — zero new bounds/budgets/timeouts; grep hits are the
  words "retry"/"replay" describing record_phase's existing replay-idempotency.

No findings deferred.
