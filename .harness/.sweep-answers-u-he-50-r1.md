# Round-1 sweep answers — u-he-50

Finding codex_review_wrapper:3bda19bb6fbb0b82d48f785e679d412051158021:12a41033849c:1
(P2, justfile:777 partial-write pair recovery) — ABSORBED, disposed `accepted` by
claude_absorber. Fix: a start-failed attempt mutes its own end emission (a lone end
would durably record a reversed pair; record_phase refuses rewrites); the start-only
crash window is pinned as a named measurement bound (retry keeps the round identity per
U-HE-49; its terminal closes the pair; original start preserved by first-write-wins).
Witness: +2 tests (fail-start injection → nothing recorded, retry coherent; crash
window closed by retry end); skip-arm mutation probe run and killed (deleted arm →
`test_recipe_start_failure_records_no_partial_pair` red), file restored byte-clean.

Class-sibling sweep (partial paired-emission class) over the whole diff: the only other
paired emissions the diff touches are the SESSION-emitted absorb/edit edges in the
interim carrier block — same crash-window property by inheritance from the ship-pr
definition (its own "Named measurement bound" prose covers it); a wrapper-side flag
cannot reach a session-layer emitter, and inventing a second enforcement point there
would violate single-enforcer. No other siblings.
