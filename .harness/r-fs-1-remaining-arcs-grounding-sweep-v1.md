# R-FS-1 Remaining-Arcs Grounding Sweep

**Authored:** 2026-06-14 · **Posture:** mode-agnostic (process-substrate; authors only this `.harness/` file). · **HEAD:** `3835408e`. · **Arc:** R-FS-1 recommendation-1 (one-time parallel grounding sweep, operator-authorized).

**What this is.** A breadth grounding pass over the **9 remaining R-FS-1 child arcs** (`E → B2 → R → B4 → CA → B5 → B6 → B7 → M`; B1 COMPLETE, B3 in spec). Produced by a read-only multi-agent Workflow (9 concurrent body-level analysts, one per arc) to right-size + sequence the downstream build and flag X-AL-3 forks early. Each arc still gets a fuller re-ground at its own arc-open — **these dossiers are durable LEADS, not final decisions** (`[[subagent-landscape-reports-need-regrounding]]`: presence-not-correctness).

**Staleness discipline (the sweep's core method).** The spine ledger (`beyond-mvp-capability-boundary-ledger.md`, HEAD `3d805d2`) + scoping doc (`cf292da`) predate B1 arcs #2–#16 + B3 design. Every agent was told to treat the embedded ledger evidence as a *lead to verify, not a fact* — re-resolve symbols, distrust line numbers, read the body not the docstring. The `Drift-from-ledger` rollup (§ end) captures what each agent found that **differs** from the stale evidence — feeds ledger hygiene.

**Re-ground hard on E next** (the arc immediately after B3); the rest are advisory until their arc-open. Do **not** gate B3-spec-1 on these.

---

## Summary table

| Arc | Name | Classification | Fork vs Impl | Confidence | Shape |
|---|---|---|---|---|---|
| **E** | Engine classes (durable execution engines, hand-roll per I-6) | `partially-built` | `fork-owed` | high | LARGE multi-slice arc, NOT thin wiring. ~3 sub-arcs (one per unbuilt engine class), each a… |
| **B2** | Multi-server MCP client | `partially-built` | `fork-owed` | high | Full fork-spec-plan-impl arc (NOT thin wiring). The singular-to-mapping C-RT-04 reshape is… |
| **R** | LLM_AS_ROUTER L3 + EMBEDDING L2 routing | `partially-built` | `mixed` | high | Two-track design-fork-first; L3 nearer, L2 larger (corpus). Sequence L3 then L2. |
| **B4** | Per-role / per-step dispatch indexing | `partially-built` | `mixed` | high | Thin-to-medium impl arc, NOT a full research→design→spec→plan→impl. The hard design questi… |
| **CA** | Cost aggregate (RunResult.cost_attribution rollup) | `partially-built` | `mixed` | high | Thin-to-medium arc: a small pre-authorized spec fork (Slice 0, ~20-40 lines C-RT-09 amendm… |
| **B5** | Memory per-deployment-surface backend selection (U-RT-80 factory) | `built-but-vacuous` | `impl-discretion` | high | Thin single-bounded impl arc (no research/design/spec phase). Shape: small registry-intern… |
| **B6** | Per-tool sandbox-tier resolution + STDIO transport-floor | `partially-built` | `fork-owed` | high | Medium fork+impl arc (NOT thin). Shape: fork (runtime-spec amendment for per-tool resolver… |
| **B7** | OD composite sampler §9.2 conditional-row over-sampling | `built-but-vacuous` | `impl-discretion` | high | Single bounded impl arc (small). Shape: predicate-refinement + sampler-wiring + test-updat… |
| **M** | managed_agents C-RT-28 contract + production wiring (thin) | `built-needs-contract-or-wiring` | `mixed` | high | Thin contract+wiring arc (capability + live-proof already exist) — fuller than trivial bec… |

---

## Arc E — Engine classes (durable execution engines, hand-roll per I-6)

**Classification:** `partially-built` · **Fork vs impl:** `fork-owed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Body-verified at HEAD 3835408e. The EngineClass enum is CLOSED at 5 members (harness-cp/src/harness_cp/engine_class.py:23-53). Only 2/5 are durable-execution-MATERIALIZED at the workflow driver: workflow_driver.py:184 _IN_SCOPE_ENGINE_CLASSES = frozenset({PURE_PATTERN_NO_ENGINE, SAVE_POINT_CHECKPOINT}); the gate at :1351-1352 RAISES EngineClassNotYetMaterializedError for EVENT_SOURCED_REPLAY / RECONCILER_LOOP / WAL_SEGMENT. SAVE_POINT_CHECKPOINT drives a real resumption fork (:1445 _determine_resume_at over IS read_by_idempotency_key); PURE_PATTERN_NO_ENGINE is in-process no-resume. So 3/5 engine classes are genuinely UNBUILT as durable-execution semantics. Sub-finding: the engine-layer recovery driver RuntimeEngineRecoveryLoop is built-but-UNWIRED (producer_map). The '2 of 5' ledger count is accurate; its DEFER disposition is superseded by the full-spec directive — see evidence_drift_from_ledger.

**Producer map.** The discriminator is producer-reachability, and it splits cleanly. (1) The OBJECT is produced: RuntimeEngineRecoveryLoop is constructed at materialize_r_cxa_2_producer_loop_stage (r_cxa_2_producer_loop_factory.py:208), reached from a real production path stage_5_loop_init.py:227 → run_bootstrap, stored at ctx.engine_recovery_loop. (2) The METHODS are dormant: .capture_pause / .attempt_resume have ZERO production callers — grep confirms invocation only in harness-runtime/tests/test_engine_recovery_loop.py:123,140 and test_r_cxa_2_producer_loop_factory.py. C-CP-49/C-CP-50 (pause_resume_protocol.py:856/951 — DRIFTED from the brief's :489) are reachable ONLY through that dormant loop via wiring.emit_*. The production pause/resume that DOES fire is the WORKFLOW-LAYER PauseResumeProtocol (attempt_resume at workflow_driver.py:1203 + the DURABLE_ASYNC HITL pause), a distinct layer emitting cp.pause-resume-protocol — NOT the engine layer. So: engine-layer recovery driver = built-but-unwired; 3 engine classes = unbuilt durable semantics. A REAL hand-rolled engine that genuinely emits engine-layer pauses is exactly the producer this arc must create — wiring that real driver is what finally fires C-CP-49/50 and brings the R-CXA-2 CP→IS engine-layer seam live in production.

**Fork vs impl detail.** I-6 is a HOW constraint (no vendored Temporal/K8s/Kafka/LangGraph), NOT a vendor-gate — there is no external corpus/model/credential to wait on; the full-spec resolution is to HAND-ROLL the substrates. So this is not vendor-gate. It is fork-owed because, while the enum + capability-floors + candidate-mapping are spec'd (C-CP-07 §7.4), the per-class DURABLE-EXECUTION SEMANTICS for the 3 unbuilt classes are underspecified, and adding a class to _IN_SCOPE_ENGINE_CLASSES + its driver dispatch is, by the fork docs' own words, 'a design-extension at the driver boundary' (X-AL-3). Under the full-spec directive that design back-flow is PRE-AUTHORIZED, but it must still be authored (runtime/CP spec amendment + clearance marker per slice), not silently absorbed. Mixed lean: EVENT_SOURCED_REPLAY / WAL_SEGMENT substrates are cleanly hand-rollable with no external dep (filesystem/sqlite event store; append-only segment log — JournalEnginePauseResumeSubstrate #475 is a real proven starting point); only RECONCILER_LOOP's LIVE e2e is infra-gated (engine_class_candidate.py:70 'requires K8s control plane'), so its proof — not its build — carries an infra gate.

**Governing spec sections:**
- **Spec_Control_Plane_v1_2.md (C-CP-07 last substantive def; preserved into v1_30 head per delta-only chain)** §7.1 / §7.2 / §7.4 — Closed 5-element engine-class taxonomy + per-deployment-surface candidate mapping + per-class F3 capability-floor preservation table. §7.4 'Deferred to implementation discretion' covers specific candidate-within-cell substrates (Temporal/DBOS/Restate; Kafka-style WAL; K8s CRD reconciler) — the substrates I-6 forbids vendoring and the full-spec arc must hand-roll.
- **Spec_Control_Plane_v1_30.md (CP head; §16.5 last substantive at v1_25-29)** §16.5.2 / §16.5.3 / §16.5.7 — Engine-layer CP→IS state-ledger emission contract: U-CP-49 emit_pause_captured (C-CP-49) + U-CP-50 emit_resume_attempted (C-CP-50); ZERO CPAuditLedgerEntry (greenfield, §16.5.9 invariant 5); distinct action_id (cp.pause-captured / cp.resume-attempted) vs workflow-layer cp.pause-resume-protocol. Go live only when a real engine driver fires them.
- **Spec_Control_Plane_v1_2.md (C-CP-10/D4 §18.1 + per_engine_class_topology_overlay)** §18.1 (HITL synchrony × engine-class) — DURABLE_ASYNC HITL matrix cell requires RECONCILER_LOOP / WAL_SEGMENT per CP §18.1. The Path-(i) e2e (test_u_rt_95) is @pytest.mark.skip pending one of the 3 unbuilt classes entering _IN_SCOPE_ENGINE_CLASSES — the e2e gate the arc un-skips.

**Key carrier files:**
- `harness-cp/src/harness_cp/engine_class.py` — Closed 5-member EngineClass enum + CapabilityFloor records + CAPABILITY_FLOORS (C-CP-07 §7.1/§7.4). BUILT for all 5 (declaration only); the 3 unbuilt classes are enum members with no durable semantics. · `EngineClass (StrEnum); EVENT_SOURCED_REPLAY/RECONCILER_LOOP/WAL_SEGMENT members; CAPABILITY_FLOORS`
- `harness-cp/src/harness_cp/workflow_driver.py` — Materialization GATE. :184 _IN_SCOPE_ENGINE_CLASSES = {PURE_PATTERN_NO_ENGINE, SAVE_POINT_CHECKPOINT}; :1351-1352 raises EngineClassNotYetMaterializedError for the 3 unbuilt classes; :1445 SAVE_POINT_CHECKPOINT resumption fork. Primary site the arc extends per class. · `_IN_SCOPE_ENGINE_CLASSES; EngineClassNotYetMaterializedError raise at line 1352`
- `harness-cp/src/harness_cp/pause_resume_protocol.py` — Defines C-CP-49 emit_pause_captured_state_ledger_entry (:856) + C-CP-50 emit_resume_attempted_state_ledger_entry (:951) engine-layer CP→IS composers, EnginePauseResumeSubstrate Protocol (:123), DeterministicEnginePauseResumeSubstrate (:171). Composers reached ONLY via the dormant recovery loop. · `emit_pause_captured_state_ledger_entry (:856); emit_resume_attempted_state_ledger_entry (:951); EnginePauseResumeSubstrate (:123)`
- `harness-runtime/src/harness_runtime/lifecycle/engine_recovery_loop.py` — RuntimeEngineRecoveryLoop (:45); .capture_pause (:52) calls wiring.emit_pause_captured, .attempt_resume (:74) calls wiring.emit_resume_attempted. Built-but-UNWIRED: methods have ZERO production callers. · `RuntimeEngineRecoveryLoop.capture_pause / .attempt_resume`
- `harness-runtime/src/harness_runtime/bootstrap/factories/r_cxa_2_producer_loop_factory.py` — Stage-5 factory materialize_r_cxa_2_producer_loop_stage (:169) constructs RuntimeEngineRecoveryLoop (:208) with in-memory DeterministicEnginePauseResumeSubstrate (:210), stores at ctx.engine_recovery_loop (:216). Invoked from stage_5_loop_init.py:227. Produces the object, not a driver of its methods. · `materialize_r_cxa_2_producer_loop_stage; RuntimeEngineRecoveryLoop(...) at :208`
- `harness-runtime/src/harness_runtime/lifecycle/journal_pause_resume_substrate.py` — JournalEnginePauseResumeSubstrate (:123) — durable filesystem-journal engine substrate (#475, F2 recovery). EXISTS + tested but UNBOUND in production (factory binds Deterministic). A real hand-rolled durable substrate the arc can repurpose for WAL_SEGMENT / EVENT_SOURCED. · `JournalEnginePauseResumeSubstrate`
- `harness-cp/src/harness_cp/engine_class_candidate.py` — Per-deployment-surface candidate mapping (U-CP-16, §7.2). :70 marks RECONCILER_LOOP excluded at local-development ('requires K8s control plane') — the one genuinely infra-gated engine class for live e2e. · `EngineClassCandidate; RECONCILER_LOOP cell-exclusion 'requires K8s control plane'`
- `harness-runtime/src/harness_runtime/api.py` — WORKFLOW-LAYER (distinct from this arc): api.run (:487) + api.resume (:645, R-CC-1 arc #3, BUILT) — durable-resume via PauseSnapshot or durable-store handle. The SIBLING capability already landed; arc E is the ENGINE layer, NOT this. · `async def resume (:645); _read_durable_pause_snapshot (:612)`

**Build slices (ordered):**
- Slice 0 (drift hygiene + scope confirmation): re-state at arc-open that the .harness/ DEFER dispositions (forward-register line 181 bounded-residual; R-CL-P2 DEFER; class_2 fork Option-3) are SUPERSEDED by the full-spec directive; the 're-open when a real event-sourced-replay/reconciler-loop/WAL-segment lands' trigger is now ACTIVE. Confirm the engine LAYER is the target (workflow-layer api.resume already done, R-CC-1 arc #3 — out of scope).
- Slice 1 — EVENT_SOURCED_REPLAY (lowest substrate cost, ~15-25 commits): design-fork → runtime/CP spec amendment for event-sourced deterministic-replay engine semantics + clearance marker; hand-roll a real in-house event store (filesystem/sqlite append-only event history with cached activity outputs, no vendored engine); add EVENT_SOURCED_REPLAY to _IN_SCOPE_ENGINE_CLASSES + driver dispatch fork at workflow_driver.py:1351; bind a real EnginePauseResumeSubstrate so RuntimeEngineRecoveryLoop gains a production driver firing C-CP-49/50; e2e against the real substrate.
- Slice 2 — WAL_SEGMENT (canonical DURABLE_ASYNC, ~20-35 commits): design-fork → spec amendment for append-only segment-log engine + per-segment resume + clearance; hand-roll segment writer + segment replay + idempotent per-segment consumer state (extend/repurpose JournalEnginePauseResumeSubstrate #475 mechanism); add to _IN_SCOPE_ENGINE_CLASSES + driver dispatch; wire engine-layer pause production → C-CP-49/50; un-skip the Path-(i) DURABLE_ASYNC e2e (test_u_rt_95_hitl_pause_trigger_durable_async_full_execution_path).
- Slice 3 — RECONCILER_LOOP (highest substrate cost, ~25-45 commits, infra-gated e2e): design-fork → spec amendment for control-loop/CRD-reconciler engine semantics (topology_fault_handling=RECONCILER, T-perm-3 D1-layer) + clearance; hand-roll a reconciler tick-loop + state read/diff/converge over a real persistence layer (etcd-style, no vendored K8s); add to _IN_SCOPE_ENGINE_CLASSES + driver dispatch. LIVE e2e requires a K8s control plane (engine_class_candidate.py:70 excludes it at local-development) — surface that infra gate; non-live unit/integration proof is local.
- Slice 4 (cross-cutting, per class): verify the 4 F3 capability-floors (CAPABILITY_FLOORS) genuinely hold per new class (durable replay across restart / idempotency-keyed exactly-once via F2 ledger / lease coordination / observable lifecycle 8 events per C-CP-05 §5.1) — not just enum-membership; update per_engine_class_topology_overlay + workload_engine_class_matrix + workload_binding_engine_class_selection consumers; refresh the relevant CP substitution rows + the Path-(i) and engine-recovery fork docs.

**Blockers / gates:**
- X-AL-3 / fork-owed per slice: each engine class is a driver-boundary design-extension → spec amendment + clearance marker BEFORE impl (full-spec directive pre-authorizes the back-flow, but it must still be authored, not silently absorbed).
- I-6 hand-roll mandate (HARD how-constraint): NO vendored engine (Temporal / Restate / DBOS / LangGraph / K8s operators / Kafka). Substrates must be in-house. Forecloses fork-doc Option-2 (cosmetic Journal swap) and the 'pipe workflow-layer pause through the engine loop' fake-producer anti-pattern.
- Line 181 'no FAKE engine recovery loop' composes with (does not void) the build: the engine that fires the recovery loop must be a REAL hand-rolled engine emitting genuine engine-layer pauses — NOT workflow-layer pause piped through, and NOT fork-doc Option-D (stub-widening _IN_SCOPE_ENGINE_CLASSES just to land the e2e).
- RECONCILER_LOOP live e2e is infra-gated (K8s control plane per engine_class_candidate.py:70) — surface as an infra gate for that sub-arc's LIVE proof only; build + non-live tests are unblocked.
- PathClass placement: a journal/event-store/segment-log on disk must resolve against the CLOSED 4-class PathClass enum (harness-is path_class_registry.py; IS-AL-1 forecloses inventing one) — decide placement WITH the driver that consumes it (legitimate here since the driver becomes real, but must fit the closed enum or carry its own IS spec-extension fork).
- Large multi-session multi-axis arc (fork-doc estimates 15-45 commits per class) — must be sequenced, not attempted as one PR.

**Cross-arc deps:**
- WORKFLOW-LAYER api.resume() (R-CC-1 arc #3, PR #513/#514) is DONE and is the DISTINCT SIBLING — NOT this arc. Do not re-scope it into E; E is the engine LAYER (ctx.engine_recovery_loop + the 3 unbuilt EngineClass durable semantics).
- B1 (topology strategies, arcs #2-#16) shares the SAME workflow_driver.py dispatch site: the topology-strategy dispatch (_execute_parallelization etc., :1375+) sits immediately after the engine-class gate (:1351). All 6 topology patterns already landed; the engine-class gate is the orthogonal axis still raising for 3/5 classes. Materializing an engine class must compose with strategy dispatch without regressing the SINGLE_THREADED_LINEAR byte-unchanged invariant (§25.10 Invariant 1).
- R-CXA-2 CP→IS engine-layer seam (C-CP-49/C-CP-50, §16.5.2): currently a ratified bounded-residual; goes LIVE in production exactly when a real engine driver fires the recovery loop — this arc is the producer that activates that seam.
- H_T-RT-35 / class_1_tension_u_rt_35 (cp_is_wiring gaps) family: the engine-layer composers are part of the §16.5 CP→IS emission cluster; materializing the driver completes the producer side that closure tracks.
- CA (cost) + B-family share workflow_driver/RunResult; a new engine class must emit the 8 lifecycle events (C-CP-05 §5.1 observable_lifecycle floor) and per-dispatch cost like the existing 2 classes.
- SHARED-RUNTIMECONFIG cluster: E converges on RuntimeConfig + the workflow_driver dispatch path with B3/B4/B6/B2 — kept SERIAL.

**Estimated shape.** LARGE multi-slice arc, NOT thin wiring. ~3 sub-arcs (one per unbuilt engine class), each a full research→spec-extension(fork)→hand-rolled-real-substrate→driver-dispatch→recovery-loop-real-producer→e2e cycle, plus a cross-cutting capability-floor + consumer-update pass. Fork-doc commit estimates: EVENT_SOURCED_REPLAY 15-25, WAL_SEGMENT 20-35, RECONCILER_LOOP 25-45. Sequence by ascending substrate cost: EVENT_SOURCED_REPLAY → WAL_SEGMENT → RECONCILER_LOOP. Each slice its own PR-cluster (likely multi-session). Sits in the SHARED-RUNTIMECONFIG cluster (serial with B3/B4/B6/B2) because it touches RuntimeConfig + the workflow_driver dispatch path.

**Open questions:**
- Does the full-spec directive require all 3 unbuilt classes, or a priority subset? (Recommend all 3 per 'nothing deferred'; sequence by cost.) Confirm at arc-open.
- Does EVENT_SOURCED_REPLAY satisfy the §18.1 DURABLE_ASYNC matrix-cell semantics, or is that strictly RECONCILER_LOOP / WAL_SEGMENT? (class_1_fork_path_i §4 option C flags this scope-clarification — affects which slice un-skips test_u_rt_95.)
- PathClass placement for the hand-rolled event-store / WAL segment-log on disk against the CLOSED 4-class enum (path_class_registry.py): which existing class, or is an IS path-class-registry spec-extension owed (IS-AL-1)? Decide WITH each driver.
- Should the existing JournalEnginePauseResumeSubstrate (#475) be the foundation for WAL_SEGMENT / EVENT_SOURCED (real proven journal mechanism), and how to bind it via a REAL driver that fires it (not the cosmetic-swap anti-pattern)?
- RECONCILER_LOOP: is a hand-rolled etcd-style CRD reconciler in scope, or is this class deferred to a deployment-surface where K8s is present (self-hosted/managed-cloud per engine_class_candidate.py)? The LIVE e2e infra gate needs an operator decision.
- Per-class capability-floor verification depth: prove all 4 F3 floors hold per new class (not enum-membership) — what is the acceptance bar (unit vs e2e) for each floor?

---

## Arc B2 — Multi-server MCP client

**Classification:** `partially-built` · **Fork vs impl:** `fork-owed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Verified at HEAD 3835408e by reading bodies and callers. The single-server MCP-client capability is fully built AND production-wired: MCPClientHost (harness-runtime/.../lifecycle/mcp_client_host.py) implements all 3 transports (stdio/streamable_http/sse) with start/health_check/shutdown/call_tool bodies (lines 283-552, not stubs); the stage-3a factory materialize_mcp_client_host_stage (mcp_client_host_factory.py:144) materializes ONE host from config.mcp_clients[0]; stage_3a_cp_clients.execute (line 60-71) binds it and calls start; the dispatcher gates trust on every dispatch (runtime_tool_dispatcher.py:628 calls self._trust_evaluator.evaluate(self._mcp_client_host.server_name)). Production caller chain is REAL not dormant: api.run (api.py:487) to run_bootstrap to orchestrator wires (CP_CLIENTS, stage_3a_cp_clients) and (LOOP_INIT, stage_5_loop_init) (bootstrap/__init__.py:105,108). What genuinely REMAINS (the B2 multi-server dimension) decomposes into the discovery/routing/trust triad: (1) DISCOVERY - per-host list_tools is built; a cross-host unified tool registry across N hosts is UNBUILT; (2) ROUTING - UNBUILT: the dispatcher is hardwired to one host (runtime_tool_dispatcher.py:617 reads self._mcp_client_host.tool_registry.get; :624/:629/:718 read the single server_name) - no tool-to-server resolution across multiple hosts exists; (3) PER-SERVER TRUST RESOLUTION - the GATE logic is built and multi-server-READY (PerServerTrustEvaluator.evaluate uses per_server_overrides[server_name], deny_list, allow_list, tier-floor, all keyed on server_name - per_server_trust_evaluator.py:178-239) and prod-wired, but is only ever CALLED with one server_name because only one host exists. Hence partially-built, not built-but-unwired (built parts ARE wired) and not built-but-vacuous (the per-server gate is sound; only the bounded telemetry vacuity below).

**Producer map.** REAL production caller, not a dormant driver. Chain verified at HEAD: api.run (harness-runtime/src/harness_runtime/api.py:487) to run_bootstrap (api.py:547,826) to bootstrap orchestrator (bootstrap/__init__.py) which wires (BootstrapStage.CP_CLIENTS, stage_3a_cp_clients) at line 105 and (BootstrapStage.LOOP_INIT, stage_5_loop_init) at line 108. stage_3a_cp_clients.execute materializes the single MCPClientHost via materialize_mcp_client_host_stage(config) and calls start (line 60-71). stage_5 builds the dispatcher plus per_server_trust_evaluator. A TOOL_STEP in a workflow reaches RuntimeToolDispatcher.dispatch then trust gate plus single-host tool_registry. So the single-server path is genuinely production-reachable. CAVEAT (confidence): wiring is confirmed (api.run to bootstrap to stages to dispatcher), but I did NOT run an e2e TOOL_STEP; v1.40 once showed AC#2 blocked, 14.9.9 (R-CC-1, 2026-06-11) claims it closed - treat single-server as wiring-confirmed, not e2e-proven. For the MULTI-server (B2) dimension specifically there is NO producer at all: nothing materializes a second host, nothing routes tool-to-server across hosts.

**Fork vs impl detail.** Clean discriminator: does the cleared C-RT-04 field type admit N hosts? No - it is singular mcp_client_host of type MCPClientHost (spec line 2206), and the factory contract returns a singular MCPClientHost (14.9.3). Multi-server CANNOT be built without a field-table amendment (singular to mapping, e.g. dict[ServerName, MCPClientHost] or list) plus factory-signature amendment plus dispatcher tool-to-server routing contract. Every remainder is spec-gated, at every site: 14.9.6 inv 1 operator-materializes-N-instances is descriptive aspiration the actual surface does not realize; 14.9.8/14.9.9 explicitly say v1 MVP is single-server per 14.9.3 factory; _trust_tier_from_level docstring says production deployments will surface a spec-committed mapping at a future arc per Q1a=(i); the factory docstring says multi-server will route to a field-table plus factory signature amendment via planner revision-pass. This is X-AL-3 territory (new contract/field/semantics not in the cleared spec) - halt plus back-flow (Class 1) before impl. NOT mixed: there is no B2 capability slice buildable now with zero spec amendment - the only impl-discretion sliver is making the factory actually emit the more-than-one-server warning its docstring falsely claims, which is doc hygiene, not a capability. The Part-2 idempotent-restart minor is ALSO fork-owed (14.9.6 inv 1 idempotent restart out of scope at v1; deferred to operator-driven restart arc - start actively refuses re-start).

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** C-RT-04 field table line 2206 — HarnessContext field-table commits singular mcp_client_host of type MCPClientHost (runtime-defined, NOT a mapping). This singular field type is the hard gate: multi-server requires a field-table amendment.
- **Spec_Harness_Runtime_v1.md** 14.9.3 stage-3a factory lines 138 1451 — materialize_mcp_client_host_stage(config) returns one MCPClientHost (singular). The field RuntimeConfig.mcp_clients is already a list of MCPClientConfig (config is already plural).
- **Spec_Harness_Runtime_v1.md** 14.9.6 invariant 1 line 3808 — Says operator materializes N MCPClientHost instances for N servers but this is descriptive aspiration; the actual contract surface (singular field plus singular factory return) does NOT realize it. Also: idempotent restart out of scope at v1 (the Part-2 restart minor).
- **Spec_Harness_Runtime_v1.md** 14.9.8 and 14.9.9 lines 3851 3891 — Both explicitly defer multi-server: 14.9.8 says multi-server resolver composition is v1 MVP single-server; 14.9.9 per-server-uniform driver from mcp_clients[0]. This is the sandbox resolver/driver surface SHARED with B6.
- **Spec_Control_Plane_v1_30.md** C-CP-27 section 27 PerServerTrust — Canonical per-server trust contract: PerServerTrustEvaluator plus TrustPolicy carriers. 4-level MCPTrustTier enum in cp_shared_types.py:180-189. The brief C-CP-43 is a STALE plan-v2.6/v2.7 cite; live gate contract is C-CP-27.
- **Spec_Action_Surface_v1.md** C-AS-10 sections 10.1 and 10.3 lines 857 877 — AS-side per-MCP-transport sandbox-tier floor table plus 4-level MCP server trust framework. MCPServerTrustLevel AS enum at sandbox_tier_floor.py:59. The AS-to-CP trust-level projection is what _trust_tier_from_level stubs vacuously.

**Key carrier files:**
- `harness-runtime/src/harness_runtime/bootstrap/factories/mcp_client_host_factory.py` — PRIMARY single-vs-multi carrier: consumes config.mcp_clients[0] (line 173), warns nothing on more-than-one (docstring lies), conservatively stubs trust mapping · `materialize_mcp_client_host_stage; _trust_tier_from_level always returns LEVEL_0_REFUSE_REMOTE at line 197`
- `harness-runtime/src/harness_runtime/lifecycle/mcp_client_host.py` — Per-server host lifecycle (built, all 3 transports). start idempotent-restart REFUSED (raises MCPHostAlreadyStartedError, line 293-299) - the Part-2 restart minor · `MCPClientHost (start/health_check/shutdown/call_tool); MCPHostAlreadyStartedError; MCPHostHealth`
- `harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py` — ROUTING carrier - HARDWIRED to one host: resolves contract and trust against self._mcp_client_host (single) · `RuntimeToolDispatcher.dispatch - tool_registry.get at line 617; server_name at 624/629/718; trust_evaluator.evaluate at 628`
- `harness-cp/src/harness_cp/per_server_trust_evaluator.py` — Per-server TRUST GATE - fully built and multi-server-READY (keyed on server_name); prod-wired but only ever called with one server · `PerServerTrustEvaluator.evaluate (per_server_overrides/deny_list/allow_list/tier-floor branches, lines 178-239)`
- `harness-runtime/src/harness_runtime/types.py` — Config and field carriers: RuntimeConfig.mcp_clients is list of MCPClientConfig (line 1238, ALREADY PLURAL); MCPClientConfig model (line 570); ctx.mcp_client_host typed Any singular (1813) · `MCPClientConfig; RuntimeConfig.mcp_clients; HarnessContext.mcp_client_host`
- `harness-runtime/src/harness_runtime/bootstrap/factories/runtime_tool_dispatcher_factory.py` — Stage-5 factory: builds trust evaluator and sandbox resolver/driver from config.mcp_clients[0] (lines 269, 281) - single-server consumption shared with B6 · `materialize_runtime_tool_dispatcher_stage; _build_host_info_lookup(ctx.mcp_client_host)`
- `harness-runtime/src/harness_runtime/bootstrap/stage_3a_cp_clients.py` — Production stage-3a body: materializes the single host and starts it if config.mcp_clients non-empty (lines 60-71) · `execute (calls materialize_mcp_client_host_stage plus ctx.mcp_client_host.start)`

**Build slices (ordered):**
- Slice 0 (FORK / spec-extension, X-AL-3 - gates everything): file a Class 1 fork then runtime-spec amendment reshaping C-RT-04 mcp_client_host from MCPClientHost to a mapping (e.g. dict[ServerName, MCPClientHost]) plus amending 14.9.3 factory contract to return N hosts plus defining the tool-to-server discovery/routing contract plus 14.9.6 inv 1 multi-host lifecycle plus CP-side per-server-trust projection (resolve _trust_tier_from_level Q1a=(i) AS-MCPServerTrustLevel to CP-MCPTrustTier mapping). Also AS C-AS-10 10.3 trust framework alignment. Decide: unified aggregated tool registry vs per-host registries with a router; collision policy for same-named tools across servers.
- Slice 1 (config - mostly DONE): RuntimeConfig.mcp_clients is already a list of MCPClientConfig; confirm per-server fields suffice for N servers (client_name as routing key already present). Likely only a server-name-uniqueness validator.
- Slice 2 (materialization): rewrite materialize_mcp_client_host_stage to materialize ALL config.mcp_clients entries (not [0]) into the new mapping; stage_3a_cp_clients.execute to start each host; per-server trust projection per resolved Slice-0 mapping (replace the always-LEVEL_0 stub).
- Slice 3 (discovery/registry): build cross-host tool discovery - aggregate each host list_tools-populated registry into a unified lookup OR a server-scoped registry keyed by server_name; handle same-tool-name collisions per Slice-0 policy.
- Slice 4 (routing - the core unbuilt capability): rewire RuntimeToolDispatcher to resolve tool_id to owning host across N hosts (replace the hardwired self._mcp_client_host at runtime_tool_dispatcher.py:617/624/628/718), then gate per-server trust against the RESOLVED host server_name and dispatch to that host.
- Slice 5 (downstream consumers): update the ~10 ctx.mcp_client_host consumers for the field reshape - runtime_tool_dispatcher_factory (info_lookup, sandbox resolver/driver from [0]), shutdown.py (drain all hosts), mutable_context, docker/e2b drivers, validator_escalation_composer.
- Slice 6 (Part-2 minor, separate fork-gate): idempotent MCP-host restart/recovery - STDIO/HTTP/SSE host lifecycle restart (start currently refuses re-start per 14.9.6 inv 1); add RT-FAIL-MCP-HOST-UNREACHABLE mid-dispatch recovery.
- Slice 7 (tests plus e2e): per-server-trust differentiation across N servers; tool routing to the correct host; e2e TOOL_STEP through api.run against 2-plus mock MCP servers.

**Blockers / gates:**
- GATE (hard): C-RT-04 singular mcp_client_host field reshape requires a Class 1 fork / runtime-spec amendment BEFORE impl (X-AL-3). No impl proceeds without operator-ratified spec extension.
- FORK DECISION at arc-open: unified aggregated tool registry vs per-host registries plus router; same-tool-name-across-servers collision policy; ServerName routing-key semantics (client_name reuse vs new type).
- FORK DECISION: the AS-MCPServerTrustLevel to CP-MCPTrustTier projection (_trust_tier_from_level Q1a=(i)) - currently vacuous-stubbed; the multi-server arc must resolve the spec-committed mapping.
- High blast radius: C-RT-04 field reshape ripples to ~10 consumers (dispatcher, dispatcher_factory, shutdown, mutable_context, docker/e2b drivers, validator_escalation_composer) - size as a full arc, expect cross-axis test breakage (shared-IS field-shape asserts, CXA-P1 enumeration).
- Idempotent-restart (Part-2 minor) is a SEPARATE spec-gate (14.9.6 inv 1 operator-driven restart arc) - own thin fork or folded into Slice 0; decide at arc-open.
- SERIAL constraint: B2 is in the SHARED-RUNTIMECONFIG cluster - must not run concurrently with B3/B4/B6 (shared RuntimeConfig plus runtime dispatch path).

**Cross-arc deps:**
- SHARED-RUNTIMECONFIG cluster (SERIAL with B3/B4/B6): B2 reshapes C-RT-04 / RuntimeConfig consumption plus the runtime tool-dispatch path - converges with B3 (smart-HITL), B4 (per-role dispatch), B6 (per-tool sandbox) on RuntimeConfig plus stage-5 dispatcher factory.
- B2 and B6 share 14.9.8/14.9.9: BOTH the multi-server resolver composition (B2) and per-tool sandbox granularity (B6) land on the SAME single-server SandboxDecisionResolver/driver (runtime_tool_dispatcher_factory.py:269,281 consume config.mcp_clients[0]). Coordinate the multi-server reshape so B2 host-mapping and B6 per-tool policy compose cleanly.
- CP axis (C-CP-27 PerServerTrust): per-server-trust evaluator is already built CP-side and multi-server-ready; B2 only needs the CP-to-runtime trust-tier PROJECTION (the AS-level-to-CP-tier mapping) resolved - minimal CP-axis work, mostly a runtime/AS projection decision.
- AS axis (C-AS-10 10.3 trust framework plus mcp_transport_floor): the per-MCP-transport sandbox-tier floor plus 4-level trust framework already exist AS-side; multi-server reshape should keep the AS floor table per-server consistent.
- CXA: no dedicated B2 seam found; field-shape change may trip the CXA-P1 enumeration allowlist plus shared-IS field asserts (run broader suite per shared-is-shape-change-ripples-cross-axis-field-asserts).

**Estimated shape.** Full fork-spec-plan-impl arc (NOT thin wiring). The singular-to-mapping C-RT-04 reshape is high-blast-radius (~10 consumers) and spec-gated at every site, so it owes: (1) a Class 1 fork doc, (2) a runtime-spec amendment (C-RT-04 field table plus 14.9.3 plus 14.9.6 plus 14.9.8/9 multi-server) likely with CP/AS spec touch for the trust projection, (3) a CP/runtime plan revision-pass for the new units (multi-host materialization, cross-host discovery/registry, dispatcher routing), (4) impl across the 8 carriers plus ~10 consumers plus e2e tests. The Part-2 idempotent-restart minor is a smaller sub-fork that can ride along or be deferred. Estimate: large arc, comparable to a topology-strategy arc - bigger than B5/B7 (independent thin) and bigger than M (thin contract plus wiring).

**Open questions:**
- Multi-host registry shape: unified aggregated ToolRegistry across N hosts, OR per-host registries with a server-scoped router? (drives the dispatcher routing contract plus same-tool-name collision policy)
- C-RT-04 reshape type: dict[ServerName, MCPClientHost] vs list of MCPClientHost? (routing-key choice; ServerName = reuse client_name or a new type)
- AS-MCPServerTrustLevel to CP-MCPTrustTier projection (the Q1a=(i) follow-on): what is the spec-committed mapping that replaces the always-LEVEL_0 stub? Should it live in runtime, CP, or AS?
- Does the Part-2 idempotent-restart minor fold into the multi-server fork or get its own thin fork? (14.9.6 inv 1 names an operator-driven restart arc)
- B2 vs B6 sequencing within the serial cluster: should the 14.9.8/9 multi-server resolver reshape be co-designed with B6 per-tool sandbox so they compose, or land B2 host-mapping first? (they share the single-server resolver/driver)
- E2E status: 14.9.9 (R-CC-1, 2026-06-11) claims single-server TOOL_STEP via api.run is unblocked - confirm with an e2e run at B2 arc-open before building multi-server on top (wiring-confirmed only, not e2e-verified here).

---

## Arc R — LLM_AS_ROUTER L3 + EMBEDDING L2 routing

**Classification:** `partially-built` · **Fork vs impl:** `mixed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Infra built+wired (bootstrap stage 5 to RuntimeLLMDispatcher.dispatch to infer to route, every step); L2/L3 decision-fns absent. NOT vacuous, NOT unbuilt. Only DECLARATIVE bound; no embedding model/corpus/router model/prompt.

**Producer map.** REAL production caller, reachable, NOT dormant. stage_5_loop_init.py:241 to materialize_llm_dispatcher_stage to RuntimeLLMDispatcher dispatch :393 to infer :547 to sync route; every inference step. GAP: infer binds ONLY DECLARATIVE; EMBEDDING+LLM_AS_ROUTER zero decision-fns (sole site :551) = the build. Post-build new fns bind at :547-553.

**Fork vs impl detail.** TWO gates. (A) FORK: Class 1 CP-spec amendment (pre-authorized). NO pure-impl path for L3. Reading B (recommended): resolve at already-async infer via injected async router callable, route unchanged, preserves ADD 5.3.3, still re-decomposes C-CP-02 2.1 = Class 1. Reading A not recommended. EMBEDDING shape UNANALYZED. (B) VENDOR-GATE never auto-fire: L3 router model+prompt+paid-call auth; L2 corpus+embedding model (neither exists). Slices 1-3 no paid boundary; only slice 4 is the gate.

**Governing spec sections:**
- **CP spec C-CP-02** 2.1 — layered resolution; L2 k-nearest on corpus; L3 invoke router model; decomposed into sync route + async infer
- **CP spec C-CP-02** 2.4 — vendor gate; defers embedding model, corpus, router prompt, router model binding to impl discretion
- **ADR-F1** Decision — capability-aware multi-LLM, rejects LiteLLM
- **ADD** 5.3.3 — determinism boundary; decides Reading A vs B

**Key carrier files:**
- `harness-cp/src/harness_cp/layered_routing_strategy.py` — sync route + LayerDecisionFn; Reading-A target · `route :62 :96-101`
- `harness-cp/src/harness_cp/routing_core_surface.py` — async infer; Reading-B integration point · `infer :129 :183`
- `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py` — sole production binding site; binds only DECLARATIVE · `RuntimeLLMDispatcher :298 _declarative_echo :496 infer :547-553`
- `harness-runtime/src/harness_runtime/bootstrap/stage_5_loop_init.py` — production caller; reachable not dormant · `materialize_llm_dispatcher_stage :241`

**Build slices (ordered):**
- L3-1 design+fork: Reading B re-decompose C-CP-02 2.1, L3 at async infer via injected router callable, clearance
- L3-2 plan: async router callable + infer router branch + trace
- L3-3 impl NO paid call: infer router branch + MOCK router fn at :547-553; tests
- L3-4 VENDOR GATE: bind real router model + prompt + gated live e2e; never auto-fire
- L2-1 design+fork: analyze EMBEDDING shape (sync vs async)
- L2-2 vendor sub-arc: author corpus + select embedding model
- L2-3 impl: k-nearest decision-fn + e2e

**Blockers / gates:**
- VENDOR GATE L3: router model+prompt+paid-call auth; never auto-fire
- VENDOR GATE L2: corpus (absent) + embedding model
- CLASS 1 FORK (pre-auth): C-CP-02 2.1 via Reading B; clearance per X-AL-3
- ARC-OPEN: Reading A vs B (recommend B)
- HOW committed: ADR-F1 per-provider SDKs NOT LiteLLM (I-6)

**Cross-arc deps:**
- NOT independent: shares infer/dispatch carrier :547-553 with SHARED-RUNTIMECONFIG cluster B3/B4/B6/B2; B4 co-located in _declarative_echo envelope
- Composes with B1 topology; router context needs agent_role (B1-B4 seam)
- Shares routing trace with CA cost (router call billable)

**Estimated shape.** Two-track design-fork-first; L3 nearer, L2 larger (corpus). Sequence L3 then L2.

**Open questions:**
- EMBEDDING shape: sync local model may fit sync LayerDecisionFn (no fork) vs async like L3
- Reading A vs B (recommend B)
- Router model impl-discretion; prefer Ollama
- CA capture router-call cost?
- L2 corpus vendored vs telemetry?

---

## Arc B4 — Per-role / per-step dispatch indexing

**Classification:** `partially-built` · **Fork vs impl:** `mixed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Body-verified at HEAD 3835408e. The per-role MODEL dimension already landed via B1 arc #14 (U-RT-114): retry_breaker_fallback.py `_effective_chain(agent_role)` (lines 448-502) augments the fallback chain with the role's `preferred_model_binding` as primary, and a real production caller reaches it — `compose_branch_child_context(..., agent_role=role)` is invoked at three fan-out sites in workflow_driver.py (2545 PARALLELIZATION default role; 3251 ORCHESTRATOR_WORKERS role=`AgentRole(str(step.step_id))`; 3860 HIERARCHICAL/DECENTRALIZED). So per-role model routing is built-and-wired for non-linear topologies. What genuinely REMAINS for B4 (all verified absent/hardcoded-default in production): (1) Per-role PROMPT threading — stage_0_preamble.py:66+80 call `reconcile_active_prompt_via_selection` and `enforce_prompt_version_approval` with NO `role=` arg, so both resolve against `_MVP_DEFAULT_AGENT_ROLE`. The resolver (`resolve_active_prompt_version_sha`, prompt_selection_manifest.py:147) DOES accept `role=` and `PromptSelectionManifest.per_role_bindings` is structurally present, but production never passes a branch role. Spec §14.5.3 confirms this is architecturally deferred (stage-0 single-prompt resolution cannot key on a branch role that does not yet exist; needs a per-dispatch role-aware prompt-resolution mechanism). (2) Per-role binding CATALOG — operator-authored `per_role_bindings` content for both routing + prompt manifests (the read mechanism exists; no B4-specific catalog authoring/validation surface). (3) Per-step OVERRIDE prompt/role surface — `StepOverride` (workflow_manifest_entry.py:51-65) carries only model_binding/engine_class/hitl_placement (NO prompt, NO role field); `resolve_step_binding` IS production-wired in the linear driver (workflow_driver.py:1543), so per-step model/engine/hitl override is built, but per-step prompt/role override is unbuilt. (4) General per-role indexer beyond orchestrator-workers — the LINEAR driver's StepExecutionContext at workflow_driver.py:1610 does NOT set agent_role (stays None); per-role model takes effect ONLY on fan-out branch paths, never on the linear/sequential path. Not 'built-but-unwired' (the model slice has real production callers); not 'unbuilt' (model dimension landed); 'partially-built' is the exact fit.

**Producer map.** Per-role MODEL slice (LANDED, real production callers): retry_breaker_fallback._effective_chain is reached on every fan-out branch dispatch because the fan-out drivers in workflow_driver.py (PARALLELIZATION ~2545, ORCHESTRATOR_WORKERS ~3251, HIERARCHICAL/DECENTRALIZED ~3860) compose branch child contexts via compose_branch_child_context(agent_role=role) and dispatch through RetryBreakerFallbackDispatcher → RuntimeLLMDispatcher. Reachability chain verified: execute_workflow → topology strategy fan-out → compose_branch_child_context(agent_role=AgentRole(str(step.step_id))) → dispatcher reads step_context.agent_role → _effective_chain augments candidate. This is a REAL production caller (the harness_run path), not a dormant driver. Per-role PROMPT slice (NOT wired): the only production caller of reconcile_active_prompt_via_selection / enforce_prompt_version_approval is stage_0_preamble (run_bootstrap), which passes NO role → always _MVP_DEFAULT_AGENT_ROLE; the role-aware resolver branch is reached only by tests. Linear-path per-role: the sequential driver loop (workflow_driver.py:1610) never sets agent_role, so non-fan-out steps discard role entirely. Per-step override: resolve_step_binding has a real production caller (workflow_driver.py:1543 linear + 3254 fan-out), but the override surface carries no prompt/role dimension.

**Fork vs impl detail.** Mostly impl-discretion against the cleared spec, with ONE likely fork. (a) IMPL-DISCRETION — Per-role PROMPT threading: spec §14.5.3 explicitly names B4 as owning per-role prompt specialization and even suggests the lookup-miss policy (fall-through to default); the per-dispatch role-aware prompt-resolution mechanism is 'deferred to implementation discretion' (§14.5.3 closing line + line 3011). The catalog content + per_role_bindings shape are already spec'd contracts (C-CP-01 §1.3, C-CP-29 §29). (b) LIKELY FORK-OWED — Per-step PROMPT/ROLE override: StepOverride (C-CP-06 §6.1) carries only model_binding/engine_class/hitl_placement. Adding a per-step prompt-binding or per-step role field to StepOverride is a NEW field on a cleared spec schema → X-AL-3 design-substrate extension (CP spec §6.1 amendment) before impl. Verify at arc-open whether §6.1's '// additional per-workload fields' extension clause already authorizes it (it authorized prior carrier growth per the v1.4 §6.1 note) — if so this collapses to impl-discretion. (c) IMPL-DISCRETION — general per-role indexer on the linear path: threading agent_role through the linear-driver StepExecutionContext is the same mechanism already cleared at §14.5.3 / §25.14 applied to a path that currently leaves it None; but note the linear/single-threaded path has no per-step role source by design (WorkflowStep carries no role), so a per-step role would need a source (manifest field) = potential fork. (d) IMPL-DISCRETION — per-step gate-level: resolve_parent_gate_level already surfaces manifest_entry.default_gate_level (CP-19 Reading A, v1.20, cleared); a per-STEP (vs per-workflow) gate-level would extend StepOverride = same fork consideration as (b).

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** §14.5.3 (C-RT-15, new at v1.48 B1-spec-2) — The B1↔B4 role seam (runtime half): branch AgentRole dispatch-read indexes per-role MODEL binding, replacing _MVP_DEFAULT_AGENT_ROLE. Explicitly states 'Model binding only — the per-role prompt is NOT reached by this dispatch-read' and 'deferred to R-FS-1 child-arc B4'. Scope boundary: B1 pins the model-routing mechanism; B4 owns the catalog + per-step override + per-role prompt specialization. Lookup-miss policy = fall-through to default (suggested).
- **Spec_Control_Plane_v1_32.md** §25.14 (NEW) B1↔B4 role seam (D2) — CP-half of the role seam. D2 decision folds role-threading mechanism into B1; the per-role binding CATALOG + per-step override surface remains the distinct R-FS-1 child-arc B4. B1↔B4 sequencing is the operator-open lever (default D2).
- **Spec_Control_Plane_v1_32.md** §6.1 / §6.2 (C-CP-06) — WorkflowManifestEntry + StepOverride per-step override schema + resolve_step_binding deterministic field-by-field application. The carrier B4 extends for per-step prompt/role override (today: model_binding/engine_class/hitl_placement only).
- **Spec_Control_Plane_v1_32.md** §29 / C-CP-29 (PromptSelectionManifest); §1.3 / C-CP-01 (RoutingManifest.per_role_bindings) — The two structurally-present per_role_bindings catalogs (prompt-selection + routing) that B4's operator-authored catalog content + role-aware runtime threading must drive.

**Key carrier files:**
- `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py` — Inner LLM-dispatch composer; carries branch role for InferenceRequest.agent_role attribution; comment at 485-486 states per-role PROMPT 'resolved once at stage 0 with default role (deferred to B4)' · `RuntimeLLMDispatcher.dispatch; _MVP_DEFAULT_AGENT_ROLE; _role = step_context.agent_role or _MVP_DEFAULT_AGENT_ROLE (line ~487)`
- `harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py` — BUILT per-role MODEL dispatch-read (U-RT-114): augments fallback chain with role's preferred_model_binding as primary — this is the landed B1 slice · `_effective_chain(self, agent_role) -> FallbackChain (lines 448-502)`
- `harness-runtime/src/harness_runtime/lifecycle/prompt_selection.py` — Per-role PROMPT resolver consumer — DEFAULTS role to _MVP_DEFAULT_AGENT_ROLE; the B4 prompt-threading target · `reconcile_active_prompt_via_selection(role=_MVP_DEFAULT_AGENT_ROLE); enforce_prompt_version_approval(role=_MVP_DEFAULT_AGENT_ROLE)`
- `harness-runtime/src/harness_runtime/bootstrap/stage_0_preamble.py` — Production caller — invokes the two prompt-selection fns at lines 66 + 80 WITHOUT role= → always default role (the per-role prompt gap site) · `reconcile_active_prompt_via_selection(...); enforce_prompt_version_approval(...) (no role kwarg)`
- `harness-cp/src/harness_cp/workflow_driver_types.py` — StepExecutionContext (agent_role field, default None) + compose_branch_child_context (sets agent_role on fan-out branches only) · `StepExecutionContext.agent_role: AgentRole | None = None (line 250); compose_branch_child_context (line 253)`
- `harness-cp/src/harness_cp/workflow_driver.py` — Linear driver does NOT set agent_role on StepExecutionContext (line 1610, stays None); fan-out sites DO set it (2545/3251/3860); per-step override wired at 1543 · `StepExecutionContext(...) at 1610 (no agent_role); resolve_step_binding at 1543; role=AgentRole(str(step.step_id)) at 3239`
- `harness-cp/src/harness_cp/per_step_override_evaluator.py` — Per-step override evaluator — BUILT + wired in linear driver; StepEffectiveBinding carries model_binding/engine_class/hitl_placement (no prompt/role) · `resolve_step_binding (line 154); StepEffectiveBinding (line 126)`
- `harness-cp/src/harness_cp/workflow_manifest_entry.py` — StepOverride schema — carries model_binding/engine_class/hitl_placement only (NO prompt, NO role field) — the B4 per-step prompt/role extension target · `class StepOverride (line 51)`
- `harness-cp/src/harness_cp/prompt_selection_manifest.py` — PromptSelectionManifest.per_role_bindings catalog (structurally present) + role-aware resolver (accepts role=, ready for B4 threading) · `PromptSelectionManifest.per_role_bindings (line 90); resolve_active_prompt_version_sha(role=...) (line 147)`

**Build slices (ordered):**
- Slice 1 (impl, no fork): Thread the branch AgentRole into per-role PROMPT resolution. Move/augment the prompt-selection reconciliation so a fan-out branch's role keys reconcile_active_prompt_via_selection / enforce_prompt_version_approval — a per-dispatch role-aware prompt-resolution step (since stage-0 single-prompt resolution cannot key on a not-yet-existent branch role per §14.5.3). Honor the fall-through-to-default lookup-miss policy. Add e2e exercising a branch role with a per_role_bindings prompt entry actually changing the injected system prompt.
- Slice 2 (impl, no fork): Author the per-role binding CATALOG surface + validation for operator-supplied per_role_bindings content (both RoutingManifest + PromptSelectionManifest), including the step_id→AgentRole derivation contract (AgentRole(str(step.step_id))) so operators can author catalogs that actually bind the fan-out-derived roles. Verify a populated catalog routes distinct workers to distinct models AND prompts end-to-end.
- Slice 3 (fork-gated): Extend StepOverride (C-CP-06 §6.1) with a per-step prompt-binding (and/or per-step role) field — file the CP spec §6.1 amendment first (verify the §6.1 extension clause does/does not already authorize it). Then wire resolve_step_binding to apply it and the dispatcher to consume it on the linear path.
- Slice 4 (impl-discretion, evaluate at arc-open): General per-role indexer on the LINEAR driver path — set agent_role on the linear-driver StepExecutionContext (workflow_driver.py:1610). Requires a per-step role SOURCE on the linear path (WorkflowStep/manifest carries none today) → couples to Slice 3's fork decision. May be out-of-scope if B4 is fan-out-role only.
- Slice 5 (minor): Per-step gate-level surfacing — if a per-STEP (not per-workflow) gate-level is in B4 scope, extend StepOverride (couples to Slice 3 fork) and thread through resolve_parent_gate_level/StepExecutionContext.parent_gate_level. Confirm at arc-open this is in B4 vs already-satisfied by the v1.20 default_gate_level surfacing.

**Blockers / gates:**
- Sequencing gate: frozen order is B1→B3→E→B2→R→B4→CA (per roadmap_status.md). B4 is sequenced AFTER B2 (multi-server MCP) and R (router/embedding). Do not open B4 until B2 + R land — it is the 6th-ish arc in the program, not next.
- SHARED-RUNTIMECONFIG cluster serialization: B4 converges on RuntimeConfig + the runtime dispatch path with B3/B6/B2; kept SERIAL. Re-verify the cluster's shared surfaces (RuntimeConfig fields, stage_0_preamble) at arc-open since B3/B2 land first and will have moved them.
- Fork decision (Slice 3/4/5): the per-step prompt/role/gate-level override is a likely X-AL-3 fork (StepOverride / CP §6.1 schema extension). Resolve fork-vs-impl at arc-open by reading whether CP spec §6.1's extension clause authorizes the new field; if not, file the spec amendment (Phase 6 CP plan / Phase 5 CP spec revision) before impl.
- Scope-boundary decision at arc-open: confirm whether B4 = (per-role prompt + catalog) only, OR also includes general per-role indexing on the linear path + per-step override. Spec §14.5.3 + §25.14 list 'catalog + per-step override surface + per-role prompt' for B4; the linear-path indexer is ambiguous (linear path has no per-step role source by design).

**Cross-arc deps:**
- SHARED-RUNTIMECONFIG cluster (SERIAL): B4 shares RuntimeConfig + the runtime dispatch path (llm_dispatch, stage_0_preamble, workflow_driver) with B3 (smart-HITL), B6 (per-tool sandbox), B2 (multi-server MCP). B3 + B2 land before B4 per frozen order — re-ground their RuntimeConfig/dispatch deltas at B4 arc-open.
- B1 (topology orchestration, COMPLETE through arc #16): B4 directly builds on B1's landed seam. B1 arc #14 (U-RT-114) already delivered the per-role MODEL slice; B4's residual is exactly the per-role PROMPT + catalog + per-step override that B1 explicitly deferred (spec §14.5.3 + §25.14). compose_branch_child_context + StepExecutionContext.agent_role are B1-landed carriers B4 consumes.
- R (router/embedding, vendor-gated): per-role MODEL routing composes with the routing layer; R lands before B4. The per_role_bindings routing catalog (Slice 2) may interact with R's router-model selection — verify no contract widening at B4 arc-open.
- R-PM-1 (prompts-management, LANDED #509): the PromptSelectionManifest.per_role_bindings catalog + resolver B4 threads were built by R-PM-1; B4 is the per-role consumer that R-PM-1's runtime consumer-site (prompt_selection.py) was explicitly built to NOT hollow-index (its docstring cites [[r-cxa-seam-wiring-is-producer-discovery]] — don't build the hollow per-role indexer until real per-role dispatch lands = B4).

**Estimated shape.** Thin-to-medium impl arc, NOT a full research→design→spec→plan→impl. The hard design questions (the B1↔B4 seam, model-vs-prompt split, fall-through policy) are already resolved in the cleared runtime spec §14.5.3 + CP §25.14. Shape: (1) confirm scope boundary + fork decision at arc-open (1 short fork-or-impl call); (2) optional thin CP spec §6.1 amendment IF per-step prompt/role override is in scope and not authorized by the extension clause; (3) impl: per-role prompt threading (Slice 1) + catalog surface (Slice 2) as the core, + conditionally per-step override (Slice 3) + linear-path indexer (Slice 4). Estimate ~2-4 build slices depending on the scope-boundary decision. The model dimension is already done, which materially shrinks this from its stale-ledger framing.

**Open questions:**
- Scope boundary: Is B4 = (per-role prompt + catalog) only, or does it ALSO include (a) general per-role indexing on the linear/single-threaded path and (b) per-step prompt/role override? Spec §14.5.3 + §25.14 list 'catalog + per-step override surface + per-role prompt' for B4 but the linear-path indexer is not explicitly enumerated.
- Fork-vs-impl for per-step override extension: Does CP spec §6.1's '// additional per-workload fields' extension clause authorize adding a per-step prompt-binding / role / gate-level field to StepOverride (collapsing to impl-discretion), or is a §6.1 spec amendment (X-AL-3 fork) required?
- Per-role prompt mechanism placement: §14.5.3 says stage-0 single-prompt resolution cannot key on a not-yet-existent branch role — so per-role prompt resolution must move to a per-dispatch site. Confirm at arc-open whether this is a per-branch reconcile at compose_branch_child_context time, or a per-dispatch resolution inside llm_dispatch (interacts with the §5.2 hash-coherence property that prompt_selection.py's docstring guards).
- Linear-path role source: the linear driver has no per-step role carrier (WorkflowStep carries none); if linear-path per-role indexing is in scope, where does the role come from — a new manifest/StepOverride field (fork) or out of scope?
- Lookup-miss policy: spec §14.5.3 SUGGESTS fall-through to default (mirroring §29's prompt fall-through) but defers to implementation discretion — confirm this is the chosen policy at arc-open.

---

## Arc CA — Cost aggregate (RunResult.cost_attribution rollup)

**Classification:** `partially-built` · **Fork vs impl:** `mixed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Body-verified at HEAD 3835408e. Per-DISPATCH half fully built AND production-wired: attribute_llm_dispatch_cost (cost_attribution_llm_dispatch.py:95) resolves a LIVE rate table (resolve_for from harness_od.rate_table_resolver, NOT the deferred PRICE_TABLE_REF), computes per-attempt cost, builds a 12-field SpanCostRecord, appends a signed AuditLedgerEntry. Real production caller: llm_dispatch.py:1476 (_attribute_cost_best_effort, in materialize_llm_dispatcher_stage). Siblings attribute_tool_dispatch_cost (runtime_tool_dispatcher.py:409), validator-dispatch, webhook-dispatch likewise wired. AGGREGATE primitive built+proven: rollup_costs_by_axis (cross_family_rollup.py:171) returns list of CrossFamilyCostRollup, OD unit coverage of all 3 axes (test_cross_family_rollup.py:77/100/113/140). Field exists: RunResult.cost_attribution tuple of CrossFamilyCostRollup (api.py:332). REMAINS: (1) run-RESULT rollup never invoked, _build_run_result (api.py:889) hard-codes cost_attribution=() at api.py:962; (2) NO source for run-end aggregation, chain stateless, audit-ledger read-back LOSSY (CostRecordAuditPayload cost_namespace.py:96-133 omits provider_discriminator/retry_attempt_number, defers cumulative_cost_usd) so re-reading cost-prefixed entries cannot feed rollup_costs_by_axis. A new in-memory accumulator or payload widening is owed, hence partially-built not M-shape. NOT vacuous: rollup simply not invoked.

**Producer map.** Per-dispatch producer is LIVE: materialize_llm_dispatcher_stage to RuntimeLLMDispatcher.dispatch to _attribute_cost_best_effort (llm_dispatch.py:1476) to attribute_llm_dispatch_cost to audit_writer.append. Non-dormant, the runtime LLM dispatch path. Tool/validator/webhook dispatch have parallel live producers (runtime_tool_dispatcher.py:409, validator_framework_factory.py:34, webhook_delivery_composer.py:323). So per-dispatch SpanCostRecords ARE produced plus persisted on every real run. The AGGREGATE producer is ABSENT: nothing calls rollup_costs_by_axis to populate RunResult.cost_attribution; _build_run_result, the sole RunResult-projection site for api.run, emits () unconditionally; test_track_b_e2e.py:123 also constructs cost_attribution=(). rollup_costs_by_axis's only callers are its OD unit tests. The aggregate is built-but-unwired in isolation, but because its INPUT, a collected list of run-scoped SpanCostRecords, has no producer/accumulator either, the arc as a whole is partially-built: proven primitive plus a new run-scoped record-collection slot plus wiring at _build_run_result are all owed.

**Fork vs impl detail.** WIRING plus accumulator is impl-discretion: collecting per-run SpanCostRecords, calling the proven rollup_costs_by_axis, setting the field, no new contract. AGGREGATE SHAPE is fork-owed but small and bounded: C-RT-09 names cost_attribution as the aggregated rollup with type tuple of CrossFamilyCostRollup, but rollup_costs_by_axis takes ONE axis and C-OD-15 15.1 frames the 3 axes as dashboard rollups; no spec sentence states which axis or all-three the flat tuple carries, nor whether per-run aggregation is a NEW contract distinct from the 15.1 dashboard rollup. That gap plus the type-name reconciliation is an X-AL-3 spec extension to author at arc-open. Under FULL-SPEC directive ([[feedback-full-spec-beyond-mvp-nothing-deferred]]) this is a BUILD arc with design back-flow PRE-AUTHORIZED, not a defer. NOT vendor-gate, rate table resolves live.

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** C-RT-09 line 2455 — Names cost_attribution as the aggregated 5-step rollup; one line; no axis, no record-source, no accumulation contract; CostAttribution vs CrossFamilyCostRollup type-name drift Class-3
- **Spec_Operational_Discipline_v1_2.md** C-OD-15 15.1 — Defines 3 rollup axes but frames them as dashboard rollups; does not say which axis RunResult.cost_attribution carries; the contract gap behind the fork
- **Spec_Operational_Discipline_v1_2.md** C-OD-14 14.3 — Per-sibling fan-out rollup at fanout-close; rollup_fanout_at_close distinct from the 15.1 by-axis rollup the field type matches
- **Spec_Operational_Discipline_v1_2.md** C-OD-15 deferred-to-discretion — Defers dashboard-query and price-table impl; dashboard aggregation is impl-discretion but no run-result aggregate shape authorized

**Key carrier files:**
- `harness-runtime/src/harness_runtime/api.py` — Carrier of the gap: RunResult schema line 291, cost_attribution field line 332, _build_run_result line 889 hard-coding cost_attribution=() at line 962; api.py 911-914 docstring STALE (cites U-OD-21 HALTED Class 1, that tension is CLOSED). · `RunResult, _build_run_result, cost_attribution=()`
- `harness-od/src/harness_od/cross_family_rollup.py` — The aggregate primitive, proven plus unit-tested; produces exactly the type RunResult.cost_attribution carries. · `rollup_costs_by_axis, CrossFamilyCostRollup, RollupAxis`
- `harness-runtime/src/harness_runtime/lifecycle/cost_attribution_llm_dispatch.py` — Per-dispatch cost recorder, production-wired; one audit-ledger entry per LLM dispatch via live rate table. · `attribute_llm_dispatch_cost`
- `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py` — The PRODUCTION caller, line 1476, _attribute_cost_best_effort, via materialize_llm_dispatcher_stage. Proves per-dispatch path non-dormant. · `_attribute_cost_best_effort to attribute_llm_dispatch_cost`
- `harness-od/src/harness_od/cost_namespace.py` — The LOSSY audit-ledger payload, lines 96-133, proving read-back cannot feed the by-axis rollup; why a new accumulator is owed. · `CostRecordAuditPayload`
- `harness-runtime/src/harness_runtime/lifecycle/cost_attribution.py` — Reference-time chain, stateless, no accumulator; carries compute_per_attempt_cost plus rollup_fanout 14.3. · `RuntimeCostAttributionChain, materialize_cost_attribution_stage`
- `.harness/fork_u_rt_49_cost_attribution_invocation_underspec.md` — Original CLOSED 2026-05-20 fork; Q3c wired per-step-body firing; carried Class-3 items 1 and 2 are CA carry-forwards. · `Q1e/Q2-bounded/Q3c`

**Build slices (ordered):**
- Slice 0 arc-open fork: author the run-result aggregate-shape contract. Decide which RollupAxis or all-3 the field carries; reconcile CostAttribution vs CrossFamilyCostRollup type-name; state whether per-run aggregation is a new contract vs the 15.1 dashboard rollup. Amend Spec_Harness_Runtime_v1.md C-RT-09 plus clearance marker. Pre-authorized X-AL-3 back-flow.
- Slice 1: add a run-scoped SpanCostRecord accumulator. Either a new in-memory collection slot the per-dispatch helpers append the returned attached record into, OR widen CostRecordAuditPayload in cost_namespace.py plus audit-ledger read-back. In-memory slot is simpler, recommend it. Impl-discretion.
- Slice 2: wire the per-dispatch helpers (attribute_llm_dispatch_cost plus tool/validator/webhook siblings) to append their produced SpanCostRecord into the run-scoped accumulator; they already RETURN attached. Impl-discretion.
- Slice 3: populate _build_run_result at api.py:962, call rollup_costs_by_axis with collected records and the decided axis, pass the tuple instead of (); refresh the stale api.py 911-914 docstring. Impl-discretion.
- Slice 4: test, extend an e2e (test_track_b_e2e.py or test_run_smoke.py) so a real or mocked-rate run yields NON-empty cost_attribution with correct per-axis sums; un-strike the U-RT-49 cost-attribution AC at the run-result level.
- Slice 5 scoping, possibly separate arc: cross-topology fan-out cost boundary; 14.3 rollup_fanout_at_close vs 15.1 by-axis; B1 proceed-cascade fan-out produces real per-branch records so per-branch aggregation is meaningful; decide whether to reconcile parent/child double-counting. Flag for arc-open.

**Blockers / gates:**
- FORK Slice 0, pre-authorized back-flow: run-result aggregate SHAPE under-specified; C-RT-09 one line plus drifted type-name; C-OD-15 15.1 frames axes as dashboard rollups not a run-result contract. Author axis-selection plus type-name reconciliation before Slice 3.
- DECISION Slice 1: accumulator vs lossy-payload-widening; audit-ledger read-back blocked by CostRecordAuditPayload's lossy projection cost_namespace.py 96-133; in-memory accumulator recommended, lower blast radius.
- SCOPING GATE Slice 5: is fan-out parent/child double-counting in-scope or a follow-on? The 14.3 fan-out rollup must not bleed into the 15.1 by-axis aggregate.

**Cross-arc deps:**
- GENUINELY INDEPENDENT of the SHARED-RUNTIMECONFIG cluster B3/B4/B6/B2 at the per-dispatch level, already wired; the run-result rollup touches only api.py _build_run_result plus a new accumulator slot; can run parallel with the serial cluster.
- DEPENDS-ON B1 COMPLETE arc 16: non-linear topologies are the motivating case; B1 proceed-cascade fan-out (CP 25.15, RunResult status partial per api.py 880-886) means multiple branches each fire the per-dispatch wiring so multiple real records exist per run; the rollup is meaningful not vacuous; the fan-out-boundary slice is reachable now.
- SHARES the audit-ledger cost-prefix action_id seam (CXA v2.9 2.3.7 row 8, harness-cxa/src/harness_cxa/cp_audit_conversion.py) with the OD audit-write path; payload-widening touches this CXA seam; in-memory accumulator does not.
- CONSUMES OD primitives rollup_costs_by_axis, CrossFamilyCostRollup, RollupAxis plus the live rate_table_resolver; all landed; no inbound OD build dep.

**Estimated shape.** Thin-to-medium arc: a small pre-authorized spec fork (Slice 0, ~20-40 lines C-RT-09 amendment plus clearance marker) plus bounded impl (Slices 1-4, new accumulator plus per-dispatch append plus _build_run_result wiring plus 1 e2e, ~80-150 LOC). NOT a full research-design-spec-plan-impl arc; design questions narrow. Slice 5 may split into its own follow-on arc. Effort: normal/high solo pass plus advisor at the fork; council not warranted.

**Open questions:**
- Which RollupAxis populates RunResult.cost_attribution: PER_PROVIDER_DISCRIMINATOR (most operator-meaningful), PER_PROVIDER_AND_MODEL, PER_FALLBACK_EVENT, or all-3 concatenated? No spec sentence decides today; the arc-open fork; lead PER_PROVIDER_DISCRIMINATOR or all-3 concatenated.
- Where do run-scoped SpanCostRecords come from at run-end: a new in-memory accumulator (recommended) or lossy-payload-widening plus audit-ledger read-back (touches the CXA cost-prefix seam)? Payload is LOSSY at cost_namespace.py 96-133.
- Is fan-out parent/child double-counting (14.3 vs 15.1) in-scope for this arc or a follow-on? B1 proceed-cascade makes it concrete; carried Class-3 item 1 from the closed U-RT-49 fork.
- Should the stale api.py 911-914 docstring and test_track_b_e2e.py:123 cost_attribution=() be refreshed in THIS arc's PR as ledger-hygiene, given the U-OD-21-HALTED carry is provably false at HEAD?

---

## Arc B5 — Memory per-deployment-surface backend selection (U-RT-80 factory)

**Classification:** `built-but-vacuous` · **Fork vs impl:** `impl-discretion` · **Confidence:** high

**Rationale (body-verified at HEAD).** resolve_backend(deployment_surface) is BUILT, WIRED to a real production caller, RUNS — but its decision is DEGENERATE: it ignores its discriminating argument. At HEAD (3835408e) harness-runtime/src/harness_runtime/lifecycle/memory_tool_registry.py:58-70 the body is literally `return self._backend` — `deployment_surface` is accepted (for API-shape stability) and dropped. PRODUCER-REACHABILITY is REAL, not dormant: the production caller is _dispatch_anthropic_with_memory at llm_dispatch.py:1327 (`backend = registry.resolve_backend(deployment_surface)`), reached on the Anthropic Memory-tool dispatch path when a step's payload carries the Memory tool def + ctx has memory_tool_registry+deployment_surface bound (llm_dispatch.py:671-685). The registry itself is bound at bootstrap stage 5 by materialize_memory_tool_registry_stage (stage_5_loop_init.py:168 → factory binds ctx.memory_tool_registry, factory line 319). So this is NOT unwired (a live production caller exists) and NOT unbuilt (function exists+runs). It is vacuous because the registry stores ONE bootstrap-frozen backend (factory resolves a single MemoryToolStorageBackend enum at lines 240-253, constructs ONE impl at 256-277, binds MemoryToolRegistry(backend=one, configured_backend=one) at 315-318) — so resolve_backend can only ever return that one backend regardless of the surface passed. The self.deployment_surface threaded at dispatch (llm_dispatch.py:355,685) is a single RuntimeConfig.deployment_surface value, identical every dispatch, making the dropped argument doubly inert. Tests confirm: test_u_rt_80_*.py exercises resolve_backend(cfg.deployment_surface) but backend choice is driven entirely by config.memory_tool_backend_config or the bootstrap default — no test asserts "one registry, two surfaces → two backends" because the registry cannot do that.

**Producer map.** REAL production caller, NOT a dormant driver. Chain verified at HEAD: bootstrap stage 5 (stage_5_loop_init.py:168) awaits materialize_memory_tool_registry_stage(config, ctx) → factory binds ctx.memory_tool_registry (factory:319). At dispatch, RuntimeLLMDispatcher carries memory_tool_registry+deployment_surface fields (llm_dispatch.py:354-355), set at production stage-5 binding; when a step payload contains the Anthropic Memory tool def, the anthropic branch routes through _dispatch_anthropic_with_memory (gated at llm_dispatch.py:671-685 on registry is not None AND deployment_surface is not None) which calls registry.resolve_backend(deployment_surface) at line 1327 → execute_with_memory_callbacks inner loop. So the carrier is reached by a genuine production path (NOT a proof-script/test-only). The vacuity is internal: the single bootstrap-frozen backend + the dropped argument. R-830 (RESOLVED) already made FILESYSTEM + SQLite-DATABASE + S3 + managed-DB backends live in the factory's construct-step — so the backend implementations B5 would route to ALL EXIST; B5 only adds the surface→backend dispatch the registry currently lacks.

**Fork vs impl detail.** Surface-aware backend routing is EXPLICITLY blessed as Phase-7 implementation discretion by the CLEARED spec+plan — NO X-AL-3 fork owed. Plan v2_14 U-RT-80 AC (line 91): 'future multi-surface support is implementation-arc discretion per §14.12.7'. Spec §14.12.1 line 4143 already describes resolve_backend as returning the backend 'for the deployment surface' (surface-parametric reading is the canonical contract; the MVP collapse is the deviation, not the target). The build is impl-to-cleared-spec, NOT a contract extension — no new field/enum/semantics. ONE hard constraint from the cleared spec must be honored (mechanism, not fork): §14.12.5 invariant 1 forbids re-resolution at dispatch-time. So B5 must resolve the surface→backend MAP at BOOTSTRAP (factory eagerly constructs an impl for each admissible backend across surfaces, or at least the candidate surfaces) and resolve_backend becomes a pure map lookup — it must NOT lazily re-resolve/re-construct per dispatch. Caveat to verify at arc-open: constructing a backend per admissible surface may require backend_params for S3/DATABASE that the operator only supplied for the active surface — so 'eager all-surface construction' may need a lazy-cache-at-first-lookup-but-resolved-once-per-surface variant (still invariant-1-compatible since each surface resolves at most once). That mechanism choice is impl-discretion. ENCRYPTED_FILESYSTEM/OPERATOR_DEFINED backends remain genuinely unimplemented (factory raises) — if B5 wants those surfaces fully routable it would need those impls too (separate, R-830-adjacent, not a fork either — operator-discretion follow-on per §16 §6.C v2 C.vii).

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** §14.12.1 — MemoryToolRegistry.resolve_backend(deployment_surface)→MemoryToolStorageBackendProtocol signature + prose (line 4143) stating it 'returns the storage-backend implementation FOR THE DEPLOYMENT SURFACE', honoring memory_tool_backend_config override OR falling back to harness_as.anthropic_graceful_degradation.memory_tool_storage_backend(deployment_surface) per C-AS-13 §13.6 step 8 — the surface-parametric reading that the as-built MVP collapses
- **Spec_Harness_Runtime_v1.md** §14.12.5 invariant 1 — Storage-backend resolved EXACTLY ONCE per bootstrap; NO re-resolution at dispatch-time. CONSTRAINS the B5 build mechanism: must resolve all admissible-surface backends at bootstrap into a surface→backend map; must NOT re-resolve per dispatch.
- **Spec_Harness_Runtime_v1.md** §14.12.7 — Deferred-to-implementation-discretion list. Note: the registry docstring cites §14.12.7 for 'future multi-surface support'; the actual §14.12.7 bullets cover module org/root-path/concurrency/inner-loop/operator-defined-introspection/backend-telemetry, NOT explicitly 'multi-surface registry' — but plan v2_14 U-RT-80 AC (line 91) makes the impl-discretion blessing explicit.
- **Spec_Action_Surface_v1.md** C-AS-13 §13.6 step 8 — memory_tool_storage_backend(deployment_surface)→frozenset[MemoryToolStorageBackend] resolver — the per-surface admissible-backend mapping (LOCAL_DEVELOPMENT→{FILESYSTEM,ENCRYPTED_FILESYSTEM}; MANAGED_CLOUD→{S3,DATABASE}; SELF_HOSTED_SERVER→{FILESYSTEM,ENCRYPTED_FILESYSTEM,DATABASE}) at harness-as/src/harness_as/anthropic_graceful_degradation.py:222-256. This is the surface-routing authority the factory already calls once and must call per-admissible-surface for B5.
- **Implementation_Plan_Harness_Runtime_v2_14.md** U-RT-80 AC (line 91) + §14.D — EXPLICIT impl-discretion blessing: 'deployment_surface param accepted for API-shape stability but IGNORED at MVP since resolution is bootstrap-time-frozen per invariant 1; future multi-surface support is IMPLEMENTATION-ARC DISCRETION per §14.12.7'. §14.D = local-filesystem-at-retirement-gate scope; S3/ENCRYPTED_FILESYSTEM/DATABASE deferred to follow-on (now LANDED via R-830).

**Key carrier files:**
- `harness-runtime/src/harness_runtime/lifecycle/memory_tool_registry.py` — THE vacuous carrier — MemoryToolRegistry class; resolve_backend body (line 58-70) returns self._backend ignoring deployment_surface; stores single backend + configured_backend enum. B5 PRIMARY edit site. · `MemoryToolRegistry.resolve_backend`
- `harness-runtime/src/harness_runtime/bootstrap/factories/memory_tool_registry_factory.py` — Stage-5 factory — resolves ONE enum (lines 240-253) + constructs ONE backend impl (256-277: FILESYSTEM/DATABASE/S3 all live) + binds MemoryToolRegistry(backend=one) (315-318). B5 SECONDARY edit site: must construct a surface→backend MAP at bootstrap. · `materialize_memory_tool_registry_stage`
- `harness-as/src/harness_as/anthropic_graceful_degradation.py` — Surface-routing AUTHORITY (already landed) — memory_tool_storage_backend(surface)→frozenset; _MEMORY_BACKENDS per-surface map (lines 222-256). The factory already calls this once; B5 calls it per admissible surface. Zero new CXA edge (already-imported carrier per spec §14.12.6). · `memory_tool_storage_backend / _MEMORY_BACKENDS`
- `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py` — Production CALLER — _dispatch_anthropic_with_memory (line 1305) calls registry.resolve_backend(deployment_surface) at 1327; deployment_surface threaded as a single dispatcher field (355, 685) from RuntimeConfig. Confirms vacuity (built+wired+runs, arg dropped). · `_dispatch_anthropic_with_memory`
- `harness-runtime/src/harness_runtime/lifecycle/memory_tool_types.py` — Type carriers — MemoryToolStorageBackendProtocol (5 CRUD), MemoryToolBackendConfig, MemoryBackendResolutionError. Unchanged by B5 (Protocol surface stable). · `MemoryToolStorageBackendProtocol`
- `harness-runtime/tests/test_u_rt_80_memory_tool_registry_factory.py` — Existing factory tests — exercise resolve_backend(cfg.deployment_surface) but selection driven by override/default, never 'one registry, two surfaces, two backends'. B5 adds the contrasting-surface test. · `test_default_resolver_picks_filesystem_at_local_development`

**Build slices (ordered):**
- Slice 1 — Registry shape change: make MemoryToolRegistry hold a surface→backend mapping (dict[DeploymentSurface, MemoryToolStorageBackendProtocol]) instead of a single backend; resolve_backend(surface) becomes a map lookup that raises RT-FAIL-MEMORY-BACKEND-RESOLUTION (or returns a documented fallback) for an unmapped surface. Preserve the configured_backend property (now per-surface or default-surface). Honor §14.12.5 invariant 1 — no per-dispatch re-resolution.
- Slice 2 — Factory build change: in materialize_memory_tool_registry_stage, when memory_tool_backend_config is None, iterate the candidate deployment surfaces, call memory_tool_storage_backend(surface) per surface, pick the implemented member (FILESYSTEM/DATABASE/S3/managed-DB — all live post-R-830), construct each impl ONCE, and assemble the surface→backend map. When memory_tool_backend_config IS set (operator override), keep current behavior (override forces the backend regardless of surface) — map every surface to the override impl, preserving operator-override precedence. Keep the unimplemented-backend raise + §14.D fork-doc pointer for ENCRYPTED_FILESYSTEM/OPERATOR_DEFINED.
- Slice 3 — Decide+implement the eager-vs-lazy construction mechanism for backends needing backend_params absent at the non-active surface (eager-all-surfaces vs resolve-once-per-surface-on-first-lookup); both are invariant-1-compatible. Default to: construct the active config.deployment_surface backend eagerly + lazily resolve-once any other surface only if its params are derivable, else map it to a raise. This is the one genuine design micro-decision for arc-open.
- Slice 4 — Tests: add the contrasting-surface test the current suite lacks — one registry, resolve_backend(LOCAL_DEVELOPMENT) vs resolve_backend(MANAGED_CLOUD) return DIFFERENT backend types (e.g. LocalFilesystem vs S3/managed-DB) — the anti-vacuity guard. Add the unmapped-surface raise test. Keep all existing U-RT-80 tests green (operator-override path unchanged).
- Slice 5 — Docstring/cite hygiene: correct the now-stale MVP docstrings in memory_tool_registry.py (lines 14-17, 62-69) and factory header that say 'ignores its argument / single pre-resolved backend / MVP'; re-point §14.12.7 cite if the multi-surface deferral is now consumed. NOT a spec edit (design-substrate untouched) — these are code-comment refreshes to prevent the next ledger from re-reading a stale docstring (the exact rot that mis-classified B3).

**Blockers / gates:**
- No X-AL-3 fork gate — cleared spec/plan already govern (impl-discretion). No operator decision strictly required to start.
- §14.12.5 invariant 1 is a HARD mechanism constraint: resolution must stay bootstrap-time (resolved-once-per-surface), never per-dispatch. Violating it would contradict the cleared spec.
- Micro-design decision (Slice 3): eager-all-surfaces vs lazy-resolve-once construction when backend_params for a non-active surface are absent. Reversible, has a sensible default (active-surface-eager + others-lazy-or-raise) — resolve in-arc, do not gate the operator.
- Full routability of ENCRYPTED_FILESYSTEM + OPERATOR_DEFINED surfaces is blocked on those backend impls (factory currently raises) — out of B5 core scope; operator-discretion follow-on per §16 §6.C v2 C.vii. Not a B5 blocker if B5 scopes to the 4 live backends (FILESYSTEM/SQLite-DATABASE/S3/managed-DB).
- No credential/vendor gate for the CORE B5 build (it is registry-internal routing logic, provider-free testable). Any live multi-surface e2e (e.g. S3 at MANAGED_CLOUD) would be R-830-style credential-gated — but that is verification, not the build, and is already R-830-proven.

**Cross-arc deps:**
- GENUINELY INDEPENDENT — CONFIRMED against HEAD. overlay-query --unit U-RT-80 returns cxa_seams: [] (zero cross-axis seams). All 6 carrier files are inside harness-runtime/. The factory's only cross-package import is harness_as.anthropic_graceful_degradation (already-landed carrier, ZERO new CXA edge per spec §14.12.6 + memory_tool_types.py:20-22 'ZERO new CXA edge').
- Does NOT touch the SHARED-RUNTIMECONFIG CLUSTER (B3 smart-HITL / B4 per-role dispatch / B6 per-tool sandbox / B2 multi-server MCP). B5 only READS RuntimeConfig.deployment_surface + RuntimeConfig.memory_tool_backend_config (read-only); it adds NO RuntimeConfig field and does not mutate the dispatch path the cluster converges on. Stage-5 ordering with the dispatcher is arbitrary (spec §14.12.3) — no data-mutation dependency.
- Depends-on (already satisfied): R-830 (RESOLVED) landed FILESYSTEM + SQLite-DATABASE + S3 + managed-DB backend impls in the factory — B5's surface→backend map routes to these existing impls; B5 builds NO new backend impl for the 4 live backends.
- Consumed-by: the production Memory-tool dispatch path (_dispatch_anthropic_with_memory) is the sole consumer; B5 makes that consumer's resolve_backend call non-vacuous. No other arc consumes the registry.
- Sequencing: B5 can run in PARALLEL with the serial RuntimeConfig cluster (no overlap) — it does NOT need to wait for B3/B4/B6/B2. Pairs naturally with B7 (composite_sampler) as the two genuinely-independent arcs.

**Estimated shape.** Thin single-bounded impl arc (no research/design/spec phase). Shape: small registry-internal refactor (single backend → surface→backend map) + factory build-step change to populate the map via the already-existing per-surface resolver + an anti-vacuity contrasting-surface test + code-comment/cite hygiene. No design-substrate edit, no CXA wiring, no new contract. ~1 focused PR. The only judgment call is the eager-vs-lazy construction mechanism (Slice 3), resolvable in-arc with advisor() if needed. Verification is provider-free (registry routing logic); live multi-surface e2e is optional/credential-gated and already R-830-proven for the backends themselves.

**Open questions:**
- Slice-3 mechanism: eager-construct-all-admissible-surface-backends at bootstrap vs resolve-once-per-surface lazily on first resolve_backend(surface) lookup? Eager is cleaner for invariant-1 but may fail when backend_params (S3 bucket / DB connection_string) are only supplied for the active surface; lazy-resolve-once is also invariant-1-compatible (each surface resolves at most once). Default recommendation: active-surface eager + other surfaces resolve-once-on-lookup-if-params-derivable-else-raise. Confirm at arc-open.
- Scope of 'surfaces' to map: only the 3 DeploymentSurface enum members, or only the ones with implemented backends in their frozenset? ENCRYPTED_FILESYSTEM + OPERATOR_DEFINED are unimplemented (factory raises) — does B5 map their containing surfaces to a raise, or does B5 also pull in those backend impls (R-830-adjacent, operator-discretion)? Recommend B5 scopes to the 4 live backends and maps unimplemented-only surfaces to the existing RT-FAIL-MEMORY-BACKEND-RESOLUTION raise.
- configured_backend property semantics after the change: it currently returns the single enum for span-attribute emission (memory.backend at C-RT-15 §14.5.1). With a surface→backend map, does it return the active-surface backend's enum, or become surface-parameterized? The span emission is per-dispatch with a single deployment_surface, so active-surface enum is the natural answer — verify the C-RT-15 consumer (execute_with_memory_callbacks backend_enum arg) at arc-open.
- Does any operator-override interaction need preserving: when memory_tool_backend_config IS set, the override 'forces the named backend regardless of deployment surface' (spec §14.12.1) — so the map should collapse to the override for all surfaces. Confirm this precedence is retained (override > surface-routing) so existing override tests stay green.

---

## Arc B6 — Per-tool sandbox-tier resolution + STDIO transport-floor

**Classification:** `partially-built` · **Fork vs impl:** `fork-owed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Verified at HEAD 3835408e by reading bodies, not docstrings. THREE distinct slices with different states: (1) PER-SERVER-UNIFORM tier resolution + driver selection is BUILT-AND-WIRED — `runtime_tool_dispatcher_factory.py:268-285` (materialize_runtime_tool_dispatcher_stage) calls `resolve_effective_sandbox_defaults(config.mcp_clients[0], ...)` then `_build_default_policy_sandbox_resolver(effective)` + `_select_tool_execution_driver(tier=..., driver_config=...)`, passing both to the bare RuntimeToolDispatcher; this is the cleared §14.9.8 (Reading B) + §14.9.9 (Reading A+) MVP and it is reachable via the api.run production path. (2) The FULL per-cell `sandbox_tier_floor(tool, deployment_surface, blast_radius_tier, mcp_transport, mcp_server)` — the 10-row table that DOES discriminate per tool (rows 1-2 forcing, row 3 STDIO→max(tier-3,floor), rows 4-6 remote trust, rows 7-10 blast-radius) — is BUILT in AS at harness-as/src/harness_as/sandbox_tier_floor.py:115-155 but is NOT wired into the dispatch tier path: the dispatch resolver `_build_default_policy_sandbox_resolver` returns a CONSTANT SandboxDispatchDecision and its `resolve(_contract, _step)` ignores both args (underscore-prefixed). (3) The STDIO Tier-3 transport-floor (ADR-D2 §1.3) is BUILT in AS as both `mcp_transport_floor` (mcp_transport_floor.py:43, STDIO→max(tier-3,floor)) and `sandbox_tier_floor` row 3 — and `mcp_transport_floor` IS wired at mcp_host.py:106, BUT mcp_host.py:111 only consumes the REFUSE outcome (`if result.outcome is SandboxTierFloorOutcome.REFUSE`) and DISCARDS the RESOLVED `.tier` value; the STDIO max(tier-3,floor) tier never reaches dispatch tier resolution. Not built-but-vacuous: the per-server constant is the CLEARED MVP scope (§14.9.8 "Per-server-uniform (scope honesty)" + §14.9.9 inv 3), an intentional honest floor, NOT a degenerate collapse of a contract that promises discrimination. The genuine remaining capability = wire the discriminating floor + add per-dispatch driver granularity. Hence partially-built.

**Producer map.** The per-server-uniform path has a REAL production producer: api.run → bootstrap → materialize_runtime_tool_dispatcher_stage (runtime_tool_dispatcher_factory.py:203) → builds constant resolver + selects driver → binds to ctx.tool_dispatcher (the C-RT-21 retry wrapper). This is reachable, not dormant. The full discriminating `sandbox_tier_floor` (harness-as) IS reached by AS-INTERNAL production consumers — `sub_agent_sandbox_tier.py:78` (max(parent_tier, sandbox_tier_floor(...))) and `gate_level_composition.py` (the §12.1 fifth max() axis) — so it is NOT a dormant/unwired symbol in absolute terms; it is specifically NOT reached by the tool-DISPATCH tier resolver. `mcp_transport_floor` is reached at mcp_host.py:106 but only for the REFUSE branch. So: dispatch tier today is produced ENTIRELY by the constant `_build_default_policy_sandbox_resolver`; the discriminating floor and the transport-floor's RESOLVED tier never feed dispatch. The 3 sandbox drivers are live and production-selected per-server (construction-time).

**Fork vs impl detail.** Discriminator: the CLEARED runtime spec affirmatively EXCLUDES per-cell composition on the dispatch path. §14.9.9 scope boundary: widening to full sandbox_tier_floor per-cell composition 'is a distinct future arc.' §14.9.8: per-tool granularity is 'NOT in scope at the MVP' and names 'fork Reading A-of-the-converter-fork shape.' Building it therefore requires a runtime-spec amendment (a new per-tool SandboxDecisionResolver contract + a per-dispatch driver-granularity contract amending §14.9.9 inv 1/inv 3) → fork-owed, pre-authorized by the R-FS-1 full-spec directive (design back-flow PRE-AUTHORIZED). This is a bundled-absorption arc touching design-substrate (CLAUDE.md §11.4), not pure Phase-7 impl. MIXED credit: ADR-D2 §1.3 (STDIO floor) + C-AS-02 §2.3 (10-row table) + ADR-D2 §1.5.1 (per-cell composition) are ALREADY cleared, so the underlying floor SEMANTICS need no fork — only the runtime dispatch CONTRACT (per-tool resolver + per-tool driver selection) and likely a ToolContract carrier extension (forcing discriminators) are fork-owed.

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** §14.9.8 (v1.41, Reading B) — Sandbox-decision-resolver contract: SandboxDispatchDecision (5-field carrier) + SandboxDecisionResolver = Callable[[ToolContract, WorkflowStep], SandboxDispatchDecision]. Explicitly states 'Per-tool sandbox granularity is a future arc (would route to a per-tool ToolContract policy map, fork Reading A-of-the-converter-fork shape); NOT in scope at the MVP.'
- **Spec_Harness_Runtime_v1.md** §14.9.9 (v1.43, Reading A+) — Tool-execution-driver selection contract. FR-1 driver-selection obligation, FR-2 fail-loud (RT-FAIL-SANDBOX-DRIVER-UNAVAILABLE), inv 3 'Per-server-uniform, construction-time'. Scope boundary (line ~3891): 'widening it to the full sandbox_tier_floor(...) per-cell composition (C-AS-02 / ADR-D2 §1.5.1) is a distinct future arc.'
- **ADR-D2.md** §1.3 Per-MCP-transport sandbox-tier floor — STDIO | zero protocol-level auth | Tier 3 container minimum regardless of blast radius. This is the canonical home of the STDIO transport-floor (NOT CXA §0.4 as the brief claims).
- **Spec_Action_Surface_v1.md** C-AS-02 §2.3 — The reconciled 10-row sandbox_tier_floor lookup table (the per-cell composition) — already cleared and built in AS (U-AS-06).
- **Spec_Action_Surface_v1.md** C-AS-10 §10.1 / C-AS-09 §9.1 — Per-MCP-transport floor table (U-AS-13) + 4-tier blast-radius sandbox model + deployment-surface×blast-radius raise-only floor cells.
- **ADR-D2.md** §1.5.1 — Per-cell sandbox-tier composition (named by §14.9.9 scope boundary as the future-arc widening target).

**Key carrier files:**
- `harness-runtime/src/harness_runtime/bootstrap/factories/runtime_tool_dispatcher_factory.py` — PRODUCTION dispatch wiring; the §14.9.8/§14.9.9 stage-5 factory. Contains the constant per-server resolver (the gap) + driver selection (per-server-uniform, construction-time). · `materialize_runtime_tool_dispatcher_stage (:203); _build_default_policy_sandbox_resolver (:82, returns constant, resolve(_contract,_step) ignores args :94); _select_tool_execution_driver (:106)`
- `harness-as/src/harness_as/sandbox_tier_floor.py` — BUILT-NOT-WIRED-INTO-DISPATCH full per-cell discriminating floor. Wiring THIS into the resolver delivers per-tool granularity AND STDIO floor (row 3) in one move. · `sandbox_tier_floor(tool, deployment_surface, blast_radius_tier, mcp_transport, mcp_server) (:115); row 3 STDIO _tier_max(TIER_3_MICROVM, floor) (:141-142); ToolMetadata (:46); MCPServer (:71)`
- `harness-as/src/harness_as/mcp_transport_floor.py` — BUILT per-MCP-transport floor (U-AS-13). STDIO→max(tier-3,floor). Wired at mcp_host but only its REFUSE outcome is consumed. · `mcp_transport_floor (:43); rejects_at_registration (:73)`
- `harness-runtime/src/harness_runtime/lifecycle/mcp_host.py` — PRODUCTION caller of mcp_transport_floor — but discards the RESOLVED tier; only acts on REFUSE. · `materialize_mcp_stage (:87); :106 mcp_transport_floor call; :111 `if result.outcome is SandboxTierFloorOutcome.REFUSE` (RESOLVED tier discarded)`
- `harness-runtime/src/harness_runtime/config/sandbox_defaults.py` — Arc #1 (#503) per-SERVER deployment-surface-aware default resolver. Per-server-uniform; does NOT compose mcp_transport_floor / sandbox_tier_floor. · `resolve_effective_sandbox_defaults (:83); surface_default_sandbox_tier (:70); EffectiveSandboxDefaults (:59)`
- `harness-as/src/harness_as/tool_contract.py` — ToolContract carrier. CRITICAL: carries minimum_tier + blast_radius_tier but NOT the ToolMetadata forcing discriminators — folds a contract extension into the per-tool fork. · `ToolContract.minimum_tier (:77), .blast_radius_tier (:80); NO forces_computer_use/forces_code_execution/is_deterministic_inhouse fields`
- `harness-runtime/src/harness_runtime/lifecycle/docker_tool_execution_driver.py` — LIVE drivers (R-410 Docker, R-411 gVisor subclass). Self-guard FR-2(ii): raise on decision.tier != required_tier. · `DockerToolRunnerExecutionDriver (:33, required_tier TIER_2 :48); GVisorRunscToolRunnerExecutionDriver (:212, required_tier TIER_3 :222); tier mismatch raise (:74)`
- `harness-runtime/src/harness_runtime/lifecycle/e2b_tool_execution_driver.py` — LIVE E2B full-VM driver (R-412), TIER_4. · `E2BManagedFullVMToolRunnerExecutionDriver (:49)`

**Build slices (ordered):**
- SLICE 0 (fork — runtime spec amendment): author the per-tool SandboxDecisionResolver contract amending §14.9.8 (resolver MAY discriminate on (contract, step)) + the per-tool / per-dispatch driver-selection contract amending §14.9.9 inv 1/inv 3 (relocate driver selection from construction-time to per-dispatch, OR pin construction-time driver at the per-server MAX tier). File clearance marker.
- SLICE 1 (contract carrier extension — likely fork-folded): extend ToolContract with the ToolMetadata forcing discriminators (forces_computer_use / forces_code_execution / is_deterministic_inhouse) so sandbox_tier_floor rows 1-2 are reachable per-tool; OR decide rows 1-2/4-6 stay per-server and only rows 3+7-10 go per-tool (this is an arc-open decision). Without this, sandbox_tier_floor cannot be fully called from the dispatch resolver.
- SLICE 2 (impl — wire the discriminating floor): replace the constant `_build_default_policy_sandbox_resolver` with a resolver that calls `sandbox_tier_floor(tool_metadata_from_contract, deployment_surface, contract.blast_radius_tier, server.transport, mcp_server)` per (contract, step). This single move delivers BOTH per-tool blast-radius granularity AND the STDIO Tier-3 transport-floor (row 3) — there is NO separate STDIO-enforcement slice; do NOT patch the discarded tier at mcp_host.py:111.
- SLICE 3 (impl — per-dispatch driver granularity): make `_select_tool_execution_driver` per-dispatch (or pin a per-server max-tier driver at construction) so a resolved per-tool tier that exceeds the construction-time driver tier does not violate §14.9.9 inv 1 (delivered-tier ≥ resolved-tier). This is the coupled second fork the per-tool resolver structurally forces.
- SLICE 4 (impl — REFUSE/floor reconciliation): ensure the resolver honors sandbox_tier_floor's REFUSE outcome (remote L0) consistently with the existing mcp_host.py registration-time REFUSE; avoid two REFUSE sites with divergent semantics.
- SLICE 5 (tests): per-tool tier divergence on one server (read-only tool → TIER_1, destructive/STDIO tool → TIER_3); STDIO server forces tier-3 even at READ_ONLY blast-radius; floor-violation raises RT-FAIL-SANDBOX-TIER-FLOOR-VIOLATION; driver-granularity invariant (delivered ≥ resolved per dispatch). Contrast-baseline test vs the per-server constant.

**Blockers / gates:**
- FORK GATE (Slice 0): per-tool resolver + per-dispatch driver-selection contracts are not in the cleared spec; a runtime-spec amendment (+ ADR-D2 §1.5.1 / C-AS-02 reference) must clear before impl. Pre-authorized by R-FS-1 full-spec directive but still owes a clearance marker + adversarial review (CLAUDE.md §11.4 bundled-absorption arc).
- ARC-OPEN DECISION: does per-tool resolution need ToolContract to carry the ToolMetadata forcing discriminators (rows 1-2) and an MCPServer trust object (rows 4-6)? ToolContract today has only minimum_tier + blast_radius_tier. Decide: (a) extend ToolContract (folds a carrier extension into the fork), or (b) scope per-tool to rows 3 + 7-10 only and keep forcing/trust per-server.
- COUPLED-FORK GATE: §14.9.9 inv 3 ('per-server-uniform, construction-time') + inv 1 (delivered ≥ resolved) make a per-tool tier that exceeds one construction-time driver a flat invariant violation. Per-tool tier resolution CANNOT land without resolving driver granularity — relocate driver selection per-dispatch OR pin construction-time driver at per-server-max tier. This is a structural coupling, not optional.
- SHARED-RUNTIMECONFIG SERIALIZATION: this arc mutates the stage-5 factory dispatch path + RuntimeConfig/MCPClientConfig surface — same files B3 (smart-HITL), B4 (per-role dispatch), B2 (multi-server MCP) touch. Must stay serial per the cluster discipline to avoid merge contention.
- VENDOR/SUBSTRATE (not a blocker for build, only for live-e2e): TIER_3/TIER_4 drivers (gVisor/E2B) need real substrate for end-to-end exercise; per the no-unilateral-paid-calls discipline, drive to the dispatch boundary, do not auto-fire E2B.

**Cross-arc deps:**
- SHARED-RUNTIMECONFIG cluster (SERIAL): converges with B3 (smart-HITL), B4 (per-role dispatch), B2 (multi-server MCP) on RuntimeConfig + the runtime_tool_dispatcher_factory stage-5 dispatch path + MCPClientConfig. B6 specifically owns the SandboxDispatchDecision resolver + driver-selection sub-paths of that factory.
- DEPENDS ON arc #1 / #503 (resolve_effective_sandbox_defaults + tier→driver selection) — B6 widens the resolver this arc established from per-server-constant to per-tool-discriminating; it is the direct successor.
- MULTI-SERVER coupling with B2: today factory reads ONLY config.mcp_clients[0] (single-server MVP). A per-tool resolver across multiple servers needs the per-server context resolved per dispatch — B2's multi-server work and B6's per-tool work compose on the same single-server-[0] limitation (§14.9.8 'Multi-server resolver composition' is deferred). Sequence B2 before or jointly with B6's multi-server aspect.
- AS axis (no fork needed there): sandbox_tier_floor (U-AS-06) + mcp_transport_floor (U-AS-13) + the 4-tier model (C-AS-09) are already cleared and built — B6 consumes them; only the RUNTIME dispatch contract is fork-owed.
- ToolContract carrier extension (if chosen) ripples to U-AS-07 / RawContractInput converter + the v1.40 stage-3a converter that stamps minimum_tier — a carrier change touches the AS↔runtime seam.

**Estimated shape.** Medium fork+impl arc (NOT thin). Shape: fork (runtime-spec amendment for per-tool resolver + per-dispatch/per-server-max driver granularity, likely + ToolContract carrier extension) → clearance marker + adversarial review → impl (wire sandbox_tier_floor into the resolver — one substantive change that delivers per-tool tier AND STDIO floor together; relocate/pin driver selection) → tests (per-tool divergence, STDIO-floor, driver-granularity invariant). The arc is genuinely TWO coupled forks (resolver granularity forces driver granularity), so right-size UP from the brief's framing. The AS-side floor logic is already built, which shrinks the impl; the coupling + carrier-extension decision is what adds weight.

**Open questions:**
- Per-tool granularity scope: full 10-row sandbox_tier_floor per tool (requires ToolContract to carry ToolMetadata forcing flags + an MCPServer trust object), OR a narrower per-tool slice (rows 3 STDIO + 7-10 blast-radius, which ToolContract.blast_radius_tier + MCPClientConfig.transport already support) with forcing/trust staying per-server? This decides whether SLICE 1 (carrier extension) is in-scope.
- Driver granularity resolution: relocate _select_tool_execution_driver to per-dispatch (so each tool gets its tier's driver), OR pin a single construction-time driver at the per-server MAX resolved tier (simpler, over-provisions low-tier tools)? §14.9.9 inv 1/inv 3 must be amended either way.
- STDIO floor on REMOTE-but-also-STDIO edge cases + REFUSE reconciliation: should the dispatch resolver REFUSE (remote L0) be the same site as mcp_host.py registration-time REFUSE, or are they intentionally two gates (registration vs dispatch)? Avoid divergent REFUSE semantics.
- Multi-server interaction with B2: the factory's single-server config.mcp_clients[0] limitation must be lifted for per-tool resolution to be meaningful across servers — sequence/joint-scope with B2 or accept single-server per-tool first?
- Does the per-tool resolver need WorkflowStep context (the _step arg currently ignored), or is (contract → decision) sufficient? §14.9.8 types the resolver as (ToolContract, WorkflowStep) → decision; confirm whether step carries any tier-relevant signal.

---

## Arc B7 — OD composite sampler §9.2 conditional-row over-sampling

**Classification:** `built-but-vacuous` · **Fork vs impl:** `impl-discretion` · **Confidence:** high

**Rationale (body-verified at HEAD).** The sampler is built AND production-wired AND runs — but its decision on the four §9.2 conditional rows is degenerate (over-produces unconditionally), which is the verbatim "built-but-vacuous" definition. Verified at HEAD by reading bodies, not docstrings: harness-od/src/harness_od/composite_sampler.py HarnessCompositeSampler.should_sample (lines ~85-109) calls is_always_sampled(name) with ONLY the span NAME; the `attributes` arg (which carries files.operation.kind, memory.operation.kind, validator.fail.permanence, root-ness) is passed through to the SamplingResult but NEVER consulted for the decision. harness-od/src/harness_od/sampling_mode.py ALWAYS_SAMPLED_EVENT_CLASSES (lines 102-123) lists `files.operation`, `memory.operation`, `validator.fail.*`, `subagent.span` as bare entries, and is_always_sampled (lines 152-161) is pure literal/dot-prefix match on the name. So EVERY files.operation samples (not only kind∈{upload,delete}); EVERY memory.operation samples (not only {write,update,delete}); EVERY validator.fail.* samples (not only permanence=permanent); subagent.span at any depth would force-sample (not only root). The spec §9.2 (Spec_Operational_Discipline_v1_2.md lines 511-532) carries those conditional qualifiers as the contract; the sampler honors a SUPERSET — conservative over-sampling, never under. The carrier-scoped view is "partially-built" (the 12 unconditional literals + the per-cell base-rate are correct; only the attribute-conditional slice for these rows remains) — but built-but-vacuous is the precise fit for the arc's named decision. NOT unbuilt: the capability runs today, it just ignores its discriminating argument.

**Producer map.** REAL production caller confirmed (not a dormant driver). Chain verified at HEAD: harness-runtime/src/harness_runtime/bootstrap/stage_4_od.py:56 calls materialize_tracer_provider_stage(config) — bootstrap OD stage, no sampler override — → tracer_provider.compose_tracer_provider with sampler=None → production branch (lines 223-236): builds CellID(persona_tier, deployment_surface), reject_excluded_cell(cell), base_rate = PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate, build_default_sampler(base_rate) → ParentBased(root=HarnessCompositeSampler(base_rate)) → registered on TracerProvider and set globally. So the over-sampling sampler is on the live trace path for every span at runtime. The per-tier base-rate (1.0/0.5/0.1/0.2 across the §10.3 8-cell envelope) is fully live and reachable through this same production caller — confirming the brief's 'per-tier base-rate IS live' AND correcting the brief's understatement (it is the full §10.3 envelope, not a hardcoded 1.0). Tests pass deterministic samplers; production never threads the sampler arg.

**Fork vs impl detail.** No X-AL-3 fork owed. The §9.2 conditional qualifiers (at kind∈{...}; at permanence=permanent; (root)) are ALREADY in the cleared contract (Spec_Operational_Discipline_v1_2.md §9.2 table, preserved verbatim into v1.3 per v1.3 §0.1). The MVP over-sample was a documented 'future arc' shortcut (composite_sampler.py docstring lines 21-33), not a blessed-forever contract. Spec v1.27 §1.1(c) closed the DETECTION MECHANISM question (runtime hook, not compile-time annotation) but did not re-scope the §9.2 conditional rows to name-only. Refining the sampler to honor the qualifiers samples LESS (a strict subset of the current over-sample) and stays within the §9.3-inviolable floor — pure Phase-7 impl of an existing spec qualifier, no new contract/field/enum/semantics. One build-shape sub-decision is impl-discretion not fork: whether OD consumes the AS U-AS-32 mutation-posture map via an OD→AS seam or reimplements the kind-discrimination in OD (see cross_arc_deps). No design-substrate edit required.

**Governing spec sections:**
- **Spec_Operational_Discipline_v1_2.md** §9.2 (C-OD-09) — Always-sampled exception set table (lines 511-532). The four conditional rows are verbatim: `files.operation` at kind∈{upload,delete}; `memory.operation` at kind∈{write,update,delete}; `validator.fail.*` at validator.fail.permanence=permanent; `subagent.span` (root). This is the governing contract the sampler currently over-satisfies.
- **Spec_Operational_Discipline_v1_2.md** §10.1 (C-OD-10) — Base-rate-sampled set table (lines 560-576). The NON-mutation complements live here: `files.operation` at kind∈{list,metadata,reference}; `memory.operation` at kind∈{read,list}. These are exactly the spans the conditional refinement must route to base-rate instead of always-sampled.
- **Spec_Operational_Discipline_v1_27.md** §1.1 element (c) — Ratifies always-sampled-event detection at SDK boundary as a runtime hook = is_always_sampled(event_name) (CLOSED-as-runtime-hook, lines 47). Confirms the detection MECHANISM is settled; does NOT bless event_name-only-forever as the contract — the §9.2 conditional qualifiers remain in-contract, so refining to consult attributes is honoring (not extending) the spec.
- **Spec_Operational_Discipline_v1_2.md** §9.3 invariants + deferred-discretion footer — Always-sampled-set inviolable + per-cell uniform (lines 534-542). Confirms the conditional refinement samples LESS (correctly) without violating the inviolable floor — the floor is the mutation/permanent/root subset, not the superset.

**Key carrier files:**
- `harness-od/src/harness_od/composite_sampler.py` — The SDK-boundary sampler that over-samples; the file to refine. should_sample ignores `attributes`. Docstring lines 21-39 self-document the over-sampling AND carry a STALE 'MVP hardcodes base_rate=1.0' claim (false at HEAD). · `HarnessCompositeSampler.should_sample / build_default_sampler`
- `harness-od/src/harness_od/sampling_mode.py` — Carries ALWAYS_SAMPLED_EVENT_CLASSES (18 entries) + is_always_sampled predicate (name-only match). The conditional logic must either extend is_always_sampled to take attributes or add a sibling attribute-aware predicate. · `ALWAYS_SAMPLED_EVENT_CLASSES / is_always_sampled / sampling_decision`
- `harness-od/src/harness_od/base_rate_set_and_envelope.py` — §10.3 per-cell base-rate envelope (8 cells: solo 1.0 / team 0.5,0.1,0.1 / mtc 0.2) + §10.1 base-rate set. Confirms per-tier base-rate is FULLY LIVE — the non-mutation spans must fall here. · `PER_CELL_BASE_RATE_ENVELOPE (lines 152-163)`
- `harness-runtime/src/harness_runtime/lifecycle/tracer_provider.py` — Production binding site. compose_tracer_provider (sampler=None path, lines 221-236) resolves base_rate from PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate and builds the sampler. Docstring lines 237-241 explicitly acknowledges 'the current default sampler ignores the mode (over-sample at SDK)'. · `compose_tracer_provider / materialize_tracer_provider_stage`
- `harness-runtime/src/harness_runtime/bootstrap/stage_4_od.py` — The REAL production producer. Line 56 calls materialize_tracer_provider_stage(config) with no sampler override → production path → registers globally. This is the live caller (not a dormant driver) proving the sampler is built-and-wired. · `materialize_tracer_provider_stage(config) (line 56)`
- `harness-as/src/harness_as/anthropic_primitive_sampling.py` — EXISTING AS-side declarative contract (U-AS-32). ANTHROPIC_PRIMITIVE_SAMPLING_POLICY already encodes HEAD_1_0_AT_MUTATION_BASE_RATE_AT_READ for files.operation + memory.operation. A posture MAP (not a runtime sampler) — but a build-shape input: OD may consume this via an OD→AS seam OR reimplement the kind-discrimination in OD. · `ANTHROPIC_PRIMITIVE_SAMPLING_POLICY / AuditFloorScope.FILES_OPERATION_MUTATION_ALWAYS_SAMPLED`
- `harness-od/tests/test_composite_sampler.py` — Tests that currently BLESS the over-sampling. test_always_sampled_literal_at_base_rate_zero_still_samples treats files.operation/memory.operation/subagent.span as unconditional literals (lines 38-43, 100-104). Must be updated so the conditional rows assert base-rate at non-mutation kinds. · `_LITERAL_ALWAYS_SAMPLED / test_always_sampled_literal_at_base_rate_zero_still_samples`

**Build slices (ordered):**
- Slice 1 — Add an attribute-aware predicate. Either extend is_always_sampled(event_name, attributes=None) in sampling_mode.py, or add a sibling is_always_sampled_conditional(name, attributes) that, for the 4 conditional rows, reads files.operation.kind / memory.operation.kind / validator.fail.permanence / root-ness and returns True ONLY at mutation/permanent/root; for all other entries falls through to the existing name-only literal/prefix match (unchanged).
- Slice 2 — Wire the predicate into HarnessCompositeSampler.should_sample so the already-received `attributes` (and span name / parent-ness) feed the conditional decision; non-mutation/non-permanent/non-root variants of the 4 rows now delegate to the TraceIdRatioBased(base_rate) branch (i.e. fall to §10.1 base-rate).
- Slice 3 — Resolve the subagent.span root-conditionality: confirm whether ParentBased composition already delivers root-only sampling (non-root spans inherit parent decision, never reach the root sampler) AND reconcile the literal name (set carries 'subagent.span' but CP emits 'subagent.execution' per topology_subagent_namespace.py:76 — likely a separate name-match gap making the current over-sample a no-op). Decide: rely on ParentBased for root-ness + fix/confirm the name, or add explicit parent-is-None handling.
- Slice 4 — Update test_composite_sampler.py: move files.operation/memory.operation/subagent.span out of the unconditional _LITERAL_ALWAYS_SAMPLED set; add parametrized cases asserting (a) mutation kinds → RECORD_AND_SAMPLE, (b) non-mutation kinds → base-rate (drop at base_rate=0.0), (c) validator.fail.* permanent → always, transient → base-rate. KEEP test_canonical_set_carries_18_entries (members unchanged; only predicate semantics refine).
- Slice 5 — Build-shape decision + (if consuming) seam: either import the AS U-AS-32 mutation-posture as the source of truth for {upload,delete}/{write,update,delete} (OD→AS seam) or hardcode the kind sets in OD against the §9.2/§10.1 spec rows. Pick the lower-coupling option; the kind enums are small and spec-fixed.
- Slice 6 — Verify by execution: run the OD test suite + a bootstrap-level trace to confirm a non-mutation files.operation span is base-rate-sampled at a low-base-rate cell (e.g. team-binding 0.1) while an upload span always samples — end-to-end, not just unit.

**Blockers / gates:**
- No hard blocker — genuinely independent arc, buildable now with stdlib + OTel SDK already in the stack (I-6 hand-roll honored; no framework pull).
- Soft gate (Slice 5): the wire-vs-reimplement build-shape choice. Reversible, has a sensible default (reimplement in OD against the spec kind-sets — lowest coupling for a low-load-bearing arc), so it is impl-discretion, not an operator gate.
- Verification gate (Slice 6): the §9.3 inviolable-floor invariant must be empirically preserved — mutation/permanent/root variants MUST still sample at base_rate=0.0 after the refinement (a regression here would under-sample a security/audit-critical span).

**Cross-arc deps:**
- GENUINELY INDEPENDENT — CONFIRMED. composite_sampler.py imports ONLY opentelemetry.* + harness_od.sampling_mode (verified). Zero coupling to RuntimeConfig, dispatch, routing, sandbox, MCP — touches nothing the SHARED-RUNTIMECONFIG cluster (B3/B4/B6/B2) owns. Can be sequenced any time, in parallel with the serial cluster.
- Soft input from AS (B6-adjacent / already-landed): harness-as U-AS-32 anthropic_primitive_sampling.py ALREADY declares HEAD_1_0_AT_MUTATION_BASE_RATE_AT_READ for files.operation + memory.operation. B7 may CONSUME this (OD→AS seam, OD is consumer-most-downstream) or reimplement. Not a blocking dep — a build-shape lead.
- Producer-side dep is already satisfied: the discriminating attributes are emitted at producer sites — files.operation.kind/memory.operation.kind at AS anthropic_attribute_namespaces.py; validator.fail.permanence consumed already by harness_od/tail_keep_classification.py (so the attribute is live on spans). No new emission needed.
- No dependency on B1 topology arcs #2-#16 for the files/memory/validator rows. The subagent.span row touches CP topology output (subagent.execution span emitted by topology_subagent_namespace.py landed via B1) — relevant only to Slice 3's name reconciliation.

**Estimated shape.** Single bounded impl arc (small). Shape: predicate-refinement + sampler-wiring + test-update, no spec authoring (impl-discretion). ~1 carrier file edited (composite_sampler.py) + 1 predicate file (sampling_mode.py) + 1 test file; optionally 1 seam if consuming AS posture. Low load-bearing (cardinality reduction only, per the brief). No research→design→spec→plan needed; this is thin Phase-7 impl against an already-cleared contract. The one genuine sub-fork (subagent.span name/root reconciliation) is cheap to resolve in-arc.

**Open questions:**
- subagent.span name mismatch: ALWAYS_SAMPLED_EVENT_CLASSES carries the literal 'subagent.span' but CP emits the span as 'subagent.execution' (topology_subagent_namespace.py:76 _SUBAGENT_SPAN). If the emitted name never matches the set literal, the current 'over-sample' on subagent is already a no-op and the '(root)' refinement is moot until the name is reconciled. Arc-open must confirm the actual emitted span name and whether 'subagent.span' should be removed/renamed in the set — possibly a separate small correctness gap, not just a cardinality refinement.
- Root-conditionality delivery: does ParentBased(root=HarnessCompositeSampler) already satisfy the §9.2 '(root)' qualifier for subagent spans (non-root spans inherit parent decision and never reach the root sampler)? If yes, Slice 3 shrinks to a name-reconciliation only and the conditional-attribute work is 3 rows (files/memory/validator), not 4.
- Build-shape fork (wire vs reimplement): consume the AS U-AS-32 mutation-posture map via an OD→AS seam, or reimplement the kind-sets in OD against the §9.2/§10.1 spec rows? Reversible; default = reimplement (lowest coupling for a low-load-bearing arc). Quick overlay-query --seam at arc-open to see if an OD→AS sampling seam is already enumerated.
- Confirm v1.27 §1.1(c) scope: it ratified the detection MECHANISM (runtime hook) as CLOSED — verify no reading treats the event_name-only implementation as a permanent contract narrowing of the §9.2 conditional qualifiers. (My read: it does not — the qualifiers stay in-contract, so this is impl-discretion. If a contrary reading surfaces, the call flips to fork/clearance.)

---

## Arc M — managed_agents C-RT-28 contract + production wiring (thin)

**Classification:** `built-needs-contract-or-wiring` · **Fork vs impl:** `mixed` · **Confidence:** high

**Rationale (body-verified at HEAD).** Body-verified at HEAD 3835408e. The capability is REAL and LIVE-PROVEN, with only the formal contract + production wiring owed (the M shape). `harness-runtime/src/harness_runtime/lifecycle/managed_agents.py` (read in full, 251 lines) carries the real machinery: `ANTHROPIC_MANAGED_AGENTS_BETA = "managed-agents-2026-04-01"`, `ManagedAgentSessionStatus` (9-state StrEnum), `ManagedAgentSession`/`ManagedAgentEvent` frozen dataclasses, `ManagedAgentsClientProtocol` async port, `AnthropicManagedAgentsClient` SDK adapter (create/send_event/retrieve/cancel via `client.beta.sessions.*` in `asyncio.to_thread`), and `managed_agents_runtime_span` async-ctx emitting `managed_agents.runtime_ms`/`session_id`/`billable_seconds`. The R-820 proof (`tools/r820_managed_agents_live_e2e.py`) drove a real Anthropic Managed Agents session to `session.status_idle` and confirmed `managed_agents.*` attrs in Cloud Trace — `.harness/substitutions.yaml:64` records H_T-AS-8f as SUBSTANTIVE_RETIRED (batch-52). So this is NOT a plain built-but-unwired item that may never have run e2e — it HAS run e2e against the live vendor. The two genuinely-owed gaps are exactly the needs-contract-or-wiring shape: (i) the formal C-RT-28 contract is unauthored — `grep -n "C-RT-28"` returns EXACTLY ONE hit (Spec_Harness_Runtime_v1.md:595, the negative prose "no §14.18 C-RT-28 sibling"); no `## §` section defines it; (ii) ZERO production callers — the only two repo-wide importers of `lifecycle.managed_agents` are the proof script and `harness-runtime/tests/test_lifecycle_managed_agents.py`. The production entrypoint `harness_runtime.api.run()` dispatches through CP `workflow_driver.execute_workflow` over `StepKind`-typed steps (DECLARATIVE/INFERENCE/TOOL/HITL/SUB_AGENT_DISPATCH); no step kind and no dispatch path reaches managed_agents. The overlay confirms `carrier_files: []` for H_T-AS-8f — the carriers are not cite-bound to a contract.

**Producer map.** DORMANT-DRIVER-ONLY. Reachability chain verified repo-wide: the only two importers of `harness_runtime.lifecycle.managed_agents` are (1) `tools/r820_managed_agents_live_e2e.py` (the R-820 proof script, invoked via `just r820...`, intentionally CI-excluded) and (2) `harness-runtime/tests/test_lifecycle_managed_agents.py`. There is NO production caller. The production path is `harness_runtime.api.run(workflow, config)` → acquires `_run_lock` → `run_bootstrap` → in-process MCP `run_workflow` tool → `harness_cp.workflow_driver.execute_workflow` → iterates `workflow.steps` dispatching by `StepKind` through the frozen `{StepKind → StepDispatcher}` registry. None of the 5 StepKind values routes to managed_agents; `_workflow_requires_inference` only inspects INFERENCE_STEP/SUB_AGENT_DISPATCH. So managed_agents is orthogonal to the live dispatch loop. The harness-as/harness-od references (anthropic_attribute_namespaces SCHEMA row, sampling posture, namespace_map, content_structure_discipline) are SCHEMA/POLICY declarations of the `managed_agents.*` namespace, not runtime producers — they describe the span the helper emits, they do not call it. R-820 is the live proof a real Anthropic session + Cloud-Trace export works; it is a proof, not production wiring.

**Fork vs impl detail.** Three distinct flavors. (a) C-RT-28 contract authoring = FORK-OWED: a new design-substrate spec delta (Spec_Harness_Runtime_v1.md §14.20 + likely §3 RuntimeConfig field + §4 HarnessContext field + a RT-FAIL-* class, mirroring C-RT-27) — an X-AL-3 design extension requiring a Class 1 fork doc + clearance marker before impl, since no cleared contract governs a managed-agents executable consumer (only the namespace-exclusion fork as_8f exists). (b) Production wiring = impl-discretion IF managed-agents dispatch rides an existing StepKind (e.g. SUB_AGENT_DISPATCH) under the already-cleared C-RT-08/C-CP-25 driver contracts; but FORK-OWED if it needs a NEW StepKind (CP spec touch → cross-axis fork, scope balloons past 'thin'). This is the load-bearing arc-open decision (see open_questions). (c) The live managed-cloud production run = VENDOR-GATE: it re-touches the Anthropic API key + GCP IAM/Cloud-Run identity-token + Cloud-Trace credential boundary (the proof script's `_fetch_cloud_run_id_token` / `_cloud_trace_token` / ANTHROPIC_API_KEY) — surface at the dispatch/cred boundary, never auto-fire per the no-unilateral-paid-calls discipline.

**Governing spec sections:**
- **Spec_Harness_Runtime_v1.md** §14.20 (next-free slot for C-RT-28; NOT §14.18) — The C-RT-28 ManagedAgents executable-consumer contract sibling to §14.17 C-RT-27. Brief assumed §14.18, but §14.18=C-RT-29 HarnessRunCLI and §14.19=C-RT-30 WorkflowManifestLoader are now occupied (NEW at v1.35); §14.NN headers run 5..19, so §14.20 is next free.
- **Spec_Harness_Runtime_v1.md** §14.17 C-RT-27 (line 4734) — Sibling-contract precedent: SkillActivationSpanEmitter + SkillActivationHook. Added RuntimeConfig.skill_activation_hook_config (§3) + HarnessContext.skill_activation_emitter field (§4) + a RT-FAIL-*-MATERIALIZE fail class (§11) + a materialize_*_stage factory. C-RT-28 should mirror this shape.
- **Spec_Harness_Runtime_v1.md** §14.18 C-RT-29 + §14.19 C-RT-30 (lines 365, 348) — The two contracts that NOW occupy the slot the brief called free — proof the line-595 'no §14.18 sibling' prose is stale.
- **Spec_Action_Surface_v1.md** §14.5 (line ~1244) H_T-AS-8f — Supersession (v1.9) banner: R-820 (PR #380) shipped the real adapter; H_T-AS-8f SUBSTANTIVE_RETIRED; local-development exclusion still TRUE but managed-cloud producer-site 'Re-opens at future H_T managed-cloud surface arc when production deployment materializes' — that arc IS arc M.
- **Architectural_Design_Document_v1_3.md** § (lines 80-90) ADR-D1/D2 3-tier — managed-cloud committed as one of 3 deployment surfaces (local-development / self-hosted-server / managed-cloud); managed-cloud admits engine class 1 with Anthropic/AWS/GCP candidates. Wiring a managed-agents production step composes with this tier.

**Key carrier files:**
- `harness-runtime/src/harness_runtime/lifecycle/managed_agents.py` — The built+proven capability — SDK adapter + session lifecycle + span helper. The carrier C-RT-28 will formalize a contract over. · `AnthropicManagedAgentsClient, ManagedAgentSession, ManagedAgentsClientProtocol, managed_agents_runtime_span, ANTHROPIC_MANAGED_AGENTS_BETA`
- `tools/r820_managed_agents_live_e2e.py` — The ONLY production-ish caller (a proof script, not api.run) — the dormant driver that live-proved the capability; not a workflow caller. · `main, _run_managed_agents_session, _emit_managed_agents_trace`
- `harness-runtime/src/harness_runtime/api.py` — Production entrypoint api.run() → in-process MCP → CP workflow_driver. The wiring TARGET; currently has no managed-agents path. · `run, _workflow_requires_inference, _INFERENCE_STEP_KINDS`
- `harness-cp/src/harness_cp/workflow_driver_types.py` — StepKind enum — the dispatch discriminator. No MANAGED_AGENTS kind; this is the load-bearing wiring fork decision. · `StepKind (DECLARATIVE_STEP, INFERENCE_STEP, TOOL_STEP, HITL_STEP, SUB_AGENT_DISPATCH)`
- `design-substrate/Spec_Harness_Runtime_v1.md` — Where C-RT-28 must be authored (fork) — §14.20, mirroring the §14.17 C-RT-27 precedent. Spec head is v1.48. · `§14.17 C-RT-27 (precedent); line 595 negative-prose hit`
- `.harness/substitutions.yaml` — Ledger row confirming H_T-AS-8f SUBSTANTIVE_RETIRED (batch-52, R-820); carrier_files empty (cite-bind owed). · `id: H_T-AS-8f, disposition: SUBSTANTIVE_RETIRED`

**Build slices (ordered):**
- Slice 1 (fork): file a Class 1 fork doc proposing the C-RT-28 ManagedAgents executable-consumer contract — resolve the StepKind question (new MANAGED_AGENTS kind vs ride SUB_AGENT_DISPATCH), the §-slot (§14.20), the contract-id convention (honor reserved C-RT-28 vs renumber to C-RT-31), and the RuntimeConfig opt-in shape. Operator-ratify.
- Slice 2 (spec): author C-RT-28 at Spec_Harness_Runtime_v1.md §14.20 mirroring the §14.17 C-RT-27 precedent — contract surface over AnthropicManagedAgentsClient/ManagedAgentsClientProtocol + (per precedent) a RuntimeConfig.managed_agents_*_config opt-in field at §3 + a HarnessContext client/emitter field at §4 + a materialize_*_stage factory + a RT-FAIL-MANAGED-AGENTS-* fail class at §11. Bump spec, file clearance marker, refresh the stale line-595 prose.
- Slice 3 (plan): decompose C-RT-28 into U-RT-NN atomic units at Implementation_Plan_Harness_Runtime (the precedent C-RT-27 → U-RT-99/100/101 3-unit linear chain). Add coverage-matrix row.
- Slice 4 (impl): wire the consumer into a production managed-cloud workflow step — bind the factory at bootstrap (gated on the managed-cloud surface + operator-supplied config opt-in, per the as_8f local-dev-exclusion still being TRUE), and route a managed-agents step through the driver dispatch registry per the Slice-1 StepKind decision. Provider-free unit tests + a skipif-gated/@pytest.mark.e2e live test.
- Slice 5 (cite-bind): tag managed_agents.py + the carriers to C-RT-28 / H_T-AS-8f so the overlay resolves them (currently carrier_files:[]); re-run `just overlay-check`.
- Slice 6 (vendor-gate, surface-only): a production managed-cloud run re-touching the ANTHROPIC_API_KEY + GCP IAM/Cloud-Run-identity + Cloud-Trace credential boundary — drive to the dispatch boundary, surface for authorization, do NOT auto-fire.

**Blockers / gates:**
- StepKind decision (BLOCKING the fork accounting): no MANAGED_AGENTS StepKind exists. New kind → CP spec C-CP-25 touch + cross-axis fork (scope expands well past 'thin'); ride SUB_AGENT_DISPATCH → impl-discretion. Must be settled in Slice 1 before spec/plan.
- C-RT-28 is an X-AL-3 design extension: requires a Class 1 fork doc + operator ratification + clearance marker BEFORE any impl edit (no cleared contract governs a managed-agents executable consumer).
- Vendor/credential gate: a live production managed-cloud run needs ANTHROPIC_API_KEY (paid) + GCP IAM/Cloud-Run identity-token + Cloud-Trace access — surface at the boundary; never auto-fire (no-unilateral-paid-calls discipline).
- Section/contract-id numbering: §14.20 is the hard correction (NOT §14.18, which is taken by C-RT-29); contract-id C-RT-28-vs-renumber-to-C-RT-31 is a convention call to confirm at arc-open.

**Cross-arc deps:**
- SHARED-RUNTIMECONFIG cluster contact (task framed M as independent — VERIFY at arc-open): by the C-RT-27 precedent, C-RT-28 likely adds a RuntimeConfig.managed_agents_*_config opt-in + a HarnessContext client/emitter field, which puts M in contact with the same RuntimeConfig surface B3/B4/B6/B2 converge on. If so, M should be sequenced into the serial cluster, not run parallel.
- CP axis (workflow_driver_types.StepKind + execute_workflow dispatch registry): a new StepKind ripples to CP spec + the driver — turns M from a runtime-only thin arc into a cross-axis one.
- AS axis (already landed, no dep owed): anthropic_attribute_namespaces SCHEMA + anthropic_primitive_sampling posture + namespace_map already declare managed_agents.* (3 attrs) — the consumer surface; no AS work owed, but C-RT-28 should cite the AS §14.5 namespace as the schema authority.
- OD axis (already landed): sampling_mode (managed_agents.runtime always-sampled floor) + content_structure_discipline already carry the 3 managed_agents.* attrs — no OD work owed.
- Persona arc: managed-cloud production composes with the team→multi-tenant-compliance persona-tier arc (ADR-D2/D5/D6); a real managed-cloud deployment is a team/multi-tenant surface, not solo-developer (the R-820 proof asserts SOLO_DEVELOPER only for determinism).

**Estimated shape.** Thin contract+wiring arc (capability + live-proof already exist) — fuller than trivial because C-RT-28 is a real design-substrate fork (spec delta + plan units + clearance marker), and one branch can expand it cross-axis. Shape ≈ fork-doc → spec(C-RT-28 §14.20) → plan(U-RT-NN chain) → impl(factory bootstrap-bind + driver wiring) → cite-bind → surfaced vendor-gate. If the StepKind decision lands on 'ride SUB_AGENT_DISPATCH' it stays runtime-thin; if it lands on 'new MANAGED_AGENTS StepKind' it balloons to a CP cross-axis arc.

**Open questions:**
- StepKind: does managed-agents dispatch get a NEW StepKind (MANAGED_AGENTS — CP spec touch, cross-axis fork) or ride an existing kind (SUB_AGENT_DISPATCH is the natural fit since it already routes to a dispatcher)? This decides fork accounting + whether M stays thin.
- Contract-id convention: honor the reserved-but-skipped C-RT-28 (consistent with the frozen arc_name), or renumber to next-sequential C-RT-31 given §14.18/19 already advanced to C-RT-29/30?
- RuntimeConfig coupling: does C-RT-28 add a RuntimeConfig.managed_agents_*_config opt-in + HarnessContext field (per C-RT-27 precedent)? If yes, M joins the SHARED-RUNTIMECONFIG serial cluster the task framed it as independent of.
- Surface-gating: the as_8f local-development exclusion remains TRUE — must production wiring gate strictly on DeploymentSurface.MANAGED_CLOUD (+ operator config opt-in), and what is the local-dev/self-hosted behavior (skip vs error)?
- Engine-class fit: managed-cloud admits event-sourced-replay/save-point/reconciler/WAL engine classes (engine_class_candidate.py) — does a managed-agents step interact with the engine-class candidate set, or is it engine-orthogonal?

---

## Drift-from-ledger rollup (ledger-hygiene feed)

Each agent's `evidence_drift_from_ledger` — what HEAD shows that differs from the stale spine-ledger/scoping evidence. Fold into the R-CL-Q1 ledger-hygiene pass.

- **E** — MATERIAL DRIFT. (1) DISPOSITION SUPERSEDED — the embedded ledger evidence and ALL grounding docs (.harness/class_2_fork_engine_durable_resume_no_production_producer.md → Option 3 'confirm DEFER'; r-cl-p2-engine-recovery-grounding.md 'P2 is substantially DEFER'; forward-register line 181 'RETIRED-AS-BOUNDED-RESIDUAL, do not build') converge on DEFER. That is exactly the stale evidence the full-spec directive overrides: 'Bucket A, re-opened as BUILD' + MEMORY feedback-full-spec-beyond-mvp-nothing-deferred ('NO defer/bounded-residual closes; every deferred capability = a BUILD arc; design back-flow PRE-AUTHORIZED'). The 're-open when a real event-sourced-replay/reconciler-loop/WAL-segment lands' trigger is now ACTIVE — the arc BUILDS them, hand-rolled. (2) LINE DRIFT — C-CP-49/C-CP-50 are at pause_resume_protocol.py:856/951, NOT the brief's :489; the engine_recovery_loop method site is :52/:74 (brief's ~:63 close); _IN_SCOPE_ENGINE_CLASSES + raise are at workflow_driver.py:184/:1351-52, NOT the fork doc's :81-83/:604-605 (drifted since bd50cd6). (3) NEW SINCE LEDGER — the WORKFLOW-LAYER durable-resume (api.resume :645) LANDED (R-CC-1 arc #3), making the engine LAYER the clean isolated residual; B1's 6 topology strategies all landed at the same workflow_driver dispatch site, so the engine-class gate now coexists with full strategy dispatch. (4) MIS-FRAME CORRECTION — this is NOT a vendor-gate; I-6 forbids vendoring but mandates hand-roll, and only RECONCILER_LOOP's LIVE e2e (K8s) is genuinely infra-gated. (5) class_1_fork_path_i (CLOSED-DEFERRED option E) un-skip trigger is now met under full-spec.
- **B2** — The brief was CORRECT that mcp_client_host is singular - C-RT-04 line 2206 confirms mcp_client_host of type MCPClientHost (NOT a mapping). The genuine drifts: (a) NEW FINDING - RuntimeConfig.mcp_clients is ALREADY a list of MCPClientConfig (types.py:1238); the config PLUMBING is plural, only MATERIALIZATION under-consumes via [0]. So the brief phrase the config field is SINGULAR conflates the materialized HOST (singular, correct) with the config LIST (already plural). (b) Per-server trust is MORE built than the brief implies: PerServerTrustEvaluator is fully built, multi-server-READY (per_server_overrides/deny/allow/tier-floor keyed on server_name) AND prod-wired at dispatch (runtime_tool_dispatcher.py:628) - only ever CALLED with one server_name because only one host exists. (c) C-CP-43 (MCPTrustTier 4-level enum) is a STALE plan-v2.6/v2.7 cite (only appears in Implementation_Plan_Control_Plane_v2_6/v2_7, marked DEFERRED) - the live carrier is MCPTrustTier at U-CP-00b, gate contract C-CP-27. (d) DOCSTRING-vs-BODY drift: mcp_client_host_factory.py docstring (lines 24-26) claims more-than-one configured raises a typed warning via the logger but the BODY (lines 173-181) SILENTLY uses [0] with NO logger call - the exact ledger-rot class (docstring lies, body does not). (e) Line numbers drifted (cited :11/:21/:169/:194 - symbols found but at different lines). (f) The Part-2 idempotent-restart is genuinely UNBUILT/REFUSED: start raises MCPHostAlreadyStartedError on re-start (mcp_client_host.py:293-299), 14.9.6 inv 1 out of scope at v1.
- **R** — DRIFT IS THE DISPOSITION not line-rot: brief confirm-deferred (L3) + deferred (L2) both SUPERSEDED by full-spec directive 2026-06-12 (live = BUILD vendor-gated). Minor: _declarative_echo now :496-499, infer call :547-553.
- **B4** — MATERIAL DRIFT from the embedded stale evidence. (1) Line number drifted: the brief cites 'llm_dispatch.py ~:489 role discarded until R-300' — at HEAD the role-handling is at lines 472-514 (U-RT-114 block); :488-489 is now `_effective_model_binding = binding.model_binding` and the comment no longer says 'role discarded until R-300' — it says per-role MODEL selection is resolved one layer out at RetryBreakerFallbackDispatcher and 'the per-role PROMPT is resolved once at stage 0 with the default role (deferred to B4)'. (2) Classification superseded: the stale evidence ('role dimension is DISCARDED at dispatch; MVP uses a single default role') is now FALSE for the MODEL dimension — arc #14 (U-RT-114, PR #546) landed per-role model dispatch-read; `_effective_chain(agent_role)` in retry_breaker_fallback.py augments the chain with the role's model, reached by real fan-out production callers. B4's residual has SHRUNK exactly as the brief anticipated: only per-role PROMPT + catalog + per-step override remain. (3) The brief's note about 'general per-role indexer beyond the orchestrator-workers path' is CONFIRMED real: the linear-driver StepExecutionContext (workflow_driver.py:1610) never sets agent_role — per-role takes effect ONLY on fan-out branches (2545/3251/3860), so the linear/sequential path still discards role for BOTH model and prompt. (4) parent_gate_level AUTO-default: the brief's 'minor scope (parent_gate_level AUTO-default in workflow_driver_types.py)' is LARGELY ALREADY BUILT — resolve_parent_gate_level (workflow_driver.py:1043) surfaces manifest_entry.default_gate_level else AUTO (CP-19 Reading A, spec v1.20 §6.1.Y); the residual is only per-STEP (vs per-workflow) gate-level, which couples to the per-step override fork. (5) Ledger hygiene: any embedded ':489 role discarded until R-300' citation should be refreshed to 'llm_dispatch.py:472-514 carries role for attribution; per-role MODEL landed at U-RT-114/retry_breaker_fallback.py:448-502; per-role PROMPT deferred to B4 per spec §14.5.3'.
- **CA** — MAJOR drift, 3 ways: (1) The fork it points at (fork_u_rt_49_cost_attribution_invocation_underspec.md) is CLOSED 2026-05-20 (Q1e/Q2-bounded/Q3c); it resolved per-STEP-BODY cost firing NOT the run-RESULT rollup; the brief conflates the two; the CA residual is the fork's carried Class-3 items 1 and 2 plus the never-built run-result aggregation. (2) Per-dispatch cost is now fully PRODUCTION-WIRED (llm_dispatch.py:1476 plus tool/validator/webhook siblings) against a LIVE rate table (rate_table_resolver/rate_table_bridge/rate_table_types exist; the PRICE_TABLE_REF deferred-to-U-OD-21 substitution is superseded). (3) DOCSTRING ROT: api.py 911-914 still claims cost_attribution empty because U-OD-21 is HALTED Class 1, but U-OD-21 is CLOSED (carrier grown 9 to 12 fields at commit 600b902; rollup_costs_by_axis landed plus unit-tested); the empty tuple is simply un-invoked. Also api.py:332 field type is tuple of CrossFamilyCostRollup (concrete) reconciling the abstract CostAttribution via a Class-3 note; the aggregate TYPE is chosen in code, only axis/invocation owed. Line numbers: RunResult api.py:291, _build_run_result api.py:889, empty-tuple api.py:962.
- **B5** — MATERIAL DRIFT from the embedded stale evidence (authored at 3d805d2/cf292da), in 5 respects: (1) LINE NUMBERS DRIFTED — the brief cites memory_tool_registry.py ~:5 ('ignores its argument') and ~:28 ('factory landing owed at U-RT-80; v2.15 MVP single pre-resolved backend'); at HEAD the actual resolve_backend body is lines 58-70 and the 'single pre-resolved' docstring is lines 14-17+62-69. Symbol-grep, not line-jump, was required. (2) 'factory landing owed at U-RT-80' is STALE/SUPERSEDED — the U-RT-80 factory IS LANDED (materialize_memory_tool_registry_stage, factory:228, wired at stage_5_loop_init.py:168). The factory landed long ago; only its surface-blindness remains. (3) BACKENDS EXPANDED since the brief — the brief says 'backends exist + are live (R-830: SQLite/S3/Neon)'; at HEAD the factory also constructs the FILESYSTEM (LocalFilesystem) AND a managed-DB (ManagedSql/psycopg) backend in addition to SQLite + S3 — FOUR live backends, and R-830 is now fully RESOLVED (roadmap line 2144: managed-DB live-proven on Neon; S3 live-proven; 'no longer carries a pending S3/cloud-vault blocker'). (4) CLASSIFICATION SHARPENED — the brief's framing ('resolve_backend IGNORES its argument') correctly anticipated vacuity; CONFIRMED at HEAD as built-but-vacuous with a REAL production caller (llm_dispatch.py:1327 via _dispatch_anthropic_with_memory), not merely 'cited'. The B3-style docstring-rot risk did NOT materialize for the resolve_backend body here (body matches docstring) — but the docstrings ARE now stale-as-described in the OPPOSITE direction: they call multi-surface 'future' when R-830 has since made all the destination backends live, so the only thing 'future' is the registry's map lookup. (5) FORK POSTURE CLARIFIED — multi-surface routing is EXPLICITLY impl-discretion per cleared plan v2_14 U-RT-80 AC line 91 + spec §14.12.7, NOT fork-owed; the surface-parametric contract is already the canonical spec reading (§14.12.1 line 4143 'for the deployment surface'). This feeds ledger hygiene: update the B5 row to 'built-but-vacuous, impl-discretion, R-830 backends all live, factory landed, only the surface→backend map owed'.
- **B6** — MATERIAL DRIFT confirmed: (1) The brief's 'CXA v2.20 §0.4 notes the STDIO tier-3 floor is not enforced' is MISATTRIBUTED — CXA v2.20 §0.4 is entirely about the R-PM-1 prompts-management forward-capability family (PROMPTS→selection→injection→governance), NOT sandbox. I grepped the whole CXA v2.20 doc; there is no STDIO/sandbox content at §0.4 (only 5 'sandbox' hits in CXA, none about the STDIO floor). The STDIO-floor-not-enforced observation is REAL but its canonical home is ADR-D2 §1.3 (ADR-D2.md:105-114) + runtime §14.9.9 scope boundary — not CXA. (2) The brief says '§14.9.8 flat resolver still bypasses the full sandbox_tier_floor composition' — TRUE and verified, but the brief undersells that the STDIO floor is ALSO built+wired-but-tier-discarded at mcp_host.py:111 (only REFUSE consumed), AND that the full sandbox_tier_floor IS reached by AS-internal consumers (sub_agent_sandbox_tier, gate_level_composition) — it is unwired specifically from DISPATCH, not globally dormant. (3) Drifted line numbers: the §14.9.8 resolver is now at runtime_tool_dispatcher_factory.py:82-103 / 268-285 (the brief's ':489' / '§14.9.8 flat resolver' line cites are stale). (4) NEW since the stale evidence: arc #1 (#503) deployment-surface-aware defaults landed (sandbox_defaults.py) and §14.9.9 (v1.43, Reading A+) driver-selection contract was authored + all 3 drivers (Docker/gVisor/E2B) are live and production-selected — confirming the brief's 'drivers already live'. (5) NEW finding not in brief: ToolContract does NOT carry the ToolMetadata forcing discriminators, so per-tool resolution via the full sandbox_tier_floor forces a ToolContract carrier-extension decision (folds a contract change into the fork).
- **B7** — MATERIAL DRIFT found vs the embedded stale evidence: (1) Line numbers drifted — the over-sampling docstring is at composite_sampler.py:21-39, not ':21-37'; the over-sampling logic is should_sample lines ~85-109. (2) The brief's 'C-OD-3 (composite sampler contract)' is a MIS-CITE — C-OD-03 in the spec is the Cell-selection contract (deferred candidate-within-class, Spec_Operational_Discipline_v1_2.md §3 line 213). The actual governing contracts are C-OD-09 §9.2 (always-sampled set) + C-OD-10 (base-rate set), as the code itself cites. There is no contract literally named 'composite sampler contract'. (3) The brief says the per-tier base-rate '1.0/0.1/0.2 IS live (done)' — TRUE but UNDERSTATED: it is the full §10.3 8-cell envelope (solo 1.0 / team 0.5,0.1,0.1 / mtc 0.2), production-wired through bootstrap stage_4_od.py → compose_tracer_provider, NOT a hardcoded 1.0. (4) The composite_sampler.py docstring's own claim (lines 35-39) that 'MVP hardcodes base_rate=1.0' is STALE/FALSE at HEAD — the production binding site overrides it with the cell-resolved rate. Same docstring-lies-while-body-moved-on rot the ledger warned about. (5) NEW since the brief: an AS-side mutation-posture contract (U-AS-32 anthropic_primitive_sampling.py) already exists declaratively — a build-shape input the stale evidence didn't mention. (6) Classification refinement: the brief implies a clean 'build the refinement' framing; the precise enum is built-but-vacuous (wired + runs, ignores discriminating attribute), NOT unbuilt.
- **M** — HEADLINE DRIFT (section-slot collision): the brief/scoping evidence and the spec's own line-595 prose say 'no §14.18 C-RT-28 sibling to §14.17 C-RT-27' — this is DOUBLY STALE. (1) §14.18 is now occupied by C-RT-29 `HarnessRunCLI` and §14.19 by C-RT-30 `WorkflowManifestLoader` (both NEW at v1.35), so the next free section is §14.20, NOT §14.18. (2) The line-595 prose was authored at v1.33 (batch-26); the spec head is now v1.48. So the 'sibling slot' the brief points at no longer exists. The C-RT-28 contract-id is reserved-by-reference-but-skipped (the §-headers/contract-ids jumped 27→29→30), so an arc-open must decide whether to honor C-RT-28 or renumber to C-RT-31. SECONDARY: the overlay reports `carrier_files: []` for H_T-AS-8f — the live-proven carrier (managed_agents.py) is NOT cite-bound to any contract, so 'cite-bind the R-820 carriers' is a concrete owed slice, not just hygiene. CONFIRMED-NOT-DRIFTED: managed_agents.py symbols, the C-RT-28-unauthored claim (exactly 1 negative-prose hit), the proof-script-only caller set, and H_T-AS-8f=SUBSTANTIVE_RETIRED all match the (more-current) scoping evidence. The AS spec §14.5 already carries a v1.9 Supersession banner explicitly naming arc M's gap: 'Re-opens at future H_T managed-cloud surface arc when production deployment materializes.'

---

*Filing footer — Artifact: `.harness/r-fs-1-remaining-arcs-grounding-sweep-v1.md`; Arc: R-FS-1 recommendation-1 grounding sweep; Posture: mode-agnostic; X-AL-3: trivially clean (zero `design-substrate/**` or `harness-*/src` edit). Method: 9-agent read-only Workflow (staleness-framed, body-level). Spine: `.harness/beyond-mvp-capability-boundary-ledger.md` + `.harness/r-fs-1-arc-1-scoping-v1.md`. Caveat: advisory leads (presence-not-correctness); re-ground at each arc-open.*
