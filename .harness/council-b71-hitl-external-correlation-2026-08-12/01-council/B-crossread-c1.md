# C1 cross-read (E1-B, verbatim)

*(Orchestrator note: the first C1 cross-read invocation was killed mid-run by a
model usage limit; this is the completed re-run, which additionally had C10's
cross-read in view — so C1 holds the last word on the seams. Returned by the
genuine C1 agent invocation, 2026-08-12.)*

*Re-adopted C1 (orchestration/control). All cites re-read at HEAD this session, not from recall. C10 has the seams already; I close S1, correct my own two MEDIUM claims, and answer C10's two open questions.*

---

**S1 (token basis — which exact run-scoped seed) — REFINE.** **C1 specifies handoff contract: the seed is `StepExecutionContext.parent_idempotency_key` as-is, and C9's `entry_version` warning is confirmed and is worse than C9 stated — so the "out-or-guarded" requirement is satisfied by persist-once (S4), not by basis surgery, plus a named precondition and a registered follow-on.** Grounds, from the actual call path: (a) `_escalate_to_secondary_channel` (`hitl_gate_composer.py:1259-1268`) takes `parent_action_id / step / placement / palette / escalation_reason / tenant_id`; both callers (`:2033-2040`, `:2166-2172`) already hold `step_context` — they read `step_context.tenant_id` at `:2039` and `:2172` — so a private-helper parameter widening reaches the branch frame with no Protocol, driver, or contract-type change; (b) `StepExecutionContext` (`workflow_driver_types.py:287-300`) declares `parent_idempotency_key: str` at `:295`, `step_index: int` at `:297`, `branch_index: int | None` at `:298`, and **no `run_id` field at all** — `run_id` is therefore *not* in composer scope and `run_idempotency_key` is not either; the only in-scope run-scoped seed is `parent_idempotency_key`; (c) C9 is right that it folds `entry_version`, and I verified the exact fold: `_compute_run_idempotency_key(run_id, workflow_id, *extras)` (`workflow_driver.py:646-665`) is invoked at `:3312-3316` with `extras=(str(manifest_entry.entry_version),)`, and `parent_idempotency_key = _compute_step_idempotency_key(run_idempotency_key, step_index)` (`:668-694`, `:8198`, `:15395`) inherits it; (d) worse than C9's "no resume-validation guard covers entry_version" — `entry_version` occurs **zero times in `harness-cp/src/harness_cp/pause_resume_protocol_types.py`** (verified, 0 matches over 1,515 lines), `snapshot_hash` covers only `(workflow_id + run_id + step_index + state_summary)` (`:929-930`), and `state_ledger_anchor` is the C-IS-05 `entry_hash` (`:935-937`), not the manifest version — so entry_version is neither captured nor guarded across the pause boundary. I **reject** threading `run_id` onto `StepExecutionContext` as the fix: that model is `frozen=True, extra="forbid"` (`:287`) and the add ripples to linear plus all five non-linear strategies plus `compose_branch_child_context`, and it would place a keyable-shaped run identity in reach of every runtime composer — a new leak surface C10's own posture argues against, for a preimage that never reaches the wire under the one-way digest anyway. **Selected: seed = `parent_idempotency_key`; digest = domain-separated over `(parent_idempotency_key, branch_index, placement.position)`, ≥128 bits; entry_version sensitivity is confined to the recompute fallback only, made inert by persist-once (S4), stated as a named precondition in the spec leg, and the wider unguarded-entry_version defect is registered (S10 item 5) — it is strictly larger than B-71 and touches the resume material-diff guard family, so absorbing it here would be silent scope-creep into a cleared mechanism.**

**S2 (one-field brief — C5-T1 drops `branch_index`) — COHERE, accepted.** C5 is right and the argument is structural, not stylistic: the brief *is* the webhook projection source (`hitl_gate_composer.py:1328-1330`), so a field on the brief is a field on the wire unless the adapter filters it, and a filtered projection splits the type's meaning by consumer — the drift the frozen model exists to prevent. My two-field proposal also admitted two illegal states (id set / index None, and the converse); `str | None` is the honest discriminated shape of the two real populations, and the B-97(a) additive-Optional precedent governs. My comprehension need survives intact: the ordinal stays in the digest **basis** (never exported), stays on the internal resume carrier (`PreDispatchGateOwningBranchResumeState.branch_index`, `pause_resume_protocol_types.py:361`+), stays on the local projection (`pause_state_projection.py:352`), and reaches the operator's eyes through C11's display-only prose `branch_context`. Net brief delta: one field.

**S3 (widening site + the F2 audit-loss finding) — COHERE on C9-R3; and YES, an OD-side check on the `hitl:` prefix family is owed.** Widen once inside `compose_hitl_action_id` (`hitl_gate_composer.py:428-440`): C9's fifth consumer is real and is the most consequential finding in the docket — the same composed string is the CP audit `action_id` and the F2 state-ledger `idempotency_key`/`step_id` at `:1543, 1552-1553, 1566-1586` (`idempotency_key=Identifier(str(hitl_action_id))` at `:1568`, `:1584`) under C-IS-07 §7.5 key-only dedup, so the second peer's HITL audit entry is dropped as an idempotent no-op *today*, independent of the webhook. Widening webhook-only would leave that unfixed and sever the `:92-93` caller-side audit join; widening once keeps webhook key, CP audit action_id, and F2 ledger key one identity family. On the OD-side check: the widening is prefix-stable by construction (`hitl:` preserved — the CXA v2.9 §0.3 8-prefix audit discriminator), so a *prefix-anchored* classifier is unaffected — but the change grows the action_id from three colon segments to four, which is exactly the shape that breaks a `split(":")`-indexed reader. **C1 requires a sixth witness in the impl-leg set: no OD-side consumer of the `hitl:` prefix family parses the action_id by segment count or exact shape** (a cheap grep-plus-assert, and the one blast-radius surface neither primary swept).

**S4 (persist-once; and my MEDIUM echo claim, CLOSED) — COHERE on C9-R1, with my claim SPLIT-falsified and the alternative threading given.** I read both construction sites. **At the construction site the caught signal is NOT in scope** — `workflow_driver.py:10374-10393` (and its `_execute_parallelization` mirror at `:14727-14744`) builds the tuple from `sorted(pre_dispatch_gate_owning_dispositions | _pre_dispatch_gate_owning_carried_forward)` at `:10388-10392`, a bare `set[int]` of ordinals; so my MEDIUM claim's falsifier fires as written. **But at the capture sites the signal object is live and in scope**: `_inflight_exc` at `:9671-9681` (name-matched `HITLPauseRequestedSignal`, `pre_dispatch_gate_owning_dispositions.add(branch_index)` at `:9680`) and `_pause_signal` at `:9909-9926` (name-match `:9925`, add `:9926`), plus the two `_execute_parallelization` mirrors at `:14073`/`:14313`. So the echo is implementable and the revision is a **carrier revision, not a shape revision**, exactly as my falsifier predicted: widen the disposition collection from `set[int]` to `dict[int, str | None]` (ordinal → token) populated at the four capture sites via duck-typed `getattr(signal, "brief", None)` access — harness-cp cannot import harness-runtime, which is why the catch is name-matched at `:9915-9916`/`:9925` — and read at the two construction sites. Two further findings that harden persist-once beyond C9's version-skew case: **(i) the carried-forward population has no live mint at all.** `_pre_dispatch_gate_owning_carried_forward` (`:10276-10281`) recovers ordinals from the *prior* snapshot for §25.19 warm-up-cohort followers that were withheld from this round's dispatch — no escalation fired, no signal exists, no token could be minted; under mint-only those rows are necessarily `None`, and only persist-once carries their identity forward. **(ii) Persist-once needs a reverse threading leg C10 did not price.** The composer mints *before* the driver persists (webhook at `:1328`, signal at `:1336`, driver records after), so on re-fire the persisted value must flow snapshot → composer — an outward direction that does not exist today. **C1 places that leg on the existing resume-carried pass-through family**: `hitl_uniform_fallback_eligible_run_id` / `effect_fence_uniform_fallback_eligible_key` / `effect_fence_tree_wide_abort_present` (`workflow_driver_types.py:483-545`) — computed once at the true depth-0 root against the operator's original un-narrowed root `PauseSnapshot` (`mcp_server.py`), threaded down unchanged, hash-inert, `None` default → byte-identical. One disanalogy to honor: those three are branch-*shared*; the token is per-branch — so the correct shape is a **fourth sibling carrying a `Mapping[int, str]` (branch_index → persisted token), inherited verbatim, with the composer selecting by `step_context.branch_index`**, which preserves the family's "cannot be recomputed at a nested level" discipline and avoids a reset-semantics decision.

**S5 (stability rescope) — COHERE, accepted.** C9-T1 is right and the miss is mine as much as C10's: my A1 failure-mode table assumed cross-resume stability implicitly ("identical identity + identical idempotency key on every re-fire") and I never flagged that it contradicts C10's written N2/MUST-8 non-commitment. The B-72 re-fire topology crosses pause epochs by construction — the gate re-fires *on resume* — so a conforming implementation could rotate at every resume boundary, violate no MUST, and produce exactly the ghost stream MUST 4 exists to prevent. Rescope adopted: **stable for the unresolved gate's lifetime within one run, across re-fires and resume cycles**; non-commitments: across runs, after resolve/abort, across any activated `new_run_id` resumption (dead at HEAD per C9's `pause_resume_protocol_types.py:1176-1177` check). C5's NC-6 epoch definition makes the witness writable.

**S6 (mint authority) — COHERE, accepted; and my "one mint site" claim restated correctly.** My A1 asserted "ONE mint site, zero new choke points," which is **false as stated about the type**: C5-T2 found the second constructor population and it is operator-authored — `ValidatorResult.escalation_brief: HITLEscalationBrief | None` (`validator_framework_types.py:170`), the brief's own docstring naming `ValidatorFramework.evaluate()` as constructor (`:138-139`), flowing into the driver at `workflow_driver.py:5669` and the escalation gate at `:5692-5695`. Correct restatement: **there is one *authoritative* mint site and one *authoritative read*.** The runtime composer at `_escalate_to_secondary_channel` is the sole authorized minter (NC-2); the validator population is a second constructor of the *type* but never a minter of the *field*, and a non-None value arriving from it is ignored-and-diagnosed at the consumption boundary — structural enforcement at the trust seam, not convention. Post-persist-once the read side sharpens symmetrically: the persisted snapshot value wins over any recompute. Without both halves, C10's tree-wide-distinctness commitment C1 is unenforceable.

**S7 (operator surface — C11's field set + palette binding + projection amendment) — COHERE, accepted into the design in full.** C11-T3 is the correction to my carrier 2 that I most needed and could not have found from the topology chair alone: my echo landed in `PreDispatchGateOwningBranchResumeState`, which no operator ever reads — the operator-facing surface is `project_pause_locations` / `PreDispatchUniformFallbackOnlyLocation` (`pause_state_projection.py:335-352`, `:816-827`), which declares no identity by design (v1.112 §2.2 constraint 2). Without the projection amendment my correlation loop terminates one layer short in a struct the operator cannot see; accepted under C10's two-rule restatement (external token yes / internal identity still never, restated alongside). On palette binding, C11-T2 is a live pre-existing violation at HEAD that neither primary audited, and from my chair it is **failure mode FM-F in my own self-audit**: a topology with operator-affecting affordances whose HITL placement advertises `proposed_response_palette` (`webhook_brief_adapter.py:81-83`) on a request the resolver silently drops (`pause_state_projection.py:338-341`). Binding palette display to the structured two-value `resolvability` is the disarm, not the documentation, of that trap — accepted as a spec-leg MUST, along with C10's condition that `branch_context` prose is contractually barred from carrying `snapshot_run_id`, the internal identity, run_id-shaped strings, or raw basis material.

**S8 (identity scope) — COHERE, confirmed.** Fan-out-only population first, placement-inclusive basis. Placement-inclusive is precisely what makes the later uniform extension *additive rather than breaking* — C5-T3's point (i) is correct and it was already my basis, so I hold it: a pre-dispatch owner can hold a `PRE_ACTION` gate on an `INFERENCE_STEP`/`TOOL_STEP`, not only `SUB_AGENT_BOUNDARY` (the round-5-corrected resume guard, and the captured-`step_kind` rationale in `PreDispatchGateOwningBranchResumeState`'s class docstring, `pause_resume_protocol_types.py:361`+), so a placement-blind basis would hand two logical gates on one branch the same token *and* the same key suffix — delivery loss manufactured by the identity format. Presence/absence as the population discriminator (NC-3) is the right discriminated shape, and the rule that had to be uniform *now* — the webhook never carries ingress keys — is uniform under this design.

**S9 (truncation floor ≥128 bits) — COHERE, accepted.** C9-R4's ground is decisive and specific to this seam: the collision-recovery posture of a dedup/equality key is "treat as duplicate," and here duplicate-treatment *is* cross-branch suppression — the exact defect B-71 exists to fix, re-created probabilistically by under-provisioned entropy. No truncation below 128 bits in the idempotency key or any equality-bearing field; display truncation (C11's `a3f81c…`) is fine because display is not an equality surface — and per S7 the human's actual discriminator is the prose `branch_context`, not the token prefix.

**S10 (follow-on registrations vs in-arc absorption) — REFINE, with one sequencing constraint.** **Registered as new `B-*` rows** (each an observable contract change to a cleared mechanism → own spec leg per X-AL-3): **(1) uniform-response target selector** — a properly-specified property-6 extension keying by the external token on a NEW resume-context field, never `hitl_responses` (my A1 close; the stable identity is exactly the handle such an extension would key by, which is why it must not be designed here); **(2) redelivery-on-posture-change** (C11-T4) — no webhook re-fires when N-1 peers resolve and the last becomes sole-addressable (`workflow_driver.py:2851-2898`, esp. `:2891-2897`); C9's knob; **(3) uniform-treatment extension** to depth-0 root and already-dispatched-child escalations — additive under the placement-inclusive basis but an observable population change; **(4) the addressing half** — original B-71's narrower half (exposing an already-dispatched child's real `run_id` so the operator can build a `hitl_responses` key); C10 answered NO for the webhook channel this arc, and I accept that answer, so what survives is the pause-view-side capability question; **(5) NEW, from my S1 grounding — unguarded `entry_version` across the pause boundary**: `run_idempotency_key` folds it (`workflow_driver.py:3312-3316`) but nothing captures or guards it (0 occurrences in `pause_resume_protocol_types.py`; `snapshot_hash` = workflow_id+run_id+step_index+state_summary, `:929-930`), so a bump across a pause silently rotates *every* step idempotency key on resume — strictly wider than B-71 and inside the resume material-diff guard family; **(6) the typed `ResumeKeyDisposition` enum + resume-outcome diagnostics carrier** (C5-NC-10 + C11's landing sites), registered as a *named companion* leg because it lands on a different carrier family (the resume outcome shape, which C11 himself marks [MEDIUM]/impl-grounded) — **not** a deferral: the design record commits to it. **Absorbed in-arc:** the F2 audit-loss fix (C9-R3 — it *is* the same widening B-71 already makes; splitting it ships the widening half-applied), NC-2 mint-authority ignore-and-diagnose, the palette/`resolvability` binding, the projection amendment, and the persist-once reverse-thread field. **C1 terminates on this sequencing constraint:** registration (6) and the B-71 spec leg must ship together, or C11's `resolvability_note` wording — *"the resume outcome reports that the response did not attach"* — is a false promise the moment it ships; if (6) slips, the wording must be softened in the same commit.

---

### C10's two remaining seam questions to C1

**Q2 — the 2+-concurrent shape: N distinct-but-flagged, not an aggregate.** Four grounds, all control-flow: (a) an aggregate has **no honest mint site** — the composer fires from inside a single branch's own frame and cannot know the live sibling count (C11 rejects a live "N outstanding" count for the same reason); an aggregate would require a new fan-in choke point at the barrier, i.e. a new topology element introduced for a display concern — a custom composition with no control-flow gain (FM-D territory, and I decline it); (b) the escalations are **not simultaneous** — each branch escalates when its own gate trips, so an aggregate body would have to be mutable and re-delivered as peers join, converting one durable webhook body into a stream of supersessions, strictly worse than N stable bodies; (c) N-distinct is what makes C9's *at-least-once-per-re-fire, dedupable-by-stable-key* contract work at all — an aggregate has no stable key as membership changes, so it defeats consumer-side dedup, which is the whole mechanism S3/S9 exist to protect; (d) MUST 5 is satisfied *without* aggregation because the flag is static at mint — `held-for-sole-resolution` follows from placement plus branch-context presence, not from a live count, which is exactly C11's design. **So: N distinguishable, stably-identified requests, each flagged non-individually-actionable, collectively parked; resolution serialized to the property-4/6 sole-membership uniform fallback (`workflow_driver.py:8340-8350`), with §1.1(c) INERT re-pause while 2+ remain — the accepted liveness limit, disclosed, not relabeled.** The *aggregate view* belongs on the pause projection, where the tree genuinely is in scope — which is exactly where C11-T3's amendment puts it.

**Q1-residual — threading for the not-yet-dispatched `PURE_PATTERN_NO_ENGINE` branch (charter constraint 4).** The basis is composed entirely from values present at the branch frame *before* any dispatch call: `parent_idempotency_key` (`workflow_driver_types.py:295`) and `branch_index` (`:298`), both set at branch-spawn by `compose_branch_child_context` (`:586-648`), plus the composer's own `placement` parameter (`hitl_gate_composer.py:1264`). None is an output of dispatch — `compose_child_run_id_seed` *consumes* `parent_idempotency_key`, it does not produce it — so for `PURE_PATTERN_NO_ENGINE`, where the seed falls through to `None` and a uuid is minted only at dispatch, the token is available strictly *earlier* than the uuid, at gate-fire time. Threading: both callers already hold `step_context` (`:2039`, `:2172`) → widen the private helper's parameter list to accept it (or `branch_index` + `parent_idempotency_key`) → **no `StepExecutionContext` field add on the mint path, no driver change, no Protocol change.** The only new context field anywhere is the persist-once *reverse* thread (S4), and that is a resume-cycle **read**, `None` on the first escalation — which is precisely the branch that has no persisted value yet. Constraint 4 satisfied by construction.

---

## Confirm-back: the integrated design as C1 now endorses it

1. One opaque field, `escalation_instance_id: str | None = None`, on `HITLEscalationBrief` (`validator_framework_types.py:134-151`); `branch_index` on neither carrier (S2). Linear paths byte-identical.
2. Basis: domain-separated one-way digest over `(parent_idempotency_key, branch_index, placement.position)`, ≥128 bits, never truncated in the key or any equality-bearing field. `parent_idempotency_key` is the seed because it is the **only** run-scoped value in composer scope — `StepExecutionContext` declares no `run_id` (`workflow_driver_types.py:287-300`).
3. `entry_version` is folded one-way into that seed (`workflow_driver.py:3312-3316` → `:646-665`) and is **unguarded across the pause** (0 occurrences in `pause_resume_protocol_types.py`). Handled by persist-once, a named recompute-only precondition, and registration (5) — not by basis surgery.
4. Mint: sole authoritative minter is the runtime composer at `_escalate_to_secondary_channel` (`hitl_gate_composer.py:1259-1268`), reached by widening the private helper to take `step_context`; both callers already hold it (`:2039`, `:2172`). Validator-population values ignored + diagnosed (NC-2).
5. Available pre-dispatch by construction — covers `PURE_PATTERN_NO_ENGINE` (charter constraint 4).
6. Persist-once: the snapshot echo is authoritative once written; deterministic recompute is the crash-fallback for the mint→persist window only.
7. Echo wiring (my corrected MEDIUM claim): widen the disposition collection from `set[int]` to `dict[int, str | None]`, populated at the four capture sites (`:9680`, `:9926`, `:14073`, `:14313`) via duck-typed brief access, read at the two construction sites (`:10374`, `:14727`).
8. Reverse thread for re-fire: a fourth sibling of the resume-carried pass-through family (`workflow_driver_types.py:483-545`) carrying `Mapping[int, str]`, computed once at the depth-0 root, hash-inert, `None` default → byte-identical.
9. The carried-forward population (`:10276-10281`) has no live mint and reads its token only from the recovered snapshot — an independent requirement for persist-once.
10. Stability: the unresolved gate's lifetime within one run, across re-fires and resume cycles; no promise across runs or past resolve/abort.
11. Widening lands once inside `compose_hitl_action_id` — webhook `Idempotency-Key`, CP audit `action_id`, and the F2 ledger key stay one family — closing the live per-peer F2 audit-loss aliasing (`hitl_gate_composer.py:1566-1586`). Six witnesses, including the OD `hitl:`-prefix no-segment-count-parsing check.
12. Delivery: at-least-once per re-fire, dedupable by stable key; no harness-side suppression store.
13. Operator surface: token + display-only prose `branch_context` (ordinal in prose, no-format commitment, never run_id-shaped material) + structured two-value `resolvability` + `resolvability_note`, all `payload_body`-only; palette display bound to the posture.
14. `PreDispatchUniformFallbackOnlyLocation` gains the external token; v1.112 §2.2 constraint 2 restated alongside — internal identity still never.
15. Ingress: advisory-correlation-only as a structural one-way rule (NC-7); matches counted-as-unaddressed **and** diagnosed via C5's typed disposition, landing on the resume outcome and the pause view.
16. Webhook carries no ingress keys — no real `run_id` transits it; addressing stays on the operator-held pause view.
17. 2+-concurrent: N distinct, stably-identified, statically-flagged requests, collectively parked; §1.1(c) INERT re-pause disclosed, not relabeled; the aggregate view lives on the projection.
18. Six registered follow-on `B-*`s; F2 fix, mint authority, palette binding, projection amendment, and the persist-once thread absorbed in-arc.

## Remaining disagreements

None with C10, C9, C5, or C11 — every seam closes COHERE or REFINE-accepted, and both of my A1 MEDIUM claims are now closed against the code (the echo claim split-falsified with the alternative threading supplied; the ordinal-stability claim untouched). Two things I hold as hard conditions rather than open disputes. **(a) Sequencing, S10:** the resume-outcome diagnostics leg (registration 6) must land with the B-71 spec leg, or C11's `resolvability_note` promise that the resume outcome *"reports that the response did not attach"* ships as a falsehood — if it slips, the wording softens in the same commit; this is a commitment the design record must carry explicitly, not an impl-leg detail. **(b) Scope, S1:** the unguarded-`entry_version` finding is registered, **not** absorbed — it is a pre-existing CP defect strictly wider than B-71 (it rotates every step idempotency key in a run, not just this token) and it sits inside the resume material-diff guard family; absorbing it here would be exactly the silent scope-creep into a cleared mechanism that X-AL-3 exists to prevent, and I will treat any move to fold it into this arc as a fork to surface rather than a convenience.
