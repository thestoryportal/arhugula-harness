# C10 cross-read (E1-B, verbatim)

*(Orchestrator note: returned by the genuine C10 agent invocation, 2026-08-12.
The paired C1 cross-read was killed mid-run by a model usage limit and re-run
after the operator switched models — see `B-crossread-c1.md`.)*

**S1 (token basis) — COHERE, restated basis accepted.** C5-T3's adjudication against my hash-the-internal-identity basis is correct on both counts and I verified the second: the internal identity `{snapshot_run_id}:pre-dispatch-gate:{branch_index}` omits placement, so one branch owning a `PRE_ACTION` gate on an `INFERENCE_STEP` plus a boundary gate would collide two tokens — violating my own commitment C1; and `snapshot_run_id` is not in composer scope while `parent_idempotency_key` (run_id folded in) is. My MUSTs were about the *construction discipline* — opaque, deterministic, one-way, no recoverable internal segments — not about which preimage feeds the hash; C1's basis satisfies every one of them under that discipline. I accept **domain-separated digest over a run-scoped seed + `branch_index` + `placement`, >=128 bits**, with C9's refinement adopted verbatim: `entry_version`/`extras` are either out of the basis or resume-guarded — an unguarded rotation input is a silent identity-rotation channel, which is my ghost-request FM by another route. This also answers C1's seam Q1: yes, a domain-separated one-way digest sufficiently severs the lineage from `parent_idempotency_key`'s role in `compose_child_run_id_seed`; the domain prefix is the load-bearing severance and must be stated in the spec leg.

**S2 (stability rescope) — REFINE accepted (correction of my own MUST 4/N2).** C9-T1 is right: the B-72 re-fire topology crosses pause epochs by construction (re-fire happens at resume), so my "stable within one pause epoch" non-commitment licensed exactly the token rotation MUST 4 exists to prevent — a conforming implementation could rotate at every resume boundary and mint the ghost stream. I accept C9-R2's rescope: **stable for the unresolved gate's lifetime within one run, across re-fires and resume cycles**; non-commitments: across runs, after resolve/abort, across any activated `new_run_id` resumption. C5's NC-6 epoch definition (pause-entry record -> next successful resume validation) makes the witness tests writable. Blast-radius note I attach: the rescoped token is a longer-lived correlation handle, and that is acceptable *only because* it is structurally advisory-only (S6/NC-7) — the stability widening and the ingress-rejection rule are one package, not separable.

**S3 (dedup-consumer enumeration) — COHERE; falsification conceded on verification, widening site accepted.** I re-read the sites before conceding. `webhook_delivery_composer.py:448-472` is cost attribution with `span_id=f"webhook-deliver-{idempotency_key}"` — my consumer (i) "pause-store synthesis" does not exist as cited; the citation was wrong. And C9's fifth consumer is real and is the most important finding in the docket for my domain: `hitl_gate_composer.py:1566-1586` composes the F2 state-ledger `idempotency_key=Identifier(str(hitl_action_id))` (:1568, :1584) from the same workflow-scoped key, so under C-IS-07 key-only dedup the second peer's HITL audit entry is dropped as an idempotent no-op **today**. Under my own posture argument — reactive containment where the tamper-evident audit trail is the *primary defense* — silent per-peer audit-entry loss is a defect one tier more severe than the webhook dedup collision; B-71's fix now repairs the primary defense layer, not just operator correlation. I accept C9-R3: **widen once, inside `compose_hitl_action_id`**, keeping webhook key, CP audit `action_id`, and F2 ledger key one identity family (preserving the `:92-93` caller-side audit join), `hitl:` prefix untouched, with witness tests over all five consumers including the new "second-peer F2 audit entry survives" witness.

**S4 (persist-once) — COHERE.** C9-R1 resolves my MODERATE claim #4's open precondition exactly: the persisted echo is authoritative once written; deterministic recompute is the crash-fallback for the mint-to-persist window (the webhook fires at :1328 before the signal at :1336, so the external world can legitimately be ahead of the snapshot — only a deterministic fallback closes that window). This also forecloses C9-T4's version-skew drift, which in my FM table is a wrong-branch-resolution vector (webhook says Y, snapshot says X). Blast-radius verdict: persisting an advisory-only, non-keyable token in the snapshot adds no capability; accepted.

**S5 (truncation floor) — REFINE accepted (tightening my own MUST 2).** My "truncated SHA-256" stated no floor, and C9-R4 identifies why a floor is load-bearing here specifically: the collision-recovery posture of a dedup/equality key is "treat as duplicate," and here duplicate-treatment *is* cross-branch suppression — the exact B-71 defect, re-created probabilistically by under-provisioned entropy. **No truncation below 128 bits in the key or any equality-bearing field**; display truncation (C11's `a3f81c...`) is fine because display is not an equality surface.

**S6 (mint authority) — COHERE, accepted as a required clause.** C5-T2 found a constructor population I missed, and it sits squarely on my trust boundary: `ValidatorResult.escalation_brief` (`validator_framework_types.py:170`) is operator-authored — and validator code runs under exactly the reduced-trust conditions my sandbox carve-out exists for. A validator-fabricated `escalation_instance_id` is a spoof/confusion vector: it could collide with a genuine token and misdirect operator attention or corrupt consumer-side dedup. NC-2's **ignore-and-diagnose at the consumption boundary** is the right shape — structural enforcement at the trust seam, not convention — and without it my tree-wide-distinctness commitment C1 is unenforceable. Required in the spec leg.

**S7 (prose-embedded ordinal) — COHERE; MUST NOT 7 is satisfied, no conflict.** C11-T1's circularity charge is correct: the B-71 trigger case is byte-identical peers, so every prose field I pointed to matches on both — my own A1 already conceded the escape ("enriched with display-only fields carrying no identity semantics"), and `branch_context` is that field. MUST NOT 7 governs the **identity** — no structured, parseable composite as the correlation value — and C11's design honors it: the ordinal rides a display-only prose sentence, `payload_body`-only, under an explicit no-format-commitment; the machine-facing contract stays ordinal-free. My disclosure objection is reduced as C11 argues: `branch_index` is already operator-visible on the local projection (verified, `pause_state_projection.py:352`), so the residual is third-parties-on-the-webhook-path seeing a fan-out ordinal + a manifest-authored step name — the operator's own data, not internal identities, and governed by the existing webhook transport trust posture; no new gate warranted. One REFINE as a spec-leg MUST: `branch_context` must never contain `snapshot_run_id`, the internal identity, the raw digest basis, or any run_id-shaped string — the prose channel must not become the leak the opaque token was built to prevent.

**S8 (palette qualification) — COHERE, accept, and here is the safety framing.** C11-T2 is a genuine pre-existing MUST-5 violation at HEAD that neither primary audited: `proposed_response_palette` is projected outward (`webhook_brief_adapter.py:81-83`) on requests whose responses the resolver silently drops (`pause_state_projection.py:338-341` — verified docstring). Safety framing: **the palette is an action affordance shipped across a trust boundary; advertising an affordance the ingress will silently discard is a manufactured operator trap** — my FM 4, live today, independent of B-71. The spec leg must bind palette display to the `resolvability` posture: when `held-for-sole-resolution`, the default consumer suppresses the reply affordance with zero intelligence (C11's structured two-value field is what makes that possible). This converts the collision defence from a wall the operator walks into after acting, into a posture disclosed before acting — disarming, not documenting, the trap.

**S9 (projection amendment) — COHERE; blast-radius verdict: accept with a two-rule restatement.** My seam-Q2 concern was the durable snapshot echo; C11-T3 correctly shows the echo terminates one layer short of the operator without the public-projection amendment. Verdict: `PreDispatchUniformFallbackOnlyLocation` gaining the EXTERNAL token does **not** undo v1.112 §2.2 constraint 2, because the two rules protect different things — constraint 2 bans declaring the *internal, run_id-shaped, never-keyable* identity (which invites the silent-drop trap); the external token is by construction non-keyable-shaped, advisory-only, and ingress-diagnosed (NC-7), so it invites nothing the resolver won't diagnose. The variant already carries `branch_index` (:352), so no new topology data is exposed; the read surface is local and operator-held; no gate change. Condition: the spec leg restates constraint 2 alongside the new field — internal-identity-still-never — so the amendment cannot be misread as precedent for exporting internal identities.

**S10 (addressing half, answered) — the webhook stays correlation-only: NO real `run_id` transits the webhook channel, this arc.** Grounds: (a) a `hitl_responses`-keyable `run_id` is an addressing **capability**, not advisory context — carrying it on the outward channel changes the payload's blast-radius class and the channel's contract from "nothing here is ever an ingress key" to "some fields are keys," per-field exceptions being exactly the Hyrum surface I want closed; (b) the residual webhook-path audience is third parties (C11's own reduction), and a capability handle in third-party-visible transit is a spoof/misattribution primitive — C1's own analysis shows a parent-run_id key is a *live misattribution path*, not inert; (c) operator capability is not diminished: C11's resolvability wording routes the operator to the pause view, the operator-held local surface where addressable run_ids already legitimately live — capability handles on the operator-held surface, advisory correlation on the outward channel, which is my posture's asymmetry applied exactly. **Scope verdict: fan-out-only population first, uniform-safe by construction.** The token populates iff fan-out branch context is present (C5 NC-3 — absence is the discriminator); the basis is placement-inclusive (S1) so a later uniform extension to depth-0 root and already-dispatched-child escalations is additive and non-breaking. The rule that must be uniform NOW is the negative one — the webhook never carries ingress keys — and it is uniform under this design. Uniform token treatment and any future addressing-channel extension route to a registered follow-on `B-*` (X-AL-3), not absorption here.

## Confirm-back: the integrated design as C10 now endorses it

One opaque correlation token, `escalation_instance_id: str | None = None`, sole new field on `HITLEscalationBrief` (C5's one-field adjudication; `branch_index` on neither carrier). Minted only by the runtime composer at `_escalate_to_secondary_channel` (NC-2 mint authority; validator-population non-None values ignored + diagnosed). Basis: domain-separated digest over a run-scoped seed + `branch_index` + `placement`, >=128 bits, never truncated in any equality-bearing field; `entry_version`/extras out of basis or resume-guarded. Persist-once: the snapshot echo is authoritative; deterministic recompute is crash-fallback only. Stability: the unresolved gate's lifetime within one run, across re-fires and resume cycles; no promise across runs or past resolve/abort; epoch per NC-6. Widening lands once, inside `compose_hitl_action_id` — webhook `Idempotency-Key`, CP audit `action_id`, and the F2 state-ledger key stay one family — closing the live per-peer F2 audit-loss aliasing (`hitl_gate_composer.py:1566-1586`), with witness tests over all five consumers. Delivery is at-least-once per re-fire, dedupable by stable key; no harness-side suppression store. Operator surface: token + `branch_context` display-only prose sentence (ordinal in prose, no-format-commitment, no internal identities ever) + structured two-value `resolvability` + `resolvability_note`, all `payload_body`-only; palette display bound to the resolvability posture (disarms the live MUST-5 trap at HEAD). Public projection variant `PreDispatchUniformFallbackOnlyLocation` gains the external token; internal identity still never declared (v1.112 constraint 2 restated alongside). Ingress: advisory-correlation-only as a structural one-way rule (NC-7, §21.3-style); matches diagnosed via C5's typed disposition enum, landing on the resume outcome + pause view. Webhook channel carries no ingress keys — no real `run_id` transits it; addressing lives on the operator-held pause view. Fan-out-only population now; uniform extension and the addressing half are registered follow-on `B-*`s.

## Remaining disagreements

None blocking. Two conditions I hold as spec-leg MUSTs rather than open disputes: (1) `branch_context` prose is contractually barred from containing `snapshot_run_id`, the internal identity, run_id-shaped strings, or raw basis material — display-channel leakage would defeat the opaque-token construction; (2) the S7 ordinal-in-prose acceptance is scoped to this display field and is not precedent for structured ordinal export — a future proposal to make `branch_index` a structured wire field re-opens my ground 3 and routes back through review.
