# Roadmap drift-detection log — full archive

Full §12.3 drift/reconciliation audit history. The live dashboard (`.harness/roadmap_status.md` → Drift detection log) shows only the most recent events; this file preserves the complete record.

| Date | Source | Resolution |
|---|---|---|
| 2026-06-10 | **Claude release-candidate handoff authored locally at `a5215dde`; post-handoff refresh.** | Hash `e13d42d7874c` → `dbfe6623fc76` (state at `a5215dde`, open PRs unavailable/empty, fork count 47, batch-56). Added `.harness/release-candidate-deployment-readiness-runbook.md` and Claude/context pointers. Next selector is the HIL release-candidate deployment-readiness scope gate, then provider-free readiness before any live deployment smoke. |
| 2026-06-10 | **Overlay-check ignored-artifact fix merged locally at `d4b6b4e3`; post-merge refresh.** | Hash `c28613a1f0a3` → `e13d42d7874c` (state at `d4b6b4e3`, open PRs unavailable/empty, fork count 47, batch-56). `overlay-check` now ignores stale gitignored `tools/semantic_overlay/overlay.json` artifacts while preserving the hard gate for stale tracked snapshots. Next selector moves to an HIL release-candidate deployment-readiness scope discussion before opening new implementation. |
| 2026-06-10 | **Post-#447 terminating refresh — PR #447 BMad runtime config restore merged at `9cf968d2`; §12.2 owed follow-on.** | Hash `48f9537038cf` → `f9c6f1ebe73d` (state at `9cf968d2`, open PRs empty, fork count 47, batch-56). `_bmad/bmm/config.yaml` is restored on main; no roadmap item status changes beyond clearing the last in-flight PR. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **PR #440 supersession / post-#464 open-PR refresh branch opened from `d24dcf57`.** | Hash `78617c29740a` → `48f9537038cf` (state at `d24dcf57`, open PRs #447 only, fork count 47, batch-56). #440 was closed as superseded: it was conflicting, predated the later R-CXA-2 implementation/back-flow sequence, and a current-main comparison showed it would remove later R-CXA-2 implementation, tests, batch records, and dashboard/overlay updates if merged. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Post-#463 terminating refresh — PR #463 R-IF-112 semantic overlay closeout merged at `925f021a`; §12.2 owed follow-on.** | Hash `ebb6d51193e6` → `78617c29740a` (state at `925f021a`, open PRs #440/#447, fork count 47, batch-56). R-IF-112 is now RESOLVED: `tools/semantic_overlay` derives the design-substrate contract keyspace, source cite layer, CXA seam endpoint graph, and substitution join; CI gates hard CXA/stale-artifact drift and reports advisory code/contract/substitution orphan classes. Next selector moves to this terminating refresh, then recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Substantive R-IF-112 implementation closeout branch opened from `62f9a327`; §12.2 owed after merge.** | Hash `a0ddc2154026` → `ebb6d51193e6` (branch base `62f9a327`, open PRs #440/#447, fork count 47, batch-56). The existing semantic overlay linter is completed against the roadmap must-pass by adding design-substrate `C-*` keyspace scanning and advisory `contract_without_code` orphan reporting, alongside the existing code-without-cite, CXA missing-endpoint, and substitution-without-carrier classes. `R-IF-112` is moved to RESOLVED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |
| 2026-06-09 | **Post-#461 corrective fixed-point refresh — PR #461 post-#460 roadmap/status/dashboard refresh merged at `41297c2a`; §12.2 anchor re-pin.** | Hash `b958e3438c97` → `a0ddc2154026` (state at `41297c2a`, open PRs #440/#447, fork count 47, batch-56). PR #461 completed the post-#460 refresh but included a dashboard test update, so the default-branch guard could not classify the merge as the expected status/dashboard-only lag. This two-file refresh restores the fixed-point shape and keeps the selector on `R-IF-112` plus recurring R-600/R-IF lanes. |
| 2026-06-09 | **Post-#460 terminating refresh — PR #460 R-CXA-1 AS→IS edge-scope/sidecar closeout merged at `b9a9ec87`; §12.2 owed follow-on.** | Hash `39009757fd12` → `b958e3438c97` (state at `b9a9ec87`, open PRs #440/#447, fork count 47, batch-56). R-CXA-1 is now merged closed; the live substitution ledger is 54/54 RETIRED and 54/54 pipeline-advanced. Next selector moves off CXA build-close work to this terminating refresh, then the post-substitution governance/process frontier (`R-IF-112` if operator selects a new substantive arc; recurring R-600/R-IF lanes by cadence). |
| 2026-06-09 | **Substantive R-CXA-1 AS→IS edge-scope/sidecar branch opened from `22d7d53`; §12.2 owed after merge.** | Hash `ffe9044984c8` → `39009757fd12` (branch base `22d7d53`, open PRs #440/#447, fork count 47, batch-56). Filed `.harness/r-cxa-1-as-is-edge-audit-2026-06-09.md`, threaded the stage-5 procedural-tier resolver into the production `RuntimeAsIsWiring` secret-fetch callback, and moved H_T-CXA-1 from PARTIAL to SUBSTANTIVE_RETIRED. Live ledger is now 54/54 RETIRED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |
| 2026-06-09 | **Post-#458 terminating refresh — PR #458 R-CXA-1 scoped secret-fetch producer merged at `d236480`; §12.2 owed follow-on.** | Hash `53da62ac5c58` → `ffe9044984c8` (state at `d2364808`, open PRs #440/#447, fork count 47, batch-55). R-CXA-1 must_pass #1 is closed by the workflow-time secret-fetch producer; H_T-CXA-1 remains PARTIAL pending must_pass #2 / AS→IS edge-scope back-flow audit. |
| 2026-06-09 | **Substantive R-CXA-1 scoped secret-fetch producer branch opened from `b7719262`; §12.2 owed after merge.** | Added a workflow-time `ToolContract.required_secrets` producer at `RuntimeToolDispatcher`, injected the production AS→IS emitter through stage-5 factory construction, and made GCP Secret Manager the non-hollow rotation-metadata source while keyring/env audit metadata fails closed. Focused runtime/config/bootstrap tests pass. This closes R-CXA-1 must_pass #1; H_T-CXA-1 remains PARTIAL pending the separate must_pass #2 / AS→IS edge-scope back-flow audit. |
| 2026-06-09 | **Post-#456 terminating refresh — PR #456 R-CXA-2 bounded-residual back-flow merged at `423730f`; §12.2 owed follow-on.** | Hash `fd41807f1782` → `53da62ac5c58` (state at `423730f`, open PRs #440/#447, fork count 47, batch-55). R-CXA-2 is closed as counted bounded residual; durable/journaled recovery remains an explicit post-MVP re-open trigger. Next action is R-CXA-1 scoped secret-fetch producer grounding/build. |
| 2026-06-09 | **Substantive R-CXA-2 bounded-residual back-flow branch opened from `597ffb96`; §12.2 owed after merge.** | Filed batch-55, moved H_T-CXA-2 from STILL_BOUNDED to counted BOUNDED_RESIDUAL, updated the substitution ledger/tests/dashboard/register, and set the selector to R-CXA-1. Durable/journaled recovery remains an explicit post-MVP re-open trigger, not a claimed substantive production loop. |
| 2026-06-09 | **Post-#454 terminating refresh — PR #454 R-CXA-2 provider-turn HITL continuation merged at `daf5d42`; §12.2 owed follow-on.** | Hash `1ffec49abd91` → `fd41807f1782` (state at `daf5d42`, open PRs #440/#447, fork count 47, batch-54). R-CXA-2 advanced by production-bound Anthropic non-memory provider-turn HITL continuation through `ctx.hitl_tool_loop`, but remains STILL-BOUNDED until durable/journaled recovery caller evidence supports retirement accounting or receives a bounded-residual disposition. Next action remains R-CXA-2 durable recovery evidence/gate, then R-CXA-1 scoped secret-fetch producer. |
| 2026-06-09 | **Substantive R-CXA-2 provider-turn HITL continuation branch opened from `c9e9dc7`; §12.2 owed after merge.** | Wired Anthropic non-memory `tool_use` responses through the bound stage-5 `ctx.hitl_tool_loop`, then returned `tool_result` continuation messages to the provider. Stage 5 now constructs the ask surface, TOOL_STEP dispatcher, and R-CXA-2 producer loop before the frozen LLM dispatcher so the production firing site is bound at construction. Focused tests prove dispatcher continuation, R-CXA-2 loop factory behavior, memory-tool path preservation, and bootstrap smoke. R-CXA-2 remains STILL-BOUNDED pending durable/journaled recovery caller evidence or a bounded-residual disposition. |
| 2026-06-09 | **Post-#452 terminating refresh — PR #452 R-CXA-2 stage-5 producer-loop composition merged at `c8f47d7`; §12.2 owed follow-on.** | Hash `d847e1c6e657` → `1ffec49abd91` (state at `c8f47d7`, open PRs #440/#447, fork count 47, batch-54). R-CXA-2 advanced by bound stage-5 HITL/recovery producer loops and direct CP→IS emission proof, but remains STILL-BOUNDED until generic provider-turn HITL continuation/journaling and durable recovery caller evidence support retirement accounting, or those concerns receive a bounded-residual disposition. Next action remains R-CXA-2 caller-boundary closure/gate, then R-CXA-1 scoped secret-fetch producer. |
| 2026-06-09 | **Substantive R-CXA-2 stage-5 producer-loop composition branch opened from `9e9b1bb`; §12.2 owed after merge.** | Bound `RuntimeHITLToolLoop` and `RuntimeEngineRecoveryLoop` into stage-5/bootstrap via `HarnessContext`; proved direct CP→IS emissions through the bound runtime context (`cp.hitl-tool-call-rewriting`, `cp.pause-captured`, `cp.resume-attempted`) plus full bootstrap field coverage. R-CXA-2 remains STILL-BOUNDED until generic provider-turn HITL continuation/journaling and durable recovery caller evidence support retirement accounting. |
| 2026-06-09 | **Post-#450 fixed-point refresh — PR #450 post-#449 roadmap/dashboard refresh merged at `05ca1f7`; §12.2 owed follow-on.** | Single-source status/dashboard refresh per §12.2. Hash `b322673a66df` → `d847e1c6e657` (state at `05ca1f7`, open PRs #440/#447, fork count 47, batch-54). No roadmap selector change beyond #450: next action remains R-CXA-2 Slice 4/5 composition/closeout, then R-CXA-1 scoped secret-fetch producer. |
| 2026-06-09 | **Post-#449 terminating refresh — PR #449 R-CXA-2 producer primitives merged at `60b52ba`; §12.2 owed follow-on.** | Status/dashboard refresh records the new R-CXA-2 boundary. Hash `abecc765168a` → `b322673a66df` (state at `60b52ba`, open PRs #440/#447, fork count 47, batch-54). R-CXA-2 is advanced by U-CP-78 Reading A plus HITL tool-loop and engine recovery-loop runtime primitives, but remains STILL-BOUNDED until stage-5/bootstrap composition binds the producers and e2e closeout evidence supports accounting. Next action: R-CXA-2 Slice 4/5 composition/closeout, then R-CXA-1 scoped secret-fetch producer. |
| 2026-06-08 | **Post-#437 terminating refresh — PR #437 R-CXA producer-loop fork ratification merged at `e61778b`; §12.2 owed follow-on.** | Single-file status/dashboard refresh per §12.2.1. Hash `0896853874a1` → `0bc3ffc19a00` (state at `e61778b`, PRS empty/unavailable, fork count 47, batch-54). Next action unchanged from the ratification merge: R-CXA-1 scoped secret-fetch producer implementation planning/build; R-CXA-2 remains STILL-BOUNDED with hollow wiring disallowed. |
| 2026-06-08 | **R-CXA-1/2 producer fork ratification arc (substantive; NOT a terminating refresh).** Ratified `class_1_fork_u_cp_78_pause_captured_type_impedance.md` as Reading A and `class_2_fork_r_cxa_2_producer_loop_ownership.md` as DP-1(c), DP-2(c), DP-3(a). Updated `.harness/r-cxa-1-2-producer-seam-spec.md` from awaiting-ratification to ratified-decisions. | Substantive PR → §12.2 owes a follow-on terminating `ops: roadmap status refresh`. Next action changes from fork ratification to R-CXA-1 scoped secret-fetch producer implementation planning/build, while R-CXA-2 remains STILL-BOUNDED on future upstream loop authoring with hollow wiring explicitly disallowed. |
| 2026-06-08 | **Post-#434/#435 terminating refresh — PR #434 R-CXA-1/2 producer-seam spec merged at `83ae8f2`; PR #435 optimize-claude-md eval hardening merged at `e3162c6`; §12.2 owed follow-on.** | Single-file status/dashboard refresh per §12.2.1. Hash `b05aae3c1d4c` → `0896853874a1` (state at `e3162c6`, PRS empty/unavailable, fork count 47, batch-54). Next action unchanged: `R-CXA-2` then `R-CXA-1`, now gated on ratifying the two new producer-loop fork docs before implementation planning. |
| 2026-06-08 | **R-CXA-1/2 producer-seam DESIGN/back-flow arc (substantive; NOT a terminating refresh).** Filed `.harness/r-cxa-1-2-producer-seam-spec.md` (3 producer families bucket-classified) + 2 forks (`class_1_fork_u_cp_78_pause_captured_type_impedance.md` Class 1 defect; `class_2_fork_r_cxa_2_producer_loop_ownership.md` Class 2 DP-1/2/3). Producer gaps SPECIFIED-not-implemented; R-CXA-1/2 stay OPEN. Design-phase posture (`.harness/**` + `Project_Roadmap_v1.md` only; zero `design-substrate/**` / `harness-*/src/**`). | Substantive PR → §12.2 owes a follow-on **terminating** `ops: roadmap status refresh`: recompute hash, bump `open_fork_doc_count` 45 → 47, regenerate `tools/dashboard/roadmap.html`. Next action unchanged (`R-CXA-2` then `R-CXA-1`) — now gated on **fork ratification** rather than producer discovery. |
| 2026-06-08 | **Post-#432 terminating refresh — PR #432 R-IF-council-workflow productization merged at `a0a8393`; §12.2 owed follow-on.** Same session: materialized the `.harness/council/` command targets, added the generic same-shape YAML, refreshed the harness-aware YAML/prose companion, and marked the roadmap row resolved. | Single-file status/dashboard refresh per §12.2.1. Hash `747d5b1264e8` → `b05aae3c1d4c` (state at `a0a8393`, PRS empty/unavailable, fork count 45, batch-54). Next action unchanged: `R-CXA-2` then `R-CXA-1`, both producer-gated and not safely wireable until real upstream producers or design/back-flow amendments appear. |
| 2026-06-08 | **Post-#430 terminating refresh — PR #430 R-901 research-path hygiene merged at `263c2d78`; §12.2 owed follow-on.** Same session: corrected the R-901 brief location from the obsolete top-level `research/` path into `.harness/01-planning/01-harness-planning/00-harness-research/`, updated R-900/R-901 metadata, and fixed R-600 NotebookLM extract pointers. | Single-file status/dashboard refresh per §12.2.1. Hash `59ece3c52b50` → `747d5b1264e8` (state at `263c2d78`; PRS empty; fork count 45; batch-54). Next action unchanged: `R-CXA-2` then `R-CXA-1`, both producer-gated and not safely wireable until real upstream producers or design/back-flow amendments appear. |
| 2026-06-08 | **Post-#428 terminating refresh — PR #428 R-901 retirement-criteria brief merged at `04348e3`; §12.2 owed follow-on.** Same session: R-901 filed the Phase-9 brief, marked Surface X research-only resolved, removed R-901 from remaining dashboard cards, and returned the selector to genuine producer-gated CXA residuals. | Single-file status/dashboard refresh per §12.2.1. Hash `ed3d997f21f7` → `59ece3c52b50` (state at `04348e3a`; PRS empty; fork count 45; batch-54). Follow-up path hygiene moved the brief into `.harness/01-planning/01-harness-planning/00-harness-research/` to match the reorganized harness research corpus. Next action: `R-CXA-2` then `R-CXA-1`, both producer-gated and not safely wireable until real upstream producers or design/back-flow amendments appear. |
| 2026-05-31 | Dashboard creation (v1 origin) | n/a |
| 2026-05-31 | First post-merge refresh — PR #112 merged at `7f3e6ce`; dashboard hash recomputed `9c31e4978c3d` → `5a077d17765f` | Refreshed via PR #113 per CLAUDE.md §12.2. |
| 2026-05-31 | Recursion-stop discipline gap surfaced — PR #113 merge left dashboard stale by 1 commit; §12.2 as written would recurse | Codified at PR #114 via §12.2.1 termination clause + §12.1 step 6 carve-out. |
| 2026-05-31 | First terminating refresh per §12.2.1 — PR #114 merged at `7da53e5`; this PR is the terminating event | Single-file dashboard-only refresh; does NOT trigger another refresh per §12.2.1. Hash `5a077d17765f` → `9bd06cb83b73`. |
| 2026-05-31 | Second terminating refresh — PR #116 bundled change merged at `63c4464`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `9bd06cb83b73` → `12f34637fd3a`. |
| 2026-05-31 | Third terminating refresh — PR #118 NotebookLM skill setup merged at `65ed646`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `12f34637fd3a` → `47c763cb4fb3`. |
| 2026-05-31 | Fourth terminating refresh — PR #120 NotebookLM MCP server supplement merged at `622790c`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `47c763cb4fb3` → `6283a744645e`. |
| 2026-05-31 | Fifth terminating refresh — PR #122 SessionStart audit hook (enforcement layer) merged at `27bfeaf`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `6283a744645e` → `82ce25e90fd5`. From this PR forward, audits auto-fire at every session start. |
| 2026-05-31 | Fresh-session reconciliation — local `main` found 13 commits stale (showed `#106`) vs `origin/main` `#123`; SessionStart hook not yet on local main so no auto-orient | Fast-forwarded local main; reset to `origin/main` `9f1bbf4` after parallel process pushed equivalent post-#122 refresh (#123); identical-tree sibling discarded. No divergence created. |
| 2026-05-31 | Sixth terminating refresh — PR #108 R-IF-108 audit merged at `54337c1`; `[ROADMAP DRIFT] action=§12.3` flagged by on-main hook; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `82ce25e90fd5` → `a9f8cc1bd85d`. R-IF-108 closed; next action recomputed to R-IF-109. |
| 2026-05-31 | Seventh terminating refresh — PR #109 R-IF-109 memory audit merged at `a81fe2d`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `a9f8cc1bd85d` → `25d4b36d4359`. R-IF-109 closed; next action recomputed to R-IF-110 (first design-phase posture entry in chain). |
| 2026-05-31 | Eighth terminating refresh — PR #110 R-IF-110 CXA v2.18 (design-phase) merged at `2f14604`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `25d4b36d4359` → `17fe5dac2209`. R-IF-110 closed; next action recomputed to R-IF-111 (last in-flight entry). Downstream `CLAUDE.md` §1.1 count cascade (107→105) flagged as owed hygiene arc per CXA v2.18 §0.9 FM-2 deferral. |
| 2026-05-31 | Ninth terminating refresh — PR #111 R-IF-111 OD plan v2.27 (design-phase) merged at `fdf120b`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `17fe5dac2209` → `a51d65ed22a2`. R-IF-111 closed — **in-flight chain #108→#109→#110→#111 fully cleared this session**. Next action recomputed to the owed hygiene cascade (CLAUDE.md §1.1 aggregate 107→105 + harness-od refresh), R-002 fallback after. |
| 2026-05-31 | Tenth terminating refresh — PR #128 R-IF-HYGIENE-CXA-COUNT-CASCADE merged at `3ba4fae`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `a51d65ed22a2` → `adb1e71359dd`. Hygiene cascade closed (canonical pointers now read 105). **All in-flight + deferred-hygiene work cleared this session.** Next action recomputed to **R-002** (substantive phase-7 substitution-retirement survey — recommend a fresh decision before opening). |
| 2026-05-31 | Eleventh terminating refresh — PR #130 R-002 Surface-I decomposition merged at `6f8dd66`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `adb1e71359dd` → `7f251d6c7cc5`. R-002 RESOLVED (7 non-RETIRED rows mapped to R-NNN; R-001/R-004 reconciled stale→RESOLVED). Next action recomputed to **R-003** (producer-site lifts, ~7 sites; substantive phase-7 cross-axis impl — recommend fresh decision + advisor before opening). **Survey-surfaced finding:** the roadmap §5.2 was authored 2026-05-31 with R-001/R-004 as RETIRE-READY-awaiting-deployment, but OD-5/AS-8d had been RETIRED via mech-β at batches 31-32 (2026-05-28) — a 3-day-stale authoring error, now corrected. |
| 2026-05-31 | Twelfth terminating refresh — PR #132 R-003 orientation-checkpoint merged at `7c79fb4`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `7f251d6c7cc5` → `754826ddff19`. Next action UNCHANGED at **R-003** but now **ACTIVE-ORIENTATION-COMPLETE** with a resume pointer to `.harness/R-003-checkpoint.md` — a fresh session (`/clear` → hook → `continue`) resumes at Cluster A without re-deriving. Demonstrates the **stop-short seamless-resume** discipline: a paused phase-7 entry persists its orientation as a checkpoint artifact + entry sub-status + dashboard pointer. |
| 2026-05-31 | Thirteenth terminating refresh — PR #134 roadmap §3 schema amendment merged at `f8572b3`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `754826ddff19` → `1c852278bfc9`. Next action UNCHANGED at **R-003 Cluster A**. §3 now declares the `resume:` field + `satisfied:<date>` advisor value + checkpoint-on-pause rule (status enum kept closed); R-003 entry reconciled to schema-legal `ACTIVE`. Closes the §3↔§5 drift the operator caught (3 off-schema constructs landed at #132 before the schema sanctioned them — schema-first discipline restored). |
| 2026-05-31 | **R-003 RESOLVED** — Cluster A (#136 `5e4a112`) + Cluster B (#137 `c339728`) merged; substantive roadmap PR (this) marks R-003 → RESOLVED + R-001-h-t-is-2-retired BLOCKED → ACTIVE | Bundled change (Project_Roadmap_v1.md + dashboard) → NOT a terminating refresh per §12.2.1; title drops the `ops: roadmap status refresh` prefix; **follow-on terminating refresh owed**. Hash `1c852278bfc9` → `03714383e971` (state at `c339728`, pre-this-PR-merge). Next action re-derived: **R-001-h-t-is-2-retired** (IS-2 producer cascade complete; file the PARTIAL → RETIRED retirement event). |
| 2026-05-31 | Fourteenth terminating refresh — PR #138 substantive R-003-close refresh merged at `a943caf`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `03714383e971` → `3111061f3b6b` (state at `a943caf`). Next action UNCHANGED at **R-001-h-t-is-2-retired**. This is the recursion-stopping fixed point for the R-003 close: dashboard now lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-05-31 | **Fresh-session reconciliation (Cluster A/B drift) + R-001 close** — a `/clear`→`continue` session's hook reported `next=R-003` though R-003 (Clusters A+B) was already shipped+merged (#136–#139); the primary checkout's local `main` was 4 commits behind `origin/main` and the hook never fetched (2nd instance of the line-92 class). | Fixed via `git merge --ff-only origin/main` (clean ff, no divergence); **hardened the hook** with a default-branch behind-origin guard (**PR #140** `e55e99b`). Then executed the true next-action: **R-001-h-t-is-2-retired RESOLVED** at **PR #141** `4a0aa1d` (H_T-IS-2 PARTIAL → RETIRED, batch-50; IS-axis 9/9 = 100%, FIRST axis fully RETIRED). This PR marks R-001 RESOLVED + reconciles stale-ACTIVE R-IF-108/109/110/111 → RESOLVED (confirmed merged) + logs this event. Substantive (roadmap + dashboard) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. Hash `1c852278bfc9` → `8780c87985f2` (state at `4a0aa1d`, pre-this-PR-merge). Next action re-derived: **R-200-ci-pytest-pyright-ruff-matrix** (§III rank 3 — §I drained, §III outranks §II). |
| 2026-05-31 | Fifteenth terminating refresh — PR #142 substantive R-001-close merged at `5ca46f5`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `8780c87985f2` → `ec4f50af797b` (state at `5ca46f5`). Next action UNCHANGED at **R-200-ci-pytest-pyright-ruff-matrix**. Recursion-stopping fixed point for the R-001 close: dashboard now lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-05-31 | **R-200-ci-coverage-gating RESOLVED** + sixteenth terminating refresh — substantive close PR #151 merged at `51f4131` (blocking `pytest` green; advisory od/pyright/ruff legs red-as-expected); this PR is the owed follow-on terminating refresh per §12.2.1 | Single-file dashboard-only refresh. Hash `b0d5c85c1136` → `e30b9dd81864` (state at `51f4131`). Next action UNCHANGED at **R-200-ci-od-cp-dependency-leak** (re-derived at PR #151; mode-agnostic §III drained → §III phase-7 entries next). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-05-31 | **R-200-ci-od-cp-dependency-leak RESOLVED** (PR #153, substantive) — od axis-isolation leg was advisory due to undeclared OD→CP + OD→IS consumer deps; advisor-gated resolution = option (a) declare the deps | Bundled (touches `Project_Roadmap_v1.md`) → NOT a terminating refresh; follow-on owed. `harness-od/pyproject.toml` declares `harness-cp` (canonical OD→CP §2.3.3; forcing consumer = `ReplayDisposition` read-only at `idempotency_join_dedup.py`) + `harness-is` (OD→IS §2.3.4; tests); both acyclic-safe. Broadened grep caught the `harness_is` 2nd dep a cp-only grep would miss. od leg green in isolation + in CI as blocking (887 passed) + `continue-on-error` carve-out dropped → full 6-leg matrix blocks. Corrected the "reverse-direction / relocate the seam" framing (OD→CP is canonical) at `.harness/class_3_drift_od_cp_undeclared_dependency.md`. Hash left stale `e30b9dd81864` (recompute owed at the post-merge terminating refresh). Next action re-derived: **R-200-ci-lint-typecheck-blocking** (§III phase-7, sole remaining ACTIVE §III; outranks §II MVP). |
| 2026-05-31 | Seventeenth terminating refresh — PR #153 substantive R-200-ci-od-cp-dependency-leak close merged at `4d0914d`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e30b9dd81864` → `7fe422eb023c` (state at `4d0914d`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-05-31 | Eighteenth terminating refresh — PR #155 ruff mechanical sweep (R-200-ci-lint-typecheck-blocking **progress**, not a close) merged at `f1f9a52`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `7fe422eb023c` → `e077fe676860` (state at `f1f9a52`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — sweep dropped ruff 366 → 136 + cleaned format tree-wide, zero regression; manual ruff + pyright + flip-blocking remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-05-31 | Nineteenth terminating refresh — PR #157 F821 real-defect fixes (R-200-ci-lint-typecheck-blocking **progress**, not a close) merged at `ce211ee`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e077fe676860` → `8070fa6466c7` (state at `ce211ee`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — 3 F821 real defects fixed, ruff 136 → 133, F821 tree-wide now 0; 133 manual ruff residual + ~894 pyright + flip-blocking remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-05-31 | Twentieth terminating refresh — PR #159 lint-half close (R-200-ci-lint-typecheck-blocking **lint half closed**, entry stays ACTIVE) merged at `1b87f08`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `8070fa6466c7` → `72ce1ef25437` (state at `1b87f08`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — `ruff check .` + `ruff format --check .` clean tree-wide; `lint` CI job flipped to blocking + verified green-as-blocking; ~894 pyright + `typecheck`-flip remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-05-31 | **R-200-ci-lint-typecheck-blocking RESOLVED** (PR #161 pyright-half close, substantive) — `uv run pyright` 846 → 0 tree-wide; `typecheck` job flipped `continue-on-error` → blocking (renamed `pyright (strict) — blocking`), verified green-as-blocking in CI with all 6 axis-isolation legs + ruff + pytest | Bundled (touches `Project_Roadmap_v1.md` §5 status ACTIVE → RESOLVED) → NOT a terminating refresh; follow-on owed. Approach: test dirs scoped via `[tool.pyright.executionEnvironments]`; src real-fixed incl. the named two-`Skill`-class bug (empty `types.Skill` stub re-exported as concrete `lifecycle.skills.Skill`; same `LedgerWriter`; `HarnessMCPServer` kept-as-stub — Pydantic-forward-ref regression caught + reverted mid-flight) + `AuditLedgerWriter.append` Protocol completion + mcp `streamablehttp_client`→`streamable_http_client` deprecation rename. 3543 passed / 0 regression. cp+od axis-isolation legs run locally (813 / 887 passed). Hash `72ce1ef25437` → `c0c447454969` (state at `d904055`, pre-this-PR-merge). **§III CI substrate gate COMPLETE** (both lint + typecheck blocking). Next action re-derived: **R-100-mvp-operator-usable-cli-shipped** (§II MVP rank 4 — §III drained, §I drained, §II is next priority tier with a dependency-met ACTIVE entry). |
| 2026-05-31 | Twenty-first terminating refresh — PR #162 bundled R-200-close merged at `149c9a0`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `c0c447454969` → `038c75a5245e` (state at `149c9a0`). Next action UNCHANGED at **R-100-mvp-operator-usable-cli-shipped** (§II MVP — §III CI substrate gate complete). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-05-31 | **Fresh-session audit + R-100 close** — `/clear`→`continue` session: §12.1 audit clean (local main 3 behind origin; ff'd to `fcfa568` #163, a terminating refresh whose dashboard hash = `compute(149c9a0)` = expected lag-by-one fixed point per §12.1 step 6; no new refresh PR). Derived next-action R-100-mvp-operator-usable-cli-shipped; use-the-product probe surfaced 4 operator-blocking gaps → shipped scaffolding + fixes + operator-authorized live green at **PR #164** `44c668d`. | Substantive R-100 close (PR #164 touches Project_Roadmap_v1.md: R-100 ACTIVE → RESOLVED + NEW R-100-mvp-config-discovery) → NOT a terminating refresh; **follow-on terminating refresh owed**. Hash `038c75a5245e` → recomputed below. Next action re-derived: **R-100-mvp-real-workflow-execution** (R-100 RESOLVED unblocks it; was BLOCKED → ACTIVE). |
| 2026-05-31 | Twenty-second terminating refresh — PR #164 bundled R-100-close merged at `44c668d`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `038c75a5245e` → `48e2fffa4434` (state at `44c668d`; open_fork_doc_count 39 → 40 for the NEW discovery fork). Next action UNCHANGED at **R-100-mvp-real-workflow-execution**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **R-100-mvp-real-workflow-execution PARTIAL + 2 forks** — same `/clear`→`continue` session: authored the real multi-step e2e (PR #166 `97c8943`). AC #1 + #3 PASS via 2 live operator-authorized Anthropic runs; AC #2 + #4 surfaced as Class 1 forks (converter gap + cost-not-firing w/ OD-5 retirement-validity question). | Substantive (PR #166 touches Project_Roadmap_v1.md: entry → ACTIVE/partial + NEW R-100-tool-step-converter + R-100-cost-attribution-firing) → NOT a terminating refresh; **follow-on terminating refresh owed**. Hash `48e2fffa4434` → recomputed below. Next action re-derived: **operator ratification of the 2 PROPOSING forks** (all §II forward entries now fork-gated; §VII process-discipline is the executable fallback). |
| 2026-06-01 | Twenty-third terminating refresh — PR #166 substantive R-100-real-workflow PR merged at `97c8943`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `48e2fffa4434` → `0deb499daab5` (state at `97c8943`; open_fork_doc_count 40 → 42 for the 2 NEW R-100 forks). Next action = **operator fork-ratification** (see Next action section). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **Fork ratification cluster** — same `/clear`→`continue` session: grounded + ratified both R-100-mvp-real-workflow-execution forks. Cost fork → RESOLVED-AS-INVALID (PR #168 `a9ee48d`; live instrumentation flipped it — test-observation bug, cost fires+writes, OD-5 valid; AC #4 test fixed → PASSES). Converter fork → RATIFIED-AS-READING-B (PR #169 `5431ed1`; operator AskUserQuestion). | Two substantive PRs (#168 touches roadmap+test; #169 touches roadmap+fork doc) → NOT terminating refreshes; this is the **owed follow-on terminating refresh** covering #167→#169. Hash `0deb499daab5` → `35586f56e497` (state at `5431ed1`). R-100-mvp-real-workflow-execution now 3/4 ACs PASS; next action re-derived: **apply Reading B at R-100-tool-step-converter** (design-phase posture spec+impl arc; confirm posture per §11.6). |
| 2026-06-01 | Twenty-fourth terminating refresh — fork-ratification cluster #168 + #169 merged; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash recompute `0deb499daab5` → `35586f56e497` (state at `5431ed1`; fork count 42 unchanged — #168/#169 flipped fork Status, added no fork files). Next action = **R-100-tool-step-converter apply arc (Reading B)**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **R-100 tool-step apply cluster landed** — `/clear`→`continue` session: operator approved (AskUserQuestion) merging the 3 ready green PRs in stack order #173 → #171 → #172. #172 (stacked on #171) was rebased onto main after #171's squash-merge diverged its base (standard stacked-PR artifact); 2 doc conflicts (CLAUDE.md runtime-spec row + roadmap status) resolved to #172's superset, §13 preserved. All blocking CI green at each merge. | Three substantive PRs (their own merges); **this is the owed follow-on terminating refresh** (Twenty-fifth) covering #170→#172. Dashboard-only → terminating per §12.2.1. Hash `35586f56e497` → `be74ce84cf0f` (state at `492d327`; fork count 42 → 39 recount). `R-100-tool-step-converter` APPLIED (#171) + `R-100-tool-step-sandbox-resolver` APPLIED-PENDING-OPERATOR-E2E (#172). Next action = **operator-gated R-100 MVP live e2e run** (AC #2 + CLI smoke + real-workflow close only by a live LLM run; not auto-fireable). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-600-clearance-marker-backfill-survey RESOLVED** (PR #175 `60d55ee`) — same session: with §II MVP operator-gated, corrected the §4 board (dirty-primary-checkout had contaminated the derivation) and executed the highest-priority ungated Claude-executable entry. Survey gap-free for all spec/CXA/Workflow + terminal plan markers; 10 intermediate superseded plan versions exempt (terminal-marker-covers-chain). | Substantive (touched roadmap + clearance README) → **this is the owed follow-on terminating refresh** (Twenty-sixth). Dashboard-only → terminating per §12.2.1. Hash `be74ce84cf0f` → `b7767c7cc1c6` (state at `60d55ee`). **`open_fork_doc_count` corrected 39 → 43** — the 39 in PR #174 was computed in the stale/dirty primary checkout (on b220dd5); the hook-recipe glob on clean main = 43. Clean-main §4 re-derivation: §I drained, §III done, §VII R-600-clearance RESOLVED + R-600-pattern-bake-in gated by deferred Workflow v1.14; `R-100-mvp-yaml-loader-shipped` verified done at non-live level (41 tests). Next Claude-executable arc = **R-XI-01 operator dashboard MVP** (trigger met; flip-eligible; outward-facing Pages deploy → teed up pending operator go-ahead). Dominant product next-step stays the operator's live R-100 e2e. Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-XI-01 operator dashboard MVP BUILT** (PR #177 `90604de`) — operator AskUserQuestion green-lit "build now, recommended stack". Generator (5 sources → static `roadmap.html`, Tailwind + Chart.js CDN) + `dashboard-deploy.yml` Pages workflow; verified in Chrome (zero console errors, all 7 sections), ruff clean, dashboard-deploy `build` job green in CI. R-XI-01 PROPOSED→ACTIVE. | Substantive (touched roadmap R-XI-01 status) → **this is the owed follow-on terminating refresh** (Twenty-seventh). Dashboard-only → terminating per §12.2.1. Hash `b7767c7cc1c6` → `8e3c6e4653af` (state at `90604de`; fork count 43 unchanged). **All Claude-executable substantive arcs now drained** — remainder is operator-gated (live R-100 e2e; enable GitHub Pages → closes R-XI-01) or blocked (R-XI-02/03 need R-XI-01 RESOLVED; pattern-bake-in gated by Workflow v1.14). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **fork-doc Status audit** (PR #179 `e821d74`) — audit-owed maintenance sweep of all 43 open fork docs; corpus current; 1 stale cross-ref refreshed at `cp_spec_section_25_contract_id_collision`; new recurring-audit log `.harness/fork_doc_status_audit_log.md`. | Substantive (touched fork doc + new log) → **this is the owed follow-on terminating refresh** (Twenty-eighth). Dashboard-only → terminating per §12.2.1. Hash `8e3c6e4653af` → `94fc7eff646a` (state at `e821d74`; `open_fork_doc_count` 43 unchanged — audit refreshed a Status line, closed no fork). Next action UNCHANGED — all Claude-executable substantive arcs drained; remainder operator-gated (live R-100 e2e; enable GitHub Pages). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-100 AC#2 closed via free local ollama** (PR #181 `39fdcbf` test ollama-branch fix + PR #182 `8529a45` close) — `R-100-mvp-real-workflow-execution` (4/4) + `R-100-tool-step-sandbox-resolver` → RESOLVED. AC#2 (TOOL_STEP via `api.run`) green against a live local ollama daemon (zero-token ping; echo-MCP dispatch, not inference — no paid call). | Substantive (test fix + roadmap §5 + fork doc) → **this is the owed follow-on terminating refresh** (Twenty-ninth), covering BOTH #181 and #182. Dashboard-only → terminating per §12.2.1. Hash `94fc7eff646a` → `dc6807a02340` (state at `8529a45`; `open_fork_doc_count` 43 unchanged). **Next action CHANGED**: R-100 MVP real-workflow + tool-step RESOLVED; `R-100-mvp-yaml-loader-shipped` flips BLOCKED → ACTIVE (dep resolved) and becomes the top §II Claude-executable candidate (close via an ollama-enable-then-verify arc on the track-B e2e, mirroring the AC#2 fix). **R-XI-01 reclassified**: GitHub Pages is BLOCKED at the account-plan level (private + free plan; HTTP 422 from `POST /pages`) — needs GitHub Pro or a public repo, NOT a Settings toggle. Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-XI-01 closed — dashboard live on Pages** (PR #184 `5984cda`) — operator made the repo public; I enabled Pages (`build_type=workflow`) + triggered `dashboard-deploy` (build+deploy GREEN, run `26750144964`); live at https://thestoryportal.github.io/arhugula-harness/ (HTTP 200). | Substantive (roadmap §5 R-XI-01 → RESOLVED) → **this is the owed follow-on terminating refresh** (Thirtieth). Dashboard-only → terminating per §12.2.1. Hash `dc6807a02340` → `91ff69664956` (state at `5984cda`; fork count 43 unchanged). Next action: top §II Claude-executable candidate remains `R-100-mvp-yaml-loader-shipped` (ACTIVE, free ollama-enable arc); R-XI-02/R-XI-03 now unblocked (PROPOSED, iteration-2). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **`just dashboard` recipes added** (PR #186 `da45c29`) — local view of the operator dashboard (`just dashboard` generate+open / `just dashboard-serve [port]` generate+serve on localhost). justfile only. | Substantive (justfile tooling, non-refresh) → **this is the owed follow-on terminating refresh** (Thirty-first). Dashboard-only → terminating per §12.2.1. Hash `91ff69664956` → `b93c17d379dc` (state at `da45c29`; fork count 43 unchanged). No roadmap-state change (no R-NNN closed); next-action UNCHANGED (`R-100-mvp-yaml-loader-shipped` top §II candidate). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-100-mvp-yaml-loader-shipped RESOLVED** (PR #188 `e3f99a8`) — YAML↔TOML load+dispatch equivalence closed at the CI/deterministic level (strengthened load test for byte-equivalent payload + NEW deterministic dispatch-equivalence test; no key/ollama/daemon). §II R-100 MVP surface now fully RESOLVED. | Substantive (test code + roadmap §5) → **this is the owed follow-on terminating refresh** (Thirty-second). Dashboard-only → terminating per §12.2.1. Hash `b93c17d379dc` → `ca30acdc6032` (state at `e3f99a8`; fork count 43 unchanged). **Next action CHANGED**: yaml-loader RESOLVED → `R-100-mvp-multi-workflow-fixture-suite` flips BLOCKED → ACTIVE as the next §II candidate (scope first — MVP materializes only SINGLE_THREADED_LINEAR, so the "all 6 topologies" scope is partly gated). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-600-pattern-bake-in-sweep EXECUTED → ACTIVE-SURVEYED** (this PR) — fresh `/clear`→`continue` session; §12.1 audit clean (`lag-expected` fixed point: dashboard `c6c79d0515e2` = compute(#194/`18b2d5d`) = compute(#195~1); recomputed to compute(main `d7574b3`) = `82afe95f7974`). Executed the hook-derived next action (`R-600-pattern-bake-in-sweep`): read-only memory-store sweep → 104 cardinality-≥2 `[[pattern]]` tokens; survey at `.harness/R-600-pattern-bake-in-sweep.md`. | Substantive (survey artifact + roadmap §5.6 entry → `resume:` pointer + ACTIVE-SURVEYED note) → **NOT a terminating refresh; follow-on terminating refresh owed** per §12.2.1. Hash `c6c79d0515e2` → `82afe95f7974` (state at `d7574b3`, pre-this-PR-merge; recompute owed at the post-merge terminating refresh). **Finding: ZERO new §7.4.7.2 sub-species owed** (strike-revision already at v1.13); the strong disciplines are all non-§7.4.7-shape → recommend a NEW §7.5/§7.4.7.X catalogue at the DEFERRED `R-600-workflow-v1-14-amendment`. Next action re-derived: MEMORY.md hygiene audit (motivated by survey §3 dups + over-cap index), then fork-doc Status cadence sweep; dominant product step stays the operator-gated R-100 live e2e. |
| 2026-06-01 | **Fresh `/clear`→`continue` session: merged PR #196 + ran the MEMORY.md hygiene audit** — §12.1 audit clean (`lag-expected` fixed point recognized; survey was committed at `f7613f3`/PR #196 OPEN). Merged #196 (`eed6a6d`, clean+green, process-substrate). Then executed the survey-§3 follow-on — the MEMORY.md hygiene audit — correcting two stale survey claims on empirical grounding (index NOT over-cap at 23,199<24,400 B; the "slug dups" are external frozen-change-note casing mismatches, not memory-file dups) and applying the genuine fixes (−2 redundant `(#)` placeholder index lines, +1 orphan `phase-1-research-corpus` line) to the global memory store. This is the owed follow-on **Thirty-third terminating refresh** for #196. | Dashboard-only → terminating per §12.2.1. Hash `82afe95f7974` → `a82a9a13881b` (state at `eed6a6d`, pre-this-PR-merge; PRS empty). Next action UNCHANGED — all Claude-executable substantive arcs drained; next mode-agnostic = fork-doc Status cadence sweep (43 open). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **fork-doc Status cadence sweep (PR #198 `4ecc208`)** — same session: ran the next named Claude-executable mode-agnostic arc (last pass #178, 18 PRs elapsed). Delegated read-sweep of all 43 fork docs vs. merges #181–#196; **corpus current — ZERO stale Status lines** (43/43 accurate). No fork-doc edit; appended the dated audit-pass entry + reset the ~5-PR cadence clock. This is the owed follow-on **Thirty-fourth terminating refresh** for #198. | Dashboard-only → terminating per §12.2.1. Hash `a82a9a13881b` → `4a0e2d4647c0` (state at `4ecc208`, pre-this-PR-merge; PRS empty). **All recurring Claude-executable mode-agnostic maintenance arcs drained this session** (R-600 survey #196 + MEMORY.md audit + fork-doc sweep #198); remainder operator-owned (live R-100 e2e; design-phase v1.14 amendment; R-XI-02/03). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-700-closure-accounting-draft RESOLVED + dashboard aligned (PR #207 `d1eaf75`), then this terminating refresh.** Fresh `/clear`→`continue` session resumed the paused R-700 arc: produced `.harness/R-700-phase-8-closure-accounting-draft.md` (Part A 54-row substitution log + Part B 8-entry beyond-substitutions register); KEY FINDING — the published `48/54 RETIRED / 49 pipeline` is internally impossible (3 PARTIAL: OD-4 + CXA-1 + CXA-4); per-row reconciliation 46–47 RETIRED; true open set 8 rows; root cause = CXA never folded into the cumulative (ledger §11.1a line 278). Aligned the live operator dashboard (retirement 48/54 batch-51 + R-700-aware buckets) + verified deploy. PR #207 squash-merged; dashboard-deploy succeeded; live site verified. | Dashboard-only → terminating per §12.2.1. Hash `4d3120974cef` → `e95a2e943993` (state at `d1eaf75b`, the #207 merge; PRS empty; fork count 43 unchanged; batch-51 latest). Next action UNCHANGED — R-700 draft feeds the BLOCKED `R-700-phase-8-substitution-accounting` (operator owns the final integer + bounded-residual sign-offs); NEW gap surfaced (CXA-1/2/3/4 + CP-17 have no R-NNN entries → recommend an R-002-style Surface-I pass over CXA + CP-17). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-300-multi-llm-routing-activation RESOLVED (PR #213 `c047aa2`), then this audit.** Fresh `/clear`→`continue` session: §12.1 audit oriented (worktree on `r-300-routing-activation`, 3 commits ahead of origin/main; the implementation + memory had already been authored this branch and PR #213 was open + all-green). Merged PR #213 (squash, all 10 checks pass, CLEAN/MERGEABLE) — `infer()` composition seam activated (C-CP-01 §1.1, U-CP-03): CP-pure `route()`+injected-async-dispatch composition + live INFERENCE_STEP routing through `infer()` + full `routing.*` span attribution; DECLARATIVE-echo behavior-preserving at MVP; council gate NOT met this arc (fires at R-300-second-provider). This audit finalizes the §5 `(PR pending)` → `(PR #213, c047aa2)` note + refreshes the dashboard. | Substantive (Project_Roadmap_v1.md §5 + dashboard) → **NOT a terminating refresh; follow-on terminating refresh owed** per §12.2.1. Hash `7646d1d29e5f` → `039ab95024f0` (state at `c047aa2`; PRS empty; fork count 43 unchanged; batch-51 latest). Next action re-derived: R-300-routing RESOLVED drains the top Claude-executable post-Phase-8 impl lever; remaining Claude-executable-now = `R-830-memory-tool-production-backend` + `R-CXA-1/2/4` seams (operator-driven menu picks). Dominant product step stays operator-gated (live R-100 e2e; R-300-second-provider creds). |
| 2026-06-01 | Thirty-fifth terminating refresh — PR #214 substantive R-300-close audit merged at `7a8802b`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `039ab95024f0` → `8fb5be5f6f46` (state at `7a8802b`, the #214 merge; PRS empty; fork count 43; batch-51). Next action UNCHANGED — R-300-routing RESOLVED; remaining Claude-executable-now = `R-830` + `R-CXA-1/2/4` (operator-driven menu); dominant product step = operator-gated live R-100 e2e + R-300-second-provider creds. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **R-100 live e2e fired green (operator-authorized) + `just mvp-r100-real` recipe landed (PR #216 `ed2e827`)**, then this terminating refresh. Same session: operator asked "R-100 e2e" → ran AC#2 TOOL_STEP via live local ollama (free, PASSED 1.17s) + operator authorized the paid half → AC#1/#3/#4 real 3-step Anthropic `claude-haiku-4-5` inference workflow via `api.run` (PASSED 4.41s) through `just -E <root>/.env mvp-r100-real` (no shell secret-sourcing per `[[secrets-via-just-recipe-not-direct-sourcing]]`). Landed the recipe (PR #216, justfile-only). | Thirty-sixth terminating refresh — dashboard-only per §12.2.1. Hash `8fb5be5f6f46` → `e45a515b0dcd` (state at `ed2e827`, the #216 merge; PRS empty; fork count 43; batch-51). `R-100-mvp-real-workflow-execution` already RESOLVED (4/4) — this run re-validated it live; no roadmap-state change. Next action UNCHANGED — Claude-executable post-Phase-8 levers (`R-830`, `R-CXA-1/2/4`) are operator-driven menu picks; `R-300-multi-llm-second-provider` + further live runs operator-gated (creds). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **R-CXA-1 must_pass #1 Class 1 fork FILED (this PR)** — "continue" derivation post-R-300/R-100: grounded the top Claude-executable post-Phase-8 lever (`R-CXA-1-as-is-seam`) before opening. The secret-fetch production-caller edge fires eagerly at bootstrap stage 3a (`stage_3a_cp_clients.py:44`), but the AS→IS emitter materializes at stage 6 CXA_WIRING (`stage_6_cxa_wiring.py:70`) — firing-site-precedes-wiring-stage (U-RT-111 shape). Filed `.harness/class_1_fork_cxa_1_secret_fetch_audit_bootstrap_ordering.md` (PROPOSING; 4 Readings, B recommended; security safe-by-design per `SecretFetchEvent` metadata-only). Operator chose "file the fork doc" at AskUserQuestion. | Bundled (fork doc + roadmap §5 note + dashboard; no design-substrate touch) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. `open_fork_doc_count` 43 → 44. Hash left stale `e45a515b0dcd` — recompute owed at the post-merge terminating refresh. Next action: **operator AskUserQuestion to ratify a Reading** for the fork (then apply arc); meanwhile other Claude-executable levers (R-830 backend class / R-CXA-4) remain operator-driven menu picks. |
| 2026-06-01 | Thirty-seventh terminating refresh — PR #218 bundled R-CXA-1-fork-filing merged at `3885074`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e45a515b0dcd` → `03d7322f983b` (state at `3885074`, the #218 merge; PRS empty; **fork count 44**; batch-51). Next action UNCHANGED — operator AskUserQuestion owed to ratify a Reading for the R-CXA-1 secret-fetch bootstrap-ordering fork; other Claude-executable levers (R-830 backend class / R-CXA-4) remain operator-driven menu picks. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-01 | **R-CXA-1 fork RESOLVED-AS-READING-D (this PR)** — apply arc opened on first-ratified Reading B; pre-substantive empirical orientation + advisor reconcile found it unwritable-as-previewed (bootstrap fetch = `resolve_bootstrap_value(NAME)`; scope-bearing `resolve()` has ZERO production callers; unsourced-field sentinels collapse the secret fingerprint to f(name); idempotency key timestamp-free = fire-once-forever; `SecretFetchEvent` machinery has no real producer — `emit_secret_fetch_audit` composes-and-discards). Operator re-ratified DON'T-WIRE (Reading D). must_pass #1 DEFERRED; R-CXA-1 PARTIAL; ZERO production code (avoided a vacuous LANDED). | Bundled (fork doc Status + §2.6 finding + roadmap §5 note + dashboard; no design-substrate touch) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. `open_fork_doc_count` stays 44 (fork doc APPLIED, not deleted). Hash left stale `03d7322f983b` — recompute owed at the post-merge terminating refresh. Next action: no auto-startable Claude-executable arc remains; R-830 / R-CXA-4 are operator-driven menu picks; live runs + R-300-second-provider creds-gated. **Pattern: advisor-before-substantive-work + use-the-product/grounding caught a vacuous-close before it shipped (2nd "wire-the-caller-is-producer-discovery" instance after U-RT-111).** |
| 2026-06-01 | Thirty-eighth terminating refresh — PR #220 bundled R-CXA-1-fork-resolution merged at `d77647b`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `03d7322f983b` → `42239f9ed02a` (state at `d77647b`, the #220 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no auto-startable Claude-executable arc remains (R-CXA-1 must_pass #1 deferred-as-Reading-D); R-830 / R-CXA-4 are operator-driven menu picks; live runs + R-300-second-provider creds-gated. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | Thirty-ninth terminating refresh — PR #239 substantive `live-auto` harness merged at `d7493de`; §12.2 owed follow-on. Fresh `/clear`→`continue` session: the session-start hook flagged `[ROADMAP DRIFT]` against the project-root dashboard; §12.1 audit found it was the benign §12.2.1 fixed-point lag (anchor at #237-state via #238's own merge). The operator-approved arc preserved the Jun-1 `live-auto/` harness off the superseded `r-xi-06-polish` worktree (#239); the companion `generate.py` visual iteration was NOT ported (superseded by Almanac Noir #234 — advisor-confirmed). | Single-file dashboard-only refresh per §12.2.1. Hash `fb0f95c78fac` → `284f7f750a01` (state at `d7493de`, the #239 merge; PRS empty; fork count 44; batch-51). Also reconciles the pre-existing one-commit anchor lag (dashboard had sat at #237-state `fb0f95c78fac` @ `23b80aef`, never capturing #238 `Surface XI complete`); this refresh advances to #239-state recording #238 + #239. Next action UNCHANGED — no auto-startable Claude-executable arc remains; R-830 / R-CXA-4 operator-driven menu picks; live runs + R-300-second-provider creds-gated. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | Fortieth terminating refresh — PR #241 substantive `research/` corpus commit merged at `4bafef7`; §12.2 owed follow-on. Same session as the #239/#240 arc: a git-sync audit (operator asked "is everything committed/pushed") surfaced 11 untracked top-level `research/` files cited as canonical substrate by `CLAUDE.md` §10.9/§13.5 but never tracked; operator-approved committing them (#241). | Single-file dashboard-only refresh per §12.2.1. Hash `284f7f750a01` → `61a20d7a9750` (state at `4bafef7`, the #241 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no auto-startable Claude-executable arc remains; remaining work operator-owned (R-700 ratification; creds/infra-gated R-300-second-provider / R-410..R-440 / R-500). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Post-#243/#244 refresh — 2 substantive merges + R-700 tee-up (this PR).** Fresh `/clear`→`continue` session executed two operator-picked arcs: **R-600 cadence-2** (PR #243 `62c98d4` — pattern-bake-in-sweep 2nd cadence + `R-600-memory-hygiene-normalization` RESOLVED) and **R-008 OD-4 gate (a)** (PR #244 `2233abb` — solo-developer §13.1 per-session redaction toggle, grounding-first, gate (a) FULLY CLOSED). Both merged green (squash); worktrees/branches cleaned; **~111 stale merged local branches pruned** (cross-referenced vs GitHub merged-PR head-refs since the repo squash-merges); local main ff'd to `2233abb`. | Substantive (both PRs touch `Project_Roadmap_v1.md`; #243 §5.6 R-600 + NEW R-600-memory-hygiene entry, #244 §5.6 R-008 + harness-od/CLAUDE.md OD-4 row) → **this is the owed follow-on terminating refresh** (Forty-first). Single-file dashboard-only per §12.2.1. Hash `61a20d7a9750` → `978cdde72d8e` (state at `2233abbc`, the #244 merge; PRS empty; fork count 44; batch-51). **Next action: R-700 Phase-8 ratification is now TEED UP for operator decision** — #244 spent the last Claude-executable OD-4 lever (gate (a) closed; gate (b) §13.2 is cross-axis/Phase-6+, not Claude-closeable), so OD-4 joins the bounded-residual-style sign-off set as a `RETIRED-AS-CROSS-AXIS-DEFERRED` candidate; the 4 R-700 PART C rulings (integer / CXA+CP-17 coverage / bounded-residual sign-offs incl. OD-4 / open-set declaration) await the operator. No auto-derivable Claude-executable ACTIVE row remains. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Forty-second terminating refresh — PR #246 substantive R-700-ratification-record merged at `165a0f4`; §12.2 owed follow-on.** Same session: the operator ruled on the teed-up R-700 PART C decisions (AskUserQuestion) → 3 of 4 RATIFIED (integer=46 cascade-deferred; CXA/CP-17 coverage CLOSED via existing R-CXA-1..4 + NEW R-010-cp-17; sign-offs incl. OD-4-as-RETIRED-AS-CROSS-AXIS-DEFERRED), formal declaration HELD. #246 recorded them (NEW R-010 entry + R-700/R-008 notes + R-700 draft PART C). | Single-file dashboard-only refresh per §12.2.1. Hash `978cdde72d8e` → `d522f4624fc3` (state at `165a0f49`, the #246 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — R-700 stays BLOCKED on the HELD operator declaration only (no Claude-executable arc; the formal close authors `phase-8-graduation.md` + the deferred 48→46 cascade + bucket flips when the operator lifts the hold). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Forty-third terminating refresh — PR #248 doc-hygiene merged at `e725f89`; §12.2 owed follow-on (cosmetic).** Same session: a repo-hygiene assessment found the per-axis CLAUDE.md §1.2 "authority" pointers badly stale (OD spec v1.4 vs canonical v1.27; CP v1.3 vs v1.30 — the exact foot-gun that misled this session's R-008 grounding) → bumped to canonical HEAD (#248) + gitignored local scratch. Grounding flipped the "#2 junk-dup deletion" premise (the files are not junk — `[[design-substrate-version-identity-hazards]]`), so #2 was deliberately NOT done. | Single-file dashboard-only refresh per §12.2.1. Hash `d522f4624fc3` → `811f1c3087dc` (state at `e725f89f`, the #248 merge; PRS empty; fork count 44; batch-51). **Next action UNCHANGED** — #248 touched per-axis CLAUDE.md + .gitignore only (NO roadmap §5 / R-NNN state change), so the §4 derivation is unmoved: no auto-derivable Claude-executable ACTIVE row remains (R-600 sweep not due — 5 of ~10 PRs since cadence-2; R-700 BLOCKED on the HELD operator declaration; creds/infra items gated). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Forty-fourth terminating refresh — PR #250 substantive no-parking-directive merged at `f263857`; §12.2 owed follow-on.** Operator made standing: operator-labeled items (`halt-route-to-operator` / operator-owned / infra-gated / PROPOSED / DEFERRED) must NOT park a session at "no Claude work"; the operator executes nothing manually. #250 encoded it in CLAUDE.md NEW §12.4.1 + roadmap §4 (`NO_ACTIVE_CANDIDATES` ≠ terminate) + the "Next action" banner here. | Single-file dashboard-only refresh per §12.2.1. Hash `811f1c3087dc` → `bdf76e986f48` (state at `f2638574`, the #250 merge; PRS empty; fork count 44; batch-51). **Next-action SEMANTICS CHANGED** — "no auto-`ACTIVE` row" is no longer a stop: future sessions drive the highest-value forward item's Claude-executable slice to the genuine gate (R-410..R-440 / R-300-second-provider / R-CXA-2 / R-810/820 slices), surfacing only real decisions/creds/paid-calls. Held operator decisions (R-700) honored; paid-call/secret boundary unchanged. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Forty-fifth terminating refresh — PR #252 substantive carve-out-hardening merged at `a91b065`; §12.2 owed follow-on.** Ensuring clean `/clear`→continue continuation surfaced a real bug: the session-start hook's §12.2.1 lag-expected carve-out matched only `post-PR-NN` refresh titles, but this session's refreshes (#245/#247/#249/#251) used `post-#NN` → the next fresh session would have mis-flagged the expected lag-by-one as `[ROADMAP DRIFT]`. #252 re-keyed the carve-out on the prefix `^ops: roadmap status refresh ` + reconciled the protocol docs. **This refresh's own title uses that prefix, so the hardened hook recognizes it** — the fixed point now self-validates. | Single-file dashboard-only refresh per §12.2.1. Hash `bdf76e986f48` → `47fedc3fbf07` (state at `a91b065b`, the #252 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no-parking directive stands (drive the highest-value forward item's slice to the genuine gate; R-700 declaration HELD; creds/infra gated). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |
| 2026-06-02 | **Substantive grounding PR (post-#252 fixed-point; `/clear`→continue) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Two producer-discovery grounding findings recorded against roadmap §5 entry notes + the dashboard banner; NO code/spec change (mode-agnostic). (1) **R-CXA-2 grounded → gates on engine-layer substrate, NO wireable slice** (2/6 §16.5 methods fired; 4/6 blocked by deliberately-STRUCK U-RT-111 X-AL-3 gaps) — 3rd CXA seam → 3rd DEFER, `[[r-cxa-seam-wiring-is-producer-discovery]]`. (2) **R-300-second-provider's mock cross-family fixture is ALREADY BUILT** (`test_iterates_three_candidates_until_success`); remaining R-300 work is live-creds-only. Banner staleness fixed: R-XI-02/R-XI-03 PROPOSED→RESOLVED (Surface XI COMPLETE). | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` NOT recomputed here — the follow-on terminating refresh recomputes it post-merge per §12.2.1. Next action UNCHANGED: high-value forward menu is genuinely slice-exhausted of un-built no-creds Claude work; every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440, R-500), the HELD R-700 declaration, or the dispositioned R-CXA-2 engine-layer arc. No-parking-compliant terminal (genuine gate reached after honest grounding), not a park. |
| 2026-06-02 | **Forty-sixth terminating refresh — PR #254 substantive grounding PR merged at `b19d26d`; §12.2 owed follow-on (this).** Same `/clear`→continue session: two producer-discovery grounding findings recorded (R-CXA-2 gates-on-engine-substrate / R-300 fixture-already-built) + stale Surface-XI banner fixed; NO code/spec change. | Single-file dashboard-only refresh per §12.2.1. Hash `47fedc3fbf07` → `9cd2b8ba6b36` (state at `b19d26d1`, the #254 merge; PRS empty; fork count 44; batch-51). **Next action UNCHANGED** — the high-value forward menu is genuinely slice-exhausted of un-built no-creds Claude work (every remaining item gates on operator creds/infra, the HELD R-700 declaration, or the dispositioned R-CXA-2 engine-layer arc); the no-parking terminal stands. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix per #252). |
| 2026-06-02 | **Substantive grounding+fork PR #256 (post-#254 fixed-point; `/clear`→continue) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** No-parking-compliant (§12.4.1): drove R-410 (the only un-blocked forward row with an un-built Claude slice). Grounding (advisor-confirmed; R-410 `advisor_required: yes`) found the resolved sandbox tier is **never enforced** (`SandboxDispatchDecision` → tier-floor + span attrs only; execution tier-independent; spec v1.41 §14.9.8 specs only the resolver). Building = X-AL-3 extension → **filed** `class_1_fork_sandbox_tier_no_execution_driver_contract.md` (PROPOSING; Readings A/B/C; C10⊥C11; routes to design-phase at the R-410 resolution arc). Fork count 44→45. R-410 roadmap note repointed at the fork. | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `501acfc` (`9a1fff34a930`, forks 44) per the line-155/#138 substantive-PR convention — the follow-on terminating refresh recomputes against the #256 merge commit (forks 45) post-merge per §12.2.1. **Next action:** after #256, the forward menu's un-built no-creds slices are genuinely exhausted again — every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), the HELD R-700 declaration, or STRUCK U-RT-111 substrate (R-CXA-2). The R-410 *design half* is now filed+routed (was the last separable design slice); its *infra* half stays gated. |
| 2026-06-02 | **Forty-seventh terminating refresh — PR #256 substantive grounding+fork PR merged at `0fca58f`; §12.2 owed follow-on (this).** Same `/clear`→continue session: filed the R-410 execution-driver Class 1 fork (the design slice #254 missed — resolved sandbox tier never enforced; PROPOSING; Readings A/B/C; C10⊥C11; routes to design-phase). | Single-file dashboard-only refresh per §12.2.1. Hash `9a1fff34a930` → `0e8a496ad344` (state at `0fca58fa`, the #256 merge; PRS empty; **fork count 45**; batch-51). **Next action UNCHANGED** — after the R-410 design half is filed+routed, the forward menu's un-built no-creds slices are genuinely exhausted again: every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), the HELD R-700 declaration, or STRUCK U-RT-111 substrate (R-CXA-2). No-parking-compliant terminal. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix per #252). |
| 2026-06-02 | **🎓 Substantive R-700 Phase-8 graduation PR #258 (operator lifted the HELD declaration) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Authored `.harness/phase-8-graduation.md` (canonical 46/54 RETIRED + 49/54 pipeline-advanced); forward supersession at ledger §11.7 + harness-cp/CLAUDE.md + this dashboard; R-700 → RESOLVED; bucket dispositions flipped (labels ≠ count-membership). Advisor confirmed 46 is forced. **Terminal milestone — surfaced for operator review/merge, NOT auto-merged.** | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `8694fa0` (`54ad811b3ed9`, forks 45) per the substantive-PR convention — the follow-on terminating refresh recomputes against the #258 merge commit post-merge per §12.2.1 (after the operator merges). **Phase 7 (substitution-retirement) COMPLETE; the workspace is now wholly on the post-Phase-8 activation/deployment/integration axis (Surfaces IV/V/VI).** Next-action: per §12.4.1 drive the highest-value forward item to its genuine gate — all remaining slices gate on operator creds/infra (R-300-second-provider, R-410..R-440, R-500), or a dispositioned design arc (R-CXA-2/3, R-810/820). |
| 2026-06-02 | **🎓 Forty-eighth terminating refresh — PR #258 Phase-8 graduation merged at `155935c` (operator-approved); §12.2 owed follow-on (this).** Operator gave the merge go-ahead for the terminal milestone. **PHASE 8 CLOSED:** canonical 46/54 RETIRED (85.2%) + 49/54 pipeline-advanced (90.7%); R-700 RESOLVED; forward supersession landed (ledger §11.7 + harness-cp/CLAUDE.md + dashboard). | Single-file dashboard-only refresh per §12.2.1. Hash `54ad811b3ed9` → `2abef0363e76` (state at `155935cb`, the #258 merge; PRS empty; fork count 45; batch-51). **Next action:** Phase 7 (substitution-retirement) is COMPLETE; the workspace is wholly on the post-Phase-8 activation/deployment/integration axis. Per §12.4.1 a future session drives the highest-value forward item to its genuine gate — every remaining slice gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), or a dispositioned design arc (R-CXA-2/3 engine-substrate/composer, R-810/820 files/managed-agents). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-02 | **Substantive roadmap-design-extension PR #260 (operator-requested post-R-700) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Scoped NEW PROPOSED `R-600-substitution-ledger-schema` (Surface VII): extract the 54-row substitution ledger to a typed source + derived counts + a CI tally gate, motivated by the R-700 `48/54`-internally-impossible bug. roadmap §5.6 only (+ this dashboard). | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `0a264d9` (`f8cfaa064640`, forks 45) per the substantive-PR convention — the follow-on terminating refresh recomputes against the #260 merge commit post-merge per §12.2.1. **Next action UNCHANGED** — the new entry is PROPOSED (not ACTIVE); it joins the forward menu but does not auto-start. Per §12.4.1 a future session may drive it (or the operator authorizes execution). |
| 2026-06-02 | **Forty-ninth terminating refresh — PR #260 roadmap-design-extension merged at `db96a61`; §12.2 owed follow-on (this).** Scoped NEW PROPOSED `R-600-substitution-ledger-schema` (operator-requested post-R-700). | Single-file dashboard-only refresh per §12.2.1. Hash `f8cfaa064640` → `784643914056` (state at `db96a61c`, the #260 merge; PRS empty; fork count 45; batch-51). **Next action UNCHANGED** — the new entry is PROPOSED (joins the forward menu; does not auto-start). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-03 | **Substantive R-600 build PR #262 (operator-authorized) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Built the substitution-ledger schema: `.harness/substitutions.yaml` (single source) + `tools/substitution_ledger.py` derive/validate + a blocking CI tally gate + `generate.py` consuming the derivation. R-600 → RESOLVED; the count-drift defect class is now CI-caught. | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `53a3a7b` (`fc784f5b0cb1`, forks 45) — the follow-on terminating refresh recomputes against the #262 merge commit post-merge per §12.2.1. **Next action UNCHANGED** — Phase 7 complete; workspace on the post-Phase-8 activation/deployment/integration axis (every remaining slice gates on operator creds/infra or a dispositioned design arc). |
| 2026-06-03 | **Fiftieth terminating refresh — PR #262 R-600 build merged at `63ae9f9`; §12.2 owed follow-on (this).** The substitution-ledger schema is LIVE: `.harness/substitutions.yaml` single source + `tools/substitution_ledger.py` derivation + a blocking CI tally gate (validates against itself on every PR). R-600 RESOLVED. | Single-file dashboard-only refresh per §12.2.1. Hash `fc784f5b0cb1` → `26a6d6cc90cf` (state at `63ae9f9f`, the #262 merge; PRS empty; fork count 45; batch-51). **Next action UNCHANGED** — Phase 7 complete; the count-drift / stale-carry defect class is now CI-caught on the accounting surface; remaining work gates on operator creds/infra or a dispositioned design arc. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-04 | **Multi-agent collision — completeness follow-on to the parallel `1f9bf76 post-#294` refresh.** #292/#293/#294/#295 all merged ~concurrently from parallel sessions. The `1f9bf76` refresh recorded only #294 (missed #293 BMAD-install + #295 council-productize; stale-listed merged #295 in-flight; anchor pinned at #294). This refresh records #293 + #295, clears in-flight, re-pins the anchor to the current tip. | Hash `f853b6b2478e` → `46d22535c509` (state at `343fe45f`/`1f9bf76` tip; PRS empty; fork count 45; batch-51). The `1f9bf76` tip already satisfied the `session-start.sh` carve-out (title-prefix + roadmap_status.md-only), so no false drift was pending; this is a content-completeness refresh (records the BMAD install #293 unrecorded by the parallel agent). HEAD remains a roadmap_status.md-only `ops: roadmap status refresh` commit → next §12.1 audit `lag-expected`. `roadmap.html` regen owed separately (parallel refresh also skipped it). |
| 2026-06-04 | **`roadmap.html` regen + closing terminating refresh — completes the 3-commit shape (`8fae324` status → `ab5b946` html → this closing status).** The committed Pages snapshot was stale since #289; regenerated via `generate.py` (69 R-NNN actions, 0 open PRs, retirement 46/54). | Hash `46d22535c509` → `fa5910d260d5` (state at `ab5b9467`, the html-regen commit; PRS empty; fork count 45; batch-51). `git_head` re-pinned to the html-regen commit; HEAD is this roadmap_status.md-only closing refresh → next §12.1 audit `lag-expected` (carve-out: title-prefix + roadmap_status.md-only). |
| 2026-06-07 | **Post-#328 terminating refresh — PR #328 substantive R-500 multi-tenant proof merged at `6f98190`; §12.2 owed follow-on.** Same session: PR #328 closed the local SELF_HOSTED_SERVER multi-tenant proof with `tenant.id` resource separation, local Collector/Tempo traces, default-off content redaction, tenant-scoped audit-ledger reads, `cost=0`, and `hosted-provider-calls=0`. | Hash `a835b786eb57` → `d3a5268dd1f8` (state at `6f981902`, the #328 merge; PRS `324:codex/notebooklm-mcp-audit`; fork count 45; batch-51). **Next action:** R-500 is now resolved on main; remaining high-value forward gates are R-411 host-fit sandbox provider selection, R-421 managed cloud / R-412 full-VM cloud, R-008 tokenization, and managed-cloud external integrations. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#324 terminating refresh — PR #324 NotebookLM MCP Codex audit merged at `7f1a2cd`; §12.2 owed follow-on.** Same session: PR #324 added `.codex/notes/notebooklm-mcp-codex-audit.md`, documenting current NotebookLM MCP/skill auth exposure, PleasePrompto candidate constraints, usage policy, and live query gates without installing a server, moving credentials, or querying NotebookLM. | Hash `d3a5268dd1f8` → `ec73782f37ba` (state at `7f1a2cd0`, the #324 merge; PRS empty; fork count 45; batch-51). **Next action unchanged:** #324 is mode-agnostic setup/audit; remaining high-value forward gates are R-411 host-fit sandbox provider selection, R-421 managed cloud / R-412 full-VM cloud, R-008 tokenization, and managed-cloud external integrations. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#331 terminating refresh — PR #331 recurring fork-doc status audit merged at `16e50f7`; §12.2 owed follow-on.** Same session: PR #331 refreshed the R-410 execution-driver fork status, normalized one durable-async leading status token, corrected stale fork-doc sample rows, appended the audit log, and regenerated the dashboard snapshot. | Hash `ec73782f37ba` → `bd58b6658cf8` (state at `16e50f7f`, the #331 merge; PRS empty; fork count 45; batch-51). **Next action unchanged:** the audit changed no R-NNN status; remaining high-value forward gates remain R-411 host-fit sandbox provider selection, R-421 managed cloud / R-412 full-VM cloud, R-008 tokenization, and managed-cloud external integrations. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#332 corrective terminating refresh — PR #332 refresh merged at `805742c`; §12.2 fixed-point re-pin owed because #332 was merge-merged.** | Hash `bd58b6658cf8` → `2d0d4b0824d3` (state at `805742c5`, the #332 merge; PRS empty; fork count 45; batch-51). **Next action unchanged:** no R-NNN status changed; this is a fixed-point correction so the next session-start audit sees the expected lag state. This PR must be squash-merged so the commit title preserves the `ops: roadmap status refresh ` carve-out prefix. |
| 2026-06-07 | **Post-#334 terminating refresh — PR #334 R-411/R-421 readiness/gate slice merged at `133f1be`; §12.2 owed follow-on.** | Hash `2d0d4b0824d3` → `1c16cb47a0ca` (state at `133f1be4`, the #334 merge; PRS empty; fork count 45; batch-51). **Next action unchanged:** R-411/R-421 are advanced to honest gates but not resolved; R-411 needs a compatible sandbox host/runtime, and R-421 needs a managed-cloud provider-secret backend plus managed collector. The E2B live probe requires explicit usage-billed approval. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#338 terminating refresh — PR #338 R-421 GCP secret backend merged at `be31e16`; §12.2 owed follow-on.** | Hash `4f9beb71f784` → `aaed8ffb78fb` (state at `be31e161`, PRS empty, fork count 45, batch-51). **Next action:** R-421 static GCP backend slice is closed; R-421 remains PROPOSED/live-gated on operator-provisioned GCP credentials, a Python environment with `google-cloud-secret-manager`, Secret Manager entries following the `servicename-secret` convention including `e2b-secret`, a non-loopback managed OTLP endpoint/collector, cost approval, and explicit approval for live GCP/E2B calls. R-411 still needs compatible sandbox host/runtime. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#340 terminating refresh — PR #340 R-421 live-secret connection slice merged at `e2f1165`; §12.2 owed follow-on.** | Hash `aaed8ffb78fb` → `98108e090096` (state at `e2f11656`, PRS empty, fork count 45, batch-51). **Next action:** R-421 now has a backend-sourced resolve-only E2B key probe and the `servicename-secret` / `e2b-secret` naming convention is recorded across code/docs/status. After operator auth fix, approved live diagnostics proved ADC + `e2b-secret` metadata access and resolve-only secret fetch with redacted output and no E2B sandbox. R-421 remains PROPOSED/live-gated on a real non-loopback managed OTLP endpoint/collector, cost approval, and explicit approval before any usage-billed E2B sandbox call. R-411 still needs compatible sandbox host/runtime. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#342 terminating refresh — PR #342 R-421 managed collector live gate merged at `0eab7d3`; §12.2 owed follow-on.** | Hash `aaed8ffb78fb` → `923fd83ca071` (state at `0eab7d3f`, PRS empty, fork count 45, batch-51). **Next action:** R-421 now has authenticated Cloud Run OTel collector deployment docs/config plus a managed-cloud live e2e command that resolves `e2b-secret`, can run the approved E2B deterministic command, and fails fast before E2B sandbox creation on the current runtime bootstrap reachability mismatch. R-421 remains PROPOSED until the `TIER_1_PROCESS` bootstrap-vs-`VENDOR_PIPELINE` managed-cloud design/runtime mismatch is resolved or runtime materialization moves into a genuine managed-cloud sandbox process. |
| 2026-06-07 | **Post-#344 terminating refresh — PR #344 R-421 runtime/design mismatch fix merged at `972533a`; §12.2 owed follow-on.** | Hash `923fd83ca071` → `9eedb088efe2` (state at `972533a6`, PRS empty, fork count 45, batch-51). **Next action:** The static runtime/design mismatch is resolved by explicit `CollectorConfig.bootstrap_sandbox_tier`: Tier-1 remains strict, and the E2B/FULL_VM template binds bootstrap reachability to `tier-4-full-vm` for `VENDOR_PIPELINE`. R-421 remains PROPOSED/live-gated until the approved full managed-cloud e2e is rerun and proves hosted E2B execution, authenticated OTLP export, and Cloud Trace visibility. |
| 2026-06-07 | **Post-#349 terminating refresh — PR #349 R-421 managed-cloud live e2e merged at `b90eeed`; §12.2 owed follow-on.** | Hash `e055cf3ff207` → `cc70ef8f85f2` (state at `b90eeed4`, PRS empty, fork count 45, batch-51). **Next action:** R-421 is now RESOLVED by the approved E2B + GCP Secret Manager + authenticated Cloud Run collector live e2e; R-412 remains deferred until the separate R-411/full-VM host-fit implementation path exists. Remaining high-value forward work shifts to R-411 compatible sandbox host/runtime selection, R-008 redaction toggle/tokenization, R-CXA-2 engine substrate, R-810/R-820 managed integrations, and R-830 production memory backend. |
| 2026-06-07 | **Post-#351 terminating refresh — PR #351 R-008 opaque redaction token substrate merged at `960636b`; §12.2 owed follow-on.** | Hash `cc70ef8f85f2` → `4cf2978581de` (state at `960636b0`, PRS empty, fork count 45, batch-51). **Next action:** R-008 advanced by an OD-owned provider-free opaque-token substrate, but remains BLOCKED/PARTIAL until durable audit-ledger-only token-map persistence and the cross-axis eval-grade semantic classifier are implemented. Remaining high-value forward work shifts to R-411 compatible sandbox host/runtime selection, R-CXA-2 engine substrate, R-810/R-820 managed integrations, and R-830 production memory backend. |
| 2026-06-07 | **Substantive R-CXA-2 caller-site coverage PR — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** | Added direct integration coverage for the `workflow_driver.py:965` `HITLPauseRequestedSignal` pause-capture path and refreshed R-CXA-2 tracking text. **Next action:** R-CXA-2 remains STILL-BOUNDED after this coverage slice; remaining work is engine-layer producer / stage-ordering / HITL-disambiguator substrate, while other high-value forward work remains R-411, R-810/R-820, and R-830. |
| 2026-06-07 | **Post-#353 terminating refresh — PR #353 R-CXA-2 HITL pause caller-site coverage merged at `887f27c`; §12.2 owed follow-on.** | Hash `4cf2978581de` → `75fab67cdb20` (state at `887f27ce`, PRS empty, fork count 45, batch-51). **Next action:** R-CXA-2 remains STILL-BOUNDED after the materialized pause/resume caller-site coverage proof; remaining work is engine-layer producer / stage-ordering / HITL-disambiguator substrate. Other high-value forward work remains R-411 compatible sandbox host/runtime selection, R-810/R-820 managed integrations, and R-830 production memory backend. |
| 2026-06-07 | **Substantive R-008 durable audit-ledger token-map PR #355 merged at `e7fd27d`; §12.2 owed follow-on.** | Added signed OD audit-entry composition for `RedactionTokenRecord` plus a runtime `AuditLedgerRedactionTokenMap` sink consumed by `OpaqueRedactionTokenizer`. **Next action:** R-008 remains BLOCKED/PARTIAL on the eval-grade semantic classifier/category pipeline only; durable audit-ledger token-map persistence is no longer the blocker. Other high-value forward work remains R-411 compatible sandbox host/runtime selection, R-CXA-2 engine substrate, R-810/R-820 managed integrations, and R-830 production memory backend. |
| 2026-06-07 | **Post-#355 terminating refresh — PR #355 R-008 durable audit-ledger token-map slice merged at `e7fd27d`; §12.2 owed follow-on.** | Hash `75fab67cdb20` → `67dfdc7c509c` (state at `e7fd27d6`, PRS empty, fork count 45, batch-51). **Next action:** R-008 remains BLOCKED/PARTIAL on the eval-grade semantic classifier/category pipeline; other high-value forward work remains R-411 compatible sandbox host/runtime selection, R-CXA-2 engine substrate, R-810/R-820 managed integrations, and R-830 production memory backend. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Post-#356 corrective terminating refresh — PR #356 roadmap/dashboard refresh merged at `3027c25`; §12.2 fixed-point re-pin owed because #356 updated dashboard generator/source copy as well as status.** | Hash `67dfdc7c509c` → `c8cdc6d2f708` (state at `3027c25b`, PRS empty, fork count 45, batch-51). **Next action unchanged:** R-008 remains BLOCKED/PARTIAL on the eval-grade semantic classifier/category pipeline; other high-value forward work remains R-411 compatible sandbox host/runtime selection, R-CXA-2 engine substrate, R-810/R-820 managed integrations, and R-830 production memory backend. This status-only refresh is the fixed-point correction so the next session-start audit sees expected lag. |
| 2026-06-07 | **Substantive R-008 category-token classifier seam branch opened from `17e3ff5`; §12.2 owed follow-on after merge.** | Added the provider-free `RedactionAttributeClassifier` seam and deterministic category classifier so token mode can emit `[REDACTED:PII:<id>]` / `[REDACTED:MCP_ARG:<id>]` placeholders and persist `audit.redaction_token.semantic_category` through the signed token-map audit entry. **Next action:** R-008 remains BLOCKED/PARTIAL on the full eval-grade semantic classifier; this branch advances the seam but does not claim OD-4 RETIRE-READY. |
| 2026-06-07 | **Post-#358 terminating refresh — PR #358 R-008 category-token classifier seam merged at `e150f45`; §12.2 owed follow-on.** | Hash `c8cdc6d2f708` → `b4ea5e307671` (state at `e150f45f`, PRS empty, fork count 45, batch-51). **Next action:** R-008 remains BLOCKED/PARTIAL on the full eval-grade semantic classifier; the provider-free category-token seam is no longer the blocker. This refresh branch is the owed fixed-point follow-on. |
| 2026-06-07 | **Substantive R-830 S3 memory backend branch opened from `384371d`; §12.2 owed follow-on after merge.** | Added provider-free S3 backend implementation and explicit S3 factory binding up to the live cloud boundary. **Next action:** R-830 remains PROPOSED/gated on real S3 bucket/credentials plus operator-approved live read/write/delete e2e; managed-DB remains a future variant. |
| 2026-06-07 | **Post-#360 terminating refresh — PR #360 R-830 S3 memory backend merged at `10de3b1`; §12.2 owed follow-on.** | Hash `b4ea5e307671` → `3df68d3b3a2d` (state at `10de3b1`, PRS empty, fork count 45, batch-51). **Next action:** R-830 remains PROPOSED/gated on live S3 bucket/credentials plus operator-approved cloud e2e; the provider-free S3 slice is no longer the blocker. |
| 2026-06-07 | **Post-#361 corrective fixed-point refresh — PR #361 merged at `f8cb91d`; §12.2 anchor re-pin completed.** | Hash `3df68d3b3a2d` → `c71f94cd1e59` (state at `f8cb91dd`, PRS empty, fork count 45, batch-51). **Next action unchanged:** R-830 remains PROPOSED/gated on live S3 bucket/credentials plus operator-approved cloud e2e; managed-DB remains a future variant. This status-only refresh corrects the anchor table after #361 refreshed the narrative/dashboard snapshot. |
| 2026-06-07 | **Post-#362 guard-compatible roadmap status refresh — PR #362 merged at `ab8a674`; §12.2 anchor re-pin completed with the guard-recognized refresh title.** | Hash `c71f94cd1e59` → `a8d6d2b6ca5a` (state at `ab8a674e`, PRS empty, fork count 45, batch-51). **Next action unchanged:** R-830 remains PROPOSED/gated on live S3 bucket/credentials plus operator-approved cloud e2e; managed-DB remains a future variant. |
| 2026-06-07 | **Substantive R-830 live S3 e2e branch opened from `2ce0e6e`; §12.2 owed follow-on after merge.** | Added the live S3 e2e recipe/test and proved real S3 create/view/update/delete through the Memory tool dispatch seam using AWS CLI login profile credentials. **Next action:** R-830 S3 cloud-vault path is live-proven; managed-DB remains future optional scope. Other high-value forward work returns to R-411 host/runtime selection, R-CXA-2 engine substrate, R-008 eval-grade classifier, and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#364 roadmap status refresh — PR #364 R-830 live S3 e2e merged at `d19a263`; §12.2 owed follow-on.** | Hash `a8d6d2b6ca5a` → `8762408faaf8` (state at `d19a263f`, PRS empty, fork count 45, batch-51). **Next action:** R-830 S3 cloud-vault path is live-proven; managed-DB remains future optional scope. Other high-value forward work returns to R-411 host/runtime selection, R-CXA-2 engine substrate, R-008 eval-grade classifier, and R-810/R-820 managed integrations. |
| 2026-06-07 | **Substantive R-008 eval-grade runtime tokenization branch opened from `185567b`; §12.2 owed follow-on after merge.** | Added provider-free `EvalGradeSemanticRedactionClassifier` and wired multi-tenant runtime redaction to audit-backed opaque tokens when an audit sink is available, with fail-closed strip mode preserved otherwise. **Next action:** R-008's runtime code residual is closed; OD-4 accounting label/tally remains unchanged here pending any separate accounting/back-flow decision. Other high-value forward work returns to R-411 host/runtime selection, R-CXA-2 engine substrate, and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#366 roadmap status refresh — PR #366 R-008 eval-grade redaction tokenization merged at `7114048`; §12.2 owed follow-on.** | Hash `8762408faaf8` → `7e818d662c54` (state at `71140480`, PRS empty, fork count 45, batch-51). **Next action:** R-008 runtime code residual is closed; OD-4 remains under its R-700 ratified accounting label unless a separate accounting/back-flow action reclassifies it. Other high-value forward work returns to R-411 host/runtime selection, R-CXA-2 engine substrate, and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#367 corrective fixed-point refresh — PR #367 merged at `4bf1989`; §12.2 anchor re-pin completed.** | Hash `7e818d662c54` → `15eaac9e4017` (state at `4bf1989f`, PRS empty, fork count 45, batch-51). **Next action unchanged:** R-008 runtime code residual is closed; remaining movement is accounting/back-flow only. Other high-value forward work returns to R-411 host/runtime selection, R-CXA-2 engine substrate, and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#369 roadmap status refresh — PR #369 R-CXA-2 engine pause/resume substrate merged at `88ad017`; §12.2 owed follow-on.** | Hash `15eaac9e4017` → `d4fac7dbb4c3` (state at `88ad0173`, PRS empty, fork count 45, batch-51). **Next action:** R-CXA-2 engine free-function substrate is no longer a pure `NotImplementedError` stub; subsequent audit reaffirmed the production firing-site remains bounded-defer because the engine-layer recovery loop is absent. The HITL rewrite production caller is likewise bounded-defer until an LLM inner tool-call interception loop exists. Other high-value forward work remains R-411 host/runtime selection and R-810/R-820 managed integrations. |
| 2026-06-07 | **Substantive R-CXA-2 CP→IS workload-selection wiring branch opened from `ffec359`; §12.2 owed follow-on after merge.** | Moved CP→IS wiring early enough for stage-3b binding-time engine selection and proved all 12 workload/persona selections emit `cp.workload-binding-class-selection` entries before OD. **Next action:** R-CXA-2 remains STILL-BOUNDED on HITL tool-call rewriting production-caller/spec disambiguation and engine free-function production firing-site connection. |
| 2026-06-07 | **Substantive R-CXA-2 firing-site disambiguation branch refreshed after PR #371 merged at `a4e3a08`; §12.2 owed follow-on after merge.** | Reaffirmed the ratified HITL rewrite and engine free-function Class 1 dispositions against current code after workload-selection landed: no valid production firing site exists for either surface at the MVP boundary, so no static-tool-dispatch or ledger-only placeholder was wired. **Next action:** R-CXA-2 remains STILL-BOUNDED on future upstream-loop authoring, while other high-value work returns to R-411 host/runtime selection and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#372 roadmap status refresh — PRs #371/#372 merged at `a4e3a08e` / `9c2c29b9`; §12.2 owed follow-on.** | Hash `d4fac7dbb4c3` → `4dd48c5877c7` (state at `9c2c29b9`, PRS empty, fork count 45, batch-51). **Next action:** R-CXA-2 is advanced but remains STILL-BOUNDED on future upstream-loop authoring only; workload selection now fires through production callers, while HITL rewriting and engine free-functions are reaffirmed bounded-defer. Other high-value work returns to R-411 host/runtime selection and R-810/R-820 managed integrations. |
| 2026-06-07 | **Post-#374 roadmap status refresh — PR #374 R-411 runtime research refresh merged at `e3bbbcf`; §12.2 owed follow-on.** | Hash `4dd48c5877c7` → `cec1944126cf` (state at `e3bbbcf`, PRS empty, fork count 45, batch-51). **Next action:** R-411 selection is refreshed and remains host-gated on a compatible Linux sandbox host/runtime. If no Linux host is provisioned now, proceed to R-810/R-820 managed integrations rather than re-litigating direct Intel macOS closure. |
| 2026-06-07 | **Substantive R-810 provider-free Files API branch opened from `65a0f9f`; §12.2 owed follow-on after merge.** | Added the provider-neutral Files API client port, file metadata record, Anthropic `container_upload` data-shape helper, and `files.operation` telemetry helper. **Next action:** R-810 remains gated on design-phase opening and managed-cloud live upload/reference e2e; this branch advances only the provider-free runtime boundary. |
| 2026-06-07 | **Post-#376 roadmap status refresh — PR #376 R-810 provider-free Files API runtime port merged at `885de862`; §12.2 owed follow-on.** | Hash `cec1944126cf` → `71920a68ad1c` (state at `885de862`, PRS empty, fork count 45, batch-51). **Next action:** R-810 now has the provider-free runtime port and telemetry helper; remaining R-810 closure is gated on design-phase opening plus managed-cloud live upload/reference e2e. Other high-value work remains R-411 on a compatible Linux host/runtime and R-820 managed Agents integration. |
| 2026-06-07 | **Substantive R-820 provider-free Managed Agents branch opened from `c40584f`; §12.2 owed follow-on after merge.** | Added the provider-neutral Managed Agents session/event port, Anthropic Managed Agents beta-header constant, and `managed_agents.runtime` telemetry helper. **Next action:** R-820 remains gated on the real Anthropic Managed Agents SDK/session integration plus managed-cloud `managed_agents.*` emission e2e; this branch advances only the provider-free runtime boundary. |
| 2026-06-07 | **Post-#378 roadmap status refresh — PR #378 R-820 provider-free Managed Agents runtime port merged at `a85a6037`; §12.2 owed follow-on.** | Hash `71920a68ad1c` → `fb9dde5e2511` (state at `a85a6037`, PRS empty, fork count 45, batch-51). **Next action:** R-820 now has the provider-free runtime boundary and telemetry helper; remaining R-820 closure is gated on real Anthropic Managed Agents SDK/session integration plus managed-cloud `managed_agents.*` emission e2e. Other high-value work remains R-411 on a compatible Linux host/runtime and any operator-approved R-810 live Files gate. |
| 2026-06-07 | **Post-#380 roadmap status refresh — PR #380 R-820 real Anthropic Managed Agents live integration merged at `6a4c6364`; §12.2 owed follow-on.** | Hash `fb9dde5e2511` → `d52e85b4c52c` (state at `6a4c6364`, PRS empty/unavailable, fork count 45, batch-51). **Next action:** R-820 is RESOLVED by SDK/session integration plus managed-cloud Cloud Trace proof; temporary IAM was removed and verified absent. Remaining high-value work returns to R-810 Files live/design path and R-411 only if a compatible Linux sandbox host/runtime is available. |
| 2026-06-07 | **Post-#381 corrective fixed-point refresh — PR #381 merged at `e25143f4`; §12.2 anchor re-pin completed.** | Hash `d52e85b4c52c` → `6f171e13d3f7` (state at `e25143f4`, PRS empty/unavailable, fork count 45, batch-51). **Next action unchanged:** R-820 remains RESOLVED; remaining high-value work returns to R-810 Files live/design path and R-411 only if a compatible Linux sandbox host/runtime is available. |
| 2026-06-08 | **Substantive R-810 real Anthropic Files API closure branch opened from `c47bfb67`; §12.2 owed follow-on after merge.** | Added the real Anthropic Files API SDK adapter, Messages document-file reference helper, Batch request composition helper, and live managed-cloud e2e command. Operator-approved live proof passed: uploaded `file_011CbqJqTs21yENfK4xfEpqp`, referenced it by file_id with sentinel response, composed Batch request shape, exported `files.operation` to the authenticated managed collector, and observed Cloud Trace `bfd28fa8fc8ecc3ba973d1e405cdb865` with `files.*` attrs. Cleanup retry returned Anthropic 404, so the file is gone; temporary GCP TokenCreator IAM was removed. **Next action:** after merge, run the normal status/dashboard fixed-point refresh; R-411 remains host-gated on a compatible Linux sandbox host/runtime selected as Linux + Docker + gVisor/runsc first, Kata on Linux/KVM next. |
| 2026-06-08 | **Post-#383 roadmap status refresh — PR #383 R-810 real Anthropic Files integration merged at `eaeff26`; §12.2 owed follow-on.** | Hash `6f171e13d3f7` → `e18ae354b8f0` (state at `eaeff26f`, PRS empty/unavailable, fork count 45, batch-51). **Next action:** R-810 is RESOLVED by the live Anthropic Files upload/reference/delete path plus managed-cloud `files.operation` Cloud Trace proof. R-411 remains host-gated on a compatible Linux sandbox host/runtime; the selected no-taxonomy-change stack is Linux + Docker + gVisor/runsc first, Kata on Linux/KVM next. If no compatible Linux host is available, continue to the next implementable integration/deployment arc rather than re-litigating Intel macOS R-411 closure. |
| 2026-06-08 | **Post-#385 roadmap status refresh — PR #385 R-411 gVisor/runsc execution provider merged at `300009e`; §12.2 owed follow-on.** | R-411 is RESOLVED by the selected Docker + gVisor/runsc path on the operator-provisioned Lima Linux VM. The normal fixed-point refresh updates the status banner from PR TBD to PR #385 and re-pins the dashboard to the merged R-411 closure state. **Next action:** R-412 remains deferred on a separate full-VM/Firecracker provider path; remaining high-value open work returns to accounting/back-flow items and any operator-selected future integration arc. |
| 2026-06-08 | **Post-#402 roadmap status refresh — PR #402 R-CXA-2 bounded-status refresh merged at `7c2b5dda`; §12.2 owed follow-on.** | Hash `8c027fc8b21e` → `b0db307f9ac8` (state at `7c2b5dda`, PRS empty/unavailable, fork count 45, batch-53). **Next action unchanged:** R-CXA-1 remains producer-gated, R-CXA-2 remains STILL-BOUNDED on future HITL rewrite / engine recovery-loop producers, R-CXA-3 remains composer/scope gated, and activation/research options remain R-412, R-830, R-XI-02/R-XI-03/R-900 by operator discretion. |
| 2026-06-08 | **Post-#404 roadmap status refresh — PR #404 R-830 memory-backend comment refresh merged at `3383fb28`; §12.2 owed follow-on.** | Hash `b0db307f9ac8` → `20676cee37c3` (state at `3383fb28`, PRS empty/unavailable, fork count 45, batch-53). **Next action unchanged:** #404 was runtime comment/docstring hygiene only; R-CXA-1/R-CXA-2/R-CXA-3 remain producer/composer/upstream-loop gated, R-412 remains deferred, and R-830 managed-DB remains future optional scope by operator choice. |
| 2026-06-08 | **Post-#406 roadmap status refresh — PR #406 R-412 E2B full-VM provider merged at `34269d27`; §12.2 owed follow-on.** | Hash `20676cee37c3` → `d5e13dae565c` (state at `34269d27`, PRS empty/unavailable, fork count 45, batch-53). R-412 is RESOLVED by `E2BManagedFullVMToolRunnerExecutionDriver` on the existing dispatcher `ToolExecutionDriver` seam plus the operator-approved live Tier-4 E2B dispatcher e2e. **Next action:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-830 managed-DB and R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Post-#407 corrective roadmap status refresh — PR #407 dashboard-maintenance guard fix merged at `3971ef9b`; §12.2 owed follow-on.** | Hash `d5e13dae565c` → `2219da02aece` (state at `3971ef9b`, PRS empty/unavailable, fork count 45, batch-53). No roadmap item changed in #407; this refresh carries the R-412 back-flow on top of the corrected Codex closeout guard. **Next action unchanged:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-830 managed-DB and R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Post-#408 fixed-point refresh — PR #408 R-412 roadmap/status/dashboard back-flow merged at `83082090`; §12.2 owed follow-on.** | Hash `2219da02aece` → `d369d13eb6a8` (state at `83082090`, PRS empty/unavailable, fork count 45, batch-53). No roadmap item changed beyond the already-merged R-412 closure back-flow; this terminating refresh only re-pins the dashboard/status fixed point to #408. **Next action unchanged:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-830 managed-DB and R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Post-#410 fixed-point refresh — PR #410 dashboard remaining-card correction merged at `c57da7f7`; §12.2 owed follow-on.** | Hash `d369d13eb6a8` → `b6b3654a4d20` (state at `c57da7f7`, PRS empty/unavailable, fork count 45, batch-53). No roadmap item changed; this refresh re-pins the dashboard/status fixed point after removing the stale closed R-411/R-412 remaining-card. **Next action unchanged:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-830 managed-DB and R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Post-#413 fixed-point refresh — PR #413 R-830 roadmap/status back-flow merged at `b4531d31`; §12.2 owed follow-on.** | Hash `27ebc427c0d7` → `558e3a835221` (state at `b4531d31`, PRS empty/unavailable, fork count 45, batch-53). R-830 no longer carries a pending S3/cloud-vault blocker; the completed filesystem, SQLite, and S3 Memory-tool backend slices remain recorded, and only optional managed-DB remains future scope. **Next action unchanged:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-830 managed-DB and R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Substantive R-830 managed-DB live proof branch — PR #417 follow-up; §12.2 owed after merge.** | Operator initialized Neon auth/project access and approved the live side-effecting DB proof. The branch now records `ManagedSqlMemoryToolBackend` as live-proven against the Neon `Arhugula` PostgreSQL project: `just r830-managed-db-live-e2e` passed create/view/str_replace/insert/delete through the Memory tool dispatch seam with cleanup. **Next action after merge:** R-CXA-1/R-CXA-2/R-CXA-3 remain the only non-retired substitution rows; R-XI-02/R-XI-03/R-900 remain optional operator-selected arcs. |
| 2026-06-08 | **Post-#420 roadmap status refresh — PR #420 R-CXA-3 CP→AS runtime composer merged at `48a047e9`; §12.2 owed follow-on.** | Hash `819760575631` → `ed9db727a1db` (state at `48a047e9`, PRS empty/unavailable, fork count 45, batch-54). R-CXA-3 is RESOLVED by the stage-6 `RuntimeCpAsWiring` composer and batch-54 retirement accounting. **Next action:** only CXA-1 and CXA-2 remain non-retired; proceed to a real AS→IS secret-fetch producer audit if one exists, otherwise CP→IS remains bounded on future HITL rewrite and engine recovery-loop producers. Optional operator-selected arcs remain R-XI-02/R-XI-03/R-900. |
| 2026-06-08 | **R-CXA-1 producer audit branch opened from `883a991a`; §12.2 owed after merge.** | Fresh grounding found no wireable AS→IS producer: `emit_secret_fetch_audit_entry` remains definition/test-only, bootstrap provider-key resolution still uses `resolve_bootstrap_value(...)`, and the only production `fetch_secret(...)` consumer is CP F5 signing-key resolution. R-CXA-1 remains PARTIAL/producer-gated; the immediate selector moves to R-CXA-2 unless a real AS secret-fetch producer or scoped runtime caller appears. |
| 2026-06-08 | **Post-#422 roadmap status refresh — PR #422 R-CXA-1 producer audit merged at `79929bbd`; §12.2 owed follow-on.** | Hash `caf7be78ea0b` → `5cc61fedc67d` (state at `79929bbd`, PRS empty/unavailable, fork count 45, batch-54). No runtime code changed; R-CXA-1 remains PARTIAL/producer-gated with evidence recorded. **Next action:** proceed to R-CXA-2 producer/upstream-loop grounding. |
| 2026-06-08 | **R-CXA-2 producer audit branch opened from `4baefe0b`; §12.2 owed after merge.** | Fresh grounding found no wireable CP→IS producer for the remaining methods: HITL tool-call rewriting still lacks a production caller/spec disambiguator, workflow-driver pause/resume sites already emit the workflow-layer `cp.pause-resume-protocol` action, and engine-layer `cp.pause-captured` / `cp.resume-attempted` still require future recovery-loop producers. R-CXA-2 remains STILL-BOUNDED; immediate forward work shifts to optional R-XI-02/R-XI-03/R-900 or a future producer/design-amendment trigger. |
| 2026-06-08 | **Post-#424 roadmap status refresh — PR #424 R-CXA-2 producer audit merged at `3c576f4f`; §12.2 owed follow-on.** | Hash `75490b0ac699` → `2353a8d1e707` (state at `3c576f4f`, PRS empty/unavailable, fork count 45, batch-54). No runtime code changed; R-CXA-2 remains STILL-BOUNDED/producer-gated with evidence recorded. **Next action:** optional R-XI-02/R-XI-03/R-900 by operator discretion, or future CXA producer/design-amendment trigger. |
| 2026-06-08 | **R-900 decomposition branch opened from `4523a857`; §12.2 owed after merge.** | Fresh grounding found the advertised R-XI-02/R-XI-03 options are already RESOLVED in `Project_Roadmap_v1.md` and implemented in `tools/dashboard/generate.py`; R-900's own must-pass requires crystallizing a specific research question before work begins. This branch resolves the placeholder by adding `R-901-phase-9-retirement-criteria` as the next explicit research frontier. |
| 2026-06-08 | **Post-#426 roadmap status refresh — PR #426 R-900 decomposition merged at `c223de07`; §12.2 owed follow-on.** | Hash `b9a21d9a2d3d` → `472c4d3cb516` (state at `c223de07`, PRS empty/unavailable, fork count 45, batch-54). R-900 is RESOLVED by decomposition into R-901, and the SQLite writer-lock fix keeps the PR pytest gate green. **Next action:** R-901 Phase-9 retirement criteria research brief. |
| 2026-07-03 | **Post-#907 terminating refresh — PR #907 U-MEM-25 memory closeout, documentation, and review evidence merged at `2864e81e`; §12.2 owed follow-on.** | Hash `6a64f3f962bb` → `f3f025ab55ab` (state at `2864e81e`, open PRs empty, fork count 87, batch-57). PR #907 implements the final planned provider-free unit of the full memory substrate: operator-facing memory policy documentation, maintainer architecture and migration notes, an R-MEM/C-MEM closeout evidence packet, provider-free `memory-closeout-check` gate wiring, docs index discoverability, and U-MEM-25 source cite grounding through the C-MEM-20 verification matrix. The full U-MEM forward sequence is complete at the provider-free boundary; optional live Anthropic native-memory and external-CLI auth checks remain explicit credential gates. Verification: PR #907 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-03 | **Post-#905 terminating refresh — PR #905 U-MEM-24 cross-provider and CLI verification suite implementation merged at `4841c921`; §12.2 owed follow-on.** | Hash `1f88899a644a` → `6a64f3f962bb` (state at `4841c921`, open PRs empty, fork count 87, batch-57). PR #905 implements the twenty-fourth atomic unit of the full memory substrate: a runtime C-MEM-20 evidence matrix, typed verification requirement/test-selector/live-gate/scenario carriers, provider-free deterministic selector coverage for every required verification item, access-mode and CLI profile scenario coverage, external-CLI route scenarios with fake-subprocess evidence separated from live auth gates, and explicit live gates for Anthropic native memory plus CLI-auth checks. The next unit is U-MEM-25 closeout, documentation, and review evidence. Verification: PR #905 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-03 | **Post-#903 terminating refresh — PR #903 U-MEM-23 migration and compatibility defaults implementation merged at `aca30019`; §12.2 owed follow-on.** | Hash `49b8db8fb015` → `1f88899a644a` (state at `aca30019`, open PRs empty, fork count 87, batch-57). PR #903 implements the twenty-third atomic unit of the full memory substrate: callback-backed memory migration over existing storage-backend protocol selections, dry-run reports over explicit `/memories/...` paths, and explicit canonical `migrate` native-memory events plus durable `native_adapter_call` ledgers. The next unit is U-MEM-24 cross-provider and CLI verification suite. Verification: PR #903 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-03 | **Post-#901 terminating refresh — PR #901 U-MEM-22 memory observability implementation merged at `7e9966c0`; §12.2 owed follow-on.** | Hash `be7f9b685fa4` → `49b8db8fb015` (state at `7e9966c0`, open PRs empty, fork count 87, batch-57). PR #901 implements the twenty-second atomic unit of the full memory substrate: C-MEM-19 operation and failure-class telemetry helpers, span wiring across capture, retrieval/context composition, promotion, native adapter callbacks, standard tool execution, lifecycle dispatch, and redaction, and additive compatibility with existing `memory.*` attributes. The next unit is U-MEM-23 migration and compatibility defaults. Verification: PR #901 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-03 | **Post-#899 terminating refresh — PR #899 U-MEM-21 redaction, tombstone, and retention implementation merged at `fa3b3e8e`; §12.2 owed follow-on.** | Hash `13ad6241e514` → `be7f9b685fa4` (state at `fa3b3e8e`, open PRs empty, fork count 87, batch-57). PR #899 implements the twenty-first atomic unit of the full memory substrate: C-MEM-18 redaction event carriers, content redaction/tombstone/retention transitions, durable memory-operation ledger redaction-event sidecars, audit-visible tombstones, physical redaction hash preservation, JSONL latest-state reads, retrieval/tool exclusion for redacted or tombstoned records, and stale-index `memory.read` fail-closed handling. The next unit is U-MEM-22 memory observability. Verification: PR #899 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#897 terminating refresh — PR #897 U-MEM-20 compaction safety hook implementation merged at `f84be5d7`; §12.2 owed follow-on.** | Hash `4c0a5ab1402b` → `13ad6241e514` (state at `f84be5d7`, open PRs empty, fork count 87, batch-57). PR #897 implements the twentieth atomic unit of the full memory substrate: runtime compaction candidate extraction, required durable disposition for every candidate, auditable compaction event records, durable COMPACTION_DECISION ledger entries, fail-closed write behavior, promotion target refs, per-candidate scope audit rows, and MEM-aware semantic-overlay cite extraction. The next unit is U-MEM-21 redaction, tombstone, and retention. Verification: PR #897 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#895 terminating refresh — PR #895 U-MEM-19 engine-class durability bindings implementation merged at `98d2b78a`; §12.2 owed follow-on.** | Hash `40615d5bed01` → `4c0a5ab1402b` (state at `98d2b78a`, open PRs empty, fork count 87, batch-57). PR #895 implements the nineteenth atomic unit of the full memory substrate: runtime C-MEM-17 durability bindings for all five closed engine classes, CP-to-IS engine-class carrier mapping, replay activity/snapshot and stabilization carriers, checkpoint store-version plus packet-hash carriers, pure-pattern state-ledger idempotency, reconciler observed versions, WAL segment/prewarm carriers, SHA-256 packet-hash validation, and commit-boundary protection so pending writes do not become active semantic memory early. The next unit is U-MEM-20 compaction safety hook. Verification: PR #895 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#893 terminating refresh — PR #893 U-MEM-18 CLI profile loading implementation merged at `108777e4`; §12.2 owed follow-on.** | Hash `76a939ac893d` → `40615d5bed01` (state at `108777e4`, open PRs empty, fork count 87, batch-57). PR #893 implements the eighteenth atomic unit of the full memory substrate: runtime CLI profile loading over the existing C-MEM-16 schema and CP `ExternalCliRoute`, built-in and custom profile resolution, explicit source-policy enforcement for Claude Code/Codex, declared instruction-source loading, external memory read/import/mutation guards, selected-provider route mismatch fail-closed behavior, and resolved profile provenance in episodic capture plus durable injection records while preserving selected provider and fallback-chain state. The next unit is U-MEM-19 engine-class durability bindings. Verification: PR #893 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#891 terminating refresh — PR #891 U-MEM-17 Anthropic native memory adapter implementation merged at `b00eb201`; §12.2 owed follow-on.** | Hash `73c9cf01cc67` → `76a939ac893d` (state at `b00eb201`, open PRs empty, fork count 87, batch-57). PR #891 implements the seventeenth atomic unit of the full memory substrate: a Runtime-owned canonical-store-backed Anthropic Memory callback adapter, `/memories/` path-validation compatibility, native access/capture/retrieval policy checks, episodic `TOOL_EVENT` state records, durable `NATIVE_ADAPTER_CALL` operation ledgers, repeated-callback ledger coverage, and no default backend-selection change. The next unit is U-MEM-18 CLI profile loading. Verification: PR #891 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#889 terminating refresh — PR #889 U-MEM-16 standard memory tool executor implementation merged at `71fbe048`; §12.2 owed follow-on.** | Hash `026796080df2` → `73c9cf01cc67` (state at `71fbe048`, open PRs empty, fork count 87, batch-57). PR #889 implements the sixteenth atomic unit of the full memory substrate: Runtime-owned provider-neutral standard memory tool execution, policy-checked search/read/write/promotion/redaction flows, durable standard-tool call ledgers, tool-call span emission, OpenAI standard-tool continuation dispatch, runtime record-scope threading, and package exports. The next unit is U-MEM-17 Anthropic native memory adapter onto canonical store. Verification: PR #889 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#887 terminating refresh — PR #887 U-MEM-15 prompt-extension packet fallback implementation merged at `582528c0`; §12.2 owed follow-on.** | Hash `df2fdc6cee97` → `026796080df2` (state at `582528c0`, open PRs empty, fork count 87, batch-57). PR #887 implements the fifteenth atomic unit of the full memory substrate: read-only prompt packet rendering, packet hash/policy-ref/selected-ref carrying in system content, provider prompt seam integration via the runtime dispatcher, existing prompt conflict propagation, and denied/redacted exclusion coverage before rendering. The next unit is U-MEM-16 standard memory tool executor. Verification: PR #887 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#885 terminating refresh — PR #885 U-MEM-14 runtime memory context composer implementation merged at `14b5f15e`; §12.2 owed follow-on.** | Hash `64d89f7987c4` → `df2fdc6cee97` (state at `14b5f15e`, open PRs empty, fork count 87, batch-57). PR #885 implements the fourteenth atomic unit of the full memory substrate: runtime run-start memory context composition, CP access-mode selection integration, IS retrieval/packet composition, packet hash and policy-ref context state before dispatch, C-MEM-08 injection/denial ledger entries, and ExternalCliRoute ref propagation. The next unit is U-MEM-15 prompt-extension packet fallback. Verification: PR #885 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#883 terminating refresh — PR #883 U-MEM-13 provider-neutral memory tools implementation merged at `e7774fea`; §12.2 owed follow-on.** | Hash `8cc7c2a7d862` → `64d89f7987c4` (state at `e7774fea`, open PRs empty, fork count 87, batch-57). PR #883 implements the thirteenth atomic unit of the full memory substrate: AS-owned provider-neutral memory tool contracts, C-MEM-14 tool vocabulary, policy requirement metadata, durable operation requirements for write-like tools, stable-ref output schemas, and public exports. The next unit is U-MEM-14 runtime memory context composer. Verification: PR #883 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#881 terminating refresh — PR #881 U-MEM-12 provider capability and access-mode selection implementation merged at `e7ec698b`; §12.2 owed follow-on.** | Hash `412732b04e06` → `8cc7c2a7d862` (state at `e7ec698b`, open PRs empty, fork count 87, batch-57). PR #881 implements the twelfth atomic unit of the full memory substrate: CP-owned provider access-mode selection, memory provider capability reflection, external CLI route metadata, policy-aware native provider memory / standard memory tools / prompt-extension packet / explicit denial selection, ledgerable denial reasons, provider-capability binding validation, and CXA guard classification for existing IS carrier imports. The next unit is U-MEM-13 provider-neutral memory tools. Verification: PR #881 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#879 terminating refresh — PR #879 U-MEM-11 retrieval, ranking, and packet assembly implementation merged at `10326ac9`; §12.2 owed follow-on.** | Hash `933b60b0d2cb` → `412732b04e06` (state at `10326ac9`, open PRs empty, fork count 87, batch-57). PR #879 implements the eleventh atomic unit of the full memory substrate: IS-owned retrieval request/result models, deterministic ranking traces, considered-ref exclusion reasons, bounded cited memory packets with stable section ordering and token budgeting, stable request/packet hashes, and actor-aware idempotent `retrieve` memory-operation ledger writes. The next unit is U-MEM-12 provider capability and access-mode selection. Verification: PR #879 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#877 terminating refresh — PR #877 U-MEM-10 derived retrieval index implementation merged at `b6a87d6c`; §12.2 owed follow-on.** | Hash `f5540e746f45` → `933b60b0d2cb` (state at `b6a87d6c`, open PRs empty, fork count 87, batch-57). PR #877 implements the tenth atomic unit of the full memory substrate: an IS-owned rebuildable `semantic/index.jsonl` retrieval-index projection over semantic/procedural canonical records, version/hash binding, stale-index detection, empty-store behavior, bounded metadata retrieval, and a non-authoritative search-accelerator hook. The next unit is U-MEM-11 retrieval, ranking, and packet assembly. Verification: PR #877 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#875 terminating refresh — PR #875 session lifecycle hook guard fix merged at `2f14f57f`; §12.2 owed follow-on.** | Hash `b86ddb6f143b` → `f5540e746f45` (state at `2f14f57f`, open PRs empty, fork count 87, batch-57). PR #875 fixes Stop/closeout handling for archived completed Codex loop state after main advances and fixes Claude SessionStart classification for merged terminating-refresh PRs so expected lag reports as `hash=lag-expected` rather than false `ROADMAP DRIFT`. The next unit remains U-MEM-10 derived retrieval indexes. Verification: PR #875 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#873 terminating refresh — PR #873 hook recovery merged at `e5abe255`; §12.2 owed follow-on.** | Hash `83b07bdc7d28` → `b86ddb6f143b` (state at `e5abe255`, open PRs empty, fork count 87, batch-57). PR #873 fixes the SessionStart next-action parser so the explicit U-MEM-10 frontier is selected before recurring R lanes, adds the regression test, and records the PR #872 title-prefix recovery. The next unit remains U-MEM-10 derived retrieval indexes. Verification: PR #873 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-02 | **Post-#872 corrective fixed-point refresh — PR #872 post-#871 promotion refresh merged at `af9241b0`; §12.2.1 title-prefix recovery.** | Hash `163e05202aff` → `83b07bdc7d28` (state at `af9241b0`, open PRs empty, fork count 87, batch-57). PR #872 already moved the memory substrate frontier to U-MEM-10, but the refresh commit title was `Refresh roadmap after U-MEM-09 merge` rather than the reserved `ops: roadmap status refresh ` prefix, so Codex SessionStart/Stop and Claude roadmap-start hooks treated the expected fixed-point lag as drift. This corrective refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to restore hook recognition; the next unit remains U-MEM-10 derived retrieval indexes. |
| 2026-07-01 | **Post-#871 terminating refresh — PR #871 U-MEM-09 promotion and review queue implementation merged at `a922e61a`; §12.2 owed follow-on.** | Hash `a09934641d1e` → `163e05202aff` (state at `a922e61a`, open PRs empty, fork count 87, batch-57). PR #871 implements the ninth atomic unit of the full memory substrate: Runtime-owned promotion decision application and review queue persistence for proposed semantic/procedural/preference records, operator approve/deny/edit/supersedes API flow, canonical memory-store writes, durable promotion decision ledger writes, and C-MEM-06 preference evidence/scope/injection validation. The next unit is U-MEM-10 derived retrieval indexes. Verification: PR #871 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#869 terminating refresh — PR #869 U-MEM-08 promotion candidate extraction implementation merged at `4a3503ea`; §12.2 owed follow-on.** | Hash `b95cd96e1c31` → `a09934641d1e` (state at `4a3503ea`, open PRs empty, fork count 87, batch-57). PR #869 implements the eighth atomic unit of the full memory substrate: Runtime-owned C-MEM-10 promotion candidate extraction with typed candidate hints/candidates, deterministic candidate IDs, source refs and source memory refs, risk flags, operator-direct vs inferred preference provenance, and policy-resolved review/auto-promotion gating. The next unit is U-MEM-09 promotion and review queue persistence. Verification: PR #869 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#867 terminating refresh — PR #867 U-MEM-07 automatic episodic capture API implementation merged at `26bd0058`; §12.2 owed follow-on.** | Hash `2c7c9025ef83` → `b95cd96e1c31` (state at `26bd0058`, open PRs empty, fork count 87, batch-57). PR #867 implements the seventh atomic unit of the full memory substrate: the Runtime-owned automatic episodic capture API for run start/close, turn completion, tool events, provider routes, failure observations, and compactions; C-MEM-04 episodic records; C-MEM-08 durable `capture` operations; redacted/minimal capture mode; summary provenance; observable failure results; and stable run-scoped `episodic_run` memory IDs. The next unit is U-MEM-08 promotion candidate extraction. Verification: PR #867 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#865 terminating refresh — PR #865 U-MEM-06 canonical memory store implementation merged at `ce0d9353`; §12.2 owed follow-on.** | Hash `2d33f5c1a589` → `2c7c9025ef83` (state at `ce0d9353`, open PRs empty, fork count 87, batch-57). PR #865 implements the sixth atomic unit of the full memory substrate: the IS-owned canonical memory store for path-registry-backed canonical IO, byte-stable envelope record serialization, durable ledger delegation through U-MEM-03 `memory_ops.jsonl`, atomic file replacement, append-only JSONL reads/writes, derived semantic-index stale markers, and audit-mode inactive-record inspection. The next unit is U-MEM-07 automatic episodic capture API. Verification: PR #865 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#863 terminating refresh — PR #863 U-MEM-05 CLI profile schema implementation merged at `820d61b4`; §12.2 owed follow-on.** | Hash `b02a2466c8c7` → `2d33f5c1a589` (state at `820d61b4`, open PRs empty, fork count 87, batch-57). PR #863 implements the fifth atomic unit of the full memory substrate: the IS-owned C-MEM-16 CLI profile schema for profile/import-policy/source/provider-binding vocabularies, generic no-assumption validation, Claude Code/Codex explicit source declarations, built-in external CLI identity bindings including custom over `generic-command`, and import-policy mutation guards. The next unit is U-MEM-06 canonical memory store. Verification: PR #863 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#861 terminating refresh — PR #861 U-MEM-04 memory policy model implementation merged at `c589cb87`; §12.2 owed follow-on.** | Hash `a79a23088ce0` → `b02a2466c8c7` (state at `c589cb87`, open PRs empty, fork count 87, batch-57). PR #861 implements the fourth atomic unit of the full memory substrate: the IS-owned C-MEM-09 memory policy model, default disabled/no-memory policy document, fail-closed promotion/injection behavior, scope-bounded retrieval/injection checks, record-kind filters, native-provider and standard-tool access gates, and package exports. The next unit is U-MEM-05 CLI profile schema. Verification: PR #861 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#859 terminating refresh — PR #859 U-MEM-03 durable memory operation ledger implementation merged at `51a57073`; §12.2 owed follow-on.** | Hash `bb2ae1cd2082` → `a79a23088ce0` (state at `51a57073`, open PRs empty, fork count 87, batch-57). PR #859 implements the third atomic unit of the full memory substrate: the IS-owned durable memory operation ledger for `durable/memory_ops.jsonl`, sidecar-aware response hashing and verification, strict idempotency retry/conflict handling, serialized appends, prior-event-hash chaining, and rebuildable projections keyed by canonical ledger `action_id`. The next unit is U-MEM-04 memory policy model. Verification: PR #859 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#857 terminating refresh — PR #857 U-MEM-02 memory path registry implementation merged at `cb886efd`; §12.2 owed follow-on.** | Hash `77f7c0739a03` → `bb2ae1cd2082` (state at `cb886efd`, open PRs empty, fork count 87, batch-57). PR #857 implements the second atomic unit of the full memory substrate: the IS-owned canonical path registry for manifest, policy, episodic, semantic, procedural, and durable-ledger roots, deployment-surface remapping, traversal rejection, and canonical directory materialization. The next unit is U-MEM-03 durable memory operation ledger. Verification: PR #857 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#855 terminating refresh — PR #855 U-MEM-01 memory record envelope implementation merged at `affcc0f7`; §12.2 owed follow-on.** | Hash `e1acd2f2e2ff` → `77f7c0739a03` (state at `affcc0f7`, open PRs empty, fork count 87, batch-57). PR #855 implements the first atomic unit of the full memory substrate: provider-neutral memory vocabulary, record envelopes, deterministic canonical content hashing, stable memory ID derivation, and supersession/redaction fields in `harness-is`. The next unit is U-MEM-02 memory path registry. Verification: PR #855 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-07-01 | **Post-#853 terminating refresh — PR #853 full memory substrate design packet merged at `e6c834fc`; §12.2 owed follow-on.** | Hash `2e4b695eeabf` → `e1acd2f2e2ff` (state at `e6c834fc`, open PRs empty, fork count 87, batch-57). PR #853 adds the Class 1 fork record for the full memory-layer substrate gap and commits the full design > PRD > ADR > spec > atomic implementation-plan packet for the provider-neutral memory substrate. No implementation code changed; execution starts from U-MEM-01 in a follow-on implementation arc. Verification: PR #853 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-06-30 | **Post-#851 terminating refresh — PR #851 R-600 Codex review pilot closure merged at `7e70eb32`; §12.2 owed follow-on.** | Hash `c5505229727f` → `2e4b695eeabf` (state at `7e70eb32`, open PRs empty, fork count 86, batch-57). PR #851 marks `R-600-codex-out-of-family-review` RESOLVED, adds the pilot closure note, and preserves out-of-family review as a standing gate rather than open roadmap work. Verification: PR #851 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-06-30 | **R-600 Codex out-of-family review pilot closure branch opened at `743837b3`.** | Hash `7f425ded55ab` → `c5505229727f` (state at `743837b3`, open PRs empty, fork count 86, batch-57). The pilot has enough A/B evidence to close: `just codex-review` is institutionalized as the concrete-diff artifact reviewer, the autonomous loop keeps it as a standing gate, and tenant-policy blocks on uncommitted private diffs are handled by local substitute reviews. This branch marks `R-600-codex-out-of-family-review` RESOLVED and adds `.harness/R-600-codex-out-of-family-review-pilot-closure.md`. |
| 2026-06-30 | **Post-#849 terminating refresh — PR #849 R-600 cadence-4 pattern bake-in merged at `c0a82220`; §12.2 owed follow-on.** | Hash `ce0b579e22c8` → `7f425ded55ab` (state at `c0a82220`, open PRs empty, fork count 86, batch-57). PR #849 promotes `full-chain-witness-not-half-proofs` as workflow v1.16 **PD-6 composed-chain non-vacuity witness**, adds the clearance marker, updates R-600/roadmap/pointer/status/dashboard surfaces, and records ZERO new §7.4.7 stale-carry species or production-code/cross-axis cascade owed. Verification: PR #849 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-06-30 | **R-600 cadence-4 pattern bake-in branch opened at `3177fab8`.** | Hash `001c985310ba` → `ce0b579e22c8` (state at `3177fab8`, open PRs empty, fork count 86, batch-57). Cadence-4 enumerated the current memory store (114 files, 558 refs, 101 distinct tokens, 72 at cardinality >=2), found `full-chain-witness-not-half-proofs` promotion-ready, and promotes it as workflow v1.16 **PD-6 composed-chain non-vacuity witness** with clearance marker. ZERO new §7.4.7 stale-carry species owed; no cross-axis cascade or production-code change. |
| 2026-06-30 | **Post-#847 terminating refresh — PR #847 stale R-IF governance closure merged at `4a88a498`; §12.2 owed follow-on.** | Hash `55f9c2203617` → `001c985310ba` (state at `4a88a498`, open PRs empty, fork count 86, batch-57). PR #847 closes stale ACTIVE `R-IF-114` and `R-IF-115` as RESOLVED, preserving the no-implementable-non-recurring selector and leaving only cadence/operator-selected recurring lanes. Verification: PR #847 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
| 2026-06-30 | **Stale ACTIVE governance closure branch — `R-IF-114` and `R-IF-115` re-grounded at `5078af55`.** | Hash `26b5e189c85d` → `55f9c2203617` (state at `5078af55`, open PRs empty, fork count 86, batch-57). `R-IF-114` was already fixed by schema/ledger-derived dashboard tests and the CI tooling lane; `R-IF-115` was already fixed by PR #676's audit artifacts, `tools/closure_gate.py`, and current `just closure-gate` evidence. Both entries are flipped to RESOLVED on this branch. Verification includes stale-active RED witness, dashboard/context focused tests, closure/documentation/certification gates, overlay-check, and diff-check. No implementable non-recurring roadmap arc remains; recurring lanes continue only on cadence or future operator selection. |
| 2026-06-30 | **Post-#845 terminating refresh — PR #845 Codex merged-refresh guard fix merged at `d5bc87a7`; §12.2 owed follow-on.** | Hash `4a55e7577ac9` → `26b5e189c85d` (state at `d5bc87a7`, open PRs empty, fork count 86, batch-57). Fixes the Codex context guard so GitHub merge commits carrying terminating roadmap refresh PRs are recognized as expected dashboard lag when both the merge first-parent diff and the second-parent commit are exactly a terminating-refresh file set; unrelated payloads remain non-exempt. Verification: RED merged-refresh regression, focused merged-refresh tests, context-guard tests, `codex-preflight`, full provider-free `codex-check`, and all PR #845 checks green. No roadmap state transition; no non-recurring close-track arc remains. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-30 | **Post-#843 terminating refresh — PR #843 R-CL-C1 closure certification merged at `62d19553`; §12.2 owed follow-on.** | Hash `3e9de3ba0932` → `4a55e7577ac9` (state at `62d19553`, open PRs empty, fork count 86, batch-57). Adds the final C1 closure certificate, provider-free certification checker/tests, and local-gate wiring. The certificate records the 5-dimension matrix for all contracts/CXA seams/ADRs, Phase-9 bounded-residual review, completeness critic, tenant-policy external-review limitation, and ship evidence. Also fixes the Codex SessionStart/default-branch drift regression by recognizing the actual three-file terminating-refresh shape. Verification: focused C1/guard lane, `closure-certification-check`, `docs-completeness-check`, `closure-gate`, `overlay-check`, `q4-packaging-check`, full provider-free `just check`, and all PR #843 checks green. `R-CL-C1` is now RESOLVED; no non-recurring close-track arc remains. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-30 | **Post-#841 terminating refresh — PR #841 R-CL-D1 documentation suite merged at `e3dba297`; §12.2 owed follow-on.** | Hash `6249f70a9652` → `3e9de3ba0932` (state at `e3dba297`, open PRs empty, fork count 86, batch-57). Adds the top-level runtime docs suite, D1 docs-completeness evidence matrix, provider-free docs checker/tests, and normal local gate wiring so docs drift fails `just check` / `just codex-check`. Corrects the operator config template to match CWD `harness.toml` discovery. Verification: focused docs lane, `docs-completeness-check`, `overlay-check`, `closure-gate`, full provider-free `just check`, and all PR #841 checks green. `R-CL-D1` is now RESOLVED. Next selector moves to **R-CL-C1** closure certification + ship. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#839 terminating refresh — PR #839 R-CL-Q4 portable packaging + deployment merged at `ce344811`; §12.2 owed follow-on.** | Hash `536fc7d16f4f` → `6249f70a9652` (state at `ce344811`, open PRs empty, fork count 86, batch-57). Adds the Q4 provider-free packaging gate, focused tests, and `deploy/images/` Dockerfile/README. The gate proves all 7 workspace wheels build, the runtime wheel exposes the operator CLI entry points, hashed third-party requirements export from `uv.lock` is workspace-free, per-tier image targets are declared, and readiness recipes remain one-command. Verification: focused Q4/readiness lane, `q4-packaging-check`, `overlay-check`, full provider-free `just check`, and all 15 PR checks green. `R-CL-Q4` is now RESOLVED. Next selector moves to **R-CL-D1** documentation suite. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#837 terminating refresh — PR #837 R-CL-Q3 QA/evidence closure merged at `3a95cf76`; §12.2 owed follow-on.** | Hash `6a900cf2daba` → `536fc7d16f4f` (state at `3a95cf76`, open PRs empty, fork count 86, batch-57). Adds the Q3 evidence matrix/checker, proves 123/123 contract test proofs and 31/31 CXA seams wired, adds the six missing authority proof markers, and records tri-tier product probes. Verification: focused Q3 lane, `overlay-check`, full provider-free `just check`, and all 15 PR checks green. `R-CL-Q3` is now RESOLVED. Next selector moves to **R-CL-Q4** portable packaging + deployment. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#835 terminating refresh — PR #835 R-CL-Q2 security review gate merged at `c15dff46`; §12.2 owed follow-on.** | Hash `5a87eac7b169` → `6a900cf2daba` (state at `c15dff46`, open PRs empty, fork count 86, batch-57). Adds the Q2 security review/threat model artifact, fixes mutable GitHub Actions by pinning CI/dashboard/X-AL-3 workflow actions to exact SHAs, and records the OD audit-signature live-signer deferral as accepted residual risk. Verification: focused Q2 lane, full provider-free `just check`, and all 15 PR checks green. `R-CL-Q2` is now RESOLVED. Next selector moves to **R-CL-Q3** QA + 100%-evidence closure. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#833 terminating refresh — PR #833 R-CL-Q1 final review gate merged at `f2831cbe`; §12.2 owed follow-on.** | Hash `2c530f5951cc` → `5a87eac7b169` (state at `f2831cbe`, open PRs empty, fork count 86, batch-57). Adds the final structured Q1 review artifact covering all seven packages plus `tools/`, `justfile`, and `harness.toml.example`; records all required review lanes passed with no open findings; fixes the default local gate so provider-free `just check` stays credential-safe under dotenv/keyring; and verifies the clean-checkout DevEx path with Q1 gate, focused tools tests, full `just check`, and CI. `R-CL-Q1` is now RESOLVED. Next selector moves to **R-CL-Q2** security testing + review. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#831 terminating refresh — PR #831 R-CL-Q1 harness-runtime review evidence merged at `682a3874`; §12.2 owed follow-on.** | Hash `6c799e9f2755` → `2c530f5951cc` (state at `682a3874`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-runtime` Q1 review artifact with all three package review lanes passed, records direct observations across runtime metadata, CLI/API entrypoints, config/provider-secret loading, workflow manifest parsing, stage-5 bootstrap, provider dispatch, MCP hosting/client/tool surfaces, tracing, collector, and provider-free gate evidence. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#829 terminating refresh — PR #829 R-CL-Q1 harness-cxa review evidence merged at `4a576ad8`; §12.2 owed follow-on.** | Hash `27c01a39feda` → `6c799e9f2755` (state at `4a576ad8`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-cxa` Q1 review artifact with all three package review lanes passed, records direct observations across the CP-to-OD audit-write converter, carrier-union coverage, namespace projection, conditional field elision, `entry_core` fallback behavior, OD-owned signing/hash delegation, package metadata, and CXA boundary side-effect discipline, and preserves the fresh-worktree sync/provider-free gate evidence. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#827 terminating refresh — PR #827 R-CL-Q1 harness-od review evidence merged at `a8397d22`; §12.2 owed follow-on.** | Hash `e0e7229585d3` → `27c01a39feda` (state at `a8397d22`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-od` Q1 review artifact with all three package review lanes passed, records direct observations across OD namespace ownership, audit-ledger hashing, cost attribution, redaction/tokenization, sampling/tail-keep boundaries, local-first collector topology, SQLite span persistence/readback, package metadata, and source side-effect discipline, and preserves the fresh-worktree sync/provider-free gate evidence. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#823 terminating refresh — PR #823 R-CL-Q1 harness-as review evidence merged at `535f8ebb`; §12.2 owed follow-on.** | Hash `ad06ffb14c13` → `af6837430f3e` (state at `535f8ebb`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-as` Q1 review artifact with all three package review lanes passed, records direct observations across AS discriminators, sandbox-tier composition, tool contracts, secret handling, MCP fail classes, Anthropic primitive matrices, seam exports, and package metadata, and preserves the fresh-worktree sync/provider-free gate evidence. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#821 terminating refresh — PR #821 R-CL-Q1 harness-is review evidence merged at `c971a7f7`; §12.2 owed follow-on.** | Hash `7915d1762055` → `ad06ffb14c13` (state at `c971a7f7`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-is` Q1 review artifact with all three package review lanes passed, records direct observations across IS path/tier registries, state ledger, prompt manifest, git/worktree primitives, seam exports, and package metadata, and preserves the fresh-worktree sync/provider-free gate evidence. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#819 terminating refresh — PR #819 R-CL-Q1 harness-core review check-path hardening merged at `b9ef7adf`; §12.2 owed follow-on.** | Hash `a68967983fd2` → `7915d1762055` (state at `b9ef7adf`, open PRs empty, fork count 86, batch-57). Adds the partial `harness-core` Q1 review artifact, fixes the fresh-check editable sync gap by making `just check` depend on `codex-sync`, and hardens daemon subprocess smoke by shortening the socket path and surfacing uvicorn serve startup/bind failures as `DaemonStartupError`. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#817 terminating refresh — PR #817 post-#816 refresh + dashboard masthead fallback merged at `fd53e93c`; §12.2 owed follow-on.** | Hash `3c09f86af705` → `a68967983fd2` (state at `fd53e93c`, open PRs empty, fork count 86, batch-57). PR #817 completed the post-#816 refresh and fixed the dashboard live-anchor fallback for squash subjects without `(#PR)`, but it changed generator/test files; this two-file refresh restores the §12.2.1 fixed-point shape. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#816 terminating refresh — PR #816 R-CL-Q1 runtime contract-ID hygiene merged at `a1e426e4`; §12.2 owed follow-on.** | Hash `23b3638f6a40` → `3c09f86af705` (state at `a1e426e4`, open PRs empty, fork count 86, batch-57). Resolves duplicate runtime contract IDs by moving §14.21 `InterStepOutputChannel` to `C-RT-34` and §30 `resume()` to `C-RT-35`, preserving §14.18 `HarnessRunCLI` as `C-RT-29` and §14.19 `WorkflowManifestLoader` as `C-RT-30`; adds the runtime contract-ID uniqueness regression and blocking tools CI wiring. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#814 terminating refresh — PR #814 R-CL-Q1 AS substitution posture drift merged at `15d5f059`; §12.2 owed follow-on.** | Hash `aead3d75874f` → `23b3638f6a40` (state at `15d5f059`, open PRs empty, fork count 86, batch-57). Aligns `harness-as/CLAUDE.md` with the authoritative AS substitution ledger: AS axis 11/11 retired; `H_T-AS-8e` / `H_T-AS-8f` batch-52 `SUBSTANTIVE_RETIRED`; adds regression coverage and closes the Q1 follow-on note. No Q1 sign-off; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#812 terminating refresh — PR #812 R-CL-Q1 unit cite hygiene (`unit_without_code` source U-token carriers) merged at `a61022d5`; §12.2 owed follow-on.** | Hash `64a33d376479` → `aead3d75874f` (state at `a61022d5`, open PRs empty, fork count 86, batch-57). Adds source-level unit tokens for `U-CP-71`, `U-CP-100`, `U-RT-01`, `U-RT-82`, `U-RT-85`, and `U-RT-133` on existing implementation carriers; overlay `unit_without_code` now reports only `U-RT-00` (documented non-unit range marker). No roadmap/ledger arc state closed; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#810 terminating refresh — PR #810 R-CL-Q1 cite hygiene (`code_without_cite` source authority lines) merged at `9076a661`; §12.2 owed follow-on.** | Hash `28e74db8d90f` → `64a33d376479` (state at `9076a661`, open PRs empty, fork count 86, batch-57). Adds parseable authority cites to the 14 source files previously reported by the semantic overlay as `code_without_cite`; Codex review caught the range-cite lookup gap, so `stage_3b_cp_routing.py` now spells out `U-RT-21` through `U-RT-26` and the intermediate unit queries find the file. No roadmap/ledger arc state closed; next selector remains **R-CL-Q1** review evidence toward Q1 sign-off. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-29 | **Post-#808 terminating refresh — PR #808 R-CL-Q1 structured review artifact gate merged at `2694ca30`; §12.2 owed follow-on.** | Hash `cd04d8f8399c` → `28e74db8d90f` (state at `2694ca30`, open PRs empty, fork count 86, batch-57). Adds the provider-free Q1 review artifact schema/checker, just targets, and blocking CI job; no roadmap/ledger arc state closed. **Next selector:** continue `R-CL-Q1` using the structured artifact gate to drive per-package and non-package surface review evidence. Lags HEAD by one commit after this terminating-refresh PR merges (fixed point, §12.2.1). |
| 2026-06-21 | **Post-#680 terminating refresh — PR #680 R-FS-1 `B-HIERARCHICAL-PAUSE` (resumable cascade_policy=pause for HIERARCHICAL_DELEGATION) merged at `3096d15d`; §12.2 owed follow-on.** | Hash `31b3d2da265a` → `9f9e9e5c22d5` (state at `3096d15d`, open PRs empty, fork count 82, batch-57). **The 20th standalone `B-*` arc.** Materializes §25.15.1 `pause → PAUSED` + the §25.18 impl-discretion recursion cascade for HIERARCHICAL_DELEGATION, in two halves: **(a) level-local** worker pause via the v1.42 `FanOutResumeState` (threading `pause_resumable=True` through the HIERARCHICAL dispatch); **(b) cross-recursion-boundary** — a `SUB_AGENT_DISPATCH` worker whose recursive child PAUSED (was SWALLOWED success-equivalent at the dispatch boundary) is surfaced via the typed `SubAgentChildPausedError`, captured into `FanOutResumeState.paused_child_branches` (the THIRD branch disposition: re-dispatch-via-child-resume), and re-entered at the child's cursor on `api.resume` via `StepExecutionContext.child_resume_snapshot` (grandchild's completed steps recovered, NOT re-executed). **Carrier (advisor):** a NEW field on `FanOutResumeState` (HIERARCHICAL IS ORCHESTRATOR_WORKERS at each level), additive default-empty + DROPPED-from-hash-when-empty (byte-compat) + covered transitively; self-nests (recursive Pydantic via one `model_rebuild()`). **impl-to-cleared-spec** (bundled CP spec **v1.44 → v1.45** §1+§2; NO operator gate — §25.18 names the recursion cascade as impl-discretion). Advisor **refused a Half-A-only slice** (not separable) + disqualified the **mislabeled** `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` (EO is SEQUENTIAL not a fan-out barrier — the §25.15.1 cascade `pause` trigger doesn't apply; arc-ledger + spine scope CORRECTED, paired with `B-HANDOFF-PAUSE` as the sequential-cursor shape) + the fork-first `B-ROUTING-MANIFEST-MODEL-FOLD`. **Discriminating full-chain witness** (grandchild completed-before-pause dispatched exactly once across pause+resume; declarative-only — the INFERENCE-child e2e is blocked by a pre-existing runtime deadlock). **Out-of-family Codex hardened it across 4 rounds** (asymptotic convergence, `[[hooks-codex-pilots-decorrelation-validated]]`): R1 a cancellation-race child-pause loss (a sibling-cancelled worker's in-flight `SubAgentChildPausedError` lands in `inflight.exception()` → was recorded terminal `completed` + the snapshot DROPPED) + resumed-child run_id reuse (real [P1]s); R2 pyright `reportConstantRedefinition` gate + missing token attrs; R3 best-effort audit on the pause path + dashboard regen; R4 the closed cardinality-3 `SubAgentResultStatus` enum violated by `result_status="paused"` → extended additively to PAUSED (3→4, C-CP-14 §14.2, advisor-adjudicated 4th outcome, own the now-false "no enum change" claim) + a child-resume `workflow_id` fail-closed guard + the cascade-cancel paused-child disposition (`completed`, not `cancelled`). **Class-3 (spine prose home):** the runtime spec §14.7.2 step-7 child-result mapping + §14.7.4 `ChildWorkflowRunner.pause_snapshot_input` co-land as runtime-internal surfaces; a batched runtime-spec refresh formalizes the §14.7.2 step-7 PAUSED row (impl-ahead-of-prose minor drift). advisor (full-transcript) + out-of-family Codex; all 15 CI checks green; pyright 0/0/0; harness-cp 1112 + 1 xfailed / harness-runtime 1986 non-e2e. `standalone_closed` 19→20; `standalone_registered` 13→12. **Next selector:** ground the forward register + pick (rec: `B-HANDOFF-PAUSE` + `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` [both SEQUENTIAL single-owner pause — pair them, fork-first] / `B-ROUTING-MANIFEST-MODEL-FOLD` [fork-first] / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#679 terminating refresh — PR #679 R-FS-1 `B-FANOUT-PAUSE-PARALLELIZATION` (resumable cascade_policy=pause for the PARALLELIZATION peer fan-out) merged at `006e2255`; §12.2 owed follow-on.** | Hash `b9bfda1ebec9` → `31b3d2da265a` (state at `006e2255`, open PRs empty, fork count 82, batch-57). **The 19th standalone `B-*` arc.** Materializes §25.15.1 `pause → PAUSED` for PARALLELIZATION — the v1.42 `B-FANOUT-PAUSE` (ORCHESTRATOR_WORKERS) shape applied PARALLELIZATION-shaped (NO orchestrator step[0]); the interim `parallelization-pause-resume-not-yet-materialized` FAILED flips to a genuine resumable PAUSED. **PREREQUISITE #678** supplied the cascade harvest. **Carrier fork = Option B (advisor, in-impl, NOT operator):** a NEW 2-field `PeerFanOutResumeState` (`branches`+`branch_count`, NO orchestrator fields), NOT a loosened `FanOutResumeState` (illegal-states-unrepresentable); a 2nd additive `PauseSnapshot.peer_fan_out_resume` field (never co-set with `fan_out_resume`) → existing snapshots byte-identical. **impl-to-cleared-spec** (bundled CP spec **v1.43 → v1.44** §1+§2; NO operator gate; materializes the R-CC-1 §1.1 re-open trigger). **`[[design-substrate-version-identity-hazards]]` caught before overwrite:** v1.43 was ALREADY `B-LAYER-BUDGET-OVERRIDE` (#667) → this delta is **v1.44** (the root/CP CLAUDE.md §1.2/§2.3 spec-head pointers are independently stale at v1.38 — a pre-existing drift the v1.39-v1.43 arcs didn't touch; left as-is per surgical-changes). **Full-chain witness** through the real `api.resume` (CP 12 tests + runtime e2e). **Codex [P2] (decorrelated catch):** stale dashboard snapshot → regenerated gh-down for CI parity (`[[regenerate-roadmap-html-after-source-edit]]`). **CI also caught** a missing `pr:` field on the closed arc-ledger entry (the `test_standalone_projection_matches_ledger_counts` dashboard-projection gate requires every closed arc to carry its PR in status_detail — fixed). advisor (full-transcript) + out-of-family Codex; all blocking CI green; pyright 0/0/0; harness-cp 1110 + 1 xfailed / harness-runtime 1982 non-e2e. `standalone_closed` 18→19. **Next selector:** ground the forward register + pick (rec: `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#678 terminating refresh — PR #678 R-FS-1 `B-PARALLELIZATION-CASCADE` (cascade_policy consumption for the PARALLELIZATION fan-out) merged at `6197a0fd`; §12.2 owed follow-on.** | Hash `dab193c3abb3` → `b9bfda1ebec9` (state at `6197a0fd`, open PRs empty, fork count 82, batch-57). **The 18th standalone `B-*` arc.** PARALLELIZATION (U-CP-86) was built happy-path-only ("U-CP-85 non-dep") → it had **no cascade_policy at all**, so a single branch failure fail-fasted the whole fan-out even under `proceed` (where §25.15.1 mandates PARTIAL with survivors). Materializes §25.15.1 PARALLELIZATION-shaped (peer branches, NO orchestrator step[0]; mirrors `_execute_orchestrator_workers` U-CP-88): proceed→harvest-survivors-PARTIAL / cascade-cancel→FAILED+cancelled-terminals / pause→honest `parallelization-pause-resume-not-yet-materialized` FAILED (the resumable PAUSED is the now-UNBLOCKED follow-on `B-FANOUT-PAUSE-PARALLELIZATION`). **impl-to-cleared-spec** (§25.15 + §25.18 anticipate it; NO design-substrate edit, NO operator gate). **Also closed the CP v1.42 line-23 spec-prose-vs-code drift** (folded into the spine ledger, not a standalone `class_3` doc — that tripped the now-NON-vacuous Codex guard DESIGN_IMPL_MIX [real since #674's full-clone fix]; deleting it net-zeroed the diff). **Codex [P2] (decorrelated catch):** `proceed`'s `gather(return_exceptions=True)` masked a pre-dispatch SETUP error (unbound `StepKind`) as a degradable PARTIAL where the linear path FAILs → fixed by a pre-flight dispatcher resolution failing loud (regression test pins it). **Also-learned (memory):** `just codex-review` materializes a clean tree → reverts uncommitted edits to HEAD (commit before running it). advisor (4 calls) + out-of-family Codex; all 14 CI checks green; harness-cp 1098 / harness-runtime 1981 non-e2e; pyright 0/0/0. `standalone_closed` 17→18. **Next selector:** ground the forward register + pick (rec: the now-unblocked `B-FANOUT-PAUSE-PARALLELIZATION` / `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#677 terminating refresh — PR #677 R-FS-1 `B-INTERSTEP-HANDOFF` (inter-step DATA flow for DECENTRALIZED_HANDOFF; the handoff slice of B-INTERSTEP-NONLINEAR) merged at `4fb3b6c6`; §12.2 owed follow-on.** | Hash `c6219183bb83` → `dab193c3abb3` (state at `4fb3b6c6`, open PRs empty, fork count 82, batch-57). **The 17th standalone `B-*` arc.** Each completed stage-expert's output is recorded INLINE (single-owner sequential, `workflow_driver.py:4933-4943`; NOT via the #648 buffered-branch drain — that serializes concurrent sibling writes, structurally absent here) so the next stage's dispatch reads `most_recent_output()` (stage B sees stage A's output). **impl-to-cleared-spec** (runtime §14.21 C-RT-34 §14.21.7 registers the recording follow-on; NO design-substrate edit), **NO operator gate** (opt-out `inter_step_data_flow=False` byte-identical; `StepDispatcher.dispatch` UNCHANGED; §25.3.3.4 + ADR-F2 single-threaded-write hold). **Slicing tie-breaker (advisor):** the 3 concurrent topologies' post-join synthesis consumer wants ALL siblings (the consumer injects only `most_recent_output()` → soft built-but-vacuous); handoff has exactly one predecessor → genuinely correct, no message-shape discriminator → cleanest first slice. **Full-chain witness** (`[[full-chain-witness-not-half-proofs]]`): CP producer witness + negative-control opt-out + a runtime full-chain test through the REAL `execute_workflow` → `_execute_decentralized_handoff` → REAL `RuntimeLLMDispatcher` over the production `SyncDispatcherFacade` bridge (stage B's actual provider call carries stage A's output). **De-mislabel (advisor-caught):** impl-to-cleared-spec with NO operator decision is NOT a Class-2 fork (§4.3) → the arc record lives in the spine ledger (the prose home), not a `class_N_fork_*.md`; deleting the mislabeled file ALSO correctly resolved the Codex guard's DESIGN_IMPL_MIX (the guard is right, the artifact was wrong). `B-INTERSTEP-NONLINEAR` re-scoped to the 3 concurrent strategies (ORCHESTRATOR_WORKERS, PARALLELIZATION, HIERARCHICAL_DELEGATION); `standalone_closed` 16→17. Class-3 note: §14.21.7 imprecisely lumps handoff under "buffered-branch drain". **Guard sequence:** first CI run HARD-failed `DESIGN_IMPL_MIX` (the mislabeled `class_2_fork`) + `DASHBOARD_SNAPSHOT_STALE` (stale `fork_count` 83 in roadmap.html) → fixed by deleting the file (net-diff drops the DESIGN surface) + regenerating roadmap.html with `fork_count` 82 (gh-down shim → 0 PRs) → all 14 checks green. advisor (4 calls) + out-of-family Codex ("no discrete introduced bug"). **Next selector:** ground the R-FS-1 forward register + pick the next highest-value arc (14 registered + 1 gated; e.g. `B-INTERSTEP-NONLINEAR` concurrent topologies, a `B-FANOUT-PAUSE` sibling, or the `B-EFFECT-FENCE`/`B-EDIT-CARRIER` families). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#676 terminating refresh — PR #676 `R-IF-115` (remaining-build audit surface + closure gate folded into the loop) merged at `67c96183`; §12.2 owed follow-on.** | Hash `846af89ad082` → `c6219183bb83` (state at `67c96183`, open PRs empty, fork count 82, batch-57). Lands the 2-pass audit (`.harness/audit/Remaining_Build_Audit_Report.md` + `Remaining_Build_Register.csv` 48 rows) + binding `Closure_Gate_v1.md` (Tier-1 build-complete gates R-FS-1 / Tier-2 quality-close + 5-dim coverage matrix gates R-CL-C1) + `tools/closure_gate.py` (`just closure-gate[-check]`; delegates to arc_ledger/overlay/substitution_ledger/roadmap, no new source-of-truth; `--check` exits 1 — 4/5 automatable Tier-1, G1.1 fails since R-FS-1 ACTIVE). **Overlay head-scoping (RB-DOC-03):** `contract_without_code` 6 phantoms → 1 (documented C-IS-11) + new head-scoped `unit_without_code` advisory; `overlay-check` 31/31 HARD green, 17 tests pass. `R-IF-115` routes every finding to its R-CL/R-IF home. **Mode-agnostic infra — NOT an R-FS-1 build arc; forward `B-*` register unchanged; next selector unchanged.** Dashboard guard: first run HARD-failed `DASHBOARD_SNAPSHOT_STALE` (R-IF-115 changed the render) → regenerated `roadmap.html` with `open_prs` empty (matching the gh-down CI guard env, since `open_prs` is not normalized) → fresh run after close/reopen all-blocking-green incl. Codex guard. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Maintenance refresh — `chore(lint): exclude vendored .agents/skills/bmad-* from ruff` committed at `18365738`; §12.2 hash re-pin owed.** | Hash `630223ee7a0f` → `846af89ad082` (state at `18365738`, open PRs empty, fork count 82, batch-57). Operator-requested: the previously-parked `.agents/skills/bmad-*` ruff `extend-exclude` (mirroring `.claude/skills/bmad-*`) is now committed so the operator-vendored bmad skill scripts stop tripping the Stop-gate / pre-commit / CI ruff on every session-stop. Pushed together with this terminating refresh (single push → CI runs once on the refresh tip, `_lag_expected`=True → green). Not a dashboard-source change; no arc/R-NNN status change. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#675 terminating refresh — PR #675 R-FS-1 `B-L2-FALLBACK-COMPOSITION` (layered routing composes with the C-RT-16 fallback chain; runtime v1.64 → v1.65 §14.6.1) merged at `4f1ae005`; §12.2 owed follow-on.** | Hash `930732c82a13` → `630223ee7a0f` (state at `4f1ae005`, open PRs empty, fork count 81→82 [+1 B-L2-FALLBACK fork doc], batch-57). **The 16th standalone `B-*` arc** — landed from the unmerged branch `f931003` via cherry-pick (the superseded `r-fs-1-arc-and-unit-map.md` + `roadmap.html` edits DROPPED: #673 retired the map to a pointer stub + made the dashboard `arc-ledger.yaml`-driven). route-once-then-fallback-the-chain (U-RT-114 §14.5.3): SELECTION factored to `routing_core_surface.resolve_routing_trace`; `resolve_routed_binding` runs routing ONCE at the C-RT-16 wrapper (seeds PRIMARY via `_augment_primary`); the inner reverts to a FAITHFUL echo; the #669 interim refuse-guard RETIRED. **Also carried a codex-context-guard fix:** #674's full-clone inadvertently activated `DESIGN_IMPL_MIX` (shallow-blind before — base-ref absent → empty changed_files → never fired) against the §11.4 bundled-absorption pattern EVERY spec-leg `B-*` arc uses → a clearance-marker exemption mirroring X-AL-3's (§4.4) presence-based recognition (advisor-reconciled vs Codex's artifact-tie suggestion — artifact-tying would make this guard stricter than X-AL-3; +2 guard tests, 26 existing green). **Decorrelated review:** advisor (3 rounds: approach + guard-fork + guard-reconcile) + 2× Codex; both [P2]s dispositioned (L3-rationale-span = the registered `B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION`; guard exemption intentional, clean deferral confirmed by grep). All 15 CI checks green (incl. Codex context guard — blocking: PASS). arc-ledger `registered → closed` (standalone_closed 15→16, `--check` green) + follow-on `B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION` registered. Next selector = ground the forward register + pick (rec: `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#655 terminating refresh — PR #655 R-FS-1 `B-EFFECT-FENCE` (at-most-once effect execution; runtime v1.59 → v1.60 §14.22 C-RT-31) merged at `868f9e0c`; §12.2 owed follow-on.** | Hash `559518834b10` → `10fef4ccdb90` (state at `868f9e0c`, open PRs empty, fork count 74→75 [+1 B-EFFECT-FENCE fork doc], batch-57). **The EIGHTH standalone `B-*` arc.** Closes floor-(ii) at-most-once EXECUTION of non-idempotent step side-effects: a crash in the `workflow_driver.py:2031`→`:2336` window (effect fired → per-step ledger COMMIT) re-fires the effect on resume. A hand-rolled (I-6) durable **effect fence** at the `RuntimeToolDispatcher` `call_tool` sink mirrors the U-RT-123 `_claim_resume_revision` crash-atomic `O_EXCL`/`os.link`/`fsync` claim, re-keyed to the per-`(run, step, tool)` `idempotency_key`: WON → fire; LOST → `EffectFenceReservedUncommittedError` → FAILED `RunResult` (honest fail-close, no silent double-fire). COMMIT = the existing per-step ledger entry (the fence adds ONLY the RESERVE). Opt-in `RuntimeConfig.effect_fencing` (default `None` → byte-identical). **At-most-once *per (run, step) invocation*, NOT exactly-once** — a legitimate loop iteration is NOT conflated with a resume re-fire (advisor catch, verified at HEAD `workflow_driver.py:3620-3628`: EO passes the monotonic `entry_index` as `parent_idempotency_key`, fan-out the branch-scoped key — MUST, else IS dedup at C-IS-07 §7.5 drops iteration-2). Fence-scope fork (C10⊥C11) probe-resolved by durable-resume scoping (Reading C) → advisor, not council. **Codex [P2] decorrelated catch (advisor + author missed):** `HARNESS_EFFECT_FENCING=true` was silently ignored → wired into both env loaders (`_ENV_SCALAR_FIELDS` + `_RuntimeEnvSettings`) + regression tests, justified because it gates a CORRECTNESS property (no-silent-failure). **Advisor Check B** → added a by-execution test that the breaker fail-fasts the fence error (1 inner call). **NO operator gate** — additive + opt-in, no committed-invariant sacrifice; §22.1 HITL pause + per-tool classification + durable-auto are spine-registered follow-ons (`B-EFFECT-FENCE-HITL-ROUTE` / `-PER-TOOL` / `-DURABLE-AUTO`; cross-host folds into deferred F-CC). Bundled-absorption: runtime v1.59 → v1.60 §14.22 C-RT-31 + clearance + fork + `harness-runtime/src` + `harness-cp` test + by-execution tests (mechanics + real-dispatcher at-most-once + NEGATIVE CONTROL + fresh-instance restart + driver re-dispatch key-stability) + spine-ledger BUILT + arc-and-unit-map §5 (8 closed · 6 remaining) + roadmap.html. CI 14/14 blocking green; pyright 0/0/0; harness-runtime 1726 (non-e2e); harness-cp 1063 + 1 xfailed; `test_effect_fence.py` 6/6; X-AL-3 green. Next selector = **`B-HITL-PLACEMENT-PER-STEP-PRODUCER`** (recommended; HIGH cross-cutting — binds per-step `hitl_placements` so ALL wrap-time HITL gates incl. #653 fire in production) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#653 terminating refresh — PR #653 R-FS-1 `B-TOOL-GATE` (tool-step MCP-trust HITL gate site; CP v1.35 §19.1.2 Producer ¶ / runtime §14.8.2 step-4c) merged at `51f8c169`; §12.2 owed follow-on.** | Hash `9c6d852700d3` → `559518834b10` (state at `51f8c169`, open PRs empty, fork count 73→74 [+1 B-TOOL-GATE fork doc], batch-57). **The SEVENTH standalone `B-*` arc.** A third `RuntimeHITLGateComposer` at the tool-step registry path feeds the resolved-owning-MCP-host trust tier into the §19.1.2 MCP-trust gate axis (new `make_step_mcp_trust_tier_resolver`, mirroring `make_step_blast_radius_resolver`; `ctx.tool_dispatcher` NOT mutated so the producer-loop/`hitl_tool_loop` read the un-gated dispatcher). **Impl-against-cleared-spec** (`MCPTrustTier` / `GateLevelInput.mcp_trust_tier` / `MCP_TRUST_GATE_LEVEL_FLOOR` all pre-exist; no new contract/field/invariant) → NO operator gate. **Codex [P1] decorrelated catch (advisor + author missed):** the wrap-time gate is placement-driven, but `hitl_placements` is WORKFLOW-level (`WorkflowManifestEntry`, C-CP-17 §17.3) and the frozen+`extra=forbid` per-step `WorkflowStep` has no src binding workflow→per-step placements → `step.hitl_placements` is always `()` through the real loader→driver path → no wrap-time HITL gate fires in production for ANY step kind — a PRE-EXISTING gap shared by ALL wrap-time gate sites (even inference/sub-agent e2e tests use a `_StepWithPlacements` proxy), the unbuilt half of the B3 spine residual → registered `B-HITL-PLACEMENT-PER-STEP-PRODUCER`. Three-claim decomposition (never blurred): (1) wiring correct (real `run_bootstrap`); (2) axis composes when the gate fires (by-execution L0→DENY→{REJECT,RESPOND}→reject-raises; L3→AUTO→delegate); (3) production gate-*firing* awaits the producer arc. First-draft "non-vacuous in production" overclaim corrected to "non-vacuous at the tool gate, at parity." Bundled-absorption: `harness-runtime/src` + by-execution tests ONLY — NO design-substrate edit / spec bump / clearance marker; fork/build-record + spine-ledger BUILT + arc-and-unit-map §5 (7 closed · 7 remaining) + roadmap.html. CI 13/13 blocking green; pyright 0/0/0; harness-runtime 1922 / 10 skipped; CXA-P1 34; dashboard 15; X-AL-3 green. Next selector = **`B-EFFECT-FENCE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#651 terminating refresh — PR #651 R-FS-1 `B-INTERSTEP` (inter-step data-flow channel; runtime v1.59 §14.21 C-RT-34) merged at `a90fe979`; §12.2 owed follow-on.** | Hash `c28fb918789b` → `9c6d852700d3` (state at `a90fe979`, open PRs empty, fork count 72→73 [+1 B-INTERSTEP fork doc], batch-57). **The SIXTH standalone `B-*` arc.** A run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`; default byte-identical) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (`EVALUATOR_OPTIMIZER` the load-bearing case). Dispatch signature UNCHANGED + §25.3.3.4 preserved → NO operator gate. **Daemon-scope hardening took 4 Codex rounds (5 real bugs) + an r5 systemic residual:** invariant 7 honestly bounded to (a) sequential reuse closed by per-run `reset()`; (b) concurrent-within-drain-timeout closed by a per-loop single-flight lock (opt-out default never locks); (c) the concurrent-with-timeout `to_thread`-zombie = a SYSTEMIC reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT lock-closeable → registered `B-INTERSTEP-PERRUN-ISOLATION` (advisor reconcile: FULL-SPEC = registration not build-here; real-but-accepted, NOT looped-to-green). The advisor caught the `B-INTERSTEP-PERRUN-ISOLATION` forward-cite phantom → registered in the spine ledger before merge (`[[spine-ledger-forward-arc-registration]]`). One CI ruff-format RED (a prior commit's consumer-test file left unformatted; repo-wide gate caught what file-scoped local checks missed) fixed pre-merge. Bundled-absorption (runtime v1.59 §14.21 + clearance + fork + impl + by-execution tests incl. negative control + spine-ledger BUILT with 3 follow-ons + arc-and-unit-map 6-closed/7-remaining + roadmap.html). CI all blocking green; pyright 0/0/0; harness-runtime 1915 / harness-cp 1062; substitution 54/54. Next selector = **`B-TOOL-GATE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#648 terminating refresh — PR #648 R-FS-1 `B-NONLINEAR-OVERRIDE-PROVENANCE` (per-step override-ledger entry on all topologies; CP v1.40 §6.6) merged at `f42ed121`; §12.2 owed follow-on.** | Hash `3a819123455c` → `0488904ae18f` (state at `ec64b116`, open PRs empty, fork count 70→71 [+1 B-NONLINEAR fork doc], batch-57). #648 closed the FOURTH standalone arc but missed bumping `tools/test_dashboard_generate.py` (it parses the real arc-map §5 yet sits outside CI's axis-package pytest scope, so the 4-way-count RED went uncaught — same drift class #646 corrected). Per the **#461 precedent** (a refresh carrying a dashboard-test update can't be the clean dashboard-only pair), the test correction lands as its own commit `ec64b116` (3→4 closed / 10→9 remaining) and THIS terminating 2-file refresh pins it, restoring the §12.2.1 fixed-point shape. **The FOURTH standalone `B-*` arc.** The `cp.per-step-override-application` state-ledger entry (U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` site only; now emits on all 6 topologies via the **buffered-branch path** (`append_branch_override_ledger_entry` → each branch's `BufferingLedgerWriter`, drained on the driver thread in branch-index order, realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across topologies (branch-scoping would be an X-AL-3 spec change). Impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh) + stale-carry refresh of the falsified v1.38 note; **NO operator gate** (no committed-invariant sacrifice; additive). advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer STEP_BOUNDARY mis-count; lost-override-on-failed-dispatch; missing CASCADE_CANCEL cancelled-terminal). Bundled-absorption (CP v1.40 + clearance + fork + impl + 13 by-execution per-topology tests + spine-ledger BUILT + arc-and-unit-map 4-closed/9-remaining). CI 14/14; pyright 0/0/0; overlay 323/31-31; substitution 54/54. Next selector = **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (recommended autonomous `B-*`) / `B-TAIL-CONDITIONAL-SAMPLING` (R-420/R-421-gated). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#635 terminating refresh — PR #635 R-FS-1 arc M (Managed Agents executable-consumer contract + production wiring; C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`) merged at `ff9f9737`; §12.2 owed follow-on.** | Hash `a1a0701a1664` → `6ecec018b867` (state at `ff9f9737`, open PRs empty, fork count 67→68 [+1 arc-M fork], batch-57). **M COMPLETE — the FINAL FROZEN child arc's autonomous half (10 of 11 done; only B6 Slice 2 remains).** A NEW `StepKind.MANAGED_AGENTS` (operator-ratified Option B; closed-at-5 Class-2 extension) dispatched by a `ManagedAgentsStepDispatcher` (vendor-runs-the-loop; `SyncDispatcherFacade`-bound; surface-gated to `MANAGED_CLOUD`+opt-in) makes the R-820-live-proven `managed_agents.py` carrier production-reachable. Option A (riding `SUB_AGENT_DISPATCH`) probe-foreclosed (harness-orchestration semantics a vendor session can't honor). Decorrelated-review hardening in-arc: decoupled facade timeout (`step_timeout_seconds` 600s ≠ shared 30s — orphaned-billable-session fix) + cancel-on-give-up + `freeze()` carrier passthrough (Codex). Vendor/credential boundary: live managed-cloud run is the surfaced vendor-gate, never auto-fired; H_T-AS-8f already SUBSTANTIVE_RETIRED (R-820). Bundled-absorption (CP v1.39 + runtime v1.55 + impl + tests + fork + 2 clearance markers + arc-and-unit-map M✅ in the substantive PR per the count-change discipline). advisor (caught the gate-dissolution attempt + facade-timeout/test-gap) + Codex (freeze-drop) clean; CI 4365 passed / 14 skipped / 19 deselected (e2e) / 1 xfailed; pyright 0/0/0; overlay 322 nodes 31/31. Next selector = **B6 Slice 2 (operator-gated; drive to gate) / autonomous standalone `B-*` arcs**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#630 terminating refresh — PR #630 R-FS-1 arc B6 Slice 1 (per-MCP-transport sandbox-tier floor composition; STDIO→TIER_3; runtime spec v1.54 §14.9.8) merged at `1a7dc6e2`; §12.2 owed follow-on.** | Hash `46e6f7ed8878` → `8cb531254be3` (state at `1a7dc6e2`, open PRs empty, fork count 66→67 [+1 B6 fork], batch-57). **B6 Slice 1 COMPLETE** — the stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor` (U-AS-13) via tier-`max()` with the §14.9.8 surface default (new `compose_transport_floor`, SSOT) → STDIO ≥ TIER_3, L2-remote → TIER_4; closes the carved-out ADR-D2 §1.3 STDIO tier-3 transport-floor gap. **C10⊥C11 probe-resolved at ADR-D2 §1.3 in C10's favor** (STDIO Tier-3 regardless of blast-radius, hard floor even at local-dev) → impl-to-cleared-ADR, NO operator gate, sacrifices no invariant. Per-server-uniform PRESERVED; per-tool granularity = Slice 2. **Real out-of-box change (named):** local-dev STDIO needs a driver or bootstrap fail-closes. **Coverage loss surfaced:** in-process-STDIO e2e (`test_r100_ac2`+`test_r_cl_p3`×3) SKIPPED (pre-B6 gap ADR-D2 §1.3 forbids; R-410 covers container completion); U-RT-86 retirement evidence verified ran-and-passed unaffected. advisor (caught the decomposition refinement + a strict-raise reason-attribution bug) + Codex clean; 1821 passed / 24 skipped / 1 xfailed; pyright 0/0/0; overlay 31/31. Bundled-absorption (runtime v1.53→v1.54 + fork + clearance + spine Slice-1/2 split). Forward arcs: `B-PER-TOOL-SANDBOX-TIER` (Slice 2, operator-gated) + `B-MCP-HOST-REMOTE-TRANSPORT`. Next selector = **B6 Slice 2 (operator-gated) / B7 (unblocked autonomous)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#628 terminating refresh — PR #628 R-FS-1 arc B5 (per-deployment-surface memory backend selection; `resolve_backend` non-vacuous; runtime spec §14.12.1) merged at `ae62fdb9`; §12.2 owed follow-on.** | Hash `cf0db8f3ab05` → `46e6f7ed8878` (state at `ae62fdb9`, open PRs empty, fork count 66, batch-57). **B5 COMPLETE** — `MemoryToolRegistry.resolve_backend(deployment_surface)` made surface-discriminating (bootstrap-frozen surface→backend map via `from_surface_map`; default path resolves config-free FILESYSTEM per surface + freezes a fail-closed deferred RT-FAIL for config-less surfaces like `MANAGED_CLOUD`; ACTIVE surface must resolve or bootstrap aborts; active-surface dispatch byte-identical; anti-vacuity = constructs-vs-raises). Impl-to-cleared-spec (impl-discretion per plan v2.14 U-RT-80 AC + §14.12.7 — no fork / no design-substrate / no new contract / no CXA seam; overlay 31/31). #628 (substantive) bundled the arc-and-unit-map (B5→done 8/11, B6→next) + spine forward-arc registration (`B-MEMORY-SURFACE-BACKEND-IMPLS`: the spec'd-but-unbuilt `ENCRYPTED_FILESYSTEM`+`OPERATOR_DEFINED` backends) + regen roadmap.html in the SUBSTANTIVE PR (the #623 build-progress-source lesson → B5 = 2 PRs, not 3). advisor (design + pre-done) + Codex clean; harness-runtime 1811 passed / 10 skipped; pyright 0/0/0. Next selector = **B6 (per-tool sandbox-tier + STDIO transport-floor; fork-owed)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#626 terminating refresh — PR #626 (substantive arc-map + status refresh post-#625) merged at `35886ef9`; terminating 2-file refresh owed.** | Hash `d883470ddab1` → `cf0db8f3ab05` (state at `35886ef9`, open PRs empty, fork count 66, batch-57). #626 bundled the arc-and-unit-map (dashboard build-progress source, #623) with roadmap_status.md → a substantive dashboard refresh (outside the `hook_is_dashboard_only_set` 2-file set), so it owed this terminating 2-file refresh to reach the §12.2.1 fixed point (same shape as #623→#624). Next selector unchanged = **B5**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#625 terminating refresh — PR #625 R-FS-1 arc CA (`RunResult.cost_attribution` rollup; runtime spec v1.53 §9 C-RT-09) merged at `7861171d`; §12.2 owed follow-on.** | Hash `3c31942c5c6b` → `d883470ddab1` (state at `7861171d`, open PRs empty, fork count 65→66 [+1 CA fork], batch-57). **CA COMPLETE** — run-result cost rollup wired (`CostRecordAccumulator` holder + 4 per-dispatch wrappers + `_build_run_result` PER_PROVIDER_AND_MODEL rollup). Axis empirically forced (advisor + HEAD body-grounding overturned the sweep's PER_PROVIDER_DISCRIMINATOR lead — production dispatch-type tags ∉ `CrossFamilyTag`). Pydantic-freeze-copies-typed-list bug caught by advisor pre-merge + fixed with the by-reference holder + a freeze()-traversing regression test. Forward items registered: `B-COST-DISCRIMINATOR-TAXONOMY` (the per-family rollup) + (prior) `B-NONLINEAR-OVERRIDE-PROVENANCE`. arc-and-unit-map refreshed (CA→done 7/11, B5→next) per the #623 build-progress-source lesson. Next selector = **B5 (memory backend per deployment surface)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#623 terminating refresh — PR #623 fix(dashboard): reflect B4 completion in the arc-and-unit-map (build-progress source) + deploy trigger; merged at `d94d78d2`; §12.2 owed follow-on.** | Hash `e89a78305084` → `3c31942c5c6b` (state at `d94d78d2`, open PRs empty, fork count 65, batch-57). Operator-flagged: the published roadmap showed CONFLICTING B4 signals (roadmap_status.md sections said B4✅/CA-next; build-progress masthead/arc-strip/cards parsed from the stale `r-fs-1-arc-and-unit-map.md` said 5/11/B4-next). Root cause: the arc-and-unit-map was never refreshed through any B4 slice AND was missing from the deploy path filter. Fixed: arc-map B4→done (6/11) + as-built units + CA→next; `generate.py` 0-new-unit render; deploy path filter += the arc-map. Verified by in-browser render (6/11, B4✓, CA▶next). Next selector unchanged = **CA**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#621 terminating refresh — PR #621 R-FS-1 B4 Slice 4 (per-step ROLE override + linear-path indexing; CP v1.38 + runtime v1.52 §14.5.3 invariant-2/3 relaxation) merged at `abb67a99`; §12.2 owed follow-on.** | Hash `5c8577ce9942` → `e89a78305084` (state at `abb67a99`, open PRs empty, fork count 64→65 [+1 B4-Slice-4 fork doc], batch-57). **B4 COMPLETE** — Slice 4 closes the tail (operator-ratified §14.5.3 relaxation; Option-B composition-time fold, runtime dispatch byte-unchanged; full-stack consumer-e2e green). Next selector = **CA (cost aggregate)** — `RunResult.cost_attribution` rollup (per-dispatch built; run-result rollup + accumulator owed; small pre-authorized C-RT-09 fork). Forward item registered: `B-NONLINEAR-OVERRIDE-PROVENANCE` (Codex-surfaced; pre-existing linear-only override-ledger emission, all dimensions). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#619 terminating refresh — PR #619 R-FS-1 B4 Slice 3 (per-step PROMPT override, CP v1.37) merged at `70ca4628`; §12.2 owed follow-on.** | Hash `4d03a1931cbd` → `5c8577ce9942` (state at `70ca4628`, open PRs empty, fork count 63→64 [+1 B4-Slice-3 fork doc], batch-57). B4 Slice 3 closed; next selector = **B4 Slice 4** (per-step ROLE gate + linear-path role indexing). Session-start hook had flagged expected mid-arc feature-branch drift (computed `f42f967c144c` ≠ dashboard `4d03a1931cbd`) — resolved by this post-merge refresh, not a workspace divergence. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-07-03 | **Post-#909 terminating refresh — PR #909 U-MEM live confirmations merged at `6a9cb627`; §12.2 owed follow-on.** | Hash `f3f025ab55ab` → `c3ae3c660bc2` (state at `6a9cb627`, open PRs empty, fork count 87, batch-57). PR #909 records the live Anthropic native-memory confirmation and the Claude Code / Codex external CLI auth confirmations, adds the corresponding e2e resume tests, and leaves Antigravity, legacy Gemini, and generic-command as explicit local live gates until their CLIs/probe are available. Verification: PR #909 all checks green; this terminating refresh updates `.harness/roadmap_status.md` and regenerates `tools/dashboard/roadmap.html` to pin the merge commit fixed point. |
