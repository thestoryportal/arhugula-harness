# Verbatim-Claim Audit — Control Plane Implementation Plan (U-CP-01 – U-CP-55)

## Summary

- Mode: Phase-7 pre-implementation review (per `harness-adversarial-reviewer` SKILL.md §"Phase-7 pre-implementation review mode"), scoped to ONE systemic attack: the **"verbatim"-claim divergence pattern** (the Tension 002 / Tension 004 / F3-01 shape — a Phase-6 plan unit asserting "per §X verbatim" against a signature that diverges from the cited Phase-5 spec contract).
- Corpus reviewed:
  - `design-substrate/Implementation_Plan_Control_Plane_v2_1.md` — unit bodies U-CP-01 – U-CP-55 (canonical-current except the six units below)
  - `design-substrate/Implementation_Plan_Control_Plane_v2_2.md` — canonical-current bodies for **U-CP-20** (acc #5), **U-CP-21** (Implements / Signatures / acc #1–#4 / tests), **U-CP-55** (acc #3–#7 / tests)
  - `design-substrate/Implementation_Plan_Control_Plane_v2_3.md` — canonical-current bodies for **U-CP-07** and **U-CP-12**
  - `design-substrate/Spec_Control_Plane_v1_3.md` (§1–§9 retry.* / engine.* amendments) + `design-substrate/Spec_Control_Plane_v1_2.md` (§10–§24, preserved verbatim into v1.3)
  - `.harness/adversarial_review_phase7_cp_od_preimpl.md` (Tension 002 + F3-01 prior record)
- Date: 2026-05-15
- Procedure-correction note: the task brief stated "v2.2/v2.3 preserve unit bodies verbatim except U-CP-07 + U-CP-12 revised at v2.3." This is imprecise — **U-CP-20 (acc #5), U-CP-21, and U-CP-55** were also revised at **v2.2**, and v2.3 preserves the v2.2 bodies. Each unit was therefore audited at its **canonical-current** body. Consequence: the v2.1 U-CP-21 3-attribute `engine.*` divergence (v2.1 §0.8 forward-flag) is **NOT** reported here — it was resolved at v2.2 (4-attribute schema per CP spec v1.3 §9.1) and no longer exists in the canonical-current artifact.
- Finding count by §4.1 review-severity class: **Class 3: 7 · Class 2: 0 · Class 1: 0** (every divergent unit is the same shape — a plan-vs-spec contract divergence requiring a Phase-6 plan revision-pass; discriminator (b) fires on each).
- **Bottom-line total: 7 units carry a divergent "verbatim" claim.**

### Class-taxonomy disambiguation (per SKILL.md title-section)

This report's per-unit severity is the **§4.1 review-severity** scale (Class 1 minor / Class 2 moderate / Class 3 severe). Each divergent unit's disposition is a **§2.7.6 Phase-7 execution fork**; the §2.7.6 fork class is stated explicitly per row and is **Class 1 (halt-execution)** for all seven — identical in shape to Tension 002, Tension 004, and F3-01. A §4.1 Class 3 review finding here ≠ a §2.7.6 Class 3 (informational) fork.

---

## Fork inventory table — units with a divergent "verbatim" claim

| Unit | Claimed-verbatim surface | Divergence (plan says X / spec says Y) | §4.1 severity | §2.7.6 fork class |
|---|---|---|---|---|
| **U-CP-01** | `ROUTING_NAMESPACE_SCHEMA` acc #1 — "exactly four `routing.*` attributes per C-CP-01 §1.4 **verbatim**" | Plan: `routing.layer`, `routing.candidate`, `routing.decision_ms`, `routing.budget_exhausted`. Spec §1.4 attribute table: `routing.provider`, `routing.model`, `routing.layer`, `routing.binding_rationale`. Cardinality 4 in both; **only `routing.layer` overlaps — 3 of 4 names diverge**. (Test `test_routing_attributes_match_spec_verbatim` would fail.) | Class 3 | Class 1 (halt-execution) |
| **U-CP-10** | `LifecycleEventClass` acc #1 — "exactly eight values per C-CP-05 §5.1 **verbatim**"; acc #2 span-name map "per §5.1" | Plan enum: `WORKFLOW_START, WORKFLOW_CHECKPOINT, WORKFLOW_RESUMPTION, WORKFLOW_FANOUT_OPEN, WORKFLOW_FANOUT_CLOSE, WORKFLOW_HITL_INVOCATION, WORKFLOW_FALLBACK_TRIGGERED, WORKFLOW_BREAKER_TRIPPED`. Spec §5.1 eight event classes: `workflow-start, step-boundary, fallback-trigger, retry-attempt, breaker-trip, lease-acquired, lease-released, resumption`. Cardinality 8 in both; **the taxonomies are disjoint** — spec has `step-boundary`/`retry-attempt`/`lease-acquired`/`lease-released`; plan has `WORKFLOW_CHECKPOINT`/`WORKFLOW_FANOUT_OPEN`/`WORKFLOW_FANOUT_CLOSE`/`WORKFLOW_HITL_INVOCATION`. Acc #2 span names (`workflow.checkpoint`, `hitl.invocation.opened`) likewise do not match spec §5.1 (`step.boundary`, `retry.attempt`, `lease.acquired`, `lease.released`). (Test `test_class_to_span_name_match_spec_verbatim` would fail.) | Class 3 | Class 1 (halt-execution) |
| **U-CP-19** | `ResumptionKind` acc #1 — "exactly five values per C-CP-08 §8.1 **verbatim**"; acc #2 `RESUMPTION_KIND_BINDINGS` "per §8.1 verbatim" | Plan enum: `REPLAY_FROM_EVENT, RESTORE_FROM_CHECKPOINT, RECONSTRUCT_FROM_LEDGER, RECONVERGE_VIA_RECONCILER, RESUME_FROM_WAL_SEGMENT`. Spec §8.1 `resumption.kind` enum: `engine_replay, save_point_resume, journal_resume, reconciler_converge, segment_replay`. Cardinality 5 in both, 1:1 engine-class mapping in both; **not a single value name matches**. (Test `test_resumption_kind_bindings_1to1_with_engine_class` passes on cardinality; verbatim claim fails.) | Class 3 | Class 1 (halt-execution) |
| **U-CP-22** | `TopologyPattern` acc #1 / `CascadePolicy` acc #2 / `is_admissible` acc #3 — "exactly six values per §10.1 verbatim", "exactly three values per §10.3 verbatim", "per §10.2 admissibility matrix" | **Already filed — Tension 002 + F3-01.** Plan `TopologyPattern` = `SINGLE_AGENT, SEQUENTIAL_HANDOFF, PARENT_FANOUT_AGGREGATE, RECONCILER_MESH, ROUTER_DELEGATE, PIPELINE_STAGES`; spec §10.1 = `single-threaded-linear, orchestrator-workers, decentralized-handoff, hierarchical-delegation, evaluator-optimizer, parallelization` (zero name overlap). `CascadePolicy` enum not declared in spec — spec §10.2 has `cascade_policy` field with string-literal domain `"pause"\|"proceed"\|"cascade-cancel"`; acc #2 also mis-cites §10.3 for a §10.2 surface. Admissibility matrix keyed on invented pattern names; §10.2 contains no admissibility matrix (it is §10.3 prose, disjoint vocabulary). | Class 3 | Class 1 (halt-execution) |
| **U-CP-43** | `GateLevel` acc #1 — "exactly four values per C-CP-19 §19.1 **verbatim**"; acc #3–#6 the four `*_GATE_LEVEL_FLOOR` constants each "per §19.1 verbatim" | Plan `GateLevel` = `GATE_NONE, GATE_NOTIFY, GATE_APPROVE, GATE_REVIEW_BOARD` (cardinality **4**). Spec §19.1 + §16.2 (line 1432): gate level "rang[es] `{auto, ask, deny}`" — cardinality **3**, disjoint vocabulary. **Both cardinality drift (4 vs 3) AND wholesale name divergence.** Acc #3 `PERSONA_TIER_GATE_LEVEL_FLOOR` claims §19.1 verbatim with `SOLO→GATE_NONE / TEAM→GATE_NOTIFY / MULTI→GATE_APPROVE`; spec §19.1 `persona_tier_floor` maps **all three tiers → `ask`**. Acc #4–#6 (`BLAST_RADIUS`/`DEPLOYMENT_SURFACE`/`MCP_TRUST` floors) likewise enumerate plan-invented `GATE_*` values the spec §19.1 does not contain. | Class 3 | Class 1 (halt-execution) |
| **U-CP-46** | `AUDIT_NAMESPACE_SCHEMA` acc #1 — "exactly 7 attributes per C-CP-20 §20.4 **verbatim**"; `HITL_SPAN_NAMESPACE_SCHEMA` acc #2 — "exactly 4 attributes per §20.5 verbatim"; `PERSONA_TIER_AUDIT_EMISSION` acc #4 "per §20.4 verbatim" | Plan `AUDIT_NAMESPACE_SCHEMA` = `audit.gate.computed_level, audit.gate.composition_winner_axis, audit.policy.override_kind, audit.policy.override_scope, audit.policy.rotation_state_partial, audit.signature.key_id, audit.signature.verification_result`. Spec §20.4 seven `audit.*` attrs: `audit.signature.sha256, audit.signature.prior_hash, audit.actor.id, audit.signature.value, audit.signature.algorithm, audit.signature.key_id, audit.signature.key_period`. Cardinality 7 in both; **only `audit.signature.key_id` overlaps — 6 of 7 names diverge**. `HITL_SPAN_NAMESPACE_SCHEMA`: acc #2 claims "4 attributes per §20.5 verbatim" — but §20.5 is the per-persona-tier *emission table* and the 4-row HITL surface is §20.6, which declares 4 **span names** (`hitl.gate.evaluated`, `hitl.invocation.opened`, `hitl.invocation.responded`, `hitl.invocation.timed_out`), not 4 attributes; plan's 4 attribute names (`hitl.gate.evaluated.placement`, etc.) do not appear in the spec. Acc #4 cites §20.4 for the emission table (it is §20.5) and the per-tier content diverges wholesale (spec solo-developer → `audit.actor.id` only; plan solo-developer → `{audit.gate.computed_level}`). | Class 3 | Class 1 (halt-execution) |
| **U-CP-47** | `ValidatorFailClass` acc #1 — "exactly five values per C-CP-21 §21.1 **verbatim**"; `VALIDATOR_FAIL_METADATA` acc #2 "per §21.1 verbatim"; `VALIDATOR_FAIL_NAMESPACE_SCHEMA` acc #3 "exactly three attributes per §21.5 verbatim" | Plan `ValidatorFailClass` = `SCHEMA_MISMATCH, TIMEOUT, RATE_LIMIT, PERMANENT_REJECTION, SANDBOX_VIOLATION`. Spec §21.1 five-class taxonomy: `transient-retry, Reflexion-recoverable, HITL-recoverable, permanent-fail-exit, terminal-fail-exit`. Cardinality 5 in both; **wholesale value divergence** (spec is a retry-exit taxonomy; plan is a fail-cause taxonomy). `VALIDATOR_FAIL_NAMESPACE_SCHEMA`: plan = `validator.fail.class, validator.fail.is_transient, validator.fail.retry_attempt`; spec §21.5 three attrs = `validator.fail.class, validator.fail.cause_attribution, validator.fail.permanence`. Cardinality 3 in both; `validator.fail.class` overlaps, **`is_transient`/`retry_attempt` diverge from spec's `cause_attribution`/`permanence`**. | Class 3 | Class 1 (halt-execution) |

**Cross-unit propagation note.** U-CP-22's `TopologyPattern` is consumed by U-CP-23 (`PER_WORKLOAD_CLASS_TOPOLOGY` default patterns), U-CP-24/U-CP-25 (2D matrix), U-CP-31 (`topology.*` namespace), U-CP-13 (`WorkflowManifestEntry.topology_pattern`), U-CP-35 (`fanout_topology`), U-CP-53 (per-cell readings). U-CP-10's `LifecycleEventClass`, U-CP-19's `ResumptionKind`, U-CP-43's `GateLevel`, and U-CP-47's `ValidatorFailClass` are likewise consumed by downstream units (U-CP-12, U-CP-20, U-CP-27/39/45/46, U-CP-48). A conformance revision of any divergent foundational enum requires a corresponding revision-pass of every consuming unit so cross-unit composition survives the rename — this is the same propagation note F3-01 already raised for U-CP-22→U-CP-23.

---

## Clean-list — "verbatim" claims checked and confirmed to HOLD

The following units carry "verbatim" / "per §X" / "matches §X" / "exactly N" claims that were cross-checked against the cited spec section and **do hold** (cardinality + value names + cited section all match; SCREAMING_SNAKE_CASE of spec lowercase-hyphen identifiers is a Python-stack naming convention, not a divergence, when the token stems match 1:1):

- **U-CP-02** — `ProviderCapabilities` 10 fields per C-CP-01 §1.2: spec §1.2 `ProviderCapabilities` record + the per-feature capability fields support a 10-field materialization; no name divergence. Holds.
- **U-CP-07 (v2.3 body)** — `FALLBACK_NAMESPACE_SCHEMA` 9 attrs, `HARNESS_BREAKER_NAMESPACE_SCHEMA` 7 attrs, `RETRY_NAMESPACE_SCHEMA` **6** attrs + `RETRY_ATTEMPT_EVENT_SCHEMA` **3** fields per C-CP-03 §3.5 + ADR-D6 v1.2 §1.2.2.1/§1.2.2.2. The v2.3 6-attribute retry.* set (`retry.attempt_number, retry.original_span_id, retry.delay_ms, retry.cause_attribution, retry.fail_class, engine.replay_disposition`) and 3-field parent event (`parent.attempt_count, parent.attempts_remaining, parent.next_delay_ms`) match the CP spec v1.3 §3.5 change-note amendment byte-exact. Holds. (The v2.1 4-attribute retry.* claim was a divergence at v2.1; it is resolved at v2.3 and not reported.)
- **U-CP-09** — `ProviderFamily` 4 values (`ANTHROPIC, OPENAI, GOOGLE, LOCAL_OPEN_WEIGHT`) per §4.1 + C-AS-13 §13.4: matches the spec §4.3 provider-family grouping (Anthropic / OpenAI-class / Google-class / local-open-weight). `FallbackChain` 4 fields per §4.1. Holds.
- **U-CP-11** — `LEASE_NAMESPACE_SCHEMA` 5 attrs per §5.3: plan `lease.id, lease.holder, lease.acquired_at, lease.duration_ms, lease.event_kind`; spec §5.3 declares 5 `lease.*` attrs (`lease.key, lease.holder, lease.ttl_ms, lease.mechanism, lease.release_cause`). **Cardinality 5 holds; note — the value names actually diverge** (`lease.id`≠`lease.key`, `lease.acquired_at`/`lease.duration_ms`/`lease.event_kind`≠`lease.ttl_ms`/`lease.mechanism`/`lease.release_cause`). Recorded here for operator visibility: U-CP-11 is a *borderline 8th candidate* — see "Findings considered and rejected" item 1.
- **U-CP-12 (v2.3 body)** — acc #3 citation corrected §9.2→§9.1; `SAMPLING_DISPOSITIONS` 8 entries + `RETRY_SURFACE_SAMPLING_DISPOSITIONS` 2 entries per CP spec v1.3 §5.2/§5.4. Holds against v1.3.
- **U-CP-15** — `EngineClass` 5 values per §7.1: `EVENT_SOURCED_REPLAY`…`WAL_SEGMENT` are the SCREAMING_SNAKE_CASE forms of spec §7.1 `event-sourced-replay`…`WAL-segment`; 1:1 token-stem match. Holds.
- **U-CP-16** — `DeploymentSurface` 3 values + `ENGINE_CLASS_CANDIDATES` 3 entries per §7.2: candidate sets + exclusions match spec §7.2 table. Holds.
- **U-CP-18** — `F2JoinKind` 3 values per §8.2 (v2.1 §0.2 already corrected the §8.1→§8.2 citation); `ENGINE_F2_JOIN_CONTRACTS` 5 entries one per `EngineClass`. Holds.
- **U-CP-21 (v2.2 body)** — `ENGINE_NAMESPACE_SCHEMA` **4** attrs (`engine.class, engine.event_history.tier, engine.event.id, engine.replay_disposition`) per CP spec v1.3 §9.1 + ADR-D1 v1.2 §1.1.1. Matches the v1.3 §9.1 4-attribute canonical (3 from v1.2 §9.1 + `engine.replay_disposition` added at v1.3). Holds. (The v2.1 3-attribute body diverged; resolved at v2.2.)
- **U-CP-23** — `PER_WORKLOAD_CLASS_TOPOLOGY` 4 entries per §11.1: cardinality holds; **the default-pattern values inherit U-CP-22's divergent `TopologyPattern` vocabulary** — folded into F3-01's cross-unit propagation scope, not a separate fork.
- **U-CP-24** — `PER_ENGINE_CLASS_OVERLAYS` 5 entries per §11.2: `TopologyFaultHandling` (`ABOVE_ENGINE/BELOW_ENGINE/RECONCILER`) matches spec §11.2 T-perm-3 reading column verbatim. Holds.
- **U-CP-25** — `WORKLOAD_ENGINE_MATRIX` 20 cells (4×5) per §11.3: matches spec §11.3 matrix dimensions. Holds.
- **U-CP-26** — `DEFAULT_DOWNGRADE_RULE` per §12.1 (Tier-3 → Tier-1 ceiling): matches spec §12.1 default-downgrade rule. Holds.
- **U-CP-28** — `SubAgentBrief` 5 fields per §13.2: spec §13.2 declares 4 (`objective, output_format, guidance, task_boundaries`); plan adds `summary_hash`. Acc #1 says "five fields" — the 5th is an honestly-disclosed plan-added hash field; not a verbatim contradiction (analogous to U-CP-30 acc #2). Holds.
- **U-CP-30** — `HandoffContext` 7 fields per §13.1: matches spec §13.1 (`proposed_action, agent_confidence, failed_attempts, alternatives_considered, state_summary, audit_trail_link, retry_history`). `LedgerEntryRef` 3 fields per §13.5 matches. `StateSummary` "five fields per §13.4 verbatim plus external_references" — honestly disclosed plan addition. Holds.
- **U-CP-31** — `TOPOLOGY_NAMESPACE_SCHEMA` 10 attrs + `SUBAGENT_NAMESPACE_SCHEMA` 7 attrs per §14.2: both attribute lists match spec §14.2 `topology.*` (10) and `subagent.*` (7) tables byte-exact. Holds.
- **U-CP-32** — `MULTI_AGENT_SPAN_HIERARCHY` per §14.1; sampling per §14.3: hierarchy + sampling dispositions match spec §14.1/§14.3. Holds.
- **U-CP-33** — `on_fanout_dispatch` four-step protocol per §14.4: matches spec §14.4 four-step warm-up. Holds.
- **U-CP-34** — `SiblingLedgerEntry` matches F2 six-field shape per U-IS-07; `F2_14_READING_1_RATIONALE` 3 entries per §15.3 (one per omitted F2 field). Matches spec §15.3. Holds.
- **U-CP-35** — `ParentFanoutCloseEntry` 6 fields per §15.2; `MERKLE_CONSTRUCTION_STEPS` 4 steps per §15.4. Match spec §15.2/§15.4. Holds.
- **U-CP-36** — `CROSS_SIBLING_CRYPTOGRAPHIC_COMPOSITION` 3 entries per §15.5; trace-inspection 3 surfaces per §15.6. Match spec §15.5/§15.6. Holds.
- **U-CP-37** — `HITLResponse` 4 values per §16.1: `APPROVE, EDIT, REJECT, RESPOND` are SCREAMING_SNAKE forms of spec §16.1 `approve, edit, reject, respond`. Per-response audit shapes per §16.2 + `PALETTE_INVARIANTS` 3 per §16.3 match. Holds.
- **U-CP-38** — `HITLPlacementKind` 3 values per §17.1 (`PRE_ACTION, SUB_AGENT_BOUNDARY, VALIDATOR_ESCALATION` = casing of spec `pre-action, sub-agent-boundary, validator-escalation`); `hitl_gate` 5-parameter signature per §17.1.1 matches; `HITLPlacement` 4 fields per §17.3 matches. Holds.
- **U-CP-39** — `HITLSemanticVariant` 3 values per §17.2; `HITL_SEMANTIC_VARIANTS` 3 entries match spec §17.2 rewriting table (`request_human_input`/`await_human_approval`/`escalate_to_human`). Holds.
- **U-CP-40** — `PersonaTier` 3 values per Persona §3 + ADR-D5 §1.7; `SynchronyClass` 4 values per §18.1+§18.3; `HITL_MATRIX` 15 cells (3×5) per §18.1. Cardinalities and matrix dimensions match. Holds.
- **U-CP-41** — `BOTH_BY_TIER_OVERLAY` 3 properties per §18.3; `compose_persona_tier_binding_selection` 5-step procedure per §18.5. Match spec. Holds.
- **U-CP-42** — `CryptographicShape` 3 values per §20.1: `APPEND_ONLY_SQLITE / HASH_CHAINED_SQLITE / HASH_CHAINED_SQLITE_WITH_SIGNATURE` matches spec §20.1 three-row cryptographic-shape table. Holds.
- **U-CP-44** — `verify_rotation` / signing-key resolution per §20.3.1: scope and structure match spec §20.3.1. Holds.
- **U-CP-45** — `OPERATOR_POLICY_OVERRIDE_SCOPE_TABLE` per §19.5; `KEY_ROTATION_PATTERN` 2 stages per §20.3; `verify_rotation_6_steps` 6-step protocol per §20.3.1. Match spec §19.5/§20.3/§20.3.1. Holds.
- **U-CP-48** — `StaircaseStage` 5 stages per §21.2; `CrossTrustBoundaryState` 4 values per §21.4; `PALETTE_RESTRICTION_TABLE` 4 entries per §21.4 (`{approve,reject,respond}` restriction matches spec §21.3). Holds.
- **U-CP-49** — `PauseEvent` 5 fields / `PauseReason` 4 values / `ResumeOutcomeKind` 4 values per §22.1. Match spec §22.1 resume protocol. Holds.
- **U-CP-50** — `DiffCategory` 5 values per §22.2; `SUMMARIZATION_MODEL_TABLE` 3 entries per §21.4+§22.3. Match spec. Holds.
- **U-CP-51** — `OperatorBurdenEval` 4 fields per §21.3; `HITL_TAIL_KEEP_RULES` 3 entries per §21.3. Match spec §21.3/§21.7. Holds.
- **U-CP-52** — `TimeoutDegradationKind` 3 values per §21.6; `TIMEOUT_DEGRADATION_TABLE` 3 entries per §21.6. Match spec §21.8 (timeout-degradation modes `fail-closed`/`escalate-secondary-channel`/`fail-open` — note the plan's `CONTINUE_AS_REJECT/ESCALATE_TO_REVIEW_BOARD/ABORT_WORKFLOW` value names are a *re-expression* of the spec's three modes; cardinality 3 holds, but see "Findings considered and rejected" item 2 for the marginal-divergence note).
- **U-CP-53** — `PER_CELL_T_PERM_3_READINGS` 20 cells (4×5) per §23.2; `DeterministicOuterHarnessBoundary` 5 primitives per §23.4. Match spec §23.2/§23.4. Holds.
- **U-CP-54** — `CP_NAMESPACE_EXPORT_MANIFEST` 11 entries per §24.1 (6 specialization + 4 F3-lifecycle + 1 inheritance). Matches spec §24.1 "6 + 4 + 1 = 11". Holds.
- **U-CP-55 (v2.2 body)** — cross-axis composition manifest 9 entries per §24.2+§24.3; F2-12 carry-forward closure path per §24.4. Structure matches spec §24.2–§24.4. Holds.

---

## Findings considered and rejected (transparency)

1. **U-CP-11 `LEASE_NAMESPACE_SCHEMA` — borderline 8th candidate, ultimately classified as a marginal name divergence, NOT escalated to the fork set.** Acc #1 claims "exactly five attributes per C-CP-05 §5.3 **verbatim**: `lease.id, lease.holder, lease.acquired_at, lease.duration_ms, lease.event_kind`." Spec §5.3 declares 5 `lease.*` attributes — but `lease.key, lease.holder, lease.ttl_ms, lease.mechanism, lease.release_cause`. Cardinality 5 holds; only `lease.holder` matches by name; `lease.id`/`lease.acquired_at`/`lease.duration_ms`/`lease.event_kind` diverge from `lease.key`/`lease.ttl_ms`/`lease.mechanism`/`lease.release_cause`. **This IS the same shape as the seven fork-set units.** It is recorded here rather than in the fork table only because the operator may judge `lease.id`≈`lease.key` and `lease.duration_ms`≈`lease.ttl_ms` to be a *naming-refinement* call rather than a *divergence* — but on the strict calibration discipline (does the signature match the cited contract verbatim?) it does not. **Operator-resolution flag: if the operator applies the strict reading, U-CP-11 becomes the 8th fork and the bottom-line total becomes 8.** Decision-vocabulary label: *proposing*.
2. **U-CP-52 `TimeoutDegradationKind` — marginal, not escalated.** Plan values `CONTINUE_AS_REJECT / ESCALATE_TO_REVIEW_BOARD / ABORT_WORKFLOW` re-express spec §21.8's `fail-closed / escalate-secondary-channel / fail-open`. Cardinality 3 holds; the plan's value names are a semantic re-expression rather than a transcription. The plan acc #1 cites "§21.6" while the spec content is at §21.8 (§21.6 is validator-failure sampling). Recorded as a marginal citation+naming drift; not escalated because the three modes map 1:1 onto the spec's three modes by intent. Operator may fold a citation correction into any U-CP-52 revision.
3. **U-CP-08 `FallThroughCause` "4 values per §3.2 verbatim" — procedural, not enum-rename; not escalated.** Spec §3.2 (`on_layer_exceed_budget`) declares no `FallThroughCause` enum at all — it is a procedure. The plan invents a 4-value enum (`LAYER_NO_DECISION, LAYER_BUDGET_EXHAUSTED, PROVIDER_UNAVAILABLE, CAPABILITY_SHORTFALL`). "Per §3.2 verbatim" is unverifiable against a section that has no enum. This is a *spec-under-specification + plan-extension* shape rather than the *enum-rename divergence* the systemic pattern targets; it would more properly route as a distinct fork class (plan declares structure the spec does not). Recorded for operator visibility; out of scope for this verbatim-divergence audit.
4. **U-CP-13 `WorkflowManifestEntry` "10 fields per §6.1 verbatim" — marginal overcommit, not escalated.** Spec §6.1 enumerates 7 named fields plus an explicit `// ... additional per-workload fields` open clause. The plan's 10-field record overcommits a count the spec leaves open-ended, but the open clause permits extension, so "10 ... verbatim" is an imprecise claim rather than a contradicted one. Recorded; not a fork.
5. **U-CP-21 v2.1 3-attribute `engine.*` divergence — correctly NOT reported.** The v2.1 body declared `engine.class, engine.resumption_kind, engine.tech` (diverging from spec §9.1) and mis-cited §5.3/§9.2 — this was the v2.1 §0.8 forward-flagged concern. It was resolved at **v2.2** (4-attribute schema per CP spec v1.3 §9.1). The canonical-current artifact does not carry the divergence; reporting it would be a stale finding (FM-H-adjacent — reviewing a superseded artifact).
6. **Casing convention is not a divergence (FM-D self-check).** SCREAMING_SNAKE_CASE rendering of spec lowercase-hyphen identifiers (`EVENT_SOURCED_REPLAY`↔`event-sourced-replay`, `APPROVE`↔`approve`, `PRE_ACTION`↔`pre-action`) was checked at U-CP-15, U-CP-37, U-CP-38, U-CP-39, U-CP-48 and treated as a Python-stack naming convention, not a verbatim divergence — the token stems match 1:1. Mechanical "any casing difference is a finding" application was avoided. The seven fork-set units are flagged because the *stems themselves* diverge, not the casing.
7. **A2 (silent scope narrowing) self-check.** Each of the 7 fork units silently substitutes a different vocabulary for the contract it cites — the same A2 shape behind Tension 002. Confirmed present; not a smoothing miss.
8. **A4 (fabricated citations) self-check.** All `Implements:` citations (`C-CP-NN §X`) resolve to real spec sections; the defect is divergence-from-cited-content, not citation fabrication. Exception: U-CP-22 acc #2 mis-cites §10.3 for a §10.2 surface and U-CP-46 acc #4 mis-cites §20.4 for the §20.5 emission table — both folded into their respective fork rows, not separate A4 findings.

---

## Disposition

**The §6 systemic-pattern threshold (≥3 occurrences) is crossed many times over.** Tension 002, Tension 004, and F3-01 confirmed three instances; this plan-wide audit surfaces **7 confirmed instances inside the CP plan alone** (U-CP-01, U-CP-10, U-CP-19, U-CP-22, U-CP-43, U-CP-46, U-CP-47), plus one borderline 8th (U-CP-11) pending an operator naming-refinement-vs-divergence call.

Per SKILL.md §6: the resolution scope is **not** seven independent inline fixes. The systemic pattern is *un-audited "verbatim" acceptance-criterion claims in the Phase-6 CP plan* — every foundational enum/namespace-declaration unit asserts "per §X verbatim" and a large fraction do not transcribe the cited contract. The higher-leverage resolution is a single Phase-6 CP-plan revision-pass that conforms all seven (eight) divergent surfaces to the spec-canonical vocabulary in one pass, with the cross-unit propagation (U-CP-22→U-CP-23/24/25/31/13/35/53; U-CP-10/19/43/47→their consumers) handled in the same pass — rather than discovering each divergence one §2.7.6 Class 1 fork at a time at execution-time.

Each divergent unit is a §4.1 **Class 3** review finding (discriminator (b) — resolving it requires revising a Phase-6 plan artifact against the Phase-5 spec) whose disposition is a §2.7.6 **Class 1 (halt-execution)** fork. The spec C-CP-* contracts are canonical for the plan per `CLAUDE.md` §1.3; the plan is the artifact in error in every row. The reviewer does not pick the conformance direction — the authority-chain reading (as in Tension 002) favours the spec.

**The complete CP-plan verbatim-divergence fork set is: {U-CP-01, U-CP-10, U-CP-19, U-CP-22, U-CP-43, U-CP-46, U-CP-47}** — 7 units (8 if the operator applies the strict reading to U-CP-11). U-CP-22 was already known (Tension 002 + F3-01); the other six are newly surfaced by this plan-wide audit.

---

*Phase-7 pre-implementation review, verbatim-claim systemic-attack scope. Read-only with respect to all design-substrate and plan artifacts — no repository file modified. Authored 2026-05-15 per `harness-adversarial-reviewer` SKILL.md Phase-7 pre-implementation review mode.*

---

# §4A Resolution Recommendation — CP-plan verbatim-divergence cluster

*Appended 2026-05-15 per `systems-architect` SKILL.md §4A (Phase-7 tension-resolution mode). This audit report is the canonical systemic-tension record for the cluster (per the Phase-7 checkpoint decision: no per-unit Tension 005+ proliferation). The §4A recommendation is **a recommendation** — the operator holds decision authority (§4A.4).*

## §4A.1 — Precise tension statement

Seven Control Plane plan units carry an acceptance criterion asserting a signature is materialized "per §X **verbatim**" against a `Spec_Control_Plane` contract whose vocabulary the signature does not transcribe. The precise per-artifact divergence — plan text vs. cited spec text — is in the **Fork inventory table** above; it is not re-summarized here per §4A.2 ("do not summarize"). The seven: **U-CP-01, U-CP-10, U-CP-19, U-CP-22, U-CP-43, U-CP-46, U-CP-47**. One borderline 8th — **U-CP-11** — is held for an operator naming-vs-divergence call (Findings-rejected item 1).

Four of the divergences were spot-verified directly against `design-substrate/` during this §4A pass: CP spec §1.4 (`routing.provider / routing.model / routing.layer / routing.binding_rationale`), §21.1 retry-exit taxonomy (`transient-retry / Reflexion-recoverable / HITL-recoverable / permanent-fail-exit / terminal-fail-exit`), §10.1 TopologyPattern, §19.1/§16.2 gate level. All four confirm the audit; the plan is the divergent artifact in each.

## §4A.2 — Authority-chain placement

`CLAUDE.md` §1.3 chain: ADR (F1–F5 + D1–D6) → ADD v1.3 → PRD v1.1 → **per-axis spec v1.x** → **per-axis plan v2.x**. The spec is canonical for the plan.

For all seven units the divergent surface is a Phase-5 **`Spec_Control_Plane`** contract (v1.3 §3.5/§9.1 amendments + v1.2 §10–§24 preserved-verbatim sections) vs. a Phase-6 **`Implementation_Plan_Control_Plane_v2.x`** unit body. The spec sits strictly above the plan on the chain. **The plan is the artifact in error in every row.** No higher-chain artifact (ADR / ADD / PRD) re-commits the plan's divergent vocabulary — the spec v1.2→v1.3 change-notes touch only `retry.*` / `engine.*` (§3.5, §9.1), none of the seven divergent surfaces (verified against the spec change-note prose).

## §4A.3 — §2-discipline analysis

- **Five-axis (§2.1):** all seven are Control-Plane-axis surfaces (routing, lifecycle, resumption, topology, gate-level, audit/HITL namespaces, validator-fail). No cross-axis re-decomposition needed; this is a within-axis plan/spec conformance defect.
- **Probabilistic–deterministic boundary (§2.2):** every divergent surface is a **deterministic-side** primitive — an enum, a namespace schema, a gate-level constant table. These are exactly the artifacts on which reliability rests. A divergent enum is not a cosmetic defect: a wrong value name propagates verbatim into every consumer and silently breaks cross-axis composition. This raises, not lowers, the cost of leaving the divergence unconformed.
- **Decision ordering (§2.3):** the divergences are **D-level** (derivative). The foundational decision — *that* the CP axis commits these enums/namespaces — is already made and is not in question; the spec holds it. The defect is purely the plan's derivative *materialization* diverging from the committed vocabulary. No F-level commitment is reopened. This is `§4A.4`'s "an artifact simply diverged from the chain" case, **not** a re-decision.

## §4A.4 — Recommended reading

**Conform the seven divergent CP-plan units to the cited `Spec_Control_Plane` vocabulary**, in a single `implementation-planner` revision-pass, with cross-unit propagation handled in the same pass. This is the authority-chain-determinate reading per §1.3 — not a re-decision of which vocabulary is better. The reviewer/architect does not pick the conformance direction; the chain does.

- **U-CP-22** is a special case: its conformance direction was **already decided** as Tension 002 (operator sign-off 2026-05-15; CP-AL-1 conformed at commit `45f104f`). TopologyPattern = spec §10.1 Set 2 (`single-threaded-linear / orchestrator-workers / decentralized-handoff / hierarchical-delegation / evaluator-optimizer / parallelization`); CascadePolicy = `pause / proceed / cascade-cancel` (spec §10.2 string-literal domain — note: spec declares a field domain, not a named enum). The U-CP-22 plan-file body still carries stale Set-1 text; conforming it is the **unfinished plan-side absorption of an already-settled decision**, not a new recommendation.
- **U-CP-01, U-CP-10, U-CP-19, U-CP-43, U-CP-46, U-CP-47** — six newly-surfaced units of identical shape. Recommended reading: conform each unit's enum/namespace signature **and** its acceptance-criterion text to the cited spec section verbatim.
- **Cross-unit propagation (mandatory, same pass):** conforming a foundational enum renames every downstream consumer. U-CP-22 `TopologyPattern` → U-CP-23 / 24 / 25 / 31 / 13 / 35 / 53. U-CP-10 `LifecycleEventClass`, U-CP-19 `ResumptionKind`, U-CP-43 `GateLevel`, U-CP-47 `ValidatorFailClass` → their consumers (U-CP-12, U-CP-20, U-CP-27/39/45/46, U-CP-48). The revision-pass that conforms a foundational enum **must** conform every consuming unit in the same pass, or cross-unit composition breaks at the rename.
- **Downstream artifacts that absorb the resolution:** `Implementation_Plan_Control_Plane` (next version bump); no `CLAUDE.md` anti-leakage rule is implicated except CP-AL-1, which was already conformed under Tension 002. No ADR edit required.

### Items NOT in the conform-to-spec scope — surfaced for separate operator disposition

1. **U-CP-08 `FallThroughCause` — design gap, not a conformance tension (§4A.4 "chain genuinely silent").** Spec §3.2 (`on_layer_exceed_budget`) declares **no enum** — it is a procedure. The plan invents a 4-value enum. There is nothing to "conform to": the chain is silent. Per `CLAUDE.md` I-2 / X-AL-3, a plan unit declaring an H_T structure the spec does not commit is a **design extension**, which Phase 7 may not silently absorb. This routes as a **§2.7.6 Class 1 fork of a distinct shape** — either (a) `spec-writer` extends `Spec_Control_Plane` §3.2 to commit the enum, or (b) the operator confirms the plan-extension is sanctioned and records the rationale. Not resolvable inside the verbatim-conformance pass.
2. **U-CP-11 `LEASE_NAMESPACE_SCHEMA` — operator naming-refinement call (§2.7.6 Class 2).** Cardinality 5 holds; `lease.id`/`lease.acquired_at`/`lease.duration_ms`/`lease.event_kind` vs spec §5.3 `lease.key`/`lease.ttl_ms`/`lease.mechanism`/`lease.release_cause`. On the strict verbatim calibration this is the same divergence shape as the seven; whether `lease.id ≈ lease.key` is a refinement or a divergence is a judgment the architect does not own. **If the operator applies the strict reading, U-CP-11 joins the conformance pass as the 8th unit.**
3. **U-CP-13 / U-CP-52 — marginal citation/naming drift.** U-CP-13 "10 fields verbatim" overcommits a spec §6.1 count left open by an explicit extension clause (imprecise claim, not a contradiction). U-CP-52 cites §21.6 for content at §21.8 and re-expresses three spec modes under different names (cardinality holds). Recommend folding both **citation corrections** into the same plan revision-pass as low-cost cleanup; neither is a fork.
4. **Tension 003 (`WorkloadClass` residence) is adjacent but distinct.** U-CP-23's `PER_WORKLOAD_CLASS_TOPOLOGY` consumes `WorkloadClass`, which Tension 003 records as undeclared. That is a **genuine design-residence choice** (which artifact declares `WorkloadClass`), not an authority-chain-determinate conformance — it is **not** folded into this cluster and remains for separate operator decision.

## §4A.5 — Tiebreaker check

**Citation-pattern note.** Unlike the OD cluster — where U-OD-30's plan body cited `ADR-D5` directly and a load-bearing ADR read was required — **no CP cluster unit cites an ADR in its divergent acceptance criterion**; every divergent CP unit cites a `Spec_Control_Plane` C-CP-NN section. The ADR-direct tiebreaker read was therefore not required for CP. This is a citation-pattern asymmetry, not a skipped step.

The single verifiable fact that makes each conformance recommendation determinate: **no ADR / ADD / PRD revision postdates the cited `Spec_Control_Plane` section and re-commits the plan's divergent vocabulary.** Verified for this pass — the spec change-notes (v1→v1.1→v1.2→v1.3) record only `breaker.*`/`retry.*`/`engine.*` namespace and §24.1 export-table revisions, none touching the seven divergent surfaces; no D-ADR revision re-enumerates routing / lifecycle / resumption / gate-level / audit-namespace / validator-fail vocabulary post-spec. The recommendation is **determinate**: spec canonical, plan conforms.

**Load-bearing-artifact flag:** the resolution touches no `CLAUDE.md` anti-leakage rule and no F-ADR. CP-AL-1 (the one anti-leakage rule in scope) was already conformed under Tension 002. The cluster therefore needs operator **ratification** of the conformance direction, not a load-bearing-artifact sign-off — except items 1 (U-CP-08, genuine design gap) and 2 (U-CP-11, naming call), which require an explicit operator decision before disposition.

## §4A.6 — Fork classification

Per `Project_Workflow_v1_8.md` §2.7.6: **Class 1 (halt-execution)** for all seven units — a design-phase artifact (the CP plan) requires revision before the affected units can land. Per the `design-substrate/`-is-canonical posture (back-flow deprecated 2026-05-15), Class 1 here means: halt landing of the seven units, run the `implementation-planner` revision-pass in-CLI, re-clear, then land. **U-CP-15 is unaffected** — it is on the audit clean-list (already landed; verified). U-CP-22 is the only cluster member inside the current operational-minimum set.

## §4A.7 — Operator decision required

**The operator decides.** This §4A section recommends; it does not decide and does not edit the plan (§4A.4 — plan edits are `implementation-planner` work, sequenced after sign-off). Operator actions:

1. **Ratify** the conform-to-spec direction for the 7-unit cluster (authority-chain-determinate; recommended).
2. **Decide U-CP-11** (item §4A.4.2): strict reading → 8-unit cluster, or naming-refinement → no action.
3. **Disposition U-CP-08** (item §4A.4.1): spec-extension via `spec-writer`, or sanction the plan-extension with recorded rationale.
4. Note **Tension 003** (`WorkloadClass`) remains separately open.

On ratification, sequencing is: `implementation-planner` revision-pass on `Implementation_Plan_Control_Plane` (conform 7–8 units + propagate to consumers + fold citation fixes) → version bump → land U-CP-22 against the conformed plan.

*§4A recommendation authored 2026-05-15 under `systems-architect` SKILL.md §4A. Read-only with respect to `design-substrate/` — no spec, plan, or ADR modified by this pass.*
