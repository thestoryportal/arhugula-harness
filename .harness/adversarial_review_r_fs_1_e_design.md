# Adversarial Review — R-FS-1 E (Durable-Execution Engine Classes: Design)

## Summary

- **Mode:** Phase-7 pre-merge design-arc review (standing posture A — review BEFORE merge), per `harness-adversarial-reviewer` SKILL.md.
- **Artifacts reviewed:**
  1. `.harness/r-fs-1-e-engine-classes-design-v1.md` (NEW — the E sub-program design leg).
  2. The status-pointer edit to `.harness/class_1_fork_path_i_durable_async_engine_class_materialization.md` (CLOSED-DEFERRED → PROPOSING).
- **Date:** 2026-06-15 · **Branch:** `r-fs-1-arc-24-e-design` · **HEAD:** `215f1b9` (design commit on top of `11c27fb`).
- **Cites re-grounded by direct read:** **24 distinct cite targets** (every load-bearing file:line + §-section in the doc). **5 failed or are misattributed** (F1-01, F1-02, F1-03, F1-04, F1-05). All structural/semantic claims re-grounded resolved correct.
- **Finding count by class:** §4.1 review-severity — **Class 3: 0 · Class 2: 1 · Class 1: 6.**
- **Highest-severity finding:** F2-01 (the C-CP-49/50 "impl-not-fork" vs SPINE "substantive build" framing reconciliation — *proposing*, defensible but worth an operator-visible note).
- **Disposition recommendation:** **APPROVE** — cleared with inline cite-hygiene fixes. No Class-3 finding; the single Class-2 is a *proposing* framing-precision note, not a blocker. The design is X-AL-3-clean, the load-bearing structural claims all re-ground correctly, and the fork-vs-impl decomposition is honest (not a defer-in-disguise).

---

## Verdict context — what the review confirmed (load-bearing claims that re-grounded correctly)

These are the claims an APPROVE rests on; each was verified by direct read, not accepted on the doc's self-assessment:

1. **Engine-materialization state (§0/§1.1).** `_IN_SCOPE_ENGINE_CLASSES = {PURE_PATTERN_NO_ENGINE, SAVE_POINT_CHECKPOINT}` at `workflow_driver.py:183-184` ✓; the gate raises `EngineClassNotYetMaterializedError` at `:1351-1352` ✓; the `EngineClass` enum is closed 5-member at `engine_class.py:23-53` (members at `:31/36/41/46/50`) ✓. **3 of 5 genuinely unbuilt** — confirmed.
2. **The built-but-UNWIRED producer gap (§1.2).** `RuntimeEngineRecoveryLoop` at `engine_recovery_loop.py:45`; `.capture_pause` `:52` → `emit_pause_captured…` `:63`; `.attempt_resume` `:74` → `emit_resume_attempted…` `:93` ✓. The OBJECT is constructed in production at `r_cxa_2_producer_loop_factory.py:208` (via `materialize_r_cxa_2_producer_loop_stage` `:169`, reached from `stage_5_loop_init.py:228`) ✓ — but its `.capture_pause`/`.attempt_resume` methods have **zero production callers** (only `test_*`). The src grep hits for `.attempt_resume(` (`pause_resume_protocol.py:291` engine free-function; `workflow_driver.py:1203` workflow-layer `PauseResumeProtocol`) are distinct surfaces, NOT the recovery-loop methods — the doc's "object produced, methods dormant" distinction is exact.
3. **Factory binds Deterministic, not Journal (§1.2).** `r_cxa_2_producer_loop_factory.py:210` binds `DeterministicEnginePauseResumeSubstrate` (in-memory) ✓; `JournalEnginePauseResumeSubstrate` (`journal_pause_resume_substrate.py:123`) has **no production binding** (only docstring references) ✓ — "EXISTS + tested but UNBOUND" is accurate.
4. **Engine-layer vs workflow-layer distinction (§0/§1.2 — the central correctness claim).** `api.resume` exists at `api.py:645` with docstring "The workflow-layer durable-resume Track-A sibling of `run()` (R-CC-1 arc #3)" ✓. The doc's claim that `api.resume` is the workflow-layer SIBLING (DONE, #513/#514) and **NOT** arc E (the engine layer) is **correct** — do-not-re-scope-`api.resume`-into-E holds.
5. **EVENT_SOURCED does NOT un-skip Path-(i) (§3.1/OQ-2).** `test_u_rt_95:129-130` reads "RECONCILER_LOOP / WAL_SEGMENT engine classes (which would map to DURABLE_ASYNC cells)" — EVENT_SOURCED_REPLAY is *not* listed ✓. The skip is at `:347` (`@pytest.mark.skip`) ✓. So the Path-(i) un-skip correctly lands at E-2 (WAL_SEGMENT), not E-1. See F1-04 + the §18.1-anchor honesty note below.
6. **PathClass closed 4-class enum (§6.2).** `path_class_registry.py:31-37` = `SKILLS/PROMPTS/ROUTING_MANIFEST/STATE_LEDGER` ✓; IS-AL-1 forecloses inventing a class (harness-is/CLAUDE.md §4.2) ✓. The STATE_LEDGER recommendation + the conditional F-E-IS sub-fork respect IS-AL-1.
7. **F3 capability-floors (§6.1).** `engine_class.py:74-111` CAPABILITY_FLOORS; the 4 floors at `:76/85/94/102` each `required_at_class=_ALL_CLASSES` ✓; floor (iv) cites the 8 lifecycle events per C-CP-05 §5.1 at `:106` ✓.
8. **X-AL-3 cleanliness.** `git diff --name-only main...HEAD` = exactly the two `.harness/` files; **no `design-substrate/**` or `harness-*/src` edit** ✓. All 3 engine materializations route to F-E-1/2/3 spec-amendment forks (authored later, with clearance markers) — the doc extends no H_T design itself. Mirrors the B3-DESIGN precedent (`.harness/r-fs-1-b3-smart-hitl-design-v1.md` EXISTS).
9. **Fake-producer foreclosure (§0/§7/§10).** The 3 foreclosed anti-patterns (option-D stub-widening; cosmetic Journal swap; workflow-pause-piped-through-engine) match the `post-phase-8-forward-register.md:181` + `r-cl-p2…md:18-20` ratified instructions exactly. No slice smuggles one in — each slice requires a REAL hand-rolled durable substrate + a REAL driver firing it.
10. **External-canon (durable-execution semantics).** Event-sourcing (deterministic replay + cached effects), WAL (write-ahead segment + per-segment idempotent resume), reconciler (level-triggered read/diff/converge) are all canonically correct vs industry patterns; the named hazards (non-deterministic replay; torn-writes/fsync; convergence-stability/level-triggered idempotency) are exactly the canonical failure modes. I-6 hand-roll (no vendored Temporal/K8s/Kafka) is the committed, rationale-bearing deviation. **No external-canon DIVERGENCE finding.**

---

## Class 2 findings (moderate — current-phase content)

### F2-01 — C-CP-49/50 "impl-not-fork" framing vs the SPINE "substantive build" framing (*proposing*)

- **Location:** design doc §2 ("`C-CP-49`/`C-CP-50` are **already-cleared contracts** … Firing them from a real driver is **impl-against-cleared-spec**, not a fork") + §7.2 + §0 footnote; vs `beyond-mvp-capability-boundary-ledger.md:90` ("**2 substantive build** → `C-CP-49/50` engine-layer emits fold into the engine-classes build") + `r-fs-1-arc-1-scoping-v1.md:28-29` (C-CP-49/50 disposition = "**BUILD → folds into engine-classes**", reachability "❌ dormant driver").
- **Defect:** The two governing authorities frame the C-CP-49/50 work with different emphasis. The SPINE/arc-1 scoping calls them "substantive build" / "BUILD"; the design doc reclassifies the *emit-firing* as "impl-against-cleared-spec, not a fork." Both readings are defensible and reconcilable — the COMPOSERS (`emit_pause_captured…` `:856`, `emit_resume_attempted…` `:951`) and the §16.5 CONTRACTS (CP spec v1.25/26, cleared at PRs #39-#44 per harness-cp/CLAUDE.md §1.1) ARE built+cleared; what is missing is a production DRIVER. So "firing the cleared composer = impl" is technically precise, and "substantive build" refers to the whole fold (driver + engine semantics + emit go-live) within which the emit-firing is the impl portion. The design doc's decomposition is in fact MORE precise than the SPINE's coarser label.
- **Discriminator that classifies as Class 2:** (a) — affects substantive content framing of the current artifact; the divergence is reading-dependent (Reading 1: SPINE coarse-grains the whole fold as "build"; Reading 2: the design doc correctly separates the cleared-composer-firing impl from the engine-semantics fork). The skill does not pick; both are supported by the text.
- **Evidence:** `r-fs-1-arc-1-scoping-v1.md:28` reads C-CP-49 reachability "❌ dormant driver" / disposition "BUILD → folds into engine-classes"; the design doc §7.2 reads "firing them from a real driver is impl." These are NOT contradictory (the design doc's "impl" is the narrow emit-firing; arc-1's "BUILD" is the surrounding fold) but the unqualified "not a fork" phrasing could be read as overriding the SPINE's "substantive build" label.
- **Anti-fabrication attack engaged:** A2 (silent scope narrowing) — checked whether labeling the emit-firing "impl" silently narrows the build's scope below what the SPINE folded in. It does NOT: the design doc preserves the full fold as fork-owed (F-E-1/2/3 carry the engine semantics + `_IN_SCOPE` widening), and explicitly enumerates the emit-firing as impl-against-cleared-spec under §7.2. Scope is preserved; only the labeling granularity differs.
- **Axis-domain attack engaged:** CP — engine-class / CP→IS composer seam.
- **Resolution path:** Add one sentence at §2 or §7.2 reconciling the labels explicitly — "the SPINE's '2 substantive build' for C-CP-49/50 = the whole fold (driver + semantics + emit go-live); within it, the cleared-composer firing is the impl portion, the engine semantics + `_IN_SCOPE` widening is the fork." Operator-visible because it touches the fork-vs-impl accounting the full-spec directive cares about. Not a blocker; the substance is already correct.

---

## Class 1 findings (minor — documentation drift)

### F1-01 — "SPINE ledger §90" is a non-resolving section cite (forward-cite-phantom shape)

- **Location:** design doc §0 ("§90 frozen arc list"), §0 scope table, §1.3, §7.1 ("SPINE ledger §90 arc-1"), §10 footer ("SPINE ledger §90") — cited ≥5 times.
- **Defect:** `beyond-mvp-capability-boundary-ledger.md` has **no `## §90` section**. Its headings are Bucket B, Bucket A, Bucket C, Confidence notes, and "Program (under the 2026-06-12 directive…)". The content the doc attributes to "§90" (the FROZEN ordered child-arc list + the C-CP-49/50 fold-in + "substantive build") lives at the Program section's numbered **list item 2 (line 90)** + item 3 (line 91) — i.e. it resolves to *line* 90, not a *section* §90. The §-cite shape is a coincidental line-number collision presented as a section identifier.
- **Discriminator:** (a/b/c all miss) — the cited *content* resolves (the frozen arc order `B1→B3→E→B2→R→B4→CA→B5→B6→B7→M` and the C-CP-49/50 fold-in ARE at line 90-91, and the design doc's stated order matches byte-for-byte). Only the cite *form* ("§90") misleads. → Class 1 (drift). Engages checklist item 3 (forward-looking cite phantom) in the cite-form-not-content variant.
- **Evidence:** `grep "^## " beyond-mvp-capability-boundary-ledger.md` returns Bucket B/A/C + Confidence notes + Program — no §90. Line 90 is "First arc = make scope provably complete… Output: the **FROZEN ordered child-arc list** `B1→B3→E(engines)→…`".
- **Resolution:** Re-cite as "SPINE ledger Program §, line 90 (the frozen ordered child-arc list)" or "the SPINE Program section" rather than "§90". Byte-exact-citation discipline (root CLAUDE.md §10.4).

### F1-02 — `line 181` re-open trigger misattributed to `r-cl-p2-engine-recovery-grounding.md`

- **Location:** design doc §0 DEFER-supersession table, **row 2**: "`r-cl-p2-engine-recovery-grounding.md` | DEFERRED (2026-06-10) | **line 181** — 're-open only when a real event-sourced replay, reconciler-loop, WAL-segment, or engine-native-pause recovery loop lands'".
- **Defect:** `r-cl-p2-engine-recovery-grounding.md` is **47 lines long** — it has no line 181, and does not carry that byte-exact trigger text at its own body. The byte-exact text "re-open only when a real event-sourced replay, reconciler-loop, WAL-segment, or engine-native-pause recovery loop lands" lives at **`post-phase-8-forward-register.md:181`** (the CXA-2 row "Close-out" column). r-cl-p2 itself *cites* the forward-register line 181 at its own line 18 ("The forward-register line 181 ratifies this…") — so r-cl-p2's re-open framing is by-reference, not by-resident-text. The §0 row-2 attribution conflates r-cl-p2's by-reference framing with the forward-register's resident text + line number.
- **Discriminator:** (a/b/c all miss) — the trigger text + line 181 DO resolve (to the forward-register, which the doc *separately* cites correctly in §0 row 4 and §1.2). The defect is a cross-row cite-bleed: row 2 borrows row 4's "line 181 + byte-exact text" and pins it to the wrong file. → Class 1 (drift / misattribution). Engages checklist item 3.
- **Evidence:** `wc -l r-cl-p2-engine-recovery-grounding.md` = 47; `grep -n "re-open only when a real event-sourced" post-phase-8-forward-register.md` = line 181 (sole match). r-cl-p2:18 reads "The forward-register line 181 ratifies this".
- **Resolution:** In §0 row 2, either drop the "line 181" + verbatim-text (r-cl-p2's actual deferral framing is at its §1, lines 14-20), or re-cite as "r-cl-p2 §1 (which itself defers to forward-register line 181)". Keep the row-4 forward-register cite as the canonical home of the byte-exact trigger.

### F1-03 — §5.3 mis-states RECONCILER_LOOP as "excluded" at server/managed (it is INCLUDED there)

- **Location:** design doc §5.3 ("`engine_class_candidate.py:70` excludes `RECONCILER_LOOP` at `local-development`… (**also excluded at server `:79,:93` / managed `:93`** per the candidate map)").
- **Defect:** RECONCILER_LOOP is **excluded only at LOCAL_DEVELOPMENT** (`engine_class_candidate.py:70`, reason "requires K8s control plane"). At SELF_HOSTED_SERVER it is an **INCLUDED member** of the candidate set (`:79`), and at MANAGED_CLOUD it is **INCLUDED** (`:93`). The lines `:79`/`:93` the doc cites as "also excluded" are the *inclusion* sites in the server/managed candidate frozensets; the actual exclusion at server/managed is of `PURE_PATTERN_NO_ENGINE` (`:84` server, `:98` managed). So the doc's parenthetical reverses the sense of `:79`/`:93`.
- **Discriminator:** (a/b/c all miss for severity) — this is a factual mis-statement of the candidate map, but it does NOT change the design's conclusion (RECONCILER_LOOP's LIVE e2e is K8s-infra-gated *at the local-development surface where the build runs* — which is the only surface that matters for the E-3 live-proof gate, and that part is correct at `:70`). → Class 1 (drift). Engages checklist item 8-adjacent (precision against the cited contract).
- **Evidence:** `engine_class_candidate.py:60-99` — LOCAL candidate_set omits RECONCILER_LOOP + exclusion_reasons names it; SERVER candidate_set `:79` INCLUDES RECONCILER_LOOP (excludes PURE_PATTERN `:84`); MANAGED candidate_set `:93` INCLUDES RECONCILER_LOOP (excludes PURE_PATTERN `:98`).
- **Resolution:** Correct §5.3 to "excluded at `local-development` only (`:70`); INCLUDED in the server (`:79`) + managed (`:93`) candidate sets" — which actually STRENGTHENS the design's point (the engine IS the canonical server/managed durable engine; only the local dev surface lacks the K8s control plane, hence the live-proof gate). The build-vs-live-proof split (§5.3) is unaffected.

### F1-04 — §3.1 misattributes the "F2 / 12-factor-agents pattern" docstring to the WAL row at `engine_class.py:43`

- **Location:** design doc §3.1, parenthetical: "(`engine_class.py:43` docstring cites the substrate as 'F2 / 12-factor-agents pattern' for the **WAL row**; the event-sourced row's substrate citation is in its own docstring per acceptance #2)".
- **Defect:** `engine_class.py:43` is inside the **PURE_PATTERN_NO_ENGINE** docstring (`:41-44`: "Substrate: F2 / 12-factor-agents pattern (C-CP-07 §7.1 row 3)"), NOT the WAL row. The **WAL_SEGMENT** row is at `:50-53` and its docstring cites "Substrate: **Kafka-style WAL** (C-CP-07 §7.1 row 5)". So the doc attributes the PURE_PATTERN substrate citation (line 43) to the WAL row.
- **Discriminator:** (a/b/c all miss) — a wrong-row docstring attribution; harmless to the E-1 design conclusion (the parenthetical is illustrative, and the actual EVENT_SOURCED row docstring `:31-34` cites "Temporal / Restate / DBOS", which the design correctly hand-rolls past per I-6). → Class 1 (drift). Engages checklist item 3 (cite resolves to wrong target).
- **Evidence:** `engine_class.py:41-44` = PURE_PATTERN docstring "F2 / 12-factor-agents pattern"; `:50-53` = WAL_SEGMENT docstring "Kafka-style WAL". EVENT_SOURCED docstring `:31-34` = "Temporal / Restate / DBOS".
- **Resolution:** Drop or correct the parenthetical — the EVENT_SOURCED substrate citation the §3.1 sentence actually wants is at `engine_class.py:31-34` ("Temporal / Restate / DBOS"), which the design hand-rolls past. The "F2 / 12-factor-agents" reference belongs to PURE_PATTERN (`:43`), not WAL.

### F1-05 — research-corpus keyword counts drift (cluster-4 idempotency / durable-execution)

- **Location:** design doc §4.2 (E-2 research grounding): "cluster-4 (WAL:4, **idempotency:25**)"; §3.2 (E-1): "cluster-4… **durable-execution:5**".
- **Defect:** Empirical counts at HEAD: cluster-4 `idempotency` = **15** (not 25); `durable-execution` = **4** (not 5). The other cited counts are accurate (PRC WAL:28 ✓, reconcil:35 ✓, checkpoint:24 ✓; cluster-4 Temporal:27 ✓, replay:10 ✓, WAL:4 ✓).
- **Discriminator:** (a/b/c all miss) — decorative keyword-frequency counts under a `[HIGH — corpus-cited]` tag; the *substance* (the patterns ARE in the corpus; the named hazards ARE canonical) is correct, so the over-counts don't drive any decision. → Class 1 (drift). Engages A5/A7 (uncertainty-signal / weak-source) lightly — a `[HIGH]` tag with a 67%-inflated sub-count is a minor over-confidence on a non-load-bearing number.
- **Evidence:** `grep -oi 'idempotency' <cluster-4>` = 15; `grep -oi 'durable-execution\|durable execution' <cluster-4>` = 4.
- **Resolution:** Correct the two counts (or drop the exact integers — the corpus-grounding point stands without them). The `[HIGH — corpus-cited]` tag is otherwise well-earned by the (accurate) majority of counts + the canonical-hazard naming.

### F1-06 — `api.resume` workflow-layer caller lines drift between sibling docs (cross-doc, non-blocking)

- **Location:** design doc §1.2 references the workflow-layer `PauseResumeProtocol` emitting `cp.pause-resume-protocol`; the sibling `r-cl-p2…md:16` cites the workflow-layer fire sites at `workflow_driver.py:571/796/952`, and `r-fs-1-arc-1-scoping-v1.md:23` cites C-CP-30 production callers at `workflow_driver.py:582/808/965`.
- **Defect:** The two sibling docs cite *different* line numbers for the same workflow-layer pause/resume production caller surface (`571/796/952` vs `582/808/965`) — a ~10-line drift indicating one was authored against an earlier HEAD. The design doc under review does NOT itself cite these specific line numbers (it cites the surface by name + `:1203` for `protocol.attempt_resume`, which I confirmed resolves), so this is a sibling-staleness observation, not a defect IN the artifact under review.
- **Discriminator:** (a/b/c all miss) — sibling-doc staleness, surfaced per checklist item 2 (sibling-spec staleness) + the cross-spec drift probe. The design doc under review is clean here; this is logged for the E-spec/E-plan legs to re-ground when they cite the workflow-layer fire sites. → Class 1 (drift, in a sibling, advisory).
- **Evidence:** `r-cl-p2…md:16` = `:571/796/952`; `r-fs-1-arc-1-scoping-v1.md:23` = `:582/808/965`; design doc under review cites `workflow_driver.py:1203` for `protocol.attempt_resume` (✓ resolves).
- **Resolution:** No action owed in THIS PR (the design doc doesn't carry these numbers). Flag for the E-spec leg: re-ground the workflow-layer pause/resume fire-site line numbers against HEAD before citing them.

---

## Findings considered and rejected (transparency — what was attacked and held)

1. **X-AL-3 silent design extension (checklist item 8 — most load-bearing).** Checked whether the doc silently extends H_T design. It does NOT — `git diff` confirms `.harness`-only; all 3 materializations route to F-E-1/2/3 spec-amendment forks authored later with clearance markers. The doc extends nothing; it proposes forks. HELD.
2. **Defer-in-disguise (build-label over a defer).** Checked whether any "build" slice is actually a defer wearing a build label. It is NOT — each slice mandates a REAL hand-rolled durable substrate (event store / segment log / etcd-style store) + a REAL driver firing C-CP-49/50, and the doc explicitly forecloses the 3 fake-producer anti-patterns (option-D stub, cosmetic Journal swap, workflow-pause-piped-through-engine) that WOULD be defers-in-disguise. The E-3 live-e2e infra-gate is honestly surfaced as a build-unblocked / live-proof-gated split (§5.3), not a slice-blocking defer. HELD.
3. **Fork-registration mechanism (the `[[spine-ledger-forward-arc-registration]]` forward-cite-phantom warning).** Checked whether F-E-1/2/3 are asserted-without-mechanism. They are NOT — E is already the tracked frozen arc (SPINE Program line 90 + Bucket A row, both verified), so the doc's stance (no new `B-*` minted; F-E-N ARE the registration, analogous to B3→F-B3-1/2) is legitimate, and §7.1 commits to minting a `B-*` SPINE entry if an E-impl arc surfaces a NEW sub-fork beyond F-E-1/2/3/IS (the B-EDIT-CARRIER precedent). The forks are genuinely owed (each = a runtime/CP spec amendment + clearance marker, X-AL-3 back-flow, full-spec PRE-AUTHORIZED but must be AUTHORED). HELD.
4. **§18.1 anchor honesty (the "phantom-cite dodge" probe).** Checked whether the doc's deliberate non-authoring of a byte-exact §18.1 cell cite is an honest deferral or a dodge. It is HONEST — the doc flags OQ-2 as SURFACED to E-spec-1, marks the §18.1 cell mapping `[MODERATE]`, and grounds the un-skip-at-WAL_SEGMENT claim on the resolvable `test_u_rt_95:129-130` test-reasoning (which I confirmed says only RECONCILER_LOOP/WAL_SEGMENT map to DURABLE_ASYNC cells). Declining to author an un-verified byte-exact §-cite is exactly the discipline; surfacing it as an open question is the correct move. HELD (this is good practice, not a finding).
5. **PathClass / IS-AL-1 placement (IS-domain attack).** Checked whether the STATE_LEDGER recommendation + conditional F-E-IS sub-fork respect the closed 4-class enum. They do — `path_class_registry.py:31-37` is the closed 4-class enum, IS-AL-1 forecloses invention, and the doc routes any non-honest mapping to an IS spec-extension sub-fork (decide-with-the-driver). HELD.
6. **api.resume re-scope (the engine-vs-workflow-layer correctness probe).** Checked whether the doc accidentally folds the DONE workflow-layer `api.resume` into arc E. It does NOT — `api.py:645` docstring self-identifies as the R-CC-1 arc #3 workflow-layer sibling, and the doc pins it as DISTINCT from E in §0, §1.2, and the §10 footer. HELD.
7. **External-canon divergence (durable-execution semantics, attack D).** Checked the event-sourcing / WAL / reconciler designs + named hazards against `Pattern_Reference_Catalog_v1.0.md` + cluster-4. The semantics and hazards are canonically correct; I-6 hand-roll is a rationale-bearing committed deviation. No divergence finding. HELD.
8. **Halt-route-split-AC (checklist item 9).** Checked whether any slice bundles materializable + unmaterializable atoms that should be split. The E-3 build / live-proof split IS exactly the correct split (build + non-live proof in scope; live K8s e2e gated + surfaced as an operator decision) — the doc applies the pattern correctly rather than violating it. HELD.
9. **Status-pointer edit to the Path-(i) fork (the second artifact).** Verified the CLOSED-DEFERRED → PROPOSING re-classification is procedurally legitimate per the fork's own §9 ("Reopening requires: re-classifying CLOSED-DEFERRED → PROPOSING + new operator routing decision + new closure arc") — the full-spec directive IS the routing decision; the status-update block correctly pins the un-skip to the WAL_SEGMENT (E-2) slice per `:129-130`. HELD.
10. **Sequence / SHARED-RUNTIMECONFIG cluster regression risk (§7.3).** Checked the "must compose with the landed 6-pattern topology dispatch without regressing SINGLE_THREADED_LINEAR byte-unchanged (CP §25.10 Invariant 1)" claim. The `workflow_driver.py:1342-1369` comments confirm the §25.10 Invariant-1 byte-unchanged SINGLE_THREADED_LINEAR path + the all-6-patterns-materialized state; the engine-class gate at `:1351` is orthogonal to the topology strategy dispatch at `:1375+`, so a new `_IN_SCOPE_ENGINE_CLASSES` member composes without touching the linear loop. The compose-without-regression note is sound. HELD.

---

## Disposition

**APPROVE — clear with inline cite-hygiene fixes.**

Per §4.1: no Class-3 finding → no phase re-opening. The single Class-2 (F2-01) is a *proposing* framing-precision reconciliation, not a content defect — both readings are defensible and the design doc's decomposition is in fact more precise than the SPINE's coarse label; a one-sentence reconciliation closes it. The 6 Class-1 findings are all cite-hygiene drift (3 misattributed/non-resolving cites F1-01/02/03/04, 1 count-drift F1-05, 1 sibling-staleness advisory F1-06) — none changes the design's substance, and the load-bearing structural claims (engine-materialization state, the built-but-unwired producer gap, the engine-vs-workflow-layer distinction, the EVENT_SOURCED-does-not-un-skip-Path-(i) claim, X-AL-3 cleanliness, the fake-producer foreclosure) all re-grounded correctly by direct read.

**The design is sound as a design dossier:** it is X-AL-3-clean (mode-agnostic, `.harness`-only, all materializations routed to authored forks), it honestly decomposes fork-vs-impl (not a defer-in-disguise), it respects the committed invariants (I-6 hand-roll, IS-AL-1 closed enum, CP §25.10 Invariant 1), and it mirrors the ratified B3-DESIGN design-first precedent. The cite-hygiene fixes (F1-01..F1-04 especially) should be applied before the E-spec legs consume this doc as authority, since those are byte-exact-citation-discipline violations (root CLAUDE.md §10.4) that would propagate if a downstream leg trusted them — but they are inline fixes, not merge blockers.

**Recommended pre-merge inline fixes (cheap):** F1-01 (re-cite "§90" → "Program §, line 90"), F1-02 (move the line-181 trigger cite from the r-cl-p2 row to the forward-register), F1-03 (correct the RECONCILER_LOOP server/managed inclusion-not-exclusion — it strengthens the point), F1-04 (correct the WAL-row docstring attribution). F1-05/F1-06 are optional polish.

---

## Severity-distribution self-audit

- Class 3: 0 · Class 2: 1 · Class 1: 6. Distribution is bottom-weighted (mostly cite-hygiene), consistent with a careful design dossier whose structural claims hold but whose citation discipline has the usual drift. Not all-Class-1 (the F2-01 framing note is a genuine moderate) and not escalation-inflated (nothing reaches Class 3 because nothing requires phase re-opening or upstream-artifact revision — the X-AL-3 cleanliness + correct fork-routing is the reason). Rejected-findings section is populated with 10 substantive checks. Every finding carries a Location + Evidence + discriminator. No author-mode drift (resolution paths describe the shape, e.g. "re-cite as", "correct to", not verbatim replacement prose).
