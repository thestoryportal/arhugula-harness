# Beyond-MVP Capability-Boundary Ledger

**Authored:** 2026-06-12 · **Posture:** mode-agnostic (process-substrate; grounds `harness-*/src` + canonical specs + roadmap at HEAD `3d805d2` by direct read; authors only this `.harness/` file). **Trigger:** operator directive — the harness is being built **BEYOND MVP**, so any feature still *bounded to MVP scope* is an OPEN item that must be **built out** OR **explicitly re-ratified as a permanent bounded-residual** before harness development can be definitively closed.

**Method:** three read-only Explore sweeps (code / specs / roadmap) → every material claim re-grounded by direct read (Explore is presence-not-correctness). The re-grounding caught and discarded several **stale docstrings** that read as boundaries but were superseded (Bucket C) — listing those as "open" would have overstated the work.

**The discriminator (per `.harness/capability-completion-inventory-v1.md` + `[[grounding-reveals-claude-closeable-slice-close-honestly]]`):** ratified-DEFER → *confirm, don't rescue*. So this ledger does **not** re-litigate items the operator already dispositioned (Bucket A); the actionable payload is **Bucket B** — boundaries that are genuinely *not* tracked as build arcs.

---

> ## ⚑ STANDING DIRECTIVE — FULL SPEC, NOTHING DEFERRED (operator 2026-06-12, will-NOT-drift)
>
> *"Nothing is deferred and the full spec will be built beyond MVP, no exceptions or excuses. This is the objective and it will not drift moving forward. If a producer, wiring, surface, etc must be researched, designed, speced and planned to unblock code implementation then it will happen...period."*
>
> **This directive REWRITES the buckets below.** "ratified-DEFER → confirm-don't-rescue" is **superseded**: **Bucket A is RE-OPENED — every deferred capability is now a BUILD item, on equal footing with Bucket B.** The only retained discriminators are: **Bucket C** (genuinely stale docstrings → doc-hygiene, nothing to build) and the **paid-call / secret / vendor decision boundary** (design/spec/plan to the point of decision, then surface the one external input — corpus, embedding-model, router-model — never auto-fire it). See `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`.
>
> **WHAT vs HOW (the line a future session must not blur):** the directive sets WHAT (full spec, nothing deferred); the committed ADRs/invariants still govern HOW — **I-6** (hand-roll the durable engines + topology orchestration; NO vendored framework/LiteLLM), **ADR-F1** (per-provider SDKs, capability-aware). "No exceptions" is about scope, NOT about overriding the invariants the spec was designed under.
>
> **Scope = two levels (both driven to zero):**
> 1. **MVP-marker boundaries** (reduced-but-present features) — Bucket A (re-opened) + Bucket B below.
> 2. **Contract-level completeness** (specced `C-*` with no/partial carrier) — the deterministic overlay audit (`just overlay-query --orphans` → `contract w/o code cite`). At HEAD: **8 candidates** — `C-CP-30, C-CP-37, C-CP-43, C-CP-49, C-CP-50, C-IS-11, C-OD-3, C-RT-28` (advisory — cite-absence ≠ non-implementation; each verified per-contract in the scoping arc). This is the *other half* of "full spec" an MVP-string sweep misses.
>
> **Vehicle:** a single ACTIVE umbrella program **`R-FS-1`** (full-spec build) with the capabilities as ordered child arcs (the proven R-CC-1 shape); the old DEFERRED roadmap entries point AT it rather than each going independently ACTIVE (avoids the §4 `all(deps RESOLVED)` trap). **Consequence the operator owns: this pushes R-CL-Q1→C1 (the entire quality/close track) behind the full-spec build program.**

---

## Bucket B — Newly-surfaced MVP capability boundaries (NOT tracked as build arcs) — THE PAYLOAD

These are reduced/stubbed/single-case at MVP, the full behavior is a known larger thing, and a roadmap grep confirms **none is on the §5 catalog as a build arc** (the closest, topology, appears only as a RESOLVED *test-fixture* entry that explicitly defers the orchestration to "CP-axis Class-1 back-flow, out of fences"). Each needs an explicit operator call: **BUILD** or **permanently RATIFY as residual**.

| # | Boundary (MVP state) | Evidence (HEAD `3d805d2`) | What "beyond MVP" requires | Load-bearing | Rec |
|---|---|---|---|---|---|
| **B1** | **Workflow topology: 1 of 6 patterns.** Only `SINGLE_THREADED_LINEAR` has real orchestration; the other 5 (`PARALLELIZATION`, `ORCHESTRATOR_WORKERS`, `DECENTRALIZED_HANDOFF`, `HIERARCHICAL_DELEGATION`, `EVALUATOR_OPTIMIZER`) are admissible + emit telemetry but are a **stateless passthrough — no orchestration semantics**. | `workflow_driver_errors.py:29` ("only SINGLE_THREADED_LINEAR is materialized; other 5 deferred per X-AL-3"); `api.py:244` ("no parallel branching at runtime"); roadmap §5 line 873 ("NOT distinct orchestration semantics (unbuilt; CP-axis Class-1 back-flow)"). | True fan-out (parallel), generator/handoff (decentralized), multi-level dynamic delegation, evaluator-optimizer loop — i.e. **multi-agent parallel + cross-agent communication**. CP-axis contract (C-CP-25) design + build. | **HIGHEST** — this is the single biggest beyond-MVP gap; it *is* the "multi-LLM parallel / cross-comm" capability. | **BUILD** (large CP-axis design+impl program; design-fork-first per X-AL-3) |
| **B2** | **Multi-server MCP client: single-server only.** The harness connects to exactly one MCP tool server per bootstrap; the v1.15 config field type is **singular** (`mcp_client_host: MCPClientHost`, not a mapping). | `mcp_client_host_factory.py:11` ("v1.15 MVP supports [one server]"); `:21` (">1 server deferred; field type is singular; future-arc schema extension"); `:169` ("one host per first configured client"). | A mapping of named MCP servers + per-server discovery/routing/trust-tier (the `:194` trust mapping is also MVP-conservative). | **HIGH** — a real agent typically needs several tool servers concurrently. | **BUILD** (AS/runtime schema extension + factory) |
| **B3** | **HITL gate decision intelligence: always-on + full-palette, placeholder placement matrix.** The gate fires + writes ledgers (proven, arc #8), but its *decision* logic is MVP-stubbed: `_hitl_required` is **always True on a matching placement** (no conditional predicate), the response palette is **`DEFAULT_FULL_PALETTE` unconditionally** (no per-cell narrowing/escalation), and `HandoffContext`/placement composition is a placeholder. | `hitl_gate_composer.py:56-81` ("v1.11 MVP… the gate always fires… palette: DEFAULT_FULL… placement composition deferred to workflow-grammar arc"); `:181-185`. | §14.8.2 step-4c conditional gating + step-4d palette narrowing + the placement matrix + HandoffContext binding. Pairs with the **OQ-6 auto-timeout-degradation** producer (Bucket A). | **HIGH** — "smart HITL" is core to the OD/HITL value proposition. | **BUILD** (runtime + CP workflow-grammar arc) |
| **B4** | **Per-role / per-step dispatch: not runtime-indexed.** Both routing (`RoutingManifest.per_role_bindings`) and prompt selection (`PromptSelectionManifest.per_role_bindings`) carry a per-role binding **structurally**, but the role dimension is **discarded at dispatch** — MVP uses a single default role. | `prompt_selection_manifest.py` (per-role "runtime-deferred at the consumer site"); routing's own `per_role_bindings` has no runtime indexer (role discarded at `llm_dispatch.py:489` until R-300, per PR #509 record). | Thread `AgentRole` (and per-step override) through dispatch so per-role model + per-role prompt actually take effect. | **MEDIUM-HIGH** — gates per-agent specialization in multi-agent topologies (composes with B1). | **BUILD** (runtime dispatch indexing; small once B1 motivates it) |
| **B5** | **Memory backend: per-deployment-surface selection stubbed.** The backends exist + are live (R-830: SQLite/S3/Neon), but `resolve_backend(deployment_surface)` **ignores its argument** and returns the single pre-resolved backend. | `memory_tool_registry.py:5` ("MVP: resolve_backend(deployment_surface) ignores its argument"); `:28` ("factory landing owed at U-RT-80; v2.15 MVP stores a single pre-resolved backend"). | Surface-aware backend routing (the `U-RT-80` factory). | **MEDIUM** — backends work; only automatic surface→backend selection is stubbed. | **BUILD** (small — U-RT-80 factory) |
| **B6** | **Per-tool sandbox-tier driver selection + STDIO transport-floor.** Arc #1 (#503) landed per-*server-uniform* tier→driver selection; the §14.9.8 flat resolver still bypasses the full `sandbox_tier_floor` composition incl. the ADR-D2 §1.3 STDIO tier-3 transport-floor. | Runtime spec §14.9.8/§14.9.9 (Scope discipline — per-tool is future); CXA v2.20 §0.4 (STDIO tier-3 floor not enforced by the per-server-uniform resolver). | Per-tool tier resolution + STDIO transport-floor enforcement. | **MEDIUM** — security-posture refinement (drivers + per-server selection already live). | **BUILD** (bounded runtime arc) — *carried forward by arc #1's own scope note* |
| **B7** | **OD sampler: §9.2 conditional-row over-sampling.** The sampler always-samples *all* `files.operation` / `memory.operation` / `validator.fail.*` / `subagent.span` spans, not only the mutation/permanent/root variants the spec mandates. Conservative (samples more, never less). | `composite_sampler.py:21-37`. **(Per-tier base-rate 1.0/0.1/0.2 IS live — proven arc #5 at the bootstrap; this row is only the conditional-row refinement.)** | Per-attribute (`kind`/`permanence`/root) conditional sampling. | **LOW** — Class-3; cardinality reduction only. | **RATIFY-or-fold-into-Q1** (over-sampling is safe) |

**Bucket-B minor field-level scopes** (roll up under whichever arc touches them; individually low): `workflow_driver_types.py` `parent_entry_hash` empty-string sentinel + `parent_gate_level` AUTO-default (per-step gate-level not operator-surfaced); `skills.py` cost estimate = description-length proxy (not token-counting, §14.17.7); `pause_resume` `StateSummary`/`PauseContextReader` capture is MVP-minimal (§14.14.7) + anchor-validation `U-CP-22` deferred.

- **B-INTERSTEP — inter-step DATA-flow channel (surfaced by U-CP-87 / arc #13; Codex [P1]; cross-topology, currently UNTRACKED → forward arc).** The driver threads **no data between steps** for ANY topology: the `SINGLE_THREADED_LINEAR` path builds `accumulated` step outputs but never passes them into `dispatch(...)`, and the `dispatch(binding, step, step_context)` signature carries no prior-output parameter. So a step output cannot reach a subsequent step's *input* at the driver level — e.g. `EVALUATOR_OPTIMIZER`'s generate-draft → evaluate-*input* and feedback → regenerate-*input* (the §25.11 "regenerate-with-feedback" content flow). At the driver this is correctly **control-flow only** (U-CP-87's plan-defined "non-hollow" bar = the per-step-prompt distinction, which is built+tested; the cleared U-CP-86 also threads no data). The DATA channel is a **runtime/dispatcher capability** (a shared run-context the dispatcher reads, or B4-adjacent per-step input composition) — NOT covered by B4 (per-role *selection*) and threading it via `StepExecutionContext`/the dispatcher Protocol would be an X-AL-3 spec extension. **Owner = a future runtime data-flow arc (design-fork-first).** Load-bearing: MEDIUM-HIGH for genuinely effective evaluator-optimizer / orchestrator-workers with real model dispatchers (composes with B1 + B4). Rec: **BUILD** (runtime arc; design-fork-first per X-AL-3). **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (runtime spec v1.58 → v1.59 NEW §14.21 C-RT-29 + `harness-runtime/src` + `harness-cp/src` + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the *"shared run context the dispatcher reads"* §25.11 itself named: a NEW run-scoped `InterStepOutputChannel` (a plain by-reference holder — the `CostRecordAccumulator` CA #625 precedent, NOT a Pydantic-copied container), bound at stage 5 LOOP_INIT only when `RuntimeConfig.inter_step_data_flow=True` (default `False` → byte-identical). The CP driver records each completed step's output (`SINGLE_THREADED_LINEAR` `accumulated` site + `EVALUATOR_OPTIMIZER` loop) via `getattr`-dynamic `_record_inter_step_output` (the `cp_is_wiring` idiom — harness-cp does not import the runtime holder); the **genuine consumer** is the C-RT-15 LLM dispatcher, which injects `most_recent_output()` into the dispatched provider payload (threaded via the `cost_record_sink` precedent). The `dispatch` Protocol signature is UNCHANGED + §25.3.3.4 step-body-opaque PRESERVED → **NO operator gate** (additive + opt-in, no committed-invariant sacrifice; mechanism resolved by landed precedent → advisor, not council). **Non-vacuity is the deliverable (advisor):** the real dispatcher injection reaches the **actual provider call** (`test_lifecycle_llm_dispatch.py::test_inter_step_channel_injects_prior_output_into_provider_call` — NOT a stub) + the producer half is proven in a real EO driver run (`test_workflow_driver_evaluator_optimizer.py::test_evaluator_optimizer_dispatch_observes_prior_step_output` — evaluate sees the generate draft; regenerate sees the evaluator feedback). **Genuinely-complete sequential-write scope** (`SINGLE_THREADED_LINEAR` + `EVALUATOR_OPTIMIZER`); **three follow-ons registered (NOT silent-deferred):** concurrent-fan-out recording for the 4 remaining non-linear strategies via the #648 buffered-branch drain (**`B-INTERSTEP-NONLINEAR`**, ADR-F2) + cross-step resume rehydration (composes with **`B-ENGINE-OUTPUT-REPLAY`** — the F2 `EntryPayload` carries only a `response_hash` digest, not the activity output; EO's within-loop flow is resume-safe) + **per-run channel isolation (`B-INTERSTEP-PERRUN-ISOLATION`)** ✅ BUILT + MERGED `#671` 2026-06-20 (v1.64; dedicated entry below) — a `ContextVar`-scoped channel that closes THREE things: REMOVES the v1.59 single-flight serialization (below) + closes the **timeout-zombie** (Codex r5 — a run exceeding `drain_timeout_seconds` leaves a non-cancellable `to_thread` worker still recording after the lock releases) + closes the IDENTICAL bootstrap-scoped shared-holder exposure `cost_record_accumulator` carries (CA #625 — a concurrent-daemon cost-rollup cross-run mix, both at completion AND under the same timeout-zombie trigger; observability-additive, lower-severity, same root). Systemic across every bootstrap-ctx holder (a channel-only run-key = false-complete); registered because B-INTERSTEP is the first capability whose *correctness* (not just observability) needs it. **Daemon-scope hardening (Codex out-of-family, 3 rounds):** the bootstrap-allocated channel is run-scoped CONTENT, so a reused `HarnessContext` (daemon-client U-RT-108) leaks across `run_workflow` invocations — invariant 7 is honestly **bounded** to: (a) *sequential* reuse closed by a per-run `reset()` (r3); (b) *concurrent-within-drain-timeout* closed by a per-running-loop single-flight **lock** wrapping `[reset + execute]` when the channel is bound (r4 — opt-out default never locks → full concurrency unchanged); (c) the *concurrent-with-timeout* `to_thread`-zombie (r5) = a SYSTEMIC reused-ctx residual NOT closeable by a lock → registered as `B-INTERSTEP-PERRUN-ISOLATION` above (real-but-accepted; triage+register, NOT looped to Codex-green). Gates: pyright 0/0/0; ruff; harness-runtime + harness-cp non-e2e green; daemon-concurrency-serialization test + negative control; no §5.2-hash change (ephemeral run-OUTPUT). advisor (full-transcript — non-vacuity-is-the-deliverable + resume/scope/Option-B + r5-residual-triage decisions) + Codex out-of-family (5 real bugs across 4 rounds + the r5 systemic-residual surface: `[[hooks-codex-pilots-decorrelation-validated]]`). Fork: `.harness/class_1_fork_b_interstep_data_flow.md`; clearance `Spec_Harness_Runtime-v1_59-cleared-2026-06-18.md`. `[[r-cxa-seam-wiring-is-producer-discovery]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[stale-carry-text-disposition]]`.

- **B-INTERSTEP-PERRUN-ISOLATION — per-run ContextVar isolation of the run-scoped inter-step channel + cost accumulator (registered B-INTERSTEP follow-on §3/§5; UNTRACKED → forward arc).** B-INTERSTEP allocated its run-scoped `InterStepOutputChannel` at **bootstrap** scope on the frozen `HarnessContext`; in daemon-client mode (U-RT-108) one bootstrapped ctx serves many `run_workflow` invocations, so concurrent runs SHARED the one channel. B-INTERSTEP's §14.21.5 invariant 7 was honestly **bounded** to (a) sequential reuse (per-run `reset()`) + (b) concurrent-within-timeout (a per-loop single-flight **lock**), with (c) the `drain_timeout_seconds` **TIMEOUT-ZOMBIE** named as a registered residual — a run exceeding the timeout leaves a non-cancellable `asyncio.to_thread` worker that keeps recording after the lock releases (the lock *serializes*, it does not *isolate*, and a released lock cannot fence a zombie). The fork §3/§5 named this **SYSTEMIC across every bootstrap-ctx holder** (the IDENTICAL exposure `cost_record_accumulator` carries, §82 Class-3) and prescribed a `ContextVar`-bound per-run channel. Load-bearing: B-INTERSTEP is the first capability whose *correctness* (not just observability) depends on per-run isolation. Rec: **BUILD** (runtime arc; design-fork-first per X-AL-3 — design authority = the B-INTERSTEP fork §3/§5). **✅ BUILT + MERGED `#671` (this arc, 2026-06-20, the FIFTEENTH standalone `B-*` arc — the first BEYOND the originally-enumerated 14-arc frontier) — bundled-absorption (runtime spec v1.63 → v1.64 §14.21.5 inv 7 CLOSED + §14.21.7 follow-on BUILT + `harness-runtime/src` + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism (the fork-prescribed `ContextVar`): each run-scoped mutable holder is scoped **per run** via a `ContextVar` resolved at dispatch; the frozen ctx binds a stable PROXY (`RunScopedInterStepOutputChannel` / `RunScopedCostRecordAccumulator`, each IS-A the field type) that delegates to the current run's holder. The `run_workflow` handler sets a fresh holder per run; the set propagates into the `asyncio.to_thread` driver worker via `contextvars.copy_context()` (**verified empirically** — caller-set → handler → worker, and a handler-set value does NOT leak back to the caller). So (a)+(b) need NO lock and (c) is closed — a timed-out zombie writes only the holder captured in ITS context copy, never a following run's. **Both holders, ONE mechanism:** (1) `InterStepOutputChannel` (opt-in — opt-out leaves `ctx.inter_step_output_channel is None`, byte-identical); (2) the always-on `CostRecordAccumulator` (§82 Class-3 — the per-dispatch cost wrappers now thread the PROXY, not a `.records` list captured once at bootstrap — the capture that defeated isolation; `api.run`/`resume` set a fresh accumulator around `[invoke + cost-read]` so the post-run rollup read resolves the same accumulator the wrappers appended to). The B-INTERSTEP single-flight lock (`_inter_step_run_lock` / `_INTER_STEP_RUN_LOCKS`) is **removed**. **Other bootstrap-ctx holders assessed (NOT a channel-only false-complete):** `resume_context_holder` is set-per-run overwrite, `engine_output_store` is `run_idempotency_key`-keyed, ledger writers are ADR-F2 single-writer + run_id-tagged — none accumulate run-scoped contents on a shared holder, so none need isolation; the generic proxy primitive composes for any future holder that does. **Non-vacuity is the deliverable (advisor):** the TIMEOUT-ZOMBIE test (`test_timeout_zombie_writes_only_its_own_per_run_channel`) exercises the REAL `asyncio.to_thread` copy_context — A's timed-out non-cancellable worker records its late entry into channel A (captured), NOT B's channel — paired with a NEGATIVE CONTROL (`test_negative_control_shared_channel_timeout_zombie_corrupts`) that reproduces the cross-run corruption WITHOUT isolation (proving the mechanism is load-bearing); plus concurrent-isolation + cost-isolation tests, each with its own negative control. The freeze-by-reference + per-run-resolution seam is re-proven end-to-end at `test_bootstrap.py::test_bootstrap_cost_accumulator_survives_freeze_by_reference`. **Child-workflow inheritance verified** (the `ChildWorkflowRunner` re-enters `execute_workflow` synchronously in the parent's worker context → inherits the parent's ContextVars → child cost rolls into the parent run, unchanged). Gates: pyright 0/0/0 · ruff · harness-runtime 1972 passed / 13 skipped · harness-cp 1093 passed / 1 xfailed · no §5.2-hash change. advisor (full-transcript — the ContextVar-propagation probe + the always-on cost-path correctness + the opt-out-byte-identical guard + the child-inheritance blind-spot) + out-of-family Codex (pre-merge). Fork: `.harness/class_1_fork_b_interstep_perrun_isolation.md`; clearance `Spec_Harness_Runtime-v1_64-cleared-2026-06-20.md`. `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[full-chain-witness-not-half-proofs]]` · `[[verification-shape-sharpened-grep-vs-e2e]]` · `[[stale-carry-text-disposition]]`.

- **B-INTERSTEP-HANDOFF — inter-step DATA flow for DECENTRALIZED_HANDOFF (registered B-INTERSTEP-NONLINEAR follow-on, handoff slice; UNTRACKED → forward arc).** B-INTERSTEP (#651/v1.59) shipped inter-step data flow for the two sequential-write topologies (`SINGLE_THREADED_LINEAR` + `EVALUATOR_OPTIMIZER`) and registered the non-linear strategies as `B-INTERSTEP-NONLINEAR`. At HEAD `_execute_decentralized_handoff` (`workflow_driver.py:4923`) dispatched each stage-expert but recorded NO output to the channel → a later stage read `most_recent_output() is None` (control-flow-only handoff: the `HandoffContext` ownership-transfer records, never the prior stage's OUTPUT content). Load-bearing: MEDIUM (genuine multi-stage pipeline data flow — stage B conditions on stage A's output). Rec: **BUILD** (the cleanest non-linear slice per the advisor decision-rule: minimal non-vacuous consumer needs NO discriminator the spec doesn't give). **✅ BUILT + PR `#677` (this arc, 2026-06-21) — impl-to-cleared-spec (NO design-substrate edit; runtime §14.21 C-RT-29 §14.21.7 registers the recording follow-on; the consumer is the existing C-RT-15 dispatcher), `harness-cp/src` (the handoff record site) + by-execution tests, NO operator gate.** Mechanism: after a stage completes (`stage_outputs[step_id]=output`, `workflow_driver.py:5137`) the driver calls the SAME `_record_inter_step_output(ctx, step_id, output)` helper the linear/EO sites use → the next stage-expert's dispatch reads `most_recent_output()` off the dispatcher construction ref (§14.21.2) + the LLM dispatcher injects it. **Single-owner sequential → recorded INLINE on the driver thread** (driver `workflow_driver.py:4933-4943` — "NO fan-out, NO `TaskGroup`"), NOT via the #648 buffered-branch drain (which serializes CONCURRENT sibling writes — structurally absent here); ADR-F2 single-threaded-write holds. **NO operator gate** (additive + operator-opt-in: opt-out `ctx.inter_step_output_channel is None` → no-op, byte-identical; `dispatch` signature UNCHANGED + §25.3.3.4 step-body-opaque PRESERVED; no committed invariant sacrificed). **Why handoff is the cleanest slice (advisor tie-breaker):** the 3 concurrent topologies' genuine post-join synthesis consumer wants ALL siblings (the existing consumer injects only `most_recent_output()` — one arbitrary last-drained sibling → shipping that = soft built-but-vacuous, `[[built-but-vacuous-reground-ledger-asis]]`); handoff has exactly one predecessor, so `most_recent_output()` is GENUINELY correct (stage B sees stage A) with no message-shape discriminator. **Non-vacuity is the deliverable — FULL-CHAIN witness (`[[full-chain-witness-not-half-proofs]]`):** the producer witness (`test_workflow_driver_decentralized_handoff.py::test_decentralized_handoff_records_inter_step_output_for_next_stage` + a NEGATIVE-CONTROL opt-out test) + the full-chain witness through the REAL path (`test_lifecycle_llm_dispatch.py::test_decentralized_handoff_inter_step_output_reaches_real_provider_call` — a 2-stage handoff via `execute_workflow` → `_execute_decentralized_handoff` → the REAL `RuntimeLLMDispatcher` over the production `SyncDispatcherFacade` async/sync bridge → stage B's ACTUAL provider call carries stage A's distinct output token as the injected "Upstream step output:" message). **Class-3 informational (logged here, this entry being the prose home per `CLAUDE.md` §4.3 Class-3 routing):** §14.21.7 + the parent fork `class_1_fork_b_interstep_data_flow.md` §3 imprecisely lump DECENTRALIZED_HANDOFF with the concurrent topologies under "buffered-branch drain" — handoff is **single-owner sequential** (driver `workflow_driver.py:4933-4943`), so it records INLINE on the driver thread like the linear/EO sites; non-blocking, the drain mechanism stays correct for the 3 concurrent topologies. A batched §14.21.7 spec refresh may correct the lumping at the concurrent-topologies arc. **Registered follow-on re-scope:** `B-INTERSTEP-NONLINEAR` now covers the 3 genuinely-CONCURRENT strategies (ORCHESTRATOR_WORKERS, PARALLELIZATION, HIERARCHICAL_DELEGATION) via the drain + the all-siblings read. Gates: pyright 0/0/0 · ruff · harness-cp `test_workflow_driver_decentralized_handoff.py`/`test_workflow_driver.py` green · harness-runtime `test_lifecycle_llm_dispatch.py` non-e2e green. advisor (full-transcript: arc selection + fork-vs-not crux + slicing tie-breaker + Class-3 call + the mislabeled-artifact catch — an impl-to-cleared-spec arc with NO operator decision is NOT a Class-2 fork, so the prose home is THIS spine ledger, not a `class_N_fork_*.md`) + out-of-family Codex (pre-merge). `[[full-chain-witness-not-half-proofs]]` · `[[built-but-vacuous-reground-ledger-asis]]` · `[[cleared-spec-resolves-it-before-first-principles-fix]]` · `[[spine-ledger-forward-arc-registration]]`.

- **B-INTERSTEP-NONLINEAR — RESOLVED-AS-HOLLOW (PR #705, 2026-06-22; runtime spec v1.74 §14.21.7/§14.21.5 prose correction).** The registered concurrent-fan-out recording follow-on (the 3 CONCURRENT strategies ORCHESTRATOR_WORKERS / PARALLELIZATION / HIERARCHICAL_DELEGATION; DECENTRALIZED_HANDOFF already closed at B-INTERSTEP-HANDOFF #677) is **resolved-as-hollow**: recording sibling/aggregate outputs into the §14.21 inter-step channel has **NO non-vacuous consumer** under the cleared C-CP-25 §25.11/§25.12 deterministic-fold semantics. Body-read (advisor-confirmed): `_aggregate_orchestrator_workers` / `_aggregate_parallelization` compose via a DETERMINISTIC fold receiving `branch_outputs` DIRECTLY (not from the channel); the fold docstring states it verbatim — *"there is NO second 'compose' dispatch (that would need the deferred inter-step DATA flow, B-INTERSTEP)"*; topology is per-workflow so the fold output IS the terminal `RunResult` (no post-join LLM read, no subsequent linear step to read `most_recent_output()`). So channel recording fails the §14.21.5-invariant-6 non-vacuity bar (`[[r-cxa-seam-wiring-is-producer-discovery]]`). The v1.59 *"genuine inter-step reads are sequential (orchestrator synthesis post-join), so the channel composes once recording lands"* framing was a **spec-prose-vs-impl drift** (`[[stale-carry-text-disposition]]`), corrected at v1.74. **Two excavated micro-options REJECTED as over-excavation (advisor):** Option A (record the fan-out aggregate for a subsequent linear step) has no consumer under per-workflow topology — the parent-linear SUB_AGENT_DISPATCH case is ALREADY covered by the v1.59 linear recording; a tuple-carrier-ADD-multi tangent (from the sibling LOOSEN check) is a different unspecified capability, not a slice of this arc. The genuine capability = Option B = **B-POSTJOIN-LLM-SYNTHESIS** (below), which is OPERATOR-GATED. Mode-agnostic record-correction PR (spec prose + arc-ledger registered→resolved + this entry + clearance); NO impl change. advisor (the hollow-confirm + the over-excavation discipline + the FULL-SPEC "build what the spec DEFERS, not every expressible monotone extension" discriminator). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[built-but-vacuous-reground-ledger-asis]]` · `[[disposition-label-is-a-claim-verify-against-spec]]`.

- **B-POSTJOIN-LLM-SYNTHESIS — post-join LLM synthesis step for concurrent fan-out (registered B-INTERSTEP-NONLINEAR successor; was gated on C-CP-25 §25.12 → AUTHORIZED 2026-06-22; 2026-06-22).** The genuine inter-step capability the now-resolved-as-hollow B-INTERSTEP-NONLINEAR gestured at: an LLM-dispatched step that, AFTER the concurrent fan-out barrier, reads the sibling branch outputs (making the §14.21 channel recording non-vacuous — the consumer that does not exist today) and composes a SYNTHESIZED result. Today these topologies compose via a DETERMINISTIC fold that the C-CP-25 §25.12 contract GUARANTEES is deterministic ("first to finish wins is forbidden"). An LLM synthesis compose makes the fan-out output **NON-deterministic** → it SACRIFICES the §25.12 determinism guarantee (a committed decision). Per CLAUDE.md §13.4 / `[[feedback-gate-only-on-meaningful-architecture-change]]` a committed-decision sacrifice is precisely the gate condition → **OPERATOR-GATED** (a Class-1 topology-semantics fork); register, do NOT build; surface the §25.12-relaxation request only if a real workflow needs LLM post-join synthesis. No concrete workflow demands it today. **➤ AUTHORIZED 2026-06-22 — supersedes the gate above:** the operator DECLINED "declare build-complete" and AUTHORIZED building BOTH final arcs for genuine full-spec closure → this arc is CLEARED to build via the design-first pipeline (council [determinism/replay ⊥ canonical-synthesis] → ratify the opt-in mechanism [default deterministic fold byte-identical; synthesis is an opt-in non-deterministic mode documented in §25.12 + replay/hash semantics] → §25.12 spec amendment + clearance → impl with full-chain witness → adversarial review) per `.harness/r-fs-1-final-closure-plan.md`. **Sequenced SECOND** (after B-HITL-PLACEMENT-PER-STEP-LOOSEN); also the non-vacuous consumer that makes B-INTERSTEP-NONLINEAR recording non-hollow (recording + consumer ship together). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[spine-ledger-forward-arc-registration]]` · `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`.

- **B-FANOUT-OUTPUT-REPLAY — reproducible cached-replay of the fan-out synthesized aggregate across a crash-resume (the §25.12-D1 follow-on to B-POSTJOIN-LLM-SYNTHESIS; CLOSED PR1 #723 + PR2 2026-06-24).** B-POSTJOIN made the synthesis non-reproducible across a crash (the synthesis re-dispatches FRESH; OPTION-B-capture-only is #705-hollow). This arc makes it reproducible. **PR1** (deterministic-fold crash-resume core, PROCEED-gated): a non-attested, branch-index-KEYED `EngineOutputStore` sidecar (operator-RATIFIED A #721 — §25.12-D1 preserved literally, NOT a second committed-invariant sacrifice; the #719 C1⊥C9 council corrected the "second sacrifice" framing) + the `_determine_fanout_resume` recovery + disposition keystone + ledger re-materialization. **PR2** (synthesis self-hash + captured-output replay): a record-local capture-time synthesis self-hash (the #719 C9 sole-authority-no-digest residual) + captured-synthesis-output replay that RELAXES the synthesis-bearing crash-resume fail-closed; the post-capture W3 window replays byte-identical, the pre-capture window re-dispatches fresh (safe — the synthesis is effect-free + the lost aggregate was never committed; Codex round-4 advisor-reconciled decline of a started-marker). Hardened over 4 out-of-family Codex rounds (rounds 1-3 = real correctness catches: incomplete-sidecar stale-replay, escaping replay-append, synthesis-removed material-diff; round 4 = the asymptotic stricter-than-PR1 tail, declined-and-documented). CP spec v1.54→v1.56 + runtime v1.75→v1.77. `[[full-chain-witness-not-half-proofs]]` · `[[durable-recovery-presence-validity-scope]]` · `[[hooks-codex-pilots-decorrelation-validated]]`.

- **B-FANOUT-CRASH-RESUME-CASCADE-POLICY — cascade-policy-aware fan-out crash-resume, STRICT-TIER-conservative (CLOSED 2026-06-24; impl-to-cleared-spec on §25.15).** Corrects the PR1 blanket fail-closed (PAUSE + CASCADE_CANCEL). For the strict tiers a crash-resume CONTINUES ONLY when the recovery is COMPLETE (every branch recovered → nothing to re-dispatch) ∧ no-error; every other state fails closed. Detection = `_crash_degraded` (recovered branch `output is None`) + `_crash_complete` (`len(branches) == worker_count`/`branch_count`). An errored ORCHESTRATOR is NOT a cascade trigger (returns FAILED directly before any worker — `cascade_policy governs WORKER failure`, :6548), so detection over `.branches` is complete (VERIFIED by orchestrator-workers witnesses — advisor BLOCKING false-negative catch). Outcomes (strict tiers): errored → CASCADE_CANCEL reproduce FAILED no-re-dispatch (obligation 7; advisor's "not-yet-dispatched ≠ effect-free") / PAUSE fail-closed-ambiguous (naive reconstruct spec-foreclosed by §25.15.1 "finish in-flight then pause" — probe-resolved, no council → B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT); INCOMPLETE → fail closed (an absent branch is ran-but-uncaptured-ambiguous, branches effect-BEARING — Codex [P1]; broad recovery → B-FANOUT-CRASH-RESUME-STRICT-TIER-INCOMPLETE, needs at-most-once dispatch); COMPLETE∧no-error → continue (finalize, re-dispatch nothing — VERIFIED). PROCEED unchanged. **The reconcile killed the broad "clean-crash lifts the common case" — the dichotomy that made it safe was about the FAILING branch's capture; Codex caught the CLEAN branch's dispatch-before-capture (effect-bearing ⟹ can't re-dispatch on ambiguous absence). Honest scoping: delivers CASCADE_CANCEL→FAILED + complete-recovery completion, NOT the common incomplete-crash recovery.** CP spec v1.56→v1.57. `[[durable-recovery-presence-validity-scope]]` · `[[probe-resolves-fork-prescribed-council]]` · `[[hooks-codex-pilots-decorrelation-validated]]`.

- **B-FANOUT-PAUSE-SYNTHESIS — synthesis on the EXPLICIT-PAUSE resume path (CLOSED #728 2026-06-24; impl-to-cleared-spec on §25.15.1 + §3/§4; CP-only).** v1.56 PR2 relaxed the CRASH-resume synthesis fail-closed but LEFT the pause-path reject (`post-join-synthesis-on-resume-unsupported`). Unlike the crash path (where the synthesis MAY have run + been captured → replay), an EXPLICIT-PAUSE halts at the worker barrier strictly BEFORE the post-join synthesis → the synthesis is guaranteed never-ran, so the pause carrier needs only the synthesis IDENTITY (presence + `step_id`), NOT a captured output. MECHANISM: `synthesis_step_id` added to `FanOutResumeState` + `PeerFanOutResumeState` (captured at the 2 pause-construction sites); the v1.56 §2 blanket reject REPLACED by a single `_synthesis_resume_material_diff(resume_snapshot, steps)` entry guard — captured-vs-resumed identity match → PROCEED (recover/re-dispatch branches + FRESH-dispatch the synthesis post-barrier, effect-free + first-and-only per B-POSTJOIN), mismatch (added/removed/changed) → fail closed `post-join-synthesis-resume-material-diff`. The ONE helper covers the silent-DROP "removed" case a placement-nested check would structurally miss (advisor). BYTE-COMPAT: `_compute_snapshot_hash` drops `synthesis_step_id` when None on BOTH carriers (mirrors the `paused_child_branches` drop on FanOut; ADDS the analogous drop to PeerFanOut, which had none) → existing snapshots hash byte-identically. HIERARCHICAL child levels re-enter via `execute_workflow(pause_snapshot_input=child_snapshot)` (traced empirically: `sub_agent_dispatch.py:637` → `child_workflow_runner` → `execute_workflow`) → the SAME entry guard against the child's own snapshot, so entry-side option-B covers the child path — no follow-on needed (advisor-gated). `step_id`-only material-diff is sufficient (fresh-dispatch not replay → a same-`step_id` body change yields a valid fresh synthesis). CP spec v1.57→v1.58. `[[full-chain-witness-not-half-proofs]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[advisor-before-substantive-work-for-cross-axis-blockers]]`.

- **B-FANOUT-RESUME-CARRIER-TOPOLOGY-MISMATCH — general fail-closed on ANY topology resume carrier/strategy mismatch (CLOSED #730 2026-06-24; impl-to-cleared-spec on §26.2 + §25.15.x; CP-only).** B-FANOUT-PAUSE-SYNTHESIS closed the carrier/topology-mismatch double-execution gap ONLY for a SYNTHESIS-bearing fan-out snapshot (the `post-join-synthesis-resume-carrier-mismatch` guard, keyed on the synthesis identity), and v1.58 §1 explicitly scoped out the rest as "PRE-EXISTING … unchanged." This arc closes that carve-out: a single `_resume_carrier_topology_mismatch(resume_snapshot, strategy)` keyed on **carrier-populated alone** (not synthesis identity) fails closed (`resume-carrier-topology-mismatch`) when a snapshot's populated topology resume carrier is NOT read by the resuming strategy — for ALL FIVE carriers (`fan_out_resume`→{ORCHESTRATOR_WORKERS,HIERARCHICAL_DELEGATION} / `peer_fan_out_resume`→{PARALLELIZATION} / `handoff_resume`→{DECENTRALIZED_HANDOFF} / `evaluator_optimizer_resume`→{EVALUATOR_OPTIMIZER} / `effect_fence_resume`→{SINGLE_THREADED_LINEAR}), synthesis-bearing or not. Each strategy reads ONLY its own carrier (`_is_resume = <own> is not None`); a topology change between pause and resume would otherwise have it read its absent carrier → run the WHOLE topology FRESH → re-dispatch effect-bearing branches/stages (an at-most-once violation). The general guard runs FIRST + SUBSUMES the v1.58 `_synthesis_resume_carrier_mismatch` (removed); the `_synthesis_resume_material_diff` synthesis identity check is RETAINED, running after against a carrier-consistent snapshot. **Built broader than the registered "fan-out" title** (preserved as the historical registration) per FULL-SPEC — handoff/EO/fence carry the IDENTICAL run-fresh bug, so a fan-out-only fix would be partial; advisor confirmed the uniform "populated carrier must match the resuming strategy" rule is BOTH simpler and more correct than a fan-out-only carve-out, and gated `effect_fence_resume` inclusion on verifying its capture site is linear-only (verified: the only fence-pause capture is `workflow_driver.py:3006` inside the linear step loop). The crash-resume path (`resume_snapshot None`) reconstructs each strategy's own carrier keyed to the executing topology → no foreign carrier → guard scoped to the explicit pause-resume entry. By-execution: the full carrier×strategy predicate matrix + every foreign carrier resumed under PARALLELIZATION → FAILED+zero-dispatch + a peer carrier resumed under every other strategy → FAILED+zero-dispatch + the matching-carrier positive controls in the sibling pause-test files unaffected. CP spec v1.58→v1.59. advisor (full-transcript — uniform-rule + subsumption-safe + effect-fence capture-site verification + record-the-superset/don't-fragment) + the §26.2/§25.15.x carrier-invariant probe (impl-to-cleared-spec, no council) + out-of-family Codex (carrier/topology mismatch is exactly what Codex flagged 3× at #728). `[[full-chain-witness-not-half-proofs]]` · `[[spec-prose-plan-body-drift-pattern]]` · `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`.

- **B-FANOUT-CRASH-RESUME-STRICT-TIER-INCOMPLETE — reserve-before-DISPATCH at-most-once for fan-out branches (CLOSED 2026-06-24; bundled-absorption CP v1.59→v1.60 + runtime v1.77→v1.78; CP+runtime).** Lifts the v1.57 strict-tier `fan-out-crash-resume-cascade-policy-incomplete-recovery` fail-closed for the COMMON (provably-not-run) incomplete-crash case (the cascade-policy arc lifted only the COMPLETE-recovery case). MECHANISM: a durable per-(run, branch) reserve-before-DISPATCH marker (C-RT-32 `record_branch_dispatched` / `present_dispatched_indexes`), written SYNCHRONOUSLY + fsynced STRICTLY BEFORE each strict-tier branch dispatches — atomic with the `ensure_future` dispatch scheduling (no yield between → no false-positive marker; a cascade-cancel can't shift the disposition), so within an instrumented run **marker-absent ⟺ effect-not-fired**. The fan-out analogue of the §14.22 C-RT-31 effect-fence `try_reserve`. CLASSIFICATION (index-scheme-agnostic — the set difference `present_dispatched_indexes − recovered`, NOT `range(expected)`): empty + the per-run dispatch-instrumented stamp ⟹ every absent branch PROVABLY-not-run ⟹ recover captured + re-dispatch ONLY the not-run ones (the existing pause-resume recovery path, now admitted for the strict tiers); non-empty ⟹ a MAYBE-RAN branch (a marker with no terminal capture — the fire→capture window) ⟹ fail closed. CROSS-VERSION GUARD (advisor BLOCKING catch): a PRE-arc crash journal has NO markers for ANY branch including a maybe-ran one, so a per-run `record_dispatch_instrumented` stamp (written at fan-out start by the new code only) gates the classification — an un-stamped journal retains the conservative fail-closed (`[[durable-recovery-presence-validity-scope]]`). PROCEED unchanged (writes/consumes no marker). By-execution: provably-not-run incomplete recovers + re-dispatches ONLY the not-run branch under CASCADE_CANCEL + PAUSE + ORCHESTRATOR_WORKERS (the no-double-fire witness `resume.dispatched == [not-run branch]`); cardinality-only no-dispatch recovers fresh; the cross-version un-stamped journal fails closed; mixed not-run+maybe-ran fails closed; the real-store marker/stamp round-trip across restart. advisor (the cross-version double-fire BLOCKING catch → the stamp; the synchronous-marker atomicity catch when a test surfaced a cascade-cancel timing regression — the `await asyncio.to_thread` marker introduced a new cancellation window, fixed by writing synchronously; the marker SEPARATES provably-not-run from maybe-ran, it does NOT eliminate ambiguity) + the §25.15 at-most-once probe (additive substrate, no council) + out-of-family Codex. `[[durable-recovery-presence-validity-scope]]` · `[[verification-shape-sharpened-grep-vs-e2e]]` · `[[full-chain-witness-not-half-proofs]]`.

- **B-FANOUT-CRASH-RESUME-ORCHESTRATOR-DISPATCH — reserve-before-DISPATCH extended to the orchestrator's own `steps[0]` sequential dispatch (CLOSED 2026-06-24; bundled-absorption CP v1.60→v1.61 + runtime v1.78→v1.79; CP+runtime).** Closes the ORCHESTRATOR_WORKERS `steps[0]` orchestrator's OWN dispatch-before-capture double-fire window (v1.60 STRICT-TIER-INCOMPLETE covered WORKER/PEER branches only — the orchestrator is a sequential dispatch, not a fan-out branch, so the per-(run, branch) markers did not cover it). The orchestrator runs FIRST, sequentially; a crash in its fire→capture window (after `dispatch()` fires, before `record_orchestrator`) left NO orchestrator record + no cardinality → `_determine_fanout_resume` returned None → a fresh re-run re-dispatched `steps[0]` → a potential double-fire on the strict tiers (this window had ALWAYS returned None, even post-#732). MECHANISM: a durable per-run orchestrator dispatch marker (C-RT-32 `record_orchestrator_dispatched` / `orchestrator_dispatched`, a SINGLE marker — there is exactly one orchestrator), written SYNCHRONOUSLY + fsynced STRICTLY BEFORE the orchestrator body dispatches; the orchestrator dispatch is itself SYNCHRONOUS (no `ensure_future` / yield), so **unlike the worker path no atomicity dance is needed** (advisor simplification — the synchronous marker→dispatch sequence cannot interleave). Within an instrumented run **marker-absent ⟺ the orchestrator's effect did NOT fire**. CLASSIFICATION (the `_determine_fanout_resume` orchestrator-None + no-branches case) keys on the marker's PRESENCE ALONE (presence-only, like `present_dispatched_indexes`): marker PRESENT ⟹ MAYBE-RAN ⟹ fail closed (NEW CP fail-class `fan-out-crash-resume-orchestrator-maybe-ran`); marker ABSENT ⟹ provably-not-run / pre-arc ⟹ the unchanged fresh re-run. The cross-version guard is INHERENT in the new-file marker (a pre-arc journal carries none) — no stamp gate needed; an orphaned marker WITHOUT the stamp ALSO fails closed (**out-of-family Codex [P2] catch** — the stamp is written before the marker, so a marker-without-stamp is an inconsistent store, never a pre-arc journal; an earlier stamp-gated draft would have let it fall through to a fresh re-dispatch double-fire; advisor had endorsed the happy paths — the decorrelation caught the orphaned-marker edge). The PARALLELIZATION changed-topology guard is WIDENED to the orchestrator marker (a maybe-ran orchestrator leaves no record, only the marker — a peer fan-out never writes it, so it only fires on a genuine changed-topology resume). PROCEED unchanged (writes/consumes no orchestrator marker). By-execution: orchestrator maybe-ran fails closed under CASCADE_CANCEL + PAUSE + HIERARCHICAL_DELEGATION (the nesting witness); provably-not-run [no marker] recovers fresh re-firing the orchestrator once; an orphaned marker WITHOUT the stamp FAILS CLOSED (the inconsistent-store case, never fresh); a dispatcher LOOKUP failure writes NO marker (Codex [P2] round-3 — the marker is written AFTER the pure lookup, before the dispatch, so a lookup failure cannot leave a false marker poisoning later recovery); PARALLELIZATION orchestrator-marker topology-mismatch fails closed; PROCEED writes no marker; the real-store orchestrator-marker round-trip + distinct-from-capture across restart; the isolation maybe-ran-raises / orphaned-marker-fails-closed / provably-not-run-→fresh unit tests. advisor (the synchronous-dispatch-needs-no-atomicity-dance simplification; REGISTER the maybe-ran residual + FOLD the PROCEED-orchestrator window into it with the #724-effect-free-synthesis-doesn't-transfer note; the `_append_path`-fsync confirmation; the HIERARCHICAL nesting witness) + the §25.15 at-most-once probe (additive substrate, no council) + out-of-family Codex. `[[durable-recovery-presence-validity-scope]]` · `[[full-chain-witness-not-half-proofs]]`.

- **B-FANOUT-CRASH-RESUME-MAYBE-RAN-RESOLUTION — re-fire-safe-kind resolution of the maybe-ran fan-out branch fire→capture window (CLOSED 2026-06-24; bundled-absorption CP v1.61→v1.62 + runtime v1.79→v1.80; CP+runtime).** Resolves a maybe-ran WORKER/PEER branch (a v1.60 dispatch marker present, but NO terminal capture — the fire→capture window, `present_dispatched_indexes − recovered`) for the RE-FIRE-SAFE step kinds, instead of the #732 blanket fail-closed (the conservative "branches are effect-BEARING … resolution is a follow-on" — distinguish-later, deferred to here). **The grounding correction (`[[grounding-reveals-claude-closeable-slice-close-honestly]]` + `[[cleared-spec-resolves-it-before-first-principles-fix]]`):** the arc was registered anticipating "a post-dispatch capture distinguishing completed-and-captured → suppress-and-continue from genuinely-ambiguous → a §26.2 pause." HEAD grounding refined it: the post-dispatch capture ALREADY exists (`_capture_branch_terminal` writes the terminal record the moment the branch output is in hand → a completed-and-captured branch is in `recovered`, never maybe-ran), so maybe-ran ⟺ genuinely fire→capture-ambiguous. The actionable distinction is not capture-vs-no-capture (already made) but **whether re-dispatching the ambiguous branch can double-fire a non-idempotent EXTERNAL effect**, determined by its STEP KIND. RE-FIRE-SAFE = `{DECLARATIVE_STEP, INFERENCE_STEP}` (`_FANOUT_MAYBE_RAN_REFIRE_SAFE_KINDS`), anchored to the cleared `step_blast_radius.py:13-22 _READ_ONLY_KINDS` ("INFERENCE_STEP / DECLARATIVE_STEP — no external effect"): an LLM inference has no external side-effect (cost + non-determinism only, which the UNFENCED linear resume already re-dispatches per §14.8.8.7 invariant 3 re-ask-per-retry), a declarative step is a pure transform — re-firing either is a fresh at-most-once-EXTERNAL execution, never a double-fire. The kind is read from the DISPATCH MARKER (runtime C-RT-32 `dispatched_branch_kinds`, the dispatch-time kind), NOT the resumed manifest — the at-most-once changed-manifest guard (**out-of-family Codex [P1]**, the decorrelated catch advisor's happy-path trace missed `[[hooks-codex-pilots-decorrelation-validated]]`): a branch dispatched as effect-bearing TOOL_STEP that crashed before capture, re-supplied at the same ordinal as a re-fire-safe DECLARATIVE_STEP on a same-cardinality resume, would otherwise be wrongly re-dispatched + double-fire the original tool effect; recording the kind in the marker (paired runtime delta v1.80) makes the classifier immune to manifest changes (an un-kinded pre-arc/torn marker fails closed). MECHANISM: a shared `_refire_unsafe_branch_indices` helper (one source of truth) keyed on the marker-kind map partitions the maybe-ran set by dispatch-time kind at BOTH the incomplete-recovery + cardinality-only classification sites (tier-agnostic, PAUSE + CASCADE_CANCEL): every maybe-ran branch re-fire-safe (∧ instrumented) → fall through to re-dispatch the absent branches (provably-not-run + re-fire-safe maybe-ran); ANY effect-bearing-kind maybe-ran (or out-of-bounds ordinal, or pre-arc un-stamped journal) → the v1.60 `fan-out-crash-resume-cascade-policy-incomplete-recovery` fail-closed (refined predicate). **Re-fire-safety is a STRICT SUBSET of READ_ONLY blast radius** — HITL_STEP (READ_ONLY-blast but operator-facing) + POST_JOIN_SYNTHESIS (effect-free but never a fan-out branch) + SUB_AGENT_DISPATCH (READ_ONLY at the parent gate but child tools fenced → inner-fence-ambiguity) are EXCLUDED (re-dispatch-double-fire-safety ≠ parent-gate blast radius). The EFFECT-BEARING residual stays fail-closed → registered `B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING`. NO contract / enum / committed-invariant change; at-most-once PRESERVED (a re-fire-safe kind has no external effect to double-fire; `[[disposition-label-is-a-claim-verify-against-spec]]`: the v1.60 maybe-ran fail-closed was the conservative IMPL, not a committed invariant). By-execution: a maybe-ran DECLARATIVE/re-fire-safe branch recovers re-dispatching only the maybe-ran branch under CASCADE_CANCEL + PAUSE; a maybe-ran TOOL_STEP/effect-bearing branch fails closed; a mixed re-fire-safe + effect-bearing maybe-ran set fails closed; the cardinality-only re-fire-safe set recovers fresh; the cardinality-only effect-bearing set fails closed. **Decorrelated:** advisor full-transcript ×3 (the thin-composition probe → the partial-fence finding [only TOOL_STEP is fenced] → the residual-sizing reconcile [unfenced INFERENCE = re-fire-safe, not irreducible]) + the per-dispatcher effect-surface trace (every registry kind, anchored to step_blast_radius READ_ONLY) + the §25.15 at-most-once probe (no council) + out-of-family Codex (diff). `[[verification-shape-sharpened-grep-vs-e2e]]` · `[[full-chain-witness-not-half-proofs]]`.

- **B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-RESOLUTION — re-fire-safe relaxation of the maybe-ran orchestrator fail-closed (CLOSED 2026-06-25 as the re-fire-safe slice; bundled-absorption CP v1.63→v1.64 + runtime v1.80→v1.81; CP+runtime).** The orchestrator analogue of the #736 worker re-fire-safety split. B-FANOUT-CRASH-RESUME-ORCHESTRATOR-DISPATCH (#734) separated provably-not-run (fresh) from maybe-ran (a dispatch marker, no terminal capture) but failed the maybe-ran case CLOSED unconditionally. This arc relaxes it for the **re-fire-safe** case: the orchestrator marker now records the orchestrator's DISPATCH-TIME kind (runtime C-RT-32 `record_orchestrator_dispatched` gains `step_kind` + the new `orchestrator_dispatched_kind` reader — the single-orchestrator analogue of the v1.80 worker `dispatched_branch_kinds`), and `_determine_fanout_resume` classifies the maybe-ran orchestrator by it: a re-fire-safe kind (`{DECLARATIVE_STEP, INFERENCE_STEP}` — the common ORCHESTRATOR_WORKERS shape where `steps[0]` is an LLM that farms sub-tasks) returns `None` → fresh re-run (sound: maybe-ran fires only with no worker recovered + no cardinality marker yet, which is fsynced AFTER the orchestrator capture); an effect-bearing or un-kinded kind (incl. a pre-v1.64 marker with no `step_kind` → `None`) STAYS fail-closed. At-most-once PRESERVED (re-dispatch only a provably no-external-effect orchestrator, keyed on the marker not the resumed manifest — the changed-manifest guard). **Grounding the arc at open** (`[[grounding-reveals-claude-closeable-slice-close-honestly]]` + `[[cleared-spec-resolves-it-before-first-principles-fix]]`) refined the registered "route to a §26.2 pause" anticipation into "per-kind re-dispatch, not a new pause" — EXACTLY as the v1.62 worker arc refined its identical registration language (a completed-and-captured orchestrator is already recovered via `read_orchestrator_output`, never maybe-ran ⟹ maybe-ran ⟺ genuinely fire→capture-ambiguous ⟹ the actionable distinction is the kind). **Decorrelated:** advisor full-transcript ×2 (the design proposal → the decomposition reconcile, where advisor REVERSED its own "build the pause-route" steer once grounded against the v1.62 worker precedent + the read confirming `_execute_orchestrator_workers` cannot reach the linear effect-fence resume site) + the orchestrator-kind-reachability grep (confirms `steps[0]` kind is UNCONSTRAINED → the effect-bearing follow-on is reachable, not vacuous) + out-of-family Codex. By-execution: re-fire-safe orchestrator maybe-ran RECOVERS fresh under CASCADE_CANCEL + PAUSE + HIERARCHICAL re-firing the orchestrator once; effect-bearing TOOL_STEP orchestrator maybe-ran fails closed; changed-manifest (TOOL-marker-vs-DECLARATIVE-manifest) fails closed; pre-v1.64 un-kinded marker fails closed; the real-store `orchestrator_dispatched_kind` round-trip. **Closes as the re-fire-safe slice + SPLITS off two narrower follow-ons** (a split per `[[spec-leg-split-on-ratification-boundary]]`, NOT a defer) → `standalone_registered` 3 → 4. `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[durable-recovery-presence-validity-scope]]`.

- **B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING — the WORKER effect-bearing maybe-ran residual, CLOSED 2026-06-25 as the TOOL_STEP slice (bundled-absorption CP v1.64→v1.65; CP-ONLY, NO runtime delta — the #738 shape).** Two halves: **(1) the live effect-fence ambiguous-pause composed through the fan-out barrier** — a fan-out branch (PARALLELIZATION peer OR ORCHESTRATOR_WORKERS / HIERARCHICAL worker) whose OWN dispatch raises `EffectFenceAmbiguousUncommittedError` (C-RT-31 §14.22) NO LONGER gets swallowed by the generic `except` → recorded `completed` → cascade; it is captured as a NON-terminal effect-fence-paused disposition (a NEW `EffectFencePausedBranchResumeState` carrier on `FanOutResumeState`/`PeerFanOutResumeState`, disjoint from terminal `branches` + `paused_child_branches`, default-empty + dropped-from-hash-when-empty) and composes through the worker barrier to a genuine §26.2 `EFFECT_FENCE_AMBIGUOUS` PAUSE; resume re-enters ONLY the fence-paused branch with the operator's key-bound `EffectFenceResolution` (the LINEAR v1.51/v1.52 + runtime v1.72/v1.73 machinery reused VERBATIM → CP-only, no runtime delta). **(2) the fence-recoverable TOOL_STEP maybe-ran crash-resume relaxation** — a maybe-ran TOOL_STEP now RECOVERS (re-dispatch re-reaches the AUTO-ACTIVE fence — `_FANOUT_REPLAY_ENGINE_CLASSES ⊂ _DURABLE_AUTO_FENCE_ENGINE_CLASSES`, grep-verified → at-most-once at the tool sink) instead of the v1.62 unconditional effect-bearing fail-closed, **PROVIDED the RESUMED branch's `step_kind` is ALSO TOOL_STEP** (the changed-kind guard). **The genuine in-flight-WIP regression caught + fixed (advisor full-transcript on the WIP review):** keying fence-recovery on the dispatch-MARKER kind ALONE re-opened the exact v1.62 changed-manifest hole — a maybe-ran TOOL_STEP re-supplied at the same ordinal as a DECLARATIVE on resume re-dispatched a step reaching NO fence → the original tool effect's ambiguity silently abandoned, recovered to SUCCESS (the existing `test_crash_resume_maybe_ran_changed_manifest_kind_fails_closed` was RED for this); the fix requires BOTH the marker kind AND the RESUMED `step_kind` be TOOL_STEP (the `_resumed_branch_kinds_by_ordinal` + `_fence_unrecoverable_maybe_ran_indices` resumed-kind conjunct, at the incomplete-recovery + cardinality-only sites). **Opaque-payload PIVOT:** the §14.22 fence key is per-`(run, step, tool)` (includes `tool_id`), but `WorkflowStep.step_payload` is OPAQUE to the driver → recording `tool_id` CP-side (Option A) would break axis isolation → the fix uses the CP-visible typed `step_kind` (Option B); **tool-swap-under-same-step_id is ACCEPTED PARITY** with the cleared LINEAR effect-fence path (per-key at-most-once preserved — each tool key fires ≤once), DOCUMENTED not registered. **GROUNDING split the effect-bearing kinds by FENCE surface:** TOOL_STEP hits the fence directly (this slice); SUB_AGENT_DISPATCH is fenced only at its child's tool sinks (→ registered); MANAGED_AGENTS is unfenced (→ registered); the one-resolution-type-per-pause limitation surfaced (→ registered). **Real-fence witnesses (the front-loaded load-bearing proof, advisor-directed):** BOTH live-composition sites (`_execute_parallelization` + `_execute_orchestrator_workers`), BOTH halves (PAUSE + key-bound resume) — a name-matched fence error through the REAL TaskGroup+shield PAUSES with `effect_fence_paused_branches` (NOT ExceptionGroup-swallowed) + resume re-enters only the paused branch. **Decorrelated:** advisor full-transcript (the WIP review — confirmed the changed-manifest failure is a CODE bug not a stale test; sharpened the tool-swap key-composition question; directed front-loading the witness) + the fence-reachability grep + the §14.22 idempotency-key read + out-of-family Codex (diff). harness-cp 1300+1xfail; runtime 2115; cxa 28; pyright 0/0/0. **Closes as the TOOL_STEP slice + registers THREE narrower follow-ons** (a split per `[[spec-leg-split-on-ratification-boundary]]`, NOT a defer) → `standalone_registered` 4 → 6. `[[full-chain-witness-not-half-proofs]]` · `[[verification-shape-sharpened-grep-vs-e2e]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[hooks-codex-pilots-decorrelation-validated]]`.

- **Forward register (6 registered FULL-SPEC follow-ons keeping R-FS-1 ACTIVE; registered in `arc-ledger.yaml` per `[[spine-ledger-forward-arc-registration]]`).** *(B-FANOUT-CRASH-RESUME-TIMEOUT-REPLAY ✅ BUILT WHOLE #738; B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-RESOLUTION ✅ BUILT as the re-fire-safe slice #740 — closes 1 + registers 2 → 3 → 4; B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING ✅ BUILT as the TOOL_STEP slice 2026-06-25 — closes 1 + registers 3 → 4 → 6, a net increase, honest FULL-SPEC accounting; B-FANOUT-EFFECT-FENCE-PER-BRANCH-RESOLUTION ✅ BUILT as the per-branch-DISTINCT resolution map 2026-06-25 — closes 1 + registers 1 [the ABORT-scoping residual] → net-zero 6.)* (1) **B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT** (NEW, split from MAYBE-RAN-EFFECT-BEARING) — resolve a maybe-ran WORKER of kind SUB_AGENT_DISPATCH: unlike TOOL_STEP its OWN dispatch does not hit the fence directly — its effects are its CHILD sub-workflow's tool calls (fenced at the child's tool sinks) → recursive child crash-resume (compose the child's own fence/pause state through the parent barrier), a larger mechanism. (2) **B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL** (NEW, split) — resolve a maybe-ran WORKER of kind MANAGED_AGENTS: the UNFENCED vendor-session external effect (create_session + send_event) needs the C-RT-31 fence EXTENDED to the managed-agents sink (a runtime-side change), then barrier-composition. (3) **B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT** (NEW 2026-06-25, split from PER-BRANCH-RESOLUTION ✅ BUILT) — per-branch-DISTINCT resolution landed for SKIP/RE_FIRE, but ABORT (single field OR map entry) stays run-level-terminal (v1.65 §1(b)); per-branch-SCOPED abort (fail just ONE fence-paused branch, let the siblings the operator CAN vouch for resolve + fold survivors per cascade_policy) is a NEW disposition semantic beyond shipped run-terminal ABORT → a new `EffectFenceResolution` value (e.g. ABORT_BRANCH) or a per-branch abort-scope flag + the cascade reaction (operator-gated decided-at-open — it may sacrifice the committed ABORT-is-run-terminal semantic). (4) **B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT** — reconstruct a lost PAUSE snapshot HONORING §25.15.1 "finish in-flight, then pause". (5) **B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING** (split from ORCHESTRATOR-MAYBE-RAN-RESOLUTION) — resolve a maybe-ran orchestrator of an EFFECT-BEARING kind (`steps[0]` kind UNCONSTRAINED): route to a §26.2 pause for operator resolution, needing a NEW resume→orchestrator application site the linear effect-fence resume cannot reach. (6) **B-FANOUT-CRASH-RESUME-ORCHESTRATOR-PROCEED-RESIDUAL** (split, **operator-gated**) — close the PROCEED-orchestrator pre-capture double-fire window; changes PROCEED's COMMITTED tier semantic (the v1.56 PR2-round-4 precedent) → a nameable **C10⊥C11** tension → a dyadic council + operator AUQ at open. On all six closed/resolved → `closure_gate.py` G1.1 passes → R-FS-1 resolves → R-CL-Q1 quality track admits.

- **B-FANOUT-PAUSE — resumable fan-out `pause` cascade (surfaced by U-CP-88 / arc #14; two decorrelated reviewers [P1]; UNTRACKED → forward arc).** Cleared CP spec §25.15.1 maps `cascade_policy=pause` → `RunStatus.PAUSED` "composing with C-CP-26 PauseResumeProtocol + C-RT-30 `api.resume`". For the LINEAR path this works (capture a position-only `PauseSnapshot`, resume from its `step_index`). For a **fan-out** it is not yet materializable: (1) resume reconstruction is **ledger-based** (§25.15.2 obl. 7 — read each branch's persisted `terminal_status`, skip the terminal branches, re-dispatch the rest), but the **resume-re-entry path does not exist** (the non-linear strategies deliberately bypass the §25.3 linear-only prefix-replay — the strategy is resume-blind by design); (2) completed-branch **outputs** are not persisted for a resume merge (the ledger carries causality + terminal_status, not the dispatch OUTPUT). Returning a `PAUSED` without (1)+(2) advertises a resumability the harness cannot honor (`api.resume` would get `pause_snapshot=None`) → a false-resumable `PAUSED`, the silent-degradation mode. **Interim (landed at arc #14):** the U-CP-88 `pause` branch fails HONESTLY → `RunStatus.FAILED` + `orchestrator-workers-pause-resume-not-yet-materialized` fail_class (NOT a false-PAUSED); audit record + salvaged outputs persist. The interim **deviation** is recorded at `.harness/class_3_fanout_pause_resume_not_yet_materialized.md` (Class 3, log-and-proceed). **The build:** (a) the fan-out resume-re-entry (obl. 7 ledger-skip); (b) completed-branch-output persistence (candidate: `PauseSnapshot.state_summary`); (c) capture a `PauseSnapshot` + return `PAUSED`; (d) the final §25.15.1 shape decision (resumable-PAUSED vs a distinct fan-out disposition). **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3).** Load-bearing: MEDIUM (TEAM_BINDING-default reachability on worker failure; the honest-FAILED interim is non-broken). Rec: **BUILD** (CP-strategy + runtime resume arc; design-fork-first). On landing, the §25.15.1 deviation closes and the `pause` branch flips FAILED → PAUSED. **✅ BUILT for `ORCHESTRATOR_WORKERS` (this arc, 2026-06-19, the ELEVENTH standalone `B-*` arc) — bundled-absorption (CP spec v1.41 → v1.42 C-CP-26 §26.2 `FanOutResumeState`/`FanOutBranchResumeState` + `PauseSnapshot.fan_out_resume` + §25.15.1 materialization note + `harness-cp/src` + `harness-runtime` e2e + by-execution tests + fork doc `class_2_fork_b_fanout_pause_resumable_fan_out.md` + clearance `Spec_Control_Plane-v1_42-cleared-2026-06-19.md`), NO operator gate.** The genuine design-fork-first surface (advisor) was piece (b) completed-branch OUTPUT recovery — it breaks R-CC-1 design §1.1's position-only model; reading §1.1 at source resolved it to a **self-documented §6 re-open trigger** ("a future execution model … would need a state-restoration story + a durable store carrying more than the position-only PauseSnapshot"), NOT an explicit forbidding invariant (contrast B4-Slice-4 §14.5.3 inv 2 → gated) → adopt-and-note + clearance under FULL-SPEC, no gate. **Mechanism:** a NEW `FanOutResumeState` (orchestrator output + the terminal branches' dispositions+outputs + `worker_count`) in `PauseSnapshot.fan_out_resume` (additive default-`None`), **COVERED by `snapshot_hash` when present** (the resumed aggregate trusts the recovered outputs → a tamper fails `attempt_resume` → `CP-FAIL-PAUSE-SNAPSHOT-CORRUPTION`; key added to the canonical hash dict ONLY when non-`None` → existing snapshots byte-identical). On a worker failure under `pause` with a bound `pause_resume_protocol`: capture the snapshot, return `RunStatus.PAUSED`; without the protocol → FAILED + `orchestrator-workers-pause-resume-protocol-not-bound` (honest detect-then-refuse, no false-PAUSED). Resume threads `execute_workflow(pause_snapshot_input=) → _execute_workflow_body(resume_snapshot=) → _execute_orchestrator_workers(resume_snapshot=)`: recover the orchestrator + terminal outputs, re-dispatch only the absent ordinals (obl. 7), compose the fused aggregate; a re-dispatched worker failing again re-pauses with the unioned terminal set. **Pause semantic:** in-flight finish → terminal; not-yet-dispatched LEFT re-dispatchable (the cascade-cancel obl-4 `cancelled` scan deliberately NOT run on `pause`); a failed worker → `completed` terminal (dispatch-boundary) → not re-dispatched. **Non-vacuity is the deliverable (advisor):** the real-entry-point resume witness through `execute_workflow(pause_snapshot_input=)` (the path `api.resume` drives — NOT a direct `_execute_orchestrator_workers` proxy: terminal-skip + re-dispatch + fused aggregate) + the gold-standard full-runtime `api.resume` e2e (`test_b_fanout_pause_resume_e2e.py`: capture → JSON round-trip → `api.resume` → SUCCESS, only worker-1 re-dispatched) + a hash-tamper negative control + an empty-branches negative control (persistence load-bearing) + the `worker_count` material-diff guard + the flipped interim test. **Scope = `ORCHESTRATOR_WORKERS` only** (the Class-3 deviation is filed only for it). **Four follow-ons registered (grounded by direct read — each carries its own `*-pause-resume-not-yet-materialized` FAILED branch, `[[subagent-landscape-reports-need-regrounding]]`):** `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` + `B-FANOUT-PAUSE-PARALLELIZATION` (sibling fan-out-barrier strategies) + `B-HANDOFF-PAUSE` (DECENTRALIZED_HANDOFF single-owner handoff-pause, a different shape) + `B-HIERARCHICAL-PAUSE` (HIERARCHICAL_DELEGATION resume crosses the child-bootstrap recursion boundary). Gates: pyright 0/0/0 · ruff · harness-cp 1081 passed + 1 xfailed · harness-runtime 1957 passed / 21 skipped / 1 xfailed. advisor (full-transcript — the §1.1-break identification + gate discriminator + hash-coverage + scope-grounding) + out-of-family Codex (pre-merge). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[full-chain-witness-not-half-proofs]]` · `[[stale-carry-text-disposition]]`.

  - **`B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / ~~`B-FANOUT-PAUSE-PARALLELIZATION`~~ (✅ BUILT 2026-06-21) / `B-HANDOFF-PAUSE` / ~~`B-HIERARCHICAL-PAUSE`~~ (✅ BUILT 2026-06-21 — dedicated entry below)** (registered forward arcs, from B-FANOUT-PAUSE 2026-06-19). PARALLELIZATION + HIERARCHICAL_DELEGATION LANDED (the 19th + 20th standalone `B-*` arcs). The TWO remaining (`B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` + `B-HANDOFF-PAUSE`) fail HONESTLY (`*-pause-resume-not-yet-materialized`). **MISLABEL CORRECTED (grounded at B-HIERARCHICAL-PAUSE open, advisor-confirmed):** the original "EO + PARALLELIZATION reuse the fan-out-barrier shape" was WRONG for EO — `EVALUATOR_OPTIMIZER` is a SEQUENTIAL generate→evaluate loop (`_execute_evaluator_optimizer`: "Sequential single-owner; NO fan-out, NO cascade_policy, NO branch_metadata"), so the §25.15.1 cascade `pause` trigger (a branch failure halting a fan-out) DOES NOT APPLY. EO + DECENTRALIZED_HANDOFF are BOTH **sequential single-owner** (an iteration cursor / a stage cursor) — a distinct, non-fan-out resume shape requiring a sequential-loop pause trigger (fork-first; pair them). The fan-out-barrier shape (the `FanOutResumeState` carrier + skip-terminal/recover-output) materialized for ORCHESTRATOR_WORKERS (#661), PARALLELIZATION (#679), and HIERARCHICAL_DELEGATION (#680 — which IS ORCHESTRATOR_WORKERS at each level + the cross-recursion-boundary child pause). Rec: **BUILD** each (design-fork-first per X-AL-3 where the resume shape is new). On each landing, the `class_3_fanout_pause_resume_not_yet_materialized.md` deviation closes for that strategy.

- **B-ENGINE-OUTPUT-REPLAY — output-carrying event-history substrate + cached-output replay (surfaced by U-CP-93 / arc E-impl-1; advisor [blocking, pre-overload]; UNTRACKED → forward arc).** Cleared CP spec C-CP-08 §8.1 `engine_replay` mandates *"activity outputs cached and replayed; no re-execution of activities."* At HEAD only the **"no re-execution"** half is materializable (the driver skips the contiguous materialized prefix — `_determine_event_replay_resume_at` → resume_at via the F2 `idempotency_key` join, the prefix is not re-dispatched). The **"outputs cached and replayed"** half is degenerate: (1) **no output-carrying substrate** — the F2 `EntryPayload` (C-IS-07 §7.1) carries no activity output, only a `response_hash` digest (the CP plan v2.34 U-CP-93 Note's "cached outputs being the entry payloads" is empirically false — Finding 1 at `.harness/r-fs-1-e-impl-1-finding.md` §1); (2) **no consumer** — the driver threads no inter-step data flow (the registered **B-INTERSTEP** arc; `accumulated` is never read downstream). So EVENT_SOURCED_REPLAY (U-CP-93) — and WAL_SEGMENT's `segment_replay` at E-impl-2 — is currently **behaviorally identical to `SAVE_POINT_CHECKPOINT` skip-prefix** (the same accepted bar the landed save-point class has; NOT a new degeneracy). **Interim (landed at E-impl-1):** EVENT_SOURCED_REPLAY ships as honest **resumption-routing** (in-scope + dispatch branch + bare RESUMPTION + no-re-fire), with the degeneracy surfaced (finding doc) and the `_determine_event_replay_resume_at` helper as the named extension seam — NOT a false-distinct vacuous class. **The build:** (a) a hand-rolled **output-carrying event-history substrate** (per I-6, no vendored Temporal/DBOS/Restate — a dedicated append-only output log, or an IS `EntryPayload` output-carrier field via design-fork) storing per-step activity outputs; (b) compose with **B-INTERSTEP** so post-resume steps deterministically observe replayed prior outputs; (c) the genuine replay-into-`accumulated` at the `_determine_event_replay_resume_at` seam. **Owner = a follow-on R-FS-1 child arc within / after the E sub-program (design-fork-first per X-AL-3, since the output-carrier substrate is a new surface).** Load-bearing: MEDIUM-HIGH — it is what gives `event-sourced-replay` (+ `segment_replay`) their distinguishing §8.1 semantic over save-point; composes with B-INTERSTEP + B1. Rec: **BUILD** (CP + runtime/IS substrate arc; design-fork-first). On landing, the §8.1 cached-output-replay clause closes for the durable engine classes. **✅ BUILT for LINEAR EVENT_SOURCED_REPLAY (this arc, 2026-06-19, the TWELFTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.62 → v1.63 NEW §14.23 C-RT-32 `EngineOutputStore` + clearance marker + fork doc `class_2_fork_b_engine_output_replay_output_carrying_substrate.md` + `harness-runtime/src` store+bootstrap + `harness-cp/src` producer+rehydrate + by-execution tests), NO operator gate.** Substrate-shape fork **probe-resolved to a dedicated runtime store** (advisor not council): the IS F2 `EntryPayload` stores a `response_hash` digest BY DESIGN; extending it ripples the §5.2 hash + JSONL + IS contract → foreclosed by I-6 + ADR-F2 + the `JournalWorkflowPauseStore` precedent. Mechanism: a hand-rolled crash-safe `EngineOutputStore` (per-run JSONL keyed by `run_idempotency_key`, fsync + torn-append self-heal, co-located under `<state_ledger_dir>/engine-output`), opt-in `RuntimeConfig.engine_output_replay`; the CP driver records each step output BEFORE the F2 ledger-append (**RESERVE-before-COMMIT** — the store always holds ≥ the ledger's materialized prefix, the advisor-caught store↔ledger SKEW rule verified at the live append site); on an EVENT_SOURCED_REPLAY resume the driver rehydrates the inter-step channel from the stored prefix **driven by `resume_at`** + **FAIL-CLOSED** on a missing output / stored-step_id≠body identity mismatch (the B-FANOUT-PAUSE symmetry). **§1.1 reuse, NO gate:** carries outputs across a resume (breaks the R-CC-1 §1.1 position-only model) but §1.1 is a self-documented §6 re-open trigger, not a forbidding invariant → adopt-and-note (the B-FANOUT-PAUSE precedent). **Non-vacuity is the deliverable:** the **full-chain witness** `test_b_engine_output_replay_rehydrated_output_reaches_real_provider_call` — a prior run's output durably stored ON DISK → the CP rehydrate into a fresh channel → the REAL `RuntimeLLMDispatcher` injects it → the recording provider RECEIVES the recovered output (NOT a channel-unit proxy) + the CP rehydrate witness (a channel-reading dispatcher proves the first re-dispatched step SEES the recovered predecessor) + a NEGATIVE CONTROL (no store → None upstream) + 2 fail-closed tests + 7 store crash-safety units. **Narrow surface** (the channel exposes only `most_recent_output()`); **capability-built, NOT "fires in production"** (observable only when `engine_output_replay` + `inter_step_data_flow` + EVENT_SOURCED_REPLAY compose). **LINEAR EVENT_SOURCED_REPLAY only;** registered forward arcs: `B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT` (WAL_SEGMENT shares the store, E-impl-2) + the non-linear resume-blind strategies (the B-FANOUT-PAUSE-family resumption arcs). Gates: pyright 0/0/0 · ruff · harness-cp 1087 passed + 1 xfailed · harness-runtime full green · cxa+core 64 · overlay 31/31 seams. advisor (full-transcript — substrate-fork probe-resolution + the store↔ledger skew rule + §1.1 reuse) + out-of-family Codex (pre-merge). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[full-chain-witness-not-half-proofs]]` · `[[probe-resolves-fork-prescribed-council]]`.
  - **`B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT` — ✅ BUILT 2026-06-21 (bundled-absorption; NO operator gate; PR #687).** Extends the §14.23 C-RT-32 `EngineOutputStore` cached-output replay from `EVENT_SOURCED_REPLAY` (the #665 LINEAR arc above) to `WAL_SEGMENT` (which SHARES the store substrate + the same F2-prefix `resume_at`). A WAL_SEGMENT run now RECORDS each step output (the producer gate, prev. `EVENT_SOURCED_REPLAY`-only, extended at the explicit `workflow_driver.py` "WAL_SEGMENT, which shares the store, extends this gate at its arc" marker), and a WAL_SEGMENT resume REHYDRATES the durably-stored prefix into the inter-step channel so the first re-dispatched segment-step reads its recovered predecessor's output (prev. left a fresh channel empty → None). **TWO call-site gate extensions** (the producer gate + the WAL_SEGMENT resume-block rehydrate, inserted between the RESUMPTION emit + the engine-pause recovery so RESUMPTION is not double-emitted); the shared helpers (`_record_durable_step_output`, `_rehydrate_inter_step_channel_on_replay`) + the store binding (`config.engine_output_replay`, engine-class-agnostic) + the WAL_SEGMENT `resume_at` (U-CP-95) all already existed. **Cross-spec check (advisor) FLIPPED the framing pure-impl → two-surface bundled-absorption:** C-CP-08 §8.1's `segment_replay` row did NOT carry the "activity outputs cached and replayed" clause the `event-sourced-replay` row does → shipping the impl WITHOUT a §8.1 amendment would be a silent X-AL-3 extension → **CP spec v1.46 → v1.47** (§8.1 `segment_replay` cached-output clause) + **runtime spec v1.65 → v1.66** (§14.23.5 producer-gate `{EVENT_SOURCED_REPLAY, WAL_SEGMENT}` + §14.23.7 follow-on LANDED) co-land with clearance markers. **record↔rehydrate ship together** (never record-only — a never-rehydrated journal is the exact defect the producer gate prevents; corruption fail-close free via the shared helper). **Full-chain witness** (`[[full-chain-witness-not-half-proofs]]`): `test_wal_segment_records_then_rehydrates_full_chain` — a real WAL_SEGMENT run records each output (phase 1) → resume rehydrates (phase 2) → step-2 reads the RECOVERED step-1 output, NOT None — + a negative control (no store → None) + a store↔ledger fail-close. pyright 0/0/0; harness-cp 1133 passed. advisor (clean-first pick + breadth-off-HITL + the record↔rehydrate-coupling correctness anchor + the cross-spec §8.1 catch that flipped the framing) + out-of-family Codex. The "non-linear resume-blind strategies" residual remains registered. `[[grounding-reveals-claude-closeable-slice-close-honestly]]`.

- **B-EFFECT-FENCE — at-most-once EXECUTION of non-idempotent step side-effects (effect-boundary fencing; surfaced by U-RT-123 / arc E-impl-3b; council + advisor; UNTRACKED → forward arc).** The U-RT-123 reconciler CAS (and every durable engine's resume) guarantees at-most-once **claim of a revision**, NOT at-most-once **execution** of the workflow steps the resume re-runs. A non-idempotent external effect (`git_push` / `send_email` / any side-effecting tool) fires *inside* the re-executed step, and only THEN can a stale/losing claimant abort — so a crash-then-retry or a cross-host concurrent resume can fire the same external effect twice. The F2 `idempotency_key` is on the `cp.resume-attempted` AUDIT entry + the per-step ledger dedup, **not** on the external effect at its sink (the effect is opaque to the harness ledger). **The build:** a hand-rolled (I-6 — no vendored Temporal/DBOS activity-dedup) **per-effect fencing discipline at every side-effect sink** — a fencing/dedup token (candidate: the run-scoped `(workflow, revision, step)` key) checked at the sink *before* the effect fires, so a re-executed step's effect is suppressed-or-idempotent rather than re-applied. Cross-cutting (NOT reconciler-specific — every durable engine class + every topology with external-effect steps); composes with the durable engines (E) + **F-CC**'s fenced bounded-synchrony lease (genuine multi-host AUTO recovery is reachable only once effects are sink-fenced) + **B-INTERSTEP**. **Interim (landed at E-impl-3b):** the reconciler fail-closes the crash-mid-resume window to HITL (§22.1) rather than risk a double-execution — honest, not a silent double-fire; the gap is the *exactly-once external effect* under a legitimate resume. **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3, since a sink-fencing surface is new).** Load-bearing: HIGH — it is the floor-(ii) "at-most-once execution" semantic the durable engine classes advertise; without it the durable engines are at-most-once-*claim* but at-least-once-*effect*. Rec: **BUILD** (runtime + AS tool-sink arc; design-fork-first). On landing, the floor-(ii) effect-idempotency clause closes for non-idempotent external effects. **✅ BUILT (this arc, 2026-06-18, the EIGHTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.59 → v1.60 NEW §14.22 C-RT-31 `RuntimeEffectFence` + `harness-runtime/src` + `harness-cp` test + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the U-RT-123 reconciler's crash-atomic `O_EXCL`/`os.link`/`fsync` `_claim_resume_revision` claim re-keyed to the per-(run, step, tool) `idempotency_key`, applied at the `RuntimeToolDispatcher` `call_tool` SINK: `try_reserve(idempotency_key)` BEFORE `call_tool` — the first dispatch wins (fires), any re-dispatch LOSES (a crash-then-resume re-run of an effected-but-uncommitted step — the `:2031` effect / `:2336` commit window `_determine_resume_at` re-dispatches — OR an in-process `RetryBreakerToolDispatcher` retry, which fail-fasts since the fence error is not a `_TRANSIENT_TOOL_DISPATCH_ERRORS` member) → `EffectFenceReservedUncommittedError` (fail-closed to §22.1, never a double-fire). **At-most-once, NOT exactly-once** (the fire-then-crash-before-commit window is ambiguous → conservative fail-close, mirroring the reconciler §22.1 posture); **COMMIT = the EXISTING per-step ledger entry** (one source of truth — the genuinely-new piece is only the RESERVE marker). Opt-in `RuntimeConfig.effect_fencing=False` default → byte-identical; the `StepDispatcher.dispatch` signature UNCHANGED (keys on the existing `step_context.parent_idempotency_key`). **The one C10⊥C11 fence-scope tension probe-resolved** by durable-resume scoping (no `ToolContract` field, no AS fork → advisor, not council). **Non-vacuity is the deliverable (advisor's #1 steer; cf. B-TOOL-GATE #653 wired-but-production-dead):** proven by-execution with NO proxy — a real `RuntimeToolDispatcher` over a counting MCP tool fires it EXACTLY ONCE across two same-key dispatches; a NEGATIVE CONTROL double-fires without the fence; a fresh-dispatcher-over-the-same-on-disk-fence-dir test is a genuine restart; the driver hands the sink a byte-identical key on the real `execute_workflow` resume path. **Three follow-ons registered (NOT silent-deferred):** `B-EFFECT-FENCE-DURABLE-AUTO` (auto-activate under durable engines) + `B-EFFECT-FENCE-PER-TOOL` (a `ToolContract` idempotency classification — cross-axis AS, design-fork-first) + `B-EFFECT-FENCE-HITL-ROUTE` (the §22.1 operator-resolvable pause + suppress-and-continue via `B-ENGINE-OUTPUT-REPLAY`, replacing the interim fail-closed FAILED raise). No §5.2-hash / IS / CP / OD contract / ADR change (the fence claim is a side file, not a state-ledger entry; I-6 + ADR-F2 honored). Fork: `.harness/class_1_fork_b_effect_fence_at_most_once_execution.md`; clearance `Spec_Harness_Runtime-v1_60-cleared-2026-06-18.md`. `[[r-cxa-seam-wiring-is-producer-discovery]]` · `[[durable-recovery-presence-validity-scope]]` · `[[test-bypass-as-runtime-truth-pattern]]`.
  - **`B-EFFECT-FENCE-DURABLE-AUTO` — ✅ BUILT 2026-06-21 (impl-to-cleared-spec; NO operator gate; PR #689).** The §14.22 C-RT-31 `RuntimeEffectFence` now AUTO-activates per-run for durable-execution engine classes — removing the operator `effect_fencing=True` opt-in for the common durable case (a crash-resume re-dispatches uncommitted steps → double-effect risk). **Mechanism = the §14.22.7-named PER-RUN RESERVE GATE** (advisor steered AWAY from the spec's other named option, factory-threading: the dispatcher is a daemon-reused singleton across runs with different engine_classes, so caching the engine class would leak cross-run — `[[run-scoped-ctx-holder-daemon-isolation]]`). The stage-5 factory now constructs the fence UNCONDITIONALLY (lazy claim dir → no footprint until a reserve fires — `RuntimeEffectFence.__init__` just stores the path; `mkdir` is in the claim path); the dispatcher carries a NEW `effect_fencing_explicit` flag (the opt-in) + gates the per-dispatch reserve on `effect_fencing_explicit OR step_context.run_engine_class ∈ durable-set`. **Channel = a NEW hash-inert `StepExecutionContext.run_engine_class` field** (the CP driver sets it from `manifest_entry.engine_class` at all 5 step-context composition sites — the `hitl_placements` producer precedent the advisor named). **Out-of-family Codex [P2] (the decorrelated catch that corrected the channel):** the first impl gated on the per-step `binding.engine_class` — but `resolve_step_binding` resolves `override.engine_class or manifest`, so a per-step `StepOverride.engine_class=PURE_PATTERN` on a DURABLE workflow would wrongly skip the fence for a step the RUN still resumes + re-dispatches (a double-fire window) → re-keyed onto `step_context.run_engine_class` (the RUN engine class that governs resume; my "cleaner binding shortcut" was the wrong economy — the advisor's original StepExecutionContext steer was right). `[[hooks-codex-pilots-decorrelation-validated]]`. **Durable-set (impl-discretion — §14.22.7 defines 'durable' = 'resume re-dispatches uncommitted steps' without enumerating):** `{SAVE_POINT_CHECKPOINT, EVENT_SOURCED_REPLAY, WAL_SEGMENT, RECONCILER_LOOP}`, EXCLUDING `PURE_PATTERN_NO_ENGINE` (the 'no-engine' baseline = the spec's 'non-durable run' carve-out; a STRICT improvement — no regression since pure-pattern was already opt-in, + it can still opt in explicitly). Witnesses (`test_lifecycle_runtime_tool_dispatcher.py`, +4 by-execution through the real dispatcher, same-key re-dispatch): durable RUN auto-fences without opt-in (2nd dispatch fail-closes) + non-durable RUN stays fence-free (both succeed) + explicit opt-in fences all + **a durable RUN with a per-step non-durable override STILL fences** (the Codex [P2] regression); plus the factory default-test updated (fence PRESENT + `effect_fencing_explicit is False`). harness-cp 1133 + harness-runtime non-e2e 2003 passed. bundled-absorption: runtime spec v1.66 → v1.67 (§14.22.7 LANDED + the durable-set reading) + clearance marker. pyright 0/0/0; harness-runtime non-e2e 2002 passed. advisor (clean-first pick + the per-run-gate-not-factory-threading steer + the meta 'stop bringing the pick to me — clean-first IS the selection algorithm') + out-of-family Codex. The `B-EFFECT-FENCE-PER-TOOL` / `B-EFFECT-FENCE-HITL-ROUTE` sibling follow-ons remain registered.
  - **`B-EFFECT-FENCE-PER-TOOL` — ✅ BUILT 2026-06-21 (impl-to-cleared-spec; cross-axis AS spec v1.11→v1.12 + runtime spec v1.69→v1.70 §14.22.7; NO operator gate; PR #<pending>).** A `ToolContract.idempotent` classification so the §14.22 C-RT-31 effect fence EXEMPTS declared-idempotent tools — only declared-**non-idempotent** tools are fenced (the §14.22 line-142 stated intent the v1.60 interim conservatively over-delivered by fencing every tool under an active fence). **BUILD-not-gate (the "design-fork-first" register label resolved by a direct §14.22 read — `[[disposition-label-is-a-claim-verify-against-spec]]`):** fence-all-when-active is the conservative IMPL of the at-most-once-for-non-idempotent intent, NOT a committed invariant; §14.22.7 explicitly NAMED this follow-on ("per-tool idempotency classification to fence only declared-non-idempotent tools"), exactly as it named `B-EFFECT-FENCE-DURABLE-AUTO` (the named-follow-on-is-BUILD precedent). **The advisor's tool-intrinsic-vs-invocation-dependent catch was load-bearing:** idempotency for a write/mutate tool is `(tool, args)`-dependent (which is WHY the fence keys per-invocation), so a per-tool flag is sound ONLY as a STRICT all-invocations property (`idempotent=true` ⟺ every invocation re-executes with no additional effect, for ALL args — pure read trivially / PUT-style yes / append-send-counter NO); default `false` = conservative fence-by-default (undeclared tools stay fenced; a mis-declaration is the author's responsibility, as `blast_radius_tier` is). **Mechanism = one conjunct at the existing fence gate:** the reserve fires on `(effect_fencing_explicit OR run_engine_class ∈ durable-set) AND NOT contract.idempotent` (the `contract` is already in scope at the dispatch site for the tier-floor check; dispatch signature UNCHANGED). An idempotent tool fires + is **safely retryable** (the v1.60 interim fail-closed an idempotent tool's transient retry — over-conservative). **Cross-axis threading mirrors the v1.11 `forces_*` precedent:** `ToolContract.idempotent` + `RawContractInput.idempotent` + the `validate_tool_contract_at_registration` converter (authored contracts) + the NEW per-server `MCPClientConfig.default_idempotent` stamped by the stage-3a `MCPToolContractConverter` (discovered tools). `idempotent` does NOT enter the C-AS-02 §2.3 `sandbox_tier_floor` (a fence-only concern). Witnesses (`test_lifecycle_runtime_tool_dispatcher.py` +2 by-execution): an idempotent tool under a durable run is NOT fenced (re-dispatch fires twice, no fence error) + the exemption holds under explicit opt-in too; default-False negative controls unchanged; AS converter threads `idempotent` (`test_tool_contract_idempotent_threads_through_converter`). harness-as 318 + harness-runtime non-e2e 2011 passed; pyright 0/0/0. Bundled-absorption: AS spec v1.12 + runtime spec v1.70 + 2 clearance markers + harness-as/src + harness-runtime/src + by-execution tests. advisor (BUILD-vs-gate discriminator + the tool-intrinsic strict-semantic catch) + out-of-family Codex. **`B-EFFECT-FENCE-HITL-ROUTE` was the sole remaining §14.22.7 follow-on — now BUILT (below); the §14.22.7 follow-on family is COMPLETE (DURABLE-AUTO + PER-TOOL + HITL-ROUTE all built).**
  - **`B-EFFECT-FENCE-HITL-ROUTE` — ✅ BUILT 2026-06-22 (operator-AUTHORIZED; runtime spec v1.71→v1.72 §14.22.8 + CP spec v1.50→v1.51 §26.2; PR #<pending>).** The §14.22 C-RT-31 effect fence's lost-reserve re-dispatch no longer uniformly fail-closes; it SPLITS two ways on a newly-captured output (the §14.22.7-named genuine HITL-routed posture, replacing the v1.60 interim FAILED raise). **OPERATOR-GATED + AUTHORIZED** (AskUserQuestion 2026-06-22 — the operator chose this build over the other 2 gated arcs [`B-INTERSTEP-NONLINEAR`, `B-HITL-PLACEMENT-PER-STEP-LOOSEN`] + the R-CL-Q1 quality track): relaxing the fence's load-bearing fail-closed into a two-case split + a new pause-reason value are committed-behavior gates the choice ratified. **The crux (advisor-sharpened, the probe debunked the deferral):** the v1.60 entry deferred suppress-and-continue to `B-ENGINE-OUTPUT-REPLAY`, but the 2026-06-22 probe found replay CANNOT supply it (only a `response_hash` digest + a `resume_at`-driven rehydrate that ignores the suppressed step's own uncommitted output) → this arc adds its OWN post-fire output capture at the fence (atomic `O_EXCL`/`os.link`, present ⟹ complete-and-valid). **Mechanism:** (1) `RuntimeEffectFence.capture_output`/`read_output` (same per-(run,step,tool) digest keying as the claim); the dispatcher captures the VALIDATED response as the last step before the success return (post-schema-validation, post-cost-attribution → "captured ⟹ valid success ∧ cost once"). (2) A lost reserve SPLITS on `read_output`: present → **suppress-and-continue** (return the captured output, NEVER re-fire) — THE deliverable; absent/corrupt → raise the NEW `EffectFenceAmbiguousUncommittedError` (renamed from `EffectFenceReservedUncommittedError`, whose sole raise site the split removes). (3) The driver name-matches the ambiguous error at the linear/TOOL_STEP boundary (harness-cp cannot import harness-runtime — the `HITLPauseRequestedSignal` precedent) → a **§26.2 `WorkflowPauseReason.EFFECT_FENCE_AMBIGUOUS` PAUSE** (bound `PauseResumeProtocol`) else FAILED (behaviorally equivalent to the pre-v1.72 fail-closed; only the fail_class string differs). **NEVER an auto-re-fire** (the #701 decline-mirror invariant transferred: auto-proceed ONLY on proof-of-completion). **The enum-layer correction (advisor + primary source):** the fork doc's verification 1 framed the value as the engine-layer §22.1 `PauseReason`; the `capture_pause_snapshot` type signature settled it the other way — the driver-routed pause is `WorkflowPauseReason`-typed (C-CP-26 §26.2); the §22.1 enum is the engine-native replay-pause substrate the driver never calls (the HITL precedent is exact: `WorkflowPauseReason.HITL_PENDING`, not the engine `HITL_INVOCATION_PENDING`). Witnesses: dispatcher suppress-and-continue (counting tool fires EXACTLY ONCE) + ambiguous-absent + ambiguous-corrupt + durable-suppress-across-restart; driver ambiguous → PAUSED [bound] / FAILED [unbound] on the linear/TOOL_STEP path; retry-breaker verbatim re-raise; 6-class `WorkflowPauseReason` cardinality. **No new fail-class taxonomy code, no §5.2-hash change, no `StepDispatcher` Protocol widening, no new CXA edge** (the CP→IS `PAUSE_CAPTURED` emission reuses the seam with `event_kind_index=3`). pyright 0/0/0; harness-runtime non-e2e 2030 + harness-cp 1167+1xfail + harness-od 950 + harness-cxa 28 passed. Bundled-absorption: runtime spec v1.72 + CP spec v1.51 + 2 clearance markers + harness-runtime/src + harness-cp/src + by-execution tests. Fork: `.harness/class_1_fork_effect_fence_hitl_route.md` (✅ BUILT). **Scope boundary (Codex pre-merge catch — capture, not resolution):** this arc builds suppress-and-continue + the ambiguous-pause CAPTURE (a labeled, non-terminal, inspectable, hash-valid pause — strictly better than the v1.60 terminal FAILED); the resume-side RESOLUTION (operator `skip-as-fired` / `re-fire` [clear the claim] / `abort`) is a safety-sensitive committed-behavior surface registered as the follow-on **`B-EFFECT-FENCE-PAUSE-RESOLUTION`** (NOT silent-deferred). Until it lands, a naive `api.resume` re-pauses identically (INERT — resume is caller-initiated, never auto-driven; pinned by `test_ambiguous_pause_resume_re_pauses_until_resolution_follow_on`). **Codex pre-merge [P2]×2 reconciled (both fixed):** (a) the suppress path now emits `sandbox.exit` to balance the pre-fence `sandbox.enter` (enter-without-exit telemetry gap; advisor concurred Codex was right) + a span-exporter test; (b) the unresolvable-pause finding → the (B) partial-land + the registered follow-on + the interim-re-pause test above. advisor (the enum-layer correction + the crux + the byte-identical-string caveat + the untested-CP→IS-emission catch → emission test; (B) partial-land + the caller-initiated-resume verify) + out-of-family Codex (2 rounds, both [P2] reconciled). `[[durable-recovery-presence-validity-scope]]` · `[[gate-enforcement-site-and-timing-asymmetry]]` (the decline-mirror) · `[[full-chain-witness-not-half-proofs]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]` (capture-not-resolution partial-land) · `[[hooks-codex-pilots-decorrelation-validated]]`.
  - **`B-EFFECT-FENCE-PAUSE-RESOLUTION` — ✅ BUILT 2026-06-22 (BUILD-not-gate; runtime spec v1.72→v1.73 §14.22.9 + CP spec v1.51→v1.52 §26.8/§26.2; PR #<pending>).** The resume-side RESOLUTION of the §26.2 `EFFECT_FENCE_AMBIGUOUS` pause #702 opened — #702 shipped the ambiguous-pause CAPTURE INERT (a naive `api.resume` re-paused identically); this arc wires the resume to RESOLVE it via an operator-supplied `EffectFenceResolution` (skip-as-fired / re-fire / abort). **BUILD-not-gate (the #702 registration's "operator-gated + safety-sensitive" label flipped at design-vet, advisor-concurred — `[[disposition-label-is-a-claim-verify-against-spec]]`):** the fence raises `EFFECT_FENCE_AMBIGUOUS` *specifically to ASK the operator "did the effect fire?"* — the harness genuinely cannot tell (the crash fell in the fire→capture window); the three resolutions are the operator ANSWERING with ground-truth the harness lacks. `RE_FIRE` = "it did NOT fire" → a fresh first-and-only execution → it **COMPLETES the at-most-once decision the harness couldn't compute, NOT breaches it** (the #529 `cascade-cancel` out-of-domain reframe; a mis-assertion is operator-error responsibility, the #700 idempotent-mis-declaration posture). `SKIP_AS_FIRED` = "it fired" → proceed with EMPTY output (the lost output is unrecoverable), never re-fire. `ABORT` = "can't tell" → FAILED. The palette composes only committed primitives (`clear_claim` + re-dispatch + empty-output accumulate + existing FAILED) → §13.4 discriminator (a) → **no operator gate, no council** (the §10.9 nameable-tension probes resolved the residuals to the minimal spec'd semantic: skip-as-fired = empty output, NO operator-supplied-output over-build per SCOPE BOUNDARY 2). **Mechanism (all additive):** `RuntimeEffectFence.clear_claim` (inverse of `try_reserve`, missing-ok); `ResumeContextHolder.peek` (non-consuming → the runtime HITL composer's one-shot `consume_and_clear` is intact); the dispatcher's §14.22 fence gate gains a KEY-BOUND three-branch resolution (apply ONLY when the recomputed dispatch key == the directive's `idempotency_key` — correctness-by-construction, a stale resolution can never mis-apply); the driver populates `PauseSnapshot.effect_fence_resume` from the runtime error's `idempotency_key` at the §26.2 capture branch, peeks the holder (gated on the carrier), key-binds, and threads an `EffectFenceResolutionDirective` onto the RESUMED linear step's hash-inert `StepExecutionContext.effect_fence_resolution`; `ABORT` → NEW `EffectFenceAbortedError` → the driver's generic FAILED mapping (not a `_TRANSIENT_TOOL_DISPATCH_ERRORS` member → the retry-breaker re-raises it verbatim). **No new fail-class taxonomy code, no §5.2-hash change, no `StepDispatcher` Protocol widening, no new CXA edge** (the `effect_fence_resume` carrier rides the `PauseSnapshot` `snapshot_hash` — NOT the IS state-ledger six-field hash; the directive is hash-inert). Witnesses (by-execution, no proxy): dispatcher RE_FIRE-fires-fresh + clear_claim-removed-the-real-claim-file + SKIP_AS_FIRED-empty-output-no-refire + ABORT-raises + key-mismatch-ignored + the clear_claim file-level unit; driver carrier-population + hash-integrity (the hash covers `effect_fence_resume`) + ABORT→FAILED + the **full-chain directive-threading producer** (the driver peeks non-consuming, key-binds, threads to the resumed step only — `[[full-chain-witness-not-half-proofs]]`). pyright 0/0/0; harness-cp 1171+1xfail; harness-runtime non-e2e 2031+. Bundled-absorption: runtime spec v1.73 + CP spec v1.52 + 2 clearance markers + harness-runtime/src + harness-cp/src + by-execution tests. Design: `.harness/class_1_fork_effect_fence_pause_resolution.md`. advisor (2 passes — the BUILD-not-gate reframe that flipped the disposition + the key-bound/consume-once + clear_claim/ABORT-claim witness additions) + out-of-family Codex. **The §14.22 effect-fence family is now COMPLETE through the operator-recoverability story** (DURABLE-AUTO + PER-TOOL + HITL-ROUTE + PAUSE-RESOLUTION all built). `[[disposition-label-is-a-claim-verify-against-spec]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[durable-recovery-presence-validity-scope]]`.

- **B-EDIT-CARRIER — HITL `EDIT` carrier-healing / structured-edit mutation discipline — ✅ BUILT 2026-06-19 (runtime spec v1.61 → v1.62 §14.8.2 step 4i + NOTE 6-ii; mutation discipline (A) JSON-decode-then-replace; `RT-FAIL-HITL-GATE-EDIT-DECODE` registered + interim `HITLGateEditCarrierDriftError` retired; `edited_proposal_hash` now POST-mutation [F3-03 closed]; full-chain witness + flipped negative control; NO operator gate — (A) PRESERVES replace-not-merge, probe-resolved, FULL-SPEC pre-authorized; fork RESOLVED).** (surfaced by U-RT-120 / arc #23 B3-impl-3; advisor [blocking]). Cleared runtime spec §14.8.2 step 4i + NOTE 6-ii mandate replace-not-merge: the operator's `edited_proposal` becomes the new `step.step_payload` *verbatim*; "consumers MUST treat it as authoritative replacement." The mandate presumes the CP-canonical **structured** carrier (`HITLGateResult.edited_proposal: Mapping[str, Any]`, `hitl_placement.py:197`; `WorkflowStep.step_payload` is itself `Mapping[str, Any]`). But the wired runtime ask-surface returns `AskUserQuestionResult.edited_proposal: str | None` (`ask_user_question_surface.py:86`), and MCP elicitation is **flat-schema** (primitives only — cannot deliver a nested Mapping, `mcp_backed_ask_user_question_surface.py:171-199`). So the structured-elicitation path (design-doc D-edit.A) is unreachable through the wired path → **D-edit.B**: the `str → Mapping` replacement is genuinely under-specified (the decode/mutation semantics NOTE 6-ii explicitly defers to "a future workflow-mutation-discipline arc"). **Interim (landed at arc #23):** the step-4i `EDIT` branch was silent non-compliance (`pass` → dispatch the step unchanged, dropping the operator's edit); it now raises `HITLGateEditCarrierDriftError` (runtime-shape, NOT pre-registered into the §14.8 `RT-FAIL-*` taxonomy) surfacing the str↔Mapping drift + citing the fork. The audit at step 4h still records the operator's edit faithfully (`edited_proposal_hash`) before the raise (symmetric to REJECT). The deviation is recorded at `.harness/class_1_fork_hitl_edit_carrier_drift_str_vs_mapping.md` (Class 1). **The build:** (a) pick the mutation discipline — JSON-decode-the-`str` (the IMPL-discretion `r_cxa_2._parse_edited_arguments` shape, no current step-payload-surface spec backing) vs structured-flat typed-field elicitation vs richer field-level patch/merge (the full NOTE 6-ii deferred family); (b) amend NOTE 6-ii (and/or mint a structured-edit contract) — a `design-substrate/` amendment carrying the fork as X-AL-3 back-flow + a clearance marker; (c) land **functional operator EDIT** (replace-not-merge actually applies); (d) register the `RT-FAIL-*` taxonomy code + retire the interim runtime-shape raise; (e) resolve the `edited_proposal_hash` "post-mutation payload hash" semantics for the no-mutation interim (reviewer F3-03 — the hash is currently over the *pre-mutation* operator `str`). **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3).** Load-bearing: HIGH — `EDIT` is a spec-mandated 4-response-palette member; it ships **non-functional (raises)** until this arc, so functional operator EDIT is a committed R-FS-1 build, NOT an indefinite defer. Rec: **BUILD** (runtime + CP workflow-mutation-discipline arc; design-fork-first). On landing, the §14.8.2 / NOTE 6-ii deviation closes and the `EDIT` branch flips raise → apply.
  - **✅ BUILT 2026-06-19 — as-built scope = the wrap-time SYNC path** (the sole `EDIT` `step_payload` application site; the real-consumer witness `test_edit_decoded_payload_reaches_real_llm_dispatcher` drives the composer's inner = the REAL `RuntimeLLMDispatcher` over a recording provider → the DECODED Mapping is what the provider received, closing the half-proof gap per advisor pre-merge / `[[full-chain-witness-not-half-proofs]]`). Mutation discipline **(A) JSON-decode-then-replace** — foreclosed by the flat-`str` carrier + the committed replace-not-merge mandate + the arbitrary-nested target; (A) PRESERVES replace-not-merge → no operator gate (probe-resolved, FULL-SPEC pre-authorized). Runtime spec v1.62; `RT-FAIL-HITL-GATE-EDIT-DECODE` registered + interim `HITLGateEditCarrierDriftError` retired; `edited_proposal_hash` POST-mutation (F3-03 closed).
  - **`B-EDIT-CARRIER-DURABLE-ASYNC-RESUME` — ✅ BUILT 2026-06-21 (impl-to-cleared-spec + impl-to-cleared-plan; NO design-substrate edit; NO operator gate; PR #683).** The durable-async **resume** Step-0 consume (§14.8.8.5; `hitl_gate_composer.py`: `resume_state.hitl_response is not None → return self._dispatch_inner(...)`) dispatched the resumed step UNCHANGED for **ALL 4** response types — the "auto-approve MVP shape." **Grounding revealed the gap is BROADER than this ledger's prior EDIT-only line** (`[[grounding-reveals-claude-closeable-slice-close-honestly]]`) AND that the code's "per v2.24 AC #7" docstring **MIS-READ AC #7** (`[[built-but-vacuous-reground-ledger-asis]]`): plan U-RT-94 AC #7 mandates `gate_result = hitl_response` → step-4i routing, and spec §14.8.8.5 says "proceed to step 4i response-routing" — it NEVER sanctioned auto-approve. So closing it is pure impl-to-cleared-spec/plan (#677 precedent, no design-substrate edit). **Two of the four resume responses were silently wrong:** EDIT dropped the operator's edit; **REJECT dispatched a REJECTED step as if approved — a fail-safe violation** (the highest-value fix; `[[enforce-floor-no-bypass-seam]]`). **As-built:** Step-0 now routes per the §14.8.2 step-4i palette — EDIT applies the structured `HITLResult.edited_proposal.payload` VERBATIM (replace-not-merge §14.8.2 NOTE 6-ii; the resume carrier is ALREADY a `Mapping` — `ActionPayload = Mapping[str, Any]` — so NO `_decode_edit_proposal`, which decodes only the sync flat-`str` MCP carrier; None-guard → typed `HITLGateEditDecodeError`, not `AttributeError`); REJECT raises the SAME `HITLGateRejectedError` as sync → `RT-FAIL-HITL-GATE-REJECTED` (no divergent classification) AND the step is NOT dispatched; APPROVE/RESPOND dispatch unchanged (already correct). **The Step-0 short-circuit is KEPT deliberately as a ONE-SHOT boundary** — flowing through to step 4f would re-enter §14.8.8.1 deliver-then-pause and **RE-PAUSE instead of resume** (advisor caught; the registered hard reason Option A is hazardous, not merely bigger). Witnesses (`test_lifecycle_hitl_gate_composer.py`, +5 by-execution): EDIT→dispatched step_payload IS the edit (orig key GONE, proving replace-not-merge); REJECT→raises + step NOT dispatched; EDIT-None→typed error; APPROVE/RESPOND→unchanged. **Out-of-family Codex caught a real [P1] that made the REJECT fix VACUOUS through the production stack** (`[[hooks-codex-pilots-decorrelation-validated]]` + `[[full-chain-witness-not-half-proofs]]`): the stage-5 stack is `retry(HITL(bare))` (the retry/fallback wrapper's `inner` IS the HITL composer), and `_classify_provider_exception` mapped ALL `Exception`s to `TRANSIENT_RETRY` — so `HITLGateRejectedError`/`HITLGateEditDecodeError` were RETRIED, and on a durable-async resume the retry re-entered an emptied holder and RE-PAUSED. **This arc therefore also touches the reliability primitive** (`retry_breaker_fallback.py`): HITL terminal control-flow exceptions (REJECT / EDIT-decode / cell-excluded / timeout / audit-compose) now PROPAGATE through the wrapper untouched (like `asyncio.CancelledError`) — never retried, never candidate-advanced (§14.6 D2: HITL outcomes are control-flow, not provider results; lazy import breaks the hitl→retry cycle; a marker-base-class drift-guard is the registered follow-on). This **also fixes the PRE-EXISTING sync REJECT/timeout-retried-by-the-wrapper latent issue** (owned, correct, an improvement). Full-chain witnesses (`test_lifecycle_retry_breaker_fallback.py`, +2): a resume REJECT / EDIT-decode through the REAL `retry(HITL(...))` stack propagates terminally with the inner LLM dispatched ZERO times (the buggy path would retry → empty holder → dispatch + return success). pyright 0/0/0; harness-runtime non-e2e 1995 passed (incl. the elicitation/pause-escape path). advisor (pick + close-shape three-way discriminator + A-vs-B re-pause hazard + EDIT+REJECT scope-broadening + Codex-[P1] propagate-not-classify approach + escape-the-outer-loop verification).
  - **`B-RESUME-RESPONSE-AUDIT-WRITE` — ✅ BUILT 2026-06-21 (impl-to-cleared-spec §14.8.8.6; NO design-substrate edit; NO operator gate; PR #685).** The §14.8.8.6 step-4h audit now fires on the durable-async resume path for ALL 4 response types (#683 landed routing but left REJECT-on-resume raising WITHOUT an audit entry + the operator's resume response unaudited). Composed in the Step-0 block BEFORE routing (so a REJECT is audited THEN raised — sync 4h→4i ordering), per matching placement. **KEY SIMPLIFICATION (grounding overturned the registration's "needs step-1..4 placement/cell" framing):** the audit needs only the matching placement(s) + `parent_action_id` — NOT matrix-cell resolution (the `cell` arg is **vestigial** in `_compose_and_persist_audit`: its only reference is a comment; `gate_level` is the hardcoded AUTO sentinel) and NOT the gate-FIRE (4-bis/4f), so the #683 one-shot Step-0 boundary + re-pause-avoidance hold. **Carrier (advisor):** the resume `HITLResult` lacks `latency_ms` (never an audit field — `[[grounding-reveals-claude-closeable-slice-close-honestly]]`: the "missing fields" were the resume audit *correctly shaped*, not a gap) + plaintext `rejection_reason` (carries the pre-computed `response_summary_hash`); a NEW additive `_compose_and_persist_audit(resume_response=)` 8a branch derives the hashes — EDIT recomputes the POST-mutation payload hash from `edited_proposal.payload` (sync NOTE 6-ii byte-parity), RESPOND hashes the plaintext `response_text`, REJECT uses `response_summary_hash`. **Composer-only — NO retry-wrapper / runtime-dispatcher change** (unlike #683). Witnesses (`test_lifecycle_hitl_gate_composer.py`, +4): REJECT audits (response="reject", rejection_reason_hash=carrier hash) → raises + not dispatched; EDIT audits the post-mutation hash + applies; APPROVE audits (response="approve") + dispatches; no-matching-placement → no audit but routing holds. pyright 0/0/0; harness-runtime non-e2e 1999 passed. advisor (pick + the cell-vestigial / latency-moot close-shape sharpening + audit-before-routing ordering). The `B-RESUME-CONTEXT-SURVIVES-RETRY` sibling follow-on (one-shot holder survives a post-consume retry) remains registered.
  - **`B-RESUME-CONTEXT-SURVIVES-RETRY` — ⛔ RESOLVED-AS-PREMISE-INVALID 2026-06-21 (advisor + literal-spec + existing-test confirmed; NOT built — the arc's premise is FALSE).** The arc was registered as a gap: an APPROVE/EDIT/RESPOND resume whose post-consume inner dispatch hits a TRANSIENT provider error → the OUTER retry wrapper [stack `RetryBreakerFallback(HITL(bare))`, retry OUTER, confirmed `stage_5_loop_init.py:352`] re-invokes the composer → `consume_and_clear()` returns an EMPTY holder → re-fire-from-scratch (re-pause for a durable-async cell) "instead of honoring the already-consumed response." **Re-grounding at the #693-tick pick (advisor-steered) found this is NOT a bug — re-ask-the-operator-on-each-retry-attempt is the deeply-committed, ratified, TESTED behavior.** FOUR cleared authorities mandate it: (1) **§14.8.8.7 invariant 3 LITERAL** — "One-shot resume delivery. `ResumeContext.hitl_response` consumed at most once per resume cycle; **subsequent gate-evaluations at the same step RE-FIRE FROM SCRATCH**"; (2) **§14.8.7 NOTE 6-iii** "operator re-asked on each retry attempt"; (3) **plan v2.9 U-RT-60 AC #12** (literal Q2 reading); (4) the EXISTING load-bearing test **`test_retry_of_gate_re_evaluates_gate_per_attempt`** asserts 3 attempts → 3 surface invocations → 3 audit entries. A transient retry's composer re-entry IS a "subsequent gate-evaluation at the same step" → invariant 3 mandates re-firing; the proposed peek-clear-on-success "fix" would BREAK that test (re-ask + re-audit per attempt is INTENDED — building it would be a silent re-litigation of a ratified invariant + a load-bearing-test break = the exact X-AL-3 silent-absorption failure mode). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` ratified-invariant → confirm-don't-rescue. **DISTINCT FORWARD OPTION (NOT this arc):** if the operator wants the UX of NOT double-approving on a transient retry, that requires RELAXING §14.8.8.7 invariant 3 + §14.8.7 NOTE 6-iii + U-RT-60 AC #12 — a Class-1 ADR/spec-amendment + operator ratification. Surfaced to the operator; re-register as a NEW relax-invariant arc IF chosen. (Was: Rec BUILD — overturned by the literal-invariant + existing-test grounding.)
  - **`B-HITL-WRAP-FAIL-CLASS-SURFACING` — ✅ BUILT 2026-06-21 (impl-to-cleared-spec; NO design-substrate-contract edit; NO operator gate; PR #691).** As-built = the registered shared-marker mechanism the rec below proposed: each of the 4 wrap-time HITL terminal exceptions (`HITLGateRejectedError` / `HITLGateEditDecodeError` / `HITLGateTimeoutError` / `HITLGateAuditComposeError`) carries an `rt_fail_class` class attribute naming its §14.8 code; the cp driver composes `fail_class` via a NEW import-free `_step_fail_class(prefix, exc)` helper reading `getattr(exc, "rt_fail_class", None) or type(exc).__name__`, applied at ALL 5 per-topology step-dispatch failure sites (linear / EVALUATOR_OPTIMIZER / ORCHESTRATOR_WORKERS / DECENTRALIZED_HANDOFF ×2). A non-marker exception falls back to the class name (byte-identical to pre-arc). `HITLCellExcludedError` intentionally NOT marked (no §14.8 code). Closes a prose-vs-code drift only (the §14.8 codes + the docstrings' "Driver maps to RT-FAIL-X" promise already existed) → runtime spec v1.67 → v1.68 change-note + clearance marker; CP spec UNCHANGED (helper is CP-internal, no cross-axis import). +4 by-execution witnesses; pyright 0/0/0; harness-cp 1136 + harness-runtime non-e2e 2004 passed. advisor + out-of-family Codex. (Was: Registered follow-on, NOT built here; Codex [P2] decorrelated catch, advisor-pre-cleared as acceptable-by-precedent.) The new `RT-FAIL-HITL-GATE-EDIT-DECODE` is registered in the §14.8 taxonomy (fork item (d)), but the cp `workflow_driver.py` surfaces wrap-time HITL exceptions via the generic `step-failure: {type(exc).__name__}: ...` path — so the literal `RT-FAIL-HITL-GATE-EDIT-DECODE` string is not emitted in `fail_class` (a caller distinguishes the mode by the exception type name, not the RT-FAIL code). This is a **PRE-EXISTING architectural property** (harness-cp cannot import harness-runtime exception types; only cp-typed validator-gate errors map to explicit `RT-FAIL-*` strings — the wrap-time `HITLGateRejectedError` / `HITLCellExcludedError` have the SAME generic surfacing), NOT introduced by this arc; the spec §14.8 EDIT-DECODE row documents it honestly. The fix (a cp-driver runtime-exception→`RT-FAIL-*` mapping for ALL wrap-time HITL errors, e.g. via a shared marker/attribute the generic except reads) is cross-cutting beyond EDIT. Rec: **BUILD** (a small CP-driver fail-class-surfacing arc covering EDIT-DECODE + REJECT + TIMEOUT + AUDIT-COMPOSE consistently).

- **B-LAYER-BUDGET-OVERRIDE — per-layer time-budget wall-clock enforcement honoring per-workload / per-persona overrides (surfaced by R-impl-1 L3 timeout / arc R-impl-1; Codex [P2], spec-reject-with-evidence; UNTRACKED → forward arc).** Cleared CP spec C-CP-03 §3.1 materializes a **per-workload-class × per-persona-tier** budget tuning surface; `LayerBudget` carries `per_workload_override` + `per_persona_override` (U-CP-06, `layer_budget.py:59-66`) and `effective_budget(layer, workload, persona)` (`layer_budget.py:79-95`) resolves the override precedence. But `effective_budget()` is **built-but-vacuous workspace-wide** (`[[built-but-vacuous-reground-ledger-asis]]`: zero production callers — only its def + `test_layer_budget.py`), and the deterministic `route()` (`layered_routing_strategy.py:62-101`) does **NO wall-clock budget enforcement at all** — it consumes only the caller-supplied `budget_exhausted` frozenset and never reads `time_budget_ms`. So per-layer time-budget enforcement that *honors the override maps* is unmaterialized for **every** layer, not just L3. R-impl-1's L3 `asyncio.wait_for` (`routing_core_surface.py:268`, `_layer_time_budget_ms` at `:163-172`) is the **first and only** site that enforces `time_budget_ms` as a real timeout — and it reads the **flat** field, not the override-resolved value. **Why flat at R-impl-1 (not a defect):** the cleared CP spec v1.36 §2.5.3 explicitly bounds the L3 router by the **flat 200 ms** `LayerBudget` (pseudocode `:71` `= LayerBudget(LLM_AS_ROUTER).time_budget_ms / 1000`; §3/C-CP-03 **PRESERVED VERBATIM**; the prose "effective L3 timeout" at §2.5.2/§2.5.3 is glossed in-sentence as "the 200 ms `LayerBudget`", NOT the `effective_budget()` term-of-art). Honoring the override maps at R-impl-1 would be an **X-AL-3 silent design extension** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`; Codex is decorrelated but spec-blind, `[[insights-recs-workspace-blind]]`). **Structural proof override-wiring is a separate not-yet-designed arc:** `effective_budget()` reads `DEFAULT_LAYER_BUDGETS` (module global), **not** the `budgets` tuple `infer()`/`route()` receive — so the helper as-built physically cannot serve the L3 timeout site; if §2.5 intended override-resolution there, the helper would read the passed budgets. **Interim (landed at R-impl-1):** the L3 timeout uses the flat `time_budget_ms` (spec-faithful per §2.5.3); the override maps stay inert — NOT a new degeneracy (they were already vacuous workspace-wide; R-impl-1 does not regress them). **The build:** (a) decide the wall-clock budget-enforcement locus (the `infer()` L3 site is the first natural consumer; the sync+fast deterministic layers are a separate question) + a `budgets`-tuple-aware `effective_budget` variant keyed on `request.workload_class` + `request.persona_tier`; (b) if honoring overrides at L3 changes the §2.5.3 flat-budget contract, a CP spec leg is owed (design-fork-first per X-AL-3 + clearance marker). **Owner = a follow-on arc (the C-CP-03 §3.1 per-layer-budget tuning-surface activation; composes with R / CA).** Load-bearing: LOW-MEDIUM — operator-tuning refinement; the flat 200 ms is a safe spec-mandated default and no current deployment supplies overrides. Rec: **BUILD** (CP budget-enforcement arc; design-fork-first per X-AL-3 if it amends §2.5.3). On landing, the C-CP-03 §3.1 override-tuning surface becomes load-bearing for real per-workload/per-persona budget enforcement. **✅ BUILT (this arc, 2026-06-19) — bundled-absorption (CP spec v1.42 → v1.43 in-place C-CP-02 §2.5.3 amendment [the L3 timeout uses the §3.1 OVERRIDE-RESOLVED effective budget keyed on `request.workload_class`/`persona_tier`, not the flat field] + clearance marker + `.harness/class_2_fork_b_layer_budget_override_l3_effective_budget.md` + `harness-cp/src` [`effective_layer_budget_ms` — the `budgets`-tuple-aware resolver `effective_budget` couldn't be (it reads the module global); the L3 site honors it; orphaned `_layer_time_budget_ms` removed] + `harness-runtime/src` [the DORMANT `RuntimeLLMDispatcher.budgets` threading seam, default `DEFAULT_LAYER_BUDGETS`] + by-execution tests [`infer()` per-persona + per-workload override witnesses with a flat-default negative control, resolver units, dispatcher-seam threading witness]), NO operator gate — §3.1 affirmatively commits the override on `llm_as_router` → impl-to-cleared-spec; the prior in-code "X-AL-3 forward arc" note is overturned by re-grounding the cleared spec (advisor-confirmed; no ratified flat-only-at-L3 decision in the ledger), behavior-preserving by default (no override ⇒ byte-identical 200 ms). Production-dormant (`router=None` + DEFAULT budgets; live firing additionally gated by the UNOWNED routing-activation gate) — capability-built, NOT "fires in production" (the non-vacuity witness lives at `infer()`). advisor-steered (lead-with-§3.1 + drop-the-"effective"-reading + frame-as-amendment + non-vacuity-at-infer) + out-of-family Codex pre-merge. The deterministic-layer wall-clock enforcement + the R-impl-2 200 ms recalibration remain separate registered concerns.**
  - **R-impl-2 empirically extended this arc (2026-06-16; advisor pre-done catch; `[[test-bypass-as-runtime-truth-pattern]]`).** Two NEW findings the live-Ollama router e2e surfaced, beyond the original override-honoring scope: **(i) the default 200 ms L3 budget is empirically UNMEETABLE by a real LOCAL router** — even a *warm* local Ollama (llama3.2:3b, `format=json`, capped `num_predict`) needs seconds, not 200 ms. (NOT a claim the spec's §2.2 "50-200 ms" is wrong — that estimate is for a fast *hosted* router; it is **UNVERIFIED for hosted Haiku** because verifying it would fire the paid call, which R-impl-2 does not. Stated precisely: empirically false for local; unverified for hosted.) **(ii) `RuntimeLLMDispatcher` HARDCODES `DEFAULT_LAYER_BUDGETS`** (`llm_dispatch.py:584`) in its `infer()` call — there is no budget-threading seam — so even a correct per-workload/persona override mechanism (the (a) build above) **could not reach the L3 timeout through the production dispatcher** without ALSO threading `budgets` from construction (the same shape as the R-impl-1 `layer_decisions_override` seam). **The composition gap (why R-impl-2's e2e calls `infer()` directly, not through the dispatcher):** R-impl-1 proved dispatcher→`infer(router=…)` (mock router, 200 ms); R-impl-2 proves real-router→`infer()` (60 s budget). They **do not compose** into "a real router resolves L3 through the production dispatcher" — that path times out at 200 ms every call. This is OK to ship ONLY because production never reaches L3 today (`router=None` + DECLARATIVE always resolves). **Net: production L3 router activation depends on THREE things, all registered-forward, none silently assumed:** (1) **the routing-activation gate** (make DECLARATIVE conditional so traffic reaches the L3 sentinel; UNOWNED — both R-300 items RESOLVED without it), (2) **a realistic L3 budget** (this arc's calibration), (3) **dispatcher budget-threading** (this arc's build (a), extended: a `budgets` construction param on `RuntimeLLMDispatcher`). Load-bearing revised **MEDIUM** (was LOW-MEDIUM): without (2)+(3) the L3 LLM_AS_ROUTER layer is unreachable in production *even after* R-300-second-provider. Add the dispatcher `budgets`-threading seam to the build set.

- **B-TOOL-GATE — tool-step HITL gate site = the real per-server MCP-trust producer (surfaced by U-RT-131 / arc B2-plan; adversarial [F2-01] + advisor; UNTRACKED → forward arc).** Cleared CP spec §19.1.2 Producer ¶ + runtime §14.8.2 step-4c envision `GateLevelInput.mcp_trust_tier` populated "from the resolved owning MCP host's declared trust … via the v1.51 §14.9.10 routing index." But at HEAD the runtime HITL gate composer (`RuntimeHITLGateComposer`) is constructed for **only two host-less placements** — `hitl_inference` (`stage_5_loop_init.py:337`, `PRE_ACTION`) + `hitl_sub_agent` (`:431`, `SUB_AGENT_BOUNDARY`); `TOOL_STEP`s dispatch through `runtime_tool_dispatcher.py`, which composes **no** HITL gate (zero `gate_level`/`GateLevelInput`/`hitl` refs). **So no gate site has an owning MCP host** — the resolved-host feed has nothing to populate. **Interim (lands at B2-impl-3, U-RT-131):** the harmful `hitl_gate_composer.py:462` `LEVEL_0_REFUSE_REMOTE` constant is replaced with the **L3 AUTO-mapping no-floor default** (the host-less gate sites contribute no MCP-trust floor — §19.1.2 invariant 3, the `per_tool_gate_level`/O-CP-3 degenerate-default analog), so U-CP-98's axis composition is safe (non-regression); the axis is materialized in `gate_level()` but feeds a degenerate default in production. **The build:** a **tool-step HITL gate site** (a `RuntimeHITLGateComposer` at a tool placement, or gate composition inside `runtime_tool_dispatcher.py`) that (a) resolves the owning MCP host for a `TOOL_STEP` via the v1.51 routing index (U-RT-128), (b) feeds its D3-projected per-server `MCPTrustTier` (U-RT-129) into `GateLevelInput.mcp_trust_tier`, (c) so an L0-server tool actually floors its gate to `DENY` and an L3 to `AUTO`. **Owner = a follow-on R-FS-1 child arc** (a runtime gate-site surface; **likely impl-against-cleared-spec** since §19.1.2 Producer ¶ + step-4c already spec the input + the routing index/D3 projection land in B2 — confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: **HIGH** — it is what makes the §19.1.2 MCP-trust gate axis **non-vacuous in production** (U-CP-98 composes the axis, but every current gate site is host-less → AUTO; the real per-server DENY/ASK floors only bite once a tool-step gate exists). Composes with B2 (routing index + D3 projection) + the B3 HITL gate-composer machinery. Rec: **BUILD** (runtime gate-site arc). On landing, the §19.1.2 Producer ¶ realizes in production and the MCP-trust axis stops being a degenerate-default. **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (`harness-runtime/src` + by-execution tests + fork doc; NO design-substrate edit, NO operator gate).** Mechanism = Option A (the spec's named producer): a THIRD `RuntimeHITLGateComposer` wraps the tool dispatcher at the **registry path** (`tool_step_dispatcher = facade(hitl_tool(ctx.tool_dispatcher))`; `ctx.tool_dispatcher` itself NOT mutated — the R-CXA-2 producer loop + provider-turn `hitl_tool_loop` still read the un-gated dispatcher), `applicable_placements={PRE_ACTION}`, same dep set as the inference/sub-agent composers + a NEW `mcp_trust_tier_resolver` (the `blast_radius_resolver` sibling; `step_mcp_trust_tier.py` — resolves `tool_id`→owning host→`MCPClientHost.trust_tier` via a host-scan, collision-guarantee-aligned with dispatch). **Impl-against-cleared-spec** (§14.8.2 step-4c already names `mcp_server_trust_tier`; `GateLevelInput.mcp_trust_tier` + `MCP_TRUST_GATE_LEVEL_FLOOR` exist; no new contract/field/invariant → no operator gate). **HONEST three-claim scope (the "non-vacuous in production" body claim above is CORRECTED here):** (1) **wiring correct** — proven through real `run_bootstrap` (`test_bootstrap.py`: TOOL_STEP registry = `facade→hitl_tool[resolver bound]→ctx.tool_dispatcher`); (2) **axis composes when the gate fires** — proven by-execution (L0→DENY→palette narrows {REJECT,RESPOND}→ask→REJECT raises; L3→AUTO→delegate; real-resolver-over-real-hosts integration); (3) **production gate-firing awaits the per-step placement producer** — the **Codex-caught [P1] decorrelated finding** advisor + author missed: the wrap-time gate is placement-driven (`getattr(step,"hitl_placements",())`), but `WorkflowManifestEntry.hitl_placements` is WORKFLOW-level (C-CP-17 §17.3) and NEVER bound onto the per-step `WorkflowStep` (frozen+`extra="forbid"`), so NO wrap-time gate (inference/sub-agent/tool) fires through the real manifest→driver path — a **PRE-EXISTING gap shared by all gate sites** (even the existing inference/sub-agent e2e tests attach placements via a `_StepWithPlacements` proxy). Registered as `B-HITL-PLACEMENT-PER-STEP-PRODUCER` (below). DENY-reachability verified sane (advisor pre-build): DENY narrows the palette to {REJECT,RESPOND} + invokes the ask surface (structural-rejection-with-HITL), NOT a stub. **PRE_ACTION forward-coupling:** the tool gate keys on `{PRE_ACTION}` like inference, so once the producer lands a workflow-level PRE_ACTION placement gates BOTH inference and tool steps — the producer arc must account for this.

- **B-HITL-PLACEMENT-PER-STEP-PRODUCER — bind workflow-level `hitl_placements` onto the per-step `WorkflowStep` so the wrap-time HITL gates fire in production (surfaced by R-FS-1 `B-TOOL-GATE`, 2026-06-18; Codex [P1] decorrelated, advisor-confirmed; UNTRACKED → forward arc; sharpens the B3 "placement composition is a placeholder" residual above).** The wrap-time HITL gate composer (`RuntimeHITLGateComposer`, all three sites — inference `PRE_ACTION`, sub-agent `SUB_AGENT_BOUNDARY`, tool `PRE_ACTION`) fires only when `getattr(step, "hitl_placements", ())` is non-empty (`hitl_gate_composer.py` step 1). But `hitl_placements` is declared at the **WORKFLOW** level (`WorkflowManifestEntry.hitl_placements`, "Declared per workflow per C-CP-17 §17.3"; the loader's `_WorkflowSection.hitl_placements`), while the per-step `WorkflowStep` the driver dispatches is frozen + `extra="forbid"` and carries **only** `step_id`/`step_kind`/`step_payload` — and **NO src binds the workflow-level placements onto the per-step steps**. So `step.hitl_placements` is **always `()`** through the real `WorkflowManifestLoader`→driver path → **NO wrap-time HITL gate fires in production for ANY step kind.** The entire wrap-time gate (the B3 smart-HITL machinery: conditional gating, palette narrowing, the MCP-trust floor `B-TOOL-GATE` just wired, the auto-approve policy) is production-unreachable today; every existing gate e2e test (`test_run_workflow_elicitation_e2e.py:234-250`, the inference/sub-agent gate tests) attaches placements via a `_StepWithPlacements` TEST PROXY for exactly this reason — `[[test-bypass-as-runtime-truth-pattern]]` at the workspace scale. **The build:** a workflow-binding-time producer that resolves the workflow's declared `hitl_placements` to the per-step gate-input the composer reads — either (a) a richer per-step carrier the driver dispatches (a `WorkflowStep` placements field, or a side-channel `dict[StepID, tuple[HITLPlacement,...]]` the composer consults), or (b) a per-step placement schema in the manifest, with the workflow→step application semantics (do workflow-level placements apply to ALL steps? a per-step `step_kind`/position match?). **Spec'd-ness — determine at arc-open:** the placement DECLARATION is spec'd (C-CP-17 §17.3 workflow-level; the composer docstring cites "workflow-binding-time per U-CP-13 + U-CP-38"), but the workflow→per-step BINDING mechanism + application semantics may be under-specified → if spec'd, a forward build-arc; if the binding semantics are unspec'd, **design-fork-first per X-AL-3** (a `design-substrate/` amendment + clearance marker). **PRE_ACTION coupling (from `B-TOOL-GATE`):** inference + tool composers both key on `{PRE_ACTION}`, so the producer must decide whether a workflow-level PRE_ACTION placement gates inference, tool, or both. **Owner = a follow-on R-FS-1 child arc** (the B3 "CP workflow-grammar arc" residual; cross-cutting — all gate sites). Load-bearing: **HIGH** — it is what makes the ENTIRE wrap-time HITL gate (inference/sub-agent/tool) fire in production; without it the major B3 + `B-TOOL-GATE` machinery is wired-but-production-dead (axis composes correctly, gate never triggers on the real path). Rec: **BUILD** (CP workflow-grammar + runtime binding arc; spec'd-ness-gated per above). On landing, all three wrap-time gate sites fire through the real manifest→driver path and `B-TOOL-GATE`'s MCP-trust floor becomes non-vacuous in production. **✅ BUILT (this arc, 2026-06-18, the NINTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.60 → v1.61 §14.8.2-step-1 reconciliation + CP spec v1.40 → v1.41 NEW §25.3 `StepExecutionContext.hitl_placements` field + `harness-cp/src` + `harness-runtime/src` + by-execution tests + fork doc + 2 clearance markers), NO operator gate.** **Spec'd-ness resolved at arc-open (advisor-decomposed, 2 rounds):** the FOLD semantics (a workflow placement applies to all matching step kinds; the composer filters by `applicable_placements`) + the PRE_ACTION → {inference, tool} coupling are **SPEC'd** at runtime §14.8.1 (wrap-asymmetry footnote "overlays per `WorkflowManifestEntry.hitl_placements`") + §14.8.4 → impl-to-cleared-spec, NO fork-halt. The one under-specified leg = the read-surface/producer (§14.8.2 step-1 named `step.hitl_placements`, structurally unpopulatable on the frozen 3-field `WorkflowStep`) → reconciled as a **faithful materialization** (the v1.9 "populated at workflow-binding time per U-CP-13" always referenced a producer). Mechanism = read surface **`StepExecutionContext.hitl_placements`** (Option B over (a) widening `WorkflowStep` [violates the §25.2 config/body split] and over carrying it on `StepEffectiveBinding` [whose `model_dump` feeds the §16.5.4 override outcome-hash — a hash-coherence regression]); the CP driver sets `hitl_placements=manifest_entry.hitl_placements` at all **5** per-step `StepExecutionContext` construction sites (linear + the 4 non-linear roots; branch children inherit via `compose_branch_child_context`'s `model_copy`) — the `tenant_id`/`parent_gate_level` binding-fix precedent; the composer reads `step_context.hitl_placements` (`getattr(step,...)` preserved as a test-proxy compat fallback only). **Import cycle broken** by moving `hitl_placement`'s annotation-only `workflow_driver_types` import under `TYPE_CHECKING` (the `hitl_gate` signature is string-annotated via `from __future__ import annotations`). **NO operator gate** — additive + opt-in (default `()` → byte-identical, gate fires only on a declared placement), no committed-invariant sacrifice, no new contract/fail-class/manifest field. **Non-vacuity is the deliverable (cf. B-TOOL-GATE #653 wired-but-production-dead):** proven by-execution with NO proxy — a PLAIN `WorkflowStep` + a `step_context` carrying a PRE_ACTION placement → the composer fires the gate (surface invoked); a NEGATIVE CONTROL (empty `step_context` placements → no surface call, no HITL spans, no ledger/audit write); the producer proven through the real `execute_workflow` on the linear path (direct construction) AND DECENTRALIZED_HANDOFF (inherited via `compose_branch_child_context`). **Scope — wrap-time gates only:** VALIDATOR_ESCALATION fires via the §14.15 mid-step re-entry path on validator outcome (`next_action == ESCALATE_HITL`), NOT a step-read placement, + its gate dispatch is a separate future arc → OUT of scope. **One follow-on registered (NOT silent-deferred):** the per-step `StepOverride.hitl_placement` override fold (silent at C-CP-06 §6.2, which specifies only `f3_invocation` overrides) → `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD` (below). Decorrelated review: advisor (2 rounds — fork-vs-impl decomposition forced the §14.8.x read that resolved it to impl-to-cleared-spec; the no-gate + scope-split determination) + out-of-family Codex (pre-merge). Fork: `.harness/class_1_fork_b_hitl_placement_per_step_producer.md`; clearances `Spec_Harness_Runtime-v1_61-cleared-2026-06-18.md` + `Spec_Control_Plane-v1_41-cleared-2026-06-18.md`. `[[cleared-spec-resolves-it-before-first-principles-fix]]` · `[[spec-prose-plan-body-drift-pattern]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[test-bypass-as-runtime-truth-pattern]]`.

- **B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD — ✅ BUILT 2026-06-21 (impl-to-cleared-spec; CP spec v1.48 → v1.49 §6.2; NO operator gate; PR #699).** compose the per-step `StepOverride.hitl_placement` override with the workflow-level placement tuple (surfaced by `B-HITL-PLACEMENT-PER-STEP-PRODUCER`, 2026-06-18; advisor-confirmed scope split; UNTRACKED → forward arc). The producer arc surfaces the WORKFLOW-level `WorkflowManifestEntry.hitl_placements` (a tuple, applies to all cells per §17.1) onto every step's `StepExecutionContext`. But the manifest ALSO carries a per-step override surface: `StepOverride.hitl_placement: HITLPlacement | None` (singular), resolved into `StepEffectiveBinding.hitl_placement` by `resolve_step_binding` (override-only — it does NOT fold the workflow tuple). That resolved per-step override placement is currently **DEAD** (not consumed by the composer — placements are read off `step_context`, not `binding`). **The build:** decide + implement how the singular per-step override composes with the workflow tuple for a step — ADD (the override supplements the workflow placements), REPLACE (the override's placements supersede), or by-position-override (the override replaces the workflow placement of the same `position`, adds otherwise) — then thread the effective per-step placement set onto `step_context` for the overridden step. **Spec'd-ness:** the fold semantics are **SILENT at C-CP-06 §6.2** (which specifies only the `f3_invocation` per-step overrides — durability/checkpoint/replay; the `StepOverride.hitl_placement` field is an impl addition with no §6.2 prose backing) → **design-fork-first per X-AL-3** (a CP `design-substrate/` amendment specifying the fold + a clearance marker). **Owner = a follow-on R-FS-1 child arc.** Load-bearing: **LOW-MEDIUM** — workflow-level placements (the producer arc, now BUILT) cover the common case; per-step override of placements is a refinement (pipeline-automation per-stage HITL customization). Rec: **BUILD** (CP workflow-grammar arc; design-fork-first per X-AL-3 — the fold semantics are unspec'd). On landing, the dead `StepEffectiveBinding.hitl_placement` resolution becomes load-bearing + per-step HITL placement override works. **AS-BUILT (the "fork-first" label flipped to BUILD-not-fork after the gated probe — `[[disposition-label-is-a-claim-verify-against-spec]]`):** the §6.2 fold is **NOT** the operator's add-vs-replace call the registration feared — it is **ADD-only**, impl-to-cleared-spec, NO operator gate. The reasoning + the advisor's pre-push decorrelated catch: (1) the singular `HITLPlacement | None` type **forecloses position-level set-removal** (illegal-states-unrepresentable, root §4) — a singular field cannot represent a tuple-replacement, so a per-step *removal* is unexpressible; (2) **BUT the singular type does NOT foreclose attribute-level loosening** — `HITLPlacement` carries `tool_filter` ("limits which tools trigger the gate") / `cascade_policy` / `timeout`, so REPLACING a same-`position` placement could loosen the §17.1 floor while the position stays present (e.g. a `tool_filter` narrowing leaves other tools ungated). The first impl's "union-by-position, tune-in-place" REPLACE was a SILENT loosening shipped under "no gate" (X-AL-3 silent-absorption — caught pre-push). (3) **ADD-only is the genuinely-monotone fix** (the placement-set analogue of the §19.1 `max()`/floor-only posture; `per_tool`/`mcp_trust` "NEVER overridden" U-CP-91; v1.35 floor-only): an override ADDs an absent position; on a same-`position` collision the **workflow placement WINS** (the override is a no-op) — it can neither remove a position NOR weaken an existing one. As-built = **ADD-only, workflow-wins-on-collision, monotone** (`fold_step_hitl_placements` helper + `_per_step_hitl_placement_override` read helper, the v1.38 `agent_role` composition-time-fold precedent, wired at all 6 topology composition sites keyed from `manifest_entry.hitl_placements` so an override on one cell never leaks to a sibling/parent). (The "no §6.2 prose backing" framing was imprecise — the *field* has §6.2 backing since v1.6/v1.17; only the *fold* was silent, now spec'd at v1.49.) by-execution witnesses: helper unit (ADD-new; same-position-collision-is-a-no-op with a narrowing `tool_filter`+short-`timeout` override IGNORED; none); linear (ADD-union + same-position-noop + step-scoped); fan-out anti-leak (worker-override-no-sibling-leak + orchestrator-override-no-worker-leak). The dead `StepEffectiveBinding.hitl_placement` resolution is now load-bearing. ALL per-step tune/replace (loosening-capable) → the operator-gated `B-HITL-PLACEMENT-PER-STEP-LOOSEN` arc below.
  - **B-HITL-PLACEMENT-PER-STEP-LOOSEN — per-step TUNE/REPLACE or REMOVAL of a workflow-level HITL gate position (registered B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD follow-on, 2026-06-21; advisor-confirmed; was OPERATOR-GATED → AUTHORIZED 2026-06-22).** The v1.49 fold is **ADD-only / monotone by construction** — a per-step override can ADD an absent gate position but on a same-`position` collision the workflow placement WINS (no-op): it can neither **remove** a workflow position NOR **modify** an existing one. Both directions can reduce coverage: removal drops a position; a tune/replace can loosen at the attribute level (`HITLPlacement.tool_filter` narrowing leaves other tools ungated; a weaker `cascade_policy` / shorter `timeout`). Should an operator want either, that is **two** committed changes: (a) relax the monotone HITL posture (a committed-invariant relaxation — the v1.38 `agent_role` precedent shows a committed-invariant relax is the operator's call even when the mechanism is clean), AND (b) a typed-shape change (a tuple-valued, replace/removal-capable per-step placement carrier — e.g. an explicit `removed_positions` set or a `replace`-mode discriminator). Load-bearing: **LOW** (narrowing a safety gate is the rare, deliberately-friction-ful direction). Rec: **register, do NOT build** — surface the ADR/spec relax-request to the operator if/when a real workflow needs per-step gate tune/narrowing. **RE-CONFIRMED OPERATOR-GATED (PR #705, advisor-checked `[[disposition-label-is-a-claim-verify-against-spec]]`):** body-read of `fold_step_hitl_placements` confirms every coverage-reducing operation (tune/replace/removal) loosens the §17.1 floor by construction → ALL need the monotone-HITL invariant relaxation → there is **no monotone sub-slice OF LOOSEN** buildable without the operator gate. The label holds. **Cardinality-1 candidate noted (NOT a slice of LOOSEN, NOT building):** a tuple-valued **ADD-only** per-step placement carrier (a step ADDing MULTIPLE gate positions — currently the singular `StepOverride.hitl_placement` caps at one; 3 positions exist: PRE_ACTION / SUB_AGENT_BOUNDARY / VALIDATOR_ESCALATION) would be monotone (strictly tightens) and gate-free — BUT it is a DIFFERENT, unspecified capability (LOOSEN *reduces* coverage; ADD-multi *increases* it; they share only a carrier shape), `StepOverride` is uniformly singular across all 5 dimensions by design with NO spec text calling for multiple placements, and there is NO concrete demand. Building it = over-excavation (FULL-SPEC = "build what the spec DEFERS," not "build every expressible monotone extension" — the unbounded slop trap; advisor 2026-06-22). Register as a cardinality-1 candidate; promote to a forward arc only at a second instance or a real workflow need. **➤ AUTHORIZED 2026-06-22 — supersedes the gate above:** the operator DECLINED "declare build-complete" and AUTHORIZED building BOTH final arcs for genuine full-spec closure → LOOSEN (the monotone-HITL relaxation, NOT the ADD-multi candidate — that stays a cardinality-1 note) is CLEARED to build via the design-first pipeline (council [C10 action-safety ⊥ C11 operator-loop] → ratify the safe mechanism [opt-in, removal/replace-capable carrier, gate-removal AUDITED, non-removable mcp_trust/per_tool floors, monotone default preserved] → §17.1/§19.1 spec amendment + clearance → impl with both-branch witnesses → adversarial review) per `.harness/r-fs-1-final-closure-plan.md`. **Sequenced FIRST** (smaller CP-contained surface; establishes the amendment rhythm). **➤ ✅ BUILT 2026-06-22 (CP spec v1.52→v1.53 §6.1/§6.2/§6.6/§17.1/§19.1; bundled-absorption; PR #713).** As-built = an opt-in per-step REMOVAL of a `SUB_AGENT_BOUNDARY` gate, NARROWER than the registration's "tune/replace/removal of any position": NEW `LoosenablePlacementKind` **one-member enum** (`SUB_AGENT_BOUNDARY` only — `PRE_ACTION` structurally excluded = the §19.1 floor-evaluation **bypass-seam**, the §19.5 `HITLAutoApprovePolicy` is the safe PRE_ACTION loosening; `VALIDATOR_ESCALATION` = §14.15-path **wrong-layer** — both unrepresentable in the type, not a runtime guard) + additive `StepOverride`/`StepEffectiveBinding.removed_placements` + `resolve_step_binding` propagation. **Remove-not-replace** (a `tool_filter`-narrowing replace silently ungates other tools — the v1.49 footgun, foreclosed). The mechanism is **NOT a fold mutation** (the v1.49 `fold_step_hitl_placements` stays ADD-only — a destructive pre-composer deletion would prevent the floor-clamp); it is a **binding-carried directive honoured at the SUB_AGENT_BOUNDARY composer** (`c_rt_17`), mirroring §19.5's floor-cell lowering at `c_rt_16`. **FLOOR-CLAMP load-bearing** (the decline-mirror): overrides ONLY the persona human-oversight floor (+ the LOCAL_MUTATION blast cell per the ratified `{read-only, local-mutation}` scope); `per_tool`/`mcp_trust` NEVER override-able (U-CP-91) + blast above local-mutation clamp it → a high-blast / deny-tier-tool / untrusted-MCP dispatch **REFUSES** the removal (gate fires). **Solo-only** (team = registered follow-on, multi-tenant structurally foreclosed). **Auto-audited fail-closed** (`response="placement-removed"`, distinct from §19.5 `"approve"`). **Residual: PREVENTIVE→DETECTIVE** oversight for a removed cell — operator consciously accepted at ratification ("Ratify as designed" AUQ 2026-06-22). Default empty ⇒ **byte-identical + monotone**. Provenance rides `binding.model_dump` (§6.6, v1.38 `agent_role` precedent) — **NO new §5.2/IS hash**. Witnesses by-execution (harness-cp 1175 passed/1xfail; harness-runtime non-e2e 2050 passed; pyright 0/0/0; ruff clean): **NON-VACUITY** (advisor's blocking check — §19.5 persona-floor knob OFF → per-step removal skips THIS step's gate while an unremoved sibling gates, the genuine delta over §19.5's global policy); floor-clamp refusal at EXTERNAL_* blast + untrusted MCP (L1) + deny-tier `per_tool`; LOCAL_MUTATION effective; non-solo no-op; fail-closed on unresolvable floor; `placement-removed` audit shape; carrier propagation (CP) + composer consumption (runtime) witnessed SEPARATELY + `model_dump` provenance (the driver seam shares the `agent_role`/`prompt_version_sha` composition wiring — precedent + inspection, NOT a single driver run); negative control; CI pytest all-axes green. C10⊥C11 dyadic council (#709) + advisor (architecture vet + non-vacuity/LOCAL_MUTATION/provenance/fail-closed/audit-honesty + seam-honesty) + operator AUQ "Ratify as designed" 2026-06-22 + out-of-family Codex (2 rounds, [P2]×3 + [P1] reconciled). Clearance `.harness/clearance/Spec_Control_Plane-v1_53-cleared-2026-06-22.md`. `[[enforce-floor-no-bypass-seam]]` · `[[gate-enforcement-site-and-timing-asymmetry]]`. `[[disposition-label-is-a-claim-verify-against-spec]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`.

- **B-L2-EMBEDDING-ACTIVATION — Layer-2 EMBEDDING production activation + fastembed dependency promotion (surfaced by R-FS-1 arc R / L2-impl, 2026-06-16; advisor pre-build #1/#4 + the onnxruntime x86-mac wall; UNTRACKED → forward arc).** L2 is **BUILT** (Option B — the CP-pure k-NN classifier + the light fastembed realization + corpus + the `RuntimeLLMDispatcher.embedding_classifier` injection seam; CP units + a real-fastembed live e2e green). But it is **production-inert at HEAD by design**, for the SAME reason as the L3 router: `_declarative_echo` (`llm_dispatch.py`) **always** resolves DECLARATIVE, so `route()` short-circuits before EMBEDDING — the bound classifier is never reached. Production L2 activation depends on three registered-forward things, none silently assumed: **(1) the routing-activation gate** (make DECLARATIVE conditional so traffic reaches the EMBEDDING layer — the SAME gate L3 shares, per `B-LAYER-BUDGET-OVERRIDE` sub-bullet (1); UNOWNED — both R-300 items RESOLVED without it); **(2) the production factory wires `embedding_classifier=`** (today only a TEST constructs it; the `stage_5_loop_init` factory leaves it None, exactly like `router=None`) from an operator-configured corpus + the light embedding; **(3) promote the optional `[embedding]` extra to a REQUIRED dependency** of `harness-runtime` at that activation (today fastembed is an OPTIONAL extra so the base install stays light + provider-free lanes never pull onnxruntime — correct while L2 is dormant; but the operator chose Option B for self-sufficiency, so when L2 routes real traffic the embedding lib must ship by default). **A contained dep note (NOT a separate arc):** `onnxruntime` is capped `<1.24` in the extra because 1.24+ dropped the **x86-64 macOS** wheel (the operator's current Intel Mac; last x86-mac cp312 wheel = 1.23.2). Revisit/drop the cap when the operator moves to Apple Silicon (`[[operator-dev-machine-and-dep-preference]]`). **Owner = UNOWNED** — R-300-second-provider (the nominal owner once named here) is RESOLVED *without* doing the DECLARATIVE-conditional work (it added credentials + cross-family fallback only; routing-activation #213 kept DECLARATIVE behavior-preserving by design). This shared L2/L3 production-activation gate therefore needs a NEW owner once a second production provider makes routing-among-candidates meaningful; (2)+(3) ride it. Surfaced visibility-only at `.harness/roadmap_status.md` “## Remaining forward work”. Load-bearing: **MEDIUM** — the L2 capability is built + proven, but routes nothing in production until R-300; no current deployment supplies the second provider that makes routing-among-candidates meaningful. Rec: **(1)/(2)/(3) fold into the (currently UNOWNED) routing-activation gate** (R-300-second-provider, its former nominal owner, is RESOLVED without it) (no design-fork — L2-sync needed none; pure impl + a dep-required bump). On landing, EMBEDDING becomes a live cheapest-deterministic routing layer between DECLARATIVE and LLM_AS_ROUTER. **▶ DISPOSITION RESOLVED (2026-06-19, advisor decision-rule): BUILD opt-in/default-off (the routing-activation gate is the LAST buildable standalone arc).** Re-grounding corrected two framings in the entry above: **(a) the GOVERNING section is C-CP-02 §2.2, NOT §2.5.6** — §2.5.6 governs the L2 CONTRACT SHAPE (sync-vs-async, already spent by Option B); §2.2's "When it resolves" table CLEARS the activation condition: *DECLARATIVE (manifest) resolves IFF "the manifest binds the (agent_role, workflow_class, step) tuple"; EMBEDDING resolves when "the manifest does NOT bind AND classifier confidence exceeds threshold"; L3 when all fall through.* So the always-resolve `_declarative_echo` is the **MVP behavior-preserving shortcut**, NOT the §2.2 contract — making it decline on a manifest-miss is **impl-to-cleared-spec**, NOT design-fork-first (the prior "no design-fork" was right for the wrong reason; the OPT-IN mechanism + precise decline-semantics still warrant a small CP spec leg, like B-LAYER-BUDGET-OVERRIDE's §2.5.3). **(b) #213 is an UNBUILT MVP GAP, not a ratified-don't-build** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`: classify the decision, not the ledger paraphrase) — the forward register frames #213 as "routing activation RESOLVED; the multi-provider EXERCISE is B-2 | impl + operator creds"; #213 deferred the live EXERCISE (creds-gated), §2.2 specs the MECHANISM. **The build (advisor decision-rule: §2.2-spec'd ∧ #213-is-MVP-gap ∧ non-dormant-stub-witness-constructible → BUILD opt-in/default-off):** (1) `RuntimeConfig.routing_activation: bool = False` (both env loaders per `[[runtimeconfig-scalar-needs-both-env-loaders]]`); (2) flag-on `_declarative_echo` declines (returns `None`) when the manifest does not bind the tuple → `route()` falls through to the (built) EMBEDDING classifier; default-off ⇒ always-resolve, byte-identical to #213 → **no reversal-gate, zero blast radius on existing deployments** (highest-blast-radius arc — default-off is NON-NEGOTIABLE per advisor); (3) factory-wire `embedding_classifier=` when flag-on. **Non-vacuity = a non-dormant 2-candidate STUB witness** (flag-on + a manifest-miss request + a 2-candidate stub corpus → EMBEDDING routes to the non-default model) — advisor's distinction: the WITNESS needs 2 candidates (stubs suffice, NO real 2nd provider), PRODUCTION needs a 2nd provider (a DEPLOYMENT gate, not a build gate). **The dep-promotion (3 in the entry above) STAYS DEFERRED** — the `[embedding]` extra stays optional (correct while the base install stays light + provider-free lanes never pull onnxruntime); the operator installs it at deployment. **Genuine gate (driven-to, NOT fired):** operator creds for the LIVE multi-provider exercise (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`) — capability-built + production-dormant, the B-LAYER-BUDGET-OVERRIDE pattern (which shares THIS gate for its L3 firing). Advisor mandated: weight Codex/advisor heavily on the activation MECHANISM regardless. **✅ BUILT (this arc, 2026-06-19) — the LAST buildable standalone `B-*` arc. impl-to-cleared-spec (the #653 pattern: NO design-substrate edit, NO clearance marker, build-record `.harness/class_2_fork_b_l2_embedding_activation_routing_gate.md` only — §2.2 ALREADY specs the activation condition; making `_declarative_echo` §2.2-faithful is catch-up, not amendment; advisor confirmed no spec leg owed). `harness-runtime/src` (the `RuntimeConfig.routing_activation` opt-in flag env-keyed in both loaders + the §2.2-conditional `_declarative_echo` declining on a manifest-miss [`agent_role ∈ per_role_bindings` OR `workload_class ∈ per_workload_overrides`] + the dispatcher `routing_activation` field + the factory classifier-build [fail-loud on a missing `[embedding]` extra] + stage-5 threading) + by-execution tests. **NO operator gate** — §2.2 is cleared, default-off preserves #213 verbatim (zero blast radius). **Non-vacuity (the advisor's load-bearing catch — the #653/#657 overclaim trap):** the non-e2e witnesses inject a STUB; the REAL factory build (`make_embedding_classifier(make_fastembed_embedding(), default_routing_corpus())`) was RUN (free-local `[embedding]` extra installed) — it composes + routes 'extract the totals…' → `ollama:llama3.2:3b`, pinned as an e2e (`test_l2_factory_builds_real_classifier_when_routing_activation_on`, self-skips without the extra). **Production-dormant** behind the genuine gate: a second configured production provider (operator creds — driven-to, NOT fired) makes routing-among-candidates meaningful; the `[embedding]` extra stays optional (the dep-promotion is the deferred deployment step). advisor (2 rounds) + Codex pre-merge. ⮕ With this arc, the **originally-enumerated** standalone `B-*` frontier is BUILT — but it SPAWNED 2 new BUILD arcs (`B-ROUTING-MANIFEST-MODEL-FOLD` + `B-L2-FALLBACK-COMPOSITION`), so under FULL-SPEC the register GREW, not emptied; routing_activation is NOT production-reachable until `B-L2-FALLBACK-COMPOSITION` + a second provider. **Whether `R-CL-Q1` admits NOW is an OPERATOR scoping call (a large operator-facing gate — starting the whole-harness quality track), NOT an automatic consequence — surface via AskUserQuestion, do NOT auto-route into the quality track.**
  - **B-ROUTING-MANIFEST-MODEL-FOLD — fold the routing-manifest per-workload / per-role MODEL bindings into the dispatched model (registered 2026-06-19; surfaced by B-L2-EMBEDDING-ACTIVATION Codex [P2] ×2, advisor-reconciled; `[[spine-ledger-forward-arc-registration]]`).** Under `routing_activation`, a DECLARATIVE hit on a model-bearing `per_workload_overrides[w].model_binding_override` (or a `per_role_bindings[role]`) currently echoes the DEFAULT `binding.model_binding`, NOT the manifest override's model — status-quo (those routing-manifest models are unconsumed in dispatch today; the per-role model is folded at the C-RT-16 wrapper, the per-workload model nowhere) so NO regression, but the §2.2 + W-2 contract's intent is the override's model should dispatch. **Why deferred (the type-smell, CLAUDE.md §4):** folding requires a per-dispatch precedence resolve (per-step > per-workload > per-role > default) that the dispatcher CAN'T do cleanly because `StepEffectiveBinding` lacks a **binding-source discriminator** (`override_applied` is coarse — model vs prompt vs engine; `model_binding` conflates override/per-role/default) — the real fix is a CP-side source-tagged binding, a bigger cross-axis arc. Load-bearing: MEDIUM (composes with B-L2 production activation + the second-provider gate). Rec: BUILD (design-fork-first per X-AL-3 — the `StepEffectiveBinding` source discriminator + the per-dispatch fold).** **✅ BUILT 2026-06-22 AS `B-MODEL-RESOLUTION-CONSOLIDATION` (RESCOPED from the per-workload sliver to the whole model-resolution-precedence fix; operator chose "consolidation" via AskUserQuestion over the sliver; PR #701; fork `.harness/class_1_fork_model_resolution_consolidation.md`).** The grounding sharpened the picture: the model was resolvable at FIVE sites but only TWO were consumed (per-role + default; routed is the third) — BOTH per-step (`StepOverride.model_binding`) AND per-workload (`per_workload_overrides[w].model_binding_override`) MODEL overrides were SILENTLY DROPPED (the C-RT-16 wrapper never read `binding.model_binding` and had no `workload_class`). **The "type-smell deferred" the registration feared is the FIX, not a blocker:** the missing `StepEffectiveBinding` source-discriminator IS the added per-step MODEL `None`-or-override SIGNAL `model_binding_override` (the v1.37/v1.38 precedent; `model_binding` stays concrete for back-compat). As-built = a SINGLE precedence authority at the wrapper `_effective_chain(binding, agent_role, *, routed)` resolving the operator-ratified **per-step > per-workload > per-role > routed > default** (each non-default winner through the existing `_augment_primary`), + an explicit wrapper `workload_class` field (stage-5-bound), + the **§14.6.2 decline-mirror invariant** (advisor's load-bearing catch): `resolve_routed_binding`'s decline predicate is aligned to MIRROR `_effective_chain` EXACTLY (never STRICTER, else a model that should route drops to default) — per-step `model_binding_override is not None` (was coarse `override_applied`, which over-declined a non-model per-step override), per-role default-role exclusion, per-workload `or _MVP_DEFAULT_WORKLOAD_CLASS` mirror. **Operator-ratified precedence (the one genuine gate, ANSWERED 2026-06-22); otherwise additive CP signal / runtime-internal — NO new contract/fail-class/§5.2-hash/Protocol-widening/CXA edge.** Per-step + per-workload MODEL tiers are LIVE present-tense (single provider, routing OFF); routed tier production-dormant until a 2nd provider. Bundled-absorption: CP spec v1.49 → v1.50 (the signal + §6.6 provenance) + runtime spec v1.70 → v1.71 (§14.6.2) + 2 clearance markers + `harness-cp/src` + `harness-runtime/src` + by-execution tests (full-chain wrapper witnesses: per-step dispatched / per-workload routing-off / default-workload-class mirror / 5 precedence pairs / negative control; inner-decline alignment; CP signal applies/none/provenance). pyright 0/0/0; harness-cp 1165+1xfail / harness-runtime non-e2e 2025 / harness-od 950 / harness-cxa 28 / harness-is 171 (cross-axis ripple clean). advisor (the multi-site seam + the decline-mirror/workload-None/default-role asymmetry catches) + out-of-family Codex. `[[disposition-label-is-a-claim-verify-against-spec]]` (the "design-fork-first" label flipped to operator-ratified-precedence + additive-signal after grounding) · `[[full-chain-witness-not-half-proofs]]` (the inner-decline tests are half-proofs; the wrapper consumption is witnessed end-to-end).
  - **B-L2-FALLBACK-COMPOSITION — compose L2/L3 routing-activation with the C-RT-16 fallback chain (registered 2026-06-19; surfaced by B-L2-EMBEDDING-ACTIVATION Codex [P2] round-2; `[[cleared-spec-resolves-it-before-first-principles-fix]]` composition-tell).** Under `routing_activation`, `route()`/EMBEDDING runs at the INNER C-RT-15 dispatcher, which the C-RT-16 `RetryBreakerFallbackDispatcher` WRAPS + re-invokes per fallback candidate (rebinding `binding.model_binding`). So on a fallback re-invocation the inner re-routes EMBEDDING + re-picks the SAME candidate, IGNORING the wrapper's rebound fallback candidate → the chain never advances (a silent fallback-defeat). This is the exact composition-tell — a feature-X read placed at a layer feature-Y re-invokes per iteration silently overrides Y's variation (the U-RT-114 precedent: MODEL selection lives at the wrapper as its PRIMARY candidate so it composes with fallback). **Interim (this arc):** a detect-then-refuse factory guard fail-louds on `routing_activation` + non-empty `fallback_chains` (no silent defeat). **The fix:** route ONCE (the primary decision) → seed the wrapper's chain with the routed candidate → fallback varies from there (routing composes with the wrapper's candidate chain, NOT re-run per attempt). R-300 left this unresolved (routing was production-dormant); B-L2 activated routing + surfaced it. Load-bearing: MEDIUM (composes with B-L2 + the second-provider deployment gate; production-dormant until then). Rec: BUILD (design-fork-first per X-AL-3 — where routing composes with the fallback chain). **✅ BUILT (2026-06-20) — bundled-absorption: runtime spec §14.6.1 (v1.64 → v1.65) + clearance marker + `.harness/class_2_fork_b_l2_fallback_composition.md`.** The fix landed as route-once-then-fallback-the-chain (the U-RT-114 §14.5.3 chain-augmentation pattern applied to layered routing): (1) the route()+L3 SELECTION factored to `harness_cp.routing_core_surface.resolve_routing_trace` (no dispatch; `infer` reuses it, behavior-preserving); (2) a NEW `RuntimeLLMDispatcher.resolve_routed_binding` runs the layered routing ONCE and returns the routed `ModelBinding` (or `None` when routing_activation off / a deterministic binding governs); (3) the inner `dispatch` reverts to FAITHFUL (`_declarative_echo` pure echo); (4) stage-5 hands the C-RT-16 wrapper a DIRECT handle to `resolve_routed_binding` (NOT via the HITL `inner` — the advisor's load-bearing catch), and the wrapper seeds the routed model as its PRIMARY via the SAME `_augment_primary` U-RT-114 uses; (5) the interim detect-then-refuse guard is RETIRED. Precedence = deterministic binding > routing > stage chain (mutual-exclusivity, routing fills the no-binding gap). **NO operator gate** (additive correctness restoring §14.6's inner-is-faithful contract; opt-out byte-identical), **NO council** (the routing-home-vs-reliability-composition tension is foreclosed by the cleared §14.5.3/§14.6 invariants → probe-resolved; advisor + Codex). Non-vacuity witnessed as TWO separate properties: (a) the chain ADVANCES under routing (routed primary fails → next fallback dispatched, incl. a full-chain witness through the real provider) + (b) routing happens ONCE (call-counter spy across 3 dispatches). Scope-boundary follow-on registered: `B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION` (the inner span carries the DECLARATIVE-echo `routing.layer`, not the real EMBEDDING/L3 layer — production-dormant fidelity gap). routing_activation is now production-reachable with fallback chains, but still production-dormant behind the operator-creds second-provider gate.**
  - **B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION — ✅ BUILT 2026-06-21 (impl-to-cleared-spec; closes the §14.6.1 scope-boundary; NO operator gate; PR #693).** As-built = the §14.6.1-named **per-dispatch context** (NOT the Protocol-widening alternative): `resolve_routed_binding` now returns a `RoutedPrimaryResolution` (the routed `ModelBinding` it already returned + the resolving `RoutingDecisionTrace` + binding rationale it previously DISCARDED at the `trace.candidate.partition(":")` line); the C-RT-16 wrapper extracts `.model_binding` for chain composition UNCHANGED and — for the routed PRIMARY candidate ONLY — publishes `(routing_trace, binding_rationale)` on a module ContextVar `ROUTED_PRIMARY_SPAN_TRACE` around the inner dispatch; `_invoke_provider` reads it for `routing.layer`/`routing.binding_rationale`, else the local echo. **ContextVar** (per-task, fan-out-safe — the B-INTERSTEP-PERRUN-ISOLATION v1.64 precedent), NOT an instance attr (the dispatcher is shared across concurrent branches). **Scope-boundary held:** a chain-selected FALLBACK candidate keeps the faithful DECLARATIVE echo (`"manifest"`) — the ContextVar is cleared to `None` for it (witnessed: a failed routed primary → haiku span `"embedding"`, opus fallback span `"manifest"`). `routing.provider`/`routing.model` still reflect the actually-dispatched candidate. **CP spec UNCHANGED** (the resolver + `RoutedBindingResolver` Protocol + `RoutedPrimaryResolution` + the ContextVar are ALL `harness_runtime`-internal → no Protocol widening, no `harness_cp` change); **observability-additive** (selection/dispatch byte-unchanged), no new contract/fail-class, no §5.2-hash change → runtime spec v1.68 → v1.69 change-note + clearance marker. Capability is **production-dormant until a 2nd provider** makes routing-among-candidates meaningful — this is the observability-fidelity fix for that dormant-but-real capability. +3 by-execution witnesses (routed-primary span `"embedding"`; routing-off `"manifest"` negative control; failed-primary haiku `"embedding"` vs opus fallback `"manifest"`); 5 existing tests updated for the richer resolver return (own-the-now-false-assertions). pyright 0/0/0; harness-runtime non-e2e 2007 + harness-cp 1136 passed. advisor (reframed the fork from the read-shape [impl-discretion] to the consumer-existence question) + out-of-family Codex. (Was: registered 2026-06-20; Rec: BUILD.)

- **B-TAIL-CONDITIONAL-SAMPLING — full §9.2 conditional-row sampling enforcement for non-root + production-tail spans (surfaced by R-FS-1 arc B7 / B7-impl, 2026-06-17; advisor pre-build [blocking] + the ParentBased-root-only + attributes-post-creation re-grounding the B7 grounding sweep missed; UNTRACKED → forward arc).** B7 built the §9.2 attribute-conditional resolution as the OD SSOT (`sampling_mode.is_always_sampled(name, attributes)` + `_conditional_always_sampled`: `files.operation`/`memory.operation` mutation-`kind`, `validator.fail.*` `permanence=permanent`; conservative-absent) + wired it into the head sampler `HarnessCompositeSampler.should_sample`. But two structural facts (both re-grounded at HEAD) bound the head-sampler refinement to **near-nil production effect**: (1) **ParentBased root-only** — production wraps `ParentBased(root=HarnessCompositeSampler)`, which consults the inner sampler ONLY for ROOT spans (non-root spans inherit the parent decision and never reach `should_sample`); the live producers emit NON-ROOT spans (`memory.operation` at `memory_tool_dispatch.py:1535`-caller under the chat/dispatch/run tree; `files_operation_span` has **no** production caller — R-810/test-only), so the §9.2 set — current over-sample AND the B7 refinement — never executes for them; (2) **attributes-post-creation** — the producers set `*.kind` via `span.set_attribute` AFTER span creation (`files_api.py:241`, `memory_tool_dispatch.py:333`), so even a ROOT files/memory span's `kind` is invisible to the head sampler at `should_sample` time → conservative-always. The §9.2-conditional decision can therefore only bite where attributes ARE visible: at **trace completion** (tail-based sampling, the §9.1 production-time mandate). **Interim (landed at B7):** the SSOT predicate + head-sampler wiring ship correct + tested (the root-span + attribute-at-creation case); the stale `composite_sampler.py` over-sample / MVP-`1.0` docstrings are refreshed (closes the Bucket-C `composite_sampler.py:37` doc-hygiene item); the production enforcement boundary is documented in the module docstring (NOT a silent no-op). **No producer change** (inert — the spans never reach the head sampler). **The build:** (a) wire the `is_always_sampled(name, attributes)` SSOT into the **tail-keep span processor** (`harness-od/tail_keep_span_processor.py`, which reads `span.attributes` at root close) so the §9.2 conditional set is honored at trace completion for non-root + production-tail spans; (b) activate the tail-keep processor in the production provider (`compose_tracer_provider` attaches only a `BatchSpanProcessor`→OTLP today; the tail-keep processor is dormant). **GATED ON R-420/R-421** (the in-process OTLP collector + tail-keep activation; OD-6 `BOUNDED_RESIDUAL` — `flush_to_sqlite`/tail-keep dormant at MVP). **Owner = a follow-on R-FS-1 child arc within / after the R-420/R-421 collector arc** (likely impl-against-cleared-spec — §9.1 tail-based-prod + §9.2/§10.2 are cleared; confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: **MEDIUM** — it is what makes the §9.2 conditional-row refinement (and the §9.2 always-sampled set for non-root spans generally) actually bite in production; at HEAD the head sampler governs only root spans, and at solo cells `base_rate=1.0` makes even that moot. Rec: **BUILD** (OD tail-keep + runtime collector-wiring arc; gated on R-420/R-421). On landing, §9.2-conditional sampling bites for non-root + production-tail spans and the B7 SSOT predicate becomes load-bearing at the tail.

---

## Bucket A — Previously ratified-deferred → **RE-OPENED AS BUILD** (per the 2026-06-12 directive)

**These are no longer "legitimate bounded-residuals."** The standing directive re-opens every one as a build arc, on equal footing with Bucket B. Each carries a documented disposition history (kept below for context) + the external decision its build will reach (corpus / embedding-model / router-model — surfaced at the vendor boundary, not auto-fired). The "confirm-don't-rescue" framing in the original column is **superseded**.

| Item | Disposition | Tracked at |
|---|---|---|
| **Engine classes: 2 of 5** (`event-sourced-replay` / `reconciler-loop` / `WAL-segment` deferred) — the durable/recoverable execution engines | **Ratified-bounded.** = the engine-layer recovery residual. Operator **Gate A chose hand-roll** (I-6: no vendored engine) and **re-aimed to the workflow-layer** durable-resume (arc #3, #513/#514, BUILT). Engine-layer stays the `line-181`/CXA-2 residual. | `R-CL-P2`; `R-CXA-2-cp-is-seam` (§5 line 2225); `class_1_tension_u_rt_35…` line 181 |
| **LLM_AS_ROUTER** (Layer-3 routing intelligence) | Confirm-deferred (operator Option C, arc #6). Declarative routing covers the reach. | `R-CL-P1`; `class_2_fork_llm_as_router_layer3…` |
| **EMBEDDING** (Layer-2 similarity routing) | **BUILT** (R-FS-1 arc R / L2; operator **Option B**, 2026-06-16 — light in-process embedding). The deferral is **reversed** by the FULL-SPEC directive. Built: a CP-pure injected-`EmbeddingFn` k-NN classifier (`harness_cp.embedding_routing.make_embedding_classifier` + `EmbeddingRoutingCorpus`) + the light **fastembed** realization + default per-workload-class corpus (`harness_runtime.lifecycle.embedding_resolution`) + the `RuntimeLLMDispatcher.embedding_classifier` injection seam. Proven: stub-embed CP units + a real-fastembed live e2e (free/local). Production-inert until R-300 (same reachability as L3) — see `B-L2-EMBEDDING-ACTIVATION`. | `R-CL-P1`; `r-fs-1-r-routing-intelligence-design-v1.md` §3 (D2 L2-sync); `[[r-l2-option-b-light-embedding]]` |
| **Cost `RunResult.cost_attribution`** aggregate | Ratified-bounded (R-CL-P5 / arc #6). Per-dispatch cost is in the audit ledger; only the run-result rollup is empty. | `U-RT-49`; `fork_u_rt_49…` |
| **HITL OQ-6 timeout-degradation** producer | Confirm-deferred (producer-gated, arc #6) — the policy is built+pure; the wall-clock-wait orchestrator that fires it has zero production callers. *(Composes with B3 — the smart-HITL build would supply its producer.)* | arc #6 fork; capability inventory #11 |
| Memory-tool backend (live-proof) / OTLP local-ingestion residuals | `CP-16` / `OD-6` bounded-residuals (deployment-surface-gated). | post-Phase-8 register Tier B |

---

## Bucket C — Stale docstrings, NOT live boundaries (Q1 doc-hygiene, not build work)

Re-grounding found these read as MVP boundaries but are **superseded** — they are doc-rot, not open capability. Fold into the R-CL-Q1 doc-hygiene pass; do **not** list as open work.

| Stale comment | Reality at HEAD |
|---|---|
| `mutable_context.py:282` "v1.6 MVP binds 1 entry (SUB_AGENT_DISPATCH); INFERENCE_STEP [struck]" | **INFERENCE_STEP is live** — R-300 wired `infer()`; arc #5 ran live-Ollama INFERENCE_STEP across tiers; arc #4 made it inference-conditional (v1.47). |
| `composite_sampler.py:37` "MVP hardcodes base_rate=1.0; persona_tier not plumbed at the sampler binding site" | **Per-tier base-rate 1.0/0.1/0.2 proven live at the bootstrap** (arc #5, spied at `materialize_tracer_provider_stage`). *(2-min Q1 check: is the `composite_sampler.py` internal default a separate dormant layer, or simply a stale docstring? Either way per-tier sampling works.)* |
| `workflow_driver_types.py:193` "tenant_id… None = single-tenant (v1.6 MVP default)" | **Multi-tenant works** — the v1.6 hardcode was *lifted*; operator-supplied non-None tenant_ids flow through the 4-substep audit composition (R-500 + arc #5 ran MULTI_TENANT). `None` is just the single-tenant default. |
| roadmap §5 line 1078 "sandbox execution is metadata-only — no container/microVM/VM" | **Stale (2026-06-01 note)** — superseded by R-410 Docker / R-411 gVisor / R-412 E2B drivers + arc #1 tier→driver selection. (Residual is only B6 per-tool selection.) |

---

## Confidence notes

- **Code-side coverage: HIGH.** 47 code files referenced "MVP"; the load-bearing ones were re-grounded by direct read.
- **Spec-side coverage: MEDIUM.** The Explore spec sweep read the canonical *heads* (CP v1.31, OD v1.29 — which are delta files), not every full delta-chain body. A fuller spec pass could surface additional spec-declared MVP scopes; the code-side sweep is the stronger signal for *what is actually reduced at runtime*.
- **B1/B2/B3 are HIGH confidence** (direct code reads + roadmap-absence confirmed). B5/B6/B7 HIGH. B4 MEDIUM-HIGH.

---

## Program (under the 2026-06-12 directive — build-vs-ratify is SETTLED: build everything)

1. **Everything in Bucket A (re-opened) + Bucket B + the contract-completeness candidates is a build arc.** The vehicle is one ACTIVE umbrella **`R-FS-1`** with ordered child arcs (R-CC-1 shape); the old DEFERRED entries point at it, not each independently ACTIVE (keeps §4 clean). **R-CL-Q1→C1 move behind `R-FS-1`.**
2. **First arc = make scope provably complete (the scoping pass). ✅ DONE — `.harness/r-fs-1-arc-1-scoping-v1.md`.** Resolved the 8 `contract-w/o-cite` candidates per-contract (5 implemented-but-uncited/phantom → Q1 hygiene; **2 substantive build** → `C-CP-49/50` engine-layer emits fold into the engine-classes build; **1 thin arc** → `C-RT-28` managed_agents consumer is **already built + R-820 live-proven** (`managed_agents.py`; H_T-AS-8f SUBSTANTIVE_RETIRED) — only the *contract* was never authored AND the consumer is proof-script-only/unwired, so it re-sizes to a **thin contract-authoring + production-wiring arc, NOT a from-scratch managed-cloud build**; advisor-caught conflation of *contract*-unauthored vs *capability*-unbuilt) + the fuller spec-body pass (live-at-head filter: no net-new *major* arc beyond Bucket A/B; circuit-breaker found BUILT, SE single-step CLOSED, +2 minor field-level scopes — breaker config-tuning, idempotent MCP-host restart). Output: the **FROZEN ordered child-arc list** `B1→B3→E(engines)→B2→R(router/embedding)→B4→CA(cost)→B5→B6→B7→M(managed-cloud)` + minor field-level + Bucket C→Q1.
3. **Recommended build sequence** (capability-unlock first; design-fork-first per X-AL-3 where a spec extension is owed): **B1 topology orchestration** (the multi-agent / parallel / cross-comm payload + the biggest) → **B3 smart-HITL** (supplies the OQ-6 producer) → engine classes (hand-roll, I-6) → **B2 multi-server MCP** → LLM_AS_ROUTER + EMBEDDING (router-model / corpus surfaced at the vendor boundary) → **B4 per-role dispatch** → cost-aggregate → **B5/B6/B7** + minor field-level scopes. Each large item is research→design→spec→plan→implement.
4. **Bucket C** (stale docstrings) sweeps into R-CL-Q1 doc-hygiene — nothing to build there.
5. **Retained gates (not deferral — the directive's own "if a vendor must be sourced"):** EMBEDDING corpus + embedding-model; LLM_AS_ROUTER router-model. Design/spec/plan to the decision point, surface the one external input, do not auto-fire (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`).

**Open operator input (sequencing only, not whether-to-build):** confirm or re-order the §3 sequence. Default if silent: proceed as listed, starting with the §2 scoping pass.

**B2 execution-decomposition (registered 2026-06-16 — `[[spine-ledger-forward-arc-registration]]`; the split is NOT a defer).** B2 (multi-server MCP) is built as: B2-DESIGN ✅ (#579) → **B2-spec-1 ✅ reshape** (C-RT-04 `mcp_client_host`→`dict[ServerName, MCPClientHost]` + tool→server routing index + fail-loud collision + per-host sandbox + identity-by-ordinal `MCPServerTrustLevel→MCPTrustTier` *telemetry* projection; runtime v1.51 / CP v1.34 / AS v1.10; `.harness/class_1_fork_b2_multi_server_mcp_client_reshape.md`) → **B2-spec-2 ✅ gate-axis** (F2-02: CP spec v1.34 → **v1.35** NEW §19.1.2 commits `MCP_TRUST_GATE_LEVEL_FLOOR: dict[MCPTrustTier, GateLevel]` — resolves the v1.15 §19.1.1.1 row-3 spec-silence, the 4th/last unmaterialized §19.1 axis [`Axis.MCP_TRUST` already in the enum; 3-of-4 → 4-of-4 §19.1 axes]. **T-B2-2 probe-resolved** to floor-only/monotone — NOT council-convened: the gate is `max()`-over-escalation-rank so a floor can only RAISE it, and U-CP-91 `gate_level_rule.py:131-132` is a landed `mcp_trust`-NEVER-overridden commitment → loosening would reverse on-main + break the locked T-perm-1 composition [§10.9 probe-first; C10⊥C11 named inline, both converge floor-only]. Operator-ratified **Table A** [L0→DENY / L1→ASK / L2→ASK / L3→AUTO]. **SPEC-ONLY** — the `gate_level()` `per_axis_floors` addition (U-CP-98) + the composer **no-floor default** [U-RT-131 — replacing the harmful `hitl_gate_composer.py:462` L0 constant with the L3 AUTO-mapping default at the host-less gate sites; the real per-server resolved-host feed is the `B-TOOL-GATE` forward arc above, since the composer gates only inference/sub-agent steps today] land at B2-impl; runtime §14.8.2 step-4c already names the composer input so ZERO runtime/AS/ADR change. `.harness/class_1_fork_b2_spec_2_gate_axis_materialization.md`) → B2-plan → B2-impl (≥2 mock MCP servers + the `_trust_tier_from_level` stub retirement + the stale `mcp_client_host.py:128-130` docstring fix). **Split rationale:** the reshape is routine (sensible defaults + council-converged) while the gate-axis materializes a 4th axis of the LOCKED T-perm-1 composition + a live HITL-security decision → advisor-recommended ratification-boundary split (mirrors B1-spec→spec-1/spec-1b/spec-2 + B3-spec→spec-1/spec-2). Forward siblings: B2-restart (idempotent host restart, D5) + server-qualified tool addressing (D2).

**R execution-decomposition (registered 2026-06-16 — `[[spine-ledger-forward-arc-registration]]`).** The **R** sub-program (routing intelligence — the Bucket A **LLM_AS_ROUTER** + **EMBEDDING** rows above, re-opened as BUILD) is built as: R-DESIGN ✅ (#596 — D1 L3 Reading B [resolve the router call at the already-async `infer()`; Reading A foreclosed by ADD §5.3.3 + §2.2 "Probabilistic"], D2 L2 conditional) → **R-spec-1 ✅** (#598 — CP spec v1.36 NEW §2.5 the Layer-3 LLM_AS_ROUTER resolution surface: injected OPTIONAL `RouterResolutionFn`, timeout-bounded by the 200 ms L3 `LayerBudget`, rationale via the C-CP-01 §1.4 `routing.binding_rationale` span attr [the frozen 4-field `RoutingDecisionTrace` NOT widened]; `.harness/r-fs-1-r-routing-intelligence-design-v1.md`) → **R-plan-1 (this arc — pending merge; ✅ on merge)** — CP plan v2.37 (U-CP-99 contract types + the `ProviderDispatchFn` Protocol-ization; U-CP-100 the `infer()` Layer-3 branch) + runtime plan v2.48 (U-RT-132 rationale-consume + span; U-RT-133 router-injection binding, production `router=None` + the test-only `layer_decisions` seam) → **R-impl-1** (ALL 4 units co-land — the `ProviderDispatchFn` Protocol-ization is a cross-package TYPE-RIPPLE CO-LAND, verified by a pyright spike: legacy 4-arg dispatch closures go pyright-unassignable until they add the defaulted `binding_rationale` param; `[[shared-is-shape-change-ripples-cross-axis-field-asserts]]`) with a **MOCK** router (NO paid call) → **R-impl-2** (the real router model + prompt + a gated live e2e — **surface, do NOT auto-fire**; free-local Ollama preferred) → **R-L2-gate → R-spec-2/R-impl** (Layer 2 EMBEDDING — R-DESIGN D2, conditional on the gated embedding-model + corpus; CP spec v1.36 §2.5.6 silent on L2 by design). **NEW forward item (surfaced by R-plan-1; registered, NOT dropped):** the **router-call cost-bucket attribution** — CP spec v1.36 §2.5.4 commits the principle (the router dispatch is a real billable LLM call → its own span + cost attribution) but **defers** the precise cost-bucket discrimination (router-call cost to the routed step vs a distinct `routing:`/router-meta bucket) to the **CA** arc (the frozen `…→R→B4→CA→…` order) + R-impl; composes with the OD 5-step cost-attribution chain + the CXA cost-attribution audit-write seam. The CA arc owns the wiring (the Bucket A **Cost `RunResult.cost_attribution`** row + this router-call sub-item). **Split rationale:** R-DESIGN/spec/plan/impl mirrors the B1/B2/B3/E sub-program shape (design-fork-first per X-AL-3, each leg a clearance marker); the L3-then-L2 order + the vendor gates follow the directive's "design/spec/plan to the decision point, surface the one external input" (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`).

**B4 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B4** arc (Bucket B row above — per-role / per-step dispatch indexing; the per-role *model* half already landed at B1 arc #14 / U-RT-114, §14.5.3) is built as ordered slices: **Slice 1 (this arc) — per-role PROMPT threading** (a fan-out branch's `step_context.agent_role` selects + injects its per-role prompt `content` at the §14.5.2 translate seam; impl + the procedural-tier coherence sub-fork below) → Slice 2 — per-role binding **catalog** surface + validation (operator-authored `per_role_bindings` content for both routing + prompt manifests; `step_id→AgentRole` derivation) → **Slice 3 (this arc) — per-step PROMPT override on `StepOverride`** (CP spec v1.36 → **v1.37**: additive `StepOverride.prompt_version_sha` + `StepEffectiveBinding` propagation + NEW §6.6 provenance-scope; the §6.1 extension clause was confirmed at arc-open to cover ONLY `WorkflowManifestEntry`, not the `StepOverride` sub-record → a genuine CP §6.1 fork per v1.27 §2(d) explicit-extension discipline, mirror precedents v1.20/v1.22/v1.34; `.harness/class_1_fork_b4_per_step_prompt_override_stepoverride_extension.md` + clearance `Spec_Control_Plane-v1_37-cleared-2026-06-17.md`. **Surface = `StepOverride` not `PromptSelectionManifest`** [`step_id` is a workflow-scoped instance, not a run-level category — Route B is a Track-B reuse hazard]. **Provenance = the WIRED per-step override state-ledger entry** [`emit_override_state_ledger_entry` hashing `binding.model_dump` at `workflow_driver.py:1913-1930`; the NEW `StepEffectiveBinding.prompt_version_sha` is auto-captured], NOT the run-level §5.2 hash → **no IS-spec change** [HEAD re-ground overturned the stale v1.27 "no production caller" reading]. Runtime dispatch precedence per-step > per-role > default + fail-loud unauthored sha + binding-tier governance parity = impl-discretion per §14.5.3. Per-step **ROLE** is OUT of scope → Slice-4 gate per the §14.5.3 single-role-source invariant. Bundled-absorption arc; no operator gate; FULL-SPEC-pre-authorized) → **Slice 4 (this arc) — per-step + linear-path ROLE override** (CP spec v1.37 → **v1.38**: additive `StepOverride.agent_role` + `StepEffectiveBinding` propagation + §6.6 provenance extension; **paired with runtime spec v1.51 → v1.52** which **relaxes the §14.5.3 invariant-2 "single role source" + invariant-3 "linear path untouched"** — the committed-invariant sacrifice Slice 3 foreclosed). **Mechanism = Option B (composition-time fold), NOT Option A (second dispatch-read):** the CP driver folds `binding.agent_role` onto the single `StepExecutionContext.agent_role` source at all 6 topology composition sites (precedence per-step > fan-out-derived > default), so the **runtime dispatch read is byte-unchanged** (no two-authority-at-dispatch; zero `harness-runtime` impl edit) — structurally the `default_gate_level → parent_gate_level` precedent (v1.20). Provenance = the WIRED per-step override state-ledger entry (`binding.model_dump`), NOT the §5.2 hash → no IS-spec change. `.harness/class_1_fork_b4_per_step_role_override_stepoverride_extension.md` + clearance `Spec_Control_Plane-v1_38-cleared-2026-06-17.md` + `Spec_Harness_Runtime-v1_52-cleared-2026-06-17.md`. **OPERATOR-GATED** — unlike the additive Slice 3, the §14.5.3 invariant relaxation is a committed-invariant sacrifice → operator ratifies before merge (`[[feedback-gate-only-on-meaningful-architecture-change]]`; advisor caught + reversed a probe-resolves attempt to dissolve the gate — the decisive asymmetry: `default_gate_level` had no invariant forbidding it, role has the explicit §14.5.3 invariant 2). Bundled-absorption arc (CP v1.38 + runtime v1.52 + `harness-cp/src` impl + by-execution tests co-land). **This closes the B4 tail.** **Forward item surfaced by out-of-family Codex review (registered, NOT a Slice-4 deliverable — `[[spine-ledger-forward-arc-registration]]`):** the per-step **override-ledger provenance** (`emit_override_state_ledger_entry`, `override_applied`-gated) fires at the `SINGLE_THREADED_LINEAR` driver-thread site ONLY — a **PRE-EXISTING** gap (verified at pre-Slice-4 HEAD `eb85641`) across ALL non-linear topologies for ALL per-step override dimensions (model v1.6 / prompt v1.37 / role v1.38). Routing is correct on every topology (the fold works everywhere); only the dedicated per-step override-*audit-entry* is linear-only — the buffered-branch / concurrent-fan-out write boundary (ADR-F2 single-threaded-write) precludes a synchronous per-worker driver-thread entry. Closing it (emit the override entry through the buffered-branch path / on the driver thread) is a **separate audit-provenance arc** (`B-NONLINEAR-OVERRIDE-PROVENANCE`), scoped honestly in CP v1.38 §6.6. **Sub-fork surfaced + CLOSED in-arc at Slice 1 (NOT a defer):** per-role prompt injection created a procedural-tier hash-coherence gap (the C-IS-05 §5.2 recipe hashed only the resolved default-role `active_prompt_version.version_sha`, leaving per-role prompt-selection bindings hash-invisible — the §14.5.2 invariant violated for the per-role dimension). RESOLVED by adding a 4th recipe component `prompt_selection_manifest_sha` mirroring `routing_manifest_sha` (IS spec v1.8 → v1.9; fork `.harness/class_1_fork_b4_per_role_prompt_procedural_tier_hash_coherence.md`; clearance `.harness/clearance/Spec_Information_Substrate-v1_9-cleared-2026-06-17.md`). Bundled-absorption arc (IS §5.2 + runtime impl co-land). **Split rationale:** Slices 1–2 are impl-against-cleared-spec (§14.5.3 explicitly assigns per-role prompt to B4 + suggests fall-through-to-default), Slice 3 is the genuine ratification-boundary fork (a new field on a cleared `StepOverride` schema); the in-arc §5.2 coherence sub-fork is additive/forward-only/precedented → no operator gate, FULL-SPEC-pre-authorized.

**CA execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **CA** arc (cost aggregate — populate `RunResult.cost_attribution`, hard-coded `()` since landing behind the now-CLOSED U-OD-21 HALTED tension) is a thin-to-medium bundled-absorption arc: **Slice 0 — spec fork** (runtime spec v1.52 → **v1.53** §9 C-RT-09: axis = `RollupAxis.PER_PROVIDER_AND_MODEL` + type-name reconcile phantom `CostAttribution` → `tuple[CrossFamilyCostRollup, ...]`; `.harness/class_1_fork_ca_run_result_cost_aggregate_shape.md` + clearance `Spec_Harness_Runtime-v1_53-cleared-2026-06-17.md`; **additive/clarifying, NO operator gate** — sacrifices no committed invariant, unlike v1.52) → Slice 1 — run-scoped in-memory `cost_record_accumulator` Stage-0 `HarnessContext` field → Slice 2 — thread the same-reference sink into the 4 stage-4/5 dispatcher/hook materializations + append the returned `SpanCostRecord` in each best-effort wrapper → Slice 3 — `_build_run_result` rollup + stale-docstring refresh → Slice 4 — e2e (non-empty `cost_attribution`, correct per-(provider,model) sums) + unit + field-assert test updates. **Axis empirically forced** (advisor + HEAD body-grounding overturned the grounding-sweep lead): `PER_PROVIDER_DISCRIMINATOR` raises `CrossFamilyRollupError` on every production record (production helpers tag dispatch-type `"llm"`/`"tool"`/`"validator"`/`"webhook"`, none `CrossFamilyTag` members); `PER_PROVIDER_AND_MODEL` is safe + meaningful; single axis preserves the `sum(total_cost)` = true-run-cost invariant. Bundled-absorption arc (runtime v1.53 + `harness-runtime/src` impl + tests co-land). Slice 5 (fan-out 14.3 vs 15.1 double-counting) is NOT owed for this run-result aggregate (each dispatch contributes one record once; 14.3 `rollup_fanout_at_close` is a distinct dashboard surface).

**`B-COST-DISCRIMINATOR-TAXONOMY` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc CA, advisor-flagged).** A **latent Class-1** contract-vs-production coherence defect: production cost helpers write a **dispatch-type** `provider_discriminator` (`"llm"`/`"tool"`/`"validator"`/`"webhook"`) into `SpanCostRecord`, but the field's contract (`rollup_costs_by_axis` PER_PROVIDER_DISCRIMINATOR + the `SpanCostRecord.provider_discriminator` docstring) **validates against the `CrossFamilyTag` fallback-family vocabulary** `{frontier_managed, frontier_managed_alt, local_ollama}` → `CrossFamilyRollupError` if PER_PROVIDER_DISCRIMINATOR is ever run on production records. Dormant only because the sole current callers are OD unit tests with synthetic family-tag records (arc CA nearly tripped it; CA routes around it via PER_PROVIDER_AND_MODEL). The dispatch-type cost breakdown (llm-vs-tool-vs-validator-vs-webhook) is arguably the **most operator-meaningful** rollup and is exactly the one this defect blocks. The arc reconciles the dispatch-type tag taxonomy against the `CrossFamilyTag` fallback-family taxonomy (options at arc-open: extend `CrossFamilyTag`; add a dedicated dispatch-type `RollupAxis`; or have producers set real `CrossFamilyTag`s + carry the dispatch-type dimension separately) so a dispatch-type / family rollup becomes admissible — likely an OD-spec C-OD-15 §15.1 amendment. **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (OD spec v1.30 §15.1 + runtime spec v1.57 §9 + impl + tests + fork doc + 2 clearance markers), NO operator gate.** The decision: **Options B+C converge** — the spec authority (C-OD-15 §15.1, `Spec_Operational_Discipline_v1_2.md:870-878`) defines `provider_discriminator` as the **cross-family family tag** (a §15.3 chain-composition concept), so the production dispatch-type write was the **bug** (Option A — extend `CrossFamilyTag` — was REJECTED: it conflates two orthogonal dimensions + corrupts the §15.3 input). As-built: a NEW typed **`DispatchKind`** enum (`{LLM, TOOL, VALIDATOR, WEBHOOK}`, homed in the U-OD-20 carrier — enum-typed directly, no `str`+validate dance) + `SpanCostRecord.dispatch_kind` + **`RollupAxis.PER_DISPATCH_KIND`** carry the dispatch-type dimension; the 4 production helpers write `dispatch_kind=...` + `provider_discriminator=None` (a per-dispatch helper has **no** chain-level family context, so `provider_discriminator` becomes `str | None`, the `PER_PROVIDER_DISCRIMINATOR` axis **skips `None`** — the error→skip refinement, a bug-fix-toward-spec, no committed-invariant sacrifice → autonomous). Surfaced non-vacuously on a NEW optional **`RunResult.cost_attribution_by_dispatch_kind`** field (orthogonal single-axis partition of the same total as `cost_attribution`; minor bump, the v1.45 `pause_snapshot` precedent). advisor (full-transcript — affirmed the spec-reading as load-bearing + sharpened the forced provider_discriminator-for-non-LLM sub-decision) + Codex (out-of-family). Gates: pyright 0/0/0; ruff clean; harness-od 950 + harness-runtime 1873 (non-e2e) + CXA-P1 34; overlay 323 nodes 31/31; no §5.2-hash change (ephemeral run-OUTPUT). `[[cleared-spec-resolves-it-before-first-principles-fix]]` · `[[built-but-vacuous-reground-ledger-asis]]`.

**`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` (standalone forward arc, registered 2026-06-18 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B-COST-DISCRIMINATOR-TAXONOMY).** Populate `SpanCostRecord.provider_discriminator` with a real `CrossFamilyTag` at the §15.3 fallback-chain composition — the `FallbackChainCostComposition` seam + the CP→OD cross-axis edge (`cross_family_rollup.py:36` — C-CP-04 cross-family fallback chain) — which makes the `PER_PROVIDER_DISCRIMINATOR` cross-family rollup **non-vacuous in production** (today every production record is correctly `None`, as a per-dispatch helper has no chain-level family context; the axis stays defined + admissible, exercised by the synthetic-record tests). Per `[[r-cxa-seam-wiring-is-producer-discovery]]`, the §15.3 producer is the CP fallback path — a genuinely separate cross-axis arc; spec §15.3 already marks the dashboard query / population "deferred to implementation discretion." **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (runtime spec v1.57 → v1.58 §9 C-RT-09 + `harness-runtime/src` impl + by-execution tests + fork doc + clearance marker), NO operator gate.** The A/B "parent-record vs tag-the-record" fork was **probe-resolved as a span-layer-vs-record-layer false dichotomy** (advisor): §15.3 governs OTel **span** attributes; §15.1.2 governs the **`SpanCostRecord` field** — and §15.1.2 explicitly assigns *this* arc to **populate the existing dispatch record's field**, not emit a separate parent record (which collapses A/B + kills the double-count-in-`PER_PROVIDER_AND_MODEL` worry by construction). As-built: the LLM cost path (`_attribute_cost_best_effort` → `attribute_llm_dispatch_cost`, optional new `provider_discriminator` param) stamps each LLM `SpanCostRecord.provider_discriminator` with the dispatched provider's family via a NEW `harness_runtime/lifecycle/cross_family_cost_tag.py` map (`ANTHROPIC → frontier_managed`, `{OPENAI, GOOGLE} → frontier_managed_alt` per the §15.1 "OpenAI/Gemini" grouping, `LOCAL_OPEN_WEIGHT → local_ollama`; `CrossFamilyTag` NOT extended; map homed in runtime — `[[carrier-home-defect-pattern]]` — + it also owns the now-one-source-of-truth provider→family map that `retry_breaker_fallback` reuses). Surfaced on a NEW optional **`RunResult.cost_attribution_by_provider_discriminator`** field — making `PER_PROVIDER_DISCRIMINATOR` **non-vacuous in production**. **Correctness: this axis partitions the LLM SUBTOTAL, not the full run total** (non-LLM records carry `provider_discriminator=None` → skipped) — the by-execution tests assert `Σ == Σ(LLM records)` (NOT total-run) + that the other two cost axes are unchanged (no regression/double-count). **The `FallbackChainCostComposition` carrier is deliberately NOT wired** (it has no consumer → constructing it would be hollow; `[[r-cxa-seam-wiring-is-producer-discovery]]`) — non-vacuity is met by `provider_discriminator` + the rollup field alone. **NO operator gate** — impl-to-cleared-spec (OD v1.30 §15.1.2 already reserves the field for this arc → no OD spec change, runtime-only); additive, no committed-invariant sacrifice. Bundled doc-hygiene: v1.58 also reconciles the v1.57 §9-body-drift (`cost_attribution_by_dispatch_kind` was change-note-declared but never tabled in §9 — `[[spec-prose-plan-body-drift-pattern]]`). Verification: `test_b_fallback_chain_family_cost_composition.py` (map; LLM-subtotal rollup; the REAL cost path tags the record; a REAL `RetryBreakerFallbackDispatcher` cross-family fallback anthropic→openai → `frontier_managed_alt`). Gates: pyright 0/0/0; ruff; harness-runtime 1889 (non-e2e); overlay 324 nodes 31/31; substitution 54/54; CXA-P1. advisor (full-transcript — resolved the false dichotomy + flagged the LLM-subtotal invariant + the hollow-carrier trap) + Codex (out-of-family). Fork: `.harness/class_1_fork_b_fallback_chain_family_cost_composition.md`; clearance `Spec_Harness_Runtime-v1_58-cleared-2026-06-18.md`. `[[cleared-spec-resolves-it-before-first-principles-fix]]`.

**`B-NONLINEAR-OVERRIDE-PROVENANCE` (standalone forward arc, registered 2026-06-17 in the B4 Slice-4 entry above — `[[spine-ledger-forward-arc-registration]]`; surfaced by the B4-Slice-4 out-of-family Codex review).** The per-step override-ledger entry (`cp.per-step-override-application`, U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` driver-thread site **only** — the 5 non-linear strategies resolved the per-step binding (the override **took effect at dispatch** on every topology — routing correct everywhere) but emitted **no dedicated override-ledger entry**, for ALL dimensions (model v1.6 / prompt v1.37 / role v1.38). A PRE-EXISTING all-topologies audit-provenance gap, scoped honestly in CP spec v1.38 §6.6 (the buffered-branch / ADR-F2 single-threaded-write boundary precluded a synchronous per-worker write). **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (CP spec v1.39 → v1.40 §6.6 topology-scope-note refresh + `harness-cp/src` impl + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the **buffered-branch path** (CP v1.38 §6.6's own named option, the thread-model-agnostic one — advisor-decisive over driver-thread-direct, which would need per-site proof the write is on the single thread outside the drain capture-window): a new `append_branch_override_ledger_entry` buffers the override entry into the branch's `BufferingLedgerWriter`, serialized on the driver thread by `drain_branch_buffers` in branch-index order (C-CP-25 §25.12 D1.b — the §25.13 step/terminal branch-entry discipline). Wired into all 5 strategies via a shared `_buffer_branch_override_if_applied` guard at each branch-plan / sequential-dispatch site (HIERARCHICAL_DELEGATION inherits it — recursion re-enters `_execute_orchestrator_workers`). **Idempotency identity = the §16.5.4 per-`(step, outcome)` key, reused VERBATIM (NOT branch-scoped)** — an override is a static binding property, so a repeated `(step, outcome)` across EVALUATOR_OPTIMIZER iterations / recursion levels idempotently dedups at the IS writer to one entry (the spec's designed semantic; branch-scoping would change the cleared §16.5.4 formula → X-AL-3, foreclosed). Both write paths compose via the factored-out `compose_override_entry_payload` → the persisted entry is **byte-shape-identical** across all topologies. **NO operator gate** — impl-to-cleared-spec on the general §6.6 provenance contract + a `[[stale-carry-text-disposition]]` refresh of the now-falsified v1.38 honest-scope note; no committed-invariant sacrifice (contrast B4 Slice 4's §14.5.3 *relaxation* gate), additive behavior (`[[feedback-gate-only-on-meaningful-architecture-change]]`). Verification: `test_workflow_driver_nonlinear_override_provenance.py` (by-execution per topology into the REAL IS writer — incl. the EVALUATOR_OPTIMIZER repeated-step → exactly-one-entry dedup + a nested-recursion case + §6.3 chain re-verification); full harness-cp suite green (1055 passed; existing topology suites use no overrides → emission is a no-op). Decorrelated review: advisor (Design A; idempotency identity; §6.6 note disposition) + out-of-family Codex (at the impl-diff PR). `[[stale-carry-text-disposition]]` · `[[shared-is-shape-change-ripples-cross-axis-field-asserts]]`. Fork: `.harness/class_1_fork_b_nonlinear_override_provenance.md`; clearance `Spec_Control_Plane-v1_40-cleared-2026-06-18.md`.

**B5 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B5** arc (Bucket B row above — memory backend per-deployment-surface selection stubbed) is a thin single-bounded **impl-against-cleared-spec** arc (no fork; impl-discretion per runtime plan v2.14 U-RT-80 AC + spec §14.12.7): `MemoryToolRegistry.resolve_backend(deployment_surface)` no longer ignores its argument — it resolves the storage backend FOR the surface from a bootstrap-frozen surface→backend map (`from_surface_map`), realizing the §14.12.1 surface-parametric reading; all resolution stays at bootstrap (§14.12.5 invariant 1). The default factory path resolves each `DeploymentSurface` independently (config-free `FILESYSTEM`, constructed once per distinct enum) and **freezes a deferred `RT-FAIL-MEMORY-BACKEND-RESOLUTION`** for a surface with no config-free backend (`MANAGED_CLOUD` admits only `{s3, database}`, both needing operator `backend_params`); the **ACTIVE surface must resolve or bootstrap aborts fail-closed** (preserves the pre-B5 active-`MANAGED_CLOUD`-no-override behavior — no silent ephemeral SQLite). Anti-vacuity = **constructs-vs-raises** (the single global override config cannot populate two distinct *constructed* types without collapsing the map — `[[built-but-vacuous-reground-ledger-asis]]`). **Active-surface dispatch is byte-identical** (production threads the single active `config.deployment_surface`); the multi-surface map is **contract-shape, not a runtime multiplex feature** (single-surface deployment ⇒ no multiplex use case). No design-substrate edit, no new contract, no CXA seam (overlay 31/31). Backends routed to (`FILESYSTEM`/SQLite-`DATABASE`/`S3`/managed-DB) all exist (R-830). **Did NOT extend `memory_tool_backend_config` into a per-surface map** (advisor: that is configurability nobody can reach + an X-AL-3 spec extension — the over-engineering trap FULL-SPEC can induce; the global override already fully expresses the one-deployment backend choice). `[[built-but-vacuous-reground-ledger-asis]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]`.

**`B-MEMORY-SURFACE-BACKEND-IMPLS` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]` + `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`; surfaced at arc B5, advisor-flagged).** Two **spec'd-but-unimplemented** memory storage backends the B5 factory `_construct_backend` else-branch still raises for: **`ENCRYPTED_FILESYSTEM`** (runtime spec §14.12.3 step 2 — a filesystem implementation wrapped with per-path encryption per an operator-supplied key reference at `backend_params`) and **`OPERATOR_DEFINED`** (§14.12.3 step 2 + §14.12.7 — an operator-supplied implementation resolved by class-qualified-name introspection via `backend_params['class_qualified_name']`, e.g. `importlib.import_module` + `getattr`, raising `RT-FAIL-MEMORY-BACKEND-RESOLUTION` on class-resolution failure). Both are declared in the `MemoryToolStorageBackend` enum + described by the cleared spec → under the FULL-SPEC standing directive they are **registered BUILD arcs, NOT operator-discretion defer-closes** (the grounding-sweep's "operator-discretion follow-on per §16 §6.C v2 C.vii" disposition predates + is overridden by the 2026-06-12 directive). Each is a small, separable backend impl + factory wiring + the `_MEMORY_BACKENDS` admissibility intersection (once `ENCRYPTED_FILESYSTEM` lands, `LOCAL_DEVELOPMENT` + `SELF_HOSTED_SERVER` gain a second admissible backend and B5's surface→backend map routes to it; once both land, every `DeploymentSurface` has an admissible config-free or operator-config backend — the B5 deferred-raise surfaces shrink). **B5's routing scope is complete without them** — they are the remaining backend *implementations*, registered for a follow-on R-FS-1 child arc. **✅ BUILT (this arc, 2026-06-17) — impl-to-cleared-spec (§14.12.3 step 2), Phase 7 posture (`harness-runtime/**` only), NO design-substrate edit, NO fork, NO contract widening.** The classification gate cleared against the **spec body** — both members are declared in the `MemoryToolStorageBackend` enum and their construction is fully described at §14.12.3 step 2 → autonomous build, not a defer-close (the grounding-sweep's "operator-discretion follow-on" disposition is overridden by the FULL-SPEC directive). **`ENCRYPTED_FILESYSTEM`** — realized as an injected `MemoryContentCodec` on `LocalFilesystemMemoryToolBackend` (new `memory_tool_encrypted.FernetContentCodec`, Fernet authenticated AES, ciphertext at rest), **NOT** an external wrapper: the codec's `encode`/`decode` run INSIDE the backend's existing per-path lock, so the `str_replace`/`insert` read-modify-write stays atomic with **one** authoritative lock — an external wrapper would have to DUPLICATE that lock (re-deriving the read-modify-write as `view`+`create`) to match (`[[cleared-spec-resolves-it-before-first-principles-fix]]` inverse-nuance: the spec's "wrap" names the *encryption invariant* [ciphertext-at-rest, preserved]; the wrapping *site* is impl-discretion per §14.12.7). Key via `backend_params['key_env_var']` (an env-var **reference**, never key material — structure-not-content per §14.12.1; env-var is the deliberately chosen scheme over Target-Stack-nominal python-keyring — it composes with the workspace `.env`/just dotenv posture, one working scheme satisfies full-spec). Fernet **lazy-imported behind RT-FAIL** (mirrors the boto3/psycopg pattern; `cryptography` 48.0.0 is a present transitive dep so live works — no pyproject change). inv-4: malformed-key/decode errors never echo the key (test-enforced). A distinct `.harness/memories-encrypted` root (no plaintext↔ciphertext co-mingling). **`OPERATOR_DEFINED`** — `_resolve_class_qualified_name` (`importlib.import_module` + `getattr`; accepts `module:Class` and `module.Class`) **ESTABLISHES the canonical class-qualified-name introspection convention** the C-RT-23 validator framework follows (runtime spec §14.13.1 line 4555: "validator … must use the same introspection-based discipline as MemoryToolStorageBackend.OPERATOR_DEFINED"). The operator class is instantiated with the full `backend_params` Mapping; conformance is the existing `_enforce_protocol_conformance` **hardened to a 3-layer check (presence + callable + async)** so a sync-`def` operator class fails CLOSED at bootstrap (ADR-F4) rather than `TypeError` at first dispatch. `_construct_backend` now handles all 5 members (`assert_never` exhaustive tail; the prior else-raise is gone). `str_replace`/`insert` switched `read_text`/`write_text` → `read_bytes` + explicit utf-8 (byte-faithful for the codec); the full harness-runtime suite incl. U-RT-81 dispatch + U-RT-82 e2e verified the encoding change non-regressive. **Decorrelated review:** advisor (forced the one-source-of-truth framing for codec-vs-wrapper + flagged the encoding blast-radius → run the full suite, not just the backend files) + **out-of-family Codex** — which caught **two real bugs advisor + I missed** (`[[hooks-codex-pilots-decorrelation-validated]]`): (1) [P2] the OPERATOR_DEFINED instantiation handler interpolated `{exc}` → an operator `__init__` could echo a secret `backend_params` value (fixed: surface the exception TYPE only, keep the chain; test-enforced); (2) [P2] a sync-`def` operator backend passed the presence-only `@runtime_checkable` check → `await` TypeError at dispatch (fixed: the async-conformance gate above; test-enforced) — Codex then converged "no actionable correctness issues." New: `memory_tool_encrypted.py` + `test_lifecycle_memory_tool_encrypted.py` (+ codec tests in the filesystem test file + the rewritten AC#3 + OPERATOR_DEFINED/ENCRYPTED suites). Gates: targeted 101 / full harness-runtime **1876 passed, 21 skipped, 1 xfailed**; pyright strict 0/0/0; ruff clean.

**B6 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B6** arc (Bucket B row above — per-tool sandbox-tier driver selection + STDIO transport-floor; arc #1/#503 landed per-*server-uniform* tier→driver selection, and B2/U-RT-130 lifted that resolver/driver from single-server `[0]` to **per-host**, but the within-host resolver is still the **constant** `_build_default_policy_sandbox_resolver` — `resolve(_contract,_step)` ignores both args) is built as two ordered slices, split on the ratification boundary: **Slice 1 (this arc) — per-host transport-floor + remote-trust composition.** The stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor(entry.transport, entry.trust_level, entry.blast_radius)` (U-AS-13) via tier-`max()` with the §14.9.8 surface-aware `effective.sandbox_tier` before building the resolver + selecting the driver — closing the **carved-out STDIO tier-3 transport-floor non-enforcement gap** (runtime spec v1.43 §14.9.9 change-note: "a bare STDIO local-development server resolves to TIER_1_PROCESS, below the §1.3 floor … explicitly carved out at the §14.9.9 Scope boundary"). **Uses `mcp_transport_floor`, NOT `sandbox_tier_floor`** — the per-host `MCPClientConfig.blast_radius` is ONE blast input per host ⇒ one tier per host (per-server-uniform PRESERVED); feeding `sandbox_tier_floor` per-tool `contract.blast_radius_tier` would silently pull in the Slice-2 per-tool/driver-granularity fork (advisor catch). REFUSE handled as a **defensive raise** (`SandboxDispatchDecision.tier` non-optional; L0-remote already refused at registration `mcp_host.py:111`, so a resolver REFUSE is unreachable-by-construction). **Impl-to-cleared-ADR — NO operator gate** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`): the apparent **C10⊥C11** tension (action-safety wants the STDIO tier-3 minimum; operator-loop/local-deployment wants out-of-box-without-substrate) is **probe-resolved at ADR-D2 §1.3 in C10's favor** — Decision item 3 commits "Tier 3 minimum for STDIO MCP transports regardless of declared blast-radius" + §1.5 `max()` composition ⇒ `max(local-dev-cell=Tier1, STDIO-floor=Tier3)=Tier3` even at local-development (a hard floor, not overridable per §1.1's cell-override-only contract). Slice 1 therefore **fulfills** a committed mandate + closes a security gap (sacrifices nothing) — unlike B4 Slice 4 which *relaxed* §14.5.3. **Real out-of-box behavioral change (named, not gated):** enforcing the floor flips local-dev STDIO TIER_1→TIER_3 (and L2-remote→TIER_4), so those hosts now require a configured microVM/VM `sandbox_driver` or bootstrap **fail-closes** (FR-2(i) `RT-FAIL-SANDBOX-DRIVER-UNAVAILABLE`) — the cleared §14.9.9 fail-loud floor makes this safe (no silent under-sandbox). **Test cascade (as-built, empirically sized):** factory tests (`test_u_rt_75`) gain a tier-3 `sandbox_driver` + assert the floored TIER_3 / fail-close / gVisor; **`test_u_rt_86` (H_T-AS-2/CP-18 retirement evidence) verified UNAFFECTED — ran-and-passed** (dispatches via the host path, not the stage-5 FR-2 factory, so the floor never hits it; ToolContract still enforced); the **in-process-STDIO `api.run` dispatch e2e** (`test_r100_ac2` + `test_r_cl_p3`×3) are **SKIPPED** (in-process STDIO is the pre-B6 gap ADR-D2 §1.3 forbids; container completion covered by Docker-gated R-410). **Coverage loss surfaced honestly:** the only default-lane proof that `api.run` *completes* a tool workflow is gated out — accepted within B6 scope; full restoration registered as `B-MCP-HOST-REMOTE-TRANSPORT` (a remote L1 RO server → TIER_1, blocked today by the host-factory `_VALID_TRANSPORTS` rejecting the granular remote enum). If "STDIO tools can't run in-process even at local-dev" is too strict for out-of-box, that's an ADR-D2 §1.3 revision route (operator's call; does not block). Bundled-absorption arc (runtime spec §14.9.8/§14.9.9 additive amendment composing the per-MCP-transport floor into the per-host resolver + fork doc + clearance + `harness-runtime/src` impl + tests co-land). → **Slice 2 (next, OPERATOR-GATED) — per-tool granularity = `B-PER-TOOL-SANDBOX-TIER` below.** **Split rationale:** Slice 1 is impl-to-cleared-ADR-D2-§1.3 (per-server-uniform preserved; no committed-invariant sacrifice) while Slice 2 RELAXES §14.9.9 inv-3 (per-server-uniform→per-dispatch driver selection) + extends `ToolContract` → the genuine ratification-boundary fork (mirrors the B4 Slice-3-additive / Slice-4-gated split).

**`B-PER-TOOL-SANDBOX-TIER` (standalone forward arc = B6 Slice 2, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B6 Slice 1).** The genuine **per-tool** sandbox-tier widening the runtime spec names as "still B6" (§14.9.10 D4: "per-tool sandbox granularity nests inside each host's resolver as the distinct future arc B6 — per-host outer × per-tool inner"; §14.9.9 Scope boundary: "widening it to the full `sandbox_tier_floor(...)` per-cell composition … is a distinct future arc"). Replaces the per-host constant resolver with a per-`(contract, step)` call to the full 10-row `sandbox_tier_floor(tool_metadata, deployment_surface, contract.blast_radius_tier, transport, mcp_server)` so rows 1-2 (forcing) + rows 7-10 (per-tool blast-radius) become reachable per tool. **Two coupled forks (structural, not optional):** (a) a `ToolContract` **carrier extension** carrying the `ToolMetadata` forcing discriminators (`forces_computer_use` / `forces_code_execution` / `is_deterministic_inhouse`) — `ToolContract` today carries only `minimum_tier` + `blast_radius_tier` — which ripples to the AS↔runtime seam (`RawContractInput` + the v1.40 stage-3a `MCPToolContractConverter`); (b) **driver-granularity relaxation of §14.9.9 inv-3** ("per-server-uniform, construction-time"): the driver self-guard is **exact-match** (`docker_tool_execution_driver.py:71` `if sandbox_decision.tier is not self.required_tier: raise`), so ANY per-tool tier diverging from the host's one construction-time driver tier breaks dispatch → per-tool tier resolution CANNOT land without per-dispatch driver selection (a per-host per-tier driver registry selected by resolved tier, OR pin-at-max + relax the guard to `>=`). Per-tool tier resolution structurally forces driver granularity — they co-land. **OPERATOR-GATED** — the §14.9.9 inv-3 relaxation is a committed-invariant sacrifice → operator ratifies before merge (mirrors B4 Slice 4 / `[[feedback-gate-only-on-meaningful-architecture-change]]`). Design-fork-first per X-AL-3 (runtime-spec amendment for the per-tool resolver + per-dispatch driver granularity + the `ToolContract` carrier extension). Not yet built; FULL-SPEC-pre-authorized when opened.

**`B-MCP-HOST-REMOTE-TRANSPORT` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B6 Slice 1, advisor-flagged).** The stage-3a MCP host factory (`mcp_client_host.py:217` `_VALID_TRANSPORTS = {'sse', 'stdio', 'streamable_http'}`) rejects the **granular** `MCPTransport` remote enum values (`streamable_http_l0/l1/l2/l3`) — it expects a COARSE transport string, but `MCPClientConfig.transport` carries the granular enum, with no granular→coarse mapping applied at the host-construction boundary. So **only STDIO (+ bare `sse`) remote-less servers materialize through the host factory**; a remote `STREAMABLE_HTTP_L*` MCP server cannot be bootstrapped today (it raises `ValueError: unknown MCP transport`). **Load-bearing consequence (surfaced by B6 Slice 1):** with STDIO now floored to TIER_3 (ADR-D2 §1.3) AND remote transports host-factory-rejected, there is **no host-factory-accepted transport that resolves to TIER_1** — so the **provider-free DEFAULT-LANE `api.run` tool-completion e2e** (the closed R-100 AC#2 `test_r100_ac2_tool_step_via_api_run` + the R-CL-P3 multi-tier tool step) can no longer run in-process (both now SKIPPED with B6 reasons; tool-completion at the ADR-mandated tier is covered by the Docker-gated `test_r410_container_tool_execution_e2e`). Fixing the host-factory transport parser (map the granular enum → coarse transport, or accept the granular set) would let a **remote L1 READ_ONLY** server resolve to TIER_1 (`mcp_transport_floor` → `blast_radius_floor(READ_ONLY)`), restoring a substrate-free default-lane tool-completion e2e. **The build:** (a) reconcile the host-factory `_VALID_TRANSPORTS` check with the granular `MCPTransport` enum (likely a granular→coarse projection at host construction); (b) verify remote-transport host materialization end-to-end (the SDK `streamablehttp_client` path); (c) restore the default-lane tool-completion e2e on a remote L1 RO server. **Owner = a follow-on R-FS-1 child arc** (likely impl-against-cleared-spec — the granular enum + coarse host transport both exist; confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: MEDIUM — real agents need remote MCP servers (composes with B2 multi-server); the immediate trigger is the B6-surfaced default-lane e2e coverage loss. **✅ BUILT (this arc, 2026-06-17) — impl-to-cleared-spec, NO operator gate, NO spec change, NO contract widening.** The classification gate cleared against the **spec** (runtime §14.9.6 inv 5 "STDIO + HTTP + SSE all supported at v1. Per Decision 1.D4"; §14.9.8 resolves `mcp_transport_floor` for remote L1/L2/L3 + confirms L0 refused at registration before stage-3a), not the `MCPTransport=Literal[...]` code comment. Fix = a `_coarse_transport` granular→coarse projection at `mcp_client_host_factory._build_host` (the prior `cast(_MCPTransportLiteral, entry.transport.value)` lied for remote values — `"streamable_http_l1"` ∉ the host's coarse `_VALID_TRANSPORTS` → `ValueError`; never exercised because no remote host had materialized through the factory): `stdio→stdio`, all `streamable_http_l*→streamable_http` (the L-level is a *trust* dimension read by the sandbox floor via `trust_level` + the gate via `server_name`, NOT a connection mechanism). **REFUSE stays single-source** — `mcp_transport_floor` owns it + stage-2 `materialize_mcp_stage` raises on L0 before stage-3a (verified: bootstrap stages PREAMBLE→IS→AS(stage_2)→CP_CLIENTS(stage_3a), abort-on-raise), so no L0 guard in the projection. **BOTH** B6-skipped default-lane tool-completion e2es RESTORED by migrating their in-process STDIO echo server (floored to TIER_3 by ADR-D2 §1.3 → fail-close) to a real **remote streamable-HTTP** echo server (remote L1 READ_ONLY → `blast_radius_floor(READ_ONLY)` = TIER_1 → host-process, no Docker — the provider-free property): `test_r100_ac2` (provider-free default lane — the registered (c) target, verified 8/8 non-flaky) + `test_r_cl_p3` (live multi-tier, un-hard-skipped; the explicit `default_sandbox_tier=TIER_1` override keeps resolved==TIER_1 on SELF_HOSTED_SERVER; 3/3 verified live against Ollama). New test fixtures `mcp_echo_server_http.py` (FastMCP streamable-http echo) + `streamable_http_echo.py` (free-port spawn + endpoint-readiness poll + hardened teardown) + factory unit tests (per-value projection + the remote-host `ValueError` regression). Decorrelated review: **advisor** (forced the spec-gate-not-code-comment discipline that kept this autonomous) + **Codex** (out-of-family — "no actionable regressions"; ran the suites independently). The "B6 e2e-migration item" named inline in `test_r_cl_p3`'s old skip is **SUBSUMED** (it was never a separately-registered forward entry — confirmed by grep). Gates: harness-runtime 1846 passed (non-e2e) / factory 17 / r100_ac2 8× / r_cl_p3 3/3 live; pyright 0/0/0 touched; ruff clean; overlay 322/31-31; CXA-P1 34; X-AL-3 green.

**`B-PARALLELIZATION-CASCADE` (standalone arc — ✅ BUILT 2026-06-21; arc-ledger `closed`).** Surfaced grounding `B-FANOUT-PAUSE-PARALLELIZATION` (the advisor's "clean reuse" pick): `_execute_parallelization` (U-CP-86) was built **happy-path-only** ("U-CP-85 non-dep") — it had **no cascade_policy resolution at all**, so a SINGLE branch failure fail-fasted the whole parallel fan-out (generic `parallelization-branch-failure` FAILED) even under `proceed` where §25.15.1 mandates PARTIAL with survivors. This is **impl-to-cleared-spec** (§25.15.1 cascade_policy semantics + §25.18 line-169 "implement per strategy … PARALLELIZATION … with a cascade-cancel idempotency test" both anticipate it) → **NO design-substrate edit, NO operator gate**. Materializes the §25.15.1 reaction for PARALLELIZATION, mirroring `_execute_orchestrator_workers` (U-CP-88) PARALLELIZATION-shaped (every step is a peer branch; NO orchestrator step[0]): `proceed`→harvest-survivors→PARTIAL (the real robustness fix) / `cascade-cancel`→FAILED+cancelled-terminals / `pause`→honest `parallelization-pause-resume-not-yet-materialized` FAILED (the resumable PAUSED is the registered follow-on `B-FANOUT-PAUSE-PARALLELIZATION`, built on this harvest like #661 followed U-CP-88). **Also closes the CP v1.42 line-23 spec-prose-vs-code drift (Class-3 informational, resolved by this arc — `[[spec-prose-plan-body-drift-pattern]]`):** §1 line 23 claimed `PARALLELIZATION` "carries its own `…-pause-resume-not-yet-materialized` FAILED branch" — but at HEAD it had NONE (only the generic `parallelization-branch-failure` of the cascade-less happy-path); ORCHESTRATOR_WORKERS / HIERARCHICAL / DECENTRALIZED_HANDOFF did. This arc makes the code match the (already-cleared) prose — the `parallelization-pause-resume-not-yet-materialized` branch now genuinely exists. No spec amendment owed (the prose was aspirationally correct; the code lagged it). **Decorrelated review:** advisor (3 calls: arc-pick + the PARALLELIZATION-lacks-cascade pivot + scope reconcile) + out-of-family Codex caught a real **[P2]** — the `proceed` harvest masked a pre-dispatch SETUP error (unbound `StepKind`) as a degradable PARTIAL where the linear path FAILs; fixed by a pre-flight dispatcher resolution that fails loud (regression test pins it). Gates: harness-cp 1100 non-e2e passed; runtime topology fixtures 37; pyright 0/0/0; arc_ledger `--check` green (standalone_closed 17→18).

**`B-FANOUT-PAUSE-PARALLELIZATION` (standalone arc — ✅ BUILT 2026-06-21; the 19th standalone `B-*` arc; arc-ledger `closed`).** The resumable `cascade_policy=pause` PAUSED for PARALLELIZATION — the v1.42 `B-FANOUT-PAUSE` (ORCHESTRATOR_WORKERS) shape applied PARALLELIZATION-shaped. **PREREQUISITE SATISFIED:** `B-PARALLELIZATION-CASCADE` (closed #678) built the cascade_policy harvest this resume builds on (PARALLELIZATION had NO cascade machinery — the original registration presumed it existed; on grounding, the harvest had to land first). **Bundled-absorption (CP spec v1.43 → v1.44 §1 NEW `PeerFanOutResumeState` carrier + `PauseSnapshot.peer_fan_out_resume` field + §2 §25.15.1 PARALLELIZATION materialization note + clearance `Spec_Control_Plane-v1_44-cleared-2026-06-21.md` + `harness-cp/src` + `harness-runtime/src` + by-execution tests), NO operator gate** (additive + opt-in; §25.15.1 already commits `pause → PAUSED`; materializes the R-CC-1 §1.1 re-open trigger, not a forbidding-invariant sacrifice). **CARRIER FORK resolved by advisor (in-impl, NOT operator):** Option B — a NEW 2-field `PeerFanOutResumeState` (`branches` + `branch_count`, NO orchestrator fields), NOT a loosened `FanOutResumeState`. Loosening the orchestrator fields to optional would make `orchestrator_output=None` *representable* for an ORCHESTRATOR_WORKERS snapshot — an illegal state for that strategy (illegal-states-unrepresentable, §4). The second-additive-field route (`peer_fan_out_resume`, never co-set with `fan_out_resume`) preserves the v1.42 additive discipline (existing snapshots byte-identical; hash key added only when present). **Mechanism:** on a branch failure under `pause` with a bound `pause_resume_protocol`, `_execute_parallelization` captures a `PeerFanOutResumeState`-bearing snapshot (the terminal branches' dispositions + recovered outputs, keyed over `steps` directly) → `RunStatus.PAUSED`; no protocol → honest FAILED + `parallelization-pause-resume-protocol-not-bound` (no false-PAUSED). Resume threads `execute_workflow(pause_snapshot_input=) → _execute_workflow_body(resume_snapshot=) → _execute_parallelization(resume_snapshot=)`: skip the terminal branches (recover their outputs into the aggregate), re-dispatch the absent ordinals (obl. 7); a re-dispatched branch failing again re-pauses with the unioned terminal set; a resumed clean-terminal with a recovered failed branch → **PARTIAL** (no silent SUCCESS). Re-keyed material-diff guard (`branch_count` + per-branch `step_id` over `steps`, NO orchestrator-identity check); RESUMPTION (not WORKFLOW_START) emitted on the resume envelope. **`pause_resumable` dropped** (nothing reuses `_execute_parallelization` — advisor). **Non-vacuity = full-chain witness:** CP `test_workflow_driver_parallelization_pause.py` (12 tests through the real `execute_workflow(pause_snapshot_input=)` entry-point resume detection — the path `api.resume` drives: PAUSED+snapshot / skip-terminal+recover+re-dispatch / all-terminal-with-failure→PARTIAL / re-pause-union / in-flight-completed capture + a hash-tamper integrity negative control + an empty-branches recovery-loss negative control + branch-count + branch-identity material-diff + JSON round-trip + backward-compat) + the runtime gold-standard `test_b_fanout_pause_parallelization_resume_e2e.py` (capture via real bootstrapped protocol → JSON round-trip → `api.resume` → SUCCESS, only branch-1 re-dispatched). **Scope = PARALLELIZATION only;** the v1.42 §2 forward-arc line for PARALLELIZATION is corrected (3 non-linear strategies' pause-resume remain registered: `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / `B-HANDOFF-PAUSE` / `B-HIERARCHICAL-PAUSE`). **Decorrelated review:** advisor (full-transcript — pick + classification + carrier-fork Option-B + the 5 carried correctness items) + out-of-family Codex (pre-merge). Gates: pyright 0/0/0 · harness-cp 1110 passed + 1 xfailed · harness-runtime 1982 passed (non-e2e) · arc_ledger `--check` green (standalone_closed 18→19). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[design-substrate-version-identity-hazards]]` (the v1.43 slot was already taken by B-LAYER-BUDGET-OVERRIDE — this delta is v1.44).

**`B-HIERARCHICAL-PAUSE` (standalone arc — ✅ BUILT 2026-06-21; the 20th standalone `B-*` arc; arc-ledger `closed`).** The resumable `cascade_policy=pause` PAUSED for `HIERARCHICAL_DELEGATION`, in TWO halves. **(a) Level-local:** HIERARCHICAL reuses `_execute_orchestrator_workers` at each recursion level, so threading `pause_resumable=True` + the resume snapshot through `_execute_hierarchical_delegation` + the `_execute_workflow_body` HIERARCHICAL dispatch materializes a level-local worker pause via the v1.42 `FanOutResumeState` (the interim `orchestrator-workers-pause-resume-not-yet-materialized` FAILED is retired). **(b) Cross-recursion-boundary (the registered hard scope):** a `SUB_AGENT_DISPATCH` worker whose recursive child sub-workflow itself PAUSED (a grandchild failing under `cascade_policy=pause`) was previously SWALLOWED as success-equivalent at the runtime dispatch boundary (`sub_agent_dispatch.py:687` else-branch — silently dropping the child's suspended state). **ADVISOR refused the Half-A-only slice** (not separable — the level-local plumbing IS the opening of Half B; splitting = parking dressed as decomposition); built as ONE PR. **ADVISOR disqualified two alternatives:** `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` (MISLABELED — EO is SEQUENTIAL, not a fan-out barrier; the §25.15.1 cascade `pause` trigger doesn't apply; arc-ledger scope corrected) + `B-ROUTING-MANIFEST-MODEL-FOLD` (genuinely fork-first per its own ledger entry — needs a new `StepEffectiveBinding` source discriminator). **Bundled-absorption (CP spec v1.44 → v1.45 §1 NEW `PausedChildBranchResumeState` carrier + `FanOutResumeState.paused_child_branches` field + hash-inert `StepExecutionContext.child_resume_snapshot` field + §2 §25.15.1/§25.18 HIERARCHICAL materialization note + clearance `Spec_Control_Plane-v1_45-cleared-2026-06-21.md` + `harness-cp/src` + `harness-runtime/src` + by-execution tests), NO operator gate** — §25.15.1 commits `pause → PAUSED`; **§25.18 EXPLICITLY names the HIERARCHICAL recursion cascade under "Deferred to implementation discretion (NOT design holes)"** → this arc IS that materialization (impl-to-cleared-spec). **Mechanism:** the runtime dispatcher now RAISES the typed `SubAgentChildPausedError` (carrying the child's `PauseSnapshot`; defined in `harness_cp.workflow_driver_types` so the CP barrier catches it typed — runtime depends on cp, axis isolation) instead of swallowing; the CP `_execute_orchestrator_workers` worker barrier captures it into `paused_child_branches` (DISJOINT from `branches` — the **THIRD branch disposition**: re-dispatch-via-child-resume, NOT skip-terminal / NOT re-dispatch-fresh) + PAUSES; on resume the paused-child worker is re-dispatched WITH the child snapshot threaded on `StepExecutionContext.child_resume_snapshot` → the runtime dispatcher forwards it to the `ChildWorkflowRunner` (additive `pause_snapshot_input` kwarg) → the child re-enters via `execute_workflow(pause_snapshot_input=...)` at its cursor — **the grandchild's completed steps are recovered, NOT re-executed**. A child PAUSE under `proceed`/`cascade-cancel` (no resumable boundary) FAILS honestly (`orchestrator-workers-child-paused-not-resumable-under-proceed`), never a silent success. **CARRIER (advisor):** a NEW field on `FanOutResumeState` (HIERARCHICAL IS ORCHESTRATOR_WORKERS at each level → the paused-child belongs inside that fan-out's resume state, NOT a top-level `PauseSnapshot` sibling like v1.44's `peer_fan_out_resume`); additive default-empty + **DROPPED from the canonical hash when empty** (so every pre-arc ORCHESTRATOR_WORKERS / PARALLELIZATION snapshot is byte-identical — the v1.44 byte-compat discipline) + COVERED transitively when non-empty (a tampered grandchild cursor fails the parent resume recompute); the carrier SELF-NESTS (a child snapshot may carry its own `paused_child_branches` — recursive Pydantic via one `FanOutResumeState.model_rebuild()`). **Non-vacuity = the discriminating full-chain witness (the advisor's acceptance bar, `[[full-chain-witness-not-half-proofs]]`):** `test_hierarchical_child_pause_resume_does_not_reexecute_grandchild` drives REAL recursive `execute_workflow → child fan-out pause → SubAgentChildPausedError → parent capture+PAUSE → api.resume → child re-entry at cursor` and asserts a grandchild completed-before-pause is dispatched EXACTLY ONCE across pause+resume (a broken re-entry that re-dispatched the child fresh would make it 2) + asserts the 2nd child call received the resume snapshot; the runtime dispatcher raise + resume-forwarding is unit-proven in `test_lifecycle_sub_agent_dispatch.py` (a faithful double stands in for `RuntimeSubAgentDispatcher` in the CP witness). The INFERENCE-child real-provider e2e is blocked by a pre-existing runtime sync/async deadlock (`.harness/runtime_defect_sub_agent_inference_child_loop_bridge_deadlock.md`) → declarative-only witness by necessity, NOT an under-proof. **Decorrelated review — out-of-family Codex earned its keep across 4 rounds** (`[[hooks-codex-pilots-decorrelation-validated]]`, asymptotic convergence): R1 — two real [P1]s advisor+author missed (a CANCELLATION-RACE child-pause loss: a sibling-cancelled worker's in-flight `SubAgentChildPausedError` lands in `inflight.exception()`, suppressed by the shielded drain → was recorded terminal `completed` + the snapshot DROPPED; fixed + a deterministic 2-worker race regression test + the resumed child must reuse the snapshot's `run_id` not a fresh uuid for ledger/idempotency lineage); R2 — a pyright `reportConstantRedefinition` gate-break (ALL_CAPS test counter → mutable holder) + the paused span missed 3 required token attrs; R3 — best-effort audit on the pause path (mirror FAILED) + dashboard regen; R4 — **`subagent.result_status="paused"` violated the CLOSED cardinality-3 `SubAgentResultStatus` enum** (advisory schema-doc, not runtime-enforced; advisor-adjudicated: child-pause is a genuine 4th outcome → extend the enum 3→4 at C-CP-14 §14.2, own the now-false "no enum change" claim, NOT reuse FAILED [corrupts failure-rate telemetry]) + the child-resume `workflow_id` mismatch fail-closed guard + the cascade-cancel paused-child mislabeled `cancelled` (→ `completed`, dispatch-boundary). advisor (full-transcript — arc-pick + slice-refusal + acceptance-bar + the enum/converge adjudication). Gates: pyright 0/0/0 · harness-cp green · harness-runtime green (non-e2e) · arc_ledger `--check` green (standalone_closed 19→20). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[fanout-pause-per-strategy-carrier]]` (registered scope mislabeled EO's SHAPE — ground the executor) · `[[hooks-codex-pilots-decorrelation-validated]]`. **Class-3 (prose home here):** the runtime spec §14.7.2 step-7 child-result mapping enumerates SUCCESS/DRAINED/FAILED only — the new child-PAUSED row + the §14.7.4 `ChildWorkflowRunner.pause_snapshot_input` kwarg co-land as runtime-internal surfaces; a batched runtime-spec refresh formalizes the §14.7.2 step-7 PAUSED row (impl-ahead-of-prose minor drift, cf. #677's §14.21.7 lumping note).

**`B-HANDOFF-PAUSE` (standalone arc — ✅ BUILT 2026-06-21; the 21st standalone `B-*` arc; arc-ledger `closed`).** The resumable `cascade_policy=pause` PAUSED for `DECENTRALIZED_HANDOFF` — the **single-owner sequential** analogue of the fan-out pause family (ORCHESTRATOR_WORKERS v1.42 / PARALLELIZATION v1.44 / HIERARCHICAL v1.45), completing the family across all 5 non-linear strategies (only the sequential `EVALUATOR_OPTIMIZER` iteration-cursor — a fork-first arc — remains). **ADVISOR picked it** over the EO arc (EO is sequential with NO `cascade_policy` + NO pause branch + NO trigger → genuinely fork-first; pairing them in one PR would be parking-dressed-as-decomposition) and **SHARPENED the load-bearing cite-precision gap:** §25.15.1's pause row is **fan-out-barrier-scoped** ("Halt the **fan-out**…"), and HANDOFF is explicitly NOT a fan-out barrier (single-owner sequential; `cascade_policy` "degenerate for single-owner") → materializing HANDOFF pause is an **EXTENSION** of the §25.15.1 reading, **NOT verbatim coverage** (the first-draft "verbatim" claim was corrected in the §2 change-note). The authority for the extension is **§25.18**, which (PRESERVED VERBATIM) defers per-strategy materialization to impl-discretion + names `DECENTRALIZED_HANDOFF` as the **LAST strategy in the simplest→hardest impl order** → impl-to-cleared-spec, **NO operator gate** (no invariant forbids a single-owner resumable pause; the existing handoff executor already resolved `cascade_policy` TEAM→pause). **Bundled-absorption (CP spec v1.45 → v1.46 §1 NEW `HandoffResumeState` + `HandoffStageResumeState` stage-cursor carriers + `PauseSnapshot.handoff_resume` field + §2 §25.15.1/§25.18 DECENTRALIZED_HANDOFF materialization note + clearance `Spec_Control_Plane-v1_46-cleared-2026-06-21.md` + `harness-cp/src` + by-execution tests).** **CARRIER (`[[fanout-pause-per-strategy-carrier]]`):** a NEW dedicated **stage CURSOR** (`completed_stages` = the CONTIGUOUS completed prefix 0..k-1 with recovered outputs + `stage_count`), NOT a loosened fan-out carrier — a handoff resume has no orchestrator output, no peer-branch set, no absent-ordinal gap, so reusing the fan-out carriers would make illegal states representable for those strategies; the third additive `PauseSnapshot.handoff_resume` (mutually exclusive with `fan_out_resume`/`peer_fan_out_resume`) is hash-COVERED-when-present + DROPPED-when-None (pre-arc snapshots byte-identical). **Mechanism:** a stage failure under `pause` (protocol bound) captures the cursor → `RunStatus.PAUSED`; no protocol → honest FAILED + `decentralized-handoff-pause-resume-protocol-not-bound` (salvaging the prefix; the detect-then-refuse mirror of `_execute_parallelization`, FAILED-not-PARTIAL). On `api.resume` the executor **RE-WALKS** the body: the completed prefix's outputs are RECOVERED (replayed, NOT re-dispatched) + the inter-step channel re-seeded; stage k onward re-dispatched. **The load-bearing correctness = the handoff causality chain (advisor-named witness):** because each stage's ctx + `action_id` is recomputed DETERMINISTICALLY through the prefix, the resumed stage's `parent_action_id` chains off the LAST COMPLETED stage's `action_id` — **NOT re-anchored to the workflow origin** (the chain is INHERENT in the recompute, not a carried string that could drift). A naive resume that re-anchored to `workflow:{wf}:step:0` would pass a "resume→SUCCESS" test while silently breaking the chain. **CP-only impl** — handoff dispatches through the ordinary `StepDispatcher` (NEVER `SUB_AGENT_DISPATCH`), so NO runtime dispatcher change (unlike #680 HIERARCHICAL's cross-recursion child-pause). **Non-vacuity = full-chain witness (`[[full-chain-witness-not-half-proofs]]`):** CP `test_workflow_driver_handoff_pause.py` (12 tests through the real `execute_workflow(pause_snapshot_input=)` resume detection — the at-most-once "prefix dispatcher never re-invoked" witness + the discriminating "resumed stage parent_action_id == clean-run chain anchor ≠ origin" causality witness + inter-step re-seed + hash-tamper integrity + stage-count/identity/non-contiguous material-diff negatives + protocol-not-bound honest-FAILED + re-pause-union) + runtime gold-standard `test_b_handoff_pause_resume_e2e.py` (capture via real bootstrapped protocol → JSON round-trip → `api.resume` → SUCCESS, only the failed-then-recovered tail re-dispatched). Existing `test_workflow_driver_decentralized_handoff.py` no-protocol test updated for the materialized honest-FAILED path (`[[closed-schema-extension-enforced-vs-advisory]]` — a test that pinned the interim behavior, honestly updated, NOT a bypass-seam). **Decorrelated review:** advisor (full-transcript — arc-pick + the §25.15.1-extension cite-precision sharpening + the invariant check + the causality-witness acceptance bar) + out-of-family Codex (pre-merge). Gates: pyright 0/0/0 · harness-cp 1124 passed + 1 xfailed · harness-runtime 1987 passed (non-e2e) · arc_ledger `--check` green (standalone_closed 20→21). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[fanout-pause-per-strategy-carrier]]` · `[[spec-prose-plan-body-drift-pattern]]` (the §25.15.1 fan-out-scoped reading — extension owned, not glossed).

**`B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` (standalone arc — ✅ BUILT 2026-06-21; the 28th standalone `B-*` arc; arc-ledger `closed`).** The resumable `cascade_policy=pause` PAUSED for `EVALUATOR_OPTIMIZER` — the **LAST** of the 5 non-linear pause materializations (the family is now COMPLETE: ORCHESTRATOR_WORKERS v1.42 / PARALLELIZATION v1.44 / HIERARCHICAL v1.45 / DECENTRALIZED_HANDOFF v1.46 / EVALUATOR_OPTIMIZER v1.48). The single-owner sequential analogue of #681's handoff, but an **ITERATION CURSOR** over the loop's completed generate/evaluate STEPS (entry-granular — the same 2 declared steps re-dispatched across iterations), NOT a stage list. **DISPOSITION CORRECTED — BUILD, not fork (the gated premise check):** the prior `registered` scope (+ the v1.46 §2 / line-27 framing) labeled EO "design-fork-first" ("§25.15.1 cascade `pause` trigger does not apply"). The advisor GATED the build on a direct §25 v1.32 read (NOT #681's paraphrase) — the symmetric premise-check the B-RESUME-CONTEXT-SURVIVES-RETRY resolution (#695) taught — which found the "fork-first" premise FALSE: (a) the §25.11 EO row "terminal on accept or cap" is normal-termination, NOT a step-failure-FAILED invariant; (b) §25.15.1 is fan-out-barrier-scoped → extending to sequential EO is impl-discretion (the #681 HANDOFF situation); (c) §25.17's failure taxonomy is branch/barrier-framed + SILENT on EO sequential step-failure → no forbidding invariant; (d) §25.18 NAMES `EVALUATOR_OPTIMIZER` in the simplest→hardest impl order → authorizes materialization, does not carve out. So it is a BUILD analogous to #681 (the #681 single-owner-sequential precedent governs), NO operator gate. **Bundled-absorption (CP spec v1.47 → v1.48 §1 NEW `EvaluatorOptimizerResumeState` + `EvaluatorOptimizerStepResumeState` iteration-cursor carriers + `PauseSnapshot.evaluator_optimizer_resume` field + §2 §25.15.1/§25.18 EVALUATOR_OPTIMIZER materialization note + clearance `Spec_Control_Plane-v1_48-cleared-2026-06-21.md` + `harness-cp/src` + `harness-runtime/src` durable forward + by-execution tests).** **SURGICAL SCOPE (advisor's load-bearing steer):** EO is an EXISTING executor (not greenfield like #681), so only the `pause` branch is materialized — `proceed`(SOLO)/`cascade-cancel`(MTC) keep EO's pre-arc terminal-FAILED with the generic `evaluator-optimizer-step-failure` class; a `proceed → PARTIAL` would be a silent regression to existing SOLO EO workflows with no arc mandate (out of scope; the two preserved-FAILED tiers are tested). **CARRIER (`[[fanout-pause-per-strategy-carrier]]`):** a NEW dedicated **iteration cursor** (`completed_steps` = the CONTIGUOUS entry-granular completed-step prefix 0..m-1; NO stored cap/step-count — the iteration semantics derive from `entry_index` parity), NOT a loosened fan-out or handoff carrier (the handoff `stage_index` is 1:1 with `steps`; EO's `entry_index` is not). The fourth additive `PauseSnapshot.evaluator_optimizer_resume` (mutually exclusive with the 3 prior resume carriers) is hash-COVERED-when-present + DROPPED-when-None (pre-arc snapshots byte-identical). **THE LOAD-BEARING CORRECTNESS = the iteration cap reconstruction across the resume boundary:** on `api.resume` the executor REPLAYS the completed prefix (outputs RECOVERED, NOT re-dispatched) reconstructing the `iterations` counter from the recovered generate count so the ORIGINAL max-iteration cap is HONORED (a resume does NOT get a fresh full cap on top of the recovered work), then re-dispatches from the failed step (finishing a partial iteration first if the cursor paused on an evaluate). A naive "resume→PAUSED→SUCCESS" passes while silently breaking the cap; the discriminating witnesses assert (a) the prefix's dispatcher is NEVER re-invoked across pause+resume — both pending-generate AND pending-evaluate pauses — AND (b) a never-accepting resume hits the ORIGINAL cap (total iterations == cap, NOT cap + recovered). **`step_index` = the failed step's DECLARED ordinal (0/1), not its entry_index** — EO's entry_index grows past `len(steps)=2` (the loop re-dispatches the same 2 steps), so it cannot serve as `PauseSnapshot.step_index` (the C-RT-30 `api.resume` guard requires `0 <= step_index < len(steps)`); the failed step's `entry_index % 2` is a valid `steps` position + the cursor carries the full resume position. This was caught by the runtime e2e (the entry-index form was rejected by the guard). No protocol → honest FAILED + `evaluator-optimizer-pause-resume-protocol-not-bound`. **CP + runtime-durable-forward impl** — the durable wrapper forwards the new `evaluator_optimizer_resume` kwarg (the #681 Codex [P1] precedent — a CP-side carrier add silently TypeErrors the durable path without it; a dedicated `test_durable_wrapper_forwards_evaluator_optimizer_resume` pins it). **Non-vacuity = full-chain witness (`[[full-chain-witness-not-half-proofs]]`):** CP `test_workflow_driver_evaluator_optimizer_pause.py` (14 tests through the real `execute_workflow(pause_snapshot_input=)` resume detection — at-most-once [pending-generate + pending-evaluate] + cap-across-resume + inter-step re-seed + re-pause-union + hash-tamper integrity + 4 material-diff negatives [step-count / identity / accepted-in-prefix / cursor-step-index] + protocol-not-bound honest-FAILED + backward-compat) + the durable-forward unit + runtime gold-standard `test_b_evaluator_optimizer_pause_resume_e2e.py` (capture via real bootstrapped protocol → JSON round-trip → `api.resume` → SUCCESS, only the failed iteration re-dispatched). Existing `test_workflow_driver_evaluator_optimizer.py` step-failure test switched to a SOLO (proceed) tier + an MTC (cascade-cancel) baseline added — the preserved terminal-FAILED paths (owning the now-false "no cascade_policy differentiation" docstring). **Decorrelated review:** advisor (full-transcript — the gated §25 premise-check correcting the fork-first mislabel + the surgical-scope steer + the cap-reconstruction acceptance bar + the at-most-once witness) + out-of-family Codex (pre-merge). Gates: pyright 0/0/0 · harness-cp 1151 passed + 1 xfailed · harness-runtime 2009 passed / 33 skipped (non-e2e) · arc_ledger `--check` green (standalone_closed 27→28; registered 6→5). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[full-chain-witness-not-half-proofs]]` · `[[fanout-pause-per-strategy-carrier]]` · `[[cleared-spec-resolves-it-before-first-principles-fix]]` (the §25 direct read correcting the stale fork-first label) · `[[advisor-before-substantive-work-for-cross-axis-blockers]]`.
