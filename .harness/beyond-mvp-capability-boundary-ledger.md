# Beyond-MVP Capability-Boundary Ledger

**Authored:** 2026-06-12 · **Posture:** mode-agnostic (process-substrate; grounds `harness-*/src` + canonical specs + roadmap at HEAD `3d805d2` by direct read; authors only this `.harness/` file). **Trigger:** operator directive — the harness is being built **BEYOND MVP**, so any feature still *bounded to MVP scope* is an OPEN item that must be **built out** OR **explicitly re-ratified as a permanent bounded-residual** before harness development can be definitively closed.

**Method:** three read-only Explore sweeps (code / specs / roadmap) → every material claim re-grounded by direct read (Explore is presence-not-correctness). The re-grounding caught and discarded several **stale docstrings** that read as boundaries but were superseded (Bucket C) — listing those as "open" would have overstated the work.

**The discriminator (per `.harness/capability-completion-inventory-v1.md` + `[[grounding-reveals-claude-closeable-slice-close-honestly]]`):** ratified-DEFER → *confirm, don't rescue*. So this ledger does **not** re-litigate items the operator already dispositioned (Bucket A); the actionable payload is **Bucket B** — boundaries that are genuinely *not* tracked as build arcs.

---

> ## ⚑ STANDING DIRECTIVE — FULL SPEC, NOTHING DEFERRED (operator 2026-06-12, will-NOT-drift)
>
> *"Nothing is deferred and the full spec will be built beyond MVP, no exceptions or excuses. This is the objective and it will not drift moving forward. If a producer, wiring, surface, etc must be researched, designed, speced and planned to unblock code implementation then it will happen...period."*
>
> **This directive REWRITES the buckets below.** "ratified-DEFER → confirm-don't-rescue" is **superseded**: **Bucket A is RE-OPENED — every deferred capability is now a BUILD item, on equal footing with Bucket B.** The only retained discriminators are: **Bucket C** (genuinely stale docstrings → doc-hygiene, nothing to build) and the **paid-call / secret / vendor decision boundary** (design/spec/plan to the point of decision, then surface the one external input — corpus, embedding-model, router-model — never auto-fire it). See `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`.
>
> **WHAT vs HOW (the line a future session must not blur):** the directive sets WHAT (full spec, nothing deferred); the committed ADRs/invariants still govern HOW — **I-6** (hand-roll the durable engines + topology orchestration; NO vendored framework/LiteLLM), **ADR-F1** (per-provider SDKs, capability-aware). "No exceptions" is about scope, NOT about overriding the invariants the spec was designed under.
>
> **Scope = two levels (both driven to zero):**
> 1. **MVP-marker boundaries** (reduced-but-present features) — Bucket A (re-opened) + Bucket B below.
> 2. **Contract-level completeness** (specced `C-*` with no/partial carrier) — the deterministic overlay audit (`just overlay-query --orphans` → `contract w/o code cite`). At HEAD: **8 candidates** — `C-CP-30, C-CP-37, C-CP-43, C-CP-49, C-CP-50, C-IS-11, C-OD-3, C-RT-28` (advisory — cite-absence ≠ non-implementation; each verified per-contract in the scoping arc). This is the *other half* of "full spec" an MVP-string sweep misses.
>
> **Vehicle:** a single ACTIVE umbrella program **`R-FS-1`** (full-spec build) with the capabilities as ordered child arcs (the proven R-CC-1 shape); the old DEFERRED roadmap entries point AT it rather than each going independently ACTIVE (avoids the §4 `all(deps RESOLVED)` trap). **Consequence the operator owns: this pushes R-CL-Q1→C1 (the entire quality/close track) behind the full-spec build program.**

---

## Bucket B — Newly-surfaced MVP capability boundaries (NOT tracked as build arcs) — THE PAYLOAD

These are reduced/stubbed/single-case at MVP, the full behavior is a known larger thing, and a roadmap grep confirms **none is on the §5 catalog as a build arc** (the closest, topology, appears only as a RESOLVED *test-fixture* entry that explicitly defers the orchestration to "CP-axis Class-1 back-flow, out of fences"). Each needs an explicit operator call: **BUILD** or **permanently RATIFY as residual**.

| # | Boundary (MVP state) | Evidence (HEAD `3d805d2`) | What "beyond MVP" requires | Load-bearing | Rec |
|---|---|---|---|---|---|
| **B1** | **Workflow topology: 1 of 6 patterns.** Only `SINGLE_THREADED_LINEAR` has real orchestration; the other 5 (`PARALLELIZATION`, `ORCHESTRATOR_WORKERS`, `DECENTRALIZED_HANDOFF`, `HIERARCHICAL_DELEGATION`, `EVALUATOR_OPTIMIZER`) are admissible + emit telemetry but are a **stateless passthrough — no orchestration semantics**. | `workflow_driver_errors.py:29` ("only SINGLE_THREADED_LINEAR is materialized; other 5 deferred per X-AL-3"); `api.py:244` ("no parallel branching at runtime"); roadmap §5 line 873 ("NOT distinct orchestration semantics (unbuilt; CP-axis Class-1 back-flow)"). | True fan-out (parallel), generator/handoff (decentralized), multi-level dynamic delegation, evaluator-optimizer loop — i.e. **multi-agent parallel + cross-agent communication**. CP-axis contract (C-CP-25) design + build. | **HIGHEST** — this is the single biggest beyond-MVP gap; it *is* the "multi-LLM parallel / cross-comm" capability. | **BUILD** (large CP-axis design+impl program; design-fork-first per X-AL-3) |
| **B2** | **Multi-server MCP client: single-server only.** The harness connects to exactly one MCP tool server per bootstrap; the v1.15 config field type is **singular** (`mcp_client_host: MCPClientHost`, not a mapping). | `mcp_client_host_factory.py:11` ("v1.15 MVP supports [one server]"); `:21` (">1 server deferred; field type is singular; future-arc schema extension"); `:169` ("one host per first configured client"). | A mapping of named MCP servers + per-server discovery/routing/trust-tier (the `:194` trust mapping is also MVP-conservative). | **HIGH** — a real agent typically needs several tool servers concurrently. | **BUILD** (AS/runtime schema extension + factory) |
| **B3** | **HITL gate decision intelligence: always-on + full-palette, placeholder placement matrix.** The gate fires + writes ledgers (proven, arc #8), but its *decision* logic is MVP-stubbed: `_hitl_required` is **always True on a matching placement** (no conditional predicate), the response palette is **`DEFAULT_FULL_PALETTE` unconditionally** (no per-cell narrowing/escalation), and `HandoffContext`/placement composition is a placeholder. | `hitl_gate_composer.py:56-81` ("v1.11 MVP… the gate always fires… palette: DEFAULT_FULL… placement composition deferred to workflow-grammar arc"); `:181-185`. | §14.8.2 step-4c conditional gating + step-4d palette narrowing + the placement matrix + HandoffContext binding. Pairs with the **OQ-6 auto-timeout-degradation** producer (Bucket A). | **HIGH** — "smart HITL" is core to the OD/HITL value proposition. | **BUILD** (runtime + CP workflow-grammar arc) |
| **B4** | **Per-role / per-step dispatch: not runtime-indexed.** Both routing (`RoutingManifest.per_role_bindings`) and prompt selection (`PromptSelectionManifest.per_role_bindings`) carry a per-role binding **structurally**, but the role dimension is **discarded at dispatch** — MVP uses a single default role. | `prompt_selection_manifest.py` (per-role "runtime-deferred at the consumer site"); routing's own `per_role_bindings` has no runtime indexer (role discarded at `llm_dispatch.py:489` until R-300, per PR #509 record). | Thread `AgentRole` (and per-step override) through dispatch so per-role model + per-role prompt actually take effect. | **MEDIUM-HIGH** — gates per-agent specialization in multi-agent topologies (composes with B1). | **BUILD** (runtime dispatch indexing; small once B1 motivates it) |
| **B5** | **Memory backend: per-deployment-surface selection stubbed.** The backends exist + are live (R-830: SQLite/S3/Neon), but `resolve_backend(deployment_surface)` **ignores its argument** and returns the single pre-resolved backend. | `memory_tool_registry.py:5` ("MVP: resolve_backend(deployment_surface) ignores its argument"); `:28` ("factory landing owed at U-RT-80; v2.15 MVP stores a single pre-resolved backend"). | Surface-aware backend routing (the `U-RT-80` factory). | **MEDIUM** — backends work; only automatic surface→backend selection is stubbed. | **BUILD** (small — U-RT-80 factory) |
| **B6** | **Per-tool sandbox-tier driver selection + STDIO transport-floor.** Arc #1 (#503) landed per-*server-uniform* tier→driver selection; the §14.9.8 flat resolver still bypasses the full `sandbox_tier_floor` composition incl. the ADR-D2 §1.3 STDIO tier-3 transport-floor. | Runtime spec §14.9.8/§14.9.9 (Scope discipline — per-tool is future); CXA v2.20 §0.4 (STDIO tier-3 floor not enforced by the per-server-uniform resolver). | Per-tool tier resolution + STDIO transport-floor enforcement. | **MEDIUM** — security-posture refinement (drivers + per-server selection already live). | **BUILD** (bounded runtime arc) — *carried forward by arc #1's own scope note* |
| **B7** | **OD sampler: §9.2 conditional-row over-sampling.** The sampler always-samples *all* `files.operation` / `memory.operation` / `validator.fail.*` / `subagent.span` spans, not only the mutation/permanent/root variants the spec mandates. Conservative (samples more, never less). | `composite_sampler.py:21-37`. **(Per-tier base-rate 1.0/0.1/0.2 IS live — proven arc #5 at the bootstrap; this row is only the conditional-row refinement.)** | Per-attribute (`kind`/`permanence`/root) conditional sampling. | **LOW** — Class-3; cardinality reduction only. | **RATIFY-or-fold-into-Q1** (over-sampling is safe) |

**Bucket-B minor field-level scopes** (roll up under whichever arc touches them; individually low): `workflow_driver_types.py` `parent_entry_hash` empty-string sentinel + `parent_gate_level` AUTO-default (per-step gate-level not operator-surfaced); `skills.py` cost estimate = description-length proxy (not token-counting, §14.17.7); `pause_resume` `StateSummary`/`PauseContextReader` capture is MVP-minimal (§14.14.7) + anchor-validation `U-CP-22` deferred.

- **B-INTERSTEP — inter-step DATA-flow channel (surfaced by U-CP-87 / arc #13; Codex [P1]; cross-topology, currently UNTRACKED → forward arc).** The driver threads **no data between steps** for ANY topology: the `SINGLE_THREADED_LINEAR` path builds `accumulated` step outputs but never passes them into `dispatch(...)`, and the `dispatch(binding, step, step_context)` signature carries no prior-output parameter. So a step output cannot reach a subsequent step's *input* at the driver level — e.g. `EVALUATOR_OPTIMIZER`'s generate-draft → evaluate-*input* and feedback → regenerate-*input* (the §25.11 "regenerate-with-feedback" content flow). At the driver this is correctly **control-flow only** (U-CP-87's plan-defined "non-hollow" bar = the per-step-prompt distinction, which is built+tested; the cleared U-CP-86 also threads no data). The DATA channel is a **runtime/dispatcher capability** (a shared run-context the dispatcher reads, or B4-adjacent per-step input composition) — NOT covered by B4 (per-role *selection*) and threading it via `StepExecutionContext`/the dispatcher Protocol would be an X-AL-3 spec extension. **Owner = a future runtime data-flow arc (design-fork-first).** Load-bearing: MEDIUM-HIGH for genuinely effective evaluator-optimizer / orchestrator-workers with real model dispatchers (composes with B1 + B4). Rec: **BUILD** (runtime arc; design-fork-first per X-AL-3). **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (runtime spec v1.58 → v1.59 NEW §14.21 C-RT-29 + `harness-runtime/src` + `harness-cp/src` + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the *"shared run context the dispatcher reads"* §25.11 itself named: a NEW run-scoped `InterStepOutputChannel` (a plain by-reference holder — the `CostRecordAccumulator` CA #625 precedent, NOT a Pydantic-copied container), bound at stage 5 LOOP_INIT only when `RuntimeConfig.inter_step_data_flow=True` (default `False` → byte-identical). The CP driver records each completed step's output (`SINGLE_THREADED_LINEAR` `accumulated` site + `EVALUATOR_OPTIMIZER` loop) via `getattr`-dynamic `_record_inter_step_output` (the `cp_is_wiring` idiom — harness-cp does not import the runtime holder); the **genuine consumer** is the C-RT-15 LLM dispatcher, which injects `most_recent_output()` into the dispatched provider payload (threaded via the `cost_record_sink` precedent). The `dispatch` Protocol signature is UNCHANGED + §25.3.3.4 step-body-opaque PRESERVED → **NO operator gate** (additive + opt-in, no committed-invariant sacrifice; mechanism resolved by landed precedent → advisor, not council). **Non-vacuity is the deliverable (advisor):** the real dispatcher injection reaches the **actual provider call** (`test_lifecycle_llm_dispatch.py::test_inter_step_channel_injects_prior_output_into_provider_call` — NOT a stub) + the producer half is proven in a real EO driver run (`test_workflow_driver_evaluator_optimizer.py::test_evaluator_optimizer_dispatch_observes_prior_step_output` — evaluate sees the generate draft; regenerate sees the evaluator feedback). **Genuinely-complete sequential-write scope** (`SINGLE_THREADED_LINEAR` + `EVALUATOR_OPTIMIZER`); **three follow-ons registered (NOT silent-deferred):** concurrent-fan-out recording for the 4 remaining non-linear strategies via the #648 buffered-branch drain (**`B-INTERSTEP-NONLINEAR`**, ADR-F2) + cross-step resume rehydration (composes with **`B-ENGINE-OUTPUT-REPLAY`** — the F2 `EntryPayload` carries only a `response_hash` digest, not the activity output; EO's within-loop flow is resume-safe) + **per-run channel isolation (`B-INTERSTEP-PERRUN-ISOLATION`)** — a `ContextVar`-scoped channel that closes THREE things: REMOVES the v1.59 single-flight serialization (below) + closes the **timeout-zombie** (Codex r5 — a run exceeding `drain_timeout_seconds` leaves a non-cancellable `to_thread` worker still recording after the lock releases) + closes the IDENTICAL bootstrap-scoped shared-holder exposure `cost_record_accumulator` carries (CA #625 — a concurrent-daemon cost-rollup cross-run mix, both at completion AND under the same timeout-zombie trigger; observability-additive, lower-severity, same root). Systemic across every bootstrap-ctx holder (a channel-only run-key = false-complete); registered because B-INTERSTEP is the first capability whose *correctness* (not just observability) needs it. **Daemon-scope hardening (Codex out-of-family, 3 rounds):** the bootstrap-allocated channel is run-scoped CONTENT, so a reused `HarnessContext` (daemon-client U-RT-108) leaks across `run_workflow` invocations — invariant 7 is honestly **bounded** to: (a) *sequential* reuse closed by a per-run `reset()` (r3); (b) *concurrent-within-drain-timeout* closed by a per-running-loop single-flight **lock** wrapping `[reset + execute]` when the channel is bound (r4 — opt-out default never locks → full concurrency unchanged); (c) the *concurrent-with-timeout* `to_thread`-zombie (r5) = a SYSTEMIC reused-ctx residual NOT closeable by a lock → registered as `B-INTERSTEP-PERRUN-ISOLATION` above (real-but-accepted; triage+register, NOT looped to Codex-green). Gates: pyright 0/0/0; ruff; harness-runtime + harness-cp non-e2e green; daemon-concurrency-serialization test + negative control; no §5.2-hash change (ephemeral run-OUTPUT). advisor (full-transcript — non-vacuity-is-the-deliverable + resume/scope/Option-B + r5-residual-triage decisions) + Codex out-of-family (5 real bugs across 4 rounds + the r5 systemic-residual surface: `[[hooks-codex-pilots-decorrelation-validated]]`). Fork: `.harness/class_1_fork_b_interstep_data_flow.md`; clearance `Spec_Harness_Runtime-v1_59-cleared-2026-06-18.md`. `[[r-cxa-seam-wiring-is-producer-discovery]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[stale-carry-text-disposition]]`.

- **B-FANOUT-PAUSE — resumable fan-out `pause` cascade (surfaced by U-CP-88 / arc #14; two decorrelated reviewers [P1]; UNTRACKED → forward arc).** Cleared CP spec §25.15.1 maps `cascade_policy=pause` → `RunStatus.PAUSED` "composing with C-CP-26 PauseResumeProtocol + C-RT-30 `api.resume`". For the LINEAR path this works (capture a position-only `PauseSnapshot`, resume from its `step_index`). For a **fan-out** it is not yet materializable: (1) resume reconstruction is **ledger-based** (§25.15.2 obl. 7 — read each branch's persisted `terminal_status`, skip the terminal branches, re-dispatch the rest), but the **resume-re-entry path does not exist** (the non-linear strategies deliberately bypass the §25.3 linear-only prefix-replay — the strategy is resume-blind by design); (2) completed-branch **outputs** are not persisted for a resume merge (the ledger carries causality + terminal_status, not the dispatch OUTPUT). Returning a `PAUSED` without (1)+(2) advertises a resumability the harness cannot honor (`api.resume` would get `pause_snapshot=None`) → a false-resumable `PAUSED`, the silent-degradation mode. **Interim (landed at arc #14):** the U-CP-88 `pause` branch fails HONESTLY → `RunStatus.FAILED` + `orchestrator-workers-pause-resume-not-yet-materialized` fail_class (NOT a false-PAUSED); audit record + salvaged outputs persist. The interim **deviation** is recorded at `.harness/class_3_fanout_pause_resume_not_yet_materialized.md` (Class 3, log-and-proceed). **The build:** (a) the fan-out resume-re-entry (obl. 7 ledger-skip); (b) completed-branch-output persistence (candidate: `PauseSnapshot.state_summary`); (c) capture a `PauseSnapshot` + return `PAUSED`; (d) the final §25.15.1 shape decision (resumable-PAUSED vs a distinct fan-out disposition). **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3).** Load-bearing: MEDIUM (TEAM_BINDING-default reachability on worker failure; the honest-FAILED interim is non-broken). Rec: **BUILD** (CP-strategy + runtime resume arc; design-fork-first). On landing, the §25.15.1 deviation closes and the `pause` branch flips FAILED → PAUSED. **✅ BUILT for `ORCHESTRATOR_WORKERS` (this arc, 2026-06-19, the ELEVENTH standalone `B-*` arc) — bundled-absorption (CP spec v1.41 → v1.42 C-CP-26 §26.2 `FanOutResumeState`/`FanOutBranchResumeState` + `PauseSnapshot.fan_out_resume` + §25.15.1 materialization note + `harness-cp/src` + `harness-runtime` e2e + by-execution tests + fork doc `class_2_fork_b_fanout_pause_resumable_fan_out.md` + clearance `Spec_Control_Plane-v1_42-cleared-2026-06-19.md`), NO operator gate.** The genuine design-fork-first surface (advisor) was piece (b) completed-branch OUTPUT recovery — it breaks R-CC-1 design §1.1's position-only model; reading §1.1 at source resolved it to a **self-documented §6 re-open trigger** ("a future execution model … would need a state-restoration story + a durable store carrying more than the position-only PauseSnapshot"), NOT an explicit forbidding invariant (contrast B4-Slice-4 §14.5.3 inv 2 → gated) → adopt-and-note + clearance under FULL-SPEC, no gate. **Mechanism:** a NEW `FanOutResumeState` (orchestrator output + the terminal branches' dispositions+outputs + `worker_count`) in `PauseSnapshot.fan_out_resume` (additive default-`None`), **COVERED by `snapshot_hash` when present** (the resumed aggregate trusts the recovered outputs → a tamper fails `attempt_resume` → `CP-FAIL-PAUSE-SNAPSHOT-CORRUPTION`; key added to the canonical hash dict ONLY when non-`None` → existing snapshots byte-identical). On a worker failure under `pause` with a bound `pause_resume_protocol`: capture the snapshot, return `RunStatus.PAUSED`; without the protocol → FAILED + `orchestrator-workers-pause-resume-protocol-not-bound` (honest detect-then-refuse, no false-PAUSED). Resume threads `execute_workflow(pause_snapshot_input=) → _execute_workflow_body(resume_snapshot=) → _execute_orchestrator_workers(resume_snapshot=)`: recover the orchestrator + terminal outputs, re-dispatch only the absent ordinals (obl. 7), compose the fused aggregate; a re-dispatched worker failing again re-pauses with the unioned terminal set. **Pause semantic:** in-flight finish → terminal; not-yet-dispatched LEFT re-dispatchable (the cascade-cancel obl-4 `cancelled` scan deliberately NOT run on `pause`); a failed worker → `completed` terminal (dispatch-boundary) → not re-dispatched. **Non-vacuity is the deliverable (advisor):** the real-entry-point resume witness through `execute_workflow(pause_snapshot_input=)` (the path `api.resume` drives — NOT a direct `_execute_orchestrator_workers` proxy: terminal-skip + re-dispatch + fused aggregate) + the gold-standard full-runtime `api.resume` e2e (`test_b_fanout_pause_resume_e2e.py`: capture → JSON round-trip → `api.resume` → SUCCESS, only worker-1 re-dispatched) + a hash-tamper negative control + an empty-branches negative control (persistence load-bearing) + the `worker_count` material-diff guard + the flipped interim test. **Scope = `ORCHESTRATOR_WORKERS` only** (the Class-3 deviation is filed only for it). **Four follow-ons registered (grounded by direct read — each carries its own `*-pause-resume-not-yet-materialized` FAILED branch, `[[subagent-landscape-reports-need-regrounding]]`):** `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` + `B-FANOUT-PAUSE-PARALLELIZATION` (sibling fan-out-barrier strategies) + `B-HANDOFF-PAUSE` (DECENTRALIZED_HANDOFF single-owner handoff-pause, a different shape) + `B-HIERARCHICAL-PAUSE` (HIERARCHICAL_DELEGATION resume crosses the child-bootstrap recursion boundary). Gates: pyright 0/0/0 · ruff · harness-cp 1081 passed + 1 xfailed · harness-runtime 1957 passed / 21 skipped / 1 xfailed. advisor (full-transcript — the §1.1-break identification + gate discriminator + hash-coverage + scope-grounding) + out-of-family Codex (pre-merge). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[full-chain-witness-not-half-proofs]]` · `[[stale-carry-text-disposition]]`.

  - **`B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / `B-FANOUT-PAUSE-PARALLELIZATION` / `B-HANDOFF-PAUSE` / `B-HIERARCHICAL-PAUSE`** (registered forward arcs, from B-FANOUT-PAUSE 2026-06-19). The four non-`ORCHESTRATOR_WORKERS` non-linear strategies' `cascade_policy=pause` paths still fail HONESTLY (`*-pause-resume-not-yet-materialized` / `decentralized-handoff-pause-resume-not-yet-materialized`). EO + PARALLELIZATION reuse the fan-out-barrier shape (the `FanOutResumeState` carrier + the skip-terminal/recover-output logic are strategy-agnostic — each needs its own capture wiring + aggregate-merge + tests); DECENTRALIZED_HANDOFF is a single-owner sequential handoff-pause (a distinct, non-fan-out resume shape — a stage cursor, not a branch set); HIERARCHICAL_DELEGATION reuses ORCHESTRATOR_WORKERS for *execution* but its *resume* crosses the child-bootstrap recursion boundary (the `ChildWorkflowRunner` seam) → genuinely separate. Rec: **BUILD** each (design-fork-first per X-AL-3 where the resume shape is new). On each landing, the `class_3_fanout_pause_resume_not_yet_materialized.md` deviation closes for that strategy.

- **B-ENGINE-OUTPUT-REPLAY — output-carrying event-history substrate + cached-output replay (surfaced by U-CP-93 / arc E-impl-1; advisor [blocking, pre-overload]; UNTRACKED → forward arc).** Cleared CP spec C-CP-08 §8.1 `engine_replay` mandates *"activity outputs cached and replayed; no re-execution of activities."* At HEAD only the **"no re-execution"** half is materializable (the driver skips the contiguous materialized prefix — `_determine_event_replay_resume_at` → resume_at via the F2 `idempotency_key` join, the prefix is not re-dispatched). The **"outputs cached and replayed"** half is degenerate: (1) **no output-carrying substrate** — the F2 `EntryPayload` (C-IS-07 §7.1) carries no activity output, only a `response_hash` digest (the CP plan v2.34 U-CP-93 Note's "cached outputs being the entry payloads" is empirically false — Finding 1 at `.harness/r-fs-1-e-impl-1-finding.md` §1); (2) **no consumer** — the driver threads no inter-step data flow (the registered **B-INTERSTEP** arc; `accumulated` is never read downstream). So EVENT_SOURCED_REPLAY (U-CP-93) — and WAL_SEGMENT's `segment_replay` at E-impl-2 — is currently **behaviorally identical to `SAVE_POINT_CHECKPOINT` skip-prefix** (the same accepted bar the landed save-point class has; NOT a new degeneracy). **Interim (landed at E-impl-1):** EVENT_SOURCED_REPLAY ships as honest **resumption-routing** (in-scope + dispatch branch + bare RESUMPTION + no-re-fire), with the degeneracy surfaced (finding doc) and the `_determine_event_replay_resume_at` helper as the named extension seam — NOT a false-distinct vacuous class. **The build:** (a) a hand-rolled **output-carrying event-history substrate** (per I-6, no vendored Temporal/DBOS/Restate — a dedicated append-only output log, or an IS `EntryPayload` output-carrier field via design-fork) storing per-step activity outputs; (b) compose with **B-INTERSTEP** so post-resume steps deterministically observe replayed prior outputs; (c) the genuine replay-into-`accumulated` at the `_determine_event_replay_resume_at` seam. **Owner = a follow-on R-FS-1 child arc within / after the E sub-program (design-fork-first per X-AL-3, since the output-carrier substrate is a new surface).** Load-bearing: MEDIUM-HIGH — it is what gives `event-sourced-replay` (+ `segment_replay`) their distinguishing §8.1 semantic over save-point; composes with B-INTERSTEP + B1. Rec: **BUILD** (CP + runtime/IS substrate arc; design-fork-first). On landing, the §8.1 cached-output-replay clause closes for the durable engine classes. **✅ BUILT for LINEAR EVENT_SOURCED_REPLAY (this arc, 2026-06-19, the TWELFTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.62 → v1.63 NEW §14.23 C-RT-32 `EngineOutputStore` + clearance marker + fork doc `class_2_fork_b_engine_output_replay_output_carrying_substrate.md` + `harness-runtime/src` store+bootstrap + `harness-cp/src` producer+rehydrate + by-execution tests), NO operator gate.** Substrate-shape fork **probe-resolved to a dedicated runtime store** (advisor not council): the IS F2 `EntryPayload` stores a `response_hash` digest BY DESIGN; extending it ripples the §5.2 hash + JSONL + IS contract → foreclosed by I-6 + ADR-F2 + the `JournalWorkflowPauseStore` precedent. Mechanism: a hand-rolled crash-safe `EngineOutputStore` (per-run JSONL keyed by `run_idempotency_key`, fsync + torn-append self-heal, co-located under `<state_ledger_dir>/engine-output`), opt-in `RuntimeConfig.engine_output_replay`; the CP driver records each step output BEFORE the F2 ledger-append (**RESERVE-before-COMMIT** — the store always holds ≥ the ledger's materialized prefix, the advisor-caught store↔ledger SKEW rule verified at the live append site); on an EVENT_SOURCED_REPLAY resume the driver rehydrates the inter-step channel from the stored prefix **driven by `resume_at`** + **FAIL-CLOSED** on a missing output / stored-step_id≠body identity mismatch (the B-FANOUT-PAUSE symmetry). **§1.1 reuse, NO gate:** carries outputs across a resume (breaks the R-CC-1 §1.1 position-only model) but §1.1 is a self-documented §6 re-open trigger, not a forbidding invariant → adopt-and-note (the B-FANOUT-PAUSE precedent). **Non-vacuity is the deliverable:** the **full-chain witness** `test_b_engine_output_replay_rehydrated_output_reaches_real_provider_call` — a prior run's output durably stored ON DISK → the CP rehydrate into a fresh channel → the REAL `RuntimeLLMDispatcher` injects it → the recording provider RECEIVES the recovered output (NOT a channel-unit proxy) + the CP rehydrate witness (a channel-reading dispatcher proves the first re-dispatched step SEES the recovered predecessor) + a NEGATIVE CONTROL (no store → None upstream) + 2 fail-closed tests + 7 store crash-safety units. **Narrow surface** (the channel exposes only `most_recent_output()`); **capability-built, NOT "fires in production"** (observable only when `engine_output_replay` + `inter_step_data_flow` + EVENT_SOURCED_REPLAY compose). **LINEAR EVENT_SOURCED_REPLAY only;** registered forward arcs: `B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT` (WAL_SEGMENT shares the store, E-impl-2) + the non-linear resume-blind strategies (the B-FANOUT-PAUSE-family resumption arcs). Gates: pyright 0/0/0 · ruff · harness-cp 1087 passed + 1 xfailed · harness-runtime full green · cxa+core 64 · overlay 31/31 seams. advisor (full-transcript — substrate-fork probe-resolution + the store↔ledger skew rule + §1.1 reuse) + out-of-family Codex (pre-merge). `[[grounding-reveals-claude-closeable-slice-close-honestly]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[full-chain-witness-not-half-proofs]]` · `[[probe-resolves-fork-prescribed-council]]`.

- **B-EFFECT-FENCE — at-most-once EXECUTION of non-idempotent step side-effects (effect-boundary fencing; surfaced by U-RT-123 / arc E-impl-3b; council + advisor; UNTRACKED → forward arc).** The U-RT-123 reconciler CAS (and every durable engine's resume) guarantees at-most-once **claim of a revision**, NOT at-most-once **execution** of the workflow steps the resume re-runs. A non-idempotent external effect (`git_push` / `send_email` / any side-effecting tool) fires *inside* the re-executed step, and only THEN can a stale/losing claimant abort — so a crash-then-retry or a cross-host concurrent resume can fire the same external effect twice. The F2 `idempotency_key` is on the `cp.resume-attempted` AUDIT entry + the per-step ledger dedup, **not** on the external effect at its sink (the effect is opaque to the harness ledger). **The build:** a hand-rolled (I-6 — no vendored Temporal/DBOS activity-dedup) **per-effect fencing discipline at every side-effect sink** — a fencing/dedup token (candidate: the run-scoped `(workflow, revision, step)` key) checked at the sink *before* the effect fires, so a re-executed step's effect is suppressed-or-idempotent rather than re-applied. Cross-cutting (NOT reconciler-specific — every durable engine class + every topology with external-effect steps); composes with the durable engines (E) + **F-CC**'s fenced bounded-synchrony lease (genuine multi-host AUTO recovery is reachable only once effects are sink-fenced) + **B-INTERSTEP**. **Interim (landed at E-impl-3b):** the reconciler fail-closes the crash-mid-resume window to HITL (§22.1) rather than risk a double-execution — honest, not a silent double-fire; the gap is the *exactly-once external effect* under a legitimate resume. **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3, since a sink-fencing surface is new).** Load-bearing: HIGH — it is the floor-(ii) "at-most-once execution" semantic the durable engine classes advertise; without it the durable engines are at-most-once-*claim* but at-least-once-*effect*. Rec: **BUILD** (runtime + AS tool-sink arc; design-fork-first). On landing, the floor-(ii) effect-idempotency clause closes for non-idempotent external effects. **✅ BUILT (this arc, 2026-06-18, the EIGHTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.59 → v1.60 NEW §14.22 C-RT-31 `RuntimeEffectFence` + `harness-runtime/src` + `harness-cp` test + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the U-RT-123 reconciler's crash-atomic `O_EXCL`/`os.link`/`fsync` `_claim_resume_revision` claim re-keyed to the per-(run, step, tool) `idempotency_key`, applied at the `RuntimeToolDispatcher` `call_tool` SINK: `try_reserve(idempotency_key)` BEFORE `call_tool` — the first dispatch wins (fires), any re-dispatch LOSES (a crash-then-resume re-run of an effected-but-uncommitted step — the `:2031` effect / `:2336` commit window `_determine_resume_at` re-dispatches — OR an in-process `RetryBreakerToolDispatcher` retry, which fail-fasts since the fence error is not a `_TRANSIENT_TOOL_DISPATCH_ERRORS` member) → `EffectFenceReservedUncommittedError` (fail-closed to §22.1, never a double-fire). **At-most-once, NOT exactly-once** (the fire-then-crash-before-commit window is ambiguous → conservative fail-close, mirroring the reconciler §22.1 posture); **COMMIT = the EXISTING per-step ledger entry** (one source of truth — the genuinely-new piece is only the RESERVE marker). Opt-in `RuntimeConfig.effect_fencing=False` default → byte-identical; the `StepDispatcher.dispatch` signature UNCHANGED (keys on the existing `step_context.parent_idempotency_key`). **The one C10⊥C11 fence-scope tension probe-resolved** by durable-resume scoping (no `ToolContract` field, no AS fork → advisor, not council). **Non-vacuity is the deliverable (advisor's #1 steer; cf. B-TOOL-GATE #653 wired-but-production-dead):** proven by-execution with NO proxy — a real `RuntimeToolDispatcher` over a counting MCP tool fires it EXACTLY ONCE across two same-key dispatches; a NEGATIVE CONTROL double-fires without the fence; a fresh-dispatcher-over-the-same-on-disk-fence-dir test is a genuine restart; the driver hands the sink a byte-identical key on the real `execute_workflow` resume path. **Three follow-ons registered (NOT silent-deferred):** `B-EFFECT-FENCE-DURABLE-AUTO` (auto-activate under durable engines) + `B-EFFECT-FENCE-PER-TOOL` (a `ToolContract` idempotency classification — cross-axis AS, design-fork-first) + `B-EFFECT-FENCE-HITL-ROUTE` (the §22.1 operator-resolvable pause + suppress-and-continue via `B-ENGINE-OUTPUT-REPLAY`, replacing the interim fail-closed FAILED raise). No §5.2-hash / IS / CP / OD contract / ADR change (the fence claim is a side file, not a state-ledger entry; I-6 + ADR-F2 honored). Fork: `.harness/class_1_fork_b_effect_fence_at_most_once_execution.md`; clearance `Spec_Harness_Runtime-v1_60-cleared-2026-06-18.md`. `[[r-cxa-seam-wiring-is-producer-discovery]]` · `[[durable-recovery-presence-validity-scope]]` · `[[test-bypass-as-runtime-truth-pattern]]`.

- **B-EDIT-CARRIER — HITL `EDIT` carrier-healing / structured-edit mutation discipline — ✅ BUILT 2026-06-19 (runtime spec v1.61 → v1.62 §14.8.2 step 4i + NOTE 6-ii; mutation discipline (A) JSON-decode-then-replace; `RT-FAIL-HITL-GATE-EDIT-DECODE` registered + interim `HITLGateEditCarrierDriftError` retired; `edited_proposal_hash` now POST-mutation [F3-03 closed]; full-chain witness + flipped negative control; NO operator gate — (A) PRESERVES replace-not-merge, probe-resolved, FULL-SPEC pre-authorized; fork RESOLVED).** (surfaced by U-RT-120 / arc #23 B3-impl-3; advisor [blocking]). Cleared runtime spec §14.8.2 step 4i + NOTE 6-ii mandate replace-not-merge: the operator's `edited_proposal` becomes the new `step.step_payload` *verbatim*; "consumers MUST treat it as authoritative replacement." The mandate presumes the CP-canonical **structured** carrier (`HITLGateResult.edited_proposal: Mapping[str, Any]`, `hitl_placement.py:197`; `WorkflowStep.step_payload` is itself `Mapping[str, Any]`). But the wired runtime ask-surface returns `AskUserQuestionResult.edited_proposal: str | None` (`ask_user_question_surface.py:86`), and MCP elicitation is **flat-schema** (primitives only — cannot deliver a nested Mapping, `mcp_backed_ask_user_question_surface.py:171-199`). So the structured-elicitation path (design-doc D-edit.A) is unreachable through the wired path → **D-edit.B**: the `str → Mapping` replacement is genuinely under-specified (the decode/mutation semantics NOTE 6-ii explicitly defers to "a future workflow-mutation-discipline arc"). **Interim (landed at arc #23):** the step-4i `EDIT` branch was silent non-compliance (`pass` → dispatch the step unchanged, dropping the operator's edit); it now raises `HITLGateEditCarrierDriftError` (runtime-shape, NOT pre-registered into the §14.8 `RT-FAIL-*` taxonomy) surfacing the str↔Mapping drift + citing the fork. The audit at step 4h still records the operator's edit faithfully (`edited_proposal_hash`) before the raise (symmetric to REJECT). The deviation is recorded at `.harness/class_1_fork_hitl_edit_carrier_drift_str_vs_mapping.md` (Class 1). **The build:** (a) pick the mutation discipline — JSON-decode-the-`str` (the IMPL-discretion `r_cxa_2._parse_edited_arguments` shape, no current step-payload-surface spec backing) vs structured-flat typed-field elicitation vs richer field-level patch/merge (the full NOTE 6-ii deferred family); (b) amend NOTE 6-ii (and/or mint a structured-edit contract) — a `design-substrate/` amendment carrying the fork as X-AL-3 back-flow + a clearance marker; (c) land **functional operator EDIT** (replace-not-merge actually applies); (d) register the `RT-FAIL-*` taxonomy code + retire the interim runtime-shape raise; (e) resolve the `edited_proposal_hash` "post-mutation payload hash" semantics for the no-mutation interim (reviewer F3-03 — the hash is currently over the *pre-mutation* operator `str`). **Owner = a follow-on R-FS-1 child arc (design-fork-first per X-AL-3).** Load-bearing: HIGH — `EDIT` is a spec-mandated 4-response-palette member; it ships **non-functional (raises)** until this arc, so functional operator EDIT is a committed R-FS-1 build, NOT an indefinite defer. Rec: **BUILD** (runtime + CP workflow-mutation-discipline arc; design-fork-first). On landing, the §14.8.2 / NOTE 6-ii deviation closes and the `EDIT` branch flips raise → apply.
  - **✅ BUILT 2026-06-19 — as-built scope = the wrap-time SYNC path** (the sole `EDIT` `step_payload` application site; the real-consumer witness `test_edit_decoded_payload_reaches_real_llm_dispatcher` drives the composer's inner = the REAL `RuntimeLLMDispatcher` over a recording provider → the DECODED Mapping is what the provider received, closing the half-proof gap per advisor pre-merge / `[[full-chain-witness-not-half-proofs]]`). Mutation discipline **(A) JSON-decode-then-replace** — foreclosed by the flat-`str` carrier + the committed replace-not-merge mandate + the arbitrary-nested target; (A) PRESERVES replace-not-merge → no operator gate (probe-resolved, FULL-SPEC pre-authorized). Runtime spec v1.62; `RT-FAIL-HITL-GATE-EDIT-DECODE` registered + interim `HITLGateEditCarrierDriftError` retired; `edited_proposal_hash` POST-mutation (F3-03 closed).
  - **Registered follow-on `B-EDIT-CARRIER-DURABLE-ASYNC-RESUME` (NOT built here).** The durable-async **resume** path (`hitl_gate_composer.py:1192-1194`: `resume_state.hitl_response is not None → return self._dispatch_inner(...)`) dispatches the resumed step UNCHANGED for **ALL** response types per the v2.24 AC #7 MVP "auto-approve the resumed gate" shape — the resumed-response integration into the 4h/4i flow is explicitly deferred per **FM-2**. So a durable-async EDIT (like a durable-async REJECT) is **silently not-applied** — a PRE-EXISTING documented limitation, NOT introduced by this arc and broader than EDIT (it spans EDIT/REJECT/RESPOND at resume). This follow-on composes with the broader FM-2 resume-response-integration arc; design-fork-first per X-AL-3. Rec: **BUILD** (when the FM-2 resume-integration arc opens).
  - **Registered follow-on `B-HITL-WRAP-FAIL-CLASS-SURFACING` (NOT built here; Codex [P2] decorrelated catch, advisor-pre-cleared as acceptable-by-precedent).** The new `RT-FAIL-HITL-GATE-EDIT-DECODE` is registered in the §14.8 taxonomy (fork item (d)), but the cp `workflow_driver.py` surfaces wrap-time HITL exceptions via the generic `step-failure: {type(exc).__name__}: ...` path — so the literal `RT-FAIL-HITL-GATE-EDIT-DECODE` string is not emitted in `fail_class` (a caller distinguishes the mode by the exception type name, not the RT-FAIL code). This is a **PRE-EXISTING architectural property** (harness-cp cannot import harness-runtime exception types; only cp-typed validator-gate errors map to explicit `RT-FAIL-*` strings — the wrap-time `HITLGateRejectedError` / `HITLCellExcludedError` have the SAME generic surfacing), NOT introduced by this arc; the spec §14.8 EDIT-DECODE row documents it honestly. The fix (a cp-driver runtime-exception→`RT-FAIL-*` mapping for ALL wrap-time HITL errors, e.g. via a shared marker/attribute the generic except reads) is cross-cutting beyond EDIT. Rec: **BUILD** (a small CP-driver fail-class-surfacing arc covering EDIT-DECODE + REJECT + TIMEOUT + AUDIT-COMPOSE consistently).

- **B-LAYER-BUDGET-OVERRIDE — per-layer time-budget wall-clock enforcement honoring per-workload / per-persona overrides (surfaced by R-impl-1 L3 timeout / arc R-impl-1; Codex [P2], spec-reject-with-evidence; UNTRACKED → forward arc).** Cleared CP spec C-CP-03 §3.1 materializes a **per-workload-class × per-persona-tier** budget tuning surface; `LayerBudget` carries `per_workload_override` + `per_persona_override` (U-CP-06, `layer_budget.py:59-66`) and `effective_budget(layer, workload, persona)` (`layer_budget.py:79-95`) resolves the override precedence. But `effective_budget()` is **built-but-vacuous workspace-wide** (`[[built-but-vacuous-reground-ledger-asis]]`: zero production callers — only its def + `test_layer_budget.py`), and the deterministic `route()` (`layered_routing_strategy.py:62-101`) does **NO wall-clock budget enforcement at all** — it consumes only the caller-supplied `budget_exhausted` frozenset and never reads `time_budget_ms`. So per-layer time-budget enforcement that *honors the override maps* is unmaterialized for **every** layer, not just L3. R-impl-1's L3 `asyncio.wait_for` (`routing_core_surface.py:268`, `_layer_time_budget_ms` at `:163-172`) is the **first and only** site that enforces `time_budget_ms` as a real timeout — and it reads the **flat** field, not the override-resolved value. **Why flat at R-impl-1 (not a defect):** the cleared CP spec v1.36 §2.5.3 explicitly bounds the L3 router by the **flat 200 ms** `LayerBudget` (pseudocode `:71` `= LayerBudget(LLM_AS_ROUTER).time_budget_ms / 1000`; §3/C-CP-03 **PRESERVED VERBATIM**; the prose "effective L3 timeout" at §2.5.2/§2.5.3 is glossed in-sentence as "the 200 ms `LayerBudget`", NOT the `effective_budget()` term-of-art). Honoring the override maps at R-impl-1 would be an **X-AL-3 silent design extension** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`; Codex is decorrelated but spec-blind, `[[insights-recs-workspace-blind]]`). **Structural proof override-wiring is a separate not-yet-designed arc:** `effective_budget()` reads `DEFAULT_LAYER_BUDGETS` (module global), **not** the `budgets` tuple `infer()`/`route()` receive — so the helper as-built physically cannot serve the L3 timeout site; if §2.5 intended override-resolution there, the helper would read the passed budgets. **Interim (landed at R-impl-1):** the L3 timeout uses the flat `time_budget_ms` (spec-faithful per §2.5.3); the override maps stay inert — NOT a new degeneracy (they were already vacuous workspace-wide; R-impl-1 does not regress them). **The build:** (a) decide the wall-clock budget-enforcement locus (the `infer()` L3 site is the first natural consumer; the sync+fast deterministic layers are a separate question) + a `budgets`-tuple-aware `effective_budget` variant keyed on `request.workload_class` + `request.persona_tier`; (b) if honoring overrides at L3 changes the §2.5.3 flat-budget contract, a CP spec leg is owed (design-fork-first per X-AL-3 + clearance marker). **Owner = a follow-on arc (the C-CP-03 §3.1 per-layer-budget tuning-surface activation; composes with R / CA).** Load-bearing: LOW-MEDIUM — operator-tuning refinement; the flat 200 ms is a safe spec-mandated default and no current deployment supplies overrides. Rec: **BUILD** (CP budget-enforcement arc; design-fork-first per X-AL-3 if it amends §2.5.3). On landing, the C-CP-03 §3.1 override-tuning surface becomes load-bearing for real per-workload/per-persona budget enforcement. **✅ BUILT (this arc, 2026-06-19) — bundled-absorption (CP spec v1.42 → v1.43 in-place C-CP-02 §2.5.3 amendment [the L3 timeout uses the §3.1 OVERRIDE-RESOLVED effective budget keyed on `request.workload_class`/`persona_tier`, not the flat field] + clearance marker + `.harness/class_2_fork_b_layer_budget_override_l3_effective_budget.md` + `harness-cp/src` [`effective_layer_budget_ms` — the `budgets`-tuple-aware resolver `effective_budget` couldn't be (it reads the module global); the L3 site honors it; orphaned `_layer_time_budget_ms` removed] + `harness-runtime/src` [the DORMANT `RuntimeLLMDispatcher.budgets` threading seam, default `DEFAULT_LAYER_BUDGETS`] + by-execution tests [`infer()` per-persona + per-workload override witnesses with a flat-default negative control, resolver units, dispatcher-seam threading witness]), NO operator gate — §3.1 affirmatively commits the override on `llm_as_router` → impl-to-cleared-spec; the prior in-code "X-AL-3 forward arc" note is overturned by re-grounding the cleared spec (advisor-confirmed; no ratified flat-only-at-L3 decision in the ledger), behavior-preserving by default (no override ⇒ byte-identical 200 ms). Production-dormant (`router=None` + DEFAULT budgets; live firing additionally gated by the UNOWNED routing-activation gate) — capability-built, NOT "fires in production" (the non-vacuity witness lives at `infer()`). advisor-steered (lead-with-§3.1 + drop-the-"effective"-reading + frame-as-amendment + non-vacuity-at-infer) + out-of-family Codex pre-merge. The deterministic-layer wall-clock enforcement + the R-impl-2 200 ms recalibration remain separate registered concerns.**
  - **R-impl-2 empirically extended this arc (2026-06-16; advisor pre-done catch; `[[test-bypass-as-runtime-truth-pattern]]`).** Two NEW findings the live-Ollama router e2e surfaced, beyond the original override-honoring scope: **(i) the default 200 ms L3 budget is empirically UNMEETABLE by a real LOCAL router** — even a *warm* local Ollama (llama3.2:3b, `format=json`, capped `num_predict`) needs seconds, not 200 ms. (NOT a claim the spec's §2.2 "50-200 ms" is wrong — that estimate is for a fast *hosted* router; it is **UNVERIFIED for hosted Haiku** because verifying it would fire the paid call, which R-impl-2 does not. Stated precisely: empirically false for local; unverified for hosted.) **(ii) `RuntimeLLMDispatcher` HARDCODES `DEFAULT_LAYER_BUDGETS`** (`llm_dispatch.py:584`) in its `infer()` call — there is no budget-threading seam — so even a correct per-workload/persona override mechanism (the (a) build above) **could not reach the L3 timeout through the production dispatcher** without ALSO threading `budgets` from construction (the same shape as the R-impl-1 `layer_decisions_override` seam). **The composition gap (why R-impl-2's e2e calls `infer()` directly, not through the dispatcher):** R-impl-1 proved dispatcher→`infer(router=…)` (mock router, 200 ms); R-impl-2 proves real-router→`infer()` (60 s budget). They **do not compose** into "a real router resolves L3 through the production dispatcher" — that path times out at 200 ms every call. This is OK to ship ONLY because production never reaches L3 today (`router=None` + DECLARATIVE always resolves). **Net: production L3 router activation depends on THREE things, all registered-forward, none silently assumed:** (1) **the routing-activation gate** (make DECLARATIVE conditional so traffic reaches the L3 sentinel; UNOWNED — both R-300 items RESOLVED without it), (2) **a realistic L3 budget** (this arc's calibration), (3) **dispatcher budget-threading** (this arc's build (a), extended: a `budgets` construction param on `RuntimeLLMDispatcher`). Load-bearing revised **MEDIUM** (was LOW-MEDIUM): without (2)+(3) the L3 LLM_AS_ROUTER layer is unreachable in production *even after* R-300-second-provider. Add the dispatcher `budgets`-threading seam to the build set.

- **B-TOOL-GATE — tool-step HITL gate site = the real per-server MCP-trust producer (surfaced by U-RT-131 / arc B2-plan; adversarial [F2-01] + advisor; UNTRACKED → forward arc).** Cleared CP spec §19.1.2 Producer ¶ + runtime §14.8.2 step-4c envision `GateLevelInput.mcp_trust_tier` populated "from the resolved owning MCP host's declared trust … via the v1.51 §14.9.10 routing index." But at HEAD the runtime HITL gate composer (`RuntimeHITLGateComposer`) is constructed for **only two host-less placements** — `hitl_inference` (`stage_5_loop_init.py:337`, `PRE_ACTION`) + `hitl_sub_agent` (`:431`, `SUB_AGENT_BOUNDARY`); `TOOL_STEP`s dispatch through `runtime_tool_dispatcher.py`, which composes **no** HITL gate (zero `gate_level`/`GateLevelInput`/`hitl` refs). **So no gate site has an owning MCP host** — the resolved-host feed has nothing to populate. **Interim (lands at B2-impl-3, U-RT-131):** the harmful `hitl_gate_composer.py:462` `LEVEL_0_REFUSE_REMOTE` constant is replaced with the **L3 AUTO-mapping no-floor default** (the host-less gate sites contribute no MCP-trust floor — §19.1.2 invariant 3, the `per_tool_gate_level`/O-CP-3 degenerate-default analog), so U-CP-98's axis composition is safe (non-regression); the axis is materialized in `gate_level()` but feeds a degenerate default in production. **The build:** a **tool-step HITL gate site** (a `RuntimeHITLGateComposer` at a tool placement, or gate composition inside `runtime_tool_dispatcher.py`) that (a) resolves the owning MCP host for a `TOOL_STEP` via the v1.51 routing index (U-RT-128), (b) feeds its D3-projected per-server `MCPTrustTier` (U-RT-129) into `GateLevelInput.mcp_trust_tier`, (c) so an L0-server tool actually floors its gate to `DENY` and an L3 to `AUTO`. **Owner = a follow-on R-FS-1 child arc** (a runtime gate-site surface; **likely impl-against-cleared-spec** since §19.1.2 Producer ¶ + step-4c already spec the input + the routing index/D3 projection land in B2 — confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: **HIGH** — it is what makes the §19.1.2 MCP-trust gate axis **non-vacuous in production** (U-CP-98 composes the axis, but every current gate site is host-less → AUTO; the real per-server DENY/ASK floors only bite once a tool-step gate exists). Composes with B2 (routing index + D3 projection) + the B3 HITL gate-composer machinery. Rec: **BUILD** (runtime gate-site arc). On landing, the §19.1.2 Producer ¶ realizes in production and the MCP-trust axis stops being a degenerate-default. **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (`harness-runtime/src` + by-execution tests + fork doc; NO design-substrate edit, NO operator gate).** Mechanism = Option A (the spec's named producer): a THIRD `RuntimeHITLGateComposer` wraps the tool dispatcher at the **registry path** (`tool_step_dispatcher = facade(hitl_tool(ctx.tool_dispatcher))`; `ctx.tool_dispatcher` itself NOT mutated — the R-CXA-2 producer loop + provider-turn `hitl_tool_loop` still read the un-gated dispatcher), `applicable_placements={PRE_ACTION}`, same dep set as the inference/sub-agent composers + a NEW `mcp_trust_tier_resolver` (the `blast_radius_resolver` sibling; `step_mcp_trust_tier.py` — resolves `tool_id`→owning host→`MCPClientHost.trust_tier` via a host-scan, collision-guarantee-aligned with dispatch). **Impl-against-cleared-spec** (§14.8.2 step-4c already names `mcp_server_trust_tier`; `GateLevelInput.mcp_trust_tier` + `MCP_TRUST_GATE_LEVEL_FLOOR` exist; no new contract/field/invariant → no operator gate). **HONEST three-claim scope (the "non-vacuous in production" body claim above is CORRECTED here):** (1) **wiring correct** — proven through real `run_bootstrap` (`test_bootstrap.py`: TOOL_STEP registry = `facade→hitl_tool[resolver bound]→ctx.tool_dispatcher`); (2) **axis composes when the gate fires** — proven by-execution (L0→DENY→palette narrows {REJECT,RESPOND}→ask→REJECT raises; L3→AUTO→delegate; real-resolver-over-real-hosts integration); (3) **production gate-firing awaits the per-step placement producer** — the **Codex-caught [P1] decorrelated finding** advisor + author missed: the wrap-time gate is placement-driven (`getattr(step,"hitl_placements",())`), but `WorkflowManifestEntry.hitl_placements` is WORKFLOW-level (C-CP-17 §17.3) and NEVER bound onto the per-step `WorkflowStep` (frozen+`extra="forbid"`), so NO wrap-time gate (inference/sub-agent/tool) fires through the real manifest→driver path — a **PRE-EXISTING gap shared by all gate sites** (even the existing inference/sub-agent e2e tests attach placements via a `_StepWithPlacements` proxy). Registered as `B-HITL-PLACEMENT-PER-STEP-PRODUCER` (below). DENY-reachability verified sane (advisor pre-build): DENY narrows the palette to {REJECT,RESPOND} + invokes the ask surface (structural-rejection-with-HITL), NOT a stub. **PRE_ACTION forward-coupling:** the tool gate keys on `{PRE_ACTION}` like inference, so once the producer lands a workflow-level PRE_ACTION placement gates BOTH inference and tool steps — the producer arc must account for this.

- **B-HITL-PLACEMENT-PER-STEP-PRODUCER — bind workflow-level `hitl_placements` onto the per-step `WorkflowStep` so the wrap-time HITL gates fire in production (surfaced by R-FS-1 `B-TOOL-GATE`, 2026-06-18; Codex [P1] decorrelated, advisor-confirmed; UNTRACKED → forward arc; sharpens the B3 "placement composition is a placeholder" residual above).** The wrap-time HITL gate composer (`RuntimeHITLGateComposer`, all three sites — inference `PRE_ACTION`, sub-agent `SUB_AGENT_BOUNDARY`, tool `PRE_ACTION`) fires only when `getattr(step, "hitl_placements", ())` is non-empty (`hitl_gate_composer.py` step 1). But `hitl_placements` is declared at the **WORKFLOW** level (`WorkflowManifestEntry.hitl_placements`, "Declared per workflow per C-CP-17 §17.3"; the loader's `_WorkflowSection.hitl_placements`), while the per-step `WorkflowStep` the driver dispatches is frozen + `extra="forbid"` and carries **only** `step_id`/`step_kind`/`step_payload` — and **NO src binds the workflow-level placements onto the per-step steps**. So `step.hitl_placements` is **always `()`** through the real `WorkflowManifestLoader`→driver path → **NO wrap-time HITL gate fires in production for ANY step kind.** The entire wrap-time gate (the B3 smart-HITL machinery: conditional gating, palette narrowing, the MCP-trust floor `B-TOOL-GATE` just wired, the auto-approve policy) is production-unreachable today; every existing gate e2e test (`test_run_workflow_elicitation_e2e.py:234-250`, the inference/sub-agent gate tests) attaches placements via a `_StepWithPlacements` TEST PROXY for exactly this reason — `[[test-bypass-as-runtime-truth-pattern]]` at the workspace scale. **The build:** a workflow-binding-time producer that resolves the workflow's declared `hitl_placements` to the per-step gate-input the composer reads — either (a) a richer per-step carrier the driver dispatches (a `WorkflowStep` placements field, or a side-channel `dict[StepID, tuple[HITLPlacement,...]]` the composer consults), or (b) a per-step placement schema in the manifest, with the workflow→step application semantics (do workflow-level placements apply to ALL steps? a per-step `step_kind`/position match?). **Spec'd-ness — determine at arc-open:** the placement DECLARATION is spec'd (C-CP-17 §17.3 workflow-level; the composer docstring cites "workflow-binding-time per U-CP-13 + U-CP-38"), but the workflow→per-step BINDING mechanism + application semantics may be under-specified → if spec'd, a forward build-arc; if the binding semantics are unspec'd, **design-fork-first per X-AL-3** (a `design-substrate/` amendment + clearance marker). **PRE_ACTION coupling (from `B-TOOL-GATE`):** inference + tool composers both key on `{PRE_ACTION}`, so the producer must decide whether a workflow-level PRE_ACTION placement gates inference, tool, or both. **Owner = a follow-on R-FS-1 child arc** (the B3 "CP workflow-grammar arc" residual; cross-cutting — all gate sites). Load-bearing: **HIGH** — it is what makes the ENTIRE wrap-time HITL gate (inference/sub-agent/tool) fire in production; without it the major B3 + `B-TOOL-GATE` machinery is wired-but-production-dead (axis composes correctly, gate never triggers on the real path). Rec: **BUILD** (CP workflow-grammar + runtime binding arc; spec'd-ness-gated per above). On landing, all three wrap-time gate sites fire through the real manifest→driver path and `B-TOOL-GATE`'s MCP-trust floor becomes non-vacuous in production. **✅ BUILT (this arc, 2026-06-18, the NINTH standalone `B-*` arc) — bundled-absorption (runtime spec v1.60 → v1.61 §14.8.2-step-1 reconciliation + CP spec v1.40 → v1.41 NEW §25.3 `StepExecutionContext.hitl_placements` field + `harness-cp/src` + `harness-runtime/src` + by-execution tests + fork doc + 2 clearance markers), NO operator gate.** **Spec'd-ness resolved at arc-open (advisor-decomposed, 2 rounds):** the FOLD semantics (a workflow placement applies to all matching step kinds; the composer filters by `applicable_placements`) + the PRE_ACTION → {inference, tool} coupling are **SPEC'd** at runtime §14.8.1 (wrap-asymmetry footnote "overlays per `WorkflowManifestEntry.hitl_placements`") + §14.8.4 → impl-to-cleared-spec, NO fork-halt. The one under-specified leg = the read-surface/producer (§14.8.2 step-1 named `step.hitl_placements`, structurally unpopulatable on the frozen 3-field `WorkflowStep`) → reconciled as a **faithful materialization** (the v1.9 "populated at workflow-binding time per U-CP-13" always referenced a producer). Mechanism = read surface **`StepExecutionContext.hitl_placements`** (Option B over (a) widening `WorkflowStep` [violates the §25.2 config/body split] and over carrying it on `StepEffectiveBinding` [whose `model_dump` feeds the §16.5.4 override outcome-hash — a hash-coherence regression]); the CP driver sets `hitl_placements=manifest_entry.hitl_placements` at all **5** per-step `StepExecutionContext` construction sites (linear + the 4 non-linear roots; branch children inherit via `compose_branch_child_context`'s `model_copy`) — the `tenant_id`/`parent_gate_level` binding-fix precedent; the composer reads `step_context.hitl_placements` (`getattr(step,...)` preserved as a test-proxy compat fallback only). **Import cycle broken** by moving `hitl_placement`'s annotation-only `workflow_driver_types` import under `TYPE_CHECKING` (the `hitl_gate` signature is string-annotated via `from __future__ import annotations`). **NO operator gate** — additive + opt-in (default `()` → byte-identical, gate fires only on a declared placement), no committed-invariant sacrifice, no new contract/fail-class/manifest field. **Non-vacuity is the deliverable (cf. B-TOOL-GATE #653 wired-but-production-dead):** proven by-execution with NO proxy — a PLAIN `WorkflowStep` + a `step_context` carrying a PRE_ACTION placement → the composer fires the gate (surface invoked); a NEGATIVE CONTROL (empty `step_context` placements → no surface call, no HITL spans, no ledger/audit write); the producer proven through the real `execute_workflow` on the linear path (direct construction) AND DECENTRALIZED_HANDOFF (inherited via `compose_branch_child_context`). **Scope — wrap-time gates only:** VALIDATOR_ESCALATION fires via the §14.15 mid-step re-entry path on validator outcome (`next_action == ESCALATE_HITL`), NOT a step-read placement, + its gate dispatch is a separate future arc → OUT of scope. **One follow-on registered (NOT silent-deferred):** the per-step `StepOverride.hitl_placement` override fold (silent at C-CP-06 §6.2, which specifies only `f3_invocation` overrides) → `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD` (below). Decorrelated review: advisor (2 rounds — fork-vs-impl decomposition forced the §14.8.x read that resolved it to impl-to-cleared-spec; the no-gate + scope-split determination) + out-of-family Codex (pre-merge). Fork: `.harness/class_1_fork_b_hitl_placement_per_step_producer.md`; clearances `Spec_Harness_Runtime-v1_61-cleared-2026-06-18.md` + `Spec_Control_Plane-v1_41-cleared-2026-06-18.md`. `[[cleared-spec-resolves-it-before-first-principles-fix]]` · `[[spec-prose-plan-body-drift-pattern]]` · `[[new-surface-audit-hash-and-config-not-carrier]]` · `[[test-bypass-as-runtime-truth-pattern]]`.

- **B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD — compose the per-step `StepOverride.hitl_placement` override with the workflow-level placement tuple (surfaced by `B-HITL-PLACEMENT-PER-STEP-PRODUCER`, 2026-06-18; advisor-confirmed scope split; UNTRACKED → forward arc).** The producer arc surfaces the WORKFLOW-level `WorkflowManifestEntry.hitl_placements` (a tuple, applies to all cells per §17.1) onto every step's `StepExecutionContext`. But the manifest ALSO carries a per-step override surface: `StepOverride.hitl_placement: HITLPlacement | None` (singular), resolved into `StepEffectiveBinding.hitl_placement` by `resolve_step_binding` (override-only — it does NOT fold the workflow tuple). That resolved per-step override placement is currently **DEAD** (not consumed by the composer — placements are read off `step_context`, not `binding`). **The build:** decide + implement how the singular per-step override composes with the workflow tuple for a step — ADD (the override supplements the workflow placements), REPLACE (the override's placements supersede), or by-position-override (the override replaces the workflow placement of the same `position`, adds otherwise) — then thread the effective per-step placement set onto `step_context` for the overridden step. **Spec'd-ness:** the fold semantics are **SILENT at C-CP-06 §6.2** (which specifies only the `f3_invocation` per-step overrides — durability/checkpoint/replay; the `StepOverride.hitl_placement` field is an impl addition with no §6.2 prose backing) → **design-fork-first per X-AL-3** (a CP `design-substrate/` amendment specifying the fold + a clearance marker). **Owner = a follow-on R-FS-1 child arc.** Load-bearing: **LOW-MEDIUM** — workflow-level placements (the producer arc, now BUILT) cover the common case; per-step override of placements is a refinement (pipeline-automation per-stage HITL customization). Rec: **BUILD** (CP workflow-grammar arc; design-fork-first per X-AL-3 — the fold semantics are unspec'd). On landing, the dead `StepEffectiveBinding.hitl_placement` resolution becomes load-bearing + per-step HITL placement override works.

- **B-L2-EMBEDDING-ACTIVATION — Layer-2 EMBEDDING production activation + fastembed dependency promotion (surfaced by R-FS-1 arc R / L2-impl, 2026-06-16; advisor pre-build #1/#4 + the onnxruntime x86-mac wall; UNTRACKED → forward arc).** L2 is **BUILT** (Option B — the CP-pure k-NN classifier + the light fastembed realization + corpus + the `RuntimeLLMDispatcher.embedding_classifier` injection seam; CP units + a real-fastembed live e2e green). But it is **production-inert at HEAD by design**, for the SAME reason as the L3 router: `_declarative_echo` (`llm_dispatch.py`) **always** resolves DECLARATIVE, so `route()` short-circuits before EMBEDDING — the bound classifier is never reached. Production L2 activation depends on three registered-forward things, none silently assumed: **(1) the routing-activation gate** (make DECLARATIVE conditional so traffic reaches the EMBEDDING layer — the SAME gate L3 shares, per `B-LAYER-BUDGET-OVERRIDE` sub-bullet (1); UNOWNED — both R-300 items RESOLVED without it); **(2) the production factory wires `embedding_classifier=`** (today only a TEST constructs it; the `stage_5_loop_init` factory leaves it None, exactly like `router=None`) from an operator-configured corpus + the light embedding; **(3) promote the optional `[embedding]` extra to a REQUIRED dependency** of `harness-runtime` at that activation (today fastembed is an OPTIONAL extra so the base install stays light + provider-free lanes never pull onnxruntime — correct while L2 is dormant; but the operator chose Option B for self-sufficiency, so when L2 routes real traffic the embedding lib must ship by default). **A contained dep note (NOT a separate arc):** `onnxruntime` is capped `<1.24` in the extra because 1.24+ dropped the **x86-64 macOS** wheel (the operator's current Intel Mac; last x86-mac cp312 wheel = 1.23.2). Revisit/drop the cap when the operator moves to Apple Silicon (`[[operator-dev-machine-and-dep-preference]]`). **Owner = UNOWNED** — R-300-second-provider (the nominal owner once named here) is RESOLVED *without* doing the DECLARATIVE-conditional work (it added credentials + cross-family fallback only; routing-activation #213 kept DECLARATIVE behavior-preserving by design). This shared L2/L3 production-activation gate therefore needs a NEW owner once a second production provider makes routing-among-candidates meaningful; (2)+(3) ride it. Surfaced visibility-only at `.harness/roadmap_status.md` “## Remaining forward work”. Load-bearing: **MEDIUM** — the L2 capability is built + proven, but routes nothing in production until R-300; no current deployment supplies the second provider that makes routing-among-candidates meaningful. Rec: **(1)/(2)/(3) fold into the (currently UNOWNED) routing-activation gate** (R-300-second-provider, its former nominal owner, is RESOLVED without it) (no design-fork — L2-sync needed none; pure impl + a dep-required bump). On landing, EMBEDDING becomes a live cheapest-deterministic routing layer between DECLARATIVE and LLM_AS_ROUTER. **▶ DISPOSITION RESOLVED (2026-06-19, advisor decision-rule): BUILD opt-in/default-off (the routing-activation gate is the LAST buildable standalone arc).** Re-grounding corrected two framings in the entry above: **(a) the GOVERNING section is C-CP-02 §2.2, NOT §2.5.6** — §2.5.6 governs the L2 CONTRACT SHAPE (sync-vs-async, already spent by Option B); §2.2's "When it resolves" table CLEARS the activation condition: *DECLARATIVE (manifest) resolves IFF "the manifest binds the (agent_role, workflow_class, step) tuple"; EMBEDDING resolves when "the manifest does NOT bind AND classifier confidence exceeds threshold"; L3 when all fall through.* So the always-resolve `_declarative_echo` is the **MVP behavior-preserving shortcut**, NOT the §2.2 contract — making it decline on a manifest-miss is **impl-to-cleared-spec**, NOT design-fork-first (the prior "no design-fork" was right for the wrong reason; the OPT-IN mechanism + precise decline-semantics still warrant a small CP spec leg, like B-LAYER-BUDGET-OVERRIDE's §2.5.3). **(b) #213 is an UNBUILT MVP GAP, not a ratified-don't-build** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`: classify the decision, not the ledger paraphrase) — the forward register frames #213 as "routing activation RESOLVED; the multi-provider EXERCISE is B-2 | impl + operator creds"; #213 deferred the live EXERCISE (creds-gated), §2.2 specs the MECHANISM. **The build (advisor decision-rule: §2.2-spec'd ∧ #213-is-MVP-gap ∧ non-dormant-stub-witness-constructible → BUILD opt-in/default-off):** (1) `RuntimeConfig.routing_activation: bool = False` (both env loaders per `[[runtimeconfig-scalar-needs-both-env-loaders]]`); (2) flag-on `_declarative_echo` declines (returns `None`) when the manifest does not bind the tuple → `route()` falls through to the (built) EMBEDDING classifier; default-off ⇒ always-resolve, byte-identical to #213 → **no reversal-gate, zero blast radius on existing deployments** (highest-blast-radius arc — default-off is NON-NEGOTIABLE per advisor); (3) factory-wire `embedding_classifier=` when flag-on. **Non-vacuity = a non-dormant 2-candidate STUB witness** (flag-on + a manifest-miss request + a 2-candidate stub corpus → EMBEDDING routes to the non-default model) — advisor's distinction: the WITNESS needs 2 candidates (stubs suffice, NO real 2nd provider), PRODUCTION needs a 2nd provider (a DEPLOYMENT gate, not a build gate). **The dep-promotion (3 in the entry above) STAYS DEFERRED** — the `[embedding]` extra stays optional (correct while the base install stays light + provider-free lanes never pull onnxruntime); the operator installs it at deployment. **Genuine gate (driven-to, NOT fired):** operator creds for the LIVE multi-provider exercise (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`) — capability-built + production-dormant, the B-LAYER-BUDGET-OVERRIDE pattern (which shares THIS gate for its L3 firing). Advisor mandated: weight Codex/advisor heavily on the activation MECHANISM regardless. Build queued for the next focused iteration.

- **B-TAIL-CONDITIONAL-SAMPLING — full §9.2 conditional-row sampling enforcement for non-root + production-tail spans (surfaced by R-FS-1 arc B7 / B7-impl, 2026-06-17; advisor pre-build [blocking] + the ParentBased-root-only + attributes-post-creation re-grounding the B7 grounding sweep missed; UNTRACKED → forward arc).** B7 built the §9.2 attribute-conditional resolution as the OD SSOT (`sampling_mode.is_always_sampled(name, attributes)` + `_conditional_always_sampled`: `files.operation`/`memory.operation` mutation-`kind`, `validator.fail.*` `permanence=permanent`; conservative-absent) + wired it into the head sampler `HarnessCompositeSampler.should_sample`. But two structural facts (both re-grounded at HEAD) bound the head-sampler refinement to **near-nil production effect**: (1) **ParentBased root-only** — production wraps `ParentBased(root=HarnessCompositeSampler)`, which consults the inner sampler ONLY for ROOT spans (non-root spans inherit the parent decision and never reach `should_sample`); the live producers emit NON-ROOT spans (`memory.operation` at `memory_tool_dispatch.py:1535`-caller under the chat/dispatch/run tree; `files_operation_span` has **no** production caller — R-810/test-only), so the §9.2 set — current over-sample AND the B7 refinement — never executes for them; (2) **attributes-post-creation** — the producers set `*.kind` via `span.set_attribute` AFTER span creation (`files_api.py:241`, `memory_tool_dispatch.py:333`), so even a ROOT files/memory span's `kind` is invisible to the head sampler at `should_sample` time → conservative-always. The §9.2-conditional decision can therefore only bite where attributes ARE visible: at **trace completion** (tail-based sampling, the §9.1 production-time mandate). **Interim (landed at B7):** the SSOT predicate + head-sampler wiring ship correct + tested (the root-span + attribute-at-creation case); the stale `composite_sampler.py` over-sample / MVP-`1.0` docstrings are refreshed (closes the Bucket-C `composite_sampler.py:37` doc-hygiene item); the production enforcement boundary is documented in the module docstring (NOT a silent no-op). **No producer change** (inert — the spans never reach the head sampler). **The build:** (a) wire the `is_always_sampled(name, attributes)` SSOT into the **tail-keep span processor** (`harness-od/tail_keep_span_processor.py`, which reads `span.attributes` at root close) so the §9.2 conditional set is honored at trace completion for non-root + production-tail spans; (b) activate the tail-keep processor in the production provider (`compose_tracer_provider` attaches only a `BatchSpanProcessor`→OTLP today; the tail-keep processor is dormant). **GATED ON R-420/R-421** (the in-process OTLP collector + tail-keep activation; OD-6 `BOUNDED_RESIDUAL` — `flush_to_sqlite`/tail-keep dormant at MVP). **Owner = a follow-on R-FS-1 child arc within / after the R-420/R-421 collector arc** (likely impl-against-cleared-spec — §9.1 tail-based-prod + §9.2/§10.2 are cleared; confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: **MEDIUM** — it is what makes the §9.2 conditional-row refinement (and the §9.2 always-sampled set for non-root spans generally) actually bite in production; at HEAD the head sampler governs only root spans, and at solo cells `base_rate=1.0` makes even that moot. Rec: **BUILD** (OD tail-keep + runtime collector-wiring arc; gated on R-420/R-421). On landing, §9.2-conditional sampling bites for non-root + production-tail spans and the B7 SSOT predicate becomes load-bearing at the tail.

---

## Bucket A — Previously ratified-deferred → **RE-OPENED AS BUILD** (per the 2026-06-12 directive)

**These are no longer "legitimate bounded-residuals."** The standing directive re-opens every one as a build arc, on equal footing with Bucket B. Each carries a documented disposition history (kept below for context) + the external decision its build will reach (corpus / embedding-model / router-model — surfaced at the vendor boundary, not auto-fired). The "confirm-don't-rescue" framing in the original column is **superseded**.

| Item | Disposition | Tracked at |
|---|---|---|
| **Engine classes: 2 of 5** (`event-sourced-replay` / `reconciler-loop` / `WAL-segment` deferred) — the durable/recoverable execution engines | **Ratified-bounded.** = the engine-layer recovery residual. Operator **Gate A chose hand-roll** (I-6: no vendored engine) and **re-aimed to the workflow-layer** durable-resume (arc #3, #513/#514, BUILT). Engine-layer stays the `line-181`/CXA-2 residual. | `R-CL-P2`; `R-CXA-2-cp-is-seam` (§5 line 2225); `class_1_tension_u_rt_35…` line 181 |
| **LLM_AS_ROUTER** (Layer-3 routing intelligence) | Confirm-deferred (operator Option C, arc #6). Declarative routing covers the reach. | `R-CL-P1`; `class_2_fork_llm_as_router_layer3…` |
| **EMBEDDING** (Layer-2 similarity routing) | **BUILT** (R-FS-1 arc R / L2; operator **Option B**, 2026-06-16 — light in-process embedding). The deferral is **reversed** by the FULL-SPEC directive. Built: a CP-pure injected-`EmbeddingFn` k-NN classifier (`harness_cp.embedding_routing.make_embedding_classifier` + `EmbeddingRoutingCorpus`) + the light **fastembed** realization + default per-workload-class corpus (`harness_runtime.lifecycle.embedding_resolution`) + the `RuntimeLLMDispatcher.embedding_classifier` injection seam. Proven: stub-embed CP units + a real-fastembed live e2e (free/local). Production-inert until R-300 (same reachability as L3) — see `B-L2-EMBEDDING-ACTIVATION`. | `R-CL-P1`; `r-fs-1-r-routing-intelligence-design-v1.md` §3 (D2 L2-sync); `[[r-l2-option-b-light-embedding]]` |
| **Cost `RunResult.cost_attribution`** aggregate | Ratified-bounded (R-CL-P5 / arc #6). Per-dispatch cost is in the audit ledger; only the run-result rollup is empty. | `U-RT-49`; `fork_u_rt_49…` |
| **HITL OQ-6 timeout-degradation** producer | Confirm-deferred (producer-gated, arc #6) — the policy is built+pure; the wall-clock-wait orchestrator that fires it has zero production callers. *(Composes with B3 — the smart-HITL build would supply its producer.)* | arc #6 fork; capability inventory #11 |
| Memory-tool backend (live-proof) / OTLP local-ingestion residuals | `CP-16` / `OD-6` bounded-residuals (deployment-surface-gated). | post-Phase-8 register Tier B |

---

## Bucket C — Stale docstrings, NOT live boundaries (Q1 doc-hygiene, not build work)

Re-grounding found these read as MVP boundaries but are **superseded** — they are doc-rot, not open capability. Fold into the R-CL-Q1 doc-hygiene pass; do **not** list as open work.

| Stale comment | Reality at HEAD |
|---|---|
| `mutable_context.py:282` "v1.6 MVP binds 1 entry (SUB_AGENT_DISPATCH); INFERENCE_STEP [struck]" | **INFERENCE_STEP is live** — R-300 wired `infer()`; arc #5 ran live-Ollama INFERENCE_STEP across tiers; arc #4 made it inference-conditional (v1.47). |
| `composite_sampler.py:37` "MVP hardcodes base_rate=1.0; persona_tier not plumbed at the sampler binding site" | **Per-tier base-rate 1.0/0.1/0.2 proven live at the bootstrap** (arc #5, spied at `materialize_tracer_provider_stage`). *(2-min Q1 check: is the `composite_sampler.py` internal default a separate dormant layer, or simply a stale docstring? Either way per-tier sampling works.)* |
| `workflow_driver_types.py:193` "tenant_id… None = single-tenant (v1.6 MVP default)" | **Multi-tenant works** — the v1.6 hardcode was *lifted*; operator-supplied non-None tenant_ids flow through the 4-substep audit composition (R-500 + arc #5 ran MULTI_TENANT). `None` is just the single-tenant default. |
| roadmap §5 line 1078 "sandbox execution is metadata-only — no container/microVM/VM" | **Stale (2026-06-01 note)** — superseded by R-410 Docker / R-411 gVisor / R-412 E2B drivers + arc #1 tier→driver selection. (Residual is only B6 per-tool selection.) |

---

## Confidence notes

- **Code-side coverage: HIGH.** 47 code files referenced "MVP"; the load-bearing ones were re-grounded by direct read.
- **Spec-side coverage: MEDIUM.** The Explore spec sweep read the canonical *heads* (CP v1.31, OD v1.29 — which are delta files), not every full delta-chain body. A fuller spec pass could surface additional spec-declared MVP scopes; the code-side sweep is the stronger signal for *what is actually reduced at runtime*.
- **B1/B2/B3 are HIGH confidence** (direct code reads + roadmap-absence confirmed). B5/B6/B7 HIGH. B4 MEDIUM-HIGH.

---

## Program (under the 2026-06-12 directive — build-vs-ratify is SETTLED: build everything)

1. **Everything in Bucket A (re-opened) + Bucket B + the contract-completeness candidates is a build arc.** The vehicle is one ACTIVE umbrella **`R-FS-1`** with ordered child arcs (R-CC-1 shape); the old DEFERRED entries point at it, not each independently ACTIVE (keeps §4 clean). **R-CL-Q1→C1 move behind `R-FS-1`.**
2. **First arc = make scope provably complete (the scoping pass). ✅ DONE — `.harness/r-fs-1-arc-1-scoping-v1.md`.** Resolved the 8 `contract-w/o-cite` candidates per-contract (5 implemented-but-uncited/phantom → Q1 hygiene; **2 substantive build** → `C-CP-49/50` engine-layer emits fold into the engine-classes build; **1 thin arc** → `C-RT-28` managed_agents consumer is **already built + R-820 live-proven** (`managed_agents.py`; H_T-AS-8f SUBSTANTIVE_RETIRED) — only the *contract* was never authored AND the consumer is proof-script-only/unwired, so it re-sizes to a **thin contract-authoring + production-wiring arc, NOT a from-scratch managed-cloud build**; advisor-caught conflation of *contract*-unauthored vs *capability*-unbuilt) + the fuller spec-body pass (live-at-head filter: no net-new *major* arc beyond Bucket A/B; circuit-breaker found BUILT, SE single-step CLOSED, +2 minor field-level scopes — breaker config-tuning, idempotent MCP-host restart). Output: the **FROZEN ordered child-arc list** `B1→B3→E(engines)→B2→R(router/embedding)→B4→CA(cost)→B5→B6→B7→M(managed-cloud)` + minor field-level + Bucket C→Q1.
3. **Recommended build sequence** (capability-unlock first; design-fork-first per X-AL-3 where a spec extension is owed): **B1 topology orchestration** (the multi-agent / parallel / cross-comm payload + the biggest) → **B3 smart-HITL** (supplies the OQ-6 producer) → engine classes (hand-roll, I-6) → **B2 multi-server MCP** → LLM_AS_ROUTER + EMBEDDING (router-model / corpus surfaced at the vendor boundary) → **B4 per-role dispatch** → cost-aggregate → **B5/B6/B7** + minor field-level scopes. Each large item is research→design→spec→plan→implement.
4. **Bucket C** (stale docstrings) sweeps into R-CL-Q1 doc-hygiene — nothing to build there.
5. **Retained gates (not deferral — the directive's own "if a vendor must be sourced"):** EMBEDDING corpus + embedding-model; LLM_AS_ROUTER router-model. Design/spec/plan to the decision point, surface the one external input, do not auto-fire (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`).

**Open operator input (sequencing only, not whether-to-build):** confirm or re-order the §3 sequence. Default if silent: proceed as listed, starting with the §2 scoping pass.

**B2 execution-decomposition (registered 2026-06-16 — `[[spine-ledger-forward-arc-registration]]`; the split is NOT a defer).** B2 (multi-server MCP) is built as: B2-DESIGN ✅ (#579) → **B2-spec-1 ✅ reshape** (C-RT-04 `mcp_client_host`→`dict[ServerName, MCPClientHost]` + tool→server routing index + fail-loud collision + per-host sandbox + identity-by-ordinal `MCPServerTrustLevel→MCPTrustTier` *telemetry* projection; runtime v1.51 / CP v1.34 / AS v1.10; `.harness/class_1_fork_b2_multi_server_mcp_client_reshape.md`) → **B2-spec-2 ✅ gate-axis** (F2-02: CP spec v1.34 → **v1.35** NEW §19.1.2 commits `MCP_TRUST_GATE_LEVEL_FLOOR: dict[MCPTrustTier, GateLevel]` — resolves the v1.15 §19.1.1.1 row-3 spec-silence, the 4th/last unmaterialized §19.1 axis [`Axis.MCP_TRUST` already in the enum; 3-of-4 → 4-of-4 §19.1 axes]. **T-B2-2 probe-resolved** to floor-only/monotone — NOT council-convened: the gate is `max()`-over-escalation-rank so a floor can only RAISE it, and U-CP-91 `gate_level_rule.py:131-132` is a landed `mcp_trust`-NEVER-overridden commitment → loosening would reverse on-main + break the locked T-perm-1 composition [§10.9 probe-first; C10⊥C11 named inline, both converge floor-only]. Operator-ratified **Table A** [L0→DENY / L1→ASK / L2→ASK / L3→AUTO]. **SPEC-ONLY** — the `gate_level()` `per_axis_floors` addition (U-CP-98) + the composer **no-floor default** [U-RT-131 — replacing the harmful `hitl_gate_composer.py:462` L0 constant with the L3 AUTO-mapping default at the host-less gate sites; the real per-server resolved-host feed is the `B-TOOL-GATE` forward arc above, since the composer gates only inference/sub-agent steps today] land at B2-impl; runtime §14.8.2 step-4c already names the composer input so ZERO runtime/AS/ADR change. `.harness/class_1_fork_b2_spec_2_gate_axis_materialization.md`) → B2-plan → B2-impl (≥2 mock MCP servers + the `_trust_tier_from_level` stub retirement + the stale `mcp_client_host.py:128-130` docstring fix). **Split rationale:** the reshape is routine (sensible defaults + council-converged) while the gate-axis materializes a 4th axis of the LOCKED T-perm-1 composition + a live HITL-security decision → advisor-recommended ratification-boundary split (mirrors B1-spec→spec-1/spec-1b/spec-2 + B3-spec→spec-1/spec-2). Forward siblings: B2-restart (idempotent host restart, D5) + server-qualified tool addressing (D2).

**R execution-decomposition (registered 2026-06-16 — `[[spine-ledger-forward-arc-registration]]`).** The **R** sub-program (routing intelligence — the Bucket A **LLM_AS_ROUTER** + **EMBEDDING** rows above, re-opened as BUILD) is built as: R-DESIGN ✅ (#596 — D1 L3 Reading B [resolve the router call at the already-async `infer()`; Reading A foreclosed by ADD §5.3.3 + §2.2 "Probabilistic"], D2 L2 conditional) → **R-spec-1 ✅** (#598 — CP spec v1.36 NEW §2.5 the Layer-3 LLM_AS_ROUTER resolution surface: injected OPTIONAL `RouterResolutionFn`, timeout-bounded by the 200 ms L3 `LayerBudget`, rationale via the C-CP-01 §1.4 `routing.binding_rationale` span attr [the frozen 4-field `RoutingDecisionTrace` NOT widened]; `.harness/r-fs-1-r-routing-intelligence-design-v1.md`) → **R-plan-1 (this arc — pending merge; ✅ on merge)** — CP plan v2.37 (U-CP-99 contract types + the `ProviderDispatchFn` Protocol-ization; U-CP-100 the `infer()` Layer-3 branch) + runtime plan v2.48 (U-RT-132 rationale-consume + span; U-RT-133 router-injection binding, production `router=None` + the test-only `layer_decisions` seam) → **R-impl-1** (ALL 4 units co-land — the `ProviderDispatchFn` Protocol-ization is a cross-package TYPE-RIPPLE CO-LAND, verified by a pyright spike: legacy 4-arg dispatch closures go pyright-unassignable until they add the defaulted `binding_rationale` param; `[[shared-is-shape-change-ripples-cross-axis-field-asserts]]`) with a **MOCK** router (NO paid call) → **R-impl-2** (the real router model + prompt + a gated live e2e — **surface, do NOT auto-fire**; free-local Ollama preferred) → **R-L2-gate → R-spec-2/R-impl** (Layer 2 EMBEDDING — R-DESIGN D2, conditional on the gated embedding-model + corpus; CP spec v1.36 §2.5.6 silent on L2 by design). **NEW forward item (surfaced by R-plan-1; registered, NOT dropped):** the **router-call cost-bucket attribution** — CP spec v1.36 §2.5.4 commits the principle (the router dispatch is a real billable LLM call → its own span + cost attribution) but **defers** the precise cost-bucket discrimination (router-call cost to the routed step vs a distinct `routing:`/router-meta bucket) to the **CA** arc (the frozen `…→R→B4→CA→…` order) + R-impl; composes with the OD 5-step cost-attribution chain + the CXA cost-attribution audit-write seam. The CA arc owns the wiring (the Bucket A **Cost `RunResult.cost_attribution`** row + this router-call sub-item). **Split rationale:** R-DESIGN/spec/plan/impl mirrors the B1/B2/B3/E sub-program shape (design-fork-first per X-AL-3, each leg a clearance marker); the L3-then-L2 order + the vendor gates follow the directive's "design/spec/plan to the decision point, surface the one external input" (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`).

**B4 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B4** arc (Bucket B row above — per-role / per-step dispatch indexing; the per-role *model* half already landed at B1 arc #14 / U-RT-114, §14.5.3) is built as ordered slices: **Slice 1 (this arc) — per-role PROMPT threading** (a fan-out branch's `step_context.agent_role` selects + injects its per-role prompt `content` at the §14.5.2 translate seam; impl + the procedural-tier coherence sub-fork below) → Slice 2 — per-role binding **catalog** surface + validation (operator-authored `per_role_bindings` content for both routing + prompt manifests; `step_id→AgentRole` derivation) → **Slice 3 (this arc) — per-step PROMPT override on `StepOverride`** (CP spec v1.36 → **v1.37**: additive `StepOverride.prompt_version_sha` + `StepEffectiveBinding` propagation + NEW §6.6 provenance-scope; the §6.1 extension clause was confirmed at arc-open to cover ONLY `WorkflowManifestEntry`, not the `StepOverride` sub-record → a genuine CP §6.1 fork per v1.27 §2(d) explicit-extension discipline, mirror precedents v1.20/v1.22/v1.34; `.harness/class_1_fork_b4_per_step_prompt_override_stepoverride_extension.md` + clearance `Spec_Control_Plane-v1_37-cleared-2026-06-17.md`. **Surface = `StepOverride` not `PromptSelectionManifest`** [`step_id` is a workflow-scoped instance, not a run-level category — Route B is a Track-B reuse hazard]. **Provenance = the WIRED per-step override state-ledger entry** [`emit_override_state_ledger_entry` hashing `binding.model_dump` at `workflow_driver.py:1913-1930`; the NEW `StepEffectiveBinding.prompt_version_sha` is auto-captured], NOT the run-level §5.2 hash → **no IS-spec change** [HEAD re-ground overturned the stale v1.27 "no production caller" reading]. Runtime dispatch precedence per-step > per-role > default + fail-loud unauthored sha + binding-tier governance parity = impl-discretion per §14.5.3. Per-step **ROLE** is OUT of scope → Slice-4 gate per the §14.5.3 single-role-source invariant. Bundled-absorption arc; no operator gate; FULL-SPEC-pre-authorized) → **Slice 4 (this arc) — per-step + linear-path ROLE override** (CP spec v1.37 → **v1.38**: additive `StepOverride.agent_role` + `StepEffectiveBinding` propagation + §6.6 provenance extension; **paired with runtime spec v1.51 → v1.52** which **relaxes the §14.5.3 invariant-2 "single role source" + invariant-3 "linear path untouched"** — the committed-invariant sacrifice Slice 3 foreclosed). **Mechanism = Option B (composition-time fold), NOT Option A (second dispatch-read):** the CP driver folds `binding.agent_role` onto the single `StepExecutionContext.agent_role` source at all 6 topology composition sites (precedence per-step > fan-out-derived > default), so the **runtime dispatch read is byte-unchanged** (no two-authority-at-dispatch; zero `harness-runtime` impl edit) — structurally the `default_gate_level → parent_gate_level` precedent (v1.20). Provenance = the WIRED per-step override state-ledger entry (`binding.model_dump`), NOT the §5.2 hash → no IS-spec change. `.harness/class_1_fork_b4_per_step_role_override_stepoverride_extension.md` + clearance `Spec_Control_Plane-v1_38-cleared-2026-06-17.md` + `Spec_Harness_Runtime-v1_52-cleared-2026-06-17.md`. **OPERATOR-GATED** — unlike the additive Slice 3, the §14.5.3 invariant relaxation is a committed-invariant sacrifice → operator ratifies before merge (`[[feedback-gate-only-on-meaningful-architecture-change]]`; advisor caught + reversed a probe-resolves attempt to dissolve the gate — the decisive asymmetry: `default_gate_level` had no invariant forbidding it, role has the explicit §14.5.3 invariant 2). Bundled-absorption arc (CP v1.38 + runtime v1.52 + `harness-cp/src` impl + by-execution tests co-land). **This closes the B4 tail.** **Forward item surfaced by out-of-family Codex review (registered, NOT a Slice-4 deliverable — `[[spine-ledger-forward-arc-registration]]`):** the per-step **override-ledger provenance** (`emit_override_state_ledger_entry`, `override_applied`-gated) fires at the `SINGLE_THREADED_LINEAR` driver-thread site ONLY — a **PRE-EXISTING** gap (verified at pre-Slice-4 HEAD `eb85641`) across ALL non-linear topologies for ALL per-step override dimensions (model v1.6 / prompt v1.37 / role v1.38). Routing is correct on every topology (the fold works everywhere); only the dedicated per-step override-*audit-entry* is linear-only — the buffered-branch / concurrent-fan-out write boundary (ADR-F2 single-threaded-write) precludes a synchronous per-worker driver-thread entry. Closing it (emit the override entry through the buffered-branch path / on the driver thread) is a **separate audit-provenance arc** (`B-NONLINEAR-OVERRIDE-PROVENANCE`), scoped honestly in CP v1.38 §6.6. **Sub-fork surfaced + CLOSED in-arc at Slice 1 (NOT a defer):** per-role prompt injection created a procedural-tier hash-coherence gap (the C-IS-05 §5.2 recipe hashed only the resolved default-role `active_prompt_version.version_sha`, leaving per-role prompt-selection bindings hash-invisible — the §14.5.2 invariant violated for the per-role dimension). RESOLVED by adding a 4th recipe component `prompt_selection_manifest_sha` mirroring `routing_manifest_sha` (IS spec v1.8 → v1.9; fork `.harness/class_1_fork_b4_per_role_prompt_procedural_tier_hash_coherence.md`; clearance `.harness/clearance/Spec_Information_Substrate-v1_9-cleared-2026-06-17.md`). Bundled-absorption arc (IS §5.2 + runtime impl co-land). **Split rationale:** Slices 1–2 are impl-against-cleared-spec (§14.5.3 explicitly assigns per-role prompt to B4 + suggests fall-through-to-default), Slice 3 is the genuine ratification-boundary fork (a new field on a cleared `StepOverride` schema); the in-arc §5.2 coherence sub-fork is additive/forward-only/precedented → no operator gate, FULL-SPEC-pre-authorized.

**CA execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **CA** arc (cost aggregate — populate `RunResult.cost_attribution`, hard-coded `()` since landing behind the now-CLOSED U-OD-21 HALTED tension) is a thin-to-medium bundled-absorption arc: **Slice 0 — spec fork** (runtime spec v1.52 → **v1.53** §9 C-RT-09: axis = `RollupAxis.PER_PROVIDER_AND_MODEL` + type-name reconcile phantom `CostAttribution` → `tuple[CrossFamilyCostRollup, ...]`; `.harness/class_1_fork_ca_run_result_cost_aggregate_shape.md` + clearance `Spec_Harness_Runtime-v1_53-cleared-2026-06-17.md`; **additive/clarifying, NO operator gate** — sacrifices no committed invariant, unlike v1.52) → Slice 1 — run-scoped in-memory `cost_record_accumulator` Stage-0 `HarnessContext` field → Slice 2 — thread the same-reference sink into the 4 stage-4/5 dispatcher/hook materializations + append the returned `SpanCostRecord` in each best-effort wrapper → Slice 3 — `_build_run_result` rollup + stale-docstring refresh → Slice 4 — e2e (non-empty `cost_attribution`, correct per-(provider,model) sums) + unit + field-assert test updates. **Axis empirically forced** (advisor + HEAD body-grounding overturned the grounding-sweep lead): `PER_PROVIDER_DISCRIMINATOR` raises `CrossFamilyRollupError` on every production record (production helpers tag dispatch-type `"llm"`/`"tool"`/`"validator"`/`"webhook"`, none `CrossFamilyTag` members); `PER_PROVIDER_AND_MODEL` is safe + meaningful; single axis preserves the `sum(total_cost)` = true-run-cost invariant. Bundled-absorption arc (runtime v1.53 + `harness-runtime/src` impl + tests co-land). Slice 5 (fan-out 14.3 vs 15.1 double-counting) is NOT owed for this run-result aggregate (each dispatch contributes one record once; 14.3 `rollup_fanout_at_close` is a distinct dashboard surface).

**`B-COST-DISCRIMINATOR-TAXONOMY` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc CA, advisor-flagged).** A **latent Class-1** contract-vs-production coherence defect: production cost helpers write a **dispatch-type** `provider_discriminator` (`"llm"`/`"tool"`/`"validator"`/`"webhook"`) into `SpanCostRecord`, but the field's contract (`rollup_costs_by_axis` PER_PROVIDER_DISCRIMINATOR + the `SpanCostRecord.provider_discriminator` docstring) **validates against the `CrossFamilyTag` fallback-family vocabulary** `{frontier_managed, frontier_managed_alt, local_ollama}` → `CrossFamilyRollupError` if PER_PROVIDER_DISCRIMINATOR is ever run on production records. Dormant only because the sole current callers are OD unit tests with synthetic family-tag records (arc CA nearly tripped it; CA routes around it via PER_PROVIDER_AND_MODEL). The dispatch-type cost breakdown (llm-vs-tool-vs-validator-vs-webhook) is arguably the **most operator-meaningful** rollup and is exactly the one this defect blocks. The arc reconciles the dispatch-type tag taxonomy against the `CrossFamilyTag` fallback-family taxonomy (options at arc-open: extend `CrossFamilyTag`; add a dedicated dispatch-type `RollupAxis`; or have producers set real `CrossFamilyTag`s + carry the dispatch-type dimension separately) so a dispatch-type / family rollup becomes admissible — likely an OD-spec C-OD-15 §15.1 amendment. **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (OD spec v1.30 §15.1 + runtime spec v1.57 §9 + impl + tests + fork doc + 2 clearance markers), NO operator gate.** The decision: **Options B+C converge** — the spec authority (C-OD-15 §15.1, `Spec_Operational_Discipline_v1_2.md:870-878`) defines `provider_discriminator` as the **cross-family family tag** (a §15.3 chain-composition concept), so the production dispatch-type write was the **bug** (Option A — extend `CrossFamilyTag` — was REJECTED: it conflates two orthogonal dimensions + corrupts the §15.3 input). As-built: a NEW typed **`DispatchKind`** enum (`{LLM, TOOL, VALIDATOR, WEBHOOK}`, homed in the U-OD-20 carrier — enum-typed directly, no `str`+validate dance) + `SpanCostRecord.dispatch_kind` + **`RollupAxis.PER_DISPATCH_KIND`** carry the dispatch-type dimension; the 4 production helpers write `dispatch_kind=...` + `provider_discriminator=None` (a per-dispatch helper has **no** chain-level family context, so `provider_discriminator` becomes `str | None`, the `PER_PROVIDER_DISCRIMINATOR` axis **skips `None`** — the error→skip refinement, a bug-fix-toward-spec, no committed-invariant sacrifice → autonomous). Surfaced non-vacuously on a NEW optional **`RunResult.cost_attribution_by_dispatch_kind`** field (orthogonal single-axis partition of the same total as `cost_attribution`; minor bump, the v1.45 `pause_snapshot` precedent). advisor (full-transcript — affirmed the spec-reading as load-bearing + sharpened the forced provider_discriminator-for-non-LLM sub-decision) + Codex (out-of-family). Gates: pyright 0/0/0; ruff clean; harness-od 950 + harness-runtime 1873 (non-e2e) + CXA-P1 34; overlay 323 nodes 31/31; no §5.2-hash change (ephemeral run-OUTPUT). `[[cleared-spec-resolves-it-before-first-principles-fix]]` · `[[built-but-vacuous-reground-ledger-asis]]`.

**`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` (standalone forward arc, registered 2026-06-18 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B-COST-DISCRIMINATOR-TAXONOMY).** Populate `SpanCostRecord.provider_discriminator` with a real `CrossFamilyTag` at the §15.3 fallback-chain composition — the `FallbackChainCostComposition` seam + the CP→OD cross-axis edge (`cross_family_rollup.py:36` — C-CP-04 cross-family fallback chain) — which makes the `PER_PROVIDER_DISCRIMINATOR` cross-family rollup **non-vacuous in production** (today every production record is correctly `None`, as a per-dispatch helper has no chain-level family context; the axis stays defined + admissible, exercised by the synthetic-record tests). Per `[[r-cxa-seam-wiring-is-producer-discovery]]`, the §15.3 producer is the CP fallback path — a genuinely separate cross-axis arc; spec §15.3 already marks the dashboard query / population "deferred to implementation discretion." **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (runtime spec v1.57 → v1.58 §9 C-RT-09 + `harness-runtime/src` impl + by-execution tests + fork doc + clearance marker), NO operator gate.** The A/B "parent-record vs tag-the-record" fork was **probe-resolved as a span-layer-vs-record-layer false dichotomy** (advisor): §15.3 governs OTel **span** attributes; §15.1.2 governs the **`SpanCostRecord` field** — and §15.1.2 explicitly assigns *this* arc to **populate the existing dispatch record's field**, not emit a separate parent record (which collapses A/B + kills the double-count-in-`PER_PROVIDER_AND_MODEL` worry by construction). As-built: the LLM cost path (`_attribute_cost_best_effort` → `attribute_llm_dispatch_cost`, optional new `provider_discriminator` param) stamps each LLM `SpanCostRecord.provider_discriminator` with the dispatched provider's family via a NEW `harness_runtime/lifecycle/cross_family_cost_tag.py` map (`ANTHROPIC → frontier_managed`, `{OPENAI, GOOGLE} → frontier_managed_alt` per the §15.1 "OpenAI/Gemini" grouping, `LOCAL_OPEN_WEIGHT → local_ollama`; `CrossFamilyTag` NOT extended; map homed in runtime — `[[carrier-home-defect-pattern]]` — + it also owns the now-one-source-of-truth provider→family map that `retry_breaker_fallback` reuses). Surfaced on a NEW optional **`RunResult.cost_attribution_by_provider_discriminator`** field — making `PER_PROVIDER_DISCRIMINATOR` **non-vacuous in production**. **Correctness: this axis partitions the LLM SUBTOTAL, not the full run total** (non-LLM records carry `provider_discriminator=None` → skipped) — the by-execution tests assert `Σ == Σ(LLM records)` (NOT total-run) + that the other two cost axes are unchanged (no regression/double-count). **The `FallbackChainCostComposition` carrier is deliberately NOT wired** (it has no consumer → constructing it would be hollow; `[[r-cxa-seam-wiring-is-producer-discovery]]`) — non-vacuity is met by `provider_discriminator` + the rollup field alone. **NO operator gate** — impl-to-cleared-spec (OD v1.30 §15.1.2 already reserves the field for this arc → no OD spec change, runtime-only); additive, no committed-invariant sacrifice. Bundled doc-hygiene: v1.58 also reconciles the v1.57 §9-body-drift (`cost_attribution_by_dispatch_kind` was change-note-declared but never tabled in §9 — `[[spec-prose-plan-body-drift-pattern]]`). Verification: `test_b_fallback_chain_family_cost_composition.py` (map; LLM-subtotal rollup; the REAL cost path tags the record; a REAL `RetryBreakerFallbackDispatcher` cross-family fallback anthropic→openai → `frontier_managed_alt`). Gates: pyright 0/0/0; ruff; harness-runtime 1889 (non-e2e); overlay 324 nodes 31/31; substitution 54/54; CXA-P1. advisor (full-transcript — resolved the false dichotomy + flagged the LLM-subtotal invariant + the hollow-carrier trap) + Codex (out-of-family). Fork: `.harness/class_1_fork_b_fallback_chain_family_cost_composition.md`; clearance `Spec_Harness_Runtime-v1_58-cleared-2026-06-18.md`. `[[cleared-spec-resolves-it-before-first-principles-fix]]`.

**`B-NONLINEAR-OVERRIDE-PROVENANCE` (standalone forward arc, registered 2026-06-17 in the B4 Slice-4 entry above — `[[spine-ledger-forward-arc-registration]]`; surfaced by the B4-Slice-4 out-of-family Codex review).** The per-step override-ledger entry (`cp.per-step-override-application`, U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` driver-thread site **only** — the 5 non-linear strategies resolved the per-step binding (the override **took effect at dispatch** on every topology — routing correct everywhere) but emitted **no dedicated override-ledger entry**, for ALL dimensions (model v1.6 / prompt v1.37 / role v1.38). A PRE-EXISTING all-topologies audit-provenance gap, scoped honestly in CP spec v1.38 §6.6 (the buffered-branch / ADR-F2 single-threaded-write boundary precluded a synchronous per-worker write). **✅ BUILT (this arc, 2026-06-18) — bundled-absorption (CP spec v1.39 → v1.40 §6.6 topology-scope-note refresh + `harness-cp/src` impl + by-execution tests + fork doc + clearance marker), NO operator gate.** Mechanism = the **buffered-branch path** (CP v1.38 §6.6's own named option, the thread-model-agnostic one — advisor-decisive over driver-thread-direct, which would need per-site proof the write is on the single thread outside the drain capture-window): a new `append_branch_override_ledger_entry` buffers the override entry into the branch's `BufferingLedgerWriter`, serialized on the driver thread by `drain_branch_buffers` in branch-index order (C-CP-25 §25.12 D1.b — the §25.13 step/terminal branch-entry discipline). Wired into all 5 strategies via a shared `_buffer_branch_override_if_applied` guard at each branch-plan / sequential-dispatch site (HIERARCHICAL_DELEGATION inherits it — recursion re-enters `_execute_orchestrator_workers`). **Idempotency identity = the §16.5.4 per-`(step, outcome)` key, reused VERBATIM (NOT branch-scoped)** — an override is a static binding property, so a repeated `(step, outcome)` across EVALUATOR_OPTIMIZER iterations / recursion levels idempotently dedups at the IS writer to one entry (the spec's designed semantic; branch-scoping would change the cleared §16.5.4 formula → X-AL-3, foreclosed). Both write paths compose via the factored-out `compose_override_entry_payload` → the persisted entry is **byte-shape-identical** across all topologies. **NO operator gate** — impl-to-cleared-spec on the general §6.6 provenance contract + a `[[stale-carry-text-disposition]]` refresh of the now-falsified v1.38 honest-scope note; no committed-invariant sacrifice (contrast B4 Slice 4's §14.5.3 *relaxation* gate), additive behavior (`[[feedback-gate-only-on-meaningful-architecture-change]]`). Verification: `test_workflow_driver_nonlinear_override_provenance.py` (by-execution per topology into the REAL IS writer — incl. the EVALUATOR_OPTIMIZER repeated-step → exactly-one-entry dedup + a nested-recursion case + §6.3 chain re-verification); full harness-cp suite green (1055 passed; existing topology suites use no overrides → emission is a no-op). Decorrelated review: advisor (Design A; idempotency identity; §6.6 note disposition) + out-of-family Codex (at the impl-diff PR). `[[stale-carry-text-disposition]]` · `[[shared-is-shape-change-ripples-cross-axis-field-asserts]]`. Fork: `.harness/class_1_fork_b_nonlinear_override_provenance.md`; clearance `Spec_Control_Plane-v1_40-cleared-2026-06-18.md`.

**B5 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B5** arc (Bucket B row above — memory backend per-deployment-surface selection stubbed) is a thin single-bounded **impl-against-cleared-spec** arc (no fork; impl-discretion per runtime plan v2.14 U-RT-80 AC + spec §14.12.7): `MemoryToolRegistry.resolve_backend(deployment_surface)` no longer ignores its argument — it resolves the storage backend FOR the surface from a bootstrap-frozen surface→backend map (`from_surface_map`), realizing the §14.12.1 surface-parametric reading; all resolution stays at bootstrap (§14.12.5 invariant 1). The default factory path resolves each `DeploymentSurface` independently (config-free `FILESYSTEM`, constructed once per distinct enum) and **freezes a deferred `RT-FAIL-MEMORY-BACKEND-RESOLUTION`** for a surface with no config-free backend (`MANAGED_CLOUD` admits only `{s3, database}`, both needing operator `backend_params`); the **ACTIVE surface must resolve or bootstrap aborts fail-closed** (preserves the pre-B5 active-`MANAGED_CLOUD`-no-override behavior — no silent ephemeral SQLite). Anti-vacuity = **constructs-vs-raises** (the single global override config cannot populate two distinct *constructed* types without collapsing the map — `[[built-but-vacuous-reground-ledger-asis]]`). **Active-surface dispatch is byte-identical** (production threads the single active `config.deployment_surface`); the multi-surface map is **contract-shape, not a runtime multiplex feature** (single-surface deployment ⇒ no multiplex use case). No design-substrate edit, no new contract, no CXA seam (overlay 31/31). Backends routed to (`FILESYSTEM`/SQLite-`DATABASE`/`S3`/managed-DB) all exist (R-830). **Did NOT extend `memory_tool_backend_config` into a per-surface map** (advisor: that is configurability nobody can reach + an X-AL-3 spec extension — the over-engineering trap FULL-SPEC can induce; the global override already fully expresses the one-deployment backend choice). `[[built-but-vacuous-reground-ledger-asis]]` · `[[grounding-reveals-claude-closeable-slice-close-honestly]]`.

**`B-MEMORY-SURFACE-BACKEND-IMPLS` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]` + `[[feedback-full-spec-beyond-mvp-nothing-deferred]]`; surfaced at arc B5, advisor-flagged).** Two **spec'd-but-unimplemented** memory storage backends the B5 factory `_construct_backend` else-branch still raises for: **`ENCRYPTED_FILESYSTEM`** (runtime spec §14.12.3 step 2 — a filesystem implementation wrapped with per-path encryption per an operator-supplied key reference at `backend_params`) and **`OPERATOR_DEFINED`** (§14.12.3 step 2 + §14.12.7 — an operator-supplied implementation resolved by class-qualified-name introspection via `backend_params['class_qualified_name']`, e.g. `importlib.import_module` + `getattr`, raising `RT-FAIL-MEMORY-BACKEND-RESOLUTION` on class-resolution failure). Both are declared in the `MemoryToolStorageBackend` enum + described by the cleared spec → under the FULL-SPEC standing directive they are **registered BUILD arcs, NOT operator-discretion defer-closes** (the grounding-sweep's "operator-discretion follow-on per §16 §6.C v2 C.vii" disposition predates + is overridden by the 2026-06-12 directive). Each is a small, separable backend impl + factory wiring + the `_MEMORY_BACKENDS` admissibility intersection (once `ENCRYPTED_FILESYSTEM` lands, `LOCAL_DEVELOPMENT` + `SELF_HOSTED_SERVER` gain a second admissible backend and B5's surface→backend map routes to it; once both land, every `DeploymentSurface` has an admissible config-free or operator-config backend — the B5 deferred-raise surfaces shrink). **B5's routing scope is complete without them** — they are the remaining backend *implementations*, registered for a follow-on R-FS-1 child arc. **✅ BUILT (this arc, 2026-06-17) — impl-to-cleared-spec (§14.12.3 step 2), Phase 7 posture (`harness-runtime/**` only), NO design-substrate edit, NO fork, NO contract widening.** The classification gate cleared against the **spec body** — both members are declared in the `MemoryToolStorageBackend` enum and their construction is fully described at §14.12.3 step 2 → autonomous build, not a defer-close (the grounding-sweep's "operator-discretion follow-on" disposition is overridden by the FULL-SPEC directive). **`ENCRYPTED_FILESYSTEM`** — realized as an injected `MemoryContentCodec` on `LocalFilesystemMemoryToolBackend` (new `memory_tool_encrypted.FernetContentCodec`, Fernet authenticated AES, ciphertext at rest), **NOT** an external wrapper: the codec's `encode`/`decode` run INSIDE the backend's existing per-path lock, so the `str_replace`/`insert` read-modify-write stays atomic with **one** authoritative lock — an external wrapper would have to DUPLICATE that lock (re-deriving the read-modify-write as `view`+`create`) to match (`[[cleared-spec-resolves-it-before-first-principles-fix]]` inverse-nuance: the spec's "wrap" names the *encryption invariant* [ciphertext-at-rest, preserved]; the wrapping *site* is impl-discretion per §14.12.7). Key via `backend_params['key_env_var']` (an env-var **reference**, never key material — structure-not-content per §14.12.1; env-var is the deliberately chosen scheme over Target-Stack-nominal python-keyring — it composes with the workspace `.env`/just dotenv posture, one working scheme satisfies full-spec). Fernet **lazy-imported behind RT-FAIL** (mirrors the boto3/psycopg pattern; `cryptography` 48.0.0 is a present transitive dep so live works — no pyproject change). inv-4: malformed-key/decode errors never echo the key (test-enforced). A distinct `.harness/memories-encrypted` root (no plaintext↔ciphertext co-mingling). **`OPERATOR_DEFINED`** — `_resolve_class_qualified_name` (`importlib.import_module` + `getattr`; accepts `module:Class` and `module.Class`) **ESTABLISHES the canonical class-qualified-name introspection convention** the C-RT-23 validator framework follows (runtime spec §14.13.1 line 4555: "validator … must use the same introspection-based discipline as MemoryToolStorageBackend.OPERATOR_DEFINED"). The operator class is instantiated with the full `backend_params` Mapping; conformance is the existing `_enforce_protocol_conformance` **hardened to a 3-layer check (presence + callable + async)** so a sync-`def` operator class fails CLOSED at bootstrap (ADR-F4) rather than `TypeError` at first dispatch. `_construct_backend` now handles all 5 members (`assert_never` exhaustive tail; the prior else-raise is gone). `str_replace`/`insert` switched `read_text`/`write_text` → `read_bytes` + explicit utf-8 (byte-faithful for the codec); the full harness-runtime suite incl. U-RT-81 dispatch + U-RT-82 e2e verified the encoding change non-regressive. **Decorrelated review:** advisor (forced the one-source-of-truth framing for codec-vs-wrapper + flagged the encoding blast-radius → run the full suite, not just the backend files) + **out-of-family Codex** — which caught **two real bugs advisor + I missed** (`[[hooks-codex-pilots-decorrelation-validated]]`): (1) [P2] the OPERATOR_DEFINED instantiation handler interpolated `{exc}` → an operator `__init__` could echo a secret `backend_params` value (fixed: surface the exception TYPE only, keep the chain; test-enforced); (2) [P2] a sync-`def` operator backend passed the presence-only `@runtime_checkable` check → `await` TypeError at dispatch (fixed: the async-conformance gate above; test-enforced) — Codex then converged "no actionable correctness issues." New: `memory_tool_encrypted.py` + `test_lifecycle_memory_tool_encrypted.py` (+ codec tests in the filesystem test file + the rewritten AC#3 + OPERATOR_DEFINED/ENCRYPTED suites). Gates: targeted 101 / full harness-runtime **1876 passed, 21 skipped, 1 xfailed**; pyright strict 0/0/0; ruff clean.

**B6 execution-decomposition (registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`).** The **B6** arc (Bucket B row above — per-tool sandbox-tier driver selection + STDIO transport-floor; arc #1/#503 landed per-*server-uniform* tier→driver selection, and B2/U-RT-130 lifted that resolver/driver from single-server `[0]` to **per-host**, but the within-host resolver is still the **constant** `_build_default_policy_sandbox_resolver` — `resolve(_contract,_step)` ignores both args) is built as two ordered slices, split on the ratification boundary: **Slice 1 (this arc) — per-host transport-floor + remote-trust composition.** The stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor(entry.transport, entry.trust_level, entry.blast_radius)` (U-AS-13) via tier-`max()` with the §14.9.8 surface-aware `effective.sandbox_tier` before building the resolver + selecting the driver — closing the **carved-out STDIO tier-3 transport-floor non-enforcement gap** (runtime spec v1.43 §14.9.9 change-note: "a bare STDIO local-development server resolves to TIER_1_PROCESS, below the §1.3 floor … explicitly carved out at the §14.9.9 Scope boundary"). **Uses `mcp_transport_floor`, NOT `sandbox_tier_floor`** — the per-host `MCPClientConfig.blast_radius` is ONE blast input per host ⇒ one tier per host (per-server-uniform PRESERVED); feeding `sandbox_tier_floor` per-tool `contract.blast_radius_tier` would silently pull in the Slice-2 per-tool/driver-granularity fork (advisor catch). REFUSE handled as a **defensive raise** (`SandboxDispatchDecision.tier` non-optional; L0-remote already refused at registration `mcp_host.py:111`, so a resolver REFUSE is unreachable-by-construction). **Impl-to-cleared-ADR — NO operator gate** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`): the apparent **C10⊥C11** tension (action-safety wants the STDIO tier-3 minimum; operator-loop/local-deployment wants out-of-box-without-substrate) is **probe-resolved at ADR-D2 §1.3 in C10's favor** — Decision item 3 commits "Tier 3 minimum for STDIO MCP transports regardless of declared blast-radius" + §1.5 `max()` composition ⇒ `max(local-dev-cell=Tier1, STDIO-floor=Tier3)=Tier3` even at local-development (a hard floor, not overridable per §1.1's cell-override-only contract). Slice 1 therefore **fulfills** a committed mandate + closes a security gap (sacrifices nothing) — unlike B4 Slice 4 which *relaxed* §14.5.3. **Real out-of-box behavioral change (named, not gated):** enforcing the floor flips local-dev STDIO TIER_1→TIER_3 (and L2-remote→TIER_4), so those hosts now require a configured microVM/VM `sandbox_driver` or bootstrap **fail-closes** (FR-2(i) `RT-FAIL-SANDBOX-DRIVER-UNAVAILABLE`) — the cleared §14.9.9 fail-loud floor makes this safe (no silent under-sandbox). **Test cascade (as-built, empirically sized):** factory tests (`test_u_rt_75`) gain a tier-3 `sandbox_driver` + assert the floored TIER_3 / fail-close / gVisor; **`test_u_rt_86` (H_T-AS-2/CP-18 retirement evidence) verified UNAFFECTED — ran-and-passed** (dispatches via the host path, not the stage-5 FR-2 factory, so the floor never hits it; ToolContract still enforced); the **in-process-STDIO `api.run` dispatch e2e** (`test_r100_ac2` + `test_r_cl_p3`×3) are **SKIPPED** (in-process STDIO is the pre-B6 gap ADR-D2 §1.3 forbids; container completion covered by Docker-gated R-410). **Coverage loss surfaced honestly:** the only default-lane proof that `api.run` *completes* a tool workflow is gated out — accepted within B6 scope; full restoration registered as `B-MCP-HOST-REMOTE-TRANSPORT` (a remote L1 RO server → TIER_1, blocked today by the host-factory `_VALID_TRANSPORTS` rejecting the granular remote enum). If "STDIO tools can't run in-process even at local-dev" is too strict for out-of-box, that's an ADR-D2 §1.3 revision route (operator's call; does not block). Bundled-absorption arc (runtime spec §14.9.8/§14.9.9 additive amendment composing the per-MCP-transport floor into the per-host resolver + fork doc + clearance + `harness-runtime/src` impl + tests co-land). → **Slice 2 (next, OPERATOR-GATED) — per-tool granularity = `B-PER-TOOL-SANDBOX-TIER` below.** **Split rationale:** Slice 1 is impl-to-cleared-ADR-D2-§1.3 (per-server-uniform preserved; no committed-invariant sacrifice) while Slice 2 RELAXES §14.9.9 inv-3 (per-server-uniform→per-dispatch driver selection) + extends `ToolContract` → the genuine ratification-boundary fork (mirrors the B4 Slice-3-additive / Slice-4-gated split).

**`B-PER-TOOL-SANDBOX-TIER` (standalone forward arc = B6 Slice 2, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B6 Slice 1).** The genuine **per-tool** sandbox-tier widening the runtime spec names as "still B6" (§14.9.10 D4: "per-tool sandbox granularity nests inside each host's resolver as the distinct future arc B6 — per-host outer × per-tool inner"; §14.9.9 Scope boundary: "widening it to the full `sandbox_tier_floor(...)` per-cell composition … is a distinct future arc"). Replaces the per-host constant resolver with a per-`(contract, step)` call to the full 10-row `sandbox_tier_floor(tool_metadata, deployment_surface, contract.blast_radius_tier, transport, mcp_server)` so rows 1-2 (forcing) + rows 7-10 (per-tool blast-radius) become reachable per tool. **Two coupled forks (structural, not optional):** (a) a `ToolContract` **carrier extension** carrying the `ToolMetadata` forcing discriminators (`forces_computer_use` / `forces_code_execution` / `is_deterministic_inhouse`) — `ToolContract` today carries only `minimum_tier` + `blast_radius_tier` — which ripples to the AS↔runtime seam (`RawContractInput` + the v1.40 stage-3a `MCPToolContractConverter`); (b) **driver-granularity relaxation of §14.9.9 inv-3** ("per-server-uniform, construction-time"): the driver self-guard is **exact-match** (`docker_tool_execution_driver.py:71` `if sandbox_decision.tier is not self.required_tier: raise`), so ANY per-tool tier diverging from the host's one construction-time driver tier breaks dispatch → per-tool tier resolution CANNOT land without per-dispatch driver selection (a per-host per-tier driver registry selected by resolved tier, OR pin-at-max + relax the guard to `>=`). Per-tool tier resolution structurally forces driver granularity — they co-land. **OPERATOR-GATED** — the §14.9.9 inv-3 relaxation is a committed-invariant sacrifice → operator ratifies before merge (mirrors B4 Slice 4 / `[[feedback-gate-only-on-meaningful-architecture-change]]`). Design-fork-first per X-AL-3 (runtime-spec amendment for the per-tool resolver + per-dispatch driver granularity + the `ToolContract` carrier extension). Not yet built; FULL-SPEC-pre-authorized when opened.

**`B-MCP-HOST-REMOTE-TRANSPORT` (standalone forward arc, registered 2026-06-17 — `[[spine-ledger-forward-arc-registration]]`; surfaced at arc B6 Slice 1, advisor-flagged).** The stage-3a MCP host factory (`mcp_client_host.py:217` `_VALID_TRANSPORTS = {'sse', 'stdio', 'streamable_http'}`) rejects the **granular** `MCPTransport` remote enum values (`streamable_http_l0/l1/l2/l3`) — it expects a COARSE transport string, but `MCPClientConfig.transport` carries the granular enum, with no granular→coarse mapping applied at the host-construction boundary. So **only STDIO (+ bare `sse`) remote-less servers materialize through the host factory**; a remote `STREAMABLE_HTTP_L*` MCP server cannot be bootstrapped today (it raises `ValueError: unknown MCP transport`). **Load-bearing consequence (surfaced by B6 Slice 1):** with STDIO now floored to TIER_3 (ADR-D2 §1.3) AND remote transports host-factory-rejected, there is **no host-factory-accepted transport that resolves to TIER_1** — so the **provider-free DEFAULT-LANE `api.run` tool-completion e2e** (the closed R-100 AC#2 `test_r100_ac2_tool_step_via_api_run` + the R-CL-P3 multi-tier tool step) can no longer run in-process (both now SKIPPED with B6 reasons; tool-completion at the ADR-mandated tier is covered by the Docker-gated `test_r410_container_tool_execution_e2e`). Fixing the host-factory transport parser (map the granular enum → coarse transport, or accept the granular set) would let a **remote L1 READ_ONLY** server resolve to TIER_1 (`mcp_transport_floor` → `blast_radius_floor(READ_ONLY)`), restoring a substrate-free default-lane tool-completion e2e. **The build:** (a) reconcile the host-factory `_VALID_TRANSPORTS` check with the granular `MCPTransport` enum (likely a granular→coarse projection at host construction); (b) verify remote-transport host materialization end-to-end (the SDK `streamablehttp_client` path); (c) restore the default-lane tool-completion e2e on a remote L1 RO server. **Owner = a follow-on R-FS-1 child arc** (likely impl-against-cleared-spec — the granular enum + coarse host transport both exist; confirm no contract widening at build, else design-fork-first per X-AL-3). Load-bearing: MEDIUM — real agents need remote MCP servers (composes with B2 multi-server); the immediate trigger is the B6-surfaced default-lane e2e coverage loss. **✅ BUILT (this arc, 2026-06-17) — impl-to-cleared-spec, NO operator gate, NO spec change, NO contract widening.** The classification gate cleared against the **spec** (runtime §14.9.6 inv 5 "STDIO + HTTP + SSE all supported at v1. Per Decision 1.D4"; §14.9.8 resolves `mcp_transport_floor` for remote L1/L2/L3 + confirms L0 refused at registration before stage-3a), not the `MCPTransport=Literal[...]` code comment. Fix = a `_coarse_transport` granular→coarse projection at `mcp_client_host_factory._build_host` (the prior `cast(_MCPTransportLiteral, entry.transport.value)` lied for remote values — `"streamable_http_l1"` ∉ the host's coarse `_VALID_TRANSPORTS` → `ValueError`; never exercised because no remote host had materialized through the factory): `stdio→stdio`, all `streamable_http_l*→streamable_http` (the L-level is a *trust* dimension read by the sandbox floor via `trust_level` + the gate via `server_name`, NOT a connection mechanism). **REFUSE stays single-source** — `mcp_transport_floor` owns it + stage-2 `materialize_mcp_stage` raises on L0 before stage-3a (verified: bootstrap stages PREAMBLE→IS→AS(stage_2)→CP_CLIENTS(stage_3a), abort-on-raise), so no L0 guard in the projection. **BOTH** B6-skipped default-lane tool-completion e2es RESTORED by migrating their in-process STDIO echo server (floored to TIER_3 by ADR-D2 §1.3 → fail-close) to a real **remote streamable-HTTP** echo server (remote L1 READ_ONLY → `blast_radius_floor(READ_ONLY)` = TIER_1 → host-process, no Docker — the provider-free property): `test_r100_ac2` (provider-free default lane — the registered (c) target, verified 8/8 non-flaky) + `test_r_cl_p3` (live multi-tier, un-hard-skipped; the explicit `default_sandbox_tier=TIER_1` override keeps resolved==TIER_1 on SELF_HOSTED_SERVER; 3/3 verified live against Ollama). New test fixtures `mcp_echo_server_http.py` (FastMCP streamable-http echo) + `streamable_http_echo.py` (free-port spawn + endpoint-readiness poll + hardened teardown) + factory unit tests (per-value projection + the remote-host `ValueError` regression). Decorrelated review: **advisor** (forced the spec-gate-not-code-comment discipline that kept this autonomous) + **Codex** (out-of-family — "no actionable regressions"; ran the suites independently). The "B6 e2e-migration item" named inline in `test_r_cl_p3`'s old skip is **SUBSUMED** (it was never a separately-registered forward entry — confirmed by grep). Gates: harness-runtime 1846 passed (non-e2e) / factory 17 / r100_ac2 8× / r_cl_p3 3/3 live; pyright 0/0/0 touched; ruff clean; overlay 322/31-31; CXA-P1 34; X-AL-3 green.
