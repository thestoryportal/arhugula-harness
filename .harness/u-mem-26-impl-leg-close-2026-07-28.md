# U-MEM-26 impl leg — close record (2026-07-28)

The implementation leg of `B-86`'s ratified `Spec_Memory_Substrate_v1.md` v1.1 contracts.
Branch `u-mem-26-impl`. This is the record the U-MEM-25 closeout evidence packet and future
sessions cite; it carries what the commit messages and the register rows do not.

## Slices

| Slice | Commit | Content |
| --- | --- | --- |
| 1 | `b592c043` + `8ddf7d82` | `harness-is` read boundary — C-MEM-03 scope asymmetry, request-side value domain (`memory_scope_value_domain.py`), 19 witnesses. |
| 2 | `76c0e879` + `ce5d6e3c` | `B-89` writer repair + `B-90` fold-in, write-boundary at every scope-AUTHORING surface, `memory_scope_family.py` composition root, 20 witnesses. |
| 3 | `8624af53` | C-MEM-13 cross-family withhold guard + production-wired witnesses; two defect-pinning tests inverted. |
| 4 | this commit | Closeout-evidence refresh (six rows), `B-86`/`B-89`/`B-90` register close-outs, this note. |

## By-rule inventory additions found during impl

The plan's own implementation note (`Implementation_Plan_Memory_Substrate_v1.md:928`) warns
that the enumerated reader/writer inventory is **review-time, not proven complete**. Re-grounding
at impl HEAD found two surfaces the enumeration missed — worth recording, because the under-count
is the point:

- **(b2), a second candidate-identity origination site.** Surface (b) was enumerated as *the*
  promotion path, `_candidate_from_hint` (`memory_promotion.py`). Grepping every
  `PromotionCandidate(` construction found a second one at `memory_tool_executor.py:417`, which
  never traverses the hint path — so a canonicalization placed only at `_candidate_from_hint`
  is bypassable. Resolved by `_require_canonical_candidate_scope`, a write-side backstop at
  `_persist_decision` that **REFUSES** an un-canonicalized candidate rather than repairing it:
  repairing there would fix the persisted scope while leaving `candidate_id` and `risk_flags`
  derived from the raw identifier — exactly the split the ordering rule exists to prevent.
- **The r300 integration surface, found by suite-run rather than by enumeration.** No reader/writer
  grep would have surfaced `test_r300_deterministic_cross_family_fallback_through_production_path`:
  it is a *test* that pinned the defect, asserting the five C-MEM-14 wire names DO reach the
  cross-family openai leg. It surfaced only when the full suite was run against the slice-3 guard.
  Inverted in place with contract cites; its wire-name / HTTP-400 coverage is preserved on the
  same-family paths that still inject. **Lesson: an inventory grep finds production carriers, not
  tests that encode the defect as expected behaviour — only a suite run does.**

## Load-flake identities verified

The slice-4 full-suite run at slice-3 HEAD: **6966 passed / 31 skipped / 8 failed** in 469s. All 8
failures are live-Ollama e2e integration nodes that pass in isolation and fail under full-suite
load (contention against a local `llama3.2:3b`), verified by a positive control —
`test_r300_live_ollama_provider_fallback_exercise` passed standalone in 31.8s, having failed in the
same run. The identities, across four modules:

- `integration/test_r300_cross_family_fallback_e2e.py` — `test_r300_live_ollama_provider_fallback_exercise`,
  `test_r_pm_1_active_prompt_injection_honored_by_live_ollama`, `test_r_pm_1_workload_selection_drives_live_ollama_injection`
- `integration/test_r_cl_p3_live_multi_tier_e2e.py::test_r_cl_p3_live_multi_tier_api_run` — all three persona-tier params
- `integration/test_u_cp_89_hierarchical_delegation_live_e2e.py::test_u_cp_89_hierarchical_delegation_depth2_live_ollama`
- `integration/test_u_cp_90_decentralized_handoff_live_e2e.py::test_u_cp_90_decentralized_handoff_three_stage_live_ollama`

None touches memory-substrate code; the provider-free suite is green. *(The slice-3 handoff named
three such identities; the slice-4 run surfaced the six functions / eight nodes above. Recorded as
observed rather than reconciled to the handoff count.)*

## Forward residuals

- **C-MEM-10 promotion eligibility is now materially reachable.** The spec v1.1 change-note carries
  "promotion eligibility of records captured during a cross-family fallback leg" as a named open
  question, and the plan puts it out of scope (`:933`). It is no longer hypothetical: capture now
  writes under the run's **composed** scope, so a cross-family fallback leg lands an
  `anthropic`-family record while the dispatch that produced it ran on `openai`. Witness:
  `test_u_mem_26_capture_is_unaffected_by_the_cross_family_withholding`
  (`harness-runtime/tests/test_automatic_memory_runtime.py`), which asserts exactly that
  `captured.envelope.scope.provider_family == ProviderFamily.ANTHROPIC.value` on an openai leg.
  Whether such a record may be **promoted** — and injected back under the primary's family — is
  the open C-MEM-10 question, now with a concrete carrier.
- **The R5-path family conjunct is inert by construction.** With the withhold guard upstream, a
  cross-family dispatch never reaches `_degraded_serve_for_unservable_arm`, so that path's own
  family conjunct can no longer fire. It is retained as defence in depth with its comment corrected
  to say so rather than claiming it is live. Any future change that makes the R5 path reachable
  cross-family must re-verify it.
- **`_request_hash` is computed pre-canonicalization.** `MemoryRetriever.retrieve`
  (`harness-is/src/harness_is/memory_retrieval.py:222`) hashes the **raw** request, then
  canonicalizes at `:227`. Two key-vs-value-equivalent requests that resolve to the same family
  therefore carry **different** `request_hash` values while returning identical results. Not a
  correctness defect — the hash is a determinism/provenance token, not an identity key, and no
  contract requires equivalence — but it is the retrieval-side mirror of the surface-(b) ordering
  rule the write path *does* enforce. Registered here as an observation, not a fork.

## Codex R1 dispositions (2026-07-28)

- **ACCEPTED [P2] — compaction canonicalized only the envelope, not the persisted content.** `complete_compaction` canonicalized the selected `event_scope` while `_disposition_content` persisted every `candidate.suggested_scope` verbatim, so an `ollama` candidate produced an envelope scoped `local_open_weight` over content carrying `provider_family: ollama` (with the content hash, hence the derived `memory_id`, taken over the raw value) and any candidate past the first faced no value-domain check at all. Fixed by canonicalizing-or-denying EVERY candidate scope at the top of `complete_compaction` (`_canonical_candidates`, rebuilding via `model_copy`) ahead of `_scope_from_candidates` / `_disposition_content` / hashing — the same pre-derivation posture slice 2 gave promotion, same registered→canonicalize / out-of-domain→deny split, same `CompactionScopeValueDomainError`. Witnesses: `test_compaction_canonicalizes_the_persisted_candidate_content` (asserts on the parsed CONTENT plus envelope/content-hash agreement) and `test_compaction_denies_an_out_of_domain_candidate_past_the_first` (deny + nothing written, against a positive control); both fail under a revert-the-content-side mutation probe.
- **DECLINED [P1] — "composition-time inject row vs dispatch-time withholding" audit nuance.** This is not an impl defect: the reading was RATIFIED at the spec leg. `Spec_Memory_Substrate_v1.md` C-MEM-13's recording-surface paragraph holds that the existing inject entry satisfies the must-ledger obligation and that the honest per-dispatch report is the C-MEM-19 span; the plan acceptance at `:890` encodes exactly that, and the slice-3 witness carries the comment recording it. Re-opening it is a spec question routed through back-flow, not something to absorb at the impl leg.

## Codex R2 dispositions (2026-07-28)

- **ACCEPTED [P2-a] — the supplied `record_scope` bypassed the write-boundary value domain.** `memory_capture._scope_for_record` returned `self._record_scope` VERBATIM whenever one was supplied, so only the residual construction was bound by the C-MEM-03 value domain; a caller handing in a registered provider KEY (`provider_family="ollama"`) persisted that key into the record, and an out-of-domain identifier persisted too — the exact raw-key partition the residual path was hardened against, reachable through the other branch. Fixed by collapsing both branches onto ONE canonicalize-or-deny: the supplied scope (or, absent one, the residual construction) is run through `resolve_scope_family`, a registered key becomes its `ProviderFamily` value via `model_copy` (so `tenant` / `workload_class` survive), and an out-of-domain value raises `MemoryCaptureScopeValueDomainError` before any write. No-op on the composed production path, which already supplies a canonical scope. Witnesses: `test_supplied_record_scope_with_a_raw_key_is_canonicalized_before_it_persists` (reads the PERSISTED record back and asserts the family value plus surviving `tenant`) and `test_supplied_record_scope_out_of_domain_is_refused_and_writes_nothing` (raises, zero C-MEM-08 rows, no episodic directory); both fail under a revert-to-verbatim-return mutation probe.
- **ACCEPTED [P2-b] — the withhold predicate compared the SCOPE side raw.** `_packet_scope_matches_dispatch_family` canonicalized only the provider side (`provider_family_for_scope_check(provider_name).value`) and compared `scope.provider_family` as the literal string it arrived as. `MemoryScope.provider_family` is a plain `str` and a statically-injected `RuntimeMemoryContext` never runs `compose_for_dispatch`, so an operator-authored context declaring `provider_family="ollama"` on an ollama dispatch compared `"ollama"` against `"local_open_weight"`, reported a CROSS-family mismatch, and withheld BOTH the tool schemas and the packet on a plainly same-family dispatch — via the shared predicate, so the guard, `_degraded_serve_disposition` and the B-83 packet path all inherited the false negative. Fixed INSIDE the shared predicate by canonicalizing the scope side through `memory_scope_family.canonical_scope_family` (the same fail-closed authority), with the B-86(3) sentinel semantics MIRRORED: a `None` canonicalization is "family UNKNOWN" and never compares equal to anything, so an out-of-domain scope value stays report-only. Docstring updated. Witnesses: `test_r2_p2b_raw_provider_key_scope_serves_on_a_same_family_dispatch` (tools armed, no degraded span), `test_r2_p2b_raw_provider_key_scope_still_withholds_across_families` (`codex` → OPENAI family on an ollama dispatch still withheld with `provider_family_scope_mismatch`), `test_r2_p2b_out_of_domain_scope_value_stays_report_only`; the existing `test_b86_unregistered_provider_key_fails_closed_against_local_scope` (the provider-side half) still passes unchanged. Mutation probe: reverting the scope-side canonicalization fails witness 1 while 2/3 and the B-86 witness stay green — exactly the isolation claimed.

## Codex R3 dispositions (2026-07-28)

- **ACCEPTED [P2-a] — an out-of-domain requested family excluded only the PARTITION-SCOPED records.** Both IS read layers (`memory_retrieval._static_exclusion_reason`, `memory_retrieval_index._filter_candidates`) filtered an out-of-domain request with `record_is_family_scoped`, so records carrying `provider_family=None` still flowed through and were returned. That was a THIRD posture: `StandardMemoryToolExecutor._resolved_scope` denies such a call outright and `Spec_Memory_Substrate_v1.md` C-MEM-03 v1.1 says out-of-domain is "rejected" — the layers answered a rejected request anyway, just with less. Fixed by rejecting the REQUEST at both layers: the retriever returns `SCOPE_MISMATCH` for EVERY entry (checked first, so the exclusion reason is uniform rather than per-record), and the derived index returns `()` before the candidate loop (no entries, no refs). The now-unused `record_is_family_scoped` helper was removed. Ripple, same defect one layer over: `CanonicalNativeMemoryToolBackend._require_retrieval_allowed` carried the identical partition-only filter while its own WRITE path (`_require_scope_family_in_domain`) already refused outright — made unconditional, so the adapter has one posture across read and write. The slice-1 module docstring's "serving unpartitioned records widens nothing" rationale is replaced with the aligned-posture one (consistency with the executor's deny + the spec's "rejected"), and it now states explicitly that a `null` REQUEST still reaches unpartitioned records — that is the null semantics, a different disposition from out-of-domain. Witnesses: `test_out_of_domain_request_family_rejects_the_request_at_the_retriever` (zero selected refs and BOTH records excluded `SCOPE_MISMATCH`, with an unpartitioned record seeded as the discriminator), `test_out_of_domain_query_family_rejects_the_query_at_the_derived_index` (end-to-end through the public `retrieve`: no refs, no entries, `considered_count == 2`), `test_null_request_family_still_reaches_unpartitioned_records` (the paired positive — the rejection must not collapse the two dispositions), `test_native_adapter_direct_reader_rejects_an_out_of_domain_scope`. Mutation probe: reverting each layer to the partition-filter-only predicate fails its witness (retriever + index together; the adapter probed separately at the read path).
- **ACCEPTED [P2-b] — `_scope_escapes_source` compared the SOURCE record's stored family raw.** Slice 2 canonicalized the hint side ahead of every derivation, but the source side is a STORED value the ordering rule never reaches: a legacy record persisted under the registered key `ollama` compared unequal to a canonical `local_open_weight` candidate and was falsely flagged `CROSS_SCOPE`, forcing review and blocking auto-promotion for a genuinely same-family promotion. Fixed at the comparison site: `provider_family` is lifted out of the raw-string field loop into `_family_escapes_source`, which canonicalizes BOTH sides through `memory_scope_family.canonical_scope_family` before comparing; an out-of-domain value on either side stays fail-closed (treated as an escape — a review, not a disclosure), and the `source_family is None` case is preserved verbatim from the loop it left. `canonical_candidate_scope`'s docstring is corrected: the ORDERING rule is still load-bearing for `_candidate_id`, but the CROSS_SCOPE half is now fixed at its own site, and the `:913` ordering witness (ii) is re-labelled defence-in-depth accordingly. Witnesses: `test_legacy_raw_key_source_is_not_flagged_cross_scope`, `test_genuinely_cross_family_legacy_source_is_still_flagged_cross_scope` (`claude_code` → ANTHROPIC vs a `local_open_weight` candidate — canonicalizing must not blunt the real flag), `test_out_of_domain_source_family_is_still_flagged_cross_scope`. Mutation probe: reverting to the raw source comparison fails the first witness while the other two stay green.
- **ACCEPTED [P2-c] — run-start capture re-scoped a resumed run's stored envelope.** `LocalAutomaticMemoryRuntime._capture_run_start_once` deduped on `_started_runs`, which is IN-PROCESS only; after a fresh bootstrap resumes a pre-v1.1 run the set is empty, so run-start captured again — and EPISODIC_RUN is stored as ONE `run.json` per `run_id` (`memory_path_registry` `episodic/runs/{run_id}/run.json`, `_memory_id_for` folding the run_id), so `write_record` REPLACED the legacy envelope under today's canonical scope. Silent re-scoping of stored history, against the C-MEM-03 v1.1 forward-only no-rewrite posture. Fixed with durable-presence idempotence: a new minimal, additive read primitive `CanonicalMemoryStore.has_run_record(run_id)` (a PRESENCE probe on the canonical path, deliberately not `read_record` — a legacy/tombstoned/unreadable envelope still counts as present; the EPISODIC_RUN path resolution is de-duplicated into `_run_record_path` so layout stays single-sourced), consulted after the capture-policy gate and before the write; on a hit the run_id is registered in `_started_runs` and the capture is SKIPPED, leaving the stored envelope byte-for-byte untouched. Concurrency is unchanged for a genuinely first write (two processes racing still resolve last-writer-wins, as before); the check closes the resume case, which is the one that rewrites history rather than re-deciding it. Witnesses: `test_run_start_capture_preserves_a_pre_existing_run_envelope` (seeds a legacy raw-key `EPISODIC_RUN` record at the run_id a real dispatch derives, drives `compose_for_dispatch` on a fresh runtime, asserts the `run.json` bytes are IDENTICAL, the stored `provider_family` is still `ollama`, and no C-MEM-08 capture row was appended) and `test_run_start_capture_still_lands_a_new_run_id` (positive control: another run's stored envelope must not suppress this run's first capture). Mutation probe: removing the presence check fails the first witness on byte inequality — the scope is rewritten.

## Codex R4 dispositions (2026-07-28)

- **ACCEPTED [P2] — the R3 durable-presence guard read file presence as capture completion.** `_capture` writes the record BEFORE its C-MEM-08 row (deliberately — a row-first order could vouch for a `memory_id` that was never persisted), so a transient `append_memory_operation` failure raises with `run.json` already durable and no CAPTURE row. R3's `has_run_record` check then made every retry an early return, so the required row was never appended on ANY attempt — a permanently row-less record. Fixed by splitting the presence check into the three states it was conflating: **(1)** record + its run-start row → skip, envelope byte-untouched (the R3 resume case, including genuinely-legacy pre-v1.1 runs whose own capture appended their row at their own capture time); **(2)** record, NO run-start row → torn write → append the **missing row only** via a new `EpisodicMemoryCapture.repair_capture_operation`, against the **stored** envelope's own `memory_id` (never a re-derived one: a legacy record's id need not equal what today's `_memory_id_for` computes for the same `run_id`), with the record not rewritten and its scope not re-derived, so the forward-only no-rewrite posture R3 exists to protect holds unchanged; **(3)** no record → normal first capture. The repair reuses the `action_id` / `idempotency_key` the interrupted attempt would have written, so it lands in exactly the slot that attempt left empty and a concurrent second repair resolves through the ledger's own idempotency rule rather than duplicating — the same semantics two concurrent FIRST captures of one run already have.
- **Read surface: no new ledger machinery was needed.** The presence question is asked by `action_id` (`capture:run_start:{memory_id}`), not by `memory_refs` — run-start and run-close write ONE shared EPISODIC_RUN `memory_id`, so a `memory_refs` match cannot tell the two capture events apart and a closed run would read as an uncaptured one. `action_id` construction is single-sourced at the new exported `memory_capture.capture_operation_action_id` (with `RUN_START_EVENT_KIND`) and the query rides the existing `CanonicalMemoryStore.read_memory_operations()` — the honest read an append-only C-MEM-08 ledger offers, and reached ONLY once a record already exists, so a fresh run never pays for it. One minimal additive read primitive was added, `CanonicalMemoryStore.read_run_record(run_id)` (sibling to `has_run_record`): EPISODIC_RUN is keyed by `run_id`, so `read_record`'s `memory_id` argument is inert for this kind and a caller holding only the `run_id` has no honest value to pass it. An unreadable stored record (legacy shape, corrupt bytes) yields no recoverable identity and therefore no row that could honestly reference it, so that case preserves the R3 skip rather than guessing.
- **Witnesses.** `test_torn_run_start_capture_appends_the_missing_row_without_rewriting` — seeds the record WITHOUT its row, drives a real `compose_for_dispatch`, asserts the `run.json` bytes are identical AND exactly one CAPTURE row now exists carrying `memory_refs == (stored memory_id,)` plus the run-start `action_id` (load-bearing: the seeded legacy record is keyed by the generic content-hash derivation, not the run-id digest, so a re-derived reference would point at nothing). `test_run_start_capture_preserves_a_pre_existing_run_envelope` (the R3 witness) is amended to the FULLY-CAPTURED shape — record AND row seeded, via the new `_seed_run_start_capture_row` helper — and asserts the envelope is untouched with no duplicate row; record-only is now the distinct torn case. `test_run_start_capture_still_lands_a_new_run_id` (fresh-run positive control) is unchanged and green. Mutation probes: **(a)** reverting to file-presence-only fails the torn witness (zero CAPTURE rows) while the fully-captured witness stays green; **(b)** routing the repair through the full `capture_run_start` instead of the row-only append fails the torn witness's byte-equality assert — the envelope is re-authored.

## Codex R5 dispositions (2026-07-28)

- **ACCEPTED [P2] — the R4 repair payload was built from the REPAIRING worker's dispatch metadata.** `_complete_torn_run_start_capture` sourced `provider` / `model` / `cli_profile` / `engine_class` / `policy_ref` from `context.selection` + the live `binding`, and `actor` from `step_context.parent_actor`, while the probe (`_run_start_capture_row_present`) and the append are not one atomic step. Two workers resuming the same torn run could therefore both pass the probe and append under ONE `idempotency_key` with UNEQUAL payloads: `append_memory_operation`'s equivalence check (`memory_operation_ledger.py:530-539`, an 18-field comparison that deliberately EXCLUDES `timestamp`) saw divergence, raised `MemoryOperationIdempotencyConflictError`, `repair_capture_operation`'s broad `except Exception` folded it into a `FAILED` result, and `_raise_capture_failure` killed the second worker's run — over a row that was already durable. Fixed in both halves. **(1) Stable payload.** `repair_capture_operation` now takes the stored `MemoryStoreRecord` and `run_id` ONLY (`_stored_run_envelope` became `_stored_run_record`; the dead `engine_class` parameter on the call path was dropped), and composes a payload that is a PURE FUNCTION of them: `action_id` / `idempotency_key` / `memory_refs` from the stored envelope's own `memory_id` as before; `cli_profile` / `engine_class` / `step_id` read back from the STORED CONTENT, where the torn capture itself recorded them (via strict `_stored_text` / `_stored_engine_class` — deliberately non-coercing, unlike `_optional_string`, so an unexpected stored shape reads as `None` rather than being `str()`-invented); `run_id` the very key the record was read by; `provider` / `model` / `policy_ref` / `procedural_snapshot_ref` `None`, because the stored record does not carry them (`provider_route` is the CHAIN, not the access-mode SELECTION the torn payload recorded) and they describe the dispatch that tore, which the repairer is not — `None` is the honest deterministic value, since a repair row attests that a capture is durable, not which dispatch performed it; and `actor` from a new exported `memory_capture.capture_repair_actor(run_id)` (`agent` / `memory-capture-repair:{run_id}`), single-sourced for the same reason `capture_operation_action_id` is. `_operation_payload` gained an explicit `actor` parameter so the normal `_capture` path still binds `self._actor` unchanged. **(2) Conflict read as completion.** `repair_capture_operation` re-raises `MemoryOperationIdempotencyConflictError` instead of folding it into the `FAILED` result (it is a concurrency signal, not an IO fault; every other append failure returns `FAILED` unchanged), and `_complete_torn_run_start_capture` catches it and returns. This is honest, not a swallow: the ledger raises ONLY when an entry already occupies that exact `idempotency_key`, and that key names this one capture of this one record — so the conflict is positive evidence the row is durable. Half (2) is retained after half (1) precisely because it covers the residual class half (1) cannot: the ORIGINAL torn attempt's own append landing inside the probe window, legitimately carrying dispatch metadata a repair cannot know.
- **Witnesses.** `test_torn_repair_payload_is_worker_invariant` — two capture APIs bound as two workers would bind them (different `actor`, different `record_scope`) repair the same stored record; the first `APPENDED`, the second `IDEMPOTENT_NOOP` (the ledger's own equivalence check IS the assertion), one row, attributed to `capture_repair_actor(run_id)` with the STORED `cli_profile` and `provider`/`model`/`policy_ref` all `None`. `test_two_workers_repairing_one_torn_run_both_succeed` — the integration shape: two fresh `materialize_automatic_memory_runtime` bootstraps (per-runtime `_started_runs` is what a second process actually looks like) on genuinely different dispatch metadata (`ollama`/`llama3.1`/LOCAL_OPEN_WEIGHT then `anthropic`/`claude-sonnet-4`/ANTHROPIC), neither raising, exactly one row. `test_a_conflicting_row_in_the_race_window_is_read_as_completion` — a divergent row seeded under the same `action_id`, the run completing and the seeded row standing untouched. The last two hold the probe window open by patching `_run_start_capture_row_present` to report ABSENT — the only fake in any of the three; both repairs run for real against the real ledger, and the probe is precisely the seam the race lives behind. All three R4 witnesses (torn repair, fully-captured skip, fresh run) stay green. **Mutation probe:** reverting BOTH halves (payload `actor` back to `self._actor` and `provider` back to a per-worker binding; call-site catch removed) fails all three new witnesses with `MemoryOperationIdempotencyConflictError` while the three R4 witnesses stay green — the isolation claimed, and confirmation that nothing pre-existing covered this class.
- **Verification.** `harness-runtime/tests/test_u_mem_26_write_boundary.py` 34 passed; the memory-surface set (`test_u_mem_26_write_boundary` + `test_memory_capture` + `test_automatic_memory_runtime` + `test_memory_context` + `test_memory_migration_compat` + `test_memory_verification_suite`) 62 passed; `harness-is` 368 passed; full `harness-runtime` **3259 passed / 27 skipped / 3 failed** in 390s, the 3 failures all live-Ollama e2e nodes from the documented load-flake identity list. Identity re-verified rather than assumed: `test_r300_live_ollama_provider_fallback_exercise` passes standalone (the positive control), and the other two (`test_r_pm_1_active_prompt_injection_honored_by_live_ollama`, `test_r_pm_1_workload_selection_drives_live_ollama_injection`) fail IDENTICALLY at pristine HEAD with this arc's changes stashed — pre-existing live-model nondeterminism (their assertion messages differ run to run), untouched by this fix. `ruff check` + `ruff format` clean; canonical `uv run pyright` 0 errors / 0 warnings.

## Codex R6 dispositions (2026-07-28)

- **ACCEPTED [P1] — the REVERSE ordering of the R5 race aborted an otherwise-successful run.** R5 closed the repairer-vs-repairer race and caught the repair LOSING to the original append. The mirror ordering was open: a second worker observes `run.json` while the ORIGINAL capture sits between its `write_record` and its ledger append, reads it as torn, and completes it with `repair_capture_operation`'s worker-invariant synthetic payload; the original's own append — legitimately carrying the dispatch metadata (`actor`, `provider`, `model`, `policy_ref`) a repair cannot know — then raised `MemoryOperationIdempotencyConflictError` into `_capture`'s broad `except Exception`, which returned `FAILED`, and `_raise_capture_failure` killed a run whose accounting was in fact durably complete. Fixed symmetrically at the ORIGINAL append site: `_capture` wraps `append_memory_operation` in its own narrow `except MemoryOperationIdempotencyConflictError` and reads the conflict as CAPTURED (`operation_result=None`, the field already being `MemoryOperationWriteResult | None`). The evidence is the same as R5's: `append_memory_operation` (`memory_operation_ledger.py:530-539`) raises ONLY when an entry already occupies this exact `idempotency_key`, and that key is a function of this `event_kind` and the `memory_id` of the record THIS worker just wrote — so the occupant can only be the repair row for this capture or a concurrent identical one; either way the record is written and the row is present, which is exactly what CAPTURED asserts. The span needs no annotation: it already declares `policy_decision=captured`, and no `failure_class` belongs on a non-failure. **Scope discriminator: `event_kind != RUN_START_EVENT_KIND` re-raises**, into the pre-existing broad handler, so every other capture kind still surfaces a conflict as `FAILED`. That is the narrow scope the finding asked for and it is grounded, not conservative-by-default: `repair_capture_operation` is reached from exactly one call site (`_complete_torn_run_start_capture`), always with `RUN_START_EVENT_KIND`, so no other event kind has a second writer sharing its keys, and `event_kind` is the whole discriminator because it is what the `action_id` — hence the idempotency key — is built from. No threading of a new discriminator was needed; `_capture` already takes `event_kind`.
- **Accepted information loss, documented in the catch.** Under this ordering the DURABLE row is the repair's synthetic payload (`provider` / `model` / `policy_ref` `None`), not this dispatch's richer metadata. Correcting it would mean REWRITING a row in an append-only, hash-chained ledger — a strictly worse trade than losing four attributes on a row whose purpose is to attest that the capture is durable. Verified by read that no envelope-divergence question arises here: in this ordering the original worker wrote the record ITSELF (the repair writes no record at all), so the stored content is that worker's own, complete.
- **Witnesses.** `test_a_repair_landing_before_the_original_append_completes_the_run` — holds the window open from the other side via `_repair_between_write_and_append`, which wraps `CanonicalMemoryStore.write_record` so a second worker's REAL `repair_capture_operation` runs against the REAL ledger between this capture's record write and its append (the record write is precisely the seam the window lives behind, the mirror of R5's `_force_probe_to_miss`); asserts the repair `APPENDED` (it genuinely won), `compose_for_dispatch` does not raise, exactly ONE row exists attributed to `capture_repair_actor(run_id)` with `provider`/`model` `None` (the documented loss), `memory_refs` naming the stored envelope, and the `run.json` bytes identical to what this worker wrote, carrying its own `provider_route`. `test_a_non_run_start_idempotency_conflict_still_fails_the_capture` — the negative control proving the scope is narrow: two `capture_provider_route` captures of IDENTICAL content (hence one `action_id`) under divergent dispatch metadata still return `FAILED` with `MemoryOperationIdempotencyConflictError` in `failure_reason`, and the divergent row never lands. All R4 + R5 witnesses stay green. **Mutation probes, both directions:** removing the catch (`raise` only) fails the new reverse-order witness with the conflict while the other 35 stay green; WIDENING it (dropping the `event_kind` guard) fails the negative control while the reverse-order witness passes — so both the catch and its scope are load-bearing, and neither is covered by anything pre-existing.
- **Verification.** `test_u_mem_26_write_boundary.py` 36 passed; the targeted memory surface (`+ test_memory_capture + test_automatic_memory_runtime`) 48 passed; `harness-is` 368 passed; full `harness-runtime` **3262 passed / 27 skipped / 2 failed** in 378s. Both failures are documented live-Ollama load-flake identities, and identity was re-verified by the leg-close method rather than assumed: with this arc's three files stashed at pristine HEAD, `test_r_pm_1_active_prompt_injection_honored_by_live_ollama` and `test_r_pm_1_workload_selection_drives_live_ollama_injection` fail IDENTICALLY (68s, arc changes absent) — pre-existing live-model nondeterminism, untouched by this fix. `ruff check` + `ruff format --check` clean; canonical `uv run pyright` 0 errors / 0 warnings.
