# Round-8 sweep answers — u-he-50

- codex_review_wrapper:66bb3a1b34304181e436f8818163ba5619a5ab65:f426492a0ce0:1 (P2,
  immutable start before in-process GATE_REFUSED) — REJECTED, standing disposition:
  fifth raise of the r5-registered residual (same finding hash f426492a0ce0 as r6/r7).
  The session layer cannot observe admit(); the structural fix is the registered
  B-218 wrapper-internal emitter; the recipe comment names the residual.
  Register-and-hold. Disposed rejected.
- codex_review_wrapper:66bb3a1b34304181e436f8818163ba5619a5ab65:db8a739dd31d:1 (P2,
  end replay closes an earlier invocation's start) — REJECTED with witnessed grounds:
  this restates the r1-ACCEPTED named measurement bound
  (`test_recipe_crash_window_closed_by_retry_end` pins it deliberately) and, taken to
  its conclusion, contradicts the CLEARED spec design itself — C-HE-27 §3's v1
  decision is one durable pair per phase with no attempt identity, and the ship-pr
  definition states the disjoint-pairs/upper-bound semantics ("the recorded
  denominator is round-1 review + the fix window") as the accepted representation
  bound. Cross-invocation closing IS how the round-1 window survives re-review
  rounds 2..n re-emitting as no-ops; demanding per-invocation pairs is a spec-design
  change, which routes to design-phase back-flow, not to this applying-arc's diff.
  Disposed rejected.

Class-sibling sweep: no code changed this round — both findings restate registered
dispositions; nothing to sweep. The codex channel has raised the same residual shape
in rounds 5, 6, 7, and 8 with no new facts; per the workspace's register-and-hold
discipline the arc proceeds to the decorrelated 3-lens merge-gate, which reads these
adjudication rows.

## Grep-hit labels, answered verbatim (no fix diff this round)

- [save/restore pair: two-armed restore] — no diff.
- [check-then-act on paths] — no diff.
- [bare counts/absolutes in prose] — round-count claims above are bound to the
  ledger's rows for this arc (grep `u-he-50` in .harness/merge-gate-log.jsonl).
- [new retry/timeout constants] — no diff.

No findings deferred.
