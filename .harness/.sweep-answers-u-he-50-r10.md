# Round-10 sweep answers — u-he-50 (budget-terminal round)

- codex_review_wrapper:171f0911693eb51e5f943fa04e7f11ba2e4b4703:f426492a0ce0:1 (P2,
  immutable start before in-process GATE_REFUSED; prescription "emit start only after
  the authoritative admission") — REJECTED, standing disposition: seventh raise of
  the r5-registered residual (finding hash f426492a0ce0 identical across rounds
  6/7/8/10). The prescribed fix requires emitting from INSIDE codex_review.py after
  admit() — the session/recipe layer cannot do it, and that in-process emitter is the
  registered B-218 item, named in the recipe comment. Disposed rejected.

Terminal state: 10 rounds spent (the full budget), 20 findings total — 10 absorbed
with tests/fixes, 10 rejected with witnessed grounds; every finding carries an
adjudication row (counted programmatically from the ledger). Rounds 8–10 raised zero
new shapes. Per register-and-hold the arc proceeds to the decorrelated 3-lens
merge-gate, which reads these rows.

## Grep-hit labels, answered verbatim (no fix diff this round)

- [save/restore pair: two-armed restore] — no diff.
- [check-then-act on paths] — no diff.
- [bare counts/absolutes in prose] — the 20/10/10 counts above were recomputed
  programmatically from .harness/merge-gate-log.jsonl this round.
- [new retry/timeout constants] — no diff.

No findings deferred.
