# Post-Phase-8 forward-work register — structured source of truth.
#
# WHY THIS EXISTS: `.harness/post-phase-8-forward-register.md` (the prose home) grew to
# 36 B-* items / ~14K words of hand-maintained markdown with no queryable structure —
# the same "hand-maintained markdown drifts / is expensive to re-read from a fresh
# context" defect class R-IF-114 fixed for the R-FS-1 build arc via
# `.harness/arc-ledger.yaml` + `tools/arc_ledger.py`. That fix does NOT cover THIS
# register: `arc-ledger.yaml`'s own `rfs1_status: resolved` pin REQUIRES zero open
# standalone rows (see `tools/arc_ledger.py`'s `RFS1_ZERO_OPEN_ALLOWED` invariant) —
# R-FS-1/R-FS-2 are CLOSED build arcs, and adding new post-closure forward items there
# as `status: registered` rows would hard-fail `arc_ledger.py --check` (verified
# empirically before this file was authored — do not fold this register into
# arc-ledger.yaml). This is a SIBLING schema/tool for a DIFFERENT, still-open
# lifecycle bucket, mirroring arc-ledger.yaml's proven pattern: one structured source,
# a pure derive(), a validate() with a snapshot pin, and a blocking `--check` CI gate.
#
# PROSE HOME: rich per-item prose (What it is / Current state / Close-out steps /
# Council rationale) stays at `.harness/post-phase-8-forward-register.md` — each row
# below carries only a one-line `summary` + `close_out` + a `heading` back-pointer
# (the exact `### B-NN ·...` markdown heading text) so a query tool can pull the full
# prose on demand. No prose is duplicated into this file.
#
# STANDING DIRECTIVE (operator 2026-07-14, [[full-register-ground-every-arc-before-blocking]],
# Project_Roadmap_v1.md §5.0b): every row below — INCLUDING registered_finding /
# operator_gated / design_substrate_gated / the empty B-16 speculative bucket — gets an
# active grounding pass against the cited spec/ADR/plan section(s) before it is built or
# left open. The status enum below describes CURRENT state, not a license to skip
# investigation. Only `held` (an already-ratified operator DEFER) stops without a fresh
# grounding pass.
#
# STATUS ENUM (the ONLY place this mapping lives):
#   open                   — actionable now, no blocker.
#   design_substrate_gated — a Class 1 design-substrate change gates the build (X-AL-3):
#                            the fork doc is either OWED, or FILED and awaiting operator
#                            ratification + the spec-apply pass (the row's summary says
#                            which phase; a filed doc does NOT flip the status — only
#                            ratified + applied spec deltas — plus the corresponding Phase-6
#                            plan deltas where the change touches a plan-pinned contract —
#                            open the row for code).
#   registered_finding     — a real gap surfaced during another arc's review/build;
#                            grounded against the spec (not skipped), and opens for BUILD
#                            once grounding confirms a genuinely buildable slice or a live
#                            consumer/trigger appears (the "register-then-triage, not drop"
#                            discipline — 2026-07-14: triage means investigate, not defer).
#   operator_gated         — needs a credential / D-ADR / cloud provisioning decision
#                            only the operator can make; still grounded + Claude-slice-built
#                            up to that genuine gate first.
#   held                   — an explicit operator DEFER-INDEFINITELY ratification;
#                            not silently reopened.
#   closed                 — built + merged, or foreclosed/superseded; kept for history.
#
# FORWARD-ONLY: a real transit (an item opens/closes, a new one surfaces) edits the
# row AND bumps the `snapshot:` block (counts AND `identity_digest`) in the SAME
# commit — mirrors arc-ledger.yaml's own forward-only discipline.
# `identity_digest` = sha256 of the sorted "id:status" pairs; catches two items
# swapping statuses while the aggregate counts stay put (a plain count pin can't).
# `tools/forward_register.py --check` is the CI gate.


snapshot:
  total: 180
  open: 1
  design_substrate_gated: 1
  registered_finding: 38
  operator_gated: 0
  held: 2
  closed: 138
  identity_digest: 4ed27d8a50cd0452
items:
- id: B-1
  title: Layered capability-aware routing activation
  status: closed
  summary: R-300 multi-LLM routing activation — declarative layer live; the multi-provider exercise is
    B-2.
  pr: '#213'
  heading: '### B-1 · Layered capability-aware routing activation `⚖️ council-eligible`'
- id: B-2
  title: Multi-provider credentials + mixed-provider exercise
  status: closed
  summary: Deterministic fallback + live Anthropic->OpenAI and live Ollama exercise.
  pr: '#281 + #283'
  heading: '### B-2 · Multi-provider credentials + mixed-provider exercise'
- id: B-3
  title: Real TIER_2_CONTAINER sandbox execution (R-410)
  status: closed
  summary: Local Docker execution driver + live TIER_2 container e2e.
  pr: R-410
  heading: '### B-3 · Real TIER_2_CONTAINER sandbox execution (R-410) `⚖️ council-eligible`'
- id: B-4
  title: TIER_3 microVM + TIER_4 full-VM execution (R-411/R-412)
  status: closed
  summary: Docker+gVisor/runsc driver (R-411) + managed E2B full-VM driver (R-412).
  pr: R-411/R-412
  heading: '### B-4 · TIER_3 microVM + TIER_4 full-VM execution (R-411 / R-412) `⚖️ council-eligible`'
- id: B-5
  title: SELF_HOSTED_SERVER + MANAGED_CLOUD deployment e2e (R-420/R-421)
  status: closed
  summary: Local self-hosted daemon+collector+keyring e2e; E2B+GCP Secret Manager+Cloud Run e2e.
  pr: R-420/R-421
  heading: '### B-5 · SELF_HOSTED_SERVER + MANAGED_CLOUD deployment e2e (R-420 / R-421)'
- id: B-6
  title: OTLP tail-keep collector-side validation (R-430)
  status: closed
  summary: Resolved by the R-420 local real-collector tail-keep live proof.
  pr: '#326'
  heading: '### B-6 · OTLP tail-keep preservation validation (R-430)'
- id: B-7
  title: Tier-level self-hosted secrets backend selector (R-440)
  status: closed
  summary: SELF_HOSTED keyring-only selector landed; cloud bootstrap-token is R-421.
  pr: R-440 (RESOLVED)
  heading: '### B-7 · Tier-level self-hosted secrets backend selector (R-440)'
- id: B-8
  title: Non-default tenant_id / non-SOLO persona_tier deployment
  status: closed
  summary: R-500 self-hosted multi-tenant live proof (redaction + tenant-scoped audit).
  pr: '#328'
  heading: '### B-8 · Non-default `tenant_id` / non-SOLO `persona_tier` deployment `⚖️ council-eligible`'
- id: B-9
  title: 'OD-4: per-session redaction toggle + opaque-token tokenization'
  status: closed
  summary: R-008 + batch-53 RESOLVED; runtime residual closed, live ledger back-flowed.
  pr: R-008
  heading: '### B-9 · OD-4: per-session redaction toggle (§13.1) + opaque-token tokenization (§13.2) `⚖️
    council-eligible`'
- id: B-10
  title: Real external MCP server connection
  status: closed
  summary: 'R-800 RESOLVED; stdio + streamable-HTTP e2e live. api.run Gap D closed at PR #515.'
  pr: '#172, #515'
  heading: '### B-10 · Real external MCP server connection'
- id: B-11
  title: Files API integration (AS-8e / CP-17)
  status: closed
  summary: R-810 RESOLVED — live Anthropic Files upload/reference/delete + Cloud Trace proof.
  pr: R-810
  heading: '### B-11 · Files API integration (AS-8e / CP-17)'
- id: B-12
  title: managed_agents integration (AS-8f)
  status: closed
  summary: R-820 RESOLVED by real Anthropic Managed Agents SDK/session live e2e.
  pr: '#380'
  heading: '### B-12 · managed_agents integration (AS-8f)'
- id: B-13
  title: Memory-tool production backend (CP-16)
  status: closed
  summary: Filesystem + SQLite + S3 + managed-PostgreSQL (Neon) backends live-proven.
  pr: R-830
  heading: '### B-13 · Memory-tool production backend (CP-16)'
- id: B-14
  title: CXA cross-axis seam completion (CXA-1..5)
  status: closed
  summary: All 5 CXA seams RETIRED (batch-53..57); mechanical wiring, no design tension.
  pr: batch-53..57
  heading: '## CXA — Cross-axis composition seam completion (B-14)'
- id: B-15
  title: Dashboard iteration-2 (R-XI-02/R-XI-03)
  status: closed
  summary: Both RESOLVED 2026-06-02; stale-open framing corrected 2026-07-14.
  pr: '#235, #237'
  heading: '### B-15 · Dashboard iteration-2 (R-XI-02 / R-XI-03) — **✅ CLOSED (stale-open correction,
    2026-07-14)**'
- id: B-16
  title: Open architectural / speculative arcs (Surface X research)
  status: registered_finding
  summary: Deliberately unpadded standing bucket for not-yet-decomposed forward research.
  close_out: Operator-seeded when a specific research question crystallizes; no concrete item today.
    2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT; the bucket terms above are
    unchanged at HEAD and nothing has matured into the bucket since the R-901 worked example was
    crystallized, closed RESOLVED and struck at the 2026-07-14 accuracy audit; disposition
    VERIFIED-DORMANT (wakes only on an operator-seeded research question).
  council: conditional — case-by-case, only if a named cross-domain tension surfaces
  heading: '### B-16 · Open architectural / speculative arcs'
- id: B-17
  title: ICM workspace-methodology adoption
  status: closed
  summary: RATIFIED 2026-07-30 — all 3 audit-§8 gates answered. Scope governance-layer-only;
    ICM <80/<200-line caps adopted for CONTEXT.md / router files ONLY (design-substrate specs
    exempt, a documented divergence); L0 refactor CONSERVATIVE. Delivered by PR `#1161`.
  close_out: DELIVERED by PR `#1161` — R-ICM-1 (root CLAUDE.md §2.3/§2.4 change-note lineage relocated byte-exact to
    .harness/claude-artifact-pointers.md, live governance kept inline) + R-ICM-2 (workspace
    CONTEXT.md task-router); the row closed at that merge. FORWARD QUESTION preserved for the
    deployment-surface arc — ICM integration into the final deployed harness package/repo is the
    better long-term question, and belongs there, not to this repo's governance layer.
  council: not convened — the nameable context-minimalism (C2-style) vs governance-completeness
    tension was answered directly by the operator ratification of 2026-07-30 (governance-only
    scope + conservative L0 split), so no convening is owed.
  pr: '#1161'
  heading: '### B-17 · ICM workspace-methodology adoption *(appended 2026-06-04 at HEAD `ca2bc34`)*'
- id: B-18
  title: U-1 — cache_control breakpoint emission
  status: closed
  summary: 'BUILT 2026-07-10 across 5 slices (PRs #919-923); stale-open framing corrected 2026-07-14.'
  pr: '#919-#923'
  heading: '### B-18 · U-1 — `cache_control` breakpoint emission *(NET-NEW, registered 2026-07-09)* —
    **✅ BUILT 2026-07-10 (stale-open correction, found 2026-07-14 at forward-register-schema grounding,
    out-of-family Codex)**'
- id: B-19
  title: breaker.cause + breaker.cooldown_ms ambient attrs
  status: closed
  summary: BUILT 2026-07-13 as breaker.tripped event attrs; cause vacuous-by-honest-design.
  pr: R-FS-2 Wave 4
  heading: '### B-19 · U-2 — breaker `breaker.cause` + `breaker.cooldown_ms` attributes *(NET-NEW, registered
    2026-07-09; operator-discretionary)* — **✅ BUILT 2026-07-13**'
- id: B-20
  title: CP-side harness.breaker.* composition-mirror attribute-name drift
  status: closed
  summary: BUILT 2026-07-14 — 7 CP-side breaker schema names renamed/split/dropped to byte-match OD's
    canonical §7.1 names.
  pr: '#994'
  council: no — mechanical once the target shape is decided (already known from OD's own schema)
  heading: '### B-20 · CP-side `harness.breaker.*` composition-mirror attribute-name drift *(NET-NEW,
    registered 2026-07-13 at Wave-5 hygiene grounding)* — **✅ BUILT 2026-07-14**'
- id: R-1
  title: Managed-cloud deployment-surface dispatch (confirmed-DEFERRED)
  status: held
  summary: AS-8f DEFER-INDEFINITELY (2026-05-28) reaffirmed 2026-07-09 — FULL-SPEC directive does not
    override this held decision.
  heading: '### R-1 confirmed-DEFERRED · Managed-cloud deployment-surface dispatch'
- id: B-21
  title: PARALLELIZATION nested-child-pause capability gap
  status: closed
  summary: BUILT 2026-07-13 — PeerFanOutResumeState.paused_child_branches added, mirrors v1.45.
  pr: CP spec v1.97
  heading: '### B-21 · PARALLELIZATION nested-child-pause capability gap *(preflight HIGH #6, `workflow_driver.py:7787`)*
    — **✅ BUILT 2026-07-13**'
- id: B-22
  title: F5 real audit-signature backend seam
  status: closed
  summary: Seam scope CLOSED 2026-07-14 (fail-loud + SigningBackend protocol); concrete backend split
    to B-36.
  pr: '#976'
  heading: '### B-22 · F5 real audit-signature backend seam *(preflight CRITICAL #1 follow-on)* — **✅
    CLOSED 2026-07-14 (seam scope only — see `B-36` for the split-off concrete-backend follow-on)**'
- id: B-23
  title: Cost-attribution composers fabricate their audit entry's IS-anchor
  status: closed
  summary: BUILT 2026-07-13 — shared compose_cost_f2_entry_core helper wired at all 4 composers.
  pr: cost_attribution_f2_write
  heading: '### B-23 · Cost-attribution composers fabricate their audit entry''s IS-anchor *(preflight
    §4 "most urgent", `cp_audit_conversion.py:161`)* — **✅ BUILT 2026-07-13**'
- id: B-24
  title: secret_negative_observation.py wrong NegativeObservationSurface label
  status: closed
  summary: BUILT 2026-07-15 — WORKFLOW_MANIFEST_ENTRY enum member + real dispatch dict added. Codex
    round 2 caught the enum's canonical signature lives at the PLAN (Implementation_Plan_Action_Surface_v1.md
    U-AS-21), not the spec — a plan delta (v1.5) + clearance marker were required and filed same-PR.
  pr: Implementation_Plan_Action_Surface v1.5
  close_out: Ratified Q1=A/Q2=a/Q3=a per filer rec (mechanical, zero prod callers). 9/9 tests pass.
  council: no — mechanical enum addition once the fork ratifies the member name
  heading: '### B-24 · `secret_negative_observation.py:127` wrong `NegativeObservationSurface` label —
    needs a new enum member *(design-substrate)* — **✅ BUILT 2026-07-15**'
- id: B-25
  title: sandbox_tier_floor.py — is_deterministic_inhouse=False branch undefined
  status: closed
  summary: RESOLVED 2026-07-15 — Q1 = Reading A (non-forcing, spec-text-only). A dyadic C10⊥C4 council
    convening (run per the fork's own Q3 recommendation) found is_deterministic_inhouse carries zero
    verification today, so a floor bump would be spoofable by the actor it targets while penalizing
    honest authors who never opted in; both voices converged on Reading A over the filer's original
    Reading-B lean. Operator selected Reading A via AskUserQuestion from the council's 4-option synthesis.
    ADR-D2 v1.2→v1.3 + Spec_Action_Surface_v1.md v1.13→v1.14 (where-block row 7 + keying paragraph
    reconciled; stale ToolContract/MCPClientConfig/ToolMetadata docstrings corrected); no code behavior
    change (production already implemented Reading A); new mutation-probed witness test added.
  close_out: Fork doc FILED 2026-07-14, addendum §1.5 added 2026-07-15, RATIFIED+CLOSED 2026-07-15
    (class_1_fork_sandbox_tier_floor_deterministic_inhouse_false_undefined.md). Clearance markers at
    .harness/clearance/adr-d2-v1-3-cleared-2026-07-15.md,
    .harness/clearance/spec-action-surface-v1-14-cleared-2026-07-15.md, and (plan-layer correction
    caught by out-of-family codex-review round 1, same category as B-24)
    .harness/clearance/implementation-plan-action-surface-v1-6-cleared-2026-07-15.md.
  pr: "#PENDING"
  council: ✅ convened 2026-07-15 (C10 action-safety/blast-radius + C4 tool-contract-semantics) — decisive
    probe finding (field is unverified/spoofable) converged both voices on Reading A
  heading: '### B-25 · `sandbox_tier_floor.py:54` — `is_deterministic_inhouse=False` branch has no defined
    floor *(design-substrate)* — **✅ RESOLVED 2026-07-15 (Reading A via council + operator decision)**'
- id: B-26
  title: five_axis_composition.py verify_rotation_6_steps hardcoded-True stub
  status: closed
  summary: BUILT 2026-07-13 — real IS hash-chain verification wired; 2 new genuine CXA P1 seams.
  pr: VERIFY_HASH_CHAIN_LINK
  heading: '### B-26 · `five_axis_composition.py:320` — `verify_rotation_6_steps` hardcoded-`True` stub
    — **✅ BUILT 2026-07-13**'
- id: B-27
  title: cli/app.py paused (HITL) workflow status shares failed's exit code
  status: closed
  summary: BUILT 2026-07-15 — runtime spec v1.99→v1.100 NEW §14.18.2 row (exit 5 PAUSED); EXIT_PAUSED
    wired at both CLI call sites. Codex round 1 caught the initial PAUSED_RESUMABLE naming overclaiming
    uniform snapshot-resumability; round 3 caught the follow-on "plain re-invocation resumes engine-native
    pauses" claim was also false + P1-unsafe (fresh run_id per invocation can't locate the prior
    (workflow_id,run_id)-keyed pause record) — spec now states plainly no safe resume path exists yet
    for WAL_SEGMENT/RECONCILER_LOOP pauses.
  pr: Spec_Harness_Runtime v1.100
  close_out: Ratified Q1=A/Q2=ii/Q3=b per filer rec. Clearance marker filed. 28/28 tests pass.
  council: no — mechanical once the disposition is decided
  heading: '### B-27 · `cli/app.py:347` — `paused` (HITL) workflow status shares `failed`''s exit code
    *(design-substrate)* — **✅ BUILT 2026-07-15**'
- id: B-28
  title: Test-quality findings untriaged (preflight report §6)
  status: closed
  summary: TRIAGED 2026-07-14 — 17 fixed / 1 stale / 1 discovered-and-registered (B-35).
  pr: preflight §6 triage
  heading: '### B-28 · Test-quality findings untriaged *(preflight report §6 — 19 findings, 3 high / 10
    medium / 6 low)* — **✅ TRIAGED 2026-07-14**'
- id: B-29
  title: Pre-existing F2-write gaps surfaced at B-23 (branch_metadata + shared clock)
  status: closed
  summary: 'Dormant sub_agent_dispatch.py-shared gaps: branch_metadata sidecar unpopulated; no shared
    time_source consumer.'
  close_out: 'CLOSED 2026-07-15 (branch_metadata half only; time_source half split to B-44). The council-conditional
    framing was stale -- the carrier-ownership question was already resolved as a Class 1 fork on 2026-06-13
    (.harness/class_1_fork_b1_branch_causality_route_x_vs_y.md, Route Y): CP spec v1.32 §25.13 names CP
    WorkflowDriver as sole producer, IS spec v1.8 §5.4 confirms it, and WorkflowDriver.append_branch_step_ledger_entry/append_branch_terminal_ledger_entry
    already populate branch_metadata at ~19 real production call sites. sub_agent_dispatch.py''s dispatch-fact
    entry and the 4 cost-attribution composers are NOT branch-spawn/terminal entries per §25.13''s scope,
    so their branch_metadata=None default is spec-conformant, not a gap. Added a one-line clarifying comment
    at both producer sites citing the spec so a future reader doesn''t re-flag this. No behavior change;
    ruff clean; 155 tests pass.'
  council: no — spec already resolved the carrier-ownership question at a prior cleared fork; no live
    tension to convene
  pr: '#1023'
  heading: '### B-29 · Pre-existing F2-write gaps surfaced at B-23 (out-of-family Codex, not novel to
    B-23) *(registered 2026-07-13)*'
- id: B-30
  title: Cost-attribution disambiguator replay-semantics inconsistency
  status: registered_finding
  summary: 'Validator''s F2 anchor write (only) IDEMPOTENT_NOOPs on a deterministic-disambiguator replay;
    tool/LLM/webhook always create a new F2 entry. The AUDIT-LEDGER append specifically is PROVEN to dedupe
    for LLM+JOURNAL_RESUME (existing witness test, on-disk audit: entry count only -- the run-scoped cost
    accumulator/RunResult.cost_attribution is NOT exercised by that test) and UNVERIFIED for the other
    3 composers/engine classes (correction, 2026-07-15). NOT an undecided policy, but also not just a
    missing consumer: ADR-D6 v1.2 §1.5 + C-OD-14 §14.5.1 already specify a complete replay-dedup algorithm
    (harness_od.idempotency_join_dedup.dedupe_on_replay) with no production ingestion-layer caller anywhere
    in the codebase, AND all 4 composers hard-code engine_replay_disposition=NO_REPLAY with no override
    -- a producer-side gap in addition to the missing consumer.'
  close_out: 'NOT closed -- sharpened, not council-eligible, not yet buildable. Personally verified (not
    just the grounding agent''s paraphrase) that ADR-D6 v1.2 §1.5''s algorithm is specified as ingest_span(span)
    -- a TRACE-INGESTION-time function operating on spans read from the observability pipeline -- not
    something a per-dispatch composer calls inline while producing its own F2 write. Wiring dedupe_on_replay
    directly into the 4 attribute_*_dispatch_cost composers (as an initial grounding pass recommended)
    would be a category error: the correct call site is an ingestion-layer consumer that doesn''t exist
    in production yet, not merely an unimplemented wiring step inside the composers. Separately, the prior
    B-COST-REPLAY-DEDUP-WITNESS closure record (.harness/b-cost-replay-dedup-witness-closure-record.md,
    CLOSED GREEN no-fix) already verified the IS-layer content-addressed ledger write independently provides
    the real replay-dedup guarantee for the JOURNAL_RESUME/PURE_PATTERN_NO_ENGINE engine class, and its
    own scope note explicitly pre-commits any adjacent replay-dedup finding to register-don''t-build,
    not a broadening of that arc. Webhook''s fresh-uuid4-per-attempt billing is confirmed deliberate --
    the actual rationale lives at webhook_delivery_composer.py:363-371''s inline comment (''this method
    fires unconditionally on every invocation -- no idempotent skip at this layer. A fresh UUID per call
    guarantees each invocation gets its own F2 anchor''), an intentional out-of-family-Codex-round-5 fix,
    not an oversight to align away. Out-of-family Codex flagged three further nuances in THIS correction
    across three rounds, all addressed same-PR: (1) the paragraph above must not be read as settling whether
    validator''s F2-key collision actually dedupes the BILLABLE cost event -- it only describes the F2
    anchor identity write, a SEPARATE content-hash-addressed audit_writer.append whose own dedup depends
    on AuditPayload byte-identity. (2) A third mechanism exists: ALL 4 production cost-wrapper call sites
    unconditionally append the returned SpanCostRecord to a run-scoped accumulator (cost_record_sink),
    which api.py''s _build_run_result rolls into RunResult.cost_attribution, bypassing both the F2 dedup
    and audit-ledger dedup. (3) Round-3 correction to both (1) and (2): test_journal_resume_completed_run_retry_does_not_double_emit_cost
    already drives attribute_llm_dispatch_cost twice under JOURNAL_RESUME and proves the audit-ledger
    layer DOES dedupe for LLM+JOURNAL_RESUME specifically -- ''unverified for any composer'' was too strong;
    the real gap is the OTHER 3 composers and OTHER engine classes. And the run-scoped accumulator is
    FRESH per public run()/resume() invocation (api.py:900, COST_ACCUM_VAR.set(CostRecordAccumulator()))
    -- a crash-then-separate-resume-call does NOT carry the prior invocation''s records forward, so the
    accumulator is NOT a confirmed cross-invocation dedup-failure layer; it only risks double-counting
    for repeated dispatches WITHIN one invocation or external aggregation across separate RunResults,
    neither established as a production concern. (4) Round-4 correction: calling the gap solely a missing
    ingestion consumer is itself incomplete -- verified all 4 attribute_*_dispatch_cost composers hard-code
    engine_replay_disposition=_DEFAULT_REPLAY_DISPOSITION (= ReplayDisposition.NO_REPLAY) with no parameter
    to carry the real disposition (cost_attribution_llm_dispatch.py:88,239; cost_attribution_tool_dispatch.py:90,295;
    cost_attribution_webhook_dispatch.py:65,204; cost_attribution_validator_dispatch.py:86,243). Even
    a built ingest_span consumer could never see CHECKPOINT_RESUME/RECONCILER_ITERATION/WAL_CONSUME from
    any composer today -- a producer-side wiring gap alongside the missing consumer. Close-out path once
    a real trigger exists: (1) resolve where ingest_span''s production call site actually lives (or should
    be built) before touching any composer; (2) verify whether double-billing is an ACTUAL exercised risk
    for the durable engine classes (SAVE_POINT_CHECKPOINT/EVENT_SOURCED_REPLAY/RECONCILER_LOOP/WAL_SEGMENT)
    whose resume_at skip logic may already prevent re-dispatch of completed steps, mirroring the JOURNAL_RESUME
    finding; (3) extend the existing JOURNAL_RESUME+LLM audit-ledger witness pattern to the other 3 composers,
    and separately verify whether a within-invocation retry (not cross-invocation resume) can double-append
    to the accumulator in production; (4) thread the real engine_replay_disposition into all 4 composers
    so a future ingestion consumer can actually discriminate replay dispositions. Only then decide whether
    composer-level wiring is even the right fix, and at which layer. Mirrors the B-19/B-42 discipline
    against building speculative infrastructure ahead of a real, demonstrated consumer. UPDATE 2026-07-25 (roadmap-continue, advisor()-checked before build): step (4) BUILT for 3 of 4 composers -- attribute_llm_dispatch_cost / attribute_tool_dispatch_cost / attribute_validator_dispatch_cost now take a run_engine_class: EngineClass | None param and resolve engine_replay_disposition via the existing closed-total REPLAY_DISPOSITION_MAPPING[EngineClass] (same table retry_breaker_fallback.py / retry_breaker_tool.py already use at a different surface), threaded from step_context.run_engine_class at each call site; None still falls back to NO_REPLAY. Build justified by re-running the B-19/B-42 defer-vs-build test: engine_replay_disposition still has zero production readers (re-confirmed), but StepExecutionContext.run_engine_class is non-vacuous (sourced from manifest_entry.engine_class; already drives production durable-auto-fence logic at runtime_tool_dispatcher.py / managed_agents_dispatch.py) -- so the hard-coded NO_REPLAY was a genuine persisted-audit-ledger correctness defect for a durable-engine-class workflow, not a vacuous no-op. attribute_webhook_dispatch_cost DELIBERATELY NOT threaded -- WebhookDeliveryComposer is a bootstrap-stage-5 PROCESS SINGLETON (not per-step), and webhook_delivery_composer_factory.py''s v1.26 empty-marker factory never binds rate_table/cost_chain/audit_writer, so the cost-attribution path is fully dormant in production at HEAD; threading a per-run engine class into a per-process singleton needs a real interface change (new per-call param, not a ctor field), deferred to the FM-2 webhook-config arc. Step (1) re-confirmed absent repo-wide (only the unrelated collector_daemon.py ingest_span_row exists, itself already documented as having no production caller) -- register-don''t-build stands. Step (2) verified NOT a live bug by direct read of workflow_driver.py:4619-4620 (the SINGLE_THREADED_LINEAR step-dispatch loop begins at resume_at, so the durably-committed step prefix is never re-dispatched on resume for any engine class). 15 new parametrized test cases (5 EngineClass members x 3 composers) plus a dedicated default-fallback test, default-preserved tests; pyright/ruff clean; full harness-runtime suite green (3064 passed); just codex-review converged clean. PR fix(B-30)-thread-real-engine_replay_disposition-into-cost-attribution-composers. UPDATE 2026-07-27 (roadmap-continue, advisor()-checked before disposition): step (3) VERIFIED, not a defect. Reachable: yes, for the TOOL composer only, via retry_breaker_tool.py:123-129''s two retried exception classes (ToolInvocationTimeoutError, MCPHostUnreachableError) each hitting runtime_tool_dispatcher.py''s failure-path attribute_tool_dispatch_cost call (~1177/~1203) before re-raise, then the eventual success-path call (~1276) appending again to the same run-scoped cost_record_sink; confirmed NOT deduped downstream (api.py''s _rollup_cost_attribution is an unconditional sum by axis, no dedup logic). NOT reachable for the LLM composer (attribution fires only post-success, llm_dispatch.py, with no retryable exception possible afterward). NOT applicable for the validator composer (its multiple on_post_evaluate firings are legitimately-separate billable evaluate() calls by design, per CP spec section 28.10.4 invariant 1). CONFIRMED DELIBERATE, not a bug: the tool composer''s failure-path attribution is symmetric across all 3 exception classes including ToolInvocationProtocolError (which is NOT even retried) -- the design axis is every physical MCP attempt, not every retried attempt -- matching this row''s own already-confirmed-deliberate webhook fresh-uuid4-per-attempt precedent, and matching U-OD-39 AC #1''s explicit every-tool-dispatch-success-and-failure requirement (the inline comment at runtime_tool_dispatcher.py:1174 cites this AC directly). No fix landed; suppressing failure-path attribution would contradict a cited AC and diverge tool from webhook without a spec change. One genuinely NEW residual this pass surfaced: U-OD-39''s own AC #1 (\"every tool dispatch, success + failure\") and AC #5 (\"1 tool call -> 1 cost-record\") use different granularity vocabulary and neither the OD spec nor plan disambiguates whether a flat_per_invocation rate should bill per physical MCP attempt or per logical dispatch when a retry succeeds -- an OD-spec-owned semantic question, not Phase-7 impl discretion; registered here, not answered unilaterally. STILL OPEN (this row stays registered_finding, not closed): the AC #1/AC #5 granularity-vocabulary gap above, and webhook''s per-call threading (deferred to FM-2 per above). 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT on every limb; cites re-verified by direct read EXCEPT the api.py RunResult accumulator/rollup path, which was NOT re-traced this pass (recorded, not assumed): step (4) threading is live for the three step-scoped composers (engine_replay_disposition=_resolve_replay_disposition(run_engine_class) at cost_attribution_llm_dispatch.py:267, cost_attribution_tool_dispatch.py:317, cost_attribution_validator_dispatch.py:263) while webhook remains hard-coded to _DEFAULT_REPLAY_DISPOSITION at cost_attribution_webhook_dispatch.py:206 — the deliberate FM-2 deferral, not drift; step (1) re-confirmed ABSENT repo-wide (no ADR-D6 section 1.5 ingest_span(span) consumer; only the unrelated, test-only collector_daemon ingest_span_row); and the U-OD-39 AC granularity residual is still unresolved in the OD plan. Disposition VERIFIED-DORMANT on the replay-dedup half; the AC-granularity residual is FORK-SHAPED and OD-spec-owned (design-substrate back-flow, not Phase-7 impl discretion). NOT re-traced this pass — the api.py RunResult accumulator/rollup lines this row cites (adjudicated across three prior rounds; no carrier file in that path changed since).'
  council: no — a specified policy already exists (ADR-D6 v1.2 §1.5); the open question is call-site +
    live-risk + per-composer/per-engine-class verification, not a design fork
  heading: '### B-30 · Cost-attribution disambiguator replay-semantics inconsistency *(surfaced at B-23
    round 6, advisor())*'
- id: B-31
  title: Paused-child resume guard does not validate child_workflow_id identity
  status: closed
  summary: Same-step_id/same-kind edit could swap the target child workflow past the resume guard undetected.
  close_out: Added child_workflow_id to PausedChildBranchResumeState (optional, byte-compat); resume guard
    now compares it against the re-supplied SUB_AGENT_DISPATCH payload via the existing _opaque_field
    convention. CP spec v1.98->v1.99 + clearance marker.
  council: no — mechanical carrier-completeness extension, no nameable cross-domain tension
  pr: '#1011'
  heading: '### B-31 · Paused-child resume guard does not validate `child_workflow_id` identity *(surfaced
    at B-21, out-of-family Codex round 2)*'
- id: B-32
  title: Nested paused-child pause-reason propagation (pause_reason half only)
  status: closed
  summary: Parent pause_reason ignored a nested child's OWN HITL_PENDING pause, always defaulting to EXPLICIT_OPERATOR.
  close_out: 'SCOPE NARROWED from the original coupled finding after out-of-family Codex review caught
    3 real bugs in a first attempt at the routing half (see B-39): (1) a naive ''any paused child -> HITL_PENDING''
    label mislabeled a nested EXPLICIT_OPERATOR pause -- fixed by checking child_snapshot.pause_reason
    directly at both PARALLELIZATION and ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION sites, any nested
    HITL_PENDING relabels, EFFECT_FENCE_AMBIGUOUS keeps top precedence. impl-to-cleared-spec (WorkflowPauseReason.HITL_PENDING''s
    existing ''HITL gate opened; workflow paused awaiting operator response'' semantic already covers
    a nested gate without extension) -- no CP spec delta needed. Mutation-probed both the true and false
    branches (contrasting-baseline + positive test); both confirmed load-bearing. A SECOND out-of-family
    Codex round then flagged the corrected label itself as a potential false-affordance (an operator seeing
    accurate HITL_PENDING might assume a single hitl_response safely resolves it even with >1 concurrently-paused
    child). Grounded via direct grep before deciding: pause_reason is read in exactly one non-telemetry
    place (this label-derivation code) and the composer''s actual resume path (consume_and_clear) never
    branches on pause_reason at all -- no consumer auto-dispatches a response off this label, so the label
    is informational-only and safe to ship as corrected. Added an explicit code comment at both sites
    documenting this + cross-referencing B-39 so the limitation cannot be silently assumed away. The response-ROUTING
    half (multiple concurrently-paused children each getting their own operator response) is NOT closed
    here -- see B-39. CORRECTION (merge-gate 3-lens review): the original close_out claimed ''mutation-probed
    both branches at both sites'' -- this was only true of the PARALLELIZATION site; the ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION
    site had ZERO test witness (test_workflow_driver_fanout_pause.py had no HITL_PENDING assertion at
    all). Fixed by adding a contrasting-baseline assertion to the existing test_cancellation_race_captures_paused_child_among_failing_siblings
    + a new positive test test_orchestrator_worker_child_hitl_pending_pause_labels_parent_hitl_pending,
    both mutation-probed (is -> is not flip at the second site''s check correctly fails both new tests;
    restored, both pass). Also, spec conformance review found design-substrate/Spec_Control_Plane_v1_97.md
    Section21''s registered-forward-work note still canonically asserted gaps (b)+(c) are ''coupled...
    travel together as one forward arc'' -- stale-carry-text now contradicted by shipping (c) alone; closed
    via a CP spec delta (v1.99->v1.100) correcting that framing, mirroring the B-31/v1.99 precedent for
    closing a Section21-registered gap.'
  council: no -- mechanical fix once the labeling logic was corrected; the routing half's council question
    is now moot per B-39's disposition
  pr: '#1015'
  heading: '### B-32 · Nested paused-child pause-reason propagation *(surfaced at B-21, out-of-family
    Codex round 3; scope narrowed after a same-session Codex review of the first attempt)*'
- id: B-39
  title: Nested paused-child HITL-response ROUTING (per-child operator response delivery)
  status: closed
  pr: '#1092, #1094, #1096, #1097'
  summary: 'CLOSED 2026-07-24 -- spec leg (PR #1092) + impl leg Slice A (PR #1094, the
    hitl_responses/hitl_response_for carrier) + impl leg Slice B (PR #1096, the HITLDeliveryCell
    per-branch delivery mechanism + resume_context_holder retirement) all landed. Per-child
    operator response delivery for concurrently-paused peer/nested children is now real,
    tested (CP suite 1753 passed, Runtime suite 3048 passed), and merge-gate-reviewed.'
  close_out: 'FINAL STATE (round 4, 2026-07-23 -- read this line, not the chronological
    history below, for the CURRENT design): ResumeContext.hitl_responses is keyed by
    the paused childs own run_id (child_snapshot.run_id), NEVER branch_path or workflow_id
    -- both were tried and found broken across earlier rounds. PausedChildBranchResumeState
    carries ZERO new fields (no branch_path field exists). CP plan units U-CP-86/88/89
    are UNAMENDED. The multi-branch fallback-safety requirement (do not misapply one
    branch''s response to another) is a black-box safety+liveness invariant deferred
    to the impl-leg resolver (CP spec v1.106 section 1.2 property 4), distinguishing
    gate-owning branches from transitively-paused container/ancestor branches (property
    5), stated as a deterministic outcome rule (round-7-sharpened: uniform fallback
    resolves a gate-owning branch only when it is the sole unaddressed member of the
    gate-owning set this cycle) -- NOT a prescribed counting/INERT mechanism (an earlier
    round tried an unscoped version of that, found it strands container branches and
    is unassignable to any unit). Spec leg LANDED at PR #1092 across 7 same-day
    correction rounds; impl leg NOT yet opened. THREE follow-ons registered separately:
    B-69 (resume_handle addressability), B-70 (the identical fallback gap in the shipped
    effect_fence_resolution_for sibling), and B-71 (operator-facing HITL escalation
    requests carry no run_id, so peer branches sharing a child_workflow_id produce
    indistinguishable requests). --- Chronological
    history below (superseded intermediate states retained for provenance; the FINAL
    STATE line above is authoritative) --- A first attempt (ResumeContext.hitl_responses keyed by workflow_id, consumed via non-destructive
    peek()) was built, mutation-probed, and reviewed out-of-family -- Codex found 3 real defects, all
    confirmed correct on inspection: (1) peek-only never one-shot-consumes, so a later HITL-composed step
    sharing the same key would silently reuse a stale response; (2) workflow_id is NOT branch-unique --
    fan-out siblings can dispatch the SAME child_workflow_id (the runtime already uses branch_path for
    exactly this disambiguation), so two paused siblings would collapse to one map entry; (3) [already
    split out as B-32, now closed]. The attempt was fully reverted (not shipped). Concrete design constraints
    for the next attempt: (a) the key must be branch-unique -- branch_path or an equivalent child-run-id
    derived value, NOT workflow_id alone; (b) delivery must be genuinely one-shot per key without racing
    concurrent siblings on the shared holder -- peek() alone is insufficient once one-shot semantics are
    required; (c) the actual delivery mechanism is unresolved -- execute_workflow/child_workflow_runner
    currently has NO resume_context parameter at all (only pause_snapshot_input), and effect_fence_resolution''s
    own per-branch mechanism resolves PARENT-side then threads a single resolved value onto the child''s
    StepExecutionContext -- that pattern does NOT carry into a recursive child''s OWN internal re-entry
    the way HITL delivery needs, so the threading path itself needs designing, not just the map shape.
    Re-grounded 2026-07-15 (B-37 session, no-parking sweep): all 3 constraints CONFIRMED still accurate
    against current HEAD by direct read (ResumeContext still singular hitl_response only; consume_and_clear
    still whole-holder; execute_workflow/child_workflow_runner still carry zero resume_context threading
    surface). Spec_Control_Plane_v1_100.md''s own change-note explicitly frames (c) as un-designed, not
    merely unimplemented -- status moved registered_finding -> design_substrate_gated to reflect this
    precisely (a Class 1 fork proposing the threading mechanism is the correct next step, before any code,
    per X-AL-3). Fork FILED 2026-07-23 at `.harness/class_1_fork_b39_nested_hitl_response_threading.md`: grounds the crux precisely (`ctx.resume_context_holder` is one mutable sidecar threaded by reference to every recursion level; `api.resume()`''s own public signature is singular too) and proposes threading `resume_context` BY VALUE through the existing `pause_snapshot_input` recursive plumbing (mirrors the already-correct per-child mechanism) rather than a shared keyed map -- this closes both prior defects (1)+(2) structurally, not by patching. Two genuine operator-gated decisions remain open at the fork''s Q1/Q2 (retire `ResumeContextHolder` for a single top-to-bottom mechanism vs keep it at depth-0; and how `api.resume()` accepts multiple per-child responses in one call, if at all, this arc). Status stays `design_substrate_gated` pending operator ratification. **RATIFIED 2026-07-23 (operator AskUserQuestion): Q1=(A) retire `ResumeContextHolder`; Q2=(A) batched map at the API boundary — the heavier reading on both, since Q2 is the genuine public-API expansion. Opens a spec leg (CP + Runtime spec deltas, clearance markers, plan units) as its own arc next, per the B-33/B-59 spec-leg/impl-leg precedent; not yet built.** ✅ **SPEC LEG LANDED 2026-07-23** — `Spec_Control_Plane_v1_106.md` + `Spec_Harness_Runtime_v1.md` v1.106 + `Implementation_Plan_Control_Plane_v2_42.md` (U-CP-64/86/88/89 amended) + `Implementation_Plan_Harness_Runtime_v2_54.md` (U-RT-94 amended) + both clearance markers. **Q2 REVISED at spec-grounding time via a second operator AskUserQuestion**: empirical grounding found the shipped `ResumeContext.effect_fence_resolutions: dict[str, EffectFenceResolution]` + `effect_fence_resolution_for(key)` sibling mechanism already solves the identical multi-branch-distinct-resolution problem with NO `api.resume()` signature change; the operator selected the keyed-field-inside-ResumeContext reading (`ResumeContext.hitl_responses: dict[str, HITLResult]` + `hitl_response_for(branch_path)`, a verbatim mirror) over the originally-ratified `Mapping[str, ResumeContext]` API-boundary widening. `api.resume()` stays singular. Status flipped `design_substrate_gated` -> `open` (spec-leg gate cleared; impl leg is the next buildable slice, NOT yet opened). ***CONTRACT-ALTITUDE CORRECTION PASS, same day.*** A first draft of the delivery-mechanism sections prescribed exact wiring (execute_workflow gaining resume_context/resolved_hitl_response parameters; attempt_resume as the depth-0 entry point; direct intra-CP recursion for nested children). just codex-review-uncommitted + an Explore grounding pass found 4 P1 defects: 3 wiring claims empirically false against the real call graph (the real depth-0 path is harness_runtime/api.py''s resume() + lifecycle/mcp_server.py; nested-child re-dispatch crosses into harness-runtime''s RuntimeSubAgentDispatcher/child_workflow_runner.py, not a CP-internal recursive call) plus a retry-replay gap (a bare parameter would replay across RetryBreakerFallbackDispatcher retries within one resume cycle, unlike the retired holder''s consume_and_clear()). advisor() recommended pulling the mechanism sections back to CONTRACT altitude (state required properties -- per-branch-distinct delivery, one-shot preserved under retry, no new global sharing -- leave wiring to the impl leg) rather than a 4th unverified mechanism attempt; all 4 spec/plan artifacts + all 4 clearance markers were revised accordingly. A genuinely new spec-level fix also landed: PausedChildBranchResumeState.branch_path (mirroring EffectFencePausedBranchResumeState.idempotency_key''s working precedent), closing the gap where hitl_responses'' keys were otherwise unaddressable by an external caller. CP plan v2.42''s U-CP-86/88/89 amendments were removed (asserted a false call graph); the propagation-mechanism wiring plus a scope-discovery pass across both axes is owed to the impl leg, which is the next buildable slice, NOT yet opened -- code + tests, per the B-33/B-59 precedent. ***SECOND CONTRACT-ALTITUDE CORRECTION PASS, same day (round 2).*** A second just codex-review-uncommitted pass on the round-1-corrected text found the hitl_responses key shape (branch_path) is ALSO wrong -- it collides when two peer branches dispatch the SAME child_workflow_id, since branch_path derives from a workflow_id-scoped action_id with no run-instance component. Grep-verified via compose_child_run_id_seed''s run_idempotency_key = sha256(run_id, workflow_id, ...) folding: run_id, not branch_path/action_id, genuinely propagates run-instance distinctness through arbitrary recursion depth. Fix: hitl_responses re-keyed to the paused child''s own run_id (PausedChildBranchResumeState.child_snapshot.run_id, an EXISTING PauseSnapshot field -- no new carrier field needed); the round-1-added PausedChildBranchResumeState.branch_path field was REMOVED as unnecessary. All 4 spec/plan artifacts + all 4 clearance markers revised again. *** IMPL LEG SLICE A LANDED 2026-07-23 (PR #1094) *** -- ResumeContext.hitl_responses + hitl_response_for(child_run_id) landed byte-exact to CP spec v1.106 section 0 (U-CP-64 AC #7), 34 tests, 3 codex rounds, merge-gate 2 rounds (round 1 test-witness BLOCK on a disconnected mutation-probe demo, fixed) to all-approve. Slice B (the delivery mechanism + resume_context_holder retirement) deliberately NOT started this leg -- advisor-recommended split; grounding confirmed the depth-0 entry point + nested-child re-dispatch path but the actual binding-site mechanism needed its own execution-grounded design pass. *** IMPL LEG SLICE B LANDED 2026-07-24 (PR #1096 + terminating refresh PR #1097) *** -- HITLDeliveryCell (per-branch one-shot cell, replaces the ctx-level ResumeContextHolder) + compute_hitl_uniform_fallback_eligible_run_id (properties 4/5) + _collect_gate_owning_run_ids tree-walk (correctly distinguishing B-32-relabeled containers from true gate-owning leaves) landed and threaded through the CP driver + Runtime (mcp_server.py depth-0 entry point, child_workflow_runner.py nested-child forwarding, sub_agent_dispatch.py). 7 just codex-review rounds to convergence (rounds 2-5 each found and fixed a genuine defect: multi-child fallback safety, B-32 container mis-detection, cross-branch map misuse, depth-0 root eligibility exclusion -- all mutation-probe verified); merge-gate 3-lens all-approve (test-witness surfaced 2 real gaps -- a forwarded field never asserted, a mutation-probe claim not actually isolated to its stated gate -- both fixed + verified before merge). Rounds 4/6/7 all re-raised the SAME finding (an ancestor SUB_AGENT_BOUNDARY gate re-firing on resume-forward, discarding a deeper-paused child''s cursor) -- grounded as reachable-but-pre-existing/orthogonal (the old global holder masked it via accidental cross-branch misapplication, not correct behavior), registered separately as B-72 rather than patched blind, per advisor()''s recommendation and independently endorsed by the merge-gate''s own spec-conformance reviewer. CP suite 1753 passed/4 skipped, Runtime suite 3048 passed/40 skipped, ruff+pyright clean. B-39 CLOSED -- both spec leg and impl leg (Slice A + Slice B) fully landed.'
  council: not re-evaluated at close -- the original C7/C8-vs-C1/C5 framing was superseded
    by execution-grounded design per this register's own no-parking discipline; no residual
    open design question remains
  heading: '### B-39 · Nested paused-child HITL-response ROUTING (per-child operator response delivery)
    *(split from B-32 after a same-session Codex review found the first routing attempt had 3 real defects;
    not shipped — CLOSED 2026-07-24, spec leg PR #1092 + impl leg Slice A PR #1094 + Slice B PR
    #1096/#1097)*'
- id: B-33
  filing: .harness/class_1_fork_b33_rotation_correlation_carrier.md
  title: verify_rotation_6_steps cannot prove a rotation boundary occurred
  status: closed
  pr: '#1082, #1083, #1084, #1087'
  summary: 'CLOSED 2026-07-23 -- all three ratified-fork (Option A) legs landed:
    (i) IS carrier -- IS spec v1.11->v1.12 (NEW §5.6 `rotation_correlation_id`
    sidecar, C-IS-05; NEW §7.7 join-key invariants, C-IS-07) + IS plan v2.8
    (U-IS-20), code at PR #1083. (ii) CP+OD+Runtime spec+plan rider -- CP spec
    v1.104->v1.105 (§20.3.1 row 7 + NEW §20.3.2), OD spec v1.34->v1.35 (NEW §24.8),
    Runtime spec v1.104->v1.105 (NEW §13.6), landed at PR #1084 (4 codex rounds,
    24 findings fixed). (iii) the real CODE -- OD `find_rotation_pair_evidence`
    + CP `RotationPairEvidenceProvider`/`KeyIdentityResolver` Protocols +
    `verify_rotation_6_steps` extension (rotation_window_entries/evidence_provider/
    key_identity_resolver, subset-membership check, dual pair_present+
    signatures_verified gate, required physical-key-distinctness attestation) +
    Runtime composition-root adapter, landed at PR #1087 (2 codex rounds to clean
    convergence + 3-lens merge-gate all-approve). `verify_rotation_6_steps` now
    genuinely cannot pass on absent/tampered/structural-only evidence -- the
    B-26 defect this fork exists to fix is closed.'
  close_out: 'Two residuals REGISTERED as separate forward dependencies, NOT part
    of B-33''s own ratified scope (the fork''s own §3 explicitly defers both):
    (a) `execute_key_rotation`''s real ledger-write path + a real
    `rotation_window_entries` producer -- `verify_rotation_6_steps` still has
    ZERO production callers; (b) the OD §21.2.2 rotation-period-aware
    cryptographic signature verifier -- `find_rotation_pair_evidence` always
    returns `signatures_verified=False` today, so `PROBE_VERIFY_AT_READ` cannot
    reach a genuine pass through any real caller until this lands. Neither
    blocks B-33''s own closure; both are candidate new B-* filings for a future
    arc when a real rotation-execution production surface is prioritized.'
  council: no -- the carrier shape is settled (re-raised a 2nd time at B-33-A spec-leg
    codex round 4 with no new consequence named; converged, artifact identical
    either way); B-36/B-47 are both closed, so no signing-backend disposition remains
    open
  heading: '### B-33 · `verify_rotation_6_steps`''s hash-chain check cannot prove a rotation boundary
    occurred *(surfaced at B-26, out-of-family Codex rounds 1 + 5, `advisor()`-confirmed; CLOSED 2026-07-23)*'
- id: B-34
  title: Concrete signature-representation gaps in the C-CP-20 §20.2.1 seam
  status: closed
  pr: '#1032'
  summary: DER-vs-64-byte ECDSA-P256 encoding unenforced; CPSignedAuditLedgerEntry.audit_signature_value
    not JSON-serializable for real bytes. NARROWED 2026-07-16 — B-36 (ADR-D8) selected AWS KMS Ed25519
    (ED25519_SHA_512), whose signatures are always a fixed 64 raw bytes with no DER-wrapping ambiguity;
    the DER-encoding concern only remains live if a future ECDSA-P256 fallback backend is added alongside
    this one. The JSON-serializability gap is unaffected by backend choice and still open. CLOSED 2026-07-16
    at PR 1032 — both gaps fixed per this row's own close-out steps -- per-algorithm byte-length
    enforcement on write+read paths (ecdsa-p256 pinned to fixed-width raw r||s, foreclosing the
    DER ambiguity by contract) + reversible base64 serializer/validator on audit_signature_value
    (consumer sweep found zero serialization consumers outside harness-cp).
  close_out: Validate signature byte-length per algorithm + add a reversible byte-encoding serializer,
    once a real backend exists (B-36).
  council: no — mechanical once a real backend exists to test against
  heading: '### B-34 · Concrete signature-representation gaps in the C-CP-20 §20.2.1 seam *(surfaced at
    B-22 round 4, out-of-family Codex, `advisor()`-confirmed non-blocking)*'
- id: B-35
  title: test_qa_evidence_matrix.py / test_closure_gate.py orphaned from CI + 12 uncited contracts
  status: closed
  summary: 12 contracts (C-IS-14 + 11 C-MEM-*) cite no test file; neither test module is wired into any
    CI job.
  close_out: 'Grounded each of the 12: C-IS-14 was a phantom/dead contract ID (runtime spec v1.93 change-note
    confirms the IS spec defines only C-IS-01..10 and ''prior v1 cited C-IS-11/14/15 which don''t exist'')
    -- moved closure_gate.py''s local, incomplete DOCUMENTED_NON_CONTRACTS={C-IS-11} constant to a shared
    tools/semantic_overlay/overlay.py::DOCUMENTED_NON_CONTRACTS={C-IS-11,C-IS-14,C-IS-15} (single source
    of truth for both overlay.py''s own contract_without_code orphan scan and qa_evidence_matrix.py''s
    test-citation scan) and excluded it there. 10 of the 11 C-MEM-* contracts (04,05,06,11,12,13,14,15,17,19)
    already had a real, already-passing test exercising their behavior -- just needed a one-line contract-id
    citation added (several already had the id embedded in the test function name in snake_case, e.g.
    test_..._c_mem_17_..., just never in the hyphenated form the citation-scan regex needs). C-MEM-01
    (the memory-plane-boundary architectural contract, no dedicated source module by design) was a genuine
    gap: added harness-runtime/tests/test_memory_plane_boundary.py with two new tests -- one proving native/standard-tools/prompt-packet
    all write to the SAME physical canonical-store ledger (mutation-probed: pointing one component at
    a different root correctly fails it), one proving promotion and injection are independently-settable
    policy decisions (mutation-probed against a real production-code coupling mutation). Also cited C-MEM-01
    in CanonicalMemoryStore''s own docstring, closing closure_gate.py''s G1.3 orphan too (0 real, down
    from 2 -- G1.3 now fully PASSES, all 5 automatable Tier-1 predicates green). Wired both test modules
    + qa_evidence_matrix.py --check + closure_gate.py --check into a new CI job (q3-evidence-and-closure-gate),
    mirroring the existing arc-ledger/substitution-ledger/semantic-overlay job pattern. tools/test_qa_evidence_matrix.py''s
    import style was also normalized to match its siblings'' self-contained sys.path convention (its prior
    `from tools import X` package-relative import only worked from repo root, inconsistent with the working-directory:
    tools convention every other tools/test_*.py file already uses).'
  council: no — mechanical once each contract was grounded; no cross-domain tension
  pr: '#1017'
  heading: '### B-35 · `tools/test_qa_evidence_matrix.py` / `test_closure_gate.py` are orphaned from CI
    + 12 contracts lack test-file citation *(surfaced during B-28 triage, 2026-07-14; closed same session
    as B-32)*'
- id: B-36
  title: Concrete F5 audit-signature backend selection
  status: closed
  summary: 'CLOSED 2026-07-16: AwsKmsSigningBackend (ADR-D8) implements the C-CP-20 SigningBackend
    Protocol, live-e2e-proven against a real provisioned KMS key. Composition-root wiring into a real
    production sign_audit_entry call site is a separate follow-on -- see B-47.'
  close_out: 'CLOSED 2026-07-16 via ADR-D8 (AWS KMS delegated signing, ECC_NIST_EDWARDS25519/Ed25519 —
    matches the ADR-D5 §1.4 default algorithm; AWS KMS''s Nov-2025 Ed25519 GA dissolved the algorithm-vs-exposure
    tradeoff). Resolved via a dyadic C10 (blast-radius) vs C11 (operator-loop) council: the already-landed
    `resolve_signing_key` TIER_4_FULL_VM/in-sandbox-HTTP tier choice + MULTI_TENANT_COMPLIANCE-exclusive
    gate already signaled a no-exposure design intent, so KMS-delegated signing (key never leaves the
    HSM boundary) was ranked over Secrets-Manager-plus-local-signing. New `harness_cp.aws_kms_signing_backend.AwsKmsSigningBackend`
    implements the C-CP-20 §20.2.1 SigningBackend Protocol via boto3 KMS Sign/Verify; zero change to
    `resolve_signing_key`/`sign_audit_entry`/`verify_audit_entry_signature` (the seam already accommodated
    this shape). Live-e2e-proven against a real provisioned AWS KMS key + a least-privilege IAM identity
    scoped to Sign/Verify/GetPublicKey/DescribeKey on that one key only (confirmed AccessDenied against
    S3 and iam:CreateUser under the same identity). Composition-root wiring of this backend into a real
    production `sign_audit_entry` call site (with the C9 breaker extended onto the signing call itself,
    per C11''s availability-cost concern) remains a separate, deployment-time arc — zero production callers
    of `sign_audit_entry` exist today. B-33/B-34 narrowed accordingly (see their own rows).'
  council: 'DONE — C10 blast-radius vs C11 operator-loop/deployment-surface, dyadic council 2026-07-16;
    ranked AWS KMS delegated signing over Secrets-Manager-plus-local-signing'
  pr: '#1031'
  heading: '### B-36 · Concrete F5 audit-signature backend selection *(split off `B-22` 2026-07-14, per
    operator direction — `B-22`''s seam is CLOSED, this is what remains; CLOSED 2026-07-16 via ADR-D8)*'
- id: B-37
  title: Remote MCP streamable-HTTP transport residuals
  status: closed
  summary: Current (non-deprecated) streamable_http_client called with the old function's kwarg shape
    (latent TypeError once populated) + MCPClientConfig has no auth-credential field.
  close_out: 'CLOSED 2026-07-15. mcp_client_host.py''s _http_connection_context now builds an httpx.AsyncClient
    from transport_config[''headers''] and passes it via http_client= (streamablehttp_client left un-migrated
    per the register''s own instruction); 3 stale docstrings naming the deprecated function fixed. MCPClientConfig
    gained auth_secret_name: str | None, resolved at stage-3a via the SAME ProviderSecretResolver.resolve_bootstrap_value(name)
    idiom U-RT-17/18 already use for LLM provider API keys (mcp_client_host_factory.py + stage_3a_cp_clients.py
    thread ctx.keyring_resolver through) rather than inventing a new credential shape; auth_present now
    wired end-to-end instead of hardcoded False. Mutation-probed both the http_client kwarg-shape fix
    and the fail-loud resolver guard against real production-code reversions.'
  council: no — mechanical once a real remote-MCP credential-supply build opens; not urgent
  pr: '#1019'
  heading: '### B-37 · Remote MCP streamable-HTTP transport residuals *(surfaced at `B-10`''s own `B-MCP-OAUTH-RS-ENFORCE`
    grounding, split out 2026-07-14 at forward-register-schema grounding; corrected 2026-07-14 round-3
    out-of-family Codex — the original finding had the deprecated/current function names backwards)*'
- id: B-38
  title: Fine-grained provider-exception classifier for breaker.cause
  status: closed
  summary: breaker.cause stays vacuous (always None) until a real per-step rate_limit/auth_failure/5xx_streak
    classifier exists.
  close_out: 'CLOSED 2026-07-15. Added _classify_breaker_cause(exc) -> BreakerCause | None (retry_breaker_fallback.py)
    — duck-typed on .status_code (429->RATE_LIMIT, 401/403->AUTH_FAILURE, 500<=code<600->FIVE_XX_STREAK,
    else None), computed once per exception and wired into all 3 breaker.record_failure() call sites.
    Out-of-family Codex round 2 found the value never reached a real OTel span: emit_breaker_trip_span_event
    (harness_breaker_schema.py) built an EventEmission bookkeeping record but never called parent_span_ref.add_event(...)
    — confirmed a genuine local bug (not a systemic OTel-absence) by comparison against the sibling emit_drift_event,
    which does call add_event; fixed to match that established pattern, so harness.breaker.cause (and
    the other 8 §7.1 attributes) now actually lands on the real span. Mutation-probed against real production-code
    reversions (record_failure(cause=breaker_cause) -> record_failure(); the 500<=code<600 bound reverted
    to >=500) confirming the e2e/table tests genuinely fail. Split out B-41 (retry-routing classifier
    still misroutes AuthenticationError to TRANSIENT_RETRY, a known tracked divergence) and B-42 (FIVE_XX_STREAK
    tags by the terminal exception only, not a verified N-consecutive streak — an acknowledged MVP simplification
    per this row''s own grounding, mirroring _classify_provider_exception''s existing MVP discrimination)
    rather than folding either into this telemetry-only change.'
  council: no — mechanical extension once opened; not urgent, speculative ahead of any consumer
  pr: '#1020'
  heading: '### B-38 · Fine-grained provider-exception classifier for `breaker.cause` *(surfaced at `B-19`''s
    own close, split out 2026-07-14 at forward-register-schema grounding, out-of-family Codex)*'
- id: B-40
  title: Memory operation ledger has no cross-process write serialization
  status: closed
  summary: append_memory_operation and append_ledger_entry each used a process-local threading.Lock only
    -- no protection against a second OS process (e.g. two concurrent `harness run` invocations against
    the same repo) writing the same canonical ledger. C-IS-09 §9.3 + C-MEM-08 already require serialization;
    §9.4 defers only the mechanism, not the requirement -- no fork needed.
  close_out: 'CLOSED 2026-07-15 at PR #1026, after a 4-round register-correction arc (PR #1025) that repeatedly
    mis-disposed this finding before landing on the correct one: round 1 wrongly called it speculative/already-decided;
    round 2 wrongly reclassified it design_substrate_gated as an open interpretive fork; round 3 correctly
    reclassified registered_finding/buildable-now but still called it dormant; round 4 (out-of-family
    Codex) corrected ''dormant'' to LIVE (RuntimeConfig.memory defaults enabled=True with root_path=<repo>/.harness/memory,
    so two ordinary concurrent `harness run` invocations already share the canonical ledger today). Build
    (PR #1026): added harness_is.cross_process_ledger_lock -- a same-host fcntl.flock helper mirroring
    the proven pattern already shipped at harness_runtime.lifecycle.reconciler_pause_resume_substrate._workflow_lock
    (POSIX advisory, same-host only; true cross-host distributed writers remain out of design horizon
    per ADR-F2 §Rationale(c)(ii), unaffected). Wired into all 4 real call sites found during grounding
    (2 more than the register originally named): append_ledger_entry, append_memory_operation, shadow_git_rollback''s
    ledger-preserve-across-checkout window, and jsonl_event_ledger_lifecycle.initialize_jsonl_event_ledger/validate_jsonl_event_ledger_format
    (the last found by the merge-gate concurrency lens -- reachable on every harness run bootstrap/resume
    via harness_runtime.lifecycle.state_ledger.materialize_state_ledger, so its pre-existing unlocked
    read could undercount entry_count and silently skip the tamper-check gate). read_ledger/read_memory_operation_ledger
    take a shared lock; internal call sites use new _unlocked variants to avoid self-deadlock (flock is
    per-open-file-description, not reentrant). Added genuine multi-process test coverage (multiprocessing,
    fork context) for all 3 append-side call sites, replacing the prior threading-only witnesses that
    could not detect a cross-process race; each new test mutation-probed (reverted the fix, confirmed
    failure across 3 runs, restored) -- the shadow_git_rollback test specifically caught by the merge-gate
    test-witness lens, which found the first 2 tests covered only 2 of 3 wired sites. Full harness-is
    suite green (281+ passed); ruff/pyright/overlay-check clean; 3-lens merge-gate (which caught the 4th
    call site + the missing shadow_git_rollback test above) and out-of-family Codex both run to convergence
    before merge. Out-of-family Codex round 1 on PR #1026 caught two further issues, both fixed before
    merge: (1) the unconditional top-level `import fcntl` broke `import harness_is` on Windows (eagerly
    imported via `memory_operation_ledger`) — C-STK-10 commits to macOS/Linux/Windows host-OS support
    "without per-OS port forking"; fixed by gating the import behind `sys.platform == "win32"` and degrading
    the lock context managers to a no-op on Windows (exact pre-B-40 parity — the in-process `threading.Lock`
    half is unaffected and cross-platform; only the new cross-process dimension is unavailable there).
    (2) the new `.lock` sidecar files were not gitignored; added `.harness/**/*.lock`, verified via `git
    check-ignore` against both actual lock paths. The genuine Windows locking *mechanism* remains unbuilt
    by design (advisor()-reviewed scope call, not an oversight) — registered separately as B-45. A further
    out-of-family Codex round (round 4, against the full PR diff vs main -- an earlier round had been
    invoked with an invalid ref and produced no usable verdict) found two more real, non-regression gaps,
    both registered rather than fixed in this PR: (1) `cross_process_read_lock`''s no-sidecar-yet fallback
    (added to fix the P1 read-only-CLI violation above) reintroduces a narrow TOCTOU -- a reader can pass
    the "sidecar doesn''t exist" check before a racing first-writer creates it, then read unguarded while
    that writer is mid-append; (2) the identical gap in `jsonl_event_ledger_lifecycle.py`''s initialization
    probe. Both are pre-existing (every read was unguarded, always, before this module existed) and narrow
    (once any writer has ever created the sidecar, all subsequent reads/writes correctly serialize) --
    not a regression, and advisor()-confirmed as correctly out of THIS PR''s scope: the durable fix (locking
    the canonical ledger file directly instead of a lazily-created sidecar) is a larger, harness-inspect-compatibility-affecting
    redesign, registered separately as B-46. The torn-read-race claim in `cross_process_read_lock`''s
    docstring was softened to state this precisely (closes the race once a ledger has been written at
    least once under the lock, not before).'
  council: no -- requirement settled (C-IS-09 §9.3 + C-MEM-08), mechanism was implementation discretion
    (§9.4); impl-to-cleared-spec
  pr: '#1026'
  heading: '### B-40 · Memory operation ledger has no cross-process write serialization *(surfaced at
    B-35''s merge-gate concurrency lens, 2026-07-15; CLOSED at PR #1026 after a 4-round register-correction
    arc at PR #1025)*'
- id: B-41
  title: _classify_provider_exception misroutes AuthenticationError to TRANSIENT_RETRY
  status: closed
  summary: The retry classifier treats an SDK AuthenticationError (401/403) as TRANSIENT_RETRY, running
    the full retry staircase before ever escalating — the new B-38 breaker.cause telemetry classifier
    correctly tags the SAME exception AUTH_FAILURE, so the two classifiers now visibly disagree on one
    real exception shape.
  close_out: 'CLOSED 2026-07-15. Verified against Spec_Harness_Runtime_v1.md v1.99 (canonical HEAD) directly:
    §14.5''s C-RT-15 fail-class table states RT-FAIL-PROVIDER-AUTH (raw SDK auth error, 401/403, not a
    harness wrapper type) is "Raise unmodified"; §14.6 D2 + its own narrative both say AUTH is fail-fast.
    The code contradicted its own docstring (which already claimed this). A straightforward conformance
    fix, not a design decision -- no fork needed. Added a status_code in (401, 403) duck-typed guard to
    _classify_provider_exception, placed after the wrapper-type check and before the TRANSIENT_RETRY catch-all
    -- deliberately NOT reusing _classify_breaker_cause so the two classifiers'' control-flow contracts
    stay independent. Closes B-38''s noted divergence as a side effect (both classifiers now agree on
    AUTH). Added a fail-fast witness test (asserts the SECOND call lands on candidate 1, not a retried
    candidate 0 -- a naive len(calls)==2 check does not discriminate fail-fast from retry-then-succeed)
    and a regression-guard test proving 429/5xx still retry (guards against an overly-broad status_code
    is not None mutation). Both mutation-probed against real production-code reversions.'
  council: no — mechanical conformance fix, spec was unambiguous
  pr: '#1021'
  heading: '### B-41 · `_classify_provider_exception` misroutes AuthenticationError to TRANSIENT_RETRY
    *(surfaced at B-38 grounding, 2026-07-15, advisor()-flagged)*'
- id: B-42
  title: breaker.cause's FIVE_XX_STREAK is tagged by the terminal exception only, not a verified streak
  status: closed
  summary: _classify_breaker_cause tags FIVE_XX_STREAK from a single last-attempt 5xx status code; BreakerStateMachine
    tracks only an aggregate fail_count, not a per-cause history, so a trip after a mixed rate-limit/auth/5xx
    failure sequence still reads FIVE_XX_STREAK from the terminal exception alone, which can misrepresent
    what actually happened across the attempt sequence.
  close_out: 'CLOSED 2026-07-15. Grounded directly against OD spec Spec_Operational_Discipline_v1_32.md
    §7.1 (only requires "the classified trip-cause, when known" -- no streak-verification mandate) plus
    BreakerStateMachine''s own source (only an aggregate fail_count, no per-cause history) and confirmed
    no live consumer reads harness.breaker.cause today -- chose disposition (b) from this row''s own close-out
    options: a documentation-only clarification, not new per-cause tracking infrastructure (which would
    be speculative ahead of any consumer, the same discipline the B-19 redundancy analysis already established).
    Added a clarifying paragraph to BreakerCause''s docstring in harness_breaker_schema.py stating FIVE_XX_STREAK
    is tagged from a single terminal 5xx exception, not a verified N-consecutive streak, mirroring the
    equivalent clarification _classify_breaker_cause''s own docstring in retry_breaker_fallback.py already
    carried since B-38''s original landing. The spec-committed enum value (5xx_streak) is UNCHANGED --
    only the docstring''s description of its derivation is now precise. No behavior/test change; ruff
    clean; OD breaker test suite (30 tests) still green. Adjacent finding (OD spec v1.32 §7.1''s own prose
    still describes breaker.cause as "vacuous-today ... absent from every emitted event," which is now
    stale since B-38 populated it for 3 of 4 values) is OUT OF SCOPE for this row -- registered separately
    as B-43 (design_substrate_gated) since fixing spec prose is a design-substrate edit requiring design-phase
    posture, not a Phase-7 code fix.'
  council: no — mechanical once grounded; spec text was unambiguous on the non-mandate
  pr: '#1022'
  heading: '### B-42 · `breaker.cause`''s `FIVE_XX_STREAK` is tagged by the terminal exception only, not
    a verified streak *(surfaced at B-38''s own PR, out-of-family Codex round 1, 2026-07-15)*'
- id: B-43
  title: OD spec v1.32 §7.1 prose still describes harness.breaker.cause as vacuous-today, stale since
    B-38
  status: closed
  pr: '#1050 (in-place correction + clearance marker)'
  summary: 'Spec_Operational_Discipline_v1_32.md §7.1''s harness.breaker.cause row states: "Vacuous-today,
    honestly documented ... the attribute is present in the schema (forward-compatible typed slot) and
    absent from every emitted event until a follow-on classifier arc supplies real signal." B-38 (PR #1020)
    landed exactly that follow-on classifier -- _classify_breaker_cause now populates RATE_LIMIT/AUTH_FAILURE/FIVE_XX_STREAK
    at all 3 real record_failure() call sites and the attribute reaches the real span. The spec prose
    was never updated to reflect this and is now factually wrong about current runtime behavior for 3
    of the 4 enum values (CAPABILITY_SHORTFALL remains genuinely vacuous, per B-38''s own grounding).'
  close_out: 'CLOSED 2026-07-18: in-place correction of the v1.32 §7.1 cause row + change-note
    historical annotation + harness-od/CLAUDE.md pointer mirror, with clearance marker
    .harness/clearance/spec-operational-discipline-v1-32-b43-correction-cleared-2026-07-18.md
    (ADR-D7 in-place-correction precedent; no contract change, so no new OD delta -- avoids
    colliding with the pending B-51/B-52/B-54 v1.34 numbering).'
  council: no — prose-accuracy delta over a cleared spec, not a contract change; needs design-phase posture,
    not council
  heading: '### B-43 · OD spec v1.32 §7.1 prose still describes `harness.breaker.cause` as vacuous-today,
    stale since B-38 *(surfaced at B-42 grounding, 2026-07-15, advisor()-flagged)*'
- id: B-44
  title: No production bootstrap site injects a non-default time_source clock
  status: registered_finding
  summary: RuntimeAuditLedgerWriter + RuntimeSubAgentDispatcher support time_source injection, and the
    shared compose_cost_f2_entry_core helper accepts one too, but none of the 4 attribute_*_dispatch_cost
    composer functions (tool/llm/webhook/validator) expose a time_source parameter of their own -- they
    call the helper without one, so it always defaults to datetime.now(UTC). No production bootstrap site
    (stage_4_od.py:185, stage_5_loop_init.py:649, migrate_audit_sidecar.py:151) injects a non-default clock
    either -- the flagged NonMonotonicTimestampError failure mode has no live trigger today. Re-verified
    2026-07-27 at HEAD 25b44127, MORE dormant than registered -- RuntimeSubAgentDispatcher.time_source is now
    vestigial (required ctor kwarg at sub_agent_dispatch.py:607, never called -- the 8b F2 entry stamps
    WRITER_OWNED_TIMESTAMP at :801), and a third construction site (migrate_audit_sidecar.py:139-152) also
    takes the default clock.
  close_out: OPERATIVE close-out (the originally-registered plan, RESTORED at out-of-family Codex round 3,
    2026-07-27) -- if a real deterministic-clock consumer ever surfaces (e.g. a multi-writer clock-skew
    test), thread an optional time_source parameter through the 4 attribute_*_dispatch_cost composer
    signatures (tool/llm/webhook/validator, which currently have none) down to the already-time_source-accepting
    compose_cost_f2_entry_core helper, plus the bootstrap sites. This is the only mechanism that satisfies
    the deterministic/injectable-clock use case, and it stays within IS spec C-IS-07 §7.6's preserved
    caller-supplied timestamp semantics for DIRECT surfaces -- no spec change needed. CONSIDERED AND
    REJECTED -- WRITER_OWNED_TIMESTAMP sentinel adoption at the composer F2 write was briefly recorded
    by the 2026-07-27 grounding pass as the likely fix, and corrected the same day at Codex round 3, on
    two spec-verified grounds. (a) IS spec C-IS-07 §7.6's "Surfaces that do NOT change" paragraph classifies
    runtime cost writes as DIRECT appends keeping caller-supplied timestamp semantics verbatim, and its
    "Registered residual" paragraph requires a separate spec back-flow before writer-owned authority may
    extend to any direct surface -- an extension explicitly assigned to B-57 (covers the remaining direct
    writers), NOT to B-44; adopting the sentinel here at impl time would be a silent spec violation.
    (b) The sentinel cannot serve B-44's own stated trigger at all -- state_ledger_write.py lines 316-320
    resolve WRITER_OWNED_TIMESTAMP through a hardcoded datetime.now(UTC) inside the write lock, so a
    deterministic/injectable-clock consumer gets no determinism from it; only an injectable time_source
    can. The sub_agent_dispatch.py:801 and workflow_driver.py:2028-2030 cites describe only what B-48
    did on the DRAIN/offloaded surfaces -- they are not a precedent for the DIRECT composer F2 write.
    Committed posture unchanged -- detect-then-refuse, register-don't-build, per the workspace's established
    B-19/B-42 discipline against building speculative infrastructure ahead of a real consumer.
    2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT and every cite the 2026-07-27
    pass refreshed still resolves exactly, by direct read — stage_5_loop_init.py:649 and
    migrate_audit_sidecar.py:151 both still inject a lambda returning datetime.now(UTC);
    stage_4_od.py contains ZERO time_source occurrences (falling through to the audit_writer
    default); all four attribute_*_dispatch_cost composer modules still expose ZERO time_source
    parameters; and RuntimeSubAgentDispatcher.time_source stays vestigial (required ctor kwarg at
    sub_agent_dispatch.py:607, retained-for-API docstring at :587-592, sole remaining
    self.time_source occurrence inside a comment at :794). Dormancy evidence unchanged — zero
    deterministic-clock consumers anywhere. Disposition VERIFIED-DORMANT.
  council: no — mechanical once a consumer exists; not urgent, genuinely dormant today
  heading: '### B-44 · No production bootstrap site injects a non-default `time_source` clock *(split
    from B-29, 2026-07-15)*'
- id: B-45
  title: Cross-process ledger lock has no genuine Windows serialization backend
  status: registered_finding
  pr: '#1224 (the combined B-93+B-45 Class 2 fork filing) + #1233 (the 2026-08-05 ratification record) + #1241 (the B-93 build leg — enumeration update only)'
  summary: '`harness_is.cross_process_ledger_lock` (B-40) degrades to a no-op on Windows (`sys.platform
    == "win32"`) rather than providing real cross-process serialization there — Windows sits at exact
    pre-B-40 parity (same as before the module existed), not at feature parity with POSIX. The identical
    gap already exists, unregistered until now, at `harness_runtime.lifecycle.reconciler_pause_resume_substrate`''s
    own unconditional `fcntl` import (that module isn''t eagerly imported at package `__init__`, so it
    doesn''t break basic import on Windows, but its lock is equally POSIX-only). 2026-07-30 post-#1159 re-check (gate-log LOW batch): TWO corrections, neither restructuring the close-out. (a) ENUMERATION -- recounted by direct read at HEAD (the row''s own count was of MODULES; what a Windows backend must actually cover is no-op SURFACES). THREE modules carry same-host POSIX `flock` locks -- unchanged, and correctly recorded at the 2026-07-27 update -- but they expose FIVE distinct `_IS_WINDOWS` no-op branches: `cross_process_write_lock` (`harness-is/src/harness_is/cross_process_ledger_lock.py:358`), `cross_process_replace_lock` (`:478`), `cross_process_read_lock` (`:560`), `cross_process_scope_lock` (`:205`), and `protected_result_store._cross_process_lock` (`harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:339` — *the branch is at `:363` at 2026-08-05, docstring `:358`*). `cross_process_scope_lock` -- added at U-MEM-27 for the memory store''s per-root guarded-write hold -- is the one this row has never named: it is neither a canonical-file lock nor a directory lock (it locks a provisioned lock FILE under a scope root), so the close-out''s "canonical-file/directory lock context managers and `_DirLock`" phrasing does not reach it. It is exclusive-only (it builds on `_DirLock`''s LOCK_EX face), so it maps cleanly onto `msvcrt.locking` and does NOT add to the LOCK_SH/LOCK_EX hard part. `_DirLock` itself has no guard of its own (callers gate), and `reconciler_pause_resume_substrate` still has none either -- its unconditional `fcntl` import is an import-time failure rather than a no-op, already recorded at (c) above. (b) URGENCY -- the "Windows sits at exact pre-B-40 parity" rationale is no longer whole. That framing holds while the no-op only withholds an improvement; U-MEM-27 made it withhold a CORRECTNESS guarantee. `CanonicalMemoryStore.write_record_guarded`''s commit binding is documented as cross-process (`memory_store.py`, the SCOPE paragraph) and witnessed against a genuine second OS process (`harness-is/tests/test_memory_store_cross_process_guard.py`); on Windows that binding silently degrades to in-process-only, so a promotion activation can commit against provenance a concurrent process already superseded -- a wrong durable write, not a missing optimization. Still not blocking (no Windows CI, no Windows deployment -- the phrase "no active Windows target" was CORRECTED IN PLACE 2026-08-05 at the B-93+B-45 combined fork filing, because Windows IS a committed host-OS surface at Target_Stack_Commitment_v1.md:42 (C-STK-10, [HIGH], "macOS, Linux, Windows ... without per-OS port forking") and ADR-F4.md:21 / :48, a fact the B-40 block already quotes verbatim; the correct statement is that Windows is a design-substrate-contracted host OS with NO CI witness and NO packaging declaration -- a standing divergence from a committed constraint, currently unobservable, NOT an uncommitted surface), but the failure mode to state at build time is data correctness, not parity.
    2026-07-31 re-check
    (at the B-103 arc): ENUMERATION REFRESHED AGAIN -- FOUR modules / SIX surfaces. The B-97(a) leg
    promoted `cross_process_journal_lock` to module scope in
    `harness-runtime/src/harness_runtime/lifecycle/journal_workflow_pause_store.py`, and its Windows
    branch (`:477`) is a SIXTH no-op surface in a FOURTH module -- same carve-out, same posture,
    newly named. NOVEL POSTURE DATUM, worth recording because it is a first: that module also added
    `journal_exclusion_is_degraded()` (`:402`), the FIRST NAMED PREDICATE anywhere over B-45''s
    degradation -- callers ask "can this primitive exclude a straggler appender?" rather than
    re-testing the platform, and the (3b) pause-journal adoption tool consults it to REFUSE BY
    DEFAULT where the lock degrades (`admin/pause_journal_adoption.py:412`). That is the first place
    the codebase ACTS on this row''s carve-out instead of merely documenting it. O-1 (lens-1,
    B-97(b) arc, 2026-07-31 -- recorded here because it is a lock-identity residual of the same
    primitive and was recoverable from no durable surface): pause-journal DISPOSAL unlinks the lock
    file and takes no lock, so a holder of the OLD inode does not exclude a re-creator (which gets a
    fresh O_CREAT inode); gated behind `--acknowledge-discarding-recoverable-state`, ACCEPTED
    RESIDUAL. STATUS UNCHANGED -- still registered, still not built.'
  close_out: 'RATIFIED-DEFERRED 2026-08-05 (the combined B-93+B-45 fork, READING B): decision
    1 -- build a Windows serialization backend now -- is NO, and this row''s STATUS IS UNCHANGED
    at `registered_finding` with its demand tests written in. THE DECIDING GROUND IS WITNESSABILITY,
    NOT THE BACKEND PICK: with no Windows CI, landing a backend flips `journal_exclusion_is_degraded()`
    (`journal_workflow_pause_store.py:414`) to False and DISARMS the refuse-by-default consult
    at `admin/pause_journal_adoption.py:412`, replacing a DECLARED SAFE DEGRADATION with an UNVERIFIED
    exclusion claim -- strictly worse than today. RE-OPEN TRIGGERS (either alone fires): D-1 --
    Windows CI exists, and it must be an EXECUTING BEHAVIOURAL witness (a lint-only `windows-latest`
    job does NOT satisfy it, codex R8); D-2 -- an operator or deployment ASKS FOR a Windows host,
    OR any packaging surface DECLARES one (an `Operating System ::` classifier or a `sys_platform`
    marker in a harness pyproject.toml -- today: ZERO). D-2 is deliberately kept in the fork section
    4 OBSERVABLE form and NOT abbreviated to ''a declared Windows target'': section 3(i) already
    establishes Windows as a design-time COMMITTED host-OS surface, so the abbreviated form would
    be satisfied at filing time and this deferral would reopen immediately. NO COUNCIL is owed
    unless decision 1 later flips (the C3 x C11 dyadic was conditional on YES). If it ever flips,
    the B-93 deadline leg MUST LAND FIRST, and the close is a MULTI-SURFACE DOC SWEEP across the
    6+ sites citing this row as the workspace''s Windows posture. Registered, not built — deliberate
    scope call (advisor()-reviewed during B-40''s own PR), not an oversight. THE FIX IS A REAL
    DECISION AND THE DECISION IS WHICH VEHICLE, with three arms — not whether the shared face
    must be degraded (rewritten current-first 2026-08-05 at the B-93+B-45 combined fork filing)
    — msvcrt exposes the non-blocking LK_NBLCK alongside LK_LOCK, and filelock''s ReadWriteLock
    (3.21.0+) provides the shared-reader/exclusive-writer split portably (SQLite-backed) — the
    vehicle Plan_Executability_Audit_v1.md:84 recommended at [HIGH] for U-IS-11, binding decision
    routed to Session 6 at line 230 and NEVER RECORDED anywhere in this workspace; any filelock
    adoption is a lock-IDENTITY migration at every converted site, not a drop-in (filing section
    3(v)). (The original registration reasoned only from msvcrt.locking being byte-range/exclusive-only
    and unable to provide the LOCK_SH/LOCK_EX split both lock modules rely on — true about msvcrt,
    insufficient as the reason the fix is hard.) A correct cross-platform abstraction preserving
    those semantics (e.g. a small shared/exclusive lock facade backed by `fcntl` on POSIX and
    a genuine Windows-native primitive, or a portable third-party lock library) is its own scoped
    arc, not a hasty `sys.platform` branch bolted onto a PR already touching hash-chain integrity.
    **Close-out steps:** (1) pick a Windows backend (`msvcrt`-based exclusive-only lock with a
    documented downgrade of the shared-read guarantee, or a vetted portable library — a real design
    choice, council-eligible if the tradeoff is non-obvious); (2) wire it in behind the `_IS_WINDOWS`
    no-op branches of the canonical-file/directory lock context managers and `_DirLock` (the pre-B-46
    `_flock` symbol was deleted at PR 1052), also covering the transitional legacy-sidecar protocol
    retirement once no pre-B-46 writers remain; (3) apply the same fix to `reconciler_pause_resume_substrate`''s
    `_workflow_lock` AND -- widened 2026-07-27 -- `protected_result_store._cross_process_lock`
    (added by B-73, PR 1120) so all THREE same-host lock primitives in this codebase reach genuine
    Windows parity together, not one at a time. The third is exclusive-only (LOCK_EX), so it maps
    cleanly onto msvcrt.locking and does NOT add to the LOCK_SH/LOCK_EX hard part. No Windows
    CI exists today, so this is currently untested-by-CI on that platform regardless — low urgency,
    not blocking (the clause "until Windows becomes an active target" was CORRECTED IN PLACE 2026-08-05
    at the B-93+B-45 combined fork filing — Windows IS committed at Target_Stack_Commitment_v1.md:42
    / ADR-F4.md:21 / :48; the real gate on this half is WITNESSABILITY, i.e. the absence of Windows
    CI, not the backend pick). 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT
    and the 2026-07-31 enumeration is exact, with ONE drifted cite corrected in the summary above.
    Six Windows no-op surfaces re-verified individually across four modules — cross_process_ledger_lock.py
    `:205` (scope), `:358` (write), `:478` (replace), `:560` (read); protected_result_store.py
    `:363` (the row text carried `:339`); journal_workflow_pause_store.py `:477` — with `_IS_WINDOWS`
    itself now defined at cross_process_ledger_lock.py:90 (the prose row carried `:80`). reconciler_pause_resume_substrate.py
    still takes an unconditional fcntl lock at `:294` with no Windows guard at all (the import-time-crash
    case, not a no-op). journal_exclusion_is_degraded() at journal_workflow_pause_store.py:402
    and its refuse-by-default consult at admin/pause_journal_adoption.py:412 both resolve exactly;
    ZERO msvcrt references repo-wide; no Windows CI. The (e) correctness-degradation sharpening
    also holds — DerivedIndexInvalidation.commit_seq now states the Windows uniqueness bound in
    its own docstring, citing this row (harness-is/src/harness_is/memory_store.py:154-163). Disposition
    VERIFIED-DORMANT, fix FORK-SHAPED (close-out step 1 is a real backend choice; msvcrt.locking
    is exclusive-only and cannot express the LOCK_SH face at cross_process_ledger_lock.py:330
    / :605 -- though that ground was itself corrected 2026-08-05, see the msvcrt parenthetical
    above; filelock.ReadWriteLock expresses the shared face portably, so the open decision is
    WHICH VEHICLE). NOT re-verified this pass — the full downstream doc-cite inventory a retirement
    sweep would need (only the six no-op surfaces themselves were re-read). FILED 2026-08-05 —
    combined Class 2 fork with B-93 at .harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md
    (grounding HEAD 966bc658). RATIFIED-DEFERRED 2026-08-05 -- the ''awaiting operator ratification''
    clause is SUPERSEDED; ''STATUS UNCHANGED (registered_finding)'' remains TRUE, and is now true
    BY DECISION rather than by pendency. EVERYTHING FROM HERE ON IS THE PRE-RATIFICATION RECORD,
    SUPERSEDED 2026-08-05 BY THE RATIFICATION PARAGRAPH ABOVE and kept only as history -- read
    it as ''what this row said before the operator answered'', never as current state.  Four readings
    — A full combined fix / B deadline-now with Windows serialization deferred (RECOMMENDED) /
    C Windows-only (dominated) / D defer both under demand tests D-0 (a second write_record_guarded
    precondition), D-1 (Windows CI exists), D-2 (a declared Windows target), D-3 (an observed
    stall), D-4 (a third row against this substrate); runner-up D. THE DECISIVE FINDING FOR THIS
    ROW — its half is gated on WITNESSABILITY, not on the backend pick. With no Windows CI, landing
    a backend flips journal_exclusion_is_degraded() (journal_workflow_pause_store.py:414) to False
    and disarms the refuse-by-default consult at admin/pause_journal_adoption.py:412, replacing
    a declared safe degradation with an UNVERIFIED exclusion claim -- strictly worse than today.
    ZERO spec and plan delta under EVERY reading, verified not assumed — C-IS-09 §9.4 (Spec_Information_Substrate_v1.md:900)
    and C-IS-07 §7.4 (:756) both defer the mechanism, and the IS spec contains ZERO occurrences
    of timeout, deadline, platform or cross-process. CROSS-REF ADDED 2026-08-05: B-95 (whose Windows
    same-value `commit_seq` limb this row''s bound produces) was CLOSED at the 2026-08-05 ratification
    batch as a ratified accepted bound, and this row is one of its named trigger-monitoring carriers
    -- a Windows target landing here also puts an agent on B-95''s surface. ENUMERATION UPDATED #1241 (the B-93 build leg, 2026-08-06) -- IN PLACE, because this row''s own count is what changed. The section 3(ii) IMPORT REPAIR LANDED: `reconciler_pause_resume_substrate` now declares `_IS_WINDOWS` and imports `fcntl` FUNCTION-LOCALLY like its three siblings, so `_workflow_lock` degrades to a documented no-op on Windows instead of the module failing to IMPORT there (the bootstrap path `bootstrap/factories/r_cxa_2_producer_loop_factory.py` raised ModuleNotFoundError). CONSEQUENCE FOR THIS ROW''S NUMBERS, superseding the 2026-08-05 re-verification above rather than sitting beside it: Windows no-op LOCK surfaces are now SEVEN, not six, across four modules; `_IS_WINDOWS` flag declarations are now FOUR, not three; and the clause ''reconciler_pause_resume_substrate still takes an unconditional fcntl lock with no Windows guard at all (the import-time-crash case, not a no-op)'' is FALSE as of this leg -- it is now a no-op case exactly like the other six. CLOSE-OUT STEP (3) IS **NOT** DISCHARGED, NOT EVEN IN PART -- corrected at the merge-gate (lens 2), because ''half-discharged'' overstated it. Step (3) demands genuine WINDOWS BACKEND PARITY across all three same-host lock primitives; what landed is FLAG parity at ONE of them, which is a PRECONDITION for that work rather than a portion of it. The step''s own deliverable is untouched: Windows still excludes nothing anywhere. THE ROW''S SUBSTANCE IS UNCHANGED -- a repair that makes a Windows process START is not a serialization claim; Windows still EXCLUDES NOTHING at any of the seven surfaces, `journal_exclusion_is_degraded()` still returns `_IS_WINDOWS`, and the refuse-by-default consult at `admin/pause_journal_adoption.py` stays armed. STATUS UNCHANGED at `registered_finding`; demand tests D-1 and D-2 unchanged; the deferral''s deciding ground (witnessability, not the backend pick) unchanged. The B-93 half is now CLOSED, so the fork''s ORDERING constraint is satisfied in advance: if decision 1 ever flips, the deadline leg has already landed and the Windows leg no longer needs to re-traverse the nine sites.'
  council: no for now — narrow implementation-discretion follow-on per C-IS-09 §9.4's own deferred-mechanism
    framing; council-eligible AT BUILD TIME only if the msvcrt-exclusive-only-lock tradeoff (weaker-than-POSIX
    shared-read guarantee on Windows) turns out non-obvious once actually scoped. ADJUDICATED
    2026-08-05 at the B-93+B-45 combined fork filing — the tradeoff was scoped and PROBE-RESOLVED as
    ORTHOGONAL to the deadline semantics, so NO convening is owed for readings B, C or D. The one
    exception, and it is a genuine nameable tension — a Reading-A selection (build the Windows
    backend BEFORE a Windows CI witness exists) owes a dyadic C3 (information-substrate integrity;
    an unverified exclusion claim is worse than a declared no-op) vs C11 (operator-loop /
    local-deployment; a committed cross-platform target that never works is a broken commitment)
    convening before any Windows code is authored
  heading: '### B-45 · Cross-process ledger lock has no genuine Windows serialization backend *(surfaced at B-40''s own PR, out-of-family Codex + advisor(), 2026-07-15)* *(**RATIFIED-DEFERRED 2026-08-05** — the Windows backend is NOT built; D-1 / D-2 are the re-open triggers)*'
- id: B-46
  title: Cross-process ledger lock has a first-write TOCTOU on lazy sidecar provisioning
  status: closed
  pr: '#1052 (canonical-file lock redesign)'
  summary: '`harness_is.cross_process_ledger_lock`''s lock sidecar (B-40) is provisioned lazily by whichever
    writer touches a ledger first. A reader racing that very first writer can pass `cross_process_read_lock`''s
    "sidecar doesn''t exist yet" existence check before the writer creates it, then read the ledger unguarded
    while that writer is mid-append -- defeating the torn-read protection for exactly the first write/read
    race against a brand-new ledger. The identical gap exists in `jsonl_event_ledger_lifecycle.py`''s
    initialization probe (a fresh ledger can take the missing-file branch and return `entry_count=0` without
    waiting, even while another process already holds the writer lock but hasn''t yet created/populated
    the file).'
  close_out: 'CLOSED 2026-07-18 (PR #1052, refreshed at merge-gate round-1 to shipped HEAD) -- the
    ratified shape BUILT and hardened through 9 codex rounds. Canonical-file flock (separate
    lock-only fd) + per-directory EXCLUSIVE lock with a per-thread REENTRANT face covering the
    absent-file state; non-blocking file-probe handoff (never block on a file lock while holding
    the dir); the lock never creates the canonical file (audit round-36 guard preserved); readers
    dir-first, never create/mkdir; inode-stability verify-retry; cross_process_replace_lock for
    inode-replacing sections (shadow_git_rollback switched onto it); ONE global lock order
    (cross-process before _WRITE_LOCK, 3 call sites reordered); transitional mixed-version
    coexistence (writers PROVISION + lock the legacy .lock sidecar with O_NOFOLLOW + dev/ino
    alias checks and loud non-regular refusal; readers lock it when present). RESIDUALS per the
    module docstring -- absent parent dir at read time; a NEW reader racing a FRESH old-version
    writer''s first sidecar creation (torn read, loud parse error). 20+ witnesses incl.
    two-process fork-based dir-flock coverage; all mutation classes probe-killed.'
  council: no -- narrow implementation-discretion follow-on, same category as B-45; not an open spec question
  heading: '### B-46 · Cross-process ledger lock has a first-write TOCTOU on lazy sidecar provisioning
    *(surfaced at B-40''s own PR, out-of-family Codex round 4 + advisor(), 2026-07-15; CLOSED 2026-07-18
    — PR #1052)*'
- id: B-47
  title: AwsKmsSigningBackend has no production composition-root call site
  status: closed
  pr: '#1033 (PR A seam) + #1034 (PR B1 sidecar) + #1036 (PR B2a threading/breaker); remainder split to B-49..B-53 at the 2026-07-17 disposition'
  summary: 'CLOSED 2026-07-17. The composition root is LANDED end-to-end: OD spec v1.33 section
    21.2.1 seam (PR #1033), the full-entry durable sidecar + config/factory + token-map threading
    (PR #1034), and the remaining composer/cost threading + C9 breaker + bootstrap validation +
    audit-offload hardening (PR #1036) -- a MULTI_TENANT_COMPLIANCE deployment with aws-kms configured
    gets real signed audit entries on every production audit write, durably persisted. Residual work
    split to B-49 (per-family verifier), B-50 (sidecar scale engineering), B-51 (tenant binding fork),
    B-52 (fail-closed policy fork), B-53 (CLI promotion); B-48 tracks the sync-inner-on-loop fork.
    Full landing narrative preserved at the heading section in the prose register.'
  close_out: 'Author a harness_runtime composition-root unit (mirrors the ProviderSecretResolver / GcpSecretManagerResolver
    precedent at R-421) that: (a) resolves a key_id -> KMS key ARN mapping from RuntimeConfig at MULTI_TENANT_COMPLIANCE
    deployment-time; (b) constructs AwsKmsSigningBackend with a boto3 KMS client sourced from the runtime''s
    own credential-resolution chain; (c) threads it through to the real sign_audit_entry / verify_audit_entry_signature
    call sites -- as of PR A (OD spec v1.33 section 21.2.1) the production-reachable injection points exist
    and are backend-ready (the OD sign_audit_entry via cp_audit_to_od_audit, plus the redaction-token
    composer/map); this unit threads a constructed backend into them; (d) per ADR-D8''s
    own C11 concession, wires the C9 per-{secret_backend, scope} breaker onto the signing call itself, not
    just key resolution, since KMS availability now sits on the write-time hot path; (e) persists the signed
    entry (or an equivalent durable sidecar) at RuntimeAuditLedgerWriter.append, which today serializes
    neither payload nor signature_attrs -- a real KMS signature must reach durable storage, not be produced
    then dropped (out-of-family Codex round-3 P1 on PR A, verified directly). PR B1 (2026-07-16) landed
    items (a) AuditSigningConfig on RuntimeConfig, (b) make_audit_signing_backend (boto3-import-guarded,
    kms_client injection seam, fail-loud on missing SDK/alias ARNs), (e) the full-entry durable sidecar
    (audit-entries.jsonl beside the IS ledger, APPENDED-only writes, B-40-locked, tenant-scoped
    read_full_entries_for_tenant rehydration), and the token-map half of (c) (stage-4 constructs the
    backend onto ctx.audit_signing_backend and threads it through materialize_span_processor_stage into
    AuditLedgerRedactionTokenMap; full-chain witness proves stage-to-sidecar real-signature round-trip).
    REMAINING for the close -- rest of (c) (hitl_gate_composer / sub_agent_dispatch / cost_attribution_*
    builder threading), (d) the C9 breaker, and (f) below; (f) binds tenant scope into
    what gets signed (out-of-family Codex round-5 P1 on PR A) -- the section 21.2.1 canonical message binds
    entry-hash/key_id/algo/period but not tenant identity, which today enters only at the writer wrap, so
    under a DEPLOYMENT-scoped signing key serving multiple tenants a signed entry could be re-presented
    under another tenant without invalidating its signature (a TENANT-scoped key_id already binds the
    tenant; the gap is deployment-scoped keys). Resolve jointly with (e) at PR B -- options include binding
    tenant into AuditPayload attrs, a fifth canonical-message segment (spec delta), or requiring
    tenant-scoped key_ids at MULTI_TENANT_COMPLIANCE. No live exposure at PR A (zero backends wired);
    (g) bounds the sidecar membership index (out-of-family Codex round-24 on PR B1) -- the in-memory
    identity-to-digest map grows linearly with complete audit history in a long-running MTC deployment.
    A bounded LRU alone cannot work (membership must prove ABSENCE, which needs complete knowledge or
    a full scan), so the fix is a disk-backed index. CORRECTED 2026-07-17 (design-disposition leg,
    .harness/b-47-pr-b2-design-disposition-v1.md section 0): the earlier phrase claiming section 21.2
    carries a hash-chained-SQLite storage commitment was an over-reading -- section 21.2 commits the
    Tier-5 durability CLASS (ADR-D1 v1.2 identifies Tier-5 as the F2 state-ledger class) plus
    hash-chain/signature properties, naming NO storage engine; ADR-D5 v1.4 reclassified its earlier
    audit-ledger SQLite schema as a NONCANONICAL DEFERRED model and committed JSONL-via-IS-composition
    as canonical (OD v1.31 completed the reconciliation); the remaining spec sqlite references are the
    Tier-3 trace ring-buffers, a separate data class. JSONL-vs-SQLite is impl discretion, NOT a spec
    fork -- a SQLite migration would itself reopen a settled ADR commitment; (g) is plain
    performance engineering; (h)
    partitions chain verification by producer family (out-of-family Codex round-30 on PR B1) -- a
    per-tenant sidecar entry set interleaves INDEPENDENT OD entry families, so the verifier must be
    per-family AND producer-aware -- families discriminated per the B-49 policy (redaction rows by
    their audit.redaction_token.* namespace keys, since caller-supplied entry_core rows have no
    reliable prefix; others by action-id prefix) -- chain
    verification ONLY for the redaction-token family; per-entry CONTENT-HASH checks for cost, HITL,
    and sub-agent dispatch, which intentionally emit genesis-style predecessors on every entry
    (superseded by the B-49 policy, codex rounds 3/5/7/8 on the disposition leg); global
    re-sequencing at sign time would touch spec-committed converter prior-hash semantics and is a
    design-phase question, not an impl patch. Reader docstring now states the
    non-single-chain contract explicitly; (i) makes redaction-chain tail-read + compose + append
    cross-process atomic (out-of-family Codex round-32 on PR B1) -- two processes appending for one
    tenant can both read the same durable family tail and sign different entries with the identical
    predecessor (the sidecar lock serializes persistence only, not chain-position selection). The
    in-process half is serialized; the clean cross-process fix is an atomic writer operation, which
    widening the existing B-40 lock scope across tail-read+compose+append gives (no new primitive;
    no SQLite redesign is spec-forced -- corrected 2026-07-17 per the disposition doc). Already
    documented in-code as a residual since round 13; formally registered now; (j) upgrade/migration
    surface (out-of-family Codex rounds 44-48 on PR B1, all fixed in-PR): the JSONL sidecar gained
    IS-refs-pre-snapshot reader ordering, warm-index truncation coverage, injective tenant tags with
    config-load validation, duplicate-row rejection, bounded daemon-thread shutdown fsyncs under one
    shared budget, and the explicit adopt_legacy_is_refs() pre-sidecar migration runnable via python -m
    harness_runtime.admin.migrate_audit_sidecar. That module is deliberately NOT in the spec-committed
    [project.scripts] inventory (runtime spec section 13.4 / 14.18.1) -- promoting it to a console
    script is a design-substrate decision to take WITH the (e)/(g) SQLite persistence fork at PR B2,
    since a substrate swap owes its own migration path from the JSONL sidecar anyway; (k) merge-gate
    round-1 follow-ups on PR B1 (all three lenses APPROVE, notes explicitly non-blocking): reorder the
    exists()-before-refs-predicate samples at the two UNLOCKED sites (shutdown flush guard +
    read_full_entries_for_tenant absence check) so a racing first-ever append cannot false-positive the
    round-36 loss error; witness the stage-4 signing_backend= kwarg through the REAL bootstrap stage
    (currently unwitnessed-but-fail-loud); one integration witness wiring a real RuntimeAuditLedgerWriter
    into flush_observability (seam currently SimpleNamespace-only); pin the migrate-CLI __main__ guard;
    pin the sidecar timed_out flag with a sidecar-only stall. Fold into the PR-B2 arc; (m) fail-open
    vs fail-closed for signing failures on best-effort audit paths (out-of-family Codex round-4 on PR
    B2a): signing failures are now TYPED (AuditSigningFailedError via the breaker wrap) and surfaced
    at ERROR level at all six best-effort swallow sites, but dispatch is still preserved (fail-open)
    because the CP validator framework''s section 28.10.4 invariant 2 (observability MUST NOT fail
    dispatch) is spec-committed. Whether MULTI_TENANT_COMPLIANCE should instead fail CLOSED on an
    unsigned audit write is a genuine design-phase policy fork (section 21.2 signed-audit commitment
    vs section 28.10.4) -- resolve with the (e)-(i) persistence-substrate design at PR B2. PR B2a
    (2026-07-17) landed the REST of (c) -- signing backend threaded into all remaining production
    audit composers (3x RuntimeHITLGateComposer + RuntimeSubAgentDispatcher + the 4 cost builders via
    their carriers/factories, for_single_host included) with a real-bootstrap identity witness -- and
    (d), the C9 BreakerGuardedSigningBackend wrapped at the factory (epoch-guarded closed/open/half-open,
    fail-loud AuditSigningBreakerOpenError; keyed on the C-AS-07 SecretBackendBreakerKey), plus
    bootstrap key-id validation, the dedicated daemon audit-offload executor with per-dispatch
    completion ack (no post-timeout audit commits), typed + loudly-surfaced signing failures, append
    serialization (in-lock timestamps), and the item-(k) witness follow-ups. REMAINING for the B-47
    close: the (e)-(j)+(m) cluster, DISPOSITIONED 2026-07-17 at
    .harness/b-47-pr-b2-design-disposition-v1.md -- (e) landed; (g)/(i) impl-discretion engineering
    (no spec surface); (h) per-family chain verifier = the next BUILDABLE impl unit (CXA action-id
    prefix partition + per-family verify_hash_chain_integrity); (f) tenant binding and (m)
    fail-open-vs-fail-closed are the two GENUINE spec-delta forks (recommendations recorded in the
    disposition doc -- (f): SUPERSEDED here -- see B-51 for the
    revised recommendation (fifth canonical-message segment primary; key-scope validation shown
    unenforceable); (m): mirror OD v1.8 section C-OD-28.2 operator-configurable default-fail-closed
    pattern via an audit_signing_fail_closed flag, default ON at MTC); (j) rides the next
    runtime-spec delta as a one-row scripts-inventory addition. B-48 tracks the sync-inner-on-loop
    fork revision separately.'
  council: 'conditional -- mechanical composition-root wiring once a real sign_audit_entry call site exists;
    revisit if the call-site design itself surfaces a cross-axis tension'
  heading: '### B-47 · `AwsKmsSigningBackend` has no production composition-root call site *(surfaced
    at `B-36`''s own PR, out-of-family Codex review, 2026-07-16)*'
- id: B-48
  title: Sync sub-agent dispatch executes on the event loop (U-RT-60 wrap-asymmetry residual)
  status: closed
  pr: '#1060'
  summary: 'OPEN -- spec+plan deltas APPLIED 2026-07-19 (Runtime v1.102 + CP v1.102 + IS v1.11 + plans v2.50/v2.39/v2.7 + Core v1.3); the executor impl arc (U-CORE-03 + U-RT-140..144 + U-CP-101 + the CP/IS amendments) is the live next step. Out-of-family Codex round-2 P1 on B-47 PR B2a: RuntimeHITLGateComposer._dispatch_inner
    invokes the sync C-RT-17 sub-agent dispatcher DIRECTLY on the event loop (the U-RT-60
    wrap-asymmetry fork Q3-ratified direct-call reading), so the entire sub-agent dispatch -- child
    workflow execution (seconds-long), its F2/ledger writes, and now the B2a-threaded KMS signing --
    blocks every concurrent workflow. Pre-existing at the fork ratification (child workflows already
    ran on the loop through this path; the audit-signing threading only ADDS KMS network latency to an
    already-blocking ratified surface). A worker-thread offload of the sync inner also needs the
    recursion/executor design (child workflows re-enter the sync driver whose facade bridges back onto
    the loop; a bounded offload pool can deadlock under recursive SUB_AGENT_DISPATCH depth) and a
    re-validation of pause/resume + fan-out machinery under off-loop execution.'
  close_out: 'Class 2 fork doc FILED 2026-07-18 at
    .harness/class_2_fork_b48_sync_subagent_dispatch_offload.md -- the filing is the routing authority
    (it supersedes this row''s earlier unbounded-or-depth-aware phrasing AND the loop-bridge deadlock
    defect record''s fix directions). Recommendation: option B = custom grow-on-demand executor +
    configurable RuntimeConfig hard cap + fail-fast at the cap, NEVER queue (the only MATERIALIZED bound is the
    per-parent fan-out cap 3 for HIERARCHICAL_DELEGATION; the section-25.11 depth phrase names no value
    or carrier, descent flag is boolean, generic recursion + aggregate active frames unbounded,
    ThreadPoolExecutor has no unbounded mode). Apply arc per the filing: construction-time dispatch-mode for all three stage-5
    composers, cancellation semantics first, pause/resume + resume-context per-child isolation, OTel
    context carry, drain-timestamp fix (IS back-flow) + xfail removal, deadlock-anchor XPASS promotion,
    Runtime spec delta (C-RT-03 cap field) + Runtime plan delta. Selection RATIFIED 2026-07-18 (option B);
    spec-apply COMPLETE 2026-07-19. Impl arc LANDED 2026-07-19 on branch b48-executor-impl (PR
    #1060, pending merge): U-CORE-03 + U-RT-140..144 (grow-on-demand executor, occupied+N+S shared
    frame budget, atomic per-branch reservation via DefaultCapacityAuthority, exactly-once
    CapacityLease release under TOCTOU, shutdown drain wiring) + U-CP-101 (fan-out admission
    gating at all four construction sites) + the B-39 interim sequential-dispatch constraint. FIVE
    successive rounds of decorrelated pre-merge review (merge-gate 3-lens + out-of-family Codex,
    each re-run to convergence before the next) surfaced 24 findings total, ALL fixed with PD-8
    mutation-probed witnesses across the arc: round 1 (8) -- input-order fitting-prefix admission,
    CapacityLease.release() TOCTOU, completed-job counting, executor.drain() wired into shutdown,
    IS C-IS-07 writer-owned timestamp widened to the sub-agent audit path, lease-leak on
    post-admission early-return paths, descendant cancel-token ambiguity bubbling, real provider
    attempts marked in-flight for the fence-ack contract, fan-out barrier deadline watchdog tripping
    the job-wide fence directly at cutoff; round 2 (5) -- capacity-lease premature-release ordering
    race, warm-up Phase-2 admission leak on Phase-1 failure, PROCEED-tier rejected branch
    mis-recorded as executed, executor thread-start-failure lease/future leak, capacity-error
    rt_fail_class marker; round 3 (6 fixed + 1 registered) -- bind_release_to_job() TOCTOU abort-
    before-submit, PROCEED-tier warm-up Phase-2 leak on deadline cutoff, shutdown ordering
    (drain-before-flush-observability), cancellation fence applied to nested tool-retry attempts,
    fence tripped on PROCEED-tier barrier cancellation, dispatch-executor drain bounded within the
    shutdown deadline, plus the IS writer-owned-timestamp-vs-future-dated-prior-entry gap assessed
    and confirmed spec-conformant (no fix needed, per Spec_Information_Substrate_v1.md section 7.6
    clause (c)); round 4/5b (4) -- child ledger writes re-fenced after dispatch (a fence tripped
    mid-dispatch no longer silently commits), the deadline-watchdog inflight-dispatch registry
    isolated per offloaded job (severs a cross-thread .cancel() hazard on nested fan-out under
    offload, mirroring the existing INTER_STEP_CHANNEL_VAR per-child rebind), the B-39 resumed-
    target-dispatches-first sequencing extended to the PROCEED cascade policy (previously only
    enforced on the strict/cascade-cancel path -- confirmed via direct grounding to be a KeyError
    crash under concurrent_cache_warmup, not merely a race), and drain() sending the idle-worker
    stop sentinel so daemon threads actually terminate instead of accumulating one leaked set per
    top-level run()/resume() call for the life of a long-running server process. Round 4/5b
    fixes landed with full local verification green (ruff + pyright + pytest across harness-cp,
    harness-runtime, harness-core, harness-is); round 6 (2) -- BRANCH_CAPACITY_LEASE_VAR reset at
    the offload boundary (a nested SUB_AGENT_DISPATCH descendant was reading and re-binding the
    ancestor branch''s own admitted lease via copy_context(), risking a premature ancestor-frame
    release while the ancestor''s job was still running) and the post-dispatch durable-write
    (ledger append) guarded atomically with DispatchCancelToken.effect_entry() instead of a plain
    check-then-write, closing a TOCTOU window where a fence trip landing between the check and the
    write went uncounted by wait_ack(); round 7 (3) -- the branch/worker capacity lease released
    when the pre-dispatch marker write (_mark_branch_dispatched) itself raises (it is the only
    statement before the dispatch-tied releaser is ever scheduled, so a marker-store failure
    previously leaked the frame permanently), HITL webhook delivery (deliver_webhook_for_brief)
    and audit persistence (_compose_and_persist_audit_off_loop) fenced with the same
    effect_entry() guard (both can run on the event loop after the offload boundary''s ack()
    already fired, so a job-wide fence trip racing them previously reported a false-clean drain),
    and sub_agent_dispatch_executor.drain() tracking a _draining flag under the same lock as its
    idle-channel sweep (a worker whose job outlasted the drain snapshot previously re-registered on
    a channel drain() would never look at again, leaking the daemon thread); round 8 (4) -- a
    concurrent-tripper race in DispatchCancelToken.trip() (a second caller tripping an
    already-tripping token could read _inflight_at_trip before the FIRST caller''s own cascade to
    its children, and any resulting bubble-up of a grandchild''s in-flight effect, had finished --
    a false ACKED_CLEAN; closed via a _cascade_done_event gating the early-return path until the
    winner''s full cascade completes), reserve()/reserve_fanout()/submit() never consulting
    _draining (only _worker()''s re-registration path did, so a shutdown-in-progress survivor
    could still admit and launch new work against clients later shutdown steps may already be
    closing; closed by extracting a synchronous idempotent begin_draining() that drain() calls as
    its own first step AND that shutdown() now calls directly, eagerly, before the bounded
    asyncio.wait_for around drain scheduling -- at a near-zero remaining budget that wait_for can
    cancel the whole coroutine before drain() itself ever runs), and step_dispatchers.lookup()
    in _proceed_worker/_cancel_worker (ORCHESTRATOR_WORKERS) stranding a branch''s admission when
    an unbound StepKind raises there (closed with the same release-on-raise guard as round 7''s
    marker-write fix; confirmed PARALLELIZATION''s _proceed_branch/_cancel_branch index a
    pre-resolved dispatcher list at the equivalent point and cannot raise the same way, so they
    did not need the identical guard). EIGHT successive rounds of decorrelated pre-merge review
    (merge-gate 3-lens + out-of-family Codex, each re-run to convergence before the next) have now
    surfaced 33 findings total, ALL fixed with PD-8 mutation-probed witnesses; all fixes landed
    with full local verification green (ruff + pyright + pytest across harness-cp, harness-runtime,
    harness-core, harness-is). Round 9 (merge-gate 3-lens + codex-review) was the pre-committed
    terminal gate per operator-advisor guidance; codex round 9 surfaced 2 new [P2] findings in the
    same executor-lifecycle class, triggering the escalation clause -- surfaced to the operator via
    AskUserQuestion, who selected "fix both, one more gate." Round 9 fixes (2) -- SubAgentDispatchExecutor
    ._run_job() published the future''s result/exception AFTER calling _job_done() (which removes the
    future from _outstanding), so a completion callback (lease release, asyncio bridge) reading the
    future via drain()''s outstanding-set snapshot could observe it as already-done-and-gone before the
    result/exception was actually set; reordered to publish first, closed via a
    test_job_done_runs_after_future_is_already_published spy witness. The second finding -- the
    WHOLE-fan-out admission call running BEFORE ~600 lines of pre-dispatch setup (reconciler-engine
    calls, durable-store writes, untrusted-index reads, the warm-up cohort-key split''s external
    dispatcher-interface call) with no release path for a setup failure -- turned out to have a far
    larger reachable surface than codex''s two named examples (direct grounding via an Explore
    subagent enumerated ~12 raise sites per topology, not 2, plus a structural asymmetry: ORCHESTRATOR_
    WORKERS''s reconciler-gate + lifecycle-emit calls run BEFORE admission there, so only PARALLELIZATION
    carried that particular risk). Fixed by DEFERRING the single _admit_fanout_branch_plan() call to
    immediately before the cascade_policy split at both sites -- eliminating the ownerless window
    entirely rather than adding N per-statement release guards (advisor-recommended: grep-verified
    that nothing between the old and new call sites reads _branch_admissions, so no site was skipped);
    the 3 now-superseded release-on-early-return call sites were removed as dead code rather than kept.
    Both fixes mutation-probed: reverting either deferral (moving admission back to its pre-fix
    position) makes the corresponding witness fail (a real, non-vacuous leak reproduces -- e.g. 2/4
    frames stranded on the cohort-key-split witness). NINE successive rounds of decorrelated pre-merge
    review have now surfaced 35 findings total, ALL fixed with PD-8 mutation-probed witnesses; all
    fixes landed with full local verification green (ruff + pyright + pytest across harness-cp,
    harness-runtime, harness-core, harness-is). Round 10 (merge-gate 3-lens + codex-review) ran as
    the operator-approved FINAL gate. All 3 merge-gate lenses APPROVED (concurrency, spec-conformance,
    test-witness -- the latter flagged one non-blocking docstring overclaim on the O-W CAS-gate test,
    fixed in the same round). Codex round-10 surfaced 3 NEW [P1] findings against the PRE-EXISTING
    cancellation-fence machinery built across rounds 4-8 (nonlinear-topology children skip the fence
    check at their strategy entry point; buffered fan-out ledger drain writes are not fenced; one
    effect_entry() guard spans multiple independent sequential writes) -- NOT regressions in round
    9/10''s own 2-commit delta (verified: none of the 3 findings cite a line this arc''s round-9/10
    commits touched). This is the pre-committed escalation trigger (a new finding in the same
    executor-lifecycle/token class after the agreed "one more gate"); surfaced to the operator via
    AskUserQuestion, who selected "Register + merge now" -- CI was fully green, all 3 lenses had
    already approved, and the 3 findings are bounded pre-existing coverage gaps, not evidence the
    round-9 fix itself regressed anything. Registered as B-60/B-61/B-62 (own rows, each citing
    file:line + codex''s exact rationale + a Class-2-fork-shaped close_out) rather than folded into
    this row or blind-fixed. PR #1060 MERGED to main -- this row transitions to closed.'
  council: 'yes at apply leg -- C1 orchestration vs C9 reliability dyad CONFIRMS the filing-settled
    position (fail-fast + typed error + RuntimeConfig ownership + default sub_agent_dispatch_max_workers
    = 256 in shared-budget frame units (N upstream frames + one inner frame per SYNC sub-agent branch; async inners uncharged), the filing-settled recommendation, RATIFIED 2026-07-18) or surfaces a reasoned deviation back to the operator; it does NOT
    re-derive the default (one-authority discipline, filing codex round-10)'
  heading: '### B-48 · Sync sub-agent dispatch executes on the event loop *(surfaced at B-47 PR B2a,
    out-of-family Codex round-2, 2026-07-17)*'
- id: B-49
  title: Per-family audit chain verifier
  status: closed
  pr: '#1040'
  summary: 'B-47 disposition item (h): a tenant''s sidecar entries interleave independent OD chain
    families (CXA v2.10 section 0.3 action-id prefixes discriminate); running verify_hash_chain_integrity
    over the raw per-tenant sequence fails by construction. PRODUCER-AWARE (codex round-3 on the
    disposition leg): cost projection defaults prior_event_hash to genesis on EVERY entry
    (cost_record_audit_writer.py:50) and HITL uses _empty_summary_hash() (hitl_gate_composer.py:1235),
    and production sub-agent dispatch likewise emits prior_event_hash genesis per entry
    (sub_agent_gate_level_descent.py:215), so cost/HITL/dispatch families get per-entry CONTENT-HASH
    checks only, NOT chain verification; chain-verified = ONLY the redaction-token family,
    discriminated by the audit.redaction_token.* NAMESPACE KEYS the composer stamps (NOT by
    entry_core/action-id prefix, which misses caller-supplied entry_core rows -- PR B1 codex
    round-39). Backend SIGNATURE verification is OUT of scope (codex round-7: section 21.2.1 leaves
    verification out of scope; verify_hash_chain_integrity never touches signature_attrs) -- a
    backend-aware verification API is spec surface routed to the fork legs.'
  close_out: 'Build verify_per_family_chains(entries) with a per-producer verification policy (chained
    vs per-entry families), witness with interleaved multi-family fixtures + mutation probes; wire into
    harness-inspect if natural.'
  council: 'no -- mechanical partition + existing verifier'
  heading: '### B-49 · Per-family audit chain verifier *(split from B-47 disposition, 2026-07-17)*'
- id: B-50
  title: Sidecar scale engineering -- disk-backed membership index + POSIX cross-process chain atomicity
  status: closed
  pr: '#1042 (item (i) transactional token map) + #1044 (item (g) index snapshot)'
  summary: 'B-47 disposition items (g)+(i), BOTH LANDED. Item (i) 2026-07-17 (b-50-transactional-token-map):
    RuntimeAuditLedgerWriter.tenant_transaction holds the full lock stack (append lock + sidecar
    thread lock + cross-process flock) across tail-read + compose + append via unlocked-inner cores;
    the token map derives its prior from the DURABLE tail inside the transaction (the in-memory
    cached position -- a second authority that could drift -- is gone on the production path; duck
    writers keep the fallback). POSIX-only cross-process hold per B-45. Item (g) 2026-07-17
    (b-50-index-snapshot): offset-checkpointed snapshot beside the sidecar (audit-entries.index-snapshot.json)
    -- a DERIVED CACHE adopted at zero-offset cold start with discard-on-any-anomaly validation
    (self-digest, covered-prefix raw-byte digest, inode, offset<=size, same-size mtime drift), atomic temp+replace+fsync writes,
    cadence floor 64 appends (geometric: max(64, members/8)) + immediate after any from-zero fold / legacy adoption; chosen over sqlite3 to
    stay one-authority JSONL-canonical (ADR-D5 v1.4). Forged-snapshot tampering sits in the same
    adversarial-filesystem tier as the forged legacy baseline (round-46 posture) -- read-time verifier
    arc owns it.'
  close_out: 'Disk-backed index (stdlib sqlite3 on the 3.12 baseline, or offset-checkpointed snapshot).
    For (i): NOT a plain outer-lock widening (codex round-6 -- POSIX flock is non-reentrant across
    separately-opened descriptors, so an outer hold self-deadlocks when read_full_entries_for_tenant
    and append reacquire the sidecar lock); needs UNLOCKED inner variants of the tail-read/append the
    outer transaction calls, or a lock-ownership/transaction API on the writer. POSIX-only; Windows
    rides B-45.'
  council: 'no'
  heading: '### B-50 · Sidecar scale engineering *(split from B-47 disposition, 2026-07-17; CLOSED
    2026-07-17 — item (i) PR #1042, item (g) PR #1044)*'
- id: B-51
  title: Tenant-scope binding under deployment-scoped signing keys
  status: closed
  pr: "#1061 + #1066"
  summary: 'RATIFIED 2026-07-18 (all gate items as recommended) -- OPEN (spec + plan deltas APPLIED 2026-07-18: OD v1.34 + CP v1.101 + Runtime v1.101 + plan deltas v2.29/v2.38/v2.49). B-47 disposition item (f): the section 21.2.1 canonical message does not bind tenant
    identity; under a deployment-scoped key a signed entry could be re-presented under another tenant.
    Recommendation REVISED at codex round-3: the fifth canonical-message segment is the PRIMARY fix
    (key_arns carries no tenant association and composers use fixed global key_ids, so a key-scope
    presence check would be a false guarantee). NO EFFECTIVE INTERIM MITIGATION EXISTS (codex
    round-10): composers select fixed global key_ids, so distinct per-tenant mapping entries would
    never be used -- cross-tenant replay under a deployment-scoped key remains unmitigated until the
    message segment lands or tenant-aware key selection is built. Class 1 fork doc FILED
    2026-07-17 at .harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md (leg 1) -- spec+plan deltas APPLIED 2026-07-18 (PR 1056); the impl arc is the live next step (U-OD-30 amended + U-CP-72 + U-RT-137). U-OD-30 (amended) LANDED 2026-07-19 (PR 1061): tenant-bearing sign_audit_entry fifth-segment + signing_token/sidecar_tag normalizer, 3 codex rounds, merge-gate 3-lens APPROVE. STILL OPEN: U-CP-72 converter amendment (threads tenant through cp_audit_to_od_audit + every production call site) and U-RT-137 (writer normalizer delegation -- the RuntimeAuditLedgerWriter._tenant_tag half already landed in PR 1061 as a merge-gate finding fix; the redaction-map AuditLedgerRedactionTokenMap tenant threading also already landed in PR 1061).'
  close_out: 'Fork doc RATIFIED 2026-07-18; spec deltas APPLIED at OD v1.34 + CP v1.101 + Runtime v1.101 + plans (PR 1056). IMPL ARC: implement the
    APPLIED fifth-segment + tenant-bearing API per U-OD-30 (amended) + U-CP-72 + U-RT-137 (the
    canonical-message contract now LIVES at OD v1.34 section 21.2.1 — no further OD amendment owed). The applied obligations (rejected alternatives REMOVED per the ratified gate): the tenant-bearing
    API threads the converters/composers AND the redaction-map compose path (OD v1.34 section 21.2.1
    rows 3-4); sign_rotation_pair is PROHIBITED at MTC until B-33 (row 7 -- threading was rejected);
    the workflow-less prewarm path is REMOVED at MTC by the gate-item-8 disable (U-RT-135 -- no
    synthetic-scope policy); the MTC tenant bootstrap invariant + the record-authenticated retag
    migration story land per U-RT-134/U-RT-139. See OD v1.34 + the plan deltas for the full
    contracts.'
  council: 'yes at apply leg -- C7 compliance vs C2 schema-minimalism dyad, REQUIRED by the
    ratified filing''s final section (dyad 1)'
  heading: '### B-51 · Tenant-scope binding under deployment-scoped keys *(split from B-47 disposition,
    2026-07-17)*'
- id: B-52
  title: Audit-signing fail-closed policy at MULTI_TENANT_COMPLIANCE
  status: closed
  pr: "#1064 + #1065"
  summary: 'RATIFIED 2026-07-18 (two-spec shape; flag-false invalid at MTC; redaction path + prewarm posture as recommended) -- spec+plan deltas APPLIED (PR 1056); impl arc open. B-47 disposition item (m): signing failures are typed + loudly surfaced but dispatch stays
    fail-open (CP section 28.10.4 invariant 2 is spec-committed). Recommendation recorded: mirror OD
    v1.8 section C-OD-28.2''s operator-configurable default-fail-closed pattern via an
    audit_signing_fail_closed flag, default ON at MTC. TWO-SPEC fork (codex rounds 4/8): the OD
    addendum AND a CP amendment (section 28.10.4 invariant 2 is CP-owned); the weaker single-spec
    alternative was REJECTED at ratification. Class 1 fork doc FILED 2026-07-17 at
    .harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md (leg 2) -- spec+plan deltas APPLIED 2026-07-18 (PR 1056); the impl arc is the live next step (U-OD-30 §21.2.3 criteria + U-CP-73 + U-RT-134/135/136).'
  close_out: 'Fork doc RATIFIED 2026-07-18; spec deltas APPLIED (PR 1056). IMPL ARC: add the flag + the ZEROTH-site bootstrap
    invariant (fail-closed requires a configured backend) + consult the flag at ALL TEN
    surfacing sites -- the six fn-internal loud-surface handlers AND the four offload-boundary
    saturation handlers (llm/tool/webhook/hitl outer catches, codex round-6) -- or centralize the
    policy check ahead of every catch. The delta is TWO-SPEC (RATIFIED gate item 2; the weaker
    single-spec fail-open-hook alternative was REJECTED at ratification): the OD text AND a CP amendment --
    section 28.10.4 invariant 2 is CP-owned (Spec_Control_Plane_v1_24 line ~135, hook exceptions MUST
    be swallowed), so fail-closing the validator hook gets a CP carve-out for the audit-signing
    failure class. Three more policy surfaces beyond the ten catch sites, each RATIFIED: the direct
    AuditLedgerRedactionTokenMap signing path is UNCONDITIONALLY fail-closed (gate item 2), the
    zeroth-site absent-backend bootstrap invariant holds, and the prewarm/keepalive swallow-all
    boundary is DISABLED at MTC under fail-closed (gate item 8).
    Post-response signing failures must BYPASS the RetryBreakerFallbackDispatcher machinery entirely
    (classifier/breaker/fallback -- a retry or fallback would duplicate the paid provider call and
    corrupt breaker state); result-preserving surfacing per the filing.'
  council: 'yes at apply leg -- C7 compliance vs C1/C9 reliability dyad, REQUIRED by the ratified
    filing''s final section (dyad 2)'
  heading: '### B-52 · Audit-signing fail-closed policy at MTC *(split from B-47 disposition,
    2026-07-17)*'
- id: B-53
  title: Migration-CLI promotion into the spec-committed scripts inventory
  status: closed
  pr: "#1069 + #1070"
  summary: 'RATIFIED 2026-07-18 (harness migrate-audit-sidecar subcommand rides the Runtime v1.101 rider) -- section 13.4 row LANDED (PR 1056); impl arc open. B-47 disposition item (j): python -m harness_runtime.admin.migrate_audit_sidecar works
    today; promotion = a `harness migrate-audit-sidecar` SUBCOMMAND under the existing flat harness
    console-script namespace (round-22/23 of the filing: no new [project.scripts] entry -- dispatch
    rides the committed harness_runtime.cli:main entry), recorded as a runtime spec section 13.4
    inventory row. LOW urgency. Fork FILED 2026-07-17 via .harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md
    (ratification-gate item 7): rides the B-51/B-52/B-54 arc''s Runtime v1.100 -> v1.101 rider as the
    one-row addition -- ratified with the same gate (item 7); section 13.4 row LANDED at Runtime
    v1.101 (PR 1056); impl arc (U-RT-102 dispatch + U-RT-139 record/retag modes) next.'
  close_out: 'Class 1 back-flow COMPLETE (PR 1056): the section 13.4 inventory row + clearance marker LANDED
    at Runtime v1.101 — remaining work is the U-RT-102 (dispatch) + U-RT-139 (record modes: authoring,
    authentication, retag, completeness refusal) IMPLEMENTATION; no pyproject change; nothing blocks on it.'
  council: 'no'
  heading: '### B-53 · Migration-CLI scripts-inventory promotion *(split from B-47 disposition,
    2026-07-17)*'
- id: B-54
  title: Backend-aware audit signature verification API
  status: closed
  pr: "#1067 + #1068 + #1069"
  summary: 'RATIFIED 2026-07-18 (section 21.2.2 API with all contract requirements, non-blocking default; section 20.3.1 reconciliation into the CP rider) -- spec+plan deltas APPLIED (PR 1056); impl arc open. Split from B-49 at codex round-11 of the disposition leg: verify_hash_chain_integrity
    checks only unkeyed content hashes + links, which a ledger-rewriting attacker can recompute; the
    production SigningBackend.verify path (real KMS verification of persisted signature_attrs,
    key/period handling, legacy-placeholder semantics) has NO consumer and section 21.2.1 explicitly
    leaves verification out of scope -- so adding the verification API is spec surface. Without it the
    MTC tamper-evidence story stops at hash-chain level. Class 1 fork doc FILED 2026-07-17 at
    .harness/class_1_fork_b51_b52_b54_od_signing_amendment_arc.md (leg 3) -- spec+plan deltas APPLIED 2026-07-18 (PR 1056); the impl arc is the live next step (NEW U-OD-55 + U-CP-44/45/42 + U-RT-138).'
  close_out: 'Fork doc RATIFIED 2026-07-18; spec deltas APPLIED (PR 1056; one OD amendment arc carried all
    three legs). IMPL ARC (the verification contract is SPECIFIED at OD v1.34 section 21.2.2 + CP v1.101
    section 3 -- PR 1056): implement the verifier per NEW U-OD-55 + wire into B-49''s per-family unit
    and harness-inspect per U-RT-138 (with the U-CP-44/45 injected-verifier boundary).'
  council: 'yes at apply leg -- C7 compliance vs C9 availability dyad; the blocking condition is
    MET by ratified gate item 5 (section 20.3.1 reconciled into the CP rider)'
  heading: '### B-54 · Backend-aware audit signature verification API *(split from B-49 at the
    disposition leg, 2026-07-17)*'
- id: B-55
  title: Lower-tier fail-closed prewarm/keepalive posture (operator disposition)
  status: closed
  pr: 'ratified-2026-07-21 (operator AUQ: MTC-only scoping ratified as-is — no code change; the gate-item-8 letter becomes the permanent contract)'
  summary: 'Registered at the B-51/B-52/B-54 apply arc (PR 1056, codex rounds 2/9/21/34): gate item 8
    ratified the prewarm/keepalive disable at MTC ONLY, but an EXPLICIT audit_signing_fail_closed=true
    at solo-developer/team-binding would leave the prewarm/keepalive swallow boundary active under a
    resolved-ON policy (a post-paid-call signing failure logged as FAILED, not raised). Neither
    remaining alternative is decidable without the operator: EXTENDING the disable below MTC is a
    behavior change item 8 did not ratify; PROPAGATION through the prewarm boundary breaks the
    committed v1.99 bootstrap-isolation contract (prewarm failure MUST NOT propagate as
    BootstrapFailure). INTERIM (Runtime v1.101): lower-tier explicit true is ACCEPTED
    and honored at every catch-site surface (the ratified non-MTC opt-in); ONLY the prewarm/keepalive
    swallow boundary keeps its v1.99 posture there until this row dispositions.'
  close_out: 'Operator selects: (a) extend the gate-item-8 disable to any tier with the resolved flag
    ON (one-line Runtime spec delta + U-RT-135 widening), or (b) define typed propagation through the
    prewarm boundary (requires amending the v1.99 bootstrap-isolation contract -- a larger delta), or
    (c) ratify the interim rejection as the permanent posture (spec note only). Recommendation at the
    apply arc: (a) -- the same integrity-over-latency tradeoff the operator already ratified at MTC,
    triggered only by the operator''s own explicit opt-in.'
  council: 'no -- a bounded three-option operator selection; the C7/C9 positions are already recorded
    at the apply-arc dyad 2 and the codex rounds cited in the summary'
  heading: '### B-55 · Lower-tier fail-closed prewarm/keepalive posture *(registered at the B-51/B-52/B-54
    apply arc, 2026-07-18)*'
- id: B-56
  title: Corrected-relabel repair for already-tagged audit history
  status: registered_finding
  summary: 'Registered at the B-51/B-52/B-54 apply arc (codex rounds 34/42): an already-tagged sidecar
    row whose mutable source_tag differs from the operator-attested tenant_scope is the relabel
    scenario the OD v1.34 section 21.2.2 row-5 trusted-source rule exists to detect -- but the
    ratified arc authorizes retagging ONLY source_tag="_single" rows, so the repair (a cross-tenant
    tag rewrite + generalized alias) is NOT buildable without its own spec delta. Current disposition:
    record validation REJECTS the mismatch typed (never silently repaired, never silently exempted)
    and surfaces it for operator investigation.'
  close_out: 'IF operator demand materializes: a narrow OD v1.34+ delta authorizing the corrected-
    relabel retag (scope: record-attested mismatches only; the generalized (source_tag, entry_hash)
    alias; the same atomicity/completeness discipline as the _single retag) + U-RT-139 widening.
    Until then the typed rejection is the complete posture. 2026-08-05 grounding re-check at HEAD
    43d4dda5 — premise INTACT and every cited line still resolves EXACTLY (no drift): the retag
    passthrough still short-circuits non-_single tags at record_migration.py:516; the typed
    cross-tenant rejection stands at audit_cutover_record.py:208 and names B-56 verbatim at :212;
    and the generalized (source_tag, entry_hash) alias remains ABSENT at
    per_family_audit_verification.py:296, which still matches only source_tag == normalized_scope,
    with the _single tier at :310. Disposition VERIFIED-DORMANT — wakes only on operator demand for
    the corrected-relabel retag.'
  council: 'no -- bounded follow-on; the C7/C10 trust reasoning is already recorded at the apply-arc
    codex rounds'
  heading: '### B-56 · Corrected-relabel repair for already-tagged audit history *(registered at the
    B-51/B-52/B-54 apply arc, 2026-07-18)*'
- id: B-57
  title: Direct-writer caller-supplied timestamp capture-order inversion
  status: closed
  pr: '#1256 (B-57 IMPL leg -- U-IS-11 ACs #14-#20 + #14-bis: the 12 C-IS-07 section 7.6.1 elections, the sentinel-docstring refresh, the audit_writer retry-loop removal, the restored_at pin and the two-process witness)'
  summary: 'Registered at the B-48 apply arc (IS v1.11 change-note; apply-PR codex round-13 made the
    row real): the C-IS-07 section 7.6 writer-owned fix covers the buffered/branch-DRAIN path only
    (the ratified narrow scope, pinned by the sibling-drain xfail). DIRECT appends keep caller-supplied
    timestamps with detect-then-refuse semantics -- two uncoordinated concurrent direct writers (e.g.
    the audit/cost interleave) can still capture timestamps outside the lock in inverted order and hit
    NonMonotonicTimestampError. SCOPE NOTE (round-28): the offloaded SUB-AGENT audit append path was
    carved OUT of this row into B-48 itself (its own concurrency exercises it -- IS section 7.6
    scope-widening); B-57 covers the remaining direct writers. The superseded defect record section 8.3 phrased the fix this broadly;
    the filing did not ratify it.'
  close_out: 'IF the direct-path inversion materializes in practice (a real NonMonotonicTimestampError
    from production direct writers): a narrow IS spec delta extending section 7.6''s writer-owned sampling
    to an opt-in direct-append mode (or per-writer serialization guidance) + U-IS-11 widening. Until then
    detect-then-refuse is the committed posture and this row is the queryable record. 2026-08-05 grounding
    re-check at HEAD 43d4dda5 — premise INTACT and the load-bearing lines read directly: state_ledger_write.py
    still carries _CLOCK_SKEW_TOLERANCE = timedelta(0) at :69, the NonMonotonicTimestampError refusal
    at :321-322, and writer-owned sampling strictly OPT-IN via the sentinel at :318 (WRITER_OWNED_TIMESTAMP
    defined at :83) -- so DIRECT appends keep caller-supplied semantics exactly as registered. Disposition
    VERIFIED-DORMANT **AS OF THAT PASS -- SUPERSEDED 2026-08-06, SEE BELOW**. NOT re-walked this pass
    -- the ~10 enumerated direct-writer call sites individually (the refusal mechanism and the opt-in
    gate are the load-bearing claims and both hold). 2026-08-06 -- DISPOSITION MOVES FROM VERIFIED-DORMANT
    TO **PARTIALLY MET**, on evidence from the B-93 build leg (`B-112`, PR #1241). This row''s close-out
    is conditioned on the direct-path inversion *materializing in practice*; `B-112` records a MECHANISM
    that raises its probability rather than an observed production failure -- the B-93 liveness deadline
    replaced a blocking `flock` with a POLLING retry, which does not preserve the kernel''s handoff fairness,
    so a direct writer whose timestamp was sampled BEFORE it queued can now be overtaken and refused.
    Reproduced 12 of 20 runs by out-of-family review against a focused two-process contention probe. THE
    DEFERRAL STILL HOLDS and the prescribed route is UNCHANGED (narrow IS spec delta extending section
    7.6 writer-owned sampling to an opt-in direct-append mode, or per-writer serialization guidance, +
    U-IS-11 widening) -- what changed is the PROBABILITY, not the remedy. `B-112` is the cause-and-evidence
    record and DEFERS the adoption question here; this row remains the remedy owner. ONE data point for
    pricing that route: `harness-is/tests/test_shadow_git_rollback.py`''s concurrent-append worker now
    opts into the sentinel on a DIRECT append surface -- adjudicated at the B-93 leg as permitted (section
    7.6 constrains the CONTRACT for every existing direct producer; a single explicit opt-in changes no
    producer''s semantics and sets no default), and recorded here as the first direct-surface use of the
    sentinel in the tree. 2026-08-07 -- GROUNDING INPUT FOR THE PENDING `B-57`/`B-112` FORK FILING, recorded
    here and NOT acted on (the fork is NOT filed at this PR). THE ENUMERATION AT THIS ROW''S 2026-07-27
    PASS UNDER-COUNTS THE DIRECT WRITERS BY FOUR. Four production sites construct a state-ledger entry
    with a CALLER-SAMPLED timestamp and appear in NO prior enumeration on this row, each re-resolved BY
    CONTENT at HEAD: `harness-cp/src/harness_cp/workload_binding_engine_class_selection.py:337` (`timestamp=datetime.now(UTC)`);
    `harness-cp/src/harness_cp/hitl_as_tool_call_rewriting.py:290` (same); `harness-cp/src/harness_cp/per_step_override_evaluator.py:489`
    (a `timestamp` PARAMETER, sampled by its caller at `:523` as `datetime.now(UTC)`); and `harness-cp/src/harness_cp/sibling_ledger_entry_composition.py:163`
    (a `timestamp` parameter on the sibling-composition path). TWO EXISTING CITES HAVE DRIFTED and are
    re-stamped: the two genuine `workflow_driver` direct appends now sit at `:6074` and `:6297` (the row
    previously cited `:6038` / `:6261`); the SUBSTANCE is unchanged -- both are still `timestamp=datetime.now(UTC)`
    on a `ctx.ledger_writer` append. THIS DOES NOT CHANGE THE ROW''S DISPOSITION -- still PARTIALLY MET,
    still deferred, remedy unchanged -- it changes the SIZE OF THE SURFACE the prescribed route must price,
    which is grounding the fork filing owes.
    2026-08-07 -- CLASS 2 FORK FILED at
    `.harness/class_2_fork_b57_direct_append_writer_owned_opt_in.md` (doc-only; status UNCHANGED at
    registered_finding and no `pr:` pointer set here -- both ride the ratification leg, per the B-88
    precedent). This row is the REMEDY OWNER; the filing prices the route this close-out already
    prescribes. READINGS: A (RECOMMENDED) -- a narrow IS delta authorizing PER-CALL-SITE opt-in to
    WRITER_OWNED_TIMESTAMP on direct surfaces, default unchanged for every non-electing producer,
    extending the EXISTING C-IS-07 section 7.6 (ZERO contract numbers, ZERO hash impact, ZERO CXA
    rows, ZERO new plan units) + a U-IS-11 AC widening + a per-site classification table; B -- HOLD
    until an observed production refusal. THE SINGLE OPERATOR GATE: whether authorizing a
    per-call-site opt-in to an ALREADY-UNCONDITIONAL writer-side mechanism (the sentinel check at
    state_ledger_write.py:316-320 fires for ANY caller that stamps it -- no surface discriminator
    exists, so the direct-path restriction is held by PROSE alone) is a spec act or needs
    ratification first. COUNCIL: NOT OWED -- PROBE-RESOLVED (B-112''s step-(3) trigger is not
    reached; the classification leaves no exposed caller stranded, so the C3/C11 tension does not
    arise). THREE GROUNDING CORRECTIONS RECORDED RATHER THAN ABSORBED: (i)
    sibling_ledger_entry_composition.py:163 is PRODUCTION-UNREACHABLE (emit_sibling_ledger_entry at
    cp_is_wiring.py:124 has ZERO non-test callers), so the exposed direct-writer surface is 11 of 12
    sites, not 12; (ii) audit_writer.py:701 /:751 and cost_attribution_f2_write.py:157 sample an
    INJECTED time_source, not datetime.now(UTC) at the site, so electing there OVERRIDES the
    test-determinism injection seam -- a real per-site cost the spec leg must resolve explicitly;
    (iii) _CLOCK_SKEW_TOLERANCE''s own comment (state_ledger_write.py:67-68) calls it
    ''configuration-supplied'' while it is a bare module constant with no configuration path at HEAD
    -- a pre-existing shipped-comment defect. ALSO RECORDED: the 2026-07-27 audit_writer
    resample-retry nuance on this row is ATTRIBUTED, NOT RE-DISCOVERED; the filing disagrees only
    about its WEIGHT (this row reads it as consistent with the committed posture, the filing reads
    it as circumstantial materialization evidence because the mitigation samples outside the lock
    and the interleave''s cost half has none) and marks that ground CONTESTED. 2026-08-07 -- RATIFIED: the operator selected READING A (per-call-site opt-in to WRITER_OWNED_TIMESTAMP on direct append surfaces; Reading B, HOLD, declined), recorded at the filing''s NEW section 11. THE PRESCRIBED ROUTE IS NOW APPLIED, NOT MERELY PRESCRIBED: IS spec v1.12 -> v1.13 extends the EXISTING C-IS-07 section 7.6 with a NEW section 7.6.1 (the per-call-site election; the eligibility rule -- elect only where the timestamp means WHEN APPENDED, never where it means WHEN THE EVENT HAPPENED; a negatively-demonstrable default-preservation property; an explicit does-NOT-authorize list), plus ONE residual-status paragraph inside section 7.6 recording the residual as DISCHARGED (the ''Surfaces that do NOT change'' and ''Registered residual'' paragraphs themselves PRESERVED VERBATIM), plus ONE cross-reference-only clause at the section 7.1 row-7 cell. IS plan v2.8 -> v2.9 amends U-IS-11 with ACs #14-#20 and carries the per-site classification table the contract routes to the plan. ZERO contract numbers, ZERO hash/canonicalization/migration impact, ZERO CXA rows, ZERO new plan units/nodes/edges/auxiliary types, ZERO code at this leg. STATUS TRANSIT registered_finding -> OPEN, and the reasoning stated rather than assumed: this row is no longer a mere surfaced finding (its decision is taken and its design substrate has landed), and it is not closed (the implementation is owed) -- the same enum reading the B-96 / B-88 rows applied when a ratified finding''s spec-and-plan legs land ahead of its impl leg. B-57 CLOSES AT THE IMPL LEG, not here. WHAT THE IMPL LEG OWES: the 10 ELECT conversions; the sentinel-docstring refresh at state_ledger_write.py:78-80 (AC #14-bis -- the OTHER prose
    carrier of the direct-path restriction section 7.6.1 replaces, named at the spec change-note and
    therefore owed rather than left to rediscovery); the 2 injection-caveat sites (audit_writer, cost_attribution_f2_write) resolved EXPLICITLY per site with rationale recorded; the audit_writer resample-retry disposition (it becomes unreachable at that site under ELECT -- take the simplification deliberately or record the retention); the shadow_git_rollback restored_at decoupling pin; a two-process contention witness under a PD-8 mutation probe; a NON-ELECTING negative witness proving the default did not move; an as_is_wiring event-time control; and a per-site table conformance check. COUNCIL: still NOT owed -- probe-resolved and RE-DETERMINED at the spec leg (B-112 step (3) is not reached: the one RETAIN site, as_is_wiring.py:129, carries genuine event time and is not an exposed caller that CANNOT adopt). A LIVE TRIGGER SURVIVES: if the impl leg judges EITHER injection-caveat site RETAIN, that is an exposed caller of exactly the shape step (3) tests for, and the C3 perp C11 determination MUST be re-run before any _CLOCK_SKEW_TOLERANCE change is authored (recorded at IS plan v2.9 section 0.6). TWO COUNT CLAIMS IN THE FILING CORRECTED AT THE SPEC LEG rather than carried, after re-resolving all fourteen classification rows by direct read at HEAD acfc1afa: its ''fourteen rows, twelve SITES'' does not reconcile with a table whose two folded companions (audit_writer :751, per_step_override_evaluator :523) are ALREADY folded into rows 12 and 9 -- by content the table is 14 ROWS; and its ''~8 conversions'' under-counts its own ELECT cell, which enumerates TEN payload-construction rows (10 ELECT + 2 injection-caveat + 1 RETAIN + 1 DEFER = 14). Neither correction changes a disposition, a reading, or any zero-cost line -- both change the SIZE of the surface the impl leg must convert. The three grounding corrections recorded above (the UNREACHABLE sibling site, the injected time_source seam, the stale _CLOCK_SKEW_TOLERANCE comment) all re-verified INTACT at acfc1afa. 2026-08-07 -- IMPL LEG LANDED; ROW CLOSES. The prescribed route is now BUILT, not merely applied: all TWELVE classified sites converted to the EXISTING WRITER_OWNED_TIMESTAMP sentinel (the ten ELECT rows 1-9/11 PLUS both injection-caveat rows 12-13, each resolved ELECT per AC #18 with rationale recorded in-code) -- so the classification table''s ''10 conversions committed, up to 12'' resolves at TWELVE. THE COUNCIL TRIGGER DID NOT FIRE: neither injection-caveat site was judged RETAIN, so no exposed caller is stranded, step (3) is still not reached and the C3 perp C11 dyad is still not owed. Row 12 (audit_writer) follows the sub_agent_dispatch B-48 precedent EXACTLY -- the time_source kwarg is RETAINED for API stability across its ~30 construction sites, only its TIMESTAMP ROLE is retired; three shipped determinism tests were UPDATED not deleted, and two of them would otherwise have gone VACUOUSLY GREEN (the injected clock is never invoked, so their assertions lived inside a function that stops being called -- a pass-on-absence liveness-discriminator loss caught and repaired). Row 13''s caveat was FALSIFIED as stated and the correction RECORDED rather than normalized: compose_cost_f2_entry_core''s time_source is a DEFAULTED parameter that NO caller -- production or test -- ever overrides, so electing there overrode no live injection and broke no determinism witness. AC #19: the bounded 5-attempt resample-retry loop is REMOVED, deliberately, with the removal witnessed -- and for a SECOND reason the AC did not state: its retry re-stamped the payload with self.time_source(), so a retry would have silently UN-ELECTED the site mid-append; the one refusal that survives election (section 7.6.1''s mixed-population limit) is not recoverable by resampling at all and now propagates loudly, exactly as the old loop did on exhaustion. AC #20: the restored_at decoupling is pinned by an explicit assertion plus an in-code comment. AC #14-bis: the state_ledger_write.py sentinel docstring is refreshed to the v1.13 contract. WITNESSES: a genuinely TWO-OS-PROCESS contention witness (real subprocess, not a thread and not a fork) staging the sampled-first/appends-second overtake DETERMINISTICALLY plus a 40-vs-40 simultaneous-hammer contention test; its RED half is a real test, not a hypothetical -- the same staging with the election reverted is REFUSED with NonMonotonicTimestampError, so B-112''s 12/20 repro becomes 20/20 in BOTH directions. The AC #15 negative default-preservation witness, the mixed-population coexistence witness, the AC #16 as_is_wiring event-time control, per-site conversion witnesses for all twelve rows, and a two-part AC #17 roster conformance check (exact per-module stamp counts PLUS an exhaustive which-modules-mention-the-sentinel pin) all land. SEVENTEEN PD-8 mutation probes were RUN, not asserted -- every one RED under reversion. GROUNDING NOTE, recorded rather than absorbed: cross_process_write_lock is NOT reentrant on its FILE-lock face within one process (only the per-directory face is refcounted), so a holder cannot append while holding it -- which is why the two-process witness stages the overtake via a child-side yield window instead of holding the lock across it. Rows 10 (DEFER, production-unreachable -- re-measured at this leg: 19 hits, exactly 3 non-test, all definition-or-docstring) and 14 (RETAIN, event time) are UNTOUCHED and witnessed unchanged. ZERO existing witnesses weakened; full suite green.'
  council: 'no -- bounded follow-on; the C3/C9 reasoning is recorded at the IS v1.11 change-note'
  heading: '### B-57 · Direct-writer caller-supplied timestamp capture-order inversion *(registered at
    the B-48 apply arc, 2026-07-19)*'
- id: B-58
  title: Pre-existing harness-cp function-level import of runtime escalation error types
  status: closed
  pr: '#1074'
  summary: 'Registered at the B-48 apply arc (codex round-27): workflow_driver.py:4737 imports three
    ValidatorEscalation* exception classes from harness_runtime.lifecycle.validator_escalation_composer
    at function level -- a pre-existing cross-package import in the OD->CP-canonical-direction
    workspace (the carrier-home-defect class: cross-axis exception types living in one package,
    consumed by another). Observed prior state, present at the merge base; the B-48 capacity-seam
    package-graph witness EXCLUDES it explicitly rather than silently passing over it.'
  close_out: 'IF a hygiene arc opens: re-home the three exception classes to harness-core (the U-CORE-03
    precedent) and drop the function-level import. Until then the import stands as documented prior
    state; nothing blocks on it.'
  council: 'no -- bounded carrier-home hygiene follow-on'
  heading: '### B-58 · Pre-existing harness-cp function-level import of runtime escalation error types
    *(registered at the B-48 apply arc, 2026-07-19)*'
- id: B-59
  filing: .harness/class_2_fork_b59_capacity_authority_across_bootstraps.md
  title: Capacity authority not preserved across sequential bootstrap invocations
  status: closed
  pr: '#1080, #1081'
  summary: 'CLOSED 2026-07-22 -- ratified option A (process-scoped capacity-authority singleton).
    Spec leg at PR #1080 (CP v1.103->v1.104 admission-guarantee span clarification + Runtime
    v1.103->v1.104 NEW §14.8.10.6 cross-bootstrap capacity-authority continuity + RT-FAIL-CAPACITY-
    AUTHORITY-BUDGET-SHRINK taxonomy row); impl leg at PR #1081 (U-RT-146 -- FrameLedger split out
    of SubAgentDispatchExecutor, adopted process-lifetime via adopt_or_create_process_capacity_ledger()
    at stage_5_loop_init.py; typed CapacityAuthorityBudgetShrinkError; 14 PD-8 mutation-probed tests;
    merge-gate test-witness BLOCK r1 fixed + re-verified APPROVE r2). Originally registered at the
    B-48 apply arc (codex round-4 [P1] "preserve one capacity authority across bootstrap runs").'
  close_out: 'DONE -- both architectural choices the fork left open resolved to option A (process-
    scoped singleton surviving across api.run() invocations, adopt-new-budget-carry-occupied
    reconciliation). See PR #1080/#1081 for the full mechanism.'
  council: 'no -- ratified via AskUserQuestion (option A), no cross-domain tension re-opened at apply'
  heading: '### B-59 · Capacity authority not preserved across sequential bootstrap invocations
    *(registered at the B-48 apply arc, 2026-07-19; CLOSED 2026-07-22)*'

- id: B-60
  title: Cancellation fence not checked at nonlinear-topology child entry points
  status: closed
  pr: '#1075'
  summary: 'Registered at the B-48 apply arc (codex round-10 [P1] "check cancellation before
    nonlinear topology dispatch"): when an offloaded child workflow uses PARALLELIZATION,
    ORCHESTRATOR_WORKERS, or HIERARCHICAL_DELEGATION, `_execute_workflow_body` returns into the
    topology-strategy handler before ever reaching the linear step loop''s `_dispatch_cancel_token.check()`
    (workflow_driver.py:4357-4359). If an ancestor timeout trips the token between fan-out phases (e.g.
    between warm-up Phase 1 and Phase 2, or between a HIERARCHICAL_DELEGATION recursion level), the
    child can continue scheduling later work past the fence grace at those branch/level boundaries --
    the linear-path check this arc added does not extend to the nonlinear dispatch entry points. Not a
    regression introduced by round 9/10''s admission-deferral or future-publication fixes; a pre-existing
    coverage gap in the round 4-8 cancellation-fence work, surfaced for the first time at round 10.'
  close_out: 'Needs equivalent `_dispatch_cancel_token.check()` calls at each nonlinear-topology
    strategy''s entry point (before `_execute_parallelization` / `_execute_orchestrator_workers` /
    the HIERARCHICAL_DELEGATION recursion re-entry begins dispatching) and likely at each fan-out
    phase boundary (post warm-up Phase 1, pre Phase 2) -- not just once at function entry, since the
    fence can trip mid-fan-out. Ground against CP spec v1.102 / Runtime spec v1.102 section 14.8.10.3''s
    fence-ack contract before building: determine whether ''check once at strategy entry'' or ''check at
    every phase boundary'' is the spec-intended granularity.'
  council: 'yes if opened -- C1 orchestration (fan-out phase structure) vs C9 reliability (fence
    completeness) is a nameable tension'
  heading: '### B-60 · Cancellation fence not checked at nonlinear-topology child entry points
    *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*'

- id: B-61
  title: Buffered fan-out ledger drain writes are not fenced
  status: closed
  pr: '#1072'
  summary: 'Registered at the B-48 apply arc (codex round-10 [P1] "fence each buffered ledger
    append"): `drain_branch_buffers`''s real `writer.append(...)` calls (workflow_driver.py:1922-1924)
    run without `effect_entry()` or `check()`. When a timed-out offloaded child is inside a nested
    fan-out, its cancel token can trip while branch tasks are still finishing; `_finish()` still
    reaches these physical ledger appends afterward, uncounted by `_effects_in_flight` and unblocked
    by the fence -- so `wait_ack()` can classify the timeout as `ACKED_CLEAN` even though F2 ledger
    entries actually begin writing after the trip. Not a regression introduced by round 9/10''s
    admission-deferral or future-publication fixes; a pre-existing coverage gap in the round 4-8
    cancellation-fence work (the effect_entry() guard was applied to the direct dispatch write paths,
    not the buffered-writer drain path), surfaced for the first time at round 10.'
  close_out: 'Needs the same `effect_entry()`/`check()` guard `drain_branch_buffers`''s sibling direct
    writes already carry (per the round 6/7 fixes), applied at each real `writer.append()` inside the
    drain loop. Ground against whether drain time is single-threaded-post-barrier (making a coarser
    single guard around the whole drain loop sufficient) or whether per-append granularity is required
    -- resolves the discriminator B-62 below shares.'
  council: 'no -- narrow, single-site guard-application fix once the drain-loop threading model is
    confirmed'
  heading: '### B-61 · Buffered fan-out ledger drain writes are not fenced
    *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*'

- id: B-62
  title: A single effect_entry() guard spans multiple independent sequential writes
  status: closed
  pr: '#1072'
  summary: 'Registered at the B-48 apply arc (codex round-10 [P1] "split the fence guard between
    sequential writes"): at sub_agent_dispatch.py:744-750 (and the durable-output/ledger pair in
    `_execute_workflow_body`), one `effect_entry()` guard wraps a SEQUENCE of independent effects
    (ledger append, then signing, then audit append). If the token trips while the FIRST write is
    executing, the guard remains entered for the whole block -- it permits the already-running
    operation to finish (correct), but ALSO permits the subsequent signing + audit append to begin
    without a fresh consultation of the token, since the guard was entered before the trip and never
    re-checked mid-block. Not a regression introduced by round 9/10''s admission-deferral or
    future-publication fixes; a pre-existing granularity gap in the round 4-8 cancellation-fence work,
    surfaced for the first time at round 10.'
  close_out: 'Needs each independent effect (ledger append / signing / audit append) wrapped in its
    OWN `effect_entry()` call rather than one guard spanning all three, so a trip between operations
    is caught before the next one begins. Ground against whether signing is itself effect-bearing
    (network call to the KMS backend per ADR-D8) or purely computational before deciding scope --
    shares the drain-loop threading-model discriminator with B-61.'
  council: 'no -- narrow, per-site guard-splitting fix once the signing effect-classification is
    confirmed'
  heading: '### B-62 · A single effect_entry() guard spans multiple independent sequential writes
    *(registered at the B-48 apply arc, codex round-10, 2026-07-19)*'

- id: B-63
  title: SigningBackend.verify() infra failures are not typed-availability at the concrete backend
  status: closed
  pr: '#1073'
  summary: 'Registered at the U-OD-55 impl arc (codex round-10 [P2] "translate verify outages into
    the OD availability type"): OD spec v1.34 section 21.2.2 row 7(b) requires backend availability
    errors during verification to surface through a dedicated typed availability error. The
    `AwsKmsSigningBackend.verify()` concrete backend (harness-cp, B-36/ADR-D8) catches only
    `KMSInvalidSignatureException` and returns False for it; any OTHER boto3/KMS failure (credential,
    network, throttling, service exception) propagates as a raw vendor exception, unclassified.
    Confirmed architecturally cross-axis at grounding: the `SigningBackend` Protocol
    (`harness_cp.f5_signing_key_resolution`) documents no availability-exception contract at all
    (checked directly, not inferred) -- and even if it did, the OD-owned
    `AuditVerificationBackendUnavailableError` cannot be raised by a CP-axis backend without CP
    importing FROM OD, which the axis-import direction (OD consumes CP; not the reverse) forbids. A
    blanket `except Exception` at the OD-axis verifier call site is not a substitute -- it would
    misclassify genuine backend defects as availability, which the same row 7(b) text forbids
    ("raises that are NOT the typed availability error and NOT AuditSignatureInvalid are DEFECTS and
    propagate unwrapped"). U-OD-55''s own per_family_audit_verification.py docstring previously
    claimed "a well-behaved backend implementation is expected to raise THIS type itself" -- corrected
    in-place at this same PR to state the gap accurately (no such contract exists yet) rather than
    assert an architecturally-impossible design. CONCRETE INSTANCE (codex round-12, same PR): when a
    resolver returns `AwsKmsSigningBackend` for a key_id absent from that instance''s OWN internal
    `_key_arns` mapping, `verify()` raises `UnknownSigningKeyIdError` (a `KeyError` subclass) BEFORE
    reaching KMS -- semantically identical to "the composition root failed to supply the key" (the
    exact category `AuditVerificationBackendUnavailableError`''s docstring already describes), but
    unreachable to translate at the OD-axis verifier without importing a name from ONE SPECIFIC
    backend''s implementation module (`harness_cp.aws_kms_signing_backend`, not the generic
    `SigningBackend` Protocol module) -- the same wrong-direction coupling this row already forecloses,
    confirmed by grep: `UnknownSigningKeyIdError` is defined and used ONLY in that one backend module,
    not on the Protocol. Same registered gap, not a second independent defect -- the close_out below
    already covers it (the shared contract, once it exists, is what any concrete backend''s "unknown
    key" condition would raise).'
  close_out: 'Needs a shared/CP-side (or harness-core) typed availability-exception contract that
    `SigningBackend` implementations (starting with `AwsKmsSigningBackend`) raise for infra failures
    distinct from a genuine signature mismatch, PLUS a translation catch at the OD-axis verifier call
    sites (`_verify_entry_signature` / `verify_cutover_record_signature`) that maps the CP/core type
    to `AuditVerificationBackendUnavailableError`. Cross-axis scope (touches harness-cp production
    code already shipped under B-36/PR #1031) -- out of U-OD-55''s OD-axis-only reach; does not block
    U-OD-55''s merge (the resolver-path availability typing -- unknown key_id -- is already correct
    and covers the row 7(b) example explicitly named in spec text; this residual is the real-backend
    infra-failure sub-case only).'
  council: 'no -- narrow, single cross-axis contract addition once a concrete infra-failure taxonomy
    is confirmed against the deployed backend(s)'
  heading: '### B-63 · SigningBackend.verify() infra failures are not typed-availability at the
    concrete backend *(registered at the U-OD-55 impl arc, codex round-10, 2026-07-20)*'

- id: B-64
  title: Greenfield cutover-record freshness probe and publication are not atomic cross-process
  status: closed
  pr: '#1071'
  summary: 'Registered at the U-RT-134 impl arc (codex round-9 [P1], the arc''s pre-committed
    terminal review round): `initialize_mtc_audit_signing_record`''s ledger-freshness probes (sidecar
    rows + IS `audit:` refs, each under its own short-lived cross_process_read_lock) release their
    locks BEFORE `_greenfield_sign_empty_record` publishes the record, so a concurrent audit append
    landing in the probe->publish window could in principle let an empty cutover record be minted
    over just-written history. Registered rather than fixed at the terminal round because (a) the
    practical attack paths are already closed: at MTC an appender only runs POST-bootstrap, and its
    own bootstrap either verified an existing record or minted one (this PR''s invariant), so a
    racing appender implies the record exists and the greenfield branch is never taken; simultaneous
    DUAL-greenfield bootstraps converge via the round-7 no-clobber `os.link` publication (the loser
    verifies the winner); and (b) the residual hardening -- holding the sidecar/ledger locks across
    probe + publication -- interacts with `harness_is.cross_process_ledger_lock` nesting semantics
    (a read-lock probe nested inside a held write lock on the same path) and needs its own
    deadlock-safety analysis, not a terminal-round patch.'
  close_out: 'Hold `cross_process_write_lock(sidecar_path)` (or an equivalent single lock scope)
    across ALL THREE probes (the persisted-row key scan shares the same probe->publish window --
    merge-gate concurrency lens, PR #1063) and the no-clobber publication in
    `harness_runtime.lifecycle.audit_signing_fail_closed_validation.initialize_mtc_audit_signing_record`,
    after confirming the IS lock implementation''s same-process nesting semantics (replace the inner
    read-lock acquisitions with lock-free reads under the held write lock if nesting is unsafe).
    Witness: a two-thread/two-process interleaving test that starts an append inside the
    probe->publish window and asserts the greenfield mint is refused. Does not block U-RT-134''s
    merge (terminal-round registration per the B-48 r10 precedent).'
  council: 'no -- single-module locking-scope hardening; needs a deadlock-safety probe of the IS
    lock nesting semantics, not a design decision'
  heading: '### B-64 · Greenfield cutover-record freshness probe and publication are not atomic
    cross-process *(registered at the U-RT-134 impl arc, codex round-9, 2026-07-20)*'
- id: B-65
  filing: .harness/class_2_fork_b65_post_effect_signing_carrier_cascade_disposition.md
  title: Post-effect signing carrier disposition at CP topology cascade handlers is unspecified
  status: closed
  pr: '1078 (impl leg); spec leg at PR #1077 (CP spec v1.103 §25.15 + Runtime spec v1.103
    §14.8.11)'
  summary: 'CLOSED 2026-07-22 (PR #1078) -- the RATIFIED option-A apply arc landed in full:
    name-match fence ahead of every branch-failure -> PAUSED/PARTIAL conversion across all 6
    topology handlers in workflow_driver.py + the encrypted/tenant-bound/ack-gated-retention
    ProtectedResultStore + result_ref threading through all 4 post-effect raise-site families
    (llm_dispatch/runtime_tool_dispatcher/sub_agent_dispatch/webhook_delivery_composer). 11
    out-of-family Codex rounds to terminal (round 11''s two findings closed the arc: one fixed
    via a collision-free tenant-scope encoding, one registered as B-68). Merge-gate round-1
    test-witness BLOCK (missing e2e coverage for 3 of 4 raise-site families) fixed +
    PD-8-probed, APPROVE at round 2. Both packages full-green throughout. Second facet (payload
    recoverability beyond the in-memory carrier) is DELIVERED by the same store -- it IS the
    resolution, not a separate follow-on.

    Original registration (2026-07-20, out-of-family codex round-2 [P1]) preserved below.
    Registered at the U-RT-136/U-CP-73 impl arc (out-of-family codex round-2 [P1]).
    Under `audit_signing_fail_closed=ON`, a `PostEffectAuditSigningError` raised at a fan-out
    branch reaches the CP workflow driver''s topology cascade handling as a generic branch
    failure: under `cascade_policy=pause` it converts to a resumable PAUSED result whose resume
    RE-DISPATCHES the same step (re-firing the already-completed PAID effect the carrier exists
    to protect), and under PROCEED-tier handling it reduces to PARTIAL without the `result_ref`.
    The v1.101 CP rider''s catch-ordering contract enumerates the per-attempt classifiers and
    forbids TRANSIENT_RETRY/candidate-advance/breaker-failure (all closed at this arc); it does
    NOT specify branch-vs-workflow terminality of the carrier under each cascade_policy, and the
    correct disposition (terminal-for-workflow vs terminal-for-branch-with-siblings-preserved vs
    a non-resumable pause variant) is a design decision over C-CP-25 SS25.10+ cascade semantics
    -- harness-cp additionally cannot import the runtime carrier, so any driver-side fence uses
    the established type-name-match pattern. Registered rather than fix-looped per the terminal
    register discipline (B-64/B-60..62 precedents); blast radius today is bounded to flag-ON
    fan-out topologies (MTC default-ON is the audience), and the carrier''s family membership
    keeps it out of retry/breaker everywhere the arc fenced.'
  close_out: 'Route the cascade disposition question to the CP spec (Class 2 fork or rider
    amendment over C-CP-25 SS25.10+/SS25.15): decide the carrier''s terminality per
    cascade_policy, then wire a name-match fence (`type(exc).__name__ ==
    "PostEffectAuditSigningError"`, the StepDispatchTimeoutError precedent) ahead of the
    branch-failure -> PAUSED/PARTIAL conversions in `workflow_driver.py` so the condition stays
    terminal and result-referenced. Witness: a fan-out run under flag-ON whose branch raises the
    carrier asserts (a) no resumable PAUSED snapshot is minted for that branch, (b) the surfaced
    failure carries the result_ref, (c) resume never re-dispatches the completed step. SECOND facet
    (codex round-3 [P1], same design surface): the payload is only reachable on the IN-MEMORY
    carrier -- once the driver stringifies to `RunResult.fail_class` the caller holds an opaque
    `result_ref` with no persisted lookup (the report log is digest-only by the round-2 redaction
    fix). The ratified acc-1b letter requires only report + caller-carried reference (delivered);
    payload RECOVERABILITY (a protected store keyed by result_ref, or carrying the payload
    through the CP/runtime result model) is the same unspecified beyond-the-boundary surface --
    resolve it in the same spec leg.'
  council: 'maybe -- C1 (orchestration/cascade semantics) vs C7/C8 (audit/compliance terminality)
    dyad if the spec leg opens; the mechanical fence wiring after ratification is single-axis'
  heading: '### B-65 · Post-effect signing carrier disposition at CP topology cascade handlers
    is unspecified *(registered at the U-RT-136/U-CP-73 impl arc, codex round-2, 2026-07-20)*'
- id: B-66
  title: B-60 fence-consult order at admission discrimination has no single-site witness
  status: registered_finding
  summary: 'Registered at the B-60 close (PR #1075 merge-gate test-witness lens, rounds 1-2,
    empirically verified by the lens): (1) the round-10 reorder (per-branch fence consult BEFORE
    the SubAgentDispatchCapacityError admission discrimination in _proceed_branch/_proceed_worker)
    has NO single-site behavioral discriminator -- the all-rejected+tripped outcome is pinned
    JOINTLY with the B-61 drain fence (the lens verified non-vacuity: reverting the order AND
    disabling the drain fence together fails the witness; either alone preserves the outcome);
    a protocol-bound all-rejected variant pauses PRE-branch for fix and mutation alike. (2) The
    _cancel_worker PRE-marker consult (and _cancel_branch:9568, its structurally-identical
    sibling -- row imprecision corrected 2026-07-27) is individually shadowed by its own post-marker sibling
    (disable-pre-alone leaves the b60 set green; the residual marker-present/effect-never-fired
    state equals the handled crash-between-marker-and-dispatch window). Both orders are
    strictly-earlier semantics, comment-documented at their sites.'
  close_out: 'IF a future edit reverts either order (the change-detector class a behavioral
    witness cannot pin): the in-code comments at the four sites are the guard, and this row is
    the queryable record. A runnable discriminator would have to be an implementation-order spy
    (monkeypatched call-order assertion) -- a change-detector, not a witness; build one only if
    a regression actually occurs. Until then the joint pinning + defense-in-depth layering is
    the accepted posture (merge-gate round-2 APPROVE, 2026-07-21). 2026-08-05 grounding re-check at
    HEAD 43d4dda5 — premise INTACT, all four order-bearing sites verified present with the correct
    enclosing functions, and ALL LINE CITES IN THIS ROW REFRESHED (uniform +36 drift from the
    unrelated B-107/U-CP-64 arc growing harness-cp/src/harness_cp/workflow_driver.py; the row and
    its prose previously read :9078/:9083, :13540/:13545, :9532 and :13945). At HEAD:
    _proceed_branch consults _consult_dispatch_fence() at :9114 BEFORE the isinstance(_admission,
    SubAgentDispatchCapacityError) discrimination at :9119; _proceed_worker :13576 precedes :13581;
    the _cancel_branch pre/post-marker pair is :9568 / :9583 and the _cancel_worker pair is :13981 /
    :13996. Order intact everywhere; posture unchanged. Disposition VERIFIED-DORMANT. NOT re-run
    this pass -- the 23 test_b60_* witnesses (read-only sweep, no test execution, so their green
    status is unconfirmed here).'
  council: no -- documentation-of-accepted-residual row; no design decision pending
  heading: '### B-66 · B-60 fence-consult order at admission discrimination has no single-site
    witness *(registered at the B-60 close, merge-gate test-witness lens, 2026-07-21)*'
- id: B-67
  title: Protected result store's TTL GC sweep has no shutdown-time hook — bootstrap half only
  status: closed
  pr: '1078 (round-4 fix, landed alongside B-65)'
  summary: 'CORRECTED at out-of-family Codex round 4: this row''s own premise ("this runtime
    has no graceful-teardown chain to hook") was FALSE — `shutdown(ctx)` at
    `harness_runtime/shutdown.py` IS that chain, invoked unconditionally in `run()`''s/
    `resume()`''s `finally` block regardless of workflow outcome (verified by direct read of
    `api.py` lines ~671/941). Fixed by wiring a new step 5b in `shutdown()`: a bounded,
    best-effort `ctx.protected_result_store.gc_sweep()` off the default executor via
    `asyncio.to_thread` (safe here specifically because shutdown only runs AFTER
    `execute_workflow` has already returned — no sync driver can be occupying the default pool
    concurrently, unlike the request-path raise sites this same round also fixed). The
    `stage_4_od.py` comment carrying the false premise was corrected in the same commit. Both
    halves (bootstrap reaps a PRIOR process''s abandoned entries; shutdown reaps THIS
    process''s own before exit) are now wired.'
  close_out: 'Closed by the fix described above (harness-runtime/src/harness_runtime/shutdown.py
    step 5b + stage_4_od.py comment correction), landing on impl/b65a-protected-result-store.'
  council: no -- corrective wiring fix once the false premise was caught by review
  heading: '### B-67 · Protected result store''s TTL GC sweep has no shutdown-time hook —
    bootstrap half only *(registered at the B-65-A impl arc, out-of-family Codex round 1 [P1],
    2026-07-22; CLOSED at round 4 correction, same date)*'
- id: B-68
  title: Protected result store's opportunistic GC can race a concurrent in-flight publication under a short TTL
  status: closed
  pr: "#1103"
  summary: 'Registered at the B-65-A impl arc (out-of-family Codex round 11 [P2]). Fixed across
    6 rounds of out-of-family Codex review on the B-68 impl branch
    (b68-protected-result-store-gc-race-mtime-authority): (1) `gc_sweep()`''s age authority
    switched from the pre-publish `_StoredEnvelope.written_at` field (captured before
    serialization/encryption/fsync -- the original gap) to the committed entry file''s own
    filesystem mtime, explicitly refreshed via `os.utime` + its own fsync AFTER the durable
    `os.link` commit -- closing the timing window the original finding described; (2) a
    process-wide `threading.Lock` registry keyed by the store root''s filesystem identity
    (`_root_locks`, module-level, not per-instance; keyed by `(st_dev, st_ino)` when the
    root exists, falling back to the resolved path string when it does not yet) now
    serializes `_publish_atomic`''s ENTIRE publish critical section (temp-file creation
    through the post-commit mtime-refresh fsync) against `gc_sweep`''s ENTIRE scan, closing
    same-process/same-root races including a co-resident concurrent-instance race the
    register''s original close_out did not anticipate, path-aliasing (`..`-relative spellings),
    and case-insensitive-filesystem aliasing (round 6, e.g. macOS APFS default volumes -- two
    differently-cased spellings of the same directory now share one lock); (3) `read()`
    degrades a missing entry to a typed `ProtectedStoreEntryNotFoundError(FileNotFoundError)`
    (the optional interim hardening the close_out named) instead of a raw `FileNotFoundError`.
    Each round''s fix is witnessed by a deterministic (non-flaky) mutation-probed test:
    reverting the fix makes the corresponding test fail; restoring it passes -- including a
    round-2 self-correction (merge-gate round-1 test-witness lens, PR #1103) where an
    earlier timing-based witness was found empirically ~80% reliable, not deterministic, and
    was rewritten to a purely Event-driven handshake. 10 new tests added; full
    `harness-runtime` + `harness-cp` suites green. Advisor-reviewed before close: round 5''s
    finding restates an ALREADY-DISCLOSED cross-PROCESS residual (see B-73) rather than a
    defect introduced by this fix, so this row closes on the in-process synchronization
    scope the original close_out described.'
  close_out: 'Closed by the fix described above
    (harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py +
    harness-runtime/tests/test_lifecycle_protected_result_store.py). The cross-PROCESS case
    -- two separate OS processes sharing one store root, each with its own in-memory
    `_root_locks` registry -- is NOT closed by an in-process `threading.Lock` and is split
    out to B-73 rather than silently folded into this closure; council: no.'
  council: no -- narrow TTL-dependent race with a documented reachability precondition; the full
    fix is a concurrency-control design decision, not an immediate judgment call
  heading: '### B-68 · Protected result store''s opportunistic GC can race a concurrent
    in-flight publication under a short TTL *(registered at the B-65-A impl arc, out-of-family
    Codex round 11 [P2], 2026-07-22; CLOSED 2026-07-25 -- in-process synchronization scope;
    cross-process residual split to B-73)*'
- id: B-73
  title: Protected result store's GC/publish synchronization is in-process only -- two co-resident
    OS processes sharing one store root are not mutually excluded
  status: closed
  pr: "#1120"
  summary: 'Split out of B-68 at its close (2026-07-25). CLOSED 2026-07-26 at PR #1120:
    `_cross_process_lock()` (an `fcntl.flock` on a dedicated `.cross_process.lock` file,
    disjoint from both `gc_sweep` glob patterns) is now composed INSIDE the existing
    in-process `self._publish_lock` at both `_publish_atomic` and `gc_sweep`''s
    candidate-selection phase -- a single, fixed nesting order everywhere, no lock-ordering
    hazard. Guarded on Windows (`_IS_WINDOWS = sys.platform == "win32"`) per C-STK-10,
    mirroring the same carve-out already shipped at `harness_is.cross_process_ledger_lock`;
    `fcntl` is imported lazily inside the lock method, never at module scope (an unguarded
    top-level import would have broken `import harness_runtime` entirely on Windows, since
    `harness_runtime.types` imports `ProtectedResultStore` unconditionally -- caught at
    out-of-family Codex round 1). 4 rounds of `just codex-review main` to clean convergence:
    round 1 (the Windows-import [P1] above, plus a [P2] where the write-once regression test
    did not isolate `_publish_atomic`''s own lock use -- a fresh child store''s opportunistic
    pre-write GC sweep already acquires the same lock, so the test passed even with
    `_publish_atomic`''s lock removed alone); round 2 (the new tests would spuriously fail on
    Windows, where the lock intentionally no-ops -- added `skipif`; the readiness marker was
    written before `flock` was ever attempted, leaving a scheduling window); round 3 (the same
    underlying concern persisted -- writing the marker "right before" the contending call
    still is not proof of being AT the `flock` attempt, since real serialization + Fernet
    encryption happen first -- fixed by wrapping `fcntl.flock` itself in each child script so
    the marker fires at the actual syscall attempt); round 4 clean. Each fix was
    mutation-probe-verified (revert the fix, confirm the corresponding regression test fails;
    restore, confirm green) before landing. `merge-gate` 3-lens review: concurrency + test-
    witness APPROVE at round 1; spec-conformance BLOCKED once (this ledger row was not
    updated in the same PR, diverging from the B-68 precedent) -- fixed by this same update.'
  close_out: 'Closed by the fix described above
    (harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py +
    harness-runtime/tests/test_lifecycle_protected_result_store.py). Witnessed with two
    genuine-subprocess regression tests (`subprocess`, not `multiprocessing` -- avoids both
    the fork-after-lock-held deadlock hazard and spawn''s pickle-by-module-name ambiguity in
    this monorepo''s several same-named `tests` packages) asserting cross-process blocking via
    `store._cross_process_lock()` held directly in the parent, rather than the originally-
    envisioned TTL-race-survival scenario -- a deliberate, reasoned deviation from the literal
    recipe above (lock-identity blocking is the more deterministic, non-flaky witness shape
    for this claim; matches this workspace''s own "races are non-deterministic -- assert
    lock-identity" discipline) rather than a silent gap.'
  council: no -- narrow TTL-dependent cross-process race with a documented, unchanged
    reachability precondition; the fix is a known primitive (fcntl.flock), not a judgment call
  heading: '### B-73 · Protected result store''s GC/publish synchronization is in-process only
    -- two co-resident OS processes sharing one store root are not mutually excluded *(split
    from B-68 at its close, out-of-family Codex round 5, 2026-07-25; CLOSED 2026-07-26 -- PR
    #1120)*'
- id: B-74
  title: Protected result store's mtime-based GC age authority can under-report publish time
    below a filesystem's timestamp granularity
  status: closed
  pr: '#1237'
  summary: 'Surfaced at the B-68 impl arc, out-of-family Codex round 7 [P2]. `_publish_atomic`''s
    `os.utime(entry_path, None)` refresh sets the entry''s mtime to the CURRENT system time, but
    the underlying filesystem is what actually STORES that value, at whatever timestamp resolution
    it supports -- on a coarse-granularity filesystem (e.g. legacy FAT32, or certain network filesystem
    configurations; NOT the modern POSIX filesystems -- ext4/APFS/NTFS -- this workspace''s committed
    local-development/self-hosted-server/managed-cloud deployment surfaces default to, which offer
    sub-millisecond resolution), the stored mtime can round DOWN by up to the filesystem''s own
    granularity (e.g. up to ~1 second on a 1-second-resolution filesystem). If the deployment-configured
    `ttl_seconds` is SHORTER than that rounding error, `gc_sweep()` can classify a just-published,
    genuinely-live entry as already expired and reclaim it -- even though the B-68 mtime-authority
    fix (round 1) is working exactly as designed; the filesystem itself cannot represent the precision
    the fix relies on. This is the SAME "operator configures a near-zero TTL" reachability floor
    B-68''s own original registration already named (roughly seven orders of magnitude below the
    factory 86400s default), now sharpened with a SECOND, filesystem-dependent dimension of that
    floor. Not council-eligible as a mechanical patch: codex''s own framing states the fix is
    either "reject TTLs below a guaranteed timestamp resolution" or "use an age authority that
    cannot predate publication by more than the TTL" -- both require picking a concrete resolution
    floor, which is FILESYSTEM-dependent and not knowable a priori without probing the actual
    deployment volume, making this a genuine design decision (what floor? how is it probed or
    configured? what''s the failure mode when unknown?) rather than a mechanical patch a fix round
    can safely author. CROSS-REFERENCE (advisor(), B-76 arc, 2026-07-26): likely ONE finding with
    B-77 -- both concern raw mtime as an untrustworthy age authority (this row: filesystem timestamp-granularity
    rounding; B-77: crash-window non-atomicity); ground as a single does-the-age-authority-stay-mtime
    question before building either in isolation. NARROWED (not closed) 2026-07-26 (PR #1123):
    the "likely ONE finding" framing above was directionally right but not literal -- B-77 and
    B-74 share raw mtime as an untrustworthy age authority, but B-77''s reorder fix only removes
    the DATA write+fsync duration from its own crash window, not the whole window (see B-77''s
    row -- a residual metadata-fsync+commit tail remains, reproduced by out-of-family Codex round
    9). This row''s own concern (filesystem storage-resolution rounding) is UNTOUCHED by that
    reorder either way -- it was never about ordering, only about what a coarse filesystem stores
    once a value is fsync''d, so it stays fully in scope, not reduced. Two same-arc attempts to
    dispose of this row were both wrong and reverted: PR #1123 round 1 tried "no realistic operator
    would configure a below-floor TTL" (rejected by Codex -- `protected_result_store_ttl_seconds`
    enforces only `gt=0.0`, no floor, and the env loader + factory pass an override through unchanged,
    so this is a SUPPORTED configuration, not an unreachable one); a later round considered adding
    that same TTL floor as a mechanical fix and advisor() rejected it too -- it is this row''s
    own close_out option ("reject TTLs below a guaranteed timestamp resolution"), already flagged
    as a genuine design decision, and the field is a spec''d deployment-configurable surface (Runtime
    SS14.8.11''s bounded-retention bullet, :4926 at v1.110 (re-refreshed 2026-08-05 at the B-96
    ratification leg; the prior :4883-at-v1.108 anchor and the council record''s :4909-at-v1.109
    prescription are both superseded) -- CITE CORRECTED 2026-07-31: SS14.8.11 carries NO AC numbering
    at all, so the prior "AC 7" resolved to nothing) Phase-7 impl discretion should not narrow
    unilaterally. Remains open, not dormant, not built this arc. NARROWED AGAIN 2026-07-30 (dated
    re-check against the landed B-77 fix, PR #1163): B-77''s first-observation grace covers this
    row''s reachable loss case but does NOT subsume the row. What is now covered: a granularity
    error up to ONE sweep interval -- the sweep that FIRST observes a coarse-stamped, genuinely-live
    entry can no longer reclaim it, which is the bootstrap-sweep-immediately-after-publication
    path (witnessed at `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight`,
    whose first assertion passes only because of the grace). What SURVIVES, and is now this row''s
    whole remaining scope: a rounding error EXCEEDING the gap between two consecutive sweeps.
    That gap is not TTL-bound -- a bootstrap sweep (`stage_4_od.py`) and an explicit or shutdown
    sweep (`shutdown.py` step 5b) can land milliseconds apart, well inside a 1-second filesystem
    granularity -- so a live entry can still be lost on such a volume. That exact residue is PINNED
    as an executable assertion in the same witness (its second `gc_sweep` reclaims the live entry,
    labelled REGISTERED RESIDUAL in the test body), so a future fix here must come back and update
    it rather than silently diverge. Not closed as subsumed-by-B-77; the close_out below (probe
    the deployment volume''s real resolution and either refuse a sub-resolution TTL typed or select
    a higher-resolution age signal) is unchanged and still the fix.'
  close_out: 'CLOSED at the B-96 impl leg (PR #1237, U-RT-150). The residue is DISSOLVED exactly as the disposition paragraph below prescribes: the age authority is no longer mtime ALONE, so a filesystem timestamp-granularity error can no longer reach the reclaim decision by itself, and the inter-sweep gap ceases to be a term in that decision at all. The WITNESS DISPOSITION this row prescribes was applied literally -- test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight is RE-PINNED AS A POSITIVE witness that the live entry SURVIVES BOTH SWEEPS, kept rather than deleted, and the two corrections this row warns the impl leg not to inherit as fact were not inherited. STATUS registered_finding -> closed. 2026-08-05 RATIFICATION-LEG DISPOSITION (B-96 Reading C / ceiling C-2): under the
    ratified C-2 form THIS ROW CLOSES, and closes in the shape its own close_out asks for -- `.harness/council-b96-grace-ceiling-2026-08-01.md`
    section 8, reached independently by all three voices and re-derived by that record''s adversarial
    pass against the live test. C-2 delivers this row''s SECOND stated option (an age authority
    that cannot predate publication by more than the TTL) STRUCTURALLY -- no volume probe, no
    floor, no knowledge of the filesystem granularity G required -- because the age authority
    stops being mtime-alone. The residue AS SCOPED (a rounding error EXCEEDING the gap between
    two consecutive sweeps) is DISSOLVED, not narrowed: the inter-sweep gap ceases to be a term
    in the reclaim decision at all. The advisor cross-reference this row carries (ground it as
    a single does-the-age-authority-stay-mtime question) is ANSWERED, and answered by C-2 and
    by nothing else on the table. WITNESS DISPOSITION at the B-96 impl leg: :1982 flips `== [entry_path.stem]`
    -> `== []` and is RE-PINNED as a positive witness that the live entry survives both sweeps,
    exactly as its own comment at :1978-:1981 instructs; :1975 and :1976 are UNTOUCHED. TWO CORRECTIONS
    THE IMPL LEG MUST NOT INHERIT AS FACT: (1) the Delta figure -- neither ''Delta ~ 1.0s'' nor
    ''Delta in [0.3, 0.9) guaranteed'' is right; Delta is the fractional part of wall-clock at
    the patched os.utime call, phase-guarded only at loop exit, so [0.3, 0.9) is overwhelmingly
    likely, NOT guaranteed. (2) The stale section 14.8.11 anchor lived in THIS row, not in B-96''s
    -- REFRESHED at this leg by direct read at HEAD 4815ebef: the bounded-retention bullet is
    :4926 at Runtime v1.110 (heading :4915); the council record''s own '':4909 at v1.109'' prescription
    was already stale. STATUS unchanged (registered_finding): this row flips to `closed` only
    when the B-96 impl leg merges. NO SEPARATE B-74-RESIDUE FILING IS OWED -- the question it
    would have carried is answered by the B-96 ratification. Design a TTL-floor validation or
    a probe-based age-authority selection that accounts for the deployment filesystem''s actual
    timestamp resolution -- e.g. probe the store root''s filesystem granularity at construction/first-write
    time (write a marker file, read back its mtime precision) and either refuse a TTL below that
    floor with a typed error, or fall back to a higher-resolution age signal (e.g. a companion
    sidecar file recording a monotonic sequence number or a separate high-resolution timestamp
    store) when the TTL is below the floor. Witness with a test that simulates a coarse-mtime
    filesystem (e.g. via a monkeypatched `os.stat`/`os.utime` rounding to whole seconds) and a
    TTL below that resolution, asserting the store either refuses the TTL or does not lose the
    live entry. 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT on every limb,
    by direct read: the publish-time refresh os.utime(entry_path, None) is at protected_result_store.py:581,
    and protected_result_store_ttl_seconds still carries NO FLOOR (Field(default=86400.0, gt=0.0,
    allow_inf_nan=False), harness-runtime/src/harness_runtime/types.py:1879), so a below-granularity
    TTL remains a SUPPORTED configuration -- the exact ground on which the no-realistic-operator
    dismissal was rejected. The narrowed residue is still pinned as an executable assertion labelled
    REGISTERED RESIDUAL (B-74) at harness-runtime/tests/test_lifecycle_protected_result_store.py:1943
    and :1978, inside test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight
    (:1924). Disposition FORK-SHAPED -- pick a concrete resolution floor, or move the age authority
    off raw mtime entirely. LINKAGE CORRECTED at the 2026-08-05 grounding pass, then SUPERSEDED
    BY THE ANSWER at the ratification leg the same day: the pre-ratification statement was ''this
    row is NOT deferred pending B-96 ratification ... and this row is independently actionable
    today'', which was TRUE WHILE B-96 WAS UNANSWERED. B-96 IS NOW ANSWERED (Reading C, ceiling
    C-2), and the shared root question -- does the age authority stay mtime -- is resolved NO.
    This row is therefore no longer independently actionable in the sense that sentence meant:
    its residue is DISSOLVED by C-2 and it CLOSES at the B-96 impl leg (see the disposition paragraph
    at the head of this close_out). What survives from the old sentence is only that the two rows
    were never in a formal blocking relation -- they shared a question, and that question now
    has an answer.'
  council: no -- narrow, filesystem-dependent TTL-precondition gap; the fix requires picking a
    concrete resolution floor which is a design decision, not an immediate judgment call
  heading: '### B-74 · Protected result store''s mtime-based GC age authority can under-report
    publish time below a filesystem''s timestamp granularity *(surfaced at the B-68 impl arc,
    out-of-family Codex round 7 [P2], 2026-07-25)* *(**CLOSED 2026-08-05 at the B-96 impl leg** -- residue DISSOLVED under ceiling form C-2, not narrowed)*'
- id: B-75
  title: Protected result store's gc_sweep candidate-selection scan can itself stall inside the
    publish lock under a pathological backlog or a hanging filesystem
  status: closed
  pr: '#1122'
  summary: 'CLOSED -- candidate ENUMERATION (`glob()`/`stat()` over `*.entry`/`.tmp-*`) now runs
    fully unlocked, producing PROVISIONAL candidates re-verified in a single brief combined lock
    acquisition before being trusted (safe because a `.tmp-*` filename is permanently bound to
    one creator whose `mkstemp` call itself runs inside the lock, so a candidate surviving to be
    re-observed under the sweep''s own lock hold is provably a genuine orphan, not an in-flight
    write -- closing the asymmetry advisor() flagged rather than scoping to `*.entry` only).
    Mutation-probe-verified regression tests added (a structural lock-held witness, plus a
    stalled-enumeration concurrent-write test).'
  heading: '### B-75 · Protected result store''s gc_sweep candidate-selection scan can itself
    stall inside the publish lock under a pathological backlog or a hanging filesystem *(surfaced
    at the B-68 impl arc, out-of-family Codex round 8 [P1], 2026-07-25; FIXED 2026-07-26 -- PR
    #1122, CLOSED)*'
- id: B-76
  title: Protected result store's lock-key registry can fail open on a transient stat() failure
    during root-identity resolution
  status: closed
  pr: '#1121'
  summary: 'CLOSED -- `_lock_for_root` no longer catches `OSError` from `resolved.stat()` and
    falls back to a resolved-path-STRING key; propagates instead, closing the transient-failure
    aliasing race. Mutation-probe-verified regression test added. Selected via advisor() as the
    cleanest of the 4 findings surfaced at the same B-68 arc round 8 (B-74/B-75/B-76/B-77, all in
    this file) -- a genuinely mechanical propagate-vs-fallback fix, no interaction with B-73''s
    cross-process flock, no reopening of the phase-1/phase-2 GC-sweep safety argument.'
  heading: '### B-76 · Protected result store''s lock-key registry can fail open on a transient
    stat() failure during root-identity resolution *(surfaced at the B-68 impl arc, out-of-family
    Codex round 8 [P2], 2026-07-25; FIXED 2026-07-26 -- PR #1121, CLOSED)*'
- id: B-77
  title: Protected result store's mtime refresh is not durable atomically with the publish
    commit, leaving a crash window where a recovered entry looks pre-aged
  status: closed
  pr: '#1163'
  summary: 'NARROWED TWICE, not closed, 2026-07-26 (PR #1123). This PR went through THREE wrong
    claims before landing here, all caught by out-of-family Codex: round 1 tried to classify
    B-77 as dormant via a "sub-millisecond os.link-to-os.utime window" argument (wrong -- the
    real pre-fix window was the FULL pre-link write+fsync duration, unbounded). Round 2 moved
    the SOLE mtime refresh from post-commit to pre-commit and claimed this "closes the window
    structurally" (wrong -- Codex round 9 [P1] reproduced a DIFFERENT, normal-path regression
    against `main`: with the stamp now BEFORE the metadata-fsync+`os.link`+directory-fsync tail
    instead of after, a successful uninterrupted write could return a reference an IMMEDIATE
    sweep reclaims, `ttl_seconds=0.02` + a 100ms-delayed directory fsync -- something that could
    never happen on `main`, where the stamp already ran after that tail). Round 3 (current):
    `_publish_atomic` now stamps TWICE -- once pre-commit (on the temp file, for crash recovery)
    AND once post-commit (on the entry, restoring `main`''s exact original normal-path
    behavior) -- rather than moving the single stamp. This is a strict improvement over `main`
    on both axes (normal path unchanged from `main`; crash-after-link path now recovers the
    pre-commit stamp instead of `main`''s raw write-time value) with no reopened ground. The
    residual crash window is now `os.link` -> the post-commit refresh completing -- the SAME
    shape B-77 originally named, starting from the fresher pre-commit baseline rather than
    write-time. Restoring the post-commit block also reintroduces a pre-existing, OUT-OF-SCOPE
    inconsistency the round-2 reorder had incidentally fixed: if the post-commit refresh''s own
    fsync fails, `write_once` reports `UnresolvableResultRef` even though `entry_path` is
    already durably linked and readable -- this matches `main`''s original semantics and is
    intentionally NOT fixed here (recording it so it is not later rediscovered as novel).
    Mutation-probe-verified regression tests added for BOTH the crash-recovery path (pre-commit
    stamp) and the normal-path regression Codex round 9 found (post-commit stamp); the residual
    (post-link-to-post-commit-refresh tail) is registered, not tested, since no fix for it has
    landed. merge-gate 3-lens review approved the round-2 diff; only the concurrency lens was
    re-run for round 3 (the durability-step change is inside the same lock its reasoning already
    covered) per advisor()-directed scope. CLOSED 2026-07-30 (PR #1163, branch
    b77-gc-first-observation-grace-20260730) -- the close_out''s own fix landed as a
    FIRST-OBSERVATION GRACE inside `gc_sweep`, with no fourth reordering of the two-stamp
    pipeline and no narrowing of `ttl_seconds`. The residual window this row named (a crash
    strictly between `os.link` and the post-commit refresh completing, leaving a live entry
    carrying the pre-commit stamp) is no longer reclaimable on sight: an entry is collected
    only if it was ALREADY observed past TTL at a PRIOR sweep of the same root. The
    OUT-OF-SCOPE inconsistency this row also recorded (a failing post-commit refresh fsync
    reporting `UnresolvableResultRef` for an entry already durably linked) is UNCHANGED and
    still not fixed -- it remains `main`''s original semantics, recorded here so it is not
    later rediscovered as novel.'
  close_out: 'THE B-77 RESIDUAL IS NOW CLOSED at the B-96 impl leg (PR #1237, U-RT-150): the sweep-COUNT grace this row landed is replaced by a DURABLE ELAPSED-TIME one, so the residual practical duration gap -- a short or immediately-failing run whose own shutdown sweep fires milliseconds after its bootstrap sweep -- no longer exists, and the persisted-sidecar reversal recorded below is now BUILT rather than merely ratified. This row status was already closed and does not move. 2026-08-05 REVERSAL RECORDED (so it is never later read as unratified drift): this
    row''s landed judgment ''no persisted sidecar'' is REVERSED by the ratified B-96 Reading C
    / ceiling C-2 form, which publishes a DEDICATED candidate-filename + timestamp sidecar (carrier
    (C-i), keyed over BOTH sweep classes per council condition #5) by temp-write + fsync + os.replace
    + directory fsync. The reversal is RATIFIED, not drift -- operator AskUserQuestion 2026-08-05
    on the B-96 fork, with the C-1/C-2 sub-decision resolved by the convening at `.harness/council-b96-grace-ceiling-2026-08-01.md`
    (PR #1183). THE B-77 RESIDUAL flips to `closed` when the B-96 IMPL leg merges; this row''s
    own status is already `closed` and does not move. CLOSED by the landed fix, 2026-07-30. MECHANISM:
    `gc_sweep` records the filenames it verified past TTL under its own lock and reclaims only
    those a PRIOR sweep of the same root already recorded (`ProtectedResultStore._observe_expired`
    + the module-level `_root_observed_expired` registry in `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py`).
    PERSISTENCE JUDGMENT: the observation record is module-wide, keyed by the root''s filesystem
    identity (`_root_identity_key`, extracted from `_lock_for_root` so both key the same way)
    -- NOT per-instance. Grounded against the real call graph: the composition root builds a FRESH
    `ProtectedResultStore` per `run()`/`resume()` bootstrap (`bootstrap/factories/protected_result_store_factory.py:78`
    <- `bootstrap/stage_4_od.py:123`, which sweeps immediately), so a per-instance record would
    hand EVERY bootstrap sweep a fresh grace and defer reclaim to shutdown indefinitely, weakening
    the spec''s bounded-retention term. A fresh PROCESS legitimately starts with an empty registry
    -- it cannot distinguish a genuine age from a crash-window artifact, so granting the grace
    is the safe direction, and that IS exactly the crash-recovery case. No new config surface,
    no new public API, no persisted sidecar. WITNESSES (all mutation-probed, in `harness-runtime/tests/test_lifecycle_protected_result_store.py`):
    `test_crash_between_link_and_post_commit_refresh_survives_a_fresh_bootstrap_sweep` (the close_out''s
    own recipe -- post-commit `os.utime` raises once, then a FRESH instance''s bootstrap-style
    sweep does not reclaim, and the next one does); `test_uninterrupted_write_survives_an_immediate_sweep_past_the_grace`
    (the PR #1123 round-2 normal-path regression class, swept TWICE so the grace cannot mask it);
    `test_genuinely_expired_entry_is_reclaimed_at_the_second_sweep_not_the_first` (liveness --
    the grace is one interval, not immortality); `test_coarse_filesystem_mtime_granularity_does_not_lose_a_live_entry_on_sight`
    (the B-74 re-assessment witness). Pre-existing single-sweep witnesses were updated to sweep
    twice at one pinned `now` (`_sweep_past_grace`) so their original TTL-classification discrimination
    is preserved rather than masked. One out-of-family `just codex-review-uncommitted` round:
    [P1] the coarse-mtime witness was wall-clock-PHASE dependent (~10% spurious failure within
    100ms of a second boundary) -- fixed by pinning the phase before the write; round 2 clean.'
  council: no -- the grace-period fix shape is a known pattern (first-observation gating), not
    an open design question; narrow implementation-discretion follow-on once picked up
  heading: '### B-77 · Protected result store''s mtime refresh is not durable atomically with
    the publish commit, leaving a crash window where a recovered entry looks pre-aged *(surfaced
    at the B-68 impl arc, out-of-family Codex round 8 [P2], 2026-07-25; CLOSED 2026-07-30 --
    first-observation grace)*'
- id: B-78
  title: A SUB_AGENT_DISPATCH step's own SUB_AGENT_BOUNDARY gate under EVALUATOR_OPTIMIZER or
    DECENTRALIZED_HANDOFF can exhibit the same pre-dispatch re-fire livelock B-72 property 6
    closes for the fan-out topologies, ungrounded by any reproduction
  status: closed
  summary: 'Split out of B-72''s spec leg at CP spec v1.108 SS1.2b (out-of-family Codex round 5,
    2026-07-25; round 7 [P2] flagged this exclusion as needing its own durable tracking, not
    just correction-history prose). Property 6 (v1.108 SS1) closes B-72 net-position item (1)
    ONLY for the PARALLELIZATION/ORCHESTRATOR_WORKERS/HIERARCHICAL_DELEGATION topologies -- the
    ones whose gate-owning-branch pause state is captured via the PeerFanOutResumeState/
    FanOutResumeState carrier family. A SUB_AGENT_DISPATCH step carrying its own
    SUB_AGENT_BOUNDARY placement under EVALUATOR_OPTIMIZER or DECENTRALIZED_HANDOFF is captured
    into a DIFFERENT carrier family instead (EvaluatorOptimizerResumeState/HandoffResumeState).
    GROUNDED 2026-07-26 (reproduction-first pass, real-stack integration tests against
    harness-runtime): the actual defect was WORSE than the registered livelock hypothesis -- a
    genuine `HITLPauseRequestedSignal` (a deliberate `BaseException`, not `Exception`) raised
    from `_dispatch_and_buffer`/the stage dispatcher inside `_execute_evaluator_optimizer` and
    `_execute_decentralized_handoff` was UNCAUGHT (no handler matched a bare `BaseException`
    subclass), crashing the run outright rather than pausing it; and separately, neither
    topology constructed a `HITLDeliveryCell` at its own resumed-step dispatch site at all, so
    even a caught pause could never deliver a resolved operator response on resume. CLOSED --
    IMPL LEG LANDED 2026-07-26 (PR #1117, branch b78-eo-dh-hitl-delivery-cell-impl-leg): mirrors
    the already-cleared LINEAR delivery-cell mechanism (CP spec v1.108 SS1 line 37) at both EO''s
    and DH''s own resumed-step dispatch sites -- a name-matched `except BaseException` catch
    (EO wraps it as `_EvaluatorOptimizerHITLPauseError`; DH catches inline) plus
    `HITLDeliveryCell` construction keyed on `resume_context.hitl_responses`/uniform-fallback
    eligibility, identical resolution shape to LINEAR/fan-out. No new CP-spec carrier field
    needed (EO/DH have no branch-ordinal ambiguity, unlike the fan-out topologies property 6 was
    built for) -- plain Phase-7 impl work, not Class 1. 5 rounds of out-of-family
    `just codex-review main` to convergence: round 1 (REJECT responses on resume must be
    terminal, not folded into a resumable pause) and round 2 (the round-1 terminal-carve-out was
    too broad, misclassifying a genuine non-HITL `rt_fail_class` as gate-terminal) and round 3
    (round 2''s narrowed carve-out was checked ahead of the `cascade_policy` branches in DH,
    wrongly forcing terminal FAILED even under `cascade_policy=PROCEED`''s documented
    PARTIAL-salvage disposition) were genuine regressions this arc''s own fixes introduced, each
    fixed and mutation-probe-verified. Round 4''s finding (an unanswered `HITL_PENDING` resume
    with no resolved response proceeds to dispatch rather than staying inert) is NOT an EO/DH-
    specific defect -- direct grep confirmed the identical unconditional-dispatch-after-
    delivery-cell-construction shape already exists at the already-shipped LINEAR resume_at site
    (`workflow_driver.py` ~4900-4990), relying on `RuntimeHITLGateComposer`''s own Step-0
    consume-or-fresh-fire logic (`hitl_gate_composer.py:1684-1688`) to re-escalate safely. This
    is the SAME root-cause gap B-79 already registers (missing HITL placement/material-diff
    validation before resume dispatch) -- folded into B-79''s scope rather than fixed here or
    registered as a duplicate row; see B-79''s amended summary.'
  pr: '#1117'
  close_out: 'CLOSED. Reproduction confirmed and exceeded the registered hypothesis (uncaught
    crash, not just livelock); fix landed mirroring the cleared LINEAR mechanism; the one residual
    finding (round 4) is B-79''s pre-existing gap reached via a new path, not a new defect --
    tracked there.'
  council: 'no -- narrow topology-specific livelock candidate with a clear grounding-first next
    step (reproduce before designing), mirroring B-72''s own precedent; not a judgment call yet'
  heading: '### B-78 · A `SUB_AGENT_DISPATCH` step''s own `SUB_AGENT_BOUNDARY` gate under
    `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` can exhibit the same pre-dispatch re-fire
    livelock `B-72` property 6 closes for the fan-out topologies, ungrounded by any
    reproduction *(split from B-72''s spec leg, out-of-family Codex round 5+7, 2026-07-25;
    GROUNDED + IMPL LEG LANDED 2026-07-26 — PR #1117, CLOSED)*'
- id: B-69
  title: resume_handle crash-recovery resume path cannot address individual paused children
    for multi-child HITL delivery
  status: closed
  pr: '#1166'
  summary: 'IMPL LEG LANDED (PR 1166) -- U-RT-148 (Runtime plan v2.55) + the U-CP-64 amendment
    (CP plan v2.47) + NEW U-OD-57 (OD plan v2.31, the delta OD spec v1.36 section 30.5.2 held for
    the impl leg; carrier option (b), a SIBLING payload type, selected). All 16 U-RT-148 ACs and
    all 10 U-CP-64 ACs met by EXECUTION; the A-prime ordering honored at COMMIT granularity (the
    fence lands before the accessor is reachable at any commit); W1/W2/W2-prime/W3/W4 all landed
    with W3 mutation-probed green. BOTH conjuncts of the closure criterion below are therefore
    satisfied. CLOSED. Residuals surfaced and REGISTERED rather than absorbed: B-100 (the
    empty-key carve-out does not reach the LINEAR/orchestrator fence carriers) and B-101
    (accessor-derived provenance is lost through base-typed Pydantic serialization). HISTORICAL
    (pre-impl-leg next-step framing, preserved): build U-RT-148 (Runtime plan v2.55) + the
    U-CP-64 amendment (CP plan v2.47) + a NEW, NOT-YET-NUMBERED OD unit carrying the OD spec
    v1.36 section 30.5.2 additive audit carrier for the PRE-BOOTSTRAP staleness refusal. THREE
    units, not two: the refusal is raised pre-bootstrap (no ResumeAttempt/ResumeOutcome exists
    yet) and PauseResumeAuditPayload is frozen with extra=forbid and no staleness-token field,
    so U-RT-148 AC #13(c) CANNOT execute until that OD-owned schema surface lands. An OD plan
    delta is owed at the impl leg under EITHER carrier option (additive field OR sibling payload
    type) because both mutate or extend an OD-owned schema and a Runtime unit cannot own that.
    DAG: U-RT-148 depends on U-CP-64 AND on the pending OD unit -- do not schedule U-RT-148 as
    ready once CP alone lands. SPEC LEG APPLIED 2026-07-30 (ratified option A prime,
    CO-REQUISITE / SEQUENCED): Runtime spec v1.106->v1.107 (NEW section 31 §14.14.9 accessor +
    closed four-variant projection DTO + five-member cause attribution + declared postures;
    NEW section 30 refusal-only staleness precondition + cause attribution on the EXISTING
    RT-FAIL-RESUME-HANDLE-UNKNOWN, no new peer class; the discharged :3274 scope-limit
    sentence replaced by a cross-reference; NEW section 14.14.8 append-only/never-truncated
    substrate invariant), CP spec v1.111->v1.112 (REQUIRED non-downgradable ResumeContext
    response-provenance carrier; NEW public projection-returning surface over the two private
    resume-tree walks; two registered scope-limit lifts), OD spec v1.35->v1.36 (NEW C-OD-30.5
    emission home), CXA v2.22->v2.23 (classification-only: NO new row, aggregate FROZEN at
    111, payload widening recorded). ORIGINAL FINDING: api.resume()''s resume_handle mode gives
    the caller no prior PauseSnapshot to read a paused child''s run_id from before resume_context
    must be constructed, so B-39''s hitl_responses keyed-map addressing is unreachable on that
    path. GENERALIZED 2026-07-24 at B-70 grounding (out-of-family Codex) -- the identical gap
    applies to effect-fence idempotency_keys, not just HITL child run_ids -- the accessor must
    expose BOTH identity kinds.'
  close_out: 'CLOSURE CRITERION, X-AL-2 CONJUNCTIVE FORM (verbatim, operator-ratified 2026-07-30):
    "B-69 closes iff (accessor landed) AND (precondition landed AND exercised by the W3
    mutation-probe); partial is non-closure. The spec surfaces and their impl units MAY land in
    separate merges within the arc, subject to one ordering constraint: the staleness precondition
    lands first or simultaneously -- never after. No interval exists in which an unfenced accessor
    is reachable on main." ENFORCEMENT IS A PRE-MERGE REVIEW OBLIGATION, NOT A VALIDATOR RULE --
    tools/forward_register.py --check validates that close_out is present and that a closed row
    cites a pr, but it does NOT parse the conjunction and cannot today refuse a premature closed
    flip. The reviewer of the closing PR MUST confirm BOTH conjuncts, with the W3 mutation-probe
    green, before this row is flipped closed. REGISTERED GAP (out-of-family review round 3 P1,
    2026-07-30): structured per-conjunct register fields plus matching validator support would
    make the conjunction mechanically enforceable; that is a tools/ change, out of scope for a
    doc-only spec leg, and is recorded here rather than silently assumed. WHY THE ACCESSOR MAY
    NOT SHIP ALONE: resume() re-derives the SNAPSHOT
    from the journal but never re-derives the operator''s RESPONSE MAP -- two different objects,
    only one is re-derived -- and CP spec v1.106 section 1.2 property 4''s sole-member carve-out
    PERMITS a uniform hitl_response to resolve the single unaddressed gate-owning branch of the
    live cycle, so a superseded read can land a response on a branch the operator never saw,
    reachable through the 1-to-1 single-location happy path. IMPL-ARC OBLIGATIONS: the council
    section 11 witnesses W1/W2(PRIMARY, 1-to-1 shape)/W2-prime/W3(the PD-8 mutation probe that is
    one half of this criterion)/W4(characterization; its correct disposition on placeholder
    replacement is DELETION or INVERSION, never repair) as Runtime plan v2.55 U-RT-148 AC #1-#16
    (SIXTEEN, recounted programmatically -- a closeout that checks only #1-#15 would skip the
    empty-fence-key and scalar-binding witnesses), where AC #15 is the source-shape
    sub-discriminator detect-then-refuse witness (the DTO variants span seven-plus source shapes
    with different field sets, so a per-variant optional step_kind would readmit an illegal
    state), AC #16 covers the empty-idempotency_key crash-reconstruction re-probe state plus the
    SCALAR-field staleness binding (a maps-only binding would leave the W2 path itself unfenced),
    and AC #13 splits success/failure emission;
    the checkable one-authority criterion carried verbatim on BOTH plan sides ("Runtime''s accessor
    contains no recursion over PauseSnapshot and reads no nested resume-carrier field"); and the
    FOLD-IN of the Class 3 cite-hygiene fix for THREE stale docstrings in
    pause_resume_protocol_types.py -- PauseSnapshot''s class docstring says "8-field pause-snapshot
    envelope" for a 15-field type, pause_reason''s says "5-class enum" for a SIX-member enum, and
    snapshot_hash''s docstring still describes a FOUR-element hash. Not cosmetic: a projection arc
    enumerating from these docstrings undercounts EFFECT_FENCE_AMBIGUOUS (the member carrying the
    2026-07-24 widening''s weight) and mis-states the hash coverage; the stale text caused two real
    miscounts inside the convening itself. FOUR findings routed OUT, not absorbed: B-97 (pause-journal
    tenant binding), B-98 (gate description absent from durable state), B-99 (summary_text reopening
    condition), plus the eval measurable (fence FALSE-MATCH rate, handed to C8 -- refusal rate alone
    would have shown a snapshot_hash fence as perfectly quiet, quiet because it never fired, which is
    indistinguishable from quiet because nothing was stale).'
  council: ran 2026-07-30, converged, record at .harness/council-b69-pause-state-accessor-2026-07-30.md
  heading: '### B-69 · `resume_handle` crash-recovery resume path cannot address individual
    paused children for multi-child HITL delivery *(registered at the B-39 spec-leg PR #1092,
    out-of-family Codex round 3 (branch-vs-main), 2026-07-23; council convened + spec leg applied
    2026-07-30)*'
- id: B-70
  filing: .harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md
  title: effect_fence_resolution_for's uniform-fallback semantics share B-39's cross-branch-misapplication
    hazard, unfixed in the already-shipped mechanism
  status: closed
  summary: The shipped ResumeContext.effect_fence_resolutions/effect_fence_resolution_for mechanism
    (B-39's own modeling precedent) has the identical uniform-fallback-to-every-unaddressed-branch
    gap B-39's CP spec v1.106 section 1.2 property 4 just closed for hitl_responses -- unfixed
    here, since it predates and is out of this arc's scope. GROUNDED 2026-07-24; spec leg LANDED
    2026-07-24 (CP spec v1.107 + plan v2.43). IMPL LEG LANDED 2026-07-25 (PR 1101) -- the
    resolver's tree-walk enumeration wired at LINEAR + ORCHESTRATOR + both fan-out consume
    sites, plus the LINEAR site's round-3 map-addressability amendment. CLOSED.
  close_out: 'GROUNDED 2026-07-24 (fork filed, .harness/class_2_fork_b70_effect_fence_resolution_uniform_fallback.md),
    resolving both open questions the round-7 close_out deferred: (1) NO container/gate-owning
    split is needed (unlike B-39''s HITL fix) -- EffectFencePausedBranchResumeState''s own
    docstring ("always tool-step in production") + Spec_Control_Plane_v1_106.md section 2 (REMOVED,
    "the effect-fence mechanism does not cross a recursion boundary the way HITL child-pause
    delivery does") both confirm a nested descendant''s own effect-fence pause propagates via
    the generic PausedChildBranchResumeState/SubAgentChildPausedError path, never via
    EffectFencePausedBranchResumeState -- every captured fence-pause entry is inherently
    gate-owning and already idempotency_key-unique across recursion depth, so the plain
    count-based-invariant branch of the round-7 conditional applies, not the property-5-mirror
    branch. (2) The fix DOES need its own CP spec property (a section 26.8.1 companion mirroring
    section 1.2 property 4''s safety+liveness shape, simpler -- no property-5 equivalent) before
    code, since restricting the auto-fallback changes observable resume behavior of an
    already-cleared mechanism -- impl-to-cleared-spec does not apply. Status flipped
    registered_finding -> design_substrate_gated accordingly. **SPEC LEG LANDED 2026-07-24**
    (operator ratified "open the spec-leg now" via AskUserQuestion) -- Spec_Control_Plane_v1_107.md
    NEW section 1 (the safety+liveness invariant, sharpened across 5 out-of-family review rounds
    from the fork''s section 3 draft -- round 1 added the previously-omitted ORCHESTRATOR carrier;
    round 2 fixed a gap where the LINEAR consume site never calls effect_fence_resolution_for at
    all and so was not actually gated by the original wording, but its own fix over-corrected;
    round 3 found round 2''s fix would permanently livelock any 2+-simultaneous-LINEAR-pause
    resume, corrected to genuine map-addressability via the LINEAR carrier''s already-existing
    idempotency_key field; round 4 added a resume_handle scope-limit note mirroring B-69; no
    property-5 equivalent still holds per the grounding above) + Implementation_Plan_Control_Plane_v2_43.md
    (ONE deferred coverage-matrix row, zero unit amendment -- mirrors how v2.42 section 5 deferred
    the parallel hitl_responses property 4/5 invariants) + 2 clearance markers. Status flipped
    design_substrate_gated -> open (spec-leg gate cleared; impl leg -- the resolver''s tree-walk
    enumeration of the unaddressed effect-fence-pause set, wired at the LINEAR + ORCHESTRATOR +
    two fan-out consume sites -- is the next buildable slice, NOT yet opened, per the B-33/B-39/B-59
    precedent). **IMPL LEG LANDED 2026-07-25** (PR #1101, merged after 2 merge-gate rounds --
    round 1 BLOCK on spec-conformance [_resolve_effect_fence_gated reimplemented
    effect_fence_resolution_for inline instead of calling it, fixed to genuinely call the
    existing method] + test-witness [sub_agent_dispatch.py''s forwarding of the new field had
    no assertion, fixed; PARALLELIZATION peer 2-unaddressed-locations safety witness added
    for symmetry with the ORCHESTRATOR_WORKERS one]; round 2 all-approve). 1767 harness-cp +
    3046 harness-runtime tests green. B-70 CLOSED.'
  pr: '#1101'
  council: no -- parity fix mirroring an already-ratified pattern (B-39's own property 4), not
    a fresh open design question; grounding confirmed no cross-domain tension.
  heading: '### B-70 · `effect_fence_resolution_for`''s uniform-fallback semantics share B-39''s
    cross-branch-misapplication hazard, unfixed in the already-shipped mechanism *(registered
    at the B-39 spec-leg PR #1092, out-of-family Codex round 3 (branch-vs-main), 2026-07-23;
    GROUNDED + fork filed 2026-07-24; SPEC LEG LANDED 2026-07-24 — CP spec v1.107 + plan v2.43;
    IMPL LEG LANDED 2026-07-25 — PR #1101, CLOSED)*'
- id: B-71
  title: Operator-facing HITL escalation requests carry no run-instance-distinct correlation
    identifier
  status: closed
  pr: "#1341"
  summary: HITLEscalationBrief and the HITL webhook payload derive their identifiers from a
    workflow_id-scoped action_id (compose_hitl_action_id), not run_id -- two peer branches sharing
    a child_workflow_id produce byte-identical escalation requests, making B-39's hitl_responses
    run_id-keyed addressing impossible to use in that exact scenario.
  close_out: 'IMPL LEG LANDED 2026-08-14 -- B-71 IS CLOSED. U-CP-102 + U-RT-155 co-landed in one arc per their mutual co-land pin (CP plan v2.53 section 0.4 / Runtime plan v2.63 section 0.4). CP side: HITLEscalationBrief.escalation_instance_id (section 25.2.Z); StepExecutionContext.pre_dispatch_escalation_basis + .pre_dispatch_escalation_instance_id (sections 25.20 / 25.21), the basis derived UNCONDITIONALLY at BOTH live fan-out composition sites -- the guarded derivation an earlier spec draft asserted would have left the mechanism INERT on the first-escalation path B-71 exists to fix; PreDispatchGateOwningBranchResumeState.escalation_instance_id (section 26.9) written from the pause signal''s own brief at all four disposition catch sites AND preserved verbatim on the section 25.19 warm-up carried-forward path; the byte-compat strip mirroring hitl_gate_config_hash so a pre-upgrade durable snapshot re-hashes to its ORIGINAL value; PreDispatchUniformFallbackOnlyLocation.escalation_instance_id (section 2.2.A) with a model_serializer that OMITS the key when None rather than emitting null; the validator trust seam at workflow_driver.py OVERWRITING a validator-supplied token (never merely ignoring it) with a GUARDED-LOGGER diagnostic (never a span attribute -- a fourth attribute would extend a CLOSED three-attribute OD namespace; and never warnings.warn -- that raises under -W error); and a one-way ingress diagnostic, sited after the drain gate and snapshot validation, derived from the WHOLE tree via walk_pause_tree, keyed by TOKEN (branch_index is snapshot-local), and guarded at BOTH collection and emission so no diagnostic-sink or projection failure can ever replace a real terminal outcome. Runtime side: resolve_escalation_instance_id implementing the section 0.4.3 THREE-ARM read order (persisted echo wins VERBATIM and is never compared against a recompute); the token folded INSIDE compose_hitl_action_id as a colon-separated SUFFIX, reaching ALL THREE live invocations (hitl_gate_composer.py webhook key, _compose_and_persist_audit''s CP audit action_id + F2 ledger key, and the hitl.invocation.audit_ledger_entry_id span attribute) -- widening only the webhook call would have left the defect witness operating on an unwidened audit key; and the four additive payload_body keys projected through project_brief_to_payload, including the BARE token that makes the webhook equality-matchable against the pause-view projection. 44 witnesses across two records (harness-cp/tests/test_b71_escalation_correlation_carriers_u_cp_102.py, harness-runtime/tests/test_b71_escalation_token_mint_and_fold_u_rt_155.py) carrying 24 mutation-probe annotations, verified by 24 distinct mutations in an executed driver, ALL PINNED; full suite 7922 passed / 0 failed. SEVEN out-of-family rounds plus a 3-lens merge gate were absorbed AFTER the first green build, and the count above is the post-round-7 recount, not the round-0 figure. Rounds 2-7 found ELEVEN P2s: the ingress diagnostic was unreachable via the public api.resume path; resolvability was typed str and admitted a second classification authority; a mandated AC-14 probe annotation was missing; the diagnostic could ABORT a resume three different ways (warnings-as-errors, a raising log handler, and a raising tree walk on version-skew step_kind); it preempted the drain gate and snapshot validation; it read only one level of the pause tree; it keyed by the snapshot-LOCAL branch_index so an outer and a nested owner at the same ordinal collided; and it minted a fourth attribute onto a CLOSED three-attribute OD span namespace. Rounds 6-7 were defects this arc''s OWN round-5 fix introduced, which is the expected late-round shape. Three of my own witnesses were vacuous and were fixed with them (a nested case that reused one snapshot so the collision was invisible; a mapping built in-test so an ordinal-keyed DRIVER was undetectable; a tamper on a field that is not hash-covered). Process note worth carrying forward: the first probe run reported 17/17 NOT PINNED with an empty failure list -- the driver''s anchored FAILED regex was defeated by pytest ANSI colour codes, so a fully-working probe set read as a fully-broken one; the driver now strips ANSI and cross-checks the pass/fail TALLY, refusing to report clean when it cannot attribute a red. Residual, named not hidden: several probes redden collateral witnesses as well as their own -- unavoidable where one mint feeds four consumers (blinding the fold necessarily breaks every token-dependent criterion), so the plans'' "and no other" clause holds only for the surgical probes; B-165 (the duplicate same-position placement question) is UNCHANGED and still open -- this leg does not touch the ratified identity basis. == SUPERSEDED HISTORY FOLLOWS (the SPEC LEG paragraph below authorised this repair; it did not close it). == SPEC LEG LANDED 2026-08-13 -- READ THIS FIRST; the paragraph beginning "SPEC LEG SCOPED, NOT LANDED" below is SUPERSEDED HISTORY, retained for its findings and NOT a live instruction. Do NOT open a new spec leg: all six artifacts are authored and merged (CP spec v1.119 with FIVE carrier amendments; Runtime spec v1.121 with THREE sites; CP plan v2.53 U-CP-102 at 15 ACs / 8 probes; Runtime plan v2.63 U-RT-155 at 11 ACs / 6 probes; clearance markers for all four). NINE out-of-family review rounds absorbed. THE REMAINING WORK ON THIS ROW IS THE IMPL LEG (U-CP-102 + U-RT-155, co-land pinned), and that is what closes it -- a spec leg authorizes a repair, it does not close a defect. The full landed detail is at the SPEC LEG LANDED 2026-08-13 paragraph further down. SUPERSEDED HISTORY FOLLOWS. == SPEC LEG SCOPED, NOT LANDED (2026-08-12) -- superseded 2026-08-13. A full spec-leg draft
    was authored and taken through FOUR out-of-family review rounds (14 findings, all absorbed) on
    branch `b71-spec-leg-v1-119` / DRAFT PR #1325. It is NOT merged, deliberately: each round kept
    opening new STRUCTURAL surface rather than converging, and round 3 caught a REGRESSION the
    draft would have landed (it composed over v1.18 while v1.19 had already widened
    fail_detail_hash, plus a §25.2.Y label collision and the wrong carrier shape). Landing an
    under-specified canonical contract is worse than landing nothing.
    THE LEG''S TRUE SHAPE, now known and the durable output of that arc -- SIX artifacts, not one:
    (1) CP spec delta with FOUR carrier amendments, not one -- HITLEscalationBrief.escalation_instance_id;
    the pre-hash basis carrier on StepExecutionContext (the mint site has NO run identity without it);
    the persisted echo on the per-branch pre-dispatch resume state; AND a read path carrying the
    POST-hash echo back to the composer (without it §0.4.2''s persisted-value-wins rule is
    unsatisfiable). (2) RUNTIME spec delta -- Spec_Harness_Runtime_v1.md §14.8.8.1 step 2 still
    specifies the TWO-argument compose_hitl_action_id and says the driver does not extract pause
    state from the signal; both contradict the design. (3) CP implementation-plan delta (units,
    files, deps, acceptance). (4) Runtime implementation-plan delta. (5) Clearance markers per
    amended spec. (6) THEN the impl leg.
    ALSO SETTLED BY THE DRAFT (carry forward, do not re-derive): the digest must be PINNED (an
    idempotency-key formula is a contract surface, not discretion) -- sha256 over a
    domain-separated canonical string, lowercase hex, placement''s enum VALUE; the leak bar needs
    ONE scoped carve-out for the ordinal in prose on branch_context (else it contradicts the
    branch_context requirement outright); the ingress DIAGNOSIS must be ADVISORY until its typed
    carrier lands, since ResumeResult/RunResult are closed schemas; and the palette-suppression
    binding stays WITHDRAWN. FORMER CURRENT STATE (v6, 2026-08-12) -- READ THIS FIRST; everything after the VERSION
    HISTORY marker below is an append-log kept for audit, NOT instructions. ALL FIVE
    preconditions are CLOSED and THE SPEC LEG IS AUTHORIZABLE. No council is owed, no gate
    remains, nothing is blocked. Design record:
    .harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md -- read s5 for the
    residual table (8 items, none of them gates) and the three council conditions (sequencing,
    scope/B-158, and the branch_context leak bar now extended to the tracing channel). Identity
    basis = (B), the run-scoped internal identity + placement; resolvability carries the resolution
    CHANNEL, never the outcome; the D-2 palette-suppression binding is WITHDRAWN (D-2 keeps a
    recorded residual); the entry_version window is scoped TWO-MODE (durable=True: the harness
    journals; durable=False default: the CALLER persists RunResult.pause_snapshot -- a shipped,
    tested capability, so the guarantee there is conditional on caller behaviour the harness
    cannot enforce). NEXT STEP: open the spec leg. Do NOT re-open preconditions 1-5 and do NOT
    re-run the council. ===== VERSION HISTORY (audit only, superseded by the block above) =====
    DESIGN RECORD v1 LANDED 2026-08-12 (council arc; .harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md) --
    the row''s standing instruction (ground the branch-distinct-identity design BEFORE drafting
    spec text) is DISCHARGED, and the spec leg is NOT yet authorizable. Five voices (C1+C10
    primaries; C9/C5/C11 consultants) reconciled to internal zero; three decorrelated reviewers
    (in-family adversarial, transcript-aware advisor, out-of-family codex) then produced a docket
    the council had not: (a) THREE-WAY CONVERGENT -- the reverse-thread carrier keyed by bare
    branch_index is not tree-wide unique; (b) ORCHESTRATOR ESCALATION -- the DIGEST BASIS itself
    has the same defect, since compose_branch_child_context inherits parent_idempotency_key AND
    step_index verbatim (workflow_driver_types.py:586-650), so nested branch 0 and root branch 0
    at one placement compose the SAME triple, recreating B-71''s defect inside the fix -- the
    council''s [HIGH] uniqueness rating is WITHDRAWN and the basis is an OPEN FORK (candidate A
    (parent_action_id, branch_index, placement) vs candidate B the hashed tree-wide internal
    identity; each with a stated empirical question); (c) CODEX-ONLY -- a static
    held-for-sole-resolution stamp is a LIVENESS FAILURE, because a SOLE pre-dispatch owner IS
    answerable (workflow_driver.py:2895-2897 `if len(unaddressed) == 1: return unaddressed[0]`),
    so the design would tell the operator not to reply to the one request whose reply resolves the
    run; (d) the entry_version crash window is NOT closed by registering the wider defect; (e) the
    C7 observability disposition two voices produced was dropped in reconciliation. SETTLED and
    unchanged: the opaque one-field token, mint authority, persist-once, one-family widening at
    compose_hitl_action_id, no-ingress-keys-on-the-webhook, the operator surface, and the 2+-
    concurrent N-distinct shape. FIVE HARD PRECONDITIONS on the spec leg are enumerated at the
    design record section 5 -- the first is to EXECUTE the nested-fan-out collision witness (the
    witness file already exists at
    harness-cp/tests/test_workflow_driver_hitl_uniform_fallback_property4.py). Row stays
    registered_finding: the design is grounded, the basis is not yet chosen. --- Registration
    record below --- DOC-ONLY GROUNDING PASS 2026-07-27 (no spec/plan change landed -- a drafted fix
    was reverted pre-merge). A run_id-threading fix was authored across both CP and Runtime
    axes (StepExecutionContext + HITLEscalationBrief gain run_id; webhook idempotency-key +
    payload incorporate it) and taken through 5 rounds of out-of-family just codex-review-uncommitted,
    each round catching a genuine defect (Optional-vs-required breakage, missing None-guard,
    wrong exception classification, wrong unit ownership). Round 5 found the fix''s premise
    itself is false: RuntimeHITLGateComposer fires _escalate_to_secondary_channel INSIDE the
    PARENT''s dispatch of a SUB_AGENT_DISPATCH step, BEFORE RuntimeSubAgentDispatcher mints any
    child run_id -- execute_workflow''s run_id is per-PARENT-RUN, not per-branch, so every peer
    branch of one fan-out shares the identical value. A `...:{run_id}` key therefore does NOT
    disambiguate B-71''s actual collision; advisor() confirmed this and directed a full revert
    rather than a 6th patch (5 consecutive rounds each fixing the wrong layer is the non-convergence
    signal). This is the SAME pre-dispatch-identity problem B-72 CP spec v1.108 property 6
    already owns (a branch_index-derived tree-wide-unique INTERNAL identity, deliberately NOT
    hitl_responses/run_id-addressable, per v1.108 Section 1.1(b): "no child_run_id exists to
    key by"). The real fix needs a branch-distinct identity co-designed with B-72''s mechanism
    -- not a mechanical run_id field-add -- and it touches an operator-facing/webhook-EXTERNAL
    payload, which is a new design decision. This is the THIRD falsified premise this row has
    carried (registration-time "run_id already accessible", round-1 "required field is safe",
    round-5 "run_id disambiguates peers") -- next attempt must ground the branch-distinct-identity
    design BEFORE drafting spec text. See .harness/post-phase-8-forward-register.md B-71 for
    full detail. 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT, re-confirmed by
    direct read of both carriers: HITLEscalationBrief
    (harness-cp/src/harness_cp/validator_framework_types.py:134-151) declares exactly
    parent_step_id, parent_action_id, fail_class, fail_detail_hash, escalation_reason and
    proposed_response_palette -- NO run_id -- and compose_hitl_action_id
    (harness-runtime/src/harness_runtime/lifecycle/hitl_gate_composer.py:424-436; the prose row
    carried :425-437) still derives its identifier from that same workflow_id-scoped
    parent_action_id, so two peer branches sharing a child_workflow_id remain byte-identical on
    every observable field. Disposition FORK-SHAPED -- do NOT attempt a build before the convening
    co-designed with B-72 CP spec v1.108 property 6. DESIGN RECORD v2 2026-08-12 (same DELIVERABLE.md, amended): preconditions 1+2 CLOSED on executed evidence, 4 BOUNDED, 3+5 still OPEN. The witness harness-runtime/tests/test_b71_escalation_identity_basis_collision_witness.py (13 green, mutation-probed four ways) composes every candidate basis from the real production composers over the realizable collision tree (two peer workers dispatching the SAME child_workflow_id, the shape compose_child_run_id_seed''s own docstring names as live). RESULT: candidate (A) is FALSIFIED -- _parallelization_fanout_action_id takes workflow_id and nothing else, so two sibling child runs compose the identical triple and the collision propagates through the real compose_hitl_action_id into the webhook Idempotency-Key, the CP audit action_id and the F2 ledger key at once (orchestrator_action_id is run-blind the same way, so it is not PARALLELIZATION-local). The v1 ORCHESTRATOR ESCALATION is CORRECTED: verbatim inheritance of parent_idempotency_key is within-run, where branch_index still separates the peers, and nested fan-out is cross-run -- the council''s (parent_idempotency_key, branch_index, placement) triple survives uniqueness but loses on precondition 4: INSIDE the mint-to-persist recompute window, with the child run_id recovered, an entry_version bump rotates (C) where (B) reproduces. (On the ordinary resume path the persisted echo is read and nothing is recomputed, so no basis rotates there -- an earlier v2 draft overreached here and codex round 3 corrected it.) BASIS RESOLVED = (B), the run-scoped internal identity + placement; its in-scope objection is answered by one additive StepExecutionContext field (the driver already composes this identity at workflow_driver.py:8346/:12670). Spec leg still NOT authorizable. DESIGN RECORD v3 2026-08-12: precondition 3 also CLOSED (witness harness-cp/tests/test_b71_resolvability_cannot_be_a_static_stamp.py, 6 tests; mutation-probed -- making the resolver ignore hitl_responses turns 3 of the 6 RED). resolvability carries the resolution CHANNEL, drawn from the EXISTING closed PauseLocationVariant (CP spec v1.112 s2.1) -- never the outcome. The held-for-sole-resolution stamp is retired on a WITNESSED flip: holding one pre-dispatch branch and its tree fixed and varying only the ResumeContext, it goes from not-eligible (2 unaddressed gate-owners) to eligible (sole) -- ELIGIBILITY (nomination), not delivery: the driver additionally needs a uniform hitl_response (a CITED code read at workflow_driver.py:8350/:12674, not executed here), a SECOND mint-time-unknowable input -- so a mint-time stamp would be a false negative exactly when the operator most needs the truth. v1''s own recommendation is FALSIFIED in passing: the pause view cannot report live status -- project_pause_locations takes the snapshot alone (WITNESSED), AND read_paused_workflow_state is ratified NOT a liveness claim (CITED not executed -- a Runtime round-trip is owed) (Runtime v1.110 s14.14.9.1 / B-104 Reading D), so a resolved pause is byte-indistinguishable from an outstanding one. resolvability_note therefore states the sole-member RULE and promises no readout. NEW sub-fork (live per-location eligibility in the view) filed to B-155, which already owns this population. D-2 BINDING WITHDRAWN (codex rounds 8+14; the round-8 answer was REVERSED on evidence): a channel-only field is time-INVARIANT and D-2''s harm is time-VARYING (the palette over-promises only in the 2+ state, and is honest when sole), so resolvability CANNOT disarm D-2. The round-8 keyed-vs-uniform split fails because the webhook carries NO ingress keys at all -- the palette there is not a keyed reply affordance but the admissible response set, and a sole pre-dispatch owner genuinely CONSUMES the uniform response (workflow_driver.py:8346-8350; witnessed by test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py:576-590, 1 POST, no re-escalation). Suppressing in both states would hide a VALID action when sole -- the same liveness failure precondition 3 closes. DISPOSITION: keep the palette; the disarm is INFORMATIONAL (resolvability_note carries the uniform routing + sole-member condition); D-2 is NOT fully absorbed and keeps a recorded residual (in the 2+ state the palette still implies an action the resolver refuses), closable only by a time-varying signal this design forbids -- the same dependency B-155 carries. CLOSURE CHALLENGED (codex round 16) AND HELD: the rebuttal was that a partial resume would re-enter the composer and mint a FRESH escalation describing the new sole-owner state, making the stamp''s error transient. It fails, and the decisive case is witnessed on the REAL fan-out stack: test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py is exactly this population (PARALLELIZATION branch with its own SUB_AGENT_BOUNDARY gate, real dispatcher registry + real RuntimeHITLGateComposer) and its core assertion at :576-590 shows the branch CONSUMES a supplied response -- 1 POST, NO re-escalation. So: an operator who OBEYS the stamp supplies nothing, nothing resumes, no corrected stamp is ever minted, and the run stays parked BECAUSE of the stamp; an operator who IGNORES it resolves the branch and no fresh escalation occurs. NARROWED at codex round 17, which supplied a THIRD path the first draft missed: the operator may obey the stamp AND answer the addressable peer via hitl_responses (the stamp says nothing about the peer), in which case a resume DOES occur, the child consumes its keyed response, and the now-eligible pre-dispatch branch gets HITLDeliveryCell(None) and may re-escalate. That path is UNRUN, so the stamp is NOT provably a PERMANENT false negative. What survives and still retires it: the stamp is a false negative AT THE MOMENT THE OPERATOR READS IT (witnessed -- eligibility flips and a mint-time value cannot track it); a later corrective re-escalation would MITIGATE that, not remove it, and arrives only after the operator was already told not to act. Precondition 3''s ask is that the field CANNOT ASSERT a false negative -- a static stamp asserts one, the channel does not. OWED: the mixed keyed-peer / no-uniform-response fan-out round-trip (same one B-155 carries); it would tell us how BAD the stamp is, not whether the channel is the right shape. DESIGN RECORD v4 2026-08-12: precondition 5 ALSO CLOSED (witness harness-runtime/tests/test_b71_escalation_token_rides_the_tracing_export.py, 8 tests, mutation-probed). The CHARTER''S PREMISE IS FALSE IN EFFECT: it declined to convene C7 because "the identity is not a span attribute", but s3 lands the widening inside compose_hitl_action_id, whose output rides the tracing export as webhook.idempotency_key on the outer hitl.webhook.deliver span (OD-canonical at C-OD-32), with NO dedicated attribute. CORRECTED at codex round 1: ONE reachable carrier, not two -- hitl.invocation.audit_ledger_entry_id is DEFAULT-ON but UNREACHABLE on this path, since _escalate_to_secondary_channel is NoReturn and the hitl.invocation.opened span opens only on the fall-through (hitl_gate_composer.py:2033-2044). One reachable carrier is enough for the conclusion, and the export half is now WITNESSED END-TO-END (PRODUCER TO EXPORTER: the real _escalate_to_secondary_channel composes its own idempotency_key and threads it to deliver_webhook_for_brief, read back off the exported span with nothing in the test supplying it; deleting the production _set, or making the helper forward a different value, each turns it RED). The audit attribute is unreachable in BOTH venues; note the timeout venue DOES open hitl.invocation.opened/timed_out before escalating, so the "homeless" rationale for declining C1''s attribute is scoped to durable-async. CONSEQUENCE: C10''s leak bar extends to tracing as a DESCRIPTION of what ships, and it binds the token''s form -- basis (B)''s raw identity contains the run_id VERBATIM (witnessed), so hashing must occur BEFORE the value enters compose_hitl_action_id, after which it is already on the exported carrier. C1''s hitl.escalation.instance_id is DECLINED (no information added; a SECOND authority over one concept, and homeless on the durable-async venue; needs its own C-OD-32/s12.2 amendment; nothing hitl.escalation.* exists today so declining is cheap to reverse). The span-export round-trip is DONE (executed, both links mutation-pinned). EGRESS: a round-5 draft called it CONDITIONAL from the implementation side; codex round 6 CORRECTED that against the canonical contract. OD spec v1.8 sC-OD-32.3 says webhook spans are head=1.0 ALWAYS-SAMPLED, while hitl.webhook.deliver is absent from the 19-entry ALWAYS_SAMPLED_EVENT_CLASSES that s9.2 defines as exactly that set -- a CONTRACT-vs-IMPL DRIFT on an inviolable s9.3 floor, now REGISTERED as B-160 (not B-71''s to fix). The contract governs here per the authority chain, so the egress is UNCONDITIONAL -- the stronger reading a leak bar must assume; the hashing requirement is unaffected by which side is fixed. Surviving asymmetry: the always-sampled hitl.invocation.opened/timed_out are precisely the spans that never carry the token. The witnesses prove EMISSION, not SURVIVAL through the production processor chain -- exercising materialize_span_processor_stage is OWED, and is also the experiment that settles B-160. STILL OPEN: whether an OD redact_span surface already filters these is unexamined. DESIGN RECORD v5 2026-08-12: precondition 4 SHARPENED but NOT closed (a draft claimed the closure and it was RETRACTED on review -- the boundary was wrong at the durable=False default, and the witness was runner-level rather than the live resume asked for). DESIGN RECORD v6 2026-08-12: precondition 4 NOW CLOSED, and a CITATION THE RECORD HAD WRONG SINCE v2 is corrected. Basis (B)''s token is composed from pre_dispatch_gate_owning_branch_identity(run_id, branch_index) at workflow_driver.py:8346, where run_id is THE RUN EXECUTING THE FAN-OUT -- for a top-level fan-out, not a child at all. The load-bearing continuity is therefore the TOP-LEVEL resume path, mcp_server.py:371-372 (C-RT-35: run_id = _resume_snapshot.run_id if ... else uuid4()), NOT child_workflow_runner.py:230-234, which applies only when the fan-out is nested inside a child run. Both continuities are now witnessed. LIVE-RESUME WITNESS delivered on the real stack for the exact population: test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py::test_b71_resume_preserves_the_run_id_the_pre_dispatch_identity_is_composed_from -- api.run -> PAUSED -> api.resume (empty ResumeContext) -> re-pause, asserting the run_id is identical across both snapshots; mutation-probed (dropping C-RT-35 continuity turns it RED, along with the module''s own core assertion). THE durable=False DECISION: the window is TWO-MODE, differing in WHO closes it -- at durable=True the harness journals the snapshot; at the shipped durable=False default the CALLER persists RunResult.pause_snapshot, a SHIPPED TESTED capability (test_r_cc_1_api_resume.py::test_api_resume_restart_proof_round_trip round-trips a real snapshot and resumes to SUCCESS under PauseResumeProtocolConfig.default(), durable=False). A v6 draft claimed the default was VACUOUS because resume_handle requires durable; codex falsified that -- resume_handle is not the only post-restart path. At the default the guarantee is conditional on caller behaviour the harness cannot enforce; a leg needing the stronger property should REQUIRE durable=True. Residual inside the window is not a gate (needs a durability change against s3''s ordering -- its own arc). STILL CITED: that the durable wrapper journals synchronously at capture. STATUS: ALL FIVE PRECONDITIONS CLOSED -- THE SPEC LEG IS AUTHORIZABLE, subject to the s5 residual table + the three council conditions. SPEC LEG LANDED 2026-08-13 -- SIX ARTIFACTS, not the one the first attempt drafted. PR #1325 carried a THREE-amendment CP delta with 14 findings absorbed across four out-of-family rounds and was deliberately CLOSED rather than merged, because round 4 found two defects that rewording could not close; #1326 recorded the true shape on main. Both are now closed. (1) THE PERSIST-ONCE CONTRACT WAS UNSATISFIABLE, not merely unimplemented: s0.4.2 puts the authoritative echo on the resume state and declares a normative use-it-do-not-recompute order, but the reader is the composer at s14.8.8.1 step 1, which holds a StepExecutionContext that knew only the pre-hash basis -- the echo was written to a carrier the reader never consults, so EVERY resume would have reached the recompute arm s0.4(5) designates as the crash-fallback. CP v1.119 s0.4.3 / s25.21 adds the echo READ carrier and states the read order as one three-arm rule over both fields; amendment count 3 -> 4. (2) THE RUNTIME SPEC WAS A ZERO-CHANGE CLAIM BY OMISSION: s0.11 listed OD / CXA / ADR / ADD / PRD as unchanged and simply did not name Runtime. It is not zero -- Spec_Harness_Runtime_v1.md s14.8.8.1 step 2 specifies the TWO-argument compose_hitl_action_id, re-verified at HEAD at this leg''s grounding pass. CP s0.13 names both owed Runtime sites and the co-requisite landed at Runtime spec v1.121. THE SIX ARTIFACTS: CP spec v1.119 (4 carrier amendments); Runtime spec v1.121 (2 canonical-reading amendments at s14.8.8.1 steps 1 and 2, bodies preserved verbatim per the v1.34 consumer-cite precedent); CP plan v2.53 (NEW U-CP-102, 10 ACs, 4 mutation-probe obligations); Runtime plan v2.63 (NEW U-RT-155, 7 ACs, 3 mutation-probe obligations); and clearance markers for all four. THE TWO UNITS ARE CO-REQUISITE IN BOTH DIRECTIONS and that is recorded as a plan-level fact in both deltas -- carriers with no minter stay None forever, a minter with no carriers has nothing to write, so a future session cannot schedule one alone and read a green suite as evidence the mechanism works. SIGNATURE SHAPE IS DELIBERATELY UNPRESCRIBED at the Runtime leg, per that file''s own s14.8.8.10 CONTRACT-not-mechanism precedent (set at v1.106 after a draft prescribed a call shape grounding falsified against the real call graph) -- the same discipline this row''s three earlier attempts violated by prescribing wiring they had not executed. THE DEFECT WITNESS IS A CRITERION, NOT A SIDE EFFECT: Runtime plan AC 5 requires two peer branches sharing a child_workflow_id to produce DISTINCT keys with BOTH HITL audit entries surviving the C-IS-07 s7.5 key-only dedup; anything weaker witnesses the carriers rather than the fix. ROW STAYS registered_finding: the IMPL LEG (U-CP-102 + U-RT-155) is owed and is what closes this row -- a spec leg cannot close a defect, only authorize its repair. The SEVEN follow-ons at CP spec s0.9 remain separately registered and none is in scope -- the seventh is B-165, the duplicate same-position placement collision this leg surfaced and declined to repair, since widening the ratified identity basis is back-flow rather than spec application. FINAL SCOPE AFTER THREE OUT-OF-FAMILY ROUNDS -- these are the numbers the impl leg must plan from, and they SUPERSEDE the counts stated earlier in this row: CP spec v1.119 carries FIVE carrier amendments (brief field; pre-hash basis; persisted echo; echo READ carrier; public correlation projection) and FOUR payload_body keys (the BARE token plus branch_context / resolvability / resolvability_note). Runtime spec v1.121 carries THREE sites (step-1 minter; the compose_hitl_action_id fold at ALL THREE live invocations hitl_gate_composer.py:1302 / :1543 / :2238; the step-3 project_brief_to_payload projection). U-CP-102 = 16 acceptance criteria + 9 mutation probes; U-RT-155 = 11 acceptance criteria + 6 mutation probes. ROUND-BY-ROUND, all absorbed: r1 4xP1 + 3xP2 (the minted token had no WRITER; already-durable snapshots would have been rejected as corrupt on upgrade; no site owned the webhook projection; the correlation loop terminated in a struct no operator reads; the token-present composed key was unpinned; the two units formed a dependency CYCLE; the validator trust seam had no owning site). r2 2xP1 + 2xP2 (the webhook carried the COMPOSED key while the projection carried the BARE token and s0.4(1) forbids parsing, so they were not comparable -- a fourth payload key closes it; the fold reached ONE call site of THREE, which would have made the one-identity-family promise false in the shipped system and left the defect witness on the unwidened audit key). r3 2xP2 (a carried-forward snapshot row could reset a persisted echo to None and rotate an unresolved escalation''s key; stale count mirrors). ONE RESIDUAL REGISTERED RATHER THAN REPAIRED: two placements declared at the SAME position collide on the token, because the ratified basis (B) hashes the position and not the declaration -- widening the digest at spec-application time would be the silent design extension X-AL-3 forbids, so the guarantee is narrowed in place to per (branch x position) and the duplicate-declaration shape needs a design-record revisit.'
  council: 'CONVENED + CLOSED 2026-08-12. The demanded convening happened (five voices;
    C1 orchestration + C10 action-safety primaries, C9/C5/C11 consultants), reconciled to
    internal zero, then was falsified on its load-bearing uniqueness claim by three
    decorrelated reviewers. Design record:
    .harness/council-b71-hitl-external-correlation-2026-08-12/DELIVERABLE.md -- identity basis
    RESOLVED to (B). CURRENT STATE (v6, 2026-08-12): ALL FIVE preconditions are CLOSED and
    the SPEC LEG IS AUTHORIZABLE. (A v5 draft claimed this prematurely and was retracted; v6
    earns it with a live-resume witness and the durable-mode resolution.) No council is owed,
    no gate remains. What the leg opens with is the s5 residual table plus the three council
    conditions. Do not re-run the council and do not re-open preconditions 1-5. What the leg opens with is a RESIDUAL list (8 items, none of them gates) plus the
    three council conditions -- sequencing, scope/B-158, and the branch_context leak bar now
    extended to the tracing channel. Read DESIGN RECORD s5 for the table; do not re-run the
    council and do not re-open preconditions 1-5.'
  heading: '### B-71 · Operator-facing HITL escalation requests carry no run-instance-distinct
    correlation identifier, making `hitl_responses` addressing impossible for peer branches
    sharing a `child_workflow_id` *(registered at the B-39 spec-leg PR #1092, out-of-family
    Codex round 6 (branch-vs-main), 2026-07-23)*'
- id: B-72
  title: SUB_AGENT_DISPATCH step's own SUB_AGENT_BOUNDARY HITL gate can re-fire on resume-forward,
    discarding an already-paused descendant's cursor
  status: closed
  summary: hitl_delivery_holder is never threaded to fan-out branches, so a SUB_AGENT_DISPATCH
    branch with its own SUB_AGENT_BOUNDARY placement re-evaluates its gate on every resume-forward;
    a fresh DURABLE_ASYNC re-pause has no evident mechanism to preserve the branch's prior-round
    child_resume_snapshot cursor. Pre-existing/orthogonal to B-39 (the old global ResumeContextHolder
    masked it via accidental cross-branch misapplication); B-39 makes it newly visible, not new.
  close_out: 'SUPERSEDED 2026-07-25 (spec leg landed, CP spec v1.108 property 6) -- the ORIGINAL
    recipe below (call hitl_response_for(child_run_id) at the fan-out branch dispatch site) is
    WRONG for a pre-dispatch gate-owning branch and MUST NOT be followed literally -- property 6
    explicitly FORBIDS ever resolving a pre-dispatch gate-owning branch via the
    child_run_id-keyed hitl_responses map (out-of-family codex [P1] caught this contradiction
    between the original recipe and the newly landed contract, 2026-07-25). CORRECTED RECIPE for
    item (1), the round-3 repro own SOLE-branch case -- (a) add a carrier (amendment to
    PeerFanOutResumeState/FanOutResumeState or equivalent) recording a pre-dispatch gate-owning
    branch presence, keyed by a tree-wide-unique internal identity per CP spec v1.108
    (ancestry-qualified, NOT merely locally-unique among sibling branches -- ship a
    collision-witness test); (b) amend _collect_gate_owning_run_ids to walk this new carrier
    alongside its existing paused_child_branches walk; (c) construct a
    HITLDeliveryCell-equivalent on that branch own StepExecutionContext at the fan-out dispatch
    sites (_execute_parallelization ~7851-7866, _execute_orchestrator_workers ~11696-11710 --
    mirroring workflow_driver.py:4766-4777 LINEAR-only construction) ONLY when this internal
    identity is the sole unaddressed member per property 4/6 own SOLE-membership rule -- NEVER
    via hitl_response_for(child_run_id), which this branch has none of. Items (2)/(3)/(4) below
    remain open, unchanged by the spec leg -- only item (2) (keyed multi-peer addressing) is
    B-71-gated per CP spec v1.108 SS1.2''s own scope note; item (3) (property 4''s general
    resolver set-membership mechanism) is independently deferred to the impl leg, not
    B-71-related; item (4) (B-72 own round-1 hybrid case) needs its own reproduction before any
    design, also not B-71-related (codex out-of-family review caught this register row
    overbroadly labeling all three B-71-gated, 2026-07-25). Full detail at CP spec v1.108 +
    .harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md. --- Original
    (now-superseded) recipe + append-only correction history, preserved for provenance --- Build
    the deferred fan-out HITL delivery-mechanism wiring -- call resume_context.
    hitl_response_for(child_run_id) at the fan-out branch dispatch site and construct a
    HITLDeliveryCell-equivalent on that branch own StepExecutionContext (mirroring
    workflow_driver.py:4777 LINEAR-only construction), while independently satisfying property 4
    multi-child fallback-safety invariant (a prior draft counting approach already failed review
    here). UPDATE 2026-07-25 round 3 -- EMPIRICALLY CONFIRMED via a built reproduction,
    harness-runtime/tests/integration/ test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py (2
    tests, both PASS -- pin the confirmed broken state directly, not via xfail). Precondition
    test confirms a PARALLELIZATION branch''s own SUB_AGENT_BOUNDARY gate correctly pauses on
    first dispatch through the REAL RuntimeHITLGateComposer. Core test pins the gap with 4
    oracles -- api.resume with a resolved answer does NOT reach completed; status stays paused,
    failure_cause is None, 2 webhook POSTs (re-escalated), and a DIRECT spy on
    RuntimeSubAgentDispatcher.dispatch stays empty (proves the real dispatcher was never reached
    -- NOT an Anthropic-client-call-count proxy, which codex round 2 showed does not
    discriminate for this child''s PURE_PATTERN_NO_ENGINE engine class). Refines round 1''s
    PARTIAL/cursor-lost prediction -- this used TEAM_BINDING/pause cascade policy, and the
    observed shape is an infinite re-pause (no state corruption, just never converges), not a
    lost cursor. The exact post-fix terminal status is deliberately NOT documented as completed
    -- codex round 2 empirically bypassed the gate entirely and the run STILL ended paused for
    this child shape (separate, unrelated quirk, independently reproduced). Full harness-runtime
    suite re-run -- 3069 passed, 40 skipped, 0 xfailed, 0 regressions (just check full monorepo
    -- 6682 passed). 4 rounds of codex review, all real, all fixed -- (1) a blanket xfail would
    have masked an unrelated resume() regression, replaced with direct oracles; (2) status-only
    + an Anthropic-client-proxy oracle were both too weak, replaced with a mutation-probed
    direct spy on RuntimeSubAgentDispatcher.dispatch; (3) this register text itself went stale
    describing the retired proxy oracle, corrected; (4) never asserted pause_snapshot is not
    None -- a re-fire that silently dropped the resumable snapshot would have passed every other
    assertion, added that check plus a workflow-identity check. Final oracle count -- 6. A
    SEPARATE finding still OPEN -- the gate fires before any child run exists, so the resulting
    snapshot carries no child run ID; hitl_response_for(child_run_id) cannot by itself
    distinguish two PEER gate-owning branches in this shape. This reproduction scopes the
    single-branch LIVENESS gap; the multi-branch addressing mechanism needs either a
    multi-peer-branch repro or grounding on branch-own pre-dispatch identity before the fix is
    genuinely scoped. UPDATE 2026-07-25 round 4 -- re-confirmed by direct read that
    hitl_delivery_holder/hitl_delivery_cell stay confined to _execute_workflow_body;
    _execute_parallelization and _execute_orchestrator_workers reference neither (matches round
    2, not a new finding). The genuinely new observation -- round 1''s own text frames this as a
    dispatch-idempotency defect (no addressing key needed, just a
    first-dispatch-vs-resume-forward check), while round 2''s reframe frames it as an
    addressing/delivery defect (needs a per-branch key) -- these do not reconcile and nobody had
    named the ambiguity. A mid-pass hypothesis floating branch_path as that key was
    advisor()-rejected before write-up -- the single-branch repro tests zero multi-branch
    addressing, so the "collision only hits nested grandchildren, not peers" claim is
    unverified, and it is exactly the reading the code''s own keying-defect note warns against
    reinventing. Explicitly NOT a finding of this pass. First question owed before design or
    code -- dispatch-idempotency (b) or addressing/delivery (a); see
    .harness/post-phase-8-forward-register.md B-72 round-4 update for full detail. UPDATE
    2026-07-25 round 5 (systems-architect tension-resolution mode, 3 out-of-family Codex
    corrections) -- the (a)-vs-(b) framing was the wrong axis for the round-3 REPRODUCTION''s
    narrower scenario specifically -- CP spec v1.106 section 1.2 properties 4-5 distinguish
    gate-owning branches from transitively-paused container/ancestor branches as two DIFFERENT
    branch roles, not competing theories, and the round-3 repro (single PARALLELIZATION branch,
    own SUB_AGENT_BOUNDARY placement, no nested child) is confirmed gate-owning. CORRECTION --
    "category (a)" here is DELIVERY-cell construction, not necessarily keyed addressing; the
    round-3 test itself resumes via the UNIFORM ResumeContext.hitl_response field (test line
    573), not a keyed map, and property 4 permits the uniform fallback for a SOLE unaddressed
    gate-owning branch -- exactly this repro''s shape. So wiring a delivery-cell construction
    alone (no keying) already fixes the round-3 repro; KEYED multi-peer addressing (2+
    concurrent gate-owning branches) is a separate, still fully open question untested by any
    evidence gathered so far. B-72''s OWN original registered scenario above (a branch that
    already dispatched into its own gate, got approved, and is now resume-forwarded toward its
    self-dispatched deeper-paused descendant) is a THIRD, hybrid case property 5 does not cover
    (it requires "never itself dispatched into a HITL gate") -- stays OPEN, unresolved. SECOND
    CORRECTION -- the keyed-addressing tiebreaker (does a run_id-compatible pre-dispatch carrier
    exist) is gated on B-71 regardless of its answer -- B-71 (already registered, separate) is
    that the operator-facing HITL escalation payload exposes NO run-instance-distinct identifier
    at all (keyed off compose_hitl_action_id, workflow_id-scoped), needing its own C-CP-28
    carrier amendment -- so even an internally-mintable-early run_id does not clear this to pure
    impl discretion until B-71 lands or is co-designed with it. CLOSE-OUT CAVEAT (codex, not
    independently verified this pass) -- _collect_gate_owning_run_ids
    (workflow_driver.py:2665-2698) treats any node carrying a fan-out resume carrier as an
    unconditional container regardless of its own pause_reason, and that carrier is built
    whenever ANY branch fails/pauses in a PARALLELIZATION run -- so a branch paused at its own
    gate with zero dispatched children may compute to an EMPTY gate-owning set, meaning
    delivery-cell construction alone may not suffice. Verify before building. NET POSITION --
    round-3 repro is gate-owning, needs delivery-cell construction (not necessarily a key),
    modulo the unverified caveat just above; keyed multi-peer addressing stays fully open and is
    B-71-gated; B-72''s own original hybrid scenario stays open and untested; council covers all
    of the above, none retired. Full detail + all corrections at
    .harness/post-phase-8-forward-register.md B-72 round-5 update. UPDATE 2026-07-25 round 6
    (roadmap-continue, advisor()-directed empirical check BEFORE any code) -- round 5''s own
    close-out caveat (whether _collect_gate_owning_run_ids returns an empty gate-owning set for
    a branch paused at its own gate with zero dispatched children) is CONFIRMED TRUE by direct
    instrumentation of the real round-3 repro object --
    compute_hitl_uniform_fallback_eligible_run_id returns None (not the branch''s own run_id);
    PeerFanOutResumeState captures branch_count=1 but BOTH branches=() and
    paused_child_branches=() are empty -- no carrier anywhere records this branch''s own
    gate-pause identity. NET POSITION ITEM (1) IS FALSIFIED -- delivery-cell construction alone
    cannot fix the round-3 repro; the fix needs a NEW per-branch carrier on
    PeerFanOutResumeState/ FanOutResumeState, which is a CP-spec-owned additive schema change
    (Class 1, not impl discretion). Filed as
    .harness/class_1_fork_b72_gate_ownership_missing_carrier.md. Status flips registered_finding
    -> design_substrate_gated (mirrors the B-70 discriminator). No design-substrate edit, no
    code edit (debug instrumentation added and reverted; git diff confirmed clean). Recommends
    co-designing the new carrier''s identity shape with B-71''s already-registered
    keyed-addressing question, since both need the same answer to "what identifies a
    not-yet-dispatched gate-owning branch, compatible with property 1''s fixed run_id-shaped
    key". UPDATE 2026-07-25 round 6 continued -- a second instrumentation pass
    (terminal_dispositions dump at workflow_driver.py:9698) confirms the mechanism directly (not
    just the outcome) -- terminal_dispositions={} branch_failed=True for this branch''s ordinal;
    branch_failed derives from the gather() BaseException scan, independent of
    terminal_dispositions, which is why no FanOutBranchResumeState/branches entry is ever
    produced. Also flags an unexplained datum for the spec-leg -- root pause_reason is
    EXPLICIT_OPERATOR, not HITL_PENDING, possibly related to round 3''s own noted "separate,
    not-yet-understood quirk"; the new carrier''s reason-labeling convention needs its own
    design decision against _collect_gate_owning_run_ids'' existing HITL_PENDING leaf test, not
    an assumed compatibility. Section 4 now states the carrier as a REQUIREMENT (identity +
    B-71-compatible key shape + reason-labeling decision), not a prescribed field name/type --
    round 5''s own lesson about premature carrier-shape prescription. OPERATOR RATIFIED
    2026-07-25 (AskUserQuestion) -- open the CP spec-leg now, co-designed with B-71. UPDATE
    round 7 (pre-authoring grounding) -- B-71''s own close_out only exposes an
    ALREADY-DISPATCHED child''s run_id (an identifier that already exists), so the co-design is
    SEQUENTIAL not simultaneous -- this carrier''s identity gets designed first, B-71 can echo
    it after. A second empirical check of compose_child_run_id_seed
    (sub_agent_dispatch.py:363-395,1078-1098) found the deterministic pre-dispatch minting path
    (round 5''s own tiebreaker option) is GATED on subagent_child_recoverable (engine in
    ESR/WAL/SAVE_POINT/RECONCILER_LOOP) -- for PURE_PATTERN_NO_ENGINE, the EXACT engine class
    the round-3 repro uses, the seed falls through to None and a fresh non-deterministic uuid is
    minted only at actual dispatch time. Deterministic-minting alone does not cover the repro''s
    own shape. Full detail (incl. an unresolved observation about whether the round-3 repro''s
    SOLE-branch case can be fixed via the uniform fallback without a property-1-compliant
    external identity at all) at the fork doc''s new section 7. SPEC LEG LANDED 2026-07-25 -- CP
    spec v1.107 -> v1.108 (NEW additive SS1.2 property 6, systems-architect tension-resolution
    mode, advisor() unavailable at authoring time, grounded directly against verbatim property
    1/4/5 text + verbatim sub_agent_dispatch.py code) + CP plan v2.43 -> v2.44 (deferred
    coverage- matrix row) + 2 clearance markers. Resolves the (a)/(b)/(c) framing -- neither
    widening property 1''s key nor a separate delivery path is needed; a pre-dispatch
    gate-owning branch is counted in property 4''s unaddressed-set test via an internal-only
    identity, never hitl_responses-keyed. B-71 confirmed NOT a co-requisite for this specific
    fix. Out-of-family just codex-review (round 1, base=main) caught 2 real [P1]s before merge
    -- (1) the plan delta''s first-draft coverage row named only the counting/carrier side,
    omitting the delivery-cell construction at the fan-out dispatch sites required to actually
    consume the resolved response -- fixed, the row now explicitly names both; (2) the
    canonicalization bundle (clearance markers, CLAUDE.md pointer bumps, this register update)
    was incomplete when the spec/plan text alone was reviewed -- fixed by this update + the
    co-landing PR. Full detail at the tension record,
    .harness/class_1_tension_b72_pre_dispatch_gate_owning_branch_identity.md. Status flips
    design_substrate_gated -> open (spec-leg gate cleared; the impl leg -- code + tests wiring
    the new carrier + the delivery-cell construction per CP plan v2.44 SS5''s now-corrected row
    -- is the next buildable slice, NOT yet opened, per the B-33/B-39/B-59/B-70
    spec-leg/impl-leg precedent). Closes ONLY net-position item (1); items (2)/(3)/(4) remain
    open exactly as rounds 5-7 scoped them. ROUND 2-5 CODEX FIXES (2026-07-25, same branch,
    pre-merge, out-of-family just codex-review base=main). Round 2 caught property 6''s internal
    identity permitting only local uniqueness, colliding under nested fan-out and violating
    property 4''s SOLE-member safety guarantee -- fixed, now requires resume-tree-wide
    uniqueness plus a collision-witness test, and this register''s own stale close-out recipe
    plus stale claude-artifact-pointers.md rows were fixed too. Round 3 caught property 6
    permitting counted-but-never-keyed without requiring actual delivery to the sole branch,
    leaving the round-3 repro''s livelock unclosed -- fixed, section 1.1(b) now requires
    delivery as an explicit contract outcome, plus this tension record''s stale Status field and
    an overbroad B-71 dependency label on items (3)/(4) here were fixed. Round 4 caught the new
    carrier''s existence and byte-compat treatment being left to unauthorized impl discretion
    despite PeerFanOutResumeState/FanOutResumeState participating in the persisted snapshot_hash
    -- fixed, NEW section 1.3a pre-authorizes ONE additive drop-when-empty/None
    hash-strip-scoped field mirroring the v1.97/v1.99 precedent, plus stale harness-cp/CLAUDE.md
    and clearance-marker summaries were fixed. Round 5 caught two real findings. (a) this
    session''s own roadmap_status.md dashboard still said the spec leg was not yet started
    despite the completed delta -- fixed at the dashboard''s round-8 update. (b) property 6''s
    contract text read as topology-agnostic while only PARALLELIZATION/ORCHESTRATOR_WORKERS are
    grounded by the round-3 reproduction (confirmed via direct grep -- the repro test uses
    TopologyPattern.PARALLELIZATION exclusively) -- fixed, NEW section 1.2b explicitly scopes
    property 6 to the two fan-out topologies and names
    EVALUATOR_OPTIMIZER/DECENTRALIZED_HANDOFF''s sequential
    EvaluatorOptimizerResumeState/HandoffResumeState carriers as a distinct, ungrounded, out-of-
    scope gap for a future arc''s own reproduction-first grounding pass, not silently folded
    into this delta''s build scope. 5 rounds total to convergence. ROUND 6 CODEX FIX
    (2026-07-25, same branch, pre-merge). Codex caught that round 5''s own fix under-scoped --
    HIERARCHICAL_DELEGATION was omitted despite recursively reusing
    _execute_orchestrator_workers per U-CP-88/89 (confirmed by direct code read of
    workflow_driver.py:13745-13849 -- _execute_hierarchical_delegation takes the identical
    crash_fan_out_resume FanOutResumeState/PeerFanOutResumeState parameter and threads it
    through the same helper, not a separate implementation) and sharing the identical carrier
    being amended -- leaving the contract inconsistent with the runtime call graph it
    constrains. Fixed -- section 1.1 and section 1.2b now include HIERARCHICAL_DELEGATION on
    carrier-identity grounds, not reproduction grounds; the plan''s coverage row already amends
    _execute_orchestrator_workers directly, which covers every HIERARCHICAL_DELEGATION recursion
    level automatically, no separate call site owed. 6 rounds total to convergence. ROUND 7
    CODEX FIXES (2026-07-25, same branch, pre-merge, 2 real P2s). (a) The delta''s own
    header/revision-shape text asserted NO new field and NO carrier-shape change while section
    1.3a explicitly authorizes one additive default-empty field on
    PeerFanOutResumeState/FanOutResumeState -- fixed, reworded to acknowledge the field, exact
    name/type still deferred to the impl leg. (b) The EVALUATOR_OPTIMIZER/DECENTRALIZED_HANDOFF
    topology exclusion (named in section 1.2b) was tracked only in this correction-history prose
    with no durable register row of its own -- fixed, split out as new B-78
    (registered_finding), with its own close-out recipe -- a future arc''s reproduction-first
    grounding pass against those carriers, mirroring how this B-72 round-3 repro preceded its
    own spec leg. 7 rounds total to convergence. IMPL LEG LANDED 2026-07-25 (PR #1116) -- closes
    net-position item (1) end-to-end. NEW additive
    PreDispatchGateOwningBranchResumeState(branch_index, step_id) carrier on
    PeerFanOutResumeState/FanOutResumeState per SS1.3a''s pre-authorization (drop-when-empty
    hash-strip scoped -- pre-B-72 snapshots re-hash byte-identically).
    _collect_gate_owning_run_ids amended to walk it via a tree-wide-unique internal identity
    (f''{snapshot_run_id}:pre-dispatch-gate:{branch_index}''). NEW
    _collect_pre_dispatch_gate_owning_identities excludes these identities from the
    hitl_responses-keyed addressability check per SS1.1(b)''s never-keyable requirement.
    HITLDeliveryCell construction wired at both _execute_parallelization and
    _execute_orchestrator_workers (the latter covers HIERARCHICAL_DELEGATION via its existing
    recursive reuse), gated on this branch''s identity being the SOLE unaddressed member per
    property 4/6. The round-3 repro''s own test renamed/re-asserted to confirm FIXED behavior
    (webhook count stays 1, RuntimeSubAgentDispatcher reached exactly once, second pause is the
    separate pre-existing EngineClass.PURE_PATTERN_NO_ENGINE child-shape quirk the original test
    already flagged out of scope) plus a NEW assertion that the first pause is labeled
    HITL_PENDING (the operator-facing signal the carrier is active). No _resumed_target_plan
    extension needed for concurrency -- property 6(c) means when 2+ branches are unaddressed no
    delivery cell is built for ANY of them, so at most one branch ever holds a HITLDeliveryCell
    in a single resume cycle; the B-39 interim-sequencing hazard the existing plan extension
    guards against is structurally unreachable here. Also confirmed -- the PROCEED
    cascade-policy path needs no equivalent wiring -- _proceed_branch/_proceed_worker have no
    except BaseException handler, so a HITLPauseRequestedSignal propagates through
    asyncio.gather(..., return_exceptions=True) unchanged and is already caught by the existing
    generic any_failed BaseException scan, matching the established
    proceed-degrades-never-resumes precedent for every other unhandled pause/failure class on
    that path. 3 rounds of out-of-family just codex-review (base=main) to convergence -- round 1
    caught 2 real [P1]s -- (a) the carrier was ordinal-only, giving _resume_body_mismatch
    nothing to validate identity against -- fixed by adding step_id to the carrier plus
    bounds/no-overlap/step_id-identity validation in both fan-out _resume_body_mismatch
    closures, empirically verified by editing a paused step''s step_id between pause and resume
    and confirming the resume now fails closed; (b)
    compute_hitl_uniform_fallback_eligible_run_id''s generic ''rid not in hitl_responses''
    filter would treat a COLLIDING map key as addressed for a pre-dispatch identity, violating
    SS1.1(b) -- fixed via the never-keyable exclusion above, regression-tested. Round 2
    confirmed both fixes clean. Round 3 found a THIRD, genuinely distinct gap this diff does NOT
    introduce -- confirmed pre-existing at the already-shipped LINEAR resume_at delivery-cell
    construction site (workflow_driver.py ~4748-4777), which this diff''s fan-out wiring mirrors
    verbatim and therefore inherits, not causes -- registered separately as B-79 (see its own
    row) rather than fixed here. Full harness-cp + harness-runtime targeted suites re-run clean
    (153 + 2 passed, 0 regressions); ruff + pyright strict clean on all touched files. ROUND 4
    CODEX FIX (2026-07-25, same PR #1116, pre-merge, base=main, out-of-family just codex-review)
    -- 2 real [P1]s in the impl leg''s OWN new code (not the round-3 pre-existing gap registered
    as B-79): (a) the carrier validated step_id identity but not step_kind or (for
    SUB_AGENT_DISPATCH) the target child_workflow_id -- a same-step_id edit swapping either
    previously passed the resume guard undetected -- fixed by mirroring
    PausedChildBranchResumeState (B-31)''s identical guards exactly: step_kind is checked
    against the CONSTANT StepKind.SUB_AGENT_DISPATCH (a pre-dispatch gate-owning branch is
    always that kind by construction, so no field is captured for it), child_workflow_id is a
    NEW optional captured field read from the step''s own payload at pause time (no dispatched
    child exists yet to source it from an exception, unlike B-31''s carrier). (b) a
    HITLDeliveryCell could still be constructed for the sole unaddressed pre-dispatch
    gate-owning peer even when a SIBLING effect-fence-paused peer resolved this resume to a
    run-level ABORT -- letting the HITL-approved peer dispatch concurrently before the ABORT
    fails the run -- fixed by gating both fan-out delivery-cell construction sites on `not
    _any_fence_abort`, mirroring the existing effect-fence-directive suppression at the same two
    sites. 3 new regression tests witness both fixes in
    test_workflow_driver_parallelization_pause.py, including a non-vacuous check that the
    suppressed peer is still re-dispatched (re-pausing INERT) rather than silently skipped. Full
    harness-cp targeted suite re-run clean (156 passed, +3 from this round); ruff + pyright
    strict clean on all touched files. 4 rounds of out-of-family just codex-review total to
    convergence for the impl leg. ROUND 5 CODEX FIX (2026-07-25, same PR #1116, pre-merge,
    base=main, out-of-family just codex-review) -- round 4''s OWN kind-changed guard was itself
    broken: it hardcoded a constant check against StepKind.SUB_AGENT_DISPATCH, wrongly assuming
    a pre-dispatch gate-owning branch can only ever be that kind. HITLPlacementKind.PRE_ACTION
    can ALSO gate INFERENCE_STEP/TOOL_STEP steps and raises the SAME name-matched
    HITLPauseRequestedSignal -- an unchanged PRE_ACTION-gated resume would have been rejected as
    a false material diff, a genuine regression round 4 introduced. Fixed by restoring step_kind
    as a CAPTURED field (compared for equality on resume), not a constant check -- the
    PausedChildBranchResumeState (B-31) precedent round 4 mirrored does not apply here, since
    that carrier IS uniquely SUB_AGENT_DISPATCH by construction while this one is genuinely
    multi-kind. child_workflow_id is unaffected (already defaulted to None via a try/except for
    non-SUB_AGENT_DISPATCH payloads). NEW regression test reproduces the exact scenario codex
    flagged: a PRE_ACTION-gated INFERENCE_STEP branch pre-dispatch-pauses, and resuming with the
    UNCHANGED workflow now succeeds instead of failing closed on a bogus kind-changed material
    diff. Full harness-cp + harness-runtime targeted suites re-run clean (157 + 2 passed, +1
    from this round); ruff + pyright strict clean on all touched files. 5 rounds of
    out-of-family just codex-review total to convergence for the impl leg. UPDATE 2026-07-25 round 6 (branch-vs-main, same PR #1116, out-of-family just codex-review) -- 2 NEW findings: (1) _any_fence_abort is scoped to the CURRENT fan-out level only, so a nested/deeper subtree effect-fence ABORT does not suppress a shallower level''s pre-dispatch HITL delivery -- confirmed PRE-EXISTING (inherited from the B-70 impl leg''s own fan-out-local _any_fence_abort computation, which the pre-existing effect-fence-directive suppression already consumes the same way) and NOT introduced by this diff -- registered separately as B-80 rather than fixed here; (2) a repeated resume starting with multiple recovered pre-dispatch gate-owning peers silently dropped one that Sec25.19 warm-up cohort scheduling withholds this round (a leader re-pausing before its cohort follower is ever dispatched) -- genuinely FIXED, since this IS the new carrier''s own property-4 sole-membership safety guarantee: carry forward any recovered ordinal that neither re-fired this round nor resolved via a different disposition (completed / recursive-child-paused / fence-paused), at both the PeerFanOutResumeState and FanOutResumeState construction sites. NEW regression test constructs the exact scenario via a real Sec25.19 cohort-key warm-up split and verifies the withheld branch is carried forward with its identity intact; mutation-probe confirmed the test fails without the fix. Full harness-cp + harness-runtime targeted suites re-run clean (158 + 2 passed, +1 from this round); ruff + pyright strict clean on all touched files. 6 rounds of out-of-family just codex-review total to convergence for the impl leg.'
  pr: '#1116'
  council: RETIRED "skip-a-gate vs strand-a-descendant" framing (2026-07-25 round 2) -- the spec's
    mechanism ROUTES a resolved answer via hitl_response_for(child_run_id), it does not skip gate
    evaluation. Design-risk flag RELOCATES, not deleted -- the deferred resolver must independently
    satisfy property 4's multi-child fallback-safety invariant, and a prior draft's counting mechanism
    was already found by out-of-family review to strand a container branch -- worth a dyadic council
    pass on the property-4-safe resolver design once the reproduction pins the observable shape.
    UPDATE 2026-07-25 round 5 -- council scope is NOT narrowed by round 5's category resolution; it now
    covers (1) the delivery-cell-construction fix for the round-3-confirmed single-branch gate-owning
    case (no keying needed), (2) the keyed multi-peer addressing design, gated on B-71 landing or
    being co-designed with it, (3) property 4's resolver set-membership mechanism, and (4) B-72's own
    original hybrid scenario (previously-gate-owning, now forwarding past its own already-satisfied
    gate) that round 5 re-opened -- property 5 does not settle it, so it is not safely retirable from
    council scope.
  heading: '### B-72 · A `SUB_AGENT_DISPATCH` step''s own `SUB_AGENT_BOUNDARY` HITL gate can
    re-fire on every resume-forward of an already-dispatched, deeper-paused child, discarding
    the child''s `paused_child_branches` cursor *(registered at the B-39 impl-leg Slice B
    branch `b39-impl-leg-slice-b-hitl-delivery-mechanism`, out-of-family Codex round 4
    (branch-vs-main), 2026-07-24)*'
- id: B-79
  title: 'A resumed branch''s HITLDeliveryCell is constructed from step_id identity alone, so a
    HITL placement/gate-configuration change between pause and resume is not detected as a
    material diff'
  status: closed
  summary: 'Out-of-family Codex round 3 (branch-vs-main, B-72 impl leg PR #1116, 2026-07-25).
    The B-72 impl leg''s _resume_body_mismatch guard (mirroring the pre-existing
    effect_fence_paused_branches/paused_child_branches precedent) validates a pre-dispatch
    gate-owning branch''s step_id identity before delivering the operator''s resolved response,
    but does not validate whether the branch''s HITL placement (e.g. SUB_AGENT_BOUNDARY) or
    step_kind still applies to the resumed step. If a workflow is edited between pause and
    resume while preserving the branch''s step_id -- removing its SUB_AGENT_BOUNDARY placement,
    for instance -- the guard still accepts the resume and RuntimeHITLGateComposer''s Step 0
    unconditionally consumes and applies the stored HITLDeliveryCell (e.g. a REJECT) to the
    now-ungated branch, before any current-placement evaluation runs. Confirmed PRE-EXISTING and
    NOT introduced by the B-72 impl leg -- the identical unconditional construction (no
    placement-identity check at all) already exists at the already-shipped LINEAR resume_at
    delivery-cell site (workflow_driver.py ~4748-4777, landed at the B-39 impl leg), which
    B-72''s fan-out wiring mirrors verbatim -- confirmed by direct grep/read of that site before
    registering this row, per this workspace''s own codex-finding-scope-verify-sibling-pattern
    discipline.'
  close_out: 'Design a HITL placement/gate-configuration identity check (e.g. persisting the
    resolved set of applicable HITL placements, or a content hash of the step''s HITL-relevant
    fields, alongside step_id at pause-capture time) and validate it at BOTH the LINEAR
    resume_at site and the fan-out _resume_body_mismatch closures (_execute_parallelization /
    _execute_orchestrator_workers) before a HITLDeliveryCell is constructed -- reject the resume
    as a material diff if the check fails, mirroring the existing step_id/step_kind precedent.
    This is a genuine design task (what identity to persist, and whether the composer''s own
    persona/engine-class placement-resolution logic -- which harness-cp does not have visibility
    into, that logic being Runtime-owned -- needs a new cross-axis seam to expose it) not a
    mechanical patch. Witness with a test that edits a paused step''s HITL placement (or
    step_kind) between pause and resume while preserving step_id, and asserts the resume is
    rejected as a material diff rather than silently delivering the stale response.

    SCOPE BROADENED 2026-07-26 (B-78 impl leg, out-of-family Codex round 4, branch-vs-main PR
    #1117): the gap also manifests via a SECOND path this row did not originally name -- when NO
    HITLDeliveryCell is constructed at all (no addressed or uniform HITLResult resolves this
    resume cycle), execution still proceeds to dispatch rather than staying inert, relying
    entirely on RuntimeHITLGateComposer''s Step-0 consume-or-fresh-fire logic
    (hitl_gate_composer.py:1684-1688) to treat the dispatch as a brand-new gate evaluation. This
    is SAFE-BUT-WASTEFUL (a second webhook fire) when the step''s HITL placement is unchanged,
    but degrades to the SAME unauthorized-dispatch risk this row already describes if the
    placement was ALSO removed/altered between pause and resume, since the fresh-fire path''s
    "does this step still need a gate" check is exactly the material-diff validation this row
    calls for and that does not yet exist anywhere. Confirmed via direct grep that this is the
    identical pre-existing shape at the already-shipped LINEAR resume_at site AND now also
    reachable at the newly-landed EVALUATOR_OPTIMIZER/DECENTRALIZED_HANDOFF delivery-cell sites
    (B-78 impl leg, PR #1117) -- one shared root cause, four call sites (LINEAR, both fan-out
    closures, EO, DH). The close_out design task above (a persisted HITL-placement/
    gate-configuration identity, checked before ANY resume dispatch regardless of whether a
    delivery cell resolves) already covers this path; no separate fix is owed, this note only
    widens the reachability set the eventual fix must cover.

    IMPL LEG SLICE 1 LANDED 2026-07-27 at PR #1132 -- the fan-out closure''s
    pre-dispatch-gate-owning delivery site only (_execute_parallelization +
    _execute_orchestrator_workers, the latter covering HIERARCHICAL_DELEGATION via
    its recursive reuse per the B-72 precedent). Per advisor()''s split
    recommendation (mirroring the B-80 8-before-7 split), slice 1 extends
    PreDispatchGateOwningBranchResumeState with ONE new field, hitl_gate_config_hash
    (str | None, default None) -- a sha256 hex digest over the ADD-only-folded
    applicable HITLPlacement tuple (fold_step_hitl_placements(manifest_entry.
    hitl_placements, binding.hitl_placement)) plus the per-step removed_placements
    directive (binding.removed_placements), mirroring step_id/step_kind/
    child_workflow_id''s own material-diff identity guard shape at the SAME
    resume-side _resume_body_mismatch closures. out-of-family codex round 2 [P1]:
    an initial draft made the field REQUIRED (no default) -- breaking
    JournalWorkflowPauseStore._parse_snapshot deserialization of a real durable
    journal snapshot captured by the PRECEDING (B-72-only) deployment, which would
    have stranded in-flight HITL-paused workflows after an upgrade; fixed --
    default-None byte-compat, _strip_default_fanout_resume_fields drops a None value
    from the hash-covered dump so a legacy row re-hashes byte-identically, and resume
    SKIPS the material-diff comparison when the snapshot''s captured value is None
    (mirrors child_workflow_id''s own "validate ONLY when present" convention).
    Round 3 converged clean ("No actionable correctness defects were found").
    17 new tests (pure discrimination witnesses for all 5 SS1.1(a) fields plus
    SS1.1(b) ADD/REMOVE symmetry, the byte-compat strip + legacy-snapshot-resume
    witnesses, plus 6 full pause-to-resume integration witnesses across both wiring
    sites), mutation-probe-verified (all 6 rejection/byte-compat witnesses genuinely
    fail when the corresponding guard is disabled).

    IMPL LEG SLICE 2 LANDED 2026-07-27 at PR #1133 (branch
    b79-slice2-sequential-material-diff) -- the three single-owner sequential sites
    (LINEAR resume_at, EVALUATOR_OPTIMIZER, DECENTRALIZED_HANDOFF), closing property
    7 end-to-end. Chose the spec''s own "reused verbatim" impl-discretion branch
    (SS1.3a): ONE new top-level PauseSnapshot.hitl_gate_config_hash field (str | None,
    default None) shared across all three sequential sites, rather than three
    distinct carriers or fields added to EvaluatorOptimizerResumeState/
    HandoffResumeState -- a sequential resume has at most one currently-relevant
    step, unlike the fan-out branches, so no per-branch carrier is needed. Reused
    slice 1''s _hash_hitl_gate_config pure function verbatim; renamed the
    topology-specific recompute helper (_pre_dispatch_gate_owning_captured_hitl_gate_config_hash
    -> _captured_hitl_gate_config_hash) since its body was always generic. Each
    site''s guard is placed immediately before its hitl_delivery_cell construction,
    deliberately NOT inside the unconditional _resume_body_mismatch closure slice 1
    used for the fan-out case -- a first draft mirrored that placement uncritically
    and advisor() caught the over-rejection risk before merge: an EO/DH resume with
    no delivery pending this cycle (e.g. a nested child under a fan-out with 2+
    unaddressed gate-owning siblings) legitimately reaches _resume_body_mismatch, and
    a FAILED there would wrongly turn a recoverable INERT re-pause into a terminal
    outcome. 3 rounds of out-of-family just codex-review to convergence: round 1
    [P2] the reject-tests only exercised an altered attribute (timeout), not the
    SS1.1(b) removal-symmetry direction -- added a placement-removed test per site;
    round 2 [P2] api.resume''s outer bounds check does not cover the internal
    ChildWorkflowRunner recursive-dispatch path -- a workflow body shortened between
    pause and resume raised an uncaught IndexError at the LINEAR site instead of a
    fail-closed RunResult (EO/DH were already protected by their own pre-existing
    _resume_body_mismatch bounds checks, confirmed empirically via the same
    shortened-body repro against both sites at the merge-gate pass); fixed +
    regression test added, mutation-probe-verified; round 3 converged clean ("No
    actionable defects were found"). DurablePauseResumeProtocol (harness-runtime)
    forwards the new kwarg to its parent -- pyright''s reportIncompatibleMethodOverride
    caught the omission before it could silently drop the field under durable config.
    17 new/updated tests (accept-unchanged / reject-changed / reject-removed /
    skip-legacy-None / no-delivery-pending-does-not-fail per site, plus the
    shortened-body bounds-check regression + a _compute_snapshot_hash byte-compat
    check), mutation-probe-verified. merge-gate 3-lens review: concurrency APPROVE;
    spec-conformance BLOCKED once (this row not updated in the same PR, unlike
    slice 1''s own precedent -- fixed, re-verified). Full harness-cp suite: 1837
    passed; monorepo-wide just check: 6775 passed, 0 failed. B-79 CLOSED
    end-to-end -- both slices land.'
  pr: '#1132, #1133'
  council: 'CONVENED 2026-07-27 (roadmap-continue, dyadic council-orchestrator pass, C10 + C1
    co-primary). Probe-first discipline run BEFORE deliberation falsified this row''s own
    premise: Runtime-owned persona/engine-class placement-resolution logic is NOT opaque to
    harness-cp for the scenario in scope -- step_context.hitl_placements (via
    fold_step_hitl_placements) and StepEffectiveBinding.removed_placements are BOTH CP-native,
    deterministic folds already computed at every dispatch/resume call; the composer
    (hitl_gate_composer.py) only CONSUMES the CP-folded value. Only the gate-LEVEL computation
    (persona_tier x blast_radius x mcp_trust_tier) is genuinely Runtime-resolved, and that is a
    function of deployment config, not the workflow-body edit this row''s reachability
    precondition describes -- so NO new cross-axis seam is needed. TENSION (narrow
    placement.position-only hash vs. broader full-HITLPlacement-tuple hash) probe-resolved:
    HITLPlacement is itself a frozen 4-field Pydantic model already inside the same tuple, so
    hashing all 4 fields (position/tool_filter/cascade_policy/timeout) + removed_placements
    costs nothing extra over the narrow framing -- both voices converged on the broader hash.
    RECOMMENDATION: persist a SHA-256 content hash over (ordered HITLPlacement tuple +
    sorted removed_placements) on the existing pause-capture carriers
    (PausedChildBranchResumeState / PreDispatchGateOwningBranchResumeState, drop-when-None
    byte-compat, mirroring the B-31 child_workflow_id / B-72 step_kind precedent exactly) +
    a new analogous carrier for the LINEAR resume_at site (which the probe found has NO
    per-step identity check at all today, not even step_id -- a slightly larger gap than the
    fan-out sites); companion fix owed at hitl_gate_composer.py''s Step 0 short-circuit
    (~1684-1750), which never gates delivery on placement drift today. FIX SIZE:
    mechanical-with-a-thin-design-decision, not a genuine architectural task. CLASSIFICATION:
    Class 1 -- every prior material-diff identity addition to these same carriers (step_kind;
    child_workflow_id at B-31/v1.99; the B-72 pre-dispatch-gate-owning identity itself at
    v1.107->v1.108) went through a CP spec-leg amendment before impl per C-CP-26 PauseResume
    Protocol authority; this is architecturally identical in kind, not bug-fix-shaped impl
    discretion. NEXT ACTION: open a CP spec-leg (fork doc or B-72-lineage follow-on) proposing
    the additive field(s) + a new §1.x invariant mirroring property 6''s shape, before any
    impl.

    SPEC LEG LANDED 2026-07-27 -- CP spec v1.110 §1 property 7 + CP plan v2.46 (two-property
    delta, co-designed with B-80). Every load-bearing call-graph claim from the council
    convening was independently re-verified at spec-authoring time before drafting (per this
    session''s own B-71 lesson): PreDispatchGateOwningBranchResumeState''s exact field shape,
    HITLPlacement''s exact 4-field frozen/hashable shape, fold_step_hitl_placements''s CP-native
    fold, removed_placements'' CP-native origin, and the Runtime composer''s Step 0 unconditional-
    delivery shape (hitl_gate_composer.py:1684-1750) all confirmed exactly as the convening found.
    Property 7 requires: gate-configuration identity validated unchanged at pause vs resume, at
    the fan-out closure''s pre-dispatch-gate-owning delivery site, the LINEAR resume_at site,
    the EVALUATOR_OPTIMIZER site, and the DECENTRALIZED_HANDOFF site (all 3 sequential sites
    have no per-step identity check of any kind today -- a net-new carrier, shared or
    per-site, is authorized). 4 out-of-family codex findings caught before merge and fixed
    across 2 rounds: (1) [P1] round 1: the initial draft also authorized a field on
    PausedChildBranchResumeState -- WRONG, that carrier only forwards child_resume_snapshot
    to a recursively-dispatched child and never itself constructs a HITLDeliveryCell, so a
    hash there would reject valid resumes on an unrelated parent-placement change without
    closing any real gap; corrected to scope property 7''s carrier authorization to
    PreDispatchGateOwningBranchResumeState only (the child''s own gate identity is covered
    recursively at its own execute_workflow() call); (2) [P2] round 1: SS1.1(c) falsely
    claimed a C-CP-20 audit-ledger emission on rejection -- corrected to match
    _resume_body_mismatch''s actual existing behavior (RunResult.fail_class only, no
    audit-ledger write); (3) [P2] round 1: property 8''s plan text falsely claimed "zero
    cross-axis cascade" -- corrected (see B-80 close_out); (4) [P1] round 2: property 7''s
    scope entirely omitted the EVALUATOR_OPTIMIZER (workflow_driver.py:10789-10806) and
    DECENTRALIZED_HANDOFF (workflow_driver.py:14803-14820) delivery-cell construction sites
    (landed later by the B-78 impl leg, PR #1117) -- both confirmed by their own inline
    comments to mirror the LINEAR mechanism EXACTLY and share the identical gap; corrected to
    cover all 3 sequential sites uniformly. Impl leg (the hashing scheme, the new fields, the
    comparison wiring, the witness tests at all 4 sites) is a separate follow-on arc, not yet
    built. Clearance at .harness/clearance/spec-control-plane-v1-110-
    cleared-2026-07-27.md + implementation-plan-control-plane-v2-46-cleared-2026-07-27.md.'
  heading: '### B-79 · A resumed branch''s `HITLDeliveryCell` is constructed from `step_id`
    identity alone, so a HITL placement/gate-configuration change between pause and resume is
    not detected as a material diff *(surfaced at the B-72 impl leg, out-of-family Codex round 3
    (branch-vs-main), 2026-07-25; SPEC LEG + IMPL LEG SLICE 1 LANDED 2026-07-27 — PR #1132; IMPL
    LEG SLICE 2 LANDED 2026-07-27 — PR #1133, CLOSED)*'
- id: B-80
  title: 'The _any_fence_abort HITL-delivery suppression guard is scoped to the current fan-out level, not the true root of a nested resume tree'
  status: closed
  pr: '#1131'
  summary: 'Out-of-family Codex round 6 (branch-vs-main, B-72 impl leg PR #1116, 2026-07-25). The round-4 fix suppresses a pre-dispatch gate-owning peer''s HITLDeliveryCell construction when _any_fence_abort is true, but that value is computed from _recovered_effect_fence_paused, which is scoped to the CURRENT fan-out level''s own recovered branches only. When an effect-fence ABORT targets a DIFFERENT, nested pause subtree (e.g. a deeper HIERARCHICAL_DELEGATION level, or a sibling peer''s recursively paused child), a shallower level''s own _any_fence_abort stays false and its pre-dispatch gate-owning peer can still receive a delivery cell and dispatch before the run-level abort is honored -- resumed branches are processed sequentially, so an earlier pre-dispatch branch can consume its HITL response and execute ahead of a later subtree processing the abort. Confirmed PRE-EXISTING and NOT introduced by the B-72 impl leg -- _any_fence_abort itself was computed fan-out-locally by the B-70 impl leg (CP spec v1.107), and the pre-existing effect-fence-directive peer suppression already consumes that same narrow value; B-72''s round-4 guard mirrors the established pattern verbatim. Affects BOTH the pre-existing effect-fence directive suppression and the new B-72 HITL delivery gate, at all four fan-out dispatch sites (_execute_parallelization and _execute_orchestrator_workers, including HIERARCHICAL_DELEGATION''s recursive reuse).'
  close_out: 'SPEC LEG LANDED 2026-07-27 -- CP spec v1.110 §2 property 8 + CP plan v2.46
    (co-designed with B-79). Every load-bearing call-graph claim from the council convening
    was independently re-verified at spec-authoring time before drafting: HIERARCHICAL_
    DELEGATION''s recursion crosses CP->Runtime->CP via a fresh execute_workflow() call at every
    level (confirmed via direct grep of execute_workflow()''s own signature threading through
    _execute_orchestrator_workers/_execute_parallelization and child_workflow_runner.py''s
    ChildWorkflowRunner Protocol at lines 88-89/185-186/258-259), and TWO existing sibling
    values (hitl_uniform_fallback_eligible_run_id from B-39; effect_fence_uniform_fallback_
    eligible_key from B-70) already thread a root-computed signal through that identical
    channel. Also confirmed: compute_effect_fence_uniform_fallback_eligible_key is a PURE
    COMPUTED VALUE derived fresh from the already-persisted root_snapshot (workflow_driver.py:
    2873-2908), never itself stored -- so property 8 needs NO new durable carrier, unlike
    property 6/7. _any_fence_abort''s exact consume sites reconfirmed at workflow_driver.py:
    7999/8037 and 12257/12281. Property 8 requires: a THIRD sibling value computed once at
    depth-0 via the EXISTING _collect_effect_fence_idempotency_keys tree walk +
    _resolve_effect_fence_gated resolver, ORed (not replacing) into the level-local
    _any_fence_abort at both consume sites -- deliberately blanket scope, not cascade_policy-
    precise (PauseSnapshot carries no cascade_policy field at any level). Also specifies a NEW
    spec-prose reconciliation (§2.3): property 8''s signal, once true, TAKES PRECEDENCE over
    property 6''s §1.1(b) mandatory-delivery outcome -- property 6''s TEXT is unamended, only the
    circumstances under which it fires are now conditioned. Out-of-family codex [P2] caught an
    initial draft''s plan text falsely claiming "zero cross-axis cascade" for property 8 --
    WRONG, the new parameter DOES touch the Runtime-owned ChildWorkflowRunner Protocol
    (child_workflow_runner.py) exactly like its two sibling parameters already do; corrected to
    state accurately: no NEW crossing point is introduced (same established CP->Runtime->CP
    boundary the two siblings already use), but the touch is real, not absent. Impl leg (the new
    function, the parameter thread, the two OR-sites, the §1.1(b) precedence wiring, the witness
    test) is a separate follow-on arc, not yet built. Clearance at .harness/clearance/spec-control-plane-
    v1-110-cleared-2026-07-27.md + implementation-plan-control-plane-v2-46-cleared-2026-07-27.md.
    IMPL LEG LANDED 2026-07-27 at PR #1131 -- CLOSED end-to-end. Added compute_effect_fence_
    tree_wide_abort_present(root_snapshot, resume_context) -> bool (reuses the existing
    _collect_effect_fence_idempotency_keys tree walk + _resolve_effect_fence_gated resolver,
    filtering empty-string idempotency_key candidates before the ABORT scan -- out-of-family
    Codex [P2] round 1 -- since a "" key is never key-bound-resolvable at the real per-branch
    consult sites, which guard on `if (_branch_fence_key and ...)`). Threaded as a NEW additive
    parameter through execute_workflow -> _execute_workflow_body -> the 5 topology strategy
    functions -> StepExecutionContext (harness_cp.workflow_driver_types) -> harness-runtime''s
    sub_agent_dispatch.py -> child_workflow_runner.py''s ChildWorkflowRunner Protocol + composer
    -> the recursive execute_workflow call for a dispatched child -- the IDENTICAL channel its
    two siblings already use, confirmed no NEW cross-axis seam. ORed (never replacing) into the
    level-local _any_fence_abort at both _execute_parallelization and _execute_orchestrator_
    workers (the latter covers HIERARCHICAL_DELEGATION via its recursive reuse). 2 out-of-family
    Codex [P2] findings across 2 rounds, both fixed: (1) the empty-key filter above; (2) the
    runtime mock recorded the new kwarg without any test asserting it -- extended the
    established test_resume_context_forwarded_to_runner_for_downward_threading precedent test
    to assert it, mutation-probe-verified. 17 new/extended tests total (12 pure unit tests for
    the compute function incl. tree-wide-reach into a nested paused_child_branches subtree +
    empty-key filtering; 2 integration suppression witnesses, one per fan-out site, each with a
    negative control, direct-kwarg-injection pattern mirroring the B-70 sibling test file''s own
    established precedent for this exact parameter class; the extended forwarding-assertion
    test), all mutation-probe-verified. Full harness-cp (1804 passed) + harness-runtime (3089
    passed, 10 skipped) suites green; ruff + pyright strict clean. No design-substrate/** edit
    (spec leg already landed at PR #1129/#1130); no clearance marker owed.'
  council: 'CONVENED 2026-07-27 (roadmap-continue, dyadic council-orchestrator pass, C1 + C10
    co-primary). Probe-first discipline confirmed HIERARCHICAL_DELEGATION''s "recursion" is
    NOT same-stack-frame Python recursion -- each nesting level re-enters via a fresh
    execute_workflow() call across the CP->Runtime->CP boundary (RuntimeSubAgentDispatcher ->
    ChildWorkflowRunner -> execute_workflow), and TWO existing sibling values
    (hitl_uniform_fallback_eligible_run_id, effect_fence_uniform_fallback_eligible_key) are
    ALREADY computed once at the true depth-0 root and threaded verbatim through this exact
    crossing -- the identical problem shape B-70 already solved. RECOMMENDATION: a THIRD
    sibling value (effect_fence_tree_wide_abort_present: bool), computed once at depth-0 via a
    ~5-line helper reusing the EXISTING _collect_effect_fence_idempotency_keys tree walk +
    _resolve_effect_fence_gated resolver, threaded through the IDENTICAL channel as the two
    siblings, ORed (not replacing) into the level-local _any_fence_abort at both consume sites
    -- NO new cross-axis seam, NO new carrier field, satisfies this row''s own registered
    constraint exactly. TENSION (blanket tree-wide boolean vs. precise cascade_policy-chain
    walk) probe-resolved in favor of the blanket reading: (a) the already-shipped local-level
    rule this arc extends is itself cascade_policy-BLIND ("tier-agnostic FAILED" per CP spec
    v1.107 SS1.1) -- a cascade_policy-aware tree-wide extension would be MORE discriminating
    than the shipped convention it extends, out of family; (b) PauseSnapshot carries ZERO
    cascade_policy field anywhere in its schema (verified directly) -- the precise reading
    would need a NEW durable carrier field, a strictly bigger, separately-scoped task beyond
    this row''s own close-out text. C1''s precision concern is registered as a named,
    deliberately-declined refinement, not silently dropped -- revisit only if a real
    production case demonstrates non-negligible over-suppression cost under
    cascade_policy=proceed nesting. GENUINE NEW FINDING (not resolvable as pure impl
    discretion): the fix surfaces an undocumented interaction between TWO existing CP-spec
    invariants -- SS1.1''s abort-suppression rule and SS1.2 property 6''s MANDATORY delivery for
    the sole-unaddressed pre-dispatch gate-owning branch (v1.108) -- since widening
    _any_fence_abort tree-wide widens the circumstances under which that mandatory delivery is
    skipped. Traced empirically: this does NOT reintroduce a livelock (suppression only
    delays a peer by one resume round; the run either terminates FAILED via the abort''s own
    path regardless, or a later round recovers the peer if the run turns out not doomed) --
    but the spec text as written does not currently reconcile SS1.1 against SS1.2 property 6 for
    the tree-wide case. FIX SIZE: mechanical (one new pure function reusing two existing
    utilities + a parameter threaded through an existing channel + a 2-line OR at each of 2
    consume sites + the already-specified witness test). CLASSIFICATION: Class 1, not Class 3
    -- mirrors the B-70/B-72 precedent (both routed through a CP spec delta + clearance marker
    + plan coverage-row before impl) exactly, and additionally owes the SS1.1/SS1.2-property-6
    reconciliation as new spec prose (likely a new SS1.1(c) or similar, noting the tree-wide
    suppression rule takes precedence over property 6''s mandatory-delivery obligation, with
    the delayed-not-lost liveness rationale this convening worked out). NEXT ACTION: route to
    a CP spec leg before any impl, co-designable with B-79 (both touch the same
    _resume_body_mismatch/pre-dispatch-carrier family). Not yet opened this pass.'
  heading: '### B-80 · The `_any_fence_abort` HITL-delivery suppression guard is scoped to the current fan-out level, not the true root of a nested resume tree *(surfaced at the B-72 impl leg, out-of-family Codex round 6 (branch-vs-main), 2026-07-25)*'
- id: B-81
  title: 'OW/HIERARCHICAL_DELEGATION test parity gaps remain for the B-72 pre-dispatch gate-owning carrier''s exclusion conjuncts and resume-side material-diff validation'
  status: closed
  summary: 'merge-gate 3-lens review round 2 (test-witness lens, B-72 impl leg PR #1116, 2026-07-25) found two coverage gaps after round 1''s OW HITLDeliveryCell-construction gap was closed by a dedicated regression test (test_ow_worker_resume_delivers_and_excludes_resolved_pre_dispatch_gate_owner in test_workflow_driver_fanout_pause.py): (1) the OW/HIERARCHICAL_DELEGATION mirror of _resume_body_mismatch''s pre-dispatch-gate-owning material-diff validation (workflow_driver.py, the FanOutResumeState-consuming closure near the PARALLELIZATION site''s ~7772-7831) has zero test coverage -- PARALLELIZATION got three dedicated witnesses (test_peer_resume_rejects_pre_dispatch_gate_owning_kind_changed, test_peer_resume_rejects_pre_dispatch_gate_owning_workflow_id_swap, plus the INFERENCE_STEP round-5 regression test) but the OW mirror has none; (2) the new exclusion tests (both the PARALLELIZATION and OW variants) only exercise the completed/terminal_dispositions arm of the carry-forward filter''s three exclusion conjuncts -- the paused_child_dispositions and effect_fence_paused_dispositions arms remain completely unwitnessed at BOTH construction sites (workflow_driver.py ~9958-9965 and ~14036-14043). Per advisor() consultation during the merge-gate loop: these are guard-hardening on symmetric, already-reviewed code (confirmed structurally identical to the tested PARALLELIZATION paths by the concurrency lens across 2 rounds), not the load-bearing delivery outcome round 1''s finding was -- registered as a follow-on rather than blocking this PR''s merge a third time. CLOSED 2026-07-26 (step 1/2 at PR #1118, step 2/2 at PR #1119). Step (1) ported the 3 PARALLELIZATION material-diff tests to their OW mirror in test_workflow_driver_fanout_pause.py, mutation-probe-verified. Step (2) added the 4 remaining exclusion-conjunct tests (paused_child_dispositions + effect_fence_paused_dispositions arms, at both PeerFanOutResumeState and FanOutResumeState construction sites) -- each recovers a pre-dispatch gate-owning branch, delivers its HITLDeliveryCell, and drives the dispatch into a REAL nested child pause (via a faithful RuntimeSubAgentDispatcher double dispatching a real execute_workflow that itself pauses) or an effect-fence-ambiguous outcome, proving exclusion from pre_dispatch_gate_owning_branches. All 7 tests total mutation-probe verified against both construction sites, confirmed byte-identical (grepped before starting step 2, per advisor()''s direction, to rule out a production asymmetry before treating this as pure test-parity work). merge-gate 3-lens all-approve at round 1 on both PRs; just codex-review converged clean at round 1 on both. merge-gate test-witness lens on PR #1119 found a docstring accuracy nit (the paused-child dispatcher docstrings overclaimed hitting the direct own-dispatch exception handler when real thread-scheduling timing more likely hits the in-flight-cancellation-race handler; both write the same disposition dict identically, so mutation-probe validity was unaffected) -- fixed inline, no re-gate needed.'
  pr: '#1118, #1119'
  close_out: '(1) Port test_peer_resume_rejects_pre_dispatch_gate_owning_kind_changed + test_peer_resume_rejects_pre_dispatch_gate_owning_workflow_id_swap + the INFERENCE_STEP round-5 regression test to test_workflow_driver_fanout_pause.py against _execute_orchestrator_workers''s own _resume_body_mismatch closure (worker_steps-based, not steps-based). (2) Add a paused_child-resolves-this-round exclusion test (a recovered pre-dispatch gate-owning branch''s delivered cell triggers a REAL nested execute_workflow dispatch that itself pauses, landing the branch in paused_child_dispositions instead of terminal_dispositions) and an effect_fence-resolves-this-round exclusion test (the branch''s delivered cell leads to an effect-fence-ambiguous dispatch), at BOTH the PeerFanOutResumeState and FanOutResumeState construction sites -- 4 new tests total. Mutation-probe each by deleting the specific exclusion conjunct it targets, mirroring the discipline already applied to the completed-arm tests this PR shipped.'
  council: 'No -- mechanical test-coverage parity against an already-established pattern (mirror an existing PARALLELIZATION test to its OW twin; add the 2 remaining exclusion-conjunct arms using the same shape as the completed-arm test this PR already ships). Not a design decision.'
  heading: '### B-81 · OW/HIERARCHICAL_DELEGATION test parity gaps remain for the B-72 pre-dispatch gate-owning carrier''s exclusion conjuncts and resume-side material-diff validation *(surfaced at the B-72 impl leg, merge-gate 3-lens review round 2, 2026-07-25; CLOSED 2026-07-26 -- PR #1118 + PR #1119)*'
- id: B-82
  title: 'Ollama dispatch branch never serializes the standard memory tools its selected access mode promises'
  status: closed
  pr: '#1140'
  summary: '`reflect_memory_provider_capabilities` reports `supports_standard_memory_tools=True` for ollama (verified empirically 2026-07-27), so `select_memory_access_mode` (`harness-cp/src/harness_cp/memory_access_mode.py:190-200`) returns `MemoryAccessMode.STANDARD_MEMORY_TOOLS` for an ollama binding under a permissive policy. But the ollama arm of `RuntimeLLMDispatcher.dispatch` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`, the `elif provider_name == "ollama":` branch) has no standard-memory-tools counterpart to the openai arm''s `_dispatch_openai_with_standard_memory_tools` -- it calls the bare `_dispatch_ollama` unconditionally. The five C-MEM-14 tools the selected access mode promises are therefore never serialized into the ollama request and no continuation loop exists to execute a returned tool call; the mode silently degrades to no memory access at all. NOT a wire-name defect (`ollama.chat` was never handed a dotted name because it was never handed a memory tool), but its fix is COUPLED to the OpenAI wire-name map: injecting the tools into ollama without routing them through an adapter-owned wire encoding would extend the OpenAI HTTP-400 failure class (dotted `function.name` rejected by `^[a-zA-Z0-9_-]{1,64}$`) to any name-validating ollama build or downstream OpenAI-compatible shim. Discovered 2026-07-27 during the gate-(d) R-300 live re-confirmation, while diagnosing the OpenAI 400 that the wire-name map closes. Class 3 doc-clarity observation surfaced at the same time: C-MEM-14''s tool-name table (`Spec_Memory_Substrate_v1.md` lines 485-497) reads as identity-normative, and the per-provider WIRE ENCODING of those identities is adapter-owned (NOT a spec-level rename) -- the enum stays dotted and provider-neutral; only the OpenAI boundary re-spells it.'
  close_out: '(1) Decide the ollama shape: `ollama.chat` accepts an OpenAI-compatible `tools=` array, so the most likely fix is an `_dispatch_ollama_with_standard_memory_tools` sibling reusing the same tool-schema builder + continuation loop, with the wire-name map (or an ollama-specific equivalent) applied at serialization AND at inbound resolve. Ground the actual ollama tool-call response shape against a real local daemon before writing the loop -- do not assume it is byte-identical to OpenAI''s `choices[].message.tool_calls[]`. (2) Alternatively, if standard memory tools are deliberately out of scope for ollama, the FIX IS IN THE OTHER DIRECTION: `reflect_memory_provider_capabilities` must stop reporting `supports_standard_memory_tools=True` for ollama so the selection degrades explicitly to `PROMPT_EXTENSION_PACKET` rather than silently to nothing. Either way the current state -- a selected mode the dispatch branch does not serve -- is a fail-silent gap. (3) Witness with a deterministic test that captures the kwargs handed to a fake `client.chat` and asserts either the tools are present under provider-safe names, or that the selected access mode is not `STANDARD_MEMORY_TOOLS` for an ollama binding. CLOSED 2026-07-27 (PR #1140) via direction (1); the wire-name coupling this row asserted is DISSOLVED by grounding -- a live probe (ollama==0.6.2, llama3.2:3b) confirmed `/api/chat` accepts the DOTTED C-MEM-14 identities verbatim and echoes them back unchanged, so there is NO wire-name map on this path and adding one would encode a false capability claim. Direction (2) REJECTED: ollama genuinely supports tool calling, so narrowing `reflect_memory_provider_capabilities` would amputate the C-MEM-14 write half for the whole local-open-weight family -- a C-MEM-13 selection-semantics regression, not a fix; the row''s own "YES if grounding shows option (2)" council condition therefore never fired. Shipped `_dispatch_ollama_with_standard_memory_tools` + 6 sibling helpers mirroring the OpenAI arm''s loop SHAPE (max_iterations=16, shared `_standard_memory_tool_context`, deepcopy-before-mutate schema discipline, `LLMDispatchPayloadShapeError` fail-loud convention) against ollama''s different wire shapes: no `choices[]` envelope (top-level `ChatResponse.message`, ollama/_types.py:418), `arguments` already decoded as a Mapping (_types.py:346, no json.loads on the happy path), and NO `tool_call_id` anywhere in `Message` (_types.py:304-355) -- the tool-result turn correlates by name as {"role":"tool","tool_name":"<dotted>","content":"<json>"}. Malformed-call semantics deliberately MIRROR OpenAI (fail-loud `MemoryToolExecutionInputError` propagation); a feed-back-as-error-turn recovery was rejected as a per-provider design extension. 6 deterministic tests + 2 mutation probes. Out-of-family Codex round 1 then found TWO real durable-double-write defects in the new loop, BOTH of which the mandatory sibling check confirmed the OpenAI arm has shared since it shipped -- fixed SYMMETRICALLY in this PR: [P1] a later invalid call was validated only AFTER earlier (possibly write-like) calls had executed, so the abort left durable memory changed; both loops now resolve+validate the COMPLETE batch via `_openai_prepared_memory_tool_calls`/`_ollama_prepared_memory_tool_calls` before executing any of it. [P2] the final iteration executed its batch and only THEN raised loop-exhausted, committing a write for a dispatch that reports failure; both loops now raise BEFORE executing once no iteration budget remains to send results back. Both aborts classify TRANSIENT_RETRY (`retry_breaker_fallback.py:299-304` fail-fasts only ProviderUnreachable/PayloadShape/401-403), so `RetryBreakerFallbackDispatcher` re-ran the whole dispatch and the committed call executed twice -- that retry classification is what made these P1/P2 rather than cosmetic. The exhaustion error type is DELIBERATELY unchanged (the same generic `RuntimeError` both loops always raised, now via the shared `_memory_tool_loop_exhausted` helper); re-typing it would change retry routing, a separate decision from the execute-before-raise defect. 4 further witnesses (batch-pre-validation + iteration-bound, one pair per provider) each mutation-probed. Codex round 2 then found TWO MORE [P1]s, both again PRE-EXISTING on the OpenAI sibling and fixed symmetrically: [P1-a] the dispatch-side context builder required `arguments["scope_ref"]` for EVERY tool, but `memory.read`/`memory.propose_promotion`/`memory.request_redaction` declare no `scope_ref` AND set `additionalProperties: false` (`harness-as/src/harness_as/memory_tool_contracts.py:181-217`, `:250-283`, `:284-...`), so a schema-conformant model CANNOT send one -- three of the five ADVERTISED tools failed before ever reaching the executor. Latent on OpenAI since the memory loop shipped (tests only exercised `memory.search`). The check is now scoped to the two contracts that declare the field, mirroring BOTH `_bind_schema_fixed_value`''s properties-presence authority AND the executor''s own partition -- `StandardMemoryToolExecutor._require_scope_ref` is reached only from `_search`/`_write_note` (`memory_tool_executor.py:195`/`:262`); the authoritative value was always `RuntimeMemoryContext.scope_ref` anyway, the argument being only a cross-check. [P1-b] a model call to a legitimate caller-advertised NON-memory tool (preserved by the dedup filter) hit the memory resolver and raised, so enabling automatic memory broke ordinary tool workflows. Both loops now classify the batch first: if ANY call names something outside our advertised memory identities, the whole response returns to the caller unprocessed with ZERO memory calls executed -- bare-path parity (`_dispatch_openai`/`_dispatch_ollama` return every tool_call untouched whatever it names, so hallucinated names go back to the caller too rather than becoming a new failure mode that appears only under automatic memory) plus batch atomicity. Raises are now reserved for MEMORY-named calls with malformed payloads. 6 further witnesses (memory.read without a scope_ref argument; mixed memory+caller batch; caller-only batch -- one triple per provider), both fixes mutation-probed. Codex round 3 added two bounded [P2]s, again symmetric: [P2-a] round 2''s bound check ran BEFORE batch preparation, so a malformed memory call in the TERMINAL batch surfaced as the generic (TRANSIENT_RETRY) exhaustion `RuntimeError` instead of the fail-fast `LLMDispatchPayloadShapeError` -- replaying up to 16 provider calls for a request that can never succeed; both loops now validate the batch, THEN check the bound, THEN execute (the caller-owned return stays ahead of all three). [P2-b] four test fixtures passed `text` to `memory.write_note`, whose contract requires `note` (`memory_tool_contracts.py:218-249`); all four corrected, and one witness now runs through a REAL `StandardMemoryToolExecutor` over a tmp_path filesystem store (built the same way `tests/test_memory_tool_executor.py::_executor` builds it) because required-argument-key enforcement lives in the EXECUTOR (`memory_tool_executor.py:266` -> `_string_arg` at `:549-553`), NOT at the dispatch layer -- no new dispatch-side validation semantics were added. 4 further witnesses (a malformed TERMINAL batch fails fast rather than as exhaustion, per loop; a real-executor write_note round trip committing a durable record; the same call with the wrong required key failing loud at the executor), each mutation-probed -- reverting the reorder reproduces the masking `RuntimeError: ... exceeded 16 continuation turns` verbatim, and restoring the `text` key fails at `memory_tool_executor.py:552`. Codex round 4 closed one more [P2] and registered one bounded residual: the loops bundled usage from only the FINAL response, so every continuation turn''s tokens vanished from turn capture / gen_ai.usage.* / cost attribution -- the OpenAI arm shared the shape verbatim (`_openai_response_bundle` read the final response''s `.usage`), and both now accumulate per-turn input/output counts across every provider call in the loop (None means ABSENT, never 0; `response_id` is NOT accumulated -- it identifies one response, so the final turn''s is preserved). Witnessed by extending both existing loop tests, whose two calls already carry distinct counts (10/4 + 12/5 on OpenAI, 20/8 + 31/9 on ollama) so the sums are discriminating against both final-turn-only and first-turn-only readings; mutation-probed. THE BOUNDED RESIDUAL IS REGISTERED AS `B-84`: an executor-level argument failure (e.g. `memory.read` missing `memory_ref`) still occurs MID-batch during sequential execution, so a valid earlier write commits before the later call raises, and the transient classification re-runs the dispatch and duplicates the write. Deliberately NOT fixed here -- dispatch-side full-contract prevalidation would mirror-copy the executor''s own semantics, the drift class this workspace treats as a defect. Codex round 5 closed one more bounded regression: `reflect_provider_capabilities` returns supports_tools=True for EVERY ollama model string (`harness-cp/src/harness_cp/provider_capabilities.py:127`, pre-existing), so the new branch sent `tools=` to models with no tool template (e.g. llava-llama3) and the daemon 400''d what had been a working TEXT dispatch. Fixed with the DAEMON as the authority rather than a per-model capability table (a real design question, out of scope): on the FIRST chat call of the memory loop only, a tool-unsupported rejection re-dispatches through the bare `_dispatch_ollama` WITHOUT tools, byte-restoring the pre-B-82 path for exactly those models (SUPERSEDED at B-83/PR #1142: the retry remains tool-free — the daemon fix is intact — but now carries the C-MEM-12 packet folded into the single system source when the B-83 repair is authorized); a mid-loop rejection stays fail-loud. Both SDK facts probed LIVE, not assumed: `ollama.ResponseError` (`ollama/_types.py:632-651`) carries only `.error` + `.status_code` (no machine-readable discriminator), and on ollama 0.6.2 the tool rejection is `.error` containing "does not support tools" with status 400 while model-not-found is the SAME type with status 404 -- so a blanket `except ResponseError` would swallow unrelated faults and the message substring is the only available discriminator. The string-match brittleness is documented at the catch site; an upstream rewording fails CLOSED (a loud dispatch failure), never silently absorbing a different error. The predicate duck-types on class name + `.error` substring rather than importing `ollama`, keeping the module free of provider-SDK imports; the TESTS use the real `ollama.ResponseError`. The OpenAI loop needs NO equivalent and none was added -- every OpenAI chat model supports tools, so a mirrored catch would be dead code. 3 witnesses, each mutation-probed against a distinct conjunct (remove the fallback; widen the catch to any ResponseError; drop the first-call restriction). A fourth round-5 observation -- the SDK silently dropping declared schema constraints -- was verified, judged an AGGRAVATOR of an existing registered class rather than a new defect, and recorded on `B-84`. Full harness-runtime non-e2e suite green (3123 passed / 10 skipped).'
  council: 'No for the mechanical half (mirroring an existing, already-reviewed openai dispatch arm). YES if grounding shows option (2) is the right answer -- narrowing a declared provider capability changes which memory access mode an entire provider family receives, which is a C-MEM-13 selection-semantics decision, not impl discretion.'
  heading: '### B-82 · Ollama dispatch branch never serializes the standard memory tools its selected access mode promises *(surfaced at the gate-(d) R-300 live re-confirmation, 2026-07-27; CLOSED 2026-07-27 — PR #1140)*'
- id: B-83
  title: 'No dispatch-side guard that the selected STANDARD_MEMORY_TOOLS access mode is actually served by the provider arm reached'
  status: closed
  pr: '#1142'
  summary: 'Surfaced by the B-82 fix (2026-07-27). B-82 was one instance of a general shape: `select_memory_access_mode` (`harness-cp/src/harness_cp/memory_access_mode.py:158-245`) picks an access mode from DECLARED provider capabilities, and `RuntimeLLMDispatcher.dispatch` then routes to a provider arm -- but nothing checks that the arm reached actually serves the mode selected. With B-82 closed, both arms the DEFAULT `reflect_memory_provider_capabilities` can select STANDARD_MEMORY_TOOLS for (openai, ollama) now serve it, so the defect is not reachable through default reflection. It remains reachable two ways: (a) a caller supplying `MemoryAccessModeRequest.provider_capabilities` explicitly (the `request.provider_capabilities or reflect_...` short-circuit at memory_access_mode.py:163-166) can declare supports_standard_memory_tools=True for anthropic or an external-CLI binding, whose dispatch arms ignore memory tools entirely (the anthropic arm branches only on `step_has_memory_tool(payload.tools)`/`hitl_tool_loop`, never on access mode; `_payload_to_external_cli_prompt` is text-only by contract); (b) more importantly, a STATICALLY INJECTED `RuntimeLLMDispatcher.memory_context` (the no-`memory_runtime` path at `llm_dispatch.py:1302`) is composed ONCE and is NOT recomposed when `RetryBreakerFallbackDispatcher` rebinds the provider per attempt (`retry_breaker_fallback.py:188-191` builds a fresh StepEffectiveBinding with a different provider) -- so a context composed for openai under STANDARD_MEMORY_TOOLS can be dispatched against an anthropic or external-CLI fallback and degrade to zero memory access, silently. The `memory_runtime`-bound path is NOT affected (it recomposes per attempt via `compose_for_dispatch`). GROUNDING NOTE -- what this row is NOT: the same pass also checked whether C-MEM-13''s "no_memory_access is a valid policy outcome and must be ledgered when memory was requested" (`Spec_Memory_Substrate_v1.md:463`) lacks a ledgering witness. It does NOT: `MemoryContextComposer.compose_run_start` ledgers the denial via `_write_injection_decision` and emits a DENIAL span with POLICY_DENIAL failure class (`harness-runtime/src/harness_runtime/memory_context.py:165-205`), witnessed at `harness-runtime/tests/test_memory_context.py:364-388`. That candidate is recorded as CHECKED-AND-CLOSED, not registered, so a future reader does not re-open it. REACHABILITY (c), ADDED 2026-07-27 CORRECTING THIS ROW''''S OWN EARLIER CLAIM: this row originally asserted the defect was "not reachable through the DEFAULT capability reflection". That is FALSE, falsified by the B-82 round-5 fallback filed the same day. `reflect_provider_capabilities` returns supports_tools=True for every ollama model string (`harness-cp/src/harness_cp/provider_capabilities.py:127`), so a model with no tool template (e.g. llava-llama3) selects STANDARD_MEMORY_TOOLS through PLAIN DEFAULT REFLECTION and the round-5 fallback then deliberately re-dispatches it bare -- no memory access at all, and NO LEDGER TRACE of the degradation: exactly this row''''s shape, on the common no-special-configuration path. The fallback remains correct for its own problem (it byte-restores a previously-working text dispatch, and the daemon is the only authority on which models take tools); it simply makes B-83 default-reachable rather than reachable only via (a)/(b), which RAISES this row''''s priority. ADJACENT, SAME FAMILY: the caller-owned mixed-batch return (B-82 round 2) hands a mixed response back whole for batch atomicity + bare-path parity, so the memory call the model asked for is not served and nothing records that it wasn''''t -- the caller cannot execute it either, not owning the memory tools. A consequence of a deliberate tradeoff rather than an independent defect, so it is scoped under this row: whatever posture close-out step (1) settles on should cover that exit path too. CLOSED 2026-07-28 (PR #1142) — dispatch-time serve-check + repair-to-packet, covering all three reachabilities from ONE authority. `_standard_memory_tools_context` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) is now the single predicate answering "will THIS dispatch inject the C-MEM-14 tools?" — read by both arm guards AND by the serve-check, so the branch and the check cannot disagree; its provider-identity conjunct (`selection.selected_provider != provider_name`) DETECTS (b), since `MemoryAccessModeSelection.selected_provider` is `request.model_binding.provider` verbatim (`harness-cp/src/harness_cp/memory_access_mode.py:372`) and the `memory_runtime` path recomposes per attempt so the conjunct is inert there. TWO DISPOSITIONS, split at out-of-family Codex round 1 [P1-a]: (a) and (c) are REPAIRED -- the context was composed FOR the dispatched provider, so its packet is provider-matched by construction and discloses nothing the selection had not already cleared; (b) is REPORT-ONLY -- neither the tools NOR the packet reach the rebound provider, because retrieval and injection enforce PROVIDER-FAMILY scope before ranking (`Spec_Memory_Substrate_v1.md:481`) and the packet was assembled under provider X''s `MemoryScope.provider_family`, so forwarding its TEXT to a Y-family provider would bypass that scope boundary (a DISCLOSURE leak, which read-only framing does not fix -- distinct from, and not covered by, the write-authority argument this row originally gave). Recomposition is impossible on that path by construction (it is definitionally the `memory_runtime`-UNBOUND configuration) and fail-closed would convert a silent degradation into a run failure, so the degradation is made VISIBLE in telemetry rather than repaired. On the REPAIRED dispositions, the already-retrieved, already-policy-checked C-MEM-12 packet is rendered into the effective system prompt via a new `render_packet_for_degraded_dispatch` sibling in `memory_context.py` (same body as `render_prompt_extension_packet` through a shared `_render_packet`; only the access-mode gate is dropped, so existing callers are byte-identical) and appended through the shared `append_rendered_memory_packet` seam. Reachability (c) — the ollama tools-unsupported fallback — repairs at its own site and reports back on a third return element so ONE emitter covers every case. ONE `memory.operation` span per degraded dispatch, every attribute reporting what ACTUALLY happened so the two dispositions are distinguishable without parsing a reason string: REPAIRED emits operation_name=injection / operation_kind=inject / access_mode=prompt_extension_packet / the SERVED packet_hash + record_count, no failure_class (a successful injection through a different seam); REPORT-ONLY emits operation_name=denial / operation_kind=inject / access_mode=no_memory_access / record_count=0 / NO packet_hash (claiming a served hash for a withheld packet would be a lie), mirroring `compose_run_start`''s own DENIAL+INJECT shape for the no-memory outcome. failure_class is deliberately UNSET on both -- no member of the closed `MemoryTelemetryFailureClass` describes a provider-scope mismatch, and reusing POLICY_DENIAL would assert a decision no resolver made. `memory.degraded_serve` + `.reason` (`arm_unservable` | `provider_scope_mismatch`) + `.selected_access_mode` carry the B-83 detail, on the MEMORY tracer''s span only (C-OD-04 §4.3 namespace discipline); the reasons are span-attribute VALUES, never new enum members. Spec anchors verified byte-exact: C-MEM-14 `Spec_Memory_Substrate_v1.md:489` states the exposure as a present-tense obligation; C-MEM-13 `:460` authorizes the packet for providers without usable tool support. POSTURE CHOSEN: repair-to-packet, NOT the fail-loud raise close-out step (1) leaned toward — a raise turns a currently-silent degradation into a run failure for any deployment on the injected-context + fallback shape, and C-MEM-13:460 already sanctions the packet, so the conservative reading serves memory rather than killing the run. NOT BUILT, deliberately, and recorded so nobody silently absorbs it later: the literal "explicit LEDGERED degradation" half of close-out (1). Writing a degradation ENTRY (as opposed to the span emitted here) is fork-shaped — the load-bearing blocker is the absent store carrier on the dispatch path (a new dispatcher-visible Protocol/field surface), an X-AL-3 design extension. (Qualified at the merge-gate: reusing the run-start DENIAL+INJECT shape needs no new enum members — compose_run_start already ledgers a no-memory outcome with existing members; new members on the three closed enums (`MemoryOperationKind`, `MemoryTelemetryOperationName`, `MemoryTelemetryFailureClass`) would be needed only for a dispatch-time-DISTINCT operation kind.) BEHAVIOR CHANGE, flagged: the B-82 round-5 ollama fallback no longer BYTE-RESTORES the pre-B-82 bare dispatch — the retry still drops the tools (which is what the daemon rejected) but now carries the packet as read-only prompt text; the R5 comment block and its witness (`test_ollama_tool_unsupported_model_falls_back_to_the_tool_free_path`, renamed) were updated to the new contract rather than deleted. OUT-OF-FAMILY CODEX ROUNDS 1-7 caught TEN real findings across FOUR defect shapes -- packet SCOPE, single-system-SOURCE folding, the repair''s own AUTHORIZATION, and the OBSERVABILITY of the decision -- all fixed on this branch, all reproduced empirically before fixing. ROUND 7 (final fix round before merge-gate), one [P2] at the intersection of the last two surfaces: [P2-e] the span always reported `packet_policy_denied` whenever the packet fallback was ineligible, because the carrier was a BOOLEAN -- so a run denied by an EMPTY TOKEN BUDGET, or by an absent provider capability, was reported as an operator POLICY decision. That is a false accusation about a configuration the operator never made, and it points any investigation at the wrong file. Fixed by replacing `RuntimeMemoryContext.prompt_packet_fallback_eligible: bool` with `prompt_packet_fallback_denial: str | None` -- None = authorized; otherwise the counterfactual selector''s OWN `MemoryAccessModeDenialReason` value, carried verbatim to the span. GROUNDED CORRECTION to the round-7 brief''s hypothesis: it anticipated three distinguishable causes (policy / budget / capability), but the selector yields only TWO reachable values -- `token_budget_empty` for the budget gate, and `no_supported_mode` for BOTH a denying `injection_access` AND an absent `supports_prompt_extension_packet`, which it does not separate once past its packet branch. Verified empirically across all five configurations before writing a line. The fix therefore removes the FALSE distinction without inventing one the selector cannot observe -- a witness asserts that non-distinction explicitly so a later reader does not mistake it for a gap. The fail-safe default is a `not_derived` sentinel, asserted DISJOINT from every real `MemoryAccessModeDenialReason` value, so an underived context is never mistaken for a gate that actually closed. ROUND 6, one [P2]: [P2-d] the degraded-serve span was emitted from a POST-dispatch block, so whenever the provider call raised (timeout, adapter error, the bare ollama retry itself failing) control left before emission and the only B-83-specific signal vanished -- even though by then the packet had already been SENT (the disclosure happened) or already been WITHHELD (the denial happened). Fixed by moving emission into a `finally` around the whole arm-dispatch chain, with a `memory.degraded_serve.dispatch_outcome` attribute (`completed` | `provider_error` | `cancelled`) plus `memory.degraded_serve.provider_error_type` on failure. Cancellation is labelled separately rather than blamed on the provider; the enclosing `gen_ai` span already records the exception itself via OTel''s `record_exception` / `set_status_on_exception` defaults, which this module relies on everywhere rather than calling explicitly, so the short degraded span carries the class as an attribute instead. SECOND HALF, and the reason round 2''s return-bundle choice had to be REVERSED: the R5 fallback decides its degradation INSIDE the arm call and returned it as a tuple element -- which travels ONLY on success, so an R5 retry that itself failed lost the report entirely, and the arm site''s own local was None on that path (the serve-check returns None for a servable arm). Replaced with a `_DegradedMemoryServeSink` single-slot carrier written the moment the disposition is settled, BEFORE the retry it describes. Both halves are separately load-bearing and separately probed. RESIDUAL EXIT PATHS, stated plainly: `asyncio.CancelledError` propagates THROUGH `finally`, so the span is still emitted (witnessed, not assumed); the same holds for any BaseException. The only losses left are process-level -- SIGKILL, a hard interpreter crash, or an OTel exporter that drops the span downstream -- none of which any in-process placement can address, and all of which lose the enclosing `gen_ai` span too, so the degraded span is no weaker than the dispatch telemetry it accompanies. ROUND 5, one [P1]: [P1-e] the repair rendered the packet WITHOUT evaluating the prompt-packet mode''s own gates. `select_memory_access_mode` returns at the FIRST mode whose gates pass, so a STANDARD_MEMORY_TOOLS selection proves NOTHING about PROMPT_EXTENSION_PACKET -- that branch was never evaluated. A policy allowing `standard_tool_access` while `injection_access` denies prompt packets is legal and expressible (DENY is the field default, and the in-repo test `test_external_cli_route_metadata_composes_with_standard_tool_context` already builds exactly that shape), and the packet branch additionally requires `token_budget > 0` which the tools branch does not -- so the degradation could disclose memory through a mode the operator had switched off, on an unbudgeted run. AUTHORITY GAP, stated plainly: the needed inputs (`workflow_policy` / `step_policy` / `token_budget` / `provider_capabilities`) live on the COMPOSITION request and are on NEITHER `RuntimeMemoryContext` NOR `MemoryAccessModeSelection`, so the dispatcher cannot answer the question at dispatch time. Rather than mirror-copying policy predicates into harness-runtime (the drift class this workspace treats as a defect) or parsing `decision_trace` strings, the answer is settled at COMPOSITION -- where the inputs are in scope -- by RE-RUNNING the real `select_memory_access_mode` with native+standard-tools capabilities masked off (the exact counterfactual the repair faces) and carried forward on a new additive `RuntimeMemoryContext.prompt_packet_fallback_eligible` field. The probe is pure (the selector is documented "without touching credentials") and composes the WHOLE decision tree in canonical order with zero duplicated logic. The field DEFAULTS FALSE: a context assembled anywhere but `compose_run_start` has not proven eligibility, and unproven reads as ineligible -- `compose_run_start` is the ONLY production producer (the static `RuntimeLLMDispatcher.memory_context` seam has no production wiring; `materialize_llm_dispatcher_stage` defaults it None), so production coverage is total while hand-built contexts fail safe. FIXTURE HONESTY, per the round-5 brief: the B-83 dispatch fixtures WERE silently relying on the unchecked path -- their implied `policy:u-mem-16` document leaves `injection_access` at its DENY default -- so the flag is now an EXPLICIT fixture parameter (default True, i.e. these witnesses model an `injection_access=PROMPT_PACKET` policy with a non-empty budget) instead of an unstated assumption. AUTHORIZATION NOW COMPOSES, and the set is EXHAUSTIVE against the selector''s own decision tree: the repair requires (1) provider IDENTITY, (2) provider FAMILY scope, (3) prompt-packet eligibility, which itself composes every gate on the path to PROMPT_EXTENSION_PACKET -- `policy.enabled`, `retrieval_access` DENY, external-CLI route/auth denial, `supports_prompt_extension_packet`, `injection_access` != PROMPT_PACKET, and `token_budget <= 0`. Verified mechanically by driving each of those six gates negative one at a time: three (`enabled`, `retrieval_access`, external-CLI auth) collapse the selection to NO_MEMORY_ACCESS, where `_packet_matches_mode` forbids a packet at all and the repair is unreachable BY CONSTRUCTION; the other three leave STANDARD_MEMORY_TOOLS selected (repair reachable) and the eligibility flag correctly reads False. There is no seventh gate on that path. ROUND 4, one [P2], the anthropic member of the fold-shape family: [P2-c] the fold asked "is there an existing system source?" by TRUTHINESS while the conflict check asks it by PRESENCE (`"system" in kwargs`). Two VALID shapes slipped through the gap -- the SDK content-block ARRAY (the same `[{"type": "text", ...}]` form `_anthropic_system_cache_block` itself emits) and the empty string -- landing in the `system`-variable branch, where the still-present `params["system"]` then collided and raised PromptInjectionConflictError: the repair converting a VALID dispatch into a failure. Fixed by keying the fold on `"system" in payload.params`, so the fold predicate and the conflict predicate agree on what "present" means, and by routing the value through the round-3 `_content_with_appended_packet` -- whose sequence branch appends a `{"type": "text", ...}` block, a valid Anthropic `TextBlockParam` (`cache_control` optional) -- so no divergent serialization was introduced. COMPLETENESS SWEEP (round 4 close-out, run through the REAL translators, 17 arm x shape combinations): for each of the four arms the FOLD predicate now mirrors that arm''s own CONFLICT predicate -- anthropic on `"system" in payload.params`; openai/ollama on a leading `role:"system"` in the POST-`params`-merge `kwargs["messages"]` (so a `params["messages"]` override is covered, matching `_inject_leading_system_message`); external-CLI on a leading `role:"system"` in `payload.messages` (that renderer ignores `params` for messages, so there is no post-merge divergence to cover) -- and every value shape reaching a fold is total: `_content_with_appended_packet` (str/None/sequence/other) for anthropic+openai+ollama, `_external_cli_message_content` (str/other) for the CLI. All 17 combinations kept the original text AND carried the packet, with no raise and exactly one system source. ONE residual, characterized not fixed: the "exactly one system source" property is INDEX-0-SCOPED on openai/ollama -- a `role:"system"` message at index>0 is seen by neither the conflict check nor the fold, so the fold prepends a leading one and the wire carries two system messages. That is the PRE-EXISTING conflict-check semantics (R-PM-1), not a B-83 regression, it raises nothing and replaces nothing, and OpenAI accepts multiple system messages. ROUND 3, both [P2], both in the fold helpers the round-2 fix introduced: [P2-a] OpenAI accepts a system-message `content` as either a plain string OR a content-part ARRAY, and the fold coerced it through `str()` before appending -- putting a Python repr (`"[{''type'': ''text'', ...}]"`, reproduced verbatim at the probe) on the wire and destroying the parts. Fixed with `_content_with_appended_packet`, which folds each shape in its own terms: string/None -> string append; a parts SEQUENCE -> ONE ADDITIONAL `{"type": "text", "text": ...}` part (the same part shape this module already emits at the Anthropic system block and the external-CLI response) with every original part preserved in order; any other shape -> JSON-serialized (`sort_keys` + `default=str`, matching `_inject_upstream_context_message`) then string-appended, so an unexpected shape is neither mangled nor raised on. GROUNDED AND SKIPPED for ollama: `ollama._types.Message.content` is `Optional[str]` (`ollama/_types.py:312`), str-only, so a parts array cannot reach the daemon through the typed client -- ollama shares the helper but can only take the string branch. [P2-b] the external-CLI fold passed None to the append helper for any non-string content, REPLACING the step''s own system instructions with the packet alone -- and since the external-CLI arm is text-only it ALWAYS degrades standard_memory_tools, making that the COMMON path there rather than an edge case. Fixed by extracting the renderer''s own serialization into `_external_cli_message_content` and reusing it at BOTH the render loop and the fold, so a repaired dispatch can never represent the same content differently from an unrepaired one. ROUND 2: [P1-c] the round-1 scope conjunct was IDENTITY-level while the disclosure boundary is FAMILY-level: on the `memory_runtime` path a cross-family fallback RECOMPOSES the context for the current candidate (so `selected_provider` matches and the identity conjunct passes) while `compose_for_dispatch` keeps deriving `record_scope.provider_family` from `fallback_chain.primary.family` (`automatic_memory.py:191-199`), and `MemoryRetriever._scope_mismatch` (`memory_retrieval.py:371-384`) filters records on exactly that field -- so a PRIMARY-family packet reached the ollama R5 retry and was rendered. Fixed by adding a family conjunct keyed on the packet''s ACTUAL `record_scope.provider_family` versus `provider_family_for_provider(provider)` (`cross_family_cost_tag.py:82` -- the workspace''s existing provider->family authority; a second mapping would be the mirror-copy drift class), reason `provider_family_scope_mismatch`, and both conjuncts now live in ONE shared `_degraded_serve_disposition` read by the arm-selection check AND the R5 site. An absent scope or absent family counts as NOT matching -- `_scope_mismatch` treats a None request family as "no constraint", so the packet may hold records of any family and unverifiable is not safe. This also exposed a FIXTURE LIE: the test helper hardcoded `provider_family=OPENAI` for every context regardless of provider, so anthropic/ollama witnesses were asserting a scope their provider did not have; the fixture now derives the family from the provider through the same authority. [P1-d] the ARM-SELECTION repair still appended the packet as a SECOND system source -- the exact class round 2 fixed at the R5 site, unfixed at its sibling -- so a payload already carrying its own system prompt raised PromptInjectionConflictError across the openai/ollama/external-CLI/anthropic translators. Fixed by threading the packet as a `memory_packet` parameter so EACH translator folds it into ITS OWN single effective system source (anthropic''s `params["system"]`-or-kwarg, folded BEFORE the cache-breakpoint decision so the block covers the merged text; openai/ollama''s leading `role:"system"` message post-`params`-merge; the external CLI''s system section) rather than duplicating each translator''s rule at the call site. A genuine TWO-BASE collision (active prompt AND payload system) still fails loud, witnessed. GROUNDING FINDING, registered not absorbed: the SERVABLE tools path crosses the same family boundary with strictly greater reach (read AND write via the fixed-value `scope_ref`), pre-existing since U-MEM-16/B-82 and gated on a C-MEM-11/13 scope-semantics decision -- NEW row B-86. ROUND 1: [P1-a]: the (b) repair forwarded provider X''s packet to a Y-family provider -- the provider-family scope leak described above; (b) is now report-only. [P1-b]: the R5 retry supplied the packet as a SECOND system source, so `_inject_leading_system_message` raised `PromptInjectionConflictError` for any payload already leading with a system message -- the previously-working tool-free fallback hard-failed for the idiomatic self-system-prompt step shape (reproduced live before fixing). Closed by folding the packet into the ONE effective system source (`_fold_memory_packet_into_system` + a defaulted `memory_packet` kwarg on `_dispatch_ollama`; None keeps every other caller byte-identical), operating on FINISHED kwargs post-`params`-merge for the same reason `_inject_leading_system_message` does -- a `params["messages"]` override can hide the real leading system message, so a payload-level merge would reintroduce the collision. 27 deterministic witnesses, 16 mutation probes (disable the serve-check → 4 fail; revert the R5 packet injection → only (c) fails; drop the provider-identity conjunct → only (b) fails; repair the provider mismatch → only the (b) report-only witness fails; revert to the two-source retry shape → the P1-b witness reproduces PromptInjectionConflictError; drop the family conjunct → only the cross-family witness fails; revert the arm-selection repair to append-as-second-source → both P1-d witnesses reproduce PromptInjectionConflictError; revert the OpenAI content fold to `str()` → the parts-array witness fails on a Python repr; revert the external-CLI fold to the None-pass → the mapping-content witness fails with the step''s directive entirely absent; revert the anthropic fold to the truthiness key → BOTH the block-array and empty-string witnesses reproduce PromptInjectionConflictError; drop the eligibility conjunct → both packet-policy-denied witnesses fail (arm site AND R5 site); stub the composition-time derivation to always-eligible → both composer-level derivation witnesses fail; move the emission back post-dispatch → all four exception-path witnesses fail; publish the R5 sink AFTER its retry instead of before → the R5-retry-failure witness fails alone; collapse the composer derivation back to one label → both composer-level precise-reason witnesses fail; collapse the span reason at the disposition → all three dispatch-level precise-reason witnesses fail). The mixed memory+caller batch case, previously scoped under this row, is genuinely unrepairable at its site and moved to NEW row B-85. Full harness-runtime non-e2e suite green (3151 passed / 10 skipped).'
  close_out: '(1) Decide the failure posture: a fail-loud typed error at the dispatch boundary when `memory_context.access_mode is STANDARD_MEMORY_TOOLS` and the resolved provider arm has no standard-memory-tools sibling, versus an explicit, ledgered degradation to PROMPT_EXTENSION_PACKET/NO_MEMORY_ACCESS (which C-MEM-13:463 would then require be ledgered -- the mechanism already exists per the grounding note, only the dispatch-side re-composition trigger would be new). Fail-loud is the cheaper and more consistent reading given the surrounding `LLMDispatchPayloadShapeError`/`MemoryToolExecutionInputError` conventions. (2) Separately decide whether a statically injected `memory_context` should be re-composed (or invalidated) on a fallback provider rebinding, or whether that configuration should simply be refused at bind time when a `fallback_chain` is also bound -- the latter is a much smaller surface. (3) Witness with two deterministic tests: an explicit-capability-override dispatch against the anthropic arm, and a rebinding test where a STANDARD_MEMORY_TOOLS context composed for openai is dispatched through a cross-family fallback to anthropic. RECORDED RESIDUAL OF THE OBSERVABILITY SURFACE: `B-87` -- the `finally` emission introduced at round 6 over-reports a PRE-wire translation failure as a completed injection (packet_hash present, zero adapter calls made). Registered rather than fixed: the imprecision OVER-reports, and this arc reached its fix-round cap at Codex R7.'
  council: 'Likely for step (1), no for step (3). Choosing fail-loud versus explicit-ledgered-degradation is a C-MEM-13 selection-semantics decision with a blast-radius dimension (a fail-loud raise turns a currently-silent degradation into a run failure for any deployment relying on the injected-context + fallback configuration) -- C10 (blast radius) paired with whichever voice owns the memory access-mode selection semantics. Step (2)''s bind-time refusal is mechanical once (1) is settled.'
  heading: '### B-83 · No dispatch-side guard that the selected `STANDARD_MEMORY_TOOLS` access mode is actually served by the provider arm reached *(surfaced by the B-82 fix, 2026-07-27; CLOSED 2026-07-28 — PR #1142)*'
- id: B-84
  title: 'Memory tool batch: an executor-level argument failure mid-batch can commit earlier writes, then retry-duplicate them'
  status: closed
  pr: '#1228 (the Class 2 fork filing) + #1233 (the 2026-08-05 ratification record) + #1243 (the Reading A build leg)'
  summary: '[SUPERSEDED 2026-08-05 -- RATIFIED as READING A (executor pure validate() pre-pass);
    status is now `open` with a build leg owed, and DIRECTION (2), the transactional/journaled
    batch, was REJECTED and must NOT be re-priced or reopened by the build leg. Everything below
    is the pre-ratification record, kept as history.] Out-of-family Codex round 4 on the B-82
    arc (2026-07-27). Round 1 made both memory tool loops validate the COMPLETE batch before executing
    any of it, closing the DISPATCH-side half of the partial-batch problem. A second half remains
    one layer down: validation the dispatch layer legitimately does not perform still happens
    mid-execution, at the executor. A batch of [memory.write_note (valid), memory.read (missing
    the contract-required `memory_ref`)] passes every dispatch-side check, so the result-message
    builders execute in order -- the write COMMITS a durable record, then the read raises MemoryToolExecutionInputError
    from `_string_arg` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:549-553`
    — *`_string_arg` is at `:678` at 2026-08-05; the whole helper inventory drifted, refreshed
    in the close_out*). That error WAS retry-classified TRANSIENT (`_classify_provider_exception`
    fail-fasted only LLMDispatchProviderUnreachableError / LLMDispatchPayloadShapeError / HTTP
    401-403), so RetryBreakerFallbackDispatcher re-ran the whole dispatch and the committed write
    executed once per retry -- the same double-write class as the round-1 [P1], reached through
    a different door. PARTIAL MITIGATION LANDED (2026-07-28): MemoryToolExecutionInputError now
    joins the fail-fast branch of `_classify_provider_exception` (`harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:326-334`),
    so the retry staircase no longer re-runs the dispatch for this class -- on the default single-candidate
    chain the mid-batch failure now consumes exactly ONE dispatch instead of max_attempts. THE
    ROW STAYS registered_finding: the RETRY-duplication half is closed, the PARTIAL-COMMIT half
    is NOT -- the earlier write in the batch still commits before the later call raises, and a
    MULTI-candidate fallback chain CAN duplicate the committed prefix -- contingent, not guaranteed
    (codex 2026-08-05): the failed inner dispatch''s prepared batch is local and not forwarded;
    the next candidate re-samples its own tool calls (repeat, differ, or absent) -- up to one
    extra copy per candidate when re-sampled alike, versus max_attempts x candidates before. ALREADY
    PREVALIDATED by the B-82 rounds-1/3 pre-pass, for every call, before anything executes: tool_call
    wire shape (function mapping; OpenAI id present); name resolution to a MemoryToolName (wire
    map on OpenAI, dotted-only on ollama); arguments normalizing to a mapping (fail-loud otherwise);
    and the RuntimeMemoryContext checks (record_scope/scope_ref present; scope_ref argument matching
    the context for the two tools whose contracts declare it). NOT checked: per-tool required-argument
    keys and per-tool policy gates -- those are the EXECUTOR''''s semantics. Reachability requires
    a multi-call batch with an earlier valid write AND a later dispatch-valid-but-executor-invalid
    call; unreachable for single-call batches. AGGRAVATOR (out-of-family Codex round 5, verified
    empirically at registration time against the locked ollama 0.6.2 SDK): the ollama client REVALIDATES
    caller-supplied tool mappings as its own `ollama.Tool` model before posting to /api/chat (`ChatRequest.tools:
    Optional[Sequence[Tool]]`, `ollama/_types.py:400`), and that model is a CLOSED FIELD SUBSET
    over a plain pydantic BaseModel -- `SubscriptableBaseModel` (`_types.py:19`) sets no `model_config`,
    so pydantic''''s default `extra=''ignore''` applies; `Tool.Function.Parameters` (`_types.py:365-371`)
    declares only type/$defs/items/required/properties, and `Parameters.Property` (`_types.py:372-379`)
    declares only type/items/description/enum. Every other JSON-Schema keyword is therefore SILENTLY
    DROPPED on the way to the daemon. Measured directly on the real C-MEM-14 contracts: `memory.search`
    loses top-level `additionalProperties: false` and `limit.minimum: 1` and `query.minLength:
    1`; `memory.write_note` loses `note.minLength: 1`. The model therefore never sees the constraints
    the contracts declare, which raises the odds it emits an argument payload the EXECUTOR then
    rejects -- i.e. it feeds exactly this row''''s mid-batch / transient-retry class. Aggravator
    only: it changes the likelihood, not the mechanism, and the mitigation menu below is unchanged.
    Note this is orthogonal to the separate, already-fixed B-82 round-5 issue of non-tool-template
    models (e.g. llava) 400-ing on `tools=` at all. 2026-07-30 post-#1159 re-check (gate-log LOW
    batch): the DEFECT premise is unchanged (both halves re-verified at the 2026-07-29 pass above
    and untouched since), but direction (2)''s COST ASSESSMENT predates U-MEM-27. Two substrate
    pieces the (1)-vs-(2) framing was written without now exist: a GENERIC compare-and-commit
    seam on the canonical store (`CanonicalMemoryStore.write_record_guarded`, `harness-is/src/harness_is/memory_store.py`
    -- it carries no promotion, provenance, or policy semantics, so a batch executor can supply
    its own precondition), and a REENTRANT per-root cross-process hold (`cross_process_scope_lock`)
    under which a multi-write section can evaluate and commit without releasing. Neither makes
    an append-only store transactional by itself, and the compensation question direction (2)
    actually turns on is untouched -- but "make the whole batch atomic" no longer starts from
    nothing, so the (1)-vs-(2) tradeoff leans differently than this row records. Re-price BOTH
    directions at build time rather than reading the recorded lean.'
  close_out: 'BUILD LEG LANDED 2026-08-06 (#1243) -- READING A BUILT, ROW CLOSED. The executor grew a side-effect-free `StandardMemoryToolExecutor.validate(request)` and BOTH provider batch pre-passes (`_openai_prepared_memory_tool_calls` / `_ollama_prepared_memory_tool_calls`) call it for every call before any of them executes. SHAPE, so a later leg does not have to re-derive it: parity is UNFALSIFIABLE BY CONSTRUCTION rather than pinned by an enumeration -- each tool has a pure `_prepare_<tool>` returning a frozen per-tool prepared-arguments value, `_prepare` dispatches over `MemoryToolName` to exactly one of them, `validate()` calls `_prepare` and discards, and `_execute_authorized` calls the SAME `_prepare` and then narrows on the RESULT TYPE, with every tool body taking its prepared value as a REQUIRED parameter. There is therefore no second checking path to drift from, and a sixth prepared variant is a type error rather than a silently unvalidated tool. This is W-4''s PREFERRED discharge, so no site cap applies. WITNESSES (51, at `harness-runtime/tests/test_b84_validate_prepass.py`): W-1 pre-fix baseline (the same batch driven through the loop against a non-validating executor STILL partial-commits -- the counterfactual that keeps the closure witnesses non-vacuous), W-2 closure PARAMETERIZED over both provider arms plus a wholly-valid-batch positive control, W-4 a 30-row (tool x argument x branch) parity matrix asserting validate() and execute() raise the SAME class with the SAME message plus two STRUCTURAL witnesses (`_prepare` totality over the enum; every body requiring its prepared value), W-7 the retry-precedence change pinned in BOTH directions, W-8 in its INVERTED form with a positive control, and the C-MEM-19 telemetry pair. MUTATION PROBES (PD-8), all confirmed RED then restored: (a) remove the OpenAI `validate()` call -> W-2[openai] + W-7[openai] fail, ollama arms stay GREEN; (b) remove the ollama call -> the mirror image; (c) empty `validate()`''s body -> 37 of 51 fail; (d) revert `_search`''s prepare-first ordering so `limit` is validated after the retrieval -> W-8 + the search/limit parity rows fail; (e) drop `validate()`''s span emission -> the telemetry witness fails; (f) emit on the success path too -> the no-double-count witness fails. TELEMETRY OBLIGATION DISCHARGED, and it is the one thing that shaped the design: `validate()` emits the same `memory.tool_call` span with the same `memory.failure_class` / `memory.policy.decision` that `execute()`''s catch would have emitted, ON THE FAILURE PATH ONLY (a success-path span would double-count every standard tool call). Witnessed by asserting BYTE-EQUAL attribute maps between the two surfaces for the identical request, over an input-error row and a policy-denial row. No C-MEM-19 weakening, so no Class 1 back-flow was owed. SINGLE-CALL `memory.search` (fork section 3(vii)): CLOSED, and this is what the ratified reading prescribes rather than a silent scope extension -- W-8''s INVERTED form requires `limit=0` rejected BEFORE any retrieval event, and the shared-prepare structure delivers it on BOTH surfaces (the dispatch pre-pass AND a direct `execute()`), which the witness asserts separately so the direct-call ordering is pinned too. W-5''s FOUR-CELL MATRIX, EXECUTED (answers recorded here as the fork''s section 12.3 requires; all four were `[MODERATE]` code-read inferences before this leg): (1) re-sampled TEXT -> different `memory_id`, ordinary APPEND, ONE JSONL line each -- an EXTRA DISTINCT note, not a duplicate; (2) same text / different MODEL -> different `memory_id` (`summary_model` is hashed), same outcome as (1); (3) same text / same model NAME / different PROVIDER -> `memory_id` COLLIDES, the record JSONL line IS appended a SECOND time, and the ledger then RAISES on its 18-field equivalence payload (`provider` differs) -- surfacing as `MemoryToolExecutionStoreError`, which `_classify_provider_exception` maps to TRANSIENT_RETRY. **CELL (3) IS CONFIRMED BY EXECUTION, so the fork''s D-2 falsifier is answered AFFIRMATIVELY: the landed fail-fast half IS re-enterable through the store-error class.** That is a pre-existing residual the fork already priced as OUT of Reading A''s scope (A never claimed to close store errors), not a regression this leg introduces and not a new defect -- recorded here as a measured fact rather than an inference, and left OPEN; (4) fully identical -> same `memory_id`, duplicate physical JSONL line, ledger IDEMPOTENT_NOOP, call reports CAPTURED. FIFTH DIMENSION also executed: a re-sampled `idempotency_key` gives a different `memory_id` and COLLAPSES cell (3) into cell (1). RESIDUALS THAT STAY OPEN AND DOCUMENTED, exactly as ratified and NOT built: the policy-flip TOCTOU window (a resolver that flips between validate and execute), store errors including the section 3(vi) INTRA-CALL window (`_capture` writes its record before appending the operation-ledger entry; `execute()` runs `_execute_authorized` before `_append_standard_tool_call`), and the section 3(iv) cross-candidate duplication measured above. Retry precedence CHANGED, as ratified. SECTION 9 ITEMS 3-7 CITE REPAIRS DONE, re-grounded at this leg''s final head: `retry_breaker_fallback.py`''s three stale cites (the executor''s argument validation, the dispatch-side context invariants, the two denied-class sites), and `memory_tool_executor.py`''s fail-fast-tuple and `memory_capture.py` offsets. Two ADJACENT stale cites carrying the same offsets, in files this leg touches, were repaired in the same pass and are recorded rather than smuggled: `llm_dispatch.py`''s `_memory_tools_declaring` docstring and the `_real_memory_tool_executor` docstring at `tests/test_lifecycle_llm_dispatch.py`. ZERO design-substrate edits, ZERO contract numbers, ZERO CXA rows, ZERO spec/plan version bumps -- the ratification authorized impl discretion and nothing here needed more. DIRECTION (2) WAS NOT RE-PRICED OR REOPENED. The pre-build record follows, retained as history. RATIFIED 2026-08-05 as READING A -- the executor''s PURE `validate()` PRE-PASS (one
    method + two dispatch call sites, zero contract text). Operator two-stage AskUserQuestion
    on the fork''s section 10.1 (`.harness/class_2_fork_b84_memory_batch_partial_commit.md`):
    stage 1 discriminator = INPUT, stage 2 = A (NOT A+C). READINGS A+C, C, B and D were NOT selected
    and are NOT partially adopted -- in particular D''s `_PreparedMemoryToolCall` docstring-qualification-at-ratification
    obligation DOES NOT FIRE, and B''s spec leg is NOT opened. THE HELD SUB-DECISION IS RESOLVED:
    the fork''s Class-2-vs-Class-1 classification -- held under its pre-committed hold notice
    after codex R2 [P1-a] / R3 [P1] and surfaced at its section 1 with an explicit operator-override
    sentence -- IS RATIFIED AS CLASS 2 (in-execution operator decision, impl discretion). The
    fork does NOT re-file as Class 1. The routing skill''s Recording-row-vs-practice divergence
    (`.claude/skills/phase-7-back-flow-routing/SKILL.md` section 2.2) is NOT thereby endorsed:
    it stays a real defect belonging to whoever next revises that skill, and the operative discriminator
    remains section 2.2''s TRIGGER row, satisfied exactly here because section 3(ii) establishes
    there is no contract to violate. WHAT THE BUILD LEG OWES: the `validate()` pre-pass plus witnesses
    W-1, W-2, W-4, W-5, W-7 and W-8 IN ITS INVERTED FORM (`limit=0` rejected BEFORE any retrieval
    event -- NOT C''s residual pin), the section 8 mutation probes, W-5''s four-cell matrix EXECUTED
    with its answers recorded back on this row, and the section 9 items 3-7 shipped-source cite
    repairs in the touched files. CROSS-FORK NOTE (out-of-family review round 6 [P2]), recorded
    so the build leg does not widen a gap silently: the `validate()` pre-pass raises malformed-model-argument
    failures on the dispatch PREPARE path (`_standard_memory_tool_context`, reached from `_openai_prepared_memory_tool_calls:4213`
    / `_ollama_prepared_memory_tool_calls:4845`), which runs BEFORE `StandardMemoryToolExecutor.execute()`
    opens its telemetry span -- and `lifecycle/llm_dispatch.py` contains ZERO `classify_memory_failure`
    calls, so those failures emit NO `memory.failure_class`. This moves MORE of that class onto
    an unclassified path, which interacts with B-88''s ratified seventh class. PRESERVATION IS
    MANDATORY -- the ''or ratify the telemetry loss'' arm this note carried at round 7 is WITHDRAWN
    at out-of-family review round 9 [P2], because it would have authorized a CONTRACT WEAKENING
    with no design-substrate change. GROUNDED: C-MEM-19 (Spec_Memory_Substrate_v1.md:835-870)
    lists ''Standard memory tool call'' among the operations memory telemetry COVERS and names
    `memory.failure_class` among its REQUIRED attributes. Reading A opened NO spec leg and this
    row is `open` with no blocker, so an impl leg CANNOT elect to drop that attribute on its own
    authority -- doing so is exactly the X-AL-3 silent-absorption failure mode. THEREFORE: the
    build leg MUST PRESERVE classification for every check it relocates off the executor''s catch,
    and MUST witness the preserved emission end-to-end. IF PRESERVATION PROVES INFEASIBLE, that
    is a CLASS 1 BACK-FLOW against C-MEM-19 -- filed and ratified BEFORE the code lands -- NOT
    an impl-discretion call and NOT closable by witnessing the absence. This constrains HOW Reading
    A is built; it does not reopen WHETHER it is built. ACCEPTED COSTS, stated so the build leg
    does not re-litigate them: the policy-flip TOCTOU and intra-call windows remain, and retry
    precedence changes. STATUS -> `open` (the fork''s section 10.2 ratification row prescribes
    the `pr:` stamp and the close_out outcome but is SILENT on status; the enum decides it --
    ratifying a build reading makes the row actionable-now-no-blocker, and there is no design-substrate
    gate under A). The row flips to `closed` when the build leg merges. NO COUNCIL is owed (the
    C10 x memory-ledger-owner dyadic was conditional on Reading B). Two candidate directions,
    neither obviously dominant. (1) An executor-exposed pure `validate()` pre-pass: the executor
    grows a side-effect-free method running exactly the argument/policy checks `_execute_authorized`
    would run, and the dispatch batch pre-pass calls it for every call before executing any --
    keeps ONE source of truth (the executor still owns the semantics; dispatch only asks) at the
    cost of a new public surface plus the discipline that every future check lands in the shared
    path. (2) Transactional / journaled batch execution at the executor: accept a batch and either
    commit all durable operations or none, using the existing memory-operation ledger as the journal
    -- strictly stronger (it also covers mid-batch failures no pre-pass could predict, e.g. a
    policy resolver flipping or an I/O error) but materially larger, touching durable-write ordering,
    and it needs a decision about what rollback means for an append-only canonical store (likely
    compensating ledger entries, not deletion). The third, cheaper mitigation is LANDED (2026-07-28)
    and does NOT close this row: executor-level INPUT errors are now fail-FAST rather than transient.
    Grounding verdict before building: IMPL-DISCRETION, not spec-enumerated. Runtime spec §14.6
    D2 states the predicate as retry-eligible IFF the exception matches the CP §21.2 transient
    staircase, FAIL-FAST OTHERWISE (AUTH, payload-shape, shutdown) -- fail-fast is the RESIDUAL
    class and the parenthetical is illustrative, not a closed set; the §14.5 C-RT-15 table classes
    and the CP C-CP-21 §21.1 five-value ValidatorRetryExitClass are what is closed, and neither
    enumerates which concrete Python exception types map to fail-fast. The classifier body already
    documented itself as MVP-conservative. A contract-invalid C-MEM-14 memory tool call matches
    nothing in §21.2 (network / rate-limit / 5xx); it is the same permanent malformed-input class
    as RT-FAIL-PAYLOAD-SHAPE, raised one layer deeper, so the reclassification moves the impl
    TOWARD the spec predicate rather than extending it. NOTE the register originally justified
    this as identical-arguments-fail-identically, which is imprecise: a retry re-dispatches to
    the provider and re-samples the tool-call arguments, so a retry COULD emit valid arguments.
    The defensible rationale is the spec predicate plus the RT-FAIL-PAYLOAD-SHAPE parity, and
    the harm removed is the duplicate commit, not wasted attempts. SCOPE: MemoryToolExecutionDeniedError
    is deliberately EXCLUDED -- `_read_record_by_ref` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:446-449`)
    wraps ANY store exception, including genuinely transient I/O, into the denied class, and `_allowed_index_entry`
    (`:424-435`) raises it off mutable index/store state, so fail-fasting the base class would
    kill retries for real flakes; whether a policy DENIAL is permanently terminal is a C-MEM policy-semantics
    decision this narrow change does not take. Three witnesses at `harness-runtime/tests/test_lifecycle_retry_breaker_fallback.py`
    (fail-fast candidate-advance plus permanent-fail-exit span class; single-candidate exactly-one-dispatch;
    denied-still-retries scope guard), each mutation-probed: reverting the isinstance addition
    fails the first two (3 dispatches instead of 1 -- the duplication class made visible), widening
    it to the MemoryToolExecutionError base fails the third. ROUND-1 OUT-OF-FAMILY CODEX FOLLOW-ON
    (same arc): `_allowed_kinds` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:574-598`,
    the bare conversion was at pre-fix `:584`) raised a BARE ValueError from `MemoryRecordKind(item)`
    on an unknown allowed_kinds string, so a memory.search with a bad kind following a committed
    memory.write_note still replayed transient -- MemoryToolExecutionInputError SUBCLASSES ValueError,
    not the reverse, so a bare ValueError misses the isinstance check entirely. Wrapped to raise
    the typed input error naming the offending value plus the accepted kinds. MANDATORY SIBLING
    SWEEP RESULT -- this was the ONLY gap. Every model-supplied argument in the executor reaches
    exactly five helpers, all already typed: `_string_arg` (:549-553), `_optional_string_arg`
    (:556-562), `_positive_int_arg` (:565-571), `_allowed_kinds` (:574-598, the fixed site), `_promotion_kind`
    (:654-669, KeyError already wrapped); `_kind_from_memory_ref` (:635-642) already wraps its
    enum conversion. MemoryID / Identifier are NewType (`harness-is/src/harness_is/memory_record_envelope.py:23`,
    `state_ledger_entry_schema.py:33`) -- zero runtime validation, so not raise sites. MemoryRetrievalRequest
    / PromotionCandidate / MemoryOperationPayload pydantic constructions carry no constraint reachable
    from a model argument (query_summary is an unconstrained str; token_budget comes from the
    harness-built context). MemoryToolExecutionContext._non_empty_string (:113-118) raises a bare
    ValueError but validates HARNESS-supplied context strings, never model input. Dispatch side:
    both `MemoryToolName(name)` conversions (`llm_dispatch.py:3562` openai, `:4160` ollama) and
    every other wire-shape check already raise LLMDispatchPayloadShapeError, which was already
    fail-fast. Witness: a real-executor memory.search with an unknown allowed_kind asserting the
    typed error, its message content, and -- cross-layer, on the REAL exception instance -- `_classify_provider_exception(...)
    is None`. Mutation-probed: unwrapping the conversion fails the witness with `ValueError: ''not_a_real_kind''
    is not a valid MemoryRecordKind`, and the same exception then classifies `transient-retry`
    (the replay). ADJACENT ROW: typing this site also SHIFTED its emitted telemetry class (the
    pre-fix bare ValueError classified provider_adapter_failure, the typed error classifies io_failure)
    -- neither value is correct; the substring-keyed classifier defect that causes it is registered
    as B-88. NEITHER (1) NOR (2) was done unilaterally at the B-82 arc: the naive fix -- full
    per-tool contract prevalidation at the dispatch layer -- would MIRROR-COPY the executor''''s
    required-key and policy semantics into a second location, the drift class this workspace treats
    as a defect in its own right (the copy rots the first time a contract''''s required set changes,
    and the two copies then disagree about which calls are legal). B-82 round 3 established the
    boundary empirically: required-key enforcement lives at `memory_tool_executor.py:266` -> `:549-553`,
    and the dispatch layer validates only scope_ref/policy_ref. 2026-07-29 dormancy re-check:
    premise intact at HEAD dd2a8c1a -- both halves of the batch pre-pass are untouched by the
    #1149-#1155 telemetry/reap arcs (the executor''s own last change is #1148; #1151/#1154 moved
    only span attributes at the dispatch site), so the dispatch-side complete-batch validation
    still precedes an executor that validates mid-execution, and the mid-batch commit-then-retry-duplicate
    window stands as described. RE-CHECKED 2026-08-05 at HEAD 43d4dda5 -- premise INTACT, and
    this is genuinely still-open work rather than completed-and-unre-checked. LANDED HALF CONFIRMED:
    MemoryToolExecutionInputError is documented in the fail-fast branch of _classify_provider_exception
    at retry_breaker_fallback.py:293 and is the isinstance member at :330, with MemoryToolExecutionDeniedError
    still deliberately EXCLUDED at :312. BOTH OPEN DIRECTIONS CONFIRMED UNBUILT: the executor
    exposes NO public validate() pre-pass -- its public execution surface is StandardMemoryToolExecutor.execute()
    at memory_tool_executor.py:231, which alone carries the access checks, telemetry, and ledger
    recording and invokes the private _execute_authorized helper at :240/:259 -- calling the helper
    directly bypasses those (direction 1 unbuilt) -- and there is no transactional/journaled batch
    surface at the ACTUAL standard-memory batch owner -- the sequential per-call loop (for call
    in prepared: standard_memory_tool_executor.execute(...)) in lifecycle/llm_dispatch.py (~:4234
    and sibling ~:4865; re-grounded 2026-08-05 by direct read -- lifecycle/memory_tool_dispatch.py
    is the Anthropic memory_20250818 callback loop and never invokes StandardMemoryToolExecutor,
    the wrong surface for this check) (direction 2 unbuilt), so the partial-commit half survives;
    cross-candidate duplication is CONTINGENT on the next provider re-emitting like calls (the
    prior prepared batch is not forwarded). HELPER LINE CITES REFRESHED (the B-84 fix itself grew
    memory_tool_executor.py; every `:5xx`/`:6xx` helper cite in this row and its prose is stale
    by roughly +129 lines): at HEAD the five argument helpers are _string_arg `:678`, _optional_string_arg
    `:685`, _positive_int_arg `:694`, _allowed_kinds `:703`, _kind_from_memory_ref `:764` and
    _promotion_kind `:783`, and the two denied-class sites are _allowed_index_entry `:516` and
    _read_record_by_ref `:545`. Disposition FORK-SHAPED -- RESOLVED 2026-08-05 as READING A (see
    the ratification paragraph at the head of this close_out); the 2026-07-30 re-price note above
    is SUPERSEDED as an INSTRUCTION and retained only as history (out-of-family review round 9
    [P2]): it told a later leg to ''re-price BOTH directions at build time'', which would REOPEN
    the transactional direction (2) the operator REJECTED on 2026-08-05 by selecting Reading A.
    **DIRECTION (2) IS NOT TO BE RE-PRICED OR REOPENED BY THE BUILD LEG.** What survives from
    that note is only the factual observation that U-MEM-27''s write_record_guarded (memory_store.py:298)
    and the reentrant cross_process_scope_lock both exist at HEAD -- facts the ratified Reading
    A build may use, not a mandate to re-open the fork.'
  council: 'NONE OWED -- SETTLED 2026-08-05. Reading A is ratified, and the fork''s section 7
    is PROBE-RESOLVED for A (the C10 x memory-ledger-owner dyadic was owed under Reading B ONLY,
    and B was not selected). The pre-ratification disposition below is SUPERSEDED and retained
    only as the filing-time record: Likely. Direction (2) is a durable-write-semantics decision
    (all-or-nothing across a batch, and what compensation means for an append-only store) with
    a blast-radius dimension -- C10 paired with whichever voice owns the IS-side memory operation
    ledger. Direction (1) and the fail-fast reclassification are closer to mechanical once the
    direction is chosen, but the choice between (1) and (2) is not: it is the difference between
    preventing the predictable case and making the whole batch atomic. Picking (1) forecloses
    nothing; picking (2) subsumes it.'
  heading: '### B-84 · Memory tool batch: an executor-level argument failure mid-batch can commit earlier writes, then retry-duplicate them *(surfaced by out-of-family Codex round 4 on the B-82 arc, 2026-07-27)* *(**RATIFIED 2026-08-05 as READING A** — the pure `validate()` pre-pass; the held element ratified **Class 2**; **BUILD LEG LANDED 2026-08-06 — CLOSED**)*'
- id: B-85
  title: A mixed memory+caller tool batch returns the advertised memory call unserved and un-ledgered
  status: closed
  pr: '#1145'
  summary: 'Surfaced at the B-83 fix (2026-07-28), split out of B-83''s own "adjacent, same family" note because the B-83 repair provably cannot reach it. Both standard-memory-tool continuation loops, on seeing a response whose tool_calls[] mixes a C-MEM-14 memory call with a caller-advertised tool call, hand the WHOLE response back unprocessed and execute nothing from it — a deliberate B-82 round-2 choice for batch atomicity + bare-path parity (a mixed batch must not half-commit memory before control returns). The consequence: the memory call the model explicitly asked for is never served, and NOTHING records that it wasn''t — no span, no ledger entry, no error. The caller cannot serve it either; it does not own the memory tools. Reachability: `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py:3389-3401` (openai arm) and `:3979-3987` (ollama arm), line numbers at the B-83 arc final HEAD (corrected at the merge-gate after later-round drift) — the two `if not _all_calls_are_memory_tools(...)` early returns; both sites now carry an inline `B-85` marker. WHY NOT FIXED AT THE B-83 ARC: B-83''s repair is prompt-composition, applied BEFORE the provider call. This exit happens AFTER a response is already composed, and a prompt packet cannot retroactively answer a tool call the model already emitted — so there is no mechanical repair to apply, only a posture to choose. NOT a defect in the atomicity choice itself, which remains correct.'
  close_out: 'Choose the posture; the mechanics follow trivially from whichever is chosen. (1) FAIL: raise at the mixed-batch detection so the unserved memory call is loud rather than silent — consistent with the surrounding `LLMDispatchPayloadShapeError` conventions, but it turns a currently-working caller-tool dispatch into a failure. (2) SPLIT: execute the memory calls, return the caller''s calls unprocessed — restores service but reopens exactly the half-commit window B-82 round 2 closed (the memory writes commit, then the caller''s handling can fail and the whole dispatch retries under the TRANSIENT classification). (3) REPAIR/RECORD: keep the return-whole semantics and emit a degraded/unserved telemetry span at the exit — cheapest and preserves atomicity, but leaves the model''s request genuinely unanswered. Note (3)''s span is emittable with the existing closed enums; a LEDGERED entry is not (same X-AL-3 constraint recorded at B-83''s close-out). CLOSED 2026-07-28 (Round 37 grounding arc, same PR as the B-86 Class 1 fork filing): posture (3) RECORD chosen and landed. GROUNDING VERIFIED THE SPEC IS SILENT -- C-MEM-14 (`Spec_Memory_Substrate_v1.md:489`) obliges tool EXPOSURE, not service; C-MEM-13''s must-ledger clause (`:463`) binds only `no_memory_access` transitions; and C-MEM-08''s closed 12-kind operation enum (`:247`/`:251`) cannot express requested-but-unserved, which confirms the LEDGER half is X-AL-3-blocked exactly as this close-out predicted while the SPAN half is emittable. EMISSION: a SEPARATE single-slot sink + emitter, deliberately NOT the degraded-serve emitter -- that emitter''s `rendered=None` shape hardcodes `DENIAL` / `no_memory_access` / `degraded_serve=True`, all three FALSE at iteration k>0 where the loop has already served k rounds. The new span carries `operation_name=standard_tool_call`, `access_mode=standard_memory_tools`, `record_count=0`, NO packet_hash, NO failure_class, `reason=mixed_batch_caller_owned`, plus a quantitative `memory.unserved_tool_call.count` -- all riding attribute VALUES per the B-83 precedent, never new members of a closed enum. Return-whole semantics are byte-identical, and a zero-memory-call caller-only batch deliberately emits nothing (negative-asserted). POSTURES (1) AND (2) REJECTED AS CLASS-2-SHAPED: (1) FAIL inverts the witnessed bare-path-parity invariant (enabling memory must never turn a working caller-tool dispatch into a failure) and pays fail-fast candidate abandonment for what is an observability gap; (2) SPLIT reopens exactly the duplicate-durable-write window B-82 round 2 and B-84/PR #1143 closed, on a path where the harness cannot serve the caller half anyway. A FOURTH OPTION was enumerated and rejected: withhold the memory tools whenever the step advertises caller tools, making the mixed batch unrepresentable -- preventive, but it disables memory on every tool-declaring step and changes WHICH memory a run reaches, which is B-86''s territory. STATED HONESTLY: the span may fire 0..N times across retry attempts, since model-emitted batches are nondeterministic -- the same property as the B-83 span, and the witnesses do not assert exactly-once. NOT FORECLOSING: advisor + grounding both record that choosing (3) leaves (1) and (2) available later as a Class 2 fork, and that (3) is the option preserving the committed B-82-round-2 atomicity decision.'
  council: 'Likely. This is the same C-MEM-13/14 serve-semantics decision B-83 answered for the pre-call case, now for the post-response case, and each option trades a different harm: (1) blast radius on working dispatches, (2) reopens a durable double-write window, (3) accepts a silent-to-the-model outcome. C10 (blast radius) paired with whichever voice owns memory access-mode selection semantics.'
  heading: '### B-85 · A mixed memory+caller tool batch returns the advertised memory call unserved and un-ledgered *(surfaced at the B-83 fix, 2026-07-28)*'
- id: B-86
  title: Cross-family fallback serves the chain primary's memory scope to a different-family provider on the SERVABLE tools path
  status: closed
  pr: '#1148'
  summary: 'Surfaced while grounding B-83 [P1-c] (2026-07-28). The B-83 fix stops the degraded-serve PACKET from crossing a provider-family boundary, but the SERVABLE tools path crosses the same boundary and was left untouched, deliberately and with a much larger blast radius. On the `memory_runtime` path a cross-family fallback RECOMPOSES the context for the current candidate, so `select_memory_access_mode` re-picks STANDARD_MEMORY_TOOLS from the CANDIDATE''''s capabilities and the tools are injected -- but `automatic_memory.compose_for_dispatch` derives `record_scope.provider_family` from `fallback_chain.primary.family` (`harness-runtime/src/harness_runtime/automatic_memory.py:191-199`), NOT from the candidate. The injected tool schemas bind `scope_ref` (a hash of that PRIMARY-family scope) and `policy_ref` as fixed enum values (`_openai_standard_memory_tools` / `_ollama_standard_memory_tools`), so the candidate can `memory.search` across, and `memory.write_note` INTO, a scope belonging to a different provider family -- read disclosure AND write attribution, where B-83''''s packet leak was read-only. `MemoryRetriever._scope_mismatch` (`harness-is/src/harness_is/memory_retrieval.py:371-384`) filters on `provider_family`, confirming the field is a real enforcement boundary and not merely descriptive. RESIDUAL, same family: `provider_family_for_provider` (`harness-runtime/src/harness_runtime/lifecycle/cross_family_cost_tag.py:82`) falls back to LOCAL_OPEN_WEIGHT for an UNKNOWN provider key, so an unregistered provider compares equal to an ollama-scoped packet at B-83''''s new family conjunct -- conservative for its original cost-attribution use, wrong for a scope-boundary check. PRE-EXISTING, not introduced by B-83: the tools path has behaved this way since U-MEM-16/B-82. Scoped out of the B-83 arc because closing it means either disabling memory entirely on every cross-family fallback or recomposing the scope per candidate -- both change WHICH memory a run can reach, a C-MEM-11/13 scope-semantics decision rather than the dispatch-side serve-check B-83 owns.'
  close_out: 'CLOSED 2026-07-28 at the U-MEM-26 impl leg (this PR). The C-MEM-13 withhold guard landed as the SIXTH conjunct of `_standard_memory_tools_context` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`), sharing `_packet_scope_matches_dispatch_family` VERBATIM with the `_degraded_serve_disposition` predicate rather than adding a second cross-family test -- a divergent test would let a cross-family dispatch fail the arm guard, fall through to `_degraded_serve_for_unservable_arm`, pass disposition conjunct 2 and land on the `arm_unservable` REPAIR branch, serving the same cross-family packet as prompt text. Downstream is by construction: the guard returns None, the disposition reports `provider_family_scope_mismatch` with `rendered=None`, the plain arm dispatches with no schemas / no `scope_ref` / no packet, and the degraded span carries DENIAL / `no_memory_access` / `record_count=0` / no `packet_hash`; the dispatch still completes. TWO defect-pinning tests were INVERTED with contract cites rather than deleted -- the B-83-era R5 witness required the tools to be ARMED cross-family so the daemon could refuse them (now `test_u_mem_26_cross_family_ollama_dispatch_withholds_tools_and_packet`), and the r300 `api.run` integration test asserted the five C-MEM-14 wire names DO reach the cross-family openai leg (now the inverted assertion in `test_r300_deterministic_cross_family_fallback_through_production_path`; its wire-name / HTTP-400 coverage is preserved on the same-family paths that still inject). Net effect of the B-86 filing''s own item (3) is unchanged and now report-only: the unregistered-key DENIAL, with harness-authored capture UNAFFECTED -- witnessed with the run''s composed scope at `test_u_mem_26_capture_is_unaffected_by_the_cross_family_withholding`, which asserts the captured record carries the chain primary''s `anthropic` family value on an openai dispatch leg. Mutation-probed: dropping the conjunct reddens all three withhold witnesses; swapping it for a raw provider-name `!=` reddens 18 same-family ollama tests. Arc record at `.harness/u-mem-26-impl-leg-close-2026-07-28.md`.'
  council: 'Likely, for (1) and (2). Whether memory scope follows the chain primary or the dispatched candidate decides which records a run can read and write across a fallback, trading a disclosure/attribution boundary against memory continuity on fallback -- C10 (blast radius) paired with whichever voice owns C-MEM-11/13 retrieval scope semantics. (3) is mechanical once (1) is settled and could land ahead of it.'
  heading: '### B-86 · Cross-family fallback serves the chain primary''s memory scope to a different-family provider on the SERVABLE tools path *(surfaced while grounding B-83 [P1-c], 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*'
- id: B-87
  title: Degraded-serve span over-reports pre-wire failures as completed injections
  status: closed
  pr: '#1149'
  summary: 'Surfaced by out-of-family Codex round 8 on the B-83 arc (2026-07-28); REGISTERED, NOT FIXED -- see the direction rationale below. MECHANISM: B-83 round 6 moved the degraded-serve span emission into a `finally` around the whole arm-dispatch chain so a repair that was SENT and then failed still reports (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`, the `_emit_degraded_memory_serve_span` call in the `finally`). That correctly covers POST-wire failures. It over-covers PRE-wire ones: when the repair is authorized, `_DegradedMemoryServe.rendered` is populated at the serve-check, BEFORE any translation runs -- and the per-arm translators can raise before the provider adapter is ever called. The `finally` then emits `operation_name=injection` + `access_mode=prompt_extension_packet` + the packet_hash + a non-zero record_count, i.e. the span asserts the packet WAS injected, when in fact nothing left the process. VERIFIED EMPIRICALLY at registration time, not hypothesised: driving the residual two-base case (an `active_system_prompt` bound AND a payload-owned `params["system"]`, which B-83 round 4 deliberately leaves as a fail-loud `PromptInjectionConflictError`) through the real dispatcher on a repair-eligible context produced ZERO adapter calls and yet a span carrying `memory.operation.name=injection`, `memory.access_mode=prompt_extension_packet`, `memory.packet_hash=cccc...`, `memory.record_count=1`, `dispatch_outcome=provider_error`, `provider_error_type=PromptInjectionConflictError`. REACHABILITY: any translation-layer raise on a REPAIRED dispatch -- the two-base `PromptInjectionConflictError` residual (reachable on all four arms), `LLMDispatchPayloadShapeError` from the external-CLI text-only tools rejection, and any future pre-adapter validation. NOT reachable for report-only dispositions (`rendered is None` already emits `denial` / `no_memory_access` / no packet_hash), and not reachable when the repair is unauthorized. Note the existing witness `test_b83_two_base_system_sources_still_fail_loud_through_the_repair` already exercises this exact path but asserts only the raise, not the span -- so the arc''''s suite passes over the defect rather than covering it. CANDIDATE FIX: a reached-the-wire marker set at the provider-call boundary (the one place per arm where the adapter is actually invoked), with the `finally` emitting a DISTINCT classification when the marker is unset -- pre-wire failures reported as e.g. `denial` plus a `dispatch_outcome="translation_error"` value and NO packet_hash, since no packet reached a provider. Mechanically small; the care is in threading the marker to every arm''''s true adapter-call site (including the ollama R5 bare retry) without reintroducing the round-6 loss on genuine post-wire failures.'
  close_out: '(1) Add a reached-the-wire marker at each arm''''s provider-call boundary and branch the `finally` emission on it: marker set -> today''''s behavior verbatim (an injection that reached a provider, `completed` or `provider_error`); marker unset -> a distinct pre-wire classification with NO packet_hash and no `injection` operation name, since claiming a packet_hash for a packet no provider ever saw is the whole defect. Ground the exact adapter-call sites first -- there is more than one per arm (anthropic has three variants; ollama has the standard-tools loop AND its R5 bare retry) -- and mind that the marker must be set BEFORE the awaited call, not after it, or a post-wire failure regresses to being reported as pre-wire (the mirror image of this row, and the exact ordering trap B-83 round 6 [P2-d] already hit once with the R5 sink). (2) Extend the existing `test_b83_two_base_system_sources_still_fail_loud_through_the_repair` to assert the SPAN as well as the raise -- the path is already covered, only the assertion is missing. (3) While there, decide whether `dispatch_outcome` should gain a `translation_error` value or whether pre-wire failures are better carried on the `reason` axis; either is an attribute VALUE, never a new member of the closed C-MEM-19 enums. CLOSED 2026-07-29 at PR #1149 (branch b87-reached-the-wire-prewire-marker) -- all three steps land. (1) MARKER: a frame-local `_ProviderWireReachedSink` (`harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) is constructed once per dispatch and threaded to every arm; `_emit_degraded_memory_serve_span` gained a `reached_wire` parameter and branches on it -- marker SET emits today''s post-wire shape VERBATIM (`injection` + packet_hash + record_count, `completed` or `provider_error`); marker UNSET emits `dispatch_outcome="pre_wire_failure"` plus a new `memory.degraded_serve.pre_wire_error_type` attribute, with NO packet_hash and no `injection` operation name (the existing closed-enum member `packet_assembly` is emitted instead). NAME CHANGE against this row''s own candidate: `translation_error` was REJECTED at out-of-family codex R1 [P2] as over-claiming -- ANY pre-wire phase can fail (payload-shape rejection, adapter selection, a repair-authorization raise), not only translation, so the shipped value is phase-neutral by construction and names the BOUNDARY, not the phase. PLACEMENT, per arm: the direct-SDK arms construct the awaitable, mark, then await -- so a call-time SYNCHRONOUS `TypeError` raised by the SDK is correctly classified pre-wire (R7); the external-CLI arm fires INSIDE the subprocess runner after `create_subprocess_exec` returns, the semantic anchor being that a child process now exists that can observe the payload (a spawn failure therefore stays pre-wire) (R2/R3); the anthropic-memory arm fires once inside `execute_with_memory_callbacks` before its first SDK call (R5), since that helper owns a tool loop the dispatcher cannot see into. TWO DECLINES ON THE RECORD: (a) IN-COROUTINE vendor-side validation that raises after the awaitable is entered is POST-wire BY DESIGN -- resolving it would require SDK-internal hooks that do not exist; (b) consequently the SDK arms'' boundary is OUR last observable point, deliberately, and this row does not claim byte-on-the-wire fidelity. COMPATIBILITY TIERS: an adapter or runner that does not DECLARE an explicit `on_wire` parameter (a bare `**kwargs` deliberately does NOT count, R4) keeps the pre-B-87 marker placement, discriminated by the single shared `accepts_explicit_on_wire` predicate; the public runner seam statically admits BOTH shapes (R6). ROBUSTNESS: an `on_wire` callback that raises after spawn kills and reaps the child, and the orphaned coroutine is closed rather than leaked (R7). (2) LANDED -- `test_b83_two_base_system_sources_still_fail_loud_through_the_repair` now asserts the SPAN as well as the raise. (3) RESOLVED as `dispatch_outcome`, an attribute VALUE; no member added to any closed C-MEM-19 enum. 8 out-of-family codex rounds (9 real findings), ~25 mutation-probed witnesses across 4 test files, `just check` 7014 passed.'
  council: 'No. This is a telemetry-accuracy fix inside an already-settled span contract, with the classification axis (attribute values, not enum members) already established by B-83 rounds 5-7. There is no cross-domain tension to surface: nothing about WHICH memory is served or WHO may see it changes -- only whether the span truthfully reports that a disclosure occurred. The one judgement call, whether pre-wire lands on `dispatch_outcome` or `reason`, is impl discretion once the marker exists.'
  heading: '### B-87 · Degraded-serve span over-reports pre-wire failures as completed injections *(surfaced by out-of-family Codex round 8 on the B-83 arc, 2026-07-28; CLOSED 2026-07-29 — PR #1149)*'
- id: B-88
  title: 'Memory failure telemetry: substring-keyed classify_memory_failure collapses the typed input-error family (and two real policy denials) to io_failure'
  status: closed
  pr: '#1220 (the Class 2 fork filing) + #1233 (the 2026-08-05 ratification record) + #1245 (the SPEC leg -- Memory spec v1.3 + Memory plan v1.3, both clearance markers) + #1247 (the IMPL leg -- U-MEM-28)'
  summary: '[CURRENT STATE 2026-08-06 -- RATIFIED as READING A with sub-decision A-ii = RE-TYPE, and
    the SPEC LEG HAS LANDED (PR #1245: Memory spec v1.3 + Memory plan v1.3, both clearance markers).
    `status: open`. THE ONLY THING OWED IS THE IMPL LEG, carried by plan unit U-MEM-28 -- do NOT
    schedule the spec leg again. This bracket REPLACES the 2026-08-05 one that said the status was
    `design_substrate_gated` with a spec leg then an impl leg owed; that reading is superseded, not
    merely appended to. Every ''the spec half REMAINS, so the row stays registered_finding'' statement
    below is the pre-ratification record, kept as history.] Surfaced by the merge-gate lens-2
    sweep on the B-84 arc (2026-07-28); PRE-EXISTING and latent, orthogonal to B-84 but made visible
    by it. `classify_memory_failure` (`harness-is/src/harness_is/memory_observability.py:150-163`
    *(the classifier sits at `:166`–`:200` at 2026-08-05; filing §10)*) classifies into the C-MEM-19
    vocabulary by matching SUBSTRINGS of the exception message and of the lowercased type name.
    Its third rule is `isinstance(exc, (OSError, IOError)) or "io" in exc_name` -- and "io" is
    a substring of "execut-io-n", so EVERY member of the MemoryToolExecutionError family matches:
    memorytoolexecutioninputerror, memorytoolexecutiondeniederror, and the base memorytoolexecutionerror.
    The rules are ordered, so a member escapes only if an EARLIER message rule fires first --
    an accident of wording, not a type distinction. EMPIRICAL EVIDENCE (probed at registration
    time on real exception instances, not inferred): all four typed input errors (unsupported
    allowed_kinds entry / argument must be a string / argument must be >= 1 / unsupported promotion
    target_kind) emit io_failure; the BASE MemoryToolExecutionError emits io_failure; and -- the
    sharper half -- TWO REAL POLICY DENIALS emit io_failure, namely MemoryToolExecutionDeniedError("memory
    ref X is superseded") (`harness-runtime/src/harness_runtime/memory_tool_executor.py:433`)
    and ("memory ref X is not retrievable") (`:435`), because neither message contains policy/denied/unavailable.
    A denial reading "denied by policy" correctly emits policy_denial -- so the vocabulary axis
    the spec names is honored only by wording luck. Finally, the pre-B-84 bare ValueError from
    `_allowed_kinds` emitted provider_adapter_failure, so B-84 typing that site SHIFTED the emitted
    class (provider_adapter_failure -> io_failure); neither value is correct, which is the point.
    WHY IT IS A DEFECT: `Spec_Memory_Substrate_v1.md:641` *(at `:870` at 2026-08-05; filing §10)*
    (C-MEM-19 Invariants) requires that failure telemetry "must distinguish policy denial, path
    violation, IO failure, serialization failure, provider adapter failure, and retrieval empty-result"
    -- two genuine policy denials emitting io_failure violates that invariant on an axis the vocabulary
    ALREADY HAS (an operator triaging memory IO failures sees records that were merely superseded
    or unretrievable). A SECOND, SPEC-SIDE HALF: the six-value vocabulary has NO input-validation
    class, so even a correct classifier has nowhere truthful to put a malformed-argument failure.
    REACHABILITY: every standard-memory-tool failure path -- `StandardMemoryToolExecutor.execute`
    (`memory_tool_executor.py:167-175`) calls classify_memory_failure in its blanket except, so
    the attribute is emitted on every raise. ADJACENT, SAME FAMILY (merge-gate lens 3): on the
    B-84 fail-fast exit the wrapper calls breaker.record_failure(cause=_classify_breaker_cause(exc)),
    which returns None for an exception with no .status_code -- correct, but UNWITNESSED: a mutation
    hardcoding BreakerCause.AUTH_FAILURE there SURVIVES the B-84 witnesses. Same telemetry-precision
    family (a failure emitting a confidently wrong classification), same fix shape (assert the
    emitted classification, not just the control flow), so it is recorded on this row rather than
    as a separate one. IMPL HALF LANDED 2026-07-28 (spec half REMAINS, so the row stays registered_finding):
    classification is now TYPE-KEYED. classify_memory_failure first reads a memory_failure_class
    class attribute declared at the exception definition site (MEMORY_FAILURE_CLASS_ATTRIBUTE,
    harness-is/src/harness_is/memory_observability.py) and that declaration wins outright; only
    a real MemoryTelemetryFailureClass member is honoured, so a typo or a bare string cannot invent
    a class outside the closed vocabulary, and getattr walks the MRO so subclasses inherit for
    free. The message/type-name heuristics survive ONLY as the residual for types that declare
    nothing, and the io-in-exc_name clause is DELETED (isinstance(exc, OSError) is the whole IO
    rule now). SHAPE CHOSEN -- declaration-at-the-definition-site, NOT a frozen name list inside
    the classifier: a name list would be a SECOND authority for a hierarchy harness-is cannot
    import (harness-runtime imports harness-is, never the reverse), the mirror-copy drift class
    this workspace already treats as a defect in its own right; in-repo precedent is memory_tool_executor.py:170-172,
    which ALREADY keys memory.policy.decision on isinstance(exc, MemoryToolExecutionDeniedError)
    -- failure_class on the very next line was the only substring-keyed attribute on that span.
    POPULATIONS (old -> new): (a) MemoryToolExecutionDeniedError, all 15 raise sites at HEAD (14
    at the impl leg; one added by a later arc -- repaired 2026-08-05 at the ratification leg per
    filing section 10): policy_denial-or-io_failure-by-wording -> policy_denial always; FOUR sites
    actually changed, not the two this row named -- memory promotion review is forbidden, memory
    ref X is superseded, memory ref X is not retrievable, memory tool scope_ref does not match
    context (named by message, not by line offset, which this arc own edits shift). (b) MemoryToolExecutionInputError,
    all 10 executor sites (18 repo-wide at 2026-08-05; filing section 10): io_failure (and policy_denial
    for the policy_ref-named argument message) -> provider_adapter_failure, the least-wrong existing
    value. (c) MemoryToolExecutionError base (:293): io_failure -> provider_adapter_failure --
    its one raise site re-raises a capture result whose failure_reason is an arbitrary string
    and may carry no exception at all, so the type determines nothing and declares the residual;
    a future subclass that declares nothing inherits the residual, never a confidently wrong class.
    (d) undeclared harness types whose NAME contains io but whose failure is not IO -- MemoryOperationIdempotencyConflictError
    / ProjectionMismatch / RedactionEventMismatch (reachable at memory_redaction.py:260) and pydantic
    ValidationError (validat-io-n): io_failure -> provider_adapter_failure. (e) MemoryPathViolationError:
    path_violation -> path_violation, now by construction. (f) MemoryCallbackIOError non-policy
    sites (not-found / out-of-range / content_b64 decode): io_failure -> io_failure, PRESERVED
    by declaring IO_FAILURE on the type -- without the declaration these eight sites would have
    regressed to the residual once the substring rule died, and the type IS the C-RT-15 RT-FAIL-MEMORY-CALLBACK-IO
    storage-backend I/O failure. (g) native-adapter POLICY denials -- _require_native_access,
    _require_capture_allowed, and both _require_retrieval_allowed branches (redaction-state +
    resolver DENY): policy_denial by wording -> policy_denial by construction, via a MODULE-PRIVATE
    _NativeMemoryPolicyDeniedError(MemoryCallbackIOError) -- the C-MEM-15 callback surface offers
    exactly one failure carrier, so a denial must be raised as a MemoryCallbackIOError; isinstance,
    every except, every pytest.raises and the transient RT-FAIL-MEMORY-CALLBACK-IO mapping are
    unchanged, so it is a telemetry discriminator and NOT an addition to the section 14.12.1 typed-exception
    surface. (h) everything else undeclared (MemoryStoreRecordUnavailableError, raw OSError, stdlib):
    unchanged. INPUT-CLASS RATIONALE, recorded for the spec half to overturn: (i) the fault originates
    in a provider/model-emitted tool call arriving through the adapter, not in the memory substrate
    own IO or record codecs; (ii) retry_breaker_fallback._classify_provider_exception already
    groups MemoryToolExecutionInputError with LLMDispatchPayloadShapeError, the provider-payload-shape
    family, so both layers read the failure the same way; (iii) it RESTORES the value this population
    emitted before B-84 typed _allowed_kinds, reverting rather than entrenching B-84 incidental
    provider_adapter_failure -> io_failure shift; (iv) serialization_failure is reserved for record-codec
    faults and would poison that bucket for triage. The declaration is written explicitly on MemoryToolExecutionInputError
    (not inherited) so it is the single flip site when the vocabulary gains an input class. RESIDUAL
    STATED HONESTLY: undeclared types are still keyed by message wording, which remains wording-luck
    for e.g. MemoryStoreRecordUnavailableError -- left alone, since those types have no vocabulary-determined
    class. ADJACENT, UNTOUCHED: memory_capture.py:545 hardcodes IO_FAILURE for the SAME write_record
    + append_memory_operation pair that memory_redaction.py:260 routes THROUGH the classifier
    -- two structurally identical except blocks with different contracts, pre-existing and orthogonal.
    WITNESSES: harness-runtime/tests/test_memory_failure_classification.py (THE population table,
    39 parametrized rows (37 at the impl leg; repaired 2026-08-05 at the ratification leg per
    filing section 10) over real instances carrying their actual raise-site messages, plus an
    agreement pin between memory.policy.decision and failure_class on the four previously-disagreeing
    denials, plus a pin that the private discriminator is still a MemoryCallbackIOError); harness-is/tests/test_memory_observability.py
    (the declaration mechanism -- wins over conflicting wording, inherited, overridable, non-member
    declaration IGNORED -- and the residual rules); harness-runtime/tests/test_lifecycle_retry_breaker_fallback.py::test_memory_tool_input_error_fail_fast_trip_carries_no_breaker_cause
    (the lens-3 half). MUTATION PROBES, all three fail post-fix and were restored: (a) restoring
    or io in exc_name -> 2 rows fail; (b) removing the DeniedError declaration -> 11 failures;
    (c) hardcoding BreakerCause.AUTH_FAILURE at the fail-fast record_failure -> 1 failure, the
    NEW test only, confirming this row own claim that the B-84 witnesses survive it. LENS-3 HALF
    CLOSED at the same arc: fail_threshold=1 so the single fail-fast failure trips a real breaker,
    a spying registry captures the emitted BreakerTransition, and the test asserts transition.cause
    is None AFTER asserting the transition exists (positive control, never vacuous). ROUND 2 (out-of-family
    Codex [P2] on this arc, same day): round 1 unconditional base declaration INTRODUCED a regression
    for one population -- a real durable-write failure. _write_note re-raises a MemoryCaptureResult
    whose status is FAILED, and that status is produced at exactly ONE place, EpisodicMemoryCapture._capture
    except around write_record + append_memory_operation (memory_capture.py:541-552), which classifies
    its OWN span io_failure; the base residual therefore reported a disk-full store write as provider_adapter_failure.
    GROUNDING: MemoryCaptureResult carries failure_reason as a formatted STRING only (f-string
    of type name plus exception) -- the original exception is NOT retained, so there is nothing
    structured to propagate and no honest way to sub-classify without message guessing; and MemoryCaptureStatus
    is a CLOSED two-value enum (CAPTURED | FAILED). FIX: a new module-level MemoryToolExecutionStoreError(MemoryToolExecutionError)
    declaring IO_FAILURE, raised when status is FAILED -- discriminated on the capture API own
    closed status enum, never on wording. It ADOPTS the capture layer classification rather than
    inventing a third answer. The base keeps the residual and its ONLY remaining raise site is
    _write_note defensive memory_id-is-None branch, unreachable through the real capture API (a
    CAPTURED result always sets memory_id). BASE RAISE-SITE SWEEP (mirroring the B-84 R1 sweep):
    raise MemoryToolExecutionError( occurs at exactly one site repo-wide, so the mislabel class
    was confined to it; store exceptions do not otherwise reach the base -- _read_record_by_ref
    wraps them into the denied class, and an un-wrapped store exception propagates raw through
    execute blanket except and hits the residual isinstance(exc, OSError) -> io_failure, already
    correct. NOTE ON THE PER-INSTANCE OPTION: genuinely foreclosed -- the codex R1 [P2] fix made
    _declared_failure_class read getattr(type(exc), ...) rather than the instance, so an instance-level
    assignment cannot shadow the hierarchy. Independently, there is no per-instance value to carry:
    MemoryCaptureResult exposes no enum. A frozen subtype is therefore both the only available
    and the type-honest shape. ROUND-2 WITNESS: test_memory_tool_executor.py::test_write_note_store_failure_emits_io_failure_class
    -- end-to-end through the real executor with store.write_record raising OSError(disk full),
    asserting memory.failure_class == io_failure on the outer memory.tool_call span. Grounded
    there: _write_note does NOT thread the executor tracer into EpisodicMemoryCapture, so on this
    path the outer span is the ONLY telemetry record of the failure. ROUND-2 PROBES: (d) restoring
    the regression (store failure raises the base again) fails the end-to-end witness; (e) removing
    the subtype IO_FAILURE declaration so it inherits the base residual -> 4 failures, the span
    assertion plus the 3 store-subtype table rows. ADJACENT ITEM UPDATED: the memory_capture.py:545
    coarseness (ANY exception from the durable-write pair labelled io_failure) is now inherited
    by one more span; it cannot be refined at the executor, which receives only the reason string.
    ROUND 3 (merge-gate test-witness lens BLOCK, closed same day): the lens found the three OTHER
    _NativeMemoryPolicyDeniedError raise sites unwitnessed -- their covering tests asserted only
    pytest.raises(MemoryCallbackIOError, ...), which the subtype satisfies IDENTICALLY, so reverting
    any one of them to the base type left the suite green while re-emitting io_failure on a genuine
    policy denial (the exact defect this arc closes); only _require_native_access was span-pinned.
    All three are now span-pinned at test_native_memory_adapter.py -- _require_capture_allowed,
    _require_retrieval_allowed resolver-DENY branch, and its redaction-state branch (which had
    NO test at all) -- via a _failure_span_attributes helper that selects the failure span by
    memory.policy.decision == failed and unpacks a single-element list, so two-or-zero failure
    spans fails rather than silently asserting the wrong span. PROBE (f): reverting each site
    individually fails exactly ONE test, its own, with AssertionError: assert io_failure == policy_denial.
    ROUND-3 ADJACENT ITEMS, all landed: (1) the THIRD classify_memory_failure consumer (memory_redaction.py:260)
    is now span-pinned by a two-row parametrized witness with DIFFERENT expected classes -- OSError
    -> io_failure and MemoryOperationIdempotencyConflictError -> provider_adapter_failure -- so
    the assertion pins the classification, not a constant; PROBE (g): hardcoding failure_class=io_failure
    at that emission site fails the non-IO row only (assert io_failure == provider_adapter_failure)
    while the IO row still passes, the correct discriminating shape. (2) WORDING-CONFLICTING rows
    added for three declarations that were deletable-green -- the base (message denied by policy,
    residual would say policy_denial), MemoryPathViolationError (a message with no path in it,
    residual would say provider_adapter_failure), and MemoryToolExecutionStoreError (a store LookupError
    reason reading is unavailable, residual would say policy_denial) -- so deleting any of the
    three now fails a row instead of passing. (3) Count corrections: DeniedError has 14 raise
    sites at the impl leg, not 12 (both files) -- 15 at HEAD, repaired 2026-08-05 at the ratification
    leg; the table row count was 37 parametrized rows at the impl leg (the YAML said 32, the prose
    34 -- mirror drift, both corrected then) -- 39 at HEAD, repaired 2026-08-05. (4) An inaccurate
    comment at test_memory_observability.py claiming the declaration beat a message that trips
    an earlier heuristic was fixed by making the message genuinely trip rule 2 (path traversal),
    so the row is a real conflict rather than a residual-agreement.'
  close_out: 'IMPL LEG LANDED 2026-08-07 (U-MEM-28) -- THIS ROW IS NOW CLOSED. WHAT SHIPPED: the seventh
    `MemoryTelemetryFailureClass` member `INPUT_VALIDATION_FAILURE`; the SINGLE declaration flip at
    `MemoryToolExecutionInputError` (the LEAST-WRONG-STOPGAP docstring rewritten to state what the class IS);
    the ratified A-ii RE-TYPING of the SIX harness-internal `lifecycle/llm_dispatch.py` raise sites onto a NEW
    dedicated `MemoryToolExecutionInternalError` -- not a subclass of the input type, not the family base, and
    declaring nothing of its own so it inherits the base RESIDUAL -- with the TWO model-supplied-`scope_ref`
    sites deliberately RETAINED on the input type; that receiving type admitted BY NAME to
    `_classify_provider_exception`''s fail-fast tuple so the fail-fast disposition is PRESERVED rather than
    silently flipped to TRANSIENT_RETRY; and the `test_memory_observability.py` fixture re-key plus a NEW
    distinct type-gate sibling. Sites were re-resolved BY CONTENT at the impl HEAD, never by offset.
    THE TYPE-BOUNDARY INVARIANT CLOSES WITH ONE KNOWN NON-CONFORMANT SITE, RECORDED AND UNREPAIRED:
    `StandardMemoryToolExecutor._prepare`''s exhaustiveness fall-through still raises the input-error type for a
    harness-internal fault. U-MEM-28 does NOT repair it, on AUTHORITY grounds rather than merit -- the operator
    ratified an A-ii site list of EXACTLY SIX, all in `lifecycle/llm_dispatch.py`, and an impl leg does not widen
    a ratified enumeration on its own reading. THAT FINDING IS OWNED BY REGISTER ROW `B-114`, which outlives this
    row and carries the grounding pass, the conformance-repair-vs-ratified-extension discriminator, and the repair
    recipe (including the same receiving-type and dual-witness constraints U-MEM-28 fixed for the six).
    THE CROSS-ENTRY-PATH DIVERGENCE IS CARRIED FORWARD UNMOVED, as this row prescribed: the same model-supplied
    `scope_ref` MISMATCH is still refused as a DENIAL at the executor and as an INPUT ERROR at the dispatch prepare
    layer, so one logical failure still carries two classes. It stays INERT (the prepare path emits no
    `memory.failure_class`) and neither side was moved: the executor class is a landed decision and the dispatch
    retention is ratified operator text. It belongs to whichever arc closes the dispatch-side coverage gap.
    THE COVERAGE STATEMENT HELD IN BOTH DIRECTIONS, and is now pinned rather than described: NO new emission was
    wired on the prepare path (witnessed by an explicit no-span assertion) and NO existing emission was withdrawn
    (witnessed by an AST pin that the classify-routed population is still exactly FIVE production call sites --
    `memory_redaction.py`, `memory_capture.py`, `memory_tool_executor.py` x2 for `execute()` + the B-84 `validate()`
    pre-pass, and `native_memory_adapter.py`). ONE GROUNDED PREMISE CORRECTION, recorded rather than absorbed: the
    two `_standard_memory_tool_context` internal checks are UNREACHABLE through a whole dispatch -- a
    `record_scope is None` context is WITHHELD by `_standard_memory_tools_context`''s C-MEM-13 conjunct
    (`_packet_scope_matches_dispatch_family` returns UNVERIFIABLE), and a `scope_ref is None` context raises at the
    sibling schema-injection guard first -- so those two of the six are witnessed as CONSTRUCTED SHAPES over the
    real production function, with the withholding guard itself pinned so the unreachability claim is falsifiable
    rather than asserted. The other four are driven END-TO-END through the real dispatch. The U-MEM-25 closeout
    packet''s three `PENDING - U-MEM-28` rows are lifted, window 3 is closed the way windows 1 and 2 were, and
    `just memory-closeout-check` re-ran green. PRECEDING RECORD FOLLOWS. SPEC LEG LANDED 2026-08-06 -- `Spec_Memory_Substrate_v1.md` v1.2 -> v1.3 (C-MEM-19 gains
    the seventh member `input_validation_failure`, the NEW `### Input validation failure` subsection
    carrying the A-ii type boundary + the coverage statement + the re-typed sites'' fail-fast retry
    disposition, and two appended invariants) AND `Implementation_Plan_Memory_Substrate_v1.md`
    v1.2 -> v1.3 (U-MEM-22 acceptance extended to seven values; NEW U-MEM-28 at G8 decomposing the
    impl leg). Both clearance markers filed. STATUS `design_substrate_gated` -> `open` per the status
    enum at this file''s head: a filed fork does not flip the status -- only ratified + applied spec
    deltas plus the corresponding Phase-6 plan deltas open the row for code, and both landed at this
    PR. WHAT REMAINS IS THE IMPL LEG (U-MEM-28), not any design gate. Two drifts against the
    ratification text are RECORDED at the spec change-note rather than absorbed: the classify-routed
    population is FIVE production call sites at HEAD 3a1300eb (not four -- PR #1243''s B-84 validate
    pre-pass added `memory_tool_executor.py:355`, and it SATISFIES the ratification''s mandatory
    C-MEM-19 preservation clause as built), and every `lifecycle/llm_dispatch.py` offset in the six/two
    A-ii enumeration has shifted (six internal-fault sites now `:4359`/`:4363`/`:4475`/`:4483`/`:4977`/`:4988`;
    the two RETAINED model-supplied-`scope_ref` sites now `:4369`/`:4371`) -- re-resolved BY CONTENT,
    and the six/two split itself is unchanged. One judgement call is recorded as overturnable at the
    plan change-note: the fork''s "no new unit" line predates A-ii''s ratified scope, so the vocabulary
    half stayed an AC amendment as prescribed while the A-ii half took NEW U-MEM-28, which no landed
    unit owned. A SEVENTH RE-TYPING WAS PROPOSED AND WITHDRAWN: `_prepare`''s executor-side exhaustiveness
    fall-through (`memory_tool_executor.py:373`) raises the input-error type for a HARNESS-INTERNAL fault
    that spec v1.3''s type-boundary rule forbids, at a site the fork never surveyed because it surveyed
    `llm_dispatch.py` only. The spec leg initially required it; out-of-family round 8 objected that a spec
    leg APPLIES ratified fixes and may not widen a ratified six-site enumeration on its own reading, and
    the objection was ACCEPTED -- the obligation is removed and the finding carried, with its consequence
    stated: at U-MEM-28''s close, C-MEM-19''s type-boundary invariant has ONE KNOWN NON-CONFORMANT SITE,
    recorded and unrepaired. THAT FINDING IS NOW OWNED BY ITS OWN ROW, `B-114`, so it survives this row''s
    close at U-MEM-28 rather than disappearing with it (out-of-family round 9 [P1]); `B-114` carries the
    grounding pass, the conformance-repair-vs-ratified-extension discriminator, and the repair recipe.
    ONE FINDING CARRIED FORWARD, recorded not repaired (out-of-family review round 1 [P1]): the
    same model-supplied `scope_ref` MISMATCH is refused as a DENIAL at the executor (`memory_tool_executor.py:794-800`,
    one of the four sites this row''s own impl half re-classified to `policy_denial`) and as an INPUT ERROR
    at the dispatch prepare layer (`lifecycle/llm_dispatch.py:4371`, retained by ratified name), so one logical
    failure carries two classes. It is INERT today (the prepare path emits no `memory.failure_class`) and NEITHER
    side is movable at a spec leg (the executor class is a landed decision; the dispatch retention is ratified
    operator text). It belongs to whichever arc closes the dispatch-side coverage gap, which is when it becomes
    observable. RATIFIED 2026-08-05 as READING A, with sub-decision A-ii = RE-TYPE. Operator AskUserQuestion
    on the fork''s section 8 (`.harness/class_2_fork_b88_memory_failure_input_validation_class.md`).
    WHAT IS RATIFIED: C-MEM-19 gains `input_validation_failure` as a SEVENTH failure class (the
    stated fallback name `input_error` is NOT taken), plus the BOUNDARY INVARIANT distinguishing
    it from provider-adapter and serialization failure. SUB-DECISION A-ii (surfaced at codex R4)
    is ratified in its RE-TYPE form: the `llm_dispatch.py` raise sites that raise `MemoryToolExecutionInputError`
    for INTERNAL `MEMORY_TOOL_CONTRACTS` schema faults contradict the boundary invariant and are
    RE-TYPED -- SCOPED TO SIX SITES, NOT EIGHT (out-of-family review round 4 [P2], accepted after
    direct read): the internal set is llm_dispatch.py `:4334` / `:4338` (the harness''s OWN `RuntimeMemoryContext`
    left unset) and `:4450` / `:4458` / `:4947` / `:4958` (`MEMORY_TOOL_CONTRACTS` schema-injection
    faults). `:4344` and `:4346` are NOT in the set and MUST NOT be re-typed -- they validate
    the MODEL-SUPPLIED `scope_ref` argument (`arguments.get(''scope_ref'')`), missing/non-string
    at `:4344` and context-mismatched at `:4346`, which is exactly the malformed-model-argument
    case the ratified `input_validation_failure` class EXISTS for; re-typing them would deny the
    new class its two clearest dispatch-side emitters -- they are NOT recorded as mis-typed-at-birth
    and left in place. CLASSIFICATION-REACHABILITY COVERAGE, NARROWED HONESTLY at out-of-family
    review round 6 [P2] after direct grounding: `classify_memory_failure` has FOUR production
    call sites -- memory_redaction.py:273, memory_capture.py:778, memory_tool_executor.py:255
    and lifecycle/native_memory_adapter.py:556 -- and **ZERO in lifecycle/llm_dispatch.py**. The
    two RETAINED model-input raises (`:4344`/`:4346`) sit in `_standard_memory_tool_context`,
    called from `_openai_prepared_memory_tool_calls:4213` and `_ollama_prepared_memory_tool_calls:4845`
    -- the dispatch PREPARE path, which runs BEFORE `StandardMemoryToolExecutor.execute()` opens
    its telemetry span. They therefore emit NO `memory.failure_class` today, and the six-site
    re-type does NOT change that. **What A-ii establishes is reachability for the CLASSIFY-ROUTED
    population (the executor''s ten sites), NOT for the dispatch prepare path.** The spec leg
    MUST state the coverage in exactly those terms and MUST NOT promise dispatch-side emission
    it does not wire. **CROSS-FORK INTERACTION, recorded so neither leg widens the gap silently:**
    B-84''s ratified `validate()` pre-pass moves MORE malformed-model-argument failures onto this
    same unclassified prepare path. TWO PARTS, SEPARATED at out-of-family review round 7 [P2]
    -- an earlier revision of this note called the whole residue PRE-EXISTING, which OVER-CLAIMED
    and is corrected here rather than defended. (i) PRE-EXISTING: the `:4344`/`:4346` context/argument
    raises already sit on the unclassified prepare path at HEAD; neither ratification put them
    there. (ii) NEWLY INTRODUCED BY THE RATIFIED B-84 PRE-PASS: checks that TODAY raise INSIDE
    `_execute_authorized()` -- `limit=0` among them -- are classified today, because `StandardMemoryToolExecutor.execute()`''s
    catch calls `classify_memory_failure` (memory_tool_executor.py:255). Moving them into `_openai_prepared_memory_tool_calls`
    / `_ollama_prepared_memory_tool_calls` REMOVES that emission, and NONE of B-84''s ratified
    witnesses (W-1/W-2/W-4/W-5/W-7/W-8) asserts it. **PRESERVATION IS MANDATORY -- the ''or ratify
    the telemetry loss'' arm this note carried at round 7 is WITHDRAWN at out-of-family review
    round 9 [P2], because it would have authorized a CONTRACT WEAKENING with no design-substrate
    change. GROUNDED: C-MEM-19 (Spec_Memory_Substrate_v1.md:835-870) lists ''Standard memory tool
    call'' among the operations memory telemetry COVERS and names `memory.failure_class` among
    its REQUIRED attributes. Reading A opened NO spec leg and this row is `open` with no blocker,
    so an impl leg CANNOT elect to drop that attribute on its own authority -- doing so is exactly
    the X-AL-3 silent-absorption failure mode. THEREFORE: the build leg MUST PRESERVE classification
    for every check it relocates off the executor''s catch, and MUST witness the preserved emission
    end-to-end. IF PRESERVATION PROVES INFEASIBLE, that is a CLASS 1 BACK-FLOW against C-MEM-19
    -- filed and ratified BEFORE the code lands -- NOT an impl-discretion call and NOT closable
    by witnessing the absence. This constrains HOW Reading A is built; it does not reopen WHETHER
    it is built.** The unclassified prepare path itself stays a build-time question, closable
    only END-TO-END, since the gap is precisely that the production path never reaches the classifier.
    LEG OWNERSHIP, made explicit (out-of-family review round 1 [P2], accepted -- an earlier draft
    said ''RE-TYPED at the spec leg'' while also forbidding any src edit at the spec leg, leaving
    the re-typing with NO owning leg): the SPEC leg records the boundary invariant and the re-type
    DECISION in contract text and establishes the dispatch population''s classification REACHABILITY;
    the IMPL leg performs the SOURCE re-typing at the llm_dispatch.py sites and carries its reachability
    WITNESS (the fork''s ''impl rider''). READINGS B and C were NOT selected and are NOT partially
    adopted -- in particular C''s docstring-rewrite-at-ratification obligation DOES NOT FIRE,
    because under A the `memory_tool_executor.py:149`-`:171` stopgap docstring is rewritten to
    match the ratified outcome AT THE IMPL LEG (fork section 9, impl row), and no `harness-*/src`
    edit rides a doc-only ratification. STATUS design_substrate_gated (fork section 9: status
    flips to design_substrate_gated under A or B): OWED are the SPEC leg -- `Spec_Memory_Substrate_v1.md`
    v1.2 -> v1.3 by a dedicated spec-writer AND `Implementation_Plan_Memory_Substrate_v1.md` v1.2
    -> v1.3 (U-MEM-22 :829 extended, NO new unit), with BOTH clearance markers per root CLAUDE.md
    section 4.5 -- and then the IMPL leg (1 enum member + 1 declaration flip + 1 docstring rewrite
    + 1 fixture re-key; 8 witness rows flip + 2 added negative rows + the mutation probes; PLUS
    the A-ii source re-typing at the SIX llm_dispatch.py internal-fault sites, and a reachability
    witness SCOPED TO THE CLASSIFY-ROUTED POPULATION -- the dispatch prepare path is out of that
    scope per the coverage narrowing above). RETRY-EXIT-CLASS CONSEQUENCE OF THE RE-TYPE, surfaced
    at out-of-family review round 7 [P2] and GROUNDED BY DIRECT READ PLUS AN EMPIRICAL PROBE:
    `_classify_provider_exception` (lifecycle/retry_breaker_fallback.py:269) returns None -- FAIL-FAST
    -- only for an isinstance match against (LLMDispatchProviderUnreachableError, LLMDispatchPayloadShapeError,
    MemoryToolExecutionInputError) at `:325`-`:333`; EVERYTHING ELSE falls through to TRANSIENT_RETRY
    at `:337`. Probed: MemoryToolExecutionInputError -> None, base MemoryToolExecutionError ->
    transient-retry. **So re-typing the six internal sites AWAY from MemoryToolExecutionInputError
    FLIPS them from fail-fast to transient-retry unless the new type is ALSO admitted to that
    tuple -- meaning internal missing-context and schema faults would begin RETRYING and ADVANCING
    CANDIDATES.** This is a control-flow change the re-type INTRODUCES, not one it inherits. THE
    SPEC LEG MUST RECORD THE INTENDED EXIT CLASSIFICATION for the new type explicitly (fail-fast
    or retry -- it is a real choice, and an internal wiring fault that retries against a different
    candidate is not obviously wrong), AND THE IMPL LEG OWES AN END-TO-END DISPATCH/RETRY WITNESS
    at the re-typed sites. This obligation is IN SCOPE for the re-type and is NOT covered by the
    classify-routed-population narrowing above, which scopes TELEMETRY reachability, not RETRY
    behaviour.. ZERO hash / CXA / contract-number impact. NO COUNCIL is owed: the convening was
    conditional on Reading B only, and B was not selected. DRIFT REPAIRS APPLIED IN PLACE AT THIS
    LEG (fork section 10''s six items, which section 8 routes to the ratification leg''s register
    touch; none changes any disposition): (1) the scope-value-domain declaration cites are `memory_capture.py:286`-`:288`
    (`MemoryCaptureScopeValueDomainError`, class :271) and `:307`-`:309` (`MemoryCaptureReservedActorError`,
    class :291) -- the previously recorded `:215`/`:236` were a blank line and a docstring; (2)
    `MemoryToolExecutionInputError` is 10 sites IN `memory_tool_executor.py` (correct as scoped
    to the classified population) but 18 REPO-WIDE; of the extra 8 in `llm_dispatch.py`, SIX are
    internal-fault sites A-ii re-types (`:4334`, `:4338`, `:4450`, `:4458`, `:4947`, `:4958`)
    and TWO -- `:4344` and `:4346` -- validate the MODEL-SUPPLIED `scope_ref` argument and KEEP
    the new class (round-4 scoping); (3) `MemoryToolExecutionDeniedError` has 15 raise sites at
    HEAD, not 14 (growth from later arcs, not a regression); (4) the classification table is 39
    parametrized rows (AST count), not 37; (5) the mutation probes are SEVEN, enumerated (a)-(g)
    -- the ''all five'' header undercounted its own body, no probe is missing; (6) `_NativeMemoryPolicyDeniedError`
    has 6 raise sites at HEAD, not 4 (`_require_scope_family_in_domain:305` and a third `_require_retrieval_allowed`
    branch at `:334` added by later arcs) -- and whether those two newcomers are span-pinned was
    NOT verified by the fork and is recorded as UNVERIFIED rather than assumed, so no later arc
    inherits ''all four are pinned'' as covering six sites. IMPL HALF LANDED 2026-07-28; the SPEC
    half keeps this row registered_finding. (1) TYPE-KEYED CLASSIFICATION -- LANDED, per the summary
    populations table. (3) WITNESSES + MUTATION PROBES -- LANDED, including the lens-3 breaker-cause
    pin. (2) DECIDE THE INPUT-VALIDATION CLASS -- REMAINS OPEN and spec-side: the impl half took
    the least-wrong existing value (provider_adapter_failure) with its rationale recorded in the
    summary; adding a member to the closed C-MEM-19 vocabulary is a Class 1 back-flow to Spec_Memory_Substrate_v1.md
    C-MEM-19, NOT absorbable at Phase 7 per X-AL-3, and when it lands the single flip site is
    MemoryToolExecutionInputError.memory_failure_class. ORIGINAL CLOSE-OUT TEXT FOLLOWS. Deliberately
    NOT fixed inside the B-84 arc: changing the classifier changes emitted telemetry for every
    memory failure path (a blast-radius decision of its own), and the vocabulary half is spec-side.
    (1) TYPE-KEYED CLASSIFICATION -- dispatch on exception TYPE (an isinstance ladder, or a registry
    keyed on the typed families) instead of message/type-name substrings, so MemoryToolExecutionDeniedError
    maps to policy_denial BY CONSTRUCTION rather than by wording, and narrow the IO rule to isinstance(exc,
    OSError), dropping the "io" in exc_name clause entirely -- it cannot be made safe, since any
    type name containing execution / region / revision collides. (2) DECIDE THE INPUT-VALIDATION
    CLASS -- either map input errors onto an existing value with a stated rationale (impl-side),
    or amend the C-MEM-19 vocabulary to add one, which is SPEC-SIDE: a Class 1 back-flow to `Spec_Memory_Substrate_v1.md`
    C-MEM-19, NOT absorbable at Phase 7 per X-AL-3. (3) Witness with a table-driven test over
    one real instance per typed family asserting the emitted memory.failure_class, plus an assertion
    pinning the breaker cause on the fail-fast exit (the lens-3 gap above); mutation-probe by
    restoring the substring rule. Cross-ref B-84 (the arc that surfaced this and shifted one class).
    2026-07-29 dormancy re-check: premise intact at HEAD dd2a8c1a -- the type-keyed classifier
    (`_declared_failure_class` wins outright, name-substring IO rule deleted) and the six-member
    `MemoryTelemetryFailureClass` vocabulary (`harness-is/src/harness_is/memory_observability.py:33-41`)
    are both unchanged, and the U-MEM-26 contract-table rows extend B-88''s own idiom rather than
    diverging from it (the scope-value-domain exceptions declare `memory_failure_class` at their
    definition sites, `memory_capture.py:286`-`:288` and `:307`-`:309` -- cite repaired 2026-08-05,
    see the drift-repair list above), so step (2) -- the input-validation class, a Class 1 C-MEM-19
    vocabulary amendment -- remained the sole open half UNTIL THE 2026-08-05 RATIFICATION, which
    DECIDED it as Reading A + A-ii (see the ratification paragraph at the head of this close_out);
    the SPEC LEG that writes the decision LANDED 2026-08-06 at PR #1245 (see the close_out lead paragraph), so what remains is the IMPL LEG at U-MEM-28, not the decision and not the spec text. SPEC-HALF FORK FILED
    2026-08-05 at `.harness/class_2_fork_b88_memory_failure_input_validation_class.md` -- readings
    A (add a seventh member `input_validation_failure`, RECOMMENDED) / B (bless the `provider_adapter_failure`
    stopgap in contract text) / C (defer under a D-0..D-3 demand test); recommendation A, runner-up
    C, discriminator named (does `memory.failure_class` name the fault KIND or the fault LOCUS);
    the council premise is PROBE-RESOLVED -- zero exhaustive consumers measured at HEAD 4f914159,
    so no convening is owed unless B is selected. Ratification joined the 2026-08-05 operator
    batch alongside B-96/B-98/B-104/B-93+B-45/B-84/B-95 and is RECORDED above.'
  council: 'NO CONVENING OWED -- settled 2026-08-05: the operator ratified READING A, and the
    conditional convening was owed only under Reading B. PROBE-RESOLVED at the 2026-08-05 filing
    -- no convening owed unless Reading B is selected. The original disposition ("Likely, for
    step (2) only... an observability-vocabulary blast radius, every consumer switching on the
    value") was measured at HEAD 4f914159 and its premise FALSIFIED: zero consumers switch exhaustively
    (no match statement, no enum iteration, no __members__ access; the only site enumerating the
    vocabulary is the enum definition itself), so a seventh member cannot break any call site.
    A dyadic C7/C8 x memory-contract-owner convening is owed ONLY if the operator selects Reading
    B (blessing the stopgap in contract text is a real observability-vs-contract tension). Steps
    (1)/(3) unchanged: mechanical once (2) is settled.'
  heading: '### B-88 · Memory failure telemetry: substring-keyed `classify_memory_failure` collapses the typed input-error family — and two real policy denials — to `io_failure` *(surfaced by the merge-gate lens-2 sweep on the B-84 arc, 2026-07-28)* *(**RATIFIED 2026-08-05 as READING A, sub-decision A-ii = RE-TYPE**; SPEC LEG LANDED 2026-08-06 at PR #1245; IMPL LEG (U-MEM-28) LANDED 2026-08-07 -- CLOSED, with ONE known non-conformant type-boundary site carried at `B-114`)*'
- id: B-89
  title: 'Memory-scope producer asymmetry: capture stamps raw provider KEYS while retrieval requests carry family VALUES'
  status: closed
  pr: '#1148'
  summary: 'Surfaced by the B-86 grounding pass (2026-07-28). MECHANISM: `harness-runtime/src/harness_runtime/memory_capture.py:585-591` constructs its OWN `MemoryScope` with `provider_family=provider` -- the raw per-turn dispatched provider KEY (vocabulary `claude_code`/`codex`/`gemini`/`antigravity`/`ollama`/`anthropic`/`openai`/`google`, `harness-runtime/src/harness_runtime/lifecycle/cross_family_cost_tag.py:60-69`) -- while retrieval requests carry the composed `record_scope`''s `ProviderFamily` VALUE (`harness-runtime/src/harness_runtime/automatic_memory.py:197`). All three enforcement sites compare raw strings with NO normalization (`harness-is/src/harness_is/memory_retrieval.py:371-384`, `memory_retrieval_index.py:379-396`, `memory_policy.py:336-359`). EMPIRICALLY CONFIRMED AT REGISTRATION: an `ollama`-written note is invisible even to a `local_open_weight`-scoped request OF ITS OWN FAMILY, and tool-written notes are write-only for 5 of the 8 registered provider keys (only `anthropic`/`openai`/`google` coincide with a family value, and by accident). CONSEQUENCE: memory continuity under primary-derived keying is largely NOTIONAL today -- the continuity side of B-86''s Q1 tension only becomes real after this row lands. EXISTENCE IS INDEPENDENT OF B-86 Q1: both Q1 readings leave the ollama note invisible, because Q1 asks primary-or-candidate while this row asks key-or-value -- orthogonal axes. DIRECTION, HOWEVER, IS DETERMINED BY THE B-86 COUNCIL RECORD (fork doc section 5-Q1): the WRITER adopts the reader''s key -- the capture path stops constructing an independent `MemoryScope` and consumes the run''s composed `record_scope` (one-source-of-truth; will incidentally close B-90 when it lands). INTRINSIC EVIDENCE FOR THE FAMILY READING: the field is literally named `provider_family` (`harness-is/src/harness_is/memory_record_envelope.py:120`) while C-MEM-03 declared no value domain AT REGISTRATION TIME -- evidence for the fork to weigh, NOT an impl licence. C-MEM-03 now DECLARES the value domain at v1.1, so this row builds against a stated contract rather than an inference. HASH-INERT: `MemoryScope` is not folded into `memory_id` (content-hash + run_id only, `memory_capture.py:574-575`), so a writer normalization moves no identities and the blast radius is low.'
  close_out: 'CLOSED 2026-07-28 at the U-MEM-26 impl leg (this PR). `EpisodicMemoryCapture` gained a `record_scope` parameter and writes records under the run''s composed scope VERBATIM instead of constructing its own from the per-turn provider key; BOTH construction sites pass the composed scope. The `provider` argument is unchanged and still feeds the C-MEM-08 payload and the C-MEM-19 span as the raw key it is -- only the RECORD scope stops deriving from it. The residual self-construction remains for callers with no composed scope and is bound by the C-MEM-03 value domain instead. The ollama divergence is the discriminating witness throughout (its provider KEY is not value-equal to its `ProviderFamily`, so a test passing under `anthropic` or `openai` proves nothing), and round-trip retrievability is witnessed directly: `test_captured_scope_round_trips_where_the_pre_repair_scope_does_not` in `harness-runtime/tests/test_u_mem_26_write_boundary.py`, carrying an explicit precondition anchor because both sides of its comparison are derived from the writer and it would otherwise stay green against a writer that repaired nothing. Mutation-probed: ignoring the composed `record_scope` reddens all four capture witnesses. Forward-only migration residual stands as stated at C-MEM-03 -- pre-repair raw-key records are not rewritten and stay unreachable under family-scoped requests.'
  council: 'No -- the keying decision is the B-86 council record''s; once the spec-leg ratifies, the repair is mechanical (writer consumes the composed record_scope).'
  heading: '### B-89 · Memory-scope producer asymmetry: capture stamps raw provider KEYS while retrieval requests carry family VALUES *(surfaced by the B-86 grounding pass, 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*'
- id: B-90
  title: 'Capture-path MemoryScope omits tenant and workload_class -- tool-captured records are tenant-unpartitioned'
  status: closed
  pr: '#1148'
  summary: 'Surfaced at the B-86 council convening (2026-07-28), as the adjacent half of Probe C. MECHANISM: `harness-runtime/src/harness_runtime/memory_capture.py:585-591` constructs its `MemoryScope` with only `project` / `workflow` / `provider_family` / `cli_profile` / `visibility` -- `tenant` and `workload_class` are omitted ENTIRELY -- while the composed REQUEST scope populates them (`harness-runtime/src/harness_runtime/automatic_memory.py:194-199`). Under the enforcement predicates'' wildcard-on-`None` semantics a `None` RECORD value passes all three sites: `MemoryRetriever._scope_mismatch` skips a `None` on either side (`harness-is/src/harness_is/memory_retrieval.py:377-383`); the index `_scope_matches` carries the same field list (`memory_retrieval_index.py:379-396`); and `memory_policy._scope_not_broader` guards only NON-`None` record values (`if record_value is not None and requested_value != record_value`, `memory_policy.py:336-359`), so a `None` record tenant passes. NET: every tool-captured / episodic record is tenant-UNPARTITIONED and reachable by ANY tenant''s family-matching request -- against the cleared threat-model mandate at `design-substrate/Spec_Memory_Substrate_v1.md:481` (''Retrieval and injection enforce project, workflow, tenant, provider-family, CLI-profile, and visibility scope before ranking''). HONEST CAVEATS: single-tenant local deployment is the current default posture, so live exposure is bounded today; and the wildcard-on-`None` semantics were, AT REGISTRATION TIME, load-bearing but undocumented -- they are DOCUMENTED as of Spec_Memory_Substrate v1.1 (C-MEM-03: a stored `null` is the unpartitioned wildcard; a `null` REQUEST does not widen access past a family-scoped record).'
  close_out: 'CLOSED 2026-07-28 at the U-MEM-26 impl leg (this PR), incidentally by the B-89 writer repair exactly as route (1) predicted: the run''s composed `record_scope` carries `tenant` and `workload_class`, so tool-captured records are no longer tenant-unpartitioned. Witnessed per this row''s own step (3) by THREE SEPARATE per-predicate tenant-denial assertions in `harness-is/tests/test_memory_scope_value_domain.py` -- `test_b90_tenant_denial_at_the_retriever_predicate` (`MemoryRetriever._scope_mismatch`), `test_b90_tenant_denial_at_the_index_predicate` (`_scope_matches`), and `test_b90_tenant_denial_at_the_policy_predicate` (`_scope_not_broader`) -- never one composite retrieval test, which is precisely how the omission survived; a fourth witness (`test_b90_null_tenant_request_is_denied_at_every_predicate`) pins the wildcard-on-`None` arm. Each is mutation-probed. The cleared threat-model mandate at `design-substrate/Spec_Memory_Substrate_v1.md:481` is now conformant for the capture path.'
  council: 'No -- conformance repair to a cleared threat-model invariant (:481); no cross-domain tension. Fold into the B-89 writer repair at the B-86 spec-leg''s impl leg.'
  heading: '### B-90 · Capture-path `MemoryScope` omits `tenant` and `workload_class` — tool-captured records are tenant-unpartitioned *(surfaced at the B-86 council convening, 2026-07-28; CLOSED 2026-07-28 — U-MEM-26 impl leg)*'
- id: B-91
  title: 'Report-only degraded-serve failure_class/policy.decision semantics for scope-mismatch and not_derived reasons'
  status: closed
  pr: '#1154'
  summary: 'Surfaced by the merge-gate spec-conformance lens on the C-MEM-19 emitter-attrs arc (PR #1151, 2026-07-29). The B-83 degraded-serve emitter''s REPORT-ONLY branch (`degraded.rendered is None`, `harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py`) fires on THREE reason families, not one: (1) the scope-mismatch conjuncts (`provider_scope_mismatch` / `provider_family_scope_mismatch`); (2) genuine `MemoryAccessModeDenialReason` values carried verbatim from `prompt_packet_fallback_denial` (`no_supported_mode` / `token_budget_empty`, `harness-runtime/src/harness_runtime/memory_context.py:134-138`); and (3) the `not_derived` / `prompt_packet_denial_undetermined` sentinels (`memory_context.py:48-52`). Family (2) was CLOSED at this PR -- it now carries `failure_class=POLICY_DENIAL`, mirroring the primary DENIAL span which sets exactly that for exactly those values (`memory_context.py:210-216`) off a real `select_memory_access_mode` run (`memory_context.py:545-549`). TWO QUESTIONS REMAIN OPEN, both bounded telemetry-semantics decisions. Q1 -- should family (1) carry `POLICY_DENIAL` too? Post-B-86 the cross-family withhold is policy-MANDATED, not a mere absence of decision: `design-substrate/Spec_Memory_Substrate_v1.md:511` records it on the C-MEM-19 surface "with the denial reason carried as an attribute value", `:523` states it as a C-MEM-13 invariant, and `:559` qualifies C-MEM-14 exposure by it. So the pre-existing rationale ("no resolver made a policy decision on this branch") is over-stated. What is genuinely undecided is whether a CONTRACT-mandated withhold is the same telemetry CLASS as a RESOLVER''s denial. Q2 -- does `not_derived` belong on `memory.policy.decision` at all? It is a fail-safe NON-decision, and it is defined to be distinct from every `MemoryAccessModeDenialReason` value (`memory_context.py:48-52`), yet it lands on the same attribute that otherwise names a gate a resolver closed; a different axis (or a dedicated sentinel-aware attribute) may be the truthful home. NO CONTRACT DEFECT EITHER WAY: C-MEM-19 requires `memory.policy.decision` on every memory span and does not enumerate which spans owe a `failure_class`, so both current answers are conformant -- this row is about telemetry TRUTHFULNESS, not conformance. CONSERVATIVE POSTURE HELD AT THIS PR: families (1) and (3) leave `failure_class` UNSET and are pinned as absent by witnesses, because adding the attribute later is additive while retracting a wrong one is not.'
  close_out: 'CLOSED 2026-07-29 (this PR). BOTH questions decided on one grounded pass against the byte-read spec. Q1 -- YES, the scope-mismatch family DOES carry `failure_class=POLICY_DENIAL`. The deciding text is the C-MEM-13 recording-surface paragraph at `design-substrate/Spec_Memory_Substrate_v1.md:511`: the withholding is recorded on the C-MEM-19 surface "whose declared coverage already includes standard memory tool calls and policy denial". That sentence justifies needing no NEW coverage item by pointing at the EXISTING list at `:674-683`, and of its two candidate items a withheld exposure is definitionally not a tool CALL (the schemas never reach the model), so "Policy denial" (`:683`) is the item being invoked. `memory.failure_class` is the only required attribute (`:687-698`) whose value domain carries that classification, and the failure-class invariant at `:703` uses the identical phrase -- so reading the coverage item and the vocabulary as unrelated would leave the coverage item with no carrier at all. The vocabulary is scoped by OUTCOME KIND (policy denial / path violation / IO failure / serialization failure / provider adapter failure / retrieval empty-result), never by WHICH component decided, which settles the genuinely-open part of the question: a CONTRACT-mandated withhold and a RESOLVER''s denial are one telemetry class. Corroborating: the withhold is a MUST NOT at `:509`, a C-MEM-13 invariant at `:523`, a C-MEM-14 exposure qualification at `:559`, and `no_memory_access` -- what both branches report -- is itself named "a valid policy outcome" at `:522`. Q1 SCOPE REFINED the same day by out-of-family Codex R1, two [P2]s both upheld -- the first application set the class on BOTH scope conjuncts unconditionally, which over-claimed on two counts, and the arc own truthfulness principle (never report a determination that was not made) is what condemns it. [P2-1] the IDENTITY conjunct must not carry the class merely because it FIRED: it fires whenever the provider KEY moved, including an `openai`-composed context rebound to `codex` -- one family, two keys -- where C-MEM-13, which withholds only across FAMILIES, mandates nothing whatsoever. R1 corrected this to an unconditional False on that branch, which out-of-family Codex R2 (same day) then found to UNDER-report by the mirror amount: an `anthropic`-composed context rebound to `openai` resolves BOTH families and finds them UNEQUAL -- a CONFIRMED cross-family withhold -- yet the identity return exits before the relation is consulted, so the span omitted the class purely because of TEST ORDER. CONVERGED RULE (R2, applied): `policy_denial` is a function of the family RELATION alone and never of which conjunct fired -- `policy_denial` iff CONFIRMED_MISMATCH, on BOTH scope conjuncts. `_packet_scope_family_relation` is now resolved ONCE, before the identity test; it reads only `record_scope` + `provider_name`, neither touched by the identity test, so evaluating it earlier carries no ordering hazard and costs two dict lookups on an already-degraded path. A SAME-family rebind and an UNVERIFIABLE rebind both stay unclassified, the withholding is unchanged on every branch, and `memory.policy.decision` still names the gate verbatim. [P2-2] the FAMILY conjunct carries the class ONLY on a CONFIRMED mismatch: `_packet_scope_matches_dispatch_family` returned False for an absent `record_scope`, an out-of-domain scope family, or an unregistered provider key -- states where the relationship is UNKNOWN, not known-unequal -- so the unconditional flag reported fail-closed VALIDATION as an operator policy decision. The Q1 reading above is unchanged and still governs the case it actually addressed: a CONFIRMED cross-family withhold, both families resolved and unequal. DISCRIMINATION MECHANISM: `_packet_scope_family_relation` (new) returns a tri-state `_ScopeFamilyRelation` -- MATCH / CONFIRMED_MISMATCH / UNVERIFIABLE -- over the SAME existing `provider_family_for_scope_check` + `canonical_scope_family` authorities, no second policy path; `_packet_scope_matches_dispatch_family` is now a pure projection of it (`is MATCH`), so both WITHHOLDING callers behave identically to before and only the telemetry class changed. Q2 -- KEEP AS IS: the `not_derived` / `prompt_packet_denial_undetermined` sentinels stay on `memory.policy.decision` verbatim, NO code change. `memory.policy.decision` is a REQUIRED attribute on every memory span (`:694`), so omitting it on the one branch where no gate closed would drop a required attribute exactly where a reader most needs to know the decision axis was empty; and mapping to the primary span''s generic `denied` fallback would assert a denial nobody issued -- the same false accusation the per-reason [P2-e] split exists to prevent. Verbatim is truthful on both halves of the B-87/B-91 principle: the sentinel names NO gate (it is defined distinct from every `MemoryAccessModeDenialReason`, `memory_context.py:48-52`, so it can never be misread as one), while the span still reports `denial` / `no_memory_access` / `record_count=0`, so the degradation is never hidden. WITNESSES STAY DISCRIMINATING per close-out step (3): the split is policy-DETERMINED (selector-denial + CONFIRMED cross-family, under EITHER scope conjunct) vs nobody-determined (same-family identity rebind + UNVERIFIABLE family states + sentinels), and all cells are pinned -- `test_u_mem_26_cross_family_ollama_dispatch_withholds_tools_and_packet` (scope family `openai`, dispatch `ollama` to `local_open_weight`, both resolve and are unequal) asserts `failure_class` PRESENT; `test_b83_provider_rebound_context_is_reported_but_never_served` (`anthropic`-composed rebound to `openai` -- CROSS-family, both sides resolve and differ) asserts it PRESENT on the IDENTITY guard, while `test_b91_same_family_provider_rebind_is_not_a_policy_denial` (`codex`-composed rebound to `openai`, ONE family) asserts it ABSENT on that same guard -- the R2 pair that pins the class to the relation rather than to the branch; `test_b83_absent_record_scope_fails_closed_to_report_only` and `test_b86_unregistered_provider_key_fails_closed_against_local_scope` assert it ABSENT on the two UNVERIFIABLE shapes; `test_b83_undervied_eligibility_reports_not_derived_not_a_gate_denial` asserts it ABSENT on the sentinel and additionally pins the Q2 verbatim value. MUTATION PROBES, ALL KILLING (two re-run at the R2 convergence, on top of three at the R1 refinement and four at the first application): reverting the family construction to an unconditional True reddens BOTH UNVERIFIABLE witnesses; dropping the CONFIRMED_MISMATCH True reddens the U-MEM-26 cross-family witness; R2 (a) forcing the identity construction back to a relation-blind False reddens the cross-family rebound witness (KeyError on `memory.failure_class`); R2 (b) forcing it unconditionally True reddens the same-family rebind witness. NO CONTRACT DEFECT AND NO SPEC AMENDMENT -- this was a telemetry-truthfulness reading of already-cleared text, decided within the C-MEM-19 CLOSED enum (attribute VALUES only, no new members).'
  council: 'No -- a bounded telemetry-semantics decision inside an already-cleared contract, with no cross-domain tension to surface. It does require a careful spec READING (C-MEM-13 `:511`/`:523` + C-MEM-14 `:559` against C-MEM-19), so it is not mechanical, but a single grounded pass settles both questions together.'
  heading: '### B-91 · Report-only degraded-serve `failure_class` / `policy.decision` semantics for the scope-mismatch and `not_derived` reason families *(surfaced by the merge-gate spec-conformance lens on the C-MEM-19 emitter-attrs arc, 2026-07-29; CLOSED 2026-07-29 — Q1 policy_denial applied (R2-converged: class follows the family relation, not the conjunct), Q2 keep-as-is)*'
- id: B-92
  filing: .harness/class_1_fork_c_mem_10_cross_family_promotion_eligibility.md
  title: 'C-MEM-10 promotion eligibility of records captured during a cross-family fallback leg'
  status: closed
  pr: '#1159'
  summary: 'The spec''s OWN named-but-undischarged open question, now filed as a Class 1 fork (`.harness/class_1_fork_c_mem_10_cross_family_promotion_eligibility.md`). Since U-MEM-26 (PR #1148) capture writes under the run''s COMPOSED scope, so a cross-family fallback leg lands an `anthropic`-family record while the dispatch that produced its content ran on `openai` -- witnessed by a shipped test, `test_u_mem_26_capture_is_unaffected_by_the_cross_family_withholding` (`harness-runtime/tests/test_automatic_memory_runtime.py:515`, assert at `:579`). Whether such a record may be PROMOTED, and thereafter injected back into future runs under the primary''s family, is the C-MEM-10 question the spec change-note paragraph "Named open question carried forward, not discharged." (`Spec_Memory_Substrate_v1.md`, change-note v1 -> v1.1) explicitly carries forward -- "v1.1 neither resolves nor forecloses it" -- and that `Implementation_Plan_Memory_Substrate_v1.md` lists out-of-scope for U-MEM-26 with the same carry, at U-MEM-26''s "Out of scope for this unit:" subsection. CITE CONVENTION (Codex R3 [P2-2]): this row cites `Spec_Memory_Substrate_v1.md` by contract ID + section name + byte-verified quoted fragment, never by line, per that spec''s own v1.1 instruction ("Surfaced finding, not patched.": "Cite this spec by contract ID and section name, not by line, in any text authored after v1.1."); code cites keep file:line and were verified at HEAD `6ab41d7f`, against which no code file differs from the `dd2a8c1a` grounding HEAD. CURRENT BEHAVIOUR: SILENT AT BOTH ENTRY POINTS, BUT ZERO LIVE AUTO-PROMOTION EXPOSURE AT HEAD -- the exposure claims of the first draft were corrected by out-of-family Codex R1 (2026-07-29, one [P1] and one [P2], both upheld on direct re-read of the code). (1) The hint path -- the candidate-side family is PRODUCER-DEFINED, hence UNDEFINED today: `_candidate_from_hint` (`harness-runtime/src/harness_runtime/memory_promotion.py:296`) builds risk flags from the HINT with `source_scope=record.envelope.scope` (`:626-629`), but the CANDIDATE side that `_family_escapes_source` (`:715-743`) compares is `hint.suggested_scope` (`:682`) -- a REQUIRED caller-supplied field (`:191`) with no default and no derivation from the record, so nothing in the pipeline makes it the family of the source record. A producer mirroring the composed run scope supplies anthropic (no flag); a producer supplying the DISPATCHED family already trips `CROSS_SCOPE` (`:683`). WHICH convention a producer must supply is itself part of what C-MEM-10 owes, and it cannot be read off code that has no producer. `_auto_promote_allowed` (`:776-790`) carries no family term and no risk-flag term. The path is also UNFED: the sole production caller passes `promotion_candidates=()` (`automatic_memory.py:273`), and the extractor is otherwise reached only from compaction safety (`memory_compaction_safety.py:159`). (2) The model-facing tool path -- NOT a live auto-promotion exposure, for two INDEPENDENT reasons. `MemoryToolExecutor._propose_promotion` (`memory_tool_executor.py:399-435`) serves the C-MEM-14 `memory.propose_promotion` tool, hardcodes `confidence=HIGH` (`:423`), and computes `risk_flags=_promotion_risk_flags(source)` (`:425`) which inspects ONLY `redaction_state` (`:882-885`) -- that no-scope-comparison point STANDS. What does NOT stand is reachability for a CAPTURED record. (a) The record KIND is denied before a candidate is ever constructed: `_read_retrievable_record_by_ref` (`:554-567`) routes the source through `resolve_retrieval`, and `_record_kind_allowed` (`harness-is/src/harness_is/memory_policy.py:327-333`) denies any kind outside `eligible_record_kinds` (`:300-304`); the SOLE production policy `_policy_from_config` (`automatic_memory.py:532-557`) -- the only ENABLED `MemoryPolicyDocument` constructed anywhere under `src/`, the other being `DEFAULT_DISABLED_MEMORY_POLICY` (`memory_policy.py:105`, `enabled=False`) -- pins that set to `_retrievable_kinds()` (`:560-569`), the seven promoted/derived kinds, EXCLUDING every kind the capture path writes (`EPISODIC_RUN` / `EPISODIC_TURN` / `TOOL_EVENT` / `COMPACTION_EVENT`, `memory_tool_executor.py:769-775`), which covers both the witness EPISODIC_TURN record and the TOOL_EVENT output of `memory.write_note` itself (`capture_tool_event`, `memory_capture.py:471-473`); the call raises `MemoryToolExecutionDeniedError` (`:565-566`) BEFORE candidate construction at `:417`. (b) An eligible record could not auto-promote either: `_policy_from_config:542` pins `promotion_decision=PROPOSE_SEMANTIC` while `_promotion_auto_allowed` (`:897-903`) returns True only for `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL`, so `auto_promote_allowed` is False for EVERY candidate on this path and `_propose_promotion` takes the `propose_for_review` branch (`:456`) with review_required True -- a branch that is DURABLE BUT INACTIVE, not ephemeral (the R2 correction of the first draft''s "review, not durability"): `propose_for_review` (`memory_promotion.py:328-352`) calls `_persist_decision` with `status=PROPOSED` and `_persist_decision` (`:451-506`) writes BOTH the semantic/procedural record (`write_record`, `:498`) AND a durable C-MEM-08 operation entry (`append_memory_operation`, `:499-506`); what review withholds is ACTIVATION, not persistence, since a PROPOSED record is excluded from retrieval by construction (`_INACTIVE_STATUS_REASONS["proposed"]` -> `RetrievalExclusionReason.PROPOSED`, `harness-is/src/harness_is/memory_retrieval.py:65`/`:71-77`) and only `approve`/`edit_and_approve` (`:354-390`/`:416-449`) flip it to ACTIVE, neither consulting any family term. WHAT THE QUESTION ACTUALLY IS, stated exactly: not that a live tool auto-promotes cross-family-captured content today, but (i) FUTURE RETRIEVABLE-KIND RECORDS -- `eligible_record_kinds` is a C-MEM-09 POLICY field, not a constant, and a SECOND-ORDER case needs no policy change at all, since a semantic/procedural record ALREADY promoted out of a cross-family leg is an eligible kind by construction and carries the same unrecorded provenance forward; (ii) FUTURE POLICY CONFIGS setting `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL` with `ReviewMode.AUTOMATIC`, which C-MEM-09 explicitly permits; (iii) the FIRST CALLER to populate `promotion_candidates`. NONE OF THE THREE IS INDIVIDUALLY SUFFICIENT, AND THIS ROW STATES THE COMBINATIONS rather than letting "a single policy field" imply otherwise (Codex R6 [P2-2], upheld by direct read at `1c99e208`; the earlier framing was true of candidate-REACHABILITY, not of active PROMOTION). The gate functions are conjunctions and one conjunct is ALREADY SATISFIED IN PRODUCTION. (a) Admitting a capture-written kind into `eligible_record_kinds` alone buys CANDIDATE-REACHABILITY ONLY -- `_policy_from_config` still pins `PROPOSE_SEMANTIC` (`automatic_memory.py:542`), so `_promotion_auto_allowed` (`memory_tool_executor.py:897-903`) is False and the record lands as a durable, retrieval-excluded PROPOSED. (b) Switching the decision to `PROMOTE_SEMANTIC`/`PROMOTE_PROCEDURAL` alone buys ACTIVE AUTO-PROMOTION OF ALREADY-ELIGIBLE KINDS ONLY -- exactly the second-order case -- since capture-written kinds are still denied before a candidate exists. (c) `ReviewMode.AUTOMATIC` buys NOTHING: IT IS ALREADY SET (`automatic_memory.py:555`), so only the decision conjunct of `_promotion_auto_allowed` is unmet at HEAD; the other two modes are live foreclosures a deployment could re-assert (`OPERATOR_REQUIRED` forces review at `:889-890`, `FORBIDDEN` denies the tool at `:406-407`). So on the TOOL PATH live auto-promotion of a CAPTURED record requires (a) AND (b), both hard-coded side by side at `_policy_from_config` (`:542` and `:556`) and drawn from no `config.memory` field -- today a code change, though C-MEM-09 POLICY fields by contract, which is why the gap is real regardless. On the HINT PATH it requires (iii) AND (b) AND non-LOW hint confidence (`memory_promotion.py:786-787`; `PROMOTE_PROCEDURAL` specifically for a `PROCEDURAL_UPDATE` kind, `:788-790`) AND AN ACTUATOR THAT DOES NOT EXIST: the only `PromotionDecisionService.approve` call site anywhere under `src/` is the tool path''s (`memory_tool_executor.py:449`), `extract_from_records` is reached solely from `CompactionSafetyHook.extract_candidates` (`memory_compaction_safety.py:153-159`) whose sibling `complete_compaction` persists DISPOSITIONS and never promotes, and `CompactionSafetyHook` itself has no production caller at all (only its definition and its `harness_runtime/__init__.py` export), so `auto_promote_allowed=True` is computed, persisted and ACTUATED BY NOTHING. The CONTRACT gap is unconditioned by all of it: C-MEM-10 has no provenance term at any policy setting. The §1 witness stands regardless -- it pins the CAPTURE-side fact, which is what makes the C-MEM-10 question well-posed at all. PREREQUISITE FINDING (the load-bearing part), SCOPED BY CODEX R2 TO THE COMPOSED-SCOPE PATH, WHICH IS THE U-MEM-26 PRODUCTION PATH: on that path the promotion pipeline CANNOT DISCRIMINATE even if policy said to. `EpisodicMemoryCapture` takes `record_scope: MemoryScope | None = None` (`memory_capture.py:253`) and `_scope_for_record` (`:1048-1086`) branches on it. COMPOSED path (scope supplied): the envelope is the run''s composed scope (`:1074`) through canonicalize-or-deny (`:1083-1086`) and the per-dispatch `provider` NEVER reaches the envelope; this is production -- the automatic-memory runtime always supplies a composed scope keyed to `fallback_chain.primary.family.value` (`automatic_memory.py:211-221`, family at `:217`, threaded at `:504-512`/`:511`) -- and it is the path the §1 witness exercises. RESIDUAL path (no scope): `_scope_for_record` builds the scope itself with `provider_family=provider` (`:1075-1082`, field at `:1079`), so such a record DOES retain its producing family and the exhaustive claim does NOT hold for it -- but it retains that family by PARTITIONING the record under it, the very per-turn re-derivation B-89 named as a defect and B-90/U-MEM-26 repaired, which means the cross-family question DOES NOT ARISE there at all (a record written under the producing family is not a foreign-family record inside a primary''s partition), so there is nothing for C-MEM-10 to discriminate. FORCE UNDIMINISHED: the path where the question is well-posed is exactly the path that cannot answer it, and it is the production path. On the composed-scope path, `capture_turn_completion` writes `content` with NO provider and NO dispatched-model field (`:407-422`); `provider`/`model` reach only the C-MEM-08 ledger row payload (`:694-707`, `MemoryOperationPayload.provider` at `:1006`, `memory_refs=(memory_id,)` at `:1010`) and the C-MEM-19 span attributes (`:686-688`, `:714-716`) -- and promotion reads neither (`_hints_from_record` reads `record.content` alone, `:545-560`). So readings B and C are not merely unbuilt for composed-scope records (every record the production path writes), they are UNREPRESENTABLE, and surfacing the discriminator is itself contract-altitude work, X-AL-3-blocked at Phase 7. A second grounded correction: `risk_flags` GATES NOTHING today -- a repo-wide grep finds it only at its definition, its two construction sites and its persistence; `_review_required` (`:762-773`) never consults it and `_auto_promote_allowed` reads `review_required`, not flags -- so any flag-based reading must state the GATE as well as the flag or it lands inert. NET, CORRECTED: the permissiveness is STRUCTURAL, not yet exercised -- live auto-promotion exposure at HEAD is NIL on the composed-scope production path (the tool path is closed twice over, by record kind and by policy decision; the hint path is unfed and its candidate convention undefined). What is real NOW is the contract gap -- which NO SINGLE NAMED CHANGE CONVERTS INTO A LIVE EXPOSURE (Codex R6 [P2-2]; the combinations are (a) AND (b) on the tool path, with (c) `ReviewMode.AUTOMATIC` already satisfied in production, or the hint producer AND (b) AND an actuator that does not exist). What a single change DOES buy is the TRANSITION BELOW IT -- (a) makes captured records candidate-reachable and durably PROPOSED; (b) makes the second-order case actively auto-promoting -- and each such transition happens WITH NO FURTHER REVIEW, because no gate on any of these paths has a family term to consult. That is exactly why this is a Class 1 SPEC question and not a Phase 7 bug, and why the URGENCY is low while the NECESSITY is not: nothing is leaking today, the last step to leaking is a two-field policy change nothing forbids, and the contract has no position to fall back on when it happens.'
  close_out: 'CLOSED 2026-07-29 at the U-MEM-27 impl leg (PR #1159). All FIVE parts of the prior LIVE NEXT STEP are discharged; the deliberation record it carried is preserved verbatim in this row''s prose block and at the fork filing. (1) WRITE SIDE, ENVELOPE -- `MemoryRecordEnvelope.captured_cross_family` landed as a tri-state typed to the new `CapturedCrossFamily` value domain (`harness-is/src/harness_is/memory_record_envelope.py`), defaulting to `unknown` so a pre-amendment payload validates and reads undetermined; hash-inert by construction (`content_hash` / `memory_id` are computed from `content` before the envelope exists) and witnessed as such after the edit. (2) WRITE SIDE, DERIVATION -- ORIGIN-AWARE and split exactly as the spec leg fixed it: `_capture` determines the content-origin disposition from `event_kind` (the one method holding it) and passes THAT into `_record`, which computes the final tri-state; origin GATES the family comparison, so non-dispatch-derived content lands `unknown` and the canonicalized-provider-vs-composed-scope comparison is never consulted. `turn_completion` + `tool_event` are DISPATCH_DERIVED unless the invocation''s capture mode is REDACTED (codex R3: wholesale harness-authored replacement material lands `unknown`); `run_start` / `run_close` / `provider_route` / `failure_observation` / `compaction` are UNDETERMINED; an unmapped `event_kind` resolves to UNDETERMINED, so the table fails closed for a writer added later. No `capture_*` signature changed. (3) READ SIDE -- the `cross_family_capture` flag vocabulary member plus the gate at BOTH entry points (`_candidate_from_hint`; the tool executor''s `_promotion_risk_flags` / `_propose_promotion`), biconditional with the gate and unconditional on policy (holds under `PROMOTE_SEMANTIC` and `PROMOTE_PROCEDURAL` with `ReviewMode.AUTOMATIC`); the illegal pair refused at every validating constructor, by direct construction and by deserialization; the reserved flag re-derived in both directions so a hint can neither introduce nor suppress it; the durable content carrier on promotion-written records (the C-MEM-08 ledger shape deliberately unchanged); and the redaction RESET, so a content-replacing transition returns a prior determination to `unknown`. (4) ACTIVATION BOUNDARY -- ONE frozen `_EffectiveProvenance` snapshot per service call feeding both the gate (a pure projection performing no lookup of its own) and every durable write; worst-value aggregation across multiple cited sources; per-branch fail-closed handling (empty refs, unparseable `memory_id`, no usable `run_id`, unreadable record) that withholds AUTOMATIC activation only and leaves the operator-approved path open -- reading B, not reading C; choke-point normalization at `_persist_decision` covering all FOUR callers (`propose_for_review` / `approve` / `deny` / `edit_and_approve`); and COMMIT BINDING via a guarded conditional write executed inside the store''s own write locks (`CanonicalMemoryStore.write_record_guarded`, both locks re-entrant in a documented `_FILE_WRITE_LOCK` -> `_JSONL_WRITE_LOCK` order), raising rather than committing an activation against a source whose recorded provenance changed after the snapshot -- compared as the EXACT per-source tri-state vector (codex R2 correction; a source that becomes unresolvable is kept distinct from a stored `unknown`), never a decision-bearing projection. The cross-process half was BUILT at codex R1 rather than scoped away: the house flock primitive is generalized to a per-root `cross_process_scope_lock` taken by BOTH `write_record` and `write_record_guarded`, witnessed by a genuine two-OS-process interleaving test. (5) CLOSEOUT -- the U-MEM-25 evidence packet''s window 2 is CLOSED the way the U-MEM-26 leg closed window 1: all six `PENDING - U-MEM-27` markers lifted (C-MEM-03, C-MEM-10, R-MEM-01, R-MEM-05, R-MEM-09, R-MEM-14), scope advanced to spec v1.2 / plan v1.2 / U-MEM-01..27, and `just memory-closeout-check` re-run green with the refreshed rows in place. 91 witnesses across the four dedicated U-MEM-27 test modules plus the envelope/store witnesses folded into existing `harness-is` files (+98 suite-wide at `just check`, 7026 -> 7124; out-of-family codex converged clean at round 4 after three fix rounds recorded in the arc note). Arc record at `.harness/u-mem-27-impl-leg-close-2026-07-29.md`. The two BOUNDS the spec leg stated remain bounds, each needing its own later amendment: the field answers PRESENT/ABSENT only (not by-family), and aggregate-run provenance is not representable per-record.'
  council: 'Not required -- the deciding council already sat. The B-86 convening (C10 action-safety/blast-radius + C3 state/memory/persistence co-primary, C6 model-routing consultant, 2026-07-28) produced the continuity argument that forecloses reading C and the write-bounded-irreversible argument that forecloses reading A''s silence; this filing''s §7 reads that record rather than re-convening, per the nameable-tension discriminator (the C10-vs-C3 tension here is the SAME one already surfaced and probe-resolved at B-86, not a new one). What remains is an operator ratification, not a deliberation. Re-convene only if the operator selects a reading against the recommendation and wants the tension re-argued at C-MEM-10 altitude.'
  heading: '### B-92 · C-MEM-10 promotion eligibility of records captured during a cross-family fallback leg *(named open question carried forward at Spec_Memory_Substrate_v1.md v1.1 change-note "Named open question carried forward, not discharged." and U-MEM-26''s "Out of scope for this unit:" list; Class 1 fork FILED 2026-07-29; CLOSED 2026-07-29 — U-MEM-27 impl leg)*'
- id: B-93
  title: 'Whole-root memory-write serialization has no deadline -- an unbounded guarded precondition stalls every memory writer on the host'
  status: closed
  pr: '#1224 (the combined B-93+B-45 Class 2 fork filing) + #1233 (the 2026-08-05 ratification record) + #1241 (the build leg — CLOSES this row)'
  summary: 'Surfaced by the merge-gate concurrency lens on PR #1159 (the U-MEM-27 impl leg, 2026-07-29); classified REGISTER at the post-#1159 gate-log grounding pass (2026-07-30). MECHANISM: every `CanonicalMemoryStore` write takes `cross_process_scope_lock` over the whole memory ROOT (`_write_scope`, `harness-is/src/harness_is/memory_store.py`), and that lock is a plain BLOCKING `flock(LOCK_EX)` with no timeout (`_DirLock.acquire`, `harness-is/src/harness_is/cross_process_ledger_lock.py`). `write_record_guarded` additionally holds it across a CALLER-SUPPLIED precondition callable, so the hold time of a root-wide exclusive lock is set by code the store does not control and cannot bound. NO LIVE HAZARD AT HEAD: the only precondition in the tree is `_provenance_unchanged` (`harness-runtime/src/harness_runtime/memory_promotion.py`), bounded at one `read_record` per cited source memory ref -- a small N of local file reads, no network, no operator wait, no unbounded loop. The exposure is a FUTURE precondition (an operator confirmation, a provider round-trip, a retrieval sweep) whose author gets no signal from the seam that they are holding a host-wide lock: every same-host memory writer -- a `harness run` alongside a `harness daemon`, the shared-root case the seam''s own SCOPE paragraph documents -- then blocks for its duration, with no deadline and no diagnostic.'
  close_out: 'RATIFIED 2026-08-05 as READING B -- decision 2 (liveness deadline) = YES, decision
    1 (Windows serialization backend) = NO, decision 3 (vehicle) = HAND-ROLLED EXTENSION of the
    existing primitive, NOT `filelock`. Operator AskUserQuestion on the fork''s section 8 (`.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md`).
    WHAT THE BUILD LEG OWES: a deadline at ALL NINE acquisition sites; ONE typed `CrossProcessLockTimeoutError`
    homed at HARNESS-CORE (settled by precedent, not open -- `ValidatorEscalationGateTimeoutError`
    at `harness-core/src/harness_core/validator_escalation_errors.py:43` was re-homed for exactly
    this carrier-home reason; defining it in harness-runtime is ILLEGAL, inverting the IS 0-outbound
    invariant); the ABBA release-before-blocking handoff PRESERVED at all three probe arms; the
    hold-time contract documented at `write_record_guarded`; the section 3(ii) RECONCILER IMPORT
    REPAIR; and >=1 process-based deadline witness per site-class -- INCLUDING the `_workflow_lock`
    carrier, which codex R9 established is UNWITNESSED at HEAD (the reconciler CAS test proves
    `os.link`, not `_workflow_lock`) -- each with a PD-8 mutation probe (revert the bound, confirm
    the witness fails, restore). VEHICLE RATIONALE, as ratified: SEVEN of the nine sites are exclusive-only
    and need no shared face, so the hand-rolled `LOCK_NB`+retry extension carries decision 2 alone;
    `filelock` is revisited only if decision 1 later goes YES. ZERO spec and plan delta under
    every reading -- verified, not assumed -- so there is NO spec leg and NO X-AL-3 exposure.
    NO COUNCIL is owed (the C3 x C11 dyadic was conditional on decision 1 = YES). ORDERING: if
    decision 1 ever flips to YES, the deadline leg MUST PRECEDE the Windows leg. STATUS -> `open`
    (fork section 9: status flips for a row whose half is a YES; `open` is the enum''s actionable-now-no-blocker
    value, and this half has no design-substrate gate). The row flips to `closed` when the deadline
    build leg merges. POST-EDIT ANCHOR NOTE (merge-gate lens 2; the B-104 precedent of recording drifted anchors durably on the row rather than only in a PR body). THREE cites in the combined fork filing `.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md` drifted BEFORE this build leg began, at PR #1237''s U-RT-150 landing. The anchors recorded here are the PRE-LEG MAIN BASELINE (`eea1dec9`, the commit this branch forked from) -- i.e. what the filing''s cites SHOULD have said when it was written -- NOT this branch''s head, where the same three sites have moved again under the leg''s own edits (`:59` / `:567` / `:617`). Naming the frame explicitly because a bare line number without one is exactly the drift being corrected: `protected_result_store.py` acquisition site `:371` -> `:592`, its `_cross_process_lock` entry `:340` -> `:561`, and its `_IS_WINDOWS` flag `:48` -> `:53`. The SITE ITSELF is exactly as the filing describes -- only the line anchors moved -- and every other cite in the filing resolved as written. THE FILING WAS DELIBERATELY NOT EDITED in this PR: a `design-substrate`/fork-filing edit riding a code PR is the DESIGN_IMPL_MIX condition the codex context guard refuses, so the durable correction lives here on the row instead. SWEEP-SCOPE CORRECTION (merge-gate lens 3, G2). The arc''s AST sweep of timeout-leak boundaries enumerated DIRECT callers of the seven lock entry surfaces. That is NOT the full exposure set: `admin/inspect.py` and `admin/migrate_audit_sidecar.py` reach locks TRANSITIVELY and both needed folds in this same PR, so neither appeared in the direct-call enumeration. The honest claim is therefore: the DIRECT-CALL set is closed by enumeration, and the two known transitive operator-facing surfaces were fixed separately and witnessed. The FULL transitive set is NOT enumerated -- doing so is a call-graph analysis in its own right -- and is recorded as residual on `B-112`. Two variants stay RECORDED AS DOMINATED so they are not re-proposed: variant
    F (a deadline on `cross_process_scope_lock` alone) and variant E (adopting `filelock` without
    deciding either half). (1) Pick the deadline shape: `LOCK_NB` + bounded retry with backoff,
    or a watchdog around the blocking call. `LOCK_NB`+retry is the portable one and composes with
    the B-45 Windows-backend work. (2) Choose the timeout''s semantics and its exception -- a
    NEW typed error distinct from `MemoryStoreGuardedWriteConflictError`, because a refused commit
    is a precondition outcome and a timeout is a liveness one; collapsing them would let a caller
    retry-loop against a stuck host. (3) Apply it CONSISTENTLY, and to the right unit of coverage.
    Counted by direct read at HEAD (RECOUNTED 2026-07-31 at the B-103 arc: NINE, not eight, across
    FOUR modules, not three -- `journal_workflow_pause_store.cross_process_journal_lock` was created
    at the B-97(a) leg, PR #1171, AFTER this row''s original count), there are NINE blocking-`flock`
    ACQUISITION SITES across FOUR modules, reachable through SEVEN entry surfaces -- and the acquisition
    site, not the entry surface, is where a deadline goes. In `harness-is/src/harness_is/cross_process_ledger_lock.py`
    (six): `_DirLock.acquire` `:133` `LOCK_EX`, the shared engine behind ALL FOUR of that module''s
    entry surfaces (`_dir_lock_for` callers at `:213` scope / `:365` write / `:484` replace /
    `:572` read); `cross_process_write_lock` `:406` `LOCK_EX`; `cross_process_replace_lock` `:507`
    `LOCK_EX`; `cross_process_read_lock` `:605` `LOCK_SH`; `_acquire_legacy_sidecar_for_writer`
    `:274` `LOCK_EX`; and `_acquire_legacy_sidecar_if_present` `:330` `LOCK_EX`/`LOCK_SH`. In
    `harness-runtime` (three): `reconciler_pause_resume_substrate._workflow_lock` `:294` `LOCK_EX`,
    `protected_result_store._cross_process_lock` `:371` **[SUPERSEDED — see the post-edit anchor note on this row: `:371` was the pre-#1237 value]** `LOCK_EX` (*cite corrected from `:347`
    at the 2026-08-05 re-check*), and -- NEW at the 2026-07-31 recount -- `journal_workflow_pause_store.cross_process_journal_lock`
    `:489` `LOCK_EX` (the per-workflow pause-journal append lock promoted to module scope at B-97(a);
    it is a LEAF, nothing inside its hold takes another cross-process lock, so it adds a site
    but no ordering hazard). INTENDED COVERAGE: all nine. The B-45 co-rider text now AGREES with
    this count -- B-45''s own 2026-07-31 refresh names the same module as its FOURTH Windows no-op
    surface, so both rows enumerate the same four-module primitive set. TWO traps a naive impl
    falls into. First, ONE entry surface can block at up to THREE distinct sites in sequence (dir
    lock -> legacy sidecar -> file lock), so a deadline bolted onto `cross_process_write_lock`''s
    own `flock` leaves the other two unbounded and the entry surface still has no end-to-end bound.
    Second, the three `LOCK_NB` calls (`:402`, `:498`, `:599`) are contention PROBES, not deadlines
    -- on contention each arm RELEASES the directory lock first, then blocks at `:406`/`:507`/`:605`
    (release-before-blocking, the ABBA-avoidance handoff a deadline impl must preserve) -- and
    they must not be counted as coverage. Note also that one site is `LOCK_SH`, so "they are all
    `LOCK_EX`" is false. That is the same primitive set B-45 must give a Windows backend, which
    is why this is B-45''s natural CO-RIDER -- both arcs traverse it, and doing them together
    costs one traversal instead of two. (4) Document the hold-time contract at `write_record_guarded`
    so a precondition author meets the constraint at the seam rather than in production. Not urgent
    while `_provenance_unchanged` is the only precondition -- but the seam is GENERIC by design,
    so the SECOND precondition is the trigger, and this row should be re-checked at every new
    caller. 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT and ALL NINE acquisition
    sites re-verified INDIVIDUALLY, with exactly ONE drifted cite corrected above (protected_result_store,
    `:347` -> `:371` -- **and `:371` has SINCE been superseded by `:592` at PR #1237; see the post-edit anchor note on this row**). The other eight resolve exactly: cross_process_ledger_lock.py `:133` /
    `:274` / `:330` / `:406` / `:507` / `:605`, reconciler_pause_resume_substrate.py `:294`, journal_workflow_pause_store.py
    `:489`. The `_dir_lock_for` caller cites `:213` / `:365` / `:484` / `:572` all resolve exactly,
    and the three `LOCK_NB` calls at `:402` / `:498` / `:599` are still contention probes, not
    deadlines; on contention each arm releases the directory lock then blocks at `:406`/`:507`/`:605`
    (release-before-blocking preserved) -- trap 2 holds as written. NOT ONE site takes a timeout.
    TRIGGER HAS NOT FIRED: `write_record_guarded` (memory_store.py:298, precondition param `:302`)
    still has exactly ONE PRODUCTION caller in the tree, `_provenance_unchanged` at memory_promotion.py:907
    (*cite corrected from `:911` at the 2026-08-05 fork filing -- `:911` is the write_record_guarded
    CALL, `:907` the def; the enclosing method is `_commit_record` at `:840`*), bounded at one
    `read_record` per cited source (direct TEST callers at test_memory_store.py:523/:539/:564/:618
    + test_memory_store_cross_process_guard.py:168 exercise the seam without converting the trigger
    -- and, *added 2026-08-05, that list was NOT exhaustive*: test_u_mem_27_activation_boundary.py
    also carries four protocol-double definitions at `:273`/`:342`/`:391`/`:629` and three delegating
    calls at `:348`/`:397`/`:638`, none of which supplies a new precondition, so the trigger verdict
    is unchanged). Disposition VERIFIED-DORMANT, with step (2) (the new typed timeout error, distinct
    from MemoryStoreGuardedWriteConflictError) the one genuine FORK-SHAPED decision -- everything
    else is fully enumerated. FILED 2026-08-05 -- combined Class 2 fork with B-45 at .harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md
    (grounding HEAD 966bc658). RATIFIED 2026-08-05 -- the ''awaiting operator ratification; STATUS
    UNCHANGED (registered_finding)'' clause this sentence used to carry is SUPERSEDED: this row
    is now `open`. EVERYTHING FROM HERE ON IS THE PRE-RATIFICATION RECORD, SUPERSEDED 2026-08-05
    BY THE RATIFICATION PARAGRAPH ABOVE and kept only as history -- read it as ''what this row
    said before the operator answered'', never as current state.  Four readings -- A full combined
    fix / B deadline-now plus the minimal Windows import repair, serialization deferred (RECOMMENDED)
    / C Windows-only (dominated) / D defer both under demand tests D-0 (a second write_record_guarded
    precondition, this row own trigger unchanged), D-1 (Windows CI exists), D-2 (a declared Windows
    target), D-3 (an observed stall), D-4 (a third row against this substrate); runner-up D, and
    the B-versus-D margin is genuinely narrow. TWO VARIANTS RECORDED AS DOMINATED so they are
    not re-proposed -- variant F, a deadline on cross_process_scope_lock alone, which trap 1 defeats
    (one entry surface blocks at up to three sites in sequence, verified by direct read of cross_process_write_lock
    at `:363`-`:406`), and variant E, adopting filelock without deciding either half. STEP (2)
    EXCEPTION HOME IS SETTLED BY PRECEDENT, NOT OPEN -- harness-core (zero workspace dependencies
    per harness-core/pyproject.toml:9-11; precedent ValidatorEscalationGateTimeoutError at harness-core/src/harness_core/validator_escalation_errors.py:43,
    re-homed for exactly this carrier-home reason at `:1`-`:13`), fallback harness-is (legal because
    harness-runtime already imports harness_is 124 times while harness-is imports harness_runtime
    zero times); defining it in harness-runtime is ILLEGAL, inverting the IS 0-outbound invariant.
    ZERO spec and plan delta under EVERY reading, verified not assumed -- C-IS-09 §9.4 (Spec_Information_Substrate_v1.md:900)
    and C-IS-07 §7.4 (`:756`) both defer the mechanism, and the IS spec contains ZERO occurrences
    of timeout, deadline, platform or cross-process. CLOSED #1241 (the build leg, 2026-08-06) -- READING B BUILT AS RATIFIED, hand-rolled, no dependency added. All NINE acquisition sites bounded by a non-blocking probe plus bounded retry against ONE typed `CrossProcessLockTimeoutError` homed at harness-core (`harness_core.cross_process_lock_deadline`), which also hosts the mechanism (`CrossProcessLockDeadline` + `flock_until_deadline`); harness-core has zero workspace dependencies so no cycle is possible, and `fcntl` is imported lazily INSIDE the helper so `import harness_core` stays Windows-safe. DESIGN CHOICE BEYOND THE RATIFICATION, flagged as overturnable: the deadline is END-TO-END PER ENTRY SURFACE, not per acquisition -- one entry surface blocks at up to THREE sites in sequence AND retries the whole acquisition on an inode-replacement race, so a per-acquisition bound would be the partial-guarantee-that-reads-as-total the fork''s variant F was dominated on; each of the seven entry surfaces mints ONE absolute deadline and threads it down, witnessed by elapsed time across two sequential waits. DEFAULT BUDGET 300s, likewise chosen not inherited (the fork ratified THAT a deadline exists, not WHAT it is): conservative because the registered harm is entirely prospective while a short bound would convert working systems into failing ones; every entry surface takes a per-call `deadline_seconds` override. ABBA release-before-blocking PRESERVED at all three probe arms and separately witnessed (a sibling ledger in the same directory stays writable while a caller waits on the file lock). Hold-time contract documented at `write_record_guarded`, naming the bounded-and-local obligation and stating that the bound protects the VICTIM, not the offender. Section 3(ii) RECONCILER IMPORT REPAIR landed -- see B-45, whose no-op-surface enumeration this changes. FIFTY-EIGHT new witnesses (22 harness-core unit + 17 harness-is per-site + 19 across the harness-runtime suites), every per-site one driven by a genuine second OS process and each pinning WHICH lock timed out via `lock_target`, so a single bounded early acquisition cannot make the other eight pass. THE UNWITNESSED `_workflow_lock` CARRIER (codex R9 on the filing) IS NOW WITNESSED BY EXECUTION: a second OS process runs the REAL `_workflow_lock` and this process provably cannot enter until it leaves -- deliberately NOT a deadline test, so exclusion and liveness stay separate claims. FORTY-THREE PD-8 mutation probes, ALL RED: reverting each of the nine sites to a bare blocking flock HANGS its witness (the pre-B-93 harm reproduced); restoring the module-scope `import fcntl` fails the import-repair witness; deleting `_workflow_lock`''s flock fails the exclusion witness; re-minting the deadline at the second site fails the end-to-end witness on elapsed time; giving up after the first probe fails the acquire-on-release witness. ONE BEHAVIOURAL CONSEQUENCE THE FILING DID NOT ANTICIPATE, recorded rather than absorbed: polling does NOT preserve the kernel''s HANDOFF FAIRNESS. A blocking flock parks the waiter in the kernel, which grants the lock on release before the ex-holder can realistically re-acquire; a poller is asleep at that instant. This changes ORDERING, never EXCLUSION, and is intrinsic to the ratified shape rather than to this implementation (`filelock` polls at 10x this interval; `msvcrt.LK_LOCK` retries at 1s). It surfaced as a real regression in ONE shipped witness -- `test_rollback_cross_process_lock_prevents_lost_concurrent_append`, whose fixed child timestamp silently PINNED the old ordering under a ZERO clock-skew tolerance. Repaired ORDER-INDEPENDENTLY, after review round 2 showed an order-PINNED formulation is flaky in BOTH directions (`flock` specifies NO acquisition ordering, so an early stamp needs the child to win and a late one needs the parent to): the child stamps the pre-existing entry''s own timestamp and, if that is refused because the parent landed first, retries with C-IS-07 section 7.6''s WRITER_OWNED_TIMESTAMP, which samples INSIDE the write critical section. The position assertion an interim round added was REMOVED — it asserted a property `flock` does not provide. The load-bearing survival assertion is unchanged, its mutation probe still discriminates, and the witness is 20/20 clean on repetition. A second shipped witness (`test_ac9d_never_holds_two_journal_locks_at_once`) keyed its flock spy on `op == LOCK_EX` and silently reported a peak of ZERO once the probe became `LOCK_EX | LOCK_NB` -- a pass-on-absence, fixed by matching the bit and by counting only SUCCESSFUL acquisitions. ZERO design-substrate delta (the fork established this, and this leg made none); ZERO contract numbers; ZERO CXA rows; Windows behaviour otherwise UNCHANGED and `journal_exclusion_is_degraded()` untouched -- B-45 stays deferred on witnessability.'
  council: 'No -- an implementation-level liveness bound on an already-cleared lock mechanism (C-IS-09 §9.4 defers the mechanism to implementation discretion), with no cross-domain tension to name. It does carry a real design choice at step (2) (fail-fast vs retry, and what a timed-out guarded write means to its caller), so it is not mechanical: advisor() + out-of-family review at build time is the right weight. Council-eligible only if it is taken WITH B-45 and the Windows-backend tradeoff makes the deadline semantics non-obvious. CONDITION MET AND ADJUDICATED 2026-08-05 at the B-93+B-45 combined fork filing -- the antecedent holds (the two rows ARE taken together) and the consequent is FALSE, PROBE-RESOLVED, so NO dyad convenes. The probe (Python msvcrt docs plus filelock 3.29.4, both accessed 2026-08-05) found the two decisions ORTHOGONAL: msvcrt.LK_NBLCK gives Windows the same non-blocking-probe shape close-out step (1) prescribes, and msvcrt.LK_LOCK already carries a fixed ~10s bound, so the backend choice cannot make the deadline semantics less expressible -- the backend decides EXCLUSION GRANULARITY, the deadline decides LIVENESS, and neither constrains the other. One residual coupling named rather than hidden -- under a filelock vehicle the library own Timeout subordinates step (2) exception design, but that is an implementation-vehicle coupling inside one arc, not a cross-domain tension. The ONE convening that IS owed -- a Reading-A selection (build the Windows backend BEFORE a Windows CI witness exists) owes a dyadic C3 vs C11 convening, per B-45 council field.'
  heading: '### B-93 · Whole-root memory-write serialization has no deadline — an unbounded guarded precondition stalls every memory writer on the host *(surfaced by the merge-gate concurrency lens on the U-MEM-27 impl leg, PR #1159, 2026-07-29; classified REGISTER at the post-#1159 gate-log grounding pass, 2026-07-30)* *(**RATIFIED 2026-08-05 as READING B** — deadline YES, Windows NO, vehicle HAND-ROLLED; **CLOSED 2026-08-06 — #1241**, the build leg)*'
- id: B-94
  title: 'Derived-index auto-refresh rebuild overlap race -- two concurrent rebuilds can leave the OLDER snapshot last and reading fresh'
  status: registered_finding
  summary: 'Surfaced by out-of-family Codex R5 at the post-#1159 gate-log PR (2026-07-30). MECHANISM: `DerivedRetrievalIndexStore.rebuild` computes its entry set from the filesystem and then appends the result, holding NO lock across the two (`harness-is/src/harness_is/memory_retrieval_index.py`; `_append_jsonl` is a bare `open("ab")` + write). Two overlapping rebuilds can therefore interleave as: X reads the record set; a commit lands; Y reads the NEWER set and appends it; X appends its OLDER set LAST. `read_current` resolves an untokenized ledger by POSITION (last rebuild wins), so it returns X''s stale snapshot with `stale=False` - a committed record missing from an index reported as current. PRE-EXISTING, verified by `git show fc40fb2b` and NOT widened by the B-93 arc: pre-branch `rebuild` at `:177-200` computed entries at `:180` and appended at `:199` with no lock; `_append_jsonl` at `:571-574` took none either; `read_current` at `:210-227` was purely positional last-wins; and `AutoRefreshingDerivedRetrievalIndexStore.read_current` (`harness-runtime/src/harness_runtime/automatic_memory.py:110-114`) rebuilt on missing/stale with no coordination - the same interleaving yields the same wrong outcome on the pre-branch code. SCOPE OF WHAT THE B-93 ARC DID AND DID NOT COVER: that arc added a commit-ordering protocol (`commit_seq` stamped inside the memory store''''s write hold, and a `rebuilt_coverage` event binding a rebuild to the commit it answers) which orders TOKENIZED rebuilds - the hook path, where the caller has an invalidation event to quote. The PRODUCTION auto-refresh path is untokenized BY DESIGN: it rebuilds because a read found the index missing or stale, not in response to a specific write, so it has no token to pass and correctly falls to the legacy positional rule. The protocol therefore does not reach this race, and an all-untokenized ledger behaves exactly as it did pre-branch.'
  close_out: 'CANDIDATE FIX SHAPE (Codex R5''''s direction, recorded not built): derive a coverage
    token on the auto-refresh path too, from the ledger length captured at the READ that decided
    to rebuild. That read already walks the whole ledger, so its byte length at that moment is
    free and is exactly the same monotonic quantity `_next_commit_seq` uses - it names "the state
    I found stale", which is the honest under-claiming attribution the rebuild contract already
    requires. `AutoRefreshingDerivedRetrievalIndexStore.read_current` would capture it in the
    `except` branch and pass it to `rebuild(commit_seq=...)`; the existing ordering rules then
    reject the older overlapping snapshot with no further change to `read_current`. OPEN QUESTIONS
    to settle at build time: (1) the capture must happen INSIDE the failed read rather than after
    it, or it re-races - `read_current` currently returns nothing on the raise, so the token needs
    a carrier on the exception or a private read variant; (2) whether the fix belongs at the `harness-runtime`
    subclass (where the auto-refresh policy lives) or at the IS-axis base (where the token semantics
    live) - the IS axis has zero outbound cross-axis edges, so a base-class change must not import
    runtime concerns; (3) whether a losing rebuild should be pruned rather than merely ignored,
    since the ledger still grows by one full snapshot per overlap. NOT URGENT: the outcome is
    a stale-but-valid index (a retrieval base that under-serves, never a wrong record), the auto-refresh
    path self-corrects at the next canonical write''''s stale marker, and single-process local
    development - the default posture - does not overlap rebuilds at all. Re-check when a second
    concurrent memory consumer becomes routine (daemon + one-shot both retrieving), which is the
    same trigger B-93 carries. 2026-08-05 grounding re-check at HEAD 43d4dda5 — premise INTACT
    and now DOCUMENTED IN CODE: the memory_retrieval_index module docstring names BOTH B-95 and
    B-94 explicitly at `:19-23` (rebuilds that pass no `commit_seq` -- which includes the production
    auto-refresh path, untokenized by design -- remain positional last-wins), and the production
    auto-refresh path is confirmed untokenized by direct read: `AutoRefreshingDerivedRetrievalIndexStore.read_current`
    calls `self.rebuild(indexed_at=datetime.now(UTC))` with NO `commit_seq` at harness-runtime/src/harness_runtime/automatic_memory.py:110-114.
    `_append_jsonl` still takes no lock and `read_current` still resolves untokenized ledgers
    positionally. Disposition FORK-SHAPED (low urgency) -- the three open questions stand: where
    the token is captured, whether the fix lands at the harness-runtime subclass or the IS-axis
    base (a real axis-placement call under the IS zero-outbound-edge invariant), and prune-vs-ignore
    for losing rebuilds. CROSS-REF ADDED 2026-08-05: B-95 (the mixed-version ordering bound over
    this same untokenized-rebuild surface) was CLOSED at the 2026-08-05 ratification batch as
    a ratified accepted bound, and this row is one of its named trigger-monitoring carriers --
    if work here surfaces a planned mixed-version deployment or the first production `derived_index_hook`
    consumer, B-95''s preserved re-open trigger fires and that row REOPENS.'
  council: 'No -- a bounded concurrency fix inside an already-cleared derived-index mechanism, with the ordering protocol it would extend already built and witnessed at the B-93 arc. There is no cross-domain tension to surface: the question is where the token is captured, not whether the index should be ordered. It does carry a real placement decision (open question 2, the IS-vs-runtime boundary under the IS axis'''' zero-outbound invariant), so advisor() + out-of-family review at build time is the right weight.'
  heading: '### B-94 · Derived-index auto-refresh rebuild overlap race — two concurrent rebuilds can leave the OLDER snapshot last and reading fresh *(surfaced by out-of-family Codex R5 on the B-93 gate-log arc, 2026-07-30; classified PRE-EXISTING by git-show verification against fc40fb2b)*'
- id: B-95
  title: 'Mixed-version ordering bound -- a live PRE-B-93 WRITER''''s untokenized stale markers cannot be ordered against a covered rebuild'
  status: closed
  pr: '#1233 (the 2026-08-05 ratification record)'
  summary: 'Surfaced by out-of-family Codex R6 at the post-#1159 gate-log PR (2026-07-30), reproduced deterministically. MECHANISM: during a mixed-version window a NEW hook captures snapshot A; a PRE-B-93 writer commits B and appends an UNTOKENIZED stale marker; the hook then appends A''''s rebuild pair last. In `read_current` (`harness-is/src/harness_is/memory_retrieval_index.py`) the untokenized marker has no `commit_seq`, so it is excluded from `max_stale_seq`, and accepting the covered rebuild resets the positional `stale_after_current` flag -- the A-only index then reads FRESH with B missing. PRECONDITION TRIPLE, each verified by direct read at HEAD and each FALSE today: (a) a live PRE-B-93 *writer* in the window -- not merely an old reader, whose parse compatibility the R3 fix already covers -- because at HEAD `commit_seq` is a REQUIRED field on `DerivedIndexInvalidation`, so every B-93+ writer stamps every marker and an untokenized marker can only come from an older writer process; (b) a WIRED `derived_index_hook` -- re-confirmed ZERO production consumers at HEAD, the only wirings being `harness-is/tests/test_memory_store.py` and `harness-is/tests/test_memory_redaction.py`; (c) an overlapping write + hook-rebuild interleaving. NOTE the sharpening: historical untokenized lines written BEFORE an upgrade are benign, because a later rebuild reads the whole filesystem and genuinely covers them; only a CONCURRENT untokenized write interleaved with an in-flight rebuild is harmful.'
  close_out: 'CLOSED 2026-08-05 by operator ratification as a RATIFIED ACCEPTED BOUND -- not built,
    not foreclosed by supersession, but ACCEPTED with its limit documented in shipped source.
    This resolves the row''s own FLAGGED-NOT-FLIPPED close candidacy: disposition (3) (accept
    and document) is FULLY EXECUTED at HEAD -- the bound is stated in the `memory_retrieval_index`
    module docstring at `:19`-`:20` and on `commit_seq` itself at `memory_store.py:154`-`:163`,
    both citing this row -- and all three preconditions were re-verified COUNTERFACTUAL by direct
    read at the 2026-08-05 grounding pass. THE RE-OPEN TRIGGER IS PRESERVED VERBATIM AND SURVIVES
    THE CLOSE: "Re-open when a genuine mixed-version deployment is planned, or when the first
    production `derived_index_hook` consumer is wired -- whichever comes first, since either alone
    makes a precondition true." Closure does NOT retire that trigger; it records that no work
    is owed while both preconditions stay counterfactual. The two rejected dispositions stay rejected
    on their recorded grounds: (1) serializing notifications during a detected mixed window reintroduces
    the callback-deadlock surface the arc''s first commit removed; (2) version-fencing the ledger
    costs a new failure mode plus an undecided what-should-a-fenced-writer-DO question. The HOLD
    RATIONALE also stands: this was the THIRD consecutive self-inflicted `read_current` finding,
    and the arms-race stop rule the arc pre-declared fired exactly as written -- no further carrier
    change is made. INFORMATION-THEORETIC LIMIT, stated precisely: a legacy stale marker carries
    no commit token BY DEFINITION, so no read-side rule can retroactively order it against a covered
    rebuild -- the information required for the comparison was never written. This is not a missing
    check; it is absent data. The choices are therefore only three. (1) SERIALIZE NOTIFICATIONS
    during a detected mixed window -- reintroduces exactly the callback-deadlock surface the arc''''s
    first commit removed (a hook holding the notify turn while its inner thread''''s write waits
    behind it), so it trades a latent ordering gap for a latent hang; rejected on that ground
    unless the window can be detected AND the serialization scoped to it. (2) VERSION-FENCE THE
    LEDGER -- have B-93+ writers refuse to share a ledger they can detect an older writer is also
    appending to (a writer-version marker line, or a fence file beside the ledger), converting
    a silent misorder into a loud refusal during the upgrade window; costs a new failure mode
    operators must understand and a decision about what a fenced writer should DO. (3) ACCEPT
    THE BOUND and document it -- taken here, since all three preconditions are counterfactual
    at HEAD. HOLD RATIONALE: this is the THIRD consecutive self-inflicted `read_current` finding
    (R4 was caused by R3''''s carrier, R5''''s [P2] by R3/R4''''s two-pass read, and this by R2''''s
    ordering protocol), which is the arms-race stop rule the arc pre-declared at round 4 firing
    exactly as written. No further carrier change is made. Re-open when a genuine mixed-version
    deployment is planned, or when the first production `derived_index_hook` consumer is wired
    -- whichever comes first, since either alone makes a precondition true. 2026-08-05 grounding
    re-check at HEAD 43d4dda5 — premise INTACT and all three preconditions re-verified COUNTERFACTUAL
    by direct read: (a) `commit_seq: int` is a REQUIRED field with no default on `DerivedIndexInvalidation`
    (harness-is/src/harness_is/memory_store.py:138), so every B-93+ writer stamps every marker;
    (b) ZERO production `derived_index_hook` consumers -- the only wirings repo-wide are harness-is/tests/test_memory_store.py:611
    and harness-is/tests/test_memory_redaction.py:68, the production surface being only the ctor
    param (memory_store.py:215) and the notify site (`:284-285`); (c) a concurrent overlap is
    still required and absent. Disposition VERIFIED-DORMANT. 2026-08-05 RECLASSIFICATION NOTE:
    disposition (3) -- accept and document -- is FULLY EXECUTED at HEAD (the bound is stated in
    the memory_retrieval_index module docstring at `:19-20` and on `commit_seq` itself at memory_store.py:154-163,
    both citing this row), so this row is not open work but a ratified accepted bound with a documented
    re-open trigger; it was flagged a CLOSE CANDIDATE at the next operator ratification batch
    -- FLAGGED, NOT FLIPPED at that pass (the accepted bound plus its re-open trigger made closure
    an operator call, so the status was deliberately left unchanged there). THAT CANDIDACY IS
    NOW ANSWERED: the operator flipped it at the 2026-08-05 batch and the row is `closed` (see
    the closure paragraph at the head of this close_out). The ''NOT FLIPPED'' clause is the historical
    record of the pass that deferred the call, not current state. TRIGGER-CARRIER NOTE, added
    at out-of-family review round 3 [P2] and recorded rather than used to reverse the operator:
    the reviewer is CORRECT that `--open` will no longer surface this row once it is `closed`,
    and that the status enum''s prose names only two closure senses (built+merged, or foreclosed/superseded)
    while this closure is a THIRD -- a RATIFIED ACCEPTED BOUND. The operator''s answer stands;
    what the finding correctly demands is that the live trigger not be orphaned, so its monitoring
    carriers are named here explicitly. (1) SHIPPED SOURCE is the primary carrier: the bound and
    this row are cited in the memory_retrieval_index module docstring (`:19`-`:20`) and on `commit_seq`
    at memory_store.py (`:154`-`:163`), so any arc touching that surface meets the bound where
    it lives -- which is strictly closer to the trigger than a register row an agent must think
    to query. (2) TWO LIVE SIBLING ROWS over the SAME mixed-version surface stay in the open inventory
    and cross-reference this one: B-94 (the untokenized auto-refresh rebuild path -- `registered_finding`)
    and B-45 (the Windows same-value `commit_seq` bound -- `registered_finding`). Either firing
    puts an agent on this surface with this row cited from the source it is reading. (3) The re-open
    trigger above is preserved VERBATIM and closure does not retire it: on a planned mixed-version
    deployment, or the first production `derived_index_hook` consumer, this row REOPENS. If a
    future arc wants the trigger back in `--open`, reopening it is a one-field edit -- the information
    required has been kept deliberately intact.'
  council: 'Not for the bound itself -- accepting a documented limit needs no deliberation. IF the version-fence direction (2) is ever taken it becomes council-eligible: refusing to write during an upgrade window is an operator-visible availability tradeoff (C11 operator-loop / local-deployment against C3 state-and-persistence integrity), and that IS a nameable cross-domain tension rather than an implementation detail.'
  heading: '### B-95 · Mixed-version ordering bound — a live PRE-B-93 **writer**''s untokenized stale markers cannot be ordered against a covered rebuild *(surfaced by out-of-family Codex R6 on the B-93 gate-log arc, 2026-07-30; REGISTER-AND-HOLD under the arc''s own stop rule)* *(**CLOSED 2026-08-05** — a ratified accepted bound; the re-open trigger is preserved verbatim)*'
- id: B-96
  title: 'The B-77 first-observation grace is bounded by SWEEP COUNT, not elapsed time --
    a short or failing run''s own shutdown sweep can reclaim a crash-recovered entry
    milliseconds after its bootstrap sweep first observed it'
  status: closed
  pr: '#1179 (the Class 2 fork filing) + #1183 (the C-1/C-2 council record) + #1233 (the 2026-08-05 ratification record) + #1235 (the SPEC leg -- Runtime spec v1.111 section 14.8.11.1 + Runtime plan v2.58 U-RT-150) + #1237 (the IMPL leg -- U-RT-150)'
  summary: 'Surfaced by out-of-family Codex round 2 at the B-77 impl arc (2026-07-30),
    against the fix landed in that same PR. `gc_sweep`''s first-observation grace refuses to
    reclaim an entry the store has not already seen past TTL at a PRIOR sweep of the same
    root -- which closes B-77''s named defect (a crash-recovered entry carrying the
    pre-commit stamp is never reclaimed BY THE SWEEP THAT FIRST SEES IT, i.e. never by the
    bootstrap sweep of the recovering process). The residue Codex names is the grace''s
    PRACTICAL DURATION: every `run()`/`resume()` sweeps AT LEAST twice by construction -- once at
    `bootstrap/stage_4_od.py:123` and once, unconditionally, at `shutdown.py` step 5b -- and a
    THIRD trigger the original count MISSED (corrected 2026-07-31): `_maybe_opportunistic_gc_sweep`
    (`protected_result_store.py:882`) fires from inside `write_once` at most once per
    `_OPPORTUNISTIC_GC_INTERVAL_SECONDS` (300s, `:72`), so a long-lived process also sweeps on
    WRITE cadence. "Twice by construction" was therefore a FLOOR, not the count. This SHARPENS
    rather than weakens the finding -- a third trigger can only SHORTEN the first-to-second
    interval -- but the premise is corrected rather than left as written. So
    the interval between the first and second observation is the RUN''s own duration, which
    for a short or immediately-failing run is milliseconds. A genuinely-live,
    crash-recovered entry therefore still survives only to that same run''s shutdown, not
    for a wall-clock interval any repair flow can rely on. This is the sweep-COUNT reading
    of B-77''s ratified close_out (verbatim: "should not reclaim an entry it has not
    observed in a PRIOR sweep (a grace period) -- bounded to one sweep interval"), landed
    as ratified and NOT redesigned mid-arc -- the B-77 row records three prior wrong
    mechanism claims on this same file, and a fourth unratified attempt is precisely the
    non-convergent-adversarial-hardening pattern this workspace stops on. Registered here
    instead, with the practical bound stated honestly in the code docstring rather than
    overclaimed. Reachability is the same narrow floor B-74/B-77 already share: it needs a
    deployment `ttl_seconds` short enough that the crash window alone tips a live entry
    past it (the factory default is 86400s, roughly seven orders of magnitude above that),
    AND a crash landing strictly between `os.link` and the post-commit mtime refresh, AND a
    recovering run short enough that its own shutdown sweep fires before any repair read.'
  close_out: 'IMPL LEG LANDED at PR #1237 -- U-RT-150 built at harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py: the sweep-COUNT grace is REPLACED by a CONJUNCTIVE, publication-bounded, DURABLE elapsed-time rule keyed on a dedicated gc-observations.json record in the store root (published temp-write + fsync + os.replace + directory fsync -- never the write-once no-replace primitive, never unlink-then-recreate; the name is disjoint from BOTH sweep globs and is NOT dot-leading, and so is its publication temporary), keyed over BOTH sweep classes, replace-not-accumulate with retained names carrying forward their ORIGINAL first_observed_at. Both falsifiability surfaces landed: the oldest-resident-candidate age as a FIELD of every sweep emission plus the which-term-fired-last discriminator, and a read-only, sweep-free harness-inspect store row reporting the record state THREE-WAY off the CONFIG-DERIVED store root (the same expression materialize_protected_result_store_stage uses, never --ledger-path). All FIFTEEN acceptance criteria are green with PD-8 mutation probes, including the AC #13 re-grounding pass over every inverted grace-dependent witness with NO witness weakened or deleted, and the AC #10 / #14(i) durability witness driven as REAL successive one-shot subprocess invocations against one store root. Registered row B-110 disposition (c) was NOT taken -- see that row. STATUS open -> closed. STATUS design_substrate_gated -> OPEN at this leg, per the register''s OWN status enum (this file,
    lines 31-39: only ratified + APPLIED spec deltas plus the corresponding Phase-6 plan deltas open
    the row for code -- both landed here). This does NOT contradict the fork section 11.5, which
    contrasts design_substrate_gated against `closed` at the RATIFICATION leg and says B-96 / the
    B-77 residual / B-74 flip to `closed` only when the IMPL leg merges -- `open` is not `closed`.
    Corrected at out-of-family review round 4 [P2]. SPEC LEG LANDED at PR #1235 -- Runtime spec v1.110 -> v1.111 adds NEW section
    14.8.11.1 carrying the council record section 7.1''s TWELVE conditions as contract terms 1-12,
    plus two strict APPENDS (the bounded-retention bullet''s cross-reference and the
    deferred-to-discretion list''s two new entries); the Runtime plan absorbs it at v2.58 as
    ONE NEW unit U-RT-150 with FIFTEEN acceptance criteria (a NEW unit, NOT an amendment of the
    landed U-RT-145, whose AC #7 remains true and unchanged -- the B-97(a) -> U-RT-149 precedent).
    Clearance markers filed for both artifacts; the FIVE-of-twelve EXPANSION FLAG (conditions #5,
    #6, #8, #11, #12 did not exist in the fork section 8 ask) is surfaced in the spec marker per
    council record section 7.2, with a Class 2 route named if the operator judges they owe a fresh
    decision. NO new C-RT-* number, NO CXA delta (aggregate frozen at 111), NO OD delta
    (C-OD-05 section 5.1 roster 15 -> 15), NO harness-*/src|tests edit. STATUS IS NOW `open`
    (see the status paragraph at the head of this close_out) -- the IMPL leg is the only leg still
    owed, and B-96 / the B-77 residual / B-74 flip to `closed` only when it merges. RATIFIED 2026-08-05 as READING C, ceiling
    sub-decision C-2 -- the GRACE TERM ALONE,
    with NO absolute mtime-keyed reclaim ceiling. Operator AskUserQuestion on the fork''s section
    8 (`.harness/class_2_fork_b96_gc_grace_elapsed_time_bound.md`, FILED at PR #1179); the HELD
    C-1/C-2 sub-decision was resolved by the dyadic convening recorded at `.harness/council-b96-grace-ceiling-2026-08-01.md`
    section 7 -- verdict C-2, UNANIMOUS across C3/C10/C7 from non-overlapping grounds, merged
    at PR #1183. RATIFIED FORM: `first_observed_at` is WALL-CLOCK; carrier is (C-i), a dedicated
    CANDIDATE-FILENAME + timestamp sidecar published by temp-write + fsync + os.replace + directory
    fsync -- the record''s content set is council condition #5''s CLOSED two-member set {candidate
    filename, first_observed_at}, KEYED ON THE CANDIDATE FILENAME over BOTH sweep classes, `*.entry`
    AND `.tmp-*`. Saying ''digest+timestamp'' would narrow the key to digest-named entries and
    leave condition #1''s second conjunct permanently UNSATISFIABLE for `.tmp-*` crash orphans
    -- forcing an impl either to reclaim one on first sight or never to reclaim one; corrected
    here at out-of-family review round 3 [P2] (explicitly NOT the write-once O_EXCL/os.link primitive,
    which is no-replace and would freeze the record at its first snapshot); record-absence reads
    as NO OBSERVATION; retention typically up to 2xTTL plus UP TO TWO sweep-trigger intervals
    -- and this figure is CONDITIONAL, never an unconditional bound (council condition #7): the
    sweep-trigger interval is UNBOUNDED in the one-shot process shape (pre-existing at HEAD, not
    introduced by the grace), and the figure holds only while the observation record is PRESENT
    AND READABLE. NO unconditional ''bounded by N x TTL'' claim is available under EITHER ceiling
    form, and the spec leg MUST NOT encode one -- corrected here at out-of-family review round
    3 [P2]. READING B (per-process elapsed time) IS RETIRED EXPLICITLY -- no future impl leg may
    build it as written. READING A (keep the sweep-COUNT bound) was NOT selected and is NOT partially
    adopted. STATUS design_substrate_gated, NOT closed (fork section 9, out-of-family round 3
    [P1]). The SPEC leg -- a Runtime section 14.8.11 amendment carrying the council record''s
    TWELVE conditions at its section 7.1, with section 7.2''s expansion flag surfaced in the clearance
    marker per root CLAUDE.md section 4.5 -- HAS LANDED at PR #1235 as section 14.8.11.1 (spec v1.111,
    plan v2.58 / U-RT-150); ONLY THE IMPL LEG REMAINS OUTSTANDING, which is why the row is `open`
    (actionable, no blocker) rather than closed -- `closed` would drop that live work out of the
    open-work inventory. (This sentence previously read "and the IMPL leg are both still outstanding";
    corrected in place at out-of-family review round 3 [P2] so the structured register cannot
    reschedule an already-landed leg.) SPEC AND IMPL DO NOT LAND TOGETHER
    (the B-33/B-39/B-59/B-69/B-70/B-72/B-97/B-107 precedent). CXA: no delta owed -- determined
    at the council record''s adversarial pass, not assumed. B-96, the B-77 residual and B-74 all
    flip to `closed` ONLY when the impl leg merges. NOT OWED BY ANY LEG, restated so it is not
    re-derived: narrowing `ttl_seconds` or adding a TTL floor; any fourth reordering of `_publish_atomic`''s
    two-stamp pipeline; carriers (C-ii)/(C-iii). THE RATIFIED RECIPE, as the pre-ratification
    close_out stated it, follows -- retained because the impl leg builds exactly it: Replace the
    sweep-COUNT grace with an elapsed-TIME one, using the first observation''s wall-clock as the
    age authority B-77''s close_out actually names ("an age authority that cannot predate publication
    by construction") -- record `first_observed_at` alongside the filename in `_root_observed_expired`
    and reclaim only when BOTH the mtime-derived age AND `now - first_observed_at` exceed `ttl_seconds`.
    Note the genuine tradeoff that makes this a ratification-owned decision rather than a mechanical
    patch: the conservative rule lengthens worst-case retention of a genuine crash orphan by up
    to one full TTL (a day at the factory default), which trades against the spec''s own bounded-retention
    term (Runtime SS14.8.11 -- ANCHOR REFRESHED 2026-08-05 at the ratification leg by direct read
    at HEAD 4815ebef: the bullet is :4926 at Runtime v1.110, heading :4915; the council record''s
    '':4909 at v1.109'' anchor was ALREADY STALE, v1.110''s B-104 Component 1 insertions having
    shifted the file; the bullet TEXT is unchanged across v1.106->v1.110 -- "a signing outage
    must not grow an unbounded store of sensitive payloads"). Pick the bound deliberately; do
    not narrow `ttl_seconds` itself (B-74''s declined option). Witness by driving a full bootstrap-then-shutdown
    pair against a crash-window entry and asserting it survives BOTH sweeps when the elapsed interval
    is under TTL, and is reclaimed once it is not.'
  council: 'CONVENED AND RESOLVED 2026-08-01 -- the conditional fired when Reading C was taken,
    the dyadic was convened (C3 + C10, C7 added on Layer-C scoring) and its record is `.harness/council-b96-grace-ceiling-2026-08-01.md`
    (merged at PR #1183). VERDICT C-2, unanimous from non-overlapping grounds: C3 on age-authority
    integrity + the implicit-floor identity + the witness-inversion discriminator; C10 on under-gating/fail-open
    in a fail-closed section + false audit emission + the non-existence of the bound C-1 sells;
    C7 concurring, plus the finding that the report log is the store''s SOLE forensic artifact.
    No third ceiling form survives -- record-keyed, hybrid absent-record and a fourth ctime-keyed
    variant were each constructed and killed, and are recorded as tested-and-dead rather than
    suppressed. NO FURTHER CONVENING is owed at the spec or impl leg.'
  heading: '### B-96 · The B-77 first-observation grace is bounded by SWEEP COUNT, not elapsed time -- a short or failing run''s own shutdown sweep can reclaim a crash-recovered entry milliseconds after its bootstrap sweep first observed it *(surfaced at the B-77 impl arc, out-of-family Codex round 2 [P2], 2026-07-30)* *(**RATIFIED 2026-08-05 as READING C, ceiling sub-decision C-2** — the grace term alone, carrier `(C-i)`; **SPEC LEG LANDED 2026-08-05 (Runtime v1.111 + plan v2.58 U-RT-150); IMPL LEG LANDED 2026-08-05 (U-RT-150) — CLOSED**)*'
- id: B-97
  title: The durable pause journal carries no tenant binding (half (a) -- RATIFIED
    Reading A, SPEC-APPLIED at v1.108, IMPL LANDED at U-RT-149); its appends were
    unserialized across processes (half (b), CLOSED)
  status: closed
  pr: '#1171'
  summary: 'HALF (b) CLOSED at PR #1167; HALF (a) CLOSED at PR #1171 -- RATIFIED, SPEC-APPLIED **and IMPL
    LANDED**. U-RT-149''s ONE CONJUNCTIVE closure criterion is satisfied across all 12 ACs, with
    the section 13.7 enumeration surface shipped as the PRECONDITION of the (3a) disposition
    rather than an optional companion. NOTHING REMAINS OWED ON THIS ROW; the two dated
    re-checks it spawned (B-103, B-104) stay open on their own rows. HALF (a) RATIFICATION (operator, 2026-07-31, two gates,
    recorded verbatim at the fork''s section 11): GATE 1 selected READING A -- per-tenant PATH
    SEGREGATION, a contractually injective CONSTANT encoding, the record shape UNCHANGED, the
    five-member PauseJournalReadCause vocabulary STAYS AT FIVE, a MANDATORY quiesced cutover, and
    the (workflow_id, run_id) rider NOT bundled. GATE 2 confirmed both priced rows as the C3/C11
    dyadic recommended: (3a) ABANDON-BY-DEFAULT for unattributed legacy records, and section 5.3.1
    as (i)+(iii) with (iii) a PRECONDITION of (3a), not an optional companion. THE FOUR REGISTER
    DECISIONS, AS ANSWERED: (1) key composition -- the tenant enters the sha256 FILENAME under a
    TOTAL injective encoding closing all THREE hazards (None-vs-real branch collision, a tenant
    containing the composition delimiter, and field-boundary shift), realized on the in-house
    length-prefixed canonical-segment shape, as a module CONSTANT with NO configuration surface,
    delegating the tenant component to the Runtime-LOCAL normalize_tenant_scope and explicitly NOT
    to harness_od''s sidecar_tag/_tenant_tag (incompatible None disposition; and the pause-journal
    key joins to nothing outside itself). (2) refusal semantics -- DISSOLVED, not discharged: under
    path segregation a wrong-tenant read resolves the caller''s OWN path and returns the ordinary
    absent, which is the TRUTH rather than a misleading diagnosis, so NO sixth cause member and no
    RT-FAIL peer class is minted. The earlier framing of this row -- that a cross-tenant refusal
    NEEDS a sixth member -- is superseded by the ratification and is corrected here rather than left
    to mislead. (3) migration -- (3a) abandon by default, with the failure-mode asymmetry stated as
    the REASON in spec text (a violated (3a) fails REFUSING; a violated (3b) fails SILENTLY AND
    EXECUTES, passing every shipped guard over a store with no hash chain); (3b) operator-declared
    RELOCATION available under four mechanical terms -- legacy-SOURCE per-workflow flock held
    source-read-through-target-publication (a directory-tree lock is explicitly NOT sufficient and
    contends on a different inode), read-back verification on record_count + latest_record_digest
    that DETECTS-and-NARROWS but does NOT CLOSE the residual, a write-once crash-atomic NO-REPLACE
    publication which IS the empty-target enforcement (no second target lock), per-journal
    idempotent re-runnable sequencing discriminated on content equality, and PRESERVATION OF THE
    FILENAME-TO-WRAPPER BINDING (adoption operates only on journals whose wrapper workflow_id
    re-derives their own legacy sha256 filename -- republishing a mis-filed record under the new
    key promotes DORMANT state into LIVE state where every shipped guard passes). (4) the (workflow_id,
    run_id) rider -- answered NO explicitly: run_id is the identifier a crash destroys while
    tenant_id comes from config and is always known, so they are not two instances of one change and
    bundling would stall both. SPEC APPLIED at Runtime spec v1.108 (section 14.14.8 keying +
    encoding + migration contract + orphan statement + named silence; section 14.14.9.1 parallel
    keying per its own both-surfaces-or-neither rule; section 30''s absent row tenant-scoped in
    MEANING and REPAIR across three states; section 13.4 inventory rows; NEW section 13.7
    enumeration surface). PLAN at Implementation_Plan_Harness_Runtime_v2_56.md, NEW U-RT-149.
    CP/OD/IS/AS specs and plans UNCHANGED and CXA carries no new row -- each DETERMINED at the leg,
    not assumed: the arc is wholly intra-harness-runtime and the one candidate crossing (tenant
    normalization) is a deliberate non-crossing.'
  close_out: 'HALF (b): CLOSED at PR #1167 -- 13 mutation-probed witnesses at
    harness-runtime/tests/test_journal_pause_store_cross_process_append_b97.py. HALF (a): CLOSED at
    PR #1171 -- the spec leg (Runtime v1.108) AND the impl leg (U-RT-149) both landed, and the
    conjunctive criterion below was met in FULL, not in part. THE ROW IS CLOSED; the criterion is
    retained verbatim below as the record of what closure required, not as outstanding work.
    CLOSURE
    CRITERION, carried verbatim from Implementation_Plan_Harness_Runtime_v2_56.md U-RT-149: B-97
    half (a) closes iff (tenant-composite keying landed AND injectivity witnessed across all THREE
    hazards) AND (the section 13.7 enumeration surface landed) AND ((3a) disposition + the orphan
    statement witnessed) AND (the (3b) adoption tool landed to its FULL FIVE-TERM set -- exclusion, read-back, atomic no-replace publication, idempotent re-run, AND the filename-to-wrapper binding preserved via the three-way classification, i.e. U-RT-149 AC #9 INCLUDING (d-ter), whose omission would promote a DORMANT mis-filed legacy journal into LIVE resumable state where every shipped guard passes). PARTIAL IS
    NON-CLOSURE. Only the OPTIONAL disposal action sits outside the conjunction, because the spec
    declares it optional in those words; if built it is all-or-nothing. (3b) IS INSIDE the
    conjunction on a correction made at the spec leg''s out-of-family round 1 [P1]: the spec says
    (3b) is AVAILABLE and section 13.4 schedules its admin action at this impl arc, and under (3a)
    it is the ONLY recovery path a deployment that upgraded with pauses outstanding has -- shipping
    the keying change without it would leave the contract and the product disagreeing. ENFORCEMENT
    IS A PRE-MERGE REVIEW OBLIGATION on the closing PR, not a validator rule:
    tools/forward_register.py --check validates close_out presence and a closed row''s pr cite
    without parsing the conjunction. THE SPEC-REFRESH OBLIGATION THIS ROW CARRIED IS DISCHARGED:
    section 30''s :3313 term-5 sentence and its :3319 empty-journal cell, both stale-as-described
    since half (b) landed, are refreshed at v1.108 -- and the row''s own note that the half-(a) leg
    would touch section 30 ''for the sixth cause member'' was wrong once Reading A was ratified; the
    leg touched section 30 for the absent-row tenant-scoping and the B-102 fold-in instead. THREE
    FURTHER OBLIGATIONS DISCHARGED AT THIS LEG: the blocking-flock-on-the-event-loop forward note is
    SPLIT to its own row B-103 (parking it here would have lost it the moment this row closes);
    B-102''s cause-table amendment is folded into the same section 30 rows so one cleared table is
    amended once rather than twice; and a NEW row B-104 records the absent pause-resolved marker
    that bounds what the section 13.7 enumeration can honestly claim. HALF (a) IMPL LANDED, and the
    CONJUNCTIVE CRITERION ABOVE IS MET IN FULL, term by term, each by EXECUTION: (i) tenant-composite
    keying landed at journal_workflow_pause_store.encode_pause_journal_key / pause_journal_filename
    (length-prefixed canonical segments, UTF-8 BYTE counts with surrogatepass, joined on |, the
    untenanted branch the ONE-segment form) with injectivity witnessed across ALL THREE hazards at
    test_pause_journal_tenant_keying_b97a.py -- field-boundary shift plus a length-varying family,
    branch collision against every string a reserved-literal scheme would have reserved, and
    delimiter collision in either position -- plus totality over lone surrogates and a
    reads-no-configuration witness; (ii) the section 13.7 enumeration surface landed at
    admin/pause_journal_enumeration.py and as a harness-inspect extension, AC #7 green term by term
    at test_pause_journal_enumeration_b97a.py including the lock-file exclusion, the
    engage-only-when-the-dir-exists byte-identity, the no-PauseSnapshot-constructed assertion and
    the EXISTING chmod-readonly fixture; (iii) the (3a) disposition and the orphan statement
    witnessed -- the untenanted default path CHANGES, no code path consults the legacy key
    (open-spied), and the store removes nothing on any path with no pruner on its surface; (iv) the
    (3b) adoption landed to its FULL FIVE-TERM set at admin/pause_journal_adoption.py, AC #9
    INCLUDING (d-ter), whose MUTATION PROBE is witnessed both ways: with the binding check removed a
    mis-filed record is republished at encode(tenant, B) and becomes LIVE and readable, and with it
    restored the same directory refuses. The OPTIONAL disposal action was ALSO built, to its full
    term set (dry-run by default, refuse-while-recoverable), at admin/pause_journal_disposal.py.
    ONE DELIBERATE ADDITION beyond the spec''s minimum disposition vocabulary, recorded rather than
    absorbed: a CURRENT-FORMAT journal takes a SKIP disposition rather than refused-as-not-LEGACY,
    because section 13.7.1 calls it ORDINARY STATE and folding it into a refusal made every
    idempotent re-run on a healthy deployment exit nonzero -- surfaced by execution, not argued.'
  council: 'DISCHARGED -- the C3 (state/persistence keying) vs C11 (operator-loop / local-deployment)
    dyadic was convened 2026-07-31 as a narrow RE-CONFIRM of the already-ratified Reading A and
    returned CONFIRM-WITH-CONDITIONS, unanimous and independently reached, with 17 conditions
    K-1..K-17 (15 load-bearing, 2 recommended). Record at
    .harness/council-b97a-tenant-path-segregation-2026-07-31.md (PR #1169). The row''s antecedent --
    ''if a tenant key adds local-deployment config burden'' -- was probe-verified FALSE as literally
    written (RuntimeConfig.tenant_id already exists, defaults None, and already reaches both
    construction sites, so the default path costs the local operator ZERO configuration); the
    convening was owed on the RESTATED cost the row did not anticipate, the upgrade/abandonment
    burden that lands on the single-tenant local default whose path changes even though it never had
    a tenant problem. No further convening is owed for the impl leg.'
  heading: '### B-97 · The durable pause journal carries no tenant binding *(surfaced at the B-69 council convening as residual R-3, routed OUT of that arc, 2026-07-30; half (b), cross-process append serialization, CLOSED 2026-07-31; half (a), the tenant binding, RATIFIED + SPEC-APPLIED 2026-07-31 as Reading A — per-tenant path segregation — with the impl leg owed)*'
- id: B-98
  title: Gate description / question text is absent from every durable pause carrier
  status: registered_finding
  pr: '#1180 (the Class 2 fork filing) + #1182 (the ratified-defer leg) + #1233 (the 2026-08-05 batch re-affirmation)'
  summary: 'An operator inspecting durable pause state can learn WHERE a workflow is paused and
    through WHICH resolution channel, but not WHAT was asked. Verified across every PauseSnapshot
    field at the B-69 convening, and symmetrically for the closed-vocabulary counter-proposal:
    HITLPlacementKind exists as a StrEnum, but NO durable pause carrier declares a field of that
    type -- every placement occurrence in pause_resume_protocol_types.py is docstring prose. Both a
    free-text description and a closed-vocabulary placement field are therefore CAPTURE-SIDE scope,
    and the same empirical fact defeats both proposals. hitl_gate_config_hash was tested as a
    substitute and rejected on its surviving ground: it is a MATERIAL-DIFF IDENTITY, not a
    description -- an opaque digest whose only operator-meaningful semantic is "the gate config
    changed since capture"; it discriminates a config EDIT, never a re-pause.'
  close_out: 'RE-AFFIRMED 2026-08-05 in the operator ratification batch: READING C (DEFER) stands,
    unchanged, with D-0 dominant. NO NEW WORK IS OWED -- the substantive ratification already
    ran on 2026-08-01 and its RATIFICATION LEG landed at PR #1182 (the fork''s `section 11 RATIFICATION`
    addendum). **PR #1182 carried NO B-98 spec-side component -- Reading C owes NO spec leg and
    NO impl leg, as this row and the fork both state; the Runtime v1.110 spec delta in that PR
    belongs to B-104, a different row and a different ratified reading. The two shared a PR, not
    a scope.** (Attribution corrected at out-of-family review round 9 [P2]: the earlier wording
    ''its spec-side component'' could let a later agent infer B-98''s carrier decision had been
    design-applied.) the row''s `pr:` pointer, which still read `#pending` for that leg, is REPAIRED
    here to name #1182. STATUS stays `registered_finding` with the four-disjunct demand test as
    the falsifiable re-open trigger, exactly as the fork''s section 9 column C prescribes. Capture-side,
    hash-affecting, X-AL-3-relevant -- materially bigger than the read-only projection B-69 authored,
    and explicitly declined for that arc by the ergonomics voice itself. Close-out requires a
    capture-side carrier decision (which field, on which resume-state carriers, with what hash
    disposition per the drop-when-empty precedent) before any read-side projection field is added.
    DISTINCT FROM B-71, which adds a run-instance correlation id to HITLEscalationBrief and the
    webhook payload -- B-71 is about DISAMBIGUATING two identical-looking escalations; this row
    is about DESCRIBING one. [SUPERSEDED 2026-08-01, struck in place: the reopening rule here
    read "Open only if the operator loop demands it" and is REPLACED -- not supplemented -- by
    the falsifiable D-0..D-3 demand test below. It was unfalsifiable as written and MUST NOT be
    applied as a second, parallel reopening criterion. Caught at out-of-family review round 2
    [P2].] The B-69 projection deliberately ships without it, and position plus pause_reason were
    judged sufficient for the map-composition task. RATIFIED 2026-08-01 (operator AskUserQuestion)
    as READING C -- DEFER, with NO CARRIER CHANGE. The fork doc .harness/class_2_fork_b98_gate_description_durable_carrier.md
    is FILED (#1180) and its section 11 RATIFICATION addendum records the outcome verbatim-in-substance.
    Reading C owes NO spec text and NO impl text (fork section 9, column C), so no design-substrate
    edit and no plan delta ride this ratification, and no plan cite anywhere contradicts it --
    B-98 is cited at NO implementation plan and at NO acceptance criterion. Runner-up A-1 was
    NOT selected and is NOT partially adopted. THE REOPENING TRIGGERS -- a FALSIFIABLE four-disjunct
    demand test, any one disjunct fires, replacing the prior unfalsifiable "open only if the operator
    loop demands it": D-0 (DOMINANT) -- THE HITL-PAUSE CLASS BECOMES PRODUCTION-REACHABLE: the
    FM-2 webhook-config arc lands (operator-supplied WebhookConfig, deferred at webhook_delivery_composer_factory.py
    lines 17-22), so a durable HITL_PENDING pause can occur outside tests at all. Until D-0 this
    row is a finding about a DORMANT class, which is why D-0 dominates the other three. D-1 --
    A DESCRIPTION EXISTS THAT THE WEBHOOK DOES NOT ALREADY DELIVER to the resuming operator (round-2
    tightened form; the untightened first form over-triggered). Two disjuncts: (a) a description
    surface lands that is NOT projected into WebhookPayload.payload_body (webhook_brief_adapter.py
    lines 76-84) -- e.g. HITLPlacement (hitl_placement.py:176) gains an author-written text field;
    or (b) the resumer ceases to be the webhook recipient (the webhook stops being the delivery
    channel, or a supported flow resumes a pause through an operator who never received it --
    i.e. the workspace advances past solo-developer on the committed bridging arc, root CLAUDE.md
    section 10.2). EXPLICITLY NOT A D-1 ON ITS OWN: HITLEscalationBrief. escalation_reason (validator_framework_types.py:150)
    acquiring a non-literal production value, since that value is projected into every webhook
    at webhook_brief_adapter.py:77 -- it fires D-1 only in conjunction with (b). D-2 -- THE CHANNEL
    OPENS ANYWAY: any arc threads content off HITLPauseRequestedSignal (hitl_gate_composer.py:381)
    across the workflow_driver.py:5375 drop point, at which point the carrier-home cost B-98 would
    pay is already paid and A-2/B become marginal. D-3 -- AN OBSERVED MISDESCRIPTION: an operator-loop
    report of a response composed against the wrong gate that pause_reason + position + step_kind
    did not prevent. The harm class, not the convenience. GROUNDING CARRIED BY THE RATIFICATION
    (fork section 3, absorbed into this close-out): (a) the durable HITL_PENDING pause class is
    NOT production-reachable in stock bootstrap at HEAD, so the row describes a dormant class;
    (b) the description ALREADY REACHES THE OPERATOR contractually via the webhook, delivery being
    a code-order precondition of the pause -- so the durable gap for the TEXT is CORRELATION,
    which NARROWS (does not abolish) this row''s own B-71-distinctness claim; (c) the PLACEMENT
    is by contrast NOT contractually available, because compose_hitl_ action_id''s shape is explicitly
    discretionary, so a typed placement field is net-new; (d) B and B-prime are DISTINCT and stay
    distinct -- B carries the already-existing exc.brief.escalation_ reason and needs NO authoring
    surface, while B-prime (this row''s literal "free-text description") needs one MINTED FIRST,
    since no author-written description exists and a system reason is not a substitute; (e) the
    undecided question is the CHANNEL, not the field (the A-1 / A-2 split); (f) the carrier answer
    is TWO sites -- PauseSnapshot top-level PLUS a per-row PreDispatchGateOwningBranchResumeState
    sibling, the split forced by CARDINALITY (several pre-dispatch rows per snapshot), not by
    taste; (g) the hash disposition is FULLY PRE-DECIDED by the five-instance drop-when-None precedent
    (nearest bare-scalar template at pause_resume_protocol.py:835); and (h) ADDITIVITY removes
    the decide-now pressure -- the B-99 exclusion asymmetry does not transfer to an inclusion.
    ORDERING RECORDED, NOT ACTED ON: B-71 DOMINATES B-98 for the operator-loop win -- the description
    already reaches the operator, and what is missing is the join B-71 owns. This ratification
    does NOT re-open B-71. CLASS 3 CITE-HYGIENE ITEM, RECORDED NOT PATCHED: Spec_Control_Plane_v1_99.md:40
    ("v1.94''s synthesis_step_id") is FALSIFIED -- first occurrence is v1.58. A design-substrate
    edit does not ride a doc-only ratification; the CP spec is untouched by this leg.'
  council: 'CONDITIONAL as of the 2026-08-01 Reading C ratification -- NARROWED from the prior
    unconditional "yes if opened" to the fork''s section 7 per-reading form. Under the RATIFIED
    Reading C (DEFER): NONE is owed now, AND ONE IS ADDITIONALLY OWED AT D-0 OR D-1 EVEN IF C IS
    STILL THE STANDING DISPOSITION -- D-0 because it makes the pause class real and so gives the
    tension a live surface, D-1 because it is precisely the condition under which free text the
    operator does NOT already hold starts to exist and the two positions separate. If instead a
    build reading is opened, the convening is owed BEFORE THE SPEC LEG for A-2 or B (C7 added as
    consultant under B) -- the nameable C10 vs C11 tension (action-safety wanting a closed
    vocabulary and no unbounded free-text channel in durable state, against operator-loop wanting a
    description a human can actually act on) bites on the CHANNEL question those two readings open.
    A-1 needs NO convening: it opens no cross-axis channel and carries a pre-decided hash
    disposition. The B-69 convening surfaced exactly that tension without resolving it, because the
    empirical absence closed the seam first.'
  heading: '### B-98 · Gate description / question text is absent from every durable pause carrier
    *(surfaced at the B-69 council convening, SEAM 3, routed OUT of that arc, 2026-07-30; Class 2
    fork FILED 2026-08-01 at PR #1180; RATIFIED 2026-08-01 as READING C — DEFER with the
    four-disjunct falsifiable demand test D-0…D-3, D-0 dominant; NO carrier change, NO spec leg,
    NO impl leg)*'
- id: B-99
  title: The B-69 projection's summary_text exclusion must be re-adjudicated when the
    pause_context_reader deferral is discharged
  status: registered_finding
  summary: 'A NAMED REOPENING CONDITION, not a defect and not a gate -- filed so the B-69
    projection''s own justification cannot go stale silently. Runtime spec v1.107 section 14.14.9.2
    excludes StateSummary.summary_text from the caller-facing projection (CITE CORRECTED 2026-07-31:
    this row read "section 31.2", which resolves to NOTHING -- the spec has no section 31, its
    highest is section 30; the exclusion is the state_summary.summary_text table row inside
    section 14.14.9.2, and the prose register already carried the correct cite). That exclusion originally
    rested on TWO legs; one was falsified at the convening''s own adversarial pass. The FALSIFIED
    leg: "the exclusion is free, since summary_text is empty everywhere" -- true today, but only
    because the MVP binding is the injected placeholder pause_context_reader that Runtime spec
    section 14.14.7 marks explicitly deferred and whose own docstring says its successor "reads the
    current ledger head." The SURVIVING leg, which now carries the argument alone: the field is an
    UNBOUNDED FORWARD CHANNEL -- a placeholder for real ledger content with no redaction contract
    anywhere. Honest counter left unsmoothed: when a real reader is composed, the ergonomics case
    for INCLUSION genuinely reopens.'
  close_out: 'TRIGGER: the discharge of Runtime spec section 14.14.7''s pause_context_reader
    deferral -- i.e. when a real reader is composed and summary_text begins carrying ledger content.
    OBLIGATION AT THAT POINT: re-adjudicate the B-69 projection''s field set, WITH A REDACTION
    CONTRACT AS A PRECONDITION OF ANY INCLUSION, and re-adjudicate the paired OD spec v1.36
    section 30.5.4 redaction posture (currently NOT ENGAGED precisely because nothing redactable is
    emitted) in the SAME arc. NO redaction gate is imposed now, and that restraint is deliberate:
    the field is excluded, so there is nothing to redact, and gating an absent channel is textbook
    over-gating. Also re-examine at the same trigger: Runtime plan v2.55 U-RT-148 AC #5 (the W4
    characterization witness) asserts a PLACEHOLDER property and will fail for a good reason once a
    real reader lands -- its correct disposition is then DELETION or INVERSION, never repair.
    DATED RE-CHECK 2026-07-31 (post-#1170/#1171, at the B-103 arc), RE-CHECKED AGAIN 2026-08-05 at
    HEAD 43d4dda5 with the Runtime spec now at v1.110, two versions past that pass: THE TRIGGER HAS
    NOT FIRED, on the spec side AND -- newly covered at this pass -- on the code side. SPEC SIDE,
    CITES REFRESHED 2026-08-05 (the B-97(a)/B-100 legs grew the file; the 2026-07-31 pass recorded
    :5865 / :5868 / :5949): the section 14.14.7 "Deferred to implementation discretion" heading is
    at :5908, the pause_context_reader deferral bullet at :5911, and the MVP constant-sentinel
    statement at :5992, with sections 14.14.9.2-.5 preserved VERBATIM through v1.110 -- so neither
    the exclusion nor the redaction posture it pairs with has moved. CODE SIDE, added at this pass:
    the production binding is still the placeholder -- _make_default_pause_context_reader
    (harness-runtime/src/harness_runtime/bootstrap/factories/pause_resume_protocol_factory.py:71-102)
    returns a StateSummary with summary_text="" at :102, its docstring explicitly labelling the
    state-tracking semantics a placeholder, and every other production StateSummary on the pause
    path likewise constructs summary_text="". Nothing owed; disposition VERIFIED-DORMANT; re-check
    again when a real reader is composed.'
  council: 'no -- the tension was already convened and adjudicated at the B-69 record (C10 vs C11 on
    payload boundary); this row records the condition under which that adjudication must be re-run,
    and the re-run inherits the same voice pair without needing a fresh activation decision.'
  heading: '### B-99 · The B-69 projection''s `summary_text` exclusion must be re-adjudicated when
    the `pause_context_reader` deferral is discharged *(named reopening condition registered at the
    B-69 spec leg, 2026-07-30)*'
- id: B-100
  title: The empty-idempotency_key key-ABSENT carve-out is scoped to the fan-out crash-reconstruction
    carrier, but the LINEAR and orchestrator fence capture sites admit an empty key too
  status: closed
  pr: '#1174'
  summary: 'Surfaced at the B-69 IMPL leg (2026-07-31), NOT absorbed. CP spec v1.112 section 2.1
    and Runtime spec v1.107 section 14.14.9.2 both scope the empty-key / key-field-ABSENT source
    shape to EffectFencePausedBranchResumeState -- the fan-out crash-reconstruction carrier, which
    the spec leg verified constructs idempotency_key="" deliberately (workflow_driver.py:4074 --
    ANCHOR CORRECTED 2026-07-31; :4078 is the `else:` four lines past it). The
    shipped LINEAR site constructs EffectFenceResumeState(idempotency_key=_fence_key) whenever
    isinstance(_fence_key, str) (workflow_driver.py:5456), which ADMITS "", and the orchestrator
    site is unconstrained the same way. Both real resume sites consult a fence resolution only when
    the key is TRUTHY, and compute_effect_fence_tree_wide_abort_present already filters ALL empty
    keys regardless of carrier -- so an empty key surfaced on either of those two shapes is the
    SAME drop-not-refuse livelock (the operator response is silently DROPPED, not refused) that the
    branch-carrier carve-out exists to foreclose, arriving one door over. The B-69 impl leg
    therefore conforms to the cleared enumeration EXACTLY (the two shapes carry their key field
    unconstrained) and files this rather than widening the carve-out, because adding keyless
    sibling source shapes for those two carriers would extend the cleared eight-shape enumeration
    -- an X-AL-3 design extension an impl leg must not make silently. TWO OF THIS SUMMARY''S OWN GROUNDS WERE FALSIFIED AT THE SPEC LEG (2026-07-31) AND ARE CORRECTED HERE IN PLACE; the DISPOSITION is unchanged, only its ground. (1) *the orchestrator site is unconstrained the same way* is FALSE: OrchestratorEffectFencePausedResumeState has exactly ONE construction site (workflow_driver.py:12392) whose guard chain at :12376-:12381 ends with `and _orch_fence_key`, a TRUTHINESS test that EXCLUDES "". That site CANNOT emit an empty key. The keyless orchestrator shape is nevertheless specified, on TYPE-TOTALITY grounds (the carrier type declares idempotency_key: str with no length constraint, and the projection is a total function over journaled records that outlive the capture-site code) -- so the shape is UNREACHABLE in production and its witness MUST be a constructed-snapshot test, never an e2e. (2) *Both real resume sites consult a fence resolution only when the key is TRUTHY* is TRUE of the fan-out per-branch dispatch sites AND of the ORCHESTRATOR consult (:12118 -- `if (_orch_fence_resume.idempotency_key and _ef_resume_ctx is not None)`, truthiness-gated the same way), and FALSE of the LINEAR consult ALONE (:4925-:4936), which calls _resolve_effect_fence_gated unconditionally; the drop-not-refuse harm there arrives instead through the runtime dispatcher''s RECOMPUTED-key match (runtime_tool_dispatcher.py:1042 vs the key composed at :989, never "") plus compute_effect_fence_tree_wide_abort_present''s empty-key filter (workflow_driver.py:3054). A THIRD fact this summary did not have: the branch carrier already reaches "" WITHOUT crash reconstruction, via the ordinary fan-out path''s defensive coercion at :9625/:9752/:14024/:14149.'
  close_out: 'Two separable dispositions, and the choice is a real one rather than a formality.
    (a) SPEC-SIDE: widen section 2.1 / section 14.14.9.2''s key-ABSENT treatment from "the
    crash-reconstruction carrier" to "any effect-fence source shape whose captured key is empty",
    adding a keyless sibling source shape for the LINEAR and orchestrator carriers -- the variant
    set stays at FOUR either way, only the SOURCE column widens, which is exactly the shape the two
    prior widenings at the spec leg took (rounds 1 and 2). (b) CAPTURE-SIDE: refuse an empty key at
    the LINEAR/orchestrator capture sites instead, so the read side never has to represent it --
    cheaper in type surface but capture-side and therefore hash-adjacent, which is why it is NOT
    the default recommendation. Note the exposure is currently DEFENSIVE-ONLY at both sites (the
    key is read by name off a runtime error, and no production path is known to yield ""), so this
    is a soundness gap rather than a live defect -- which is why it is registered rather than
    treated as a halt. SPEC LEG APPLIED 2026-07-31 -- disposition (a), SPEC-SIDE, at the B-100 spec leg (PR #1173). CP spec v1.112->v1.113 (ONE amendment site: section 2.1 key-ABSENT treatment generalized from a PROVENANCE scope to a STATE scope -- any effect-fence source shape whose captured key is EMPTY carries NO key field) + Runtime spec v1.108->v1.109 (identical amendment inside section 14.14.9.2) + CP plan v2.47->v2.48 (U-CP-64, ONE ADDED AC #A11; #A1-#A10 preserved verbatim) + Runtime plan v2.56->v2.57 (U-RT-148, AC #15(b) + #16(a) WIDENED; sixteen ACs stay sixteen). VARIANT set UNCHANGED at FOUR; effect-fence-addressable source shapes 4->6, union 8->10, distinct source CARRIERS UNCHANGED at SEVEN. ZERO new contract numbers, ZERO carriers retyped, ZERO fields added, ZERO snapshot_hash impact, ZERO new CXA rows (v2.23 UNCHANGED, aggregate frozen at 111), ZERO new units/clusters/DAG edges. Disposition (b) NOT applied, per this close-out. FOUR findings surfaced at the leg rather than absorbed. (1) THIS ROW''S GROUND FOR THE ORCHESTRATOR HALF IS FALSIFIED: OrchestratorEffectFencePausedResumeState has exactly ONE construction site (workflow_driver.py:12392) whose guard chain at :12376-:12381 ends with `and _orch_fence_key` -- a TRUTHINESS test excluding "". The keyless orchestrator shape is applied anyway on a CORRECTED ground (TYPE TOTALITY: the carrier type declares idempotency_key: str with no length constraint, and the projection is a total function over journaled records that outlive the capture-site code), and is UNREACHABLE in production -- its acceptance witness MUST be a constructed-snapshot test, never an e2e. (2) THIS ROW''S MECHANISM CLAIM is accurate for the fan-out per-branch dispatch sites AND for the ORCHESTRATOR consult (:12118, truthiness-gated the same way) and INACCURATE for the LINEAR consult ALONE (:4925-:4936), which calls _resolve_effect_fence_gated unconditionally; the real mechanism there is the runtime dispatcher applying a directive only when the RECOMPUTED per-(run, step, tool) key matches (runtime_tool_dispatcher.py:1042 against the key composed at :989), never "" -- plus compute_effect_fence_tree_wide_abort_present filtering all empty keys (workflow_driver.py:3054). Same DROP-not-refuse harm, different route. (3) The branch keyless shape was ALREADY under-scoped by its provenance naming: the ordinary fan-out fence-pause path coerces a missing key to "" at :9625/:9752/:14024/:14149, and the shipped projection already routes by EMPTINESS (pause_state_projection.py:621-:649) -- the generalization closes this as a side effect of the authorized fix. (4) A PRE-EXISTING count/table gap at CP section 2.1 / Runtime section 14.14.9.2 (seven rows for eight shapes; a stale "(3 shapes)" parenthetical), caused by a shape-vs-CARRIER conflation, refreshed here because the widening makes those numbers load-bearing. IMPL DELTA IS OWED AND IS NOT THIS LEG: walk_pause_tree constructs the LINEAR (pause_state_projection.py:509) and orchestrator (:522) projections UNCONDITIONALLY from the carrier key with no emptiness routing, so an empty key is surfaced TODAY as a "" key value -- the two new projection types plus the sibling of _effect_fence_branch_projection are owed at U-CP-64 (CP plan v2.48 AC #A11) and consumed at U-RT-148 (Runtime plan v2.57 AC #15(b)/#16(a)). THE ROW IS THEREFORE **NARROWED TO THAT IMPL REMAINDER AND FLIPPED registered_finding -> open**, NOT CLOSED -- the status enum at this file''s header defines `closed` as *built + merged, or foreclosed/superseded*, and neither holds while the two projection types and their emptiness routing are unbuilt. This follows the B-97 precedent exactly (its half (a) row stayed open through the v1.108 SPEC leg at PR #1168 and closed only at PR #1171 once U-RT-149 IMPL landed) and the B-102 precedent (*narrowed to that remainder rather than closed*). CLOSURE CRITERION FOR THE REMAINDER: B-100 closes iff (the LINEAR and orchestrator KEY-ABSENT projection types exist AND route by emptiness) AND (CP plan v2.48 AC #A11 (a)-(d) are witnessed green, with (b) as a CONSTRUCTED-SNAPSHOT test) AND (Runtime plan v2.57''s widened AC #15(b) + #16(a) are witnessed green). Partial is non-closure. A spec leg alone does not close this row. STATUS TRANSIT AT THIS LEG, stated so the snapshot bump is legible: registered_finding -> OPEN. The enum at this file''s header defines registered_finding as a gap that *opens for BUILD once grounding confirms a genuinely buildable slice*, and open as *actionable now, no blocker* -- the grounding is done and the slice is specified down to acceptance criteria (CP plan v2.48 AC #A11; Runtime plan v2.57''s widened AC #15(b) + #16(a)), so the row is actionable and must derive as such rather than await another grounding pass (out-of-family review round 9 [P2]).
    IMPL LEG LANDED 2026-08-01 (PR #1174) -- the remainder this row was NARROWED to at the spec leg.
    ONE file: harness-cp/src/harness_cp/pause_state_projection.py. TWO new source-shape members + TWO new
    projection types (KeyAbsentLinearEffectFenceAddressableLocation,
    KeyAbsentOrchestratorEffectFenceAddressableLocation) + TWO sibling routing helpers
    (_effect_fence_linear_projection, _effect_fence_orchestrator_projection) mirroring the shipped
    _effect_fence_branch_projection exactly, plus min_length=1 on the two key-BEARING shapes. Source shapes
    8 -> 10; VARIANT set UNCHANGED at FOUR; carriers UNCHANGED at SEVEN. ZERO capture-side change, ZERO
    carrier retyped, ZERO hash impact -- disposition (b) still NOT applied. CLOSURE CRITERION SATISFIED, per
    conjunct: (1) the LINEAR + orchestrator KEY-ABSENT projection types EXIST and ROUTE BY EMPTINESS; (2) CP
    plan v2.48 AC #A11 (a)-(d) witnessed green, with (b) a CONSTRUCTED-SNAPSHOT test as its witness
    constraint requires; (3) Runtime plan v2.57 AC #15(b) + #16(a) as widened witnessed green over ALL THREE
    carriers. NINE witnesses, EVERY ONE MUTATION-PROBED (revert the routing -> the named test fails; restore
    -> green): a11a, a11b, a11c1, a11c2, a11c3, a11d, the resolver-invariance guard, ac16a(ii), ac16a(iii).
    TWO probes are load-bearing beyond the obvious: narrowing PauseTreeWalkEntry.effect_fence_key to None
    FAILS the invariance guard (that field MUST keep surfacing "" -- suppressing it would silently change a
    CP CLASSIFICATION RULE, which is B-107 scope, not this row''s), and a key-bearing shape given a DEFAULT
    still passes (c2) while failing (c3), which is exactly why (c3) is not redundant with (c2). Tests 1859
    -> 1866 CP, 3557 -> 3559 Runtime; pyright 0/0/0; ruff clean. VERIFIED-AND-REPORTED, not fixed: the four
    ordinary-fan-out ""-coercion sites (workflow_driver.py:9625/:9752/:14024/:14149) DO route correctly at
    this head -- each writes "" into effect_fence_paused_dispositions, which reaches
    EffectFencePausedBranchResumeState(idempotency_key="") UNFILTERED at :10272 / :14631 (no truthiness
    guard on either path) and thence to the branch keyless shape, exactly as the spec leg predicted at
    finding (iii). B-107 NOT touched: this diff changes no resolution or refusal channel -- hitl_responses,
    _resolve_effect_fence_gated and compute_effect_fence_uniform_fallback_eligible_key are all
    byte-unchanged. STATUS TRANSIT: open -> CLOSED. The status enum defines closed as *built + merged*; the
    built conjunct is satisfied here and the merged conjunct at this PR''s merge -- recorded so the bar is
    legible rather than assumed.'
  council: 'no -- the tension was already convened and adjudicated at the B-69 record; this row
    only asks whether the SAME adjudicated rule reaches two more source carriers, which is a
    scoping question for a spec-writer apply pass, not a fresh cross-domain tension.'
  heading: '### B-100 · The empty-`idempotency_key` key-ABSENT carve-out does not reach the LINEAR
    and orchestrator fence carriers *(surfaced at the `B-69` impl leg, reported not absorbed,
    2026-07-31)*'
- id: B-101
  title: Accessor-derived resume-context provenance is LOST through base-typed Pydantic
    serialization, and closing it requires a ResumeContext spec change
  status: closed
  pr: '#1175'
  summary: 'SPEC LEG APPLIED 2026-08-01, DISPOSITION (b)-PLUS -- CP spec v1.113->v1.114, ONE
    amendment site: section 1.2 constraint 3''s stated residual is APPENDED TO with ONE paragraph -- NO TEXT
    retracted or reworded, though section 1.3(c)''s reach IS made explicit where v1.112 left it implicit (it
    binds the CP-OWNED affordances its own detect-then-refuse witness exercises -- no downgrade helper, frozen-
    carrier mutation refused, the accessor-derived variant''s OWN dump refused into the legacy type by
    extra=forbid -- and NOT generic base-schema serialization, which slips past that third guard because the base
    schema never emits the provenance field for extra=forbid to see). Under the BROADEST reading of section
    1.3(c) that is a NARROWING rather than a clarification; the counter-reading is recorded IN FULL at the
    delta''s section 0.5 finding (vi) -- raised as a [P1] at this leg''s out-of-family round 2 -- with the
    narrower reading''s three grounds and the ONE-STEP escalation route (disposition (a)) named, so a reviewer
    can overturn it rather than discover it. The paragraph (i) declares the ORDINARY-SERIALIZATION
    boundary formally OUT of the staleness fence''s scope, (ii) NAMES StepExecutionContext.resume_context
    (harness-cp/src/harness_cp/workflow_driver_types.py:460) as the ONE live base-typed carrier, (iii)
    RECORDS the three grounded facts that keep it unreachable at HEAD -- re-verified at that leg rather
    than carried from the 2026-07-31 note: ZERO model_dump/model_dump_json/TypeAdapter of that field OR
    of its enclosing StepExecutionContext across src AND tests (and StepExecutionContext is never a field
    of another model, so never transitively serialized), compose_branch_child_context''s model_copy is
    shallow and non-revalidating, and revalidate_instances is unset repository-wide so Pydantic''s never
    default holds -- and (iv) states the normative PROMOTION TRIGGER, keyed on MECHANISM rather than
    on the named carrier so a named-carrier reading cannot under-cover it: any future arc introducing ANY
    BASE-SCHEMA SERIALIZATION BOUNDARY over a ResumeContext value on a resume path -- a ResumeContext-typed
    model field (of which StepExecutionContext.resume_context is today the only live one), a base TypeAdapter
    or RootModel over ResumeContext (the route the pin test demonstrates directly at :960), a base-annotated
    parameter or return crossing a schema-generating boundary, or any equivalent -- MUST promote this row to
    disposition (a) (the closed variant discriminator on ResumeContext) and land it BEFORE or SIMULTANEOUSLY
    WITH that arc, never after. Naming the carrier makes the trigger checkable at today''s one live site; it
    does NOT bound it. Disposition (a) is
    NOT applied here: it breaks the field-set-preserved-verbatim guarantee CP spec v1.112 section 0.4
    makes, and this row''s own close_out conditions it on a serialization boundary appearing on a real
    resume path, which does not exist at HEAD. ZERO contract numbers, ZERO carriers retyped, ZERO fields
    added, ZERO hash impact, ZERO impl units owed, ZERO plan deltas owed, ZERO test changes owed --
    verified: B-101 is cited at exactly ONE place in design-substrate (Spec_Control_Plane_v1_113.md
    section 0.4 finding (vi), as the obstacle B-107 disposition (d) would hit) and at NO implementation
    plan. THE PIN TEST STAYS AND MUST NOT BE DELETED AT THIS CLOSE:
    test_provenance_is_lost_through_base_typed_serialization_registered_b101
    (harness-runtime/tests/test_paused_workflow_state_accessor_b69.py:924) is the executable half of the
    trigger -- it asserts the CURRENT lossy behavior deliberately so any change to it is reviewed. HONEST
    LIMIT, stated in the spec rather than papered over: trigger enforcement is a PRE-MERGE REVIEW
    OBLIGATION, not a validator rule, and the pin test breaks on a BEHAVIOR change but does NOT fire on
    the bare addition of a model_dump call. ORIGINAL FINDING: surfaced at the B-69 IMPL leg by out-of-family review round 3 [P1], reported not
    absorbed. AccessorDerivedResumeContext is a SUBCLASS of the CP-owned ResumeContext, and
    Pydantic v2 serializes a subclass through a base-typed field or TypeAdapter using the BASE
    schema -- so a round-trip through a ResumeContext-typed boundary emits only the four legacy
    fields and silently DROPS pause_state. Revalidating yields a plain ResumeContext, after which
    api.resume()''s variant check sees a legacy context and the section 30 staleness fence does not
    fire. NOT reachable on any shipped path today (api.resume takes the object directly and the
    in-process MCP handoff stores it without serializing), so this is a latent hazard rather than a
    live defect. It is an instance of the residual CP spec v1.112 section 1.2 already states on the
    record -- "a caller who takes an accessor read and then, independently, constructs a legacy
    ResumeContext is unfenced ... the type system cannot track a side effect that happened before
    construction" -- but it arrives through ORDINARY SERIALIZATION rather than a deliberate act,
    which is the sharper form and the reason it is filed. Pinned by
    test_provenance_is_lost_through_base_typed_serialization_registered_b101, which asserts the
    CURRENT behavior so the boundary is visible and any future change to it is deliberate. NEAREST-APPROACH VECTOR (scoped re-gate at the B-97(b) arc, 2026-07-31): ResumeContext is ALREADY a base-typed field of StepExecutionContext (workflow_driver_types.py:460). It is not dumped today -- only HandoffContext is model_dump_json''d, model_copy preserves object references rather than revalidating, and revalidate_instances defaults to never -- so the not-reachable-today claim STANDS. But the hazard is ONE model_dump away on an existing carrier, not a hypothetical future boundary, and disposition (b) (documenting the boundary as out of scope) would NOT close that vector.'
  close_out: 'DISCHARGED 2026-08-01 by the (b)-PLUS spec leg (CP spec v1.114 section 1, clearance
    marker .harness/clearance/spec-control-plane-v1-114-cleared-2026-08-01.md). Disposition (b) is
    DOCUMENTARY BY CONSTRUCTION -- it declares a boundary rather than moving one -- so unlike the
    B-100 / B-97(a) / B-102 precedent of NARROWING at a spec leg, this row CLOSES: those rows left
    unbuilt impl remainders and closing them would have presented unshipped work as done, whereas this
    close-out is fully discharged with no impl, plan or test remainder. The row is REOPENED, not
    re-litigated, at the promotion trigger. ORIGINAL CLOSE-OUT (preserved): CANNOT be closed from the
    subclass: the behavior is controlled by the BASE type''s
    Pydantic schema, and CP spec v1.112 requires ResumeContext''s field set be PRESERVED VERBATIM,
    so adding a variant discriminator to it is a SPEC change an impl leg must not make. Two
    dispositions, both spec-side: (a) amend CP spec section 1 so ResumeContext itself carries a
    closed variant discriminator and the two variants become an explicit discriminated union --
    strictly stronger, but it breaks the "field set preserved verbatim" guarantee section 0.4 makes
    and would need its own byte-compat argument for every existing construction site; (b) declare
    the serialization boundary formally OUT of the fence''s scope, extending section 1.2''s stated
    residual to name it, so the limit is documented at contract altitude rather than only in a
    test. (b) is the cheaper and more honest default given the residual is already on the record;
    (a) is warranted only if a serialization boundary ever appears on a real resume path.'
  council: 'no -- this is a scoping question about an already-adjudicated residual, for a
    spec-writer apply pass; no new cross-domain tension is present.'
  heading: '### B-101 · Accessor-derived provenance is lost through base-typed Pydantic
    serialization *(surfaced at the `B-69` impl leg, out-of-family review round 3, reported not
    absorbed, 2026-07-31; CLOSED 2026-08-01 — disposition (b)-PLUS applied at CP spec v1.114, with a
    normative PROMOTION TRIGGER to disposition (a) and the pin test retained as the boundary marker)*'
- id: B-102
  title: corrupt-latest AND absent are presented as decisive while the same cross-process
    reachability argument that qualifies empty-journal applies to both (spec half DISCHARGED
    at v1.108; read-side attribution flip LANDED at the B-97(a) impl leg)
  status: closed
  pr: '#1171'
  summary: 'NARROWED at the B-97(a) spec leg, 2026-07-31: THE SPEC HALF IS DISCHARGED, THE IMPL HALF IS THE
    LIVE REMAINDER. Runtime spec v1.108 amends section 30''s cause table so absent (:3318) and
    corrupt-latest (:3321) each carry the SAME reachability qualification empty-journal (:3319)
    already carried -- permanent for the single-process deployment where it is decisive,
    INDETERMINATE across processes, and an implementation MUST NOT present either as decisive loss
    where a second writer is reachable. All THREE permanent-absence causes are now qualified rather
    than one of three, and the RT-FAIL-RESUME-HANDLE-UNKNOWN taxonomy row plus section 14.14.9.4''s
    routing clause were both refreshed in the same edit so no surface flattens the table. This is
    disposition (a) of the row''s own close-out, and it needed no new cause member -- which is why
    it composed cleanly with B-97 half (a) rather than competing with it. ORIGINAL FINDING (surfaced
    at the B-97(b) arc, lens-1 F1, 2026-07-31; widened at that arc''s out-of-family round 2 to a
    second row): a torn trailing line is exactly what an in-flight append looks like from a reader,
    so a read landing mid-append attributes corrupt-latest to a record about to become well-formed;
    and a read of a workflow whose capture has been dispatched but has not yet created its journal
    file reports absent while that capture may complete moments later. The two differ from
    empty-journal only in WHERE the concurrent writer is caught, which is not a difference the
    operator-repair column can act on. B-97(b) SHRINKS the window but does not close it: a writer
    killed mid-append still leaves a torn tail, and a reader racing a single live writer is
    unaffected by a lock the reader deliberately does not take. NOT a live defect on the shipped
    single-process deployment.'
  close_out: 'SPEC HALF DISCHARGED at the B-97(a) spec leg (Runtime spec v1.108, section 30 cause table + the
    taxonomy row + section 14.14.9.4''s routing clause). IMPL HALF OUTSTANDING and it is the whole
    remainder: flip the SHIPPED read-side attribution to the indeterminate disposition for absent
    and corrupt-latest where a second writer is reachable, matching what empty-journal already
    reports (journal_workflow_pause_store.py hard-codes retryable=False, indeterminate=False for
    both today). That flip rides Implementation_Plan_Harness_Runtime_v2_56.md U-RT-149 AC #4(c) at
    the B-97(a) IMPL leg -- it is NOT independently scheduled, because both touch the same
    attribution site and two arcs editing one function is the duplication this register exists to
    prevent. The row is NARROWED rather than CLOSED deliberately: closing it while half its own
    close-out is unshipped would present unshipped work as done, and the contract and the code would
    disagree with nothing tracking the gap. Disposition (b) (document why they ARE decisive despite
    the symmetry) stays REJECTED -- nobody produced the asymmetry. No new cause member was needed
    and none was minted. IMPL HALF LANDED at the B-97(a) impl leg (U-RT-149 AC #4(c)), and the row
    now CLOSES: read_latest_attributed reports indeterminate=True for absent (a capture dispatched
    but not yet having created its file) and for corrupt-latest''s PARSE-failure branch (a torn tail
    is what an in-flight append looks like to a reader), UNCONDITIONALLY -- exactly as empty-journal
    already did -- because the store cannot know whether a second writer is reachable and the
    contract term is MUST-NOT-PRESENT-AS-DECISIVE. THE DECODE-FAILURE BRANCH IS DELIBERATELY NOT
    FLIPPED and stays unconditionally permanent: invalid bytes persist on disk, an append leaves
    them in place, and every re-read reproduces them, so routing it indeterminate would send the
    operator into exactly the futile re-read loop section 30''s cause refinement exists to prevent.
    read-error and workflow-mismatch are likewise untouched. Witnessed at
    test_pause_journal_tenant_keying_b97a.py (four causes, each asserted separately) and at the
    B-69 accessor suite, whose AC #8 decisive-cause assertion was re-pointed from corrupt-latest --
    no longer decisive by contract -- onto the decode branch and workflow-mismatch, preserving the
    assertion''s SHAPE while moving its instances.'
  council: 'no -- this asks whether an already-adjudicated qualification reaches one more row
    of the same table; it is a scoping question for a spec-writer apply pass, not a fresh
    cross-domain tension.'
  heading: '### B-102 · `corrupt-latest` is presented as decisive, but a torn in-flight append tail is cross-process reachable exactly as `empty-journal` is *(surfaced at the `B-97`(b) arc, lens-1 F1, 2026-07-31; spec half DISCHARGED at Runtime spec v1.108, 2026-07-31 — the read-side attribution flip rides the `B-97`(a) impl leg)*'
- id: B-103
  title: The B-97(b) pause-journal append lock is a blocking flock taken on the event loop
    (PREMISE FALSIFIED -- the shipped call path has no event loop to block)
  status: closed
  pr: '#1172'
  summary: '[SUPERSEDED 2026-08-04 -- the premise below was FALSIFIED at the PR #1172 merge gate: no production
    capture site runs on a live event loop (every one reaches the store through
    workflow_driver._run_protocol_method_sync -> asyncio.run on a private single-task loop in a to_thread
    worker), so the loop this row describes does not exist at any shipped call site; text preserved as the
    registration-time claim.] The B-97(b) cross-process append lock is a BLOCKING fcntl.flock syscall taken ON THE EVENT
    LOOP: JournalWorkflowPauseStore.capture() is called synchronously from the async
    capture_pause_snapshot, so a contended acquisition stalls the whole loop -- every other
    workflow, every pending timer -- for the duration of another process''s append. PRECEDENTED,
    which is why it is registered rather than argued: the sibling protected result store grew an
    off-loop resolution path for exactly this shape. SURFACED at the B-97(b) pre-merge gate as an
    out-of-family [P2] and DELIBERATELY NOT BUILT there; SPLIT to its own row at the B-97(a) spec
    leg, 2026-07-31, per that leg''s standing obligation -- parking it in B-97''s prose would have
    lost it from --open the moment that row closes, or falsely implied half (a) was unfinished if
    the row were held open for it. IT IS A MECHANISM / LATENCY QUESTION WITH NO KEYING CONTENT AND
    NO SPEC TEXT, which is precisely why it was excluded from the keying arc: folding it in would
    have mixed two unrelated review surfaces. SEVERITY, STATED HONESTLY: a durable pause is a
    human-latency event and the lock is held for one append, so the contention window is small and
    no live stall has been observed -- this is a soundness/latency gap, not a live defect.'
  close_out: '[SUPERSEDED 2026-08-04 -- this directive was BUILT, then DECLINED at the PR #1172 merge gate and
    STRIPPED (premise falsified: the rescued loop does not exist at any shipped call site); retained as history,
    NOT operative guidance. Reopening trigger: a captured-loop bridge replacing the private-loop asyncio.run --
    see the closing record at the end of this field.] Move the blocking acquisition off the event loop, on the sibling''s own precedent (an
    asyncio.to_thread-shaped offload of the locked append, or an equivalent off-loop resolution
    path), and witness it by EXECUTION: an unrelated concurrent workflow and a pending loop timer
    demonstrably advance while a second process holds the journal lock -- with the mutation probe
    being the direct synchronous call, under which that witness must fail. TWO THINGS THIS ROW MUST
    NOT DO: it must not change the LOCK''S GRANULARITY (per-workflow was grounded and defended at
    the B-97(b) arc against a directory-tree alternative, and re-opening it here would relitigate a
    settled decision), and it must not change the READ path, which deliberately takes no lock --
    that no-lock property is what keeps Runtime spec section 30''s empty-journal INDETERMINATE
    clause exact as written. NO SPEC TEXT IS EXPECTED TO BE OWED: the offload is an execution-venue
    mechanism, and section 14.8.1''s wrap-asymmetry lineage already covers the class -- but confirm
    that before building, and file a fork if it turns out otherwise rather than absorbing it.
    DATED RE-CHECK 2026-07-31 (B-97(a) impl leg, touched-surface overlap): the leg REFACTORED the
    lock -- _cross_process_append_lock now DELEGATES to a new module-level
    cross_process_journal_lock, promoted so the (3b) adoption holds the same lock of the same
    construction on the same inode rather than a second copy -- and a public
    journal_exclusion_is_degraded() predicate was added for the refuse-where-degraded term. NEITHER
    CHANGES THIS ROW: the primitive is byte-identical (same fcntl.flock, same dedicated lock file,
    same per-workflow granularity, same Windows no-op), the acquisition is still BLOCKING and still
    taken synchronously from capture(), and the READ path still takes no lock. The two things this
    row forbids -- changing the lock''s GRANULARITY and changing the READ path -- were both
    preserved. STILL OPEN, unchanged in substance. CLOSED 2026-07-31 AT PR #1172 -- PREMISE
    FALSIFIED, NOT A DELIVERED FIX. MECHANISM CLAIM RETRACTED AND REWRITTEN IN PLACE 2026-08-05:
    this stretch previously recorded that DurablePauseResumeProtocol.capture_pause_snapshot
    dispatches capture() via run_audit_off_loop (lifecycle/audit_offload.py) instead of calling it
    inline. IT DOES NOT, AND NEVER DID ON MAIN -- that offload was built and witnessed, then
    DECLINED at the merge gate and STRIPPED before merge, so what landed is PURE SUBTRACTION. At
    HEAD 43d4dda5, re-verified by direct read, capture_pause_snapshot still calls
    self._store.capture(snapshot) INLINE
    (harness-runtime/src/harness_runtime/lifecycle/durable_pause_resume_protocol.py:124) and the
    pause-capture path carries ZERO run_audit_off_loop references -- the helper exists at
    lifecycle/audit_offload.py:256; the PAUSE-CAPTURE path has ZERO references to it (live consumers on OTHER paths at 2026-08-05: hitl_gate_composer.py:1363, llm_dispatch.py:5107, cost_attribution_validator_dispatch.py:419, webhook_delivery_composer.py:390, runtime_tool_dispatcher.py:590). The only src
    delta versus main is that module docstring, which carries the premise-falsified account. WHAT
    FOLLOWS TO THE END OF THIS PARAGRAPH IS THE HISTORY OF THE DECLINED, UNMERGED DESIGN, NOT A
    DESCRIPTION OF SHIPPED BEHAVIOUR -- PRIMITIVE CHOICE,
    on the source and not on the analogy: asyncio.to_thread was REJECTED (it queues onto the loop''s
    DEFAULT executor, where the CP driver''s sync execute_workflow already blocks awaiting loop
    coroutines -- the exhaustion deadlock that module exists to avoid), and
    run_off_loop_detach_on_cancel was REJECTED because it is for effects with NO durability
    requirement (shutdown''s opportunistic GC sweep); a pause capture''s whole contract is
    durable-before-return, so the JOIN-on-cancel variant is the fitting one -- the same property
    resolve_result_ref_off_loop already relies on. WITNESSED BY EXECUTION at
    the then-branch harness-runtime/tests/test_pause_capture_off_loop_b103.py (11 tests; NOT
    PRESENT AT HEAD -- stripped with the offload, absence re-verified 2026-08-05): an unrelated coroutine
    and a pending loop timer both advanced while a genuine second OS process held the lock, and the
    mutation probe WAS BUILT AS A PERMANENT ARM rather than a prose claim -- the direct synchronous
    call was parametrized alongside the offload path with INVERTED expectations, and reproduced loop
    starvation exactly (sample latency 2.98s vs the 0.6s sample window; 1 unrelated-coroutine tick
    vs ~30; timer 2.98s late). Cancellation is witnessed against a detach-on-cancel mutant the same
    way. NEITHER FORBIDDEN THING MOVED: granularity is pinned by a same-target/same-inode witness,
    and the read path is pinned lockless AND still synchronous. NO SPEC TEXT OWED, confirmed by
    direct read before building: Runtime spec v1.108 section 14.8.1''s v1.102 paragraph states the
    governing principle verbatim -- "The wrap chain above pins composition, not execution venue" --
    and selects an off-loop venue for a blocking sync inner with no contract-surface change. This
    arc is the same class; no config scalar, failure class, record shape or observable surface
    moves, so no fork was owed and none was filed. THAT SPEC READING STANDS -- but it answered the
    wrong question, and the row is CLOSED ON A FALSIFIED PREMISE, NOT ON A DELIVERED FIX. END OF
    THE DECLINED-DESIGN HISTORY.
    FALSE PREMISE, ESTABLISHED AT SOURCE AT THE MERGE GATE (lens 1, 2026-07-31, independently
    re-verified by the orchestrator and again by this session against
    harness-cp/src/harness_cp/workflow_driver.py): THERE IS NO EVENT LOOP TO STARVE AT ANY SHIPPED
    CALL SITE. Every production capture_pause_snapshot invocation -- all ELEVEN of them, each a
    protocol.capture_pause_snapshot(...) wrapped in _run_protocol_method_sync(...) -- reaches the
    protocol through _run_protocol_method_sync (:2583-2603), which runs the coroutine to completion
    via asyncio.run(coro) on a PRIVATE, SINGLE-TASK loop constructed for that one call, inside the
    driver''s own asyncio.to_thread worker thread. That helper''s own docstring states the
    condition verbatim: "The workflow_driver runs in a worker thread spawned by asyncio.to_thread
    from harness_runtime.api.run -- no current event loop is bound on the worker thread. asyncio.run
    constructs a new loop for this single coroutine." So the blocking flock blocks a thread that is
    ALREADY blocked awaiting that very coroutine, and there is no co-resident coroutine or pending
    timer on that private loop to starve. THE OFFLOAD WAS BUILT, GATED, AND DECLINED. It is
    preserved at this branch''s pre-rework commits (head 3636762c-era, unmerged): a dedicated
    executor plus a join-on-cancel pause-journal helper, with a 12-test execution witness. Measured
    against production, it would have made things WORSE -- blocking a SECOND thread (the offload
    worker) in addition to the already-blocked driver worker, and adding a cancellation window in
    which an append could land after the caller observed CancelledError (itself registered as B-105
    and now WITHDRAWN with the offload) -- for ZERO recovered concurrency. The witnesses were sound
    for the shape they tested and reproduced loop starvation exactly; that shape is one production
    never uses (a capture co-resident with a live main loop). REOPENING TRIGGER, THE REASON THIS IS
    CLOSED RATHER THAN DELETED: if a future arc replaces _run_protocol_method_sync''s asyncio.run
    with a captured-loop bridge -- the evolution that helper''s OWN docstring anticipates at
    :2598-2601 ("the sync-bridging mechanism is impl-discretion. The MVP uses asyncio.run; future
    arcs may substitute a SyncDispatcherFacade-style captured-loop bridge if the protocol body ever
    introduces real async I/O") -- then the capture becomes genuinely loop-co-resident, this row''s
    premise goes LIVE, and the preserved offload shape at the pre-rework commits applies directly.
    Re-check this row at any change to that helper.'
  council: 'no -- a single-axis execution-venue question with no nameable cross-domain tension. The
    granularity decision that WOULD have carried one is already settled and is explicitly out of
    this row''s scope; route to C9 (reliability/recovery) directly plus advisor() if the offload
    shape turns out to interact with drain or shutdown ordering.'
  heading: '### B-103 · The `B-97`(b) cross-process append `flock` is a BLOCKING syscall taken on the event loop *(surfaced at the `B-97`(b) pre-merge gate as an out-of-family [P2], 2026-07-31; split to its own row at the `B-97`(a) spec leg, 2026-07-31)*'
- id: B-104
  title: The pause journal has no pause-resolved marker, so an outstanding pause is not
    distinguishable from a resolved one
  notes: 'O-3 (lens-1, B-97(b) arc, 2026-07-31 -- recorded here because this is the section 13.7
    enumeration row and it was recoverable from no durable surface): LINE-COUNT SEMANTICS DIFFER
    ACROSS SURFACES -- str.splitlines() splits on \x0b, \x85 and \u2028 while bytes.splitlines()
    does not, so two surfaces counting "records" over the same journal can disagree. UNREACHABLE
    for store-written records, and the LOAD-BEARING PROPERTY IS NAMED PRECISELY because it is an
    editable default rather than a law: `_append` serializes with `json.dumps(record, sort_keys=True)`,
    which leaves `ensure_ascii` at its default TRUE, so every non-ASCII character -- including
    \x85 and \u2028 -- is emitted as an escaped `\uXXXX` sequence and `\x0b` as `\u000b`. Say
    "ensure_ascii=True", NOT "json.dumps escapes control characters": SEVENTEEN other modules in
    this tree pass `ensure_ascii=False` explicitly (counted programmatically across all seven axis
    packages at this arc -- the merge-gate lens said "seven", the empirical count at HEAD is 17),
    so the vaguer claim would silently falsify the moment someone added that argument here. Recorded as a bound on
    the enumeration''s arithmetic under HAND-EDITED or foreign-written journals, not as a live
    defect.'
  status: registered_finding
  pr: '#1181 (the Class 2 fork filing) + #1182 (the ratified-defer leg + Runtime v1.110 Component 1) + #1233 (the 2026-08-05 batch re-affirmation)'
  summary: 'The pause journal is append-only and NEVER TRUNCATED (Runtime spec section 14.14.8''s v1.107
    substrate invariant), and NOTHING is written when a resume succeeds -- there is no
    pause-resolved marker, no status field, and no removal path anywhere in the store. A journal
    whose pause was successfully resumed is therefore BYTE-INDISTINGUISHABLE from one whose pause is
    still outstanding. CONSEQUENCE, and why it surfaced now: Runtime spec v1.108 section 13.7
    declares the operator enumeration surface that makes (3a) abandon-by-default ratifiable, and the
    council condition K-10(c) asks that surface to report ''how much would be LOST''. It cannot. It
    can only bound the at-risk set FROM ABOVE. The spec leg applied K-10''s mechanism unchanged and
    scoped its claim honestly at section 13.7 term 3-bis -- the enumeration is an upper bound, it
    cannot discriminate outstanding from resolved, and it MUST NOT be cited as proof a cutover is
    safe. SURFACED at the B-97(a) spec leg, out-of-family review round 1 [P1], 2026-07-31; reported
    not absorbed. SEVERITY, STATED HONESTLY: not a live defect -- nothing miscomputes and nothing is
    lost that would otherwise survive; it is a CEILING on what the operator recourse can tell a
    human, and it makes ''drain pauses before upgrading'' a procedure the operator must verify by
    their own means rather than read off the harness.'
  close_out: 'RE-AFFIRMED 2026-08-05 in the operator ratification batch: READING D (DECLARE +
    DEFER) stands, unchanged, with D-0 (a retention/pruning-policy arc) dominant. NO NEW WORK
    IS OWED -- Component 1, the MANDATORY symmetric declaration, ALREADY LANDED at Runtime spec
    v1.109 -> v1.110 (PR #1182, clearance marker `.harness/clearance/spec-harness-runtime-v1-110-cleared-2026-08-01.md`),
    and Component 2, the discriminator, stays DEFERRED under D-0..D-3; the row''s `pr:` pointer,
    which still read `#pending` for that leg, is REPAIRED here to name #1182. STATUS stays `registered_finding`.
    Readings A/B/C remain NOT selected; the runner-up A remains registered-and-held and is NOT
    advanced by this re-affirmation. The discriminator is CAPTURE-SIDE and therefore fork-shaped,
    which is exactly why it was not absorbed: any resolved-marker is a write the store does not
    make today, on a record shape whose hash disposition is load-bearing (the B-73/B-80 split-out
    discipline that kept the tenant binding itself out of the read-only B-69 arc). TWO CANDIDATE
    SHAPES, neither chosen here: (a) a RESOLVED SENTINEL RECORD appended at successful resume
    -- cheapest against the append-only invariant, since an append rewrites and truncates nothing
    and the latest-record read would then report the journal as resolved; it needs a decision
    on whether that sentinel is a PauseSnapshot at all, and on what the five-member cause vocabulary
    reports for a resolved journal, which is a closed-vocabulary question an impl leg must not
    settle. (b) an out-of-band per-journal marker FILE -- avoids the record shape entirely but
    adds a second authority over one journal''s state, the exact defect the no-fallback-read rule
    exists to prevent. PREFER (a) IF TAKEN, but neither is recommended without grounding: check
    FIRST whether an operator who has the section 13.7 upper bound plus their own drain procedure
    actually needs the discrimination, because a marker bought for a need nobody has is a capture-side
    change against a hash-adjacent record for nothing. DO NOT bundle this into the B-97(a) impl
    leg -- it is capture-side and that leg is not. DATED RE-CHECK 2026-07-31 (B-97(a) impl leg):
    NOT bundled, as instructed -- no capture-side marker was added and the record shape is byte-unchanged.
    What the leg DID ship is the honest SCOPING of the ceiling this row names: the section 13.7
    surface emits an explicit upper-bound disclaimer in BOTH its human and --json renderings (admin/pause_journal_enumeration.py
    UPPER_BOUND_DISCLAIMER), stating in its own output that it is not an outstanding-pause inventory,
    that a resolved journal is byte-indistinguishable from an outstanding one, and that it MUST
    NOT be cited as proof a cutover is safe -- witnessed at test_pause_journal_enumeration_b97a.py
    AC #7(d-bis). The row''s ceiling is therefore now VISIBLE to the operator rather than latent,
    which does not close it: the discrimination itself remains absent and capture-side. STILL
    OPEN, unchanged in substance. RATIFIED 2026-08-01 (operator AskUserQuestion) as READING D
    -- DECLARE the resolved-ambiguity limit SYMMETRICALLY (Component 1, MANDATORY), then DEFER
    the discriminator. The fork doc .harness/class_2_fork_b104_pause_resolved_marker.md is FILED
    (#1181) and its section 11 RATIFICATION addendum records the outcome verbatim-in-substance.
    Readings A, B and C were NOT selected and are NOT partially adopted; runner-up A is REGISTERED-AND-HELD
    at the fork''s section 6 and is NOT advanced by this ratification. COMPONENT 1 -- DISCHARGED
    AT THIS LEG''S SPEC DELTA. Runtime spec v1.109 -> v1.110, TWO amendment sites, both pure DECLARATIONS
    of an EXISTING limit and symmetric with section 13.7 term 3-bis: a new closing paragraph at
    section 14.14.9.1 (the accessor) and a NEW invariant bullet in section 30 immediately after
    "Durable-handle read (v1.46)". Both state that the store is append-only, never truncated and
    writes NO pause-resolved marker, so a resolved journal is BYTE-INDISTINGUISHABLE from an outstanding
    one and the read reports the LATEST DURABLE RECORD, which is NOT a liveness claim -- and that
    the limit holds IN STEADY STATE for a current-format journal, NOT merely for the pre-v1.108
    legacy set (the fork''s section 3(vii) scope correction). The section 30 bullet additionally
    states what section 14.14.9.1 does not need: resume()''s shipped guards validate INTEGRITY
    AND APPLICABILITY, never OUTSTANDINGNESS, and the section 30 staleness precondition fences
    a composed claim against a CHANGED record -- so a re-resume of an UNCHANGED, already-resolved
    record presents no mismatch and IS ADMITTED. The MANDATORY docstring companions land on both
    public surfaces (harness-runtime/src/harness_runtime/api.py -- read_ paused_workflow_state:925,
    and resume() at :1190 POST-EDIT on BOTH its durable-handle prose and its separately-consulted
    resume_handle parameter entry at :1260 POST-EDIT), ZERO logic changed. ANCHORS ARE STATED
    POST-EDIT deliberately: this leg''s own docstrings shift api.py''s lower half, so the fork''s
    pre-leg :1170 / :1197 / :1223 / :1321 cites do not resolve in the landed tree (caught at out-of-family
    review round 2 [P2]). THE LIMIT IS DECLARED, NOT FIXED: it closes the DISCLOSURE half of section
    3(vii); it does NOT close the blindness. ZERO contract numbers minted (a doc-only leg cannot),
    ZERO carriers retyped, ZERO fields added, ZERO snapshot_hash impact, the five-member PauseJournalReadCause
    vocabulary stays CLOSED at five, ZERO plan units owed at the Runtime, OD or CP plans, and
    ZERO CXA rows (aggregate frozen at 111 -- this leg introduces no cross-package consumption
    at all). VERIFIED, not assumed: B-104 is cited at NO implementation plan and at NO acceptance
    criterion, so no plan cite contradicts the deferral. Clearance marker .harness/clearance/spec-harness-runtime-v1-110-cleared-2026-08-01.md.
    COMPONENT 2 -- THE DISCRIMINATOR IS DEFERRED, status stays registered_finding, and the reopening
    triggers are a FALSIFIABLE four-disjunct demand test, any one disjunct fires: D-0 (DOMINANT)
    -- A RETENTION / PRUNING POLICY ARC OPENS. Section 14.14.8 declares a retention policy a FOLLOW-ON
    ARC explicitly not owed there (pause_journal_disposal.py lines 14-17). A PRUNER IS UNBUILDABLE
    WITHOUT THIS DISCRIMINATOR: it cannot be permitted to reclaim an outstanding pause, and format-keying
    gives it nothing, since post-cutover every journal is CURRENT_FORMAT. This is the one condition
    under which the absence becomes a HARD BLOCKER rather than a ceiling, which is why it dominates
    the other three. D-1 -- ANY SURFACE BRANCHES ON LIVENESS, OR A STALE RE-RESUME IS OBSERVED
    (round-2 re-scoped form; the first form -- "a second flow acts on the distinction for a current-format
    journal" -- was ALREADY SATISFIED AT HEAD by section 3(vii) and would have been vacuous on
    the day it was written). Two disjuncts: (a) any code path makes a CONTROL-FLOW decision conditioned
    on whether a durable pause is outstanding, as opposed to merely reading the latest record
    and being blind to it (which is HEAD''s shipped behaviour and is what Component 1 declares);
    or (b) an operator-loop report of a resume(resume_handle=...) re-entering an ALREADY-RESOLVED
    snapshot in a fresh process. The HARM class, not the asymmetry -- the asymmetry is discharged
    by Component 1. D-2 -- A SECOND KEYING CHANGE IS OPENED. Section 14.14.8 contemplates one
    ("A future encoding change is a KEYING change and MUST take this arc''s fork path"); it would
    re-run the whole (3a)/(3b) cutover over an accumulated directory holding one journal per workflow
    that ever paused, where the upper bound approaches vacuity. D-3 -- THE RESUMER CEASES TO BE
    THE PARTY PERFORMING THE CUTOVER. The workspace advances past solo-developer on the committed
    bridging arc (root CLAUDE.md section 10.2), so the operator confirming a drain is not the
    party who ran the resume() calls and cannot supply the confirmation out-of-band. Same shape
    as B-98''s D-1(b), and the same premise. EXPLICITLY NOT A TRIGGER ON ITS OWN: the section
    13.7 listing merely being loose. That is this row''s already-disclosed, already-contract-stated
    condition (term 3-bis) and is what the UPPER_BOUND_DISCLAIMER exists to communicate. GROUNDING
    CARRIED BY THE RATIFICATION (fork section 3, absorbed here): (a) the hypothesized derive-from-existing
    option is FALSIFIED -- no resume-specific record carries a re-derivable identity, and no identity-bearing
    record is tenant-qualified; the near-miss is Reading C, not an unexplored possibility. (b)
    Sub-decision (a)-1 is FORCED, NOT OPEN: a PauseSnapshot sentinel is RESUMABLE, so the only
    non-defective sentinel form is a WRAPPER-level record. (c) Sub-decision (a)-2 is CONFIRMED
    GENUINE and its no-decision default is WORSE than unspecified -- corrupt-latest with indeterminate=True,
    an in-flight-torn-append diagnosis for a cleanly resolved journal (empirically probed); a
    SIXTH cause member is MANDATORY under Reading A. (d) The second-authority objection to shape
    (b) DOES NOT HOLD AS WRITTEN: the journal is not a first authority over a question it cannot
    answer, and the divergence hazard is common to all three build readings. (e) The ambiguous-and-consequential
    set is FROZEN, ONE-TIME AND DRAINABLE -- every consumer is LEGACY-scoped and no path creates
    a legacy journal post-cutover. (f) NOT-IN-THE-ROW CROSS-AXIS COST: the sixth cause member
    FORCES an OD spec + plan leg, because harness-od re-declares the vocabulary independently
    (pause_resume_namespace.py lines 482-498) and api.py:1183 (POST-EDIT anchor) converts BY VALUE,
    so a Runtime-only member breaks the section C-OD-30.5 accessor emission -- this is what makes
    A more expensive than B and inverted the filing''s runner-up. (g) NOT-IN-THE-ROW OCCURRENCE-KEY
    REQUIREMENT common to all three build readings: workflow-keyed resolution admits a FALSE ALL-CLEAR
    on re-pause; the composition is NOT open -- it is pinned to the shipped section 30 staleness
    token''s (record_count, latest_record_digest), the digest alone being insufficient. (h) Any
    build reading MAY re-key disposal''s acknowledgement gate from unconditional to conditional
    -- AN EXPLICIT DECISION EACH OWES, NOT an automatic consequence and NOT an argument for deferral.
    PR #1170''S SCOPE, RECORDED CORRECTLY: it was a STRINGS-ONLY self-description refresh (78/16,
    zero logic) that removed a PHANTOM blocker on the drain recourse and left this one untouched
    -- this row must NOT be read as having been narrowed by it. ORDERING RECORDED, NOT ACTED ON:
    the cheapest actionable operator-loop improvement is in NONE of the four readings -- the drain
    recourse has NO resume ADMIN-CLI command (the admin surface ships seven commands, none of
    them resume), so an operator driving pauses to resolution must call the PUBLIC PACKAGE-ROOT
    PYTHON API harness_runtime.resume() from their own code. PRECISION, corrected at out-of-family
    review round 4 [P2]: resume() IS a SUPPORTED surface -- it is one of the THREE package-root
    async functions B-106''s own ruling enumerates as the operator-facing API (run / resume /
    read_paused_workflow_state). What B-106 ruled NOT SUPPORTED is the distinct EMBEDDER path
    -- direct run_bootstrap() / ctx.pause_resume_protocol access -- so the gap here is an ABSENT
    CLI AFFORDANCE, not an absent capability, and an earlier draft of this line conflated the
    two. This ratification does NOT open that CLI surface.'
  council: 'NARROWED at the 2026-08-01 Reading D ratification to the fork''s section 7 per-reading
    form, replacing the prior "convene only if shape (a)". Under the RATIFIED Reading D: NONE is
    owed now -- Component 1 is a pure declaration and a documentation close needs no convening --
    and ONE IS OWED AT D-0, the retention/pruning-policy trigger. If a build reading is later
    opened: Reading A -> a dyadic C3 vs C11 SCOPED to the sixth cause member plus the disposal
    brake; Reading B -> C11 plus advisor(), no convening; Reading C -> a convening PLUS C7/C8
    (OD-owned schema + tenant/disclosure). The tension itself is unchanged and nameable: C3
    (state/persistence integrity, wanting the record shape and the append-only invariant left alone)
    vs C11 (operator loop, wanting an answerable ''is anything actually outstanding''), the same
    pair the B-97(a) dyadic convened.'
  heading: '### B-104 · The pause journal writes no pause-RESOLVED marker, so the §13.7 enumeration can only bound the at-risk set from ABOVE *(surfaced at the `B-97`(a) spec leg, out-of-family review round 1 [P1], reported not absorbed, 2026-07-31; Class 2 fork FILED 2026-08-01 at PR #1181; RATIFIED 2026-08-01 as READING D — DECLARE + DEFER: Component 1 DISCHARGED at Runtime spec v1.110, the discriminator DEFERRED with the four-disjunct demand test D-0…D-3, D-0 dominant)*'
- id: B-107
  title: No channel REFUSES an operator resolution addressed to an EMPTY effect-fence key --
    the response map and the scalar uniform fallback DROP it on all three carriers, and the
    level-local abort scans MIS-APPLY it to a keyed sibling
  status: closed
  pr: '#1214'
  summary: 'RATIFIED 2026-08-03: Reading A-hybrid is selected. The design-substrate gate is discharged; B-107 remains OPEN only for the separately owed U-CP-64 implementation, b80 update, eight-cell witness grid, and PD-8 probes. Scalar membership removes empty keys; the map refuses them at ordinary construction via a validated immutable copy; resolver-boundary removal makes validation-bypassed empty keys inert. B-101(a) is NOT jointly discharged -- this map-field change pays the same compatibility-cost category but does not add B-101(a)''s closed variant discriminator; B-101 remains separately closed under (b)-PLUS with its promotion trigger unchanged, and the rows remain separate. ID NOTE: this row is B-107, NOT B-105 -- B-105 was minted and WITHDRAWN at PR #1172
    (the B-103 premise-falsified rework; see .harness/roadmap_status.md and
    .harness/merge-gate-log.md row #1172), -- reusing it would make register lookup and the historical record resolve to two different issues; caught at out-of-family review round 5 [P1]) and NOT B-106 (minted CONCURRENTLY on main at commit d9f68b02 for the embedder-path event-loop exposure, while this leg was in flight; renumbered at the merge rather than contested, since that row landed first).
    SURFACED at the B-100 SPEC leg by out-of-family review rounds 3 and 8, 2026-07-31, and REPORTED
    not absorbed -- it is ADJACENT to that leg''s authorized disposition (a), not part of it, and its
    SCOPE was corrected twice at review before filing. THE RESIDUAL: an EMPTY captured
    idempotency_key is reachable on the durable effect-fence carriers, and NO channel REFUSES an
    operator resolution addressed to it. TWO FAILURE MODES, not one -- the second ESTABLISHED at
    the 2026-08-01 grounding pass (see close_out): most routes DROP it silently, and the
    level-local _any_fence_abort scans MIS-APPLY it, since a {"": ABORT} suppresses a KEYED
    sibling''s valid directive at :8238 / :12576 -- the cross-location misattribution CP spec
    v1.107 section 1.1(a) forbids. TWO CHANNELS. (1) THE MAP
    CHANNEL. B-100''s projection change removes the ADVERTISEMENT -- an operator composing from the
    projection has no key to paste -- but it does NOT make the state unrepresentable:
    ResumeContext.effect_fence_resolutions is a dict[str, EffectFenceResolution] with UNCONSTRAINED
    keys, so a caller acting outside the projection can hand-build {"": ...} (directly or through
    AccessorDerivedResumeContext), and _resolve_effect_fence_gated treats "" as a mapped resolution.
    A read-side projection contract cannot close this; only a key constraint on the CP-owned map, or
    a refusal at the consult seam, can. (2) THE SCALAR CHANNEL.
    compute_effect_fence_uniform_fallback_eligible_key (workflow_driver.py:2935-2970) does NOT filter
    empty keys -- unlike its sibling compute_effect_fence_tree_wide_abort_present, whose
    candidate_keys comprehension at :3054 does -- so when a key-absent location is the SOLE
    unaddressed effect-fence location, that function returns "" as the eligible key and
    _resolve_effect_fence_gated accepts the caller''s scalar
    resume_context.effect_fence_resolution for it. THE TERMINAL LOSS DIFFERS BY CARRIER, AND BOTH
    SHAPES ARE IN SCOPE (scope corrected at round 8 [P2]; an earlier draft was LINEAR-only): at the
    LINEAR consult (:4925-:4936) the resolution IS looked up and threaded as
    EffectFenceResolutionDirective(idempotency_key=""), which the runtime dispatcher then never
    matches (runtime_tool_dispatcher.py:1042 vs the key composed at :989, never ""); at the FAN-OUT
    PER-BRANCH and ORCHESTRATOR consults the truthiness guard means the resolution is never consulted
    at all (SCOPE-CORRECTED 2026-08-01: true of those per-branch/orchestrator DELIVERY consults ONLY --
    the level-local _any_fence_abort scans at :7839 / :11708 DO consult the BRANCH carrier with an
    empty key, UNGUARDED; see the CORRECTED SEVEN-SITE MAP at this row''s close_out). Different mechanism, IDENTICAL outcome -- a silent INERT re-pause with no diagnostic.
    REACHABILITY, per carrier: the BRANCH carrier reaches "" DELIBERATELY at crash reconstruction
    (:4074) and defensively through the ordinary fan-out coercion (:9625/:9752/:14024/:14149) -- a
    PRODUCTION shape, not a defensive-only one; the LINEAR carrier reaches it defensively (:5456);
    the ORCHESTRATOR carrier does NOT reach it at all (truthiness-guarded at :12376-:12381), so its
    exposure is type-level only. IT IS NOT A DEFECT IN THE B-100 LEG: read-side projection scoping is
    the whole of what disposition (a) authorizes, and a projection can constrain neither a scalar
    field nor a map the caller populates independently of it.'
  close_out: 'CLOSED 2026-08-04 -- U-CP-64 AC #A12 IMPLEMENTATION LANDED; BOTH X-AL-2 CONJUNCTS SATISFIED. (1) ACs LANDED. Three amendment sites, all in harness-cp, matching CP spec v1.115 sections 1.1-1.5 and CP plan v2.49 section 1.1 exactly: SCALAR (1.1) -- compute_effect_fence_uniform_fallback_eligible_key filters empty captured keys out of the candidate set BEFORE the sole-unaddressed computation, so the uniform fallback can never nominate the empty key; the filter now matches the one compute_effect_fence_tree_wide_abort_present already shipped, so the two agree by construction instead of the downstream sibling compensating. MAP (1.2) -- ResumeContext.effect_fence_resolutions refuses empty keys at ordinary construction and stores a validated immutable COPY via a new _ImmutableEffectFenceResolutions dict subclass (every mutation method refused, slots-based one-shot seal, explicit __reduce__), inherited by AccessorDerivedResumeContext through BOTH its construction entry points. The declared field type stays dict[str, EffectFenceResolution] and dumps stay byte-identical in both modes -- section 0.3 compatibility preserved. RESOLVER (1.3) -- _resolve_effect_fence_gated refuses an empty idempotency_key BEFORE the map-hit check and BEFORE the uniform-eligibility comparison, which makes a caller-supplied effect_fence_uniform_fallback_eligible_key of empty string powerless and forged (model_construct) map content inert per section 1.5, with no diagnostic owed for the forged object. ZERO capture-side change, ZERO snapshot_hash impact, ZERO contract numbers, ZERO Runtime or CXA delta; PauseTreeWalkEntry.effect_fence_key still surfaces the empty key for key-absent shapes; B-101 untouched. (2) WITNESSES GREEN. New module harness-cp/tests/test_workflow_driver_effect_fence_empty_key_b107.py (29 tests) carries all five AC #A12 clauses: the EIGHT-CELL GRID (map + scalar channels at LINEAR, PARALLELIZATION branch, ORCHESTRATOR_WORKERS worker, ORCHESTRATOR-own; branch and worker cells in BOTH carrier orders via real captured pauses; the two ORCHESTRATOR-own cells are constructed-snapshot classification tests and are labelled in-test as NOT section 1.3 closure evidence, because the shipped orchestrator consult is truthiness-gated -- section 1.3 discrimination binds at the LINEAR site and the two ungated level-local _any_fence_abort branch scans); the SCALAR removal witnesses plus the direct caller-supplied empty eligible-key case; FOUR PD-8 map probes (validation / copy / immutability / serialization-and-clone compatibility); model_construct inertness; and the PROGRAMMATIC reader inventory, re-derived at run time -- SEVEN resolver call sites (compute_effect_fence_tree_wide_abort_present x1, _execute_workflow_body x1, _execute_parallelization x2, _execute_orchestrator_workers x3) and TWO non-test terminal key-match consumers (runtime_tool_dispatcher.py, managed_agents_dispatch.py), matching this row BINDING RULE inventory exactly. The b80 pin flip is RATIFIED, not drift: test_map_addressed_keyed_abort_activates_alongside_an_unrelated_keyless_pause final assert goes False -> True (fork section 11 item 1) while its sole-keyless and two-keyed siblings stay False. SIX MUTATION PROBES, each reverted then restored: the 1.1 filter, the 1.2 validation, the 1.2 copy, the 1.2 immutability, the 1.2 clone/serialization carrier, and the 1.3 resolver guard -- every one produced at least one NAMED failing test. THREE out-of-family codex rounds to convergence: round 1 [P2] found a real unauthorized compatibility regression (a MappingProxyType field broke deepcopy / model_copy(deep=True) / pickle for ANY valid map, which section 0.3 does not authorize) -- fixed by adopting the settled _ImmutableKeyArns dict-subclass precedent; round 2 [P2] found inherited dict.__init__ re-populated the sealed map in place -- sealed; round 3 [P2] found the seal itself reopenable by plain attribute assignment -- moved behind slots and a refusing __setattr__, with the termination point of that hardening line stated explicitly in the type docstring (routes that NAME a base class are the same boundary section 1.5 draws for model_construct). ONE ROUND-2 FINDING DECLINED WITH GROUNDS, recorded not absorbed: extending the empty-key rule to the PUBLIC ResumeContext.effect_fence_resolution_for lookup. Fork section 11 item 5 names _resolve_effect_fence_gated as the site; section 1.3 wording (yields no DIRECTIVE, before a map-hit or UNIFORM-ELIGIBILITY comparison) describes that function and not the pure lookup; and spec v1.115 sections 0.1/3 enumerate exactly THREE amendment sites, so narrowing a method published at v1.66 would be a FOURTH and an X-AL-3 silent design extension at impl time. Safety does not rest on that reading: a pin test walks EVERY harness-*/src tree and asserts the method has exactly ONE production caller, the now-guarded resolver, so a second caller or a later ratification breaks there rather than drifting. ALSO OBSERVED, NOT FIXED (out of surgical scope, reported upward): the sibling _ImmutableKeyArns at harness-runtime/src/harness_runtime/types.py carries the identical inherited dict.__init__ hole. VERIFICATION: just check exit 0 at the final tree -- ruff clean, pyright strict 0 errors / 0 warnings / 0 informations, full suite green; just overlay-check OK, 397 nodes, 36/36 seams wired. RATIFICATION SUPERSEDES the unratified alternative framing below: A-hybrid is applied; no Runtime section 30 delta or fail class, no CXA row, and no council are owed. The four variants, ten source shapes, and seven carriers remain unchanged. Implementation remains separately owed; this row is not closed. The 2026-08-01 transit note''s Class-1-fork-owed wording below was the pre-filing forecast; the actual instrument is the CLASS 2 fork (class_2_fork_b107_empty_fence_key_resolution_refusal.md, PR #1178) whose section 11 ratification authorizes the apply pass; no Class 1 doc exists or is owed. STATUS TRANSIT 2026-08-01, stated so the snapshot bump is legible: registered_finding ->
    DESIGN_SUBSTRATE_GATED (out-of-family review round 4 [P2]). The enum at this file''s header
    defines registered_finding as a gap that opens for BUILD once grounding confirms a genuinely
    buildable slice; this grounding confirms there is NONE -- no existing contract term supports
    a build-now refusal, and even the separable _any_fence_abort slice owes a v1.111 section 2
    reconciliation -- so the row moves to the gate that actually blocks it: a Class 1
    design-substrate change, FORK DOC OWED (not yet filed, not yet ratified).

    GROUNDED 2026-08-01 (dedicated grounding pass; every cite below re-verified by direct read at
    head c1afb225 -- NO anchor moved). VERDICT RECORDED: SPEC-LEG-FIRST, AND THE SPEC LEG IS ITSELF
    FORK-SHAPED. NO EXISTING CONTRACT TERM SUPPORTS A BUILD-NOW REFUSAL. (i) CP spec v1.107 section
    1.1 unaddressed-set membership INCLUDES "" -- membership is `not in resolutions` over an
    enumeration that keeps "" BY DESIGN (workflow_driver.py:2965-:2967; section 1.1 words it "whose
    own idempotency_key is NOT a key in effect_fence_resolutions"), and CP spec v1.112 section 2.1
    states it outright -- "the authoritative effect-fence walk INCLUDES the empty key, the
    uniform-fallback computation COUNTS it, and only the abort walk filters it". (ii) The "cannot
    address" language (CP spec v1.113 section 1.2, the amended effect-fence-addressable row of
    v1.112 section 2.1''s projection table) is PUBLICATION, and v1.112 section 2.4 disqualifies it
    as a rule source in its own words -- "section 2 publishes; it does not decide ... MUST NOT
    become a second place where classification semantics are stated". (iii) CP spec v1.114 holds
    this row and B-101 SEPARATE (section 0.5 finding (v)). EMPIRICAL PIN: disposition (a) FLIPS
    test_map_addressed_keyed_abort_activates_alongside_an_unrelated_keyless_pause
    (harness-cp/tests/test_workflow_driver_effect_fence_tree_wide_abort_b80.py:222-257, the :254 `is
    False` assert) -- filtering the keyless candidate leaves the KEYED location solely eligible, so
    the uniform ABORT reaches it and that assert becomes True. It is the EXECUTABLE PIN of the rule
    (a) would move, which is why (a) is a spec amendment and not an impl tweak.

    CORRECTED SEVEN-SITE MAP (this REPLACES the round-11 (c) claim, which was FALSE for the BRANCH
    carrier; (c) below is corrected in place). Exactly SEVEN _resolve_effect_fence_gated call sites
    exist at head: #1 :3056 GUARDED (the `if k` filter at :3053); #2 :4926 LINEAR consult UNGUARDED;
    #3 :7839 fan-out level-local _any_fence_abort scan UNGUARDED (:7841 iterates
    _recovered_effect_fence_paused.values() with no filter); #4 :8216 GUARDED (`if
    (_branch_fence_key and _ef_resume_ctx is not None)`); #5 :11708 orchestrator-workers level-local
    _any_fence_abort scan UNGUARDED (:11710, no filter); #6 :12118 GUARDED (the ORCHESTRATOR''s OWN
    carrier short-circuits on `_orch_fence_resume.idempotency_key`); #7 :12554 GUARDED. CONSEQUENCE:
    a refusal placed inside _resolve_effect_fence_gated DOES reach the BRANCH/WORKER carrier''s
    empty keys (sites #3 and #5 call the resolver with "" ungated) and does NOT reach the
    ORCHESTRATOR''s OWN carrier (site #6 is its only consult, and it is guarded). "Misses BOTH
    entirely" was wrong on the branch half; "misses the orchestrator entirely" stands.

    NEW FINDING (i) -- THE any() IS POSITION-DEPENDENT, an INDEPENDENT reason the single-function
    refusal shape is unsound. _recovered_effect_fence_paused is a dict built in carrier order off
    effect_fence_paused_branches, and that tuple is tuple(sorted(keyed dispositions)) +
    crash_pause_reconstruct_fence_paused (:10280; the orchestrator-workers twin at :14639) -- the
    crash-reconstruction "" entries are appended LAST. A keyed sibling resolving ABORT therefore
    SHORT-CIRCUITS the any() before the "" entry is ever evaluated, so a resolver-internal refusal
    would fire or not fire depending on SIBLING RESOLUTION ORDER rather than on the presence of the
    empty key. A refusal whose firing depends on iteration order is not a refusal.

    NEW FINDING (ii) -- A FOURTH TERMINAL CONSUMPTION SITE this row had not named:
    managed_agents_dispatch.py:300 performs the IDENTICAL key match to
    runtime_tool_dispatcher.py:1042 (step_context.effect_fence_resolution.idempotency_key ==
    idempotency_key). A programmatic sweep of the non-test tree finds EXACTLY these two terminal
    key-match sites. This is the binding rule below doing its job, not an exception to it.

    CLOSURE-CRITERION FLAG -- THE CRITERION AS WRITTEN UNDER-COVERS THE WORST CASE. The {"": ABORT}
    suppression is OVER-APPLICATION, not a drop: a resolution addressed to "" reaches a DIFFERENT
    location''s decision at :8238 / :12576 (the run-level ABORT guard suppresses a keyed sibling''s
    valid directive) -- exactly the cross-location misattribution CP spec v1.107 section 1.1(a)
    forbids ("never a cross-location misattribution of a SKIP_AS_FIRED/RE_FIRE/ABORT/ABORT_BRANCH
    judgment intended for a different location"). But the criterion below says the row closes iff no
    channel delivers a resolution "without either applying it or REFUSING it" -- and this case DOES
    apply it, at the wrong location. LITERALLY READ, THE CRITERION IS SATISFIED BY THE DEFECT. THE WIDENING IS APPLIED AT THIS PASS rather than left to the opening arc (out-of-family review
    round 1 [P1]) -- see the amended CLOSURE CRITERION below.

    THE SEPARABLE _any_fence_abort SLICE IS NOT FREE IMPL DISCRETION. The two-line `if _k` filter at
    :7841 / :11710 looks self-contained, but CP spec v1.111 section 2.1(b) requires the level-local
    guard be combined with the tree-wide signal as "a strict widening of when suppression fires,
    never a narrowing" -- and filtering empty keys OUT of the level-local scan is a NARROWING. Even
    that slice therefore owes a stated reconciliation, plausibly a v1.111 section 2 amendment. It
    COULD nonetheless ship as an INDEPENDENT NARROW LEG before the (a)/(c)/(d) fork settles: it is
    the one piece that does not depend on the fork''s outcome.

    THE FOUR FORK DECISIONS the eventual filing must put to the operator. (1) (a)+(d) VS (c) -- two
    dispositions (one per channel) versus the one that converts DROP into REFUSE everywhere; not a
    preference, since they amend different things (a CP classification rule vs a resume path''s
    failure mode vs a ResumeContext field''s type). (2) FAIL-LOUD VS INERT -- the C9-shaped tension
    this row''s council note already predicts; the bar is Runtime spec section 30 term 4, "a refusal
    the operator cannot act on is a livelock with good manners", so a refusal on an empty key is
    worth raising only if the operator has something to re-read and recompose from. (3) SEAM
    PLACEMENT, RE-DECIDED AGAINST THE CORRECTED MAP -- the close_out''s "cheapest sound placement is
    an ADMISSION-TIME check" was chosen against the FALSE map. Admission via api.py (where
    _enforce_pause_state_staleness_precondition sits, :823) is BYPASSABLE: execute_workflow is a
    public export of harness_cp.workflow_driver (its __all__), so a caller can reach the consults
    without transiting api.resume(). THE NON-BYPASSABLE CHOKEPOINT IS execute_workflow ITSELF (workflow_driver.py:3061-:3076), which
    ALREADY receives pause_snapshot_input, resume_context AND all three root-computed signals as
    parameters -- every route (api.resume via mcp_server, a direct public caller, and the recursive
    child re-entry at child_workflow_runner.py:261) transits it. (CORRECTED 2026-08-01 at
    out-of-family review round 1 [P1]: an earlier draft of this bullet claimed mcp_server.py:378-403
    was "the one place both objects exist together"; that is FALSE, and it would have left direct
    execute_workflow callers unfenced.) THE REAL DISCRIMINANT IS TREE SCOPE, NOT REACHABILITY:
    execute_workflow re-enters PER RECURSION LEVEL with a NARROWED pause_snapshot_input, so a check
    placed there sees only its own subtree, whereas mcp_server.py:378-403 is the ONE site holding
    the UN-NARROWED depth-0 root (which is exactly why the three tree-wide signals are computed
    there once and threaded down unchanged). BUT THE DEPTH-0 SITE IS NOT A SAFE HOME EITHER (out-of-family review round 2 [P1]):
    mcp_server.py:378-403 is merely where those signals HAPPEN to be computed today, and it is
    bypassed by a direct execute_workflow caller exactly as api.py is. The OUTER execute_workflow
    invocation itself receives the caller-supplied, UN-NARROWED pause_snapshot_input -- narrowing
    happens only on RE-ENTRY (child_workflow_runner.py:261 passes the child cursor snapshot) -- so
    BOTH a per-location AND a tree-wide check can be homed at, or explicitly propagated from, the
    DRIVER ENTRY, which is the only non-bypassable one. Discriminating the outer entry from a
    recursive re-entry is itself part of the fork''s design work; the driver already carries
    sub_agent_descent and receives the three root-computed signals as parameters, and whether either
    is a sound discriminator must be grounded rather than assumed. WHAT IS SETTLED: any placement
    that leaves a direct execute_workflow caller unfenced is NOT a candidate. (4) ORCHESTRATOR
    SCOPE IS TYPE-LEVEL ONLY -- that carrier is PRODUCTION-UNREACHABLE (its one construction site''s
    guard chain at :12376-:12381 ends in a truthiness test), so its witnesses MUST be
    constructed-snapshot detect-then-refuse tests per the B-100 precedent (recorded at CP plan v2.48
    AC #A11 / Runtime plan v2.57 AC #16(a)), never an e2e run. WITNESS SHAPE (CORRECTED at out-of-family review round 2 [P2] -- a plain 2 x 3 grid
    under-specifies it): the BRANCH carrier has TWO INDEPENDENT TOPOLOGY SITES with their own
    _any_fence_abort scan and their own suppression consumer -- PARALLELIZATION at :7839 / :8238 and
    ORCHESTRATOR_WORKERS (HIERARCHICAL_DELEGATION reuses it per CP spec v1.111 section 2.2) at
    :11708 / :12576 -- so a matrix keyed on CARRIER alone can be satisfied by exercising one of them
    while the other retains the cross-location suppression. The grid is therefore 2 CHANNELS (map,
    scalar) x 4 CARRIER-SITES (LINEAR; PARALLELIZATION branch; ORCHESTRATOR_WORKERS worker; the
    ORCHESTRATOR''s OWN carrier) = EIGHT cells, each branch cell exercising an empty-key location
    ALONGSIDE a keyed sibling, and the two ORCHESTRATOR cells constructed-snapshot.

    The (d) / B-101(a) SHARED-OBSTACLE CROSS-REF IS UNCHANGED, as recorded at CP spec v1.114 section
    0.5 finding (v): the same ResumeContext field-set-preserved-verbatim obstacle would discharge
    both, and the rows stay SEPARATE -- no merge is proposed and none is performed.

    FOUR candidate dispositions, and the choice is a real one. NONE of them is subsumed by
    B-100''s own remainder, and TWO OF THEM ARE NEEDED FOR FULL CLOSURE (one per channel) unless (c)
    is taken. (a) RESOLVER-SIDE, closes the SCALAR channel only: filter empty keys out of
    compute_effect_fence_uniform_fallback_eligible_key''s candidate set, exactly as
    compute_effect_fence_tree_wide_abort_present already does at :3054, making an empty-key location
    UNADDRESSED-BY-CONSTRUCTION. It changes a CP CLASSIFICATION RULE (which locations the sole-member
    test counts), so it needs a CP spec amendment to section 1.1(a) -- the B-100 spec leg explicitly
    could not author it, since CP spec v1.112 section 2.4 binds section 2 to publish classification
    rather than define it. LIVENESS, GROUNDED RATHER THAN ASSUMED (an earlier draft claimed a
    liveness COST; out-of-family review round 4 [P2] falsified it): filtering is NEUTRAL in the
    lone-empty-key case -- the run ALREADY re-pauses INERT today -- and STRICTLY MORE LIVE in the
    mixed case (one empty + one keyed), where today unaddressed has 2 members so NEITHER may take the
    uniform fallback, whereas filtering leaves the KEYED location solely eligible. BUT NOTE WHAT (a)
    DOES NOT DO: it removes a FALSE ACCEPTANCE without producing a REFUSAL, and it does not touch the
    map channel. (b) CAPTURE-SIDE: refuse an empty key at the capture sites -- this is B-100''s own
    declined disposition (b), still hash-adjacent. IT DOES NOT SUBSUME THIS ROW, contrary to an
    earlier draft of this text (corrected at round 8 [P2]): refusing at the LINEAR site (:5456) leaves
    the BRANCH carrier''s empty key untouched, and that one is DELIBERATE (crash reconstruction
    constructs it at :4074 precisely because CP cannot derive the runtime fence key), so it would have
    to be REDESIGNED, not merely refused. (c) REFUSE-AT-THE-SEAM: raise rather than silently skip or thread when a resolution is addressed to an empty key. This is the ONLY option that converts DROP into REFUSE. BUT ITS SEAM IS NOT ONE FUNCTION -- corrected at round 11 [P2], and CORRECTED AGAIN 2026-08-01 because the round-11 sentence was itself FALSE for the BRANCH carrier and contradicted this row''s OWN round-11 site finding: placing the refusal inside _resolve_effect_fence_gated alone misses the ORCHESTRATOR''s OWN carrier entirely (site #6, :12118, its only consult, truthiness-guarded) but DOES reach the BRANCH/WORKER carrier, whose level-local _any_fence_abort scans at :7839 and :11708 call the resolver with "" UNGUARDED -- per the CORRECTED SEVEN-SITE MAP at the top of this close_out. Full coverage therefore needs EITHER an earlier validation seam that sees the location before any consult is skipped -- e.g. at resume-context admission or at the collector -- OR a per-consult refusal at each of the three sites. Cheapest sound placement is an ADMISSION-TIME check, which also subsumes (d)''s map channel; ground that before assuming the single-function version. (RE-OPENED 2026-08-01: that placement was chosen against the FALSE map AND against an api.py admission point that the public harness_cp.workflow_driver.execute_workflow export bypasses -- see fork decision (3) at the top of this close_out.) It changes a resume path''s failure mode either way, which is a real contract change. (d) CONSTRAIN THE MAP KEY: give effect_fence_resolutions a min-length-1 key type so {"":
    ...} is unrepresentable. Closes the map channel BY TYPE, which is the strongest form -- but it is
    a shape change to a ResumeContext field that CP spec v1.112 section 0.4 guarantees PRESERVED
    VERBATIM, so it needs its own byte-compat argument (the same obstacle B-101 records for that
    carrier). CLOSURE CRITERION (WIDENED 2026-08-01 across TWO review rounds -- see the CLOSURE-CRITERION FLAG
    at the top of this close_out; round 1 [P1] required the widening be applied HERE rather than
    deferred to the opening arc, since the prior wording was LITERALLY SATISFIED BY THE DEFECT, and
    round 5 [P2] found the first widening STILL permitted an empty key to be HONOURED as an address
    locally -- which contradicts the ADJUDICATED RULE settled at the B-69 / B-100 records and
    published at CP spec v1.113 section 1.2, that an empty fence key is POSITION-ONLY and the resume
    path CANNOT address it). B-107 closes iff, on EVERY channel and EVERY carrier, an empty-key
    location is either (i) REMOVED BY CONSTRUCTION from the addressable/eligible set, or (ii)
    REFUSED -- and NEVER (a) silently DROPPED, NOR (b) HONOURED as an address (a resolution keyed to
    "", or a scalar uniform value threaded at an empty-key location, is a forbidden outcome even
    when it affects nothing else, since the directive it produces carries idempotency_key="" and no
    runtime fence can ever match it), NOR (c) permitted to alter ANOTHER location''s decision (the
    {"": ABORT} over-application at :8238 / :12576). Witnessed per channel (map and scalar) and per
    carrier-SITE (see WITNESS SHAPE above). HONOURING AN EMPTY KEY LOCALLY, AND MIS-APPLICATION AT A
    DIFFERENT LOCATION, ARE BOTH NON-CLOSURE, exactly as silent drop is. Partial is non-closure. GROUND (a)+(d) AGAINST (c) BEFORE OPENING ANY OF THEM. ENUMERATION IS BOUNDED BY RULE, NOT BY THIS LIST -- an explicit review-time-inventory note, since four successive out-of-family rounds each found one more empty-key consumption site (the scalar eligibility computation at round 3, the LINEAR runtime key-match at round 3, the level-local _any_fence_abort scans at :7838-:7842 / :11707-:11711 at round 11, the truthiness short-circuits at :12118 and the per-branch sites at round 11) and a fifth is not ruled out. THE BINDING RULE: **every site that reads a fence key off a resume carrier or off resume_context, on any path, is in this row''s scope whether or not it is named above** -- the sites enumerated here are the inventory AS OF 2026-08-01 -- extended by that day''s grounding pass with the CORRECTED SEVEN-SITE resolver map and a FOURTH terminal consumption site, managed_agents_dispatch.py:300 (the identical key match to runtime_tool_dispatcher.py:1042; a programmatic sweep of the non-test tree finds EXACTLY those two), both recorded at the top of this close_out -- not the definition. The arc that opens B-107 MUST re-derive the site set programmatically from the carriers and from resume_context''s two fence surfaces before selecting a disposition; a disposition chosen against this list alone is unsound by construction.
    CROSS-REF 2026-08-01: the B-100 IMPL leg landed (PR #1174) and DELIBERATELY did NOT touch this
    row''s surfaces -- verified against that diff, which is confined to
    harness-cp/src/harness_cp/pause_state_projection.py. Both channels this row tracks remain exactly as
    described: the MAP channel (ResumeContext.effect_fence_resolutions is still dict[str, ...] with
    unconstrained keys) and the SCALAR channel (compute_effect_fence_uniform_fallback_eligible_key still
    does not filter empty keys). What B-100 removed is the ADVERTISEMENT on all three carriers -- an
    operator composing from the projection now has no empty key to paste on ANY of them, where before the
    LINEAR and orchestrator shapes surfaced one. The projection ALSO deliberately preserves
    PauseTreeWalkEntry.effect_fence_key == "" for every key-absent shape (mutation-probed at that leg), so
    this row''s site inventory is UNCHANGED by it and no classification rule moved. Read this row''s own
    close_out, not that leg, before selecting a disposition -- and honour its binding rule: re-derive the
    site set programmatically before choosing.'
  council: 'PROBE-RESOLVED 2026-08-03: no convening is owed under the ratified A-hybrid reading; this supersedes the conditional historical text below. CONDITIONAL as of 2026-08-01, REVISED FROM ''no'' (out-of-family review round 4 [P2]).
    The grounding pass ELEVATES the disposition-(c) fail-loud-vs-INERT question from hypothetical
    (''if a genuine tension appears'') to a NAMED fork decision the filing must put to the operator,
    which trips the workspace nameable-tension discriminator. A DYADIC convening is therefore OWED
    at the arc that opens (c) -- C9 (reliability: a silent INERT re-pause is an undiagnosable
    livelock) against the operator-actionability half Runtime spec section 30 term 4 states as the
    bar (''a refusal the operator cannot act on is a livelock with good manners''), for which C11
    (operator loop) is the natural second voice. PROBE-FIRST still applies: if a direct read of the
    section 30 refusal battery settles whether an empty-key refusal is operator-actionable, record
    it as probe-resolved rather than convening. The PRIOR TEXT, still accurate on its own terms and
    retained: no, and NO REOPENING TRIGGER IS ATTACHED. The adjudicated rule (an empty fence key must
    never be surfaced or honoured as an address) is settled at the B-69 / B-100 records; this row asks
    only which SEAM enforces it, which is a scoping question, not a fresh cross-domain tension. An
    earlier draft attached a reopen-if-(a)-is-taken trigger on a claimed LIVENESS COST; that cost was
    FALSIFIED by the corrected analysis in the close_out above, so the trigger is REMOVED rather than
    left to fire a council gate on a stale rationale (out-of-family review round 5 [P2]). If a genuine
    tension appears it will be the disposition-(c) fail-loud-vs-INERT question -- a C9-shaped
    liveness-vs-diagnosability call -- which belongs to whichever arc opens (c).'
  heading: '### B-107 · No channel REFUSES an operator resolution addressed to an EMPTY effect-fence key — the response map and the scalar uniform fallback DROP it on all three carriers, and the level-local abort scans MIS-APPLY it to a keyed sibling *(surfaced at the `B-100` spec leg, out-of-family review rounds 3 + 8, reported not absorbed, 2026-07-31; the over-application half established at the 2026-08-01 grounding pass)*'
- id: B-106
  title: Embedder-path event-loop exposure for capture_pause_snapshot (outside the shipped driver path)
  status: closed
  pr: '#1176'
  summary: 'Surfaced by the out-of-family confirming round on the B-103 rework (PR #1172, codex [P2], 2026-07-31,
    delivered AFTER the merge -- recorded here as the follow-up). B-103 was closed PREMISE-FALSIFIED for every SHIPPED
    call site (all production capture_pause_snapshot calls run under workflow_driver._run_protocol_method_sync ->
    asyncio.run on a private single-task loop in the driver''s to_thread worker). Codex identified a path OUTSIDE
    that set: run_bootstrap() is a public export (harness_runtime.__all__), ctx.pause_resume_protocol is a context
    field, and an EMBEDDING application that directly awaits ctx.pause_resume_protocol.capture_pause_snapshot(...)
    on its own live loop bypasses the private-loop helper entirely -- codex measured a 300ms loop sleep delayed
    to ~2s while a second process held the journal lock. The B-103 close_out''s shipped-call-site scoping stays
    literally true; THIS row carries the decorrelated-reviewer disagreement (lens-1/orchestrator vs codex) instead
    of silently adjudicating it.'
  close_out: 'STRUCTURAL-OPTION PREFERENCE RESOLVED 2026-08-05 by operator ratification: `run_bootstrap`
    STAYS EXPORTED in `harness_runtime.__all__` (`__init__.py:266`, re-exported at `:44`-`:49`).
    The default stands -- the operator-decidable follow-on this row NAMED-BUT-DID-NOT-ABSORB (dropping
    the package-root export to make the not-supported ruling STRUCTURAL at zero call-site cost)
    is DECLINED, on the ground this row already recorded against it: it is a PUBLIC-SURFACE NARROWING,
    major-bump-shaped by the corpus''s own version-evolution grammar. The exclusion-defined ruling
    therefore remains DOCUMENTARY, which is what it already was and what the four grounding legs
    support. This resolves the standing preference; it does NOT reopen the B-103/B-106 shared
    trigger, which is unchanged. CLOSED 2026-08-01 -- the embedder path is NOT SUPPORTED. The
    embedding contract is defined by EXCLUSION, not by silence, so the grounding verdict is a
    ruling and not an absence of evidence. FOUR LEGS, each re-verified by direct read at this
    head. (1) EXCLUSION-DEFINED CONTRACT: Runtime spec section 4 (:3093) states it verbatim --
    "HarnessContext is an internal type (consumers reach into specific fields, not the whole context)
    ... The type is not part of any operator-facing surface in Track A." ctx.pause_resume_protocol
    is one of those fields; an embedder awaiting it is reaching into an internal type by the spec''s
    own words. (2) THE OPERATOR-FACING API IS EXACTLY THREE package-root async functions and it
    enumerates itself: section 8 (:3264) run(), section 30 (:3363) resume(), section 14.14.9.1
    (:3456) read_paused_workflow_state() -- each introduced as "exactly one" / "a second" / "a
    third async function exposed at the harness_runtime package root". run_bootstrap is not among
    them; a closed self-counting enumeration is the strongest form of exclusion this corpus has.
    (3) THE CONTRACTED EXTERNAL-CALLER PAUSE ROUTE IS DESIGNATED, AND IT IS NOT THE PROTOCOL:
    pause_requested_flag (section 4 field row :3085 -- "Caller-side pause-signaling primitive.
    Set by external caller (operator API, MCP tool, etc.)"; section 14.14.7 :5897 leaves only
    the flag-setting surface''s shape to implementation discretion). The external caller SETS
    THE FLAG and the driver captures at the next per-step pre-entry; the capture is the driver''s
    to perform, never the caller''s. GROUNDED SHARPENING (out-of-family round 1, [P2], ACCEPTED
    on the source, not argued down): no external setter for that flag is EXPOSED today -- the
    only in-repo production setter is the internal HITL gate composer (lifecycle/hitl_gate_composer.py:1329);
    every other setter is a test reaching through ctx. That is not a hole in this ruling, it is
    the ruling''s own evidence -- the designated external route is deliberately still at implementation
    discretion (section 14.14.7), which is exactly what ''the embedding surface has not been minted
    yet'' looks like from the inside. So the embedder path is not an undocumented corner of a
    supported route -- it is a SECOND, uncontracted route to something the contract routes elsewhere
    and has not yet opened. (4) NO EMBEDDING SURFACE EXISTS TO BE IN SCOPE: a full scan of design-substrate/**.md
    finds ZERO library-embedding text -- every ''embed*'' hit is either the unrelated CP EMBEDDING
    routing layer (C-CP-02 section 2) or ''embedded credentials'' / ''embedded in HandoffContext''.
    The committed deployment surfaces are the ADR-D2 v1.2 THREE-TIER set (local-development /
    self-hosted-server / managed-cloud; CLAUDE.md section 10.2), and ''harness embedded as a library
    in a host application'' is not among them. PRECEDENT: B-69 is the controlling how-surfaces-get-minted
    rule -- an operator-facing surface is MINTED by a ratified spec leg (there, the new public
    projection-returning surface over the private resume-tree walks), never inherited by an internal
    field happening to be reachable. Minting one here would be an X-AL-3 silent design extension.
    THE MECHANISM HALF IS REAL AND STAYS TRUE: DurablePauseResumeProtocol.capture_pause_snapshot
    calls self._store.capture(snapshot) SYNCHRONOUSLY inside the async method (lifecycle/durable_pause_resume_protocol.py:124),
    and the flock inside would block a live loop -- codex measured a 300ms sleep delayed to ~2s.
    That measurement is not disputed and is not being explained away; it is simply a measurement
    of a path outside the contract. THE DAEMON WAS CHECKED rather than assumed: its captures also
    route through workflow_driver._run_protocol_method_sync''s private single-task loop, so B-103''s
    falsification survives it and the embedder path is the SOLE live-loop route -- which is exactly
    why closing it as out-of-contract closes the whole question. WHAT SHIPPED (documentation only,
    zero logic, impl-discretion -- it restates the cleared section 4 invariant and mints nothing):
    an AUDIENCE paragraph on run_bootstrap''s docstring (bootstrap/__init__.py) naming the internal-type
    ruling, the three contracted functions, the caller''s own-loop obligation, and the pause_requested_flag
    route; and the concrete blocking-flock fact on the pause_resume_protocol field comment (types.py).
    NO SPEC TEXT OWED -- confirmed by B-103''s own recorded confirmation for the mechanism half
    (section 14.8.1''s "The wrap chain above pins composition, not execution venue"), and by this
    row adding no contract, no config scalar, no failure class and no observable surface. THE
    B-103 OFFLOAD STAYS SHELVED, preserved at PR #1172''s pre-rework commits (3636762c-era head
    of b103-off-loop-journal-lock -- dedicated executor, join-on-cancel, 12-test witness). B-103''s
    REOPENING TRIGGER IS UNCHANGED: a future arc replacing _run_protocol_method_sync''s asyncio.run
    with a captured-loop bridge (the evolution that helper''s own docstring anticipates at :2598-2601).
    ADDED HERE: RE-CHECK B-106 ALONGSIDE B-103 AT ANY CHANGE TO THAT HELPER -- if the bridge lands,
    the SHIPPED path joins the embedder path in being loop-co-resident, the harness itself acquires
    an in-house benefit from the offload, and the SUPPORTED branch reopens on that benefit rather
    than on the embedder''s. The two rows share one trigger and must be evaluated together. NAMED,
    NOT ABSORBED -- THE STRUCTURAL OPTION, an OPERATOR-DECIDABLE follow-up: run_bootstrap sits
    in harness_runtime.__all__ (__init__.py:266, re-exported at :44-49) with ZERO in-repo package-root
    consumers -- 215 occurrences across 40 files, and EVERY import site is ''from harness_runtime.bootstrap
    import run_bootstrap'' (28 import lines; the two non-test callers are api.py and cli/app.py,
    both intra-package). Dropping the package-root export would make this ruling STRUCTURAL at
    ZERO call-site cost -- the embedder would have to reach into a private submodule to get the
    context at all. IT IS NOT ABSORBED HERE because it is a PUBLIC-SURFACE NARROWING, major-bump-shaped
    by the corpus''s own version-evolution grammar (section 9 :3343: "Removing a field is always
    a major bump"; section 4 :3093: "field-removals or type-changes are major (consumers break)"),
    and because the spec itself references harness_runtime.run_bootstrap(...) illustratively at
    :6262 (inside an implementation-discretion note, so not a contract -- but not nothing either).
    A major-bump-shaped narrowing of a public surface is an operator call, not an impl-discretion
    one. DEFAULT = LEAVE THE EXPORT; the docstrings carry the ruling without it.'
  council: no -- the mechanism is settled (built + preserved); the open question is support-surface scope, a grounding
    question against the spec's own embedding contract, with advisor() + out-of- family review at the leg. Councilable
    only if grounding finds the embedding contract itself ambiguous.
  heading: '### B-106 · Embedder-path event-loop exposure for `capture_pause_snapshot` *(surfaced by the out-of-family
    confirming round on the `B-103` rework, PR #1172 codex [P2], delivered post-merge, 2026-07-31)*'
- id: B-108
  title: 'Spec SS14.8.11''s typed report-log term has no sink the HARNESS configures -- absent host/operator logging configuration, report-log lines reach stderr and die with the process'
  status: registered_finding
  summary: 'Surfaced by out-of-family Codex round 2 [P1] against the B-96 council record (2026-08-01) as a falsification of C7''s [MODERATE]-tagged cross-run log-durability premise, then independently re-verified and CONCEDED by C7 in its round-2 response. MECHANISM: Runtime spec SS14.8.11 :4909 mandates that TTL expiry surfaces as a TYPED report-log line, and the protected result store ships four such emissions (protected_result_store.py :830, :839, :849, :855). But the harness CONFIGURES NO LOGGING SINK OF ITS OWN -- scoped at round 3 (out-of-family [P2]): the claim is about the STOCK, unconfigured deployment, NOT that no sink can exist, since harness.* sets no propagate=False anywhere (zero occurrences; stdlib default True holds) so a host or operator CAN attach a handler and capture these lines durably. Evidence: a whole-tree scan of all SEVEN production src trees (harness-runtime, harness-core, harness-is, harness-as, harness-cp, harness-od, harness-cxa) for basicConfig / dictConfig / fileConfig / addHandler / logging.config / StreamHandler / FileHandler / configure_logging / setup_logging / handlers[ / lastResort returns ZERO matches, re-run programmatically at this filing. Every emission site (30 in total, all in harness-runtime/src, all on the harness.* logger hierarchy) therefore falls through to Python''s logging.lastResort handler, which writes WARNING+ to process stderr and nowhere else. SCOPE: workspace-wide, not B-96-specific -- it is a property of every logger.* call in the harness, and it applies identically at HEAD, under C-1 and under C-2, which is exactly the orthogonality discriminator the B-96 council used to route the write-driven-cadence gap out of that filing. CONSEQUENCE, stated precisely: the report log is a TIMELY NOTIFICATION on stderr, not a durable measurement -- in the one-shot process shape nothing accumulates across runs, so any diagnostic that requires comparing successive emissions is unavailable in the stock deployment. It is NOT a live data-loss defect: nothing miscomputes and no payload is lost; what is absent is the forensic surface the spec term names. B-96''s council record absorbed the CLAIM half already (its condition 12 now states carrier durability is deployment-configured and MUST NOT be assumed, and its condition 8(b) moves the authoritative diagnostic onto an on-demand pull surface with no configuration dependency); what stays open here is the GAP half -- whether the harness owes a default sink at all.'
  close_out: 'GROUND FIRST -- the genuine question is whether a default sink is OWED, and by which surface; do not reach for a handler config before answering it. THREE CANDIDATE SHAPES, none chosen: (1) DEFAULT HANDLER AT THE CLI ENTRY -- configure the harness.* hierarchy once at the harness-runtime/src/harness_runtime/cli/app.py entry point (level + destination operator-overridable). Cheapest and closes the one-shot case, but it is an OPERATOR-FACING BEHAVIOUR CHANGE (where lines go by default, at what level, with what rotation) and it does NOT cover the library/api.py path, where configuring the root logger from inside a library is the anti-pattern. (2) OTEL LOG BRIDGE -- route harness.* into the OTel pipeline that already exists for spans (LoggerProvider + LoggingHandler). This makes the report log a first-class signal alongside the 15-namespace OD schema, but MINTS A NEW SIGNAL FAMILY and is therefore X-AL-3 SPEC-GATED: a Class 1 back-flow plus an OD/Runtime spec leg is owed BEFORE any code, not after. Note the two independently verified absences it would close together: no metrics pipeline (no get_meter / MeterProvider anywhere) and no log bridge (no LoggerProvider / LoggingHandler anywhere). (3) DOCUMENT AND CLOSE -- state normatively that carrier durability is deployment-configured and that no condition may depend on emission durability for its correctness. HALF-TAKEN ALREADY by the B-96 council record''s conditions 12 and 6 (spec-leg text owed at the B-96 spec leg), which is why this row carries only the residue: (3) closes the false-claim half, it does not decide whether a default sink is owed. WITNESS -- DIFFERENT PER SHAPE (scoped at round 3, out-of-family [P2]): for shapes (1) and (2), which ADD a sink, an end-to-end run (not a unit test) that drives a real TTL-expiry sweep and asserts the typed report line is retrievable from the configured sink AFTER the emitting process has exited -- a caplog assertion proves nothing here, since caplog installs the very handler whose absence IS the finding. For shape (3), which adds NO sink, a post-exit witness is IMPOSSIBLE by construction and demanding one would make the shape unclosable -- its witness is documentary and negative: the spec states the deployment-configured limit, and no condition depends on emission durability for its correctness.'
  council: 'conditional -- shape (3) alone needs no convening (stating a true negative is not a design decision). Shapes (1) and (2) carry a nameable C7 (observability -- owns the signal, its carrier and its namespace, and wants the store''s sole forensic artifact to actually persist) against C11 (operator loop / local-first -- bears the configuration burden and the stderr noise on a local-development CLI, and pays for any default the harness picks on the operator''s behalf) tension. Convene dyadic ONLY if (1) or (2) is taken.'
  heading: '### B-108 · §14.8.11''s typed report-log term has NO sink the HARNESS configures — absent host/operator logging configuration every report-log line reaches stderr and dies with the process *(surfaced by out-of-family Codex round 2 on the `B-96` council record, independently re-verified and CONCEDED by C7; scoped at round 3, 2026-08-01)*'
- id: B-109
  title: Sibling immutable-map carrier _ImmutableKeyArns retains the inherited dict.__init__ re-population hole
  status: closed
  pr: '#1216'
  summary: 'SURFACED at the B-107 impl leg (codex round 2, 2026-08-04): the B-107 carrier
    _ImmutableEffectFenceResolutions had its inherited dict.__init__ re-population route sealed
    (calling __init__ on the already-constructed instance silently repopulated the sealed map
    in place), and the fix adopted the settled _ImmutableKeyArns dict-subclass precedent -- but
    the PRECEDENT ITSELF, _ImmutableKeyArns at harness-runtime/src/harness_runtime/types.py,
    carries the IDENTICAL hole: obj.__init__({...}) bypasses its immutability posture. Observed
    and deliberately NOT fixed at the B-107 leg (out of surgical scope -- harness-runtime was
    read-only for that arc). GROUNDED at the closing arc (2026-08-04): the hole reproduced live
    at HEAD on BOTH the raw carrier and a validated frozen AuditSigningConfig.key_arns field;
    construction sites exactly two (__reduce__ + the field validator); the tree-wide sweep found
    NO further sealed-mapping siblings. The B-107 carrier''s sealed form (__slots__ +
    refusing __setattr__ + sealed __init__) was the template; the fix stayed bounded to the
    sibling type. Explicit-base-class routes (dict.__setitem__(obj, ...)) remain out of scope
    per the B-107 section 6.1 / CP v1.115 section 1.5 forged-instance boundary, extended to
    explicit base-class routes at the B-107 impl-leg docstring.'
  heading: '### B-109 · Sibling immutable-map carrier `_ImmutableKeyArns` retains the inherited `dict.__init__` re-population hole *(surfaced at the B-107 impl leg, out-of-family review round 2, 2026-08-04; observed and deliberately not fixed there — harness-runtime was read-only for that arc)*'
  close_out: 'CLOSED 2026-08-04, BUILT (PR #1216). The closure
    criterion -- the sibling refuses re-initialization with witnesses mirroring test_pd8_probe_b/c
    and a mutation probe -- is MET: sealed per the B-107 template
    (_ImmutableEffectFenceResolutions at harness-cp/src/harness_cp/pause_resume_protocol_types.py)
    with a sealed positional-only __init__ over a read-only MappingProxyType({}) default,
    __slots__ = ("_sealed",), refusing __setattr__/__delattr__, and __reduce__ byte-unchanged so
    deepcopy/pickle/model_copy(deep=True) keep working with every copy itself sealed. Witnesses:
    4 new tests in harness-runtime/tests/test_config_audit_signing.py (16 -> 20) -- re-init
    refused + byte-unchanged on raw AND validated-field carriers; seal-reopen (_sealed
    assignment/del) refused with no instance __dict__; copies sealed; no-arg construction valid.
    PD-8 mutation probe PERFORMED, not asserted: the __init__ override deleted -> all four named
    tests FAIL (DID NOT RAISE TypeError) -> restored -> green. Test totals, CI-MEASURED (not
    derived): 7373 passed / 11 skipped / 36 deselected at the PR branch head dff5b25a, blocking
    "pytest (all axis packages)" job, run 30972358886 -- identical at the merge commit ec438578
    (run 30972622806); the local run was 7372 passed + 2 known harness.toml env-artifact reds;
    pyright strict 0/0. Explicit-base-class routes (dict.__setitem__(obj, ...),
    object.__setattr__) stay out of scope per the B-107 section 6.1 / CP v1.115 section 1.5
    forged-instance boundary, extended to explicit base-class routes at the B-107 impl-leg
    docstring, restated in the type''s own docstring.'
  council: 'no -- single-axis mechanical hardening of an existing internal type; no cross-domain
    tension nameable in advance; route per the standing single-voice-plus-advisor default if a
    design question surfaces at grounding.'
- id: B-110
  title: The GC observation record cannot distinguish generations of a REUSED temporary
    candidate name, because its ratified content set is closed at two members
  status: closed
  pr: '#1235 (surfaced) + #1260 (mechanism (c) landed at the U-RT-151 companion arc)'
  summary: 'Surfaced at the B-96 SPEC leg (PR #1235) by out-of-family Codex rounds 3-4, against
    that leg''s own text -- reported, not absorbed. Runtime spec v1.111 section 14.8.11.1 term 5
    keys the durable grace record on the CANDIDATE FILENAME over both sweep classes, with a
    content set RATIFIED CLOSED at two members {candidate filename, first_observed_at}. The
    authorizing council record (`.harness/council-b96-grace-ceiling-2026-08-01.md` condition #5)
    asserts a name is "stable and never reused for a different file" for BOTH classes, grounding
    it on the absence of any rename/replace in the store. Round 3 [P2] showed that ground
    establishes the LIFETIME half only: a temporary name is drawn by a mechanism that avoids only
    names CURRENTLY PRESENT, so once an orphan is removed its name is redrawable and a surviving
    record row can match a DIFFERENT later file. The published-entry class is NOT exposed -- those
    names are non-invertible digests of a uuid4-bearing composite key. Round 4 [P1] then showed
    that MANDATING a generation check (the spec leg''s first response) is UNSATISFIABLE inside the
    ratified two-member set, since there is no field to carry one and adding one would widen a
    ratified closed set. A SECOND, DISTINCT and PRE-EXISTING mechanism, surfaced at out-of-family round 6 [P2] and verified
    by direct read: the sweep SELECTS candidates under the publish + cross-process locks
    (protected_result_store.py:781-800) but REMOVES them OUTSIDE the lock (:821-822, :845-847), so two
    concurrent sweeps can both select one orphan and the second sweep''s STALE PATH can unlink an active
    replacement -- a PREMATURE DELETION, not merely a lost grace. That is a property of the shipped
    sweep''s phase split, NOT of the observation record: it is present at HEAD under the sweep-COUNT
    grace, reachable with no record at all, and NOT introduced by the B-96 amendment. Disposition (c)
    below covers it. WHAT THE PRIMARY ITEM COSTS, EXACTLY: the conjunctive rule still gates the recreated file -- term 1
    conjunct (a) is keyed on the NEW file''s own filesystem timestamp, so the reclaim DECISION cannot
    precede that file''s own TIMESTAMP + TTL. That is the TIMESTAMP-DERIVED bound and it is NOT term 2:
    term 2 bounds reclaim by PUBLICATION, a temporary orphan is not a published entry, and on a volume
    whose timestamps under-report creation the timestamp + TTL check can elapse before the new file''s
    own creation + TTL. Term 2''s publication bound therefore does NOT extend to a reused temporary
    candidate name, and neither the spec nor this row claims it does (corrected at out-of-family round 8 [P2]). What degrades is the ADDITIONAL grace: for a reused temporary name alone
    the reclaim bound falls back to the pre-existing timestamp-derived one, reintroducing the
    under-report-width window for that single candidate. Reachability is remote (the name is drawn
    from a large random space and must be redrawn in the interval between two sweeps) and is
    confined to crash orphans, never to a paid effect''s stored entry.'
  close_out: 'Two grounded dispositions, neither owed by the B-96 impl leg. (a) ACCEPT AND DOCUMENT --
    the residual is already stated at Runtime spec v1.111 section 14.8.11.1 term 5 as a bounded, named
    residual with its exact cost, and B-96 U-RT-150 AC #5 witnesses the TIMESTAMP-DERIVED bound -- explicitly
    NOT term 2, whose publication bound does not extend to a reused temporary name -- rather than an unbuildable
    freshness guarantee; on this disposition the row closes as documented-and-accepted. (b) WIDEN THE
    RATIFIED CONTENT SET -- add a generation-stable identity as a third member, or key on an identity
    that is not the bare basename. That is a CLASS 2 OPERATOR DECISION, not a spec-leg or impl-leg act:
    the two-member closure is ratified at the B-96 fork section 11.3 table and at council condition #5,
    and its stated ground is that widening adds no field, no carrier and no namespace. A fork filing is
    owed before (b) may be built. (c) LOCK-BOUND IDENTITY REVALIDATION BEFORE REMOVAL -- re-verify under
    the lock that the path still names the file the selection decided on. Cheap, and B-96 U-RT-150 is
    already working at that exact site so that unit MAY take it, but it is NOT owed by the B-96 legs,
    whose ratified scope is the reclaim RULE; mandating it there would be scope creep on a pre-existing
    hazard. NOT OWED BY ANY LEG: reopening the B-96 ceiling question, or any change to the candidate-filename
    key over BOTH sweep classes, which condition #5 requires for an independent reason (crash orphans
    would otherwise be permanently unreclaimable). 2026-08-07 -- RE-SCOPED AT A GROUNDING PASS; THE ROW
    NOW CARRIES MECHANISM (c) ALONE. (i) THE PRIMARY HALF IS DISCHARGED IN SUBSTANCE. Disposition (a)
    ACCEPT-AND-DOCUMENT is SATISFIED at HEAD: the landed U-RT-150 witness `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp`
    (`harness-runtime/tests/test_lifecycle_protected_result_store.py:2731-2795`) asserts exactly the TIMESTAMP-DERIVED
    bound this row says survives a name reuse -- the recreated file is NOT reclaimed before its own filesystem
    timestamp + TTL and IS reclaimed after (`:2794-2795`) -- i.e. the degradation is witnessed at the
    strength the ratified two-member carrier can actually deliver, and no unbuildable freshness guarantee
    is claimed anywhere. Disposition (b) WIDEN-THE-RATIFIED-CONTENT-SET remains a Class 2 OPERATOR decision
    and is NOT opened by this pass; the row does not carry it as owed work. (ii) WHAT THE ROW RETAINS:
    mechanism (c) -- the SECOND, DISTINCT, PRE-EXISTING sweep-phase-split hazard -- ALONE. OFFSETS RE-STAMPED
    AT HEAD BY CONTENT (the summary''s `:781-800` / `:821-822` / `:845-847` cites are from the B-96-era
    file and are SUPERSEDED): selection of BOTH candidate classes happens under `self._publish_lock` +
    `self._cross_process_lock()` at `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:1111-1135`;
    the ENTRY unlink is OFF-LOCK at `:1173`; the TMP-ORPHAN unlink is OFF-LOCK at `:1201`. (iii) SEVERITY
    SHARPENED -- (c) is a HOLE IN `B-68`''s OWN whole-lifetime-lock fix, not merely an adjacent race.
    `_publish_atomic` holds BOTH the publish lock and the cross-process lock across the temporary''s ENTIRE
    lifetime (`:810-858` -- `mkstemp` at `:811` inside the `with` opened at `:810`, `os.unlink(tmp_name)`
    in the `finally` at `:857-858`) PRECISELY so that no sweep can unlink an in-flight temporary. A SECOND
    concurrent sweep''s OFF-LOCK unlink at `:1201` walks a path captured BEFORE that hold began and therefore
    BYPASSES it. If it fires, the publisher''s subsequent `os.link` raises `FileNotFoundError` -- a LOST
    RECOVERY RECORD FOR A COMPLETED PAID EFFECT, which is the exact loss the protected result store exists
    to prevent. (iv) REACHABILITY, QUANTIFIED -- this is why it stays REGISTERED rather than opening:
    firing requires an EXACT `tempfile._RandomNameSequence` redraw of the just-freed name inside the window,
    and that sequence draws 8 characters over a 37-symbol alphabet = 3.51e12 distinct names, so ~2.8e-13
    per redraw (ground-truthed against Python 3.12.13''s `tempfile` on the dev machine), narrowed further
    by requiring two overlapping sweeps with a publish landing between them. (v) TRIGGER REPLACED. The
    ''B-96''s U-RT-150 is already working at that exact site so that unit MAY take it'' trigger is EXPIRED
    -- U-RT-150 has landed and did NOT take it. The standing trigger is now OPPORTUNISTIC: THE NEXT ARC
    THAT TOUCHES `gc_sweep`''s PHASE SPLIT TAKES IT. Nothing else opens the row. (vi) THE FIX IS PRICED
    for that arc so it does not have to be re-derived: move the `tmp_candidates` unlink loop (`:1199-1201`)
    back UNDER the lock -- `B-75`''s off-lock rationale does NOT bind it, since that loop performs no
    decrypt and no unbounded work -- and add identity revalidation against the `(st_dev, st_ino, st_mtime)`
    triple captured at SELECTION time, because a bare re-`stat()` is INSUFFICIENT (POSIX offers no compare-inode-and-unlink
    primitive, so the comparison must be made under the same lock the unlink takes). ~25 lines plus ONE
    injected witness: monkeypatch `tempfile.mkstemp` to hand the publisher the just-freed name, and assert
    the second sweep REFUSES to unlink it.'
  council: 'NOT owed at registration. The question is whether a ratified CLOSED content set should
    grow one member to close a remote, bounded, crash-orphan-only residual -- a scoping question
    for an operator ratification, not a nameable cross-domain tension. If disposition (b) is opened,
    re-evaluate: a third member touches the same C3 (state/persistence integrity) vs C10/C7
    (content restriction on a sensitive-payload store) axis condition #5 was authored on.'
  heading: '### B-110 · The GC observation record cannot distinguish generations of a REUSED temporary candidate name *(surfaced at the B-96 spec leg, out-of-family Codex rounds 3-4, 2026-08-05)*'
- id: B-111
  title: 'The GC observation record''s publication temporary is unreclaimable after a mid-publication kill, because both sweep globs exclude it by construction'
  status: closed
  pr: '#1237 (the B-96 IMPL leg this surfaced against -- DECLINED with a grounded pin and registered) + #1258 + #1260 (the PLAN leg -- Runtime plan v2.59: the AC #15 qualifier + NEW unit U-RT-151 -- and the IMPL leg that landed U-RT-151 and closed the row)'
  summary: 'RATIFIED 2026-08-07: DISPOSITION (a) is selected -- narrow U-RT-150 AC #15''s no-removal clause to the SWEEP''s PAYLOAD-orphan accounting path, then build the prefix-scoped cleanup. APPLIED at Runtime plan v2.59 (design-substrate/Implementation_Plan_Harness_Runtime_v2_59.md, cleared 2026-08-07): U-RT-150 AC #15 gains an APPENDED narrow qualifier -- the SWEEP half (enumeration into count/age accounting, the AC #8 gauge at both surfaces, candidate reporting, and unlink-as-ciphertext) is PRESERVED UNQUALIFIED and the `.tmp-*` PAYLOAD-orphan semantics are BYTE-UNCHANGED with NO glob widened, while the record''s OWN publication path MAY remove stale leftovers of its OWN `_GC_OBSERVATION_RECORD_TEMP_PREFIX`, prefix-scoped BY CONSTRUCTION and under the locks the publication already holds. THE RUNTIME SPEC IS UNTOUCHED -- v1.111 stands and NO spec delta is owed: sec14.8.11.1 ''The observation record''''s carrier'' commits the publication mechanism and lock scope and EXPLICITLY ACKNOWLEDGES the orphan (''a failed write leaves only an orphan temporary file'') while stating NO disposition for it, so the prohibition this row runs into exists ONLY in the plan. THE QUALIFIER IS A PERMISSION, NOT AN OBLIGATION -- deliberately, because U-RT-150 is LANDED and CLOSED (PR #1237) and a MUST written into AC #15 would make its conjunctive closure criterion RETROACTIVELY FALSE; the MUST therefore lives at NEW unit U-RT-151 per the B-97(a) -> U-RT-149 precedent. U-RT-151 (FOUR ACs, ~15 lines across TWO functional sites plus a collected-list parameter threaded between them -- FOUR signature/call touch points: `_observe_candidates` :1224-:1230 / :1130 and `_publish_observation_record` :1282 / :1269) owes: the prefix-scoped cleanup with its exact-set scope refusal, whose ENUMERATION MUST RUN OFF THE CROSS-PROCESS LOCK -- `_publish_observation_record` is reached from `_observe_candidates` at protected_result_store.py:1130, INSIDE the lock opened at :1111, so a root glob/iterdir there is O(store size), the cost B-75 moved OFF the lock (:1020-:1036), and would falsify the method''s own docstring pin at :1317-:1319; the enumeration therefore joins the sweep''s already-unlocked pass (:1068-:1104) and only a bounded unlink loop (zero-length in steady state) runs under the lock, which also makes the path''s own in-flight temporary -- created by the mkstemp at :1327, AFTER the pre-lock pass -- excluded BY CONSTRUCTION (POSIX -- on Windows `_cross_process_lock` is a documented NO-OP at :608-:610 and the exclusion degrades to the in-process lock: bounded by AC #1''s past-TTL age gate, under which a LIVE co-resident publication''s in-flight temporary is never selectable, so the residual matches the PRE-EXISTING B-45 shape the sweep''s own past-TTL `.tmp-*` re-verify at :1041-:1046 already carries -- the identity claim is TRUE only WITH the gate, corrected at out-of-family round 4). ORDERING IS PINNED: the removal runs only AFTER the `os.replace` at :1333 returns and before the directory fsync at :1340 -- without that pin AC #2''s failed-publication witness and AC #4''s suppressed-OSError criterion are mutually unsatisfiable; ONE DECLARED re-pin of `test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan` -- its `exists()` assertion flips to a POSITIVE removed-by-a-LATER-PUBLICATION witness that ATTRIBUTES the removal to the publication path (absence alone would also pass a widened sweep glob), while its prefix-disjointness and not-reported assertion groups are PRESERVED VERBATIM; an assertion that the `.tmp-*` payload class is classified exactly as before; and the success-path no-new-surface absences (failure-path unlink OSError reported on the EXISTING report-log carrier -- round-4 absorb). ZERO contract numbers, ZERO carriers retyped, ZERO fields added, ZERO snapshot_hash impact, ZERO cluster, ZERO cross-axis edge, ZERO CXA rows (aggregate frozen at 111). B-110 is NEITHER closed NOR widened. ROW STATUS registered_finding -> open per this file''s own status enum (ratified + applied plan delta opens the row for code); it flips to `closed` ONLY when U-RT-151 merges. ORIGINAL REGISTRATION FOLLOWS. Surfaced at the B-96 IMPL leg (U-RT-150) by out-of-family Codex round 2 [P2], against that
    leg''s own code -- reported, not absorbed. `_publish_observation_record` publishes the durable grace
    record by `tempfile.mkstemp(prefix="gc-observations.publishing-")` -> write -> fsync -> `os.replace`,
    with an `except BaseException: unlink(tmp); raise` cleanup. A process KILLED strictly between `mkstemp`
    and `os.replace` runs NO Python cleanup, so the temporary survives. That prefix is deliberately disjoint
    from BOTH sweep globs (U-RT-150 AC #15''s second half REQUIRES the disjointness: reusing the `.tmp-`
    payload prefix would make the next sweep classify the artifact as crash-orphaned ciphertext and unlink
    it as though it were a payload), and nothing else in the store enumerates it -- so no path ever removes
    it, and it is invisible to the term-8 gauge and to the `harness-inspect` counts as well. A repeated
    crash loop landing in that window could therefore accumulate files on the store volume with no bound
    and no operator-visible signal. Reachability is REMOTE and of the same order as B-110: the window
    is the sub-millisecond gap between `mkstemp` and `os.replace`, once per sweep, and it needs a KILL
    (not an exception -- an exception takes the cleanup path, which is witnessed by `test_record_publication_is_crash_atomic_and_never_no_replace_or_unlink_recreate`).
    WINDOW CORRECTION (2026-08-07 grounding pass, by direct read). The text above calls the window *"the
    sub-millisecond gap between `mkstemp` and `os.replace`"*. THAT UNDERSTATES IT. The gap at `harness-runtime/src/harness_runtime/lifecycle/protected_result_store.py:1329-1332`
    is FSYNC-INCLUSIVE -- `os.fdopen` -> `handle.write` -> `handle.flush` -> `os.fsync(handle.fileno())`
    all sit INSIDE it, before the `os.replace` at `:1333` -- so on a slow or contended volume the window
    is a DURABILITY-BOUNDED interval, not a sub-millisecond one. Worse, it is CORRELATED with exactly
    the condition that provokes an external kill: a supervisor SIGKILLing a process that appears hung
    on a slow mount lands PREFERENTIALLY inside this window. COUNTER-CORRECTION, STATED AGAINST INTEREST:
    the *"accumulate files on the store volume with no bound"* framing OVERSTATES the cost in the other
    direction -- each leftover is ONE record-sized file per killed sweep, so exhausting a volume needs
    an implausible number of kills. THE REAL COST IS ACCOUNTING INVISIBILITY, not capacity: the leftovers
    are invisible to the term-8 gauge and to the `harness-inspect` counts, so an operator''s orphan accounting
    silently UNDER-REPORTS. NET: reachability is HIGHER than registered and the harm is NARROWER than
    registered. GROUNDING RECOMMENDS DISPOSITION (a) -- the AC #15 narrowing -- on PLAN-TEXT-CORRECTNESS
    grounds rather than on severity: AC #15''s no-removal clause, read literally, forbids the store from
    cleaning up its OWN prefix, which is not what the AC exists to protect (it exists to stop the record
    temporary being unlinked AS CIPHERTEXT). DECISION PENDING OPERATOR -- this note records the grounding,
    it does not select the disposition.'
  close_out: 'DECLINED AT THE B-96 IMPL LEG WITH A GROUNDED PIN, registered rather than absorbed. The natural fix -- having `_publish_observation_record` remove stale siblings of its OWN prefix at publication time, mirroring `_publish_atomic`''s `finally: os.unlink(tmp_name)` for payload temps -- is FORECLOSED by U-RT-150 AC #15 as written: its mutation probe states that a sweep after a simulated mid-publication crash "must not enumerate, report or remove it", and the AC''s witness (`test_record_publication_temporary_is_never_enumerated_as_a_payload_orphan`) asserts exactly that the leftover STILL EXISTS after a later sweep. Since `_publish_observation_record` is reached only FROM `gc_sweep`, any cleanup there IS a sweep removing it, so building the fix at this leg would mean absorbing a plan-level contradiction -- precisely the silent-absorption failure mode X-AL-3 forbids. TWO GROUNDED DISPOSITIONS, neither owed by U-RT-150. (a) NARROW AC #15 at a plan delta so its no-removal clause binds the PAYLOAD-orphan accounting path specifically (enumeration into count/age/report, and unlink AS ciphertext) rather than any removal whatsoever, then build the prefix-scoped cleanup under the lock the publication already holds -- cheap, and strictly in the bounded-retention direction the store exists to serve. This is a Phase-6 plan act, not an impl act. (b) ACCEPT AND DOCUMENT -- the residual is stated in `_publish_observation_record`''s own docstring with its exact cost and reachability, and the row closes as documented-and-accepted. NOT OWED BY ANY LEG: changing the temporary''s prefix back toward `.tmp-` (AC #15 requires the disjointness for an independent and stronger reason), or widening the sweep globs (which would reintroduce exactly the unlink-as-ciphertext defect AC #15 exists to close).'
  council: 'NOT owed at registration. The question is whether an acceptance criterion''s no-removal clause should be narrowed by one qualifier so a self-owned cleanup becomes legal -- a plan-scoping question, not a nameable cross-domain tension. If disposition (a) is opened, re-evaluate: it touches the same C3 (state/persistence integrity) vs C10 (blast radius on a sensitive-payload store) axis AC #15 was authored on.'
  heading: '### B-111 · The GC observation record''s publication temporary is unreclaimable after a mid-publication kill, because both sweep globs exclude it by construction *(surfaced at the B-96 impl leg, out-of-family Codex round 2 [P2], 2026-08-05)*'
- id: B-112
  title: 'Deadline''d lock polling drops kernel handoff fairness, so a caller-sampled ledger timestamp can be overtaken and refused'
  status: closed
  pr: '#1241 (the B-93 build leg that surfaced it); closed at the B-57 impl leg (#1256) on its own prescribed two-process-witness evidence'
  summary: 'Surfaced by out-of-family Codex round 1 on the B-93 build leg (2026-08-06) and ACCEPTED. MECHANISM:
    the B-93 deadline replaces a bare blocking `fcntl.flock` with the ratified non-blocking-probe-plus-bounded-retry
    shape. A blocking `flock` PARKS the waiter in the kernel, which grants the lock on release before
    the ex-holder can realistically re-acquire it; a POLLING waiter is asleep at that instant, so an ex-holder
    that releases and immediately re-acquires wins the next round. This changes ORDERING, never EXCLUSION
    -- mutual exclusion is the same kernel `flock` it always was. It reaches a production contract at
    exactly one place: `append_ledger_entry` (`harness-is/src/harness_is/state_ledger_write.py`) validates
    a CALLER-SAMPLED timestamp against PHYSICAL APPEND ORDER under a `_CLOCK_SKEW_TOLERANCE` of ZERO,
    so a waiter whose timestamp was sampled BEFORE it queued can now be overtaken by a later-arriving
    writer and refused with `NonMonotonicTimestampError`. The reviewer reproduced the dropped waiter in
    12 of 20 runs of a focused two-process contention probe. NOT NEWLY POSSIBLE, NEWLY LIKELY -- stated
    against interest: the same refusal was always reachable whenever the later writer genuinely acquired
    first, and POSIX `flock` specifies NO acquisition ordering at all, so the pre-B-93 code was relying
    on an UNSPECIFIED kernel behaviour. B-93 raises the probability; it does not create the class. THE
    LOCK LAYER CANNOT FIX THIS AND SHOULD NOT TRY: no acquisition ordering makes a timestamp sampled arbitrarily
    long before acquisition correct, and no portable primitive offers a kernel-parked wait WITH a timeout
    (`F_SETLKW` has none; a watchdog thread would orphan an acquisition in exactly the wedged-holder case
    the deadline targets, accumulating one stuck thread per attempt). Polling is intrinsic to the ratified
    shape, not to the implementation: `filelock` -- the vehicle the B-93+B-45 fork evaluated and DECLINED
    -- polls ten times coarser (`poll_interval=0.05`), and `msvcrt.LK_LOCK` retries at 1s. THE MECHANISM
    EXISTS IN THE CLEARED SPEC BUT IS SCOPED AWAY FROM THIS ROW''S SURFACE -- CORRECTED AT THE MERGE-GATE
    (lens 2), because the first filing of this row got the disposition WRONG in the row''s own favour.
    C-IS-07 section 7.6 (IS spec v1.11) added `WRITER_OWNED_TIMESTAMP`, whose sentinel makes `append_ledger_entry`
    sample the timestamp INSIDE the write critical section, so on the surface it covers the C-IS-05 section
    5 monotonic constraint (verbatim) *"holds **by construction** on this surface — concurrent sibling
    drains cannot invert"*. THAT SURFACE IS THE BUFFERED/BRANCH-DRAIN PATH ONLY. Section 7.6''s *"Surfaces
    that do NOT change"* paragraph keeps EVERY DIRECT append on caller-supplied semantics verbatim, and
    its REGISTERED RESIDUAL states that extending writer-owned authority to direct surfaces *"would change
    caller-supplied semantics for every existing direct producer and requires its own back-flow"* -- naming
    `B-57` as the row that covers the remaining direct writers. Its deferred-to-discretion footer likewise
    constrains the carrier to *"preserve the direct-path caller-supplied contract byte-verbatim"*. THE
    EARLIER CLAIM OF ''ZERO design-substrate delta ... a per-caller adoption decision inside cleared contract''
    IS THEREFORE RETRACTED: adopting the sentinel on direct producers is exactly the extension section
    7.6 routes to back-flow, and `B-57` (`.harness/forward-register.yaml` B-57 row) ALREADY OWNS that
    residual and ALREADY PRESCRIBES the route -- *a narrow IS spec delta extending section 7.6''s writer-owned
    sampling to an opt-in direct-append mode (or per-writer serialization guidance) + U-IS-11 widening*.
    THE ADOPTION QUESTION IS THEREFORE DEFERRED TO `B-57`, NOT DECIDED HERE. WHY THIS STAYS A SEPARATE
    ROW RATHER THAN FOLDING INTO `B-57`, recorded as the reasoning the gate asked for: `B-57` registers
    the direct-writer inversion as a LATENT possibility whose close-out is conditioned on it *"materializ[ing]
    in practice"*; this row is the EVIDENCE that its probability changed -- a specific, reproduced (12/20)
    mechanism, introduced by a specific arc, on a specific lock. Folding it in would bury a falsifiable
    trigger inside a dormancy note. The rows are therefore CROSS-LINKED: `B-112` is the cause-and-evidence
    record and `B-57` is the remedy owner, and `B-57`''s materialization condition should be read as PARTIALLY
    MET by this row''s evidence. TEST-SIDE ADJUDICATION (the gate asked for a direct read of section 7.6''s
    scope, not a reflex): `harness-is/tests/test_shadow_git_rollback.py`''s concurrent-append worker retries
    with `WRITER_OWNED_TIMESTAMP` on a DIRECT append surface. Section 7.6''s constraint is about changing
    the CONTRACT -- *"caller-supplied semantics for every existing direct producer"* -- and the sentinel
    is the ratified opt-in CARRIER, deliberately left to implementation discretion. A single test explicitly
    opting in changes no producer''s semantics and sets no default, so the constraint does NOT forbid
    it and the test is UNCHANGED. It is recorded here as EVIDENCE on the row -- the first direct-surface
    use of the sentinel in the tree, and therefore a data point `B-57` should weigh when it prices the
    opt-in mode. WHY IT IS REGISTERED RATHER THAN APPLIED AT THE B-93 LEG: selecting writer-owned sampling
    changes WHOSE CLOCK a given entry records, which is a semantic decision per call site, not a mechanical
    substitution -- some entries deliberately carry an event time that is not the append time. Converting
    callers wholesale inside a lock-deadline arc would be exactly the opportunistic-scope-creep the workspace''s
    surgical-change discipline forbids. One SHIPPED WITNESS was already repaired in place at the B-93
    leg for this reason (`test_rollback_cross_process_lock_prevents_lost_concurrent_append`, whose fixed
    child timestamp had silently PINNED the old ordering); its load-bearing survival assertion is unchanged
    and still discriminates under mutation. IN-PROCESS WIDENING (2026-08-07 grounding pass; CODE-GROUNDED,
    EXPLICITLY NOT REPRODUCED). The overtaking is NOT confined to two OS processes. `flock` contends between
    separate FILE DESCRIPTORS EVEN WITHIN ONE PROCESS -- stated in the lock module''s own text at `harness-is/src/harness_is/cross_process_ledger_lock.py:120`
    (*"`flock` contends between separate fds even within one process"*), which is precisely why the module
    carries a refcounted IN-PROCESS `_rlock` face at all. A sibling THREAD in the SAME process that loses
    the in-process probe therefore enters the SAME polling wait (`:173-184`: `self._rlock.acquire(blocking=False)`
    fails -> `resolved.note_contention()` -> a `timeout`-bounded `acquire`), and the cross-process arm
    beneath it polls the same way (`:524`). The exposed population is therefore BROADER than the `harness
    run` alongside `harness daemon` two-process topology close-out step (1) names: it is ANY two concurrent
    direct appenders on one ledger root, THREADS INCLUDED. Recorded as a code-grounded WIDENING OF SCOPE
    only -- the 12/20 figure remains a TWO-PROCESS measurement and NO in-process probe was run at this
    pass.'
  close_out: 'ADJACENT INFRA GAP, REGISTERED HERE RATHER THAN BUILT (merge-gate lens 3, G3). This arc hit a SILENT TEST-LOSS defect: two new test files shared a basename (`test_b93_cross_process_lock_deadline.py` in both `harness-is/tests/` and `harness-runtime/tests/`), and because EVERY `tests/` directory is a package literally named `tests`, the module path `tests.test_b93_cross_process_lock_deadline` COLLIDED -- pytest imported ONE module for BOTH paths and silently dropped 12 witnesses (34 collected where 35 were written). It was caught only by recounting programmatically; the suite was GREEN throughout. NOTHING GUARDS AGAINST A RECURRENCE: `__init__.py` is present in every tests dir and does NOT prevent this (the packages are all named `tests`), so the invariant the tree actually relies on is UNIQUE TEST BASENAMES REPO-WIDE -- currently true, currently unenforced, and violated silently rather than loudly. A CI guard (a basename-uniqueness check across `harness-*/tests/**`, or per-package `pytest` rootdir isolation, or `--import-mode=importlib` with unique package names) is its own mini-arc with its own tradeoffs and is NOT built here. Recorded on this row because it was surfaced by the same arc; a future infra arc may lift it to its own row. (1) SWEEP the production callers of `append_ledger_entry` and classify each: does it pass a caller-sampled timestamp, and can its ledger be appended to by a SECOND OS PROCESS concurrently? Only the intersection is exposed. The `harness run` alongside `harness daemon` shared-root topology named in `CanonicalMemoryStore.write_record_guarded`''s own SCOPE paragraph is the reference shape. (2) For each exposed caller, decide between `WRITER_OWNED_TIMESTAMP` (C-IS-07 section 7.6 -- monotonic by construction; correct wherever the entry''s timestamp means ''when this was appended'') and keeping caller-supplied semantics (correct wherever it means ''when the event happened'', where an out-of-order refusal is the honest outcome and the caller should handle it). This is the actual decision and it is per-caller. (3) If ANY exposed caller must keep caller-supplied semantics, decide separately whether `_CLOCK_SKEW_TOLERANCE` -- documented as ''configuration-supplied'' but hard-coded to `timedelta(0)` -- should become genuinely configurable; note that widening it weakens the C-IS-05 section 5 monotonicity guarantee for everyone, so it is a contract question, not a tuning knob. (4) Witness with a two-OS-process probe: a waiter whose timestamp is sampled first must not be refused after a later writer overtakes it. DO NOT attempt a fairness fix in the lock layer -- the substrate reasons above are why.
    2026-08-07 -- CLASS 2 FORK FILED at
    `.harness/class_2_fork_b57_direct_append_writer_owned_opt_in.md` (doc-only; status UNCHANGED at
    registered_finding). This row is the CAUSE-AND-EVIDENCE record and the adoption question it
    defers to B-57 is what that filing decides. THIS ROW''S COUNCIL CONDITION IS RESOLVED NEGATIVE
    BY PROBE: step (3) (_CLOCK_SKEW_TOLERANCE configurability, the C3/C11 dyad) is reached ''only if
    step (2) leaves an exposed caller that cannot adopt writer-owned sampling'', and the filing''s
    twelve-site classification leaves exactly ONE site retaining caller-supplied semantics --
    as_is_wiring.py:129 (timestamp=composed.timestamp), genuine EVENT time, single-producer, for
    which an out-of-order refusal is the honest outcome by design. That is not a stranded exposed
    caller, so STEP (3) IS NEVER REACHED and no convening is owed. CLOSE-OUT STEP (1) IS DISCHARGED
    by the filing''s section 3(ii)/(iii): the production callers of append_ledger_entry are swept
    and classified BY CONTENT at HEAD 73b0e87b, and one of them
    (sibling_ledger_entry_composition.py:163) proved PRODUCTION-UNREACHABLE -- its
    cp_is_wiring.emit_sibling_ledger_entry entry point has ZERO non-test callers -- so the exposed
    surface is 11 sites, not 12. Re-commissioning the sweep at a later leg is NOT owed; re-grounding
    it at the spec leg is. THE IN-PROCESS WIDENING recorded on this row was RE-VERIFIED by direct
    read at the filing''s HEAD (cross_process_ledger_lock.py:120 module text, :173-184 in-process
    probe then bounded acquire, :524 cross-process probe) and remains CODE-GROUNDED, NOT REPRODUCED
    -- the 12/20 figure is still a two-process measurement only. 2026-08-07 -- THE DEFERRED ADOPTION QUESTION IS ANSWERED. This row deferred adoption to B-57; the B-57 Class 2 fork (.harness/class_2_fork_b57_direct_append_writer_owned_opt_in.md) was RATIFIED by the operator on 2026-08-07 as READING A -- per-call-site opt-in to WRITER_OWNED_TIMESTAMP on DIRECT append surfaces, default unchanged for every non-electing producer -- and applied at IS spec v1.13 (NEW C-IS-07 section 7.6.1, extending the EXISTING section 7.6; no contract number minted) + IS plan v2.9 (U-IS-11 ACs #14-#20 + the per-site classification table). THIS ROW''S STATUS DOES NOT MOVE at that leg and stays registered_finding: it is the CAUSE-AND-EVIDENCE record, not the remedy owner, and what it records -- the polling wait dropping kernel handoff fairness -- is unchanged and unrepaired by the ratification. What changed is that its named consequence now has an authorized remedy at the exposed call sites. ITS OWN REMAINING DISPOSITION RESOLVES AT THE B-57 IMPL LEG, on evidence: the two-process contention witness that leg owes is the direct analogue of this row''s own 12/20 repro, and a green witness under a PD-8 mutation probe is what would let this row close. STEP (3) -- widening _CLOCK_SKEW_TOLERANCE, this row''s stated council-eligible trigger -- IS STILL NOT REACHED, re-determined (not inherited) at the spec leg: the classification leaves exactly ONE site retaining caller-supplied semantics (as_is_wiring.py:129, genuine EVENT time), which is a caller for which an out-of-order refusal is honest BY DESIGN, not an exposed caller that CANNOT adopt. LIVE TRIGGER: if the B-57 impl leg judges EITHER injection-caveat site (audit_writer, cost_attribution_f2_write) RETAIN, step (3) IS reached and the C3 perp C11 dyad is owed BEFORE any tolerance change is authored. 2026-08-07 -- ROW CLOSES AT THE B-57 IMPL LEG, on the evidence this row itself prescribed: ''the two-process contention witness that leg owes is the direct analogue of this row''s own 12/20 repro, and a green witness under a PD-8 mutation probe is what would let this row close.'' That witness LANDED (test_electing_direct_append_survives_lock_race_overtake, a real second OS process) with its PD-8 probe RUN RED -- and its RED half is itself a shipped test (test_non_electing_direct_append_is_refused_after_lock_race_overtake), so the 12/20 becomes 20/20 in BOTH directions. THE MECHANISM THIS ROW RECORDS IS UNCHANGED AND STILL UNREPAIRED, stated against interest: the polling wait still drops kernel handoff fairness, and the lock layer still cannot and should not fix it. What closes is this row''s REMAINING DISPOSITION -- its named consequence now has an authorized remedy that is BUILT and adopted at every exposed direct caller, so a caller-sampled timestamp is no longer what the classified production sites persist. Close-out step (1) was discharged at the fork; steps (2) and (4) are discharged here (per-caller adoption decided and landed for all twelve classified sites; the two-OS-process probe written and green). STEP (3) IS STILL NOT REACHED and its council condition RESOLVES NEGATIVE PERMANENTLY for this arc: BOTH injection-caveat sites were resolved ELECT, so the live trigger recorded at IS plan v2.9 section 0.6 DID NOT FIRE, no exposed caller is stranded, and no _CLOCK_SKEW_TOLERANCE change is authored or owed. The IN-PROCESS WIDENING this row records remains code-grounded and NOT reproduced -- unchanged by this leg, and covered in substance by the same remedy (an electing site is immune to its own sample-then-lose-the-race inversion whether the racer is a thread or a process). The pre-existing shipped-comment defect this row''s sibling recorded (_CLOCK_SKEW_TOLERANCE documented ''configuration-supplied'' but hard-coded to timedelta(0)) is DELIBERATELY NOT touched here -- it is a separate surface, out of this arc''s scope, and re-verified INTACT.'
  council: 'No. Step (2) is a per-caller semantic classification against an ALREADY-CLEARED contract (C-IS-07 section 7.6 exists precisely for this), with no cross-domain tension to name. Step (3) WOULD be council-eligible if it is reached -- widening the skew tolerance trades information-substrate integrity (C3) against operator-loop robustness (C11) across every ledger writer at once -- but it is reached only if step (2) leaves an exposed caller that cannot adopt writer-owned sampling. Re-evaluate at that point, not now.'
  heading: '### B-112 · Deadline''d lock polling drops kernel handoff fairness, so a caller-sampled ledger timestamp can be overtaken and refused *(surfaced by out-of-family review round 1 on the B-93 build leg, 2026-08-06; reproduced 12/20 by the reviewer''s own contention probe)*'
- id: B-113
  title: 'ABBA contention probes classify by exception class, leaving three sites unbounded on an emulated-flock platform'
  status: closed
  pr: '#1251 (the close-unreachable grounding pass; surfaced at #1241, the B-93 build leg)'
  summary: 'Surfaced by out-of-family Codex rounds 8 and 10 on the B-93 build leg (2026-08-06); the reviewer raised it twice and it is ACCEPTED AS A FINDING but DECLINED AS A CHANGE AT THAT LEG. MECHANISM: `cross_process_write_lock`, `cross_process_replace_lock` and `cross_process_read_lock` each run a NON-BLOCKING probe under the directory hold and key their contention arm on `except BlockingIOError` -- i.e. on the EXCEPTION CLASS, which CPython raises for `EWOULDBLOCK`/`EAGAIN` only. `fcntl.flock`''s own docstring records that it is *"emulated using fcntl() on some systems"*, and the `lockf` docs spell out that a non-blocking failure arrives as `EACCES` OR `EAGAIN` with an explicit instruction to "check for either value". On such a platform those three probes would raise `PermissionError` immediately instead of entering their ABBA release-and-wait arm -- so `flock_until_deadline` is never reached and THREE OF THE NINE B-93 SITES WOULD NOT BE DEADLINE-BOUNDED THERE, while the leg''s own documentation says all nine are. WHAT B-93 DID FIX: the shared helper `harness_core.cross_process_lock_deadline.flock_until_deadline` now classifies contention by ERRNO (`_CONTENTION_ERRNOS` = EWOULDBLOCK/EAGAIN/EACCES), witnessed by an injected-EACCES test. So every acquisition the helper performs is correct on an emulated platform; only the three PRE-EXISTING probes that gate entry to it are not. WHY DECLINED AT THE B-93 LEG, stated rather than smoothed: (1) the narrow catch is SHIPPED B-46 behaviour, not introduced by B-93 -- the helper mirrored it deliberately; (2) widening it means MERGING each arm''s `except BlockingIOError` with the `except OSError` beneath it that classifies `ENOTSUP` and yields UNGUARDED, i.e. restructuring three hardened arms whose ABBA release-before-blocking invariant the fork forbids breaking; (3) `EACCES` from a NATIVE `flock` is not contention at all but a genuine permission failure, so the widening is only unambiguously correct on the emulated platform and only ambiguously so everywhere else; (4) the exposure is DORMANT on every platform this workspace can observe -- Linux and macOS both provide native `flock` returning `EWOULDBLOCK`, CI is `ubuntu-latest` only, and the operator''s machine is an x86 Mac. Doing it under the review cap, on concurrency code, for a platform no test can reach, is the regression-risk trade the workspace''s own non-convergent-hardening discipline warns against.'
  close_out: '(1) Determine WHICH platforms CPython actually emulates `flock` via `fcntl()` on -- the
    `configure` probe is `HAVE_FLOCK`, so the answer is a build-time fact about the target, not a runtime
    one. If no target the workspace supports lacks native `flock`, this row closes as UNREACHABLE rather
    than fixed. (2) If a target does: merge each of the three arms'' `except BlockingIOError` into a single
    `except OSError` that dispatches on errno -- contention errnos take the ABBA release-and-wait path,
    `ENOTSUP` keeps its yield-unguarded arm, everything else re-raises. The three arms are `cross_process_write_lock`,
    `cross_process_replace_lock` and `cross_process_read_lock` in `harness-is/src/harness_is/cross_process_ledger_lock.py`.
    (3) Witness by INJECTION, as the B-93 helper''s own EACCES witness does -- a real emulated-`flock`
    platform is not available, and pretending otherwise is the unwitnessable-claim failure `B-45` exists
    to avoid. (4) NOTE THE COUPLING TO `B-45`: if a Windows backend is ever built, the same errno-vs-class
    question recurs there (`msvcrt.LK_NBLCK` raises `OSError`, not `BlockingIOError`), so these two rows
    should be read together at that point. CLOSED 2026-08-07 AS UNREACHABLE, NOT FIXED -- the disposition
    this row''s OWN step (1) provides for. THE FINDING IS CORRECT BY INSPECTION AND IS DECLINED ON REACHABILITY,
    NOT ON CORRECTNESS: the three arms do key their contention path on the exception CLASS, and on a platform
    where CPython emulates `flock` via `fcntl()` they would raise `PermissionError` and skip the ABBA
    release-and-wait arm exactly as described. WHAT STEP (1) ESTABLISHED. (i) The discriminator is the
    BUILD-TIME `configure` probe `HAVE_FLOCK`, not a runtime condition -- a CPython built with `HAVE_FLOCK`
    defined calls native `flock(2)`, and only a build WITHOUT it takes the `fcntl()` emulation whose non-blocking
    failure can arrive as `EACCES`. (ii) EMPIRICAL POSITIVE CONTROL on the operator''s dev machine (Python
    3.12.13, `darwin`, x86_64): `sysconfig.get_config_var(''HAVE_FLOCK'')` is `1`, and a REAL two-fd contention
    probe (`LOCK_EX` held on one fd, `LOCK_EX|LOCK_NB` attempted on a second fd over the same file) raised
    `BlockingIOError` with `errno 35` (`EAGAIN`) -- native, not emulated, so the shipped `except BlockingIOError`
    arm is correct there. (iii) THE COMMITTED DEPLOYMENT SURFACE CONTAINS NO EMULATED TARGET. The ADR-D2
    decision matrix (`design-substrate/ADR-D2.md:96-98`) commits exactly three deployment surfaces --
    local-development, self-hosted-server, managed-cloud -- and every one is a Linux/macOS/BSD-family
    target whose CPython builds provide native `flock`; CI is `ubuntu-latest` only; the RC runbook''s
    Docker / Lima path is a Linux VM. The platform families where CPython actually emulates `flock` via
    `fcntl()` (the Solaris / HP-UX / z-OS class) appear NOWHERE in the committed surface -- not in ADR-D2,
    not in CI, not in the RC runbook. Step (1)''s condition -- *if no target the workspace supports lacks
    native `flock`, this row closes as UNREACHABLE rather than fixed* -- is therefore MET, and steps (2)-(3)
    are NOT owed. TWO REOPEN TRIGGERS, both PRESERVED rather than discharged by this close. (A) `B-45`:
    if a Windows serialization backend is ever built, the same errno-vs-class question recurs there --
    `msvcrt.LK_NBLCK` raises `OSError`, NOT `BlockingIOError` -- so the two rows must be read TOGETHER
    at that point, exactly as step (4) says. (B) NEW, and INDEPENDENT of `B-45`: any new SUPPORTED DEPLOYMENT
    TARGET whose CPython build lacks `HAVE_FLOCK` reopens this row directly, because the close rests on
    the composition of the committed deployment surface and on nothing else. Neither trigger is a defect
    in the finding; both are the conditions under which its dormancy ends.'
  council: 'No. An implementation-level errno classification inside an already-cleared lock mechanism (C-IS-09 section 9.4 defers the mechanism), with no cross-domain tension to name. The only judgement is the platform-reachability question at close-out step (1), which is a fact to establish rather than a value to trade.'
  heading: '### B-113 · The three ABBA contention probes classify contention by EXCEPTION CLASS, so an emulated-`flock` platform would leave three of the nine acquisition sites unbounded *(surfaced by out-of-family review rounds 8 and 10 on the B-93 build leg, 2026-08-06; DECLINED AT THE LEG, registered)*'
- id: B-114
  title: '`_prepare`''s executor-side exhaustiveness fall-through raises the memory-tool input-error
    type for a harness-internal fault, violating the C-MEM-19 type-boundary invariant introduced at
    Memory spec v1.3'
  status: closed
  pr: '#1249 (the conformance repair; surfaced at #1245, the B-88 spec leg, where the repair was withdrawn on authority grounds)'
  summary: 'Surfaced by out-of-family Codex round 3 on the B-88 spec leg (2026-08-06) and re-scoped
    at rounds 7 and 8. `StandardMemoryToolExecutor._prepare` (`harness-runtime/src/harness_runtime/memory_tool_executor.py:359-373`)
    ends with `raise MemoryToolExecutionInputError(f"unsupported memory tool {tool_name!s}")` at `:373`.
    But `MemoryToolExecutionRequest.tool_name` is typed `MemoryToolName` (`:205-212`, the field at `:210`)
    on an `extra="forbid"` model, so a request that reaches `_prepare` ALWAYS carries a valid enum member
    and that branch fires only when the executor lacks a `_prepare_*` for one -- a HARNESS-INTERNAL
    EXHAUSTIVENESS fault, not a caller- or model-supplied argument. `Spec_Memory_Substrate_v1.md` v1.3
    C-MEM-19 (`### Input validation failure`, plus the appended type-boundary invariant) states that where
    an implementation keys the failure class on the raised exception type, the type carrying
    `input_validation_failure` is raised ONLY for argument refusals and a harness-internal fault is not
    raised as that type. Once U-MEM-28 flips `MemoryToolExecutionInputError` to the new class, THIS SITE
    BECOMES A KNOWN NON-CONFORMANCE with a cleared contract. It is latent rather than harmful: an operator
    triaging `input_validation_failure` would see one entry that is really an internal wiring fault, and
    the branch is defensive (unreachable while every enum member has a `_prepare_*`).'
  close_out: 'WHY IT IS A SEPARATE ROW RATHER THAN U-MEM-28 WORK -- authority, not merit. The B-88 spec
    leg initially required this seventh re-typing alongside the ratified six in `lifecycle/llm_dispatch.py`.
    Out-of-family round 8 objected that the operator ratified an A-ii site list of EXACTLY SIX, that a
    spec leg APPLIES operator-decided fixes rather than deciding them, and that recording a contradiction
    does not authorize it; the objection was ACCEPTED and the obligation removed (fork §11.6 item 4;
    plan v1.3 change-note; U-MEM-28 Out-of-scope). Round 9 then observed that a finding carried only on
    `B-88`''s close-out would VANISH from open work when that row closes at U-MEM-28 -- hence this row,
    which OWNS the question and outlives `B-88`. CLOSE-OUT STEPS. (1) GROUND FIRST at the then-current
    HEAD: re-read `_prepare` and `MemoryToolExecutionRequest.tool_name`; if either has changed such that
    the branch is reachable from caller-supplied input, the site is CONFORMANT as it stands and this row
    closes with that finding recorded -- no code change owed. (2) If it is still internal, decide the
    DISPOSITION, and the discriminator is whether this needs an operator gate at all: (a) plain conformance
    repair against a CLEARED contract -- the C-MEM-19 type-boundary rule is already ratified spec text and
    the executor site was NEVER BEFORE THE OPERATOR (the B-88 fork surveyed `lifecycle/llm_dispatch.py`
    only), so extending nothing the operator decided, this is impl work under the cleared floor; or
    (b) a genuine extension of the ratified A-ii enumeration, needing a Class 2 fork + operator
    ratification. Reading (a) is the STRONGER one on the grounding above and is where a grounding pass
    should start, but the B-88 leg deliberately did NOT take it on its own authority and this row does not
    pre-decide it. (3) If repaired: re-type `:373` to a type outside the input-error family declaring the
    residual -- the SAME receiving-type constraints U-MEM-28 fixes for the six dispatch sites (not a
    subclass of `MemoryToolExecutionInputError`; not the family base itself, since `isinstance` is
    subclass-inclusive and admitting the base to `_classify_provider_exception`''s fail-fast tuple would
    silently fail-fast the DENIED and STORE subtypes that deliberately stay retryable) -- and witness BOTH
    halves per the same discipline: the emitted `memory.failure_class` AND `_classify_provider_exception`
    returning `None`, since telemetry alone cannot see a base-raise regression. The
    `test_memory_failure_classification.py` row carrying the "unsupported memory tool" message re-keys with
    it. (4) DEPENDS ON U-MEM-28 having landed: before the flip, `MemoryToolExecutionInputError` still
    declares `provider_adapter_failure` and this site is conformant, so the row is not actionable earlier. CLOSED 2026-08-07 - the grounding pass resolved the step-(2) discriminator to READING (a), PLAIN CONFORMANCE REPAIR AGAINST A CLEARED CONTRACT, and built it. THE ADJUDICATION, ON THE INVARIANT''S OWN WORDS. `Spec_Memory_Substrate_v1.md` v1.3 C-MEM-19 `### Invariants` reads: *"Where the failure class is determined from the raised exception type, the type carrying input validation failure is raised only for those argument refusals, and a harness-internal fault is not raised as that type; separating such a fault out does not change its retry disposition."* The prose paragraph it summarizes is identical in scope: *"the type carrying `input_validation_failure` must be raised ONLY for the argument refusals defined above. A harness-internal fault - an unset runtime memory context, or a missing or malformed entry in the harness''s own memory tool schema table - is not a caller- or model-supplied argument, and must not be raised as that type"*, closing with *"The rule constrains the type-to-class relation only; it fixes neither the number nor the naming of an implementation''s exception types"*. That is KIND-OF-FAULT language quantified over the TYPE, not an enumerated-sites list - the em-dash clause glosses what *harness-internal* means and is introduced as a definition, not as the exhaustive set of covered sites. The ratified "exactly six" is the enumeration of what the B-88 ARC undertook; the invariant is what the CODEBASE must satisfy. Reading (b) would require the cleared invariant NOT to bind this site - which the design substrate itself denies: fork §11.6 item 4 and plan v1.3 U-MEM-28 Out-of-scope both call it a *"REAL non-conformance"* / *"ONE known non-conformant site"* with the cleared contract, i.e. design-phase authority already adjudicated that the invariant binds here and withheld only the SPEC LEG''s authority to do the repair in that leg. Treating six as a ceiling on conformance would make cleared spec text unenforceable wherever a filing did not happen to survey, inverting the CLAUDE.md §1.3 authority chain. This arc is an IMPL leg, edits ZERO design-substrate files, mints no contract number and needs no re-clearance. PREMISE CHECK AT HEAD (step 1''s falsifier NOT met): offsets moved (`_prepare` at `:407-421`, the raise at `:421`; `tool_name` at `:258`) but the substance is unchanged - `tool_name` is still typed `MemoryToolName` on an `extra="forbid"` model, all five members still have `_prepare_*` arms, so the branch is still reachable only from a harness-internal fault; and step (4)''s precondition is met, U-MEM-28 having landed at #1247. WHAT LANDED (offsets resolved BY CONTENT - the docstring edits above the site shift every offset in this file, so the raise sits at `:421` pre-change and `:441` at the landed commit). The fall-through raise re-typed to `MemoryToolExecutionInternalError` (already satisfying the three receiving-type constraints, already in `_classify_provider_exception`''s fail-fast tuple BY NAME as of U-MEM-28); docstrings at the receiving type and at the fail-fast tuple amended to name the seventh raise site; the `test_memory_failure_classification.py` row RE-KEYED onto the internal type expecting the inherited residual `provider_adapter_failure` (eight input rows -> seven + one internal), which the U-MEM-28 comment there said it would do; NEW `harness-runtime/tests/test_b114_prepare_fall_through_conformance.py` with the both-halves witnesses (raised type + not-a-subclass-of-the-input-type; emitted `memory.failure_class` on BOTH classify-routed paths, `execute()` and the `validate()` pre-pass; and `_classify_provider_exception(exc) is None`) plus the reachability positive control (every shipped `MemoryToolName` member reaches its own arm, which doubles as the tripwire for a member added without one). Unlike the six dispatch sites this one IS on a classify-routed path, so the repair MOVES the emission to the residual rather than withdrawing one - conforming per C-MEM-19 v1.3''s own residual paragraph (*"a report ... is the ABSENCE of a claim"*) and consistent with its coverage paragraph, which forbids withdrawal. PD-8 probes, both restored: (a) revert the fall-through raise to the input type -> the three real-site witnesses go RED (the population-table row stays green, correctly - it pins a type''s declaration, not a raise site); (b) drop the internal type from the fail-fast tuple -> ONLY the retry-exit assertion goes RED while all three telemetry witnesses stay green, the discriminating shape the two independent obligations require. Zero existing witnesses weakened (full `harness-runtime` suite 3692 passed / 39 skipped); ruff + pyright clean. The `.harness/u-mem-25-memory-closeout-evidence.md` C-MEM-19 row and its recorded-residual paragraph were refreshed in place so the U-MEM-28 certification does not carry a stale open residual.'
  council: 'No. There is no nameable cross-domain tension: the contract rule is already ratified, the
    blast radius is one defensive branch and one witness row, and the only open judgement is the
    conformance-repair-vs-ratified-extension discriminator at close-out step (2), which is a governance
    reading to be resolved by grounding rather than a design fork between substantive alternatives.
    Escalates to a Class 2 fork ONLY if step (2) resolves to reading (b).'
  heading: '### B-114 · `_prepare`''s executor-side exhaustiveness fall-through raises the memory-tool INPUT-error type for a harness-internal fault, violating the C-MEM-19 type-boundary invariant v1.3 introduced *(surfaced at the B-88 spec leg, out-of-family Codex rounds 3/7/8, 2026-08-06; the re-typing was PROPOSED and WITHDRAWN there on authority grounds, registered)*'
- id: B-115
  title: 'Memory capture same-text / same-model-NAME / different-PROVIDER re-entry: a duplicate record JSONL line, then a TRANSIENT_RETRY staircase via MemoryToolExecutionStoreError'
  status: closed
  pr: '#1274 (the (b-prime) bundled leg -- Runtime spec v1.113 section 14.6.3 row 6 discharged +
    plan v2.61 U-RT-153 + the impl; spec, plan and impl land TOGETHER because row 6 conditions the
    contract text on a fact only the build can establish)'
  summary: 'MEASURED, not inferred -- executed at the B-84 build leg (PR #1243) as cell (3) of that leg''s
    W-5 four-cell matrix, and recorded there on the now-CLOSED B-84 row''s close_out. THE MECHANISM:
    a memory capture whose TEXT and model NAME are unchanged but whose PROVIDER differs re-derives
    the SAME memory_id (summary_model is hashed into the id; the provider is not), so the record
    JSONL line IS APPENDED A SECOND TIME, and the operation ledger then RAISES on its 18-field
    equivalence payload (because provider differs) -- surfacing as MemoryToolExecutionStoreError.
    WHY THAT IS THE LIVE HALF: _classify_provider_exception admits MemoryToolExecutionInputError and
    MemoryToolExecutionInternalError to its fail-fast branch BY NAME and deliberately NOT the family
    base, precisely so the denied and STORE subtypes are not swept in -- so the store error is NOT
    fail-fast, classifies TRANSIENT_RETRY, and RE-ENTERS the retry staircase. THIS ANSWERS B-84''s
    FILING D-2 FALSIFIER AFFIRMATIVELY: the landed fail-fast half IS re-enterable through the
    store-error class. STATED AGAINST INTEREST: this is a PRE-EXISTING residual that B-84''s
    ratified Reading A explicitly priced as OUT of its scope (A never claimed to close store
    errors), NOT a regression the build leg introduced and NOT a new defect -- B-84 left it OPEN and
    measured. WHY THIS ROW EXISTS: B-84 is CLOSED, so the residual currently lives only inside a
    closed row''s close_out and is not open-row-queryable. This row makes the live residual
    queryable at its own id. NO disposition is committed here. Ratified by the operator 2026-08-07
    on recommendation.'
  close_out: 'ALL FOUR registered steps EXECUTED; disposition (b-prime) BUILT; ROW CLOSED. (1) GROUNDING,
    by content at HEAD 1bce5d15 and NOT from this row''s own snapshot, which had drifted: the
    memory_id derivation re-read (_memory_id_for returns derive_memory_id over content_hash alone for
    non-EPISODIC_RUN kinds; capture_tool_event''s content dict carries summary_model and NOT provider,
    so provider is NOT among the hashed dimensions); the record-append path re-read (_JSONL_BY_KIND
    includes TOOL_EVENT; _append_jsonl is unconditional -- no dedup, no skip, no error); the ledger''s
    SINGLE conflict raise site and its two 18-field equivalence projections re-read (provider is field
    11 of 18; timestamp is EXCLUDED). THE ROW''S SNAPSHOT WAS STALE AND IS CORRECTED: it said
    _classify_provider_exception admits only the input and internal memory types; at HEAD it admits
    FIVE named types plus the 401/403 duck-typed arm, LLMDispatchPayloadShapeInternalError having been
    added by the B-116 impl leg (PR #1271) after this row was written. The store subtype was
    nonetheless re-confirmed ABSENT from that tuple, so the mechanism held unchanged -- and it was
    re-confirmed BY EXECUTION, not by reading: the landed W-5 cell-(3) witness ran green at HEAD.
    (2) DISPOSITION: (b-prime), as recommended at the #1265 grounding (commit 05abb419), which
    FALSIFIED reading (b)-as-registered -- making the whole store-error class non-re-enterable is
    wrong because that class also wraps genuinely transient store I/O a retry CAN clear. (b-prime)
    splits the DETERMINISTIC ledger-conflict case into a NEW sibling type and leaves the transient
    half untouched. Reading (a) -- provider joining the capture identity -- is NOT built and remains
    the registered residual; it is an identity-dimension change to a content-addressed memory_id and
    carries the full new-surface hash audit. Reading (c) declined. NO COUNCIL was owed: the council
    row made convening conditional on step (2) leaning (a); it leans (b-prime), which is within a
    cleared contract. (3) INTERACTION with B-84''s other two recorded-and-left-open residuals: NEITHER
    IS RE-OPENED OR CONTRADICTED, and the reason is structural rather than asserted. The policy-flip
    TOCTOU window lives between validate() and execute() at the POLICY RESOLVER and this leg touches
    no resolver; the intra-call window is the record-written-before-ledger-row ordering, which this
    leg PRESERVES BYTE-FOR-BYTE -- indeed AC #6 pins it, and the B-84 W-5 cell-(3) duplicate-line
    assertion (== 2) is PRESERVED VERBATIM precisely so the leg cannot claim a closure it did not
    deliver. What (b-prime) closes is the RETRY AMPLIFICATION that turned one duplicate line into
    max_attempts duplicates, not the duplicate line itself. (4) WITNESSED with the executed-matrix
    discipline B-84 used, including PD-8. Determinism -- which section 14.6.3 row 6 required but which
    NEITHER the spec NOR the council DELIVERABLE ever defined (REVIEW-e2.md F-05 flagged exactly that
    circularity) -- is discharged against a four-part definition this leg supplies and states at
    contract altitude: repeat-invariance (8/8 identical outcomes), candidate-independence (third and
    fourth candidates refuse identically), state-driven-not-stochastic (the origin candidate''s
    payload resolves as IDEMPOTENT_NOOP -- the positive control that stops repeat-invariance passing
    vacuously), and structural clock-exclusion (the 18-field set pinned BY NAME plus a source-level
    assertion that neither projection reads a clock). Witnessed at the LEDGER
    (harness-is/tests/test_b115_ledger_conflict_determinism.py) rather than at the executor, so the
    evidence survives any future re-typing of the executor surface. SCOPE WIDENED BEYOND THIS ROW''S
    OWN FRAMING, recorded rather than smuggled: the row named ONE executor surface; grounding found
    THREE, and the two it missed escaped RAW onto the retry staircase. Both were proven reachable BY
    EXECUTION before scope was fixed, per this row''s own requirement that a code-read inference is
    not sufficient -- _append_standard_tool_call (which runs after EVERY successful standard tool
    call, so a read-only memory.search replay from another candidate reaches it with no write_note
    involved) and _request_redaction. All three are covered. HONEST BOUNDARY, stated rather than
    hidden: the refusal is deterministic over the SAME inputs, not permanent under all inputs --
    re-presenting the originating candidate''s payload succeeds, and a re-sampled note text yields a
    different memory_id and no conflict. Neither is provider recovery, which is why section 14.6.3''s
    attributability clause still governs. ONE BUILD-TIME CORRECTION, recorded rather than
    absorbed: the obvious discriminator shape -- re-raising the ledger exception out of
    EpisodicMemoryCapture._capture, on the _repair_capture_operation precedent -- was tried and
    FALSIFIED by the existing suite. _capture has SIX public entry points and
    FAILED-on-conflict is a CONTRACTED outcome for at least two of them (U-MEM-26 / Codex
    R6+R8: a divergent second run-start must be REPORTED rather than read as completion; a
    non-run-start conflict must still surface as FAILED), each pinned by a landed witness whose
    docstring states that widening the catch would silently mask the divergence. The precedent
    does NOT transfer -- _repair_capture_operation has ONE caller and no contracted FAILED
    semantics. The discriminator is therefore carried as a VALUE (a closed
    MemoryCaptureFailureKind enum on the existing result model), leaving every caller''s
    control flow BYTE-UNCHANGED and NOT widening the closed two-value status enum. AC #9 pins
    it: the two U-MEM-26 witnesses pass UNMODIFIED. ROUND 2 COMPLETED THE REALIZATION rather
    than relaxing it: out-of-family review found the value discriminator had left the CAPTURE
    surface raising an UNCHAINED error while the two direct-append surfaces chained -- the same
    substrate event losing its traceback origin depending on which surface caught it. The
    capture result now also RETAINS the ledger exception (failure_cause, present IFF the kind
    is LEDGER_CONFLICT, enforced BOTH ways by a validator because the missing-cause direction
    would degrade the wrapper to raise-from-None, which SUPPRESSES the context rather than
    omitting it) and the wrapper chains from it. The plan AC was STRENGTHENED -- chaining on
    EVERY surface plus a joint symmetry witness, since the observed failure mode was
    two-of-three chaining, which per-surface witnesses alone do not catch -- NOT narrowed to
    fit what was built. ROUND 3 found the retained live exception had regressed the PUBLICLY
    EXPORTED result model''s model_dump_json and model_json_schema, both of which worked before
    this leg (the schema break at CLASS level, so every instance). ROUND 2''S GROUNDING IS
    RETRACTED AS TOO NARROW: it verified that no CURRENT consumer serializes the model, which
    is true and beside the point for an exported type whose Pydantic APIs are part of its
    contract regardless of today''s callers -- the same defect shape the workspace polices in
    the B-101 / B-107 serialization-boundary lineage. Fixed by EXCLUSION (SkipJsonSchema plus
    Field(exclude=True)) rather than a string field-serializer: failure_reason already carries
    the same fact verbatim, so a serialized copy would be a second drifting authority, while
    exclusion loses nothing because the exception object serves chaining, which has no JSON
    meaning. The in-process object is still present and witnessed. ROUND 4 returned TWO findings,
    both real, and together they showed the round-2 and round-3 fixes COLLIDING: the IFF cause
    rule plus the wire-form exclusion made model_validate_json(model_dump_json(...)) raise for
    EVERY conflict result; and independently a CAPTURED result accepted a failure_kind while a
    FAILED one accepted none. A THIRD contradiction (CAPTURED carrying a failure_reason) was
    found by this leg''s own enumeration and closed with them. RESOLVED AS A WHOLE rather than
    corner-patched a third time -- three clauses in ONE validator; the cause rule made ONE-WAY
    because the reverse cannot survive deserialization by construction, that direction re-homed
    to a producer-side witness; CAPTURED-implies-no-cause left DERIVED from the other two and
    asserted as a derived row; the wrapper raise guarding on cause-presence so it chains when
    present and raises plainly when absent, NEVER from None. Clause (b) was verified TOTAL at
    the producers rather than assumed (all four construction sites read; both FAILED sites set
    a kind unconditionally), and failure_reason deliberately NOT required on FAILED because
    automatic_memory._raise_capture_failure supplies its own default. ONE deliberate
    whole-contract pass closed the round: FOURTEEN (status x kind x cause x reason) rows, four
    legal and ten refused, in a single parametrized witness plus a full round-trip witness for
    every legal shape -- the shape that stops a fifth corner. ARTIFACTS: Runtime spec v1.113 (three amendment sites, all
    inside section 14.6.3 -- row 6 discharged, guard tuple four -> five with a sibling-not-subclass
    constraint, and the declared residual narrowed), plan v2.61 U-RT-153 (a NEW unit, NOT an amendment
    of the LANDED-and-CLOSED U-RT-152, per the B-97(a) -> U-RT-149 and B-111 -> U-RT-151 precedent),
    and both clearance markers. B-132 is NARROWED, NOT CLOSED. B-134 minted for the dedicated
    C-MEM-19 ledger_conflict class question, which is an operator-gated Class 2 decision this leg may
    not take. ZERO contract numbers, ZERO Memory-spec leg, ZERO CXA rows (111), ZERO hash impact.
    THE PRE-DISPOSITION REGISTERED STEPS FOLLOW, retained as history. (1) GROUND FIRST at the then-current HEAD, by content: re-read the memory_id derivation (which
    dimensions are hashed, and specifically whether provider is among them), the record-append path,
    and the operation ledger''s 18-field equivalence payload; and re-confirm that
    _classify_provider_exception still admits only the input and internal memory types BY NAME so
    the store subtype still classifies TRANSIENT_RETRY. If any of those has changed, the cell-(3)
    mechanism may no longer hold and this row closes with that finding recorded. (2) DECIDE THE
    DISPOSITION. The candidate readings, none pre-selected: (a) make provider part of the capture
    identity, which collapses cell (3) into cell (1) -- an EXTRA DISTINCT note rather than a
    duplicate -- but is an IDENTITY-DIMENSION change to a content-addressed id and therefore carries
    the full new-surface hash audit (does the id capture the new dimension; what happens to
    already-persisted records); (b) leave the identity alone and make the store-error class
    non-re-enterable (fail-fast, or a dedicated non-retryable disposition), which addresses the
    STAIRCASE half but not the duplicate JSONL line; (c) accept and document, on the grounds that
    the duplicate line is inert and the ledger raise is the honest outcome. (3) NOTE THE INTERACTION
    with B-84''s other two recorded-and-left-open residuals (the policy-flip TOCTOU window and the
    intra-call window) -- a disposition here should not silently re-open or contradict those. (4)
    WITNESS whatever is chosen with the same executed-matrix discipline the B-84 leg used, including
    a PD-8 mutation probe; a code-read inference is not sufficient for this row, since its entire
    value is that cell (3) was EXECUTED rather than reasoned about.'
  council: 'NO at registration -- this row only makes a measured residual queryable, and framing it commits
    to no design. CONDITIONALLY YES at step (2): reading (a) is an identity-dimension change to a
    content-addressed memory_id, which trades capture fidelity against the stability of an
    already-persisted address space -- a nameable tension between the information-substrate
    integrity voice (C3) and whichever voice owns memory-capture semantics. Convene ONLY if step (2)
    leans (a), and only after step (1)''s grounding, per the workspace probe-first discipline.
    Readings (b) and (c) are within a cleared contract and owe no convening.'
  heading: '### B-115 · Memory capture same-text / same-model-NAME / different-PROVIDER re-entry: a duplicate record JSONL line, then a `TRANSIENT_RETRY` staircase via `MemoryToolExecutionStoreError` *(MEASURED at the `B-84` build leg, PR #1243, W-5 cell (3); ratified by the operator 2026-08-07; **CLOSED at the (b′) split — Runtime v1.113 §14.6.3 row 6 discharged + plan v2.61 U-RT-153, waiver tuple 4 → 5**)*'
- id: B-116
  title: 'Should DETERMINISTIC harness-internal faults count toward a PROVIDER''s circuit-breaker state?'
  status: closed
  pr: '#1271 (the U-RT-152 impl leg -- Runtime guard + LLMDispatchPayloadShapeInternalError re-type + t1/t2 waived-charge emission; merge commit 3ba68285) + #1272 (the B-116-t3 OD leg -- OD spec v1.37 section 9.2 roster 18->19 + plan v2.32 U-OD-58 + the roster/fixture impl). Section 14.6.3 t3-prime required BOTH; with both landed the row closes.'
  summary: 'THE QUESTION, and this row frames it rather than answering it. When the retry/breaker/fallback
    dispatcher fail-fasts a candidate, it records ONE breaker failure against that candidate''s
    PROVIDER before exiting. GROUNDED BY CONTENT at HEAD 73b0e87b (the inbound note''s ~:1025 is
    CORRECTED -- :1025 is the ''retry.fail_class'' attribute-set line): retry_breaker_fallback.py
    computes breaker_cause = _classify_breaker_cause(exc) at :1006, and on the cause-None
    (fail-fast) branch calls transition = breaker.record_failure(cause=breaker_cause) at :1028. THE
    FAIL-FAST SET (_classify_provider_exception, from :272) is FIVE members:
    LLMDispatchProviderUnreachableError; LLMDispatchPayloadShapeError; a raw SDK exception with
    .status_code in (401, 403); MemoryToolExecutionInternalError; and MemoryToolExecutionInputError.
    THE SHARPENED FRAMING, which is finer than ''internal faults are tagged as provider faults'':
    _classify_breaker_cause is duck-typed on .status_code and returns None for anything without one
    (:404-406), so a MemoryToolExecutionInternalError yields cause=None -- it is NOT mis-tagged. But
    BreakerRegistry.record_failure (retry_breaker.py:219) increments fail_count REGARDLESS of cause
    -- cause is telemetry-only (:228-233) -- so the failure still counts toward fail_threshold and
    still moves the provider toward OPEN. THE TENSION: MemoryToolExecutionInternalError is BY
    CONSTRUCTION a harness-internal wiring fault (an unset RuntimeMemoryContext.record_scope /
    .scope_ref; a MEMORY_TOOL_CONTRACTS entry whose input schema has no properties mapping) and is
    admitted to fail-fast on the express grounds that it is ''deterministic and
    candidate-independent''. A deterministic, candidate-independent, harness-internal fault is not
    evidence about the PROVIDER''s health -- yet it consumes provider breaker budget, and on a
    repeated internal misconfiguration could open a healthy provider''s breaker. PRE-EXISTING, NOT
    INTRODUCED by any recent arc; spans C-CP-21 and Runtime section 14.6 (Runtime + CP). RATIFIED
    Reading (II) by the operator 2026-08-07 -- a breaker failure means ''this provider-model is
    unhealthy'', per the council-unanimous DELIVERABLE at
    .harness/council/b116-breaker-semantics/. APPLIED at the ratification leg: Runtime spec v1.112
    (NEW section 14.6.3 -- the recovery-model normative test, the 7-row per-member disposition
    table, the four-type waiver tuple BY NAME, terms t1-t5, the persistence pin, the
    trip-threshold revisit-trigger) + Runtime plan v2.60 (NEW U-RT-152 -- the guard ~30-35 src
    lines across retry_breaker_fallback.py + llm_dispatch.py incl. the row-2a re-type to NEW
    LLMDispatchPayloadShapeInternalError, Probes B/C as assertions, positive controls, the 3/25
    raise-site partition witness, PD-8). Closes ONLY when BOTH U-RT-152 AND the B-116-t3 OD leg
    (section 9.2 roster 18->19, a NAMED closure gate per section 14.6.3 t3-prime) merge.'
  close_out: 'ALL FOUR registered steps EXECUTED. (1)+(2) at the grounding leg (main 35afeda1):
    membership, duck-typed None return, cause-None charge and count-regardless-of-cause all
    re-verified by content; the contract found SILENT everywhere -- the silence was the finding.
    (3) decided via the operator-convened FULL council (E1 primaries C9/C11 -> consultants C1/C7
    -> cross-read -> E2 adversarial 8 findings incl. one halt-class -> operator-authorized E2b
    reconcile; UNANIMOUS) and operator ratification 2026-08-07: Reading (II). (4) honored
    PER-MEMBER at spec section 14.6.3''s 7-row table -- 401/403 and the 25 response-parsing
    payload-shape sites STILL CHARGE; the four harness-internal types are WAIVED by name.
    BOTH CLOSURE LEGS HAVE NOW LANDED, so the row CLOSES. (i) The U-RT-152 impl leg merged at
    PR #1271 (merge commit 3ba68285) -- the guard, the row-2a re-type to
    LLMDispatchPayloadShapeInternalError, the t1+t2 waived-charge emission, Probes B/C as
    assertions and the 3/25 raise-site partition witness. (ii) The B-116-t3 OD leg lands at THIS
    PR -- OD spec v1.37 amends C-OD-09 section 9.2 18 -> 19 (adding fallback.exhausted, source
    declaration C-CP-03 section 3.5), OD plan v2.32 adds U-OD-58, and the substrate roster +
    five fixture edit points + two swept src count claims are reconciled in the same commit. The
    C-c7-reconcile rider (a) -- the multi-tenant per-cell cardinality-budget check registered as
    owed BEFORE the leg lands -- was EXECUTED and PASSES on the structural ground that the
    C-OD-11 section 11.1 budget enforces at COLLECTOR_BOUNDARY / BACKEND_INGESTION, downstream of
    and independent of the sampling decision, while fallback.exhausted stays a terminal
    once-per-exhausted-chain event with one emission site and no per-attempt multiplier; the F-08
    population question remains explicitly unmeasured per C7''s own FM-K refusal. Riders (b)/(c)/(d)
    are cross-referenced to the already-minted rows B-123 / B-124 / B-125 and were NOT re-minted;
    none is a precondition of the roster row. SEQUENCED AFTER: B-115 (b-prime) -- its type joins the waiver tuple only on
    confirmed determinism at its own build (racy => section 14.6.3 row 6 VOID + C5 reroute).
    FIFTEEN forward rows B-118..B-132 minted at the ratification-leg PR -- B-118..B-131 from
    the DELIVERABLE''s forward-row enumeration, B-132 the catch-all residual surfaced at the
    leg''s fresh-context review.'
  council: 'NO at registration -- the row frames a question and commits to nothing, so there is no decision
    for voices to disagree about yet. CONDITIONALLY YES at step (3), and the tension is nameable in
    advance: changing what counts toward a breaker trades control-plane reliability semantics (C1/C9
    -- a breaker that under-counts fails to protect a genuinely failing chain) against operator-loop
    robustness (C11 -- a breaker that over-counts opens a healthy provider on the operator''s own
    misconfiguration). Convene ONLY if step (2) finds the contract silent AND step (3) leans toward
    changing the counting rule; if C-CP-21 or Runtime section 14.6 already settles the meaning, this
    is conformance work and owes no convening. RESOLVED: step (2) found the contract SILENT and
    step (3) leaned toward changing the counting rule, so the condition FIRED -- the operator
    convened the FULL workflow (not merely the conditional dyad) and it ran to a UNANIMOUS
    corrected package; the nameable C1/C9-vs-C11 tension resolved in favor of reading (II) with
    C9''s and C11''s conditional sign-offs honored structurally (the t3 closure gate + the
    trip-threshold revisit-trigger at section 14.6.3).'
  heading: '### B-116 · Should DETERMINISTIC harness-internal faults count toward a PROVIDER''s circuit-breaker state? *(pre-existing; spans C-CP-03 §3.4+§3.5 + Runtime §14.6; RATIFIED Reading (II) 2026-08-07 — spec v1.112 + plan v2.60 landed; U-RT-152 impl leg merged #1271; **CLOSED at the `B-116-t3` OD leg — OD v1.37 §9.2 18 → 19 + plan v2.32 U-OD-58**)*'
- id: B-117
  title: Duplicate pytest test-file basenames across workspace packages silently drop witnesses from collection
  status: closed
  summary: 'LIFTED to its own row 2026-08-07 from the B-112 close-out''s registered ADJACENT INFRA GAP
    (merge-gate lens 3, G3 at the B-93 build leg), per that row''s own "a future infra arc may lift
    it to its own row". THE DEFECT CLASS: every harness-*/tests/ directory is a package literally
    named `tests` (all seven carry an __init__.py), so two test files sharing a BASENAME across two
    packages resolve to the SAME module path -- pytest imports ONE module for BOTH paths and
    SILENTLY DROPS the loser''s tests. It is not hypothetical: at the B-93 build leg (#1241)
    test_b93_cross_process_lock_deadline.py existed in both harness-is/tests/ and
    harness-runtime/tests/, and 12 written witnesses never ran (34 collected where 35 were written).
    The suite was GREEN THROUGHOUT; it was caught only by recounting programmatically. WHY IT IS
    SILENT IN THE WORST DIRECTION: it removes coverage while every signal -- exit code, pass count,
    CI green -- says nothing is wrong. --import-mode=importlib IS ALREADY SET AND DOES NOT FIX IT,
    REPRODUCED at HEAD e711a857 and stated against the B-112 close-out''s own candidate list: root
    pyproject.toml:380 carries addopts = "--import-mode=importlib" (comment at :377-379 naming
    exactly this collision), yet a positive-control probe -- one test_zz_collision_probe.py in
    harness-is/tests/ (1 test) and another in harness-runtime/tests/ (2 tests) -- collected 2 OF 3
    and reported harness-runtime/tests/test_zz_collision_probe.py::test_is_side_probe, i.e. the
    runtime path resolved to the IS module''s contents. The __init__.py-bearing `tests` packages
    defeat the import-mode fix. Probe files were removed after the measurement; nothing was left in
    the tree. THE INVARIANT THE TREE ACTUALLY RELIES ON is UNIQUE TEST BASENAMES REPO-WIDE --
    CURRENTLY TRUE (511 harness-*/tests/**/test_*.py files, zero duplicate basenames, measured at
    HEAD e711a857), CURRENTLY UNENFORCED, and violated SILENTLY rather than loudly. NO COMMITMENT to
    a particular guard shape is made here.'
  close_out: '(1) RE-MEASURE FIRST at the then-current HEAD -- re-run the basename-uniqueness count and
    the positive-control collision probe; if a duplicate already exists, the guard''s first run is
    also a repair. (2) PRICE THE GUARD against the three candidate shapes the B-112 close-out named,
    now that one of them is falsified: a BASENAME-UNIQUENESS CHECK across harness-*/tests/** (the
    cheapest -- a ~15-line conftest.py collection hook or a standalone CI step, and the only shape
    the reproduction above leaves standing); PER-PACKAGE PYTEST ROOTDIR ISOLATION (heavier -- changes
    how the whole suite is invoked); or --import-mode=importlib WITH UNIQUE PACKAGE NAMES (the
    import-mode half is already in place, so this reduces to RENAMING the seven `tests` packages, a
    tree-wide rename, priced accordingly). (3) DECIDE THE FAILURE VENUE -- a collection-time hard
    failure (loud, blocks the developer immediately, but fires inside every pytest invocation)
    versus a dedicated CI step (cheaper at inner-loop time, later feedback). (4) WITNESS IT UNDER
    PD-8: the guard MUST be shown to FAIL on a deliberately introduced duplicate basename and pass
    once it is removed -- a guard that has never been seen red is not a guard. TRIGGER: the next
    test-infrastructure arc, OR any recurrence (a duplicate basename appearing, or a
    written-versus-collected count mismatch surfacing at any leg).'
  council: 'No. A test-infrastructure hygiene guard with no cross-domain tension to name -- the only
    tradeoff is inner-loop cost versus feedback latency (close-out step 3), which is a single-domain
    judgment, not a nameable dyad. Route to a single voice plus advisor() if it is contested at all.'
  pr: '#1315'
  heading: '### B-117 · Duplicate pytest test-file BASENAMES across workspace packages silently drop witnesses from collection, and nothing guards against it *(lifted to its own row 2026-08-07 from the `B-112` close-out''s registered ADJACENT INFRA GAP, per that row''s own "a future infra arc may lift it to its own row")*'
- id: B-118
  title: 'The half-open latch is dead code, so OPEN is an absorbing breaker state for the process lifetime'
  status: closed
  pr: '#1275 (the B-118 bundled leg -- Runtime spec v1.114 section 14.6 step 4 three-way breaker pre-check + NEW section 14.6.4 nine-cell trial-outcome matrix + the dead-half-open-latch residual DISCHARGED, plan v2.62 U-RT-154, and the impl with both recovery directions witnessed end-to-end through the real composer)'
  summary: 'WHAT IT IS. BreakerStateMachine.attempt_half_open()
    (harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py:289) has ZERO production call sites
    — the only three occurrences across every harness-*/src tree are two docstrings (:51, :201) and the
    definition itself (:289), re-verified at HEAD this session. should_attempt() (:309-311) is a pure
    state read that holds no clock, and RuntimeRetryBreaker.get_breaker (:379-400) caches breakers
    identity-stable for the process. Therefore OPEN is an ABSORBING state for the process lifetime at
    HEAD — the shipped breaker is a one-way latch. The contract already writes text for the transition
    the runtime never performs: OD spec v1.32 §7.1 names the "caller-driven open → half_open
    cooldown-elapsed transition" (design-substrate/Spec_Operational_Discipline_v1_32.md:46), and
    record_failure''s own half_open → open arm (retry_breaker.py:239-251) is unreachable for the same
    reason. PRIORITY RAISED, AND WHY. This was C9''s A1 recommendation 3 (register separately, do not
    fold into B-116). At the E2b reconcile it was raised to the highest-priority forward row by finding
    F-06: B-116''s ratified normative test is stated over exactly this path — "a fault charges the
    provider-model breaker only if a half-open trial call could return a different result than the trip
    did, for a reason attributable to the {provider, model} the breaker is keyed to." The test is not
    void (OD §7.1 contracts the transition, so it stands as design intent), but B-116 now depends on a
    recovery path with no production caller. C9 stated the dependency direction plainly at cross-read:
    ratifying a reading whose cost is a function of the latch ratifies the latch''s consequences without
    ratifying the latch. SURFACED AT THE B-116 COUNCIL DELIBERATION, C9 A1 primary recommendation 3,
    priority RAISED at the E2b reconcile per adversarial finding F-06, 2026-08-07.'
  close_out: '(1) Re-verify zero production call sites and that get_breaker still returns identity-stable
    machines. (2) Decide who holds the clock — the state machine deliberately holds none
    (retry_breaker.py:290-296 assigns it to the L8 orchestrator), so the caller must thread one; C9''s
    Pillar-4 requirement is half-open after the cooldown, one trial call, close on success, re-open on
    failure, which attempt_half_open / record_success / record_failure already implement. (3) Wire it at
    or beside the breaker pre-check (retry_breaker_fallback.py:674). (4) Witness both directions — a
    real open → half_open → closed recovery AND an open → half_open → open re-trip; a green unit test on
    the state machine alone is not the witness, because the state machine is already green and the gap
    is reachability. (5) Re-ground B-116''s witnesses — C9''s A1 recommendation 2 forbade asserting a
    half-open path while it is unreachable, so B-116 asserts fail_count == 0 and state is CLOSED
    instead; once this row lands those may be strengthened, and they should be, deliberately. WHAT WOULD
    FALSIFY IT. A production call site appearing (then the row is already closed), or a decision that
    absorbing-OPEN is intentional — in which case OD §7.1''s transition text and B-116''s normative test
    both need amending, which is a Class 1 design-substrate route, not an impl fix. CLOSED 2026-08-08 at
    Runtime spec v1.114 + plan v2.62 (U-RT-154), spec + plan + impl in ONE PR. ALL FIVE CLOSE-OUT STEPS
    EXECUTED. (1) Re-verified at HEAD fb3dda44 -- zero production call sites (the only src occurrences
    were two docstrings and the definition), should_attempt() clock-free, get_breaker identity-stable.
    (2) The clock is SPLIT. The COMPOSER holds the source (an injectable monotonic field defaulting to
    time.monotonic, mirroring the existing sleep_fn posture); the STATE MACHINE holds only the
    caller-recorded opened_at deadline, because that state is per-(scope, identifier) and the composer
    has no per-breaker storage -- a composer-side map would be a SECOND authority for breaker state. The
    machine still never reads a clock. NOT a RuntimeConfig scalar. The in-tree shape template is
    BreakerGuardedSigningBackend at harness-runtime/src/harness_runtime/config/audit_signing.py, which
    has carried an injected-clock plus single-probe breaker since ADR-D8. (3) Wired at the pre-check,
    now THREE-WAY -- closed proceeds / open attempts a conjunctively-gated trial capped at ONE attempt /
    half_open means a sibling holds the permit and is skipped with the new half-open-trial-in-flight
    reason. should_attempt() narrows to state-is-closed, which IS the permit, since half_open exists
    only while a trial is in flight and the prior state-is-not-open admitted every concurrent branch
    into a call contracted to be single. No lock -- all mutators run on one event loop, so the hazard is
    interleaving at the await rather than a data race (contrast the signing breaker, which DOES lock
    because span-end worker threads sign). (4) BOTH directions witnessed end-to-end through the REAL
    composer with an injected clock -- open to half_open to closed recovery AND open to half_open to
    open re-trip -- plus a three-branch concurrency witness whose interleaving is GUARANTEED by an
    asyncio.Event rather than hoped for. (5) The B-116 witnesses were strengthened DELIBERATELY -- seven
    rows gained their now-available assertions (the Probe-C clock-advance form discriminating
    never-opened from opened-and-reset; the 401 and response-parsing RECOVERY-COMPLETION positive
    controls that make the charge mean something; the exhaustion-identity admissibility loop), three
    were left unchanged by name, and the B-115 IS-side determinism witnesses were untouched. THE ONE
    OPERATOR GATE. Grounding surfaced an interaction the ratified Reading (II) does NOT answer. With the
    B-116 guard, a WAIVED fail-fast during a trial charges nothing, so record_failure never runs and the
    machine is STRANDED in half_open -- a NEW absorbing state strictly worse than the absorbing OPEN
    this row removes, and one the wiring would itself have introduced. Four further cells (audit-signing
    hard failure, terminal HITL control flow, DispatchFenceTrippedSignal, CancelledError) strand
    identically. INCONCLUSIVE-then-re-arm is derivable from the Reading (II) attributability clause; the
    FRESH-versus-PRESERVED cooldown choice is NOT, and was surfaced. OPERATOR-RATIFIED 2026-08-08
    (AskUserQuestion) -- FRESH cooldown, trigger_count 0, fail_count UNCHANGED. All nine cells are
    contracted at NEW Runtime spec v1.114 section 14.6.4. THE COUNCIL CONDITION DID NOT FIRE -- the
    design is in-memory monotonic with nothing persisted, so the durable-cooldown-clock trigger for the
        C9-C11 dyad was never reached and no council was convened. OUT-OF-FAMILY REVIEW ROUND 1 -- TWO
    findings, both adjudicated REAL and fixed on the branch. (i) THE STALE-OUTCOME RACE. The first build
    wired the permit as one-admission-at-a-time and DROPPED the epoch guard this row''s own grounding
    had cited from the in-tree BreakerGuardedSigningBackend template, which carries it for exactly this
    race. A request admitted while the breaker was CLOSED can still be awaiting its provider when
    siblings trip it and a LATER request admits the trial; a stale SUCCESS then closes an unhealthy
    breaker, and the real trial''s failure lands on a CLOSED machine as one ordinary failure under
    fail_threshold. Fixed by a per-breaker admission EPOCH -- attempt_half_open increments it, the
    admitted caller captures it as a trial token, and record_success / record_failure /
    re_arm_half_open_trial resolve a HALF_OPEN machine ONLY on the current epoch. Stale outcomes are
    DROPPED with no transition, no state change and no fail_count movement (the same treatment an
    outcome arriving while OPEN already got, and for the same reason). CLOSED-state accounting stays
    tokenless and BYTE-UNCHANGED. Contracted at 14.6.4, planned at AC #11, witnessed at BOTH altitudes
    incl. the exact A/B interleaving end-to-end through the real composer, with four new PD-8 probes
    P11-P14. (ii) CELL 4 WAS UNREACHABLE -- under the ratified ONE-attempt trial cap the trial''s only
    attempt is necessarily is_last_attempt, so a transient on it takes the exhaustion branch and IS cell
    5. The cap is the ratified term, so the cell is marked VOID BY CONSTRUCTION with its derivation and
    is NOT renumbered, keeping cell numbering stable against the plan, this register and every citing
        witness. The matrix is nine cells declared, EIGHT live. ROUND 2 (same review): one mechanical
    count reconciliation (11 ACs / 14 probes) and one CROSS-TRANSITION stale-outcome variant the epoch
    guard does NOT cover -- a request admitted while CLOSED that survives a trip AND a recovery, then
    resets fail_count on the tokenless CLOSED arm. ADJUDICATED PRE-EXISTING AND REGISTERED, NOT ABSORBED
    -- the CLOSED-arm success-reset is shipped consecutive-failure semantics reachable with zero trips,
    and guarding it would mean epoch-capturing every ordinary dispatch and rewriting contracted
    success-reset semantics, a redesign this leg does not price. Declared at Runtime spec v1.114 section
        14.6.4''s honest-boundary paragraph and minted as forward row B-135. ROUND 3 (same review): ONE
    finding, REAL -- the round-1 release obligation guarded the DISPATCH but left the admission''s own
    transition EMISSION outside it, so a raising span exporter stranded the shared breaker in HALF_OPEN
    with no owner: the process-lifetime absorbing latch this row removes, recreated by its own fix, and
    strictly worse than absorbing OPEN because the re-arm refuses a non-owner too. Fixed structurally --
    the protected region now begins at the TRANSITION rather than at the dispatch, protection state is
    captured with no statement in between, and the emission plus the capped-policy construction run
    inside a guard that re-arms (STATE-ONLY, so the broken emitter is not re-entered and the original
    fault is not masked) before re-raising. The other four transition emissions were SWEPT and verified
    to follow their state mutation, so the admission is the one site where the ordering is structurally
    impossible. AC #12 + probe P15 added; unit now TWELVE ACs / FIFTEEN probes, with the closure
        criterion restated over the SETS rather than a count. ROUND 4 (same review): ONE finding, REAL
    -- the MIRROR of round 3 at the inconclusive EXITS. When a trial exits through cells 6-8 and the
    re-arm''s own transition emission failed, the emitter error raised BEFORE the bare raise and
    REPLACED the propagating exception -- AuditSigningFailedError, the terminal HITL signals and
    DispatchFenceTrippedSignal are each contracted to travel verbatim, so a terminal RT-FAIL-* mapping
    and the fence''s at-most-once effect semantics were being traded for a telemetry fault. Fixed by a
    disposition flip keyed on whether an exception is IN FLIGHT rather than a blanket guard: with one
    present the emitter failure is SUPPRESSED and attached to the propagating exception via add_note
    (suppressed is NOT discarded); with none present it still propagates as its own fault, matching
    main''s unguarded posture at every other harness.breaker.* emission site -- a blanket rule would
    hide exporter breakage on the common path. Cell 9 VERIFIED to emit nothing by construction; the
    success/charging emissions SWEPT and left unchanged, none of them sitting on a path that must
    propagate a terminal exception verbatim. AC #13 + probe P16 added; unit now THIRTEEN ACs / SIXTEEN
        probes under the set-phrased closure criterion. ROUND 6 (same review): ONE finding whose fix is
    the CONTRACT rather than the code. A dispatch admitted while the breaker was CLOSED keeps its
    multi-attempt policy, so if siblings trip the breaker and a later dispatch takes the trial permit,
    the stale dispatch resumes attempt 2 with NO breaker re-consultation -- a concurrent PAID call
    overlapping the single trial. The non-re-consultation is PRE-EXISTING (the pre-check is
    PER-CANDIDATE by contract at section 14.6 step 4; the pre-v1.114 loop behaved identically), so this
    leg neither introduced nor priced it. What round 6 caught is that the section 14.6.4 text THIS LEG
    authored OVERCLAIMED -- C9 Pillar-4 and the cell-3 ratification bind THE TRIAL to one call and never
    deliberated stale CLOSED-era dispatches, so reading single as process-wide was an unratified
    widening introduced by the WORDING. Section 14.6.4 now scopes exclusivity to ERA-CONSISTENT
    admissions, the step-4 bullet carries the matching qualifier, and AC #14 PINS the declared behaviour
    by witness (both inner-call counts asserted) so it cannot drift back into an overclaim.
    Mid-staircase re-consultation was REFUSED as unpriced semantic surgery -- it would abandon a
    candidate partway through its attempt loop and change exhaustion and fallback.last_failure_class
    accounting for a pre-existing path. Folded into B-135 rather than minted separately: round 2
        registered the OUTCOME side, round 6 adds the ATTEMPT side, one family two sides. MERGE GATE
    (#1275, 3-lens decorrelated): lens 1 APPROVE, lens 2 APPROVE after a one-line md-mirror permutation
    (a B-118 narrative bullet had been filed under the B-119 heading -- a blind spot of the heading-only
    --check), lens 3 BLOCK on a MEASURED witness-adequacy gap and the most useful finding of the leg.
    Deleting trial_token from the max-attempts-exhaustion record_failure left the WHOLE SUITE GREEN
    while fail_count went 3 to 2 and trigger_count 3 to 0, because the cell-5 witness asserted only
    state-is-OPEN and a fresh opened_at -- both equally true of an INCONCLUSIVE RE-ARM, so it could not
    tell a real exhaustion charge from a stale-dropped one. Cell 5 was the single cell in the nine-cell
    matrix carrying no fail_count/trigger_count assertion. Closed with those two assertions plus NEW
    probe P17; the staircase escalation site''s unwitnessed token threading resolved by DETERMINING
    unreachability from content (the classifier''s only non-None return is TRANSIENT_RETRY, the composer
    always passes STAGE_1_REFLEXION, and that pair maps to STAGE_2) and PINNING the determination by
    witness; cells 7 and 8 given the opened_at-FRESH assertion AC #6 already required of every
    inconclusive cell; both BaseException-boundary asymmetries (narrow suppression at the release, wide
    guard at the admission) witnessed rather than recorded as gaps. NO contract text changed -- the
    spec''s cell-5 disposition was correct as written; what was missing was the assertion that the
    implementation delivers it. Unit now FIFTEEN ACs / SEVENTEEN probes.'
  council: 'No at registration. A single-domain C9 reliability-mechanism gap with no nameable cross-domain
    tension — the mechanism, the contract text, and the recovery model are all in one voice''s lineage.
    Conditionally yes only if step (2) proposes a durable cooldown clock: that re-opens the persistence
    tension B-116 term t4 pinned (a durable breaker opened by a fault the operator cannot see survives
    the restart the operator reaches for first), which is a genuine C9 ⊥ C11 dyad.'
  heading: '### B-118 · The half-open latch is dead code, so `OPEN` is an absorbing breaker state for the
    process lifetime *(surfaced at the B-116 council deliberation (C9), 2026-08-07)*'
- id: B-119
  title: 'No per-provider breaker is constructed anywhere, so BreakerScope.PER_PROVIDER is a
    declared-but-unbuilt scope'
  status: closed
  pr: '#1277 (the B-119/B-125 observability-family disposition leg -- doc/register only, no code and no
    design-substrate edit). Closed as a FORWARD-ONLY RECORD in the capability_shortfall shape
    (Spec_Operational_Discipline_v1_32.md:50), NOT as built. The enum member stays a forward-compatible
    slot with a four-disjunct demand trigger stated at close_out; a future arc electing BUILD re-opens
    the row rather than amending this closure.'
  summary: 'WHAT IT IS. BreakerScope declares two values — PER_MODEL and PER_PROVIDER
    (harness-od/src/harness_od/harness_breaker_schema.py:105-112) — and both are contract-declared: CP
    §3.5 gives harness.breaker.scope ∈ {per_model, per_provider}
    (design-substrate/Spec_Control_Plane_v1_2.md:402, re-tabled at Spec_Control_Plane_v1_3.md:75) and OD
    §7.1 mirrors it (Spec_Operational_Discipline_v1_32.md:38). The only breaker constructed anywhere is
    PER_MODEL, at retry_breaker_fallback.py:662-665, keyed f"{candidate.provider}:{candidate.model}"
    (:662). C9 records this as his own FM-O (single-breaker-scope), live at HEAD. WHY IT MATTERS, IN
    B-116''S OWN TERMS. With no provider-scope aggregation, the only provider-wide health signal an
    operator can get today is "every per-model breaker in the chain opened" — which is precisely the
    signal a candidate-independent fault manufactures. B-116''s executed Probe B showed one dispatch
    burning one failure on every chain breaker across both providers, and Probe C showed fail_threshold
    repeats opening them all. So the absent scope is not merely unbuilt; its absence is what let a
    config-shaped fault imitate a provider outage. SURFACED AT THE B-116 COUNCIL DELIBERATION, C9 A1
    primary (FM-O), re-registered at the E2b reconcile as R2, 2026-08-07.'
  close_out: '(1) Re-verify that PER_MODEL is still the only constructed scope. (2) Decide whether PER_PROVIDER
    should exist at all — either build it (a second get_breaker(BreakerScope.PER_PROVIDER, provider)
    charge alongside the per-model one, with its own threshold and its own trip semantics) or record the
    enum member as deliberately forward-only in the same shape OD v1.32 used for capability_shortfall
    (Spec_Operational_Discipline_v1_32.md:50). Do not leave it declared-and-silent. (3) If built, price
    the B-116 interaction explicitly: the ratified guard is stated on record_failure at
    retry_breaker_fallback.py:1028, and a second charge site must inherit the same negative predicate —
    otherwise the four demoted harness-internal members re-enter the breaker through the provider scope
    and B-116 is silently undone. (4) Witness scope discrimination at the emission, which today can only
    ever report per_model. (5) Note the coupling to B-125 — emit_breaker_transition_event defaults
    tool_id to the breaker key only at PER_MODEL scope (retry_breaker.py:459-461), so constructing a
    second scope changes which trips carry that default. WHAT WOULD FALSIFY IT. A PER_PROVIDER
    construction site appearing anywhere in harness-*/src. CROSS-NOTE, 2026-08-08 (B-118 closed) -- this
    row is UNTOUCHED and NOT dependent on B-118, but its C11 half is materially changed. The registered
    council framing weighed detection breadth against the fact that one provider-scope OPEN disables
    every model of that provider at once, AND WITH B-118 UNFIXED IT DOES SO IRREVERSIBLY. B-118 is now
    closed -- OPEN is escapable, bounded by a real cooldown with a single-trial half-open probe -- so
    the irreversibility clause no longer holds and the C11 objection is weaker than when it was
    registered. The dyad is NOT dissolved (a provider-scope breaker still has a wider blast radius than
    a per-model one, and a recovery probe still costs one real call), but any future step-(2)
    deliberation MUST re-ground the C11 position against escapable-OPEN rather than quoting the
    registered text. GROUNDED + DISPOSITIONED 2026-08-08 (#1277) -- FORWARD-ONLY RECORD. This paragraph
    states the CURRENT result; the registration text above is preserved and superseded where it
    conflicts. STEP (1) RE-VERIFIED AT HEAD 49591934. PER_MODEL is still the ONLY constructed scope: the
    sole production get_breaker call is retry_breaker_fallback.py:774-776 (breaker_identifier =
    f"{candidate.provider}:{candidate.model}" at :774, then get_breaker(BreakerScope.PER_MODEL,
    breaker_identifier) at :775-776). The registration cite :662-665 is LINE-DRIFTED, not falsified --
    B-118 (#1275) moved it. PER_PROVIDER appears in src ONLY at its enum declaration
    harness_breaker_schema.py:112 plus one docstring mention at config/audit_signing.py:81; the only
    constructions anywhere are TESTS (test_lifecycle_retry_breaker.py:187 and :497) through the
    scope-agnostic registry (retry_breaker.py:546-568). The falsifier ("a PER_PROVIDER construction site
    appearing anywhere in harness-*/src") is NOT met. STEP (3) PRICED HONESTLY, POST-B-118, AND THE
    REGISTRATION FRAMING UNDERSTATES IT. The B-116 waiver predicate is FREE to inherit:
    _is_breaker_charge_waived(exc) (retry_breaker_fallback.py:456-467) is purely exception-type-based
    over the five-member tuple at :420-427 and is KEY-AGNOSTIC, so a second charge site reuses it
    verbatim -- the registration''s fear that "the four demoted harness-internal members re-enter the
    breaker through the provider scope" is unfounded at HEAD (and the tuple is FIVE, not four, since
    B-115 (b-prime)). What is NOT free is the B-118 machinery. The pre-check block at
    retry_breaker_fallback.py:804-895 is a SINGLE-breaker state machine: one half_open_trial flag, one
    trial_token = breaker.trial_epoch, one effective_policy = RetryPolicy(max_attempts=1, ...) trial cap,
    and a try/except BaseException -> re_arm_half_open_trial protection boundary added at round 3
    specifically because an unguarded raise strands the breaker with no owner. A second scope makes
    admission a PRODUCT OF TWO INDEPENDENT PERMITS -- per-model CLOSED x per-provider HALF_OPEN has no
    defined cell, and Runtime spec v1.114 §14.6.4''s nine-cell trial-outcome matrix is written for ONE
    machine -- so ONE dispatch outcome would have to resolve TWO trial tokens. That is a Runtime SPEC
    DELTA (§14.6/§14.6.4 generalization), not the "a second get_breaker charge alongside the per-model
    one" the registration priced. HALF-OPEN COHERENCE, ANSWERED: a provider-scope trial is semantically
    coherent (any model of that provider probes provider health) and §14.6.3''s normative test is
    satisfiable at {provider} keying -- the incoherence is COMPOSITIONAL (two breakers, one call, one
    outcome), not semantic. STEP (2)/(4) -- DEMAND IS ABSENT AT HEAD, MEASURED NOT ASSUMED. ZERO
    consumers branch on harness.breaker.scope; the ONLY production read of the value is B-125''s own
    default at retry_breaker.py:627. Every other occurrence is declaration/manifest
    (harness_breaker_schema.py:93 and :267, harness-cp retry_fallback_namespace.py:150, harness-od
    attribute_class_enforcement.py:53, content_structure_discipline.py:177). NO CONFIG SURFACE --
    fail_threshold / cooldown_seconds are module constants on RetryBreakerRegistry, not RuntimeConfig
    fields; the only configurable breaker knobs under config/ belong to the SIGNING breaker
    (audit_signing.py:60-61). NO OPERATOR SURFACE -- a breaker grep over
    harness-runtime/src/harness_runtime/admin/inspect.py and cli/app.py returns ZERO hits. Step (4)
    ("witness scope discrimination at the emission") is therefore VOID BY CONSTRUCTION under this
    disposition: with one scope there is nothing to discriminate, and the witness becomes owed only if
    the demand trigger fires. STEP (5) COUPLING TO B-125 -- DISCHARGED IN THE SAME LEG: because
    PER_MODEL remains the only scope, the :627 scope guard stays vacuously-true and B-125 collapses to a
    declared bound (closed at #1277 under disposition (a)). THE FOUR-DISJUNCT DEMAND TRIGGER (verbatim,
    falsifiable, any ONE re-opens this row) -- (i) a RuntimeConfig scalar or HARNESS_* env knob selecting
    breaker scope; (ii) a consumer that BRANCHES on harness.breaker.scope (as opposed to declaring or
    echoing it); (iii) an operator-facing surface (a harness-inspect row or a CLI subcommand) reporting
    breaker state; (iv) a SECOND get_breaker(...) call site appearing anywhere in harness-*/src. A bare
    "we might want provider-level trips someday" is NOT a trigger -- this is the demand-trigger shape the
    workspace requires of a forward-only record, not a bare deferral. COUNCIL: the conditional C9 (x)
    C11 dyad registered below convenes ONLY if a future arc elects BUILD at step (2); under this
    forward-only-record disposition it is NOT convened, and any such future deliberation MUST re-ground
    the C11 position against escapable-OPEN per the 2026-08-08 cross-note above -- BreakerStateMachine
    now carries attempt_half_open + opened_at + trial_epoch (retry_breaker.py:236-268), so the
    irreversibility clause the registered C11 text rests on is FALSIFIED.'
  council: 'No at registration — nothing is decided here. Conditionally yes at step (2): building a second
    breaker scope trades detection breadth (C9 — a provider outage should be visible as one fact, not
    inferred from N per-model facts) against operator recoverability under a materially wider blast
    radius (C11 — one provider-scope OPEN disables every model of that provider at once, and with B-118
    unfixed it does so irreversibly). That is nameable in advance and dyadic.'
  heading: '### B-119 · No per-provider breaker is constructed anywhere, so `BreakerScope.PER_PROVIDER` is a
    declared-but-unbuilt scope *(surfaced at the B-116 council deliberation (C9), 2026-08-07)*'
- id: B-120
  title: 'The breaker-transition emission passes no permanent_fail_repeats and no model_version, so two
    declared attributes have no producer'
  status: registered_finding
  summary: 'WHAT IT IS. RetryBreakerFallbackDispatcher._emit_breaker_transition
    (retry_breaker_fallback.py:1130-1136) calls emit_breaker_transition_event(transition, parent_span)
    positionally and passes none of the three optional correlation keywords the registry accepts —
    permanent_fail_repeats, tool_id, model_version (retry_breaker.py:430-437). BreakerTransition itself
    carries no field for any of them (retry_breaker.py:183-189). Two of the three are therefore dead at
    every shipped trip: harness.breaker.permanent_fail_repeats, the declared C10 gating signal ("Whether
    this trip is from repeated C5 permanent-fail-exits — the C10 gating signal",
    Spec_Operational_Discipline_v1_32.md:42; CP §3.5 Spec_Control_Plane_v1_2.md:402), and
    harness.breaker.model_version (Spec_Operational_Discipline_v1_32.md:44). C9 names this his own FM-Q,
    live. THE ONE-THIRD CORRECTION, CARRIED RATHER THAN DROPPED. C9''s A1 asserted all three are
    vacuous. That is true of the call, and false on the wire for tool_id: emit_breaker_transition_event
    sets effective_tool_id = transition.identifier whenever tool_id is None at PER_MODEL scope
    (retry_breaker.py:459-461), with the default stated as contract in the docstring (:445-446). C7
    repeated the "never populated" claim at A2, C11 falsified it at cross-read, and C7 formally withdrew
    it at E2b. Since PER_MODEL is the only constructed scope (B-119), every shipped trip already emits
    harness.breaker.tool_id = "provider:model". That half is B-125''s row, not this one. WHY IT MATTERS
    DIRECTIONALLY. C9''s warning, not a slogan: the fail-fast branch maps to PERMANENT_FAIL_EXIT
    (retry_breaker_fallback.py:1024-1027), so the moment anyone wires permanent_fail_repeats — the
    natural follow-on — it begins gating on exactly the branch B-116 re-scoped. Under the rejected
    reading (I) that would have driven C10''s gate_combination to gate a healthy model+tool combination
    off a harness config bug. Reading (II)-as-ratified makes the signal more meaningful, not less, but
    the sequencing is load-bearing. SURFACED AT THE B-116 COUNCIL DELIBERATION, C9 A1 primary refinement
    4 (FM-Q), re-registered at the E2b reconcile as R3, 2026-08-07.'
  close_out: '(1) Re-verify that the call site passes no keywords and that BreakerTransition still has no carrier
    fields. (2) Decide per attribute whether a real producer exists — permanent_fail_repeats needs
    per-cause failure history the state machine does not keep (the same gap FIVE_XX_STREAK already
    carries in-code at harness_breaker_schema.py:145-149), whereas model_version needs a binding-side
    value the dispatcher demonstrably has. These are not one task. (3) For any attribute with no
    producer, either wire it or record it as a forward-compatible slot in the shape OD v1.32 used for
    capability_shortfall (Spec_Operational_Discipline_v1_32.md:50) — declared-and-dead-and-undocumented
    is the state to leave. (4) Sequence AFTER B-116''s guard lands, per C9''s own directional warning
    above. (5) Witness a trip carrying each newly-populated attribute, and a trip that legitimately
    omits it (the attributes are optional by contract, and absent is a meaningful value here). WHAT
    WOULD FALSIFY IT. A producer already existing for either attribute on some path this search missed.'
  council: 'No. Populating declared-but-unset attributes inside a cleared schema is adjudicated by the attribute
    owner (C7/OD); there is no cross-domain tradeoff to name. Route to a single voice plus advisor() if
    step (2) is contested.'
  heading: '### B-120 · The breaker-transition emission passes no `permanent_fail_repeats` and no
    `model_version`, so two declared attributes have no producer *(surfaced at the B-116 council
    deliberation (C9), 2026-08-07)*'
- id: B-121
  title: 'LLMDispatchProviderUnreachableError means UNREGISTERED at all three raise sites, and the misleading
    name is a demonstrated defect vector'
  status: closed
  summary: 'WHAT IT IS. The type is named Unreachable; all three of its raise sites are harness-side registry
    lookups that never contact a provider. Counted at HEAD: exactly 3 raise
    LLMDispatchProviderUnreachableError sites in
    harness-runtime/src/harness_runtime/lifecycle/llm_dispatch.py — :1525 (provider_name not in
    self.providers, an in-process dict), :1549 (_PROVIDER_OPERATIONS.get(...) is None, whose own comment
    at :1546-1548 reads "Defensive — every key in self.providers is one of the three constructed at
    stage 3a per C-RT-05"), and :1865 (the terminal else of the isinstance adapter-arm chain). The class
    docstring is explicit: "Raised when binding.model_binding.provider resolves to a provider absent
    from ctx.providers" (:184-193). The semantics are unregistered, not unreachable. WHY IT IS A DEFECT
    AND NOT A NIT. The name is the defect vector, demonstrably. It misled the B-116 fork filing''s §5
    table and all four council voices into keeping member #1 counting through A1 and the entire
    cross-read; the adversarial leg''s halt-class finding F-01 demoted it on raise-site evidence, and C9
    recorded the correction against himself: "This is a defect I authored — my A1 row 1 came from the
    type''s NAME, not its raise sites." Under the ratified recovery-model test a half-open trial
    re-executes an in-process dict built once at bootstrap and re-fails identically for the process
    lifetime. Genuine provider-down never travels as this type at all: a real network fault raises the
    provider SDK''s own exception, carries no .status_code, falls to the catch-all TRANSIENT_RETRY
    (retry_breaker_fallback.py:371) and charges the breaker at :1072/:1091, outside the branch B-116
    touches. THIS IS A CLASS 2 BACK-FLOW, AND THE ROW SAYS SO RATHER THAN WALKING IT. The type maps to
    the RT-FAIL-PROVIDER-UNREACHABLE fail-class token per C-RT-14 (docstring :189-190), and that token
    is carried in cleared design-substrate — Runtime §C-RT-16''s fail-class narrative propagates it
    through the wrapper (design-substrate/Spec_Harness_Runtime_v1.md:4194). Renaming the Python type is
    code-local; renaming or re-taxonomising the token is a design-substrate/ change and routes as a
    Class 2 fork per root CLAUDE.md §4.3, with the taxonomy value owned by C5/C7/OD — not by an impl
    leg. Status stays registered_finding; the fork route is named here, not taken. SURFACED AT THE B-116
    COUNCIL DELIBERATION, C9 E2b reconcile R4, on adversarial finding F-01, 2026-08-07.'
  close_out: '(1) Re-verify the three raise sites and that none contacts a provider. (2) Decide the scope: (a)
    rename the Python type only, keeping RT-FAIL-PROVIDER-UNREACHABLE — cheapest, but leaves the
    operator-facing token still misleading, which is most of the harm; (b) mint a distinct
    RT-FAIL-PROVIDER-UNREGISTERED token and re-map, which is the design-substrate change. (3) Route (b)
    as a Class 2 fork against the C-RT-14 taxonomy BEFORE any code lands (X-AL-3 — no silent H_T design
    extension at Phase 7). (4) Do not re-litigate B-116: member #1''s demotion stands on raise-site
    evidence regardless of what the type is called. (5) Note the sibling — B-122 is the same raise
    sites'' other half (should this fault be discoverable at bootstrap at all). WHAT WOULD FALSIFY IT. A
    fourth raise site that does contact a provider (then the type is genuinely heterogeneous and needs a
    split, not a rename — the B-116 member-#2 shape).'
  council: 'No. The substantive question — does it count toward the breaker — was settled at B-116 on evidence.
    What remains is a naming/taxonomy call inside a contract with a named owner, plus a routing decision
    this row already names.'
  pr: '#1317'
  heading: '### B-121 · `LLMDispatchProviderUnreachableError` means **unregistered** at all three raise sites,
    and the misleading name is a demonstrated defect vector *(surfaced at the B-116 council deliberation
    (C9 E2b reconcile, on adversarial finding F-01), 2026-08-07)*'
- id: B-122
  title: 'Nothing validates at bootstrap that every fallback-chain candidate names a registered provider'
  status: closed
  summary: 'WHAT IT IS. The same three raise sites that ground B-121 establish the shape: a fallback-chain
    candidate naming a provider absent from ctx.providers is discovered at dispatch time, per dispatch,
    as a runtime exception (llm_dispatch.py:1525) — and, after B-116, as one that charges no health
    counter at all. Nothing validates the chain against the registry earlier. The comment at
    llm_dispatch.py:1546-1548 records that the provider set is fixed at stage 3a per C-RT-05, so the
    check is in principle decidable before any dispatch runs: both the chain and the registry are known.
    C9''s framing — "config faults fail at stage 3a, not at a runtime health counter" — and he assigned
    the surface to C1/C11 rather than authoring it. STATED HONESTLY: THIS ROW IS THINNER THAN ITS
    SIBLINGS. C9 registered it in a single line at the E2b reconcile, with no probe run, no venue
    chosen, and no shape priced. Everything above beyond the raise-site facts is this row observing that
    the check is decidable — not a council finding, and not a designed proposal. It is registered so the
    observation is queryable, not because it arrived costed. Read it as a question, not a plan. SURFACED
    AT THE B-116 COUNCIL DELIBERATION, C9 E2b reconcile R5, on adversarial finding F-01, 2026-08-07.'
  close_out: '(1) Ground the bootstrap sequence first — where the fallback chain is composed relative to where
    providers is constructed, and whether a candidate can enter a chain after bootstrap. It can,
    partially: _advance_or_exhaust traverses a per-dispatch effective chain that may be role-augmented
    rather than the stage-bound one (retry_breaker_fallback.py:878-883), which bounds how total a
    bootstrap check can be. Answer this before pricing anything. (2) Decide the failure venue, and
    answer the counter-argument first: the class docstring names "Ollama-degraded path skipped
    registration" as an expected case (llm_dispatch.py:186-187) — if skipping registration is a
    supported deployment shape, a hard bootstrap refusal is wrong, and the correct answer is a startup
    diagnostic, not a gate. (3) Price whatever survives (2). (4) Witness with a configuration naming an
    unregistered provider, asserting the chosen venue fires and that a legitimately degraded deployment
    still boots. WHAT WOULD FALSIFY IT. Step (2) can kill the row outright, and that is a real outcome
    rather than a formality — the degraded-provider shape is sanctioned by the type''s own docstring.
    Step (1) can also narrow it to near-nothing if augmented chains make the check substantially
    partial.'
  council: 'No at registration — the row is a question with no priced alternatives. Conditionally yes at step
    (2), and only if step (1) finds the check can be made total: a bootstrap hard-fail trades fail-early
    configuration hygiene (C1/C9) against the degraded-provider local-deployment shape the docstring
    already sanctions (C11). Do not convene on the current thin state.'
  pr: '#1316'
  heading: '### B-122 · Nothing validates at bootstrap that every fallback-chain candidate names a
    **registered** provider *(surfaced at the B-116 council deliberation (C9 E2b reconcile, on
    adversarial finding F-01), 2026-08-07)*'
- id: B-123
  title: 'The tail-keep breaker trigger matches a SPAN NAME while the breaker emits a span EVENT, so the
    trigger may be inert'
  status: closed
  summary: 'WHAT IT IS. is_classification_trigger matches the §10.2 breaker trigger on the span name: if name ==
    BREAKER_TRIPPED_SPAN_NAME (harness-od/src/harness_od/tail_keep_classification.py:94-95 — re-grounded
    at HEAD; C7 cited :92-93, which at HEAD is the sibling SANDBOX_VIOLATION_SPAN_NAME arm). The real
    breaker emission is a span event on a parent span: parent_span_ref.add_event(name="breaker.tripped",
    attributes=attributes) (harness-od/src/harness_od/harness_breaker_schema.py:282, with the shape
    committed in the function docstring at :236-243). If no span is ever named breaker.tripped, the
    §10.2 trigger is inert on the dispatch path — and every claim about breaker-trip visibility at
    tail-sampled production surfaces is smaller than either B-116 primary assumed. C7 FILED THIS AS A
    PROBE, NOT A FINDING — PRESERVED VERBATIM HERE. His own words: "I surface this as a probe, not a
    finding — it needs a positive-control run before anyone cites it." That framing is load-bearing and
    is not upgraded by registration. The row exists so the probe is queued, not so the conclusion can be
    cited. SURFACED AT THE B-116 COUNCIL DELIBERATION, C7 A2 consultant registered adjacency 1, carried
    as rider (b) of the B-116-t3 leg, 2026-08-07. PROBE OBSERVATION RECORDED AT THE B-133
    LEG, 2026-08-08 -- AN OBSERVATION, NOT A CLOSURE, AND THIS ROW STAYS registered_finding. The
    B-133 positive control drove a REAL charging-fault dispatch to exhaustion through the REAL
    TailKeepSpanProcessor and observed the predicate directly on the carrying span:
    is_classification_trigger(span) returned False on the wrapper span
    harness.runtime.retry_breaker_fallback that carries the breaker.tripped EVENT, and the trace''s
    buffered siblings were consequently DROPPED at root close. THAT ANSWERS THIS ROW''S STEP (1) IN
    THE AFFIRMATIVE FOR THE BREAKER ARM: the §10.2 trigger is inert on the dispatch path. It does NOT
    answer step (3) -- the sandbox.violation sibling arm was not exercised -- and it does not decide
    step (2)''s repair. OD spec v1.38 §9.2.1 term 3 DELIBERATELY declined to widen
    is_classification_trigger to event names while closing the §9.2 half beside it, and U-OD-59''s
    witness W7 PINS the boundary (an event-carried breaker.tripped forwards its own carrier under the
    §9.2 floor but sets no keep flag), so widening it later is a deliberate, test-visible act rather
    than silent drift. C7''s "I surface this as a probe, not a finding" framing is preserved: the probe
    has now RUN for one of its two arms, and the result is recorded here so the conclusion can be
    cited for that arm -- with the repair decision still owed.'
  close_out: '(1) RUN THE POSITIVE CONTROL FIRST. Drive a real breaker trip through the tail-keep processor and
    observe whether the trace is preserved. Grep proves presence; only the run proves behaviour, and
    this row is entirely about behaviour. (2) If inert, decide the repair — match the event name as well
    as the span name, or emit a span so named. (3) Widen the probe by one arm: the identical
    span-name-only shape governs sandbox.violation (tail_keep_classification.py:92-93), so cover both
    rather than assuming the sibling is fine. (4) Note the B-116-t3 interaction: C7''s E2b argument for
    pinning fallback.exhausted to the §9.2 floor leaned on head=1.0 precisely because the tail-keep
    mitigation might be inert. If this probe returns the trigger fires, t3''s merits argument changes —
    it does not evaporate (a head=1.0 guarantee does not depend on a trigger), but the row it rests on
    becomes weaker and should be re-stated rather than carried. GROUNDED 2026-08-08 (#1277) -- STEPS (3)
    AND (4) ANSWERED, STEP (2) SETTLED; ROW STAYS registered_finding AND CLOSES AT THE LEG-1 IMPL. This
    paragraph states the CURRENT result; the registration text above is preserved and superseded where
    it conflicts. STEP (3), THE sandbox.violation ARM -- RESOLVED NEGATIVE, THE SIBLING IS FINE.
    sandbox.violation is emitted as a REAL SPAN, not an event:
    tracer.start_as_current_span("sandbox.violation") at
    harness-runtime/src/harness_runtime/lifecycle/runtime_tool_dispatcher.py:725, inside
    _emit_sandbox_violation (:707-728). An EXHAUSTIVE add_event( sweep over all seven harness-*/src trees
    returns exactly SIX sites -- harness_breaker_schema.py:282, alignment_floor_drift_detection.py:242,
    retry_breaker_fallback.py:749 / :879 / :1151, and retry_breaker_tool.py:314 -- and NONE is named
    sandbox.violation. So tail_keep_classification.py:92-93 matches a name that is genuinely produced.
    The row''s scope NARROWS to the breaker arm alone; the registration''s "cover both rather than
    assuming the sibling is fine" was the right instinct and the answer is that the sibling IS fine.
    WHAT IS STILL BROKEN AFTER B-133, SHARPENED BY DIRECT READ. The landed §9.2.1 event-aware arm is
    tail_keep_span_processor.py:364-388. It forwards the carrier span, then at :376-377 calls
    is_classification_trigger(span) -- which at tail_keep_classification.py:91-95 matches SPAN NAMES
    ONLY. The carrier is named harness.runtime.retry_breaker_fallback, so the predicate returns False,
    the per-trace keep flag is NEVER set, and :386-387 then calls _materialize_trace_decision(trace_id),
    which at :445-452 DROPS the buffered spans when keep is False. NET EFFECT AT HEAD: the trip''s own
    CARRIER span survives (B-133''s delivery), but its entire SIBLING TREE is dropped at root close.
    B-133 changed SIGNAL delivery, not TREE preservation. The code states this deliberately at :369-375
    and names this row as owner, and U-OD-59''s W7 pins the boundary -- so the debt is test-visible, not
    silent. STEP (2), THE REPAIR -- SETTLED, AND NOT AN OPERATOR GATE. The §10.2 contract table at
    design-substrate/Spec_Operational_Discipline_v1_2.md:582-584 reads VERBATIM "Parent + sibling spans
    of any sandbox.violation EVENT preserved" and "Parent + sibling spans of any breaker.tripped EVENT
    preserved". THE CONTRACT ALREADY SAYS EVENT. The code''s name-only predicate is therefore
    NON-CONFORMANT TO THE CONTRACT AS WRITTEN, and widening is_classification_trigger to also scan
    span.events is a CONFORMANCE FIX, not a contestable spec direction -- there is no genuine fork here
    and nothing to route to the operator. Of the registration''s two candidates ("match the event name as
    well as the span name" vs "emit a span so named"), the FIRST is selected: the second mints a new
    emission primitive and is the X-AL-3 back-flow route B-133 already refused as venue (b). SHAPE AND
    VENUE: single-domain C7/OD as the council disposition says; roughly four lines in
    tail_keep_classification.py reusing the _carries_always_sampled_event pattern
    (tail_keep_span_processor.py:190-223) but matched against the TWO §10.2 names rather than the §9.2
    SSOT, plus the AC that the keep flag now sets for an event-carried trip, witnessed BY EXECUTION
    through the real processor and a real exhausted dispatch (the B-133 harnesses are the template).
    ONE DOC OBLIGATION RIDES IT: OD spec v1.38 §9.2.1 term 3 explicitly DECLINED to widen and named this
    row, so the impl leg owes a small OD spec realization delta flipping term 3 plus an OD plan unit in
    the U-OD-58/U-OD-59 precedent shape. REALISM CAVEAT THE LEG MUST STATE RATHER THAN OVERCLAIM: per
    B-137, NON-ROOT spans are head-starved (~9% preservation measured at base-rate 0.1 for a non-root
    sandbox.violation trigger), so this repair delivers the sibling tree only for head-ADMITTED
    carriers. It is still correct to build; it must not be sold as a full floor. STEP (4), THE
    B-116-t3 RESTATEMENT -- ONE LINE, AND THE DIRECTION IS THE OPPOSITE OF THE ONE THE REGISTRATION
    ANTICIPATED. B-116 is CLOSED (PRs #1271 + #1272). C7''s E2b argument for pinning fallback.exhausted
    to the §9.2 floor leaned on head=1.0 PRECISELY BECAUSE the tail-keep mitigation might be inert; the
    probe returned that the trigger IS inert, so that argument is CONFIRMED, not weakened. t3''s
    head=1.0 guarantee never depended on a trigger either way. The registration''s conditional ("if this
    probe returns the trigger fires, t3''s merits argument changes") is therefore NOT triggered and needs
    no carry. [SUPERSEDED 2026-08-09 -- THIS PARAGRAPH IS FALSIFIED; see the CLOSED paragraph at the end of this field. Its premise was checked by direct read and the cite DOES resolve. Preserved verbatim below per the registration-text convention, NOT restated as current.] CLASS-3 INFORMATIONAL, HOMED HERE BECAUSE IT SITS ON THE SAME §10.2 TABLE AND A FULL ROW
    WOULD BE OVERWEIGHT: §10.2 row 1 cites "classification == permanent-fail per ADR-D2 §1.8 fail-class
    taxonomy" (Spec_Operational_Discipline_v1_2.md:582), but ADR-D2 §1.8 is the SANDBOX-VIOLATION
    fail-class taxonomy -- escape_attempt / egress_denied / timeout / oom / signal / exit_nonzero /
    policy_override (design-substrate/ADR-D2.md:285 and :301) -- and contains NO permanent-fail value.
    The cite does NOT resolve. [FALSIFIED -- IT DOES; see below.] Non-blocking; the code''s realization (validator.fail.permanence ==
    "permanent" per C-CP-21 §21.6, Spec_Control_Plane_v1_2.md:1914-1921) is a downstream interpretation
    that happens to be sound. Carry as a rider on the next OD §10.2 spec delta -- which IS the Leg-1
    term-3 flip, so it costs nothing extra. REPAIR OWED AT LEG 1; this row closes there, not here.  CLOSED
    2026-08-09 AT THE LEG-1 IMPL (PR #1279). Step (2)s selected repair is BUILT: the predicate now
    resolves the two section-10.2 trigger names against the span NAME and, when the name does not match,
    against the names of the spans EVENTS, via a DERIVED two-member frozenset composed from the existing
    name constants so the span arm and the event arm cannot drift apart under a rename. The owed doc
    obligation is DISCHARGED: OD spec v1.38 section 9.2.1 term 3, which explicitly DECLINED to widen and
    named this row, is REPLACED IN FULL at v1.39, and the plan leg is v2.34 U-OD-60. SECTION 10.2 ITSELF
    IS NOT AMENDED, and that is the point -- its table already specified the event carrier, so this was a
    conformance repair to cleared contract text, not a contestable spec direction, and nothing routed to
    the operator. TWO THINGS THE LEG CORRECTED RATHER THAN INHERITING FROM THIS ROW. (i) THIS ROWS OWN
    CLASS-3 INFORMATIONAL RIDER IS FALSIFIED. The claim recorded above -- that section 10.2 row 1s cite
    (classification == permanent-fail per ADR-D2 section 1.8 fail-class taxonomy) does NOT resolve
    because ADR-D2 section 1.8 contains NO permanent-fail value -- is WRONG. ADR-D2.md section 1.8 is at
    line 301 and its table at lines 303-311 carries THREE columns, the second headed C5 fail-class, whose
    values include permanent-fail at line 305 (escape_attempt), line 306 (egress_denied) and line 309
    (signal). The original claim was derived from the sandbox.fail.class value enum at ADR-D2.md:285
    alone and missed the fail-class column of the very table it cites. THE CITE RESOLVES. The rider was
    NOT carried into v1.39 or v2.34; the falsification is recorded at v1.39 section 0.6 and plan section
    0.5, reached INDEPENDENTLY at both legs by separate direct reads. (ii) A PD-8 PROBE CHANGED THE
    DELIVERABLE. Deleting the event arms own root-close materialize call left the first real-dispatch
    witness GREEN, exposing that the shape OD spec v1.38 section 9.2.1 term 5 says actually ships (the
    dispatch carrier IS routinely the root close) had NO real-dispatch coverage under the widened flag. A
    SECOND witness was ADDED rather than the probe restated, and the re-run turns it RED. THE REPAIR IS
    ADMISSION-BOUNDED and the leg says so: per B-137 the sibling tree is preserved only for head-ADMITTED
    carriers (~10% at base-rate 0.1, ~20% at 0.2); no full-floor claim is made anywhere. Section 10.2 row
    1 stays B-124s.'
  council: 'No. A probe with a binary answer and a named positive control. Convene nothing until it has run; if
    it returns inert, the repair is single-domain (C7 owns both the predicate and the emission shape).'
  pr: '#1279 (the B-123 repair leg -- OD spec v1.39 section 9.2.1 term 3 replaced in full + plan v2.34
    U-OD-60 + the event-scan arm at is_classification_trigger + the twelve-AC witness set incl. the U-OD-59
    W7 in-place inversion and BOTH real-dispatch witnesses; both clearance markers filed)'
  heading: '### B-123 · The tail-keep breaker trigger matches a **span name** while the breaker emits a span
    **event**, so the trigger may be inert *(surfaced at the B-116 council deliberation (C7 A2
    consultant, registered adjacency 1), 2026-08-07)*'
- id: B-124
  title: 'validator.fail.permanence is never set on any span, so its OD 9.2 always-sampled row and its 10.2
    tail-keep trigger are both inert'
  status: registered_finding
  summary: 'WHAT IT IS. Two live surfaces read this attribute. OD §9.2''s always-sampled floor carries the row
    "validator.fail.* at validator.fail.permanence=permanent"
    (design-substrate/Spec_Operational_Discipline_v1_2.md:530), realized in code at
    harness-od/src/harness_od/sampling_mode.py:136. is_classification_trigger''s §10.2 row-1 arm reads
    the same attribute (tail_keep_classification.py:58 declares the key, :99 compares it). The value is
    derived by validator_fail_permanence(fail_class)
    (harness-cp/src/harness_cp/validator_fail_taxonomy.py:169-176) and declared in the CP schema at
    :149. But the function has zero call sites across harness-{runtime,cp,od}/src, and nothing anywhere
    sets "validator.fail.permanence" as a span attribute (searched at HEAD). Both rows are therefore
    inert on the dispatch path. ONE REFINEMENT THE COUNCIL DID NOT CARRY, ADDED BY DIRECT READ AT HEAD.
    There is exactly one structured carrier of the value:
    ReplaySemanticDivergenceError.validator_fail_permanence: str = "permanent"
    (harness-od/src/harness_od/idempotency_join_dedup.py:361-368, per §14.5.3). It is a frozen Pydantic
    record field, not an OTel span attribute — so neither the attribute-reading tail-keep predicate nor
    the always-sampled lookup can see it. The finding stands; its precise statement sharpens from
    nothing carries the value to nothing carries it on a span, which is what the two readers require.
    SURFACED AT THE B-116 COUNCIL DELIBERATION, C7 A2 consultant registered adjacency 2, carried as
    rider (c) of the B-116-t3 leg, 2026-08-07.'
  close_out: '(1) Re-verify zero span-attribute set sites and that the derivation function still has no callers.
    (2) Determine whether a validator-fail span or event is emitted at all on any live path. If none is,
    this row is about a whole missing emission rather than one unset attribute, and it must be re-scoped
    upward before it is priced — fixing the attribute on a span that never opens is motion, not
    progress. (3) If an emission exists, wire the derived attribute at it and witness that the §9.2
    always-sampled lookup and the §10.2 trigger both fire, by execution rather than by grep. (4) Carry
    it as rider (c) of the B-116-t3 leg, per C7''s own registration — t3 is the arc that opens OD §9.2,
    and touching the table twice is the waste to avoid. WHAT WOULD FALSIFY IT. An emission site existing
    on a path this search missed — the search covered harness-{runtime,cp,od}/src only. GROUNDED 2026-08-08
    (#1277) -- THE ROW SPLITS; IT IS NOT ONE FINDING. This paragraph states the CURRENT result; the
    registration text above is preserved and superseded where it conflicts. Rider (c) of B-116-t3 is
    RETIRED as a routing (step (4)): B-116 closed at #1271 + #1272 and the t3 leg is gone, so the two
    halves below route independently. STEP (1) RE-VERIFIED AT HEAD 49591934 -- BOTH HALVES HOLD.
    validator_fail_permanence() is defined at harness-cp/src/harness_cp/validator_fail_taxonomy.py:169-176
    and has ZERO src callers: a repo-wide search returns only the definition, one docstring reference at
    harness-od/src/harness_od/tail_keep_classification.py:60, and three test assertions at
    harness-cp/tests/test_validator_fail_taxonomy.py:76-83. NO span-attribute set site for
    "validator.fail.permanence" exists anywhere. The sole structured carrier remains the frozen Pydantic
    field at idempotency_join_dedup.py:366. STEP (2) ANSWERED -- AN EMISSION DOES EXIST, SO THE ROW DOES
    NOT RE-SCOPE UPWARD; BUT THE HONEST ANSWER IS SHARPER THAN THE REGISTRATION''S BINARY. A live
    validator.evaluate span is opened at harness-cp/src/harness_cp/workflow_driver.py:5600 and its
    attributes are set from evaluation.span_attributes at :5620-5622. Those are built at
    harness-cp/src/harness_cp/validator_framework.py:320-352, and on non-PASS the builder sets FOUR
    sibling validator.fail.* attributes at :343-349 -- next_action, escalation_owed, class, detail_hash --
    but NOT permanence. So the emission exists and already carries the namespace. The registration''s
    falsifier ("an emission site existing on a path this search missed") IS met: the search covered the
    DERIVATION function and the ATTRIBUTE NAME, and missed the emission because the emission never names
    the attribute. STEP (3) -- THE TWO READERS DO NOT SHARE A FIX, AND THAT IS THE LOAD-BEARING RESULT.
    (A) THE §10.2 HALF IS CLOSEABLE. is_classification_trigger reads the attribute off ANY span
    (tail_keep_classification.py:96-100), so setting validator.fail.permanence on the validator.evaluate
    span WOULD make row 1 fire. OWED AT LEG 1, NAMED: pr: pending-leg-1 -- roughly three functional lines
    at validator_framework.py:_build_span_attributes deriving from result.outcome
    (ValidatorOutcome.PERMANENT_FAIL -> "permanent", else "transient"), witnessed BY EXECUTION through
    the real TailKeepSpanProcessor (the trigger fires and the buffered siblings are preserved), NOT by
    grep. (B) THE §9.2 HALF IS NOT CLOSEABLE BY WIRING THE ATTRIBUTE, AND MUST BE RE-SCOPED FORWARD.
    is_always_sampled resolves the validator.fail.* row against the span/event NAME, never against a
    carrier''s attributes: _conditional_always_sampled gates on event_name.startswith("validator.fail.")
    at harness-od/src/harness_od/sampling_mode.py:239-241, and the literal / dot-anchored-prefix
    fallbacks at :264-269 also key on name. The live span is named validator.evaluate, so setting the
    attribute changes NOTHING there. Nor does B-133''s arm reach it -- that arm resolves EVENT NAMES
    (tail_keep_span_processor.py:190-223) and the validator data is ATTRIBUTES, not events. Note too that
    a span named exactly "validator.fail" (the declared SPAN_SITE_VALIDATOR_FAIL at
    harness-od/src/harness_od/validator_namespace.py:54) would ALSO miss, the derived prefix carrying the
    dot anchor. THIS IS A THIRD REALIZATION-MISMATCH SHAPE -- attribute-on-a-differently-named-span --
    distinct from B-133''s (event-on-carrier-span) and B-123''s (event-vs-name at the §10.2 predicate).
    The family is now four rows, not three. DEMAND TRIGGER FOR THE §9.2 HALF: any arc that touches §9.2
    REALIZATION (as opposed to §9.2 membership) -- the natural rider is B-137''s step-(3) posture
    decision, since candidate A/B/C all re-open how the floor is resolved at the SDK boundary. Until
    then the §9.2 half is recorded, not repaired. THE TAXONOMY-COLLISION CAUTION -- SEQUENCING, NOT
    OPTIONAL. The naive wire (calling validator_fail_permanence at the emission) is BLOCKED by a type
    boundary: that function consumes ValidatorRetryExitClass (the C-CP-21 §21.1 five-value retry-exit
    taxonomy at validator_fail_taxonomy.py:38-62) while the live span''s validator.fail.class carries
    ValidatorFailClass (the C-CP-25 §25.2 pre-emit categorization at
    harness-cp/src/harness_cp/validator_framework_types.py:70-90, whose own docstring says it is
    "Distinct from C-CP-21 §21.1 ValidatorRetryExitClass"). That collision is registered SEPARATELY as
    B-138, and B-138 MUST be dispositioned BEFORE any arc touches _build_span_attributes -- including
    Leg 1''s (A) above -- because deriving permanence from result.outcome mints a second derivation from
    a third taxonomy on a function whose authoritative domain is itself unsettled. ROW STAYS
    registered_finding; it closes when (A) lands. CROSS-NOTE 2026-08-09 (B-138 CLOSED at #1278, disposition (a)): the B-138 sequencing gate on _build_span_attributes is DISCHARGED -- the authoritative domain is settled (ValidatorFailClass, CP v1.116). BUT the naive outcome wire this row planned (permanent iff PERMANENT_FAIL from result.outcome) is FORECLOSED as LOSSY at CP v1.116 §1.3: ESCALATE conflates permanent-fail-exit with HITL-recoverable, so that wire would silently narrow §21.6''s always-sample-permanent coverage to the abort path. This row now OWNS the derivation sub-decision (materialize the retry-exit classification at the emission site / ratify the narrowing WITH §21.6 re-specified / another carrier) and notes B-140 (the validator.fail span-site gap) as ADJACENT, not gating: this row''s §10.2 trigger reads the permanence ATTRIBUTE from any span, so the span-site gap gates the §9.2-style NAME keying family (B-123/B-133/B-137), not this half (codex round-3 venue correction).  FILED
    2026-08-09 (PR #1283, doc-only): the derivation sub-decision this row OWNS is now a Class 2 fork at
    .harness/class_2_fork_b124_validator_fail_permanence_derivation.md -- FOUR readings (A-i materialize
    the retry-exit classification at the emission site / A-ii EXPLICITLY FORECLOSED as fabrication rather
    than materialization, since no axis the producer holds carries permanence / B ratify the outcome-
    based narrowing WITH section 21.6 re-specified / C another carrier / D defer with a demand trigger),
    RECOMMENDED D with runner-up A-i and a named discriminator. STATUS AND pr: POINTER DELIBERATELY
    UNMOVED -- both ride the RATIFICATION leg per the B-88 / B-57+B-112 precedent; this note is a LOCATOR
    only and asserts no disposition. TWO GROUNDING RESULTS THE FILING ADDS. (1) THE LOSSINESS IS
    CONFIRMED FROM TWO DIRECTIONS, not one: permanent-fail-exit and HITL-recoverable collide on
    outcome=ESCALATE AND on next_action=ESCALATE_HITL, so NEITHER disposition-shaped field the producer
    holds can discriminate them. (2) A HYPOTHESIS TESTED AND FALSIFIED: CP v1.116 section 1.3s claim that
    outcome is the ONLY candidate the producer holds is loose in LETTER -- ValidatorResult carries five
    fields and fail_class is already on the wire -- but CORRECT IN SUBSTANCE, because ValidatorFailClass
    is a CAUSE-of-failure axis (schema_violation / semantic_inconsistency / safety_policy /
    resource_constraint / external_rejection), not a retry-disposition axis, and no ValidatorFailClass ->
    ValidatorRetryExitClass mapping exists anywhere in harness-*/src. Recorded so it is not re-proposed
    as a cheap fix. ONE CARRIED CLAIM SHARPENED: the section 9.2 half is blocked by a NAME GATE
    specifically, not by attribute-blindness -- _conditional_always_sampled at sampling_mode.py:239-241
    DOES read the permanence attribute, but only inside an event_name.startswith(validator.fail.) gate
    the live validator.evaluate span never satisfies. The section 9.2 half remains OUT OF SCOPE for this
    fork and deferred to B-137 step (3).'
  council: 'No. Wiring a declared-but-unset attribute inside a cleared schema, with a named owner (C7/OD). The
    only judgement is step (2)''s scope question, which grounding answers rather than deliberation.'
  heading: '### B-124 · `validator.fail.permanence` is never set on any span, so its OD §9.2 always-sampled row
    and its §10.2 tail-keep trigger are both inert *(surfaced at the B-116 council deliberation (C7 A2
    consultant, registered adjacency 2), 2026-08-07)*'
- id: B-125
  title: 'harness.breaker.tool_id is a homonym-in-waiting: it echoes the breaker KEY at every shipped trip
    while its contract gloss promises a tool identity'
  status: closed
  pr: '#1277 (the B-119/B-125 observability-family disposition leg -- doc/register only, no code and no
    design-substrate edit). Closed under DISPOSITION (a): the breaker-key echo is KEPT, real tool ids
    are FORBIDDEN on this attribute, and the limit is DECLARED with a promotion trigger -- the B-101
    (b)-PLUS declared-bound shape (Runtime v1.114). NO shipped behaviour changes. The OD §7.1 / CP §3.5
    gloss corrections are OWED and carried as a NAMED RIDER on the next OD/CP spec delta (see close_out);
    they are NOT applied here because this leg is doc/register-pure and edits no design-substrate file.'
  summary: 'WHAT IT IS. The attribute is declared at OD §7.1 as "Specific tool ID the failures correlate with
    (when scope is per-model and failures correlate with a specific tool)"
    (design-substrate/Spec_Operational_Discipline_v1_32.md:43) and at CP §3.5 as "per-model scope
    correlation" (design-substrate/Spec_Control_Plane_v1_2.md:402); it sits in the nine-member
    HARNESS_BREAKER_ATTRIBUTES tuple at harness-od/src/harness_od/harness_breaker_schema.py:98
    (re-grounded at HEAD — C7 cited :101, which at HEAD is harness.breaker.cooldown_ms). What it
    actually carries is the breaker key: RetryBreakerRegistry.emit_breaker_transition_event sets
    effective_tool_id = transition.identifier whenever tool_id is None at PER_MODEL scope
    (harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py:459-461), with the default stated as
    contract in the docstring (:445-446). Since PER_MODEL is the only scope constructed (B-119), every
    shipped trip emits harness.breaker.tool_id = "provider:model". THE CORRECTION CHAIN, RECORDED
    BECAUSE IT IS THE ROW''S WHOLE SUBSTANCE. C7 proposed at A2 populating the attribute with a real
    tool id to discriminate a memory-tool trip, on the claim it is "never populated" and that
    BreakerTransition has no such field. BreakerTransition indeed has no field
    (retry_breaker.py:183-189) — but the registry supplies the default, so the wire value is populated.
    C11 falsified the claim at cross-read by direct read of :459-461; C7 formally withdrew it at E2b:
    "My A2 claim that harness.breaker.tool_id is ''never populated'' is FALSE and I withdraw it." The
    consequence C11 drew and C7 adopted against his own proposal: populating it with a real tool id
    makes one attribute carry two domains — tool identity at one call site, breaker-key echo at every
    other — discriminable only by string shape. That is exactly the cross-namespace homonym C7 had just
    refused for capability_shortfall (harness_breaker_schema.py:141-143). WHY IT IS REGISTERED RATHER
    THAN REPAIRED. C7 declined the repair rather than pricing it, on his own B-131 standard: the skip
    must demote the detail, never delete it — and suppressing the :459-461 default would delete the only
    populated meaning the attribute has at every shipped PER_MODEL trip. He also recorded the full price
    had he taken it: the default removal, the :442-446 contract docstring, every witness asserting the
    default, and a re-read of CP §3.5''s "per-model scope correlation" gloss. It is moot for B-116''s
    member #5 (conceded — there is no #5 trip left to discriminate) and not owed for the members that
    still trip, because harness.breaker.cause is populated at all three real record_failure() sites
    (retry_breaker.py:451-453, B-38), so those trips are already discriminated. SURFACED AT THE B-116
    COUNCIL DELIBERATION, C7 A2 consultant, falsified by C11 at cross-read and formally withdrawn by C7
    at the E2b reconcile, carried as rider (d) of the B-116-t3 leg, 2026-08-07.'
  close_out: '(1) Re-verify the default and that PER_MODEL is still the only constructed scope — this couples to
    B-119, since building PER_PROVIDER changes which trips receive the default at all. (2) Decide the
    disposition BEFORE any caller passes a real tool_id: (a) keep the breaker-key echo and forbid real
    tool ids on this attribute — in which case the OD §7.1 and CP §3.5 glosses are wrong as written and
    owe a correction; or (b) suppress the default and mint a separate attribute for the key echo, at
    C7''s enumerated price above. (3) The trigger is a caller passing a real tool id — that is the
    moment the homonym goes live and the attribute becomes uninterpretable without a string-shape
    heuristic. This row must be dispositioned before that, not after. GROUNDED + DISPOSITIONED 2026-08-08
    (#1277) -- DISPOSITION (a), DECLARED BOUND. This paragraph states the CURRENT result; the
    registration text above is preserved and superseded where it conflicts. STEP (1) RE-VERIFIED AT HEAD
    49591934, WITH A LINE-DRIFT CORRECTION. B-118 (#1275) MOVED the default: it is now at
    harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py:626-628 (effective_tool_id = tool_id
    at :626; if effective_tool_id is None and transition.scope is BreakerScope.PER_MODEL at :627;
    effective_tool_id = transition.identifier at :628), and the contract docstring is now at :609-612.
    The registration''s :459-461 and :445-446 are STALE CITES, not falsified findings -- the mechanism is
    byte-identical. The attribute still sits at harness_breaker_schema.py:98 in the nine-member tuple.
    PER_MODEL is still the ONLY constructed scope (B-119, closed at this same leg as a forward-only
    record), so EVERY shipped trip carries harness.breaker.tool_id = "provider:model". TWO GROUNDED
    FACTS THAT CHANGE THE CALCULUS. (i) THE TRIGGER IS ARCHITECTURALLY DISTANT, NOT IMMINENT. Step (3)
    names the trigger as "a caller passing a real tool id". At HEAD there is exactly ONE production
    caller -- _emit_breaker_transition at retry_breaker_fallback.py:1477-1482 -- and it passes NO
    tool_id. The composer that would supply one does not participate at all: retry_breaker_tool.py:152-153
    states "per Q1a=(i) ratification, get_breaker / breaker transition events are NOT invoked at v1.15".
    So a real tool id can arrive only AFTER a tool-scope breaker lands -- a separate, explicitly-ratified-
    as-absent surface. The homonym cannot go live by accident. (ii) C7''S ENUMERATED PRICE FOR OPTION (b)
    IS OVERSTATED AT HEAD, FALSIFIED BY COUNT. "Every witness asserting the default" is ONE test:
    harness-runtime/tests/test_lifecycle_retry_breaker.py:583-604 -- and it asserts only
    emission.attribute_count == 8, never the VALUE. The only other harness.breaker.tool_id occurrences in
    tests are attribute-NAME list assertions (harness-od/tests/test_harness_breaker_schema.py:33,
    harness-cp/tests/test_retry_fallback_namespace.py:49). (That witness constructs the breaker with
    identifier "search-tool" -- a tool-shaped key at PER_MODEL scope, the homonym in miniature.) The
    price correction does NOT flip the disposition: C7''s substantive objection -- that suppressing the
    default DELETES the only populated meaning the attribute has at every shipped trip, violating his own
    B-131 "demote the detail, never delete it" standard -- stands unchanged and is what decides it.
    STEP (2) DECIDED -- (a), NOT (b). Keep the breaker-key echo; FORBID real tool ids on this attribute;
    declare the limit. This is the B-101 (b)-PLUS shape ratified at Runtime v1.114: a limit DECLARED, not
    fixed, with a normative promotion trigger keyed on MECHANISM. THE PROMOTION TRIGGER (falsifiable, one
    disjunct): the FIRST caller passing a non-None tool_id to emit_breaker_transition_event. That is the
    instant the attribute carries two domains discriminable only by string shape, and it re-opens this
    row to disposition (b) -- suppress the default, mint a separate attribute for the key echo -- at the
    (now measured) price above. A second, weaker trigger is B-119''s: constructing PER_PROVIDER changes
    which trips receive the default at all. STEP (5) OF B-119 IS THEREFORE DISCHARGED HERE: with one
    scope, the :627 guard is vacuously-true and this row collapses to the declaration. THE OWED GLOSS
    CORRECTIONS, CARRIED AS A NAMED RIDER RATHER THAN APPLIED (the #1263 stale-prose-carrier rider
    precedent). Under (a) the two contract glosses are WRONG AS WRITTEN and owe a correction, which this
    doc/register leg deliberately does NOT make because it edits no design-substrate file: (1) OD §7.1
    "Specific tool ID the failures correlate with (when scope is per-model and failures correlate with a
    specific tool)" (design-substrate/Spec_Operational_Discipline_v1_32.md:43) -- owed correction: the
    shipped meaning is the BREAKER KEY ("provider:model"), a real tool id is forbidden until the
    promotion trigger fires. (2) CP §3.5 "harness.breaker.tool_id (string; per-model scope correlation)"
    (design-substrate/Spec_Control_Plane_v1_2.md:402) -- owed correction: same. RIDER OWNER: the NEXT
    OD or CP spec delta that opens §7.1 or §3.5 respectively MUST carry these two corrections; they are
    NOT a standalone arc. The code-side docstring at retry_breaker.py:609-612 already states the default
    as contract and needs no change. ROW CLOSED; the limit is declared, the trigger is stated, and no
    shipped behaviour moves.'
  council: 'No. An attribute-domain call owned by C7/OD, already argued to a conclusion in venue by two voices
    including a formal withdrawal. What remains is choosing (a) or (b) and paying for it.'
  heading: '### B-125 · `harness.breaker.tool_id` is a homonym-in-waiting: it echoes the breaker **key** at
    every shipped trip while its contract gloss promises a **tool identity** *(surfaced at the B-116
    council deliberation (C7 A2 -> C11 cross-read falsification -> C7 E2b withdrawal), 2026-08-07)*'
- id: B-126
  title: The retry.* namespace has three different cardinalities (manifest 4, code 6, wire 8 after B-116) and only
    the first is verified
  status: closed
  summary: 'WHAT IT IS. CP §3.5 declares the retry.* namespace, and the CP export manifest pins it at attribute_count=4
    (harness-cp/src/harness_cp/cp_namespace_export_manifest.py:131-138), feeding the CP_EXPORTED_ATTRIBUTE_COUNT
    63-sum acceptance (:165-170). The code-side RETRY_NAMESPACE_SCHEMA already carries six (harness-cp/src/harness_cp/retry_fallback_namespace.py:168-207,
    whose own docstring records "v2.3 amendment from 4"; the contract re-table is design-substrate/Spec_Control_Plane_v1_3.md:76,
    against the four-attribute v1.2 row at Spec_Control_Plane_v1_2.md:403). On top of that, the shipped retry.skipped.reason
    / retry.skipped.candidate pair (harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:678-679)
    rides the retry.* namespace outside any §3.5 declaration. WHY THAT IS LEGAL TODAY, AND WHY C7 STILL FILED IT
    [HIGH]. The pair is authorized by the Runtime §14.6 discretion bullet — "Whether the wrapper emits a separate
    retry.skipped span event on breaker-open candidate skip, or just an outer-span attribute — defer to OTel telemetry-volume
    discretion at implementation; spec-MUST is just that the skip is observable" (design-substrate/Spec_Harness_Runtime_v1.md:4213;
    re-grounded at HEAD — C7 cited :4195, which at HEAD is blank). C7''s point is that this is precedent, and precedent
    accumulates: B-116''s ratified terms t1+t2 add two more undeclared retry.* attributes (retry.breaker_waived.reason
    / .candidate), which he required be "filed alongside" the existing pair rather than silently added. The manifest''s
    4, the code''s 6, and the wire''s 8-after-B-116 are three numbers for one namespace, and only the first is cardinality-verified.
    SURFACED AT THE B-116 COUNCIL DELIBERATION, C7 A2 consultant registered adjacency 3, 2026-08-07.'
  close_out: '(1) Recount all three at the then-current HEAD — these drift every arc, so a stale count is worse
    than none. (2) Decide whether the discretion lane has a boundary. If it does not, say so in the contract: an
    unbounded lane means the pinned attribute_count=4 is not a cardinality guarantee about the namespace, only about
    the declared subset, and the 63-sum acceptance means less than it reads. That is the finding either way. (3)
    If bounded, the cheapest shape is a declared discretion-lane list the manifest acknowledges without ingesting
    — no §3.5 amendment, no cardinality bump. (4) Do NOT use this row to block B-116''s t1/t2: those were ratified
    in venue under exactly this discretion, and C7''s requirement was filing, not refusal. Reading this row as a
    veto would invert its author''s own position.  RE-GROUNDED 2026-08-09 (PR #1285, batch pass) -- SHARPENED, AND
    THIS ROWS OWN COUNT IS WRONG. The three- divergent-cardinalities structure holds (manifest attribute_count=4
    at cp_namespace_export_manifest.py:129-135; RETRY_NAMESPACE_SCHEMA exactly 6, test-enforced at harness- cp/tests/test_retry_fallback_namespace.py:99),
    but the wire figure is UNDERCOUNTED. An exhaustive literal sweep of harness-*/src returns FIVE undeclared keys,
    not four: retry.skipped.reason, retry.skipped.candidate, retry.breaker_waived.reason, retry.breaker_waived.candidate,
    AND retry.terminal (retry_breaker_fallback.py:1253, the audit-signing fail-closed path). git log -S shows retry.terminal
    landed at PR #1065 / U-RT-136, BEFORE the 2026-08-07 council that filed this row -- so it is not new drift,
    it was missed at filing. Corrected total: 5 declared literal retry.-prefixed + 5 undeclared = TEN distinct wire
    keys, not eight. STEP (2) IS ANSWERED BY THE SPEC, NOT OPEN: Runtime section 14.6s discretion bullet (Spec_Harness_Runtime_v1.md:4279,
    drifted from the rows :4213) states no cap -- the lane is intentionally unbounded, so this is manifest-conformance,
    not a fork. SHAPE: BUILD-NOW -- declare the discretion lane (the rows own cheapest option) without ingesting
    the five into the 63-sum, correct the count language, and add a drift-catching test asserting the wire-literal
    set equals the schema names UNION the declared lane. NOT superseded: no commit has touched either file since
    PR #1109. CLOSED 2026-08-09 (this arc) -- BUILT, AND THE ROW''S OWN FRAMING WAS FALSIFIED IN THE PROCESS. The
    three-cardinality structure holds and the corrected wire count of TEN is CONFIRMED by two independent sweep
    shapes (a broad literal sweep and a precise emission-position sweep, both over harness-*/src, both returning
    exactly 10 distinct retry.-prefixed ATTRIBUTE keys = 5 declared at C-CP-03 3.5 + 5 undeclared). BUT THE ''DISCRETION
    LANE'' PREMISE IS WRONG, and that is the arc''s load-bearing finding: of the 5 keys beyond 3.5, only retry.skipped.candidate
    rides pure telemetry-volume discretion. The other FOUR are MANDATED BY NAME at Runtime-spec normative step bullets
    and binding terms -- retry.skipped.reason at Spec_Harness_Runtime_v1.md:4217 (14.6 step 4, which names the token),
    retry.breaker_waived.reason/.candidate at :4364 (14.6.3 terms t1+t2, ''Every waived charge sets a string attribute
    pair''), and retry.terminal at :4227-4230 (14.6) plus :5684-5687 (14.9). So retry.* is declared at TWO contract
    venues and the C-CP-24 24.1 manifest counts only one of them; it is not an unbounded free-for-all with 4 declared
    keys. BUILT: RETRY_WIRE_REGISTER (7 rows: attribute_name + declaring_authority + binding + emitted) and RETRY_SPAN_AND_EVENT_NAMES
    (3 rows, keeping span/event NAMES out of the attribute count -- the row''s own count conflated them) at harness-cp/src/harness_cp/retry_fallback_namespace.py,
    homed beside the 3.5 schema because splitting the namespace''s accounting across two packages is the fragmentation
    this row names; count-language corrections at cp_namespace_export_manifest.py; and SEVENTEEN drift-catching tests /
    29 assertions at harness-cp/tests/test_b126_retry_wire_register.py, each load-bearing line shown RED under a
    mutation probe -- recounted programmatically at the arc close, correcting an earlier 8 here and a
    contradictory 10 in the prose home, both round-1/round-3 figures never refreshed as the file grew and both
    caught by the merge gate (the very count-drift class this arc kept finding elsewhere) (an undeclared emitted
    key fails BOTH sweeps; a lying `emitted` flag fails the precise sweep in either direction; relabelling an attribute
    as a span NAME to silence the broad sweep fails the precise one). STEP (2) IS ANSWERED AS THE RE-GROUNDING SAID
    -- the lane is unbounded (Runtime :4279 and :5729 state no cap) -- so the register is a REGISTER, not a cap:
    a new key is permitted, but not invisibly. NO cardinality bump, NO 3.5 amendment, NO contract number, NO plan
    leg, NO design-substrate edit. TWO NEW FINDINGS SURFACED AND REGISTERED RATHER THAN ABSORBED: `B-144` (24.1.B''s
    Attribute count column was never re-tabled after 3.5''s two amendments, and the code manifest resolves the resulting
    divergence INCONSISTENTLY per row) and `B-145` (retry.backoff_ms + retry.cause_class are mandated at four Runtime
    step bullets and set by ZERO producers). A THIRD stale figure was found and fixed in place: the manifest''s
    docstring and comment both claimed a 63-attribute sum while the derived constant is 65, and the sibling test
    misattributed the +2 to 24.1.A (34->36) when it belongs to 24.1.B (25->27).'
  council: No at registration — nothing is decided until the recount. Conditionally yes at step (2) if it leans
    toward closing the lane, which trades emission-site observability freedom (C7) against the cardinality and cost
    guarantees the pinned manifest exists to give (C2/C6). Do not convene before the recount, per the workspace
    probe-first discipline.
  pr: '#1289 (the retry.* wire register + drift catchers; B-144 + B-145 minted)'
  heading: '### B-126 · The `retry.*` namespace has three different cardinalities (manifest 4, code 6, wire 8 after
    `B-116`) and only the first is verified *(surfaced at the B-116 council deliberation (C7 A2 consultant, registered
    adjacency 3), 2026-08-07)*'
- id: B-127
  title: 'The breaker-open advance passes no exhaustion_cause, so an all-breakers-open chain reports RETRY
    exhaustion when no attempt ran'
  status: closed
  summary: 'WHAT IT IS. _advance_or_exhaust takes exhaustion_cause: str = "per-candidate-retry-exhaustion"
    (harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:866) and writes it onto the
    terminal fallback.exhausted event as fallback.exhaustion_cause (:891, in the attribute dict at
    :888-897). The capability-shortfall pre-check passes exhaustion_cause="capability-shortfall"
    explicitly (:656), and the method''s own docstring states why: "so an all-candidates-incapable chain
    is not mis-classified as retry exhaustion (no provider attempt ran in that case)" (:874-876). The
    breaker-open pre-check, twenty-eight lines below the shortfall one, passes no exhaustion_cause
    (:684-691) and therefore takes the default. So an all-breakers-open chain exhaustion is reported as
    retry exhaustion — the identical mis-classification the sibling path was written to prevent, on the
    identical premise (no attempt ran). SCOPE AND OWNERSHIP AS THE VOICE LEFT IT. C1 surfaced this as an
    adjacency he "did not see filed", one line away from C11''s :683 row (B-131), and routed the value
    to C7/OD while keeping only the naming: "I own naming that the topology emits the wrong-shaped
    terminal event." He explicitly registered it rather than folding it into B-116''s diff. It fires
    under either B-116 reading, and after ratification it still fires for the members that keep
    counting. PRICE, STATED BECAUSE IT IS ITSELF THE ARGUMENT. One keyword argument at one call site,
    plus a value decision. The sibling path is the shipped precedent for both the fix and its rationale.
    A defect this cheap to close is one to close rather than carry. SURFACED AT THE B-116 COUNCIL
    DELIBERATION, C1 A2 consultant standing observability pre-check, 2026-08-07.'
  close_out: '(1) Re-verify the default, both call sites, and that fallback.exhaustion_cause is written
    unconditionally — unlike its sibling fallback.last_failure_detail, which is written only if
    last_failure_detail is not None (:893-894; that asymmetry is B-131). (2) Select rather than mint, if
    possible: FallThroughCause.BREAKER_OPEN already exists byte-exact in the closed CP §3.5
    fallback.cause domain (harness-cp/src/harness_cp/fall_through_procedure.py:67-68;
    design-substrate/Spec_Control_Plane_v1_2.md:401). But fallback.cause and fallback.exhaustion_cause
    are different attributes, and the value domains must be checked before reuse — the same homonym
    discipline C7 applied when refusing capability_shortfall. (3) Pass it at :684-691. (4) Witness both
    — a breaker-open exhaustion carrying the new value, and a per-candidate retry exhaustion still
    carrying the default, so the fix is discriminating rather than blanket.  RE-GROUNDED
    2026-08-09 (PR #1285, batch pass) -- INTACT, AND CONFIRMED BY EXECUTION RATHER THAN READING. A
    standalone probe tripped BOTH chain candidates breakers OPEN with cooldown unexpired and dispatched:
    inner provider calls = 0, skip reasons = [breaker-open, breaker-open], and fallback.exhaustion_cause
    = per-candidate-retry-exhaustion. A chain in which NO ATTEMPT EVER RAN reports retry exhaustion.
    Cites re-pinned (the rows drifted): the capability-shortfall pre-check passes exhaustion_cause
    explicitly at retry_breaker_fallback.py:768, while the breaker-open / half-open-trial skip branch
    calls _advance_or_exhaust at :888-895 with NO exhaustion_cause kwarg, taking the default at the :1122
    signature; skip_reason is set at :809. STEP (2) SETTLED EMPIRICALLY: fallback.exhaustion_cause is
    declared in NO spec (zero hits across the CP and OD specs) -- it is a free str carrying its own
    hyphenated local vocabulary, so the fix is a matching literal breaker-open, NOT reuse of the
    underscored CP FallThroughCause enum. SHAPE: BUILD-NOW -- one keyword argument at one call site, zero
    contract risk. Witness: an all-breakers-open chain asserting exhaustion_cause == breaker-open with
    zero inner dispatches, PLUS a companion assertion that ordinary per-candidate retry exhaustion still
    defaults correctly (a discriminating pair, per the rows own step 4). NOT superseded -- B-118 touched
    adjacent lines in the same block but did not add the kwarg. LAND TOGETHER WITH B-131: same call site,
    different attributes, neither fix resolves the other.  CLOSED
    2026-08-09 AT PR #1287, PAIRED WITH B-131 (same call site). The skip branch now passes
    exhaustion_cause=skip_reason to _advance_or_exhaust, so an all-breakers-open chain reports the ACTUAL
    terminal cause instead of silently defaulting to per-candidate-retry-exhaustion. THE VALUE IS PASSED
    THROUGH, NOT HARD-CODED, AND THAT IS LOAD-BEARING: skip_reason takes TWO values on this path --
    breaker-open and half-open-trial-in-flight (the second added by the B-118 arc) -- so a hard-coded
    breaker-open literal would MISLABEL every half-open-trial skip. A dedicated witness exists for
    exactly that mutation and PD-8 probe P2 (hard-code the literal) turns it RED. No new vocabulary is
    invented: the emitted cause now matches the retry.skipped.reason already emitted on the same path.
    THREE WITNESSES: the all-breakers-open chain asserting cause == breaker-open AND zero inner
    dispatches (the no-attempt-ran half is the point); a DISCRIMINATOR asserting ordinary retry
    exhaustion still reports per-candidate-retry-exhaustion (without it, a mutation hard-coding the new
    value everywhere would pass); and the half-open-trial variant, built on a REAL event-gated concurrent
    in-flight trial mirroring the B-118 permit test rather than a synthesized state. PD-8: P1 (drop the
    kwarg) turns witnesses 1+3 RED; P2 (hard-code the literal) turns witness 3 RED. Both matched
    prediction. _advance_or_exhaust signature and default UNCHANGED; the capability-shortfall pre-check
    and the retry.skipped event UNTOUCHED.'
  council: 'No -- and the ORIGINAL premise of this field was WRONG, corrected here rather than carried
    into a closed row. It read: a one-value fix inside a closed, declared domain with a shipped sibling
    precedent and a named value owner. THE DOMAIN IS NOT CLOSED AND NOT DECLARED. Grounding at PR #1285
    established, and the merge-gate spec-conformance lens at PR #1287 independently re-verified, that
    fallback.exhaustion_cause has NO declared value domain anywhere in design-substrate/**: the only
    mention outside the register is Spec_Harness_Runtime_v1.md:4231, which merely NAMES the attribute in
    a list, while C-CP-04 section 4.2 (Spec_Control_Plane_v1_2.md:448-455) declares a value domain for a
    DIFFERENT attribute, fallback.cause (breaker_open / rate_limit_storm), and section 4.4 says only emit
    fallback.exhausted with no domain at all. The conclusion is UNCHANGED -- still No council -- but for
    a STRONGER reason than the field originally gave: because the attribute is a free str with its own
    local hyphenated vocabulary, adding values owes no spec leg and there is no closed domain to contest.
    The step (2) reuse question the field wanted routed was itself settled empirically at the grounding
    pass (the emitted cause now matches the retry.skipped.reason already emitted at the same site, rather
    than reusing the underscored CP FallThroughCause enum).'
  pr: '#1287 (the paired B-127 + B-131 exhaustion-attribution fix at the shared skip call site)'
  heading: '### B-127 · The breaker-open advance passes no `exhaustion_cause`, so an all-breakers-open chain
    reports **retry** exhaustion when no attempt ran *(surfaced at the B-116 council deliberation (C1 A2
    consultant, standing observability pre-check), 2026-08-07)*'
- id: B-128
  title: 'The dispatcher borrows the validator-side PERMANENT_FAIL_EXIT label while performing none of the
    escalation that label routes to'
  status: closed
  summary: 'WHAT IT IS. The fail-fast branch tags the inner attempt span retry.fail_class =
    ValidatorRetryExitClass.PERMANENT_FAIL_EXIT.value
    (harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:1024-1027). C-CP-21
    §21.1''s five-class fail taxonomy routes permanent-fail-exit as "SKIP STAIRCASE; route directly to
    C11 HITL (validator-escalation placement per §17.1)"
    (design-substrate/Spec_Control_Plane_v1_2.md:1852, in the §21.1 table at :1843-1853). But that
    taxonomy is validator-side: the dispatcher borrows the label for a span attribute while the
    exception propagates to a plain step failure — workflow_driver.py:5564-5572 returns RunStatus.FAILED
    with a terminal_step_index, and no HITL invocation occurs. C1''s statement: the escalation the
    attribute names is not the escalation the topology performs. THE OPERATIVE WARNING, WHICH IS WHY IT
    IS A ROW AND NOT A FOOTNOTE. C1 flagged it [MODERATE], declared it "not this fork''s business", and
    attached the reason it must still be recorded: the label "must not be quietly used as evidence that
    an escalation path exists." That is a stale-carry hazard aimed at future readers of the span
    attribute, not at B-116. SURFACED AT THE B-116 COUNCIL DELIBERATION, C1 A2 consultant HITL
    pre-check, 2026-08-07.'
  close_out: '(1) Enumerate producers and consumers first. Every producer of retry.fail_class =
    permanent-fail-exit and — the load-bearing half — every consumer that reads the class as a routing
    signal rather than as telemetry. The divergence only bites where such a consumer exists. If there is
    none, the row is documentation-shaped, and that is the honest outcome to record rather than a reason
    to build. (2) Then decide: either the dispatcher stops borrowing the validator label (a new
    dispatch-side value in the retry.fail_class enum — a closed-domain extension against a C5/C7-owned
    vocabulary, expensive), or the contract records that retry.fail_class and validator.fail.class share
    a vocabulary but not a routing (cheap, and matches what the code does today). (3) Do NOT resolve it
    by wiring the HITL escalation. No voice proposed that, C1 and C11 concurred independently that no
    gate belongs on this path, and B-116 ratified term t5 (no HITL gate here). A fix that adds one would
    contradict a ratified term. WHAT WOULD FALSIFY IT. A consumer that routes on the dispatcher-set
    value would upgrade this from a labelling note to a live defect — that is the single check step (1)
    exists to run.  RE-GROUNDED
    2026-08-09 (PR #1285) -- CLOSED AS A FORWARD-ONLY RECORD, BECAUSE THIS ROWS OWN FALSIFIER WAS RUN AND
    DOES NOT FIRE. Premise INTACT as stated: the fail-fast branch sets retry.fail_class =
    PERMANENT_FAIL_EXIT at retry_breaker_fallback.py:1281-1284 (row cited :1024-1027, drifted) and
    returns _PerCandidateTerminal at :1315-1318 without raising; the chain then raises
    RetryBreakerFallbackExhaustedError, and workflow_driver.py has NO name-match arm for it among its
    :5401-5560 handlers, so it falls to the generic terminal branch at :5558-5566 -- no HITL routing
    anywhere, while CP section 21.1 says permanent-fail-exit routes directly to C11 HITL. BUT the rows
    close-out step (1) demanded an enumeration of readers, and it was run: SIX hits for retry.fail_class
    across harness-*/src, of which five are producers and the ONLY consumer-shaped reference is
    per_class_attribute_composition.py:209s tail_keep_on_attribute -- a PRESENCE-ONLY tail-sampling
    trigger (corroborated by the in-code comment at retry_breaker_fallback.py:1392). NOTHING branches
    control flow on the string value. The row states its own disposition for exactly this result: If
    there is none, the row is documentation-shaped, and that is the honest outcome to record rather than
    a reason to build. Closed on that instruction. NOTE: B-138 demoted ValidatorRetryExitClass off
    validator.fail.class -- a DIFFERENT attribute from retry.fail_class -- so it does not touch this
    site. DEMAND TRIGGER IF IT RETURNS: any future commit adding a reader that branches on the VALUE of
    retry.fail_class (re-run the six-hit enumeration and inspect for value-based branching).'
  council: 'No at registration. Conditionally yes at step (2) only if the enum-extension branch is chosen, which
    touches a closed C5/C7-owned domain and trades vocabulary precision against domain closure — and
    only after step (1) shows a routing consumer exists.'
  pr: '#1285 (the B-126..B-132 batch re-grounding pass; B-128 closed as a forward-only record on its own
    falsifier)'
  heading: '### B-128 · The dispatcher borrows the validator-side `PERMANENT_FAIL_EXIT` label while performing
    none of the escalation that label routes to *(surfaced at the B-116 council deliberation (C1 A2
    consultant, HITL pre-check), 2026-08-07)*'
- id: B-129
  title: 'The memory-tool argument-shape degradation signal has no vocabulary home after B-116 demoted it off
    the breaker'
  status: registered_finding
  summary: 'WHAT IT IS. B-116 ratified member #5 (MemoryToolExecutionInputError) as don''t-count — but C9''s
    substantive point survived his own concession: a model that repeatedly cannot emit the declared
    memory-tool argument shape is information, and reading (II) orphans it. The signal is real at the
    raise site: llm_dispatch.py:4376-4383 validates the model-supplied scope_ref, and the in-file
    comment at :4362-4367 explicitly distinguishes it from the two harness-internal sibling checks
    immediately above (:4368-4375), which the B-88 A-ii re-typing moved to
    MemoryToolExecutionInternalError. What is missing is a home for the signal. WHERE THE TYPE SYSTEM
    POINTS, PER C1. The only BreakerCause that would fit is capability_shortfall, whose in-code scope
    explicitly disclaims exactly this use — "names a separate, out-of-scope cross-family
    fallback-exhaustion path, not a per-step dispatch failure"
    (harness-od/src/harness_od/harness_breaker_schema.py:141-143, mirroring OD §7.1''s forward-slot note
    at design-substrate/Spec_Operational_Discipline_v1_32.md:50). Meanwhile
    FallThroughCause.CAPABILITY_SHORTFALL (harness-cp/src/harness_cp/fall_through_procedure.py:64-65) is
    the per-dispatch chain-advance vocabulary. C1: "the type system is pointing at the fall-through
    path, not the counter" — and he declined to author a value, because FallThroughCause is closed at 4
    and its own docstring names extension "a Workflow §4.1.2 Class-2 D-revision" (:52-58), with the
    domain CP-declared at design-substrate/Spec_Control_Plane_v1_2.md:401. STATED AGAINST INTEREST. C9
    accepted the placement while naming exactly what it costs him: "be honest about what it gives my
    domain: a registered home, not a live signal. I accept that trade only because the alternative live
    signal is a corrupted counter." C11 accepted it conditional on the B-116 t3 sampling term, since a
    signal relocated from an inviolable §9.2 floor to a base-rate surface is dropped nine times in ten
    at team-binding × self-hosted. SURFACED AT THE B-116 COUNCIL DELIBERATION, C1 A2 consultant,
    conceded by C9 at cross-read and conditioned by C11, 2026-08-07.'
  close_out: '(1) Re-verify both enums'' scoping text and the closed-at-4 cardinality. (2) Decide whether a
    fall-through value is warranted at all — and note this requires a POPULATION question, not a code
    read. Does the shape actually recur in practice? That cannot be answered from the source, so the
    honest first step is measurement, not a mint. (3) If warranted, route the extension as the Class-2
    D-revision the enum''s own docstring names (C5/C7/OD own the value) — a spec leg, not an impl leg.
    (4) Choose the producer site deliberately: B-116''s guard means #5 no longer reaches record_failure
    at all, so the emission must be placed on the fall-through path itself, not inherited from the old
    one. (5) Note the B-116-t3 coupling — C11''s acceptance is conditioned on the sampling guarantee, so
    landing a value onto a base-rate-only surface satisfies C1''s placement while failing C11''s
    condition.  RE-GROUNDED
    2026-08-09 (PR #1285) -- INTACT, WITH ONE OF ITS TWO STATED OBSTACLES ALREADY REMOVED BY A LATER ARC.
    Raise site re-pinned to llm_dispatch.py:4414-4419 (row cited :4376-4383) with the discriminating
    comment at :4397-4404; the harness-internal siblings raise MemoryToolExecutionInternalError at
    :4406-4411; MemoryToolExecutionInputError is confirmed in the B-116 waiver set at
    retry_breaker_fallback.py:425; BreakerCause.CAPABILITY_SHORTFALLs out-of-scope disclaimer is byte-
    exact at harness_breaker_schema.py:141-143; FallThroughCause is confirmed CLOSED at four members with
    the Class-2-D-revision docstring at fall_through_procedure.py:52-70. THE SHARPENING: this rows close-
    out step (5) worries that its own preferred placement (the fall-through path) lands on a base-rate-
    only surface dropped nine times in ten, making C11s condition unmet. THAT WORRY IS STALE.
    fallback.exhausted was already declared always-sampled at CP section 3.5, and the OD-side ingestion
    gap was CLOSED by the landed B-116-t3 leg -- it is now row 19 of the OD section 9.2 roster
    (Spec_Operational_Discipline_v1_38.md:78). So C11s stated condition is ALREADY SATISFIED for the
    placement the row itself recommends, and the placement is cheaper than the row assumes. SHAPE: DEFER-
    WITH-TRIGGER, unchanged -- step (2) is a MEASUREMENT question (does the shape actually recur in
    practice) that a read-only pass cannot answer, and minting a FallThroughCause member is a closed-
    domain Class 2 D-revision owing a spec leg. TRIGGER: a measured recurrence population, or an
    operator-visible incident.'
  council: 'No at registration. Conditionally yes at step (3): extending a closed, CP-declared domain to carry a
    reliability signal is the C9 ⊥ C7 tension (signal fidelity versus vocabulary closure) the
    deliberation already surfaced once and resolved by refusal. Convene only after step (2) produces a
    measured population — convening on an unmeasured one would repeat the mistake the probe-first
    discipline exists to prevent.'
  heading: '### B-129 · The memory-tool argument-shape degradation signal has no vocabulary home after `B-116`
    demoted it off the breaker *(surfaced at the B-116 council deliberation (C1 A2 consultant; conceded
    by C9, conditioned by C11), 2026-08-07)*'
- id: B-130
  title: 'All-candidates-permanently-ineligible is a reachable terminal state no canonical artifact declares'
  status: closed
  summary: 'WHAT IT IS. The breaker pre-check at
    harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:674-683 is not telemetry —
    it is a topology gate that deletes a candidate slot from the chain. Combined with B-118 (OPEN is
    absorbing for the process lifetime), a trip is an irreversible candidate-set deletion. C1''s end
    state: a declared N-candidate fallback chain that has silently become a null topology — every
    subsequent dispatch walks N slots, emits N retry.skipped events (:675-681), does zero work, and
    exhausts. What the design does declare, both verified at HEAD: candidate exhaustion within a
    dispatch (RetryBreakerFallbackExhaustedError, :899) and first terminal step failure within a run
    (harness-cp/src/harness_cp/workflow_driver.py:5564-5572, RunStatus.FAILED plus terminal_step_index).
    "All candidates permanently ineligible" is neither. THE AMPLIFIER NEITHER PRIMARY NAMED.
    _advance_or_exhaust is a multiplier: one candidate-independent fault is charged N times per
    dispatch, N = chain length (B-116''s executed Probe B: three breakers, one dispatch). So lengthening
    the fallback chain to improve resilience makes the harm strictly worse and trips the whole chain
    sooner — a topology decision inverting into a reliability regression. Scope after ratification,
    stated so the row is not read as larger than it is: B-116''s guard removes the four harness-internal
    members from the multiplier, so the shape narrows to the members that still count. It does not
    disappear. SURFACED AT THE B-116 COUNCIL DELIBERATION, C1 A2 consultant, 2026-08-07.'
  close_out: '(1) Re-verify the two declared terminations and that no third exists. (2) Decide whether the state
    needs naming at all — and weigh C1''s own three-level bound, which argues it may not: max_attempts=3
    within a candidate (retry_breaker_fallback.py:130-134), chain length then :899 within a dispatch,
    and first-failed-step termination within a run (workflow_driver.py:5564-5572), with the other
    topologies bounded too (evaluator-optimizer caps at _DEFAULT_EVALUATOR_OPTIMIZER_MAX_ITERATIONS = 3,
    workflow_driver.py:10555). For a candidate-independent fault the bound is one step per run, which is
    the honest argument for leaving the state undeclared. (3) If it is declared, do not mint twice — the
    natural carrier is the fallback.exhaustion_cause value B-127 is already about. (4) SEQUENCE B-118
    FIRST: wiring the half-open latch stops OPEN being absorbing, which weakens this row''s premise
    substantially and may retire it.  RE-GROUNDED
    2026-08-09 (PR #1285) -- CLOSED AS A FORWARD-ONLY RECORD; THE ROW IS PARTIALLY FALSIFIED AND ITS OWN
    STEP (4) ANTICIPATED THIS. The row bundles two claims and they part company at HEAD. CLAIM A (a
    fault-driven ZERO-ATTEMPT exhaustion is reachable) is STILL TRUE and was reproduced BY EXECUTION: a
    3-candidate chain with all three breakers pre-tripped OPEN and the clock pinned raised
    RetryBreakerFallbackExhaustedError with inner.calls == 0, three retry.skipped events and
    fallback.chain_length == 3. CLAIM B (that this is PERMANENT / IRREVERSIBLE, combined with B-118
    making OPEN absorbing for the process lifetime) is FALSE at HEAD: B-118 LANDED, and the SAME repro
    extended past the cooldown window recovered -- candidate 1 transitioned OPEN -> half_open -> closed
    on a successful trial and the dispatch returned normally. The state is BOUNDED AND SELF-HEALING, not
    an irreversible candidate-set deletion. The rows close-out step (4) said SEQUENCE B-118 FIRST ... may
    retire it; B-118 retired the framing that made this look like topology corruption rather than a
    circuit breaker working as designed for a genuinely-down provider set. The negative claim was still
    enumerated and HOLDS (only RT-FAIL-FALLBACK-EXHAUSTED and RunStatus.FAILED are declared), but with
    self-healing confirmed, the rows own step (2) lean -- the state may not need naming at all -- is
    stronger, not weaker. NO new terminal-state enum is warranted. The one useful residual -- making
    zero-attempt exhaustion DISTINGUISHABLE in telemetry -- is already owned by the cheaper sibling B-127
    and is not duplicated here. DEMAND TRIGGER IF IT RETURNS: any arc that removes or weakens cooldown-
    based half-open recovery, or a correlated-fault case that does NOT recover after cooldown while a
    candidates provider is healthy.'
  council: 'No at registration. A C1-domain termination-criterion question with C9''s mechanism underneath
    (T-perm-3, C1 ⊥ C9, fires literally here — the breaker is a reliability primitive performing an
    irreversible topology edit, and C1 asked that it be recorded as such on the row). But C1 argued both
    halves in venue and the probe-first answer — the driver''s own terminality — is grounded at HEAD.
    Re-convene only if step (2) leans toward declaring a new terminal state.'
  pr: '#1285 (the B-126..B-132 batch re-grounding pass; B-130 closed as a forward-only record on its own
    falsifier)'
  heading: '### B-130 · All-candidates-permanently-ineligible is a reachable terminal state no canonical
    artifact declares *(surfaced at the B-116 council deliberation (C1 A2 consultant), 2026-08-07)*'
- id: B-131
  title: 'The breaker-open skip DELETES the prior candidate''s failure detail instead of demoting it, so a
    chain exhaustion reports no cause at all'
  status: closed
  summary: 'WHAT IT IS. Before the breakers open, the operator''s terminal is correct and loud: _failure_detail
    composes "<Type>: <message>" bounded
    (harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:178-187),
    RetryBreakerFallbackExhaustedError.__init__ appends it as "; last failure: ..." (:169-175),
    _step_fail_class interpolates str(exc) (harness-cp/src/harness_cp/workflow_driver.py:1686-1687), and
    the CLI echoes it to stderr (harness-runtime/src/harness_runtime/cli/app.py:141-143 and :243-244).
    After a breaker opens, the pre-check sets last_failure_class = "breaker-open" and
    last_failure_detail = None (:682-683) — and the exhausted event writes the detail attribute only if
    last_failure_detail is not None (:893-894). So the overwrite does not set the field to None: it
    makes fallback.last_failure_detail vanish from the event entirely, while fallback.last_failure_class
    falls through to the literal "unknown" (:890). Within one dispatch, candidate 1''s real fail-fast
    detail (set at :998) is destroyed by candidate 2''s skip. THE MECHANISM, IN C7''S FRAMING WHICH C11
    ADOPTED VERBATIM. "A strictly-information-destroying write by a path that learned nothing" — a
    monotonicity violation on a last-write-wins diagnostic slot. The skip must demote the detail, never
    delete it. C11 adopted that as the row''s title at cross-read. It fires under either B-116 reading,
    for the members that still count — i.e. genuine provider outages arrive at the operator as "chain
    exhausted, no reason given". There is no compensating surface: zero breaker hits across
    harness-runtime/src/harness_runtime/cli/ (three files, re-verified at HEAD), so no operator-facing
    breaker readout exists to cite in either direction. THE CORRECTION CARRIED, NOT LAUNDERED. C11''s A1
    additionally called retry.skipped.reason = "breaker-open" false. C7 refused that half — "it is a
    proximate-mechanism statement about why THIS candidate was skipped, and the breaker WAS open;
    calling a true attribute false weakens the row" — and C11 conceded and withdrew the sentence at
    cross-read. This row therefore does not rest on any truth-value claim about retry.skipped.reason. It
    rests on the erasure mechanism, which C11 then grounded more sharply than either voice had written
    it. SURFACED AT THE B-116 COUNCIL DELIBERATION, C11 A1 primary refinement (c), reframed by C7 at A2
    and re-grounded by C11 at cross-read, 2026-08-07.'
  close_out: '(1) Re-verify the erasure chain — :682-683, :890, :893-894, :998. (2) Implement demotion rather than
    deletion: preserve the prior candidate''s detail (and class) across a skip instead of overwriting,
    or carry the skip as an additional fact alongside the retained detail. The open choice is the
    retention shape — first-write-wins, a list, or a two-field split — not whether to retain. (3)
    Respect the B-116 adjacency: B-116''s binding term (C11''s (a)) already requires the non-counted
    fault to keep carrying type(exc).__name__ and the bounded message on last_failure_detail at
    :1031-1035. This row and that guard touch adjacent lines and must not fight; land them in a known
    order. (4) Witness the multi-candidate case specifically — candidate 1 fails with a real detail,
    candidate 2 is skipped breaker-open, and the fallback.exhausted event must still carry candidate
    1''s detail. A single-candidate test cannot see this defect. (5) Note B-127 — the same terminal
    event''s exhaustion_cause is mislabelled on the same path; the two are one event apart and should be
    priced together. WHAT WOULD FALSIFY IT. Nothing found. Two voices grounded the mechanism
    independently at HEAD, and the one contested claim was withdrawn by its author rather than carried.  RE-GROUNDED
    2026-08-09 (PR #1285) -- INTACT IN MECHANISM, SHARPENED IN ONE SUB-CLAIM, AND REPRODUCED BY
    EXECUTION. The rows own step (4) scenario was run: candidate 0 fails with a real
    LLMDispatchPayloadShapeError diagnostic, candidate 1 (last) is breaker-open-skipped, and the
    fallback.exhausted event carries {chain_length: 2, last_failure_class: breaker-open,
    exhaustion_cause: per-candidate-retry-exhaustion} with the fallback.last_failure_detail key ABSENT
    ENTIRELY -- candidate 0s diagnostic is gone. THE SHARPENING: the row says last_failure_class falls
    through to the literal unknown. IT DOES NOT. last_failure_class is SET to the skip reason (breaker-
    open), which is truthy, so the or unknown fallback at :1146 never fires -- the attribute is
    overwritten with a REAL-BUT-WRONG value that masks candidate 0s actual failure class. That is worse
    than unknown, because it looks legitimate to a reader. Substance of the defect is unchanged; the rows
    wording was imprecise. Cites re-pinned (all drifted ~190-260 lines by B-118s insertions): erasure at
    retry_breaker_fallback.py:886-887 (row cited :682-683); conditional detail write at :1149-1150 (row
    cited :893-894); real detail set at :1256 (row cited :998). WITNESS GAP CONFIRMED: no existing test
    exercises the multi-candidate erasure -- test_breaker_open_skips_candidate_emits_retry_skipped skips
    candidate 0 with no prior real failure to erase. Zero breaker hits in harness-
    runtime/src/harness_runtime/cli/ confirms no compensating operator-facing surface. SHAPE: BUILD-NOW
    -- first-write-wins retention across a skip. NOT superseded: B-118 reshaped the surrounding control
    flow but the erasure line is untouched in substance. LAND TOGETHER WITH B-127: same call site,
    different attributes of the same event; neither fix resolves the other.  CLOSED
    2026-08-09 AT PR #1287, PAIRED WITH B-127 (same call site, different attributes of the same
    fallback.exhausted event; neither fix resolved the other). A skip no longer clobbers a REAL candidate
    diagnostic: the unconditional last_failure_class = skip_reason / last_failure_detail = None overwrite
    is now gated on a new local flag last_failure_is_real_attempt, set only at the one site where a
    genuine per-candidate attempt ran and failed. THE FLAG SHAPE WAS CHOSEN OVER A last_failure_class is
    None CHECK, AND THE REASONING IS LOAD-BEARING: a chain can take CONSECUTIVE skips with DIFFERENT
    reasons (breaker-open, then half-open-trial-in-flight on a later candidate), and those must keep
    overwriting each other exactly as before -- a None-check would have frozen the FIRST skip reason
    instead of the last, silently breaking pre-existing multi-skip behaviour. THE ROWS OWN SUB-CLAIM WAS
    CORRECTED AT GROUNDING and the fix reflects the corrected reading: last_failure_class never fell
    through to the literal unknown on this path, because the skip reason is truthy and the or unknown
    fallback never fired -- the attribute carried a REAL-BUT-WRONG value masking the actual failure
    class, which is worse than unknown because it looks legitimate. TWO WITNESSES: candidate 0 fails with
    a uniquely-identifiable real diagnostic and candidate 1 is breaker-open-skipped, asserting the event
    STILL carries candidate 0s detail AND its real class; plus a preservation witness proving the pure-
    skip chain with no prior real failure still reports the skip reason with detail absent, exactly as
    today. PD-8: P3 (restore the unconditional erasure) turns the first RED; P4 (over-correct so a skip
    never touches the class at all) turns the second RED, surfacing unknown instead of breaker-open. Both
    matched prediction. The witness gap the grounding identified is closed: no test previously exercised
    the multi-candidate erasure.'
  council: 'No. C7 (attribute and schema owner) and C11 (operator surface) converged in venue on both the
    mechanism and the framing, including one concession in each direction. What remains is choosing the
    retention shape — a single-domain implementation judgement.'
  pr: '#1287 (the paired B-127 + B-131 exhaustion-attribution fix at the shared skip call site)'
  heading: '### B-131 · The breaker-open skip **deletes** the prior candidate''s failure detail instead of
    demoting it, so a chain exhaustion reports no cause at all *(surfaced at the B-116 council
    deliberation (C11 A1 primary (c), reframed by C7 A2, re-grounded by C11 at cross-read), 2026-08-07)*'
- id: B-132
  title: 'The classifier catch-all routes harness-side memory subtypes to the staircase, where they
    charge the breaker at escalation/exhaustion'
  status: registered_finding
  summary: 'WHAT IT IS. _classify_provider_exception''s terminal arm
    (retry_breaker_fallback.py:371) maps EVERY unlisted Exception subclass to TRANSIENT_RETRY --
    deliberately, per its own docstring, treating unknown faults as transient until proven
    otherwise. MemoryToolExecutionDeniedError and MemoryToolExecutionStoreError are siblings of the
    two WAIVED memory types under the same family base, fall to that catch-all, travel the retry
    staircase, and charge the provider-model breaker at both staircase record_failure charge
    sites (:1072, :1091 at HEAD). Under the B-116 ratified normative test (a fault
    charges only if a half-open trial could return a different result, attributably to the
    {provider, model}), a DETERMINISTIC policy denial or a local durable-store write failure
    charging the breaker is a residual -- the same defect shape B-116 closed at the fail-fast site,
    alive on the staircase path. WHY IT IS A RESIDUAL AND NOT A MISS: the council''s ratified scope
    binds the fail-fast site only, and the classifier''s exclusion of the denied class from
    fail-fast is itself deliberate and documented (its raise sites read mutable index/store state
    and it WRAPS genuinely transient store I/O, so wholesale fail-fasting it would kill retries for
    real flakes -- the docstring''s own grounds at :343-349). The residual was stated as a FALSE
    universal ("every other path passes the test by construction") in the first drafting of spec
    section 14.6.3; the fresh-context review round falsified it by direct read and the spec now
    declares the residual and cites this row. SURFACED at the B-116 spec leg''s fresh-context
    review round (P1), 2026-08-08. NARROWED 2026-08-08 at the B-115 (b-prime) leg -- NOT CLOSED, and
    the distinction is load-bearing. That leg SPLIT the DETERMINISTIC ledger divergent-replay
    refusal out of MemoryToolExecutionStoreError into a new sibling type
    MemoryToolExecutionLedgerConflictError, which is now admitted to fail-fast BY NAME and WAIVED as
    the fifth member of the section 14.6.3 tuple. So the population still riding the catch-all is
    strictly smaller: the genuinely transient store I/O the store subtype thereafter exclusively
    carries (disk-full, permission, and other faults a retry CAN clear), plus
    MemoryToolExecutionDeniedError. The store subtype''s disposition is otherwise UNCHANGED -- it
    keeps io_failure, keeps classifying TRANSIENT_RETRY, and still charges at both staircase sites --
    because wholesale fail-fasting it remains refused on the classifier''s own documented grounds.
    WHAT THIS MEANS FOR THE ROW''S FORWARD WORK: the deterministic sub-case that was the easiest
    candidate for a finer split has been taken, so what remains is the genuinely harder question the
    row was really about -- whether a class that wraps transient I/O can be discriminated at all
    without message guessing, and whether a staircase-side waiver is warranted for instances that
    fail the recovery-model test. The (b-prime) leg is also the WORKED PRECEDENT for step (2)''s
    finer-split option: it discriminated on the wrapped exception''s own TYPE by making that type
    survive the capture boundary, rather than by reading failure_reason wording.'
  close_out: '(1) Re-verify the catch-all membership and the two staircase charge sites at the
    then-current HEAD. (2) Decide PER-SUBTYPE whether a finer classification is owed: a class that
    wraps genuinely transient I/O CANNOT be wholesale fail-fasted (the classifier''s own documented
    grounds), so any fix is a finer split -- discriminating persistent policy denial from wrapped
    transient I/O (e.g. on the wrapped exception''s own type), or a staircase-side waiver at the
    exhaustion charge for instances that fail the recovery-model test. (3) Any change routes
    through the B-116 normative test PER-MEMBER, per the section 14.6.3 discipline -- a wholesale
    reclassification would repeat the exact mistake the per-member table exists to prevent. WHAT
    WOULD FALSIFY IT: the catch-all arm gaining member-specific handling for the denied/store
    subtypes, or an operator decision recording the staircase charge as accepted behaviour.  RE-GROUNDED
    2026-08-09 (PR #1285) -- INTACT, AND CORROBORATED BY THE SHIPPED CODES OWN COMMENTS. Verified by
    direct classifier calls, not grep: MemoryToolExecutionDeniedError -> transient-retry / waived False;
    MemoryToolExecutionStoreError -> transient-retry / waived False;
    MemoryToolExecutionLedgerConflictError -> classify None / waived True (the B-115 (b-prime) split,
    confirmed live). The two staircase charge sites are unconditional record_failure calls now at
    retry_breaker_fallback.py:1353 (max-attempts exhaustion) and :1376 (escalation) -- the rows cites
    :1072/:1091 have drifted. The _BREAKER_CHARGE_WAIVED_TYPES docstring at :428-448 states the residual
    near-verbatim, and Spec_Harness_Runtime_v1.md:65-73 / :4346 independently record B-132 stays OPEN /
    NARROWED, NOT closed. WHAT PRECISELY REMAINS AFTER THE B-115 NARROWING: exactly TWO members --
    MemoryToolExecutionDeniedError, and MemoryToolExecutionStoreError minus the ledger-conflict slice
    that left it (now exclusively disk-full / permission / other genuinely-transient I/O). Both still
    charge the PROVIDER-MODEL breaker for a candidate-independent harness-local fault. SHAPE: FORK-
    SHAPED, not Claude-closeable. The two live alternatives: (a) a finer split on the WRAPPED exceptions
    own type for whatever slice of store errors is provably deterministic (the B-115 worked precedent),
    or (b) a staircase-side charge waiver keyed on the B-116 recovery-model test, which REOPENS the B-116
    C1/C9 vs C11 dyad (under-counting vs over-counting) on a new path. The hard part is untouched:
    whether transient store I/O can be discriminated from a persistent local fault WITHOUT message-
    sniffing.'
  council: 'No at registration. Conditionally yes at step (2) if a staircase-side waiver is
    proposed: that re-opens the B-116 C1/C9-vs-C11 dyad (under-counting fails to protect a
    genuinely failing chain vs over-counting opens healthy providers on harness-side faults) on a
    NEW path with its own retry-visibility tradeoffs; the per-member discipline carries either
    way.'
  heading: '### B-132 · The classifier catch-all routes harness-side memory subtypes to the staircase, where they charge the breaker at escalation/exhaustion *(surfaced at the B-116 spec leg, fresh-context review round, 2026-08-08; **NARROWED — not closed — at the `B-115` (b′) leg, which removed the deterministic ledger-refusal population**)*'
- id: B-133
  title: 'The §9.2 always-sampled floor classifies span NAMES at both consumers while three of its members
    are span EVENTS, so their head=1.0 floor is unrealized on the dispatch path'
  status: closed
  pr: '#1276 (the B-133 realization leg -- OD spec v1.38 NEW section 9.2.1 + plan v2.33 U-OD-59 + the
    event-aware arm at TailKeepSpanProcessor.on_end + the fourteen-witness set; both clearance markers filed)'
  summary: 'WHAT IT IS. Both C-OD-09 §9.2 consumers resolve the always-sampled floor against a SPAN NAME and
    never against an EVENT name. Head: HarnessCompositeSampler.should_sample
    (harness-od/src/harness_od/composite_sampler.py:100) branches on is_always_sampled(name, attributes)
    at :110, where name is the span name supplied at span creation. Tail: TailKeepSpanProcessor.on_end
    (harness-od/src/harness_od/tail_keep_span_processor.py:207) branches on
    is_always_sampled(span.name, span.attributes) at :259 and NEVER inspects span.events -- grounded at
    HEAD by direct read: the module contains ZERO occurrences of ".events". THREE §9.2 members are
    emitted as span EVENTS on a wrapper span, not as spans of their own: fallback.triggered
    (outer_span.add_event at harness-runtime/src/harness_runtime/lifecycle/retry_breaker_fallback.py:688-689),
    fallback.exhausted (outer_span.add_event at :946-947, the row this leg added), and breaker.tripped
    (parent_span_ref.add_event(name="breaker.tripped", ...) at
    harness-od/src/harness_od/harness_breaker_schema.py:282, the same emission shape B-123 grounds).
    Consequence: for all three, the §9.2 head=1.0 guarantee is carried only insofar as the WRAPPER span
    is itself kept -- at a base-rate-sampled cell the wrapper can be dropped and the always-sampled
    event goes with it. PRE-EXISTING, NOT NEW EXPOSURE FROM THE B-116-t3 LEG, and this is the load-bearing
    scope fact: the two siblings fallback.triggered and breaker.tripped have carried the identical shape
    since the ORIGINAL OD ingestion of §9.2, so the characteristic is a property of the floor mechanism
    rather than of the row this leg added. WHAT IS NOT IN QUESTION. The §9.2 MEMBERSHIP CONTRACT is
    correct regardless, and so is the ingestion-chain direction that placed the row in OD rather than
    demoting CP -- neither turns on how the floor is realized at the SDK boundary. B-116 CLOSED at its
    ratified gate, whose stated content was the roster widening plus its sampling fixtures per the
    council record C-c7-reconcile.md''s own pricing; this row carries the REALIZATION debt that pricing
    never covered. REGISTRATION IS THE NON-SILENT DISPOSITION -- the finding is neither absorbed into a
    closed row nor repaired by redesign at a leg that has no authority for it. WHY NOT FIXED AT THE t3
    LEG. The out-of-family reviewer''s proposed remedies -- emit a dedicated span for the event, or make
    the wrapper span itself always-sampled -- were both REJECTED there. The first mints a new emission
    primitive that no cleared spec authorizes, which is an X-AL-3 design extension routing through
    back-flow, not an impl fix. The second would always-sample EVERY dispatch wrapper, over-sampling far
    beyond the three rows at issue and silently rewriting the §10.3 per-cell envelope. SURFACED BY
    OUT-OF-FAMILY CODEX ROUND 1 AT THE B-116-t3 LEG (PR #1272), 2026-08-08; root cause SHARED WITH
    B-123 (the §10.2 tail-keep trigger arm of the same span-name-vs-span-event mismatch) and B-124 (the
    inert attribute-qualified row) -- the three are one family and should be dispositioned together.'
  close_out: 'CLOSED 2026-08-08 -- STEPS (1) AND (2) EXECUTED; STEP (3) STILL OPEN, carried as a named residual on this
    closed row (the B-104 precedent for a residual that outlives its row''s disposition) and MERGED with B-137.
    This field states the CURRENT result: where an earlier framing was superseded it is named in one clause
    rather than restated, and the full correction narrative lives at OD spec v1.38 §0.2 and OD plan v2.33 §0.9.

    (1) THE POSITIVE CONTROL WAS RUN FIRST, END-TO-END, AND BEFORE ANY REPAIR WAS WRITTEN. A REAL
    RetryBreakerFallbackDispatcher.dispatch driven to fallback-chain exhaustion through a TracerProvider wired
    with the REAL HarnessCompositeSampler AND the REAL TailKeepSpanProcessor, across SEVEN configurations
    covering ALL THREE event-shaped members by their own real dispatch shapes (chain exhaustion for
    fallback.exhausted; capability-shortfall exhaustion for fallback.triggered; charging-fault exhaustion at
    fail_threshold=1 for breaker.tripped -- LLMDispatchProviderUnreachableError is one of the §14.6.3 WAIVED
    types and does NOT trip, which is why the charging fault was selected deliberately). RESULT -- ZERO spans
    exported at BOTH consumers for ALL THREE members: head-only at base_rate=0.0 exported 0; the tail arm with
    the head admitting exported 0 WITHOUT force_flush; the combined production shape exported 0. The
    base_rate=1.0 control exported 3, proving the carrier and its event exist. THE DECISIVE DISCRIMINATOR: at
    base_rate=0.0 a span NAMED fallback.exhausted SURVIVES while a span CARRYING it as an event is DROPPED --
    which makes "event-shaped, not name-shaped" the MEASURED cause rather than an inference. The tail mechanism
    was read off the same run: the carrier span harness.runtime.retry_breaker_fallback is in NEITHER §9.2 (by
    name) NOR §10.2 (by trigger predicate), so it buffers and drops at root close.

    (2) REPAIR VENUE (a) BUILT, as this row ranked it. An event-aware arm at TailKeepSpanProcessor.on_end
    resolves the span''s EVENT names against the SAME is_always_sampled SSOT after the name check fails, passing
    event attributes through so the conditional rows keep conservative-absent, and forwards the carrier
    immediately on a match. Venue (b) REFUSED as the X-AL-3 extension this row already classified it as.
    Contract at OD spec v1.38 NEW §C-OD-09 §9.2.1 (five normative terms); unit at OD plan v2.33 U-OD-59;
    SIXTEEN witnesses W1-W16 (40 cases under parametrization; homed across the axis boundary -- the SIX
    real-dispatch witnesses at harness-runtime/tests/test_b133_event_aware_tail_floor_real_dispatch.py, the TEN
    processor-contract witnesses at harness-od/tests/test_b133_event_aware_tail_floor.py, which stays
    harness_runtime-FREE because the axis-isolation CI leg prunes every sibling of harness-od) plus SEVEN PD-8
    probes, every figure RE-MEASURED against the final roster. THE DECORRELATED 3-LENS MERGE GATE ON PR #1276
    RETURNED A REAL BLOCK, and it is recorded because it found a hole this row''s own PD-8 discipline had missed:
    a first-event-only scan passed all 65 cases then in the suite while REINSTATING B-133 on a
    PRODUCTION-REACHABLE ordering -- an already-open breaker emits the NON-member retry.skipped on the outer span
    and fallback.exhausted lands on that SAME span at exhaustion, measured as
    [''retry.skipped'', ''retry.skipped'', ''fallback.exhausted'', ''exception'']. Every witness up to that point put a
    member FIRST. Closed by W16 (the real dispatch, both breakers pre-opened, asserting no provider call ran) and
    W15 (four parametrized orderings on the processor alone), with probe (vii) red at 5 failed / 65 passed. TWO probes changed the
    deliverable: probe (ii) caught the trigger-flag mirror witness PASSING under the mutation it existed to
    catch -- it asserted membership where the arm''s discriminator is export ORDER, and was sharpened, then
    re-run RED (the figure first recorded here, 27 failed / 5 passed, was STALE and did not sum to its roster --
    the merge gate''s lens-2 caught the arithmetic; every probe figure is now re-measured against the final
    roster) -- and probe (iii) returned GREEN, which constrains LESS than first claimed: no witness distinguishes
    the two arm orderings, and the only observable divergence would narrow B-136 rather than this arm,
    ordering being a cost property rather than a correctness one. Probe (vi) VALIDATES rather than changes:
    making the head unconditionally admitting inverts EXACTLY the bound witnesses (W5, W13 x2, W14) and nothing
    else.

    WHAT THE ARM DELIVERS, STATED AS MEASURED SCOPE RATHER THAN AS COVERAGE. The arm realizes the §9.2 floor for
    carriers the HEAD ADMITTED, and for those it delivers 100% (measured 420/420 and 837/837) -- what bounds it
    is admission, not classification. tracer_provider.py binds the §10.3 per-cell ratio at the head
    UNCONDITIONALLY in BOTH §9.1 modes (its own words: "the current default sampler ignores the mode"), so
    measurement over 4,000 carriers per cell returned 10.4% admitted at team-binding x self-hosted-server, 20.9%
    at multi-tenant-compliance x managed-cloud, and 100% at the base-rate-1.0 cells. THE HEAD BOUND THEREFORE
    REACHES FIVE OF THE EIGHT ACTIVE CELLS -- every cell whose head base-rate is below 1.0 -- AND IT IS
    SHAPE-SPECIFIC: at the same cell and the same rate, a §9.2 member realized as a ROOT SPAN NAME is admitted
    at 100% (measured 4,000/4,000 for sandbox.violation), so only EVENT-carried members and NON-ROOT spans are
    starved, the latter including non-root §10.2 triggers at ~9% preservation. The bound is pinned by W5 plus
    W13 (parametrized over the two production cells, asserting BOTH the shortfall AND that every admitted
    carrier exports) and W14 (the root-name-vs-event asymmetry), on the discipline of naming a bound rather than
    hiding it.

    HISTORY, COMPRESSED RATHER THAN CARRIED TWICE. Two successive framings of this head bound were wrong and
    were corrected by MEASUREMENT rather than by argument: first this row''s own step-(2) hypothesis that a
    head-based-dev cell "samples at 1.0 anyway, so the head arm may need nothing", and then the leg''s first
    drafting, which over-corrected to "exactly one cell" and claimed every TAIL_BASED_PROD cell was covered by
    terms 1-3. BOTH ARE SUPERSEDED BY THE PARAGRAPH ABOVE. The row''s original close-out step (2) text and the
    prose block''s earlier bullets preserve the first framing as registration history; they are to be read
    against this field, not beside it.

    TWO CLOSING ROUTES REMAIN, BOTH OUTSIDE THIS ROW. The SHAPE half -- giving the three members a span shape at
    emission -- is venue (b), a NEW emission primitive routing through §4.3 back-flow per X-AL-3, and is
    authorized nowhere at this leg. The ADMISSION half -- an unconditionally admitting head in TAIL_BASED_PROD
    mode with the §10.3 ratio moved into the tail consumer -- is B-137.

    (3) STILL OPEN, DELIBERATELY, AND NOW MERGED WITH B-137 -- THE F-08 PER-TENANT KEEP-VOLUME MEASUREMENT
    against the C-OD-11 §11.1 1,000 spans/sec budget. This leg realizes the floor at the tail and so makes the
    measurement MEANINGFUL FOR THE FIRST TIME (before realization it would have measured the wrong thing), and
    deliberately does NOT make it: no volume claim is asserted anywhere in the spec or plan leg and no term or
    acceptance criterion depends on one, consistent with C7''s FM-K refusal. What CAN be stated without
    measurement is structural rather than populational: the arm forwards the CARRIER span, the smallest unit the
    OTel export boundary can deliver an event in, so keep granularity is one carrier span per event-carrying
    dispatch, and the arm adds ZERO emission sites and ZERO per-attempt multiplier. WHOEVER PICKS UP THE
    RESIDUAL must measure at a TAIL_BASED_PROD multi-tenant cell -- the head-based cells engage no tail consumer
    at all -- AND must measure it JOINTLY WITH B-137: today''s ~20% multi-tenant admission is the CEILING on
    this arm''s added keep-volume, and B-137''s candidate A removes that ceiling, so a figure taken against
    today''s admission would not survive that repair.

    B-123 IS NOT CLOSED BY THIS LEG. Its step-(1) probe question is ANSWERED as a side effect, and the finding
    is recorded here and at OD spec v1.38 §0.1: is_classification_trigger matches breaker.tripped by span NAME,
    so an event-carried trip forwards its OWN carrier under §9.2 (term 2) but does NOT flag its trace for §10.2
    sibling preservation. §9.2.1 term 3 declines to widen the trigger predicate deliberately, and witness W7
    PINS the boundary so widening it later is a deliberate, test-visible act. B-123 owns its own disposition and
    its step (3) -- the sandbox.violation sibling arm -- is untouched.

    B-136 NEWLY REGISTERED at this leg: a probe found the PRE-EXISTING name-matching arm leaks its trace buffer
    when the always-sampled span is the ROOT close. Outside this row''s authority to repair, but this row''s own
    arm must not EXTEND it (term 5 + AC #6c + PD-8 probe (iv), which goes RED without it).

    B-137 NEWLY REGISTERED after OUT-OF-FAMILY CODEX ROUND 1 against this leg''s commit ff63c725, whose P1 was
    ADJUDICATED EMPIRICALLY AT THE REAL PRODUCTION WIRING RATHER THAN ON ARGUMENT, and CONFIRMED. The repair
    this row built is REAL BUT PARTIAL; what was defective was the leg''s contract TEXT, not the arm, and term 4
    was rewritten accordingly. B-137 carries the architecture question -- it spans every class the tail
    preserves and multiplies admitted volume by 1/base_rate at every production cell -- with a YES council
    disposition (C7 observability floor vs C11 telemetry volume) convened at its own step (3), after
    re-measurement.'
  council: 'No -- a probe-then-repair row in the B-123 family; convene nothing until the positive control runs.
    If step (1) returns dropped and step (2) selects candidate (b), that IS a convening trigger: a new
    emission primitive re-opens the C7 observability-floor vs C11 telemetry-volume tension on a surface
    wider than these three rows. Candidate (a) is single-domain and owes no convening. AT CLOSURE: the
    control returned DROPPED and step (2) selected candidate (a), so NO convening was owed and none was
    held. The conditional survives for whoever takes the HEAD half, which can only be closed at
    candidate (b)''s venue and therefore DOES carry the trigger.'
  heading: '### B-133 · The §9.2 always-sampled floor classifies **span names** at both consumers while three of
    its members are span **events**, so their head=1.0 floor is unrealized on the dispatch path
    *(surfaced by out-of-family Codex round 1 at the `B-116-t3` leg, 2026-08-08; same family as B-123 /
    B-124)*'
- id: B-134
  title: 'Should C-MEM-19 gain a dedicated failure class for DETERMINISTIC ledger conflicts, rather than
    routing them to the residual?'
  status: registered_finding
  summary: 'WHAT IT IS, and it is a question this leg deliberately did NOT answer on its own authority. The
    B-115 (b-prime) leg introduced MemoryToolExecutionLedgerConflictError for the C-MEM-08 operation
    ledger''s divergent-replay refusal. That type DECLARES no C-MEM-19 failure class and inherits the
    family base''s RESIDUAL provider_adapter_failure. The choice was between three options and none is
    obviously dominant. (i) KEEP io_failure -- REJECTED, and this is the one clearly wrong answer: a
    ledger refusal is a pure comparison over durable state under a lock, not an I/O fault, so
    io_failure is a positively WRONG claim. (ii) INHERIT THE RESIDUAL -- TAKEN. Per C-MEM-19 v1.3 a
    residual report is the ABSENCE of a claim rather than an assertion of adapter-hood, so this
    WITHDRAWS a false positive without inventing a replacement; it follows the U-MEM-28 precedent,
    which moved an emission input_validation_failure -> residual on exactly these grounds; and it
    makes the two classify_memory_failure consumers AGREE, since memory_redaction.py already reports
    the underlying MemoryOperationIdempotencyConflictError as provider_adapter_failure under a landed
    two-row discriminating witness. (iii) MINT A DEDICATED ledger_conflict CLASS -- NOT TAKEN, and the
    reason is jurisdictional rather than technical. It would widen C-MEM-19''s ratified failure
    vocabulary, and Spec_Memory_Substrate_v1.md v1.3 records at its own change-note (the round-8
    finding on the seventh re-typing) that widening a ratified enumeration on a leg''s own reading of
    the rule is the X-AL-3 shape even when the reading is sound -- the operator ratified a vocabulary,
    and only an operator-ratified Class 2 fork may extend it. THE HONEST COST OF (ii), stated so the
    row is not read as cost-free: the residual bucket now carries a well-typed, deliberately
    classified population alongside genuinely untriaged failures, which is the same triage-blindness
    C-MEM-19 v1.3''s input_validation_failure section was written to forbid for a DIFFERENT
    population. The difference in scale (that one was large and this one is narrow) is a reason to
    defer, not a reason the concern does not apply.'
  close_out: '(1) RE-GROUND at the then-current HEAD: re-read C-MEM-19''s failure-class enumeration and its
    invariants, confirm the residual is still provider_adapter_failure, and confirm
    MemoryToolExecutionLedgerConflictError still declares nothing (the witness at
    harness-runtime/tests/test_b115_ledger_conflict_split.py asserts the declaration-ABSENCE over
    vars(), so a silent later declaration would already have failed there). (2) MEASURE THE RESIDUAL
    BUCKET rather than arguing about it: enumerate every type and raise-site population currently
    reporting provider_adapter_failure and state how much of it is deliberately-classified-but-
    unnamed versus genuinely untriaged. If the deliberate half dominates, the residual has stopped
    meaning "absence of a claim" and the vocabulary owes a member -- that is the falsifiable trigger.
    (3) IF a class is warranted, it is a Class 2 fork against C-MEM-19 with a Memory spec leg (v1.3 ->
    v1.4) plus a Memory plan unit, on the B-88 / U-MEM-28 precedent -- which is a full spec-leg arc,
    not an impl-discretion flip. Note that the flip site is ONE line (the type declares a ClassVar
    instead of inheriting), by construction: the B-115 leg wrote the declaration explicitly as an
    absence rather than relying on the base, so the change surface is minimal whenever it is taken.
    (3-bis) REVISIT THE CONSUMER ROUTING IN THE SAME ARC. MemoryCaptureFailureKind is CLOSED at
    two, and the closure is enforced by a pinned witness
    (test_b115_ledger_conflict_split.py::test_the_failure_kind_vocabulary_is_pinned_at_two_members),
    NOT by the consumers: _write_note''s re-type is an if-LEDGER_CONFLICT/else, so a third member
    would fall through to the store-error branch and travel the retry staircase -- silently, and
    on the conservative side (a new kind stays retryable rather than becoming permanent), but
    silently nonetheless. The pin makes adding a member FAIL that test, which is the forcing
    function; whichever arc adds one MUST decide that routing explicitly in the same arc rather
    than inheriting the fall-through by default. Note the two vocabularies are distinct: a new
    C-MEM-19 telemetry class does not by itself require a new MemoryCaptureFailureKind member --
    check whether it does before assuming this clause fires.
    (4) CONSIDER SCOPE: the same question applies to MemoryToolExecutionInternalError, which inherits
    the same residual for the same stated reason at U-MEM-28. A vocabulary arc should decide both
    together rather than minting one member and leaving its sibling in the residual. WHAT WOULD
    FALSIFY THE DEFERRAL: the residual bucket measured at step (2) turning out to be majority-
    deliberate, or an operator decision recording the residual routing as accepted permanently.'
  council: 'No at registration -- the row frames a vocabulary question and commits to nothing. CONDITIONALLY
    YES at step (3) if a class is proposed: minting a C-MEM-19 member trades telemetry triage
    precision (the information-substrate integrity voice C3, which owns record and ledger semantics)
    against vocabulary stability for every downstream consumer that switches on the enum (C7
    observability). The tension is nameable in advance, which is the workspace''s convening
    discriminator. Convene ONLY if step (2)''s measurement supports the widening; if the residual is
    measured and found genuinely residual, this is a documentation outcome and owes no convening.'
  heading: '### B-134 · Should `C-MEM-19` gain a dedicated failure class for deterministic ledger conflicts,
    rather than routing them to the residual? *(surfaced at the `B-115` (b′) leg, 2026-08-08 — the leg
    took the residual per the U-MEM-28 precedent and registered the vocabulary question rather than
    widening a ratified enumeration on its own authority)*'
- id: B-135
  title: 'Stale-era breaker dispatches act under a superseded era on BOTH sides -- outcomes reset the
    CLOSED-arm fail_count un-epoch-guarded, and attempts continue a multi-attempt staircase across a
    trip that can overlap the single half-open trial'
  status: registered_finding
  summary: 'WHAT IT IS. The B-118 trial-epoch guard (Runtime spec v1.114 section 14.6.4) binds the
    HALF_OPEN resolution arms ONLY -- record_success / record_failure / re_arm_half_open_trial refuse to
    resolve a HALF_OPEN machine unless the caller presents the current admission epoch. The CLOSED arms
    stay TOKENLESS by design. Codex round 2 at the B-118 leg named a scenario that crosses the boundary:
    a request admitted while the breaker was CLOSED survives, in flight, through a trip AND a successful
    half-open recovery, and its stale success then arrives while the breaker is CLOSED again. It takes
    the tokenless CLOSED arm, resets fail_count, and erases whatever failure accumulation has built up
    SINCE the recovery. WHY THIS IS PRE-EXISTING AND NOT A B-118 REGRESSION, VERIFIED NOT ASSUMED.
    record_success resetting fail_count in CLOSED is the shipped CONSECUTIVE-failure breaker semantics
    on main (harness-runtime/src/harness_runtime/lifecycle/retry_breaker.py, the record_success CLOSED
    arm, pre-B-118), and the erasure is reachable with ZERO trips and ZERO recoveries: siblings fail
    twice, an older in-flight success lands, the count resets. That is the definition of a
    consecutive-failure counter and it long predates this leg. B-118 adds ONE MORE PATH into the same
    arm (a request that outlived a full trip-and-recover cycle) but changes NOTHING about the arm''s
    semantics. WHY THE B-118 GUARD DELIBERATELY STOPS AT HALF_OPEN. The HALF_OPEN race is categorically
    different -- there a stale success does not merely reset a counter, it RESOLVES A TRIAL IT DOES NOT
    OWN and closes a breaker the machine had already judged unhealthy, after which the real trial''s
    failure lands under threshold and the provider-model is silently readmitted. Guarding CLOSED-arm
    outcomes instead requires capturing an epoch on EVERY ordinary dispatch and changes CONTRACTED
    success-reset semantics for every caller -- an unpriced redesign, not a bug fix, which is why the
    B-118 leg declared the boundary at section 14.6.4 rather than absorbing it. SURFACED BY
        OUT-OF-FAMILY CODEX REVIEW ROUND 2 AT THE B-118 LEG, 2026-08-08. THE ATTEMPT SIDE, ADDED
    2026-08-08 (out-of-family codex review round 6 at the same B-118 leg). The same family has a second
    half, on the ATTEMPT side rather than the OUTCOME side. A dispatch admitted while the breaker was
    CLOSED keeps its multi-attempt RetryPolicy. If it fails transiently, and siblings then trip the
    breaker while a later dispatch acquires the HALF_OPEN permit, the CLOSED-era dispatch resumes
    attempt 2 with NO breaker re-consultation -- a concurrent PAID call overlapping what section 14.6.4
    calls the single trial. WHY THIS TOO IS PRE-EXISTING. The breaker pre-check is PER-CANDIDATE by
    contract (section 14.6 step 4): it runs once when a candidate is selected and is never re-consulted
    between that candidate''s own retry attempts. The pre-v1.114 loop behaved identically, so B-118
    neither introduced nor priced this. What round 6 actually caught is that the section 14.6.4 text
    B-118 authored OVERCLAIMED -- the ratified intent (C9 Pillar-4, the cell-3 operator ratification)
    binds THE TRIAL to one call and never deliberated stale CLOSED-era dispatches. That text is now
    SCOPED at section 14.6.4 to era-consistent admissions, with the boundary declared and witnessed
    rather than defended. ONE FAMILY, TWO SIDES -- a dispatch admitted under an earlier breaker era
    continuing to act under that era''s policy, whether it is reporting an OUTCOME or making another
    ATTEMPT.'
  close_out: '(1) DECIDE WHETHER CLOSED-ARM OUTCOME ORDERING SHOULD BE EPOCH-AWARE AT ALL. This is the
    whole question, and it is not obviously yes -- a consecutive-failure counter that forgives on ANY
    success is a deliberate, contracted design, and making it order-aware changes what fail_threshold
    means for every caller. Ground it against the C-CP-03 section 3.5 breaker semantics before pricing
    anything. Likely a C9 single-domain call; council only if step (2) proposes a DURABLE or PERSISTENT
    epoch, which re-opens the same persistence tension B-116 term t4 pinned. (2) IF YES, PRICE BOTH
    SIDES honestly, because epoch-awareness on one alone leaves the family half-open. (2a) THE OUTCOME
    SIDE -- the ordinary-dispatch epoch capture: every dispatch would have to capture an epoch and every
    CLOSED-arm resolution present it, which touches the hot path rather than the rare recovery path the
    B-118 guard touches, and would need its own witness set plus a decision about what a stale
    CLOSED-arm success DOES instead of resetting (drop it? apply it to the epoch it belongs to? neither
    is free). (2b) THE ATTEMPT SIDE -- mid-staircase re-consultation, which is the more expensive half
    and the reason this is registered rather than absorbed: re-checking the breaker BETWEEN a
    candidate''s own retry attempts means ABANDONING a candidate partway through its attempt loop, which
    changes exhaustion accounting and the fallback.last_failure_class attribution for a path that
    predates B-118 entirely. That is semantic surgery on the staircase, not a defect repair, and it must
    be priced against the pre-existing per-candidate pre-check contract at section 14.6 step 4 rather
    than bolted on. Note also that the two sides can be decided INDEPENDENTLY -- guarding outcomes
    without guarding attempts is coherent (it stops a stale success closing a breaker while still
    allowing a stale paid call), and the reverse is not, so if only one is built it should be the
    outcome side. (3) NOTE THE B-116 t4 INTERACTION -- any future breaker-persistence arc must replay
    epochs consistently, or a rehydrated breaker will treat every in-flight outcome as stale (or every
    stale one as current), which is a worse failure than the one this row describes. WHAT WOULD FALSIFY
    IT. A demonstration that the CLOSED-arm reset is NOT reachable without a trip -- which the
    pre-existing grounding above already contradicts -- or a ratified decision that
    consecutive-failure-with-forgiveness is exactly the intended semantics, in which case this row
    closes as WORKING-AS-DESIGNED and section 14.6.4''s boundary paragraph becomes the permanent
    statement of it.'
  council: 'No at registration. A single-domain C9 breaker-semantics question -- what fail_threshold
    counts, and whether a counter that forgives on any success is intended -- with no nameable
    cross-domain tension. Conditionally yes only if step (2) proposes a DURABLE or PERSISTENT epoch,
    which re-opens the persistence tension B-116 term t4 pinned (a durable breaker opened by a fault the
    operator cannot see survives the restart the operator reaches for first) and is a genuine C9 vs C11
    dyad.'
  heading: '### B-135 · Stale-era breaker dispatches act under a superseded era on BOTH sides — outcomes
    reset the `closed`-arm `fail_count` un-epoch-guarded, and attempts continue a multi-attempt
    staircase across a trip that can overlap the single half-open trial *(outcome side surfaced by
    out-of-family codex review round 2 at the `B-118` leg, attempt side by round 6, both 2026-08-08 —
    each adjudicated PRE-EXISTING, so the leg declared the boundaries rather than absorbing an unpriced
    redesign)*'
- id: B-136
  title: 'The tail-keep name-matching always-sampled arm returns before the root-close materialization, so an
    always-sampled ROOT leaves its trace buffered until force_flush'
  status: closed
  pr: 1331
  summary: 'WHAT IT IS, MEASURED RATHER THAN REASONED. TailKeepSpanProcessor.on_end''s §9.2 name-matching arm
    (harness-od/src/harness_od/tail_keep_span_processor.py, the is_always_sampled(span.name,
    span.attributes) branch) forwards the span to the downstream processor, optionally sets the §10.2
    keep flag, and RETURNS -- unconditionally, before the is_root_close ->
    _materialize_trace_decision(trace_id) step further down the method. When the always-sampled span is
    itself the trace''s ROOT CLOSE, the trace''s buffered siblings are therefore never resolved: they sit
    in self._buffer, holding a max_buffered_traces slot, until force_flush drains them keep-all at
    shutdown. Probed directly at the B-133 leg, 2026-08-08: a trace whose root is named sandbox.violation
    with ONE ordinary buffered child leaves buffered_trace_count == 1 after the root closes; the control
    trace with an ordinary root returns 0. WHY IT MATTERS BEYOND MEMORY. The slot is not merely held, it
    is CHARGED: under max_buffered_traces pressure _evict_oldest_trace sheds the oldest buffered trace
    FIFO, so a population of never-resolving always-sampled-root traces displaces live traces that would
    otherwise have taken a real keep decision. The module''s own "Eviction fidelity tradeoff" paragraph
    reasons about eviction on the assumption that buffered traces resolve at their root close; this arm
    is the case where they do not. WHY IT IS PRE-EXISTING AND NOT B-133 EXPOSURE. The name-matching arm
    and its unconditional return long predate the B-133 leg; that leg only ASKED the question, because
    its own new event-aware arm sits beside the same return and the dispatch carrier span IS routinely
    the root close, so an unconditional early return there would have put the common case into the
    leaking shape. THE B-133 ARM DOES NOT LEAK -- OD spec v1.38 §9.2.1 term 5 makes the root-close
    materialization normative for the event arm, U-OD-59 AC #6c witnesses it, and PD-8 probe (iv) goes
    RED without it. The asymmetry between the two arms is therefore DELIBERATE and recorded, not an
    oversight: repairing the name arm changes shipped behaviour for every name-shaped always-sampled
    root, which the B-133 arc had no authority to do. SURFACED AT THE B-133 LEG BY DIRECT PROBE,
    2026-08-08.'
  close_out: 'SUPERSEDED-NOTICE: this row is CLOSED at PR #1331. Everything below the CLOSED paragraph is
    PRE-REPAIR HISTORY -- the ''returns unconditionally'' description and the (1)-(5) close-out steps are
    all DISCHARGED and must not be re-run. (1) RE-GROUND, because the arm may have moved. Re-run the two-trace
    probe at the then-current HEAD (always-sampled ROOT + one ordinary child, versus an ordinary root +
    one ordinary child) and confirm buffered_trace_count is still 1 versus 0. Grep proves the return statement;
    only the run proves the trace is still stranded. (2) DECIDE WHETHER IT IS A DEFECT AT ALL, and this
    is a real question rather than a formality. force_flush drains keep-all, so nothing is LOST -- what
    is affected is WHEN the siblings are exported (at drain rather than at root close) and how much max_buffered_traces
    headroom a long-running process retains. A deployment whose always-sampled roots are rare pays nothing.
    Ground the answer against C-OD-09 §9.3''s implementer-discretion clause, which is where the buffering
    algorithm''s shape lives, before pricing anything. (3) IF IT IS A DEFECT, THE REPAIR IS SMALL BUT NOT
    FREE: mirror OD spec v1.38 §9.2.1 term 5 onto the name arm (materialize the trace decision when the
    forwarded span is the root close). It is ~2 lines. What is NOT free is the behaviour change -- every
    existing witness that asserts on export ORDER or on buffered_trace_count around an always-sampled root
    must be re-grounded, and a trace whose siblings previously survived to force_flush keep-all would, after
    the repair, be DROPPED at root close if no §10.2 trigger fired. That is a real reduction in exported
    spans for a real population, and it is the reason this was registered rather than folded into the B-133
    arm. (4) WITNESS BOTH DIRECTIONS if built: the root-close resolution (buffered_trace_count -> 0) AND
    the drop-when-no-trigger consequence, so the repair is not mistaken for a pure bookkeeping fix. (5)
    DECIDE THE VENUE. If the answer to (2) is that force_flush keep-all is the CONTRACTED behaviour, this
    closes as WORKING-AS-DESIGNED and the module docstring''s "Drop semantics (false-but-bounded)" paragraph
    should be extended to say so explicitly, since it currently describes only the never-root-closing case
    and not this one. WHAT WOULD FALSIFY IT. A re-run showing buffered_trace_count == 0 for the always-sampled-root
    trace (the arm having been changed since), or a ratified reading that deferring always-sampled-root
    traces to force_flush is intended. CLOSED 2026-08-13 (PR #1331) -- REPAIRED, and it turns out to matter
    more than the row priced. (1) RE-GROUND at HEAD: the two-trace probe still read 1 vs 0, so the arm had
    not moved. (2) IS IT A DEFECT -- YES, and the row''s own ''nothing is lost, force_flush drains keep-all''
    premise is FALSE UNDER PRESSURE: measured at max_buffered_traces=3 over 100 always-sampled-root traces,
    97 are EVICTED and their children never export at all. More decisively it contradicts v1.28 s1.1''s
    OWN containment model, which reasons that a trace whose first observed span is already its root-close
    materializes and frees its slot so it does NOT evict -- an always-sampled-root trace root-closed yet
    behaved like a never-closing one, displacing live traces. (3) REPAIR: mirror OD spec v1.38 s9.2.1 term
    5 onto the name arm -- the same two lines the B-133 EVENT arm already carries, whose in-line comment
    names B-136 as the case it must not extend. Consistency with the spec-normative arm is the justification,
    not novelty. (4) BOTH DIRECTIONS WITNESSED: slot freed at root close for every root shape; a TRIGGERED
    trace now exports its siblings EARLIER (at root close rather than at drain, same spans); a QUIET always-sampled
    root (always-sampled but not a s10.2 trigger, e.g. hitl.gate.evaluated) now DROPS its siblings -- a
    real reduction, and exactly what an ORDINARY root already did. (5) VENUE: not WORKING-AS-DESIGNED. s9.3
    grants implementer discretion over the buffering algorithm, and this exercises it toward the model v1.28
    already documents. NO existing witness asserted the stranding (all 1178 OD tests passed unchanged),
    so the row''s re-grounding concern was precautionary. WITNESS: harness-od/tests/test_b136_name_arm_root_close_materialization.py
    -- 6 tests, mutation-probed (4 of 6 RED against the pre-repair processor; the other 2 are premise/invariant
    checks that correctly hold either way). COUPLING TO B-137 STEP (3) -- THE MOST CONSEQUENTIAL PART OF
    THIS CLOSURE. B-137''s candidate C1 (add workflow.envelope to s9.2) was priced ONE DAY EARLIER as costing
    DATA LOSS rather than delay, because under C1 every workflow trace has an always-sampled root and therefore
    hit exactly this arm: measured 3 buffered / 97 evicted at a cap of 3. POST-REPAIR the same C1 counterfactual
    reads 0 buffered / 0 evicted, and with a s10.2 trigger present 100/100 ordinary children export where
    previously 0 did. So C1''s principal cost belonged to B-136, NOT to C1 -- fixing B-136 makes C1 substantially
    MORE attractive, leaving it at its 1/base_rate volume multiplier with ordinary trace semantics. B-137''s
    step-(3) council MUST be briefed on this: the deferral filed earlier today cited C1''s eviction cost
    and that citation is now SUPERSEDED. The two B-137 C1 witnesses were re-priced in the same PR. ADJACENT
    FINDING REGISTERED (codex round 2 on this PR): the three materialization sites -- :478 (original ordinary-root
    close), :437 (B-133 event arm) and :391 (this repair) -- all pop the per-trace bucket with NO lock,
    while a concurrent child on_end can setdefault an empty bucket and append to it after the pop, losing
    the span entirely. That window is PRE-EXISTING and module-wide (no threading.Lock anywhere), so it is
    NOT introduced here; this repair widens the exposed population by making every always-sampled root materialize.
    Registered AND FIXED as B-163 in this same PR (codex escalated to P1 on the widening ground, correctly):
    one threading.RLock guards every buffer/keep/counter mutation, never held across downstream.on_end,
    with a coordinated deterministic interleaving witness that loses the span when the lock is neutered.'
  council: 'No. A single-domain C7 question about the tail-keep buffering algorithm''s own bookkeeping, sitting
    squarely inside §9.3''s implementer-discretion clause, with a named two-trace probe that answers it.
    Convene nothing. The one thing that would change that is step (3) turning out to reduce exported
    spans for a population an operator depends on, which would put the C7 observability floor against the
    C11 telemetry-volume posture -- but that is a consequence to MEASURE at step (4), not a tension
    nameable in advance, so it does not meet the convening discriminator at registration.'
  heading: '### B-136 · The tail-keep **name-matching** always-sampled arm returns before the root-close
    materialization, so an always-sampled **root** leaves its trace buffered until `force_flush`
    *(surfaced by direct probe at the `B-133` leg, 2026-08-08 — pre-existing; the `B-133` event arm
    deliberately does NOT share the shape)*'
- id: B-137
  title: 'Head sampling starves the tail-keep mechanism for every preserved class that is NOT a root span
    name -- the shipped head sampler applies the section 10.3 ratio in BOTH section 9.1 modes'
  status: registered_finding
  summary: 'WHAT IT IS, MEASURED AT THE REAL PRODUCTION COMPOSITION. C-OD-09 section 9.1 says TAIL_BASED_PROD
    means the sampling decision is taken at TRACE COMPLETION. The shipped composition does not deliver
    that: harness-runtime/src/harness_runtime/lifecycle/tracer_provider.py resolves
    build_default_sampler(base_rate=PER_CELL_BASE_RATE_ENVELOPE[cell].default_rate) UNCONDITIONALLY, for
    every cell, and states in the same function that "the current default sampler ignores the mode ...
    Future units may wire mode-conditional samplers". So at a TAIL_BASED_PROD cell the section 10.3 ratio
    is applied by ParentBased(HarnessCompositeSampler(base_rate)) AT SPAN CREATION, and the
    TailKeepSpanProcessor only ever sees the admitted fraction. A span the head dropped never reaches
    on_end at all, so no tail-side preservation rule -- not section 10.2 tail-keep-on-classification, not
    the section 9.2 always-sampled forward, not the B-133 event-aware arm -- can act on it. MEASURED over
    4,000 carriers per cell at HEAD: team-binding x self-hosted-server (base 0.1) admitted 10.4%;
    multi-tenant-compliance x managed-cloud (base 0.2) admitted 20.9%; solo-developer x
    self-hosted-server (base 1.0) admitted 100%. THE STARVATION IS NOT UNIFORM, AND THE NON-UNIFORMITY IS
    THE FINDING -- an earlier framing of it as "every preserved class is equally starved" was FALSIFIED by
    the same run. A section 9.2 member realized as a ROOT SPAN NAME is admitted at 100% even at base-rate
    0.1 (measured 4,000/4,000 for sandbox.violation), because the head sampler resolves is_always_sampled
    against the span NAME and returns RECORD_AND_SAMPLE. What IS starved is everything the head cannot
    classify at creation, and there are exactly two such populations: (i) section 9.2 members carried as
    span EVENTS -- the B-133 family, measured 10.4% at base-rate 0.1 -- and (ii) NON-ROOT spans of any
    class, because ParentBased consults the inner sampler ONLY for roots and children inherit an
    unsampled parent''s decision. Population (ii) includes the section 10.2 classification triggers when
    they are not roots: a trace with an ordinary root and a sandbox.violation CHILD was preserved in 182
    of 2,000 traces (~9%) at base-rate 0.1. WHY THIS IS PRE-EXISTING AND NOT B-133 EXPOSURE. Population
    (ii) is the bound composite_sampler.py''s own "Enforcement boundary (honest scope)" docstring already
    declares and that the B-TAIL-CONDITIONAL-SAMPLING arc was opened for; it long predates B-133.
    Population (i) is B-133''s, whose TAIL half B-133 closed for head-ADMITTED carriers and whose HEAD
    half OD spec v1.38 section 9.2.1 term 4 declares as a bound with the per-cell table. NEITHER row
    priced the architecture question this row carries. WHAT MAKES IT ARCHITECTURE RATHER THAN A DEFECT
    REPAIR. Closing it means making the head unconditionally admitting in TAIL_BASED_PROD mode and moving
    the section 10.3 ratio into the tail consumer -- which multiplies the admitted span volume by
    1/base_rate at every production cell (x10 at the team cells, x5 at multi-tenant), changes what
    max_buffered_traces and the C-OD-11 section 11.1 per-cell budgets are sized against, and alters the
    posture of EVERY class the tail preserves, not only the three B-133 named. SURFACED BY OUT-OF-FAMILY
    CODEX ROUND 1 AGAINST THE B-133 LEG''S COMMIT ff63c725, 2026-08-08 -- the reviewer''s premise was
    ADJUDICATED EMPIRICALLY AND CONFIRMED at the real production wiring rather than accepted or declined
    on argument, and the leg''s own contract TEXT was corrected where it had overclaimed coverage.'
  close_out: '(1) RE-GROUND THE WIRING FIRST, because it is explicitly marked as provisional. Re-read tracer_provider.py''s
    sampler resolution at the then-current HEAD and confirm it still binds the per-cell ratio in both modes
    and still carries the "current default sampler ignores the mode ... Future units may wire mode-conditional
    samplers" note. If a mode-conditional sampler has since landed, this row may already be closed by that
    work and should be re-measured rather than re-argued. (2) RE-MEASURE, do not re-reason. The probe is
    four lines of composition (build_default_sampler at the cell rate + the real TailKeepSpanProcessor,
    counting arrivals at on_end) across the three populations the summary names -- root-name-matched, event-carried,
    and non-root trigger -- so the non-uniformity is re-established rather than assumed. (3) DECIDE THE
    POSTURE, and this is a genuine architectural fork rather than a bug with a fix. Candidate A: mode-conditional
    head -- ALWAYS_ON (or ParentBased(ALWAYS_ON)) in TAIL_BASED_PROD mode, with the section 10.3 ratio applied
    inside the tail consumer at the root-close decision. This is what section 9.1 actually says and what
    tracer_provider.py already names as an unbuilt surface, but it multiplies in-process span volume by
    1/base_rate and moves the ratio decision to a component that currently has no ratio logic at all. Candidate
    B: rejected-carrier recording -- leave the head as is, but have the sampler record a counter or a minimal
    marker for dropped spans that WOULD have carried a preserved class, so the loss is observable rather
    than silent. Cheaper and honest, but it does not deliver the floor. Candidate C: accept and document
    -- ratify that the section 9.2 floor is a floor over the ADMITTED stream, which would require amending
    section 9.2/section 9.3''s "head=1.0 across all cells" language, since that language is what makes A
    the literal reading. (4) PRICE THE VOLUME CONSEQUENCE BEFORE CHOOSING A -- and note that this MERGES
    with B-133''s still-open F-08 residual rather than sitting beside it. Today''s ~20% multi-tenant admission
    is the ceiling on the B-133 arm''s added keep-volume; candidate A removes that ceiling, so F-08 measured
    against today''s admission would not survive this repair. The two MUST be measured together. (5) NOTE
    THE B-136 INTERACTION -- candidate A increases the population of buffered traces by 1/base_rate, which
    makes B-136''s never-resolving always-sampled-root traces a proportionally larger share of max_buffered_traces
    pressure. WHAT WOULD FALSIFY IT. A re-measurement showing event-carried and non-root-trigger admission
    at parity with root-name-matched admission (which would mean the head composition changed), or a ratified
    reading under candidate C that the floor was always scoped to the admitted stream. RE-MEASURED AT THE
    REAL VENUE 2026-08-13 -- STEPS (1) AND (2) ARE DONE; STEP (3) IS THE ONLY LIVE STEP. (1) tracer_provider.py:236-240
    still binds the per-cell ratio unconditionally, still discards the mode, and still carries the note
    that future units may wire mode-conditional samplers -- NO mode-conditional sampler has landed, so this
    row is NOT closed by other work. (2) EXECUTED THROUGH api.run, not a manufactured composition (an earlier
    draft of this arc was correctly rejected by out-of-family Codex for driving an uncalled helper): running
    the B-72 fan-out workflow end to end with a real TracerProvider and the production sampler at base_rate=0.0,
    NOTHING is exported at all -- including hitl.gate.evaluated, which IS a s9.2 member with a declared
    head=1.0 floor. With an always-on provider the same run records exactly workflow.envelope (ROOT) and
    hitl.gate.evaluated nested under it, so the loss is structural: workflow_driver.py:3305 opens workflow.envelope
    as the trace root, workflow.envelope is NOT a s9.2 member, and ParentBased consults the composite sampler
    ONLY for roots -- the root loses its draw and the child inherits the drop with its own name never consulted.
    THE s9.2 FLOOR IS ROOT-ONLY. TAIL HALF EXECUTED TOO (the row''s step (2) names TailKeepSpanProcessor
    explicitly): driving the REAL processor through the REAL run at base_rate=0.0, on_end receives ZERO
    arrivals -- a head-dropped span is never recorded, so the processor''s own s9.2 bypass-the-buffer arm
    can never act on it. SCOPE, STATED HONESTLY (an earlier draft of this arc claimed all 19 members are
    children of the envelope; that was FALSE and is WITHDRAWN): only 11 of the 19 members have a span-open
    site in src/ at all -- the other 8 are event-carried or unimplemented, i.e. population (i) -- and at
    least one span-backed member is a ROOT, since skill.activation is emitted from workflow_driver.py:3206
    which PRECEDES the envelope open at :3305. The starvation is therefore SCOPED to members emitted inside
    the envelope, and any repricing of step (3) must be against that set, not all 19. WITNESS: harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py
    -- 18 tests, deterministic at base_rate=0.0, with a positive control on the membership patch, mutation-probed
    by replacing ParentBased(root=X) with the bare X (both load-bearing tests go RED). STEP (3) REPRICED
    BY TWO MEASURED COUNTERFACTUALS AT THE SAME VENUE. (a) CANDIDATE A-PRIME -- NOT the row''s candidate
    A, which is mode-conditional ALWAYS_ON with the s10.3 ratio moved INTO the tail consumer and was NOT
    measured here; A-prime is dropping ParentBased so the composite sampler is consulted for EVERY span.
    A-prime admits hitl.gate.evaluated but STILL DROPS workflow.envelope, so it yields ORPHANED floor spans
    whose parent trace was discarded -- and its volume cost is 1/base_rate applied to FLOOR-MEMBER spans
    only, NOT to all spans, which is cheaper than the row''s original x10-on-everything framing but buys
    broken traces. (b) NEW candidate C1 -- add workflow.envelope itself to s9.2 -- delivers the floor to
    every in-workflow member by inheritance, in a one-line change. CORRECTED 2026-08-13 (codex round 3):
    C1 does NOT keep traces whole, and its real cost is worse than a volume multiplier. Measured through
    the SHIPPED TailKeepSpanProcessor, C1 forwards the envelope and every s9.2 member (both take the always-sampled
    bypass arm at tail_keep_span_processor.py:361), but an ordinary non-member child stays BUFFERED at root
    close -- and a s10.2 classification trigger does NOT rescue it, because making the ROOT always-sampled
    means the root takes the bypass arm and returns BEFORE the root-close flush-or-drop decision runs. So
    under C1 a workflow trace THAT CONTAINS AT LEAST ONE ORDINARY NON-MEMBER CHILD becomes a never-resolving
    buffered trace drained only by force_flush -- and WORSE THAN THAT under production bounds (measured
    2026-08-13, codex round 12): span_processor.py:373-374 always supplies max_buffered_traces / max_spans_per_trace
    from CollectorConfig (defaults 4096 each, types.py:741,752) and the buffer evicts DROP-OLDEST, so beyond
    the ceiling the ordinary children are EVICTED AND LOST rather than held -- force_flush never sees them.
    Measured at a cap of 3 over 100 traces: 3 buffered, 97 evicted. Under sustained load that IS the steady
    state, since under C1 every trace with an ordinary child occupies a slot and none is released at root
    close. So C1''s cost is DATA LOSS, not delay. SCOPED 2026-08-13 (codex round 4): an earlier wording
    said EVERY trace, which is wrong -- this arc''s own B-72 venue emits only workflow.envelope + hitl.gate.evaluated,
    both of which bypass the buffer under C1, leaving its buffer at ZERO; the measured stranding used a
    manufactured validator.evaluate child. HOW OFTEN that child population occurs in real workflows is UNMEASURED
    and is owed before step (3) chooses C1 (the driver does open ordinary spans -- validator.evaluate at
    workflow_driver.py:5600, plus tool.dispatch / sandbox.* / secret.fetch in the tool dispatcher -- so
    the population is non-empty, but its frequency is not established here) -- exactly the B-136 pressure
    this row''s own step (5) predicts, which C1 MAXIMISES rather than avoids. So the fork is no longer A-vs-B-vs-C
    but a sharper trade, and note the labels are FIVE distinct options not three: ORPHANED FLOOR SPANS AT
    LOWER VOLUME (A-PRIME, measured; the ORIGINAL candidate A -- mode-conditional ALWAYS_ON with the ratio
    moved into the tail consumer -- REMAINS IN THE OPTION SET, unmeasured, at its stated x10-on-everything
    cost) vs (C1) FULL TRACE ADMITTED AT THE HEAD BUT ORDINARY CHILDREN STRANDED IN THE TAIL BUFFER, at
    full volume -- NOT ''whole traces'' (corrected 2026-08-13, codex round 5): C1 admits the envelope and
    every s9.2 member at the head, but an ordinary non-member child is still buffered at root close and
    a s10.2 trigger does not rescue it vs RATIFYING A ROOT-ONLY FLOOR (C, which must amend s9.2/s9.3''s
    head=1.0-across-all-cells language). Candidate B remains a reporting option, not a repair. The dyadic
    C7+C11 council''s stated precondition (step (2) re-measurement first) is now MET. ALL THREE OF STEP
    (2)''s POPULATIONS ARE NOW EXECUTED (codex round 6): the api.run witness covers the non-root population
    only; test_all_three_starved_populations_at_one_composition covers root-name-matched (survives), non-root
    (dropped) and event-carried (dropped) at one composition, which is the probe shape step (2) actually
    prescribes. AND A TRIPWIRE FOR THE FIX THIS ROW AWAITS: every other measurement supplies a deterministic
    sampler override, so a future mode-conditional sampler repairing only TAIL_BASED_PROD would leave them
    all green -- test_a_production_cell_still_binds_the_unconditional_ratio_sampler composes a real production
    cell (team-binding x self-hosted-server, rate 0.1) through the shipped stage with NO override, so it
    reddens the moment that sampler lands. STEP (2)''s TAIL ARMS ARE NOW ALL EXECUTED (codex round 13 closed
    the contradiction the round-12 note conceded): test_all_three_populations_through_the_real_tail_processor
    runs root-name-matched, non-root member, non-root TRIGGER and event-carried through the REAL TailKeepSpanProcessor
    at production buffer bounds, spying on the processor''s own on_end. Only the root-name-matched span
    arrives; the starved populations never reach the tail at all, because the head drops them before recording.
    Step (2) is therefore discharged at the shape the row prescribes (build_default_sampler + the real tail
    processor across all three populations), not merely at the head. SEPARATELY, the nested-resume witness
    is STRUCTURAL, not executed: it proves the child dispatch is lexically inside the subagent.span block,
    NOT that the OTel context is still current at the child driver''s resume detection nor that the snapshot
    value is non-None. The conclusion (resume.attempted is a non-root child for nested resumes) therefore
    rests on containment plus the ordinary OTel context-propagation semantics, and an executed nested-resume
    parentage witness is OWED before B-162 acts on it. QUALIFIED 2026-08-13 (codex round 14, the SAME defect
    class as round 7 which this arc fixed for resume.attempted and initially missed here): skill.activation''s
    ROOT status holds for a TOP-LEVEL run only. The emit sits inside execute_workflow, which is exactly
    the function the child runner re-enters under an active subagent.span, so under a nested recursive run
    the same pre-envelope emit is a CHILD and is starved like the rest. The counterexample therefore SCOPES
    the starved population (it is not universal) rather than exempting skill.activation outright. GENERAL
    LESSON now applied to every root/child claim on this row: source-line ordering relative to the local
    envelope proves ORDERING, never TRACE-ROOT STATUS -- root status depends on the call venue, and this
    workspace has at least two venues (top-level api.run and nested sub-agent dispatch) for the same code.
    A-PRIME IS A PARTIAL, NAME-ONLY REMEDY (measured 2026-08-13, codex round 15): it addresses only ONE
    of the two starved populations. Removing ParentBased exposes every span''s NAME to HarnessCompositeSampler,
    repairing the non-root NAME-BACKED members; it does NOTHING for the EVENT-CARRIED members (the B-133
    family, which B-137 explicitly includes), because those ride as span EVENTS on an ordinary carrier and
    at span creation the head still sees only the carrier''s name and applies the base-rate draw. Measured
    at base_rate=0.0: A-prime exports hitl.gate.evaluated and NOT the event-carrying ordinary.carrier. So
    A-prime must NOT be selected at step (3) as a complete repair -- it would leave the event-carried floor
    starved. Pinned by test_candidate_a_prime_is_a_name_only_remedy. C1 RE-PRICED 2026-08-13 (PR #1331)
    -- SUPERSEDES this row''s C1 cost statement and the same-day deferral note. C1''s ''data loss, not delay''
    cost was a consequence of B-136''s name-arm early return, NOT of C1 itself: under C1 every workflow
    trace has an always-sampled root and so hit that arm. With B-136 REPAIRED the identical C1 counterfactual
    reads 0 buffered / 0 evicted (was 3 / 97 at a cap of 3), and with a s10.2 trigger present 100/100 ordinary
    children export where 0 did before. C1 therefore now costs its 1/base_rate volume multiplier with ORDINARY
    trace semantics -- no stranding, no eviction, no never-resolving population, and the B-136 interaction
    this row''s step (5) predicted is DISCHARGED rather than aggravated. The step-(3) council must weigh
    C1 at this corrected price; A-prime''s partial name-only limitation is UNCHANGED, so the A-prime-vs-C1
    comparison has moved materially toward C1.'
  council: 'YES, and this is the row in the family that genuinely earns one -- unlike B-133, whose repair was
    single-domain. Candidate A trades a real observability floor (C7: a preserved class that is dropped
    before any preservation rule can act on it is not preserved) against a 5x-to-10x in-process span
    volume increase at every production cell (C11: telemetry volume, collector capacity, and the C-OD-11
    section 11.1 per-cell budgets those caps were sized against). The tension is nameable in advance
    between two specific voices, which is the convening discriminator, and the decision changes a
    committed per-cell envelope rather than an implementation detail. Convene DYADIC C7 + C11 at step
    (3), NOT before step (2)''s re-measurement -- the pilot lesson is that councils convened before the
    probe converge to a single voice plus cosmetic consultants.'
  heading: '### B-137 · Head sampling starves the tail-keep mechanism for every preserved class that is **not a
    root span name** — the shipped head sampler applies the §10.3 ratio in **both** §9.1 modes
    *(surfaced by out-of-family Codex round 1 against the `B-133` leg''s commit `ff63c725`, 2026-08-08;
    premise adjudicated empirically at the real production wiring and CONFIRMED, with the
    non-uniformity falsifying the first framing)*'
- id: B-138
  title: 'validator.fail.class carries TWO taxonomies at one attribute name -- the shipped value is
    C-CP-25 ValidatorFailClass while C-CP-21 declares the attribute as ValidatorRetryExitClass'
  status: closed
  pr: '#1278 (the B-138 spec-correction leg -- design-substrate + register only, ZERO code). CLOSED
    2026-08-09 under operator-ratified DISPOSITION (a) (AskUserQuestion, after a full grounding pass with
    every cite re-read by content at HEAD): the wire attribute validator.fail.class carries the
    C-CP-25->C-CP-28 §25.2 ValidatorFailClass domain. STEP (1) EXECUTED: the producer write re-verified
    (drifted :346 -> :347); a SECOND live producer found that the registration missed --
    harness-runtime/src/harness_runtime/lifecycle/validator_escalation_composer.py:151-153 writes
    brief.fail_class.value (ValidatorFailClass | None, "unspecified" on None). STEP (2): the row''s OWN
    FALSIFIER FIRED -- C-CP-28 §25.5 (Spec_Control_Plane_v1_10.md:228-233, with §25.2''s ValidatorFailClass
    + ValidatorResult declarations at :140-206) is a cleared surface declaring the validator.fail span
    emitting exactly the shipped four attributes, so this was a stale-CP-gloss doc fix rather than a
    genuine live homonym; OD''s C-OD-29.1 ingestion schema (validator_namespace.py:112-136) was built
    against that shape; the downstream reader inventory came back ALL-AGNOSTIC (bare-name membership sets
    + count gates only -- attribute_class_enforcement.py:56, cp_source_namespace_verification.py:61-66,
    namespace_map.py:185 attribute_count=3, cp_namespace_export_manifest.py:114-121 attribute_count=3);
    NO reader asserts a ValidatorRetryExitClass value on the attribute; two CP tests assert
    "safety_policy" (test_validator_framework.py:274, test_workflow_driver_validator_hook.py:327); NO
    projection function exists in harness-{cp,od,runtime}/src. The conditional C5-C7 dyad NEVER FIRED --
    it was conditioned on (b) trending, and (b) was contradicted by every downstream fact. STEP (3)
    dispositioned in the same pass: the CP-3/OD-4 divergence is two contracts declaring
    disjoint-except-class subsets of one namespace, NOT one contract violated; §21.5''s declared count
    stays THREE so the count gates are untouched; validator.fail.permanence''s former derivation clause DEMOTED
    (the replacement derivation is NOT declared -- see STEP (4) below for why the outcome projection
    was rejected as lossy);
    cause_attribution stays declared-unwired (pre-existing). SPEC LEG: Spec_Control_Plane_v1_116.md
    (three amendment sites, all within C-CP-21 §21: §21.5 row-1 domain; the §21 retry-exit binding
    declaration; §21.5 row-3 derivation clause; plus the §1.4 stale-cross-cite reconciliation covering
    Spec_Control_Plane_v1_18.md item (iii) and v1_10 §25.2''s H_T-CP-21 parenthetical) + clearance
    marker spec-control-plane-v1-116-cleared-2026-08-09.md. STEP (4) DISCHARGED: B-124 is UNBLOCKED on the
    TAXONOMY question; the permanence-derivation SUB-DECISION is routed to B-124 explicitly (v1.116 §1.3
    -- the outcome projection is LOSSY: ESCALATE conflates permanent-fail-exit with HITL-recoverable, so
    a naive PERMANENT_FAIL wire would silently narrow §21.6''s always-sample-permanent coverage to the
    abort path; B-124 must materialize the retry-exit classification at the emission site, ratify the
    narrowing WITH §21.6 re-specified, or select another carrier). OUT-OF-FAMILY REVIEW (codex, 3 rounds -- round 1:
    2 P1 + 2 P2; round 2: 1 P1 + 4 P2; round 3: 3 P1 + 1 P2 -- all verified against source; one
    declined-then-accommodated): (P1) the ADR-D5 §1.10.1 authority-chain
    conflict -- fixed by the BUNDLED ADR-D5 v1.5 -> v1.6 amendment at the canonical declaration site,
    not overridden downstream; (P1) the lossy outcome-derivation -- §1.3 rewritten from a re-basing to
    the routed sub-decision above; (P2) the fail_class=None emission gap + the composer''s out-of-domain
    "unspecified" sentinel -- §1.1''s emission condition corrected to conditional-on-populated-fail_class
    with the sentinel declared non-member; (P2) cause_attribution declared-required with zero producers
    -- MINTED as B-139 rather than absorbed. ROUND 2: (P1) NO production
    path opens the declared validator.fail span -- attributes ride validator.evaluate
    (workflow_driver.py:5600) and validator.escalation (validator_escalation_composer.py:141) while OD
    sampling keys on validator.fail.*-shaped span NAMES production never emits -- MINTED as B-140
    together with the round-1/2 sentinel disposition, and §1.1 re-scoped to the attribute DOMAIN with
    the span-site gap recorded; (P2) the closure prose''s own stale re-based clause fixed on BOTH
    register surfaces; (P2) B-124''s row cross-noted (gate discharged, naive wire foreclosed, B-140
    coordination owed); (P2) the harness-cp CLAUDE.md ADR-D5 pointer row bumped v1.5 -> v1.6; (P2
    DECLINED, STANDING -- re-raised at rounds 5/6/8 and accommodated three ways: the close_out
    supersession marker, the section-leading banner, and per-bullet HISTORY marking in place; full
    replacement remains declined per the B-119 append-with-supersession precedent, recorded here as
    a standing reviewer disagreement) the replace-not-append register-prose demand -- the workspace convention is
    append-with-supersession-note per the B-119 precedent; the internal contradiction it flagged was
    real and IS fixed; at round 3 a SUPERSEDED-BY-CLOSURE marker was prepended to this row''s
    close_out so --detail output leads with the operative record. ROUND 3: (P1) the grounding claim
    "no reader asserts a retry-exit value" was FALSIFIED -- the C-OD-14 §14.5.3 replay-divergence
    carrier ReplaySemanticDivergenceError (declaration + test, ZERO production emission sites) fixes
    terminal-fail-exit and ADR-D6 v1.2 declares the retry-exit domain -- MINTED as B-141 (the OD/D6
    cascade) with the corrected claim stated at v1.116 §0.1; (P1) ADR-D5 §1.10''s own normative
    carrier references renamed IN PLACE (four sites -- the interpretive-note-only approach rejected);
    (P2) B-140''s remedy sharpened (trailing-dot prefix precision) and its venue corrected §10.2 ->
    §9.2-family, with B-124''s cross-note corrected to ADJACENT-not-gating. RESIDUALS: the stale "NEW at C-CP-25" docstring at
    validator_framework_types.py:71 (Class 3, rides the next arc touching that file); the second
    producer recorded here for B-124''s witness scope.'
  summary: 'WHAT IT IS, AND IT IS LIVE RATHER THAN IN-WAITING. The span attribute validator.fail.class is
    declared by CP as an ENUM_REF at Cardinality.LOW with the schema docstring stating "validator.fail.class
    is bounded(5)" (harness-cp/src/harness_cp/validator_fail_taxonomy.py:137-141, docstring at :154-159,
    per C-CP-21 §21.5 verbatim). The five values that declaration names are ValidatorRetryExitClass --
    transient-retry / Reflexion-recoverable / HITL-recoverable / permanent-fail-exit / terminal-fail-exit
    (:38-62, C-CP-21 §21.1). But the ONLY live producer of the attribute writes a DIFFERENT enum:
    harness-cp/src/harness_cp/validator_framework.py:346 sets attrs["validator.fail.class"] =
    result.fail_class.value where result.fail_class is a ValidatorFailClass -- the C-CP-25 §25.2 pre-emit
    categorization whose members are schema_violation / semantic_inconsistency / safety_policy /
    resource_constraint / external_rejection (harness-cp/src/harness_cp/validator_framework_types.py:70-90).
    That class''s OWN docstring names the collision without resolving it: "Distinct from C-CP-21 §21.1
    ValidatorRetryExitClass (post-fail retry-exit classification) homed at
    harness_cp.validator_fail_taxonomy" (:73-74). The attribute reaches the wire through the live
    validator.evaluate span at harness-cp/src/harness_cp/workflow_driver.py:5600 with attributes applied
    at :5620-5622. SO ONE ATTRIBUTE NAME CARRIES TWO CLOSED FIVE-VALUE DOMAINS WITH DISJOINT MEMBER SETS,
    discriminable only by string shape -- EXACTLY the cross-namespace homonym class as B-125, except that
    B-125''s is a homonym-IN-WAITING (no caller can trigger it, retry_breaker_tool.py:152-153) while this
    one is ALREADY SHIPPED at every non-PASS validator evaluation. A SECOND, SMALLER DIVERGENCE ON THE
    SAME NAMESPACE, RECORDED BECAUSE IT IS THE SAME DEFECT CLASS. CP declares THREE validator.fail.*
    attributes -- class, cause_attribution, permanence (validator_fail_taxonomy.py:137-153, C-CP-21 §21.5).
    OD declares FOUR, at span-site "validator.fail" -- class, detail_hash, next_action, escalation_owed
    (harness-od/src/harness_od/validator_namespace.py:112-136, C-OD-29.1). The two sets OVERLAP ON class
    ONLY: CP''s cause_attribution and permanence are absent from OD''s, OD''s detail_hash / next_action /
    escalation_owed are absent from CP''s. The live emission
    (validator_framework.py:_build_span_attributes, :343-349) produces OD''s four minus permanence and
    NONE of CP''s two remaining -- so neither declaration describes what ships. SURFACED BY DIRECT READ
    AT THE B-124 GROUNDING PASS, 2026-08-08 (#1277) -- not by any council, and not previously registered.'
  close_out: 'SUPERSEDED BY CLOSURE (#1278, disposition (a)) -- the steps below are REGISTRATION HISTORY; the operative record is this row''s pr: field. (1) RE-GROUND BOTH HALVES AT HEAD before pricing. Confirm validator_framework.py:346 still writes
    a ValidatorFailClass value and that CP §21.5''s declaration still names the ValidatorRetryExitClass
    domain; confirm the CP-3 / OD-4 attribute-set divergence still stands. Direct read, not recall -- the
    B-124 pass found the registration cites of three sibling rows line-drifted by one intervening arc.
    (2) DECIDE THE AUTHORITATIVE TAXONOMY FOR validator.fail.class -- this is the whole row, and it is a
    genuine fork rather than a wiring bug. Three candidates, none free. (a) C-CP-25 ValidatorFailClass is
    authoritative: the shipped value is correct and CP §21.5''s bounded(5) gloss plus the §21.1 five-value
    table are wrong as written and owe a design-substrate correction; the consequence is that
    validator_fail_permanence(ValidatorRetryExitClass) is derived from a taxonomy the attribute does NOT
    carry, which is exactly why B-124''s naive wire is blocked. (b) C-CP-21 ValidatorRetryExitClass is
    authoritative: the shipped value is a CONTRACT VIOLATION and validator_framework.py:346 must be
    re-typed or its value projected, which changes a live wire value every downstream consumer of the
    attribute sees. (c) TWO attributes: keep validator.fail.class for one domain and mint a distinct name
    for the other -- the B-125 disposition-(b) shape, priced against the same "demote the detail, never
    delete it" standard. Do NOT pick by preference; ground it on which declaration the DOWNSTREAM readers
    were built against (start with harness-od attribute_class_enforcement.py:56, namespace_map.py:185,
    cp_source_namespace_verification.py:64, and the CP export-manifest cardinality sums, which is where a
    wrong choice breaks a count gate). (3) DISPOSITION THE CP-3-vs-OD-4 SET DIVERGENCE in the same pass
    -- it is the same defect class at set granularity and splitting it across two arcs guarantees one is
    forgotten. Note the emission produces neither set exactly, so "make code match the spec" is
    under-determined until (2) resolves. (4) SEQUENCING, AND IT IS BINDING RATHER THAN ADVISORY: this row
    MUST be dispositioned BEFORE any arc touches
    harness-cp/src/harness_cp/validator_framework.py:_build_span_attributes -- specifically before
    B-124''s §10.2 half is wired there, because deriving validator.fail.permanence from result.outcome
    mints a THIRD derivation on a function whose authoritative domain is unsettled, and a later (b)
    would then have to unpick two changes instead of one. WHAT WOULD FALSIFY IT. A cleared spec surface
    stating that validator.fail.class carries the C-CP-25 domain (which would make this a stale-CP-gloss
    doc fix rather than a live homonym), or a projection function between the two taxonomies existing on
    a path this read missed -- the search covered harness-{cp,od,runtime}/src.'
  council: 'Not at registration -- nothing is decided here and step (1) is a re-grounding pass. Conditionally
    yes at step (2), and the discriminator is which candidate survives: (a) and (c) are single-domain
    C5/CP attribute-domain calls of the kind B-125 settled in venue, but (b) changes a LIVE wire value
    that OD-side readers and cardinality gates are built against, which puts C5 (validator semantics --
    the shipped categorization is the one operators actually see) against C7 (observability contract --
    a declared ENUM_REF domain that no producer honours is not a contract). That is nameable in advance
    and dyadic, so if step (2) trends toward (b), convene dyadic C5 + C7 AFTER the step-(1) re-grounding
    and the downstream-reader inventory, never before -- the pilot lesson that councils convened ahead of
    the probe collapse to one voice applies directly.'
  heading: '### B-138 · `validator.fail.class` carries **two taxonomies** at one attribute name — the shipped
    value is C-CP-25 `ValidatorFailClass` while C-CP-21 §21.5 declares the attribute as
    `ValidatorRetryExitClass` *(surfaced by direct read at the `B-124` grounding pass, 2026-08-08 — a
    LIVE homonym, same class as `B-125` but already shipped)*'

- id: B-139
  title: 'validator.fail.cause_attribution is declared always-emitted at TWO canonical surfaces
    (ADR-D5 §1.10.1 + C-CP-21 §21.5) but has ZERO producers'
  status: registered_finding
  summary: 'WHAT IT IS. Both canonical declaration surfaces state the attribute is always-emitted on every
    validator-failure event -- ADR-D5 §1.10.1 bullet 2 (open-set enum, 10 base values + 5 F5 secret-*
    refinements, with the FM-J framing "a fail-class without cause_attribution is FM-J per
    c5-validation-contract SKILL.md") and C-CP-21 §21.5 row 2 (Spec_Control_Plane_v1_2.md §21.5,
    medium/open-set cardinality). But ZERO producers exist: the framework producer
    (validator_framework.py:_build_span_attributes, :320-352) sets class/detail_hash/next_action/
    escalation_owed and never this key; the runtime escalation composer (validator_escalation_composer.py
    :143-153) likewise; an rg over harness-*/src finds no write site. SURFACED at the B-138 spec-leg
    out-of-family codex round (P2), 2026-08-09 -- the B-138 leg corrected the class domain and demoted
    the permanence derivation clause, and without this row the third §21.5 attribute would be left
    declared-required with no owner.'
  close_out: '(1) RE-VERIFY zero producers at HEAD (rg "validator.fail.cause_attribution" over
    harness-*/src -- declaration/manifest hits only). (2) DECIDE: (a) wire the emission at the
    fail-classification site per the declared C5 ownership cell -- pricing availability honestly: the
    cause values live in §21.2''s staircase branches and the retry/breaker machinery, NOT in
    ValidatorResult, so the producer does not currently hold a cause to emit; or (b) re-declare the
    emission condition at BOTH canonical surfaces via back-flow (demote "always-emitted" to
    conditional-or-deferred, the same demote-not-delete standard B-138 used). (3) Whichever way,
    ADDRESS THE FM-J FRAMING explicitly -- decide whether it is live debt or aspirational, and record
    it; a declared failure-mode name attached to an unwired attribute is exactly the
    stale-carry-text class. WHAT WOULD FALSIFY IT: a producer writing the key anywhere in
    harness-*/src.'
  council: 'No at registration -- grounding first. Conditionally at step (2): wiring a medium-cardinality
    open-set attribute trades validation-contract completeness (C5 -- the FM-J rule exists because
    cause-blind fail classes defeat the staircase''s cause-conditioned branch) against emission cost and
    operator signal value (C7/C8). Nameable and dyadic if (a) trends; a probe-first pass over what the
    staircase machinery actually holds may resolve it without convening.'
  heading: '### B-139 · `validator.fail.cause_attribution` is declared always-emitted at TWO canonical
    surfaces (ADR-D5 §1.10.1 + C-CP-21 §21.5) but has ZERO producers *(surfaced at the B-138 codex
    round, 2026-08-09)*'

- id: B-140
  title: 'The validator.fail.* wire realization diverges from its declared span-site and value alphabet --
    no production path opens a validator.fail span, and the escalation composer ships an out-of-domain
    sentinel'
  status: registered_finding
  summary: 'TWO GAPS, ONE FAMILY -- both are declared-vs-shipped divergences on the validator.fail.*
    namespace that survive B-138''s domain settlement. (A) SPAN-SITE: C-CP-28 §25.5 declares a
    validator.fail SPAN (Spec_Control_Plane_v1_10.md:233) and OD''s C-OD-29.1 declares span_site
    "validator.fail" (validator_namespace.py:54, :112-136), but NO production path opens such a span --
    the attributes ride the validator.evaluate span (workflow_driver.py:5600, §C-OD-29.1 envelope
    attach loop) and the runtime composer''s validator.escalation span
    (validator_escalation_composer.py:141). OD sampling fixtures are built against
    validator.fail.*-SHAPED SPAN NAMES production never emits (test_sampling_mode.py:215
    "validator.fail.semantic_inconsistency"; test_composite_sampler.py:265-268), so the declared
    §21.6/§25.5 always-sample keying cannot match a live span name -- LOAD-BEARING for the NAME/prefix-keyed always-sample family
    (B-123/B-133/B-137''s §9.2-style keying -- is_always_sampled matches the trailing-dot prefix
    "validator.fail." and would NOT match a span named exactly "validator.fail", so the step-(2)
    open-the-span remedy MUST use a concrete prefixed name or change the sampler) -- NOT for B-124''s
    §10.2 half, whose trigger reads the permanence ATTRIBUTE from any span and is not gated by span
    naming (codex round-3 venue correction). (B) SENTINEL: on HITLEscalationBrief.fail_class=None the composer writes
    validator.fail.class="unspecified" (validator_escalation_composer.py:151-153) -- an out-of-domain
    value under the closed five-member declaration (CP v1.116 §1.1; ADR-D5 v1.6 §1.10.1), while the
    framework producer OMITS the attribute on fail_class=None. SURFACED at the B-138 spec-leg codex
    rounds (round 2 P1 + round 1/2 P2), 2026-08-09; CP v1.116 §1.1 records both gaps and routes them
    here rather than absorbing them as conformance.'
  close_out: '(1) RE-GROUND both gaps at HEAD (span-open sites; the OD sampling key shapes; the two
    None-path behaviours). (2) SPAN-SITE DECISION: open a validator.fail.-PREFIXED span (a concrete
    suffixed name -- the exact name "validator.fail" alone leaves sampling_mode.py:239''s
    trailing-dot prefix match INERT, so this option MUST pair a suffixed name or a sampler change, AND MUST
    amend the exact-span declarations it diverges from -- C-CP-28 §25.5 + C-OD-29.1 -- to declare
    the suffixed/prefix family, else the repair itself re-opens the contract violation)
    at the producer(s), OR re-declare the span-site at both contracts (C-CP-28 §25.5 + C-OD-29.1) to the
    shipped validator.evaluate/validator.escalation carriers, OR alias at the OD classification layer
    -- grounded on what the OD samplers + B-123/B-133 tail machinery actually key on, and
    coordinated with B-124 as ADJACENCY, not a gate -- its §10.2 trigger is attribute-keyed
    (tail_keep_classification.py:96-100) and is not blocked by span naming. (3) SENTINEL DECISION: absent-on-None at the composer
    (matching the framework producer) OR widen the wire alphabet at both canonical surfaces; either
    way the two producers must agree. (4) COORDINATE: B-124 (ADJACENT -- attribute-keyed §10.2 trigger, not gated), B-123/B-133 family
    (tail keying), B-139 (sibling §21.5 gap). WHAT WOULD FALSIFY IT: a production
    start_as_current_span("validator.fail...") site, or a cleared surface declaring
    validator.evaluate as the canonical carrier span for these attributes.'
  council: 'No at registration -- grounding first. Conditionally at step (2): span-site repair trades
    contract fidelity (C7 -- a declared span-site no sampler can match is dead contract) against
    emission-shape churn every OD consumer sees (C5/C8). Nameable and dyadic if the re-declare route
    trends; probe-first over the OD keying may resolve it.'
  heading: '### B-140 · The `validator.fail.*` wire realization diverges from its declared span-site and
    value alphabet *(surfaced at the B-138 codex rounds, 2026-08-09)*'

- id: B-141
  title: 'The replay-semantic-divergence surfaces (C-OD-14 §14.5.3 + ADR-D6 v1.2) fix
    validator.fail.class = terminal-fail-exit -- a retry-exit value now out-of-domain under the B-138
    disposition (a) -- and owe a cascade at the OD/D6 venue'
  status: closed
  pr: "#1318 (the B-141 cascade leg: OD spec v1.41 + ADR-D6 v1.3 + CP plan v2.52 + OD plan
    v2.35 + carrier/witness updates)"
  summary: 'WHAT IT IS. B-138 settled the wire attribute validator.fail.class on the C-CP-25->C-CP-28
    ValidatorFailClass domain (CP v1.116, operator-ratified (a), 2026-08-09). ONE DECLARED surface
    family still binds the OLD retry-exit domain to the wire name: C-OD-14 §14.5.3''s
    replay-semantic-divergence escalation carrier ReplaySemanticDivergenceError
    (harness-od/src/harness_od/idempotency_join_dedup.py:345-367) fixes validator_fail_class =
    "terminal-fail-exit" (plus cause_attribution = "replay_semantic_divergence", permanence =
    "permanent", always_sampled = True), enforced by test_idempotency_join_dedup.py:304-306; and
    ADR-D6 v1.2 declares the retry-exit five-value domain for the attribute at its ingestion rows
    (ADR-D6_v1_2.md:120 domain row; :298 requiring terminal-fail-exit). ZERO production emission
    sites construct the carrier at HEAD (declaration + test only), so no live wire value violates
    the B-138 disposition -- but the DECLARED surfaces contradict it and the register''s B-138
    grounding claim ("no reader asserts a retry-exit value") was falsified by this family at codex
    round 3. A SECOND deferred carrier is in the same family (codex round 5):
    VerifierResult.validator_fail_class (both_by_tier_overlay.py:78-95, the U-CP-41 two-agent-observer
    verifier output) is documented as drawn from the C-CP-21 §21.5 five-value set, enforced at
    test_both_by_tier_overlay.py:121-126, with U-CP-41 plan criteria at
    Implementation_Plan_Control_Plane_v2_9.md:621-630/:683-686 -- deferred (no live producer), but its
    audit contract routes to validator.fail.*, so it would violate the authoritative wire domain when
    the observer activates. The CP plan delta-chain history (Implementation_Plan_Control_Plane_v2_4.md
    :604/:624, the U-CP-47-era criteria) also carries the five-value wire framing -- HISTORY, not retroactively
    edited, recorded here so no future reader mistakes it for current authority. SURFACED at the
    B-138 spec-leg codex round 3 (P1), 2026-08-09.'
  close_out: 'CLOSED 2026-08-12 (PR #1318) -- ALL THREE STEPS DISCHARGED; the operative
    record is .harness/b-141-validator-fail-class-cascade-2026-08-12.md. (1) re-grounded at HEAD
    (carrier at idempotency_join_dedup.py:347 drifted from :345; witness at
    test_idempotency_join_dedup.py:307; ADR-D6 rows :120/:298; production emission sites ZERO by
    rg over harness-*/src+tests). (2) cascade landed at all FIVE venues (the fifth, the OD plan, surfaced by this arc''s
    own cross-spec drift grep -- not named at registration) under carried value
    validator.fail.class = semantic_inconsistency (the row''s own natural candidate -- a
    ValidatorFailClass member; the replay contradicts the F2 ledger''s prior recorded state; NO new
    wire attribute minted so the council clause never fired): ADR-D6 v1.2 -> v1.3 IN PLACE
    (§1.5.2 table row + §1.2.2.1 retry.fail_class cross-ref clause reworded -- retry.fail_class
    KEEPS the retry-exit domain); OD spec v1.40 -> v1.41 (C-OD-14 §14.5.3 escalation-table row;
    permanence=permanent + its C-OD-09 §9.2 always-sampled arm PRESERVED VERBATIM so the intent
    survives structurally); CP plan v2.51 -> v2.52 (U-CP-41 acc #5 + U-CP-47-era
    VALIDATOR_FAIL_NAMESPACE_SCHEMA superseded AS-DOMAIN as NOTES, landed units NOT amended per the
    B-97(a)/B-118 precedent; test-roster supersession test_verifier_fail_class_in_cp_21_5_set ->
    test_verifier_fail_class_in_validator_fail_class_domain); OD plan v2.34 -> v2.35 (U-OD-20-era
    ESCALATION-to-terminal-fail-exit criteria superseded AS VALUE as a NOTE -- v2_2.md:55/:66/:167/:368
    were the standing authority at head v2.34 and would have contradicted OD spec v1.41 in-axis); carrier default + docstrings + both
    witness files updated (59+9 pass). (3) honored -- declared-shape-only; NO emission wiring. B-124
    derivation sub-decision, B-139 cause_attribution producers (incl. the NEVER-DISCHARGED D6-v1.2
    forward flag that ADR-D5 would absorb replay_semantic_divergence -- recorded at the back-flow
    doc §4, not chased), and B-140 span-site/keying family all UNTOUCHED per the coordination notes.
    --- Registration record below --- (1) RE-GROUND at HEAD: the carrier defaults + test; the ADR-D6 rows; confirm production
    emission sites are still ZERO. (2) CASCADE at the owning venues, per the B-138 disposition: an
    ADR-D6 amendment (in-place revision, the ADR-D5 v1.6 mechanics) + an OD spec leg for C-OD-14
    §14.5.3 + the carrier-default/test updates + a CP PLAN-NOTE DELTA superseding the landed
    U-CP-47 criteria as-domain (Implementation_Plan_Control_Plane_v2_4.md:631-638, preserved at
    v2.16 -- an ACTIVE spec/plan contradiction for any future re-validation arc, per codex round 4;
    the landed unit is NOT amended, per the B-97(a)/B-118 new-unit precedent) + the U-CP-41
    family (the VerifierResult carrier docstrings + test_both_by_tier_overlay.py:121-126 + the
    U-CP-41 plan-note, same as-domain supersession) -- deciding WHAT the divergence escalation carries
    under the new domain: a ValidatorFailClass member (semantic_inconsistency is the natural
    candidate), a distinct attribute for the retry-exit classification (the B-138 candidate-(c)
    shape), or a re-declaration of the §14.5.3 fixed attributes. The permanence="permanent" +
    always-sampled intent MUST survive whichever choice (coordinate with B-124''s derivation
    sub-decision and B-140''s keying family). (3) The choice changes DECLARED event shape only at
    HEAD (zero emission sites) -- price accordingly; do not let the cascade silently widen into
    wiring the emission (that would be its own arc). WHAT WOULD FALSIFY IT: a production
    construction site of ReplaySemanticDivergenceError appearing before the cascade lands (that
    would make the out-of-domain value LIVE and upgrade urgency).'
  council: 'No at registration -- the cascade direction is largely forced by the ratified B-138
    disposition; the step-(2) carried-value choice is a C5/C7 attribute-semantics call of the kind
    B-125/B-138 settled in venue. Convene only if the distinct-attribute route (candidate-(c) shape)
    trends AND it would mint a new wire attribute.'
  heading: '### B-141 · The replay-semantic-divergence surfaces (C-OD-14 §14.5.3 + ADR-D6 v1.2) fix
    `validator.fail.class = terminal-fail-exit` -- out-of-domain under B-138 (a); cascade owed at the
    OD/D6 venue *(surfaced at the B-138 codex round 3, 2026-08-09)*'
- id: B-142
  title: Two harness-inspect pause-journal witnesses assert that no usable config exists without isolating
    an ambient auto-discovered harness.toml, so they pass in CI and fail on any machine carrying the documented
    local config
  status: closed
  summary: 'WHAT IT IS. harness-runtime/tests/test_pause_journal_enumeration_b97a.py contains two witnesses
    -- test_an_undeterminable_scope_is_reported_not_assumed_untenanted and test_round4_an_unknown_scope_does_not_classify_an_untenanted_journal_current_format
    -- whose end-to-end legs drive main([--ledger-path, ..., --json]) and assert payload[pause_journal_deployment_scope_known]
    is False on the stated premise "where no usable config exists at all". They do NOT isolate the ambient
    config source. _authoritative_tenant_scope (harness-runtime/src/harness_runtime/admin/inspect.py:1035)
    deliberately loads through the NORMAL RuntimeConfigSource precedence chain rather than only an explicit
    --runtime-config -- that widening was itself an out-of-family round-3 [P2] repair and it is CORRECT --
    so an auto-discovered harness.toml satisfies the load and scope_known comes back True. SURFACED AT THE
    B-123 ARC, 2026-08-09, by a full `just check` on the developer machine: 2 failed / 7705 passed, both failures
    these two tests, on a branch whose diff touches NO source either test imports. GROUNDED, NOT ASSUMED. (a)
    mains own CI is GREEN at the same base commit e496d97e, so the divergence is local, not a product defect.
    (b) The cause was probed directly: RuntimeConfigSource.load(config_file=None) executed in the repo root
    RETURNS a config (tenant_id=None) rather than raising RuntimeConfigLoadError, because a GITIGNORED harness.toml
    sits at the repo root -- the documented normal local setup. (c) A first hypothesis, that the justfiles
    `set dotenv-load := true` was injecting the config, was TESTED AND FALSIFIED: the two tests fail under
    a plain `uv run pytest` with no dotenv, and .env carries no HARNESS_* keys. THE DEFECT IS TEST ISOLATION,
    NOT PRODUCT BEHAVIOUR. The production widening is contracted and desirable; what is missing is that a witness
    asserting the ABSENCE of a usable config must neutralize the ambient discovery it asserts against, or it
    is asserting a property of the developers filesystem. The consequence is a CI-INVISIBLE RED GATE: `just
    check` cannot be clean for any developer with a local harness.toml, which trains the reader to discount
    gate failures -- the failure mode that hides a real one.'
  close_out: '(1) RE-GROUND FIRST at HEAD: run the two tests with and without a harness.toml at the repo root
    and confirm the discriminator is exactly the ambient file -- this rows whole premise is an environment-dependence
    claim and only the run proves it. (2) DECIDE THE ISOLATION MECHANISM; the candidates differ in kind and
    the choice is NOT obvious: (a) point the loader at an absent/empty config path for these legs, local and
    honest but fixes only the two call sites; (b) an autouse fixture neutralizing ambient config discovery
    for the whole runtime test package, broader but may mask a genuinely config-dependent regression elsewhere;
    (c) make the discovery root injectable at RuntimeConfigSource -- the only candidate touching src/ and therefore
    the only one that could owe a contract question, so ground before reaching for it. (3) SWEEP FOR SIBLINGS
    rather than assuming these two are alone: any other witness asserting an ABSENT ambient source is exposed
    to the same class. (4) DO NOT repair by deleting or weakening the assertions -- scope_known is load-bearing,
    and the tests own docstrings record it as a mutation probe (the flag is consumed in exactly one place,
    so a weakened assertion would silently restore the false all-clear the B-97(a) round-4 [P2] review existed
    to prevent). NOT BLOCKING the B-123 arc: pre-existing, environment-only, unrelated to that diff, and disclosed
    in PR #1279s body rather than absorbed.  CLOSED
    2026-08-09 AT PR #1281, ALL FOUR STEPS DISCHARGED. STEP (1) RE-GROUNDED NON-DESTRUCTIVELY -- the
    discriminator is confirmed to be EXACTLY the ambient file, and it was proven WITHOUT touching the
    operators real gitignored config: the same test file was run twice, once with cwd = repo root (2
    failed / 19 passed) and once with cwd = a clean temp dir via uv run --project with absolute test
    paths (21 PASSED). The mechanism is RuntimeConfigSource._discover_default_config resolving Path.cwd()
    / harness.toml -- PROCESS CWD ONLY, no parent walk. STEP (2) MECHANISM SELECTED -- candidate (a).
    Both exposed tests now call a shared helper that monkeypatch.chdir-s to an empty tmp_path AND asserts
    the premise, so the isolation is a POSITIVE CONTROL rather than an assumption: if it ever silently
    stops working the test fails loudly at the premise instead of mysteriously at the scope_known
    outcome. monkeypatch.chdir is used rather than a bare os.chdir so the cwd restores even on failure.
    Candidates (b) and (c) were REJECTED on grounds, not cost: (b) a suite-wide autouse fixture can mask
    a genuinely config-dependent regression elsewhere, and (c) an injectable discovery root touches src/
    and could owe a contract question for no benefit here. ZERO src change; the C-OD/inspect precedence-
    chain widening at _authoritative_tenant_scope is CONTRACTED and CORRECT and is preserved verbatim.
    STEP (3) SIBLING SWEEP -- COMPLETENESS ARGUED, NOT ASSUMED. All harness-*/tests trees swept;
    RuntimeConfigSource is runtime-axis-only (zero importers in the other five tests trees). ELEVEN
    candidate files checked and cleared with a stated reason each (test_config_source.py already carries
    an equivalent autouse chdir fixture -- the pre-existing precedent this fix follows;
    test_admin_inspect.py never creates a pause-journal dir so the tenant-scope path is unreachable; two
    cli_overrides callers are CLI-precedence-dominated and their asserted fields are absent from the
    ambient file; the rest touch unrelated surfaces). The decisive bound: grep -rn Path.cwd() over all
    harness-*/src returns EXACTLY ONE hit -- config_source.py -- so this auto-discovery defect class has
    exactly ONE production site and no further siblings can exist. STEP (4) HONORED -- no assertion
    weakened, deleted or relaxed; the scope_known mutation-probe semantics the B-97(a) round-4 [P2]
    review established are byte-preserved. LOAD-BEARING BY MUTATION: removing the isolation line
    reproduces the original RED from the repo root; restored from a cp backup and verified byte-identical
    with diff -q. THE ARCS PURPOSE, VERIFIED BY EXECUTION: just check on the developer machine WITH the
    ambient harness.toml present now exits 0 at 7714 passed / 0 failed, where it was 2 failed / 7705
    passed before. The operators harness.toml was never moved, renamed, deleted or modified at any point.  NAMED
    OPEN RESIDUAL ON THE CLOSED ROW (the B-104 / B-133 precedent), raised by the merge-gate test-witness
    lens at PR #1281 and recorded rather than absorbed: THE ISOLATION IS NOT STRUCTURALLY GUARANTEED. A
    future edit that drops the helper call from ONE of the two tests -- or reorders it after main() --
    reproduces this exact defect for that test, and CI STRUCTURALLY CANNOT CATCH IT, because CI never
    carries the ambient harness.toml that exposes the divergence. The option landscape at step (2)
    considered a SUITE-WIDE autouse fixture (rejected: can mask a genuinely config-dependent regression
    elsewhere) and an injectable discovery root (rejected: touches src/), but did NOT consider a FILE-
    SCOPED autouse fixture -- which is precedented in this very test tree at harness-
    runtime/tests/test_config_source.py, whose module-local autouse fixture chdir-s every test in that
    file. A file-scoped fixture would make the isolation structural for the whole module without the
    suite-wide masking risk, leaving the explicit premise assertion where it carries meaning. NOT a
    correctness defect in what shipped -- the lens verified the shipped isolation is load-bearing and
    non-vacuous and returned APPROVE -- and NOT a hidden gap, since it is named here. A second named
    caveat from the same lens: the premise assertion could in principle pass for the WRONG reason if a
    malformed ambient HARNESS_* env var made RuntimeConfigSource.load raise via the env-coercion path
    BEFORE file discovery is consulted; narrow, absent in the verified environment, and recorded for
    honesty rather than because it fired.'
  council: No. A test-isolation defect with a named, reproducible discriminator and a repair that is local
    unless step (2) selects candidate (c). Convene only if (c) is chosen and it turns out to owe a contract
    question at the RuntimeConfigSource precedence chain.
  pr: '#1281 (the B-142 test-isolation leg -- both exposed pause-journal witnesses pinned to a
    deterministic empty CWD with the no-config premise asserted as a positive control; sibling sweep
    bounded by the single Path.cwd() production site; zero src change)'
  heading: '### B-142 · Two `harness-inspect` pause-journal witnesses assert *"no usable config exists"* without
    isolating an ambient auto-discovered `harness.toml`, so they pass in CI and fail on any machine with the
    documented local config *(surfaced at the `B-123` arc by a full `just check` run, 2026-08-09)*'
- id: B-143
  title: A wall-clock shutdown-bounding assertion has zero headroom -- its threshold equals the simulated hang,
    so scheduler noise on a loaded CI runner flips it
  status: closed
  summary: 'HISTORICAL REGISTRATION GROUNDING (anchors resolve against the pre-closure PR #1285 tree). harness-runtime/tests/test_shutdown.py::test_shutdown_bounded_by_timeout_when_dispatch_executor_drain_hangs
    asserted `elapsed < 0.3` at :894. The simulated hang was `drain_sleep_seconds=0.3` (:886) and the bound under
    test is `shutdown(ctx, timeout=0.02)` (:892), so the INTENDED elapsed is ~0.02s while the assertion threshold
    is 0.3 -- the SAME value as the hang. The margin between bounded (0.02) and not-bounded (0.3) is therefore entirely
    consumed by event-loop scheduling delay, and on a loaded CI runner the measurement overshoots. OBSERVED TWICE
    IN ONE SESSION, both on DOC-ONLY PRs where no code changed and the test could not have regressed: 2026-08-09
    at PR #1280 (`assert 0.34517101499997693 < 0.3`) and again at PR #1285 (`assert 0.3072814269999924 < 0.3`).
    The #1280 occurrence was rerun and passed green, confirming flakiness rather than breakage. THE DISCRIMINATOR CANNOT SIMPLY
    BE DELETED. The sibling assertions at :895-896 (`sub_agent_dispatch_executor` in report.failures, report.timed_out
    is True) do NOT discriminate the mutation this test exists to catch: the test''s own docstring (:884-885) says
    a regression makes shutdown revert to waiting out the full 0.3s sleep instead of bounding to ~0.02s -- and that
    regression STILL sets timed_out, so :894 is the only assertion that can see it. The defect is the THRESHOLD''s
    lack of headroom, not the assertion''s existence.'
  close_out: '(1) RE-GROUND by re-reading :884-896 at HEAD and confirming the three figures still stand (hang 0.3,
    timeout 0.02, threshold 0.3) -- if any has moved the analysis changes. (2) CONFIRM THE FAILURE MODE IS TIMING,
    NOT LOGIC, by running the test under artificial load or with a deliberately delayed event loop and observing
    elapsed cross 0.3 while report.timed_out stays True; a logic regression would flip the sibling assertions too.
    (3) CHOOSE THE FIX. The workspace precedent is PR #1226, which hardened an analogous wall-clock flake TEST-ONLY
    by moving to a scripted clock rather than by widening a bound -- prefer that shape, since widening the threshold
    toward the hang duration destroys the discrimination the assertion exists for, and widening the hang instead
    makes every run slower. A scripted/injected clock keeps the discriminator exact and removes the scheduler dependency
    entirely. (4) SUPERSEDED AT CLOSURE: preserve :894''s discriminating INTENT; the literal elapsed assertion may
    be replaced by a structural witness that fails on the same missing-wait_for mutation. The sibling report assertions
    alone remain satisfied by both the good and the bad behaviour.
    (5) SWEEP FOR SIBLINGS: enumerate other wall-clock elapsed-time assertions across harness-*/tests whose threshold
    is within the same order of magnitude as the delay they measure, and report whether any share this zero-headroom
    shape. WHAT WOULD FALSIFY IT. The test failing with report.timed_out False or the failures set empty -- that
    would be a real logic regression, not a timing flake. Or a run under deliberate load where elapsed stays comfortably
    under 0.3, which would mean the two observations had another cause. SIBLING FOUND 2026-08-09 (#1289, the B-126
    arc) -- STEP (5)''s SWEEP HAS ITS FIRST CONCRETE HIT, AND IT IS A DIFFERENT MECHANISM IN THE SAME CLASS. harness-runtime/tests/test_lifecycle_protected_result_store.py::test_gc_sweep_blocks_across_separate_processes_via_cross_process_lock
    asserted a child process signals readiness within a 10.0s deadline (historical pre-closure anchor :1481;
    current shared readiness helper at :252, child-script gc_sweep call at :238, and readiness-helper invocation
    in the gc_sweep test at :1533).
    The child is a COLD interpreter that must import the harness packages before it can write the marker, and that
    import cost is what the deadline is really racing. MEASURED, NOT INFERRED: three consecutive standalone runs
    of the very same child script gave time-to-ready of 5.39s / 33.89s / 7.69s -- so the deadline sits INSIDE the
    observed spread and one trial exceeded it by 3.4x. THE FAILURE MODE IS TIMING, NOT LOGIC, established by positive
    control: the child run standalone exits rc 0 with an empty stderr and DOES write its marker, so it is not crashing
    on import. Observed RED once during this arc''s own gate run (which was competing with three review subagents
    plus an out-of-family reviewer) and again in ISOLATION on the loaded machine, while passing in FOUR earlier
    full-gate runs on the same commit range -- the signature of load-sensitive variance rather than a regression.
    DISTINCT FROM THIS ROW''S OWN CASE in mechanism and therefore in remedy: :894''s threshold EQUALS the simulated
    hang, whereas this one''s deadline is unrelated to what it measures (interpreter startup + package import),
    so the #1226 scripted-clock shape does NOT transfer -- a clock cannot make a real cold import faster. The candidate
    remedies are instead to warm/pre-import in the child before the handshake, to raise the deadline to a real multiple
    of measured cold-import cost, or to replace the readiness handshake with a deterministic signal the parent can
    await. The handshake itself must NOT simply be deleted: its own docstring records that it is LOAD-BEARING (a
    cold child''s import cost would otherwise mask a missing lock, which is the defect the test exists to catch).
    NOT A DEFECT INTRODUCED BY #1289 -- that arc touches harness-cp tests plus register prose only, and harness-runtime/
    is byte-unchanged in its diff. CLOSED 2026-08-09 (this arc) -- THE CLASS WAS CLOSED TEST-ONLY WITHOUT WEAKENING
    ITS MUTATION DISCRIMINATORS. The named dispatch-executor witness and its three exact shutdown siblings (tracer,
    provider and MCP host) no longer compare real elapsed time to the same 0.3s hang they simulate. Each now parks
    the fake operation on an explicit release gate beneath an outer mutation bound that fails if the production wait_for
    wrapper is removed. The synchronous tracer/dispatch witnesses record the to_thread call attempt without requiring
    the worker body to win the same 0.02s scheduling race and prove non-completion; the dispatch stage separately
    proves its reachable timeout-before-worker-start venue. The async provider/MCP fakes isolate already-witnessed
    predecessor stages, use a 0.5s per-stage budget beneath a 2.0s outer mutation bound, and record start, cancellation,
    and non-completion at an asyncio.Event wait. This is the #1226 principle -- make the discriminating state exact rather than widen
    a wall-clock bound -- applied with a structural gate because the state under test is cancellation, not TTL arithmetic.
    The sibling sweep found FOUR zero-headroom shutdown tests, not one, and all four are hardened. The two cross-process
    lock witnesses shared the cold-import shape (write_once as well as the registered gc_sweep test): their 10s deadlines
    are replaced by one 60s cold-child budget, above the measured 33.89s maximum and consistent with existing process
    witnesses, plus early failure when the child exits before reaching flock. The ready-marker handshake itself is
    preserved verbatim and remains load-bearing. The aggregate gate then measured TWO additional process-start siblings:
    the Antigravity CLI descendant witness missed its arbitrary 5s startup deadline, and a Codex session-start hook
    exceeded the shared 30s subprocess deadline; both passed immediately in isolation (6.49s and 3.04s complete test
    durations), falsifying a logic defect. Their startup budgets are now 60s, and the reviewer witness also fails
    immediately if its controller exits before creating the child. Its independent 3s descendant-reap discriminator
    and 10s SIGTERM-completion bound remain unchanged. ZERO src change, ZERO design-substrate change, ZERO load-bearing
    discriminator deletion; elapsed assertions were replaced by stronger state/call witnesses. A later aggregate rerun exposed one observation-layer sibling: the pure PR-head mismatch unit test could
    be preempted by the production session-state shell probe returning UNKNOWN after its own 10s timeout. The four
    pure merge-proof classifier tests now declare SessionState.INACTIVE as an explicit precondition; all session-state
    integration tests retain the real hook path, so the classifier assertions remain discriminating without inheriting
    unrelated shell-probe timing. A third aggregate rerun found the exact original shape in the session-start advisory-hygiene
    witness: real elapsed was 5.77s against a 5s assertion despite the 1s hygiene bound returning correctly. That test
    now captures the exact `1` argument passed at the real hook_bounded entry and asserts the deliberately hung hygiene
    process received TERM; a 15s outer subprocess bound still kills a mutation that removes the hook''s bound before
    the fake''s 30s sleep completes. ZERO production-tool change.'
  council: No. A test-hygiene defect with two measured occurrences, a named precedent for the fix shape (#1226's
    scripted clock) and no contract surface. The only judgement is step (3)'s fix shape, which the precedent already
    answers.
  pr: '#1290'
  heading: '### B-143 · A wall-clock shutdown-bounding assertion has **zero headroom** — its threshold equals the
    simulated hang, so scheduler noise on a loaded CI runner flips it *(observed twice on doc-only PRs, 2026-08-09)*'
- id: B-144
  title: C-CP-24 24.1.B's Attribute count column was never re-tabled after C-CP-03 3.5's two amendments, and the
    code manifest resolves the divergence inconsistently per row
  status: closed
  pr: '#1311 (RESOLVED venue A: 24.1.B re-tabled + 24.1.A engine 3->4 at CP v1.117, declared sum 65->68,
    plan re-pin v2.50; breaker growth venue corrected to OD v1.32; hitl third case split out as B-153;
    the summary/close_out above are the ORIGINAL FILING, preserved per closed-row convention)'
  summary: 'WHAT IT IS. 24.1.B (the four F3-capability-floor lifecycle-event-attribute export rows) is declared
    at exactly ONE place in the whole CP spec chain -- Spec_Control_Plane_v1_2.md:2152-2159 -- verified by scanning
    all 115 Spec_Control_Plane_v1_*.md files on disk for the string ''24.1.B'', which matches only that file (:2152
    the sub-heading, :2167 a summary paragraph). Its table pins retry.* at ''4 attributes'' (:2157) and harness.breaker.*
    at ''7 attributes'' (:2159). BOTH figures were superseded at the OTHER venue and never carried across: C-CP-03
    3.5 extended retry.* 4 -> 6 at v1.3 (Spec_Control_Plane_v1_3.md:76, an outright REPLACEMENT of the attribute
    set -- the v1.2 four were retry.attempt / retry.cause / retry.backoff_ms / retry.policy, none of which survive),
    and extended harness.breaker.* 7 -> 9 at v1.32 (B-19-BREAKER-AMBIENT-ATTRS, +cause +cooldown_ms). The v1.3 change-note
    even routes a downstream export update (Spec_Control_Plane_v1_3.md:44 names ''8.4 F2-12 closure -> U-CP-55 24.4
    export manifest update'') but never names 24.1''s own sub-table. WHY IT IS MORE THAN A STALE COUNT. The code
    manifest resolves the two-venue divergence INCONSISTENTLY, one row each way: cp_namespace_export_manifest.py
    pins retry.* at 4 (tracking the stale 24.1.B row, NOT 3.5) and harness.breaker.* at 9 (tracking 3.5, NOT 24.1.B).
    So the manifest conforms to neither venue as a whole, and its test named test_per_namespace_attribute_count_match_spec
    asserts a mixture. This is the spec-vs-spec ingestion-gap shape B-116-t3 closed for OD 9.2, not code drift:
    each individual code row is byte-exact to SOME cleared contract text. SURFACED at the B-126 build arc, 2026-08-09,
    by direct read; B-126 itself is closed and deliberately did NOT touch these counts.'
  close_out: '(1) RE-VERIFY the single-venue claim at the then-current HEAD by re-scanning every Spec_Control_Plane_v1_*.md
    for ''24.1.B'' -- if a later delta has re-tabled it, the finding is discharged and this row closes on its own
    falsifier. (2) DECIDE THE VENUE, and note this is a genuine fork, not a lookup: amending 24.1.B to match 3.5
    (retry 4 -> 6, breaker 7 -> 9) bumps the declared export sum 65 -> 67 and moves what CP claims to export for
    ADR-D6 1.2 ingestion, which is an OD-facing count change; leaving 24.1.B as-is means the 24.1 table is knowingly
    a historical snapshot and should SAY so. Do not pick silently. (3) NOTE THE ASYMMETRY when pricing: the retry.*
    row''s 4 refers to a RETIRED attribute set (all four names were replaced at v1.3), whereas the harness.breaker.*
    row''s 7 is a proper subset of the current 9 -- the two rows are stale in different ways and may not want the
    same remedy. (4) The 65-vs-63 arithmetic that used to be carried in the manifest''s own comments is ALREADY
    FIXED at the B-126 arc; do not re-open that as part of this row. (5) DO NOT change any attribute_count as a
    drive-by: the sum feeds acceptance #6 and its test, so a count edit is a contract-count change needing the venue
    decision at (2) first. WHAT WOULD FALSIFY IT. A later CP spec delta re-tabling 24.1.B (see step 1). Or a declaration,
    somewhere in the chain, that 24.1''s counts are deliberately v1.2-frozen historical figures rather than live
    export claims -- which would make the code manifest''s harness.breaker.* 9 the nonconformant row instead. THIRD SAME-CLASS CASE, ADDED 2026-08-09 (merge-gate lens 2, non-blocking at the B-126 arc): the divergence is NOT confined to 24.1.B. Spec_Control_Plane_v1_2.md:2148 gives hitl.* NO attribute count at all -- it reads ''per-event attributes across 4 span names'' -- while cp_namespace_export_manifest.py pins attribute_count=4 for it, i.e. the code counts SPAN NAMES as attributes, and that 4 feeds both the 24.1.A subtotal of 34 and the 65 sum. Pre-existing, untouched by #1289, and it makes nothing this row says false (the row claims nothing about 24.1.A). But step (2)''s pricing of ''bumps the sum 65 -> 67'' covers only the two 24.1.B rows, so a closer acting on that figure would not learn of a third, structurally different case whose repair is not a count bump at all but a decision about what the column MEANS.'
  council: Conditionally yes at step (2). The venue decision trades OD-facing export-count stability and the acceptance-#6
    gate's meaning (C2/C6) against a contract table that knowingly misreports what CP exports (C7). Do not convene
    before step (1)'s re-verification, per the workspace probe-first discipline.
  heading: '### B-144 · C-CP-24 §24.1.B''s `Attribute count` column was never re-tabled after C-CP-03 §3.5''s two
    amendments, and the code manifest resolves the divergence inconsistently per row *(surfaced at the B-126 build
    arc, 2026-08-09)*'
- id: B-145
  title: retry.backoff_ms + retry.cause_class have zero producers, and the mandated retry.terminal = escalate value
    is never emitted -- collapsed into max-attempts
  status: closed
  summary: 'WHAT IT IS. TWO DISTINCT GAPS AT ONE ATTRIBUTE FAMILY, both against normative Runtime step text rather
    than a discretion bullet. GAP 1 -- TWO KEYS WITH ZERO PRODUCERS. Spec_Harness_Runtime_v1.md:4229 says ''annotate
    inner span with retry.terminal = "retry" + retry.backoff_ms'' and :5685 says the same for the tool path; :4228
    says ''annotate inner span with retry.terminal = "fail-fast" + retry.cause_class = "{class_name}"'' and :5686
    says the same for the tool path. An emission-position sweep across all seven harness-*/src trees returns ZERO
    sites for either name; before this row''s own register landed, the bare string did not appear in src at all
    (checked for f-string / concatenated construction too -- none). GAP 2 -- A MANDATED retry.terminal VALUE THAT
    IS NEVER EMITTED, AND A COLLAPSE THAT MISATTRIBUTES TWO DISPOSITIONS ONTO ONE VALUE. Each path mandates FIVE
    values, not four: success / fail-fast / retry / max-attempts / escalate -- the fifth at :4229 (''If staircase
    result escalates beyond RETRY_WITH_BACKOFF (e.g., CROSS_FAMILY_FALLBACK / LOCAL_TERMINAL / HITL_ESCALATION):
    annotate inner span with retry.terminal = "escalate"'') and at :5685 (same clause, LOCAL_TERMINAL / HITL_ESCALATION).
    THE TOOL PATH CARRIES FOUR OF FIVE: retry_breaker_tool.py emits success / retry / fail-fast / max-attempts (plus
    a non-mandated ''audit-signing-fail-closed''), and NEVER emits ''escalate'' -- its else branch at :275-286,
    whose OWN COMMENT reads ''Exhaustion: either last attempt or staircase escalation past STAGE_2'', stamps ''max-attempts''
    for BOTH, so an escalation is indistinguishable on the wire from a retry-budget exhaustion. That is the same
    misattribution class B-127 + B-131 just closed at the sibling skip call site. THE DISPATCH PATH CARRIES ZERO
    OF FIVE: retry_breaker_fallback.py sets retry.terminal at exactly ONE site (:1294, value ''audit-signing-fail-closed'',
    which §14.6 does not name), DESPITE having the staircase branch the values belong to at :1364-:1383. SURFACED
    at the B-126 build arc, 2026-08-09. GAP 2''s five-value inventory and the tool-path non-conformance were supplied
    by OUT-OF-FAMILY REVIEW ROUND 1, which caught this row''s first filing claiming FOUR values and calling the
    tool path conformant -- the author''s spec read had been truncated at exactly the escalate clause. The two GAP-1
    keys are carried as emitted=False in RETRY_WIRE_REGISTER with a test pinning that set exactly; GAP 2 is value-level,
    not expressible at key-level granularity, and is recorded here only.'
  close_out: '(1) RE-GROUND by re-reading :4227-:4230 + :5684-:5687 at the then-current HEAD (the Runtime spec is
    a delta chain and these offsets move) and re-running the sweep. USE THE EMISSION-POSITION SWEEP, NOT A BARE
    LITERAL SWEEP -- this is load-bearing and was an out-of-family round-1 catch against this row''s own first filing:
    since the B-126 register declares both GAP-1 names as data, a literal sweep over harness-*/src now NECESSARILY
    returns nonzero, so a closer running the obvious probe would discharge the row without wiring anything. The
    correct probe is the one at harness-cp/tests/test_b126_retry_wire_register.py -- set_attribute first-argument
    plus attributes-dict key position -- or a literal sweep that explicitly excludes the declaration home. (2) SEPARATE
    THE TWO GAPS BEFORE PRICING. They sit at different granularities (key-level vs value-level) and may not want
    one commit. (3) FOR GAP 1, the derivation is NOT free: retry.backoff_ms wants the jittered delay already computed
    at the retry branch, so it is nearly free THERE, while retry.cause_class wants a class name at the fail-fast
    branch -- CHECK FIRST whether that duplicates retry.fail_class, which IS emitted at both producers; if it does,
    the honest disposition may be a spec correction retiring the redundant attribute rather than an emission. DO
    NOT assume both keys are wanted just because both are written. (4) FOR GAP 2, treat B-127/B-131''s lesson as
    binding: the collapse of escalation into ''max-attempts'' is precisely a hard-coded literal misattributing a
    cause, so any fix must pass the disposition through and carry a per-branch witness; and note the dispatch path
    needs all five values wired, not one added. (5) UPDATE RETRY_WIRE_REGISTER''s emitted flags in the SAME commit
    as any GAP-1 emission -- test_unemitted_keys_are_exactly_the_two_b145_registers exists to force that edit. WHAT
    WOULD FALSIFY IT. A later Runtime delta demoting these attributes or values to discretion, or deleting them
    from the step bullets -- in which case the register rows should be dropped, not wired. Or an emission site on
    a path the sweep missed; the sweep covered harness-*/src only.'
  council: No. Conformance to already-cleared normative step text with a named owner, and the only judgement --
    step (3)'s question of whether retry.cause_class duplicates the emitted retry.fail_class -- is answered by reading
    the two contracts, not by deliberation.
  grounded_2026_08_11: 'SPLIT DISPOSITION (loop grounding + codex round-1 corrections; full doc at
    .harness/b145-grounding-split-2026-08-11.md). GAP 1 keys are STALE PRE-RENAME ALIASES, not missing
    derivations: retry.backoff_ms is the v1.2-era name the CP §3.5 v1.3 amendment RENAMED to retry.delay_ms
    (Spec_Control_Plane_v1_3.md:76 says so verbatim), and the same Runtime sections that carry the stale
    bullets also bind the §3.5 names at :4225/:5682 (rows repeated at Spec_Harness_Runtime_v1.md:2715-2716);
    both producers already emit the §3.5 names (retry_breaker_tool.py:263/:292; fallback staircase region).
    GAP 1 routes as a SPEC LEG (align the four bullet mentions to the post-rename names + re-disposition the
    two emitted=False RETRY_WIRE_REGISTER rows + pinning test; X-AL-3). GAP 2a (tool path escalate value):
    UNREACHABLE BY CONSTRUCTION — the tool dispatcher hard-codes STAGE_1 on every advance_staircase call
    (retry_breaker_tool.py:252-256) and (STAGE_1, TRANSIENT_RETRY) maps unconditionally to STAGE_2
    (validator_fail_transient_staircase.py:114-119, stage-keyed, attempt ignored), so the collapsed else is
    genuinely last-attempt-only today; routes as a control-flow/spec leg (thread the stage across attempts
    OR ratify the carve-out), NOT a telemetry stamp. GAP 2b (dispatch path zero-of-five) stays the impl
    repair, with per-branch staircase reachability re-grounded before wiring.'
  build_state_2026_08_11: 'GAP-2b LANDED at PR #1306 (five retry.terminal values wired on the dispatch
    path; escalate branch verified unreachable-by-construction with a branch-contract witness, discrepancy
    routed to the GAP-2a leg). GAP-1 SPEC LEG LANDED at the Runtime v1.115 delta (this arc): four bullet
    mentions aligned to the CP 3.5 names + the two emitted=False RETRY_WIRE_REGISTER alias rows removed with
    the pinning test re-pinned to the empty set in the same commit; clearance
    spec-harness-runtime-v1-115-cleared-2026-08-11.md. GAP-2a CLOSED at the Runtime v1.116 delta
    (PR #1309, 2026-08-11): the phantom attempt-indexed advance_staircase
    signature in the two escalate bullets corrected to the landed stage-keyed API consulted
    stage-fresh as a cause-class classifier; retry.terminal = escalate ratified a DEFENSIVE
    BRANCH CONTRACT (unreachable-by-construction at the v1.116 classifiers, witness-pinned on
    both composers -- tool-path collapsed else split into max-attempts vs defensive escalate
    with branch-contract + stage-fresh-lookup witnesses); stage-threading REJECTED (operator
    max_attempts dead-letter; CROSS_FAMILY_FALLBACK void on the candidate-less tool path;
    escalation already structural) and declared a future design arc; /resolve codex=B
    advisor=B agreement; fork doc class_1_fork_b145_gap2a_escalate_clause_phantom_api.md;
    clearance spec-harness-runtime-v1-116-cleared-2026-08-11.md. ALL THREE B-145 LEGS CLOSED
    (GAP-1 #1308 / GAP-2b #1306 / GAP-2a #1309); row CLOSES.'
  pr: '#1309 (the B-145 GAP-2a carve-out; GAP-1 at #1308; GAP-2b at #1306)'
  heading: '### B-145 · `retry.backoff_ms` + `retry.cause_class` have **zero** producers, and the mandated `retry.terminal
    = "escalate"` value is never emitted — collapsed into `max-attempts` *(surfaced at the B-126 build arc, 2026-08-09;
    Gap 2 from out-of-family review round 1)*'
- id: B-146
  title: The retry.* static drift catcher cannot guarantee that no undeclared key hides -- the enumerated escape
    shapes, recorded rather than chased
  status: registered_finding
  summary: 'WHAT IT IS. The B-126 arc shipped a source-sweep drift catcher for the retry.* wire surface (harness-cp/tests/test_b126_retry_wire_register.py).
    Its header originally claimed ''an undeclared key cannot hide''. THAT CLAIM IS FALSIFIED AND HAS BEEN RETRACTED
    IN PLACE, by execution rather than argument, at two independent reviewers: (a) out-of-family round 7 built a
    key as NAMESPACE = ''retry'' followed by NAMESPACE + ''.'' + suffix, where NO fragment contains the literal
    ''retry.'', and the whole suite stayed green; (b) merge-gate lens 3 declared a key constant IN THE DECLARATION
    HOME and emitted it from a producer module, wiring an emitted=False key with all tests green, because the literal
    then lives only in the one module the liveness check excludes. WHY THIS IS A ROW AND NOT A FIX. Seven consecutive
    review rounds each closed one or two indirection shapes and each surfaced more: a regex that credited declarations,
    an alias arm that credited any call argument, a rebound alias crediting a stale literal, a dict write with no
    telemetry sink, split-literal composition, f-string interpolation, join/format/%-formatting. That is the non-convergent
    adversarial-hardening signature, and the workspace''s own discipline says re-scope rather than chase. The arc
    therefore CLOSED the shapes that were cheap and measured, and RECORDED the boundary here. THE ENUMERATED RESIDUALS
    AT THE ARC''S CLOSE. (1) A fragment held as a class attribute, imported from another module, accumulated with
    +=, or routed through functools.partial escapes the composition refusal. (2) A key constant declared in the
    declaration home and imported by a producer escapes every arm, including the liveness check by construction.
    (3) A forwarding helper reached as a METHOD (self._set(...)), a helper-to-helper chain, and a key passed to
    a helper BY KEYWORD are not resolved. (4) An inline attributes={...} at a NON-emitting callee is still credited
    as an emission -- the same class as the defect the arc fixed, narrowed but not eliminated; reachability is low
    because the only two in-tree non-emitter attributes= sites pass a parameter rather than an inline dict. A MEASURED
    BOUNDARY, NOT A GUESS: widening the composition net to any string merely CONTAINING ''retry'' was measured at
    HEAD and flags TWELVE sites, all log and error messages, so the net is drawn at the bare namespace plus the
    dotted prefix, which flags ZERO. SURFACED at #1289 across out-of-family rounds 2/3/5/6/7 and merge-gate lens
    3.'
  close_out: '(1) DO NOT reflexively chase another shape. Re-read the arms-race note above first: the value of this
    row is the recorded boundary, and a shape-by-shape campaign is what it exists to stop. Only act when a PRODUCER
    actually adopts one of the escape shapes, which makes the hole concrete rather than hypothetical. (2) IF a producer
    does adopt one, prefer FAIL-CLOSED over resolution, per the precedent already in the file: refuse the shape
    and require the key be written whole, rather than teaching the resolver to evaluate it. Resolution is unbounded
    work; refusal is a one-line net plus a witness. (3) THE ONE RESIDUAL WORTH PRE-EMPTIVE THOUGHT is (2) in the
    list above -- a key constant declared in the declaration home and imported. It defeats the liveness check BY
    CONSTRUCTION, so it cannot be closed by widening a net; it needs either a rule that the home declares no emittable
    key constants (cheap, enforceable, and true today) or cross-module import resolution (expensive). Prefer the
    former. (4) RE-MEASURE BEFORE WIDENING ANY NET. The twelve-versus-zero figure above is the reason the boundary
    sits where it does; a future widening that skips that measurement will re-introduce false positives into a blocking
    test. (5) NOTE THE COST BUDGET: FOUR checks share one cached parse of ~398 modules (~5s). A FIFTH full-tree
    pass, or any per-shape scan added outside the shared cache, re-introduces the cost that out-of-family round
    6 already flagged once. WHAT WOULD FALSIFY IT. A demonstration that the enumerated residuals are all unreachable
    in this codebase''s conventions -- which would make the row dormant rather than wrong. Or the opposite: a producer
    landing one of the shapes, which promotes the specific shape to a build item and leaves the rest of the row
    standing. RESIDUAL (5), ADDED 2026-08-09 (merge-gate lens 3, non-blocking): the rebind invalidation that stops a stale alias being credited keys on ast.Assign only, so a name rebound by += (AugAssign), an annotated assignment, a tuple-unpack, a for target, a walrus, or with...as is NOT invalidated and its old literal is still credited. Reported here for completeness rather than as a hole, because the escape is structurally unreachable where it would matter: every in-tree attribute-key constant follows the UPPERCASE convention, and lens 3 confirmed by running the repo''s own pyright config that AugAssign / tuple-unpack / for-target rebinds of such a name produce THREE reportConstantRedefinition errors in a BLOCKING CI lane. If that convention ever relaxes, this residual becomes live and the cheap fix is to widen the invalidation to every binding form. ALSO NOTE, same source: an AnnAssign constant (X: str = ''retry.x'') is not registered as an alias at all, so it fails in the UNDER-count direction -- loud, not silent. RESIDUAL (6), ADDED 2026-08-09 (merge-gate lens 3, non-blocking): the ATTRIBUTES-MAP alias arm invalidates only on rebinding to another dict literal, whereas the scalar arm invalidates on rebinding to ANY non-literal. So ATTRS = {''retry.backoff_ms'': 1} followed by ATTRS = compute() and then attributes=ATTRS still credits the stale key, where the scalar analogue correctly credits nothing. The in-code comment calling the map fix ''the map-shaped sibling'' of the scalar one therefore OVERSTATES the parity, and that is recorded here rather than silently corrected. Pre-existing in substance (the earlier setdefault/update form purged neither channel) and ZERO live sites -- every **-unpack dict literal at HEAD carries no retry.* key and none targets a sink name -- so the record-don''t-chase discipline applies. Also from the same source: a mapping rebuilt as ATTRS = {**ATTRS, ...} now credits only the new keys.'
  council: No. A recorded limit of a test-side static analysis, with a named owner and no contract surface. The
    only judgement -- whether to chase shapes or record the boundary -- was answered by the arms-race discipline
    the workspace already holds, and by measurement (twelve false positives versus zero).
  heading: '### B-146 · The `retry.*` static drift catcher **cannot** guarantee that no undeclared key hides — the
    enumerated escape shapes, recorded rather than chased *(surfaced across out-of-family rounds 2/3/5/6/7 + merge-gate
    lens 3 at #1289, 2026-08-09)*'
- id: B-147
  title: shutdown can hang before tracer close when force_flush waits in the saturated default executor queue
  status: closed
  pr: '#1303'
  summary: 'WHAT IT IS. shutdown() bounds dispatch drain before observability flush, but flush_observability()
    submits tracer force_flush through asyncio.to_thread without an outer wait_for covering default-executor queue
    delay. A timed-out workflow worker can still occupy that same loop default executor when api.run() enters shutdown.
    With a one-worker executor saturated, force_flush queues without starting, the top-level deadline expires without
    being observed, tracer close is never reached, and the caller receives no ShutdownReport. Reproduced at PR #1290
    merge-gate Lens 1 with a real saturated executor: the outer positive-control probe printed OUTER_TIMEOUT_BEFORE_REPORT.
    This is production behavior discovered while hardening B-143 tests; B-143 remains test-only and does not absorb it.'
  close_out: '(1) Reproduce through the shipped api.run/MCP call chain, not only a constructed HarnessContext, and
    prove the timed-out CP worker and shutdown share the same loop default executor. (2) Bound force_flush submission
    plus execution by the remaining shutdown budget; do not rely only on the OTel timeout_millis argument, which begins
    mattering only after the worker starts. (3) Preserve per-surface failure isolation and decide the exact failure tag
    when queue delay exhausts the budget. (4) Add a real-executor witness that does not stub away an upstream stage,
    plus a wrapper-removal mutation proving the witness fails. (5) Sweep the later flush fsync offloads for the same
    queue-delay venue before closing. WHAT WOULD FALSIFY IT. A shipped call-chain trace showing the workflow worker
    cannot still occupy the same default executor when shutdown begins, or an existing outer deadline around the
    force_flush to_thread submission that the merge-gate probe bypassed.'
  council: No. This is a concrete bounded-shutdown conformance defect with a reproduced venue and a local mechanical
    repair; escalate only if the failure-tag contract requires a new public surface.
  heading: '### B-147 · `shutdown()` can hang before tracer close when `force_flush` waits in the saturated default-executor
    queue *(surfaced by PR #1290 merge-gate Lens 1, 2026-08-09)*'
- id: B-148
  title: R-CTX-1 context-optimization program — approved execution of Arcs 0–7, Waves 0–5 (Arc 8 parked at B-149)
  status: open
  summary: 'WHAT IT IS. The context-preload optimization program (R-CTX-1): baseline ~98–100k tokens
    preloaded per session, reconciled Floor-B target ~71k (−28%), acceptance gate ≤76k measured. Plan
    authored via a 3-model adversarial pipeline (Codex gpt-5.6-sol × Claude Opus 5 independent reviews
    → 10-item deliberation → Fable 5 reconciliation); reconciled verdict BLOCK-as-written → APPROVE-WITH-CONDITIONS
    on a re-scoped plan of 9 arcs of which 8 ARE EXECUTABLE (Arcs 0–7; Arc 8 / Floor C is PARKED at
    sibling row B-149 and NOT authorized — this open row''s scope is Arcs 0–7 only). OPERATOR RATIFICATIONS (2026-08-10, verbatim from the approved plan):
    R1 — "Arc 2''s three rules (roadmap_status truncation + archive + query-not-Read for the .harness
    working set)". R2 — "Floor B is TERMINAL. Arc 8 / Floor C parked in forward register with D-H probe
    preconditions. Do not run probes." R3 — "12 machine-local bmad payloads: move ignore entries `.git/info/exclude`
    → `.gitignore` + documented reversible local removal." Earlier ratifications this program: D1 runner-neutral
    docs/governance/; D2 design-skills + bmad removal both runners; D3 per-repo skill disconnect preserving
    other repos + Codex superpowers/CodeRabbit intact. Operator approved "execute all waves" 2026-08-10.
    EXECUTION DOC: .harness/r-ctx-1-implementation-plan-v1.md (repo-committed at this row''s own PR;
    the gstack checkpoint copy is the advisory origin per AGENTS.md cross-runner-state discipline) —
    units U-CTX-00..22, wave/gate structure, routing law, sub-agent brief contract. Arc 8 / Floor C is
    the SIBLING held row B-149 — not part of this open scope.'
  close_out: 'Program AC (U-CTX-21/22): measured cold-start preload ≤ 76k (gate; target ~71k) via `just
    context-budget` first-turn metric, AND a post-compaction measurement via a TESTED compaction-generation
    selector in the tool (first assistant call after each compaction boundary) or a separate documented
    post-compaction acceptance procedure — the first-turn metric alone CANNOT satisfy the post-compaction
    half (a post-compaction request is not the session''s first turn; plan errata E4); component-separated
    A/B for both; all new CI gates green on main; `git log --stat` audit shows ZERO design-substrate/**
    diffs across all program PRs; register flip to closed + memory entries + final terminating refresh
    at U-CTX-22.'
  council: 'No. The 3-model reconciliation pipeline (Codex × Opus × Fable reconciler) already served
    as the decorrelated deliberation for this program, and the operator ratified R1–R3 + all-waves execution
    2026-08-10 — no further multi-voice fork remains open.'
  build_state_2026_08_11: 'ALL BUILD ARCS LANDED — PRs #1293/#1294/#1295/#1296/#1297/#1298/#1299/#1300/#1301
    merged with terminating refreshes; zero design-substrate/** paths across all nine PRs (verified by
    per-PR file enumeration 2026-08-11). Root CLAUDE.md 123,442 → 44,853 B; axis CLAUDE.mds −73,509 B;
    roadmap_status head ≤25,600 B gated; E4 post-compaction selector TESTED (#1297, 32 witnesses).
    REMAINING for close: the ≤76k cold-start gate is NOT yet conclusively measured in the eligible
    */cli cohort — every eligible session predates the slim merges (pre-slim first turns 107,968–131,020).
    A fresh post-merge sdk-cli probe measured 49,324 total (10 in + 31,232 cw + 18,082 cr), well under
    the gate, but sdk-cli loads a lighter stack and is cohort-excluded by design; the arithmetic
    projection from the bg/cli baseline (~108k − ~21k root-slim) sits near ~87k. The two bracket the
    gate, so the row stays OPEN pending the first post-merge eligible cli session: run `just
    context-budget --sessions 1` after the operator''s next interactive session and flip this row if
    ≤76k; if >76k the remaining mass is the measured component split (system prompt + tools + memory),
    routed back to the held B-149 probes for an operator decision. Lean review protocol (operator
    cost directive 2026-08-11) recorded at PRs #1298–#1301: codex 1-round caps + single/skipped
    merge-gate lenses with residual classes in PR bodies. FIRST ELIGIBLE MEASUREMENT 2026-08-11: post-slim bg/cli session 06fb5dd9 first turn = 76,656 (gate 76,000; miss 656 tokens / 0.86%, n=1). NOT routed to B-149: the miss equals the removable ~650-token mass of the 4 design-skill dirs still on disk (the E7 machine-local half, owed since PR #1299; the reversible mv was classifier-blocked for the agent) — operator command + on-disk 21-name verification at .harness/r-ctx-1-e7-machine-local-orchestrator-step.md; full U-CTX-21 record (component split, pre-slim baseline median 108,544 = program delta -29.4%, post-compaction E4 instrument live but no post-slim boundary yet) at .harness/r-ctx-1-u-ctx-21-measurement-2026-08-11.md. Verdict PENDING-REMEASURE after the E7 move; if the re-measure still exceeds 76k the residual component split routes to the held B-149 probes per this row''s close-out note.'
  heading: '### B-148 · R-CTX-1 context-optimization program — approved execution, Arcs 0–7 / Waves 0–5 *(operator-ratified 2026-08-10; 3-model reconciliation; Arc 8 parked at B-149)*'
- id: B-149
  title: R-CTX-1 Arc 8 / Floor C conditional-pack redesign — PARKED (Floor B terminal per operator R2)
  status: held
  summary: 'OPERATOR R2 RATIFICATION (2026-08-10, verbatim): "Floor B is TERMINAL. Arc 8 / Floor C parked
    in forward register with D-H probe preconditions. Do not run probes." D-H PROBE PRECONDITIONS (carried
    verbatim from the reconciliation''s Codex round-2 D-H position, the agreed probe sequence gating any
    future un-parking): "split and ship P6a hygiene first; build a shared pack router in observation-only
    mode; prove identical pack IDs for Claude/Codex payload fixtures across Bash, `apply_patch`, Edit/Write,
    MCP, multi-path, rename, and bypass cases; run harmless live PreToolUse probes on both installed hosts
    and inspect transcripts for context-before-side-effect plus deny-and-retry behavior; test missing/malformed
    packs, timeout, missing dependencies, and untrusted hooks; verify startup/resume/clear/manual-compact/auto-compact
    with `(session_id, compaction_generation)` reinjection; then shadow-log real sessions before moving
    one reference pack at a time. `SubagentStart` is not categorically unavailable, but editing the existing
    emitter would break its byte-exact fixture (tools/hooks/test_subagent_validate.sh:98); a separate handler
    still needs live aggregation and ordering proof. If PreToolUse transport, coverage, failure closure,
    parity, or compaction reinjection fails, Floor B is terminal and no load-bearing rule moves." The safety
    kernel (parent delegation, posture/X-AL-3, paid/secret/destructive boundaries, recovery, PR/CI fixed-point
    rules) is NEVER a conditional-pack candidate. Held = ratified operator DEFER — the probes are NOT to
    be run; this row un-parks only on a fresh explicit operator decision.'
  heading: '### B-149 · R-CTX-1 Arc 8 / Floor C conditional-pack redesign — PARKED, Floor B terminal *(operator R2, 2026-08-10; D-H probe preconditions carried)*'
- id: B-150
  title: deferred-flush aftermath surfaces — collector stopped before a timed-out flush completes; standalone flush timeout leaves the SDK atexit racing the worker
  status: closed
  summary: 'WHAT IT IS. Two aftermath surfaces of the B-147 bounded-flush machinery (PR #1303), surfaced by the
    codex delta round AFTER the 10-round loop closed and deliberately REGISTERED rather than absorbed (the
    #1290 precedent). (1) COLLECTOR ORDERING (codex P1): C-RT-10 orders collector stop (step 3a) before the
    tracer close (3b); a flush whose worker timed out therefore can complete its export AFTER
    _close_collector_daemon() ran, so the late spans land on a stopped collector — flush-start ->
    collector-stop -> flush-end. This is inherent to the spec step order under ANY bounded shutdown (the
    pre-B-147 behavior on this venue was an unbounded hang with no collector semantics at all, and the flush
    is already reported failed in the ShutdownReport), but the deferred-close machinery now lets the late
    completion happen where it previously hung, making the window observable. Candidate repair: extend the
    B-147 in-flight condition to the collector surface (defer step 3a like 3b) — a REAL C-RT-10 sequencing
    question, possibly spec-touching. (2) STANDALONE-FLUSH ATEXIT (codex P2): the ordered-backstop swap arms
    only in _spawn_deferred_tracer_close (the shutdown path); a STANDALONE flush_observability() timeout
    followed by process exit without shutdown(ctx) leaves the SDK shutdown_on_exit handler racing the live
    worker (subprocess probe: atexit_shutdown_while_flush_active=True). The shipped call graph has exactly
    one flush caller — shutdown() itself (shutdown.py, verified PR #1303 round 9) — so this is reachable only
    by a future external caller of the public primitive.'
  close_out: '(GROUNDED 2026-08-11 — the decide/check halves of the original steps are ANSWERED; see
    grounded_2026_08_11. What remains:) (1) COLLECTOR HALF — confirmed spec-touching: author the C-RT-10
    spec leg FIRST (either an in-flight-conditional step-3a deferral mirroring 3b, or an explicit
    ratified-cost carve-out), with clearance marker, THEN the impl if the deferral reading is chosen.
    (2) ATEXIT HALF — confirmed warranted (SDK does not serialize): arm the ordered atexit replacement on
    the flush-surface timeout path itself, witnessed with the subprocess-probe shape; impl-only, no spec
    touch. (3) Re-run the #1303 witness set green after either change. Original falsifiers: both checked
    2026-08-11, neither fired (C-RT-10 order unconditional at :3718-3733; OTel 1.41.1 BatchProcessor
    closes the exporter outside _export_lock with an admitted in-flight-export window).'
  council: No. Bounded sequencing repairs inside one axis with a named owner; the only judgement is close_out
    step (1), which grounding against the C-RT-10 text answers.
  grounded_2026_08_11: 'BOTH FALSIFIERS CHECKED AT HEAD — NEITHER FIRES; the row stands as filed. (1)
    C-RT-10 read in full (design-substrate/Spec_Harness_Runtime_v1.md §10, :3718-3733): the sequence is
    unconditional — step 2 flush, then step 3 closes in fixed reverse order with "Stop collector daemon
    (structured-stop per C-RT-07)" BEFORE "await tracer_provider.shutdown()", with no conditionality of the
    collector stop on flush completion anywhere in the section. Any in-flight-conditional deferral of step
    3a is therefore a spec-behavior change: the collector half is CONFIRMED spec-touching and routes as a
    spec leg per X-AL-3 before building. (2) Pinned OTel SDK 1.41.1 read at the installed source
    (opentelemetry/sdk/_shared_internal/__init__.py:224-258): BatchProcessor.shutdown() sets _shutdown,
    joins the worker bounded, then calls exporter.shutdown() DIRECTLY — not under _export_lock — and its
    own comment admits an in-flight export may still be running ("we will return instead of waiting");
    force_flush checks _shutdown only at entry then blocks in _export. The SDK does NOT internally
    serialize shutdown against an in-flight force_flush, so the standalone-flush atexit-arming half stays
    warranted for any future external caller of the public primitive.'
  closed_2026_08_11: 'BOTH HALVES CLOSED. Atexit half at PR #1307 (impl-only; ordered backstop armed on
    the standalone flush-timeout path). Collector half at the Runtime v1.117 spec leg (this PR): C-RT-10
    step-3a gains the in-flight-conditional deferral mirroring 3b (deferred watcher runs 3a->3b in order;
    between-3a-and-3b worker finish completes the stop inline with the tag withdrawn); deferred-close
    discipline ratified in contract text (ordered atexit backstop, loop-bound stop with closed-loop treated
    terminated, timeout-bound exclusion, narrows-not-eliminates honesty); both step-6 invariants qualified
    deferred-path-only. Impl companion same-PR: 3a mirror condition + inline race completion + watcher-chain
    collector stop with loop-dead guard; 3 witnesses, 2 mutation probes RED (probe 2 caught a vacuous
    first-draft race witness -- fixed to frame-scoped scripting), #1303 witness set re-run 84/84 per
    close_out step 3. /resolve advisor=A (3 corrections absorbed) + codex unparseable-recorded. Fork doc
    class_1_fork_b150_collector_stop_ordering_deferral.md; clearance
    spec-harness-runtime-v1-117-cleared-2026-08-11.md.'
  pr: '#1310 (the B-150 collector-half spec+impl leg; atexit half at #1307)'
  heading: '### B-150 · deferred-flush aftermath — collector-stop ordering + standalone-flush atexit arming *(surfaced by PR #1303 codex delta round post-cap, 2026-08-11)*'
- id: B-152
  title: B-150 deferral-unavailable residuals -- stalled-loop stop can land after tracer close; watcher
    start-failure orphans the collector in a long-lived process
  status: closed
  summary: 'TWO residuals of the B-150 collector-half deferral (PR #1310), surfaced by out-of-family codex
    round 2 and independently adjudicated BENIGN-DIRECTION/BOUNDED by merge-gate lens 1 on the same PR --
    the weight disagreement (codex P1/P2 vs lens-1 non-blocking) is recorded per the disagreement-is-signal
    discipline. (1) STALLED-LOOP LATE STOP (codex P1; shutdown.py _deferred_collector_stop): when the
    harness loop is stopped-but-open the guard treats the collector terminated and the tracer closes; a loop
    that later RESUMES leaves the daemon task alive (collector outlives, tracer-before-collector inversion),
    and a loop stalled >10s can run the queued stop AFTER the tracer close. Lens-1 adjudication: the
    inversion is in the HARMLESS direction relative to the B-150 hazard (collector stopping LATE cannot lose
    late-export spans; stopping EARLY was the defect) and the bound is declared best-effort by the v1.117
    deferred-close paragraph. Absorbing it would require proof-of-termination on a loop that cannot execute
    -- an unbounded wait, the exact class B-147 removed. (2) WATCHER START-FAILURE ORPHAN (codex P2): if
    Thread.start() fails after the collector handoff (thread exhaustion at shutdown), nothing ever stops the
    collector in a long-lived process (the ordered backstop is tracer-only, exit-only). Lens-1: bounded
    resource leak (one health-check task), already reported in ShutdownReport failures. An on-loop fallback
    watcher is a second ownership mechanism -- design work, not a patch.'
  close_out: '(1) Ground whether any production path can hold the harness loop stopped-but-open across a
    shutdown with a lingering flush worker (the daemon-thread-loop deployment lens-1 named) -- if none, (1)
    is vacuous and closes as a declared boundary. (2) For the start-failure orphan, price an on-loop
    fallback task (await workers then stop collector) vs a boundary declaration; either lands as a small
    Runtime delta per X-AL-3 if spec-touching. (3) Both fixes, if built, need witnesses per the #1310
    subprocess/ordering shapes. WHAT WOULD FALSIFY IT: a Runtime delta retiring the deferred chain entirely,
    or the live OTLP receiver arc restructuring collector ownership -- re-ground there first.'
  council: No. Bounded sequencing residuals inside one axis with a named owner; the only judgement is the
    benign-direction adjudication, already made by two decorrelated reviewers on the filing PR.
  pr: '#1313 (close; registered at #1310)'
  heading: '### B-152 · B-150 deferral-unavailable residuals -- stalled-loop late stop + watcher
    start-failure orphan *(surfaced at PR #1310 codex round 2, 2026-08-11)*'
- id: B-153
  title: C-CP-24 24.1 Attribute-count column semantics undefined at the hitl.* row -- code counts span
    names as attributes
  status: closed
  summary: 'THE THIRD SAME-CLASS CASE split out of B-144 at its venue-A close (PR #1311): the hitl.* row
    of 24.1.A (Spec_Control_Plane_v1_2.md:2148) carries NO attribute count at all -- it reads ''per-event
    attributes across 4 span names'' -- while cp_namespace_export_manifest.py pins attribute_count=4,
    i.e. the code counts SPAN NAMES as attributes. That 4 feeds the 24.1.A subtotal (35 since CP v1.117,
    which also carried engine.* 3 -> 4 per C-CP-09 §9.1 v1.3) and the declared export sum (68). Unlike the two B-144 rows this is not a stale count with a live
    figure waiting at another venue -- no venue declares ANY hitl.* attribute count -- so the repair is a
    decision about what the Attribute-count COLUMN MEANS, which may ripple to other qualified 24.1.A rows
    (audit.* reads ''7 attributes per persona-tier emission discipline''). CP v1.117 §0.5 explicitly
    declares the re-table does NOT ratify the span-names-as-attributes reading. Adjudication lineage: at
    the B-144 /resolve the scope call SPLIT (codex: fix in-delta; advisor: separate follow-up) and the
    safer surgical option was taken per the resolver split rule (RESOLVE-SPLIT row, 2026-08-11).'
  close_out: '(1) ENUMERATE the actual per-event attribute inventory across the 4 HITL span names from
    C-CP-20 §20.6 at the then-current HEAD -- the column decision needs the real cardinality on the
    table, not just the span-name count. (2) DECIDE THE COLUMN SEMANTICS (a declared live export claim
    in attributes is the B-144 venue-A precedent) and, under that definition, correct the hitl.* cell --
    and audit the audit.* qualifier row under the same definition or record why it already conforms.
    (3) Any resulting count edit is a contract-count change feeding acceptance #6 and its test -- same
    discipline as B-144: spec delta first (X-AL-3 back-flow pairing), manifest + test cascade same-PR.
    (4) GROUNDING LEAD (added at the B-144 OD leg): OD already interprets the count as ATTRIBUTES --
    C-OD-05 §5.1 row 6 (Spec_Operational_Discipline_v1_2.md:334) commits hitl.* at ''11 attributes across
    4 span names'' and the OD map/test ingest 11 -- a live cross-axis counterexample to the CP manifest''s
    span-names-as-attributes 4. (5) WHAT WOULD FALSIFY IT: a CP delta defining the column semantics, or
    one declaring hitl.* exports at span-event granularity deliberately.'
  council: 'No. Single-axis column-semantics decision with the B-144 venue-A precedent on file; the
    U-HK-13 decorrelated resolver suffices at the decision point.'
  pr: '#1314 (close; minted at #1311)'
  heading: '### B-153 · C-CP-24 §24.1 Attribute-count column semantics undefined at hitl.* — span names
    counted as attributes *(split from B-144 at the CP v1.117 re-table, 2026-08-11)*'
- id: B-151
  title: standalone atexit backstop residual concurrency shapes -- re-arm after deferred takeover creates one
    unserialized shutdown-vs-shutdown pair; dead-probe gate overwrite under id reuse
  status: closed
  summary: 'TWO NARROW RESIDUALS of the B-150 atexit half (PR #1307), surfaced by merge-gate lens 1 at that PR
    and deliberately REGISTERED rather than absorbed (the #1290/#1303 precedent; lens verdict was APPROVE with
    register-do-not-block). (1) RE-ARM AFTER DEFERRED TAKEOVER: flush timeout arms the standalone backstop ->
    shutdown(ctx) spawns the deferred close and disarms it -> a LATER standalone flush_observability registers a
    new worker (legal: the _deferred_closing gate is only set when the registry empties) and times out -> the
    arm''s gate check does not detect the in-progress takeover, so a fresh standalone backstop is armed alongside
    the live watcher. If process exit lands in the gap where the last worker just finished but the watcher has
    not yet set the gate, the standalone backstop and the watcher''s _close_once_serialized can run
    provider.shutdown() CONCURRENTLY -- shutdown-vs-shutdown only: whichever closer commits the gate first fences
    any new force_flush, so the export-vs-teardown race B-150 closed is NOT reintroduced, and OTel 1.41.1''s
    TracerProvider.shutdown is idempotent-with-warning on this shape. The one closer pair with no serializer.
    (2) DEAD-PROBE GATE OVERWRITE UNDER ID REUSE: if the armed ctx is gc''d while its worker is wedged and its
    id() is reused by a live ctx, the exit-time backstop treats the new ctx''s gate/entry as stale (weak probe
    returns None) and commits _deferred_closing[ctx_key] with a dead ref -- clobbering the live ctx''s closing
    gate, which _register_inflight_tracer_flush would then pop as stale, re-opening the round-10 registration
    door for the NEW provider mid-close. Requires gc + id reuse + exit concurrent with another thread''s loop;
    the identical overwrite shape PRE-EXISTS in _live_workers_or_gate.'
  close_out: '(1) RE-GROUND both shapes at the then-current HEAD -- the B-150 collector-half spec leg (still
    open) may restructure the close-ownership machinery and moot either. (2) For shape 1, candidate repairs
    named by the lens: the arm refuses while any worker event from a deferred snapshot is outstanding, OR the
    deferred takeover leaves a per-ctx deferred-owns-forever tombstone the arm checks; either wants a
    deterministic interleaving witness (spy-injected, the PR #1307 pattern). (3) For shape 2, candidate repair:
    skip the gate commit when the ctx weak probe returns None (a dead ctx needs no flush fence; registration is
    keyed on a live object) -- and if fixed here, fix the SAME shape in _live_workers_or_gate in the same commit
    or record why not. (4) Severity honest-labeling: both are benign-in-consequence today (idempotent double
    shutdown; astronomically narrow preconditions) -- do not inflate; close as wont-fix-with-rationale is a
    legitimate disposition if re-grounding confirms consequence-free.'
  council: No. Bounded intra-file concurrency repairs with a named owner and the lens''s own candidate fixes on
    file; no cross-domain tension.
  pr: '#1312'
  heading: '### B-151 · standalone atexit backstop residual concurrency shapes *(surfaced by merge-gate lens 1 at PR #1307, 2026-08-11)*'
- id: B-154
  title: Uniform-response target selector keyed by the external correlation token
  status: registered_finding
  summary: 'A properly-specified CP property-6 extension letting an operator target ONE of N
    parked pre-dispatch gate-owning branches, keyed by the B-71 external token on a NEW
    resume-context field -- NEVER hitl_responses (v1.108 section 1.1(b) forbids keying the
    pre-dispatch identity there, and B-71 deliberately makes the external token non-keyable).
    Surfaced at the B-71 council: a stable external identity is exactly the handle such an
    extension would key by, which is why B-71 registers it rather than designing it.'
  close_out: 'Gated on the B-71 spec leg landing (the token must exist and be stable before
    anything can target by it). Then: an observable contract change to a cleared mechanism ->
    its own Class-2 fork + CP spec leg per X-AL-3. Do NOT widen property 1''s key shape.'
  council: 'yes at open -- it re-opens the property-4/6 sole-membership safety invariant that
    B-72 established, so C1 (orchestration) + C10 (action-safety) at minimum.'
  heading: '### B-154 · Uniform-response target selector keyed by the external correlation token
    *(registered at the B-71 council arc, 2026-08-12)*'
- id: B-155
  title: Nothing signals the operator when a parked pre-dispatch branch becomes
    sole-addressable (the original "no webhook re-fires" premise is UNVERIFIED -- see summary)
  status: registered_finding
  summary: 'PREMISE RE-SCOPED 2026-08-12 (B-71 v3 s4-ter.1b): this row was registered as an
    ABSENCE of redelivery, and that premise is UNVERIFIED and may be false -- a LINEAR depth-0
    gate DOES re-escalate via the webhook on re-pause
    (test_hitl_resume_without_resolved_response_repauses, 2 POSTs asserted); whether the FAN-OUT
    pre-dispatch path does the same is UNRUN. What is certainly real is that no POSTURE SIGNAL
    reaches the operator: any re-fired request is byte-identical and cannot say the branch became
    sole. Re-ground with a fan-out driver/composer round-trip at open; re-scope or merge into
    B-71 if the re-fire happens. ORIGINAL REGISTRATION TEXT FOLLOWS. When N-1 peers resolve and the last pre-dispatch gate owner becomes the sole
    unaddressed member, the uniform fallback may deliver on the next resume
    (workflow_driver.py:2851-2898, esp. :2891-2897) -- but NO webhook re-fires to tell the
    operator that the request they were told to wait on is now actionable. Surfaced by C11 at the
    B-71 council (T4) and sharpened by out-of-family codex, which showed the same gap makes a
    STATIC held-for-sole-resolution stamp a liveness failure.'
  close_out: 'Coupled to the B-71 spec leg''s resolvability re-derivation (precondition 3): if
    that precondition is met by routing the operator to the live pause view rather than asserting
    non-actionability, this row stays a UX-latency item; if it is met by a live posture field,
    this row becomes the mechanism that keeps the field honest. Re-ground both before opening. [SUPERSEDED 2026-08-12 -- the fork in the preceding sentence is DECIDED, do not re-open it: B-71 DESIGN RECORD v3 s4-ter.3 chose (b), state-the-rule-promise-no-readout. This row IS the UX-latency item; there is no live posture field to keep honest yet, and the dependency below states what must clear first.] DEPENDENCY ADDED 2026-08-12 (B-71 DESIGN RECORD v3): a live posture/eligibility field is NOT merely unbuilt, it is blocked on two facts this row must answer first. (i) project_pause_locations takes root_snapshot ALONE (pause_state_projection.py:816) and PausedWorkflowState carries no ResumeContext, so per-location eligibility cannot be computed from the published projection without amending a CLEARED contract (CP spec v1.112 s2.1) -- its own spec leg under X-AL-3. (ii) More binding: read_paused_workflow_state is RATIFIED as NOT a liveness claim (Runtime v1.110 s14.14.9.1 / B-104 Reading D Component 1) -- the journal writes no pause-resolved marker, so a resolved pause is byte-indistinguishable from an outstanding one and the read must not be presented to an operator as an outstanding-pause assertion. A per-location eligibility flag on a read with that limit would be exactly the false-positive twin of the held-for-sole-resolution false negative B-71 precondition 3 retired. So this row owns the live-view sub-fork surfaced at B-71 v3 s4-ter.3 option (a), and must clear the B-104 staleness limit before any live posture field is authorized. PREMISE FLAGGED 2026-08-12 (B-71 v3, out-of-family round 11): this row''s own title premise -- NO webhook re-fires -- is UNVERIFIED and may be FALSE. test_hitl_resume_without_resolved_response_repauses (harness-runtime/tests/integration/test_u_rt_95_hitl_resume_consume_cycle_e2e.py:649) shows a resume with an empty ResumeContext falling through to Step 4-bis and RE-ESCALATING via the webhook (2 POSTs asserted) -- for a LINEAR depth-0 gate. Whether the FAN-OUT pre-dispatch branch re-escalates the same way is not shown by that test and was not run at B-71 v3. RE-GROUND BEFORE OPENING: run a fan-out driver/composer round-trip. If the fan-out path does re-fire, this row is NOT an absence-of-redelivery item -- what survives is the weaker claim that the re-fired request is byte-identical and carries no posture signal, which is B-71''s original defect rather than a separate gap, and this row should be re-scoped or merged.'
  council: 'no at registration -- C9 owns the redelivery knob and C11 the operator surface; both
    already deliberated at the B-71 arc. Convene only if a posture-transition webhook is proposed
    (a NEW outward event class, which is C10 territory).'
  heading: '### B-155 · Nothing signals the operator when a parked pre-dispatch branch becomes sole-addressable *(registered
  at the B-71 council arc, 2026-08-12; the original "no webhook re-fires" premise was RE-SCOPED 2026-08-12
  as UNVERIFIED — see the PREMISE FLAGGED bullet)*'
- id: B-156
  title: Uniform-treatment extension of the correlation token to depth-0 root and already-dispatched-child escalations
  status: registered_finding
  summary: 'B-71 populates the external correlation token for fan-out branch escalations ONLY
    (presence/absence is the population discriminator). The depth-0 root escalation and an
    already-dispatched child''s own internal-gate escalation carry no token. Additive and
    non-breaking under B-71''s placement-inclusive basis, but an observable population change.'
  close_out: 'Gated on the B-71 spec leg. The negative rule B-71 makes uniform NOW -- the webhook
    never carries ingress keys -- already holds for these populations, so this is a
    population-widening question, not a safety question.'
  council: 'no -- a scoped population widening under an already-adjudicated rule.'
  heading: '### B-156 · Uniform-treatment extension of the correlation token to depth-0 root and
    already-dispatched-child escalations *(registered at the B-71 council arc, 2026-08-12)*'
- id: B-157
  title: The addressing half of B-71 -- pause-view-side capability for already-dispatched children
  status: registered_finding
  summary: 'B-71''s original narrower half: exposing an already-dispatched child''s real run_id so
    the operator can build a hitl_responses key. The B-71 council answered NO for the WEBHOOK
    channel (a keyable run_id is an addressing CAPABILITY, not advisory context; putting it in
    third-party-visible transit is a spoof/misattribution primitive). What survives is the
    pause-view-side question: the operator-held local surface is where addressable run_ids already
    legitimately live, so the question is whether the projection should surface them more usably.'
  close_out: 'Re-ground against the public projection''s current field set
    (pause_state_projection.py) before opening -- the capability may already be present and the
    row may reduce to a usability item. The webhook answer is SETTLED NO and must not be re-opened
    without a new fork.'
  council: 'conditional -- only if a proposal would move a capability handle onto an outward
    channel, which re-opens the settled C10 answer.'
  heading: '### B-157 · The addressing half of B-71 -- pause-view-side capability for
    already-dispatched children *(registered at the B-71 council arc, 2026-08-12)*'
- id: B-158
  title: entry_version is folded into every step idempotency key but is unguarded across the pause boundary
  status: registered_finding
  summary: 'WHAT IT IS, and it is strictly wider than B-71. run_idempotency_key folds
    entry_version (workflow_driver.py:3312-3316 -> :646-665) and parent_idempotency_key inherits
    it (:668-694), so EVERY step idempotency key in a run depends on the manifest entry_version.
    But entry_version occurs ZERO times in harness-cp/src/harness_cp/pause_resume_protocol_types.py
    (verified, 1515 lines); snapshot_hash covers only (workflow_id + run_id + step_index +
    state_summary) at :929-930, and the resume material-diff guards cover step_id / step_kind /
    child_workflow_id / gate-config, not entry_version. A manifest version bump across a pause
    therefore SILENTLY ROTATES every step idempotency key on resume, with no guard firing.
    Surfaced at the B-71 council (C9 raised the sensitivity, C1 ground it to the zero-occurrence
    fact); out-of-family codex independently showed it leaves a B-71-specific crash window open.'
  close_out: '(1) RE-GROUND at HEAD: re-verify the zero-occurrence claim and the snapshot_hash
    field set. (2) DECIDE whether the correct posture is to GUARD (reject a resume whose
    entry_version differs -- fail-closed, consistent with the other material-diff guards) or to
    EXCLUDE entry_version from the key derivation (changes every key, a migration question), or to
    DECLARE the rotation intentional (it may be: a manifest edit arguably SHOULD invalidate
    idempotency). This is a real fork, not a wiring bug -- do not pick by preference. (3) Note the
    B-71 interaction: whichever way this resolves may dissolve B-71 precondition 4 for free.'
  council: 'conditional -- if the disposition trends toward EXCLUDE (which changes every step key
    in every run) that is a C3 (state/ledger integrity) vs C9 (recovery semantics) tension worth a
    dyad. GUARD or DECLARE are single-domain calls.'
  heading: '### B-158 · entry_version is folded into every step idempotency key but is unguarded
    across the pause boundary *(surfaced at the B-71 council arc, 2026-08-12 -- pre-existing,
    strictly wider than B-71)*'
- id: B-159
  title: Typed ResumeKeyDisposition + a resume-outcome diagnostics carrier for non-honored hitl_responses keys
  status: registered_finding
  summary: 'Today a hitl_responses key that matches nothing addressable is SILENTLY excluded
    (workflow_driver.py:2824-2848, :2885-2894; pause_state_projection.py:338-341) and the operator
    observes only that the run paused again with no explanation. B-71 makes this materially worse
    if unmitigated, because it HANDS the operator an identity they may reasonably try to key with.
    C5 specified the shape: a typed per-key disposition (ADDRESSED / UNKNOWN_KEY /
    NEVER_KEYABLE_INTERNAL_MATCH / CORRELATION_TOKEN_MATCH), with the last two distinct because the
    operator errors differ (a probe/leak vs an invited mistake). C11 ranked the landing sites: the
    resume OUTCOME (primary, mandatory), the pause projection (secondary, mandatory), a report-log
    line (tertiary, never sufficient alone -- solo operators do not tail logs).'
  close_out: 'NAMED COMPANION LEG of the B-71 spec leg, NOT a deferral -- the B-71 design record
    commits to it, and C1 holds the sequencing condition: it ships WITH the B-71 spec leg, or
    C11''s resolvability_note promise that the resume outcome reports a non-attaching response
    must be softened in the same commit. Impl-grounded open item: the exact resume-outcome result
    shape (marked [MEDIUM] by C11 -- not read at the council).'
  council: 'no -- the shape was specified by C5 and the landing sites ranked by C11 at the B-71
    arc; what remains is carrier grounding.'
  heading: '### B-159 · Typed ResumeKeyDisposition + a resume-outcome diagnostics carrier for
    non-honored hitl_responses keys *(registered as a named companion leg at the B-71 council arc,
    2026-08-12)*'
- id: B-160
  title: C-OD-32.3 declares webhook spans head=1.0 always-sampled, but hitl.webhook.deliver
    is absent from the 19-entry ALWAYS_SAMPLED_EVENT_CLASSES
  status: registered_finding
  summary: 'CONTRACT-vs-IMPLEMENTATION DRIFT on a sampling FLOOR, surfaced by the B-71 v4
    leak analysis (DESIGN RECORD s4-quater.2b). OD spec v1.8 §C-OD-32.3 states "Webhook spans
    head=1.0 (always-sampled -- HITL delivery audit-critical)", carried verbatim in
    harness_od/hitl_webhook_namespace.py. §9.2 defines ALWAYS_SAMPLED_EVENT_CLASSES as exactly
    the head=1.0 set, and hitl.webhook.deliver is NOT in its 19 members -- so on any
    deployment_surface != LOCAL_DEVELOPMENT the TailKeepSpanProcessor may drop a span the
    contract declares inviolable. Both halves are pinned by
    harness-runtime/tests/test_b71_escalation_token_rides_the_tracing_export.py so the drift
    cannot be silently closed in either direction. WHY IT MATTERS BEYOND B-71: §9.3 calls the
    always-sampled set inviolable, so this is a floor-conformance question, not a preference --
    and HITL delivery is the audit-critical case the floor exists for.'
  close_out: 'GROUNDED 2026-08-12 (PR #1328) -- the close-out''s own first step is DONE and it RESCOPES
    the row: this is a CLASS, not an instance -- SIX span families across FOUR contracts, in two shapes.
    FOUR unconditional head=1.0 span-name declarations across TWO contracts are absent from the s9.2 floor
    set -- hitl.webhook.deliver + hitl.webhook.attempt (C-OD-32.3) and pause.captured + resume.attempted
    (C-OD-30.3). Witness: harness-od/tests/test_b160_head_one_declarations_vs_always_sampled_set.py (3 tests,
    mutation-probed -- adding the webhook names alone reds it, which is the point: a one-sided edit must
    force re-grounding). NOT the B-153 category mistake: the set''s own members are span/event names of
    the same shape (hitl.gate.evaluated, hitl.invocation.opened, topology.fanout.opened, mcp.tool.call)
    and NO wildcard covers the absentees -- the hitl.invocation.* members are enumerated individually and
    there is no hitl.* or pause.* wildcard, unlike audit.* / validator.fail.*. CONDITIONAL declarations
    are ALSO part of the class (this sentence previously read that they were deliberately EXCLUDED because
    s9.2''s conditional rows covered them -- STRUCK 2026-08-12, that premise is FALSE): s9.2''s four conditional
    rows are files.operation, memory.operation, validator.fail.* and subagent.span (sampling_mode.py:176-190),
    and neither mcp.trust.evaluate nor the operator-burden span is among them. The class is SIX, in two
    shapes. NOTE: pause.captured is arguably the worse case of the four, since operator-explicit pause is
    exactly the audit-critical event a sampling floor exists for. THE B-137 DEPENDENCY APPLIES TO ALL FOUR
    FOR COMPLETE CONFORMANCE (this line briefly read ''THREE OF THE FOUR'' on 2026-08-13; RESTORED the same
    day at codex round 9). resume.attempted escapes the dependency ONLY for a TOP-LEVEL resume, where its
    entry-point call precedes the envelope and it is a trace root. For a NESTED CHILD resume it is emitted
    while subagent.span is current, so it is a non-root child and ParentBased never consults its name. Reducing
    the dependency to three would let B-160 close while nested resumes still miss the floor, so the CONFORMANCE-level
    dependency is FOUR; the top-level-only carve-out is a partial-credit note, not an exemption: ParentBased(root=...)
    never consults a non-root child''s name, so membership alone cannot deliver the floor for any of these
    child spans. REMAINING: decide which side is canonical and make the other conform: either (a) add ALL
    FOUR unconditional names to the §9.2 member set (19 -> 23) -- hitl.webhook.deliver, hitl.webhook.attempt,
    pause.captured, resume.attempted -- AND add the TWO conditional ones (mcp.trust.evaluate at audit_required;
    the operator-burden span at degrade) to the conditional resolver, which today covers only files.operation
    / memory.operation / validator.fail.* / subagent.span. A one-name edit does NOT close this row and would
    leave the witness red, so the implementation meets the §C-OD-32.3 floor -- note the §9.2 count is cited
    in several places and a count-contract sweep is owed with it. MEMBERSHIP ALONE IS NOT SUFFICIENT (codex
    round 7 sibling-overlap check): hitl.webhook.deliver is a CHILD span, and ParentBased(root=HarnessCompositeSampler)
    never consults a non-root child''s name -- it inherits the parent workflow span''s drop decision. B-137
    already registers exactly this non-root starvation, so (a) DEPENDS ON or MERGES WITH B-137; closing
    it standalone would claim a floor the sampler still cannot deliver; or (b) amend §C-OD-32.3 if webhook
    spans are genuinely NOT intended to be always-sampled, which would be a cleared-contract change under
    X-AL-3 and would need to explain how HITL delivery stays audit-critical without a sampling floor. Grounding:
    DONE 2026-08-12 -- it IS a class (four unconditional + two conditional, across four contracts). Do not
    repeat it. The materialize_span_processor_stage end-to-end run owed on the B-71 spec leg is also the
    experiment that settles this empirically. B-137 DEPENDENCY MEASURED 2026-08-13, AND TWO OF THE FOUR
    NAMES ARE DOUBLY INERT. The row carried the B-137 dependency as a reasoned claim (codex round 7); it
    is now executed at the real api.run venue. At base_rate=0.0 through the shipped path, hitl.gate.evaluated
    -- an EXISTING s9.2 member with a declared floor -- is not exported at all, because ParentBased consults
    the sampler only for the workflow.envelope root and the child inherits the drop. So ADDING A NAME TO
    THE SET DOES NOT DELIVER ITS FLOOR for any member emitted inside the envelope, which is what disposition
    (a) assumes. SEQUENCING: land B-137 step (3)''s disposition FIRST; B-160''s conformance sweep is then
    mechanical. SEPARATELY -- emit_pause_captured_span and emit_resume_attempted_span have NO CALLER ANYWHERE
    IN src/ (registered as B-162), so pause.captured and resume.attempted are never emitted in production
    at all. Adding a never-emitted name to the floor set changes nothing, so two of this row''s four unconditional
    names are inert for a SECOND, independent reason, and B-160''s disposition (a) should not be written
    as though all four are symmetric. Executed at harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py.
    AND THE TWO UNCALLED NAMES ARE THEMSELVES ASYMMETRIC (2026-08-13, codex round 2): resume.attempted would
    be emitted BEFORE workflow.envelope opens (workflow_driver.py:3213-3225 vs :3305), so once B-162 wires
    it, membership ALONE delivers its floor -- it is the single name of the four for which disposition (a)
    is sufficient on its own. pause.captured would be an envelope child and is gated on B-137 as well as
    on B-162. SCOPED TO TOP-LEVEL RESUMES 2026-08-13 (codex round 7): the root claim above generalized LOCAL
    LINE ORDERING inside one driver function into TRACE-ROOT status, and that is valid only for a TOP-LEVEL
    resume. For a NESTED CHILD resume it is FALSE -- sub_agent_dispatch.py opens subagent.span and invokes
    child_workflow_runner(..., pause_snapshot_input=...) INSIDE that block, and the child runner forwards
    the snapshot to execute_workflow, so the child driver''s entry-point resume detection runs while subagent.span
    is current. resume.attempted would then be a NON-ROOT child, ParentBased would never consult its membership,
    and it stays gated on B-137 exactly like pause.captured. So ''membership alone is sufficient'' holds
    for TOP-LEVEL resumes ONLY. Pinned by test_a_nested_child_resume_runs_under_subagent_span. NOTE 2026-08-13
    (codex round 12): the nested-resume witness is STRUCTURAL (lexical containment of the child dispatch
    inside the subagent.span block), not an executed parentage observation -- an executed nested-resume
    witness is OWED before acting on the top-level-vs-nested split. UPDATE 2026-08-13 (PR #1330): B-162
    is CLOSED by WIRING -- pause.captured and resume.attempted are now genuinely emitted in production,
    so the ''doubly inert'' qualifier on those two names is RETIRED. They are now ordinary members of this
    row''s conformance sweep, still gated on B-137 for the floor to actually reach them.'
  council: 'no at registration -- this is a conformance reconciliation between two OD surfaces,
    not a design fork. It becomes council-eligible only under disposition (b), which would
    relax an inviolable §9.3 floor.'
  heading: '### B-160 · `C-OD-32.3` declares webhook spans head=1.0 always-sampled, but
    `hitl.webhook.deliver` is absent from the 19-entry `ALWAYS_SAMPLED_EVENT_CLASSES`
    *(surfaced by the B-71 v4 leak analysis, 2026-08-12)*'
- id: B-161
  title: test_shutdown.py::test_force_flush_returning_false_surfaces_timed_out is ORDER-DEPENDENT
    -- it reports a spurious ('ledger',) failure under full-suite ordering
  status: closed
  pr: 1327
  summary: 'Surfaced 2026-08-12 on PR #1324 CI (run 31659849345) and once in a local `just check`.
    The test asserts report.failures == () for a pure-timeout shutdown and instead observed
    (''ledger'',). EVIDENCE IT IS A FLAKE, NOT A REGRESSION: the same commit passed on CI re-run
    (18/18); the test passes 6/6 in isolation; the full harness-runtime suite passed 3/3 locally
    (including once with -p no:randomly); and main was green on all 8 preceding runs. It reproduces
    only under FULL-SUITE (all axis packages) ordering, which suggests cross-module state leakage
    into the shutdown report rather than a defect in the shutdown logic itself. NOT introduced by
    #1324 -- that PR added one integration test and doc/register edits, and the pair
    (b72 integration module + test_shutdown.py) passes together 90/90.'
  close_out: 'CLOSED 2026-08-12 -- ROOT-CAUSED and FIXED, with the repro the close-out demanded.
    NOT state leakage, as first hypothesised: a TIMING race on the SHARED flush budget.
    flush_observability gives the ledger fsync only what is LEFT of the budget
    (deadline - time.monotonic(), shutdown.py:943-947); with the test''s tight 100ms total, a
    loaded runner''s real fsync overran the remainder, raised TimeoutError, and appended
    "ledger" -- turning `failures == ()` red for a reason unrelated to the tracer behaviour
    under test. DETERMINISTIC REPRO: fast False tracer + a 500ms fsync reproduces the EXACT
    CI shape (failures == (''ledger'',), timed_out True) every time; slowing the TRACER instead
    does not, because the tracer flush is bounded too and tags itself. FIX: widen that test''s
    budget to 5000ms -- the assertion stays EXACT (failures == ()), so a genuine tracer-path
    regression still fails; only the timing dependence is removed. NOT a retry marker, per
    this row''s own warning. Plus a NEW mechanism witness,
    test_slow_ledger_fsync_is_tagged_and_times_out, pinning that an overrunning fsync IS
    tagged -- mutation-probed (removing the tag reds it, along with 4 pre-existing tests that
    already covered the tag). No production change: the shared-budget behaviour is correct by
    design and already covered by test_flush_surfaces_share_one_timeout_budget. PR #1327.'
  council: 'no -- a test-isolation defect, not a design fork.'
  heading: '### B-161 · `test_shutdown.py::test_force_flush_returning_false_surfaces_timed_out` is
    order-dependent — a spurious `(''ledger'',)` failure under full-suite ordering *(surfaced on the
    B-71 precondition-4 PR #1324, 2026-08-12)*'
- id: B-162
  title: C-OD-30.3's two declared head=1.0 spans (pause.captured / resume.attempted) have emitters that
    NOTHING in src/ calls, so neither span is ever emitted in production
  status: closed
  pr: 1330
  summary: 'harness-cp/src/harness_cp/pause_resume_protocol.py:904 and :925 define emit_pause_captured_span
    / emit_resume_attempted_span, each opening the span its C-OD-30.3 contract declares at head=1.0. NO
    call site exists anywhere under harness-*/src/ -- the only matches are the definitions themselves
    and a docstring cross-reference at per_server_trust_evaluator.py:294. Both docstrings describe a caller-side
    invocation per the U-CP-61 pattern in which the workflow driver invokes protocol.capture_pause_snapshot(...)
    then this helper with the returned snapshot and driver-held tracer; the driver performs the first
    half and NOT the second. WHY IT MATTERS: two of the four unconditional names in B-160''s disposition
    (a) are these, and adding a never-emitted name to the s9.2 floor set changes nothing -- so B-160''s
    four names are NOT symmetric. It is also a plain contract-vs-implementation gap on its own terms.
    FOUND WHILE re-measuring B-137 at the real venue: an earlier draft used emit_pause_captured_span as
    its real production emitter and drove it directly; the function is real, the PATH is not, and out-of-family
    Codex rejected the witness on exactly that ground.'
  close_out: '(1) Decide whether C-OD-30.3 intends these spans to be live. If yes, wire the driver''s pause
    and resume branches to call them with the driver-held tracer per the pattern both docstrings already
    specify, and add emission witnesses. If no, the contract''s head=1.0 declaration is what to amend, via
    design-phase back-flow. (2) Either way, re-word B-160''s disposition (a) so its four names are no longer
    presented as one symmetric sweep. (3) SUPERSEDED 2026-08-13 -- true for pause.captured ONLY. pause.captured
    would be an envelope child and so lands in B-137''s root-only starvation; resume.attempted would be
    emitted BEFORE the envelope opens and so would be a ROOT, receiving its s9.2 floor directly once added
    to the set. Do not block the top-level resume slice on B-137; the NESTED child resume still needs it
    (the child driver''s resume detection runs while subagent.span is current), so B-162''s own wiring can
    land independently but COMPLETE conformance for resume.attempted still depends on B-137. WITNESS: harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py::test_the_pause_span_emitters_have_no_caller_in_src
    -- enumerates src/ for callers of both helpers and fails if either acquires one. TOPOLOGY SPLIT 2026-08-13
    (out-of-family Codex round 2) -- THE TWO SPANS ARE NOT SYMMETRIC. A first draft of this row gated both
    on B-137. That holds for pause.captured (an envelope child) and FAILS for resume.attempted: the driver
    runs entry-point resume detection at workflow_driver.py:3213-3225, BEFORE workflow.envelope opens at
    :3305, and says so in-line (the resume detection runs BEFORE the workflow.envelope opens -- a failed
    resume returns FAILED without opening a new envelope). So a resume.attempted span at its prescribed
    call site would be a ROOT, would consult the composite sampler directly, and WOULD receive its s9.2
    floor once added to the set. resume.attempted is therefore THE ONE NAME among B-160''s four for which
    membership alone is sufficient -- after wiring. So close-out step (3) above applies to pause.captured
    ONLY, and B-160''s disposition (a) must treat the two separately. SCOPED TO TOP-LEVEL RESUMES 2026-08-13
    (codex round 7): the root claim above generalized LOCAL LINE ORDERING inside one driver function into
    TRACE-ROOT status, and that is valid only for a TOP-LEVEL resume. For a NESTED CHILD resume it is FALSE
    -- sub_agent_dispatch.py opens subagent.span and invokes child_workflow_runner(..., pause_snapshot_input=...)
    INSIDE that block, and the child runner forwards the snapshot to execute_workflow, so the child driver''s
    entry-point resume detection runs while subagent.span is current. resume.attempted would then be a NON-ROOT
    child, ParentBased would never consult its membership, and it stays gated on B-137 exactly like pause.captured.
    So ''membership alone is sufficient'' holds for TOP-LEVEL resumes ONLY. Pinned by test_a_nested_child_resume_runs_under_subagent_span.
    NOTE 2026-08-13 (codex round 12): the nested-resume witness is STRUCTURAL (lexical containment of the
    child dispatch inside the subagent.span block), not an executed parentage observation -- an executed
    nested-resume witness is OWED before acting on the top-level-vs-nested split. CLOSED 2026-08-13 (PR
    #1330) -- WIRED, not amended. Grounding settled the live-vs-amend question decisively: U-CP-65''s ACs
    require the spans to EMIT (AC #1, AC #2) and require an INTEGRATION TEST verifying emission via an OTel
    collector (AC #5), so this was an incomplete unit landing rather than a contract ambiguity -- no back-flow
    needed. workflow_driver.py now calls _emit_pause_captured after every protocol.capture_pause_snapshot
    (11 sites across the 6 topology executors) and _emit_resume_attempted after the entry-point protocol.attempt_resume
    (1 site; the 3 _engine_recovery_loop.attempt_resume calls are a different, engine-layer function and
    are OUT of scope). The resume wrapper honours the corruption carve-out the emitter''s own docstring
    prescribes (s C-OD-30.1''s resume.outcome enum has no value for corruption). WITNESS: harness-runtime/tests/integration/test_b162_pause_resume_span_emission_is_wired.py
    -- 6 tests driving the shipped api.run AND resume() entry points, asserting the EXACT 4-attribute canonical
    sets (not mere presence) plus the per-site AST coverage so a partial wiring cannot pass. Mutation-probed
    against the pre-wiring driver: all 6 go RED. The B-137 witness''s own no-caller tripwire fired on this
    change exactly as designed and is now INVERTED to guard the wiring. SCOPE: these spans are emitted INSIDE
    workflow.envelope on the pause path, so their s9.2 floor remains subject to B-137''s root-only starvation
    -- this is AC conformance, NOT a sampling fix, and B-160''s disposition still depends on B-137.'
  council: no -- a wiring gap plus a contract-intent question, single-domain; it becomes design-phase
    back-flow only if the answer to step (1) is that the contract should be amended.
  heading: '### B-162 · `C-OD-30.3`''s two declared `head=1.0` spans (`pause.captured` / `resume.attempted`)
    have emitters that **nothing in `src/` calls**, so neither span is ever emitted in production *(surfaced
    while re-measuring B-137 at the real venue, 2026-08-13; the overclaim it corrects was caught by out-of-family
    Codex)*'
- id: B-163
  title: TailKeepSpanProcessor mutates _buffer / _keep / its counters with NO lock, so a concurrent child-on_end
    and root-on_end can lose a span entirely -- pre-existing at all three materialization sites
  status: closed
  pr: 1331
  summary: 'OTel''s SpanProcessor.on_end is called from whichever thread ends the span, so the SDK contract
    requires thread-safety. TailKeepSpanProcessor guards NONE of _buffer, _keep, _dropped_trace_count
    or _dropped_span_count -- no threading.Lock anywhere in the module and no docstring claim of single-threaded
    use. LOSING INTERLEAVING: a child on_end runs bucket = self._buffer.setdefault(trace_id, []) creating
    an empty list and is descheduled before bucket.append(span); a root on_end on another thread runs
    _materialize_trace_decision, whose first act is self._buffer.pop(trace_id, []) -- it pops the empty
    list and forwards nothing; the child resumes and appends to a detached list. The span reaches NEITHER
    downstream NOR _buffer, so even force_flush cannot recover it, and when the root is a s10.2 trigger
    the lost span is exactly the context the keep flag was meant to preserve. PRE-EXISTING and NOT introduced
    by B-136: the identical shape sits at all three materialization sites -- :478 (original ordinary-root
    close), :437 (B-133 event arm), :391 (B-136 name arm). B-136 widened the exposed population but the
    unguarded pop-vs-setdefault window is the module''s from the start.'
  close_out: '(1) ESTABLISH REACHABILITY before sizing the fix -- the runtime is predominantly single-event-loop,
    so the question is which real paths end spans off that thread (BatchSpanProcessor''s worker, shutdown/atexit
    drains, any thread-pool sub-agent dispatch). A race no shipped path can reach is a latent contract violation
    rather than a live defect, and that sets the priority. (2) THE FIX IS SMALL AND CONTAINED -- one threading.Lock
    (or RLock, since _materialize_trace_decision is called from inside on_end) guarding the buffer/keep
    mutations in on_end, _materialize_trace_decision, _evict_oldest_trace and force_flush. Hold it only
    around the dict work, NEVER across downstream.on_end, or a slow exporter serialises every span. (3)
    WITNESS WITH A COORDINATED INTERLEAVING, not a stress loop: drive the exact setdefault-pause-pop-append
    order via an event or a monkeypatched setdefault so the test is deterministic and actually fails without
    the lock. FALSIFIER: a demonstration that on_end is only ever invoked from one thread in every shipped
    composition (latent, not live), or a lock landing that makes the coordinated witness pass. CLOSED 2026-08-13
    (PR #1331) -- FIXED in the same PR that surfaced it, because that PR WIDENED the reachable surface.
    Out-of-family Codex escalated to P1 on exactly that ground: the race pre-exists at :478 and :437, but
    before B-136 the name arm never materialized, so B-136 made it newly reachable for name-matched roots
    -- widening a known data-loss path without guarding it is not acceptable scoping. THE FIX: one threading.RLock
    (RLock because _materialize_trace_decision and _evict_oldest_trace are called from inside on_end) guarding
    every mutation of _buffer / _keep / the counters. SCOPE DISCIPLINE HELD: the lock is NEVER held across
    downstream.on_end -- _materialize_trace_decision detaches the bucket under the lock and forwards outside
    it, and force_flush drains atomically then forwards outside, so a slow exporter cannot serialize every
    span in the process. The eviction test, the setdefault and the append are now ONE critical section,
    which is precisely the interleaving that was losing spans. WITNESS: harness-od/tests/test_b136_name_arm_root_close_materialization.py::test_b163_a_concurrent_child_and_root_close_cannot_lose_a_span
    -- a COORDINATED interleaving, not a stress loop: a hooked setdefault blocks the child thread at the
    exact pause point while the root thread attempts its pop, so the test is deterministic. Mutation-probed
    by replacing the RLock with contextlib.nullcontext(): the child span is LOST every run (reaches neither
    downstream nor the buffer), and with the lock it survives. Close-out step (1) -- reachability -- was
    therefore ANSWERED by construction rather than deferred: the losing order is reproducible on demand,
    so this was a live defect, not a latent one. ROUND-2 HARDENING + THE RESIDUAL, STATED (codex round 4,
    P1): the first lock pass guarded each mutation separately, which was NOT enough -- the keep write and
    the root materialization sat in DIFFERENT critical sections, so a thread could leave TORN state (a _keep
    entry written against a trace whose buffer another thread had already popped, which nothing would ever
    pop). All three arms now go through ONE indivisible transition, _publish_keep_and_maybe_detach, which
    publishes the keep flag and detaches the trace under a single lock acquisition and returns the spans
    for the caller to forward OUTSIDE the lock. The now-orphaned _materialize_trace_decision was removed.
    RESIDUAL, NOT CLOSED AND NOT CLAIMED: mutual exclusion fixes TORN STATE, it cannot fix ORDERING. If
    a root''s on_end runs to completion before a child''s on_end begins, the trace materializes before that
    child is accounted for, and no lock discipline inside on_end can change that. Reaching it requires a
    ROOT TO END BEFORE ITS OWN CHILD, which violates the span-nesting contract that OTel and this processor''s
    entire root-close design assume -- so it is a malformed-trace shape, not a concurrency bug, and is recorded
    here rather than papered over. An earlier draft of the witness asserted the ordering case and failed,
    which is what surfaced the distinction; the committed witness asserts the property that IS guaranteed
    -- for a well-formed trace, a trigger published concurrently with sibling buffering always preserves
    the siblings and no _keep entry survives materialization. ROUND-3 HARDENING (codex round 5, P1): the
    state transition now runs BEFORE downstream.on_end(span) in both always-sampled arms, not after. Forwarding
    never depended on the keep flag, and a SLOW downstream otherwise widened the publication window arbitrarily
    -- long enough for a concurrent root close to detach the trace with keep=False, dropping the buffered
    siblings and leaving a stale _keep entry. CRUCIALLY this window opened for a WELL-FORMED trace (trigger
    child ends before its root, as the nesting contract requires), so unlike the ordering residual above
    it is CLOSED rather than documented. Witness drives the exact shape: a buffered ordinary child, a sandbox.violation
    trigger BLOCKED INSIDE downstream, and a quiet always-sampled root closing concurrently. Mutation-probed
    by moving the forward back ahead of the transition: the sibling is dropped every run. SCOPE OF THIS
    CLOSURE, FINAL: B-163 is closed for the three windows a lock CAN close, each with a deterministic mutation-probed
    witness. Codex rounds 4-6 then surfaced two further items that are NOT lock-shaped -- force_flush returning
    before an in-flight detached batch reaches downstream, and a child paused before it registers -- and
    those are registered as B-164 rather than patched reactively here. That is arc-sizing, not deferral:
    the first needs in-flight batch accounting that changes shutdown semantics, and the second cannot be
    closed by any lock.'
  council: no -- a single-domain C7 thread-safety repair against an explicit OTel SDK contract, with no
    cross-axis surface and no committed envelope to renegotiate.
  heading: '### B-163 · `TailKeepSpanProcessor` mutates `_buffer` / `_keep` / its counters with **no lock**,
    so a concurrent child-`on_end` and root-`on_end` can lose a span entirely — pre-existing at all three
    materialization sites *(surfaced by out-of-family Codex at the B-136 arc, 2026-08-13)*'
- id: B-164
  title: 'TailKeepSpanProcessor concurrency: (a) force_flush returning before an in-flight detached batch
    reaches downstream -- CLOSED at PR #1332; (b) a span arriving after its trace materialized -- CLOSED
    by the late-arrival path; TWO RESIDUAL P2s remain registered (bounded-history edges, not the defect)'
  status: registered_finding
  summary: '(a) CLOSED at PR #1332 -- force_flush now waits for work that entered the processor but has
    not reached downstream (bounded drain-until-stable loop; see close_out for the nine windows it took
    to converge). (b) STILL OPEN. A first closure at PR #1334 argued (b) was UNREACHABLE because production
    holds exactly ONE manual-lifetime span (operator_burden_eval_primitives.py:252) and it has NO CALLER.
    The AST inventory is TRUE and is now pinned by a witness, but the CLOSURE it was used to justify is
    NOT: that basis rested on the universal "the interleaving needs a span whose end() can be called out
    of order, or one handed to another thread; production has neither", and production HAS the second.
    harness-cp workflow_driver._run_fanout_to_completion abandons its executor on ANY exception (the §25.11
    barrier deadline OR a branch failure) via shutdown(wait=False) -- its own docstring says the orphaned
    thread runs to completion in the background -- while branch dispatch runs under asyncio.to_thread,
    which copies the contextvars context so the OTel parent propagates into that thread, where llm_dispatch
    opens a REAL child span. The deadline then unwinds the workflow.envelope root while that child is still
    open. That is a root ending before its own child with ZERO manual start_span calls -- exactly (b)''s
    precondition. CONFIRMED 2026-08-13 BY REPRO at harness-runtime/tests/test_b164b_fanout_orphaned_child_span_repro.py,
    which drives the REAL _run_fanout_to_completion (not a re-implementation) and is deterministic (10/10
    identical runs; the failing branch waits on a child-span-open event rather than a sleep). MEASURED
    COST, three distinct harms: (1) the orphaned child is stranded in _buffer and never detaches, so it
    MISSES the root-close materialization that exports its trace; (2) with no max_buffered_traces ceiling
    it is merely DELAYED to force_flush, but under buffer pressure it is EVICTED (dropped_trace_count=1)
    and force_flush never recovers it -- a permanent loss, and the stranded bucket is the OLDEST entry so
    it is the FIRST shed; (3) if the orphan is itself the s10.2 trigger, the keep decision is taken
    WITHOUT it -- the root and its siblings DROP -- and a _keep entry is left that no root close will ever
    pop. The span most likely to be lost is the wedged branch dispatch, i.e. exactly the one an operator
    wants when a branch hung badly enough to trip the barrier. (b) is now a CONFIRMED defect, not a
    hypothesis. PRECISION CORRECTION + RESIDUAL (out-of-family witness-adequacy lens on PR #1335,
    P2): the repro opens the child span DIRECTLY in the abandoned worker, which is the minimal faithful
    shape, but for every currently-wired dispatcher the span opens one hop further out -- SyncDispatcherFacade
    (which opens no span itself) bridges back to a captured persistent OUTER loop via
    asyncio.run_coroutine_threadsafe, so RuntimeLLMDispatcher.dispatch runs its span on the outer loop
    thread, NOT on the to_thread worker that gets abandoned. The lens wired a REAL SyncDispatcherFacade and
    confirmed the SAME root-before-child ordering reproduces through that path, so reachability is
    unaffected -- but the late-arrival window is gated by the outer loop coroutine lifetime rather than
    literally by the abandoned executor, which any fix must account for. A facade-level witness is the
    natural next strengthening. The lens also ran the load-bearing COUNTERFACTUAL: swapping the abandon for
    joining semantics makes the ordering DISAPPEAR (child before root), proving shutdown(wait=False) -- not
    the fixture -- produces it.'
  close_out: '(1) DECIDE WHETHER (a) IS IN SCOPE for a best-effort tail processor at all -- the module''s
    own ''Drop semantics (false-but-bounded)'' posture and s9.3''s implementer-discretion clause may already
    contemplate it; ground there first, exactly as B-136''s step (5) did. (2) IF IT IS, implement in-flight
    accounting (a batch counter plus a condition variable force_flush waits on, or an explicit hand-off
    queue -- both change shutdown semantics) and witness the shutdown race directly: a slow downstream
    plus a concurrent force_flush, asserting delivery before return. (3) FOR (b), decide whether a malformed
    trace shape is worth defending against at all -- if yes, the honest mechanism is a post-materialization
    late-arrival path, NOT a wider lock. FALSIFIER: a demonstration that force_flush is only ever called
    after all span-producing threads have quiesced (making (a) unreachable), or a ratified reading that
    best-effort delivery at shutdown is contracted. (a) CLOSED 2026-08-13 (PR #1332); (b) REMAINS OPEN
    AS THE WHOLE OF THIS ROW. STEP (1) GROUNDED AND ANSWERED: (a) IS in scope, and the module''s own contract
    settles it -- force_flush is documented as existing to ''flush any still-buffered traces (treat them
    as keep-all to avoid silent loss on shutdown)'', so returning before delivery defeats its stated purpose.
    The ''Drop semantics (false-but-bounded)'' posture does NOT cover it: that paragraph is about traces
    which never root-close, not about spans already handed to the forwarder. STEP (2) BUILT: batches detached
    under _state_lock are now forwarded through _forward_detached, which registers them as in-flight (increment
    before, decrement in a finally, notify) so a concurrent force_flush waits for delivery instead of
    seeing an empty buffer and returning. downstream.on_end is still called with NO processor lock held,
    preserving the reason the forward was outside the lock in the first place. THE WAIT IS BOUNDED by
    the caller''s own timeout_millis, and the remaining budget is passed to the downstream flush -- without
    that bound this would trade a silent-loss bug for a shutdown hang, which is strictly worse. WITNESSES
    (2, both deterministic): a trigger root detaches a sibling and blocks inside a slow downstream while
    another thread calls force_flush (asserting it does NOT return), and a wedged exporter with a 300ms
    budget (asserting it DOES return). Mutation-probed: removing the wait reddens the first every run.
    REMAINING SCOPE OF THIS ROW = (b) ONLY: a child descheduled BEFORE it reaches the buffering critical
    section cannot be registered by any lock, so a concurrent always-sampled root can materialize first
    and the child is stranded or evicted. Moving the lock ahead of classification NARROWS but cannot close
    it. Reaching it requires a root to end before its own child, which violates the span-nesting contract
    the root-close design assumes -- so step (3)''s question stands: decide whether a malformed trace
    shape is worth defending against at all, and if so the honest mechanism is a post-materialization
    late-arrival path, NOT a wider lock. ROUND-2 (codex on PR #1332): the FIRST version of this fix registered
    the batch inside _forward_detached -- i.e. AFTER _publish_keep_and_maybe_detach had popped it and
    released the lock. A force_flush landing in that gap saw an EMPTY BUFFER AND ZERO IN-FLIGHT, delegated
    and returned, reproducing the exact loss the fix was meant to close. Registration now happens INSIDE
    the detaching critical section (the condition shares _state_lock), and force_flush registers its own
    drained batch the same way. NOTE ON WITNESS QUALITY: the original force_flush-waits witness could
    NOT tell the two versions apart -- mutation-probing it against the gap PASSED, because it starts its
    flusher long before the window and so only proves that a REGISTERED batch is waited for. A second
    witness discriminates by construction: it suspends the producer at the entry to _forward_detached
    (exactly after detachment, before any post-hoc increment) and asserts the batch is ALREADY counted
    there; that one reddens deterministically against the gap. Both timing-based test assumptions were
    also removed -- the flush witness is now ORDER-based (delivery recorded before force_flush''s return)
    rather than a wait(0.5) that could pass merely because the scheduler had not run the flusher, and
    the wedged-exporter witness waits on a producer ENTRY EVENT instead of a 100ms sleep. ROUND-3 (codex
    on PR #1332): three more, all in code this arc introduced. (i) force_flush returned True on timeout
    expiry -- telling the runtime shutdown may proceed while batches are demonstrably outstanding, a quieter
    form of the very bug this row is about; it now returns downstream_ok AND drained_in_time, still delegating
    so a wedged forwarder cannot stop the downstream flushing what it has. (ii) The registration LEAKED
    if the current-span forward raised: _publish_keep_and_maybe_detach had already counted the batch,
    so an exception from downstream.on_end(span) skipped the release and left the count positive FOREVER
    -- every later force_flush would burn its whole budget and now also report failure. Both always-sampled
    arms now release in a finally. (iii) The flush witness was STILL scheduler-dependent: setting an event
    just before calling force_flush does not prove the flusher reached the wait, so a deschedule there
    let the release fire first and the test passed even with the wait deleted. It now hooks the condition''s
    wait() and synchronizes on a point reached INSIDE it -- mutation-probed 3/3 red with the wait removed,
    where the previous version passed. Incidental: the SDK PROPAGATES processor exceptions rather than
    swallowing them (an earlier draft assumed otherwise and the RuntimeError escaped the test). ROUND-4
    (codex on PR #1332) -- A REPORTING FAILURE OF MINE, worth recording as the lesson. Round 3 claimed
    the flush witness had been switched to synchronize INSIDE the condition wait. It had not: the edit
    script used a str.replace with no assertion on the anchor, the anchor did not match, the replace silently
    no-op''d, and the script still printed success -- so the committed file kept the old set-event-then-sleep(0.2)
    shape while the commit message said otherwise. Codex caught it by RE-READING THE COMMITTED FILE rather
    than trusting the message. Now applied for real and verified present by grep, with the edit assertion-guarded;
    mutation-probed 3/3 RED with the wait removed. DISCIPLINE: never report an edit from a script''s own
    success print -- assert the anchor matched, then grep the committed file. ROUND-5 (codex on PR #1332):
    the batch counter alone cannot cover a span whose on_end has STARTED but not yet taken _state_lock
    -- at that moment no batch exists to register, so force_flush could observe zero, enter the downstream
    flush, and only then have the child buffer and its root detach a batch, with registration landing
    AFTER the only wait and the flush returning True with a batch undelivered. on_end now counts its own
    entry and exit (_active_on_end, incremented at the top, decremented in a finally, notified), and force_flush
    waits for BOTH counters. The body moved to _on_end_inner so the accounting wraps every path including
    early returns. Witness suspends the producer INSIDE on_end before any buffering and asserts force_flush
    parks; mutation-probed 3/3 RED with the entry counter dropped from the wait condition. ROUND-6 (codex
    on PR #1332) -- THE SINGLE DRAIN PASS WAS ITSELF UNSOUND, and this is the structural end of (a). Counters
    fix two windows but not a third: a non-root span that entered on_end BEFORE the drain and buffers
    AFTER it leaves the span in _buffer with BOTH counters back at zero, so the wait exits and force_flush
    returns True with the span buffered and never delivered. force_flush is now a DRAIN-UNTIL-STABLE loop:
    drain, forward, wait for the counters to settle, re-check the buffer, and repeat until a pass finds
    nothing new -- still bounded by the caller''s deadline, reporting failure if it exits with work outstanding.
    The three windows are now named together in the code comment ((i) detached-not-forwarded, (ii) entered-not-registered,
    (iii) buffered-after-the-pass) so the next reader sees why a single pass cannot work. Witness suspends
    the producer inside on_end so the first drain sees an EMPTY buffer, releases it only once the flusher
    is parked, and asserts delivery; mutation-probed 3/3 RED with the loop reduced to one pass. ROUND-7
    (codex on PR #1332) -- THE ENTRY COUNTER NEEDED ITS OWN LOCK. Guarding it with _state_lock (which
    _inflight_cv shares) reintroduced the same hole one level down: a callback reaching on_end while force_flush
    held that lock blocked BEFORE being counted, so the flusher could release, immediately reacquire for
    its zero check, observe nothing and return -- after which the queued callback buffered an already-ended
    span. _active_on_end now lives under a dedicated _entry_lock held for two integer operations only,
    read via _entrants_pending(); the state lock and the entry lock are never nested in a way that could
    deadlock, since _entrants_pending takes only the entry lock. Witness holds _state_lock in the test
    and asserts a span ending on another thread is ALREADY counted while provably unable to proceed (buffer
    still empty). Mutation-probed 3/3 RED with the entry counted under _state_lock again. ROUND-8 (codex
    on PR #1332, P2 -- severity dropping): waiting on a bare any-entrant-active predicate is OVER-CONSERVATIVE.
    It includes callbacks that started AFTER the flush began, so under sustained tracing the count never
    reaches zero, force_flush burns its whole budget and reports FAILURE even though everything present
    at invocation drained -- a spurious shutdown error rather than a loss. Entrants now carry a monotonic
    sequence number and each flush snapshots an _entry_cutoff(), waiting only for entrants at or below
    it. The bare counter was replaced by an _active_entries set; the now-dead _active_on_end field and
    its stale comment reference were removed. Witness takes a cutoff while idle, starts a LATE span inside
    on_end, and asserts the late entrant IS registered globally but is NOT waited for by the earlier cutoff;
    mutation-probed 3/3 RED with the cutoff ignored. ROUND-9 (codex on PR #1332, P2) -- THE SYMMETRIC
    HALF OF ROUND 8. The cutoff filtered entrants but the batch count stayed global, so a batch detached
    by a POST-CUTOFF callback still made the flush wait: the same spurious-False shape one level down.
    Batches now carry the generation of the on_end that produced them (via a thread-local current-entry
    id, so no signature threading), and the flush waits only for batches at or below its own cutoff; force_flush''s
    own drained batch is tagged 0, at or below every cutoff, so a flush always waits for itself. WITNESS
    CONSTRUCTION MATTERED: a first draft started the late trace BEFORE calling force_flush, which makes
    it legitimately PRE-cutoff work the flush SHOULD wait for -- the test failed for the right reason
    and the code was fine. It now hooks _entry_cutoff and creates the late trace from inside it, so the
    batch is genuinely post-cutoff, and it exercises force_flush() end-to-end as the reviewer asked rather
    than only the predicate. Mutation-probed 3/3 RED with the batch cutoff ignored. (b) ATTEMPTED CLOSURE
    2026-08-13 (PR #1334) -- WITHDRAWN AT REVIEW; (b) REMAINS OPEN. Two successive bases were advanced
    and BOTH were falsified; they are recorded here as SUPERSEDED history, NOT as findings. BASIS 1 (superseded,
    do not cite): production holds ZERO manual-lifetime spans, grounded by an AST scan over harness-*/src/.
    FALSE WHEN WRITTEN and its test VACUOUS -- caught by out-of-family codex as a P1 -- because the scan
    filtered on ast.Assign and so skipped `child: ChildSpanRef = tracer.start_span(...)`, an ANNOTATED
    assignment at operator_burden_eval_primitives.py:252. BASIS 2 (superseded, do not cite): EXACTLY ONE
    start_span site exists in src/ and it has NO CALLER (emit_eval_as_child_span, docstring ''never invoked''
    -- the landed-but-uncalled shape of B-162), every other site using `with tracer.start_as_current_span(...)`
    whose lexical nesting forecloses a parent-ends-first ordering. The INVENTORY half of basis 2 is true
    and is retained as a maintained invariant (test_b164b_the_only_manual_lifetime_span_is_uncalled, which
    matches at the CALL level -- an ast.Assign filter misses annotated assignments, walrus bindings and
    bare expressions -- and pins BOTH parts). What FALSIFIES basis 2 as a CLOSURE is its unstated universal
    "the interleaving needs a span whose end() can be called out of order, OR ONE HANDED TO ANOTHER THREAD;
    production has neither": production has the second, by deliberate design. workflow_driver._run_fanout_to_completion
    abandons its ThreadPoolExecutor on ANY exception via shutdown(wait=False) -- its docstring states the
    orphaned thread RUNS TO COMPLETION IN THE BACKGROUND -- and branch dispatch runs under asyncio.to_thread,
    which copies the contextvars context, so the OTel parent propagates into that thread and llm_dispatch
    opens a real child span there. The §25.11 barrier deadline then unwinds the `with tracer.start_as_current_span("workflow.envelope")`
    root while that child is still open, ending the ROOT FIRST. A sibling comment at the to_thread call
    site already records the orphaned-thread-outlives-cancellation shape as a prior codex P1 + concurrency-lens
    finding, so the mechanism is independently attested in-tree. REPRO BUILT 2026-08-13 (see summary): the ordering is
    reachable through the shipped fan-out and the processor DOES mishandle it, so the adjudication asked
    for here is DONE and answered against the closure. NEXT STEP for (b) is now the FIX, and the shape the
    row already predicted stands: a post-materialization LATE-ARRIVAL path, NOT a wider lock -- a span that
    has not entered the processor cannot be accounted for by any lock, so the processor must handle a span
    whose trace has already materialized (forward it immediately on the prior keep decision, or re-open the
    trace). Any fix must also decide what a LATE TRIGGER means: the keep decision is already published, so
    either it is honoured late or the late trigger is accepted as lost -- and the stale _keep entry must
    stop leaking either way. The repro test flips from xfail to XPASS under strict mode when the fix lands,
    which is the signal to close this row. (a) remains CLOSED at #1332 and is unaffected.
    ROUND-8 CLOSURE OF (b). The late-arrival path is BUILT and the three xfail witnesses flipped to XPASS,
    which is the signal this row named. Root close now records the trace in a materialization memory, and
    a span whose trace is in that memory is FORWARDED IMMEDIATELY and never buffered -- keep-all, the
    posture already used wherever a tail decision is unavailable. The late-arrival test sits INSIDE the
    existing buffering critical section, so a root close cannot land between the test and the append.
    Retention composes two rules because the orphan arrives in TWO orderings, each found by out-of-family
    review in turn: LIVE-PINNED (the child was already open at root close -- exact, tracked by per-trace
    open-span counts at on_start/on_end) and HISTORY-RETAINED (the worker held a copied context and had not
    yet opened its span -- only a bounded memory of the past decision catches that one). FIFO eviction
    skips live-pinned entries so the bounded rule can never expire the exact one.
    THE LATE-TRIGGER QUESTION THIS ROW ASKED IS ANSWERED: accepted as lost, not honoured late. A trigger
    arriving after materialization cannot rescue siblings already dropped; retaining dropped spans against
    the chance of a later trigger reintroduces the unbounded memory the drop exists to avoid. The trigger
    span itself is still forwarded, so the s10.2 failure SIGNAL survives and only the buffered tree-context
    is shed -- the same tradeoff eviction already documents. Pinned as a strict xfail so the bound cannot
    be silently widened. The stale _keep leak IS closed: the write is suppressed for an already-materialized
    trace, and _keep additionally carries a sweep that removes only PROVABLY stale flags (neither buffered
    nor live), so bounding the leak can never discard a valid pending decision.
    EIGHT ROUNDS OF OUT-OF-FAMILY REVIEW, severity P1/P1/P1 then P2 x5: each of the first three falsified a
    real reachability or bound claim by execution (a fixed 1024-entry window aged out a still-open orphan
    at exactly 1024; deleting the entry when liveness hit zero stranded the starts-late child; past the
    history the _keep leak returned unbounded). Rounds 4-7 were bookkeeping in the fix itself: a blind FIFO
    on _keep discarded valid decisions silently; an _open_spans cap made live traces read as finished; the
    all-live fallback evicted a proven-live pin; and both threshold expansions were one-way, leaking a
    high-water mark across time. All fixed and each mutation-probed to redden only its own witness.
    RESIDUALS REGISTERED, NOT ABSORBED (round 8, both P2, both from the same reviewer): (i) at EXACTLY the
    ceiling with every entry live-pinned, a newly materialized trace can be the only unpinned candidate and
    be evicted by the compaction that fires on the closing root''s own release -- equality should stay
    capacity pressure, or history needs capacity separate from the liveness pins; (ii) when a recognised
    late span drains, its trace keeps its ORIGINAL FIFO age instead of moving to the history tail, so the
    next unrelated root can evict it immediately even though its post-liveness window has just begun.
    Both need >=4096 CONCURRENT abandoned orphan threads to reach, which this harness does not produce, and
    both are edges in round-7 bookkeeping rather than in the defect this row exists for. Stopped here per
    the standing B-164 process note (merge at first convergence; do not extend a best-effort component into
    an open-ended hardening programme) and the arms-race discipline -- four consecutive flat-P2 rounds each
    finding a new edge in the previous round''s bookkeeping is the signal to register and hold, not to keep
    absorbing. Fix (ii) is ~2 lines and is the better first move for whoever picks this up.'
  council: no -- single-domain C7 concurrency-correctness work inside s9.3's implementer-discretion clause,
    with no cross-axis surface.
  heading: '### B-164 · `TailKeepSpanProcessor` concurrency — **(a) `force_flush` returning before an
    in-flight detached batch reaches downstream: CLOSED at PR #1332**; **(b) a child paused before it
    registers: OPEN — the PR #1334 unreachability basis was falsified at review** *(surfaced by out-of-family
    Codex rounds 4–6 at the B-136/B-163 arc, 2026-08-13)*'
- id: B-165
  title: GROUNDED + CONFIRMED -- two placements declared at the SAME position collide onto ONE audit identity
    on the sync venue, contradicting Runtime spec s14.8.7 NOTE 6-i; routed to back-flow as a Class 1 fork
  status: design_substrate_gated
  summary: 'The premise was registered UNVERIFIED and is now CONFIRMED BY EXECUTION (grounding leg, 2026-08-14).
    Answers to the row''s own (a)/(b)/(c): (a) on the durable-async webhook venue the collision is NOT
    reachable within one pass -- with TWO same-position placements declared, exactly ONE escalation is
    entered, because _escalate_to_secondary_channel is NoReturn; witnessed with a POSITIVE control (a
    counter reading exactly 1), since the durable-async path does not use the record''s ledger writer
    and ''zero writes observed'' could not otherwise be told apart from ''looked in the wrong place''.
    (b) YES -- the SYNC-BLOCKING loop mints for BOTH placements: each is gated independently and each
    reaches an audit write -- via s14.8.2 step 4h on an ASK-tier gate, and via the policy AUTO-APPROVAL
    branch (before the step-4j skip continue) on a READ_ONLY solo gate, which never reaches 4h at all.
    (c) the key is BYTE-IDENTICAL including the B-71 token, because both placements share parent, position
    and branch -- every input the basis hashes. The reachability chain is witnessed link by link: WorkflowManifestEntry
    has NO validator rejecting the pair (a full model_validate accepts it); fold_step_hitl_placements
    preserves both and disclaims de-duplication as ''a workflow-validation concern, out of scope here''
    (workflow_driver_types.py:753-756) -- an owner that does not exist anywhere. NOT a B-71 regression:
    the two-argument key already collided pre-arc, and the witness asserts BOTH halves (peer BRANCHES
    separate correctly; peer PLACEMENTS do not). THE DEFECT IS IN THE SPEC, not the code: Spec_Harness_Runtime_v1.md:4864
    NOTE 6-i asserts ''Each placement''s audit entry uses a distinct action_id'' and that sibling-distinguishability
    ''is preserved'', justifying it by ''action_id includes placement.position.value'' -- which holds
    only when the positions DIFFER, as in its own PRE_ACTION + SUB_AGENT_BOUNDARY example. COST: the composed
    key is also the F2 idempotency_key, and append_ledger_entry dedups key-only (state_ledger_write.py:330),
    so the second placement''s entry is an IDEMPOTENT_NOOP and is DROPPED -- a silent omission of oversight
    evidence, not a noisy duplicate.'
  close_out: 'GROUNDING IS DONE; the row now awaits an operator decision on a committed surface. Filed
    as Class 1 back-flow at .harness/class_1_fork_b165_same_position_placement_identity_collision.md;
    witnessed by harness-runtime/tests/test_b165_same_position_placement_identity_witness.py (13 test
    functions / 16 parametrized nodes across three modules, 13/13 mutation-proven -- each annotation applied
    literally and restored from a file backup, since the comment-out driver cannot apply additive mutations).
    THREE candidate repairs, recommendation (A): (A) narrow NOTE 6-i so the distinctness claim is scoped
    to DISTINCT positions and the same-position collision is stated as a known bound -- zero blast radius,
    makes the spec true, leaves (B) available later; (B) widen the ratified CP v1.119 s0.4(2) basis with
    a per-declaration ordinal -- re-opens the s0.12 byte-identity criterion for the whole linear/validator
    population and should NOT be taken speculatively for a shape no in-tree workflow declares; (C) create
    the missing workflow-validation layer and refuse the shape -- a new committed invariant. RESIDUAL,
    STATED NOT HIDDEN: the ACROSS-PASSES half of (a) (re-fire / resume / retry recurring the same branch
    and position) was NOT executed this leg. It does not change the verdict -- (b) already confirms the
    collision on a live venue -- but it is unproven, and the durable-async resume path is known to dispatch
    the resumed step UNCHANGED, so anyone reopening this must execute that path rather than infer it.
    The Phase-7 side is CLOSED: the witness and the s2.3 comment correction landed; NO design-substrate
    edit was made, which is the outcome this row predicted.'
  council: No -- and the row's original reasoning held. Grounding was step 1 and it resolved the question
    without deliberation; what remains is an operator choice among three named repairs on a ratified record,
    which is a design-record revisit, not a new convening.
  heading: '### B-165 · GROUNDED — two same-position placements collide onto ONE audit identity, and Runtime
    spec §14.8.7 NOTE 6-i asserts the opposite *(registered UNVERIFIED at the `B-71` spec leg PR 2026-08-13;
    CONFIRMED BY EXECUTION at the grounding leg 2026-08-14 and routed to Class 1 back-flow — the fork
    filing, not code, is the deliverable the row predicted)*'
- id: B-166
  title: Wall-clock deadline witness test_ow_proceed_deadline_stashed_child_completed_not_cancelled
    flakes only under full-suite interference, risking a masked genuine red in the B-71 impl leg
  status: registered_finding
  summary: 'A wall-clock deadline witness that fails intermittently in FULL-SUITE runs and never in
    isolation. Observed 4 of ~12 full-suite runs on 2026-08-13, on an idle machine as well as a loaded
    one, and -- decisively -- against branch diffs containing ZERO Python files (PRs #1336 and #1337),
    so no arc under review can have caused it. MECHANISM (grounded by code read at HEAD, NOT by reproduction):
    the test monkeypatches _DEFAULT_FANOUT_BARRIER_DEADLINE_SECONDS to 0.3 (line 1103) and scripts worker-1
    to block for 1.2s, then asserts an exact terminal split _branch_terminals(ledger) == {0: ''completed'',
    1: ''timed_out''} -- i.e. it asserts that worker-0''s stash lands BEFORE the 0.3s barrier deadline
    fires. Nothing synchronizes the two: the stash-then-deadline ordering the docstring names is left
    to the scheduler, so under full-suite interference worker-0 can be cut by the same deadline and
    report timed_out. Same family as the closed B-143 zero-headroom wall-clock assertion. NEGATIVE CONTROL
    RUN AT REGISTRATION: 25 consecutive isolated runs of this test, 25 passed / 0 failed -- which does
    NOT clear the test, it CONFIRMS the flake is interference-dependent and therefore invisible to any
    per-test or per-file re-run used to ''verify'' it.'
  close_out: 'WHY THIS IS REGISTERED RATHER THAN WAVED OFF: the cost is not the lost minutes, it is
    MASKING. The next arc (the B-71 impl leg, U-CP-102 + U-RT-155) is a real code arc touching the fan-out
    driver, and a session that has learned to call this red ''the known flake'' will wave away a GENUINE
    regression in exactly the subsystem this test guards. STEP 1 -- REPRODUCE IT AS A FULL-SUITE PHENOMENON,
    which is the only venue it appears in: run the full suite repeatedly (the 25-run isolated probe
    already recorded here is a NEGATIVE control and must not be mistaken for a falsifier), capture the
    failing assertion''s actual terminal map, and confirm whether the deviation is worker-0 reporting
    timed_out (the predicted mechanism) or something else (in which case the mechanism above is FALSIFIED
    and this row must be rewritten, not patched). STEP 2 -- decide the fix shape only after step 1:
    if the mechanism holds, the repair is to make the stash-then-deadline ordering STRUCTURAL (an explicit
    synchronization point that makes worker-0''s stash a precondition of the deadline rather than a
    race the scheduler usually wins), NOT to widen the 0.3s threshold -- widening buys silence and leaves
    the assertion non-discriminating, the failure mode B-143''s close-out already names. STEP 3 -- whatever
    lands must keep the witness discriminating: mutation-probe it per PD-8, since a synchronization
    that also removes the race the test exists to pin would be a green test that witnesses nothing.
    FALSIFIER: a full-suite reproduction showing the deviation is NOT the predicted worker-0 cut, or
    evidence that the 4/12 observation was itself a misread of a different assertion.'
  heading: '### B-166 · Wall-clock deadline witness `test_ow_proceed_deadline_stashed_child_completed_not_cancelled`
    flakes ONLY in full-suite runs, so it can mask a genuine red in the `B-71` impl leg *(registered
    by the prevention arc that precedes `U-CP-102`, 2026-08-13)*'
  council: No. This is a test-hygiene grounding question with a named mechanism (unsynchronized 0.3s
    barrier deadline vs worker-0's stash) and a named falsifier (a full-suite reproduction whose deviation
    is NOT the predicted worker-0 cut). There is no cross-domain tension to convene over -- step 1 is
    execution, not deliberation.
- id: B-167
  title: leg-selfcheck's count check compares claims against each other rather than against a derived
    ground-truth count, so a mirror in an untouched file is not checked
  status: registered_finding
  summary: 'The count check detects DISAGREEMENT between count claims; it does not RECOUNT. Out-of-family
    review raised this as a [P1] against the arc''s own brief, which asked for cross-checking each claim
    against the artifact it describes AND against every mirror (spec preamble, section heading, body,
    plan deltas, clearance markers, artifact-pointers, forward-register.yaml, the prose register). WHAT
    LANDED: added-lines scanning plus diff CONTEXT lines, so a single edited mirror can still disagree
    with an untouched co-located one -- the cheapest sound widening, with no extra file reads and no
    whole-file noise. WHAT DID NOT: a mirror in a file the arc never touched, or beyond the diff''s
    context radius, is still invisible; and no claim is ever checked against a derived ground-truth
    count. THE REASON IS NOT COST, IT IS SOUNDNESS: deriving a ground-truth count for an arbitrary noun
    in an arbitrary artifact is not mechanizable in general, and the obvious cheap approximation --
    scanning the FULL TEXT of touched files -- was tried in design and rejected because aggregate carriers
    (the 165-row prose register, the roadmap status) carry unrelated counts of the same nouns across
    unrelated rows, which would produce constant false disagreements and get the gate muted. A gate
    that fires on everything is worse than no gate.'
  close_out: 'STEP 1 -- decide whether a FORMAT-SCOPED recount is worth building, which is the only
    sound version. The brief supplied concrete recipes for two specific shapes: `grep -c "^[0-9]\\+\\.
    "` to count acceptance criteria in a plan unit block, and `grep -o "Criteria [0-9, and]*"` for mutation
    probes. Both presuppose the per-axis plan''s structure (a numbered list under a `U-XXX-NN` heading).
    Establish BY EXECUTION against the current plan heads whether those recipes return the right number
    for a unit whose count is already known -- if they do not, the recount is not buildable as specified
    and this row closes as a declared bound. STEP 2 -- if they do, scope the recount to exactly those
    two nouns in exactly the plan/spec artifacts, never as a general free-text facility, and make the
    ground-truth comparison HARD only where the derivation is exact. STEP 3 -- whatever lands must be
    measured for FALSE POSITIVES against a sample of already-merged arcs before it is wired into the
    pre-push path; a gate is only as useful as its precision. FALSIFIER (closes this row as a declared
    bound rather than a gap): a demonstration that the format-scoped recount cannot be made exact on
    the real artifacts, in which case the claims-versus-claims check plus diff context IS the sound
    ceiling and the tool''s docstring already states it correctly.'
  council: No. This is a precision/soundness question with an executable step 1 (do the brief's own
    recipes return the right number on a known unit?). There is no cross-domain tension to convene over
    -- the trade-off between recall and false-positive rate is settled by measurement, not deliberation.
  heading: '### B-167 · `just leg-selfcheck`''s count check compares CLAIMS against each other, never
    against GROUND TRUTH, so a mirror in an untouched file is invisible *(registered at the prevention
    arc that precedes `U-CP-102`, from out-of-family review round 1 [P1], 2026-08-13)*'
- id: B-168
  title: 'archiving a superseded next-action round has no legal commit shape: prior-only archive, single-file
    terminating refresh, and one-non-refresh-commit lag tolerance cannot all hold'
  status: closed
  summary: 'THE TRILEMMA, each leg independently established and none of them new to this arc. (a) The
    next-action archive is PRIOR-ROUNDS-ONLY by design -- its own header states the current round lives
    only in the live head, because a one-file terminating refresh can never rotate the archive (recorded
    as codex round-6 resolution (b) at the U-CTX-03 arc), and test_actual_next_action_archive_exists_and_is_not_referenced_as_a_read_target
    enforces it. (b) CLAUDE.md s12.2.1 requires a terminating refresh to change EXACTLY roadmap_status.md,
    so the archive write cannot ride it. (c) codex_context_guard._owed_lag tolerates exactly ONE non-refresh
    commit after a verified refresh (it requires HEAD''s PARENT to be that refresh), so an archive-only
    PR landing AFTER the substantive content merge is a SECOND non-refresh commit and hard-fails ROADMAP_STATUS_DRIFT
    on main. Together: the archive write cannot ride the refresh (b), cannot follow the content merge
    (c), and cannot carry a still-current round (a) -- which is what riding the content PR itself would
    do. EACH LEG WAS VERIFIED: (c) was reproduced against the guard; (a) was reproduced by running --archive-current-next-action
    in the content PR and watching the enforcing test go RED.'
  close_out: 'RESOLVED 2026-08-14 by EXIT (iii), selected by the operator''s settle-first direction
    and implemented at PR #1338. Archive round N-1 -- the most recent ALREADY-SUPERSEDED round, read
    from the live head''s git history -- rather than round N. This satisfies ALL THREE constraints UNCHANGED:
    the archive keeps its prior-only invariant (the live round is never written there), the terminating
    refresh still touches only roadmap_status.md, and the archive write rides the substantive CONTENT
    PR so the content merge remains the single non-refresh commit _owed_lag tolerates. NO GOVERNANCE
    AMENDMENT WAS NEEDED, because exit (iii) is not a new convention at all: the archive''s own header
    has always specified exactly this ("archiving happens in the NEXT content PR after a round is superseded...
    the superseded text is taken from git history of the live head"). The defect was that the tool''s
    first draft archived the CURRENT round instead. SHIPPED: --archive-superseded (archive-only write;
    finds the superseded round by walking the head''s history for the most recent differing round label),
    and the archive-before-replace precondition on --refresh --next-action was REMOVED, since it silently
    encoded exit (i) and made the refresh unrunnable at the one moment it is needed. CONSEQUENCE, stated
    plainly: the archive lags by exactly one arc by design, and the live head''s git history (git log
    -p -- .harness/roadmap_status.md) is the lossless record for that interval. WITNESSED on the real
    repository: with post-#1337 live, --archive-superseded selected post-#1336, wrote only the archive,
    left roadmap_status.md byte-identical, and the prior-only enforcing test stayed green.'
  council: Possibly. Two of the three legs are governance surfaces (the prior-only invariant and s12.2.1)
    and the third is the drift guard, so the choice is a cross-domain trade rather than a coding preference.
    If the operator does not simply select a leg, a dyadic convening on process-invariant vs guard-precision
    is the right shape.
  heading: '### B-168 · Archiving a superseded next-action round has no legal commit shape — three workspace
    constraints collide *(surfaced by out-of-family review rounds 11–13 of the prevention arc preceding
    `U-CP-102`, 2026-08-14)*'
  pr: 'PR #1338'
- id: B-169
  title: 'test_loop_gc.sh S16 STALE-boundary pin flakes under load: the quiet-side fixture is one minute
    from the boundary and real subprocess work runs between the two assertions'
  status: closed
  summary: 'A wall-clock boundary assertion with one minute of headroom. tools/hooks/test_loop_gc.sh
    case S16 pins the unreconciled-subagent STALE boundary from both sides: it stamps a 29-minute fixture
    (expected QUIET), asserts, then stamps a 31-minute fixture and asserts exactly "1 unreconciled subagent".
    Between those two assertions it runs real subprocess work (sw_ctx invokes the hook), so on a slow
    or loaded runner the 29-minute fixture crosses 30 minutes and the count becomes 2. OBSERVED: main
    CI at be081e9d failed with ''FAIL: S16 31-min agent missed (STALE boundary drifted high): [hygiene]
    2 unreconciled subagent(s) (oldest: s16stale, 31m).'' -- note the message says the 31-minute agent
    was ''missed'', but the real deviation is that the 29-minute one was ALSO counted. EVIDENCE IS SAME-SHA
    DIFFERENTIAL, the strongest available: a rerun of the IDENTICAL commit passed 73/73 with zero code
    change. Same family as the closed B-143 (zero-headroom wall-clock assertion) and the open B-166
    (fan-out deadline witness). NOT caused by the arc that observed it: PR #1338 touched ZERO files
    under tools/hooks/ and the same job passed 18/18 on its head 6aeaeee3 minutes earlier; the test
    last changed at #1296.'
  close_out: 'CLOSED AT #1350. STEP 1 DISCHARGED BY EXECUTION and the mechanism CONFIRMED -- with this row''s
    own headroom figure corrected in the process. The budget was never ''one minute'': it is a SAWTOOTH
    between ~0s and 60s, because `touch -t` takes MINUTE granularity (`date -v-29M +%Y%m%d%H%M` truncates
    seconds to :00), so the quiet-side fixture''s real age is uniform in [29m00s, 29m59s], decided purely
    by the wall-clock phase the suite happens to reach S16 at. Measured against the real hook: phase :00
    -> 59.29s of headroom, phase :30 -> 29.80s, phase :58 -> 1.74s, phase :59 -> 0.70s. POSITIVE CONTROL:
    replaying S16''s exact logic at phase :59 under 12 CPU spinners reproduced the red, and the deviation
    was the PREDICTED one -- the 29-minute fixture was ALSO counted, both assertions failing, with ''[hygiene]
    2 unreconciled subagent(s) (oldest: s16stale, 31m).'', the same shape seen on main at be081e9d. The
    identical run idle PASSED, which is why per-test re-runs never caught it. Mechanism NOT falsified. STEP
    2 FIXED THE HEADROOM, NOT THE THRESHOLD, and both pins STAY at 29/31 -- deliberately NOT the candidate
    repair this row registered (moving the quiet side to ~20 minutes), because that buys headroom by UN-PINNING
    the constant and leaves any STALE in (20m, 31m) undetected, the non-discriminating-witness failure B-143''s
    close-out already names. Two compounding leaks were closed instead: (a) a new `sw_stamp` helper sets
    an EXACT mtime via os.utime, killing the 0-59s truncation sawtooth; (b) the quiet side is RE-STAMPED
    immediately before the assertion that re-evaluates it, so every hook invocation sees a fixture that
    is exactly 29m old. Measured: 0.48s of the 60s budget spent at phase :59 under 24 spinners -- a ~125x
    margin, phase-INDEPENDENT. A FIRST VERSION OF (b) SPLIT the two fixtures onto separate registries and
    was REJECTED in self-review: it bought the same headroom but quietly DROPPED a witness -- the second
    assertion''s value is that it reads exactly 1 WHILE A FRESH SIBLING IS ALSO PRESENT, which pins the
    per-key rule that a stale entry must not drag its fresh sibling into the count. Both fixtures therefore
    stay in ONE registry exactly as before this arc, and the mutation probe shows that difference is real
    rather than theoretical. STEP 3 -- the pins are provably still discriminating: mutating STALE in loop-gc.sh
    to 20*60 reds BOTH S16 assertions (2 failed -- where the rejected split shape red only one) and to 40*60
    reds ONLY the 31-minute side (1 failed); restored from a cp backup, never git checkout; loop-gc.sh diff
    empty after). SCOPE SWEEP: S16''s -29M/-31M pair is the ONLY near-boundary relative stamp in the file
    -- the other relative stamps are -2H against the same 30-minute boundary (90 minutes of slack, truncation
    immaterial) and the rest are absolute year-2020 dates, so no sibling site is owed the same repair. VENUE
    GROUNDED, correcting a wrong first reading of my own: a sweep for a NAMED CI step running this suite
    found none and nearly produced a ''CI never runs this'' claim. It does -- tools/codex-parity-check.sh:20
    loops over `tools/hooks/test_*.sh` under `set -euo pipefail`, wired at .github/workflows/ci.yml:79 (the
    `run:` line; :78 is the step''s `name:`) as a blocking step -- so this row''s ''main CI'' attribution
    is CORRECT, reached through a glob rather than a named invocation. That hunt also surfaced B-179 (the
    documented local gate `just check` omits this lane). VERIFICATION: suite 74 passed / 0 failed, identical
    to main''s own baseline of 74 measured by a differential run of HEAD''s version (this row''s ''73/73''
    predates #1347-#1349); all 23 shell suites in the codex-parity glob rc=0; `just check` green at 7939
    passed.'
  council: No. Mechanism is named, the falsifier is executable, and the repair is a test-hygiene change
    with no contract surface. Same disposition as B-166.
  heading: '### B-169 · `test_loop_gc.sh` S16 STALE-boundary pin flakes on a loaded runner — a 29-minute
    fixture sits ONE minute from the 30-minute boundary *(observed on `main` at `be081e9d`, 2026-08-14;
    same commit passed on rerun)*'
  pr: '#1350'

- id: B-170
  title: 'Arc-metrics ledger — per-arc wall-clock capture for efficacy tracking'
  status: closed
  summary: 'GATE UNIT — blocks B-171..B-174. One row per arc at `.harness/arc-metrics.jsonl`; the load-bearing field is levers_active, so efficacy is a cohort comparison rather than an assertion. Fields carry provenance (derived / declared / unmapped) and an absent measurement is never recorded as a measured zero. LANDED at PR #1340 with all four close-out steps discharged and the falsifier re-run at HEAD.'
  close_out: 'STEP 1 -- land the uncommitted work: tools/arc_metrics.py, tools/test_arc_metrics.py (10 tests, each carrying a mutation-probe annotation), .harness/arc-metrics.jsonl (16 baseline rows), and the justfile arc-metrics recipe. STEP 2 -- verify the witness still holds at HEAD: the tool must reproduce 10 rounds for #1337 and 1408 additions from git/gh, NOT from any report. STEP 3 -- confirm fail-closed by mutation probe: remove the zero-logs guard in round_metrics() and the test must RED with DID NOT RAISE, then restore. STEP 4 -- wire capture into ship-pr so every future arc appends a row. FALSIFIER: if the backfilled figures cannot be reproduced from git/gh at HEAD, the tool is wrong and no lever may be evaluated against it.'
  council: 'No. The instrument is built and its acceptance criteria are executable; nothing here changes a committed contract.'
  pr: '#1340'
  heading: '### B-170 · Arc-metrics ledger — per-arc wall-clock capture, the GATE the other four units are measured against *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-178` in that plan)*'
- id: B-171
  title: 'Round-timing instrumentation — split codex think-time from agent absorption'
  status: registered_finding
  summary: 'The codex CLI prints no timing or token data -- verified directly against 1.6 MB transcripts. The split between codex thinking and agent absorbing inside each round is therefore unknowable, and every claim about the review loop is an estimate. One date +%s.%N either side of the codex review invocation fixes that on the very next round. PRECONDITION for B-173''s kill condition, which compares per-reviewer round wall clock.'
  close_out: 'STEP 1 -- wrap the codex-review recipe (justfile:564) to record {pr, round, started_at, ended_at, codex_wall_s, base_sha} to a sidecar ledger. STEP 2 -- the wrapper must NOT change the review''s exit status: a failed review still fails the recipe, and a failed write surfaces rather than being swallowed. STEP 3 -- flow codex_wall_s into B-170''s per-arc row. WITNESS: one arc where sum(codex_wall_s) < sum(round_wall_s), the residual being absorption; a residual at or below zero means the wrapper measures the wrong span.'
  council: 'No. One recipe line, no contract surface.'
  heading: '### B-171 · Round-timing instrumentation — the codex CLI emits NO timing data, so the loop''s 78% is currently unpriceable *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-173`)*'
- id: B-172
  title: 'Doc-only grounding sweep — batch ~20 register items into one PR'
  status: registered_finding
  summary: 'Roughly 20 register items carry a close_out beginning "(1) Re-verify... (2) Decide..." -- their genuine next step is grounding, not code. Batched into ONE doc-only PR they pay the per-arc wrapper once instead of ~20 times. MEASURED saving approximately 8 h, from 19 avoided wrappers at the #1023 analogue of 25 min each; the estimate rests on n=1 well-matched analogue, which B-170 exists to improve. Five further items (B-16, B-45, B-66, B-98, B-104) carry an explicit no-work-owed disposition and close in the same sweep.'
  close_out: 'STEP 1 -- ground each item against HEAD, never recall: grep the cited symbol, re-read the cited section, write the verdict into close_out WITH the command that produced it. STEP 2 -- one commit per item, so the sweep is reviewable item-by-item rather than as one wall of diff. STEP 3 -- every verdict is one of: still-not-buildable (stays registered), build-now (spins a scoped arc), or no-work-owed (closes). STEP 4 -- ZERO harness-*/src or harness-*/tests files in the diff; one code file drags the whole sweep through the 3-lens gate and destroys the saving. STEP 5 -- run just leg-selfcheck --uncommitted before EVERY push: doc-only buys a merge-gate skip, never a grounding skip, and this sweep touches registers dense with cites and counts. ESCAPE HATCH: split into two sweeps if review or byte load proves unwieldy, at a named cost of one extra wrapper.'
  council: 'No. Grounding is per-item empirical work against HEAD; each verdict is individually verifiable.'
  heading: '### B-172 · Doc-only grounding sweep — ~20 register items whose honest next step is a grounding pass, not a code arc *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-170`)*'
- id: B-173
  title: 'Gemini AGY parallel-review trial — OPERATOR-APPROVED, kill condition pre-committed'
  status: registered_finding
  summary: 'OPERATOR-APPROVED 2026-08-14. Runs just gemini-review alongside just codex-review against the identical diff, concurrently, on a code arc that is happening anyway -- no dedicated arc is spent. Infrastructure verified present: agy v1.1.12 on PATH and tools/agy_review.py, a 624-line fail-closed wrapper with diff-chunking, timeout and model verification, already emitting the same P1/P2/P3 shape as codex. AUTH STATUS UNCONFIRMED at filing -- the check needs a TTY -- so confirming it is step 1, not an assumption. UPSIDE at the measured 21 min/round median across 7 remaining code arcs: 2.5 h if it removes one round per arc, 9.9 h if four, ZERO if the kill condition fires.'
  close_out: 'STEP 1 -- confirm agy auth non-interactively before anything else. STEP 2 -- run both reviewers against the IDENTICAL diff, concurrently, for at least 3 rounds. STEP 3 -- classify findings both-raised / codex-unique / gemini-unique, and hand-verify each gemini-unique finding against the diff as real-and-fixable rather than hallucinated. STEP 4 -- record per-reviewer round wall clock via B-171. KILL CONDITION, PRE-COMMITTED: at most one gemini-unique P1 across three rounds, OR gemini''s round wall clock exceeding codex''s -- either kills the lever, because two parallel waits that do not overlap are not parallelism. ADOPTION BAR: a genuine P1 codex missed, at comparable or shorter wall clock, in at least 2 of 3 rounds. Record the false-positive rate: a second reviewer that adds fix-cycles for non-defects is net-negative and that must be visible. The verdict is declared against these thresholds, never chosen after seeing the data.'
  council: 'No. Operator has approved the trial; the thresholds are pre-committed and the outcome is a measurement, not a judgement call.'
  heading: '### B-173 · Gemini AGY parallel decorrelated review — the only untried lever that touches the review loop''s 78% *(operator-APPROVED 2026-08-14; provisionally numbered `B-176`)*'
- id: B-174
  title: 'Mechanize the finding class each arc — targeting self-inflicted bookkeeping drift'
  status: registered_finding
  summary: 'When a review round surfaces a finding a deterministic check could have caught, the check is written IN THE SAME ARC. Precedent is measured: the trilemma class cost approximately 32 min of review across two rounds and now costs about 1 s in tools/test_roadmap_ritual_smoke.py. Unlike a round cap this removes a class WITHOUT narrowing what any review checks. TARGETING RULE, added after grounding: classify each finding original vs SELF-INFLICTED (introduced by this arc''s own earlier rounds) and prioritise mechanising the self-inflicted ones. Reading #1337''s ten absorption commits found roughly 6 of 10 rounds carrying a self-inflicted finding pre-mitigation -- two of them naming the agent explicitly (I OVER-TIGHTENED THE FILE SCOPE; I REUSED OCCUPIED SECTION LABELS) -- and these are overwhelmingly BOOKKEEPING (stale counts, occupied section labels, self-contradicting register rows), the class a deterministic check catches by construction. leg-selfcheck already cut this from about 60% to about 29% of rounds; the residual is roughly 85 min per arc at the measured 21 min/round median.'
  close_out: 'STEP 1 -- add a CLAUDE.md 13.1 always-on row stating the discipline. STEP 2 -- record {class, arc, rounds_consumed, test_path, runtime_s, origin} per mechanised class in B-170''s ledger, where origin is original or self-inflicted. STEP 3 -- ship ONE real instance with the discipline, not the rule alone. NON-GOAL, stated explicitly: this must never become a reason to shorten review -- it removes classes from the loop, it does not license capping it. CAVEAT ON THE NUMBERS: the 6-of-10 origin split is a prose judgement over ONE arc and the 29% figure comes from a count on #1338 that could not be independently derived (no round logs survive for it). Both are exactly the kind of claim that moved twice under measurement during the audit, so record the classification going forward rather than trusting either.'
  council: 'No. The discipline is additive and the targeting rule is grounded in measured origin data; nothing narrows an existing gate.'
  heading: '### B-174 · Mechanize the finding class, every arc — targeted at SELF-INFLICTED bookkeeping drift, which is ~29% of rounds post-mitigation *(2026-08-14 arc-wall-clock audit; provisionally numbered `B-177`)*'
- id: B-175
  title: 'Arc-metrics queue/drain cross-process durability residue'
  status: registered_finding
  summary: 'Two residual cross-process hazards in the B-170 queue/drain protocol, both real and both requiring coordination beyond a single unit. (1) LEDGER is per-REPO while QUEUE_DIR is shared, so two parallel next-arc worktrees draining at once take disjoint arcs into disjoint local ledgers; each exits 0, and the rows land in two different PRs. Deferred acknowledgement (rows released only once present in COMMITTED history) removes the LOSS case, but not the split. (2) _claim_arc''s dead-claim takeover reads liveness then unlinks; two drains can both judge the same claim dead and one can unlink the other''s freshly created claim. Neither is reachable in the single-lane flow that ship-pr documents.'
  close_out: 'STEP 1 -- decide whether parallel-lane drain is a supported mode at all, or whether drain is declared single-lane by contract (the cheap resolution: ship-pr already serialises merges, so serialising drain costs nothing). STEP 2 -- if supported, give the takeover a real compare-and-swap (O_EXCL sentinel or flock) instead of liveness-read-then-unlink. STEP 3 -- if supported, make the ledger append itself the serialisation point rather than per-entry claiming. FALSIFIER: a two-process probe that drains the same queue concurrently must produce exactly one row per arc in exactly one ledger, with no queue entry released before its row is committed.'
  council: 'No. The mechanism is named and the falsifier is executable; this is a scope decision about whether parallel drain is supported, not a change to any committed contract.'
  heading: '### B-175 · Arc-metrics queue/drain cross-process durability residue — two parallel worktrees hold DIFFERENT ledgers, and a dead-claim takeover is not atomic *(surfaced by codex round 6 on PR #1340, 2026-08-14; registered rather than fixed at the arms-race point)*'
- id: B-176
  title: Ring-buffer flush budget test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5 asserts a bare
    100ms wall-clock budget with no isolation from load -- the third member of the B-166/B-169 same-sha-differential
    flake class
  status: closed
  summary: 'A wall-clock BUDGET assertion, not a wall-clock ORDERING one -- which makes it a sibling of
    B-166 and B-169 rather than a duplicate. harness-runtime/tests/test_lifecycle_ring_buffer.py:525 seeds
    100 span rows, calls ring.flush_to_sqlite(conn) against a real on-disk sqlite database under tmp_path,
    and asserts elapsed_ns < 100_000_000 (line 539). Nothing bounds what else is running: the measured
    interval spans real filesystem I/O, so on a loaded runner -- or one where another test in the same
    session is doing disk work -- the budget can be exceeded without the code under test having changed
    at all. EVIDENCE IS SAME-SHA DIFFERENTIAL, which is the only kind that proves it: it went RED once
    during PR #1341 and PASSED on a rerun of the SAME sha. MECHANISM IS GROUNDED BY CODE READ, NOT BY
    REPRODUCTION -- the read establishes that the assertion is load-sensitive by construction; it does
    NOT establish that load is what actually happened on the observed red.'
  close_out: 'CLOSED AT #1347. STEP 1 DISCHARGED BY EXECUTION, not by code read: the same unchanged flush measured median 2.51ms / max 4.54ms idle (n=40), median 5.76ms / max 78.52ms under 12 cpu + 4 io spinners (n=40), and median 5.96ms / max 139.92ms with 3 of 60 samples BREACHING the 100ms budget under 24 cpu + 8 io spinners. The median moves 2.4x while the tail moves 31x and crosses, and the breaching samples form a discrete ~130-140ms cluster -- a scheduler stall on the two asyncio.to_thread hops inside the timed window, not slow work. Mechanism CONFIRMED, not falsified. STEP 2 DISCHARGED AS BEST-OF-5 with the 100ms threshold untouched and the assertion retained; each attempt uses a FRESH database and asserts its own inserted == 100, because insert_spans is INSERT OR IGNORE and reusing one database would make attempts 2..N no-ops that clear the budget trivially. PD-8 probe: the FIRST annotation was WRONG and the probe caught it -- a per-row execute+commit runs 33-57ms and PASSES; the verified kill is PRAGMA fullfsync=ON + per-row commit at ~2.0s, which fails all 5 attempts and so confirms best-of-N stays discriminating. The non-kill is registered as B-177 rather than absorbed. ORIGINAL FILING FOLLOWS. REGISTERED RATHER THAN WAVED OFF, for the same reason B-169 was: this workspace shipped
    B-166 specifically so a session would stop dismissing a red as ''the known flake'', and observing
    a THIRD same-sha-differential red without filing it would contradict that discipline in the same breath.
    Filed here by the B-165 grounding leg because the post-#1341 next-action head named the omission explicitly.
    STEP 1 -- REPRODUCE UNDER LOAD: run the test with the machine deliberately busy (or with a constrained
    I/O path) and capture the actual elapsed_ns distribution. If the failures are NOT clustered near the
    boundary under load, the load mechanism is FALSIFIED and this row must be rewritten rather than patched
    -- the alternative hypothesis being a genuine flush-path regression, which is exactly what a mis-filed
    flake row would hide. STEP 2 -- FIX THE SHAPE, NOT THE NUMBER. Raising 100ms to some larger number
    buys silence, not information, and re-arms the same failure at the new threshold; the AC #5 budget
    is a real performance claim and should be measured in a way that survives an unquiet machine (e.g.
    best-of-N, or an explicitly perf-marked test excluded from the correctness suite). Do NOT simply delete
    the assertion: AC #5 is a stated contract and dropping its only witness would retire the claim silently.
    CROSS-REF: B-166 (unsynchronized barrier deadline) and B-169 (a 29-minute fixture one minute from
    a 30-minute boundary) -- three rows, one class, and the class is worth naming: an assertion whose
    truth depends on how busy the machine is.'
  council: 'No. Same disposition as B-166 and B-169: the mechanism is named, the falsifier is executable,
    and the repair is a test-hygiene change with no contract surface -- though STEP 2 should not silently
    weaken the AC #5 performance claim while making it stable.'
  heading: '### B-176 · Ring-buffer flush budget `test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5`
    asserts a bare 100 ms wall-clock budget with no isolation from load — the third member of the `B-166`/`B-169`
    flake class *(observed RED once on PR #1341 and GREEN on a same-sha rerun, 2026-08-14; filed by the
    `B-165` grounding leg, which the post-#1341 head had flagged as owing this registration)*'
  pr: '#1347'
- id: B-177
  title: 'AC #5''s 100 ms flush budget has ~40x slack over the ~2.5 ms of real work it measures, so it
    discriminates only catastrophic regressions -- a moderate (16x) per-row-commit regression measures
    33-57 ms and passes'
  status: closed
  summary: 'SURFACED BY A MUTATION PROBE THAT FAILED TO KILL, during the B-176 arc.
    test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5 asserts a 100 ms budget against a flush that
    actually takes ~2.5 ms idle -- roughly 40x slack. MEASURED, NOT INFERRED: replacing the
    conn.executemany batch at harness_od/sqlite_span_store.py:159 with a per-row conn.execute +
    conn.commit loop (100 separate commits) measures 33-57 ms across 15 samples and the test PASSES. Only
    a catastrophic mutation kills it -- PRAGMA fullfsync=ON plus per-row commit, at ~2.0 s. CRITICALLY,
    THIS IS NOT A BEST-OF-N ARTIFACT: the slack is a property of AC #5''s chosen number, and the
    single-sample assertion B-176 replaced had the identical blind spot, so the witness was never
    stronger than this. The test''s own mutation-probe annotation records the measured non-kill so it
    cannot overstate itself.'
  close_out: 'CLOSED AT #1349. STEP 1 ANSWERED: AC #5 IS A TEST THRESHOLD WITH NO SPEC-LEVEL BASIS, which is a STRONGER reason not to tighten it than the one this arc first argued. CORRECTED IN REVIEW, and the correction is the finding worth keeping: the first draft claimed the spec CORROBORATED a 1000ms production cycle the 100ms bound fits inside (citing Spec_Operational_Discipline_v1_8.md §27.2). The merge-gate spec lens BLOCKED on it and was RIGHT -- Spec_Operational_Discipline_v1_25.md §1.3 is a canonical-reading amendment over that exact line, and it says cadence is ''operator-orchestrator-driven (NOT bound to flush_interval_ms at v1.8-baseline default 1000ms -- the default-cadence binding is deferred to the orchestrator composition site)''. Reading the v1.8 baseline without sweeping the delta chain for amendments to that section is exactly the staleness hazard this workspace already documents. WHAT SURVIVES, on firmer ground: the 100ms figure appears nowhere in design-substrate except U-OD-43''s own AC list (grep-verified -- two hits, the AC at v2.14:147 and its v2.24:81 coverage-matrix mirror), where it is self-framed as an ''Integration test:'' criterion rather than a production bound, and the spec states NO flush latency contract at all. So there is no cycle the 100ms must fit inside, and tightening it would INVENT a contract the spec deliberately declined to state. That the number is undetermined rather than tuned is ALSO exactly why a separate measurement-derived guard is the only thing that can detect drift. STEP 2 DISCHARGED AS THE SEPARATE ASSERTION THIS ROW ASKED FOR, additive and citing no spec -- but AS A SHAPE ASSERTION, NOT A CLOCK, AND THAT CHANGE IS THE SECOND FINDING OF THIS ARC. The first implementation asserted min-of-5 under a 15ms bound derived from a three-way measurement (healthy idle 2.29-2.57ms; healthy under 24 cpu + 8 io spinners 4.07-6.18ms; per-row-commit regression 26.58-41.26ms -- non-overlapping, 4.3x gap, bound at the geometric centre). An independent worktree-isolated witness lens corroborated it empirically at ~155 attempts with 2.7x margin under DOUBLE that load. CODEX STILL RAISED A P1 AND WAS RIGHT: a host-calibrated wall-clock bound in the BLOCKING suite can fail on unchanged code on slower storage or a contended runner, and this same file already records a 139.92ms stall on this very path -- closing B-177 by adding a TIGHTER member of the B-166/B-169/B-176/B-178 flake class would have been a poor trade, however careful the calibration. The guard now counts the sqlite calls instead: one flush is executemany=1 / execute=1 (the retention DELETE) / commit=2, and a per-row loop reads as 0 / 101 / 101. STEP 3 DISCHARGED BY EXECUTION and now DETERMINISTICALLY -- with the per-row commit applied the guard FAILS on those counts while the AC #5 ceiling PASSES in the same invocation, the gap this row was opened for, with the host removed from the assertion entirely. Bounded honestly: the shape guard does NOT catch slowdowns that preserve the call shape (an added sleep, an O(n^2) projection); those remain covered only by AC #5''s ceiling at its stated 40x slack, and the docstring says so. AC #5''s recorded NON-kill stays recorded (it is still true of THAT witness) with a pointer to the guard that now covers it. ORIGINAL FILING FOLLOWS. REGISTERED RATHER THAN PATCHED, deliberately: tightening 100 ms toward the ~2.5 ms
    measurement would re-spec a STATED performance contract (AC #5), which is design-substrate territory,
    not test hygiene -- exactly the silent-absorption failure mode X-AL-3 forecloses. STEP 1 -- decide
    whether AC #5''s 100 ms is a LATENCY CEILING (a service-level bound the flush must never exceed, in
    which case the slack is intentional and this row closes as by-design) or a REGRESSION GUARD (in which
    case it is ~40x too loose to be one). THE TWO CARRIERS FRAME IT DIFFERENTLY, which is why the
    question is live: the canonical AC text (U-OD-43 AC #5 at
    design-substrate/Implementation_Plan_Operational_Discipline_v2_14.md:147) says only ''Integration
    test: 100-span batch flush in < 100ms'', framing a test threshold, while the flush_to_sqlite
    docstring at harness-runtime/src/harness_runtime/lifecycle/ring_buffer.py:333 calls the same
    number an ''AC #5 latency target''. A ceiling and a guard want different numbers, and one number
    is currently doing both jobs. STEP 2 -- if a
    regression guard is wanted, it needs a SEPARATE assertion with a number derived from measurement
    (best-of-N makes a tight bound viable; the same probe that produced 2.51 ms median can set it), not a
    re-pointing of the ceiling. STEP 3 -- whatever lands must kill the per-row-commit mutation, which is
    the concrete regression this row proves escapes today. FALSIFIER: evidence that AC #5 was specified
    as a ceiling with the slack intended, which closes this as by-design rather than as a gap. CROSS-REF:
    B-176 (the flake repair that surfaced this); note the two are independent -- B-176 was about WHEN the
    assertion is true, this is about WHAT it can detect.'
  council: 'No. The measurement is unambiguous and the open question is a scoping one (ceiling vs guard)
    that the spec text can settle; there is no cross-domain tension to convene over. It does touch a
    stated contract, which is precisely why it is registered rather than fixed in the arc that found it.'
  heading: '### B-177 · The AC #5 100 ms flush budget has ~40x slack over real work, so it discriminates
    only catastrophic regressions — a per-row-commit regression runs 16x slower and still PASSES
    *(measured by the `B-176` mutation probe, 2026-08-14; the annotation it falsified was retracted
    rather than kept)*'
  pr: '#1349'

- id: B-178
  title: 'test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp compares a synthetic
    now against a real-clock first_observed_at, so it holds only while under ~0.15 s of real time elapses
    -- fourth of the B-166/B-169/B-176 class'
  status: closed
  summary: 'A wall-clock-sensitive assertion whose sensitivity comes from MIXING an injected clock with
    the real one, which makes it a distinct sub-mechanism from its three siblings rather than a
    duplicate. harness-runtime/tests/test_lifecycle_protected_result_store.py:2784 takes at = time.time()
    (:2819), backdates the orphan''''s observation row by 10 s (:2825), recreates the file with a
    synthetic mtime recreated_at = at + 0.05 (:2831-2832), then sweeps at a SYNTHETIC now of recreated_at
    + ttl - 0.1 (:2836) and again at recreated_at + ttl + 0.1 (:2847), asserting the file is reclaimed.
    NEITHER of those two sweeps passes observed_at. MECHANISM, GROUNDED IN THE PRODUCTION CODE AND THEN
    REPRODUCED: _record_observations builds its updated map ONLY from past_ttl_names and keys each row
    record.observations.get(name, sample_time) (protected_result_store.py:1343-1346). At the intermediate
    sweep the recreated file''''s age is 0.90 s against a 1.0 s TTL, so it is NOT past-TTL and its
    backdated row is DROPPED from the record entirely. At the final sweep it IS past-TTL, so the row is
    RE-CREATED using sample_time = observed_at if observed_at is not None else time.time() (:1342) --
    i.e. the REAL clock, because the call omits observed_at. Term 3''''s grace conjunct then compares the
    SYNTHETIC now (at + 1.15) against a REAL first_observed_at (at + delta), so the assertion holds only
    while delta -- the real elapsed time across two full sweeps, a JSON record read+write, an unlink, a
    write and a utime -- stays under ~0.15 s. EVIDENCE IS A POSITIVE CONTROL, not inference: injecting
    time.sleep(0.25) immediately after :2819 reproduces the CI failure exactly and deterministically
    (same AssertionError: assert not True at :2849), and the captured log emits ''''no GC observation
    record was read -- a fresh grace begins for every past-TTL candidate (past_ttl_orphans=1)'''', which
    is the dropped-then-recreated row stating itself. Removing the sleep restores green.'
  close_out: 'CLOSED AT #1348. STEP 2 RESOLVED AS INJECT-THE-CLOCK, not widen-the-margin: both remaining sweeps now pass observed_at explicitly, matching the idiom the test''s OWN first sweep already used (gc_sweep(now=at, observed_at=at)) -- the two later calls had simply omitted it. The injected value restates the row _backdate_observation_record established, so the scenario''s meaning (this name has been under observation since well before) is preserved while the judgement stops depending on how long the preceding lines took. THE DROPPED-ROW FACT IS NOW ASSERTED, NOT ASSUMED: a new _read_observation_record(store) == {} assertion pins that the intermediate sweep DROPS the backdated row, which is what makes the final sweep MINT a row rather than read one -- if that ever changes, the test says so instead of silently re-becoming clock-dependent. VERIFIED BY EXECUTION: the 0.25 s injected delay that reproduced the original failure now PASSES, and so does a 3.0 s delay (20x the breaking delay) -- load-independent, not merely wider. STEP 3 DISCHARGED, and it corrected the docstring''s own probe: CONJUNCT (a) HAS TWO CARRIERS (the tmp_age > ttl filter building provisional_tmp at :1103, and the current_time - tmp_mtime > ttl re-check building verified_tmp at :1174). Dropping only the second leaves the first gating and the test PASSES -- a false NON-kill that would have graded this witness as weak. With BOTH dropped the test FAILS on its own ''conjunct (a) is not gating'' assertion with the reclaim logged at age_s=0.9, and the PRE-FIX test fails identically against the same mutation, so witness strength is provably unchanged. The two-carrier note is now IN the docstring so the next prober cannot repeat the mis-grade. ORIGINAL FILING FOLLOWS. STEP 1 IS ALREADY DISCHARGED -- unlike B-166/B-169/B-176 at filing, this row is REPRODUCED,
    so no further reproduction is owed and the mechanism above is not a hypothesis. STEP 2 -- DECIDE THE
    FIX, AND NOTE IT IS NOT A MARGIN WIDENING. The naive repair (pass observed_at at the two sweeps that
    omit it) does NOT work as written: at the final sweep a row created with observed_at = now has zero
    grace, so the grace conjunct becomes unsatisfiable and the test reds deterministically instead of
    flakily. The repair must first settle WHICH first_observed_at the scenario intends, and the
    candidates are (i) keep the backdated row alive by arranging the intermediate sweep to see the
    candidate as past-TTL too, (ii) pass an explicit, deliberately-old observed_at at the final sweep, or
    (iii) restructure so the outcome never depends on a re-created row at all. STEP 3 -- whatever lands
    must still kill the mutation the docstring already names (dropping conjunct (a) so a stale record row
    alone can reclaim must still make the recreated file disappear at the first post-recreation sweep). A
    NOTE THE REPAIR MUST NOT SKIP, because it is the real finding here: the docstring at :2843-2846 says
    the reclaim bound ''''falls back to the pre-existing timestamp-derived one'''', but the pre-existing
    row is in fact DROPPED before the final sweep, so today the test passes via a freshly-sampled
    real-clock observation that merely happens to precede the synthetic now by more than one TTL. The
    witness is therefore passing for a reason its own docstring does not describe -- which is why this is
    registered rather than patched inside the B-176 arc that found it: it touches what the witness proves
    about the ratified B-110 residual, and getting that wrong would weaken a ratified witness while
    making it stable. FALSIFIER: evidence that the real-clock sample is not what gates it -- e.g. the
    failure reproducing with the injected delay REMOVED, or the backdated row demonstrably surviving the
    intermediate sweep. CROSS-REF: B-166, B-169, B-176 -- four rows, one class (an assertion whose truth
    depends on how busy the machine is); B-110, whose residual this test exists to witness.'
  council: 'No. The mechanism is reproduced and the falsifier is executable. It is a test-hygiene repair
    with no contract surface, but it is NOT trivial -- it must preserve what the witness proves about the
    ratified B-110 residual, which is why it is registered for its own arc rather than absorbed.'
  heading: '### B-178 · `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp`
    compares a SYNTHETIC `now` against a REAL-clock `first_observed_at`, so it holds only while under
    ~0.15 s of real time elapses — fourth of the `B-166`/`B-169`/`B-176` class, and a distinct
    sub-mechanism *(observed RED on CI at `58c9b90b` during PR #1347; mechanism REPRODUCED by positive
    control, 2026-08-14)*'
  pr: '#1348'
- id: B-179
  title: Gate guidance across BOTH runners names `just check`, which never runs the hook shell suites; only
    the .agents/ skill mirrors name `just codex-check`, so Codex's own guidance contradicts itself
  status: registered_finding
  summary: 'The gate that the workflow docs name does not execute the tests it is trusted to cover, and
    the exposure spans BOTH runners rather than one. justfile:47 defines `check: ... test`, and `test` (justfile:23-24)
    is `uv run pytest -m ''not e2e''` -- pytest ONLY. The hook and statusline SHELL suites are run exclusively
    by tools/codex-parity-check.sh:20 (`for test_script in tools/hooks/test_*.sh tools/statusline/test_*.sh`),
    which sits in justfile:66 `codex-check` and in CI at .github/workflows/ci.yml:79 -- but NOT in `check`.
    WEAK CARRIERS (name `just check`): .claude/skills/roadmap-continue/SKILL.md:31 outright; .claude/skills/ship-pr/SKILL.md:14
    says `just check` plus ''the relevant test suites'', a discretionary hedge that never names the shell
    suites; and AGENTS.md:45 outright -- which is a CODEX carrier, not a Claude one, since AGENTS.md:1-3
    declares itself ''the authoritative compact projection of the project rules'' FOR CODEX. CORRECT CARRIERS
    (name `just codex-check`): .agents/skills/roadmap-continue/SKILL.md:37 and .agents/skills/ship-pr/SKILL.md:17.
    THE SHARPENED FINDING: Codex guidance is INTERNALLY INCONSISTENT -- its skill mirrors prescribe the
    strong gate while its own root projection prescribes the weak one -- so a Codex session is exposed depending
    on which carrier it follows, and the Claude side is uniformly weak. SCOPE CORRECTED TWICE BY OUT-OF-FAMILY
    REVIEW: round 1 showed the .agents/ skill mirrors are already right (the first filing had not named
    a tree at all); round 2 showed the resulting ''Claude-side-ONLY'' framing was ALSO wrong, because AGENTS.md
    is Codex''s authoritative projection. GROUNDED, NOT HYPOTHETICAL: in the B-169 arc `just check` went
    green (7939 passed) on a branch whose ONLY change was tools/hooks/test_loop_gc.sh; that file was exercised
    only because the arc separately ran the shell-suite glob by hand. Same family as the ''a gate cannot
    tell empty from unlooked'' discipline. Exposure is LOCAL-ONLY -- CI does run the glob -- so this delays
    discovery to CI rather than losing it.'
  close_out: 'NOT fixed inside the B-169 arc: changing the primary gate''s composition (or the guidance
    that selects it) is a scope expansion with its own blast radius -- `just check` is invoked by skills,
    hooks and operator habit, and codex-parity-check adds a ~3-minute pytest lane plus 23 shell suites to
    what is currently a ~4.5-minute run. STEP 1 -- decide the shape: (a) add `codex-parity-check` to `check`,
    paying the full cost on every invocation; (b) add a narrower `hook-suites` recipe running only the shell
    globs and put THAT in `check`, leaving the codex pytest lane where it is; or (c) align the three weak
    carriers (.claude/skills/roadmap-continue:31, .claude/skills/ship-pr:14, AGENTS.md:45) with the .agents/
    skill mirrors that already prescribe `just codex-check`. (c) IS THE RECOMMENDATION: the .agents/ skill
    tree already runs exactly this fix, making (c) a proven docs-only change with no build-tooling risk
    -- and it additionally resolves the internal contradiction inside the Codex projection, which (a) and
    (b) would leave standing. (b) remains the fallback if the full codex-check cost proves too heavy, buying
    the shell-suite coverage alone at ~90s measured. STEP 2 -- whichever lands, verify BY EXECUTION that
    a deliberately reddened hook suite fails the gate the guidance names; a change that does not actually
    propagate the non-zero exit is the same did-not-look failure wearing a new name. FALSIFIER: evidence
    that some always-on path (a git hook, a session hook, a pre-commit) already runs the shell suites locally
    on either runner, in which case the exposure is only documentary and this row closes as over-stated.'
  council: No. The gap is grounded by file:line and the repair is a build-tooling composition choice with
    three enumerated options and a named recommendation. There is no cross-domain tension to convene over
    -- step 1 is an operator-visible cost/coverage trade, not a design fork.
  heading: '### B-179 · Gate guidance on BOTH runners names `just check`, which never runs the hook shell
    suites — only the `.agents/` skill mirrors name `just codex-check`, so Codex''s own guidance contradicts
    itself *(surfaced while grounding `B-169`''s CI venue; scope corrected by codex rounds 1 and 2, 2026-08-15)*'
- id: B-180
  title: Absence-based shell assertions in test_loop_gc.sh cannot distinguish a genuinely quiet hook from
    a hook that produced no output at all
  status: registered_finding
  summary: 'tools/hooks/test_loop_gc.sh S16''s first assertion is `printf ''%s'' "$OUT" | grep -q "unreconciled"
    && bad ... || ok ...` -- it PASSES whenever the substring is absent, FOR ANY REASON. `sw_ctx` pipes
    the hook through `jq -r ... 2>/dev/null` and the exit status is discarded by command substitution, so
    a crashed hook, malformed JSON, or an empty stdout all yield OUT='''' and the assertion reports ok.
    This is not hypothetical for THIS hook specifically: loop-gc.sh:114-118 states the sweep is deliberately
    ''failure-invisible'' -- ''any python failure yields no clause on stdout ... and this hook still exits
    0'' -- so total silence is an ENGINEERED, reachable state rather than a remote crash scenario. The paired
    second assertion is presence-based and correctly FAILS on the same empty input, so the two halves are
    asymmetric: a regression that silences the sweep entirely reds the 31-min pin but is reported as a clean
    PASS by the 29-min pin. PRE-EXISTING AND UNCHANGED BY #1350: both assertion bodies are byte-identical
    before and after that arc, which only altered fixture stamping. A second, narrower observation from
    the same lens: the presence-based greps in this file are UNANCHORED, so `grep -q "1 unreconciled subagent"`
    would also match ''21 unreconciled subagent(s)'' -- not live in S16, whose two-row fixture bounds the
    count to {0,1,2}, but latent in S1/S5/S6/S7 which share the form.'
  close_out: 'REGISTERED RATHER THAN PATCHED INSIDE #1350, on the same rule that arc applied to B-179: it
    is a pre-existing weakness in a test the arc happened to touch, not a defect the arc introduced, and
    the witness lens that found it explicitly judged it out of scope for the STALE-boundary repair. STEP
    1 -- decide the assertion shape. The candidate is a POSITIVE CONTROL: assert the hook produced a well-formed
    envelope at all (e.g. that `sw_run` exited 0 and its stdout parsed as JSON) BEFORE asserting the clause
    is absent, so ''quiet'' and ''dead'' become distinguishable. Note the tension to resolve first: loop-gc.sh''s
    failure-invisibility is DELIBERATE and load-bearing for production (SessionStart must never be blocked
    by a sweep bug), so the test must assert the envelope without requiring the hook to become fail-loud
    -- these are different surfaces and conflating them would re-litigate a ratified design choice. STEP
    2 -- the same absence-based form appears in other cases in this file; sweep them and fix the class rather
    than the instance, but only after step 1 settles the shape. STEP 3 -- mutation-probe it: break the sweep
    so it emits nothing and confirm the repaired assertion REDS, since an absence-check that still passes
    on silence is precisely the witness this row exists to remove. FALSIFIER: a demonstration that `sw_ctx`
    cannot in fact yield an empty string for a broken sweep (e.g. jq erroring in a way that aborts the case
    under `set -e`), in which case the silence path is unreachable and this row closes as over-stated.'
  council: No. The mechanism is grounded by file:line, the repair is a test-hygiene change, and the one
    real tension (production failure-invisibility vs test observability) is nameable and resolvable without
    cross-domain deliberation -- the test can assert the envelope without changing the hook.
  heading: '### B-180 · S16''s quiet-side assertion is ABSENCE-based, so a crashed or silent hook reports
    a PASS — and `loop-gc.sh` is deliberately failure-invisible, which makes silence a reachable state *(found
    by the merge-gate witness lens during `B-169`, 2026-08-15)*'
