# Round-6 sweep answers — u-he-50

- codex_review_wrapper:e84589838f36b33ddd120abe129025e40635e291:f426492a0ce0:1 (P2,
  immutable start before in-process GATE_REFUSED) — REJECTED, standing disposition:
  this is the r5-registered residual re-raised verbatim (third raise of the same
  TOCTOU shape). The session layer cannot observe admit() and record_phase refuses
  rewrites; the structural fix is the registered B-218 wrapper-internal emitter, and
  the recipe comment names the residual. Register-and-hold: a registered residual is
  not reopened by restatement. Disposed rejected.
- codex_review_wrapper:e84589838f36b33ddd120abe129025e40635e291:17567641325e:1 (P2,
  Codex carrier's unlogged-gemini rounds lack executable verify commands) — ABSORBED:
  the .agents carrier now holds both verify edge commands inline and points at the
  single definition home by path (`.claude/skills/ship-pr/SKILL.md` "Phase-span
  edges"); the parity witness asserts the executable command shape and the pointer,
  so gutting the sentence reds the suite. Disposed accepted.

Class-sibling sweep (executable-carrier class): every review venue either has the
wrapper emitter (logged path, both runners) or carries the inline commands
(.agents gemini path; the .claude carrier deliberately does NOT instruct manual
verify — its logged wrapper emits, and hand-emission there is discouraged as a no-op
replay). No other venue exists in either carrier.

## Grep-hit labels, answered verbatim (fix diff)

- [save/restore pair: two-armed restore] — none added (doc + test only).
- [check-then-act on paths] — none added.
- [bare counts/absolutes in prose] — "the single definition home" is the
  one-source-of-truth designation this arc maintains (definition lives only in the
  Claude ship-pr block; both carriers point at it).
- [new retry/timeout constants] — zero.

No findings deferred.
