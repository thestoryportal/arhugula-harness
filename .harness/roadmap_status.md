# Roadmap status dashboard

*Refreshed at session-start audit + post-PR-merge audit per workspace `CLAUDE.md` §12. Consumed by the next-action derivation rule at `Project_Roadmap_v1.md` §4. **Do not hand-edit during execution — refresh via the protocol at `Project_Roadmap_v1.md` §7.2.***

---

## Workspace state anchor

| Field | Value |
|---|---|
| `workspace_state_hash` | `9c6d852700d3` |
| `last_refreshed` | 2026-06-18T13:05:19+00:00 |
| `git_head` | `a90fe979` — the substantive arc is #651 **feat(runtime): R-FS-1 `B-INTERSTEP` — inter-step data-flow channel (runtime v1.59 §14.21 C-RT-29)**. The SIXTH standalone `B-*` arc since the FROZEN order completed. Closes the gap where the workflow driver threaded **only control flow** between steps for *every* topology — most visibly `EVALUATOR_OPTIMIZER`, where the evaluator could not *see* the generate draft nor the regenerate the evaluator's feedback (the §25.11 driver comment names the intended mechanism: *"a shared run context the dispatcher reads"*). **Mechanism:** a run-scoped `InterStepOutputChannel` — a plain by-reference holder (the `CostRecordAccumulator` CA #625 precedent; a typed container would be Pydantic-copied at `freeze()`) — constructed + bound at stage-5 LOOP_INIT **only when `RuntimeConfig.inter_step_data_flow=True`** (default `False` → `ctx.inter_step_output_channel is None` → **byte-identical to pre-v1.59**). The CP driver `record()`s each completed step's output (`getattr`-dynamic, no `harness-cp → harness-runtime` import); the **genuine consumer** is the C-RT-15 LLM dispatcher, which injects `most_recent_output()` into the **actual provider call** `messages` (proven by-execution, not a stub). `StepDispatcher.dispatch(...)` signature UNCHANGED + §25.3.3.4 step-body-opaque PRESERVED → **NO operator gate** (additive + opt-in). **Daemon-scope hardening (5 Codex bugs across 4 rounds + the r5 systemic residual):** the bootstrap-allocated channel holds run-scoped CONTENT, so a reused `HarnessContext` (daemon-client U-RT-108) leaks across `run_workflow` invocations — invariant 7 is honestly bounded to (a) *sequential* reuse closed by per-run `reset()` (r3); (b) *concurrent-within-drain-timeout* closed by a per-running-loop single-flight **lock** (r4 — opt-out default never locks → full concurrency unchanged); (c) the *concurrent-with-timeout* `to_thread`-zombie (r5) = a SYSTEMIC reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT closeable by a lock → registered `B-INTERSTEP-PERRUN-ISOLATION` (real-but-accepted; triage+register, not looped-to-green). Bundled-absorption: runtime spec v1.58 → **v1.59 §14.21 C-RT-29** + clearance marker + fork doc + harness-runtime/harness-cp impl + by-execution tests (serialization + negative control + loop-safety) + spine-ledger BUILT (3 follow-ons: `B-INTERSTEP-NONLINEAR` / `B-ENGINE-OUTPUT-REPLAY` / `B-INTERSTEP-PERRUN-ISOLATION`) + arc-and-unit-map §5 (6 closed · 7 remaining) + roadmap.html. **⚠️ Operationally-surprising property:** the opt-in inter-step path is single-flight, so one run on a long HITL wait head-of-line-blocks other inter-step runs on that daemon (documented invariant 7b; removed by the registered isolation follow-on). Decorrelated review: advisor (Option-B lock + the r5-residual triage/honest-bounding) + out-of-family Codex (5 real bugs + the systemic residual). Gates: pyright 0/0/0 · ruff · harness-runtime 1915 (non-e2e) · harness-cp 1062 · overlay green · substitution 54/54 · X-AL-3 green · CI all blocking green. Lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). |
| `latest_retirement_batch` | `.harness/phase-7d-retirement-events-batch-57.md` |
| `open_fork_doc_count` | 73 |

**Hash recipe.** `sha256(git_head[:8] + "|" + sorted_open_pr_csv + "|" + open_fork_doc_count + "|" + latest_retirement_batch_path)[:12]`. See `Project_Roadmap_v1.md` §7.1.

---

## Next action

**Purpose.** Section 02 names the next Claude/Codex-executable frontier. Completion history lives in `Recently completed`; the full remaining itemization lives in `Remaining forward work` (next section).

**Frontier.** The R-FS-1 full-spec build umbrella (operator directive 2026-06-12) remains the single **ACTIVE** program. Its **FROZEN order is COMPLETE — all 11 ordered child arcs landed** (B1…M). R-FS-1 stays ACTIVE because the **standalone `B-*` build arcs** (surfaced during the frozen builds; full-spec, nothing-deferred) remain — they are the next-action frontier; the quality track stays gated behind R-FS-1's resolution.

**Next = the autonomous standalone `B-*` build arcs (R-FS-1's continuing work, FULL-SPEC committed, no operator gate).** **`B-MCP-HOST-REMOTE-TRANSPORT` ✅ COMPLETE (#640)** — the FIRST standalone `B-*` arc landed since the FROZEN order completed: the stage-3a factory now admits remote streamable-HTTP MCP hosts (a `_coarse_transport` granular→coarse projection; impl-to-cleared-spec against runtime §14.9.6 inv 5, no operator gate), and BOTH B6-skipped default-lane tool-completion e2es are RESTORED on a real remote streamable-HTTP echo server (remote L1 READ_ONLY → TIER_1, no Docker; `test_r100_ac2` 8/8 non-flaky in CI + `test_r_cl_p3` 3/3 live-Ollama). **🎉 R-FS-1 FROZEN order COMPLETE — 11/11:** B1✅ B3✅ E✅ B2✅ R✅ B4✅ CA✅ B5✅ B6✅ B7✅ M✅. **`B-MEMORY-SURFACE-BACKEND-IMPLS` ✅ COMPLETE (#642)** — the SECOND standalone `B-*` arc: both spec'd-but-unbuilt memory backends built (`ENCRYPTED_FILESYSTEM` + `OPERATOR_DEFINED`; impl-to-cleared-spec §14.12.3). **`B-COST-DISCRIMINATOR-TAXONOMY` ✅ COMPLETE (#644)** — the THIRD standalone `B-*` arc: the latent Class-1 contract-vs-production defect (production helpers wrote dispatch-type strings into the `CrossFamilyTag`-validated `provider_discriminator`) reconciled by a NEW typed `DispatchKind` dimension + `RollupAxis.PER_DISPATCH_KIND` + the new `RunResult.cost_attribution_by_dispatch_kind` field; `provider_discriminator`→`str|None` (per-dispatch carries `None`, the family tag is a §15.3 chain concept); bundled-absorption OD v1.30 §15.1 + runtime v1.57 §9 + OD plan v2.28, no operator gate, advisor + Codex clean. **`B-NONLINEAR-OVERRIDE-PROVENANCE` ✅ COMPLETE (#648)** — the FOURTH standalone `B-*` arc: the per-step override-application state-ledger entry (`cp.per-step-override-application`, U-CP-14 §16.5) now fires on **all 6 topologies** (linear inline; the 5 non-linear strategies via the **buffered-branch path** — `append_branch_override_ledger_entry` buffered into each branch's `BufferingLedgerWriter`, drained on the driver thread realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across all topologies; impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh), NO operator gate; advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer mis-count, lost-override-on-failed-dispatch, missing CASCADE_CANCEL terminal). **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` ✅ COMPLETE (#649)** — the FIFTH standalone `B-*` arc: `RollupAxis.PER_PROVIDER_DISCRIMINATOR` is now **non-vacuous in production** — the LLM cost path stamps each `SpanCostRecord.provider_discriminator` with the dispatched provider's family (`ANTHROPIC → frontier_managed`, `{OPENAI, GOOGLE} → frontier_managed_alt`, `LOCAL_OPEN_WEIGHT → local_ollama`; new `cross_family_cost_tag.py` map, carrier-homed in runtime) surfaced on a NEW optional `RunResult.cost_attribution_by_provider_discriminator` (runtime v1.58 §9). The A/B fork was advisor-resolved as a span-layer-vs-record-layer false dichotomy (§15.1.2 assigns the field-population to this arc); impl-to-cleared-spec, no OD spec change, runtime-only, NO operator gate; this axis partitions the LLM subtotal (skips `None`-tag non-LLM records). Bundled doc-hygiene reconciled the v1.57 §9-body-drift. **`B-INTERSTEP` ✅ COMPLETE (#651)** — the SIXTH standalone `B-*` arc: a run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (foundational; `EVALUATOR_OPTIMIZER` the load-bearing case). NO operator gate (additive + opt-in, default byte-identical). Invariant 7 honestly bounded to 3 daemon-reuse cases (5 Codex bugs across 4 rounds; the r5 systemic timeout-zombie residual registered as `B-INTERSTEP-PERRUN-ISOLATION`). **Autonomous next builds (no operator gate, FULL-SPEC committed; ground at arc-open per `[[grounding-reveals-claude-closeable-slice-close-honestly]]`, pick the highest-value):** of the 7 remaining buildable standalone arcs — `B-TOOL-GATE` (wire the real per-server MCP-trust source at the tool-step approval gate — **recommended next**, HIGH load-bearing), `B-EFFECT-FENCE` (at-most-once side-effecting steps across retries/resumes — HIGH), `B-FANOUT-PAUSE`, `B-ENGINE-OUTPUT-REPLAY`, `B-EDIT-CARRIER`, `B-LAYER-BUDGET-OVERRIDE`, `B-L2-EMBEDDING-ACTIVATION` (routing-activation gate). Newly-registered follow-ons (from #651): `B-INTERSTEP-NONLINEAR` (concurrent-fan-out recording), `B-INTERSTEP-PERRUN-ISOLATION` (per-run channel isolation — removes the single-flight + closes the timeout-zombie + covers `cost_record_accumulator`). `B-TAIL-CONDITIONAL-SAMPLING` remains R-420/R-421-gated; `B-PER-DISPATCH-DRIVER-PRECISION` is registered-only (Option A chosen → N/A). **Still gated:** `R-CL-Q1→C1` (quality track) — admits only after R-FS-1 fully resolves (the standalone `B-*` arcs land); quality runs once, on the complete full-spec harness. Ground at arc-open (presence-not-correctness) + no-parking (`[[feedback-operator-labels-are-claude-driven-no-parking]]`). **Forward items registered:** `B-TAIL-CONDITIONAL-SAMPLING` (B7 #632; gated). **Landed:** `B-MCP-HOST-REMOTE-TRANSPORT` (#640), `B-MEMORY-SURFACE-BACKEND-IMPLS` (#642), `B-COST-DISCRIMINATOR-TAXONOMY` (#644), `B-NONLINEAR-OVERRIDE-PROVENANCE` (#648), `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` (#649), `B-INTERSTEP` (#651).

**Recurring lanes** run on cadence alongside: `R-600-*` (pattern bake-in, codex out-of-family review), `R-IF-roadmap-refresh`.

**Do not re-open as next action.** Closed/parked: `R-411/412/420/421/430`, `R-500`, `R-810/820/830`, `R-008`/OD-4, `R-CXA-1/2/4`, `R-100-*`, `R-300-*` (both routing-activation #213 and second-provider #281/#283 RESOLVED), `R-901` (research frontier closed). `R-CL-Q1→C1` (quality track) **stays BLOCKED behind R-FS-1** — the FROZEN order is 11/11 (#637), but R-FS-1 (full-spec, nothing-deferred) is NOT resolved while the standalone `B-*` build arcs remain; quality runs once, on the *complete* full-spec harness, so it admits only after the standalone arcs land.

---

## Remaining forward work

**The full-spec build (R-FS-1).** All remaining forward build work is child arcs of the single ACTIVE umbrella **R-FS-1** (operator FULL-SPEC directive 2026-06-12 — nothing deferred; every capability built beyond MVP).

**Single arc→unit source.** The full itemization — all 11 arcs in plain language, with status, build position, before/parallel dependencies, and every atomic unit (real as-built for the 5 done arcs; anticipated slices for the 6 remaining) — lives in **`.harness/r-fs-1-arc-and-unit-map.md`**. The dashboard's Build-progress section parses that file; this section is a pointer, not a second parseable copy (no duplicate to drift). Per-arc grounding leads: `.harness/r-fs-1-remaining-arcs-grounding-sweep-v1.md` (re-ground at arc-open, presence-not-correctness). Spine ledger (every surfaced boundary + standalone arc): `.harness/beyond-mvp-capability-boundary-ledger.md`.

**Frozen order** (B1→B3→E→B2→R→B4→CA→B5→B6→B7→M) — **COMPLETE, 11/11.** DONE: **B1✅ B3✅ E✅ B2✅ R✅ B4✅ CA✅ B5✅ B6✅ B7✅ M✅** (R incl. L3 LLM_AS_ROUTER #604 + L2 EMBEDDING #606; B4 incl. Slice 1✅#616 / 2✅#618 / 3✅#619 / 4✅#621; CA✅#625 runtime spec v1.53 §9; B5✅#628 `resolve_backend` surface-map; **B6 = Slice 1✅#630 STDIO transport-floor [runtime v1.54 §14.9.8] + Slice 2✅#637 per-tool tier + per-dispatch driver [runtime v1.56 §14.9.11 / AS v1.11; `B-PER-TOOL-SANDBOX-TIER`]**; B7✅#632 OD sampler §9.2 attribute-conditional predicate, OD SSOT; M✅#635 C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`). **🎉 R-FS-1 FROZEN order COMPLETE (11/11); R-FS-1 stays ACTIVE.** NEXT: the **standalone design-fork-first `B-*` arcs** (R-FS-1's continuing work — autonomous, FULL-SPEC committed — landed: `B-MCP-HOST-REMOTE-TRANSPORT`✅#640, `B-MEMORY-SURFACE-BACKEND-IMPLS`✅#642, `B-COST-DISCRIMINATOR-TAXONOMY`✅#644, `B-NONLINEAR-OVERRIDE-PROVENANCE`✅#648, `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`✅#649, `B-INTERSTEP`✅#651; remaining: `B-TOOL-GATE` [recommended next], `B-EFFECT-FENCE`, `B-FANOUT-PAUSE`, `B-ENGINE-OUTPUT-REPLAY`, `B-EDIT-CARRIER`, `B-LAYER-BUDGET-OVERRIDE`, `B-L2-EMBEDDING-ACTIVATION`; `B-TAIL-CONDITIONAL-SAMPLING` #632 R-420/R-421-gated) + the visibility-only **routing-activation gate** (UNOWNED until a second production provider is configured). The **`R-CL-Q1` quality track stays gated** until R-FS-1 fully resolves (standalone arcs land). All detailed in the spine ledger + arc-and-unit map.

---

## In-flight (open PRs)

| PR | Branch | R-NNN | Posture |
|---|---|---|---|
| *(none)* | — | — | No open PRs at refresh time. |

---

## Recently completed (last 5)

| R-NNN / PR | Closed at | Notes |
|---|---|---|
| PR #651 | 2026-06-18 | **feat(runtime): R-FS-1 `B-INTERSTEP` — inter-step data-flow channel (runtime v1.59 §14.21 C-RT-29).** The SIXTH standalone `B-*` arc. Closes the cross-topology gap where the workflow driver threaded **only control flow** between steps — most visibly `EVALUATOR_OPTIMIZER`, where the evaluator never *saw* the generate draft nor the regenerate the evaluator's feedback (the §25.11 driver comment names the mechanism: *"a shared run context the dispatcher reads"*). A run-scoped **`InterStepOutputChannel`** (a plain **by-reference holder** — the `CostRecordAccumulator` CA #625 precedent; a typed container would be Pydantic-copied at `freeze()`, silently disconnecting producer from consumer) is constructed + bound at stage-5 LOOP_INIT **only when `RuntimeConfig.inter_step_data_flow=True`** (default `False` → `ctx.inter_step_output_channel is None` → **byte-identical to pre-v1.59**). The CP driver `record()`s each completed step's output via a `getattr`-dynamic helper (the `cp_is_wiring` idiom — `harness-cp` does not import the runtime holder); the **genuine consumer** is the C-RT-15 LLM dispatcher, injecting `most_recent_output()` into the **actual provider call** `messages` (proven by-execution at the provider boundary, NOT a stub — `[[test-bypass-as-runtime-truth-pattern]]` avoided). `StepDispatcher.dispatch(...)` signature **UNCHANGED** + §25.3.3.4 step-body-opaque **PRESERVED** → **NO operator gate** (additive + opt-in, no committed-invariant sacrifice; mechanism resolved by landed precedent → advisor, not council). **Daemon-scope hardening — 5 real Codex bugs across 4 rounds (params-override drop / system-conflict masking / HITL tool-loop / daemon sequential-leak / daemon concurrent-leak), all decorrelated misses advisor + author did not catch (`[[hooks-codex-pilots-decorrelation-validated]]`):** the bootstrap-allocated channel holds run-scoped CONTENT, so a reused `HarnessContext` (daemon-client U-RT-108) leaks across `run_workflow` invocations → invariant 7 honestly **bounded** to (a) *sequential* reuse closed by per-run `reset()`; (b) *concurrent-within-drain-timeout* closed by a per-running-loop single-flight **lock** wrapping `[reset + execute]` (opt-out default never locks → full concurrency unchanged; per-loop keying avoids the "bound to a different event loop" multi-`asyncio.run` hazard); (c) the *concurrent-with-timeout* `to_thread`-zombie (Codex r5) = a **SYSTEMIC** reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT closeable by a lock → triaged as real-but-accepted + registered `B-INTERSTEP-PERRUN-ISOLATION` (advisor reconcile: FULL-SPEC = registration, not build-everything-here; NOT looped to Codex-green). **⚠️ Operationally-surprising:** the opt-in inter-step path is single-flight, so one run on a long HITL wait head-of-line-blocks other inter-step runs on that daemon (documented invariant 7b; the isolation follow-on removes it). Bundled-absorption: runtime spec v1.58 → **v1.59 §14.21 C-RT-29** + clearance marker + fork doc + harness-runtime/harness-cp impl + by-execution tests (channel unit + genuine dispatcher-injection + real EO driver producer + serialization **+ negative control** proving the guard is load-bearing + loop-safety) + spine-ledger BUILT (3 registered follow-ons: `B-INTERSTEP-NONLINEAR` / `B-ENGINE-OUTPUT-REPLAY` / `B-INTERSTEP-PERRUN-ISOLATION`) + arc-and-unit-map §5 (6 closed · 7 remaining) + roadmap.html. **Decorrelated review:** advisor (Option-B lock decision; the r5 systemic-residual triage + honest invariant-7 bounding; caught the `B-INTERSTEP-PERRUN-ISOLATION` forward-cite phantom → registered in the spine ledger) + out-of-family Codex (5 bugs + the r5 residual). Gates: pyright 0/0/0 · ruff · harness-runtime **1915** (non-e2e) · harness-cp **1062** · overlay green · substitution 54/54 · X-AL-3 green · CI all blocking green. → NEXT = **`B-TOOL-GATE`** (recommended; HIGH) / the other remaining standalone `B-*` arcs. |
| PR #649 | 2026-06-18 | **feat(runtime): R-FS-1 `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` — populate the cross-family family tag on production LLM cost records (runtime v1.58 §9).** The FIFTH standalone `B-*` arc. `RollupAxis.PER_PROVIDER_DISCRIMINATOR` was defined + admissible but **vacuous in production** — every production LLM cost record wrote `provider_discriminator = None` (no producer of the §15.3 family tag, by OD v1.30 §15.1.2's own design, which reserved the field for *this* arc to populate). The LLM cost path (`_attribute_cost_best_effort` → `attribute_llm_dispatch_cost`, a new optional `provider_discriminator` param) now stamps each LLM `SpanCostRecord.provider_discriminator` with the dispatched provider's family via a NEW `harness_runtime/lifecycle/cross_family_cost_tag.py` map (`ANTHROPIC → frontier_managed`, `{OPENAI, GOOGLE} → frontier_managed_alt` per the §15.1 "OpenAI/Gemini" grouping, `LOCAL_OPEN_WEIGHT → local_ollama`; `CrossFamilyTag` NOT extended; map homed in runtime per `[[carrier-home-defect-pattern]]` — `CrossFamilyTag` OD-homed + `ProviderFamily` CP-homed — and it owns the now-one-source-of-truth provider→family map `retry_breaker_fallback` reuses, deduping its local `_provider_family`). Surfaced on a NEW optional **`RunResult.cost_attribution_by_provider_discriminator`**. **Advisor resolved the apparent A/B fork** (emit a separate parent family-tagged record **vs** tag the existing dispatch record) as a **span-layer-vs-record-layer false dichotomy**: §15.3 governs OTel **span** attributes; §15.1.2 governs the **`SpanCostRecord` field** and explicitly assigns this arc to **populate the existing record's field** — collapsing A/B + killing the double-count-in-`PER_PROVIDER_AND_MODEL` worry by construction. **NO operator gate** — impl-to-cleared-spec (OD v1.30 §15.1.2 already owns the field semantics → **no OD spec change, runtime-only**); additive, no committed-invariant sacrifice. **Correctness: this axis partitions the LLM SUBTOTAL, not the full run total** (non-LLM records carry `provider_discriminator = None` → skipped) — tests assert `Σ == Σ(LLM records)` (NOT total-run) + the other two cost axes unchanged (no regression/double-count); the hollow `FallbackChainCostComposition` carrier is deliberately NOT wired (no consumer). **Bundled doc-hygiene:** runtime spec v1.57 → **v1.58** §9 also reconciles the v1.57 §9-body-drift — `cost_attribution_by_dispatch_kind` was change-note-declared but never tabled in the §9 body (`[[spec-prose-plan-body-drift-pattern]]`). Bundled-absorption: runtime v1.58 §9 + clearance marker + fork doc + harness-runtime impl + by-execution tests (the provider→tag map; the LLM-subtotal rollup; the REAL cost path tagging a record; a REAL `RetryBreakerFallbackDispatcher` cross-family fallback anthropic→openai → `frontier_managed_alt`) + spine-ledger BUILT + arc-and-unit-map §5 (5 closed · 8 remaining) + roadmap.html. **Decorrelated review:** advisor (resolved the false dichotomy; flagged the LLM-subtotal invariant + the hollow-carrier trap) + out-of-family Codex ("no actionable correctness issues; internally consistent and covered by targeted tests"). Gates: pyright 0/0/0 · ruff · harness-runtime 1889 (non-e2e) · dashboard 15 · overlay 324/31-31 · substitution 54/54 · X-AL-3 green · CI all blocking green. → NEXT = **`B-INTERSTEP`** (recommended; ground at arc-open) / the other remaining standalone `B-*` arcs. |
| PR #648 | 2026-06-18 | **feat(cp): R-FS-1 `B-NONLINEAR-OVERRIDE-PROVENANCE` — per-step override-ledger entry on all topologies (CP v1.40 §6.6).** The FOURTH standalone `B-*` arc. The per-step override-application state-ledger entry (`cp.per-step-override-application`, U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` driver-thread site ONLY; the 5 non-linear strategies resolved the per-step binding (override took effect at dispatch on every topology) but emitted NO dedicated override-ledger entry — a PRE-EXISTING all-topologies, all-dimensions (model v1.6 / prompt v1.37 / role v1.38) audit-**provenance** gap, scoped honestly in CP spec v1.38 §6.6. **Closed via the buffered-branch path** (CP §6.6's own named, thread-model-agnostic option; C-CP-25 §25.12 D1.b): `append_branch_override_ledger_entry` buffers the entry into the branch's `BufferingLedgerWriter`, serialized on the driver thread by `drain_branch_buffers` in branch-index order — realizing the **ADR-F2 v1.2 single-threaded-write boundary** with zero six-field / hash-chain change. Wired into all 5 strategies via a shared `_buffer_branch_override_if_applied` guard, **uniformly BEFORE dispatch** (HIERARCHICAL_DELEGATION inherits it via the orchestrator-workers recursion). **Idempotency = the §16.5.4 per-`(step, outcome)` key, reused VERBATIM** (not branch-scoped): an override is a static binding property, so a repeated `(step, outcome)` across iterations / recursion dedups at the IS writer to one entry — both write paths compose via the factored-out `compose_override_entry_payload` → the persisted entry is **byte-shape-identical across all topologies** (branch-scoping the key would change the cleared §16.5.4 formula → X-AL-3, foreclosed). **NO operator gate** — impl-to-cleared-spec on the **general** §6.6 provenance contract + a `[[stale-carry-text-disposition]]` refresh of the now-falsified v1.38 honest-scope note; sacrifices no committed invariant (contrast B4 Slice 4's §14.5.3 relaxation gate), additive behavior; CP-axis driver-internal audit-emission, no nameable cross-domain tension → advisor, not council. **Decorrelated review (advisor + 2 rounds out-of-family Codex)** hardened 3 failure-path bugs against the `branch_metadata=None` override entry: (1) `_writer_ran_a_step` requires a present `branch_metadata` so an override-only writer (dispatch raised before a step entry) is not mis-counted as a ran-step on the FAILURE drain → no spurious `STEP_BOUNDARY` — **advisor**; (2) the override is buffered BEFORE dispatch on **all** strategies (orchestrator step + decentralized-handoff stage restructured: binding resolved + override buffered pre-try; orchestrator-failure drains `orchestrator_writer`; handoff stage writer joins `stage_writers` pre-try) so a failed dispatch still records the applied override — **Codex**; (3) the CASCADE_CANCEL not-yet-dispatched scan keys on the **absence of a step/terminal disposition** (`_writer_has_branch_disposition`), NOT `entry_count == 0` — else an overridden worker cancelled before dispatch (whose override makes `entry_count == 1`) would miss its `cancelled` terminal, breaking resume-idempotency (§25.15.2 obl. 4) — **Codex**. Bundled-absorption: CP spec v1.39 → v1.40 (§6.6 topology-scope-note refresh) + clearance marker + fork doc + harness-cp impl + 13 by-execution per-topology tests (real IS writer; §16.5.4 dedup + §6.3 chain `verify_chain` VALID; incl. EVALUATOR_OPTIMIZER repeated-step dedup-to-one, nested recursion, fan-out / orchestrator / stage FAILURE-with-override persistence + no-spurious-boundary, CASCADE_CANCEL overridden-worker cancelled-terminal) + spine-ledger BUILT + arc-and-unit-map §5 (4 closed · 9 remaining) + roadmap.html. Gates: pyright 0/0/0 · ruff · harness-cp 1059 + 1 xfailed · harness-runtime 1873 (non-e2e) · CXA-P1 34 · overlay 323/31-31 · substitution 54/54 · X-AL-3 green · CI 14/14. → NEXT = **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (recommended) / `B-TAIL-CONDITIONAL-SAMPLING` (gated). |
| PR #646 | 2026-06-18 | **ops(dashboard): standalone `B-*` arcs panel — full enumeration + 4-way status.** The §5 standalone-arcs panel (the dashboard source in `.harness/r-fs-1-arc-and-unit-map.md`) showed only the **8 unbuilt** family-1 arcs with **no status**, so the live dashboard never reflected which standalone arcs are *closed*. Expanded §5 to the **complete 16-arc enumeration** — reconciled against a `B-*` grep of the spine ledger + roadmap (advisor caught a dropped 16th, `B-PER-DISPATCH-DRIVER-PRECISION`) — with a **Status column** + 4-way status: **3 closed** (`B-MCP-HOST-REMOTE-TRANSPORT` #640 · `B-MEMORY-SURFACE-BACKEND-IMPLS` #642 · `B-COST-DISCRIMINATOR-TAXONOMY` #644) · **10 remaining** · **1 gated** (`B-TAIL-CONDITIONAL-SAMPLING`, R-420/R-421) · **2 resolved** (`B-PER-TOOL-SANDBOX-TIER` = frozen B6 Slice 2 #637 · `B-PER-DISPATCH-DRIVER-PRECISION` = N/A, foreclosed by B6 Option A). `generate.py` parser captures the Status cell + classifies it; masthead + panel caption split `closed/remaining/gated/resolved` — "remain" **EXCLUDES** closed/gated/resolved so neither resolved arc is double-counted; the fail-loud guard now fires on zero *open* (remaining/gated) standalone. **Mode-agnostic dashboard tooling — NO operator gate / design-substrate edit.** Decorrelated review: **advisor** (caught the dropped 16th arc) + **out-of-family Codex** ("no actionable correctness issues"; its `DESIGN_IMPL_MIX` flag was a stale-local-base artifact — real diff = 4 files, zero design-substrate). Gates: 15 dashboard tests green · ruff clean · CI 13/13 · browser-render verified (no console errors). Fixes a pre-existing RED `8==9` dashboard-test drift. Merging fired the Pages deploy → the live site now reflects the panel. | 
| PR #644 | 2026-06-18 | **feat(runtime): R-FS-1 `B-COST-DISCRIMINATOR-TAXONOMY` — dispatch-type cost rollup (`PER_DISPATCH_KIND` + `DispatchKind`; OD v1.30 §15.1 / runtime v1.57 §9 / OD plan v2.28).** The THIRD standalone `B-*` arc since the FROZEN order completed. Reconciles the latent Class-1 contract-vs-production defect surfaced + registered at arc CA: the 4 production cost helpers wrote dispatch-type strings (`"llm"`/`"tool"`/`"validator"`/`"webhook"`) into `SpanCostRecord.provider_discriminator`, but C-OD-15 §15.1 reserves that field for the cross-family `CrossFamilyTag` family tag → `rollup_costs_by_axis(PER_PROVIDER_DISCRIMINATOR)` would raise `CrossFamilyRollupError` on every production record (dormant — only synthetic-record unit tests exercised it; production uses `PER_PROVIDER_AND_MODEL`). **Spec authority decides it** — §15.1 (`Spec_Operational_Discipline_v1_2.md:870`) defines `provider_discriminator` as the family tag (a §15.3 *chain-composition* concept), so the production write was the bug; **Option A (extend `CrossFamilyTag`) REJECTED** (conflates two orthogonal dimensions). **Options B+C converge:** a NEW typed **`DispatchKind`** enum `{LLM,TOOL,VALIDATOR,WEBHOOK}` (homed in the U-OD-20 carrier — enum-typed directly, illegal states unrepresentable, no `str`+validate dance) + `SpanCostRecord.dispatch_kind` + **`RollupAxis.PER_DISPATCH_KIND`**; the 4 helpers write `dispatch_kind` + `provider_discriminator=None` (a per-dispatch helper has no chain-level family context) → `provider_discriminator: str | None`, `PER_PROVIDER_DISCRIMINATOR` **skips `None`** (error→skip — a bug-fix toward spec, no committed-invariant sacrifice); NEW optional **`RunResult.cost_attribution_by_dispatch_kind`** surfaces it non-vacuously (orthogonal single-axis partition of the same total as `cost_attribution`; minor bump, the v1.45 `pause_snapshot` precedent). **NO operator gate** (additive + bug-fix; no nameable cross-domain tension → single-voice C7 + advisor, not council). **Bundled-absorption:** OD spec v1.30 §15.1 + runtime spec v1.57 §9 + OD plan v2.28 (U-OD-21 acc #2/#3 + `Tests:` reconciliation — surfaced by the cross-spec drift grep, `[[spec-prose-plan-body-drift-pattern]]`) + fork doc + **3 clearance markers** + impl + tests. No §5.2-hash change (ephemeral run-OUTPUT — per v1.53 §9 precedent). Registered forward arc **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (§15.3 production population of `provider_discriminator` via the CP→OD seam → `PER_PROVIDER_DISCRIMINATOR` non-vacuous in production). **Decorrelated review: advisor** (affirmed the spec-reading as the load-bearing finding; sharpened the forced `provider_discriminator`-for-non-LLM sub-decision; flagged the `local_first_otlp_collector` count-coherence → resolved not-spec-pinned + led to the OD plan v2.28 U-OD-21 reconciliation) + **out-of-family Codex** ("No actionable correctness issues; wired through the carrier, production cost helpers, and RunResult construction consistently"). Gates: pyright 0/0/0; ruff clean; harness-od 950 + harness-runtime 1873 (non-e2e) + targeted cost suites 175 + CXA-P1 34; overlay 323 nodes 31/31; X-AL-3 green. → NEXT = **`B-NONLINEAR-OVERRIDE-PROVENANCE`** (recommended) / `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`. |
---

---

## Outstanding fork docs (47 total — latest additions filed by PR #434)

Sample (highest-leverage open):

| Fork doc | Class | Status |
|---|---|---|
| `class_1_fork_sandbox_tier_no_execution_driver_contract.md` | Class 1 | ✅ APPLIED-AS-BOUNDED-READING-B (status-line refreshed 2026-06-08) — R-410 supplied `ToolExecutionDriver` + local Docker provider; R-411 added gVisor/runsc; R-412 adds managed E2B full-VM. |
| `class_1_fork_tool_step_no_operator_supplied_converter.md` | Class 1 | ✅ APPLIED (PR #171, spec v1.40) — converter config surface landed (Reading B) |
| `class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` | Class 1 | ✅ APPLIED-AS-READING-B (PR #172, spec v1.41 §14.9.8) — resolver + 5 bootstrap gaps wired; AC #2 final close = operator live e2e |
| `class_1_fork_llm_cost_attribution_not_firing_on_real_dispatch.md` | Class 1 | ❌ RESOLVED-AS-INVALID (PR #168) — test-observation bug, not a defect; cost-attribution fires + writes; OD-5 retirement VALID |
| `class_1_fork_harness_toml_default_discovery_unimplemented.md` | Class 1 | ✅ APPLIED-AS-READING-A (PR #305 status refresh after PR #279 implementation) — CWD `harness.toml` discovery shipped; no longer open. |
| `class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` | Class 1 | ✅ APPLIED-AS-READING-C (PR #107, spec v1.30) |
| `class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.39, pyyaml StrictSafeLoader) — R-100-mvp-yaml-loader-shipped verified done at non-live level (41 loader/equivalence tests pass) |
| `class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.38, defer-to-runtime) |
| `class_2_fork_audit_stub_timestamp_universal_fix_plus_per_tier_annotation.md` | Class 2 | ✅ APPLIED-AS-(D) |
| *(complete enumeration at `ls .harness/class_*_fork_*.md`)* | — | — |

**Audit status:** 2026-06-07 cadence pass closed in this PR: sandbox execution-driver fork refreshed from historical PROPOSING; Path (i) durable-async fork leading status token normalized to `**Status:**`; stale roadmap sample row for the already-applied `harness.toml` fork refreshed; no other stale fork-doc headline status lines found. Next cadence: ~5 PRs or operator-discretion.

---

## Phase 7 retirement progress

> **🎓 PHASE-8 GRADUATION + BATCH-56 LIVE LEDGER.** The R-700 reconciliation is CLOSED — the operator ratified **accounting (i)** (PR #246) and lifted the declaration hold. Historical declaration: **RETIRED 46/54 (85.2%) + pipeline-advanced 49/54 (90.7%)**. Batch-52 back-flowed the live R-810/R-820 evidence for AS-8e, AS-8f, and CP-17; batch-53 back-flows the OD-4 runtime residual and the 0-wireable CXA-4 bookkeeping row; batch-54 lands the CP→AS runtime composer; batch-55 records CXA-2 as a counted bounded residual; batch-56 records CXA-1 as substantively retired after the AS→IS producer and edge-scope audit: **live ledger RETIRED 54/54 (100.0%) + pipeline-advanced 54/54 (100.0%)**. Prior batch records stand verbatim; this section reports the current live 54-row set derived from `.harness/substitutions.yaml`.

The bucket rows below sum to **54** under the batch-56 live ledger (RETIRED 54).

| Bucket | Count | Notes |
|---|---|---|
| RETIRED | **54/54 (100.0%)** live (batch-56) | 43 substantive + 8 authoring-only + 3 bounded-residual (CP-16, OD-6, CXA-2). IS 9/9; AS 11/11; CP 21/21; OD 8/8; CXA 5/5. Phase-8 historical declaration remains 46/54; batch-52 added AS-8e, AS-8f, and CP-17 after R-810/R-820 live proofs; batch-53 adds OD-4 and CXA-4 after accounting/back-flow; batch-54 adds CXA-3 after the CP→AS runtime composer landed; batch-55 adds CXA-2 as bounded residual after producer-loop evidence landed; batch-56 adds CXA-1 after the AS→IS producer and edge-scope audit landed. |
| RETIRE-READY | **0 active (bucket EMPTY post-batch-51)** | OD-3 + OD-6 transited RETIRE-READY → RETIRED at batch-51 (PR #200). |
| PARTIAL | **0** | Empty after batch-56. CXA-1 moved to SUBSTANTIVE_RETIRED after the AS→IS edge-scope audit and resolver-bound secret-fetch write landed. |
| STILL-BOUNDED | **0** | Empty after batch-55 and remains empty after batch-56. |
| RETIRED-AS-AUTHORING-ONLY | **8** (of the 54 RETIRED) | accounting (i): IS-4, IS-10, AS-9, CP-12, CP-23, OD-1, OD-7, OD-8 (accounting (ii) = 9, adds CP-24). The H_T contract is the typed declaration itself; no runtime behavior (sub-species 10 categorical-mismatch). |
| RETIRED-AS-BOUNDED-RESIDUAL | **3** (of the 54 RETIRED — *counted*) | CP-16 (memory, batch-44) + OD-6 (OTLP, batch-51 — FIRST in ledger) + CXA-2 (CP→IS durable recovery, batch-55). Production substrate dormant or post-MVP at MVP; substantive close deferred to a real deployment/recovery surface (X-AL-2 §5.3). |

**Decomposition status (R-002 RESOLVED 2026-05-31) + R-700 draft correction (2026-06-01):** R-002 mapped all non-RETIRED rows *in the per-axis `§4.1` view* to R-NNN entries — IS-2 (R-003 + R-001-h-t-is-2-retired), OD-3 (R-007), OD-4 (R-008), OD-6 (R-009), AS-8e (R-005), AS-8f (R-006); R-001 (OD-5) + R-004 (AS-8d) reconciled stale BLOCKED → RESOLVED. **The R-700 draft surfaced that the R-002 survey covered only `§4.1`, which excludes the CXA axis (no `§4.1` file) + CP-17's SB-INDEF reclassification — so 5 non-RETIRED rows (`CXA-1`/`CXA-2`/`CXA-3`/`CXA-4` + `CP-17`) have NO `R-NNN` entries.** Recommended follow-on (draft §C item 2): an `R-002`-style Surface-I decomposition pass over CXA + CP-17. **→ CLOSED at PR #246 (R-700 PART C item 2):** grounding found CXA-1/2/3/4 already had `R-CXA-1..4` (authored #209); NEW `R-010-cp-17-files-indefinite` authored for CP-17 → all 5 flagged rows now have dedicated entries. Ratified at the Phase-8 graduation (2026-06-02).

---

## Drift detection log


_Showing the 10 most recent drift/reconciliation events. The full audit history (155 events) is archived at `.harness/roadmap_drift_log_archive.md`._

| Date | Source | Resolution |
|---|---|---|
| 2026-06-18 | **Post-#651 terminating refresh — PR #651 R-FS-1 `B-INTERSTEP` (inter-step data-flow channel; runtime v1.59 §14.21 C-RT-29) merged at `a90fe979`; §12.2 owed follow-on.** | Hash `c28fb918789b` → `9c6d852700d3` (state at `a90fe979`, open PRs empty, fork count 72→73 [+1 B-INTERSTEP fork doc], batch-57). **The SIXTH standalone `B-*` arc.** A run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`; default byte-identical) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (`EVALUATOR_OPTIMIZER` the load-bearing case). Dispatch signature UNCHANGED + §25.3.3.4 preserved → NO operator gate. **Daemon-scope hardening took 4 Codex rounds (5 real bugs) + an r5 systemic residual:** invariant 7 honestly bounded to (a) sequential reuse closed by per-run `reset()`; (b) concurrent-within-drain-timeout closed by a per-loop single-flight lock (opt-out default never locks); (c) the concurrent-with-timeout `to_thread`-zombie = a SYSTEMIC reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT lock-closeable → registered `B-INTERSTEP-PERRUN-ISOLATION` (advisor reconcile: FULL-SPEC = registration not build-here; real-but-accepted, NOT looped-to-green). The advisor caught the `B-INTERSTEP-PERRUN-ISOLATION` forward-cite phantom → registered in the spine ledger before merge (`[[spine-ledger-forward-arc-registration]]`). One CI ruff-format RED (a prior commit's consumer-test file left unformatted; repo-wide gate caught what file-scoped local checks missed) fixed pre-merge. Bundled-absorption (runtime v1.59 §14.21 + clearance + fork + impl + by-execution tests incl. negative control + spine-ledger BUILT with 3 follow-ons + arc-and-unit-map 6-closed/7-remaining + roadmap.html). CI all blocking green; pyright 0/0/0; harness-runtime 1915 / harness-cp 1062; substitution 54/54. Next selector = **`B-TOOL-GATE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#648 terminating refresh — PR #648 R-FS-1 `B-NONLINEAR-OVERRIDE-PROVENANCE` (per-step override-ledger entry on all topologies; CP v1.40 §6.6) merged at `f42ed121`; §12.2 owed follow-on.** | Hash `3a819123455c` → `0488904ae18f` (state at `ec64b116`, open PRs empty, fork count 70→71 [+1 B-NONLINEAR fork doc], batch-57). #648 closed the FOURTH standalone arc but missed bumping `tools/test_dashboard_generate.py` (it parses the real arc-map §5 yet sits outside CI's axis-package pytest scope, so the 4-way-count RED went uncaught — same drift class #646 corrected). Per the **#461 precedent** (a refresh carrying a dashboard-test update can't be the clean dashboard-only pair), the test correction lands as its own commit `ec64b116` (3→4 closed / 10→9 remaining) and THIS terminating 2-file refresh pins it, restoring the §12.2.1 fixed-point shape. **The FOURTH standalone `B-*` arc.** The `cp.per-step-override-application` state-ledger entry (U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` site only; now emits on all 6 topologies via the **buffered-branch path** (`append_branch_override_ledger_entry` → each branch's `BufferingLedgerWriter`, drained on the driver thread in branch-index order, realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across topologies (branch-scoping would be an X-AL-3 spec change). Impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh) + stale-carry refresh of the falsified v1.38 note; **NO operator gate** (no committed-invariant sacrifice; additive). advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer STEP_BOUNDARY mis-count; lost-override-on-failed-dispatch; missing CASCADE_CANCEL cancelled-terminal). Bundled-absorption (CP v1.40 + clearance + fork + impl + 13 by-execution per-topology tests + spine-ledger BUILT + arc-and-unit-map 4-closed/9-remaining). CI 14/14; pyright 0/0/0; overlay 323/31-31; substitution 54/54. Next selector = **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (recommended autonomous `B-*`) / `B-TAIL-CONDITIONAL-SAMPLING` (R-420/R-421-gated). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#635 terminating refresh — PR #635 R-FS-1 arc M (Managed Agents executable-consumer contract + production wiring; C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`) merged at `ff9f9737`; §12.2 owed follow-on.** | Hash `a1a0701a1664` → `6ecec018b867` (state at `ff9f9737`, open PRs empty, fork count 67→68 [+1 arc-M fork], batch-57). **M COMPLETE — the FINAL FROZEN child arc's autonomous half (10 of 11 done; only B6 Slice 2 remains).** A NEW `StepKind.MANAGED_AGENTS` (operator-ratified Option B; closed-at-5 Class-2 extension) dispatched by a `ManagedAgentsStepDispatcher` (vendor-runs-the-loop; `SyncDispatcherFacade`-bound; surface-gated to `MANAGED_CLOUD`+opt-in) makes the R-820-live-proven `managed_agents.py` carrier production-reachable. Option A (riding `SUB_AGENT_DISPATCH`) probe-foreclosed (harness-orchestration semantics a vendor session can't honor). Decorrelated-review hardening in-arc: decoupled facade timeout (`step_timeout_seconds` 600s ≠ shared 30s — orphaned-billable-session fix) + cancel-on-give-up + `freeze()` carrier passthrough (Codex). Vendor/credential boundary: live managed-cloud run is the surfaced vendor-gate, never auto-fired; H_T-AS-8f already SUBSTANTIVE_RETIRED (R-820). Bundled-absorption (CP v1.39 + runtime v1.55 + impl + tests + fork + 2 clearance markers + arc-and-unit-map M✅ in the substantive PR per the count-change discipline). advisor (caught the gate-dissolution attempt + facade-timeout/test-gap) + Codex (freeze-drop) clean; CI 4365 passed / 14 skipped / 19 deselected (e2e) / 1 xfailed; pyright 0/0/0; overlay 322 nodes 31/31. Next selector = **B6 Slice 2 (operator-gated; drive to gate) / autonomous standalone `B-*` arcs**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#630 terminating refresh — PR #630 R-FS-1 arc B6 Slice 1 (per-MCP-transport sandbox-tier floor composition; STDIO→TIER_3; runtime spec v1.54 §14.9.8) merged at `1a7dc6e2`; §12.2 owed follow-on.** | Hash `46e6f7ed8878` → `8cb531254be3` (state at `1a7dc6e2`, open PRs empty, fork count 66→67 [+1 B6 fork], batch-57). **B6 Slice 1 COMPLETE** — the stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor` (U-AS-13) via tier-`max()` with the §14.9.8 surface default (new `compose_transport_floor`, SSOT) → STDIO ≥ TIER_3, L2-remote → TIER_4; closes the carved-out ADR-D2 §1.3 STDIO tier-3 transport-floor gap. **C10⊥C11 probe-resolved at ADR-D2 §1.3 in C10's favor** (STDIO Tier-3 regardless of blast-radius, hard floor even at local-dev) → impl-to-cleared-ADR, NO operator gate, sacrifices no invariant. Per-server-uniform PRESERVED; per-tool granularity = Slice 2. **Real out-of-box change (named):** local-dev STDIO needs a driver or bootstrap fail-closes. **Coverage loss surfaced:** in-process-STDIO e2e (`test_r100_ac2`+`test_r_cl_p3`×3) SKIPPED (pre-B6 gap ADR-D2 §1.3 forbids; R-410 covers container completion); U-RT-86 retirement evidence verified ran-and-passed unaffected. advisor (caught the decomposition refinement + a strict-raise reason-attribution bug) + Codex clean; 1821 passed / 24 skipped / 1 xfailed; pyright 0/0/0; overlay 31/31. Bundled-absorption (runtime v1.53→v1.54 + fork + clearance + spine Slice-1/2 split). Forward arcs: `B-PER-TOOL-SANDBOX-TIER` (Slice 2, operator-gated) + `B-MCP-HOST-REMOTE-TRANSPORT`. Next selector = **B6 Slice 2 (operator-gated) / B7 (unblocked autonomous)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#628 terminating refresh — PR #628 R-FS-1 arc B5 (per-deployment-surface memory backend selection; `resolve_backend` non-vacuous; runtime spec §14.12.1) merged at `ae62fdb9`; §12.2 owed follow-on.** | Hash `cf0db8f3ab05` → `46e6f7ed8878` (state at `ae62fdb9`, open PRs empty, fork count 66, batch-57). **B5 COMPLETE** — `MemoryToolRegistry.resolve_backend(deployment_surface)` made surface-discriminating (bootstrap-frozen surface→backend map via `from_surface_map`; default path resolves config-free FILESYSTEM per surface + freezes a fail-closed deferred RT-FAIL for config-less surfaces like `MANAGED_CLOUD`; ACTIVE surface must resolve or bootstrap aborts; active-surface dispatch byte-identical; anti-vacuity = constructs-vs-raises). Impl-to-cleared-spec (impl-discretion per plan v2.14 U-RT-80 AC + §14.12.7 — no fork / no design-substrate / no new contract / no CXA seam; overlay 31/31). #628 (substantive) bundled the arc-and-unit-map (B5→done 8/11, B6→next) + spine forward-arc registration (`B-MEMORY-SURFACE-BACKEND-IMPLS`: the spec'd-but-unbuilt `ENCRYPTED_FILESYSTEM`+`OPERATOR_DEFINED` backends) + regen roadmap.html in the SUBSTANTIVE PR (the #623 build-progress-source lesson → B5 = 2 PRs, not 3). advisor (design + pre-done) + Codex clean; harness-runtime 1811 passed / 10 skipped; pyright 0/0/0. Next selector = **B6 (per-tool sandbox-tier + STDIO transport-floor; fork-owed)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#626 terminating refresh — PR #626 (substantive arc-map + status refresh post-#625) merged at `35886ef9`; terminating 2-file refresh owed.** | Hash `d883470ddab1` → `cf0db8f3ab05` (state at `35886ef9`, open PRs empty, fork count 66, batch-57). #626 bundled the arc-and-unit-map (dashboard build-progress source, #623) with roadmap_status.md → a substantive dashboard refresh (outside the `hook_is_dashboard_only_set` 2-file set), so it owed this terminating 2-file refresh to reach the §12.2.1 fixed point (same shape as #623→#624). Next selector unchanged = **B5**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#625 terminating refresh — PR #625 R-FS-1 arc CA (`RunResult.cost_attribution` rollup; runtime spec v1.53 §9 C-RT-09) merged at `7861171d`; §12.2 owed follow-on.** | Hash `3c31942c5c6b` → `d883470ddab1` (state at `7861171d`, open PRs empty, fork count 65→66 [+1 CA fork], batch-57). **CA COMPLETE** — run-result cost rollup wired (`CostRecordAccumulator` holder + 4 per-dispatch wrappers + `_build_run_result` PER_PROVIDER_AND_MODEL rollup). Axis empirically forced (advisor + HEAD body-grounding overturned the sweep's PER_PROVIDER_DISCRIMINATOR lead — production dispatch-type tags ∉ `CrossFamilyTag`). Pydantic-freeze-copies-typed-list bug caught by advisor pre-merge + fixed with the by-reference holder + a freeze()-traversing regression test. Forward items registered: `B-COST-DISCRIMINATOR-TAXONOMY` (the per-family rollup) + (prior) `B-NONLINEAR-OVERRIDE-PROVENANCE`. arc-and-unit-map refreshed (CA→done 7/11, B5→next) per the #623 build-progress-source lesson. Next selector = **B5 (memory backend per deployment surface)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#623 terminating refresh — PR #623 fix(dashboard): reflect B4 completion in the arc-and-unit-map (build-progress source) + deploy trigger; merged at `d94d78d2`; §12.2 owed follow-on.** | Hash `e89a78305084` → `3c31942c5c6b` (state at `d94d78d2`, open PRs empty, fork count 65, batch-57). Operator-flagged: the published roadmap showed CONFLICTING B4 signals (roadmap_status.md sections said B4✅/CA-next; build-progress masthead/arc-strip/cards parsed from the stale `r-fs-1-arc-and-unit-map.md` said 5/11/B4-next). Root cause: the arc-and-unit-map was never refreshed through any B4 slice AND was missing from the deploy path filter. Fixed: arc-map B4→done (6/11) + as-built units + CA→next; `generate.py` 0-new-unit render; deploy path filter += the arc-map. Verified by in-browser render (6/11, B4✓, CA▶next). Next selector unchanged = **CA**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#621 terminating refresh — PR #621 R-FS-1 B4 Slice 4 (per-step ROLE override + linear-path indexing; CP v1.38 + runtime v1.52 §14.5.3 invariant-2/3 relaxation) merged at `abb67a99`; §12.2 owed follow-on.** | Hash `5c8577ce9942` → `e89a78305084` (state at `abb67a99`, open PRs empty, fork count 64→65 [+1 B4-Slice-4 fork doc], batch-57). **B4 COMPLETE** — Slice 4 closes the tail (operator-ratified §14.5.3 relaxation; Option-B composition-time fold, runtime dispatch byte-unchanged; full-stack consumer-e2e green). Next selector = **CA (cost aggregate)** — `RunResult.cost_attribution` rollup (per-dispatch built; run-result rollup + accumulator owed; small pre-authorized C-RT-09 fork). Forward item registered: `B-NONLINEAR-OVERRIDE-PROVENANCE` (Codex-surfaced; pre-existing linear-only override-ledger emission, all dimensions). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#619 terminating refresh — PR #619 R-FS-1 B4 Slice 3 (per-step PROMPT override, CP v1.37) merged at `70ca4628`; §12.2 owed follow-on.** | Hash `4d03a1931cbd` → `5c8577ce9942` (state at `70ca4628`, open PRs empty, fork count 63→64 [+1 B4-Slice-3 fork doc], batch-57). B4 Slice 3 closed; next selector = **B4 Slice 4** (per-step ROLE gate + linear-path role indexing). Session-start hook had flagged expected mid-arc feature-branch drift (computed `f42f967c144c` ≠ dashboard `4d03a1931cbd`) — resolved by this post-merge refresh, not a workspace divergence. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-10 | **Claude release-candidate handoff authored locally at `a5215dde`; post-handoff refresh.** | Hash `e13d42d7874c` → `dbfe6623fc76` (state at `a5215dde`, open PRs unavailable/empty, fork count 47, batch-56). Added `.harness/release-candidate-deployment-readiness-runbook.md` and Claude/context pointers. Next selector is the HIL release-candidate deployment-readiness scope gate, then provider-free readiness before any live deployment smoke. |
| 2026-06-10 | **Overlay-check ignored-artifact fix merged locally at `d4b6b4e3`; post-merge refresh.** | Hash `c28613a1f0a3` → `e13d42d7874c` (state at `d4b6b4e3`, open PRs unavailable/empty, fork count 47, batch-56). `overlay-check` now ignores stale gitignored `tools/semantic_overlay/overlay.json` artifacts while preserving the hard gate for stale tracked snapshots. Next selector moves to an HIL release-candidate deployment-readiness scope discussion before opening new implementation. |
| 2026-06-10 | **Post-#447 terminating refresh — PR #447 BMad runtime config restore merged at `9cf968d2`; §12.2 owed follow-on.** | Hash `48f9537038cf` → `f9c6f1ebe73d` (state at `9cf968d2`, open PRs empty, fork count 47, batch-56). `_bmad/bmm/config.yaml` is restored on main; no roadmap item status changes beyond clearing the last in-flight PR. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **PR #440 supersession / post-#464 open-PR refresh branch opened from `d24dcf57`.** | Hash `78617c29740a` → `48f9537038cf` (state at `d24dcf57`, open PRs #447 only, fork count 47, batch-56). #440 was closed as superseded: it was conflicting, predated the later R-CXA-2 implementation/back-flow sequence, and a current-main comparison showed it would remove later R-CXA-2 implementation, tests, batch records, and dashboard/overlay updates if merged. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Post-#463 terminating refresh — PR #463 R-IF-112 semantic overlay closeout merged at `925f021a`; §12.2 owed follow-on.** | Hash `ebb6d51193e6` → `78617c29740a` (state at `925f021a`, open PRs #440/#447, fork count 47, batch-56). R-IF-112 is now RESOLVED: `tools/semantic_overlay` derives the design-substrate contract keyspace, source cite layer, CXA seam endpoint graph, and substitution join; CI gates hard CXA/stale-artifact drift and reports advisory code/contract/substitution orphan classes. Next selector moves to this terminating refresh, then recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Substantive R-IF-112 implementation closeout branch opened from `62f9a327`; §12.2 owed after merge.** | Hash `a0ddc2154026` → `ebb6d51193e6` (branch base `62f9a327`, open PRs #440/#447, fork count 47, batch-56). The existing semantic overlay linter is completed against the roadmap must-pass by adding design-substrate `C-*` keyspace scanning and advisory `contract_without_code` orphan reporting, alongside the existing code-without-cite, CXA missing-endpoint, and substitution-without-carrier classes. `R-IF-112` is moved to RESOLVED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |
| 2026-06-09 | **Post-#461 corrective fixed-point refresh — PR #461 post-#460 roadmap/status/dashboard refresh merged at `41297c2a`; §12.2 anchor re-pin.** | Hash `b958e3438c97` → `a0ddc2154026` (state at `41297c2a`, open PRs #440/#447, fork count 47, batch-56). PR #461 completed the post-#460 refresh but included a dashboard test update, so the default-branch guard could not classify the merge as the expected status/dashboard-only lag. This two-file refresh restores the fixed-point shape and keeps the selector on `R-IF-112` plus recurring R-600/R-IF lanes. |
| 2026-06-09 | **Post-#460 terminating refresh — PR #460 R-CXA-1 AS→IS edge-scope/sidecar closeout merged at `b9a9ec87`; §12.2 owed follow-on.** | Hash `39009757fd12` → `b958e3438c97` (state at `b9a9ec87`, open PRs #440/#447, fork count 47, batch-56). R-CXA-1 is now merged closed; the live substitution ledger is 54/54 RETIRED and 54/54 pipeline-advanced. Next selector moves off CXA build-close work to this terminating refresh, then the post-substitution governance/process frontier (`R-IF-112` if operator selects a new substantive arc; recurring R-600/R-IF lanes by cadence). |
| 2026-06-09 | **Substantive R-CXA-1 AS→IS edge-scope/sidecar branch opened from `22d7d53`; §12.2 owed after merge.** | Hash `ffe9044984c8` → `39009757fd12` (branch base `22d7d53`, open PRs #440/#447, fork count 47, batch-56). Filed `.harness/r-cxa-1-as-is-edge-audit-2026-06-09.md`, threaded the stage-5 procedural-tier resolver into the production `RuntimeAsIsWiring` secret-fetch callback, and moved H_T-CXA-1 from PARTIAL to SUBSTANTIVE_RETIRED. Live ledger is now 54/54 RETIRED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |

---

## Audit checklist (run at session start)

- [ ] `workspace_state_hash` matches computed value per recipe above
- [ ] All open PRs at GitHub appear in `In-flight`
- [ ] `recently_completed` reflects last 5 merged PRs
- [ ] `latest_retirement_batch` matches `ls .harness/phase-7d-retirement-events-batch-*.md | tail -1`
- [ ] No `R-NNN` at `Project_Roadmap_v1.md` §5 has `status: ACTIVE` while its `depends_on` are not all RESOLVED (re-derive per §4)

If any check fails → HALT, route per `Project_Roadmap_v1.md` §7.1 step 4.

---

*End of dashboard. Master roadmap at `Project_Roadmap_v1.md`. Enforcement at `CLAUDE.md` §12.*
