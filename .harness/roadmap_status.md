# Roadmap status dashboard

*Refreshed at session-start audit + post-PR-merge audit per workspace `CLAUDE.md` §12. Consumed by the next-action derivation rule at `Project_Roadmap_v1.md` §4. **Do not hand-edit during execution — refresh via the protocol at `Project_Roadmap_v1.md` §7.2.***

---

## Workspace state anchor

| Field | Value |
|---|---|
| `workspace_state_hash` | `9fe4085e2713` |
| `last_refreshed` | 2026-06-06T20:00:48-06:00 |
| `git_head` | `b430dd19` (main) — post-PR #314 Stop-hook JSON fix + PR #315 R-410 execution-gate record. |
| `latest_retirement_batch` | `.harness/phase-7d-retirement-events-batch-51.md` |
| `open_fork_doc_count` | 45 |

**Hash recipe.** `sha256(git_head[:8] + "|" + sorted_open_pr_csv + "|" + open_fork_doc_count + "|" + latest_retirement_batch_path)[:12]`. See `Project_Roadmap_v1.md` §7.1.

---

## Next action

> **🚦 STANDING DIRECTIVE — operator-labeled items are Claude-driven; NO parking (2026-06-02; CLAUDE.md §12.4.1, `[[feedback-operator-labels-are-claude-driven-no-parking]]`).** "No auto-derivable Claude-executable ACTIVE row remains" is **NOT** a stop condition. The operator executes NOTHING manually — Claude does all the work. When the auto-`ACTIVE` queue is empty, **pick the highest-value forward item** (drivable now: `R-410..R-440` sandbox/deploy/secrets slices, `R-CXA-2` engine-substrate, `R-810/820` files/managed-agents, `R-830` memory backend — each has a Claude-executable slice grounding reveals), **ground it, build its slice to the genuine gate**, and surface only a real decision / credential / paid-call / irreversible action (batched, minimal). Don't re-stop here citing "operator-owned." *(R-700's declaration was HELD; the operator LIFTED the hold 2026-06-02 — Phase 8 is now CLOSED, see banner below. `R-300-multi-llm-second-provider` B-2 is now fully closed by PRs #281 + #283.)*

> **🎓 PHASE 8 CLOSED — substitution accounting DECLARED 2026-06-02 (`.harness/phase-8-graduation.md`).** The operator lifted the HELD R-700 declaration ("kick off the declaration"); item 4 executed. **Canonical: 46/54 RETIRED (85.2%) + 49/54 pipeline-advanced (90.7%)** (operator-ratified accounting (i)). The deferred 48→46 supersession is applied forward (ledger §11.7 + harness-cp/CLAUDE.md + this dashboard; prior batch records stand verbatim). 8 rows carry ratified terminal sign-off dispositions (OD-4 `RETIRED-AS-CROSS-AXIS-DEFERRED`; AS-8e/8f/CP-17 accepted-indefinite-defer; CXA-1..4 Phase-2-runtime-deferred) — **labels do NOT re-tally into the 46** (the OD-6-in-both-lists tell). `R-700` → RESOLVED. Phase 7 (substitution-retirement execution) is complete; the workspace is now wholly on the **post-Phase-8 activation/deployment/integration axis** (Surfaces IV/V/VI). **The forward menu's un-built no-creds slices are exhausted** (R-410 design half filed PR #256; everything else gates on operator creds/infra, or a dispositioned design arc) — per §12.4.1 a future session drives the highest-value forward item to its genuine gate.

**(This session — 2026-06-06. PRs #314 + #315 MERGED.)** PR #314 fixed the Codex Stop hook protocol: `.codex/hooks/stop_gate.py` now emits valid Stop-hook JSON (`continue` + `systemMessage`) instead of raw posture text on stdout, with a regression test at `tools/test_codex_stop_gate.py`. PR #315 logged the honest R-410 execution gate: runtime sandbox tier resolution remains metadata/span attribution only, while TOOL_STEP execution still flows through in-process FastMCP; the gate ledger, roadmap note, and dashboard snapshot now record that the next real R-410 slice is to resolve the execution-driver reading, use the operator-approved local container runtime, and run the minimal TIER_2 container TOOL_STEP e2e. Both PRs were CI-green before merge.

**(This session — 2026-06-06. PR #312 MERGED.)** Closed the concrete loop-hardening bug surfaced by the post-#311 re-grounding: `hook_roadmap_next` no longer treats roadmap range/menu tokens such as `R-410..R-440` as actionable next units. It now scopes to `## Next action`, strips backticks, skips `..` range tokens, and returns the first concrete `R-*` token. Regression test flipped red first (`R-410..R-440` incorrectly returned), then green after the fix. This prevents SessionStart / prompt-context / stop-loop from injecting a broad gated range as the next item and re-entering the false forward-menu loop. PR #312 CI was fully green.

**(This session — 2026-06-06. PRs #306 + #307 MERGED.)** PR #306 fixed the dashboard generator so exact-ID `RESOLVED` / `CANCELLED` roadmap actions no longer linger in the manual "remaining" list. PR #307 then closed stale R-300 tracking: `R-300-multi-llm-second-provider` / forward-register B-2 is now marked RESOLVED from the already-merged PR #281 deterministic + live OpenAI evidence and PR #283 live Ollama evidence. The dashboard remaining list no longer includes R-300; no paid live provider recipe was rerun in this refresh. This is the owed post-merge terminating refresh recording those two PRs.

**(This session — 2026-06-06. PRs #303, #304, #305 MERGED.)** The operator ratified Reading A for `R-100-mvp-config-discovery`; grounding showed CWD `harness.toml` discovery had already shipped in PR #279 (`a394032`), so the stale fork/roadmap/register tracking is now closed at PR #305 (`b8c4671`). Two prerequisite guard/dashboard fixes landed first: PR #303 exempted only the generated dashboard snapshot from design/implementation mix classification while keeping dashboard sources guarded, and PR #304 made system-Python dashboard regeneration pin the retired-trend endpoint to the canonical substitution ledger snapshot even without PyYAML. All three PRs were CI-green. This refresh records that post-merge state; next action remains the post-Phase-8 forward menu under the no-parking directive.

**(Prior this session — 2026-06-06. PR #302 MERGED.)** Codex context guard hardening landed at `a038c148` after the operator-requested OpenAI API adversarial review found deployment blockers in the initial guard. The guard now checks committed PR ranges via `--base-ref/--head-ref`, local clean feature worktrees via `--include-branch-diff`, keeps default-branch dashboard drift hard even when drift allowance is passed, emits a dedicated expected-lag warning, broadens design/implementation surface classification to Codex hooks/tools/CI/justfile + class-3/architect surfaces, and surfaces unavailable `gh pr list` context explicitly. All PR #302 checks passed, including the Codex context guard CI job and advisory coverage.

**(Prior this session — 2026-06-05. PR #301 MERGED + post-merge tracking close committed.)** Codex deterministic context guard is live: `just codex-preflight` / `codex-checkpoint` / `codex-closeout` materialize repository state from HEAD, Codex hooks propagate hard guard findings, local ignored context checkpoints enforce freshness at closeout, and credential-gated work now has a deterministic ledger path (`.harness/codex_credential_gates.jsonl`) with redaction + required human-facing tracking. PR #301 merged at `9549e68`; follow-on tracking close `b7179ad6` marked `R-600-codex-context-guard` RESOLVED and regenerated `tools/dashboard/roadmap.html`. This refresh records that post-merge state; next action remains the post-Phase-8 forward menu under the no-parking directive.

**(This session — 2026-06-05. Reconciliation pass (operator-requested, autonomous; bg-isolation guard disabled via operator AskUserQuestion). Recorded the un-refreshed `a60222d` brainstorming doc + closed the drift; the operator's in-flight manual reorg is NOT committed by this refresh.)** The §12.1 audit found genuine drift: `a60222d` (the 2026-06-04 brainstorming doc) landed on top of the `4f59e0f` "post-#296" refresh **without a subsequent refresh** (tip is a non-refresh commit → carve-out step 6 correctly did NOT fire). PR #293 (BMAD skills) confirmed **already MERGED** (`6ed8416`); #296 (worktree live-session guard) + `ca2bc34` (ICM+BMAD-METHOD) already on origin. This refresh records the `a60222d` state: `workspace_state_hash` `99f5e956ad86` → **`2c9c6bb9e438`** (verified via canonical `hook_state_hash` recipe), `git_head` → `a60222d0`. **The operator's manual reorg LANDED at `chore(reorg)` `cfc6cc4`** (operator-confirmed via AskUserQuestion after the bg-isolation guard was scoped-disabled in gitignored `settings.local.json`): 118 files — git-detected renames preserved content (research/ → `.harness/01-planning/01-harness-planning/00-harness-research/`; tools/loop → `tools/04-loop/`; council workflow YAMLs → `.claude/workflows` + `.claude/skills/council/workflows`; brainstorming doc + ICM audit relocated), 61 deletions (council deliberation tree productized-into-#295-then-pruned + `_bmad` config prune). **Grounding caught + fixed a break before it landed:** the tools/loop→tools/04-loop move had stranded 6 executable refs (justfile `just loop`, permission-guard allowlist regex, stop-loop, loop_lib, 2 hook test suites) — all repointed, **4 loop suites green** (permission_guard 112 / stop_loop 21 / loop_wrappers 9 / loop_run 16). CLAUDE.md §10.9 `research/` path refs → `.harness/01-planning/`. `.claude/audio/` gitignored (ephemeral TTS cache, NOT committed). **No worktree deleted; no stash popped; spec-writer session untouched; `design-substrate/` untouched.** This 2026-06-05 reconciliation refreshed the dashboard TWICE: first recording `a60222d` (`2c9c6bb9e438`), then this post-reorg state (`0ef1b006d905` at `cfc6cc4`). Next forward item per §12.4.1 stays `R-410..R-440` (infra/creds-gated). This is the owed terminating refresh; the next §12.1 audit recognizes the expected lag-by-one. **Prior arc below:**

**(Prior this session — 2026-06-04. Installed 5 BMAD analysis/planning skills + vendored `_bmad/` runtime: PR #293 MERGED `6ed8416`. Follow-on refresh recording what the parallel post-#294 refresh missed.)** Operator-requested install of the BMAD-METHOD analysis (`bmad-technical-research`, `bmad-product-brief`, `bmad-prfaq`) + planning (`bmad-agent-pm`, `bmad-prd`) skills — the exact 5 URLs, **not** the full repo. Skills → `.claude/skills/bmad-*/` (Claude convention → invocable). Their parent-repo dependency vendored (option #1 — no skill edits) into an **ICM-hygiened** `_bmad/` at repo root (MIT stdlib `scripts/resolve_customization.py` + `bmm/config.yaml` + `custom/` + git-ignored `artifacts/` + `CONTEXT.md`/`README.md`); `_bmad` added to the ruff exclude; 16 optional `bmad-*` cross-refs documented as no-ops. 39 files; resolver smoke-tested all 5; all 12 CI checks green; worktree hygiene done. **Mode-agnostic tooling, NOT an R-NNN item.** **Multi-agent collision note:** #292/#293/#294/#295 all merged ~concurrently from parallel sessions; the parallel `1f9bf76 post-#294` refresh recorded only #294, so **this refresh records #293 + #295** + clears the stale in-flight #295 + re-pins the anchor to the current tip (`343fe45`/#295). The session-start carve-out (`session-start.sh` title-prefix + roadmap_status.md-only) was already satisfied by `1f9bf76`; this is a completeness follow-on. `roadmap.html` regen owed as a separate hygiene commit (the parallel refresh also skipped it). **Prior arc below:**

**(Prior — 2026-06-04, post-#291 continuation → R-IF-112 stage-04 (agent-workflow integration) SHIPPED: PR #294 MERGED `cc3f0b5`.)** With the overlay BUILT (#291), the operator asked to document *where in the agent workflow* it fits + *how agents use it*. Per a 3-question AskUserQuestion the operator chose: **new skill · doc + light wiring · query-CLI focus.** Delivered (mode-agnostic, 4 files): **NEW `.claude/skills/overlay-query/SKILL.md`** — query-CLI-focused, a *query-mode → workflow-moment* map (`--contract`/`--unit`/`--seam`/`--file`/`--substitution`/`--orphans`/`overlay-check`), posture-neutral, with an explicit **Scope** caveat (derives from code docstring cites + `substitutions.yaml` + `PATTERN_P1_SEAMS`; does NOT scan `design-substrate/**` sibling specs); **CLAUDE.md §13.1** names the overlay as the instrument for *empirical cite-grounding* + a *complementary code-side* check for *cross-spec drift grep* (NOT a replacement for the sibling-spec `rg`) + **§13.5** cross-ref; **roadmap-continue step-3** grounding clause; **R-IF-112 dev-ledger** stage-04 note. **Out-of-family `just codex-review` (gpt-5.5, subscription) earned its keep** — caught a real over-claim (overlay positioned as the cross-spec-drift *replacement*) → reframed to complementary + Scope caveat; re-ran to convergence on my diff. 12/12 blocking CI green (incl. semantic-overlay drift gate). 2 memory entries added (`overlay-query-agent-workflow`; `git-diff-pathspec-bad-revision` — the rtk git-rewrite strips `--` pathspecs). **R-IF-112 stages 01–04 COMPLETE.** This is the owed terminating refresh recording substantive #294; the next §12.1 audit recognizes the expected lag-by-one. **Prior arc below:**

**(Prior this session — 2026-06-04 `/goal` → R-IF-112 stages 02-design + 03-build SHIPPED: PR #291 MERGED `560afa3`. The spec↔code↔CXA↔substitution semantic overlay is BUILT.)** The operator set the goal "build the spec/CXA/substitution semantic layer overlay onto the code graph of Understand Anything to surface a powerful codebase reference tool for agent work." Grounded on the stage-01 seed (R-IF-112) + advisor: **one Understand-Anything-format graph artifact, three consumers** (dissolved the linter-vs-viz split). Shipped `tools/semantic_overlay/` (670 LoC `overlay.py` + 166 LoC tests/14 pass + README) — deterministic, no-LLM, sibling to `substitution_ledger.py`: **(query CLI)** agent-facing `query --contract/--unit/--seam/--file/--substitution/--orphans` (cross-axis `--seam` resolves consumer↔producer file/axis+symbol — the layer code↔code graphs are blind to); **(dashboard)** `enrich-ua` left-joins overlay attrs + `cxa_seam` edges onto a UA `knowledge-graph.json`; **(`--check`)** new blocking CI `semantic-overlay` drift gate. Four layers joined: docstring spec-cites (287/291=98.6%, 120 contracts, 286 units), the **31 code-resident `PATTERN_P1_SEAMS`** (NOT the delta-only CXA markdown — v2.19 holds 0 physical rows, the `[[wrong-version-read-delta-only-baseline]]` hazard), `substitutions.yaml` (honestly-thin 9/55 H_T-carrier join), code structure. Surfaced 4 real code-without-cite orphans. **advisor pre-build (track-confirm) + pre-done (caught the un-navigable seam layer → added `--seam`/`--file`-resolved-endpoints).** Honest caveats stated: cites are *presence* not implementation-correctness; cross-package seams ride `overlay.json`+CLI not per-package enrich-ua. `overlay.json` gitignored (a committed derived snapshot would manufacture the drift it detects — stage-01 finding #2). X-AL-3-clean (reads design-substrate, never amends). **R-IF-112 stages 01–03 COMPLETE.** This refresh records substantive #291; the next §12.1 audit recognizes the expected lag-by-one. **Prior arc below:**

**(Prior this session — 2026-06-04, dashboard-driving → seeded the spec-code-overlay arc. PR #288 merged `89c20bb`; R-IF-112 registered.)** Operator drove the local understand-anything dashboard (the `harness-is` knowledge-graph) and asked whether it would serve agents in-loop. Honest assessment: the GUI is high-friction + staleness-prone for agents (the very drift we fight), and its graph models **code↔code only** — blind to this repo's load-bearing **spec-contract ↔ code ↔ CXA-edge ↔ substitution** layer. A live probe found that traceability layer is **~99.6% (277/278 source files carry a parseable authority cite; 286 `U-*` IDs; full `C-*-NN` keyspace)** and **deterministically extractable** (no LLM pass) — grounding a cheap `tools/` orphan-linter (sibling to `substitution_ledger.py`) as the overlay's highest-leverage core. Seeded a **canonical-ICM** arc at `.harness/spec-code-overlay/` (root `CONTEXT.md` → `stages/01-exploration/{CONTEXT.md, output/}`; layout grounded on `RinDig/Interpreted-Context-Methdology`) — PR #288 — and registered **R-IF-112** (Surface VII; PROPOSED; forward stages 02-design + 03-build). A first draft was authored into the council context-memory worktree then **relocated** out (it's overlay-tooling, not council evidence). This block records substantive #288 + R-IF-112; a follow-on **terminating refresh is owed** (§12.2.1) to reach the fixed point. **Prior arc below:**

**(This session — 2026-06-03, post-R-300 continuation. PREPARED the loop/HIL HARDENING dynamic-workflow context package — operator-requested; merged #285 `4a786a8`. NEXT ACTION: operator `/clear` → prompt `begin workflow`.)** The operator probed whether the loop's hook/skill/advisor disciplines are actually being run; honest audit found **~13 disciplines rely on Claude RECALL (MANUAL); only ~5 are hook-AUTOMATIC** — which is why this session lapsed (codex-review skipped on both R-300 merges, advisor under-used, `/resolve` unused, the defer-and-halt error, cwd-split worktree failures). Operator directed a **deep-dive dynamic workflow** to *definitively harden the HIL + loop-run workflow*, run from a FRESH context window. This session assembled the complete, verified context package at `.harness/hardening-workflow/` (`BRIEF.md` + `inventory-hooks-skills-disciplines.md` [every hook/skill/discipline classified AUTO-vs-MANUAL, cited] + `session-evidence.md` [this session's lapses = ground-truth test set] + `references/claude-code-hooks.md` [authoritative hooks capability ref, fetched live] + `references/insights-report-2026-06-03.md` [201-session corroboration: D1/D6/D7 recurring + NEW D14 output-token-limit] + `workflow.js` [audit→adversarial-verify→synthesize→critic over D1..D14 → `HARDENING_PLAN.md`]) + the `begin-workflow` skill (`.claude/skills/begin-workflow/`). Assembled via 4 parallel collectors + a live doc fetch + an adversarial verification pass (PACKAGE-ACCURATE on substance — all file:line cites + the D6 paid-call deny-vs-proceed contradiction confirmed). **To run:** from the **main checkout** (`/Users/robertrhu/Projects/arhugula-v2`), `/clear`, then prompt **`begin workflow`** (or `/begin-workflow`); the skill loads the brief + launches `workflow.js` (multi-agent; produces `HARDENING_PLAN.md`). Scope: hardens the **U-HK loop/HIL process machinery only** — NOT H_T product code or design-substrate. **Held items:** none — refresh PRs #282/#284 + package #285 all merged. This refresh records substantive #285; the next §12.1 audit recognizes the expected lag-by-one. **Prior arc below:**

**(Prior this session — 2026-06-03 `/loop-start` continuation, post-#282 fixed-point. Operator CORRECTED the loop's gate-handling; R-300 second-provider B-2 FULLY CLOSED — OpenAI + Ollama both exercised LIVE: PRs #281 + #283.)** **Operator correction (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]` LOOP-MODE OVERRIDE):** I had wrongly *deferred* the R-300 live paid run + treated it as a halt. The actual loop rule: **`/loop-start` IS the standing authorization — creds available → PROCEED (run the paid call); a missing dep (cred OR any HIL-provided gap: daemon/infra/ratification/backend) → log it DEFERRED-HIL + CONTINUE to the next unit; halt ONLY on HIL-stop or genuinely zero buildable units.** Applied: **(1)** ran `just mvp-r300-cross-family` from main's `.env` → **LIVE PASS 4.55s** (real anthropic 404 → real openai `gpt-4o-mini`; `gen_ai.provider.name=openai` on the success span) — R-300's OpenAI half live-validated, not deferred. **(2)** the Ollama half was NOT HIL-gated after all — the local ollama daemon (`127.0.0.1:11434`, `llama3.2:3b`) is a **free** provider → built + ran `test_r300_live_ollama_provider_fallback_exercise` (PR #283 `e436252`): same-family ollama fallback (invalid-model → `llama3.2:3b`) through the real `api.run` path, **LIVE PASS 4.17s**, zero-token/zero-secret. Incidental test-only finding: `ProviderAgnosticPayload.params` is verbatim provider-specific pass-through (anthropic/openai `max_tokens`; ollama `options={num_predict}`) — not a prod bug; the ollama test supplies the ollama shape. **R-300-multi-llm-second-provider B-2 now FULLY exercised** (deterministic CI-green + OpenAI live + Ollama live). **Remaining forward units are genuinely HIL-gated** (re-grounded, not a stale exhaustion claim per `[[feedback-reground-forward-work-dont-trust-stale-slice-exhausted]]`): R-410 sandbox tier-2 (design fork #256 PROPOSING — X-AL-3-blocked driver + container-runtime infra), R-420/421 deployment e2e (real server/cloud), R-430 OTLP collector validation (real collector), R-440 secrets backend (real backend), R-500 multi-tenant (multi-tenant deploy); R-CXA-2 engine-substrate-gated (STRUCK U-RT-111); R-810/820 deferred-by-design. Their HIL-deps are logged to the loop ledger + surfaced — the loop pauses here at the genuine "zero free-buildable units" terminal (NOT a premature halt; the operator lifts an infra/ratification gate to resume). This refresh records substantive #283; the next §12.1 audit recognizes the expected lag-by-one. **Prior arc below:**

**(Prior this session — 2026-06-03 `/clear`→`/loop-start` (loop ON), post-#280 fixed-point. R-300 second-provider B-2 cross-family fallback exercise SHIPPED: PR #281 MERGED `2dc25e6`.)** Loop mode activated; the §12.1 audit recognized the expected lag-by-one (carve-out step 6; main @ `0d60cc1`/#280). Per §12.4.1 drove the dashboard's named next build — **R-300 (B-2) cross-family fallback exercise** — re-grounding first per `[[feedback-reground-forward-work-dont-trust-stale-slice-exhausted]]`. **Grounding flipped the prior "declarative-only" framing favorably:** the `llm_dispatch.py:30` "fallback out of scope" note describes only the *inner* bare C-RT-15 dispatcher; the production `ctx.llm_dispatcher` is the *outer* `RetryBreakerFallbackDispatcher` (C-RT-16/U-RT-58, bound at `stage_5_loop_init.py:279` to `config.routing_manifest.fallback_chains[0]`) which **does** walk the chain (primary → `advance_or_raise` → cross_family). So a cross-family fallback exercise via `api.run` is real, not declarative-only. **advisor reframed the slice into stronger loop output:** ship a **deterministic** counterpart (CI-green, no creds) running the real `api.run` path with **fake SDK *leaf* clients** (anthropic raises → openai canned success) at the stage-3a `materialize_provider_clients_stage` seam — chain / classification / `advance_or_raise` / bootstrap / dispatcher all REAL — proving the cross-family fallback *plumbing* end-to-end NOW (anthropic primary fails → advances → openai answers → workflow completes; both providers on the captured `gen_ai.provider.name` spans + outer fallback span `chain_length≥2`). Faking provider *leaves* (not the dispatcher/host under test) is standard, NOT `test-bypass-as-runtime-truth`. The **live** confirmation (skipif on both keys; primary anthropic invalid-model 404 → real openai `gpt-4o-mini`, via `just mvp-r300-cross-family`) is the real-provider check on top, **DEFERRED to the operator** (paid run; logged DEFERRED-HIL per `[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`). Verified: deterministic PASS 0.5s; live SKIPPED without keys; ruff + format clean; pyright strict 0/0/0; all blocking CI green; no production code touched. This refresh is the owed terminating refresh recording substantive #281; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. **Forward menu:** R-300-second-provider's deterministic slice is now LANDED; its live e2e + the Ollama branch remain operator-gated; the other un-built no-creds slices stay thin (R-410 design fork filed #256 awaiting A/B/C ratification; R-CXA-2 engine-substrate-gated; R-810/820 deferred-by-design). **Prior arc below:**

**(Prior this session — 2026-06-03, post-#277 continuation. R-100 MVP fully validated LIVE + A-4 config-discovery SHIPPED (#279); the "slice-exhausted" framing CORRECTED.)** The operator challenged the post-Wave-4 "no buildable units" conclusion — correctly. Re-grounding the forward register (`.harness/post-phase-8-forward-register.md`) + the actual gates surfaced three corrections. **(1) A real no-secrets build existed:** `harness.toml` auto-discovery (A-4) — spec §3.7 declared it, `DEFAULT_CONFIG_FILE_NAME` had zero use-sites → BUILT as Reading A (CWD discovery), merged **#279** (`a394032`), closes `R-100-mvp-config-discovery`; 4 tests + hermetic CWD isolation; Codex round-1 clean; CI green (pyright/pytest/ruff). **(2) The provider secrets ARE available** — `.env` is gitignored so it lives in the **MAIN checkout** (not worktrees, which is why a worktree `[ -f .env ]` missed it); `just`'s dotenv-load supplies `ANTHROPIC_API_KEY` + `OPENAI_API_KEY` (+ `E2B_API_KEY` for R-410's cloud sandbox, `GEMINI_API_KEY`). So "creds-gated" is **wrong** for Anthropic/OpenAI items — they run with paid-call authorization. **(3) R-100 is now FULLY validated live** (operator-authorized paid runs): `just mvp-r100-real` (AC#1/#3/#4, real Anthropic Haiku 3-step → PASS 4.06s) + `test_r100_ac2_tool_step_e2e` (AC#2 / "Gap D" tool-step via `api.run` → PASS 2.95s) — **R-100's last open gate closed.** **Standing correction (`[[feedback-reground-forward-work-dont-trust-stale-slice-exhausted]]`):** do NOT re-assert a prior session's "slice-exhausted" without re-grounding the forward register + the secrets-via-`just` reality first. **Next build (operator-authorized, in progress):** R-300 cross-family fallback exercise (B-2) — needs a live test BUILT (`primary=anthropic`/invalid-model → `cross_family=openai`, assert fallback); both keys present. This refresh records #279 + the R-100 live closure. **Prior arc below:**

**(Prior this session — 2026-06-03 `/clear`→`/loop-start` (loop ON). Wave 4 of the hooks-driven autonomy plan SHIPPED: U-HK-26..29 via PRs #274/#275/#276/#277 → `66e672e`.)** Loop mode was activated; the §12.1 audit recognized the expected lag-by-one. The operator opened a **hook/skill/advisor reconciliation** (`.harness/hook-advisor-workflow-review.md`): the autonomy *back-half* (cleanup / hygiene / context-recovery) had been collapsed to advisory-only by applying "destructive ops stay explicit" *uniformly* — correct for HIL, wrong for the unattended loop. **Wave 4 ("build little, reconcile much") closed it:** U-HK-26 loop-mode **worktree GC** (#274; worktrees-only, gh-`headRefOid` exact-identity, ignored-aware [never deletes `.env`/`harness.toml`], fail-safe, self-excluding; **8 out-of-family Codex rounds / 11 genuine bugs** — highest-blast-radius unit, it deletes worktrees) + SessionStart hygiene visibility; U-HK-27 **context-recovery StatusLine** (#275; proactive checkpoint at 60/75/85% — corrects the §9 "R-3 infeasible" claim, StatusLine *does* expose `context_window.used_percentage`; chains the operator's themed statusline); U-HK-28 `ruff format --check` + hook **timeouts** (#276); U-HK-29 **doc-reconciliation** (#277; review-doc §10 closure + CLAUDE.md §12.5.3). **Operator-supplied research (GitHub `anthropics/claude-code#6428`) established `SessionEnd`-on-`/clear` is unreliable** → *validated* the SessionStart-routing design (the cleanup that matters never depended on SessionEnd). **Architecture synthesis (operator surfaced SFAs + the Ralph loop):** `tools/loop/run.sh` is **already a Ralph loop** with all four guardrails (scope=roadmap / backpressure=CI+hooks / completion=`.loop-halt` / stuck=`defer.sh` / cap=`HARNESS_LOOP_MAX`); SFAs are the right primitive for the **data-pipeline domain**, not the deterministic harness cleanup (which stays hooks). **Readiness: unit-hardened + Codex-reviewed; ready for a SUPERVISED first autonomous run** (iteration cap on, watched — per the Ralph source: "$500 bill" without `MAX_ITERATIONS`; "80% is watching"). Status record: `.harness/wave4-hooks-status.md`. This refresh is the owed **terminating refresh recording #274–#277**; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. *(The "forward menu creds/infra-gated" claim that stood here was CORRECTED at the lead block above — A-4 was a buildable no-secrets unit, and the provider keys are in main's `.env`.)* **Prior arc below:**

**(Prior — 2026-06-03, post-#271 fixed-point. Corrected the autonomous loop's gate-handling to DEFER-AND-CONTINUE: PR #272 MERGED `d11d2b5d`.)** During a pre-run readiness review the operator flagged that the loop's ratified design (plan decision #1: "missing creds/vendor → log to loop-status, **keep working around**") is **defer-and-continue**, not halt-at-first-gate. The shipped Wave-2 cut wrongly instructed Claude to raise `.loop-halt` + stand down the WHOLE run at the first gated item (an internal contradiction — the `/loop-start` + `/resolve` skill docs already said "keep working on other items"). **Fixed (U-HK-14/15):** at a gate, build the un-gated slice → `tools/loop/defer.sh <item-id> '<what input is needed>'` → ADVANCE to the next forward item per §12.4.1; `.loop-halt` (via `tools/loop/halt.sh`) reserved for TRUE exhaustion (every forward item deferred) or operator stop. **Mechanical skip-set** (advisor's load-bearing point): the persistent `loop_status.md` DEFERRED-HIL rows are injected into the continue-reason + computed by `run.sh` per iteration, so a fresh headless `claude -p` child never re-attempts a deferred item off the static dashboard pointer (the overnight burn-the-cap-on-one-item failure). **Operator surfacing:** `loop_pending_hil_summary` is appended at every SessionStart so the last run's deferrals are clearly presented at next engagement. Tool-level hard-stop UNCHANGED. **Driven through `just codex-review` to convergence — 4 rounds of real findings → round 5 clean** (malformed/denied `source A B` → executable allowlisted wrappers; `$VAR` secret-leak into ledger + skip-set over-match → `$VAR` rejected + leading-id-only; deferral-reason-names-gate tripped the deny-list → wrapper short-circuit before the free-text scan; "ACTIVATE" in a reason dropped deferrals + reason-less defer → kind matched by COLUMN + reason required). 21/21 hook/loop/roadmap suites green (+~30 regression assertions; permission_guard 107). Mode-agnostic; all blocking CI green (the lone red is the advisory coverage job's known-flaky sqlite-timing test). **Loop-run readiness: was HOLD pending this fix; now GO** — `/clear` → `/loop-start` (the `/loop-start` turn auto-kicks the loop; no separate `continue` needed). This refresh is the owed follow-on terminating refresh recording substantive #272; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. Status record: `.harness/wave2-hooks-status.md` §"Gate handling". **Prior arc below:**

**(Prior — 2026-06-03 `/clear`→continue to wave 3, post-#269 fixed-point. Wave 3 of the hooks-driven autonomy plan SHIPPED: PR #270 MERGED `3a71e39a`.)** The §12.1 audit recognized the expected lag-by-one (carve-out step 6; HEAD `15d5378` pre-merge). Built **Wave 3 = U-HK-18..25, the self-improvement + Codex/council + prompt/skill tier** — the **low-blast-radius** layer (docs + advisory hooks + skills; **no auto-approve/headless machinery** — it stays clear of the Wave-2 safety-classifier line): U-HK-18 Codex/advisor **division of labor** in CLAUDE.md §13 + memory; U-HK-19 council-orchestrator §5b opt-in Codex decorator; U-HK-20 `/optimize-claude-md` (PR-only + design-substrate-excluded, both enforced); U-HK-21 `skill-activation-check.sh` (typo'd `/slash-command` hint, silent on knowns); U-HK-22 `prompt-lint.sh` (bare-deictic flag, silent on idioms/`continue`/context); U-HK-23 `/roadmap-continue` + `/ship-pr` (cite §12, don't duplicate); U-HK-24 `/self-heal`; U-HK-25 `/fan-out`. **U-HK-18 was the one governance beat** — the unit title said "Codex *replaces* advisor", but R-600 is still an ACTIVE pilot and that contradicts the decorrelation thesis + the shipped `/resolve`; surfaced as one batched **AskUserQuestion → operator chose "keep both"** (Codex = default out-of-family diff reviewer; advisor = transcript-aware decision-fork half). **Driven through `just codex-review` (gpt-5.5, out-of-family) to convergence: round 1 caught 3 real issues on the skill hook** (missed NESTED council skills; noisy short-command hints; a self-introduced ~5s/prompt latency regression from an unbounded recursive `find`) — all fixed + regression-tested (latency 5.13s → 0.24s; combined 3-hook `UserPromptSubmit` 0.46s) — **round 2 clean (0 findings)**; 17/17 hook suites green; all CI green. **Wave-1 Hook A fired on the #270 merge + pre-computed this exact hash `de06493640b6` (verified independently) — the autonomy infra validating itself a third time.** This refresh is the owed follow-on terminating refresh recording substantive #270; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. **The hooks-driven autonomy plan (Waves 1–3, U-HK-01..25) is now COMPLETE.** Forward menu otherwise unchanged (slice-exhausted of un-built no-creds work; `R-410..R-440` + `R-300-second-provider` etc. remain creds/infra-gated). Status record: `.harness/wave3-hooks-status.md`. **Prior arc below:**

**(Prior — 2026-06-03 `/clear`→continue, post-#267 fixed-point. Wave 2 of the hooks-driven autonomy plan SHIPPED: PR #268 MERGED `574f3331`.)** The §12.1 audit recognized the expected lag-by-one (carve-out step 6; HEAD `d7f0c1f` pre-merge). Built **Wave 2 = U-HK-11..17, the loop-mode autonomy tier** (OFF by default behind `loop_mode_active()`): U-HK-11 loop ledger + `/loop-start`·`/loop-stop` skills; U-HK-12 guardrailed auto-approve `permission-guard.sh` (PreToolUse/PermissionRequest, tri-state deny→allow→ask); U-HK-13 Codex+Advisor `/resolve` resolver; U-HK-14 in-session Stop-continue (bounded cap + halt marker); U-HK-15 headless `just loop` runner; U-HK-16 git-arc-guard (advisory); U-HK-17 subagent self-validation. **Operator-authorized via AskUserQuestion** (the auto-approve/headless tier tripped the auto-mode safety classifier — it landed only with explicit human direction + accept-edits mode; documented at `.harness/wave2-hooks-status.md`). **Driven through `just codex-review` (gpt-5.5, out-of-family) to PRACTICAL convergence: 12 rounds, ~30 genuine bypasses fixed** (almost all on the auto-approve guard — the highest-blast-radius unit), each hermetically regression-tested (permission_guard alone 99 assertions; 18 hook suites green). **Operator accepted the hardened state at round 12** (AskUserQuestion 2026-06-03): 2 residuals documented + mitigated (broad Grep/git-diff in-tree-secret — gitignore-covered; pytest incidental paid calls — loop is an explicit paid opt-in) rather than gutting core loop function (pytest/git-diff/grep auto-allow) for codex-absolute-zero. **Wave 1 Hook A fired on the #268 merge + pre-computed this exact hash `6ad3d7f83916` (verified independently) — the autonomy infra validating itself.** This refresh is the owed follow-on terminating refresh recording substantive #268; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. **Wave 3 (U-HK-18..25: Codex-default reviewer, council Codex-decorator, self-improving CLAUDE.md optimizer, skill/prompt validation, self-heal, fan-out) remains queued** — may warrant a fresh `/clear` + operator approval. Forward menu otherwise unchanged (slice-exhausted of un-built no-creds work). **Prior arc below:**

**(Prior — 2026-06-03 `/clear`→continue + restore, post-#264 fixed-point. Wave 1 SHIPPED: PR #266 MERGED `4d0baee3`.)** Resumed from the `wave1-shipped-pr266-wave2-handoff` checkpoint. Drove PR #266 (U-HK-01..10 always-on autonomy-infrastructure hooks) through the pre-merge `just codex-review` gate to convergence: **5 rounds, findings 4 → 2 → 2 → 1 → 0**, every one a real bug fixed + hermetically regression-tested (82 assertions across 11 suites). The gate hardened the §12.2.1 terminating-refresh both-conditions guard **uniformly across all 3 roadmap hooks** (session-start + post-merge-refresh + prompt-context), fixed the next-action extractor (was surfacing the stale `R-010` from the R-700 narrative — now correctly scopes to `## Next action`), the stop-gate fail-open + untracked-`.py` + path-array gaps, the precompact async race, and session-scoped failure recurrence. **Mid-arc: switched the Codex reviewer to `gpt-5.5`** (operator direction; default in `~/.codex/config.toml`; subscription-auth verified, $0 metered) — it caught 2 genuine issues 5.4 missed on its first runs. **The Wave 1 hooks went LIVE this session: Hook A (`post-merge-refresh.sh`) auto-fired on the #266 merge, detected the substantive merge, and pre-computed this exact `workspace_state_hash` (`7c90fcce0eaa`) — the hand-computed-hash toil it was built to kill, verified end-to-end.** This refresh is the owed follow-on terminating refresh recording substantive #266; the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. **PAUSED for operator Wave 2 approval** (U-HK-11..17 loop-mode autonomy; the guardrailed auto-approve/auto-continue tier — may warrant a fresh `/clear` session). Forward menu otherwise unchanged (slice-exhausted of un-built no-creds work). **Prior arc below:**

**(Prior — 2026-06-03 post-#263 fixed-point, PR #264 MERGED.)** The §12.1 audit recognized the expected lag-by-one (carve-out step 6; HEAD `6683017` pre-merge). Resumed the 2026-06-02 hooks/codex exploration; operator chose to BUILD both pilots + RUN them immediately on their ChatGPT **subscription** (not metered API — Codex CLI = H_E dev tooling, X-AL-1, distinct from H_T's metered OpenAI provider / R-300). **Hook A** (`R-600-post-merge-refresh-hook`, RESOLVED) = `PostToolUse(Bash)` §12.2 refresh-reminder (advisory, live next session). **Codex** (`R-600-codex-out-of-family-review`, ACTIVE pilot) = `just codex-review` decorrelated reviewer alongside `advisor()`. **The pilot proved its worth on its first runs:** Codex reviewed this PR's own diff and caught **4 real Hook-A bugs that advisor()+I missed** across 2 rounds (P1 stale hash inputs / P2 inert-on-stock-macOS / P2-a title-only refresh-suppression / P2-b unbounded fetch) — all fixed + regression-tested (hermetic 6/6); review loop stopped at 2 rounds (surface-issues-not-loop-to-zero). The decorrelation A/B proper continues over the next 3-4 high-stakes forks — `R-600-codex-out-of-family-review` stays ACTIVE until enough data to decide keep/expand/drop. This refresh is the owed follow-on terminating refresh recording the substantive #264 (Hook A can't catch its own merge); the next §12.1 audit recognizes the expected lag-by-one and does NOT spawn another. Forward menu otherwise unchanged (slice-exhausted of un-built no-creds work). **Prior arc below:**

**(This session — 2026-06-02 `/clear`→continue, post-#254 fixed-point. Filed the R-410 execution-driver Class 1 fork — the design slice #254 missed.)** The §12.1 audit recognized the expected lag-by-one (carve-out step 6; HEAD `501acfc`, no open PRs). The hook's next `R-010-cp-17-files-indefinite` has no Claude slice (RATIFIED sign-off folded into HELD R-700). Per the no-parking directive (§12.4.1) I drove the highest-value un-blocked forward item — **R-410-sandbox-tier-2-container-execution** (`depends_on: []`; the only forward row both un-blocked AND with an un-built Claude slice). **Grounding (advisor-confirmed, R-410 `advisor_required: yes`) found a genuine, distinct, un-filed design gap:** the resolved `SandboxDispatchDecision` drives ONLY tier-floor enforcement (`runtime_tool_dispatcher.py:454`) + `sandbox.enter/exit` span attributes (`:475/:477`); execution at `:519` is `call_tool` → in-process MCP session **regardless of resolved tier/provider/tech** — no branch maps a tier to a real isolation mechanism (verified: `grep` for container/microvm/firecracker/gvisor/isolat returns only field-docstring examples + the tier-rank int map). Runtime spec v1.41 §14.9.8 (the sole sandbox spec anchor) authors only the *resolver* (decides a tier + floor) and is **silent on tier→mechanism enforcement**. The two applied sibling forks (`..._no_bootstrap_sandbox_decision_resolver` v1.41 §14.9.8; `..._sandbox_decision_policy_phantom_cite`) cover the resolver + the empty-marker policy — **neither covers the execution driver.** **Building** the driver would be X-AL-3 silent design extension (I-2) → **filing the fork is the X-AL-3-clean slice**, and R-410's own notes predicted it ("Almost certainly opens a Class 1 fork"). **FILED:** `.harness/class_1_fork_sandbox_tier_no_execution_driver_contract.md` (PROPOSING) — gap → Readings A (tier→provider-class registry) / B (per-mechanism execution-driver Protocol) / C (defer-indefinitely, bounded-residual = honest MVP end-state) → routes to design-phase; names the **C10⊥C11** tension (action-safety wants real isolation vs operator-loop wants minimal provisioning — CLAUDE.md §13.4 "the council that was missed"), council-eligible at the resolution arc. **No AskUserQuestion fired** — the e2e close co-gates on a real container runtime (R-410 infra), so resolving A/B/C now unblocks nothing buildable; "default to doing + reporting" (§12.4.1). The DESIGN half is orthogonal to R-410's INFRA gate — this advances R-410 without infra. One-time fork; R-411/R-412 inherit the same driver contract. PR is `.harness/`-only + roadmap note + dashboard → X-AL-3 guard trivially satisfied; mode-agnostic. **Conclusion: this is the no-parking-compliant slice #254 lumped into "infra-gated" and missed.** After this: the forward menu's remaining un-built no-creds slices are genuinely exhausted (every other item gates on operator creds/infra, a HELD R-700 decision, or STRUCK U-RT-111 substrate per R-CXA-2).

**➡️ POST-PHASE-8 NOW TRACKED UNDER THE R-NNN DISCIPLINE (PR #209).** The forward register `.harness/post-phase-8-forward-register.md` is decomposed into **12 NEW roadmap `R-NNN` entries** (§5.10–§5.14) — so every post-Phase-8 item flows through the same machinery as all prior work: **dashboard surface** (NEW "Post-Phase-8 forward register" panel), **next-action derivation** (§4), **memory-on-close** (§12.5.3), and **status/state refresh** as items close. This also **discharges the roadmap §9 decomposition** owed for Surfaces IV/VI/IX/X and closes the no-`R-NNN`-entry gap R-700 surfaced (CXA-1/2/3/4 → R-CXA-1..4; CP-17 folded into R-810). **Thesis:** Phase 8 closes the *substitution accounting* (88.9% RETIRED — **legitimate per X-AL-2**; production exercise was never an X-AL-2 condition); post-Phase-8 is the **activation / deployment / integration** axis (many capabilities are library-complete but unexercised).

**The 20 forward items (move through them together).** *(See the register for per-item spec cite + current state + close-out steps + council-eligibility.)*

- **Tier A — Phase-8 closure residuals (declare Phase 8 done; operator-owned):** `R-700-phase-8-substitution-accounting` (BLOCKED — **3 of 4 PART C rulings RATIFIED 2026-06-02 (#246); the formal declaration is HELD**: integer=46 (cascade deferred); CXA/CP-17 coverage CLOSED (CXA-1..4 already existed + NEW R-010-cp-17); sign-offs ratified incl. OD-4 as `RETIRED-AS-CROSS-AXIS-DEFERRED`; only the operator's formal close remains — **see below**); the `harness.toml` config-discovery fork (`R-100-mvp-config-discovery`, BLOCKED).
- **Tier B — forward activation (the menu):**
  - **Claude-executable now (impl):** *(`R-830` SELF_HOSTED DATABASE (SQLite) slice ✅ LANDED this PR; the entry stays `PROPOSED` tracking the MANAGED_CLOUD cloud-vault/managed-db remainder (operator-gated, creds). `R-CXA-1` ✅ grounded → DEFER-DON'T-WIRE (#220); `R-CXA-4` ✅ grounded → **0 wireable edges** (#222) — both confirmed `[[r-cxa-seam-wiring-is-producer-discovery]]`; `R-CXA-2` ✅ grounded 2026-06-02 → **gates on engine-layer substrate, NO wireable slice** (2/6 §16.5 methods fired; 4/6 blocked by deliberately-STRUCK U-RT-111 X-AL-3 gaps — engine stubs + bootstrap-stage-ordering + HITL-disambiguator). `R-300-multi-llm-routing-activation` ✅ RESOLVED PR #213; `R-800-external-mcp-server` ✅ RESOLVED — already wired at PR #172.)*
  - **Operator-gated (creds / infra):** `R-300-multi-llm-second-provider` (OpenAI+Ollama creds + live e2e — the mock cross-family fixture is already built, grounded 2026-06-02; remaining work is live-creds-only); `R-410..R-440` sandbox/deployment/secrets (real container runtime, server, collector, secrets backend); `R-420`/`R-421` SELF_HOSTED/MANAGED_CLOUD e2e; `R-500-multi-tenant-deployment`.
  - **Deferred-by-design (operator-discretion timing at a managed-cloud arc):** `R-810-files-api-integration` (AS-8e/CP-17); `R-820-managed-agents-integration` (AS-8f); `R-CXA-3-cp-as-seam`.
  - **`⚖️ council-eligible` when opened (named two-voice tension):** R-410/411/412 (C10⊥C11), R-440 (C8⊥C11), R-500 (C7+C8⊥C11), R-008 (C7⊥eval-utility), `R-300-multi-llm-second-provider` (C5⊥C9⊥capability — the live routing decision; R-300-routing-activation closed below without firing this gate because only the DECLARATIVE layer is live at MVP).
- **Dashboard iteration-2:** `R-XI-02` ✅ RESOLVED (PR #235) + `R-XI-03` ✅ RESOLVED (PR #237) — **Surface XI COMPLETE**; no open XI lever.

**Recommended next Claude-executable step:** `R-300-multi-llm-routing-activation` ✅ RESOLVED (PR #213) + R-100 live e2e re-validated this session. **`R-CXA-1-as-is-seam` must_pass #1 fork RESOLVED-AS-READING-D (defer)** (`.harness/class_1_fork_cxa_1_secret_fetch_audit_bootstrap_ordering.md`, ✅ APPLIED-AS-READING-D). The apply-arc empirical orientation found Reading B unwritable-as-previewed — the bootstrap fetch is `resolve_bootstrap_value(NAME)` (name only; the scope-bearing `resolve()` path has ZERO production callers), the unsourced-field sentinels are MATERIAL (collapse the secret fingerprint to f(name)), the idempotency key is timestamp-free (fire-once-forever), and the whole `SecretFetchEvent` machinery has no real producer. Wiring bootstrap-value = a hollow seam → operator ratified DON'T-WIRE. **must_pass #1 DEFERRED; R-CXA-1 stays PARTIAL; ZERO production code.** Re-opens on a real scoped-`resolve()` producer OR a Reading-C contract reshape (design-substrate amendment). **Lesson: the "wire a production caller" lever is producer-discovery, not bootstrap-ordering — grounding before authoring avoided a vacuous LANDED.** **`R-CXA-4-od-multi-seam`: the convention-formalization follow-on was a PHANTOM — corrected this session (CXA v2.19), which also fixed a real v2.18 matrix defect it surfaced.** Per CXA §2.5 the 26 OD-outbound edges = 1 genuine typed seam (already wired, 4 producers) + 19 convention + 6 phase-2-runtime (already at bootstrap stage 6). **CORRECTED 2026-06-01:** the prior "stale `U-AS-NN`/`U-CP-NN` placeholders → CXA convention-formalization revision owed" follow-on was itself a wrong-version-read mis-framing (§10.5) — the placeholders were RESOLVED to real producer unit IDs at **CXA v2.3 (2026-05-17) + OD plan v2.11 (2026-05-16)** (canonical HEAD; they survive only in the superseded v2.1 baseline). **There is no cleanup task.** Probing that phantom follow-on instead surfaced a **real defect in v2.18**: it re-absorbed the already-done C3-15 OD→IS cleanup (same wrong-version-read), corrupting the §2.1 matrix (AS→IS 13→11, CP→OD 0→8, OD→CP 8→12) and publishing aggregate **105** (correct = **107**) + a fabricated 37/48/20 sub-split (correct = 37/48/22) → fixed at **CXA v2.19** (this PR) + CLAUDE.md §1.1/§2.4 + clearance marker + register §B-14 + roadmap §5. v2.18's OD→IS=4 / OD-outbound=26 end-state was already correct since v2.3 and is preserved; "CXA-OD-IS-EDGE-DRIFT" halt-doc Item 11 was a **phantom drift** (CXA already conformed at v2.3). R-CXA-4 stays PARTIAL; no further Claude-executable follow-on. **Lesson `[[wrong-version-read-delta-only-baseline]]`:** in a delta-only chain, canonical = last full re-table + subsequent cell-amendments, never the baseline. **`R-830` SELF_HOSTED DATABASE backend (SQLite) LANDED this session (operator-picked, grounding-first).** `SqliteMemoryToolBackend` (stdlib sqlite3) implements the already-spec'd `MemoryToolStorageBackend.DATABASE` enum value (§14.12.3 `connection_string`); operator binds via `memory_tool_backend_config`; full read/write/delete e2e through the real dispatch seam, no creds, no new deps. **Closed HONESTLY per advisor (no §10.5 overclaim):** this is the SELF_HOSTED_SERVER SQL sibling of FILESYSTEM — **NOT** the MANAGED_CLOUD cloud-vault/managed-db the R-830 title names. R-830 stays **`PROPOSED`** tracking the cloud remainder (`S3` / real managed DB + creds) — operator-gated (MANAGED_CLOUD without an explicit `DATABASE` override still raises — proof the cloud intent is unmet). All operator-driven menu picks per "move through them together." `R-100` live runs + `R-300-multi-llm-second-provider` remain operator-gated (creds; not fired unilaterally per `[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`).

**(This session — 2026-06-02 `/clear`→continue, post-#252 fixed-point. NO new code; two producer-discovery grounding findings + a stale-banner fix — mode-agnostic.)** The §12.1 audit recognized the expected lag-by-one (carve-out step 6); HEAD `a91b065` (#252), no open PRs. The hook pointed next at `R-010-cp-17-files-indefinite` — a DEFERRED/operator-gated accounting entry whose sign-off is already RATIFIED at R-700 and whose formal close is folded into the **HELD** R-700 declaration → no Claude-executable slice (driving it = authoring the operator-gated Files surface OR overriding the held declaration; both forbidden). Per the no-parking directive (§12.4.1) I drove the highest-value forward item with a *plausible* slice instead. **Two findings, both honest-terminal (advisor-guided, [[r-cxa-seam-wiring-is-producer-discovery]]):** (1) **`R-CXA-2` grounded → gates on engine-layer substrate, NO wireable slice** — 2/6 §16.5 methods fired (pause_resume + override via workflow_driver), 4/6 blocked by deliberately-STRUCK U-RT-111 X-AL-3 gaps (engine `capture_pause_snapshot`/`attempt_resume` NotImplementedError stubs + bootstrap-stage-ordering + HITL-disambiguator). Same DEFER shape as R-CXA-1/R-CXA-4; the dashboard's prior "Claude-executable-now lever R-CXA-2" claim was optimistic. (2) **`R-300-multi-llm-second-provider`: the creds-separable mock cross-family fixture is ALREADY BUILT** (`test_iterates_three_candidates_until_success` asserts anthropic→anthropic→openai advance; §4.3 flag + exhaustive CP-suite flag coverage + fallback.* spans all present); the remaining R-300 work is genuinely live-creds-gated — NOT a buildable-unbuilt slice. **Conclusion: the high-value forward menu is genuinely slice-exhausted of un-built no-creds Claude work** — every remaining item gates on operator creds/infra (`R-300-second-provider`, `R-410..R-440`, `R-500`), a HELD operator decision (`R-700` formal Phase-8 declaration), or a dispositioned design-phase substrate arc (`R-CXA-2` engine-layer, dispositioned STILL-BOUNDED at R-700). This is the no-parking-compliant terminal (reaching the genuine gate after honest grounding), not a park. No new AskUserQuestion fired — all gates are already documented; "default to doing + reporting over asking" (§12.4.1).

**(R-700 context, for reference.)** The R-700 draft (PR #207) found the published `48/54 RETIRED / 49 pipeline` internally impossible (3 PARTIAL: OD-4 + CXA-1 + CXA-4); per-row truth `46 RETIRED + 3 PARTIAL + 2 STILL-BOUNDED + 3 SB-INDEF = 54` (accounting (i); RETIRED integer 46–47, buckets co-vary); true open set 8 rows; root cause = CXA never folded into the cumulative (ledger §11.1a line 278). NOT a regression; the operator-ratified `48/54` is carried forward as a flagged Phase-8 ratification item (draft §C).

---

**`R-800-external-mcp-server` RESOLVED (this session) — verified already complete; no production change.** The operator picked R-800 from the post-Phase-8 menu; pre-substantive empirical grounding found **all 3 must_pass already met**: `host.start()` at `stage_3a_cp_clients.py:59` + `host.shutdown()` at `shutdown.py:484-489` (both landed at **PR #172**, spec v1.41 §14.9.8 Gaps B/F), and a **real external stdio MCP server** exercised CI-green & unconditional at `test_u_rt_86` (production factory + real subprocess + handshake + list_tools + TOOL_STEP dispatch + 7-attr `mcp.*` span; 9/9 pass with `test_ac2_bootstrap_path_wiring`). The PROPOSED-era `notes` + register §B-10 "unwired at bootstrap" framing was **stale** (authored PR #209; described pre-#172 state) — a stale-carry-text disposition (CLAUDE.md §10.5), now refreshed. **Residual (out of R-800 scope):** the full `api.run` TOOL_STEP path is **Gap D / R-100 AC#2** — bootstrap pings ≥1 provider regardless of step kind → operator-gated by design; a bootstrap-skip-provider-ping-for-inference-free-workflows change is a Class 1 fork candidate (C9⊥C11 → dyadic-council-eligible) if opened. ZERO src/spec/test change. Mode-agnostic.

---

**3 R-NNN entries closed in the prior session (PRs #200 + #201) — 2 R-700 Phase-8 blockers drained + the v1.14 process-discipline catalogue established.**

**`R-007` + `R-009` RESOLVED (PR #200 `phase-7: ... batch-51`).** Both flipped BLOCKED → eligible per §4 step 3 once `R-100-mvp-real-workflow-execution` RESOLVED (2026-06-01); a per-substitution X-AL-2 condition-(B) audit (advisor-prompted) split them: **H_T-OD-3 → substantive RETIRED** (`HarnessCompositeSampler` live at MVP via `materialize_tracer_provider_stage`; 7a-scaffold sampler no longer invoked; "tail-keep at OTLP collector" reframed as R-430 feature-validation, NOT a retirement gate — gate-text-stale-vs-production-landings, sub-species 10's 3rd closure); **H_T-OD-6 → RETIRED-AS-BOUNDED-RESIDUAL** (`flush_to_sqlite` dormant at MVP / zero callers → boundary unmoved → substantive RETIRED would be silent absorption; closed bounded-residual per X-AL-2 §5.3 — **FIRST bounded-residual close in the ledger**; gated on R-420/R-421). Workspace RETIRED 46→**48/54 (88.9%)**; OD-axis RETIRE-READY bucket EMPTY; pipeline-advanced unchanged 49/54.

**`R-600-workflow-v1-14-amendment` RESOLVED (PR #201).** `design-substrate/Project_Workflow_v1_14.md` — NEW **§7.5 Process-discipline catalogue** under §7 (sibling to §7.4), following the v1.9 §7.4.7 framework-establishing precedent. 4 disciplines seeded (PD-1 halt-route-split-AC; PD-2 use-the-product-probe; PD-3 verification-shape-grep-vs-e2e w/ §13.1 cross-ref; PD-4 plan-revision-against-not-yet-built-substrate, single-unit-multi-rescope + SUPERSEDES the U-RT-111 v2.36→v2.38 §7.4.7.2-sub-species framing) + OPEN clause + 5 parked candidates. §10.9 adversarial-gate run → CLEAR (0/0/1; F1-01 inline-fixed). **`must_pass[1]` ("derivation rule canonicalized at workflow doc layer") N/A — operator-RATIFIED at AskUserQuestion 2026-06-01 (PR #203): a speculative pre-survey criterion inconsistent with the entry's actual §7.5-catalogue scope; the derivation rule's home stays roadmap §4 + CLAUDE.md §12. Entry FULLY CLOSED on must_pass[0]+[2].**

**§4 re-derivation (clean-main, 2026-06-01 post-#201):** §I — IS 9/9 + AS active-9/9 + CP 21/22 + **OD now 7/8 RETIRED + 1 PARTIAL (OD-4)**; the only Claude-executable §I lever left is **R-008** (OD-4 PARTIAL → RETIRE-READY — needs §13.1 per-session toggle (session-control substrate) + §13.2 tokenization; further substrate before transit, not trivially closeable). §II/§III complete; §IV + §V (`R-410..R-440`) infra-gated; §VII R-600 sweep recurring (ACTIVE-SURVEYED). **No auto-derivable Claude-executable ACTIVE row remains.** Remaining work is operator-owned: (a) the operator-gated `R-100-mvp-real-workflow-execution` live inference e2e (RESOLVED at deterministic level; not fired unilaterally per `[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`); (b) `R-700-phase-8-substitution-accounting` (BLOCKED — needs comprehensive operator review; blocker set now shorter: OD-4 PARTIAL + AS-8e/AS-8f DEFERRED-INDEFINITELY); (c) `R-XI-02`/`R-XI-03` (PROPOSED dashboard iteration-2).

**`R-XI-01` (operator dashboard MVP) — RESOLVED + LIVE** at **https://thestoryportal.github.io/arhugula-harness/** (HTTP 200).

**(This refresh — post-#243/#244, 2026-06-02.** Records two SUBSTANTIVE merges (not dashboard-only), so this is the owed follow-on refresh per §12.2: **PR #243** (`ops(R-600)`: pattern-bake-in-sweep **cadence-2** — prior cycle discharged by the v1.14 §7.5 absorption; ONE new §7.5 PD-5 candidate `producer-discovery` cardinality-qualified; + `R-600-memory-hygiene-normalization` filed + RESOLVED, 39 `[[..]]` token renames) and **PR #244** (`feat(od)`: **R-008 OD-4 gate (a)** — solo-developer §13.1 per-session redaction toggle `session_content_capture()`; grounding-first close, the fork-doc "session-control-substrate arc" was already satisfied by the existing per-session ContextVar; gate (a) FULLY CLOSED; X-AL-3-clean). Recomputes `workspace_state_hash` `61a20d7a9750` → `978cdde72d8e` + `git_head` → `2233abbc` (PRS empty / FORKS 44 / batch-51).

**§4 re-derivation (clean-main, 2026-06-02 post-#244):** §I — IS 9/9 + AS active-9/9 + CP 21/22 + OD 7/8 RETIRED + **1 PARTIAL (OD-4, gate (a) now CLOSED; gate (b) §13.2 cross-axis-blocked)**. **R-008's gate (a) lever is now SPENT** — the only remaining OD-4 gate is §13.2 opaque-token tokenization, which is **cross-axis** (`c10-action-safety` eval-grade pipeline, Phase-6+) and NOT OD-axis-Claude-closeable. So OD-4 can never reach RETIRE-READY via Claude. **No auto-derivable Claude-executable ACTIVE row remains** (unchanged conclusion; R-008's Claude-executable portion is now exhausted). Remaining work is operator-owned: **R-700 Phase-8 ratification (teed up — see below)**; creds/infra-gated `R-300-second-provider` / `R-410..R-440` / `R-500`; the recurring `R-600` sweep (next cadence ~10 PRs out).

**🏁 R-700 Phase-8 ratification — 3 of 4 RATIFIED 2026-06-02 (#246); formal declaration HELD.** Per the R-700 draft (`.harness/R-700-phase-8-closure-accounting-draft.md` PART C), the operator ruled (AskUserQuestion): **(1) RETIRED integer = `46`** (accounting (i)) RATIFIED — the wide `48→46` cascade across ledger / `harness-cp/CLAUDE.md` / dashboard is **DEFERRED** to the formal declaration, so the published `48/54` still stands with a carve-out; **(2) CXA + CP-17 coverage CLOSED** — grounding found `CXA-1/2/3/4` already covered by `R-CXA-1..4` (authored #209; the draft's "no R-entry" framing was stale), and NEW **`R-010-cp-17-files-indefinite`** authored for CP-17 → all 5 flagged rows now tracked; **(3) deferred sign-offs RATIFIED** — `AS-8e`/`AS-8f`/`CP-17`/`OD-6` + **`OD-4` (R-008) as a NEW `RETIRED-AS-CROSS-AXIS-DEFERRED` disposition** (gate (a) closed #244; gate (b) cross-axis/Phase-6+); **(4) formal Phase-8 declaration HELD** — the operator accepts the dispositions but defers the formal `phase-8-graduation.md` close pending review. R-700 stays BLOCKED on that held declaration only; the bucket flips + the 48→46 cascade execute when the hold is lifted. **This refresh (this PR) is the owed follow-on terminating refresh recording the substantive #246; the next §12.1 audit recognizes the expected lag-by-one (carve-out step 6) and does NOT spawn a new refresh. Prior arc: PR #245 (post-#243/#244 refresh).)**

---

## In-flight (open PRs)

| PR | Branch | R-NNN | Posture |
|---|---|---|---|
| — | — | — | (none open; #292–#296 all MERGED 2026-06-04..05) |

---

## Recently completed (last 5)

| R-NNN / PR | Closed at | Notes |
|---|---|---|
| **R-100 config discovery tracking / PR #305** (`b8c4671`) | 2026-06-06 | **Tracking closure after operator-ratified Reading A.** CWD `harness.toml` discovery already shipped in PR #279 (`a394032`); refreshed the fork doc to APPLIED-AS-READING-A, marked `R-100-mvp-config-discovery` RESOLVED, closed forward-register A-4, and regenerated the dashboard snapshot. No runtime code changed in #305. |
| **dashboard generator / PR #304** (`0765237`) | 2026-06-06 | **System-Python dashboard regeneration fixed.** Added a PyYAML-free parser for the `.harness/substitutions.yaml` `snapshot:` pins so Codex guard regeneration keeps the retired-trend endpoint at the canonical 46 rather than stale batch prose 48; added a regression test and refreshed the dashboard snapshot. |
| **Codex guard snapshot classifier / PR #303** (`94dd44d`) | 2026-06-06 | **Generated dashboard snapshot no longer trips design/implementation mix.** Exempted only `tools/dashboard/roadmap.html` from the classifier while preserving dashboard source and all other `tools/**` implementation classification; added a regression test for roadmap/fork-doc changes paired with the required generated snapshot. |
| **Codex guard hardening / PR #302** (`a038c148`) | 2026-06-06 | **OpenAI API adversarial review follow-up shipped.** Added committed-range and local branch-diff enforcement to `tools/codex_context_guard.py`, wired PR base/head refs into CI, wired branch-diff mode into local closeout + Stop hook, kept default-branch dashboard drift hard, made expected lag explicit, broadened design/implementation classification, and surfaced unavailable open-PR context. All PR checks green. Mode-agnostic; process tooling. |
| **R-600 Codex context guard / PR #301** (`9549e68` + tracking close `b7179ad`) | 2026-06-05 | **Codex deterministic context guard shipped.** Added checked-in Codex source-of-truth workflow note, `tools/codex_context_guard.py`, focused tests, `just codex-*` recipes, CI anti-rot gate, hook propagation, local checkpoint freshness, and the credential-gate ledger semantics requested by the operator. All blocking CI green before merge. Mode-agnostic; process tooling. |
| **workspace reorg** (`cfc6cc4`) | 2026-06-05 | **Workspace reorganization (operator manual, Claude-completed + reference-fixed).** research/ → `.harness/01-planning/01-harness-planning/00-harness-research/` (nested numbered scheme); tools/loop → `tools/04-loop/` (6 stranded executable refs repointed across justfile/permission-guard/stop-loop/loop_lib/2 test suites — **4 loop suites green**); council deliberation tree productized-into-#295-then-pruned; `_bmad` config prune; pyproject ruff `bmad-*` exclude; CLAUDE.md §10.9 `research/` paths → `.harness/01-planning/`; `.claude/audio/` gitignored. 118 files; renames preserved content; design-substrate untouched. Mode-agnostic; ops. |
| **brainstorming doc** (`a60222d`) | 2026-06-04 | **Post-build architecture exploration** — `brainstorming/2026-06-04-agents-sfas-eval-writers-autoresearch.md` (164 lines; agents / SFAs / eval-writers / autoresearch). Committed locally, not pushed until this reconciliation; the in-flight operator reorg relocates it to `.harness/01-planning/`. Mode-agnostic; docs. |
| **#296 worktree live-session guard** (`71231f6`) | 2026-06-04 | **`fix(hooks)`: never remove a worktree with a live Claude session.** Transit-mtime liveness guard (`worktree_has_live_session`) so `git worktree remove` / loop-GC can't orphan a live session pinned to the deleted root. Refresh `4f59e0f` followed (`ops: roadmap status refresh post-#296`); this reconciliation records the merge row the post-#296 refresh's table omitted. Mode-agnostic; tooling. |
| **council productize / PR #295** (`343fe45`) | 2026-06-04 | **Council workflow productized** (parallel arc; recorded here by the post-#293 follow-on refresh the post-#294 refresh missed). 2 YAMLs + 2 `/commands` (`/council-workflow`, `/council-generic`) + workflow doc + roadmap registration. Mode-agnostic; process-substrate. |
| **R-IF-112 stage-04 / PR #294** (`cc3f0b5`) | 2026-06-04 | **Overlay agent-workflow integration SHIPPED (stage-04).** Documents *where the overlay fits the agent loop* + routes agents to its query CLI by reflex. NEW `.claude/skills/overlay-query/SKILL.md` (query-CLI focus: query-mode → workflow-moment map; posture-neutral; explicit Scope caveat — derives from code docstring cites + `substitutions.yaml` + `PATTERN_P1_SEAMS`, does NOT scan `design-substrate/**` sibling specs). CLAUDE.md §13.1 names it the instrument for *empirical cite-grounding* + a *complementary code-side* check for *cross-spec drift grep* (not a replacement for sibling-spec `rg`) + §13.5 cross-ref; roadmap-continue step-3 grounding clause; R-IF-112 dev-ledger stage-04 note. Operator chose new-skill · doc+light-wiring · query-CLI focus (AskUserQuestion). `just codex-review` (gpt-5.5) caught + fixed an over-claim. 2 memory entries. R-IF-112 stages 01–04 COMPLETE. Mode-agnostic; process-substrate. |
| **BMAD skills install / PR #293** (`6ed8416`) | 2026-06-04 | **Installed 5 BMAD analysis/planning skills + vendored `_bmad/` runtime** (operator-requested; the exact 5 URLs, not the full repo). Skills → `.claude/skills/bmad-*/` (Claude convention → invocable): `bmad-technical-research`, `bmad-product-brief`, `bmad-prfaq`, `bmad-agent-pm`, `bmad-prd`. Dependency vendored (option #1, no skill edits) to an ICM-hygiened `_bmad/`: MIT stdlib `scripts/resolve_customization.py` + `bmm/config.yaml` + `custom/` + git-ignored `artifacts/` + `CONTEXT.md`/`README.md`; `_bmad` added to ruff exclude. 16 optional `bmad-*` cross-refs documented as no-ops. 39 files; resolver smoke-tested all 5; all 12 CI checks green; worktree hygiene done. Mode-agnostic tooling (NOT an R-NNN item). |
| **R-IF-112 stages 02/03 / PR #291** (`560afa3`) | 2026-06-04 | **Spec↔code↔CXA↔substitution semantic overlay SHIPPED** (stages 02-design + 03-build) — `tools/semantic_overlay/` (deterministic, no-LLM; sibling to `substitution_ledger.py`). One UA-format graph artifact, three consumers: agent query CLI (`--contract/--unit/--seam/--file/--substitution/--orphans`; cross-axis `--seam` resolves consumer↔producer file/axis+symbol), `enrich-ua` dashboard left-join, new blocking CI `semantic-overlay` drift gate. Joins docstring cites (287/291=98.6%; 120 contracts/286 units), the 31 code-resident `PATTERN_P1_SEAMS` (not the delta-only CXA markdown — v2.19=0 rows), `substitutions.yaml` (thin 9/55), code structure; 4 real code-without-cite orphans surfaced; 14 tests. `overlay.json` gitignored (stale snapshot = manufactured drift). X-AL-3-clean. R-IF-112 stages 01–03 COMPLETE. Mode-agnostic; process-substrate. |
| **R-IF-112 refresh / PR #289** (`5bb980d`) | 2026-06-04 | Registered **R-IF-112** in `Project_Roadmap_v1.md` §5.1 (Surface VII, PROPOSED) + dashboard refresh post-#288 + regenerated the `tools/dashboard/roadmap.html` snapshot (67 R-NNN actions). Substantive (new R-NNN) → this terminating refresh closes the §12.2.1 fixed point. Mode-agnostic. |
| **R-IF-112 seed / PR #288** (`89c20bb`) | 2026-06-04 | **Seeded the spec-code-overlay arc** — overlay the spec↔code↔CXA↔substitution semantic layer onto the code graph (drift-detection). Canonical-ICM workspace at `.harness/spec-code-overlay/` (root `CONTEXT.md` → `stages/01-exploration/{CONTEXT.md, output/}`; layout grounded on `RinDig/Interpreted-Context-Methdology`). Stage-01 exploration: understand-anything tool assessment (GUI wrong for in-loop agents; staleness = the drift we fight; models code↔code only) + cross-axis spec-cite probe (**277/278 ≈ 99.6%** of source files carry a parseable authority cite; 286 `U-*` IDs; full `C-*-NN` keyspace → deterministically extractable) + linter-MVP shape (orphan-detector sibling to `substitution_ledger.py`, before any viz). Registered **R-IF-112** (Surface VII, PROPOSED; forward stages 02-design + 03-build). Mode-agnostic; process-substrate only. |
| **Hardening-workflow package / PR #285** (`4a786a8`) | 2026-06-03 | **Prepared the loop/HIL HARDENING dynamic-workflow context package + `begin-workflow` trigger** (operator-requested; run from a fresh session via `begin workflow`). The operator probed whether the loop's hook/skill/advisor disciplines are actually run — audit found ~13 are MANUAL (Claude-recall), only ~5 hook-AUTOMATIC, which is why this session lapsed. Package at `.harness/hardening-workflow/`: BRIEF + inventory (AUTO-vs-MANUAL, cited) + session-evidence (lapses = test set) + references/claude-code-hooks.md (live capability ref) + references/insights-report-2026-06-03.md (201-session corroboration; NEW D14 output-token-limit) + workflow.js (audit→adversarial-verify→synthesize→critic over D1..D14 → HARDENING_PLAN.md). Assembled via 4 parallel collectors + live fetch + adversarial verify (PACKAGE-ACCURATE; D6 paid-call deny-vs-proceed contradiction confirmed). NEXT: `/clear` → `begin workflow`. Scope: U-HK process machinery only. Mode-agnostic; no prod code. |
| **tooling / PR #272** (`d11d2b5`) | 2026-06-03 | **Corrected the autonomous loop to DEFER-AND-CONTINUE at a gate (U-HK-14/15)** — the operator flagged, during pre-run readiness review, that the ratified design (keep working around a gate) was violated: the shipped Wave-2 cut raised `.loop-halt` + stood the whole run down at the FIRST gated item. Fix: at a gate, build the un-gated slice → `tools/loop/defer.sh <id> '<input needed>'` → advance to the next forward item per §12.4.1; `.loop-halt` (via `tools/loop/halt.sh`) reserved for TRUE exhaustion. **Mechanical ledger-as-skip-set** (DEFERRED-HIL rows injected into the continue-reason + computed per-iteration by `run.sh`) so a fresh headless `claude -p` never re-attempts a deferred item (the overnight burn-the-cap failure). `loop_pending_hil_summary` surfaced at SessionStart for operator review. Tool-level hard-stop UNCHANGED. Driven through `just codex-review` to convergence — **4 rounds of real findings → round 5 clean** (allowlisted executable wrappers; `$VAR` secret-leak + skip-set over-match; reason-names-gate-tripped-deny-list; ACTIVATE-in-reason + reason-required). 21/21 suites (+~30 assertions; permission_guard 107). Mode-agnostic; blocking CI green (lone red = advisory coverage's known-flaky sqlite-timing test). **Loop-run readiness: HOLD → GO.** |
| **tooling / PR #270** (`3a71e39`) | 2026-06-03 | **Wave 3 of the hooks-driven autonomy plan — the self-improvement + Codex/council + prompt/skill tier (U-HK-18..25); completes the plan (Waves 1–3 = U-HK-01..25).** Low-blast-radius (docs + advisory hooks + skills; no auto-approve/headless → clear of the Wave-2 classifier line): U-HK-18 Codex/advisor **division of labor** (CLAUDE.md §13 + memory; operator AskUserQuestion → **"keep both"**, NOT "replaces" — R-600 pilot still ACTIVE); U-HK-19 council §5b opt-in Codex decorator; U-HK-20 `/optimize-claude-md` (PR-only + design-substrate-excluded, enforced); U-HK-21/22 two advisory `UserPromptSubmit` hooks (silent-when-correct); U-HK-23 `/roadmap-continue` + `/ship-pr`; U-HK-24 `/self-heal`; U-HK-25 `/fan-out`. Driven through `just codex-review` to convergence — **round 1 caught 3 real issues** (nested-skill discovery, noisy short-command hints, a self-introduced 5s/prompt latency regression), **round 2 clean**; 17/17 hook suites green; 3-hook `UserPromptSubmit` 0.46s. **Hook A auto-fired on this merge, pre-computing `de06493640b6`** — verified independently (3rd time the infra validated itself). Mode-agnostic; all CI green. |
| **tooling / PR #266** (`4d0baee`) | 2026-06-03 | **Wave 1 of the hooks-driven autonomy plan — 10 always-on autonomy-infrastructure hooks (U-HK-01..10):** shared `tools/hooks/lib.sh` keystone + PreToolUse cache-clear + PostToolUse lint-on-edit + PreCompact checkpoint + PostCompact re-inject + failure/token-cap capture + UserPromptSubmit context injector + SessionEnd cleanup + Stop lint-gate; `.claude/settings.json` wires 10 events. All advisory/observational — ZERO autonomy risk (auto-approve/auto-continue deferred to Wave 2, loop-mode-gated off). Driven through `just codex-review` to convergence: **5 rounds, findings 4→2→2→1→0**, all real bugs fixed + hermetically tested (82 assertions / 11 suites). Hardened the §12.2.1 both-conditions guard uniformly across all 3 roadmap hooks; fixed the next-action extractor (was surfacing stale `R-010` from the R-700 narrative). **Codex reviewer switched to gpt-5.5** (operator direction; subscription-auth, $0). **Hook A went live + auto-fired on this merge, pre-computing `7c90fcce0eaa`** — verified end-to-end. Mode-agnostic; CI green. **Wave 2 PAUSED for operator approval.** |
| **tooling / PR #264** (`cf214aa`) | 2026-06-03 | **Built Hook A + Codex out-of-family review pilots (operator-picked, from the 2026-06-02 hooks/codex exploration).** **Hook A** (`R-600-post-merge-refresh-hook`, RESOLVED): `PostToolUse(Bash)` hook detects a substantive PR merge (origin advanced past the dashboard pin to a non-§12.2.1-refresh commit), pre-computes the new `workspace_state_hash`, injects the §12.2 checklist — kills the hand-computed-hash toil. Advisory, never blocks; goes live next session. **Codex** (`R-600-codex-out-of-family-review`, ACTIVE pilot): `just codex-review` runs the Codex CLI as a decorrelated out-of-family reviewer alongside `advisor()`, on the operator's ChatGPT **subscription** ($0 metered; `_require-codex-subscription` guard fails loud on stale login). **Pilot worked emphatically:** Codex reviewed this PR's own diff and caught **4 real Hook-A bugs advisor()+I missed** across 2 rounds (P1 stale hash inputs / P2 inert on stock macOS / P2-a title-only suppression / P2-b unbounded fetch) — all fixed + regression-tested (hermetic 6/6). Codex = H_E dev tooling, NOT H_T's OpenAI provider (X-AL-1 / R-300). Mode-agnostic; CI green. |
| **R-600 / PR #262** (`63ae9f9`) | 2026-06-02 | **Built `R-600-substitution-ledger-schema` (operator-authorized) — RESOLVED.** Killed the count-drift / stale-carry defect class on the accounting surface (the class that produced the R-700 `48/54`-impossible bug). NEW `.harness/substitutions.yaml` (single source: 55 rows = 54 canonical + CP-24) + `tools/substitution_ledger.py` (derive/validate, `--check` CI gate) + `tools/test_substitution_ledger.py` (canonical pin 46/49/54 + both per-axis breakdowns + label≠count + 4 negative tests) + NEW blocking `ci.yml` tally-gate job + `generate.py` consumes the derivation (dropped hardcoded counts + stale 46-48 range prose) + 4 canonical-source pointers. Derivation reproduces graduation §3 exactly (46/49/54); an impossible tally now fails CI. advisor-reviewed pre-build. Mode-agnostic. |
| PR #260 (`db96a61`) | 2026-06-02 | **Scoped `R-600-substitution-ledger-schema` (NEW PROPOSED, Surface VII).** Operator-requested follow-on to the R-700 close: extract the 54-row substitution ledger to a typed `.harness/substitutions.yaml` + a generator that DERIVES the buckets + a CI tally-validation gate, so the count-drift / stale-carry defect class (workspace #1, §10.9) cannot recur on the accounting surface. The `48/54`-internally-impossible bug (survived ~51 batch footers + 5 hand-maintained files) is the motivating case. Scope discipline encoded: schema-fy ONLY counted/derived state; prose reasoning stays prose. `depends_on R-700` (RESOLVED). **PROPOSED — scoping only; execution is a separate authorization.** roadmap §5.6; `roadmap-design-extension`. |
| **R-700 / PR #258** (`155935c`) | 2026-06-02 | **🎓 PHASE 8 CLOSED — formal substitution-accounting declaration.** Operator lifted the HELD R-700 declaration ("kick off the declaration"); item 4 executed. Authored `.harness/phase-8-graduation.md`: canonical **46/54 RETIRED (85.2%) + 49/54 pipeline-advanced (90.7%)** (operator-ratified accounting (i), #246). Advisor confirmed **46 is the forced reading** of the joint item-1+item-3 ratification (OD-6-in-both-lists tell — sign-off label ≠ count-membership). Forward supersession applied (ledger §11.7 + harness-cp/CLAUDE.md + dashboard; prior batch records verbatim per §0.5). 8 terminal sign-off dispositions (OD-4 `RETIRED-AS-CROSS-AXIS-DEFERRED`; AS-8e/8f/CP-17 accepted-indefinite-defer; CXA-1..4 Phase-2-runtime-deferred) — labels do NOT re-tally. must_pass SATISFIED (49 §5 rows accounted + bounded-residual rationale). Council convene SKIPPED-justified. `R-700` → RESOLVED. **Terminal milestone — surfaced for operator review/merge, not auto-merged.** |
| PR #256 (`0fca58f`) | 2026-06-02 | **Filed the R-410 execution-driver Class 1 fork — the design slice #254 missed.** No-parking-compliant (§12.4.1): drove R-410 (only un-blocked forward row with an un-built Claude slice). Grounding (advisor-confirmed) found the resolved sandbox tier is **never enforced** — `SandboxDispatchDecision` drives only tier-floor (`runtime_tool_dispatcher.py:454`) + span attrs (`:475/:477`); execution (`:519` in-process MCP) is tier-independent; spec v1.41 §14.9.8 specs only the *resolver*, silent on tier→mechanism. Building = X-AL-3 extension → **filed** `class_1_fork_sandbox_tier_no_execution_driver_contract.md` (PROPOSING; Readings A/B/C; C10⊥C11). No AUQ (e2e co-gates on container infra → unblocks nothing buildable). Design half orthogonal to R-410's infra gate. `.harness/`-only + roadmap note + dashboard; mode-agnostic. Bundled substantive → follow-on terminating refresh owed (§12.2.1). |
| PR #250 (`f263857`) | 2026-06-02 | **Operationalized the no-parking directive — operator-labeled items are Claude-driven.** Standing operator directive: `halt-route-to-operator` / "operator-owned" / infra-gated / PROPOSED / DEFERRED labels must NOT park a session at "no Claude work"; the operator executes nothing manually. Encoded in CLAUDE.md **NEW §12.4.1**, roadmap **§4** (`NO_ACTIVE_CANDIDATES` ≠ session-terminate → drive the highest-value forward item's slice to the genuine gate), and this dashboard's "Next action" banner. Paid-call/secret boundary unchanged; held operator decisions (R-700) honored. Mode-agnostic; 10/10 CI green. `[[feedback-operator-labels-are-claude-driven-no-parking]]`. |
| PR #248 (`e725f89`) | 2026-06-02 | **Doc hygiene — stale per-axis CLAUDE.md §1.2 authority pointers + gitignore scratch.** Refreshed badly-stale version pointers (loaded every session; caused this session's R-008 wrong-version-read): harness-od spec v1.4→**v1.27** / plan v2.11→**v2.27**; harness-cp spec v1.3→**v1.30** / plan v2.10→**v2.31** (AS/IS already current). Surgical (version cell only); contract counts preserved (internally consistent at 23/24; flagged inline). gitignored the 4 untracked `.claude/skills/` design-skill reference clones (council/* stay tracked) + Mustard-era `dashboard-design/` scratch. **#2 (the "3 junk spec dups") NOT done** — grounding showed they're not junk (lowercase `_v1_1` = same APFS inode; the `v1 (n)` files are the only v1.0 base) → left untouched per `[[design-substrate-version-identity-hazards]]`. No design-substrate contract change. 10/10 CI green. |
| PR #246 (`165a0f4`) | 2026-06-02 | **R-700 Phase-8 ratifications recorded (3/4); declaration HELD.** Applied the operator's AskUserQuestion rulings: integer=**46** RATIFIED (48→46 cascade DEFERRED to the formal declaration — published 48/54 stands with a carve-out); CXA/CP-17 coverage CLOSED (CXA-1..4 already had entries via #209; NEW `R-010-cp-17-files-indefinite` authored); deferred sign-offs RATIFIED (AS-8e/AS-8f/CP-17/OD-6 + **OD-4 as NEW `RETIRED-AS-CROSS-AXIS-DEFERRED`**); formal Phase-8 declaration HELD. Grounding caught that the draft's "CXA rows have no R-NNN entry" was stale (avoided duplicate authoring). No code; no count cascade (deferred). 10/10 CI green. |

---

## Outstanding fork docs (45 total — this PR files #45)

Sample (highest-leverage open):

| Fork doc | Class | Status |
|---|---|---|
| `class_1_fork_sandbox_tier_no_execution_driver_contract.md` | Class 1 | PROPOSING (this PR) — resolved sandbox tier never enforced; no tier→mechanism execution driver. Deferred-far; routes to design-phase at the R-410 resolution arc (C10⊥C11). Unblocks nothing buildable at MVP (Reading C bounded-residual). |
| `class_1_fork_tool_step_no_operator_supplied_converter.md` | Class 1 | ✅ APPLIED (PR #171, spec v1.40) — converter config surface landed (Reading B) |
| `class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` | Class 1 | ✅ APPLIED-AS-READING-B (PR #172, spec v1.41 §14.9.8) — resolver + 5 bootstrap gaps wired; AC #2 final close = operator live e2e |
| `class_1_fork_llm_cost_attribution_not_firing_on_real_dispatch.md` | Class 1 | ❌ RESOLVED-AS-INVALID (PR #168) — test-observation bug, not a defect; cost-attribution fires + writes; OD-5 retirement VALID |
| `class_1_fork_harness_toml_default_discovery_unimplemented.md` | Class 1 | PROPOSING (PR #164) — spec §3.7 auto-discovery declared-but-unimplemented; tracked at R-100-mvp-config-discovery; does NOT block the MVP |
| `class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` | Class 1 | ✅ APPLIED-AS-READING-C (PR #107, spec v1.30) |
| `class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.39, pyyaml StrictSafeLoader) — R-100-mvp-yaml-loader-shipped verified done at non-live level (41 loader/equivalence tests pass) |
| `class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.38, defer-to-runtime) |
| `class_2_fork_audit_stub_timestamp_universal_fix_plus_per_tier_annotation.md` | Class 2 | ✅ APPLIED-AS-(D) |
| *(complete enumeration at `ls .harness/class_*_fork_*.md`)* | — | — |

**Audit-owed:** survey fork-doc files for Status-line refreshes against current production state. Cadence: every ~5 PRs or operator-discretion.

---

## Phase 7 retirement progress

> **🎓 PHASE-8 GRADUATION CANONICAL (2026-06-02, `.harness/phase-8-graduation.md`).** The R-700 reconciliation is CLOSED — the operator ratified **accounting (i)** (PR #246) and lifted the declaration hold. **Canonical: RETIRED 46/54 (85.2%) + pipeline-advanced 49/54 (90.7%).** The prior published `48/54` over-counted by 2 (un-folded CXA accounting ledger §11.1a line 278 + CP-17 SB-INDEF reclass + CP-21-vs-22 / AS-3↔AS-9 bookkeeping); superseded forward, prior batch records stand verbatim. The buckets below are the canonical 54-row set; **disposition labels (e.g. OD-4 `RETIRED-AS-CROSS-AXIS-DEFERRED`) do NOT re-tally into the 46** (the OD-6-in-both-lists tell — see graduation doc §4).

The 6 bucket rows below sum to **54** under the ratified **accounting (i)** (RETIRED 46).

| Bucket | Count | Notes |
|---|---|---|
| RETIRED | **46/54 (85.2%)** canonical (Phase-8 graduation) | 36 substantive + 8 authoring-only + 2 bounded-residual (CP-16, OD-6). IS 9/9; AS-1/2/4/5/8a/8b/8c/8d; CP 21; OD-2/3/5; CXA-5. Supersedes the prior published `48` (over-counted by 2 per the un-folded CXA accounting + CP-17 SB-INDEF reclass). |
| RETIRE-READY | **0 active (bucket EMPTY post-batch-51)** | OD-3 + OD-6 transited RETIRE-READY → RETIRED at batch-51 (PR #200). |
| PARTIAL | **3 — OD-4 + CXA-1 + CXA-4** (pipeline-advanced; NOT in the 46) | **OD-4 disposition label = `RETIRED-AS-CROSS-AXIS-DEFERRED`** (NEW class, ratified item 3): gate (a) §13.1 CLOSED (#244); sole remaining gate (b) §13.2 is cross-axis (c10-action-safety/Phase-6+) → never OD-axis-Claude-closeable (R-008). CXA-1 (AS→IS, zero prod callers; R-CXA-1) + CXA-4 (OD→multi, batch-42; R-CXA-4) Phase-2-runtime-deferred. The labels record sign-off; count-membership stays PARTIAL. |
| STILL-BOUNDED | **2 — CXA-2 + CXA-3** (Phase-2-runtime-deferred) | CP→IS (R-CXA-2, gates on STRUCK engine substrate) + CP→AS (R-CXA-3, no runtime composer). |
| STILL-BOUNDED-INDEFINITELY | **3 — AS-8e + AS-8f + CP-17** (accepted-indefinite-defer sign-off; NOT counted in the 46) | AS-8e files.* (R-005) + AS-8f managed_agents.* (R-006) + CP-17 files-primitives (R-010). **Distinct from the *counted* RETIRED-AS-BOUNDED-RESIDUAL** of CP-16/OD-6 — these stay in the non-pipeline-advanced 5. |
| RETIRED-AS-AUTHORING-ONLY | **8** (of the 46 RETIRED) | accounting (i): IS-4, IS-10, AS-9, CP-12, CP-23, OD-1, OD-7, OD-8 (accounting (ii) = 9, adds CP-24). The H_T contract is the typed declaration itself; no runtime behavior (sub-species 10 categorical-mismatch). |
| RETIRED-AS-BOUNDED-RESIDUAL | **2** (of the 46 RETIRED — *counted*) | CP-16 (memory, batch-44) + OD-6 (OTLP, batch-51 — FIRST in ledger). Production substrate dormant at MVP; substantive close deferred to a real deployment surface (X-AL-2 §5.3). **NOTE:** distinct from the AS-8e/8f/CP-17 *accepted-indefinite-defer sign-off* (NOT counted) — same words, different bucket. |

**Decomposition status (R-002 RESOLVED 2026-05-31) + R-700 draft correction (2026-06-01):** R-002 mapped all non-RETIRED rows *in the per-axis `§4.1` view* to R-NNN entries — IS-2 (R-003 + R-001-h-t-is-2-retired), OD-3 (R-007), OD-4 (R-008), OD-6 (R-009), AS-8e (R-005), AS-8f (R-006); R-001 (OD-5) + R-004 (AS-8d) reconciled stale BLOCKED → RESOLVED. **The R-700 draft surfaced that the R-002 survey covered only `§4.1`, which excludes the CXA axis (no `§4.1` file) + CP-17's SB-INDEF reclassification — so 5 non-RETIRED rows (`CXA-1`/`CXA-2`/`CXA-3`/`CXA-4` + `CP-17`) have NO `R-NNN` entries.** Recommended follow-on (draft §C item 2): an `R-002`-style Surface-I decomposition pass over CXA + CP-17. **→ CLOSED at PR #246 (R-700 PART C item 2):** grounding found CXA-1/2/3/4 already had `R-CXA-1..4` (authored #209); NEW `R-010-cp-17-files-indefinite` authored for CP-17 → all 5 flagged rows now have dedicated entries. Ratified at the Phase-8 graduation (2026-06-02).

---

## Drift detection log

| Date | Source | Resolution |
|---|---|---|
| 2026-05-31 | Dashboard creation (v1 origin) | n/a |
| 2026-05-31 | First post-merge refresh — PR #112 merged at `7f3e6ce`; dashboard hash recomputed `9c31e4978c3d` → `5a077d17765f` | Refreshed via PR #113 per CLAUDE.md §12.2. |
| 2026-05-31 | Recursion-stop discipline gap surfaced — PR #113 merge left dashboard stale by 1 commit; §12.2 as written would recurse | Codified at PR #114 via §12.2.1 termination clause + §12.1 step 6 carve-out. |
| 2026-05-31 | First terminating refresh per §12.2.1 — PR #114 merged at `7da53e5`; this PR is the terminating event | Single-file dashboard-only refresh; does NOT trigger another refresh per §12.2.1. Hash `5a077d17765f` → `9bd06cb83b73`. |
| 2026-05-31 | Second terminating refresh — PR #116 bundled change merged at `63c4464`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `9bd06cb83b73` → `12f34637fd3a`. |
| 2026-05-31 | Third terminating refresh — PR #118 NotebookLM skill setup merged at `65ed646`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `12f34637fd3a` → `47c763cb4fb3`. |
| 2026-05-31 | Fourth terminating refresh — PR #120 NotebookLM MCP server supplement merged at `622790c`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `47c763cb4fb3` → `6283a744645e`. |
| 2026-05-31 | Fifth terminating refresh — PR #122 SessionStart audit hook (enforcement layer) merged at `27bfeaf`; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `6283a744645e` → `82ce25e90fd5`. From this PR forward, audits auto-fire at every session start. |
| 2026-05-31 | Fresh-session reconciliation — local `main` found 13 commits stale (showed `#106`) vs `origin/main` `#123`; SessionStart hook not yet on local main so no auto-orient | Fast-forwarded local main; reset to `origin/main` `9f1bbf4` after parallel process pushed equivalent post-#122 refresh (#123); identical-tree sibling discarded. No divergence created. |
| 2026-05-31 | Sixth terminating refresh — PR #108 R-IF-108 audit merged at `54337c1`; `[ROADMAP DRIFT] action=§12.3` flagged by on-main hook; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `82ce25e90fd5` → `a9f8cc1bd85d`. R-IF-108 closed; next action recomputed to R-IF-109. |
| 2026-05-31 | Seventh terminating refresh — PR #109 R-IF-109 memory audit merged at `a81fe2d`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `a9f8cc1bd85d` → `25d4b36d4359`. R-IF-109 closed; next action recomputed to R-IF-110 (first design-phase posture entry in chain). |
| 2026-05-31 | Eighth terminating refresh — PR #110 R-IF-110 CXA v2.18 (design-phase) merged at `2f14604`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `25d4b36d4359` → `17fe5dac2209`. R-IF-110 closed; next action recomputed to R-IF-111 (last in-flight entry). Downstream `CLAUDE.md` §1.1 count cascade (107→105) flagged as owed hygiene arc per CXA v2.18 §0.9 FM-2 deferral. |
| 2026-05-31 | Ninth terminating refresh — PR #111 R-IF-111 OD plan v2.27 (design-phase) merged at `fdf120b`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `17fe5dac2209` → `a51d65ed22a2`. R-IF-111 closed — **in-flight chain #108→#109→#110→#111 fully cleared this session**. Next action recomputed to the owed hygiene cascade (CLAUDE.md §1.1 aggregate 107→105 + harness-od refresh), R-002 fallback after. |
| 2026-05-31 | Tenth terminating refresh — PR #128 R-IF-HYGIENE-CXA-COUNT-CASCADE merged at `3ba4fae`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `a51d65ed22a2` → `adb1e71359dd`. Hygiene cascade closed (canonical pointers now read 105). **All in-flight + deferred-hygiene work cleared this session.** Next action recomputed to **R-002** (substantive phase-7 substitution-retirement survey — recommend a fresh decision before opening). |
| 2026-05-31 | Eleventh terminating refresh — PR #130 R-002 Surface-I decomposition merged at `6f8dd66`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `adb1e71359dd` → `7f251d6c7cc5`. R-002 RESOLVED (7 non-RETIRED rows mapped to R-NNN; R-001/R-004 reconciled stale→RESOLVED). Next action recomputed to **R-003** (producer-site lifts, ~7 sites; substantive phase-7 cross-axis impl — recommend fresh decision + advisor before opening). **Survey-surfaced finding:** the roadmap §5.2 was authored 2026-05-31 with R-001/R-004 as RETIRE-READY-awaiting-deployment, but OD-5/AS-8d had been RETIRED via mech-β at batches 31-32 (2026-05-28) — a 3-day-stale authoring error, now corrected. |

| 2026-05-31 | Twelfth terminating refresh — PR #132 R-003 orientation-checkpoint merged at `7c79fb4`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `7f251d6c7cc5` → `754826ddff19`. Next action UNCHANGED at **R-003** but now **ACTIVE-ORIENTATION-COMPLETE** with a resume pointer to `.harness/R-003-checkpoint.md` — a fresh session (`/clear` → hook → `continue`) resumes at Cluster A without re-deriving. Demonstrates the **stop-short seamless-resume** discipline: a paused phase-7 entry persists its orientation as a checkpoint artifact + entry sub-status + dashboard pointer. |

| 2026-05-31 | Thirteenth terminating refresh — PR #134 roadmap §3 schema amendment merged at `f8572b3`; `[ROADMAP DRIFT] action=§12.3` flagged; §12.2 owed follow-on refresh | Single-file dashboard-only refresh per §12.2.1. Hash `754826ddff19` → `1c852278bfc9`. Next action UNCHANGED at **R-003 Cluster A**. §3 now declares the `resume:` field + `satisfied:<date>` advisor value + checkpoint-on-pause rule (status enum kept closed); R-003 entry reconciled to schema-legal `ACTIVE`. Closes the §3↔§5 drift the operator caught (3 off-schema constructs landed at #132 before the schema sanctioned them — schema-first discipline restored). |

| 2026-05-31 | **R-003 RESOLVED** — Cluster A (#136 `5e4a112`) + Cluster B (#137 `c339728`) merged; substantive roadmap PR (this) marks R-003 → RESOLVED + R-001-h-t-is-2-retired BLOCKED → ACTIVE | Bundled change (Project_Roadmap_v1.md + dashboard) → NOT a terminating refresh per §12.2.1; title drops the `ops: roadmap status refresh` prefix; **follow-on terminating refresh owed**. Hash `1c852278bfc9` → `03714383e971` (state at `c339728`, pre-this-PR-merge). Next action re-derived: **R-001-h-t-is-2-retired** (IS-2 producer cascade complete; file the PARTIAL → RETIRED retirement event). |

| 2026-05-31 | Fourteenth terminating refresh — PR #138 substantive R-003-close refresh merged at `a943caf`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `03714383e971` → `3111061f3b6b` (state at `a943caf`). Next action UNCHANGED at **R-001-h-t-is-2-retired**. This is the recursion-stopping fixed point for the R-003 close: dashboard now lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-05-31 | **Fresh-session reconciliation (Cluster A/B drift) + R-001 close** — a `/clear`→`continue` session's hook reported `next=R-003` though R-003 (Clusters A+B) was already shipped+merged (#136–#139); the primary checkout's local `main` was 4 commits behind `origin/main` and the hook never fetched (2nd instance of the line-92 class). | Fixed via `git merge --ff-only origin/main` (clean ff, no divergence); **hardened the hook** with a default-branch behind-origin guard (**PR #140** `e55e99b`). Then executed the true next-action: **R-001-h-t-is-2-retired RESOLVED** at **PR #141** `4a0aa1d` (H_T-IS-2 PARTIAL → RETIRED, batch-50; IS-axis 9/9 = 100%, FIRST axis fully RETIRED). This PR marks R-001 RESOLVED + reconciles stale-ACTIVE R-IF-108/109/110/111 → RESOLVED (confirmed merged) + logs this event. Substantive (roadmap + dashboard) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. Hash `1c852278bfc9` → `8780c87985f2` (state at `4a0aa1d`, pre-this-PR-merge). Next action re-derived: **R-200-ci-pytest-pyright-ruff-matrix** (§III rank 3 — §I drained, §III outranks §II). |

| 2026-05-31 | Fifteenth terminating refresh — PR #142 substantive R-001-close merged at `5ca46f5`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `8780c87985f2` → `ec4f50af797b` (state at `5ca46f5`). Next action UNCHANGED at **R-200-ci-pytest-pyright-ruff-matrix**. Recursion-stopping fixed point for the R-001 close: dashboard now lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-05-31 | **R-200-ci-coverage-gating RESOLVED** + sixteenth terminating refresh — substantive close PR #151 merged at `51f4131` (blocking `pytest` green; advisory od/pyright/ruff legs red-as-expected); this PR is the owed follow-on terminating refresh per §12.2.1 | Single-file dashboard-only refresh. Hash `b0d5c85c1136` → `e30b9dd81864` (state at `51f4131`). Next action UNCHANGED at **R-200-ci-od-cp-dependency-leak** (re-derived at PR #151; mode-agnostic §III drained → §III phase-7 entries next). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-05-31 | **R-200-ci-od-cp-dependency-leak RESOLVED** (PR #153, substantive) — od axis-isolation leg was advisory due to undeclared OD→CP + OD→IS consumer deps; advisor-gated resolution = option (a) declare the deps | Bundled (touches `Project_Roadmap_v1.md`) → NOT a terminating refresh; follow-on owed. `harness-od/pyproject.toml` declares `harness-cp` (canonical OD→CP §2.3.3; forcing consumer = `ReplayDisposition` read-only at `idempotency_join_dedup.py`) + `harness-is` (OD→IS §2.3.4; tests); both acyclic-safe. Broadened grep caught the `harness_is` 2nd dep a cp-only grep would miss. od leg green in isolation + in CI as blocking (887 passed) + `continue-on-error` carve-out dropped → full 6-leg matrix blocks. Corrected the "reverse-direction / relocate the seam" framing (OD→CP is canonical) at `.harness/class_3_drift_od_cp_undeclared_dependency.md`. Hash left stale `e30b9dd81864` (recompute owed at the post-merge terminating refresh). Next action re-derived: **R-200-ci-lint-typecheck-blocking** (§III phase-7, sole remaining ACTIVE §III; outranks §II MVP). |

| 2026-05-31 | Seventeenth terminating refresh — PR #153 substantive R-200-ci-od-cp-dependency-leak close merged at `4d0914d`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e30b9dd81864` → `7fe422eb023c` (state at `4d0914d`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-05-31 | Eighteenth terminating refresh — PR #155 ruff mechanical sweep (R-200-ci-lint-typecheck-blocking **progress**, not a close) merged at `f1f9a52`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `7fe422eb023c` → `e077fe676860` (state at `f1f9a52`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — sweep dropped ruff 366 → 136 + cleaned format tree-wide, zero regression; manual ruff + pyright + flip-blocking remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-05-31 | Nineteenth terminating refresh — PR #157 F821 real-defect fixes (R-200-ci-lint-typecheck-blocking **progress**, not a close) merged at `ce211ee`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e077fe676860` → `8070fa6466c7` (state at `ce211ee`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — 3 F821 real defects fixed, ruff 136 → 133, F821 tree-wide now 0; 133 manual ruff residual + ~894 pyright + flip-blocking remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-05-31 | Twentieth terminating refresh — PR #159 lint-half close (R-200-ci-lint-typecheck-blocking **lint half closed**, entry stays ACTIVE) merged at `1b87f08`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `8070fa6466c7` → `72ce1ef25437` (state at `1b87f08`). Next action UNCHANGED at **R-200-ci-lint-typecheck-blocking** (entry still ACTIVE — `ruff check .` + `ruff format --check .` clean tree-wide; `lint` CI job flipped to blocking + verified green-as-blocking; ~894 pyright + `typecheck`-flip remain). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-05-31 | **R-200-ci-lint-typecheck-blocking RESOLVED** (PR #161 pyright-half close, substantive) — `uv run pyright` 846 → 0 tree-wide; `typecheck` job flipped `continue-on-error` → blocking (renamed `pyright (strict) — blocking`), verified green-as-blocking in CI with all 6 axis-isolation legs + ruff + pytest | Bundled (touches `Project_Roadmap_v1.md` §5 status ACTIVE → RESOLVED) → NOT a terminating refresh; follow-on owed. Approach: test dirs scoped via `[tool.pyright.executionEnvironments]`; src real-fixed incl. the named two-`Skill`-class bug (empty `types.Skill` stub re-exported as concrete `lifecycle.skills.Skill`; same `LedgerWriter`; `HarnessMCPServer` kept-as-stub — Pydantic-forward-ref regression caught + reverted mid-flight) + `AuditLedgerWriter.append` Protocol completion + mcp `streamablehttp_client`→`streamable_http_client` deprecation rename. 3543 passed / 0 regression. cp+od axis-isolation legs run locally (813 / 887 passed). Hash `72ce1ef25437` → `c0c447454969` (state at `d904055`, pre-this-PR-merge). **§III CI substrate gate COMPLETE** (both lint + typecheck blocking). Next action re-derived: **R-100-mvp-operator-usable-cli-shipped** (§II MVP rank 4 — §III drained, §I drained, §II is next priority tier with a dependency-met ACTIVE entry). |

| 2026-05-31 | Twenty-first terminating refresh — PR #162 bundled R-200-close merged at `149c9a0`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `c0c447454969` → `038c75a5245e` (state at `149c9a0`). Next action UNCHANGED at **R-100-mvp-operator-usable-cli-shipped** (§II MVP — §III CI substrate gate complete). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-05-31 | **Fresh-session audit + R-100 close** — `/clear`→`continue` session: §12.1 audit clean (local main 3 behind origin; ff'd to `fcfa568` #163, a terminating refresh whose dashboard hash = `compute(149c9a0)` = expected lag-by-one fixed point per §12.1 step 6; no new refresh PR). Derived next-action R-100-mvp-operator-usable-cli-shipped; use-the-product probe surfaced 4 operator-blocking gaps → shipped scaffolding + fixes + operator-authorized live green at **PR #164** `44c668d`. | Substantive R-100 close (PR #164 touches Project_Roadmap_v1.md: R-100 ACTIVE → RESOLVED + NEW R-100-mvp-config-discovery) → NOT a terminating refresh; **follow-on terminating refresh owed**. Hash `038c75a5245e` → recomputed below. Next action re-derived: **R-100-mvp-real-workflow-execution** (R-100 RESOLVED unblocks it; was BLOCKED → ACTIVE). |

| 2026-05-31 | Twenty-second terminating refresh — PR #164 bundled R-100-close merged at `44c668d`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `038c75a5245e` → `48e2fffa4434` (state at `44c668d`; open_fork_doc_count 39 → 40 for the NEW discovery fork). Next action UNCHANGED at **R-100-mvp-real-workflow-execution**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **R-100-mvp-real-workflow-execution PARTIAL + 2 forks** — same `/clear`→`continue` session: authored the real multi-step e2e (PR #166 `97c8943`). AC #1 + #3 PASS via 2 live operator-authorized Anthropic runs; AC #2 + #4 surfaced as Class 1 forks (converter gap + cost-not-firing w/ OD-5 retirement-validity question). | Substantive (PR #166 touches Project_Roadmap_v1.md: entry → ACTIVE/partial + NEW R-100-tool-step-converter + R-100-cost-attribution-firing) → NOT a terminating refresh; **follow-on terminating refresh owed**. Hash `48e2fffa4434` → recomputed below. Next action re-derived: **operator ratification of the 2 PROPOSING forks** (all §II forward entries now fork-gated; §VII process-discipline is the executable fallback). |

| 2026-06-01 | Twenty-third terminating refresh — PR #166 substantive R-100-real-workflow PR merged at `97c8943`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `48e2fffa4434` → `0deb499daab5` (state at `97c8943`; open_fork_doc_count 40 → 42 for the 2 NEW R-100 forks). Next action = **operator fork-ratification** (see Next action section). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **Fork ratification cluster** — same `/clear`→`continue` session: grounded + ratified both R-100-mvp-real-workflow-execution forks. Cost fork → RESOLVED-AS-INVALID (PR #168 `a9ee48d`; live instrumentation flipped it — test-observation bug, cost fires+writes, OD-5 valid; AC #4 test fixed → PASSES). Converter fork → RATIFIED-AS-READING-B (PR #169 `5431ed1`; operator AskUserQuestion). | Two substantive PRs (#168 touches roadmap+test; #169 touches roadmap+fork doc) → NOT terminating refreshes; this is the **owed follow-on terminating refresh** covering #167→#169. Hash `0deb499daab5` → `35586f56e497` (state at `5431ed1`). R-100-mvp-real-workflow-execution now 3/4 ACs PASS; next action re-derived: **apply Reading B at R-100-tool-step-converter** (design-phase posture spec+impl arc; confirm posture per §11.6). |

| 2026-06-01 | Twenty-fourth terminating refresh — fork-ratification cluster #168 + #169 merged; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash recompute `0deb499daab5` → `35586f56e497` (state at `5431ed1`; fork count 42 unchanged — #168/#169 flipped fork Status, added no fork files). Next action = **R-100-tool-step-converter apply arc (Reading B)**. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **R-100 tool-step apply cluster landed** — `/clear`→`continue` session: operator approved (AskUserQuestion) merging the 3 ready green PRs in stack order #173 → #171 → #172. #172 (stacked on #171) was rebased onto main after #171's squash-merge diverged its base (standard stacked-PR artifact); 2 doc conflicts (CLAUDE.md runtime-spec row + roadmap status) resolved to #172's superset, §13 preserved. All blocking CI green at each merge. | Three substantive PRs (their own merges); **this is the owed follow-on terminating refresh** (Twenty-fifth) covering #170→#172. Dashboard-only → terminating per §12.2.1. Hash `35586f56e497` → `be74ce84cf0f` (state at `492d327`; fork count 42 → 39 recount). `R-100-tool-step-converter` APPLIED (#171) + `R-100-tool-step-sandbox-resolver` APPLIED-PENDING-OPERATOR-E2E (#172). Next action = **operator-gated R-100 MVP live e2e run** (AC #2 + CLI smoke + real-workflow close only by a live LLM run; not auto-fireable). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-06-01 | **R-600-clearance-marker-backfill-survey RESOLVED** (PR #175 `60d55ee`) — same session: with §II MVP operator-gated, corrected the §4 board (dirty-primary-checkout had contaminated the derivation) and executed the highest-priority ungated Claude-executable entry. Survey gap-free for all spec/CXA/Workflow + terminal plan markers; 10 intermediate superseded plan versions exempt (terminal-marker-covers-chain). | Substantive (touched roadmap + clearance README) → **this is the owed follow-on terminating refresh** (Twenty-sixth). Dashboard-only → terminating per §12.2.1. Hash `be74ce84cf0f` → `b7767c7cc1c6` (state at `60d55ee`). **`open_fork_doc_count` corrected 39 → 43** — the 39 in PR #174 was computed in the stale/dirty primary checkout (on b220dd5); the hook-recipe glob on clean main = 43. Clean-main §4 re-derivation: §I drained, §III done, §VII R-600-clearance RESOLVED + R-600-pattern-bake-in gated by deferred Workflow v1.14; `R-100-mvp-yaml-loader-shipped` verified done at non-live level (41 tests). Next Claude-executable arc = **R-XI-01 operator dashboard MVP** (trigger met; flip-eligible; outward-facing Pages deploy → teed up pending operator go-ahead). Dominant product next-step stays the operator's live R-100 e2e. Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-06-01 | **R-XI-01 operator dashboard MVP BUILT** (PR #177 `90604de`) — operator AskUserQuestion green-lit "build now, recommended stack". Generator (5 sources → static `roadmap.html`, Tailwind + Chart.js CDN) + `dashboard-deploy.yml` Pages workflow; verified in Chrome (zero console errors, all 7 sections), ruff clean, dashboard-deploy `build` job green in CI. R-XI-01 PROPOSED→ACTIVE. | Substantive (touched roadmap R-XI-01 status) → **this is the owed follow-on terminating refresh** (Twenty-seventh). Dashboard-only → terminating per §12.2.1. Hash `b7767c7cc1c6` → `8e3c6e4653af` (state at `90604de`; fork count 43 unchanged). **All Claude-executable substantive arcs now drained** — remainder is operator-gated (live R-100 e2e; enable GitHub Pages → closes R-XI-01) or blocked (R-XI-02/03 need R-XI-01 RESOLVED; pattern-bake-in gated by Workflow v1.14). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **fork-doc Status audit** (PR #179 `e821d74`) — audit-owed maintenance sweep of all 43 open fork docs; corpus current; 1 stale cross-ref refreshed at `cp_spec_section_25_contract_id_collision`; new recurring-audit log `.harness/fork_doc_status_audit_log.md`. | Substantive (touched fork doc + new log) → **this is the owed follow-on terminating refresh** (Twenty-eighth). Dashboard-only → terminating per §12.2.1. Hash `8e3c6e4653af` → `94fc7eff646a` (state at `e821d74`; `open_fork_doc_count` 43 unchanged — audit refreshed a Status line, closed no fork). Next action UNCHANGED — all Claude-executable substantive arcs drained; remainder operator-gated (live R-100 e2e; enable GitHub Pages). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-100 AC#2 closed via free local ollama** (PR #181 `39fdcbf` test ollama-branch fix + PR #182 `8529a45` close) — `R-100-mvp-real-workflow-execution` (4/4) + `R-100-tool-step-sandbox-resolver` → RESOLVED. AC#2 (TOOL_STEP via `api.run`) green against a live local ollama daemon (zero-token ping; echo-MCP dispatch, not inference — no paid call). | Substantive (test fix + roadmap §5 + fork doc) → **this is the owed follow-on terminating refresh** (Twenty-ninth), covering BOTH #181 and #182. Dashboard-only → terminating per §12.2.1. Hash `94fc7eff646a` → `dc6807a02340` (state at `8529a45`; `open_fork_doc_count` 43 unchanged). **Next action CHANGED**: R-100 MVP real-workflow + tool-step RESOLVED; `R-100-mvp-yaml-loader-shipped` flips BLOCKED → ACTIVE (dep resolved) and becomes the top §II Claude-executable candidate (close via an ollama-enable-then-verify arc on the track-B e2e, mirroring the AC#2 fix). **R-XI-01 reclassified**: GitHub Pages is BLOCKED at the account-plan level (private + free plan; HTTP 422 from `POST /pages`) — needs GitHub Pro or a public repo, NOT a Settings toggle. Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-XI-01 closed — dashboard live on Pages** (PR #184 `5984cda`) — operator made the repo public; I enabled Pages (`build_type=workflow`) + triggered `dashboard-deploy` (build+deploy GREEN, run `26750144964`); live at https://thestoryportal.github.io/arhugula-harness/ (HTTP 200). | Substantive (roadmap §5 R-XI-01 → RESOLVED) → **this is the owed follow-on terminating refresh** (Thirtieth). Dashboard-only → terminating per §12.2.1. Hash `dc6807a02340` → `91ff69664956` (state at `5984cda`; fork count 43 unchanged). Next action: top §II Claude-executable candidate remains `R-100-mvp-yaml-loader-shipped` (ACTIVE, free ollama-enable arc); R-XI-02/R-XI-03 now unblocked (PROPOSED, iteration-2). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **`just dashboard` recipes added** (PR #186 `da45c29`) — local view of the operator dashboard (`just dashboard` generate+open / `just dashboard-serve [port]` generate+serve on localhost). justfile only. | Substantive (justfile tooling, non-refresh) → **this is the owed follow-on terminating refresh** (Thirty-first). Dashboard-only → terminating per §12.2.1. Hash `91ff69664956` → `b93c17d379dc` (state at `da45c29`; fork count 43 unchanged). No roadmap-state change (no R-NNN closed); next-action UNCHANGED (`R-100-mvp-yaml-loader-shipped` top §II candidate). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-100-mvp-yaml-loader-shipped RESOLVED** (PR #188 `e3f99a8`) — YAML↔TOML load+dispatch equivalence closed at the CI/deterministic level (strengthened load test for byte-equivalent payload + NEW deterministic dispatch-equivalence test; no key/ollama/daemon). §II R-100 MVP surface now fully RESOLVED. | Substantive (test code + roadmap §5) → **this is the owed follow-on terminating refresh** (Thirty-second). Dashboard-only → terminating per §12.2.1. Hash `b93c17d379dc` → `ca30acdc6032` (state at `e3f99a8`; fork count 43 unchanged). **Next action CHANGED**: yaml-loader RESOLVED → `R-100-mvp-multi-workflow-fixture-suite` flips BLOCKED → ACTIVE as the next §II candidate (scope first — MVP materializes only SINGLE_THREADED_LINEAR, so the "all 6 topologies" scope is partly gated). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |

| 2026-06-01 | **R-600-pattern-bake-in-sweep EXECUTED → ACTIVE-SURVEYED** (this PR) — fresh `/clear`→`continue` session; §12.1 audit clean (`lag-expected` fixed point: dashboard `c6c79d0515e2` = compute(#194/`18b2d5d`) = compute(#195~1); recomputed to compute(main `d7574b3`) = `82afe95f7974`). Executed the hook-derived next action (`R-600-pattern-bake-in-sweep`): read-only memory-store sweep → 104 cardinality-≥2 `[[pattern]]` tokens; survey at `.harness/R-600-pattern-bake-in-sweep.md`. | Substantive (survey artifact + roadmap §5.6 entry → `resume:` pointer + ACTIVE-SURVEYED note) → **NOT a terminating refresh; follow-on terminating refresh owed** per §12.2.1. Hash `c6c79d0515e2` → `82afe95f7974` (state at `d7574b3`, pre-this-PR-merge; recompute owed at the post-merge terminating refresh). **Finding: ZERO new §7.4.7.2 sub-species owed** (strike-revision already at v1.13); the strong disciplines are all non-§7.4.7-shape → recommend a NEW §7.5/§7.4.7.X catalogue at the DEFERRED `R-600-workflow-v1-14-amendment`. Next action re-derived: MEMORY.md hygiene audit (motivated by survey §3 dups + over-cap index), then fork-doc Status cadence sweep; dominant product step stays the operator-gated R-100 live e2e. |

| 2026-06-01 | **Fresh `/clear`→`continue` session: merged PR #196 + ran the MEMORY.md hygiene audit** — §12.1 audit clean (`lag-expected` fixed point recognized; survey was committed at `f7613f3`/PR #196 OPEN). Merged #196 (`eed6a6d`, clean+green, process-substrate). Then executed the survey-§3 follow-on — the MEMORY.md hygiene audit — correcting two stale survey claims on empirical grounding (index NOT over-cap at 23,199<24,400 B; the "slug dups" are external frozen-change-note casing mismatches, not memory-file dups) and applying the genuine fixes (−2 redundant `(#)` placeholder index lines, +1 orphan `phase-1-research-corpus` line) to the global memory store. This is the owed follow-on **Thirty-third terminating refresh** for #196. | Dashboard-only → terminating per §12.2.1. Hash `82afe95f7974` → `a82a9a13881b` (state at `eed6a6d`, pre-this-PR-merge; PRS empty). Next action UNCHANGED — all Claude-executable substantive arcs drained; next mode-agnostic = fork-doc Status cadence sweep (43 open). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **fork-doc Status cadence sweep (PR #198 `4ecc208`)** — same session: ran the next named Claude-executable mode-agnostic arc (last pass #178, 18 PRs elapsed). Delegated read-sweep of all 43 fork docs vs. merges #181–#196; **corpus current — ZERO stale Status lines** (43/43 accurate). No fork-doc edit; appended the dated audit-pass entry + reset the ~5-PR cadence clock. This is the owed follow-on **Thirty-fourth terminating refresh** for #198. | Dashboard-only → terminating per §12.2.1. Hash `a82a9a13881b` → `4a0e2d4647c0` (state at `4ecc208`, pre-this-PR-merge; PRS empty). **All recurring Claude-executable mode-agnostic maintenance arcs drained this session** (R-600 survey #196 + MEMORY.md audit + fork-doc sweep #198); remainder operator-owned (live R-100 e2e; design-phase v1.14 amendment; R-XI-02/03). Recursion-stopping fixed point: dashboard lags by exactly one commit, recognized as `lag-expected` by the next §12.1 audit. |
| 2026-06-01 | **R-700-closure-accounting-draft RESOLVED + dashboard aligned (PR #207 `d1eaf75`), then this terminating refresh.** Fresh `/clear`→`continue` session resumed the paused R-700 arc: produced `.harness/R-700-phase-8-closure-accounting-draft.md` (Part A 54-row substitution log + Part B 8-entry beyond-substitutions register); KEY FINDING — the published `48/54 RETIRED / 49 pipeline` is internally impossible (3 PARTIAL: OD-4 + CXA-1 + CXA-4); per-row reconciliation 46–47 RETIRED; true open set 8 rows; root cause = CXA never folded into the cumulative (ledger §11.1a line 278). Aligned the live operator dashboard (retirement 48/54 batch-51 + R-700-aware buckets) + verified deploy. PR #207 squash-merged; dashboard-deploy succeeded; live site verified. | Dashboard-only → terminating per §12.2.1. Hash `4d3120974cef` → `e95a2e943993` (state at `d1eaf75b`, the #207 merge; PRS empty; fork count 43 unchanged; batch-51 latest). Next action UNCHANGED — R-700 draft feeds the BLOCKED `R-700-phase-8-substitution-accounting` (operator owns the final integer + bounded-residual sign-offs); NEW gap surfaced (CXA-1/2/3/4 + CP-17 have no R-NNN entries → recommend an R-002-style Surface-I pass over CXA + CP-17). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 audit. |

| 2026-06-01 | **R-300-multi-llm-routing-activation RESOLVED (PR #213 `c047aa2`), then this audit.** Fresh `/clear`→`continue` session: §12.1 audit oriented (worktree on `r-300-routing-activation`, 3 commits ahead of origin/main; the implementation + memory had already been authored this branch and PR #213 was open + all-green). Merged PR #213 (squash, all 10 checks pass, CLEAN/MERGEABLE) — `infer()` composition seam activated (C-CP-01 §1.1, U-CP-03): CP-pure `route()`+injected-async-dispatch composition + live INFERENCE_STEP routing through `infer()` + full `routing.*` span attribution; DECLARATIVE-echo behavior-preserving at MVP; council gate NOT met this arc (fires at R-300-second-provider). This audit finalizes the §5 `(PR pending)` → `(PR #213, c047aa2)` note + refreshes the dashboard. | Substantive (Project_Roadmap_v1.md §5 + dashboard) → **NOT a terminating refresh; follow-on terminating refresh owed** per §12.2.1. Hash `7646d1d29e5f` → `039ab95024f0` (state at `c047aa2`; PRS empty; fork count 43 unchanged; batch-51 latest). Next action re-derived: R-300-routing RESOLVED drains the top Claude-executable post-Phase-8 impl lever; remaining Claude-executable-now = `R-830-memory-tool-production-backend` + `R-CXA-1/2/4` seams (operator-driven menu picks). Dominant product step stays operator-gated (live R-100 e2e; R-300-second-provider creds). |

| 2026-06-01 | Thirty-fifth terminating refresh — PR #214 substantive R-300-close audit merged at `7a8802b`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `039ab95024f0` → `8fb5be5f6f46` (state at `7a8802b`, the #214 merge; PRS empty; fork count 43; batch-51). Next action UNCHANGED — R-300-routing RESOLVED; remaining Claude-executable-now = `R-830` + `R-CXA-1/2/4` (operator-driven menu); dominant product step = operator-gated live R-100 e2e + R-300-second-provider creds. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **R-100 live e2e fired green (operator-authorized) + `just mvp-r100-real` recipe landed (PR #216 `ed2e827`)**, then this terminating refresh. Same session: operator asked "R-100 e2e" → ran AC#2 TOOL_STEP via live local ollama (free, PASSED 1.17s) + operator authorized the paid half → AC#1/#3/#4 real 3-step Anthropic `claude-haiku-4-5` inference workflow via `api.run` (PASSED 4.41s) through `just -E <root>/.env mvp-r100-real` (no shell secret-sourcing per `[[secrets-via-just-recipe-not-direct-sourcing]]`). Landed the recipe (PR #216, justfile-only). | Thirty-sixth terminating refresh — dashboard-only per §12.2.1. Hash `8fb5be5f6f46` → `e45a515b0dcd` (state at `ed2e827`, the #216 merge; PRS empty; fork count 43; batch-51). `R-100-mvp-real-workflow-execution` already RESOLVED (4/4) — this run re-validated it live; no roadmap-state change. Next action UNCHANGED — Claude-executable post-Phase-8 levers (`R-830`, `R-CXA-1/2/4`) are operator-driven menu picks; `R-300-multi-llm-second-provider` + further live runs operator-gated (creds). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **R-CXA-1 must_pass #1 Class 1 fork FILED (this PR)** — "continue" derivation post-R-300/R-100: grounded the top Claude-executable post-Phase-8 lever (`R-CXA-1-as-is-seam`) before opening. The secret-fetch production-caller edge fires eagerly at bootstrap stage 3a (`stage_3a_cp_clients.py:44`), but the AS→IS emitter materializes at stage 6 CXA_WIRING (`stage_6_cxa_wiring.py:70`) — firing-site-precedes-wiring-stage (U-RT-111 shape). Filed `.harness/class_1_fork_cxa_1_secret_fetch_audit_bootstrap_ordering.md` (PROPOSING; 4 Readings, B recommended; security safe-by-design per `SecretFetchEvent` metadata-only). Operator chose "file the fork doc" at AskUserQuestion. | Bundled (fork doc + roadmap §5 note + dashboard; no design-substrate touch) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. `open_fork_doc_count` 43 → 44. Hash left stale `e45a515b0dcd` — recompute owed at the post-merge terminating refresh. Next action: **operator AskUserQuestion to ratify a Reading** for the fork (then apply arc); meanwhile other Claude-executable levers (R-830 backend class / R-CXA-4) remain operator-driven menu picks. |

| 2026-06-01 | Thirty-seventh terminating refresh — PR #218 bundled R-CXA-1-fork-filing merged at `3885074`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `e45a515b0dcd` → `03d7322f983b` (state at `3885074`, the #218 merge; PRS empty; **fork count 44**; batch-51). Next action UNCHANGED — operator AskUserQuestion owed to ratify a Reading for the R-CXA-1 secret-fetch bootstrap-ordering fork; other Claude-executable levers (R-830 backend class / R-CXA-4) remain operator-driven menu picks. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-01 | **R-CXA-1 fork RESOLVED-AS-READING-D (this PR)** — apply arc opened on first-ratified Reading B; pre-substantive empirical orientation + advisor reconcile found it unwritable-as-previewed (bootstrap fetch = `resolve_bootstrap_value(NAME)`; scope-bearing `resolve()` has ZERO production callers; unsourced-field sentinels collapse the secret fingerprint to f(name); idempotency key timestamp-free = fire-once-forever; `SecretFetchEvent` machinery has no real producer — `emit_secret_fetch_audit` composes-and-discards). Operator re-ratified DON'T-WIRE (Reading D). must_pass #1 DEFERRED; R-CXA-1 PARTIAL; ZERO production code (avoided a vacuous LANDED). | Bundled (fork doc Status + §2.6 finding + roadmap §5 note + dashboard; no design-substrate touch) → NOT a terminating refresh; **follow-on terminating refresh owed** per §12.2.1. `open_fork_doc_count` stays 44 (fork doc APPLIED, not deleted). Hash left stale `03d7322f983b` — recompute owed at the post-merge terminating refresh. Next action: no auto-startable Claude-executable arc remains; R-830 / R-CXA-4 are operator-driven menu picks; live runs + R-300-second-provider creds-gated. **Pattern: advisor-before-substantive-work + use-the-product/grounding caught a vacuous-close before it shipped (2nd "wire-the-caller-is-producer-discovery" instance after U-RT-111).** |

| 2026-06-01 | Thirty-eighth terminating refresh — PR #220 bundled R-CXA-1-fork-resolution merged at `d77647b`; §12.2 owed follow-on | Single-file dashboard-only refresh per §12.2.1. Hash `03d7322f983b` → `42239f9ed02a` (state at `d77647b`, the #220 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no auto-startable Claude-executable arc remains (R-CXA-1 must_pass #1 deferred-as-Reading-D); R-830 / R-CXA-4 are operator-driven menu picks; live runs + R-300-second-provider creds-gated. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | Thirty-ninth terminating refresh — PR #239 substantive `live-auto` harness merged at `d7493de`; §12.2 owed follow-on. Fresh `/clear`→`continue` session: the session-start hook flagged `[ROADMAP DRIFT]` against the project-root dashboard; §12.1 audit found it was the benign §12.2.1 fixed-point lag (anchor at #237-state via #238's own merge). The operator-approved arc preserved the Jun-1 `live-auto/` harness off the superseded `r-xi-06-polish` worktree (#239); the companion `generate.py` visual iteration was NOT ported (superseded by Almanac Noir #234 — advisor-confirmed). | Single-file dashboard-only refresh per §12.2.1. Hash `fb0f95c78fac` → `284f7f750a01` (state at `d7493de`, the #239 merge; PRS empty; fork count 44; batch-51). Also reconciles the pre-existing one-commit anchor lag (dashboard had sat at #237-state `fb0f95c78fac` @ `23b80aef`, never capturing #238 `Surface XI complete`); this refresh advances to #239-state recording #238 + #239. Next action UNCHANGED — no auto-startable Claude-executable arc remains; R-830 / R-CXA-4 operator-driven menu picks; live runs + R-300-second-provider creds-gated. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | Fortieth terminating refresh — PR #241 substantive `research/` corpus commit merged at `4bafef7`; §12.2 owed follow-on. Same session as the #239/#240 arc: a git-sync audit (operator asked "is everything committed/pushed") surfaced 11 untracked top-level `research/` files cited as canonical substrate by `CLAUDE.md` §10.9/§13.5 but never tracked; operator-approved committing them (#241). | Single-file dashboard-only refresh per §12.2.1. Hash `284f7f750a01` → `61a20d7a9750` (state at `4bafef7`, the #241 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no auto-startable Claude-executable arc remains; remaining work operator-owned (R-700 ratification; creds/infra-gated R-300-second-provider / R-410..R-440 / R-500). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Post-#243/#244 refresh — 2 substantive merges + R-700 tee-up (this PR).** Fresh `/clear`→`continue` session executed two operator-picked arcs: **R-600 cadence-2** (PR #243 `62c98d4` — pattern-bake-in-sweep 2nd cadence + `R-600-memory-hygiene-normalization` RESOLVED) and **R-008 OD-4 gate (a)** (PR #244 `2233abb` — solo-developer §13.1 per-session redaction toggle, grounding-first, gate (a) FULLY CLOSED). Both merged green (squash); worktrees/branches cleaned; **~111 stale merged local branches pruned** (cross-referenced vs GitHub merged-PR head-refs since the repo squash-merges); local main ff'd to `2233abb`. | Substantive (both PRs touch `Project_Roadmap_v1.md`; #243 §5.6 R-600 + NEW R-600-memory-hygiene entry, #244 §5.6 R-008 + harness-od/CLAUDE.md OD-4 row) → **this is the owed follow-on terminating refresh** (Forty-first). Single-file dashboard-only per §12.2.1. Hash `61a20d7a9750` → `978cdde72d8e` (state at `2233abbc`, the #244 merge; PRS empty; fork count 44; batch-51). **Next action: R-700 Phase-8 ratification is now TEED UP for operator decision** — #244 spent the last Claude-executable OD-4 lever (gate (a) closed; gate (b) §13.2 is cross-axis/Phase-6+, not Claude-closeable), so OD-4 joins the bounded-residual-style sign-off set as a `RETIRED-AS-CROSS-AXIS-DEFERRED` candidate; the 4 R-700 PART C rulings (integer / CXA+CP-17 coverage / bounded-residual sign-offs incl. OD-4 / open-set declaration) await the operator. No auto-derivable Claude-executable ACTIVE row remains. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Forty-second terminating refresh — PR #246 substantive R-700-ratification-record merged at `165a0f4`; §12.2 owed follow-on.** Same session: the operator ruled on the teed-up R-700 PART C decisions (AskUserQuestion) → 3 of 4 RATIFIED (integer=46 cascade-deferred; CXA/CP-17 coverage CLOSED via existing R-CXA-1..4 + NEW R-010-cp-17; sign-offs incl. OD-4-as-RETIRED-AS-CROSS-AXIS-DEFERRED), formal declaration HELD. #246 recorded them (NEW R-010 entry + R-700/R-008 notes + R-700 draft PART C). | Single-file dashboard-only refresh per §12.2.1. Hash `978cdde72d8e` → `d522f4624fc3` (state at `165a0f49`, the #246 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — R-700 stays BLOCKED on the HELD operator declaration only (no Claude-executable arc; the formal close authors `phase-8-graduation.md` + the deferred 48→46 cascade + bucket flips when the operator lifts the hold). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Forty-third terminating refresh — PR #248 doc-hygiene merged at `e725f89`; §12.2 owed follow-on (cosmetic).** Same session: a repo-hygiene assessment found the per-axis CLAUDE.md §1.2 "authority" pointers badly stale (OD spec v1.4 vs canonical v1.27; CP v1.3 vs v1.30 — the exact foot-gun that misled this session's R-008 grounding) → bumped to canonical HEAD (#248) + gitignored local scratch. Grounding flipped the "#2 junk-dup deletion" premise (the files are not junk — `[[design-substrate-version-identity-hazards]]`), so #2 was deliberately NOT done. | Single-file dashboard-only refresh per §12.2.1. Hash `d522f4624fc3` → `811f1c3087dc` (state at `e725f89f`, the #248 merge; PRS empty; fork count 44; batch-51). **Next action UNCHANGED** — #248 touched per-axis CLAUDE.md + .gitignore only (NO roadmap §5 / R-NNN state change), so the §4 derivation is unmoved: no auto-derivable Claude-executable ACTIVE row remains (R-600 sweep not due — 5 of ~10 PRs since cadence-2; R-700 BLOCKED on the HELD operator declaration; creds/infra items gated). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Forty-fourth terminating refresh — PR #250 substantive no-parking-directive merged at `f263857`; §12.2 owed follow-on.** Operator made standing: operator-labeled items (`halt-route-to-operator` / operator-owned / infra-gated / PROPOSED / DEFERRED) must NOT park a session at "no Claude work"; the operator executes nothing manually. #250 encoded it in CLAUDE.md NEW §12.4.1 + roadmap §4 (`NO_ACTIVE_CANDIDATES` ≠ terminate) + the "Next action" banner here. | Single-file dashboard-only refresh per §12.2.1. Hash `811f1c3087dc` → `bdf76e986f48` (state at `f2638574`, the #250 merge; PRS empty; fork count 44; batch-51). **Next-action SEMANTICS CHANGED** — "no auto-`ACTIVE` row" is no longer a stop: future sessions drive the highest-value forward item's Claude-executable slice to the genuine gate (R-410..R-440 / R-300-second-provider / R-CXA-2 / R-810/820 slices), surfacing only real decisions/creds/paid-calls. Held operator decisions (R-700) honored; paid-call/secret boundary unchanged. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Forty-fifth terminating refresh — PR #252 substantive carve-out-hardening merged at `a91b065`; §12.2 owed follow-on.** Ensuring clean `/clear`→continue continuation surfaced a real bug: the session-start hook's §12.2.1 lag-expected carve-out matched only `post-PR-NN` refresh titles, but this session's refreshes (#245/#247/#249/#251) used `post-#NN` → the next fresh session would have mis-flagged the expected lag-by-one as `[ROADMAP DRIFT]`. #252 re-keyed the carve-out on the prefix `^ops: roadmap status refresh ` + reconciled the protocol docs. **This refresh's own title uses that prefix, so the hardened hook recognizes it** — the fixed point now self-validates. | Single-file dashboard-only refresh per §12.2.1. Hash `bdf76e986f48` → `47fedc3fbf07` (state at `a91b065b`, the #252 merge; PRS empty; fork count 44; batch-51). Next action UNCHANGED — no-parking directive stands (drive the highest-value forward item's slice to the genuine gate; R-700 declaration HELD; creds/infra gated). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit. |

| 2026-06-02 | **Substantive grounding PR (post-#252 fixed-point; `/clear`→continue) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Two producer-discovery grounding findings recorded against roadmap §5 entry notes + the dashboard banner; NO code/spec change (mode-agnostic). (1) **R-CXA-2 grounded → gates on engine-layer substrate, NO wireable slice** (2/6 §16.5 methods fired; 4/6 blocked by deliberately-STRUCK U-RT-111 X-AL-3 gaps) — 3rd CXA seam → 3rd DEFER, `[[r-cxa-seam-wiring-is-producer-discovery]]`. (2) **R-300-second-provider's mock cross-family fixture is ALREADY BUILT** (`test_iterates_three_candidates_until_success`); remaining R-300 work is live-creds-only. Banner staleness fixed: R-XI-02/R-XI-03 PROPOSED→RESOLVED (Surface XI COMPLETE). | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` NOT recomputed here — the follow-on terminating refresh recomputes it post-merge per §12.2.1. Next action UNCHANGED: high-value forward menu is genuinely slice-exhausted of un-built no-creds Claude work; every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440, R-500), the HELD R-700 declaration, or the dispositioned R-CXA-2 engine-layer arc. No-parking-compliant terminal (genuine gate reached after honest grounding), not a park. |

| 2026-06-02 | **Forty-sixth terminating refresh — PR #254 substantive grounding PR merged at `b19d26d`; §12.2 owed follow-on (this).** Same `/clear`→continue session: two producer-discovery grounding findings recorded (R-CXA-2 gates-on-engine-substrate / R-300 fixture-already-built) + stale Surface-XI banner fixed; NO code/spec change. | Single-file dashboard-only refresh per §12.2.1. Hash `47fedc3fbf07` → `9cd2b8ba6b36` (state at `b19d26d1`, the #254 merge; PRS empty; fork count 44; batch-51). **Next action UNCHANGED** — the high-value forward menu is genuinely slice-exhausted of un-built no-creds Claude work (every remaining item gates on operator creds/infra, the HELD R-700 declaration, or the dispositioned R-CXA-2 engine-layer arc); the no-parking terminal stands. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix per #252). |
| 2026-06-02 | **Substantive grounding+fork PR #256 (post-#254 fixed-point; `/clear`→continue) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** No-parking-compliant (§12.4.1): drove R-410 (the only un-blocked forward row with an un-built Claude slice). Grounding (advisor-confirmed; R-410 `advisor_required: yes`) found the resolved sandbox tier is **never enforced** (`SandboxDispatchDecision` → tier-floor + span attrs only; execution tier-independent; spec v1.41 §14.9.8 specs only the resolver). Building = X-AL-3 extension → **filed** `class_1_fork_sandbox_tier_no_execution_driver_contract.md` (PROPOSING; Readings A/B/C; C10⊥C11; routes to design-phase at the R-410 resolution arc). Fork count 44→45. R-410 roadmap note repointed at the fork. | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `501acfc` (`9a1fff34a930`, forks 44) per the line-155/#138 substantive-PR convention — the follow-on terminating refresh recomputes against the #256 merge commit (forks 45) post-merge per §12.2.1. **Next action:** after #256, the forward menu's un-built no-creds slices are genuinely exhausted again — every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), the HELD R-700 declaration, or STRUCK U-RT-111 substrate (R-CXA-2). The R-410 *design half* is now filed+routed (was the last separable design slice); its *infra* half stays gated. |
| 2026-06-02 | **Forty-seventh terminating refresh — PR #256 substantive grounding+fork PR merged at `0fca58f`; §12.2 owed follow-on (this).** Same `/clear`→continue session: filed the R-410 execution-driver Class 1 fork (the design slice #254 missed — resolved sandbox tier never enforced; PROPOSING; Readings A/B/C; C10⊥C11; routes to design-phase). | Single-file dashboard-only refresh per §12.2.1. Hash `9a1fff34a930` → `0e8a496ad344` (state at `0fca58fa`, the #256 merge; PRS empty; **fork count 45**; batch-51). **Next action UNCHANGED** — after the R-410 design half is filed+routed, the forward menu's un-built no-creds slices are genuinely exhausted again: every remaining item gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), the HELD R-700 declaration, or STRUCK U-RT-111 substrate (R-CXA-2). No-parking-compliant terminal. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix per #252). |
| 2026-06-02 | **🎓 Substantive R-700 Phase-8 graduation PR #258 (operator lifted the HELD declaration) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Authored `.harness/phase-8-graduation.md` (canonical 46/54 RETIRED + 49/54 pipeline-advanced); forward supersession at ledger §11.7 + harness-cp/CLAUDE.md + this dashboard; R-700 → RESOLVED; bucket dispositions flipped (labels ≠ count-membership). Advisor confirmed 46 is forced. **Terminal milestone — surfaced for operator review/merge, NOT auto-merged.** | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `8694fa0` (`54ad811b3ed9`, forks 45) per the substantive-PR convention — the follow-on terminating refresh recomputes against the #258 merge commit post-merge per §12.2.1 (after the operator merges). **Phase 7 (substitution-retirement) COMPLETE; the workspace is now wholly on the post-Phase-8 activation/deployment/integration axis (Surfaces IV/V/VI).** Next-action: per §12.4.1 drive the highest-value forward item to its genuine gate — all remaining slices gate on operator creds/infra (R-300-second-provider, R-410..R-440, R-500), or a dispositioned design arc (R-CXA-2/3, R-810/820). |
| 2026-06-02 | **🎓 Forty-eighth terminating refresh — PR #258 Phase-8 graduation merged at `155935c` (operator-approved); §12.2 owed follow-on (this).** Operator gave the merge go-ahead for the terminal milestone. **PHASE 8 CLOSED:** canonical 46/54 RETIRED (85.2%) + 49/54 pipeline-advanced (90.7%); R-700 RESOLVED; forward supersession landed (ledger §11.7 + harness-cp/CLAUDE.md + dashboard). | Single-file dashboard-only refresh per §12.2.1. Hash `54ad811b3ed9` → `2abef0363e76` (state at `155935cb`, the #258 merge; PRS empty; fork count 45; batch-51). **Next action:** Phase 7 (substitution-retirement) is COMPLETE; the workspace is wholly on the post-Phase-8 activation/deployment/integration axis. Per §12.4.1 a future session drives the highest-value forward item to its genuine gate — every remaining slice gates on operator creds/infra (R-300-second-provider, R-410..R-440 execution/deploy, R-500), or a dispositioned design arc (R-CXA-2/3 engine-substrate/composer, R-810/820 files/managed-agents). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-02 | **Substantive roadmap-design-extension PR #260 (operator-requested post-R-700) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Scoped NEW PROPOSED `R-600-substitution-ledger-schema` (Surface VII): extract the 54-row substitution ledger to a typed source + derived counts + a CI tally gate, motivated by the R-700 `48/54`-internally-impossible bug. roadmap §5.6 only (+ this dashboard). | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `0a264d9` (`f8cfaa064640`, forks 45) per the substantive-PR convention — the follow-on terminating refresh recomputes against the #260 merge commit post-merge per §12.2.1. **Next action UNCHANGED** — the new entry is PROPOSED (not ACTIVE); it joins the forward menu but does not auto-start. Per §12.4.1 a future session may drive it (or the operator authorizes execution). |
| 2026-06-02 | **Forty-ninth terminating refresh — PR #260 roadmap-design-extension merged at `db96a61`; §12.2 owed follow-on (this).** Scoped NEW PROPOSED `R-600-substitution-ledger-schema` (operator-requested post-R-700). | Single-file dashboard-only refresh per §12.2.1. Hash `f8cfaa064640` → `784643914056` (state at `db96a61c`, the #260 merge; PRS empty; fork count 45; batch-51). **Next action UNCHANGED** — the new entry is PROPOSED (joins the forward menu; does not auto-start). Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-03 | **Substantive R-600 build PR #262 (operator-authorized) — drops the `ops: roadmap status refresh` prefix → §12.2 owed follow-on terminating refresh.** Built the substitution-ledger schema: `.harness/substitutions.yaml` (single source) + `tools/substitution_ledger.py` derive/validate + a blocking CI tally gate + `generate.py` consuming the derivation. R-600 → RESOLVED; the count-drift defect class is now CI-caught. | NOT a terminating refresh (substantive — title drops the prefix); `workspace_state_hash` anchored to the **pre-this-PR-merge** commit `53a3a7b` (`fc784f5b0cb1`, forks 45) — the follow-on terminating refresh recomputes against the #262 merge commit post-merge per §12.2.1. **Next action UNCHANGED** — Phase 7 complete; workspace on the post-Phase-8 activation/deployment/integration axis (every remaining slice gates on operator creds/infra or a dispositioned design arc). |
| 2026-06-03 | **Fiftieth terminating refresh — PR #262 R-600 build merged at `63ae9f9`; §12.2 owed follow-on (this).** The substitution-ledger schema is LIVE: `.harness/substitutions.yaml` single source + `tools/substitution_ledger.py` derivation + a blocking CI tally gate (validates against itself on every PR). R-600 RESOLVED. | Single-file dashboard-only refresh per §12.2.1. Hash `fc784f5b0cb1` → `26a6d6cc90cf` (state at `63ae9f9f`, the #262 merge; PRS empty; fork count 45; batch-51). **Next action UNCHANGED** — Phase 7 complete; the count-drift / stale-carry defect class is now CI-caught on the accounting surface; remaining work gates on operator creds/infra or a dispositioned design arc. Recursion-stopping fixed point: dashboard lags by exactly one commit (this refresh's own merge), recognized as `lag-expected` by the next §12.1 session-start audit (carve-out keys on the `^ops: roadmap status refresh ` prefix). |
| 2026-06-04 | **Multi-agent collision — completeness follow-on to the parallel `1f9bf76 post-#294` refresh.** #292/#293/#294/#295 all merged ~concurrently from parallel sessions. The `1f9bf76` refresh recorded only #294 (missed #293 BMAD-install + #295 council-productize; stale-listed merged #295 in-flight; anchor pinned at #294). This refresh records #293 + #295, clears in-flight, re-pins the anchor to the current tip. | Hash `f853b6b2478e` → `46d22535c509` (state at `343fe45f`/`1f9bf76` tip; PRS empty; fork count 45; batch-51). The `1f9bf76` tip already satisfied the `session-start.sh` carve-out (title-prefix + roadmap_status.md-only), so no false drift was pending; this is a content-completeness refresh (records the BMAD install #293 unrecorded by the parallel agent). HEAD remains a roadmap_status.md-only `ops: roadmap status refresh` commit → next §12.1 audit `lag-expected`. `roadmap.html` regen owed separately (parallel refresh also skipped it). |
| 2026-06-04 | **`roadmap.html` regen + closing terminating refresh — completes the 3-commit shape (`8fae324` status → `ab5b946` html → this closing status).** The committed Pages snapshot was stale since #289; regenerated via `generate.py` (69 R-NNN actions, 0 open PRs, retirement 46/54). | Hash `46d22535c509` → `fa5910d260d5` (state at `ab5b9467`, the html-regen commit; PRS empty; fork count 45; batch-51). `git_head` re-pinned to the html-regen commit; HEAD is this roadmap_status.md-only closing refresh → next §12.1 audit `lag-expected` (carve-out: title-prefix + roadmap_status.md-only). |

**Audit protocol exercised across 31 terminating-refresh closures + 2 fresh-session reconciliations + 10 substantive closes + 1 substantive partial + 1 Class 1 fork filing + 1 Class 1 fork resolved-as-defer + 1 operator-approved 3-PR merge cluster + R-XI-01 dashboard build + 1 process-discipline sweep (R-600-pattern-bake-in).** Discipline + enforcement layers operational; hook hardened against the local-behind-origin drift class at PR #140. **2026-06-01 session: landed the R-100 tool-step apply cluster (#173/#171/#172), closed R-600-clearance-marker-backfill-survey (#175), built+landed R-XI-01 operator dashboard MVP (#177), and ran the first R-600-pattern-bake-in-sweep (ACTIVE-SURVEYED; v1.14 absorption deferred).** **All Claude-executable substantive arcs are drained.** The deterministic next-action is operator-owned: **(a) run the live R-100 e2e**, and/or **(b)** any operator-scoped design-phase arc (e.g. the `R-600-workflow-v1-14-amendment` the sweep now has evidence for). Remaining Claude-executable mode-agnostic work = MEMORY.md hygiene audit + fork-doc Status cadence sweep; §V/VI/IV gated on MVP-shipped / live e2e.

---

## Audit checklist (run at session start)

- [ ] `workspace_state_hash` matches computed value per recipe above
- [ ] All open PRs at GitHub appear in `In-flight`
- [ ] `recently_completed` reflects last 5 merged PRs
- [ ] `latest_retirement_batch` matches `ls .harness/phase-7d-retirement-events-batch-*.md | tail -1`
- [ ] No `R-NNN` at `Project_Roadmap_v1.md` §5 has `status: ACTIVE` while its `depends_on` are not all RESOLVED (re-derive per §4)

If any check fails → HALT, route per `Project_Roadmap_v1.md` §7.1 step 4.

---

*End of dashboard. Master roadmap at `Project_Roadmap_v1.md`. Enforcement at `CLAUDE.md` §12.*
