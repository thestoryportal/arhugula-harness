# Roadmap status dashboard

*Refreshed at session-start audit + post-PR-merge audit per workspace `CLAUDE.md` §12. Consumed by the next-action derivation rule at `Project_Roadmap_v1.md` §4. **Do not hand-edit during execution — refresh via the protocol at `Project_Roadmap_v1.md` §7.2.***

---

## Workspace state anchor

| Field | Value |
|---|---|
| `workspace_state_hash` | `e9cc83fbf870` |
| `last_refreshed` | 2026-06-20T14:00:00+00:00 |
| `git_head` | `7998914f` — the substantive arc is #671 **feat(runtime): R-FS-1 `B-INTERSTEP-PERRUN-ISOLATION` — ContextVar per-run holder isolation (runtime v1.64 §14.21.5 inv 7 CLOSED)**. The **15th standalone `B-*` arc** (the FIRST beyond the originally-enumerated 14-arc frontier) — closes the registered B-INTERSTEP follow-on §3/§5. B-INTERSTEP allocated its run-scoped `InterStepOutputChannel` (and the sibling always-on `CostRecordAccumulator`, §82 Class-3) at BOOTSTRAP scope on the frozen `HarnessContext`; in daemon-client mode (U-RT-108) one ctx serves many concurrent `run_workflow` invocations → they SHARED the holder. Invariant 7 was bounded to (a) sequential reuse + (b) concurrent-within-timeout (a per-loop single-flight LOCK), with (c) the `drain_timeout_seconds` TIMEOUT-ZOMBIE a registered residual a released lock can't fence. **The build (fork-prescribed ContextVar):** each holder is scoped per run via a ContextVar resolved at dispatch; the frozen ctx binds a stable proxy (`RunScopedInterStepOutputChannel` / `RunScopedCostRecordAccumulator`, IS-A the field type) delegating to the current run's holder. The `run_workflow` handler sets a fresh holder per run; the set propagates into the `asyncio.to_thread` worker via `contextvars.copy_context()` (verified empirically) — so (a)+(b) need NO lock and (c) is closed (a zombie writes only the holder captured in ITS copy). The single-flight lock is removed. **Both holders, one mechanism:** channel (opt-in; opt-out stays None, byte-identical) + the always-on cost accumulator (the per-dispatch cost wrappers now thread the PROXY, not a `.records` list captured once at bootstrap — the capture that defeated isolation; `api.run`/`resume` set a fresh accumulator around `[invoke + cost-read]`). Other bootstrap-ctx holders assessed (resume_context_holder set-per-run; engine_output_store run_idempotency_key-keyed; ledger writers ADR-F2 single-writer + run_id-tagged) — none need isolation; not a channel-only false-complete. **NO operator gate** (additive correctness, no committed-invariant sacrifice, opt-out byte-identical; advisor + Codex, not council). **Decorrelated review:** advisor (full-transcript — the ContextVar-propagation probe, the always-on cost-path, the opt-out guard, the child-inheritance blind-spot) + out-of-family Codex — which caught a real [P2] (the handler must reset its self-set var so sequential same-task reuse re-allocates) → fixed (token + finally reset, mirroring `_CURRENT_TOOL_CTX`) → re-review CLEAN. Bundled-absorption: runtime spec v1.63 → v1.64 (§14.21.5 inv 7 CLOSED + §14.21.7 follow-on BUILT) + fork doc + clearance marker + `harness-runtime/src` + by-execution tests (timeout-zombie deliverable + negative controls) + spine-ledger BUILT. Gates: pyright 0/0/0 · ruff · harness-runtime 1974 passed / 13 skipped · harness-cp 1093 passed / 1 xfailed · no §5.2-hash change · CI 14/14 blocking green · X-AL-3 green (clearance marker + fork doc). *(prior #669 `B-L2-EMBEDDING-ACTIVATION` narrative preserved in git history.)* Lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). |
| `latest_retirement_batch` | `.harness/phase-7d-retirement-events-batch-57.md` |
| `open_fork_doc_count` | 81 |

**Hash recipe.** `sha256(git_head[:8] + "|" + sorted_open_pr_csv + "|" + open_fork_doc_count + "|" + latest_retirement_batch_path)[:12]`. See `Project_Roadmap_v1.md` §7.1.

---

## Next action

**Purpose.** Section 02 names the next Claude/Codex-executable frontier. Completion history lives in `Recently completed`; the full remaining itemization lives in `Remaining forward work` (next section).

**Frontier.** The R-FS-1 full-spec build umbrella (operator directive 2026-06-12) remains the single **ACTIVE** program. Its **FROZEN order is COMPLETE — all 11 ordered child arcs landed** (B1…M). R-FS-1 stays ACTIVE because the **standalone `B-*` build arcs** (surfaced during the frozen builds; full-spec, nothing-deferred) remain — they are the next-action frontier; the quality track stays gated behind R-FS-1's resolution.

**Next = the autonomous standalone `B-*` build arcs (R-FS-1's continuing work, FULL-SPEC committed, no operator gate).** **`B-MCP-HOST-REMOTE-TRANSPORT` ✅ COMPLETE (#640)** — the FIRST standalone `B-*` arc landed since the FROZEN order completed: the stage-3a factory now admits remote streamable-HTTP MCP hosts (a `_coarse_transport` granular→coarse projection; impl-to-cleared-spec against runtime §14.9.6 inv 5, no operator gate), and BOTH B6-skipped default-lane tool-completion e2es are RESTORED on a real remote streamable-HTTP echo server (remote L1 READ_ONLY → TIER_1, no Docker; `test_r100_ac2` 8/8 non-flaky in CI + `test_r_cl_p3` 3/3 live-Ollama). **🎉 R-FS-1 FROZEN order COMPLETE — 11/11:** B1✅ B3✅ E✅ B2✅ R✅ B4✅ CA✅ B5✅ B6✅ B7✅ M✅. **`B-MEMORY-SURFACE-BACKEND-IMPLS` ✅ COMPLETE (#642)** — the SECOND standalone `B-*` arc: both spec'd-but-unbuilt memory backends built (`ENCRYPTED_FILESYSTEM` + `OPERATOR_DEFINED`; impl-to-cleared-spec §14.12.3). **`B-COST-DISCRIMINATOR-TAXONOMY` ✅ COMPLETE (#644)** — the THIRD standalone `B-*` arc: the latent Class-1 contract-vs-production defect (production helpers wrote dispatch-type strings into the `CrossFamilyTag`-validated `provider_discriminator`) reconciled by a NEW typed `DispatchKind` dimension + `RollupAxis.PER_DISPATCH_KIND` + the new `RunResult.cost_attribution_by_dispatch_kind` field; `provider_discriminator`→`str|None` (per-dispatch carries `None`, the family tag is a §15.3 chain concept); bundled-absorption OD v1.30 §15.1 + runtime v1.57 §9 + OD plan v2.28, no operator gate, advisor + Codex clean. **`B-NONLINEAR-OVERRIDE-PROVENANCE` ✅ COMPLETE (#648)** — the FOURTH standalone `B-*` arc: the per-step override-application state-ledger entry (`cp.per-step-override-application`, U-CP-14 §16.5) now fires on **all 6 topologies** (linear inline; the 5 non-linear strategies via the **buffered-branch path** — `append_branch_override_ledger_entry` buffered into each branch's `BufferingLedgerWriter`, drained on the driver thread realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across all topologies; impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh), NO operator gate; advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer mis-count, lost-override-on-failed-dispatch, missing CASCADE_CANCEL terminal). **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` ✅ COMPLETE (#649)** — the FIFTH standalone `B-*` arc: `RollupAxis.PER_PROVIDER_DISCRIMINATOR` is now **non-vacuous in production** — the LLM cost path stamps each `SpanCostRecord.provider_discriminator` with the dispatched provider's family (`ANTHROPIC → frontier_managed`, `{OPENAI, GOOGLE} → frontier_managed_alt`, `LOCAL_OPEN_WEIGHT → local_ollama`; new `cross_family_cost_tag.py` map, carrier-homed in runtime) surfaced on a NEW optional `RunResult.cost_attribution_by_provider_discriminator` (runtime v1.58 §9). The A/B fork was advisor-resolved as a span-layer-vs-record-layer false dichotomy (§15.1.2 assigns the field-population to this arc); impl-to-cleared-spec, no OD spec change, runtime-only, NO operator gate; this axis partitions the LLM subtotal (skips `None`-tag non-LLM records). Bundled doc-hygiene reconciled the v1.57 §9-body-drift. **`B-INTERSTEP` ✅ COMPLETE (#651)** — the SIXTH standalone `B-*` arc: a run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (foundational; `EVALUATOR_OPTIMIZER` the load-bearing case). NO operator gate (additive + opt-in, default byte-identical). Invariant 7 honestly bounded to 3 daemon-reuse cases (5 Codex bugs across 4 rounds; the r5 systemic timeout-zombie residual registered as `B-INTERSTEP-PERRUN-ISOLATION`). **Autonomous next builds (no operator gate, FULL-SPEC committed; ground at arc-open per `[[grounding-reveals-claude-closeable-slice-close-honestly]]`, pick the highest-value):** **`B-HITL-PLACEMENT-PER-STEP-PRODUCER` ✅ COMPLETE (#657)** — the NINTH standalone `B-*` arc: the wrap-time HITL gates (inference / sub-agent / tool — incl. #653 `B-TOOL-GATE`) now **fire in production** — the CP driver surfaces the workflow's declared `WorkflowManifestEntry.hitl_placements` onto the per-step `StepExecutionContext.hitl_placements` (all 5 construction sites; branch children inherit via `compose_branch_child_context`), and the composer reads that surface (runtime §14.8.2 step-1 reconciled `step`→`step_context`). Closes the B3 placement-composition residual the #653 Codex [P1] surfaced. Read surface = `StepExecutionContext` (NOT `StepEffectiveBinding` — avoids the §16.5.4 override-hash ripple). Bundled-absorption runtime v1.61 + CP v1.41, NO operator gate (additive + opt-in, default `()` byte-identical); advisor full-chain-witness catch (producer + composer composed in one real `execute_workflow` run) + excluded-cell sanity + out-of-family Codex clean. **`B-FANOUT-PAUSE` ✅ COMPLETE (#661)** — the ELEVENTH standalone `B-*` arc: resumable `cascade_policy=pause` fan-out for `ORCHESTRATOR_WORKERS` (the cleared §25.15.1 `pause → PAUSED` materialized; a `FanOutResumeState`-bearing snapshot recovers orchestrator + terminal-branch outputs, `api.resume` skips terminal branches + re-dispatches the rest per obl. 7; CP v1.41 → v1.42 §26.2; NO operator gate — the R-CC-1 §1.1 self-documented re-open trigger; decorrelated review (advisor + 2 Codex rounds + CI pyright) caught SIX silent-degradation/false-resumable holes, all fixed; gated to top-level ORCHESTRATOR_WORKERS, 4 forward arcs registered). **`B-ENGINE-OUTPUT-REPLAY` ✅ COMPLETE (#665)** — the TWELFTH standalone `B-*` arc: the output-carrying event-history substrate (NEW runtime C-RT-32 `EngineOutputStore`, v1.63 §14.23) materializing the C-CP-08 §8.1 "activity outputs cached and replayed" clause that LINEAR `EVENT_SOURCED_REPLAY` shipped DEGENERATE — the CP driver records each step output to a dedicated hand-rolled durable store (per-run JSONL, `JournalWorkflowPauseStore` crash-safety) **BEFORE** the F2 ledger-append (RESERVE-before-COMMIT), and on resume `_rehydrate_inter_step_channel_on_replay` replays the stored prefix into the B-INTERSTEP channel so the first re-dispatched step reads its recovered predecessor (proven end-to-end through the REAL `RuntimeLLMDispatcher` → real provider). Substrate-fork probe-resolved to the dedicated store over an IS `EntryPayload` extension (advisor not council; the F2 ledger stores a digest BY DESIGN). NO operator gate (R-CC-1 §1.1 self-documented §6 re-open trigger — the linear analogue of B-FANOUT-PAUSE's fan-out re-open). Decorrelated review composed on the `read_outputs()==empty` resume edge: advisor caught the config-flip false-FAILURE, Codex caught the read-failure false-SUCCESS → a file-existence discriminator (`journal_exists`: absent→degrade, present-unreadable→fail-closed). **`B-LAYER-BUDGET-OVERRIDE` ✅ COMPLETE (#667)** — the THIRTEENTH standalone `B-*` arc: the L3 router timeout at `infer()` now uses the C-CP-03 §3.1 OVERRIDE-RESOLVED effective budget (keyed on the request's `workload_class` + `persona_tier`), not the flat field — materializing the §3.1 tuning surface §3.1 commits explicitly on `llm_as_router` (built-but-vacuous: zero prior callers). NO operator gate (§3.1 affirmatively commits the override → impl-to-cleared-spec; the prior in-code X-AL-3 note overturned by re-grounding, advisor-confirmed; behavior-preserving by default). Capability-built + production-dormant (the dormant `RuntimeLLMDispatcher.budgets` seam; live firing gated by the UNOWNED routing-activation gate). **`B-L2-EMBEDDING-ACTIVATION` ✅ COMPLETE (#669)** — the FOURTEENTH + LAST originally-enumerated standalone `B-*` arc: the §2.2 routing-activation gate (opt-in `routing_activation`; DECLARATIVE declines on a manifest-miss → EMBEDDING → L3). impl-to-cleared-spec (#653 pattern, no spec leg); 3 Codex [P2] bugs fixed/guarded + 1 false-premise [P1] rejected on primary-source; real-fastembed factory e2e. **⚑ The originally-enumerated standalone `B-*` frontier is COMPLETE (14/14) — but the register GREW, not emptied:** this arc spawned 2 NEW BUILD arcs (`B-ROUTING-MANIFEST-MODEL-FOLD`, `B-L2-FALLBACK-COMPOSITION`), joining the standing follow-on register (`B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT`, `B-FANOUT-PAUSE-*` / `B-HANDOFF-PAUSE` / `B-HIERARCHICAL-PAUSE`, `B-EFFECT-FENCE-*`, `B-INTERSTEP-*`, `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD`, `B-EDIT-CARRIER-*`, …). **NEXT = the forward follow-on `B-*` register** (FULL-SPEC, no-parking) — highest-value next is **`B-L2-FALLBACK-COMPOSITION`** (it + a 2nd provider are what make routing_activation production-reachable). **`R-CL-Q1` does NOT auto-unblock** — routing_activation isn't production-reachable yet + the register grew, so admitting the whole-harness quality track is a GENUINE OPERATOR scoping call (surface via AskUserQuestion next iteration, do NOT auto-route). Newly-registered follow-on (from #665): `B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT` (WAL_SEGMENT shares the store, E-impl-2). Newly-registered follow-ons (from #661): `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / `B-FANOUT-PAUSE-PARALLELIZATION` (sibling fan-out-barrier pause-resume) / `B-HANDOFF-PAUSE` (DECENTRALIZED_HANDOFF single-owner handoff-pause) / `B-HIERARCHICAL-PAUSE` (resume across the child-bootstrap recursion boundary); (from #655): `B-EFFECT-FENCE-HITL-ROUTE` (turn the interim fail-closed raise into a genuine §22.1 operator-resolvable PAUSE), `B-EFFECT-FENCE-PER-TOOL` (a `ToolContract` idempotency classification so only declared-non-idempotent tools are fenced — cross-axis AS surface), `B-EFFECT-FENCE-DURABLE-AUTO` (auto-activate the fence under a durable `engine_class`, removing the opt-in flag); (from #651) `B-INTERSTEP-NONLINEAR` (concurrent-fan-out recording), `B-INTERSTEP-PERRUN-ISOLATION` (per-run channel isolation — removes the single-flight + closes the timeout-zombie + covers `cost_record_accumulator`); (from #657) `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD` (the per-step `StepOverride.hitl_placement` override fold composed with the workflow tuple — design-fork-first per X-AL-3; the fold semantics are silent at C-CP-06 §6.2). `B-TAIL-CONDITIONAL-SAMPLING` remains R-420/R-421-gated; `B-PER-DISPATCH-DRIVER-PRECISION` is registered-only (Option A chosen → N/A); cross-host effect fencing folds into deferred F-CC. **Still gated:** `R-CL-Q1→C1` (quality track) — admits only after R-FS-1 fully resolves (the standalone `B-*` arcs land); quality runs once, on the complete full-spec harness. Ground at arc-open (presence-not-correctness) + no-parking (`[[feedback-operator-labels-are-claude-driven-no-parking]]`). **Forward items registered:** `B-TAIL-CONDITIONAL-SAMPLING` (B7 #632; gated). **Landed:** `B-MCP-HOST-REMOTE-TRANSPORT` (#640), `B-MEMORY-SURFACE-BACKEND-IMPLS` (#642), `B-COST-DISCRIMINATOR-TAXONOMY` (#644), `B-NONLINEAR-OVERRIDE-PROVENANCE` (#648), `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION` (#649), `B-INTERSTEP` (#651), `B-TOOL-GATE` (#653), `B-EFFECT-FENCE` (#655), `B-HITL-PLACEMENT-PER-STEP-PRODUCER` (#657), `B-EDIT-CARRIER` (#659), `B-FANOUT-PAUSE` (#661), `B-ENGINE-OUTPUT-REPLAY` (#665), `B-LAYER-BUDGET-OVERRIDE` (#667), `B-L2-EMBEDDING-ACTIVATION` (#669).

**Recurring lanes** run on cadence alongside: `R-600-*` (pattern bake-in, codex out-of-family review), `R-IF-roadmap-refresh`.

**Do not re-open as next action.** Closed/parked: `R-411/412/420/421/430`, `R-500`, `R-810/820/830`, `R-008`/OD-4, `R-CXA-1/2/4`, `R-100-*`, `R-300-*` (both routing-activation #213 and second-provider #281/#283 RESOLVED), `R-901` (research frontier closed). `R-CL-Q1→C1` (quality track) **stays BLOCKED behind R-FS-1** — the FROZEN order is 11/11 (#637), but R-FS-1 (full-spec, nothing-deferred) is NOT resolved while the standalone `B-*` build arcs remain; quality runs once, on the *complete* full-spec harness, so it admits only after the standalone arcs land.

---

## Remaining forward work

**The full-spec build (R-FS-1).** All remaining forward build work is child arcs of the single ACTIVE umbrella **R-FS-1** (operator FULL-SPEC directive 2026-06-12 — nothing deferred; every capability built beyond MVP).

**Single arc→unit source.** The full itemization — all 11 arcs in plain language, with status, build position, before/parallel dependencies, and every atomic unit (real as-built for the 5 done arcs; anticipated slices for the 6 remaining) — lives in **`.harness/r-fs-1-arc-and-unit-map.md`**. The dashboard's Build-progress section parses that file; this section is a pointer, not a second parseable copy (no duplicate to drift). Per-arc grounding leads: `.harness/r-fs-1-remaining-arcs-grounding-sweep-v1.md` (re-ground at arc-open, presence-not-correctness). Spine ledger (every surfaced boundary + standalone arc): `.harness/beyond-mvp-capability-boundary-ledger.md`.

**Frozen order** (B1→B3→E→B2→R→B4→CA→B5→B6→B7→M) — **COMPLETE, 11/11.** DONE: **B1✅ B3✅ E✅ B2✅ R✅ B4✅ CA✅ B5✅ B6✅ B7✅ M✅** (R incl. L3 LLM_AS_ROUTER #604 + L2 EMBEDDING #606; B4 incl. Slice 1✅#616 / 2✅#618 / 3✅#619 / 4✅#621; CA✅#625 runtime spec v1.53 §9; B5✅#628 `resolve_backend` surface-map; **B6 = Slice 1✅#630 STDIO transport-floor [runtime v1.54 §14.9.8] + Slice 2✅#637 per-tool tier + per-dispatch driver [runtime v1.56 §14.9.11 / AS v1.11; `B-PER-TOOL-SANDBOX-TIER`]**; B7✅#632 OD sampler §9.2 attribute-conditional predicate, OD SSOT; M✅#635 C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`). **🎉 R-FS-1 FROZEN order COMPLETE (11/11); R-FS-1 stays ACTIVE.** NEXT: the **standalone design-fork-first `B-*` arcs** (R-FS-1's continuing work — autonomous, FULL-SPEC committed — landed: `B-MCP-HOST-REMOTE-TRANSPORT`✅#640, `B-MEMORY-SURFACE-BACKEND-IMPLS`✅#642, `B-COST-DISCRIMINATOR-TAXONOMY`✅#644, `B-NONLINEAR-OVERRIDE-PROVENANCE`✅#648, `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`✅#649, `B-INTERSTEP`✅#651, `B-TOOL-GATE`✅#653, `B-EFFECT-FENCE`✅#655, `B-HITL-PLACEMENT-PER-STEP-PRODUCER`✅#657 [the NINTH — wrap-time HITL gates fire in production; runtime v1.61 §14.8.2 + CP v1.41 §25.3], `B-EDIT-CARRIER`✅#659 [the TENTH — functional operator EDIT; runtime v1.62 §14.8.2 step 4i / NOTE 6-ii], `B-FANOUT-PAUSE`✅#661 [the ELEVENTH — resumable cascade_policy=pause fan-out; CP v1.42 §26.2], `B-ENGINE-OUTPUT-REPLAY`✅#665 [the TWELFTH — output-carrying event-history substrate; runtime v1.63 §14.23 C-RT-32]; remaining: `B-LAYER-BUDGET-OVERRIDE` [recommended next], `B-L2-EMBEDDING-ACTIVATION`; newly-registered follow-ons (from #655): `B-EFFECT-FENCE-HITL-ROUTE` / `B-EFFECT-FENCE-PER-TOOL` / `B-EFFECT-FENCE-DURABLE-AUTO`; (from #657) `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD`; `B-TAIL-CONDITIONAL-SAMPLING` #632 R-420/R-421-gated) + the visibility-only **routing-activation gate** (UNOWNED until a second production provider is configured). The **`R-CL-Q1` quality track stays gated** until R-FS-1 fully resolves (standalone arcs land). All detailed in the spine ledger + arc-and-unit map.

---

## In-flight (open PRs)

| PR | Branch | R-NNN | Posture |
|---|---|---|---|
| *(none)* | — | — | No open PRs at refresh time. |

---

## Recently completed (last 5)

| R-NNN / PR | Closed at | Notes |
|---|---|---|
| PR #671 | 2026-06-20 | **feat(runtime): R-FS-1 `B-INTERSTEP-PERRUN-ISOLATION` — ContextVar per-run holder isolation (runtime v1.64 §14.21.5 inv 7 CLOSED).** The 15th standalone `B-*` arc (the FIRST beyond the originally-enumerated 14-arc frontier); closes the registered B-INTERSTEP follow-on §3/§5. B-INTERSTEP allocated its run-scoped `InterStepOutputChannel` (and the always-on `CostRecordAccumulator`, §82 Class-3) at BOOTSTRAP scope on the frozen `HarnessContext`; daemon-client mode (U-RT-108) reuses one ctx across concurrent `run_workflow` invocations → they SHARED the holder. Invariant 7 was bounded to (a) sequential reuse + (b) concurrent-within-timeout (a single-flight LOCK), with (c) the `drain_timeout_seconds` TIMEOUT-ZOMBIE a registered residual a released lock can't fence. **Build (fork-prescribed ContextVar):** each holder is scoped per run via a ContextVar resolved at dispatch; the frozen ctx binds a stable proxy (`RunScopedInterStepOutputChannel` / `RunScopedCostRecordAccumulator`, IS-A the field type) delegating to the current run's holder. The `run_workflow` handler sets a fresh holder per run; the set propagates into the `asyncio.to_thread` worker via `contextvars.copy_context()` (verified empirically) — (a)+(b) need NO lock, (c) closed (a zombie writes only the holder captured in ITS copy). The single-flight lock is removed. **Both holders, one mechanism:** channel (opt-in; opt-out stays None, byte-identical) + the always-on cost accumulator (the cost wrappers now thread the PROXY, not a `.records` list captured once at bootstrap — the capture that defeated isolation; `api.run`/`resume` set a fresh accumulator around `[invoke + cost-read]`, so the post-run rollup resolves the same accumulator). Other bootstrap-ctx holders assessed (resume_context_holder set-per-run; engine_output_store run_idempotency_key-keyed; ledger writers ADR-F2 single-writer + run_id-tagged) — none need isolation; not a channel-only false-complete. **NO operator gate** (additive correctness, no committed-invariant sacrifice, opt-out byte-identical; advisor + Codex, not council — no nameable cross-domain tension). **Non-vacuity is the deliverable:** `test_timeout_zombie_writes_only_its_own_per_run_channel` (REAL `asyncio.to_thread` copy_context — A's timed-out zombie writes channel A, not B) + a negative control reproducing the cross-run corruption WITHOUT isolation; concurrent + cost isolation, each with a negative control; the freeze-by-reference + per-run-resolution re-proof at `test_bootstrap.py`. **Child-workflow inheritance verified** (the `ChildWorkflowRunner` re-enters `execute_workflow` synchronously in the parent's worker context → inherits the parent's ContextVars → child cost rolls into the parent run). **Decorrelated review:** advisor (full-transcript — the ContextVar-propagation probe + the always-on cost-path + the opt-out guard + the child-inheritance blind-spot) + out-of-family Codex — which caught a real [P2] (the handler must reset its self-set var so sequential same-task reuse re-allocates) → fixed (token + finally reset, mirroring `_CURRENT_TOOL_CTX`) → re-review CLEAN. Bundled-absorption: `design-substrate/Spec_Harness_Runtime_v1.md` v1.64 + clearance marker + `class_1_fork_b_interstep_perrun_isolation.md` + `harness-runtime/src` + by-execution tests + spine-ledger BUILT (dedicated entry + B-INTERSTEP follow-on mention refreshed) + roadmap.html. Gates: pyright 0/0/0 · ruff · harness-runtime 1974 passed / 13 skipped · harness-cp 1093 passed / 1 xfailed · no §5.2-hash change · CI 14/14 blocking green · X-AL-3 green. **The B-INTERSTEP-PERRUN-ISOLATION follow-on is now BUILT; remaining forward `B-*` register continues (highest-value: `B-L2-FALLBACK-COMPOSITION`); `R-CL-Q1` admission stays an OPERATOR scoping call.** |
| PR #669 | 2026-06-19 | **feat(runtime): R-FS-1 `B-L2-EMBEDDING-ACTIVATION` — §2.2 routing-activation gate (C-CP-02 §2.2).** The FOURTEENTH + LAST originally-enumerated standalone `B-*` arc. The DECLARATIVE routing layer becomes §2.2-faithful behind opt-in `RuntimeConfig.routing_activation` (default-off, env-keyed in both loaders): a manifest-miss DECLINES → `route()` falls through to the (built) EMBEDDING classifier → L3. **impl-to-cleared-spec** — §2.2 ALREADY specs the activation condition ("the manifest binds the (agent_role, workflow_class, step) tuple") → the #653 pattern (NO design-substrate edit, NO clearance marker, build-record doc only); the decline rule maps EXACTLY onto §2.2's 3 tuple dimensions (`override_applied`→step, `per_role_bindings`→agent_role, model-bearing `per_workload_overrides`→workflow_class). Default-off ⇒ byte-identical, ZERO blast radius; the factory builds the real fastembed classifier when flag-on (fail-loud on a missing `[embedding]` extra). **Decorrelated review — decorrelation working BOTH ways (advisor 3 rounds + Codex 3 rounds):** Codex caught 3 REAL [P2] bugs — (1) an explicit per-step model override wrongly declined → EMBEDDING hijacked the operator's choice; (2) a non-model workload override counted (blocked EMBEDDING) + a model-bearing one ignored its model; (3) routing_activation defeats the C-RT-16 fallback chain (the inner re-routes per fallback attempt) — all fixed (conservative decline rule) / guarded (detect-then-refuse) + 2 follow-ons registered (`B-ROUTING-MANIFEST-MODEL-FOLD`, `B-L2-FALLBACK-COMPOSITION`). Codex's 4th finding ([P1] "the guard makes routing_activation unusable — fallback_chains mandatory") was **REJECTED on primary-source**: the default manifest has `fallback_chains=()`, no validation requires it, and a real full-bootstrap live e2e (`test_r_impl_2_ollama_router_live_e2e`) runs with empty fallback_chains (pinned by a regression assertion). **Non-vacuity:** the REAL fastembed factory composition was RUN (free-local extra) — routes 'extract the totals…' → `ollama:llama3.2:3b`, pinned as a self-skipping e2e — + a non-dormant stub witness (manifest-miss → EMBEDDING routes the non-default model) + a flag-off negative control + the 3 Codex regressions + the fallback guard + env round-trips. **Production-dormant** behind the operator-creds second-provider gate + `B-L2-FALLBACK-COMPOSITION` (the capability isn't production-reachable yet). Bundled-absorption: build-record `class_2_fork_b_l2_embedding_activation_routing_gate.md` + spine-ledger BUILT (+ 2 follow-ons) + arc-and-unit-map (14 closed · 0 originally-enumerated remaining · 1 gated) + roadmap.html; NO design-substrate edit. Gates: whole-workspace pyright 0/0/0 · ruff · harness-runtime non-e2e 1971 passed / 10 skipped · L2 e2e 3 passed (extra installed) · overlay 31/31 · CI blocking green · X-AL-3 no-op. **⚑ Originally-enumerated standalone `B-*` frontier COMPLETE (14/14); the register GREW (2 new follow-ons). NEXT = the forward follow-on `B-*` register (highest-value: `B-L2-FALLBACK-COMPOSITION`); `R-CL-Q1` admission is an OPERATOR scoping call (surface via AskUserQuestion, NOT auto-routed).** |
| PR #667 | 2026-06-19 | **feat(cp+runtime): R-FS-1 `B-LAYER-BUDGET-OVERRIDE` — L3 router timeout honors the §3.1 effective (override-resolved) budget (CP v1.42 → v1.43 §2.5.3).** The THIRTEENTH standalone `B-*` arc. The Layer-3 router timeout at `infer()` uses the C-CP-03 §3.1 OVERRIDE-RESOLVED effective `LayerBudget` — keyed on `request.workload_class` + `persona_tier` — not the flat `time_budget_ms` (the flat 200 ms is the no-override default). **Materializes the cleared §3.1 tuning surface** ("per layer × per workload class × per persona tier"), which §3.1 commits explicitly **on `llm_as_router`** and which shipped built-but-vacuous (the override carrier existed, zero production callers). **Build:** `effective_layer_budget_ms` — the `budgets`-tuple-aware resolver (`effective_budget` reads the module global → physically can't serve the L3 site, which receives the operator-bound tuple) + the L3 site honors it (orphaned flat `_layer_time_budget_ms` removed) + the DORMANT `RuntimeLLMDispatcher.budgets` construction seam (default `DEFAULT_LAYER_BUDGETS`, threaded to `infer(budgets=)`). **NO operator gate** — §3.1 affirmatively commits the override on `llm_as_router` (fulfilment, not override of a committed invariant; contrast B4-Slice-4's role relaxation, gated because §14.5.3 inv 2 *forbade* it); the prior in-code "X-AL-3 forward arc" note is overturned by re-grounding the cleared spec (advisor-confirmed; no ratified flat-only-at-L3 decision in the fork/clearance ledger). **Production-dormant** (`router=None` + DEFAULT budgets; live firing gated by the UNOWNED routing-activation gate) — capability-built, NOT "fires in production" (the non-vacuity witness lives at `infer()`). **Scope:** the L3 timeout site only (deterministic-layer wall-clock enforcement + the R-impl-2 200 ms recalibration are separate registered concerns). **Decorrelated review:** advisor (pre-build — confirmed the X-AL-3 reversal, lead-with-§3.1, frame-as-amendment + version bump, verify the no-gate discriminator via a ledger grep [none found], the non-vacuity-at-infer trap) + out-of-family Codex on the diff (one [P2] — roadmap_status not refreshed — dispositioned WORKSPACE-BLIND on the §12.2.1 two-phase refresh: the substantive PR updates the arc-map + roadmap.html, roadmap_status is the separate post-merge terminating refresh; verified against #665's identical split). **Witness:** `test_infer_l3_honors_per_persona_budget_override` (a 1 ms override governs over a HUGE 5 s flat default → the 30 ms router times out → raise; SAME router, no override → flat governs → success — both branches, self-discriminating) + `..._per_workload_...` + 4 resolver units + the dispatcher-seam threading witness + default-field control. Bundled-absorption: `design-substrate/Spec_Control_Plane_v1_43.md` (in-place §2.5.3 amendment) + clearance marker + `class_2_fork` + `harness-cp/src` + `harness-runtime/src` + by-execution tests + spine-ledger BUILT + arc-and-unit-map §5 (13 closed · 1 remaining · 1 gated) + roadmap.html. Gates: whole-workspace pyright 0/0/0 · ruff · harness-cp 1093 passed + 1 xfailed · harness-runtime 1961 passed / 10 skipped (non-e2e) · cxa 28 · core 26 · semantic overlay 31/31 · CI blocking green · X-AL-3 green. → NEXT = **`B-L2-EMBEDDING-ACTIVATION`** (the last buildable standalone arc — visibility-only, UNOWNED until a second production provider) / then the `R-CL-Q1` quality track admits once R-FS-1 fully resolves. |
| PR #665 | 2026-06-19 | **feat(runtime+cp): R-FS-1 `B-ENGINE-OUTPUT-REPLAY` — output-carrying event-history substrate (runtime v1.62 → v1.63 §14.23 C-RT-32; LINEAR EVENT_SOURCED_REPLAY).** The TWELFTH standalone `B-*` arc. Materializes the cleared C-CP-08 §8.1 `engine_replay` "activity outputs cached and replayed" clause that `EVENT_SOURCED_REPLAY` shipped DEGENERATE (E-impl-1 Finding 2: the F2 `EntryPayload` stores a `response_hash` digest not outputs; a skip-prefix resume leaves the B-INTERSTEP inter-step channel fresh-empty so a downstream consumer reads `None`). **Substrate (NEW C-RT-32 `EngineOutputStore`):** a dedicated hand-rolled durable store — **probe-resolved** over extending the IS `EntryPayload` (the IS ledger stores a digest BY DESIGN; I-6 + ADR-F2 + the `JournalWorkflowPauseStore` precedent foreclose → advisor not council) — mirroring the pause-store crash-safety (per-run JSONL keyed by `run_idempotency_key`, fsync + torn-append self-heal, under `STATE_LEDGER/engine-output`), opt-in `RuntimeConfig.engine_output_replay`. **Producer:** the CP driver records each step output BEFORE the F2 ledger-append (**RESERVE-before-COMMIT** — the store always holds ≥ the ledger's materialized prefix, the advisor-caught store↔ledger SKEW rule, verified at the live append site), gated on EVENT_SOURCED_REPLAY. **Rehydrate:** on resume the driver replays the stored prefix into the inter-step channel driven by `resume_at`, with a refined edge discriminator — file-absent → DEGRADE (config-flip, not a false-failure); file-present-but-unreadable → FAIL-CLOSED (corruption, not a false-success); partial-prefix / identity-mismatch → FAIL-CLOSED. **NO operator gate** (§1.1 reuse — the linear analogue of B-FANOUT-PAUSE's self-documented §6 re-open trigger). **Non-vacuity:** the full-chain witness — a prior run's output durably stored ON DISK → the CP rehydrate into a fresh channel → the REAL `RuntimeLLMDispatcher` injects it → the recording provider RECEIVES the recovered output (not a channel-unit proxy) + the CP rehydrate witness + a negative control + 4 fail-closed/degrade tests + 8 store crash-safety units. **Decorrelated pre-merge review (advisor + Codex) — complementary findings on the resume edge:** advisor caught the config-flip false-FAILURE; Codex caught that the fix over-degraded a read-failure into a false-SUCCESS → synthesis is a file-existence discriminator (more correct than either alone). **Scope — LINEAR EVENT_SOURCED_REPLAY only** (`B-ENGINE-OUTPUT-REPLAY-WAL-SEGMENT` + the non-linear resume-blind strategies registered forward). Bundled-absorption: `design-substrate/Spec_Harness_Runtime_v1.md` v1.63 + clearance marker + `class_2_fork` + `harness-runtime/src` store+bootstrap + `harness-cp/src` producer+rehydrate + by-execution tests + spine-ledger BUILT + arc-and-unit-map §5 (12 closed · 2 remaining · 1 gated) + roadmap.html. Gates: whole-workspace pyright 0/0/0 · ruff · harness-cp 1087 passed + 1 xfailed · harness-runtime 1965 passed / 21 skipped / 1 xfailed · cxa+core 64 · semantic overlay 31/31 · CI 14/14 blocking green · X-AL-3 green. → NEXT = **`B-LAYER-BUDGET-OVERRIDE`** (recommended) / the other remaining standalone `B-*` arcs. |
| PR #659 | 2026-06-19 | **feat(runtime): R-FS-1 `B-EDIT-CARRIER` — functional operator EDIT (runtime v1.61 → v1.62 §14.8.2 step 4i / NOTE 6-ii).** The TENTH standalone `B-*` arc. Lands **functional operator EDIT** at the wrap-time HITL gate, RESOLVING the U-RT-120 / B3-impl-3 carrier-drift fork (D-edit.B interim) — `EDIT` shipped non-functional (raised `HITLGateEditCarrierDriftError`) until this arc; #657 made the EDIT gate reachable. **Mutation discipline (A) JSON-decode-then-replace** (the cleared constraints — flat-`str` MCP-elicitation carrier + the committed replace-not-merge mandate + the arbitrary-nested `step_payload` target — foreclose to (A); (B) typed-flat can't carry arbitrary nesting, (C) field-merge would override replace-not-merge; (A) PRESERVES the committed decision → **NO operator gate**, probe-resolved, FULL-SPEC pre-authorized; mirrors `_parse_edited_arguments`). **Build (harness-runtime composer):** decode at 4h-pre (the post-mutation payload feeds BOTH the 4g `summary_hash` + the 4h `edited_proposal_hash` per NOTE 6-ii); a decode failure is deferred past the 4h audit (records the attempt, symmetric to REJECT) then raised at 4i → new `RT-FAIL-HITL-GATE-EDIT-DECODE`; 4i `step.model_copy(update={"step_payload": decoded})`; new `HITLGateEditDecodeError` retires the interim raise; **strict RFC-8259** (NaN/Infinity rejected — Codex [P3]); `edited_proposal_hash` now POST-mutation (closes reviewer F3-03). **Non-vacuity = the deliverable (advisor's #1 steer):** the **real-consumer witness** `test_edit_decoded_payload_reaches_real_llm_dispatcher` — composer's inner = the REAL `RuntimeLLMDispatcher` over a recording provider → the provider RECEIVES the decoded messages (not the original), the `str`→`Mapping`→`step_payload`→`_coerce_payload`→provider chain end-to-end (closing the half-proof gap advisor caught — the composer-unit witness mocked the consumer); + the composer-unit full-chain witness (real `WorkflowStep` + `step_context` producer surface, NO proxy) + the interim-raise test FLIPPED into the invalid-JSON negative control + parametrized non-object/None/non-finite decode rejections + a post-mutation-hash assertion. **Scope — wrap-time SYNC path only;** two follow-ons registered: `B-EDIT-CARRIER-DURABLE-ASYNC-RESUME` (the durable-async resume path dispatches the resumed step UNCHANGED for ALL response types per v2.24 AC #7 / FM-2 — a PRE-EXISTING silent-drop, not introduced here) + `B-HITL-WRAP-FAIL-CLASS-SURFACING` (Codex [P2], advisor-pre-cleared: the registered RT-FAIL code surfaces via the cp driver's generic `step-failure: {type}` path — cp can't import runtime exception types — the SAME generic surfacing as the pre-existing wrap-time REJECT). Bundled-absorption: `design-substrate/Spec_Harness_Runtime_v1.md` **v1.62** + clearance marker + fork doc RESOLVED + `harness-runtime/src` + by-execution tests + spine-ledger BUILT (+ 2 follow-ons) + arc-and-unit-map §5 (10 closed · 4 remaining · 1 gated) + roadmap.html. **Decorrelated review:** advisor (2 rounds — discipline foreclosure + the real-consumer-witness half-proof catch + durable-async characterization) + out-of-family Codex ([P3] fixed; [P2] accepted-by-precedent + registered). Gates: pyright 0/0/0 · ruff · harness-runtime 1949 / 10 skipped (non-e2e) · cp+cxa+core 1122 · od+is+as 1438 · overlay 31/31 · CI 14/14 blocking green · X-AL-3 green. → NEXT = **`B-FANOUT-PAUSE`** / the other remaining standalone `B-*` arcs. |
| PR #657 | 2026-06-18 | **feat(runtime): R-FS-1 `B-HITL-PLACEMENT-PER-STEP-PRODUCER` — wrap-time HITL gates fire in production (runtime v1.60 → v1.61 §14.8.2 step-1 / CP v1.40 → v1.41 §25.3).** The NINTH standalone `B-*` arc. Makes the wrap-time HITL gates (inference / sub-agent / tool — incl. #653 `B-TOOL-GATE`) **fire in production** by surfacing the workflow's declared `WorkflowManifestEntry.hitl_placements` onto the per-step `StepExecutionContext.hitl_placements`. Closes the B3 placement-composition residual the **#653 Codex [P1]** surfaced: the composer read `step.hitl_placements`, but the frozen 3-field `WorkflowStep` (workflow *body*, not config per C-CP-25 §25.2) could never carry it → no wrap-time gate fired for ANY step kind (every gate e2e used a `_StepWithPlacements` proxy — `[[test-bypass-as-runtime-truth-pattern]]`). **Build:** producer = the CP driver (`hitl_placements=manifest_entry.hitl_placements` at all 5 per-step `StepExecutionContext` constructions; branch children inherit via `compose_branch_child_context`'s `model_copy` → all 6 topologies); composer reads `step_context.hitl_placements` (runtime §14.8.2 step-1 reconciled `step`→`step_context`; `getattr(step,...)` preserved as a test-proxy compat fallback). **Read surface = `StepExecutionContext`** (NOT `StepEffectiveBinding` — whose `model_dump` feeds the §16.5.4 override outcome-hash; a hash-coherence regression avoided — the `tenant_id`/`parent_gate_level` binding-fix precedent); import cycle broken via `TYPE_CHECKING` in `hitl_placement.py`. **Spec'd-ness (advisor-decomposed, 2 rounds):** FOLD semantics + PRE_ACTION→{inference,tool} coupling SPEC'd at runtime §14.8.1/§14.8.4 → impl-to-cleared-spec; read-surface = faithful materialization → bundled-absorption (runtime v1.61 + CP v1.41), **NO operator gate** (additive + opt-in, default `()` byte-identical, no new contract/fail-class/manifest field). **Non-vacuity is the deliverable (advisor's #1 steer; cf. #653 wired-but-production-dead):** the **full-chain witness** (advisor pre-merge catch — the producer + composer half-proofs never met in one run) `test_e2e_manifest_placement_fires_gate_via_producer`: a manifest-declared PRE_ACTION placement + a PLAIN `WorkflowStep` → `run_workflow`→real `execute_workflow`→real composer → gate fires (`ctx.elicit` once, `hitl.gate.evaluated` span, SUCCESS); the producer proven via real `execute_workflow` on linear + DECENTRALIZED_HANDOFF; a NEGATIVE CONTROL (empty → byte-identical); + an **excluded-cell sanity** test (a declared placement on an EXCLUDED `(TEAM_BINDING, PURE_PATTERN_NO_ENGINE)` cell raises the typed `HITLCellExcludedError`, C-CP-18 §18.1, not a crash). **Scope — wrap-time gates only:** VALIDATOR_ESCALATION (§14.15 validator-outcome) out of scope; the per-step `StepOverride.hitl_placement` override fold (silent at C-CP-06 §6.2) registered as follow-on `B-HITL-PLACEMENT-PER-STEP-OVERRIDE-FOLD`. Bundled-absorption: `design-substrate/Spec_Harness_Runtime_v1.md` v1.61 (§14.8.2 step-1) + NEW `Spec_Control_Plane_v1_41.md` (§25.3 field) + 2 clearance markers + fork doc + `harness-cp/src` + `harness-runtime/src` + by-execution tests + spine-ledger BUILT + arc-and-unit-map §5 (9 closed · 5 remaining · 1 gated) + roadmap.html. **Decorrelated review:** advisor (2 rounds — fork-vs-impl decomposition + the full-chain witness + excluded-cell catches) + out-of-family Codex (clean — "I did not find a discrete regression"). Gates: pyright 0/0/0 · ruff · harness-cp 1067 + 1 xfailed · harness-runtime 1937 / 10 skipped (non-e2e) · CXA-P1 34 · cxa 28 / od 950 / core 26 · CI 13/13 blocking green · X-AL-3 green. → NEXT = **`B-EDIT-CARRIER`** (recommended; HIGH — EDIT ships non-functional/raises, now gate-reachable post-#657) / the other remaining standalone `B-*` arcs. |
---

---

## Outstanding fork docs (47 total — latest additions filed by PR #434)

Sample (highest-leverage open):

| Fork doc | Class | Status |
|---|---|---|
| `class_1_fork_sandbox_tier_no_execution_driver_contract.md` | Class 1 | ✅ APPLIED-AS-BOUNDED-READING-B (status-line refreshed 2026-06-08) — R-410 supplied `ToolExecutionDriver` + local Docker provider; R-411 added gVisor/runsc; R-412 adds managed E2B full-VM. |
| `class_1_fork_tool_step_no_operator_supplied_converter.md` | Class 1 | ✅ APPLIED (PR #171, spec v1.40) — converter config surface landed (Reading B) |
| `class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` | Class 1 | ✅ APPLIED-AS-READING-B (PR #172, spec v1.41 §14.9.8) — resolver + 5 bootstrap gaps wired; AC #2 final close = operator live e2e |
| `class_1_fork_llm_cost_attribution_not_firing_on_real_dispatch.md` | Class 1 | ❌ RESOLVED-AS-INVALID (PR #168) — test-observation bug, not a defect; cost-attribution fires + writes; OD-5 retirement VALID |
| `class_1_fork_harness_toml_default_discovery_unimplemented.md` | Class 1 | ✅ APPLIED-AS-READING-A (PR #305 status refresh after PR #279 implementation) — CWD `harness.toml` discovery shipped; no longer open. |
| `class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` | Class 1 | ✅ APPLIED-AS-READING-C (PR #107, spec v1.30) |
| `class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.39, pyyaml StrictSafeLoader) — R-100-mvp-yaml-loader-shipped verified done at non-live level (41 loader/equivalence tests pass) |
| `class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.38, defer-to-runtime) |
| `class_2_fork_audit_stub_timestamp_universal_fix_plus_per_tier_annotation.md` | Class 2 | ✅ APPLIED-AS-(D) |
| *(complete enumeration at `ls .harness/class_*_fork_*.md`)* | — | — |

**Audit status:** 2026-06-07 cadence pass closed in this PR: sandbox execution-driver fork refreshed from historical PROPOSING; Path (i) durable-async fork leading status token normalized to `**Status:**`; stale roadmap sample row for the already-applied `harness.toml` fork refreshed; no other stale fork-doc headline status lines found. Next cadence: ~5 PRs or operator-discretion.

---

## Phase 7 retirement progress

> **🎓 PHASE-8 GRADUATION + BATCH-56 LIVE LEDGER.** The R-700 reconciliation is CLOSED — the operator ratified **accounting (i)** (PR #246) and lifted the declaration hold. Historical declaration: **RETIRED 46/54 (85.2%) + pipeline-advanced 49/54 (90.7%)**. Batch-52 back-flowed the live R-810/R-820 evidence for AS-8e, AS-8f, and CP-17; batch-53 back-flows the OD-4 runtime residual and the 0-wireable CXA-4 bookkeeping row; batch-54 lands the CP→AS runtime composer; batch-55 records CXA-2 as a counted bounded residual; batch-56 records CXA-1 as substantively retired after the AS→IS producer and edge-scope audit: **live ledger RETIRED 54/54 (100.0%) + pipeline-advanced 54/54 (100.0%)**. Prior batch records stand verbatim; this section reports the current live 54-row set derived from `.harness/substitutions.yaml`.

The bucket rows below sum to **54** under the batch-56 live ledger (RETIRED 54).

| Bucket | Count | Notes |
|---|---|---|
| RETIRED | **54/54 (100.0%)** live (batch-56) | 43 substantive + 8 authoring-only + 3 bounded-residual (CP-16, OD-6, CXA-2). IS 9/9; AS 11/11; CP 21/21; OD 8/8; CXA 5/5. Phase-8 historical declaration remains 46/54; batch-52 added AS-8e, AS-8f, and CP-17 after R-810/R-820 live proofs; batch-53 adds OD-4 and CXA-4 after accounting/back-flow; batch-54 adds CXA-3 after the CP→AS runtime composer landed; batch-55 adds CXA-2 as bounded residual after producer-loop evidence landed; batch-56 adds CXA-1 after the AS→IS producer and edge-scope audit landed. |
| RETIRE-READY | **0 active (bucket EMPTY post-batch-51)** | OD-3 + OD-6 transited RETIRE-READY → RETIRED at batch-51 (PR #200). |
| PARTIAL | **0** | Empty after batch-56. CXA-1 moved to SUBSTANTIVE_RETIRED after the AS→IS edge-scope audit and resolver-bound secret-fetch write landed. |
| STILL-BOUNDED | **0** | Empty after batch-55 and remains empty after batch-56. |
| RETIRED-AS-AUTHORING-ONLY | **8** (of the 54 RETIRED) | accounting (i): IS-4, IS-10, AS-9, CP-12, CP-23, OD-1, OD-7, OD-8 (accounting (ii) = 9, adds CP-24). The H_T contract is the typed declaration itself; no runtime behavior (sub-species 10 categorical-mismatch). |
| RETIRED-AS-BOUNDED-RESIDUAL | **3** (of the 54 RETIRED — *counted*) | CP-16 (memory, batch-44) + OD-6 (OTLP, batch-51 — FIRST in ledger) + CXA-2 (CP→IS durable recovery, batch-55). Production substrate dormant or post-MVP at MVP; substantive close deferred to a real deployment/recovery surface (X-AL-2 §5.3). |

**Decomposition status (R-002 RESOLVED 2026-05-31) + R-700 draft correction (2026-06-01):** R-002 mapped all non-RETIRED rows *in the per-axis `§4.1` view* to R-NNN entries — IS-2 (R-003 + R-001-h-t-is-2-retired), OD-3 (R-007), OD-4 (R-008), OD-6 (R-009), AS-8e (R-005), AS-8f (R-006); R-001 (OD-5) + R-004 (AS-8d) reconciled stale BLOCKED → RESOLVED. **The R-700 draft surfaced that the R-002 survey covered only `§4.1`, which excludes the CXA axis (no `§4.1` file) + CP-17's SB-INDEF reclassification — so 5 non-RETIRED rows (`CXA-1`/`CXA-2`/`CXA-3`/`CXA-4` + `CP-17`) have NO `R-NNN` entries.** Recommended follow-on (draft §C item 2): an `R-002`-style Surface-I decomposition pass over CXA + CP-17. **→ CLOSED at PR #246 (R-700 PART C item 2):** grounding found CXA-1/2/3/4 already had `R-CXA-1..4` (authored #209); NEW `R-010-cp-17-files-indefinite` authored for CP-17 → all 5 flagged rows now have dedicated entries. Ratified at the Phase-8 graduation (2026-06-02).

---

## Drift detection log


_Showing the 10 most recent drift/reconciliation events. The full audit history (155 events) is archived at `.harness/roadmap_drift_log_archive.md`._

| Date | Source | Resolution |
|---|---|---|
| 2026-06-18 | **Post-#655 terminating refresh — PR #655 R-FS-1 `B-EFFECT-FENCE` (at-most-once effect execution; runtime v1.59 → v1.60 §14.22 C-RT-31) merged at `868f9e0c`; §12.2 owed follow-on.** | Hash `559518834b10` → `10fef4ccdb90` (state at `868f9e0c`, open PRs empty, fork count 74→75 [+1 B-EFFECT-FENCE fork doc], batch-57). **The EIGHTH standalone `B-*` arc.** Closes floor-(ii) at-most-once EXECUTION of non-idempotent step side-effects: a crash in the `workflow_driver.py:2031`→`:2336` window (effect fired → per-step ledger COMMIT) re-fires the effect on resume. A hand-rolled (I-6) durable **effect fence** at the `RuntimeToolDispatcher` `call_tool` sink mirrors the U-RT-123 `_claim_resume_revision` crash-atomic `O_EXCL`/`os.link`/`fsync` claim, re-keyed to the per-`(run, step, tool)` `idempotency_key`: WON → fire; LOST → `EffectFenceReservedUncommittedError` → FAILED `RunResult` (honest fail-close, no silent double-fire). COMMIT = the existing per-step ledger entry (the fence adds ONLY the RESERVE). Opt-in `RuntimeConfig.effect_fencing` (default `None` → byte-identical). **At-most-once *per (run, step) invocation*, NOT exactly-once** — a legitimate loop iteration is NOT conflated with a resume re-fire (advisor catch, verified at HEAD `workflow_driver.py:3620-3628`: EO passes the monotonic `entry_index` as `parent_idempotency_key`, fan-out the branch-scoped key — MUST, else IS dedup at C-IS-07 §7.5 drops iteration-2). Fence-scope fork (C10⊥C11) probe-resolved by durable-resume scoping (Reading C) → advisor, not council. **Codex [P2] decorrelated catch (advisor + author missed):** `HARNESS_EFFECT_FENCING=true` was silently ignored → wired into both env loaders (`_ENV_SCALAR_FIELDS` + `_RuntimeEnvSettings`) + regression tests, justified because it gates a CORRECTNESS property (no-silent-failure). **Advisor Check B** → added a by-execution test that the breaker fail-fasts the fence error (1 inner call). **NO operator gate** — additive + opt-in, no committed-invariant sacrifice; §22.1 HITL pause + per-tool classification + durable-auto are spine-registered follow-ons (`B-EFFECT-FENCE-HITL-ROUTE` / `-PER-TOOL` / `-DURABLE-AUTO`; cross-host folds into deferred F-CC). Bundled-absorption: runtime v1.59 → v1.60 §14.22 C-RT-31 + clearance + fork + `harness-runtime/src` + `harness-cp` test + by-execution tests (mechanics + real-dispatcher at-most-once + NEGATIVE CONTROL + fresh-instance restart + driver re-dispatch key-stability) + spine-ledger BUILT + arc-and-unit-map §5 (8 closed · 6 remaining) + roadmap.html. CI 14/14 blocking green; pyright 0/0/0; harness-runtime 1726 (non-e2e); harness-cp 1063 + 1 xfailed; `test_effect_fence.py` 6/6; X-AL-3 green. Next selector = **`B-HITL-PLACEMENT-PER-STEP-PRODUCER`** (recommended; HIGH cross-cutting — binds per-step `hitl_placements` so ALL wrap-time HITL gates incl. #653 fire in production) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#653 terminating refresh — PR #653 R-FS-1 `B-TOOL-GATE` (tool-step MCP-trust HITL gate site; CP v1.35 §19.1.2 Producer ¶ / runtime §14.8.2 step-4c) merged at `51f8c169`; §12.2 owed follow-on.** | Hash `9c6d852700d3` → `559518834b10` (state at `51f8c169`, open PRs empty, fork count 73→74 [+1 B-TOOL-GATE fork doc], batch-57). **The SEVENTH standalone `B-*` arc.** A third `RuntimeHITLGateComposer` at the tool-step registry path feeds the resolved-owning-MCP-host trust tier into the §19.1.2 MCP-trust gate axis (new `make_step_mcp_trust_tier_resolver`, mirroring `make_step_blast_radius_resolver`; `ctx.tool_dispatcher` NOT mutated so the producer-loop/`hitl_tool_loop` read the un-gated dispatcher). **Impl-against-cleared-spec** (`MCPTrustTier` / `GateLevelInput.mcp_trust_tier` / `MCP_TRUST_GATE_LEVEL_FLOOR` all pre-exist; no new contract/field/invariant) → NO operator gate. **Codex [P1] decorrelated catch (advisor + author missed):** the wrap-time gate is placement-driven, but `hitl_placements` is WORKFLOW-level (`WorkflowManifestEntry`, C-CP-17 §17.3) and the frozen+`extra=forbid` per-step `WorkflowStep` has no src binding workflow→per-step placements → `step.hitl_placements` is always `()` through the real loader→driver path → no wrap-time HITL gate fires in production for ANY step kind — a PRE-EXISTING gap shared by ALL wrap-time gate sites (even inference/sub-agent e2e tests use a `_StepWithPlacements` proxy), the unbuilt half of the B3 spine residual → registered `B-HITL-PLACEMENT-PER-STEP-PRODUCER`. Three-claim decomposition (never blurred): (1) wiring correct (real `run_bootstrap`); (2) axis composes when the gate fires (by-execution L0→DENY→{REJECT,RESPOND}→reject-raises; L3→AUTO→delegate); (3) production gate-*firing* awaits the producer arc. First-draft "non-vacuous in production" overclaim corrected to "non-vacuous at the tool gate, at parity." Bundled-absorption: `harness-runtime/src` + by-execution tests ONLY — NO design-substrate edit / spec bump / clearance marker; fork/build-record + spine-ledger BUILT + arc-and-unit-map §5 (7 closed · 7 remaining) + roadmap.html. CI 13/13 blocking green; pyright 0/0/0; harness-runtime 1922 / 10 skipped; CXA-P1 34; dashboard 15; X-AL-3 green. Next selector = **`B-EFFECT-FENCE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#651 terminating refresh — PR #651 R-FS-1 `B-INTERSTEP` (inter-step data-flow channel; runtime v1.59 §14.21 C-RT-29) merged at `a90fe979`; §12.2 owed follow-on.** | Hash `c28fb918789b` → `9c6d852700d3` (state at `a90fe979`, open PRs empty, fork count 72→73 [+1 B-INTERSTEP fork doc], batch-57). **The SIXTH standalone `B-*` arc.** A run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`; default byte-identical) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (`EVALUATOR_OPTIMIZER` the load-bearing case). Dispatch signature UNCHANGED + §25.3.3.4 preserved → NO operator gate. **Daemon-scope hardening took 4 Codex rounds (5 real bugs) + an r5 systemic residual:** invariant 7 honestly bounded to (a) sequential reuse closed by per-run `reset()`; (b) concurrent-within-drain-timeout closed by a per-loop single-flight lock (opt-out default never locks); (c) the concurrent-with-timeout `to_thread`-zombie = a SYSTEMIC reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT lock-closeable → registered `B-INTERSTEP-PERRUN-ISOLATION` (advisor reconcile: FULL-SPEC = registration not build-here; real-but-accepted, NOT looped-to-green). The advisor caught the `B-INTERSTEP-PERRUN-ISOLATION` forward-cite phantom → registered in the spine ledger before merge (`[[spine-ledger-forward-arc-registration]]`). One CI ruff-format RED (a prior commit's consumer-test file left unformatted; repo-wide gate caught what file-scoped local checks missed) fixed pre-merge. Bundled-absorption (runtime v1.59 §14.21 + clearance + fork + impl + by-execution tests incl. negative control + spine-ledger BUILT with 3 follow-ons + arc-and-unit-map 6-closed/7-remaining + roadmap.html). CI all blocking green; pyright 0/0/0; harness-runtime 1915 / harness-cp 1062; substitution 54/54. Next selector = **`B-TOOL-GATE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#648 terminating refresh — PR #648 R-FS-1 `B-NONLINEAR-OVERRIDE-PROVENANCE` (per-step override-ledger entry on all topologies; CP v1.40 §6.6) merged at `f42ed121`; §12.2 owed follow-on.** | Hash `3a819123455c` → `0488904ae18f` (state at `ec64b116`, open PRs empty, fork count 70→71 [+1 B-NONLINEAR fork doc], batch-57). #648 closed the FOURTH standalone arc but missed bumping `tools/test_dashboard_generate.py` (it parses the real arc-map §5 yet sits outside CI's axis-package pytest scope, so the 4-way-count RED went uncaught — same drift class #646 corrected). Per the **#461 precedent** (a refresh carrying a dashboard-test update can't be the clean dashboard-only pair), the test correction lands as its own commit `ec64b116` (3→4 closed / 10→9 remaining) and THIS terminating 2-file refresh pins it, restoring the §12.2.1 fixed-point shape. **The FOURTH standalone `B-*` arc.** The `cp.per-step-override-application` state-ledger entry (U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` site only; now emits on all 6 topologies via the **buffered-branch path** (`append_branch_override_ledger_entry` → each branch's `BufferingLedgerWriter`, drained on the driver thread in branch-index order, realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across topologies (branch-scoping would be an X-AL-3 spec change). Impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh) + stale-carry refresh of the falsified v1.38 note; **NO operator gate** (no committed-invariant sacrifice; additive). advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer STEP_BOUNDARY mis-count; lost-override-on-failed-dispatch; missing CASCADE_CANCEL cancelled-terminal). Bundled-absorption (CP v1.40 + clearance + fork + impl + 13 by-execution per-topology tests + spine-ledger BUILT + arc-and-unit-map 4-closed/9-remaining). CI 14/14; pyright 0/0/0; overlay 323/31-31; substitution 54/54. Next selector = **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (recommended autonomous `B-*`) / `B-TAIL-CONDITIONAL-SAMPLING` (R-420/R-421-gated). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#635 terminating refresh — PR #635 R-FS-1 arc M (Managed Agents executable-consumer contract + production wiring; C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`) merged at `ff9f9737`; §12.2 owed follow-on.** | Hash `a1a0701a1664` → `6ecec018b867` (state at `ff9f9737`, open PRs empty, fork count 67→68 [+1 arc-M fork], batch-57). **M COMPLETE — the FINAL FROZEN child arc's autonomous half (10 of 11 done; only B6 Slice 2 remains).** A NEW `StepKind.MANAGED_AGENTS` (operator-ratified Option B; closed-at-5 Class-2 extension) dispatched by a `ManagedAgentsStepDispatcher` (vendor-runs-the-loop; `SyncDispatcherFacade`-bound; surface-gated to `MANAGED_CLOUD`+opt-in) makes the R-820-live-proven `managed_agents.py` carrier production-reachable. Option A (riding `SUB_AGENT_DISPATCH`) probe-foreclosed (harness-orchestration semantics a vendor session can't honor). Decorrelated-review hardening in-arc: decoupled facade timeout (`step_timeout_seconds` 600s ≠ shared 30s — orphaned-billable-session fix) + cancel-on-give-up + `freeze()` carrier passthrough (Codex). Vendor/credential boundary: live managed-cloud run is the surfaced vendor-gate, never auto-fired; H_T-AS-8f already SUBSTANTIVE_RETIRED (R-820). Bundled-absorption (CP v1.39 + runtime v1.55 + impl + tests + fork + 2 clearance markers + arc-and-unit-map M✅ in the substantive PR per the count-change discipline). advisor (caught the gate-dissolution attempt + facade-timeout/test-gap) + Codex (freeze-drop) clean; CI 4365 passed / 14 skipped / 19 deselected (e2e) / 1 xfailed; pyright 0/0/0; overlay 322 nodes 31/31. Next selector = **B6 Slice 2 (operator-gated; drive to gate) / autonomous standalone `B-*` arcs**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#630 terminating refresh — PR #630 R-FS-1 arc B6 Slice 1 (per-MCP-transport sandbox-tier floor composition; STDIO→TIER_3; runtime spec v1.54 §14.9.8) merged at `1a7dc6e2`; §12.2 owed follow-on.** | Hash `46e6f7ed8878` → `8cb531254be3` (state at `1a7dc6e2`, open PRs empty, fork count 66→67 [+1 B6 fork], batch-57). **B6 Slice 1 COMPLETE** — the stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor` (U-AS-13) via tier-`max()` with the §14.9.8 surface default (new `compose_transport_floor`, SSOT) → STDIO ≥ TIER_3, L2-remote → TIER_4; closes the carved-out ADR-D2 §1.3 STDIO tier-3 transport-floor gap. **C10⊥C11 probe-resolved at ADR-D2 §1.3 in C10's favor** (STDIO Tier-3 regardless of blast-radius, hard floor even at local-dev) → impl-to-cleared-ADR, NO operator gate, sacrifices no invariant. Per-server-uniform PRESERVED; per-tool granularity = Slice 2. **Real out-of-box change (named):** local-dev STDIO needs a driver or bootstrap fail-closes. **Coverage loss surfaced:** in-process-STDIO e2e (`test_r100_ac2`+`test_r_cl_p3`×3) SKIPPED (pre-B6 gap ADR-D2 §1.3 forbids; R-410 covers container completion); U-RT-86 retirement evidence verified ran-and-passed unaffected. advisor (caught the decomposition refinement + a strict-raise reason-attribution bug) + Codex clean; 1821 passed / 24 skipped / 1 xfailed; pyright 0/0/0; overlay 31/31. Bundled-absorption (runtime v1.53→v1.54 + fork + clearance + spine Slice-1/2 split). Forward arcs: `B-PER-TOOL-SANDBOX-TIER` (Slice 2, operator-gated) + `B-MCP-HOST-REMOTE-TRANSPORT`. Next selector = **B6 Slice 2 (operator-gated) / B7 (unblocked autonomous)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#628 terminating refresh — PR #628 R-FS-1 arc B5 (per-deployment-surface memory backend selection; `resolve_backend` non-vacuous; runtime spec §14.12.1) merged at `ae62fdb9`; §12.2 owed follow-on.** | Hash `cf0db8f3ab05` → `46e6f7ed8878` (state at `ae62fdb9`, open PRs empty, fork count 66, batch-57). **B5 COMPLETE** — `MemoryToolRegistry.resolve_backend(deployment_surface)` made surface-discriminating (bootstrap-frozen surface→backend map via `from_surface_map`; default path resolves config-free FILESYSTEM per surface + freezes a fail-closed deferred RT-FAIL for config-less surfaces like `MANAGED_CLOUD`; ACTIVE surface must resolve or bootstrap aborts; active-surface dispatch byte-identical; anti-vacuity = constructs-vs-raises). Impl-to-cleared-spec (impl-discretion per plan v2.14 U-RT-80 AC + §14.12.7 — no fork / no design-substrate / no new contract / no CXA seam; overlay 31/31). #628 (substantive) bundled the arc-and-unit-map (B5→done 8/11, B6→next) + spine forward-arc registration (`B-MEMORY-SURFACE-BACKEND-IMPLS`: the spec'd-but-unbuilt `ENCRYPTED_FILESYSTEM`+`OPERATOR_DEFINED` backends) + regen roadmap.html in the SUBSTANTIVE PR (the #623 build-progress-source lesson → B5 = 2 PRs, not 3). advisor (design + pre-done) + Codex clean; harness-runtime 1811 passed / 10 skipped; pyright 0/0/0. Next selector = **B6 (per-tool sandbox-tier + STDIO transport-floor; fork-owed)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#626 terminating refresh — PR #626 (substantive arc-map + status refresh post-#625) merged at `35886ef9`; terminating 2-file refresh owed.** | Hash `d883470ddab1` → `cf0db8f3ab05` (state at `35886ef9`, open PRs empty, fork count 66, batch-57). #626 bundled the arc-and-unit-map (dashboard build-progress source, #623) with roadmap_status.md → a substantive dashboard refresh (outside the `hook_is_dashboard_only_set` 2-file set), so it owed this terminating 2-file refresh to reach the §12.2.1 fixed point (same shape as #623→#624). Next selector unchanged = **B5**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#625 terminating refresh — PR #625 R-FS-1 arc CA (`RunResult.cost_attribution` rollup; runtime spec v1.53 §9 C-RT-09) merged at `7861171d`; §12.2 owed follow-on.** | Hash `3c31942c5c6b` → `d883470ddab1` (state at `7861171d`, open PRs empty, fork count 65→66 [+1 CA fork], batch-57). **CA COMPLETE** — run-result cost rollup wired (`CostRecordAccumulator` holder + 4 per-dispatch wrappers + `_build_run_result` PER_PROVIDER_AND_MODEL rollup). Axis empirically forced (advisor + HEAD body-grounding overturned the sweep's PER_PROVIDER_DISCRIMINATOR lead — production dispatch-type tags ∉ `CrossFamilyTag`). Pydantic-freeze-copies-typed-list bug caught by advisor pre-merge + fixed with the by-reference holder + a freeze()-traversing regression test. Forward items registered: `B-COST-DISCRIMINATOR-TAXONOMY` (the per-family rollup) + (prior) `B-NONLINEAR-OVERRIDE-PROVENANCE`. arc-and-unit-map refreshed (CA→done 7/11, B5→next) per the #623 build-progress-source lesson. Next selector = **B5 (memory backend per deployment surface)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#623 terminating refresh — PR #623 fix(dashboard): reflect B4 completion in the arc-and-unit-map (build-progress source) + deploy trigger; merged at `d94d78d2`; §12.2 owed follow-on.** | Hash `e89a78305084` → `3c31942c5c6b` (state at `d94d78d2`, open PRs empty, fork count 65, batch-57). Operator-flagged: the published roadmap showed CONFLICTING B4 signals (roadmap_status.md sections said B4✅/CA-next; build-progress masthead/arc-strip/cards parsed from the stale `r-fs-1-arc-and-unit-map.md` said 5/11/B4-next). Root cause: the arc-and-unit-map was never refreshed through any B4 slice AND was missing from the deploy path filter. Fixed: arc-map B4→done (6/11) + as-built units + CA→next; `generate.py` 0-new-unit render; deploy path filter += the arc-map. Verified by in-browser render (6/11, B4✓, CA▶next). Next selector unchanged = **CA**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#621 terminating refresh — PR #621 R-FS-1 B4 Slice 4 (per-step ROLE override + linear-path indexing; CP v1.38 + runtime v1.52 §14.5.3 invariant-2/3 relaxation) merged at `abb67a99`; §12.2 owed follow-on.** | Hash `5c8577ce9942` → `e89a78305084` (state at `abb67a99`, open PRs empty, fork count 64→65 [+1 B4-Slice-4 fork doc], batch-57). **B4 COMPLETE** — Slice 4 closes the tail (operator-ratified §14.5.3 relaxation; Option-B composition-time fold, runtime dispatch byte-unchanged; full-stack consumer-e2e green). Next selector = **CA (cost aggregate)** — `RunResult.cost_attribution` rollup (per-dispatch built; run-result rollup + accumulator owed; small pre-authorized C-RT-09 fork). Forward item registered: `B-NONLINEAR-OVERRIDE-PROVENANCE` (Codex-surfaced; pre-existing linear-only override-ledger emission, all dimensions). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#619 terminating refresh — PR #619 R-FS-1 B4 Slice 3 (per-step PROMPT override, CP v1.37) merged at `70ca4628`; §12.2 owed follow-on.** | Hash `4d03a1931cbd` → `5c8577ce9942` (state at `70ca4628`, open PRs empty, fork count 63→64 [+1 B4-Slice-3 fork doc], batch-57). B4 Slice 3 closed; next selector = **B4 Slice 4** (per-step ROLE gate + linear-path role indexing). Session-start hook had flagged expected mid-arc feature-branch drift (computed `f42f967c144c` ≠ dashboard `4d03a1931cbd`) — resolved by this post-merge refresh, not a workspace divergence. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-10 | **Claude release-candidate handoff authored locally at `a5215dde`; post-handoff refresh.** | Hash `e13d42d7874c` → `dbfe6623fc76` (state at `a5215dde`, open PRs unavailable/empty, fork count 47, batch-56). Added `.harness/release-candidate-deployment-readiness-runbook.md` and Claude/context pointers. Next selector is the HIL release-candidate deployment-readiness scope gate, then provider-free readiness before any live deployment smoke. |
| 2026-06-10 | **Overlay-check ignored-artifact fix merged locally at `d4b6b4e3`; post-merge refresh.** | Hash `c28613a1f0a3` → `e13d42d7874c` (state at `d4b6b4e3`, open PRs unavailable/empty, fork count 47, batch-56). `overlay-check` now ignores stale gitignored `tools/semantic_overlay/overlay.json` artifacts while preserving the hard gate for stale tracked snapshots. Next selector moves to an HIL release-candidate deployment-readiness scope discussion before opening new implementation. |
| 2026-06-10 | **Post-#447 terminating refresh — PR #447 BMad runtime config restore merged at `9cf968d2`; §12.2 owed follow-on.** | Hash `48f9537038cf` → `f9c6f1ebe73d` (state at `9cf968d2`, open PRs empty, fork count 47, batch-56). `_bmad/bmm/config.yaml` is restored on main; no roadmap item status changes beyond clearing the last in-flight PR. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **PR #440 supersession / post-#464 open-PR refresh branch opened from `d24dcf57`.** | Hash `78617c29740a` → `48f9537038cf` (state at `d24dcf57`, open PRs #447 only, fork count 47, batch-56). #440 was closed as superseded: it was conflicting, predated the later R-CXA-2 implementation/back-flow sequence, and a current-main comparison showed it would remove later R-CXA-2 implementation, tests, batch records, and dashboard/overlay updates if merged. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Post-#463 terminating refresh — PR #463 R-IF-112 semantic overlay closeout merged at `925f021a`; §12.2 owed follow-on.** | Hash `ebb6d51193e6` → `78617c29740a` (state at `925f021a`, open PRs #440/#447, fork count 47, batch-56). R-IF-112 is now RESOLVED: `tools/semantic_overlay` derives the design-substrate contract keyspace, source cite layer, CXA seam endpoint graph, and substitution join; CI gates hard CXA/stale-artifact drift and reports advisory code/contract/substitution orphan classes. Next selector moves to this terminating refresh, then recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Substantive R-IF-112 implementation closeout branch opened from `62f9a327`; §12.2 owed after merge.** | Hash `a0ddc2154026` → `ebb6d51193e6` (branch base `62f9a327`, open PRs #440/#447, fork count 47, batch-56). The existing semantic overlay linter is completed against the roadmap must-pass by adding design-substrate `C-*` keyspace scanning and advisory `contract_without_code` orphan reporting, alongside the existing code-without-cite, CXA missing-endpoint, and substitution-without-carrier classes. `R-IF-112` is moved to RESOLVED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |
| 2026-06-09 | **Post-#461 corrective fixed-point refresh — PR #461 post-#460 roadmap/status/dashboard refresh merged at `41297c2a`; §12.2 anchor re-pin.** | Hash `b958e3438c97` → `a0ddc2154026` (state at `41297c2a`, open PRs #440/#447, fork count 47, batch-56). PR #461 completed the post-#460 refresh but included a dashboard test update, so the default-branch guard could not classify the merge as the expected status/dashboard-only lag. This two-file refresh restores the fixed-point shape and keeps the selector on `R-IF-112` plus recurring R-600/R-IF lanes. |
| 2026-06-09 | **Post-#460 terminating refresh — PR #460 R-CXA-1 AS→IS edge-scope/sidecar closeout merged at `b9a9ec87`; §12.2 owed follow-on.** | Hash `39009757fd12` → `b958e3438c97` (state at `b9a9ec87`, open PRs #440/#447, fork count 47, batch-56). R-CXA-1 is now merged closed; the live substitution ledger is 54/54 RETIRED and 54/54 pipeline-advanced. Next selector moves off CXA build-close work to this terminating refresh, then the post-substitution governance/process frontier (`R-IF-112` if operator selects a new substantive arc; recurring R-600/R-IF lanes by cadence). |
| 2026-06-09 | **Substantive R-CXA-1 AS→IS edge-scope/sidecar branch opened from `22d7d53`; §12.2 owed after merge.** | Hash `ffe9044984c8` → `39009757fd12` (branch base `22d7d53`, open PRs #440/#447, fork count 47, batch-56). Filed `.harness/r-cxa-1-as-is-edge-audit-2026-06-09.md`, threaded the stage-5 procedural-tier resolver into the production `RuntimeAsIsWiring` secret-fetch callback, and moved H_T-CXA-1 from PARTIAL to SUBSTANTIVE_RETIRED. Live ledger is now 54/54 RETIRED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |

---

## Audit checklist (run at session start)

- [ ] `workspace_state_hash` matches computed value per recipe above
- [ ] All open PRs at GitHub appear in `In-flight`
- [ ] `recently_completed` reflects last 5 merged PRs
- [ ] `latest_retirement_batch` matches `ls .harness/phase-7d-retirement-events-batch-*.md | tail -1`
- [ ] No `R-NNN` at `Project_Roadmap_v1.md` §5 has `status: ACTIVE` while its `depends_on` are not all RESOLVED (re-derive per §4)

If any check fails → HALT, route per `Project_Roadmap_v1.md` §7.1 step 4.

---

*End of dashboard. Master roadmap at `Project_Roadmap_v1.md`. Enforcement at `CLAUDE.md` §12.*
