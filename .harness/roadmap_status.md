# Roadmap status dashboard

*Refreshed at session-start audit + post-PR-merge audit per workspace `CLAUDE.md` §12. Consumed by the next-action derivation rule at `Project_Roadmap_v1.md` §4. **Do not hand-edit during execution — refresh via the protocol at `Project_Roadmap_v1.md` §7.2.***

---

## Workspace state anchor

| Field | Value |
|---|---|
| `workspace_state_hash` | `0bc25cd0c20f` |
| `last_refreshed` | 2026-06-27T07:26:34+00:00 |
| `git_head` | `f4f4d501` — `feat(R-FS-1): close B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD (CP v1.82 / runtime v1.88) (#784)`. **#784 — CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD` (CP v1.81→v1.82 + runtime change-note v1.87→v1.88, CP + runtime) as the RECONCILER slice — the FINAL durable engine class.** A maybe-ran fan-out / orchestrator `SUB_AGENT_DISPATCH` whose child is a `SINGLE_THREADED_LINEAR` `RECONCILER_LOOP` leaf now RECOVERS by re-dispatch instead of failing the run closed — completing the maybe-ran-subagent recoverability family across ALL FOUR durable engine classes {ESR,WAL,SAVE_POINT,RECONCILER} (`PURE_PATTERN_NO_ENGINE` the lone non-member). **Mechanism:** `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES` += RECONCILER_LOOP in BOTH mirror predicates; worker + orchestrator share the predicate. **GROUNDING (advisor, BEFORE substantive work) resolved the open F-1 at-most-once probe → option (a):** a re-dispatched RECONCILER child runs its OWN crash-resume, the U-CP-97 reconverge gates at the CAS revision claim UPSTREAM of the step loop — clean re-claim → committed prefix auto-resumes + final_state reconstructs; the F-1 won-claim-then-crashed window → `ABORT_REVALIDATION_FAILED` → child FAILED before any step re-executes → parent fold raises `SubAgentChildFailedError` (fail-closed, never a SUCCESS aggregate). The F-1 ABORT→§22.1-HITL is the already-on-main accepted RECONCILER-resume posture (CP v1.79/v1.80); the F-1 engine-lock auto-recovery is separate/pre-existing/broader, NOT a prerequisite → WHOLE close. **Cross-engine-class swap guard (out-of-family Codex [P1], absorbed in-arc — CP v1.82 §3):** the widened set + engine-class-agnostic `compose_child_run_id_seed` meant a same-step_id RECONCILER→SAVE_POINT swap would route the child through a different recovery mechanism, bypassing the CAS protection → FIX: a THIRD identity leg (marker child engine == resumed child engine, fail-closed) in the worker + orchestrator gates, backed by an additive non-attested `child_engine_class` marker field + 2 readers; RED-without-fix witnesses at both surfaces. **WHOLE close (net −1):** standalone_closed 62→63, standalone_registered 4→3 (the FIRST net-negative close of the maybe-ran-subagent family). **Register: 63 closed / 0 gated / 4 resolved / 3 forward** (`closure_gate.py` G1.1 = 3+0; R-FS-1 ACTIVE). **Decorrelated:** advisor full-transcript (option-(a) GO + the two SAVE_POINT-didn't-need verifications + pre-done: fix correct, don't add a redundant full-chain swap test) + out-of-family Codex (`just codex-review` --base, gpt-5.5 $0 OAuth: "implementation + tests coherent", zero code findings; sole [P2] = this very dashboard refresh). harness-cp 1434 passed / 1 xfailed; harness-runtime 2156 passed / 22 skipped / 1 xfailed; pyright 0/0/0; ruff; CI all 15 blocking green. **NEXT = BUILD one of the 3 registered forward arcs:** the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator — real non-mocked recursive-child harness, heavy); `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#782, preserved):** `219acb22` — `feat(R-FS-1): close B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD (CP v1.81 / runtime v1.87) (#782)`. **#782 — CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD` (CP v1.80→v1.81 + runtime change-note v1.86→v1.87, CP + runtime) as the SAVE_POINT slice of the registered maybe-ran SUB_AGENT re-dispatch recoverability extension.** A maybe-ran fan-out / orchestrator `SUB_AGENT_DISPATCH` whose child is a `SINGLE_THREADED_LINEAR` `SAVE_POINT_CHECKPOINT` leaf now RECOVERS by re-dispatch (the child auto-resumes from its durable store under the deterministic child run_id, result-faithfully) instead of failing the run closed. **Mechanism:** extend the DEDICATED `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES` conjunct-1 set `{ESR,WAL}` → `{ESR,WAL,SAVE_POINT}` in BOTH mirror predicates (CP `_subagent_child_recoverable` + runtime `subagent_child_recoverable`; agreement witness enforces parity); worker + orchestrator share the predicate so one change covers both. **Carrier segregation (advisor):** the recoverability set is DEDICATED, distinct from `_FANOUT_REPLAY_ENGINE_CLASSES` which the separate B-FANOUT-OUTPUT-REPLAY `_fanout_replay_store` branch-capture gate consumes and must NOT admit SAVE_POINT. **GROUNDING (advisor, BEFORE substantive work) resolved the open at-most-once probe for SAVE_POINT:** the CP driver computes `resume_at` for SAVE_POINT via `_determine_resume_at` — the engine-class-agnostic F2-prefix join (SAVE_POINT is its reference impl) — so `resume_at>0` → committed prefix auto-resumed, NOT re-fired (at-most-once) → final_state reconstructs (v1.79 store); a SAVE_POINT resume fires NEITHER the ESR/WAL-only inter-step rehydrate NOR the engine-layer recovery loop → NO CAS lease, NO F-1 ABORT window (the cleanest auto-resume). RECONCILER stays OUT (its reconverge carries the F-1 won-CAS-claim-retry-ABORTs-to-HITL window). **Spec-vs-fork:** bundled-absorption, NOT X-AL-3 (§25.15 v1.77 §3 framed `{ESR,WAL}` as the witnessed slice + named SAVE_POINT/RECONCILER as a registered follow-on; v1.77 §3 precedent = additive recovery for a previously-fail-closed surface sacrifices no committed decision → no operator gate; blocker retired at v1.79). **DECOMPOSE — close 1 + register 1 (net-zero):** registers `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD` (the F-1 CAS-claim window). **Register: 62 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; R-FS-1 ACTIVE). **Witnesses (RED-without-fix, empirically confirmed by reverting source to main):** runtime predicate flip SAVE_POINT→True + the new RECONCILER→False control + the verdict-locked CP↔runtime agreement witness; the EFFECT-level recursive-child SAVE_POINT crash-resume reconstruct (full final_state + only step-2 re-dispatched → committed prefix not re-fired, at-most-once); the CP full-chain parent-driven orchestrator SAVE_POINT-child RECOVERS (RED confirmed) + the repurposed RECONCILER negative-control fail-closed; stale SAVE_POINT-as-non-recoverable docstrings corrected. **Decorrelated: advisor full-transcript** (decompose GO + five sharpenings: EFFECT-level crux witness, carrier-segregation grep, RECONCILER contrasting baseline, §25.15-body fail-closed confirmation, don't-over-excavate-RECONCILER; pre-done flagged the two completeness checks, both addressed) **+ out-of-family Codex** (hit the documented codex-CLI verdict-streaming capture limitation in non-interactive mode — diff/context assembled but no model verdict captured, only wrapper env warnings; recorded honestly per `[[hooks-codex-pilots-decorrelation-validated]]`, the diff rests on advisor + the witness set + green suites; change mirrors the cleared #774/#777 family verification shape). harness-cp 1427 passed / 1 xfailed; harness-runtime 2152 passed / 22 skipped / 1 xfailed; pyright 0/0/0; ruff; overlay 330/31; CI all 15 blocking green. **NEXT = BUILD one of the 4 registered forward arcs:** `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD` (the RECONCILER F-1 leaf — probe at open whether the maybe-ran RECONCILER child re-dispatch ABORT→HITL is the correct disposition or needs the F-1 lock-holding arc first); the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator, real non-mocked recursive-child harness — heavy); `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#781, preserved):** `170be08a` — `feat(R-FS-1): close B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER (CP v1.79→v1.80) (#781)`. **#781 — CLOSED `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` (CP v1.79→v1.80, CP-ONLY) as the RECONCILER slice — COMPLETING the final_state-reconstruction family across ALL FOUR durable resumable classes {ESR, WAL, SAVE_POINT, RECONCILER}.** A crash-resumed SINGLE_THREADED_LINEAR `RECONCILER_LOOP` run (child OR top-level) over a committed prefix now reconstructs its COMPLETE `final_state` from the durable EngineOutputStore instead of a silently suffix-only one. **GROUNDING RESOLVED the registered two-authorities probe in favor of store-reuse:** the `ReconcilerEnginePauseResumeSubstrate` (U-RT-123) does NOT persist the per-step `accumulated` output map — its `PauseEvent` carries a `StateSummary` DIGEST (summary_text + summary_hash + ledger refs) for the etcd CAS-lease + revalidation (the v1.79 §3 + the prior code comment's “partial_state in its PauseEvent” was a mischaracterization — there is no partial_state field; corrected). The reconciler substrate is the authority for engine-layer CONVERGENCE state; the EngineOutputStore is the authority for the CP per-step OUTPUT map → different data, no one-source-of-truth violation; derive-from-the-substrate is non-viable. A real RECONCILER run flows through the SAME linear dispatch loop as ESR/WAL/SAVE_POINT → the close is a CP-side gate extension: add RECONCILER to the ONE `_FINAL_STATE_RECONSTRUCT_ENGINE_CLASSES` constant (gates producer + seed). owner_axis “CP + runtime” anticipated → CP-ONLY confirmed (the store binds class-agnostically). **Blocking witness (advisor):** a producer-half full-chain RECONCILER round-trip through the REAL `execute_workflow` — RED on HEAD (phase 1 recorded nothing, `assert set()=={0,1,2}`) → GREEN with the gate extension; plus the gate-load-bearing guard + the RECONCILER-specific CAS-ordering pins (aborting reconverge → FAILED with final_state=None upstream of the seed; won/clean → falls through + reconstructs). **CLOSE 1 + REGISTER 1 (net-zero):** out-of-family Codex caught that the maybe-ran SUB_AGENT recoverability predicates (CP `_subagent_child_recoverable` + the runtime mirror, both gating {ESR,WAL}) cited THIS arc as their exclusion blocker — final_state now reconstructs for SAVE_POINT/RECONCILER children, retiring that “no store → suffix-only” reason, but re-dispatch recoverability stays fail-closed pending the newly-registered `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-RECONCILER-CHILD` (the RECONCILER F-1 CAS-claim at-most-once window). Both stale docstrings corrected; predicate behavior UNCHANGED. **Register: 61 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; R-FS-1 ACTIVE). Decorrelated: advisor full-transcript (two-authorities probe-resolve + the blocking-witness criterion + the owner_axis CP-only check + the register-don't-fold disposition) + out-of-family Codex (clean re-review after the recoverability-predicate stale-rationale catch). harness-cp 1426 passed / 1 xfailed; harness-runtime 2142 passed / 10 skipped (non-e2e); pyright 0/0/0; ruff; overlay 330/31; CI all 15 blocking green. **NEXT = BUILD one of the 4 registered forward arcs:** `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-RECONCILER-CHILD` (the recoverability extension this arc unblocked — ground the RECONCILER F-1 CAS-claim at-most-once at open; SAVE_POINT leaf likely the cleaner decomposed slice); the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator, real non-mocked recursive-child harness — heavy); `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). **PRIOR LINEAGE (#779, preserved):** `c4ee1a8c` — `feat(R-FS-1): close B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT (CP v1.78→v1.79) (#779)`. **#779 — CLOSED `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT` (CP v1.78→v1.79, CP-ONLY) as the SAVE_POINT slice of the registered SAVE-POINT-RECONCILER arc.** A crash-resumed SINGLE_THREADED_LINEAR `SAVE_POINT_CHECKPOINT` run (child OR top-level) over a committed prefix now reconstructs its COMPLETE `final_state` instead of a silently suffix-only one (a result-fidelity bug; a child fold corrupted the parent aggregate). **GROUNDING overturned the registered "needs an entirely NEW output substrate" anticipation:** the `EngineOutputStore` is mechanically engine-class-AGNOSTIC (binds on `engine_output_replay`, not engine class) + a real SAVE_POINT run flows through the SAME linear dispatch loop as ESR/WAL → the close is a CP-side gate extension reusing the class-agnostic store (the producer + seed gates unified onto ONE `_FINAL_STATE_RECONSTRUCT_ENGINE_CLASSES` constant — "never record-only" structural). CP-ONLY (no runtime SPEC delta; the `engine_output_replay` docstring clarified impl-side). NO committed-invariant sacrifice; at-most-once PRESERVED (pure store READ); NO §5.2 IS-hash arc. **Blocking witness (advisor):** a producer-half full-chain SAVE_POINT round-trip through the REAL `execute_workflow` (NOT hand-seeded) — RED on HEAD (phase 1 recorded nothing — producer excluded SAVE_POINT) → GREEN with the gate extensions; the honest-vs-vacuous gate. LINEAR-scoped (mirrors v1.75). **Decorrelated: advisor full-transcript** (Reading A over the new-substrate anticipation + the blocking producer-half witness criterion + the RECONCILER two-authorities split + the pre-done SAVE_POINT-vs-RECONCILER discriminator: LangGraph is SAVE_POINT's *reference* substrate not the harness impl path → no competing harness authority) **+ out-of-family Codex** ("the code changes appear coherent" — no code finding; its one [P2] = the §12.2.1 roadmap-status process-artifact, this refresh). harness-cp **1423** passed / 1 xfailed; harness-runtime **2142** passed / 10 skipped (non-e2e); pyright 0/0/0; ruff; overlay 330 nodes / 31 seams; CI all 15 blocking green (incl. Codex anti-rot after a gh-down dashboard regen for CI parity). **DECOMPOSE — close 1 + register 1** (`B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` — RECONCILER is ENGINE-OWNS-SUBSTRATE, its `partial_state` reconciler substrate is the authority, so store-reuse-vs-derive needs its own grounding) → `standalone_registered` **4 → 4** net-zero. **Register: 60 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; R-FS-1 ACTIVE). **NEXT = BUILD one of the 4 registered forward arcs:** `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` (the RECONCILER reconstruction — probe store-reuse-vs-derive-from-the-reconciler-substrate at open; the natural value-rank next, completes the final_state-reconstruction family); the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator, need a real non-mocked recursive-child harness — heavy); `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#777, preserved):** `8339a791` — `feat(R-FS-1): close B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT (orchestrator analogue, LINEAR-{ESR,WAL}-leaf slice) (#777)`. **#777 — CLOSED `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT` (CP v1.77→v1.78 + runtime v1.85→v1.86) as the LINEAR-`{ESR,WAL}`-leaf-child slice — the ORCHESTRATOR analogue of the #774 worker arc.** A maybe-ran fan-out `SUB_AGENT_DISPATCH` ORCHESTRATOR (`steps[0]`, ORCHESTRATOR_WORKERS / HIERARCHICAL_DELEGATION) on a strict-tier crash-resume now RECOVERS by re-dispatch (the whole fan-out re-runs fresh → the orchestrator re-dispatches → its child auto-resumes under the deterministic child run_id, result-faithfully) instead of failing the run closed. **Mechanism (REUSES the worker predicate/seed/dual-gate verbatim — no new typed surface):** the `is_orchestrator_dispatch` hash-inert `StepExecutionContext` flag extends the deterministic `child_run_id_seed` to the orchestrator dispatch (`branch_path=None`, distinct from worker `branch_path` seeds, RESET on every fan-out child → no orchestrator↔worker child aliasing, no leak to the EVALUATOR_OPTIMIZER/RECONCILER_LOOP iterated steps) + `record_orchestrator_dispatched(child_recoverable=...)` marker + `orchestrator_subagent_child_recoverable` reader + the SUB_AGENT recovery disjunct in `_determine_fanout_resume`'s orchestrator maybe-ran block ([P1-b] dispatch∧resumed dual gate + same-step_id guard). **Selection per advisor convergence-value criterion** (rank forward arcs by unblock/avoid-spawn over code-freshness; close-≥-register tripwire grounding-confirmed net-zero — the SAME profile as the worker #774). **Decorrelated: advisor full-transcript** (arc sound — pristine-window guard, PROCEED exclusion, branch_path distinctness; flagged + closed the HIERARCHICAL_DELEGATION parity gap + named the half-proof composition: the orchestrator child's real-store auto-resume is composed from the seed-wiring test + the worker recursive-child witness [caller-agnostic `ChildWorkflowRunner`] + the full-chain driver tests) **+ out-of-family Codex** (found 2 genuine [P2] additive-store-boundary bugs advisor+I both missed — the new recoverability reader called directly → `AttributeError` on a pre-v1.86/partial store + the orchestrator marker writer always passing `child_recoverable=` → `TypeError` on a pre-v1.86 3-arg store; BOTH fixed structurally [`getattr` + only-pass-when-not-None, mirroring the worker idioms] with regression witnesses; a 3rd [P2] = regenerate `roadmap.html` for the ledger bump, done). **Full suites green:** harness-cp **1422** passed / 1 xfailed; harness-runtime **2149** passed / 23 skipped / 1 xfailed; pyright 0/0/0; ruff; overlay 31/31; CI all 15 blocking green (incl. X-AL-3 back-flow + the codex anti-rot gate). **close 1 + register 1** (`B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` — orchestrator-child fan-out + nested-sub-agent recursion) → `standalone_registered` **4 → 4** net-zero. **Register: 59 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; R-FS-1 ACTIVE). **NEXT = BUILD `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT-RECONCILER`** — the foundational output-substrate unblock (advisor's convergence-value pick: it unblocks the SAVE_POINT/RECONCILER child case across worker-SUBAGENT + orchestrator-SUBAGENT + top-level; design-heaviest, deserves a fresh context). The other 3 forward: `…-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` (worker deeper recursion), `…-ORCHESTRATOR-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` (orchestrator deeper recursion), `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#774, preserved):** `4502c721` — `ops: untrack 327 files a git add -A swept into PR #774 (#775)` over `91cecfe7` `fix(R-FS-1): close B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT — recursive-child crash-resume (LINEAR-{ESR,WAL}-leaf slice) (#774)`. **#774 — CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` (CP v1.76→v1.77 + runtime v1.84→v1.85) as the LINEAR-`{ESR,WAL}`-leaf-child slice.** A maybe-ran fan-out `SUB_AGENT_DISPATCH` worker on a strict-tier crash-resume now RECOVERS by re-dispatch (the deterministic child run_id makes the child's OWN crash-resume auto-resume from the shared durable store, result-faithfully) instead of failing the run closed. **Mechanism:** E1 (`child_run_id_seed` — branch_path-folded + fan-out-worker-scoped) + the `subagent_child_recoverable` predicate (`{ESR,WAL}` ∧ SINGLE_THREADED_LINEAR ∧ leaf) + the CP `_fence_unrecoverable_maybe_ran_indices` SUB_AGENT recovery disjunct with the [P1-b] dispatch∧resumed dual gate. Corrected the #746 reverted branch (LINEAR-narrowing = the [P1-a] fix; seed-gating). **Out-of-family Codex caught a real [P1]** — the seed `(parent_idempotency_key, child_workflow_id)` collided across fan-out siblings (`compose_branch_child_context` inherits `parent_idempotency_key` verbatim from the fan-out parent) → FIXED by folding §25.16 `branch_path` + scoping to fan-out workers (also forecloses the EVALUATOR_OPTIMIZER/RECONCILER_LOOP `step_index`-reuse hazard, empirically confirmed at `workflow_driver.py:2040`). **advisor (transcript-aware) flagged the missing full-chain witness** → added (a) seed-resume-stability through a REAL PARALLELIZATION crash-resume (the resume path rebuilds the branch ctx identically) + (b) the CP↔runtime predicate-agreement witness (the CP-True/runtime-False double-fire drift foreclosed). Full suites green: harness-cp **1411** passed / 1 xfailed; harness-runtime **2137** passed; pyright 0/0/0; ruff; CI all blocking green (incl. X-AL-3 back-flow + the codex-closeout anti-rot gate). **close 1 + register 1** (`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` — fan-out-child + nested-sub-agent recursion) → `standalone_registered` **4 → 4** net-zero. **#775 — cleanup:** PR #774's first impl commit's `git add -A` swept 327 pre-existing UNTRACKED files (`.agents/skills/`, `dashboard-design/output`+`archive` [the out-of-family Codex [P2] session-metadata-leak logs], `.codex/agents/`, `.claude/agents/`) into the squash merge → `git rm --cached` + `.gitignore` restored them to untracked (the 5 pre-existing tracked workspace skills retained). **Register (current): 58 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; R-FS-1 ACTIVE). **NEXT = BUILD `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT`** — the orchestrator analogue of this just-closed worker arc (builds on the SAME E1 + recoverability-predicate + classifier mechanism; the natural value-rank next). The other 3 forward: `…-NONLEAF-CHILD` (deeper recursion), `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT-RECONCILER` (output substrate, heavy), `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#772, preserved):** `9e08420f` — `ops: remediate the --uncommitted review-pollution flaw (loop → --base; verification-arc handoff) (#772)`. **VERIFICATION ARC (operator-opened 2026-06-26; NOT an R-FS-1 forward arc — register UNCHANGED 57 closed / 0 gated / 4 resolved / 4 forward).** The operator surfaced that the loop's out-of-family Codex review was being run via `just codex-review-uncommitted` while ~2,500 untracked files (dashboard-design/, .agents/skills/, .codex/agents/, present since Jun-18) diluted the review of the actual diff across 13 sessions (Jun 21-26). **Honest causal read (advisor): Codex is non-exhaustive in ANY mode → "pollution CAUSED missed bugs" is UNPROVABLE; the process flaw is real, the causal story is not.** **Verification (deterministic, bounding the residual):** full provider-free suite **5,032 passed / 0 failed**; pyright 0/0/0; ruff; **diff-coverage of the window (`c5dd9189..HEAD`) = 94%** (1,638 changed lines, 93 uncovered — ~75 defensive, ~18 load-bearing CONVERGING with the Codex sweep on the orchestrator-fence/ABORT_BRANCH/torn-marker neighborhood). **3 merges this arc:** **#770** (`69bdf0a0`) — the recursive-child crash-resume final_state [P1-a] discriminator witness (de-risks `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT`: [P1-a] EMPIRICALLY CONFIRMED closed for the LINEAR child; remaining scope = E1 + [P1-b] gate + LINEAR narrowing); **#771** (`3a3b78ce`) — **CONFIRMED+FIXED a real at-most-once [P1]: a torn fan-out cardinality marker** (`read_fanout_cardinality`→None / `fanout_cardinality_present`→True) misread as absent at TWO sites — `workflow_driver.py:5513` (changed-cardinality guard → silently DROPS in-flight branches) + `:2296` (strict-tier maybe-ran → fresh-re-dispatch = DOUBLE-FIRE); fix = branch on `fanout_cardinality_present` (5513 gated non-orchestrator; orchestrator 5662 already handled it), 4 regression witnesses, advisor + codex-review --base (0 findings) + broad suite green; **#772** (`9e08420f`, this) — process remediation (loop → `--base`; `ship-pr/SKILL.md:15` loophole removed) + the durable handoff `.harness/uncommitted-review-flaw-verification-arc.md`. **NEXT = the VERIFICATION CONTINUATION (fresh context, decorrelated mechanisms — operator is non-coding, code findings NOT surfaced):** triage the 4 remaining [P2] candidate sites (`workflow_driver.py:7961/8220/6307` + `pause_resume_protocol.py:636`); run a `harness-adversarial-reviewer` dedicated-agent sweep over the ~18 uncovered load-bearing lines (the operator's "another mechanism"); fix the pre-existing #542 flaky cascade test (timing race) — all per the handoff doc §6. **OR resume an R-FS-1 forward arc** (the 4 registered: MAYBE-RAN-SUBAGENT [now de-risked by #770] + ORCHESTRATOR-MAYBE-RAN-SUBAGENT + SAVE-POINT-RECONCILER + PROCEED-RESIDUAL [operator-gated]). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#768, preserved):** `5b246303` — `feat(R-FS-1): B-TOP-LEVEL-CRASH-RESUME-FINAL-STATE-RECONSTRUCT — top-level resume final_state reconstruction (CP v1.76) (#768)`. **CLOSED the v1.75-registered `B-TOP-LEVEL-CRASH-RESUME-FINAL-STATE-RECONSTRUCT` as the GENERAL top-level reconstruction (framing B), DECIDED AT OPEN as impl-not-fork — a WHOLE close.** On a durable-engine-class resume over a committed prefix the top-level `run_workflow` handler / `api.run`+`api.resume` returned a SUFFIX-ONLY `final_state` (v1.75 fixed only the CHILD case, default-off for top-level) → a top-level resumed run reported status SUCCESS while silently truncating its output (a silent-corruption result-fidelity bug). **Fork question (v1.75 §2: "is top-level resume transparency a committed invariant?") answered impl-not-fork by three decorrelated signals:** (i) the §25.2 insurance read at SOURCE — the spec is SILENT on the resume final_state shape (no final_state output-equivalence / resume-transparency invariant; ADR-D1 §1.1 commits only the INPUT-side channel replay) → no forbidding invariant; (ii) the v1.75 "candidate fork" + punt were deferral-to-grounding, and the flag-isolation control test was v1.75's own punt-voice, not a commitment; (iii) the **decisive empirical probe** — flip the `execute_workflow` `reconstruct_final_state` default `False→True`, run the FULL suites, sweep ALL consumers → broke **EXACTLY ONE test** (that flag-isolation control), CP 1395/1 + runtime 2134/0 (incl. live-e2e api.run/api.resume) → no real consumer relies on suffix-only → the SAME impl-not-fork classification the child case earned. **FIX (CP-only default flip):** `reconstruct_final_state` default `False→True` on execute_workflow/_execute_workflow_body; the top-level `mcp_server.py:427` handler takes the default → reconstructs with NO runtime src edit (CP-only, mirrors v1.66/v1.73). CP spec v1.76 §1 DEFINES the resume-transparency invariant under C-CP-25 §25.2/§25.6 (a resumed run's final_state/partial_state reconstructs the COMPLETE terminal state for ESR/WAL when the store is bound; store↔ledger skew FAILS closed — honest failure over silent truncation, now reaches the top-level path; SAVE_POINT/RECONCILER degrade — the registered output-substrate follow-on, scope extended to cover the top-level case; explicit `reconstruct_final_state=False` opt-out). NO contract removal / closed-enum / committed-invariant sacrifice / new-fail-class; at-most-once PRESERVED (pure store READ). **Witnesses:** `test_top_level_default_reconstructs_final_state` (default-on reconstructs full state — RED before the flip) + `test_explicit_opt_out_leaves_suffix_only_final_state` (gated-not-automatic control, reframed from the v1.75 flag-isolation control) + the preserved v1.75 witnesses. **Full suites green:** harness-cp **1397** passed / 1 xfailed; harness-runtime **2134** passed / 23 skipped / 1 xfailed; **full workspace 5046 passed** (the lone failure is the documented AF_UNIX-path-too-long socket env artifact — passes with `--basetemp=/tmp/hs`, skips in provider-free CI; CI pytest green 56s); pyright 0/0/0; ruff; overlay 31/31; CI all blocking green (incl. the X-AL-3 back-flow guard). **Decorrelated: advisor full-transcript** (pick-TOP-LEVEL + value-requires-default-on + a reconcile overturning a provisional fork-lean as over-caution → the empirical probe + the §25.2 insurance read + 3 accounting fixes) **+ out-of-family Codex** (no code defect; its 2 [P2]s were out-of-scope process artifacts). CP v1.75→v1.76 + 1 clearance marker; CP-ONLY, NO runtime SPEC/src delta. **WHOLE close** (registers nothing) → `standalone_registered` **5 → 4**. Register: **57 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0). **NEXT = BUILD one of the 4 registered forward arcs:** the 2 recursive-child SUBAGENT legs (`MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT`, UNBLOCKED-for-ESR/WAL by v1.75+v1.76 but needing a REAL non-mocked recursive-child harness — heavy; MAYBE-RAN-SUBAGENT was ATTEMPTED+REVERTED at #746); the `SAVE-POINT-RECONCILER` output-substrate (now covers child AND top-level — a larger lift); `ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated (C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#766, preserved):** `3f2c4d8c` — `feat(R-FS-1): B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT — child-scoped final_state reconstruction (CP v1.75) (#766)`. **Built the registered arc as the CHILD-SCOPED, SINGLE_THREADED_LINEAR EVENT_SOURCED_REPLAY / WAL_SEGMENT slice (a DECOMPOSE).** On a durable-engine-class crash-resume over a committed prefix `execute_workflow` returned a SUFFIX-ONLY `final_state` (loop starts at `resume_at` with `accumulated` empty; the committed prefix is skipped + never seeded) → a child sub-workflow folds that truncated state into the parent fan-out / B-HIERARCHICAL-PAUSE (#680) aggregate, silently corrupting it (finding-v2's by-execution witness). **FIX (child-scoped framing A, additive — advisor forced the discriminating grep that found NO committed result-fidelity / resume-transparency invariant, so impl-against-the-silent-gap NOT a fork):** an additive opt-in `reconstruct_final_state` (only `child_workflow_runner` passes True; top-level `api.run` passes default False → byte-unchanged) seeds the durably-stored committed prefix `[0, resume_at)` into `accumulated` BEFORE the loop via the shared `_read_durable_replay_prefix` (the read+validate half factored from the inter-step channel rehydrate; fail-closed on store↔ledger skew / identity-mismatch / unreadable-store; config-flip degrades to suffix-only). Both resume paths (crash-recovery + the `resume_at_step_index_override` path the #680 re-enter drives) reach the same path-agnostic seeding site → ALSO closes the pre-existing #680 suffix-only fold for LINEAR ESR/WAL children. `final_state` AND DRAINED `partial_state` reconstruct; a FAN-OUT child's aggregate is reconstructed by the separate B-FANOUT-OUTPUT-REPLAY family (not this arc); SAVE_POINT/RECONCILER children have no output store → degrade → registered follow-on. NO committed-invariant change; at-most-once PRESERVED (pure read of already-durable outputs). **DECOMPOSE — close 1 + register 2** (`B-CHILD-...-SAVE-POINT-RECONCILER` output-substrate + the fork-bearing `B-TOP-LEVEL-...`) → the 2 recursive-child SUBAGENT arcs are UNBLOCKED-for-LINEAR-ESR/WAL (result-fidelity blocker removed; their own recursive-child mechanism still to build). **Witnesses (by-execution, RED-without-fix):** seeds-committed-prefix + RED-without-flag control + store-skew fail-closed + SAVE_POINT out-of-scope degrade + no-journal degrade + a producer→crash→consumer store round-trip + DRAINED partial_state + the #680 override-path reconstruction + a runtime `child_workflow_runner` wiring witness. **Full suites green:** harness-cp **1394** passed / 1 xfailed; harness-runtime **2127** passed / 10 skipped; harness-cxa 28; workspace **5026** passed; pyright 0/0/0; ruff clean; overlay 31/31; CI all 15 blocking green (incl. the X-AL-3 back-flow guard). **Decorrelated: advisor full-transcript** (A-vs-B framing BEFORE substantive work + pre-done caught the untested `partial_state` behavior change + the unwitnessed #680 claim, both closed with witnesses) **+ out-of-family Codex** (code reviewed cleanly). CP v1.74→v1.75 + 1 clearance marker; NO runtime SPEC delta (the `child_workflow_runner` opt-in is impl-to-cleared-spec). Register: **56 closed / 0 gated / 4 resolved / 5 forward** (`closure_gate.py` G1.1 = 5+0; `standalone_registered` 4 → 5). **NEXT = BUILD one of the 5 registered forward arcs:** the 2 recursive-child SUBAGENT legs (`MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT`, now unblocked-for-LINEAR-ESR/WAL but needing a REAL non-mocked recursive-child harness — heavy; MAYBE-RAN-SUBAGENT was ATTEMPTED+REVERTED at #746); the NEW `SAVE-POINT-RECONCILER` (needs an output substrate for those classes — a larger lift) + `TOP-LEVEL` (fork-bearing — ground fork-vs-impl at open); `ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated. This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#764, preserved):** `fc5160ba` — `docs(R-FS-1): B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT finding v2 — option (a) ruled out; register B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT (#764)`. **STOP disposition (no code change — the current fail-closed behavior is correct).** A by-execution witness over the REAL `execute_workflow` proved a durable-engine-class resume over an F2-committed prefix returns a SILENTLY suffix-only `final_state` (status SUCCESS; `accumulated` empty at `resume_at`; prefix-output replay degenerate-at-HEAD) for SAVE_POINT_CHECKPOINT + EVENT_SOURCED_REPLAY. A SUB_AGENT child runs through the same `execute_workflow` and the parent folds its `final_state` verbatim (`sub_agent_dispatch.py:660-663`) → a re-dispatched maybe-ran child corrupts the parent aggregate. NOT fan-out-specific → option (a)'s LINEAR narrowing (finding v1) does NOT escape it → option (a) ruled out; disposition reverts to option (b) (stay fail-closed). REGISTERED the true prerequisite `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT` (seed `accumulated` from the durable prefix outputs on resume — the store EXISTS via B-ENGINE-OUTPUT-REPLAY but seeds the inter-step CHANNEL, not final_state) + repointed both SUBAGENT arcs' blocker at it; ALSO closes the pre-existing B-HIERARCHICAL-PAUSE (#680) suffix-only fold. Continuity: the #746 session already found this (untracked class_3 + arc-open docs, never landed) — committed here with supersession headers. Decorrelated: advisor full-transcript ×2; Codex N/A (no code diff). Register: **55 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0; `standalone_registered` 3 → 4). **NEXT = `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT`** — the unblocker for the 2 SUBAGENT arcs; child-scoped-vs-top-level-resume fork question at open (advisor: deep-arc reschedule, fresh context). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#762, preserved):** `feat(R-FS-1): B-FANOUT-EFFECT-FENCE-SCOPED-ABORT-CRASH-DURABLE — crash-durable scoped-abort (CP v1.74 / runtime v1.84) (#762)`. **Crash-DURABLE persistence of the scoped-abort disposition** (the v1.73 §2 residual, WHOLE close). v1.73 reused the `completed`/no-output store terminal for an `ABORT_BRANCH` branch → indistinguishable from a ran-and-errored branch on crash-resume → an all-scoped-abort resume that CRASHED after the captures reconstructed PAUSED→PARTIAL, not the in-resume all-abort FAILED (status-fidelity only; NO double-fire — a recovered terminal is never re-dispatched). **FIX (replay-store side — the disposition-vocabulary decision answered: NOT a new IS terminal_status → NO §5.2 IS-hash arc):** the durable store records a scoped-abort branch under the DISTINCT disposition `scoped_aborted` (the `_capture_branch_terminal` calls at both fan-out sites; the IS-hash-bearing F2 ledger terminal STAYS `completed`); the runtime store read-back closed-set guard accepts `scoped_aborted` additively (a `scoped_aborted` record carrying a non-None output is rejected as malformed — the out-of-family Codex [P2], else a mixed abort+survivor recovery folds a spurious survivor). On crash-resume the two recovered-terminal seed loops reconstruct `_scoped_abort_ordinals` → the existing all-abort guard reproduces FAILED (all-abort) / PARTIAL (mixed survivor) across a crash; the `completed`-keyed `_crash_pause_trigger`/`_crash_pause_reestablish` gates no longer mis-fire on a scoped-abort branch (the `timed_out` precedent); recovered scoped-abort ordinals are EXCLUDED from the re-record loop (no duplicate ledger terminal). **Witnesses: GENUINE producer→crash→consumer chains at BOTH topologies** (real `ABORT_BRANCH` resume WRITES the store → fresh-ledger run RECONSTRUCTS — PARALLELIZATION all-abort → FAILED + mixed → PARTIAL; ORCHESTRATOR_WORKERS all-abort → FAILED) — each RED-without-fix verified — + a runtime store round-trip + malformed-sidecar fail-closed (RED without the accept-set extension / the output-None invariant). **Full suites green:** harness-cp **1388** passed / 1 xfailed; harness-runtime **2132** passed / 23 skipped / 1 xfailed; harness-cxa 28 passed; pyright 0/0/0; ruff clean; overlay-check 31/31; CI all 15 blocking green (incl. the X-AL-3 back-flow guard). NO contract/closed-enum/committed-invariant change; at-most-once PRESERVED. **Decorrelated: advisor full-transcript** (BEFORE substantive work — the A-vs-B replay-store-vs-IS-ledger determination + the ENFORCED read-back-guard catch + the result-fidelity bar; pre-done) **+ out-of-family Codex** (1 complete round → the [P2] output-None invariant, fixed + witnessed; confirmatory re-runs hit a codex-CLI verdict-streaming capture limitation in non-interactive mode — the fix is a minimal structural guard mirroring the adjacent corruption check). CP v1.73→v1.74 + runtime v1.83→v1.84 + 2 clearance markers. **WHOLE close** (registers nothing) → `standalone_registered` **4 → 3**. Register: **55 closed / 0 gated / 4 resolved / 3 forward** (`closure_gate.py` G1.1 = 3+0). **NEXT = BUILD one of the 3 registered B-FANOUT follow-ons:** the 2 recursive-child legs (`MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT`) need a REAL non-mocked recursive-child crash-resume harness (heavy; MAYBE-RAN-SUBAGENT was ATTEMPTED+REVERTED at #746); `ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated (C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **PRIOR LINEAGE (#760, preserved):** `feat(R-FS-1): B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT — per-branch-scoped fence abort (CP v1.73) (#760)`. **Per-branch-SCOPED effect-fence abort:** a 4th `EffectFenceResolution` value `ABORT_BRANCH` fails JUST one fence-paused fan-out branch (record terminal, never re-dispatched → at-most-once: ambiguous effect never re-fired) while the vouched-for siblings (SKIP_AS_FIRED/RE_FIRE) FIRE and the run folds survivors per cascade_policy (PARTIAL with survivors; FAILED if none survive; CASCADE_CANCEL → FAILED; a PROCEED resume rejected requires-strict-tier). **DECIDED-AT-OPEN: NOT operator-gated** (grounded from the cleared spec, overturning the prior dashboard's "operator-gated" label): ABORT stays byte-identically RUN-terminal (v1.65 §1(b); the `_any_fence_abort` guard keys on `is ABORT`) → no committed-invariant sacrifice; C10 probe-resolved inline by the v1.65 §1(d) PROCEED precedent → no council. **CP-only** (ABORT_BRANCH never reaches the runtime §14.22 fence gate → no runtime delta) + no §5.2 IS-hash change (reuses the `completed` terminal_status). Enum 3→4 additive (SubAgentResultStatus precedent; cardinality test). **Hardened over 4 out-of-family Codex rounds (decorrelated, asymptotic):** 2×[P1] (mixed-map interception-before-suppression + CASCADE_CANCEL-hiding) + 2×[P2] (iterative-provenance → FAILED-only-provenance; PROCEED durable-corruption → recording PROCEED-guarded). A 5th Codex [P1] + advisor reframed rounds 3/4/5 as ONE root cause (reuse-`completed` cannot carry the scoped-abort semantic across the DURABLE crash-resume store) → REGISTER (the distinguishable-durable-record fix splits the disposition vocabulary → round-6 risk); the residual (all-scoped-abort resume that CRASHES after the captures reconstructs PAUSED→PARTIAL not FAILED) is status-fidelity-only, NO double-fire (execution-repro: `re-dispatched=[]`) → `B-FANOUT-EFFECT-FENCE-SCOPED-ABORT-CRASH-DURABLE`. **Full suites green:** harness-cp **1385** passed / 1 xfailed; runtime fence 33 passed; pyright 0/0/0; ruff clean; CI all 15 blocking green. **close 1 + register 1 (net-zero)** → `standalone_registered` **4 → 4**. Register: **54 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0). **NEXT = BUILD one of the 4 registered B-FANOUT follow-ons:** the 2 recursive-child legs (`MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT`) need a REAL non-mocked recursive-child crash-resume harness (heavy; MAYBE-RAN-SUBAGENT was ATTEMPTED+REVERTED at #746); `ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated (C10⊥C11 council + AUQ at open); the NEW `SCOPED-ABORT-CRASH-DURABLE` needs the disposition-vocabulary decision (replay-store vs IS-ledger; possibly a §5.2 IS-hash arc). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). **WHOLE close of the FENCE-STEP-ID arc (registered at v1.71 §2):** the v1.71 §1 PAUSE-reconstruct lift (re-fire-safe only) is EXTENDED to recover the FENCE-RECOVERABLE (TOOL_STEP/MANAGED_AGENTS) maybe-ran ordinals — CARRIED into the reconstructed snapshot's `effect_fence_paused_branches` so `api.resume` re-dispatches each THROUGH the existing v1.65 resume material-diff guard (changed-step_id / changed-kind → fail closed, the #756 Codex [P1] fix) + the auto-active fence (at-most-once). **Grounding overturned v1.71 §2's "new carrier" anticipation:** the WORKER carrier `EffectFencePausedBranchResumeState` already exists and IS the mirror (the orchestrator needed a new top-level carrier ONLY because steps[0] runs before any branch exists) → NO new model/snapshot-field/resume-guard, the v1.65 machinery REUSED. `idempotency_key=""` eventually-correct (CP can't derive the opaque-tool_id runtime fence key; the operator is never asked at reconstruct → the first resume re-dispatches fresh into the auto-active fence which re-pauses with the real key if ambiguous). §2 folds the #742 crash-time changed-step_id hole into the SoT classifier `_fence_unrecoverable_maybe_ran_indices` (a marker-step_id==resumed-step_id conjunct fed by the NEW runtime `dispatched_branch_step_ids` reader). Paired runtime v1.82→v1.83 (the thin reader over the already-recorded marker step_id; no new field). **Decorrelated: advisor full-transcript (the reuse insight + co-existence gate + peer-path guard verification + the key="" reconcile) + out-of-family Codex [P1]** — caught the ORCHESTRATOR_WORKERS/HIERARCHICAL gap: reconstruct-no-dispatch always short-circuits at the EARLY `_reestablish_fan_out_resume` (empty branch_plan) BEFORE the worker-failed pause site, dropping the carrier for that topology (the PARALLELIZATION-only witnesses passed while orch-workers had the hole); FIXED at `workflow_driver.py:8553` + an orch-workers witness (incl. a clean-resume positive control) + the regression guard verified genuinely RED without the fix. **Full suites green:** harness-cp **1371** passed / 1 xfailed; harness-runtime + harness-cxa **2159** passed / 23 skipped / 1 xfailed; pyright 0/0/0; ruff clean; overlay-check 31/31; CI all blocking green (incl. the X-AL-3 back-flow guard). NO contract/closed-enum/committed-invariant change; at-most-once PRESERVED. CP v1.71→v1.72 + runtime v1.82→v1.83. **WHOLE close** (registers nothing) → `standalone_registered` **5 → 4**. Register: **53 closed / 0 gated / 4 resolved / 4 forward** (`closure_gate.py` G1.1 = 4+0). **NEXT = BUILD one of the 4 registered B-FANOUT follow-ons:** the 2 non-operator-gated (`MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT`) need a REAL non-mocked recursive-child crash-resume harness (heavy; MAYBE-RAN-SUBAGENT was ATTEMPTED+REVERTED at #746); `EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT` + `ORCHESTRATOR-PROCEED-RESIDUAL` are operator-gated (C10⊥C11 council + AUQ at open). This refresh lags HEAD by one commit (the §12.2.1 terminating-refresh fixed point). |
| `latest_retirement_batch` | `.harness/phase-7d-retirement-events-batch-57.md` |
| `open_fork_doc_count` | 86 |

**Hash recipe.** `sha256(git_head[:8] + "|" + sorted_open_pr_csv + "|" + open_fork_doc_count + "|" + latest_retirement_batch_path)[:12]`. See `Project_Roadmap_v1.md` §7.1.

---

## Next action

**Purpose.** Section 02 names the next Claude/Codex-executable frontier. Completion history lives in `Recently completed`; the full remaining itemization lives in `Remaining forward work` (next section).

**Frontier.** The R-FS-1 full-spec build umbrella (operator directive 2026-06-12) remains the single **ACTIVE** program. Its **FROZEN order is COMPLETE — all 11 ordered child arcs landed** (B1…M). R-FS-1 stays ACTIVE because the **standalone `B-*` build arcs** (surfaced during the frozen builds; full-spec, nothing-deferred) remain — they are the next-action frontier; the quality track stays gated behind R-FS-1's resolution.

**#784 (this refresh) — CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD` (CP v1.81→v1.82 + runtime change-note v1.87→v1.88, CP + runtime) as the RECONCILER slice — the FINAL durable engine class; WHOLE close (net −1).** A maybe-ran fan-out / orchestrator `SUB_AGENT_DISPATCH` whose child is a `SINGLE_THREADED_LINEAR` `RECONCILER_LOOP` leaf now RECOVERS by re-dispatch — completing the maybe-ran-subagent recoverability family across ALL FOUR durable engine classes {ESR,WAL,SAVE_POINT,RECONCILER} (`PURE_PATTERN` the lone non-member). Mechanism: `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES` += RECONCILER_LOOP in both mirror predicates; worker + orchestrator share the predicate. GROUNDING (advisor, BEFORE substantive work) resolved the open F-1 at-most-once probe → option (a): a re-dispatched RECONCILER child runs its OWN crash-resume, the U-CP-97 reconverge gates at the CAS revision claim UPSTREAM of the step loop — clean re-claim → committed prefix auto-resumes + final_state reconstructs; the F-1 won-claim-then-crashed window → `ABORT_REVALIDATION_FAILED` → child FAILED before any step re-executes → parent fold raises `SubAgentChildFailedError` (fail-closed, never a SUCCESS aggregate). The F-1 ABORT→§22.1-HITL is the already-on-main accepted RECONCILER-resume posture; the F-1 engine-lock auto-recovery is separate/pre-existing/broader, NOT a prerequisite → WHOLE close. **Cross-engine-class swap guard (out-of-family Codex [P1], absorbed in-arc — CP v1.82 §3):** the widened set + engine-class-agnostic `compose_child_run_id_seed` meant a same-step_id RECONCILER→SAVE_POINT swap would route the child through a different recovery mechanism, bypassing the CAS protection → FIX: a THIRD identity leg (marker child engine == resumed child engine, fail-closed) in the worker + orchestrator gates, backed by an additive non-attested `child_engine_class` marker field + 2 readers; RED-without-fix witnesses at both surfaces. **Register: 63 closed / 0 gated / 4 resolved / 3 forward** (G1.1 = 3+0; R-FS-1 ACTIVE). Decorrelated: advisor full-transcript (option-(a) GO + two SAVE_POINT-didn't-need verifications + pre-done) + out-of-family Codex (`just codex-review` --base, gpt-5.5 $0 OAuth: "implementation + tests coherent", zero code findings; sole [P2] = this dashboard refresh). harness-cp 1434 / harness-runtime 2156 / 22 skipped non-e2e green; pyright 0/0/0; ruff; CI all 15 blocking green. **NEXT = BUILD one of the 3 registered forward arcs:** the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator — real non-mocked recursive-child harness, heavy); `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). *(The #782 + prior blocks below are preserved verbatim; their counts are pre-#784 historical.)* —

**#782 — CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-CHILD` (CP v1.80→v1.81 + runtime change-note v1.86→v1.87, CP + runtime) as the SAVE_POINT slice of the registered maybe-ran SUB_AGENT re-dispatch recoverability extension; close 1 + register 1 (net-zero).** A maybe-ran fan-out / orchestrator `SUB_AGENT_DISPATCH` whose child is a `SINGLE_THREADED_LINEAR` `SAVE_POINT_CHECKPOINT` leaf now RECOVERS by re-dispatch (the child auto-resumes from its durable store under the deterministic child run_id) instead of failing closed. Mechanism: extend the DEDICATED `_SUBAGENT_RECOVERABLE_CHILD_ENGINE_CLASSES` conjunct-1 set `{ESR,WAL}` → `{ESR,WAL,SAVE_POINT}` in BOTH mirror predicates (carrier-segregated from `_FANOUT_REPLAY_ENGINE_CLASSES`); worker + orchestrator share the predicate. GROUNDING (advisor) resolved the at-most-once probe: SAVE_POINT computes `resume_at` via the engine-class-agnostic F2-prefix join (`_determine_resume_at`, its reference impl) → committed prefix auto-resumed, not re-fired; fires no inter-step rehydrate / recovery loop → no CAS lease, no F-1 window (the cleanest auto-resume). RECONCILER stays OUT (its reconverge's F-1 won-CAS-claim-retry-ABORTs-to-HITL window) → registered `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-RECONCILER-CHILD`. Witnesses (RED-without-fix, source-revert confirmed): predicate flip SAVE_POINT→True + RECONCILER→False control + verdict-locked agreement witness; EFFECT-level recursive-child SAVE_POINT reconstruct (full final_state + only step-2 re-dispatched); CP full-chain parent-driven SAVE_POINT-child RECOVERS + RECONCILER negative-control fail-closed. **Register: 62 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0; R-FS-1 ACTIVE). Decorrelated: advisor full-transcript (decompose GO + five sharpenings, all honored) + out-of-family Codex (hit the verdict-streaming capture limitation in non-interactive mode; recorded honestly — diff rests on advisor + witnesses + green suites). harness-cp 1427 / harness-runtime 2152 non-e2e green; CI all 15 blocking green. **NEXT = BUILD one of the 4 registered forward arcs** (the RECONCILER F-1 leaf — probe ABORT→HITL-vs-lock-arc at open; the 2 `…-NONLEAF-CHILD` deeper-recursion legs; `…-ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated). *(The #781 + prior blocks below are preserved verbatim; their counts are pre-#782 historical.)* —

**#781 — CLOSED `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` (CP v1.79→v1.80, CP-ONLY) as the RECONCILER slice — COMPLETING the final_state-reconstruction family across {ESR, WAL, SAVE_POINT, RECONCILER}; close 1 + register 1 (net-zero).** A crash-resumed SINGLE_THREADED_LINEAR `RECONCILER_LOOP` run (child OR top-level) over a committed prefix now reconstructs its COMPLETE `final_state`. GROUNDING resolved the registered two-authorities probe in favor of store-reuse: the U-RT-123 reconciler substrate persists a `StateSummary` CONVERGENCE DIGEST for its CAS-lease, NOT the per-step output map → no competing output authority; derive non-viable. A real RECONCILER run flows through the SAME linear loop → add RECONCILER to the ONE `_FINAL_STATE_RECONSTRUCT_ENGINE_CLASSES` constant. owner_axis “CP + runtime” → CP-ONLY confirmed. Blocking witness (advisor): producer-half full-chain round-trip through the REAL `execute_workflow` (RED on HEAD) + the CAS-ordering pins (abort → FAILED upstream of the seed; clean → reconstructs). **Register: 61 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0; R-FS-1 ACTIVE). Out-of-family Codex surfaced that the maybe-ran SUB_AGENT recoverability predicates cited THIS arc as their blocker → registered `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-SAVE-POINT-RECONCILER-CHILD` (the RECONCILER F-1 CAS-claim at-most-once window) + corrected both stale docstrings. **NEXT = BUILD one of the 4 registered forward arcs** (the recoverability extension this arc unblocked is the value-rank candidate; the 2 `…-NONLEAF-CHILD` deeper-recursion legs; `…-ORCHESTRATOR-PROCEED-RESIDUAL` is operator-gated). harness-cp 1426 / harness-runtime 2142 non-e2e green; CI all 15 blocking green. *(The #779 + prior blocks below are preserved verbatim; their counts are pre-#781 historical.)* —

**#779 (prior) — CLOSED `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT` (CP v1.78→v1.79, CP-ONLY) as the SAVE_POINT slice of the registered SAVE-POINT-RECONCILER arc — a net-zero close (close 1 + register 1).** A crash-resumed SINGLE_THREADED_LINEAR `SAVE_POINT_CHECKPOINT` run (child OR top-level) over a committed prefix now reconstructs its COMPLETE `final_state` instead of a silently suffix-only one. **GROUNDING overturned the registered "needs an entirely NEW output substrate" anticipation:** the `EngineOutputStore` is mechanically engine-class-AGNOSTIC (binds on `engine_output_replay`, not engine class) + a real SAVE_POINT run flows through the SAME linear dispatch loop as ESR/WAL → the close is a CP-side gate extension reusing the class-agnostic store (the producer + seed gates unified onto ONE `_FINAL_STATE_RECONSTRUCT_ENGINE_CLASSES` constant — "never record-only" structural). **Blocking witness (advisor):** a producer-half full-chain SAVE_POINT round-trip through the REAL `execute_workflow` (NOT hand-seeded) — RED on HEAD (phase 1 recorded nothing) → GREEN with the gate extensions. LINEAR-scoped (mirrors v1.75). Decorrelated: advisor full-transcript (Reading A + the blocking producer-half witness criterion + the RECONCILER two-authorities split + the pre-done SAVE_POINT-vs-RECONCILER discriminator) + out-of-family Codex ("the code changes appear coherent" — no code finding; one [P2] = the §12.2.1 roadmap-status process-artifact, this refresh). harness-cp 1423 / harness-runtime 2142 non-e2e green; pyright 0/0/0; ruff; overlay 330/31; CI all 15 blocking green (incl. Codex anti-rot after a gh-down dashboard regen). **DECOMPOSE — close 1 + register 1** (`B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` — RECONCILER is ENGINE-OWNS-SUBSTRATE, store-reuse-vs-derive needs its own grounding) → `standalone_registered` 4 → 4 net-zero. **Register (current): 60 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0; R-FS-1 ACTIVE). **NEXT = BUILD one of the 4 registered forward arcs:** `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` (completes the final_state-reconstruction family — probe store-reuse-vs-derive-from-the-reconciler-substrate at open) · the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator, real non-mocked recursive-child harness — heavy) · `…-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). *(The #777 + #768 prose below is preserved verbatim; its counts are pre-#779 historical.)* —

**#777 (prior) — CLOSED `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT` (CP v1.77→v1.78 + runtime v1.85→v1.86) as the LINEAR-`{ESR,WAL}`-leaf orchestrator slice — the ORCHESTRATOR analogue of the #774 worker arc, a net-zero close (close 1 + register 1).** A maybe-ran fan-out `SUB_AGENT_DISPATCH` orchestrator (`steps[0]`, ORCHESTRATOR_WORKERS / HIERARCHICAL_DELEGATION) on a strict-tier crash-resume now RECOVERS by re-dispatch (the whole fan-out re-runs fresh → the orchestrator re-dispatches → its child auto-resumes under the deterministic child run_id) instead of failing the run closed. Mechanism REUSES the worker predicate/seed/dual-gate verbatim: the `is_orchestrator_dispatch` hash-inert flag extends the deterministic `child_run_id_seed` to the orchestrator dispatch (`branch_path=None`, distinct from worker seeds, no leak to loop iterations) + `record_orchestrator_dispatched(child_recoverable=...)` + the SUB_AGENT recovery disjunct ([P1-b] dual gate + same-step_id). Selection per advisor's convergence-value criterion (unblock/avoid-spawn over freshness). Decorrelated: advisor (sound; closed the HIERARCHICAL parity gap + named the half-proof composition) + out-of-family Codex (2 genuine [P2] additive-store-boundary bugs — direct reader call → AttributeError + always-pass-kwarg → TypeError on pre-v1.86 stores — both fixed structurally [getattr + only-pass-when-not-None] with regression witnesses; a 3rd [P2] = roadmap.html regen, done). harness-cp 1422 / harness-runtime 2149 green; CI all 15 blocking green. **Register (current): 59 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0; R-FS-1 ACTIVE). **NEXT = BUILD `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT-RECONCILER`** — advisor's convergence-value pick (the foundational output-substrate unblock: removes the result-fidelity blocker for SAVE_POINT/RECONCILER children across worker-SUBAGENT + orchestrator-SUBAGENT + top-level; design-heaviest, needs a fresh context). The other 3 forward: the 2 `…-NONLEAF-CHILD` deeper-recursion legs (worker + orchestrator) · `ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated — C10⊥C11 council + AUQ at open). *(The #768 prose below is preserved verbatim; its counts are pre-#770..#777 historical.)* —

**#768 (prior) — CLOSED `B-TOP-LEVEL-CRASH-RESUME-FINAL-STATE-RECONSTRUCT` (CP v1.75→v1.76) as the GENERAL top-level reconstruction (framing B), DECIDED AT OPEN as impl-not-fork — a WHOLE close.** v1.75 fixed only the CHILD case (default-off for top-level); a top-level resume over a committed prefix returned a SUFFIX-ONLY `final_state` (status SUCCESS, silently truncated output — a result-fidelity bug). The v1.75 §2 fork question ("is top-level resume transparency a committed invariant?") was answered impl-not-fork by three decorrelated signals: (i) the §25.2 insurance read at SOURCE confirmed the spec is SILENT (no resume-transparency invariant); (ii) the v1.75 "candidate fork"/punt were deferral-to-grounding + the flag-isolation control was v1.75's own punt-voice; (iii) the **decisive empirical probe** — flip the `execute_workflow` `reconstruct_final_state` default `False→True`, run the FULL suites, sweep ALL consumers → broke EXACTLY ONE test (that control), CP 1395/1 + runtime 2134/0 (incl. live-e2e) → no real consumer relies on suffix-only. FIX (CP-only default flip): the top-level `mcp_server.py:427` handler takes the new default → reconstructs with NO runtime src edit; CP v1.76 §1 DEFINES the resume-transparency invariant (store↔ledger skew now FAILS closed at the top-level path; SAVE_POINT/RECONCILER degrade → the registered follow-on, scope extended to cover top-level; explicit `=False` opt-out). NO committed-invariant sacrifice; at-most-once PRESERVED. Decorrelated: advisor full-transcript (pick + value-requires-default-on + a reconcile overturning a provisional fork-lean → the empirical probe + 3 accounting fixes) + out-of-family Codex (no code defect). **Register (current): 57 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0; `standalone_registered` 5 → 4). **NEXT = BUILD one of the 4 forward arcs:** the 2 recursive-child SUBAGENT legs (UNBLOCKED-for-ESR/WAL but needing a REAL non-mocked recursive-child harness — heavy; reverted at #746) · `SAVE-POINT-RECONCILER` (output substrate, now covers child + top-level — larger lift) · `ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated). *(The #766 prose below is preserved verbatim; its counts are pre-#768 historical.)* —

**#766 (prior) — CLOSED `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT` (CP v1.74→v1.75) as the CHILD-SCOPED, SINGLE_THREADED_LINEAR ESR/WAL slice (a DECOMPOSE).** On a durable-engine-class child crash-resume `execute_workflow` returned a SUFFIX-ONLY `final_state` (loop starts at `resume_at` with `accumulated` empty; the committed prefix is skipped + never seeded) → the parent fan-out / B-HIERARCHICAL-PAUSE (#680) fold silently corrupted the parent aggregate (finding-v2's by-execution witness). FIX (child-scoped framing A, additive — advisor forced the discriminating grep that found NO committed result-fidelity / resume-transparency invariant → impl-against-the-silent-gap, NOT a fork): an additive opt-in `reconstruct_final_state` (only `child_workflow_runner` passes True; top-level `api.run` default False → byte-unchanged) seeds the durable committed prefix `[0, resume_at)` into `accumulated` before the loop via the shared `_read_durable_replay_prefix` (fail-closed on store↔ledger skew; config-flip degrades to suffix-only). Both resume paths reach the same path-agnostic seeding site → ALSO closes the pre-existing #680 suffix-only fold for LINEAR ESR/WAL children. `final_state` + DRAINED `partial_state` reconstruct; FAN-OUT children → B-FANOUT-OUTPUT-REPLAY; SAVE_POINT/RECONCILER → no store → registered follow-on. NO committed-invariant change; at-most-once PRESERVED. **DECOMPOSE — close 1 + register 2** (`B-CHILD-...-SAVE-POINT-RECONCILER` + the fork-bearing `B-TOP-LEVEL-...`) → the 2 SUBAGENT arcs UNBLOCKED-for-LINEAR-ESR/WAL. Decorrelated: advisor full-transcript (A-vs-B framing + pre-done caught the untested partial_state + the unwitnessed #680, both closed) + out-of-family Codex (clean). **Register (current): 56 closed / 0 gated / 4 resolved / 5 forward** (G1.1 = 5+0; `standalone_registered` 4 → 5). **NEXT = BUILD one of the 5 forward arcs:** the 2 recursive-child SUBAGENT legs (now unblocked-for-LINEAR-ESR/WAL but needing a REAL non-mocked recursive-child harness — heavy; reverted at #746) · the NEW `SAVE-POINT-RECONCILER` (needs an output substrate — larger lift) · `TOP-LEVEL` (fork-bearing — ground fork-vs-impl at open) · `ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated). *(The #764 prose below is preserved verbatim; its counts are pre-#766 historical.)* —

**#764 (prior) — STOP disposition for `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` (NO code change; finding v2).** A by-execution witness over the real `execute_workflow` proved option (a) (the finding-v1 LINEAR narrowing) is ALSO unsound: a durable-engine-class resume over an F2-committed prefix returns a silently suffix-only `final_state` (status SUCCESS), which a re-dispatched maybe-ran child folds corrupted into the parent (`sub_agent_dispatch.py:660-663`) — NOT fan-out-specific. Disposition reverts to option (b) (stay fail-closed, the current correct behavior). REGISTERED the genuine prerequisite **`B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT`** (`standalone_registered` 3 → 4) + repointed both SUBAGENT arcs' blocker at it + landed the #746 session's untracked analysis (which had already found this) + surfaced the pre-existing B-HIERARCHICAL-PAUSE (#680) suffix-only latent defect (same fix). **NEXT = `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT`** — the unblocker for the 2 SUBAGENT arcs; ground the child-scoped (additive, no gate) vs general top-level-resume (changes the accepted suffix-only semantic → likely a fork) framing at open (advisor: deep-arc, fresh context). The other 2 forward arcs: `ORCHESTRATOR-MAYBE-RAN-SUBAGENT` (same blocker) + `ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated). **Register (current): 55 closed / 0 gated / 4 resolved / 4 forward** (G1.1 = 4+0). *(The #762 lineage prose below is preserved verbatim; its counts are pre-#764 historical.)* —

**#762 CLOSED `B-FANOUT-EFFECT-FENCE-SCOPED-ABORT-CRASH-DURABLE` (CP v1.73→v1.74 + runtime v1.83→v1.84) as a WHOLE close** — crash-DURABLE persistence of the scoped-abort disposition (the v1.73 §2 residual). v1.73 reused the `completed`/no-output store terminal for an `ABORT_BRANCH` branch → indistinguishable from a ran-and-errored branch on crash-resume → an all-scoped-abort resume that CRASHED after the captures reconstructed PAUSED→PARTIAL, not the in-resume all-abort FAILED (status-fidelity only; NO double-fire). FIX (replay-store side — the disposition-vocabulary decision answered: NOT a new IS terminal_status → NO §5.2 IS-hash arc): the durable store records a scoped-abort branch under the DISTINCT disposition `scoped_aborted` (the IS-hash-bearing F2 ledger stays `completed`); the store read-back guard accepts it additively + rejects a malformed `scoped_aborted`+output record (out-of-family Codex [P2]); CP crash-resume reconstructs `_scoped_abort_ordinals` from recovered terminals at both fan-out sites → the existing all-abort guard reproduces FAILED (all-abort) / PARTIAL (mixed survivor). Witnessed by GENUINE producer→crash→consumer chains at both topologies (each RED-without-fix). Decorrelated: advisor full-transcript + out-of-family Codex (1 round, [P2] fixed). **Register (current): 55 closed / 0 gated / 4 resolved / 3 forward** (G1.1 = 3+0; net decrement — WHOLE close, registers nothing). The 3 forward: `MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT` (recursive-child, heavy) + `ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated). **NEXT = BUILD one of the 3 forward arcs** (the lineage prose below is preserved verbatim; its pre-#762 counts are historical). — **Three final-closure standalone arcs have now LANDED: `B-HITL-PLACEMENT-PER-STEP-LOOSEN` (#713), `B-POSTJOIN-LLM-SYNTHESIS` (#715), `B-TAIL-CONDITIONAL-SAMPLING` (#717) — and #717 closed the LAST `gated` arc.** Each was design-first-vetted (council → ratify → spec/impl → adversarial review + Codex + advisor → PR + §12.2 refresh); B-TAIL was a pure impl-to-cleared-spec enforcement of the §9.2 conditional sampling at the tail-keep processor (the R-420/R-421 dependency gates both resolved), hardened over 3 out-of-family Codex rounds (2 genuine §14.3 subagent cross-contract catches; round 3 = the known §12.2.1 process-artifact). **The narrowed §25.12-D1 gate is now CLEARED — operator RATIFIED A (#721, AUQ this `/loop continue` iteration): the second fan-out recovery authority is a NON-attested, branch-index-KEYED sidecar** (the council recommendation, precedent-consistent with the shipped `EngineOutputStore` #665 — commits completion-order, reads by `branch_index`, store↔ledger skew fail-closed, NOT in the §6 hash chain → §25.12-D1 preserved LITERALLY; the synthesis output gets a record-local capture-time self-hash either way). **Nine fan-out crash-resume family arcs are now CLOSED: `B-FANOUT-OUTPUT-REPLAY` (PR1 #723 + PR2 #724) + `B-FANOUT-CRASH-RESUME-CASCADE-POLICY` (#726) + `B-FANOUT-PAUSE-SYNTHESIS` (#728) + `B-FANOUT-RESUME-CARRIER-TOPOLOGY-MISMATCH` (#730) + `B-FANOUT-CRASH-RESUME-STRICT-TIER-INCOMPLETE` (#732) + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-DISPATCH` (#734) + `B-FANOUT-CRASH-RESUME-MAYBE-RAN-RESOLUTION` (#736) + `B-FANOUT-CRASH-RESUME-TIMEOUT-REPLAY` (#738) + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-RESOLUTION` (#740).** #740 closes the orchestrator-maybe-ran fail-closed (#734) as the **re-fire-safe slice** — the orchestrator analogue of the #736 worker re-fire-safety split: the orchestrator dispatched-marker records its DISPATCH-TIME kind (CP v1.63→v1.64 + runtime v1.80→v1.81), and a re-fire-safe orchestrator (`{DECLARATIVE_STEP, INFERENCE_STEP}`) re-dispatches fresh **only in the proven-pristine window** (a DEFAULT-DENY allowlist tied to the `_execute_orchestrator_workers` writer artifact list — adopted after 4 out-of-family Codex rounds each found a fresh at-most-once double-fire edge of an enumerated denylist; advisor-directed structural fix; R5 converged code-clean). Grounding refined the registered "route to a §26.2 pause" into "per-kind re-dispatch"; **closes the re-fire-safe slice + SPLITS off two narrower follow-ons** (a split, NOT a defer) → `standalone_registered` **3 → 4** (a net increase — honest FULL-SPEC accounting; grounding revealed the arc is genuinely three sub-problems). At-most-once PRESERVED; NO new contract/enum/committed-invariant change. #742 CLOSED `B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING` as the TOOL_STEP slice (CP v1.64→v1.65) → closes 1 + registers 3 → `standalone_registered` 4 → 6. **#744 (this refresh) CLOSED `B-FANOUT-EFFECT-FENCE-PER-BRANCH-RESOLUTION` as the per-branch-DISTINCT resolution map** (an additive `ResumeContext.effect_fence_resolutions` key→resolution map + `effect_fence_resolution_for` resolver consumed per-branch at the two fan-out resume sites → two branches resolve DIFFERENTLY [SKIP vs RE_FIRE] in one resume; ADDITIVE not a replacement, single field PRESERVED as the uniform default; CP v1.65→v1.66, CP-ONLY; out-of-family Codex [P1] run-level-ABORT guard fixed) → closes 1 + registers 1 (`B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT`) → `standalone_registered` **6 → 6 (net-zero)**. **R-FS-1 does NOT resolve.** Under FULL-SPEC the remaining fan-out capabilities are genuine build arcs, **registered (5)**: (1) **`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT`** (the maybe-ran SUB_AGENT_DISPATCH worker, fenced only at the CHILD's tool sinks → recursive child crash-resume, a larger mechanism); (2) **`B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL` ✅ CLOSED #748** (the §14.22 fence EXTENDED to the §14.20 MANAGED_AGENTS vendor-session sink + the CP fence-recoverable classifier + a same-kind guard — a WHOLE close; the 5 STILL-registered are items (1)(3)(4)(5)(6)); (3) **`B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT`** (NEW #744 — per-branch-DISTINCT resolution landed for SKIP/RE_FIRE, but ABORT stays run-level-terminal; per-branch-SCOPED abort [fail one branch, let siblings resolve] is a new disposition semantic, operator-gated-decided-at-open); (4) **`B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT`** (reconstruct a lost PAUSE snapshot honoring §25.15.1 finish-in-flight); (5) **`B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING`** (split from #740 — the effect-bearing-orchestrator §26.2 pause-route, needs a new resume→orchestrator application site); (6) **`B-FANOUT-CRASH-RESUME-ORCHESTRATOR-PROCEED-RESIDUAL`** (split from #740, **operator-gated** — the PROCEED committed-tier-semantic change, C10⊥C11 dyadic council + AUQ at open). `closure_gate.py` G1.1 = `standalone_registered + standalone_gated == 0` is now **5+0** (R-FS-1 ACTIVE). **NEXT = BUILD one of the 5 registered B-FANOUT follow-ons** in a fresh-context window. **`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` (drain #2) was ATTEMPTED at #746 and found unsound as the "leaf-fenced single-level slice" (Codex 2×[P1]: recursive RESULT-fidelity + resumed-child-fence) → REVERTED, stays registered, re-scoped** (see the git_head narrative + `.harness/b-fanout-crash-resume-maybe-ran-subagent-design-finding-v1.md`). Its next attempt is **option (a)** — narrow to `topology==SINGLE_THREADED_LINEAR` child + a recorded-original AND resumed-child fence-active gate + a REAL (non-mocked) recursive-child crash-resume witness (none exists today) — a heavier lift than a thin slice. **Drain #3 `B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL` is now CLOSED (#748, WHOLE close); `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT` ✅ CLOSED (CP v1.68) as the COMPLETE-recovery slice — closes 1 + registers 2 (the incomplete case → NOT-YET-DISPATCHED + MAYBE-RAN) → standalone_registered 5 → 6.** The lighter non-operator-gated forward item is now ORCHESTRATOR-EFFECT-BEARING; SUBAGENT needs a real non-mocked recursive-child harness (heavy); SCOPED-ABORT + PROCEED-RESIDUAL are operator-gated (council last); the 2 new PAUSE-RECONSTRUCT splits round out the register. On all six closed/resolved → G1.1 passes → R-FS-1 resolves → the R-CL-Q1 whole-harness quality track admits. Per-arc rationale + the forward register: `.harness/beyond-mvp-capability-boundary-ledger.md`; the complete enumeration: `.harness/arc-ledger.yaml`. **Register (current): 52 closed / 0 gated / 4 resolved / 5 forward** (G1.1 = 5+0; #754 closed `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-NOT-YET-DISPATCHED` as a WHOLE close — the v1.68 §2 incomplete-recovery PAUSE fail-closed LIFTED for the provably-not-run window [re-pause-WITHOUT-dispatch: skip the not-yet-dispatched ordinals → empty branch_plan → the v1.68 reconstruct re-establishes PAUSED omitting them; the gate is STRICTER than the §25.15 incomplete leg — `_instrumented ∧ ¬_maybe_ran`], CP v1.69→v1.70, CP-only, registers nothing → net decrement `forward` 6 → 5; the 5 still-registered: `MAYBE-RAN-SUBAGENT` + `EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT` + `ORCHESTRATOR-MAYBE-RAN-SUBAGENT` + `ORCHESTRATOR-PROCEED-RESIDUAL` [operator-gated] + `PAUSE-RECONSTRUCT-MAYBE-RAN`). The **lightest non-operator-gated next item is the NEW `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT-MAYBE-RAN-FENCE-STEP-ID`** (the fence-recoverable TOOL_STEP/MANAGED_AGENTS case split from #756 by Codex [P1] — needs a snapshot step_id carrier mirroring the orchestrator's `OrchestratorEffectFencePausedResumeState`); the worker MAYBE-RAN-SUBAGENT + ORCHESTRATOR-MAYBE-RAN-SUBAGENT need a real non-mocked recursive-child harness (heavy). The historical `48/0/4/5` + `49/0/4/6` + `50/0/4/6` counts in this section's lineage prose are the state at #748/#750/#752, preserved verbatim. **Resolved-as-invalid (not re-openable without an operator ADR/spec request):** `B-RESUME-CONTEXT-SURVIVES-RETRY` (#695 — re-ask-per-retry is COMMITTED+TESTED per §14.8.8.7 invariant 3). The complete arc + unit + status enumeration is the single source **`.harness/arc-ledger.yaml`** (rendered in the dashboard's Arc & unit map); per-arc rationale + the forward register live in `.harness/beyond-mvp-capability-boundary-ledger.md`.

**Recurring lanes** run on cadence alongside: `R-600-*` (pattern bake-in, codex out-of-family review), `R-IF-roadmap-refresh`.

**Do not re-open as next action.** Closed/parked: `R-411/412/420/421/430`, `R-500`, `R-810/820/830`, `R-008`/OD-4, `R-CXA-1/2/4`, `R-100-*`, `R-300-*` (both routing-activation #213 and second-provider #281/#283 RESOLVED), `R-901` (research frontier closed). `R-CL-Q1→C1` (quality track) **stays gated until the final two arcs land** — the operator chose full-spec closure (2026-06-22): build BOTH `B-HITL-PLACEMENT-PER-STEP-LOOSEN` + `B-POSTJOIN-LLM-SYNTHESIS` via the §02 design-first pipeline (`.harness/r-fs-1-final-closure-plan.md`), PLUS dispose the lone `gated` `B-TAIL-CONDITIONAL-SAMPLING` (the `closure_gate.py` G1.1 = registered+gated==0). On all three → R-FS-1 resolves → R-CL-Q1 admits + runs the 5-dimension quality pass on the complete full-spec harness.

---

## Remaining forward work

**The full-spec build (R-FS-1).** All remaining forward build work is child arcs of the single ACTIVE umbrella **R-FS-1** (operator FULL-SPEC directive 2026-06-12 — nothing deferred; every capability built beyond MVP).

**Single arc→unit source.** The full itemization — every arc + unit in plain language, with status, build position, dependencies, and as-built units — lives in the single structured source **`.harness/arc-ledger.yaml`** (derived by `tools/arc_ledger.py`, rendered in the dashboard's Arc & unit map; the forward `B-*` register is shown with decompose-at-open markers). The spine ledger `.harness/beyond-mvp-capability-boundary-ledger.md` carries every surfaced boundary's rationale; per-arc grounding leads at `.harness/r-fs-1-remaining-arcs-grounding-sweep-v1.md` (re-ground at arc-open, presence-not-correctness).

**Frozen order** (B1→B3→E→B2→R→B4→CA→B5→B6→B7→M) — **COMPLETE, 11/11.** R-FS-1's standalone `B-*` register is **52 closed / 5 forward / 0 gated / 4 resolved** (after PR #756 — closed 1 + registered 1, net-zero `forward` 5; the dashboard Arc & unit map is the live source). The fan-out crash-resume family is being built out under FULL-SPEC; CLOSED so far: `B-FANOUT-OUTPUT-REPLAY` (#723+#724) + `B-FANOUT-CRASH-RESUME-CASCADE-POLICY` (#726) + `B-FANOUT-PAUSE-SYNTHESIS` (#728) + `B-FANOUT-RESUME-CARRIER-TOPOLOGY-MISMATCH` (#730) + `B-FANOUT-CRASH-RESUME-STRICT-TIER-INCOMPLETE` (#732) + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-DISPATCH` (#734) + `B-FANOUT-CRASH-RESUME-MAYBE-RAN-RESOLUTION` (#736) + `B-FANOUT-CRASH-RESUME-TIMEOUT-REPLAY` (#738) + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-RESOLUTION` (#740) + `B-FANOUT-CRASH-RESUME-MAYBE-RAN-EFFECT-BEARING` (#742, the TOOL_STEP slice) + `B-FANOUT-EFFECT-FENCE-PER-BRANCH-RESOLUTION` (#744, the per-branch-DISTINCT resolution map — closed + split off the ABORT-scoping follow-on). **FIVE standalone arcs now block `closure_gate.py` G1.1** (`standalone_registered + standalone_gated == 0`, now **5+0**): the **5 forward** follow-ons — `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` + `B-FANOUT-EFFECT-FENCE-PER-BRANCH-SCOPED-ABORT` + `B-FANOUT-CRASH-RESUME-PAUSE-RECONSTRUCT` + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-EFFECT-BEARING` + `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-PROCEED-RESIDUAL` (operator-gated). `B-FANOUT-CRASH-RESUME-MAYBE-RAN-UNFENCED-EXTERNAL` CLOSED at #748. The complete enumeration + status is the single source `.harness/arc-ledger.yaml` (dashboard Arc & unit map). The **`R-CL-Q1` quality track admits once all five follow-ons land/resolve** (R-FS-1 then resolves) — it runs the 5-dimension quality pass on the complete full-spec harness.

---

## In-flight (open PRs)

| PR | Branch | R-NNN | Posture |
|---|---|---|---|
| *(none)* | — | — | No open PRs at refresh time. |

---

## Recently completed (last 5)

| R-NNN / PR | Closed at | Notes |
|---|---|---|
| PR #781 | 2026-06-27 | **R-FS-1 `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` BUILT + MERGED → arc CLOSED as the RECONCILER slice, COMPLETING the final_state-reconstruction family across {ESR,WAL,SAVE_POINT,RECONCILER} (CP v1.79→v1.80, CP-ONLY).** Two-authorities probe resolved (the reconciler substrate carries a convergence DIGEST, not the per-step output map → store-reuse, no second authority). close 1 + register 1 net-zero (Codex surfaced the maybe-ran SUB_AGENT recoverability extension → registered). 61/0/4/4. |
| PR #779 | 2026-06-27 | **R-FS-1 `B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-SAVE-POINT` BUILT + MERGED → arc CLOSED as the SAVE_POINT slice of the registered SAVE-POINT-RECONCILER arc** (bundled-absorption: CP spec v1.78→v1.79 + 1 clearance marker + harness-cp/src + a harness-runtime docstring clarification + tests + arc-open grounding doc + arc-ledger + dashboard regen; **CP-ONLY, NO runtime SPEC delta, NO runtime src behavior change**). A crash-resumed `SINGLE_THREADED_LINEAR` `SAVE_POINT_CHECKPOINT` run (child OR top-level) over a committed prefix now reconstructs its COMPLETE `final_state` instead of a silently SUFFIX-ONLY one (status SUCCESS that truncated output to the post-resume steps — a result-fidelity bug; a child fold corrupted the parent aggregate). **GROUNDING overturned the registered "needs an entirely NEW output substrate" anticipation:** the `EngineOutputStore` is mechanically engine-class-AGNOSTIC (per-run JSONL keyed by `run_idempotency_key`; the stage-5 binding is gated ONLY on `RuntimeConfig.engine_output_replay`, never engine class) and a real `SAVE_POINT_CHECKPOINT` run flows through the SAME `SINGLE_THREADED_LINEAR` dispatch loop as ESR/WAL (its `resume_at` is the same F2-prefix join) → the close is a CP-side gate extension reusing the class-agnostic store, NOT a new substrate (`[[grounding-reveals-claude-closeable-slice-close-honestly]]`). **Mechanism:** a NEW `_FINAL_STATE_RECONSTRUCT_ENGINE_CLASSES = {ESR, WAL, SAVE_POINT_CHECKPOINT}` constant gates BOTH the `_record_durable_step_output` producer AND the `final_state` seed (the "never record-only / never seed-only" discipline made structural — SAVE_POINT's journal is consumed by the seed, its only consumer, since it has no cached-output-replay channel rehydrate). LINEAR-scoped (both gates in the linear loop; non-linear strategies return before the seed), mirroring the v1.75 child-scoped narrowing. NO contract/closed-enum/committed-invariant/new-fail-class change; at-most-once PRESERVED (pure store READ); NO §5.2 IS-hash arc. **Blocking witness (advisor — the honest-vs-vacuous gate):** `test_reconstruct_final_state_round_trips_through_store_save_point` — a producer→crash→consumer full-chain round-trip through the REAL `execute_workflow` (phase 1 fresh SAVE_POINT run RECORDS via the real producer [asserted store holds {0,1,2}], phase 2 resume RECONSTRUCTS {step-0,step-1,step-2}); **RED before the gate extension** (phase 1 recorded NOTHING — the producer excluded SAVE_POINT). Plus `test_reconstruct_final_state_reconciler_out_of_scope_degrades` (RECONCILER stays suffix-only, reframed from the old SAVE_POINT-degrade test) + the preserved v1.75/v1.76 ESR/WAL witnesses. Full suites green: harness-cp **1423** passed / 1 xfailed; harness-runtime **2142** passed / 10 skipped (non-e2e); pyright 0/0/0; ruff; overlay 330 nodes / 31 seams; CI all 15 blocking green (incl. X-AL-3 back-flow + the Codex anti-rot gate — passed after a gh-down dashboard regen for CI parity, the only iteration). **Decorrelated: advisor full-transcript** (Reading A over the new-substrate anticipation + the BLOCKING producer-half witness criterion + the RECONCILER two-authorities split + the pre-done SAVE_POINT-vs-RECONCILER discriminator: LangGraph is SAVE_POINT's *reference* substrate not the harness impl path → no competing harness authority, vs RECONCILER's LIVE U-RT-123 substrate) **+ out-of-family Codex** ("the code changes appear coherent" — no code finding; its one [P2] = the §12.2.1 roadmap-status process-artifact, refreshed by THIS PR). **DECOMPOSE — close 1 + register 1** (`B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT-RECONCILER` — RECONCILER is ENGINE-OWNS-SUBSTRATE, its reconciler-substrate `partial_state` is the authority, so store-reuse-vs-derive needs its own grounding) → G1.1 = 4+0. arc-ledger tally 59/0/4/4 → **60/0/4/4** (net-zero forward). The `workspace_state_hash`/`git_head` refresh to the merge commit (`c4ee1a8c`) is THIS §12.2.1 terminating-refresh PR. |
| PR #777 | 2026-06-27 | **R-FS-1 `B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT` BUILT + MERGED → arc CLOSED as the LINEAR-`{ESR,WAL}`-leaf-child slice — the ORCHESTRATOR analogue of #774** (bundled-absorption: CP spec v1.77→v1.78 + runtime spec v1.85→v1.86 + 2 clearance markers + harness-cp/src + harness-runtime/src + tests + arc-ledger + dashboard regen). A maybe-ran fan-out `SUB_AGENT_DISPATCH` ORCHESTRATOR (`steps[0]`, ORCHESTRATOR_WORKERS / HIERARCHICAL_DELEGATION) on a strict-tier crash-resume now RECOVERS by re-dispatch (the whole fan-out re-runs fresh → the orchestrator re-dispatches → its child auto-resumes under the deterministic child run_id, result-faithfully) instead of failing the run closed — unlike the fence-recoverable kinds (whose own dispatch reaches a fence), a sub-agent orchestrator's effects live at its CHILD's tool sinks, so recovery is compositional recursive child crash-resume. **Mechanism (REUSES the worker predicate/seed/dual-gate verbatim — no new typed surface):** the `is_orchestrator_dispatch` hash-inert `StepExecutionContext` flag extends the deterministic `child_run_id_seed` to the orchestrator dispatch (`branch_path=None`, distinct from worker `branch_path` seeds, RESET on every fan-out child → no orchestrator↔worker child aliasing; the orchestrator dispatches exactly once so the seed is loop-suppression-safe, unlike the EVALUATOR_OPTIMIZER/RECONCILER_LOOP iterated steps the `branch_index` gate excludes) + `record_orchestrator_dispatched(child_recoverable=...)` marker + `orchestrator_subagent_child_recoverable` reader + the SUB_AGENT recovery disjunct in `_determine_fanout_resume`'s orchestrator maybe-ran block ([P1-b] dispatch∧resumed dual gate + same-step_id guard). **Selection per advisor convergence-value criterion** (rank forward arcs by unblock/avoid-spawn over code-freshness; close-≥-register tripwire grounding-confirmed net-zero). **Decorrelated: advisor full-transcript** (arc sound — pristine-window guard, PROCEED exclusion, branch_path distinctness; flagged + closed the HIERARCHICAL_DELEGATION parity gap [HIERARCHICAL is recursive ORCHESTRATOR_WORKERS → inherits the recovery; parity test added] + named the half-proof composition: the orchestrator child's real-store auto-resume is composed from the seed-wiring test + the worker recursive-child witness [caller-agnostic `ChildWorkflowRunner` → the worker witness's real-store integration transfers] + the full-chain driver tests) **+ out-of-family Codex** (found 2 genuine [P2] additive-store-boundary bugs advisor+I both missed — the decorrelation value: the new recoverability reader called directly → `AttributeError` on a pre-v1.86/partial store + the orchestrator marker writer always passing `child_recoverable=` → `TypeError` on a pre-v1.86 3-arg store; BOTH fixed structurally [`getattr` + only-pass-when-not-None, mirroring the worker idioms] with regression witnesses; a 3rd [P2] = regenerate `roadmap.html` for the ledger bump, done). **Witnesses:** orchestrator seed-wiring through the real dispatcher + orchestrator↔worker seed-distinctness + no-leak-to-loop-iterations regression guard (runtime) + classifier positive recovery / [P1-b] dual gate / same-step_id / negative control + the `_orchestrator_subagent_recoverable` helper getattr tests + the full-chain production-marker recovery/fail-closed pair + the legacy-3arg-store compatibility witness + the HIERARCHICAL parity test (CP). Full suites green: harness-cp **1422** passed / 1 xfailed; harness-runtime **2149** passed / 23 skipped / 1 xfailed; pyright 0/0/0; ruff; overlay 31/31; CI all 15 blocking green (incl. X-AL-3 back-flow + the codex anti-rot gate). NO contract/closed-enum/committed-invariant/new-fail-class change; at-most-once PRESERVED. **close 1 + register 1** (`B-FANOUT-CRASH-RESUME-ORCHESTRATOR-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD`) → G1.1 = 4+0. arc-ledger tally 58/0/4/4 → **59/0/4/4** (net-zero forward). The `workspace_state_hash`/`git_head` refresh to the merge commit is THIS §12.2.1 terminating-refresh PR. |
| PR #774 (+ #775 cleanup) | 2026-06-26 | **R-FS-1 `B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT` BUILT + MERGED → arc CLOSED as the LINEAR-`{ESR,WAL}`-leaf-child slice** (bundled-absorption: CP spec v1.76→v1.77 + runtime spec v1.84→v1.85 + 2 clearance markers + harness-cp/src + harness-runtime/src + tests + arc-ledger + dashboard regen). A maybe-ran fan-out `SUB_AGENT_DISPATCH` worker on a strict-tier crash-resume now RECOVERS by re-dispatch (the deterministic child run_id makes the child's OWN crash-resume auto-resume from the shared durable store, result-faithfully) instead of failing the run closed — unlike a TOOL_STEP (fenced at its own sink) a sub-agent's external effects live at its CHILD's tool sinks, so recovery is compositional recursive child crash-resume. **Mechanism:** E1 (`child_run_id_seed` — deterministic, `branch_path`-folded + fan-out-worker-scoped) + the `subagent_child_recoverable` predicate (`{ESR,WAL}` ∧ SINGLE_THREADED_LINEAR ∧ leaf) + the CP `_fence_unrecoverable_maybe_ran_indices` SUB_AGENT recovery disjunct with the [P1-b] dispatch∧resumed dual gate (the `child_recoverable` marker + a resumed-manifest re-read). **Two corrections over the #746 reverted branch (advisor-vetted):** LINEAR-narrowing (the [P1-a] result-fidelity fix, now sound post B-CHILD-CRASH-RESUME-FINAL-STATE-RECONSTRUCT #764/#766/#768 + the #770 witness) + seed-gating (the deterministic seed scoped to recoverable children so a SAVE_POINT/RECONCILER child never auto-resumes into a suffix-only fold). **Out-of-family Codex caught a REAL [P1]:** the seed `(parent_idempotency_key, child_workflow_id)` collided across fan-out siblings because `compose_branch_child_context` inherits `parent_idempotency_key` VERBATIM from the fan-out parent (the branch-distinct key is composed downstream) → two siblings dispatching the same `child_workflow_id` would alias one child run_id EVEN WITHOUT A CRASH → FIXED by folding §25.16 `branch_path` into the seed + scoping it to fan-out workers, which also forecloses the sequential-loop (EVALUATOR_OPTIMIZER/RECONCILER_LOOP) `step_index`-reuse hazard (empirically confirmed at `workflow_driver.py:2040`). **advisor (transcript-aware) flagged the missing full-chain witness** (component witnesses don't compose) → added (a) seed-resume-stability through a REAL PARALLELIZATION crash-resume (the resume path rebuilds the branch ctx's `(parent_idempotency_key, branch_path)` identically → seed resume-stable by the real driver) + (b) the CP↔runtime predicate-agreement witness (the CP-True/runtime-False double-fire drift foreclosed). **Witnesses:** E1-live full-chain reconstruction; recoverability-predicate negative controls (SAVE_POINT / fan-out / nested fail-closed); seed-gating; fan-out sibling distinctness; classifier dual-gate (10 cases); seed-resume-stability; predicate agreement. Full suites green: harness-cp **1411** passed / 1 xfailed; harness-runtime **2137** passed / 10 skipped; pyright 0/0/0; ruff; CI all blocking green (incl. X-AL-3 back-flow + the codex-closeout anti-rot gate). NO contract/closed-enum/committed-invariant/new-fail-class change; at-most-once PRESERVED. **close 1 + register 1** (`B-FANOUT-CRASH-RESUME-MAYBE-RAN-SUBAGENT-NONLEAF-CHILD` — fan-out-child + nested-sub-agent recursion) → G1.1 = 4+0. arc-ledger tally 57/0/4/4 → **58/0/4/4** (net-zero forward). **#775 cleanup:** PR #774's first impl commit's `git add -A` swept 327 pre-existing UNTRACKED files (`.agents/skills/`, `dashboard-design/output`+`archive` [the Codex [P2] session-metadata-leak logs], `.codex/agents/`, `.claude/agents/`) into the squash merge → `git rm --cached` + `.gitignore` restored them (the 5 tracked workspace skills retained). The `workspace_state_hash`/`git_head` refresh to the merge commits is THIS §12.2.1 terminating-refresh PR. |
| PR #768 | 2026-06-26 | **R-FS-1 `B-TOP-LEVEL-CRASH-RESUME-FINAL-STATE-RECONSTRUCT` BUILT + MERGED → arc CLOSED as the GENERAL top-level reconstruction (framing B), DECIDED AT OPEN as impl-not-fork — a WHOLE close** (bundled-absorption: CP spec v1.75→v1.76 + 1 clearance marker + harness-cp/src + tests + arc-open grounding doc + arc-ledger + dashboard regen; **CP-ONLY, NO runtime SPEC/src delta**). On a durable-engine-class resume over a committed prefix the top-level `run_workflow` handler / `api.run`+`api.resume` returned a SUFFIX-ONLY `final_state` (v1.75 fixed only the CHILD case, default-off for top-level) → a top-level resumed run reported status SUCCESS while silently truncating its output (a silent-corruption result-fidelity bug). **The v1.75 §2 fork question ("is top-level resume transparency a committed invariant?") was answered impl-not-fork by three decorrelated signals:** (i) the **§25.2 insurance read at SOURCE** — CP v1.75 line 11 confirms the spec is SILENT on the resume final_state shape (no final_state output-equivalence / resume-transparency invariant; ADR-D1 §1.1 commits only the INPUT-side channel replay) → no forbidding invariant; (ii) the v1.75 "candidate fork" + deliberate punt were **deferral-to-grounding, NOT evidence-for-fork**, and the flag-isolation control test was v1.75's own punt-voice, not an independent commitment; (iii) the **decisive empirical probe** — flip the `execute_workflow` `reconstruct_final_state` default `False→True`, run the FULL suites + sweep ALL consumers → broke **EXACTLY ONE test** (that flag-isolation control), harness-cp 1395 passed / 1 failed + harness-runtime 2134 passed / 0 failed (incl. the live-e2e `api.run`/`api.resume` integration tests through the real `mcp_server.py:427` path) → **no real consumer relies on suffix-only** → the SAME impl-not-fork classification the child case earned. **FIX (CP-only default flip):** `reconstruct_final_state` default `False→True` on `execute_workflow`/`_execute_workflow_body` (reconstruction is correct; suffix-only was the bug). The top-level `mcp_server.py:427` `run_workflow` handler takes the default → reconstructs with NO runtime src edit (CP-only, mirrors v1.66/v1.73). CP spec v1.76 §1 DEFINES the resume-transparency invariant under C-CP-25 §25.2/§25.6: a resumed run's `final_state`/`partial_state` reconstructs the COMPLETE terminal state for ESR/WAL when the output store is bound; a store↔ledger skew now FAILS closed at the top-level path (a FAILED run rather than a silently-truncated SUCCESS — honest failure over silent truncation); SAVE_POINT/RECONCILER degrade to suffix-only → the registered output-substrate follow-on (scope EXTENDED to cover the top-level case — the shared storage-half blocker means one arc covers both child and top-level); explicit `reconstruct_final_state=False` is the opt-out (no production caller takes it). NO contract removal / closed-enum / committed-invariant sacrifice / new-fail-class change (reuses the existing `engine-output-replay-*` fail-classes); at-most-once PRESERVED (pure store READ — the committed prefix is never re-dispatched). Default-config runs (`engine_output_replay=False`) byte-unchanged. **Witnesses (harness-cp, by-execution):** `test_top_level_default_reconstructs_final_state` (default-on now reconstructs the full {step-0,step-1,step-2} — RED before the flip) + `test_explicit_opt_out_leaves_suffix_only_final_state` (the gated-not-automatic control, reframed from the v1.75 flag-isolation control via explicit opt-out) + the preserved v1.75 reconstruct / store-skew fail-closed / SAVE_POINT-degrade / no-journal-degrade / WAL round-trip / DRAINED partial_state / #680 override-path witnesses (all default-on now, still green). **Coverage residual (with a default-ON blast-radius asymmetry vs v1.75's default-OFF — recorded in the clearance marker, NOT implied parity):** the real-handler-through-`execute_workflow` integration witness stays deferred; the transit (handler → True default) is read-verified at `mcp_server.py:427` (no override) + the full workspace suite is green under the flip. **Full suites green: harness-cp 1397 passed / 1 xfailed; harness-runtime 2134 passed / 23 skipped / 1 xfailed; full workspace 5046 passed** (the lone failure is the documented `AF_UNIX path too long` socket env artifact — daemon socket binding under the long default `$TMPDIR` `tmp_path`; passes with `--basetemp=/tmp/hs` [positive control: 1 passed], skips in provider-free CI → CI pytest green at 56s); pyright 0/0/0; ruff clean; overlay-check 31/31; CI all blocking green (incl. the X-AL-3 back-flow guard — the clearance marker satisfied it). **Decorrelated: advisor full-transcript** (pick-TOP-LEVEL over the heavier SUBAGENT fixture + the value-requires-default-on framing + "ground honestly, don't let the tidier impl reading win"; a **reconcile** overturning a provisional fork-lean as over-caution → prescribing the decisive empirical probe + the §25.2 insurance read; pre-done caught 3 accounting fixes — run the FULL workspace suite [which caught the env-artifact], extend the SAVE-POINT-RECONCILER scope to cover top-level, record the default-on residual asymmetry) **+ out-of-family Codex** (no code defect — "passed the narrow harness-cp tests"; its 2 [P2]s were out-of-scope process artifacts: the §12.2.1 dashboard-vs-roadmap-status lag + a pre-existing untracked `dashboard-design/HANDOFF.md`). **WHOLE close** (registers nothing) → G1.1 = 4+0. arc-ledger tally 56/0/4/5 → **57/0/4/4** (one closed, net decrement `forward`). The `workspace_state_hash`/`git_head` refresh to the merge commit is THIS §12.2.1 terminating-refresh PR. |
---

---

## Outstanding fork docs (47 total — latest additions filed by PR #434)

Sample (highest-leverage open):

| Fork doc | Class | Status |
|---|---|---|
| `class_1_fork_sandbox_tier_no_execution_driver_contract.md` | Class 1 | ✅ APPLIED-AS-BOUNDED-READING-B (status-line refreshed 2026-06-08) — R-410 supplied `ToolExecutionDriver` + local Docker provider; R-411 added gVisor/runsc; R-412 adds managed E2B full-VM. |
| `class_1_fork_tool_step_no_operator_supplied_converter.md` | Class 1 | ✅ APPLIED (PR #171, spec v1.40) — converter config surface landed (Reading B) |
| `class_1_fork_tool_step_no_bootstrap_sandbox_decision_resolver.md` | Class 1 | ✅ APPLIED-AS-READING-B (PR #172, spec v1.41 §14.9.8) — resolver + 5 bootstrap gaps wired; AC #2 final close = operator live e2e |
| `class_1_fork_llm_cost_attribution_not_firing_on_real_dispatch.md` | Class 1 | ❌ RESOLVED-AS-INVALID (PR #168) — test-observation bug, not a defect; cost-attribution fires + writes; OD-5 retirement VALID |
| `class_1_fork_harness_toml_default_discovery_unimplemented.md` | Class 1 | ✅ APPLIED-AS-READING-A (PR #305 status refresh after PR #279 implementation) — CWD `harness.toml` discovery shipped; no longer open. |
| `class_1_fork_pr_2_workflow_layer_ctx_access_recipe_underspecified.md` | Class 1 | ✅ APPLIED-AS-READING-C (PR #107, spec v1.30) |
| `class_1_fork_yaml_loader_step_payload_scalar_coercion_gap.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.39, pyyaml StrictSafeLoader) — R-100-mvp-yaml-loader-shipped verified done at non-live level (41 loader/equivalence tests pass) |
| `class_1_fork_topology_admissibility_check_load_time_vs_runtime_asymmetry.md` | Class 1 | ✅ APPLIED-AS-READING-A (spec v1.38, defer-to-runtime) |
| `class_2_fork_audit_stub_timestamp_universal_fix_plus_per_tier_annotation.md` | Class 2 | ✅ APPLIED-AS-(D) |
| *(complete enumeration at `ls .harness/class_*_fork_*.md`)* | — | — |

**Audit status:** 2026-06-07 cadence pass closed in this PR: sandbox execution-driver fork refreshed from historical PROPOSING; Path (i) durable-async fork leading status token normalized to `**Status:**`; stale roadmap sample row for the already-applied `harness.toml` fork refreshed; no other stale fork-doc headline status lines found. Next cadence: ~5 PRs or operator-discretion.

---

## Phase 7 retirement progress

> **🎓 PHASE-8 GRADUATION + BATCH-56 LIVE LEDGER.** The R-700 reconciliation is CLOSED — the operator ratified **accounting (i)** (PR #246) and lifted the declaration hold. Historical declaration: **RETIRED 46/54 (85.2%) + pipeline-advanced 49/54 (90.7%)**. Batch-52 back-flowed the live R-810/R-820 evidence for AS-8e, AS-8f, and CP-17; batch-53 back-flows the OD-4 runtime residual and the 0-wireable CXA-4 bookkeeping row; batch-54 lands the CP→AS runtime composer; batch-55 records CXA-2 as a counted bounded residual; batch-56 records CXA-1 as substantively retired after the AS→IS producer and edge-scope audit: **live ledger RETIRED 54/54 (100.0%) + pipeline-advanced 54/54 (100.0%)**. Prior batch records stand verbatim; this section reports the current live 54-row set derived from `.harness/substitutions.yaml`.

The bucket rows below sum to **54** under the batch-56 live ledger (RETIRED 54).

| Bucket | Count | Notes |
|---|---|---|
| RETIRED | **54/54 (100.0%)** live (batch-56) | 43 substantive + 8 authoring-only + 3 bounded-residual (CP-16, OD-6, CXA-2). IS 9/9; AS 11/11; CP 21/21; OD 8/8; CXA 5/5. Phase-8 historical declaration remains 46/54; batch-52 added AS-8e, AS-8f, and CP-17 after R-810/R-820 live proofs; batch-53 adds OD-4 and CXA-4 after accounting/back-flow; batch-54 adds CXA-3 after the CP→AS runtime composer landed; batch-55 adds CXA-2 as bounded residual after producer-loop evidence landed; batch-56 adds CXA-1 after the AS→IS producer and edge-scope audit landed. |
| RETIRE-READY | **0 active (bucket EMPTY post-batch-51)** | OD-3 + OD-6 transited RETIRE-READY → RETIRED at batch-51 (PR #200). |
| PARTIAL | **0** | Empty after batch-56. CXA-1 moved to SUBSTANTIVE_RETIRED after the AS→IS edge-scope audit and resolver-bound secret-fetch write landed. |
| STILL-BOUNDED | **0** | Empty after batch-55 and remains empty after batch-56. |
| RETIRED-AS-AUTHORING-ONLY | **8** (of the 54 RETIRED) | accounting (i): IS-4, IS-10, AS-9, CP-12, CP-23, OD-1, OD-7, OD-8 (accounting (ii) = 9, adds CP-24). The H_T contract is the typed declaration itself; no runtime behavior (sub-species 10 categorical-mismatch). |
| RETIRED-AS-BOUNDED-RESIDUAL | **3** (of the 54 RETIRED — *counted*) | CP-16 (memory, batch-44) + OD-6 (OTLP, batch-51 — FIRST in ledger) + CXA-2 (CP→IS durable recovery, batch-55). Production substrate dormant or post-MVP at MVP; substantive close deferred to a real deployment/recovery surface (X-AL-2 §5.3). |

**Decomposition status (R-002 RESOLVED 2026-05-31) + R-700 draft correction (2026-06-01):** R-002 mapped all non-RETIRED rows *in the per-axis `§4.1` view* to R-NNN entries — IS-2 (R-003 + R-001-h-t-is-2-retired), OD-3 (R-007), OD-4 (R-008), OD-6 (R-009), AS-8e (R-005), AS-8f (R-006); R-001 (OD-5) + R-004 (AS-8d) reconciled stale BLOCKED → RESOLVED. **The R-700 draft surfaced that the R-002 survey covered only `§4.1`, which excludes the CXA axis (no `§4.1` file) + CP-17's SB-INDEF reclassification — so 5 non-RETIRED rows (`CXA-1`/`CXA-2`/`CXA-3`/`CXA-4` + `CP-17`) have NO `R-NNN` entries.** Recommended follow-on (draft §C item 2): an `R-002`-style Surface-I decomposition pass over CXA + CP-17. **→ CLOSED at PR #246 (R-700 PART C item 2):** grounding found CXA-1/2/3/4 already had `R-CXA-1..4` (authored #209); NEW `R-010-cp-17-files-indefinite` authored for CP-17 → all 5 flagged rows now have dedicated entries. Ratified at the Phase-8 graduation (2026-06-02).

---

## Drift detection log


_Showing the 10 most recent drift/reconciliation events. The full audit history (155 events) is archived at `.harness/roadmap_drift_log_archive.md`._

| Date | Source | Resolution |
|---|---|---|
| 2026-06-21 | **Post-#680 terminating refresh — PR #680 R-FS-1 `B-HIERARCHICAL-PAUSE` (resumable cascade_policy=pause for HIERARCHICAL_DELEGATION) merged at `3096d15d`; §12.2 owed follow-on.** | Hash `31b3d2da265a` → `9f9e9e5c22d5` (state at `3096d15d`, open PRs empty, fork count 82, batch-57). **The 20th standalone `B-*` arc.** Materializes §25.15.1 `pause → PAUSED` + the §25.18 impl-discretion recursion cascade for HIERARCHICAL_DELEGATION, in two halves: **(a) level-local** worker pause via the v1.42 `FanOutResumeState` (threading `pause_resumable=True` through the HIERARCHICAL dispatch); **(b) cross-recursion-boundary** — a `SUB_AGENT_DISPATCH` worker whose recursive child PAUSED (was SWALLOWED success-equivalent at the dispatch boundary) is surfaced via the typed `SubAgentChildPausedError`, captured into `FanOutResumeState.paused_child_branches` (the THIRD branch disposition: re-dispatch-via-child-resume), and re-entered at the child's cursor on `api.resume` via `StepExecutionContext.child_resume_snapshot` (grandchild's completed steps recovered, NOT re-executed). **Carrier (advisor):** a NEW field on `FanOutResumeState` (HIERARCHICAL IS ORCHESTRATOR_WORKERS at each level), additive default-empty + DROPPED-from-hash-when-empty (byte-compat) + covered transitively; self-nests (recursive Pydantic via one `model_rebuild()`). **impl-to-cleared-spec** (bundled CP spec **v1.44 → v1.45** §1+§2; NO operator gate — §25.18 names the recursion cascade as impl-discretion). Advisor **refused a Half-A-only slice** (not separable) + disqualified the **mislabeled** `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` (EO is SEQUENTIAL not a fan-out barrier — the §25.15.1 cascade `pause` trigger doesn't apply; arc-ledger + spine scope CORRECTED, paired with `B-HANDOFF-PAUSE` as the sequential-cursor shape) + the fork-first `B-ROUTING-MANIFEST-MODEL-FOLD`. **Discriminating full-chain witness** (grandchild completed-before-pause dispatched exactly once across pause+resume; declarative-only — the INFERENCE-child e2e is blocked by a pre-existing runtime deadlock). **Out-of-family Codex hardened it across 4 rounds** (asymptotic convergence, `[[hooks-codex-pilots-decorrelation-validated]]`): R1 a cancellation-race child-pause loss (a sibling-cancelled worker's in-flight `SubAgentChildPausedError` lands in `inflight.exception()` → was recorded terminal `completed` + the snapshot DROPPED) + resumed-child run_id reuse (real [P1]s); R2 pyright `reportConstantRedefinition` gate + missing token attrs; R3 best-effort audit on the pause path + dashboard regen; R4 the closed cardinality-3 `SubAgentResultStatus` enum violated by `result_status="paused"` → extended additively to PAUSED (3→4, C-CP-14 §14.2, advisor-adjudicated 4th outcome, own the now-false "no enum change" claim) + a child-resume `workflow_id` fail-closed guard + the cascade-cancel paused-child disposition (`completed`, not `cancelled`). **Class-3 (spine prose home):** the runtime spec §14.7.2 step-7 child-result mapping + §14.7.4 `ChildWorkflowRunner.pause_snapshot_input` co-land as runtime-internal surfaces; a batched runtime-spec refresh formalizes the §14.7.2 step-7 PAUSED row (impl-ahead-of-prose minor drift). advisor (full-transcript) + out-of-family Codex; all 15 CI checks green; pyright 0/0/0; harness-cp 1112 + 1 xfailed / harness-runtime 1986 non-e2e. `standalone_closed` 19→20; `standalone_registered` 13→12. **Next selector:** ground the forward register + pick (rec: `B-HANDOFF-PAUSE` + `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` [both SEQUENTIAL single-owner pause — pair them, fork-first] / `B-ROUTING-MANIFEST-MODEL-FOLD` [fork-first] / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#679 terminating refresh — PR #679 R-FS-1 `B-FANOUT-PAUSE-PARALLELIZATION` (resumable cascade_policy=pause for the PARALLELIZATION peer fan-out) merged at `006e2255`; §12.2 owed follow-on.** | Hash `b9bfda1ebec9` → `31b3d2da265a` (state at `006e2255`, open PRs empty, fork count 82, batch-57). **The 19th standalone `B-*` arc.** Materializes §25.15.1 `pause → PAUSED` for PARALLELIZATION — the v1.42 `B-FANOUT-PAUSE` (ORCHESTRATOR_WORKERS) shape applied PARALLELIZATION-shaped (NO orchestrator step[0]); the interim `parallelization-pause-resume-not-yet-materialized` FAILED flips to a genuine resumable PAUSED. **PREREQUISITE #678** supplied the cascade harvest. **Carrier fork = Option B (advisor, in-impl, NOT operator):** a NEW 2-field `PeerFanOutResumeState` (`branches`+`branch_count`, NO orchestrator fields), NOT a loosened `FanOutResumeState` (illegal-states-unrepresentable); a 2nd additive `PauseSnapshot.peer_fan_out_resume` field (never co-set with `fan_out_resume`) → existing snapshots byte-identical. **impl-to-cleared-spec** (bundled CP spec **v1.43 → v1.44** §1+§2; NO operator gate; materializes the R-CC-1 §1.1 re-open trigger). **`[[design-substrate-version-identity-hazards]]` caught before overwrite:** v1.43 was ALREADY `B-LAYER-BUDGET-OVERRIDE` (#667) → this delta is **v1.44** (the root/CP CLAUDE.md §1.2/§2.3 spec-head pointers are independently stale at v1.38 — a pre-existing drift the v1.39-v1.43 arcs didn't touch; left as-is per surgical-changes). **Full-chain witness** through the real `api.resume` (CP 12 tests + runtime e2e). **Codex [P2] (decorrelated catch):** stale dashboard snapshot → regenerated gh-down for CI parity (`[[regenerate-roadmap-html-after-source-edit]]`). **CI also caught** a missing `pr:` field on the closed arc-ledger entry (the `test_standalone_projection_matches_ledger_counts` dashboard-projection gate requires every closed arc to carry its PR in status_detail — fixed). advisor (full-transcript) + out-of-family Codex; all blocking CI green; pyright 0/0/0; harness-cp 1110 + 1 xfailed / harness-runtime 1982 non-e2e. `standalone_closed` 18→19. **Next selector:** ground the forward register + pick (rec: `B-FANOUT-PAUSE-EVALUATOR-OPTIMIZER` / `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#678 terminating refresh — PR #678 R-FS-1 `B-PARALLELIZATION-CASCADE` (cascade_policy consumption for the PARALLELIZATION fan-out) merged at `6197a0fd`; §12.2 owed follow-on.** | Hash `dab193c3abb3` → `b9bfda1ebec9` (state at `6197a0fd`, open PRs empty, fork count 82, batch-57). **The 18th standalone `B-*` arc.** PARALLELIZATION (U-CP-86) was built happy-path-only ("U-CP-85 non-dep") → it had **no cascade_policy at all**, so a single branch failure fail-fasted the whole fan-out even under `proceed` (where §25.15.1 mandates PARTIAL with survivors). Materializes §25.15.1 PARALLELIZATION-shaped (peer branches, NO orchestrator step[0]; mirrors `_execute_orchestrator_workers` U-CP-88): proceed→harvest-survivors-PARTIAL / cascade-cancel→FAILED+cancelled-terminals / pause→honest `parallelization-pause-resume-not-yet-materialized` FAILED (the resumable PAUSED is the now-UNBLOCKED follow-on `B-FANOUT-PAUSE-PARALLELIZATION`). **impl-to-cleared-spec** (§25.15 + §25.18 anticipate it; NO design-substrate edit, NO operator gate). **Also closed the CP v1.42 line-23 spec-prose-vs-code drift** (folded into the spine ledger, not a standalone `class_3` doc — that tripped the now-NON-vacuous Codex guard DESIGN_IMPL_MIX [real since #674's full-clone fix]; deleting it net-zeroed the diff). **Codex [P2] (decorrelated catch):** `proceed`'s `gather(return_exceptions=True)` masked a pre-dispatch SETUP error (unbound `StepKind`) as a degradable PARTIAL where the linear path FAILs → fixed by a pre-flight dispatcher resolution failing loud (regression test pins it). **Also-learned (memory):** `just codex-review` materializes a clean tree → reverts uncommitted edits to HEAD (commit before running it). advisor (4 calls) + out-of-family Codex; all 14 CI checks green; harness-cp 1098 / harness-runtime 1981 non-e2e; pyright 0/0/0. `standalone_closed` 17→18. **Next selector:** ground the forward register + pick (rec: the now-unblocked `B-FANOUT-PAUSE-PARALLELIZATION` / `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-INTERSTEP-NONLINEAR`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#677 terminating refresh — PR #677 R-FS-1 `B-INTERSTEP-HANDOFF` (inter-step DATA flow for DECENTRALIZED_HANDOFF; the handoff slice of B-INTERSTEP-NONLINEAR) merged at `4fb3b6c6`; §12.2 owed follow-on.** | Hash `c6219183bb83` → `dab193c3abb3` (state at `4fb3b6c6`, open PRs empty, fork count 82, batch-57). **The 17th standalone `B-*` arc.** Each completed stage-expert's output is recorded INLINE (single-owner sequential, `workflow_driver.py:4933-4943`; NOT via the #648 buffered-branch drain — that serializes concurrent sibling writes, structurally absent here) so the next stage's dispatch reads `most_recent_output()` (stage B sees stage A's output). **impl-to-cleared-spec** (runtime §14.21 C-RT-29 §14.21.7 registers the recording follow-on; NO design-substrate edit), **NO operator gate** (opt-out `inter_step_data_flow=False` byte-identical; `StepDispatcher.dispatch` UNCHANGED; §25.3.3.4 + ADR-F2 single-threaded-write hold). **Slicing tie-breaker (advisor):** the 3 concurrent topologies' post-join synthesis consumer wants ALL siblings (the consumer injects only `most_recent_output()` → soft built-but-vacuous); handoff has exactly one predecessor → genuinely correct, no message-shape discriminator → cleanest first slice. **Full-chain witness** (`[[full-chain-witness-not-half-proofs]]`): CP producer witness + negative-control opt-out + a runtime full-chain test through the REAL `execute_workflow` → `_execute_decentralized_handoff` → REAL `RuntimeLLMDispatcher` over the production `SyncDispatcherFacade` bridge (stage B's actual provider call carries stage A's output). **De-mislabel (advisor-caught):** impl-to-cleared-spec with NO operator decision is NOT a Class-2 fork (§4.3) → the arc record lives in the spine ledger (the prose home), not a `class_N_fork_*.md`; deleting the mislabeled file ALSO correctly resolved the Codex guard's DESIGN_IMPL_MIX (the guard is right, the artifact was wrong). `B-INTERSTEP-NONLINEAR` re-scoped to the 3 concurrent strategies (ORCHESTRATOR_WORKERS, PARALLELIZATION, HIERARCHICAL_DELEGATION); `standalone_closed` 16→17. Class-3 note: §14.21.7 imprecisely lumps handoff under "buffered-branch drain". **Guard sequence:** first CI run HARD-failed `DESIGN_IMPL_MIX` (the mislabeled `class_2_fork`) + `DASHBOARD_SNAPSHOT_STALE` (stale `fork_count` 83 in roadmap.html) → fixed by deleting the file (net-diff drops the DESIGN surface) + regenerating roadmap.html with `fork_count` 82 (gh-down shim → 0 PRs) → all 14 checks green. advisor (4 calls) + out-of-family Codex ("no discrete introduced bug"). **Next selector:** ground the R-FS-1 forward register + pick the next highest-value arc (14 registered + 1 gated; e.g. `B-INTERSTEP-NONLINEAR` concurrent topologies, a `B-FANOUT-PAUSE` sibling, or the `B-EFFECT-FENCE`/`B-EDIT-CARRIER` families). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#676 terminating refresh — PR #676 `R-IF-115` (remaining-build audit surface + closure gate folded into the loop) merged at `67c96183`; §12.2 owed follow-on.** | Hash `846af89ad082` → `c6219183bb83` (state at `67c96183`, open PRs empty, fork count 82, batch-57). Lands the 2-pass audit (`.harness/audit/Remaining_Build_Audit_Report.md` + `Remaining_Build_Register.csv` 48 rows) + binding `Closure_Gate_v1.md` (Tier-1 build-complete gates R-FS-1 / Tier-2 quality-close + 5-dim coverage matrix gates R-CL-C1) + `tools/closure_gate.py` (`just closure-gate[-check]`; delegates to arc_ledger/overlay/substitution_ledger/roadmap, no new source-of-truth; `--check` exits 1 — 4/5 automatable Tier-1, G1.1 fails since R-FS-1 ACTIVE). **Overlay head-scoping (RB-DOC-03):** `contract_without_code` 6 phantoms → 1 (documented C-IS-11) + new head-scoped `unit_without_code` advisory; `overlay-check` 31/31 HARD green, 17 tests pass. `R-IF-115` routes every finding to its R-CL/R-IF home. **Mode-agnostic infra — NOT an R-FS-1 build arc; forward `B-*` register unchanged; next selector unchanged.** Dashboard guard: first run HARD-failed `DASHBOARD_SNAPSHOT_STALE` (R-IF-115 changed the render) → regenerated `roadmap.html` with `open_prs` empty (matching the gh-down CI guard env, since `open_prs` is not normalized) → fresh run after close/reopen all-blocking-green incl. Codex guard. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Maintenance refresh — `chore(lint): exclude vendored .agents/skills/bmad-* from ruff` committed at `18365738`; §12.2 hash re-pin owed.** | Hash `630223ee7a0f` → `846af89ad082` (state at `18365738`, open PRs empty, fork count 82, batch-57). Operator-requested: the previously-parked `.agents/skills/bmad-*` ruff `extend-exclude` (mirroring `.claude/skills/bmad-*`) is now committed so the operator-vendored bmad skill scripts stop tripping the Stop-gate / pre-commit / CI ruff on every session-stop. Pushed together with this terminating refresh (single push → CI runs once on the refresh tip, `_lag_expected`=True → green). Not a dashboard-source change; no arc/R-NNN status change. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-21 | **Post-#675 terminating refresh — PR #675 R-FS-1 `B-L2-FALLBACK-COMPOSITION` (layered routing composes with the C-RT-16 fallback chain; runtime v1.64 → v1.65 §14.6.1) merged at `4f1ae005`; §12.2 owed follow-on.** | Hash `930732c82a13` → `630223ee7a0f` (state at `4f1ae005`, open PRs empty, fork count 81→82 [+1 B-L2-FALLBACK fork doc], batch-57). **The 16th standalone `B-*` arc** — landed from the unmerged branch `f931003` via cherry-pick (the superseded `r-fs-1-arc-and-unit-map.md` + `roadmap.html` edits DROPPED: #673 retired the map to a pointer stub + made the dashboard `arc-ledger.yaml`-driven). route-once-then-fallback-the-chain (U-RT-114 §14.5.3): SELECTION factored to `routing_core_surface.resolve_routing_trace`; `resolve_routed_binding` runs routing ONCE at the C-RT-16 wrapper (seeds PRIMARY via `_augment_primary`); the inner reverts to a FAITHFUL echo; the #669 interim refuse-guard RETIRED. **Also carried a codex-context-guard fix:** #674's full-clone inadvertently activated `DESIGN_IMPL_MIX` (shallow-blind before — base-ref absent → empty changed_files → never fired) against the §11.4 bundled-absorption pattern EVERY spec-leg `B-*` arc uses → a clearance-marker exemption mirroring X-AL-3's (§4.4) presence-based recognition (advisor-reconciled vs Codex's artifact-tie suggestion — artifact-tying would make this guard stricter than X-AL-3; +2 guard tests, 26 existing green). **Decorrelated review:** advisor (3 rounds: approach + guard-fork + guard-reconcile) + 2× Codex; both [P2]s dispositioned (L3-rationale-span = the registered `B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION`; guard exemption intentional, clean deferral confirmed by grep). All 15 CI checks green (incl. Codex context guard — blocking: PASS). arc-ledger `registered → closed` (standalone_closed 15→16, `--check` green) + follow-on `B-L2-ROUTING-SPAN-LAYER-ATTRIBUTION` registered. Next selector = ground the forward register + pick (rec: `B-ROUTING-MANIFEST-MODEL-FOLD` / `B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#655 terminating refresh — PR #655 R-FS-1 `B-EFFECT-FENCE` (at-most-once effect execution; runtime v1.59 → v1.60 §14.22 C-RT-31) merged at `868f9e0c`; §12.2 owed follow-on.** | Hash `559518834b10` → `10fef4ccdb90` (state at `868f9e0c`, open PRs empty, fork count 74→75 [+1 B-EFFECT-FENCE fork doc], batch-57). **The EIGHTH standalone `B-*` arc.** Closes floor-(ii) at-most-once EXECUTION of non-idempotent step side-effects: a crash in the `workflow_driver.py:2031`→`:2336` window (effect fired → per-step ledger COMMIT) re-fires the effect on resume. A hand-rolled (I-6) durable **effect fence** at the `RuntimeToolDispatcher` `call_tool` sink mirrors the U-RT-123 `_claim_resume_revision` crash-atomic `O_EXCL`/`os.link`/`fsync` claim, re-keyed to the per-`(run, step, tool)` `idempotency_key`: WON → fire; LOST → `EffectFenceReservedUncommittedError` → FAILED `RunResult` (honest fail-close, no silent double-fire). COMMIT = the existing per-step ledger entry (the fence adds ONLY the RESERVE). Opt-in `RuntimeConfig.effect_fencing` (default `None` → byte-identical). **At-most-once *per (run, step) invocation*, NOT exactly-once** — a legitimate loop iteration is NOT conflated with a resume re-fire (advisor catch, verified at HEAD `workflow_driver.py:3620-3628`: EO passes the monotonic `entry_index` as `parent_idempotency_key`, fan-out the branch-scoped key — MUST, else IS dedup at C-IS-07 §7.5 drops iteration-2). Fence-scope fork (C10⊥C11) probe-resolved by durable-resume scoping (Reading C) → advisor, not council. **Codex [P2] decorrelated catch (advisor + author missed):** `HARNESS_EFFECT_FENCING=true` was silently ignored → wired into both env loaders (`_ENV_SCALAR_FIELDS` + `_RuntimeEnvSettings`) + regression tests, justified because it gates a CORRECTNESS property (no-silent-failure). **Advisor Check B** → added a by-execution test that the breaker fail-fasts the fence error (1 inner call). **NO operator gate** — additive + opt-in, no committed-invariant sacrifice; §22.1 HITL pause + per-tool classification + durable-auto are spine-registered follow-ons (`B-EFFECT-FENCE-HITL-ROUTE` / `-PER-TOOL` / `-DURABLE-AUTO`; cross-host folds into deferred F-CC). Bundled-absorption: runtime v1.59 → v1.60 §14.22 C-RT-31 + clearance + fork + `harness-runtime/src` + `harness-cp` test + by-execution tests (mechanics + real-dispatcher at-most-once + NEGATIVE CONTROL + fresh-instance restart + driver re-dispatch key-stability) + spine-ledger BUILT + arc-and-unit-map §5 (8 closed · 6 remaining) + roadmap.html. CI 14/14 blocking green; pyright 0/0/0; harness-runtime 1726 (non-e2e); harness-cp 1063 + 1 xfailed; `test_effect_fence.py` 6/6; X-AL-3 green. Next selector = **`B-HITL-PLACEMENT-PER-STEP-PRODUCER`** (recommended; HIGH cross-cutting — binds per-step `hitl_placements` so ALL wrap-time HITL gates incl. #653 fire in production) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#653 terminating refresh — PR #653 R-FS-1 `B-TOOL-GATE` (tool-step MCP-trust HITL gate site; CP v1.35 §19.1.2 Producer ¶ / runtime §14.8.2 step-4c) merged at `51f8c169`; §12.2 owed follow-on.** | Hash `9c6d852700d3` → `559518834b10` (state at `51f8c169`, open PRs empty, fork count 73→74 [+1 B-TOOL-GATE fork doc], batch-57). **The SEVENTH standalone `B-*` arc.** A third `RuntimeHITLGateComposer` at the tool-step registry path feeds the resolved-owning-MCP-host trust tier into the §19.1.2 MCP-trust gate axis (new `make_step_mcp_trust_tier_resolver`, mirroring `make_step_blast_radius_resolver`; `ctx.tool_dispatcher` NOT mutated so the producer-loop/`hitl_tool_loop` read the un-gated dispatcher). **Impl-against-cleared-spec** (`MCPTrustTier` / `GateLevelInput.mcp_trust_tier` / `MCP_TRUST_GATE_LEVEL_FLOOR` all pre-exist; no new contract/field/invariant) → NO operator gate. **Codex [P1] decorrelated catch (advisor + author missed):** the wrap-time gate is placement-driven, but `hitl_placements` is WORKFLOW-level (`WorkflowManifestEntry`, C-CP-17 §17.3) and the frozen+`extra=forbid` per-step `WorkflowStep` has no src binding workflow→per-step placements → `step.hitl_placements` is always `()` through the real loader→driver path → no wrap-time HITL gate fires in production for ANY step kind — a PRE-EXISTING gap shared by ALL wrap-time gate sites (even inference/sub-agent e2e tests use a `_StepWithPlacements` proxy), the unbuilt half of the B3 spine residual → registered `B-HITL-PLACEMENT-PER-STEP-PRODUCER`. Three-claim decomposition (never blurred): (1) wiring correct (real `run_bootstrap`); (2) axis composes when the gate fires (by-execution L0→DENY→{REJECT,RESPOND}→reject-raises; L3→AUTO→delegate); (3) production gate-*firing* awaits the producer arc. First-draft "non-vacuous in production" overclaim corrected to "non-vacuous at the tool gate, at parity." Bundled-absorption: `harness-runtime/src` + by-execution tests ONLY — NO design-substrate edit / spec bump / clearance marker; fork/build-record + spine-ledger BUILT + arc-and-unit-map §5 (7 closed · 7 remaining) + roadmap.html. CI 13/13 blocking green; pyright 0/0/0; harness-runtime 1922 / 10 skipped; CXA-P1 34; dashboard 15; X-AL-3 green. Next selector = **`B-EFFECT-FENCE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#651 terminating refresh — PR #651 R-FS-1 `B-INTERSTEP` (inter-step data-flow channel; runtime v1.59 §14.21 C-RT-29) merged at `a90fe979`; §12.2 owed follow-on.** | Hash `c28fb918789b` → `9c6d852700d3` (state at `a90fe979`, open PRs empty, fork count 72→73 [+1 B-INTERSTEP fork doc], batch-57). **The SIXTH standalone `B-*` arc.** A run-scoped `InterStepOutputChannel` (by-reference holder, opt-in `RuntimeConfig.inter_step_data_flow`; default byte-identical) lets the genuine LLM dispatcher inject the immediately-prior step's output into the actual provider call — closing the cross-topology no-data-flow gap (`EVALUATOR_OPTIMIZER` the load-bearing case). Dispatch signature UNCHANGED + §25.3.3.4 preserved → NO operator gate. **Daemon-scope hardening took 4 Codex rounds (5 real bugs) + an r5 systemic residual:** invariant 7 honestly bounded to (a) sequential reuse closed by per-run `reset()`; (b) concurrent-within-drain-timeout closed by a per-loop single-flight lock (opt-out default never locks); (c) the concurrent-with-timeout `to_thread`-zombie = a SYSTEMIC reused-ctx residual across every bootstrap-ctx holder (`cost_record_accumulator` included), NOT lock-closeable → registered `B-INTERSTEP-PERRUN-ISOLATION` (advisor reconcile: FULL-SPEC = registration not build-here; real-but-accepted, NOT looped-to-green). The advisor caught the `B-INTERSTEP-PERRUN-ISOLATION` forward-cite phantom → registered in the spine ledger before merge (`[[spine-ledger-forward-arc-registration]]`). One CI ruff-format RED (a prior commit's consumer-test file left unformatted; repo-wide gate caught what file-scoped local checks missed) fixed pre-merge. Bundled-absorption (runtime v1.59 §14.21 + clearance + fork + impl + by-execution tests incl. negative control + spine-ledger BUILT with 3 follow-ons + arc-and-unit-map 6-closed/7-remaining + roadmap.html). CI all blocking green; pyright 0/0/0; harness-runtime 1915 / harness-cp 1062; substitution 54/54. Next selector = **`B-TOOL-GATE`** (recommended autonomous `B-*`, HIGH) / the other remaining standalone arcs. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-18 | **Post-#648 terminating refresh — PR #648 R-FS-1 `B-NONLINEAR-OVERRIDE-PROVENANCE` (per-step override-ledger entry on all topologies; CP v1.40 §6.6) merged at `f42ed121`; §12.2 owed follow-on.** | Hash `3a819123455c` → `0488904ae18f` (state at `ec64b116`, open PRs empty, fork count 70→71 [+1 B-NONLINEAR fork doc], batch-57). #648 closed the FOURTH standalone arc but missed bumping `tools/test_dashboard_generate.py` (it parses the real arc-map §5 yet sits outside CI's axis-package pytest scope, so the 4-way-count RED went uncaught — same drift class #646 corrected). Per the **#461 precedent** (a refresh carrying a dashboard-test update can't be the clean dashboard-only pair), the test correction lands as its own commit `ec64b116` (3→4 closed / 10→9 remaining) and THIS terminating 2-file refresh pins it, restoring the §12.2.1 fixed-point shape. **The FOURTH standalone `B-*` arc.** The `cp.per-step-override-application` state-ledger entry (U-CP-14 §16.5) fired at the `SINGLE_THREADED_LINEAR` site only; now emits on all 6 topologies via the **buffered-branch path** (`append_branch_override_ledger_entry` → each branch's `BufferingLedgerWriter`, drained on the driver thread in branch-index order, realizing the ADR-F2 single-threaded-write boundary). §16.5.4 idempotency key reused VERBATIM → byte-shape-identical entry across topologies (branch-scoping would be an X-AL-3 spec change). Impl-to-cleared-spec on the general §6.6 provenance contract (CP v1.39 → v1.40 §6.6 topology-scope-note refresh) + stale-carry refresh of the falsified v1.38 note; **NO operator gate** (no committed-invariant sacrifice; additive). advisor + 2 rounds out-of-family Codex hardened 3 failure-path bugs (override-only-writer STEP_BOUNDARY mis-count; lost-override-on-failed-dispatch; missing CASCADE_CANCEL cancelled-terminal). Bundled-absorption (CP v1.40 + clearance + fork + impl + 13 by-execution per-topology tests + spine-ledger BUILT + arc-and-unit-map 4-closed/9-remaining). CI 14/14; pyright 0/0/0; overlay 323/31-31; substitution 54/54. Next selector = **`B-FALLBACK-CHAIN-FAMILY-COST-COMPOSITION`** (recommended autonomous `B-*`) / `B-TAIL-CONDITIONAL-SAMPLING` (R-420/R-421-gated). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#635 terminating refresh — PR #635 R-FS-1 arc M (Managed Agents executable-consumer contract + production wiring; C-RT-28 §14.20 + CP v1.39 `StepKind.MANAGED_AGENTS`) merged at `ff9f9737`; §12.2 owed follow-on.** | Hash `a1a0701a1664` → `6ecec018b867` (state at `ff9f9737`, open PRs empty, fork count 67→68 [+1 arc-M fork], batch-57). **M COMPLETE — the FINAL FROZEN child arc's autonomous half (10 of 11 done; only B6 Slice 2 remains).** A NEW `StepKind.MANAGED_AGENTS` (operator-ratified Option B; closed-at-5 Class-2 extension) dispatched by a `ManagedAgentsStepDispatcher` (vendor-runs-the-loop; `SyncDispatcherFacade`-bound; surface-gated to `MANAGED_CLOUD`+opt-in) makes the R-820-live-proven `managed_agents.py` carrier production-reachable. Option A (riding `SUB_AGENT_DISPATCH`) probe-foreclosed (harness-orchestration semantics a vendor session can't honor). Decorrelated-review hardening in-arc: decoupled facade timeout (`step_timeout_seconds` 600s ≠ shared 30s — orphaned-billable-session fix) + cancel-on-give-up + `freeze()` carrier passthrough (Codex). Vendor/credential boundary: live managed-cloud run is the surfaced vendor-gate, never auto-fired; H_T-AS-8f already SUBSTANTIVE_RETIRED (R-820). Bundled-absorption (CP v1.39 + runtime v1.55 + impl + tests + fork + 2 clearance markers + arc-and-unit-map M✅ in the substantive PR per the count-change discipline). advisor (caught the gate-dissolution attempt + facade-timeout/test-gap) + Codex (freeze-drop) clean; CI 4365 passed / 14 skipped / 19 deselected (e2e) / 1 xfailed; pyright 0/0/0; overlay 322 nodes 31/31. Next selector = **B6 Slice 2 (operator-gated; drive to gate) / autonomous standalone `B-*` arcs**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#630 terminating refresh — PR #630 R-FS-1 arc B6 Slice 1 (per-MCP-transport sandbox-tier floor composition; STDIO→TIER_3; runtime spec v1.54 §14.9.8) merged at `1a7dc6e2`; §12.2 owed follow-on.** | Hash `46e6f7ed8878` → `8cb531254be3` (state at `1a7dc6e2`, open PRs empty, fork count 66→67 [+1 B6 fork], batch-57). **B6 Slice 1 COMPLETE** — the stage-5 factory's per-host resolver/driver loop composes `mcp_transport_floor` (U-AS-13) via tier-`max()` with the §14.9.8 surface default (new `compose_transport_floor`, SSOT) → STDIO ≥ TIER_3, L2-remote → TIER_4; closes the carved-out ADR-D2 §1.3 STDIO tier-3 transport-floor gap. **C10⊥C11 probe-resolved at ADR-D2 §1.3 in C10's favor** (STDIO Tier-3 regardless of blast-radius, hard floor even at local-dev) → impl-to-cleared-ADR, NO operator gate, sacrifices no invariant. Per-server-uniform PRESERVED; per-tool granularity = Slice 2. **Real out-of-box change (named):** local-dev STDIO needs a driver or bootstrap fail-closes. **Coverage loss surfaced:** in-process-STDIO e2e (`test_r100_ac2`+`test_r_cl_p3`×3) SKIPPED (pre-B6 gap ADR-D2 §1.3 forbids; R-410 covers container completion); U-RT-86 retirement evidence verified ran-and-passed unaffected. advisor (caught the decomposition refinement + a strict-raise reason-attribution bug) + Codex clean; 1821 passed / 24 skipped / 1 xfailed; pyright 0/0/0; overlay 31/31. Bundled-absorption (runtime v1.53→v1.54 + fork + clearance + spine Slice-1/2 split). Forward arcs: `B-PER-TOOL-SANDBOX-TIER` (Slice 2, operator-gated) + `B-MCP-HOST-REMOTE-TRANSPORT`. Next selector = **B6 Slice 2 (operator-gated) / B7 (unblocked autonomous)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#628 terminating refresh — PR #628 R-FS-1 arc B5 (per-deployment-surface memory backend selection; `resolve_backend` non-vacuous; runtime spec §14.12.1) merged at `ae62fdb9`; §12.2 owed follow-on.** | Hash `cf0db8f3ab05` → `46e6f7ed8878` (state at `ae62fdb9`, open PRs empty, fork count 66, batch-57). **B5 COMPLETE** — `MemoryToolRegistry.resolve_backend(deployment_surface)` made surface-discriminating (bootstrap-frozen surface→backend map via `from_surface_map`; default path resolves config-free FILESYSTEM per surface + freezes a fail-closed deferred RT-FAIL for config-less surfaces like `MANAGED_CLOUD`; ACTIVE surface must resolve or bootstrap aborts; active-surface dispatch byte-identical; anti-vacuity = constructs-vs-raises). Impl-to-cleared-spec (impl-discretion per plan v2.14 U-RT-80 AC + §14.12.7 — no fork / no design-substrate / no new contract / no CXA seam; overlay 31/31). #628 (substantive) bundled the arc-and-unit-map (B5→done 8/11, B6→next) + spine forward-arc registration (`B-MEMORY-SURFACE-BACKEND-IMPLS`: the spec'd-but-unbuilt `ENCRYPTED_FILESYSTEM`+`OPERATOR_DEFINED` backends) + regen roadmap.html in the SUBSTANTIVE PR (the #623 build-progress-source lesson → B5 = 2 PRs, not 3). advisor (design + pre-done) + Codex clean; harness-runtime 1811 passed / 10 skipped; pyright 0/0/0. Next selector = **B6 (per-tool sandbox-tier + STDIO transport-floor; fork-owed)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#626 terminating refresh — PR #626 (substantive arc-map + status refresh post-#625) merged at `35886ef9`; terminating 2-file refresh owed.** | Hash `d883470ddab1` → `cf0db8f3ab05` (state at `35886ef9`, open PRs empty, fork count 66, batch-57). #626 bundled the arc-and-unit-map (dashboard build-progress source, #623) with roadmap_status.md → a substantive dashboard refresh (outside the `hook_is_dashboard_only_set` 2-file set), so it owed this terminating 2-file refresh to reach the §12.2.1 fixed point (same shape as #623→#624). Next selector unchanged = **B5**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#625 terminating refresh — PR #625 R-FS-1 arc CA (`RunResult.cost_attribution` rollup; runtime spec v1.53 §9 C-RT-09) merged at `7861171d`; §12.2 owed follow-on.** | Hash `3c31942c5c6b` → `d883470ddab1` (state at `7861171d`, open PRs empty, fork count 65→66 [+1 CA fork], batch-57). **CA COMPLETE** — run-result cost rollup wired (`CostRecordAccumulator` holder + 4 per-dispatch wrappers + `_build_run_result` PER_PROVIDER_AND_MODEL rollup). Axis empirically forced (advisor + HEAD body-grounding overturned the sweep's PER_PROVIDER_DISCRIMINATOR lead — production dispatch-type tags ∉ `CrossFamilyTag`). Pydantic-freeze-copies-typed-list bug caught by advisor pre-merge + fixed with the by-reference holder + a freeze()-traversing regression test. Forward items registered: `B-COST-DISCRIMINATOR-TAXONOMY` (the per-family rollup) + (prior) `B-NONLINEAR-OVERRIDE-PROVENANCE`. arc-and-unit-map refreshed (CA→done 7/11, B5→next) per the #623 build-progress-source lesson. Next selector = **B5 (memory backend per deployment surface)**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#623 terminating refresh — PR #623 fix(dashboard): reflect B4 completion in the arc-and-unit-map (build-progress source) + deploy trigger; merged at `d94d78d2`; §12.2 owed follow-on.** | Hash `e89a78305084` → `3c31942c5c6b` (state at `d94d78d2`, open PRs empty, fork count 65, batch-57). Operator-flagged: the published roadmap showed CONFLICTING B4 signals (roadmap_status.md sections said B4✅/CA-next; build-progress masthead/arc-strip/cards parsed from the stale `r-fs-1-arc-and-unit-map.md` said 5/11/B4-next). Root cause: the arc-and-unit-map was never refreshed through any B4 slice AND was missing from the deploy path filter. Fixed: arc-map B4→done (6/11) + as-built units + CA→next; `generate.py` 0-new-unit render; deploy path filter += the arc-map. Verified by in-browser render (6/11, B4✓, CA▶next). Next selector unchanged = **CA**. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#621 terminating refresh — PR #621 R-FS-1 B4 Slice 4 (per-step ROLE override + linear-path indexing; CP v1.38 + runtime v1.52 §14.5.3 invariant-2/3 relaxation) merged at `abb67a99`; §12.2 owed follow-on.** | Hash `5c8577ce9942` → `e89a78305084` (state at `abb67a99`, open PRs empty, fork count 64→65 [+1 B4-Slice-4 fork doc], batch-57). **B4 COMPLETE** — Slice 4 closes the tail (operator-ratified §14.5.3 relaxation; Option-B composition-time fold, runtime dispatch byte-unchanged; full-stack consumer-e2e green). Next selector = **CA (cost aggregate)** — `RunResult.cost_attribution` rollup (per-dispatch built; run-result rollup + accumulator owed; small pre-authorized C-RT-09 fork). Forward item registered: `B-NONLINEAR-OVERRIDE-PROVENANCE` (Codex-surfaced; pre-existing linear-only override-ledger emission, all dimensions). Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-17 | **Post-#619 terminating refresh — PR #619 R-FS-1 B4 Slice 3 (per-step PROMPT override, CP v1.37) merged at `70ca4628`; §12.2 owed follow-on.** | Hash `4d03a1931cbd` → `5c8577ce9942` (state at `70ca4628`, open PRs empty, fork count 63→64 [+1 B4-Slice-3 fork doc], batch-57). B4 Slice 3 closed; next selector = **B4 Slice 4** (per-step ROLE gate + linear-path role indexing). Session-start hook had flagged expected mid-arc feature-branch drift (computed `f42f967c144c` ≠ dashboard `4d03a1931cbd`) — resolved by this post-merge refresh, not a workspace divergence. Lags HEAD by one commit (this terminating-refresh fixed point, §12.2.1). |
| 2026-06-10 | **Claude release-candidate handoff authored locally at `a5215dde`; post-handoff refresh.** | Hash `e13d42d7874c` → `dbfe6623fc76` (state at `a5215dde`, open PRs unavailable/empty, fork count 47, batch-56). Added `.harness/release-candidate-deployment-readiness-runbook.md` and Claude/context pointers. Next selector is the HIL release-candidate deployment-readiness scope gate, then provider-free readiness before any live deployment smoke. |
| 2026-06-10 | **Overlay-check ignored-artifact fix merged locally at `d4b6b4e3`; post-merge refresh.** | Hash `c28613a1f0a3` → `e13d42d7874c` (state at `d4b6b4e3`, open PRs unavailable/empty, fork count 47, batch-56). `overlay-check` now ignores stale gitignored `tools/semantic_overlay/overlay.json` artifacts while preserving the hard gate for stale tracked snapshots. Next selector moves to an HIL release-candidate deployment-readiness scope discussion before opening new implementation. |
| 2026-06-10 | **Post-#447 terminating refresh — PR #447 BMad runtime config restore merged at `9cf968d2`; §12.2 owed follow-on.** | Hash `48f9537038cf` → `f9c6f1ebe73d` (state at `9cf968d2`, open PRs empty, fork count 47, batch-56). `_bmad/bmm/config.yaml` is restored on main; no roadmap item status changes beyond clearing the last in-flight PR. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **PR #440 supersession / post-#464 open-PR refresh branch opened from `d24dcf57`.** | Hash `78617c29740a` → `48f9537038cf` (state at `d24dcf57`, open PRs #447 only, fork count 47, batch-56). #440 was closed as superseded: it was conflicting, predated the later R-CXA-2 implementation/back-flow sequence, and a current-main comparison showed it would remove later R-CXA-2 implementation, tests, batch records, and dashboard/overlay updates if merged. Next selector remains recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Post-#463 terminating refresh — PR #463 R-IF-112 semantic overlay closeout merged at `925f021a`; §12.2 owed follow-on.** | Hash `ebb6d51193e6` → `78617c29740a` (state at `925f021a`, open PRs #440/#447, fork count 47, batch-56). R-IF-112 is now RESOLVED: `tools/semantic_overlay` derives the design-substrate contract keyspace, source cite layer, CXA seam endpoint graph, and substitution join; CI gates hard CXA/stale-artifact drift and reports advisory code/contract/substitution orphan classes. Next selector moves to this terminating refresh, then recurring R-600/R-IF lanes unless the operator selects a new non-recurring frontier. |
| 2026-06-09 | **Substantive R-IF-112 implementation closeout branch opened from `62f9a327`; §12.2 owed after merge.** | Hash `a0ddc2154026` → `ebb6d51193e6` (branch base `62f9a327`, open PRs #440/#447, fork count 47, batch-56). The existing semantic overlay linter is completed against the roadmap must-pass by adding design-substrate `C-*` keyspace scanning and advisory `contract_without_code` orphan reporting, alongside the existing code-without-cite, CXA missing-endpoint, and substitution-without-carrier classes. `R-IF-112` is moved to RESOLVED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |
| 2026-06-09 | **Post-#461 corrective fixed-point refresh — PR #461 post-#460 roadmap/status/dashboard refresh merged at `41297c2a`; §12.2 anchor re-pin.** | Hash `b958e3438c97` → `a0ddc2154026` (state at `41297c2a`, open PRs #440/#447, fork count 47, batch-56). PR #461 completed the post-#460 refresh but included a dashboard test update, so the default-branch guard could not classify the merge as the expected status/dashboard-only lag. This two-file refresh restores the fixed-point shape and keeps the selector on `R-IF-112` plus recurring R-600/R-IF lanes. |
| 2026-06-09 | **Post-#460 terminating refresh — PR #460 R-CXA-1 AS→IS edge-scope/sidecar closeout merged at `b9a9ec87`; §12.2 owed follow-on.** | Hash `39009757fd12` → `b958e3438c97` (state at `b9a9ec87`, open PRs #440/#447, fork count 47, batch-56). R-CXA-1 is now merged closed; the live substitution ledger is 54/54 RETIRED and 54/54 pipeline-advanced. Next selector moves off CXA build-close work to this terminating refresh, then the post-substitution governance/process frontier (`R-IF-112` if operator selects a new substantive arc; recurring R-600/R-IF lanes by cadence). |
| 2026-06-09 | **Substantive R-CXA-1 AS→IS edge-scope/sidecar branch opened from `22d7d53`; §12.2 owed after merge.** | Hash `ffe9044984c8` → `39009757fd12` (branch base `22d7d53`, open PRs #440/#447, fork count 47, batch-56). Filed `.harness/r-cxa-1-as-is-edge-audit-2026-06-09.md`, threaded the stage-5 procedural-tier resolver into the production `RuntimeAsIsWiring` secret-fetch callback, and moved H_T-CXA-1 from PARTIAL to SUBSTANTIVE_RETIRED. Live ledger is now 54/54 RETIRED on this branch; a terminating refresh is owed after merge to pin the final merge commit/dashboard hash. |

---

## Audit checklist (run at session start)

- [ ] `workspace_state_hash` matches computed value per recipe above
- [ ] All open PRs at GitHub appear in `In-flight`
- [ ] `recently_completed` reflects last 5 merged PRs
- [ ] `latest_retirement_batch` matches `ls .harness/phase-7d-retirement-events-batch-*.md | tail -1`
- [ ] No `R-NNN` at `Project_Roadmap_v1.md` §5 has `status: ACTIVE` while its `depends_on` are not all RESOLVED (re-derive per §4)

If any check fails → HALT, route per `Project_Roadmap_v1.md` §7.1 step 4.

---

*End of dashboard. Master roadmap at `Project_Roadmap_v1.md`. Enforcement at `CLAUDE.md` §12.*
