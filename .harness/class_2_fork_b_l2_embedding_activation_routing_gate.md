# Class 2 (in-execution, FULL-SPEC-pre-authorized) — `B-L2-EMBEDDING-ACTIVATION`: the routing-activation gate (§2.2-faithful conditional DECLARATIVE)

**Status:** ✅ BUILT (2026-06-19) — the LAST buildable standalone `B-*` arc (the 14th since the FROZEN order completed). **Impl-to-cleared-spec (the #653 pattern): NO design-substrate edit, NO clearance marker, build-record doc only.** Co-lands `harness-runtime/src` (the opt-in flag + the §2.2-conditional `_declarative_echo` + the factory classifier-build) + by-execution tests (non-e2e stub witnesses + a real-fastembed factory e2e).
**Filed at:** R-FS-1 standalone arc `B-L2-EMBEDDING-ACTIVATION` (the routing-activation gate the L2 EMBEDDING + L3 router both wait on).
**Locus:** `harness-runtime/.../lifecycle/llm_dispatch.py` (`_declarative_echo` + the `routing_activation`/`embedding_classifier` dispatcher fields + `materialize_llm_dispatcher_stage`) + `harness-runtime/.../types.py` + the two env loaders + `bootstrap/stage_5_loop_init.py` vs cleared `Spec_Control_Plane` **C-CP-02 §2.2** + `.harness/beyond-mvp-capability-boundary-ledger.md` (the B-L2-EMBEDDING-ACTIVATION registration + the disposition note).
**Classification:** Class 2 (in-execution; FULL-SPEC directive 2026-06-12 pre-authorizes the build). **NO operator gate** (see below).
**Routing:** Build-record (the #653 B-TOOL-GATE shape — impl against a cleared contract, no spec bump).

## What was built

L2 EMBEDDING was BUILT (Option B — the CP-pure k-NN classifier + the light fastembed realization + corpus + the `RuntimeLLMDispatcher.embedding_classifier` injection seam) but **production-inert**: the production `_declarative_echo` (`llm_dispatch.py`) ALWAYS resolved the effective binding, so `route()` short-circuited before EMBEDDING. This arc makes DECLARATIVE **§2.2-faithful** so traffic reaches the (built) EMBEDDING classifier (then LLM_AS_ROUTER):

1. **The opt-in flag** — `RuntimeConfig.routing_activation: bool = False`, env-keyed in BOTH loaders (`HARNESS_ROUTING_ACTIVATION`) for the no-silent-failure reason `effect_fencing` needed (`[[runtimeconfig-scalar-needs-both-env-loaders]]`; it gates a behavior-changing property — which model serves a workload). Default-off → byte-identical / **zero blast radius**.
2. **The §2.2-conditional `_declarative_echo`** — when `routing_activation` is on, DECLARATIVE resolves ONLY when the manifest binds the request's tuple (`agent_role ∈ per_role_bindings` **OR** `workload_class ∈ per_workload_overrides` — the OR is the right realization of the §2.2 "binds the (agent_role, workflow_class, step) tuple" given the manifest's two keyed maps + cheapest-deterministic-first: either deterministic source resolves); on a manifest-miss it DECLINES (returns None) → `route()` falls through to EMBEDDING.
3. **The dispatcher + factory threading** — a `routing_activation` dispatcher field + an injectable `embedding_classifier` factory kwarg; when flag-on + no classifier injected, the factory builds the default L2 classifier (`make_embedding_classifier(embed=make_fastembed_embedding(), corpus=default_routing_corpus())`), fail-louding with the install hint if the optional `[embedding]` extra is absent. `routing_activation=config.routing_activation` threaded at stage 5.

## Why NO operator gate + NO spec leg (advisor-decomposed)

- **C-CP-02 §2.2 ALREADY specs the activation condition** (the "When it resolves" table: DECLARATIVE resolves "when the manifest binds the (agent_role, workflow_class, step) tuple"; EMBEDDING when "the manifest does NOT bind AND classifier confidence exceeds threshold"). The always-echo `_declarative_echo` was the **#213 MVP behavior-preserving shortcut**, NOT the §2.2 contract. Making it §2.2-faithful is **impl-to-cleared-spec** — DECLARATIVE catches up to the cleared contract. So this is the **#653 pattern (no design-substrate edit, no clearance marker, build-record doc only)**, NOT the #667 §2.5.3-amendment pattern (advisor: "§2.2 already says it — check it leaves something unsaid before manufacturing a spec leg"; it does not).
- **#213 is an unbuilt MVP gap, not a ratified-don't-build** (`[[cleared-spec-resolves-it-before-first-principles-fix]]`: classify the decision, not the ledger paraphrase). The forward register framed #213 as "routing activation RESOLVED; the multi-provider EXERCISE is B-2 | impl + operator creds" — #213 deferred the live exercise (creds-gated), §2.2 specs the mechanism.
- **No committed invariant is sacrificed.** Default-off preserves #213 verbatim; the default-off-until-opt-in posture is impl-discretion (like `inter_step_data_flow` / `effect_fencing`), not a §2.2 divergence to amend. Highest-blast-radius arc → **default-off is non-negotiable** (advisor).

## Non-vacuity (the #653/#657 overclaim trap — advisor's load-bearing catch)

The non-e2e unit witnesses inject a **stub** classifier (fastembed-free). The advisor flagged: the REAL factory build (`make_embedding_classifier(embed=make_fastembed_embedding(), corpus=default_routing_corpus())`) is the production path and pyright proves only the signatures, not that the composition works. **Resolved by RUNNING it** (the `[embedding]` extra installed locally — free/local, `[[feedback-run-credential-gated-live-e2e-authorized]]`): the real composition builds a callable classifier that routes "extract the totals from each row of this csv" → `ollama:llama3.2:3b`. Pinned as `test_l2_factory_builds_real_classifier_when_routing_activation_on` (e2e-marked + self-skips without the extra → never leaks into the provider-free lane). So the claim is scoped honestly: the §2.2 mechanism is **unit-witnessed through the production `_declarative_echo` path** (stub classifier) + the **real fastembed factory build is e2e-exercised**.

## Production-dormancy + the genuine gate (driven-to, NOT fired)

**Capability-built, production-dormant** — NOT "routes in production today": the live multi-provider EXERCISE (routing-among-candidates being meaningful) needs a **second configured production provider** — an operator-creds DEPLOYMENT gate (`[[feedback-background-agent-no-unilateral-paid-calls-or-secret-relocation]]`), driven-to but not fired. The advisor's distinction: the WITNESS needs 2 candidates (stubs / the free-local fastembed corpus suffice, no real 2nd provider) — a build concern, satisfied; PRODUCTION needs a 2nd provider — a deployment concern, the gate. The `[embedding]` extra **stays optional** (the base install stays light + provider-free lanes never pull onnxruntime); flag-on requires the operator to install it (`uv sync --extra embedding`) — promoting it to a required dependency is the deferred deployment step (`onnxruntime` capped `<1.24` for x86-mac per `[[operator-dev-machine-and-dep-preference]]`).

## Scope + the footgun

In scope: the §2.2-conditional DECLARATIVE + the opt-in flag + the factory classifier-build + the witnesses. The footgun (advisor #3): flag-on + no classifier via DIRECT construction (not the factory) → a manifest-miss falls through EMBEDDING (empty) → L3 (`router=None`) → a dispatch raise. The factory closes it (builds-or-fail-louds); production goes through the factory → test-only → documented in the dispatcher field comment.

## Decorrelated review (pre-merge)

advisor (full-transcript, 2 rounds) — round 1: the decision-rule (§2.2 clears the condition; #213 is an MVP gap; build opt-in/default-off; the witness needs stubs, production needs a 2nd provider); round 2 (on the built mechanism): the three tests genuinely discriminate, the **load-bearing factory-real-build was unexercised** (→ the e2e), **no spec leg owed** (§2.2 already says it → #653 pattern), the footgun comment, and named the OR realization. All applied. Out-of-family Codex review at the impl-diff PR (decorrelated; pending).

## Verification

- harness-runtime (non-e2e): `test_lifecycle_llm_dispatch.py` — `test_routing_activation_declines_declarative_on_manifest_miss_routes_embedding` (the non-dormant witness: flag-on + manifest-miss + stub classifier → EMBEDDING routes to the non-default model through the production `_declarative_echo`) + `..._off_declarative_echoes_default_zero_blast_radius` (negative control) + `..._manifest_binds_keeps_declarative` (the §2.2 binds-half) + `test_factory_threads_routing_activation_and_injected_classifier`; `test_config_loader.py` + `test_config_source.py` — the `HARNESS_ROUTING_ACTIVATION` env round-trips.
- harness-runtime (e2e): `integration/test_l2_embedding_live_e2e.py::test_l2_factory_builds_real_classifier_when_routing_activation_on` — the real fastembed factory build composes + routes (3 e2e green with the extra installed).
- Gates: whole-workspace pyright 0/0/0 · ruff · harness-runtime non-e2e 1967 passed / 10 skipped · decorrelated review advisor (2 rounds) + out-of-family Codex (pre-merge).
