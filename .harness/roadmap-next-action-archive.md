# Roadmap next-action round archive

**PRIOR rounds only** of `.harness/roadmap_status.md`'s `## Next action` section, most-recent-first. The CURRENT round lives ONLY in the live head — never here (R1 rule 2: a terminating refresh writes exactly one file, so this archive cannot track currency; codex round-6 resolution (b), prior-only by definition). Each entry is the round's text as it stood in the live head when superseded — its label rewritten `Current` → `Prior next action (post-#N)` at archiving, the body verbatim. Rounds archived before the U-CTX-03 truncation retain their original full-length text; rounds after it are the compact synopses the truncated head carries. Archiving happens in the NEXT content PR after a round is superseded (the superseded text is taken from git history of the live head), never in the terminating refresh commit itself — so between a terminating refresh and the next content PR, the NEWEST superseded round is not yet here; for that lag interval resolve it from the live head's own git history (`git log -p -- .harness/roadmap_status.md`), which is lossless. **Not part of any session-start, post-merge, or drift-audit read path** — resolve a specific round via targeted grep (a PR number, a `B-`/`R-` id, a round number), never by reading this file wholesale.

---

**Prior next action (post-#1489).** The next implementable unit is the §8 R2 mechanical sweep U-SR-09 — charter §2 "deliberately absent" + [B] b1/b4/b5 (c1 subsumed): b1 pin scope (bind the mutation pin to the probed block's digest or pin once at ship — implementer picks the arm and records why; witness: edit an unrelated line, the pin stays fresh; baseline 12 re-pins, 13 of 29 commits, 1.07M IET [B] F7), b4 rtk rewrite shapes (`\|` alternation → rg parse error, `--glob` landing on BSD grep, `(`/`)` in fixed-string greps — fix or pass-through, each shape round-trips; [B] F5: 29 wasted calls), b5 edit-hook timing (time `postedit-lint.sh` and `graft-hooks.cjs post-edit` separately on one `.py` edit; put `ruff` on the hook-shell PATH if `uv run ruff` is the path taken; measured numbers in the commit message, post-fix median < the [B] F8 baseline of 8.9 s). U-SR-08 landed at #1489: WR-15 as `.claude/skills/context-save-lean/` (the gstack save flow only, 12,548 bytes against the 54,977-byte gstack skill; a project-level `context-save` can never run because personal overrides project — CLAUDE.md §6 corrected) with every Claude/Codex carrier, the Codex binding notes, governance body and tool prose repointed and a repo-wide negative sweep pinning it; WR-16 premise-corrected (rtk 0.40.0 emits nothing on a no-rewrite; PreToolUse stdout never reaches model context) and pinned by tools/hooks/test_pretooluse_bash_emit_policy.sh; six out-of-family rounds (19 findings, all accepted; r6 APPROVE) and the 3-lens merge gate. Named residuals: `/context-save` typed by hand still resolves to the gstack skill; the §8.1 pin on tools/lanes_verify.py is stale again after the row append (re-pin owed by the next arc touching that pair, 66 unprobed annotations pre-existing). Remaining §8 sequence after U-SR-09: U-HE-36-as-R3 with U-HE-51 riding it; then U-SR-09 as the next unit.

**Prior next action (post-#1485).** The next implementable unit is one of the remaining §8 R1 levers `U-SR-07…08` (order-free; U-SR-07 — session-shape codification, charter WR-14 — is the natural pick: three habit lines into the loop skills' close-out carriers — facts-brief → fresh-session for heavy audits, cache-warmth handoff before >TTL background waits at >400k context, read-before-grep — with a grep-witness acceptance and wiring suite green). U-SR-06 landed at #1485: WR-13's REWRITE arm is executed — every live governance carrier (61 in the witness enumeration: CLAUDE.md, the loop/council/optimize skills, both commands' allowed-tools, docs/governance + startup routers, the .harness/council canonicals, AGENTS.md + the Codex parity note, all 30 .agents bridges, the roadmap's live schema clauses, the justfile comment) now names the transcript-brief review — a fresh-context Agent reviewer on a written session brief (fork where supported) — instead of the never-provisioned advisor(); the Codex venue's contract is fail-closed (isolated guard-shape exec where invocable; unattended arcs HALT at the owing fork, DEFERRED-HIL shape; resolve/optimize-claude-md carry the decision-time fork-defers exception), witnessed by tools/hooks/test_skill_advisor_reconciliation.sh (25 checks, auto-collected by the codex-parity glob, every check mutation-probed). Out-of-family loop ran the full ten-round budget r1–r10 all-BLOCK-absorbed-adjudicated (r11 GATE_REFUSED at the budget — the register-and-hold point taken as designed); 3-lens merge gate ALL-APPROVE at 470a2b300. Named residuals: B-226 (unattended review-debt halt is a prose floor pending an executable gate — a class-11 surface), historical records keep the old name by design. Remaining §8 sequence: U-SR-07…08 (R1), then U-SR-09 (R2), then U-HE-36-as-R3 with U-HE-51 riding it; then U-SR-07 as the next unit.

**Prior next action (post-#1473).** U-HE-50 (R1: C-HE-27 §5 X6a span emission) landed at #1473 — the logged review wrapper now emits the verify start/end edges at its own process boundaries (first-write-wins keeps the durable pair the round-1 window; a refused launch or in-process GATE_REFUSED opens/completes no pair; partial-write arms tested fail-start/fail-end/crash), and the interim absorb/edit emission block rides BOTH roadmap-continue carriers (Claude + Codex bridge, executable commands inline, deletion trigger = the B-218 wrapper-internal emitter, which also owes the in-process-refusal TOCTOU close). The arc dogfooded its own unit: the u-he-50 reservation carries all five span pairs (U-HE-35 baseline was 0 of 60 expected edges). Ten codex rounds (20 findings: 10 absorbed, 10 rejected with witnessed grounds, all adjudicated) + 3-lens merge-gate all-APPROVE round 1. The next implementable unit is `U-SR-01` (R1: preflight — three new classes + four grep shapes + planted-defect evals for both P1 shapes, Charter WR-01/02/03/07); the remaining §8 sequence after it: U-SR-02…08 (R1, order-free), then U-SR-09 (R2), then U-HE-36-as-R3 with U-HE-51 riding it.

**Prior next action (post-#1458).** U-HE-34 landed at #1458: durable phase spans + N6 (C-HE-27) — `phase_spans`/`n6` in `tools/arc_metrics.py` (numerator windowed to contributing arcs, interval-arithmetic downtime exclusion, fail-loud reversed pairs), the queue/execute/absorb/edit/verify/verify_unavailable emitters wired through `reservations.py phase` with a holder fence at the store's write funnel, the guard's positional exact-shape allows, and the `review-with-failover-logged` + `tools/round_log_publish.py` round-log producer (O_NOFOLLOW dir-fd walk, write-once link install, destination pinned under `.harness/tmp/`). C-HE-27 §1's `capture` pair and `result_capture` split are registered on `B-218` (structurally session-unrecordable — spec reconciliation owed) with the per-round-record representation limit; `B-217` gained the reservations-verb `update` sibling. The arc ran 10 codex rounds to its own live BUDGET_EXHAUSTED refusal and a 3-round merge-gate to ALL-APPROVE. Then the next implementable unit is `U-HE-35` (`tools/reviewer_concurrency_probe.py`, plan S6).

**Prior next action (post-#1434).** The next implementable unit is `U-HE-32` (S4d — `hook_git_retry` in `tools/hooks/lib.sh`: bounded retry with full jitter `{base 100 ms, factor 2, cap 5 s, max 8}` on local `index.lock` / `.lock: File exists` contention, exhaustion failing the git op and emitting a `NOTIFY` under a `git-ref-lock:` cause that never routes to the merge-door budget, per C-HE-11 §3) per `.harness/plan/Implementation_Plan_HE_Loop_Lanes_v1.md`. U-HE-01..31 have landed; 14 units remain (U-HE-32..45). U-HE-31 landed lane initialisation (C-HE-11 §1/§2/§4/§5): `tools/hooks/lane-init.sh` exports a per-worktree `HARNESS_LANE_ID` (minted once, published temp-then-`ln`, the marker authoritative over the environment) and `HARNESS_LANE_INDEX` (exclusive-create claim, one per worktree, post-verified against a concurrent init), sets `gc.auto 0` once, and gates the R-420 stack on machine-class plus available-memory headroom; `tools/lane_ports.py` is the sole authority for the per-lane Compose project and the `30000 + 100k + {0..3}` port block, with lane 0 unchanged; teardown brings a lane's stack down with its volumes before recycling its index, fencing an index it cannot verify. Nineteen out-of-family rounds plus three merge-gate lenses; 117 hermetic shell assertions and 308 permission-guard assertions. Two review-driven fixes were REVERTED as worse than the leak they addressed (a stranded-claim reclamation, and a legacy-marker migration that would have orphaned reservations). It carries three registered obligations: **B-201** (a failed `gc.auto 0` write warns rather than refusing the lane — its close-out condition is U-HE-32's bounded retry, making the next unit the one that settles it), **B-202** (one missing primitive with four symptoms — a claim↔cleanup lock, available memory inside the Docker VM, the stranded claim after a reaped worktree, and at-most-one claim under a race whose participants cannot see each other), and **B-200** (per-lane telemetry endpoints — the stack is isolated, its consumers still dial lane 0).

**Prior next action (post-#1430).** The next implementable unit is `U-HE-31` (S4d — `tools/hooks/lane-init.sh`: mints `HARNESS_LANE_ID` / `HARNESS_LANE_INDEX` by exclusive create of `QUEUE_DIR/lanes/<k>`, sets `gc.auto 0` repo-wide once idempotently, probes RAM headroom before a lane-`k >= 2` stack below the 32 GB floor, and parameterizes the `compose.yaml` ports + `-p` project name per C-HE-11 §1-§5) per `.harness/plan/Implementation_Plan_HE_Loop_Lanes_v1.md`. U-HE-01..30 have landed; 15 units remain (U-HE-31..45). U-HE-30 landed HIL gate coalescing by `cause_signature` with windowed pull-based delivery (C-HE-10 §1-§4) after 5 out-of-family review rounds and 12/12 mutation probes; it carries one open obligation — the Class 1 fork filed at `.harness/class_1_fork_c_he_10_delivery_coverage_rule_swallows_adjacent_generation.md` (#1431) shows C-HE-10 §2 delivery-coverage rule permanently swallows an adjacent same-cause generation, so the implementation uses per-member coverage and the spec amendment (C-HE-10 §2 + C-HE-09 §5 row shape, version bump, change-note, clearance marker) is owed on ratification of Reading B.

**Prior next action (post-#1416).** The next implementable unit is `U-HE-26` — push-to-main client-side emit_deny predicates in the audited deny block (C-HE-08 §1, `_push_targets_main` parser + bare-push-on-main checkout case); then U-HE-27 (`tools/main_protection.py` + `just main-protection-{show,apply,rollback,tiebreaker,verify}`, C-HE-08 §2–§5) and U-HE-28 (`--emit-refresh-pr-json` refresh producer — also flips safe-merge.sh's pre-lease guard and its real-CLI test pin per the U-HE-25 rev note).

**Prior next action (post-#1413).** S4c continues: U-HE-22 is on main (#1413). The recommended next action / next implementable unit is `U-HE-23` (merge-door landing steps (ii)–(ix) — land() driver: gh head/base verify, local-base-cas-check vs attested_merge_tree, mark-attempted-before-merge, bounded 120s gh pr merge, confirmed flip to merged, 45-min post-merge-CI + refresh continuation under the held lease, timeout/crash reconcile, caller backoff, §9 gate rows; C-HE-06 §1/§4/§5/§8/§9/§10, C-HE-19 §2, C-HE-12 §2 hook; deps U-HE-14 + U-HE-17 + U-HE-22 landed; plan §S4c + second half of tools/test_merge_door.py incl. AC#2(c) subprocess crash-resume suite), then U-HE-24/U-HE-25 per plan §3. Registered residuals routed to U-HE-23: the mark_blocked-outside-marker-namespace check-then-act (merge-gate r1 P3, rev item xvi — driver-ordering fix), the symlink-swap TOCTOU dirfd/O_NOFOLLOW design candidate (rev item xv c), the cross-process marker-race/claim-ABA witnesses (rev item xv b/d — the AC#2(c) suite), and the completion-claim crash window ground-truth recovery. At U-HE-25: the exact-shape guard allowlist additions (reservation CLI verbs, HARNESS_ARC_ID=/HARNESS_LANE_ID= env-prefix strip, git merge-tree — headless lanes cannot reserve until then). At U-HE-31: atomic lane-id mint. At U-HE-29: detached-launch supervision, RESOLVED-HIL minting, emission dedup, §0-vs-§1 ordering contradiction, activation-gate go-live witness. Alternatives if the operator defers the lanes program: B-186 step (1), B-160, B-148 (unchanged).

**Prior next action (post-#1403).** S4a is on main. The recommended next action / next implementable unit is `U-HE-17` (S4b arc reservation: tools/reservations.py generation-CAS record, alloc_seq, reserve with required arc_type, transition re-validate, walk_terminal, gc, mint_lane_id — C-HE-03 §1-4/§8, C-HE-26 §1), then S4b U-HE-18/19/20/21 per plan §3. Registered S4a residuals close in S4b: the drain⇄reservation integration (U-HE-19) owns the displaced-owner fence + claim-in-flight recovery window + exit-0 scan-window; U-HE-20 owns the concurrent takeover + kill-after-append certificates. Alternatives if the operator defers the lanes program: B-186 step (1), B-160, B-148 (unchanged).

**Prior next action (post-#1401).** **S3 is on main (PR #1401, squash `47d4428f`)** — `U-HE-14` durable store audit: `.harness/spec/store-audit-he-loop-lanes.md` (eight C-HE-30 stores, one authority each; families classified `derived` / `part of store N` / `sole carrier (new fact)`; transient non-stores) + `tools/test_store_audit.py` (6-test phase0 witness; idiom-complete literal extractor — pathlib chains, chained/single-quoted/call-root joins, glob/with_suffix/.tmp, templated shapes → `*.*`, shell `$(..)/x` + `${VAR:-d}/x`; segment-bound listing; LANDED/PENDING promotion contract) wired into §8.1 (`Row C-HE-30`, 18 phase0 rows PASS) and `codex-parity-check.sh`. The audit forced **spec v1.3 X3** (three wording edits inside C-HE-30: clearance-fold parenthetical → "derived OR sole carrier of a NEW coordination fact, never a second authority for an existing fact"; count sentence; Verification wording) — marker `spec-he-loop-lanes-v1.3-cleared-2026-08-19.md`, artifact-heads v1.3/4, plan U-HE-14 body revised to match (rev note at Status block). 11 `review-with-failover` rounds (r11 APPROVE) + 3-lens merge-gate 3× APPROVE recorded through U-HE-13 machinery (landing-delta TRANSFER). **Residuals registered:** U-HE-30 HIL claim-before-delivery crash window (claim ≠ delivery — audit HIL row); extractor covers named idioms only (a genuinely novel path idiom in S4 needs a pattern row — the per-module non-vacuity assert is the tripwire). The recommended next action / next implementable unit is `U-HE-15` (S4a drain fault isolation: per-arc `_drain_one`, three FNF guards, systemic-abort split, restore-or-republish, `ARC_METRICS_TEST_KILL_AFTER` seam, teardown ahead-of-`@{u}` refusal), then S4a `U-HE-16` per plan §3. Alternatives if the operator defers the lanes program: B-186 step (1), B-160, B-148 (unchanged).

**Prior next action (post-#1395).** **`U-HE-01` is on main (PR #1395, merge `c0430832`)** — the C-HE-24 finding record every later unit emits into: `tools/finding_record.py` (8-field core + envelope, closed schema at `tools/review_schemas/finding_record.schema.json`, write-time lifecycle rules, one flock'd critical section per append, `next_finding_id`/`append_observation` per-observation id minting, file-order reducer, `Finding` projection, `validate` CLI), 36 tests, 15 mutation probes PINNED, registered in `tools/codex-parity-check.sh`. **Bundled and reversible:** `Spec_HE_Loop_Lanes` v1 → **v1.1** change-note X1 corrects C-HE-23 §2's mechanism parenthetical (a C-HE-24 row cannot fit under macOS `PIPE_BUF`; the clause now states the regular-file guarantee the code carries) — successor marker `.harness/clearance/spec-he-loop-lanes-v1.1-cleared-2026-08-18.md`, `artifact-heads.md` regenerated; **operator may reverse via a v1.2 note** (`_append_line` is the single site). Ten out-of-family codex rounds (P1 yield 2/1/0/0/1/0/1/0/1/2, every finding absorbed with a probed witness; R9→R10 oscillation on `round_n` resolved on the spec-literal reading) + the 3-lens merge-gate (L1/L2 APPROVE, L3 BLOCK → `append_observation` concurrency witness → APPROVE); the stopping rule was applied at round 10 with three residual classes named in the PR body. **Interface notes later units MUST absorb (deviations from the plan's literal code):** U-HE-02 `outcome_rows` / U-HE-13 mint ids with `append_observation` (or `next_finding_id`), NOT `enumerate(..., start=1)`; a null-`head_sha` row mints under the `nohead` token; only `finding` rows are adjudicable; `equivalence_proof` rows (U-HE-41) carry `accepted` + the decorrelated prover inline; `finding` rows require non-empty location / evidence / expected contract; adjudication `ts` is strictly later. **The recommended next action is the S1 cluster as ONE PR in plan §3 order — `U-HE-03` (verdict schemas, data first) → `U-HE-02` (`review_wrapper_common`) → `U-HE-04` (`codex_review.py` fail-closed wrapper + `just codex-review` reroute) → `U-HE-06` → `U-HE-07` → `U-HE-08` → `U-HE-09`;** then S2 (`U-HE-05`, `U-HE-10..13`; U-HE-05 adds the `.harness/mutation-probe-log.jsonl` emitter and this unit's manifest rows). Carry-forwards for `U-HE-44`'s register rows: the X1 note, U-HE-01's three residual classes (lifecycle edges for kinds no unit emits yet; per-observation ids vs N6's DISTINCT count on re-observation; legacy-log reader policy), the `severity` dual vocabulary (plan §6 #1). Alternatives if the operator defers the lanes program: B-186 step (1), B-160, B-148 (unchanged).

**Prior next action (post-#1393).** The H_E autonomous-loop + parallel-lanes design chain is on main and cleared: `.harness/adr/ADR-HE-1..4` → `.harness/spec/Spec_HE_Loop_Lanes_v1.md` (35 `C-HE-*` contracts; clearance marker `.harness/clearance/spec-he-loop-lanes-v1-cleared-2026-08-18.md`) → `.harness/plan/Implementation_Plan_HE_Loop_Lanes_v1.md` (45 atomic units `U-HE-01..45`, milestone-led on spec §6 S1–S8; five out-of-family codex rounds absorbed, terminal record at plan §7 item 8 with six residual defect classes carried by unit execution). Namespace is H_E dev tooling only (spec §0.2) — no H_T design surface, no X-AL-3 implication. **The recommended next action is executing the plan in its §3 topological order, starting at `U-HE-01` (finding record — `tools/finding_record.py`, C-HE-24): one unit per RED-first cycle, one PR per S-step cluster through ship-pr, per-PR `just codex-review` + `merge-gate` on code PRs;** the first content PR also owes `just roadmap-status --archive-superseded` (the post-#1390 round). Phase 0 = S1–S4 = `U-HE-01..33` gates running N ≥ 2 lanes at all (`just lanes-phase0-check`); the single operator gate in the whole plan is `just main-protection-apply-confirm` (C-HE-08, one AskUserQuestion with the printed diff). U-HE-44/45 register the spec §11 carry-forwards, the C-HE-06 §7-vs-G4 invariant-wording tension (plan §6 item 13, v1.1 change-note candidate), and the six residual classes as forward-register rows. Alternatives if the operator defers the lanes program: B-186 step (1) grounding (unchanged from post-#1390), B-160's conformance sweep, or the standing R-CTX-1 program at B-148.

**Prior next action (post-#1390).** B-137 is CLOSED — OD spec v1.42 §0.2 landed candidate C1 (`workflow.envelope` as C-OD-09 §9.2 row 20) with plan v2.36 / U-OD-61, both clearance markers, and the back-flow record at `.harness/b137-c1-admit-the-envelope-root-2026-08-16.md`. Build-time grounding reframed the arc twice and both reframings are load-bearing for whoever reads the row next: (i) C1 is an INGESTION REPAIR, not a Phase-7 design extension — C-OD-25 §25.3 (`Spec_Operational_Discipline_v1_8.md:90`) has declared the envelope's head=1.0 floor since v1.8, preserved verbatim through v1.9→v1.41, and §9.2 simply never absorbed it; (ii) the ratification's carried-forward "data loss, not delay" cost was already superseded by B-136's #1331 repair, and the arc's own replacement claim ("no configuration loses a span the status quo would have kept") was ALSO false and is withdrawn — it compared cardinality, and by trace identity C1 displaces above `max_buffered_traces` while strictly dominating below it. Two residuals were split to their own rows rather than left on a closed parent: **B-185** (concurrency threshold — displacement above the cap, dormant on the same Persona §11 open-item-4 throughput denominator as B-182/B-183) and **B-186** (the floor does NOT survive an unsampled ambient OTel parent; confirmed reachable via the MCP `copy_context()` path, registered rather than repaired because forcing the envelope to a trace root trades the observability floor against distributed-trace continuity). **The recommended next action is B-186 close-out step (1)**, which is fully Claude-executable and gates everything else on that row: determine by grep + execution whether any SHIPPED entry point can begin `api.run` / the MCP daemon under an ambient OTel span that is unsampled — the harness's own bootstrap does not, so the row may be dormant-by-construction, and that finding is worth as much as a repair. Only if step (1) finds the venue real does the (a) forced-root / (b) parent-as-link / (c) ratify-the-scope fork become live, and only then is the C7-versus-trace-interop council owed. Alternatives if that grounds out: B-160's conformance sweep (its head=1.0 divergence class grew four → five at this arc, and its close-out must now re-derive from a scope wider than C-OD-30…33), or the standing R-CTX-1 program at B-148.

**Prior next action (post-#1380).** The register stands at 36 `registered_finding` + 1 `open` (B-148) + **3** `design_substrate_gated` + 2 `held` / 142 closed of **184** rows. **`B-183`'s owed fork doc is FILED** at `.harness/class_1_fork_b183_tenant_limit_quantity_declared_two_ways.md` — close-out step (2) is discharged. **What the filing established, after grounding falsified TWO of the prior round's framings.** #1378 recorded that *"the OD plan's own field signature says SPANS/SEC"* for the tenant cap. **It does not** — that comment sits on `cell_rate_limit` (`Implementation_Plan_Operational_Discipline_v2.md:978`); the tenant field at `:977` carries **no unit at all**, and every plan surface speaking of it in prose says **cardinality**, including `:2186` (the acceptance criterion for that exact function) and `v2_6:721` (the counter's declared shape, which the shipped code carries **verbatim**). A first fork draft therefore concluded *"one label against everything else"* and recommended (A); **out-of-family [P1] falsified that** — v1.37 does not merely mention `spans/sec`, it **rules** that the caps bound admitted span throughput and at `:40` leaves explicitly **OPEN** *"whether the realized keep-volume approaches the per-tenant 1,000 spans/sec budget"* (carried as `B-133` step (3), re-stated at v1.38`:69`); a distinct-series bound makes that question **malformed**. A second draft then **over-corrected**, ranking the spec passage over the plan via the §1.3 chain; **[P2] falsified that too** — v1.37 declares *"exactly ONE amendment"* (to C-OD-09, `:3-8`) and lists **C-OD-11 §11.1 UNCHANGED** (`:106`), as does v1.39 (`:47-58`), and §1.3 orders artifact **levels**, not passages within one artifact. **STABLE SHAPE: an INTERNAL CONTRADICTION inside the OD spec lineage.** `C-OD-11`'s **contract body** frames the cap as cardinality and the plan and shipped code implement exactly that; two later deltas' **change-note prose** reasons in `spans/sec` while disclaiming any change to `C-OD-11`. **No codified rule breaks the tie** — resolution needs **`ADR-D6` §1.3** (a bracket placeholder here at `ADR-D6_v1_2.md:227-229`; the design-phase workspace holds the body, and reading it is the cheapest first probe) or a new OD amendment. The fork **recommends no disposition** and keeps the evidentiary argument separate from a precedence rule so the menu stays neutral. **(A)'s named price:** it invalidates v1.37's open population question and `B-133` step (3), which must be **re-expressed, not re-worded**. **TWO decisions are routed, not one** — the quantity, and separately the temporal interpretation (standing cardinality / per-window / a rate). **`B-182` is DECOUPLED — #1378's claim that one decision governs both halves is WITHDRAWN.** `cell_rate_limit` is declared a span rate at plan `:978`; the two fields plausibly carry different quantities by design. Both rows' close-out steps were narrowed to **tenant-side only** after review caught them giving opposite instructions. So **`B-182` is once again an un-gated actionable candidate.** **NEXT SLICE.** `B-137` remains DEFERRED to the operator (architectural ratification). Un-gated actionable candidates: **`B-182`** (disposition FACT 1 + FACT 2, deriving placement from `C-OD-20` §20.1), `B-44` (no production bootstrap injects a non-default `time_source`), `B-94` (derived-index rebuild overlap race), `B-104` (no pause-resolved marker), `B-172` (doc-only grounding sweep batching ~20 rows). **One impl-only follow-up is OWED and small:** `harness-od/tests/test_b183_tenant_limit_quantity_conflict.py`'s `test_the_plan_signature_declares_spans_per_second` reads `:978` — the **cell** limit — inside a module about the **tenant** limit; true, still green, wrongly relevant. Fork docs match the codex guard's `DESIGN_RE`, so it could not ride #1380. **A LOCAL-ONLY BLOCKER, operator-owned:** `just codex-check` reds because `.codex/hooks/stop_gate.py` inspects the ROOT checkout, which now carries the operator's Graft install (`.claude/settings.json`, `.gitignore`, `.mcp.json`, `AGENTS.md` modified plus untracked helpers) alongside four untracked `.claude/skills/` dirs. Pre-existing and verified by stashing; CI clean-checkout green throughout, 19/19 on #1380. **STANDING CAUTION, ten consecutive arcs:** every failure was a claim asserted from a LABEL or a convenient surface instead of the thing that determines it. This arc added a sharper variant — **an absorption can over-correct past the truth**: round 1's [P1] was right that v1.37/v1.38 are normative, and the fix I built on it (an authority-chain ranking) was itself wrong, caught only at round 2. **When a review overturns a claim, fix the claim — do not import a NEW rule to replace it without checking that rule holds.**

**Prior next action (post-#1378).** The register stands at 36 `registered_finding` + 1 `open` (B-148) + **3** `design_substrate_gated` + 2 `held` / 142 closed of **184** rows. **`B-137` is DEFERRED to the operator this run** (step-3 posture changes a committed per-cell base-rate envelope; both council halves grounded at #1362/#1364, measurement withdrawn with its requirement at #1366). **`B-183` TRANSITED to `design_substrate_gated` at #1378, fork doc OWED — and it now GATES `B-182` too.** Grounding located a three-way quantity conflict on the same limit: **`C-OD-11` frames the contract as CARDINALITY** (*"Cardinality budget per cell"*, ADR-D6 §1.3, and `C-OD-21 §21.4`'s *"Per-tenant cardinality isolation"* row, with §11.2/§11.3 using cardinality as **distinct values**); **the OD plan's own field signature and TWO CLEARED SPEC DELTAS say SPANS/SEC** (`Implementation_Plan_Operational_Discipline_v2.md:978`, v1.37, v1.38); **the shipped code compares SERIES** (`observed_series`). Spans and series are not interchangeable, so at most one reading is right and the choice changes what a **compliance** surface measures at the two multi-tenant cells. **The owed fork doc carries TWO decisions, not one** — the **quantity**, and separately the **temporal interpretation** (standing cardinality vs distinct-series-per-window vs a cardinality rate; the carrier already records an `observation_window`). **`B-182` must NOT be settled independently:** its `cell_rate_limit` carries the same *"spans/sec"* documentation, so one decision governs both halves. **NEXT SLICE — with B-137, B-183 and (transitively) B-182 all now gated, pick from the un-gated actionable set**, e.g. `B-44` (no production bootstrap injects a non-default `time_source`), `B-94` (derived-index rebuild overlap race), `B-104` (no pause-resolved marker), or `B-172` (doc-only grounding sweep batching ~20 rows). **A LOCAL-ONLY BLOCKER, unchanged and operator-owned:** `just codex-check` reds on `tools/test_codex_stop_gate.py` because `.codex/hooks/stop_gate.py` inspects the ROOT checkout, dirty with four untracked `.claude/skills/` dirs standing since session start; verified pre-existing by stashing, CI clean-checkout green throughout. **STANDING CAUTION, nine consecutive arcs:** every failure was a claim asserted from a LABEL or a convenient surface instead of the thing that determines it — and TWICE this session the specific form was **reading a frozen historical delta instead of the effective head**, most recently in the very witness written to pin a gated row (closed with a head-advance tripwire on `v1.41`/`v2.35`). **Before pinning any design-substrate quote, ask what a RESOLVING delta would leave untouched.**

**Prior next action (post-#1350).** **`B-169` IS CLOSED (#1350), AND STEP 1 CORRECTED THE ROW'S OWN CENTRAL NUMBER.** The row said the S16 fixture sat "one minute" from the 30-minute STALE boundary. It never did: `touch -t` takes MINUTE granularity, so `date -v-29M +%Y%m%d%H%M` truncates seconds to `:00` and the quiet-side fixture's real age was uniform in [29m00s, 29m59s] — headroom was a SAWTOOTH decided purely by the wall-clock phase the suite happened to reach S16 at (measured: 59.29s at phase `:00`, 1.74s at `:58`, **0.70s at `:59`**). POSITIVE CONTROL against the real hook: at phase `:59` under 12 CPU spinners S16 went RED with the PREDICTED deviation — the 29-minute fixture was ALSO counted, emitting `2 unreconciled subagent(s) (oldest: s16stale, 31m).`, the same shape seen on main at `be081e9d` — while the identical run idle PASSED, which is exactly why per-test re-runs never caught it. Mechanism CONFIRMED, not falsified. STEP 2 fixed the HEADROOM, not the threshold: both pins stay at 29/31 (deliberately NOT the row's own registered candidate of moving the quiet side to ~20 minutes, which buys headroom by UN-PINNING the constant — the `B-143` non-discriminating-witness failure), via exact `os.utime` stamping plus RE-STAMPING the quiet side immediately before the assertion that re-evaluates it; budget spent drops to **0.48s of 60s at phase `:59` under 24 spinners**, phase-INDEPENDENT. **THE ARC CORRECTED ITSELF ONCE AND THE GATE PROVED THE CORRECTION LOAD-BEARING:** a first shape SPLIT the fixtures onto two registries — same headroom, but it dropped the witness that a stale entry must not drag its fresh sibling into the count; merge-gate L3 independently built that mutant, showed the split FALSE-PASSES it, and established S16 is the ONLY test in the suite pairing a fresh-and-quiet key with a stale-and-flagged key. **CODEX CORRECTED MY SCOPING TWICE ON `B-179`**: my "Claude-side-ONLY" framing was wrong because `AGENTS.md:1-3` declares itself the authoritative projection FOR CODEX — the real finding is that Codex's guidance is INTERNALLY INCONSISTENT (`.agents/skills/*` name `just codex-check`, its root projection names `just check`). Gate 3/3 APPROVE; codex 3 rounds 0 P1 / 4 P2, converged clean; CI 18/18. **FORWARD: `B-166` is now the LAST original member of the `B-166`/`B-169` flake class and the most dangerous of the four** — its own row says it can MASK a genuine red in the fan-out driver that the `B-71` impl leg touches. Grounded this session: `harness-cp/tests/test_workflow_driver_orchestrator_workers_fence_ledger.py:1092-1129` monkeypatches `_DEFAULT_FANOUT_BARRIER_DEADLINE_SECONDS` to 0.3 at `:1103`, scripts worker-1 to block 1.2s, and asserts `_branch_terminals(ledger) == {0: "completed", 1: "timed_out"}` — i.e. that worker-0's stash lands BEFORE the 0.3s barrier deadline, with NOTHING synchronizing the two. Its step 1 is a FULL-SUITE reproduction (the venue it appears in); the 25-run isolated probe already recorded is a NEGATIVE control and must not be mistaken for a falsifier. Two cheaper newly-registered items also sit open: **`B-179`** (recommendation (c) — align the three weak carriers with the `.agents/` mirrors; docs-only and already proven by those mirrors) and **`B-180`** (S16's quiet-side assertion is ABSENCE-based, so a hook that emits nothing reports a PASS — and `loop-gc.sh:114-118` makes the sweep DELIBERATELY failure-invisible, so silence is engineered and reachable). The one genuine operator gate remains **`B-165`**'s three-way spec repair.

**Prior next action (post-#1347).** **`B-176` IS CLOSED (#1347) AND THE ARC REGISTERED TWO NEW ROWS, ONE OF THEM A REPRODUCED CI FLAKE.** The `B-166`/`B-169`/`B-176` class — an assertion whose truth depends on how busy the machine is — now has a FOURTH member and its first fully-reproduced one. **What closed:** `B-176`'s step 1 was discharged BY EXECUTION rather than code read (the gap its own row named): the unchanged 100-span flush measures median 2.51 ms / max 4.54 ms idle (n=40) and median 5.96 ms / max 139.92 ms with 3 of 60 BREACHING under 24 cpu + 8 io spinners. The median moves 2.4x while the tail moves 31x, and the breaches form a discrete ~130-140 ms cluster — a scheduler stall on the two `asyncio.to_thread` hops inside the timed window, not slow work. Repaired as best-of-5 with the 100 ms threshold untouched and the assertion retained; each attempt uses a fresh database and asserts its own `inserted == 100`, which together make every attempt a real attempt and make a violation loud rather than vacuous. **THE HEADLINE PROCESS FINDING: A MUTATION PROBE THAT FAILED TO KILL WAS KEPT AS DATA RATHER THAN RETRIED AWAY.** The first annotation named a per-row `execute`+`commit` at `sqlite_span_store.py:159`; it measures 33-57 ms and PASSES. Escalating severity until something kills and keeping only that would have been honest-looking and misleading, so the non-kill is recorded IN the annotation beside the verified kill (`PRAGMA fullfsync=ON` + per-row commit, ~2.0 s, fails all 5). The gap it exposed — a 100 ms budget against ~2.5 ms of work discriminates only >=40x regressions, never a 16x one, and best-of-N did NOT introduce that blind spot because the single-sample form had it identically — is **`B-177`**, registered not patched because tightening the number re-specs a stated contract. **`B-178` IS THE NEXT AUTO-`ACTIVE` ITEM** and is the highest-value forward row: `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp` reddened this very PR's CI on a module the diff never touched, and the mechanism is REPRODUCED, not hypothesised — `_record_observations` builds its map only from `past_ttl_names` (`protected_result_store.py:1343-1346`), so at the intermediate sweep the recreated file (age 0.90 s vs a 1.0 s TTL) is NOT past-TTL and its backdated row is DROPPED; at the final sweep it IS past-TTL, so the row is RE-CREATED from `time.time()` (`:1342`, because the call omits `observed_at`), leaving a SYNTHETIC `now` compared against a REAL `first_observed_at` and only ~0.15 s of headroom across two sweeps, a JSON read+write and three file ops. Injecting `time.sleep(0.25)` reproduces it exactly; removing it restores green. **Its step 2 is NOT a margin widening and must not be attempted as one** — passing `observed_at = now` at the final sweep makes the grace conjunct unsatisfiable and reds the test deterministically, so the repair must first settle which `first_observed_at` the scenario intends, and it must preserve what the witness proves about the ratified `B-110` residual. **The one genuine operator gate is still `B-165`'s** three-way spec repair (narrow NOTE 6-i / widen the CP §0.4(2) basis / build the missing workflow-validation layer), untouched by this arc. **PROCESS LESSONS PAID FOR HERE:** (1) a stable log size is NOT a finished reviewer — codex sat silent inside its own `just check` and a size-stability probe read as "done"; verify the process, not the artifact's quiescence; (2) "no checks reported" is a CI gap between runs, not completion, and a waiter keyed on absence-of-pending fires falsely there — require the checks to EXIST first; (3) an unrelated `findings` JSON in an ambient TTS process nearly read as this arc's verdict — confirm a verdict's provenance before believing it.

**Prior next action (post-#1340).** **`B-170` IS CLOSED AND THE MEASUREMENT GATE IS OPEN (#1340; main GREEN).** The arc-metrics ledger landed with a 16-row `levers_active: []` baseline, so `B-171`..`B-174` are now evaluable as cohort comparisons rather than assertions — but the next action is unchanged: the `B-71` impl leg, **`U-CP-102` + `U-RT-155`, CO-LAND PINNED** (neither is independently observable). Read the plan ACs first, since three encode defects review already caught: derive the basis UNCONDITIONALLY at both `workflow_driver.py` composition sites; the fold must reach ALL THREE `compose_hitl_action_id` invocations; the identity witness is split by venue because `_escalate_to_secondary_channel` is `NoReturn`. **Capture your own arc:** `just arc-metrics queue --pr <NNN> --arc-type <inventing|applying> --decisions <N>` at closure (writes OUTSIDE the repo — never dirty a topic worktree), then `just arc-metrics drain` early in the FOLLOWING arc; drain exits NON-ZERO while anything is outstanding and that is not a failure, since a capture is released only once its row reaches MERGED history. **Read the summary's own caveats before quoting a number:** `arc span` is a LOWER BOUND (round-log mtimes mark round COMPLETION, so round 1's own duration is missing — `B-171` is what supplies real start timestamps), it is never pooled with `PR-open window`, and partial rows are excluded from the exact lines. **Use what landed earlier:** `just leg-selfcheck` BEFORE EVERY PUSH (per ROUND, not per arc); install this pointer with `--refresh --next-action`, never by hand; archive the superseded round with `--archive-superseded` INSIDE the content PR. **Registered, not fixed:** `B-175` (queue/drain cross-process residue — two parallel worktrees hold DIFFERENT ledgers, and the dead-claim takeover is not atomic; unreachable in the single-lane flow, filed with an executable two-process falsifier), plus `B-166`/`B-169` grounded-but-unreproduced wall-clock flakes — **do NOT wave away a red as "the known flake"** — and `B-167`, a stated bound on leg-selfcheck. **Process note from #1340:** the out-of-family reviewer truncated mid-diff past ~2000 lines and TWICE flushed its verdict only AFTER process exit; reading a log too early made a completed review look clean, and treating that as "reviewed clean" would have shipped four defects. Re-read every reviewer log after exit.

**Prior next action (post-#1336).** *`B-164(b)` is CLOSED — the late-arrival path landed. The forward queue has no auto-`ACTIVE` code arc; per §12.4.1 pick the highest-value item and ground it first.* **Forward items: (A) `B-137` step (3)** — the dyadic **C7 + C11** council, and the highest-value next item. Its C1 pricing **MOVED at #1331** (the *data-loss* cost was `B-136`'s, not C1's), so A′-vs-C1 shifted toward **C1** — brief the council on that first; all five options are already measured against executed numbers, so this is a ratification, not a re-measurement. **(B) the B-71 spec leg** — `#1325` is a DELIBERATE draft, not stalled: under-scoped (4 carrier amendments + a Runtime delta + 2 plan deltas + clearance markers), true shape recorded at #1326; a fresh session is the better authoring venue. **(C) `B-164` residuals (i)/(ii)** — registered at #1336, NOT absorbed; both need ≥4096 concurrent abandoned orphan threads, and (ii) (refresh a drained late-arrival's FIFO age) is ~2 lines and the better first move if picked up. **PROCESS NOTE (#1336):** closing a leak with a bounded structure makes every bound a NEW correctness surface — prefer an exact liveness-derived condition over a size window, evict only provably-dead entries, make expansion two-way. Flat-severity review rounds each finding an edge in the PREVIOUS round's bookkeeping = register-and-hold, not keep-absorbing. **OPERATOR OWED: (1) E7 move then `just context-budget --sessions 1` → flip B-148 if ≤76k; (2) Codex `/hooks` re-trust; (3) B-124 ratification (rec D at #1283); (4) `#1292` has been open since 2026-08-10 from a prior session.** `held=2` honored.

**Prior next action (post-#1293).** **R-CTX-1 Wave 0 IS MERGED** (PR #1293, squash `42f95fe3`): the operator-approved context-optimization program is registered (forward-register **B-148** open / **B-149** held, snapshot 147→149; execution doc committed at `.harness/r-ctx-1-implementation-plan-v1.md` with errata E1–E7 from six codex review rounds) and the U-CTX-01 config-hygiene baseline snapshot + machine-local changes are applied. **Program execution continues per the committed plan:** next serial merges are **PR-1** (U-CTX-02: `.mcp.json` RB-SUB-03 cleanup + `tools/context_budget.py`, measured baseline first-turn median 107.4k) then **PR-2** (Arc 2, built + orchestrator-reviewed in worktree: roadmap_status truncation + witnesses + query-not-Read prose + artifact-pointers split). **Gate G1:** #1292 is Codex-owned and still under Codex's own review — poll, never touch; Wave 2 (Arc 3 hooks, Arc 4 heads table) launches only after #1292 merges (Arc 4 also after PR-2). The **`B-124` HIL ratification** remains the open operator gate for the harness-code frontier (unchanged from post-#1290; queue after it: `B-147` → `B-145` → `B-144`). `held=2` honored (R-1, B-149).

**Prior next action (post-#1290).** **`B-143` IS CLOSED END TO END** (PR #1290, squash merge `6108ac32`) as a test-only timing-hygiene arc: four shutdown deadline witnesses now use structural attempt/start, cancellation, and non-completion state; cold-child, Antigravity CLI, Codex session-start, and merge-proof classifier witnesses no longer race arbitrary startup thresholds; the live-child deadline and loop-owned executor restoration are independently mutation-discriminating. The five affected modules pass **294 tests**; the three-lens gate is all-approve after scoped repairs; OAuth-only Antigravity **Gemini 3.1 Pro (High)** returned `ARTIFACT: COMPLETE` / `VERDICT: APPROVE`; the final PR head and the content merge each passed **16/16 CI checks**. The merge gate also reproduced a distinct production defect and registered **`B-147`** rather than absorbing it: `flush_observability()` can queue `force_flush` indefinitely behind a saturated default executor before tracer close. Register: **147 items / 0 open / 116 closed / 30 registered_finding / 1 held**. **PAUSE FOR HIL:** the next operator-owned gate remains the **`B-124` ratification**; do not silently select a reading. After that HIL, the next Claude/Codex-executable queue is **`B-147`** (bounded-shutdown conformance repair), **`B-145`** (mandated retry attributes/terminal value with missing producers), then **`B-144`** (the §24.1.B export-count venue decision, council-conditional after re-verification). `held=1` honored.

**Prior next action (post-#1285).** **THE `B-126`..`B-132` GROUNDING QUEUE IS DISCHARGED** in ONE batch pass (merged `d1949fa1`) — every premise re-verified at HEAD by direct read, every behavioural claim by **EXECUTION** rather than grep. **TWO ROWS CLOSED, both on their OWN falsifiers rather than on judgement.** **`B-128`** — its close-out step (1) demanded an enumeration of `retry.fail_class` readers; it was run and returns SIX hits, five producers and one **presence-only** tail-keep trigger, with nothing branching on the value. The row states its own disposition for exactly that result. **`B-130`** — **PARTIALLY FALSIFIED**: claim A (a zero-attempt exhaustion is reachable) reproduced with `inner.calls == 0`, but claim B (that it is **permanent/irreversible**) is now FALSE — `B-118` landed, and the same repro past the cooldown window **RECOVERED** (`OPEN → half_open → closed` on a successful trial). The state is **bounded and self-healing**, a breaker working as designed, not topology corruption; the row's own step (4) predicted `B-118` might retire it. **THREE SHARPENED.** **`B-126`** — *this row's own count is wrong*: five undeclared wire keys, not four (the missed one, `retry.terminal`, landed at **#1065 BEFORE** the council that filed the row), so **TEN** distinct keys, not eight; and step (2) is answered by Runtime §14.6 (no cap — the discretion lane is intentionally unbounded), making it manifest-conformance, not a fork. **`B-129`** — one of its two stated obstacles is **already gone**: `fallback.exhausted` is now §9.2 row 19 via the landed `B-116-t3` leg, so C11's sampling condition is satisfied for the row's own preferred placement. **`B-131`** — the row says `last_failure_class` falls through to `"unknown"`; **it does not** — it is set to the skip reason (truthy), so the fallback never fires and the attribute carries a **real-but-wrong** value masking the actual failure class, which is worse than `"unknown"` because it looks legitimate. **THE BUILD PAIR:** **`B-127` + `B-131` are distinct findings on the SAME call site** — different attributes of the same `fallback.exhausted` event, neither fix resolving the other — and must land in ONE PR. `B-127` was confirmed by probe (both breakers OPEN, ZERO inner dispatches, yet `exhaustion_cause` reports `per-candidate-retry-exhaustion`). **`B-132` stays FORK-SHAPED**, narrowed by `B-115` to exactly two members with the hard question untouched. **NEW `B-143` REGISTERED** (not absorbed): a **zero-headroom** wall-clock assertion — `test_shutdown.py:894` asserts `elapsed < 0.3` while the simulated hang IS `0.3` and the bound under test is `0.02`, so scheduler noise flips it; observed flaking TWICE this session on DOC-ONLY PRs (#1280 at 0.345, #1285 at 0.307). It cannot simply be deleted — the sibling structural assertions do not discriminate the regression it guards. Register: **143 items / 0 open / 112 closed / 30 registered_finding / 1 held**. **NEXT:** (1) **the `B-127` + `B-131` PAIRED BUILD** — the highest-value Claude-executable item on the board: one call site, two kwargs, zero contract risk, both witnesses already designed and both defects already reproduced; (2) **`B-126`** (declare the discretion lane + a drift-catching test); (3) **`B-143`** (the scripted-clock hardening, #1226 precedent); (4) the **`B-124` RATIFICATION** — still the single open operator gate; (5) `B-129`/`B-132`/`B-134` deferred or fork-shaped; `B-135`/`B-136`/`B-137` council owed; `B-139`/`B-140`/`B-141` groundings. held=1 honored.

**Prior next action (post-#1283).** **THE `B-124` DERIVATION SUB-DECISION IS FILED AS A CLASS 2 FORK** (PR #1283, doc-only, merged `132ccebf`) — **and it is now the single open operator gate.** CP spec v1.116 §1.3 deliberately declined to declare a replacement wire derivation for `validator.fail.permanence` and routed the decision here; this files it and does NOT decide it. **`B-124` stays `registered_finding`; its status and `pr:` pointer ride the RATIFICATION leg per the `B-88` / `B-57`+`B-112` precedent** — the close_out gained a LOCATOR cross-note only. **FOUR READINGS** at `.harness/class_2_fork_b124_validator_fail_permanence_derivation.md`: **A-i** materialize the retry-exit classification at the emission site (`ValidatorResult` gains an optional `retry_exit_class` supplied by the Validator; permanence then derives through the EXISTING `validator_fail_permanence()`) — **runner-up, and the only non-lossy path**; **A-ii EXPLICITLY FORECLOSED** — deriving the class inside the framework would be *fabricating* a classification, not materializing one; **B** ratify the outcome-based narrowing WITH §21.6 re-specified — rejected on the workspace's own asymmetry (an addition later is free, a retraction later is not); **C** another carrier — rejected as compounding the reader divergence; **D** defer with a demand trigger — **RECOMMENDED**, with a named discriminator. **TWO GROUNDING RESULTS THE FILING ADDS.** (1) **The lossiness is confirmed from TWO directions, not one** — `permanent-fail-exit` and `HITL-recoverable` collide on `outcome=ESCALATE` **and** on `next_action=ESCALATE_HITL`, so neither disposition-shaped field the producer holds can separate them, and they carry OPPOSITE permanence. (2) **A hypothesis TESTED AND FALSIFIED** — §1.3's *"the only candidate the producer holds"* is loose in LETTER (`ValidatorResult` carries five fields; `fail_class` is already on the wire) but CORRECT IN SUBSTANCE, because `ValidatorFailClass` is a **cause-of-failure** axis, not a retry-disposition axis, and no `ValidatorFailClass` → `ValidatorRetryExitClass` mapping exists anywhere in `harness-*/src`. Recorded so it is not re-proposed as a cheap fix. **ONE CARRIED CLAIM SHARPENED:** the §9.2 half is blocked by a **NAME GATE**, not by attribute-blindness — `_conditional_always_sampled` (`sampling_mode.py:239-241`) DOES read the permanence attribute, but only inside an `event_name.startswith("validator.fail.")` gate the live `validator.evaluate` span never satisfies; the §9.2 half stays OUT OF SCOPE and deferred to `B-137` step (3). Register: **142 items / 0 open / 110 closed / 31 registered_finding / 1 held**. **NEXT:** (1) **the `B-124` RATIFICATION** — an operator gate, batchable with any other pending ratification, after which the selected reading's spec/impl legs follow; (2) the **`B-126`..`B-132` grounding queue** — the largest remaining Claude-executable frontier and the natural next leg while the gate is open; (3) `B-134` (Class 2 fork if pursued), `B-135`/`B-136`/`B-137` (dyadic C7⊥C11 council owed at re-measurement), the `B-139`/`B-140`/`B-141` groundings. held=1 honored.

**Prior next action (post-#1281).** **`B-142` IS CLOSED — the two `harness-inspect` pause-journal witnesses no longer depend on the developer's filesystem** (merged #1281 at `f8471d62`). Both asserted `pause_journal_deployment_scope_known is False` on the premise *"where no usable config exists at all"* while `_authoritative_tenant_scope` loads through the NORMAL `RuntimeConfigSource` precedence chain and `_discover_default_config` resolves `Path.cwd() / harness.toml` — **process CWD only, no parent walk** — so an ambient gitignored `harness.toml` made them fail locally and pass in CI: **a CI-invisible red `just check`**. **GROUNDED NON-DESTRUCTIVELY** (the operator's real config was never moved, renamed or deleted): same file, cwd = repo root → *2 failed / 19 passed*; cwd = clean temp dir → **21 passed**. **FIX — candidate (a):** both tests call a shared helper that `monkeypatch.chdir`s to an empty `tmp_path` **and asserts the premise** via `pytest.raises(RuntimeConfigLoadError)`, making the isolation a POSITIVE CONTROL rather than an assumption. Candidates (b)/(c) rejected **on grounds, not cost**. **ZERO `src` change** — the precedence-chain widening is contracted and preserved verbatim. **COMPLETENESS ARGUED, NOT ASSUMED:** eleven candidate files cleared with a stated reason each, bounded decisively by `grep -rn "Path.cwd()"` over all `harness-*/src` returning **exactly one** hit. **PURPOSE VERIFIED BY EXECUTION: `just check` now exits 0 at 7714 passed / 0 failed** (was *2 failed / 7705 passed*). Codex converged R1 zero findings; **3-lens merge gate ALL-APPROVE FIRST PASS** — lens 1 resolved the genuine process-global-CWD question empirically (no `pytest-xdist` installed, no parallel runner anywhere, `harness-runtime` excluded from the axis matrix); lens 3 proved the isolation load-bearing by direct probe and named **one non-blocking gap now carried as a NAMED OPEN RESIDUAL on the closed row** (the `B-104`/`B-133` precedent): the isolation is not STRUCTURALLY guaranteed — a future edit dropping it from one of the two tests recurs the defect and **CI cannot catch that class** — with a **file-scoped autouse fixture** (precedented at `test_config_source.py:45-56`) identified as an option step (2) never weighed. Register: **142 items / 0 open / 110 closed / 31 registered_finding / 1 held**. **NEXT — Leg 1 continues:** (1) the **`B-124` §10.2 half** — the substantive fork, UNBLOCKED on the taxonomy question but with the naive `result.outcome` wire **FORECLOSED as lossy** (CP v1.116 §1.3), so the arc must materialize the retry-exit classification at the emission site, ratify the narrowing WITH §21.6 re-specified, or select another carrier — expect a Class 2 fork filing; (2) the **`B-126`..`B-132` grounding queue**; (3) `B-134` (Class 2 fork if pursued), `B-135`/`B-136`/`B-137` (dyadic C7⊥C11 council owed at re-measurement), the `B-139`/`B-140`/`B-141` groundings. held=1 honored.

**Prior next action (post-#1279).** **`B-123` IS CLOSED END-TO-END — the §10.2 tail-keep classification trigger now resolves span EVENT names, not just span names** (merged #1279 at `aaec1b4d`). **The framing is the whole disposition: this was a CONFORMANCE REPAIR, not a spec direction.** C-OD-10 §10.2's two event-shaped rows have read *"Parent + sibling spans of any `sandbox.violation` **event** preserved"* / *"…any `breaker.tripped` **event**…"* since the contract was written (`Spec_Operational_Discipline_v1_2.md:583-584`, read verbatim) while the realization matched by span NAME — so the code was non-conformant to cleared text, there was no fork to route, and **§10.2 itself is NOT amended; its non-amendment is the point.** Landed as **OD spec v1.38→v1.39** (ONE amendment site: §C-OD-09 **§9.2.1 term 3 REPLACED IN FULL**, making normative that the two-member §10.2 trigger set and the nineteen-member §9.2 roster are DISJOINT and must not be collapsed, that both name sets derive from a SINGLE declaration per name, and that the predicate stays TOTAL and side-effect-free) **+ plan v2.33→v2.34** (NEW **U-OD-60**, **TWELVE ACs**, **SEVEN PD-8 probes**) + both clearance markers. **ZERO contract numbers, ZERO §10.2 edit, ZERO roster change (19), ZERO namespace delta (15), ZERO emission-site change, ZERO head-sampler edit, ZERO CP/Runtime delta, ZERO CXA rows (111), ZERO hash impact.** Register: **142 items / 0 open / 109 closed / 32 registered_finding / 1 held**. **FOUR THINGS THIS ARC CORRECTED RATHER THAN INHERITED.** (i) **A queued Class-3 rider was FALSIFIED** — the register claimed §10.2 row 1's *"permanent-fail per ADR-D2 §1.8"* cite does not resolve; it does (§1.8's table carries a *"C5 fail-class"* column with `permanent-fail` at `ADR-D2.md:305/:306/:309`). Dropped, not carried; reached **independently at both legs**, and the row's own prose now leads with the correction. (ii) **PD-8 probe (v) changed the deliverable** — it left the first real-dispatch witness GREEN, exposing that the shape term 5 says actually ships (*the dispatch carrier IS routinely the root close*) had no real-dispatch coverage; **AC4b was ADDED** rather than the probe restated. (iii) **Codex round 1 [P2]** caught a doubled `span.events` read per buffered span — a regression this arc introduced — fixed by threading ONE snapshot, witnessed by **AC11's `== 1`** counting double (deliberately not `<= 2`, which would pass against the code being fixed). (iv) **The 3-lens merge gate BLOCKED on lens 3** — a first-event-only reduction of the scan survived the ENTIRE witness set; **AC12** added (sibling-survival assertion, non-member ahead of the member, parametrized first/middle/**LAST**) + probe (vii), which kills it with **zero collateral**. The scoped re-gate reproduced the mutation and defeated **five** further weakenings. **This was a REPEAT catch of the #1276 finding on the sibling function — the class is systemic to this event-scanning pattern.** **ADMISSION-BOUNDED, and the leg says so:** per `B-137` the sibling tree is preserved only for head-ADMITTED carriers (~10% at base-rate 0.1, ~20% at 0.2) — no full-floor claim anywhere. **NEW `B-142` REGISTERED** (not absorbed): two `harness-inspect` pause-journal witnesses assert *"no usable config exists at all"* without isolating the ambient source, so an auto-discovered **gitignored `harness.toml`** at the repo root makes `scope_known` True locally and False in CI — a **CI-invisible red `just check`** for any developer with the documented local setup. Cause pinned empirically (main CI green at the same base commit; a dotenv hypothesis tested and FALSIFIED); it is a TEST-ISOLATION defect, not product behaviour. Codex converged at **round 5**; CI 17/17. **NEXT — Leg 1 continues:** (1) the **`B-124` §10.2 half** — UNBLOCKED on the taxonomy question, but the naive `result.outcome` wire stays **FORECLOSED as lossy** (CP v1.116 §1.3), so the arc must materialize the retry-exit classification at the emission site, ratify the narrowing WITH §21.6 re-specified, or select another carrier; (2) the **`B-126`..`B-132` grounding queue**; (3) **`B-142`** (test-isolation, self-contained and Claude-closeable); (4) `B-134` (Class 2 fork if pursued), `B-135`/`B-136`/`B-137` (dyadic C7⊥C11 council owed at re-measurement), and the `B-139`/`B-140`/`B-141` groundings. held=1 honored.

**Prior next action (post-#1278).** **`B-138` IS CLOSED — the `validator.fail.class` taxonomy fork is settled** (operator-ratified **disposition (a)**, 2026-08-09): the wire attribute carries the C-CP-25→C-CP-28 §25.2 **`ValidatorFailClass`** domain. Landed as **CP spec v1.116 + bundled ADR-D5 v1.5→v1.6** (§1.10/§1.10.1 reconciled at the canonical source — authority chain, not spec-side override), merged #1278 at `24409ce0` after **8 codex rounds to convergence** with one standing decline recorded (replace-vs-append register prose, B-119 precedent). Register: **141 items / 0 open / 108 closed / 32 registered_finding / 1 held**; THREE rows minted from review findings — **`B-139`** (`cause_attribution` declared-always-emitted at both canonical surfaces, ZERO producers), **`B-140`** (no production `validator.fail` span — attributes ride `validator.evaluate`/`validator.escalation` while OD sampling keys on `validator.fail.`-prefixed NAMES production never emits — plus the composer's out-of-domain `unspecified` sentinel), **`B-141`** (the C-OD-14 §14.5.3 `ReplaySemanticDivergenceError` / ADR-D6 v1.2 / U-CP-41 `VerifierResult` / U-CP-47-criteria retry-exit cascade — all declared-only, zero production emission sites). **DISCLOSURE:** the ratification-time grounding claim *"no reader asserts a retry-exit value"* was FALSIFIED at codex round 3 by the declared-only §14.5.3 carrier — the disposition stands on all LIVE-wire evidence; the corrected claim is stated at v1.116 §0.1. **NEXT — Leg 1 continues, re-sequenced by what this leg settled:** (1) **the `B-123` repair** (widen `is_classification_trigger` to event names, OD spec v1.38 §9.2.1 term-3 flip + plan v2.34 U-OD-60 + clearances, execution-witnessed — closes `B-123`); (2) **the `B-124` §10.2 half** — UNBLOCKED on the taxonomy question but the naive `result.outcome` wire is **FORECLOSED as lossy** (v1.116 §1.3): the arc must materialize the retry-exit classification at the emission site, ratify the narrowing WITH §21.6 re-specified, or select another carrier (B-140 is ADJACENT, not gating — the §10.2 trigger is attribute-keyed); (3) then the **`B-126`..`B-132` grounding queue**, `B-134` (Class 2 fork if pursued), `B-135`/`B-136`/`B-137` (dyadic C7⊥C11 council owed at re-measurement), the new `B-139`/`B-140`/`B-141` groundings, codex E3 optional cold run. held=1 honored.

**Prior next action (post-#1277).** **THE B-119/B-123/B-124/B-125 DISPOSITION LEG IS LANDED** (PR #1277, doc/register-only, merged `a0c6c41c`; merge-gate + codex both SKIPPED per doc-only scope rules, the #1273 precedent): **`B-119` → CLOSED as a FORWARD-ONLY RECORD** (capability_shortfall shape; PER_MODEL remains the only constructed breaker scope at HEAD — `retry_breaker_fallback.py:774-776`; four-disjunct demand trigger recorded); **`B-125` → CLOSED as a DECLARED BOUND + promotion trigger** (the B-101 (b)-PLUS shape); **`B-124` SPLIT** (§10.2 half closeable at Leg 1; §9.2 half re-scoped to a B-137 rider); **`B-123`'s repair SETTLED as a §10.2 CONFORMANCE fix** (the contract already says "event" at `Spec_Operational_Discipline_v1_2.md:582-584`; build owed); **NEW `B-138`** minted (LIVE `validator.fail.class` two-taxonomy homonym: shipped C-CP-25 `ValidatorFailClass` at `validator_framework.py:346` vs CP §21.5's bounded(5) `ValidatorRetryExitClass` at `validator_fail_taxonomy.py:137-141`, + the CP-3-attr vs OD-4-attr `validator.fail.*` divergence). Register: **138 items / 0 open / 107 closed**; both check tools green. **NEXT — Leg 1, three SEQUENCED obligations (order binding, B-138 gates the file):** (1) **ground + disposition `B-138`** (three close_out candidates + a conditional C5⊥C7 dyad only if candidate (b) trends; may carry ONE operator question); (2) **the `B-123` repair** — widen `is_classification_trigger` (`harness-od/src/harness_od/tail_keep_classification.py:91-95`) to match `breaker.tripped`/`sandbox.violation` as EVENT names (~4 lines, the `_carries_always_sampled_event` pattern at `tail_keep_span_processor.py:190-223`) + OD spec v1.38 §9.2.1 term-3 flip + the ADR-D2 §1.8 non-resolving-cite rider + OD plan v2.34 (NEW U-OD-60) + clearance markers, witnessed by execution through the real processor (B-133 harness is the template; non-member-first orderings) — closes `B-123`; (3) **the `B-124` §10.2 half** — set `validator.fail.permanence` in `validator_framework.py:_build_span_attributes` (~:320-352) from `result.outcome`, execution-witnessed — closes `B-124`; discharge the B-125 rider (OD §7.1 / CP §3.5 gloss corrections) if the leg touches those files anyway. **After Leg 1:** the `B-126`..`B-132` grounding queue (batch 3-4 per agent), `B-134` (Class 2 fork if pursued), `B-135`, `B-136`, `B-137` (dyadic C7⊥C11 council owed at its re-measurement step), codex E3 optional cold run. held=1 honored.

**Prior next action (post-#1276).** **`B-133` IS CLOSED — the §9.2 floor is realized at the tail for the three event-shaped members** (positive control EXECUTED → venue-(a) repair → codex ×3 incl. the head-starves-tail architecture catch → 3-lens gate with one measured BLOCK remedied → merged #1276 at f6b688aa): the event-aware `on_end` arm forwards event-carrying spans; the head-admission bound is MEASURED and contracted (OD v1.38 §9.2.1, 8-cell table — shape-dependent: event-carried 10.4%/20.9% at the sub-1.0 production cells, name-matched roots 100%); plan v2.33 U-OD-59. **NEW `B-136`** (pre-existing name-arm buffer leak) + **`B-137`** (head sampling starves tail evaluation for event-carried + non-root shapes — three candidates, dyadic C7⊥C11 council owed at its re-measurement step, F-08 merged in); `B-123`'s breaker-arm probe answered affirmatively (its row stays open for the sandbox arm + repair). Register: 137 items / 0 open / 31 registered_finding / 1 held / 105 closed. **The remaining frontier — the B-119..B-137 grounding queue** per the full-register ground-first directive: (a) **`B-119`** (per-provider breaker scope — materially reshaped by B-118's close: the C11 irreversibility half is gone; its close_out prices the B-116 guard interaction at any second charge site); (b) **`B-123`** (the sandbox.violation trigger arm + the repair decision — half-answered at #1276); (c) **`B-124`** (inert validator.fail.permanence), **`B-125`** (tool_id homonym), **`B-126`..`B-132`** (council-enumerated), **`B-134`** (ledger-conflict class fork question), **`B-135`** (stale-era family), **`B-136`/`B-137`** (fresh); (d) codex E3 optional cold run post-quota-reset Sat 8/9. held=1 honored.

**Prior next action (post-#1255).** **THE `B-57` SPEC LEG IS LANDED** (PR #1255, design-phase, ZERO code; merged `6719488b`) — the operator-ratified **Reading A** (2026-08-07) applied end to end. **IS spec v1.12 → v1.13**: NEW **`C-IS-07` §7.6.1** authorizing **PER-CALL-SITE** election of writer-owned timestamp sampling on **DIRECT** append surfaces via the **EXISTING** `WRITER_OWNED_TIMESTAMP` sentinel — **never a default, never a mode on the writer**; every non-electing direct producer keeps caller-supplied semantics **BYTE-VERBATIM**; a site MAY elect **only** where its timestamp means *when the entry was appended*. Plus ONE **residual-status paragraph** recording §7.6's registered residual as **DISCHARGED** (the `"Surfaces that do NOT change"` + `Registered residual` paragraphs themselves **PRESERVED VERBATIM** — the §7.5-over-§7.4 shape) and ONE **cross-reference-only** clause at the §7.1 row-7 cell. **IS plan v2.8 → v2.9**: U-IS-11 amended with **ACs #14–#20 + #14-bis** + the **14-row per-site classification table** the contract routes to the plan (**10 ELECT / 2 injection-caveat / 1 RETAIN / 1 DEFER**). **ZERO** contract numbers, hash/canonicalization/migration, CXA rows, new units/nodes/edges/auxiliary types, and ZERO code. Two clearance markers filed. **`B-57` `registered_finding` → `open`** (ratified + spec AND plan applied, impl owed — the `B-96`/`B-88` enum precedent); **`B-112` unmoved** at `registered_finding` (cause-and-evidence record; its adoption question is now answered, its own disposition resolving on the impl leg's two-process witness). **Grounding + review:** all 14 classification rows re-resolved by direct read at `acfc1afa`; the `emit_sibling_ledger_entry` sweep re-run (19 hits, exactly 3 non-test); **two of the filing's own count claims CORRECTED** (*"twelve SITES"* → **14 rows**; *"~8 conversions"* → **10 ELECT**) plus one cite-range; 3 review rounds (2 BLOCK → APPROVE) surfacing stale root + axis `CLAUDE.md` pointers, a wrong `restored_at` cite, an unowned sentinel-docstring prose carrier, and the mixed-population limit (**election is NOT refusal immunity**) now stated at §7.6.1.

**NEXT UP.** (1) **The `B-57` IMPL LEG** — the immediate successor and fully unblocked: the **10 ELECT** conversions; the **sentinel-docstring refresh** at `state_ledger_write.py:78-80` (AC #14-bis); the **2 injection-caveat** resolutions (`audit_writer`, `cost_attribution_f2_write`) recorded **with rationale** — a **RETAIN** verdict on either **FIRES the live C3 ⊥ C11 council trigger** at IS plan v2.9 §0.6 before any `_CLOCK_SKEW_TOLERANCE` work; the `audit_writer` resample-retry disposition; the `shadow_git_rollback` `restored_at` decoupling pin (scoped to `:151`, the RESTORED return); the **two-process contention witness** under a PD-8 mutation probe; the **NON-ELECTING negative witness**; the `as_is_wiring` event-time control; and the per-site table conformance check. `B-57` closes there. (2) **The `B-111` PLAN-DELTA LEG** (ratified **(a)**) **+ its impl leg**. (3) **`B-110` (c)** on its stated trigger. (4) **`B-115` / `B-116` groundings** later.

**Prior next action (post-#1253).** **THE `B-57`/`B-112` CLASS 2 FORK IS FILED** (PR #1253, doc-only, `.harness/**` only, merged `2c2a5c8d`) — and it is now **the single open operator gate**. **NEITHER ROW'S STATUS MOVED:** `B-57` (the **remedy owner**) and `B-112` (the **cause-and-evidence record**) both stay `registered_finding`; their `pr:` pointers and any status flip ride the **ratification** leg, per the `B-88` precedent. **THE DEFECT:** `C-IS-07` §7.6's registered residual (`Spec_Information_Substrate_v1.md:778` — *"requires its own back-flow"*) keeps every DIRECT append on caller-supplied timestamps under a **zero**-tolerance monotonicity check (`state_ledger_write.py:69`; refusal `:321-324`), while `B-112`'s reproduced mechanism (12/20 two-process repro) **plus** its code-grounded in-process widening makes out-of-order arrival live. **THE DECISIVE CHEAPENER:** the `WRITER_OWNED_TIMESTAMP` substitution at `state_ledger_write.py:316-320` is **UNCONDITIONAL at HEAD** — no surface discriminator exists, so **the mechanism is already there and only the contract authorization is missing**, and the direct-path restriction is currently held by **prose alone** while a test already opts in. **READINGS: A (RECOMMENDED)** — a narrow IS delta authorizing **PER-CALL-SITE** opt-in on direct surfaces, default unchanged for every non-electing producer, extending the **EXISTING** `C-IS-07` §7.6 (**ZERO** contract numbers minted, ZERO hash/canonicalization impact, ZERO CXA rows, ZERO new plan units) + a `U-IS-11` AC widening + a per-site classification table; **B** — HOLD until an observed production refusal. **COUNCIL NOT CONVENED — PROBE-RESOLVED** per §10.9: `B-112`'s step-(3) C3⊥C11 trigger (`_CLOCK_SKEW_TOLERANCE` configurability) is reached *only if* a caller is left stranded, and the classification leaves exactly one site retaining caller-supplied semantics — `as_is_wiring.py:129`, genuine **single-producer EVENT time** — which is not a stranded caller. **Step (3) is never reached.** **PLUS TWO OPERATOR-RATIFIED MINTS (2026-08-07), unrelated to the fork:** **`B-115`** — the W-5 cell-3 successor, owning the **MEASURED** same-text/same-model-NAME/different-**PROVIDER** duplicate record JSONL line **and** the `TRANSIENT_RETRY` re-entry via `MemoryToolExecutionStoreError` (the store subtype is *not* in the fail-fast set), answering `B-84`'s **D-2** falsifier affirmatively and cross-linked to that now-**closed** row so the live residual is **open-row-queryable**; **`B-116`** — the breaker-scope **question** (should DETERMINISTIC harness-internal faults count toward a **provider's** breaker?), pre-existing, spanning `C-CP-21` + Runtime §14.6, framing the question only. **Register: 114 → 116 items / 97 closed / 0 open / 0 gated / 16 → 18 `registered_finding` / 1 held**; snapshot re-pinned in the same commit (`9ec02294adeb2892` -> `9ffeb9fc4a59175d`), `--check` green. **GROUNDING: 27/27 automated checks green**, every cite re-read **BY CONTENT** — the recorded line numbers were explicitly **not** trusted, and **FIVE drift items are reported, never absorbed**: three inbound module **PATHS** were wrong; **`sibling_ledger_entry_composition.py:163` is PRODUCTION-UNREACHABLE** (`emit_sibling_ledger_entry` at `cp_is_wiring.py:124` has **zero non-test callers**), so **the exposed surface is 11 of 12 sites** and that site is re-classified DEFER; `retry_breaker_fallback` `~:1025` -> **`:1028`**; the two **injected-`time_source`** sites (`audit_writer`, `cost_attribution_f2_write`) **cannot adopt without overriding the test-determinism seam** — a real per-site cost surfaced now rather than at the impl leg; and `_CLOCK_SKEW_TOLERANCE`'s own comment falsely calls itself *"configuration-supplied"*. One **attribution correction**: the `audit_writer` resample-retry is **already on `B-57`** (2026-07-27), which reads it as *consistent with* the posture — the filing attributes it, quotes that contrary reading, disagrees only on **weight**, and marks the ground **CONTESTED**. Out-of-family review (gemini, 2 rounds): **APPROVE/APPROVE** — round 2 checked four falsifiable claims against verbatim source and found **no omitted undermining fact**. CI 16/16 green; `merge-gate` skipped (doc-only). **THE FRONTIER AFTER THIS: (1) THE RATIFICATION ASK for the `B-57` fork — a SINGLE gate**, stated in the filing as *is authorizing a per-call-site opt-in to an ALREADY-UNCONDITIONAL writer-side mechanism, on a cleared contract whose residual routes the extension to back-flow, a **spec act** this leg may perform, or does it require operator ratification first?* — which, if ratified A, opens the spec leg (IS v1.12 -> v1.13 + IS plan v2.8 -> v2.9 + both clearance markers) then the impl leg (~8 one-line conversions, the 2 injection-caveat decisions, and the two-process contention witness). **(2) THEN the `B-111` PLAN-DELTA LEG** (ratified **(a)**, the AC #15 narrowing) **+ its impl leg**. **(3) `B-110`'s mechanism (c)** waits on its **opportunistic** standing trigger (*the next arc that touches `gc_sweep`'s phase split takes it*) and opens no work now. held=1 honored.

**Prior next action (post-#1251).** **THE `B-110`..`B-113` GROUNDING QUEUE IS DISCHARGED** (PR #1251, doc-only, `.harness/**` only, merged `2827fe3f`). **`B-113` -> CLOSED, UNREACHABLE NOT FIXED** -- the disposition the row's own step (1) provides for: the discriminator is the BUILD-TIME `configure` probe `HAVE_FLOCK`; an empirical positive control on the dev machine (Python 3.12.13, `darwin`, `HAVE_FLOCK == 1`, a real two-fd contention probe raising `BlockingIOError`/errno 35 `EAGAIN` -- native, not emulated) plus the committed deployment surface (ADR-D2's three surfaces at `design-substrate/ADR-D2.md:96-98`, all Linux/macOS/BSD-family; CI `ubuntu-latest`; RC Docker/Lima = a Linux VM) contain NO fcntl-emulation target -- the Solaris/HP-UX/z-OS class appears nowhere in it. The finding is PRESERVED as correct-by-inspection and declined on REACHABILITY, with **two reopen triggers**: the `B-45` Windows coupling (`msvcrt.LK_NBLCK` raises `OSError`) and a NEW one -- any supported target whose CPython build lacks `HAVE_FLOCK`. **`B-110` RE-SCOPED (stays `registered_finding`)** -- the PRIMARY half is discharged in substance by the landed U-RT-150 witness `test_reused_temporary_name_stays_bounded_by_its_own_filesystem_timestamp`, so the row now retains **mechanism (c) ALONE**: re-stamped HEAD anchors (select-under-lock `protected_result_store.py:1111-1135`, entry unlink off-lock `:1173`, tmp-orphan unlink off-lock `:1201`), **severity sharpened** -- (c) is a HOLE in `B-68`'s own whole-lifetime-lock fix (`_publish_atomic` holds both locks across `:810-858` precisely so no sweep can unlink an in-flight temp; a SECOND sweep's off-lock unlink bypasses that hold; firing = `FileNotFoundError` at `os.link` = **lost recovery for a completed paid effect**) -- **reachability quantified** (~2.8e-13 per `tempfile._RandomNameSequence` redraw, 8 chars / 37-alphabet = 3.51e12 names), the EXPIRED "U-RT-150 MAY take it" trigger **replaced** by an opportunistic standing one (*the next arc that touches `gc_sweep`'s phase split takes it*), and the fix **priced** (~25 lines: unlink loop back under the lock + `(st_dev, st_ino, st_mtime)` identity revalidation + one injected witness). **`B-114`'s `pr:` reconciled** `#pending` -> `#1249` (the #1249 lens-2 rider, cleared). **Three sibling grounding notes recorded, not acted on:** `B-57`'s direct-writer enumeration **under-counts by four** at HEAD (`workload_binding_engine_class_selection.py:337`, `hitl_as_tool_call_rewriting.py:290`, `per_step_override_evaluator.py:489` via caller `:523`, `sibling_ledger_entry_composition.py:163`) with two drifted `workflow_driver` cites re-stamped (`:6038`->`:6074`, `:6261`->`:6297`); `B-112`'s **in-process widening** (`flock` contends between fds within ONE process, so a sibling THREAD enters the same polling wait -- `cross_process_ledger_lock.py:120`/`:173-184`/`:524`, code-grounded, NOT reproduced); `B-111`'s **window correction** (the `mkstemp`->`replace` window is FSYNC-INCLUSIVE at `protected_result_store.py:1329-1332`, not sub-millisecond, correlated with a supervisor-SIGKILL-on-slow-mount -- and, against interest, the "exhaust disk" framing OVERSTATES: the real cost is **accounting invisibility**). **Register: 114 items / 97 closed / 0 open / 0 gated / 16 registered_finding / 1 held**; `--check` green, snapshot re-pinned in the same commit (`2ff7b2e9a9b0f97d` -> `9ec02294adeb2892`). Out-of-family review (gemini, one round): APPROVE, no findings. **THE FRONTIER AFTER THIS: the `B-57`/`B-112` Class 2 FORK FILING** -- fork-shaped, doc-only, and now fully grounded (this PR's B-57 note is exactly the enumeration input it owes; the remedy route is unchanged -- a narrow IS spec delta extending C-IS-07 section 7.6's writer-owned sampling to an opt-in direct-append mode, or per-writer serialization guidance, + U-IS-11 widening). **THEN the OPERATOR BATCH, to be surfaced together rather than drip-fed:** (i) `B-111`'s (a)-vs-(b) disposition -- grounding RECOMMENDS **(a)**, the AC #15 narrowing, on plan-text-correctness grounds (AC #15's no-removal clause read literally forbids the store from cleaning up its OWN prefix, which is not what the AC exists to protect); (ii) the **W-5 cell-3 successor-row mint** (standing from #1243); (iii) the **breaker-scope question** (deterministic harness-internal faults vs provider breaker -- Runtime/CP scope, standing from #1247). **`B-110`'s mechanism (c) waits on its opportunistic trigger** and opens no work now. held=1 honored.

**Prior next action (post-#1249).** **B-114 is CLOSED** (PR #1249, merged pinned to gate-approved head `3e238da0`): the `_prepare` exhaustiveness fall-through re-typed to `MemoryToolExecutionInternalError` as a **reading-(a) conformance repair against cleared contract text** — no new ratification owed, per the invariant's kind-of-fault quantification ("raised ONLY for those argument refusals") and the plan's own delegation of the repair to B-114; merge-gate lens 2 re-adjudicated the discriminator independently and concurred. 4 witnesses + 2 discriminating probes (lens 3 executed all mutations — the drive mechanism provably reaches production `:441`); 3-lens gate **ALL-APPROVE FIRST PASS** (third consecutive). **Register: 114 items / 96 closed / 0 open / 17 registered_finding / 1 held.** One rider from lens 2: B-114's `pr:` reads `#pending` — reconcile to `#1249` at the next register touch. **The grounding queue continues:** (a) **B-110** (GC observation-record post-removal name-reuse remainder + the select-under-lock/remove-outside-lock MAY-take disposition (c)); (b) **B-111** (AC #15's no-removal clause forecloses publication-temp cleanup — dispositions (a) plan-delta narrowing / (b) accept-and-document, Phase-6 acts); (c) **B-112** (poll-fairness evidence row; adoption defers to B-57's spec-delta route — grounding decides whether B-57's spec delta is now warranted); (d) **B-113** (ABBA-probe EACCES widening; establish-reachability-first, may close unreachable). **Operator-optional items standing:** the W-5 cell-3 successor-row mint; the breaker-scope question (Runtime/CP). held=1 honored.

**Prior next action (post-#1247).** **THE 2026-08-05 RATIFIED LEG QUEUE IS COMPLETE** — B-88 closed end-to-end (filing #1220 → ratification #1233 → spec leg #1245 → impl leg #1247 U-MEM-28): the six dispatch internal-schema-fault sites re-typed to the plan-prescribed `MemoryToolExecutionInternalError` (fail-fast by name, C-MEM-19 telemetry per v1.3), the two genuinely-model-supplied sites retained as input errors, B-114's seventh site untouched on authority grounds. **17 witnesses + 9 PD-8 probes; 3-lens merge-gate ALL-APPROVE FIRST PASS — lens 3 executed SEVENTEEN real mutations, all confirming; review 1 codex + 5 gemini rounds** (the gemini fallback engaged deliberately to conserve the ~16% codex floor, resets Sat 8/9). Every batch verdict is now BUILT: B-96 (#1235+#1237), B-93 (#1241), B-84 (#1243), B-88 (#1245+#1247), B-95/B-98/B-104 dispositions recorded (#1233), `run_bootstrap` kept. **Register: 114 items / 95 closed / 0 open / 0 gated / 18 registered_finding / 1 held. The remaining Claude-authorable frontier is the GROUNDING QUEUE:** (a) **B-114** (now actionable — the declaration has flipped; discriminator: conformance-repair vs Class 2 fork; MAY carry a genuine operator gate); (b) **B-110** (name-reuse remainder + the select/remove-lock MAY-take); (c) **B-111** (AC #15 no-removal vs temp-accumulation); (d) **B-112** (poll-fairness evidence, adoption deferred to B-57's route); (e) **B-113** (ABBA-probe EACCES, establish-reachability-first). **Operator-optional items surfaced, not taken:** the W-5 cell-3 successor-row mint (#1243 gate); the breaker-scope question (deterministic harness-internal faults vs provider breaker — Runtime/CP scope, surfaced at #1247). Riders: residual Class 3 cite sweeps. held=1 honored.

**Prior next action (post-#1245).** **The `B-88` SPEC LEG IS LANDED** (grounding -> fork filing #1220 -> ratification #1233 -> spec leg #1245): `Spec_Memory_Substrate_v1.md` **v1.2 -> v1.3** and `Implementation_Plan_Memory_Substrate_v1.md` **v1.2 -> v1.3**, both clearance markers filed. C-MEM-19 gains **`input_validation_failure`** as a SEVENTH failure class (ratified reading A; the fallback name `input_error` NOT taken), a NEW `### Input validation failure` subsection carrying the ratified **A-ii type boundary**, the **coverage statement** (classify-routed population only -- the dispatch prepare path emits nothing today and this leg neither wires nor promises one, while the converse is NOT licensed, so no existing emission may be withdrawn by relocating a check onto an unclassified path), and the **retry disposition** (fail-fast preserved for the re-typed internal faults, decided at this leg as the ratification required), plus TWO appended invariants. The plan extends U-MEM-22's acceptance line to seven values in place and adds **NEW U-MEM-28 at G8**. The U-MEM-25 closeout packet carries its **annotation half** (`PENDING - U-MEM-28` on C-MEM-19 / R-MEM-01 / R-MEM-13 + a window-3 paragraph), per the `B-86` / `B-92` precedent. **ZERO contract numbers minted, ZERO hash impact, ZERO CXA rows, ZERO code.** Out-of-family review ran **TEN rounds and converged CLEAN at R10**; the contract text did not move after R4, and R6-R9 were governance findings against the leg's own authority -- most consequentially **R8**, which withdrew a seventh executor-side re-typing the leg had added on its own reading (the operator ratified exactly six), and **R9**, which minted **NEW register row `B-114`** so that withdrawn finding outlives `B-88`. Register: **`B-88` `design_substrate_gated` -> `open`** (only the impl leg remains), `B-114` `registered_finding`; snapshot 114 items, `--check` green.

**NEXT UP.** (1) **The `B-88` IMPL LEG -- `U-MEM-28`**, the immediate successor and fully unblocked: the seventh enum member, the single declaration flip on `MemoryToolExecutionInputError`, the stopgap-docstring rewrite that class's own shipped text promises, the **A-ii six-site re-typing** in `lifecycle/llm_dispatch.py` (re-resolve BY CONTENT -- every offset has already drifted once) with its receiving-type constraints (**not** a subclass of the input type, **not** the family base, since `isinstance` is subclass-inclusive), fail-fast preservation with a **per-site** retry assertion paired to each per-site telemetry assertion, the fixture re-key, 8 of 39 witness rows flipping plus two negative rows with **distinct** literals, three mutation probes, and the closeout **re-open-and-extend** half. (2) Then the **`B-110`..`B-113` groundings** (GC observation-record generations; the unreclaimable publication temporary; deadline'd-lock handoff fairness; the ABBA exception-class contention probes), plus the newly registered **`B-114`** -- which is deliberately NOT actionable until U-MEM-28 lands, since the site is conformant until the declaration flips.

**Prior next action (post-#1243).** **The B-84 chain is CLOSED END-TO-END** (grounding → filing #1228 → ratification 2026-08-05 → build leg #1243): the ratified Reading A — a side-effect-free executor `validate()` pre-pass built on the filing's PREFERRED W-4 discharge (one shared `_prepare_<tool>` per tool returning frozen prepared values; `_execute_authorized` narrows on result type, so validate/execute parity is a TYPE ERROR, not a test), wired into BOTH dispatch batch pre-passes; the single-call `memory.search` `limit=0` case closed in W-8's inverted form on both surfaces. **51 witnesses + 6 PD-8 probes — all six independently re-executed RED at the gate at exact claimed cardinality; 3-lens merge-gate ALL-APPROVE FIRST PASS** (lens 1: the re-run-`_prepare`-at-execute design forecloses the TOCTOU class; lens 2 ADJUDICATED the affirmative D-2/W-5-cell-3 answer — duplicate JSONL + TRANSIENT_RETRY re-entry via `MemoryToolExecutionStoreError`, now a MEASURED fact — as on-row-recording-sufficient per the filing's §3(iv) pre-adjudication, with an optional successor-row mint surfaced as an operator call; lens 3: four minor residuals recorded, none blocking). **Register: B-84 CLOSED — the register reaches 0 open rows.** Tally **113 items / 94 closed / 0 open / 1 design_substrate_gated (B-88) / 17 registered_finding / 1 held**. **Riders queued:** the Class 3 cite-drift sweep from the ~180-line executor code motion (~18 files: 3 in-PR-scope test cites, ~7 sibling test/source cites, plus `design-substrate` Spec/Plan_Memory_Substrate cites + 4 clearance markers — the design-substrate half routes via Class 3 back-flow/overlay pass, NOT a direct edit); W-5 cell-3 successor-row mint (operator-optional). **The ratified leg queue continues:** (a) **B-88 spec leg — NEXT** (C-MEM-19 seventh member `input_validation_failure` + boundary invariant + sub-decision A-ii re-type of the llm_dispatch internal-schema-fault raise sites; row `design_substrate_gated`) → impl leg; (b) B-110/B-111/B-112/B-113 groundings when reached. held=1 honored.

**Prior next action (post-#1241).** **The B-93 build leg is LANDED** (PR #1241, merged pinned to the gate-approved head `294864c2` after a multi-hour GitHub Actions outage was waited out with a recovery-gated single rerun): the ratified Reading B — deadline (300s default, end-to-end shape, both flagged overturnable) + typed `CrossProcessLockTimeoutError` homed harness-core at ALL NINE cross-process lock sites + the in-process `_DirLock` RLock face bounded + the reconciler import repair + the previously-unwitnessed `_workflow_lock` now witnessed by a real second OS process; HAND-ROLLED vehicle; Windows serialization deferred on witnessability. **58 witnesses / 43 PD-8 probes; 3-lens gate APPROVE/BLOCK/BLOCK → two fix rounds → final scoped verify APPROVE** — the gate's executing-witness demand exposed a genuinely BROKEN fold (a ledger read outside its try), and codex rounds closed a real deadline-bypass at the manual `LOCK_NB` probes (`refuse_after_acquire()` at all three ABBA arms). **Register: B-93 CLOSED; B-45 enumeration corrected (Windows step-3 NOT discharged); B-57 VERIFIED-DORMANT → PARTIALLY MET (B-112's evidence, remedy route unchanged); NEW B-112** (poll-fairness → append_ledger_entry timestamp check; adoption defers to B-57's spec-delta route) **+ NEW B-113** (pre-existing ABBA-probe EACCES widening declined, establish-reachability-first). Tally **113 items / 93 closed / 1 open (B-84) / 1 design_substrate_gated (B-88) / 17 registered_finding / 1 held**. **The ratified leg queue continues:** (a) **B-84 build leg — NEXT** (executor pure `validate()` pre-pass; row `open`); (b) **B-88 spec leg** (C-MEM-19 seventh member + A-ii re-type) → impl leg; (c) B-110/B-111/B-112/B-113 groundings when reached. held=1 honored.

**Prior next action (post-#1239).** **The B-96 chain's LAST loose end is closed** (PR #1239: the §11.9 fork-filing cross-stamp, landed doc-only after the DESIGN_IMPL_MIX split; the filing records the chain COMPLETE at leg 4 of 4, zero PENDING clauses). The #1237 arc-exit report is fully green (merge `39389d09`, post-merge main CI success, refresh `b90d5e76`). **The ratified leg queue resumes:** (a) **B-93 build leg — NEXT** (nine-site cross-process lock deadline + typed `CrossProcessLockTimeoutError` homed harness-core + reconciler import repair + the UNWITNESSED `_workflow_lock` witness; HAND-ROLLED vehicle per ratification; Windows serialization deferred on witnessability; register row B-93 is `open`); (b) **B-84 build leg** (executor pure `validate()` pre-pass; row `open`); (c) **B-88 spec leg** (C-MEM-19 seventh member `input_validation_failure` + boundary invariant + A-ii re-type; row `design_substrate_gated`) → impl leg; (d) B-110/B-111 groundings when reached. Tally unchanged: **111 items / 92 closed / 2 open / 1 design_substrate_gated / 15 registered_finding / 1 held**. held=1 honored.

**Prior next action (post-#1237).** **THE B-96 CHAIN IS CLOSED END-TO-END** (grounding → fork #1179 → ratification 2026-08-05 → council C-2 #1183 → spec leg #1235 Runtime v1.111 + plan v2.58 → impl leg #1237 U-RT-150): durable elapsed-time GC grace landed — 15/15 ACs witnessed (incl. one-shot accumulation via THREE real subprocess invocations), PD-8 matrix 18 KILLED / 2 honestly-inert (M2/M2b single-site conjunct removals; M2c both-site killed), all five standing runtime riders taken (#1226 nits ×3 + #1216 lens-3 ×2 — the rider queue is now EMPTY), codex 4 rounds — R1-R3 pre-PR (4 taken / 4 declined-pinned, honest non-convergence exit), R4 post-gate catching a bool-version [P1] (instant first-sweep reclaim, the term-4-forbidden direction) + a gauge-glob-parity [P2], both fixed+witnessed at the merged head; post-merge scoped verification of the ungated delta: APPROVE. **3-lens merge-gate: APPROVE / BLOCK / APPROVE** — lens 2 caught a blind `#pending`→`#1237` substitution corrupting four historical B-98/B-104 close_out sentences; fixed by 4-string revert, scoped lens-2 re-gate APPROVE (25th merge-gate catch). **Register: B-96 CLOSED, B-74 CLOSED (residue dissolved under C-2), B-77 residual closed; NEW B-111** (AC #15's no-removal clause forecloses publication-temp cleanup — dispositions (a) plan-delta narrowing / (b) accept-and-document, Phase-6 acts). Tally **111 items / 92 closed / 2 open (B-93, B-84) / 1 design_substrate_gated (B-88) / 15 registered_finding / 1 held**. **Owed doc-only rider:** the B-96 fork-filing §11.9 cross-stamp (impl leg landed at #1237) — deliberately held off the code PR (DESIGN_IMPL_MIX guard); prepared patch at `/tmp/b96-fork-cross-stamp-11-9.patch`; ride it on the next `.harness` doc PR. **The ratified leg queue continues:** (a) **B-93 build leg** (nine-site lock deadline + typed `CrossProcessLockTimeoutError` homed harness-core + reconciler import repair + `_workflow_lock` witness; HAND-ROLLED vehicle; Windows serialization deferred on witnessability); (b) **B-84 build leg** (executor pure `validate()` pre-pass); (c) **B-88 spec leg** (C-MEM-19 seventh member + A-ii re-type) → impl leg; (d) B-110/B-111 groundings when reached. held=1 honored.

**Prior next action (post-#1235).** **The `B-96` SPEC leg is LANDED** (PR #1235, merge `55e1c96f`): `Spec_Harness_Runtime_v1.md` **v1.110 → v1.111** with **THREE amendment sites, all inside §14.8.11** — a NEW **§14.8.11.1** carrying the `.harness/council-b96-grace-ceiling-2026-08-01.md` §7.1 **TWELVE** conditions as contract terms 1–12, plus two **strict APPENDS** (the bounded-retention bullet's cross-reference; the deferred-to-discretion list gaining the observation record's file name + serialization format + the emissions' field keys / message shapes). The reclaim rule becomes **CONJUNCTIVE and publication-bounded** — past-TTL filesystem age **AND** past-TTL elapsed time since a **DURABLY RECORDED** first observation, **no third path**, **NO absolute `k × ttl_seconds` ceiling** (ratified form **C-2**). `Implementation_Plan_Harness_Runtime_v2_57.md` → **v2.58** carries **ONE NEW unit `U-RT-150`** with **FIFTEEN** ACs — deliberately a new unit, **not** an amendment of the landed U-RT-145 whose AC #7 stays true (the `B-97`(a) → U-RT-149 precedent). Two clearance markers filed; the **FIVE-of-twelve EXPANSION FLAG** (conditions #5/#6/#8/#11/#12 did not exist in the fork's §8 ask) surfaced in the spec marker with a **Class 2 route named**. **ZERO** new `C-RT-*` numbers; **ZERO** CXA rows (frozen at **111**); **ZERO** OD delta (`C-OD-05` §5.1 roster **15 → 15**); **ZERO** `harness-*/src|tests` edit. **Register: `B-96` `design_substrate_gated` → `open`** per the register's own status enum (ratified + APPLIED spec *and* plan deltas open the row for code) — **not** `closed`, which still waits on the impl leg — and **ONE new row `B-110`** registered against this leg's own text rather than absorbed (the observation record cannot distinguish generations of a **reused temporary candidate name**, its ratified content set being closed at two members; plus a second, **pre-existing**, unlocked-removal mechanism recorded on the same row). Tally now **110 items: 3 open / 1 design_substrate_gated / 15 registered_finding / 0 operator_gated / 1 held / 90 closed**. Out-of-family Codex ran the **full 10-round cap — 21 findings, ALL UPHELD and FOLDED, zero declined, zero held** — exiting **CAP-REACHED, not reviewer-quiet**: round 10 surfaced a fresh substantive **[P1]** (term 9's *replace wholesale* admitted re-sampling a retained `first_observed_at`, which under sub-TTL sweep cadence slides every deadline forever and reclaims **nothing**), and its absorptions have not themselves been through a further round — recorded rather than glossed. Two findings reached **beyond this leg**: one **corrected a premise of the authorizing council record** (condition #5's *"never reused"* ground covers a file's lifetime, not post-removal reuse), one **went beyond the authority chain** (the clock assumption holds for **both** step directions, not only the backward one the fork names).

**NEXT UP — the `B-96` IMPL leg (`U-RT-150`)**: the durable candidate-filename + timestamp sidecar, the conjunctive reclaim rule, the two falsifiability surfaces (report-line field + a read-only `harness-inspect` row extension reporting the record's state THREE-WAY), the one-shot multi-invocation witness that discriminates the ratified form from the retired Reading B, and the re-grounding pass over every inverted grace-dependent witness including the `B-74` pin. **`B-96`, the `B-77` residual and `B-74` all flip to `closed` only at that merge.** Then: **`B-93` build leg**, **`B-84` build leg**, **`B-88` spec leg**.

**Prior next action (post-#1233).** **The 2026-08-05 ratification batch is RECORDED on every canonical surface** (PR #1233): six Class 2 fork filings stamped RATIFIED with their own prescribed mechanics (`B-96` §11, `B-88` §11, `B-93`+`B-45` §12, `B-84` §12, `B-98` §11.4, `B-104` §11.5), both register surfaces updated current-first, and the register tally re-derived — **109 items: 2 open / 2 design_substrate_gated / 14 registered_finding / 0 operator_gated / 1 held / 90 closed**. Out-of-family Codex ran the **full 10-round cap — 21 findings, ALL [P2], ALL accepted**, exiting on soundness rather than convergence-to-zero. **Substance landed in R1–R4 and R6–R9**: B-88's A-ii re-typing had no owning leg (R1); B-45's D-2 was abbreviated into an already-satisfied form (R1); the A-ii scope was eight sites when direct read shows **six** — `llm_dispatch.py:4344`/`:4346` validate the **model-supplied** `scope_ref` and must keep the new class (R4); row provenance had collapsed the filing PRs into #1233 (R4); the two retained raises sit on a **dispatch prepare path with zero `classify_memory_failure` calls** (R6); re-typing flips the six sites out of `_classify_provider_exception`'s fail-fast tuple into **`TRANSIENT_RETRY`**, verified by probe (R7); and — the most serious — round 7's own "or ratify the telemetry loss" wording would have let an impl leg weaken **C-MEM-19** with no spec leg, an **X-AL-3 hole**, so **preservation is now MANDATORY with a CLASS 1 back-flow route** (R9). **R5, R8 and R10 were propagation of those corrections** across surfaces — three instances of one class, which is why the arc closes on a programmatic cross-surface check (7 forbidden claims absent everywhere, 7 required claims present on every surface that must carry them). **No finding contradicted any verdict; every one constrained HOW a ratified reading is built, never WHETHER.** **held stays 1** — that is `R-1`, the confirmed-DEFERRED managed-cloud dispatch; the batch's own held sub-decision (B-96's C-1/C-2 ceiling) is RESOLVED, so no ratification-batch item is held any longer. **THE POST-RATIFICATION LEG QUEUE, in dependency-then-value order:**

**(a) `B-96` spec leg — HIGHEST VALUE, and it gates the most.** A Runtime **§14.8.11** amendment carrying the council record's **twelve conditions** (`.harness/council-b96-grace-ceiling-2026-08-01.md` §7.1), with §7.2's expansion flag surfaced in the clearance marker per `CLAUDE.md` §4.5. X-AL-3-gated: §14.8.11's deferred-to-impl list does not name a retention-extending grace, so landing it silently would be a violation. Anchor grounded at HEAD: the bounded-retention bullet is **`:4926` at Runtime v1.110** (heading `:4915`). **(b) `B-96` impl leg** — the `(C-i)` sidecar (temp-write + `fsync` + `os.replace` + directory `fsync`), the C-2 reclaim rule, the `_observe_expired` signature change, `first_observed_at` sampled under the lock, the `gc_sweep(now=)` seam, and the witness pass over the eleven `_sweep_past_grace` call sites + four reclaim asserts, each mutation-probed. **Spec and impl do NOT land together.** **Closing this leg closes THREE rows: `B-96`, the `B-77` residual and `B-74`.**

**(c) `B-93` build leg — the one immediately actionable `open` row with a correctness stake.** Nine acquisition sites bounded; one typed `CrossProcessLockTimeoutError` homed at **`harness-core`**; the ABBA release-before-blocking handoff preserved at all three probe arms; the hold-time contract at `write_record_guarded`; the §3(ii) reconciler import repair; ≥1 process-based witness per site-class **including the UNWITNESSED `_workflow_lock` carrier**, each PD-8 mutation-probed. **No spec leg** under any reading. **(d) `B-84` build leg** — the executor's pure `validate()` pre-pass, witnesses W-1/W-2/W-4/W-5/W-7 + **W-8 inverted**, W-5's four-cell matrix executed with answers recorded back on the row, and the §9 items 3–7 cite repairs.

**(e) `B-88` spec leg, then its impl leg.** `Spec_Memory_Substrate_v1.md` v1.2 → v1.3 **and** `Implementation_Plan_Memory_Substrate_v1.md` v1.2 → v1.3 (U-MEM-22 `:829`, no new unit) with **both** clearance markers; then the impl leg — 1 enum member + 1 declaration flip + 1 docstring rewrite + 1 fixture re-key, 8 witness rows flipped + 2 added negative rows + the mutation probe, **plus the A-ii source re-typing** at the `llm_dispatch.py` internal-fault sites with its reachability witness.

**TWO ITEMS THE GROUNDING PASS FALSIFIED, recorded rather than queued.** **(i) There is NO owed `B-98` docstring mini-leg.** The docstring-rewrite-at-ratification obligation is **Reading C's under `B-88`**, not `B-98`'s — and `B-88` ratified as **A**, so it does not fire; under A that rewrite rides the impl leg at (e). `B-98`'s own filing routes its only docstring item (`PauseSnapshot`'s *"8-field"* prose against a 15-field type) to the **`B-69` impl arc** and explicitly declines to re-register it. **(ii) NO separate `B-74`-residue filing is owed.** Under the ratified C-2 the council record's §8 finds `B-74` **closes** — its residual is *dissolved, not narrowed* — at the `B-96` impl leg, so the question a residue filing would have carried is already answered.

**Riders unchanged:** the `Spec_Control_Plane_v1_99.md:40` cite at the next CP delta (v1.116+); the lens-3 sharpenings + #1226 nits at the next runtime-touching arc. **`B-108`** (the missing logging-sink configuration, registered at PR #1183) stays `registered_finding` — ground it before reaching for a handler config; its three candidate shapes are unchosen and shape (2) is X-AL-3 spec-gated.

**Prior next action (post-#1183).** **THE RATIFICATION BATCH IS ANSWERED — all EIGHT verdicts ratified by the operator on 2026-08-05, every one on the filing's recommendation.** (1) **B-96 → Reading C with ceiling C-2** (elapsed-time grace WITH durable observation state, grace-term-alone form, carrier C-i dedicated sidecar) — the held C-1/C-2 sub-decision is RESOLVED as C-2, the council verdict carried by PR #1183, which is now **MERGED** (its DO-NOT-MERGE hold LIFTED) together with the new `B-108` register row. (2) **B-98 → Reading C (defer)** under D-0..D-3, D-0 dominant. (3) **B-104 → Reading D (declare + defer)** under D-0..D-3, Component 1 already landed at Runtime v1.110. (4) **B-88 → Reading A with sub-decision A-ii = re-type** (seventh `C-MEM-19` member `input_validation_failure` + boundary invariant). (5) **B-93+B-45 → Reading B, vehicle = hand-rolled extension** (deadline at all nine sites + typed `CrossProcessLockTimeoutError` homed at `harness-core` + reconciler import repair + the `_workflow_lock` witness; Windows serialization deferred on witnessability; NOT `filelock`). (6) **B-84 → Reading A** (executor pure `validate()` pre-pass), with the filing's held element ratified as **Class 2 (impl-discretion)**, not Class 1. (7) **B-95 → CLOSE** as a ratified accepted bound, re-open trigger preserved verbatim. (8) **`run_bootstrap` stays EXPORTED** in `harness_runtime.__all__` (the B-106 standing preference resolved: default stands). The verdict record rides the ratification-record PR that follows this refresh. **held=1 honored** (`R-1`, the confirmed-DEFERRED managed-cloud dispatch — B-96's ceiling resolution retired the batch's only held sub-decision, not a register `held` row).

**Prior next action (post-#1230).** **The justfile `{{ARGS}}` quoting hazard is FIXED** (PR #1230): the five remaining text-interpolation recipes converted to the twice-verified `"$@"` positional form after the third recurrence (this session, at the #1228 refresh) fired the memory entry's own escalation trigger; witnessed by execution through all four value-taking recipes including a `#`+spaces argument, codex converged R1 zero findings, and the post-#1230 refresh itself ran through the fixed `just roadmap-status` as the live post-merge witness. **The Claude-authorable frontier is now EXHAUSTED — the loop is at its genuine operator gate: THE RATIFICATION BATCH (SEVEN asks + B-95's close candidacy):** B-96 (A-or-C + held C-1/C-2 ceiling; PR #1183 **DO NOT MERGE**), B-98 (C/A-1/A-2/B), B-104 (D/A/B/C), B-88 (A/B/C + A-ii), B-93+B-45 (A/B/C/D + vehicle), **B-84 (A/C/B/D + the held Class-2-vs-Class-1 element — NEW at #1228)**, the `run_bootstrap` `__all__` preference. Riders stay queued on their trigger arcs (v1_99 cite at the next CP delta v1.116+; lens-3 sharpenings + #1226 nits at the next runtime-touching arc). Post-ratification the chosen spec/impl legs resume the chain; B-74-residue unblocks at B-96's answer. held=1 honored.

**Prior next action (post-#1228).** **The B-84 Class 2 fork is FILED** (PR #1228, `.harness/class_2_fork_b84_memory_batch_partial_commit.md`, 999 lines): the memory-tool batch partial-commit half — readings **A** (**RECOMMENDED** — executor pure `validate()` pre-pass, 1 method + 2 dispatch call sites, zero contract text; leaves policy-flip TOCTOU/intra-call windows and changes retry precedence) / **C** (runner-up — dispatch positional constraint: ≤1 durable-effecting call, must-be-last, from `requires_durable_memory_operation` metadata; blind to intra-call and single-call-`search` cases) / **B** (transactional/journaled batch — NOT buildable under this filing, opens a spec leg) / **D** (defer under D-0..D-3). Discriminator: is the harm an INPUT problem (→A) or an ORDERING problem (bounded→C, exceptionless→B)? Council probe-resolved for A/C/D, owed only under B. Codex ran the full 10-round cap — **cap exit on soundness, recorded honestly** (35 findings: 33 accepted, 2 partial on the Class-2-vs-Class-1 element, held under a pre-committed hold notice and routed to ratification). **Premise falsifications recorded in the filing (§9), row update rides ratification:** every successful call appends a ledger row (read-only tools included); the U-MEM-27 re-price cuts both ways (`write_record_guarded` is single-record CAS, unused by the memory-tool write path); the scope lock does NOT cover ledger appends (and is a Windows no-op — B-93+B-45's surface); the harm class is wider than batches (a single-call read-only `memory.search` with `limit=0` commits a durable retrieval event then raises); seven stale cites, five in shipped source. **THE RATIFICATION BATCH now holds SEVEN asks + B-95's close candidacy:** B-96 (A-or-C + held ceiling), B-98, B-104, B-88 (A/B/C + A-ii), B-93+B-45 (A/B/C/D + vehicle), B-84 (A/C/B/D + the held Class-2-vs-Class-1 element), the `run_bootstrap` `__all__` preference. **The Claude-authorable frontier is exhausted to riders** (v1_99 cite at the next CP delta v1.116+; lens-3 sharpenings + #1226 nits at the next runtime-touching arc; the stale codex-loop state cleanup DONE this session, local-only). B-96 PR #1183 remains **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1226).** **The flake-hardening arc is CLOSED** (PR #1226, operator-reported at #1213): the protected-result-store cardinality-4 CI-flake class hardened TEST-ONLY — six tests moved to a scripted clock driving the two `os.utime` stamps + `gc_sweep(now=)`, the granularity test phase-pinned, one incidental join widened; per-test PD-8 mutation-kill matrix + the counterfactual (0.5s stall injection reproduces the exact reported CI failure on the pre-fix copy); 5× + 6-concurrent determinism green; codex converged R1 zero findings; **3-lens merge-gate ALL-APPROVE FIRST PASS** (lens 2 independently re-ran all four mutation probes against Runtime §14.8.11 terms; B-74's residual witness and B-96's sweep-count semantics verified untouched). Three nits carried as riders on the next arc touching that file: the `:1528` same-shape join widen; a "both patches" undercount comment; the mutation-table drop-pre-commit column nuance. **Remaining Claude-authorable frontier:** (a) **the B-84 Class 2 filing** — the last sweep-shortlist item (live partial-commit defect: executor `execute()` commits earlier batch writes before a later argument failure raises; fork-shaped — validate-pre-pass vs transactional-batch directions, re-priced with U-MEM-27's seam at HEAD); (b) riders (v1_99 cite at next CP delta; stale codex-loop state cleanup). **THE RATIFICATION BATCH stands at SIX asks + B-95's close candidacy** (B-96 A-or-C + held ceiling; B-98; B-104; B-88 A/B/C + A-ii; B-93+B-45 A/B/C/D + vehicle; `run_bootstrap` `__all__`). B-96 PR #1183 remains **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1224).** **The B-93+B-45 combined Class 2 fork is FILED** (PR #1224, `.harness/class_2_fork_b93_b45_lock_deadline_windows_backend.md`): readings **A** (full fix incl. Windows backend — gated-but-self-dischargeable, its PR can carry the `windows-latest` behavioral witness; alone owes a dyadic C3 ⊥ C11) / **B** (**RECOMMENDED** — deadline at all nine sites + typed `CrossProcessLockTimeoutError` homed `harness-core` + the reconciler import repair; Windows serialization deferred on WITNESSABILITY) / **C** (Windows only — dominated) / **D** (defer both under D-0..D-4; runner-up, margin to B genuinely narrow). Two substantive corrections on the record: **Windows IS a committed host OS** (C-STK-10 + ADR-F4 — the clause dropped when B-40's dismissal propagated to B-45; both register surfaces rewritten current-first) and **`filelock` was recommended [HIGH] at `Plan_Executability_Audit_v1.md:84` with the binding decision never recorded anywhere** — resurfaced as the vehicle decision's third arm with the identity-migration caveat. The reconciler's `_workflow_lock` is UNWITNESSED at HEAD (build leg must add its witness). Codex TEN rounds to the cap, all accepted, recommendation never contested. **THE RATIFICATION BATCH now holds SIX asks + B-95's close candidacy:** B-96 (A-or-C + held ceiling), B-98, B-104, B-88 (A/B/C + A-ii), B-93+B-45 (A/B/C/D + vehicle sub-decision), the `run_bootstrap` `__all__` preference. **Remaining Claude-authorable frontier:** (a) **B-84** — the last shortlist item (live partial-commit defect; fork-shaped, filing-sized); (b) **the protected-result-store flake-hardening arc** (cardinality-4 CI flake; queued per the operator's 2026-08-05 #1213 report — a small code arc, full 3-lens gate, flows through merge+refresh per the operator's standing lens-3 instruction); (c) riders (lens-3 sharpenings; v1_99 cite at next CP delta; stale codex-loop state cleanup). B-96 PR #1183 remains **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1222).** **The elder-row grounding sweep is LANDED** (PR #1222): all 14 elder `registered_finding` rows re-grounded at HEAD — 14/14 premises intact, ZERO falsified, ZERO build-now (every actionable row is fork-shaped); dormancy re-stamps + 6-row stale-cite refresh in place; **B-103 reconciled** (`pr: '#1172'` + SUPERSEDED framing on both surfaces); **B-95 flagged as a close candidate** for the ratification batch; **B-74/B-96 corrected to siblings** (B-74 independently actionable). Codex ran 10 rounds to the cap — ten accepted single-surface precision repairs, zero contested dispositions. **The sweep's ranked next-arc shortlist:** (1) **B-93 + B-45 as ONE arc** — same nine-site/four-module lock primitive set, the one pairing with a correctness stake (the Windows no-op path silently degrades `write_record_guarded`'s cross-process binding); fork-shaped (deadline shape + typed timeout exception + Windows backend) → a Class 2 filing joining the ratification batch; (2) **B-84** — the only live reachable durable-data defect (partial-commit half; the re-price note applies, U-MEM-27's seam exists at HEAD); (3) **B-74** — independently actionable, same-file coordination with B-96's eventual leg advisable. **THE RATIFICATION BATCH stands at FIVE asks + B-95's close candidacy** (B-96 A-or-C + held ceiling; B-98; B-104; B-88 A/B/C+A-ii; the `run_bootstrap` `__all__` preference). Riders queued: #1216 lens-3 sharpenings (next runtime-touching arc); the v1_99 cite rider (next CP delta v1.116+); the stale codex-loop state cleanup. B-96 PR #1183 remains **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1220).** **The B-88 spec-half Class 2 fork is FILED** (PR #1220, `.harness/class_2_fork_b88_memory_failure_input_validation_class.md`): ONE decision, three options — **A** add `input_validation_failure` as a seventh C-MEM-19 member + boundary invariant (RECOMMENDED; carries sub-decision A-ii — the `llm_dispatch.py` internal-schema-fault raise sites must be re-typed or recorded mis-typed, their telemetry fate established at the spec leg) / **B** bless the `provider_adapter_failure` stopgap in contract text (the only option costing a later retraction; dyadic C7/C8 ⊥ memory-contract-owner convening owed ONLY here) / **C** defer under D-0..D-3 (docstring rewrite owed AT ratification). Discriminator: does `memory.failure_class` name the fault's KIND (→A) or LOCUS (→B)? Council PROBE-RESOLVED (zero exhaustive consumers measured at HEAD); codex 8 rounds / 13 findings all fixed, soundness exit at R8. **THE RATIFICATION BATCH for the operator's return now holds FIVE asks: B-107 (RESOLVED — closed end-to-end), B-96 (A-or-C + held C-1/C-2 ceiling), B-98 (C/A-1/A-2/B), B-104 (D/A/B/C), B-88 (A/B/C + A-ii), plus the standing `run_bootstrap` `__all__` preference.** Remaining Claude-authorable frontier: (a) the registered-pool sweep continues — 17 rows never re-grounded this round (B-16/B-30/B-44/B-45/B-56/B-57/B-66/B-71/B-74/B-84/B-93/B-94/B-95/B-99 + the three fork-filed); (b) the B-103 close_out reconcile + `pr: '#1172'` stamp at the next register touch; (c) the #1216 lens-3 sharpenings as riders on the next runtime-touching arc; (d) the v1_99 cite-correction rider at the next CP delta (v1.116+); (e) the stale codex-loop state cleanup rider. B-96 PR #1183 remains **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1218).** **The queued cite-hygiene pass is DONE** (PR #1218; dispositions: 5 fixed / 1 falsified-no-edit (CP "102 units" is the plan's own correct count) / 1 not-locatable (the "v2.36-vs-v1.27" carry has zero co-occurrence in-scope — retire or re-state the queue item precisely) / 1 confirmed-DEFERRED — the `Spec_Control_Plane_v1_99.md:9` `synthesis_step_id` v1.94→v1.58 cite fix rides the NEXT canonical CP spec delta (v1.116+) as a one-line rider, per the codex-accepted append-only-delta-chain [P2] (`v1_100.md:3` declares the v1.99 body preserved verbatim — in-place edits to historical deltas are forbidden)). **Register follow-ups queued at the next register touch:** (a) **B-103** — stamp `pr: '#1172'` AND reconcile its close_out to the PREMISE-FALSIFIED record (the close_out asserts a `run_audit_off_loop` dispatch mechanism that does not exist at HEAD; the offload was declined at the #1172 merge gate and stripped — roadmap Round 64); (b) B-98/B-104 carry `#pending` on `registered_finding` rows (correct for their status; B-104's Component-1 leg landed at Runtime v1.110, flagged only). **The frontier:** (a) the registered-pool ground-past-the-label sweep continues (18 registered_finding rows — B-98's D-0..D-3 demand tests, B-104's D-0..D-3, B-106's cross-trigger, the elder verified-dormant set); (b) the #1216 lens-3 one-line sharpenings (`match=` on `test_config_audit_signing.py:229/:231`; an explicit `validate_default` seal witness) as riders on the next runtime-touching arc; (c) the stale codex-loop state file (b107-arc worktrees) HARD-fails `just codex-review`'s closeout recipe — cleanup rider. B-96 PR #1183 remains ready, CI-green, and explicitly **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1216).** **B-109 is CLOSED END-TO-END** — grounded live pre-build (the `obj.__init__({...})` hole reproduced on BOTH the raw carrier and a validated frozen `AuditSigningConfig.key_arns` field; construction sites exactly two; tree-wide sweep found no further sealed-mapping siblings), then sealed per the B-107 template by a dedicated Opus impl agent (`__slots__` + refusing `__setattr__`/`__delattr__` + sealed positional-only `__init__`; `__reduce__` byte-unchanged so deepcopy/pickle/`model_copy(deep=True)` keep working with every copy itself sealed), 4 witnesses (16→20) + a PERFORMED PD-8 mutation probe, codex 2 rounds (R1's one [P2] — contradictory register narrative — fixed per replace-not-append), 3-lens merge-gate **ALL-APPROVE FIRST PASS**. Register: 108 items / 0 open / 18 registered_finding / 1 held / 89 closed. *Authorization note: the fresh operator `/loop /roadmap-continue` invocation post-#1215 was taken as the continue directive superseding the post-#1214 "surface before starting further arcs" hold — recorded here for the operator's review.* **The frontier (Claude-authorable):** (a) **the queued cite-hygiene pass** — now SIX accounting/cite claims: the three #1212 Gemini R3 pointer-file items (CP '102 units' enumeration arithmetic; Runtime '146 units' staleness vs U-RT-147/148/149; the Runtime-v2.36-strike-vs-CP-v1.27 carry), the B-98-era `Spec_Control_Plane_v1_99.md:40` `synthesis_step_id` misattribution, and the two #1216 lens-2 riders (the register close_out's derived-not-measured "7374 passed" → the CI-measured figure + run id; the `B-107 §6.1` cite wording → "B-107 §6.1 / CP v1.115 §1.5 forged-instance boundary, extended to explicit base-class routes at the B-107 impl-leg docstring") — each needs a programmatic recount before any edit, plus the `pr: '#pending'` → `'#1216'` reconcile on B-109's row at the same touch; (b) **the registered-pool ground-past-the-label sweep continues** (18 registered_finding rows); (c) the #1216 lens-3 one-line sharpenings (`match=` on the two bare `pytest.raises` at `test_config_audit_signing.py:229/:231`; an explicit `validate_default` seal witness) as riders on the next runtime-touching arc. B-96 PR #1183 remains ready, CI-green, and explicitly **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1214).** **B-107 is CLOSED END-TO-END** — the full chain (grounding #1177 → Class 2 fork #1178 → ratification 2026-08-03 → spec leg #1212 CP v1.115 + plan v2.49 → impl leg #1214 U-CP-64 AC #A12) is complete: scalar empty-key exclusion + validated immutable-copy map carrier + resolver-boundary guard, 29 new witnesses + the ratified b80 flip, codex 4 rounds converged (one decline PINNED on X-AL-3 fourth-amendment-site grounds), 3-lens merge-gate ALL-APPROVE FIRST PASS. Register: 108 items / 0 open / 19 registered_finding / 1 held / 88 closed; **NEW B-109** (sibling `_ImmutableKeyArns` inherited-`dict.__init__` re-population hole, surfaced at the impl leg's codex R2 — grounding owed before build, fix template = the B-107 sealed carrier). **The operator's 2026-08-04 authorization covered the impl leg ONLY — surface before starting further arcs.** Claude-authorable candidates awaiting the operator's pick: (a) the queued cite-hygiene pass (three pointer-file accounting claims from the #1212 Gemini R3 round — programmatic recount before any edit); (b) the B-109 grounding + fix leg; (c) the registered pool sweep continues per the ground-past-the-label discipline. B-96 PR #1183 remains ready, CI-green, and explicitly **DO NOT MERGE**; held=1 honored.

**Prior next action (post-#1212).** The B-107 A-hybrid specification arc is COMPLETE: PR #1212 landed CP spec v1.115 + plan v2.49 (U-CP-64 AC #A12) + the fork §11 ratification + 2 clearance markers + the register flip `design_substrate_gated → open`, with the three 2026-08-03 handoff findings adjudicated (2 repaired, 1 declined on corpus), Gemini rounds 1-3 adjudicated (R4 outage-blocked; its final attempt's 5 P1s all falsified against measured bytes), and the 3-lens merge-gate all-approve + scoped re-gate. **The loop is now AT THE HANDOFF'S HIL BOUNDARY: do NOT start the B-107 implementation leg (U-CP-64 AC #A12 — eight-cell grid, PD-8 probes, b80 flip, reader inventory; fresh worktree agent per the B-97/B-69 precedent, full 3-lens gate) without explicit operator authorization.** Claude-authorable while held: the queued cite-hygiene pass (three pointer-file accounting claims from Gemini R3 — CP '102 units' enumeration arithmetic, Runtime '146 units' staleness vs U-RT-147/148/149, the Runtime-v2.36-strike-vs-CP-v1.27 carry — each needs a programmatic recount before any edit). B-96 PR #1183 remains ready, CI-green, and explicitly **DO NOT MERGE**. The B-107 implementer-leg worktree `.codex-worktrees/b107-a-hybrid-spec-leg` is retained untouched for the impl leg; the controller worktree's branch `b107-a-hybrid-spec-codex` is merged (operator prune candidate).

**Prior next action (post-#1210).** The R-IF-116 insights-residue workspace-tooling program is COMPLETE and RESOLVED (8 of 8 PRs merged: #1196 #1198 #1200 #1202 #1204 #1206 #1208 #1210; U-WT-02/U-WT-07 remain plan-documented conditional follow-ons, deliberately not register rows; per-unit as-built record at `.harness/r-if-116-insights-residue-plan.md`; wave5 hooks outcome at `.harness/wave5-hooks-status.md`). The remaining frontier is (2) from the post-#1196 derivation: continue the launched B-107 A-hybrid specification arc using the Codex GPT-5.6 Sol orchestrator / GPT-5.6 Terra implementer pattern — rebase its controller onto current `main`, reconcile the outstanding large-diff review findings against the actual design-only delta, re-record every fingerprint-bound autonomous gate, obtain OAuth-only Antigravity Gemini 3.1 Pro approval, and drive the specification PR through CI, merge, and its terminating refresh. B-96 PR #1183 is ready and CI-green and remains explicitly **DO NOT MERGE**. Stop for HIL after the B-107 terminating refresh, before starting B-107 implementation, so Claude can resume from a clean roadmap fixed point.

**Round 73 — the B-104 Class 2 fork FILED 2026-08-01 at PR #1181 (doc-only, 1218 lines; the filing queue is COMPLETE and the loop's authorable frontier is exhausted to the RATIFICATION BATCH).** The demand gate answered in layers: the discrimination IS structurally required by the drain recourse's confirmation half (resolution writes nothing — the enumeration is monotone non-decreasing under a perfect drain; only the zero case self-confirms; the spec concedes the shape at `:5944`), BUT every flow that branches on it is LEGACY-scoped and frozen post-cutover, and disposal fails conservative. **Shape (c) "derive from existing records" GROUNDED AND FALSIFIED** (the resume-specific IS-ledger write is constant-`action_id`, fires BEFORE the success test, and carries no tenant while the journal address does — co-tenant resume would produce a false all-clear); **a FOURTH shape found** (a resume event in the existing OD §C-OD-30.5 pause-state sink, carried as Reading C, priced with the sink's missing locking + tenant qualifier). **Two empirical probes:** both sentinel shapes route to `CORRUPT_LATEST` → `indeterminate=True` at HEAD (a resolved journal would report as an in-flight torn append — the row's (a)-2 default is actively wrong); the sixth cause member forces a CROSS-AXIS OD LEG (harness-od re-declares the five values; `api.py:1163` converts by value and raises on unknown). Plus: the ambiguity is NOT legacy-confined (both public read surfaces report an already-resolved pause as current, the limit declared at §13.7 and undeclared on both — Reading D's mandatory Component 1 fixes exactly that asymmetry). **Recommendation D (declare + defer with D-0..D-3); runner-up A NARROWLY and HELD** (the A-vs-B element moved three times on three grounds → [[reviewer-oscillation-register-and-hold]], routed to ratification). Council: probe-resolved for D; dyadic C3⊥C11 only under A; +C7/C8 under C. Codex 3 rounds, 13 findings incl. THREE absorbed against the filing's own interest. **Next action — THE RATIFICATION BATCH (the loop's genuine operator gate):** B-107 (A-hybrid/B/C/D), B-96 (A-or-C + the held C-1/C-2 ceiling), B-98 (C/A-1/A-2/B), B-104 (D/A/B/C), plus the standing `run_bootstrap` `__all__` preference. Post-ratification: the chosen spec legs resume the chain (B-96's council convening if C; B-107's leg; etc.); B-74-residue unblocks at B-96's answer. held=1 honored.

**Round 72 — the B-98 Class 2 fork FILED 2026-08-01 at PR #1180 (doc-only, 943 lines; the demand question answered by FALSIFYING the filing's own first draft).** Codex round 1's [P1] found the load-bearing fact: **the HITL-pause class is production-DORMANT in stock bootstrap** — the sole `HITLPauseRequestedSignal` producer cannot pass the webhook delivery at `hitl_gate_composer.py:1324` without an operator-supplied `WebhookConfig` no shipped config surface provides, and that raise PRECEDES both the pause flag and the signal; B-98 describes a real gap in a dormant class. Second find: **the description text already reaches the operator contractually** in the webhook payload (a code-order PRECONDITION of the pause), so the durable-text gap is really CORRELATION — B-71's surface — narrowing the row's own distinctness claim; the PLACEMENT (not contractual — `compose_hitl_action_id`'s shape is impl-discretion) is the genuinely net-new field. Readings: A-1 declared-placement tuple (runner-up) / A-2 firing placement (+carrier-home/channel costs) / B `escalation_reason` / B′ author-text / **C DEFER (RECOMMENDED)** with a FOUR-DISJUNCT falsifiable demand test (D-0 the class going production-reachable; D-1..D-3 evidence triggers) so the row reopens on evidence, not vibes. Hash disposition pre-decided (the five-instance drop-when-`None` precedent from CP v1.45); carriers collapse to two sites; the undecided question is the CHANNEL, not the field. **Council reversed from the row's unconditional yes-if-opened → probe-resolved for C/A-1**; dyadic C10⊥C11 owed only under A-2/B. **6 codex rounds, 17 findings all upheld by direct read; recommendation and runner-up never moved; soundness exit per the deferred-mechanism discipline.** Two cite corrections recorded + a Class 3 cite-hygiene find (`Spec_Control_Plane_v1_99.md:40` misattributes `synthesis_step_id` to v1.94; first occurrence v1.58 — programmatically falsified; queue for the next cite-hygiene pass). Process note on the record: the agent twice pre-wrote "Round N — clean" before running the round — both caught and corrected; the record states real outcomes. **Next action:** (a) **the B-104 filing** (last of the queue; its self-imposed operator-need grounding gate folds in as B-98's demand question did); (b) **THE RATIFICATION BATCH** now holds FOUR asks for the operator's return: B-107 (A-hybrid/B/C/D), B-96 (A-or-C + held ceiling), B-98 (C/A-1/A-2/B), the `run_bootstrap` `__all__` preference; (c) B-74-residue deferred pending B-96; held=1 honored.

**Round 71 — the B-96 Class 2 fork FILED 2026-08-01 at PR #1179 (doc-only, 843 lines; the sweep's third finding PROVED DECISIVE — the row's own close_out design is recommended AGAINST).** The registered elapsed-time-per-process design (Reading B) is DOMINATED: `_root_observed_expired` is in-process, so in the dominant one-shot `harness run` shape a `first_observed_at` never survives to reach TTL — a genuinely expired entry is NEVER reclaimed, retention UNBOUNDED (a §14.8.11 FAILURE, not the close_out's claimed one-TTL trade). **Recommendation: Reading C** (elapsed-time WITH durable observation state; form C-2 grace-term-alone, carrier C-i), strengthened by the filing's own counter-direction find — the PUBLICATION-BOUND LEMMA (`t_first_obs ≥ t_pub`: a sweep cannot observe a file that does not yet exist), making the durable elapsed-time bound precisely the "age authority that cannot predate publication by construction" that BOTH B-77 and B-74 name. Runner-up A (keep sweep-count + document honestly + close). Grounding extras: no repair-flow consumer exists at HEAD (read/ack_delete: zero production call sites); the 300s opportunistic trigger floors sweep-count in the DAEMON shape only; B-74's pinned residual was placed to be updated by exactly this fix (so **B-74-residue's own filing is DEFERRED until B-96 ratifies** — its arithmetic hangs on the outcome). **Council position REVERSED on the record: CONVENE** (dyadic C3+C7, scoped to C-1-vs-C-2 — the probe established §14.8.11's word is "unbounded" not "exactly TTL", foreclosing the strongest objection but RELOCATING the tension into the ceiling choice; [[probe-resolves-fork-prescribed-council]] recorded in the negative). One sub-decision (C-1 vs C-2) HELD under the oscillation rule after three moves. **Codex 6 rounds to soundness exit** — every finding absorbed including THREE arguing against the recommendation; one self-authored observation probed and FALSIFIED before filing (recorded, not deleted). One anchor moved (v1.108 `:4883` → v1.109 `:4909`, byte-identical text). **Next action:** (a) **the B-98 filing** (gate-description carrier: the field/carriers/hash tri-decision); (b) then **B-104** (its operator-need grounding gate first); (c) the RATIFICATION BATCH for the operator's return now holds: B-107 (A-hybrid/B/C/D + 8 sub-decisions), B-96 (A or C + the held C-1/C-2 if C), the `run_bootstrap` `__all__` preference; B-74-residue deferred pending B-96; held=1 honored.

**Round 70 — the B-107 Class 2 fork FILED 2026-08-01 at PR #1178 (doc-only, 983 lines; the first of the five filing legs).** Readings: **A (RECOMMENDED, a HYBRID review forced to three parts)** — (a) amend CP v1.107 §1.1 membership so an empty key leaves the unaddressed/eligible set + (d) refuse operator-authored `{"": …}` at construction via a key-domain constraint PLUS a validated immutable mapping + (e) enforce both at `_resolve_effect_fence_gated`, the one private site all seven consults transit; **B (runner-up)** — an input-keyed refusal at the `execute_workflow` driver entry (a 13th §30 fail class; re-priced at codex R2 — no root discriminator, no tree walk); **C** — the bounded `_any_fence_abort` slice, explicitly non-closure and subsumed by A; **D** — defer. The row's admission-seam recommendation WITHDRAWN on verified bypasses (`execute_workflow` is a public export; the depth-0 seam bypassable too). **The filing's empirical find:** `frozen=True` does NOT freeze the nested dict — `c.effect_fence_resolutions[""] = ABORT` is ACCEPTED at HEAD post-construction, reproduced live at codex R1 — which forced part (e) and struck "unrepresentable" from the disposition prose; R3 found B's own post-check mutation window (a fourth mandatory term); R4's `[P1]` (a `model_construct` refusal) PARTIALLY DECLINED with threat-model grounds in a new §6.1 (the bar would unmake every type contract in the tree; the B-97(a) §3(iii) precedent). **Zero recommendation flips across 4 rounds; soundness exit declared; council probe-resolved for every reading — no convening owed, so leg 2 is a single ask with no gate 2.** Margin between A and B stated THIN with the discriminator named: fix the scalar classification, or stop the harm at the door. Register unchanged (the row flip rides ratification). **Next action:** (a) the remaining four filings in sweep order — **B-96** (elapsed-time grace + the crash-lost `first_observed_at` question the sweep surfaced), **B-74-residue**, **B-98**, **B-104**; (b) **the ratification batch for the operator's return**: B-107's A/B/C/D (+8 sub-decisions), plus the standing `run_bootstrap` `__all__` preference; (c) held=1 honored.

**Round 69 — the B-107 grounding ABSORBED 2026-08-01 at PR #1177 (register-truth arc; the row's own round-11 self-contradiction fixed in place).** The grounding verdict — SPEC-LEG-FIRST with the spec leg itself FORK-SHAPED — is now on the row, with: the corrected SEVEN-SITE consult map (the false "branch and orchestrator consults short-circuit" claim replaced — sites `:7839`/`:11708` call the resolver with `""` UNGUARDED, the row's own earlier finding; the orchestrator's sole consult IS guarded); the two new findings (the `any()` position-dependence — crash-reconstruction `""` entries append LAST, so a keyed ABORT short-circuits before the empty entry is evaluated, an independent unsoundness of the single-function shape; the FOURTH terminal consumption site `managed_agents_dispatch.py:300`, verified exactly-two tree-wide); the closure-criterion AMENDMENT (codex [P1]: the unwidened wording was literally satisfiable BY the defect — the `{"": ABORT}` suppression APPLIES the resolution with cross-location collateral, the misattribution v1.107 §1.1(a) forbids); the four fork decisions with the 2×3 witness matrix (branch carrier spanning TWO topology sites per codex R2); and the not-free `_any_fence_abort` slice (v1.111 §2.1(b)'s never-a-narrowing clause). **Status transited `registered_finding` → `design_substrate_gated`** — the register's own enum for a no-buildable-slice spec-first verdict (digest re-pinned). Reconciles: B-100→`#1174`, B-101→`#1175`, B-106→`#1176`, plus stale inline `#pending` promises cleared (B-69→`#1166` among them). Codex 6 rounds (R1 caught the seam-placement claim FALSE — `execute_workflow` receives everything; R2 the depth-0 bypass + the two-topology-site witness widening; R5 the criterion still permitting local honoring of `""`). Register **107 items / 87 closed / 18 registered_finding / 1 design_substrate_gated / 1 held**. **Next action — the frontier is now FIVE fork/filing legs, all Claude-authorable with operator ratifications batching at their ends:** (a) **B-107's Class 2 filing** (the freshest grounding; the four decisions + amended criterion are the filing's skeleton); (b) B-96 (+ the crash-lost `first_observed_at` question); (c) B-74-residue; (d) B-98; (e) B-104 (its operator-need grounding gate first). Serial authoring in that order unless the operator re-prioritizes; each filing merges doc-only, then its ratification AUQ batches. held=1 honored.

**Round 68 — B-106 CLOSED NOT-SUPPORTED 2026-08-01 at PR #1176 (documentation-only; the embedder question adjudicated on a four-leg grounding).** The grounding ruled the embedding contract DEFINED BY EXPLICIT EXCLUSION, not undefined: Runtime §4 `:3093` labels `HarnessContext` "not part of any operator-facing surface in Track A" (with §9's `RunResult` labeled the opposite one section later — deliberate contrast); the operator-facing API self-enumerates as EXACTLY three package-root async functions ("exactly one"/"a second"/"a third"); the contracted external pause route is `pause_requested_flag` (set the flag, the driver captures — §4 `:3085` + §14.14.7); zero library-embedding text exists anywhere in design-substrate (every `embed*` hit is the CP EMBEDDING routing layer or embedded-credentials prose); and the persona/deployment surfaces (ADR-D2's three tiers) never contemplate library embedding — with **B-69 as the controlling precedent for how a surface GETS minted** (spec § + contract decision + ADR cite + council + ratification + package-root export; `ctx.pause_resume_protocol` has had none of it). The daemon path was checked and B-103's falsification SURVIVES it (its captures also route through the private-loop helper) — the embedder path was the sole live-loop route, and it is out of contract. **Landed:** two docstring paragraphs (the `run_bootstrap` AUDIENCE paragraph; the `pause_resume_protocol` field-comment hazard note) — both hunks verified zero-code-lines; B-106 → closed with the four legs + the shelved-offload CROSS-TRIGGER (re-check B-106 alongside B-103 if `_run_protocol_method_sync` ever changes — the SUPPORTED branch would reopen on an in-house benefit); codex 3 rounds with TWO real prose defects accepted on source (the private-loop claim was false for `read_paused_workflow_state`; the pause-route sentence advertised a route with no exposed external setter — now recorded as a grounded sharpening: the designated route being deliberately at-discretion is the ruling's own evidence). **OPERATOR-DECIDABLE note, default taken:** `run_bootstrap` sits in `harness_runtime.__all__` with zero in-repo package-root consumers; dropping it would make the ruling structural but is a §9-major-bump-shaped narrowing — left in place, awaiting the operator's preference. Register **107 items / 87 closed / 19 registered_finding / 1 held**. **Next action:** (a) **B-107's grounding leg** (the empty-fence-key resolution-refusal gap — cheap read-only grounding: which channel should refuse, what the fix shape is, whether spec text is owed); (b) the four fork-shaped rows await Class 2 filings (B-96 + the crash-lost-first_observed_at question; B-74-residue; B-98; B-104's operator-need gate); (c) the pr reconciles (B-100/B-101/B-106 → real numbers) ride the next register touch; held=1 honored.

**Round 67 — B-101 CLOSED 2026-08-01 at PR #1175 (CP v1.113 → v1.114; the (b)-PLUS single-paragraph leg, landed with more rigor than its size suggests).** The §1.2 residual now formally scopes ORDINARY base-schema Pydantic serialization OUT of the staleness fence (the base schema, not the value, decides what is emitted — the provenance carrier drops with no observable conversion); the one live base-typed carrier is NAMED (`StepExecutionContext.resume_context`, `workflow_driver_types.py:460`) with three grounded HEAD-dated facts keeping it unreachable (zero `model_dump`/`TypeAdapter` of the field or its enclosing model repo-wide — re-verified fresh, not carried; shallow non-revalidating `model_copy`; `revalidate_instances` unset); and a **MECHANISM-keyed promotion trigger**: ANY future base-schema serialization boundary over a ResumeContext value on a resume path promotes B-101 to disposition (a) BEFORE-or-simultaneously — keyed on the mechanism after codex R3 caught the named-carrier wording missing the direct-TypeAdapter route the pin test itself demonstrates. **Codex 4 rounds; the R2 [P1] handled at full honesty** — the "this narrows §1.3(c) rather than clarifies" counter-reading was neither conceded nor hand-waved: the glib scoping sentence was replaced with the precise CP-owned-affordances ground (AC #A2's actual three-route witness) and the counter-reading recorded IN FULL as §0.5 finding (vi) with its one-step escalation route, a reviewer-overturnable scoping determination at contract altitude. The pin test stays as the boundary marker with its own gap stated (fires on behavior, not on the mere addition of a dump call — pre-merge review is the enforcement). No plan unit owed, verified. Register **107 items / 86 closed / 20 registered_finding / 1 held**. **Next action — the frontier is now entirely fork-filings + grounding legs:** (a) the four fork-shaped rows await Class 2 filings — **B-96** (elapsed-time grace; MUST name the crash-lost `first_observed_at` question the sweep surfaced beyond its close_out), **B-74-residue** (TTL-floor vs probe-fallback branches with different back-flow classes), **B-98** (gate-description carrier: field/carriers/hash tri-decision), **B-104** (pause-RESOLVED marker: the self-imposed operator-need grounding gate first); (b) **B-106** (embedder-contract grounding — is `ctx.pause_resume_protocol` a supported surface) and **B-107** (empty-key resolution refusal) grounding legs; (c) `pr:` reconciles (B-100/B-101 → real numbers) ride the next register touch; held=1 honored. Pick order per value: B-106's grounding is cheapest and unblocks-or-closes the preserved B-103 offload; B-104's grounding gate likewise cheap; the four fork filings are heavier authoring arcs.

**Round 66 — the B-100 impl remainder CLOSED 2026-08-01 at PR #1174; B-100 CLOSED END-TO-END — and the session's FIRST all-approve-first-pass gate.** The bounded arc landed the keyless sibling routing at both projection sites (mirroring the branch template; every construction site now inside its routing helper — the unconditional constructions at `:509/:522` are gone) with `min_length=1` on the key-bearing shapes landed ATOMICALLY (the agent DISCOVERED the coupling the ACs never stated: the b80 keyless-LINEAR tests drive `compute_effect_fence_tree_wide_abort_present` whose empty-key filter runs AFTER the walk — tightening alone would raise on a reachable production state; the coupling turns out doubly witnessed by pre-existing b80 tests, a half-revert failing FIVE tests, two outside the PR's blast radius). All AC #A11(a)-(d) + Runtime #15(b)/#16(a)(ii)/(iii) witnessed and probed (M4b proving c3's non-redundancy via a defaulted key; M7 pinning the B-107 scope boundary — `PauseTreeWalkEntry.effect_fence_key` must keep surfacing `""`, doubly pinned by a pre-existing b80 witness); the four fan-out coercion sites verified through the call graph (route correctly via the branch sibling — the spec leg's finding (iii) confirmed); B-107's channels byte-unchanged (hash-verified by lens 2). **Gate: ALL-APPROVE FIRST PASS** — lens 1 (pure projection; validator raise contained by the pre-existing CORRUPT_LATEST wrap), lens 2 (10-shape enumeration exact; min_length REQUIRED by the ACs; the carrier prohibition honored — `pause_resume_protocol_types.py` byte-identical), lens 3 (all nine witnesses discriminate BY EXECUTION; zero pre-existing witnesses weakened, AST-verified). Codex converged round 1. Tests CP 1859→1866, Runtime 3557→3559. Register **107 items / 85 closed / 21 registered_finding / 1 held; B-100 closed `pr: '#pending'`→reconcile at next touch**. **Next action:** (a) **B-101's (b)-plus spec paragraph** — the smallest bounded remaining arc (extend CP §1.2's residual to name ordinary base-typed serialization AND the `StepExecutionContext.resume_context` carrier by name, recording that no `model_dump` of that field exists today and that adding one promotes the row to disposition (a); single-paragraph delta + marker + row close); (b) the fork-shaped rows (B-96, B-74-residue, B-98, B-104) await their Class 2 filings; B-106 awaits its embedding-contract grounding; B-107 its channel-refusal leg; (c) held=1 honored.

**Round 65 — the B-100 spec leg CLOSED 2026-08-01 at PR #1173 (CP v1.113 + Runtime v1.109; the pre-selected source-column widening applied, with the row's own premise partially FALSIFIED on the record).** The dedicated spec-writer applied disposition (a): the key-ABSENT carve-out widened from "the crash-reconstruction carrier" to ANY effect-fence source shape whose captured key is empty; keyless sibling source shapes for the LINEAR and orchestrator carriers; the variant set explicitly UNCHANGED at four (effect-fence-addressable shapes 4→6, union 8→10, carriers steady at 7 with three now dual-shape); plan riders CP v2.48 (U-CP-64 NEW AC #A11, split c1/c2/c3 after codex caught the omitted-key pass) + Runtime v2.57 (U-RT-148 ACs widened); 4 clearance markers. **Four conflicts REPORTED, never absorbed:** (1) the row's ORCHESTRATOR premise is FALSIFIED — `workflow_driver.py:12376-12381` ends with a truthiness guard, so that site cannot emit `""`; the shape was applied anyway on a corrected TYPE-TOTALITY ground (the carrier field is unconstrained `str`; journaled records outlive capture code) with the unreachability recorded as constructed-snapshot-witness-only and flagged overturnable; (2) the mechanism claim was over-broad (false for LINEAR); (3) the branch keyless shape was already under-scoped (ordinary fan-out coerces to `""` at four sites); (4) a pre-existing §2.1 shape-vs-carrier count conflation. **Codex 12 rounds** (2 P1 + 13 P2; the level-local `_any_fence_abort` empty-key suppression catch; ONE P1 — "get operator ratification" — REBUTTED with grounding recorded in-doc: the row's own council field routes to a spec-writer apply, the rule was ratified at B-69, and the hash-adjacent option was not applied). **Deviations, both correct:** B-100 flipped `registered_finding → OPEN` (not closed — the register enum's own built+merged bar; the B-97(a)/B-102 precedent), narrowed to the impl remainder: `walk_pause_tree` builds both shapes UNCONDITIONALLY from the carrier key today (`pause_state_projection.py:509/:522` — an empty key surfaces as a `""` value); the two new shapes need `_effect_fence_branch_projection`'s sibling at U-CP-64/U-RT-148. **NEW B-107** (no channel refuses a resolution addressed to an empty fence key — the response map and the scalar fallback both drop it silently on all three carriers; its liveness-cost claim was falsified at codex and re-scoped). Handled a CONCURRENT register mint (B-106 landed on main mid-arc) by merge+renumber. Register **107 items / 84 closed / 21 registered_finding / 1 OPEN (B-100) / 1 held**. **Next action:** (a) **the B-100 impl remainder** — the keyless-shape emptiness routing at both projection sites + the A11 c1/c2/c3 witnesses (bounded; the branch sibling at `:621-:649` is the template; full 3-lens gate on the code PR); (b) B-106/B-107 join the registered pool; the fork-shaped rows (B-96/B-74res/B-98/B-104) await filings; (c) held=1 honored.

**Round 64 — B-103 CLOSED PREMISE-FALSIFIED 2026-07-31 at PR #1172 — the merge-gate's strongest catch of the session, at ARC scale.** The registered-pool sweep had ranked B-103 the sole BUILD-NOW row (zero grounding debt; witness pre-specified by its own close_out); the arc BUILT it well (dedicated executor after a real codex P1 on shared-audit-pool stalls; a measured loop-starvation witness — 0.6s vs 3.0s, 27 ticks vs 1; join-on-cancel chosen by reading the source over the brief's inverted characterization). **Then lens 1 read one function deeper**: every production `capture_pause_snapshot` (~11 sites) reaches the store through `workflow_driver._run_protocol_method_sync` → `asyncio.run` on a PRIVATE single-task loop inside the driver's own `to_thread` worker — the docstring itself says "no current event loop is bound on the worker thread". **The loop the offload rescues does not exist at any shipped call site**; the offload = a second blocked thread + a new cancellation window (the just-registered B-105) for ZERO recovered concurrency; the witnesses were sound for a call shape production never uses. Lens 2 added a mechanical BLOCK (B-105's close-for-free routing pointed at B-93 whose 8-site enumeration EXCLUDED the site) + verified the no-spec-text call correct on STRONGER grounds (the cleared v1.108 change-note itself adjudicated it); lens 3 APPROVE with 3 minor gaps. **Rework (`1249f938`): honest subtraction** — offload stripped (src byte-identical to main except the docstring, now carrying the premise-falsified account + the reopening trigger: a captured-loop bridge replacing that `asyncio.run`, the evolution the helper's own docstring anticipates); B-105 WITHDRAWN (its window only existed with the offload); B-103 `closed (PREMISE FALSIFIED)` with the interleaving + built-gated-declined record + preserved pre-rework commits; **B-93 → NINE sites / FOUR modules in both surfaces** (the co-rider B-45 text now agrees); the 7-item hygiene rider kept with the lens's own numbers corrected (anchors re-measured at the merge tree; 17 not 7 `ensure_ascii=False` modules). Codex: 2 real pre-rework rounds; the final confirming round NOT OBTAINED (the known streaming-capture limitation) — honestly recorded, mitigated by pure-subtraction scope. Memory updated: the unverified-hypothesis discipline gains the escalated instance ("when a row's premise names a RUNTIME CONDITION, grounding must trace the call chain to the VENUE — ask on WHICH loop/thread/process before building"). Register **105 items / 84 closed / 20 registered_finding / 1 held**; digest `36332a5aa9cdcc2e`. **Next action:** (a) **the B-100 spec leg** — the sweep's runner-up, disposition pre-selected by its own close_out ((a) spec-side widening: key-ABSENT scoped to ANY effect-fence source shape with an empty captured key; keyless sibling source shapes for the LINEAR + orchestrator carriers; variant set stays FOUR; one paragraph in each of CP v1.112 §2.1 `:109` and Runtime §14.14.9.2 `:3475` + plan rider + markers — bounded, precedented by the two prior source-column widenings); (b) the fork-shaped rows (B-96, B-74-residue, B-98, B-104) await their filings when picked up; B-99/B-101 hold their dated re-checks; (c) held=1 (R-1) honored. **Post-merge follow-up (same day):** codex's #1172 confirming round flushed LATE with two P2s — the B-93 prose all-eight→all-nine fix landed, and **B-106 registered** (embedder-path loop exposure: `run_bootstrap()` public + `ctx.pause_resume_protocol` reachable on a live app loop bypasses the private-loop helper; codex measured 300ms→~2s; the built offload sits preserved on the shelf; the row carries the decorrelated-reviewer disagreement as a SCOPE question — is the embedder surface supported — rather than silently adjudicating).

**Round 63 — the B-97(a) impl leg CLOSED 2026-07-31 at PR #1171; B-97 CLOSED END-TO-END — the full chain (grounding split → (b) #1167 → fork #1168 + Reading-A ratification → council #1169 + Gate-2 → spec #1170 → impl #1171) completed across two loop days and one machine shutdown/resume.** The leg survived the shutdown as a pushed WIP (all 12 ACs implemented pre-pause; a finisher agent completed the 3 mechanical pyright errors — one honestly relocated from where the WIP note claimed — + the 4 codex-R4 witnesses, and caught TWO WIP-state substance defects on the record: a probe gone VACUOUS through double-enforcement, now reverting both points, and an exhaustive-signature witness updated without weakening). **Shipped:** tenant enters the FILENAME DIGEST (the spec-MANDATED realization — v1.108 `:5912` forecloses the subdirectory layout; lens 2 corrected the PR's own over-cautious "deviation" framing); required `tenant_id` kwarg; the (3b) adoption tool (under-lock re-classification per a codex-R4 P1; intent-account durable BEFORE publication per another; write-once `os.link` commit as the empty-target enforcement); disposal (dry-run default, refuse-while-recoverable); §13.7 enumeration (lock-file exclusion, chmod-read-only fixture); the B-102 read-side flip. **K-17 determined** (no OD carrier — 4-leg grounding verified by lens 2). **Gate:** lens 1 APPROVE (8 interleavings clear); lens 2 APPROVE (encoding grammar exact; K-17 verified; register digest recomputed); **lens 3 BLOCK — the TWO-HALVES class again**: the capture-side tenant wiring (`pause_resume_protocol_factory.py:207-210`) had ZERO witness — `tenant_id=None` passed all ~107 new tests while the READ side was fully witnessed (the misdiagnosis direction: a tenanted deployment would capture untenanted and resume would silently return `absent`). Fixed test-only at `3636762c` (lens 1 valid by construction): a ROUND-TRIP witness through the real stage-5 factory + the public read under one config, second-direction probe INDEPENDENTLY reproducing the zero-coverage claim; G2-G5 riders (typer success-path forwarding, current-format exit-0, required-kwarg pin, lock-sibling unlink) all probed; one VACUOUS G4 first-probe caught and recorded rather than counted. **Scoped re-gate APPROVE, empirical** (each mutation exactly one named failure; vacuity escapes foreclosed). Codex across the arc: 21 findings 21 accepted, R6 clean. Tests: runtime 3557 / workspace 7332; pyright 0/0/0. Register **105 items / 83 closed / 21 registered_finding / 1 held; B-97 + B-102 CLOSED at `pr: '#1171'`**. Carry nits for the next register touch: B-97 close_out's stale lead sentence; lens-1's O-1 (disposal lock-unlink residual — behind the acknowledge flag) + O-3 (splitlines semantics) worth one-line notes. **Next action:** (a) the ground-past-the-label sweep continues over the registered pool — **B-96** (grace bounded by sweep count not elapsed time), **B-74-residue**, **B-98/B-99** (spec-leg registrations), **B-100** (the empty-idempotency_key carve-out spec conflict), **B-101** (Pydantic base-schema round-trip), **B-103/B-104** (this chain's registrations) + the elder verified-dormant set — ground each at HEAD before building or re-registering; (b) held=1 (R-1) honored; (c) the session-issues log + accumulated micro-nits are natural riders on whichever arc lands next.

**Round 62 — the B-97(a) spec leg CLOSED 2026-07-31 at PR #1170 (Runtime v1.107 → v1.108 + plan v2.56; ALL 17 council K-conditions discharged).** Authored by a dedicated Opus spec-writer from the fork §6 skeleton under the council record's binding conditions: §14.14.8 keying AMENDED to per-tenant path segregation under a TOTAL injective CONSTANT encoding (length-prefixed canonical segments; untenanted branch a distinct VALUE; Runtime-local `normalize_tenant_scope` delegation; all THREE collision hazards closed; codex pinned the BYTE grammar — two in-house length-prefix precedents diverge char-vs-UTF-8-byte counting — and `surrogatepass` totality); the (3a)-abandon migration contract (single per-journal legacy-source flock + read-back verify + idempotent per-journal-atomic re-run + refuse-where-no-op; crash-atomic clarified ABSENT-OR-COMPLETE after codex caught the phrasing licensing deletion of a valid publication; the filename↔wrapper binding whose omission would promote dormant mis-filed state into live resumable state — with the agent's OWN round-4 fix then failing for every post-upgrade journal, forcing a three-way classification); the orphan statement (`:5834` scoped to LIVE journals); name-the-silence; the (3a)-rationale as spec text; §30 `absent` tenant-scoped + **B-102's spec half DISCHARGED** (row narrowed — the read-side attribution flip rides U-RT-149 AC #4(c)); **NEW §13.7/§13.7.1** harness-inspect enumeration surface (lock files EXCLUDED from enumeration — codex R10's genuinely-new find; K-10(c) honestly narrowed to an UPPER bound since no pause-resolved marker exists → **B-104 registered** as the capture-side discriminator gap; **B-103** also new). The SIX stale-as-described fold-ins done — five in-spec + the `shutdown_cli` strings-only code touch (whose FIRST draft swapped one false unconditional for another — the daemon exits via uvicorn's `capture_signals()`, not `drained_flag` — caught at codex R4 and recorded in-text; test assertions re-pinned to the corrected message with a guard against the class). Determinations, made not assumed: **CP UNCHANGED / OD UNCHANGED / CXA UNCHANGED-no-delta** (zero cross-package consumption — stronger ground than B-69's one-crossing case). **Codex 11 rounds → 0 findings, soundness exit** with PD-9 discriminators in the PR body (zero findings ever touched Reading A, the (3a) default, the (i)+(iii) disposition, or the closed vocabulary). **Merge-gate: proportional skip recorded** (strings-only code touch, line-by-line verified by the orchestrator; the log row carries the call). Register **105 items / 81 closed / 21 registered_finding / 2 open / 1 held** (B-97 `open` between legs per the B-92 precedent). **Next action: the B-97(a) impl leg — U-RT-149** (keying + encoding + migration machinery + §13.7 surface + adoption/disposal admin tools under ONE conjunctive closure; 12 execution ACs; the B-102 read-side flip at AC #4(c); K-17's emission determination owed here) — fresh worktree Opus agent re-grounding at its own HEAD; full 3-lens merge-gate on the code PR. After it: B-97 closes end-to-end; the registered pool (B-96/98/99/100/101/103/104 + elders) continues the ground-past-the-label sweep.

**Round 61 — the B-97(a) council re-confirm CLOSED 2026-07-31 at PR #1169 (dyadic C3 ⊥ C11 + orchestrator; CONFIRM-WITH-CONDITIONS, unanimous and reached BLIND).** The convening's three earned findings: (1) **a violated (3b) adoption is SILENT and EXECUTES** — every shipped guard passes on stale-but-self-consistent state; the v1.107 staleness precondition never fires for resume_handle callers (`api.py:840-841` early return); not one guard is a recency check; (2) **no directory-scoped liveness predicate exists** (pidfile is repository_root-keyed; the journal resolves from path_bindings) — the spec must state the negative fact, never a discharged quiescence precondition; (3) **ratified-A CREATES an unenumerable durable class** — §5.3's own "drain pauses before upgrading" recourse is NOT EXECUTABLE at HEAD (0 `pause` matches across all 8 admin modules), making the (iii) enumeration surface a PRECONDITION of (3a), not a companion. **5 seams, 0 unresolved**; cross-corrections on the record (C11 withdrew the directory-tree lock as mechanically inert — different inode; C3 withdrew its OD normalization delegate to PROBE 2, conceded its landing over-price, dropped its disposal-on-inspect requirement to the read-only invariant); the orchestrator adjudicated the one residual by REMOVING a mechanism (§14.8.11's write-once no-replace commit IS the empty-target enforcement — no second lock). **17 conditions K-1..K-17 (15 LOAD-BEARING)** for the spec leg: dual→single per-journal legacy-source flock + read-back verify (`record_count`+`latest_record_digest`, zero new persistence), refuse-where-no-op (Windows loses (3b), keeps (3a)), idempotent re-runnable per-journal-atomic adoption, the no-unverifiable-precondition rule, name-the-silence, the (3a)-default rationale as spec text, §30 `absent` tenant-scoping, the harness-inspect §13.5 enumeration extension (read-only; disposal on a separate admin action, dry-run default), the merged orphan statement (live-record scoping of `:5834`), constant total injective encoding closing ALL THREE hazards (branch/delimiter/field-boundary) delegating to Runtime-local `normalize_tenant_scope`, and the now-SIX stale-as-described fold-ins (+`shutdown_cli`'s drain-STRUCK text, the second leg of the non-actionable recourse). E2 adversarial CLEAR-WITH-FOLD (7) + E3 codex (6, incl. a real internal contradiction K-11-vs-K-5 and a factual rebuttal verified-and-accepted). **Next action:** (a) **the Gate-2 operator confirmation** — (3a) abandon-by-default + §5.3.1 (i)+(iii)-with-(iii)-precondition (the council supports confirming both; the answer stays the operator's; the spec leg MUST NOT open first); (b) post-Gate-2: the spec leg per the fork §6 + K-1..K-17 + the six fold-ins; (c) the impl leg. Register unchanged (103 items; the B-97 flips ride the spec leg).

**Round 60 — the B-97(a) Class 2 fork FILED at PR #1168 AND RATIFIED same-turn 2026-07-31: Reading A (path segregation).** The filing (doc-only, 1027 lines, `.harness/class_2_fork_b97a_pause_journal_tenant_binding.md`): four readings — **A path-segregation** (per-tenant journal dirs under a contractually injective encoding; record shape unchanged; the closed five-cause vocabulary STAYS AT FIVE — the typed-refusal question DISSOLVES since a wrong-tenant read finds no stream) / A+B (+in-record attribution, +1 cause; its marginal whole-directory-restore case collapsed at round 3) / B (KILLED at round 1 — shared file + the `:5834` append-only invariant convert silent cross-tenant execution into MUTUAL RESUME DENIAL) / C (defer + declared non-support). **ELEVEN codex rounds, 40 findings, all accepted**; a declared soundness exit was REOPENED at round 10 when a [P1] hit the recommended reading's own reopening criterion (the mixed-version rollout window — an old reader ignores unknown wrapper keys and accepts a new tenant-stamped record uncheck­ed) → absorbed as §5.3.2 mandatory quiesced cutover; **PD-9 discriminators recorded IN-BODY** (narrowing-not-invalidation; recommendation flipped twice B→A+B→A, below the register-and-hold cap); two of the author's own overturned claims kept on the record. Cite verification found a FIFTH stale spec site beyond the brief's four (`:5824` — §14.14.8 still says directory-fsync-on-NEW-FILE while #1167 made both unconditional). **Operator RATIFIED Reading A via AskUserQuestion** immediately after the merge (all blocking CI green pre-merge this time). **Next action (the fork's own two-gate sequencing):** (a) **the dyadic C3⊥C11 council convening** — narrow re-confirm of ratified-A's local-deployment/persistence-keying tension (the register row mandates convening when (a) opens; the probe split the antecedent: zero config burden on the default path BUT a real upgrade burden under A); (b) **the spec leg** after the council record: §14.14.8 keying amendment (per-tenant path + injective encoding + quiesced-cutover clause) + the accumulated §30 discharges (`:3313`/`:3319` stale-as-described, B-102's absent+corrupt-latest indeterminacy, tenant-scoping the `absent` row, the change-note `:4636/:4639` cites, the NEW `:5824` fsync-wording fix) + plan units + markers + the register flips (B-97 ratification record; B-69-family pr reconciles rode #1167); (c) the impl leg per precedent. Register 103 items / 81 closed / 21 registered_finding / 1 held.

**Round 59 — B-97 half (b) CLOSED 2026-07-31 at PR #1167 (the ground-past-the-label sweep's second build; half (a) stays open as the fork-shaped remainder).** Grounding split the row decisively: **(b)** impl-discretion NOW (spec §30 term 5 NAMES cross-process write serialization unguaranteed; B-73 precedent; house primitive with 3 runtime→IS import precedents) / **(a)** FORK-SHAPED (4 genuine decisions: in-record vs path-only key, the closed 5-cause vocabulary, migration vs the `:5834` append-only invariant, the (workflow_id,run_id) rider) / lens-1's F1 its own row. **Built:** per-workflow `fcntl.flock` on `_append` (the house directory-wide lock IMPLEMENTED FIRST then REJECTED at codex R2 — cross-workflow blocking is externally visible via unlocked reads reporting PERMANENT `absent` during the wait); reads deliberately unlocked (keeping §30 `:3319`'s indeterminacy premise true = the zero-spec-text warrant); **the register's own torn-record framing EMPIRICALLY RETRACTED** (1 write() syscall per record at 60KB/4MB; 2×12 unlocked concurrent appends → 0 malformed; the lock re-justified on exclusion-at-all + NFS/SMB unspecifiedness + self-heal soundness — recorded under a SHARPENING RECORDED HONESTLY clause, not smoothed); a REAL pre-existing crash window closed (codex R1 [P1] dir-fsync keying). **Gate:** lens 1 APPROVE (every interleaving constructed resolves sound; F1 [P2] the `is_new_file or dir_is_new` form NARROWS-not-closes + F2 [P2] blocking flock on the event loop → deferred with the `resolve_result_ref_off_loop` precedent), lens 2 APPROVE (stale-as-described §30 sentences ruled FORCED-left, fix-when-next-touched at the (a) leg; the obligation now DURABLE on B-97's close_out), **lens 3 BLOCK — a FALSE PROBE ROW** (the `dir_is_new`-after-mkdir mutation was 0 FAIL at head; stale pre-widening carry — the count-drift class inside a probe table) + O_NOFOLLOW and the Windows carve-out mutable-to-broken all-green. **Fix (`4fb2dd7b`):** UNCONDITIONAL parent-fsync-after-mkdir-BEFORE-the-lock + unconditional journal-dir fsync, BOTH gating flags deleted — sounder than lens-1's own gated suggestion (which retained a one-statement window); the probe table replaced by a **DERIVED mutation matrix** (script applies 10 mutations to real source, runs the real suite: 0 undiscriminated); 18 witnesses (+O_NOFOLLOW symlink pair, Windows round-trip, dead-writer crash-residue pair); skipif narrowed per-test; codex 6 rounds. **Scoped re-gate APPROVE** — invariant walked exception-free at every statement boundary; the matrix INDEPENDENTLY re-derived (M5 empirically confirming the BLOCK's premise against the superseded form); lens-1 blast radius covered (pre-lock parent fsync benign; LEAF preserved). Tests 3435→3444; register **103 items / 81 closed / 21 registered_finding / 1 held** (B-102 filed + WIDENED at codex R2: `absent` AND `corrupt-latest` both lack §30's indeterminacy qualification). CI 16/16. **Next action:** (a) **the B-97(a) Class 2 fork doc** — the tenant-binding design fork (4 decisions enumerated on the live row; mirrors B-65's fork→ratify→spec→impl chain; the fork FILING is doc-only and Claude-executable now; ratification is the operator gate); it also owes the §30 `:3313`/`:3319` refresh + the B-102 cause-table touch + the spec change-note stale cite at the same leg. (b) The remaining registered pool (B-96/74-residue/98/99/100/101/102 + the older verified-dormant set). (c) held=1 honored.

**Round 58 — the B-69 impl leg CLOSED 2026-07-31 at PR #1166; B-69 CLOSED END-TO-END — the full chain (grounding → council #1164 → A′ ratification → spec leg #1165 → impl #1166) completed inside one loop day.** Built by a worktree Opus agent: `read_paused_workflow_state` at the Runtime package root (workflow_id-only keying, fail-closed-on-inability-to-mint, CP-homed `PausedWorkflowState` 4-variant closed union — module residence within §14.14.9's own deferral) + the §30 refusal-only staleness precondition RELOCATED INSIDE `_run_lock` (a deliberate divergence from a codex suggestion — preserves the await-free window making the `locked()` guard sound; lens 1 verified the relocation REQUIRED, not stylistic) + the CP carrier with EXACT BY-VALUE snapshot binding (codex R2 caught R1's own `created_at` fix colliding within a millisecond) + OD plan v2.31 NEW U-OD-57 as documented bundled absorption (the sibling-payload option (b), discharging Runtime plan v2.55's own PENDING-not-yet-numbered dependency). **26/26 ACs; 8 mutation probes all load-bearing; A′ honored at COMMIT granularity** (fence commit precedes accessor exposure; no commit carries an unfenced accessor). **Review stack:** codex R1-R2 (10 findings applied; R3 returned no verdict across two attempts — the known streaming-capture limitation — treated as converged-through-R2), **merge-gate 3-lens ALL-APPROVE** (lens 1: TOCTOU closed by the monotone-count token, count-catches-what-digest-cannot; lens 3: the ms-collision by-value witness judged strongest in the PR, W2's misattribution half = C9's real falsifier against the shipped resolver), then — the agent itself flagging that its final commit bundled two codex-R3 LOGIC changes the gate never saw — a **SCOPED RE-GATE on exactly that delta: APPROVE with empirical discrimination** (change 1 authorized verbatim at Runtime `:3319`'s indeterminate-across-processes clause; change 2 the only conformant landing for version-skew; the B-101 pin a genuine boundary marker). Tests +55 across three packages (CP 1859 / OD 1121 / RT 3423→3435 at re-gate); pyright 0/0/0. Register **102 items / 81 closed / 20 registered_finding / 1 held**; **B-69 `closed`** (both conjuncts verified by the closing reviewer); NEW rows this arc: **B-100** (empty-idempotency_key carve-out scoping — a REPORTED-not-absorbed spec conflict) + **B-101** (Pydantic base-schema round-trip drops pause_state — unreachable today, pinned). **Carry items for the next register touch:** B-69/B-100/B-101 `pr:` reconciles to `#1166`; lens-1 F1 (the accessor as first off-loop journal reader — B-97's in-process sibling) + F2 hardening; lens-3 G1 (fence-placement AST witness) + G2 (count-mapping witness); re-gate's three P3s incl. B-101's unnamed StepExecutionContext vector. **Next action:** (a) the registered pool now holds 20 rows incl. 5 fresh from this chain (B-97/98/99/100/101) — the ground-past-the-label sweep continues, with B-97 (pause-journal tenant binding + unserialized `_append`, urgency raised by lens-1's F1 sibling finding) the natural head; (b) the carry-item register touch is a bounded rider on whichever arc lands next; (c) held=1 (R-1) honored.

**Round 57 — the B-69 spec leg CLOSED 2026-07-31 at PR #1165 (doc-only; the ratified A′ apply arc; all nine §9 rows landed).** Authored by a dedicated Opus spec-writer agent from the council record's §9 table: **Runtime v1.107** (in-place per the file's own house style — NEW §14.14.9 accessor under C-RT-24 with the closed 4-variant projection DTO; `:3274`'s "registered, NOT designed or scoped" sentence DISCHARGED to a cross-reference; NEW §30 refusal-only staleness precondition with 5-member cause attribution on the EXISTING RT-FAIL-RESUME-HANDLE-UNKNOWN + the §14.14.8 append-only/never-truncated substrate invariant STATED); **CP v1.112** (CP-homed typed response carrier on ResumeContext — the council's 3-surface sizing correction landed as recommended; the public projection surface; both scope-limit lifts with their v1.106/v1.107 delta-chain homes named); **CXA v2.23** (NO new row — payload widening on the existing crossing, aggregate FROZEN at 111); **OD v1.36** (real bump, not a rides-existing statement — the refusal is pre-bootstrap, composing through NEITHER existing helper; an OD plan delta owed at the impl leg either way); plans **Runtime v2.55 (NEW U-RT-148, 16 ACs carrying §11's W1-W4)** + **CP v2.47 (U-CP-64 amended, 10 ACs, C10's one-authority criterion verbatim)**; 6 clearance markers; register **B-69 → `open`** with the X-AL-2 conjunctive closure criterion + **B-97/B-98/B-99 filed** (the §10 routed-out rows incl. R-3's pause-journal tenant binding + unserialized `_append`); council record §14 ratification addendum. **Codex: SIXTEEN rounds** (R1 2P1+3P2 → R16 2P1), every finding re-grounded against shipped text; **two reversals on the record**: (1) a drafted FIFTH projection variant broke the one-authority invariant → became a source shape with the key field absent; (2) **the contract-number reversal, which generalizes**: a first draft minted C-RT-36 for the accessor — the Q3-evidence and closure-G1.3 blocking gates independently encode *a declared `C-*` contract must have implementing code* (exclusion set = confirmed-phantom only), so **a doc-only spec leg cannot mint a contract number** — re-homed to §14.14.9 under C-RT-24; this constraint is now operative for every future spec leg (it explains why every prior leg extended an existing contract; never before written down). §14.4 records three places the council record was self-inconsistent and how each was resolved WITHIN the record's own statements — the leg exceeds the record in zero places. CI 17/17. Register 100 items / 1 open (B-69) / 18 registered_finding / 1 held / 80 closed. **Next action: the B-69 impl leg** — U-RT-148 (accessor + DTO + fail attribution + the precondition, A′ ordering: precondition lands FIRST OR SIMULTANEOUSLY) + U-CP-64's amended surface + the owed OD plan delta; per the B-92 precedent run as a FRESH agent re-grounding every touchpoint at its own HEAD; the W1-W4 witnesses BY EXECUTION are the acceptance bar; full merge-gate 3-lens on the code PR. After it: B-97/98/99 join the registered pool; the loop's ground-past-the-label sweep continues.

**Round 56 — the B-69 council leg CLOSED 2026-07-30 at PR #1164 (doc-only; the genuine multi-voice workflow, run as a dedicated adopting agent).** Grounding (same day, separate read-only agent) had ruled SPEC-LEG-FIRST — cleared text affirmatively disclaims the accessor (Runtime `:3274` "registered, NOT designed or scoped"; CP v1.106 `:250` "NEITHER resolved") — and the row's `council: TBD` resolved YES on its own trigger. The convening: C10 ⊥ C11 primaries + C9 consultant (a load-bearing expansion — C9 produced the decisive finding), **4 tension seams, 4 resolved, 0 surfaced-unresolved** (3 probe-resolved; probe P4 found the nominated fence carrier FAIL-OPEN for the linear case and reversed C9's own nomination). **The exposure tradeoff — the register's anticipated operator question — is NOT one**: the accessor grants a strict subset of resume()'s existing authority (P0 authority-equivalence), and C10 declined to manufacture a gate, naming over-gating as its own failure mode. **Recommended reading:** NEW standalone module-level read function on resume()'s identical input triple → closed discriminated-union DTO (4 location variants, never bare identifier sets) → co-requisite REFUSAL-ONLY staleness precondition on resume() (token-bound, required typed carrier — possibly CP-homed, the 3-surface sizing correction); runner-up (resume() pre-flight mode) rejected unanimously as fail-open mode-confusion. **ONE operator-tier fork survives, and it is SCOPE not exposure** (§8.2): the precondition amends the already-cleared C-RT-35 resume() contract that B-69's row does not authorize — (A′) co-requisite-sequenced [recommended] / (A) co-merged / (B) accessor-only [rejected as land-the-regression-and-register-the-fix]. Review: E2 in-family adversarial (0 severe / 7 moderate) + E3 codex (4 [P1] + 1 [P2], near-zero overlap with E2) + consolidated reconcile-to-zero — 20 positions withdrawn/corrected, incl. a booked staleness residual WITHDRAWN as self-contradictory. §10 routes out: R-3 (pause journal tenant binding + unserialized `_append` — NEW B-* row owed, urgency raised), the gate-description durable-state absence (NEW B-* candidate), 3 stale docstrings in `pause_resume_protocol_types.py` (fold into the impl arc). **Process slip on the record:** the orchestrator merged #1164 while the blocking pytest check was still pending (doc-only, materially harmless, main push CI confirms — but against the CI-green precondition; noted). **Next action:** (a) **the §8.2 operator ratification** — the A′/A/B scope question, surfaced via AskUserQuestion (the B-70 precedent); (b) post-ratification: the spec leg per the record's §9 nine-row delta set (Runtime v1.107 + CP v1.112 + CXA v2.23 + OD v1.36-or-statement + 2 plan deltas + clearance markers + the register flips incl. the §10 new rows); (c) the impl leg with §11's witnesses (W2 1→1 primary). The B-33/B-59/B-92 sequencing precedent governs.

**Round 55 — B-77 CLOSED 2026-07-30 at PR #1163 (the first registered_finding row re-opened and BUILT under the ground-every-arc directive; B-74 narrowed; B-96 registered).** Grounding past the dormancy labels found B-77's close_out was a fully-ratified advisor-directed design, not a wait-state: `gc_sweep` first-observation grace gating (module-wide registry keyed by root `st_dev:st_ino`, recorded inside the locked re-verify; reclaim only if observed expired in a PRIOR sweep) closes the crash-between-`os.link`-and-post-commit-refresh reclaim window — implementable entirely inside `gc_sweep`/`_publish_atomic`, no config-contract change, TTL floor correctly NOT added (B-74's declined option). Built by a worktree Opus agent (Fable-5-orchestrator discipline; survived TWO transient API-stream kills, resumed clean each time). The central design judgment — module-wide-per-root NOT per-instance — grounded against the real call graph (fresh store per bootstrap at `protected_result_store_factory.py:78`; sweeps at `stage_4_od.py:123` + `shutdown.py:800`). **Codex 5 rounds** (R1 [P1] wall-clock-phase flake pinned; R2 [P2] sweep-count-vs-elapsed-time bound → **B-96 registered** rather than a 4th unratified mechanism attempt — the arms-race stop rule; R3 [P2] a grace-masked pre-existing witness re-armed; R4/R5 clean). **Merge-gate 3-lens: one REAL lens-3 BLOCK** — the pass-on-absence class AGAIN: the per-root-registry design judgment had ZERO discriminating witness (per-instance mutation passed the ENTIRE suite; the fresh-bootstrap witness's freshness was not load-bearing — `write_once`'s pre-publish opportunistic sweep primes the registry at `_last_gc_at=0.0`) — fixed at `227541e4` (test-only: the two-instance-one-root witness + per-root-vs-global + the `.tmp-*` half + the lock-held-at-call witness, each probed BOTH directions), re-gate APPROVE verified BY EXECUTION (each mutation killed by exactly one test, 61 green); lens 1 APPROVE (every race resolves retain-direction; in-flight-tmp observation structurally unreachable); lens 2 APPROVE (§14.8.11 mandates which sweeps exist, not first-observation timing; X-AL-3 clean; 4-row register diff digest-verified). **B-74** stays `registered_finding`, honestly narrowed to the granularity-exceeds-the-inter-sweep-gap residue, pinned as an executable `REGISTERED RESIDUAL` assertion. Tests 3388→3392 (-m "not e2e"); store file 54→62. Register **97 items / 80 closed / 16 registered_finding / 1 held**. **Next action:** (a) apply the same ground-past-the-label pass to the remaining actionable-looking registered_finding rows — B-69 (durable-pause-state read accessor design) is the next close_out that reads as a design rather than a wait-state; ground it at its own HEAD before building; (b) B-96 + B-74's narrowed residue stay registered (fresh, premises live); (c) held=1 (R-1) honored.

**Round 54 — the OTLP live-e2e standing gate RESOLVED 2026-07-30 (no PR; evidence commit `58e7fdd2`).** The "IAM grant → 3 OTLP e2es" operator unlock carried since the 2026-06-10 RC close is CLOSED end-to-end: operator standing authorization → grant applied by the agent and verified → first attempt failed FAST AND FREE at secret resolution exposing the TRUE blocker (project billing disabled — never IAM) → operator re-enabled billing → **r421 / r810 / r820 all PASS on first post-billing attempt** (trace IDs + full evidence at `.harness/otlp-e2e-revalidation-2026-07-30.md`) → grant REVOKED at close, TOML copy deleted, zero resources left behind. Readiness 7/7 pre-flight; cost: 3 usage-billed hosted-provider calls. **Next action:** the forward queue is genuinely slice-exhausted again — (a) the 16 `registered_finding` rows (verified-dormant; re-ground only on surface overlap), (b) `held`=1 (R-1, honored), (c) residual micro-cleanups recorded at PR #1162's body (B-17 row self-referential prose; `post-phase-8-forward-register.md:211` stale in-flight text) — candidates for the next natural register touch, not an arc of their own. `operator_gated`=0; the two long-standing operator unlocks (branch hygiene; IAM/OTLP) are BOTH closed as of this date.

**Round 53 — the §2.2 follow-on CLOSED 2026-07-30 at PR #1162 (the B-17 arc's recorded remainder; R-ICM-1 complete).** Per Round 52 item (b): CLAUDE.md §2.2's dated narrative relocated by PURE DELETION (74,929 → 74,715 B; zero inserted characters, so byte-preservation is provable trivially — both removed segments verified verbatim in `.harness/claude-artifact-pointers.md` under the C6/C7 continuation of #1161's chunk numbering; current ADR-D7/PRD-v1.2 authority statements kept inline) + the B-17 register row reconciled `pr: '#pending'` → `'#1161'`. Codex ×2 zero findings; CI 15 blocking green; orchestrator re-verified the diff directly. **Process note on the record:** the classifier denied the agent-initiated `gh pr merge` (self-merge without fresh authorization — correct behavior); the operator then issued a STANDING approval: *"Merge 1162 approved as well as all forward merges as long as full review evidences merge is safe and non-destructive"*, plus merge+IAM-grant permissions going forward. Residual candidates recorded in the PR body: the B-17 row's self-referential summary prose; `post-phase-8-forward-register.md:211` stale in-flight text. **Next action:** (a) the IAM grant (now Claude-executable per the standing approval) → the 3 staged OTLP e2es (readiness all-PASS, TOML in place) → revoke at close per the runbook; (b) queue otherwise unchanged from Round 52 (16 registered_finding dormant; held=1).

**Round 52 — B-17 CLOSED 2026-07-30 at PR #1161 (governance-only ICM adoption; the operator-gate register empties to 0).** The operator answered all three ICM-audit §8 gates via one batched AskUserQuestion (scope governance-layer-only — with the operator's forward note preserved on the register row that ICM integration into the FINAL DEPLOYED harness package/repo is the better long-term question, routed to the deployment-surface arc; caps adopted for CONTEXT.md/router files ONLY, design-substrate exempt as documented divergence; L0 refactor CONSERVATIVE) plus the IAM re-grant (approved; grant not yet applied at close — the 3 OTLP e2es stay parked on it). Delivered by a worktree-isolated Opus agent under the Fable-5-orchestrator discipline: **R-ICM-1** — root CLAUDE.md 88,325 → 74,929 B, the §2.3/§2.4 inline change-note lineage relocated to `.harness/claude-artifact-pointers.md`; byte-preservation verified TWICE (the agent's 5-chunk check script + the orchestrator's independent difflib segment re-verification: 13,487 removed bytes, 0 segments missing, zero other-line drift beyond the 2-line Start-here router pointer); **R-ICM-2** — new 30-line workspace `CONTEXT.md` posture router, every statement §-cited (router only, no new rules); **register flip** — B-17 `closed` with ratification + forward question (deviation on the record: the enum has no in-flight state, so closed-at-open with `pr: '#pending'` — reconcile to `#1161` at the next register touch). Scope grew past the 3 named tasks, each consequence-of-CONTEXT.md and PR-bodied: the `optimize-claude-md` skill bundle (7 files — it declared "no L1 router exists", mis-classified relocation as deletion, stale eval baseline), `AGENTS.md` pointer, `post-phase-8-forward-register.md` stale-text supersession, and `tools/test_forward_register.py` un-hard-coded (closing B-17 emptied `operator_gated` 1→0 and the old test raised StopIteration on a legitimate transit). 6 codex rounds, 11 findings (1 P1 with mutation-probe witness), all addressed; CI 16/16; merge operator-authorized via AskUserQuestion (the agent had been user-stopped mid-run once and resumed — the explicit merge ask was the disposition). Also this day, pre-arc: the branch-hygiene operator gate closed END-TO-END (operator ran the generated prune script — 264 verified-merged local branches deleted, 307→46; + 2 stale remote refs; all 9 loop_status DEFERRED-HIL close-outs marked RESOLVED-HIL). **Next action:** (a) **the IAM grant → 3 OTLP e2es** (r421-managed-cloud / r810-files / r820-managed-agents) — operator approved, grant command handed over, run them the moment the SA policy shows the token-creator binding, then hand the revoke; (b) the **§2.2 inline-history follow-on** (recorded in PR #1161's body) — a bounded optimize-claude-md pass relocating §2.2's smaller dated block, plus the `pr: '#pending'` → `#1161` register reconcile riding it; (c) the 16 `registered_finding` rows stay verified-dormant (PR #1161 touched no code surface — no overlap, no re-check owed); B-17's close leaves `operator_gated` at 0 and `held` at 1 (R-1, honored).

**Round 51 — the gate-log LOW batch CLOSED 2026-07-30 at PR #1160 (the #1150-precedent ground-first classify + bounded arc over PR #1159's seven carry items).** Grounding verdicts: BUILD-NOW ×3, REGISTER ×1, ACCEPT ×3, all with file:line evidence. **Landed** (`3b6a9a84`): the derived-index hook notification hoisted out of BOTH write holds (`_write_record_locked` extraction; the guarded arm is the one a partial fix silently misses — per-arm deadlock witnesses with bounded joins); `_DirLock.release()` exception-safe at depth 0 (try/finally symmetry with the already-hardened `acquire()`); the episodic no-run_id pre-check pinned at the Protocol seam (`_RunIdBlindSourceStore` — deleting the pre-check now flips exactly one test to silent auto-promotion; the concrete store provably cannot distinguish). **Registered**: B-93 (whole-root write serialization has no deadline — recounted to 8 blocking-flock sites / 6 entry surfaces / 3 modules with the two coverage traps: multi-site sequential blocking per surface, and the LOCK_NB probes that are contention probes not deadlines; co-rider on B-45) + dated re-check notes on B-45 (a FOURTH Windows no-op surface; on Windows the U-MEM-27 cross-process commit binding silently degrades to in-process-only — a correctness consequence, not parity) and B-84 (the guarded-write primitives shift the transactional-batch cost tradeoff; re-price at build time). **Codex R1-R6, every round real**: R1 two register-prose consistency fixes; R2 the hoist's commit-ordering regression → `commit_seq` stamped inside the hold from the index ledger's byte length (cross-process monotonic) + the pre-lock serialization regression reverted (git-show verified); R3 mixed-version reader break → coverage moved to a NEW `rebuilt_coverage` event kind old readers verifiably skip, `rebuilt` line byte-identical to pre-patch, verbatim-old-parser witness; R4 hash-keyed coverage bleed → claim-walk pairing to a specific rebuild instance, emit-order AFTER proven load-bearing; R5 split disposition — the two-pass retention regression FIXED (exactly-one-decode per read_current, probed) and the auto-refresh overlap race verified PRE-EXISTING with four pre-branch cites → **B-94 registered**; **R6 HELD per the arms-race stop rule** — the mixed-version legacy-WRITER stale-marker gap is information-theoretically unfixable read-side (legacy markers carry no token; the data was never written) → **B-95 registered**, SCOPE bounds documented at both caller-facing surfaces, one witness overclaim re-scoped to reader-compat-only; all three preconditions counterfactual at HEAD (zero production hook consumers). **Merge-gate 3-lens all-approve R1** — lens 1 walked the claim-walk mis-claim and proved it benign (content-keyed pairing; winner rule takes max seq); lens 2 verified X-AL-3 clean (the new event kind appears nowhere under design-substrate; `semantic/index.jsonl` is spec-unconstrained "Rebuildable" per Spec_Memory_Substrate `:156`), the full B-93 8-site enumeration, and the commit-binding hold intact post-refactor; lens 3 verified the old-parser replay field-for-field literal vs `fc40fb2b` and gated all arc mutation-probe claims against the code; four LOW items closed inline pre-merge (`334a3e5f`: claim-walk comment truth ×2, the Windows commit_seq bound added to the SCOPE paragraph, the B-84 prose mirror); one LOW carried on the B-94/B-95 re-check trigger (a two-pair claim-walk witness). `just check` **7134** (+6). Register **96 items / 78 closed / 16 registered_finding / 1 operator_gated / 1 held**. **Next action:** (a) **the 16 `registered_finding` rows** — dormancy re-check only where #1159/#1160 touched surfaces (B-45/B-84/B-88 already re-checked this round; B-93/B-94/B-95 are fresh); the rest verified-dormant per round 47 — re-ground only on surface overlap per the standing discipline. (b) The two standing operator unlocks (IAM grant → 3 OTLP e2es; guard-blocked branch deletes — the loop_status.md close-out backlog). (c) If (a) yields nothing buildable, the queue is honestly slice-exhausted to gates — report the gates, don't invent busywork.

**Round 50 — the U-MEM-27 impl leg CLOSED 2026-07-30 at PR #1159; B-92 CLOSED end-to-end — the B-92 family (fork #1156 → ratification #1157 → spec leg #1158 → impl #1159) is complete.** Built as 4 sequential slices + 3 codex fix rounds + 1 merge-gate fix round on one branch under the Fable-5-orchestrator discipline (all impl + review legs on Opus agents). **Slice 1+2** (`ef201f36`): `CapturedCrossFamily` tri-state on `MemoryRecordEnvelope` (absent-reads-`unknown`, hash-inert, default-`unknown`-never-`false`) + the origin-aware write-side derivation split exactly per the unit — `_capture` determines the origin disposition from `event_kind`, `_record` computes the final value gating the family comparison over the B-86/B-89 authorities; all four undetermined cases land `unknown`; run-close verified producer-less at HEAD; `compaction` assigned UNDETERMINED by the same per-invocation rule as `failure_observation`. **Slice 3a** (`5447b653`): `PromotionRiskFlag.CROSS_FAMILY_CAPTURE`; single-authority flag+gate at BOTH entry points incl. the `PROMOTE_PROCEDURAL` kind branch; validator refusing the illegal pair (structurally enforcing the biconditional — the gate-revert probe surfaces as `ValidationError`); reserved-flag re-derivation both directions; durable-carrier content key (C-MEM-08 untouched); the redaction/tombstone RESET-to-`unknown` (spec transition-reset rule; routed here from a slice-1 handoff note). **Slice 3b** (`f658668f`): `PromotionDecisionStore.read_record` widening; ONE frozen `_EffectiveProvenance` snapshot per service call (gate = pure projection); worst-value aggregation; per-branch fail-closed; the activation boundary OVERRIDES `auto_promote_allowed` and lets the pre-existing disjunction decide (reading B preserved — operator-approved activation reaches ACTIVE); choke-point normalization at `_persist_decision` over all FOUR callers incl. `deny`; **commit binding STATED as required: a generic guarded conditional write (`write_record_guarded`) evaluating the precondition inside the store's own write locks**. **Slice 4** (`d72c7966`): six `PENDING — U-MEM-27` markers lifted, window 2 closed to v1.2, B-92 register flip, arc note `.harness/u-mem-27-impl-leg-close-2026-07-29.md`. **Codex R1-R4** (every round real): R1 [P1] the in-process-only guard → the cross-process half BUILT (house flock primitive generalized to `cross_process_scope_lock(root)`, taken by `write_record` AND `write_record_guarded`, genuine two-OS-process interleaving witness; topology grounded LIVE — daemon + one-shot share the repo-derived root; the sibling ops ledger already flock'd) + [P2] `OSError`→fail-closed (`5f60bfda`); R2 [P2] the `.gated` projection compare narrowed `:1022`'s literal obligation → EXACT per-source vector compared at commit, unresolvable-`None` kept distinct from stored `unknown`, the slice-3b deviation retracted on the record + [P2] `TypeError`→fail-closed (`7aa2c222`); R3 [P1] REDACTED captures were classified dispatch-derived → origin decision consults capture mode, wholesale-replacement grounding with three spec quotes (`4fc7c7a2`); R4 clean. **Merge-gate 3-lens: one REAL lens-2 BLOCK** — the evidence packet certified a witness renamed at codex R2 (cited the nonexistent `..._still_commits` test asserting the INVERSE of shipped commit-binding semantics) + two count drifts (22→26, eleven→15; a third found during the fix: R-MEM-01's 58→66 — the `[[count-claims-drift-every-review-round]]` class, again caught only by programmatic recount) + the two plan-mandated slice-3a mutation probes unrecorded — all fixed (`a2768946`), re-gated APPROVE R2; lens 1 APPROVE R1 (6 NOTE-level carry items: `derived_index_hook` now runs under the scope lock — latent, no production wiring; `_DirLock.release()` not exception-safe at depth 0 — pre-existing, blast radius grown; whole-root write serialization with no timeout; the `_commit_record`→`append_memory_operation` split leaves an ACTIVE record if the ledger append fails — pre-existing ordering; `_DIR_LOCKS` unbounded; one safe-direction timing assert); lens 3 APPROVE R1 (the LOW-confidence witness pin landed in-arc; residuals: the episodic no-`run_id` pre-check not independently pinned — behavior-preserving; e2e LOW-shadow pre-dates the arc, recorded in-comment). ~10 mutation probes across the arc, each with named failing tests. `just check` **7124** (+98); the 4 live-Ollama integration failures baselined pre-existing on the clean tree. Register **93 items / 78 closed / 0 open / 13 registered_finding**. **Next action:** (a) **the lens-1 gate-log LOW batch** — ground-first classify (BUILD-NOW / REGISTER / FALSE-PREMISE per item, the #1150 precedent) over the six lens-1 NOTE items + lens-3's episodic pre-check pin; bounded arc. (b) The 13 verified-dormant `registered_finding` rows (dormancy re-check only where this arc's surfaces overlap — memory_store/memory_promotion/cross_process_ledger_lock were touched). (c) The two standing operator unlocks (IAM grant → 3 OTLP e2es; guard-blocked branch deletes). A future iteration re-grounds before building, per the standing discipline.

**Round 49 — the B-92 spec leg CLOSED 2026-07-29 at PR #1158 (doc-only Class 1 apply leg).** Per round 48's queue head, a dedicated Opus spec-writer agent (Fable-5-orchestrated, the B-86 → #1147 precedent) applied the ratified Reading B: **Spec_Memory_Substrate v1.1 → v1.2** — C-MEM-10 gains the cross-family-captured condition + the `cross_family_capture` risk-flag vocabulary value + the GATE (review-required, never auto-promotable; eligibility PRESERVED — reading C foreclosed), the single-gate-authority biconditional, the two-surface authority model (activation re-derives from stored source records, withholding the AUTOMATIC path only — operator-approved activation preserved, the R4 catch that stopped a silent drift into foreclosed Reading C), worst-value multi-source aggregation, the uniform re-derived-mark rule over every durable write (the `deny` path included — grounding corrected codex's own three-path enumeration to four), the frozen-snapshot single-read rule + its commit-binding property (the R9 grounding proved the race LIVE: `_JSONL_BY_KIND` kinds append with last-line-wins under a hash-inert same-`memory_id` collision), and the durable carrier in promotion-written record content; C-MEM-03 gains the tri-state hash-inert forward-only `MemoryRecordEnvelope.captured_cross_family` field — **Q2 decided at this leg by stating the requirement first** (stored-version provenance present/absent → the fork's `(i-envelope-bool)`), with content-ORIGIN keying (a `harness_rule` summary of a real provider response IS dispatch-derived; `summary_source` disqualified as discriminator), per-invocation origin semantics, the transition-reset rule (redaction/tombstone/expiry REPLACE content → the new version writes `unknown`), and stated bounds (present/absent only; no aggregate-run). Threat model byte-identical, verified programmatically. **Plan v1.1 → v1.2: NEW U-MEM-27** (~30 named witnesses incl. mutation probes and the between-recheck-and-write interleaving; the completed capture call-site sweep — `capture_tool_event`'s sole production caller `_write_note` dispatch-derived; run_close/provider_route/failure_observation producer-less, re-verified). Clearance markers ×2 in-PR; register B-92 → `open` with a five-part LIVE NEXT STEP head; U-MEM-25 packet: SIX rows `PENDING — U-MEM-27` (R-MEM-14 added at R10); root CLAUDE.md §2.3/§2.4 → v1.2. **Review: TEN out-of-family codex rounds, TWENTY-SEVEN findings — 25 applied (every one re-grounded by direct code read before the fix), 1 answered at property altitude (the R10 atomicity demand — the machinery choice routed to the impl leg), 1 DECLINED with grounded threat-model rationale (operator-approval identity-binding = review-surface work, no C-MEM contract claims actor-identity verification, verified); PD-9 SOUNDNESS EXIT declared in both change-notes after 4 rounds of monotone narrowing on one activation-race surface (R3 carrier → R7 snapshot → R9 property → R10 atomicity force), premise never invalidated.** Process notes: the arc rode out a major Anthropic-side incident (the sonnet-5 safety classifier + 529s killed the agent once mid-pass and blocked Bash/ScheduleWakeup in both sessions; work resumed clean from the committed branch each time); the invariant-count claim drifted in SIX consecutive review rounds and was caught each time only by the programmatic recount — the recount is load-bearing for any multi-round doc arc. Merge-gate correctly SKIPPED (doc-only); CI 17/17 green pre-merge + main CI green post-merge at `1eba1ae0`. **Next action: the U-MEM-27 impl leg** — a substantial multi-axis code arc (harness-is envelope field + harness-runtime derivation/gate/store-widening surfaces; closes B-92, lifts the six closeout pendings, closes window 2); per the U-MEM-26 precedent, run it as a FRESH session/iteration, re-grounding every capture call site and the four `_persist_decision` callers at its own HEAD per the unit's own cap note, and it must STATE the chosen commit-binding mechanism and why. After that: the 13 verified-dormant `registered_finding` rows and the two standing operator unlocks (IAM grant → 3 OTLP e2es; guard-blocked branch deletes) remain the only other frontier.

**Round 48 — B-92 RATIFIED 2026-07-29 at PR #1157: Reading B (flag + gate).** The operator answered the fork's A/B/C ask via AskUserQuestion at the loop session's close: cross-family-produced promotion candidates carry a risk flag AND a review gate — eligibility preserved (C foreclosed per the B-86 council's continuity record), silent auto-promotion removed (A's permanent posture denied). Q2 — the discriminator mechanism — deliberately NOT decided at ratification: routed to the spec leg per the filing's leveled analysis and the ratified option's own text. The ratification is recorded at fork doc §11 + the B-92 register close_out (status correctly HELD at `design_substrate_gated` — only applied spec deltas flip it, per the register's own enum rule; the B-39/B-72 record shape). **Next action — the C-MEM-10 spec-writer apply leg**, recommended as a FRESH focused session per the filing's §10 cap (the spec leg re-grounds all mechanics at its own HEAD): C-MEM-10 amendment (Reading B's flag+gate contract text), the discriminator's owed contract text (mechanism chosen from the leveled Q2 analysis — C-MEM-03 envelope tri-state vs C-MEM-08 join, with the six UNVERIFIABLE cases / coherence bind / aggregate-run-provenance requirement question as decision inputs), a NEW U-MEM-27 plan unit, clearance markers in-PR, out-of-family codex decorator on the C-MEM diff (the B-86 → #1147 → #1148 precedent chain); the impl leg follows. After that leg: the 13 verified-dormant `registered_finding` rows and the two standing operator unlocks (IAM grant → 3 OTLP e2es; guard-blocked branch deletes) remain the only other frontier.

**Round 47 — the C-MEM-10 Class 1 fork FILED 2026-07-29 at PR #1156 (doc-only); B-92 registered `design_substrate_gated`; the loop reaches its genuine operator gate.** Per round 46's queue head, the U-MEM-26 C-MEM-10 residual was grounded FORK-SHAPED (the spec v1.1 change-note explicitly carries the question forward unresolved; current behavior is permissive BY BLINDNESS — the promotion layer has no discriminator for the composed-scope production path; live auto-promotion exposure at HEAD verified NIL through both entry points) and the Class 1 filing authored at `.harness/class_1_fork_c_mem_10_cross_family_promotion_eligibility.md`: three readings (A explicit-eligible / B flag+gate / C refuse), **Reading B recommended** on the B-86 council record (C3/C6 continuity forecloses C; C10's write-bounded-irreversible classification denies A's silence; A-as-recorded-interim a close second with named ending triggers), Q2 (discriminator mechanics) presented honestly LEVELED after the review corrected it three times — the deciding factor is an unstated requirement (stored-version vs aggregate-run provenance) named for the spec leg. **Nine out-of-family codex rounds, every round real, none rubber-stamps** (exposure corrections R1-R2; join mechanics through six UNVERIFIABLE cases + a record/ledger coherence bind R3-R7 incl. the reconstructible idempotency-key find and the repair-rows-attest-durability-not-provenance case; Q2 leveled R7-R9; the ratification ask precision-qualified R9 — durable contract commitment with costly-partially-complete unwind, NOT "irreversible"); the A/B/C readings + Q1 recommendation stable across all nine; SOUNDNESS EXIT recorded in the filing's §10 with further sub-altitude mechanics routed to the spec leg by rule. Also in-PR: B-84/B-88 dormancy re-checks dated (premises intact). Register **93 items / 77 closed / 1 design_substrate_gated / 13 registered_finding**. **The forward queue is now genuinely slice-exhausted to gates:** every Claude-executable item from rounds 39-46 is closed; the remaining frontier is (a) **the B-92 ratification** — the A/B/C reading choice, a genuine operator fork (surfaced at loop close via AskUserQuestion; its spec leg + impl leg follow ratification per the B-86→U-MEM-26 precedent); (b) the 13 verified-dormant `registered_finding` rows; (c) the two standing operator unlocks (IAM grant → 3 OTLP e2es; the older guard-blocked branch deletes). A future iteration re-grounds only if intervening merges touch the relevant surfaces.

**Round 46 — the cite-hygiene leg CLOSED 2026-07-29 at PR #1155 (comment-only; the design-phase half DISSOLVED on grounding).** Per round 45's queue head, a grounding pass resolved the three recorded stale-cite findings: **(1)** `Spec_Harness_Runtime_v1.md:518`'s stale driver cite — LEAVE: it sits inside the v1.55→v1.56 change-note block whose own text mandates prior blocks PRESERVED VERBATIM; **(2)** the Runtime plan v2_47/v2_48 cites — LEAVE: superseded delta files, with the v1.93 clearance marker stating the corpus rule verbatim ("superseded delta-chain versions (preserved verbatim)") + the fix-when-next-touched convention; the head plan doesn't carry the cite; **(3)** the REAL and broader-than-recorded half: PR #1147 (spec v1→v1.1, +62 lines) silently invalidated every pre-v1.1 code cite below the C-MEM-13 insertion — 12 stale tokens across 6 files (`:481` resolved to a BLANK LINE, `:641` to the wrong contract's header; the `§641` shape conflated line with section — the spec has no §-numbered sections). All 12 replaced with durable contract-ID anchors (contract + subsection + byte-verified quote — the form already in-file at `memory_scope_family.py:3`), immune to future spec growth; the v1.1-era correct cites deliberately untouched; ONE semantic fix beyond anchor-swaps — the B-83 repair docstring's "present-tense obligation" reading of C-MEM-14 gained the v1.1 qualification clause (the obligation is now conditioned; the `arm_unservable` branch satisfies the condition same-family-by-construction). Codex R1 clean; merge-gate SKIPPED per its own doc-only scope rule (zero executable-line changes, independently verified); `just check` **7026**. Root-cause note recorded: spec-growth PRs have no obligation to sweep dependent code line-cites and no gate catches the class (the overlay keys on contract/unit IDs, not prose line-cites) — the durable-anchor convention is the standing mitigation; register-don't-build was considered and the row deliberately NOT opened (~40 such tokens repo-wide, the convention now precedented at 12 sites). **Next action:** (a) **the U-MEM-26 forward residuals** — the C-MEM-10 promotion-eligibility question (now materially reachable: a cross-family leg captures under the primary scope, witnessed — ground what the actual open question is from the leg-close doc `.harness/u-mem-26-impl-leg-close-2026-07-28.md`) and the `_request_hash` raw-vs-canonical observation; ground both before building. (b) 13 `registered_finding` rows (dormancy re-check only if intervening merges touched their surfaces — the reap/telemetry arcs touched llm_dispatch + external_cli_provider + docker driver; check whether any row's surface overlaps). (c) The two operator unlocks stand (IAM grant → 3 OTLP e2es; older guard-blocked branch deletes — #1155's branch auto-deleted at merge).

**Round 45 — B-91 CLOSED 2026-07-29 at PR #1154 (the report-only telemetry-semantics decide-and-apply leg).** Per round 44's queue head, both questions decided against byte-read spec text and applied. **Q1**: the scope-mismatch withholds DO carry `failure_class=POLICY_DENIAL` — spec `:511` points the C-MEM-13 recorded outcome at C-MEM-19's existing "Policy denial" coverage item (`:683`), `failure_class` is that item's only class-carrying required attribute, and the `:703` vocabulary uniformly names outcome KINDS not deciding components (so not resolver-scoped). Refined across two real codex rounds to the converged predicate: **`policy_denial` ⟺ the family relation is CONFIRMED_MISMATCH** (both families resolve and demonstrably differ), under EITHER conjunct — same-family provider rebinds (a stale-context guard, R1) and unverifiable fail-closed states (absent record_scope / unregistered keys / out-of-domain values — the relationship never confirmed, R1) truthfully stay unset, while a confirmed cross-family REBIND does carry the class even though the identity conjunct fires first (R2 — the mirror under-claim). Mechanism: a `_ScopeFamilyRelation` tri-state (MATCH/CONFIRMED_MISMATCH/UNVERIFIABLE) resolved once from the existing authorities; the withhold guard's predicate becomes a pure projection, bit-identical. **Q2**: the `not_derived`/undetermined sentinels stay VERBATIM on `memory.policy.decision` — omission would drop a required attribute (`:694`, no stated value domain), and a generic `"denied"` asserts a denial nobody issued. Merge-gate 3-lens **all-approve R1** (concurrency: the TOCTOU shape ELIMINATED by carrying the class as frozen data; spec lens: the CONFIRMED-only reading verified honest — `:509/:523/:559` all predicate on families differing, spec silent on unverifiable states; witness lens: every tri-state arrow pinned, 10 failure_class assertions accounted for) with 2 inline closures (the out-of-domain witness's missing absence assertion; a supersession marker on the R1-era prose bullet). Codex R1→R3, 11 mutation probes total. One blocking-CI wall-clock flake (`test_flush_to_sqlite_100_span_batch_under_100ms_per_ac_5`, 201ms on a slow runner) rerun green. `just check` **7026**; register **92 items / 77 closed / 13 registered_finding**. **Next action:** (a) **the design-substrate cite-hygiene leg** — the stale line-cites into `docker_tool_execution_driver.py` (Runtime spec `:518` cites `:71` for the exact-match self-guard; Runtime plan v2_47/v2_48 cite `:63` for the `mcp_client_host` read — recorded at #1152's gate, already stale on main pre-#1152) + the `:481`-era threat-model cites in `llm_dispatch.py` docstrings that now resolve to `:541` (noticed at the B-91 leg); design-phase posture, back-flow doc owed per X-AL-3 (a Class 3 cite-hygiene filing precedent exists — check `.harness/` for the F-B cite-hygiene precedent shape). (b) U-MEM-26 forward residuals (C-MEM-10 promotion-eligibility — materially reachable; `_request_hash` raw-vs-canonical). (c) 13 `registered_finding` rows (dormancy re-check only if surfaces touched). The two operator unlocks stand (IAM grant → 3 OTLP e2es; older guard-blocked branch deletes — #1154's branch auto-deleted at merge).

**Round 44 — the carry-forward witness batch CLOSED 2026-07-29 at PR #1153 (tests-only).** Per round 43's queue item (a): **(1)** the retriever-layer production-path tenant witness — grounding found the load-bearing subtlety: selection-only assertions are structurally non-discriminating (the policy layer redundantly denies one step later), so before this arc the retriever's ENTIRE `_scope_mismatch` consultation was deletable-green across all 371 harness-is tests; the new end-to-end witness pins the exclusion REASON (`SCOPE_MISMATCH` vs `POLICY_DENIED`) — the only layer-separating observable — with a null-tenant arm verified load-bearing against the actual pre-U-MEM-26 either-side-null-skips predicate via `git show`; the deletion now fails exactly this one test (probe re-run INDEPENDENTLY by the spec-conformance lens: 1 failed / 370 passed). **(2)** The 4 raw-indexed span lookups replaced with the exactly-one helper — zero unguarded lookups remain in the file. **(3)** #1148's still-live notes dispositioned: B-90-trio pre-existing-pins note SUPERSEDED (three production-path witnesses now cover all three layers); the tmp_path tautology FOUND+FIXED (a vacuous `tmp_path.exists()` on a filesystem-free extraction path); the per-dispatch sink invariant RECORDED-as-accepted (a witness would test CPython, not harness code). Merge-gate 3-lens **all-approve R1**; codex R1 clean; one advisory-coverage CI flake (the known slow-fsync family under instrumentation) rerun green. `just check` **7025**. Review heuristic carried in the gate log: `memory_policy._scope_not_broader` hand-lists its fields rather than consuming `SCOPE_PARTITION_FIELDS` — a future partition-field change silently misses the third layer; and e2e reason-coverage is tenant-only (a provider_family drop is caught only at predicate level). **Next action:** (a) **B-91** — the decide-and-apply leg for the two report-only telemetry-semantics questions (scope-mismatch `POLICY_DENIAL` post-B-86; `not_derived` on the `policy.decision` axis); needs a careful spec reading (`:511/:523/:559` + C-MEM-19), `council: No`; outcome is either a small impl PR or a reasoned keep-unset disposition on the row. (b) The stale design-substrate line-cites into `docker_tool_execution_driver.py` (Runtime spec `:518` + plan v2_47/v2_48 — design-phase leg, needs back-flow doc; bounded cite-hygiene). (c) U-MEM-26 forward residuals (C-MEM-10 promotion-eligibility — materially reachable; `_request_hash` raw-vs-canonical observation). (d) 14 `registered_finding` rows (dormancy re-check only if surfaces touched). The two operator unlocks stand (IAM grant → 3 OTLP e2es; older guard-blocked branch deletes — #1153's branch auto-deleted at merge).

**Round 43 — the docker-driver reap-race carry-forward CLOSED 2026-07-29 at PR #1152.** Per round 42's queue head, the #1150 idiom ported to the tier driver: `docker_tool_execution_driver.py`'s single `_communicate` site (shared by the Docker and gVisor drivers — the subclass verified field-defaults-only) had two unguarded `proc.kill()`/`await proc.wait()` pairs where the already-exited transport race (`_check_proc` → `ProcessLookupError`) would replace the in-flight `ToolInvocationTimeoutError`/`CancelledError`; both now route through a local `_reap_child` (deliberate local duplicate — importing the sibling's module-private helper would couple the tier driver to the external-CLI lifecycle module; extraction owed at a third consumer; the sibling docstring now back-references it). Grounding note: unlike the CLI runner this driver had NO cancel-path reap gap — it already carried a cancel reap clause with a live regression test; the carry-forward was purely the suppression. **Merge-gate: one REAL test-witness BLOCK, the pass-on-absence class AGAIN** — deleting the timeout-branch reap entirely left all tests green (orphaned container on every tool timeout) because the new fake dropped the `kill_calls`/`wait_calls` liveness counters the settled #1150 sibling carries — fixed (counters incrementing BEFORE the raise so a suppressed reap still counts; `(process,)=spawned` argv-discriminated unpack pinning the docker-run child; both branch deletions now fail exactly their own witness, probed empirically) — re-gated APPROVE R2; concurrency + spec-conformance APPROVE R1 (real-child + double-cancel empirical probes; a very-thorough design-substrate sweep confirming NO canonical text constrains reap mechanics while C-AS-15's timeout binding is preserved; 2 PRE-EXISTING design-substrate line-cite drifts into this file recorded — Runtime spec `:518` + plan v2_47/v2_48 cite stale line numbers, already stale on main, needs a design-phase leg). Codex R1 clean + post-fix re-converged. `just check` **7024**. The `.kill()` race class is now fully closed repo-wide (exactly two producers, both `_reap_child`-routed). **Next action:** (a) remaining #1150/#1152 carry-forwards — the retriever-layer (`MemoryRetriever.retrieve`) production-path tenant witness; the 4 raw-indexed span lookups (`:3884/:4033/:4111/:4142`); #1148's still-live notes; the NEW stale design-substrate line-cites into `docker_tool_execution_driver.py` (design-phase leg, needs back-flow doc). (b) B-91 (decide-and-apply leg — the two report-only telemetry-semantics questions; careful spec reading). (c) U-MEM-26 forward residuals (C-MEM-10 promotion-eligibility; `_request_hash` raw-vs-canonical). (d) 14 `registered_finding` rows (dormancy re-check only if surfaces touched). The two operator unlocks stand (IAM grant → 3 OTLP e2es; older guard-blocked branch deletes — #1152's branch auto-deleted at merge).

**Round 42 — the C-MEM-19 emitter-attributes follow-on CLOSED 2026-07-29 at PR #1151; B-91 registered.** Per round 41's queue head, the two dispatch-site memory emitters (`_emit_degraded_memory_serve_span` + `_emit_unserved_memory_tool_calls_span`) now emit `memory.cli_profile` (from the frozen `memory_context.selection.cli_profile_ref`, one shared local at the dispatch `finally`) and `memory.policy.decision` — closing the gap carried in the gate log since #1142. Decision values per the primary-path precedent: `allowed` on injection AND pre-wire branches (a rendered packet proves the gate opened; the delivery axis is `dispatch_outcome`'s), the denial reason VERBATIM on report-only (which is what spec `:511`'s denial-reason-as-attribute-value requirement literally asks for — lens 2's own finding), constant `allowed` on the unserved emitter. **Merge-gate: one REAL spec-conformance BLOCK** — the failure_class-stays-unset docstring claimed "no resolver made a decision," false for the selector-denial family (`no_supported_mode`/`token_budget_empty` come from a real `select_memory_access_mode` run and the primary DENIAL span sets `POLICY_DENIAL` for exactly those values) — fixed: a data-carried `_DegradedMemoryServe.policy_denial` discriminator (frozenset-derived from the closed enum, set at the single disposition authority covering both dispatch routes) emits `POLICY_DENIAL` for that family only; the docstring rewritten three-family-truthful; **NEW B-91 registered** holding the two genuinely-open questions (scope-mismatch `POLICY_DENIAL` post-B-86 per spec `:511/:523/:559`; whether the `not_derived` sentinel belongs on the `policy.decision` axis) — register **92 items / 14 registered_finding**. Re-gated APPROVE R2 (flag⟺family bidirectionality verified; both dispatch routes covered presence-side); concurrency + test-witness APPROVE R1 (frozen-model totality of the finally-block reads; exact-value witnesses with two-directional constant-collapse kills incl. the subtle rendered-vs-reached_wire discriminator pin). The `memory.tier` half of the historical three-attribute gap dispositioned FALSE-PREMISE (primary composer omits tier identically; packet-level aggregation = spec question). Codex R1 clean + post-fix re-converged. `just check` **7022**. **Next action:** (a) **the docker-driver reap-race carry-forward** — `docker_tool_execution_driver.py:197-207` carries the identical un-suppressed `proc.kill()`/`await proc.wait()` pair #1150 fixed in the CLI runner (same ProcessLookupError race would replace `ToolInvocationTimeoutError`/`CancelledError`); bounded, the `_reap_child` idiom is the template. (b) Remaining #1150 carry-forwards (retriever-layer tenant witness; 4 raw-indexed span lookups; #1148's still-live notes). (c) B-91 (needs a careful spec reading — a decide-and-apply leg). (d) U-MEM-26 forward residuals (C-MEM-10 promotion-eligibility; `_request_hash`). (e) 14 `registered_finding` rows. The two operator unlocks stand (IAM grant → 3 OTLP e2es; older guard-blocked branch deletes — #1151's branch auto-deleted at merge).

**Round 41 — the gate-log residual-close arc CLOSED 2026-07-29 at PR #1150.** Per round 40's queue item (a), a grounding agent classified the five accumulated LOW items first (BUILD-NOW / REGISTER / FALSE-PREMISE per item, file:line evidence), then the bounded arc landed: **(1)** the CancelledError child leak from #1149's concurrency lens — the communicate block caught only `TimeoutError`, a mid-`communicate` cancellation leaked the child; fixed, then codex R1 caught the already-exited race (`kill()` → transport `_check_proc` `ProcessLookupError` replacing the in-flight exception) → ALL THREE reap sites (cancel/timeout/observer) now share `_reap_child` with the suppression, grounded against CPython 3.12's actual transport source (EPERM deliberately NOT suppressed); 4 witnesses incl. a real-300s-child cancellation reap. **(2)** the cancel-path `reached_wire` axis pinned (marker-below-await now fails exactly the new assertions). **(3)** B-90 production-path tenant witnesses — index end-to-end (3 request shapes + pre-filter liveness control) + policy layer via public `resolve_retrieval` (ALLOW control); the two probes are complementary per layer, confirming `_scope_not_broader`'s hand-list is a genuinely independent third path. **Dispositions on the record:** `{prompt}`-under-stdin construction fence DECLINED (would break a deliberate B-87 ordering witness; spawn-failure path named as the alternative trigger); `Promotion/CompactionScopeValueDomainError` declarations re-verified INERT at HEAD; the C-MEM-19 `memory.tier` half of the carried three-attribute gap is a FALSE PREMISE (the primary injection span omits tier identically — packet-level aggregation would be a spec question), independently confirmed by the spec-conformance lens. Merge-gate 3-lens **all-approve R1** with 4 inline closures (stale ONLY-reap-site comment; `_check_proc` attribution; two unbounded spawn-wait spin loops now bounded-fail-not-hang; the `[0]` span lookup → `_one_degraded_serve_span`). Codex R2 converged. `just check` **7022**. **Next action:** (a) **the C-MEM-19 emitter-attributes follow-on** — add `memory.cli_profile` + `memory.policy.decision` to both dispatch-site memory emitters (`_emit_degraded_memory_serve_span` + `_emit_unserved_memory_tool_calls_span`); grounded BUILD-NOW: values verified in scope (`memory_context.selection.cli_profile_ref`; policy-decision values well-defined by the primary-path precedent — "allowed" when served / the denial reason on report-only), `memory_telemetry_span` already accepts both kwargs, no signature change; the tier disposition travels with it. (b) The new carry-forwards: `docker_tool_execution_driver.py:197-207`'s identical un-suppressed kill/wait pair (same ProcessLookupError race would replace `ToolInvocationTimeoutError`/`CancelledError` — bounded candidate, possibly foldable into (a)'s PR or its own); the retriever-layer (`MemoryRetriever.retrieve`) production-path tenant witness; the 4 sibling raw-indexed span lookups (:3884/:4033/:4111/:4142); #1148's still-live notes (B-90 trio pins; tmp_path tautology; per-dispatch sink invariant unwitnessed). (c) The U-MEM-26 leg-close forward residuals (C-MEM-10 promotion-eligibility — materially reachable; `_request_hash` raw-vs-canonical). (d) 13 `registered_finding` rows (dormancy re-check only if surfaces touched). The two operator unlocks stand (IAM grant → 3 OTLP e2es; guard-blocked branch deletes — #1150's branch auto-deleted at merge).

**Round 40 — B-87 CLOSED end-to-end 2026-07-29 at PR #1149 (the reached-the-wire pre-wire classification arc).** Per round 39's queue head, the last open impl candidate from the B-83 family closed under the Fable-5-orchestrator discipline (all impl + review legs on Opus agents): a frame-local `_ProviderWireReachedSink` created per dispatch gates `_emit_degraded_memory_serve_span` — marker SET → the post-wire shape verbatim (`injection` + packet_hash, `completed`/`provider_error`); UNSET with a rendered packet → `dispatch_outcome="pre_wire_failure"` + `memory.degraded_serve.pre_wire_error_type`, NO packet_hash, `packet_assembly` operation name (attribute values/names only under C-MEM-19 §701's additive invariant — no closed enum touched; the register's own `translation_error` candidate REJECTED at codex R1 as over-claiming the phase). Placement per arm: construct-the-awaitable → mark → await at every direct-SDK site (call-time synchronous `TypeError` = pre-wire; in-coroutine vendor validation = post-wire, DECLINED on the record in the sink docstring — the boundary is OUR last observable point); external-CLI threads `on_wire` down to the subprocess runner, fired after `create_subprocess_exec` succeeds ("a child process exists that can observe the payload"; spawn failure = pre-wire; a raising observer kills+reaps the child); anthropic-memory fires once inside `execute_with_memory_callbacks` before its first SDK call. Compatibility: explicit-named-parameter capability checks at BOTH seams (adapter + runner; bare `**kwargs` deliberately does NOT count — a swallower would silently under-report a real disclosure), legacy tiers keep pre-B-87 placement; public runner seam statically admits both shapes (`WireAwareExternalCLISubprocessRunner` internal narrow + a durable pyright witness deliberately stated in `reportAssignmentType` form since the tests dir suppresses `reportArgumentType`/`reportCallIssue`). **Codex R1-R7 all real (9 findings), R8 clean:** phase-neutral naming; adapter-local argv validation; spawn boundary + legacy adapter contract; legacy runner seam + kwargs tightening; in-repo helper fire + per-callable awareness (class-keyed cache deleted as a correctness bug); static seam typing; call-time SDK validation split + child reap. **Merge-gate 3-lens: one REAL lens-3 BLOCK** — the OpenAI arm's marker at `:3650` had no discriminating witness (nothing read `memory.operation.name`/`packet_hash` on that arm; the mutation silently reclassified every successful OpenAI repair as `packet_assembly` with no hash — the under-reporting direction B-87 exists to close) — fixed (strengthened repair witness + call-time pre-wire + post-wire ordering witnesses, 3-mutation kill map verified empirically at re-gate) + 2 lens-2 LOWs fixed in-PR; re-gated APPROVE R2; concurrency + spec-conformance APPROVE R1. ~30 mutation-probed witnesses; `just check` **7016**; register **91 items / 76 closed / 0 open / 13 registered_finding**. **Next action:** (a) the accumulated gate-log LOW items — now including this arc's: lens-1's pre-existing symmetric child leak (the `communicate` timeout block catches only `TimeoutError`, so a `CancelledError` during `wait_for` unwinds without killing the child — register candidate), the `{prompt}`-under-stdin config accepted at construction and only refused at dispatch (a `model_validator` fence candidate), the cancellation witness leaving the `reached_wire` axis unpinned on the cancel path, and #1148's carried LOWs (Promotion/CompactionScopeValueDomainError `memory_failure_class` declarations; the B-90 production-path tenant witness; cli_profile/policy.decision/tier on both memory emitters). (b) Forward residuals from the U-MEM-26 leg-close doc (the C-MEM-10 promotion-eligibility question — now materially reachable; `_request_hash` raw-vs-canonical observation). (c) 13 `registered_finding` rows stand (dormancy re-check owed only if intervening merges touch their surfaces). The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes; #1149's branch auto-deleted at merge).

**Round 39 — the U-MEM-26 impl leg CLOSED 2026-07-29 at PR #1148; B-86, B-89, B-90 ALL CLOSED — the B-86 family (fork #1146 → spec leg #1147 → impl #1148) is complete.** Built as 4 sequential slices on one branch under the Fable-5-orchestrator discipline (an operator stop mid-slice-1 was honored and the arc resumed on explicit direction; one agent API death recovered clean). **Slice 1** (harness-is read boundary): per-field asymmetric null semantics (provider_family + tenant, contract-cited; other fields deliberately unchanged), the missing scope-boundary-before-ranking at the public index `retrieve` (absent scope ≠ wildcard), and the injected `ScopeFamilyCanonicalizer` seam (IS keeps zero outbound axis imports — the SigningBackend precedent; the fail-closed `provider_family_for_scope_check` bound at composition, never the LOCAL_OPEN_WEIGHT-defaulting cost authority). **Slice 2** (writer repair): `EpisodicMemoryCapture` consumes the run's composed `record_scope` (B-89; provider key still feeds payload/span; tenant+workload_class stamped — B-90); canonicalize-registered/deny-out-of-domain at all authoring surfaces with both ordering placements (promotion BEFORE risk/identity derivation — canonicalization deliberately changes candidate_id for raw-key inputs; native BEFORE the ledger append); redaction preserve-verbatim (legacy records stay redactable). **Slice 3** (dispatch withhold): ONE conjunct in `_standard_memory_tools_context` sharing `_packet_scope_matches_dispatch_family` verbatim with the disposition (a divergent predicate would route into the repair branch — the leak); TWO defect-pinning tests inverted, incl. the r300 production-path integration test that asserted the five memory tools reach the cross-family leg — the withhold now fires on the canonical `api.run` cross-family fallback path, report-only, capture unaffected. **Slice 4**: six closeout evidence rows certified, version-scoping lifted, arc note at `.harness/u-mem-26-impl-leg-close-2026-07-28.md`. **Codex R1-R10** (every round real, R10 clean): compaction candidate-content canonicalization; supplied-capture-scope + both-sides-canonical shared predicate (a raw-key static context no longer falsely withholds same-family); out-of-domain requests reject at ALL layers (no third posture); both promotion sides canonical; resume no-rewrite; then the R4-R9 torn-capture-repair hardening chain — three-state durable-presence guard, worker-invariant repair payloads, reverse-race completion, run-close-record guard, denial telemetry span, occupant verification, reserved repair-actor namespace (root cause named: EPISODIC_RUN memory_id derives from run_id alone). One codex decline recorded with spec `:511` cites (the composition-time inject row nuance — ratified at the spec leg; lens 2 independently verified the cite resolves). **Merge-gate 3-lens: one REAL lens-3 BLOCK** — the inverted r300 e2e had lost its only liveness discriminator (passed whenever memory was absent for ANY reason — the pass-on-absence class) and the reserved-actor error's POLICY_DENIAL declaration was deletable-green — both fixed (span-reason discriminator selecting `memory.degraded_serve.reason == provider_family_scope_mismatch` exactly-once in the deterministic production-path test, mutation-probed memory-off; two wording-conflicting contract-table rows), re-gated APPROVE R2; concurrency + spec-conformance APPROVE R1 (the full four-ordering race matrix independently re-derived against the real lock discipline; all 13 acceptance criteria walked). ~95 new witnesses, every one mutation-probed; `just check` **6983**; register 91 items / **75 closed / 0 open** / 14 registered_finding. Process note: the merge fired while the final doc-only gate-log commit's CI was still pending (every code commit had green CI; post-merge main CI verifies the tree — confirm green). **Next action:** (a) **B-87** (reached-the-wire pre-wire marker — bounded impl, `council: No`, the last open impl candidate from the B-83 family); (b) the accumulated gate-log LOW items (incl. #1148's: Promotion/CompactionScopeValueDomainError missing `memory_failure_class` declarations — inert today; the B-90 production-path tenant witness; cli_profile/policy.decision/tier on both memory emitters; the two historical clearance-marker carry-notes); (c) forward residuals from the leg-close doc (the C-MEM-10 promotion-eligibility question is now materially reachable — a cross-family leg captures under the primary scope, witnessed; `_request_hash` raw-vs-canonical observation). The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes; #1148's branch auto-deleted).

**Round 38 — the B-86 spec-leg CLOSED 2026-07-28 at PR #1147 (doc-only Class 1 apply leg).** Per round 37's queue, a dedicated Opus spec-writer agent (genuinely adopting the skill) applied the fork-ratified resolution: **Spec_Memory_Substrate v1 → v1.1** — C-MEM-03 `provider_family` value domain (`ProviderFamily` values, never provider keys) + **asymmetric** `null` semantics (stored-side wildcard; a null REQUEST is a *broader* ask and is denied — the symmetric first draft was caught by codex R1 against `memory_policy._scope_not_broader`, and the R10 rationale inversion was caught too) + run-level derivation rule (chain primary, once, at run-scope composition; narrowed at R12 to automatically-captured records — other authoring writers own their scopes subject to the value domain) + request-side value-domain binding (R12: the unretrievability claim was bypassable via raw-key request scopes); C-MEM-13 cross-family withholding invariant (MUST NOT expose tool schemas or `scope_ref` cross-family; harness-authored capture unaffected; family-equality necessary-not-sufficient stated); C-MEM-14 exposure qualification (the fork's "ledgered outcome" phrasing corrected to recorded-outcome — C-MEM-08's closed 12-kind enum, with the `no_memory_access` must-ledger discharge grounded at `compose_run_start`'s per-dispatch `inject` row); threat model `:481` untouched as authority. **Plan v1.1: NEW U-MEM-26** (axes runtime+CP+OD+IS; depends on U-MEM-07/09/14/16/22/25) — withhold guard, writer repair, write-boundary enforcement across the grounded authoring surfaces (capture/promotion/compaction/native — with the redaction transition as the explicit preserve-verbatim exception so legacy raw-key records stay REDACTABLE, a compliance hazard codex R9 caught; promotion canonicalizes BEFORE risk/candidate-identity derivation and native validates BEFORE the ledger append, R11), request- + direct-reader-boundary enforcement (R13: the public index `retrieve` has NO policy leg — its predicate fix sits exactly at `:481`'s before-ranking position), per-predicate null/tenant denial witnesses (three separate, never composite), six-row closeout-evidence refresh, and a **review-time-inventory cap note binding unlisted surfaces by rule, not listing**. Closeout evidence packet version-scoped to v1 with PENDING—U-MEM-26 markers; the codex-proposed red-until-impl gate DECLINED on the record (main-always-green); clearance markers for both artifacts (X-AL-3 satisfied in-PR). Register: **B-86/B-89/B-90 all transited to `open`** with live build-U-MEM-26 next-step heads (3 open / 0 design_substrate_gated / 14 registered_finding). Review: **14 codex rounds, every round a real finding, none rubber-stamps** — R12 correctly REOPENED the contract after R11 was prematurely labelled terminal (recorded honestly in the reviewer chain); exit argued on soundness (contract quiet post-R12 across two rounds; residual enumeration bound by the cap rule; a contract-level defect reopens on R12's terms). One transient agent API death (ENOTFOUND) recovered clean — the branch was verified unchanged before re-driving. `forward_register.py --check` + `memory_closeout_check.py --check` both clean; CI 17/17 green; doc-only so merge-gate correctly skipped. **Next action:** (a) **the U-MEM-26 impl leg** — the queue head: a substantial multi-axis code arc (harness-runtime + harness-is + harness-cp surfaces; re-ground the full reader/writer inventory at impl HEAD first per the unit's own cap note; closes B-86, B-89, B-90 together); recommend a FRESH session/iteration given its size. (b) **B-87** (bounded impl, council: No). (c) Gate-log LOW items. The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes; #1147's branch auto-deleted at merge).

**Round 37 — the B-85/B-86 grounding arc CLOSED end-to-end 2026-07-28 at PRs #1146 (doc-only Class 1 filing) + #1145 (impl + register).** Per round 36's queue, a fresh session ran the two-item grounding under the Fable-5-orchestrator discipline: two parallel Opus grounding agents → transcript-aware `advisor()` (GO + 4 adjustments) → a genuinely-convened council (C10+C3 co-primary, C6 consultant — expansion beyond dyadic justified by a distinct third concern, fallback-chain semantics). **B-85 CLOSED with posture (3) RECORD as impl discretion** — grounding verified the spec silent (C-MEM-14 `:489` obliges tool EXPOSURE not service; C-MEM-13 `:463` binds only `no_memory_access` transitions; C-MEM-08 `:247`/`:251`'s closed 12-kind enum cannot express requested-but-unserved → the ledger half X-AL-3-blocked; C-MEM-19 `:619` already covers the span), the row's own `council: Likely` was stale against the narrowed option set (postures (1) FAIL / (2) SPLIT rejected as Class-2-shaped — invariant inversion / duplicate-durable-write reopen; a 4th withhold-tools-when-caller-tools option enumerated-and-rejected). Landed: separate single-slot sink + emitter (never the degraded-serve emitter — its `rendered=None` shape asserts `no_memory_access`/`degraded_serve=True`, both false at iteration k>0), `operation_name=standard_tool_call`, `record_count=0`, no packet_hash, `memory.unserved_tool_call.*` attribute VALUES per the B-83 precedent, per-arm k=0 + k>0 witnesses, caller-only negative, 4 mutation probes. **B-86 flipped `registered_finding` → `design_substrate_gated`** with the Class 1 filing `.harness/class_1_fork_b86_memory_scope_provider_family_keying.md`: the spec is genuinely silent on `MemoryScope.provider_family` keying (C-MEM-03 bare `string | null`, no value domain; C-MEM-11/13 carry route + scope as independent inputs; threat model `:481` mandates enforcement without keying). The council probe-RESOLVED the C10↔C3 tension: the leak's carrier is the unconditional `scope_ref` binding at the injection site, NOT the scope's key (Probe E — the writer is already candidate-derived today and the leak occurs anyway); recommendations **Q1 = chain-primary run-level keying stated in spec, Q2 = withhold tools on cross-family SERVABLE dispatch with a named denial reason, harness-authored automatic capture unaffected** (C3's condition); C6's stated limit: family equality necessary-not-sufficient (`local_terminal` is within-family). **Item (3) landed as the interim floor**: `provider_family_for_scope_check` fails closed (`None`) on unregistered keys at `_packet_scope_matches_dispatch_family` — previously an unknown key compared EQUAL to a `local_open_weight` scope and the degraded packet got REPAIRED (a live leak; now report-only, mutation-probed). **B-89 registered (`design_substrate_gated`)**: producer key-vs-value asymmetry — capture stamps raw provider KEYS (`memory_capture.py:585-591`) while retrieval requests carry family VALUES; an ollama-written note is invisible even to its own family (empirically confirmed; tool-written notes write-only for 5 of 8 keys, so the continuity the primary-derived reading supposedly buys is largely notional until B-89 lands); direction determined by the council record (the WRITER adopts the run's composed `record_scope`), gated on the spec-leg. **B-90 registered**: capture scope omits `tenant`/`workload_class` → tool-captured records tenant-unpartitioned under wildcard-on-`None` at all three enforcement sites, against `:481`'s tenant mandate; incidentally closed by the B-89 repair. Process note worth repeating: the codex context guard's DESIGN_IMPL_MIX correctly hard-failed the fork-doc+impl bundle — fork filings ship doc-only (#1146 split out and merged FIRST so the register's fork-doc reference resolves on main at all times). Review: codex converged R1 clean on the full branch diff; `merge-gate` 3-lens all-approve R1 (lens 1: sink per-dispatch frame-local, sinks proven never co-populated with the invariant correctly non-load-bearing, 1 docstring over-claim trimmed inline; lens 2: all 5 spec cites byte-verified incl. the 12-kind count, X-AL-3 clean, 4 LOW notes recorded incl. the `cli_profile`/`policy.decision`/`tier` omission shared with the degraded emitter — move together when the #1142 LOW closes; lens 3: every mutation probe traced to a named failing test, per-arm witnesses independent, 2 gaps closed inline — the vacuous same-span degraded negative replaced with a real cross-span `_degraded_serve_spans()==[]`, the ollama caller-only witness gained its missing unserved-span negative). `just check` 6916; register 91 items / 15 registered_finding / 2 design_substrate_gated / 72 closed. **Next action:** (a) **the B-86 spec-leg** — C-MEM-03 value domain + explicit `null` semantics, the derivation rule, the C-MEM-13 withhold invariant, the C-MEM-14 exposure qualification (drafting targets at fork doc §6; open as a FRESH focused iteration per the B-79/B-80 precedent; the council explicitly recommends an out-of-family codex decorator on the eventual C-MEM diff); its impl leg (Q2 withhold guard + B-89 writer repair + B-90 fold-in) follows ratification. (b) **B-87** (reached-the-wire pre-wire marker — bounded impl, `council: No`). (c) The accumulated gate-log LOW items. The two operator unlocks stand (IAM grant → 3 OTLP e2es; the 6 guard-blocked branch deletes — #1145/#1146's own remote branches were auto-deleted at merge, no new hygiene debt).

**Round 36 — B-88 impl half CLOSED 2026-07-28 at PR #1144.** The failure-class declaration now lives at the exception definition site: `classify_memory_failure` honors a `memory_failure_class` attribute read from `type(exc)` (hierarchy-only authority per codex R1; MRO-inherited; honored only for real members of the closed C-MEM-19 vocabulary — a typo cannot widen it), with substring rules surviving only as the undeclared residual. Grounding chose declaration-at-definition-site over an in-classifier name set (harness-is cannot see the runtime hierarchy; a name list = mirror-copy drift) and corrected the row's own prescription (which would have regressed all 8 genuine IO sites). Populations: 14 DeniedError sites → `policy_denial` by type (4 mis-worded sites fixed, not the row's 2); InputError → `provider_adapter_failure` (stopgap restoring the pre-B-84 value; the input-validation vocabulary class stays SPEC-SIDE registered — B-88 remains `registered_finding`); codex R2's store-failure population fixed via `MemoryToolExecutionStoreError` declaring IO_FAILURE, keyed on the closed `MemoryCaptureStatus` enum (the only structured discriminator — the capture result retains just a reason string; grounding strengthened the finding: the capture span is a NO-OP on that path, so the outer span was the sole record). Review: codex converged R4 (3 bounded findings: type-vs-instance read; the store population; public-API export); `merge-gate` 3-lens — concurrency + spec-conformance APPROVE R1 (the declaration convention grounded BELOW seam altitude: zero overlay seams have harness-runtime as an endpoint), test-witness **BLOCKED once on the arc's own defect class** (3 of 4 new `_NativeMemoryPolicyDeniedError` sites revertible-to-base with a green suite since `pytest.raises(base)` is satisfied identically) — fixed with span-level pins selected by `policy.decision` not position, per-site probes failing exactly-one-test each, the previously-untested redaction-state case covered, plus wording-CONFLICTING rows making the base/path/store declarations non-deletable-green and the redaction consumer's emission pinned discriminatingly; re-gated APPROVE with the pins verified 1:1 BY CONSTRUCTION (each trigger appears exactly once). Register counts corrected (37 rows / 14 sites) with drift-prone line cites replaced by message/method anchors. `just check` 6906; register 89 items / 16 registered_finding. **Next action:** the Claude-executable register queue is now **B-85/B-86 grounding** (posture/scope-semantics — possibly fork-shaped; B-86 likely council per its C-MEM-11/13 shape), **B-87** (pre-wire marker at the degraded-serve emission), and the accumulated gate-log LOW items (incl. lens 3's noted companion: a wording-conflicting InputError row belongs in the SPEC-half arc when the flip site moves). The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes). Session note: rounds 29-36, 10 substantive PRs (#1137-#1144); context is very deep — a fresh session picking up from this roadmap + the checkpoints is the recommended continuation shape for the B-85/B-86 grounding arcs.

**Round 35 — B-84 partial mitigation CLOSED 2026-07-28 at PR #1143; B-88 registered.** Per round 34's queue, the most-bounded code arc landed: `MemoryToolExecutionInputError` joins the retry classifier's fail-fast set. **Grounded first as impl-discretion:** runtime spec §14.6 D2 (`Spec_Harness_Runtime_v1.md:3796`) makes fail-fast the RESIDUAL class ("retry-eligible iff §21.2 transient staircase; fail-fast otherwise") — the closed enumerations constrain the classes, not the exception→class mapping, and B-41 is in-workspace precedent for extending the mapping spec-free. The same-candidate duplicate-commit staircase closes; candidate advance preserved; DENIED deliberately excluded (it wraps genuinely transient store I/O — witnessed); the rationale honestly corrected in the register (retries re-sample model arguments — the harm removed is the duplicate durable commit, not wasted attempts); the multi-candidate residual keeps **B-84 `registered_finding`**. Codex R1 caught a real [P1]: the `_allowed_kinds` bare enum conversion sailed past the new branch — the inverted-subclass trap (`MemoryToolExecutionInputError` SUBCLASSES `ValueError`) — wrapped at the executor with an exhaustive sweep (5 helpers, 12 argument keys, both layers: the only reachable bare raise); R2 converged. `merge-gate` 3-lens all-approve R1; spec-conformance surfaced a REAL pre-existing latent defect, registered as **B-88** and SHARPENED by the closing agent's own probe: `classify_memory_failure` substring-keys on `"io"`, which matches "execut-io-n" — the entire typed-input-error family emits `io_failure`, and worse, genuine policy denials ("is superseded"/"is not retrievable") ALSO emit `io_failure` (only the literal wording "denied by policy" is rescued) — violating C-MEM-19 §641's distinguish invariant on an axis the vocabulary already has; the row splits the fix (type-keyed classification = impl-side; an input-validation vocabulary class = spec-side Class 1, explicitly not absorbable). The lens-3 typed-raise pin (3-case parametrize) landed inline. `just check` 6848; suite 3160 at close; register 89 items / 16 registered_finding. **Next action:** the Claude-executable queue is now **B-88's impl-side half** (type-keyed `classify_memory_failure` — bounded, harness-is-local, the spec-side vocabulary half stays registered), **B-85/B-86 grounding** (posture/scope-semantics — possibly fork-shaped; B-86 likely council per its C-MEM-11/13 shape), **B-87** (pre-wire marker), and the accumulated gate-log LOW items. The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes). Session note: rounds 29-35 shipped 8 substantive PRs (#1137-#1143) under the round-28 standing approval — a fresh iteration should consider whether continuing in this session or a clean session serves the next arc better (context is deep; the loop discipline held throughout).

**Round 34 — B-83 CLOSED end-to-end 2026-07-28 at PR #1142 (the memory serve-check arc).** Per round 33's queue, the raised-priority `B-83` (nothing verified the dispatch arm serves the selected memory access mode — a fully-retrieved, policy-checked packet sat unused while the model silently got zero memory) closed with the grounded direction: a dispatch-time serve-check + **policy-authorized repair-to-packet** at one shared disposition authority. Repair requires THREE conjuncts — provider identity; provider-FAMILY scope keyed on the packet's actual `record_scope.provider_family` via the one existing provider→family authority; and prompt-packet eligibility derived at composition by re-running the REAL `select_memory_access_mode` with tools masked (the exact counterfactual, zero mirror-copied policy logic, fail-safe `not_derived` default) — any conjunct failing is report-only with the selector's own denial reason on the span. Packet folds happen at each translator's own settle point (each arm's fold predicate mirrors its own conflict predicate; closed by a 17-combination sweep); emission is exception-safe (finally + pre-published sink; cancellation labelled honestly). **Deliberate behavior change:** the B-82 R5 ollama fallback no longer byte-restores bare dispatch — the retry stays tool-free but carries the packet when authorized (the restoration property WAS the defect on the commonest path; superseded note recorded on B-82's close-out). Review: **8 codex rounds, 10 real findings fixed + 1 registered at the cap** across four surfaces (scope / source-folding / authorization / observability — the R8 pre-wire over-reporting registered as `B-87`, fail-safe direction); `merge-gate` 3-lens: concurrency + test-witness APPROVE R1, spec-conformance BLOCKED once on 3 real ledger items (B-85 cite drift through the arc's own rounds, B-82's falsified byte-restore claim, an overstated enum clause) — fixed + re-gated APPROVE, plus the test-witness lens's two false-green witnesses (arm-site family mismatch; absent-record_scope fail-closed) closed inline, mutation-probed. **Dual live $0 proofs at the model:** same-family repair — the reply paraphrases the injected record; cross-family — the disclosure boundary holds. Registered out of the arc: `B-85` (mixed-batch posture), `B-86` (servable tools path crosses the family boundary with read AND write reach — C-MEM-11/13 scope semantics), `B-87`. `just check` 6842; suite 3153 at close; register 88 items. One impl-agent API-death mid-gate-fixes was recovered by the orchestrator applying the five specified items directly. **Next action:** the Claude-executable register queue is now `B-84` (mid-batch executor-failure atomicity — three candidate directions incl. the cheap fail-fast reclassification), `B-85`/`B-86` (posture/scope-semantics decisions — ground first, they may be fork-shaped), `B-87` (pre-wire marker), the gate-log LOW items (rounds 29/32/34: parametrize the fold sweep, empty-content fold pinning, degraded-span cli_profile/policy.decision attrs, emitter-raise-in-finally guard, R7 redaction-family fixture polish). The two operator unlocks stand (IAM grant → 3 OTLP e2es; 6 guard-blocked branch deletes). A fresh iteration should ground-and-pick per §4 order — B-84's fail-fast reclassification is the most bounded next code arc; B-86 likely needs a council/fork given its scope-semantics shape.

**Round 33 — RC re-validation CLOSED 2026-07-28 at PR #1141: PASS with one operator-gated residual.** The closure report `.harness/release-candidate-deployment-readiness-report-2026-07-28.md` (runbook §7-conformant, verdict `deploy` with the 3 OTLP e2es as the named residual) records the full batch: Phase A green; **Phase B 3/3 green after THREE genuine drift findings** — (1) the r500 script's fabricated audit entries carried placeholder hashes the B-56-era write-side content-integrity check refuses, and (deeper, codex) the entries never chained while the per-tenant OD audit chain was never verified at all → fixed, chained, per-tenant `verify_hash_chain_integrity` added over the rehydration surface, mutation-probed live, and the probe FROZEN as CI regression tests (the r500 test module had never run in CI; now wired into the Q3 tools job); (2) the deploy template's stdio MCP echo server foreclosed at tier-1 by the enforced C-AS-02 §2.3 row-3 floor → config-only unblock via a Lima gVisor wrapper driver, template + README now document the floor, the wrapper pattern, and the exact-`runsc` registration requirement; (3) the template's explicit host-process/host labels survive the tier raise and mislabeled runsc execution on `sandbox.enter` spans → unset-to-derive (gvisor/runsc), honest re-run green, first runs' false labels recorded. R-411 green; Phase C non-GCP 4/4 green; Phase D baseline-match. Runbook de-staled (`aws login --profile r830`, Lima state, placeholder warnings both copy steps, r500 note); CLAUDE.md §2.6's aws-login phrase de-staled in-PR (precedented). Codex: 8 real findings across 7 rounds — R7's producer-aware-verifier point DECLINED on soundness grounds with a scope note in the report (the proof is a §21.2 primitive-level witness over a deliberately-chained single-family fixture; a redaction-token-family variant is registerable polish). Merge-gate 3-lens all-approve R1 with inline closures (identity assert, false comment, cross-ref/heading nits, CI witness wiring). Report calibration line worth repeating: **two of the three defects produced perfectly passing runs — a green live-e2e is a weaker witness than it looks.** **Next action:** the five-gate queue is now fully executed except: (1) the operator IAM unlock → 3 OTLP e2es (resume recipe incl. managed-TOML recreation is in the report §Remaining-risks); (2) the 6 guard-blocked branch deletes (Round 31 list); both purely operator-side. Claude-executable register work remaining: B-83 (raised priority — mode-vs-arm serving verification incl. the fallback + mixed-batch un-ledgered degradations), B-84 (+ aggravator; cheap fail-fast reclassification candidate), the round-29/32 gate-log LOW items, and the R7 redaction-family-fixture polish. A fresh iteration should ground-and-pick from those per §4 order, or honestly idle-report if the operator unlocks arrive first. Services left running: Ollama daemon, Lima r411-gvisor VM, Docker Desktop; scratch selfhosted TOML + wrapper retained (recorded deviation); managed-cloud TOML deleted.

**Round 32 — B-82 CLOSED end-to-end 2026-07-28 at PR #1140; Phase B attempted, surfaced a REAL template-vs-floor drift.** The B-82 impl leg (direction (A), grounded by live probes that falsified the register's own coupling premise — ollama's native `/api/chat` accepts the dotted C-MEM-14 identities verbatim, no wire map needed, and direction (B) would have encoded a false capability claim) landed the ollama standard-memory-tools loop AND hardened BOTH memory loops symmetrically: batch prevalidation via a frozen prepared-call carrier (durable double-write class closed), exhaustion raised before the terminal batch with malformed terminal batches failing fast, the dispatch `scope_ref` check aligned to the executor's own contract line (3 of 5 advertised tools had been latently unexecutable since the memory loop shipped — on OpenAI too), non-memory tool calls returned to the caller unprocessed (bare-path parity), usage accumulated across all continuation calls (cost attribution previously dropped every non-final turn), and a first-call-only daemon-authoritative fallback for non-tool ollama models. Review: 6 codex rounds, 8 real findings, none rubber-stamps; merge-gate 3-lens all-approve R1 with 2 inline closures (lens 2 caught B-83's same-day stale-carry — the R5 fallback created a third, DEFAULT-reachable instance of B-83's shape, now recorded as reachability (c); lens 3's multi-call order/correlation witnesses added, 5 probes). `B-83` (mode-vs-arm serving verification, priority RAISED by (c)) and `B-84` (executor-level mid-batch argument failure; SDK schema-drop aggravator; 3 candidate directions incl. cheap fail-fast reclassification) both registered. **Live: the ollama memory path completed end-to-end vs llama3.2:3b ($0).** `just check` 6814 passed; full runtime suite 3125. **Phase B (RC gate (c)) attempted after Docker came up:** the compose stack ran (collector/tempo/grafana up, stack-down clean) but the r420 live e2e is BLOCKED by a genuine re-validation finding — the deploy template's stdio MCP echo server is FORECLOSED at its declared tier-1 by the now-enforced C-AS-02 §2.3 row-3 floor (STDIO transport → TIER_3_MICROVM), and the TOML-level `SandboxDriverConfig` carries only a single `docker_binary` string while the working r411 precedent needs the multi-word Lima gVisor invocation — the template predates floor enforcement (the 2026-06-10 GO pass ran before it). Both scratch TOMLs had stale `/absolute/path` placeholders too (fixed locally for the run). **Next action:** (1) a fresh iteration should open the Phase-B unblock arc — decide between wiring a tier-3 driver config into the self-hosted template (ground whether `docker_binary` can carry a wrapper script for the Lima gVisor docker; the VM is Running) vs a documented template fix routed as the runbook's own follow-on — then finish Phase B + write the RC closure report per runbook §7 (Leg 8: remove both scratch TOMLs; nothing to revoke — the IAM grant was never applied); (2) the GCP token-creator re-grant remains the one operator unlock for the 3 OTLP e2es (classifier-blocked for Claude; exact command in Round 30); (3) the 6 guard-blocked branch deletes (Round 31 list); (4) remaining registered work: B-83 (raised priority), B-84, and the round-29 gate-log LOW items. Ollama daemon + Lima VM left running; Docker Desktop left up.

**Round 31 — G3/G4 witness arc CLOSED 2026-07-27 at PR #1139; branch prune 3/9 executed.** The queued G3/G4 false-green witness arc (recorded at the #1137 merge-gate, queued rounds 29-30) landed as a tests-only PR: 7 new CI witnesses (the explicit `--auth-check`/`--no-auth-check` override in both polarities on preset AND no-preset paths; all three `_assert_configured_auth_command_succeeds` branches through the production constructors with a scripted fake runner, probe argv + timeout grounded to the wire; the no-flag default-True preset pinned against an argparse-default regression; the no-preset `bool(auth_args)` derivation asserted). All mutation-probed (each named mutation kills exactly its witnesses); `merge-gate` 3-lens all-approve at R1 — lens 3 named 3 adjacent cheap residuals, closed inline pre-merge; residuals 4-6 (exit-code `>0` variant, stdout-fallback arm, exit-0-only probe contract note) recorded in the gate log; codex R1 clean; full suite 3099 passed. **Also this round, gate (e) partial execution:** the lease-guarded remote prune succeeded for 3 of the 9 deferred branches (`rc-rebaseline-phase-d-reaudit`, `b81-ow-material-diff-test-parity-step1`, `grounding-pass-b44-b45-b56-b57-b66` — each verified MERGED + head-OID-leased + 404-confirmed) before the permission guard began HARD-STOPping the identical operation; the guard's permit/deny behavior was inconsistent across identical command shapes (worth operator attention). The 6 remaining, all verified merged with recorded head OIDs: `b39-spec-leg-nested-hitl-response-routing` (#1092), `b72-impl-leg-pre-dispatch-gate-owning-carrier` (#1116), `b78-eo-dh-hitl-delivery-cell-impl-leg` (#1117), `b81-exclusion-conjunct-tests-step2` (#1119), `roadmap-status-refresh-post-1092` (#1093), `u-mem-live-gates-and-grounding-drift` (#1136). **Next action:** unchanged from round 30 — the queue residue is the two operator unlocks (Docker vmnetd sudo repair → Phase B + RC closure report; GCP token-creator re-grant → the 3 OTLP e2es → revoke at close) plus the 6 guard-blocked branch deletes; probe both unlocks at each wake. The remaining Claude-executable queue item is the `B-82` ollama-gap impl leg (grounded at PR #1138's register row; needs a direction choice between inject-with-wire-encoding vs narrow-the-declared-capability — ground against C-MEM-14 + the ollama API's actual tool support before building).

**Round 30 — five-gate queue gates (c) [4 of 6 sub-legs] + (d) CLOSED 2026-07-27; gate (d) surfaced and fixed a REAL production regression at PR #1138.** Per round 29's queue, this round drove the RC re-validation batch (gate c) and the paid cross-family re-confirmation (gate d). **Gate (c) executed:** Phase A fully green (`just check` 6782; overlay 389/36; both ledgers; tools tests 34; both readiness probes all-PASS); Phase D overlay audit matches the 2026-07-27 baseline exactly; **R-411 gVisor live e2e PASSED** (grounding found the runbook's "volume ABSENT" claim STALE — the Lima volume was mounted with `r411-gvisor` merely Stopped; `limactl start` sufficed, no re-provision); **Phase C non-GCP legs 4/4 PASSED live** (E2B probe `r421-e2b-ok`; r412 full-VM; managed-DB Neon; S3 via the test's documented static-key fallback — no browser needed; separately found the runbook's `aws sso login` guidance stale, this host uses AWS CLI v2.34's `aws login` flow). **Two legs remain operator-blocked, surfaced live to the operator this session:** Phase B compose-stack smoke (Docker Desktop fails to start with a vmnetd/privileged-port AppleScript repair error — sudo repair command provided) and the three OTLP e2es (the auto-mode classifier hard-blocks `gcloud iam ... add-iam-policy-binding` regardless of the recorded standing approval — exact re-grant command provided; do NOT route around). **Gate (d) CLOSED with a major find:** the live `mvp-r300-cross-family` FAILED — not credentials: OpenAI 400-rejects `tools[0].function.name` `memory.search`. Root cause (Opus diagnosis, confirmed by credential-free repro): the automatic memory substrate (default-on since PR #912, 2026-07-04) injects the five dotted C-MEM-14 `memory.*` identities into every OpenAI dispatch; OpenAI requires `^[a-zA-Z0-9_-]+$`; **every default-config OpenAI dispatch had been broken since 2026-07-02/04**, invisible because the deterministic fake swallowed kwargs and the ollama variant silently drops the tools. Fix at PR #1138 (impl-discretion — AS spec §3.3 defers serialization; identities stay dotted): bidirectional dict wire-name map (`memory_search`, ...) at the emit site, tool-result message, dedup filter (union of both spellings), inbound resolve (wire first, dotted tolerated, unknown raises), plus an import-time coverage/uniqueness/grammar guard. Witnesses mutation-probed; the deterministic r300 fake now RECORDS kwargs. **Live before/after: 400-failed pre-fix → PASSED post-fix** (fallback chain completes on `openai:gpt-4o-mini`). `B-82` registered: ollama access-mode selects standard memory tools but the dispatch branch never serializes them (+ Class 3 note: C-MEM-14 is identity-normative, wire encoding adapter-owned; docs/memory-layer-readme.md:18,68 over-promise is B-82's doc co-edit). Review: codex converged R1; merge-gate 3-lens all-approve R1 (test-witness's 2 cheap gap-closures applied inline: wire-spelled caller dedup with a `len()` check set-equality cannot see, dotted-tolerance resolve branch). **Next action:** the five-gate queue residue is now exactly three operator-unlocks — (1) Docker vmnetd sudo repair → then run Phase B (stack-up/status/live-e2e/tail-keep/multitenant/stack-down) + write the RC re-validation closure report per runbook §7 (Leg 8: remove both scratch TOMLs, revoke nothing — the grant was never applied); (2) the GCP token-creator re-grant → then the three OTLP e2es (r421/r810/r820, flags recorded in the runbook) → revoke at close; (3) gate (e) branch prune (attempt the lease-guarded recipe; expect the permission-guard denial; report). Also queued: the B-82 ollama-gap impl leg (grounded, two candidate directions recorded in the register) and the small G3/G4 witness arc from round 29. When the operator applies unlocks (1)/(2), a fresh iteration finishes the RC batch and writes the closure report.

**Round 29 — five-gate queue gates (a)+(b) CLOSED end-to-end 2026-07-27 at PR #1137.** Per round 28's standing-approval queue, this round executed the first two pre-authorized gates under the Fable-5-orchestrator discipline (impl + review legs delegated to Opus agents). **Gate (a) — generic-command live gate: PASS.** `U_MEM_24_GENERIC_COMMAND_AUTH_PROBE="ollama list"` is now the STANDING declaration, embedded in the gate's `resume_command` at `memory_verification_suite.py`; the live run bound `generic-command:custom` (ollama 0.18.3; the daemon was already up via Ollama.app and was left running — "restore after" meant nothing to restore). **Gate (b) — gemini_legacy: probe wired + fired, terminally NOT CONFIRMED on this host by upstream refusal.** The free-tier probe (`--skip-trust -p "Reply with the single word OK."`) is DECLARED at `PROVIDER_PRESETS["gemini"].auth_args` (preset keeps `auth_check=False` so adapter construction never fires it implicitly; explicit `--auth-arg` overrides); fired live under standing approval it exits 1 with `IneligibleTierError` — Google has deprecated the legacy CLI for individual accounts in favor of Antigravity, whose own gate is already CONFIRMED via `agy`. The skip-stub live test was replaced with a real preset-sourced test through the production `construct_gemini_cli_adapter`. Review was genuinely multi-round: codex converged at R6 after 5 real [P2]s, each about newly-added code (production-adapter bypass in the generic-command test — the same defect class PR #1136 fixed for antigravity; probe-output leak into fail/skip reasons; gutted-declaration silent skip; argv-rot invisibility → bounded internal marker classification, FAIL on recognized malformed-argv shapes, skip preserved for unknown auth drift; exception-chaining re-leak → fail/skip raised outside the except block, redaction empirically proven by a bad-flag control containing zero probe text). `merge-gate` 3-lens: concurrency + test-witness APPROVE at R1; spec-conformance BLOCKED once on a real one-sentence defect (the evidence semantics prose still claimed the gemini skip carries "the probe's own failure text" — false post-redaction and self-contradicting the outcome row), fixed + re-gated APPROVE at R2. Follow-up candidates recorded in the gate log, NOT yet registered: test-witness G3 (`--auth-check` override path untested through `materialize_external_cli_config` — a mutation dropping the explicit-argument override stays CI-green) and G4 (`_assert_configured_auth_command_succeeds` branches have no CI test — the false-green class), plus concurrency's pre-existing cancellation-orphan window at `external_cli_provider.py:177-185` (no try/finally around `wait_for`; widened by the 60s gemini spawn). `just check` 6782 passed ×2; one transient `agy models` timeout observed mid-arc, passed on re-runs ×3. **Next action:** continue the round-28 standing-approval queue in order — **(c) the RC live re-validation batch per the runbook** (start Docker Desktop; re-mount/re-provision the R-411 Lima volume; run the paid Phase-C batch, cents class; re-grant GCP `roles/iam.serviceAccountTokenCreator`; `aws sso login --profile r830`, recording + continuing past a stalled headless SSO round-trip); **(d) the paid `just mvp-r300-cross-family` re-confirmation**; **(e) the branch-hygiene prune of the deferred merged branches** (attempt the lease-guarded delete; if the permission guard denies, report the tooling constraint — do NOT self-edit the guard). A small G3/G4 witness arc (two fake-runner tests + one materialize-with-auth-check assertion) is a cheap optional insert before (c) if a fresh iteration prefers a code arc; otherwise fold it into the next U-MEM-adjacent arc.

**Round 28 — §4-order grounding sweep CLOSED end-to-end 2026-07-27 at PR #1136; the forward queue is now genuinely slice-exhausted to operator gates.** Per round 27's next-action, three parallel Opus grounding agents re-ground the remaining §4-order candidates. Findings: **R-300-second-provider RESOLVED since 2026-06-03** (PRs #281/#283) — and re-proven live this round: `just mvp-r300-ollama` PASSED (real fallback through the production `RetryBreakerFallbackDispatcher` to `llama3.2:3b`, $0), full file 4-passed/3-credential-skipped; **R-CXA-2 residual DISCHARGED at batch-57** (WAL + reconciler substrates landed and fire at production sites; verified by running the go-live tests, 22 passed) with `Project_Roadmap_v1.md` §5.14 the lone drifted copy (fixed this PR); **R-CXA-3 fully closed** (batch-54, accurate everywhere); **U-MEM Antigravity gate CONFIRMED live** — the recorded NOT CONFIRMED had probed the wrong binary (`antigravity`) while the repo's own preset declares `command="agy"`, installed and passing its declared probe (`agy models` exit 0, 11 models); the B-28-gutted test body was implemented through the PRODUCTION adapter path (`construct_antigravity_cli_adapter` → `_assert_antigravity_authenticated`), preset-sourced so the defect can't recur, PASSED live with a negative control; the orchestrator's own `antigravity:agy` route_ref hypothesis was empirically DISPROVEN by the impl agent (route identity ≠ executable; the resolver raises on it) — a live instance of the STOP-clause discipline working; **Gemini legacy honestly re-dispositioned** (CLI v0.49.0 installed, OAuth session live with refresh token — the prior "not installed" claim was false — but upstream ships no auth-status subcommand, so the gate is open by upstream absence); **generic-command mechanism VERIFIED end-to-end** (`ollama list` candidate bound `generic-command:custom`; server started only for the demo and restored down). A codex-round-1 propagation sweep found the stale "per-role B4 gated to R-300" claim in **4 production docstrings** + 2 doc mirrors (6 live-guidance fixes; ~40 historical hits correctly left); B4's beyond-mvp ledger row flipped BUILD→DONE (per-role model AND prompt dispatch both landed at U-RT-114/B4, verified at `llm_dispatch.py`/`prompt_selection.py`). Review: codex converged round 2; merge-gate 3-lens **all-approve at round 1** (concurrency: AST-verified inert src edits + empirical event-loop control + double CI-hang immunity; spec-conformance: live gate independently re-run PASSED, all ledgers agree, no U-MEM-24 overclaim; test-witness: unauthenticated state empirically forced via fresh HOME proving credential discrimination; 3 LOW gaps recorded in the gate log); 2 stale-comment nits fixed inline. `just check` 6780 passed; main CI green post-merge. **Next action — the no-parking-compliant terminal, reached after honest grounding:** every remaining §4/forward item is now either closed, verified dormant (11 `registered_finding` rows, round 26), `held` (operator answer), or behind a genuine operator gate. **OPERATOR STANDING APPROVAL (2026-07-27, same day, logged before session clear): all five gates below are pre-authorized for AUTONOMOUS execution in the loop — "No more need for HIL gates for these." Do NOT re-ask; execute them as the next loop iterations' work queue** (durable memory: `operator-approval-five-gates-autonomous-2026-07-27`): (a) declare `U_MEM_24_GENERIC_COMMAND_AUTH_PROBE="ollama list"` as the standing generic-command probe and run/record the live gate (start `ollama serve` for the run, restore after); (b) wire a free-tier `gemini -p` probe as the gemini_legacy declared `auth_args` and run/record that live gate; (c) the RC live re-validation batch per the runbook (start Docker Desktop; re-mount/re-provision the R-411 Lima volume; run the paid Phase-C batch, cents class; re-grant GCP `roles/iam.serviceAccountTokenCreator` on the accessor SA; `aws sso login --profile r830` — if the SSO browser round-trip stalls headless, record and continue the non-AWS legs); (d) the paid `just mvp-r300-cross-family` re-confirmation (cents, keys from MAIN `.env` via just); (e) the branch-hygiene prune of the 9 deferred merged branches via the ship-pr lease-guarded recipe — CAVEAT: `permission-guard.sh`'s deny-list mechanically blocks branch deletion regardless of this consent; do NOT self-edit the guard — attempt the delete, and if denied report the tooling constraint for the operator to relax. A future iteration should work through (a)-(e) in order, deferring any leg whose infra genuinely fails, and should NOT re-ground the closed/dormant items — re-ground only if intervening merges touch the relevant surfaces.

**Round 27 — RC deployment-readiness handoff RE-BASELINED 2026-07-27 at PR #1135.** Per round 26's next-action, the §4 priority derivation surfaced `CLAUDE.md` §2.6's standing pointer to `.harness/release-candidate-deployment-readiness-runbook.md` as the highest-value forward item — and the grounding pass (Opus subagent, per the Fable-5-orchestrator discipline) found the arc **already ran end-to-end to a `GO for release candidate` verdict on 2026-06-10** (closure report + Phase-D audit both on disk), with the runbook, §2.6, and the advisory counts all ~47 days stale. The re-baseline PR landed: a NEW Phase-D audit at HEAD (`.harness/overlay-advisory-traceability-audit-2026-07-27.md` — `contract_without_code` 8→0 closed by intervening work; `code_without_cite` 14→2→**0** via two comment-only docstring cite formalizations, `protected_result_store_factory.py`→`U-RT-145` and `audit_offload.py`→`U-RT-145`/`U-RT-141`, both grounded byte-exact against plan/spec text; `substitution_without_carrier` 47→40; NEW `unit_without_code` bucket enumerated — `U-MEM-17` tests-only carrier + `U-RT-00` fixture phantom, both ACCEPTED); the runbook de-staled (GO-closed banner, root commit corrected to `d45ce125`, overlay 389/36 + batch-57 pins, eliminated-dashboard gates removed, Phase-A lines corrected to CI-shaped `uv run` + subshell forms, Phase-C OTLP e2e lines now carry the required `--cloud-run-auth-audience`/`--cloud-run-auth-impersonate-service-account` flags verified in all three scripts' argparse, live-gate states probed and recorded); a REAL deploy-template defect fixed (`harness.selfhosted.local.example.toml` bound `STATE_LEDGER` to the repo's `.harness/state.jsonl` FILE — empirically proven to hard-fail bootstrap stage 1 with `FileExistsError` at `materialize_path_registry` before any chain verification, since STATE_LEDGER resolves as a DIRECTORY with the ledger at `<dir>/state.jsonl`; both rows now bind the gitignored `.harness/r420-scratch/`); and `CLAUDE.md` §2.6 rewritten (arc CLOSED-GO, runbook = re-validation recipe, no standing RC handoff). Review: 6 out-of-family codex rounds (8 real [P2]s — every round found something genuine, none rubber-stamps) + `merge-gate` 3-lens **all-approve at round 1** (concurrency proved the src edits docstring-only at the AST level; spec-conformance verified both cites byte-exact; test-witness ran every newly-documented command green) with 3 non-blocking nits fixed inline. `just check` 6780 passed; CI green at every commit; post-merge main CI green. **Next action:** the live RC re-validation remains operator-gated behind four batched gates recorded in the runbook §4/§5 (Docker daemon up; R-411 Lima volume `/Volumes/Development/arhugula-r411/` re-mounted or re-provisioned; paid Phase-C batch authorization — cents class; GCP `roles/iam.serviceAccountTokenCreator` re-grant + `aws sso login --profile r830`) — surface them to the operator in ONE batch when they next engage; do NOT fire any unilaterally. Otherwise the auto-`ACTIVE` queue is empty, all 11 `registered_finding` rows verified dormant (round 26), R-FS-1 resolved, memory substrate complete, substitutions 54/54: the next iteration should ground the remaining §4-order candidates (`R-300-second-provider`, `R-CXA-2/3` bounded residuals, the U-MEM local live gates pending CLI availability) fresh before picking, or honestly report the slice-exhausted no-parking terminal if grounding confirms every remainder is operator-gated. Branch-hygiene close-outs (now 8 branches incl. `rc-rebaseline-phase-d-reaudit`) stay deferred to an interactive session — deletion is hard-blocked in loop mode.

**Round 26 — 2026-07-27 grounding-pass close-out CLOSED end-to-end at PR #1134; the full `registered_finding` backlog is now freshly grounded.** Per round 25's next-action, this round ran under the operator's new Fable-5-orchestrator discipline (all implementation and review legs delegated to Opus 5 subagents; saved as a durable memory). Five parallel grounding agents re-verified `B-44`/`B-45`/`B-56`/`B-57`/`B-66` against HEAD `25b44127` — ALL FIVE STILL DORMANT with file:line evidence, every row staying `registered_finding`. The close-out PR landed: dated register grounding updates for all five rows; a new mutation-probed negative-path witness for `B-56`'s cross-tenant-relabel raise (`audit_cutover_record.py:201-214`, previously unwitnessed at the record layer); and a comment-only stale-carry fix at `workflow_driver.py` ~6752-6786 (the block still described the pre-B-48 drain-timestamp gap as open + the witness test as xfail — all false at HEAD). Review was genuinely multi-round: `merge-gate` R1 concurrency BLOCKED on a real false-guarantee (the rewritten comment's parenthetical claimed direct audit/cost writes "can no longer invert" — only the drain-vs-drain half was closed by B-48; spec-conformance independently flagged the same clause plus 9 self-inflicted off-by-4 line cites caused by the PR's own +4-line comment growth), fixed and re-gated APPROVE at R2; `just codex-review` took 5 rounds — R2 caught the B-44 close_out carrying two conflicting instructions; R3 caught that the "sentinel adoption PRIMARY" guidance itself was WRONG on two spec grounds (C-IS-07 §7.6 preserves caller-supplied semantics on DIRECT surfaces and gates writer-owned extension behind a separate back-flow that is B-57's scope, and the sentinel resolves through hardcoded `datetime.now(UTC)` so it cannot serve B-44's own deterministic-clock trigger) — the originally-registered clock-threading close-out was restored as operative with the sentinel recorded CONSIDERED-AND-REJECTED (a live instance of `[[over-correction-away-from-mostly-right-baseline]]`: round-0 was right); R4 caught two register-precision defects (the B-57 direct-writer list wrongly included the buffered `:10550` site — all three call sites bind a fresh `BufferingLedgerWriter`; B-56's "byte-unchanged" claim narrowed to semantic tag/entry/hash preservation since the retag re-serializes with default separators); R5 converged clean. CI green at every step (one occurrence of the known `test_write_once_survives_immediate_sweep_under_slow_fsync_and_short_ttl` flake, rerun clean); `just check` 6780 passed. Grounding side-findings recorded: main's per-file git-history floor moved to 2026-07-25 (`d45ce125` whole-tree re-add — memory updated); B-45 is now the codebase's NAMED Windows posture across 6+ surfaces. **Next action:** the auto-`ACTIVE` queue remains empty and the full forward register is now freshly grounded — every `registered_finding` row (11) is verified dormant, `operator_gated`/`held` rows are honored answers. Per §12.4.1 the next session should re-derive from `Project_Roadmap_v1.md` §4's priority order (R-700 close, R-410..R-440, R-300-second-provider, R-CXA-2/3, R-810/820, …) and ground the highest-value forward item fresh — no dormancy re-check of the 11 rows is owed unless intervening merges touch their surfaces. The 6 branch-hygiene close-out items (now 7 with `grounding-pass-b44-b45-b56-b57-b66`) remain deferred: local/remote branch deletion is hard-blocked by the permission guard; an interactive session (or the operator) should run the `[[squash-merge-branch-prune-recipe]]` prune.

**Round 25 — `B-79` impl leg SLICE 2 (LINEAR/`EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF`) CLOSED end-to-end 2026-07-27 at PR #1133 — `B-79` CLOSED, both slices land.** Per round 24's next-action, `advisor()` (consulted before writing any code) confirmed the shared-top-level-field carrier design: ONE new `PauseSnapshot.hitl_gate_config_hash` field (str | None, default None) reused across all three sequential sites — the spec's own §1.3a "reused verbatim" impl-discretion branch — rather than three distinct carriers or fields added to `EvaluatorOptimizerResumeState`/`HandoffResumeState` (which would each need their own new None-strip machinery for byte-compat). Reused slice 1's `_hash_hitl_gate_config` pure function verbatim; renamed the topology-specific recompute helper (`_pre_dispatch_gate_owning_captured_hitl_gate_config_hash` → `_captured_hitl_gate_config_hash`) since its body was always generic. Each site's guard was deliberately placed immediately before its `hitl_delivery_cell` construction, NOT inside the unconditional `_resume_body_mismatch` closure slice 1 used for the fan-out case — a first draft mirrored that placement uncritically and `advisor()` caught a genuine over-rejection risk before merge: an EO/DH resume with no delivery pending this cycle (e.g. a nested child under a fan-out with 2+ unaddressed gate-owning siblings) legitimately reaches `_resume_body_mismatch`, and a FAILED there would wrongly turn a recoverable INERT re-pause into a terminal outcome — caught and fixed BEFORE the first `just codex-review` round even ran (self-caught via `advisor()`, not a review finding). `just codex-review` took 3 rounds to converge: round 1 [P2] the reject-tests only exercised an altered attribute (timeout), not the §1.1(b) removal-symmetry direction — added a placement-removed test per site; round 2 [P2] `api.resume`'s outer bounds check does not cover the internal `ChildWorkflowRunner` recursive-dispatch path — a workflow body shortened between pause and resume raised an uncaught `IndexError` at the LINEAR site instead of a fail-closed `RunResult` (empirically confirmed EO/DH were ALREADY protected by their own pre-existing `_resume_body_mismatch` bounds checks — verified via the same shortened-body repro against both sites, no fix needed there); fixed + regression test added, mutation-probe-verified (reverting the fix reproduces the exact `IndexError`, confirmed); round 3 converged clean ("No actionable defects were found"). `DurablePauseResumeProtocol` (harness-runtime) needed the new kwarg forwarded to its parent — caught by pyright's `reportIncompatibleMethodOverride`, not by codex, a reminder that `just check`'s monorepo-wide pyright pass catches cross-axis-package LSP violations codex's diff-scoped review can miss. `merge-gate` 3-lens: concurrency APPROVE (flagged the EO/DH bounds-check question as "worth a follow-up" — resolved via the same empirical shortened-body repro, confirmed pre-existing bounds checks already cover it, no fix owed); spec-conformance BLOCKED once — a genuine gap, not a false positive: `.harness/forward-register.yaml`'s B-79 row was not updated in this PR, diverging from slice 1's own same-arc precedent (PR #1132 updated it in-PR) — fixed (status → closed, `pr: '#1132, #1133'`, close-out narrative appended, heading synced byte-exact in both the YAML and its `post-phase-8-forward-register.md` prose mirror per `tools/forward_register.py`'s `check_prose_drift`, snapshot pin bumped open 1→0/closed 68→69 + recomputed `identity_digest`), `tools/forward_register.py --check` now clean; test-witness APPROVE (one non-blocking doc nit: the new test file's module docstring overstated that `test_linear_resume_changed_config_with_no_delivery_pending_does_not_fail` witnesses the same "misplaced inside `_resume_body_mismatch`" mistake as its EO/DH siblings — LINEAR never had such a closure to misplace the guard into, so its witness is real but narrower; fixed inline, no re-gate needed per the merge-gate skill's own narrow-fix allowance). 17 new/updated tests (accept-unchanged / reject-changed / reject-removed / skip-legacy-None / no-delivery-pending-does-not-fail per site, plus the shortened-body bounds-check regression + a `_compute_snapshot_hash` byte-compat check), mutation-probe-verified throughout. Full `harness-cp` suite: 1837 passed; monorepo-wide `just check`: 6775 passed, 0 failed; ruff + pyright strict clean across `harness-cp` + `harness-runtime`. **Next action:** per `CLAUDE.md` §12.4.1, the remaining ungrounded forward-register candidates from round 18 (`B-44`/`B-45`/`B-56`/`B-57`/`B-66`) still have not been re-ground this session and should not be assumed still-dormant without a fresh check — a fresh session should run the full §12.1 roadmap audit, then ground (not just re-read) each `registered_finding` row against current code/spec state before picking the next one to build, rather than assuming this arc's own priority ordering still holds. The 6 branch-hygiene close-out items flagged at the start of this session (`b81-ow-material-diff-test-parity-step1`, `b78-eo-dh-hitl-delivery-cell-impl-leg`, `b72-impl-leg-pre-dispatch-gate-owning-carrier`, +3 more — all already-merged PRs' local branches) remain undeleted: local `git branch -D` is hard-blocked by the permission guard regardless of consent (`[[branch-deletion-hard-blocked-by-permission-guard]]`); a name-matched-vs-`gh pr list --state merged` safe-delete candidate list (113 branches) was built at `/tmp/safe_delete.txt` earlier this session but not handed off as a final artifact — a fresh session (or the operator directly) should regenerate it via the `[[squash-merge-branch-prune-recipe]]` and run the prune manually.

**Round 24 — `B-79` impl leg SLICE 1 (fan-out) CLOSED end-to-end 2026-07-27 at PR #1132.** Per round 23's next-action, `advisor()` (consulted before writing any code) recommended splitting property 7 into two slices rather than one bundled PR: slice 1 extends the already-shipped `PreDispatchGateOwningBranchResumeState` (fan-out closure, mirrors the `v1.108` `step_kind`/`child_workflow_id` precedent) while slice 2 (a net-new carrier at the LINEAR/`EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` sites, which have no per-step identity carrier of any kind today) is a strictly bigger, separately-scoped task — landing slice 1 first proves the pattern before slice 2's higher-blast-radius net-new-carrier design. Built `hitl_gate_config_hash` — a sha256 hex digest over the ADD-only-folded applicable `HITLPlacement` tuple (`fold_step_hitl_placements`) plus the per-step `removed_placements` directive — wired at both `_execute_parallelization` and `_execute_orchestrator_workers`'s `_resume_body_mismatch` closures (the latter covers `HIERARCHICAL_DELEGATION` via its recursive reuse). `just codex-review` needed 5 rounds: round 1 found a real [P1] — the new field was REQUIRED (no default), which broke `JournalWorkflowPauseStore._parse_snapshot`'s deserialization of a real pre-existing durable journal snapshot captured by the preceding `B-72`-only deployment (the field is being added to an ALREADY-SHIPPED carrier row type, unlike `child_workflow_id`/`step_kind`, which shipped alongside the type itself — a byte-compat distinction this round's own reasoning initially missed) — fixed via default-`None` + a new `_strip_default_fanout_resume_fields` per-row strip + a resume-side skip-when-`None` guard, mirroring `child_workflow_id`'s own "validate ONLY when present" convention; round 2 converged clean. `merge-gate` 3-lens: concurrency + spec-conformance APPROVE at round 1 (spec-conformance flagged one non-blocking wording nitpick, fixed); test-witness BLOCKED at round 1 on 2 genuine mutation-probe gaps (the per-step `StepOverride.hitl_placement` ADD-fold contribution was never exercised end-to-end, only at the pure-function level; the OW site's own legacy-skip guard — a separately-duplicated check — had no dedicated witness) — both fixed with 2 new mutation-probe-verified tests, re-verified APPROVE at round 2 (the cap). A separate out-of-family codex round 4 (after the merge-gate fixes) caught 2 more real issues in the same pass: a stale `ruff format` miss in a sibling test file from an earlier edit, and — independently confirming what the merge-gate round 2 also found — the OW site's missing placement-removed witness; both fixed, round 5 converged clean ("No actionable correctness defects were identified"). Full `harness-cp` suite green (1824 passed, 4 skipped); ruff + pyright strict clean. `B-79` stays `open` (slice 2 remains); forward-register progress note + `tools/forward_register.py --check` clean. **Next action:** `B-79` slice 2 (the LINEAR `resume_at` + `EVALUATOR_OPTIMIZER` + `DECENTRALIZED_HANDOFF` net-new carrier per §1.1(d)) is the natural next candidate — re-locate the current-HEAD line numbers for those 3 sites first (they will have shifted after both this round's and round 23's parameter-threading edits), then design the carrier (shared vs per-site, at impl discretion per §1.3a) before writing any code. The remaining ungrounded forward-register candidates from round 18 (`B-44`/`B-45`/`B-56`/`B-57`/`B-66`) have not been re-ground this session.

**Round 23 — `B-80` impl leg CLOSED end-to-end 2026-07-27 at PR #1131.** Per round 22's next-action, opened the B-79/B-80 impl leg; `advisor()`, consulted first, recommended splitting into two follow-on arcs (property 8 first — mechanical, low blast radius — then property 7 — carrier + hashing, higher blast radius) rather than one bundled PR, both landing on the same code region (`workflow_driver.py:8036-8045`/`12279-12289`), with 8 first so 7's diff lands on a settled guard; also flagged a real line-range discrepancy in the spec's own LINEAR-site citation to resolve by direct read before writing any property-7 code (not yet done this round — property 7/`B-79` remains open). Built `compute_effect_fence_tree_wide_abort_present` — the THIRD sibling of `compute_hitl_uniform_fallback_eligible_run_id`/`compute_effect_fence_uniform_fallback_eligible_key`, computed once at the true depth-0 root (`mcp_server.py`) and threaded through the identical CP↔Runtime `ChildWorkflowRunner` crossing its two siblings already use, ORed into `_any_fence_abort` at both fan-out sites. `just codex-review` round 1 found 2 real [P2]s (empty-`idempotency_key` candidates could spuriously activate the tree-wide signal since the real per-branch consult sites explicitly guard on a truthy key; the runtime mock recorded the new forwarded kwarg without any test asserting it), both fixed and mutation-probe-verified; round 2 converged clean. `merge-gate` 3-lens: concurrency + test-witness APPROVE at round 1; spec-conformance BLOCKED once on a genuine but already-self-caught-and-fixed gap (the `.harness/forward-register.yaml` B-80 closure existed only as uncommitted local state at the time that lens ran, not yet in the PR diff) — fixed (committed + pushed before the block was even reported) and re-verified APPROVE at round 2. Full `harness-cp` (1804 passed) + `harness-runtime` (3089 passed, 10 skipped) suites green; ruff + pyright strict clean. `B-80` row closed same-PR (`pr: '#1131'`), `tools/forward_register.py --check` clean. **Next action:** `B-79`'s impl leg (property 7 — the hashing scheme, the new carrier field on `PreDispatchGateOwningBranchResumeState` + a net-new LINEAR-site carrier, the comparison wiring at all 4 sites) is the natural next candidate, now that property 8 has proven the pattern end-to-end — but MUST first resolve the LINEAR-site line-range discrepancy `advisor()` flagged this round (the change-note cites `~4748-4777`, §1.1(d) cites the EO comment's `4830-4859`; direct read this round already resolved it in passing — the real site is the `hitl_delivery_cell = HITLDeliveryCell(_resolved_hitl)` construction at current-HEAD lines ~4843-4872, confirmed matching the EO citation, not the effect_fence_directive block at ~4798-4810 the change-note's own range actually points at) before writing any property-7 code. The remaining ungrounded forward-register candidates from round 18 (`B-44`/`B-45`/`B-56`/`B-57`/`B-66`) have not been re-ground this session.

**Round 22 — CP spec v1.111 prose-only correction 2026-07-27 at PR #1130, before opening the B-79/B-80 impl leg.** Per round 21's next-action, the next iteration proposed opening the B-79/B-80 impl leg directly. `advisor()`, consulted first, redirected: PR #1129's 6-round review had converged on individual claim-correctness at each round but the merged document had never been checked as a single coherent artifact read fresh end-to-end — and flagged 3 specific consistency risks to verify before building against it. A fresh full re-read found one real defect: §1.1(d) asserted "ALL FOUR of LINEAR/EO/DH" while naming only three sites (a miscount — the very next sentence in the same paragraph already correctly says "all three"). The other two flagged risks were checked and confirmed NOT defects: the Revision-shape line's correction-narrative placement matches the `v1.106` delta's own precedent; a possible `PreDispatchGateOwningBranchResumeState`/`PausedChildBranchResumeState` naming-confusion is resolved by the surrounding text's explicit disambiguation. Filed v1.111 (prose-only, mirrors the v1.109/v1.100 precedent) fixing only the miscount — no contract, carrier, or scope change. `just codex-review-uncommitted` was run first but reviewed the WRONG diff (the correction was already committed, so "uncommitted" only saw pre-existing untracked clutter from other sessions) — caught and corrected by re-running `just codex-review` (branch vs main), which converged clean: "the patch consistently corrects the sequential-site count from four to three, updates the canonical pointers, and adds the expected clearance record." CI's one `pytest (all axis packages)` failure (`test_slow_post_commit_tail_does_not_falsely_expire_a_successful_write`) was confirmed a pre-existing flake, not a regression — the identical suite passed in the parallel `coverage` job at the same commit, the diff touches zero Python code, and a rerun of the failed job passed clean. **Next action:** the B-79/B-80 impl leg (the hashing scheme, the new carrier fields at 4 sites, the comparison wiring, the witness tests) is the next candidate, now grounded against a verified-consistent spec. The remaining ungrounded forward-register candidates from round 18 (`B-44`/`B-45`/`B-56`/`B-57`/`B-66`) were already re-verified as still accurately dormant at round 18.

**Round 21 — co-designed `B-79`/`B-80` CP spec leg LANDED 2026-07-27 at PR #1129 (CP spec v1.110 + plan v2.46).** Per round 20's next-action, drafted the CP spec delta both council convenings recommended — property 7 (closes `B-79`, HITL-placement material-diff identity) and property 8 (closes `B-80`, tree-wide effect-fence-abort suppression + property-6 precedence). Every load-bearing call-graph claim from both convenings was independently re-verified against current HEAD before drafting, per this session's own earlier `B-71` lesson (a spec delta built on an unverified premise required a full 5-round revert). Despite that discipline, **6 rounds of out-of-family `just codex-review-uncommitted` still caught 6 real defects before merge**: round 1 (a mis-scoped carrier — `PausedChildBranchResumeState` never actually constructs a `HITLDeliveryCell`, confirmed by direct code read, so authorizing a hash there would reject valid resumes without closing any gap; a false claim that rejection emits a C-CP-20 audit-ledger entry; a false "zero cross-axis cascade" claim for property 8's Runtime-crossing parameter); round 2 (property 7's scope had ENTIRELY OMITTED the `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` delivery-cell sites — landed later by the `B-78` arc, both confirmed by their own inline comments to mirror LINEAR's mechanism exactly and share the identical gap); rounds 3-5 (consistency-drift — fixes applied to the spec file weren't consistently propagated to the plan file's own coverage row, the spec's own header, the companion `.md` prose mirror, and both clearance markers; one self-inflicted citation error was caught and fixed when correcting the plan clearance). Round 6 converged clean. This validates the workspace's own multi-round review discipline — every round found something genuinely real, none were rubber-stamps, and the discipline caught defects that easily could have shipped as canonical spec text otherwise. Both rows flip `registered_finding` → `open`. **Next action:** the impl leg (the hashing scheme, the new carrier fields at 4 sites, the comparison wiring, the witness tests) is a separate follow-on arc per the B-33/B-39/B-59/B-70/B-72 precedent — not yet opened. Otherwise, the remaining ungrounded forward-register candidates from round 18 (`B-44`/`B-45`/`B-56`/`B-57`/`B-66`) were already re-verified as still accurately dormant at round 18; the full backlog is grounded.

**Round 20 — `B-80` council convening 2026-07-27 at PR #1128; mechanical fix identified, Class 1, spec-leg owed.** `B-80`'s companion convening (C1 + C10 co-primary) completed. Probe-first check confirmed `HIERARCHICAL_DELEGATION`'s recursion crosses the CP→Runtime→CP boundary via a FRESH `execute_workflow()` call at every level (not same-stack-frame Python recursion), and found TWO existing sibling values (`hitl_uniform_fallback_eligible_run_id` from B-39, `effect_fence_uniform_fallback_eligible_key` from B-70) already thread a root-computed signal through that exact channel — the identical problem shape B-70 already solved. Recommendation: a THIRD sibling value, computed once at depth-0 via existing tree-walk utilities, `OR`ed into the level-local `_any_fence_abort` at both consume sites — no new cross-axis seam, no new carrier field. A TENSION (blanket tree-wide boolean vs. a precise `cascade_policy`-chain walk) was probe-resolved for the blanket reading: the already-shipped local rule is itself `cascade_policy`-blind, and `PauseSnapshot` carries zero `cascade_policy` field anywhere in its schema, so the precise reading would need a new durable carrier field — a strictly bigger, separately-scoped task. Genuinely new finding: the fix surfaces an undocumented interaction between CP spec §1.1's abort-suppression rule and §1.2 property 6's mandatory-delivery obligation for the sole-unaddressed pre-dispatch gate-owning branch, traced empirically to NOT reintroduce a livelock but which the spec text doesn't currently reconcile for the tree-wide case. Classified **Class 1** — mirrors the B-70/B-72 spec-leg precedent exactly. No spec/code change landed; doc-only, per PR #1128. **Next action:** both `B-79` and `B-80` are now fully grounded and Class-1-classified, each with a clear, mechanical, well-precedented fix recommendation and an identified spec-prose gap. The next iteration should open a co-designed CP spec-leg covering both (they touch the same `_resume_body_mismatch`/pre-dispatch-carrier family) — as a FRESH, focused iteration, not squeezed into an already-long session, per this session's own earlier `B-71` lesson (a 5-round non-convergent fix built on an unverified premise) about giving spec-leg drafting undivided attention and re-verifying every call-graph claim empirically before writing spec text, not just trusting a council's already-strong grounding at face value.

**Round 19 — `B-79` council convening 2026-07-27 at PR #1127; falsified premise, Class 1, spec-leg owed.** Per round 18's next-action, `B-79`/`B-80` were dispatched as background dyadic `council-orchestrator` convenings rather than resolved unilaterally, since both are genuinely council-eligible design decisions touching a claimed cross-axis seam. `B-79`'s convening (C10 + C1 co-primary) ran the orchestrator's mandatory probe-first check before any deliberation, and the probe **falsified the row's own registered premise**: Runtime-owned persona/engine-class placement-resolution logic is NOT opaque to `harness-cp` for the scenario in scope — `step_context.hitl_placements` and `removed_placements` are both CP-native folds already available at pause-capture time; the composer only consumes them. **No new cross-axis seam is needed.** A TENSION (narrow `.position`-only hash vs. broader full-`HITLPlacement`-tuple hash) was probe-resolved when re-checking found `HITLPlacement` is itself a 4-field frozen model already inside the same tuple — both voices converged on the broader hash at zero extra cost. Recommendation: persist a content hash over `(HITLPlacement tuple, removed_placements)` on the existing pause-capture carriers, mirroring the B-31/B-72 material-diff-identity precedent exactly; a companion fix is owed at `hitl_gate_composer.py`'s Step 0 short-circuit, which never gates delivery on placement drift today. Classified **Class 1** — every prior addition to these carriers went through a CP spec-leg amendment before impl, and this is architecturally identical in kind; a spec leg is owed before any code lands, not yet opened this pass (doc-only landing). `B-80`'s companion convening (C1 + C10) was still running in the background at merge time — its outcome + terminating refresh follow in a subsequent round. **Next action:** process `B-80`'s convening result when it lands, then decide whether to open the `B-79`/`B-80` CP spec-leg(s) as a follow-on arc (likely co-designable, both touch the same `_resume_body_mismatch`/pre-dispatch-carrier family).

**Round 18 — `B-16`/`B-30` ground 2026-07-27; `B-30` doc-only update at PR #1126.** Per round 17's next-action, the remaining candidates were ground in order. `B-16` is a deliberately-unpadded speculative bucket with zero concrete items — nothing to build, confirmed by direct read, no register change owed. `B-30`'s residual close-out step (3) — "verify whether a within-invocation retry can double-append to the run-scoped cost accumulator" — was dispatched to an `Explore` agent, which traced the call graph and confirmed the double-append IS reachable for the tool composer (`retry_breaker_tool.py`'s 2 retried exception classes each hit a failure-path `attribute_tool_dispatch_cost` call before re-raising, then the eventual success re-appends; confirmed not deduped downstream by direct read of `api.py`'s `_rollup_cost_attribution`, an unconditional sum with no dedup logic). The agent's own framing of this as a "LIVE bug" was NOT adopted — `advisor()` caught it as a presence-not-correctness finding before any fix was drafted: the tool composer's failure-path attribution is symmetric across all 3 exception classes including `ToolInvocationProtocolError` (NOT even retried), the tell that the design axis is "every physical MCP attempt," matching `U-OD-39 AC #1`'s explicit requirement and this same row's already-confirmed-deliberate webhook fresh-`uuid4`-per-attempt precedent exactly — applying a different disposition to tool than webhook without a spec change would itself be the inconsistency `[[forward-register-consistent-dismissal-criterion]]` warns against. No fix landed. One genuinely new residual surfaced and registered (not answered unilaterally): `U-OD-39`'s AC #1 ("every dispatch, success + failure") and AC #5 ("1 tool call → 1 cost-record") use different granularity vocabulary that neither the OD spec nor plan disambiguates for the retry-then-succeed case — an OD-spec-owned semantic question. `B-30` stays `registered_finding`. Full detail at `.harness/post-phase-8-forward-register.md` B-30's 2026-07-27 update. **Next action:** the remaining ungrounded `registered_finding` candidates are `B-44`/`B-45`/`B-56`/`B-57`/`B-66`/`B-79`/`B-80` — none have been re-ground this session and should not be assumed still-dormant without a fresh check per `CLAUDE.md` §12.4.1.

**Round 17 — `B-71` doc-only grounding pass 2026-07-27 at PR #1125; drafted fix reverted, not landed.** Per round 16's next-action, `B-71` (peer-branch HITL escalation collision) was ground next. A `run_id`-threading fix was drafted across CP + Runtime spec/plan deltas (`StepExecutionContext`/`HITLEscalationBrief` gain `run_id`; webhook idempotency-key + payload incorporate it) and taken through 5 rounds of out-of-family `just codex-review-uncommitted`, each catching a genuine defect (Optional-vs-required breakage against a 6th out-of-driver constructor; a missing `None`-guard that would silently recreate the exact collision; a new exception not classified terminal by the driver's fail-class discriminator nor added to the retry/breaker allowlist; wrong unit ownership). **Round 5 found the fix's premise itself false**, `advisor()`-confirmed against the full transcript: `RuntimeHITLGateComposer` escalates inside the PARENT's dispatch of a `SUB_AGENT_DISPATCH` step, before any child `run_id` is minted — `execute_workflow`'s `run_id` is per-parent-run, not per-branch, so every peer branch of one fan-out shares the identical value; a `...:{run_id}` key does not disambiguate B-71's actual collision. `advisor()` directed a full revert rather than a sixth patch — 5 consecutive rounds each fixing a different layer of the same wrong approach is the non-convergence signal, not an invitation to keep iterating (matches `[[reviewer-oscillation-register-and-hold]]`). All drafted spec/plan artifacts and 4 clearance markers were reverted (never committed, so a clean no-op on design-substrate); PR #1125 landed only the register rewrite — `B-71` stays `registered_finding`, with the falsified premise and the real dependency on `B-72` CP spec v1.108 property 6's branch-distinct internal-identity mechanism now documented (council flips from "TBD, likely narrow" to genuinely open, co-designed with `B-72`). Full detail at `.harness/post-phase-8-forward-register.md` B-71's 2026-07-27 update. **Next action:** the remaining ungrounded `registered_finding` candidates are `B-16`/`B-30`/`B-44`/`B-45`/`B-56`/`B-57`/`B-66`/`B-79`/`B-80` (`B-71` is now grounded-but-open, gated on a `B-72`-co-designed council pass, not a mechanical build) — none of the other 9 have been re-ground this session and should not be assumed still-dormant without a fresh check per `CLAUDE.md` §12.4.1.

**Round 16 — `B-77` NARROWED (not closed) 2026-07-27 at PR #1123; standalone CI-hygiene fix at PR #1124.** Per round 15's next-action, the remaining `B-74`/`B-77` pair was ground next: `advisor()` was consulted before any edit, per the workspace's own `[[forward-register-consistent-dismissal-criterion]]` memory (apply the SAME dormancy test consistently, don't re-litigate a declined disposition narrower). Three self-caught wrong turns preceded the landed fix, each caught by out-of-family Codex or advisor() before merge — recorded here in full rather than smoothed over, per the workspace's stale-carry-text discipline: **Round 1** tried to classify B-74/B-77 as dormant via reachability arguments (a "sub-millisecond crash window" claim for B-77; a "no realistic operator would configure this" claim for B-74) — Codex caught both as resting on speculation rather than the code-level unreachability fact B-44/B-45's own precedent actually requires. **Round 2** moved B-77's sole mtime refresh from post-commit to pre-commit and claimed this "closes the crash window structurally" — Codex round 9 [P1] reproduced a DIFFERENT, normal-path regression against `main`: with the stamp now running before the commit tail instead of after, a successful uninterrupted write's own entry could be immediately reclaimed under a short TTL + a slow tail, something `main` never exhibited. **Round 3 (landed)**, advisor()-directed: `_publish_atomic` now stamps mtime TWICE — once pre-commit (temp file, crash recovery) and once post-commit (entry, restoring `main`'s exact original normal-path behavior) — a strict improvement over `main` on both axes, no reopened ground. `B-77` stays `registered_finding`, narrowed twice, with the residual (the `os.link`-to-post-commit-refresh window, same shape as originally named but starting from a fresher baseline) named precisely and a grace-period close_out recorded. `B-74`'s own concern (filesystem storage-resolution rounding) was never about ordering and stays fully in scope, untouched by this fix. Two mutation-probed regression tests added (crash-recovery path; Codex round 9's normal-path regression); merge-gate's 3-lens review approved round 2's diff, with only the concurrency lens re-run for round 3 per advisor()-directed scope (the added durability step is inside the same lock its prior APPROVE already traced) — full history at `.harness/merge-gate-log.md`. Separately, CI on `main` itself was found red (commit `ce710b42`) from a pre-existing `roadmap_status.md` table-cap violation (6 rows, cap 5) accumulated across the B-76/B-75 arcs — fixed as its own standalone terminating-refresh PR (#1124) before PR #1123 could merge cleanly, per the workspace's own §12.2.1 discipline (a substantive PR should never be bundled with a pure roadmap-refresh commit). Full `harness-runtime` suite: 54 passed (`test_lifecycle_protected_result_store.py`); ruff + pyright strict clean on both touched files. Next action: the remaining ungrounded `registered_finding` candidates are `B-16`/`B-30`/`B-44`/`B-45`/`B-56`/`B-57`/`B-66`/`B-69`/`B-71`/`B-79`/`B-80` (`B-44`/`B-45` were consulted this round as the dormancy-test PRECEDENT, not re-ground themselves) — none have been re-ground this session and should not be assumed still-dormant without a fresh check per `CLAUDE.md` §12.4.1.

**Round 15 — `B-75` CLOSED 2026-07-26 at PR #1122.** Per round 14's next-action, `advisor()` was consulted again on which of the remaining same-file candidates to build: recommended `B-75` over the still-open `B-74`/`B-77` pair — its close_out already named a known fix shape (batch-with-yield or snapshot-then-verify, no open design question), it's the P1, and `B-73` had escalated it to cross-process blast radius (candidate selection also held the cross-process flock, so a stalled scan could block other OS processes, not just threads). `gc_sweep`'s `glob()`/`stat()` candidate enumeration over `*.entry`/`.tmp-*` now runs FULLY UNLOCKED — the part whose cost scales with total store size (or, on a hanging filesystem mount, has no bound at all), and the actual hazard the row registered — producing PROVISIONAL candidates that are re-verified in a single, brief, COMBINED lock acquisition before being trusted (never one acquisition per candidate, which would have turned `_cross_process_lock`'s per-call `fcntl.flock` into N kernel round-trips). Before writing code, advisor() flagged an asymmetry to resolve first: does the fix cover `.tmp-*` too, or only `*.entry`? Worked through it by hand: a `.tmp-*` file's mtime is NOT a trustworthy "definitely done" signal unlocked (it could be mid-write) the way an entry's is (monotonic expiry + fresh uuid4 keys) — but since `tempfile.mkstemp` itself only ever runs INSIDE the lock, a temp-file candidate that survives to be re-observed once the sweep holds the lock can only be a genuine crash orphan (its creator either already finished — and already unlinked it as part of releasing the lock — or crashed while holding a lock that's now free). This closed the asymmetry: the same unlocked-scan-then-brief-relock-reverify pattern is correct and safe for BOTH file types, so the fix isn't scoped to `*.entry` only. Two new regression tests: a structural witness recording whether `_publish_lock.locked()` is true at each `glob()` call (asserts unheld, not a wall-clock check), and a stalled-enumeration concurrent-write test mirroring the existing stalled-decrypt pattern. Both mutation-probe-verified (revert the fix to the pre-fix fully-locked shape, confirm both new tests fail; restore, confirm green). Hand-traced the fix against the two most load-bearing pre-existing lock tests before writing any code, to confirm neither would regress — both passed unchanged, confirmed empirically after the fix landed. Full `harness-runtime` suite: 3087 passed, 10 skipped; ruff + pyright strict clean. `just codex-review-uncommitted`'s only findings were pre-existing untracked workspace clutter from other sessions (unrelated skill installs, a stale retired dashboard snapshot), same pattern as PR #1121 — zero findings on the actual diff. `merge-gate` 3-lens review: **all-approve at round 1** (concurrency independently re-derived both safety arguments from the actual code and confirmed them; spec-conformance independently re-ran `tools/forward_register.py --check` and the mutation probe against the real PR HEAD commit; test-witness independently RAN both new tests directly rather than reasoning-only, confirmed 0.39s combined with no timing marginality, and hand-mutation-probed both). **Next action:** 11 `registered_finding` rows remain ungrounded (`B-16`, `B-30`, `B-44`, `B-45`, `B-56`, `B-57`, `B-66`, `B-69`, `B-71`, `B-74`/`B-77` as a likely-combined pair, `B-79`/`B-80`). Per `CLAUDE.md` §12.4.1, a fresh session should ground the remainder before picking the next one to build — `B-74`/`B-77` is a genuine design decision (a filesystem-resolution TTL floor "not knowable a priori without probing the actual deployment volume" per B-74's own self-contradicting `council: no` line, flagged by `advisor()` at the B-75 arc as needing a spec-shaped choice, not Phase-7 impl discretion — likely warrants a Class 2 fork or at minimum an explicit design decision before building); `B-79`/`B-80` remain explicitly council-flagged design tasks; the other 9 rows (`B-16`/`B-30`/`B-44`/`B-45`/`B-56`/`B-57`/`B-66`/`B-69`/`B-71`) have not been re-grounded this session and should not be assumed still-dormant without a fresh check.

**Round 14 — `B-76` CLOSED 2026-07-26 at PR #1121.** Per round 13's next-action (ground `B-74`/`B-75`/`B-76`/`B-77` — the same-file, `council: no` continuous candidates split from `B-68`), `advisor()` was consulted on which of the 4 to build: recommended `B-76` as the cleanest — a genuinely mechanical propagate-vs-fallback fix, no interaction with `B-73`'s new cross-process flock, no reopening of the phase-1/phase-2 GC-sweep safety argument. Removed `_lock_for_root`'s `except OSError: key = str(resolved)` fallback (the unconditional `mkdir` round 6 of the B-68 arc added already makes the not-yet-created-directory case unreachable, so the fallback only ever caught a transient I/O error — and silently switching key namespaces there let two near-simultaneous callers key DIFFERENT locks for the SAME logical root); the `OSError` now propagates, and all 3 direct/indirect callers were verified by direct grep to already tolerate it (`write_once` folds it to `UnresolvableResultRef`; bootstrap + shutdown-step-5b `gc_sweep()` callers each wrap it in `except Exception`; `_maybe_opportunistic_gc_sweep` does too). New regression test uses a frame-targeted `Path.stat` monkeypatch (only intercepts calls whose immediate caller is `_lock_for_root` itself, not pathlib's own internal `stat()` calls inside `resolve()`/`mkdir()` — a naive global intercept was tried first and failed for exactly this reason, discovered empirically before the fix was even a candidate for a false pass). Mutation-probe-verified (revert, confirm `DID NOT RAISE`; restore, confirm green). Full `harness-runtime` suite: 3085 passed, 10 skipped; ruff + pyright strict clean. `just codex-review-uncommitted`'s only diff-scoped finding was "close B-76 in the forward register" (its other findings were pre-existing untracked workspace clutter from other sessions — `ui-ux-pro-max`/`impeccable` skill installs, a stale retired dashboard snapshot — out of scope for this PR). First attempt to close the register used a full `yaml.dump()` re-serialization that rewrote all 2232 lines of `forward-register.yaml`; caught before committing and reverted in favor of a surgical text-splice (22 insertions/30 deletions) — logged as a reusable caution for any future register edit. `merge-gate` 3-lens review: **all-approve at round 1** (concurrency: the removal closes a real aliasing race, introduces none; spec-conformance: register internally consistent, `tools/forward_register.py --check` clean, X-AL-3-clean; test-witness: the property-access test exercises the real call graph, mutation probe confirmed genuine, the frame-check degrades to an honest failure rather than silent vacuity on a hypothetical rename). Also recorded two `advisor()` cross-observations at the still-open `B-74`/`B-75`/`B-77` rows: `B-73`'s cross-process flock escalated `B-75` (candidate-selection now blocks other OS PROCESSES, not just threads — worse than its original P1 framing) and `B-74`/`B-77` are likely ONE finding sharing an mtime-as-untrustworthy-age-authority root cause. **Next action:** 13 `registered_finding` rows remain ungrounded (`B-16`, `B-30`, `B-44`, `B-45`, `B-56`, `B-57`, `B-66`, `B-69`, `B-71`, `B-74`/`B-77` as a likely-combined pair, `B-75`, `B-79`, `B-80`). Per `CLAUDE.md` §12.4.1, a fresh session should ground the remainder before picking the next one to build — `B-74`/`B-77` (now flagged as one finding) is the most naturally-continuous next candidate in the same file, while `B-75` is P1-and-escalated and may warrant its own arc regardless of grounding order; `B-79`/`B-80` remain explicitly council-flagged design tasks.

**Round 13 — `B-73` CLOSED 2026-07-26 at PR #1120.** Per the round-12 next-action (ground the 16 `registered_finding` rows fresh before picking), this session's grounding pass found `B-73` — cross-process GC/publish synchronization gap in the protected result store, split from `B-68` — the cleanest immediately-buildable slice: "council: no ... a known primitive (fcntl.flock)". Landed `_cross_process_lock()` (an `fcntl.flock` on a dedicated lockfile, composed inside the existing in-process `threading.Lock` at both `_publish_atomic` and `gc_sweep`'s candidate-selection phase, single fixed nesting order). **4 rounds of out-of-family `just codex-review main` to clean convergence:** round 1 (unguarded top-level `import fcntl` broke the declared Windows target since `harness_runtime.types` imports the store unconditionally, plus a write-once regression test that didn't isolate `_publish_atomic`'s own lock use — a fresh store's opportunistic pre-write GC sweep already acquires the same lock); round 2 (the new tests would spuriously fail on Windows, where the lock intentionally no-ops, plus a readiness-marker scheduling window); round 3 (the same scheduling-window concern persisted at a tighter placement — fixed by wrapping `fcntl.flock` itself in each child script so the marker signals at the actual syscall attempt); round 4 clean. Each fix mutation-probe-verified (revert, confirm the regression test fails; restore, confirm green). **`merge-gate` 3-lens review:** concurrency + test-witness APPROVE at round 1; spec-conformance BLOCKED once (`.harness/forward-register.yaml`'s B-73 row was not updated in the same PR, diverging from the B-68/PR #1103 precedent) — fixed by a follow-on commit flipping the row to `closed` + `pr: "#1120"` + rewritten summary/close_out + snapshot bump, `tools/forward_register.py --check` verified passing; all-approve after 1 fix round. Also this session: cleaned up 4 stale merged remote branches (root cause was a repo-name mismatch — `origin` resolves to `arhugula-harness`, not `arhugula` — that had been causing prior branch-deletion attempts to silently target the wrong repo); ran a dormancy re-check on the other 9 previously-grounded `registered_finding` rows (`B-16`/`B-30`/`B-44`/`B-45`/`B-56`/`B-57`/`B-66`/`B-69`/`B-71`) — 8 confirmed STILL DORMANT, `B-71` flagged POSSIBLY-LIVE-BY-REFERENCE-ONLY (the `B-72` arc's round 5/6/7 grounding now names it as a concrete blocking dependency for `B-72`'s still-open item (2), though no code/spec change has landed on `B-71` itself — worth a fresh session checking whether `B-72` item (2) is about to open before assuming `B-71` stays dormant). **Next action:** 14 `registered_finding` rows remain ungrounded-this-session (`B-16`, `B-30`, `B-44`, `B-45`, `B-56`, `B-57`, `B-66`, `B-69`, `B-71`, `B-74`, `B-75`, `B-76`, `B-77`, `B-79`, `B-80` — `B-74`/`B-75`/`B-76`/`B-77` were split from `B-68` at the same close as `B-73` but never independently grounded/prioritized this session beyond confirming they're still open in the current codebase). Per `CLAUDE.md` §12.4.1, a fresh session should ground the remainder (not just re-read close_out text) before picking the next one to build — `B-74`/`B-75`/`B-76`/`B-77` are the most naturally-continuous next candidates (same file, same `council: no` disposition, i.e. Claude-buildable without a spec leg), while `B-79`/`B-80` are explicitly council-flagged design tasks (Runtime-owned HITL-placement resolution; cross-recursion-boundary abort propagation) that likely warrant a dyadic council pass or at minimum probing per `[[probe-resolves-fork-prescribed-council]]` before committing to a design.

**Frontier:** `B-72` reproduction BUILT 2026-07-25 at PR #1107 — a real-stack test (`harness-runtime/tests/integration/test_b72_fanout_sub_agent_dispatch_hitl_gate_resume.py`) empirically confirms the fan-out HITL delivery-mechanism gap the register's round-2 spec-citation reframe predicted: a `PARALLELIZATION` branch's own `SUB_AGENT_BOUNDARY` gate correctly pauses on first dispatch, but a resolved operator answer delivered via `api.resume()` is never consumed — the gate re-fires (2nd webhook POST) instead of dispatching the child, and the run re-pauses cleanly with no state corruption but never converges. Pinned with 6 oracles including a mutation-probed direct spy on `RuntimeSubAgentDispatcher.dispatch` (0 calls before resume, 1 when the gate is bypassed — confirmed non-vacuous), not a blanket `xfail` or a weak Anthropic-client-call-count proxy (both tried and rejected across 5 codex rounds). Merge-gate 3-lens all-approve at round 1. A genuine merge conflict against the concurrently-merging B-68 PR's own terminating refresh was resolved cleanly (kept this PR's round-3 B-72 text, preserved B-68's new B-73-B-77 rows). **A separate, still-open finding**: this single-branch reproduction scopes the liveness gap but not the multi-branch addressing mechanism (the gate fires before any child run exists to key `hitl_response_for(child_run_id)` by) — needs either a multi-peer-branch reproduction or grounding on branch-own pre-dispatch identity before the fix is genuinely scoped. **Round 4 grounding (2026-07-25, doc-only, advisor()-checked twice):** re-confirmed `hitl_delivery_holder`/`hitl_delivery_cell` stay confined to `_execute_workflow_body`; `_execute_parallelization`/`_execute_orchestrator_workers` reference neither (matches round 2). The new finding: the register's own round-1 text ("dispatch entrypoint can't distinguish first-dispatch from forwarding an already-dispatched branch") and round-2 text ("a resolved answer has no delivery path") describe two DIFFERENT bug categories — dispatch-idempotency (no key needed) vs. addressing/delivery (needs a per-branch key) — and nobody had named that split. A mid-pass hypothesis floating `branch_path` as that key was `advisor()`-rejected before write-up: the single-branch repro tests zero multi-branch addressing, so "collision only hits nested grandchildren, not peers" is unverified — and it's exactly the reading the code's own keying-defect note warns against reinventing. Not a finding; flagged so it isn't independently re-derived and trusted. **Next action:** the next session (or council pass) must first resolve which bug category B-72 is — (a) addressing/delivery or (b) dispatch-idempotency — before any design or code; that answer determines whether an addressing key is needed at all. Full detail at `.harness/post-phase-8-forward-register.md` B-72 round-4 update. Treat as a fresh start, not a continuation. **Resolved this session:** `test_write_once_survives_immediate_sweep_under_slow_fsync_and_short_ttl` (B-68's own test) — the one CI failure on the post-merge run was confirmed a genuine one-off flake, not a regression: reran clean with zero code changes on the terminating-refresh commit's own CI run. `B-68` CLOSED 2026-07-25 at PR #1103 (prior frontier) — full detail at `.harness/post-phase-8-forward-register.md` B-68 through B-77.

**Round 6 (2026-07-25, PR #1111, doc-only):** before wiring net-position item (1) (delivery-cell construction into `_execute_parallelization`/`_execute_orchestrator_workers`), `advisor()` directed an empirical check of round 5's own unverified close-out caveat first. The check falsifies item (1): direct instrumentation of the real round-3 repro object shows `compute_hitl_uniform_fallback_eligible_run_id` returns `None`, not the branch's own `run_id` — `PeerFanOutResumeState.branches` and `.paused_child_branches` are both empty for a branch that pauses at its own gate before dispatching any child, so there is no carrier anywhere recording that branch's identity for delivery addressing. A second instrumentation pass confirms the mechanism directly (`terminal_dispositions={}` for that branch's ordinal). The fix needs a NEW CP-spec-owned carrier on `PeerFanOutResumeState`/`FanOutResumeState` — Class 1, not Phase-7 impl discretion. Filed `.harness/class_1_fork_b72_gate_ownership_missing_carrier.md`; `B-72`'s register status flips `registered_finding` → `design_substrate_gated` (mirrors the `B-70` discriminator). Both debug instrumentation passes were reverted (doc-only PR — zero source/test diff). Recommends co-designing the new carrier's identity shape with `B-71`'s already-registered keyed-addressing question (both need the same answer).

**Round 7 (2026-07-25, PR #1113, doc-only, pre-authoring grounding):** operator ratified opening the CP spec-leg for `B-72`+`B-71` co-designed via `AskUserQuestion`. Before writing spec text, `advisor()` asked whether `B-71` genuinely shares one identity with this carrier gap. Direct read of `B-71`'s full `close_out`: it only exposes an ALREADY-DISPATCHED child's `run_id` — it doesn't mint an identity for a not-yet-dispatched branch. **The co-design is sequential, not simultaneous** — `B-72`'s carrier identity is designed first, `B-71` can echo it after. A second empirical check of `compose_child_run_id_seed` (`sub_agent_dispatch.py:363-395`, called at `:1078-1098`) found round 5's own tiebreaker option (mint the child's `run_id` deterministically before dispatch) is gated on `subagent_child_recoverable` (engine ∈ `{ESR, WAL, SAVE_POINT, RECONCILER_LOOP}`) — for `PURE_PATTERN_NO_ENGINE`, the exact engine class the round-3 repro's child uses, no deterministic seed exists; a fresh `uuid` is minted only at actual dispatch time. Deterministic pre-dispatch minting alone does not cover the repro's own shape. Full reasoning (including an unresolved, not-yet-verified observation about whether the round-3 repro's SOLE-gate-owning-branch case could be fixed via the uniform fallback without any property-1-compliant external identity at all) at the fork doc's new §7.

**Round 8 (2026-07-25, branch `b72-cp-spec-leg-property-6`, doc-only, pre-merge):** the CP spec-leg is AUTHORED and out-of-family-reviewed to convergence — CP spec v1.107→v1.108 (NEW §1 property 6, closing `B-72` net-position item (1) via internal-only counting-and-deliver, no `hitl_responses` key widening, no separate delivery path) + CP plan v2.43→v2.44 (coverage-matrix-only deferral row) + 2 clearance markers + pointer bumps + register/tension-record sync, across 7 rounds of `just codex-review main`: round 1 fixed a plan-scope omission (delivery-cell construction) + an incomplete canonicalization bundle; round 2 fixed a nested-fan-out identity-collision gap (tree-wide-uniqueness now required, with a collision-witness test owed at impl); round 3 fixed a "counted-but-never-delivered" gap (delivery is now an explicit required outcome, not just counting); round 4 fixed an unauthorized-carrier-existence gap (NEW §1.3a pre-authorizes ONE additive, drop-when-empty/`None` hash-strip-scoped field on `PeerFanOutResumeState`/`FanOutResumeState`, mirroring the `v1.97`/`v1.99` precedent); round 5 fixed (a) this dashboard's own staleness (this paragraph) and (b) a genuine topology-scope overclaim — property 6's text read as topology-agnostic while only `PARALLELIZATION`/`ORCHESTRATOR_WORKERS` are grounded by the round-3 repro (confirmed via direct grep: the repro test uses `TopologyPattern.PARALLELIZATION` exclusively), so NEW §1.2b explicitly scopes property 6 to the two fan-out topologies and names `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF`'s sequential carriers as a distinct, ungrounded, out-of-scope gap for a future arc's own reproduction-first grounding pass; round 6 caught that round 5's own fix under-scoped — `HIERARCHICAL_DELEGATION` was omitted despite recursively reusing `_execute_orchestrator_workers` (U-CP-88/89, confirmed by direct code read of `workflow_driver.py:13745-13849`) and sharing the identical `FanOutResumeState`/`PeerFanOutResumeState` carrier, so §1.1/§1.2b now include it on carrier-identity grounds (amending `_execute_orchestrator_workers`'s own sites already covers every recursion level, no separate call site owed); round 7 fixed 2 [P2]s — the delta's header/revision-shape text contradicted §1.3a's field authorization (fixed, reworded) and the `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` exclusion had no durable register row (fixed — split out as new `B-78`, `registered_finding`, its own close-out recipe: a future arc's reproduction-first grounding pass). Properties 1-5 (v1.106 §1.2) PRESERVED VERBATIM throughout. **MERGED 2026-07-25 at PR #1115** (squash, doc-only, CI green — 8 out-of-family `just codex-review` rounds total to convergence). `B-72` net-position item (1) is spec-closed; register status `open` (impl leg not yet opened). New `B-78` registered (`registered_finding`) for the `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` topology gap property 6 explicitly excludes.

**Round 9 — impl leg LANDED 2026-07-25 at PR #1116 (`B-72` net-position item (1) CLOSED end-to-end).** New `PreDispatchGateOwningBranchResumeState(branch_index, step_id, step_kind, child_workflow_id)` carrier on `PeerFanOutResumeState`/`FanOutResumeState` per §1.3a; `_collect_gate_owning_run_ids` + a new `_collect_pre_dispatch_gate_owning_identities` never-keyable discriminator; `HITLDeliveryCell` construction wired at `_execute_parallelization` and `_execute_orchestrator_workers` (the latter covers `HIERARCHICAL_DELEGATION` via its recursive reuse), gated on sole-unaddressed-membership AND no run-level effect-fence abort in play; a repeated resume carries forward any recovered pre-dispatch gate-owning ordinal withheld by §25.19 warm-up scheduling rather than silently dropping it. **9 out-of-family `just codex-review` rounds to clean convergence:** rounds 1-3 (carrier identity + never-keyable addressability; `B-79` registered for a confirmed-pre-existing HITL-placement material-diff gap shared with the already-shipped LINEAR site); round 4 (2 real P1s — workflow-id/kind material-diff swap; same-level effect-fence-abort HITL-delivery suppression); round 5 (round-4's own kind-changed guard regressed an unchanged `PRE_ACTION`-gated `INFERENCE_STEP` resume — fixed by capturing `step_kind`, not hardcoding it); round 6 (the carry-forward fix above, plus `B-80` registered for a confirmed-pre-existing fence-abort tree-scope gap inherited from the `B-70` impl leg); rounds 7-9 clean. **3-round `merge-gate` 3-lens review to all-approve:** concurrency + spec-conformance approved unchanged across all 3 rounds; test-witness blocked twice on real coverage gaps (round 1: the carry-forward union's inclusion-only coverage, plus zero coverage anywhere for the ORCHESTRATOR_WORKERS mirror site; round 2: the OW mirror's actual `HITLDeliveryCell` *construction* — the delivery/liveness half of the required contract outcome, not merely the counting half) — both fixed with mutation-probed regression tests; the residual guard-hardening gaps (OW's `_resume_body_mismatch` coverage; 2 of 3 exclusion-conjunct arms) registered as `B-81` per `advisor()`'s two-halves-of-one-mechanism framing rather than blocking a third round. Full harness-cp + harness-runtime targeted suites: 161 + 2 passed; ruff + pyright strict clean. Full audit at `.harness/merge-gate-log.md`; full round-by-round detail at `.harness/post-phase-8-forward-register.md` `B-72`/`B-79`/`B-80`/`B-81`. **Next action:** `B-78`'s own reproduction-first grounding pass (construct an `EVALUATOR_OPTIMIZER`/`DECENTRALIZED_HANDOFF` workflow exercising the analogous carriers, confirm or falsify whether property 6's pattern applies) is the most naturally-continuous next step from this arc; a fresh session should also re-run the full §12.1 roadmap audit to confirm no higher-priority forward-register item should be picked instead.

**Round 12 — `B-81` CLOSED IN FULL 2026-07-26 at PR #1119 (step 2/2 of the close-out).** Before starting, grepped both fan-out construction sites (`workflow_driver.py` ~9971-9978 PARALLELIZATION, ~14249-14256 OW) and confirmed the 4-conjunct exclusion filter is byte-identical at both — no production asymmetry, safe to treat as pure test-parity work rather than a Class-1 finding (per `advisor()`'s explicit direction to check this before writing any fixtures). Added the 4 remaining exclusion-conjunct tests (2 per file): each recovers a pre-dispatch gate-owning branch from round 1, delivers its `HITLDeliveryCell` in round 2, and drives the delivered dispatch into either a REAL nested-child pause (`SubAgentChildPausedError`, via a faithful `RuntimeSubAgentDispatcher` double dispatching an actual `execute_workflow` that itself pauses — reusing the file's own pre-existing `_PeerGrandchildDispatcher`/`_GrandchildDispatcher` + child-manifest helpers) or an effect-fence-ambiguous outcome (`EffectFenceAmbiguousUncommittedError` test-local stand-in), proving the branch lands in `paused_child_branches`/`effect_fence_paused_branches` and is excluded from `pre_dispatch_gate_owning_branches`. All 4 mutation-probe-verified (a naive single-marker `str.replace` correctly raised an ambiguity error on the workflow-id-swap-style probe since PARALLELIZATION's and OW's guard text is byte-identical at two locations in the same file — disambiguated via a unique nearby anchor; sharpening appended to `mutation-probe-load-bearing-witness` memory). `just codex-review main` converged clean at round 1 (zero findings) on both this file-pair PR and the prior step-1 PR. `merge-gate` 3-lens all-approve at round 1 on both PRs; the test-witness lens on PR #1119 found a genuine but non-blocking docstring-accuracy nit (the paused-child dispatcher docstrings overclaimed hitting the direct own-dispatch exception handler when real thread-scheduling timing more likely hits the in-flight-cancellation-race handler — both write the identical disposition dict, so mutation-probe validity was unaffected) — fixed inline pre-merge, no re-gate needed. Full harness-cp suite: 1790 passed, 4 skipped; ruff + pyright strict clean. `B-81`'s row is now `closed` in `.harness/forward-register.yaml` (`pr: '#1118, #1119'`). Full audit at `.harness/merge-gate-log.md`; full detail at `.harness/post-phase-8-forward-register.md` `B-81`. **Next action:** run the full §12.1 roadmap audit and ground the remaining 16 `registered_finding` rows fresh before picking the next one to build — per `CLAUDE.md` §12.4.1, do not assume this arc's own priority ordering still holds.

**Round 11 — `B-81` PARTIALLY closed 2026-07-26 at PR #1118 (step 1/2 of the close-out).** Per `advisor()`'s explicit guidance (shipping the completed, independently-valuable slice now rather than blocking on the separable remaining work): ported PARALLELIZATION's 3 existing pre-dispatch gate-owning material-diff tests (kind-changed rejection, workflow-id-swap rejection, unchanged-`PRE_ACTION`-gated-`INFERENCE_STEP` acceptance) to their OW/`_execute_orchestrator_workers` mirror in `test_workflow_driver_fanout_pause.py` — each mutation-probe-verified against the exact OW-specific guard line (`worker_steps[pg.branch_index]`, distinct from PARALLELIZATION's identical-text `steps[pg.branch_index]` guard elsewhere in the same file; a naive single-marker `str.replace` correctly raised an ambiguity error, disambiguated via a unique nearby anchor instead). Docstrings record each test's actual discrimination mechanism (fail_class substring vs. status flip vs. hardcoded-constant regression shape). `just codex-review main` converged clean at round 1 (zero findings); `merge-gate` 3-lens all-approve at round 1 (no fix rounds needed). 67/67 OW tests green (64 baseline + 3 new); ruff + pyright strict clean. `B-81`'s row in `.harness/forward-register.yaml` stays `registered_finding` (not closed) — step (2), the 4 remaining exclusion-conjunct tests (`paused_child_dispositions`/`effect_fence_paused_dispositions` arms at both `PeerFanOutResumeState` and `FanOutResumeState`), is separable follow-on work needing new fixture construction (nested-child-pause + effect-fence-ambiguous plumbing), not a straightforward port. Full audit at `.harness/merge-gate-log.md`. **Next action:** either continue `B-81`'s own step (2), or run the full §12.1 roadmap audit and ground the remaining `registered_finding` rows fresh before picking — per `CLAUDE.md` §12.4.1, do not assume this arc's own priority ordering still holds.

**Round 10 — `B-78` CLOSED 2026-07-26 at PR #1117 (reproduction-first grounding pass + impl leg, same arc).** The reproduction confirmed a defect WORSE than the registered livelock hypothesis: a genuine `HITLPauseRequestedSignal` `BaseException` raised inside `_execute_evaluator_optimizer`/`_execute_decentralized_handoff` was UNCAUGHT (crashed the run), and neither topology constructed a `HITLDeliveryCell` at its own resumed-step dispatch site. Fixed by mirroring the already-cleared LINEAR mechanism at both sites — NO new CP-spec carrier field needed (EO/DH have no branch-ordinal ambiguity), so this was plain Phase-7 impl work despite B-78's registered close-out anticipating a possible spec leg. **6 out-of-family `just codex-review main` rounds to convergence:** rounds 1-3 each fixed a genuine regression this arc's own immediately-prior fix introduced (REJECT-must-be-terminal; the terminal carve-out over-matched non-HITL `rt_fail_class`es; the narrowed carve-out was checked ahead of `cascade_policy=PROCEED`'s PARTIAL-salvage disposition in DH); round 4's finding (an unanswered `HITL_PENDING` resume proceeds to dispatch rather than staying inert) was confirmed via direct grep to be the identical pre-existing shape already at the shipped LINEAR site — folded into `B-79`'s scope (broadened to name this second reachability path across LINEAR/fan-out/EO/DH, not duplicated as a new row) rather than fixed here, per `advisor()`'s "did my fix cause this?" discriminator (it didn't — this is inherited, not introduced); round 5 clean; round 6 (triggered by the `merge-gate` spec-conformance lens's own review, not a fresh `codex-review` invocation) fixed a genuine [P2] audit-completeness gap — the new EO/DH pause handlers omitted the `PAUSE_CAPTURED` CP→IS state-ledger emission LINEAR's own HITL branch fires, fixed with mutation-probed regression tests at both sites. **`merge-gate` 3-lens review, all-approve after 2 fix rounds** (concurrency + test-witness APPROVE unchanged both rounds; spec-conformance BLOCKED twice on stale-carry-text — round 1: CP spec v1.108 §1.2/CP plan v2.44 §5 still asserted the gap "ungrounded... not yet even reproduced," corrected via prose-only `Spec_Control_Plane_v1_109.md`/`Implementation_Plan_Control_Plane_v2_45.md` + 2 clearance markers; round 2: the round-1 fix's own filing footer + `.harness/post-phase-8-forward-register.md`'s B-78 section were themselves left stale, corrected). `advisor()`-confirmed no round 3 was needed — both BLOCKs were narrow mechanical defects, not disagreement ping-pong. Full harness-cp suite: 1783 passed, 4 skipped; ruff + pyright strict clean. Full audit at `.harness/merge-gate-log.md`; full detail at `.harness/post-phase-8-forward-register.md` `B-78`/`B-79`. **Next action:** the buildable frontier at this point is the remaining 17 `registered_finding` rows in `.harness/forward-register.yaml` (including the just-broadened `B-79`, plus `B-80`/`B-81` from the `B-72` arc and others predating it) — per `CLAUDE.md` §12.4.1's no-parking directive, a fresh session should run the full §12.1 roadmap audit, then ground (not just re-read) each `registered_finding` row against current code/spec state before picking the next one to build, rather than assuming this arc's own priority ordering still holds.
