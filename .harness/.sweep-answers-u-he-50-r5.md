# Round-5 sweep answers — u-he-50

- codex_review_wrapper:2530eaf62dd5e1d330efbbb842af033865992095:9d09c811a831:1 (P2,
  abort the launch when start emission fails) — REJECTED with grounds: a measurement
  write must never gate the hard review path (the workspace's standing soft-gate
  discipline); C-HE-27 §3 treats an absent span as legal null, never a blocker; the
  final-round repair path is documented in both carriers and witnessed by
  `test_recipe_terminal_end_failure_repaired_by_documented_command` (r4). Fail-closing
  reviews on a store hiccup would deny the arc's review over telemetry. Disposed
  rejected.
- codex_review_wrapper:2530eaf62dd5e1d330efbbb842af033865992095:31b76eabe283:1 (P2,
  immutable start from an inner-refused attempt contaminates the next span) — REJECTED
  as a registered residual: the session layer cannot observe the in-process admit()
  (the CLI stamps now(); record_phase refuses rewrites), so no recipe-layer mechanism
  can retract the start; the structural fix — emission after admit(), inside the
  process — is the registered B-218 wrapper-internal emitter, and the recipe comment
  now names this residual explicitly (a stated bound, not a blessed accident).
  Disposed rejected.
- codex_review_wrapper:2530eaf62dd5e1d330efbbb842af033865992095:b90a0bc8c2f6:1 (P3,
  flag inherited from environment) — ABSORBED: `_verify_start_failed=0` initialized by
  the recipe; test asserts an exported =1 cannot mute end emission. Disposed accepted.

Class-sibling sweep: recipe-local shell state — the only other recipe variables
(`dest`, `rc`) are assigned before first read on every path (dest `|| exit`, rc from
PIPESTATUS immediately after the pipeline); no other inherited-env hazard. The
oscillation pattern (r1 "don't record partial pairs" → r5 "you left starts
unrecorded/unclosed") is register-and-hold per workspace discipline: the residual is
named in-comment and homed at B-218.

## Grep-hit labels, answered verbatim (fix diff)

- [save/restore pair: two-armed restore] — none added; the init line is
  assignment-once, no restore arm.
- [check-then-act on paths] — none added.
- [bare counts/absolutes in prose] — "cannot see admit()" restates the process
  boundary (admit runs inside codex_review.py, U-HE-49 "enforcer of record");
  "refuses rewrites" cites record_phase (reservations.py:540-545).
- [new retry/timeout constants] — zero.

No findings deferred.
