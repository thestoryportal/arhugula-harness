FINDINGS: 1
- [P1] harness-runtime/tests/integration/test_b162_pause_resume_span_emission_is_wired.py:165-172,182 — the AST filter that is supposed to prove every `capture_pause_snapshot` call site has a matching `_emit_pause_captured` call only matches 3 of the 11 real capture sites, so the assertion at line 182 provides no coverage for 8 of them.
  scenario: the filter requires `node.func.value` to be an `ast.Name` with id `"protocol"` (lines 169-172), but 8 of the 11 sites in workflow_driver.py call `cast(PauseResumeProtocol, protocol).capture_pause_snapshot(...)` (or use `_orch_pause_protocol` / `_reestablish_protocol`), whose `func.value` is a `Call` node, not a bare `Name`; running the exact filter against workflow_driver.py yields `capture_sites = [5276, 5482, 5556]` (3 entries) against `emit_sites` with 11 entries, so `len(emit_sites) >= len(capture_sites)` (11 >= 3) passes trivially. If a future edit deletes the `_emit_pause_captured` call after any of the 8 unmatched sites (e.g. the ORCHESTRATOR_WORKERS or DECENTRALIZED_HANDOFF topology executor at line 12545 or 13370), this test stays green even though `pause.captured` silently stops being emitted for that topology, contradicting the test's own docstring claim that "the pause span must exist for all six topology executors... asserted structurally by AST."
PACK_USED: yes
