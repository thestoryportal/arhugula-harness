FINDINGS: 1
- [P1] harness-runtime/src/harness_runtime/lifecycle/hitl_gate_composer.py:481 — `resolve_escalation_instance_id` mints a non-`None` `B-71` correlation token for any HITL escalation in a fan-out branch, not only the branch's actual pre-dispatch (no-child-yet) escalation.
  scenario: `pre_dispatch_escalation_basis` is derived once, unconditionally, at branch composition (harness-cp/src/harness_cp/workflow_driver.py:8569-8602) and is carried forward to every later step in that branch via `model_copy` (its own docstring at harness-cp/src/harness_cp/workflow_driver_types.py:585-601 states "a branch child never re-derives it"). A branch whose step 0 (`SUB_AGENT_DISPATCH`) already dispatched a child (so it now has a real `child_run_id` and is normally addressable via `hitl_responses`) can still hit a later `PRE_ACTION` or `SUB_AGENT_BOUNDARY` gate that escalates through `_escalate_to_secondary_channel` — the `dispatch()` loop at line 1964 filters out only `VALIDATOR_ESCALATION`, and the durable-async branch at line ~2192-2205 fires for any matching placement keyed only on `cell.synchrony_class`, passing the same `step_context` through. Because `pre_dispatch_escalation_basis` is still set, `resolve_escalation_instance_id` takes arm 2 and mints a token, so `_escalation_operator_surface` (line 547) unconditionally stamps `resolvability: PauseLocationVariant.UNIFORM_FALLBACK_ONLY` and `branch_context: "Escalated from fan-out branch ordinal N..."` on the webhook payload for a pause that is in fact normally resolvable by its `child_run_id` — telling the operator/webhook consumer to use the uniform fallback for a gate that already has a working direct address, and also widening `compose_hitl_action_id`'s key format to this population beyond the pre-dispatch-only population the PR's own byte-compat claim (§0.12) describes.
PACK_USED: yes
