FINDINGS: 1
- [P2] harness-runtime/tests/integration/test_b137_ninety_two_floor_at_the_real_run_venue.py:966 — `test_the_driver_emits_at_every_capture_site`'s AST scanner only matches `protocol.capture_pause_snapshot(` call sites where the receiver is a bare `ast.Name` with id `"protocol"`, so it counts 3 of the 11 real `capture_pause_snapshot` sites in `workflow_driver.py` (the other 8 are `cast(PauseResumeProtocol, protocol).capture_pause_snapshot(...)` or use a differently-named receiver like `_orch_pause_protocol` / `_reestablish_protocol`, all of which put a `Call` node, not an `ast.Name`, at `node.func.value`).
  scenario: a future edit removes `_emit_pause_captured(...)` from any of the 8 `cast(...)`-wrapped capture sites (e.g. `workflow_driver.py:11345`, `12545`, `13370`); `capture_sites` still only ever equals 3 (the docstring-literal-`protocol.` sites), so as long as `emit_sites` stays at or above 3 the assertion `len(emit_sites) >= len(capture_sites)` keeps passing and the regression — a topology executor silently pausing without emitting `pause.captured` — goes undetected despite the test's docstring claiming "the site count is asserted structurally by AST" for "all six topology executors".
PACK_USED: none
