FINDINGS: 1
- [P1] harness-runtime/src/harness_runtime/lifecycle/hitl_gate_composer.py:1713 — `_compose_and_persist_audit`'s `hitl_action_id` computation folds `resolve_escalation_instance_id(step_context, placement.position)` unconditionally, but `harness-cp/src/harness_cp/workflow_driver.py:8569` (and the mirror at :12982) now derives `pre_dispatch_escalation_basis` for every fan-out branch regardless of whether that branch's gate ever escalates ("The BASIS is set for EVERY branch", workflow_driver.py:8596-8597), so `resolve_escalation_instance_id`'s arm 2 mints a non-`None` token for any HITL placement on that branch.
  scenario: a `PARALLELIZATION`/`ORCHESTRATOR_WORKERS` workflow with a HITL gate that resolves synchronously through the primary channel — an ordinary APPROVE/REJECT/EDIT via `ask_user_question_surface.ask` at hitl_gate_composer.py:2237, or an `auto_approved`/`placement_removed` disposition in `_compose_and_persist_audit` — never reaches `_escalate_to_secondary_channel`, yet its `StepExecutionContext.pre_dispatch_escalation_basis` is already set from the unconditional branch derivation; the resulting F2 ledger `idempotency_key`/CP audit `action_id`/`hitl.invocation.audit_ledger_entry_id` span attribute (hitl_gate_composer.py:2419-2422) silently gain a `:<sha256>` suffix for a non-escalation event, changing the `hitl:{parent_action_id}:{position}` key format the audit/dedup layer previously produced for every fan-out-branch HITL gate outcome — a population the new tests (`test_b71_escalation_correlation_carriers_u_cp_102.py`, `test_b71_escalation_token_mint_and_fold_u_rt_155.py`) never exercise.
PACK_USED: none
