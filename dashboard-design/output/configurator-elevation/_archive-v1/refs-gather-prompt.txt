You are a design researcher building a CANDIDATE POOL of Dribbble references. You do NOT make the final selection - downstream specialist agents converge on that. Your job: formulate skill-informed queries and pull a broad, on-brand candidate pool with image URLs.

THE UI WE ARE ELEVATING - read these two files first to ground yourself:
- /Users/robertrhu/Projects/arhugula-v2/dashboard-design/output/configurator-elevation/target/index.html  (markup)
- /Users/robertrhu/.claude/powerline-config/static/style.css  ("Almanac Noir" tokens: warm near-black ground #15120d, bone text #e8e0cf, single amber accent #f0a830, ember #d8542f alerts-only, 2px radius, hairline rules, fonts Big Shoulders Display / IBM Plex Sans / JetBrains Mono)

It is a dark, warm "instrument-ledger" statusline-configurator web app. Static elements being elevated: (1) the "PWRL / Statusline / configurator" masthead brand, (2) three numbered section headers ("Theme & display", "Segments", "Custom data"), (3) a terminal-window live-preview frame with a column-width slider.

STEP 1 - invoke ALL FOUR skills with the Skill tool, one at a time, to absorb their web/dashboard/UI-UX/graphic/userflow design context. USE that context to decide what reference PATTERNS a dark instrument-panel configurator needs:
  - impeccable
  - design-taste-frontend
  - ui-ux-pro-max
  - frontend-design

STEP 2 - call dribbble_status. If not logged in, output {"error":"not_logged_in"} and stop.

STEP 3 - informed by the skills + the actual UI, formulate 4 TARGETED queries (not generic) for the patterns this UI needs - e.g. dev-tool masthead/brand identity, numbered settings/config section hierarchy, terminal/console preview framing, dark warm instrument-panel dashboards. Run dribbble_search (limit 8) per query.

STEP 4 - from results, keep the ~10 most plausibly on-brand candidates (dark / precise / instrument-like - skip bright/playful/light/consumer). Call dribbble_shot on EACH to get its image URL. Do NOT rank or down-select to a final set; this is a POOL for downstream convergence.

STEP 5 - output STRICT JSON ONLY (no prose, no fences):
{"skillsInvoked":["impeccable","design-taste-frontend","ui-ux-pro-max","frontend-design"],"status":"<dribbble_status text>","queries":["..."],"pool":[{"id":1,"title":"...","url":"...","image":"...","element":"brand|step-heads|terminal-preview|overall"}]}
with 8 to 10 entries in "pool", each having a non-empty "image" URL.
